// automatically generated by the FlatBuffers compiler, do not modify


#ifndef FLATBUFFERS_GENERATED_MAIN_H_
#define FLATBUFFERS_GENERATED_MAIN_H_

#include "flatbuffers/flatbuffers.h"

// Ensure the included flatbuffers.h is the same version as when this file was
// generated, otherwise it may not be compatible.
static_assert(FLATBUFFERS_VERSION_MAJOR == 25 &&
              FLATBUFFERS_VERSION_MINOR == 12 &&
              FLATBUFFERS_VERSION_REVISION == 19,
             "Non-compatible flatbuffers version included");

struct CPS;
struct CPSBuilder;

/// Compression algorithm applied to a fixed-length packet stream.
/// Append new values only; never reorder or reuse existing values. Decoders
/// must reject an algorithm value they do not recognize rather than guessing.
enum packetCompressionAlgorithm : uint8_t {
  /// CCSDS 124.0-B-1 POCKET+ — lossless compression of fixed-length
  /// spacecraft housekeeping telemetry.
  packetCompressionAlgorithm_POCKET_PLUS = 0,
  packetCompressionAlgorithm_MIN = packetCompressionAlgorithm_POCKET_PLUS,
  packetCompressionAlgorithm_MAX = packetCompressionAlgorithm_POCKET_PLUS
};

inline const packetCompressionAlgorithm (&EnumValuespacketCompressionAlgorithm())[1] {
  static const packetCompressionAlgorithm values[] = {
    packetCompressionAlgorithm_POCKET_PLUS
  };
  return values;
}

inline const char * const *EnumNamespacketCompressionAlgorithm() {
  static const char * const names[2] = {
    "POCKET_PLUS",
    nullptr
  };
  return names;
}

inline const char *EnumNamepacketCompressionAlgorithm(packetCompressionAlgorithm e) {
  if (::flatbuffers::IsOutRange(e, packetCompressionAlgorithm_POCKET_PLUS, packetCompressionAlgorithm_POCKET_PLUS)) return "";
  const size_t index = static_cast<size_t>(e);
  return EnumNamespacketCompressionAlgorithm()[index];
}

/// Compressed Packet Stream — a run of fixed-length CCSDS packets losslessly
/// compressed as one stateful stream (CCSDS 124.0-B-1 POCKET+). Decoded output
/// is a sequence of PACKET_COUNT packets of PACKET_LENGTH bytes, each of which
/// projects to an $SPP record. This record carries a compressed packet stream
/// only; it is not a general-purpose compression envelope for SDS records.
struct CPS FLATBUFFERS_FINAL_CLASS : private ::flatbuffers::Table {
  typedef CPSBuilder Builder;
  enum FlatBuffersVTableOffset FLATBUFFERS_VTABLE_UNDERLYING_TYPE {
    VT_ALGORITHM = 4,
    VT_PACKET_LENGTH = 6,
    VT_PACKET_COUNT = 8,
    VT_ROBUSTNESS = 10,
    VT_MASK_UPDATE_PERIOD = 12,
    VT_MASK_SEND_PERIOD = 14,
    VT_REFERENCE_PERIOD = 16,
    VT_SELF_SYNCHRONIZING = 18,
    VT_STREAM_ID = 20,
    VT_SEGMENT_INDEX = 22,
    VT_APID = 24,
    VT_SPACECRAFT_ID = 26,
    VT_START_EPOCH = 28,
    VT_STOP_EPOCH = 30,
    VT_COMPRESSED_DATA = 32,
    VT_UNCOMPRESSED_LENGTH = 34,
    VT_UNCOMPRESSED_CRC32 = 36
  };
  /// Compression algorithm used to produce COMPRESSED_DATA.
  packetCompressionAlgorithm ALGORITHM() const {
    return static_cast<packetCompressionAlgorithm>(GetField<uint8_t>(VT_ALGORITHM, 0));
  }
  /// Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
  /// defined only for a fixed packet length; a length change starts a new
  /// stream and therefore a new $CPS record.
  uint16_t PACKET_LENGTH() const {
    return GetField<uint16_t>(VT_PACKET_LENGTH, 0);
  }
  /// Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
  /// this is the only output-size bound a decoder has. The compressed
  /// bitstream alone does not bound the decoded size, and a compression-ratio
  /// guess is unsafe in both directions. Decoders MUST reject a record whose
  /// PACKET_COUNT is zero, and MUST allocate exactly
  /// PACKET_COUNT * PACKET_LENGTH bytes of output.
  uint32_t PACKET_COUNT() const {
    return GetField<uint32_t>(VT_PACKET_COUNT, 0);
  }
  /// POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
  /// a window depth, NOT a retransmission interval — it does not by itself
  /// configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
  /// REFERENCE_PERIOD, which are three independent periods.
  uint8_t ROBUSTNESS() const {
    return GetField<uint8_t>(VT_ROBUSTNESS, 0);
  }
  /// POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
  /// MASK_SEND_PERIOD, and REFERENCE_PERIOD.
  uint16_t MASK_UPDATE_PERIOD() const {
    return GetField<uint16_t>(VT_MASK_UPDATE_PERIOD, 0);
  }
  /// POCKET+ mask send period (ft), in packets — how often the change mask is
  /// transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
  /// REFERENCE_PERIOD.
  uint16_t MASK_SEND_PERIOD() const {
    return GetField<uint16_t>(VT_MASK_SEND_PERIOD, 0);
  }
  /// POCKET+ reference packet retransmission period (rt), in packets.
  /// Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
  uint16_t REFERENCE_PERIOD() const {
    return GetField<uint16_t>(VT_REFERENCE_PERIOD, 0);
  }
  /// True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
  /// ft=1, i.e. it transmits a full reference packet and a full mask, so this
  /// record decodes standalone without its predecessor. There is no
  /// synchronization flag in the POCKET+ bitstream itself: this field is the
  /// producer's assertion about the first packet, and defaults to false.
  bool SELF_SYNCHRONIZING() const {
    return GetField<uint8_t>(VT_SELF_SYNCHRONIZING, 0) != 0;
  }
  /// Producer-defined stream identity. Records sharing STREAM_ID decode in
  /// SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
  /// poisons every subsequent segment up to the next segment with
  /// SELF_SYNCHRONIZING = true.
  const ::flatbuffers::String *STREAM_ID() const {
    return GetPointer<const ::flatbuffers::String *>(VT_STREAM_ID);
  }
  /// Zero-based index of this segment within STREAM_ID.
  uint32_t SEGMENT_INDEX() const {
    return GetField<uint32_t>(VT_SEGMENT_INDEX, 0);
  }
  /// CCSDS Application Process Identifier of the packets, when known.
  uint16_t APID() const {
    return GetField<uint16_t>(VT_APID, 0);
  }
  /// Spacecraft identifier of the packets, when known.
  uint16_t SPACECRAFT_ID() const {
    return GetField<uint16_t>(VT_SPACECRAFT_ID, 0);
  }
  /// UTC epoch of the first packet in the segment, ISO 8601.
  const ::flatbuffers::String *START_EPOCH() const {
    return GetPointer<const ::flatbuffers::String *>(VT_START_EPOCH);
  }
  /// UTC epoch of the last packet in the segment, ISO 8601.
  const ::flatbuffers::String *STOP_EPOCH() const {
    return GetPointer<const ::flatbuffers::String *>(VT_STOP_EPOCH);
  }
  /// The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
  /// packet is individually octet-aligned; padding is per PACKET, never per
  /// stream. Bit-concatenating packets diverges from the reference
  /// implementation and MUST NOT be done.
  const ::flatbuffers::Vector<uint8_t> *COMPRESSED_DATA() const {
    return GetPointer<const ::flatbuffers::Vector<uint8_t> *>(VT_COMPRESSED_DATA);
  }
  /// PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
  /// output length without recomputing the product from a corrupt header.
  uint32_t UNCOMPRESSED_LENGTH() const {
    return GetField<uint32_t>(VT_UNCOMPRESSED_LENGTH, 0);
  }
  /// CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
  uint32_t UNCOMPRESSED_CRC32() const {
    return GetField<uint32_t>(VT_UNCOMPRESSED_CRC32, 0);
  }
  template <bool B = false>
  bool Verify(::flatbuffers::VerifierTemplate<B> &verifier) const {
    return VerifyTableStart(verifier) &&
           VerifyField<uint8_t>(verifier, VT_ALGORITHM, 1) &&
           VerifyField<uint16_t>(verifier, VT_PACKET_LENGTH, 2) &&
           VerifyField<uint32_t>(verifier, VT_PACKET_COUNT, 4) &&
           VerifyField<uint8_t>(verifier, VT_ROBUSTNESS, 1) &&
           VerifyField<uint16_t>(verifier, VT_MASK_UPDATE_PERIOD, 2) &&
           VerifyField<uint16_t>(verifier, VT_MASK_SEND_PERIOD, 2) &&
           VerifyField<uint16_t>(verifier, VT_REFERENCE_PERIOD, 2) &&
           VerifyField<uint8_t>(verifier, VT_SELF_SYNCHRONIZING, 1) &&
           VerifyOffset(verifier, VT_STREAM_ID) &&
           verifier.VerifyString(STREAM_ID()) &&
           VerifyField<uint32_t>(verifier, VT_SEGMENT_INDEX, 4) &&
           VerifyField<uint16_t>(verifier, VT_APID, 2) &&
           VerifyField<uint16_t>(verifier, VT_SPACECRAFT_ID, 2) &&
           VerifyOffset(verifier, VT_START_EPOCH) &&
           verifier.VerifyString(START_EPOCH()) &&
           VerifyOffset(verifier, VT_STOP_EPOCH) &&
           verifier.VerifyString(STOP_EPOCH()) &&
           VerifyOffsetRequired(verifier, VT_COMPRESSED_DATA) &&
           verifier.VerifyVector(COMPRESSED_DATA()) &&
           VerifyField<uint32_t>(verifier, VT_UNCOMPRESSED_LENGTH, 4) &&
           VerifyField<uint32_t>(verifier, VT_UNCOMPRESSED_CRC32, 4) &&
           verifier.EndTable();
  }
};

struct CPSBuilder {
  typedef CPS Table;
  ::flatbuffers::FlatBufferBuilder &fbb_;
  ::flatbuffers::uoffset_t start_;
  void add_ALGORITHM(packetCompressionAlgorithm ALGORITHM) {
    fbb_.AddElement<uint8_t>(CPS::VT_ALGORITHM, static_cast<uint8_t>(ALGORITHM), 0);
  }
  void add_PACKET_LENGTH(uint16_t PACKET_LENGTH) {
    fbb_.AddElement<uint16_t>(CPS::VT_PACKET_LENGTH, PACKET_LENGTH, 0);
  }
  void add_PACKET_COUNT(uint32_t PACKET_COUNT) {
    fbb_.AddElement<uint32_t>(CPS::VT_PACKET_COUNT, PACKET_COUNT, 0);
  }
  void add_ROBUSTNESS(uint8_t ROBUSTNESS) {
    fbb_.AddElement<uint8_t>(CPS::VT_ROBUSTNESS, ROBUSTNESS, 0);
  }
  void add_MASK_UPDATE_PERIOD(uint16_t MASK_UPDATE_PERIOD) {
    fbb_.AddElement<uint16_t>(CPS::VT_MASK_UPDATE_PERIOD, MASK_UPDATE_PERIOD, 0);
  }
  void add_MASK_SEND_PERIOD(uint16_t MASK_SEND_PERIOD) {
    fbb_.AddElement<uint16_t>(CPS::VT_MASK_SEND_PERIOD, MASK_SEND_PERIOD, 0);
  }
  void add_REFERENCE_PERIOD(uint16_t REFERENCE_PERIOD) {
    fbb_.AddElement<uint16_t>(CPS::VT_REFERENCE_PERIOD, REFERENCE_PERIOD, 0);
  }
  void add_SELF_SYNCHRONIZING(bool SELF_SYNCHRONIZING) {
    fbb_.AddElement<uint8_t>(CPS::VT_SELF_SYNCHRONIZING, static_cast<uint8_t>(SELF_SYNCHRONIZING), 0);
  }
  void add_STREAM_ID(::flatbuffers::Offset<::flatbuffers::String> STREAM_ID) {
    fbb_.AddOffset(CPS::VT_STREAM_ID, STREAM_ID);
  }
  void add_SEGMENT_INDEX(uint32_t SEGMENT_INDEX) {
    fbb_.AddElement<uint32_t>(CPS::VT_SEGMENT_INDEX, SEGMENT_INDEX, 0);
  }
  void add_APID(uint16_t APID) {
    fbb_.AddElement<uint16_t>(CPS::VT_APID, APID, 0);
  }
  void add_SPACECRAFT_ID(uint16_t SPACECRAFT_ID) {
    fbb_.AddElement<uint16_t>(CPS::VT_SPACECRAFT_ID, SPACECRAFT_ID, 0);
  }
  void add_START_EPOCH(::flatbuffers::Offset<::flatbuffers::String> START_EPOCH) {
    fbb_.AddOffset(CPS::VT_START_EPOCH, START_EPOCH);
  }
  void add_STOP_EPOCH(::flatbuffers::Offset<::flatbuffers::String> STOP_EPOCH) {
    fbb_.AddOffset(CPS::VT_STOP_EPOCH, STOP_EPOCH);
  }
  void add_COMPRESSED_DATA(::flatbuffers::Offset<::flatbuffers::Vector<uint8_t>> COMPRESSED_DATA) {
    fbb_.AddOffset(CPS::VT_COMPRESSED_DATA, COMPRESSED_DATA);
  }
  void add_UNCOMPRESSED_LENGTH(uint32_t UNCOMPRESSED_LENGTH) {
    fbb_.AddElement<uint32_t>(CPS::VT_UNCOMPRESSED_LENGTH, UNCOMPRESSED_LENGTH, 0);
  }
  void add_UNCOMPRESSED_CRC32(uint32_t UNCOMPRESSED_CRC32) {
    fbb_.AddElement<uint32_t>(CPS::VT_UNCOMPRESSED_CRC32, UNCOMPRESSED_CRC32, 0);
  }
  explicit CPSBuilder(::flatbuffers::FlatBufferBuilder &_fbb)
        : fbb_(_fbb) {
    start_ = fbb_.StartTable();
  }
  ::flatbuffers::Offset<CPS> Finish() {
    const auto end = fbb_.EndTable(start_);
    auto o = ::flatbuffers::Offset<CPS>(end);
    fbb_.Required(o, CPS::VT_COMPRESSED_DATA);
    return o;
  }
};

inline ::flatbuffers::Offset<CPS> CreateCPS(
    ::flatbuffers::FlatBufferBuilder &_fbb,
    packetCompressionAlgorithm ALGORITHM = packetCompressionAlgorithm_POCKET_PLUS,
    uint16_t PACKET_LENGTH = 0,
    uint32_t PACKET_COUNT = 0,
    uint8_t ROBUSTNESS = 0,
    uint16_t MASK_UPDATE_PERIOD = 0,
    uint16_t MASK_SEND_PERIOD = 0,
    uint16_t REFERENCE_PERIOD = 0,
    bool SELF_SYNCHRONIZING = false,
    ::flatbuffers::Offset<::flatbuffers::String> STREAM_ID = 0,
    uint32_t SEGMENT_INDEX = 0,
    uint16_t APID = 0,
    uint16_t SPACECRAFT_ID = 0,
    ::flatbuffers::Offset<::flatbuffers::String> START_EPOCH = 0,
    ::flatbuffers::Offset<::flatbuffers::String> STOP_EPOCH = 0,
    ::flatbuffers::Offset<::flatbuffers::Vector<uint8_t>> COMPRESSED_DATA = 0,
    uint32_t UNCOMPRESSED_LENGTH = 0,
    uint32_t UNCOMPRESSED_CRC32 = 0) {
  CPSBuilder builder_(_fbb);
  builder_.add_UNCOMPRESSED_CRC32(UNCOMPRESSED_CRC32);
  builder_.add_UNCOMPRESSED_LENGTH(UNCOMPRESSED_LENGTH);
  builder_.add_COMPRESSED_DATA(COMPRESSED_DATA);
  builder_.add_STOP_EPOCH(STOP_EPOCH);
  builder_.add_START_EPOCH(START_EPOCH);
  builder_.add_SEGMENT_INDEX(SEGMENT_INDEX);
  builder_.add_STREAM_ID(STREAM_ID);
  builder_.add_PACKET_COUNT(PACKET_COUNT);
  builder_.add_SPACECRAFT_ID(SPACECRAFT_ID);
  builder_.add_APID(APID);
  builder_.add_REFERENCE_PERIOD(REFERENCE_PERIOD);
  builder_.add_MASK_SEND_PERIOD(MASK_SEND_PERIOD);
  builder_.add_MASK_UPDATE_PERIOD(MASK_UPDATE_PERIOD);
  builder_.add_PACKET_LENGTH(PACKET_LENGTH);
  builder_.add_SELF_SYNCHRONIZING(SELF_SYNCHRONIZING);
  builder_.add_ROBUSTNESS(ROBUSTNESS);
  builder_.add_ALGORITHM(ALGORITHM);
  return builder_.Finish();
}

inline ::flatbuffers::Offset<CPS> CreateCPSDirect(
    ::flatbuffers::FlatBufferBuilder &_fbb,
    packetCompressionAlgorithm ALGORITHM = packetCompressionAlgorithm_POCKET_PLUS,
    uint16_t PACKET_LENGTH = 0,
    uint32_t PACKET_COUNT = 0,
    uint8_t ROBUSTNESS = 0,
    uint16_t MASK_UPDATE_PERIOD = 0,
    uint16_t MASK_SEND_PERIOD = 0,
    uint16_t REFERENCE_PERIOD = 0,
    bool SELF_SYNCHRONIZING = false,
    const char *STREAM_ID = nullptr,
    uint32_t SEGMENT_INDEX = 0,
    uint16_t APID = 0,
    uint16_t SPACECRAFT_ID = 0,
    const char *START_EPOCH = nullptr,
    const char *STOP_EPOCH = nullptr,
    const std::vector<uint8_t> *COMPRESSED_DATA = nullptr,
    uint32_t UNCOMPRESSED_LENGTH = 0,
    uint32_t UNCOMPRESSED_CRC32 = 0) {
  auto STREAM_ID__ = STREAM_ID ? _fbb.CreateString(STREAM_ID) : 0;
  auto START_EPOCH__ = START_EPOCH ? _fbb.CreateString(START_EPOCH) : 0;
  auto STOP_EPOCH__ = STOP_EPOCH ? _fbb.CreateString(STOP_EPOCH) : 0;
  auto COMPRESSED_DATA__ = COMPRESSED_DATA ? _fbb.CreateVector<uint8_t>(*COMPRESSED_DATA) : 0;
  return CreateCPS(
      _fbb,
      ALGORITHM,
      PACKET_LENGTH,
      PACKET_COUNT,
      ROBUSTNESS,
      MASK_UPDATE_PERIOD,
      MASK_SEND_PERIOD,
      REFERENCE_PERIOD,
      SELF_SYNCHRONIZING,
      STREAM_ID__,
      SEGMENT_INDEX,
      APID,
      SPACECRAFT_ID,
      START_EPOCH__,
      STOP_EPOCH__,
      COMPRESSED_DATA__,
      UNCOMPRESSED_LENGTH,
      UNCOMPRESSED_CRC32);
}

inline const CPS *GetCPS(const void *buf) {
  return ::flatbuffers::GetRoot<CPS>(buf);
}

inline const CPS *GetSizePrefixedCPS(const void *buf) {
  return ::flatbuffers::GetSizePrefixedRoot<CPS>(buf);
}

inline const char *CPSIdentifier() {
  return "$CPS";
}

inline bool CPSBufferHasIdentifier(const void *buf) {
  return ::flatbuffers::BufferHasIdentifier(
      buf, CPSIdentifier());
}

inline bool SizePrefixedCPSBufferHasIdentifier(const void *buf) {
  return ::flatbuffers::BufferHasIdentifier(
      buf, CPSIdentifier(), true);
}

template <bool B = false>
inline bool VerifyCPSBuffer(
    ::flatbuffers::VerifierTemplate<B> &verifier) {
  return verifier.template VerifyBuffer<CPS>(CPSIdentifier());
}

template <bool B = false>
inline bool VerifySizePrefixedCPSBuffer(
    ::flatbuffers::VerifierTemplate<B> &verifier) {
  return verifier.template VerifySizePrefixedBuffer<CPS>(CPSIdentifier());
}

inline void FinishCPSBuffer(
    ::flatbuffers::FlatBufferBuilder &fbb,
    ::flatbuffers::Offset<CPS> root) {
  fbb.Finish(root, CPSIdentifier());
}

inline void FinishSizePrefixedCPSBuffer(
    ::flatbuffers::FlatBufferBuilder &fbb,
    ::flatbuffers::Offset<CPS> root) {
  fbb.FinishSizePrefixed(root, CPSIdentifier());
}

#endif  // FLATBUFFERS_GENERATED_MAIN_H_
