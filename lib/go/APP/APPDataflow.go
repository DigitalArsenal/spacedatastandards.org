// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package APP

import (
	"bytes"
	flatbuffers "github.com/google/flatbuffers/go"
)

/// One unit of the page's data contract: a named flow describing what data
/// enters or leaves the running page, the SDS standard it carries, how it is
/// transported, and — when applicable — the loaded module method port bound
/// to it. Standards-only rule: every flow payload is an SDS record (a
/// canonical size-prefixed FlatBuffer) or a CID pointing at one; this table
/// defines no data shapes of its own, it only references an existing
/// spacedatastandards.org schema by its established code. Locators are
/// content-addressed and IPFS-first.
type APPDataflow struct {
	_tab flatbuffers.Table
}

func GetRootAsAPPDataflow(buf []byte, offset flatbuffers.UOffsetT) *APPDataflow {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &APPDataflow{}
	x.Init(buf, n+offset)
	return x
}

func FinishAPPDataflowBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsAPPDataflow(buf []byte, offset flatbuffers.UOffsetT) *APPDataflow {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &APPDataflow{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedAPPDataflowBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *APPDataflow) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *APPDataflow) Table() flatbuffers.Table {
	return rcv._tab
}

/// App-local stable name for this flow. Required, unique within the
/// manifest.
func (rcv *APPDataflow) NAME() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APPDataflow) Name() []byte {
	return rcv.NAME()
}

/// App-local stable name for this flow. Required, unique within the
/// manifest.
func APPDataflowKeyCompare(o1, o2 flatbuffers.UOffsetT, buf []byte) bool {
	obj1 := &APPDataflow{}
	obj2 := &APPDataflow{}
	obj1.Init(buf, flatbuffers.UOffsetT(len(buf))-o1)
	obj2.Init(buf, flatbuffers.UOffsetT(len(buf))-o2)
	return string(obj1.NAME()) < string(obj2.NAME())
}

func (rcv *APPDataflow) LookupByKey(key string, vectorLocation flatbuffers.UOffsetT, buf []byte) bool {
	span := flatbuffers.GetUOffsetT(buf[vectorLocation-4:])
	start := flatbuffers.UOffsetT(0)
	bKey := []byte(key)
	for span != 0 {
		middle := span / 2
		tableOffset := flatbuffers.GetIndirectOffset(buf, vectorLocation+4*(start+middle))
		obj := &APPDataflow{}
		obj.Init(buf, tableOffset)
		comp := bytes.Compare(obj.NAME(), bKey)
		if comp > 0 {
			span = middle
		} else if comp < 0 {
			middle += 1
			start += middle
			span -= middle
		} else {
			rcv.Init(buf, tableOffset)
			return true
		}
	}
	return false
}

/// Which way the payload crosses the page boundary at runtime.
func (rcv *APPDataflow) DIRECTION() appFlowDirection {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return appFlowDirection(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *APPDataflow) Direction() appFlowDirection {
	return rcv.DIRECTION()
}

/// Which way the payload crosses the page boundary at runtime.
func (rcv *APPDataflow) MutateDIRECTION(n appFlowDirection) bool {
	return rcv._tab.MutateByteSlot(6, byte(n))
}

func (rcv *APPDataflow) MutateDirection(n appFlowDirection) bool {
	return rcv.MutateDIRECTION(n)
}

/// Existing SDS schema code carried by this flow, for example OMM, OEM, or
/// PNM. Required. Mirrors APPDataRef.SDS_TYPE but named for the standard
/// the flow carries; the app defines no schema of its own.
func (rcv *APPDataflow) SDS_SCHEMA() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APPDataflow) SdsSchema() []byte {
	return rcv.SDS_SCHEMA()
}

/// Existing SDS schema code carried by this flow, for example OMM, OEM, or
/// PNM. Required. Mirrors APPDataRef.SDS_TYPE but named for the standard
/// the flow carries; the app defines no schema of its own.
/// Transport that moves the payload. Defaults to IPFS_CID per the
/// content-addressed, IPFS-first rule.
func (rcv *APPDataflow) TRANSPORT() appFlowTransport {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return appFlowTransport(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *APPDataflow) Transport() appFlowTransport {
	return rcv.TRANSPORT()
}

/// Transport that moves the payload. Defaults to IPFS_CID per the
/// content-addressed, IPFS-first rule.
func (rcv *APPDataflow) MutateTRANSPORT(n appFlowTransport) bool {
	return rcv._tab.MutateByteSlot(10, byte(n))
}

func (rcv *APPDataflow) MutateTransport(n appFlowTransport) bool {
	return rcv.MutateTRANSPORT(n)
}

/// Where to fetch or reach the payload, interpreted per TRANSPORT: a CID
/// for IPFS_CID, a gossip topic name for PUBSUB_TOPIC, or a same-origin
/// route template for GATEWAY_ROUTE.
func (rcv *APPDataflow) LOCATOR() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APPDataflow) Locator() []byte {
	return rcv.LOCATOR()
}

/// Where to fetch or reach the payload, interpreted per TRANSPORT: a CID
/// for IPFS_CID, a gossip topic name for PUBSUB_TOPIC, or a same-origin
/// route template for GATEWAY_ROUTE.
/// When present, must equal an APPModuleRef.ID in the same manifest — the
/// loaded module that produces or consumes this flow. Binds the flow to a
/// specific module method port together with METHOD_ID and PORT_ID.
func (rcv *APPDataflow) MODULE_ID() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APPDataflow) ModuleId() []byte {
	return rcv.MODULE_ID()
}

/// When present, must equal an APPModuleRef.ID in the same manifest — the
/// loaded module that produces or consumes this flow. Binds the flow to a
/// specific module method port together with METHOD_ID and PORT_ID.
/// When present, the PLG.PLGMethodManifest.METHOD_ID on MODULE_ID that
/// this flow is bound to.
func (rcv *APPDataflow) METHOD_ID() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APPDataflow) MethodId() []byte {
	return rcv.METHOD_ID()
}

/// When present, the PLG.PLGMethodManifest.METHOD_ID on MODULE_ID that
/// this flow is bound to.
/// When present, the PLG.PLGPortManifest.PORT_ID on METHOD_ID that this
/// flow is bound to.
func (rcv *APPDataflow) PORT_ID() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APPDataflow) PortId() []byte {
	return rcv.PORT_ID()
}

/// When present, the PLG.PLGPortManifest.PORT_ID on METHOD_ID that this
/// flow is bound to.
/// String/compression form of the payload as it crosses the channel,
/// reusing the page content-encoding vocabulary. For flows carrying
/// canonical SDS FlatBuffer bytes (or a CID string), UTF8 denotes the raw
/// bytes/string with no extra wrapper; BASE64, BASE64_GZIP, and
/// BASE64_BROTLI denote a base64 text wrapper (optionally compressed)
/// applied when the channel is text-only.
func (rcv *APPDataflow) CONTENT_ENCODING() appContentEncoding {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(20))
	if o != 0 {
		return appContentEncoding(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *APPDataflow) ContentEncoding() appContentEncoding {
	return rcv.CONTENT_ENCODING()
}

/// String/compression form of the payload as it crosses the channel,
/// reusing the page content-encoding vocabulary. For flows carrying
/// canonical SDS FlatBuffer bytes (or a CID string), UTF8 denotes the raw
/// bytes/string with no extra wrapper; BASE64, BASE64_GZIP, and
/// BASE64_BROTLI denote a base64 text wrapper (optionally compressed)
/// applied when the channel is text-only.
func (rcv *APPDataflow) MutateCONTENT_ENCODING(n appContentEncoding) bool {
	return rcv._tab.MutateByteSlot(20, byte(n))
}

func (rcv *APPDataflow) MutateContentEncoding(n appContentEncoding) bool {
	return rcv.MutateCONTENT_ENCODING(n)
}

/// Human-readable summary.
func (rcv *APPDataflow) DESCRIPTION() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(22))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APPDataflow) Description() []byte {
	return rcv.DESCRIPTION()
}

/// Human-readable summary.
func APPDataflowStart(builder *flatbuffers.Builder) {
	builder.StartObject(10)
}
func APPDataflowAddNAME(builder *flatbuffers.Builder, NAME flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(0, flatbuffers.UOffsetT(NAME), 0)
}
func APPDataflowAddName(builder *flatbuffers.Builder, NAME flatbuffers.UOffsetT) {
	APPDataflowAddNAME(builder, NAME)
}
func APPDataflowAddDIRECTION(builder *flatbuffers.Builder, DIRECTION appFlowDirection) {
	builder.PrependByteSlot(1, byte(DIRECTION), 0)
}
func APPDataflowAddDirection(builder *flatbuffers.Builder, DIRECTION appFlowDirection) {
	APPDataflowAddDIRECTION(builder, DIRECTION)
}
func APPDataflowAddSDS_SCHEMA(builder *flatbuffers.Builder, SDS_SCHEMA flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(2, flatbuffers.UOffsetT(SDS_SCHEMA), 0)
}
func APPDataflowAddSdsSchema(builder *flatbuffers.Builder, SDS_SCHEMA flatbuffers.UOffsetT) {
	APPDataflowAddSDS_SCHEMA(builder, SDS_SCHEMA)
}
func APPDataflowAddTRANSPORT(builder *flatbuffers.Builder, TRANSPORT appFlowTransport) {
	builder.PrependByteSlot(3, byte(TRANSPORT), 0)
}
func APPDataflowAddTransport(builder *flatbuffers.Builder, TRANSPORT appFlowTransport) {
	APPDataflowAddTRANSPORT(builder, TRANSPORT)
}
func APPDataflowAddLOCATOR(builder *flatbuffers.Builder, LOCATOR flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(4, flatbuffers.UOffsetT(LOCATOR), 0)
}
func APPDataflowAddLocator(builder *flatbuffers.Builder, LOCATOR flatbuffers.UOffsetT) {
	APPDataflowAddLOCATOR(builder, LOCATOR)
}
func APPDataflowAddMODULE_ID(builder *flatbuffers.Builder, MODULE_ID flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(5, flatbuffers.UOffsetT(MODULE_ID), 0)
}
func APPDataflowAddModuleId(builder *flatbuffers.Builder, MODULE_ID flatbuffers.UOffsetT) {
	APPDataflowAddMODULE_ID(builder, MODULE_ID)
}
func APPDataflowAddMETHOD_ID(builder *flatbuffers.Builder, METHOD_ID flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(6, flatbuffers.UOffsetT(METHOD_ID), 0)
}
func APPDataflowAddMethodId(builder *flatbuffers.Builder, METHOD_ID flatbuffers.UOffsetT) {
	APPDataflowAddMETHOD_ID(builder, METHOD_ID)
}
func APPDataflowAddPORT_ID(builder *flatbuffers.Builder, PORT_ID flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(7, flatbuffers.UOffsetT(PORT_ID), 0)
}
func APPDataflowAddPortId(builder *flatbuffers.Builder, PORT_ID flatbuffers.UOffsetT) {
	APPDataflowAddPORT_ID(builder, PORT_ID)
}
func APPDataflowAddCONTENT_ENCODING(builder *flatbuffers.Builder, CONTENT_ENCODING appContentEncoding) {
	builder.PrependByteSlot(8, byte(CONTENT_ENCODING), 0)
}
func APPDataflowAddContentEncoding(builder *flatbuffers.Builder, CONTENT_ENCODING appContentEncoding) {
	APPDataflowAddCONTENT_ENCODING(builder, CONTENT_ENCODING)
}
func APPDataflowAddDESCRIPTION(builder *flatbuffers.Builder, DESCRIPTION flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(9, flatbuffers.UOffsetT(DESCRIPTION), 0)
}
func APPDataflowAddDescription(builder *flatbuffers.Builder, DESCRIPTION flatbuffers.UOffsetT) {
	APPDataflowAddDESCRIPTION(builder, DESCRIPTION)
}
func APPDataflowEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
