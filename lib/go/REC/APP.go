// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package APP

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

/// Application Package Manifest — one launchable app.
type APP struct {
	_tab flatbuffers.Table
}

const APPIdentifier = "$APP"

func GetRootAsAPP(buf []byte, offset flatbuffers.UOffsetT) *APP {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &APP{}
	x.Init(buf, n+offset)
	return x
}

func FinishAPPBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	identifierBytes := []byte(APPIdentifier)
	builder.FinishWithFileIdentifier(offset, identifierBytes)
}

func APPBufferHasIdentifier(buf []byte) bool {
	return flatbuffers.BufferHasIdentifier(buf, APPIdentifier)
}

func GetSizePrefixedRootAsAPP(buf []byte, offset flatbuffers.UOffsetT) *APP {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &APP{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedAPPBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	identifierBytes := []byte(APPIdentifier)
	builder.FinishSizePrefixedWithFileIdentifier(offset, identifierBytes)
}

func SizePrefixedAPPBufferHasIdentifier(buf []byte) bool {
	return flatbuffers.SizePrefixedBufferHasIdentifier(buf, APPIdentifier)
}

func (rcv *APP) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *APP) Table() flatbuffers.Table {
	return rcv._tab
}

/// Stable app identity, unique per publisher. Required.
func (rcv *APP) ID() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APP) Id() []byte {
	return rcv.ID()
}

/// Stable app identity, unique per publisher. Required.
/// Display name.
func (rcv *APP) NAME() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APP) Name() []byte {
	return rcv.NAME()
}

/// Display name.
/// SemVer 2.0.0 app version.
func (rcv *APP) VERSION() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APP) Version() []byte {
	return rcv.VERSION()
}

/// SemVer 2.0.0 app version.
/// Human-readable summary.
func (rcv *APP) DESCRIPTION() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APP) Description() []byte {
	return rcv.DESCRIPTION()
}

/// Human-readable summary.
/// Member WASM modules. Referential integrity: every MODULE_ID and every
/// MODULE-kind REF elsewhere in the manifest must resolve into this list.
func (rcv *APP) MODULES(obj *APPModuleRef, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		if obj == nil {
			obj = new(APPModuleRef)
		}
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *APP) Modules(obj *APPModuleRef, j int) bool {
	return rcv.MODULES(obj, j)
}

func (rcv *APP) MODULESByKey(obj *APPModuleRef, key string) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *APP) ModulesByKey(obj *APPModuleRef, key string) bool {
	return rcv.MODULESByKey(obj, key)
}

func (rcv *APP) MODULESLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *APP) ModulesLength() int {
	return rcv.MODULESLength()
}

/// Member WASM modules. Referential integrity: every MODULE_ID and every
/// MODULE-kind REF elsewhere in the manifest must resolve into this list.
/// SDS record types produced and consumed.
func (rcv *APP) DATA(obj *APPDataRef, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		if obj == nil {
			obj = new(APPDataRef)
		}
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *APP) Data(obj *APPDataRef, j int) bool {
	return rcv.DATA(obj, j)
}

func (rcv *APP) DATAByKey(obj *APPDataRef, key string) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *APP) DataByKey(obj *APPDataRef, key string) bool {
	return rcv.DATAByKey(obj, key)
}

func (rcv *APP) DATALength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *APP) DataLength() int {
	return rcv.DATALength()
}

/// SDS record types produced and consumed.
/// Upstream sources depended on.
func (rcv *APP) SOURCES(obj *APPSourceRef, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		if obj == nil {
			obj = new(APPSourceRef)
		}
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *APP) Sources(obj *APPSourceRef, j int) bool {
	return rcv.SOURCES(obj, j)
}

func (rcv *APP) SOURCESByKey(obj *APPSourceRef, key string) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *APP) SourcesByKey(obj *APPSourceRef, key string) bool {
	return rcv.SOURCESByKey(obj, key)
}

func (rcv *APP) SOURCESLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *APP) SourcesLength() int {
	return rcv.SOURCESLength()
}

/// Upstream sources depended on.
/// UI pages. Exactly one entry page when nonempty.
func (rcv *APP) UI(obj *APPUIPage, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		if obj == nil {
			obj = new(APPUIPage)
		}
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *APP) Ui(obj *APPUIPage, j int) bool {
	return rcv.UI(obj, j)
}

func (rcv *APP) UIByKey(obj *APPUIPage, key string) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *APP) UiByKey(obj *APPUIPage, key string) bool {
	return rcv.UIByKey(obj, key)
}

func (rcv *APP) UILength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *APP) UiLength() int {
	return rcv.UILength()
}

/// UI pages. Exactly one entry page when nonempty.
/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the manifest was created.
func (rcv *APP) CREATED_AT() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(20))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APP) CreatedAt() []byte {
	return rcv.CREATED_AT()
}

/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the manifest was created.
/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the manifest was last updated.
func (rcv *APP) UPDATED_AT() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(22))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APP) UpdatedAt() []byte {
	return rcv.UPDATED_AT()
}

/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the manifest was last updated.
/// The page's declarative data contract: what data enters and leaves the
/// running page and how. Referential integrity: every MODULE_ID here must
/// resolve into MODULES, and each MODULE_ID/METHOD_ID/PORT_ID triple must
/// name a method port advertised by that module's PLG manifest.
func (rcv *APP) DATAFLOW(obj *APPDataflow, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(24))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		if obj == nil {
			obj = new(APPDataflow)
		}
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *APP) Dataflow(obj *APPDataflow, j int) bool {
	return rcv.DATAFLOW(obj, j)
}

func (rcv *APP) DATAFLOWByKey(obj *APPDataflow, key string) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(24))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *APP) DataflowByKey(obj *APPDataflow, key string) bool {
	return rcv.DATAFLOWByKey(obj, key)
}

func (rcv *APP) DATAFLOWLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(24))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *APP) DataflowLength() int {
	return rcv.DATAFLOWLength()
}

/// The page's declarative data contract: what data enters and leaves the
/// running page and how. Referential integrity: every MODULE_ID here must
/// resolve into MODULES, and each MODULE_ID/METHOD_ID/PORT_ID triple must
/// name a method port advertised by that module's PLG manifest.
/// App-wide runtime class, reusing appRuntimeTarget. Lets a pulled manifest
/// self-describe where the app as a whole is meant to run, instead of that
/// classification being supplied externally at install time. This is the
/// app-level DEFAULT/DECLARATION only: an individual APPModuleRef.
/// RUNTIME_TARGET still governs where that specific member module loads and
/// may specialize away from RUNTIME_CLASS (for example a NODE-class app
/// with one PAGE-capable module). Defaults to NODE to preserve the prior
/// node-only assumption of manifests written before this field existed.
func (rcv *APP) RUNTIME_CLASS() appRuntimeTarget {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(26))
	if o != 0 {
		return appRuntimeTarget(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *APP) RuntimeClass() appRuntimeTarget {
	return rcv.RUNTIME_CLASS()
}

/// App-wide runtime class, reusing appRuntimeTarget. Lets a pulled manifest
/// self-describe where the app as a whole is meant to run, instead of that
/// classification being supplied externally at install time. This is the
/// app-level DEFAULT/DECLARATION only: an individual APPModuleRef.
/// RUNTIME_TARGET still governs where that specific member module loads and
/// may specialize away from RUNTIME_CLASS (for example a NODE-class app
/// with one PAGE-capable module). Defaults to NODE to preserve the prior
/// node-only assumption of manifests written before this field existed.
func (rcv *APP) MutateRUNTIME_CLASS(n appRuntimeTarget) bool {
	return rcv._tab.MutateByteSlot(26, byte(n))
}

func (rcv *APP) MutateRuntimeClass(n appRuntimeTarget) bool {
	return rcv.MutateRUNTIME_CLASS(n)
}

/// The one ratified $CCT category this app is shelved under, using the same
/// vocabulary and the same semantics as PLG.PRIMARY_CATEGORY, so a storefront
/// or library shelf holds apps and modules together without translating
/// between two classification schemes. RUNTIME_CLASS says WHERE an app runs;
/// PRIMARY_CATEGORY says WHAT IT DOES. They are independent: a NODE-class app
/// and a PAGE-class app can share a category.
/// UNSPECIFIED means the publisher did not classify the app; a consumer
/// renders it ungrouped and never infers a class.
func (rcv *APP) PRIMARY_CATEGORY() capabilityClass {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(28))
	if o != 0 {
		return capabilityClass(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *APP) PrimaryCategory() capabilityClass {
	return rcv.PRIMARY_CATEGORY()
}

/// The one ratified $CCT category this app is shelved under, using the same
/// vocabulary and the same semantics as PLG.PRIMARY_CATEGORY, so a storefront
/// or library shelf holds apps and modules together without translating
/// between two classification schemes. RUNTIME_CLASS says WHERE an app runs;
/// PRIMARY_CATEGORY says WHAT IT DOES. They are independent: a NODE-class app
/// and a PAGE-class app can share a category.
/// UNSPECIFIED means the publisher did not classify the app; a consumer
/// renders it ungrouped and never infers a class.
func (rcv *APP) MutatePRIMARY_CATEGORY(n capabilityClass) bool {
	return rcv._tab.MutateByteSlot(28, byte(n))
}

func (rcv *APP) MutatePrimaryCategory(n capabilityClass) bool {
	return rcv.MutatePRIMARY_CATEGORY(n)
}

/// Every ratified $CCT category this app belongs to, for browse, filter and
/// per-category counting. An app MAY carry several. If nonempty it MUST
/// include PRIMARY_CATEGORY. Codes MUST NOT repeat.
func (rcv *APP) CATEGORIES(j int) capabilityClass {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(30))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return capabilityClass(rcv._tab.GetByte(a + flatbuffers.UOffsetT(j*1)))
	}
	return 0
}

func (rcv *APP) Categories(j int) capabilityClass {
	return rcv.CATEGORIES(j)
}

func (rcv *APP) CATEGORIESLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(30))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *APP) CategoriesLength() int {
	return rcv.CATEGORIESLength()
}

func (rcv *APP) CATEGORIESBytes() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(30))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *APP) CategoriesBytes() []byte {
	return rcv.CATEGORIESBytes()
}

/// Every ratified $CCT category this app belongs to, for browse, filter and
/// per-category counting. An app MAY carry several. If nonempty it MUST
/// include PRIMARY_CATEGORY. Codes MUST NOT repeat.
func (rcv *APP) MutateCATEGORIES(j int, n capabilityClass) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(30))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.MutateByte(a+flatbuffers.UOffsetT(j*1), byte(n))
	}
	return false
}

func (rcv *APP) MutateCategories(j int, n capabilityClass) bool {
	return rcv.MutateCATEGORIES(j, n)
}

func APPStart(builder *flatbuffers.Builder) {
	builder.StartObject(14)
}
func APPAddID(builder *flatbuffers.Builder, ID flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(0, flatbuffers.UOffsetT(ID), 0)
}
func APPAddId(builder *flatbuffers.Builder, ID flatbuffers.UOffsetT) {
	APPAddID(builder, ID)
}
func APPAddNAME(builder *flatbuffers.Builder, NAME flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(1, flatbuffers.UOffsetT(NAME), 0)
}
func APPAddName(builder *flatbuffers.Builder, NAME flatbuffers.UOffsetT) {
	APPAddNAME(builder, NAME)
}
func APPAddVERSION(builder *flatbuffers.Builder, VERSION flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(2, flatbuffers.UOffsetT(VERSION), 0)
}
func APPAddVersion(builder *flatbuffers.Builder, VERSION flatbuffers.UOffsetT) {
	APPAddVERSION(builder, VERSION)
}
func APPAddDESCRIPTION(builder *flatbuffers.Builder, DESCRIPTION flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(3, flatbuffers.UOffsetT(DESCRIPTION), 0)
}
func APPAddDescription(builder *flatbuffers.Builder, DESCRIPTION flatbuffers.UOffsetT) {
	APPAddDESCRIPTION(builder, DESCRIPTION)
}
func APPAddMODULES(builder *flatbuffers.Builder, MODULES flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(4, flatbuffers.UOffsetT(MODULES), 0)
}
func APPAddModules(builder *flatbuffers.Builder, MODULES flatbuffers.UOffsetT) {
	APPAddMODULES(builder, MODULES)
}
func APPStartMODULESVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func APPStartModulesVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return APPStartMODULESVector(builder, numElems)
}
func APPAddDATA(builder *flatbuffers.Builder, DATA flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(5, flatbuffers.UOffsetT(DATA), 0)
}
func APPAddData(builder *flatbuffers.Builder, DATA flatbuffers.UOffsetT) {
	APPAddDATA(builder, DATA)
}
func APPStartDATAVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func APPStartDataVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return APPStartDATAVector(builder, numElems)
}
func APPAddSOURCES(builder *flatbuffers.Builder, SOURCES flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(6, flatbuffers.UOffsetT(SOURCES), 0)
}
func APPAddSources(builder *flatbuffers.Builder, SOURCES flatbuffers.UOffsetT) {
	APPAddSOURCES(builder, SOURCES)
}
func APPStartSOURCESVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func APPStartSourcesVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return APPStartSOURCESVector(builder, numElems)
}
func APPAddUI(builder *flatbuffers.Builder, UI flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(7, flatbuffers.UOffsetT(UI), 0)
}
func APPAddUi(builder *flatbuffers.Builder, UI flatbuffers.UOffsetT) {
	APPAddUI(builder, UI)
}
func APPStartUIVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func APPStartUiVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return APPStartUIVector(builder, numElems)
}
func APPAddCREATED_AT(builder *flatbuffers.Builder, CREATED_AT flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(8, flatbuffers.UOffsetT(CREATED_AT), 0)
}
func APPAddCreatedAt(builder *flatbuffers.Builder, CREATED_AT flatbuffers.UOffsetT) {
	APPAddCREATED_AT(builder, CREATED_AT)
}
func APPAddUPDATED_AT(builder *flatbuffers.Builder, UPDATED_AT flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(9, flatbuffers.UOffsetT(UPDATED_AT), 0)
}
func APPAddUpdatedAt(builder *flatbuffers.Builder, UPDATED_AT flatbuffers.UOffsetT) {
	APPAddUPDATED_AT(builder, UPDATED_AT)
}
func APPAddDATAFLOW(builder *flatbuffers.Builder, DATAFLOW flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(10, flatbuffers.UOffsetT(DATAFLOW), 0)
}
func APPAddDataflow(builder *flatbuffers.Builder, DATAFLOW flatbuffers.UOffsetT) {
	APPAddDATAFLOW(builder, DATAFLOW)
}
func APPStartDATAFLOWVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func APPStartDataflowVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return APPStartDATAFLOWVector(builder, numElems)
}
func APPAddRUNTIME_CLASS(builder *flatbuffers.Builder, RUNTIME_CLASS appRuntimeTarget) {
	builder.PrependByteSlot(11, byte(RUNTIME_CLASS), 0)
}
func APPAddRuntimeClass(builder *flatbuffers.Builder, RUNTIME_CLASS appRuntimeTarget) {
	APPAddRUNTIME_CLASS(builder, RUNTIME_CLASS)
}
func APPAddPRIMARY_CATEGORY(builder *flatbuffers.Builder, PRIMARY_CATEGORY capabilityClass) {
	builder.PrependByteSlot(12, byte(PRIMARY_CATEGORY), 0)
}
func APPAddPrimaryCategory(builder *flatbuffers.Builder, PRIMARY_CATEGORY capabilityClass) {
	APPAddPRIMARY_CATEGORY(builder, PRIMARY_CATEGORY)
}
func APPAddCATEGORIES(builder *flatbuffers.Builder, CATEGORIES flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(13, flatbuffers.UOffsetT(CATEGORIES), 0)
}
func APPAddCategories(builder *flatbuffers.Builder, CATEGORIES flatbuffers.UOffsetT) {
	APPAddCATEGORIES(builder, CATEGORIES)
}
func APPStartCATEGORIESVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(1, numElems, 1)
}
func APPStartCategoriesVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return APPStartCATEGORIESVector(builder, numElems)
}
func APPEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
