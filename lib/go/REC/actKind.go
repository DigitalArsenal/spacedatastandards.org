// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package REC

import "strconv"

/// Account Record (ACT)
/// Description
/// A node-local operator account, a signed-in session, a sign-in challenge or
/// an external wallet link, keyed by the extended public key the operator
/// proves control of. Sign-in is a challenge the node issues and a signature
/// the operator returns over it; a session is the node's record of a verified
/// signature until EXPIRES_AT. Key slots list the derivation paths the node
/// uses for each purpose key so an operator can see and rotate them. A record
/// never carries private key material.
/// Role of the record. Append new values only; never reorder or reuse
/// existing values.
type actKind int8

const (
	actKindOperator     actKind = 0
	actKindSession      actKind = 1
	actKindChallenge    actKind = 2
	actKindExternalLink actKind = 3
)

var EnumNamesactKind = map[actKind]string{
	actKindOperator:     "Operator",
	actKindSession:      "Session",
	actKindChallenge:    "Challenge",
	actKindExternalLink: "ExternalLink",
}

var EnumValuesactKind = map[string]actKind{
	"Operator":     actKindOperator,
	"Session":      actKindSession,
	"Challenge":    actKindChallenge,
	"ExternalLink": actKindExternalLink,
}

func (v actKind) String() string {
	if s, ok := EnumNamesactKind[v]; ok {
		return s
	}
	return "actKind(" + strconv.FormatInt(int64(v), 10) + ")"
}
