// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package CPS

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

/// Compressed Packet Stream — a run of fixed-length CCSDS packets losslessly
/// compressed as one stateful stream (CCSDS 124.0-B-1 POCKET+). Decoded output
/// is a sequence of PACKET_COUNT packets of PACKET_LENGTH bytes, each of which
/// projects to an $SPP record. This record carries a compressed packet stream
/// only; it is not a general-purpose compression envelope for SDS records.
type CPS struct {
	_tab flatbuffers.Table
}

const CPSIdentifier = "$CPS"

func GetRootAsCPS(buf []byte, offset flatbuffers.UOffsetT) *CPS {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &CPS{}
	x.Init(buf, n+offset)
	return x
}

func FinishCPSBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	identifierBytes := []byte(CPSIdentifier)
	builder.FinishWithFileIdentifier(offset, identifierBytes)
}

func CPSBufferHasIdentifier(buf []byte) bool {
	return flatbuffers.BufferHasIdentifier(buf, CPSIdentifier)
}

func GetSizePrefixedRootAsCPS(buf []byte, offset flatbuffers.UOffsetT) *CPS {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &CPS{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedCPSBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	identifierBytes := []byte(CPSIdentifier)
	builder.FinishSizePrefixedWithFileIdentifier(offset, identifierBytes)
}

func SizePrefixedCPSBufferHasIdentifier(buf []byte) bool {
	return flatbuffers.SizePrefixedBufferHasIdentifier(buf, CPSIdentifier)
}

func (rcv *CPS) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *CPS) Table() flatbuffers.Table {
	return rcv._tab
}

/// Compression algorithm used to produce COMPRESSED_DATA.
func (rcv *CPS) ALGORITHM() packetCompressionAlgorithm {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return packetCompressionAlgorithm(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *CPS) Algorithm() packetCompressionAlgorithm {
	return rcv.ALGORITHM()
}

/// Compression algorithm used to produce COMPRESSED_DATA.
func (rcv *CPS) MutateALGORITHM(n packetCompressionAlgorithm) bool {
	return rcv._tab.MutateByteSlot(4, byte(n))
}

func (rcv *CPS) MutateAlgorithm(n packetCompressionAlgorithm) bool {
	return rcv.MutateALGORITHM(n)
}

/// Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
/// defined only for a fixed packet length; a length change starts a new
/// stream and therefore a new $CPS record.
func (rcv *CPS) PACKET_LENGTH() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) PacketLength() uint16 {
	return rcv.PACKET_LENGTH()
}

/// Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
/// defined only for a fixed packet length; a length change starts a new
/// stream and therefore a new $CPS record.
func (rcv *CPS) MutatePACKET_LENGTH(n uint16) bool {
	return rcv._tab.MutateUint16Slot(6, n)
}

func (rcv *CPS) MutatePacketLength(n uint16) bool {
	return rcv.MutatePACKET_LENGTH(n)
}

/// Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
/// this is the only output-size bound a decoder has. The compressed
/// bitstream alone does not bound the decoded size, and a compression-ratio
/// guess is unsafe in both directions. Decoders MUST reject a record whose
/// PACKET_COUNT is zero, and MUST allocate exactly
/// PACKET_COUNT * PACKET_LENGTH bytes of output.
func (rcv *CPS) PACKET_COUNT() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) PacketCount() uint32 {
	return rcv.PACKET_COUNT()
}

/// Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
/// this is the only output-size bound a decoder has. The compressed
/// bitstream alone does not bound the decoded size, and a compression-ratio
/// guess is unsafe in both directions. Decoders MUST reject a record whose
/// PACKET_COUNT is zero, and MUST allocate exactly
/// PACKET_COUNT * PACKET_LENGTH bytes of output.
func (rcv *CPS) MutatePACKET_COUNT(n uint32) bool {
	return rcv._tab.MutateUint32Slot(8, n)
}

func (rcv *CPS) MutatePacketCount(n uint32) bool {
	return rcv.MutatePACKET_COUNT(n)
}

/// POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
/// a window depth, NOT a retransmission interval — it does not by itself
/// configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
/// REFERENCE_PERIOD, which are three independent periods.
func (rcv *CPS) ROBUSTNESS() byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetByte(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) Robustness() byte {
	return rcv.ROBUSTNESS()
}

/// POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
/// a window depth, NOT a retransmission interval — it does not by itself
/// configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
/// REFERENCE_PERIOD, which are three independent periods.
func (rcv *CPS) MutateROBUSTNESS(n byte) bool {
	return rcv._tab.MutateByteSlot(10, n)
}

func (rcv *CPS) MutateRobustness(n byte) bool {
	return rcv.MutateROBUSTNESS(n)
}

/// POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
/// MASK_SEND_PERIOD, and REFERENCE_PERIOD.
func (rcv *CPS) MASK_UPDATE_PERIOD() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) MaskUpdatePeriod() uint16 {
	return rcv.MASK_UPDATE_PERIOD()
}

/// POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
/// MASK_SEND_PERIOD, and REFERENCE_PERIOD.
func (rcv *CPS) MutateMASK_UPDATE_PERIOD(n uint16) bool {
	return rcv._tab.MutateUint16Slot(12, n)
}

func (rcv *CPS) MutateMaskUpdatePeriod(n uint16) bool {
	return rcv.MutateMASK_UPDATE_PERIOD(n)
}

/// POCKET+ mask send period (ft), in packets — how often the change mask is
/// transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
/// REFERENCE_PERIOD.
func (rcv *CPS) MASK_SEND_PERIOD() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) MaskSendPeriod() uint16 {
	return rcv.MASK_SEND_PERIOD()
}

/// POCKET+ mask send period (ft), in packets — how often the change mask is
/// transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
/// REFERENCE_PERIOD.
func (rcv *CPS) MutateMASK_SEND_PERIOD(n uint16) bool {
	return rcv._tab.MutateUint16Slot(14, n)
}

func (rcv *CPS) MutateMaskSendPeriod(n uint16) bool {
	return rcv.MutateMASK_SEND_PERIOD(n)
}

/// POCKET+ reference packet retransmission period (rt), in packets.
/// Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
func (rcv *CPS) REFERENCE_PERIOD() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) ReferencePeriod() uint16 {
	return rcv.REFERENCE_PERIOD()
}

/// POCKET+ reference packet retransmission period (rt), in packets.
/// Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
func (rcv *CPS) MutateREFERENCE_PERIOD(n uint16) bool {
	return rcv._tab.MutateUint16Slot(16, n)
}

func (rcv *CPS) MutateReferencePeriod(n uint16) bool {
	return rcv.MutateREFERENCE_PERIOD(n)
}

/// True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
/// ft=1, i.e. it transmits a full reference packet and a full mask, so this
/// record decodes standalone without its predecessor. There is no
/// synchronization flag in the POCKET+ bitstream itself: this field is the
/// producer's assertion about the first packet, and defaults to false.
func (rcv *CPS) SELF_SYNCHRONIZING() bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.GetBool(o + rcv._tab.Pos)
	}
	return false
}

func (rcv *CPS) SelfSynchronizing() bool {
	return rcv.SELF_SYNCHRONIZING()
}

/// True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
/// ft=1, i.e. it transmits a full reference packet and a full mask, so this
/// record decodes standalone without its predecessor. There is no
/// synchronization flag in the POCKET+ bitstream itself: this field is the
/// producer's assertion about the first packet, and defaults to false.
func (rcv *CPS) MutateSELF_SYNCHRONIZING(n bool) bool {
	return rcv._tab.MutateBoolSlot(18, n)
}

func (rcv *CPS) MutateSelfSynchronizing(n bool) bool {
	return rcv.MutateSELF_SYNCHRONIZING(n)
}

/// Producer-defined stream identity. Records sharing STREAM_ID decode in
/// SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
/// poisons every subsequent segment up to the next segment with
/// SELF_SYNCHRONIZING = true.
func (rcv *CPS) STREAM_ID() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(20))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *CPS) StreamId() []byte {
	return rcv.STREAM_ID()
}

/// Producer-defined stream identity. Records sharing STREAM_ID decode in
/// SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
/// poisons every subsequent segment up to the next segment with
/// SELF_SYNCHRONIZING = true.
/// Zero-based index of this segment within STREAM_ID.
func (rcv *CPS) SEGMENT_INDEX() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(22))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) SegmentIndex() uint32 {
	return rcv.SEGMENT_INDEX()
}

/// Zero-based index of this segment within STREAM_ID.
func (rcv *CPS) MutateSEGMENT_INDEX(n uint32) bool {
	return rcv._tab.MutateUint32Slot(22, n)
}

func (rcv *CPS) MutateSegmentIndex(n uint32) bool {
	return rcv.MutateSEGMENT_INDEX(n)
}

/// CCSDS Application Process Identifier of the packets, when known.
func (rcv *CPS) APID() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(24))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) Apid() uint16 {
	return rcv.APID()
}

/// CCSDS Application Process Identifier of the packets, when known.
func (rcv *CPS) MutateAPID(n uint16) bool {
	return rcv._tab.MutateUint16Slot(24, n)
}

func (rcv *CPS) MutateApid(n uint16) bool {
	return rcv.MutateAPID(n)
}

/// Spacecraft identifier of the packets, when known.
func (rcv *CPS) SPACECRAFT_ID() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(26))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) SpacecraftId() uint16 {
	return rcv.SPACECRAFT_ID()
}

/// Spacecraft identifier of the packets, when known.
func (rcv *CPS) MutateSPACECRAFT_ID(n uint16) bool {
	return rcv._tab.MutateUint16Slot(26, n)
}

func (rcv *CPS) MutateSpacecraftId(n uint16) bool {
	return rcv.MutateSPACECRAFT_ID(n)
}

/// UTC epoch of the first packet in the segment, ISO 8601.
func (rcv *CPS) START_EPOCH() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(28))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *CPS) StartEpoch() []byte {
	return rcv.START_EPOCH()
}

/// UTC epoch of the first packet in the segment, ISO 8601.
/// UTC epoch of the last packet in the segment, ISO 8601.
func (rcv *CPS) STOP_EPOCH() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(30))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *CPS) StopEpoch() []byte {
	return rcv.STOP_EPOCH()
}

/// UTC epoch of the last packet in the segment, ISO 8601.
/// The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
/// packet is individually octet-aligned; padding is per PACKET, never per
/// stream. Bit-concatenating packets diverges from the reference
/// implementation and MUST NOT be done.
func (rcv *CPS) COMPRESSED_DATA(j int) byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(32))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.GetByte(a + flatbuffers.UOffsetT(j*1))
	}
	return 0
}

func (rcv *CPS) CompressedData(j int) byte {
	return rcv.COMPRESSED_DATA(j)
}

func (rcv *CPS) COMPRESSED_DATALength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(32))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *CPS) CompressedDataLength() int {
	return rcv.COMPRESSED_DATALength()
}

func (rcv *CPS) COMPRESSED_DATABytes() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(32))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *CPS) CompressedDataBytes() []byte {
	return rcv.COMPRESSED_DATABytes()
}

/// The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
/// packet is individually octet-aligned; padding is per PACKET, never per
/// stream. Bit-concatenating packets diverges from the reference
/// implementation and MUST NOT be done.
func (rcv *CPS) MutateCOMPRESSED_DATA(j int, n byte) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(32))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.MutateByte(a+flatbuffers.UOffsetT(j*1), n)
	}
	return false
}

func (rcv *CPS) MutateCompressedData(j int, n byte) bool {
	return rcv.MutateCOMPRESSED_DATA(j, n)
}

/// PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
/// output length without recomputing the product from a corrupt header.
func (rcv *CPS) UNCOMPRESSED_LENGTH() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(34))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) UncompressedLength() uint32 {
	return rcv.UNCOMPRESSED_LENGTH()
}

/// PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
/// output length without recomputing the product from a corrupt header.
func (rcv *CPS) MutateUNCOMPRESSED_LENGTH(n uint32) bool {
	return rcv._tab.MutateUint32Slot(34, n)
}

func (rcv *CPS) MutateUncompressedLength(n uint32) bool {
	return rcv.MutateUNCOMPRESSED_LENGTH(n)
}

/// CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
func (rcv *CPS) UNCOMPRESSED_CRC32() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(36))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *CPS) UncompressedCrc32() uint32 {
	return rcv.UNCOMPRESSED_CRC32()
}

/// CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
func (rcv *CPS) MutateUNCOMPRESSED_CRC32(n uint32) bool {
	return rcv._tab.MutateUint32Slot(36, n)
}

func (rcv *CPS) MutateUncompressedCrc32(n uint32) bool {
	return rcv.MutateUNCOMPRESSED_CRC32(n)
}

func CPSStart(builder *flatbuffers.Builder) {
	builder.StartObject(17)
}
func CPSAddALGORITHM(builder *flatbuffers.Builder, ALGORITHM packetCompressionAlgorithm) {
	builder.PrependByteSlot(0, byte(ALGORITHM), 0)
}
func CPSAddAlgorithm(builder *flatbuffers.Builder, ALGORITHM packetCompressionAlgorithm) {
	CPSAddALGORITHM(builder, ALGORITHM)
}
func CPSAddPACKET_LENGTH(builder *flatbuffers.Builder, PACKET_LENGTH uint16) {
	builder.PrependUint16Slot(1, PACKET_LENGTH, 0)
}
func CPSAddPacketLength(builder *flatbuffers.Builder, PACKET_LENGTH uint16) {
	CPSAddPACKET_LENGTH(builder, PACKET_LENGTH)
}
func CPSAddPACKET_COUNT(builder *flatbuffers.Builder, PACKET_COUNT uint32) {
	builder.PrependUint32Slot(2, PACKET_COUNT, 0)
}
func CPSAddPacketCount(builder *flatbuffers.Builder, PACKET_COUNT uint32) {
	CPSAddPACKET_COUNT(builder, PACKET_COUNT)
}
func CPSAddROBUSTNESS(builder *flatbuffers.Builder, ROBUSTNESS byte) {
	builder.PrependByteSlot(3, ROBUSTNESS, 0)
}
func CPSAddRobustness(builder *flatbuffers.Builder, ROBUSTNESS byte) {
	CPSAddROBUSTNESS(builder, ROBUSTNESS)
}
func CPSAddMASK_UPDATE_PERIOD(builder *flatbuffers.Builder, MASK_UPDATE_PERIOD uint16) {
	builder.PrependUint16Slot(4, MASK_UPDATE_PERIOD, 0)
}
func CPSAddMaskUpdatePeriod(builder *flatbuffers.Builder, MASK_UPDATE_PERIOD uint16) {
	CPSAddMASK_UPDATE_PERIOD(builder, MASK_UPDATE_PERIOD)
}
func CPSAddMASK_SEND_PERIOD(builder *flatbuffers.Builder, MASK_SEND_PERIOD uint16) {
	builder.PrependUint16Slot(5, MASK_SEND_PERIOD, 0)
}
func CPSAddMaskSendPeriod(builder *flatbuffers.Builder, MASK_SEND_PERIOD uint16) {
	CPSAddMASK_SEND_PERIOD(builder, MASK_SEND_PERIOD)
}
func CPSAddREFERENCE_PERIOD(builder *flatbuffers.Builder, REFERENCE_PERIOD uint16) {
	builder.PrependUint16Slot(6, REFERENCE_PERIOD, 0)
}
func CPSAddReferencePeriod(builder *flatbuffers.Builder, REFERENCE_PERIOD uint16) {
	CPSAddREFERENCE_PERIOD(builder, REFERENCE_PERIOD)
}
func CPSAddSELF_SYNCHRONIZING(builder *flatbuffers.Builder, SELF_SYNCHRONIZING bool) {
	builder.PrependBoolSlot(7, SELF_SYNCHRONIZING, false)
}
func CPSAddSelfSynchronizing(builder *flatbuffers.Builder, SELF_SYNCHRONIZING bool) {
	CPSAddSELF_SYNCHRONIZING(builder, SELF_SYNCHRONIZING)
}
func CPSAddSTREAM_ID(builder *flatbuffers.Builder, STREAM_ID flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(8, flatbuffers.UOffsetT(STREAM_ID), 0)
}
func CPSAddStreamId(builder *flatbuffers.Builder, STREAM_ID flatbuffers.UOffsetT) {
	CPSAddSTREAM_ID(builder, STREAM_ID)
}
func CPSAddSEGMENT_INDEX(builder *flatbuffers.Builder, SEGMENT_INDEX uint32) {
	builder.PrependUint32Slot(9, SEGMENT_INDEX, 0)
}
func CPSAddSegmentIndex(builder *flatbuffers.Builder, SEGMENT_INDEX uint32) {
	CPSAddSEGMENT_INDEX(builder, SEGMENT_INDEX)
}
func CPSAddAPID(builder *flatbuffers.Builder, APID uint16) {
	builder.PrependUint16Slot(10, APID, 0)
}
func CPSAddApid(builder *flatbuffers.Builder, APID uint16) {
	CPSAddAPID(builder, APID)
}
func CPSAddSPACECRAFT_ID(builder *flatbuffers.Builder, SPACECRAFT_ID uint16) {
	builder.PrependUint16Slot(11, SPACECRAFT_ID, 0)
}
func CPSAddSpacecraftId(builder *flatbuffers.Builder, SPACECRAFT_ID uint16) {
	CPSAddSPACECRAFT_ID(builder, SPACECRAFT_ID)
}
func CPSAddSTART_EPOCH(builder *flatbuffers.Builder, START_EPOCH flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(12, flatbuffers.UOffsetT(START_EPOCH), 0)
}
func CPSAddStartEpoch(builder *flatbuffers.Builder, START_EPOCH flatbuffers.UOffsetT) {
	CPSAddSTART_EPOCH(builder, START_EPOCH)
}
func CPSAddSTOP_EPOCH(builder *flatbuffers.Builder, STOP_EPOCH flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(13, flatbuffers.UOffsetT(STOP_EPOCH), 0)
}
func CPSAddStopEpoch(builder *flatbuffers.Builder, STOP_EPOCH flatbuffers.UOffsetT) {
	CPSAddSTOP_EPOCH(builder, STOP_EPOCH)
}
func CPSAddCOMPRESSED_DATA(builder *flatbuffers.Builder, COMPRESSED_DATA flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(14, flatbuffers.UOffsetT(COMPRESSED_DATA), 0)
}
func CPSAddCompressedData(builder *flatbuffers.Builder, COMPRESSED_DATA flatbuffers.UOffsetT) {
	CPSAddCOMPRESSED_DATA(builder, COMPRESSED_DATA)
}
func CPSStartCOMPRESSED_DATAVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(1, numElems, 1)
}
func CPSStartCompressedDataVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return CPSStartCOMPRESSED_DATAVector(builder, numElems)
}
func CPSAddUNCOMPRESSED_LENGTH(builder *flatbuffers.Builder, UNCOMPRESSED_LENGTH uint32) {
	builder.PrependUint32Slot(15, UNCOMPRESSED_LENGTH, 0)
}
func CPSAddUncompressedLength(builder *flatbuffers.Builder, UNCOMPRESSED_LENGTH uint32) {
	CPSAddUNCOMPRESSED_LENGTH(builder, UNCOMPRESSED_LENGTH)
}
func CPSAddUNCOMPRESSED_CRC32(builder *flatbuffers.Builder, UNCOMPRESSED_CRC32 uint32) {
	builder.PrependUint32Slot(16, UNCOMPRESSED_CRC32, 0)
}
func CPSAddUncompressedCrc32(builder *flatbuffers.Builder, UNCOMPRESSED_CRC32 uint32) {
	CPSAddUNCOMPRESSED_CRC32(builder, UNCOMPRESSED_CRC32)
}
func CPSEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
