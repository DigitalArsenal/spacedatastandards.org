// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package REC

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

/// Everything a resuming reader needs to decode a chunk that does not begin at
/// offset 0. Required whenever NEXT_OFFSET is past the source's own header.
type IRMDecodeContext struct {
	_tab flatbuffers.Table
}

func GetRootAsIRMDecodeContext(buf []byte, offset flatbuffers.UOffsetT) *IRMDecodeContext {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &IRMDecodeContext{}
	x.Init(buf, n+offset)
	return x
}

func FinishIRMDecodeContextBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsIRMDecodeContext(buf []byte, offset flatbuffers.UOffsetT) *IRMDecodeContext {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &IRMDecodeContext{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedIRMDecodeContextBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *IRMDecodeContext) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *IRMDecodeContext) Table() flatbuffers.Table {
	return rcv._tab
}

/// Format token the decoder dispatches on, verbatim and lowercase, e.g.
/// `text/csv`. A consumer that does not implement the format refuses the chunk
/// rather than guessing at a layout.
func (rcv *IRMDecodeContext) FORMAT() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) Format() []byte {
	return rcv.FORMAT()
}

/// Format token the decoder dispatches on, verbatim and lowercase, e.g.
/// `text/csv`. A consumer that does not implement the format refuses the chunk
/// rather than guessing at a layout.
/// The source's header line verbatim, exactly as it appeared at offset 0 and
/// excluding its line terminator. This is the field that makes chunk N>0
/// decodable; without it a headerless chunk parses to zero rows and looks like
/// a clean tail.
func (rcv *IRMDecodeContext) HEADER_LINE() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) HeaderLine() []byte {
	return rcv.HEADER_LINE()
}

/// The source's header line verbatim, exactly as it appeared at offset 0 and
/// excluding its line terminator. This is the field that makes chunk N>0
/// decodable; without it a headerless chunk parses to zero rows and looks like
/// a clean tail.
/// Byte length the header occupied at the start of the source, including its
/// terminator, so a reader can tell a header-bearing prefix from a data row.
func (rcv *IRMDecodeContext) HEADER_BYTE_LENGTH() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *IRMDecodeContext) HeaderByteLength() uint64 {
	return rcv.HEADER_BYTE_LENGTH()
}

/// Byte length the header occupied at the start of the source, including its
/// terminator, so a reader can tell a header-bearing prefix from a data row.
func (rcv *IRMDecodeContext) MutateHEADER_BYTE_LENGTH(n uint64) bool {
	return rcv._tab.MutateUint64Slot(8, n)
}

func (rcv *IRMDecodeContext) MutateHeaderByteLength(n uint64) bool {
	return rcv.MutateHEADER_BYTE_LENGTH(n)
}

/// Column names in source order, when the publisher parsed them. Advisory:
/// HEADER_LINE is authoritative and a disagreement is resolved in its favour.
func (rcv *IRMDecodeContext) COLUMN_NAMES(j int) []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.ByteVector(a + flatbuffers.UOffsetT(j*4))
	}
	return nil
}

func (rcv *IRMDecodeContext) ColumnNames(j int) []byte {
	return rcv.COLUMN_NAMES(j)
}

func (rcv *IRMDecodeContext) COLUMN_NAMESLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *IRMDecodeContext) ColumnNamesLength() int {
	return rcv.COLUMN_NAMESLength()
}

/// Column names in source order, when the publisher parsed them. Advisory:
/// HEADER_LINE is authoritative and a disagreement is resolved in its favour.
/// Field delimiter verbatim.
func (rcv *IRMDecodeContext) DELIMITER() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) Delimiter() []byte {
	return rcv.DELIMITER()
}

/// Field delimiter verbatim.
/// Quote character verbatim.
func (rcv *IRMDecodeContext) QUOTE() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) Quote() []byte {
	return rcv.QUOTE()
}

/// Quote character verbatim.
/// Escape character verbatim.
func (rcv *IRMDecodeContext) ESCAPE() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) Escape() []byte {
	return rcv.ESCAPE()
}

/// Escape character verbatim.
/// Record terminator verbatim, e.g. `\n` or `\r\n`.
func (rcv *IRMDecodeContext) LINE_TERMINATOR() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) LineTerminator() []byte {
	return rcv.LINE_TERMINATOR()
}

/// Record terminator verbatim, e.g. `\n` or `\r\n`.
/// Character encoding label verbatim as the provider states it.
func (rcv *IRMDecodeContext) CHARSET() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(20))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) Charset() []byte {
	return rcv.CHARSET()
}

/// Character encoding label verbatim as the provider states it.
/// Stream-compression codec applied to the source bytes, verbatim and
/// lowercase. Empty means the ranged bytes are read as-is. Non-empty means
/// NEXT_OFFSET is a position in the COMPRESSED stream and DECODER_STATE,
/// WINDOW and PARTIAL_RECORD are all required to resume.
func (rcv *IRMDecodeContext) COMPRESSION() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(22))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) Compression() []byte {
	return rcv.COMPRESSION()
}

/// Stream-compression codec applied to the source bytes, verbatim and
/// lowercase. Empty means the ranged bytes are read as-is. Non-empty means
/// NEXT_OFFSET is a position in the COMPRESSED stream and DECODER_STATE,
/// WINDOW and PARTIAL_RECORD are all required to resume.
/// Position in the DECODED stream corresponding to NEXT_OFFSET, when the
/// source is compressed. 0 with a non-empty COMPRESSION means the decoded
/// stream has not been advanced.
func (rcv *IRMDecodeContext) DECODED_OFFSET() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(24))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *IRMDecodeContext) DecodedOffset() uint64 {
	return rcv.DECODED_OFFSET()
}

/// Position in the DECODED stream corresponding to NEXT_OFFSET, when the
/// source is compressed. 0 with a non-empty COMPRESSION means the decoded
/// stream has not been advanced.
func (rcv *IRMDecodeContext) MutateDECODED_OFFSET(n uint64) bool {
	return rcv._tab.MutateUint64Slot(24, n)
}

func (rcv *IRMDecodeContext) MutateDecodedOffset(n uint64) bool {
	return rcv.MutateDECODED_OFFSET(n)
}

/// Serialized, pointer-free image of the decoder at the seam — the state a
/// codec with no restart point needs in order to have one. Opaque to every
/// consumer but a decoder whose stamps match exactly. Never partially applied.
func (rcv *IRMDecodeContext) DECODER_STATE(j int) byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(26))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.GetByte(a + flatbuffers.UOffsetT(j*1))
	}
	return 0
}

func (rcv *IRMDecodeContext) DecoderState(j int) byte {
	return rcv.DECODER_STATE(j)
}

func (rcv *IRMDecodeContext) DECODER_STATELength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(26))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *IRMDecodeContext) DecoderStateLength() int {
	return rcv.DECODER_STATELength()
}

func (rcv *IRMDecodeContext) DECODER_STATEBytes() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(26))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) DecoderStateBytes() []byte {
	return rcv.DECODER_STATEBytes()
}

/// Serialized, pointer-free image of the decoder at the seam — the state a
/// codec with no restart point needs in order to have one. Opaque to every
/// consumer but a decoder whose stamps match exactly. Never partially applied.
func (rcv *IRMDecodeContext) MutateDECODER_STATE(j int, n byte) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(26))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.MutateByte(a+flatbuffers.UOffsetT(j*1), n)
	}
	return false
}

func (rcv *IRMDecodeContext) MutateDecoderState(j int, n byte) bool {
	return rcv.MutateDECODER_STATE(j, n)
}

/// Layout identifier of DECODER_STATE, verbatim, naming the decoder struct the
/// image was taken from. A consumer that does not know the token restarts.
func (rcv *IRMDecodeContext) DECODER_STATE_FORMAT() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(28))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) DecoderStateFormat() []byte {
	return rcv.DECODER_STATE_FORMAT()
}

/// Layout identifier of DECODER_STATE, verbatim, naming the decoder struct the
/// image was taken from. A consumer that does not know the token restarts.
/// Version of that layout, verbatim. Any disagreement restarts the job.
func (rcv *IRMDecodeContext) DECODER_STATE_VERSION() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(30))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) DecoderStateVersion() []byte {
	return rcv.DECODER_STATE_VERSION()
}

/// Version of that layout, verbatim. Any disagreement restarts the job.
/// Byte length DECODER_STATE is expected to have. Carried even though the
/// vector knows its own size, so a size disagreement is detectable BEFORE the
/// image is loaded into a differently shaped struct.
func (rcv *IRMDecodeContext) DECODER_STATE_BYTE_LENGTH() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(32))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *IRMDecodeContext) DecoderStateByteLength() uint64 {
	return rcv.DECODER_STATE_BYTE_LENGTH()
}

/// Byte length DECODER_STATE is expected to have. Carried even though the
/// vector knows its own size, so a size disagreement is detectable BEFORE the
/// image is loaded into a differently shaped struct.
func (rcv *IRMDecodeContext) MutateDECODER_STATE_BYTE_LENGTH(n uint64) bool {
	return rcv._tab.MutateUint64Slot(32, n)
}

func (rcv *IRMDecodeContext) MutateDecoderStateByteLength(n uint64) bool {
	return rcv.MutateDECODER_STATE_BYTE_LENGTH(n)
}

/// SHA-256 of DECODER_STATE as 64 lowercase hexadecimal characters.
func (rcv *IRMDecodeContext) DECODER_STATE_SHA256() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(34))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) DecoderStateSha256() []byte {
	return rcv.DECODER_STATE_SHA256()
}

/// SHA-256 of DECODER_STATE as 64 lowercase hexadecimal characters.
/// Build identifier of the decoder that produced DECODER_STATE, verbatim, so
/// two builds of one codec version are still distinguishable.
func (rcv *IRMDecodeContext) DECODER_BUILD_ID() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(36))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) DecoderBuildId() []byte {
	return rcv.DECODER_BUILD_ID()
}

/// Build identifier of the decoder that produced DECODER_STATE, verbatim, so
/// two builds of one codec version are still distinguishable.
/// Media type of DECODER_STATE, so an unrelated decoder refuses it instead of
/// misreading it.
func (rcv *IRMDecodeContext) DECODER_STATE_MEDIA_TYPE() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(38))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) DecoderStateMediaType() []byte {
	return rcv.DECODER_STATE_MEDIA_TYPE()
}

/// Media type of DECODER_STATE, so an unrelated decoder refuses it instead of
/// misreading it.
/// The decoder's history window at the seam — the bytes that ARE the codec's
/// back-reference dictionary. Carried separately from DECODER_STATE because it
/// is bulk data of a size the codec fixes, not part of the struct image.
func (rcv *IRMDecodeContext) WINDOW(j int) byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(40))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.GetByte(a + flatbuffers.UOffsetT(j*1))
	}
	return 0
}

func (rcv *IRMDecodeContext) Window(j int) byte {
	return rcv.WINDOW(j)
}

func (rcv *IRMDecodeContext) WINDOWLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(40))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *IRMDecodeContext) WindowLength() int {
	return rcv.WINDOWLength()
}

func (rcv *IRMDecodeContext) WINDOWBytes() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(40))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) WindowBytes() []byte {
	return rcv.WINDOWBytes()
}

/// The decoder's history window at the seam — the bytes that ARE the codec's
/// back-reference dictionary. Carried separately from DECODER_STATE because it
/// is bulk data of a size the codec fixes, not part of the struct image.
func (rcv *IRMDecodeContext) MutateWINDOW(j int, n byte) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(40))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.MutateByte(a+flatbuffers.UOffsetT(j*1), n)
	}
	return false
}

func (rcv *IRMDecodeContext) MutateWindow(j int, n byte) bool {
	return rcv.MutateWINDOW(j, n)
}

/// Byte length WINDOW is expected to have, for the same detect-before-load
/// reason as DECODER_STATE_BYTE_LENGTH.
func (rcv *IRMDecodeContext) WINDOW_BYTE_LENGTH() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(42))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *IRMDecodeContext) WindowByteLength() uint64 {
	return rcv.WINDOW_BYTE_LENGTH()
}

/// Byte length WINDOW is expected to have, for the same detect-before-load
/// reason as DECODER_STATE_BYTE_LENGTH.
func (rcv *IRMDecodeContext) MutateWINDOW_BYTE_LENGTH(n uint64) bool {
	return rcv._tab.MutateUint64Slot(42, n)
}

func (rcv *IRMDecodeContext) MutateWindowByteLength(n uint64) bool {
	return rcv.MutateWINDOW_BYTE_LENGTH(n)
}

/// Offset into WINDOW at which the codec will next write, when the window is
/// a circular buffer whose contents cannot be interpreted without it.
func (rcv *IRMDecodeContext) WINDOW_POSITION() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(44))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *IRMDecodeContext) WindowPosition() uint64 {
	return rcv.WINDOW_POSITION()
}

/// Offset into WINDOW at which the codec will next write, when the window is
/// a circular buffer whose contents cannot be interpreted without it.
func (rcv *IRMDecodeContext) MutateWINDOW_POSITION(n uint64) bool {
	return rcv._tab.MutateUint64Slot(44, n)
}

func (rcv *IRMDecodeContext) MutateWindowPosition(n uint64) bool {
	return rcv.MutateWINDOW_POSITION(n)
}

/// The bytes of the record that straddles the seam: everything the previous
/// chunk decoded of a record it could not finish. A resuming decoder prepends
/// these to the next chunk's decoded output before parsing. Carried as bytes,
/// not text, because a seam may fall inside a multi-byte character.
func (rcv *IRMDecodeContext) PARTIAL_RECORD(j int) byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(46))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.GetByte(a + flatbuffers.UOffsetT(j*1))
	}
	return 0
}

func (rcv *IRMDecodeContext) PartialRecord(j int) byte {
	return rcv.PARTIAL_RECORD(j)
}

func (rcv *IRMDecodeContext) PARTIAL_RECORDLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(46))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *IRMDecodeContext) PartialRecordLength() int {
	return rcv.PARTIAL_RECORDLength()
}

func (rcv *IRMDecodeContext) PARTIAL_RECORDBytes() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(46))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *IRMDecodeContext) PartialRecordBytes() []byte {
	return rcv.PARTIAL_RECORDBytes()
}

/// The bytes of the record that straddles the seam: everything the previous
/// chunk decoded of a record it could not finish. A resuming decoder prepends
/// these to the next chunk's decoded output before parsing. Carried as bytes,
/// not text, because a seam may fall inside a multi-byte character.
func (rcv *IRMDecodeContext) MutatePARTIAL_RECORD(j int, n byte) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(46))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.MutateByte(a+flatbuffers.UOffsetT(j*1), n)
	}
	return false
}

func (rcv *IRMDecodeContext) MutatePartialRecord(j int, n byte) bool {
	return rcv.MutatePARTIAL_RECORD(j, n)
}

func IRMDecodeContextStart(builder *flatbuffers.Builder) {
	builder.StartObject(22)
}
func IRMDecodeContextAddFORMAT(builder *flatbuffers.Builder, FORMAT flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(0, flatbuffers.UOffsetT(FORMAT), 0)
}
func IRMDecodeContextAddFormat(builder *flatbuffers.Builder, FORMAT flatbuffers.UOffsetT) {
	IRMDecodeContextAddFORMAT(builder, FORMAT)
}
func IRMDecodeContextAddHEADER_LINE(builder *flatbuffers.Builder, HEADER_LINE flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(1, flatbuffers.UOffsetT(HEADER_LINE), 0)
}
func IRMDecodeContextAddHeaderLine(builder *flatbuffers.Builder, HEADER_LINE flatbuffers.UOffsetT) {
	IRMDecodeContextAddHEADER_LINE(builder, HEADER_LINE)
}
func IRMDecodeContextAddHEADER_BYTE_LENGTH(builder *flatbuffers.Builder, HEADER_BYTE_LENGTH uint64) {
	builder.PrependUint64Slot(2, HEADER_BYTE_LENGTH, 0)
}
func IRMDecodeContextAddHeaderByteLength(builder *flatbuffers.Builder, HEADER_BYTE_LENGTH uint64) {
	IRMDecodeContextAddHEADER_BYTE_LENGTH(builder, HEADER_BYTE_LENGTH)
}
func IRMDecodeContextAddCOLUMN_NAMES(builder *flatbuffers.Builder, COLUMN_NAMES flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(3, flatbuffers.UOffsetT(COLUMN_NAMES), 0)
}
func IRMDecodeContextAddColumnNames(builder *flatbuffers.Builder, COLUMN_NAMES flatbuffers.UOffsetT) {
	IRMDecodeContextAddCOLUMN_NAMES(builder, COLUMN_NAMES)
}
func IRMDecodeContextStartCOLUMN_NAMESVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func IRMDecodeContextStartColumnNamesVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return IRMDecodeContextStartCOLUMN_NAMESVector(builder, numElems)
}
func IRMDecodeContextAddDELIMITER(builder *flatbuffers.Builder, DELIMITER flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(4, flatbuffers.UOffsetT(DELIMITER), 0)
}
func IRMDecodeContextAddDelimiter(builder *flatbuffers.Builder, DELIMITER flatbuffers.UOffsetT) {
	IRMDecodeContextAddDELIMITER(builder, DELIMITER)
}
func IRMDecodeContextAddQUOTE(builder *flatbuffers.Builder, QUOTE flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(5, flatbuffers.UOffsetT(QUOTE), 0)
}
func IRMDecodeContextAddQuote(builder *flatbuffers.Builder, QUOTE flatbuffers.UOffsetT) {
	IRMDecodeContextAddQUOTE(builder, QUOTE)
}
func IRMDecodeContextAddESCAPE(builder *flatbuffers.Builder, ESCAPE flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(6, flatbuffers.UOffsetT(ESCAPE), 0)
}
func IRMDecodeContextAddEscape(builder *flatbuffers.Builder, ESCAPE flatbuffers.UOffsetT) {
	IRMDecodeContextAddESCAPE(builder, ESCAPE)
}
func IRMDecodeContextAddLINE_TERMINATOR(builder *flatbuffers.Builder, LINE_TERMINATOR flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(7, flatbuffers.UOffsetT(LINE_TERMINATOR), 0)
}
func IRMDecodeContextAddLineTerminator(builder *flatbuffers.Builder, LINE_TERMINATOR flatbuffers.UOffsetT) {
	IRMDecodeContextAddLINE_TERMINATOR(builder, LINE_TERMINATOR)
}
func IRMDecodeContextAddCHARSET(builder *flatbuffers.Builder, CHARSET flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(8, flatbuffers.UOffsetT(CHARSET), 0)
}
func IRMDecodeContextAddCharset(builder *flatbuffers.Builder, CHARSET flatbuffers.UOffsetT) {
	IRMDecodeContextAddCHARSET(builder, CHARSET)
}
func IRMDecodeContextAddCOMPRESSION(builder *flatbuffers.Builder, COMPRESSION flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(9, flatbuffers.UOffsetT(COMPRESSION), 0)
}
func IRMDecodeContextAddCompression(builder *flatbuffers.Builder, COMPRESSION flatbuffers.UOffsetT) {
	IRMDecodeContextAddCOMPRESSION(builder, COMPRESSION)
}
func IRMDecodeContextAddDECODED_OFFSET(builder *flatbuffers.Builder, DECODED_OFFSET uint64) {
	builder.PrependUint64Slot(10, DECODED_OFFSET, 0)
}
func IRMDecodeContextAddDecodedOffset(builder *flatbuffers.Builder, DECODED_OFFSET uint64) {
	IRMDecodeContextAddDECODED_OFFSET(builder, DECODED_OFFSET)
}
func IRMDecodeContextAddDECODER_STATE(builder *flatbuffers.Builder, DECODER_STATE flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(11, flatbuffers.UOffsetT(DECODER_STATE), 0)
}
func IRMDecodeContextAddDecoderState(builder *flatbuffers.Builder, DECODER_STATE flatbuffers.UOffsetT) {
	IRMDecodeContextAddDECODER_STATE(builder, DECODER_STATE)
}
func IRMDecodeContextStartDECODER_STATEVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(1, numElems, 1)
}
func IRMDecodeContextStartDecoderStateVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return IRMDecodeContextStartDECODER_STATEVector(builder, numElems)
}
func IRMDecodeContextAddDECODER_STATE_FORMAT(builder *flatbuffers.Builder, DECODER_STATE_FORMAT flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(12, flatbuffers.UOffsetT(DECODER_STATE_FORMAT), 0)
}
func IRMDecodeContextAddDecoderStateFormat(builder *flatbuffers.Builder, DECODER_STATE_FORMAT flatbuffers.UOffsetT) {
	IRMDecodeContextAddDECODER_STATE_FORMAT(builder, DECODER_STATE_FORMAT)
}
func IRMDecodeContextAddDECODER_STATE_VERSION(builder *flatbuffers.Builder, DECODER_STATE_VERSION flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(13, flatbuffers.UOffsetT(DECODER_STATE_VERSION), 0)
}
func IRMDecodeContextAddDecoderStateVersion(builder *flatbuffers.Builder, DECODER_STATE_VERSION flatbuffers.UOffsetT) {
	IRMDecodeContextAddDECODER_STATE_VERSION(builder, DECODER_STATE_VERSION)
}
func IRMDecodeContextAddDECODER_STATE_BYTE_LENGTH(builder *flatbuffers.Builder, DECODER_STATE_BYTE_LENGTH uint64) {
	builder.PrependUint64Slot(14, DECODER_STATE_BYTE_LENGTH, 0)
}
func IRMDecodeContextAddDecoderStateByteLength(builder *flatbuffers.Builder, DECODER_STATE_BYTE_LENGTH uint64) {
	IRMDecodeContextAddDECODER_STATE_BYTE_LENGTH(builder, DECODER_STATE_BYTE_LENGTH)
}
func IRMDecodeContextAddDECODER_STATE_SHA256(builder *flatbuffers.Builder, DECODER_STATE_SHA256 flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(15, flatbuffers.UOffsetT(DECODER_STATE_SHA256), 0)
}
func IRMDecodeContextAddDecoderStateSha256(builder *flatbuffers.Builder, DECODER_STATE_SHA256 flatbuffers.UOffsetT) {
	IRMDecodeContextAddDECODER_STATE_SHA256(builder, DECODER_STATE_SHA256)
}
func IRMDecodeContextAddDECODER_BUILD_ID(builder *flatbuffers.Builder, DECODER_BUILD_ID flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(16, flatbuffers.UOffsetT(DECODER_BUILD_ID), 0)
}
func IRMDecodeContextAddDecoderBuildId(builder *flatbuffers.Builder, DECODER_BUILD_ID flatbuffers.UOffsetT) {
	IRMDecodeContextAddDECODER_BUILD_ID(builder, DECODER_BUILD_ID)
}
func IRMDecodeContextAddDECODER_STATE_MEDIA_TYPE(builder *flatbuffers.Builder, DECODER_STATE_MEDIA_TYPE flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(17, flatbuffers.UOffsetT(DECODER_STATE_MEDIA_TYPE), 0)
}
func IRMDecodeContextAddDecoderStateMediaType(builder *flatbuffers.Builder, DECODER_STATE_MEDIA_TYPE flatbuffers.UOffsetT) {
	IRMDecodeContextAddDECODER_STATE_MEDIA_TYPE(builder, DECODER_STATE_MEDIA_TYPE)
}
func IRMDecodeContextAddWINDOW(builder *flatbuffers.Builder, WINDOW flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(18, flatbuffers.UOffsetT(WINDOW), 0)
}
func IRMDecodeContextAddWindow(builder *flatbuffers.Builder, WINDOW flatbuffers.UOffsetT) {
	IRMDecodeContextAddWINDOW(builder, WINDOW)
}
func IRMDecodeContextStartWINDOWVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(1, numElems, 1)
}
func IRMDecodeContextStartWindowVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return IRMDecodeContextStartWINDOWVector(builder, numElems)
}
func IRMDecodeContextAddWINDOW_BYTE_LENGTH(builder *flatbuffers.Builder, WINDOW_BYTE_LENGTH uint64) {
	builder.PrependUint64Slot(19, WINDOW_BYTE_LENGTH, 0)
}
func IRMDecodeContextAddWindowByteLength(builder *flatbuffers.Builder, WINDOW_BYTE_LENGTH uint64) {
	IRMDecodeContextAddWINDOW_BYTE_LENGTH(builder, WINDOW_BYTE_LENGTH)
}
func IRMDecodeContextAddWINDOW_POSITION(builder *flatbuffers.Builder, WINDOW_POSITION uint64) {
	builder.PrependUint64Slot(20, WINDOW_POSITION, 0)
}
func IRMDecodeContextAddWindowPosition(builder *flatbuffers.Builder, WINDOW_POSITION uint64) {
	IRMDecodeContextAddWINDOW_POSITION(builder, WINDOW_POSITION)
}
func IRMDecodeContextAddPARTIAL_RECORD(builder *flatbuffers.Builder, PARTIAL_RECORD flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(21, flatbuffers.UOffsetT(PARTIAL_RECORD), 0)
}
func IRMDecodeContextAddPartialRecord(builder *flatbuffers.Builder, PARTIAL_RECORD flatbuffers.UOffsetT) {
	IRMDecodeContextAddPARTIAL_RECORD(builder, PARTIAL_RECORD)
}
func IRMDecodeContextStartPARTIAL_RECORDVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(1, numElems, 1)
}
func IRMDecodeContextStartPartialRecordVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return IRMDecodeContextStartPARTIAL_RECORDVector(builder, numElems)
}
func IRMDecodeContextEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
