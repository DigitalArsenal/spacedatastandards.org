// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package REC

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

/// The runtime restriction the produced artifact enforces on itself.
///
/// AUTHORIZATION. A host is authorized when it equals an `ALLOWED_DOMAINS`
/// entry, OR ends with an `ALLOWED_TLDS` entry, OR equals a `DEV_DOMAINS`
/// entry. Suffix rules are DOT-ANCHORED: `.mil` admits `x.mil` and `a.b.mil`
/// and admits neither `mil` nor `evilmil`; a suffix rule never matches a
/// host that merely contains it (`x.mil.example` is refused).
///
/// UNLOCKED IS EXPLICIT. `ALLOWED_DOMAINS` empty AND `ALLOWED_TLDS` empty
/// means every host is authorized. This is a stated, allowed configuration,
/// which is why this table is required on `BPF`: an absent lock is never
/// read as an unlocked one.
///
/// EXPIRY IS INDEPENDENT of authorization: an unlocked profile still
/// expires. There is deliberately no stored expiry instant — expiry is
/// `COMPILED_AT_MS + TTL_DAYS * 86400000` and a second, storable copy of it
/// could disagree with the two values that produce it.
type BPFRuntimeLock struct {
	_tab flatbuffers.Table
}

func GetRootAsBPFRuntimeLock(buf []byte, offset flatbuffers.UOffsetT) *BPFRuntimeLock {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &BPFRuntimeLock{}
	x.Init(buf, n+offset)
	return x
}

func FinishBPFRuntimeLockBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsBPFRuntimeLock(buf []byte, offset flatbuffers.UOffsetT) *BPFRuntimeLock {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &BPFRuntimeLock{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedBPFRuntimeLockBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *BPFRuntimeLock) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *BPFRuntimeLock) Table() flatbuffers.Table {
	return rcv._tab
}

/// Exact hostnames authorized to run the produced artifact. Empty
/// together with `ALLOWED_TLDS` means unrestricted.
func (rcv *BPFRuntimeLock) ALLOWED_DOMAINS(j int) []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.ByteVector(a + flatbuffers.UOffsetT(j*4))
	}
	return nil
}

func (rcv *BPFRuntimeLock) AllowedDomains(j int) []byte {
	return rcv.ALLOWED_DOMAINS(j)
}

func (rcv *BPFRuntimeLock) ALLOWED_DOMAINSLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *BPFRuntimeLock) AllowedDomainsLength() int {
	return rcv.ALLOWED_DOMAINSLength()
}

/// Exact hostnames authorized to run the produced artifact. Empty
/// together with `ALLOWED_TLDS` means unrestricted.
/// Dot-anchored hostname suffix rules, each written with its leading dot,
/// for example `.gov`. Empty together with `ALLOWED_DOMAINS` means
/// unrestricted.
func (rcv *BPFRuntimeLock) ALLOWED_TLDS(j int) []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.ByteVector(a + flatbuffers.UOffsetT(j*4))
	}
	return nil
}

func (rcv *BPFRuntimeLock) AllowedTlds(j int) []byte {
	return rcv.ALLOWED_TLDS(j)
}

func (rcv *BPFRuntimeLock) ALLOWED_TLDSLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *BPFRuntimeLock) AllowedTldsLength() int {
	return rcv.ALLOWED_TLDSLength()
}

/// Dot-anchored hostname suffix rules, each written with its leading dot,
/// for example `.gov`. Empty together with `ALLOWED_DOMAINS` means
/// unrestricted.
/// Development hosts authorized in addition to the rules above, so a
/// locked profile remains runnable in a development context. Kept
/// separate from `ALLOWED_DOMAINS` because a development exemption and a
/// deployment authorization are different facts: folded together, a
/// development host would be indistinguishable from a deployment grant
/// and would silently persist into a locked artifact.
func (rcv *BPFRuntimeLock) DEV_DOMAINS(j int) []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.ByteVector(a + flatbuffers.UOffsetT(j*4))
	}
	return nil
}

func (rcv *BPFRuntimeLock) DevDomains(j int) []byte {
	return rcv.DEV_DOMAINS(j)
}

func (rcv *BPFRuntimeLock) DEV_DOMAINSLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *BPFRuntimeLock) DevDomainsLength() int {
	return rcv.DEV_DOMAINSLength()
}

/// Development hosts authorized in addition to the rules above, so a
/// locked profile remains runnable in a development context. Kept
/// separate from `ALLOWED_DOMAINS` because a development exemption and a
/// deployment authorization are different facts: folded together, a
/// development host would be indistinguishable from a deployment grant
/// and would silently persist into a locked artifact.
/// Lifetime of the produced artifact in days from `COMPILED_AT_MS`.
/// Normative range 1 through 365 inclusive; 180 when unstated. A value of
/// 0 or above 365 is invalid and MUST be rejected, never clamped.
func (rcv *BPFRuntimeLock) TTL_DAYS() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 180
}

func (rcv *BPFRuntimeLock) TtlDays() uint32 {
	return rcv.TTL_DAYS()
}

/// Lifetime of the produced artifact in days from `COMPILED_AT_MS`.
/// Normative range 1 through 365 inclusive; 180 when unstated. A value of
/// 0 or above 365 is invalid and MUST be rejected, never clamped.
func (rcv *BPFRuntimeLock) MutateTTL_DAYS(n uint32) bool {
	return rcv._tab.MutateUint32Slot(10, n)
}

func (rcv *BPFRuntimeLock) MutateTtlDays(n uint32) bool {
	return rcv.MutateTTL_DAYS(n)
}

/// Milliseconds since the Unix epoch at which the artifact was composed.
/// 0 means the profile has not been used to compose an artifact yet and
/// carries no expiry; the composing step stamps it.
func (rcv *BPFRuntimeLock) COMPILED_AT_MS() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *BPFRuntimeLock) CompiledAtMs() uint64 {
	return rcv.COMPILED_AT_MS()
}

/// Milliseconds since the Unix epoch at which the artifact was composed.
/// 0 means the profile has not been used to compose an artifact yet and
/// carries no expiry; the composing step stamps it.
func (rcv *BPFRuntimeLock) MutateCOMPILED_AT_MS(n uint64) bool {
	return rcv._tab.MutateUint64Slot(12, n)
}

func (rcv *BPFRuntimeLock) MutateCompiledAtMs(n uint64) bool {
	return rcv.MutateCOMPILED_AT_MS(n)
}

func BPFRuntimeLockStart(builder *flatbuffers.Builder) {
	builder.StartObject(5)
}
func BPFRuntimeLockAddALLOWED_DOMAINS(builder *flatbuffers.Builder, ALLOWED_DOMAINS flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(0, flatbuffers.UOffsetT(ALLOWED_DOMAINS), 0)
}
func BPFRuntimeLockAddAllowedDomains(builder *flatbuffers.Builder, ALLOWED_DOMAINS flatbuffers.UOffsetT) {
	BPFRuntimeLockAddALLOWED_DOMAINS(builder, ALLOWED_DOMAINS)
}
func BPFRuntimeLockStartALLOWED_DOMAINSVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func BPFRuntimeLockStartAllowedDomainsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return BPFRuntimeLockStartALLOWED_DOMAINSVector(builder, numElems)
}
func BPFRuntimeLockAddALLOWED_TLDS(builder *flatbuffers.Builder, ALLOWED_TLDS flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(1, flatbuffers.UOffsetT(ALLOWED_TLDS), 0)
}
func BPFRuntimeLockAddAllowedTlds(builder *flatbuffers.Builder, ALLOWED_TLDS flatbuffers.UOffsetT) {
	BPFRuntimeLockAddALLOWED_TLDS(builder, ALLOWED_TLDS)
}
func BPFRuntimeLockStartALLOWED_TLDSVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func BPFRuntimeLockStartAllowedTldsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return BPFRuntimeLockStartALLOWED_TLDSVector(builder, numElems)
}
func BPFRuntimeLockAddDEV_DOMAINS(builder *flatbuffers.Builder, DEV_DOMAINS flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(2, flatbuffers.UOffsetT(DEV_DOMAINS), 0)
}
func BPFRuntimeLockAddDevDomains(builder *flatbuffers.Builder, DEV_DOMAINS flatbuffers.UOffsetT) {
	BPFRuntimeLockAddDEV_DOMAINS(builder, DEV_DOMAINS)
}
func BPFRuntimeLockStartDEV_DOMAINSVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func BPFRuntimeLockStartDevDomainsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return BPFRuntimeLockStartDEV_DOMAINSVector(builder, numElems)
}
func BPFRuntimeLockAddTTL_DAYS(builder *flatbuffers.Builder, TTL_DAYS uint32) {
	builder.PrependUint32Slot(3, TTL_DAYS, 180)
}
func BPFRuntimeLockAddTtlDays(builder *flatbuffers.Builder, TTL_DAYS uint32) {
	BPFRuntimeLockAddTTL_DAYS(builder, TTL_DAYS)
}
func BPFRuntimeLockAddCOMPILED_AT_MS(builder *flatbuffers.Builder, COMPILED_AT_MS uint64) {
	builder.PrependUint64Slot(4, COMPILED_AT_MS, 0)
}
func BPFRuntimeLockAddCompiledAtMs(builder *flatbuffers.Builder, COMPILED_AT_MS uint64) {
	BPFRuntimeLockAddCOMPILED_AT_MS(builder, COMPILED_AT_MS)
}
func BPFRuntimeLockEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
