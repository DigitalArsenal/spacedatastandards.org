// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package REC

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

/// One identity address row: the machine-readable half of the card.
type VCFAlias struct {
	_tab flatbuffers.Table
}

func GetRootAsVCFAlias(buf []byte, offset flatbuffers.UOffsetT) *VCFAlias {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &VCFAlias{}
	x.Init(buf, n+offset)
	return x
}

func FinishVCFAliasBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsVCFAlias(buf []byte, offset flatbuffers.UOffsetT) *VCFAlias {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &VCFAlias{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedVCFAliasBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *VCFAlias) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *VCFAlias) Table() flatbuffers.Table {
	return rcv._tab
}

/// What the row carries.
func (rcv *VCFAlias) KIND() vcfAliasKind {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return vcfAliasKind(rcv._tab.GetInt8(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *VCFAlias) Kind() vcfAliasKind {
	return rcv.KIND()
}

/// What the row carries.
func (rcv *VCFAlias) MutateKIND(n vcfAliasKind) bool {
	return rcv._tab.MutateInt8Slot(4, int8(n))
}

func (rcv *VCFAlias) MutateKind(n vcfAliasKind) bool {
	return rcv.MutateKIND(n)
}

/// The row's kind label verbatim as emitted — the leftmost domain label.
/// Authoritative when KIND is OTHER.
func (rcv *VCFAlias) KIND_TOKEN() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) KindToken() []byte {
	return rcv.KIND_TOKEN()
}

/// The row's kind label verbatim as emitted — the leftmost domain label.
/// Authoritative when KIND is OTHER.
/// Everything left of the separator in the emitted address, verbatim.
func (rcv *VCFAlias) LOCAL_PART() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) LocalPart() []byte {
	return rcv.LOCAL_PART()
}

/// Everything left of the separator in the emitted address, verbatim.
/// Everything right of the separator in the emitted address, verbatim. This
/// is publisher data: the identity domain of the deployment that produced the
/// card, never a value this standard fixes.
func (rcv *VCFAlias) DOMAIN() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) Domain() []byte {
	return rcv.DOMAIN()
}

/// Everything right of the separator in the emitted address, verbatim. This
/// is publisher data: the identity domain of the deployment that produced the
/// card, never a value this standard fixes.
/// The complete emitted address, `LOCAL_PART` then the separator then
/// `DOMAIN`, verbatim. Carried whole because it is what the importer stores
/// and what a consumer matches on.
func (rcv *VCFAlias) ADDRESS() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) Address() []byte {
	return rcv.ADDRESS()
}

/// The complete emitted address, `LOCAL_PART` then the separator then
/// `DOMAIN`, verbatim. Carried whole because it is what the importer stores
/// and what a consumer matches on.
/// Transport encoding of LOCAL_PART verbatim and lowercase. An address
/// local part has a hard octet cap, so raw hexadecimal key material does not
/// fit and a compact binary-to-text encoding is REQUIRED for key rows.
func (rcv *VCFAlias) ENCODING() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) Encoding() []byte {
	return rcv.ENCODING()
}

/// Transport encoding of LOCAL_PART verbatim and lowercase. An address
/// local part has a hard octet cap, so raw hexadecimal key material does not
/// fit and a compact binary-to-text encoding is REQUIRED for key rows.
/// Cryptographic algorithm the row's key or signature belongs to, verbatim as
/// the profile states it. A key without a named algorithm is not verifiable.
func (rcv *VCFAlias) ALGORITHM() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) Algorithm() []byte {
	return rcv.ALGORITHM()
}

/// Cryptographic algorithm the row's key or signature belongs to, verbatim as
/// the profile states it. A key without a named algorithm is not verifiable.
/// The profile's published public key value verbatim, for key rows. The SAME
/// bytes LOCAL_PART transport-encodes, carried in the profile's own spelling
/// so a consumer can join card to profile without decoding. NEVER an extended
/// public key and NEVER a derivation path.
func (rcv *VCFAlias) PUBLIC_KEY() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) PublicKey() []byte {
	return rcv.PUBLIC_KEY()
}

/// The profile's published public key value verbatim, for key rows. The SAME
/// bytes LOCAL_PART transport-encodes, carried in the profile's own spelling
/// so a consumer can join card to profile without decoding. NEVER an extended
/// public key and NEVER a derivation path.
/// Ledger token verbatim for LEDGER_ACCOUNT rows.
func (rcv *VCFAlias) CHAIN() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(20))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VCFAlias) Chain() []byte {
	return rcv.CHAIN()
}

/// Ledger token verbatim for LEDGER_ACCOUNT rows.
/// Whether this row survives into the COMPACT form.
func (rcv *VCFAlias) ON_COMPACT_CARD() bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(22))
	if o != 0 {
		return rcv._tab.GetBool(o + rcv._tab.Pos)
	}
	return false
}

func (rcv *VCFAlias) OnCompactCard() bool {
	return rcv.ON_COMPACT_CARD()
}

/// Whether this row survives into the COMPACT form.
func (rcv *VCFAlias) MutateON_COMPACT_CARD(n bool) bool {
	return rcv._tab.MutateBoolSlot(22, n)
}

func (rcv *VCFAlias) MutateOnCompactCard(n bool) bool {
	return rcv.MutateON_COMPACT_CARD(n)
}

/// Octet length of the complete UNFOLDED row as emitted.
func (rcv *VCFAlias) OCTET_LENGTH() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(24))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *VCFAlias) OctetLength() uint32 {
	return rcv.OCTET_LENGTH()
}

/// Octet length of the complete UNFOLDED row as emitted.
func (rcv *VCFAlias) MutateOCTET_LENGTH(n uint32) bool {
	return rcv._tab.MutateUint32Slot(24, n)
}

func (rcv *VCFAlias) MutateOctetLength(n uint32) bool {
	return rcv.MutateOCTET_LENGTH(n)
}

func VCFAliasStart(builder *flatbuffers.Builder) {
	builder.StartObject(11)
}
func VCFAliasAddKIND(builder *flatbuffers.Builder, KIND vcfAliasKind) {
	builder.PrependInt8Slot(0, int8(KIND), 0)
}
func VCFAliasAddKind(builder *flatbuffers.Builder, KIND vcfAliasKind) {
	VCFAliasAddKIND(builder, KIND)
}
func VCFAliasAddKIND_TOKEN(builder *flatbuffers.Builder, KIND_TOKEN flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(1, flatbuffers.UOffsetT(KIND_TOKEN), 0)
}
func VCFAliasAddKindToken(builder *flatbuffers.Builder, KIND_TOKEN flatbuffers.UOffsetT) {
	VCFAliasAddKIND_TOKEN(builder, KIND_TOKEN)
}
func VCFAliasAddLOCAL_PART(builder *flatbuffers.Builder, LOCAL_PART flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(2, flatbuffers.UOffsetT(LOCAL_PART), 0)
}
func VCFAliasAddLocalPart(builder *flatbuffers.Builder, LOCAL_PART flatbuffers.UOffsetT) {
	VCFAliasAddLOCAL_PART(builder, LOCAL_PART)
}
func VCFAliasAddDOMAIN(builder *flatbuffers.Builder, DOMAIN flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(3, flatbuffers.UOffsetT(DOMAIN), 0)
}
func VCFAliasAddDomain(builder *flatbuffers.Builder, DOMAIN flatbuffers.UOffsetT) {
	VCFAliasAddDOMAIN(builder, DOMAIN)
}
func VCFAliasAddADDRESS(builder *flatbuffers.Builder, ADDRESS flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(4, flatbuffers.UOffsetT(ADDRESS), 0)
}
func VCFAliasAddAddress(builder *flatbuffers.Builder, ADDRESS flatbuffers.UOffsetT) {
	VCFAliasAddADDRESS(builder, ADDRESS)
}
func VCFAliasAddENCODING(builder *flatbuffers.Builder, ENCODING flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(5, flatbuffers.UOffsetT(ENCODING), 0)
}
func VCFAliasAddEncoding(builder *flatbuffers.Builder, ENCODING flatbuffers.UOffsetT) {
	VCFAliasAddENCODING(builder, ENCODING)
}
func VCFAliasAddALGORITHM(builder *flatbuffers.Builder, ALGORITHM flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(6, flatbuffers.UOffsetT(ALGORITHM), 0)
}
func VCFAliasAddAlgorithm(builder *flatbuffers.Builder, ALGORITHM flatbuffers.UOffsetT) {
	VCFAliasAddALGORITHM(builder, ALGORITHM)
}
func VCFAliasAddPUBLIC_KEY(builder *flatbuffers.Builder, PUBLIC_KEY flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(7, flatbuffers.UOffsetT(PUBLIC_KEY), 0)
}
func VCFAliasAddPublicKey(builder *flatbuffers.Builder, PUBLIC_KEY flatbuffers.UOffsetT) {
	VCFAliasAddPUBLIC_KEY(builder, PUBLIC_KEY)
}
func VCFAliasAddCHAIN(builder *flatbuffers.Builder, CHAIN flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(8, flatbuffers.UOffsetT(CHAIN), 0)
}
func VCFAliasAddChain(builder *flatbuffers.Builder, CHAIN flatbuffers.UOffsetT) {
	VCFAliasAddCHAIN(builder, CHAIN)
}
func VCFAliasAddON_COMPACT_CARD(builder *flatbuffers.Builder, ON_COMPACT_CARD bool) {
	builder.PrependBoolSlot(9, ON_COMPACT_CARD, false)
}
func VCFAliasAddOnCompactCard(builder *flatbuffers.Builder, ON_COMPACT_CARD bool) {
	VCFAliasAddON_COMPACT_CARD(builder, ON_COMPACT_CARD)
}
func VCFAliasAddOCTET_LENGTH(builder *flatbuffers.Builder, OCTET_LENGTH uint32) {
	builder.PrependUint32Slot(10, OCTET_LENGTH, 0)
}
func VCFAliasAddOctetLength(builder *flatbuffers.Builder, OCTET_LENGTH uint32) {
	VCFAliasAddOCTET_LENGTH(builder, OCTET_LENGTH)
}
func VCFAliasEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
