// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package BPF

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

/// Build Profile — one saved, portable build configuration.
///
/// PORTABILITY. The export unit is a `$REC` collection carrying this record
/// and, optionally, an `$APP` manifest that names it through an `APPDataRef`
/// with `SDS_TYPE: "BPF"`. The `$APP` wrapper is launcher metadata and
/// carries no authority; all authority over a profile lives in
/// `ATTESTATION`. A bare size-prefixed `$BPF` buffer is also a complete,
/// valid export.
type BPF struct {
	_tab flatbuffers.Table
}

const BPFIdentifier = "$BPF"

func GetRootAsBPF(buf []byte, offset flatbuffers.UOffsetT) *BPF {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &BPF{}
	x.Init(buf, n+offset)
	return x
}

func FinishBPFBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	identifierBytes := []byte(BPFIdentifier)
	builder.FinishWithFileIdentifier(offset, identifierBytes)
}

func BPFBufferHasIdentifier(buf []byte) bool {
	return flatbuffers.BufferHasIdentifier(buf, BPFIdentifier)
}

func GetSizePrefixedRootAsBPF(buf []byte, offset flatbuffers.UOffsetT) *BPF {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &BPF{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedBPFBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	identifierBytes := []byte(BPFIdentifier)
	builder.FinishSizePrefixedWithFileIdentifier(offset, identifierBytes)
}

func SizePrefixedBPFBufferHasIdentifier(buf []byte) bool {
	return flatbuffers.SizePrefixedBufferHasIdentifier(buf, BPFIdentifier)
}

func (rcv *BPF) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *BPF) Table() flatbuffers.Table {
	return rcv._tab
}

/// Stable profile identity, unique per author. Required.
func (rcv *BPF) PROFILE_ID() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *BPF) ProfileId() []byte {
	return rcv.PROFILE_ID()
}

/// Stable profile identity, unique per author. Required.
/// Display name of the profile. Required.
func (rcv *BPF) NAME() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *BPF) Name() []byte {
	return rcv.NAME()
}

/// Display name of the profile. Required.
/// Human-readable summary.
func (rcv *BPF) DESCRIPTION() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *BPF) Description() []byte {
	return rcv.DESCRIPTION()
}

/// Human-readable summary.
/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the profile was created.
func (rcv *BPF) CREATED_AT() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *BPF) CreatedAt() []byte {
	return rcv.CREATED_AT()
}

/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the profile was created.
/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the profile was last updated.
func (rcv *BPF) UPDATED_AT() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *BPF) UpdatedAt() []byte {
	return rcv.UPDATED_AT()
}

/// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
/// when the profile was last updated.
/// 64 lowercase hexadecimal characters encoding SHA-256 of the build
/// template this profile was authored against. Required: a profile that
/// does not say which template it configures cannot be validated against
/// one. A consumer holding a different template MAY still import the
/// profile but MUST mark it as authored against another template and MUST
/// NOT silently re-map its parts.
func (rcv *BPF) TEMPLATE_SHA256() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *BPF) TemplateSha256() []byte {
	return rcv.TEMPLATE_SHA256()
}

/// 64 lowercase hexadecimal characters encoding SHA-256 of the build
/// template this profile was authored against. Required: a profile that
/// does not say which template it configures cannot be validated against
/// one. A consumer holding a different template MAY still import the
/// profile but MUST mark it as authored against another template and MUST
/// NOT silently re-map its parts.
/// Template slots and their inclusion decisions. A slot the template
/// exposes and this vector omits is NOT included.
func (rcv *BPF) PARTS(obj *BPFPart, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		if obj == nil {
			obj = new(BPFPart)
		}
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *BPF) Parts(obj *BPFPart, j int) bool {
	return rcv.PARTS(obj, j)
}

func (rcv *BPF) PARTSByKey(obj *BPFPart, key string) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *BPF) PartsByKey(obj *BPFPart, key string) bool {
	return rcv.PARTSByKey(obj, key)
}

func (rcv *BPF) PARTSLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *BPF) PartsLength() int {
	return rcv.PARTSLength()
}

/// Template slots and their inclusion decisions. A slot the template
/// exposes and this vector omits is NOT included.
/// Modules selected into the build and their protection tiers.
func (rcv *BPF) MODULES(obj *BPFModule, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		if obj == nil {
			obj = new(BPFModule)
		}
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *BPF) Modules(obj *BPFModule, j int) bool {
	return rcv.MODULES(obj, j)
}

func (rcv *BPF) MODULESByKey(obj *BPFModule, key string) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *BPF) ModulesByKey(obj *BPFModule, key string) bool {
	return rcv.MODULESByKey(obj, key)
}

func (rcv *BPF) MODULESLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *BPF) ModulesLength() int {
	return rcv.MODULESLength()
}

/// Modules selected into the build and their protection tiers.
/// The runtime restriction the produced artifact enforces. Required, so
/// that an unlocked build is a stated configuration rather than a missing
/// one.
func (rcv *BPF) RUNTIME_LOCK(obj *BPFRuntimeLock) *BPFRuntimeLock {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(20))
	if o != 0 {
		x := rcv._tab.Indirect(o + rcv._tab.Pos)
		if obj == nil {
			obj = new(BPFRuntimeLock)
		}
		obj.Init(rcv._tab.Bytes, x)
		return obj
	}
	return nil
}

func (rcv *BPF) RuntimeLock(obj *BPFRuntimeLock) *BPFRuntimeLock {
	return rcv.RUNTIME_LOCK(obj)
}

/// The runtime restriction the produced artifact enforces. Required, so
/// that an unlocked build is a stated configuration rather than a missing
/// one.
/// Distribution-control mode of the produced artifact.
func (rcv *BPF) LICENSE_MODE() bpfLicenseMode {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(22))
	if o != 0 {
		return bpfLicenseMode(rcv._tab.GetInt8(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *BPF) LicenseMode() bpfLicenseMode {
	return rcv.LICENSE_MODE()
}

/// Distribution-control mode of the produced artifact.
func (rcv *BPF) MutateLICENSE_MODE(n bpfLicenseMode) bool {
	return rcv._tab.MutateInt8Slot(22, int8(n))
}

func (rcv *BPF) MutateLicenseMode(n bpfLicenseMode) bool {
	return rcv.MutateLICENSE_MODE(n)
}

/// Publisher attestation. Absent means the profile is unsigned; see
/// `BPFAttestation` for the fail-closed rule when it is present.
func (rcv *BPF) ATTESTATION(obj *BPFAttestation) *BPFAttestation {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(24))
	if o != 0 {
		x := rcv._tab.Indirect(o + rcv._tab.Pos)
		if obj == nil {
			obj = new(BPFAttestation)
		}
		obj.Init(rcv._tab.Bytes, x)
		return obj
	}
	return nil
}

func (rcv *BPF) Attestation(obj *BPFAttestation) *BPFAttestation {
	return rcv.ATTESTATION(obj)
}

/// Publisher attestation. Absent means the profile is unsigned; see
/// `BPFAttestation` for the fail-closed rule when it is present.
func BPFStart(builder *flatbuffers.Builder) {
	builder.StartObject(11)
}
func BPFAddPROFILE_ID(builder *flatbuffers.Builder, PROFILE_ID flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(0, flatbuffers.UOffsetT(PROFILE_ID), 0)
}
func BPFAddProfileId(builder *flatbuffers.Builder, PROFILE_ID flatbuffers.UOffsetT) {
	BPFAddPROFILE_ID(builder, PROFILE_ID)
}
func BPFAddNAME(builder *flatbuffers.Builder, NAME flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(1, flatbuffers.UOffsetT(NAME), 0)
}
func BPFAddName(builder *flatbuffers.Builder, NAME flatbuffers.UOffsetT) {
	BPFAddNAME(builder, NAME)
}
func BPFAddDESCRIPTION(builder *flatbuffers.Builder, DESCRIPTION flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(2, flatbuffers.UOffsetT(DESCRIPTION), 0)
}
func BPFAddDescription(builder *flatbuffers.Builder, DESCRIPTION flatbuffers.UOffsetT) {
	BPFAddDESCRIPTION(builder, DESCRIPTION)
}
func BPFAddCREATED_AT(builder *flatbuffers.Builder, CREATED_AT flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(3, flatbuffers.UOffsetT(CREATED_AT), 0)
}
func BPFAddCreatedAt(builder *flatbuffers.Builder, CREATED_AT flatbuffers.UOffsetT) {
	BPFAddCREATED_AT(builder, CREATED_AT)
}
func BPFAddUPDATED_AT(builder *flatbuffers.Builder, UPDATED_AT flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(4, flatbuffers.UOffsetT(UPDATED_AT), 0)
}
func BPFAddUpdatedAt(builder *flatbuffers.Builder, UPDATED_AT flatbuffers.UOffsetT) {
	BPFAddUPDATED_AT(builder, UPDATED_AT)
}
func BPFAddTEMPLATE_SHA256(builder *flatbuffers.Builder, TEMPLATE_SHA256 flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(5, flatbuffers.UOffsetT(TEMPLATE_SHA256), 0)
}
func BPFAddTemplateSha256(builder *flatbuffers.Builder, TEMPLATE_SHA256 flatbuffers.UOffsetT) {
	BPFAddTEMPLATE_SHA256(builder, TEMPLATE_SHA256)
}
func BPFAddPARTS(builder *flatbuffers.Builder, PARTS flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(6, flatbuffers.UOffsetT(PARTS), 0)
}
func BPFAddParts(builder *flatbuffers.Builder, PARTS flatbuffers.UOffsetT) {
	BPFAddPARTS(builder, PARTS)
}
func BPFStartPARTSVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func BPFStartPartsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return BPFStartPARTSVector(builder, numElems)
}
func BPFAddMODULES(builder *flatbuffers.Builder, MODULES flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(7, flatbuffers.UOffsetT(MODULES), 0)
}
func BPFAddModules(builder *flatbuffers.Builder, MODULES flatbuffers.UOffsetT) {
	BPFAddMODULES(builder, MODULES)
}
func BPFStartMODULESVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func BPFStartModulesVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return BPFStartMODULESVector(builder, numElems)
}
func BPFAddRUNTIME_LOCK(builder *flatbuffers.Builder, RUNTIME_LOCK flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(8, flatbuffers.UOffsetT(RUNTIME_LOCK), 0)
}
func BPFAddRuntimeLock(builder *flatbuffers.Builder, RUNTIME_LOCK flatbuffers.UOffsetT) {
	BPFAddRUNTIME_LOCK(builder, RUNTIME_LOCK)
}
func BPFAddLICENSE_MODE(builder *flatbuffers.Builder, LICENSE_MODE bpfLicenseMode) {
	builder.PrependInt8Slot(9, int8(LICENSE_MODE), 0)
}
func BPFAddLicenseMode(builder *flatbuffers.Builder, LICENSE_MODE bpfLicenseMode) {
	BPFAddLICENSE_MODE(builder, LICENSE_MODE)
}
func BPFAddATTESTATION(builder *flatbuffers.Builder, ATTESTATION flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(10, flatbuffers.UOffsetT(ATTESTATION), 0)
}
func BPFAddAttestation(builder *flatbuffers.Builder, ATTESTATION flatbuffers.UOffsetT) {
	BPFAddATTESTATION(builder, ATTESTATION)
}
func BPFEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
