// automatically generated by the FlatBuffers compiler, do not modify
// @generated

extern crate alloc;

#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MIN_IRM_JOB_STATE: i8 = 0;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MAX_IRM_JOB_STATE: i8 = 5;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
#[allow(non_camel_case_types)]
pub const ENUM_VALUES_IRM_JOB_STATE: [irmJobState; 6] = [
  irmJobState::UNSPECIFIED,
  irmJobState::IN_PROGRESS,
  irmJobState::COMPLETE,
  irmJobState::FAILED,
  irmJobState::ABANDONED,
  irmJobState::INVALIDATED,
];

/// Lifecycle of the job the mark describes.
#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
#[repr(transparent)]
pub struct irmJobState(pub i8);
#[allow(non_upper_case_globals)]
impl irmJobState {
  pub const UNSPECIFIED: Self = Self(0);
  /// Chunks remain; NEXT_OFFSET is where the next read starts.
  pub const IN_PROGRESS: Self = Self(1);
  /// The source was read to its end and every chunk was committed. NEXT_OFFSET
  /// equals the total length when the length is known.
  pub const COMPLETE: Self = Self(2);
  /// The publisher stopped on an error it did not recover from; FAULT states
  /// what happened. The mark stays valid as a resume point.
  pub const FAILED: Self = Self(3);
  /// The publisher abandoned the job deliberately; it is not a resume point and
  /// a consumer restarts from offset 0.
  pub const ABANDONED: Self = Self(4);
  /// The source changed identity underneath the job (VALIDATOR mismatch), so the
  /// position no longer refers to the same bytes. A conforming consumer restarts
  /// from offset 0 rather than resuming.
  pub const INVALIDATED: Self = Self(5);

  pub const ENUM_MIN: i8 = 0;
  pub const ENUM_MAX: i8 = 5;
  pub const ENUM_VALUES: &'static [Self] = &[
    Self::UNSPECIFIED,
    Self::IN_PROGRESS,
    Self::COMPLETE,
    Self::FAILED,
    Self::ABANDONED,
    Self::INVALIDATED,
  ];
  /// Returns the variant's name or "" if unknown.
  pub fn variant_name(self) -> Option<&'static str> {
    match self {
      Self::UNSPECIFIED => Some("UNSPECIFIED"),
      Self::IN_PROGRESS => Some("IN_PROGRESS"),
      Self::COMPLETE => Some("COMPLETE"),
      Self::FAILED => Some("FAILED"),
      Self::ABANDONED => Some("ABANDONED"),
      Self::INVALIDATED => Some("INVALIDATED"),
      _ => None,
    }
  }
}
impl ::core::fmt::Debug for irmJobState {
  fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
    if let Some(name) = self.variant_name() {
      f.write_str(name)
    } else {
      f.write_fmt(format_args!("<UNKNOWN {:?}>", self.0))
    }
  }
}
impl<'a> ::flatbuffers::Follow<'a> for irmJobState {
  type Inner = Self;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    let b = unsafe { ::flatbuffers::read_scalar_at::<i8>(buf, loc) };
    Self(b)
  }
}

impl ::flatbuffers::Push for irmJobState {
    type Output = irmJobState;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        unsafe { ::flatbuffers::emplace_scalar::<i8>(dst, self.0) };
    }
}

impl ::flatbuffers::EndianScalar for irmJobState {
  type Scalar = i8;
  #[inline]
  fn to_little_endian(self) -> i8 {
    self.0.to_le()
  }
  #[inline]
  #[allow(clippy::wrong_self_convention)]
  fn from_little_endian(v: i8) -> Self {
    let b = i8::from_le(v);
    Self(b)
  }
}

impl<'a> ::flatbuffers::Verifiable for irmJobState {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    i8::run_verifier(v, pos)
  }
}

impl ::flatbuffers::SimpleToVerifyInSlice for irmJobState {}
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MIN_IRM_RANGE_MODE: i8 = 0;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MAX_IRM_RANGE_MODE: i8 = 3;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
#[allow(non_camel_case_types)]
pub const ENUM_VALUES_IRM_RANGE_MODE: [irmRangeMode; 4] = [
  irmRangeMode::UNSPECIFIED,
  irmRangeMode::OFFSET_LENGTH,
  irmRangeMode::INCLUSIVE_BYTE_RANGE,
  irmRangeMode::PART_INDEX,
];

/// How the reader addresses the source's byte ranges.
#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
#[repr(transparent)]
pub struct irmRangeMode(pub i8);
#[allow(non_upper_case_globals)]
impl irmRangeMode {
  pub const UNSPECIFIED: Self = Self(0);
  /// Half-open ranges expressed as a start offset and a byte count.
  pub const OFFSET_LENGTH: Self = Self(1);
  /// Inclusive first/last byte positions, the convention of ranged transfer
  /// protocols: a chunk of `RANGE_FIRST_BYTE`..`RANGE_LAST_BYTE` inclusive.
  pub const INCLUSIVE_BYTE_RANGE: Self = Self(2);
  /// The source is delivered as discrete numbered parts and byte offsets are not
  /// addressable; CHUNK_INDEX is the only position.
  pub const PART_INDEX: Self = Self(3);

  pub const ENUM_MIN: i8 = 0;
  pub const ENUM_MAX: i8 = 3;
  pub const ENUM_VALUES: &'static [Self] = &[
    Self::UNSPECIFIED,
    Self::OFFSET_LENGTH,
    Self::INCLUSIVE_BYTE_RANGE,
    Self::PART_INDEX,
  ];
  /// Returns the variant's name or "" if unknown.
  pub fn variant_name(self) -> Option<&'static str> {
    match self {
      Self::UNSPECIFIED => Some("UNSPECIFIED"),
      Self::OFFSET_LENGTH => Some("OFFSET_LENGTH"),
      Self::INCLUSIVE_BYTE_RANGE => Some("INCLUSIVE_BYTE_RANGE"),
      Self::PART_INDEX => Some("PART_INDEX"),
      _ => None,
    }
  }
}
impl ::core::fmt::Debug for irmRangeMode {
  fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
    if let Some(name) = self.variant_name() {
      f.write_str(name)
    } else {
      f.write_fmt(format_args!("<UNKNOWN {:?}>", self.0))
    }
  }
}
impl<'a> ::flatbuffers::Follow<'a> for irmRangeMode {
  type Inner = Self;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    let b = unsafe { ::flatbuffers::read_scalar_at::<i8>(buf, loc) };
    Self(b)
  }
}

impl ::flatbuffers::Push for irmRangeMode {
    type Output = irmRangeMode;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        unsafe { ::flatbuffers::emplace_scalar::<i8>(dst, self.0) };
    }
}

impl ::flatbuffers::EndianScalar for irmRangeMode {
  type Scalar = i8;
  #[inline]
  fn to_little_endian(self) -> i8 {
    self.0.to_le()
  }
  #[inline]
  #[allow(clippy::wrong_self_convention)]
  fn from_little_endian(v: i8) -> Self {
    let b = i8::from_le(v);
    Self(b)
  }
}

impl<'a> ::flatbuffers::Verifiable for irmRangeMode {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    i8::run_verifier(v, pos)
  }
}

impl ::flatbuffers::SimpleToVerifyInSlice for irmRangeMode {}
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MIN_IRM_VALIDATOR_MATCH: i8 = 0;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MAX_IRM_VALIDATOR_MATCH: i8 = 3;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
#[allow(non_camel_case_types)]
pub const ENUM_VALUES_IRM_VALIDATOR_MATCH: [irmValidatorMatch; 4] = [
  irmValidatorMatch::UNSPECIFIED,
  irmValidatorMatch::MATCHED,
  irmValidatorMatch::MISMATCHED,
  irmValidatorMatch::UNVERIFIABLE,
];

/// What a publisher may conclude when it re-reads the source's validators.
#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
#[repr(transparent)]
pub struct irmValidatorMatch(pub i8);
#[allow(non_upper_case_globals)]
impl irmValidatorMatch {
  pub const UNSPECIFIED: Self = Self(0);
  /// The publisher compared validators and they agree; resuming is safe.
  pub const MATCHED: Self = Self(1);
  /// The publisher compared validators and they disagree; the bytes behind the
  /// offset are not the bytes that were read.
  pub const MISMATCHED: Self = Self(2);
  /// The source published no validator to compare. This is NOT a match: a
  /// publisher that cannot verify identity says so instead of assuming it.
  pub const UNVERIFIABLE: Self = Self(3);

  pub const ENUM_MIN: i8 = 0;
  pub const ENUM_MAX: i8 = 3;
  pub const ENUM_VALUES: &'static [Self] = &[
    Self::UNSPECIFIED,
    Self::MATCHED,
    Self::MISMATCHED,
    Self::UNVERIFIABLE,
  ];
  /// Returns the variant's name or "" if unknown.
  pub fn variant_name(self) -> Option<&'static str> {
    match self {
      Self::UNSPECIFIED => Some("UNSPECIFIED"),
      Self::MATCHED => Some("MATCHED"),
      Self::MISMATCHED => Some("MISMATCHED"),
      Self::UNVERIFIABLE => Some("UNVERIFIABLE"),
      _ => None,
    }
  }
}
impl ::core::fmt::Debug for irmValidatorMatch {
  fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
    if let Some(name) = self.variant_name() {
      f.write_str(name)
    } else {
      f.write_fmt(format_args!("<UNKNOWN {:?}>", self.0))
    }
  }
}
impl<'a> ::flatbuffers::Follow<'a> for irmValidatorMatch {
  type Inner = Self;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    let b = unsafe { ::flatbuffers::read_scalar_at::<i8>(buf, loc) };
    Self(b)
  }
}

impl ::flatbuffers::Push for irmValidatorMatch {
    type Output = irmValidatorMatch;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        unsafe { ::flatbuffers::emplace_scalar::<i8>(dst, self.0) };
    }
}

impl ::flatbuffers::EndianScalar for irmValidatorMatch {
  type Scalar = i8;
  #[inline]
  fn to_little_endian(self) -> i8 {
    self.0.to_le()
  }
  #[inline]
  #[allow(clippy::wrong_self_convention)]
  fn from_little_endian(v: i8) -> Self {
    let b = i8::from_le(v);
    Self(b)
  }
}

impl<'a> ::flatbuffers::Verifiable for irmValidatorMatch {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    i8::run_verifier(v, pos)
  }
}

impl ::flatbuffers::SimpleToVerifyInSlice for irmValidatorMatch {}
pub enum IRMSourceOffset {}
#[derive(Copy, Clone, PartialEq)]

/// The source object a job is reading, and the validators that prove a resumed
/// read is reading the same bytes.
pub struct IRMSource<'a> {
  pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for IRMSource<'a> {
  type Inner = IRMSource<'a>;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    Self { _tab: unsafe { ::flatbuffers::Table::new(buf, loc) } }
  }
}

impl<'a> IRMSource<'a> {
  pub const VT_SOURCE_URL: ::flatbuffers::VOffsetT = 4;
  pub const VT_SOURCE_OBJECT_ID: ::flatbuffers::VOffsetT = 6;
  pub const VT_SOURCE_CID: ::flatbuffers::VOffsetT = 8;
  pub const VT_MEDIA_TYPE: ::flatbuffers::VOffsetT = 10;
  pub const VT_TOTAL_BYTES: ::flatbuffers::VOffsetT = 12;
  pub const VT_ENTITY_TAG: ::flatbuffers::VOffsetT = 14;
  pub const VT_LAST_MODIFIED: ::flatbuffers::VOffsetT = 16;
  pub const VT_SOURCE_SHA256: ::flatbuffers::VOffsetT = 18;
  pub const VT_SOURCE_VERSION: ::flatbuffers::VOffsetT = 20;
  pub const VT_VALIDATOR_MATCH: ::flatbuffers::VOffsetT = 22;
  pub const VT_VALIDATED_AT: ::flatbuffers::VOffsetT = 24;

  #[inline]
  pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
    IRMSource { _tab: table }
  }
  #[allow(unused_mut)]
  pub fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: ::flatbuffers::Allocator + 'bldr>(
    _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
    args: &'args IRMSourceArgs<'args>
  ) -> ::flatbuffers::WIPOffset<IRMSource<'bldr>> {
    let mut builder = IRMSourceBuilder::new(_fbb);
    builder.add_TOTAL_BYTES(args.TOTAL_BYTES);
    if let Some(x) = args.VALIDATED_AT { builder.add_VALIDATED_AT(x); }
    if let Some(x) = args.SOURCE_VERSION { builder.add_SOURCE_VERSION(x); }
    if let Some(x) = args.SOURCE_SHA256 { builder.add_SOURCE_SHA256(x); }
    if let Some(x) = args.LAST_MODIFIED { builder.add_LAST_MODIFIED(x); }
    if let Some(x) = args.ENTITY_TAG { builder.add_ENTITY_TAG(x); }
    if let Some(x) = args.MEDIA_TYPE { builder.add_MEDIA_TYPE(x); }
    if let Some(x) = args.SOURCE_CID { builder.add_SOURCE_CID(x); }
    if let Some(x) = args.SOURCE_OBJECT_ID { builder.add_SOURCE_OBJECT_ID(x); }
    if let Some(x) = args.SOURCE_URL { builder.add_SOURCE_URL(x); }
    builder.add_VALIDATOR_MATCH(args.VALIDATOR_MATCH);
    builder.finish()
  }

  pub fn unpack(&self) -> IRMSourceT {
    let SOURCE_URL = {
      let x = self.SOURCE_URL();
      alloc::string::ToString::to_string(x)
    };
    let SOURCE_OBJECT_ID = self.SOURCE_OBJECT_ID().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let SOURCE_CID = self.SOURCE_CID().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let MEDIA_TYPE = self.MEDIA_TYPE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let TOTAL_BYTES = self.TOTAL_BYTES();
    let ENTITY_TAG = self.ENTITY_TAG().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let LAST_MODIFIED = self.LAST_MODIFIED().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let SOURCE_SHA256 = self.SOURCE_SHA256().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let SOURCE_VERSION = self.SOURCE_VERSION().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let VALIDATOR_MATCH = self.VALIDATOR_MATCH();
    let VALIDATED_AT = self.VALIDATED_AT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    IRMSourceT {
      SOURCE_URL,
      SOURCE_OBJECT_ID,
      SOURCE_CID,
      MEDIA_TYPE,
      TOTAL_BYTES,
      ENTITY_TAG,
      LAST_MODIFIED,
      SOURCE_SHA256,
      SOURCE_VERSION,
      VALIDATOR_MATCH,
      VALIDATED_AT,
    }
  }

  /// Replayable locator of the source object, verbatim.
  #[inline]
  pub fn SOURCE_URL(&self) -> &'a str {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_SOURCE_URL, None).unwrap()}
  }
  /// Source-native object, export, or product identifier verbatim, when the
  /// provider names one independently of its locator.
  #[inline]
  pub fn SOURCE_OBJECT_ID(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_SOURCE_OBJECT_ID, None)}
  }
  /// Content identifier pinning the exact source bytes, when the source is
  /// content-addressed.
  #[inline]
  pub fn SOURCE_CID(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_SOURCE_CID, None)}
  }
  /// Media type of the source object verbatim as the provider states it.
  #[inline]
  pub fn MEDIA_TYPE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_MEDIA_TYPE, None)}
  }
  /// Total length of the source object in bytes. 0 means the provider did not
  /// state a length; it never means an empty object.
  #[inline]
  pub fn TOTAL_BYTES(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMSource::VT_TOTAL_BYTES, Some(0)).unwrap()}
  }
  /// Opaque entity validator verbatim as the provider published it.
  #[inline]
  pub fn ENTITY_TAG(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_ENTITY_TAG, None)}
  }
  /// Provider-stated last-modification timestamp verbatim.
  #[inline]
  pub fn LAST_MODIFIED(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_LAST_MODIFIED, None)}
  }
  /// SHA-256 of the complete source object as 64 lowercase hexadecimal
  /// characters, when the provider publishes one. Never computed from a partial
  /// read.
  #[inline]
  pub fn SOURCE_SHA256(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_SOURCE_SHA256, None)}
  }
  /// Provider-stated edition, revision, or release marker verbatim.
  #[inline]
  pub fn SOURCE_VERSION(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_SOURCE_VERSION, None)}
  }
  /// What the publisher concluded when it last compared validators.
  #[inline]
  pub fn VALIDATOR_MATCH(&self) -> irmValidatorMatch {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<irmValidatorMatch>(IRMSource::VT_VALIDATOR_MATCH, Some(irmValidatorMatch::UNSPECIFIED)).unwrap()}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp of that comparison.
  #[inline]
  pub fn VALIDATED_AT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMSource::VT_VALIDATED_AT, None)}
  }
}

impl ::flatbuffers::Verifiable for IRMSource<'_> {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    v.visit_table(pos)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("SOURCE_URL", Self::VT_SOURCE_URL, true)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("SOURCE_OBJECT_ID", Self::VT_SOURCE_OBJECT_ID, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("SOURCE_CID", Self::VT_SOURCE_CID, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("MEDIA_TYPE", Self::VT_MEDIA_TYPE, false)?
     .visit_field::<u64>("TOTAL_BYTES", Self::VT_TOTAL_BYTES, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("ENTITY_TAG", Self::VT_ENTITY_TAG, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("LAST_MODIFIED", Self::VT_LAST_MODIFIED, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("SOURCE_SHA256", Self::VT_SOURCE_SHA256, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("SOURCE_VERSION", Self::VT_SOURCE_VERSION, false)?
     .visit_field::<irmValidatorMatch>("VALIDATOR_MATCH", Self::VT_VALIDATOR_MATCH, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("VALIDATED_AT", Self::VT_VALIDATED_AT, false)?
     .finish();
    Ok(())
  }
}
pub struct IRMSourceArgs<'a> {
    pub SOURCE_URL: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub SOURCE_OBJECT_ID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub SOURCE_CID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub MEDIA_TYPE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub TOTAL_BYTES: u64,
    pub ENTITY_TAG: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub LAST_MODIFIED: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub SOURCE_SHA256: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub SOURCE_VERSION: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub VALIDATOR_MATCH: irmValidatorMatch,
    pub VALIDATED_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
}
impl<'a> Default for IRMSourceArgs<'a> {
  #[inline]
  fn default() -> Self {
    IRMSourceArgs {
      SOURCE_URL: None, // required field
      SOURCE_OBJECT_ID: None,
      SOURCE_CID: None,
      MEDIA_TYPE: None,
      TOTAL_BYTES: 0,
      ENTITY_TAG: None,
      LAST_MODIFIED: None,
      SOURCE_SHA256: None,
      SOURCE_VERSION: None,
      VALIDATOR_MATCH: irmValidatorMatch::UNSPECIFIED,
      VALIDATED_AT: None,
    }
  }
}

pub struct IRMSourceBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
  fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
  start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> IRMSourceBuilder<'a, 'b, A> {
  #[inline]
  pub fn add_SOURCE_URL(&mut self, SOURCE_URL: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_SOURCE_URL, SOURCE_URL);
  }
  #[inline]
  pub fn add_SOURCE_OBJECT_ID(&mut self, SOURCE_OBJECT_ID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_SOURCE_OBJECT_ID, SOURCE_OBJECT_ID);
  }
  #[inline]
  pub fn add_SOURCE_CID(&mut self, SOURCE_CID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_SOURCE_CID, SOURCE_CID);
  }
  #[inline]
  pub fn add_MEDIA_TYPE(&mut self, MEDIA_TYPE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_MEDIA_TYPE, MEDIA_TYPE);
  }
  #[inline]
  pub fn add_TOTAL_BYTES(&mut self, TOTAL_BYTES: u64) {
    self.fbb_.push_slot::<u64>(IRMSource::VT_TOTAL_BYTES, TOTAL_BYTES, 0);
  }
  #[inline]
  pub fn add_ENTITY_TAG(&mut self, ENTITY_TAG: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_ENTITY_TAG, ENTITY_TAG);
  }
  #[inline]
  pub fn add_LAST_MODIFIED(&mut self, LAST_MODIFIED: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_LAST_MODIFIED, LAST_MODIFIED);
  }
  #[inline]
  pub fn add_SOURCE_SHA256(&mut self, SOURCE_SHA256: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_SOURCE_SHA256, SOURCE_SHA256);
  }
  #[inline]
  pub fn add_SOURCE_VERSION(&mut self, SOURCE_VERSION: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_SOURCE_VERSION, SOURCE_VERSION);
  }
  #[inline]
  pub fn add_VALIDATOR_MATCH(&mut self, VALIDATOR_MATCH: irmValidatorMatch) {
    self.fbb_.push_slot::<irmValidatorMatch>(IRMSource::VT_VALIDATOR_MATCH, VALIDATOR_MATCH, irmValidatorMatch::UNSPECIFIED);
  }
  #[inline]
  pub fn add_VALIDATED_AT(&mut self, VALIDATED_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMSource::VT_VALIDATED_AT, VALIDATED_AT);
  }
  #[inline]
  pub fn new(_fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>) -> IRMSourceBuilder<'a, 'b, A> {
    let start = _fbb.start_table();
    IRMSourceBuilder {
      fbb_: _fbb,
      start_: start,
    }
  }
  #[inline]
  pub fn finish(self) -> ::flatbuffers::WIPOffset<IRMSource<'a>> {
    let o = self.fbb_.end_table(self.start_);
    self.fbb_.required(o, IRMSource::VT_SOURCE_URL,"SOURCE_URL");
    ::flatbuffers::WIPOffset::new(o.value())
  }
}

impl ::core::fmt::Debug for IRMSource<'_> {
  fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
    let mut ds = f.debug_struct("IRMSource");
      ds.field("SOURCE_URL", &self.SOURCE_URL());
      ds.field("SOURCE_OBJECT_ID", &self.SOURCE_OBJECT_ID());
      ds.field("SOURCE_CID", &self.SOURCE_CID());
      ds.field("MEDIA_TYPE", &self.MEDIA_TYPE());
      ds.field("TOTAL_BYTES", &self.TOTAL_BYTES());
      ds.field("ENTITY_TAG", &self.ENTITY_TAG());
      ds.field("LAST_MODIFIED", &self.LAST_MODIFIED());
      ds.field("SOURCE_SHA256", &self.SOURCE_SHA256());
      ds.field("SOURCE_VERSION", &self.SOURCE_VERSION());
      ds.field("VALIDATOR_MATCH", &self.VALIDATOR_MATCH());
      ds.field("VALIDATED_AT", &self.VALIDATED_AT());
      ds.finish()
  }
}
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq)]
pub struct IRMSourceT {
  pub SOURCE_URL: alloc::string::String,
  pub SOURCE_OBJECT_ID: Option<alloc::string::String>,
  pub SOURCE_CID: Option<alloc::string::String>,
  pub MEDIA_TYPE: Option<alloc::string::String>,
  pub TOTAL_BYTES: u64,
  pub ENTITY_TAG: Option<alloc::string::String>,
  pub LAST_MODIFIED: Option<alloc::string::String>,
  pub SOURCE_SHA256: Option<alloc::string::String>,
  pub SOURCE_VERSION: Option<alloc::string::String>,
  pub VALIDATOR_MATCH: irmValidatorMatch,
  pub VALIDATED_AT: Option<alloc::string::String>,
}
impl Default for IRMSourceT {
  fn default() -> Self {
    Self {
      SOURCE_URL: alloc::string::ToString::to_string(""),
      SOURCE_OBJECT_ID: None,
      SOURCE_CID: None,
      MEDIA_TYPE: None,
      TOTAL_BYTES: 0,
      ENTITY_TAG: None,
      LAST_MODIFIED: None,
      SOURCE_SHA256: None,
      SOURCE_VERSION: None,
      VALIDATOR_MATCH: irmValidatorMatch::UNSPECIFIED,
      VALIDATED_AT: None,
    }
  }
}
impl IRMSourceT {
  pub fn pack<'b, A: ::flatbuffers::Allocator + 'b>(
    &self,
    _fbb: &mut ::flatbuffers::FlatBufferBuilder<'b, A>
  ) -> ::flatbuffers::WIPOffset<IRMSource<'b>> {
    let SOURCE_URL = Some({
      let x = &self.SOURCE_URL;
      _fbb.create_string(x)
    });
    let SOURCE_OBJECT_ID = self.SOURCE_OBJECT_ID.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let SOURCE_CID = self.SOURCE_CID.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let MEDIA_TYPE = self.MEDIA_TYPE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let TOTAL_BYTES = self.TOTAL_BYTES;
    let ENTITY_TAG = self.ENTITY_TAG.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let LAST_MODIFIED = self.LAST_MODIFIED.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let SOURCE_SHA256 = self.SOURCE_SHA256.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let SOURCE_VERSION = self.SOURCE_VERSION.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let VALIDATOR_MATCH = self.VALIDATOR_MATCH;
    let VALIDATED_AT = self.VALIDATED_AT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    IRMSource::create(_fbb, &IRMSourceArgs{
      SOURCE_URL,
      SOURCE_OBJECT_ID,
      SOURCE_CID,
      MEDIA_TYPE,
      TOTAL_BYTES,
      ENTITY_TAG,
      LAST_MODIFIED,
      SOURCE_SHA256,
      SOURCE_VERSION,
      VALIDATOR_MATCH,
      VALIDATED_AT,
    })
  }
}
pub enum IRMDecodeContextOffset {}
#[derive(Copy, Clone, PartialEq)]

/// Everything a resuming reader needs to decode a chunk that does not begin at
/// offset 0. Required whenever NEXT_OFFSET is past the source's own header.
pub struct IRMDecodeContext<'a> {
  pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for IRMDecodeContext<'a> {
  type Inner = IRMDecodeContext<'a>;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    Self { _tab: unsafe { ::flatbuffers::Table::new(buf, loc) } }
  }
}

impl<'a> IRMDecodeContext<'a> {
  pub const VT_FORMAT: ::flatbuffers::VOffsetT = 4;
  pub const VT_HEADER_LINE: ::flatbuffers::VOffsetT = 6;
  pub const VT_HEADER_BYTE_LENGTH: ::flatbuffers::VOffsetT = 8;
  pub const VT_COLUMN_NAMES: ::flatbuffers::VOffsetT = 10;
  pub const VT_DELIMITER: ::flatbuffers::VOffsetT = 12;
  pub const VT_QUOTE: ::flatbuffers::VOffsetT = 14;
  pub const VT_ESCAPE: ::flatbuffers::VOffsetT = 16;
  pub const VT_LINE_TERMINATOR: ::flatbuffers::VOffsetT = 18;
  pub const VT_CHARSET: ::flatbuffers::VOffsetT = 20;
  pub const VT_COMPRESSION: ::flatbuffers::VOffsetT = 22;
  pub const VT_DECODED_OFFSET: ::flatbuffers::VOffsetT = 24;
  pub const VT_DECODER_STATE: ::flatbuffers::VOffsetT = 26;
  pub const VT_DECODER_STATE_FORMAT: ::flatbuffers::VOffsetT = 28;
  pub const VT_DECODER_STATE_VERSION: ::flatbuffers::VOffsetT = 30;
  pub const VT_DECODER_STATE_BYTE_LENGTH: ::flatbuffers::VOffsetT = 32;
  pub const VT_DECODER_STATE_SHA256: ::flatbuffers::VOffsetT = 34;
  pub const VT_DECODER_BUILD_ID: ::flatbuffers::VOffsetT = 36;
  pub const VT_DECODER_STATE_MEDIA_TYPE: ::flatbuffers::VOffsetT = 38;
  pub const VT_WINDOW: ::flatbuffers::VOffsetT = 40;
  pub const VT_WINDOW_BYTE_LENGTH: ::flatbuffers::VOffsetT = 42;
  pub const VT_WINDOW_POSITION: ::flatbuffers::VOffsetT = 44;
  pub const VT_PARTIAL_RECORD: ::flatbuffers::VOffsetT = 46;

  #[inline]
  pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
    IRMDecodeContext { _tab: table }
  }
  #[allow(unused_mut)]
  pub fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: ::flatbuffers::Allocator + 'bldr>(
    _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
    args: &'args IRMDecodeContextArgs<'args>
  ) -> ::flatbuffers::WIPOffset<IRMDecodeContext<'bldr>> {
    let mut builder = IRMDecodeContextBuilder::new(_fbb);
    builder.add_WINDOW_POSITION(args.WINDOW_POSITION);
    builder.add_WINDOW_BYTE_LENGTH(args.WINDOW_BYTE_LENGTH);
    builder.add_DECODER_STATE_BYTE_LENGTH(args.DECODER_STATE_BYTE_LENGTH);
    builder.add_DECODED_OFFSET(args.DECODED_OFFSET);
    builder.add_HEADER_BYTE_LENGTH(args.HEADER_BYTE_LENGTH);
    if let Some(x) = args.PARTIAL_RECORD { builder.add_PARTIAL_RECORD(x); }
    if let Some(x) = args.WINDOW { builder.add_WINDOW(x); }
    if let Some(x) = args.DECODER_STATE_MEDIA_TYPE { builder.add_DECODER_STATE_MEDIA_TYPE(x); }
    if let Some(x) = args.DECODER_BUILD_ID { builder.add_DECODER_BUILD_ID(x); }
    if let Some(x) = args.DECODER_STATE_SHA256 { builder.add_DECODER_STATE_SHA256(x); }
    if let Some(x) = args.DECODER_STATE_VERSION { builder.add_DECODER_STATE_VERSION(x); }
    if let Some(x) = args.DECODER_STATE_FORMAT { builder.add_DECODER_STATE_FORMAT(x); }
    if let Some(x) = args.DECODER_STATE { builder.add_DECODER_STATE(x); }
    if let Some(x) = args.COMPRESSION { builder.add_COMPRESSION(x); }
    if let Some(x) = args.CHARSET { builder.add_CHARSET(x); }
    if let Some(x) = args.LINE_TERMINATOR { builder.add_LINE_TERMINATOR(x); }
    if let Some(x) = args.ESCAPE { builder.add_ESCAPE(x); }
    if let Some(x) = args.QUOTE { builder.add_QUOTE(x); }
    if let Some(x) = args.DELIMITER { builder.add_DELIMITER(x); }
    if let Some(x) = args.COLUMN_NAMES { builder.add_COLUMN_NAMES(x); }
    if let Some(x) = args.HEADER_LINE { builder.add_HEADER_LINE(x); }
    if let Some(x) = args.FORMAT { builder.add_FORMAT(x); }
    builder.finish()
  }

  pub fn unpack(&self) -> IRMDecodeContextT {
    let FORMAT = {
      let x = self.FORMAT();
      alloc::string::ToString::to_string(x)
    };
    let HEADER_LINE = self.HEADER_LINE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let HEADER_BYTE_LENGTH = self.HEADER_BYTE_LENGTH();
    let COLUMN_NAMES = self.COLUMN_NAMES().map(|x| {
      x.iter().map(|s| alloc::string::ToString::to_string(s)).collect()
    });
    let DELIMITER = self.DELIMITER().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let QUOTE = self.QUOTE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let ESCAPE = self.ESCAPE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let LINE_TERMINATOR = self.LINE_TERMINATOR().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let CHARSET = self.CHARSET().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let COMPRESSION = self.COMPRESSION().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let DECODED_OFFSET = self.DECODED_OFFSET();
    let DECODER_STATE = self.DECODER_STATE().map(|x| {
      x.into_iter().collect()
    });
    let DECODER_STATE_FORMAT = self.DECODER_STATE_FORMAT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let DECODER_STATE_VERSION = self.DECODER_STATE_VERSION().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let DECODER_STATE_BYTE_LENGTH = self.DECODER_STATE_BYTE_LENGTH();
    let DECODER_STATE_SHA256 = self.DECODER_STATE_SHA256().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let DECODER_BUILD_ID = self.DECODER_BUILD_ID().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let DECODER_STATE_MEDIA_TYPE = self.DECODER_STATE_MEDIA_TYPE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let WINDOW = self.WINDOW().map(|x| {
      x.into_iter().collect()
    });
    let WINDOW_BYTE_LENGTH = self.WINDOW_BYTE_LENGTH();
    let WINDOW_POSITION = self.WINDOW_POSITION();
    let PARTIAL_RECORD = self.PARTIAL_RECORD().map(|x| {
      x.into_iter().collect()
    });
    IRMDecodeContextT {
      FORMAT,
      HEADER_LINE,
      HEADER_BYTE_LENGTH,
      COLUMN_NAMES,
      DELIMITER,
      QUOTE,
      ESCAPE,
      LINE_TERMINATOR,
      CHARSET,
      COMPRESSION,
      DECODED_OFFSET,
      DECODER_STATE,
      DECODER_STATE_FORMAT,
      DECODER_STATE_VERSION,
      DECODER_STATE_BYTE_LENGTH,
      DECODER_STATE_SHA256,
      DECODER_BUILD_ID,
      DECODER_STATE_MEDIA_TYPE,
      WINDOW,
      WINDOW_BYTE_LENGTH,
      WINDOW_POSITION,
      PARTIAL_RECORD,
    }
  }

  /// Format token the decoder dispatches on, verbatim and lowercase, e.g.
  /// `text/csv`. A consumer that does not implement the format refuses the chunk
  /// rather than guessing at a layout.
  #[inline]
  pub fn FORMAT(&self) -> &'a str {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_FORMAT, None).unwrap()}
  }
  /// The source's header line verbatim, exactly as it appeared at offset 0 and
  /// excluding its line terminator. This is the field that makes chunk N>0
  /// decodable; without it a headerless chunk parses to zero rows and looks like
  /// a clean tail.
  #[inline]
  pub fn HEADER_LINE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_HEADER_LINE, None)}
  }
  /// Byte length the header occupied at the start of the source, including its
  /// terminator, so a reader can tell a header-bearing prefix from a data row.
  #[inline]
  pub fn HEADER_BYTE_LENGTH(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMDecodeContext::VT_HEADER_BYTE_LENGTH, Some(0)).unwrap()}
  }
  /// Column names in source order, when the publisher parsed them. Advisory:
  /// HEADER_LINE is authoritative and a disagreement is resolved in its favour.
  #[inline]
  pub fn COLUMN_NAMES(&self) -> Option<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<&'a str>>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<&'a str>>>>(IRMDecodeContext::VT_COLUMN_NAMES, None)}
  }
  /// Field delimiter verbatim.
  #[inline]
  pub fn DELIMITER(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_DELIMITER, None)}
  }
  /// Quote character verbatim.
  #[inline]
  pub fn QUOTE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_QUOTE, None)}
  }
  /// Escape character verbatim.
  #[inline]
  pub fn ESCAPE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_ESCAPE, None)}
  }
  /// Record terminator verbatim, e.g. `\n` or `\r\n`.
  #[inline]
  pub fn LINE_TERMINATOR(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_LINE_TERMINATOR, None)}
  }
  /// Character encoding label verbatim as the provider states it.
  #[inline]
  pub fn CHARSET(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_CHARSET, None)}
  }
  /// Stream-compression codec applied to the source bytes, verbatim and
  /// lowercase. Empty means the ranged bytes are read as-is. Non-empty means
  /// NEXT_OFFSET is a position in the COMPRESSED stream and DECODER_STATE,
  /// WINDOW and PARTIAL_RECORD are all required to resume.
  #[inline]
  pub fn COMPRESSION(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_COMPRESSION, None)}
  }
  /// Position in the DECODED stream corresponding to NEXT_OFFSET, when the
  /// source is compressed. 0 with a non-empty COMPRESSION means the decoded
  /// stream has not been advanced.
  #[inline]
  pub fn DECODED_OFFSET(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMDecodeContext::VT_DECODED_OFFSET, Some(0)).unwrap()}
  }
  /// Serialized, pointer-free image of the decoder at the seam — the state a
  /// codec with no restart point needs in order to have one. Opaque to every
  /// consumer but a decoder whose stamps match exactly. Never partially applied.
  #[inline]
  pub fn DECODER_STATE(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(IRMDecodeContext::VT_DECODER_STATE, None)}
  }
  /// Layout identifier of DECODER_STATE, verbatim, naming the decoder struct the
  /// image was taken from. A consumer that does not know the token restarts.
  #[inline]
  pub fn DECODER_STATE_FORMAT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_DECODER_STATE_FORMAT, None)}
  }
  /// Version of that layout, verbatim. Any disagreement restarts the job.
  #[inline]
  pub fn DECODER_STATE_VERSION(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_DECODER_STATE_VERSION, None)}
  }
  /// Byte length DECODER_STATE is expected to have. Carried even though the
  /// vector knows its own size, so a size disagreement is detectable BEFORE the
  /// image is loaded into a differently shaped struct.
  #[inline]
  pub fn DECODER_STATE_BYTE_LENGTH(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMDecodeContext::VT_DECODER_STATE_BYTE_LENGTH, Some(0)).unwrap()}
  }
  /// SHA-256 of DECODER_STATE as 64 lowercase hexadecimal characters.
  #[inline]
  pub fn DECODER_STATE_SHA256(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_DECODER_STATE_SHA256, None)}
  }
  /// Build identifier of the decoder that produced DECODER_STATE, verbatim, so
  /// two builds of one codec version are still distinguishable.
  #[inline]
  pub fn DECODER_BUILD_ID(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_DECODER_BUILD_ID, None)}
  }
  /// Media type of DECODER_STATE, so an unrelated decoder refuses it instead of
  /// misreading it.
  #[inline]
  pub fn DECODER_STATE_MEDIA_TYPE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMDecodeContext::VT_DECODER_STATE_MEDIA_TYPE, None)}
  }
  /// The decoder's history window at the seam — the bytes that ARE the codec's
  /// back-reference dictionary. Carried separately from DECODER_STATE because it
  /// is bulk data of a size the codec fixes, not part of the struct image.
  #[inline]
  pub fn WINDOW(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(IRMDecodeContext::VT_WINDOW, None)}
  }
  /// Byte length WINDOW is expected to have, for the same detect-before-load
  /// reason as DECODER_STATE_BYTE_LENGTH.
  #[inline]
  pub fn WINDOW_BYTE_LENGTH(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMDecodeContext::VT_WINDOW_BYTE_LENGTH, Some(0)).unwrap()}
  }
  /// Offset into WINDOW at which the codec will next write, when the window is
  /// a circular buffer whose contents cannot be interpreted without it.
  #[inline]
  pub fn WINDOW_POSITION(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMDecodeContext::VT_WINDOW_POSITION, Some(0)).unwrap()}
  }
  /// The bytes of the record that straddles the seam: everything the previous
  /// chunk decoded of a record it could not finish. A resuming decoder prepends
  /// these to the next chunk's decoded output before parsing. Carried as bytes,
  /// not text, because a seam may fall inside a multi-byte character.
  #[inline]
  pub fn PARTIAL_RECORD(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(IRMDecodeContext::VT_PARTIAL_RECORD, None)}
  }
}

impl ::flatbuffers::Verifiable for IRMDecodeContext<'_> {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    v.visit_table(pos)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("FORMAT", Self::VT_FORMAT, true)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("HEADER_LINE", Self::VT_HEADER_LINE, false)?
     .visit_field::<u64>("HEADER_BYTE_LENGTH", Self::VT_HEADER_BYTE_LENGTH, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ::flatbuffers::ForwardsUOffset<&'_ str>>>>("COLUMN_NAMES", Self::VT_COLUMN_NAMES, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("DELIMITER", Self::VT_DELIMITER, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("QUOTE", Self::VT_QUOTE, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("ESCAPE", Self::VT_ESCAPE, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("LINE_TERMINATOR", Self::VT_LINE_TERMINATOR, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("CHARSET", Self::VT_CHARSET, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("COMPRESSION", Self::VT_COMPRESSION, false)?
     .visit_field::<u64>("DECODED_OFFSET", Self::VT_DECODED_OFFSET, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>("DECODER_STATE", Self::VT_DECODER_STATE, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("DECODER_STATE_FORMAT", Self::VT_DECODER_STATE_FORMAT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("DECODER_STATE_VERSION", Self::VT_DECODER_STATE_VERSION, false)?
     .visit_field::<u64>("DECODER_STATE_BYTE_LENGTH", Self::VT_DECODER_STATE_BYTE_LENGTH, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("DECODER_STATE_SHA256", Self::VT_DECODER_STATE_SHA256, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("DECODER_BUILD_ID", Self::VT_DECODER_BUILD_ID, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("DECODER_STATE_MEDIA_TYPE", Self::VT_DECODER_STATE_MEDIA_TYPE, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>("WINDOW", Self::VT_WINDOW, false)?
     .visit_field::<u64>("WINDOW_BYTE_LENGTH", Self::VT_WINDOW_BYTE_LENGTH, false)?
     .visit_field::<u64>("WINDOW_POSITION", Self::VT_WINDOW_POSITION, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>("PARTIAL_RECORD", Self::VT_PARTIAL_RECORD, false)?
     .finish();
    Ok(())
  }
}
pub struct IRMDecodeContextArgs<'a> {
    pub FORMAT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub HEADER_LINE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub HEADER_BYTE_LENGTH: u64,
    pub COLUMN_NAMES: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<&'a str>>>>,
    pub DELIMITER: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub QUOTE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub ESCAPE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub LINE_TERMINATOR: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub CHARSET: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub COMPRESSION: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub DECODED_OFFSET: u64,
    pub DECODER_STATE: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
    pub DECODER_STATE_FORMAT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub DECODER_STATE_VERSION: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub DECODER_STATE_BYTE_LENGTH: u64,
    pub DECODER_STATE_SHA256: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub DECODER_BUILD_ID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub DECODER_STATE_MEDIA_TYPE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub WINDOW: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
    pub WINDOW_BYTE_LENGTH: u64,
    pub WINDOW_POSITION: u64,
    pub PARTIAL_RECORD: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
}
impl<'a> Default for IRMDecodeContextArgs<'a> {
  #[inline]
  fn default() -> Self {
    IRMDecodeContextArgs {
      FORMAT: None, // required field
      HEADER_LINE: None,
      HEADER_BYTE_LENGTH: 0,
      COLUMN_NAMES: None,
      DELIMITER: None,
      QUOTE: None,
      ESCAPE: None,
      LINE_TERMINATOR: None,
      CHARSET: None,
      COMPRESSION: None,
      DECODED_OFFSET: 0,
      DECODER_STATE: None,
      DECODER_STATE_FORMAT: None,
      DECODER_STATE_VERSION: None,
      DECODER_STATE_BYTE_LENGTH: 0,
      DECODER_STATE_SHA256: None,
      DECODER_BUILD_ID: None,
      DECODER_STATE_MEDIA_TYPE: None,
      WINDOW: None,
      WINDOW_BYTE_LENGTH: 0,
      WINDOW_POSITION: 0,
      PARTIAL_RECORD: None,
    }
  }
}

pub struct IRMDecodeContextBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
  fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
  start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> IRMDecodeContextBuilder<'a, 'b, A> {
  #[inline]
  pub fn add_FORMAT(&mut self, FORMAT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_FORMAT, FORMAT);
  }
  #[inline]
  pub fn add_HEADER_LINE(&mut self, HEADER_LINE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_HEADER_LINE, HEADER_LINE);
  }
  #[inline]
  pub fn add_HEADER_BYTE_LENGTH(&mut self, HEADER_BYTE_LENGTH: u64) {
    self.fbb_.push_slot::<u64>(IRMDecodeContext::VT_HEADER_BYTE_LENGTH, HEADER_BYTE_LENGTH, 0);
  }
  #[inline]
  pub fn add_COLUMN_NAMES(&mut self, COLUMN_NAMES: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b , ::flatbuffers::ForwardsUOffset<&'b  str>>>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_COLUMN_NAMES, COLUMN_NAMES);
  }
  #[inline]
  pub fn add_DELIMITER(&mut self, DELIMITER: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_DELIMITER, DELIMITER);
  }
  #[inline]
  pub fn add_QUOTE(&mut self, QUOTE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_QUOTE, QUOTE);
  }
  #[inline]
  pub fn add_ESCAPE(&mut self, ESCAPE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_ESCAPE, ESCAPE);
  }
  #[inline]
  pub fn add_LINE_TERMINATOR(&mut self, LINE_TERMINATOR: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_LINE_TERMINATOR, LINE_TERMINATOR);
  }
  #[inline]
  pub fn add_CHARSET(&mut self, CHARSET: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_CHARSET, CHARSET);
  }
  #[inline]
  pub fn add_COMPRESSION(&mut self, COMPRESSION: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_COMPRESSION, COMPRESSION);
  }
  #[inline]
  pub fn add_DECODED_OFFSET(&mut self, DECODED_OFFSET: u64) {
    self.fbb_.push_slot::<u64>(IRMDecodeContext::VT_DECODED_OFFSET, DECODED_OFFSET, 0);
  }
  #[inline]
  pub fn add_DECODER_STATE(&mut self, DECODER_STATE: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b , u8>>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_DECODER_STATE, DECODER_STATE);
  }
  #[inline]
  pub fn add_DECODER_STATE_FORMAT(&mut self, DECODER_STATE_FORMAT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_DECODER_STATE_FORMAT, DECODER_STATE_FORMAT);
  }
  #[inline]
  pub fn add_DECODER_STATE_VERSION(&mut self, DECODER_STATE_VERSION: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_DECODER_STATE_VERSION, DECODER_STATE_VERSION);
  }
  #[inline]
  pub fn add_DECODER_STATE_BYTE_LENGTH(&mut self, DECODER_STATE_BYTE_LENGTH: u64) {
    self.fbb_.push_slot::<u64>(IRMDecodeContext::VT_DECODER_STATE_BYTE_LENGTH, DECODER_STATE_BYTE_LENGTH, 0);
  }
  #[inline]
  pub fn add_DECODER_STATE_SHA256(&mut self, DECODER_STATE_SHA256: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_DECODER_STATE_SHA256, DECODER_STATE_SHA256);
  }
  #[inline]
  pub fn add_DECODER_BUILD_ID(&mut self, DECODER_BUILD_ID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_DECODER_BUILD_ID, DECODER_BUILD_ID);
  }
  #[inline]
  pub fn add_DECODER_STATE_MEDIA_TYPE(&mut self, DECODER_STATE_MEDIA_TYPE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_DECODER_STATE_MEDIA_TYPE, DECODER_STATE_MEDIA_TYPE);
  }
  #[inline]
  pub fn add_WINDOW(&mut self, WINDOW: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b , u8>>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_WINDOW, WINDOW);
  }
  #[inline]
  pub fn add_WINDOW_BYTE_LENGTH(&mut self, WINDOW_BYTE_LENGTH: u64) {
    self.fbb_.push_slot::<u64>(IRMDecodeContext::VT_WINDOW_BYTE_LENGTH, WINDOW_BYTE_LENGTH, 0);
  }
  #[inline]
  pub fn add_WINDOW_POSITION(&mut self, WINDOW_POSITION: u64) {
    self.fbb_.push_slot::<u64>(IRMDecodeContext::VT_WINDOW_POSITION, WINDOW_POSITION, 0);
  }
  #[inline]
  pub fn add_PARTIAL_RECORD(&mut self, PARTIAL_RECORD: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b , u8>>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMDecodeContext::VT_PARTIAL_RECORD, PARTIAL_RECORD);
  }
  #[inline]
  pub fn new(_fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>) -> IRMDecodeContextBuilder<'a, 'b, A> {
    let start = _fbb.start_table();
    IRMDecodeContextBuilder {
      fbb_: _fbb,
      start_: start,
    }
  }
  #[inline]
  pub fn finish(self) -> ::flatbuffers::WIPOffset<IRMDecodeContext<'a>> {
    let o = self.fbb_.end_table(self.start_);
    self.fbb_.required(o, IRMDecodeContext::VT_FORMAT,"FORMAT");
    ::flatbuffers::WIPOffset::new(o.value())
  }
}

impl ::core::fmt::Debug for IRMDecodeContext<'_> {
  fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
    let mut ds = f.debug_struct("IRMDecodeContext");
      ds.field("FORMAT", &self.FORMAT());
      ds.field("HEADER_LINE", &self.HEADER_LINE());
      ds.field("HEADER_BYTE_LENGTH", &self.HEADER_BYTE_LENGTH());
      ds.field("COLUMN_NAMES", &self.COLUMN_NAMES());
      ds.field("DELIMITER", &self.DELIMITER());
      ds.field("QUOTE", &self.QUOTE());
      ds.field("ESCAPE", &self.ESCAPE());
      ds.field("LINE_TERMINATOR", &self.LINE_TERMINATOR());
      ds.field("CHARSET", &self.CHARSET());
      ds.field("COMPRESSION", &self.COMPRESSION());
      ds.field("DECODED_OFFSET", &self.DECODED_OFFSET());
      ds.field("DECODER_STATE", &self.DECODER_STATE());
      ds.field("DECODER_STATE_FORMAT", &self.DECODER_STATE_FORMAT());
      ds.field("DECODER_STATE_VERSION", &self.DECODER_STATE_VERSION());
      ds.field("DECODER_STATE_BYTE_LENGTH", &self.DECODER_STATE_BYTE_LENGTH());
      ds.field("DECODER_STATE_SHA256", &self.DECODER_STATE_SHA256());
      ds.field("DECODER_BUILD_ID", &self.DECODER_BUILD_ID());
      ds.field("DECODER_STATE_MEDIA_TYPE", &self.DECODER_STATE_MEDIA_TYPE());
      ds.field("WINDOW", &self.WINDOW());
      ds.field("WINDOW_BYTE_LENGTH", &self.WINDOW_BYTE_LENGTH());
      ds.field("WINDOW_POSITION", &self.WINDOW_POSITION());
      ds.field("PARTIAL_RECORD", &self.PARTIAL_RECORD());
      ds.finish()
  }
}
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq)]
pub struct IRMDecodeContextT {
  pub FORMAT: alloc::string::String,
  pub HEADER_LINE: Option<alloc::string::String>,
  pub HEADER_BYTE_LENGTH: u64,
  pub COLUMN_NAMES: Option<alloc::vec::Vec<alloc::string::String>>,
  pub DELIMITER: Option<alloc::string::String>,
  pub QUOTE: Option<alloc::string::String>,
  pub ESCAPE: Option<alloc::string::String>,
  pub LINE_TERMINATOR: Option<alloc::string::String>,
  pub CHARSET: Option<alloc::string::String>,
  pub COMPRESSION: Option<alloc::string::String>,
  pub DECODED_OFFSET: u64,
  pub DECODER_STATE: Option<alloc::vec::Vec<u8>>,
  pub DECODER_STATE_FORMAT: Option<alloc::string::String>,
  pub DECODER_STATE_VERSION: Option<alloc::string::String>,
  pub DECODER_STATE_BYTE_LENGTH: u64,
  pub DECODER_STATE_SHA256: Option<alloc::string::String>,
  pub DECODER_BUILD_ID: Option<alloc::string::String>,
  pub DECODER_STATE_MEDIA_TYPE: Option<alloc::string::String>,
  pub WINDOW: Option<alloc::vec::Vec<u8>>,
  pub WINDOW_BYTE_LENGTH: u64,
  pub WINDOW_POSITION: u64,
  pub PARTIAL_RECORD: Option<alloc::vec::Vec<u8>>,
}
impl Default for IRMDecodeContextT {
  fn default() -> Self {
    Self {
      FORMAT: alloc::string::ToString::to_string(""),
      HEADER_LINE: None,
      HEADER_BYTE_LENGTH: 0,
      COLUMN_NAMES: None,
      DELIMITER: None,
      QUOTE: None,
      ESCAPE: None,
      LINE_TERMINATOR: None,
      CHARSET: None,
      COMPRESSION: None,
      DECODED_OFFSET: 0,
      DECODER_STATE: None,
      DECODER_STATE_FORMAT: None,
      DECODER_STATE_VERSION: None,
      DECODER_STATE_BYTE_LENGTH: 0,
      DECODER_STATE_SHA256: None,
      DECODER_BUILD_ID: None,
      DECODER_STATE_MEDIA_TYPE: None,
      WINDOW: None,
      WINDOW_BYTE_LENGTH: 0,
      WINDOW_POSITION: 0,
      PARTIAL_RECORD: None,
    }
  }
}
impl IRMDecodeContextT {
  pub fn pack<'b, A: ::flatbuffers::Allocator + 'b>(
    &self,
    _fbb: &mut ::flatbuffers::FlatBufferBuilder<'b, A>
  ) -> ::flatbuffers::WIPOffset<IRMDecodeContext<'b>> {
    let FORMAT = Some({
      let x = &self.FORMAT;
      _fbb.create_string(x)
    });
    let HEADER_LINE = self.HEADER_LINE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let HEADER_BYTE_LENGTH = self.HEADER_BYTE_LENGTH;
    let COLUMN_NAMES = self.COLUMN_NAMES.as_ref().map(|x|{
      let w: alloc::vec::Vec<_> = x.iter().map(|s| _fbb.create_string(s)).collect();_fbb.create_vector(&w)
    });
    let DELIMITER = self.DELIMITER.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let QUOTE = self.QUOTE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let ESCAPE = self.ESCAPE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let LINE_TERMINATOR = self.LINE_TERMINATOR.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let CHARSET = self.CHARSET.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let COMPRESSION = self.COMPRESSION.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let DECODED_OFFSET = self.DECODED_OFFSET;
    let DECODER_STATE = self.DECODER_STATE.as_ref().map(|x|{
      _fbb.create_vector(x)
    });
    let DECODER_STATE_FORMAT = self.DECODER_STATE_FORMAT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let DECODER_STATE_VERSION = self.DECODER_STATE_VERSION.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let DECODER_STATE_BYTE_LENGTH = self.DECODER_STATE_BYTE_LENGTH;
    let DECODER_STATE_SHA256 = self.DECODER_STATE_SHA256.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let DECODER_BUILD_ID = self.DECODER_BUILD_ID.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let DECODER_STATE_MEDIA_TYPE = self.DECODER_STATE_MEDIA_TYPE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let WINDOW = self.WINDOW.as_ref().map(|x|{
      _fbb.create_vector(x)
    });
    let WINDOW_BYTE_LENGTH = self.WINDOW_BYTE_LENGTH;
    let WINDOW_POSITION = self.WINDOW_POSITION;
    let PARTIAL_RECORD = self.PARTIAL_RECORD.as_ref().map(|x|{
      _fbb.create_vector(x)
    });
    IRMDecodeContext::create(_fbb, &IRMDecodeContextArgs{
      FORMAT,
      HEADER_LINE,
      HEADER_BYTE_LENGTH,
      COLUMN_NAMES,
      DELIMITER,
      QUOTE,
      ESCAPE,
      LINE_TERMINATOR,
      CHARSET,
      COMPRESSION,
      DECODED_OFFSET,
      DECODER_STATE,
      DECODER_STATE_FORMAT,
      DECODER_STATE_VERSION,
      DECODER_STATE_BYTE_LENGTH,
      DECODER_STATE_SHA256,
      DECODER_BUILD_ID,
      DECODER_STATE_MEDIA_TYPE,
      WINDOW,
      WINDOW_BYTE_LENGTH,
      WINDOW_POSITION,
      PARTIAL_RECORD,
    })
  }
}
pub enum IRMChunkOffset {}
#[derive(Copy, Clone, PartialEq)]

/// One chunk that was read and durably committed.
pub struct IRMChunk<'a> {
  pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for IRMChunk<'a> {
  type Inner = IRMChunk<'a>;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    Self { _tab: unsafe { ::flatbuffers::Table::new(buf, loc) } }
  }
}

impl<'a> IRMChunk<'a> {
  pub const VT_CHUNK_INDEX: ::flatbuffers::VOffsetT = 4;
  pub const VT_BATCH_ID: ::flatbuffers::VOffsetT = 6;
  pub const VT_RANGE_FIRST_BYTE: ::flatbuffers::VOffsetT = 8;
  pub const VT_RANGE_LAST_BYTE: ::flatbuffers::VOffsetT = 10;
  pub const VT_BYTE_LENGTH: ::flatbuffers::VOffsetT = 12;
  pub const VT_CHUNK_SHA256: ::flatbuffers::VOffsetT = 14;
  pub const VT_RECORDS_DECODED: ::flatbuffers::VOffsetT = 16;
  pub const VT_RECORDS_STORED: ::flatbuffers::VOffsetT = 18;
  pub const VT_RECORDS_REJECTED: ::flatbuffers::VOffsetT = 20;
  pub const VT_ATTEMPTS: ::flatbuffers::VOffsetT = 22;
  pub const VT_COMMITTED_AT: ::flatbuffers::VOffsetT = 24;

  #[inline]
  pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
    IRMChunk { _tab: table }
  }
  #[allow(unused_mut)]
  pub fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: ::flatbuffers::Allocator + 'bldr>(
    _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
    args: &'args IRMChunkArgs<'args>
  ) -> ::flatbuffers::WIPOffset<IRMChunk<'bldr>> {
    let mut builder = IRMChunkBuilder::new(_fbb);
    builder.add_BYTE_LENGTH(args.BYTE_LENGTH);
    builder.add_RANGE_LAST_BYTE(args.RANGE_LAST_BYTE);
    builder.add_RANGE_FIRST_BYTE(args.RANGE_FIRST_BYTE);
    if let Some(x) = args.COMMITTED_AT { builder.add_COMMITTED_AT(x); }
    builder.add_ATTEMPTS(args.ATTEMPTS);
    builder.add_RECORDS_REJECTED(args.RECORDS_REJECTED);
    builder.add_RECORDS_STORED(args.RECORDS_STORED);
    builder.add_RECORDS_DECODED(args.RECORDS_DECODED);
    if let Some(x) = args.CHUNK_SHA256 { builder.add_CHUNK_SHA256(x); }
    if let Some(x) = args.BATCH_ID { builder.add_BATCH_ID(x); }
    builder.add_CHUNK_INDEX(args.CHUNK_INDEX);
    builder.finish()
  }

  pub fn unpack(&self) -> IRMChunkT {
    let CHUNK_INDEX = self.CHUNK_INDEX();
    let BATCH_ID = self.BATCH_ID().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let RANGE_FIRST_BYTE = self.RANGE_FIRST_BYTE();
    let RANGE_LAST_BYTE = self.RANGE_LAST_BYTE();
    let BYTE_LENGTH = self.BYTE_LENGTH();
    let CHUNK_SHA256 = self.CHUNK_SHA256().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let RECORDS_DECODED = self.RECORDS_DECODED();
    let RECORDS_STORED = self.RECORDS_STORED();
    let RECORDS_REJECTED = self.RECORDS_REJECTED();
    let ATTEMPTS = self.ATTEMPTS();
    let COMMITTED_AT = self.COMMITTED_AT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    IRMChunkT {
      CHUNK_INDEX,
      BATCH_ID,
      RANGE_FIRST_BYTE,
      RANGE_LAST_BYTE,
      BYTE_LENGTH,
      CHUNK_SHA256,
      RECORDS_DECODED,
      RECORDS_STORED,
      RECORDS_REJECTED,
      ATTEMPTS,
      COMMITTED_AT,
    }
  }

  /// Zero-based position of the chunk in the job's read sequence.
  #[inline]
  pub fn CHUNK_INDEX(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRMChunk::VT_CHUNK_INDEX, Some(0)).unwrap()}
  }
  /// Publisher-stable batch identifier the chunk's records were stored under,
  /// verbatim, so a consumer can join the mark to the stored rows.
  #[inline]
  pub fn BATCH_ID(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMChunk::VT_BATCH_ID, None)}
  }
  /// First byte of the range that was read.
  #[inline]
  pub fn RANGE_FIRST_BYTE(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMChunk::VT_RANGE_FIRST_BYTE, Some(0)).unwrap()}
  }
  /// Last byte of the range that was read. Interpreted per RANGE_MODE:
  /// inclusive under INCLUSIVE_BYTE_RANGE, exclusive under OFFSET_LENGTH.
  #[inline]
  pub fn RANGE_LAST_BYTE(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMChunk::VT_RANGE_LAST_BYTE, Some(0)).unwrap()}
  }
  /// Bytes the read actually returned, which may be shorter than the range
  /// requested.
  #[inline]
  pub fn BYTE_LENGTH(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRMChunk::VT_BYTE_LENGTH, Some(0)).unwrap()}
  }
  /// SHA-256 of the chunk's bytes as 64 lowercase hexadecimal characters.
  #[inline]
  pub fn CHUNK_SHA256(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMChunk::VT_CHUNK_SHA256, None)}
  }
  /// Records the decoder produced from the chunk.
  #[inline]
  pub fn RECORDS_DECODED(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRMChunk::VT_RECORDS_DECODED, Some(0)).unwrap()}
  }
  /// Records the storage lane reported as durably committed. A chunk whose
  /// stored count is 0 is not marked at all.
  #[inline]
  pub fn RECORDS_STORED(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRMChunk::VT_RECORDS_STORED, Some(0)).unwrap()}
  }
  /// Records the storage lane rejected or collapsed as duplicates.
  #[inline]
  pub fn RECORDS_REJECTED(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRMChunk::VT_RECORDS_REJECTED, Some(0)).unwrap()}
  }
  /// Number of read attempts this chunk cost, including the successful one.
  #[inline]
  pub fn ATTEMPTS(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRMChunk::VT_ATTEMPTS, Some(0)).unwrap()}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp at which the storage lane
  /// confirmed the commit.
  #[inline]
  pub fn COMMITTED_AT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMChunk::VT_COMMITTED_AT, None)}
  }
}

impl ::flatbuffers::Verifiable for IRMChunk<'_> {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    v.visit_table(pos)?
     .visit_field::<u32>("CHUNK_INDEX", Self::VT_CHUNK_INDEX, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("BATCH_ID", Self::VT_BATCH_ID, false)?
     .visit_field::<u64>("RANGE_FIRST_BYTE", Self::VT_RANGE_FIRST_BYTE, false)?
     .visit_field::<u64>("RANGE_LAST_BYTE", Self::VT_RANGE_LAST_BYTE, false)?
     .visit_field::<u64>("BYTE_LENGTH", Self::VT_BYTE_LENGTH, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("CHUNK_SHA256", Self::VT_CHUNK_SHA256, false)?
     .visit_field::<u32>("RECORDS_DECODED", Self::VT_RECORDS_DECODED, false)?
     .visit_field::<u32>("RECORDS_STORED", Self::VT_RECORDS_STORED, false)?
     .visit_field::<u32>("RECORDS_REJECTED", Self::VT_RECORDS_REJECTED, false)?
     .visit_field::<u32>("ATTEMPTS", Self::VT_ATTEMPTS, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("COMMITTED_AT", Self::VT_COMMITTED_AT, false)?
     .finish();
    Ok(())
  }
}
pub struct IRMChunkArgs<'a> {
    pub CHUNK_INDEX: u32,
    pub BATCH_ID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub RANGE_FIRST_BYTE: u64,
    pub RANGE_LAST_BYTE: u64,
    pub BYTE_LENGTH: u64,
    pub CHUNK_SHA256: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub RECORDS_DECODED: u32,
    pub RECORDS_STORED: u32,
    pub RECORDS_REJECTED: u32,
    pub ATTEMPTS: u32,
    pub COMMITTED_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
}
impl<'a> Default for IRMChunkArgs<'a> {
  #[inline]
  fn default() -> Self {
    IRMChunkArgs {
      CHUNK_INDEX: 0,
      BATCH_ID: None,
      RANGE_FIRST_BYTE: 0,
      RANGE_LAST_BYTE: 0,
      BYTE_LENGTH: 0,
      CHUNK_SHA256: None,
      RECORDS_DECODED: 0,
      RECORDS_STORED: 0,
      RECORDS_REJECTED: 0,
      ATTEMPTS: 0,
      COMMITTED_AT: None,
    }
  }
}

pub struct IRMChunkBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
  fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
  start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> IRMChunkBuilder<'a, 'b, A> {
  #[inline]
  pub fn add_CHUNK_INDEX(&mut self, CHUNK_INDEX: u32) {
    self.fbb_.push_slot::<u32>(IRMChunk::VT_CHUNK_INDEX, CHUNK_INDEX, 0);
  }
  #[inline]
  pub fn add_BATCH_ID(&mut self, BATCH_ID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMChunk::VT_BATCH_ID, BATCH_ID);
  }
  #[inline]
  pub fn add_RANGE_FIRST_BYTE(&mut self, RANGE_FIRST_BYTE: u64) {
    self.fbb_.push_slot::<u64>(IRMChunk::VT_RANGE_FIRST_BYTE, RANGE_FIRST_BYTE, 0);
  }
  #[inline]
  pub fn add_RANGE_LAST_BYTE(&mut self, RANGE_LAST_BYTE: u64) {
    self.fbb_.push_slot::<u64>(IRMChunk::VT_RANGE_LAST_BYTE, RANGE_LAST_BYTE, 0);
  }
  #[inline]
  pub fn add_BYTE_LENGTH(&mut self, BYTE_LENGTH: u64) {
    self.fbb_.push_slot::<u64>(IRMChunk::VT_BYTE_LENGTH, BYTE_LENGTH, 0);
  }
  #[inline]
  pub fn add_CHUNK_SHA256(&mut self, CHUNK_SHA256: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMChunk::VT_CHUNK_SHA256, CHUNK_SHA256);
  }
  #[inline]
  pub fn add_RECORDS_DECODED(&mut self, RECORDS_DECODED: u32) {
    self.fbb_.push_slot::<u32>(IRMChunk::VT_RECORDS_DECODED, RECORDS_DECODED, 0);
  }
  #[inline]
  pub fn add_RECORDS_STORED(&mut self, RECORDS_STORED: u32) {
    self.fbb_.push_slot::<u32>(IRMChunk::VT_RECORDS_STORED, RECORDS_STORED, 0);
  }
  #[inline]
  pub fn add_RECORDS_REJECTED(&mut self, RECORDS_REJECTED: u32) {
    self.fbb_.push_slot::<u32>(IRMChunk::VT_RECORDS_REJECTED, RECORDS_REJECTED, 0);
  }
  #[inline]
  pub fn add_ATTEMPTS(&mut self, ATTEMPTS: u32) {
    self.fbb_.push_slot::<u32>(IRMChunk::VT_ATTEMPTS, ATTEMPTS, 0);
  }
  #[inline]
  pub fn add_COMMITTED_AT(&mut self, COMMITTED_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMChunk::VT_COMMITTED_AT, COMMITTED_AT);
  }
  #[inline]
  pub fn new(_fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>) -> IRMChunkBuilder<'a, 'b, A> {
    let start = _fbb.start_table();
    IRMChunkBuilder {
      fbb_: _fbb,
      start_: start,
    }
  }
  #[inline]
  pub fn finish(self) -> ::flatbuffers::WIPOffset<IRMChunk<'a>> {
    let o = self.fbb_.end_table(self.start_);
    ::flatbuffers::WIPOffset::new(o.value())
  }
}

impl ::core::fmt::Debug for IRMChunk<'_> {
  fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
    let mut ds = f.debug_struct("IRMChunk");
      ds.field("CHUNK_INDEX", &self.CHUNK_INDEX());
      ds.field("BATCH_ID", &self.BATCH_ID());
      ds.field("RANGE_FIRST_BYTE", &self.RANGE_FIRST_BYTE());
      ds.field("RANGE_LAST_BYTE", &self.RANGE_LAST_BYTE());
      ds.field("BYTE_LENGTH", &self.BYTE_LENGTH());
      ds.field("CHUNK_SHA256", &self.CHUNK_SHA256());
      ds.field("RECORDS_DECODED", &self.RECORDS_DECODED());
      ds.field("RECORDS_STORED", &self.RECORDS_STORED());
      ds.field("RECORDS_REJECTED", &self.RECORDS_REJECTED());
      ds.field("ATTEMPTS", &self.ATTEMPTS());
      ds.field("COMMITTED_AT", &self.COMMITTED_AT());
      ds.finish()
  }
}
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq)]
pub struct IRMChunkT {
  pub CHUNK_INDEX: u32,
  pub BATCH_ID: Option<alloc::string::String>,
  pub RANGE_FIRST_BYTE: u64,
  pub RANGE_LAST_BYTE: u64,
  pub BYTE_LENGTH: u64,
  pub CHUNK_SHA256: Option<alloc::string::String>,
  pub RECORDS_DECODED: u32,
  pub RECORDS_STORED: u32,
  pub RECORDS_REJECTED: u32,
  pub ATTEMPTS: u32,
  pub COMMITTED_AT: Option<alloc::string::String>,
}
impl Default for IRMChunkT {
  fn default() -> Self {
    Self {
      CHUNK_INDEX: 0,
      BATCH_ID: None,
      RANGE_FIRST_BYTE: 0,
      RANGE_LAST_BYTE: 0,
      BYTE_LENGTH: 0,
      CHUNK_SHA256: None,
      RECORDS_DECODED: 0,
      RECORDS_STORED: 0,
      RECORDS_REJECTED: 0,
      ATTEMPTS: 0,
      COMMITTED_AT: None,
    }
  }
}
impl IRMChunkT {
  pub fn pack<'b, A: ::flatbuffers::Allocator + 'b>(
    &self,
    _fbb: &mut ::flatbuffers::FlatBufferBuilder<'b, A>
  ) -> ::flatbuffers::WIPOffset<IRMChunk<'b>> {
    let CHUNK_INDEX = self.CHUNK_INDEX;
    let BATCH_ID = self.BATCH_ID.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let RANGE_FIRST_BYTE = self.RANGE_FIRST_BYTE;
    let RANGE_LAST_BYTE = self.RANGE_LAST_BYTE;
    let BYTE_LENGTH = self.BYTE_LENGTH;
    let CHUNK_SHA256 = self.CHUNK_SHA256.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let RECORDS_DECODED = self.RECORDS_DECODED;
    let RECORDS_STORED = self.RECORDS_STORED;
    let RECORDS_REJECTED = self.RECORDS_REJECTED;
    let ATTEMPTS = self.ATTEMPTS;
    let COMMITTED_AT = self.COMMITTED_AT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    IRMChunk::create(_fbb, &IRMChunkArgs{
      CHUNK_INDEX,
      BATCH_ID,
      RANGE_FIRST_BYTE,
      RANGE_LAST_BYTE,
      BYTE_LENGTH,
      CHUNK_SHA256,
      RECORDS_DECODED,
      RECORDS_STORED,
      RECORDS_REJECTED,
      ATTEMPTS,
      COMMITTED_AT,
    })
  }
}
pub enum IRMFaultOffset {}
#[derive(Copy, Clone, PartialEq)]

/// The last unrecovered error, carried so a resumed run can refuse to loop.
pub struct IRMFault<'a> {
  pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for IRMFault<'a> {
  type Inner = IRMFault<'a>;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    Self { _tab: unsafe { ::flatbuffers::Table::new(buf, loc) } }
  }
}

impl<'a> IRMFault<'a> {
  pub const VT_CODE: ::flatbuffers::VOffsetT = 4;
  pub const VT_MESSAGE: ::flatbuffers::VOffsetT = 6;
  pub const VT_CHUNK_INDEX: ::flatbuffers::VOffsetT = 8;
  pub const VT_CONSECUTIVE_FAILURES: ::flatbuffers::VOffsetT = 10;
  pub const VT_OCCURRED_AT: ::flatbuffers::VOffsetT = 12;
  pub const VT_RETRY_NOT_BEFORE: ::flatbuffers::VOffsetT = 14;

  #[inline]
  pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
    IRMFault { _tab: table }
  }
  #[allow(unused_mut)]
  pub fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: ::flatbuffers::Allocator + 'bldr>(
    _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
    args: &'args IRMFaultArgs<'args>
  ) -> ::flatbuffers::WIPOffset<IRMFault<'bldr>> {
    let mut builder = IRMFaultBuilder::new(_fbb);
    if let Some(x) = args.RETRY_NOT_BEFORE { builder.add_RETRY_NOT_BEFORE(x); }
    if let Some(x) = args.OCCURRED_AT { builder.add_OCCURRED_AT(x); }
    builder.add_CONSECUTIVE_FAILURES(args.CONSECUTIVE_FAILURES);
    builder.add_CHUNK_INDEX(args.CHUNK_INDEX);
    if let Some(x) = args.MESSAGE { builder.add_MESSAGE(x); }
    if let Some(x) = args.CODE { builder.add_CODE(x); }
    builder.finish()
  }

  pub fn unpack(&self) -> IRMFaultT {
    let CODE = {
      let x = self.CODE();
      alloc::string::ToString::to_string(x)
    };
    let MESSAGE = self.MESSAGE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let CHUNK_INDEX = self.CHUNK_INDEX();
    let CONSECUTIVE_FAILURES = self.CONSECUTIVE_FAILURES();
    let OCCURRED_AT = self.OCCURRED_AT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let RETRY_NOT_BEFORE = self.RETRY_NOT_BEFORE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    IRMFaultT {
      CODE,
      MESSAGE,
      CHUNK_INDEX,
      CONSECUTIVE_FAILURES,
      OCCURRED_AT,
      RETRY_NOT_BEFORE,
    }
  }

  /// Publisher-stable error code verbatim.
  #[inline]
  pub fn CODE(&self) -> &'a str {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMFault::VT_CODE, None).unwrap()}
  }
  /// Human-readable message verbatim; never a substitute for CODE.
  #[inline]
  pub fn MESSAGE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMFault::VT_MESSAGE, None)}
  }
  /// Zero-based index of the chunk the fault occurred on.
  #[inline]
  pub fn CHUNK_INDEX(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRMFault::VT_CHUNK_INDEX, Some(0)).unwrap()}
  }
  /// Consecutive failed attempts at that chunk.
  #[inline]
  pub fn CONSECUTIVE_FAILURES(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRMFault::VT_CONSECUTIVE_FAILURES, Some(0)).unwrap()}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp of the fault.
  #[inline]
  pub fn OCCURRED_AT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMFault::VT_OCCURRED_AT, None)}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp before which a consumer must not
  /// retry. Empty means the publisher states no backoff.
  #[inline]
  pub fn RETRY_NOT_BEFORE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRMFault::VT_RETRY_NOT_BEFORE, None)}
  }
}

impl ::flatbuffers::Verifiable for IRMFault<'_> {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    v.visit_table(pos)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("CODE", Self::VT_CODE, true)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("MESSAGE", Self::VT_MESSAGE, false)?
     .visit_field::<u32>("CHUNK_INDEX", Self::VT_CHUNK_INDEX, false)?
     .visit_field::<u32>("CONSECUTIVE_FAILURES", Self::VT_CONSECUTIVE_FAILURES, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("OCCURRED_AT", Self::VT_OCCURRED_AT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("RETRY_NOT_BEFORE", Self::VT_RETRY_NOT_BEFORE, false)?
     .finish();
    Ok(())
  }
}
pub struct IRMFaultArgs<'a> {
    pub CODE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub MESSAGE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub CHUNK_INDEX: u32,
    pub CONSECUTIVE_FAILURES: u32,
    pub OCCURRED_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub RETRY_NOT_BEFORE: Option<::flatbuffers::WIPOffset<&'a str>>,
}
impl<'a> Default for IRMFaultArgs<'a> {
  #[inline]
  fn default() -> Self {
    IRMFaultArgs {
      CODE: None, // required field
      MESSAGE: None,
      CHUNK_INDEX: 0,
      CONSECUTIVE_FAILURES: 0,
      OCCURRED_AT: None,
      RETRY_NOT_BEFORE: None,
    }
  }
}

pub struct IRMFaultBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
  fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
  start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> IRMFaultBuilder<'a, 'b, A> {
  #[inline]
  pub fn add_CODE(&mut self, CODE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMFault::VT_CODE, CODE);
  }
  #[inline]
  pub fn add_MESSAGE(&mut self, MESSAGE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMFault::VT_MESSAGE, MESSAGE);
  }
  #[inline]
  pub fn add_CHUNK_INDEX(&mut self, CHUNK_INDEX: u32) {
    self.fbb_.push_slot::<u32>(IRMFault::VT_CHUNK_INDEX, CHUNK_INDEX, 0);
  }
  #[inline]
  pub fn add_CONSECUTIVE_FAILURES(&mut self, CONSECUTIVE_FAILURES: u32) {
    self.fbb_.push_slot::<u32>(IRMFault::VT_CONSECUTIVE_FAILURES, CONSECUTIVE_FAILURES, 0);
  }
  #[inline]
  pub fn add_OCCURRED_AT(&mut self, OCCURRED_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMFault::VT_OCCURRED_AT, OCCURRED_AT);
  }
  #[inline]
  pub fn add_RETRY_NOT_BEFORE(&mut self, RETRY_NOT_BEFORE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRMFault::VT_RETRY_NOT_BEFORE, RETRY_NOT_BEFORE);
  }
  #[inline]
  pub fn new(_fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>) -> IRMFaultBuilder<'a, 'b, A> {
    let start = _fbb.start_table();
    IRMFaultBuilder {
      fbb_: _fbb,
      start_: start,
    }
  }
  #[inline]
  pub fn finish(self) -> ::flatbuffers::WIPOffset<IRMFault<'a>> {
    let o = self.fbb_.end_table(self.start_);
    self.fbb_.required(o, IRMFault::VT_CODE,"CODE");
    ::flatbuffers::WIPOffset::new(o.value())
  }
}

impl ::core::fmt::Debug for IRMFault<'_> {
  fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
    let mut ds = f.debug_struct("IRMFault");
      ds.field("CODE", &self.CODE());
      ds.field("MESSAGE", &self.MESSAGE());
      ds.field("CHUNK_INDEX", &self.CHUNK_INDEX());
      ds.field("CONSECUTIVE_FAILURES", &self.CONSECUTIVE_FAILURES());
      ds.field("OCCURRED_AT", &self.OCCURRED_AT());
      ds.field("RETRY_NOT_BEFORE", &self.RETRY_NOT_BEFORE());
      ds.finish()
  }
}
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq)]
pub struct IRMFaultT {
  pub CODE: alloc::string::String,
  pub MESSAGE: Option<alloc::string::String>,
  pub CHUNK_INDEX: u32,
  pub CONSECUTIVE_FAILURES: u32,
  pub OCCURRED_AT: Option<alloc::string::String>,
  pub RETRY_NOT_BEFORE: Option<alloc::string::String>,
}
impl Default for IRMFaultT {
  fn default() -> Self {
    Self {
      CODE: alloc::string::ToString::to_string(""),
      MESSAGE: None,
      CHUNK_INDEX: 0,
      CONSECUTIVE_FAILURES: 0,
      OCCURRED_AT: None,
      RETRY_NOT_BEFORE: None,
    }
  }
}
impl IRMFaultT {
  pub fn pack<'b, A: ::flatbuffers::Allocator + 'b>(
    &self,
    _fbb: &mut ::flatbuffers::FlatBufferBuilder<'b, A>
  ) -> ::flatbuffers::WIPOffset<IRMFault<'b>> {
    let CODE = Some({
      let x = &self.CODE;
      _fbb.create_string(x)
    });
    let MESSAGE = self.MESSAGE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let CHUNK_INDEX = self.CHUNK_INDEX;
    let CONSECUTIVE_FAILURES = self.CONSECUTIVE_FAILURES;
    let OCCURRED_AT = self.OCCURRED_AT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let RETRY_NOT_BEFORE = self.RETRY_NOT_BEFORE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    IRMFault::create(_fbb, &IRMFaultArgs{
      CODE,
      MESSAGE,
      CHUNK_INDEX,
      CONSECUTIVE_FAILURES,
      OCCURRED_AT,
      RETRY_NOT_BEFORE,
    })
  }
}
pub enum IRMOffset {}
#[derive(Copy, Clone, PartialEq)]

/// Ingest Resume Mark
pub struct IRM<'a> {
  pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for IRM<'a> {
  type Inner = IRM<'a>;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    Self { _tab: unsafe { ::flatbuffers::Table::new(buf, loc) } }
  }
}

impl<'a> IRM<'a> {
  pub const VT_JOB_ID: ::flatbuffers::VOffsetT = 4;
  pub const VT_SEQUENCE: ::flatbuffers::VOffsetT = 6;
  pub const VT_PROVIDER_ID: ::flatbuffers::VOffsetT = 8;
  pub const VT_INGESTOR_ID: ::flatbuffers::VOffsetT = 10;
  pub const VT_SOURCE: ::flatbuffers::VOffsetT = 12;
  pub const VT_STATE: ::flatbuffers::VOffsetT = 14;
  pub const VT_RANGE_MODE: ::flatbuffers::VOffsetT = 16;
  pub const VT_NEXT_OFFSET: ::flatbuffers::VOffsetT = 18;
  pub const VT_NEXT_CHUNK_INDEX: ::flatbuffers::VOffsetT = 20;
  pub const VT_CHUNK_BYTE_BUDGET: ::flatbuffers::VOffsetT = 22;
  pub const VT_DECODE_CONTEXT: ::flatbuffers::VOffsetT = 24;
  pub const VT_LAST_CHUNK: ::flatbuffers::VOffsetT = 26;
  pub const VT_CHUNKS_COMMITTED: ::flatbuffers::VOffsetT = 28;
  pub const VT_BYTES_COMMITTED: ::flatbuffers::VOffsetT = 30;
  pub const VT_RECORDS_COMMITTED: ::flatbuffers::VOffsetT = 32;
  pub const VT_TARGET_STANDARD: ::flatbuffers::VOffsetT = 34;
  pub const VT_RECONCILE_MODE: ::flatbuffers::VOffsetT = 36;
  pub const VT_MERGE_POLICY: ::flatbuffers::VOffsetT = 38;
  pub const VT_FAULT: ::flatbuffers::VOffsetT = 40;
  pub const VT_CREATED_AT: ::flatbuffers::VOffsetT = 42;
  pub const VT_STARTED_AT: ::flatbuffers::VOffsetT = 44;
  pub const VT_UPDATED_AT: ::flatbuffers::VOffsetT = 46;
  pub const VT_COMPLETED_AT: ::flatbuffers::VOffsetT = 48;
  pub const VT_EXPIRES_AT: ::flatbuffers::VOffsetT = 50;
  pub const VT_TAGS: ::flatbuffers::VOffsetT = 52;
  pub const VT_NOTES: ::flatbuffers::VOffsetT = 54;

  #[inline]
  pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
    IRM { _tab: table }
  }
  #[allow(unused_mut)]
  pub fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: ::flatbuffers::Allocator + 'bldr>(
    _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
    args: &'args IRMArgs<'args>
  ) -> ::flatbuffers::WIPOffset<IRM<'bldr>> {
    let mut builder = IRMBuilder::new(_fbb);
    builder.add_RECORDS_COMMITTED(args.RECORDS_COMMITTED);
    builder.add_BYTES_COMMITTED(args.BYTES_COMMITTED);
    builder.add_CHUNK_BYTE_BUDGET(args.CHUNK_BYTE_BUDGET);
    builder.add_NEXT_OFFSET(args.NEXT_OFFSET);
    builder.add_SEQUENCE(args.SEQUENCE);
    if let Some(x) = args.NOTES { builder.add_NOTES(x); }
    if let Some(x) = args.TAGS { builder.add_TAGS(x); }
    if let Some(x) = args.EXPIRES_AT { builder.add_EXPIRES_AT(x); }
    if let Some(x) = args.COMPLETED_AT { builder.add_COMPLETED_AT(x); }
    if let Some(x) = args.UPDATED_AT { builder.add_UPDATED_AT(x); }
    if let Some(x) = args.STARTED_AT { builder.add_STARTED_AT(x); }
    if let Some(x) = args.CREATED_AT { builder.add_CREATED_AT(x); }
    if let Some(x) = args.FAULT { builder.add_FAULT(x); }
    if let Some(x) = args.MERGE_POLICY { builder.add_MERGE_POLICY(x); }
    if let Some(x) = args.RECONCILE_MODE { builder.add_RECONCILE_MODE(x); }
    if let Some(x) = args.TARGET_STANDARD { builder.add_TARGET_STANDARD(x); }
    builder.add_CHUNKS_COMMITTED(args.CHUNKS_COMMITTED);
    if let Some(x) = args.LAST_CHUNK { builder.add_LAST_CHUNK(x); }
    if let Some(x) = args.DECODE_CONTEXT { builder.add_DECODE_CONTEXT(x); }
    builder.add_NEXT_CHUNK_INDEX(args.NEXT_CHUNK_INDEX);
    if let Some(x) = args.SOURCE { builder.add_SOURCE(x); }
    if let Some(x) = args.INGESTOR_ID { builder.add_INGESTOR_ID(x); }
    if let Some(x) = args.PROVIDER_ID { builder.add_PROVIDER_ID(x); }
    if let Some(x) = args.JOB_ID { builder.add_JOB_ID(x); }
    builder.add_RANGE_MODE(args.RANGE_MODE);
    builder.add_STATE(args.STATE);
    builder.finish()
  }

  pub fn unpack(&self) -> IRMT {
    let JOB_ID = {
      let x = self.JOB_ID();
      alloc::string::ToString::to_string(x)
    };
    let SEQUENCE = self.SEQUENCE();
    let PROVIDER_ID = {
      let x = self.PROVIDER_ID();
      alloc::string::ToString::to_string(x)
    };
    let INGESTOR_ID = self.INGESTOR_ID().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let SOURCE = {
      let x = self.SOURCE();
      alloc::boxed::Box::new(x.unpack())
    };
    let STATE = self.STATE();
    let RANGE_MODE = self.RANGE_MODE();
    let NEXT_OFFSET = self.NEXT_OFFSET();
    let NEXT_CHUNK_INDEX = self.NEXT_CHUNK_INDEX();
    let CHUNK_BYTE_BUDGET = self.CHUNK_BYTE_BUDGET();
    let DECODE_CONTEXT = self.DECODE_CONTEXT().map(|x| {
      alloc::boxed::Box::new(x.unpack())
    });
    let LAST_CHUNK = self.LAST_CHUNK().map(|x| {
      alloc::boxed::Box::new(x.unpack())
    });
    let CHUNKS_COMMITTED = self.CHUNKS_COMMITTED();
    let BYTES_COMMITTED = self.BYTES_COMMITTED();
    let RECORDS_COMMITTED = self.RECORDS_COMMITTED();
    let TARGET_STANDARD = self.TARGET_STANDARD().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let RECONCILE_MODE = self.RECONCILE_MODE().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let MERGE_POLICY = self.MERGE_POLICY().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let FAULT = self.FAULT().map(|x| {
      alloc::boxed::Box::new(x.unpack())
    });
    let CREATED_AT = self.CREATED_AT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let STARTED_AT = self.STARTED_AT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let UPDATED_AT = {
      let x = self.UPDATED_AT();
      alloc::string::ToString::to_string(x)
    };
    let COMPLETED_AT = self.COMPLETED_AT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let EXPIRES_AT = self.EXPIRES_AT().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let TAGS = self.TAGS().map(|x| {
      x.iter().map(|s| alloc::string::ToString::to_string(s)).collect()
    });
    let NOTES = self.NOTES().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    IRMT {
      JOB_ID,
      SEQUENCE,
      PROVIDER_ID,
      INGESTOR_ID,
      SOURCE,
      STATE,
      RANGE_MODE,
      NEXT_OFFSET,
      NEXT_CHUNK_INDEX,
      CHUNK_BYTE_BUDGET,
      DECODE_CONTEXT,
      LAST_CHUNK,
      CHUNKS_COMMITTED,
      BYTES_COMMITTED,
      RECORDS_COMMITTED,
      TARGET_STANDARD,
      RECONCILE_MODE,
      MERGE_POLICY,
      FAULT,
      CREATED_AT,
      STARTED_AT,
      UPDATED_AT,
      COMPLETED_AT,
      EXPIRES_AT,
      TAGS,
      NOTES,
    }
  }

  /// Publisher-stable identifier of the ingest job. Together with the publishing
  /// key it is the job's identity across every mark it emits.
  #[inline]
  pub fn JOB_ID(&self) -> &'a str {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_JOB_ID, None).unwrap()}
  }
  /// Monotonic version of this mark within JOB_ID. Highest wins per publishing
  /// key. Never reused, never decremented.
  #[inline]
  pub fn SEQUENCE(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRM::VT_SEQUENCE, Some(0)).unwrap()}
  }
  /// Publisher-stable identifier of the data provider being read, verbatim.
  /// Publisher data: it may name a real provider.
  #[inline]
  pub fn PROVIDER_ID(&self) -> &'a str {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_PROVIDER_ID, None).unwrap()}
  }
  /// Reverse-DNS identifier of the ingest module or flow that owns the job, so
  /// two pipelines reading one provider do not share a resume point.
  #[inline]
  pub fn INGESTOR_ID(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_INGESTOR_ID, None)}
  }
  /// The source object and its validators.
  #[inline]
  pub fn SOURCE(&self) -> IRMSource<'a> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<IRMSource>>(IRM::VT_SOURCE, None).unwrap()}
  }
  /// Lifecycle of the job.
  #[inline]
  pub fn STATE(&self) -> irmJobState {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<irmJobState>(IRM::VT_STATE, Some(irmJobState::UNSPECIFIED)).unwrap()}
  }
  /// How byte positions in this mark are addressed.
  #[inline]
  pub fn RANGE_MODE(&self) -> irmRangeMode {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<irmRangeMode>(IRM::VT_RANGE_MODE, Some(irmRangeMode::UNSPECIFIED)).unwrap()}
  }
  /// Byte offset at which the next read begins, in the SOURCE's own byte stream:
  /// the compressed stream when DECODE_CONTEXT.COMPRESSION is non-empty, in
  /// which case it is a record boundary only in company with the decoder state,
  /// window and partial record. On an uncompressed source it is ALWAYS a
  /// complete-record boundary, corrected backwards from the previous chunk's
  /// byte budget. 0 is a valid value and means the job has committed nothing yet.
  #[inline]
  pub fn NEXT_OFFSET(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRM::VT_NEXT_OFFSET, Some(0)).unwrap()}
  }
  /// Zero-based index of the next chunk to read. With PART_INDEX addressing this
  /// is the only position.
  #[inline]
  pub fn NEXT_CHUNK_INDEX(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRM::VT_NEXT_CHUNK_INDEX, Some(0)).unwrap()}
  }
  /// Byte budget the publisher reads per chunk. Records the ceiling the job
  /// actually ran under, which a host may have clamped below the configured one.
  #[inline]
  pub fn CHUNK_BYTE_BUDGET(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRM::VT_CHUNK_BYTE_BUDGET, Some(0)).unwrap()}
  }
  /// Everything needed to decode the chunk at NEXT_OFFSET. REQUIRED in practice
  /// whenever NEXT_OFFSET is past the source's own header; a consumer that finds
  /// it absent there restarts from offset 0 rather than decoding blind.
  #[inline]
  pub fn DECODE_CONTEXT(&self) -> Option<IRMDecodeContext<'a>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<IRMDecodeContext>>(IRM::VT_DECODE_CONTEXT, None)}
  }
  /// The chunk this mark was advanced from — the last one durably committed.
  #[inline]
  pub fn LAST_CHUNK(&self) -> Option<IRMChunk<'a>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<IRMChunk>>(IRM::VT_LAST_CHUNK, None)}
  }
  /// Chunks durably committed by this job so far.
  #[inline]
  pub fn CHUNKS_COMMITTED(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(IRM::VT_CHUNKS_COMMITTED, Some(0)).unwrap()}
  }
  /// Source bytes accounted for by committed chunks.
  #[inline]
  pub fn BYTES_COMMITTED(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRM::VT_BYTES_COMMITTED, Some(0)).unwrap()}
  }
  /// Records durably committed by this job so far, across every chunk.
  #[inline]
  pub fn RECORDS_COMMITTED(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(IRM::VT_RECORDS_COMMITTED, Some(0)).unwrap()}
  }
  /// Four-character standard code of the records this job writes, `$` included,
  /// e.g. `$TBS`. Never a union ordinal.
  #[inline]
  pub fn TARGET_STANDARD(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_TARGET_STANDARD, None)}
  }
  /// Reconciliation mode the storage lane was called with, verbatim, e.g.
  /// `append`. A chunked job never reconciles per batch: reconciling would delete
  /// every earlier chunk as the next one arrived.
  #[inline]
  pub fn RECONCILE_MODE(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_RECONCILE_MODE, None)}
  }
  /// Deduplication or merge policy the job applied, verbatim.
  #[inline]
  pub fn MERGE_POLICY(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_MERGE_POLICY, None)}
  }
  /// The last unrecovered error, when there is one.
  #[inline]
  pub fn FAULT(&self) -> Option<IRMFault<'a>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<IRMFault>>(IRM::VT_FAULT, None)}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp at which the job was created.
  #[inline]
  pub fn CREATED_AT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_CREATED_AT, None)}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp at which the first chunk was read.
  #[inline]
  pub fn STARTED_AT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_STARTED_AT, None)}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp at which this mark was published.
  /// With JOB_ID and SEQUENCE this is the version key; SEQUENCE breaks ties.
  #[inline]
  pub fn UPDATED_AT(&self) -> &'a str {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_UPDATED_AT, None).unwrap()}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp at which the job reached COMPLETE.
  #[inline]
  pub fn COMPLETED_AT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_COMPLETED_AT, None)}
  }
  /// RFC 3339 UTC fixed-millisecond timestamp after which a consumer treats the
  /// mark as stale and re-validates the source before resuming. Empty means the
  /// publisher states no expiry.
  #[inline]
  pub fn EXPIRES_AT(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_EXPIRES_AT, None)}
  }
  /// `namespace:value` facets, sorted and deduplicated. See TAG GRAMMAR above.
  #[inline]
  pub fn TAGS(&self) -> Option<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<&'a str>>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<&'a str>>>>(IRM::VT_TAGS, None)}
  }
  /// Publisher note about this job; never a substitute for a field.
  #[inline]
  pub fn NOTES(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(IRM::VT_NOTES, None)}
  }
}

impl ::flatbuffers::Verifiable for IRM<'_> {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    v.visit_table(pos)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("JOB_ID", Self::VT_JOB_ID, true)?
     .visit_field::<u64>("SEQUENCE", Self::VT_SEQUENCE, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("PROVIDER_ID", Self::VT_PROVIDER_ID, true)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("INGESTOR_ID", Self::VT_INGESTOR_ID, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<IRMSource>>("SOURCE", Self::VT_SOURCE, true)?
     .visit_field::<irmJobState>("STATE", Self::VT_STATE, false)?
     .visit_field::<irmRangeMode>("RANGE_MODE", Self::VT_RANGE_MODE, false)?
     .visit_field::<u64>("NEXT_OFFSET", Self::VT_NEXT_OFFSET, false)?
     .visit_field::<u32>("NEXT_CHUNK_INDEX", Self::VT_NEXT_CHUNK_INDEX, false)?
     .visit_field::<u64>("CHUNK_BYTE_BUDGET", Self::VT_CHUNK_BYTE_BUDGET, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<IRMDecodeContext>>("DECODE_CONTEXT", Self::VT_DECODE_CONTEXT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<IRMChunk>>("LAST_CHUNK", Self::VT_LAST_CHUNK, false)?
     .visit_field::<u32>("CHUNKS_COMMITTED", Self::VT_CHUNKS_COMMITTED, false)?
     .visit_field::<u64>("BYTES_COMMITTED", Self::VT_BYTES_COMMITTED, false)?
     .visit_field::<u64>("RECORDS_COMMITTED", Self::VT_RECORDS_COMMITTED, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("TARGET_STANDARD", Self::VT_TARGET_STANDARD, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("RECONCILE_MODE", Self::VT_RECONCILE_MODE, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("MERGE_POLICY", Self::VT_MERGE_POLICY, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<IRMFault>>("FAULT", Self::VT_FAULT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("CREATED_AT", Self::VT_CREATED_AT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("STARTED_AT", Self::VT_STARTED_AT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("UPDATED_AT", Self::VT_UPDATED_AT, true)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("COMPLETED_AT", Self::VT_COMPLETED_AT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("EXPIRES_AT", Self::VT_EXPIRES_AT, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ::flatbuffers::ForwardsUOffset<&'_ str>>>>("TAGS", Self::VT_TAGS, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("NOTES", Self::VT_NOTES, false)?
     .finish();
    Ok(())
  }
}
pub struct IRMArgs<'a> {
    pub JOB_ID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub SEQUENCE: u64,
    pub PROVIDER_ID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub INGESTOR_ID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub SOURCE: Option<::flatbuffers::WIPOffset<IRMSource<'a>>>,
    pub STATE: irmJobState,
    pub RANGE_MODE: irmRangeMode,
    pub NEXT_OFFSET: u64,
    pub NEXT_CHUNK_INDEX: u32,
    pub CHUNK_BYTE_BUDGET: u64,
    pub DECODE_CONTEXT: Option<::flatbuffers::WIPOffset<IRMDecodeContext<'a>>>,
    pub LAST_CHUNK: Option<::flatbuffers::WIPOffset<IRMChunk<'a>>>,
    pub CHUNKS_COMMITTED: u32,
    pub BYTES_COMMITTED: u64,
    pub RECORDS_COMMITTED: u64,
    pub TARGET_STANDARD: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub RECONCILE_MODE: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub MERGE_POLICY: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub FAULT: Option<::flatbuffers::WIPOffset<IRMFault<'a>>>,
    pub CREATED_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub STARTED_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub UPDATED_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub COMPLETED_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub EXPIRES_AT: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub TAGS: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<&'a str>>>>,
    pub NOTES: Option<::flatbuffers::WIPOffset<&'a str>>,
}
impl<'a> Default for IRMArgs<'a> {
  #[inline]
  fn default() -> Self {
    IRMArgs {
      JOB_ID: None, // required field
      SEQUENCE: 0,
      PROVIDER_ID: None, // required field
      INGESTOR_ID: None,
      SOURCE: None, // required field
      STATE: irmJobState::UNSPECIFIED,
      RANGE_MODE: irmRangeMode::UNSPECIFIED,
      NEXT_OFFSET: 0,
      NEXT_CHUNK_INDEX: 0,
      CHUNK_BYTE_BUDGET: 0,
      DECODE_CONTEXT: None,
      LAST_CHUNK: None,
      CHUNKS_COMMITTED: 0,
      BYTES_COMMITTED: 0,
      RECORDS_COMMITTED: 0,
      TARGET_STANDARD: None,
      RECONCILE_MODE: None,
      MERGE_POLICY: None,
      FAULT: None,
      CREATED_AT: None,
      STARTED_AT: None,
      UPDATED_AT: None, // required field
      COMPLETED_AT: None,
      EXPIRES_AT: None,
      TAGS: None,
      NOTES: None,
    }
  }
}

pub struct IRMBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
  fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
  start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> IRMBuilder<'a, 'b, A> {
  #[inline]
  pub fn add_JOB_ID(&mut self, JOB_ID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_JOB_ID, JOB_ID);
  }
  #[inline]
  pub fn add_SEQUENCE(&mut self, SEQUENCE: u64) {
    self.fbb_.push_slot::<u64>(IRM::VT_SEQUENCE, SEQUENCE, 0);
  }
  #[inline]
  pub fn add_PROVIDER_ID(&mut self, PROVIDER_ID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_PROVIDER_ID, PROVIDER_ID);
  }
  #[inline]
  pub fn add_INGESTOR_ID(&mut self, INGESTOR_ID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_INGESTOR_ID, INGESTOR_ID);
  }
  #[inline]
  pub fn add_SOURCE(&mut self, SOURCE: ::flatbuffers::WIPOffset<IRMSource<'b >>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<IRMSource>>(IRM::VT_SOURCE, SOURCE);
  }
  #[inline]
  pub fn add_STATE(&mut self, STATE: irmJobState) {
    self.fbb_.push_slot::<irmJobState>(IRM::VT_STATE, STATE, irmJobState::UNSPECIFIED);
  }
  #[inline]
  pub fn add_RANGE_MODE(&mut self, RANGE_MODE: irmRangeMode) {
    self.fbb_.push_slot::<irmRangeMode>(IRM::VT_RANGE_MODE, RANGE_MODE, irmRangeMode::UNSPECIFIED);
  }
  #[inline]
  pub fn add_NEXT_OFFSET(&mut self, NEXT_OFFSET: u64) {
    self.fbb_.push_slot::<u64>(IRM::VT_NEXT_OFFSET, NEXT_OFFSET, 0);
  }
  #[inline]
  pub fn add_NEXT_CHUNK_INDEX(&mut self, NEXT_CHUNK_INDEX: u32) {
    self.fbb_.push_slot::<u32>(IRM::VT_NEXT_CHUNK_INDEX, NEXT_CHUNK_INDEX, 0);
  }
  #[inline]
  pub fn add_CHUNK_BYTE_BUDGET(&mut self, CHUNK_BYTE_BUDGET: u64) {
    self.fbb_.push_slot::<u64>(IRM::VT_CHUNK_BYTE_BUDGET, CHUNK_BYTE_BUDGET, 0);
  }
  #[inline]
  pub fn add_DECODE_CONTEXT(&mut self, DECODE_CONTEXT: ::flatbuffers::WIPOffset<IRMDecodeContext<'b >>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<IRMDecodeContext>>(IRM::VT_DECODE_CONTEXT, DECODE_CONTEXT);
  }
  #[inline]
  pub fn add_LAST_CHUNK(&mut self, LAST_CHUNK: ::flatbuffers::WIPOffset<IRMChunk<'b >>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<IRMChunk>>(IRM::VT_LAST_CHUNK, LAST_CHUNK);
  }
  #[inline]
  pub fn add_CHUNKS_COMMITTED(&mut self, CHUNKS_COMMITTED: u32) {
    self.fbb_.push_slot::<u32>(IRM::VT_CHUNKS_COMMITTED, CHUNKS_COMMITTED, 0);
  }
  #[inline]
  pub fn add_BYTES_COMMITTED(&mut self, BYTES_COMMITTED: u64) {
    self.fbb_.push_slot::<u64>(IRM::VT_BYTES_COMMITTED, BYTES_COMMITTED, 0);
  }
  #[inline]
  pub fn add_RECORDS_COMMITTED(&mut self, RECORDS_COMMITTED: u64) {
    self.fbb_.push_slot::<u64>(IRM::VT_RECORDS_COMMITTED, RECORDS_COMMITTED, 0);
  }
  #[inline]
  pub fn add_TARGET_STANDARD(&mut self, TARGET_STANDARD: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_TARGET_STANDARD, TARGET_STANDARD);
  }
  #[inline]
  pub fn add_RECONCILE_MODE(&mut self, RECONCILE_MODE: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_RECONCILE_MODE, RECONCILE_MODE);
  }
  #[inline]
  pub fn add_MERGE_POLICY(&mut self, MERGE_POLICY: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_MERGE_POLICY, MERGE_POLICY);
  }
  #[inline]
  pub fn add_FAULT(&mut self, FAULT: ::flatbuffers::WIPOffset<IRMFault<'b >>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<IRMFault>>(IRM::VT_FAULT, FAULT);
  }
  #[inline]
  pub fn add_CREATED_AT(&mut self, CREATED_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_CREATED_AT, CREATED_AT);
  }
  #[inline]
  pub fn add_STARTED_AT(&mut self, STARTED_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_STARTED_AT, STARTED_AT);
  }
  #[inline]
  pub fn add_UPDATED_AT(&mut self, UPDATED_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_UPDATED_AT, UPDATED_AT);
  }
  #[inline]
  pub fn add_COMPLETED_AT(&mut self, COMPLETED_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_COMPLETED_AT, COMPLETED_AT);
  }
  #[inline]
  pub fn add_EXPIRES_AT(&mut self, EXPIRES_AT: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_EXPIRES_AT, EXPIRES_AT);
  }
  #[inline]
  pub fn add_TAGS(&mut self, TAGS: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b , ::flatbuffers::ForwardsUOffset<&'b  str>>>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_TAGS, TAGS);
  }
  #[inline]
  pub fn add_NOTES(&mut self, NOTES: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(IRM::VT_NOTES, NOTES);
  }
  #[inline]
  pub fn new(_fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>) -> IRMBuilder<'a, 'b, A> {
    let start = _fbb.start_table();
    IRMBuilder {
      fbb_: _fbb,
      start_: start,
    }
  }
  #[inline]
  pub fn finish(self) -> ::flatbuffers::WIPOffset<IRM<'a>> {
    let o = self.fbb_.end_table(self.start_);
    self.fbb_.required(o, IRM::VT_JOB_ID,"JOB_ID");
    self.fbb_.required(o, IRM::VT_PROVIDER_ID,"PROVIDER_ID");
    self.fbb_.required(o, IRM::VT_SOURCE,"SOURCE");
    self.fbb_.required(o, IRM::VT_UPDATED_AT,"UPDATED_AT");
    ::flatbuffers::WIPOffset::new(o.value())
  }
}

impl ::core::fmt::Debug for IRM<'_> {
  fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
    let mut ds = f.debug_struct("IRM");
      ds.field("JOB_ID", &self.JOB_ID());
      ds.field("SEQUENCE", &self.SEQUENCE());
      ds.field("PROVIDER_ID", &self.PROVIDER_ID());
      ds.field("INGESTOR_ID", &self.INGESTOR_ID());
      ds.field("SOURCE", &self.SOURCE());
      ds.field("STATE", &self.STATE());
      ds.field("RANGE_MODE", &self.RANGE_MODE());
      ds.field("NEXT_OFFSET", &self.NEXT_OFFSET());
      ds.field("NEXT_CHUNK_INDEX", &self.NEXT_CHUNK_INDEX());
      ds.field("CHUNK_BYTE_BUDGET", &self.CHUNK_BYTE_BUDGET());
      ds.field("DECODE_CONTEXT", &self.DECODE_CONTEXT());
      ds.field("LAST_CHUNK", &self.LAST_CHUNK());
      ds.field("CHUNKS_COMMITTED", &self.CHUNKS_COMMITTED());
      ds.field("BYTES_COMMITTED", &self.BYTES_COMMITTED());
      ds.field("RECORDS_COMMITTED", &self.RECORDS_COMMITTED());
      ds.field("TARGET_STANDARD", &self.TARGET_STANDARD());
      ds.field("RECONCILE_MODE", &self.RECONCILE_MODE());
      ds.field("MERGE_POLICY", &self.MERGE_POLICY());
      ds.field("FAULT", &self.FAULT());
      ds.field("CREATED_AT", &self.CREATED_AT());
      ds.field("STARTED_AT", &self.STARTED_AT());
      ds.field("UPDATED_AT", &self.UPDATED_AT());
      ds.field("COMPLETED_AT", &self.COMPLETED_AT());
      ds.field("EXPIRES_AT", &self.EXPIRES_AT());
      ds.field("TAGS", &self.TAGS());
      ds.field("NOTES", &self.NOTES());
      ds.finish()
  }
}
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq)]
pub struct IRMT {
  pub JOB_ID: alloc::string::String,
  pub SEQUENCE: u64,
  pub PROVIDER_ID: alloc::string::String,
  pub INGESTOR_ID: Option<alloc::string::String>,
  pub SOURCE: alloc::boxed::Box<IRMSourceT>,
  pub STATE: irmJobState,
  pub RANGE_MODE: irmRangeMode,
  pub NEXT_OFFSET: u64,
  pub NEXT_CHUNK_INDEX: u32,
  pub CHUNK_BYTE_BUDGET: u64,
  pub DECODE_CONTEXT: Option<alloc::boxed::Box<IRMDecodeContextT>>,
  pub LAST_CHUNK: Option<alloc::boxed::Box<IRMChunkT>>,
  pub CHUNKS_COMMITTED: u32,
  pub BYTES_COMMITTED: u64,
  pub RECORDS_COMMITTED: u64,
  pub TARGET_STANDARD: Option<alloc::string::String>,
  pub RECONCILE_MODE: Option<alloc::string::String>,
  pub MERGE_POLICY: Option<alloc::string::String>,
  pub FAULT: Option<alloc::boxed::Box<IRMFaultT>>,
  pub CREATED_AT: Option<alloc::string::String>,
  pub STARTED_AT: Option<alloc::string::String>,
  pub UPDATED_AT: alloc::string::String,
  pub COMPLETED_AT: Option<alloc::string::String>,
  pub EXPIRES_AT: Option<alloc::string::String>,
  pub TAGS: Option<alloc::vec::Vec<alloc::string::String>>,
  pub NOTES: Option<alloc::string::String>,
}
impl Default for IRMT {
  fn default() -> Self {
    Self {
      JOB_ID: alloc::string::ToString::to_string(""),
      SEQUENCE: 0,
      PROVIDER_ID: alloc::string::ToString::to_string(""),
      INGESTOR_ID: None,
      SOURCE: Default::default(),
      STATE: irmJobState::UNSPECIFIED,
      RANGE_MODE: irmRangeMode::UNSPECIFIED,
      NEXT_OFFSET: 0,
      NEXT_CHUNK_INDEX: 0,
      CHUNK_BYTE_BUDGET: 0,
      DECODE_CONTEXT: None,
      LAST_CHUNK: None,
      CHUNKS_COMMITTED: 0,
      BYTES_COMMITTED: 0,
      RECORDS_COMMITTED: 0,
      TARGET_STANDARD: None,
      RECONCILE_MODE: None,
      MERGE_POLICY: None,
      FAULT: None,
      CREATED_AT: None,
      STARTED_AT: None,
      UPDATED_AT: alloc::string::ToString::to_string(""),
      COMPLETED_AT: None,
      EXPIRES_AT: None,
      TAGS: None,
      NOTES: None,
    }
  }
}
impl IRMT {
  pub fn pack<'b, A: ::flatbuffers::Allocator + 'b>(
    &self,
    _fbb: &mut ::flatbuffers::FlatBufferBuilder<'b, A>
  ) -> ::flatbuffers::WIPOffset<IRM<'b>> {
    let JOB_ID = Some({
      let x = &self.JOB_ID;
      _fbb.create_string(x)
    });
    let SEQUENCE = self.SEQUENCE;
    let PROVIDER_ID = Some({
      let x = &self.PROVIDER_ID;
      _fbb.create_string(x)
    });
    let INGESTOR_ID = self.INGESTOR_ID.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let SOURCE = Some({
      let x = &self.SOURCE;
      x.pack(_fbb)
    });
    let STATE = self.STATE;
    let RANGE_MODE = self.RANGE_MODE;
    let NEXT_OFFSET = self.NEXT_OFFSET;
    let NEXT_CHUNK_INDEX = self.NEXT_CHUNK_INDEX;
    let CHUNK_BYTE_BUDGET = self.CHUNK_BYTE_BUDGET;
    let DECODE_CONTEXT = self.DECODE_CONTEXT.as_ref().map(|x|{
      x.pack(_fbb)
    });
    let LAST_CHUNK = self.LAST_CHUNK.as_ref().map(|x|{
      x.pack(_fbb)
    });
    let CHUNKS_COMMITTED = self.CHUNKS_COMMITTED;
    let BYTES_COMMITTED = self.BYTES_COMMITTED;
    let RECORDS_COMMITTED = self.RECORDS_COMMITTED;
    let TARGET_STANDARD = self.TARGET_STANDARD.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let RECONCILE_MODE = self.RECONCILE_MODE.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let MERGE_POLICY = self.MERGE_POLICY.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let FAULT = self.FAULT.as_ref().map(|x|{
      x.pack(_fbb)
    });
    let CREATED_AT = self.CREATED_AT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let STARTED_AT = self.STARTED_AT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let UPDATED_AT = Some({
      let x = &self.UPDATED_AT;
      _fbb.create_string(x)
    });
    let COMPLETED_AT = self.COMPLETED_AT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let EXPIRES_AT = self.EXPIRES_AT.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let TAGS = self.TAGS.as_ref().map(|x|{
      let w: alloc::vec::Vec<_> = x.iter().map(|s| _fbb.create_string(s)).collect();_fbb.create_vector(&w)
    });
    let NOTES = self.NOTES.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    IRM::create(_fbb, &IRMArgs{
      JOB_ID,
      SEQUENCE,
      PROVIDER_ID,
      INGESTOR_ID,
      SOURCE,
      STATE,
      RANGE_MODE,
      NEXT_OFFSET,
      NEXT_CHUNK_INDEX,
      CHUNK_BYTE_BUDGET,
      DECODE_CONTEXT,
      LAST_CHUNK,
      CHUNKS_COMMITTED,
      BYTES_COMMITTED,
      RECORDS_COMMITTED,
      TARGET_STANDARD,
      RECONCILE_MODE,
      MERGE_POLICY,
      FAULT,
      CREATED_AT,
      STARTED_AT,
      UPDATED_AT,
      COMPLETED_AT,
      EXPIRES_AT,
      TAGS,
      NOTES,
    })
  }
}
#[inline]
/// Verifies that a buffer of bytes contains a `IRM`
/// and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_irm_unchecked`.
pub fn root_as_irm(buf: &[u8]) -> Result<IRM<'_>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::root::<IRM>(buf)
}
#[inline]
/// Verifies that a buffer of bytes contains a size prefixed
/// `IRM` and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `size_prefixed_root_as_irm_unchecked`.
pub fn size_prefixed_root_as_irm(buf: &[u8]) -> Result<IRM<'_>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::size_prefixed_root::<IRM>(buf)
}
#[inline]
/// Verifies, with the given options, that a buffer of bytes
/// contains a `IRM` and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_irm_unchecked`.
pub fn root_as_irm_with_opts<'b, 'o>(
  opts: &'o ::flatbuffers::VerifierOptions,
  buf: &'b [u8],
) -> Result<IRM<'b>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::root_with_opts::<IRM<'b>>(opts, buf)
}
#[inline]
/// Verifies, with the given verifier options, that a buffer of
/// bytes contains a size prefixed `IRM` and returns
/// it. Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_irm_unchecked`.
pub fn size_prefixed_root_as_irm_with_opts<'b, 'o>(
  opts: &'o ::flatbuffers::VerifierOptions,
  buf: &'b [u8],
) -> Result<IRM<'b>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::size_prefixed_root_with_opts::<IRM<'b>>(opts, buf)
}
#[inline]
/// Assumes, without verification, that a buffer of bytes contains a IRM and returns it.
/// # Safety
/// Callers must trust the given bytes do indeed contain a valid `IRM`.
pub unsafe fn root_as_irm_unchecked(buf: &[u8]) -> IRM<'_> {
  unsafe { ::flatbuffers::root_unchecked::<IRM>(buf) }
}
#[inline]
/// Assumes, without verification, that a buffer of bytes contains a size prefixed IRM and returns it.
/// # Safety
/// Callers must trust the given bytes do indeed contain a valid size prefixed `IRM`.
pub unsafe fn size_prefixed_root_as_irm_unchecked(buf: &[u8]) -> IRM<'_> {
  unsafe { ::flatbuffers::size_prefixed_root_unchecked::<IRM>(buf) }
}
pub const IRM_IDENTIFIER: &str = "$IRM";

#[inline]
pub fn irm_buffer_has_identifier(buf: &[u8]) -> bool {
  ::flatbuffers::buffer_has_identifier(buf, IRM_IDENTIFIER, false)
}

#[inline]
pub fn irm_size_prefixed_buffer_has_identifier(buf: &[u8]) -> bool {
  ::flatbuffers::buffer_has_identifier(buf, IRM_IDENTIFIER, true)
}

#[inline]
pub fn finish_irm_buffer<'a, 'b, A: ::flatbuffers::Allocator + 'a>(
    fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    root: ::flatbuffers::WIPOffset<IRM<'a>>) {
  fbb.finish(root, Some(IRM_IDENTIFIER));
}

#[inline]
pub fn finish_size_prefixed_irm_buffer<'a, 'b, A: ::flatbuffers::Allocator + 'a>(fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>, root: ::flatbuffers::WIPOffset<IRM<'a>>) {
  fbb.finish_size_prefixed(root, Some(IRM_IDENTIFIER));
}
