// automatically generated by the FlatBuffers compiler, do not modify
// @generated

extern crate alloc;

#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MIN_PACKET_COMPRESSION_ALGORITHM: u8 = 0;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MAX_PACKET_COMPRESSION_ALGORITHM: u8 = 0;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
#[allow(non_camel_case_types)]
pub const ENUM_VALUES_PACKET_COMPRESSION_ALGORITHM: [packetCompressionAlgorithm; 1] = [
  packetCompressionAlgorithm::POCKET_PLUS,
];

/// Compression algorithm applied to a fixed-length packet stream.
/// Append new values only; never reorder or reuse existing values. Decoders
/// must reject an algorithm value they do not recognize rather than guessing.
#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
#[repr(transparent)]
pub struct packetCompressionAlgorithm(pub u8);
#[allow(non_upper_case_globals)]
impl packetCompressionAlgorithm {
  /// CCSDS 124.0-B-1 POCKET+ — lossless compression of fixed-length
  /// spacecraft housekeeping telemetry.
  pub const POCKET_PLUS: Self = Self(0);

  pub const ENUM_MIN: u8 = 0;
  pub const ENUM_MAX: u8 = 0;
  pub const ENUM_VALUES: &'static [Self] = &[
    Self::POCKET_PLUS,
  ];
  /// Returns the variant's name or "" if unknown.
  pub fn variant_name(self) -> Option<&'static str> {
    match self {
      Self::POCKET_PLUS => Some("POCKET_PLUS"),
      _ => None,
    }
  }
}
impl ::core::fmt::Debug for packetCompressionAlgorithm {
  fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
    if let Some(name) = self.variant_name() {
      f.write_str(name)
    } else {
      f.write_fmt(format_args!("<UNKNOWN {:?}>", self.0))
    }
  }
}
impl<'a> ::flatbuffers::Follow<'a> for packetCompressionAlgorithm {
  type Inner = Self;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    let b = unsafe { ::flatbuffers::read_scalar_at::<u8>(buf, loc) };
    Self(b)
  }
}

impl ::flatbuffers::Push for packetCompressionAlgorithm {
    type Output = packetCompressionAlgorithm;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        unsafe { ::flatbuffers::emplace_scalar::<u8>(dst, self.0) };
    }
}

impl ::flatbuffers::EndianScalar for packetCompressionAlgorithm {
  type Scalar = u8;
  #[inline]
  fn to_little_endian(self) -> u8 {
    self.0.to_le()
  }
  #[inline]
  #[allow(clippy::wrong_self_convention)]
  fn from_little_endian(v: u8) -> Self {
    let b = u8::from_le(v);
    Self(b)
  }
}

impl<'a> ::flatbuffers::Verifiable for packetCompressionAlgorithm {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    u8::run_verifier(v, pos)
  }
}

impl ::flatbuffers::SimpleToVerifyInSlice for packetCompressionAlgorithm {}
pub enum CPSOffset {}
#[derive(Copy, Clone, PartialEq)]

/// Compressed Packet Stream — a run of fixed-length CCSDS packets losslessly
/// compressed as one stateful stream (CCSDS 124.0-B-1 POCKET+). Decoded output
/// is a sequence of PACKET_COUNT packets of PACKET_LENGTH bytes, each of which
/// projects to an $SPP record. This record carries a compressed packet stream
/// only; it is not a general-purpose compression envelope for SDS records.
pub struct CPS<'a> {
  pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for CPS<'a> {
  type Inner = CPS<'a>;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    Self { _tab: unsafe { ::flatbuffers::Table::new(buf, loc) } }
  }
}

impl<'a> CPS<'a> {
  pub const VT_ALGORITHM: ::flatbuffers::VOffsetT = 4;
  pub const VT_PACKET_LENGTH: ::flatbuffers::VOffsetT = 6;
  pub const VT_PACKET_COUNT: ::flatbuffers::VOffsetT = 8;
  pub const VT_ROBUSTNESS: ::flatbuffers::VOffsetT = 10;
  pub const VT_MASK_UPDATE_PERIOD: ::flatbuffers::VOffsetT = 12;
  pub const VT_MASK_SEND_PERIOD: ::flatbuffers::VOffsetT = 14;
  pub const VT_REFERENCE_PERIOD: ::flatbuffers::VOffsetT = 16;
  pub const VT_SELF_SYNCHRONIZING: ::flatbuffers::VOffsetT = 18;
  pub const VT_STREAM_ID: ::flatbuffers::VOffsetT = 20;
  pub const VT_SEGMENT_INDEX: ::flatbuffers::VOffsetT = 22;
  pub const VT_APID: ::flatbuffers::VOffsetT = 24;
  pub const VT_SPACECRAFT_ID: ::flatbuffers::VOffsetT = 26;
  pub const VT_START_EPOCH: ::flatbuffers::VOffsetT = 28;
  pub const VT_STOP_EPOCH: ::flatbuffers::VOffsetT = 30;
  pub const VT_COMPRESSED_DATA: ::flatbuffers::VOffsetT = 32;
  pub const VT_UNCOMPRESSED_LENGTH: ::flatbuffers::VOffsetT = 34;
  pub const VT_UNCOMPRESSED_CRC32: ::flatbuffers::VOffsetT = 36;

  #[inline]
  pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
    CPS { _tab: table }
  }
  #[allow(unused_mut)]
  pub fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: ::flatbuffers::Allocator + 'bldr>(
    _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
    args: &'args CPSArgs<'args>
  ) -> ::flatbuffers::WIPOffset<CPS<'bldr>> {
    let mut builder = CPSBuilder::new(_fbb);
    builder.add_UNCOMPRESSED_CRC32(args.UNCOMPRESSED_CRC32);
    builder.add_UNCOMPRESSED_LENGTH(args.UNCOMPRESSED_LENGTH);
    if let Some(x) = args.COMPRESSED_DATA { builder.add_COMPRESSED_DATA(x); }
    if let Some(x) = args.STOP_EPOCH { builder.add_STOP_EPOCH(x); }
    if let Some(x) = args.START_EPOCH { builder.add_START_EPOCH(x); }
    builder.add_SEGMENT_INDEX(args.SEGMENT_INDEX);
    if let Some(x) = args.STREAM_ID { builder.add_STREAM_ID(x); }
    builder.add_PACKET_COUNT(args.PACKET_COUNT);
    builder.add_SPACECRAFT_ID(args.SPACECRAFT_ID);
    builder.add_APID(args.APID);
    builder.add_REFERENCE_PERIOD(args.REFERENCE_PERIOD);
    builder.add_MASK_SEND_PERIOD(args.MASK_SEND_PERIOD);
    builder.add_MASK_UPDATE_PERIOD(args.MASK_UPDATE_PERIOD);
    builder.add_PACKET_LENGTH(args.PACKET_LENGTH);
    builder.add_SELF_SYNCHRONIZING(args.SELF_SYNCHRONIZING);
    builder.add_ROBUSTNESS(args.ROBUSTNESS);
    builder.add_ALGORITHM(args.ALGORITHM);
    builder.finish()
  }

  pub fn unpack(&self) -> CPST {
    let ALGORITHM = self.ALGORITHM();
    let PACKET_LENGTH = self.PACKET_LENGTH();
    let PACKET_COUNT = self.PACKET_COUNT();
    let ROBUSTNESS = self.ROBUSTNESS();
    let MASK_UPDATE_PERIOD = self.MASK_UPDATE_PERIOD();
    let MASK_SEND_PERIOD = self.MASK_SEND_PERIOD();
    let REFERENCE_PERIOD = self.REFERENCE_PERIOD();
    let SELF_SYNCHRONIZING = self.SELF_SYNCHRONIZING();
    let STREAM_ID = self.STREAM_ID().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let SEGMENT_INDEX = self.SEGMENT_INDEX();
    let APID = self.APID();
    let SPACECRAFT_ID = self.SPACECRAFT_ID();
    let START_EPOCH = self.START_EPOCH().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let STOP_EPOCH = self.STOP_EPOCH().map(|x| {
      alloc::string::ToString::to_string(x)
    });
    let COMPRESSED_DATA = {
      let x = self.COMPRESSED_DATA();
      x.into_iter().collect()
    };
    let UNCOMPRESSED_LENGTH = self.UNCOMPRESSED_LENGTH();
    let UNCOMPRESSED_CRC32 = self.UNCOMPRESSED_CRC32();
    CPST {
      ALGORITHM,
      PACKET_LENGTH,
      PACKET_COUNT,
      ROBUSTNESS,
      MASK_UPDATE_PERIOD,
      MASK_SEND_PERIOD,
      REFERENCE_PERIOD,
      SELF_SYNCHRONIZING,
      STREAM_ID,
      SEGMENT_INDEX,
      APID,
      SPACECRAFT_ID,
      START_EPOCH,
      STOP_EPOCH,
      COMPRESSED_DATA,
      UNCOMPRESSED_LENGTH,
      UNCOMPRESSED_CRC32,
    }
  }

  /// Compression algorithm used to produce COMPRESSED_DATA.
  #[inline]
  pub fn ALGORITHM(&self) -> packetCompressionAlgorithm {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<packetCompressionAlgorithm>(CPS::VT_ALGORITHM, Some(packetCompressionAlgorithm::POCKET_PLUS)).unwrap()}
  }
  /// Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
  /// defined only for a fixed packet length; a length change starts a new
  /// stream and therefore a new $CPS record.
  #[inline]
  pub fn PACKET_LENGTH(&self) -> u16 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u16>(CPS::VT_PACKET_LENGTH, Some(0)).unwrap()}
  }
  /// Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
  /// this is the only output-size bound a decoder has. The compressed
  /// bitstream alone does not bound the decoded size, and a compression-ratio
  /// guess is unsafe in both directions. Decoders MUST reject a record whose
  /// PACKET_COUNT is zero, and MUST allocate exactly
  /// PACKET_COUNT * PACKET_LENGTH bytes of output.
  #[inline]
  pub fn PACKET_COUNT(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(CPS::VT_PACKET_COUNT, Some(0)).unwrap()}
  }
  /// POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
  /// a window depth, NOT a retransmission interval — it does not by itself
  /// configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
  /// REFERENCE_PERIOD, which are three independent periods.
  #[inline]
  pub fn ROBUSTNESS(&self) -> u8 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u8>(CPS::VT_ROBUSTNESS, Some(0)).unwrap()}
  }
  /// POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
  /// MASK_SEND_PERIOD, and REFERENCE_PERIOD.
  #[inline]
  pub fn MASK_UPDATE_PERIOD(&self) -> u16 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u16>(CPS::VT_MASK_UPDATE_PERIOD, Some(0)).unwrap()}
  }
  /// POCKET+ mask send period (ft), in packets — how often the change mask is
  /// transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
  /// REFERENCE_PERIOD.
  #[inline]
  pub fn MASK_SEND_PERIOD(&self) -> u16 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u16>(CPS::VT_MASK_SEND_PERIOD, Some(0)).unwrap()}
  }
  /// POCKET+ reference packet retransmission period (rt), in packets.
  /// Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
  #[inline]
  pub fn REFERENCE_PERIOD(&self) -> u16 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u16>(CPS::VT_REFERENCE_PERIOD, Some(0)).unwrap()}
  }
  /// True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
  /// ft=1, i.e. it transmits a full reference packet and a full mask, so this
  /// record decodes standalone without its predecessor. There is no
  /// synchronization flag in the POCKET+ bitstream itself: this field is the
  /// producer's assertion about the first packet, and defaults to false.
  #[inline]
  pub fn SELF_SYNCHRONIZING(&self) -> bool {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<bool>(CPS::VT_SELF_SYNCHRONIZING, Some(false)).unwrap()}
  }
  /// Producer-defined stream identity. Records sharing STREAM_ID decode in
  /// SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
  /// poisons every subsequent segment up to the next segment with
  /// SELF_SYNCHRONIZING = true.
  #[inline]
  pub fn STREAM_ID(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(CPS::VT_STREAM_ID, None)}
  }
  /// Zero-based index of this segment within STREAM_ID.
  #[inline]
  pub fn SEGMENT_INDEX(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(CPS::VT_SEGMENT_INDEX, Some(0)).unwrap()}
  }
  /// CCSDS Application Process Identifier of the packets, when known.
  #[inline]
  pub fn APID(&self) -> u16 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u16>(CPS::VT_APID, Some(0)).unwrap()}
  }
  /// Spacecraft identifier of the packets, when known.
  #[inline]
  pub fn SPACECRAFT_ID(&self) -> u16 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u16>(CPS::VT_SPACECRAFT_ID, Some(0)).unwrap()}
  }
  /// UTC epoch of the first packet in the segment, ISO 8601.
  #[inline]
  pub fn START_EPOCH(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(CPS::VT_START_EPOCH, None)}
  }
  /// UTC epoch of the last packet in the segment, ISO 8601.
  #[inline]
  pub fn STOP_EPOCH(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<&str>>(CPS::VT_STOP_EPOCH, None)}
  }
  /// The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
  /// packet is individually octet-aligned; padding is per PACKET, never per
  /// stream. Bit-concatenating packets diverges from the reference
  /// implementation and MUST NOT be done.
  #[inline]
  pub fn COMPRESSED_DATA(&self) -> ::flatbuffers::Vector<'a, u8> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(CPS::VT_COMPRESSED_DATA, None).unwrap()}
  }
  /// PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
  /// output length without recomputing the product from a corrupt header.
  #[inline]
  pub fn UNCOMPRESSED_LENGTH(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(CPS::VT_UNCOMPRESSED_LENGTH, Some(0)).unwrap()}
  }
  /// CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
  #[inline]
  pub fn UNCOMPRESSED_CRC32(&self) -> u32 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u32>(CPS::VT_UNCOMPRESSED_CRC32, Some(0)).unwrap()}
  }
}

impl ::flatbuffers::Verifiable for CPS<'_> {
  #[inline]
  fn run_verifier(
    v: &mut ::flatbuffers::Verifier, pos: usize
  ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
    v.visit_table(pos)?
     .visit_field::<packetCompressionAlgorithm>("ALGORITHM", Self::VT_ALGORITHM, false)?
     .visit_field::<u16>("PACKET_LENGTH", Self::VT_PACKET_LENGTH, false)?
     .visit_field::<u32>("PACKET_COUNT", Self::VT_PACKET_COUNT, false)?
     .visit_field::<u8>("ROBUSTNESS", Self::VT_ROBUSTNESS, false)?
     .visit_field::<u16>("MASK_UPDATE_PERIOD", Self::VT_MASK_UPDATE_PERIOD, false)?
     .visit_field::<u16>("MASK_SEND_PERIOD", Self::VT_MASK_SEND_PERIOD, false)?
     .visit_field::<u16>("REFERENCE_PERIOD", Self::VT_REFERENCE_PERIOD, false)?
     .visit_field::<bool>("SELF_SYNCHRONIZING", Self::VT_SELF_SYNCHRONIZING, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("STREAM_ID", Self::VT_STREAM_ID, false)?
     .visit_field::<u32>("SEGMENT_INDEX", Self::VT_SEGMENT_INDEX, false)?
     .visit_field::<u16>("APID", Self::VT_APID, false)?
     .visit_field::<u16>("SPACECRAFT_ID", Self::VT_SPACECRAFT_ID, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("START_EPOCH", Self::VT_START_EPOCH, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<&str>>("STOP_EPOCH", Self::VT_STOP_EPOCH, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>("COMPRESSED_DATA", Self::VT_COMPRESSED_DATA, true)?
     .visit_field::<u32>("UNCOMPRESSED_LENGTH", Self::VT_UNCOMPRESSED_LENGTH, false)?
     .visit_field::<u32>("UNCOMPRESSED_CRC32", Self::VT_UNCOMPRESSED_CRC32, false)?
     .finish();
    Ok(())
  }
}
pub struct CPSArgs<'a> {
    pub ALGORITHM: packetCompressionAlgorithm,
    pub PACKET_LENGTH: u16,
    pub PACKET_COUNT: u32,
    pub ROBUSTNESS: u8,
    pub MASK_UPDATE_PERIOD: u16,
    pub MASK_SEND_PERIOD: u16,
    pub REFERENCE_PERIOD: u16,
    pub SELF_SYNCHRONIZING: bool,
    pub STREAM_ID: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub SEGMENT_INDEX: u32,
    pub APID: u16,
    pub SPACECRAFT_ID: u16,
    pub START_EPOCH: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub STOP_EPOCH: Option<::flatbuffers::WIPOffset<&'a str>>,
    pub COMPRESSED_DATA: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
    pub UNCOMPRESSED_LENGTH: u32,
    pub UNCOMPRESSED_CRC32: u32,
}
impl<'a> Default for CPSArgs<'a> {
  #[inline]
  fn default() -> Self {
    CPSArgs {
      ALGORITHM: packetCompressionAlgorithm::POCKET_PLUS,
      PACKET_LENGTH: 0,
      PACKET_COUNT: 0,
      ROBUSTNESS: 0,
      MASK_UPDATE_PERIOD: 0,
      MASK_SEND_PERIOD: 0,
      REFERENCE_PERIOD: 0,
      SELF_SYNCHRONIZING: false,
      STREAM_ID: None,
      SEGMENT_INDEX: 0,
      APID: 0,
      SPACECRAFT_ID: 0,
      START_EPOCH: None,
      STOP_EPOCH: None,
      COMPRESSED_DATA: None, // required field
      UNCOMPRESSED_LENGTH: 0,
      UNCOMPRESSED_CRC32: 0,
    }
  }
}

pub struct CPSBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
  fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
  start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> CPSBuilder<'a, 'b, A> {
  #[inline]
  pub fn add_ALGORITHM(&mut self, ALGORITHM: packetCompressionAlgorithm) {
    self.fbb_.push_slot::<packetCompressionAlgorithm>(CPS::VT_ALGORITHM, ALGORITHM, packetCompressionAlgorithm::POCKET_PLUS);
  }
  #[inline]
  pub fn add_PACKET_LENGTH(&mut self, PACKET_LENGTH: u16) {
    self.fbb_.push_slot::<u16>(CPS::VT_PACKET_LENGTH, PACKET_LENGTH, 0);
  }
  #[inline]
  pub fn add_PACKET_COUNT(&mut self, PACKET_COUNT: u32) {
    self.fbb_.push_slot::<u32>(CPS::VT_PACKET_COUNT, PACKET_COUNT, 0);
  }
  #[inline]
  pub fn add_ROBUSTNESS(&mut self, ROBUSTNESS: u8) {
    self.fbb_.push_slot::<u8>(CPS::VT_ROBUSTNESS, ROBUSTNESS, 0);
  }
  #[inline]
  pub fn add_MASK_UPDATE_PERIOD(&mut self, MASK_UPDATE_PERIOD: u16) {
    self.fbb_.push_slot::<u16>(CPS::VT_MASK_UPDATE_PERIOD, MASK_UPDATE_PERIOD, 0);
  }
  #[inline]
  pub fn add_MASK_SEND_PERIOD(&mut self, MASK_SEND_PERIOD: u16) {
    self.fbb_.push_slot::<u16>(CPS::VT_MASK_SEND_PERIOD, MASK_SEND_PERIOD, 0);
  }
  #[inline]
  pub fn add_REFERENCE_PERIOD(&mut self, REFERENCE_PERIOD: u16) {
    self.fbb_.push_slot::<u16>(CPS::VT_REFERENCE_PERIOD, REFERENCE_PERIOD, 0);
  }
  #[inline]
  pub fn add_SELF_SYNCHRONIZING(&mut self, SELF_SYNCHRONIZING: bool) {
    self.fbb_.push_slot::<bool>(CPS::VT_SELF_SYNCHRONIZING, SELF_SYNCHRONIZING, false);
  }
  #[inline]
  pub fn add_STREAM_ID(&mut self, STREAM_ID: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(CPS::VT_STREAM_ID, STREAM_ID);
  }
  #[inline]
  pub fn add_SEGMENT_INDEX(&mut self, SEGMENT_INDEX: u32) {
    self.fbb_.push_slot::<u32>(CPS::VT_SEGMENT_INDEX, SEGMENT_INDEX, 0);
  }
  #[inline]
  pub fn add_APID(&mut self, APID: u16) {
    self.fbb_.push_slot::<u16>(CPS::VT_APID, APID, 0);
  }
  #[inline]
  pub fn add_SPACECRAFT_ID(&mut self, SPACECRAFT_ID: u16) {
    self.fbb_.push_slot::<u16>(CPS::VT_SPACECRAFT_ID, SPACECRAFT_ID, 0);
  }
  #[inline]
  pub fn add_START_EPOCH(&mut self, START_EPOCH: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(CPS::VT_START_EPOCH, START_EPOCH);
  }
  #[inline]
  pub fn add_STOP_EPOCH(&mut self, STOP_EPOCH: ::flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(CPS::VT_STOP_EPOCH, STOP_EPOCH);
  }
  #[inline]
  pub fn add_COMPRESSED_DATA(&mut self, COMPRESSED_DATA: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b , u8>>) {
    self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(CPS::VT_COMPRESSED_DATA, COMPRESSED_DATA);
  }
  #[inline]
  pub fn add_UNCOMPRESSED_LENGTH(&mut self, UNCOMPRESSED_LENGTH: u32) {
    self.fbb_.push_slot::<u32>(CPS::VT_UNCOMPRESSED_LENGTH, UNCOMPRESSED_LENGTH, 0);
  }
  #[inline]
  pub fn add_UNCOMPRESSED_CRC32(&mut self, UNCOMPRESSED_CRC32: u32) {
    self.fbb_.push_slot::<u32>(CPS::VT_UNCOMPRESSED_CRC32, UNCOMPRESSED_CRC32, 0);
  }
  #[inline]
  pub fn new(_fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>) -> CPSBuilder<'a, 'b, A> {
    let start = _fbb.start_table();
    CPSBuilder {
      fbb_: _fbb,
      start_: start,
    }
  }
  #[inline]
  pub fn finish(self) -> ::flatbuffers::WIPOffset<CPS<'a>> {
    let o = self.fbb_.end_table(self.start_);
    self.fbb_.required(o, CPS::VT_COMPRESSED_DATA,"COMPRESSED_DATA");
    ::flatbuffers::WIPOffset::new(o.value())
  }
}

impl ::core::fmt::Debug for CPS<'_> {
  fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
    let mut ds = f.debug_struct("CPS");
      ds.field("ALGORITHM", &self.ALGORITHM());
      ds.field("PACKET_LENGTH", &self.PACKET_LENGTH());
      ds.field("PACKET_COUNT", &self.PACKET_COUNT());
      ds.field("ROBUSTNESS", &self.ROBUSTNESS());
      ds.field("MASK_UPDATE_PERIOD", &self.MASK_UPDATE_PERIOD());
      ds.field("MASK_SEND_PERIOD", &self.MASK_SEND_PERIOD());
      ds.field("REFERENCE_PERIOD", &self.REFERENCE_PERIOD());
      ds.field("SELF_SYNCHRONIZING", &self.SELF_SYNCHRONIZING());
      ds.field("STREAM_ID", &self.STREAM_ID());
      ds.field("SEGMENT_INDEX", &self.SEGMENT_INDEX());
      ds.field("APID", &self.APID());
      ds.field("SPACECRAFT_ID", &self.SPACECRAFT_ID());
      ds.field("START_EPOCH", &self.START_EPOCH());
      ds.field("STOP_EPOCH", &self.STOP_EPOCH());
      ds.field("COMPRESSED_DATA", &self.COMPRESSED_DATA());
      ds.field("UNCOMPRESSED_LENGTH", &self.UNCOMPRESSED_LENGTH());
      ds.field("UNCOMPRESSED_CRC32", &self.UNCOMPRESSED_CRC32());
      ds.finish()
  }
}
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq)]
pub struct CPST {
  pub ALGORITHM: packetCompressionAlgorithm,
  pub PACKET_LENGTH: u16,
  pub PACKET_COUNT: u32,
  pub ROBUSTNESS: u8,
  pub MASK_UPDATE_PERIOD: u16,
  pub MASK_SEND_PERIOD: u16,
  pub REFERENCE_PERIOD: u16,
  pub SELF_SYNCHRONIZING: bool,
  pub STREAM_ID: Option<alloc::string::String>,
  pub SEGMENT_INDEX: u32,
  pub APID: u16,
  pub SPACECRAFT_ID: u16,
  pub START_EPOCH: Option<alloc::string::String>,
  pub STOP_EPOCH: Option<alloc::string::String>,
  pub COMPRESSED_DATA: alloc::vec::Vec<u8>,
  pub UNCOMPRESSED_LENGTH: u32,
  pub UNCOMPRESSED_CRC32: u32,
}
impl Default for CPST {
  fn default() -> Self {
    Self {
      ALGORITHM: packetCompressionAlgorithm::POCKET_PLUS,
      PACKET_LENGTH: 0,
      PACKET_COUNT: 0,
      ROBUSTNESS: 0,
      MASK_UPDATE_PERIOD: 0,
      MASK_SEND_PERIOD: 0,
      REFERENCE_PERIOD: 0,
      SELF_SYNCHRONIZING: false,
      STREAM_ID: None,
      SEGMENT_INDEX: 0,
      APID: 0,
      SPACECRAFT_ID: 0,
      START_EPOCH: None,
      STOP_EPOCH: None,
      COMPRESSED_DATA: Default::default(),
      UNCOMPRESSED_LENGTH: 0,
      UNCOMPRESSED_CRC32: 0,
    }
  }
}
impl CPST {
  pub fn pack<'b, A: ::flatbuffers::Allocator + 'b>(
    &self,
    _fbb: &mut ::flatbuffers::FlatBufferBuilder<'b, A>
  ) -> ::flatbuffers::WIPOffset<CPS<'b>> {
    let ALGORITHM = self.ALGORITHM;
    let PACKET_LENGTH = self.PACKET_LENGTH;
    let PACKET_COUNT = self.PACKET_COUNT;
    let ROBUSTNESS = self.ROBUSTNESS;
    let MASK_UPDATE_PERIOD = self.MASK_UPDATE_PERIOD;
    let MASK_SEND_PERIOD = self.MASK_SEND_PERIOD;
    let REFERENCE_PERIOD = self.REFERENCE_PERIOD;
    let SELF_SYNCHRONIZING = self.SELF_SYNCHRONIZING;
    let STREAM_ID = self.STREAM_ID.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let SEGMENT_INDEX = self.SEGMENT_INDEX;
    let APID = self.APID;
    let SPACECRAFT_ID = self.SPACECRAFT_ID;
    let START_EPOCH = self.START_EPOCH.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let STOP_EPOCH = self.STOP_EPOCH.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let COMPRESSED_DATA = Some({
      let x = &self.COMPRESSED_DATA;
      _fbb.create_vector(x)
    });
    let UNCOMPRESSED_LENGTH = self.UNCOMPRESSED_LENGTH;
    let UNCOMPRESSED_CRC32 = self.UNCOMPRESSED_CRC32;
    CPS::create(_fbb, &CPSArgs{
      ALGORITHM,
      PACKET_LENGTH,
      PACKET_COUNT,
      ROBUSTNESS,
      MASK_UPDATE_PERIOD,
      MASK_SEND_PERIOD,
      REFERENCE_PERIOD,
      SELF_SYNCHRONIZING,
      STREAM_ID,
      SEGMENT_INDEX,
      APID,
      SPACECRAFT_ID,
      START_EPOCH,
      STOP_EPOCH,
      COMPRESSED_DATA,
      UNCOMPRESSED_LENGTH,
      UNCOMPRESSED_CRC32,
    })
  }
}
#[inline]
/// Verifies that a buffer of bytes contains a `CPS`
/// and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_cps_unchecked`.
pub fn root_as_cps(buf: &[u8]) -> Result<CPS<'_>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::root::<CPS>(buf)
}
#[inline]
/// Verifies that a buffer of bytes contains a size prefixed
/// `CPS` and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `size_prefixed_root_as_cps_unchecked`.
pub fn size_prefixed_root_as_cps(buf: &[u8]) -> Result<CPS<'_>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::size_prefixed_root::<CPS>(buf)
}
#[inline]
/// Verifies, with the given options, that a buffer of bytes
/// contains a `CPS` and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_cps_unchecked`.
pub fn root_as_cps_with_opts<'b, 'o>(
  opts: &'o ::flatbuffers::VerifierOptions,
  buf: &'b [u8],
) -> Result<CPS<'b>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::root_with_opts::<CPS<'b>>(opts, buf)
}
#[inline]
/// Verifies, with the given verifier options, that a buffer of
/// bytes contains a size prefixed `CPS` and returns
/// it. Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_cps_unchecked`.
pub fn size_prefixed_root_as_cps_with_opts<'b, 'o>(
  opts: &'o ::flatbuffers::VerifierOptions,
  buf: &'b [u8],
) -> Result<CPS<'b>, ::flatbuffers::InvalidFlatbuffer> {
  ::flatbuffers::size_prefixed_root_with_opts::<CPS<'b>>(opts, buf)
}
#[inline]
/// Assumes, without verification, that a buffer of bytes contains a CPS and returns it.
/// # Safety
/// Callers must trust the given bytes do indeed contain a valid `CPS`.
pub unsafe fn root_as_cps_unchecked(buf: &[u8]) -> CPS<'_> {
  unsafe { ::flatbuffers::root_unchecked::<CPS>(buf) }
}
#[inline]
/// Assumes, without verification, that a buffer of bytes contains a size prefixed CPS and returns it.
/// # Safety
/// Callers must trust the given bytes do indeed contain a valid size prefixed `CPS`.
pub unsafe fn size_prefixed_root_as_cps_unchecked(buf: &[u8]) -> CPS<'_> {
  unsafe { ::flatbuffers::size_prefixed_root_unchecked::<CPS>(buf) }
}
pub const CPS_IDENTIFIER: &str = "$CPS";

#[inline]
pub fn cps_buffer_has_identifier(buf: &[u8]) -> bool {
  ::flatbuffers::buffer_has_identifier(buf, CPS_IDENTIFIER, false)
}

#[inline]
pub fn cps_size_prefixed_buffer_has_identifier(buf: &[u8]) -> bool {
  ::flatbuffers::buffer_has_identifier(buf, CPS_IDENTIFIER, true)
}

#[inline]
pub fn finish_cps_buffer<'a, 'b, A: ::flatbuffers::Allocator + 'a>(
    fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    root: ::flatbuffers::WIPOffset<CPS<'a>>) {
  fbb.finish(root, Some(CPS_IDENTIFIER));
}

#[inline]
pub fn finish_size_prefixed_cps_buffer<'a, 'b, A: ::flatbuffers::Allocator + 'a>(fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>, root: ::flatbuffers::WIPOffset<CPS<'a>>) {
  fbb.finish_size_prefixed(root, Some(CPS_IDENTIFIER));
}
