// <auto-generated>
//  automatically generated by the FlatBuffers compiler, do not modify
// </auto-generated>

using global::System;
using global::System.Collections.Generic;
using global::Google.FlatBuffers;

/// One member WASM module of the app. References module identity; never
/// embeds the module artifact itself (delivery is the module-bundle lane).
public struct APPModuleRef : IFlatbufferObject
{
  private Table __p;
  public ByteBuffer ByteBuffer { get { return __p.bb; } }
  public static void ValidateVersion() { FlatBufferConstants.FLATBUFFERS_25_12_19(); }
  public static APPModuleRef GetRootAsAPPModuleRef(ByteBuffer _bb) { return GetRootAsAPPModuleRef(_bb, new APPModuleRef()); }
  public static APPModuleRef GetRootAsAPPModuleRef(ByteBuffer _bb, APPModuleRef obj) { return (obj.__assign(_bb.GetInt(_bb.Position) + _bb.Position, _bb)); }
  public void __init(int _i, ByteBuffer _bb) { __p = new Table(_i, _bb); }
  public APPModuleRef __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /// App-local stable reference for this module. Required, unique within
  /// the manifest.
  public string ID { get { int o = __p.__offset(4); return o != 0 ? __p.__string(o + __p.bb_pos) : null; } }
#if ENABLE_SPAN_T
  public Span<byte> GetIDBytes() { return __p.__vector_as_span<byte>(4, 1); }
#else
  public ArraySegment<byte>? GetIDBytes() { return __p.__vector_as_arraysegment(4); }
#endif
  public byte[] GetIDArray() { return __p.__vector_as_array<byte>(4); }
  /// Plugin identifier of the module as published in its plugin listing.
  public string PLUGIN_ID { get { int o = __p.__offset(6); return o != 0 ? __p.__string(o + __p.bb_pos) : null; } }
#if ENABLE_SPAN_T
  public Span<byte> GetPLUGIN_IDBytes() { return __p.__vector_as_span<byte>(6, 1); }
#else
  public ArraySegment<byte>? GetPLUGIN_IDBytes() { return __p.__vector_as_arraysegment(6); }
#endif
  public byte[] GetPLUGIN_IDArray() { return __p.__vector_as_array<byte>(6); }
  /// 64 lowercase hexadecimal characters encoding SHA-256 of the module's
  /// portable (pre-AOT, publication-trailer-stripped) WASM bytes. This is
  /// the identity capability policies and signature policies key on.
  public string CONTENT_HASH { get { int o = __p.__offset(8); return o != 0 ? __p.__string(o + __p.bb_pos) : null; } }
#if ENABLE_SPAN_T
  public Span<byte> GetCONTENT_HASHBytes() { return __p.__vector_as_span<byte>(8, 1); }
#else
  public ArraySegment<byte>? GetCONTENT_HASHBytes() { return __p.__vector_as_arraysegment(8); }
#endif
  public byte[] GetCONTENT_HASHArray() { return __p.__vector_as_array<byte>(8); }
  /// Module version expected by the app.
  public string VERSION { get { int o = __p.__offset(10); return o != 0 ? __p.__string(o + __p.bb_pos) : null; } }
#if ENABLE_SPAN_T
  public Span<byte> GetVERSIONBytes() { return __p.__vector_as_span<byte>(10, 1); }
#else
  public ArraySegment<byte>? GetVERSIONBytes() { return __p.__vector_as_arraysegment(10); }
#endif
  public byte[] GetVERSIONArray() { return __p.__vector_as_array<byte>(10); }
  /// Free-text launcher hint, for example primary, worker, or ui-host.
  public string ROLE { get { int o = __p.__offset(12); return o != 0 ? __p.__string(o + __p.bb_pos) : null; } }
#if ENABLE_SPAN_T
  public Span<byte> GetROLEBytes() { return __p.__vector_as_span<byte>(12, 1); }
#else
  public ArraySegment<byte>? GetROLEBytes() { return __p.__vector_as_arraysegment(12); }
#endif
  public byte[] GetROLEArray() { return __p.__vector_as_array<byte>(12); }
  /// Human-readable summary.
  public string DESCRIPTION { get { int o = __p.__offset(14); return o != 0 ? __p.__string(o + __p.bb_pos) : null; } }
#if ENABLE_SPAN_T
  public Span<byte> GetDESCRIPTIONBytes() { return __p.__vector_as_span<byte>(14, 1); }
#else
  public ArraySegment<byte>? GetDESCRIPTIONBytes() { return __p.__vector_as_arraysegment(14); }
#endif
  public byte[] GetDESCRIPTIONArray() { return __p.__vector_as_array<byte>(14); }
  /// Per-invocation wall-clock budget override in milliseconds. Zero means
  /// the host runtime default applies.
  public ulong MAX_WALL_CLOCK_MS { get { int o = __p.__offset(16); return o != 0 ? __p.bb.GetUlong(o + __p.bb_pos) : (ulong)0; } }
  /// Per-invocation execution-cost (fuel) budget override. Zero means the
  /// host runtime default applies.
  public ulong MAX_COST_UNITS { get { int o = __p.__offset(18); return o != 0 ? __p.bb.GetUlong(o + __p.bb_pos) : (ulong)0; } }
  /// Linear-memory ceiling override in 64 KiB WASM pages. Zero means the
  /// host runtime default applies.
  public uint MAX_MEMORY_PAGES { get { int o = __p.__offset(20); return o != 0 ? __p.bb.GetUint(o + __p.bb_pos) : (uint)0; } }
  /// Where this module is instantiated. PAGE or BOTH means the module also
  /// loads in the browser: the page resolves its bytes by CONTENT_HASH over
  /// IPFS and instantiates it through the SAME isomorphic module-sdk harness
  /// ABI the SDN nodes use (manifest + plugin_invoke_stream) — never through
  /// a bespoke page-only loader. Defaults to NODE to preserve the prior
  /// node-only behavior of manifests written before this field existed.
  public appRuntimeTarget RUNTIME_TARGET { get { int o = __p.__offset(22); return o != 0 ? (appRuntimeTarget)__p.bb.Get(o + __p.bb_pos) : appRuntimeTarget.NODE; } }

  public static Offset<APPModuleRef> CreateAPPModuleRef(FlatBufferBuilder builder,
      StringOffset IDOffset = default(StringOffset),
      StringOffset PLUGIN_IDOffset = default(StringOffset),
      StringOffset CONTENT_HASHOffset = default(StringOffset),
      StringOffset VERSIONOffset = default(StringOffset),
      StringOffset ROLEOffset = default(StringOffset),
      StringOffset DESCRIPTIONOffset = default(StringOffset),
      ulong MAX_WALL_CLOCK_MS = 0,
      ulong MAX_COST_UNITS = 0,
      uint MAX_MEMORY_PAGES = 0,
      appRuntimeTarget RUNTIME_TARGET = appRuntimeTarget.NODE) {
    builder.StartTable(10);
    APPModuleRef.AddMAX_COST_UNITS(builder, MAX_COST_UNITS);
    APPModuleRef.AddMAX_WALL_CLOCK_MS(builder, MAX_WALL_CLOCK_MS);
    APPModuleRef.AddMAX_MEMORY_PAGES(builder, MAX_MEMORY_PAGES);
    APPModuleRef.AddDESCRIPTION(builder, DESCRIPTIONOffset);
    APPModuleRef.AddROLE(builder, ROLEOffset);
    APPModuleRef.AddVERSION(builder, VERSIONOffset);
    APPModuleRef.AddCONTENT_HASH(builder, CONTENT_HASHOffset);
    APPModuleRef.AddPLUGIN_ID(builder, PLUGIN_IDOffset);
    APPModuleRef.AddID(builder, IDOffset);
    APPModuleRef.AddRUNTIME_TARGET(builder, RUNTIME_TARGET);
    return APPModuleRef.EndAPPModuleRef(builder);
  }

  public static void StartAPPModuleRef(FlatBufferBuilder builder) { builder.StartTable(10); }
  public static void AddID(FlatBufferBuilder builder, StringOffset IDOffset) { builder.AddOffset(0, IDOffset.Value, 0); }
  public static void AddPLUGIN_ID(FlatBufferBuilder builder, StringOffset PLUGIN_IDOffset) { builder.AddOffset(1, PLUGIN_IDOffset.Value, 0); }
  public static void AddCONTENT_HASH(FlatBufferBuilder builder, StringOffset CONTENT_HASHOffset) { builder.AddOffset(2, CONTENT_HASHOffset.Value, 0); }
  public static void AddVERSION(FlatBufferBuilder builder, StringOffset VERSIONOffset) { builder.AddOffset(3, VERSIONOffset.Value, 0); }
  public static void AddROLE(FlatBufferBuilder builder, StringOffset ROLEOffset) { builder.AddOffset(4, ROLEOffset.Value, 0); }
  public static void AddDESCRIPTION(FlatBufferBuilder builder, StringOffset DESCRIPTIONOffset) { builder.AddOffset(5, DESCRIPTIONOffset.Value, 0); }
  public static void AddMAX_WALL_CLOCK_MS(FlatBufferBuilder builder, ulong MAX_WALL_CLOCK_MS) { builder.AddUlong(6, MAX_WALL_CLOCK_MS, 0); }
  public static void AddMAX_COST_UNITS(FlatBufferBuilder builder, ulong MAX_COST_UNITS) { builder.AddUlong(7, MAX_COST_UNITS, 0); }
  public static void AddMAX_MEMORY_PAGES(FlatBufferBuilder builder, uint MAX_MEMORY_PAGES) { builder.AddUint(8, MAX_MEMORY_PAGES, 0); }
  public static void AddRUNTIME_TARGET(FlatBufferBuilder builder, appRuntimeTarget RUNTIME_TARGET) { builder.AddByte(9, (byte)RUNTIME_TARGET, 0); }
  public static Offset<APPModuleRef> EndAPPModuleRef(FlatBufferBuilder builder) {
    int o = builder.EndTable();
    builder.Required(o, 4);  // ID
    return new Offset<APPModuleRef>(o);
  }

  public static VectorOffset CreateSortedVectorOfAPPModuleRef(FlatBufferBuilder builder, Offset<APPModuleRef>[] offsets) {
    Array.Sort(offsets,
      (Offset<APPModuleRef> o1, Offset<APPModuleRef> o2) =>
        string.CompareOrdinal(new APPModuleRef().__assign(builder.DataBuffer.Length - o1.Value, builder.DataBuffer).ID, new APPModuleRef().__assign(builder.DataBuffer.Length - o2.Value, builder.DataBuffer).ID));
    return builder.CreateVectorOfTables(offsets);
  }

  public static APPModuleRef? __lookup_by_key(int vectorLocation, string key, ByteBuffer bb) {
    APPModuleRef obj_ = new APPModuleRef();
    int span = bb.GetInt(vectorLocation - 4);
    int start = 0;
    while (span != 0) {
      int middle = span / 2;
      int tableOffset = Table.__indirect(vectorLocation + 4 * (start + middle), bb);
      obj_.__assign(tableOffset, bb);
      int comp = string.CompareOrdinal(obj_.ID, key);
      if (comp > 0) {
        span = middle;
      } else if (comp < 0) {
        middle++;
        start += middle;
        span -= middle;
      } else {
        return obj_;
      }
    }
    return null;
  }
  public APPModuleRefT UnPack() {
    var _o = new APPModuleRefT();
    this.UnPackTo(_o);
    return _o;
  }
  public void UnPackTo(APPModuleRefT _o) {
    _o.ID = this.ID;
    _o.PLUGIN_ID = this.PLUGIN_ID;
    _o.CONTENT_HASH = this.CONTENT_HASH;
    _o.VERSION = this.VERSION;
    _o.ROLE = this.ROLE;
    _o.DESCRIPTION = this.DESCRIPTION;
    _o.MAX_WALL_CLOCK_MS = this.MAX_WALL_CLOCK_MS;
    _o.MAX_COST_UNITS = this.MAX_COST_UNITS;
    _o.MAX_MEMORY_PAGES = this.MAX_MEMORY_PAGES;
    _o.RUNTIME_TARGET = this.RUNTIME_TARGET;
  }
  public static Offset<APPModuleRef> Pack(FlatBufferBuilder builder, APPModuleRefT _o) {
    if (_o == null) return default(Offset<APPModuleRef>);
    var _ID = _o.ID == null ? default(StringOffset) : builder.CreateString(_o.ID);
    var _PLUGIN_ID = _o.PLUGIN_ID == null ? default(StringOffset) : builder.CreateString(_o.PLUGIN_ID);
    var _CONTENT_HASH = _o.CONTENT_HASH == null ? default(StringOffset) : builder.CreateString(_o.CONTENT_HASH);
    var _VERSION = _o.VERSION == null ? default(StringOffset) : builder.CreateString(_o.VERSION);
    var _ROLE = _o.ROLE == null ? default(StringOffset) : builder.CreateString(_o.ROLE);
    var _DESCRIPTION = _o.DESCRIPTION == null ? default(StringOffset) : builder.CreateString(_o.DESCRIPTION);
    return CreateAPPModuleRef(
      builder,
      _ID,
      _PLUGIN_ID,
      _CONTENT_HASH,
      _VERSION,
      _ROLE,
      _DESCRIPTION,
      _o.MAX_WALL_CLOCK_MS,
      _o.MAX_COST_UNITS,
      _o.MAX_MEMORY_PAGES,
      _o.RUNTIME_TARGET);
  }
}

public class APPModuleRefT
{
  public string ID { get; set; }
  public string PLUGIN_ID { get; set; }
  public string CONTENT_HASH { get; set; }
  public string VERSION { get; set; }
  public string ROLE { get; set; }
  public string DESCRIPTION { get; set; }
  public ulong MAX_WALL_CLOCK_MS { get; set; }
  public ulong MAX_COST_UNITS { get; set; }
  public uint MAX_MEMORY_PAGES { get; set; }
  public appRuntimeTarget RUNTIME_TARGET { get; set; }

  public APPModuleRefT() {
    this.ID = null;
    this.PLUGIN_ID = null;
    this.CONTENT_HASH = null;
    this.VERSION = null;
    this.ROLE = null;
    this.DESCRIPTION = null;
    this.MAX_WALL_CLOCK_MS = 0;
    this.MAX_COST_UNITS = 0;
    this.MAX_MEMORY_PAGES = 0;
    this.RUNTIME_TARGET = appRuntimeTarget.NODE;
  }
}


static public class APPModuleRefVerify
{
  static public bool Verify(Google.FlatBuffers.Verifier verifier, uint tablePos)
  {
    return verifier.VerifyTableStart(tablePos)
      && verifier.VerifyString(tablePos, 4 /*ID*/, true)
      && verifier.VerifyString(tablePos, 6 /*PLUGIN_ID*/, false)
      && verifier.VerifyString(tablePos, 8 /*CONTENT_HASH*/, false)
      && verifier.VerifyString(tablePos, 10 /*VERSION*/, false)
      && verifier.VerifyString(tablePos, 12 /*ROLE*/, false)
      && verifier.VerifyString(tablePos, 14 /*DESCRIPTION*/, false)
      && verifier.VerifyField(tablePos, 16 /*MAX_WALL_CLOCK_MS*/, 8 /*ulong*/, 8, false)
      && verifier.VerifyField(tablePos, 18 /*MAX_COST_UNITS*/, 8 /*ulong*/, 8, false)
      && verifier.VerifyField(tablePos, 20 /*MAX_MEMORY_PAGES*/, 4 /*uint*/, 4, false)
      && verifier.VerifyField(tablePos, 22 /*RUNTIME_TARGET*/, 1 /*appRuntimeTarget*/, 1, false)
      && verifier.VerifyTableEnd(tablePos);
  }
}
