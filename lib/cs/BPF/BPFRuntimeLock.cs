// <auto-generated>
//  automatically generated by the FlatBuffers compiler, do not modify
// </auto-generated>

using global::System;
using global::System.Collections.Generic;
using global::Google.FlatBuffers;

/// The runtime restriction the produced artifact enforces on itself.
///
/// AUTHORIZATION. A host is authorized when it equals an `ALLOWED_DOMAINS`
/// entry, OR ends with an `ALLOWED_TLDS` entry, OR equals a `DEV_DOMAINS`
/// entry. Suffix rules are DOT-ANCHORED: `.mil` admits `x.mil` and `a.b.mil`
/// and admits neither `mil` nor `evilmil`; a suffix rule never matches a
/// host that merely contains it (`x.mil.example` is refused).
///
/// UNLOCKED IS EXPLICIT. `ALLOWED_DOMAINS` empty AND `ALLOWED_TLDS` empty
/// means every host is authorized. This is a stated, allowed configuration,
/// which is why this table is required on `BPF`: an absent lock is never
/// read as an unlocked one.
///
/// EXPIRY IS INDEPENDENT of authorization: an unlocked profile still
/// expires. There is deliberately no stored expiry instant — expiry is
/// `COMPILED_AT_MS + TTL_DAYS * 86400000` and a second, storable copy of it
/// could disagree with the two values that produce it.
public struct BPFRuntimeLock : IFlatbufferObject
{
  private Table __p;
  public ByteBuffer ByteBuffer { get { return __p.bb; } }
  public static void ValidateVersion() { FlatBufferConstants.FLATBUFFERS_25_12_19(); }
  public static BPFRuntimeLock GetRootAsBPFRuntimeLock(ByteBuffer _bb) { return GetRootAsBPFRuntimeLock(_bb, new BPFRuntimeLock()); }
  public static BPFRuntimeLock GetRootAsBPFRuntimeLock(ByteBuffer _bb, BPFRuntimeLock obj) { return (obj.__assign(_bb.GetInt(_bb.Position) + _bb.Position, _bb)); }
  public void __init(int _i, ByteBuffer _bb) { __p = new Table(_i, _bb); }
  public BPFRuntimeLock __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /// Exact hostnames authorized to run the produced artifact. Empty
  /// together with `ALLOWED_TLDS` means unrestricted.
  public string ALLOWED_DOMAINS(int j) { int o = __p.__offset(4); return o != 0 ? __p.__string(__p.__vector(o) + j * 4) : null; }
  public int ALLOWED_DOMAINSLength { get { int o = __p.__offset(4); return o != 0 ? __p.__vector_len(o) : 0; } }
  /// Dot-anchored hostname suffix rules, each written with its leading dot,
  /// for example `.gov`. Empty together with `ALLOWED_DOMAINS` means
  /// unrestricted.
  public string ALLOWED_TLDS(int j) { int o = __p.__offset(6); return o != 0 ? __p.__string(__p.__vector(o) + j * 4) : null; }
  public int ALLOWED_TLDSLength { get { int o = __p.__offset(6); return o != 0 ? __p.__vector_len(o) : 0; } }
  /// Development hosts authorized in addition to the rules above, so a
  /// locked profile remains runnable in a development context. Kept
  /// separate from `ALLOWED_DOMAINS` because a development exemption and a
  /// deployment authorization are different facts: folded together, a
  /// development host would be indistinguishable from a deployment grant
  /// and would silently persist into a locked artifact.
  public string DEV_DOMAINS(int j) { int o = __p.__offset(8); return o != 0 ? __p.__string(__p.__vector(o) + j * 4) : null; }
  public int DEV_DOMAINSLength { get { int o = __p.__offset(8); return o != 0 ? __p.__vector_len(o) : 0; } }
  /// Lifetime of the produced artifact in days from `COMPILED_AT_MS`.
  /// Normative range 1 through 365 inclusive; 180 when unstated. A value of
  /// 0 or above 365 is invalid and MUST be rejected, never clamped.
  public uint TTL_DAYS { get { int o = __p.__offset(10); return o != 0 ? __p.bb.GetUint(o + __p.bb_pos) : (uint)180; } }
  /// Milliseconds since the Unix epoch at which the artifact was composed.
  /// 0 means the profile has not been used to compose an artifact yet and
  /// carries no expiry; the composing step stamps it.
  public ulong COMPILED_AT_MS { get { int o = __p.__offset(12); return o != 0 ? __p.bb.GetUlong(o + __p.bb_pos) : (ulong)0; } }

  public static Offset<BPFRuntimeLock> CreateBPFRuntimeLock(FlatBufferBuilder builder,
      VectorOffset ALLOWED_DOMAINSOffset = default(VectorOffset),
      VectorOffset ALLOWED_TLDSOffset = default(VectorOffset),
      VectorOffset DEV_DOMAINSOffset = default(VectorOffset),
      uint TTL_DAYS = 180,
      ulong COMPILED_AT_MS = 0) {
    builder.StartTable(5);
    BPFRuntimeLock.AddCOMPILED_AT_MS(builder, COMPILED_AT_MS);
    BPFRuntimeLock.AddTTL_DAYS(builder, TTL_DAYS);
    BPFRuntimeLock.AddDEV_DOMAINS(builder, DEV_DOMAINSOffset);
    BPFRuntimeLock.AddALLOWED_TLDS(builder, ALLOWED_TLDSOffset);
    BPFRuntimeLock.AddALLOWED_DOMAINS(builder, ALLOWED_DOMAINSOffset);
    return BPFRuntimeLock.EndBPFRuntimeLock(builder);
  }

  public static void StartBPFRuntimeLock(FlatBufferBuilder builder) { builder.StartTable(5); }
  public static void AddALLOWED_DOMAINS(FlatBufferBuilder builder, VectorOffset ALLOWED_DOMAINSOffset) { builder.AddOffset(0, ALLOWED_DOMAINSOffset.Value, 0); }
  public static VectorOffset CreateALLOWED_DOMAINSVector(FlatBufferBuilder builder, StringOffset[] data) { builder.StartVector(4, data.Length, 4); for (int i = data.Length - 1; i >= 0; i--) builder.AddOffset(data[i].Value); return builder.EndVector(); }
  public static VectorOffset CreateALLOWED_DOMAINSVectorBlock(FlatBufferBuilder builder, StringOffset[] data) { builder.StartVector(4, data.Length, 4); builder.Add(data); return builder.EndVector(); }
  public static VectorOffset CreateALLOWED_DOMAINSVectorBlock(FlatBufferBuilder builder, ArraySegment<StringOffset> data) { builder.StartVector(4, data.Count, 4); builder.Add(data); return builder.EndVector(); }
  public static VectorOffset CreateALLOWED_DOMAINSVectorBlock(FlatBufferBuilder builder, IntPtr dataPtr, int sizeInBytes) { builder.StartVector(1, sizeInBytes, 1); builder.Add<StringOffset>(dataPtr, sizeInBytes); return builder.EndVector(); }
  public static void StartALLOWED_DOMAINSVector(FlatBufferBuilder builder, int numElems) { builder.StartVector(4, numElems, 4); }
  public static void AddALLOWED_TLDS(FlatBufferBuilder builder, VectorOffset ALLOWED_TLDSOffset) { builder.AddOffset(1, ALLOWED_TLDSOffset.Value, 0); }
  public static VectorOffset CreateALLOWED_TLDSVector(FlatBufferBuilder builder, StringOffset[] data) { builder.StartVector(4, data.Length, 4); for (int i = data.Length - 1; i >= 0; i--) builder.AddOffset(data[i].Value); return builder.EndVector(); }
  public static VectorOffset CreateALLOWED_TLDSVectorBlock(FlatBufferBuilder builder, StringOffset[] data) { builder.StartVector(4, data.Length, 4); builder.Add(data); return builder.EndVector(); }
  public static VectorOffset CreateALLOWED_TLDSVectorBlock(FlatBufferBuilder builder, ArraySegment<StringOffset> data) { builder.StartVector(4, data.Count, 4); builder.Add(data); return builder.EndVector(); }
  public static VectorOffset CreateALLOWED_TLDSVectorBlock(FlatBufferBuilder builder, IntPtr dataPtr, int sizeInBytes) { builder.StartVector(1, sizeInBytes, 1); builder.Add<StringOffset>(dataPtr, sizeInBytes); return builder.EndVector(); }
  public static void StartALLOWED_TLDSVector(FlatBufferBuilder builder, int numElems) { builder.StartVector(4, numElems, 4); }
  public static void AddDEV_DOMAINS(FlatBufferBuilder builder, VectorOffset DEV_DOMAINSOffset) { builder.AddOffset(2, DEV_DOMAINSOffset.Value, 0); }
  public static VectorOffset CreateDEV_DOMAINSVector(FlatBufferBuilder builder, StringOffset[] data) { builder.StartVector(4, data.Length, 4); for (int i = data.Length - 1; i >= 0; i--) builder.AddOffset(data[i].Value); return builder.EndVector(); }
  public static VectorOffset CreateDEV_DOMAINSVectorBlock(FlatBufferBuilder builder, StringOffset[] data) { builder.StartVector(4, data.Length, 4); builder.Add(data); return builder.EndVector(); }
  public static VectorOffset CreateDEV_DOMAINSVectorBlock(FlatBufferBuilder builder, ArraySegment<StringOffset> data) { builder.StartVector(4, data.Count, 4); builder.Add(data); return builder.EndVector(); }
  public static VectorOffset CreateDEV_DOMAINSVectorBlock(FlatBufferBuilder builder, IntPtr dataPtr, int sizeInBytes) { builder.StartVector(1, sizeInBytes, 1); builder.Add<StringOffset>(dataPtr, sizeInBytes); return builder.EndVector(); }
  public static void StartDEV_DOMAINSVector(FlatBufferBuilder builder, int numElems) { builder.StartVector(4, numElems, 4); }
  public static void AddTTL_DAYS(FlatBufferBuilder builder, uint TTL_DAYS) { builder.AddUint(3, TTL_DAYS, 180); }
  public static void AddCOMPILED_AT_MS(FlatBufferBuilder builder, ulong COMPILED_AT_MS) { builder.AddUlong(4, COMPILED_AT_MS, 0); }
  public static Offset<BPFRuntimeLock> EndBPFRuntimeLock(FlatBufferBuilder builder) {
    int o = builder.EndTable();
    return new Offset<BPFRuntimeLock>(o);
  }
  public BPFRuntimeLockT UnPack() {
    var _o = new BPFRuntimeLockT();
    this.UnPackTo(_o);
    return _o;
  }
  public void UnPackTo(BPFRuntimeLockT _o) {
    _o.ALLOWED_DOMAINS = new List<string>();
    for (var _j = 0; _j < this.ALLOWED_DOMAINSLength; ++_j) {_o.ALLOWED_DOMAINS.Add(this.ALLOWED_DOMAINS(_j));}
    _o.ALLOWED_TLDS = new List<string>();
    for (var _j = 0; _j < this.ALLOWED_TLDSLength; ++_j) {_o.ALLOWED_TLDS.Add(this.ALLOWED_TLDS(_j));}
    _o.DEV_DOMAINS = new List<string>();
    for (var _j = 0; _j < this.DEV_DOMAINSLength; ++_j) {_o.DEV_DOMAINS.Add(this.DEV_DOMAINS(_j));}
    _o.TTL_DAYS = this.TTL_DAYS;
    _o.COMPILED_AT_MS = this.COMPILED_AT_MS;
  }
  public static Offset<BPFRuntimeLock> Pack(FlatBufferBuilder builder, BPFRuntimeLockT _o) {
    if (_o == null) return default(Offset<BPFRuntimeLock>);
    var _ALLOWED_DOMAINS = default(VectorOffset);
    if (_o.ALLOWED_DOMAINS != null) {
      var __ALLOWED_DOMAINS = new StringOffset[_o.ALLOWED_DOMAINS.Count];
      for (var _j = 0; _j < __ALLOWED_DOMAINS.Length; ++_j) { __ALLOWED_DOMAINS[_j] = builder.CreateString(_o.ALLOWED_DOMAINS[_j]); }
      _ALLOWED_DOMAINS = CreateALLOWED_DOMAINSVector(builder, __ALLOWED_DOMAINS);
    }
    var _ALLOWED_TLDS = default(VectorOffset);
    if (_o.ALLOWED_TLDS != null) {
      var __ALLOWED_TLDS = new StringOffset[_o.ALLOWED_TLDS.Count];
      for (var _j = 0; _j < __ALLOWED_TLDS.Length; ++_j) { __ALLOWED_TLDS[_j] = builder.CreateString(_o.ALLOWED_TLDS[_j]); }
      _ALLOWED_TLDS = CreateALLOWED_TLDSVector(builder, __ALLOWED_TLDS);
    }
    var _DEV_DOMAINS = default(VectorOffset);
    if (_o.DEV_DOMAINS != null) {
      var __DEV_DOMAINS = new StringOffset[_o.DEV_DOMAINS.Count];
      for (var _j = 0; _j < __DEV_DOMAINS.Length; ++_j) { __DEV_DOMAINS[_j] = builder.CreateString(_o.DEV_DOMAINS[_j]); }
      _DEV_DOMAINS = CreateDEV_DOMAINSVector(builder, __DEV_DOMAINS);
    }
    return CreateBPFRuntimeLock(
      builder,
      _ALLOWED_DOMAINS,
      _ALLOWED_TLDS,
      _DEV_DOMAINS,
      _o.TTL_DAYS,
      _o.COMPILED_AT_MS);
  }
}

public class BPFRuntimeLockT
{
  public List<string> ALLOWED_DOMAINS { get; set; }
  public List<string> ALLOWED_TLDS { get; set; }
  public List<string> DEV_DOMAINS { get; set; }
  public uint TTL_DAYS { get; set; }
  public ulong COMPILED_AT_MS { get; set; }

  public BPFRuntimeLockT() {
    this.ALLOWED_DOMAINS = null;
    this.ALLOWED_TLDS = null;
    this.DEV_DOMAINS = null;
    this.TTL_DAYS = 180;
    this.COMPILED_AT_MS = 0;
  }
}


static public class BPFRuntimeLockVerify
{
  static public bool Verify(Google.FlatBuffers.Verifier verifier, uint tablePos)
  {
    return verifier.VerifyTableStart(tablePos)
      && verifier.VerifyVectorOfStrings(tablePos, 4 /*ALLOWED_DOMAINS*/, false)
      && verifier.VerifyVectorOfStrings(tablePos, 6 /*ALLOWED_TLDS*/, false)
      && verifier.VerifyVectorOfStrings(tablePos, 8 /*DEV_DOMAINS*/, false)
      && verifier.VerifyField(tablePos, 10 /*TTL_DAYS*/, 4 /*uint*/, 4, false)
      && verifier.VerifyField(tablePos, 12 /*COMPILED_AT_MS*/, 8 /*ulong*/, 8, false)
      && verifier.VerifyTableEnd(tablePos);
  }
}
