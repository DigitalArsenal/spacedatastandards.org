# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Licensing Configuration Frame
class LCF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LCF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLCF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def LCFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4C\x43\x46", size_prefixed=size_prefixed)

    # LCF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Distinguishes configuration inputs from runtime status outputs.
    # LCF
    def MESSAGE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Indicates whether the configuration/status applies to server/provider or requester/client behavior.
    # LCF
    def ROLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Peer ID advertised by the licensing provider.
    # LCF
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Host-managed long-lived signing key used for grant signatures.
    # LCF
    def PROVIDER_SIGNING_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from KRF import KRF
            obj = KRF()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Host-managed wrapping key used to protect per-publication content keys for requesters.
    # LCF
    def PROVIDER_WRAPPING_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from KRF import KRF
            obj = KRF()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Optional requester signing key reference for requester-hosted flows.
    # LCF
    def REQUESTER_SIGNING_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from KRF import KRF
            obj = KRF()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Logical active publication/grant key version.
    # LCF
    def ACTIVE_KEY_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Runtime expiry in unix milliseconds, or 0 if unbounded.
    # LCF
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Maximum accepted requester/provider clock skew in milliseconds.
    # LCF
    def MAX_CLOCK_SKEW_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Challenge time-to-live in milliseconds.
    # LCF
    def CHALLENGE_TTL_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Optional capability token bytes returned with successful grants.
    # LCF
    def CAPABILITY_TOKEN(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LCF
    def CAPABILITY_TOKENAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LCF
    def CAPABILITY_TOKENLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LCF
    def CAPABILITY_TOKENIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # Whether the runtime has been initialized successfully.
    # LCF
    def INITIALIZED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Whether the runtime needs issuer key rotation or refreshed configuration.
    # LCF
    def NEEDS_ROTATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Optional machine-readable status code for status/error responses.
    # LCF
    def STATUS_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Optional human-readable status or error message.
    # LCF
    def STATUS_MESSAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def LCFStart(builder):
    builder.StartObject(15)

def Start(builder):
    LCFStart(builder)

def LCFAddMESSAGE_TYPE(builder, MESSAGE_TYPE):
    builder.PrependInt8Slot(0, MESSAGE_TYPE, 0)

def AddMESSAGE_TYPE(builder, MESSAGE_TYPE):
    LCFAddMESSAGE_TYPE(builder, MESSAGE_TYPE)

def LCFAddROLE(builder, ROLE):
    builder.PrependInt8Slot(1, ROLE, 0)

def AddROLE(builder, ROLE):
    LCFAddROLE(builder, ROLE)

def LCFAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    LCFAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def LCFAddPROVIDER_SIGNING_KEY(builder, PROVIDER_SIGNING_KEY):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_SIGNING_KEY), 0)

def AddPROVIDER_SIGNING_KEY(builder, PROVIDER_SIGNING_KEY):
    LCFAddPROVIDER_SIGNING_KEY(builder, PROVIDER_SIGNING_KEY)

def LCFAddPROVIDER_WRAPPING_KEY(builder, PROVIDER_WRAPPING_KEY):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_WRAPPING_KEY), 0)

def AddPROVIDER_WRAPPING_KEY(builder, PROVIDER_WRAPPING_KEY):
    LCFAddPROVIDER_WRAPPING_KEY(builder, PROVIDER_WRAPPING_KEY)

def LCFAddREQUESTER_SIGNING_KEY(builder, REQUESTER_SIGNING_KEY):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTER_SIGNING_KEY), 0)

def AddREQUESTER_SIGNING_KEY(builder, REQUESTER_SIGNING_KEY):
    LCFAddREQUESTER_SIGNING_KEY(builder, REQUESTER_SIGNING_KEY)

def LCFAddACTIVE_KEY_VERSION(builder, ACTIVE_KEY_VERSION):
    builder.PrependUint32Slot(6, ACTIVE_KEY_VERSION, 0)

def AddACTIVE_KEY_VERSION(builder, ACTIVE_KEY_VERSION):
    LCFAddACTIVE_KEY_VERSION(builder, ACTIVE_KEY_VERSION)

def LCFAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(7, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    LCFAddEXPIRES_AT(builder, EXPIRES_AT)

def LCFAddMAX_CLOCK_SKEW_MS(builder, MAX_CLOCK_SKEW_MS):
    builder.PrependUint64Slot(8, MAX_CLOCK_SKEW_MS, 0)

def AddMAX_CLOCK_SKEW_MS(builder, MAX_CLOCK_SKEW_MS):
    LCFAddMAX_CLOCK_SKEW_MS(builder, MAX_CLOCK_SKEW_MS)

def LCFAddCHALLENGE_TTL_MS(builder, CHALLENGE_TTL_MS):
    builder.PrependUint64Slot(9, CHALLENGE_TTL_MS, 0)

def AddCHALLENGE_TTL_MS(builder, CHALLENGE_TTL_MS):
    LCFAddCHALLENGE_TTL_MS(builder, CHALLENGE_TTL_MS)

def LCFAddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(CAPABILITY_TOKEN), 0)

def AddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN):
    LCFAddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN)

def LCFStartCAPABILITY_TOKENVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCAPABILITY_TOKENVector(builder, numElems):
    return LCFStartCAPABILITY_TOKENVector(builder, numElems)

def LCFCreateCAPABILITY_TOKENVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCAPABILITY_TOKENVector(builder, data):
    LCFCreateCAPABILITY_TOKENVector(builder, data)

def LCFAddINITIALIZED(builder, INITIALIZED):
    builder.PrependBoolSlot(11, INITIALIZED, 0)

def AddINITIALIZED(builder, INITIALIZED):
    LCFAddINITIALIZED(builder, INITIALIZED)

def LCFAddNEEDS_ROTATION(builder, NEEDS_ROTATION):
    builder.PrependBoolSlot(12, NEEDS_ROTATION, 0)

def AddNEEDS_ROTATION(builder, NEEDS_ROTATION):
    LCFAddNEEDS_ROTATION(builder, NEEDS_ROTATION)

def LCFAddSTATUS_CODE(builder, STATUS_CODE):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(STATUS_CODE), 0)

def AddSTATUS_CODE(builder, STATUS_CODE):
    LCFAddSTATUS_CODE(builder, STATUS_CODE)

def LCFAddSTATUS_MESSAGE(builder, STATUS_MESSAGE):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(STATUS_MESSAGE), 0)

def AddSTATUS_MESSAGE(builder, STATUS_MESSAGE):
    LCFAddSTATUS_MESSAGE(builder, STATUS_MESSAGE)

def LCFEnd(builder):
    return builder.EndObject()

def End(builder):
    return LCFEnd(builder)

import KRF
try:
    from typing import List, Optional
except:
    pass

class LCFT(object):

    # LCFT
    def __init__(
        self,
        MESSAGE_TYPE = 0,
        ROLE = 0,
        PROVIDER_PEER_ID = None,
        PROVIDER_SIGNING_KEY = None,
        PROVIDER_WRAPPING_KEY = None,
        REQUESTER_SIGNING_KEY = None,
        ACTIVE_KEY_VERSION = 0,
        EXPIRES_AT = 0,
        MAX_CLOCK_SKEW_MS = 0,
        CHALLENGE_TTL_MS = 0,
        CAPABILITY_TOKEN = None,
        INITIALIZED = False,
        NEEDS_ROTATION = False,
        STATUS_CODE = None,
        STATUS_MESSAGE = None,
    ):
        self.MESSAGE_TYPE = MESSAGE_TYPE  # type: int
        self.ROLE = ROLE  # type: int
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.PROVIDER_SIGNING_KEY = PROVIDER_SIGNING_KEY  # type: Optional[KRF.KRFT]
        self.PROVIDER_WRAPPING_KEY = PROVIDER_WRAPPING_KEY  # type: Optional[KRF.KRFT]
        self.REQUESTER_SIGNING_KEY = REQUESTER_SIGNING_KEY  # type: Optional[KRF.KRFT]
        self.ACTIVE_KEY_VERSION = ACTIVE_KEY_VERSION  # type: int
        self.EXPIRES_AT = EXPIRES_AT  # type: int
        self.MAX_CLOCK_SKEW_MS = MAX_CLOCK_SKEW_MS  # type: int
        self.CHALLENGE_TTL_MS = CHALLENGE_TTL_MS  # type: int
        self.CAPABILITY_TOKEN = CAPABILITY_TOKEN  # type: Optional[List[int]]
        self.INITIALIZED = INITIALIZED  # type: bool
        self.NEEDS_ROTATION = NEEDS_ROTATION  # type: bool
        self.STATUS_CODE = STATUS_CODE  # type: Optional[str]
        self.STATUS_MESSAGE = STATUS_MESSAGE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpLcf = LCF()
        tmpLcf.Init(buf, pos)
        return cls.InitFromObj(tmpLcf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpLcf):
        x = LCFT()
        x._UnPack(tmpLcf)
        return x

    # LCFT
    def _UnPack(self, LCF):
        if LCF is None:
            return
        self.MESSAGE_TYPE = LCF.MESSAGE_TYPE()
        self.ROLE = LCF.ROLE()
        self.PROVIDER_PEER_ID = LCF.PROVIDER_PEER_ID()
        if LCF.PROVIDER_SIGNING_KEY() is not None:
            self.PROVIDER_SIGNING_KEY = KRF.KRFT.InitFromObj(LCF.PROVIDER_SIGNING_KEY())
        if LCF.PROVIDER_WRAPPING_KEY() is not None:
            self.PROVIDER_WRAPPING_KEY = KRF.KRFT.InitFromObj(LCF.PROVIDER_WRAPPING_KEY())
        if LCF.REQUESTER_SIGNING_KEY() is not None:
            self.REQUESTER_SIGNING_KEY = KRF.KRFT.InitFromObj(LCF.REQUESTER_SIGNING_KEY())
        self.ACTIVE_KEY_VERSION = LCF.ACTIVE_KEY_VERSION()
        self.EXPIRES_AT = LCF.EXPIRES_AT()
        self.MAX_CLOCK_SKEW_MS = LCF.MAX_CLOCK_SKEW_MS()
        self.CHALLENGE_TTL_MS = LCF.CHALLENGE_TTL_MS()
        if not LCF.CAPABILITY_TOKENIsNone():
            if np is None:
                self.CAPABILITY_TOKEN = []
                for i in range(LCF.CAPABILITY_TOKENLength()):
                    self.CAPABILITY_TOKEN.append(LCF.CAPABILITY_TOKEN(i))
            else:
                self.CAPABILITY_TOKEN = LCF.CAPABILITY_TOKENAsNumpy()
        self.INITIALIZED = LCF.INITIALIZED()
        self.NEEDS_ROTATION = LCF.NEEDS_ROTATION()
        self.STATUS_CODE = LCF.STATUS_CODE()
        self.STATUS_MESSAGE = LCF.STATUS_MESSAGE()

    # LCFT
    def Pack(self, builder):
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.PROVIDER_SIGNING_KEY is not None:
            PROVIDER_SIGNING_KEY = self.PROVIDER_SIGNING_KEY.Pack(builder)
        if self.PROVIDER_WRAPPING_KEY is not None:
            PROVIDER_WRAPPING_KEY = self.PROVIDER_WRAPPING_KEY.Pack(builder)
        if self.REQUESTER_SIGNING_KEY is not None:
            REQUESTER_SIGNING_KEY = self.REQUESTER_SIGNING_KEY.Pack(builder)
        if self.CAPABILITY_TOKEN is not None:
            if np is not None and type(self.CAPABILITY_TOKEN) is np.ndarray:
                CAPABILITY_TOKEN = builder.CreateNumpyVector(self.CAPABILITY_TOKEN)
            else:
                LCFStartCAPABILITY_TOKENVector(builder, len(self.CAPABILITY_TOKEN))
                for i in reversed(range(len(self.CAPABILITY_TOKEN))):
                    builder.PrependUint8(self.CAPABILITY_TOKEN[i])
                CAPABILITY_TOKEN = builder.EndVector()
        if self.STATUS_CODE is not None:
            STATUS_CODE = builder.CreateString(self.STATUS_CODE)
        if self.STATUS_MESSAGE is not None:
            STATUS_MESSAGE = builder.CreateString(self.STATUS_MESSAGE)
        LCFStart(builder)
        LCFAddMESSAGE_TYPE(builder, self.MESSAGE_TYPE)
        LCFAddROLE(builder, self.ROLE)
        if self.PROVIDER_PEER_ID is not None:
            LCFAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.PROVIDER_SIGNING_KEY is not None:
            LCFAddPROVIDER_SIGNING_KEY(builder, PROVIDER_SIGNING_KEY)
        if self.PROVIDER_WRAPPING_KEY is not None:
            LCFAddPROVIDER_WRAPPING_KEY(builder, PROVIDER_WRAPPING_KEY)
        if self.REQUESTER_SIGNING_KEY is not None:
            LCFAddREQUESTER_SIGNING_KEY(builder, REQUESTER_SIGNING_KEY)
        LCFAddACTIVE_KEY_VERSION(builder, self.ACTIVE_KEY_VERSION)
        LCFAddEXPIRES_AT(builder, self.EXPIRES_AT)
        LCFAddMAX_CLOCK_SKEW_MS(builder, self.MAX_CLOCK_SKEW_MS)
        LCFAddCHALLENGE_TTL_MS(builder, self.CHALLENGE_TTL_MS)
        if self.CAPABILITY_TOKEN is not None:
            LCFAddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN)
        LCFAddINITIALIZED(builder, self.INITIALIZED)
        LCFAddNEEDS_ROTATION(builder, self.NEEDS_ROTATION)
        if self.STATUS_CODE is not None:
            LCFAddSTATUS_CODE(builder, STATUS_CODE)
        if self.STATUS_MESSAGE is not None:
            LCFAddSTATUS_MESSAGE(builder, STATUS_MESSAGE)
        LCF = LCFEnd(builder)
        return LCF
