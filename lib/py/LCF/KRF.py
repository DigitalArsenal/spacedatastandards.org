# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Key Reference Frame
class KRF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KRF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKRF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KRFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x52\x46", size_prefixed=size_prefixed)

    # KRF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Logical key identifier used across publication and grant records.
    # KRF
    def KEY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Host-managed key slot or key handle identifier.
    # KRF
    def SLOT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Role the referenced key fulfills for the module flow.
    # KRF
    def ROLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Algorithm or key family for the referenced key.
    # KRF
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Optional public bytes for verification or peer identity binding.
    # KRF
    def PUBLIC_KEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # KRF
    def PUBLIC_KEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # KRF
    def PUBLIC_KEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KRF
    def PUBLIC_KEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Logical version of the referenced key.
    # KRF
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Expiration time in unix milliseconds, or 0 if unbounded.
    # KRF
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # True when the secret material remains in the host key slot.
    # KRF
    def HOST_MANAGED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return True

def KRFStart(builder):
    builder.StartObject(8)

def Start(builder):
    KRFStart(builder)

def KRFAddKEY_ID(builder, KEY_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_ID), 0)

def AddKEY_ID(builder, KEY_ID):
    KRFAddKEY_ID(builder, KEY_ID)

def KRFAddSLOT_ID(builder, SLOT_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SLOT_ID), 0)

def AddSLOT_ID(builder, SLOT_ID):
    KRFAddSLOT_ID(builder, SLOT_ID)

def KRFAddROLE(builder, ROLE):
    builder.PrependInt8Slot(2, ROLE, 0)

def AddROLE(builder, ROLE):
    KRFAddROLE(builder, ROLE)

def KRFAddALGORITHM(builder, ALGORITHM):
    builder.PrependInt8Slot(3, ALGORITHM, 0)

def AddALGORITHM(builder, ALGORITHM):
    KRFAddALGORITHM(builder, ALGORITHM)

def KRFAddPUBLIC_KEY(builder, PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PUBLIC_KEY), 0)

def AddPUBLIC_KEY(builder, PUBLIC_KEY):
    KRFAddPUBLIC_KEY(builder, PUBLIC_KEY)

def KRFStartPUBLIC_KEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPUBLIC_KEYVector(builder, numElems):
    return KRFStartPUBLIC_KEYVector(builder, numElems)

def KRFCreatePUBLIC_KEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePUBLIC_KEYVector(builder, data):
    KRFCreatePUBLIC_KEYVector(builder, data)

def KRFAddVERSION(builder, VERSION):
    builder.PrependUint32Slot(5, VERSION, 0)

def AddVERSION(builder, VERSION):
    KRFAddVERSION(builder, VERSION)

def KRFAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(6, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    KRFAddEXPIRES_AT(builder, EXPIRES_AT)

def KRFAddHOST_MANAGED(builder, HOST_MANAGED):
    builder.PrependBoolSlot(7, HOST_MANAGED, 1)

def AddHOST_MANAGED(builder, HOST_MANAGED):
    KRFAddHOST_MANAGED(builder, HOST_MANAGED)

def KRFEnd(builder):
    return builder.EndObject()

def End(builder):
    return KRFEnd(builder)

try:
    from typing import List
except:
    pass

class KRFT(object):

    # KRFT
    def __init__(
        self,
        KEY_ID = None,
        SLOT_ID = None,
        ROLE = 0,
        ALGORITHM = 0,
        PUBLIC_KEY = None,
        VERSION = 0,
        EXPIRES_AT = 0,
        HOST_MANAGED = True,
    ):
        self.KEY_ID = KEY_ID  # type: Optional[str]
        self.SLOT_ID = SLOT_ID  # type: Optional[str]
        self.ROLE = ROLE  # type: int
        self.ALGORITHM = ALGORITHM  # type: int
        self.PUBLIC_KEY = PUBLIC_KEY  # type: Optional[List[int]]
        self.VERSION = VERSION  # type: int
        self.EXPIRES_AT = EXPIRES_AT  # type: int
        self.HOST_MANAGED = HOST_MANAGED  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpKrf = KRF()
        tmpKrf.Init(buf, pos)
        return cls.InitFromObj(tmpKrf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpKrf):
        x = KRFT()
        x._UnPack(tmpKrf)
        return x

    # KRFT
    def _UnPack(self, KRF):
        if KRF is None:
            return
        self.KEY_ID = KRF.KEY_ID()
        self.SLOT_ID = KRF.SLOT_ID()
        self.ROLE = KRF.ROLE()
        self.ALGORITHM = KRF.ALGORITHM()
        if not KRF.PUBLIC_KEYIsNone():
            if np is None:
                self.PUBLIC_KEY = []
                for i in range(KRF.PUBLIC_KEYLength()):
                    self.PUBLIC_KEY.append(KRF.PUBLIC_KEY(i))
            else:
                self.PUBLIC_KEY = KRF.PUBLIC_KEYAsNumpy()
        self.VERSION = KRF.VERSION()
        self.EXPIRES_AT = KRF.EXPIRES_AT()
        self.HOST_MANAGED = KRF.HOST_MANAGED()

    # KRFT
    def Pack(self, builder):
        if self.KEY_ID is not None:
            KEY_ID = builder.CreateString(self.KEY_ID)
        if self.SLOT_ID is not None:
            SLOT_ID = builder.CreateString(self.SLOT_ID)
        if self.PUBLIC_KEY is not None:
            if np is not None and type(self.PUBLIC_KEY) is np.ndarray:
                PUBLIC_KEY = builder.CreateNumpyVector(self.PUBLIC_KEY)
            else:
                KRFStartPUBLIC_KEYVector(builder, len(self.PUBLIC_KEY))
                for i in reversed(range(len(self.PUBLIC_KEY))):
                    builder.PrependUint8(self.PUBLIC_KEY[i])
                PUBLIC_KEY = builder.EndVector()
        KRFStart(builder)
        if self.KEY_ID is not None:
            KRFAddKEY_ID(builder, KEY_ID)
        if self.SLOT_ID is not None:
            KRFAddSLOT_ID(builder, SLOT_ID)
        KRFAddROLE(builder, self.ROLE)
        KRFAddALGORITHM(builder, self.ALGORITHM)
        if self.PUBLIC_KEY is not None:
            KRFAddPUBLIC_KEY(builder, PUBLIC_KEY)
        KRFAddVERSION(builder, self.VERSION)
        KRFAddEXPIRES_AT(builder, self.EXPIRES_AT)
        KRFAddHOST_MANAGED(builder, self.HOST_MANAGED)
        KRF = KRFEnd(builder)
        return KRF
