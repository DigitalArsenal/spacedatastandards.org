# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Request current state from a scenario controls host.
class SCCRequestState(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCCRequestState()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCCRequestState(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCCRequestStateBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x43", size_prefixed=size_prefixed)

    # SCCRequestState
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Request correlation id echoed by the matching state response.
    # SCCRequestState
    def REQUEST_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # State payload requested by the caller.
    # SCCRequestState
    def REQUEST_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def SCCRequestStateStart(builder):
    builder.StartObject(2)

def Start(builder):
    SCCRequestStateStart(builder)

def SCCRequestStateAddREQUEST_ID(builder, REQUEST_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_ID), 0)

def AddREQUEST_ID(builder, REQUEST_ID):
    SCCRequestStateAddREQUEST_ID(builder, REQUEST_ID)

def SCCRequestStateAddREQUEST_KIND(builder, REQUEST_KIND):
    builder.PrependInt8Slot(1, REQUEST_KIND, 0)

def AddREQUEST_KIND(builder, REQUEST_KIND):
    SCCRequestStateAddREQUEST_KIND(builder, REQUEST_KIND)

def SCCRequestStateEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCCRequestStateEnd(builder)


class SCCRequestStateT(object):

    # SCCRequestStateT
    def __init__(
        self,
        REQUEST_ID = None,
        REQUEST_KIND = 0,
    ):
        self.REQUEST_ID = REQUEST_ID  # type: Optional[str]
        self.REQUEST_KIND = REQUEST_KIND  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSccrequestState = SCCRequestState()
        tmpSccrequestState.Init(buf, pos)
        return cls.InitFromObj(tmpSccrequestState)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSccrequestState):
        x = SCCRequestStateT()
        x._UnPack(tmpSccrequestState)
        return x

    # SCCRequestStateT
    def _UnPack(self, SCCRequestState):
        if SCCRequestState is None:
            return
        self.REQUEST_ID = SCCRequestState.REQUEST_ID()
        self.REQUEST_KIND = SCCRequestState.REQUEST_KIND()

    # SCCRequestStateT
    def Pack(self, builder):
        if self.REQUEST_ID is not None:
            REQUEST_ID = builder.CreateString(self.REQUEST_ID)
        SCCRequestStateStart(builder)
        if self.REQUEST_ID is not None:
            SCCRequestStateAddREQUEST_ID(builder, REQUEST_ID)
        SCCRequestStateAddREQUEST_KIND(builder, self.REQUEST_KIND)
        SCCRequestState = SCCRequestStateEnd(builder)
        return SCCRequestState
