# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Asset-picker request or acknowledgement.
class SCCAssetPicker(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCCAssetPicker()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCCAssetPicker(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCCAssetPickerBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x43", size_prefixed=size_prefixed)

    # SCCAssetPicker
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCCAssetPicker
    def REQUEST_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SCCAssetPicker
    def CATALOG_TAB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # SCCAssetPicker
    def ACKNOWLEDGED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def SCCAssetPickerStart(builder):
    builder.StartObject(3)

def Start(builder):
    SCCAssetPickerStart(builder)

def SCCAssetPickerAddREQUEST_ID(builder, REQUEST_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_ID), 0)

def AddREQUEST_ID(builder, REQUEST_ID):
    SCCAssetPickerAddREQUEST_ID(builder, REQUEST_ID)

def SCCAssetPickerAddCATALOG_TAB(builder, CATALOG_TAB):
    builder.PrependInt8Slot(1, CATALOG_TAB, 0)

def AddCATALOG_TAB(builder, CATALOG_TAB):
    SCCAssetPickerAddCATALOG_TAB(builder, CATALOG_TAB)

def SCCAssetPickerAddACKNOWLEDGED(builder, ACKNOWLEDGED):
    builder.PrependBoolSlot(2, ACKNOWLEDGED, 0)

def AddACKNOWLEDGED(builder, ACKNOWLEDGED):
    SCCAssetPickerAddACKNOWLEDGED(builder, ACKNOWLEDGED)

def SCCAssetPickerEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCCAssetPickerEnd(builder)


class SCCAssetPickerT(object):

    # SCCAssetPickerT
    def __init__(
        self,
        REQUEST_ID = None,
        CATALOG_TAB = 0,
        ACKNOWLEDGED = False,
    ):
        self.REQUEST_ID = REQUEST_ID  # type: Optional[str]
        self.CATALOG_TAB = CATALOG_TAB  # type: int
        self.ACKNOWLEDGED = ACKNOWLEDGED  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSccassetPicker = SCCAssetPicker()
        tmpSccassetPicker.Init(buf, pos)
        return cls.InitFromObj(tmpSccassetPicker)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSccassetPicker):
        x = SCCAssetPickerT()
        x._UnPack(tmpSccassetPicker)
        return x

    # SCCAssetPickerT
    def _UnPack(self, SCCAssetPicker):
        if SCCAssetPicker is None:
            return
        self.REQUEST_ID = SCCAssetPicker.REQUEST_ID()
        self.CATALOG_TAB = SCCAssetPicker.CATALOG_TAB()
        self.ACKNOWLEDGED = SCCAssetPicker.ACKNOWLEDGED()

    # SCCAssetPickerT
    def Pack(self, builder):
        if self.REQUEST_ID is not None:
            REQUEST_ID = builder.CreateString(self.REQUEST_ID)
        SCCAssetPickerStart(builder)
        if self.REQUEST_ID is not None:
            SCCAssetPickerAddREQUEST_ID(builder, REQUEST_ID)
        SCCAssetPickerAddCATALOG_TAB(builder, self.CATALOG_TAB)
        SCCAssetPickerAddACKNOWLEDGED(builder, self.ACKNOWLEDGED)
        SCCAssetPicker = SCCAssetPickerEnd(builder)
        return SCCAssetPicker
