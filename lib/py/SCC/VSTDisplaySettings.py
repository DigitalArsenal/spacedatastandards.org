# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# App display toggles for a scenario viewer.
class VSTDisplaySettings(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VSTDisplaySettings()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVSTDisplaySettings(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VSTDisplaySettingsBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x53\x54", size_prefixed=size_prefixed)

    # VSTDisplaySettings
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Active map rendering mode for the scenario viewer.
    # VSTDisplaySettings
    def MAP_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Show geosynchronous belt reference markers.
    # VSTDisplaySettings
    def SHOW_GEO_BELT_MARKERS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Show cislunar reference content.
    # VSTDisplaySettings
    def SHOW_CIS_LUNAR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Show the equator overlay.
    # VSTDisplaySettings
    def SHOW_EQUATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Show latitude grid lines.
    # VSTDisplaySettings
    def SHOW_LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Show longitude grid lines.
    # VSTDisplaySettings
    def SHOW_LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Show body shadow-cone overlays.
    # VSTDisplaySettings
    def SHOW_SHADOW_CONES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Show animated capture or GIF export controls.
    # VSTDisplaySettings
    def SHOW_GIF_CONTROLS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Render the atmospheric shell around the central body.
    # VSTDisplaySettings
    def SHOW_ATMOSPHERE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Show the sun-direction indicator overlay.
    # VSTDisplaySettings
    def SHOW_SUN_DIRECTION_INDICATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Render the starfield background.
    # VSTDisplaySettings
    def USE_STARFIELD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Render orbits as stitched continuous paths.
    # VSTDisplaySettings
    def USE_STITCHED_ORBIT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def VSTDisplaySettingsStart(builder):
    builder.StartObject(12)

def Start(builder):
    VSTDisplaySettingsStart(builder)

def VSTDisplaySettingsAddMAP_MODE(builder, MAP_MODE):
    builder.PrependInt8Slot(0, MAP_MODE, 0)

def AddMAP_MODE(builder, MAP_MODE):
    VSTDisplaySettingsAddMAP_MODE(builder, MAP_MODE)

def VSTDisplaySettingsAddSHOW_GEO_BELT_MARKERS(builder, SHOW_GEO_BELT_MARKERS):
    builder.PrependBoolSlot(1, SHOW_GEO_BELT_MARKERS, 0)

def AddSHOW_GEO_BELT_MARKERS(builder, SHOW_GEO_BELT_MARKERS):
    VSTDisplaySettingsAddSHOW_GEO_BELT_MARKERS(builder, SHOW_GEO_BELT_MARKERS)

def VSTDisplaySettingsAddSHOW_CIS_LUNAR(builder, SHOW_CIS_LUNAR):
    builder.PrependBoolSlot(2, SHOW_CIS_LUNAR, 0)

def AddSHOW_CIS_LUNAR(builder, SHOW_CIS_LUNAR):
    VSTDisplaySettingsAddSHOW_CIS_LUNAR(builder, SHOW_CIS_LUNAR)

def VSTDisplaySettingsAddSHOW_EQUATOR(builder, SHOW_EQUATOR):
    builder.PrependBoolSlot(3, SHOW_EQUATOR, 0)

def AddSHOW_EQUATOR(builder, SHOW_EQUATOR):
    VSTDisplaySettingsAddSHOW_EQUATOR(builder, SHOW_EQUATOR)

def VSTDisplaySettingsAddSHOW_LATITUDE(builder, SHOW_LATITUDE):
    builder.PrependBoolSlot(4, SHOW_LATITUDE, 0)

def AddSHOW_LATITUDE(builder, SHOW_LATITUDE):
    VSTDisplaySettingsAddSHOW_LATITUDE(builder, SHOW_LATITUDE)

def VSTDisplaySettingsAddSHOW_LONGITUDE(builder, SHOW_LONGITUDE):
    builder.PrependBoolSlot(5, SHOW_LONGITUDE, 0)

def AddSHOW_LONGITUDE(builder, SHOW_LONGITUDE):
    VSTDisplaySettingsAddSHOW_LONGITUDE(builder, SHOW_LONGITUDE)

def VSTDisplaySettingsAddSHOW_SHADOW_CONES(builder, SHOW_SHADOW_CONES):
    builder.PrependBoolSlot(6, SHOW_SHADOW_CONES, 0)

def AddSHOW_SHADOW_CONES(builder, SHOW_SHADOW_CONES):
    VSTDisplaySettingsAddSHOW_SHADOW_CONES(builder, SHOW_SHADOW_CONES)

def VSTDisplaySettingsAddSHOW_GIF_CONTROLS(builder, SHOW_GIF_CONTROLS):
    builder.PrependBoolSlot(7, SHOW_GIF_CONTROLS, 0)

def AddSHOW_GIF_CONTROLS(builder, SHOW_GIF_CONTROLS):
    VSTDisplaySettingsAddSHOW_GIF_CONTROLS(builder, SHOW_GIF_CONTROLS)

def VSTDisplaySettingsAddSHOW_ATMOSPHERE(builder, SHOW_ATMOSPHERE):
    builder.PrependBoolSlot(8, SHOW_ATMOSPHERE, 0)

def AddSHOW_ATMOSPHERE(builder, SHOW_ATMOSPHERE):
    VSTDisplaySettingsAddSHOW_ATMOSPHERE(builder, SHOW_ATMOSPHERE)

def VSTDisplaySettingsAddSHOW_SUN_DIRECTION_INDICATOR(builder, SHOW_SUN_DIRECTION_INDICATOR):
    builder.PrependBoolSlot(9, SHOW_SUN_DIRECTION_INDICATOR, 0)

def AddSHOW_SUN_DIRECTION_INDICATOR(builder, SHOW_SUN_DIRECTION_INDICATOR):
    VSTDisplaySettingsAddSHOW_SUN_DIRECTION_INDICATOR(builder, SHOW_SUN_DIRECTION_INDICATOR)

def VSTDisplaySettingsAddUSE_STARFIELD(builder, USE_STARFIELD):
    builder.PrependBoolSlot(10, USE_STARFIELD, 0)

def AddUSE_STARFIELD(builder, USE_STARFIELD):
    VSTDisplaySettingsAddUSE_STARFIELD(builder, USE_STARFIELD)

def VSTDisplaySettingsAddUSE_STITCHED_ORBIT(builder, USE_STITCHED_ORBIT):
    builder.PrependBoolSlot(11, USE_STITCHED_ORBIT, 0)

def AddUSE_STITCHED_ORBIT(builder, USE_STITCHED_ORBIT):
    VSTDisplaySettingsAddUSE_STITCHED_ORBIT(builder, USE_STITCHED_ORBIT)

def VSTDisplaySettingsEnd(builder):
    return builder.EndObject()

def End(builder):
    return VSTDisplaySettingsEnd(builder)


class VSTDisplaySettingsT(object):

    # VSTDisplaySettingsT
    def __init__(
        self,
        MAP_MODE = 0,
        SHOW_GEO_BELT_MARKERS = False,
        SHOW_CIS_LUNAR = False,
        SHOW_EQUATOR = False,
        SHOW_LATITUDE = False,
        SHOW_LONGITUDE = False,
        SHOW_SHADOW_CONES = False,
        SHOW_GIF_CONTROLS = False,
        SHOW_ATMOSPHERE = False,
        SHOW_SUN_DIRECTION_INDICATOR = False,
        USE_STARFIELD = False,
        USE_STITCHED_ORBIT = False,
    ):
        self.MAP_MODE = MAP_MODE  # type: int
        self.SHOW_GEO_BELT_MARKERS = SHOW_GEO_BELT_MARKERS  # type: bool
        self.SHOW_CIS_LUNAR = SHOW_CIS_LUNAR  # type: bool
        self.SHOW_EQUATOR = SHOW_EQUATOR  # type: bool
        self.SHOW_LATITUDE = SHOW_LATITUDE  # type: bool
        self.SHOW_LONGITUDE = SHOW_LONGITUDE  # type: bool
        self.SHOW_SHADOW_CONES = SHOW_SHADOW_CONES  # type: bool
        self.SHOW_GIF_CONTROLS = SHOW_GIF_CONTROLS  # type: bool
        self.SHOW_ATMOSPHERE = SHOW_ATMOSPHERE  # type: bool
        self.SHOW_SUN_DIRECTION_INDICATOR = SHOW_SUN_DIRECTION_INDICATOR  # type: bool
        self.USE_STARFIELD = USE_STARFIELD  # type: bool
        self.USE_STITCHED_ORBIT = USE_STITCHED_ORBIT  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVstdisplaySettings = VSTDisplaySettings()
        tmpVstdisplaySettings.Init(buf, pos)
        return cls.InitFromObj(tmpVstdisplaySettings)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVstdisplaySettings):
        x = VSTDisplaySettingsT()
        x._UnPack(tmpVstdisplaySettings)
        return x

    # VSTDisplaySettingsT
    def _UnPack(self, VSTDisplaySettings):
        if VSTDisplaySettings is None:
            return
        self.MAP_MODE = VSTDisplaySettings.MAP_MODE()
        self.SHOW_GEO_BELT_MARKERS = VSTDisplaySettings.SHOW_GEO_BELT_MARKERS()
        self.SHOW_CIS_LUNAR = VSTDisplaySettings.SHOW_CIS_LUNAR()
        self.SHOW_EQUATOR = VSTDisplaySettings.SHOW_EQUATOR()
        self.SHOW_LATITUDE = VSTDisplaySettings.SHOW_LATITUDE()
        self.SHOW_LONGITUDE = VSTDisplaySettings.SHOW_LONGITUDE()
        self.SHOW_SHADOW_CONES = VSTDisplaySettings.SHOW_SHADOW_CONES()
        self.SHOW_GIF_CONTROLS = VSTDisplaySettings.SHOW_GIF_CONTROLS()
        self.SHOW_ATMOSPHERE = VSTDisplaySettings.SHOW_ATMOSPHERE()
        self.SHOW_SUN_DIRECTION_INDICATOR = VSTDisplaySettings.SHOW_SUN_DIRECTION_INDICATOR()
        self.USE_STARFIELD = VSTDisplaySettings.USE_STARFIELD()
        self.USE_STITCHED_ORBIT = VSTDisplaySettings.USE_STITCHED_ORBIT()

    # VSTDisplaySettingsT
    def Pack(self, builder):
        VSTDisplaySettingsStart(builder)
        VSTDisplaySettingsAddMAP_MODE(builder, self.MAP_MODE)
        VSTDisplaySettingsAddSHOW_GEO_BELT_MARKERS(builder, self.SHOW_GEO_BELT_MARKERS)
        VSTDisplaySettingsAddSHOW_CIS_LUNAR(builder, self.SHOW_CIS_LUNAR)
        VSTDisplaySettingsAddSHOW_EQUATOR(builder, self.SHOW_EQUATOR)
        VSTDisplaySettingsAddSHOW_LATITUDE(builder, self.SHOW_LATITUDE)
        VSTDisplaySettingsAddSHOW_LONGITUDE(builder, self.SHOW_LONGITUDE)
        VSTDisplaySettingsAddSHOW_SHADOW_CONES(builder, self.SHOW_SHADOW_CONES)
        VSTDisplaySettingsAddSHOW_GIF_CONTROLS(builder, self.SHOW_GIF_CONTROLS)
        VSTDisplaySettingsAddSHOW_ATMOSPHERE(builder, self.SHOW_ATMOSPHERE)
        VSTDisplaySettingsAddSHOW_SUN_DIRECTION_INDICATOR(builder, self.SHOW_SUN_DIRECTION_INDICATOR)
        VSTDisplaySettingsAddUSE_STARFIELD(builder, self.USE_STARFIELD)
        VSTDisplaySettingsAddUSE_STITCHED_ORBIT(builder, self.USE_STITCHED_ORBIT)
        VSTDisplaySettings = VSTDisplaySettingsEnd(builder)
        return VSTDisplaySettings
