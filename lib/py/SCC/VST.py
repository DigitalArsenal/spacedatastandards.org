# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Viewer State — display and camera state associated with a scenario.
class VST(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VST()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVST(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VSTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x53\x54", size_prefixed=size_prefixed)

    # VST
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # VST
    def CAMERA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VSTCameraOptions import VSTCameraOptions
            obj = VSTCameraOptions()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VST
    def DISPLAY_SETTINGS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VSTDisplaySettings import VSTDisplaySettings
            obj = VSTDisplaySettings()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VST
    def FOCUSED_REFERENCE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # VST
    def FOCUSED_REFERENCE_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return -1

def VSTStart(builder):
    builder.StartObject(4)

def Start(builder):
    VSTStart(builder)

def VSTAddCAMERA(builder, CAMERA):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CAMERA), 0)

def AddCAMERA(builder, CAMERA):
    VSTAddCAMERA(builder, CAMERA)

def VSTAddDISPLAY_SETTINGS(builder, DISPLAY_SETTINGS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(DISPLAY_SETTINGS), 0)

def AddDISPLAY_SETTINGS(builder, DISPLAY_SETTINGS):
    VSTAddDISPLAY_SETTINGS(builder, DISPLAY_SETTINGS)

def VSTAddFOCUSED_REFERENCE_ID(builder, FOCUSED_REFERENCE_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(FOCUSED_REFERENCE_ID), 0)

def AddFOCUSED_REFERENCE_ID(builder, FOCUSED_REFERENCE_ID):
    VSTAddFOCUSED_REFERENCE_ID(builder, FOCUSED_REFERENCE_ID)

def VSTAddFOCUSED_REFERENCE_INDEX(builder, FOCUSED_REFERENCE_INDEX):
    builder.PrependInt32Slot(3, FOCUSED_REFERENCE_INDEX, -1)

def AddFOCUSED_REFERENCE_INDEX(builder, FOCUSED_REFERENCE_INDEX):
    VSTAddFOCUSED_REFERENCE_INDEX(builder, FOCUSED_REFERENCE_INDEX)

def VSTEnd(builder):
    return builder.EndObject()

def End(builder):
    return VSTEnd(builder)

import VSTCameraOptions
import VSTDisplaySettings
try:
    from typing import Optional
except:
    pass

class VSTT(object):

    # VSTT
    def __init__(
        self,
        CAMERA = None,
        DISPLAY_SETTINGS = None,
        FOCUSED_REFERENCE_ID = None,
        FOCUSED_REFERENCE_INDEX = -1,
    ):
        self.CAMERA = CAMERA  # type: Optional[VSTCameraOptions.VSTCameraOptionsT]
        self.DISPLAY_SETTINGS = DISPLAY_SETTINGS  # type: Optional[VSTDisplaySettings.VSTDisplaySettingsT]
        self.FOCUSED_REFERENCE_ID = FOCUSED_REFERENCE_ID  # type: Optional[str]
        self.FOCUSED_REFERENCE_INDEX = FOCUSED_REFERENCE_INDEX  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVst = VST()
        tmpVst.Init(buf, pos)
        return cls.InitFromObj(tmpVst)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVst):
        x = VSTT()
        x._UnPack(tmpVst)
        return x

    # VSTT
    def _UnPack(self, VST):
        if VST is None:
            return
        if VST.CAMERA() is not None:
            self.CAMERA = VSTCameraOptions.VSTCameraOptionsT.InitFromObj(VST.CAMERA())
        if VST.DISPLAY_SETTINGS() is not None:
            self.DISPLAY_SETTINGS = VSTDisplaySettings.VSTDisplaySettingsT.InitFromObj(VST.DISPLAY_SETTINGS())
        self.FOCUSED_REFERENCE_ID = VST.FOCUSED_REFERENCE_ID()
        self.FOCUSED_REFERENCE_INDEX = VST.FOCUSED_REFERENCE_INDEX()

    # VSTT
    def Pack(self, builder):
        if self.CAMERA is not None:
            CAMERA = self.CAMERA.Pack(builder)
        if self.DISPLAY_SETTINGS is not None:
            DISPLAY_SETTINGS = self.DISPLAY_SETTINGS.Pack(builder)
        if self.FOCUSED_REFERENCE_ID is not None:
            FOCUSED_REFERENCE_ID = builder.CreateString(self.FOCUSED_REFERENCE_ID)
        VSTStart(builder)
        if self.CAMERA is not None:
            VSTAddCAMERA(builder, CAMERA)
        if self.DISPLAY_SETTINGS is not None:
            VSTAddDISPLAY_SETTINGS(builder, DISPLAY_SETTINGS)
        if self.FOCUSED_REFERENCE_ID is not None:
            VSTAddFOCUSED_REFERENCE_ID(builder, FOCUSED_REFERENCE_ID)
        VSTAddFOCUSED_REFERENCE_INDEX(builder, self.FOCUSED_REFERENCE_INDEX)
        VST = VSTEnd(builder)
        return VST
