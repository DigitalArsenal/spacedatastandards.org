# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Imported scenario event payload composed from existing SDS records.
class SCNEvent(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCNEvent()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCNEvent(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCNEventBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x4E", size_prefixed=size_prefixed)

    # SCNEvent
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCNEvent
    def EVENT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SCNEvent
    def ELSETS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from OMM import OMM
            obj = OMM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNEvent
    def ELSETSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNEvent
    def ELSETSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # SCNEvent
    def STATES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from STV import STV
            obj = STV()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNEvent
    def STATESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNEvent
    def STATESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # SCNEvent
    def EO_OBSERVATIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from EOO import EOO
            obj = EOO()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNEvent
    def EO_OBSERVATIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNEvent
    def EO_OBSERVATIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # SCNEvent
    def RADAR_OBSERVATIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from RDO import RDO
            obj = RDO()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNEvent
    def RADAR_OBSERVATIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNEvent
    def RADAR_OBSERVATIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def SCNEventStart(builder):
    builder.StartObject(5)

def Start(builder):
    SCNEventStart(builder)

def SCNEventAddEVENT_ID(builder, EVENT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EVENT_ID), 0)

def AddEVENT_ID(builder, EVENT_ID):
    SCNEventAddEVENT_ID(builder, EVENT_ID)

def SCNEventAddELSETS(builder, ELSETS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ELSETS), 0)

def AddELSETS(builder, ELSETS):
    SCNEventAddELSETS(builder, ELSETS)

def SCNEventStartELSETSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartELSETSVector(builder, numElems):
    return SCNEventStartELSETSVector(builder, numElems)

def SCNEventCreateELSETSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateELSETSVector(builder, data):
    SCNEventCreateELSETSVector(builder, data)

def SCNEventAddSTATES(builder, STATES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(STATES), 0)

def AddSTATES(builder, STATES):
    SCNEventAddSTATES(builder, STATES)

def SCNEventStartSTATESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSTATESVector(builder, numElems):
    return SCNEventStartSTATESVector(builder, numElems)

def SCNEventCreateSTATESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSTATESVector(builder, data):
    SCNEventCreateSTATESVector(builder, data)

def SCNEventAddEO_OBSERVATIONS(builder, EO_OBSERVATIONS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(EO_OBSERVATIONS), 0)

def AddEO_OBSERVATIONS(builder, EO_OBSERVATIONS):
    SCNEventAddEO_OBSERVATIONS(builder, EO_OBSERVATIONS)

def SCNEventStartEO_OBSERVATIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartEO_OBSERVATIONSVector(builder, numElems):
    return SCNEventStartEO_OBSERVATIONSVector(builder, numElems)

def SCNEventCreateEO_OBSERVATIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateEO_OBSERVATIONSVector(builder, data):
    SCNEventCreateEO_OBSERVATIONSVector(builder, data)

def SCNEventAddRADAR_OBSERVATIONS(builder, RADAR_OBSERVATIONS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(RADAR_OBSERVATIONS), 0)

def AddRADAR_OBSERVATIONS(builder, RADAR_OBSERVATIONS):
    SCNEventAddRADAR_OBSERVATIONS(builder, RADAR_OBSERVATIONS)

def SCNEventStartRADAR_OBSERVATIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRADAR_OBSERVATIONSVector(builder, numElems):
    return SCNEventStartRADAR_OBSERVATIONSVector(builder, numElems)

def SCNEventCreateRADAR_OBSERVATIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateRADAR_OBSERVATIONSVector(builder, data):
    SCNEventCreateRADAR_OBSERVATIONSVector(builder, data)

def SCNEventEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCNEventEnd(builder)

import EOO
import OMM
import RDO
import STV
try:
    from typing import List
except:
    pass

class SCNEventT(object):

    # SCNEventT
    def __init__(
        self,
        EVENT_ID = None,
        ELSETS = None,
        STATES = None,
        EO_OBSERVATIONS = None,
        RADAR_OBSERVATIONS = None,
    ):
        self.EVENT_ID = EVENT_ID  # type: Optional[str]
        self.ELSETS = ELSETS  # type: Optional[List[OMM.OMMT]]
        self.STATES = STATES  # type: Optional[List[STV.STVT]]
        self.EO_OBSERVATIONS = EO_OBSERVATIONS  # type: Optional[List[EOO.EOOT]]
        self.RADAR_OBSERVATIONS = RADAR_OBSERVATIONS  # type: Optional[List[RDO.RDOT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScnevent = SCNEvent()
        tmpScnevent.Init(buf, pos)
        return cls.InitFromObj(tmpScnevent)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScnevent):
        x = SCNEventT()
        x._UnPack(tmpScnevent)
        return x

    # SCNEventT
    def _UnPack(self, SCNEvent):
        if SCNEvent is None:
            return
        self.EVENT_ID = SCNEvent.EVENT_ID()
        if not SCNEvent.ELSETSIsNone():
            self.ELSETS = []
            for i in range(SCNEvent.ELSETSLength()):
                if SCNEvent.ELSETS(i) is None:
                    self.ELSETS.append(None)
                else:
                    oMM_ = OMM.OMMT.InitFromObj(SCNEvent.ELSETS(i))
                    self.ELSETS.append(oMM_)
        if not SCNEvent.STATESIsNone():
            self.STATES = []
            for i in range(SCNEvent.STATESLength()):
                if SCNEvent.STATES(i) is None:
                    self.STATES.append(None)
                else:
                    sTV_ = STV.STVT.InitFromObj(SCNEvent.STATES(i))
                    self.STATES.append(sTV_)
        if not SCNEvent.EO_OBSERVATIONSIsNone():
            self.EO_OBSERVATIONS = []
            for i in range(SCNEvent.EO_OBSERVATIONSLength()):
                if SCNEvent.EO_OBSERVATIONS(i) is None:
                    self.EO_OBSERVATIONS.append(None)
                else:
                    eOO_ = EOO.EOOT.InitFromObj(SCNEvent.EO_OBSERVATIONS(i))
                    self.EO_OBSERVATIONS.append(eOO_)
        if not SCNEvent.RADAR_OBSERVATIONSIsNone():
            self.RADAR_OBSERVATIONS = []
            for i in range(SCNEvent.RADAR_OBSERVATIONSLength()):
                if SCNEvent.RADAR_OBSERVATIONS(i) is None:
                    self.RADAR_OBSERVATIONS.append(None)
                else:
                    rDO_ = RDO.RDOT.InitFromObj(SCNEvent.RADAR_OBSERVATIONS(i))
                    self.RADAR_OBSERVATIONS.append(rDO_)

    # SCNEventT
    def Pack(self, builder):
        if self.EVENT_ID is not None:
            EVENT_ID = builder.CreateString(self.EVENT_ID)
        if self.ELSETS is not None:
            ELSETSlist = []
            for i in range(len(self.ELSETS)):
                ELSETSlist.append(self.ELSETS[i].Pack(builder))
            SCNEventStartELSETSVector(builder, len(self.ELSETS))
            for i in reversed(range(len(self.ELSETS))):
                builder.PrependUOffsetTRelative(ELSETSlist[i])
            ELSETS = builder.EndVector()
        if self.STATES is not None:
            STATESlist = []
            for i in range(len(self.STATES)):
                STATESlist.append(self.STATES[i].Pack(builder))
            SCNEventStartSTATESVector(builder, len(self.STATES))
            for i in reversed(range(len(self.STATES))):
                builder.PrependUOffsetTRelative(STATESlist[i])
            STATES = builder.EndVector()
        if self.EO_OBSERVATIONS is not None:
            EO_OBSERVATIONSlist = []
            for i in range(len(self.EO_OBSERVATIONS)):
                EO_OBSERVATIONSlist.append(self.EO_OBSERVATIONS[i].Pack(builder))
            SCNEventStartEO_OBSERVATIONSVector(builder, len(self.EO_OBSERVATIONS))
            for i in reversed(range(len(self.EO_OBSERVATIONS))):
                builder.PrependUOffsetTRelative(EO_OBSERVATIONSlist[i])
            EO_OBSERVATIONS = builder.EndVector()
        if self.RADAR_OBSERVATIONS is not None:
            RADAR_OBSERVATIONSlist = []
            for i in range(len(self.RADAR_OBSERVATIONS)):
                RADAR_OBSERVATIONSlist.append(self.RADAR_OBSERVATIONS[i].Pack(builder))
            SCNEventStartRADAR_OBSERVATIONSVector(builder, len(self.RADAR_OBSERVATIONS))
            for i in reversed(range(len(self.RADAR_OBSERVATIONS))):
                builder.PrependUOffsetTRelative(RADAR_OBSERVATIONSlist[i])
            RADAR_OBSERVATIONS = builder.EndVector()
        SCNEventStart(builder)
        if self.EVENT_ID is not None:
            SCNEventAddEVENT_ID(builder, EVENT_ID)
        if self.ELSETS is not None:
            SCNEventAddELSETS(builder, ELSETS)
        if self.STATES is not None:
            SCNEventAddSTATES(builder, STATES)
        if self.EO_OBSERVATIONS is not None:
            SCNEventAddEO_OBSERVATIONS(builder, EO_OBSERVATIONS)
        if self.RADAR_OBSERVATIONS is not None:
            SCNEventAddRADAR_OBSERVATIONS(builder, RADAR_OBSERVATIONS)
        SCNEvent = SCNEventEnd(builder)
        return SCNEvent
