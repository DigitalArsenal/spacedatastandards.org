# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Time-tagged point or annotation shown in a scenario.
class SCNPointOfInterest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCNPointOfInterest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCNPointOfInterest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCNPointOfInterestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x4E", size_prefixed=size_prefixed)

    # SCNPointOfInterest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Display name for the point of interest.
    # SCNPointOfInterest
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Optional detail text shown with the point of interest.
    # SCNPointOfInterest
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # UTC epoch associated with the point of interest.
    # SCNPointOfInterest
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Seconds before the epoch when highlighting begins.
    # SCNPointOfInterest
    def HIGHLIGHT_BEFORE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Seconds after the epoch when highlighting remains active.
    # SCNPointOfInterest
    def HIGHLIGHT_AFTER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Display color token for the point of interest.
    # SCNPointOfInterest
    def COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # WGS84 geodetic position for the point of interest.
    # SCNPointOfInterest
    def POSITION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from GJNPosition import GJNPosition
            obj = GJNPosition()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def SCNPointOfInterestStart(builder):
    builder.StartObject(7)

def Start(builder):
    SCNPointOfInterestStart(builder)

def SCNPointOfInterestAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    SCNPointOfInterestAddNAME(builder, NAME)

def SCNPointOfInterestAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    SCNPointOfInterestAddDESCRIPTION(builder, DESCRIPTION)

def SCNPointOfInterestAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    SCNPointOfInterestAddEPOCH(builder, EPOCH)

def SCNPointOfInterestAddHIGHLIGHT_BEFORE(builder, HIGHLIGHT_BEFORE):
    builder.PrependFloat64Slot(3, HIGHLIGHT_BEFORE, 0.0)

def AddHIGHLIGHT_BEFORE(builder, HIGHLIGHT_BEFORE):
    SCNPointOfInterestAddHIGHLIGHT_BEFORE(builder, HIGHLIGHT_BEFORE)

def SCNPointOfInterestAddHIGHLIGHT_AFTER(builder, HIGHLIGHT_AFTER):
    builder.PrependFloat64Slot(4, HIGHLIGHT_AFTER, 0.0)

def AddHIGHLIGHT_AFTER(builder, HIGHLIGHT_AFTER):
    SCNPointOfInterestAddHIGHLIGHT_AFTER(builder, HIGHLIGHT_AFTER)

def SCNPointOfInterestAddCOLOR(builder, COLOR):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(COLOR), 0)

def AddCOLOR(builder, COLOR):
    SCNPointOfInterestAddCOLOR(builder, COLOR)

def SCNPointOfInterestAddPOSITION(builder, POSITION):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(POSITION), 0)

def AddPOSITION(builder, POSITION):
    SCNPointOfInterestAddPOSITION(builder, POSITION)

def SCNPointOfInterestEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCNPointOfInterestEnd(builder)

import GJNPosition
try:
    from typing import Optional
except:
    pass

class SCNPointOfInterestT(object):

    # SCNPointOfInterestT
    def __init__(
        self,
        NAME = None,
        DESCRIPTION = None,
        EPOCH = None,
        HIGHLIGHT_BEFORE = 0.0,
        HIGHLIGHT_AFTER = 0.0,
        COLOR = None,
        POSITION = None,
    ):
        self.NAME = NAME  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.EPOCH = EPOCH  # type: Optional[str]
        self.HIGHLIGHT_BEFORE = HIGHLIGHT_BEFORE  # type: float
        self.HIGHLIGHT_AFTER = HIGHLIGHT_AFTER  # type: float
        self.COLOR = COLOR  # type: Optional[str]
        self.POSITION = POSITION  # type: Optional[GJNPosition.GJNPositionT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScnpointOfInterest = SCNPointOfInterest()
        tmpScnpointOfInterest.Init(buf, pos)
        return cls.InitFromObj(tmpScnpointOfInterest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScnpointOfInterest):
        x = SCNPointOfInterestT()
        x._UnPack(tmpScnpointOfInterest)
        return x

    # SCNPointOfInterestT
    def _UnPack(self, SCNPointOfInterest):
        if SCNPointOfInterest is None:
            return
        self.NAME = SCNPointOfInterest.NAME()
        self.DESCRIPTION = SCNPointOfInterest.DESCRIPTION()
        self.EPOCH = SCNPointOfInterest.EPOCH()
        self.HIGHLIGHT_BEFORE = SCNPointOfInterest.HIGHLIGHT_BEFORE()
        self.HIGHLIGHT_AFTER = SCNPointOfInterest.HIGHLIGHT_AFTER()
        self.COLOR = SCNPointOfInterest.COLOR()
        if SCNPointOfInterest.POSITION() is not None:
            self.POSITION = GJNPosition.GJNPositionT.InitFromObj(SCNPointOfInterest.POSITION())

    # SCNPointOfInterestT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        if self.COLOR is not None:
            COLOR = builder.CreateString(self.COLOR)
        if self.POSITION is not None:
            POSITION = self.POSITION.Pack(builder)
        SCNPointOfInterestStart(builder)
        if self.NAME is not None:
            SCNPointOfInterestAddNAME(builder, NAME)
        if self.DESCRIPTION is not None:
            SCNPointOfInterestAddDESCRIPTION(builder, DESCRIPTION)
        if self.EPOCH is not None:
            SCNPointOfInterestAddEPOCH(builder, EPOCH)
        SCNPointOfInterestAddHIGHLIGHT_BEFORE(builder, self.HIGHLIGHT_BEFORE)
        SCNPointOfInterestAddHIGHLIGHT_AFTER(builder, self.HIGHLIGHT_AFTER)
        if self.COLOR is not None:
            SCNPointOfInterestAddCOLOR(builder, COLOR)
        if self.POSITION is not None:
            SCNPointOfInterestAddPOSITION(builder, POSITION)
        SCNPointOfInterest = SCNPointOfInterestEnd(builder)
        return SCNPointOfInterest
