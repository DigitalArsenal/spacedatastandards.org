# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Command Link Transmission Unit Service (CCSDS 912.3-B-2)
class CLT(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CLT()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCLT(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CLTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4C\x54", size_prefixed=size_prefixed)

    # CLT
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PDU type
    # CLT
    def PDU_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Initiator identifier
    # CLT
    def INITIATOR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Responder port identifier
    # CLT
    def RESPONDER_PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Service type
    # CLT
    def SERVICE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Version number
    # CLT
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Invoke ID
    # CLT
    def INVOKE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # CLTU data
    # CLT
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CLT
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # CLT
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CLT
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def CLTStart(builder):
    builder.StartObject(7)

def Start(builder):
    CLTStart(builder)

def CLTAddPDU_TYPE(builder, PDU_TYPE):
    builder.PrependInt8Slot(0, PDU_TYPE, 0)

def AddPDU_TYPE(builder, PDU_TYPE):
    CLTAddPDU_TYPE(builder, PDU_TYPE)

def CLTAddINITIATOR_ID(builder, INITIATOR_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(INITIATOR_ID), 0)

def AddINITIATOR_ID(builder, INITIATOR_ID):
    CLTAddINITIATOR_ID(builder, INITIATOR_ID)

def CLTAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(RESPONDER_PORT_ID), 0)

def AddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID):
    CLTAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID)

def CLTAddSERVICE_TYPE(builder, SERVICE_TYPE):
    builder.PrependUint8Slot(3, SERVICE_TYPE, 0)

def AddSERVICE_TYPE(builder, SERVICE_TYPE):
    CLTAddSERVICE_TYPE(builder, SERVICE_TYPE)

def CLTAddVERSION(builder, VERSION):
    builder.PrependUint16Slot(4, VERSION, 0)

def AddVERSION(builder, VERSION):
    CLTAddVERSION(builder, VERSION)

def CLTAddINVOKE_ID(builder, INVOKE_ID):
    builder.PrependUint32Slot(5, INVOKE_ID, 0)

def AddINVOKE_ID(builder, INVOKE_ID):
    CLTAddINVOKE_ID(builder, INVOKE_ID)

def CLTAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    CLTAddDATA(builder, DATA)

def CLTStartDATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDATAVector(builder, numElems):
    return CLTStartDATAVector(builder, numElems)

def CLTEnd(builder):
    return builder.EndObject()

def End(builder):
    return CLTEnd(builder)

try:
    from typing import List
except:
    pass

class CLTT(object):

    # CLTT
    def __init__(self):
        self.PDU_TYPE = 0  # type: int
        self.INITIATOR_ID = None  # type: str
        self.RESPONDER_PORT_ID = None  # type: str
        self.SERVICE_TYPE = 0  # type: int
        self.VERSION = 0  # type: int
        self.INVOKE_ID = 0  # type: int
        self.DATA = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        CLT = CLT()
        CLT.Init(buf, pos)
        return cls.InitFromObj(CLT)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, CLT):
        x = CLTT()
        x._UnPack(CLT)
        return x

    # CLTT
    def _UnPack(self, CLT):
        if CLT is None:
            return
        self.PDU_TYPE = CLT.PDU_TYPE()
        self.INITIATOR_ID = CLT.INITIATOR_ID()
        self.RESPONDER_PORT_ID = CLT.RESPONDER_PORT_ID()
        self.SERVICE_TYPE = CLT.SERVICE_TYPE()
        self.VERSION = CLT.VERSION()
        self.INVOKE_ID = CLT.INVOKE_ID()
        if not CLT.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(CLT.DATALength()):
                    self.DATA.append(CLT.DATA(i))
            else:
                self.DATA = CLT.DATAAsNumpy()

    # CLTT
    def Pack(self, builder):
        if self.INITIATOR_ID is not None:
            INITIATOR_ID = builder.CreateString(self.INITIATOR_ID)
        if self.RESPONDER_PORT_ID is not None:
            RESPONDER_PORT_ID = builder.CreateString(self.RESPONDER_PORT_ID)
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                CLTStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependUint8(self.DATA[i])
                DATA = builder.EndVector()
        CLTStart(builder)
        CLTAddPDU_TYPE(builder, self.PDU_TYPE)
        if self.INITIATOR_ID is not None:
            CLTAddINITIATOR_ID(builder, INITIATOR_ID)
        if self.RESPONDER_PORT_ID is not None:
            CLTAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID)
        CLTAddSERVICE_TYPE(builder, self.SERVICE_TYPE)
        CLTAddVERSION(builder, self.VERSION)
        CLTAddINVOKE_ID(builder, self.INVOKE_ID)
        if self.DATA is not None:
            CLTAddDATA(builder, DATA)
        CLT = CLTEnd(builder)
        return CLT
