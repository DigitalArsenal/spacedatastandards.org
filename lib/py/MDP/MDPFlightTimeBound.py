# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Time-of-flight bound between two encounters. Adjacent stages give per-leg
# bounds; non-adjacent stages give cumulative bounds such as
# "Earth to Mars to Earth within 3 years".
class MDPFlightTimeBound(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MDPFlightTimeBound()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMDPFlightTimeBound(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def MDPFlightTimeBoundBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4D\x44\x50", size_prefixed=size_prefixed)

    # MDPFlightTimeBound
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stage indices the bound spans. TO_STAGE_INDEX > FROM_STAGE_INDEX.
    # MDPFlightTimeBound
    def FROM_STAGE_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # MDPFlightTimeBound
    def TO_STAGE_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Inclusive bounds on elapsed time between those two encounters, days.
    # MDPFlightTimeBound
    def MIN_DAYS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # MDPFlightTimeBound
    def MAX_DAYS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def MDPFlightTimeBoundStart(builder):
    builder.StartObject(4)

def Start(builder):
    MDPFlightTimeBoundStart(builder)

def MDPFlightTimeBoundAddFROM_STAGE_INDEX(builder, FROM_STAGE_INDEX):
    builder.PrependUint32Slot(0, FROM_STAGE_INDEX, 0)

def AddFROM_STAGE_INDEX(builder, FROM_STAGE_INDEX):
    MDPFlightTimeBoundAddFROM_STAGE_INDEX(builder, FROM_STAGE_INDEX)

def MDPFlightTimeBoundAddTO_STAGE_INDEX(builder, TO_STAGE_INDEX):
    builder.PrependUint32Slot(1, TO_STAGE_INDEX, 0)

def AddTO_STAGE_INDEX(builder, TO_STAGE_INDEX):
    MDPFlightTimeBoundAddTO_STAGE_INDEX(builder, TO_STAGE_INDEX)

def MDPFlightTimeBoundAddMIN_DAYS(builder, MIN_DAYS):
    builder.PrependFloat64Slot(2, MIN_DAYS, 0.0)

def AddMIN_DAYS(builder, MIN_DAYS):
    MDPFlightTimeBoundAddMIN_DAYS(builder, MIN_DAYS)

def MDPFlightTimeBoundAddMAX_DAYS(builder, MAX_DAYS):
    builder.PrependFloat64Slot(3, MAX_DAYS, 0.0)

def AddMAX_DAYS(builder, MAX_DAYS):
    MDPFlightTimeBoundAddMAX_DAYS(builder, MAX_DAYS)

def MDPFlightTimeBoundEnd(builder):
    return builder.EndObject()

def End(builder):
    return MDPFlightTimeBoundEnd(builder)


class MDPFlightTimeBoundT(object):

    # MDPFlightTimeBoundT
    def __init__(
        self,
        FROM_STAGE_INDEX = 0,
        TO_STAGE_INDEX = 0,
        MIN_DAYS = 0.0,
        MAX_DAYS = 0.0,
    ):
        self.FROM_STAGE_INDEX = FROM_STAGE_INDEX  # type: int
        self.TO_STAGE_INDEX = TO_STAGE_INDEX  # type: int
        self.MIN_DAYS = MIN_DAYS  # type: float
        self.MAX_DAYS = MAX_DAYS  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpMdpflightTimeBound = MDPFlightTimeBound()
        tmpMdpflightTimeBound.Init(buf, pos)
        return cls.InitFromObj(tmpMdpflightTimeBound)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpMdpflightTimeBound):
        x = MDPFlightTimeBoundT()
        x._UnPack(tmpMdpflightTimeBound)
        return x

    # MDPFlightTimeBoundT
    def _UnPack(self, MDPFlightTimeBound):
        if MDPFlightTimeBound is None:
            return
        self.FROM_STAGE_INDEX = MDPFlightTimeBound.FROM_STAGE_INDEX()
        self.TO_STAGE_INDEX = MDPFlightTimeBound.TO_STAGE_INDEX()
        self.MIN_DAYS = MDPFlightTimeBound.MIN_DAYS()
        self.MAX_DAYS = MDPFlightTimeBound.MAX_DAYS()

    # MDPFlightTimeBoundT
    def Pack(self, builder):
        MDPFlightTimeBoundStart(builder)
        MDPFlightTimeBoundAddFROM_STAGE_INDEX(builder, self.FROM_STAGE_INDEX)
        MDPFlightTimeBoundAddTO_STAGE_INDEX(builder, self.TO_STAGE_INDEX)
        MDPFlightTimeBoundAddMIN_DAYS(builder, self.MIN_DAYS)
        MDPFlightTimeBoundAddMAX_DAYS(builder, self.MAX_DAYS)
        MDPFlightTimeBound = MDPFlightTimeBoundEnd(builder)
        return MDPFlightTimeBound
