# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Commission Terms / Revenue Split. Binds a listing, store, or publisher to a
# platform commission and the on-chain split that enforces it. This record
# makes "the platform earns on every transaction" auditable and portable
# across store deployments. Commission is realized purely by the buyer's
# payment routing through the on-chain splitter contract described in
# CONTRACTS — never by a manual or off-chain settlement step.
class CMT(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CMT()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCMT(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CMTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4D\x54", size_prefixed=size_prefixed)

    # CMT
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable terms identifier, referenced by `$PUR`/`$ACL`/listings
    # CMT
    def TERMS_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # What these terms cover
    # CMT
    def SCOPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # The `$PLG.PLUGIN_ID` / store id / publisher xpub the terms apply to
    # CMT
    def TARGET_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Platform commission in basis points (e.g. 500 = 5%)
    # CMT
    def COMMISSION_BPS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Recipient splits; each in basis points, summing with the commission to 10000
    # CMT
    def SPLITS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from RevenueSplit import RevenueSplit
            obj = RevenueSplit()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CMT
    def SPLITSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CMT
    def SPLITSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # The platform's per-chain payout identity (the commission recipient)
    # CMT
    def PLATFORM_RECIPIENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SplitRecipient import SplitRecipient
            obj = SplitRecipient()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # How the split is enforced (on-chain splitter vs provider-attested)
    # CMT
    def ENFORCEMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # The established splitter contract(s)/program(s) that perform the split,
    # one per accepted chain/asset
    # CMT
    def CONTRACTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCX import SCX
            obj = SCX()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CMT
    def CONTRACTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CMT
    def CONTRACTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Unix ms when these terms take effect
    # CMT
    def EFFECTIVE_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix ms when these terms expire, or 0 for no expiry
    # CMT
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Ed25519 signature by the store operator `$EPM`
    # CMT
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CMT
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # CMT
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CMT
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

def CMTStart(builder):
    builder.StartObject(11)

def Start(builder):
    CMTStart(builder)

def CMTAddTERMS_ID(builder, TERMS_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(TERMS_ID), 0)

def AddTERMS_ID(builder, TERMS_ID):
    CMTAddTERMS_ID(builder, TERMS_ID)

def CMTAddSCOPE(builder, SCOPE):
    builder.PrependInt8Slot(1, SCOPE, 0)

def AddSCOPE(builder, SCOPE):
    CMTAddSCOPE(builder, SCOPE)

def CMTAddTARGET_ID(builder, TARGET_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TARGET_ID), 0)

def AddTARGET_ID(builder, TARGET_ID):
    CMTAddTARGET_ID(builder, TARGET_ID)

def CMTAddCOMMISSION_BPS(builder, COMMISSION_BPS):
    builder.PrependUint16Slot(3, COMMISSION_BPS, 0)

def AddCOMMISSION_BPS(builder, COMMISSION_BPS):
    CMTAddCOMMISSION_BPS(builder, COMMISSION_BPS)

def CMTAddSPLITS(builder, SPLITS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SPLITS), 0)

def AddSPLITS(builder, SPLITS):
    CMTAddSPLITS(builder, SPLITS)

def CMTStartSPLITSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSPLITSVector(builder, numElems):
    return CMTStartSPLITSVector(builder, numElems)

def CMTCreateSPLITSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSPLITSVector(builder, data):
    CMTCreateSPLITSVector(builder, data)

def CMTAddPLATFORM_RECIPIENT(builder, PLATFORM_RECIPIENT):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PLATFORM_RECIPIENT), 0)

def AddPLATFORM_RECIPIENT(builder, PLATFORM_RECIPIENT):
    CMTAddPLATFORM_RECIPIENT(builder, PLATFORM_RECIPIENT)

def CMTAddENFORCEMENT(builder, ENFORCEMENT):
    builder.PrependInt8Slot(6, ENFORCEMENT, 0)

def AddENFORCEMENT(builder, ENFORCEMENT):
    CMTAddENFORCEMENT(builder, ENFORCEMENT)

def CMTAddCONTRACTS(builder, CONTRACTS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(CONTRACTS), 0)

def AddCONTRACTS(builder, CONTRACTS):
    CMTAddCONTRACTS(builder, CONTRACTS)

def CMTStartCONTRACTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCONTRACTSVector(builder, numElems):
    return CMTStartCONTRACTSVector(builder, numElems)

def CMTCreateCONTRACTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCONTRACTSVector(builder, data):
    CMTCreateCONTRACTSVector(builder, data)

def CMTAddEFFECTIVE_AT(builder, EFFECTIVE_AT):
    builder.PrependUint64Slot(8, EFFECTIVE_AT, 0)

def AddEFFECTIVE_AT(builder, EFFECTIVE_AT):
    CMTAddEFFECTIVE_AT(builder, EFFECTIVE_AT)

def CMTAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(9, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    CMTAddEXPIRES_AT(builder, EXPIRES_AT)

def CMTAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    CMTAddSIGNATURE(builder, SIGNATURE)

def CMTStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return CMTStartSIGNATUREVector(builder, numElems)

def CMTCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    CMTCreateSIGNATUREVector(builder, data)

def CMTEnd(builder):
    return builder.EndObject()

def End(builder):
    return CMTEnd(builder)

import RevenueSplit
import SCX
import SplitRecipient
try:
    from typing import List, Optional
except:
    pass

class CMTT(object):

    # CMTT
    def __init__(
        self,
        TERMS_ID = None,
        SCOPE = 0,
        TARGET_ID = None,
        COMMISSION_BPS = 0,
        SPLITS = None,
        PLATFORM_RECIPIENT = None,
        ENFORCEMENT = 0,
        CONTRACTS = None,
        EFFECTIVE_AT = 0,
        EXPIRES_AT = 0,
        SIGNATURE = None,
    ):
        self.TERMS_ID = TERMS_ID  # type: Optional[str]
        self.SCOPE = SCOPE  # type: int
        self.TARGET_ID = TARGET_ID  # type: Optional[str]
        self.COMMISSION_BPS = COMMISSION_BPS  # type: int
        self.SPLITS = SPLITS  # type: Optional[List[RevenueSplit.RevenueSplitT]]
        self.PLATFORM_RECIPIENT = PLATFORM_RECIPIENT  # type: Optional[SplitRecipient.SplitRecipientT]
        self.ENFORCEMENT = ENFORCEMENT  # type: int
        self.CONTRACTS = CONTRACTS  # type: Optional[List[SCX.SCXT]]
        self.EFFECTIVE_AT = EFFECTIVE_AT  # type: int
        self.EXPIRES_AT = EXPIRES_AT  # type: int
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCmt = CMT()
        tmpCmt.Init(buf, pos)
        return cls.InitFromObj(tmpCmt)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCmt):
        x = CMTT()
        x._UnPack(tmpCmt)
        return x

    # CMTT
    def _UnPack(self, CMT):
        if CMT is None:
            return
        self.TERMS_ID = CMT.TERMS_ID()
        self.SCOPE = CMT.SCOPE()
        self.TARGET_ID = CMT.TARGET_ID()
        self.COMMISSION_BPS = CMT.COMMISSION_BPS()
        if not CMT.SPLITSIsNone():
            self.SPLITS = []
            for i in range(CMT.SPLITSLength()):
                if CMT.SPLITS(i) is None:
                    self.SPLITS.append(None)
                else:
                    revenueSplit_ = RevenueSplit.RevenueSplitT.InitFromObj(CMT.SPLITS(i))
                    self.SPLITS.append(revenueSplit_)
        if CMT.PLATFORM_RECIPIENT() is not None:
            self.PLATFORM_RECIPIENT = SplitRecipient.SplitRecipientT.InitFromObj(CMT.PLATFORM_RECIPIENT())
        self.ENFORCEMENT = CMT.ENFORCEMENT()
        if not CMT.CONTRACTSIsNone():
            self.CONTRACTS = []
            for i in range(CMT.CONTRACTSLength()):
                if CMT.CONTRACTS(i) is None:
                    self.CONTRACTS.append(None)
                else:
                    sCX_ = SCX.SCXT.InitFromObj(CMT.CONTRACTS(i))
                    self.CONTRACTS.append(sCX_)
        self.EFFECTIVE_AT = CMT.EFFECTIVE_AT()
        self.EXPIRES_AT = CMT.EXPIRES_AT()
        if not CMT.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(CMT.SIGNATURELength()):
                    self.SIGNATURE.append(CMT.SIGNATURE(i))
            else:
                self.SIGNATURE = CMT.SIGNATUREAsNumpy()

    # CMTT
    def Pack(self, builder):
        if self.TERMS_ID is not None:
            TERMS_ID = builder.CreateString(self.TERMS_ID)
        if self.TARGET_ID is not None:
            TARGET_ID = builder.CreateString(self.TARGET_ID)
        if self.SPLITS is not None:
            SPLITSlist = []
            for i in range(len(self.SPLITS)):
                SPLITSlist.append(self.SPLITS[i].Pack(builder))
            CMTStartSPLITSVector(builder, len(self.SPLITS))
            for i in reversed(range(len(self.SPLITS))):
                builder.PrependUOffsetTRelative(SPLITSlist[i])
            SPLITS = builder.EndVector()
        if self.PLATFORM_RECIPIENT is not None:
            PLATFORM_RECIPIENT = self.PLATFORM_RECIPIENT.Pack(builder)
        if self.CONTRACTS is not None:
            CONTRACTSlist = []
            for i in range(len(self.CONTRACTS)):
                CONTRACTSlist.append(self.CONTRACTS[i].Pack(builder))
            CMTStartCONTRACTSVector(builder, len(self.CONTRACTS))
            for i in reversed(range(len(self.CONTRACTS))):
                builder.PrependUOffsetTRelative(CONTRACTSlist[i])
            CONTRACTS = builder.EndVector()
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                CMTStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        CMTStart(builder)
        if self.TERMS_ID is not None:
            CMTAddTERMS_ID(builder, TERMS_ID)
        CMTAddSCOPE(builder, self.SCOPE)
        if self.TARGET_ID is not None:
            CMTAddTARGET_ID(builder, TARGET_ID)
        CMTAddCOMMISSION_BPS(builder, self.COMMISSION_BPS)
        if self.SPLITS is not None:
            CMTAddSPLITS(builder, SPLITS)
        if self.PLATFORM_RECIPIENT is not None:
            CMTAddPLATFORM_RECIPIENT(builder, PLATFORM_RECIPIENT)
        CMTAddENFORCEMENT(builder, self.ENFORCEMENT)
        if self.CONTRACTS is not None:
            CMTAddCONTRACTS(builder, CONTRACTS)
        CMTAddEFFECTIVE_AT(builder, self.EFFECTIVE_AT)
        CMTAddEXPIRES_AT(builder, self.EXPIRES_AT)
        if self.SIGNATURE is not None:
            CMTAddSIGNATURE(builder, SIGNATURE)
        CMT = CMTEnd(builder)
        return CMT
