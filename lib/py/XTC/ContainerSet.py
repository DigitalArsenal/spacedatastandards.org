# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Collection of sequence containers
class ContainerSet(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ContainerSet()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsContainerSet(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ContainerSetBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # ContainerSet
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Sequence containers
    # ContainerSet
    def CONTAINERS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SequenceContainer import SequenceContainer
            obj = SequenceContainer()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ContainerSet
    def CONTAINERSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ContainerSet
    def CONTAINERSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ContainerSetStart(builder):
    builder.StartObject(1)

def Start(builder):
    ContainerSetStart(builder)

def ContainerSetAddCONTAINERS(builder, CONTAINERS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CONTAINERS), 0)

def AddCONTAINERS(builder, CONTAINERS):
    ContainerSetAddCONTAINERS(builder, CONTAINERS)

def ContainerSetStartCONTAINERSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCONTAINERSVector(builder, numElems):
    return ContainerSetStartCONTAINERSVector(builder, numElems)

def ContainerSetEnd(builder):
    return builder.EndObject()

def End(builder):
    return ContainerSetEnd(builder)

import SequenceContainer
try:
    from typing import List
except:
    pass

class ContainerSetT(object):

    # ContainerSetT
    def __init__(self):
        self.CONTAINERS = None  # type: List[SequenceContainer.SequenceContainerT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        containerSet = ContainerSet()
        containerSet.Init(buf, pos)
        return cls.InitFromObj(containerSet)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, containerSet):
        x = ContainerSetT()
        x._UnPack(containerSet)
        return x

    # ContainerSetT
    def _UnPack(self, containerSet):
        if containerSet is None:
            return
        if not containerSet.CONTAINERSIsNone():
            self.CONTAINERS = []
            for i in range(containerSet.CONTAINERSLength()):
                if containerSet.CONTAINERS(i) is None:
                    self.CONTAINERS.append(None)
                else:
                    sequenceContainer_ = SequenceContainer.SequenceContainerT.InitFromObj(containerSet.CONTAINERS(i))
                    self.CONTAINERS.append(sequenceContainer_)

    # ContainerSetT
    def Pack(self, builder):
        if self.CONTAINERS is not None:
            CONTAINERSlist = []
            for i in range(len(self.CONTAINERS)):
                CONTAINERSlist.append(self.CONTAINERS[i].Pack(builder))
            ContainerSetStartCONTAINERSVector(builder, len(self.CONTAINERS))
            for i in reversed(range(len(self.CONTAINERS))):
                builder.PrependUOffsetTRelative(CONTAINERSlist[i])
            CONTAINERS = builder.EndVector()
        ContainerSetStart(builder)
        if self.CONTAINERS is not None:
            ContainerSetAddCONTAINERS(builder, CONTAINERS)
        containerSet = ContainerSetEnd(builder)
        return containerSet
