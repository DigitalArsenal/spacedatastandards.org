# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Array parameter type
class ArrayParameterType(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ArrayParameterType()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArrayParameterType(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ArrayParameterTypeBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # ArrayParameterType
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Type name
    # ArrayParameterType
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Short description
    # ArrayParameterType
    def SHORT_DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Long description
    # ArrayParameterType
    def LONG_DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference to the array element type
    # ArrayParameterType
    def ARRAY_TYPE_REF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Dimension list
    # ArrayParameterType
    def DIMENSIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ArrayDimension import ArrayDimension
            obj = ArrayDimension()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ArrayParameterType
    def DIMENSIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ArrayParameterType
    def DIMENSIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def ArrayParameterTypeStart(builder):
    builder.StartObject(5)

def Start(builder):
    ArrayParameterTypeStart(builder)

def ArrayParameterTypeAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    ArrayParameterTypeAddNAME(builder, NAME)

def ArrayParameterTypeAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SHORT_DESCRIPTION), 0)

def AddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    ArrayParameterTypeAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)

def ArrayParameterTypeAddLONG_DESCRIPTION(builder, LONG_DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(LONG_DESCRIPTION), 0)

def AddLONG_DESCRIPTION(builder, LONG_DESCRIPTION):
    ArrayParameterTypeAddLONG_DESCRIPTION(builder, LONG_DESCRIPTION)

def ArrayParameterTypeAddARRAY_TYPE_REF(builder, ARRAY_TYPE_REF):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ARRAY_TYPE_REF), 0)

def AddARRAY_TYPE_REF(builder, ARRAY_TYPE_REF):
    ArrayParameterTypeAddARRAY_TYPE_REF(builder, ARRAY_TYPE_REF)

def ArrayParameterTypeAddDIMENSIONS(builder, DIMENSIONS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(DIMENSIONS), 0)

def AddDIMENSIONS(builder, DIMENSIONS):
    ArrayParameterTypeAddDIMENSIONS(builder, DIMENSIONS)

def ArrayParameterTypeStartDIMENSIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDIMENSIONSVector(builder, numElems):
    return ArrayParameterTypeStartDIMENSIONSVector(builder, numElems)

def ArrayParameterTypeEnd(builder):
    return builder.EndObject()

def End(builder):
    return ArrayParameterTypeEnd(builder)

import ArrayDimension
try:
    from typing import List
except:
    pass

class ArrayParameterTypeT(object):

    # ArrayParameterTypeT
    def __init__(self):
        self.NAME = None  # type: str
        self.SHORT_DESCRIPTION = None  # type: str
        self.LONG_DESCRIPTION = None  # type: str
        self.ARRAY_TYPE_REF = None  # type: str
        self.DIMENSIONS = None  # type: List[ArrayDimension.ArrayDimensionT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arrayParameterType = ArrayParameterType()
        arrayParameterType.Init(buf, pos)
        return cls.InitFromObj(arrayParameterType)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arrayParameterType):
        x = ArrayParameterTypeT()
        x._UnPack(arrayParameterType)
        return x

    # ArrayParameterTypeT
    def _UnPack(self, arrayParameterType):
        if arrayParameterType is None:
            return
        self.NAME = arrayParameterType.NAME()
        self.SHORT_DESCRIPTION = arrayParameterType.SHORT_DESCRIPTION()
        self.LONG_DESCRIPTION = arrayParameterType.LONG_DESCRIPTION()
        self.ARRAY_TYPE_REF = arrayParameterType.ARRAY_TYPE_REF()
        if not arrayParameterType.DIMENSIONSIsNone():
            self.DIMENSIONS = []
            for i in range(arrayParameterType.DIMENSIONSLength()):
                if arrayParameterType.DIMENSIONS(i) is None:
                    self.DIMENSIONS.append(None)
                else:
                    arrayDimension_ = ArrayDimension.ArrayDimensionT.InitFromObj(arrayParameterType.DIMENSIONS(i))
                    self.DIMENSIONS.append(arrayDimension_)

    # ArrayParameterTypeT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.SHORT_DESCRIPTION is not None:
            SHORT_DESCRIPTION = builder.CreateString(self.SHORT_DESCRIPTION)
        if self.LONG_DESCRIPTION is not None:
            LONG_DESCRIPTION = builder.CreateString(self.LONG_DESCRIPTION)
        if self.ARRAY_TYPE_REF is not None:
            ARRAY_TYPE_REF = builder.CreateString(self.ARRAY_TYPE_REF)
        if self.DIMENSIONS is not None:
            DIMENSIONSlist = []
            for i in range(len(self.DIMENSIONS)):
                DIMENSIONSlist.append(self.DIMENSIONS[i].Pack(builder))
            ArrayParameterTypeStartDIMENSIONSVector(builder, len(self.DIMENSIONS))
            for i in reversed(range(len(self.DIMENSIONS))):
                builder.PrependUOffsetTRelative(DIMENSIONSlist[i])
            DIMENSIONS = builder.EndVector()
        ArrayParameterTypeStart(builder)
        if self.NAME is not None:
            ArrayParameterTypeAddNAME(builder, NAME)
        if self.SHORT_DESCRIPTION is not None:
            ArrayParameterTypeAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)
        if self.LONG_DESCRIPTION is not None:
            ArrayParameterTypeAddLONG_DESCRIPTION(builder, LONG_DESCRIPTION)
        if self.ARRAY_TYPE_REF is not None:
            ArrayParameterTypeAddARRAY_TYPE_REF(builder, ARRAY_TYPE_REF)
        if self.DIMENSIONS is not None:
            ArrayParameterTypeAddDIMENSIONS(builder, DIMENSIONS)
        arrayParameterType = ArrayParameterTypeEnd(builder)
        return arrayParameterType
