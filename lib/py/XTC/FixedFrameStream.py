# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Fixed frame stream
class FixedFrameStream(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FixedFrameStream()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFixedFrameStream(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FixedFrameStreamBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # FixedFrameStream
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stream name
    # FixedFrameStream
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Short description
    # FixedFrameStream
    def SHORT_DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Frame size in bits
    # FixedFrameStream
    def FRAME_SIZE_IN_BITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Sync pattern (hex string)
    # FixedFrameStream
    def SYNC_PATTERN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def FixedFrameStreamStart(builder):
    builder.StartObject(4)

def Start(builder):
    FixedFrameStreamStart(builder)

def FixedFrameStreamAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    FixedFrameStreamAddNAME(builder, NAME)

def FixedFrameStreamAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SHORT_DESCRIPTION), 0)

def AddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    FixedFrameStreamAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)

def FixedFrameStreamAddFRAME_SIZE_IN_BITS(builder, FRAME_SIZE_IN_BITS):
    builder.PrependUint32Slot(2, FRAME_SIZE_IN_BITS, 0)

def AddFRAME_SIZE_IN_BITS(builder, FRAME_SIZE_IN_BITS):
    FixedFrameStreamAddFRAME_SIZE_IN_BITS(builder, FRAME_SIZE_IN_BITS)

def FixedFrameStreamAddSYNC_PATTERN(builder, SYNC_PATTERN):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SYNC_PATTERN), 0)

def AddSYNC_PATTERN(builder, SYNC_PATTERN):
    FixedFrameStreamAddSYNC_PATTERN(builder, SYNC_PATTERN)

def FixedFrameStreamEnd(builder):
    return builder.EndObject()

def End(builder):
    return FixedFrameStreamEnd(builder)


class FixedFrameStreamT(object):

    # FixedFrameStreamT
    def __init__(self):
        self.NAME = None  # type: str
        self.SHORT_DESCRIPTION = None  # type: str
        self.FRAME_SIZE_IN_BITS = 0  # type: int
        self.SYNC_PATTERN = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        fixedFrameStream = FixedFrameStream()
        fixedFrameStream.Init(buf, pos)
        return cls.InitFromObj(fixedFrameStream)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, fixedFrameStream):
        x = FixedFrameStreamT()
        x._UnPack(fixedFrameStream)
        return x

    # FixedFrameStreamT
    def _UnPack(self, fixedFrameStream):
        if fixedFrameStream is None:
            return
        self.NAME = fixedFrameStream.NAME()
        self.SHORT_DESCRIPTION = fixedFrameStream.SHORT_DESCRIPTION()
        self.FRAME_SIZE_IN_BITS = fixedFrameStream.FRAME_SIZE_IN_BITS()
        self.SYNC_PATTERN = fixedFrameStream.SYNC_PATTERN()

    # FixedFrameStreamT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.SHORT_DESCRIPTION is not None:
            SHORT_DESCRIPTION = builder.CreateString(self.SHORT_DESCRIPTION)
        if self.SYNC_PATTERN is not None:
            SYNC_PATTERN = builder.CreateString(self.SYNC_PATTERN)
        FixedFrameStreamStart(builder)
        if self.NAME is not None:
            FixedFrameStreamAddNAME(builder, NAME)
        if self.SHORT_DESCRIPTION is not None:
            FixedFrameStreamAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)
        FixedFrameStreamAddFRAME_SIZE_IN_BITS(builder, self.FRAME_SIZE_IN_BITS)
        if self.SYNC_PATTERN is not None:
            FixedFrameStreamAddSYNC_PATTERN(builder, SYNC_PATTERN)
        fixedFrameStream = FixedFrameStreamEnd(builder)
        return fixedFrameStream
