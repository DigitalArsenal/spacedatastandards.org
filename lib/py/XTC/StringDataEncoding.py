# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# String data encoding specification
class StringDataEncoding(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StringDataEncoding()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStringDataEncoding(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def StringDataEncodingBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # StringDataEncoding
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Character encoding
    # StringDataEncoding
    def ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 3

    # Size type specification
    # StringDataEncoding
    def SIZE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Fixed size in bits (when SIZE_TYPE=FIXED)
    # StringDataEncoding
    def SIZE_IN_BITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Termination character (when SIZE_TYPE=TERMINATION_CHAR)
    # StringDataEncoding
    def TERMINATION_CHAR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Maximum size in bits (for variable length)
    # StringDataEncoding
    def MAX_SIZE_IN_BITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Leading size field bits (when SIZE_TYPE=LEADING_SIZE)
    # StringDataEncoding
    def LEADING_SIZE_BITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def StringDataEncodingStart(builder):
    builder.StartObject(6)

def Start(builder):
    StringDataEncodingStart(builder)

def StringDataEncodingAddENCODING(builder, ENCODING):
    builder.PrependInt8Slot(0, ENCODING, 3)

def AddENCODING(builder, ENCODING):
    StringDataEncodingAddENCODING(builder, ENCODING)

def StringDataEncodingAddSIZE_TYPE(builder, SIZE_TYPE):
    builder.PrependInt8Slot(1, SIZE_TYPE, 0)

def AddSIZE_TYPE(builder, SIZE_TYPE):
    StringDataEncodingAddSIZE_TYPE(builder, SIZE_TYPE)

def StringDataEncodingAddSIZE_IN_BITS(builder, SIZE_IN_BITS):
    builder.PrependUint16Slot(2, SIZE_IN_BITS, 0)

def AddSIZE_IN_BITS(builder, SIZE_IN_BITS):
    StringDataEncodingAddSIZE_IN_BITS(builder, SIZE_IN_BITS)

def StringDataEncodingAddTERMINATION_CHAR(builder, TERMINATION_CHAR):
    builder.PrependUint8Slot(3, TERMINATION_CHAR, 0)

def AddTERMINATION_CHAR(builder, TERMINATION_CHAR):
    StringDataEncodingAddTERMINATION_CHAR(builder, TERMINATION_CHAR)

def StringDataEncodingAddMAX_SIZE_IN_BITS(builder, MAX_SIZE_IN_BITS):
    builder.PrependUint16Slot(4, MAX_SIZE_IN_BITS, 0)

def AddMAX_SIZE_IN_BITS(builder, MAX_SIZE_IN_BITS):
    StringDataEncodingAddMAX_SIZE_IN_BITS(builder, MAX_SIZE_IN_BITS)

def StringDataEncodingAddLEADING_SIZE_BITS(builder, LEADING_SIZE_BITS):
    builder.PrependUint8Slot(5, LEADING_SIZE_BITS, 0)

def AddLEADING_SIZE_BITS(builder, LEADING_SIZE_BITS):
    StringDataEncodingAddLEADING_SIZE_BITS(builder, LEADING_SIZE_BITS)

def StringDataEncodingEnd(builder):
    return builder.EndObject()

def End(builder):
    return StringDataEncodingEnd(builder)


class StringDataEncodingT(object):

    # StringDataEncodingT
    def __init__(self):
        self.ENCODING = 3  # type: int
        self.SIZE_TYPE = 0  # type: int
        self.SIZE_IN_BITS = 0  # type: int
        self.TERMINATION_CHAR = 0  # type: int
        self.MAX_SIZE_IN_BITS = 0  # type: int
        self.LEADING_SIZE_BITS = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stringDataEncoding = StringDataEncoding()
        stringDataEncoding.Init(buf, pos)
        return cls.InitFromObj(stringDataEncoding)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stringDataEncoding):
        x = StringDataEncodingT()
        x._UnPack(stringDataEncoding)
        return x

    # StringDataEncodingT
    def _UnPack(self, stringDataEncoding):
        if stringDataEncoding is None:
            return
        self.ENCODING = stringDataEncoding.ENCODING()
        self.SIZE_TYPE = stringDataEncoding.SIZE_TYPE()
        self.SIZE_IN_BITS = stringDataEncoding.SIZE_IN_BITS()
        self.TERMINATION_CHAR = stringDataEncoding.TERMINATION_CHAR()
        self.MAX_SIZE_IN_BITS = stringDataEncoding.MAX_SIZE_IN_BITS()
        self.LEADING_SIZE_BITS = stringDataEncoding.LEADING_SIZE_BITS()

    # StringDataEncodingT
    def Pack(self, builder):
        StringDataEncodingStart(builder)
        StringDataEncodingAddENCODING(builder, self.ENCODING)
        StringDataEncodingAddSIZE_TYPE(builder, self.SIZE_TYPE)
        StringDataEncodingAddSIZE_IN_BITS(builder, self.SIZE_IN_BITS)
        StringDataEncodingAddTERMINATION_CHAR(builder, self.TERMINATION_CHAR)
        StringDataEncodingAddMAX_SIZE_IN_BITS(builder, self.MAX_SIZE_IN_BITS)
        StringDataEncodingAddLEADING_SIZE_BITS(builder, self.LEADING_SIZE_BITS)
        stringDataEncoding = StringDataEncodingEnd(builder)
        return stringDataEncoding
