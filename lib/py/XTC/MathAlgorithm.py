# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Math algorithm (inline calculation)
class MathAlgorithm(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MathAlgorithm()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMathAlgorithm(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def MathAlgorithmBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # MathAlgorithm
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Algorithm name
    # MathAlgorithm
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Short description
    # MathAlgorithm
    def SHORT_DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Math operation in RPN
    # MathAlgorithm
    def MATH_OPERATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Output parameter reference
    # MathAlgorithm
    def OUTPUT_PARAMETER_REF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Trigger conditions
    # MathAlgorithm
    def TRIGGERS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from AlgorithmTrigger import AlgorithmTrigger
            obj = AlgorithmTrigger()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MathAlgorithm
    def TRIGGERSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MathAlgorithm
    def TRIGGERSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def MathAlgorithmStart(builder):
    builder.StartObject(5)

def Start(builder):
    MathAlgorithmStart(builder)

def MathAlgorithmAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    MathAlgorithmAddNAME(builder, NAME)

def MathAlgorithmAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SHORT_DESCRIPTION), 0)

def AddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    MathAlgorithmAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)

def MathAlgorithmAddMATH_OPERATION(builder, MATH_OPERATION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MATH_OPERATION), 0)

def AddMATH_OPERATION(builder, MATH_OPERATION):
    MathAlgorithmAddMATH_OPERATION(builder, MATH_OPERATION)

def MathAlgorithmAddOUTPUT_PARAMETER_REF(builder, OUTPUT_PARAMETER_REF):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(OUTPUT_PARAMETER_REF), 0)

def AddOUTPUT_PARAMETER_REF(builder, OUTPUT_PARAMETER_REF):
    MathAlgorithmAddOUTPUT_PARAMETER_REF(builder, OUTPUT_PARAMETER_REF)

def MathAlgorithmAddTRIGGERS(builder, TRIGGERS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(TRIGGERS), 0)

def AddTRIGGERS(builder, TRIGGERS):
    MathAlgorithmAddTRIGGERS(builder, TRIGGERS)

def MathAlgorithmStartTRIGGERSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTRIGGERSVector(builder, numElems):
    return MathAlgorithmStartTRIGGERSVector(builder, numElems)

def MathAlgorithmEnd(builder):
    return builder.EndObject()

def End(builder):
    return MathAlgorithmEnd(builder)

import AlgorithmTrigger
try:
    from typing import List
except:
    pass

class MathAlgorithmT(object):

    # MathAlgorithmT
    def __init__(self):
        self.NAME = None  # type: str
        self.SHORT_DESCRIPTION = None  # type: str
        self.MATH_OPERATION = None  # type: str
        self.OUTPUT_PARAMETER_REF = None  # type: str
        self.TRIGGERS = None  # type: List[AlgorithmTrigger.AlgorithmTriggerT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        mathAlgorithm = MathAlgorithm()
        mathAlgorithm.Init(buf, pos)
        return cls.InitFromObj(mathAlgorithm)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, mathAlgorithm):
        x = MathAlgorithmT()
        x._UnPack(mathAlgorithm)
        return x

    # MathAlgorithmT
    def _UnPack(self, mathAlgorithm):
        if mathAlgorithm is None:
            return
        self.NAME = mathAlgorithm.NAME()
        self.SHORT_DESCRIPTION = mathAlgorithm.SHORT_DESCRIPTION()
        self.MATH_OPERATION = mathAlgorithm.MATH_OPERATION()
        self.OUTPUT_PARAMETER_REF = mathAlgorithm.OUTPUT_PARAMETER_REF()
        if not mathAlgorithm.TRIGGERSIsNone():
            self.TRIGGERS = []
            for i in range(mathAlgorithm.TRIGGERSLength()):
                if mathAlgorithm.TRIGGERS(i) is None:
                    self.TRIGGERS.append(None)
                else:
                    algorithmTrigger_ = AlgorithmTrigger.AlgorithmTriggerT.InitFromObj(mathAlgorithm.TRIGGERS(i))
                    self.TRIGGERS.append(algorithmTrigger_)

    # MathAlgorithmT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.SHORT_DESCRIPTION is not None:
            SHORT_DESCRIPTION = builder.CreateString(self.SHORT_DESCRIPTION)
        if self.MATH_OPERATION is not None:
            MATH_OPERATION = builder.CreateString(self.MATH_OPERATION)
        if self.OUTPUT_PARAMETER_REF is not None:
            OUTPUT_PARAMETER_REF = builder.CreateString(self.OUTPUT_PARAMETER_REF)
        if self.TRIGGERS is not None:
            TRIGGERSlist = []
            for i in range(len(self.TRIGGERS)):
                TRIGGERSlist.append(self.TRIGGERS[i].Pack(builder))
            MathAlgorithmStartTRIGGERSVector(builder, len(self.TRIGGERS))
            for i in reversed(range(len(self.TRIGGERS))):
                builder.PrependUOffsetTRelative(TRIGGERSlist[i])
            TRIGGERS = builder.EndVector()
        MathAlgorithmStart(builder)
        if self.NAME is not None:
            MathAlgorithmAddNAME(builder, NAME)
        if self.SHORT_DESCRIPTION is not None:
            MathAlgorithmAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)
        if self.MATH_OPERATION is not None:
            MathAlgorithmAddMATH_OPERATION(builder, MATH_OPERATION)
        if self.OUTPUT_PARAMETER_REF is not None:
            MathAlgorithmAddOUTPUT_PARAMETER_REF(builder, OUTPUT_PARAMETER_REF)
        if self.TRIGGERS is not None:
            MathAlgorithmAddTRIGGERS(builder, TRIGGERS)
        mathAlgorithm = MathAlgorithmEnd(builder)
        return mathAlgorithm
