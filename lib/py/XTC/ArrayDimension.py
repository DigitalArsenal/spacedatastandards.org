# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Array dimension specification
class ArrayDimension(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ArrayDimension()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArrayDimension(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ArrayDimensionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # ArrayDimension
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Fixed size of this dimension
    # ArrayDimension
    def SIZE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Dynamic size from parameter reference
    # ArrayDimension
    def SIZE_PARAMETER_REF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def ArrayDimensionStart(builder):
    builder.StartObject(2)

def Start(builder):
    ArrayDimensionStart(builder)

def ArrayDimensionAddSIZE(builder, SIZE):
    builder.PrependUint32Slot(0, SIZE, 0)

def AddSIZE(builder, SIZE):
    ArrayDimensionAddSIZE(builder, SIZE)

def ArrayDimensionAddSIZE_PARAMETER_REF(builder, SIZE_PARAMETER_REF):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SIZE_PARAMETER_REF), 0)

def AddSIZE_PARAMETER_REF(builder, SIZE_PARAMETER_REF):
    ArrayDimensionAddSIZE_PARAMETER_REF(builder, SIZE_PARAMETER_REF)

def ArrayDimensionEnd(builder):
    return builder.EndObject()

def End(builder):
    return ArrayDimensionEnd(builder)


class ArrayDimensionT(object):

    # ArrayDimensionT
    def __init__(self):
        self.SIZE = 0  # type: int
        self.SIZE_PARAMETER_REF = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arrayDimension = ArrayDimension()
        arrayDimension.Init(buf, pos)
        return cls.InitFromObj(arrayDimension)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arrayDimension):
        x = ArrayDimensionT()
        x._UnPack(arrayDimension)
        return x

    # ArrayDimensionT
    def _UnPack(self, arrayDimension):
        if arrayDimension is None:
            return
        self.SIZE = arrayDimension.SIZE()
        self.SIZE_PARAMETER_REF = arrayDimension.SIZE_PARAMETER_REF()

    # ArrayDimensionT
    def Pack(self, builder):
        if self.SIZE_PARAMETER_REF is not None:
            SIZE_PARAMETER_REF = builder.CreateString(self.SIZE_PARAMETER_REF)
        ArrayDimensionStart(builder)
        ArrayDimensionAddSIZE(builder, self.SIZE)
        if self.SIZE_PARAMETER_REF is not None:
            ArrayDimensionAddSIZE_PARAMETER_REF(builder, SIZE_PARAMETER_REF)
        arrayDimension = ArrayDimensionEnd(builder)
        return arrayDimension
