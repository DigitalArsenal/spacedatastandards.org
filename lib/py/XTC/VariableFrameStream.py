# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Variable frame stream
class VariableFrameStream(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VariableFrameStream()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVariableFrameStream(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VariableFrameStreamBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # VariableFrameStream
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stream name
    # VariableFrameStream
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Short description
    # VariableFrameStream
    def SHORT_DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Minimum frame size in bits
    # VariableFrameStream
    def MIN_FRAME_SIZE_IN_BITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Maximum frame size in bits
    # VariableFrameStream
    def MAX_FRAME_SIZE_IN_BITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Size field location in bits
    # VariableFrameStream
    def SIZE_FIELD_OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Size field size in bits
    # VariableFrameStream
    def SIZE_FIELD_SIZE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

def VariableFrameStreamStart(builder):
    builder.StartObject(6)

def Start(builder):
    VariableFrameStreamStart(builder)

def VariableFrameStreamAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    VariableFrameStreamAddNAME(builder, NAME)

def VariableFrameStreamAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SHORT_DESCRIPTION), 0)

def AddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION):
    VariableFrameStreamAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)

def VariableFrameStreamAddMIN_FRAME_SIZE_IN_BITS(builder, MIN_FRAME_SIZE_IN_BITS):
    builder.PrependUint32Slot(2, MIN_FRAME_SIZE_IN_BITS, 0)

def AddMIN_FRAME_SIZE_IN_BITS(builder, MIN_FRAME_SIZE_IN_BITS):
    VariableFrameStreamAddMIN_FRAME_SIZE_IN_BITS(builder, MIN_FRAME_SIZE_IN_BITS)

def VariableFrameStreamAddMAX_FRAME_SIZE_IN_BITS(builder, MAX_FRAME_SIZE_IN_BITS):
    builder.PrependUint32Slot(3, MAX_FRAME_SIZE_IN_BITS, 0)

def AddMAX_FRAME_SIZE_IN_BITS(builder, MAX_FRAME_SIZE_IN_BITS):
    VariableFrameStreamAddMAX_FRAME_SIZE_IN_BITS(builder, MAX_FRAME_SIZE_IN_BITS)

def VariableFrameStreamAddSIZE_FIELD_OFFSET(builder, SIZE_FIELD_OFFSET):
    builder.PrependUint32Slot(4, SIZE_FIELD_OFFSET, 0)

def AddSIZE_FIELD_OFFSET(builder, SIZE_FIELD_OFFSET):
    VariableFrameStreamAddSIZE_FIELD_OFFSET(builder, SIZE_FIELD_OFFSET)

def VariableFrameStreamAddSIZE_FIELD_SIZE(builder, SIZE_FIELD_SIZE):
    builder.PrependUint16Slot(5, SIZE_FIELD_SIZE, 0)

def AddSIZE_FIELD_SIZE(builder, SIZE_FIELD_SIZE):
    VariableFrameStreamAddSIZE_FIELD_SIZE(builder, SIZE_FIELD_SIZE)

def VariableFrameStreamEnd(builder):
    return builder.EndObject()

def End(builder):
    return VariableFrameStreamEnd(builder)


class VariableFrameStreamT(object):

    # VariableFrameStreamT
    def __init__(self):
        self.NAME = None  # type: str
        self.SHORT_DESCRIPTION = None  # type: str
        self.MIN_FRAME_SIZE_IN_BITS = 0  # type: int
        self.MAX_FRAME_SIZE_IN_BITS = 0  # type: int
        self.SIZE_FIELD_OFFSET = 0  # type: int
        self.SIZE_FIELD_SIZE = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        variableFrameStream = VariableFrameStream()
        variableFrameStream.Init(buf, pos)
        return cls.InitFromObj(variableFrameStream)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, variableFrameStream):
        x = VariableFrameStreamT()
        x._UnPack(variableFrameStream)
        return x

    # VariableFrameStreamT
    def _UnPack(self, variableFrameStream):
        if variableFrameStream is None:
            return
        self.NAME = variableFrameStream.NAME()
        self.SHORT_DESCRIPTION = variableFrameStream.SHORT_DESCRIPTION()
        self.MIN_FRAME_SIZE_IN_BITS = variableFrameStream.MIN_FRAME_SIZE_IN_BITS()
        self.MAX_FRAME_SIZE_IN_BITS = variableFrameStream.MAX_FRAME_SIZE_IN_BITS()
        self.SIZE_FIELD_OFFSET = variableFrameStream.SIZE_FIELD_OFFSET()
        self.SIZE_FIELD_SIZE = variableFrameStream.SIZE_FIELD_SIZE()

    # VariableFrameStreamT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.SHORT_DESCRIPTION is not None:
            SHORT_DESCRIPTION = builder.CreateString(self.SHORT_DESCRIPTION)
        VariableFrameStreamStart(builder)
        if self.NAME is not None:
            VariableFrameStreamAddNAME(builder, NAME)
        if self.SHORT_DESCRIPTION is not None:
            VariableFrameStreamAddSHORT_DESCRIPTION(builder, SHORT_DESCRIPTION)
        VariableFrameStreamAddMIN_FRAME_SIZE_IN_BITS(builder, self.MIN_FRAME_SIZE_IN_BITS)
        VariableFrameStreamAddMAX_FRAME_SIZE_IN_BITS(builder, self.MAX_FRAME_SIZE_IN_BITS)
        VariableFrameStreamAddSIZE_FIELD_OFFSET(builder, self.SIZE_FIELD_OFFSET)
        VariableFrameStreamAddSIZE_FIELD_SIZE(builder, self.SIZE_FIELD_SIZE)
        variableFrameStream = VariableFrameStreamEnd(builder)
        return variableFrameStream
