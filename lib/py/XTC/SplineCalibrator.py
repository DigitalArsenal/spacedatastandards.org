# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Spline calibrator - piecewise linear interpolation
class SplineCalibrator(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SplineCalibrator()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSplineCalibrator(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SplineCalibratorBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # SplineCalibrator
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Spline points ordered by raw value
    # SplineCalibrator
    def POINTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SplinePoint import SplinePoint
            obj = SplinePoint()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SplineCalibrator
    def POINTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SplineCalibrator
    def POINTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Extrapolate below minimum point
    # SplineCalibrator
    def EXTRAPOLATE_LOW(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Extrapolate above maximum point
    # SplineCalibrator
    def EXTRAPOLATE_HIGH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def SplineCalibratorStart(builder):
    builder.StartObject(3)

def Start(builder):
    SplineCalibratorStart(builder)

def SplineCalibratorAddPOINTS(builder, POINTS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(POINTS), 0)

def AddPOINTS(builder, POINTS):
    SplineCalibratorAddPOINTS(builder, POINTS)

def SplineCalibratorStartPOINTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPOINTSVector(builder, numElems):
    return SplineCalibratorStartPOINTSVector(builder, numElems)

def SplineCalibratorAddEXTRAPOLATE_LOW(builder, EXTRAPOLATE_LOW):
    builder.PrependBoolSlot(1, EXTRAPOLATE_LOW, 0)

def AddEXTRAPOLATE_LOW(builder, EXTRAPOLATE_LOW):
    SplineCalibratorAddEXTRAPOLATE_LOW(builder, EXTRAPOLATE_LOW)

def SplineCalibratorAddEXTRAPOLATE_HIGH(builder, EXTRAPOLATE_HIGH):
    builder.PrependBoolSlot(2, EXTRAPOLATE_HIGH, 0)

def AddEXTRAPOLATE_HIGH(builder, EXTRAPOLATE_HIGH):
    SplineCalibratorAddEXTRAPOLATE_HIGH(builder, EXTRAPOLATE_HIGH)

def SplineCalibratorEnd(builder):
    return builder.EndObject()

def End(builder):
    return SplineCalibratorEnd(builder)

import SplinePoint
try:
    from typing import List
except:
    pass

class SplineCalibratorT(object):

    # SplineCalibratorT
    def __init__(self):
        self.POINTS = None  # type: List[SplinePoint.SplinePointT]
        self.EXTRAPOLATE_LOW = False  # type: bool
        self.EXTRAPOLATE_HIGH = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        splineCalibrator = SplineCalibrator()
        splineCalibrator.Init(buf, pos)
        return cls.InitFromObj(splineCalibrator)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, splineCalibrator):
        x = SplineCalibratorT()
        x._UnPack(splineCalibrator)
        return x

    # SplineCalibratorT
    def _UnPack(self, splineCalibrator):
        if splineCalibrator is None:
            return
        if not splineCalibrator.POINTSIsNone():
            self.POINTS = []
            for i in range(splineCalibrator.POINTSLength()):
                if splineCalibrator.POINTS(i) is None:
                    self.POINTS.append(None)
                else:
                    splinePoint_ = SplinePoint.SplinePointT.InitFromObj(splineCalibrator.POINTS(i))
                    self.POINTS.append(splinePoint_)
        self.EXTRAPOLATE_LOW = splineCalibrator.EXTRAPOLATE_LOW()
        self.EXTRAPOLATE_HIGH = splineCalibrator.EXTRAPOLATE_HIGH()

    # SplineCalibratorT
    def Pack(self, builder):
        if self.POINTS is not None:
            POINTSlist = []
            for i in range(len(self.POINTS)):
                POINTSlist.append(self.POINTS[i].Pack(builder))
            SplineCalibratorStartPOINTSVector(builder, len(self.POINTS))
            for i in reversed(range(len(self.POINTS))):
                builder.PrependUOffsetTRelative(POINTSlist[i])
            POINTS = builder.EndVector()
        SplineCalibratorStart(builder)
        if self.POINTS is not None:
            SplineCalibratorAddPOINTS(builder, POINTS)
        SplineCalibratorAddEXTRAPOLATE_LOW(builder, self.EXTRAPOLATE_LOW)
        SplineCalibratorAddEXTRAPOLATE_HIGH(builder, self.EXTRAPOLATE_HIGH)
        splineCalibrator = SplineCalibratorEnd(builder)
        return splineCalibrator
