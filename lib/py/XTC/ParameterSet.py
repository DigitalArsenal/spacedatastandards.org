# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Collection of parameters
class ParameterSet(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ParameterSet()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsParameterSet(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ParameterSetBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # ParameterSet
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Parameters
    # ParameterSet
    def PARAMETERS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from Parameter import Parameter
            obj = Parameter()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ParameterSet
    def PARAMETERSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ParameterSet
    def PARAMETERSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ParameterSetStart(builder):
    builder.StartObject(1)

def Start(builder):
    ParameterSetStart(builder)

def ParameterSetAddPARAMETERS(builder, PARAMETERS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PARAMETERS), 0)

def AddPARAMETERS(builder, PARAMETERS):
    ParameterSetAddPARAMETERS(builder, PARAMETERS)

def ParameterSetStartPARAMETERSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPARAMETERSVector(builder, numElems):
    return ParameterSetStartPARAMETERSVector(builder, numElems)

def ParameterSetEnd(builder):
    return builder.EndObject()

def End(builder):
    return ParameterSetEnd(builder)

import Parameter
try:
    from typing import List
except:
    pass

class ParameterSetT(object):

    # ParameterSetT
    def __init__(self):
        self.PARAMETERS = None  # type: List[Parameter.ParameterT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        parameterSet = ParameterSet()
        parameterSet.Init(buf, pos)
        return cls.InitFromObj(parameterSet)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, parameterSet):
        x = ParameterSetT()
        x._UnPack(parameterSet)
        return x

    # ParameterSetT
    def _UnPack(self, parameterSet):
        if parameterSet is None:
            return
        if not parameterSet.PARAMETERSIsNone():
            self.PARAMETERS = []
            for i in range(parameterSet.PARAMETERSLength()):
                if parameterSet.PARAMETERS(i) is None:
                    self.PARAMETERS.append(None)
                else:
                    parameter_ = Parameter.ParameterT.InitFromObj(parameterSet.PARAMETERS(i))
                    self.PARAMETERS.append(parameter_)

    # ParameterSetT
    def Pack(self, builder):
        if self.PARAMETERS is not None:
            PARAMETERSlist = []
            for i in range(len(self.PARAMETERS)):
                PARAMETERSlist.append(self.PARAMETERS[i].Pack(builder))
            ParameterSetStartPARAMETERSVector(builder, len(self.PARAMETERS))
            for i in reversed(range(len(self.PARAMETERS))):
                builder.PrependUOffsetTRelative(PARAMETERSlist[i])
            PARAMETERS = builder.EndVector()
        ParameterSetStart(builder)
        if self.PARAMETERS is not None:
            ParameterSetAddPARAMETERS(builder, PARAMETERS)
        parameterSet = ParameterSetEnd(builder)
        return parameterSet
