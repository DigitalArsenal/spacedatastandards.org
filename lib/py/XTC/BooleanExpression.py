# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Boolean expression combining comparisons
class BooleanExpression(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BooleanExpression()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBooleanExpression(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BooleanExpressionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # BooleanExpression
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Boolean operator
    # BooleanExpression
    def OPERATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Comparison conditions
    # BooleanExpression
    def CONDITIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ParameterComparison import ParameterComparison
            obj = ParameterComparison()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # BooleanExpression
    def CONDITIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BooleanExpression
    def CONDITIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Nested expressions
    # BooleanExpression
    def EXPRESSIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from BooleanExpression import BooleanExpression
            obj = BooleanExpression()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # BooleanExpression
    def EXPRESSIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BooleanExpression
    def EXPRESSIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def BooleanExpressionStart(builder):
    builder.StartObject(3)

def Start(builder):
    BooleanExpressionStart(builder)

def BooleanExpressionAddOPERATOR(builder, OPERATOR):
    builder.PrependInt8Slot(0, OPERATOR, 0)

def AddOPERATOR(builder, OPERATOR):
    BooleanExpressionAddOPERATOR(builder, OPERATOR)

def BooleanExpressionAddCONDITIONS(builder, CONDITIONS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CONDITIONS), 0)

def AddCONDITIONS(builder, CONDITIONS):
    BooleanExpressionAddCONDITIONS(builder, CONDITIONS)

def BooleanExpressionStartCONDITIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCONDITIONSVector(builder, numElems):
    return BooleanExpressionStartCONDITIONSVector(builder, numElems)

def BooleanExpressionAddEXPRESSIONS(builder, EXPRESSIONS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(EXPRESSIONS), 0)

def AddEXPRESSIONS(builder, EXPRESSIONS):
    BooleanExpressionAddEXPRESSIONS(builder, EXPRESSIONS)

def BooleanExpressionStartEXPRESSIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartEXPRESSIONSVector(builder, numElems):
    return BooleanExpressionStartEXPRESSIONSVector(builder, numElems)

def BooleanExpressionEnd(builder):
    return builder.EndObject()

def End(builder):
    return BooleanExpressionEnd(builder)

import ParameterComparison
try:
    from typing import List
except:
    pass

class BooleanExpressionT(object):

    # BooleanExpressionT
    def __init__(self):
        self.OPERATOR = 0  # type: int
        self.CONDITIONS = None  # type: List[ParameterComparison.ParameterComparisonT]
        self.EXPRESSIONS = None  # type: List[BooleanExpression.BooleanExpressionT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        booleanExpression = BooleanExpression()
        booleanExpression.Init(buf, pos)
        return cls.InitFromObj(booleanExpression)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, booleanExpression):
        x = BooleanExpressionT()
        x._UnPack(booleanExpression)
        return x

    # BooleanExpressionT
    def _UnPack(self, booleanExpression):
        if booleanExpression is None:
            return
        self.OPERATOR = booleanExpression.OPERATOR()
        if not booleanExpression.CONDITIONSIsNone():
            self.CONDITIONS = []
            for i in range(booleanExpression.CONDITIONSLength()):
                if booleanExpression.CONDITIONS(i) is None:
                    self.CONDITIONS.append(None)
                else:
                    parameterComparison_ = ParameterComparison.ParameterComparisonT.InitFromObj(booleanExpression.CONDITIONS(i))
                    self.CONDITIONS.append(parameterComparison_)
        if not booleanExpression.EXPRESSIONSIsNone():
            self.EXPRESSIONS = []
            for i in range(booleanExpression.EXPRESSIONSLength()):
                if booleanExpression.EXPRESSIONS(i) is None:
                    self.EXPRESSIONS.append(None)
                else:
                    booleanExpression_ = BooleanExpression.BooleanExpressionT.InitFromObj(booleanExpression.EXPRESSIONS(i))
                    self.EXPRESSIONS.append(booleanExpression_)

    # BooleanExpressionT
    def Pack(self, builder):
        if self.CONDITIONS is not None:
            CONDITIONSlist = []
            for i in range(len(self.CONDITIONS)):
                CONDITIONSlist.append(self.CONDITIONS[i].Pack(builder))
            BooleanExpressionStartCONDITIONSVector(builder, len(self.CONDITIONS))
            for i in reversed(range(len(self.CONDITIONS))):
                builder.PrependUOffsetTRelative(CONDITIONSlist[i])
            CONDITIONS = builder.EndVector()
        if self.EXPRESSIONS is not None:
            EXPRESSIONSlist = []
            for i in range(len(self.EXPRESSIONS)):
                EXPRESSIONSlist.append(self.EXPRESSIONS[i].Pack(builder))
            BooleanExpressionStartEXPRESSIONSVector(builder, len(self.EXPRESSIONS))
            for i in reversed(range(len(self.EXPRESSIONS))):
                builder.PrependUOffsetTRelative(EXPRESSIONSlist[i])
            EXPRESSIONS = builder.EndVector()
        BooleanExpressionStart(builder)
        BooleanExpressionAddOPERATOR(builder, self.OPERATOR)
        if self.CONDITIONS is not None:
            BooleanExpressionAddCONDITIONS(builder, CONDITIONS)
        if self.EXPRESSIONS is not None:
            BooleanExpressionAddEXPRESSIONS(builder, EXPRESSIONS)
        booleanExpression = BooleanExpressionEnd(builder)
        return booleanExpression
