# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Collection of services
class ServiceSet(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ServiceSet()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsServiceSet(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ServiceSetBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x58\x54\x43", size_prefixed=size_prefixed)

    # ServiceSet
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Services
    # ServiceSet
    def SERVICES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from Service import Service
            obj = Service()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ServiceSet
    def SERVICESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ServiceSet
    def SERVICESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ServiceSetStart(builder):
    builder.StartObject(1)

def Start(builder):
    ServiceSetStart(builder)

def ServiceSetAddSERVICES(builder, SERVICES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SERVICES), 0)

def AddSERVICES(builder, SERVICES):
    ServiceSetAddSERVICES(builder, SERVICES)

def ServiceSetStartSERVICESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSERVICESVector(builder, numElems):
    return ServiceSetStartSERVICESVector(builder, numElems)

def ServiceSetEnd(builder):
    return builder.EndObject()

def End(builder):
    return ServiceSetEnd(builder)

import Service
try:
    from typing import List
except:
    pass

class ServiceSetT(object):

    # ServiceSetT
    def __init__(self):
        self.SERVICES = None  # type: List[Service.ServiceT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        serviceSet = ServiceSet()
        serviceSet.Init(buf, pos)
        return cls.InitFromObj(serviceSet)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, serviceSet):
        x = ServiceSetT()
        x._UnPack(serviceSet)
        return x

    # ServiceSetT
    def _UnPack(self, serviceSet):
        if serviceSet is None:
            return
        if not serviceSet.SERVICESIsNone():
            self.SERVICES = []
            for i in range(serviceSet.SERVICESLength()):
                if serviceSet.SERVICES(i) is None:
                    self.SERVICES.append(None)
                else:
                    service_ = Service.ServiceT.InitFromObj(serviceSet.SERVICES(i))
                    self.SERVICES.append(service_)

    # ServiceSetT
    def Pack(self, builder):
        if self.SERVICES is not None:
            SERVICESlist = []
            for i in range(len(self.SERVICES)):
                SERVICESlist.append(self.SERVICES[i].Pack(builder))
            ServiceSetStartSERVICESVector(builder, len(self.SERVICES))
            for i in reversed(range(len(self.SERVICES))):
                builder.PrependUOffsetTRelative(SERVICESlist[i])
            SERVICES = builder.EndVector()
        ServiceSetStart(builder)
        if self.SERVICES is not None:
            ServiceSetAddSERVICES(builder, SERVICES)
        serviceSet = ServiceSetEnd(builder)
        return serviceSet
