# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class OSMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OSMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOSMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OSMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x53\x4D", size_prefixed=size_prefixed)

    # OSMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # OSMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from OSM import OSM
            obj = OSM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OSMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # OSMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def OSMCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    OSMCOLLECTIONStart(builder)

def OSMCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    OSMCOLLECTIONAddRECORDS(builder, RECORDS)

def OSMCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return OSMCOLLECTIONStartRECORDSVector(builder, numElems)

def OSMCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return OSMCOLLECTIONEnd(builder)

import OSM
try:
    from typing import List
except:
    pass

class OSMCOLLECTIONT(object):

    # OSMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[OSM.OSMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        OSMCOLLECTION = OSMCOLLECTION()
        OSMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(OSMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, OSMCOLLECTION):
        x = OSMCOLLECTIONT()
        x._UnPack(OSMCOLLECTION)
        return x

    # OSMCOLLECTIONT
    def _UnPack(self, OSMCOLLECTION):
        if OSMCOLLECTION is None:
            return
        if not OSMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(OSMCOLLECTION.RECORDSLength()):
                if OSMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    oSM_ = OSM.OSMT.InitFromObj(OSMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(oSM_)

    # OSMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            OSMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        OSMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            OSMCOLLECTIONAddRECORDS(builder, RECORDS)
        OSMCOLLECTION = OSMCOLLECTIONEnd(builder)
        return OSMCOLLECTION
