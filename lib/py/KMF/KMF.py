# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
from .FlatbuffersEncryption import FlatbuffersEncryption
np = import_numpy()

# Key Material Frame
class KMF(object):
    __slots__ = ['_tab', '_encryption_ctx']

    @classmethod
    def GetRootAs(cls, buf, offset=0, encryption_ctx=None):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMF()
        x.Init(buf, n + offset, encryption_ctx)
        return x

    @classmethod
    def GetRootAsKMF(cls, buf, offset=0, encryption_ctx=None):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset, encryption_ctx)
    @classmethod
    def KMFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x46", size_prefixed=size_prefixed)

    # KMF
    def Init(self, buf, pos, encryption_ctx=None):
        self._tab = flatbuffers.table.Table(buf, pos)
        self._encryption_ctx = encryption_ctx

    # Logical key identifier used across publication and grant records.
    # KMF
    def KEY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Role the key bytes fulfill for the module flow.
    # KMF
    def ROLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Algorithm or key family for the key bytes.
    # KMF
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Encoding of KEY_BYTES.
    # KMF
    def ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Explicit key bytes when a module must receive them on a port.
    # This may be field-encrypted using the SDS/da-flatbuffers header-first
    # encryption flow when transported to a specific recipient.
    # KMF
    def KEY_BYTES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # KMF
    def KEY_BYTESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # KMF
    def KEY_BYTESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KMF
    def KEY_BYTESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Logical version of the key material.
    # KMF
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Expiration time in unix milliseconds, or 0 if unbounded.
    # KMF
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def KMFStart(builder):
    builder.StartObject(7)

def Start(builder):
    KMFStart(builder)

def KMFAddKEY_ID(builder, KEY_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_ID), 0)

def AddKEY_ID(builder, KEY_ID):
    KMFAddKEY_ID(builder, KEY_ID)

def KMFAddROLE(builder, ROLE):
    builder.PrependInt8Slot(1, ROLE, 0)

def AddROLE(builder, ROLE):
    KMFAddROLE(builder, ROLE)

def KMFAddALGORITHM(builder, ALGORITHM):
    builder.PrependInt8Slot(2, ALGORITHM, 0)

def AddALGORITHM(builder, ALGORITHM):
    KMFAddALGORITHM(builder, ALGORITHM)

def KMFAddENCODING(builder, ENCODING):
    builder.PrependInt8Slot(3, ENCODING, 0)

def AddENCODING(builder, ENCODING):
    KMFAddENCODING(builder, ENCODING)

def KMFAddKEY_BYTES(builder, KEY_BYTES):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_BYTES), 0)

def AddKEY_BYTES(builder, KEY_BYTES):
    KMFAddKEY_BYTES(builder, KEY_BYTES)

def KMFStartKEY_BYTESVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartKEY_BYTESVector(builder, numElems):
    return KMFStartKEY_BYTESVector(builder, numElems)

def KMFCreateKEY_BYTESVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateKEY_BYTESVector(builder, data):
    KMFCreateKEY_BYTESVector(builder, data)

def KMFAddVERSION(builder, VERSION):
    builder.PrependUint32Slot(5, VERSION, 0)

def AddVERSION(builder, VERSION):
    KMFAddVERSION(builder, VERSION)

def KMFAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(6, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    KMFAddEXPIRES_AT(builder, EXPIRES_AT)

def KMFEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMFEnd(builder)

try:
    from typing import List
except:
    pass

class KMFT(object):

    # KMFT
    def __init__(
        self,
        KEY_ID = None,
        ROLE = 0,
        ALGORITHM = 0,
        ENCODING = 0,
        KEY_BYTES = None,
        VERSION = 0,
        EXPIRES_AT = 0,
    ):
        self.KEY_ID = KEY_ID  # type: Optional[str]
        self.ROLE = ROLE  # type: int
        self.ALGORITHM = ALGORITHM  # type: int
        self.ENCODING = ENCODING  # type: int
        self.KEY_BYTES = KEY_BYTES  # type: Optional[List[int]]
        self.VERSION = VERSION  # type: int
        self.EXPIRES_AT = EXPIRES_AT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpKmf = KMF()
        tmpKmf.Init(buf, pos)
        return cls.InitFromObj(tmpKmf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpKmf):
        x = KMFT()
        x._UnPack(tmpKmf)
        return x

    # KMFT
    def _UnPack(self, KMF):
        if KMF is None:
            return
        self.KEY_ID = KMF.KEY_ID()
        self.ROLE = KMF.ROLE()
        self.ALGORITHM = KMF.ALGORITHM()
        self.ENCODING = KMF.ENCODING()
        if not KMF.KEY_BYTESIsNone():
            if np is None:
                self.KEY_BYTES = []
                for i in range(KMF.KEY_BYTESLength()):
                    self.KEY_BYTES.append(KMF.KEY_BYTES(i))
            else:
                self.KEY_BYTES = KMF.KEY_BYTESAsNumpy()
        self.VERSION = KMF.VERSION()
        self.EXPIRES_AT = KMF.EXPIRES_AT()

    # KMFT
    def Pack(self, builder):
        if self.KEY_ID is not None:
            KEY_ID = builder.CreateString(self.KEY_ID)
        if self.KEY_BYTES is not None:
            if np is not None and type(self.KEY_BYTES) is np.ndarray:
                KEY_BYTES = builder.CreateNumpyVector(self.KEY_BYTES)
            else:
                KMFStartKEY_BYTESVector(builder, len(self.KEY_BYTES))
                for i in reversed(range(len(self.KEY_BYTES))):
                    builder.PrependUint8(self.KEY_BYTES[i])
                KEY_BYTES = builder.EndVector()
        KMFStart(builder)
        if self.KEY_ID is not None:
            KMFAddKEY_ID(builder, KEY_ID)
        KMFAddROLE(builder, self.ROLE)
        KMFAddALGORITHM(builder, self.ALGORITHM)
        KMFAddENCODING(builder, self.ENCODING)
        if self.KEY_BYTES is not None:
            KMFAddKEY_BYTES(builder, KEY_BYTES)
        KMFAddVERSION(builder, self.VERSION)
        KMFAddEXPIRES_AT(builder, self.EXPIRES_AT)
        KMF = KMFEnd(builder)
        return KMF
