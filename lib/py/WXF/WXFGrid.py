# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Regular grid the samples are laid out on. Samples are row-major with
# latitude as the outer index: cell (i, j) for 0 <= i < NLAT, 0 <= j < NLON
# sits at VALUES[i * NLON + j] and is centred at
# (LAT0 + i * DLAT, LON0 + j * DLON). DLAT is negative when rows run north
# to south. Longitudes are in [-180, 180) or [0, 360) exactly as the
# producer publishes them; a consumer normalises.
class WXFGrid(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = WXFGrid()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsWXFGrid(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def WXFGridBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x57\x58\x46", size_prefixed=size_prefixed)

    # WXFGrid
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Grid geometry.
    # WXFGrid
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Latitude of the first row's cell centre, degrees north.
    # WXFGrid
    def LAT0(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Longitude of the first column's cell centre, degrees east.
    # WXFGrid
    def LON0(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Latitude step between rows, degrees (signed).
    # WXFGrid
    def DLAT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Longitude step between columns, degrees (signed).
    # WXFGrid
    def DLON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Number of rows (latitudes).
    # WXFGrid
    def NLAT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Number of columns (longitudes).
    # WXFGrid
    def NLON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Whether the last column wraps to the first (global grid).
    # WXFGrid
    def PERIODIC_LON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def WXFGridStart(builder):
    builder.StartObject(8)

def Start(builder):
    WXFGridStart(builder)

def WXFGridAddKIND(builder, KIND):
    builder.PrependInt8Slot(0, KIND, 0)

def AddKIND(builder, KIND):
    WXFGridAddKIND(builder, KIND)

def WXFGridAddLAT0(builder, LAT0):
    builder.PrependFloat64Slot(1, LAT0, 0.0)

def AddLAT0(builder, LAT0):
    WXFGridAddLAT0(builder, LAT0)

def WXFGridAddLON0(builder, LON0):
    builder.PrependFloat64Slot(2, LON0, 0.0)

def AddLON0(builder, LON0):
    WXFGridAddLON0(builder, LON0)

def WXFGridAddDLAT(builder, DLAT):
    builder.PrependFloat64Slot(3, DLAT, 0.0)

def AddDLAT(builder, DLAT):
    WXFGridAddDLAT(builder, DLAT)

def WXFGridAddDLON(builder, DLON):
    builder.PrependFloat64Slot(4, DLON, 0.0)

def AddDLON(builder, DLON):
    WXFGridAddDLON(builder, DLON)

def WXFGridAddNLAT(builder, NLAT):
    builder.PrependUint32Slot(5, NLAT, 0)

def AddNLAT(builder, NLAT):
    WXFGridAddNLAT(builder, NLAT)

def WXFGridAddNLON(builder, NLON):
    builder.PrependUint32Slot(6, NLON, 0)

def AddNLON(builder, NLON):
    WXFGridAddNLON(builder, NLON)

def WXFGridAddPERIODIC_LON(builder, PERIODIC_LON):
    builder.PrependBoolSlot(7, PERIODIC_LON, 0)

def AddPERIODIC_LON(builder, PERIODIC_LON):
    WXFGridAddPERIODIC_LON(builder, PERIODIC_LON)

def WXFGridEnd(builder):
    return builder.EndObject()

def End(builder):
    return WXFGridEnd(builder)


class WXFGridT(object):

    # WXFGridT
    def __init__(
        self,
        KIND = 0,
        LAT0 = 0.0,
        LON0 = 0.0,
        DLAT = 0.0,
        DLON = 0.0,
        NLAT = 0,
        NLON = 0,
        PERIODIC_LON = False,
    ):
        self.KIND = KIND  # type: int
        self.LAT0 = LAT0  # type: float
        self.LON0 = LON0  # type: float
        self.DLAT = DLAT  # type: float
        self.DLON = DLON  # type: float
        self.NLAT = NLAT  # type: int
        self.NLON = NLON  # type: int
        self.PERIODIC_LON = PERIODIC_LON  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpWxfgrid = WXFGrid()
        tmpWxfgrid.Init(buf, pos)
        return cls.InitFromObj(tmpWxfgrid)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpWxfgrid):
        x = WXFGridT()
        x._UnPack(tmpWxfgrid)
        return x

    # WXFGridT
    def _UnPack(self, WXFGrid):
        if WXFGrid is None:
            return
        self.KIND = WXFGrid.KIND()
        self.LAT0 = WXFGrid.LAT0()
        self.LON0 = WXFGrid.LON0()
        self.DLAT = WXFGrid.DLAT()
        self.DLON = WXFGrid.DLON()
        self.NLAT = WXFGrid.NLAT()
        self.NLON = WXFGrid.NLON()
        self.PERIODIC_LON = WXFGrid.PERIODIC_LON()

    # WXFGridT
    def Pack(self, builder):
        WXFGridStart(builder)
        WXFGridAddKIND(builder, self.KIND)
        WXFGridAddLAT0(builder, self.LAT0)
        WXFGridAddLON0(builder, self.LON0)
        WXFGridAddDLAT(builder, self.DLAT)
        WXFGridAddDLON(builder, self.DLON)
        WXFGridAddNLAT(builder, self.NLAT)
        WXFGridAddNLON(builder, self.NLON)
        WXFGridAddPERIODIC_LON(builder, self.PERIODIC_LON)
        WXFGrid = WXFGridEnd(builder)
        return WXFGrid
