# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Query Encoder Model.
#
# One QEM publishes the pruned static token-embedding table a lightweight
# client (e.g. a browser) loads to encode a search query into the same
# vector space a paired $CES shard's rows live in, without shipping a full
# transformer. A consumer pools the rows of the query's tokens (per
# POOLING_METHOD) into a query vector, then dot-products it against
# CES.ROWS.
#
# A QEM is compatible with a CES only when MODEL_ID, MODEL_VERSION,
# MODEL_WEIGHTS_SHA256, POOLING_METHOD and TEMPLATE_VERSION all match; a
# mismatch on any one means the two vector spaces are not comparable.
class QEM(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = QEM()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsQEM(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def QEMBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x51\x45\x4D", size_prefixed=size_prefixed)

    # QEM
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Encoder model identifier/name. Compared verbatim against CES.MODEL_ID.
    # QEM
    def MODEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Encoder model version. Compared verbatim against CES.MODEL_VERSION.
    # QEM
    def MODEL_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SHA-256 of the encoder weights, lowercase hex. Same convention as
    # DPMAsset.BYTE_SHA256. Compared verbatim against CES.MODEL_WEIGHTS_SHA256.
    # QEM
    def MODEL_WEIGHTS_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Sentence/document pooling method the encoder uses. Compared verbatim
    # against CES.POOLING_METHOD.
    # QEM
    def POOLING_METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Version of the text template documents were rendered from before
    # encoding. Compared verbatim against CES.TEMPLATE_VERSION.
    # QEM
    def TEMPLATE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Fixed embedding dimensionality; must equal the paired CES.DIMENSIONS.
    # QEM
    def DIMENSIONS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Row element encoding used across TOKENS.
    # QEM
    def ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Row-vector normalization applied before quantization.
    # QEM
    def NORMALIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 1

    # Number of tokens in the pruned vocabulary.
    # QEM
    def VOCAB_SIZE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # One row per pruned vocabulary token.
    # QEM
    def TOKENS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from QEMTokenVector import QEMTokenVector
            obj = QEMTokenVector()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # QEM
    def TOKENSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # QEM
    def TOKENSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

def QEMStart(builder):
    builder.StartObject(10)

def Start(builder):
    QEMStart(builder)

def QEMAddMODEL_ID(builder, MODEL_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL_ID), 0)

def AddMODEL_ID(builder, MODEL_ID):
    QEMAddMODEL_ID(builder, MODEL_ID)

def QEMAddMODEL_VERSION(builder, MODEL_VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL_VERSION), 0)

def AddMODEL_VERSION(builder, MODEL_VERSION):
    QEMAddMODEL_VERSION(builder, MODEL_VERSION)

def QEMAddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL_WEIGHTS_SHA256), 0)

def AddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256):
    QEMAddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256)

def QEMAddPOOLING_METHOD(builder, POOLING_METHOD):
    builder.PrependInt8Slot(3, POOLING_METHOD, 0)

def AddPOOLING_METHOD(builder, POOLING_METHOD):
    QEMAddPOOLING_METHOD(builder, POOLING_METHOD)

def QEMAddTEMPLATE_VERSION(builder, TEMPLATE_VERSION):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(TEMPLATE_VERSION), 0)

def AddTEMPLATE_VERSION(builder, TEMPLATE_VERSION):
    QEMAddTEMPLATE_VERSION(builder, TEMPLATE_VERSION)

def QEMAddDIMENSIONS(builder, DIMENSIONS):
    builder.PrependUint32Slot(5, DIMENSIONS, 0)

def AddDIMENSIONS(builder, DIMENSIONS):
    QEMAddDIMENSIONS(builder, DIMENSIONS)

def QEMAddENCODING(builder, ENCODING):
    builder.PrependInt8Slot(6, ENCODING, 0)

def AddENCODING(builder, ENCODING):
    QEMAddENCODING(builder, ENCODING)

def QEMAddNORMALIZATION(builder, NORMALIZATION):
    builder.PrependInt8Slot(7, NORMALIZATION, 1)

def AddNORMALIZATION(builder, NORMALIZATION):
    QEMAddNORMALIZATION(builder, NORMALIZATION)

def QEMAddVOCAB_SIZE(builder, VOCAB_SIZE):
    builder.PrependUint32Slot(8, VOCAB_SIZE, 0)

def AddVOCAB_SIZE(builder, VOCAB_SIZE):
    QEMAddVOCAB_SIZE(builder, VOCAB_SIZE)

def QEMAddTOKENS(builder, TOKENS):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(TOKENS), 0)

def AddTOKENS(builder, TOKENS):
    QEMAddTOKENS(builder, TOKENS)

def QEMStartTOKENSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTOKENSVector(builder, numElems):
    return QEMStartTOKENSVector(builder, numElems)

def QEMCreateTOKENSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateTOKENSVector(builder, data):
    QEMCreateTOKENSVector(builder, data)

def QEMEnd(builder):
    return builder.EndObject()

def End(builder):
    return QEMEnd(builder)

import QEMTokenVector
try:
    from typing import List
except:
    pass

class QEMT(object):

    # QEMT
    def __init__(
        self,
        MODEL_ID = None,
        MODEL_VERSION = None,
        MODEL_WEIGHTS_SHA256 = None,
        POOLING_METHOD = 0,
        TEMPLATE_VERSION = None,
        DIMENSIONS = 0,
        ENCODING = 0,
        NORMALIZATION = 1,
        VOCAB_SIZE = 0,
        TOKENS = None,
    ):
        self.MODEL_ID = MODEL_ID  # type: Optional[str]
        self.MODEL_VERSION = MODEL_VERSION  # type: Optional[str]
        self.MODEL_WEIGHTS_SHA256 = MODEL_WEIGHTS_SHA256  # type: Optional[str]
        self.POOLING_METHOD = POOLING_METHOD  # type: int
        self.TEMPLATE_VERSION = TEMPLATE_VERSION  # type: Optional[str]
        self.DIMENSIONS = DIMENSIONS  # type: int
        self.ENCODING = ENCODING  # type: int
        self.NORMALIZATION = NORMALIZATION  # type: int
        self.VOCAB_SIZE = VOCAB_SIZE  # type: int
        self.TOKENS = TOKENS  # type: Optional[List[QEMTokenVector.QEMTokenVectorT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpQem = QEM()
        tmpQem.Init(buf, pos)
        return cls.InitFromObj(tmpQem)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpQem):
        x = QEMT()
        x._UnPack(tmpQem)
        return x

    # QEMT
    def _UnPack(self, QEM):
        if QEM is None:
            return
        self.MODEL_ID = QEM.MODEL_ID()
        self.MODEL_VERSION = QEM.MODEL_VERSION()
        self.MODEL_WEIGHTS_SHA256 = QEM.MODEL_WEIGHTS_SHA256()
        self.POOLING_METHOD = QEM.POOLING_METHOD()
        self.TEMPLATE_VERSION = QEM.TEMPLATE_VERSION()
        self.DIMENSIONS = QEM.DIMENSIONS()
        self.ENCODING = QEM.ENCODING()
        self.NORMALIZATION = QEM.NORMALIZATION()
        self.VOCAB_SIZE = QEM.VOCAB_SIZE()
        if not QEM.TOKENSIsNone():
            self.TOKENS = []
            for i in range(QEM.TOKENSLength()):
                if QEM.TOKENS(i) is None:
                    self.TOKENS.append(None)
                else:
                    qEMTokenVector_ = QEMTokenVector.QEMTokenVectorT.InitFromObj(QEM.TOKENS(i))
                    self.TOKENS.append(qEMTokenVector_)

    # QEMT
    def Pack(self, builder):
        if self.MODEL_ID is not None:
            MODEL_ID = builder.CreateString(self.MODEL_ID)
        if self.MODEL_VERSION is not None:
            MODEL_VERSION = builder.CreateString(self.MODEL_VERSION)
        if self.MODEL_WEIGHTS_SHA256 is not None:
            MODEL_WEIGHTS_SHA256 = builder.CreateString(self.MODEL_WEIGHTS_SHA256)
        if self.TEMPLATE_VERSION is not None:
            TEMPLATE_VERSION = builder.CreateString(self.TEMPLATE_VERSION)
        if self.TOKENS is not None:
            TOKENSlist = []
            for i in range(len(self.TOKENS)):
                TOKENSlist.append(self.TOKENS[i].Pack(builder))
            QEMStartTOKENSVector(builder, len(self.TOKENS))
            for i in reversed(range(len(self.TOKENS))):
                builder.PrependUOffsetTRelative(TOKENSlist[i])
            TOKENS = builder.EndVector()
        QEMStart(builder)
        if self.MODEL_ID is not None:
            QEMAddMODEL_ID(builder, MODEL_ID)
        if self.MODEL_VERSION is not None:
            QEMAddMODEL_VERSION(builder, MODEL_VERSION)
        if self.MODEL_WEIGHTS_SHA256 is not None:
            QEMAddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256)
        QEMAddPOOLING_METHOD(builder, self.POOLING_METHOD)
        if self.TEMPLATE_VERSION is not None:
            QEMAddTEMPLATE_VERSION(builder, TEMPLATE_VERSION)
        QEMAddDIMENSIONS(builder, self.DIMENSIONS)
        QEMAddENCODING(builder, self.ENCODING)
        QEMAddNORMALIZATION(builder, self.NORMALIZATION)
        QEMAddVOCAB_SIZE(builder, self.VOCAB_SIZE)
        if self.TOKENS is not None:
            QEMAddTOKENS(builder, TOKENS)
        QEM = QEMEnd(builder)
        return QEM
