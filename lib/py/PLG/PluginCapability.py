# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Plugin capability declaration
class PluginCapability(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PluginCapability()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPluginCapability(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PluginCapabilityBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # PluginCapability
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Capability name, e.g., "gpu_compute", "wasm_simd"
    # PluginCapability
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Capability version
    # PluginCapability
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Whether this capability is required
    # PluginCapability
    def REQUIRED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def PluginCapabilityStart(builder):
    builder.StartObject(3)

def Start(builder):
    PluginCapabilityStart(builder)

def PluginCapabilityAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    PluginCapabilityAddNAME(builder, NAME)

def PluginCapabilityAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    PluginCapabilityAddVERSION(builder, VERSION)

def PluginCapabilityAddREQUIRED(builder, REQUIRED):
    builder.PrependBoolSlot(2, REQUIRED, 0)

def AddREQUIRED(builder, REQUIRED):
    PluginCapabilityAddREQUIRED(builder, REQUIRED)

def PluginCapabilityEnd(builder):
    return builder.EndObject()

def End(builder):
    return PluginCapabilityEnd(builder)


class PluginCapabilityT(object):

    # PluginCapabilityT
    def __init__(self):
        self.NAME = None  # type: str
        self.VERSION = None  # type: str
        self.REQUIRED = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        pluginCapability = PluginCapability()
        pluginCapability.Init(buf, pos)
        return cls.InitFromObj(pluginCapability)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, pluginCapability):
        x = PluginCapabilityT()
        x._UnPack(pluginCapability)
        return x

    # PluginCapabilityT
    def _UnPack(self, pluginCapability):
        if pluginCapability is None:
            return
        self.NAME = pluginCapability.NAME()
        self.VERSION = pluginCapability.VERSION()
        self.REQUIRED = pluginCapability.REQUIRED()

    # PluginCapabilityT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        PluginCapabilityStart(builder)
        if self.NAME is not None:
            PluginCapabilityAddNAME(builder, NAME)
        if self.VERSION is not None:
            PluginCapabilityAddVERSION(builder, VERSION)
        PluginCapabilityAddREQUIRED(builder, self.REQUIRED)
        pluginCapability = PluginCapabilityEnd(builder)
        return pluginCapability
