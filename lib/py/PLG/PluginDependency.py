# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Plugin dependency on another plugin
class PluginDependency(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PluginDependency()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPluginDependency(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PluginDependencyBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # PluginDependency
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Plugin ID of the dependency
    # PluginDependency
    def PLUGIN_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Minimum version required (semver)
    # PluginDependency
    def MIN_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Maximum version allowed (optional)
    # PluginDependency
    def MAX_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PluginDependencyStart(builder):
    builder.StartObject(3)

def Start(builder):
    PluginDependencyStart(builder)

def PluginDependencyAddPLUGIN_ID(builder, PLUGIN_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PLUGIN_ID), 0)

def AddPLUGIN_ID(builder, PLUGIN_ID):
    PluginDependencyAddPLUGIN_ID(builder, PLUGIN_ID)

def PluginDependencyAddMIN_VERSION(builder, MIN_VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(MIN_VERSION), 0)

def AddMIN_VERSION(builder, MIN_VERSION):
    PluginDependencyAddMIN_VERSION(builder, MIN_VERSION)

def PluginDependencyAddMAX_VERSION(builder, MAX_VERSION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MAX_VERSION), 0)

def AddMAX_VERSION(builder, MAX_VERSION):
    PluginDependencyAddMAX_VERSION(builder, MAX_VERSION)

def PluginDependencyEnd(builder):
    return builder.EndObject()

def End(builder):
    return PluginDependencyEnd(builder)


class PluginDependencyT(object):

    # PluginDependencyT
    def __init__(self):
        self.PLUGIN_ID = None  # type: str
        self.MIN_VERSION = None  # type: str
        self.MAX_VERSION = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        pluginDependency = PluginDependency()
        pluginDependency.Init(buf, pos)
        return cls.InitFromObj(pluginDependency)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, pluginDependency):
        x = PluginDependencyT()
        x._UnPack(pluginDependency)
        return x

    # PluginDependencyT
    def _UnPack(self, pluginDependency):
        if pluginDependency is None:
            return
        self.PLUGIN_ID = pluginDependency.PLUGIN_ID()
        self.MIN_VERSION = pluginDependency.MIN_VERSION()
        self.MAX_VERSION = pluginDependency.MAX_VERSION()

    # PluginDependencyT
    def Pack(self, builder):
        if self.PLUGIN_ID is not None:
            PLUGIN_ID = builder.CreateString(self.PLUGIN_ID)
        if self.MIN_VERSION is not None:
            MIN_VERSION = builder.CreateString(self.MIN_VERSION)
        if self.MAX_VERSION is not None:
            MAX_VERSION = builder.CreateString(self.MAX_VERSION)
        PluginDependencyStart(builder)
        if self.PLUGIN_ID is not None:
            PluginDependencyAddPLUGIN_ID(builder, PLUGIN_ID)
        if self.MIN_VERSION is not None:
            PluginDependencyAddMIN_VERSION(builder, MIN_VERSION)
        if self.MAX_VERSION is not None:
            PluginDependencyAddMAX_VERSION(builder, MAX_VERSION)
        pluginDependency = PluginDependencyEnd(builder)
        return pluginDependency
