# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Accepted schema family for a port. When a port accepts multiple wire
# formats (canonical FlatBuffer + aligned-binary), each ALLOWED_TYPE
# entry carries its own TAB.FlatBufferTypeRef with the schema identity,
# and the enclosing PLGPortManifest advertises both wire formats via
# ALLOWED_WIRE_FORMATS. Per SDK contract: a port that advertises
# aligned-binary MUST also advertise the canonical flatbuffer fallback
# for the same schema and file identifier in the same set.
class PLGAcceptedTypeSet(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PLGAcceptedTypeSet()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPLGAcceptedTypeSet(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PLGAcceptedTypeSetBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # PLGAcceptedTypeSet
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable type-set identifier within the port.
    # PLGAcceptedTypeSet
    def SET_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Specific FlatBuffer types accepted by the set.
    # PLGAcceptedTypeSet
    def ALLOWED_TYPES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FlatBufferTypeRef import FlatBufferTypeRef
            obj = FlatBufferTypeRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PLGAcceptedTypeSet
    def ALLOWED_TYPESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PLGAcceptedTypeSet
    def ALLOWED_TYPESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Wire formats this set accepts. If ALIGNED_BINARY is present,
    # FLATBUFFER MUST also be present for the same schemas.
    # PLGAcceptedTypeSet
    def ALLOWED_WIRE_FORMATS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # PLGAcceptedTypeSet
    def ALLOWED_WIRE_FORMATSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # PLGAcceptedTypeSet
    def ALLOWED_WIRE_FORMATSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PLGAcceptedTypeSet
    def ALLOWED_WIRE_FORMATSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Human-readable explanation of the accepted schema family.
    # PLGAcceptedTypeSet
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PLGAcceptedTypeSetStart(builder):
    builder.StartObject(4)

def Start(builder):
    PLGAcceptedTypeSetStart(builder)

def PLGAcceptedTypeSetAddSET_ID(builder, SET_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SET_ID), 0)

def AddSET_ID(builder, SET_ID):
    PLGAcceptedTypeSetAddSET_ID(builder, SET_ID)

def PLGAcceptedTypeSetAddALLOWED_TYPES(builder, ALLOWED_TYPES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ALLOWED_TYPES), 0)

def AddALLOWED_TYPES(builder, ALLOWED_TYPES):
    PLGAcceptedTypeSetAddALLOWED_TYPES(builder, ALLOWED_TYPES)

def PLGAcceptedTypeSetStartALLOWED_TYPESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartALLOWED_TYPESVector(builder, numElems):
    return PLGAcceptedTypeSetStartALLOWED_TYPESVector(builder, numElems)

def PLGAcceptedTypeSetCreateALLOWED_TYPESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateALLOWED_TYPESVector(builder, data):
    PLGAcceptedTypeSetCreateALLOWED_TYPESVector(builder, data)

def PLGAcceptedTypeSetAddALLOWED_WIRE_FORMATS(builder, ALLOWED_WIRE_FORMATS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ALLOWED_WIRE_FORMATS), 0)

def AddALLOWED_WIRE_FORMATS(builder, ALLOWED_WIRE_FORMATS):
    PLGAcceptedTypeSetAddALLOWED_WIRE_FORMATS(builder, ALLOWED_WIRE_FORMATS)

def PLGAcceptedTypeSetStartALLOWED_WIRE_FORMATSVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartALLOWED_WIRE_FORMATSVector(builder, numElems):
    return PLGAcceptedTypeSetStartALLOWED_WIRE_FORMATSVector(builder, numElems)

def PLGAcceptedTypeSetCreateALLOWED_WIRE_FORMATSVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateALLOWED_WIRE_FORMATSVector(builder, data):
    PLGAcceptedTypeSetCreateALLOWED_WIRE_FORMATSVector(builder, data)

def PLGAcceptedTypeSetAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    PLGAcceptedTypeSetAddDESCRIPTION(builder, DESCRIPTION)

def PLGAcceptedTypeSetEnd(builder):
    return builder.EndObject()

def End(builder):
    return PLGAcceptedTypeSetEnd(builder)

import FlatBufferTypeRef
try:
    from typing import List
except:
    pass

class PLGAcceptedTypeSetT(object):

    # PLGAcceptedTypeSetT
    def __init__(
        self,
        SET_ID = None,
        ALLOWED_TYPES = None,
        ALLOWED_WIRE_FORMATS = None,
        DESCRIPTION = None,
    ):
        self.SET_ID = SET_ID  # type: Optional[str]
        self.ALLOWED_TYPES = ALLOWED_TYPES  # type: Optional[List[FlatBufferTypeRef.FlatBufferTypeRefT]]
        self.ALLOWED_WIRE_FORMATS = ALLOWED_WIRE_FORMATS  # type: Optional[List[int]]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPlgacceptedTypeSet = PLGAcceptedTypeSet()
        tmpPlgacceptedTypeSet.Init(buf, pos)
        return cls.InitFromObj(tmpPlgacceptedTypeSet)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPlgacceptedTypeSet):
        x = PLGAcceptedTypeSetT()
        x._UnPack(tmpPlgacceptedTypeSet)
        return x

    # PLGAcceptedTypeSetT
    def _UnPack(self, PLGAcceptedTypeSet):
        if PLGAcceptedTypeSet is None:
            return
        self.SET_ID = PLGAcceptedTypeSet.SET_ID()
        if not PLGAcceptedTypeSet.ALLOWED_TYPESIsNone():
            self.ALLOWED_TYPES = []
            for i in range(PLGAcceptedTypeSet.ALLOWED_TYPESLength()):
                if PLGAcceptedTypeSet.ALLOWED_TYPES(i) is None:
                    self.ALLOWED_TYPES.append(None)
                else:
                    flatBufferTypeRef_ = FlatBufferTypeRef.FlatBufferTypeRefT.InitFromObj(PLGAcceptedTypeSet.ALLOWED_TYPES(i))
                    self.ALLOWED_TYPES.append(flatBufferTypeRef_)
        if not PLGAcceptedTypeSet.ALLOWED_WIRE_FORMATSIsNone():
            if np is None:
                self.ALLOWED_WIRE_FORMATS = []
                for i in range(PLGAcceptedTypeSet.ALLOWED_WIRE_FORMATSLength()):
                    self.ALLOWED_WIRE_FORMATS.append(PLGAcceptedTypeSet.ALLOWED_WIRE_FORMATS(i))
            else:
                self.ALLOWED_WIRE_FORMATS = PLGAcceptedTypeSet.ALLOWED_WIRE_FORMATSAsNumpy()
        self.DESCRIPTION = PLGAcceptedTypeSet.DESCRIPTION()

    # PLGAcceptedTypeSetT
    def Pack(self, builder):
        if self.SET_ID is not None:
            SET_ID = builder.CreateString(self.SET_ID)
        if self.ALLOWED_TYPES is not None:
            ALLOWED_TYPESlist = []
            for i in range(len(self.ALLOWED_TYPES)):
                ALLOWED_TYPESlist.append(self.ALLOWED_TYPES[i].Pack(builder))
            PLGAcceptedTypeSetStartALLOWED_TYPESVector(builder, len(self.ALLOWED_TYPES))
            for i in reversed(range(len(self.ALLOWED_TYPES))):
                builder.PrependUOffsetTRelative(ALLOWED_TYPESlist[i])
            ALLOWED_TYPES = builder.EndVector()
        if self.ALLOWED_WIRE_FORMATS is not None:
            if np is not None and type(self.ALLOWED_WIRE_FORMATS) is np.ndarray:
                ALLOWED_WIRE_FORMATS = builder.CreateNumpyVector(self.ALLOWED_WIRE_FORMATS)
            else:
                PLGAcceptedTypeSetStartALLOWED_WIRE_FORMATSVector(builder, len(self.ALLOWED_WIRE_FORMATS))
                for i in reversed(range(len(self.ALLOWED_WIRE_FORMATS))):
                    builder.PrependUint8(self.ALLOWED_WIRE_FORMATS[i])
                ALLOWED_WIRE_FORMATS = builder.EndVector()
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        PLGAcceptedTypeSetStart(builder)
        if self.SET_ID is not None:
            PLGAcceptedTypeSetAddSET_ID(builder, SET_ID)
        if self.ALLOWED_TYPES is not None:
            PLGAcceptedTypeSetAddALLOWED_TYPES(builder, ALLOWED_TYPES)
        if self.ALLOWED_WIRE_FORMATS is not None:
            PLGAcceptedTypeSetAddALLOWED_WIRE_FORMATS(builder, ALLOWED_WIRE_FORMATS)
        if self.DESCRIPTION is not None:
            PLGAcceptedTypeSetAddDESCRIPTION(builder, DESCRIPTION)
        PLGAcceptedTypeSet = PLGAcceptedTypeSetEnd(builder)
        return PLGAcceptedTypeSet
