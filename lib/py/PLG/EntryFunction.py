# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Plugin entry point function definition
class EntryFunction(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EntryFunction()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEntryFunction(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def EntryFunctionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # EntryFunction
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Function name as exported from WASM
    # EntryFunction
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable description
    # EntryFunction
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Input parameter types (FlatBuffer schema names)
    # EntryFunction
    def INPUT_SCHEMAS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # EntryFunction
    def INPUT_SCHEMASLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EntryFunction
    def INPUT_SCHEMASIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Output type (FlatBuffer schema name)
    # EntryFunction
    def OUTPUT_SCHEMA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def EntryFunctionStart(builder):
    builder.StartObject(4)

def Start(builder):
    EntryFunctionStart(builder)

def EntryFunctionAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    EntryFunctionAddNAME(builder, NAME)

def EntryFunctionAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    EntryFunctionAddDESCRIPTION(builder, DESCRIPTION)

def EntryFunctionAddINPUT_SCHEMAS(builder, INPUT_SCHEMAS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(INPUT_SCHEMAS), 0)

def AddINPUT_SCHEMAS(builder, INPUT_SCHEMAS):
    EntryFunctionAddINPUT_SCHEMAS(builder, INPUT_SCHEMAS)

def EntryFunctionStartINPUT_SCHEMASVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartINPUT_SCHEMASVector(builder, numElems):
    return EntryFunctionStartINPUT_SCHEMASVector(builder, numElems)

def EntryFunctionAddOUTPUT_SCHEMA(builder, OUTPUT_SCHEMA):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(OUTPUT_SCHEMA), 0)

def AddOUTPUT_SCHEMA(builder, OUTPUT_SCHEMA):
    EntryFunctionAddOUTPUT_SCHEMA(builder, OUTPUT_SCHEMA)

def EntryFunctionEnd(builder):
    return builder.EndObject()

def End(builder):
    return EntryFunctionEnd(builder)

try:
    from typing import List
except:
    pass

class EntryFunctionT(object):

    # EntryFunctionT
    def __init__(self):
        self.NAME = None  # type: str
        self.DESCRIPTION = None  # type: str
        self.INPUT_SCHEMAS = None  # type: List[str]
        self.OUTPUT_SCHEMA = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        entryFunction = EntryFunction()
        entryFunction.Init(buf, pos)
        return cls.InitFromObj(entryFunction)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, entryFunction):
        x = EntryFunctionT()
        x._UnPack(entryFunction)
        return x

    # EntryFunctionT
    def _UnPack(self, entryFunction):
        if entryFunction is None:
            return
        self.NAME = entryFunction.NAME()
        self.DESCRIPTION = entryFunction.DESCRIPTION()
        if not entryFunction.INPUT_SCHEMASIsNone():
            self.INPUT_SCHEMAS = []
            for i in range(entryFunction.INPUT_SCHEMASLength()):
                self.INPUT_SCHEMAS.append(entryFunction.INPUT_SCHEMAS(i))
        self.OUTPUT_SCHEMA = entryFunction.OUTPUT_SCHEMA()

    # EntryFunctionT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.INPUT_SCHEMAS is not None:
            INPUT_SCHEMASlist = []
            for i in range(len(self.INPUT_SCHEMAS)):
                INPUT_SCHEMASlist.append(builder.CreateString(self.INPUT_SCHEMAS[i]))
            EntryFunctionStartINPUT_SCHEMASVector(builder, len(self.INPUT_SCHEMAS))
            for i in reversed(range(len(self.INPUT_SCHEMAS))):
                builder.PrependUOffsetTRelative(INPUT_SCHEMASlist[i])
            INPUT_SCHEMAS = builder.EndVector()
        if self.OUTPUT_SCHEMA is not None:
            OUTPUT_SCHEMA = builder.CreateString(self.OUTPUT_SCHEMA)
        EntryFunctionStart(builder)
        if self.NAME is not None:
            EntryFunctionAddNAME(builder, NAME)
        if self.DESCRIPTION is not None:
            EntryFunctionAddDESCRIPTION(builder, DESCRIPTION)
        if self.INPUT_SCHEMAS is not None:
            EntryFunctionAddINPUT_SCHEMAS(builder, INPUT_SCHEMAS)
        if self.OUTPUT_SCHEMA is not None:
            EntryFunctionAddOUTPUT_SCHEMA(builder, OUTPUT_SCHEMA)
        entryFunction = EntryFunctionEnd(builder)
        return entryFunction
