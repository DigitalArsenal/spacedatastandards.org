# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Sky Tile: one HEALPix tile of an all-sky point-source or diffuse-map layer.
class SKT(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SKT()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSKT(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SKTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x4B\x54", size_prefixed=size_prefixed)

    # SKT
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier of the layer: equal across every tile of one
    # (producer, source catalogue or survey, band, place, epoch).
    # SKT
    def LAYER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Frame the HEALPix grid and the point positions are stated in.
    # SKT
    def FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 1

    # HEALPix order; NSIDE = 2^ORDER.
    # SKT
    def ORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Nested pixel index at ORDER.
    # SKT
    def PIXEL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Deepest order of the layer, when the producer knows it; 255 when
    # unstated.
    # SKT
    def MAX_ORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 255

    # Which of the four children exist (bit i for child 4 * PIXEL + i). 0 on
    # a leaf.
    # SKT
    def CHILD_MASK(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Payload the tile carries.
    # SKT
    def PAYLOAD_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # How the tile relates to its ancestors and children.
    # SKT
    def REFINE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Coarse spectral region of the band.
    # SKT
    def SPECTRAL_REGIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Band identifier; resolves to a `$PHB` BAND_ID. MAG and map values are
    # in this band.
    # SKT
    def BAND_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Effective wavelength of the band, nanometres (vacuum).
    # SKT
    def WAVELENGTH_EFF_NM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return float('nan')

    # Short edge of a band-integrated map, nanometres.
    # SKT
    def WAVELENGTH_MIN_NM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return float('nan')

    # Long edge of a band-integrated map, nanometres.
    # SKT
    def WAVELENGTH_MAX_NM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return float('nan')

    # Calibration system of MAG.
    # SKT
    def MAGNITUDE_SYSTEM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Which place point positions are.
    # SKT
    def PLACE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 1

    # Epoch of point positions, Julian years (TT).
    # SKT
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return float('nan')

    # Observer an ASTROMETRIC or APPARENT tile was computed for (a site, a
    # spacecraft or the geocentre), as the producer names it.
    # SKT
    def OBSERVER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of points in the payload.
    # SKT
    def POINT_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Brightest magnitude in the tile.
    # SKT
    def MAG_BRIGHTEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return float('nan')

    # Faintest magnitude in the tile; with the ancestors, the layer is
    # complete to this limit inside the pixel.
    # SKT
    def MAG_LIMIT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return float('nan')

    # What each point's COLOR holds.
    # SKT
    def COLOR_ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Bluer band of a COLOR_INDEX colour.
    # SKT
    def COLOR_BLUE_BAND_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Redder band of a COLOR_INDEX colour.
    # SKT
    def COLOR_RED_BAND_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Packed points, brightest first.
    # SKT
    def POINTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 24
            from SKTPoint import SKTPoint
            obj = SKTPoint()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SKT
    def POINTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SKT
    def POINTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        return o == 0

    # Content identifier of the full `$STR` / `$CSO` records of the points,
    # a size-prefixed record sequence in ROW order.
    # SKT
    def ROW_COLLECTION_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Order offset of the map pixels: the tile holds 4^MAP_DEPTH values.
    # SKT
    def MAP_DEPTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(54))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Numeric encoding of MAP_VALUES.
    # SKT
    def MAP_ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(56))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Physical quantity of the map.
    # SKT
    def MAP_QUANTITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(58))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Producer's name for the quantity when MAP_QUANTITY is OTHER.
    # SKT
    def MAP_QUANTITY_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(60))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Units of the physical map values, UCUM style (e.g. "K", "MJy/sr",
    # "count/s/sr").
    # SKT
    def MAP_UNITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(62))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Scale of a scaled integer encoding.
    # SKT
    def MAP_SCALE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(64))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1.0

    # Offset of a scaled integer encoding.
    # SKT
    def MAP_OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(66))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Raw value marking a pixel with no data in a scaled integer encoding.
    # SKT
    def MAP_BLANK(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(68))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 4294967295

    # Raw map bytes, 4^MAP_DEPTH values in nested order.
    # SKT
    def MAP_VALUES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(70))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SKT
    def MAP_VALUESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(70))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SKT
    def MAP_VALUESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(70))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SKT
    def MAP_VALUESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(70))
        return o == 0

    # Recommended stretch.
    # SKT
    def STRETCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(72))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Physical value drawn black.
    # SKT
    def BLACK_POINT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(74))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return float('nan')

    # Physical value drawn white.
    # SKT
    def WHITE_POINT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(76))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return float('nan')

    # Softening of an ASINH stretch, in physical units.
    # SKT
    def STRETCH_SOFTENING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(78))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return float('nan')

    # Percentile levels of the layer's value distribution, in [0, 100],
    # ascending.
    # SKT
    def PERCENTILE_LEVELS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(80))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # SKT
    def PERCENTILE_LEVELSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(80))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # SKT
    def PERCENTILE_LEVELSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(80))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SKT
    def PERCENTILE_LEVELSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(80))
        return o == 0

    # Physical value at each PERCENTILE_LEVELS entry. Computed over the whole
    # layer, so every tile stretches alike.
    # SKT
    def PERCENTILE_VALUES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(82))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SKT
    def PERCENTILE_VALUESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(82))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SKT
    def PERCENTILE_VALUESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(82))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SKT
    def PERCENTILE_VALUESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(82))
        return o == 0

    # Content identifier of the payload when POINTS and MAP_VALUES are absent.
    # SKT
    def PAYLOAD_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(84))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Length of the payload at PAYLOAD_CID, bytes.
    # SKT
    def PAYLOAD_BYTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(86))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Start of the observations the tile summarises, ISO 8601 UTC.
    # SKT
    def OBSERVATION_START(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(88))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # End of the observations the tile summarises, ISO 8601 UTC.
    # SKT
    def OBSERVATION_END(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(90))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Verbatim names of the catalogues or surveys the tile was derived from.
    # SKT
    def SOURCE_NAMES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(92))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # SKT
    def SOURCE_NAMESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(92))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SKT
    def SOURCE_NAMESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(92))
        return o == 0

    # Peer identifier of the node that produced the tile.
    # SKT
    def PRODUCER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(94))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Producer software and version.
    # SKT
    def PRODUCER_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(96))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Time the tile was produced, ISO 8601 UTC.
    # SKT
    def PRODUCED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(98))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Citation the upstream publishers ask for.
    # SKT
    def CITATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(100))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # URL of the licence or terms of the source data.
    # SKT
    def LICENSE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(102))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def SKTStart(builder):
    builder.StartObject(50)

def Start(builder):
    SKTStart(builder)

def SKTAddLAYER_ID(builder, LAYER_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(LAYER_ID), 0)

def AddLAYER_ID(builder, LAYER_ID):
    SKTAddLAYER_ID(builder, LAYER_ID)

def SKTAddFRAME(builder, FRAME):
    builder.PrependUint8Slot(1, FRAME, 1)

def AddFRAME(builder, FRAME):
    SKTAddFRAME(builder, FRAME)

def SKTAddORDER(builder, ORDER):
    builder.PrependUint8Slot(2, ORDER, 0)

def AddORDER(builder, ORDER):
    SKTAddORDER(builder, ORDER)

def SKTAddPIXEL(builder, PIXEL):
    builder.PrependUint64Slot(3, PIXEL, 0)

def AddPIXEL(builder, PIXEL):
    SKTAddPIXEL(builder, PIXEL)

def SKTAddMAX_ORDER(builder, MAX_ORDER):
    builder.PrependUint8Slot(4, MAX_ORDER, 255)

def AddMAX_ORDER(builder, MAX_ORDER):
    SKTAddMAX_ORDER(builder, MAX_ORDER)

def SKTAddCHILD_MASK(builder, CHILD_MASK):
    builder.PrependUint8Slot(5, CHILD_MASK, 0)

def AddCHILD_MASK(builder, CHILD_MASK):
    SKTAddCHILD_MASK(builder, CHILD_MASK)

def SKTAddPAYLOAD_KIND(builder, PAYLOAD_KIND):
    builder.PrependUint8Slot(6, PAYLOAD_KIND, 0)

def AddPAYLOAD_KIND(builder, PAYLOAD_KIND):
    SKTAddPAYLOAD_KIND(builder, PAYLOAD_KIND)

def SKTAddREFINE(builder, REFINE):
    builder.PrependUint8Slot(7, REFINE, 0)

def AddREFINE(builder, REFINE):
    SKTAddREFINE(builder, REFINE)

def SKTAddSPECTRAL_REGIME(builder, SPECTRAL_REGIME):
    builder.PrependUint8Slot(8, SPECTRAL_REGIME, 0)

def AddSPECTRAL_REGIME(builder, SPECTRAL_REGIME):
    SKTAddSPECTRAL_REGIME(builder, SPECTRAL_REGIME)

def SKTAddBAND_ID(builder, BAND_ID):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(BAND_ID), 0)

def AddBAND_ID(builder, BAND_ID):
    SKTAddBAND_ID(builder, BAND_ID)

def SKTAddWAVELENGTH_EFF_NM(builder, WAVELENGTH_EFF_NM):
    builder.PrependFloat64Slot(10, WAVELENGTH_EFF_NM, float('nan'))

def AddWAVELENGTH_EFF_NM(builder, WAVELENGTH_EFF_NM):
    SKTAddWAVELENGTH_EFF_NM(builder, WAVELENGTH_EFF_NM)

def SKTAddWAVELENGTH_MIN_NM(builder, WAVELENGTH_MIN_NM):
    builder.PrependFloat64Slot(11, WAVELENGTH_MIN_NM, float('nan'))

def AddWAVELENGTH_MIN_NM(builder, WAVELENGTH_MIN_NM):
    SKTAddWAVELENGTH_MIN_NM(builder, WAVELENGTH_MIN_NM)

def SKTAddWAVELENGTH_MAX_NM(builder, WAVELENGTH_MAX_NM):
    builder.PrependFloat64Slot(12, WAVELENGTH_MAX_NM, float('nan'))

def AddWAVELENGTH_MAX_NM(builder, WAVELENGTH_MAX_NM):
    SKTAddWAVELENGTH_MAX_NM(builder, WAVELENGTH_MAX_NM)

def SKTAddMAGNITUDE_SYSTEM(builder, MAGNITUDE_SYSTEM):
    builder.PrependUint8Slot(13, MAGNITUDE_SYSTEM, 0)

def AddMAGNITUDE_SYSTEM(builder, MAGNITUDE_SYSTEM):
    SKTAddMAGNITUDE_SYSTEM(builder, MAGNITUDE_SYSTEM)

def SKTAddPLACE(builder, PLACE):
    builder.PrependUint8Slot(14, PLACE, 1)

def AddPLACE(builder, PLACE):
    SKTAddPLACE(builder, PLACE)

def SKTAddEPOCH(builder, EPOCH):
    builder.PrependFloat64Slot(15, EPOCH, float('nan'))

def AddEPOCH(builder, EPOCH):
    SKTAddEPOCH(builder, EPOCH)

def SKTAddOBSERVER_ID(builder, OBSERVER_ID):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(OBSERVER_ID), 0)

def AddOBSERVER_ID(builder, OBSERVER_ID):
    SKTAddOBSERVER_ID(builder, OBSERVER_ID)

def SKTAddPOINT_COUNT(builder, POINT_COUNT):
    builder.PrependUint32Slot(17, POINT_COUNT, 0)

def AddPOINT_COUNT(builder, POINT_COUNT):
    SKTAddPOINT_COUNT(builder, POINT_COUNT)

def SKTAddMAG_BRIGHTEST(builder, MAG_BRIGHTEST):
    builder.PrependFloat32Slot(18, MAG_BRIGHTEST, float('nan'))

def AddMAG_BRIGHTEST(builder, MAG_BRIGHTEST):
    SKTAddMAG_BRIGHTEST(builder, MAG_BRIGHTEST)

def SKTAddMAG_LIMIT(builder, MAG_LIMIT):
    builder.PrependFloat32Slot(19, MAG_LIMIT, float('nan'))

def AddMAG_LIMIT(builder, MAG_LIMIT):
    SKTAddMAG_LIMIT(builder, MAG_LIMIT)

def SKTAddCOLOR_ENCODING(builder, COLOR_ENCODING):
    builder.PrependUint8Slot(20, COLOR_ENCODING, 0)

def AddCOLOR_ENCODING(builder, COLOR_ENCODING):
    SKTAddCOLOR_ENCODING(builder, COLOR_ENCODING)

def SKTAddCOLOR_BLUE_BAND_ID(builder, COLOR_BLUE_BAND_ID):
    builder.PrependUOffsetTRelativeSlot(21, flatbuffers.number_types.UOffsetTFlags.py_type(COLOR_BLUE_BAND_ID), 0)

def AddCOLOR_BLUE_BAND_ID(builder, COLOR_BLUE_BAND_ID):
    SKTAddCOLOR_BLUE_BAND_ID(builder, COLOR_BLUE_BAND_ID)

def SKTAddCOLOR_RED_BAND_ID(builder, COLOR_RED_BAND_ID):
    builder.PrependUOffsetTRelativeSlot(22, flatbuffers.number_types.UOffsetTFlags.py_type(COLOR_RED_BAND_ID), 0)

def AddCOLOR_RED_BAND_ID(builder, COLOR_RED_BAND_ID):
    SKTAddCOLOR_RED_BAND_ID(builder, COLOR_RED_BAND_ID)

def SKTAddPOINTS(builder, POINTS):
    builder.PrependUOffsetTRelativeSlot(23, flatbuffers.number_types.UOffsetTFlags.py_type(POINTS), 0)

def AddPOINTS(builder, POINTS):
    SKTAddPOINTS(builder, POINTS)

def SKTStartPOINTSVector(builder, numElems):
    return builder.StartVector(24, numElems, 4)

def StartPOINTSVector(builder, numElems):
    return SKTStartPOINTSVector(builder, numElems)

def SKTCreatePOINTSVector(builder, data):
    data = list(data)
    builder.StartVector(24, len(data), 4)
    for item in reversed(data):
        item.Pack(builder)
    return builder.EndVector()

def CreatePOINTSVector(builder, data):
    SKTCreatePOINTSVector(builder, data)

def SKTAddROW_COLLECTION_CID(builder, ROW_COLLECTION_CID):
    builder.PrependUOffsetTRelativeSlot(24, flatbuffers.number_types.UOffsetTFlags.py_type(ROW_COLLECTION_CID), 0)

def AddROW_COLLECTION_CID(builder, ROW_COLLECTION_CID):
    SKTAddROW_COLLECTION_CID(builder, ROW_COLLECTION_CID)

def SKTAddMAP_DEPTH(builder, MAP_DEPTH):
    builder.PrependUint8Slot(25, MAP_DEPTH, 0)

def AddMAP_DEPTH(builder, MAP_DEPTH):
    SKTAddMAP_DEPTH(builder, MAP_DEPTH)

def SKTAddMAP_ENCODING(builder, MAP_ENCODING):
    builder.PrependUint8Slot(26, MAP_ENCODING, 0)

def AddMAP_ENCODING(builder, MAP_ENCODING):
    SKTAddMAP_ENCODING(builder, MAP_ENCODING)

def SKTAddMAP_QUANTITY(builder, MAP_QUANTITY):
    builder.PrependUint8Slot(27, MAP_QUANTITY, 0)

def AddMAP_QUANTITY(builder, MAP_QUANTITY):
    SKTAddMAP_QUANTITY(builder, MAP_QUANTITY)

def SKTAddMAP_QUANTITY_NAME(builder, MAP_QUANTITY_NAME):
    builder.PrependUOffsetTRelativeSlot(28, flatbuffers.number_types.UOffsetTFlags.py_type(MAP_QUANTITY_NAME), 0)

def AddMAP_QUANTITY_NAME(builder, MAP_QUANTITY_NAME):
    SKTAddMAP_QUANTITY_NAME(builder, MAP_QUANTITY_NAME)

def SKTAddMAP_UNITS(builder, MAP_UNITS):
    builder.PrependUOffsetTRelativeSlot(29, flatbuffers.number_types.UOffsetTFlags.py_type(MAP_UNITS), 0)

def AddMAP_UNITS(builder, MAP_UNITS):
    SKTAddMAP_UNITS(builder, MAP_UNITS)

def SKTAddMAP_SCALE(builder, MAP_SCALE):
    builder.PrependFloat64Slot(30, MAP_SCALE, 1.0)

def AddMAP_SCALE(builder, MAP_SCALE):
    SKTAddMAP_SCALE(builder, MAP_SCALE)

def SKTAddMAP_OFFSET(builder, MAP_OFFSET):
    builder.PrependFloat64Slot(31, MAP_OFFSET, 0.0)

def AddMAP_OFFSET(builder, MAP_OFFSET):
    SKTAddMAP_OFFSET(builder, MAP_OFFSET)

def SKTAddMAP_BLANK(builder, MAP_BLANK):
    builder.PrependUint32Slot(32, MAP_BLANK, 4294967295)

def AddMAP_BLANK(builder, MAP_BLANK):
    SKTAddMAP_BLANK(builder, MAP_BLANK)

def SKTAddMAP_VALUES(builder, MAP_VALUES):
    builder.PrependUOffsetTRelativeSlot(33, flatbuffers.number_types.UOffsetTFlags.py_type(MAP_VALUES), 0)

def AddMAP_VALUES(builder, MAP_VALUES):
    SKTAddMAP_VALUES(builder, MAP_VALUES)

def SKTStartMAP_VALUESVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartMAP_VALUESVector(builder, numElems):
    return SKTStartMAP_VALUESVector(builder, numElems)

def SKTCreateMAP_VALUESVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateMAP_VALUESVector(builder, data):
    SKTCreateMAP_VALUESVector(builder, data)

def SKTAddSTRETCH(builder, STRETCH):
    builder.PrependUint8Slot(34, STRETCH, 0)

def AddSTRETCH(builder, STRETCH):
    SKTAddSTRETCH(builder, STRETCH)

def SKTAddBLACK_POINT(builder, BLACK_POINT):
    builder.PrependFloat64Slot(35, BLACK_POINT, float('nan'))

def AddBLACK_POINT(builder, BLACK_POINT):
    SKTAddBLACK_POINT(builder, BLACK_POINT)

def SKTAddWHITE_POINT(builder, WHITE_POINT):
    builder.PrependFloat64Slot(36, WHITE_POINT, float('nan'))

def AddWHITE_POINT(builder, WHITE_POINT):
    SKTAddWHITE_POINT(builder, WHITE_POINT)

def SKTAddSTRETCH_SOFTENING(builder, STRETCH_SOFTENING):
    builder.PrependFloat64Slot(37, STRETCH_SOFTENING, float('nan'))

def AddSTRETCH_SOFTENING(builder, STRETCH_SOFTENING):
    SKTAddSTRETCH_SOFTENING(builder, STRETCH_SOFTENING)

def SKTAddPERCENTILE_LEVELS(builder, PERCENTILE_LEVELS):
    builder.PrependUOffsetTRelativeSlot(38, flatbuffers.number_types.UOffsetTFlags.py_type(PERCENTILE_LEVELS), 0)

def AddPERCENTILE_LEVELS(builder, PERCENTILE_LEVELS):
    SKTAddPERCENTILE_LEVELS(builder, PERCENTILE_LEVELS)

def SKTStartPERCENTILE_LEVELSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPERCENTILE_LEVELSVector(builder, numElems):
    return SKTStartPERCENTILE_LEVELSVector(builder, numElems)

def SKTCreatePERCENTILE_LEVELSVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreatePERCENTILE_LEVELSVector(builder, data):
    SKTCreatePERCENTILE_LEVELSVector(builder, data)

def SKTAddPERCENTILE_VALUES(builder, PERCENTILE_VALUES):
    builder.PrependUOffsetTRelativeSlot(39, flatbuffers.number_types.UOffsetTFlags.py_type(PERCENTILE_VALUES), 0)

def AddPERCENTILE_VALUES(builder, PERCENTILE_VALUES):
    SKTAddPERCENTILE_VALUES(builder, PERCENTILE_VALUES)

def SKTStartPERCENTILE_VALUESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartPERCENTILE_VALUESVector(builder, numElems):
    return SKTStartPERCENTILE_VALUESVector(builder, numElems)

def SKTCreatePERCENTILE_VALUESVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreatePERCENTILE_VALUESVector(builder, data):
    SKTCreatePERCENTILE_VALUESVector(builder, data)

def SKTAddPAYLOAD_CID(builder, PAYLOAD_CID):
    builder.PrependUOffsetTRelativeSlot(40, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD_CID), 0)

def AddPAYLOAD_CID(builder, PAYLOAD_CID):
    SKTAddPAYLOAD_CID(builder, PAYLOAD_CID)

def SKTAddPAYLOAD_BYTES(builder, PAYLOAD_BYTES):
    builder.PrependUint64Slot(41, PAYLOAD_BYTES, 0)

def AddPAYLOAD_BYTES(builder, PAYLOAD_BYTES):
    SKTAddPAYLOAD_BYTES(builder, PAYLOAD_BYTES)

def SKTAddOBSERVATION_START(builder, OBSERVATION_START):
    builder.PrependUOffsetTRelativeSlot(42, flatbuffers.number_types.UOffsetTFlags.py_type(OBSERVATION_START), 0)

def AddOBSERVATION_START(builder, OBSERVATION_START):
    SKTAddOBSERVATION_START(builder, OBSERVATION_START)

def SKTAddOBSERVATION_END(builder, OBSERVATION_END):
    builder.PrependUOffsetTRelativeSlot(43, flatbuffers.number_types.UOffsetTFlags.py_type(OBSERVATION_END), 0)

def AddOBSERVATION_END(builder, OBSERVATION_END):
    SKTAddOBSERVATION_END(builder, OBSERVATION_END)

def SKTAddSOURCE_NAMES(builder, SOURCE_NAMES):
    builder.PrependUOffsetTRelativeSlot(44, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_NAMES), 0)

def AddSOURCE_NAMES(builder, SOURCE_NAMES):
    SKTAddSOURCE_NAMES(builder, SOURCE_NAMES)

def SKTStartSOURCE_NAMESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCE_NAMESVector(builder, numElems):
    return SKTStartSOURCE_NAMESVector(builder, numElems)

def SKTCreateSOURCE_NAMESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCE_NAMESVector(builder, data):
    SKTCreateSOURCE_NAMESVector(builder, data)

def SKTAddPRODUCER_PEER_ID(builder, PRODUCER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(45, flatbuffers.number_types.UOffsetTFlags.py_type(PRODUCER_PEER_ID), 0)

def AddPRODUCER_PEER_ID(builder, PRODUCER_PEER_ID):
    SKTAddPRODUCER_PEER_ID(builder, PRODUCER_PEER_ID)

def SKTAddPRODUCER_VERSION(builder, PRODUCER_VERSION):
    builder.PrependUOffsetTRelativeSlot(46, flatbuffers.number_types.UOffsetTFlags.py_type(PRODUCER_VERSION), 0)

def AddPRODUCER_VERSION(builder, PRODUCER_VERSION):
    SKTAddPRODUCER_VERSION(builder, PRODUCER_VERSION)

def SKTAddPRODUCED_AT(builder, PRODUCED_AT):
    builder.PrependUOffsetTRelativeSlot(47, flatbuffers.number_types.UOffsetTFlags.py_type(PRODUCED_AT), 0)

def AddPRODUCED_AT(builder, PRODUCED_AT):
    SKTAddPRODUCED_AT(builder, PRODUCED_AT)

def SKTAddCITATION(builder, CITATION):
    builder.PrependUOffsetTRelativeSlot(48, flatbuffers.number_types.UOffsetTFlags.py_type(CITATION), 0)

def AddCITATION(builder, CITATION):
    SKTAddCITATION(builder, CITATION)

def SKTAddLICENSE_URL(builder, LICENSE_URL):
    builder.PrependUOffsetTRelativeSlot(49, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE_URL), 0)

def AddLICENSE_URL(builder, LICENSE_URL):
    SKTAddLICENSE_URL(builder, LICENSE_URL)

def SKTEnd(builder):
    return builder.EndObject()

def End(builder):
    return SKTEnd(builder)

import SKTPoint
try:
    from typing import List
except:
    pass

class SKTT(object):

    # SKTT
    def __init__(
        self,
        LAYER_ID = None,
        FRAME = 1,
        ORDER = 0,
        PIXEL = 0,
        MAX_ORDER = 255,
        CHILD_MASK = 0,
        PAYLOAD_KIND = 0,
        REFINE = 0,
        SPECTRAL_REGIME = 0,
        BAND_ID = None,
        WAVELENGTH_EFF_NM = float('nan'),
        WAVELENGTH_MIN_NM = float('nan'),
        WAVELENGTH_MAX_NM = float('nan'),
        MAGNITUDE_SYSTEM = 0,
        PLACE = 1,
        EPOCH = float('nan'),
        OBSERVER_ID = None,
        POINT_COUNT = 0,
        MAG_BRIGHTEST = float('nan'),
        MAG_LIMIT = float('nan'),
        COLOR_ENCODING = 0,
        COLOR_BLUE_BAND_ID = None,
        COLOR_RED_BAND_ID = None,
        POINTS = None,
        ROW_COLLECTION_CID = None,
        MAP_DEPTH = 0,
        MAP_ENCODING = 0,
        MAP_QUANTITY = 0,
        MAP_QUANTITY_NAME = None,
        MAP_UNITS = None,
        MAP_SCALE = 1.0,
        MAP_OFFSET = 0.0,
        MAP_BLANK = 4294967295,
        MAP_VALUES = None,
        STRETCH = 0,
        BLACK_POINT = float('nan'),
        WHITE_POINT = float('nan'),
        STRETCH_SOFTENING = float('nan'),
        PERCENTILE_LEVELS = None,
        PERCENTILE_VALUES = None,
        PAYLOAD_CID = None,
        PAYLOAD_BYTES = 0,
        OBSERVATION_START = None,
        OBSERVATION_END = None,
        SOURCE_NAMES = None,
        PRODUCER_PEER_ID = None,
        PRODUCER_VERSION = None,
        PRODUCED_AT = None,
        CITATION = None,
        LICENSE_URL = None,
    ):
        self.LAYER_ID = LAYER_ID  # type: Optional[str]
        self.FRAME = FRAME  # type: int
        self.ORDER = ORDER  # type: int
        self.PIXEL = PIXEL  # type: int
        self.MAX_ORDER = MAX_ORDER  # type: int
        self.CHILD_MASK = CHILD_MASK  # type: int
        self.PAYLOAD_KIND = PAYLOAD_KIND  # type: int
        self.REFINE = REFINE  # type: int
        self.SPECTRAL_REGIME = SPECTRAL_REGIME  # type: int
        self.BAND_ID = BAND_ID  # type: Optional[str]
        self.WAVELENGTH_EFF_NM = WAVELENGTH_EFF_NM  # type: float
        self.WAVELENGTH_MIN_NM = WAVELENGTH_MIN_NM  # type: float
        self.WAVELENGTH_MAX_NM = WAVELENGTH_MAX_NM  # type: float
        self.MAGNITUDE_SYSTEM = MAGNITUDE_SYSTEM  # type: int
        self.PLACE = PLACE  # type: int
        self.EPOCH = EPOCH  # type: float
        self.OBSERVER_ID = OBSERVER_ID  # type: Optional[str]
        self.POINT_COUNT = POINT_COUNT  # type: int
        self.MAG_BRIGHTEST = MAG_BRIGHTEST  # type: float
        self.MAG_LIMIT = MAG_LIMIT  # type: float
        self.COLOR_ENCODING = COLOR_ENCODING  # type: int
        self.COLOR_BLUE_BAND_ID = COLOR_BLUE_BAND_ID  # type: Optional[str]
        self.COLOR_RED_BAND_ID = COLOR_RED_BAND_ID  # type: Optional[str]
        self.POINTS = POINTS  # type: Optional[List[SKTPoint.SKTPointT]]
        self.ROW_COLLECTION_CID = ROW_COLLECTION_CID  # type: Optional[str]
        self.MAP_DEPTH = MAP_DEPTH  # type: int
        self.MAP_ENCODING = MAP_ENCODING  # type: int
        self.MAP_QUANTITY = MAP_QUANTITY  # type: int
        self.MAP_QUANTITY_NAME = MAP_QUANTITY_NAME  # type: Optional[str]
        self.MAP_UNITS = MAP_UNITS  # type: Optional[str]
        self.MAP_SCALE = MAP_SCALE  # type: float
        self.MAP_OFFSET = MAP_OFFSET  # type: float
        self.MAP_BLANK = MAP_BLANK  # type: int
        self.MAP_VALUES = MAP_VALUES  # type: Optional[List[int]]
        self.STRETCH = STRETCH  # type: int
        self.BLACK_POINT = BLACK_POINT  # type: float
        self.WHITE_POINT = WHITE_POINT  # type: float
        self.STRETCH_SOFTENING = STRETCH_SOFTENING  # type: float
        self.PERCENTILE_LEVELS = PERCENTILE_LEVELS  # type: Optional[List[float]]
        self.PERCENTILE_VALUES = PERCENTILE_VALUES  # type: Optional[List[float]]
        self.PAYLOAD_CID = PAYLOAD_CID  # type: Optional[str]
        self.PAYLOAD_BYTES = PAYLOAD_BYTES  # type: int
        self.OBSERVATION_START = OBSERVATION_START  # type: Optional[str]
        self.OBSERVATION_END = OBSERVATION_END  # type: Optional[str]
        self.SOURCE_NAMES = SOURCE_NAMES  # type: Optional[List[Optional[str]]]
        self.PRODUCER_PEER_ID = PRODUCER_PEER_ID  # type: Optional[str]
        self.PRODUCER_VERSION = PRODUCER_VERSION  # type: Optional[str]
        self.PRODUCED_AT = PRODUCED_AT  # type: Optional[str]
        self.CITATION = CITATION  # type: Optional[str]
        self.LICENSE_URL = LICENSE_URL  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSkt = SKT()
        tmpSkt.Init(buf, pos)
        return cls.InitFromObj(tmpSkt)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSkt):
        x = SKTT()
        x._UnPack(tmpSkt)
        return x

    # SKTT
    def _UnPack(self, SKT):
        if SKT is None:
            return
        self.LAYER_ID = SKT.LAYER_ID()
        self.FRAME = SKT.FRAME()
        self.ORDER = SKT.ORDER()
        self.PIXEL = SKT.PIXEL()
        self.MAX_ORDER = SKT.MAX_ORDER()
        self.CHILD_MASK = SKT.CHILD_MASK()
        self.PAYLOAD_KIND = SKT.PAYLOAD_KIND()
        self.REFINE = SKT.REFINE()
        self.SPECTRAL_REGIME = SKT.SPECTRAL_REGIME()
        self.BAND_ID = SKT.BAND_ID()
        self.WAVELENGTH_EFF_NM = SKT.WAVELENGTH_EFF_NM()
        self.WAVELENGTH_MIN_NM = SKT.WAVELENGTH_MIN_NM()
        self.WAVELENGTH_MAX_NM = SKT.WAVELENGTH_MAX_NM()
        self.MAGNITUDE_SYSTEM = SKT.MAGNITUDE_SYSTEM()
        self.PLACE = SKT.PLACE()
        self.EPOCH = SKT.EPOCH()
        self.OBSERVER_ID = SKT.OBSERVER_ID()
        self.POINT_COUNT = SKT.POINT_COUNT()
        self.MAG_BRIGHTEST = SKT.MAG_BRIGHTEST()
        self.MAG_LIMIT = SKT.MAG_LIMIT()
        self.COLOR_ENCODING = SKT.COLOR_ENCODING()
        self.COLOR_BLUE_BAND_ID = SKT.COLOR_BLUE_BAND_ID()
        self.COLOR_RED_BAND_ID = SKT.COLOR_RED_BAND_ID()
        if not SKT.POINTSIsNone():
            self.POINTS = []
            for i in range(SKT.POINTSLength()):
                if SKT.POINTS(i) is None:
                    self.POINTS.append(None)
                else:
                    sKTPoint_ = SKTPoint.SKTPointT.InitFromObj(SKT.POINTS(i))
                    self.POINTS.append(sKTPoint_)
        self.ROW_COLLECTION_CID = SKT.ROW_COLLECTION_CID()
        self.MAP_DEPTH = SKT.MAP_DEPTH()
        self.MAP_ENCODING = SKT.MAP_ENCODING()
        self.MAP_QUANTITY = SKT.MAP_QUANTITY()
        self.MAP_QUANTITY_NAME = SKT.MAP_QUANTITY_NAME()
        self.MAP_UNITS = SKT.MAP_UNITS()
        self.MAP_SCALE = SKT.MAP_SCALE()
        self.MAP_OFFSET = SKT.MAP_OFFSET()
        self.MAP_BLANK = SKT.MAP_BLANK()
        if not SKT.MAP_VALUESIsNone():
            if np is None:
                self.MAP_VALUES = []
                for i in range(SKT.MAP_VALUESLength()):
                    self.MAP_VALUES.append(SKT.MAP_VALUES(i))
            else:
                self.MAP_VALUES = SKT.MAP_VALUESAsNumpy()
        self.STRETCH = SKT.STRETCH()
        self.BLACK_POINT = SKT.BLACK_POINT()
        self.WHITE_POINT = SKT.WHITE_POINT()
        self.STRETCH_SOFTENING = SKT.STRETCH_SOFTENING()
        if not SKT.PERCENTILE_LEVELSIsNone():
            if np is None:
                self.PERCENTILE_LEVELS = []
                for i in range(SKT.PERCENTILE_LEVELSLength()):
                    self.PERCENTILE_LEVELS.append(SKT.PERCENTILE_LEVELS(i))
            else:
                self.PERCENTILE_LEVELS = SKT.PERCENTILE_LEVELSAsNumpy()
        if not SKT.PERCENTILE_VALUESIsNone():
            if np is None:
                self.PERCENTILE_VALUES = []
                for i in range(SKT.PERCENTILE_VALUESLength()):
                    self.PERCENTILE_VALUES.append(SKT.PERCENTILE_VALUES(i))
            else:
                self.PERCENTILE_VALUES = SKT.PERCENTILE_VALUESAsNumpy()
        self.PAYLOAD_CID = SKT.PAYLOAD_CID()
        self.PAYLOAD_BYTES = SKT.PAYLOAD_BYTES()
        self.OBSERVATION_START = SKT.OBSERVATION_START()
        self.OBSERVATION_END = SKT.OBSERVATION_END()
        if not SKT.SOURCE_NAMESIsNone():
            self.SOURCE_NAMES = []
            for i in range(SKT.SOURCE_NAMESLength()):
                self.SOURCE_NAMES.append(SKT.SOURCE_NAMES(i))
        self.PRODUCER_PEER_ID = SKT.PRODUCER_PEER_ID()
        self.PRODUCER_VERSION = SKT.PRODUCER_VERSION()
        self.PRODUCED_AT = SKT.PRODUCED_AT()
        self.CITATION = SKT.CITATION()
        self.LICENSE_URL = SKT.LICENSE_URL()

    # SKTT
    def Pack(self, builder):
        if self.LAYER_ID is not None:
            LAYER_ID = builder.CreateString(self.LAYER_ID)
        if self.BAND_ID is not None:
            BAND_ID = builder.CreateString(self.BAND_ID)
        if self.OBSERVER_ID is not None:
            OBSERVER_ID = builder.CreateString(self.OBSERVER_ID)
        if self.COLOR_BLUE_BAND_ID is not None:
            COLOR_BLUE_BAND_ID = builder.CreateString(self.COLOR_BLUE_BAND_ID)
        if self.COLOR_RED_BAND_ID is not None:
            COLOR_RED_BAND_ID = builder.CreateString(self.COLOR_RED_BAND_ID)
        if self.POINTS is not None:
            SKTStartPOINTSVector(builder, len(self.POINTS))
            for i in reversed(range(len(self.POINTS))):
                self.POINTS[i].Pack(builder)
            POINTS = builder.EndVector()
        if self.ROW_COLLECTION_CID is not None:
            ROW_COLLECTION_CID = builder.CreateString(self.ROW_COLLECTION_CID)
        if self.MAP_QUANTITY_NAME is not None:
            MAP_QUANTITY_NAME = builder.CreateString(self.MAP_QUANTITY_NAME)
        if self.MAP_UNITS is not None:
            MAP_UNITS = builder.CreateString(self.MAP_UNITS)
        if self.MAP_VALUES is not None:
            if np is not None and type(self.MAP_VALUES) is np.ndarray:
                MAP_VALUES = builder.CreateNumpyVector(self.MAP_VALUES)
            else:
                SKTStartMAP_VALUESVector(builder, len(self.MAP_VALUES))
                for i in reversed(range(len(self.MAP_VALUES))):
                    builder.PrependUint8(self.MAP_VALUES[i])
                MAP_VALUES = builder.EndVector()
        if self.PERCENTILE_LEVELS is not None:
            if np is not None and type(self.PERCENTILE_LEVELS) is np.ndarray:
                PERCENTILE_LEVELS = builder.CreateNumpyVector(self.PERCENTILE_LEVELS)
            else:
                SKTStartPERCENTILE_LEVELSVector(builder, len(self.PERCENTILE_LEVELS))
                for i in reversed(range(len(self.PERCENTILE_LEVELS))):
                    builder.PrependFloat32(self.PERCENTILE_LEVELS[i])
                PERCENTILE_LEVELS = builder.EndVector()
        if self.PERCENTILE_VALUES is not None:
            if np is not None and type(self.PERCENTILE_VALUES) is np.ndarray:
                PERCENTILE_VALUES = builder.CreateNumpyVector(self.PERCENTILE_VALUES)
            else:
                SKTStartPERCENTILE_VALUESVector(builder, len(self.PERCENTILE_VALUES))
                for i in reversed(range(len(self.PERCENTILE_VALUES))):
                    builder.PrependFloat64(self.PERCENTILE_VALUES[i])
                PERCENTILE_VALUES = builder.EndVector()
        if self.PAYLOAD_CID is not None:
            PAYLOAD_CID = builder.CreateString(self.PAYLOAD_CID)
        if self.OBSERVATION_START is not None:
            OBSERVATION_START = builder.CreateString(self.OBSERVATION_START)
        if self.OBSERVATION_END is not None:
            OBSERVATION_END = builder.CreateString(self.OBSERVATION_END)
        if self.SOURCE_NAMES is not None:
            SOURCE_NAMESlist = []
            for i in range(len(self.SOURCE_NAMES)):
                SOURCE_NAMESlist.append(builder.CreateString(self.SOURCE_NAMES[i]))
            SKTStartSOURCE_NAMESVector(builder, len(self.SOURCE_NAMES))
            for i in reversed(range(len(self.SOURCE_NAMES))):
                builder.PrependUOffsetTRelative(SOURCE_NAMESlist[i])
            SOURCE_NAMES = builder.EndVector()
        if self.PRODUCER_PEER_ID is not None:
            PRODUCER_PEER_ID = builder.CreateString(self.PRODUCER_PEER_ID)
        if self.PRODUCER_VERSION is not None:
            PRODUCER_VERSION = builder.CreateString(self.PRODUCER_VERSION)
        if self.PRODUCED_AT is not None:
            PRODUCED_AT = builder.CreateString(self.PRODUCED_AT)
        if self.CITATION is not None:
            CITATION = builder.CreateString(self.CITATION)
        if self.LICENSE_URL is not None:
            LICENSE_URL = builder.CreateString(self.LICENSE_URL)
        SKTStart(builder)
        if self.LAYER_ID is not None:
            SKTAddLAYER_ID(builder, LAYER_ID)
        SKTAddFRAME(builder, self.FRAME)
        SKTAddORDER(builder, self.ORDER)
        SKTAddPIXEL(builder, self.PIXEL)
        SKTAddMAX_ORDER(builder, self.MAX_ORDER)
        SKTAddCHILD_MASK(builder, self.CHILD_MASK)
        SKTAddPAYLOAD_KIND(builder, self.PAYLOAD_KIND)
        SKTAddREFINE(builder, self.REFINE)
        SKTAddSPECTRAL_REGIME(builder, self.SPECTRAL_REGIME)
        if self.BAND_ID is not None:
            SKTAddBAND_ID(builder, BAND_ID)
        SKTAddWAVELENGTH_EFF_NM(builder, self.WAVELENGTH_EFF_NM)
        SKTAddWAVELENGTH_MIN_NM(builder, self.WAVELENGTH_MIN_NM)
        SKTAddWAVELENGTH_MAX_NM(builder, self.WAVELENGTH_MAX_NM)
        SKTAddMAGNITUDE_SYSTEM(builder, self.MAGNITUDE_SYSTEM)
        SKTAddPLACE(builder, self.PLACE)
        SKTAddEPOCH(builder, self.EPOCH)
        if self.OBSERVER_ID is not None:
            SKTAddOBSERVER_ID(builder, OBSERVER_ID)
        SKTAddPOINT_COUNT(builder, self.POINT_COUNT)
        SKTAddMAG_BRIGHTEST(builder, self.MAG_BRIGHTEST)
        SKTAddMAG_LIMIT(builder, self.MAG_LIMIT)
        SKTAddCOLOR_ENCODING(builder, self.COLOR_ENCODING)
        if self.COLOR_BLUE_BAND_ID is not None:
            SKTAddCOLOR_BLUE_BAND_ID(builder, COLOR_BLUE_BAND_ID)
        if self.COLOR_RED_BAND_ID is not None:
            SKTAddCOLOR_RED_BAND_ID(builder, COLOR_RED_BAND_ID)
        if self.POINTS is not None:
            SKTAddPOINTS(builder, POINTS)
        if self.ROW_COLLECTION_CID is not None:
            SKTAddROW_COLLECTION_CID(builder, ROW_COLLECTION_CID)
        SKTAddMAP_DEPTH(builder, self.MAP_DEPTH)
        SKTAddMAP_ENCODING(builder, self.MAP_ENCODING)
        SKTAddMAP_QUANTITY(builder, self.MAP_QUANTITY)
        if self.MAP_QUANTITY_NAME is not None:
            SKTAddMAP_QUANTITY_NAME(builder, MAP_QUANTITY_NAME)
        if self.MAP_UNITS is not None:
            SKTAddMAP_UNITS(builder, MAP_UNITS)
        SKTAddMAP_SCALE(builder, self.MAP_SCALE)
        SKTAddMAP_OFFSET(builder, self.MAP_OFFSET)
        SKTAddMAP_BLANK(builder, self.MAP_BLANK)
        if self.MAP_VALUES is not None:
            SKTAddMAP_VALUES(builder, MAP_VALUES)
        SKTAddSTRETCH(builder, self.STRETCH)
        SKTAddBLACK_POINT(builder, self.BLACK_POINT)
        SKTAddWHITE_POINT(builder, self.WHITE_POINT)
        SKTAddSTRETCH_SOFTENING(builder, self.STRETCH_SOFTENING)
        if self.PERCENTILE_LEVELS is not None:
            SKTAddPERCENTILE_LEVELS(builder, PERCENTILE_LEVELS)
        if self.PERCENTILE_VALUES is not None:
            SKTAddPERCENTILE_VALUES(builder, PERCENTILE_VALUES)
        if self.PAYLOAD_CID is not None:
            SKTAddPAYLOAD_CID(builder, PAYLOAD_CID)
        SKTAddPAYLOAD_BYTES(builder, self.PAYLOAD_BYTES)
        if self.OBSERVATION_START is not None:
            SKTAddOBSERVATION_START(builder, OBSERVATION_START)
        if self.OBSERVATION_END is not None:
            SKTAddOBSERVATION_END(builder, OBSERVATION_END)
        if self.SOURCE_NAMES is not None:
            SKTAddSOURCE_NAMES(builder, SOURCE_NAMES)
        if self.PRODUCER_PEER_ID is not None:
            SKTAddPRODUCER_PEER_ID(builder, PRODUCER_PEER_ID)
        if self.PRODUCER_VERSION is not None:
            SKTAddPRODUCER_VERSION(builder, PRODUCER_VERSION)
        if self.PRODUCED_AT is not None:
            SKTAddPRODUCED_AT(builder, PRODUCED_AT)
        if self.CITATION is not None:
            SKTAddCITATION(builder, CITATION)
        if self.LICENSE_URL is not None:
            SKTAddLICENSE_URL(builder, LICENSE_URL)
        SKT = SKTEnd(builder)
        return SKT
