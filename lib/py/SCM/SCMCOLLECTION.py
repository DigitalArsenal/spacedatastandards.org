# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Declaring the root type
class SCMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x4D", size_prefixed=size_prefixed)

    # SCMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCM import SCM
            obj = SCM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def SCMCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    SCMCOLLECTIONStart(builder)

def SCMCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    SCMCOLLECTIONAddRECORDS(builder, RECORDS)

def SCMCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return SCMCOLLECTIONStartRECORDSVector(builder, numElems)

def SCMCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCMCOLLECTIONEnd(builder)

import SCM
try:
    from typing import List
except:
    pass

class SCMCOLLECTIONT(object):

    # SCMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[SCM.SCMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        SCMCOLLECTION = SCMCOLLECTION()
        SCMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(SCMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, SCMCOLLECTION):
        x = SCMCOLLECTIONT()
        x._UnPack(SCMCOLLECTION)
        return x

    # SCMCOLLECTIONT
    def _UnPack(self, SCMCOLLECTION):
        if SCMCOLLECTION is None:
            return
        if not SCMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(SCMCOLLECTION.RECORDSLength()):
                if SCMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    sCM_ = SCM.SCMT.InitFromObj(SCMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(sCM_)

    # SCMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            SCMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        SCMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            SCMCOLLECTIONAddRECORDS(builder, RECORDS)
        SCMCOLLECTION = SCMCOLLECTIONEnd(builder)
        return SCMCOLLECTION
