# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One category tally.
class AGRCategory(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AGRCategory()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAGRCategory(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AGRCategoryBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x47\x52", size_prefixed=size_prefixed)

    # AGRCategory
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # AGRCategory
    def KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # AGRCategory
    def COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # AGRCategory
    def SUM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def AGRCategoryStart(builder):
    builder.StartObject(3)

def Start(builder):
    AGRCategoryStart(builder)

def AGRCategoryAddKEY(builder, KEY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(KEY), 0)

def AddKEY(builder, KEY):
    AGRCategoryAddKEY(builder, KEY)

def AGRCategoryAddCOUNT(builder, COUNT):
    builder.PrependUint64Slot(1, COUNT, 0)

def AddCOUNT(builder, COUNT):
    AGRCategoryAddCOUNT(builder, COUNT)

def AGRCategoryAddSUM(builder, SUM):
    builder.PrependFloat64Slot(2, SUM, 0.0)

def AddSUM(builder, SUM):
    AGRCategoryAddSUM(builder, SUM)

def AGRCategoryEnd(builder):
    return builder.EndObject()

def End(builder):
    return AGRCategoryEnd(builder)


class AGRCategoryT(object):

    # AGRCategoryT
    def __init__(
        self,
        KEY = None,
        COUNT = 0,
        SUM = 0.0,
    ):
        self.KEY = KEY  # type: Optional[str]
        self.COUNT = COUNT  # type: int
        self.SUM = SUM  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAgrcategory = AGRCategory()
        tmpAgrcategory.Init(buf, pos)
        return cls.InitFromObj(tmpAgrcategory)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAgrcategory):
        x = AGRCategoryT()
        x._UnPack(tmpAgrcategory)
        return x

    # AGRCategoryT
    def _UnPack(self, AGRCategory):
        if AGRCategory is None:
            return
        self.KEY = AGRCategory.KEY()
        self.COUNT = AGRCategory.COUNT()
        self.SUM = AGRCategory.SUM()

    # AGRCategoryT
    def Pack(self, builder):
        if self.KEY is not None:
            KEY = builder.CreateString(self.KEY)
        AGRCategoryStart(builder)
        if self.KEY is not None:
            AGRCategoryAddKEY(builder, KEY)
        AGRCategoryAddCOUNT(builder, self.COUNT)
        AGRCategoryAddSUM(builder, self.SUM)
        AGRCategory = AGRCategoryEnd(builder)
        return AGRCategory
