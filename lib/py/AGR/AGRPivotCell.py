# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One pivot cell.
class AGRPivotCell(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AGRPivotCell()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAGRPivotCell(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AGRPivotCellBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x47\x52", size_prefixed=size_prefixed)

    # AGRPivotCell
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # AGRPivotCell
    def ROW_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # AGRPivotCell
    def COL_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # AGRPivotCell
    def COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # AGRPivotCell
    def SUM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def AGRPivotCellStart(builder):
    builder.StartObject(4)

def Start(builder):
    AGRPivotCellStart(builder)

def AGRPivotCellAddROW_KEY(builder, ROW_KEY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ROW_KEY), 0)

def AddROW_KEY(builder, ROW_KEY):
    AGRPivotCellAddROW_KEY(builder, ROW_KEY)

def AGRPivotCellAddCOL_KEY(builder, COL_KEY):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(COL_KEY), 0)

def AddCOL_KEY(builder, COL_KEY):
    AGRPivotCellAddCOL_KEY(builder, COL_KEY)

def AGRPivotCellAddCOUNT(builder, COUNT):
    builder.PrependUint64Slot(2, COUNT, 0)

def AddCOUNT(builder, COUNT):
    AGRPivotCellAddCOUNT(builder, COUNT)

def AGRPivotCellAddSUM(builder, SUM):
    builder.PrependFloat64Slot(3, SUM, 0.0)

def AddSUM(builder, SUM):
    AGRPivotCellAddSUM(builder, SUM)

def AGRPivotCellEnd(builder):
    return builder.EndObject()

def End(builder):
    return AGRPivotCellEnd(builder)


class AGRPivotCellT(object):

    # AGRPivotCellT
    def __init__(
        self,
        ROW_KEY = None,
        COL_KEY = None,
        COUNT = 0,
        SUM = 0.0,
    ):
        self.ROW_KEY = ROW_KEY  # type: Optional[str]
        self.COL_KEY = COL_KEY  # type: Optional[str]
        self.COUNT = COUNT  # type: int
        self.SUM = SUM  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAgrpivotCell = AGRPivotCell()
        tmpAgrpivotCell.Init(buf, pos)
        return cls.InitFromObj(tmpAgrpivotCell)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAgrpivotCell):
        x = AGRPivotCellT()
        x._UnPack(tmpAgrpivotCell)
        return x

    # AGRPivotCellT
    def _UnPack(self, AGRPivotCell):
        if AGRPivotCell is None:
            return
        self.ROW_KEY = AGRPivotCell.ROW_KEY()
        self.COL_KEY = AGRPivotCell.COL_KEY()
        self.COUNT = AGRPivotCell.COUNT()
        self.SUM = AGRPivotCell.SUM()

    # AGRPivotCellT
    def Pack(self, builder):
        if self.ROW_KEY is not None:
            ROW_KEY = builder.CreateString(self.ROW_KEY)
        if self.COL_KEY is not None:
            COL_KEY = builder.CreateString(self.COL_KEY)
        AGRPivotCellStart(builder)
        if self.ROW_KEY is not None:
            AGRPivotCellAddROW_KEY(builder, ROW_KEY)
        if self.COL_KEY is not None:
            AGRPivotCellAddCOL_KEY(builder, COL_KEY)
        AGRPivotCellAddCOUNT(builder, self.COUNT)
        AGRPivotCellAddSUM(builder, self.SUM)
        AGRPivotCell = AGRPivotCellEnd(builder)
        return AGRPivotCell
