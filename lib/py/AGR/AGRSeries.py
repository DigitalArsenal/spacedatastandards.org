# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One named series with parallel X and Y vectors.
class AGRSeries(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AGRSeries()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAGRSeries(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AGRSeriesBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x47\x52", size_prefixed=size_prefixed)

    # AGRSeries
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # AGRSeries
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # AGRSeries
    def X(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # AGRSeries
    def XAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # AGRSeries
    def XLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AGRSeries
    def XIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # AGRSeries
    def Y(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # AGRSeries
    def YAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # AGRSeries
    def YLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AGRSeries
    def YIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def AGRSeriesStart(builder):
    builder.StartObject(3)

def Start(builder):
    AGRSeriesStart(builder)

def AGRSeriesAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    AGRSeriesAddNAME(builder, NAME)

def AGRSeriesAddX(builder, X):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(X), 0)

def AddX(builder, X):
    AGRSeriesAddX(builder, X)

def AGRSeriesStartXVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartXVector(builder, numElems):
    return AGRSeriesStartXVector(builder, numElems)

def AGRSeriesCreateXVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateXVector(builder, data):
    AGRSeriesCreateXVector(builder, data)

def AGRSeriesAddY(builder, Y):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(Y), 0)

def AddY(builder, Y):
    AGRSeriesAddY(builder, Y)

def AGRSeriesStartYVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartYVector(builder, numElems):
    return AGRSeriesStartYVector(builder, numElems)

def AGRSeriesCreateYVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateYVector(builder, data):
    AGRSeriesCreateYVector(builder, data)

def AGRSeriesEnd(builder):
    return builder.EndObject()

def End(builder):
    return AGRSeriesEnd(builder)

try:
    from typing import List
except:
    pass

class AGRSeriesT(object):

    # AGRSeriesT
    def __init__(
        self,
        NAME = None,
        X = None,
        Y = None,
    ):
        self.NAME = NAME  # type: Optional[str]
        self.X = X  # type: Optional[List[float]]
        self.Y = Y  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAgrseries = AGRSeries()
        tmpAgrseries.Init(buf, pos)
        return cls.InitFromObj(tmpAgrseries)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAgrseries):
        x = AGRSeriesT()
        x._UnPack(tmpAgrseries)
        return x

    # AGRSeriesT
    def _UnPack(self, AGRSeries):
        if AGRSeries is None:
            return
        self.NAME = AGRSeries.NAME()
        if not AGRSeries.XIsNone():
            if np is None:
                self.X = []
                for i in range(AGRSeries.XLength()):
                    self.X.append(AGRSeries.X(i))
            else:
                self.X = AGRSeries.XAsNumpy()
        if not AGRSeries.YIsNone():
            if np is None:
                self.Y = []
                for i in range(AGRSeries.YLength()):
                    self.Y.append(AGRSeries.Y(i))
            else:
                self.Y = AGRSeries.YAsNumpy()

    # AGRSeriesT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.X is not None:
            if np is not None and type(self.X) is np.ndarray:
                X = builder.CreateNumpyVector(self.X)
            else:
                AGRSeriesStartXVector(builder, len(self.X))
                for i in reversed(range(len(self.X))):
                    builder.PrependFloat64(self.X[i])
                X = builder.EndVector()
        if self.Y is not None:
            if np is not None and type(self.Y) is np.ndarray:
                Y = builder.CreateNumpyVector(self.Y)
            else:
                AGRSeriesStartYVector(builder, len(self.Y))
                for i in reversed(range(len(self.Y))):
                    builder.PrependFloat64(self.Y[i])
                Y = builder.EndVector()
        AGRSeriesStart(builder)
        if self.NAME is not None:
            AGRSeriesAddNAME(builder, NAME)
        if self.X is not None:
            AGRSeriesAddX(builder, X)
        if self.Y is not None:
            AGRSeriesAddY(builder, Y)
        AGRSeries = AGRSeriesEnd(builder)
        return AGRSeries
