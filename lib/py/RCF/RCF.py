# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Return Channel Frames Service (CCSDS 913.5-B-2)
class RCF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RCF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRCF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RCFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x43\x46", size_prefixed=size_prefixed)

    # RCF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PDU type
    # RCF
    def PDU_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Initiator identifier
    # RCF
    def INITIATOR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Responder port identifier
    # RCF
    def RESPONDER_PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Service type
    # RCF
    def SERVICE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Version number
    # RCF
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Invoke ID
    # RCF
    def INVOKE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Spacecraft ID filter
    # RCF
    def SPACECRAFT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Virtual channel ID filter
    # RCF
    def VIRTUAL_CHANNEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Data
    # RCF
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # RCF
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # RCF
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RCF
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

def RCFStart(builder):
    builder.StartObject(9)

def Start(builder):
    RCFStart(builder)

def RCFAddPDU_TYPE(builder, PDU_TYPE):
    builder.PrependInt8Slot(0, PDU_TYPE, 0)

def AddPDU_TYPE(builder, PDU_TYPE):
    RCFAddPDU_TYPE(builder, PDU_TYPE)

def RCFAddINITIATOR_ID(builder, INITIATOR_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(INITIATOR_ID), 0)

def AddINITIATOR_ID(builder, INITIATOR_ID):
    RCFAddINITIATOR_ID(builder, INITIATOR_ID)

def RCFAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(RESPONDER_PORT_ID), 0)

def AddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID):
    RCFAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID)

def RCFAddSERVICE_TYPE(builder, SERVICE_TYPE):
    builder.PrependUint8Slot(3, SERVICE_TYPE, 0)

def AddSERVICE_TYPE(builder, SERVICE_TYPE):
    RCFAddSERVICE_TYPE(builder, SERVICE_TYPE)

def RCFAddVERSION(builder, VERSION):
    builder.PrependUint16Slot(4, VERSION, 0)

def AddVERSION(builder, VERSION):
    RCFAddVERSION(builder, VERSION)

def RCFAddINVOKE_ID(builder, INVOKE_ID):
    builder.PrependUint32Slot(5, INVOKE_ID, 0)

def AddINVOKE_ID(builder, INVOKE_ID):
    RCFAddINVOKE_ID(builder, INVOKE_ID)

def RCFAddSPACECRAFT_ID(builder, SPACECRAFT_ID):
    builder.PrependUint16Slot(6, SPACECRAFT_ID, 0)

def AddSPACECRAFT_ID(builder, SPACECRAFT_ID):
    RCFAddSPACECRAFT_ID(builder, SPACECRAFT_ID)

def RCFAddVIRTUAL_CHANNEL_ID(builder, VIRTUAL_CHANNEL_ID):
    builder.PrependUint8Slot(7, VIRTUAL_CHANNEL_ID, 0)

def AddVIRTUAL_CHANNEL_ID(builder, VIRTUAL_CHANNEL_ID):
    RCFAddVIRTUAL_CHANNEL_ID(builder, VIRTUAL_CHANNEL_ID)

def RCFAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    RCFAddDATA(builder, DATA)

def RCFStartDATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDATAVector(builder, numElems):
    return RCFStartDATAVector(builder, numElems)

def RCFEnd(builder):
    return builder.EndObject()

def End(builder):
    return RCFEnd(builder)

try:
    from typing import List
except:
    pass

class RCFT(object):

    # RCFT
    def __init__(self):
        self.PDU_TYPE = 0  # type: int
        self.INITIATOR_ID = None  # type: str
        self.RESPONDER_PORT_ID = None  # type: str
        self.SERVICE_TYPE = 0  # type: int
        self.VERSION = 0  # type: int
        self.INVOKE_ID = 0  # type: int
        self.SPACECRAFT_ID = 0  # type: int
        self.VIRTUAL_CHANNEL_ID = 0  # type: int
        self.DATA = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        RCF = RCF()
        RCF.Init(buf, pos)
        return cls.InitFromObj(RCF)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, RCF):
        x = RCFT()
        x._UnPack(RCF)
        return x

    # RCFT
    def _UnPack(self, RCF):
        if RCF is None:
            return
        self.PDU_TYPE = RCF.PDU_TYPE()
        self.INITIATOR_ID = RCF.INITIATOR_ID()
        self.RESPONDER_PORT_ID = RCF.RESPONDER_PORT_ID()
        self.SERVICE_TYPE = RCF.SERVICE_TYPE()
        self.VERSION = RCF.VERSION()
        self.INVOKE_ID = RCF.INVOKE_ID()
        self.SPACECRAFT_ID = RCF.SPACECRAFT_ID()
        self.VIRTUAL_CHANNEL_ID = RCF.VIRTUAL_CHANNEL_ID()
        if not RCF.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(RCF.DATALength()):
                    self.DATA.append(RCF.DATA(i))
            else:
                self.DATA = RCF.DATAAsNumpy()

    # RCFT
    def Pack(self, builder):
        if self.INITIATOR_ID is not None:
            INITIATOR_ID = builder.CreateString(self.INITIATOR_ID)
        if self.RESPONDER_PORT_ID is not None:
            RESPONDER_PORT_ID = builder.CreateString(self.RESPONDER_PORT_ID)
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                RCFStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependUint8(self.DATA[i])
                DATA = builder.EndVector()
        RCFStart(builder)
        RCFAddPDU_TYPE(builder, self.PDU_TYPE)
        if self.INITIATOR_ID is not None:
            RCFAddINITIATOR_ID(builder, INITIATOR_ID)
        if self.RESPONDER_PORT_ID is not None:
            RCFAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID)
        RCFAddSERVICE_TYPE(builder, self.SERVICE_TYPE)
        RCFAddVERSION(builder, self.VERSION)
        RCFAddINVOKE_ID(builder, self.INVOKE_ID)
        RCFAddSPACECRAFT_ID(builder, self.SPACECRAFT_ID)
        RCFAddVIRTUAL_CHANNEL_ID(builder, self.VIRTUAL_CHANNEL_ID)
        if self.DATA is not None:
            RCFAddDATA(builder, DATA)
        RCF = RCFEnd(builder)
        return RCF
