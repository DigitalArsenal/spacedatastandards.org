# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# FlatSQL Byte Stream — a bounded, typed chunk used for append/query and
# opaque snapshot/WAL persistence. DATA carries at most one MiB so larger
# inputs and snapshots are delivered as ordered chunks. The host may persist
# canonical FSB bytes but must not interpret DATA as tables, rows, or SQL.
class FSB(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FSB()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFSB(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FSBBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x46\x53\x42", size_prefixed=size_prefixed)

    # FSB
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FSB
    def REQUEST_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSB
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # FSB
    def CHUNK_SEQUENCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FSB
    def FINAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # FSB
    def TOTAL_BYTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSB
    def RECORD_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSB
    def COLUMN_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FSB
    def SCHEMA_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSB
    def FILE_IDENTIFIER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSB
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSB
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSB
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSB
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # FSB
    def SHA256(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSB
    def SHA256AsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSB
    def SHA256Length(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSB
    def SHA256IsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

def FSBStart(builder):
    builder.StartObject(11)

def Start(builder):
    FSBStart(builder)

def FSBAddREQUEST_ID(builder, REQUEST_ID):
    builder.PrependUint64Slot(0, REQUEST_ID, 0)

def AddREQUEST_ID(builder, REQUEST_ID):
    FSBAddREQUEST_ID(builder, REQUEST_ID)

def FSBAddKIND(builder, KIND):
    builder.PrependUint8Slot(1, KIND, 0)

def AddKIND(builder, KIND):
    FSBAddKIND(builder, KIND)

def FSBAddCHUNK_SEQUENCE(builder, CHUNK_SEQUENCE):
    builder.PrependUint32Slot(2, CHUNK_SEQUENCE, 0)

def AddCHUNK_SEQUENCE(builder, CHUNK_SEQUENCE):
    FSBAddCHUNK_SEQUENCE(builder, CHUNK_SEQUENCE)

def FSBAddFINAL(builder, FINAL):
    builder.PrependBoolSlot(3, FINAL, 0)

def AddFINAL(builder, FINAL):
    FSBAddFINAL(builder, FINAL)

def FSBAddTOTAL_BYTES(builder, TOTAL_BYTES):
    builder.PrependUint64Slot(4, TOTAL_BYTES, 0)

def AddTOTAL_BYTES(builder, TOTAL_BYTES):
    FSBAddTOTAL_BYTES(builder, TOTAL_BYTES)

def FSBAddRECORD_COUNT(builder, RECORD_COUNT):
    builder.PrependUint64Slot(5, RECORD_COUNT, 0)

def AddRECORD_COUNT(builder, RECORD_COUNT):
    FSBAddRECORD_COUNT(builder, RECORD_COUNT)

def FSBAddCOLUMN_COUNT(builder, COLUMN_COUNT):
    builder.PrependUint32Slot(6, COLUMN_COUNT, 0)

def AddCOLUMN_COUNT(builder, COLUMN_COUNT):
    FSBAddCOLUMN_COUNT(builder, COLUMN_COUNT)

def FSBAddSCHEMA_NAME(builder, SCHEMA_NAME):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_NAME), 0)

def AddSCHEMA_NAME(builder, SCHEMA_NAME):
    FSBAddSCHEMA_NAME(builder, SCHEMA_NAME)

def FSBAddFILE_IDENTIFIER(builder, FILE_IDENTIFIER):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(FILE_IDENTIFIER), 0)

def AddFILE_IDENTIFIER(builder, FILE_IDENTIFIER):
    FSBAddFILE_IDENTIFIER(builder, FILE_IDENTIFIER)

def FSBAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    FSBAddDATA(builder, DATA)

def FSBStartDATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDATAVector(builder, numElems):
    return FSBStartDATAVector(builder, numElems)

def FSBCreateDATAVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateDATAVector(builder, data):
    FSBCreateDATAVector(builder, data)

def FSBAddSHA256(builder, SHA256):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(SHA256), 0)

def AddSHA256(builder, SHA256):
    FSBAddSHA256(builder, SHA256)

def FSBStartSHA256Vector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSHA256Vector(builder, numElems):
    return FSBStartSHA256Vector(builder, numElems)

def FSBCreateSHA256Vector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSHA256Vector(builder, data):
    FSBCreateSHA256Vector(builder, data)

def FSBEnd(builder):
    return builder.EndObject()

def End(builder):
    return FSBEnd(builder)

try:
    from typing import List
except:
    pass

class FSBT(object):

    # FSBT
    def __init__(
        self,
        REQUEST_ID = 0,
        KIND = 0,
        CHUNK_SEQUENCE = 0,
        FINAL = False,
        TOTAL_BYTES = 0,
        RECORD_COUNT = 0,
        COLUMN_COUNT = 0,
        SCHEMA_NAME = None,
        FILE_IDENTIFIER = None,
        DATA = None,
        SHA256 = None,
    ):
        self.REQUEST_ID = REQUEST_ID  # type: int
        self.KIND = KIND  # type: int
        self.CHUNK_SEQUENCE = CHUNK_SEQUENCE  # type: int
        self.FINAL = FINAL  # type: bool
        self.TOTAL_BYTES = TOTAL_BYTES  # type: int
        self.RECORD_COUNT = RECORD_COUNT  # type: int
        self.COLUMN_COUNT = COLUMN_COUNT  # type: int
        self.SCHEMA_NAME = SCHEMA_NAME  # type: Optional[str]
        self.FILE_IDENTIFIER = FILE_IDENTIFIER  # type: Optional[str]
        self.DATA = DATA  # type: Optional[List[int]]
        self.SHA256 = SHA256  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpFsb = FSB()
        tmpFsb.Init(buf, pos)
        return cls.InitFromObj(tmpFsb)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpFsb):
        x = FSBT()
        x._UnPack(tmpFsb)
        return x

    # FSBT
    def _UnPack(self, FSB):
        if FSB is None:
            return
        self.REQUEST_ID = FSB.REQUEST_ID()
        self.KIND = FSB.KIND()
        self.CHUNK_SEQUENCE = FSB.CHUNK_SEQUENCE()
        self.FINAL = FSB.FINAL()
        self.TOTAL_BYTES = FSB.TOTAL_BYTES()
        self.RECORD_COUNT = FSB.RECORD_COUNT()
        self.COLUMN_COUNT = FSB.COLUMN_COUNT()
        self.SCHEMA_NAME = FSB.SCHEMA_NAME()
        self.FILE_IDENTIFIER = FSB.FILE_IDENTIFIER()
        if not FSB.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(FSB.DATALength()):
                    self.DATA.append(FSB.DATA(i))
            else:
                self.DATA = FSB.DATAAsNumpy()
        if not FSB.SHA256IsNone():
            if np is None:
                self.SHA256 = []
                for i in range(FSB.SHA256Length()):
                    self.SHA256.append(FSB.SHA256(i))
            else:
                self.SHA256 = FSB.SHA256AsNumpy()

    # FSBT
    def Pack(self, builder):
        if self.SCHEMA_NAME is not None:
            SCHEMA_NAME = builder.CreateString(self.SCHEMA_NAME)
        if self.FILE_IDENTIFIER is not None:
            FILE_IDENTIFIER = builder.CreateString(self.FILE_IDENTIFIER)
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                FSBStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependUint8(self.DATA[i])
                DATA = builder.EndVector()
        if self.SHA256 is not None:
            if np is not None and type(self.SHA256) is np.ndarray:
                SHA256 = builder.CreateNumpyVector(self.SHA256)
            else:
                FSBStartSHA256Vector(builder, len(self.SHA256))
                for i in reversed(range(len(self.SHA256))):
                    builder.PrependUint8(self.SHA256[i])
                SHA256 = builder.EndVector()
        FSBStart(builder)
        FSBAddREQUEST_ID(builder, self.REQUEST_ID)
        FSBAddKIND(builder, self.KIND)
        FSBAddCHUNK_SEQUENCE(builder, self.CHUNK_SEQUENCE)
        FSBAddFINAL(builder, self.FINAL)
        FSBAddTOTAL_BYTES(builder, self.TOTAL_BYTES)
        FSBAddRECORD_COUNT(builder, self.RECORD_COUNT)
        FSBAddCOLUMN_COUNT(builder, self.COLUMN_COUNT)
        if self.SCHEMA_NAME is not None:
            FSBAddSCHEMA_NAME(builder, SCHEMA_NAME)
        if self.FILE_IDENTIFIER is not None:
            FSBAddFILE_IDENTIFIER(builder, FILE_IDENTIFIER)
        if self.DATA is not None:
            FSBAddDATA(builder, DATA)
        if self.SHA256 is not None:
            FSBAddSHA256(builder, SHA256)
        FSB = FSBEnd(builder)
        return FSB
