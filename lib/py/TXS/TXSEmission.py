# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One documented emission of one facility: a carrier or channel the facility
# is stated to radiate, with the power and antenna terms published for it.
#
# A facility radiating many carriers carries many entries; a facility whose
# frequency changes by season or hour carries the documented set here and the
# time-bounded usage in `$STX`. Unknown terms are ABSENT.
class TXSEmission(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TXSEmission()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTXSEmission(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TXSEmissionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x58\x53", size_prefixed=size_prefixed)

    # TXSEmission
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher-stable identifier for this emission within its facility record.
    # TXSEmission
    def EMISSION_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Assigned or nominal centre frequency in kilohertz. Kilohertz is the base
    # unit for this family because it spans the low-frequency broadcasting
    # allocations to the microwave allocations without unit switching; a
    # consumer joining to `$RFB` or `$TBS`, which carry megahertz, or to
    # `$IQC`, which carries hertz, converts at the join, explicitly. Zero means
    # unpublished.
    # TXSEmission
    def FREQUENCY_KHZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Necessary or occupied bandwidth in kilohertz as the source states it.
    # Zero means unpublished.
    # TXSEmission
    def BANDWIDTH_KHZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ITU emission designator verbatim, e.g. the necessary-bandwidth and
    # class-of-emission string a licence record carries. Authoritative over
    # MODULATION whenever both are present.
    # TXSEmission
    def EMISSION_DESIGNATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TXSEmission
    def MODULATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # TXSEmission
    def POLARIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Service class of THIS emission when it differs from the facility's.
    # TXSEmission
    def SERVICE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Transmitter output power at the antenna feed, kilowatts. Zero means
    # unpublished. Never derived from radiated power.
    # TXSEmission
    def TRANSMITTER_POWER_KW(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Effective radiated power referenced to a half-wave dipole, kilowatts.
    # Zero means unpublished.
    # TXSEmission
    def EFFECTIVE_RADIATED_POWER_KW(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Equivalent isotropically radiated power, kilowatts. Zero means
    # unpublished. Published alongside, never instead of, the dipole-referenced
    # figure: the two differ by the dipole gain and this standard never
    # converts between them.
    # TXSEmission
    def EIRP_KW(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Antenna centre of radiation above ground level, metres. Zero means
    # unpublished.
    # TXSEmission
    def ANTENNA_HEIGHT_AGL_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Main-lobe bearing, degrees clockwise from true north. Absent for a
    # non-directional emission; a non-directional emission is stated by
    # OMNIDIRECTIONAL, never by a bearing of zero.
    # TXSEmission
    def ANTENNA_AZIMUTH_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # The emission is radiated without a preferred bearing.
    # TXSEmission
    def OMNIDIRECTIONAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Half-power beamwidth in degrees. Zero means unpublished.
    # TXSEmission
    def BEAMWIDTH_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Antenna gain in decibels referenced to an isotropic radiator. Zero means
    # unpublished; a real 0 dBi figure is published as a source term in
    # provenance rather than being indistinguishable from absence.
    # TXSEmission
    def ANTENNA_GAIN_DBI(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Station identification radiated on this emission, verbatim: a callsign, a
    # spoken or keyed identifier, or the identity string the source publishes.
    # Never synthesized.
    # TXSEmission
    def STATION_IDENTITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TXSEmission
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # RFC 3339 UTC bounds of the authorization or documented use of this
    # emission. Either bound is absent when unpublished.
    # TXSEmission
    def VALID_FROM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TXSEmission
    def VALID_UNTIL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TXSEmissionStart(builder):
    builder.StartObject(19)

def Start(builder):
    TXSEmissionStart(builder)

def TXSEmissionAddEMISSION_ID(builder, EMISSION_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EMISSION_ID), 0)

def AddEMISSION_ID(builder, EMISSION_ID):
    TXSEmissionAddEMISSION_ID(builder, EMISSION_ID)

def TXSEmissionAddFREQUENCY_KHZ(builder, FREQUENCY_KHZ):
    builder.PrependFloat64Slot(1, FREQUENCY_KHZ, 0.0)

def AddFREQUENCY_KHZ(builder, FREQUENCY_KHZ):
    TXSEmissionAddFREQUENCY_KHZ(builder, FREQUENCY_KHZ)

def TXSEmissionAddBANDWIDTH_KHZ(builder, BANDWIDTH_KHZ):
    builder.PrependFloat64Slot(2, BANDWIDTH_KHZ, 0.0)

def AddBANDWIDTH_KHZ(builder, BANDWIDTH_KHZ):
    TXSEmissionAddBANDWIDTH_KHZ(builder, BANDWIDTH_KHZ)

def TXSEmissionAddEMISSION_DESIGNATOR(builder, EMISSION_DESIGNATOR):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(EMISSION_DESIGNATOR), 0)

def AddEMISSION_DESIGNATOR(builder, EMISSION_DESIGNATOR):
    TXSEmissionAddEMISSION_DESIGNATOR(builder, EMISSION_DESIGNATOR)

def TXSEmissionAddMODULATION(builder, MODULATION):
    builder.PrependInt8Slot(4, MODULATION, 0)

def AddMODULATION(builder, MODULATION):
    TXSEmissionAddMODULATION(builder, MODULATION)

def TXSEmissionAddPOLARIZATION(builder, POLARIZATION):
    builder.PrependInt8Slot(5, POLARIZATION, 0)

def AddPOLARIZATION(builder, POLARIZATION):
    TXSEmissionAddPOLARIZATION(builder, POLARIZATION)

def TXSEmissionAddSERVICE(builder, SERVICE):
    builder.PrependInt8Slot(6, SERVICE, 0)

def AddSERVICE(builder, SERVICE):
    TXSEmissionAddSERVICE(builder, SERVICE)

def TXSEmissionAddTRANSMITTER_POWER_KW(builder, TRANSMITTER_POWER_KW):
    builder.PrependFloat64Slot(7, TRANSMITTER_POWER_KW, 0.0)

def AddTRANSMITTER_POWER_KW(builder, TRANSMITTER_POWER_KW):
    TXSEmissionAddTRANSMITTER_POWER_KW(builder, TRANSMITTER_POWER_KW)

def TXSEmissionAddEFFECTIVE_RADIATED_POWER_KW(builder, EFFECTIVE_RADIATED_POWER_KW):
    builder.PrependFloat64Slot(8, EFFECTIVE_RADIATED_POWER_KW, 0.0)

def AddEFFECTIVE_RADIATED_POWER_KW(builder, EFFECTIVE_RADIATED_POWER_KW):
    TXSEmissionAddEFFECTIVE_RADIATED_POWER_KW(builder, EFFECTIVE_RADIATED_POWER_KW)

def TXSEmissionAddEIRP_KW(builder, EIRP_KW):
    builder.PrependFloat64Slot(9, EIRP_KW, 0.0)

def AddEIRP_KW(builder, EIRP_KW):
    TXSEmissionAddEIRP_KW(builder, EIRP_KW)

def TXSEmissionAddANTENNA_HEIGHT_AGL_M(builder, ANTENNA_HEIGHT_AGL_M):
    builder.PrependFloat64Slot(10, ANTENNA_HEIGHT_AGL_M, 0.0)

def AddANTENNA_HEIGHT_AGL_M(builder, ANTENNA_HEIGHT_AGL_M):
    TXSEmissionAddANTENNA_HEIGHT_AGL_M(builder, ANTENNA_HEIGHT_AGL_M)

def TXSEmissionAddANTENNA_AZIMUTH_DEG(builder, ANTENNA_AZIMUTH_DEG):
    builder.PrependFloat64Slot(11, ANTENNA_AZIMUTH_DEG, 0.0)

def AddANTENNA_AZIMUTH_DEG(builder, ANTENNA_AZIMUTH_DEG):
    TXSEmissionAddANTENNA_AZIMUTH_DEG(builder, ANTENNA_AZIMUTH_DEG)

def TXSEmissionAddOMNIDIRECTIONAL(builder, OMNIDIRECTIONAL):
    builder.PrependBoolSlot(12, OMNIDIRECTIONAL, 0)

def AddOMNIDIRECTIONAL(builder, OMNIDIRECTIONAL):
    TXSEmissionAddOMNIDIRECTIONAL(builder, OMNIDIRECTIONAL)

def TXSEmissionAddBEAMWIDTH_DEG(builder, BEAMWIDTH_DEG):
    builder.PrependFloat64Slot(13, BEAMWIDTH_DEG, 0.0)

def AddBEAMWIDTH_DEG(builder, BEAMWIDTH_DEG):
    TXSEmissionAddBEAMWIDTH_DEG(builder, BEAMWIDTH_DEG)

def TXSEmissionAddANTENNA_GAIN_DBI(builder, ANTENNA_GAIN_DBI):
    builder.PrependFloat64Slot(14, ANTENNA_GAIN_DBI, 0.0)

def AddANTENNA_GAIN_DBI(builder, ANTENNA_GAIN_DBI):
    TXSEmissionAddANTENNA_GAIN_DBI(builder, ANTENNA_GAIN_DBI)

def TXSEmissionAddSTATION_IDENTITY(builder, STATION_IDENTITY):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(STATION_IDENTITY), 0)

def AddSTATION_IDENTITY(builder, STATION_IDENTITY):
    TXSEmissionAddSTATION_IDENTITY(builder, STATION_IDENTITY)

def TXSEmissionAddSTATUS(builder, STATUS):
    builder.PrependInt8Slot(16, STATUS, 0)

def AddSTATUS(builder, STATUS):
    TXSEmissionAddSTATUS(builder, STATUS)

def TXSEmissionAddVALID_FROM(builder, VALID_FROM):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(VALID_FROM), 0)

def AddVALID_FROM(builder, VALID_FROM):
    TXSEmissionAddVALID_FROM(builder, VALID_FROM)

def TXSEmissionAddVALID_UNTIL(builder, VALID_UNTIL):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(VALID_UNTIL), 0)

def AddVALID_UNTIL(builder, VALID_UNTIL):
    TXSEmissionAddVALID_UNTIL(builder, VALID_UNTIL)

def TXSEmissionEnd(builder):
    return builder.EndObject()

def End(builder):
    return TXSEmissionEnd(builder)


class TXSEmissionT(object):

    # TXSEmissionT
    def __init__(
        self,
        EMISSION_ID = None,
        FREQUENCY_KHZ = 0.0,
        BANDWIDTH_KHZ = 0.0,
        EMISSION_DESIGNATOR = None,
        MODULATION = 0,
        POLARIZATION = 0,
        SERVICE = 0,
        TRANSMITTER_POWER_KW = 0.0,
        EFFECTIVE_RADIATED_POWER_KW = 0.0,
        EIRP_KW = 0.0,
        ANTENNA_HEIGHT_AGL_M = 0.0,
        ANTENNA_AZIMUTH_DEG = 0.0,
        OMNIDIRECTIONAL = False,
        BEAMWIDTH_DEG = 0.0,
        ANTENNA_GAIN_DBI = 0.0,
        STATION_IDENTITY = None,
        STATUS = 0,
        VALID_FROM = None,
        VALID_UNTIL = None,
    ):
        self.EMISSION_ID = EMISSION_ID  # type: Optional[str]
        self.FREQUENCY_KHZ = FREQUENCY_KHZ  # type: float
        self.BANDWIDTH_KHZ = BANDWIDTH_KHZ  # type: float
        self.EMISSION_DESIGNATOR = EMISSION_DESIGNATOR  # type: Optional[str]
        self.MODULATION = MODULATION  # type: int
        self.POLARIZATION = POLARIZATION  # type: int
        self.SERVICE = SERVICE  # type: int
        self.TRANSMITTER_POWER_KW = TRANSMITTER_POWER_KW  # type: float
        self.EFFECTIVE_RADIATED_POWER_KW = EFFECTIVE_RADIATED_POWER_KW  # type: float
        self.EIRP_KW = EIRP_KW  # type: float
        self.ANTENNA_HEIGHT_AGL_M = ANTENNA_HEIGHT_AGL_M  # type: float
        self.ANTENNA_AZIMUTH_DEG = ANTENNA_AZIMUTH_DEG  # type: float
        self.OMNIDIRECTIONAL = OMNIDIRECTIONAL  # type: bool
        self.BEAMWIDTH_DEG = BEAMWIDTH_DEG  # type: float
        self.ANTENNA_GAIN_DBI = ANTENNA_GAIN_DBI  # type: float
        self.STATION_IDENTITY = STATION_IDENTITY  # type: Optional[str]
        self.STATUS = STATUS  # type: int
        self.VALID_FROM = VALID_FROM  # type: Optional[str]
        self.VALID_UNTIL = VALID_UNTIL  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTxsemission = TXSEmission()
        tmpTxsemission.Init(buf, pos)
        return cls.InitFromObj(tmpTxsemission)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTxsemission):
        x = TXSEmissionT()
        x._UnPack(tmpTxsemission)
        return x

    # TXSEmissionT
    def _UnPack(self, TXSEmission):
        if TXSEmission is None:
            return
        self.EMISSION_ID = TXSEmission.EMISSION_ID()
        self.FREQUENCY_KHZ = TXSEmission.FREQUENCY_KHZ()
        self.BANDWIDTH_KHZ = TXSEmission.BANDWIDTH_KHZ()
        self.EMISSION_DESIGNATOR = TXSEmission.EMISSION_DESIGNATOR()
        self.MODULATION = TXSEmission.MODULATION()
        self.POLARIZATION = TXSEmission.POLARIZATION()
        self.SERVICE = TXSEmission.SERVICE()
        self.TRANSMITTER_POWER_KW = TXSEmission.TRANSMITTER_POWER_KW()
        self.EFFECTIVE_RADIATED_POWER_KW = TXSEmission.EFFECTIVE_RADIATED_POWER_KW()
        self.EIRP_KW = TXSEmission.EIRP_KW()
        self.ANTENNA_HEIGHT_AGL_M = TXSEmission.ANTENNA_HEIGHT_AGL_M()
        self.ANTENNA_AZIMUTH_DEG = TXSEmission.ANTENNA_AZIMUTH_DEG()
        self.OMNIDIRECTIONAL = TXSEmission.OMNIDIRECTIONAL()
        self.BEAMWIDTH_DEG = TXSEmission.BEAMWIDTH_DEG()
        self.ANTENNA_GAIN_DBI = TXSEmission.ANTENNA_GAIN_DBI()
        self.STATION_IDENTITY = TXSEmission.STATION_IDENTITY()
        self.STATUS = TXSEmission.STATUS()
        self.VALID_FROM = TXSEmission.VALID_FROM()
        self.VALID_UNTIL = TXSEmission.VALID_UNTIL()

    # TXSEmissionT
    def Pack(self, builder):
        if self.EMISSION_ID is not None:
            EMISSION_ID = builder.CreateString(self.EMISSION_ID)
        if self.EMISSION_DESIGNATOR is not None:
            EMISSION_DESIGNATOR = builder.CreateString(self.EMISSION_DESIGNATOR)
        if self.STATION_IDENTITY is not None:
            STATION_IDENTITY = builder.CreateString(self.STATION_IDENTITY)
        if self.VALID_FROM is not None:
            VALID_FROM = builder.CreateString(self.VALID_FROM)
        if self.VALID_UNTIL is not None:
            VALID_UNTIL = builder.CreateString(self.VALID_UNTIL)
        TXSEmissionStart(builder)
        if self.EMISSION_ID is not None:
            TXSEmissionAddEMISSION_ID(builder, EMISSION_ID)
        TXSEmissionAddFREQUENCY_KHZ(builder, self.FREQUENCY_KHZ)
        TXSEmissionAddBANDWIDTH_KHZ(builder, self.BANDWIDTH_KHZ)
        if self.EMISSION_DESIGNATOR is not None:
            TXSEmissionAddEMISSION_DESIGNATOR(builder, EMISSION_DESIGNATOR)
        TXSEmissionAddMODULATION(builder, self.MODULATION)
        TXSEmissionAddPOLARIZATION(builder, self.POLARIZATION)
        TXSEmissionAddSERVICE(builder, self.SERVICE)
        TXSEmissionAddTRANSMITTER_POWER_KW(builder, self.TRANSMITTER_POWER_KW)
        TXSEmissionAddEFFECTIVE_RADIATED_POWER_KW(builder, self.EFFECTIVE_RADIATED_POWER_KW)
        TXSEmissionAddEIRP_KW(builder, self.EIRP_KW)
        TXSEmissionAddANTENNA_HEIGHT_AGL_M(builder, self.ANTENNA_HEIGHT_AGL_M)
        TXSEmissionAddANTENNA_AZIMUTH_DEG(builder, self.ANTENNA_AZIMUTH_DEG)
        TXSEmissionAddOMNIDIRECTIONAL(builder, self.OMNIDIRECTIONAL)
        TXSEmissionAddBEAMWIDTH_DEG(builder, self.BEAMWIDTH_DEG)
        TXSEmissionAddANTENNA_GAIN_DBI(builder, self.ANTENNA_GAIN_DBI)
        if self.STATION_IDENTITY is not None:
            TXSEmissionAddSTATION_IDENTITY(builder, STATION_IDENTITY)
        TXSEmissionAddSTATUS(builder, self.STATUS)
        if self.VALID_FROM is not None:
            TXSEmissionAddVALID_FROM(builder, VALID_FROM)
        if self.VALID_UNTIL is not None:
            TXSEmissionAddVALID_UNTIL(builder, VALID_UNTIL)
        TXSEmission = TXSEmissionEnd(builder)
        return TXSEmission
