# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# On-Orbit Object List
class OOL(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OOL()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOOL(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OOLBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x4F\x4C", size_prefixed=size_prefixed)

    # OOL
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # OOL
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # OOL
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # OOL
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # OOL
    def ONORBITS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # OOL
    def ONORBITSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # OOL
    def ONORBITSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def OOLStart(builder):
    builder.StartObject(4)

def Start(builder):
    OOLStart(builder)

def OOLAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    OOLAddID(builder, ID)

def OOLAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    OOLAddNAME(builder, NAME)

def OOLAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    OOLAddDESCRIPTION(builder, DESCRIPTION)

def OOLAddONORBITS(builder, ONORBITS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ONORBITS), 0)

def AddONORBITS(builder, ONORBITS):
    OOLAddONORBITS(builder, ONORBITS)

def OOLStartONORBITSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartONORBITSVector(builder, numElems):
    return OOLStartONORBITSVector(builder, numElems)

def OOLEnd(builder):
    return builder.EndObject()

def End(builder):
    return OOLEnd(builder)

try:
    from typing import List
except:
    pass

class OOLT(object):

    # OOLT
    def __init__(self):
        self.ID = None  # type: str
        self.NAME = None  # type: str
        self.DESCRIPTION = None  # type: str
        self.ONORBITS = None  # type: List[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        OOL = OOL()
        OOL.Init(buf, pos)
        return cls.InitFromObj(OOL)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, OOL):
        x = OOLT()
        x._UnPack(OOL)
        return x

    # OOLT
    def _UnPack(self, OOL):
        if OOL is None:
            return
        self.ID = OOL.ID()
        self.NAME = OOL.NAME()
        self.DESCRIPTION = OOL.DESCRIPTION()
        if not OOL.ONORBITSIsNone():
            self.ONORBITS = []
            for i in range(OOL.ONORBITSLength()):
                self.ONORBITS.append(OOL.ONORBITS(i))

    # OOLT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.ONORBITS is not None:
            ONORBITSlist = []
            for i in range(len(self.ONORBITS)):
                ONORBITSlist.append(builder.CreateString(self.ONORBITS[i]))
            OOLStartONORBITSVector(builder, len(self.ONORBITS))
            for i in reversed(range(len(self.ONORBITS))):
                builder.PrependUOffsetTRelative(ONORBITSlist[i])
            ONORBITS = builder.EndVector()
        OOLStart(builder)
        if self.ID is not None:
            OOLAddID(builder, ID)
        if self.NAME is not None:
            OOLAddNAME(builder, NAME)
        if self.DESCRIPTION is not None:
            OOLAddDESCRIPTION(builder, DESCRIPTION)
        if self.ONORBITS is not None:
            OOLAddONORBITS(builder, ONORBITS)
        OOL = OOLEnd(builder)
        return OOL
