# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Satellite Bus Specification
class BUS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BUS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBUS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BUSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x55\x53", size_prefixed=size_prefixed)

    # BUS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Unique identifier
    # BUS
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Bus name or model
    # BUS
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Manufacturer
    # BUS
    def MANUFACTURER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Bus size category
    # BUS
    def SIZE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Dry mass in kg
    # BUS
    def DRY_MASS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum wet mass (with propellant) in kg
    # BUS
    def WET_MASS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum payload mass in kg
    # BUS
    def PAYLOAD_MASS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Bus dimensions X in meters
    # BUS
    def DIM_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Bus dimensions Y in meters
    # BUS
    def DIM_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Bus dimensions Z in meters
    # BUS
    def DIM_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Stowed dimensions X in meters
    # BUS
    def STOWED_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Stowed dimensions Y in meters
    # BUS
    def STOWED_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Stowed dimensions Z in meters
    # BUS
    def STOWED_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Total power generation in Watts
    # BUS
    def POWER_GENERATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Available payload power in Watts
    # BUS
    def PAYLOAD_POWER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Battery capacity in Watt-hours
    # BUS
    def BATTERY_CAPACITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Stabilization method
    # BUS
    def STABILIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Pointing accuracy in degrees
    # BUS
    def POINTING_ACCURACY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Pointing knowledge in degrees
    # BUS
    def POINTING_KNOWLEDGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Design life in years
    # BUS
    def DESIGN_LIFE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Data storage capacity in Gbits
    # BUS
    def DATA_STORAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Downlink data rate in Mbps
    # BUS
    def DOWNLINK_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Number of payload slots/interfaces
    # BUS
    def PAYLOAD_SLOTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Heritage missions count
    # BUS
    def HERITAGE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Additional notes
    # BUS
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def BUSStart(builder):
    builder.StartObject(25)

def Start(builder):
    BUSStart(builder)

def BUSAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    BUSAddID(builder, ID)

def BUSAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    BUSAddNAME(builder, NAME)

def BUSAddMANUFACTURER(builder, MANUFACTURER):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MANUFACTURER), 0)

def AddMANUFACTURER(builder, MANUFACTURER):
    BUSAddMANUFACTURER(builder, MANUFACTURER)

def BUSAddSIZE(builder, SIZE):
    builder.PrependInt8Slot(3, SIZE, 0)

def AddSIZE(builder, SIZE):
    BUSAddSIZE(builder, SIZE)

def BUSAddDRY_MASS(builder, DRY_MASS):
    builder.PrependFloat64Slot(4, DRY_MASS, 0.0)

def AddDRY_MASS(builder, DRY_MASS):
    BUSAddDRY_MASS(builder, DRY_MASS)

def BUSAddWET_MASS(builder, WET_MASS):
    builder.PrependFloat64Slot(5, WET_MASS, 0.0)

def AddWET_MASS(builder, WET_MASS):
    BUSAddWET_MASS(builder, WET_MASS)

def BUSAddPAYLOAD_MASS(builder, PAYLOAD_MASS):
    builder.PrependFloat64Slot(6, PAYLOAD_MASS, 0.0)

def AddPAYLOAD_MASS(builder, PAYLOAD_MASS):
    BUSAddPAYLOAD_MASS(builder, PAYLOAD_MASS)

def BUSAddDIM_X(builder, DIM_X):
    builder.PrependFloat64Slot(7, DIM_X, 0.0)

def AddDIM_X(builder, DIM_X):
    BUSAddDIM_X(builder, DIM_X)

def BUSAddDIM_Y(builder, DIM_Y):
    builder.PrependFloat64Slot(8, DIM_Y, 0.0)

def AddDIM_Y(builder, DIM_Y):
    BUSAddDIM_Y(builder, DIM_Y)

def BUSAddDIM_Z(builder, DIM_Z):
    builder.PrependFloat64Slot(9, DIM_Z, 0.0)

def AddDIM_Z(builder, DIM_Z):
    BUSAddDIM_Z(builder, DIM_Z)

def BUSAddSTOWED_X(builder, STOWED_X):
    builder.PrependFloat64Slot(10, STOWED_X, 0.0)

def AddSTOWED_X(builder, STOWED_X):
    BUSAddSTOWED_X(builder, STOWED_X)

def BUSAddSTOWED_Y(builder, STOWED_Y):
    builder.PrependFloat64Slot(11, STOWED_Y, 0.0)

def AddSTOWED_Y(builder, STOWED_Y):
    BUSAddSTOWED_Y(builder, STOWED_Y)

def BUSAddSTOWED_Z(builder, STOWED_Z):
    builder.PrependFloat64Slot(12, STOWED_Z, 0.0)

def AddSTOWED_Z(builder, STOWED_Z):
    BUSAddSTOWED_Z(builder, STOWED_Z)

def BUSAddPOWER_GENERATION(builder, POWER_GENERATION):
    builder.PrependFloat64Slot(13, POWER_GENERATION, 0.0)

def AddPOWER_GENERATION(builder, POWER_GENERATION):
    BUSAddPOWER_GENERATION(builder, POWER_GENERATION)

def BUSAddPAYLOAD_POWER(builder, PAYLOAD_POWER):
    builder.PrependFloat64Slot(14, PAYLOAD_POWER, 0.0)

def AddPAYLOAD_POWER(builder, PAYLOAD_POWER):
    BUSAddPAYLOAD_POWER(builder, PAYLOAD_POWER)

def BUSAddBATTERY_CAPACITY(builder, BATTERY_CAPACITY):
    builder.PrependFloat64Slot(15, BATTERY_CAPACITY, 0.0)

def AddBATTERY_CAPACITY(builder, BATTERY_CAPACITY):
    BUSAddBATTERY_CAPACITY(builder, BATTERY_CAPACITY)

def BUSAddSTABILIZATION(builder, STABILIZATION):
    builder.PrependInt8Slot(16, STABILIZATION, 0)

def AddSTABILIZATION(builder, STABILIZATION):
    BUSAddSTABILIZATION(builder, STABILIZATION)

def BUSAddPOINTING_ACCURACY(builder, POINTING_ACCURACY):
    builder.PrependFloat64Slot(17, POINTING_ACCURACY, 0.0)

def AddPOINTING_ACCURACY(builder, POINTING_ACCURACY):
    BUSAddPOINTING_ACCURACY(builder, POINTING_ACCURACY)

def BUSAddPOINTING_KNOWLEDGE(builder, POINTING_KNOWLEDGE):
    builder.PrependFloat64Slot(18, POINTING_KNOWLEDGE, 0.0)

def AddPOINTING_KNOWLEDGE(builder, POINTING_KNOWLEDGE):
    BUSAddPOINTING_KNOWLEDGE(builder, POINTING_KNOWLEDGE)

def BUSAddDESIGN_LIFE(builder, DESIGN_LIFE):
    builder.PrependFloat64Slot(19, DESIGN_LIFE, 0.0)

def AddDESIGN_LIFE(builder, DESIGN_LIFE):
    BUSAddDESIGN_LIFE(builder, DESIGN_LIFE)

def BUSAddDATA_STORAGE(builder, DATA_STORAGE):
    builder.PrependFloat64Slot(20, DATA_STORAGE, 0.0)

def AddDATA_STORAGE(builder, DATA_STORAGE):
    BUSAddDATA_STORAGE(builder, DATA_STORAGE)

def BUSAddDOWNLINK_RATE(builder, DOWNLINK_RATE):
    builder.PrependFloat64Slot(21, DOWNLINK_RATE, 0.0)

def AddDOWNLINK_RATE(builder, DOWNLINK_RATE):
    BUSAddDOWNLINK_RATE(builder, DOWNLINK_RATE)

def BUSAddPAYLOAD_SLOTS(builder, PAYLOAD_SLOTS):
    builder.PrependUint32Slot(22, PAYLOAD_SLOTS, 0)

def AddPAYLOAD_SLOTS(builder, PAYLOAD_SLOTS):
    BUSAddPAYLOAD_SLOTS(builder, PAYLOAD_SLOTS)

def BUSAddHERITAGE_COUNT(builder, HERITAGE_COUNT):
    builder.PrependUint32Slot(23, HERITAGE_COUNT, 0)

def AddHERITAGE_COUNT(builder, HERITAGE_COUNT):
    BUSAddHERITAGE_COUNT(builder, HERITAGE_COUNT)

def BUSAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(24, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    BUSAddNOTES(builder, NOTES)

def BUSEnd(builder):
    return builder.EndObject()

def End(builder):
    return BUSEnd(builder)


class BUST(object):

    # BUST
    def __init__(self):
        self.ID = None  # type: str
        self.NAME = None  # type: str
        self.MANUFACTURER = None  # type: str
        self.SIZE = 0  # type: int
        self.DRY_MASS = 0.0  # type: float
        self.WET_MASS = 0.0  # type: float
        self.PAYLOAD_MASS = 0.0  # type: float
        self.DIM_X = 0.0  # type: float
        self.DIM_Y = 0.0  # type: float
        self.DIM_Z = 0.0  # type: float
        self.STOWED_X = 0.0  # type: float
        self.STOWED_Y = 0.0  # type: float
        self.STOWED_Z = 0.0  # type: float
        self.POWER_GENERATION = 0.0  # type: float
        self.PAYLOAD_POWER = 0.0  # type: float
        self.BATTERY_CAPACITY = 0.0  # type: float
        self.STABILIZATION = 0  # type: int
        self.POINTING_ACCURACY = 0.0  # type: float
        self.POINTING_KNOWLEDGE = 0.0  # type: float
        self.DESIGN_LIFE = 0.0  # type: float
        self.DATA_STORAGE = 0.0  # type: float
        self.DOWNLINK_RATE = 0.0  # type: float
        self.PAYLOAD_SLOTS = 0  # type: int
        self.HERITAGE_COUNT = 0  # type: int
        self.NOTES = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        BUS = BUS()
        BUS.Init(buf, pos)
        return cls.InitFromObj(BUS)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, BUS):
        x = BUST()
        x._UnPack(BUS)
        return x

    # BUST
    def _UnPack(self, BUS):
        if BUS is None:
            return
        self.ID = BUS.ID()
        self.NAME = BUS.NAME()
        self.MANUFACTURER = BUS.MANUFACTURER()
        self.SIZE = BUS.SIZE()
        self.DRY_MASS = BUS.DRY_MASS()
        self.WET_MASS = BUS.WET_MASS()
        self.PAYLOAD_MASS = BUS.PAYLOAD_MASS()
        self.DIM_X = BUS.DIM_X()
        self.DIM_Y = BUS.DIM_Y()
        self.DIM_Z = BUS.DIM_Z()
        self.STOWED_X = BUS.STOWED_X()
        self.STOWED_Y = BUS.STOWED_Y()
        self.STOWED_Z = BUS.STOWED_Z()
        self.POWER_GENERATION = BUS.POWER_GENERATION()
        self.PAYLOAD_POWER = BUS.PAYLOAD_POWER()
        self.BATTERY_CAPACITY = BUS.BATTERY_CAPACITY()
        self.STABILIZATION = BUS.STABILIZATION()
        self.POINTING_ACCURACY = BUS.POINTING_ACCURACY()
        self.POINTING_KNOWLEDGE = BUS.POINTING_KNOWLEDGE()
        self.DESIGN_LIFE = BUS.DESIGN_LIFE()
        self.DATA_STORAGE = BUS.DATA_STORAGE()
        self.DOWNLINK_RATE = BUS.DOWNLINK_RATE()
        self.PAYLOAD_SLOTS = BUS.PAYLOAD_SLOTS()
        self.HERITAGE_COUNT = BUS.HERITAGE_COUNT()
        self.NOTES = BUS.NOTES()

    # BUST
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.MANUFACTURER is not None:
            MANUFACTURER = builder.CreateString(self.MANUFACTURER)
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        BUSStart(builder)
        if self.ID is not None:
            BUSAddID(builder, ID)
        if self.NAME is not None:
            BUSAddNAME(builder, NAME)
        if self.MANUFACTURER is not None:
            BUSAddMANUFACTURER(builder, MANUFACTURER)
        BUSAddSIZE(builder, self.SIZE)
        BUSAddDRY_MASS(builder, self.DRY_MASS)
        BUSAddWET_MASS(builder, self.WET_MASS)
        BUSAddPAYLOAD_MASS(builder, self.PAYLOAD_MASS)
        BUSAddDIM_X(builder, self.DIM_X)
        BUSAddDIM_Y(builder, self.DIM_Y)
        BUSAddDIM_Z(builder, self.DIM_Z)
        BUSAddSTOWED_X(builder, self.STOWED_X)
        BUSAddSTOWED_Y(builder, self.STOWED_Y)
        BUSAddSTOWED_Z(builder, self.STOWED_Z)
        BUSAddPOWER_GENERATION(builder, self.POWER_GENERATION)
        BUSAddPAYLOAD_POWER(builder, self.PAYLOAD_POWER)
        BUSAddBATTERY_CAPACITY(builder, self.BATTERY_CAPACITY)
        BUSAddSTABILIZATION(builder, self.STABILIZATION)
        BUSAddPOINTING_ACCURACY(builder, self.POINTING_ACCURACY)
        BUSAddPOINTING_KNOWLEDGE(builder, self.POINTING_KNOWLEDGE)
        BUSAddDESIGN_LIFE(builder, self.DESIGN_LIFE)
        BUSAddDATA_STORAGE(builder, self.DATA_STORAGE)
        BUSAddDOWNLINK_RATE(builder, self.DOWNLINK_RATE)
        BUSAddPAYLOAD_SLOTS(builder, self.PAYLOAD_SLOTS)
        BUSAddHERITAGE_COUNT(builder, self.HERITAGE_COUNT)
        if self.NOTES is not None:
            BUSAddNOTES(builder, NOTES)
        BUS = BUSEnd(builder)
        return BUS
