# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Collection of Rocket Configurations
class ROCCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ROCCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsROCCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ROCCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x4F\x43", size_prefixed=size_prefixed)

    # ROCCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Records of Rocket Configurations
    # ROCCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ROC import ROC
            obj = ROC()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ROCCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ROCCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ROCCOLLECTIONStart(builder): builder.StartObject(1)
def Start(builder):
    return ROCCOLLECTIONStart(builder)
def ROCCOLLECTIONAddRECORDS(builder, RECORDS): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)
def AddRECORDS(builder, RECORDS):
    return ROCCOLLECTIONAddRECORDS(builder, RECORDS)
def ROCCOLLECTIONStartRECORDSVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartRECORDSVector(builder, numElems):
    return ROCCOLLECTIONStartRECORDSVector(builder, numElems)
def ROCCOLLECTIONEnd(builder): return builder.EndObject()
def End(builder):
    return ROCCOLLECTIONEnd(builder)
import ROC
try:
    from typing import List
except:
    pass

class ROCCOLLECTIONT(object):

    # ROCCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[ROC.ROCT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ROCCOLLECTION = ROCCOLLECTION()
        ROCCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(ROCCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ROCCOLLECTION):
        x = ROCCOLLECTIONT()
        x._UnPack(ROCCOLLECTION)
        return x

    # ROCCOLLECTIONT
    def _UnPack(self, ROCCOLLECTION):
        if ROCCOLLECTION is None:
            return
        if not ROCCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(ROCCOLLECTION.RECORDSLength()):
                if ROCCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    rOC_ = ROC.ROCT.InitFromObj(ROCCOLLECTION.RECORDS(i))
                    self.RECORDS.append(rOC_)

    # ROCCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            ROCCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        ROCCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            ROCCOLLECTIONAddRECORDS(builder, RECORDS)
        ROCCOLLECTION = ROCCOLLECTIONEnd(builder)
        return ROCCOLLECTION
