# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Geometry, material, texture, and bounds measurements.
class VAMMetrics(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VAMMetrics()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVAMMetrics(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VAMMetricsBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x41\x4D", size_prefixed=size_prefixed)

    # VAMMetrics
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # VAMMetrics
    def TRIANGLE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def VERTEX_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def PRIMITIVE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def DRAW_CALL_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def MATERIAL_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def TEXTURE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def TEXTURE_BYTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def ANIMATION_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # VAMMetrics
    def BOUNDING_BOX_MIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VAMVector3 import VAMVector3
            obj = VAMVector3()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VAMMetrics
    def BOUNDING_BOX_MAX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VAMVector3 import VAMVector3
            obj = VAMVector3()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VAMMetrics
    def DIMENSIONS_METERS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VAMVector3 import VAMVector3
            obj = VAMVector3()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def VAMMetricsStart(builder):
    builder.StartObject(11)

def Start(builder):
    VAMMetricsStart(builder)

def VAMMetricsAddTRIANGLE_COUNT(builder, TRIANGLE_COUNT):
    builder.PrependUint64Slot(0, TRIANGLE_COUNT, 0)

def AddTRIANGLE_COUNT(builder, TRIANGLE_COUNT):
    VAMMetricsAddTRIANGLE_COUNT(builder, TRIANGLE_COUNT)

def VAMMetricsAddVERTEX_COUNT(builder, VERTEX_COUNT):
    builder.PrependUint64Slot(1, VERTEX_COUNT, 0)

def AddVERTEX_COUNT(builder, VERTEX_COUNT):
    VAMMetricsAddVERTEX_COUNT(builder, VERTEX_COUNT)

def VAMMetricsAddPRIMITIVE_COUNT(builder, PRIMITIVE_COUNT):
    builder.PrependUint32Slot(2, PRIMITIVE_COUNT, 0)

def AddPRIMITIVE_COUNT(builder, PRIMITIVE_COUNT):
    VAMMetricsAddPRIMITIVE_COUNT(builder, PRIMITIVE_COUNT)

def VAMMetricsAddDRAW_CALL_COUNT(builder, DRAW_CALL_COUNT):
    builder.PrependUint32Slot(3, DRAW_CALL_COUNT, 0)

def AddDRAW_CALL_COUNT(builder, DRAW_CALL_COUNT):
    VAMMetricsAddDRAW_CALL_COUNT(builder, DRAW_CALL_COUNT)

def VAMMetricsAddMATERIAL_COUNT(builder, MATERIAL_COUNT):
    builder.PrependUint32Slot(4, MATERIAL_COUNT, 0)

def AddMATERIAL_COUNT(builder, MATERIAL_COUNT):
    VAMMetricsAddMATERIAL_COUNT(builder, MATERIAL_COUNT)

def VAMMetricsAddTEXTURE_COUNT(builder, TEXTURE_COUNT):
    builder.PrependUint32Slot(5, TEXTURE_COUNT, 0)

def AddTEXTURE_COUNT(builder, TEXTURE_COUNT):
    VAMMetricsAddTEXTURE_COUNT(builder, TEXTURE_COUNT)

def VAMMetricsAddTEXTURE_BYTES(builder, TEXTURE_BYTES):
    builder.PrependUint64Slot(6, TEXTURE_BYTES, 0)

def AddTEXTURE_BYTES(builder, TEXTURE_BYTES):
    VAMMetricsAddTEXTURE_BYTES(builder, TEXTURE_BYTES)

def VAMMetricsAddANIMATION_COUNT(builder, ANIMATION_COUNT):
    builder.PrependUint32Slot(7, ANIMATION_COUNT, 0)

def AddANIMATION_COUNT(builder, ANIMATION_COUNT):
    VAMMetricsAddANIMATION_COUNT(builder, ANIMATION_COUNT)

def VAMMetricsAddBOUNDING_BOX_MIN(builder, BOUNDING_BOX_MIN):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(BOUNDING_BOX_MIN), 0)

def AddBOUNDING_BOX_MIN(builder, BOUNDING_BOX_MIN):
    VAMMetricsAddBOUNDING_BOX_MIN(builder, BOUNDING_BOX_MIN)

def VAMMetricsAddBOUNDING_BOX_MAX(builder, BOUNDING_BOX_MAX):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(BOUNDING_BOX_MAX), 0)

def AddBOUNDING_BOX_MAX(builder, BOUNDING_BOX_MAX):
    VAMMetricsAddBOUNDING_BOX_MAX(builder, BOUNDING_BOX_MAX)

def VAMMetricsAddDIMENSIONS_METERS(builder, DIMENSIONS_METERS):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(DIMENSIONS_METERS), 0)

def AddDIMENSIONS_METERS(builder, DIMENSIONS_METERS):
    VAMMetricsAddDIMENSIONS_METERS(builder, DIMENSIONS_METERS)

def VAMMetricsEnd(builder):
    return builder.EndObject()

def End(builder):
    return VAMMetricsEnd(builder)

import VAMVector3
try:
    from typing import Optional
except:
    pass

class VAMMetricsT(object):

    # VAMMetricsT
    def __init__(
        self,
        TRIANGLE_COUNT = 0,
        VERTEX_COUNT = 0,
        PRIMITIVE_COUNT = 0,
        DRAW_CALL_COUNT = 0,
        MATERIAL_COUNT = 0,
        TEXTURE_COUNT = 0,
        TEXTURE_BYTES = 0,
        ANIMATION_COUNT = 0,
        BOUNDING_BOX_MIN = None,
        BOUNDING_BOX_MAX = None,
        DIMENSIONS_METERS = None,
    ):
        self.TRIANGLE_COUNT = TRIANGLE_COUNT  # type: int
        self.VERTEX_COUNT = VERTEX_COUNT  # type: int
        self.PRIMITIVE_COUNT = PRIMITIVE_COUNT  # type: int
        self.DRAW_CALL_COUNT = DRAW_CALL_COUNT  # type: int
        self.MATERIAL_COUNT = MATERIAL_COUNT  # type: int
        self.TEXTURE_COUNT = TEXTURE_COUNT  # type: int
        self.TEXTURE_BYTES = TEXTURE_BYTES  # type: int
        self.ANIMATION_COUNT = ANIMATION_COUNT  # type: int
        self.BOUNDING_BOX_MIN = BOUNDING_BOX_MIN  # type: Optional[VAMVector3.VAMVector3T]
        self.BOUNDING_BOX_MAX = BOUNDING_BOX_MAX  # type: Optional[VAMVector3.VAMVector3T]
        self.DIMENSIONS_METERS = DIMENSIONS_METERS  # type: Optional[VAMVector3.VAMVector3T]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVammetrics = VAMMetrics()
        tmpVammetrics.Init(buf, pos)
        return cls.InitFromObj(tmpVammetrics)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVammetrics):
        x = VAMMetricsT()
        x._UnPack(tmpVammetrics)
        return x

    # VAMMetricsT
    def _UnPack(self, VAMMetrics):
        if VAMMetrics is None:
            return
        self.TRIANGLE_COUNT = VAMMetrics.TRIANGLE_COUNT()
        self.VERTEX_COUNT = VAMMetrics.VERTEX_COUNT()
        self.PRIMITIVE_COUNT = VAMMetrics.PRIMITIVE_COUNT()
        self.DRAW_CALL_COUNT = VAMMetrics.DRAW_CALL_COUNT()
        self.MATERIAL_COUNT = VAMMetrics.MATERIAL_COUNT()
        self.TEXTURE_COUNT = VAMMetrics.TEXTURE_COUNT()
        self.TEXTURE_BYTES = VAMMetrics.TEXTURE_BYTES()
        self.ANIMATION_COUNT = VAMMetrics.ANIMATION_COUNT()
        if VAMMetrics.BOUNDING_BOX_MIN() is not None:
            self.BOUNDING_BOX_MIN = VAMVector3.VAMVector3T.InitFromObj(VAMMetrics.BOUNDING_BOX_MIN())
        if VAMMetrics.BOUNDING_BOX_MAX() is not None:
            self.BOUNDING_BOX_MAX = VAMVector3.VAMVector3T.InitFromObj(VAMMetrics.BOUNDING_BOX_MAX())
        if VAMMetrics.DIMENSIONS_METERS() is not None:
            self.DIMENSIONS_METERS = VAMVector3.VAMVector3T.InitFromObj(VAMMetrics.DIMENSIONS_METERS())

    # VAMMetricsT
    def Pack(self, builder):
        if self.BOUNDING_BOX_MIN is not None:
            BOUNDING_BOX_MIN = self.BOUNDING_BOX_MIN.Pack(builder)
        if self.BOUNDING_BOX_MAX is not None:
            BOUNDING_BOX_MAX = self.BOUNDING_BOX_MAX.Pack(builder)
        if self.DIMENSIONS_METERS is not None:
            DIMENSIONS_METERS = self.DIMENSIONS_METERS.Pack(builder)
        VAMMetricsStart(builder)
        VAMMetricsAddTRIANGLE_COUNT(builder, self.TRIANGLE_COUNT)
        VAMMetricsAddVERTEX_COUNT(builder, self.VERTEX_COUNT)
        VAMMetricsAddPRIMITIVE_COUNT(builder, self.PRIMITIVE_COUNT)
        VAMMetricsAddDRAW_CALL_COUNT(builder, self.DRAW_CALL_COUNT)
        VAMMetricsAddMATERIAL_COUNT(builder, self.MATERIAL_COUNT)
        VAMMetricsAddTEXTURE_COUNT(builder, self.TEXTURE_COUNT)
        VAMMetricsAddTEXTURE_BYTES(builder, self.TEXTURE_BYTES)
        VAMMetricsAddANIMATION_COUNT(builder, self.ANIMATION_COUNT)
        if self.BOUNDING_BOX_MIN is not None:
            VAMMetricsAddBOUNDING_BOX_MIN(builder, BOUNDING_BOX_MIN)
        if self.BOUNDING_BOX_MAX is not None:
            VAMMetricsAddBOUNDING_BOX_MAX(builder, BOUNDING_BOX_MAX)
        if self.DIMENSIONS_METERS is not None:
            VAMMetricsAddDIMENSIONS_METERS(builder, DIMENSIONS_METERS)
        VAMMetrics = VAMMetricsEnd(builder)
        return VAMMetrics
