# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Request envelope carried by `plugin_invoke_stream`.
class PIVRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PIVRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPIVRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PIVRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x49\x56", size_prefixed=size_prefixed)

    # PIVRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable method identifier from PLG.METHODS[].METHOD_ID.
    # PIVRequest
    def METHOD_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Input frames. Each TAB.TYPE_REF names the payload schema and each
    # TAB.WIRE_FORMAT selects FLATBUFFER or ALIGNED_BINARY. TAB.OFFSET
    # is arena-relative into PAYLOAD_ARENA (or an absolute pointer when
    # PAYLOAD_ARENA is empty).
    # PIVRequest
    def INPUTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TAB import TAB
            obj = TAB()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PIVRequest
    def INPUTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PIVRequest
    def INPUTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Optional arena backing the INPUTS payload bodies. Empty means the
    # caller manages arena state out-of-band.
    # PIVRequest
    def PAYLOAD_ARENA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # PIVRequest
    def PAYLOAD_ARENAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # PIVRequest
    def PAYLOAD_ARENALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PIVRequest
    def PAYLOAD_ARENAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Optional trace identifier for request correlation.
    # PIVRequest
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Optional yield-budget hint. Plugins MAY pause queued work after
    # draining this many output frames and return YIELDED for the caller
    # to resume. 0 = no hint.
    # PIVRequest
    def OUTPUT_STREAM_CAP(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def PIVRequestStart(builder):
    builder.StartObject(5)

def Start(builder):
    PIVRequestStart(builder)

def PIVRequestAddMETHOD_ID(builder, METHOD_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(METHOD_ID), 0)

def AddMETHOD_ID(builder, METHOD_ID):
    PIVRequestAddMETHOD_ID(builder, METHOD_ID)

def PIVRequestAddINPUTS(builder, INPUTS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(INPUTS), 0)

def AddINPUTS(builder, INPUTS):
    PIVRequestAddINPUTS(builder, INPUTS)

def PIVRequestStartINPUTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartINPUTSVector(builder, numElems):
    return PIVRequestStartINPUTSVector(builder, numElems)

def PIVRequestCreateINPUTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateINPUTSVector(builder, data):
    PIVRequestCreateINPUTSVector(builder, data)

def PIVRequestAddPAYLOAD_ARENA(builder, PAYLOAD_ARENA):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD_ARENA), 0)

def AddPAYLOAD_ARENA(builder, PAYLOAD_ARENA):
    PIVRequestAddPAYLOAD_ARENA(builder, PAYLOAD_ARENA)

def PIVRequestStartPAYLOAD_ARENAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPAYLOAD_ARENAVector(builder, numElems):
    return PIVRequestStartPAYLOAD_ARENAVector(builder, numElems)

def PIVRequestCreatePAYLOAD_ARENAVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePAYLOAD_ARENAVector(builder, data):
    PIVRequestCreatePAYLOAD_ARENAVector(builder, data)

def PIVRequestAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUint64Slot(3, TRACE_ID, 0)

def AddTRACE_ID(builder, TRACE_ID):
    PIVRequestAddTRACE_ID(builder, TRACE_ID)

def PIVRequestAddOUTPUT_STREAM_CAP(builder, OUTPUT_STREAM_CAP):
    builder.PrependUint32Slot(4, OUTPUT_STREAM_CAP, 0)

def AddOUTPUT_STREAM_CAP(builder, OUTPUT_STREAM_CAP):
    PIVRequestAddOUTPUT_STREAM_CAP(builder, OUTPUT_STREAM_CAP)

def PIVRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return PIVRequestEnd(builder)

import TAB
try:
    from typing import List
except:
    pass

class PIVRequestT(object):

    # PIVRequestT
    def __init__(
        self,
        METHOD_ID = None,
        INPUTS = None,
        PAYLOAD_ARENA = None,
        TRACE_ID = 0,
        OUTPUT_STREAM_CAP = 0,
    ):
        self.METHOD_ID = METHOD_ID  # type: Optional[str]
        self.INPUTS = INPUTS  # type: Optional[List[TAB.TABT]]
        self.PAYLOAD_ARENA = PAYLOAD_ARENA  # type: Optional[List[int]]
        self.TRACE_ID = TRACE_ID  # type: int
        self.OUTPUT_STREAM_CAP = OUTPUT_STREAM_CAP  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPivrequest = PIVRequest()
        tmpPivrequest.Init(buf, pos)
        return cls.InitFromObj(tmpPivrequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPivrequest):
        x = PIVRequestT()
        x._UnPack(tmpPivrequest)
        return x

    # PIVRequestT
    def _UnPack(self, PIVRequest):
        if PIVRequest is None:
            return
        self.METHOD_ID = PIVRequest.METHOD_ID()
        if not PIVRequest.INPUTSIsNone():
            self.INPUTS = []
            for i in range(PIVRequest.INPUTSLength()):
                if PIVRequest.INPUTS(i) is None:
                    self.INPUTS.append(None)
                else:
                    tAB_ = TAB.TABT.InitFromObj(PIVRequest.INPUTS(i))
                    self.INPUTS.append(tAB_)
        if not PIVRequest.PAYLOAD_ARENAIsNone():
            if np is None:
                self.PAYLOAD_ARENA = []
                for i in range(PIVRequest.PAYLOAD_ARENALength()):
                    self.PAYLOAD_ARENA.append(PIVRequest.PAYLOAD_ARENA(i))
            else:
                self.PAYLOAD_ARENA = PIVRequest.PAYLOAD_ARENAAsNumpy()
        self.TRACE_ID = PIVRequest.TRACE_ID()
        self.OUTPUT_STREAM_CAP = PIVRequest.OUTPUT_STREAM_CAP()

    # PIVRequestT
    def Pack(self, builder):
        if self.METHOD_ID is not None:
            METHOD_ID = builder.CreateString(self.METHOD_ID)
        if self.INPUTS is not None:
            INPUTSlist = []
            for i in range(len(self.INPUTS)):
                INPUTSlist.append(self.INPUTS[i].Pack(builder))
            PIVRequestStartINPUTSVector(builder, len(self.INPUTS))
            for i in reversed(range(len(self.INPUTS))):
                builder.PrependUOffsetTRelative(INPUTSlist[i])
            INPUTS = builder.EndVector()
        if self.PAYLOAD_ARENA is not None:
            if np is not None and type(self.PAYLOAD_ARENA) is np.ndarray:
                PAYLOAD_ARENA = builder.CreateNumpyVector(self.PAYLOAD_ARENA)
            else:
                PIVRequestStartPAYLOAD_ARENAVector(builder, len(self.PAYLOAD_ARENA))
                for i in reversed(range(len(self.PAYLOAD_ARENA))):
                    builder.PrependUint8(self.PAYLOAD_ARENA[i])
                PAYLOAD_ARENA = builder.EndVector()
        PIVRequestStart(builder)
        if self.METHOD_ID is not None:
            PIVRequestAddMETHOD_ID(builder, METHOD_ID)
        if self.INPUTS is not None:
            PIVRequestAddINPUTS(builder, INPUTS)
        if self.PAYLOAD_ARENA is not None:
            PIVRequestAddPAYLOAD_ARENA(builder, PAYLOAD_ARENA)
        PIVRequestAddTRACE_ID(builder, self.TRACE_ID)
        PIVRequestAddOUTPUT_STREAM_CAP(builder, self.OUTPUT_STREAM_CAP)
        PIVRequest = PIVRequestEnd(builder)
        return PIVRequest
