# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PRGCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRGCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRGCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRGCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x47", size_prefixed=size_prefixed)

    # PRGCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRGCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PRG import PRG
            obj = PRG()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRGCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRGCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def PRGCOLLECTIONStart(builder): builder.StartObject(1)
def Start(builder):
    return PRGCOLLECTIONStart(builder)
def PRGCOLLECTIONAddRECORDS(builder, RECORDS): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)
def AddRECORDS(builder, RECORDS):
    return PRGCOLLECTIONAddRECORDS(builder, RECORDS)
def PRGCOLLECTIONStartRECORDSVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartRECORDSVector(builder, numElems):
    return PRGCOLLECTIONStartRECORDSVector(builder, numElems)
def PRGCOLLECTIONEnd(builder): return builder.EndObject()
def End(builder):
    return PRGCOLLECTIONEnd(builder)
import PRG
try:
    from typing import List
except:
    pass

class PRGCOLLECTIONT(object):

    # PRGCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[PRG.PRGT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        PRGCOLLECTION = PRGCOLLECTION()
        PRGCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(PRGCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, PRGCOLLECTION):
        x = PRGCOLLECTIONT()
        x._UnPack(PRGCOLLECTION)
        return x

    # PRGCOLLECTIONT
    def _UnPack(self, PRGCOLLECTION):
        if PRGCOLLECTION is None:
            return
        if not PRGCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(PRGCOLLECTION.RECORDSLength()):
                if PRGCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    pRG_ = PRG.PRGT.InitFromObj(PRGCOLLECTION.RECORDS(i))
                    self.RECORDS.append(pRG_)

    # PRGCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            PRGCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        PRGCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            PRGCOLLECTIONAddRECORDS(builder, RECORDS)
        PRGCOLLECTION = PRGCOLLECTIONEnd(builder)
        return PRGCOLLECTION
