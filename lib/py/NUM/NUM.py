# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Numerical utility envelope.
class NUM(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NUM()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNUM(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NUMBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x55\x4D", size_prefixed=size_prefixed)

    # NUM
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # NUM
    def ROOT_SOLVE_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMRootSolveRequest import NUMRootSolveRequest
            obj = NUMRootSolveRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def ROOT_SOLVE_RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMRootSolveResult import NUMRootSolveResult
            obj = NUMRootSolveResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def VECTOR_SATURATE_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMVectorSaturateRequest import NUMVectorSaturateRequest
            obj = NUMVectorSaturateRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def VECTOR_SATURATE_RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMVectorSaturateResult import NUMVectorSaturateResult
            obj = NUMVectorSaturateResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def VECTOR_DISCRETIZE_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMVectorDiscretizeRequest import NUMVectorDiscretizeRequest
            obj = NUMVectorDiscretizeRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def VECTOR_DISCRETIZE_RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMVectorDiscretizeResult import NUMVectorDiscretizeResult
            obj = NUMVectorDiscretizeResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def SCALAR_INTERPOLATION_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMScalarInterpolationRequest import NUMScalarInterpolationRequest
            obj = NUMScalarInterpolationRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def SCALAR_INTERPOLATION_RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMScalarInterpolationResult import NUMScalarInterpolationResult
            obj = NUMScalarInterpolationResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def GAUSS_MARKOV_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMGaussMarkovRequest import NUMGaussMarkovRequest
            obj = NUMGaussMarkovRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NUM
    def GAUSS_MARKOV_RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NUMGaussMarkovResult import NUMGaussMarkovResult
            obj = NUMGaussMarkovResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def NUMStart(builder):
    builder.StartObject(10)

def Start(builder):
    NUMStart(builder)

def NUMAddROOT_SOLVE_REQUEST(builder, ROOT_SOLVE_REQUEST):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ROOT_SOLVE_REQUEST), 0)

def AddROOT_SOLVE_REQUEST(builder, ROOT_SOLVE_REQUEST):
    NUMAddROOT_SOLVE_REQUEST(builder, ROOT_SOLVE_REQUEST)

def NUMAddROOT_SOLVE_RESULT(builder, ROOT_SOLVE_RESULT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ROOT_SOLVE_RESULT), 0)

def AddROOT_SOLVE_RESULT(builder, ROOT_SOLVE_RESULT):
    NUMAddROOT_SOLVE_RESULT(builder, ROOT_SOLVE_RESULT)

def NUMAddVECTOR_SATURATE_REQUEST(builder, VECTOR_SATURATE_REQUEST):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VECTOR_SATURATE_REQUEST), 0)

def AddVECTOR_SATURATE_REQUEST(builder, VECTOR_SATURATE_REQUEST):
    NUMAddVECTOR_SATURATE_REQUEST(builder, VECTOR_SATURATE_REQUEST)

def NUMAddVECTOR_SATURATE_RESULT(builder, VECTOR_SATURATE_RESULT):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(VECTOR_SATURATE_RESULT), 0)

def AddVECTOR_SATURATE_RESULT(builder, VECTOR_SATURATE_RESULT):
    NUMAddVECTOR_SATURATE_RESULT(builder, VECTOR_SATURATE_RESULT)

def NUMAddVECTOR_DISCRETIZE_REQUEST(builder, VECTOR_DISCRETIZE_REQUEST):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(VECTOR_DISCRETIZE_REQUEST), 0)

def AddVECTOR_DISCRETIZE_REQUEST(builder, VECTOR_DISCRETIZE_REQUEST):
    NUMAddVECTOR_DISCRETIZE_REQUEST(builder, VECTOR_DISCRETIZE_REQUEST)

def NUMAddVECTOR_DISCRETIZE_RESULT(builder, VECTOR_DISCRETIZE_RESULT):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(VECTOR_DISCRETIZE_RESULT), 0)

def AddVECTOR_DISCRETIZE_RESULT(builder, VECTOR_DISCRETIZE_RESULT):
    NUMAddVECTOR_DISCRETIZE_RESULT(builder, VECTOR_DISCRETIZE_RESULT)

def NUMAddSCALAR_INTERPOLATION_REQUEST(builder, SCALAR_INTERPOLATION_REQUEST):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(SCALAR_INTERPOLATION_REQUEST), 0)

def AddSCALAR_INTERPOLATION_REQUEST(builder, SCALAR_INTERPOLATION_REQUEST):
    NUMAddSCALAR_INTERPOLATION_REQUEST(builder, SCALAR_INTERPOLATION_REQUEST)

def NUMAddSCALAR_INTERPOLATION_RESULT(builder, SCALAR_INTERPOLATION_RESULT):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(SCALAR_INTERPOLATION_RESULT), 0)

def AddSCALAR_INTERPOLATION_RESULT(builder, SCALAR_INTERPOLATION_RESULT):
    NUMAddSCALAR_INTERPOLATION_RESULT(builder, SCALAR_INTERPOLATION_RESULT)

def NUMAddGAUSS_MARKOV_REQUEST(builder, GAUSS_MARKOV_REQUEST):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(GAUSS_MARKOV_REQUEST), 0)

def AddGAUSS_MARKOV_REQUEST(builder, GAUSS_MARKOV_REQUEST):
    NUMAddGAUSS_MARKOV_REQUEST(builder, GAUSS_MARKOV_REQUEST)

def NUMAddGAUSS_MARKOV_RESULT(builder, GAUSS_MARKOV_RESULT):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(GAUSS_MARKOV_RESULT), 0)

def AddGAUSS_MARKOV_RESULT(builder, GAUSS_MARKOV_RESULT):
    NUMAddGAUSS_MARKOV_RESULT(builder, GAUSS_MARKOV_RESULT)

def NUMEnd(builder):
    return builder.EndObject()

def End(builder):
    return NUMEnd(builder)

import NUMGaussMarkovRequest
import NUMGaussMarkovResult
import NUMRootSolveRequest
import NUMRootSolveResult
import NUMScalarInterpolationRequest
import NUMScalarInterpolationResult
import NUMVectorDiscretizeRequest
import NUMVectorDiscretizeResult
import NUMVectorSaturateRequest
import NUMVectorSaturateResult
try:
    from typing import Optional
except:
    pass

class NUMT(object):

    # NUMT
    def __init__(
        self,
        ROOT_SOLVE_REQUEST = None,
        ROOT_SOLVE_RESULT = None,
        VECTOR_SATURATE_REQUEST = None,
        VECTOR_SATURATE_RESULT = None,
        VECTOR_DISCRETIZE_REQUEST = None,
        VECTOR_DISCRETIZE_RESULT = None,
        SCALAR_INTERPOLATION_REQUEST = None,
        SCALAR_INTERPOLATION_RESULT = None,
        GAUSS_MARKOV_REQUEST = None,
        GAUSS_MARKOV_RESULT = None,
    ):
        self.ROOT_SOLVE_REQUEST = ROOT_SOLVE_REQUEST  # type: Optional[NUMRootSolveRequest.NUMRootSolveRequestT]
        self.ROOT_SOLVE_RESULT = ROOT_SOLVE_RESULT  # type: Optional[NUMRootSolveResult.NUMRootSolveResultT]
        self.VECTOR_SATURATE_REQUEST = VECTOR_SATURATE_REQUEST  # type: Optional[NUMVectorSaturateRequest.NUMVectorSaturateRequestT]
        self.VECTOR_SATURATE_RESULT = VECTOR_SATURATE_RESULT  # type: Optional[NUMVectorSaturateResult.NUMVectorSaturateResultT]
        self.VECTOR_DISCRETIZE_REQUEST = VECTOR_DISCRETIZE_REQUEST  # type: Optional[NUMVectorDiscretizeRequest.NUMVectorDiscretizeRequestT]
        self.VECTOR_DISCRETIZE_RESULT = VECTOR_DISCRETIZE_RESULT  # type: Optional[NUMVectorDiscretizeResult.NUMVectorDiscretizeResultT]
        self.SCALAR_INTERPOLATION_REQUEST = SCALAR_INTERPOLATION_REQUEST  # type: Optional[NUMScalarInterpolationRequest.NUMScalarInterpolationRequestT]
        self.SCALAR_INTERPOLATION_RESULT = SCALAR_INTERPOLATION_RESULT  # type: Optional[NUMScalarInterpolationResult.NUMScalarInterpolationResultT]
        self.GAUSS_MARKOV_REQUEST = GAUSS_MARKOV_REQUEST  # type: Optional[NUMGaussMarkovRequest.NUMGaussMarkovRequestT]
        self.GAUSS_MARKOV_RESULT = GAUSS_MARKOV_RESULT  # type: Optional[NUMGaussMarkovResult.NUMGaussMarkovResultT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNum = NUM()
        tmpNum.Init(buf, pos)
        return cls.InitFromObj(tmpNum)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNum):
        x = NUMT()
        x._UnPack(tmpNum)
        return x

    # NUMT
    def _UnPack(self, NUM):
        if NUM is None:
            return
        if NUM.ROOT_SOLVE_REQUEST() is not None:
            self.ROOT_SOLVE_REQUEST = NUMRootSolveRequest.NUMRootSolveRequestT.InitFromObj(NUM.ROOT_SOLVE_REQUEST())
        if NUM.ROOT_SOLVE_RESULT() is not None:
            self.ROOT_SOLVE_RESULT = NUMRootSolveResult.NUMRootSolveResultT.InitFromObj(NUM.ROOT_SOLVE_RESULT())
        if NUM.VECTOR_SATURATE_REQUEST() is not None:
            self.VECTOR_SATURATE_REQUEST = NUMVectorSaturateRequest.NUMVectorSaturateRequestT.InitFromObj(NUM.VECTOR_SATURATE_REQUEST())
        if NUM.VECTOR_SATURATE_RESULT() is not None:
            self.VECTOR_SATURATE_RESULT = NUMVectorSaturateResult.NUMVectorSaturateResultT.InitFromObj(NUM.VECTOR_SATURATE_RESULT())
        if NUM.VECTOR_DISCRETIZE_REQUEST() is not None:
            self.VECTOR_DISCRETIZE_REQUEST = NUMVectorDiscretizeRequest.NUMVectorDiscretizeRequestT.InitFromObj(NUM.VECTOR_DISCRETIZE_REQUEST())
        if NUM.VECTOR_DISCRETIZE_RESULT() is not None:
            self.VECTOR_DISCRETIZE_RESULT = NUMVectorDiscretizeResult.NUMVectorDiscretizeResultT.InitFromObj(NUM.VECTOR_DISCRETIZE_RESULT())
        if NUM.SCALAR_INTERPOLATION_REQUEST() is not None:
            self.SCALAR_INTERPOLATION_REQUEST = NUMScalarInterpolationRequest.NUMScalarInterpolationRequestT.InitFromObj(NUM.SCALAR_INTERPOLATION_REQUEST())
        if NUM.SCALAR_INTERPOLATION_RESULT() is not None:
            self.SCALAR_INTERPOLATION_RESULT = NUMScalarInterpolationResult.NUMScalarInterpolationResultT.InitFromObj(NUM.SCALAR_INTERPOLATION_RESULT())
        if NUM.GAUSS_MARKOV_REQUEST() is not None:
            self.GAUSS_MARKOV_REQUEST = NUMGaussMarkovRequest.NUMGaussMarkovRequestT.InitFromObj(NUM.GAUSS_MARKOV_REQUEST())
        if NUM.GAUSS_MARKOV_RESULT() is not None:
            self.GAUSS_MARKOV_RESULT = NUMGaussMarkovResult.NUMGaussMarkovResultT.InitFromObj(NUM.GAUSS_MARKOV_RESULT())

    # NUMT
    def Pack(self, builder):
        if self.ROOT_SOLVE_REQUEST is not None:
            ROOT_SOLVE_REQUEST = self.ROOT_SOLVE_REQUEST.Pack(builder)
        if self.ROOT_SOLVE_RESULT is not None:
            ROOT_SOLVE_RESULT = self.ROOT_SOLVE_RESULT.Pack(builder)
        if self.VECTOR_SATURATE_REQUEST is not None:
            VECTOR_SATURATE_REQUEST = self.VECTOR_SATURATE_REQUEST.Pack(builder)
        if self.VECTOR_SATURATE_RESULT is not None:
            VECTOR_SATURATE_RESULT = self.VECTOR_SATURATE_RESULT.Pack(builder)
        if self.VECTOR_DISCRETIZE_REQUEST is not None:
            VECTOR_DISCRETIZE_REQUEST = self.VECTOR_DISCRETIZE_REQUEST.Pack(builder)
        if self.VECTOR_DISCRETIZE_RESULT is not None:
            VECTOR_DISCRETIZE_RESULT = self.VECTOR_DISCRETIZE_RESULT.Pack(builder)
        if self.SCALAR_INTERPOLATION_REQUEST is not None:
            SCALAR_INTERPOLATION_REQUEST = self.SCALAR_INTERPOLATION_REQUEST.Pack(builder)
        if self.SCALAR_INTERPOLATION_RESULT is not None:
            SCALAR_INTERPOLATION_RESULT = self.SCALAR_INTERPOLATION_RESULT.Pack(builder)
        if self.GAUSS_MARKOV_REQUEST is not None:
            GAUSS_MARKOV_REQUEST = self.GAUSS_MARKOV_REQUEST.Pack(builder)
        if self.GAUSS_MARKOV_RESULT is not None:
            GAUSS_MARKOV_RESULT = self.GAUSS_MARKOV_RESULT.Pack(builder)
        NUMStart(builder)
        if self.ROOT_SOLVE_REQUEST is not None:
            NUMAddROOT_SOLVE_REQUEST(builder, ROOT_SOLVE_REQUEST)
        if self.ROOT_SOLVE_RESULT is not None:
            NUMAddROOT_SOLVE_RESULT(builder, ROOT_SOLVE_RESULT)
        if self.VECTOR_SATURATE_REQUEST is not None:
            NUMAddVECTOR_SATURATE_REQUEST(builder, VECTOR_SATURATE_REQUEST)
        if self.VECTOR_SATURATE_RESULT is not None:
            NUMAddVECTOR_SATURATE_RESULT(builder, VECTOR_SATURATE_RESULT)
        if self.VECTOR_DISCRETIZE_REQUEST is not None:
            NUMAddVECTOR_DISCRETIZE_REQUEST(builder, VECTOR_DISCRETIZE_REQUEST)
        if self.VECTOR_DISCRETIZE_RESULT is not None:
            NUMAddVECTOR_DISCRETIZE_RESULT(builder, VECTOR_DISCRETIZE_RESULT)
        if self.SCALAR_INTERPOLATION_REQUEST is not None:
            NUMAddSCALAR_INTERPOLATION_REQUEST(builder, SCALAR_INTERPOLATION_REQUEST)
        if self.SCALAR_INTERPOLATION_RESULT is not None:
            NUMAddSCALAR_INTERPOLATION_RESULT(builder, SCALAR_INTERPOLATION_RESULT)
        if self.GAUSS_MARKOV_REQUEST is not None:
            NUMAddGAUSS_MARKOV_REQUEST(builder, GAUSS_MARKOV_REQUEST)
        if self.GAUSS_MARKOV_RESULT is not None:
            NUMAddGAUSS_MARKOV_RESULT(builder, GAUSS_MARKOV_RESULT)
        NUM = NUMEnd(builder)
        return NUM
