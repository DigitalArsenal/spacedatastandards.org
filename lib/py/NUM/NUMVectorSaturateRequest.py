# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Request for component-wise vector saturation.
class NUMVectorSaturateRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NUMVectorSaturateRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNUMVectorSaturateRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NUMVectorSaturateRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x55\x4D", size_prefixed=size_prefixed)

    # NUMVectorSaturateRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Input vector to clamp.
    # NUMVectorSaturateRequest
    def STATE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMVectorSaturateRequest
    def STATEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMVectorSaturateRequest
    def STATELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMVectorSaturateRequest
    def STATEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Per-component lower bounds. Length must match STATE.
    # NUMVectorSaturateRequest
    def LOWER_BOUNDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMVectorSaturateRequest
    def LOWER_BOUNDSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMVectorSaturateRequest
    def LOWER_BOUNDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMVectorSaturateRequest
    def LOWER_BOUNDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Per-component upper bounds. Length must match STATE.
    # NUMVectorSaturateRequest
    def UPPER_BOUNDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMVectorSaturateRequest
    def UPPER_BOUNDSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMVectorSaturateRequest
    def UPPER_BOUNDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMVectorSaturateRequest
    def UPPER_BOUNDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Optional trace/correlation identifier.
    # NUMVectorSaturateRequest
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def NUMVectorSaturateRequestStart(builder):
    builder.StartObject(4)

def Start(builder):
    NUMVectorSaturateRequestStart(builder)

def NUMVectorSaturateRequestAddSTATE(builder, STATE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(STATE), 0)

def AddSTATE(builder, STATE):
    NUMVectorSaturateRequestAddSTATE(builder, STATE)

def NUMVectorSaturateRequestStartSTATEVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSTATEVector(builder, numElems):
    return NUMVectorSaturateRequestStartSTATEVector(builder, numElems)

def NUMVectorSaturateRequestCreateSTATEVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSTATEVector(builder, data):
    NUMVectorSaturateRequestCreateSTATEVector(builder, data)

def NUMVectorSaturateRequestAddLOWER_BOUNDS(builder, LOWER_BOUNDS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(LOWER_BOUNDS), 0)

def AddLOWER_BOUNDS(builder, LOWER_BOUNDS):
    NUMVectorSaturateRequestAddLOWER_BOUNDS(builder, LOWER_BOUNDS)

def NUMVectorSaturateRequestStartLOWER_BOUNDSVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartLOWER_BOUNDSVector(builder, numElems):
    return NUMVectorSaturateRequestStartLOWER_BOUNDSVector(builder, numElems)

def NUMVectorSaturateRequestCreateLOWER_BOUNDSVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateLOWER_BOUNDSVector(builder, data):
    NUMVectorSaturateRequestCreateLOWER_BOUNDSVector(builder, data)

def NUMVectorSaturateRequestAddUPPER_BOUNDS(builder, UPPER_BOUNDS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(UPPER_BOUNDS), 0)

def AddUPPER_BOUNDS(builder, UPPER_BOUNDS):
    NUMVectorSaturateRequestAddUPPER_BOUNDS(builder, UPPER_BOUNDS)

def NUMVectorSaturateRequestStartUPPER_BOUNDSVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartUPPER_BOUNDSVector(builder, numElems):
    return NUMVectorSaturateRequestStartUPPER_BOUNDSVector(builder, numElems)

def NUMVectorSaturateRequestCreateUPPER_BOUNDSVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateUPPER_BOUNDSVector(builder, data):
    NUMVectorSaturateRequestCreateUPPER_BOUNDSVector(builder, data)

def NUMVectorSaturateRequestAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(TRACE_ID), 0)

def AddTRACE_ID(builder, TRACE_ID):
    NUMVectorSaturateRequestAddTRACE_ID(builder, TRACE_ID)

def NUMVectorSaturateRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return NUMVectorSaturateRequestEnd(builder)

try:
    from typing import List
except:
    pass

class NUMVectorSaturateRequestT(object):

    # NUMVectorSaturateRequestT
    def __init__(
        self,
        STATE = None,
        LOWER_BOUNDS = None,
        UPPER_BOUNDS = None,
        TRACE_ID = None,
    ):
        self.STATE = STATE  # type: Optional[List[float]]
        self.LOWER_BOUNDS = LOWER_BOUNDS  # type: Optional[List[float]]
        self.UPPER_BOUNDS = UPPER_BOUNDS  # type: Optional[List[float]]
        self.TRACE_ID = TRACE_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNumvectorSaturateRequest = NUMVectorSaturateRequest()
        tmpNumvectorSaturateRequest.Init(buf, pos)
        return cls.InitFromObj(tmpNumvectorSaturateRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNumvectorSaturateRequest):
        x = NUMVectorSaturateRequestT()
        x._UnPack(tmpNumvectorSaturateRequest)
        return x

    # NUMVectorSaturateRequestT
    def _UnPack(self, NUMVectorSaturateRequest):
        if NUMVectorSaturateRequest is None:
            return
        if not NUMVectorSaturateRequest.STATEIsNone():
            if np is None:
                self.STATE = []
                for i in range(NUMVectorSaturateRequest.STATELength()):
                    self.STATE.append(NUMVectorSaturateRequest.STATE(i))
            else:
                self.STATE = NUMVectorSaturateRequest.STATEAsNumpy()
        if not NUMVectorSaturateRequest.LOWER_BOUNDSIsNone():
            if np is None:
                self.LOWER_BOUNDS = []
                for i in range(NUMVectorSaturateRequest.LOWER_BOUNDSLength()):
                    self.LOWER_BOUNDS.append(NUMVectorSaturateRequest.LOWER_BOUNDS(i))
            else:
                self.LOWER_BOUNDS = NUMVectorSaturateRequest.LOWER_BOUNDSAsNumpy()
        if not NUMVectorSaturateRequest.UPPER_BOUNDSIsNone():
            if np is None:
                self.UPPER_BOUNDS = []
                for i in range(NUMVectorSaturateRequest.UPPER_BOUNDSLength()):
                    self.UPPER_BOUNDS.append(NUMVectorSaturateRequest.UPPER_BOUNDS(i))
            else:
                self.UPPER_BOUNDS = NUMVectorSaturateRequest.UPPER_BOUNDSAsNumpy()
        self.TRACE_ID = NUMVectorSaturateRequest.TRACE_ID()

    # NUMVectorSaturateRequestT
    def Pack(self, builder):
        if self.STATE is not None:
            if np is not None and type(self.STATE) is np.ndarray:
                STATE = builder.CreateNumpyVector(self.STATE)
            else:
                NUMVectorSaturateRequestStartSTATEVector(builder, len(self.STATE))
                for i in reversed(range(len(self.STATE))):
                    builder.PrependFloat64(self.STATE[i])
                STATE = builder.EndVector()
        if self.LOWER_BOUNDS is not None:
            if np is not None and type(self.LOWER_BOUNDS) is np.ndarray:
                LOWER_BOUNDS = builder.CreateNumpyVector(self.LOWER_BOUNDS)
            else:
                NUMVectorSaturateRequestStartLOWER_BOUNDSVector(builder, len(self.LOWER_BOUNDS))
                for i in reversed(range(len(self.LOWER_BOUNDS))):
                    builder.PrependFloat64(self.LOWER_BOUNDS[i])
                LOWER_BOUNDS = builder.EndVector()
        if self.UPPER_BOUNDS is not None:
            if np is not None and type(self.UPPER_BOUNDS) is np.ndarray:
                UPPER_BOUNDS = builder.CreateNumpyVector(self.UPPER_BOUNDS)
            else:
                NUMVectorSaturateRequestStartUPPER_BOUNDSVector(builder, len(self.UPPER_BOUNDS))
                for i in reversed(range(len(self.UPPER_BOUNDS))):
                    builder.PrependFloat64(self.UPPER_BOUNDS[i])
                UPPER_BOUNDS = builder.EndVector()
        if self.TRACE_ID is not None:
            TRACE_ID = builder.CreateString(self.TRACE_ID)
        NUMVectorSaturateRequestStart(builder)
        if self.STATE is not None:
            NUMVectorSaturateRequestAddSTATE(builder, STATE)
        if self.LOWER_BOUNDS is not None:
            NUMVectorSaturateRequestAddLOWER_BOUNDS(builder, LOWER_BOUNDS)
        if self.UPPER_BOUNDS is not None:
            NUMVectorSaturateRequestAddUPPER_BOUNDS(builder, UPPER_BOUNDS)
        if self.TRACE_ID is not None:
            NUMVectorSaturateRequestAddTRACE_ID(builder, TRACE_ID)
        NUMVectorSaturateRequest = NUMVectorSaturateRequestEnd(builder)
        return NUMVectorSaturateRequest
