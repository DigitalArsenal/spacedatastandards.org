# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SLPIteration(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SLPIteration()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSLPIteration(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SLPIterationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x4C\x50", size_prefixed=size_prefixed)

    # SLPIteration
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SLPIteration
    def ITERATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # SLPIteration
    def VARIABLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SLPIteration
    def VARIABLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SLPIteration
    def VARIABLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SLPIteration
    def VARIABLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # SLPIteration
    def VALUES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SLPIteration
    def VALUESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SLPIteration
    def VALUESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SLPIteration
    def VALUESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # SLPIteration
    def RESIDUAL_NORM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SLPIteration
    def OBJECTIVE_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SLPIteration
    def MAXIMUM_CONSTRAINT_VIOLATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SLPIteration
    def JACOBIAN_CONDITION_ESTIMATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SLPIteration
    def KKT_RESIDUAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SLPIteration
    def EVALUATION_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def SLPIterationStart(builder):
    builder.StartObject(9)

def Start(builder):
    SLPIterationStart(builder)

def SLPIterationAddITERATION(builder, ITERATION):
    builder.PrependUint32Slot(0, ITERATION, 0)

def AddITERATION(builder, ITERATION):
    SLPIterationAddITERATION(builder, ITERATION)

def SLPIterationAddVARIABLES(builder, VARIABLES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VARIABLES), 0)

def AddVARIABLES(builder, VARIABLES):
    SLPIterationAddVARIABLES(builder, VARIABLES)

def SLPIterationStartVARIABLESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartVARIABLESVector(builder, numElems):
    return SLPIterationStartVARIABLESVector(builder, numElems)

def SLPIterationCreateVARIABLESVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateVARIABLESVector(builder, data):
    SLPIterationCreateVARIABLESVector(builder, data)

def SLPIterationAddVALUES(builder, VALUES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VALUES), 0)

def AddVALUES(builder, VALUES):
    SLPIterationAddVALUES(builder, VALUES)

def SLPIterationStartVALUESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartVALUESVector(builder, numElems):
    return SLPIterationStartVALUESVector(builder, numElems)

def SLPIterationCreateVALUESVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateVALUESVector(builder, data):
    SLPIterationCreateVALUESVector(builder, data)

def SLPIterationAddRESIDUAL_NORM(builder, RESIDUAL_NORM):
    builder.PrependFloat64Slot(3, RESIDUAL_NORM, 0.0)

def AddRESIDUAL_NORM(builder, RESIDUAL_NORM):
    SLPIterationAddRESIDUAL_NORM(builder, RESIDUAL_NORM)

def SLPIterationAddOBJECTIVE_VALUE(builder, OBJECTIVE_VALUE):
    builder.PrependFloat64Slot(4, OBJECTIVE_VALUE, 0.0)

def AddOBJECTIVE_VALUE(builder, OBJECTIVE_VALUE):
    SLPIterationAddOBJECTIVE_VALUE(builder, OBJECTIVE_VALUE)

def SLPIterationAddMAXIMUM_CONSTRAINT_VIOLATION(builder, MAXIMUM_CONSTRAINT_VIOLATION):
    builder.PrependFloat64Slot(5, MAXIMUM_CONSTRAINT_VIOLATION, 0.0)

def AddMAXIMUM_CONSTRAINT_VIOLATION(builder, MAXIMUM_CONSTRAINT_VIOLATION):
    SLPIterationAddMAXIMUM_CONSTRAINT_VIOLATION(builder, MAXIMUM_CONSTRAINT_VIOLATION)

def SLPIterationAddJACOBIAN_CONDITION_ESTIMATE(builder, JACOBIAN_CONDITION_ESTIMATE):
    builder.PrependFloat64Slot(6, JACOBIAN_CONDITION_ESTIMATE, 0.0)

def AddJACOBIAN_CONDITION_ESTIMATE(builder, JACOBIAN_CONDITION_ESTIMATE):
    SLPIterationAddJACOBIAN_CONDITION_ESTIMATE(builder, JACOBIAN_CONDITION_ESTIMATE)

def SLPIterationAddKKT_RESIDUAL(builder, KKT_RESIDUAL):
    builder.PrependFloat64Slot(7, KKT_RESIDUAL, 0.0)

def AddKKT_RESIDUAL(builder, KKT_RESIDUAL):
    SLPIterationAddKKT_RESIDUAL(builder, KKT_RESIDUAL)

def SLPIterationAddEVALUATION_COUNT(builder, EVALUATION_COUNT):
    builder.PrependUint32Slot(8, EVALUATION_COUNT, 0)

def AddEVALUATION_COUNT(builder, EVALUATION_COUNT):
    SLPIterationAddEVALUATION_COUNT(builder, EVALUATION_COUNT)

def SLPIterationEnd(builder):
    return builder.EndObject()

def End(builder):
    return SLPIterationEnd(builder)

try:
    from typing import List
except:
    pass

class SLPIterationT(object):

    # SLPIterationT
    def __init__(
        self,
        ITERATION = 0,
        VARIABLES = None,
        VALUES = None,
        RESIDUAL_NORM = 0.0,
        OBJECTIVE_VALUE = 0.0,
        MAXIMUM_CONSTRAINT_VIOLATION = 0.0,
        JACOBIAN_CONDITION_ESTIMATE = 0.0,
        KKT_RESIDUAL = 0.0,
        EVALUATION_COUNT = 0,
    ):
        self.ITERATION = ITERATION  # type: int
        self.VARIABLES = VARIABLES  # type: Optional[List[float]]
        self.VALUES = VALUES  # type: Optional[List[float]]
        self.RESIDUAL_NORM = RESIDUAL_NORM  # type: float
        self.OBJECTIVE_VALUE = OBJECTIVE_VALUE  # type: float
        self.MAXIMUM_CONSTRAINT_VIOLATION = MAXIMUM_CONSTRAINT_VIOLATION  # type: float
        self.JACOBIAN_CONDITION_ESTIMATE = JACOBIAN_CONDITION_ESTIMATE  # type: float
        self.KKT_RESIDUAL = KKT_RESIDUAL  # type: float
        self.EVALUATION_COUNT = EVALUATION_COUNT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSlpiteration = SLPIteration()
        tmpSlpiteration.Init(buf, pos)
        return cls.InitFromObj(tmpSlpiteration)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSlpiteration):
        x = SLPIterationT()
        x._UnPack(tmpSlpiteration)
        return x

    # SLPIterationT
    def _UnPack(self, SLPIteration):
        if SLPIteration is None:
            return
        self.ITERATION = SLPIteration.ITERATION()
        if not SLPIteration.VARIABLESIsNone():
            if np is None:
                self.VARIABLES = []
                for i in range(SLPIteration.VARIABLESLength()):
                    self.VARIABLES.append(SLPIteration.VARIABLES(i))
            else:
                self.VARIABLES = SLPIteration.VARIABLESAsNumpy()
        if not SLPIteration.VALUESIsNone():
            if np is None:
                self.VALUES = []
                for i in range(SLPIteration.VALUESLength()):
                    self.VALUES.append(SLPIteration.VALUES(i))
            else:
                self.VALUES = SLPIteration.VALUESAsNumpy()
        self.RESIDUAL_NORM = SLPIteration.RESIDUAL_NORM()
        self.OBJECTIVE_VALUE = SLPIteration.OBJECTIVE_VALUE()
        self.MAXIMUM_CONSTRAINT_VIOLATION = SLPIteration.MAXIMUM_CONSTRAINT_VIOLATION()
        self.JACOBIAN_CONDITION_ESTIMATE = SLPIteration.JACOBIAN_CONDITION_ESTIMATE()
        self.KKT_RESIDUAL = SLPIteration.KKT_RESIDUAL()
        self.EVALUATION_COUNT = SLPIteration.EVALUATION_COUNT()

    # SLPIterationT
    def Pack(self, builder):
        if self.VARIABLES is not None:
            if np is not None and type(self.VARIABLES) is np.ndarray:
                VARIABLES = builder.CreateNumpyVector(self.VARIABLES)
            else:
                SLPIterationStartVARIABLESVector(builder, len(self.VARIABLES))
                for i in reversed(range(len(self.VARIABLES))):
                    builder.PrependFloat64(self.VARIABLES[i])
                VARIABLES = builder.EndVector()
        if self.VALUES is not None:
            if np is not None and type(self.VALUES) is np.ndarray:
                VALUES = builder.CreateNumpyVector(self.VALUES)
            else:
                SLPIterationStartVALUESVector(builder, len(self.VALUES))
                for i in reversed(range(len(self.VALUES))):
                    builder.PrependFloat64(self.VALUES[i])
                VALUES = builder.EndVector()
        SLPIterationStart(builder)
        SLPIterationAddITERATION(builder, self.ITERATION)
        if self.VARIABLES is not None:
            SLPIterationAddVARIABLES(builder, VARIABLES)
        if self.VALUES is not None:
            SLPIterationAddVALUES(builder, VALUES)
        SLPIterationAddRESIDUAL_NORM(builder, self.RESIDUAL_NORM)
        SLPIterationAddOBJECTIVE_VALUE(builder, self.OBJECTIVE_VALUE)
        SLPIterationAddMAXIMUM_CONSTRAINT_VIOLATION(builder, self.MAXIMUM_CONSTRAINT_VIOLATION)
        SLPIterationAddJACOBIAN_CONDITION_ESTIMATE(builder, self.JACOBIAN_CONDITION_ESTIMATE)
        SLPIterationAddKKT_RESIDUAL(builder, self.KKT_RESIDUAL)
        SLPIterationAddEVALUATION_COUNT(builder, self.EVALUATION_COUNT)
        SLPIteration = SLPIterationEnd(builder)
        return SLPIteration
