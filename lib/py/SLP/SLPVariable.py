# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SLPVariable(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SLPVariable()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSLPVariable(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SLPVariableBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x4C\x50", size_prefixed=size_prefixed)

    # SLPVariable
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SLPVariable
    def VARIABLE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLPVariable
    def PARAMETER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # SLPVariable
    def PROVIDER_DEFINED_PARAMETER_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLPVariable
    def INITIAL_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SLPVariable
    def LOWER_BOUND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return -1.7976931348623157e+308

    # SLPVariable
    def UPPER_BOUND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1.7976931348623157e+308

    # SLPVariable
    def PERTURBATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SLPVariable
    def SCALE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1.0

def SLPVariableStart(builder):
    builder.StartObject(8)

def Start(builder):
    SLPVariableStart(builder)

def SLPVariableAddVARIABLE_ID(builder, VARIABLE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(VARIABLE_ID), 0)

def AddVARIABLE_ID(builder, VARIABLE_ID):
    SLPVariableAddVARIABLE_ID(builder, VARIABLE_ID)

def SLPVariableAddPARAMETER(builder, PARAMETER):
    builder.PrependUint16Slot(1, PARAMETER, 0)

def AddPARAMETER(builder, PARAMETER):
    SLPVariableAddPARAMETER(builder, PARAMETER)

def SLPVariableAddPROVIDER_DEFINED_PARAMETER_NAME(builder, PROVIDER_DEFINED_PARAMETER_NAME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_DEFINED_PARAMETER_NAME), 0)

def AddPROVIDER_DEFINED_PARAMETER_NAME(builder, PROVIDER_DEFINED_PARAMETER_NAME):
    SLPVariableAddPROVIDER_DEFINED_PARAMETER_NAME(builder, PROVIDER_DEFINED_PARAMETER_NAME)

def SLPVariableAddINITIAL_VALUE(builder, INITIAL_VALUE):
    builder.PrependFloat64Slot(3, INITIAL_VALUE, 0.0)

def AddINITIAL_VALUE(builder, INITIAL_VALUE):
    SLPVariableAddINITIAL_VALUE(builder, INITIAL_VALUE)

def SLPVariableAddLOWER_BOUND(builder, LOWER_BOUND):
    builder.PrependFloat64Slot(4, LOWER_BOUND, -1.7976931348623157e+308)

def AddLOWER_BOUND(builder, LOWER_BOUND):
    SLPVariableAddLOWER_BOUND(builder, LOWER_BOUND)

def SLPVariableAddUPPER_BOUND(builder, UPPER_BOUND):
    builder.PrependFloat64Slot(5, UPPER_BOUND, 1.7976931348623157e+308)

def AddUPPER_BOUND(builder, UPPER_BOUND):
    SLPVariableAddUPPER_BOUND(builder, UPPER_BOUND)

def SLPVariableAddPERTURBATION(builder, PERTURBATION):
    builder.PrependFloat64Slot(6, PERTURBATION, 0.0)

def AddPERTURBATION(builder, PERTURBATION):
    SLPVariableAddPERTURBATION(builder, PERTURBATION)

def SLPVariableAddSCALE(builder, SCALE):
    builder.PrependFloat64Slot(7, SCALE, 1.0)

def AddSCALE(builder, SCALE):
    SLPVariableAddSCALE(builder, SCALE)

def SLPVariableEnd(builder):
    return builder.EndObject()

def End(builder):
    return SLPVariableEnd(builder)


class SLPVariableT(object):

    # SLPVariableT
    def __init__(
        self,
        VARIABLE_ID = None,
        PARAMETER = 0,
        PROVIDER_DEFINED_PARAMETER_NAME = None,
        INITIAL_VALUE = 0.0,
        LOWER_BOUND = -1.7976931348623157e+308,
        UPPER_BOUND = 1.7976931348623157e+308,
        PERTURBATION = 0.0,
        SCALE = 1.0,
    ):
        self.VARIABLE_ID = VARIABLE_ID  # type: Optional[str]
        self.PARAMETER = PARAMETER  # type: int
        self.PROVIDER_DEFINED_PARAMETER_NAME = PROVIDER_DEFINED_PARAMETER_NAME  # type: Optional[str]
        self.INITIAL_VALUE = INITIAL_VALUE  # type: float
        self.LOWER_BOUND = LOWER_BOUND  # type: float
        self.UPPER_BOUND = UPPER_BOUND  # type: float
        self.PERTURBATION = PERTURBATION  # type: float
        self.SCALE = SCALE  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSlpvariable = SLPVariable()
        tmpSlpvariable.Init(buf, pos)
        return cls.InitFromObj(tmpSlpvariable)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSlpvariable):
        x = SLPVariableT()
        x._UnPack(tmpSlpvariable)
        return x

    # SLPVariableT
    def _UnPack(self, SLPVariable):
        if SLPVariable is None:
            return
        self.VARIABLE_ID = SLPVariable.VARIABLE_ID()
        self.PARAMETER = SLPVariable.PARAMETER()
        self.PROVIDER_DEFINED_PARAMETER_NAME = SLPVariable.PROVIDER_DEFINED_PARAMETER_NAME()
        self.INITIAL_VALUE = SLPVariable.INITIAL_VALUE()
        self.LOWER_BOUND = SLPVariable.LOWER_BOUND()
        self.UPPER_BOUND = SLPVariable.UPPER_BOUND()
        self.PERTURBATION = SLPVariable.PERTURBATION()
        self.SCALE = SLPVariable.SCALE()

    # SLPVariableT
    def Pack(self, builder):
        if self.VARIABLE_ID is not None:
            VARIABLE_ID = builder.CreateString(self.VARIABLE_ID)
        if self.PROVIDER_DEFINED_PARAMETER_NAME is not None:
            PROVIDER_DEFINED_PARAMETER_NAME = builder.CreateString(self.PROVIDER_DEFINED_PARAMETER_NAME)
        SLPVariableStart(builder)
        if self.VARIABLE_ID is not None:
            SLPVariableAddVARIABLE_ID(builder, VARIABLE_ID)
        SLPVariableAddPARAMETER(builder, self.PARAMETER)
        if self.PROVIDER_DEFINED_PARAMETER_NAME is not None:
            SLPVariableAddPROVIDER_DEFINED_PARAMETER_NAME(builder, PROVIDER_DEFINED_PARAMETER_NAME)
        SLPVariableAddINITIAL_VALUE(builder, self.INITIAL_VALUE)
        SLPVariableAddLOWER_BOUND(builder, self.LOWER_BOUND)
        SLPVariableAddUPPER_BOUND(builder, self.UPPER_BOUND)
        SLPVariableAddPERTURBATION(builder, self.PERTURBATION)
        SLPVariableAddSCALE(builder, self.SCALE)
        SLPVariable = SLPVariableEnd(builder)
        return SLPVariable
