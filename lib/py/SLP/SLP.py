# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SLP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SLP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSLP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SLPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x4C\x50", size_prefixed=size_prefixed)

    # SLP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SLP
    def PROBLEM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLP
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLP
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # SLP
    def PROPAGATOR_PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLP
    def OBJECTIVE_EVALUATOR_PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLP
    def VARIABLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SLPVariable import SLPVariable
            obj = SLPVariable()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SLP
    def VARIABLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SLP
    def VARIABLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # SLP
    def GOALS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SLPGoal import SLPGoal
            obj = SLPGoal()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SLP
    def GOALSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SLP
    def GOALSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # SLP
    def OBJECTIVE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SLPObjective import SLPObjective
            obj = SLPObjective()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SLP
    def CONSTRAINTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SLPNonlinearConstraint import SLPNonlinearConstraint
            obj = SLPNonlinearConstraint()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SLP
    def CONSTRAINTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SLP
    def CONSTRAINTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # SLP
    def SETTINGS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SLPSolverSettings import SLPSolverSettings
            obj = SLPSolverSettings()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SLP
    def REPORT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SLPSolverReport import SLPSolverReport
            obj = SLPSolverReport()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SLP
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLP
    def ATTESTATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SLPAttestation import SLPAttestation
            obj = SLPAttestation()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def SLPStart(builder):
    builder.StartObject(13)

def Start(builder):
    SLPStart(builder)

def SLPAddPROBLEM_ID(builder, PROBLEM_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PROBLEM_ID), 0)

def AddPROBLEM_ID(builder, PROBLEM_ID):
    SLPAddPROBLEM_ID(builder, PROBLEM_ID)

def SLPAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    SLPAddNAME(builder, NAME)

def SLPAddKIND(builder, KIND):
    builder.PrependUint8Slot(2, KIND, 0)

def AddKIND(builder, KIND):
    SLPAddKIND(builder, KIND)

def SLPAddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(PROPAGATOR_PORT_ID), 0)

def AddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID):
    SLPAddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID)

def SLPAddOBJECTIVE_EVALUATOR_PORT_ID(builder, OBJECTIVE_EVALUATOR_PORT_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECTIVE_EVALUATOR_PORT_ID), 0)

def AddOBJECTIVE_EVALUATOR_PORT_ID(builder, OBJECTIVE_EVALUATOR_PORT_ID):
    SLPAddOBJECTIVE_EVALUATOR_PORT_ID(builder, OBJECTIVE_EVALUATOR_PORT_ID)

def SLPAddVARIABLES(builder, VARIABLES):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(VARIABLES), 0)

def AddVARIABLES(builder, VARIABLES):
    SLPAddVARIABLES(builder, VARIABLES)

def SLPStartVARIABLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartVARIABLESVector(builder, numElems):
    return SLPStartVARIABLESVector(builder, numElems)

def SLPCreateVARIABLESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateVARIABLESVector(builder, data):
    SLPCreateVARIABLESVector(builder, data)

def SLPAddGOALS(builder, GOALS):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(GOALS), 0)

def AddGOALS(builder, GOALS):
    SLPAddGOALS(builder, GOALS)

def SLPStartGOALSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartGOALSVector(builder, numElems):
    return SLPStartGOALSVector(builder, numElems)

def SLPCreateGOALSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateGOALSVector(builder, data):
    SLPCreateGOALSVector(builder, data)

def SLPAddOBJECTIVE(builder, OBJECTIVE):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECTIVE), 0)

def AddOBJECTIVE(builder, OBJECTIVE):
    SLPAddOBJECTIVE(builder, OBJECTIVE)

def SLPAddCONSTRAINTS(builder, CONSTRAINTS):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(CONSTRAINTS), 0)

def AddCONSTRAINTS(builder, CONSTRAINTS):
    SLPAddCONSTRAINTS(builder, CONSTRAINTS)

def SLPStartCONSTRAINTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCONSTRAINTSVector(builder, numElems):
    return SLPStartCONSTRAINTSVector(builder, numElems)

def SLPCreateCONSTRAINTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCONSTRAINTSVector(builder, data):
    SLPCreateCONSTRAINTSVector(builder, data)

def SLPAddSETTINGS(builder, SETTINGS):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(SETTINGS), 0)

def AddSETTINGS(builder, SETTINGS):
    SLPAddSETTINGS(builder, SETTINGS)

def SLPAddREPORT(builder, REPORT):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(REPORT), 0)

def AddREPORT(builder, REPORT):
    SLPAddREPORT(builder, REPORT)

def SLPAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(CREATED_AT), 0)

def AddCREATED_AT(builder, CREATED_AT):
    SLPAddCREATED_AT(builder, CREATED_AT)

def SLPAddATTESTATION(builder, ATTESTATION):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(ATTESTATION), 0)

def AddATTESTATION(builder, ATTESTATION):
    SLPAddATTESTATION(builder, ATTESTATION)

def SLPEnd(builder):
    return builder.EndObject()

def End(builder):
    return SLPEnd(builder)

import SLPAttestation
import SLPGoal
import SLPNonlinearConstraint
import SLPObjective
import SLPSolverReport
import SLPSolverSettings
import SLPVariable
try:
    from typing import List, Optional
except:
    pass

class SLPT(object):

    # SLPT
    def __init__(
        self,
        PROBLEM_ID = None,
        NAME = None,
        KIND = 0,
        PROPAGATOR_PORT_ID = None,
        OBJECTIVE_EVALUATOR_PORT_ID = None,
        VARIABLES = None,
        GOALS = None,
        OBJECTIVE = None,
        CONSTRAINTS = None,
        SETTINGS = None,
        REPORT = None,
        CREATED_AT = None,
        ATTESTATION = None,
    ):
        self.PROBLEM_ID = PROBLEM_ID  # type: Optional[str]
        self.NAME = NAME  # type: Optional[str]
        self.KIND = KIND  # type: int
        self.PROPAGATOR_PORT_ID = PROPAGATOR_PORT_ID  # type: Optional[str]
        self.OBJECTIVE_EVALUATOR_PORT_ID = OBJECTIVE_EVALUATOR_PORT_ID  # type: Optional[str]
        self.VARIABLES = VARIABLES  # type: Optional[List[SLPVariable.SLPVariableT]]
        self.GOALS = GOALS  # type: Optional[List[SLPGoal.SLPGoalT]]
        self.OBJECTIVE = OBJECTIVE  # type: Optional[SLPObjective.SLPObjectiveT]
        self.CONSTRAINTS = CONSTRAINTS  # type: Optional[List[SLPNonlinearConstraint.SLPNonlinearConstraintT]]
        self.SETTINGS = SETTINGS  # type: Optional[SLPSolverSettings.SLPSolverSettingsT]
        self.REPORT = REPORT  # type: Optional[SLPSolverReport.SLPSolverReportT]
        self.CREATED_AT = CREATED_AT  # type: Optional[str]
        self.ATTESTATION = ATTESTATION  # type: Optional[SLPAttestation.SLPAttestationT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSlp = SLP()
        tmpSlp.Init(buf, pos)
        return cls.InitFromObj(tmpSlp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSlp):
        x = SLPT()
        x._UnPack(tmpSlp)
        return x

    # SLPT
    def _UnPack(self, SLP):
        if SLP is None:
            return
        self.PROBLEM_ID = SLP.PROBLEM_ID()
        self.NAME = SLP.NAME()
        self.KIND = SLP.KIND()
        self.PROPAGATOR_PORT_ID = SLP.PROPAGATOR_PORT_ID()
        self.OBJECTIVE_EVALUATOR_PORT_ID = SLP.OBJECTIVE_EVALUATOR_PORT_ID()
        if not SLP.VARIABLESIsNone():
            self.VARIABLES = []
            for i in range(SLP.VARIABLESLength()):
                if SLP.VARIABLES(i) is None:
                    self.VARIABLES.append(None)
                else:
                    sLPVariable_ = SLPVariable.SLPVariableT.InitFromObj(SLP.VARIABLES(i))
                    self.VARIABLES.append(sLPVariable_)
        if not SLP.GOALSIsNone():
            self.GOALS = []
            for i in range(SLP.GOALSLength()):
                if SLP.GOALS(i) is None:
                    self.GOALS.append(None)
                else:
                    sLPGoal_ = SLPGoal.SLPGoalT.InitFromObj(SLP.GOALS(i))
                    self.GOALS.append(sLPGoal_)
        if SLP.OBJECTIVE() is not None:
            self.OBJECTIVE = SLPObjective.SLPObjectiveT.InitFromObj(SLP.OBJECTIVE())
        if not SLP.CONSTRAINTSIsNone():
            self.CONSTRAINTS = []
            for i in range(SLP.CONSTRAINTSLength()):
                if SLP.CONSTRAINTS(i) is None:
                    self.CONSTRAINTS.append(None)
                else:
                    sLPNonlinearConstraint_ = SLPNonlinearConstraint.SLPNonlinearConstraintT.InitFromObj(SLP.CONSTRAINTS(i))
                    self.CONSTRAINTS.append(sLPNonlinearConstraint_)
        if SLP.SETTINGS() is not None:
            self.SETTINGS = SLPSolverSettings.SLPSolverSettingsT.InitFromObj(SLP.SETTINGS())
        if SLP.REPORT() is not None:
            self.REPORT = SLPSolverReport.SLPSolverReportT.InitFromObj(SLP.REPORT())
        self.CREATED_AT = SLP.CREATED_AT()
        if SLP.ATTESTATION() is not None:
            self.ATTESTATION = SLPAttestation.SLPAttestationT.InitFromObj(SLP.ATTESTATION())

    # SLPT
    def Pack(self, builder):
        if self.PROBLEM_ID is not None:
            PROBLEM_ID = builder.CreateString(self.PROBLEM_ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.PROPAGATOR_PORT_ID is not None:
            PROPAGATOR_PORT_ID = builder.CreateString(self.PROPAGATOR_PORT_ID)
        if self.OBJECTIVE_EVALUATOR_PORT_ID is not None:
            OBJECTIVE_EVALUATOR_PORT_ID = builder.CreateString(self.OBJECTIVE_EVALUATOR_PORT_ID)
        if self.VARIABLES is not None:
            VARIABLESlist = []
            for i in range(len(self.VARIABLES)):
                VARIABLESlist.append(self.VARIABLES[i].Pack(builder))
            SLPStartVARIABLESVector(builder, len(self.VARIABLES))
            for i in reversed(range(len(self.VARIABLES))):
                builder.PrependUOffsetTRelative(VARIABLESlist[i])
            VARIABLES = builder.EndVector()
        if self.GOALS is not None:
            GOALSlist = []
            for i in range(len(self.GOALS)):
                GOALSlist.append(self.GOALS[i].Pack(builder))
            SLPStartGOALSVector(builder, len(self.GOALS))
            for i in reversed(range(len(self.GOALS))):
                builder.PrependUOffsetTRelative(GOALSlist[i])
            GOALS = builder.EndVector()
        if self.OBJECTIVE is not None:
            OBJECTIVE = self.OBJECTIVE.Pack(builder)
        if self.CONSTRAINTS is not None:
            CONSTRAINTSlist = []
            for i in range(len(self.CONSTRAINTS)):
                CONSTRAINTSlist.append(self.CONSTRAINTS[i].Pack(builder))
            SLPStartCONSTRAINTSVector(builder, len(self.CONSTRAINTS))
            for i in reversed(range(len(self.CONSTRAINTS))):
                builder.PrependUOffsetTRelative(CONSTRAINTSlist[i])
            CONSTRAINTS = builder.EndVector()
        if self.SETTINGS is not None:
            SETTINGS = self.SETTINGS.Pack(builder)
        if self.REPORT is not None:
            REPORT = self.REPORT.Pack(builder)
        if self.CREATED_AT is not None:
            CREATED_AT = builder.CreateString(self.CREATED_AT)
        if self.ATTESTATION is not None:
            ATTESTATION = self.ATTESTATION.Pack(builder)
        SLPStart(builder)
        if self.PROBLEM_ID is not None:
            SLPAddPROBLEM_ID(builder, PROBLEM_ID)
        if self.NAME is not None:
            SLPAddNAME(builder, NAME)
        SLPAddKIND(builder, self.KIND)
        if self.PROPAGATOR_PORT_ID is not None:
            SLPAddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID)
        if self.OBJECTIVE_EVALUATOR_PORT_ID is not None:
            SLPAddOBJECTIVE_EVALUATOR_PORT_ID(builder, OBJECTIVE_EVALUATOR_PORT_ID)
        if self.VARIABLES is not None:
            SLPAddVARIABLES(builder, VARIABLES)
        if self.GOALS is not None:
            SLPAddGOALS(builder, GOALS)
        if self.OBJECTIVE is not None:
            SLPAddOBJECTIVE(builder, OBJECTIVE)
        if self.CONSTRAINTS is not None:
            SLPAddCONSTRAINTS(builder, CONSTRAINTS)
        if self.SETTINGS is not None:
            SLPAddSETTINGS(builder, SETTINGS)
        if self.REPORT is not None:
            SLPAddREPORT(builder, REPORT)
        if self.CREATED_AT is not None:
            SLPAddCREATED_AT(builder, CREATED_AT)
        if self.ATTESTATION is not None:
            SLPAddATTESTATION(builder, ATTESTATION)
        SLP = SLPEnd(builder)
        return SLP
