# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A row a body's disc passes over.
class SKROccultation(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 32

    # SKROccultation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SKROccultation
    def BODY_ID(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # SKROccultation
    def ROW(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))
    # Closest approach of the body's centre to the row.
    # SKROccultation
    def MIN_SEPARATION_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # Where along the track the closest approach falls, from 0 (start) to 1
    # (end).
    # SKROccultation
    def TRACK_FRACTION(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))
    # SKROccultation
    def MAG(self): return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(24))
    # SKROccultation
    def RESERVED(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(28))

def CreateSKROccultation(builder, BODY_ID, ROW, MIN_SEPARATION_DEG, TRACK_FRACTION, MAG, RESERVED):
    builder.Prep(8, 32)
    builder.PrependUint32(RESERVED)
    builder.PrependFloat32(MAG)
    builder.PrependFloat64(TRACK_FRACTION)
    builder.PrependFloat64(MIN_SEPARATION_DEG)
    builder.PrependUint32(ROW)
    builder.PrependUint32(BODY_ID)
    return builder.Offset()


class SKROccultationT(object):

    # SKROccultationT
    def __init__(
        self,
        BODY_ID = 0,
        ROW = 0,
        MIN_SEPARATION_DEG = 0.0,
        TRACK_FRACTION = 0.0,
        MAG = 0.0,
        RESERVED = 0,
    ):
        self.BODY_ID = BODY_ID  # type: int
        self.ROW = ROW  # type: int
        self.MIN_SEPARATION_DEG = MIN_SEPARATION_DEG  # type: float
        self.TRACK_FRACTION = TRACK_FRACTION  # type: float
        self.MAG = MAG  # type: float
        self.RESERVED = RESERVED  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSkroccultation = SKROccultation()
        tmpSkroccultation.Init(buf, pos)
        return cls.InitFromObj(tmpSkroccultation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSkroccultation):
        x = SKROccultationT()
        x._UnPack(tmpSkroccultation)
        return x

    # SKROccultationT
    def _UnPack(self, SKROccultation):
        if SKROccultation is None:
            return
        self.BODY_ID = SKROccultation.BODY_ID()
        self.ROW = SKROccultation.ROW()
        self.MIN_SEPARATION_DEG = SKROccultation.MIN_SEPARATION_DEG()
        self.TRACK_FRACTION = SKROccultation.TRACK_FRACTION()
        self.MAG = SKROccultation.MAG()
        self.RESERVED = SKROccultation.RESERVED()

    # SKROccultationT
    def Pack(self, builder):
        return CreateSKROccultation(builder, self.BODY_ID, self.ROW, self.MIN_SEPARATION_DEG, self.TRACK_FRACTION, self.MAG, self.RESERVED)
