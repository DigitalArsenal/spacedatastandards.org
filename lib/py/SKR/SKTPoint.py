# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One point source packed for drawing (24 bytes).
class SKTPoint(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 24

    # SKTPoint
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Right ascension (or longitude in FRAME) as a fraction of a full turn:
    # degrees = RA_FIXED * 360 / 2^32.
    # SKTPoint
    def RA_FIXED(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # Declination (or latitude in FRAME): degrees = DEC_FIXED * 90 / 2^31.
    # SKTPoint
    def DEC_FIXED(self): return self._tab.Get(flatbuffers.number_types.Int32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))
    # Proper motion in RA times cos(DEC), milliarcseconds per year. -32768
    # when unknown or out of range (FLAGS bit 2).
    # SKTPoint
    def PMRA(self): return self._tab.Get(flatbuffers.number_types.Int16Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # Proper motion in DEC, milliarcseconds per year. -32768 when unknown or
    # out of range (FLAGS bit 2).
    # SKTPoint
    def PMDEC(self): return self._tab.Get(flatbuffers.number_types.Int16Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(10))
    # Parallax in units of 0.02 milliarcseconds. 0 when unknown, not
    # positive or out of range (FLAGS bit 3).
    # SKTPoint
    def PARALLAX(self): return self._tab.Get(flatbuffers.number_types.Uint16Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(12))
    # Magnitude in the tile's band, millimagnitudes. -32768 when unknown.
    # SKTPoint
    def MAG(self): return self._tab.Get(flatbuffers.number_types.Int16Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(14))
    # Colour, encoded as the tile's COLOR_ENCODING states.
    # SKTPoint
    def COLOR(self): return self._tab.Get(flatbuffers.number_types.Uint16Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))
    # Bit flags. 0: variable. 1: member of a multiple system. 2: PMRA/PMDEC
    # not usable. 3: PARALLAX not usable. 4: MAG is modelled, not measured.
    # 5: ROW refers to a `$CSO` rather than a `$STR`. 6: a neighbour lies
    # within the confusion radius. 7: extended source. 8-15: reserved, 0.
    # SKTPoint
    def FLAGS(self): return self._tab.Get(flatbuffers.number_types.Uint16Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(18))
    # Index of the source's full record in the tile's row collection
    # (ROW_COLLECTION_CID), from 0. 0xFFFFFFFF when the tile has none.
    # SKTPoint
    def ROW(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(20))

def CreateSKTPoint(builder, RA_FIXED, DEC_FIXED, PMRA, PMDEC, PARALLAX, MAG, COLOR, FLAGS, ROW):
    builder.Prep(4, 24)
    builder.PrependUint32(ROW)
    builder.PrependUint16(FLAGS)
    builder.PrependUint16(COLOR)
    builder.PrependInt16(MAG)
    builder.PrependUint16(PARALLAX)
    builder.PrependInt16(PMDEC)
    builder.PrependInt16(PMRA)
    builder.PrependInt32(DEC_FIXED)
    builder.PrependUint32(RA_FIXED)
    return builder.Offset()


class SKTPointT(object):

    # SKTPointT
    def __init__(
        self,
        RA_FIXED = 0,
        DEC_FIXED = 0,
        PMRA = 0,
        PMDEC = 0,
        PARALLAX = 0,
        MAG = 0,
        COLOR = 0,
        FLAGS = 0,
        ROW = 0,
    ):
        self.RA_FIXED = RA_FIXED  # type: int
        self.DEC_FIXED = DEC_FIXED  # type: int
        self.PMRA = PMRA  # type: int
        self.PMDEC = PMDEC  # type: int
        self.PARALLAX = PARALLAX  # type: int
        self.MAG = MAG  # type: int
        self.COLOR = COLOR  # type: int
        self.FLAGS = FLAGS  # type: int
        self.ROW = ROW  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSktpoint = SKTPoint()
        tmpSktpoint.Init(buf, pos)
        return cls.InitFromObj(tmpSktpoint)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSktpoint):
        x = SKTPointT()
        x._UnPack(tmpSktpoint)
        return x

    # SKTPointT
    def _UnPack(self, SKTPoint):
        if SKTPoint is None:
            return
        self.RA_FIXED = SKTPoint.RA_FIXED()
        self.DEC_FIXED = SKTPoint.DEC_FIXED()
        self.PMRA = SKTPoint.PMRA()
        self.PMDEC = SKTPoint.PMDEC()
        self.PARALLAX = SKTPoint.PARALLAX()
        self.MAG = SKTPoint.MAG()
        self.COLOR = SKTPoint.COLOR()
        self.FLAGS = SKTPoint.FLAGS()
        self.ROW = SKTPoint.ROW()

    # SKTPointT
    def Pack(self, builder):
        return CreateSKTPoint(builder, self.RA_FIXED, self.DEC_FIXED, self.PMRA, self.PMDEC, self.PARALLAX, self.MAG, self.COLOR, self.FLAGS, self.ROW)
