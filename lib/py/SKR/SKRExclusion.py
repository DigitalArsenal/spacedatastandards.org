# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Bright rows near one pointing.
class SKRExclusion(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 32

    # SKRExclusion
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SKRExclusion
    def POINTING_ID(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # Rows within the radius at least as bright as the threshold.
    # SKRExclusion
    def VIOLATING_COUNT(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))
    # The brightest of them; 4294967295 when there are none.
    # SKRExclusion
    def BRIGHTEST_ROW(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # SKRExclusion
    def RESERVED(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(12))
    # SKRExclusion
    def BRIGHTEST_SEPARATION_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))
    # SKRExclusion
    def BRIGHTEST_MAG(self): return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(24))
    # SKRExclusion
    def RESERVED2(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(28))

def CreateSKRExclusion(builder, POINTING_ID, VIOLATING_COUNT, BRIGHTEST_ROW, RESERVED, BRIGHTEST_SEPARATION_DEG, BRIGHTEST_MAG, RESERVED2):
    builder.Prep(8, 32)
    builder.PrependUint32(RESERVED2)
    builder.PrependFloat32(BRIGHTEST_MAG)
    builder.PrependFloat64(BRIGHTEST_SEPARATION_DEG)
    builder.PrependUint32(RESERVED)
    builder.PrependUint32(BRIGHTEST_ROW)
    builder.PrependUint32(VIOLATING_COUNT)
    builder.PrependUint32(POINTING_ID)
    return builder.Offset()


class SKRExclusionT(object):

    # SKRExclusionT
    def __init__(
        self,
        POINTING_ID = 0,
        VIOLATING_COUNT = 0,
        BRIGHTEST_ROW = 0,
        RESERVED = 0,
        BRIGHTEST_SEPARATION_DEG = 0.0,
        BRIGHTEST_MAG = 0.0,
        RESERVED2 = 0,
    ):
        self.POINTING_ID = POINTING_ID  # type: int
        self.VIOLATING_COUNT = VIOLATING_COUNT  # type: int
        self.BRIGHTEST_ROW = BRIGHTEST_ROW  # type: int
        self.RESERVED = RESERVED  # type: int
        self.BRIGHTEST_SEPARATION_DEG = BRIGHTEST_SEPARATION_DEG  # type: float
        self.BRIGHTEST_MAG = BRIGHTEST_MAG  # type: float
        self.RESERVED2 = RESERVED2  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSkrexclusion = SKRExclusion()
        tmpSkrexclusion.Init(buf, pos)
        return cls.InitFromObj(tmpSkrexclusion)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSkrexclusion):
        x = SKRExclusionT()
        x._UnPack(tmpSkrexclusion)
        return x

    # SKRExclusionT
    def _UnPack(self, SKRExclusion):
        if SKRExclusion is None:
            return
        self.POINTING_ID = SKRExclusion.POINTING_ID()
        self.VIOLATING_COUNT = SKRExclusion.VIOLATING_COUNT()
        self.BRIGHTEST_ROW = SKRExclusion.BRIGHTEST_ROW()
        self.RESERVED = SKRExclusion.RESERVED()
        self.BRIGHTEST_SEPARATION_DEG = SKRExclusion.BRIGHTEST_SEPARATION_DEG()
        self.BRIGHTEST_MAG = SKRExclusion.BRIGHTEST_MAG()
        self.RESERVED2 = SKRExclusion.RESERVED2()

    # SKRExclusionT
    def Pack(self, builder):
        return CreateSKRExclusion(builder, self.POINTING_ID, self.VIOLATING_COUNT, self.BRIGHTEST_ROW, self.RESERVED, self.BRIGHTEST_SEPARATION_DEG, self.BRIGHTEST_MAG, self.RESERVED2)
