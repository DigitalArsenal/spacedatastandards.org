# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A body's apparent path across the sky over an interval, and its disc.
class SKQTrack(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 48

    # SKQTrack
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Direction of the body's centre at the start of the interval.
    # SKQTrack
    def START_RA_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # SKQTrack
    def START_DEC_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # Direction at the end; equal to the start for an instant.
    # SKQTrack
    def END_RA_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))
    # SKQTrack
    def END_DEC_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(24))
    # Angular radius of the body's disc.
    # SKQTrack
    def RADIUS_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(32))
    # Caller's identifier for the body, echoed in the result.
    # SKQTrack
    def BODY_ID(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(40))
    # SKQTrack
    def RESERVED(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(44))

def CreateSKQTrack(builder, START_RA_DEG, START_DEC_DEG, END_RA_DEG, END_DEC_DEG, RADIUS_DEG, BODY_ID, RESERVED):
    builder.Prep(8, 48)
    builder.PrependUint32(RESERVED)
    builder.PrependUint32(BODY_ID)
    builder.PrependFloat64(RADIUS_DEG)
    builder.PrependFloat64(END_DEC_DEG)
    builder.PrependFloat64(END_RA_DEG)
    builder.PrependFloat64(START_DEC_DEG)
    builder.PrependFloat64(START_RA_DEG)
    return builder.Offset()


class SKQTrackT(object):

    # SKQTrackT
    def __init__(
        self,
        START_RA_DEG = 0.0,
        START_DEC_DEG = 0.0,
        END_RA_DEG = 0.0,
        END_DEC_DEG = 0.0,
        RADIUS_DEG = 0.0,
        BODY_ID = 0,
        RESERVED = 0,
    ):
        self.START_RA_DEG = START_RA_DEG  # type: float
        self.START_DEC_DEG = START_DEC_DEG  # type: float
        self.END_RA_DEG = END_RA_DEG  # type: float
        self.END_DEC_DEG = END_DEC_DEG  # type: float
        self.RADIUS_DEG = RADIUS_DEG  # type: float
        self.BODY_ID = BODY_ID  # type: int
        self.RESERVED = RESERVED  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSkqtrack = SKQTrack()
        tmpSkqtrack.Init(buf, pos)
        return cls.InitFromObj(tmpSkqtrack)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSkqtrack):
        x = SKQTrackT()
        x._UnPack(tmpSkqtrack)
        return x

    # SKQTrackT
    def _UnPack(self, SKQTrack):
        if SKQTrack is None:
            return
        self.START_RA_DEG = SKQTrack.START_RA_DEG()
        self.START_DEC_DEG = SKQTrack.START_DEC_DEG()
        self.END_RA_DEG = SKQTrack.END_RA_DEG()
        self.END_DEC_DEG = SKQTrack.END_DEC_DEG()
        self.RADIUS_DEG = SKQTrack.RADIUS_DEG()
        self.BODY_ID = SKQTrack.BODY_ID()
        self.RESERVED = SKQTrack.RESERVED()

    # SKQTrackT
    def Pack(self, builder):
        return CreateSKQTrack(builder, self.START_RA_DEG, self.START_DEC_DEG, self.END_RA_DEG, self.END_DEC_DEG, self.RADIUS_DEG, self.BODY_ID, self.RESERVED)
