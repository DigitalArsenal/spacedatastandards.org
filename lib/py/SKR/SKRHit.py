# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A row that answers a CONE or FIELD_OF_VIEW query.
class SKRHit(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 56

    # SKRHit
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SKRHit
    def ROW(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # SKTPoint FLAGS bits for the row.
    # SKRHit
    def FLAGS(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))
    # SKRHit
    def RA_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # SKRHit
    def DEC_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))
    # Angle from the centre or boresight.
    # SKRHit
    def SEPARATION_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(24))
    # Field coordinates: angles along the field's x and y axes (the
    # standard coordinates of a cone query). NaN behind the tangent plane.
    # SKRHit
    def FIELD_X_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(32))
    # SKRHit
    def FIELD_Y_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(40))
    # Magnitude in the query's band; NaN when unknown.
    # SKRHit
    def MAG(self): return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(48))
    # SKRHit
    def RESERVED(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(52))

def CreateSKRHit(builder, ROW, FLAGS, RA_DEG, DEC_DEG, SEPARATION_DEG, FIELD_X_DEG, FIELD_Y_DEG, MAG, RESERVED):
    builder.Prep(8, 56)
    builder.PrependUint32(RESERVED)
    builder.PrependFloat32(MAG)
    builder.PrependFloat64(FIELD_Y_DEG)
    builder.PrependFloat64(FIELD_X_DEG)
    builder.PrependFloat64(SEPARATION_DEG)
    builder.PrependFloat64(DEC_DEG)
    builder.PrependFloat64(RA_DEG)
    builder.PrependUint32(FLAGS)
    builder.PrependUint32(ROW)
    return builder.Offset()


class SKRHitT(object):

    # SKRHitT
    def __init__(
        self,
        ROW = 0,
        FLAGS = 0,
        RA_DEG = 0.0,
        DEC_DEG = 0.0,
        SEPARATION_DEG = 0.0,
        FIELD_X_DEG = 0.0,
        FIELD_Y_DEG = 0.0,
        MAG = 0.0,
        RESERVED = 0,
    ):
        self.ROW = ROW  # type: int
        self.FLAGS = FLAGS  # type: int
        self.RA_DEG = RA_DEG  # type: float
        self.DEC_DEG = DEC_DEG  # type: float
        self.SEPARATION_DEG = SEPARATION_DEG  # type: float
        self.FIELD_X_DEG = FIELD_X_DEG  # type: float
        self.FIELD_Y_DEG = FIELD_Y_DEG  # type: float
        self.MAG = MAG  # type: float
        self.RESERVED = RESERVED  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSkrhit = SKRHit()
        tmpSkrhit.Init(buf, pos)
        return cls.InitFromObj(tmpSkrhit)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSkrhit):
        x = SKRHitT()
        x._UnPack(tmpSkrhit)
        return x

    # SKRHitT
    def _UnPack(self, SKRHit):
        if SKRHit is None:
            return
        self.ROW = SKRHit.ROW()
        self.FLAGS = SKRHit.FLAGS()
        self.RA_DEG = SKRHit.RA_DEG()
        self.DEC_DEG = SKRHit.DEC_DEG()
        self.SEPARATION_DEG = SKRHit.SEPARATION_DEG()
        self.FIELD_X_DEG = SKRHit.FIELD_X_DEG()
        self.FIELD_Y_DEG = SKRHit.FIELD_Y_DEG()
        self.MAG = SKRHit.MAG()
        self.RESERVED = SKRHit.RESERVED()

    # SKRHitT
    def Pack(self, builder):
        return CreateSKRHit(builder, self.ROW, self.FLAGS, self.RA_DEG, self.DEC_DEG, self.SEPARATION_DEG, self.FIELD_X_DEG, self.FIELD_Y_DEG, self.MAG, self.RESERVED)
