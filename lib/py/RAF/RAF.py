# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Return All Frames Service (CCSDS 913.1-B-2)
class RAF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RAF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRAF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RAFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x41\x46", size_prefixed=size_prefixed)

    # RAF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PDU type
    # RAF
    def PDU_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Initiator identifier
    # RAF
    def INITIATOR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Responder port identifier
    # RAF
    def RESPONDER_PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Service type
    # RAF
    def SERVICE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Version number
    # RAF
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Invoke ID
    # RAF
    def INVOKE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Frame quality
    # RAF
    def FRAME_QUALITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Data
    # RAF
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # RAF
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # RAF
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RAF
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

def RAFStart(builder):
    builder.StartObject(8)

def Start(builder):
    RAFStart(builder)

def RAFAddPDU_TYPE(builder, PDU_TYPE):
    builder.PrependInt8Slot(0, PDU_TYPE, 0)

def AddPDU_TYPE(builder, PDU_TYPE):
    RAFAddPDU_TYPE(builder, PDU_TYPE)

def RAFAddINITIATOR_ID(builder, INITIATOR_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(INITIATOR_ID), 0)

def AddINITIATOR_ID(builder, INITIATOR_ID):
    RAFAddINITIATOR_ID(builder, INITIATOR_ID)

def RAFAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(RESPONDER_PORT_ID), 0)

def AddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID):
    RAFAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID)

def RAFAddSERVICE_TYPE(builder, SERVICE_TYPE):
    builder.PrependUint8Slot(3, SERVICE_TYPE, 0)

def AddSERVICE_TYPE(builder, SERVICE_TYPE):
    RAFAddSERVICE_TYPE(builder, SERVICE_TYPE)

def RAFAddVERSION(builder, VERSION):
    builder.PrependUint16Slot(4, VERSION, 0)

def AddVERSION(builder, VERSION):
    RAFAddVERSION(builder, VERSION)

def RAFAddINVOKE_ID(builder, INVOKE_ID):
    builder.PrependUint32Slot(5, INVOKE_ID, 0)

def AddINVOKE_ID(builder, INVOKE_ID):
    RAFAddINVOKE_ID(builder, INVOKE_ID)

def RAFAddFRAME_QUALITY(builder, FRAME_QUALITY):
    builder.PrependUint8Slot(6, FRAME_QUALITY, 0)

def AddFRAME_QUALITY(builder, FRAME_QUALITY):
    RAFAddFRAME_QUALITY(builder, FRAME_QUALITY)

def RAFAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    RAFAddDATA(builder, DATA)

def RAFStartDATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDATAVector(builder, numElems):
    return RAFStartDATAVector(builder, numElems)

def RAFEnd(builder):
    return builder.EndObject()

def End(builder):
    return RAFEnd(builder)

try:
    from typing import List
except:
    pass

class RAFT(object):

    # RAFT
    def __init__(self):
        self.PDU_TYPE = 0  # type: int
        self.INITIATOR_ID = None  # type: str
        self.RESPONDER_PORT_ID = None  # type: str
        self.SERVICE_TYPE = 0  # type: int
        self.VERSION = 0  # type: int
        self.INVOKE_ID = 0  # type: int
        self.FRAME_QUALITY = 0  # type: int
        self.DATA = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        RAF = RAF()
        RAF.Init(buf, pos)
        return cls.InitFromObj(RAF)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, RAF):
        x = RAFT()
        x._UnPack(RAF)
        return x

    # RAFT
    def _UnPack(self, RAF):
        if RAF is None:
            return
        self.PDU_TYPE = RAF.PDU_TYPE()
        self.INITIATOR_ID = RAF.INITIATOR_ID()
        self.RESPONDER_PORT_ID = RAF.RESPONDER_PORT_ID()
        self.SERVICE_TYPE = RAF.SERVICE_TYPE()
        self.VERSION = RAF.VERSION()
        self.INVOKE_ID = RAF.INVOKE_ID()
        self.FRAME_QUALITY = RAF.FRAME_QUALITY()
        if not RAF.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(RAF.DATALength()):
                    self.DATA.append(RAF.DATA(i))
            else:
                self.DATA = RAF.DATAAsNumpy()

    # RAFT
    def Pack(self, builder):
        if self.INITIATOR_ID is not None:
            INITIATOR_ID = builder.CreateString(self.INITIATOR_ID)
        if self.RESPONDER_PORT_ID is not None:
            RESPONDER_PORT_ID = builder.CreateString(self.RESPONDER_PORT_ID)
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                RAFStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependUint8(self.DATA[i])
                DATA = builder.EndVector()
        RAFStart(builder)
        RAFAddPDU_TYPE(builder, self.PDU_TYPE)
        if self.INITIATOR_ID is not None:
            RAFAddINITIATOR_ID(builder, INITIATOR_ID)
        if self.RESPONDER_PORT_ID is not None:
            RAFAddRESPONDER_PORT_ID(builder, RESPONDER_PORT_ID)
        RAFAddSERVICE_TYPE(builder, self.SERVICE_TYPE)
        RAFAddVERSION(builder, self.VERSION)
        RAFAddINVOKE_ID(builder, self.INVOKE_ID)
        RAFAddFRAME_QUALITY(builder, self.FRAME_QUALITY)
        if self.DATA is not None:
            RAFAddDATA(builder, DATA)
        RAF = RAFEnd(builder)
        return RAF
