# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A sensor pointing to check for bright stars.
class SKQPointing(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 24

    # SKQPointing
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SKQPointing
    def RA_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # SKQPointing
    def DEC_DEG(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # Caller's identifier for the pointing, echoed in the result.
    # SKQPointing
    def POINTING_ID(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))
    # SKQPointing
    def RESERVED(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(20))

def CreateSKQPointing(builder, RA_DEG, DEC_DEG, POINTING_ID, RESERVED):
    builder.Prep(8, 24)
    builder.PrependUint32(RESERVED)
    builder.PrependUint32(POINTING_ID)
    builder.PrependFloat64(DEC_DEG)
    builder.PrependFloat64(RA_DEG)
    return builder.Offset()


class SKQPointingT(object):

    # SKQPointingT
    def __init__(
        self,
        RA_DEG = 0.0,
        DEC_DEG = 0.0,
        POINTING_ID = 0,
        RESERVED = 0,
    ):
        self.RA_DEG = RA_DEG  # type: float
        self.DEC_DEG = DEC_DEG  # type: float
        self.POINTING_ID = POINTING_ID  # type: int
        self.RESERVED = RESERVED  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSkqpointing = SKQPointing()
        tmpSkqpointing.Init(buf, pos)
        return cls.InitFromObj(tmpSkqpointing)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSkqpointing):
        x = SKQPointingT()
        x._UnPack(tmpSkqpointing)
        return x

    # SKQPointingT
    def _UnPack(self, SKQPointing):
        if SKQPointing is None:
            return
        self.RA_DEG = SKQPointing.RA_DEG()
        self.DEC_DEG = SKQPointing.DEC_DEG()
        self.POINTING_ID = SKQPointing.POINTING_ID()
        self.RESERVED = SKQPointing.RESERVED()

    # SKQPointingT
    def Pack(self, builder):
        return CreateSKQPointing(builder, self.RA_DEG, self.DEC_DEG, self.POINTING_ID, self.RESERVED)
