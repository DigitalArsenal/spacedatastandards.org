# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CSMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CSMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCSMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CSMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x53\x4D", size_prefixed=size_prefixed)

    # CSMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # A vector of CSM records
    # CSMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from CSM import CSM
            obj = CSM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CSMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CSMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def CSMCOLLECTIONStart(builder): builder.StartObject(1)
def Start(builder):
    return CSMCOLLECTIONStart(builder)
def CSMCOLLECTIONAddRECORDS(builder, RECORDS): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)
def AddRECORDS(builder, RECORDS):
    return CSMCOLLECTIONAddRECORDS(builder, RECORDS)
def CSMCOLLECTIONStartRECORDSVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartRECORDSVector(builder, numElems):
    return CSMCOLLECTIONStartRECORDSVector(builder, numElems)
def CSMCOLLECTIONEnd(builder): return builder.EndObject()
def End(builder):
    return CSMCOLLECTIONEnd(builder)
import CSM
try:
    from typing import List
except:
    pass

class CSMCOLLECTIONT(object):

    # CSMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[CSM.CSMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        CSMCOLLECTION = CSMCOLLECTION()
        CSMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(CSMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, CSMCOLLECTION):
        x = CSMCOLLECTIONT()
        x._UnPack(CSMCOLLECTION)
        return x

    # CSMCOLLECTIONT
    def _UnPack(self, CSMCOLLECTION):
        if CSMCOLLECTION is None:
            return
        if not CSMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(CSMCOLLECTION.RECORDSLength()):
                if CSMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    cSM_ = CSM.CSMT.InitFromObj(CSMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(cSM_)

    # CSMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            CSMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        CSMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            CSMCOLLECTIONAddRECORDS(builder, RECORDS)
        CSMCOLLECTION = CSMCOLLECTIONEnd(builder)
        return CSMCOLLECTION
