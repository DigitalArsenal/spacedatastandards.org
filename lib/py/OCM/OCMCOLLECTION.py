# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class OCMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OCMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOCMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OCMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x43\x4D", size_prefixed=size_prefixed)

    # OCMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Collection of OCM records.
    # OCMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from OCM import OCM
            obj = OCM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OCMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # OCMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def OCMCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    OCMCOLLECTIONStart(builder)

def OCMCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    OCMCOLLECTIONAddRECORDS(builder, RECORDS)

def OCMCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return OCMCOLLECTIONStartRECORDSVector(builder, numElems)

def OCMCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return OCMCOLLECTIONEnd(builder)

import OCM
try:
    from typing import List
except:
    pass

class OCMCOLLECTIONT(object):

    # OCMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[OCM.OCMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        OCMCOLLECTION = OCMCOLLECTION()
        OCMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(OCMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, OCMCOLLECTION):
        x = OCMCOLLECTIONT()
        x._UnPack(OCMCOLLECTION)
        return x

    # OCMCOLLECTIONT
    def _UnPack(self, OCMCOLLECTION):
        if OCMCOLLECTION is None:
            return
        if not OCMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(OCMCOLLECTION.RECORDSLength()):
                if OCMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    oCM_ = OCM.OCMT.InitFromObj(OCMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(oCM_)

    # OCMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            OCMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        OCMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            OCMCOLLECTIONAddRECORDS(builder, RECORDS)
        OCMCOLLECTION = OCMCOLLECTIONEnd(builder)
        return OCMCOLLECTION
