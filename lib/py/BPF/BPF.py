# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Build Profile — one saved, portable build configuration.
#
# PORTABILITY. The export unit is a `$REC` collection carrying this record
# and, optionally, an `$APP` manifest that names it through an `APPDataRef`
# with `SDS_TYPE: "BPF"`. The `$APP` wrapper is launcher metadata and
# carries no authority; all authority over a profile lives in
# `ATTESTATION`. A bare size-prefixed `$BPF` buffer is also a complete,
# valid export.
class BPF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BPF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBPF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BPFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x50\x46", size_prefixed=size_prefixed)

    # BPF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable profile identity, unique per author. Required.
    # BPF
    def PROFILE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Display name of the profile. Required.
    # BPF
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable summary.
    # BPF
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
    # when the profile was created.
    # BPF
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
    # when the profile was last updated.
    # BPF
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # 64 lowercase hexadecimal characters encoding SHA-256 of the build
    # template this profile was authored against. Required: a profile that
    # does not say which template it configures cannot be validated against
    # one. A consumer holding a different template MAY still import the
    # profile but MUST mark it as authored against another template and MUST
    # NOT silently re-map its parts.
    # BPF
    def TEMPLATE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Template slots and their inclusion decisions. A slot the template
    # exposes and this vector omits is NOT included.
    # BPF
    def PARTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from BPFPart import BPFPart
            obj = BPFPart()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # BPF
    def PARTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BPF
    def PARTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Modules selected into the build and their protection tiers.
    # BPF
    def MODULES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from BPFModule import BPFModule
            obj = BPFModule()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # BPF
    def MODULESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BPF
    def MODULESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # The runtime restriction the produced artifact enforces. Required, so
    # that an unlocked build is a stated configuration rather than a missing
    # one.
    # BPF
    def RUNTIME_LOCK(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from BPFRuntimeLock import BPFRuntimeLock
            obj = BPFRuntimeLock()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Distribution-control mode of the produced artifact.
    # BPF
    def LICENSE_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Publisher attestation. Absent means the profile is unsigned; see
    # `BPFAttestation` for the fail-closed rule when it is present.
    # BPF
    def ATTESTATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from BPFAttestation import BPFAttestation
            obj = BPFAttestation()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def BPFStart(builder):
    builder.StartObject(11)

def Start(builder):
    BPFStart(builder)

def BPFAddPROFILE_ID(builder, PROFILE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PROFILE_ID), 0)

def AddPROFILE_ID(builder, PROFILE_ID):
    BPFAddPROFILE_ID(builder, PROFILE_ID)

def BPFAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    BPFAddNAME(builder, NAME)

def BPFAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    BPFAddDESCRIPTION(builder, DESCRIPTION)

def BPFAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CREATED_AT), 0)

def AddCREATED_AT(builder, CREATED_AT):
    BPFAddCREATED_AT(builder, CREATED_AT)

def BPFAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(UPDATED_AT), 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    BPFAddUPDATED_AT(builder, UPDATED_AT)

def BPFAddTEMPLATE_SHA256(builder, TEMPLATE_SHA256):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(TEMPLATE_SHA256), 0)

def AddTEMPLATE_SHA256(builder, TEMPLATE_SHA256):
    BPFAddTEMPLATE_SHA256(builder, TEMPLATE_SHA256)

def BPFAddPARTS(builder, PARTS):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PARTS), 0)

def AddPARTS(builder, PARTS):
    BPFAddPARTS(builder, PARTS)

def BPFStartPARTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPARTSVector(builder, numElems):
    return BPFStartPARTSVector(builder, numElems)

def BPFCreatePARTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePARTSVector(builder, data):
    BPFCreatePARTSVector(builder, data)

def BPFAddMODULES(builder, MODULES):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(MODULES), 0)

def AddMODULES(builder, MODULES):
    BPFAddMODULES(builder, MODULES)

def BPFStartMODULESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartMODULESVector(builder, numElems):
    return BPFStartMODULESVector(builder, numElems)

def BPFCreateMODULESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateMODULESVector(builder, data):
    BPFCreateMODULESVector(builder, data)

def BPFAddRUNTIME_LOCK(builder, RUNTIME_LOCK):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(RUNTIME_LOCK), 0)

def AddRUNTIME_LOCK(builder, RUNTIME_LOCK):
    BPFAddRUNTIME_LOCK(builder, RUNTIME_LOCK)

def BPFAddLICENSE_MODE(builder, LICENSE_MODE):
    builder.PrependInt8Slot(9, LICENSE_MODE, 0)

def AddLICENSE_MODE(builder, LICENSE_MODE):
    BPFAddLICENSE_MODE(builder, LICENSE_MODE)

def BPFAddATTESTATION(builder, ATTESTATION):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(ATTESTATION), 0)

def AddATTESTATION(builder, ATTESTATION):
    BPFAddATTESTATION(builder, ATTESTATION)

def BPFEnd(builder):
    return builder.EndObject()

def End(builder):
    return BPFEnd(builder)

import BPFAttestation
import BPFModule
import BPFPart
import BPFRuntimeLock
try:
    from typing import List, Optional
except:
    pass

class BPFT(object):

    # BPFT
    def __init__(
        self,
        PROFILE_ID = None,
        NAME = None,
        DESCRIPTION = None,
        CREATED_AT = None,
        UPDATED_AT = None,
        TEMPLATE_SHA256 = None,
        PARTS = None,
        MODULES = None,
        RUNTIME_LOCK = None,
        LICENSE_MODE = 0,
        ATTESTATION = None,
    ):
        self.PROFILE_ID = PROFILE_ID  # type: Optional[str]
        self.NAME = NAME  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.CREATED_AT = CREATED_AT  # type: Optional[str]
        self.UPDATED_AT = UPDATED_AT  # type: Optional[str]
        self.TEMPLATE_SHA256 = TEMPLATE_SHA256  # type: Optional[str]
        self.PARTS = PARTS  # type: Optional[List[BPFPart.BPFPartT]]
        self.MODULES = MODULES  # type: Optional[List[BPFModule.BPFModuleT]]
        self.RUNTIME_LOCK = RUNTIME_LOCK  # type: Optional[BPFRuntimeLock.BPFRuntimeLockT]
        self.LICENSE_MODE = LICENSE_MODE  # type: int
        self.ATTESTATION = ATTESTATION  # type: Optional[BPFAttestation.BPFAttestationT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpBpf = BPF()
        tmpBpf.Init(buf, pos)
        return cls.InitFromObj(tmpBpf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpBpf):
        x = BPFT()
        x._UnPack(tmpBpf)
        return x

    # BPFT
    def _UnPack(self, BPF):
        if BPF is None:
            return
        self.PROFILE_ID = BPF.PROFILE_ID()
        self.NAME = BPF.NAME()
        self.DESCRIPTION = BPF.DESCRIPTION()
        self.CREATED_AT = BPF.CREATED_AT()
        self.UPDATED_AT = BPF.UPDATED_AT()
        self.TEMPLATE_SHA256 = BPF.TEMPLATE_SHA256()
        if not BPF.PARTSIsNone():
            self.PARTS = []
            for i in range(BPF.PARTSLength()):
                if BPF.PARTS(i) is None:
                    self.PARTS.append(None)
                else:
                    bPFPart_ = BPFPart.BPFPartT.InitFromObj(BPF.PARTS(i))
                    self.PARTS.append(bPFPart_)
        if not BPF.MODULESIsNone():
            self.MODULES = []
            for i in range(BPF.MODULESLength()):
                if BPF.MODULES(i) is None:
                    self.MODULES.append(None)
                else:
                    bPFModule_ = BPFModule.BPFModuleT.InitFromObj(BPF.MODULES(i))
                    self.MODULES.append(bPFModule_)
        if BPF.RUNTIME_LOCK() is not None:
            self.RUNTIME_LOCK = BPFRuntimeLock.BPFRuntimeLockT.InitFromObj(BPF.RUNTIME_LOCK())
        self.LICENSE_MODE = BPF.LICENSE_MODE()
        if BPF.ATTESTATION() is not None:
            self.ATTESTATION = BPFAttestation.BPFAttestationT.InitFromObj(BPF.ATTESTATION())

    # BPFT
    def Pack(self, builder):
        if self.PROFILE_ID is not None:
            PROFILE_ID = builder.CreateString(self.PROFILE_ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.CREATED_AT is not None:
            CREATED_AT = builder.CreateString(self.CREATED_AT)
        if self.UPDATED_AT is not None:
            UPDATED_AT = builder.CreateString(self.UPDATED_AT)
        if self.TEMPLATE_SHA256 is not None:
            TEMPLATE_SHA256 = builder.CreateString(self.TEMPLATE_SHA256)
        if self.PARTS is not None:
            PARTSlist = []
            for i in range(len(self.PARTS)):
                PARTSlist.append(self.PARTS[i].Pack(builder))
            BPFStartPARTSVector(builder, len(self.PARTS))
            for i in reversed(range(len(self.PARTS))):
                builder.PrependUOffsetTRelative(PARTSlist[i])
            PARTS = builder.EndVector()
        if self.MODULES is not None:
            MODULESlist = []
            for i in range(len(self.MODULES)):
                MODULESlist.append(self.MODULES[i].Pack(builder))
            BPFStartMODULESVector(builder, len(self.MODULES))
            for i in reversed(range(len(self.MODULES))):
                builder.PrependUOffsetTRelative(MODULESlist[i])
            MODULES = builder.EndVector()
        if self.RUNTIME_LOCK is not None:
            RUNTIME_LOCK = self.RUNTIME_LOCK.Pack(builder)
        if self.ATTESTATION is not None:
            ATTESTATION = self.ATTESTATION.Pack(builder)
        BPFStart(builder)
        if self.PROFILE_ID is not None:
            BPFAddPROFILE_ID(builder, PROFILE_ID)
        if self.NAME is not None:
            BPFAddNAME(builder, NAME)
        if self.DESCRIPTION is not None:
            BPFAddDESCRIPTION(builder, DESCRIPTION)
        if self.CREATED_AT is not None:
            BPFAddCREATED_AT(builder, CREATED_AT)
        if self.UPDATED_AT is not None:
            BPFAddUPDATED_AT(builder, UPDATED_AT)
        if self.TEMPLATE_SHA256 is not None:
            BPFAddTEMPLATE_SHA256(builder, TEMPLATE_SHA256)
        if self.PARTS is not None:
            BPFAddPARTS(builder, PARTS)
        if self.MODULES is not None:
            BPFAddMODULES(builder, MODULES)
        if self.RUNTIME_LOCK is not None:
            BPFAddRUNTIME_LOCK(builder, RUNTIME_LOCK)
        BPFAddLICENSE_MODE(builder, self.LICENSE_MODE)
        if self.ATTESTATION is not None:
            BPFAddATTESTATION(builder, ATTESTATION)
        BPF = BPFEnd(builder)
        return BPF
