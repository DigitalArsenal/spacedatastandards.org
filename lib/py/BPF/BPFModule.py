# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One module selected into the build and the tier its payload is protected
# at.
class BPFModule(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BPFModule()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBPFModule(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BPFModuleBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x50\x46", size_prefixed=size_prefixed)

    # BPFModule
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Canonical module identifier. Required, unique within the profile.
    # BPFModule
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Module version or version range.
    # BPFModule
    def MODULE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # True when the module is embedded in the produced artifact.
    # BPFModule
    def INCLUDED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Protection tier applied to this module's payload.
    # BPFModule
    def PROTECTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # 64 lowercase hexadecimal characters encoding SHA-256 of the module's
    # portable (pre-AOT, publication-trailer-stripped) bytes. This is the
    # identity a capability or signature policy keys on.
    # BPFModule
    def CONTENT_HASH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Published module descriptor, carried when the authoring context holds
    # it. Advisory: `MODULE_ID` and `CONTENT_HASH` are the identity, and a
    # consumer MUST NOT prefer a carried descriptor over the module's own
    # published manifest.
    # BPFModule
    def MODULE_DESCRIPTOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PLG import PLG
            obj = PLG()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def BPFModuleStart(builder):
    builder.StartObject(6)

def Start(builder):
    BPFModuleStart(builder)

def BPFModuleAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    BPFModuleAddMODULE_ID(builder, MODULE_ID)

def BPFModuleAddMODULE_VERSION(builder, MODULE_VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_VERSION), 0)

def AddMODULE_VERSION(builder, MODULE_VERSION):
    BPFModuleAddMODULE_VERSION(builder, MODULE_VERSION)

def BPFModuleAddINCLUDED(builder, INCLUDED):
    builder.PrependBoolSlot(2, INCLUDED, 0)

def AddINCLUDED(builder, INCLUDED):
    BPFModuleAddINCLUDED(builder, INCLUDED)

def BPFModuleAddPROTECTION(builder, PROTECTION):
    builder.PrependInt8Slot(3, PROTECTION, 0)

def AddPROTECTION(builder, PROTECTION):
    BPFModuleAddPROTECTION(builder, PROTECTION)

def BPFModuleAddCONTENT_HASH(builder, CONTENT_HASH):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT_HASH), 0)

def AddCONTENT_HASH(builder, CONTENT_HASH):
    BPFModuleAddCONTENT_HASH(builder, CONTENT_HASH)

def BPFModuleAddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_DESCRIPTOR), 0)

def AddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR):
    BPFModuleAddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR)

def BPFModuleEnd(builder):
    return builder.EndObject()

def End(builder):
    return BPFModuleEnd(builder)

import PLG
try:
    from typing import Optional
except:
    pass

class BPFModuleT(object):

    # BPFModuleT
    def __init__(
        self,
        MODULE_ID = None,
        MODULE_VERSION = None,
        INCLUDED = False,
        PROTECTION = 0,
        CONTENT_HASH = None,
        MODULE_DESCRIPTOR = None,
    ):
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.MODULE_VERSION = MODULE_VERSION  # type: Optional[str]
        self.INCLUDED = INCLUDED  # type: bool
        self.PROTECTION = PROTECTION  # type: int
        self.CONTENT_HASH = CONTENT_HASH  # type: Optional[str]
        self.MODULE_DESCRIPTOR = MODULE_DESCRIPTOR  # type: Optional[PLG.PLGT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpBpfmodule = BPFModule()
        tmpBpfmodule.Init(buf, pos)
        return cls.InitFromObj(tmpBpfmodule)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpBpfmodule):
        x = BPFModuleT()
        x._UnPack(tmpBpfmodule)
        return x

    # BPFModuleT
    def _UnPack(self, BPFModule):
        if BPFModule is None:
            return
        self.MODULE_ID = BPFModule.MODULE_ID()
        self.MODULE_VERSION = BPFModule.MODULE_VERSION()
        self.INCLUDED = BPFModule.INCLUDED()
        self.PROTECTION = BPFModule.PROTECTION()
        self.CONTENT_HASH = BPFModule.CONTENT_HASH()
        if BPFModule.MODULE_DESCRIPTOR() is not None:
            self.MODULE_DESCRIPTOR = PLG.PLGT.InitFromObj(BPFModule.MODULE_DESCRIPTOR())

    # BPFModuleT
    def Pack(self, builder):
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.MODULE_VERSION is not None:
            MODULE_VERSION = builder.CreateString(self.MODULE_VERSION)
        if self.CONTENT_HASH is not None:
            CONTENT_HASH = builder.CreateString(self.CONTENT_HASH)
        if self.MODULE_DESCRIPTOR is not None:
            MODULE_DESCRIPTOR = self.MODULE_DESCRIPTOR.Pack(builder)
        BPFModuleStart(builder)
        if self.MODULE_ID is not None:
            BPFModuleAddMODULE_ID(builder, MODULE_ID)
        if self.MODULE_VERSION is not None:
            BPFModuleAddMODULE_VERSION(builder, MODULE_VERSION)
        BPFModuleAddINCLUDED(builder, self.INCLUDED)
        BPFModuleAddPROTECTION(builder, self.PROTECTION)
        if self.CONTENT_HASH is not None:
            BPFModuleAddCONTENT_HASH(builder, CONTENT_HASH)
        if self.MODULE_DESCRIPTOR is not None:
            BPFModuleAddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR)
        BPFModule = BPFModuleEnd(builder)
        return BPFModule
