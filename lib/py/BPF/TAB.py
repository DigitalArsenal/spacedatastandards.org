# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Typed Arena Buffer — one descriptor for a payload slot in a shared arena.
class TAB(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TAB()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTAB(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TABBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x41\x42", size_prefixed=size_prefixed)

    # TAB
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Byte offset of the payload body within the arena.
    # TAB
    def OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Byte length of the payload body.
    # TAB
    def SIZE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Required start alignment of the payload body (in bytes).
    # TAB
    def ALIGNMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Wire format for the body.
    # TAB
    def WIRE_FORMAT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Optional payload schema identity.
    # TAB
    def TYPE_REF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FlatBufferTypeRef import FlatBufferTypeRef
            obj = FlatBufferTypeRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Mutability contract for the slot.
    # TAB
    def MUTABILITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Ownership contract for the slot.
    # TAB
    def OWNERSHIP(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Optional opaque frame identifier for stream bookkeeping.
    # TAB
    def FRAME_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Optional port identifier for frames that route to/from a named
    # input or output port on a method (maps to
    # `PLG.PLGPortManifest.PORT_ID`). Empty for arena frames that carry
    # no port routing hint.
    # TAB
    def PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TABStart(builder):
    builder.StartObject(9)

def Start(builder):
    TABStart(builder)

def TABAddOFFSET(builder, OFFSET):
    builder.PrependUint32Slot(0, OFFSET, 0)

def AddOFFSET(builder, OFFSET):
    TABAddOFFSET(builder, OFFSET)

def TABAddSIZE(builder, SIZE):
    builder.PrependUint32Slot(1, SIZE, 0)

def AddSIZE(builder, SIZE):
    TABAddSIZE(builder, SIZE)

def TABAddALIGNMENT(builder, ALIGNMENT):
    builder.PrependUint32Slot(2, ALIGNMENT, 0)

def AddALIGNMENT(builder, ALIGNMENT):
    TABAddALIGNMENT(builder, ALIGNMENT)

def TABAddWIRE_FORMAT(builder, WIRE_FORMAT):
    builder.PrependUint8Slot(3, WIRE_FORMAT, 0)

def AddWIRE_FORMAT(builder, WIRE_FORMAT):
    TABAddWIRE_FORMAT(builder, WIRE_FORMAT)

def TABAddTYPE_REF(builder, TYPE_REF):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(TYPE_REF), 0)

def AddTYPE_REF(builder, TYPE_REF):
    TABAddTYPE_REF(builder, TYPE_REF)

def TABAddMUTABILITY(builder, MUTABILITY):
    builder.PrependUint8Slot(5, MUTABILITY, 0)

def AddMUTABILITY(builder, MUTABILITY):
    TABAddMUTABILITY(builder, MUTABILITY)

def TABAddOWNERSHIP(builder, OWNERSHIP):
    builder.PrependUint8Slot(6, OWNERSHIP, 0)

def AddOWNERSHIP(builder, OWNERSHIP):
    TABAddOWNERSHIP(builder, OWNERSHIP)

def TABAddFRAME_ID(builder, FRAME_ID):
    builder.PrependUint64Slot(7, FRAME_ID, 0)

def AddFRAME_ID(builder, FRAME_ID):
    TABAddFRAME_ID(builder, FRAME_ID)

def TABAddPORT_ID(builder, PORT_ID):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(PORT_ID), 0)

def AddPORT_ID(builder, PORT_ID):
    TABAddPORT_ID(builder, PORT_ID)

def TABEnd(builder):
    return builder.EndObject()

def End(builder):
    return TABEnd(builder)

import FlatBufferTypeRef
try:
    from typing import Optional
except:
    pass

class TABT(object):

    # TABT
    def __init__(
        self,
        OFFSET = 0,
        SIZE = 0,
        ALIGNMENT = 0,
        WIRE_FORMAT = 0,
        TYPE_REF = None,
        MUTABILITY = 0,
        OWNERSHIP = 0,
        FRAME_ID = 0,
        PORT_ID = None,
    ):
        self.OFFSET = OFFSET  # type: int
        self.SIZE = SIZE  # type: int
        self.ALIGNMENT = ALIGNMENT  # type: int
        self.WIRE_FORMAT = WIRE_FORMAT  # type: int
        self.TYPE_REF = TYPE_REF  # type: Optional[FlatBufferTypeRef.FlatBufferTypeRefT]
        self.MUTABILITY = MUTABILITY  # type: int
        self.OWNERSHIP = OWNERSHIP  # type: int
        self.FRAME_ID = FRAME_ID  # type: int
        self.PORT_ID = PORT_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTab = TAB()
        tmpTab.Init(buf, pos)
        return cls.InitFromObj(tmpTab)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTab):
        x = TABT()
        x._UnPack(tmpTab)
        return x

    # TABT
    def _UnPack(self, TAB):
        if TAB is None:
            return
        self.OFFSET = TAB.OFFSET()
        self.SIZE = TAB.SIZE()
        self.ALIGNMENT = TAB.ALIGNMENT()
        self.WIRE_FORMAT = TAB.WIRE_FORMAT()
        if TAB.TYPE_REF() is not None:
            self.TYPE_REF = FlatBufferTypeRef.FlatBufferTypeRefT.InitFromObj(TAB.TYPE_REF())
        self.MUTABILITY = TAB.MUTABILITY()
        self.OWNERSHIP = TAB.OWNERSHIP()
        self.FRAME_ID = TAB.FRAME_ID()
        self.PORT_ID = TAB.PORT_ID()

    # TABT
    def Pack(self, builder):
        if self.TYPE_REF is not None:
            TYPE_REF = self.TYPE_REF.Pack(builder)
        if self.PORT_ID is not None:
            PORT_ID = builder.CreateString(self.PORT_ID)
        TABStart(builder)
        TABAddOFFSET(builder, self.OFFSET)
        TABAddSIZE(builder, self.SIZE)
        TABAddALIGNMENT(builder, self.ALIGNMENT)
        TABAddWIRE_FORMAT(builder, self.WIRE_FORMAT)
        if self.TYPE_REF is not None:
            TABAddTYPE_REF(builder, TYPE_REF)
        TABAddMUTABILITY(builder, self.MUTABILITY)
        TABAddOWNERSHIP(builder, self.OWNERSHIP)
        TABAddFRAME_ID(builder, self.FRAME_ID)
        if self.PORT_ID is not None:
            TABAddPORT_ID(builder, PORT_ID)
        TAB = TABEnd(builder)
        return TAB
