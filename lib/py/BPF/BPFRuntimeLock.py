# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# The runtime restriction the produced artifact enforces on itself.
#
# AUTHORIZATION. A host is authorized when it equals an `ALLOWED_DOMAINS`
# entry, OR ends with an `ALLOWED_TLDS` entry, OR equals a `DEV_DOMAINS`
# entry. Suffix rules are DOT-ANCHORED: `.mil` admits `x.mil` and `a.b.mil`
# and admits neither `mil` nor `evilmil`; a suffix rule never matches a
# host that merely contains it (`x.mil.example` is refused).
#
# UNLOCKED IS EXPLICIT. `ALLOWED_DOMAINS` empty AND `ALLOWED_TLDS` empty
# means every host is authorized. This is a stated, allowed configuration,
# which is why this table is required on `BPF`: an absent lock is never
# read as an unlocked one.
#
# EXPIRY IS INDEPENDENT of authorization: an unlocked profile still
# expires. There is deliberately no stored expiry instant — expiry is
# `COMPILED_AT_MS + TTL_DAYS * 86400000` and a second, storable copy of it
# could disagree with the two values that produce it.
class BPFRuntimeLock(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BPFRuntimeLock()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBPFRuntimeLock(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BPFRuntimeLockBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x50\x46", size_prefixed=size_prefixed)

    # BPFRuntimeLock
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Exact hostnames authorized to run the produced artifact. Empty
    # together with `ALLOWED_TLDS` means unrestricted.
    # BPFRuntimeLock
    def ALLOWED_DOMAINS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # BPFRuntimeLock
    def ALLOWED_DOMAINSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BPFRuntimeLock
    def ALLOWED_DOMAINSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Dot-anchored hostname suffix rules, each written with its leading dot,
    # for example `.gov`. Empty together with `ALLOWED_DOMAINS` means
    # unrestricted.
    # BPFRuntimeLock
    def ALLOWED_TLDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # BPFRuntimeLock
    def ALLOWED_TLDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BPFRuntimeLock
    def ALLOWED_TLDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Development hosts authorized in addition to the rules above, so a
    # locked profile remains runnable in a development context. Kept
    # separate from `ALLOWED_DOMAINS` because a development exemption and a
    # deployment authorization are different facts: folded together, a
    # development host would be indistinguishable from a deployment grant
    # and would silently persist into a locked artifact.
    # BPFRuntimeLock
    def DEV_DOMAINS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # BPFRuntimeLock
    def DEV_DOMAINSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BPFRuntimeLock
    def DEV_DOMAINSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Lifetime of the produced artifact in days from `COMPILED_AT_MS`.
    # Normative range 1 through 365 inclusive; 180 when unstated. A value of
    # 0 or above 365 is invalid and MUST be rejected, never clamped.
    # BPFRuntimeLock
    def TTL_DAYS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 180

    # Milliseconds since the Unix epoch at which the artifact was composed.
    # 0 means the profile has not been used to compose an artifact yet and
    # carries no expiry; the composing step stamps it.
    # BPFRuntimeLock
    def COMPILED_AT_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def BPFRuntimeLockStart(builder):
    builder.StartObject(5)

def Start(builder):
    BPFRuntimeLockStart(builder)

def BPFRuntimeLockAddALLOWED_DOMAINS(builder, ALLOWED_DOMAINS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ALLOWED_DOMAINS), 0)

def AddALLOWED_DOMAINS(builder, ALLOWED_DOMAINS):
    BPFRuntimeLockAddALLOWED_DOMAINS(builder, ALLOWED_DOMAINS)

def BPFRuntimeLockStartALLOWED_DOMAINSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartALLOWED_DOMAINSVector(builder, numElems):
    return BPFRuntimeLockStartALLOWED_DOMAINSVector(builder, numElems)

def BPFRuntimeLockCreateALLOWED_DOMAINSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateALLOWED_DOMAINSVector(builder, data):
    BPFRuntimeLockCreateALLOWED_DOMAINSVector(builder, data)

def BPFRuntimeLockAddALLOWED_TLDS(builder, ALLOWED_TLDS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ALLOWED_TLDS), 0)

def AddALLOWED_TLDS(builder, ALLOWED_TLDS):
    BPFRuntimeLockAddALLOWED_TLDS(builder, ALLOWED_TLDS)

def BPFRuntimeLockStartALLOWED_TLDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartALLOWED_TLDSVector(builder, numElems):
    return BPFRuntimeLockStartALLOWED_TLDSVector(builder, numElems)

def BPFRuntimeLockCreateALLOWED_TLDSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateALLOWED_TLDSVector(builder, data):
    BPFRuntimeLockCreateALLOWED_TLDSVector(builder, data)

def BPFRuntimeLockAddDEV_DOMAINS(builder, DEV_DOMAINS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DEV_DOMAINS), 0)

def AddDEV_DOMAINS(builder, DEV_DOMAINS):
    BPFRuntimeLockAddDEV_DOMAINS(builder, DEV_DOMAINS)

def BPFRuntimeLockStartDEV_DOMAINSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDEV_DOMAINSVector(builder, numElems):
    return BPFRuntimeLockStartDEV_DOMAINSVector(builder, numElems)

def BPFRuntimeLockCreateDEV_DOMAINSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateDEV_DOMAINSVector(builder, data):
    BPFRuntimeLockCreateDEV_DOMAINSVector(builder, data)

def BPFRuntimeLockAddTTL_DAYS(builder, TTL_DAYS):
    builder.PrependUint32Slot(3, TTL_DAYS, 180)

def AddTTL_DAYS(builder, TTL_DAYS):
    BPFRuntimeLockAddTTL_DAYS(builder, TTL_DAYS)

def BPFRuntimeLockAddCOMPILED_AT_MS(builder, COMPILED_AT_MS):
    builder.PrependUint64Slot(4, COMPILED_AT_MS, 0)

def AddCOMPILED_AT_MS(builder, COMPILED_AT_MS):
    BPFRuntimeLockAddCOMPILED_AT_MS(builder, COMPILED_AT_MS)

def BPFRuntimeLockEnd(builder):
    return builder.EndObject()

def End(builder):
    return BPFRuntimeLockEnd(builder)

try:
    from typing import List
except:
    pass

class BPFRuntimeLockT(object):

    # BPFRuntimeLockT
    def __init__(
        self,
        ALLOWED_DOMAINS = None,
        ALLOWED_TLDS = None,
        DEV_DOMAINS = None,
        TTL_DAYS = 180,
        COMPILED_AT_MS = 0,
    ):
        self.ALLOWED_DOMAINS = ALLOWED_DOMAINS  # type: Optional[List[Optional[str]]]
        self.ALLOWED_TLDS = ALLOWED_TLDS  # type: Optional[List[Optional[str]]]
        self.DEV_DOMAINS = DEV_DOMAINS  # type: Optional[List[Optional[str]]]
        self.TTL_DAYS = TTL_DAYS  # type: int
        self.COMPILED_AT_MS = COMPILED_AT_MS  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpBpfruntimeLock = BPFRuntimeLock()
        tmpBpfruntimeLock.Init(buf, pos)
        return cls.InitFromObj(tmpBpfruntimeLock)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpBpfruntimeLock):
        x = BPFRuntimeLockT()
        x._UnPack(tmpBpfruntimeLock)
        return x

    # BPFRuntimeLockT
    def _UnPack(self, BPFRuntimeLock):
        if BPFRuntimeLock is None:
            return
        if not BPFRuntimeLock.ALLOWED_DOMAINSIsNone():
            self.ALLOWED_DOMAINS = []
            for i in range(BPFRuntimeLock.ALLOWED_DOMAINSLength()):
                self.ALLOWED_DOMAINS.append(BPFRuntimeLock.ALLOWED_DOMAINS(i))
        if not BPFRuntimeLock.ALLOWED_TLDSIsNone():
            self.ALLOWED_TLDS = []
            for i in range(BPFRuntimeLock.ALLOWED_TLDSLength()):
                self.ALLOWED_TLDS.append(BPFRuntimeLock.ALLOWED_TLDS(i))
        if not BPFRuntimeLock.DEV_DOMAINSIsNone():
            self.DEV_DOMAINS = []
            for i in range(BPFRuntimeLock.DEV_DOMAINSLength()):
                self.DEV_DOMAINS.append(BPFRuntimeLock.DEV_DOMAINS(i))
        self.TTL_DAYS = BPFRuntimeLock.TTL_DAYS()
        self.COMPILED_AT_MS = BPFRuntimeLock.COMPILED_AT_MS()

    # BPFRuntimeLockT
    def Pack(self, builder):
        if self.ALLOWED_DOMAINS is not None:
            ALLOWED_DOMAINSlist = []
            for i in range(len(self.ALLOWED_DOMAINS)):
                ALLOWED_DOMAINSlist.append(builder.CreateString(self.ALLOWED_DOMAINS[i]))
            BPFRuntimeLockStartALLOWED_DOMAINSVector(builder, len(self.ALLOWED_DOMAINS))
            for i in reversed(range(len(self.ALLOWED_DOMAINS))):
                builder.PrependUOffsetTRelative(ALLOWED_DOMAINSlist[i])
            ALLOWED_DOMAINS = builder.EndVector()
        if self.ALLOWED_TLDS is not None:
            ALLOWED_TLDSlist = []
            for i in range(len(self.ALLOWED_TLDS)):
                ALLOWED_TLDSlist.append(builder.CreateString(self.ALLOWED_TLDS[i]))
            BPFRuntimeLockStartALLOWED_TLDSVector(builder, len(self.ALLOWED_TLDS))
            for i in reversed(range(len(self.ALLOWED_TLDS))):
                builder.PrependUOffsetTRelative(ALLOWED_TLDSlist[i])
            ALLOWED_TLDS = builder.EndVector()
        if self.DEV_DOMAINS is not None:
            DEV_DOMAINSlist = []
            for i in range(len(self.DEV_DOMAINS)):
                DEV_DOMAINSlist.append(builder.CreateString(self.DEV_DOMAINS[i]))
            BPFRuntimeLockStartDEV_DOMAINSVector(builder, len(self.DEV_DOMAINS))
            for i in reversed(range(len(self.DEV_DOMAINS))):
                builder.PrependUOffsetTRelative(DEV_DOMAINSlist[i])
            DEV_DOMAINS = builder.EndVector()
        BPFRuntimeLockStart(builder)
        if self.ALLOWED_DOMAINS is not None:
            BPFRuntimeLockAddALLOWED_DOMAINS(builder, ALLOWED_DOMAINS)
        if self.ALLOWED_TLDS is not None:
            BPFRuntimeLockAddALLOWED_TLDS(builder, ALLOWED_TLDS)
        if self.DEV_DOMAINS is not None:
            BPFRuntimeLockAddDEV_DOMAINS(builder, DEV_DOMAINS)
        BPFRuntimeLockAddTTL_DAYS(builder, self.TTL_DAYS)
        BPFRuntimeLockAddCOMPILED_AT_MS(builder, self.COMPILED_AT_MS)
        BPFRuntimeLock = BPFRuntimeLockEnd(builder)
        return BPFRuntimeLock
