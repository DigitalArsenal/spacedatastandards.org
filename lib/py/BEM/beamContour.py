# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Beam Contour (iso-gain boundary)
class beamContour(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = beamContour()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsbeamContour(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def beamContourBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x45\x4D", size_prefixed=size_prefixed)

    # beamContour
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Contour level identifier
    # beamContour
    def CONTOUR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Gain level in dBi
    # beamContour
    def GAIN_LEVEL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Contour boundary points
    # beamContour
    def POINTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from beamContourPoint import beamContourPoint
            obj = beamContourPoint()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # beamContour
    def POINTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # beamContour
    def POINTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def beamContourStart(builder):
    builder.StartObject(3)

def Start(builder):
    beamContourStart(builder)

def beamContourAddCONTOUR_ID(builder, CONTOUR_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CONTOUR_ID), 0)

def AddCONTOUR_ID(builder, CONTOUR_ID):
    beamContourAddCONTOUR_ID(builder, CONTOUR_ID)

def beamContourAddGAIN_LEVEL(builder, GAIN_LEVEL):
    builder.PrependFloat64Slot(1, GAIN_LEVEL, 0.0)

def AddGAIN_LEVEL(builder, GAIN_LEVEL):
    beamContourAddGAIN_LEVEL(builder, GAIN_LEVEL)

def beamContourAddPOINTS(builder, POINTS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(POINTS), 0)

def AddPOINTS(builder, POINTS):
    beamContourAddPOINTS(builder, POINTS)

def beamContourStartPOINTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPOINTSVector(builder, numElems):
    return beamContourStartPOINTSVector(builder, numElems)

def beamContourEnd(builder):
    return builder.EndObject()

def End(builder):
    return beamContourEnd(builder)

import beamContourPoint
try:
    from typing import List
except:
    pass

class beamContourT(object):

    # beamContourT
    def __init__(self):
        self.CONTOUR_ID = None  # type: str
        self.GAIN_LEVEL = 0.0  # type: float
        self.POINTS = None  # type: List[beamContourPoint.beamContourPointT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        beamContour = beamContour()
        beamContour.Init(buf, pos)
        return cls.InitFromObj(beamContour)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, beamContour):
        x = beamContourT()
        x._UnPack(beamContour)
        return x

    # beamContourT
    def _UnPack(self, beamContour):
        if beamContour is None:
            return
        self.CONTOUR_ID = beamContour.CONTOUR_ID()
        self.GAIN_LEVEL = beamContour.GAIN_LEVEL()
        if not beamContour.POINTSIsNone():
            self.POINTS = []
            for i in range(beamContour.POINTSLength()):
                if beamContour.POINTS(i) is None:
                    self.POINTS.append(None)
                else:
                    beamContourPoint_ = beamContourPoint.beamContourPointT.InitFromObj(beamContour.POINTS(i))
                    self.POINTS.append(beamContourPoint_)

    # beamContourT
    def Pack(self, builder):
        if self.CONTOUR_ID is not None:
            CONTOUR_ID = builder.CreateString(self.CONTOUR_ID)
        if self.POINTS is not None:
            POINTSlist = []
            for i in range(len(self.POINTS)):
                POINTSlist.append(self.POINTS[i].Pack(builder))
            beamContourStartPOINTSVector(builder, len(self.POINTS))
            for i in reversed(range(len(self.POINTS))):
                builder.PrependUOffsetTRelative(POINTSlist[i])
            POINTS = builder.EndVector()
        beamContourStart(builder)
        if self.CONTOUR_ID is not None:
            beamContourAddCONTOUR_ID(builder, CONTOUR_ID)
        beamContourAddGAIN_LEVEL(builder, self.GAIN_LEVEL)
        if self.POINTS is not None:
            beamContourAddPOINTS(builder, POINTS)
        beamContour = beamContourEnd(builder)
        return beamContour
