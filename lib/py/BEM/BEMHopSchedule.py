# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Periodic beam-hopping schedule carried by a deployed beam descriptor.
class BEMHopSchedule(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BEMHopSchedule()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBEMHopSchedule(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BEMHopScheduleBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x45\x4D", size_prefixed=size_prefixed)

    # BEMHopSchedule
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # BEMHopSchedule
    def SCHEDULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Seconds since 1970-01-01T00:00:00 UTC at which slot offsets begin.
    # BEMHopSchedule
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # BEMHopSchedule
    def PERIOD_S(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # BEMHopSchedule
    def REPEATS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # BEMHopSchedule
    def SLOTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from BEMHopSlot import BEMHopSlot
            obj = BEMHopSlot()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # BEMHopSchedule
    def SLOTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BEMHopSchedule
    def SLOTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # BEMHopSchedule
    def PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from BEMProvenance import BEMProvenance
            obj = BEMProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def BEMHopScheduleStart(builder):
    builder.StartObject(6)

def Start(builder):
    BEMHopScheduleStart(builder)

def BEMHopScheduleAddSCHEDULE_ID(builder, SCHEDULE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEDULE_ID), 0)

def AddSCHEDULE_ID(builder, SCHEDULE_ID):
    BEMHopScheduleAddSCHEDULE_ID(builder, SCHEDULE_ID)

def BEMHopScheduleAddEPOCH(builder, EPOCH):
    builder.PrependFloat64Slot(1, EPOCH, 0.0)

def AddEPOCH(builder, EPOCH):
    BEMHopScheduleAddEPOCH(builder, EPOCH)

def BEMHopScheduleAddPERIOD_S(builder, PERIOD_S):
    builder.PrependFloat64Slot(2, PERIOD_S, 0.0)

def AddPERIOD_S(builder, PERIOD_S):
    BEMHopScheduleAddPERIOD_S(builder, PERIOD_S)

def BEMHopScheduleAddREPEATS(builder, REPEATS):
    builder.PrependBoolSlot(3, REPEATS, 0)

def AddREPEATS(builder, REPEATS):
    BEMHopScheduleAddREPEATS(builder, REPEATS)

def BEMHopScheduleAddSLOTS(builder, SLOTS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SLOTS), 0)

def AddSLOTS(builder, SLOTS):
    BEMHopScheduleAddSLOTS(builder, SLOTS)

def BEMHopScheduleStartSLOTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSLOTSVector(builder, numElems):
    return BEMHopScheduleStartSLOTSVector(builder, numElems)

def BEMHopScheduleCreateSLOTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSLOTSVector(builder, data):
    BEMHopScheduleCreateSLOTSVector(builder, data)

def BEMHopScheduleAddPROVENANCE(builder, PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PROVENANCE), 0)

def AddPROVENANCE(builder, PROVENANCE):
    BEMHopScheduleAddPROVENANCE(builder, PROVENANCE)

def BEMHopScheduleEnd(builder):
    return builder.EndObject()

def End(builder):
    return BEMHopScheduleEnd(builder)

import BEMHopSlot
import BEMProvenance
try:
    from typing import List, Optional
except:
    pass

class BEMHopScheduleT(object):

    # BEMHopScheduleT
    def __init__(
        self,
        SCHEDULE_ID = None,
        EPOCH = 0.0,
        PERIOD_S = 0.0,
        REPEATS = False,
        SLOTS = None,
        PROVENANCE = None,
    ):
        self.SCHEDULE_ID = SCHEDULE_ID  # type: Optional[str]
        self.EPOCH = EPOCH  # type: float
        self.PERIOD_S = PERIOD_S  # type: float
        self.REPEATS = REPEATS  # type: bool
        self.SLOTS = SLOTS  # type: Optional[List[BEMHopSlot.BEMHopSlotT]]
        self.PROVENANCE = PROVENANCE  # type: Optional[BEMProvenance.BEMProvenanceT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpBemhopSchedule = BEMHopSchedule()
        tmpBemhopSchedule.Init(buf, pos)
        return cls.InitFromObj(tmpBemhopSchedule)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpBemhopSchedule):
        x = BEMHopScheduleT()
        x._UnPack(tmpBemhopSchedule)
        return x

    # BEMHopScheduleT
    def _UnPack(self, BEMHopSchedule):
        if BEMHopSchedule is None:
            return
        self.SCHEDULE_ID = BEMHopSchedule.SCHEDULE_ID()
        self.EPOCH = BEMHopSchedule.EPOCH()
        self.PERIOD_S = BEMHopSchedule.PERIOD_S()
        self.REPEATS = BEMHopSchedule.REPEATS()
        if not BEMHopSchedule.SLOTSIsNone():
            self.SLOTS = []
            for i in range(BEMHopSchedule.SLOTSLength()):
                if BEMHopSchedule.SLOTS(i) is None:
                    self.SLOTS.append(None)
                else:
                    bEMHopSlot_ = BEMHopSlot.BEMHopSlotT.InitFromObj(BEMHopSchedule.SLOTS(i))
                    self.SLOTS.append(bEMHopSlot_)
        if BEMHopSchedule.PROVENANCE() is not None:
            self.PROVENANCE = BEMProvenance.BEMProvenanceT.InitFromObj(BEMHopSchedule.PROVENANCE())

    # BEMHopScheduleT
    def Pack(self, builder):
        if self.SCHEDULE_ID is not None:
            SCHEDULE_ID = builder.CreateString(self.SCHEDULE_ID)
        if self.SLOTS is not None:
            SLOTSlist = []
            for i in range(len(self.SLOTS)):
                SLOTSlist.append(self.SLOTS[i].Pack(builder))
            BEMHopScheduleStartSLOTSVector(builder, len(self.SLOTS))
            for i in reversed(range(len(self.SLOTS))):
                builder.PrependUOffsetTRelative(SLOTSlist[i])
            SLOTS = builder.EndVector()
        if self.PROVENANCE is not None:
            PROVENANCE = self.PROVENANCE.Pack(builder)
        BEMHopScheduleStart(builder)
        if self.SCHEDULE_ID is not None:
            BEMHopScheduleAddSCHEDULE_ID(builder, SCHEDULE_ID)
        BEMHopScheduleAddEPOCH(builder, self.EPOCH)
        BEMHopScheduleAddPERIOD_S(builder, self.PERIOD_S)
        BEMHopScheduleAddREPEATS(builder, self.REPEATS)
        if self.SLOTS is not None:
            BEMHopScheduleAddSLOTS(builder, SLOTS)
        if self.PROVENANCE is not None:
            BEMHopScheduleAddPROVENANCE(builder, PROVENANCE)
        BEMHopSchedule = BEMHopScheduleEnd(builder)
        return BEMHopSchedule
