# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Catalog Embedding Shard.
#
# One CES publishes the dense semantic-search vector table for one dataset
# update: one row per catalog object, alongside that update's $OMM shard and
# $DPM manifest (DPMAsset.ASSET_KIND = EMBEDDING_SHARD). Vectors are
# L2-normalised at the encoder's output; similarity between two rows, or
# between a row and a query vector produced by a paired $QEM, is dot
# product (equivalent to cosine similarity for unit-norm vectors).
#
# A consumer MUST compare MODEL_ID, MODEL_VERSION, MODEL_WEIGHTS_SHA256,
# POOLING_METHOD and TEMPLATE_VERSION against a QEM record before trusting
# cross-encoding similarity between a query vector and CES rows; a mismatch
# on any one field means the two vector spaces are not comparable.
class CES(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CES()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCES(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CESBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x45\x53", size_prefixed=size_prefixed)

    # CES
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Joins to DPM.DATASET_ID for the publication this shard belongs to.
    # CES
    def DATASET_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Joins to DPM.UPDATE_ID for the specific update this shard was computed
    # against.
    # CES
    def UPDATE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Fixed embedding dimensionality shared by every row (e.g. 384).
    # CES
    def DIMENSIONS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Row element encoding used across ROWS.
    # CES
    def ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Row-vector normalization applied before quantization.
    # CES
    def NORMALIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 1

    # Similarity function rows are intended to be compared with.
    # CES
    def SIMILARITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Encoder model identifier/name.
    # CES
    def MODEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Encoder model version.
    # CES
    def MODEL_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SHA-256 of the encoder weights, lowercase hex. Same convention as
    # DPMAsset.BYTE_SHA256.
    # CES
    def MODEL_WEIGHTS_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Sentence/document pooling method the encoder used.
    # CES
    def POOLING_METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Version of the text template documents were rendered from before
    # encoding. Must match a QEM's TEMPLATE_VERSION for compatible retrieval.
    # CES
    def TEMPLATE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # One row per catalog object.
    # CES
    def ROWS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from CESObjectVector import CESObjectVector
            obj = CESObjectVector()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CES
    def ROWSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CES
    def ROWSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

def CESStart(builder):
    builder.StartObject(12)

def Start(builder):
    CESStart(builder)

def CESAddDATASET_ID(builder, DATASET_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(DATASET_ID), 0)

def AddDATASET_ID(builder, DATASET_ID):
    CESAddDATASET_ID(builder, DATASET_ID)

def CESAddUPDATE_ID(builder, UPDATE_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(UPDATE_ID), 0)

def AddUPDATE_ID(builder, UPDATE_ID):
    CESAddUPDATE_ID(builder, UPDATE_ID)

def CESAddDIMENSIONS(builder, DIMENSIONS):
    builder.PrependUint32Slot(2, DIMENSIONS, 0)

def AddDIMENSIONS(builder, DIMENSIONS):
    CESAddDIMENSIONS(builder, DIMENSIONS)

def CESAddENCODING(builder, ENCODING):
    builder.PrependInt8Slot(3, ENCODING, 0)

def AddENCODING(builder, ENCODING):
    CESAddENCODING(builder, ENCODING)

def CESAddNORMALIZATION(builder, NORMALIZATION):
    builder.PrependInt8Slot(4, NORMALIZATION, 1)

def AddNORMALIZATION(builder, NORMALIZATION):
    CESAddNORMALIZATION(builder, NORMALIZATION)

def CESAddSIMILARITY(builder, SIMILARITY):
    builder.PrependInt8Slot(5, SIMILARITY, 0)

def AddSIMILARITY(builder, SIMILARITY):
    CESAddSIMILARITY(builder, SIMILARITY)

def CESAddMODEL_ID(builder, MODEL_ID):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL_ID), 0)

def AddMODEL_ID(builder, MODEL_ID):
    CESAddMODEL_ID(builder, MODEL_ID)

def CESAddMODEL_VERSION(builder, MODEL_VERSION):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL_VERSION), 0)

def AddMODEL_VERSION(builder, MODEL_VERSION):
    CESAddMODEL_VERSION(builder, MODEL_VERSION)

def CESAddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL_WEIGHTS_SHA256), 0)

def AddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256):
    CESAddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256)

def CESAddPOOLING_METHOD(builder, POOLING_METHOD):
    builder.PrependInt8Slot(9, POOLING_METHOD, 0)

def AddPOOLING_METHOD(builder, POOLING_METHOD):
    CESAddPOOLING_METHOD(builder, POOLING_METHOD)

def CESAddTEMPLATE_VERSION(builder, TEMPLATE_VERSION):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(TEMPLATE_VERSION), 0)

def AddTEMPLATE_VERSION(builder, TEMPLATE_VERSION):
    CESAddTEMPLATE_VERSION(builder, TEMPLATE_VERSION)

def CESAddROWS(builder, ROWS):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(ROWS), 0)

def AddROWS(builder, ROWS):
    CESAddROWS(builder, ROWS)

def CESStartROWSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartROWSVector(builder, numElems):
    return CESStartROWSVector(builder, numElems)

def CESCreateROWSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateROWSVector(builder, data):
    CESCreateROWSVector(builder, data)

def CESEnd(builder):
    return builder.EndObject()

def End(builder):
    return CESEnd(builder)

import CESObjectVector
try:
    from typing import List
except:
    pass

class CEST(object):

    # CEST
    def __init__(
        self,
        DATASET_ID = None,
        UPDATE_ID = None,
        DIMENSIONS = 0,
        ENCODING = 0,
        NORMALIZATION = 1,
        SIMILARITY = 0,
        MODEL_ID = None,
        MODEL_VERSION = None,
        MODEL_WEIGHTS_SHA256 = None,
        POOLING_METHOD = 0,
        TEMPLATE_VERSION = None,
        ROWS = None,
    ):
        self.DATASET_ID = DATASET_ID  # type: Optional[str]
        self.UPDATE_ID = UPDATE_ID  # type: Optional[str]
        self.DIMENSIONS = DIMENSIONS  # type: int
        self.ENCODING = ENCODING  # type: int
        self.NORMALIZATION = NORMALIZATION  # type: int
        self.SIMILARITY = SIMILARITY  # type: int
        self.MODEL_ID = MODEL_ID  # type: Optional[str]
        self.MODEL_VERSION = MODEL_VERSION  # type: Optional[str]
        self.MODEL_WEIGHTS_SHA256 = MODEL_WEIGHTS_SHA256  # type: Optional[str]
        self.POOLING_METHOD = POOLING_METHOD  # type: int
        self.TEMPLATE_VERSION = TEMPLATE_VERSION  # type: Optional[str]
        self.ROWS = ROWS  # type: Optional[List[CESObjectVector.CESObjectVectorT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCes = CES()
        tmpCes.Init(buf, pos)
        return cls.InitFromObj(tmpCes)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCes):
        x = CEST()
        x._UnPack(tmpCes)
        return x

    # CEST
    def _UnPack(self, CES):
        if CES is None:
            return
        self.DATASET_ID = CES.DATASET_ID()
        self.UPDATE_ID = CES.UPDATE_ID()
        self.DIMENSIONS = CES.DIMENSIONS()
        self.ENCODING = CES.ENCODING()
        self.NORMALIZATION = CES.NORMALIZATION()
        self.SIMILARITY = CES.SIMILARITY()
        self.MODEL_ID = CES.MODEL_ID()
        self.MODEL_VERSION = CES.MODEL_VERSION()
        self.MODEL_WEIGHTS_SHA256 = CES.MODEL_WEIGHTS_SHA256()
        self.POOLING_METHOD = CES.POOLING_METHOD()
        self.TEMPLATE_VERSION = CES.TEMPLATE_VERSION()
        if not CES.ROWSIsNone():
            self.ROWS = []
            for i in range(CES.ROWSLength()):
                if CES.ROWS(i) is None:
                    self.ROWS.append(None)
                else:
                    cESObjectVector_ = CESObjectVector.CESObjectVectorT.InitFromObj(CES.ROWS(i))
                    self.ROWS.append(cESObjectVector_)

    # CEST
    def Pack(self, builder):
        if self.DATASET_ID is not None:
            DATASET_ID = builder.CreateString(self.DATASET_ID)
        if self.UPDATE_ID is not None:
            UPDATE_ID = builder.CreateString(self.UPDATE_ID)
        if self.MODEL_ID is not None:
            MODEL_ID = builder.CreateString(self.MODEL_ID)
        if self.MODEL_VERSION is not None:
            MODEL_VERSION = builder.CreateString(self.MODEL_VERSION)
        if self.MODEL_WEIGHTS_SHA256 is not None:
            MODEL_WEIGHTS_SHA256 = builder.CreateString(self.MODEL_WEIGHTS_SHA256)
        if self.TEMPLATE_VERSION is not None:
            TEMPLATE_VERSION = builder.CreateString(self.TEMPLATE_VERSION)
        if self.ROWS is not None:
            ROWSlist = []
            for i in range(len(self.ROWS)):
                ROWSlist.append(self.ROWS[i].Pack(builder))
            CESStartROWSVector(builder, len(self.ROWS))
            for i in reversed(range(len(self.ROWS))):
                builder.PrependUOffsetTRelative(ROWSlist[i])
            ROWS = builder.EndVector()
        CESStart(builder)
        if self.DATASET_ID is not None:
            CESAddDATASET_ID(builder, DATASET_ID)
        if self.UPDATE_ID is not None:
            CESAddUPDATE_ID(builder, UPDATE_ID)
        CESAddDIMENSIONS(builder, self.DIMENSIONS)
        CESAddENCODING(builder, self.ENCODING)
        CESAddNORMALIZATION(builder, self.NORMALIZATION)
        CESAddSIMILARITY(builder, self.SIMILARITY)
        if self.MODEL_ID is not None:
            CESAddMODEL_ID(builder, MODEL_ID)
        if self.MODEL_VERSION is not None:
            CESAddMODEL_VERSION(builder, MODEL_VERSION)
        if self.MODEL_WEIGHTS_SHA256 is not None:
            CESAddMODEL_WEIGHTS_SHA256(builder, MODEL_WEIGHTS_SHA256)
        CESAddPOOLING_METHOD(builder, self.POOLING_METHOD)
        if self.TEMPLATE_VERSION is not None:
            CESAddTEMPLATE_VERSION(builder, TEMPLATE_VERSION)
        if self.ROWS is not None:
            CESAddROWS(builder, ROWS)
        CES = CESEnd(builder)
        return CES
