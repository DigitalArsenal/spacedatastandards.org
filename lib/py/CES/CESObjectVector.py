# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One catalog object's dense embedding row.
class CESObjectVector(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CESObjectVector()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCESObjectVector(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CESObjectVectorBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x45\x53", size_prefixed=size_prefixed)

    # CESObjectVector
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Joins to CAT.NORAD_CAT_ID / OMM.NORAD_CAT_ID. 0 when unbound.
    # CESObjectVector
    def NORAD_CAT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # International designator. Joins to CAT.OBJECT_ID.
    # CESObjectVector
    def OBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Row values when the parent CES.ENCODING is INT8. Length equals
    # CES.DIMENSIONS.
    # CESObjectVector
    def VECTOR_INT8(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CESObjectVector
    def VECTOR_INT8AsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int8Flags, o)
        return 0

    # CESObjectVector
    def VECTOR_INT8Length(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CESObjectVector
    def VECTOR_INT8IsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Per-row dequantization scale: original value ~= VECTOR_INT8[i] * SCALE.
    # Unused when ENCODING is FLOAT32.
    # CESObjectVector
    def SCALE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Row values when the parent CES.ENCODING is FLOAT32. Length equals
    # CES.DIMENSIONS.
    # CESObjectVector
    def VECTOR_FLOAT32(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # CESObjectVector
    def VECTOR_FLOAT32AsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # CESObjectVector
    def VECTOR_FLOAT32Length(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CESObjectVector
    def VECTOR_FLOAT32IsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def CESObjectVectorStart(builder):
    builder.StartObject(5)

def Start(builder):
    CESObjectVectorStart(builder)

def CESObjectVectorAddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    builder.PrependUint32Slot(0, NORAD_CAT_ID, 0)

def AddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    CESObjectVectorAddNORAD_CAT_ID(builder, NORAD_CAT_ID)

def CESObjectVectorAddOBJECT_ID(builder, OBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT_ID), 0)

def AddOBJECT_ID(builder, OBJECT_ID):
    CESObjectVectorAddOBJECT_ID(builder, OBJECT_ID)

def CESObjectVectorAddVECTOR_INT8(builder, VECTOR_INT8):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VECTOR_INT8), 0)

def AddVECTOR_INT8(builder, VECTOR_INT8):
    CESObjectVectorAddVECTOR_INT8(builder, VECTOR_INT8)

def CESObjectVectorStartVECTOR_INT8Vector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartVECTOR_INT8Vector(builder, numElems):
    return CESObjectVectorStartVECTOR_INT8Vector(builder, numElems)

def CESObjectVectorCreateVECTOR_INT8Vector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependInt8(item)
    return builder.EndVector()

def CreateVECTOR_INT8Vector(builder, data):
    CESObjectVectorCreateVECTOR_INT8Vector(builder, data)

def CESObjectVectorAddSCALE(builder, SCALE):
    builder.PrependFloat32Slot(3, SCALE, 0.0)

def AddSCALE(builder, SCALE):
    CESObjectVectorAddSCALE(builder, SCALE)

def CESObjectVectorAddVECTOR_FLOAT32(builder, VECTOR_FLOAT32):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(VECTOR_FLOAT32), 0)

def AddVECTOR_FLOAT32(builder, VECTOR_FLOAT32):
    CESObjectVectorAddVECTOR_FLOAT32(builder, VECTOR_FLOAT32)

def CESObjectVectorStartVECTOR_FLOAT32Vector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartVECTOR_FLOAT32Vector(builder, numElems):
    return CESObjectVectorStartVECTOR_FLOAT32Vector(builder, numElems)

def CESObjectVectorCreateVECTOR_FLOAT32Vector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateVECTOR_FLOAT32Vector(builder, data):
    CESObjectVectorCreateVECTOR_FLOAT32Vector(builder, data)

def CESObjectVectorEnd(builder):
    return builder.EndObject()

def End(builder):
    return CESObjectVectorEnd(builder)

try:
    from typing import List
except:
    pass

class CESObjectVectorT(object):

    # CESObjectVectorT
    def __init__(
        self,
        NORAD_CAT_ID = 0,
        OBJECT_ID = None,
        VECTOR_INT8 = None,
        SCALE = 0.0,
        VECTOR_FLOAT32 = None,
    ):
        self.NORAD_CAT_ID = NORAD_CAT_ID  # type: int
        self.OBJECT_ID = OBJECT_ID  # type: Optional[str]
        self.VECTOR_INT8 = VECTOR_INT8  # type: Optional[List[int]]
        self.SCALE = SCALE  # type: float
        self.VECTOR_FLOAT32 = VECTOR_FLOAT32  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCesobjectVector = CESObjectVector()
        tmpCesobjectVector.Init(buf, pos)
        return cls.InitFromObj(tmpCesobjectVector)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCesobjectVector):
        x = CESObjectVectorT()
        x._UnPack(tmpCesobjectVector)
        return x

    # CESObjectVectorT
    def _UnPack(self, CESObjectVector):
        if CESObjectVector is None:
            return
        self.NORAD_CAT_ID = CESObjectVector.NORAD_CAT_ID()
        self.OBJECT_ID = CESObjectVector.OBJECT_ID()
        if not CESObjectVector.VECTOR_INT8IsNone():
            if np is None:
                self.VECTOR_INT8 = []
                for i in range(CESObjectVector.VECTOR_INT8Length()):
                    self.VECTOR_INT8.append(CESObjectVector.VECTOR_INT8(i))
            else:
                self.VECTOR_INT8 = CESObjectVector.VECTOR_INT8AsNumpy()
        self.SCALE = CESObjectVector.SCALE()
        if not CESObjectVector.VECTOR_FLOAT32IsNone():
            if np is None:
                self.VECTOR_FLOAT32 = []
                for i in range(CESObjectVector.VECTOR_FLOAT32Length()):
                    self.VECTOR_FLOAT32.append(CESObjectVector.VECTOR_FLOAT32(i))
            else:
                self.VECTOR_FLOAT32 = CESObjectVector.VECTOR_FLOAT32AsNumpy()

    # CESObjectVectorT
    def Pack(self, builder):
        if self.OBJECT_ID is not None:
            OBJECT_ID = builder.CreateString(self.OBJECT_ID)
        if self.VECTOR_INT8 is not None:
            if np is not None and type(self.VECTOR_INT8) is np.ndarray:
                VECTOR_INT8 = builder.CreateNumpyVector(self.VECTOR_INT8)
            else:
                CESObjectVectorStartVECTOR_INT8Vector(builder, len(self.VECTOR_INT8))
                for i in reversed(range(len(self.VECTOR_INT8))):
                    builder.PrependByte(self.VECTOR_INT8[i])
                VECTOR_INT8 = builder.EndVector()
        if self.VECTOR_FLOAT32 is not None:
            if np is not None and type(self.VECTOR_FLOAT32) is np.ndarray:
                VECTOR_FLOAT32 = builder.CreateNumpyVector(self.VECTOR_FLOAT32)
            else:
                CESObjectVectorStartVECTOR_FLOAT32Vector(builder, len(self.VECTOR_FLOAT32))
                for i in reversed(range(len(self.VECTOR_FLOAT32))):
                    builder.PrependFloat32(self.VECTOR_FLOAT32[i])
                VECTOR_FLOAT32 = builder.EndVector()
        CESObjectVectorStart(builder)
        CESObjectVectorAddNORAD_CAT_ID(builder, self.NORAD_CAT_ID)
        if self.OBJECT_ID is not None:
            CESObjectVectorAddOBJECT_ID(builder, OBJECT_ID)
        if self.VECTOR_INT8 is not None:
            CESObjectVectorAddVECTOR_INT8(builder, VECTOR_INT8)
        CESObjectVectorAddSCALE(builder, self.SCALE)
        if self.VECTOR_FLOAT32 is not None:
            CESObjectVectorAddVECTOR_FLOAT32(builder, VECTOR_FLOAT32)
        CESObjectVector = CESObjectVectorEnd(builder)
        return CESObjectVector
