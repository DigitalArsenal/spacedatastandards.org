# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class FSP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FSP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFSP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FSPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x46\x53\x50", size_prefixed=size_prefixed)

    # FSP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FSP
    def POLICY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def POLICY_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 1

    # FSP
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def LISTING_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def STREAM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def SCHEMA_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def SCHEMA_HASH(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSP
    def SCHEMA_HASHAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSP
    def SCHEMA_HASHLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSP
    def SCHEMA_HASHIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # FSP
    def AUDIENCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FieldStreamAudience import FieldStreamAudience
            obj = FieldStreamAudience()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FSP
    def AUDIENCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSP
    def AUDIENCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # FSP
    def RULES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FieldStreamRule import FieldStreamRule
            obj = FieldStreamRule()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FSP
    def RULESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSP
    def RULESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # FSP
    def ALLOWED_OPERATIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSP
    def ALLOWED_OPERATIONSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int8Flags, o)
        return 0

    # FSP
    def ALLOWED_OPERATIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSP
    def ALLOWED_OPERATIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # FSP
    def KEY_SCOPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def KEY_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def VALID_FROM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSP
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSP
    def REVOCATION_STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # FSP
    def REVOKED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSP
    def REVOCATION_REASON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSP
    def PROVIDER_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSP
    def PROVIDER_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSP
    def PROVIDER_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSP
    def PROVIDER_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        return o == 0

def FSPStart(builder):
    builder.StartObject(18)

def Start(builder):
    FSPStart(builder)

def FSPAddPOLICY_ID(builder, POLICY_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(POLICY_ID), 0)

def AddPOLICY_ID(builder, POLICY_ID):
    FSPAddPOLICY_ID(builder, POLICY_ID)

def FSPAddPOLICY_VERSION(builder, POLICY_VERSION):
    builder.PrependUint32Slot(1, POLICY_VERSION, 1)

def AddPOLICY_VERSION(builder, POLICY_VERSION):
    FSPAddPOLICY_VERSION(builder, POLICY_VERSION)

def FSPAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    FSPAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def FSPAddLISTING_ID(builder, LISTING_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(LISTING_ID), 0)

def AddLISTING_ID(builder, LISTING_ID):
    FSPAddLISTING_ID(builder, LISTING_ID)

def FSPAddSTREAM_ID(builder, STREAM_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(STREAM_ID), 0)

def AddSTREAM_ID(builder, STREAM_ID):
    FSPAddSTREAM_ID(builder, STREAM_ID)

def FSPAddSCHEMA_CODE(builder, SCHEMA_CODE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_CODE), 0)

def AddSCHEMA_CODE(builder, SCHEMA_CODE):
    FSPAddSCHEMA_CODE(builder, SCHEMA_CODE)

def FSPAddSCHEMA_HASH(builder, SCHEMA_HASH):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_HASH), 0)

def AddSCHEMA_HASH(builder, SCHEMA_HASH):
    FSPAddSCHEMA_HASH(builder, SCHEMA_HASH)

def FSPStartSCHEMA_HASHVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSCHEMA_HASHVector(builder, numElems):
    return FSPStartSCHEMA_HASHVector(builder, numElems)

def FSPCreateSCHEMA_HASHVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSCHEMA_HASHVector(builder, data):
    FSPCreateSCHEMA_HASHVector(builder, data)

def FSPAddAUDIENCES(builder, AUDIENCES):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(AUDIENCES), 0)

def AddAUDIENCES(builder, AUDIENCES):
    FSPAddAUDIENCES(builder, AUDIENCES)

def FSPStartAUDIENCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartAUDIENCESVector(builder, numElems):
    return FSPStartAUDIENCESVector(builder, numElems)

def FSPCreateAUDIENCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateAUDIENCESVector(builder, data):
    FSPCreateAUDIENCESVector(builder, data)

def FSPAddRULES(builder, RULES):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(RULES), 0)

def AddRULES(builder, RULES):
    FSPAddRULES(builder, RULES)

def FSPStartRULESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRULESVector(builder, numElems):
    return FSPStartRULESVector(builder, numElems)

def FSPCreateRULESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateRULESVector(builder, data):
    FSPCreateRULESVector(builder, data)

def FSPAddALLOWED_OPERATIONS(builder, ALLOWED_OPERATIONS):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(ALLOWED_OPERATIONS), 0)

def AddALLOWED_OPERATIONS(builder, ALLOWED_OPERATIONS):
    FSPAddALLOWED_OPERATIONS(builder, ALLOWED_OPERATIONS)

def FSPStartALLOWED_OPERATIONSVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartALLOWED_OPERATIONSVector(builder, numElems):
    return FSPStartALLOWED_OPERATIONSVector(builder, numElems)

def FSPCreateALLOWED_OPERATIONSVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependInt8(item)
    return builder.EndVector()

def CreateALLOWED_OPERATIONSVector(builder, data):
    FSPCreateALLOWED_OPERATIONSVector(builder, data)

def FSPAddKEY_SCOPE(builder, KEY_SCOPE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_SCOPE), 0)

def AddKEY_SCOPE(builder, KEY_SCOPE):
    FSPAddKEY_SCOPE(builder, KEY_SCOPE)

def FSPAddKEY_EPOCH(builder, KEY_EPOCH):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_EPOCH), 0)

def AddKEY_EPOCH(builder, KEY_EPOCH):
    FSPAddKEY_EPOCH(builder, KEY_EPOCH)

def FSPAddVALID_FROM(builder, VALID_FROM):
    builder.PrependUint64Slot(12, VALID_FROM, 0)

def AddVALID_FROM(builder, VALID_FROM):
    FSPAddVALID_FROM(builder, VALID_FROM)

def FSPAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(13, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    FSPAddEXPIRES_AT(builder, EXPIRES_AT)

def FSPAddREVOCATION_STATUS(builder, REVOCATION_STATUS):
    builder.PrependInt8Slot(14, REVOCATION_STATUS, 0)

def AddREVOCATION_STATUS(builder, REVOCATION_STATUS):
    FSPAddREVOCATION_STATUS(builder, REVOCATION_STATUS)

def FSPAddREVOKED_AT(builder, REVOKED_AT):
    builder.PrependUint64Slot(15, REVOKED_AT, 0)

def AddREVOKED_AT(builder, REVOKED_AT):
    FSPAddREVOKED_AT(builder, REVOKED_AT)

def FSPAddREVOCATION_REASON(builder, REVOCATION_REASON):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(REVOCATION_REASON), 0)

def AddREVOCATION_REASON(builder, REVOCATION_REASON):
    FSPAddREVOCATION_REASON(builder, REVOCATION_REASON)

def FSPAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_SIGNATURE), 0)

def AddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    FSPAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)

def FSPStartPROVIDER_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPROVIDER_SIGNATUREVector(builder, numElems):
    return FSPStartPROVIDER_SIGNATUREVector(builder, numElems)

def FSPCreatePROVIDER_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePROVIDER_SIGNATUREVector(builder, data):
    FSPCreatePROVIDER_SIGNATUREVector(builder, data)

def FSPEnd(builder):
    return builder.EndObject()

def End(builder):
    return FSPEnd(builder)

import FieldStreamAudience
import FieldStreamRule
try:
    from typing import List
except:
    pass

class FSPT(object):

    # FSPT
    def __init__(
        self,
        POLICY_ID = None,
        POLICY_VERSION = 1,
        PROVIDER_PEER_ID = None,
        LISTING_ID = None,
        STREAM_ID = None,
        SCHEMA_CODE = None,
        SCHEMA_HASH = None,
        AUDIENCES = None,
        RULES = None,
        ALLOWED_OPERATIONS = None,
        KEY_SCOPE = None,
        KEY_EPOCH = None,
        VALID_FROM = 0,
        EXPIRES_AT = 0,
        REVOCATION_STATUS = 0,
        REVOKED_AT = 0,
        REVOCATION_REASON = None,
        PROVIDER_SIGNATURE = None,
    ):
        self.POLICY_ID = POLICY_ID  # type: Optional[str]
        self.POLICY_VERSION = POLICY_VERSION  # type: int
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.LISTING_ID = LISTING_ID  # type: Optional[str]
        self.STREAM_ID = STREAM_ID  # type: Optional[str]
        self.SCHEMA_CODE = SCHEMA_CODE  # type: Optional[str]
        self.SCHEMA_HASH = SCHEMA_HASH  # type: Optional[List[int]]
        self.AUDIENCES = AUDIENCES  # type: Optional[List[FieldStreamAudience.FieldStreamAudienceT]]
        self.RULES = RULES  # type: Optional[List[FieldStreamRule.FieldStreamRuleT]]
        self.ALLOWED_OPERATIONS = ALLOWED_OPERATIONS  # type: Optional[List[int]]
        self.KEY_SCOPE = KEY_SCOPE  # type: Optional[str]
        self.KEY_EPOCH = KEY_EPOCH  # type: Optional[str]
        self.VALID_FROM = VALID_FROM  # type: int
        self.EXPIRES_AT = EXPIRES_AT  # type: int
        self.REVOCATION_STATUS = REVOCATION_STATUS  # type: int
        self.REVOKED_AT = REVOKED_AT  # type: int
        self.REVOCATION_REASON = REVOCATION_REASON  # type: Optional[str]
        self.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpFsp = FSP()
        tmpFsp.Init(buf, pos)
        return cls.InitFromObj(tmpFsp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpFsp):
        x = FSPT()
        x._UnPack(tmpFsp)
        return x

    # FSPT
    def _UnPack(self, FSP):
        if FSP is None:
            return
        self.POLICY_ID = FSP.POLICY_ID()
        self.POLICY_VERSION = FSP.POLICY_VERSION()
        self.PROVIDER_PEER_ID = FSP.PROVIDER_PEER_ID()
        self.LISTING_ID = FSP.LISTING_ID()
        self.STREAM_ID = FSP.STREAM_ID()
        self.SCHEMA_CODE = FSP.SCHEMA_CODE()
        if not FSP.SCHEMA_HASHIsNone():
            if np is None:
                self.SCHEMA_HASH = []
                for i in range(FSP.SCHEMA_HASHLength()):
                    self.SCHEMA_HASH.append(FSP.SCHEMA_HASH(i))
            else:
                self.SCHEMA_HASH = FSP.SCHEMA_HASHAsNumpy()
        if not FSP.AUDIENCESIsNone():
            self.AUDIENCES = []
            for i in range(FSP.AUDIENCESLength()):
                if FSP.AUDIENCES(i) is None:
                    self.AUDIENCES.append(None)
                else:
                    fieldStreamAudience_ = FieldStreamAudience.FieldStreamAudienceT.InitFromObj(FSP.AUDIENCES(i))
                    self.AUDIENCES.append(fieldStreamAudience_)
        if not FSP.RULESIsNone():
            self.RULES = []
            for i in range(FSP.RULESLength()):
                if FSP.RULES(i) is None:
                    self.RULES.append(None)
                else:
                    fieldStreamRule_ = FieldStreamRule.FieldStreamRuleT.InitFromObj(FSP.RULES(i))
                    self.RULES.append(fieldStreamRule_)
        if not FSP.ALLOWED_OPERATIONSIsNone():
            if np is None:
                self.ALLOWED_OPERATIONS = []
                for i in range(FSP.ALLOWED_OPERATIONSLength()):
                    self.ALLOWED_OPERATIONS.append(FSP.ALLOWED_OPERATIONS(i))
            else:
                self.ALLOWED_OPERATIONS = FSP.ALLOWED_OPERATIONSAsNumpy()
        self.KEY_SCOPE = FSP.KEY_SCOPE()
        self.KEY_EPOCH = FSP.KEY_EPOCH()
        self.VALID_FROM = FSP.VALID_FROM()
        self.EXPIRES_AT = FSP.EXPIRES_AT()
        self.REVOCATION_STATUS = FSP.REVOCATION_STATUS()
        self.REVOKED_AT = FSP.REVOKED_AT()
        self.REVOCATION_REASON = FSP.REVOCATION_REASON()
        if not FSP.PROVIDER_SIGNATUREIsNone():
            if np is None:
                self.PROVIDER_SIGNATURE = []
                for i in range(FSP.PROVIDER_SIGNATURELength()):
                    self.PROVIDER_SIGNATURE.append(FSP.PROVIDER_SIGNATURE(i))
            else:
                self.PROVIDER_SIGNATURE = FSP.PROVIDER_SIGNATUREAsNumpy()

    # FSPT
    def Pack(self, builder):
        if self.POLICY_ID is not None:
            POLICY_ID = builder.CreateString(self.POLICY_ID)
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.LISTING_ID is not None:
            LISTING_ID = builder.CreateString(self.LISTING_ID)
        if self.STREAM_ID is not None:
            STREAM_ID = builder.CreateString(self.STREAM_ID)
        if self.SCHEMA_CODE is not None:
            SCHEMA_CODE = builder.CreateString(self.SCHEMA_CODE)
        if self.SCHEMA_HASH is not None:
            if np is not None and type(self.SCHEMA_HASH) is np.ndarray:
                SCHEMA_HASH = builder.CreateNumpyVector(self.SCHEMA_HASH)
            else:
                FSPStartSCHEMA_HASHVector(builder, len(self.SCHEMA_HASH))
                for i in reversed(range(len(self.SCHEMA_HASH))):
                    builder.PrependUint8(self.SCHEMA_HASH[i])
                SCHEMA_HASH = builder.EndVector()
        if self.AUDIENCES is not None:
            AUDIENCESlist = []
            for i in range(len(self.AUDIENCES)):
                AUDIENCESlist.append(self.AUDIENCES[i].Pack(builder))
            FSPStartAUDIENCESVector(builder, len(self.AUDIENCES))
            for i in reversed(range(len(self.AUDIENCES))):
                builder.PrependUOffsetTRelative(AUDIENCESlist[i])
            AUDIENCES = builder.EndVector()
        if self.RULES is not None:
            RULESlist = []
            for i in range(len(self.RULES)):
                RULESlist.append(self.RULES[i].Pack(builder))
            FSPStartRULESVector(builder, len(self.RULES))
            for i in reversed(range(len(self.RULES))):
                builder.PrependUOffsetTRelative(RULESlist[i])
            RULES = builder.EndVector()
        if self.ALLOWED_OPERATIONS is not None:
            if np is not None and type(self.ALLOWED_OPERATIONS) is np.ndarray:
                ALLOWED_OPERATIONS = builder.CreateNumpyVector(self.ALLOWED_OPERATIONS)
            else:
                FSPStartALLOWED_OPERATIONSVector(builder, len(self.ALLOWED_OPERATIONS))
                for i in reversed(range(len(self.ALLOWED_OPERATIONS))):
                    builder.PrependByte(self.ALLOWED_OPERATIONS[i])
                ALLOWED_OPERATIONS = builder.EndVector()
        if self.KEY_SCOPE is not None:
            KEY_SCOPE = builder.CreateString(self.KEY_SCOPE)
        if self.KEY_EPOCH is not None:
            KEY_EPOCH = builder.CreateString(self.KEY_EPOCH)
        if self.REVOCATION_REASON is not None:
            REVOCATION_REASON = builder.CreateString(self.REVOCATION_REASON)
        if self.PROVIDER_SIGNATURE is not None:
            if np is not None and type(self.PROVIDER_SIGNATURE) is np.ndarray:
                PROVIDER_SIGNATURE = builder.CreateNumpyVector(self.PROVIDER_SIGNATURE)
            else:
                FSPStartPROVIDER_SIGNATUREVector(builder, len(self.PROVIDER_SIGNATURE))
                for i in reversed(range(len(self.PROVIDER_SIGNATURE))):
                    builder.PrependUint8(self.PROVIDER_SIGNATURE[i])
                PROVIDER_SIGNATURE = builder.EndVector()
        FSPStart(builder)
        if self.POLICY_ID is not None:
            FSPAddPOLICY_ID(builder, POLICY_ID)
        FSPAddPOLICY_VERSION(builder, self.POLICY_VERSION)
        if self.PROVIDER_PEER_ID is not None:
            FSPAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.LISTING_ID is not None:
            FSPAddLISTING_ID(builder, LISTING_ID)
        if self.STREAM_ID is not None:
            FSPAddSTREAM_ID(builder, STREAM_ID)
        if self.SCHEMA_CODE is not None:
            FSPAddSCHEMA_CODE(builder, SCHEMA_CODE)
        if self.SCHEMA_HASH is not None:
            FSPAddSCHEMA_HASH(builder, SCHEMA_HASH)
        if self.AUDIENCES is not None:
            FSPAddAUDIENCES(builder, AUDIENCES)
        if self.RULES is not None:
            FSPAddRULES(builder, RULES)
        if self.ALLOWED_OPERATIONS is not None:
            FSPAddALLOWED_OPERATIONS(builder, ALLOWED_OPERATIONS)
        if self.KEY_SCOPE is not None:
            FSPAddKEY_SCOPE(builder, KEY_SCOPE)
        if self.KEY_EPOCH is not None:
            FSPAddKEY_EPOCH(builder, KEY_EPOCH)
        FSPAddVALID_FROM(builder, self.VALID_FROM)
        FSPAddEXPIRES_AT(builder, self.EXPIRES_AT)
        FSPAddREVOCATION_STATUS(builder, self.REVOCATION_STATUS)
        FSPAddREVOKED_AT(builder, self.REVOKED_AT)
        if self.REVOCATION_REASON is not None:
            FSPAddREVOCATION_REASON(builder, REVOCATION_REASON)
        if self.PROVIDER_SIGNATURE is not None:
            FSPAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)
        FSP = FSPEnd(builder)
        return FSP
