# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One SigMF capture segment: the point in the sample stream at which the
# listed tuning took effect. A retuned or frequency-hopped recording has
# several; a single-tune recording has one.
class IQCSegment(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IQCSegment()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIQCSegment(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IQCSegmentBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x51\x43", size_prefixed=size_prefixed)

    # IQCSegment
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SigMF `core:sample_start` — index of the first sample of this segment.
    # IQCSegment
    def SAMPLE_START(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SigMF `core:global_index` — index of this sample in the receiver's
    # free-running sample counter.
    # IQCSegment
    def GLOBAL_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SigMF `core:header_bytes` — bytes preceding this segment in the data
    # file.
    # IQCSegment
    def HEADER_BYTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SigMF `core:frequency` — centre frequency of this segment, HERTZ.
    # IQCSegment
    def CENTER_FREQ_HZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SigMF `core:datetime` — ISO 8601 UTC timestamp of SAMPLE_START.
    # IQCSegment
    def DATETIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def IQCSegmentStart(builder):
    builder.StartObject(5)

def Start(builder):
    IQCSegmentStart(builder)

def IQCSegmentAddSAMPLE_START(builder, SAMPLE_START):
    builder.PrependUint64Slot(0, SAMPLE_START, 0)

def AddSAMPLE_START(builder, SAMPLE_START):
    IQCSegmentAddSAMPLE_START(builder, SAMPLE_START)

def IQCSegmentAddGLOBAL_INDEX(builder, GLOBAL_INDEX):
    builder.PrependUint64Slot(1, GLOBAL_INDEX, 0)

def AddGLOBAL_INDEX(builder, GLOBAL_INDEX):
    IQCSegmentAddGLOBAL_INDEX(builder, GLOBAL_INDEX)

def IQCSegmentAddHEADER_BYTES(builder, HEADER_BYTES):
    builder.PrependUint64Slot(2, HEADER_BYTES, 0)

def AddHEADER_BYTES(builder, HEADER_BYTES):
    IQCSegmentAddHEADER_BYTES(builder, HEADER_BYTES)

def IQCSegmentAddCENTER_FREQ_HZ(builder, CENTER_FREQ_HZ):
    builder.PrependFloat64Slot(3, CENTER_FREQ_HZ, 0.0)

def AddCENTER_FREQ_HZ(builder, CENTER_FREQ_HZ):
    IQCSegmentAddCENTER_FREQ_HZ(builder, CENTER_FREQ_HZ)

def IQCSegmentAddDATETIME(builder, DATETIME):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(DATETIME), 0)

def AddDATETIME(builder, DATETIME):
    IQCSegmentAddDATETIME(builder, DATETIME)

def IQCSegmentEnd(builder):
    return builder.EndObject()

def End(builder):
    return IQCSegmentEnd(builder)


class IQCSegmentT(object):

    # IQCSegmentT
    def __init__(
        self,
        SAMPLE_START = 0,
        GLOBAL_INDEX = 0,
        HEADER_BYTES = 0,
        CENTER_FREQ_HZ = 0.0,
        DATETIME = None,
    ):
        self.SAMPLE_START = SAMPLE_START  # type: int
        self.GLOBAL_INDEX = GLOBAL_INDEX  # type: int
        self.HEADER_BYTES = HEADER_BYTES  # type: int
        self.CENTER_FREQ_HZ = CENTER_FREQ_HZ  # type: float
        self.DATETIME = DATETIME  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIqcsegment = IQCSegment()
        tmpIqcsegment.Init(buf, pos)
        return cls.InitFromObj(tmpIqcsegment)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIqcsegment):
        x = IQCSegmentT()
        x._UnPack(tmpIqcsegment)
        return x

    # IQCSegmentT
    def _UnPack(self, IQCSegment):
        if IQCSegment is None:
            return
        self.SAMPLE_START = IQCSegment.SAMPLE_START()
        self.GLOBAL_INDEX = IQCSegment.GLOBAL_INDEX()
        self.HEADER_BYTES = IQCSegment.HEADER_BYTES()
        self.CENTER_FREQ_HZ = IQCSegment.CENTER_FREQ_HZ()
        self.DATETIME = IQCSegment.DATETIME()

    # IQCSegmentT
    def Pack(self, builder):
        if self.DATETIME is not None:
            DATETIME = builder.CreateString(self.DATETIME)
        IQCSegmentStart(builder)
        IQCSegmentAddSAMPLE_START(builder, self.SAMPLE_START)
        IQCSegmentAddGLOBAL_INDEX(builder, self.GLOBAL_INDEX)
        IQCSegmentAddHEADER_BYTES(builder, self.HEADER_BYTES)
        IQCSegmentAddCENTER_FREQ_HZ(builder, self.CENTER_FREQ_HZ)
        if self.DATETIME is not None:
            IQCSegmentAddDATETIME(builder, DATETIME)
        IQCSegment = IQCSegmentEnd(builder)
        return IQCSegment
