# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Receive chain that produced the samples.
#
# Every field is a verbatim string. SigMF `core:hw` is free text and the
# non-SigMF archives publish nothing more structured; splitting free text
# into a manufacturer and a model by guessing is a defect.
class IQCHardware(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IQCHardware()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIQCHardware(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IQCHardwareBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x51\x43", size_prefixed=size_prefixed)

    # IQCHardware
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SigMF `core:hw` verbatim, or the source's equivalent free-text
    # description of the receive chain.
    # IQCHardware
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Receiver manufacturer, only when the source names it as its own field.
    # IQCHardware
    def MANUFACTURER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Receiver model, only when the source names it as its own field.
    # IQCHardware
    def MODEL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Antenna description, only when the source names it as its own field.
    # IQCHardware
    def ANTENNA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SigMF `core:recorder` — the SOFTWARE that wrote the recording.
    # IQCHardware
    def RECORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def IQCHardwareStart(builder):
    builder.StartObject(5)

def Start(builder):
    IQCHardwareStart(builder)

def IQCHardwareAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    IQCHardwareAddDESCRIPTION(builder, DESCRIPTION)

def IQCHardwareAddMANUFACTURER(builder, MANUFACTURER):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(MANUFACTURER), 0)

def AddMANUFACTURER(builder, MANUFACTURER):
    IQCHardwareAddMANUFACTURER(builder, MANUFACTURER)

def IQCHardwareAddMODEL(builder, MODEL):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL), 0)

def AddMODEL(builder, MODEL):
    IQCHardwareAddMODEL(builder, MODEL)

def IQCHardwareAddANTENNA(builder, ANTENNA):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ANTENNA), 0)

def AddANTENNA(builder, ANTENNA):
    IQCHardwareAddANTENNA(builder, ANTENNA)

def IQCHardwareAddRECORDER(builder, RECORDER):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDER), 0)

def AddRECORDER(builder, RECORDER):
    IQCHardwareAddRECORDER(builder, RECORDER)

def IQCHardwareEnd(builder):
    return builder.EndObject()

def End(builder):
    return IQCHardwareEnd(builder)


class IQCHardwareT(object):

    # IQCHardwareT
    def __init__(
        self,
        DESCRIPTION = None,
        MANUFACTURER = None,
        MODEL = None,
        ANTENNA = None,
        RECORDER = None,
    ):
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.MANUFACTURER = MANUFACTURER  # type: Optional[str]
        self.MODEL = MODEL  # type: Optional[str]
        self.ANTENNA = ANTENNA  # type: Optional[str]
        self.RECORDER = RECORDER  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIqchardware = IQCHardware()
        tmpIqchardware.Init(buf, pos)
        return cls.InitFromObj(tmpIqchardware)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIqchardware):
        x = IQCHardwareT()
        x._UnPack(tmpIqchardware)
        return x

    # IQCHardwareT
    def _UnPack(self, IQCHardware):
        if IQCHardware is None:
            return
        self.DESCRIPTION = IQCHardware.DESCRIPTION()
        self.MANUFACTURER = IQCHardware.MANUFACTURER()
        self.MODEL = IQCHardware.MODEL()
        self.ANTENNA = IQCHardware.ANTENNA()
        self.RECORDER = IQCHardware.RECORDER()

    # IQCHardwareT
    def Pack(self, builder):
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.MANUFACTURER is not None:
            MANUFACTURER = builder.CreateString(self.MANUFACTURER)
        if self.MODEL is not None:
            MODEL = builder.CreateString(self.MODEL)
        if self.ANTENNA is not None:
            ANTENNA = builder.CreateString(self.ANTENNA)
        if self.RECORDER is not None:
            RECORDER = builder.CreateString(self.RECORDER)
        IQCHardwareStart(builder)
        if self.DESCRIPTION is not None:
            IQCHardwareAddDESCRIPTION(builder, DESCRIPTION)
        if self.MANUFACTURER is not None:
            IQCHardwareAddMANUFACTURER(builder, MANUFACTURER)
        if self.MODEL is not None:
            IQCHardwareAddMODEL(builder, MODEL)
        if self.ANTENNA is not None:
            IQCHardwareAddANTENNA(builder, ANTENNA)
        if self.RECORDER is not None:
            IQCHardwareAddRECORDER(builder, RECORDER)
        IQCHardware = IQCHardwareEnd(builder)
        return IQCHardware
