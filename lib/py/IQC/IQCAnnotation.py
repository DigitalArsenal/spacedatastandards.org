# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One labelled region of the capture in time and frequency.
class IQCAnnotation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IQCAnnotation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIQCAnnotation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IQCAnnotationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x51\x43", size_prefixed=size_prefixed)

    # IQCAnnotation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SigMF `core:sample_start` — first sample of the annotated region.
    # IQCAnnotation
    def SAMPLE_START(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SigMF `core:sample_count` — length of the annotated region in samples.
    # IQCAnnotation
    def SAMPLE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SigMF `core:freq_lower_edge` — lower edge of the annotated band, HERTZ.
    # IQCAnnotation
    def FREQ_LOWER_EDGE_HZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SigMF `core:freq_upper_edge` — upper edge of the annotated band, HERTZ.
    # IQCAnnotation
    def FREQ_UPPER_EDGE_HZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SigMF `core:label` — the short human label ("LoRa", "ADS-B", "AIS").
    # IQCAnnotation
    def LABEL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SigMF `core:comment` — free-text note.
    # IQCAnnotation
    def COMMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SigMF `core:generator` — the tool or person that produced the
    # annotation. An annotation asserted by an automatic classifier and one
    # asserted by a human are not interchangeable evidence; this field is how
    # a consumer tells them apart.
    # IQCAnnotation
    def GENERATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SigMF `core:uuid` — the annotation's own identifier, when present.
    # IQCAnnotation
    def UUID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def IQCAnnotationStart(builder):
    builder.StartObject(8)

def Start(builder):
    IQCAnnotationStart(builder)

def IQCAnnotationAddSAMPLE_START(builder, SAMPLE_START):
    builder.PrependUint64Slot(0, SAMPLE_START, 0)

def AddSAMPLE_START(builder, SAMPLE_START):
    IQCAnnotationAddSAMPLE_START(builder, SAMPLE_START)

def IQCAnnotationAddSAMPLE_COUNT(builder, SAMPLE_COUNT):
    builder.PrependUint64Slot(1, SAMPLE_COUNT, 0)

def AddSAMPLE_COUNT(builder, SAMPLE_COUNT):
    IQCAnnotationAddSAMPLE_COUNT(builder, SAMPLE_COUNT)

def IQCAnnotationAddFREQ_LOWER_EDGE_HZ(builder, FREQ_LOWER_EDGE_HZ):
    builder.PrependFloat64Slot(2, FREQ_LOWER_EDGE_HZ, 0.0)

def AddFREQ_LOWER_EDGE_HZ(builder, FREQ_LOWER_EDGE_HZ):
    IQCAnnotationAddFREQ_LOWER_EDGE_HZ(builder, FREQ_LOWER_EDGE_HZ)

def IQCAnnotationAddFREQ_UPPER_EDGE_HZ(builder, FREQ_UPPER_EDGE_HZ):
    builder.PrependFloat64Slot(3, FREQ_UPPER_EDGE_HZ, 0.0)

def AddFREQ_UPPER_EDGE_HZ(builder, FREQ_UPPER_EDGE_HZ):
    IQCAnnotationAddFREQ_UPPER_EDGE_HZ(builder, FREQ_UPPER_EDGE_HZ)

def IQCAnnotationAddLABEL(builder, LABEL):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(LABEL), 0)

def AddLABEL(builder, LABEL):
    IQCAnnotationAddLABEL(builder, LABEL)

def IQCAnnotationAddCOMMENT(builder, COMMENT):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(COMMENT), 0)

def AddCOMMENT(builder, COMMENT):
    IQCAnnotationAddCOMMENT(builder, COMMENT)

def IQCAnnotationAddGENERATOR(builder, GENERATOR):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(GENERATOR), 0)

def AddGENERATOR(builder, GENERATOR):
    IQCAnnotationAddGENERATOR(builder, GENERATOR)

def IQCAnnotationAddUUID(builder, UUID):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(UUID), 0)

def AddUUID(builder, UUID):
    IQCAnnotationAddUUID(builder, UUID)

def IQCAnnotationEnd(builder):
    return builder.EndObject()

def End(builder):
    return IQCAnnotationEnd(builder)


class IQCAnnotationT(object):

    # IQCAnnotationT
    def __init__(
        self,
        SAMPLE_START = 0,
        SAMPLE_COUNT = 0,
        FREQ_LOWER_EDGE_HZ = 0.0,
        FREQ_UPPER_EDGE_HZ = 0.0,
        LABEL = None,
        COMMENT = None,
        GENERATOR = None,
        UUID = None,
    ):
        self.SAMPLE_START = SAMPLE_START  # type: int
        self.SAMPLE_COUNT = SAMPLE_COUNT  # type: int
        self.FREQ_LOWER_EDGE_HZ = FREQ_LOWER_EDGE_HZ  # type: float
        self.FREQ_UPPER_EDGE_HZ = FREQ_UPPER_EDGE_HZ  # type: float
        self.LABEL = LABEL  # type: Optional[str]
        self.COMMENT = COMMENT  # type: Optional[str]
        self.GENERATOR = GENERATOR  # type: Optional[str]
        self.UUID = UUID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIqcannotation = IQCAnnotation()
        tmpIqcannotation.Init(buf, pos)
        return cls.InitFromObj(tmpIqcannotation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIqcannotation):
        x = IQCAnnotationT()
        x._UnPack(tmpIqcannotation)
        return x

    # IQCAnnotationT
    def _UnPack(self, IQCAnnotation):
        if IQCAnnotation is None:
            return
        self.SAMPLE_START = IQCAnnotation.SAMPLE_START()
        self.SAMPLE_COUNT = IQCAnnotation.SAMPLE_COUNT()
        self.FREQ_LOWER_EDGE_HZ = IQCAnnotation.FREQ_LOWER_EDGE_HZ()
        self.FREQ_UPPER_EDGE_HZ = IQCAnnotation.FREQ_UPPER_EDGE_HZ()
        self.LABEL = IQCAnnotation.LABEL()
        self.COMMENT = IQCAnnotation.COMMENT()
        self.GENERATOR = IQCAnnotation.GENERATOR()
        self.UUID = IQCAnnotation.UUID()

    # IQCAnnotationT
    def Pack(self, builder):
        if self.LABEL is not None:
            LABEL = builder.CreateString(self.LABEL)
        if self.COMMENT is not None:
            COMMENT = builder.CreateString(self.COMMENT)
        if self.GENERATOR is not None:
            GENERATOR = builder.CreateString(self.GENERATOR)
        if self.UUID is not None:
            UUID = builder.CreateString(self.UUID)
        IQCAnnotationStart(builder)
        IQCAnnotationAddSAMPLE_START(builder, self.SAMPLE_START)
        IQCAnnotationAddSAMPLE_COUNT(builder, self.SAMPLE_COUNT)
        IQCAnnotationAddFREQ_LOWER_EDGE_HZ(builder, self.FREQ_LOWER_EDGE_HZ)
        IQCAnnotationAddFREQ_UPPER_EDGE_HZ(builder, self.FREQ_UPPER_EDGE_HZ)
        if self.LABEL is not None:
            IQCAnnotationAddLABEL(builder, LABEL)
        if self.COMMENT is not None:
            IQCAnnotationAddCOMMENT(builder, COMMENT)
        if self.GENERATOR is not None:
            IQCAnnotationAddGENERATOR(builder, GENERATOR)
        if self.UUID is not None:
            IQCAnnotationAddUUID(builder, UUID)
        IQCAnnotation = IQCAnnotationEnd(builder)
        return IQCAnnotation
