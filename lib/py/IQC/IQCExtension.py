# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A SigMF extension namespace the upstream metadata declares.
class IQCExtension(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IQCExtension()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIQCExtension(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IQCExtensionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x51\x43", size_prefixed=size_prefixed)

    # IQCExtension
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Extension namespace name.
    # IQCExtension
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Extension version.
    # IQCExtension
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # True when the extension is declared optional for interpretation.
    # IQCExtension
    def IS_OPTIONAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def IQCExtensionStart(builder):
    builder.StartObject(3)

def Start(builder):
    IQCExtensionStart(builder)

def IQCExtensionAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    IQCExtensionAddNAME(builder, NAME)

def IQCExtensionAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    IQCExtensionAddVERSION(builder, VERSION)

def IQCExtensionAddIS_OPTIONAL(builder, IS_OPTIONAL):
    builder.PrependBoolSlot(2, IS_OPTIONAL, 0)

def AddIS_OPTIONAL(builder, IS_OPTIONAL):
    IQCExtensionAddIS_OPTIONAL(builder, IS_OPTIONAL)

def IQCExtensionEnd(builder):
    return builder.EndObject()

def End(builder):
    return IQCExtensionEnd(builder)


class IQCExtensionT(object):

    # IQCExtensionT
    def __init__(
        self,
        NAME = None,
        VERSION = None,
        IS_OPTIONAL = False,
    ):
        self.NAME = NAME  # type: Optional[str]
        self.VERSION = VERSION  # type: Optional[str]
        self.IS_OPTIONAL = IS_OPTIONAL  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIqcextension = IQCExtension()
        tmpIqcextension.Init(buf, pos)
        return cls.InitFromObj(tmpIqcextension)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIqcextension):
        x = IQCExtensionT()
        x._UnPack(tmpIqcextension)
        return x

    # IQCExtensionT
    def _UnPack(self, IQCExtension):
        if IQCExtension is None:
            return
        self.NAME = IQCExtension.NAME()
        self.VERSION = IQCExtension.VERSION()
        self.IS_OPTIONAL = IQCExtension.IS_OPTIONAL()

    # IQCExtensionT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        IQCExtensionStart(builder)
        if self.NAME is not None:
            IQCExtensionAddNAME(builder, NAME)
        if self.VERSION is not None:
            IQCExtensionAddVERSION(builder, VERSION)
        IQCExtensionAddIS_OPTIONAL(builder, self.IS_OPTIONAL)
        IQCExtension = IQCExtensionEnd(builder)
        return IQCExtension
