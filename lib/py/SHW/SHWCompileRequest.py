# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Compile request — one shader source (typically a fragment of a
# larger stage) plus metadata for the host splice.
class SHWCompileRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SHWCompileRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSHWCompileRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SHWCompileRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x48\x57", size_prefixed=size_prefixed)

    # SHWCompileRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Logical shader name (used in logs and uniform prefixing).
    # SHWCompileRequest
    def SHADER_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Target GLSL stage.
    # SHWCompileRequest
    def SHADER_STAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Intended injection point in the host pipeline.
    # SHWCompileRequest
    def SHADER_INJECTION_POINT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # GLSL source (or preprocessed chunk).
    # SHWCompileRequest
    def SHADER_SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # GLSL version string (for example `300 es`).
    # SHWCompileRequest
    def GLSL_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Expected uniforms declared by the source.
    # SHWCompileRequest
    def SHADER_UNIFORMS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SHWUniform import SHWUniform
            obj = SHWUniform()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SHWCompileRequest
    def SHADER_UNIFORMSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHWCompileRequest
    def SHADER_UNIFORMSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

def SHWCompileRequestStart(builder):
    builder.StartObject(6)

def Start(builder):
    SHWCompileRequestStart(builder)

def SHWCompileRequestAddSHADER_NAME(builder, SHADER_NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SHADER_NAME), 0)

def AddSHADER_NAME(builder, SHADER_NAME):
    SHWCompileRequestAddSHADER_NAME(builder, SHADER_NAME)

def SHWCompileRequestAddSHADER_STAGE(builder, SHADER_STAGE):
    builder.PrependUint8Slot(1, SHADER_STAGE, 0)

def AddSHADER_STAGE(builder, SHADER_STAGE):
    SHWCompileRequestAddSHADER_STAGE(builder, SHADER_STAGE)

def SHWCompileRequestAddSHADER_INJECTION_POINT(builder, SHADER_INJECTION_POINT):
    builder.PrependUint8Slot(2, SHADER_INJECTION_POINT, 0)

def AddSHADER_INJECTION_POINT(builder, SHADER_INJECTION_POINT):
    SHWCompileRequestAddSHADER_INJECTION_POINT(builder, SHADER_INJECTION_POINT)

def SHWCompileRequestAddSHADER_SOURCE(builder, SHADER_SOURCE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SHADER_SOURCE), 0)

def AddSHADER_SOURCE(builder, SHADER_SOURCE):
    SHWCompileRequestAddSHADER_SOURCE(builder, SHADER_SOURCE)

def SHWCompileRequestAddGLSL_VERSION(builder, GLSL_VERSION):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(GLSL_VERSION), 0)

def AddGLSL_VERSION(builder, GLSL_VERSION):
    SHWCompileRequestAddGLSL_VERSION(builder, GLSL_VERSION)

def SHWCompileRequestAddSHADER_UNIFORMS(builder, SHADER_UNIFORMS):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SHADER_UNIFORMS), 0)

def AddSHADER_UNIFORMS(builder, SHADER_UNIFORMS):
    SHWCompileRequestAddSHADER_UNIFORMS(builder, SHADER_UNIFORMS)

def SHWCompileRequestStartSHADER_UNIFORMSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSHADER_UNIFORMSVector(builder, numElems):
    return SHWCompileRequestStartSHADER_UNIFORMSVector(builder, numElems)

def SHWCompileRequestCreateSHADER_UNIFORMSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSHADER_UNIFORMSVector(builder, data):
    SHWCompileRequestCreateSHADER_UNIFORMSVector(builder, data)

def SHWCompileRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return SHWCompileRequestEnd(builder)

import SHWUniform
try:
    from typing import List
except:
    pass

class SHWCompileRequestT(object):

    # SHWCompileRequestT
    def __init__(
        self,
        SHADER_NAME = None,
        SHADER_STAGE = 0,
        SHADER_INJECTION_POINT = 0,
        SHADER_SOURCE = None,
        GLSL_VERSION = None,
        SHADER_UNIFORMS = None,
    ):
        self.SHADER_NAME = SHADER_NAME  # type: Optional[str]
        self.SHADER_STAGE = SHADER_STAGE  # type: int
        self.SHADER_INJECTION_POINT = SHADER_INJECTION_POINT  # type: int
        self.SHADER_SOURCE = SHADER_SOURCE  # type: Optional[str]
        self.GLSL_VERSION = GLSL_VERSION  # type: Optional[str]
        self.SHADER_UNIFORMS = SHADER_UNIFORMS  # type: Optional[List[SHWUniform.SHWUniformT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpShwcompileRequest = SHWCompileRequest()
        tmpShwcompileRequest.Init(buf, pos)
        return cls.InitFromObj(tmpShwcompileRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpShwcompileRequest):
        x = SHWCompileRequestT()
        x._UnPack(tmpShwcompileRequest)
        return x

    # SHWCompileRequestT
    def _UnPack(self, SHWCompileRequest):
        if SHWCompileRequest is None:
            return
        self.SHADER_NAME = SHWCompileRequest.SHADER_NAME()
        self.SHADER_STAGE = SHWCompileRequest.SHADER_STAGE()
        self.SHADER_INJECTION_POINT = SHWCompileRequest.SHADER_INJECTION_POINT()
        self.SHADER_SOURCE = SHWCompileRequest.SHADER_SOURCE()
        self.GLSL_VERSION = SHWCompileRequest.GLSL_VERSION()
        if not SHWCompileRequest.SHADER_UNIFORMSIsNone():
            self.SHADER_UNIFORMS = []
            for i in range(SHWCompileRequest.SHADER_UNIFORMSLength()):
                if SHWCompileRequest.SHADER_UNIFORMS(i) is None:
                    self.SHADER_UNIFORMS.append(None)
                else:
                    sHWUniform_ = SHWUniform.SHWUniformT.InitFromObj(SHWCompileRequest.SHADER_UNIFORMS(i))
                    self.SHADER_UNIFORMS.append(sHWUniform_)

    # SHWCompileRequestT
    def Pack(self, builder):
        if self.SHADER_NAME is not None:
            SHADER_NAME = builder.CreateString(self.SHADER_NAME)
        if self.SHADER_SOURCE is not None:
            SHADER_SOURCE = builder.CreateString(self.SHADER_SOURCE)
        if self.GLSL_VERSION is not None:
            GLSL_VERSION = builder.CreateString(self.GLSL_VERSION)
        if self.SHADER_UNIFORMS is not None:
            SHADER_UNIFORMSlist = []
            for i in range(len(self.SHADER_UNIFORMS)):
                SHADER_UNIFORMSlist.append(self.SHADER_UNIFORMS[i].Pack(builder))
            SHWCompileRequestStartSHADER_UNIFORMSVector(builder, len(self.SHADER_UNIFORMS))
            for i in reversed(range(len(self.SHADER_UNIFORMS))):
                builder.PrependUOffsetTRelative(SHADER_UNIFORMSlist[i])
            SHADER_UNIFORMS = builder.EndVector()
        SHWCompileRequestStart(builder)
        if self.SHADER_NAME is not None:
            SHWCompileRequestAddSHADER_NAME(builder, SHADER_NAME)
        SHWCompileRequestAddSHADER_STAGE(builder, self.SHADER_STAGE)
        SHWCompileRequestAddSHADER_INJECTION_POINT(builder, self.SHADER_INJECTION_POINT)
        if self.SHADER_SOURCE is not None:
            SHWCompileRequestAddSHADER_SOURCE(builder, SHADER_SOURCE)
        if self.GLSL_VERSION is not None:
            SHWCompileRequestAddGLSL_VERSION(builder, GLSL_VERSION)
        if self.SHADER_UNIFORMS is not None:
            SHWCompileRequestAddSHADER_UNIFORMS(builder, SHADER_UNIFORMS)
        SHWCompileRequest = SHWCompileRequestEnd(builder)
        return SHWCompileRequest
