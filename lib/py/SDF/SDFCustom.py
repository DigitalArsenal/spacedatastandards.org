# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Custom SDF defined by a set of unit boundary-direction vectors.
class SDFCustom(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDFCustom()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDFCustom(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFCustomBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDFCustom
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Unit vectors defining the boundary packed as [x0, y0, z0, x1, y1, z1, ...].
    # SDFCustom
    def DIRECTIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SDFCustom
    def DIRECTIONSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SDFCustom
    def DIRECTIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDFCustom
    def DIRECTIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # SDFCustom
    def MAX_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SDFCustomStart(builder):
    builder.StartObject(2)

def Start(builder):
    SDFCustomStart(builder)

def SDFCustomAddDIRECTIONS(builder, DIRECTIONS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(DIRECTIONS), 0)

def AddDIRECTIONS(builder, DIRECTIONS):
    SDFCustomAddDIRECTIONS(builder, DIRECTIONS)

def SDFCustomStartDIRECTIONSVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartDIRECTIONSVector(builder, numElems):
    return SDFCustomStartDIRECTIONSVector(builder, numElems)

def SDFCustomCreateDIRECTIONSVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateDIRECTIONSVector(builder, data):
    SDFCustomCreateDIRECTIONSVector(builder, data)

def SDFCustomAddMAX_RANGE(builder, MAX_RANGE):
    builder.PrependFloat64Slot(1, MAX_RANGE, 0.0)

def AddMAX_RANGE(builder, MAX_RANGE):
    SDFCustomAddMAX_RANGE(builder, MAX_RANGE)

def SDFCustomEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFCustomEnd(builder)

try:
    from typing import List
except:
    pass

class SDFCustomT(object):

    # SDFCustomT
    def __init__(
        self,
        DIRECTIONS = None,
        MAX_RANGE = 0.0,
    ):
        self.DIRECTIONS = DIRECTIONS  # type: Optional[List[float]]
        self.MAX_RANGE = MAX_RANGE  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfcustom = SDFCustom()
        tmpSdfcustom.Init(buf, pos)
        return cls.InitFromObj(tmpSdfcustom)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfcustom):
        x = SDFCustomT()
        x._UnPack(tmpSdfcustom)
        return x

    # SDFCustomT
    def _UnPack(self, SDFCustom):
        if SDFCustom is None:
            return
        if not SDFCustom.DIRECTIONSIsNone():
            if np is None:
                self.DIRECTIONS = []
                for i in range(SDFCustom.DIRECTIONSLength()):
                    self.DIRECTIONS.append(SDFCustom.DIRECTIONS(i))
            else:
                self.DIRECTIONS = SDFCustom.DIRECTIONSAsNumpy()
        self.MAX_RANGE = SDFCustom.MAX_RANGE()

    # SDFCustomT
    def Pack(self, builder):
        if self.DIRECTIONS is not None:
            if np is not None and type(self.DIRECTIONS) is np.ndarray:
                DIRECTIONS = builder.CreateNumpyVector(self.DIRECTIONS)
            else:
                SDFCustomStartDIRECTIONSVector(builder, len(self.DIRECTIONS))
                for i in reversed(range(len(self.DIRECTIONS))):
                    builder.PrependFloat64(self.DIRECTIONS[i])
                DIRECTIONS = builder.EndVector()
        SDFCustomStart(builder)
        if self.DIRECTIONS is not None:
            SDFCustomAddDIRECTIONS(builder, DIRECTIONS)
        SDFCustomAddMAX_RANGE(builder, self.MAX_RANGE)
        SDFCustom = SDFCustomEnd(builder)
        return SDFCustom
