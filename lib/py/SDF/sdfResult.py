# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Result of a single SDF point-in-volume query (24 bytes, 8-byte aligned).
class sdfResult(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 24

    # sdfResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Entity identifier.
    # sdfResult
    def ENTITY_ID(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # Detection flag (1 == inside, 0 == outside).
    # sdfResult
    def DETECTED(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))
    # Signed distance (negative inside, zero on boundary, positive outside).
    # sdfResult
    def SIGNED_DISTANCE(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # Distance to the closest boundary point (always positive).
    # sdfResult
    def CLOSEST_DISTANCE(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))

def CreatesdfResult(builder, ENTITY_ID, DETECTED, SIGNED_DISTANCE, CLOSEST_DISTANCE):
    builder.Prep(8, 24)
    builder.PrependFloat64(CLOSEST_DISTANCE)
    builder.PrependFloat64(SIGNED_DISTANCE)
    builder.PrependUint32(DETECTED)
    builder.PrependUint32(ENTITY_ID)
    return builder.Offset()


class sdfResultT(object):

    # sdfResultT
    def __init__(
        self,
        ENTITY_ID = 0,
        DETECTED = 0,
        SIGNED_DISTANCE = 0.0,
        CLOSEST_DISTANCE = 0.0,
    ):
        self.ENTITY_ID = ENTITY_ID  # type: int
        self.DETECTED = DETECTED  # type: int
        self.SIGNED_DISTANCE = SIGNED_DISTANCE  # type: float
        self.CLOSEST_DISTANCE = CLOSEST_DISTANCE  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfResult = sdfResult()
        tmpSdfResult.Init(buf, pos)
        return cls.InitFromObj(tmpSdfResult)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfResult):
        x = sdfResultT()
        x._UnPack(tmpSdfResult)
        return x

    # sdfResultT
    def _UnPack(self, sdfResult):
        if sdfResult is None:
            return
        self.ENTITY_ID = sdfResult.ENTITY_ID()
        self.DETECTED = sdfResult.DETECTED()
        self.SIGNED_DISTANCE = sdfResult.SIGNED_DISTANCE()
        self.CLOSEST_DISTANCE = sdfResult.CLOSEST_DISTANCE()

    # sdfResultT
    def Pack(self, builder):
        return CreatesdfResult(builder, self.ENTITY_ID, self.DETECTED, self.SIGNED_DISTANCE, self.CLOSEST_DISTANCE)
