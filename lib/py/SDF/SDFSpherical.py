# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Spherical SDF parameters.
class SDFSpherical(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDFSpherical()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDFSpherical(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFSphericalBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDFSpherical
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDFSpherical
    def RADIUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Inner radius for hollow sphere (0 == solid).
    # SDFSpherical
    def INNER_RADIUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SDFSphericalStart(builder):
    builder.StartObject(2)

def Start(builder):
    SDFSphericalStart(builder)

def SDFSphericalAddRADIUS(builder, RADIUS):
    builder.PrependFloat64Slot(0, RADIUS, 0.0)

def AddRADIUS(builder, RADIUS):
    SDFSphericalAddRADIUS(builder, RADIUS)

def SDFSphericalAddINNER_RADIUS(builder, INNER_RADIUS):
    builder.PrependFloat64Slot(1, INNER_RADIUS, 0.0)

def AddINNER_RADIUS(builder, INNER_RADIUS):
    SDFSphericalAddINNER_RADIUS(builder, INNER_RADIUS)

def SDFSphericalEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFSphericalEnd(builder)


class SDFSphericalT(object):

    # SDFSphericalT
    def __init__(
        self,
        RADIUS = 0.0,
        INNER_RADIUS = 0.0,
    ):
        self.RADIUS = RADIUS  # type: float
        self.INNER_RADIUS = INNER_RADIUS  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfspherical = SDFSpherical()
        tmpSdfspherical.Init(buf, pos)
        return cls.InitFromObj(tmpSdfspherical)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfspherical):
        x = SDFSphericalT()
        x._UnPack(tmpSdfspherical)
        return x

    # SDFSphericalT
    def _UnPack(self, SDFSpherical):
        if SDFSpherical is None:
            return
        self.RADIUS = SDFSpherical.RADIUS()
        self.INNER_RADIUS = SDFSpherical.INNER_RADIUS()

    # SDFSphericalT
    def Pack(self, builder):
        SDFSphericalStart(builder)
        SDFSphericalAddRADIUS(builder, self.RADIUS)
        SDFSphericalAddINNER_RADIUS(builder, self.INNER_RADIUS)
        SDFSpherical = SDFSphericalEnd(builder)
        return SDFSpherical
