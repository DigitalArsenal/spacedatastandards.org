# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Complete SDF configuration.
class SDFConfig(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDFConfig()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDFConfig(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFConfigBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDFConfig
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Root primitive type (useful when COMPONENTS[] is empty).
    # SDFConfig
    def PRIMITIVE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # One or more components that may be combined via COMPOSITIONS[].
    # SDFConfig
    def COMPONENTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SDFComponent import SDFComponent
            obj = SDFComponent()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SDFConfig
    def COMPONENTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDFConfig
    def COMPONENTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Composition tree over COMPONENTS[].
    # SDFConfig
    def COMPOSITIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SDFComposition import SDFComposition
            obj = SDFComposition()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SDFConfig
    def COMPOSITIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDFConfig
    def COMPOSITIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # World-to-local transform (4x4 column-major, 16 doubles).
    # SDFConfig
    def INVERSE_TRANSFORM(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SDFConfig
    def INVERSE_TRANSFORMAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SDFConfig
    def INVERSE_TRANSFORMLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDFConfig
    def INVERSE_TRANSFORMIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def SDFConfigStart(builder):
    builder.StartObject(4)

def Start(builder):
    SDFConfigStart(builder)

def SDFConfigAddPRIMITIVE(builder, PRIMITIVE):
    builder.PrependUint8Slot(0, PRIMITIVE, 0)

def AddPRIMITIVE(builder, PRIMITIVE):
    SDFConfigAddPRIMITIVE(builder, PRIMITIVE)

def SDFConfigAddCOMPONENTS(builder, COMPONENTS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(COMPONENTS), 0)

def AddCOMPONENTS(builder, COMPONENTS):
    SDFConfigAddCOMPONENTS(builder, COMPONENTS)

def SDFConfigStartCOMPONENTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCOMPONENTSVector(builder, numElems):
    return SDFConfigStartCOMPONENTSVector(builder, numElems)

def SDFConfigCreateCOMPONENTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCOMPONENTSVector(builder, data):
    SDFConfigCreateCOMPONENTSVector(builder, data)

def SDFConfigAddCOMPOSITIONS(builder, COMPOSITIONS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(COMPOSITIONS), 0)

def AddCOMPOSITIONS(builder, COMPOSITIONS):
    SDFConfigAddCOMPOSITIONS(builder, COMPOSITIONS)

def SDFConfigStartCOMPOSITIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCOMPOSITIONSVector(builder, numElems):
    return SDFConfigStartCOMPOSITIONSVector(builder, numElems)

def SDFConfigCreateCOMPOSITIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCOMPOSITIONSVector(builder, data):
    SDFConfigCreateCOMPOSITIONSVector(builder, data)

def SDFConfigAddINVERSE_TRANSFORM(builder, INVERSE_TRANSFORM):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(INVERSE_TRANSFORM), 0)

def AddINVERSE_TRANSFORM(builder, INVERSE_TRANSFORM):
    SDFConfigAddINVERSE_TRANSFORM(builder, INVERSE_TRANSFORM)

def SDFConfigStartINVERSE_TRANSFORMVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartINVERSE_TRANSFORMVector(builder, numElems):
    return SDFConfigStartINVERSE_TRANSFORMVector(builder, numElems)

def SDFConfigCreateINVERSE_TRANSFORMVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateINVERSE_TRANSFORMVector(builder, data):
    SDFConfigCreateINVERSE_TRANSFORMVector(builder, data)

def SDFConfigEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFConfigEnd(builder)

import SDFComponent
import SDFComposition
try:
    from typing import List
except:
    pass

class SDFConfigT(object):

    # SDFConfigT
    def __init__(
        self,
        PRIMITIVE = 0,
        COMPONENTS = None,
        COMPOSITIONS = None,
        INVERSE_TRANSFORM = None,
    ):
        self.PRIMITIVE = PRIMITIVE  # type: int
        self.COMPONENTS = COMPONENTS  # type: Optional[List[SDFComponent.SDFComponentT]]
        self.COMPOSITIONS = COMPOSITIONS  # type: Optional[List[SDFComposition.SDFCompositionT]]
        self.INVERSE_TRANSFORM = INVERSE_TRANSFORM  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfconfig = SDFConfig()
        tmpSdfconfig.Init(buf, pos)
        return cls.InitFromObj(tmpSdfconfig)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfconfig):
        x = SDFConfigT()
        x._UnPack(tmpSdfconfig)
        return x

    # SDFConfigT
    def _UnPack(self, SDFConfig):
        if SDFConfig is None:
            return
        self.PRIMITIVE = SDFConfig.PRIMITIVE()
        if not SDFConfig.COMPONENTSIsNone():
            self.COMPONENTS = []
            for i in range(SDFConfig.COMPONENTSLength()):
                if SDFConfig.COMPONENTS(i) is None:
                    self.COMPONENTS.append(None)
                else:
                    sDFComponent_ = SDFComponent.SDFComponentT.InitFromObj(SDFConfig.COMPONENTS(i))
                    self.COMPONENTS.append(sDFComponent_)
        if not SDFConfig.COMPOSITIONSIsNone():
            self.COMPOSITIONS = []
            for i in range(SDFConfig.COMPOSITIONSLength()):
                if SDFConfig.COMPOSITIONS(i) is None:
                    self.COMPOSITIONS.append(None)
                else:
                    sDFComposition_ = SDFComposition.SDFCompositionT.InitFromObj(SDFConfig.COMPOSITIONS(i))
                    self.COMPOSITIONS.append(sDFComposition_)
        if not SDFConfig.INVERSE_TRANSFORMIsNone():
            if np is None:
                self.INVERSE_TRANSFORM = []
                for i in range(SDFConfig.INVERSE_TRANSFORMLength()):
                    self.INVERSE_TRANSFORM.append(SDFConfig.INVERSE_TRANSFORM(i))
            else:
                self.INVERSE_TRANSFORM = SDFConfig.INVERSE_TRANSFORMAsNumpy()

    # SDFConfigT
    def Pack(self, builder):
        if self.COMPONENTS is not None:
            COMPONENTSlist = []
            for i in range(len(self.COMPONENTS)):
                COMPONENTSlist.append(self.COMPONENTS[i].Pack(builder))
            SDFConfigStartCOMPONENTSVector(builder, len(self.COMPONENTS))
            for i in reversed(range(len(self.COMPONENTS))):
                builder.PrependUOffsetTRelative(COMPONENTSlist[i])
            COMPONENTS = builder.EndVector()
        if self.COMPOSITIONS is not None:
            COMPOSITIONSlist = []
            for i in range(len(self.COMPOSITIONS)):
                COMPOSITIONSlist.append(self.COMPOSITIONS[i].Pack(builder))
            SDFConfigStartCOMPOSITIONSVector(builder, len(self.COMPOSITIONS))
            for i in reversed(range(len(self.COMPOSITIONS))):
                builder.PrependUOffsetTRelative(COMPOSITIONSlist[i])
            COMPOSITIONS = builder.EndVector()
        if self.INVERSE_TRANSFORM is not None:
            if np is not None and type(self.INVERSE_TRANSFORM) is np.ndarray:
                INVERSE_TRANSFORM = builder.CreateNumpyVector(self.INVERSE_TRANSFORM)
            else:
                SDFConfigStartINVERSE_TRANSFORMVector(builder, len(self.INVERSE_TRANSFORM))
                for i in reversed(range(len(self.INVERSE_TRANSFORM))):
                    builder.PrependFloat64(self.INVERSE_TRANSFORM[i])
                INVERSE_TRANSFORM = builder.EndVector()
        SDFConfigStart(builder)
        SDFConfigAddPRIMITIVE(builder, self.PRIMITIVE)
        if self.COMPONENTS is not None:
            SDFConfigAddCOMPONENTS(builder, COMPONENTS)
        if self.COMPOSITIONS is not None:
            SDFConfigAddCOMPOSITIONS(builder, COMPOSITIONS)
        if self.INVERSE_TRANSFORM is not None:
            SDFConfigAddINVERSE_TRANSFORM(builder, INVERSE_TRANSFORM)
        SDFConfig = SDFConfigEnd(builder)
        return SDFConfig
