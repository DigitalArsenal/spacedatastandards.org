# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Signed Distance Field envelope message — carries a configuration, a batch
# request, a batch response, or single / time-range result records.
class SDF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDF
    def CONFIG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SDFConfig import SDFConfig
            obj = SDFConfig()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SDF
    def BATCH_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SDFBatchRequest import SDFBatchRequest
            obj = SDFBatchRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SDF
    def BATCH_RESPONSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SDFBatchResponse import SDFBatchResponse
            obj = SDFBatchResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SDF
    def RESULTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 24
            from sdfResult import sdfResult
            obj = sdfResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SDF
    def RESULTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDF
    def RESULTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # SDF
    def TIME_RANGE_RESULTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 40
            from sdfTimeRangeResult import sdfTimeRangeResult
            obj = sdfTimeRangeResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SDF
    def TIME_RANGE_RESULTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDF
    def TIME_RANGE_RESULTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def SDFStart(builder):
    builder.StartObject(5)

def Start(builder):
    SDFStart(builder)

def SDFAddCONFIG(builder, CONFIG):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CONFIG), 0)

def AddCONFIG(builder, CONFIG):
    SDFAddCONFIG(builder, CONFIG)

def SDFAddBATCH_REQUEST(builder, BATCH_REQUEST):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(BATCH_REQUEST), 0)

def AddBATCH_REQUEST(builder, BATCH_REQUEST):
    SDFAddBATCH_REQUEST(builder, BATCH_REQUEST)

def SDFAddBATCH_RESPONSE(builder, BATCH_RESPONSE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(BATCH_RESPONSE), 0)

def AddBATCH_RESPONSE(builder, BATCH_RESPONSE):
    SDFAddBATCH_RESPONSE(builder, BATCH_RESPONSE)

def SDFAddRESULTS(builder, RESULTS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(RESULTS), 0)

def AddRESULTS(builder, RESULTS):
    SDFAddRESULTS(builder, RESULTS)

def SDFStartRESULTSVector(builder, numElems):
    return builder.StartVector(24, numElems, 8)

def StartRESULTSVector(builder, numElems):
    return SDFStartRESULTSVector(builder, numElems)

def SDFCreateRESULTSVector(builder, data):
    data = list(data)
    builder.StartVector(24, len(data), 8)
    for item in reversed(data):
        item.Pack(builder)
    return builder.EndVector()

def CreateRESULTSVector(builder, data):
    SDFCreateRESULTSVector(builder, data)

def SDFAddTIME_RANGE_RESULTS(builder, TIME_RANGE_RESULTS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(TIME_RANGE_RESULTS), 0)

def AddTIME_RANGE_RESULTS(builder, TIME_RANGE_RESULTS):
    SDFAddTIME_RANGE_RESULTS(builder, TIME_RANGE_RESULTS)

def SDFStartTIME_RANGE_RESULTSVector(builder, numElems):
    return builder.StartVector(40, numElems, 8)

def StartTIME_RANGE_RESULTSVector(builder, numElems):
    return SDFStartTIME_RANGE_RESULTSVector(builder, numElems)

def SDFCreateTIME_RANGE_RESULTSVector(builder, data):
    data = list(data)
    builder.StartVector(40, len(data), 8)
    for item in reversed(data):
        item.Pack(builder)
    return builder.EndVector()

def CreateTIME_RANGE_RESULTSVector(builder, data):
    SDFCreateTIME_RANGE_RESULTSVector(builder, data)

def SDFEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFEnd(builder)

import SDFBatchRequest
import SDFBatchResponse
import SDFConfig
import sdfResult
import sdfTimeRangeResult
try:
    from typing import List, Optional
except:
    pass

class SDFT(object):

    # SDFT
    def __init__(
        self,
        CONFIG = None,
        BATCH_REQUEST = None,
        BATCH_RESPONSE = None,
        RESULTS = None,
        TIME_RANGE_RESULTS = None,
    ):
        self.CONFIG = CONFIG  # type: Optional[SDFConfig.SDFConfigT]
        self.BATCH_REQUEST = BATCH_REQUEST  # type: Optional[SDFBatchRequest.SDFBatchRequestT]
        self.BATCH_RESPONSE = BATCH_RESPONSE  # type: Optional[SDFBatchResponse.SDFBatchResponseT]
        self.RESULTS = RESULTS  # type: Optional[List[sdfResult.sdfResultT]]
        self.TIME_RANGE_RESULTS = TIME_RANGE_RESULTS  # type: Optional[List[sdfTimeRangeResult.sdfTimeRangeResultT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdf = SDF()
        tmpSdf.Init(buf, pos)
        return cls.InitFromObj(tmpSdf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdf):
        x = SDFT()
        x._UnPack(tmpSdf)
        return x

    # SDFT
    def _UnPack(self, SDF):
        if SDF is None:
            return
        if SDF.CONFIG() is not None:
            self.CONFIG = SDFConfig.SDFConfigT.InitFromObj(SDF.CONFIG())
        if SDF.BATCH_REQUEST() is not None:
            self.BATCH_REQUEST = SDFBatchRequest.SDFBatchRequestT.InitFromObj(SDF.BATCH_REQUEST())
        if SDF.BATCH_RESPONSE() is not None:
            self.BATCH_RESPONSE = SDFBatchResponse.SDFBatchResponseT.InitFromObj(SDF.BATCH_RESPONSE())
        if not SDF.RESULTSIsNone():
            self.RESULTS = []
            for i in range(SDF.RESULTSLength()):
                if SDF.RESULTS(i) is None:
                    self.RESULTS.append(None)
                else:
                    sdfResult_ = sdfResult.sdfResultT.InitFromObj(SDF.RESULTS(i))
                    self.RESULTS.append(sdfResult_)
        if not SDF.TIME_RANGE_RESULTSIsNone():
            self.TIME_RANGE_RESULTS = []
            for i in range(SDF.TIME_RANGE_RESULTSLength()):
                if SDF.TIME_RANGE_RESULTS(i) is None:
                    self.TIME_RANGE_RESULTS.append(None)
                else:
                    sdfTimeRangeResult_ = sdfTimeRangeResult.sdfTimeRangeResultT.InitFromObj(SDF.TIME_RANGE_RESULTS(i))
                    self.TIME_RANGE_RESULTS.append(sdfTimeRangeResult_)

    # SDFT
    def Pack(self, builder):
        if self.CONFIG is not None:
            CONFIG = self.CONFIG.Pack(builder)
        if self.BATCH_REQUEST is not None:
            BATCH_REQUEST = self.BATCH_REQUEST.Pack(builder)
        if self.BATCH_RESPONSE is not None:
            BATCH_RESPONSE = self.BATCH_RESPONSE.Pack(builder)
        if self.RESULTS is not None:
            SDFStartRESULTSVector(builder, len(self.RESULTS))
            for i in reversed(range(len(self.RESULTS))):
                self.RESULTS[i].Pack(builder)
            RESULTS = builder.EndVector()
        if self.TIME_RANGE_RESULTS is not None:
            SDFStartTIME_RANGE_RESULTSVector(builder, len(self.TIME_RANGE_RESULTS))
            for i in reversed(range(len(self.TIME_RANGE_RESULTS))):
                self.TIME_RANGE_RESULTS[i].Pack(builder)
            TIME_RANGE_RESULTS = builder.EndVector()
        SDFStart(builder)
        if self.CONFIG is not None:
            SDFAddCONFIG(builder, CONFIG)
        if self.BATCH_REQUEST is not None:
            SDFAddBATCH_REQUEST(builder, BATCH_REQUEST)
        if self.BATCH_RESPONSE is not None:
            SDFAddBATCH_RESPONSE(builder, BATCH_RESPONSE)
        if self.RESULTS is not None:
            SDFAddRESULTS(builder, RESULTS)
        if self.TIME_RANGE_RESULTS is not None:
            SDFAddTIME_RANGE_RESULTS(builder, TIME_RANGE_RESULTS)
        SDF = SDFEnd(builder)
        return SDF
