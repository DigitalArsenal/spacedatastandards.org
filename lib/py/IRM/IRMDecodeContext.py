# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Everything a resuming reader needs to decode a chunk that does not begin at
# offset 0. Required whenever NEXT_OFFSET is past the source's own header.
class IRMDecodeContext(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IRMDecodeContext()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIRMDecodeContext(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IRMDecodeContextBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x52\x4D", size_prefixed=size_prefixed)

    # IRMDecodeContext
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Format token the decoder dispatches on, verbatim and lowercase, e.g.
    # `text/csv`. A consumer that does not implement the format refuses the chunk
    # rather than guessing at a layout.
    # IRMDecodeContext
    def FORMAT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The source's header line verbatim, exactly as it appeared at offset 0 and
    # excluding its line terminator. This is the field that makes chunk N>0
    # decodable; without it a headerless chunk parses to zero rows and looks like
    # a clean tail.
    # IRMDecodeContext
    def HEADER_LINE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Byte length the header occupied at the start of the source, including its
    # terminator, so a reader can tell a header-bearing prefix from a data row.
    # IRMDecodeContext
    def HEADER_BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Column names in source order, when the publisher parsed them. Advisory:
    # HEADER_LINE is authoritative and a disagreement is resolved in its favour.
    # IRMDecodeContext
    def COLUMN_NAMES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # IRMDecodeContext
    def COLUMN_NAMESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # IRMDecodeContext
    def COLUMN_NAMESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Field delimiter verbatim.
    # IRMDecodeContext
    def DELIMITER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Quote character verbatim.
    # IRMDecodeContext
    def QUOTE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Escape character verbatim.
    # IRMDecodeContext
    def ESCAPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Record terminator verbatim, e.g. `\n` or `\r\n`.
    # IRMDecodeContext
    def LINE_TERMINATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Character encoding label verbatim as the provider states it.
    # IRMDecodeContext
    def CHARSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Stream-compression codec applied to the source bytes, verbatim and
    # lowercase. Empty means the ranged bytes are read as-is. Non-empty means
    # NEXT_OFFSET is a position in the COMPRESSED stream and DECODER_STATE,
    # WINDOW and PARTIAL_RECORD are all required to resume.
    # IRMDecodeContext
    def COMPRESSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Position in the DECODED stream corresponding to NEXT_OFFSET, when the
    # source is compressed. 0 with a non-empty COMPRESSION means the decoded
    # stream has not been advanced.
    # IRMDecodeContext
    def DECODED_OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Serialized, pointer-free image of the decoder at the seam — the state a
    # codec with no restart point needs in order to have one. Opaque to every
    # consumer but a decoder whose stamps match exactly. Never partially applied.
    # IRMDecodeContext
    def DECODER_STATE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # IRMDecodeContext
    def DECODER_STATEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # IRMDecodeContext
    def DECODER_STATELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # IRMDecodeContext
    def DECODER_STATEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # Layout identifier of DECODER_STATE, verbatim, naming the decoder struct the
    # image was taken from. A consumer that does not know the token restarts.
    # IRMDecodeContext
    def DECODER_STATE_FORMAT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Version of that layout, verbatim. Any disagreement restarts the job.
    # IRMDecodeContext
    def DECODER_STATE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Byte length DECODER_STATE is expected to have. Carried even though the
    # vector knows its own size, so a size disagreement is detectable BEFORE the
    # image is loaded into a differently shaped struct.
    # IRMDecodeContext
    def DECODER_STATE_BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SHA-256 of DECODER_STATE as 64 lowercase hexadecimal characters.
    # IRMDecodeContext
    def DECODER_STATE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Build identifier of the decoder that produced DECODER_STATE, verbatim, so
    # two builds of one codec version are still distinguishable.
    # IRMDecodeContext
    def DECODER_BUILD_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Media type of DECODER_STATE, so an unrelated decoder refuses it instead of
    # misreading it.
    # IRMDecodeContext
    def DECODER_STATE_MEDIA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The decoder's history window at the seam — the bytes that ARE the codec's
    # back-reference dictionary. Carried separately from DECODER_STATE because it
    # is bulk data of a size the codec fixes, not part of the struct image.
    # IRMDecodeContext
    def WINDOW(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # IRMDecodeContext
    def WINDOWAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # IRMDecodeContext
    def WINDOWLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # IRMDecodeContext
    def WINDOWIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        return o == 0

    # Byte length WINDOW is expected to have, for the same detect-before-load
    # reason as DECODER_STATE_BYTE_LENGTH.
    # IRMDecodeContext
    def WINDOW_BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Offset into WINDOW at which the codec will next write, when the window is
    # a circular buffer whose contents cannot be interpreted without it.
    # IRMDecodeContext
    def WINDOW_POSITION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # The bytes of the record that straddles the seam: everything the previous
    # chunk decoded of a record it could not finish. A resuming decoder prepends
    # these to the next chunk's decoded output before parsing. Carried as bytes,
    # not text, because a seam may fall inside a multi-byte character.
    # IRMDecodeContext
    def PARTIAL_RECORD(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # IRMDecodeContext
    def PARTIAL_RECORDAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # IRMDecodeContext
    def PARTIAL_RECORDLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # IRMDecodeContext
    def PARTIAL_RECORDIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        return o == 0

def IRMDecodeContextStart(builder):
    builder.StartObject(22)

def Start(builder):
    IRMDecodeContextStart(builder)

def IRMDecodeContextAddFORMAT(builder, FORMAT):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(FORMAT), 0)

def AddFORMAT(builder, FORMAT):
    IRMDecodeContextAddFORMAT(builder, FORMAT)

def IRMDecodeContextAddHEADER_LINE(builder, HEADER_LINE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(HEADER_LINE), 0)

def AddHEADER_LINE(builder, HEADER_LINE):
    IRMDecodeContextAddHEADER_LINE(builder, HEADER_LINE)

def IRMDecodeContextAddHEADER_BYTE_LENGTH(builder, HEADER_BYTE_LENGTH):
    builder.PrependUint64Slot(2, HEADER_BYTE_LENGTH, 0)

def AddHEADER_BYTE_LENGTH(builder, HEADER_BYTE_LENGTH):
    IRMDecodeContextAddHEADER_BYTE_LENGTH(builder, HEADER_BYTE_LENGTH)

def IRMDecodeContextAddCOLUMN_NAMES(builder, COLUMN_NAMES):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(COLUMN_NAMES), 0)

def AddCOLUMN_NAMES(builder, COLUMN_NAMES):
    IRMDecodeContextAddCOLUMN_NAMES(builder, COLUMN_NAMES)

def IRMDecodeContextStartCOLUMN_NAMESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCOLUMN_NAMESVector(builder, numElems):
    return IRMDecodeContextStartCOLUMN_NAMESVector(builder, numElems)

def IRMDecodeContextCreateCOLUMN_NAMESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCOLUMN_NAMESVector(builder, data):
    IRMDecodeContextCreateCOLUMN_NAMESVector(builder, data)

def IRMDecodeContextAddDELIMITER(builder, DELIMITER):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(DELIMITER), 0)

def AddDELIMITER(builder, DELIMITER):
    IRMDecodeContextAddDELIMITER(builder, DELIMITER)

def IRMDecodeContextAddQUOTE(builder, QUOTE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(QUOTE), 0)

def AddQUOTE(builder, QUOTE):
    IRMDecodeContextAddQUOTE(builder, QUOTE)

def IRMDecodeContextAddESCAPE(builder, ESCAPE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ESCAPE), 0)

def AddESCAPE(builder, ESCAPE):
    IRMDecodeContextAddESCAPE(builder, ESCAPE)

def IRMDecodeContextAddLINE_TERMINATOR(builder, LINE_TERMINATOR):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(LINE_TERMINATOR), 0)

def AddLINE_TERMINATOR(builder, LINE_TERMINATOR):
    IRMDecodeContextAddLINE_TERMINATOR(builder, LINE_TERMINATOR)

def IRMDecodeContextAddCHARSET(builder, CHARSET):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(CHARSET), 0)

def AddCHARSET(builder, CHARSET):
    IRMDecodeContextAddCHARSET(builder, CHARSET)

def IRMDecodeContextAddCOMPRESSION(builder, COMPRESSION):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(COMPRESSION), 0)

def AddCOMPRESSION(builder, COMPRESSION):
    IRMDecodeContextAddCOMPRESSION(builder, COMPRESSION)

def IRMDecodeContextAddDECODED_OFFSET(builder, DECODED_OFFSET):
    builder.PrependUint64Slot(10, DECODED_OFFSET, 0)

def AddDECODED_OFFSET(builder, DECODED_OFFSET):
    IRMDecodeContextAddDECODED_OFFSET(builder, DECODED_OFFSET)

def IRMDecodeContextAddDECODER_STATE(builder, DECODER_STATE):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(DECODER_STATE), 0)

def AddDECODER_STATE(builder, DECODER_STATE):
    IRMDecodeContextAddDECODER_STATE(builder, DECODER_STATE)

def IRMDecodeContextStartDECODER_STATEVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDECODER_STATEVector(builder, numElems):
    return IRMDecodeContextStartDECODER_STATEVector(builder, numElems)

def IRMDecodeContextCreateDECODER_STATEVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateDECODER_STATEVector(builder, data):
    IRMDecodeContextCreateDECODER_STATEVector(builder, data)

def IRMDecodeContextAddDECODER_STATE_FORMAT(builder, DECODER_STATE_FORMAT):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(DECODER_STATE_FORMAT), 0)

def AddDECODER_STATE_FORMAT(builder, DECODER_STATE_FORMAT):
    IRMDecodeContextAddDECODER_STATE_FORMAT(builder, DECODER_STATE_FORMAT)

def IRMDecodeContextAddDECODER_STATE_VERSION(builder, DECODER_STATE_VERSION):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(DECODER_STATE_VERSION), 0)

def AddDECODER_STATE_VERSION(builder, DECODER_STATE_VERSION):
    IRMDecodeContextAddDECODER_STATE_VERSION(builder, DECODER_STATE_VERSION)

def IRMDecodeContextAddDECODER_STATE_BYTE_LENGTH(builder, DECODER_STATE_BYTE_LENGTH):
    builder.PrependUint64Slot(14, DECODER_STATE_BYTE_LENGTH, 0)

def AddDECODER_STATE_BYTE_LENGTH(builder, DECODER_STATE_BYTE_LENGTH):
    IRMDecodeContextAddDECODER_STATE_BYTE_LENGTH(builder, DECODER_STATE_BYTE_LENGTH)

def IRMDecodeContextAddDECODER_STATE_SHA256(builder, DECODER_STATE_SHA256):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(DECODER_STATE_SHA256), 0)

def AddDECODER_STATE_SHA256(builder, DECODER_STATE_SHA256):
    IRMDecodeContextAddDECODER_STATE_SHA256(builder, DECODER_STATE_SHA256)

def IRMDecodeContextAddDECODER_BUILD_ID(builder, DECODER_BUILD_ID):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(DECODER_BUILD_ID), 0)

def AddDECODER_BUILD_ID(builder, DECODER_BUILD_ID):
    IRMDecodeContextAddDECODER_BUILD_ID(builder, DECODER_BUILD_ID)

def IRMDecodeContextAddDECODER_STATE_MEDIA_TYPE(builder, DECODER_STATE_MEDIA_TYPE):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(DECODER_STATE_MEDIA_TYPE), 0)

def AddDECODER_STATE_MEDIA_TYPE(builder, DECODER_STATE_MEDIA_TYPE):
    IRMDecodeContextAddDECODER_STATE_MEDIA_TYPE(builder, DECODER_STATE_MEDIA_TYPE)

def IRMDecodeContextAddWINDOW(builder, WINDOW):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(WINDOW), 0)

def AddWINDOW(builder, WINDOW):
    IRMDecodeContextAddWINDOW(builder, WINDOW)

def IRMDecodeContextStartWINDOWVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartWINDOWVector(builder, numElems):
    return IRMDecodeContextStartWINDOWVector(builder, numElems)

def IRMDecodeContextCreateWINDOWVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateWINDOWVector(builder, data):
    IRMDecodeContextCreateWINDOWVector(builder, data)

def IRMDecodeContextAddWINDOW_BYTE_LENGTH(builder, WINDOW_BYTE_LENGTH):
    builder.PrependUint64Slot(19, WINDOW_BYTE_LENGTH, 0)

def AddWINDOW_BYTE_LENGTH(builder, WINDOW_BYTE_LENGTH):
    IRMDecodeContextAddWINDOW_BYTE_LENGTH(builder, WINDOW_BYTE_LENGTH)

def IRMDecodeContextAddWINDOW_POSITION(builder, WINDOW_POSITION):
    builder.PrependUint64Slot(20, WINDOW_POSITION, 0)

def AddWINDOW_POSITION(builder, WINDOW_POSITION):
    IRMDecodeContextAddWINDOW_POSITION(builder, WINDOW_POSITION)

def IRMDecodeContextAddPARTIAL_RECORD(builder, PARTIAL_RECORD):
    builder.PrependUOffsetTRelativeSlot(21, flatbuffers.number_types.UOffsetTFlags.py_type(PARTIAL_RECORD), 0)

def AddPARTIAL_RECORD(builder, PARTIAL_RECORD):
    IRMDecodeContextAddPARTIAL_RECORD(builder, PARTIAL_RECORD)

def IRMDecodeContextStartPARTIAL_RECORDVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPARTIAL_RECORDVector(builder, numElems):
    return IRMDecodeContextStartPARTIAL_RECORDVector(builder, numElems)

def IRMDecodeContextCreatePARTIAL_RECORDVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePARTIAL_RECORDVector(builder, data):
    IRMDecodeContextCreatePARTIAL_RECORDVector(builder, data)

def IRMDecodeContextEnd(builder):
    return builder.EndObject()

def End(builder):
    return IRMDecodeContextEnd(builder)

try:
    from typing import List
except:
    pass

class IRMDecodeContextT(object):

    # IRMDecodeContextT
    def __init__(
        self,
        FORMAT = None,
        HEADER_LINE = None,
        HEADER_BYTE_LENGTH = 0,
        COLUMN_NAMES = None,
        DELIMITER = None,
        QUOTE = None,
        ESCAPE = None,
        LINE_TERMINATOR = None,
        CHARSET = None,
        COMPRESSION = None,
        DECODED_OFFSET = 0,
        DECODER_STATE = None,
        DECODER_STATE_FORMAT = None,
        DECODER_STATE_VERSION = None,
        DECODER_STATE_BYTE_LENGTH = 0,
        DECODER_STATE_SHA256 = None,
        DECODER_BUILD_ID = None,
        DECODER_STATE_MEDIA_TYPE = None,
        WINDOW = None,
        WINDOW_BYTE_LENGTH = 0,
        WINDOW_POSITION = 0,
        PARTIAL_RECORD = None,
    ):
        self.FORMAT = FORMAT  # type: Optional[str]
        self.HEADER_LINE = HEADER_LINE  # type: Optional[str]
        self.HEADER_BYTE_LENGTH = HEADER_BYTE_LENGTH  # type: int
        self.COLUMN_NAMES = COLUMN_NAMES  # type: Optional[List[Optional[str]]]
        self.DELIMITER = DELIMITER  # type: Optional[str]
        self.QUOTE = QUOTE  # type: Optional[str]
        self.ESCAPE = ESCAPE  # type: Optional[str]
        self.LINE_TERMINATOR = LINE_TERMINATOR  # type: Optional[str]
        self.CHARSET = CHARSET  # type: Optional[str]
        self.COMPRESSION = COMPRESSION  # type: Optional[str]
        self.DECODED_OFFSET = DECODED_OFFSET  # type: int
        self.DECODER_STATE = DECODER_STATE  # type: Optional[List[int]]
        self.DECODER_STATE_FORMAT = DECODER_STATE_FORMAT  # type: Optional[str]
        self.DECODER_STATE_VERSION = DECODER_STATE_VERSION  # type: Optional[str]
        self.DECODER_STATE_BYTE_LENGTH = DECODER_STATE_BYTE_LENGTH  # type: int
        self.DECODER_STATE_SHA256 = DECODER_STATE_SHA256  # type: Optional[str]
        self.DECODER_BUILD_ID = DECODER_BUILD_ID  # type: Optional[str]
        self.DECODER_STATE_MEDIA_TYPE = DECODER_STATE_MEDIA_TYPE  # type: Optional[str]
        self.WINDOW = WINDOW  # type: Optional[List[int]]
        self.WINDOW_BYTE_LENGTH = WINDOW_BYTE_LENGTH  # type: int
        self.WINDOW_POSITION = WINDOW_POSITION  # type: int
        self.PARTIAL_RECORD = PARTIAL_RECORD  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIrmdecodeContext = IRMDecodeContext()
        tmpIrmdecodeContext.Init(buf, pos)
        return cls.InitFromObj(tmpIrmdecodeContext)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIrmdecodeContext):
        x = IRMDecodeContextT()
        x._UnPack(tmpIrmdecodeContext)
        return x

    # IRMDecodeContextT
    def _UnPack(self, IRMDecodeContext):
        if IRMDecodeContext is None:
            return
        self.FORMAT = IRMDecodeContext.FORMAT()
        self.HEADER_LINE = IRMDecodeContext.HEADER_LINE()
        self.HEADER_BYTE_LENGTH = IRMDecodeContext.HEADER_BYTE_LENGTH()
        if not IRMDecodeContext.COLUMN_NAMESIsNone():
            self.COLUMN_NAMES = []
            for i in range(IRMDecodeContext.COLUMN_NAMESLength()):
                self.COLUMN_NAMES.append(IRMDecodeContext.COLUMN_NAMES(i))
        self.DELIMITER = IRMDecodeContext.DELIMITER()
        self.QUOTE = IRMDecodeContext.QUOTE()
        self.ESCAPE = IRMDecodeContext.ESCAPE()
        self.LINE_TERMINATOR = IRMDecodeContext.LINE_TERMINATOR()
        self.CHARSET = IRMDecodeContext.CHARSET()
        self.COMPRESSION = IRMDecodeContext.COMPRESSION()
        self.DECODED_OFFSET = IRMDecodeContext.DECODED_OFFSET()
        if not IRMDecodeContext.DECODER_STATEIsNone():
            if np is None:
                self.DECODER_STATE = []
                for i in range(IRMDecodeContext.DECODER_STATELength()):
                    self.DECODER_STATE.append(IRMDecodeContext.DECODER_STATE(i))
            else:
                self.DECODER_STATE = IRMDecodeContext.DECODER_STATEAsNumpy()
        self.DECODER_STATE_FORMAT = IRMDecodeContext.DECODER_STATE_FORMAT()
        self.DECODER_STATE_VERSION = IRMDecodeContext.DECODER_STATE_VERSION()
        self.DECODER_STATE_BYTE_LENGTH = IRMDecodeContext.DECODER_STATE_BYTE_LENGTH()
        self.DECODER_STATE_SHA256 = IRMDecodeContext.DECODER_STATE_SHA256()
        self.DECODER_BUILD_ID = IRMDecodeContext.DECODER_BUILD_ID()
        self.DECODER_STATE_MEDIA_TYPE = IRMDecodeContext.DECODER_STATE_MEDIA_TYPE()
        if not IRMDecodeContext.WINDOWIsNone():
            if np is None:
                self.WINDOW = []
                for i in range(IRMDecodeContext.WINDOWLength()):
                    self.WINDOW.append(IRMDecodeContext.WINDOW(i))
            else:
                self.WINDOW = IRMDecodeContext.WINDOWAsNumpy()
        self.WINDOW_BYTE_LENGTH = IRMDecodeContext.WINDOW_BYTE_LENGTH()
        self.WINDOW_POSITION = IRMDecodeContext.WINDOW_POSITION()
        if not IRMDecodeContext.PARTIAL_RECORDIsNone():
            if np is None:
                self.PARTIAL_RECORD = []
                for i in range(IRMDecodeContext.PARTIAL_RECORDLength()):
                    self.PARTIAL_RECORD.append(IRMDecodeContext.PARTIAL_RECORD(i))
            else:
                self.PARTIAL_RECORD = IRMDecodeContext.PARTIAL_RECORDAsNumpy()

    # IRMDecodeContextT
    def Pack(self, builder):
        if self.FORMAT is not None:
            FORMAT = builder.CreateString(self.FORMAT)
        if self.HEADER_LINE is not None:
            HEADER_LINE = builder.CreateString(self.HEADER_LINE)
        if self.COLUMN_NAMES is not None:
            COLUMN_NAMESlist = []
            for i in range(len(self.COLUMN_NAMES)):
                COLUMN_NAMESlist.append(builder.CreateString(self.COLUMN_NAMES[i]))
            IRMDecodeContextStartCOLUMN_NAMESVector(builder, len(self.COLUMN_NAMES))
            for i in reversed(range(len(self.COLUMN_NAMES))):
                builder.PrependUOffsetTRelative(COLUMN_NAMESlist[i])
            COLUMN_NAMES = builder.EndVector()
        if self.DELIMITER is not None:
            DELIMITER = builder.CreateString(self.DELIMITER)
        if self.QUOTE is not None:
            QUOTE = builder.CreateString(self.QUOTE)
        if self.ESCAPE is not None:
            ESCAPE = builder.CreateString(self.ESCAPE)
        if self.LINE_TERMINATOR is not None:
            LINE_TERMINATOR = builder.CreateString(self.LINE_TERMINATOR)
        if self.CHARSET is not None:
            CHARSET = builder.CreateString(self.CHARSET)
        if self.COMPRESSION is not None:
            COMPRESSION = builder.CreateString(self.COMPRESSION)
        if self.DECODER_STATE is not None:
            if np is not None and type(self.DECODER_STATE) is np.ndarray:
                DECODER_STATE = builder.CreateNumpyVector(self.DECODER_STATE)
            else:
                IRMDecodeContextStartDECODER_STATEVector(builder, len(self.DECODER_STATE))
                for i in reversed(range(len(self.DECODER_STATE))):
                    builder.PrependUint8(self.DECODER_STATE[i])
                DECODER_STATE = builder.EndVector()
        if self.DECODER_STATE_FORMAT is not None:
            DECODER_STATE_FORMAT = builder.CreateString(self.DECODER_STATE_FORMAT)
        if self.DECODER_STATE_VERSION is not None:
            DECODER_STATE_VERSION = builder.CreateString(self.DECODER_STATE_VERSION)
        if self.DECODER_STATE_SHA256 is not None:
            DECODER_STATE_SHA256 = builder.CreateString(self.DECODER_STATE_SHA256)
        if self.DECODER_BUILD_ID is not None:
            DECODER_BUILD_ID = builder.CreateString(self.DECODER_BUILD_ID)
        if self.DECODER_STATE_MEDIA_TYPE is not None:
            DECODER_STATE_MEDIA_TYPE = builder.CreateString(self.DECODER_STATE_MEDIA_TYPE)
        if self.WINDOW is not None:
            if np is not None and type(self.WINDOW) is np.ndarray:
                WINDOW = builder.CreateNumpyVector(self.WINDOW)
            else:
                IRMDecodeContextStartWINDOWVector(builder, len(self.WINDOW))
                for i in reversed(range(len(self.WINDOW))):
                    builder.PrependUint8(self.WINDOW[i])
                WINDOW = builder.EndVector()
        if self.PARTIAL_RECORD is not None:
            if np is not None and type(self.PARTIAL_RECORD) is np.ndarray:
                PARTIAL_RECORD = builder.CreateNumpyVector(self.PARTIAL_RECORD)
            else:
                IRMDecodeContextStartPARTIAL_RECORDVector(builder, len(self.PARTIAL_RECORD))
                for i in reversed(range(len(self.PARTIAL_RECORD))):
                    builder.PrependUint8(self.PARTIAL_RECORD[i])
                PARTIAL_RECORD = builder.EndVector()
        IRMDecodeContextStart(builder)
        if self.FORMAT is not None:
            IRMDecodeContextAddFORMAT(builder, FORMAT)
        if self.HEADER_LINE is not None:
            IRMDecodeContextAddHEADER_LINE(builder, HEADER_LINE)
        IRMDecodeContextAddHEADER_BYTE_LENGTH(builder, self.HEADER_BYTE_LENGTH)
        if self.COLUMN_NAMES is not None:
            IRMDecodeContextAddCOLUMN_NAMES(builder, COLUMN_NAMES)
        if self.DELIMITER is not None:
            IRMDecodeContextAddDELIMITER(builder, DELIMITER)
        if self.QUOTE is not None:
            IRMDecodeContextAddQUOTE(builder, QUOTE)
        if self.ESCAPE is not None:
            IRMDecodeContextAddESCAPE(builder, ESCAPE)
        if self.LINE_TERMINATOR is not None:
            IRMDecodeContextAddLINE_TERMINATOR(builder, LINE_TERMINATOR)
        if self.CHARSET is not None:
            IRMDecodeContextAddCHARSET(builder, CHARSET)
        if self.COMPRESSION is not None:
            IRMDecodeContextAddCOMPRESSION(builder, COMPRESSION)
        IRMDecodeContextAddDECODED_OFFSET(builder, self.DECODED_OFFSET)
        if self.DECODER_STATE is not None:
            IRMDecodeContextAddDECODER_STATE(builder, DECODER_STATE)
        if self.DECODER_STATE_FORMAT is not None:
            IRMDecodeContextAddDECODER_STATE_FORMAT(builder, DECODER_STATE_FORMAT)
        if self.DECODER_STATE_VERSION is not None:
            IRMDecodeContextAddDECODER_STATE_VERSION(builder, DECODER_STATE_VERSION)
        IRMDecodeContextAddDECODER_STATE_BYTE_LENGTH(builder, self.DECODER_STATE_BYTE_LENGTH)
        if self.DECODER_STATE_SHA256 is not None:
            IRMDecodeContextAddDECODER_STATE_SHA256(builder, DECODER_STATE_SHA256)
        if self.DECODER_BUILD_ID is not None:
            IRMDecodeContextAddDECODER_BUILD_ID(builder, DECODER_BUILD_ID)
        if self.DECODER_STATE_MEDIA_TYPE is not None:
            IRMDecodeContextAddDECODER_STATE_MEDIA_TYPE(builder, DECODER_STATE_MEDIA_TYPE)
        if self.WINDOW is not None:
            IRMDecodeContextAddWINDOW(builder, WINDOW)
        IRMDecodeContextAddWINDOW_BYTE_LENGTH(builder, self.WINDOW_BYTE_LENGTH)
        IRMDecodeContextAddWINDOW_POSITION(builder, self.WINDOW_POSITION)
        if self.PARTIAL_RECORD is not None:
            IRMDecodeContextAddPARTIAL_RECORD(builder, PARTIAL_RECORD)
        IRMDecodeContext = IRMDecodeContextEnd(builder)
        return IRMDecodeContext
