# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One immutable asset or provider-mediated query contract published for a
# dataset update.
class DPMAsset(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DPMAsset()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDPMAsset(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DPMAssetBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x44\x50\x4D", size_prefixed=size_prefixed)

    # DPMAsset
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Asset role.
    # DPMAsset
    def ASSET_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 3

    # Transport profile for this asset. CONTENT_ADDRESS assets use CID and
    # MULTIFORMAT_ADDRESS. SDN_QUERY assets use TRANSPORT_PROTOCOL plus the
    # signed DPM query and root fields; they are not required to be published as
    # discoverable IPFS files.
    # DPMAsset
    def TRANSPORT_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Optional IPFS CIDv1/multihash content identifier. This field is required
    # for CONTENT_ADDRESS assets and SHOULD be empty for SDN_QUERY assets whose
    # bytes are retrieved through a provider protocol.
    # DPMAsset
    def CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Multiformat address. For CONTENT_ADDRESS this is usually /ipfs/{CID}. For
    # SDN_QUERY this MAY be a provider peer multiaddr, relay hint, or empty when
    # provider routing is resolved from the DPM provider identity.
    # DPMAsset
    def MULTIFORMAT_ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # File name or logical artifact name.
    # DPMAsset
    def FILE_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Canonical publication/update partition identity for this asset. FILE_ID is
    # not a display filename; it is the stable identifier used everywhere this
    # update is referenced: PNMs, DPMs, assets, manifests, entitlements, query
    # requests, subscriber caches, replay, audit, and completeness proofs.
    # Example:
    # celestrak:gp:OMM.fbs:2026-05-06T03:00:00Z.
    # DPMAsset
    def FILE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider protocol name/version used to fetch this asset when
    # TRANSPORT_KIND is SDN_QUERY, e.g. /sdn/dataset-query/1.0.0. The protocol
    # response MUST be verifiable against DATA_ROOT, INDEXES, QUERY, and the
    # provider signature in this DPM.
    # DPMAsset
    def TRANSPORT_PROTOCOL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Byte length of the published object.
    # DPMAsset
    def BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SHA-256 hash of the exact published bytes, lowercase hex.
    # DPMAsset
    def BYTE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Merkle root over canonical records in this asset, lowercase hex. For
    # provider-mediated query delivery, subscribers verify returned records and
    # proof paths against this root before importing data. The proof material is
    # carried by the provider query response, not by the DPM itself; this field
    # is the signed root that makes those proofs meaningful.
    # DPMAsset
    def DATA_ROOT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SDS schema name for data artifacts, e.g. OMM.fbs, CAT.fbs, SPW.fbs.
    # DPMAsset
    def SCHEMA_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Hash of the SDS schema used to encode this object.
    # DPMAsset
    def SCHEMA_HASH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Optional content-key identifier for encrypted artifacts.
    # DPMAsset
    def CONTENT_KEY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def DPMAssetStart(builder):
    builder.StartObject(13)

def Start(builder):
    DPMAssetStart(builder)

def DPMAssetAddASSET_KIND(builder, ASSET_KIND):
    builder.PrependInt8Slot(0, ASSET_KIND, 3)

def AddASSET_KIND(builder, ASSET_KIND):
    DPMAssetAddASSET_KIND(builder, ASSET_KIND)

def DPMAssetAddTRANSPORT_KIND(builder, TRANSPORT_KIND):
    builder.PrependInt8Slot(1, TRANSPORT_KIND, 0)

def AddTRANSPORT_KIND(builder, TRANSPORT_KIND):
    DPMAssetAddTRANSPORT_KIND(builder, TRANSPORT_KIND)

def DPMAssetAddCID(builder, CID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(CID), 0)

def AddCID(builder, CID):
    DPMAssetAddCID(builder, CID)

def DPMAssetAddMULTIFORMAT_ADDRESS(builder, MULTIFORMAT_ADDRESS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MULTIFORMAT_ADDRESS), 0)

def AddMULTIFORMAT_ADDRESS(builder, MULTIFORMAT_ADDRESS):
    DPMAssetAddMULTIFORMAT_ADDRESS(builder, MULTIFORMAT_ADDRESS)

def DPMAssetAddFILE_NAME(builder, FILE_NAME):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(FILE_NAME), 0)

def AddFILE_NAME(builder, FILE_NAME):
    DPMAssetAddFILE_NAME(builder, FILE_NAME)

def DPMAssetAddFILE_ID(builder, FILE_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(FILE_ID), 0)

def AddFILE_ID(builder, FILE_ID):
    DPMAssetAddFILE_ID(builder, FILE_ID)

def DPMAssetAddTRANSPORT_PROTOCOL(builder, TRANSPORT_PROTOCOL):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(TRANSPORT_PROTOCOL), 0)

def AddTRANSPORT_PROTOCOL(builder, TRANSPORT_PROTOCOL):
    DPMAssetAddTRANSPORT_PROTOCOL(builder, TRANSPORT_PROTOCOL)

def DPMAssetAddBYTE_LENGTH(builder, BYTE_LENGTH):
    builder.PrependUint64Slot(7, BYTE_LENGTH, 0)

def AddBYTE_LENGTH(builder, BYTE_LENGTH):
    DPMAssetAddBYTE_LENGTH(builder, BYTE_LENGTH)

def DPMAssetAddBYTE_SHA256(builder, BYTE_SHA256):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(BYTE_SHA256), 0)

def AddBYTE_SHA256(builder, BYTE_SHA256):
    DPMAssetAddBYTE_SHA256(builder, BYTE_SHA256)

def DPMAssetAddDATA_ROOT(builder, DATA_ROOT):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(DATA_ROOT), 0)

def AddDATA_ROOT(builder, DATA_ROOT):
    DPMAssetAddDATA_ROOT(builder, DATA_ROOT)

def DPMAssetAddSCHEMA_NAME(builder, SCHEMA_NAME):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_NAME), 0)

def AddSCHEMA_NAME(builder, SCHEMA_NAME):
    DPMAssetAddSCHEMA_NAME(builder, SCHEMA_NAME)

def DPMAssetAddSCHEMA_HASH(builder, SCHEMA_HASH):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_HASH), 0)

def AddSCHEMA_HASH(builder, SCHEMA_HASH):
    DPMAssetAddSCHEMA_HASH(builder, SCHEMA_HASH)

def DPMAssetAddCONTENT_KEY_ID(builder, CONTENT_KEY_ID):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT_KEY_ID), 0)

def AddCONTENT_KEY_ID(builder, CONTENT_KEY_ID):
    DPMAssetAddCONTENT_KEY_ID(builder, CONTENT_KEY_ID)

def DPMAssetEnd(builder):
    return builder.EndObject()

def End(builder):
    return DPMAssetEnd(builder)


class DPMAssetT(object):

    # DPMAssetT
    def __init__(
        self,
        ASSET_KIND = 3,
        TRANSPORT_KIND = 0,
        CID = None,
        MULTIFORMAT_ADDRESS = None,
        FILE_NAME = None,
        FILE_ID = None,
        TRANSPORT_PROTOCOL = None,
        BYTE_LENGTH = 0,
        BYTE_SHA256 = None,
        DATA_ROOT = None,
        SCHEMA_NAME = None,
        SCHEMA_HASH = None,
        CONTENT_KEY_ID = None,
    ):
        self.ASSET_KIND = ASSET_KIND  # type: int
        self.TRANSPORT_KIND = TRANSPORT_KIND  # type: int
        self.CID = CID  # type: Optional[str]
        self.MULTIFORMAT_ADDRESS = MULTIFORMAT_ADDRESS  # type: Optional[str]
        self.FILE_NAME = FILE_NAME  # type: Optional[str]
        self.FILE_ID = FILE_ID  # type: Optional[str]
        self.TRANSPORT_PROTOCOL = TRANSPORT_PROTOCOL  # type: Optional[str]
        self.BYTE_LENGTH = BYTE_LENGTH  # type: int
        self.BYTE_SHA256 = BYTE_SHA256  # type: Optional[str]
        self.DATA_ROOT = DATA_ROOT  # type: Optional[str]
        self.SCHEMA_NAME = SCHEMA_NAME  # type: Optional[str]
        self.SCHEMA_HASH = SCHEMA_HASH  # type: Optional[str]
        self.CONTENT_KEY_ID = CONTENT_KEY_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpDpmasset = DPMAsset()
        tmpDpmasset.Init(buf, pos)
        return cls.InitFromObj(tmpDpmasset)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpDpmasset):
        x = DPMAssetT()
        x._UnPack(tmpDpmasset)
        return x

    # DPMAssetT
    def _UnPack(self, DPMAsset):
        if DPMAsset is None:
            return
        self.ASSET_KIND = DPMAsset.ASSET_KIND()
        self.TRANSPORT_KIND = DPMAsset.TRANSPORT_KIND()
        self.CID = DPMAsset.CID()
        self.MULTIFORMAT_ADDRESS = DPMAsset.MULTIFORMAT_ADDRESS()
        self.FILE_NAME = DPMAsset.FILE_NAME()
        self.FILE_ID = DPMAsset.FILE_ID()
        self.TRANSPORT_PROTOCOL = DPMAsset.TRANSPORT_PROTOCOL()
        self.BYTE_LENGTH = DPMAsset.BYTE_LENGTH()
        self.BYTE_SHA256 = DPMAsset.BYTE_SHA256()
        self.DATA_ROOT = DPMAsset.DATA_ROOT()
        self.SCHEMA_NAME = DPMAsset.SCHEMA_NAME()
        self.SCHEMA_HASH = DPMAsset.SCHEMA_HASH()
        self.CONTENT_KEY_ID = DPMAsset.CONTENT_KEY_ID()

    # DPMAssetT
    def Pack(self, builder):
        if self.CID is not None:
            CID = builder.CreateString(self.CID)
        if self.MULTIFORMAT_ADDRESS is not None:
            MULTIFORMAT_ADDRESS = builder.CreateString(self.MULTIFORMAT_ADDRESS)
        if self.FILE_NAME is not None:
            FILE_NAME = builder.CreateString(self.FILE_NAME)
        if self.FILE_ID is not None:
            FILE_ID = builder.CreateString(self.FILE_ID)
        if self.TRANSPORT_PROTOCOL is not None:
            TRANSPORT_PROTOCOL = builder.CreateString(self.TRANSPORT_PROTOCOL)
        if self.BYTE_SHA256 is not None:
            BYTE_SHA256 = builder.CreateString(self.BYTE_SHA256)
        if self.DATA_ROOT is not None:
            DATA_ROOT = builder.CreateString(self.DATA_ROOT)
        if self.SCHEMA_NAME is not None:
            SCHEMA_NAME = builder.CreateString(self.SCHEMA_NAME)
        if self.SCHEMA_HASH is not None:
            SCHEMA_HASH = builder.CreateString(self.SCHEMA_HASH)
        if self.CONTENT_KEY_ID is not None:
            CONTENT_KEY_ID = builder.CreateString(self.CONTENT_KEY_ID)
        DPMAssetStart(builder)
        DPMAssetAddASSET_KIND(builder, self.ASSET_KIND)
        DPMAssetAddTRANSPORT_KIND(builder, self.TRANSPORT_KIND)
        if self.CID is not None:
            DPMAssetAddCID(builder, CID)
        if self.MULTIFORMAT_ADDRESS is not None:
            DPMAssetAddMULTIFORMAT_ADDRESS(builder, MULTIFORMAT_ADDRESS)
        if self.FILE_NAME is not None:
            DPMAssetAddFILE_NAME(builder, FILE_NAME)
        if self.FILE_ID is not None:
            DPMAssetAddFILE_ID(builder, FILE_ID)
        if self.TRANSPORT_PROTOCOL is not None:
            DPMAssetAddTRANSPORT_PROTOCOL(builder, TRANSPORT_PROTOCOL)
        DPMAssetAddBYTE_LENGTH(builder, self.BYTE_LENGTH)
        if self.BYTE_SHA256 is not None:
            DPMAssetAddBYTE_SHA256(builder, BYTE_SHA256)
        if self.DATA_ROOT is not None:
            DPMAssetAddDATA_ROOT(builder, DATA_ROOT)
        if self.SCHEMA_NAME is not None:
            DPMAssetAddSCHEMA_NAME(builder, SCHEMA_NAME)
        if self.SCHEMA_HASH is not None:
            DPMAssetAddSCHEMA_HASH(builder, SCHEMA_HASH)
        if self.CONTENT_KEY_ID is not None:
            DPMAssetAddCONTENT_KEY_ID(builder, CONTENT_KEY_ID)
        DPMAsset = DPMAssetEnd(builder)
        return DPMAsset
