# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# How one site's independent provider reports were reduced to the single
# published position, and how strongly those reports agreed.
class TBSConsensus(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TBSConsensus()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTBSConsensus(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TBSConsensusBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x42\x53", size_prefixed=size_prefixed)

    # TBSConsensus
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TBSConsensus
    def METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 5

    # Providers queried for this site, including those that returned nothing.
    # Never fewer than the number of SOURCES entries.
    # TBSConsensus
    def PROVIDERS_CONSULTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Providers whose reported position fell within the publisher's agreement
    # threshold. A value of 1 means the published position rests on a single
    # report and was never independently corroborated.
    # TBSConsensus
    def PROVIDERS_AGREEING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # PROVIDER_ID of the report the merge selected, copied verbatim from the
    # matching SOURCES entry. Absent when METHOD is CENTROID: a computed
    # position belongs to no single provider.
    # TBSConsensus
    def WINNING_PROVIDER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Greatest surface distance in metres between any two agreeing reported
    # positions. Zero is meaningful only when PROVIDERS_AGREEING is 1.
    # TBSConsensus
    def POSITION_SPREAD_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # The publisher's own confidence in the merged position, on [0,1]. Never a
    # provider's figure and never a probability from a model this standard does
    # not name.
    # TBSConsensus
    def CONFIDENCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFC 3339 UTC fixed-millisecond instant at which the merge was performed.
    # TBSConsensus
    def MERGED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TBSConsensusStart(builder):
    builder.StartObject(7)

def Start(builder):
    TBSConsensusStart(builder)

def TBSConsensusAddMETHOD(builder, METHOD):
    builder.PrependInt8Slot(0, METHOD, 5)

def AddMETHOD(builder, METHOD):
    TBSConsensusAddMETHOD(builder, METHOD)

def TBSConsensusAddPROVIDERS_CONSULTED(builder, PROVIDERS_CONSULTED):
    builder.PrependUint32Slot(1, PROVIDERS_CONSULTED, 0)

def AddPROVIDERS_CONSULTED(builder, PROVIDERS_CONSULTED):
    TBSConsensusAddPROVIDERS_CONSULTED(builder, PROVIDERS_CONSULTED)

def TBSConsensusAddPROVIDERS_AGREEING(builder, PROVIDERS_AGREEING):
    builder.PrependUint32Slot(2, PROVIDERS_AGREEING, 0)

def AddPROVIDERS_AGREEING(builder, PROVIDERS_AGREEING):
    TBSConsensusAddPROVIDERS_AGREEING(builder, PROVIDERS_AGREEING)

def TBSConsensusAddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(WINNING_PROVIDER_ID), 0)

def AddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID):
    TBSConsensusAddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID)

def TBSConsensusAddPOSITION_SPREAD_M(builder, POSITION_SPREAD_M):
    builder.PrependFloat64Slot(4, POSITION_SPREAD_M, 0.0)

def AddPOSITION_SPREAD_M(builder, POSITION_SPREAD_M):
    TBSConsensusAddPOSITION_SPREAD_M(builder, POSITION_SPREAD_M)

def TBSConsensusAddCONFIDENCE(builder, CONFIDENCE):
    builder.PrependFloat64Slot(5, CONFIDENCE, 0.0)

def AddCONFIDENCE(builder, CONFIDENCE):
    TBSConsensusAddCONFIDENCE(builder, CONFIDENCE)

def TBSConsensusAddMERGED_AT(builder, MERGED_AT):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(MERGED_AT), 0)

def AddMERGED_AT(builder, MERGED_AT):
    TBSConsensusAddMERGED_AT(builder, MERGED_AT)

def TBSConsensusEnd(builder):
    return builder.EndObject()

def End(builder):
    return TBSConsensusEnd(builder)


class TBSConsensusT(object):

    # TBSConsensusT
    def __init__(
        self,
        METHOD = 5,
        PROVIDERS_CONSULTED = 0,
        PROVIDERS_AGREEING = 0,
        WINNING_PROVIDER_ID = None,
        POSITION_SPREAD_M = 0.0,
        CONFIDENCE = 0.0,
        MERGED_AT = None,
    ):
        self.METHOD = METHOD  # type: int
        self.PROVIDERS_CONSULTED = PROVIDERS_CONSULTED  # type: int
        self.PROVIDERS_AGREEING = PROVIDERS_AGREEING  # type: int
        self.WINNING_PROVIDER_ID = WINNING_PROVIDER_ID  # type: Optional[str]
        self.POSITION_SPREAD_M = POSITION_SPREAD_M  # type: float
        self.CONFIDENCE = CONFIDENCE  # type: float
        self.MERGED_AT = MERGED_AT  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTbsconsensus = TBSConsensus()
        tmpTbsconsensus.Init(buf, pos)
        return cls.InitFromObj(tmpTbsconsensus)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTbsconsensus):
        x = TBSConsensusT()
        x._UnPack(tmpTbsconsensus)
        return x

    # TBSConsensusT
    def _UnPack(self, TBSConsensus):
        if TBSConsensus is None:
            return
        self.METHOD = TBSConsensus.METHOD()
        self.PROVIDERS_CONSULTED = TBSConsensus.PROVIDERS_CONSULTED()
        self.PROVIDERS_AGREEING = TBSConsensus.PROVIDERS_AGREEING()
        self.WINNING_PROVIDER_ID = TBSConsensus.WINNING_PROVIDER_ID()
        self.POSITION_SPREAD_M = TBSConsensus.POSITION_SPREAD_M()
        self.CONFIDENCE = TBSConsensus.CONFIDENCE()
        self.MERGED_AT = TBSConsensus.MERGED_AT()

    # TBSConsensusT
    def Pack(self, builder):
        if self.WINNING_PROVIDER_ID is not None:
            WINNING_PROVIDER_ID = builder.CreateString(self.WINNING_PROVIDER_ID)
        if self.MERGED_AT is not None:
            MERGED_AT = builder.CreateString(self.MERGED_AT)
        TBSConsensusStart(builder)
        TBSConsensusAddMETHOD(builder, self.METHOD)
        TBSConsensusAddPROVIDERS_CONSULTED(builder, self.PROVIDERS_CONSULTED)
        TBSConsensusAddPROVIDERS_AGREEING(builder, self.PROVIDERS_AGREEING)
        if self.WINNING_PROVIDER_ID is not None:
            TBSConsensusAddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID)
        TBSConsensusAddPOSITION_SPREAD_M(builder, self.POSITION_SPREAD_M)
        TBSConsensusAddCONFIDENCE(builder, self.CONFIDENCE)
        if self.MERGED_AT is not None:
            TBSConsensusAddMERGED_AT(builder, MERGED_AT)
        TBSConsensus = TBSConsensusEnd(builder)
        return TBSConsensus
