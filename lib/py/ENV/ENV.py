# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Atmosphere and Environment
class ENV(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ENV()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsENV(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ENVBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x4E\x56", size_prefixed=size_prefixed)

    # ENV
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ENV
    def ATMOSPHERE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ENV
    def WEATHER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ENV
    def TIME_UTC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def SUN_AZIMUTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def SUN_ELEVATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def MOON_PHASE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def ILLUMINATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def MAGNETIC_DECLINATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def MAGNETIC_INCLINATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # ENV
    def RESERVED(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # ENV
    def RESERVEDAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # ENV
    def RESERVEDLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ENV
    def RESERVEDIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

def ENVStart(builder):
    builder.StartObject(12)

def Start(builder):
    ENVStart(builder)

def ENVAddATMOSPHERE(builder, ATMOSPHERE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ATMOSPHERE), 0)

def AddATMOSPHERE(builder, ATMOSPHERE):
    ENVAddATMOSPHERE(builder, ATMOSPHERE)

def ENVAddWEATHER(builder, WEATHER):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(WEATHER), 0)

def AddWEATHER(builder, WEATHER):
    ENVAddWEATHER(builder, WEATHER)

def ENVAddTIME_UTC(builder, TIME_UTC):
    builder.PrependFloat64Slot(2, TIME_UTC, 0.0)

def AddTIME_UTC(builder, TIME_UTC):
    ENVAddTIME_UTC(builder, TIME_UTC)

def ENVAddLATITUDE(builder, LATITUDE):
    builder.PrependFloat64Slot(3, LATITUDE, 0.0)

def AddLATITUDE(builder, LATITUDE):
    ENVAddLATITUDE(builder, LATITUDE)

def ENVAddLONGITUDE(builder, LONGITUDE):
    builder.PrependFloat64Slot(4, LONGITUDE, 0.0)

def AddLONGITUDE(builder, LONGITUDE):
    ENVAddLONGITUDE(builder, LONGITUDE)

def ENVAddSUN_AZIMUTH(builder, SUN_AZIMUTH):
    builder.PrependFloat32Slot(5, SUN_AZIMUTH, 0.0)

def AddSUN_AZIMUTH(builder, SUN_AZIMUTH):
    ENVAddSUN_AZIMUTH(builder, SUN_AZIMUTH)

def ENVAddSUN_ELEVATION(builder, SUN_ELEVATION):
    builder.PrependFloat32Slot(6, SUN_ELEVATION, 0.0)

def AddSUN_ELEVATION(builder, SUN_ELEVATION):
    ENVAddSUN_ELEVATION(builder, SUN_ELEVATION)

def ENVAddMOON_PHASE(builder, MOON_PHASE):
    builder.PrependFloat32Slot(7, MOON_PHASE, 0.0)

def AddMOON_PHASE(builder, MOON_PHASE):
    ENVAddMOON_PHASE(builder, MOON_PHASE)

def ENVAddILLUMINATION(builder, ILLUMINATION):
    builder.PrependFloat32Slot(8, ILLUMINATION, 0.0)

def AddILLUMINATION(builder, ILLUMINATION):
    ENVAddILLUMINATION(builder, ILLUMINATION)

def ENVAddMAGNETIC_DECLINATION(builder, MAGNETIC_DECLINATION):
    builder.PrependFloat32Slot(9, MAGNETIC_DECLINATION, 0.0)

def AddMAGNETIC_DECLINATION(builder, MAGNETIC_DECLINATION):
    ENVAddMAGNETIC_DECLINATION(builder, MAGNETIC_DECLINATION)

def ENVAddMAGNETIC_INCLINATION(builder, MAGNETIC_INCLINATION):
    builder.PrependFloat32Slot(10, MAGNETIC_INCLINATION, 0.0)

def AddMAGNETIC_INCLINATION(builder, MAGNETIC_INCLINATION):
    ENVAddMAGNETIC_INCLINATION(builder, MAGNETIC_INCLINATION)

def ENVAddRESERVED(builder, RESERVED):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(RESERVED), 0)

def AddRESERVED(builder, RESERVED):
    ENVAddRESERVED(builder, RESERVED)

def ENVStartRESERVEDVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartRESERVEDVector(builder, numElems):
    return ENVStartRESERVEDVector(builder, numElems)

def ENVEnd(builder):
    return builder.EndObject()

def End(builder):
    return ENVEnd(builder)

try:
    from typing import List
except:
    pass

class ENVT(object):

    # ENVT
    def __init__(self):
        self.ATMOSPHERE = None  # type: str
        self.WEATHER = None  # type: str
        self.TIME_UTC = 0.0  # type: float
        self.LATITUDE = 0.0  # type: float
        self.LONGITUDE = 0.0  # type: float
        self.SUN_AZIMUTH = 0.0  # type: float
        self.SUN_ELEVATION = 0.0  # type: float
        self.MOON_PHASE = 0.0  # type: float
        self.ILLUMINATION = 0.0  # type: float
        self.MAGNETIC_DECLINATION = 0.0  # type: float
        self.MAGNETIC_INCLINATION = 0.0  # type: float
        self.RESERVED = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ENV = ENV()
        ENV.Init(buf, pos)
        return cls.InitFromObj(ENV)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ENV):
        x = ENVT()
        x._UnPack(ENV)
        return x

    # ENVT
    def _UnPack(self, ENV):
        if ENV is None:
            return
        self.ATMOSPHERE = ENV.ATMOSPHERE()
        self.WEATHER = ENV.WEATHER()
        self.TIME_UTC = ENV.TIME_UTC()
        self.LATITUDE = ENV.LATITUDE()
        self.LONGITUDE = ENV.LONGITUDE()
        self.SUN_AZIMUTH = ENV.SUN_AZIMUTH()
        self.SUN_ELEVATION = ENV.SUN_ELEVATION()
        self.MOON_PHASE = ENV.MOON_PHASE()
        self.ILLUMINATION = ENV.ILLUMINATION()
        self.MAGNETIC_DECLINATION = ENV.MAGNETIC_DECLINATION()
        self.MAGNETIC_INCLINATION = ENV.MAGNETIC_INCLINATION()
        if not ENV.RESERVEDIsNone():
            if np is None:
                self.RESERVED = []
                for i in range(ENV.RESERVEDLength()):
                    self.RESERVED.append(ENV.RESERVED(i))
            else:
                self.RESERVED = ENV.RESERVEDAsNumpy()

    # ENVT
    def Pack(self, builder):
        if self.ATMOSPHERE is not None:
            ATMOSPHERE = builder.CreateString(self.ATMOSPHERE)
        if self.WEATHER is not None:
            WEATHER = builder.CreateString(self.WEATHER)
        if self.RESERVED is not None:
            if np is not None and type(self.RESERVED) is np.ndarray:
                RESERVED = builder.CreateNumpyVector(self.RESERVED)
            else:
                ENVStartRESERVEDVector(builder, len(self.RESERVED))
                for i in reversed(range(len(self.RESERVED))):
                    builder.PrependUint8(self.RESERVED[i])
                RESERVED = builder.EndVector()
        ENVStart(builder)
        if self.ATMOSPHERE is not None:
            ENVAddATMOSPHERE(builder, ATMOSPHERE)
        if self.WEATHER is not None:
            ENVAddWEATHER(builder, WEATHER)
        ENVAddTIME_UTC(builder, self.TIME_UTC)
        ENVAddLATITUDE(builder, self.LATITUDE)
        ENVAddLONGITUDE(builder, self.LONGITUDE)
        ENVAddSUN_AZIMUTH(builder, self.SUN_AZIMUTH)
        ENVAddSUN_ELEVATION(builder, self.SUN_ELEVATION)
        ENVAddMOON_PHASE(builder, self.MOON_PHASE)
        ENVAddILLUMINATION(builder, self.ILLUMINATION)
        ENVAddMAGNETIC_DECLINATION(builder, self.MAGNETIC_DECLINATION)
        ENVAddMAGNETIC_INCLINATION(builder, self.MAGNETIC_INCLINATION)
        if self.RESERVED is not None:
            ENVAddRESERVED(builder, RESERVED)
        ENV = ENVEnd(builder)
        return ENV
