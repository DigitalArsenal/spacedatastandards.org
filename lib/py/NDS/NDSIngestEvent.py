# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One source-ingest transition surfaced to the dashboard.
class NDSIngestEvent(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NDSIngestEvent()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNDSIngestEvent(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NDSIngestEventBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x44\x53", size_prefixed=size_prefixed)

    # NDSIngestEvent
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # NDSIngestEvent
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # NDSIngestEvent
    def SCHEMA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # NDSIngestEvent
    def PROVIDER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # NDSIngestEvent
    def SOURCE_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # NDSIngestEvent
    def MESSAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # NDSIngestEvent
    def COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # Unix seconds when the transition occurred.
    # NDSIngestEvent
    def AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

def NDSIngestEventStart(builder):
    builder.StartObject(7)

def Start(builder):
    NDSIngestEventStart(builder)

def NDSIngestEventAddKIND(builder, KIND):
    builder.PrependInt8Slot(0, KIND, 0)

def AddKIND(builder, KIND):
    NDSIngestEventAddKIND(builder, KIND)

def NDSIngestEventAddSCHEMA(builder, SCHEMA):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA), 0)

def AddSCHEMA(builder, SCHEMA):
    NDSIngestEventAddSCHEMA(builder, SCHEMA)

def NDSIngestEventAddPROVIDER_ID(builder, PROVIDER_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_ID), 0)

def AddPROVIDER_ID(builder, PROVIDER_ID):
    NDSIngestEventAddPROVIDER_ID(builder, PROVIDER_ID)

def NDSIngestEventAddSOURCE_NAME(builder, SOURCE_NAME):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_NAME), 0)

def AddSOURCE_NAME(builder, SOURCE_NAME):
    NDSIngestEventAddSOURCE_NAME(builder, SOURCE_NAME)

def NDSIngestEventAddMESSAGE(builder, MESSAGE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(MESSAGE), 0)

def AddMESSAGE(builder, MESSAGE):
    NDSIngestEventAddMESSAGE(builder, MESSAGE)

def NDSIngestEventAddCOUNT(builder, COUNT):
    builder.PrependInt64Slot(5, COUNT, 0)

def AddCOUNT(builder, COUNT):
    NDSIngestEventAddCOUNT(builder, COUNT)

def NDSIngestEventAddAT(builder, AT):
    builder.PrependInt64Slot(6, AT, 0)

def AddAT(builder, AT):
    NDSIngestEventAddAT(builder, AT)

def NDSIngestEventEnd(builder):
    return builder.EndObject()

def End(builder):
    return NDSIngestEventEnd(builder)


class NDSIngestEventT(object):

    # NDSIngestEventT
    def __init__(
        self,
        KIND = 0,
        SCHEMA = None,
        PROVIDER_ID = None,
        SOURCE_NAME = None,
        MESSAGE = None,
        COUNT = 0,
        AT = 0,
    ):
        self.KIND = KIND  # type: int
        self.SCHEMA = SCHEMA  # type: Optional[str]
        self.PROVIDER_ID = PROVIDER_ID  # type: Optional[str]
        self.SOURCE_NAME = SOURCE_NAME  # type: Optional[str]
        self.MESSAGE = MESSAGE  # type: Optional[str]
        self.COUNT = COUNT  # type: int
        self.AT = AT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNdsingestEvent = NDSIngestEvent()
        tmpNdsingestEvent.Init(buf, pos)
        return cls.InitFromObj(tmpNdsingestEvent)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNdsingestEvent):
        x = NDSIngestEventT()
        x._UnPack(tmpNdsingestEvent)
        return x

    # NDSIngestEventT
    def _UnPack(self, NDSIngestEvent):
        if NDSIngestEvent is None:
            return
        self.KIND = NDSIngestEvent.KIND()
        self.SCHEMA = NDSIngestEvent.SCHEMA()
        self.PROVIDER_ID = NDSIngestEvent.PROVIDER_ID()
        self.SOURCE_NAME = NDSIngestEvent.SOURCE_NAME()
        self.MESSAGE = NDSIngestEvent.MESSAGE()
        self.COUNT = NDSIngestEvent.COUNT()
        self.AT = NDSIngestEvent.AT()

    # NDSIngestEventT
    def Pack(self, builder):
        if self.SCHEMA is not None:
            SCHEMA = builder.CreateString(self.SCHEMA)
        if self.PROVIDER_ID is not None:
            PROVIDER_ID = builder.CreateString(self.PROVIDER_ID)
        if self.SOURCE_NAME is not None:
            SOURCE_NAME = builder.CreateString(self.SOURCE_NAME)
        if self.MESSAGE is not None:
            MESSAGE = builder.CreateString(self.MESSAGE)
        NDSIngestEventStart(builder)
        NDSIngestEventAddKIND(builder, self.KIND)
        if self.SCHEMA is not None:
            NDSIngestEventAddSCHEMA(builder, SCHEMA)
        if self.PROVIDER_ID is not None:
            NDSIngestEventAddPROVIDER_ID(builder, PROVIDER_ID)
        if self.SOURCE_NAME is not None:
            NDSIngestEventAddSOURCE_NAME(builder, SOURCE_NAME)
        if self.MESSAGE is not None:
            NDSIngestEventAddMESSAGE(builder, MESSAGE)
        NDSIngestEventAddCOUNT(builder, self.COUNT)
        NDSIngestEventAddAT(builder, self.AT)
        NDSIngestEvent = NDSIngestEventEnd(builder)
        return NDSIngestEvent
