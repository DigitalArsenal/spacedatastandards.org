# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Counters for the node's trust rule evaluation engine.
class NDSTrustEngineStat(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NDSTrustEngineStat()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNDSTrustEngineStat(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NDSTrustEngineStatBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x44\x53", size_prefixed=size_prefixed)

    # NDSTrustEngineStat
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Minimum interval between evaluation passes, milliseconds.
    # NDSTrustEngineStat
    def MIN_INTERVAL_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # NDSTrustEngineStat
    def RUNS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # Unix seconds; 0 = never.
    # NDSTrustEngineStat
    def LAST_RUN_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

def NDSTrustEngineStatStart(builder):
    builder.StartObject(3)

def Start(builder):
    NDSTrustEngineStatStart(builder)

def NDSTrustEngineStatAddMIN_INTERVAL_MS(builder, MIN_INTERVAL_MS):
    builder.PrependInt64Slot(0, MIN_INTERVAL_MS, 0)

def AddMIN_INTERVAL_MS(builder, MIN_INTERVAL_MS):
    NDSTrustEngineStatAddMIN_INTERVAL_MS(builder, MIN_INTERVAL_MS)

def NDSTrustEngineStatAddRUNS(builder, RUNS):
    builder.PrependInt64Slot(1, RUNS, 0)

def AddRUNS(builder, RUNS):
    NDSTrustEngineStatAddRUNS(builder, RUNS)

def NDSTrustEngineStatAddLAST_RUN_AT(builder, LAST_RUN_AT):
    builder.PrependInt64Slot(2, LAST_RUN_AT, 0)

def AddLAST_RUN_AT(builder, LAST_RUN_AT):
    NDSTrustEngineStatAddLAST_RUN_AT(builder, LAST_RUN_AT)

def NDSTrustEngineStatEnd(builder):
    return builder.EndObject()

def End(builder):
    return NDSTrustEngineStatEnd(builder)


class NDSTrustEngineStatT(object):

    # NDSTrustEngineStatT
    def __init__(
        self,
        MIN_INTERVAL_MS = 0,
        RUNS = 0,
        LAST_RUN_AT = 0,
    ):
        self.MIN_INTERVAL_MS = MIN_INTERVAL_MS  # type: int
        self.RUNS = RUNS  # type: int
        self.LAST_RUN_AT = LAST_RUN_AT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNdstrustEngineStat = NDSTrustEngineStat()
        tmpNdstrustEngineStat.Init(buf, pos)
        return cls.InitFromObj(tmpNdstrustEngineStat)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNdstrustEngineStat):
        x = NDSTrustEngineStatT()
        x._UnPack(tmpNdstrustEngineStat)
        return x

    # NDSTrustEngineStatT
    def _UnPack(self, NDSTrustEngineStat):
        if NDSTrustEngineStat is None:
            return
        self.MIN_INTERVAL_MS = NDSTrustEngineStat.MIN_INTERVAL_MS()
        self.RUNS = NDSTrustEngineStat.RUNS()
        self.LAST_RUN_AT = NDSTrustEngineStat.LAST_RUN_AT()

    # NDSTrustEngineStatT
    def Pack(self, builder):
        NDSTrustEngineStatStart(builder)
        NDSTrustEngineStatAddMIN_INTERVAL_MS(builder, self.MIN_INTERVAL_MS)
        NDSTrustEngineStatAddRUNS(builder, self.RUNS)
        NDSTrustEngineStatAddLAST_RUN_AT(builder, self.LAST_RUN_AT)
        NDSTrustEngineStat = NDSTrustEngineStatEnd(builder)
        return NDSTrustEngineStat
