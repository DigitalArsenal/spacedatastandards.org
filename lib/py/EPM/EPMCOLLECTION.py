# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Collection of Entity Profile Messages
class EPMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EPMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEPMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def EPMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x50\x4D", size_prefixed=size_prefixed)

    # EPMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Records of Entity Profile Messages
    # EPMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from EPM import EPM
            obj = EPM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # EPMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EPMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def EPMCOLLECTIONStart(builder): builder.StartObject(1)
def Start(builder):
    return EPMCOLLECTIONStart(builder)
def EPMCOLLECTIONAddRECORDS(builder, RECORDS): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)
def AddRECORDS(builder, RECORDS):
    return EPMCOLLECTIONAddRECORDS(builder, RECORDS)
def EPMCOLLECTIONStartRECORDSVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartRECORDSVector(builder, numElems):
    return EPMCOLLECTIONStartRECORDSVector(builder, numElems)
def EPMCOLLECTIONEnd(builder): return builder.EndObject()
def End(builder):
    return EPMCOLLECTIONEnd(builder)
import EPM
try:
    from typing import List
except:
    pass

class EPMCOLLECTIONT(object):

    # EPMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[EPM.EPMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        EPMCOLLECTION = EPMCOLLECTION()
        EPMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(EPMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, EPMCOLLECTION):
        x = EPMCOLLECTIONT()
        x._UnPack(EPMCOLLECTION)
        return x

    # EPMCOLLECTIONT
    def _UnPack(self, EPMCOLLECTION):
        if EPMCOLLECTION is None:
            return
        if not EPMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(EPMCOLLECTION.RECORDSLength()):
                if EPMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    ePM_ = EPM.EPMT.InitFromObj(EPMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(ePM_)

    # EPMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            EPMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        EPMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            EPMCOLLECTIONAddRECORDS(builder, RECORDS)
        EPMCOLLECTION = EPMCOLLECTIONEnd(builder)
        return EPMCOLLECTION
