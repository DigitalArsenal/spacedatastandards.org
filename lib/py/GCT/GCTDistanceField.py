# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A signed distance field of a variant's surface (the isosurface of the
# mixture at DENSITY_THRESHOLD), on a cube of N^3 voxels over the variant's
# bounds, stored as bytes: distance / DISTANCE_SCALE_M, negative inside,
# clamped to [-128, 127]. Optional; a consumer without it sphere-traces
# nothing and lights the variant from an atlas instead.
class GCTDistanceField(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = GCTDistanceField()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsGCTDistanceField(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def GCTDistanceFieldBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x47\x43\x54", size_prefixed=size_prefixed)

    # GCTDistanceField
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Voxels per axis.
    # GCTDistanceField
    def N(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Origin of voxel (0, 0, 0) in the variant frame, metres (3 values).
    # GCTDistanceField
    def ORIGIN_M(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # GCTDistanceField
    def ORIGIN_MAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # GCTDistanceField
    def ORIGIN_MLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTDistanceField
    def ORIGIN_MIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Extent of the cube along each axis, metres (3 values).
    # GCTDistanceField
    def SPAN_M(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # GCTDistanceField
    def SPAN_MAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # GCTDistanceField
    def SPAN_MLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTDistanceField
    def SPAN_MIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Metres per byte step of DATA.
    # GCTDistanceField
    def DISTANCE_SCALE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Density threshold, in AMPLITUDE_UNITS, that defines the surface.
    # GCTDistanceField
    def DENSITY_THRESHOLD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Mean amplitude over the occupied volume, AMPLITUDE_UNITS.
    # GCTDistanceField
    def MEAN_AMPLITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # N^3 signed bytes, x fastest, then y, then z. Absent when the field
    # travels as the chunk named by CHUNK_CID.
    # GCTDistanceField
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # GCTDistanceField
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int8Flags, o)
        return 0

    # GCTDistanceField
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTDistanceField
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Content identifier of the field when DATA is absent.
    # GCTDistanceField
    def CHUNK_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def GCTDistanceFieldStart(builder):
    builder.StartObject(8)

def Start(builder):
    GCTDistanceFieldStart(builder)

def GCTDistanceFieldAddN(builder, N):
    builder.PrependUint16Slot(0, N, 0)

def AddN(builder, N):
    GCTDistanceFieldAddN(builder, N)

def GCTDistanceFieldAddORIGIN_M(builder, ORIGIN_M):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ORIGIN_M), 0)

def AddORIGIN_M(builder, ORIGIN_M):
    GCTDistanceFieldAddORIGIN_M(builder, ORIGIN_M)

def GCTDistanceFieldStartORIGIN_MVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartORIGIN_MVector(builder, numElems):
    return GCTDistanceFieldStartORIGIN_MVector(builder, numElems)

def GCTDistanceFieldCreateORIGIN_MVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateORIGIN_MVector(builder, data):
    GCTDistanceFieldCreateORIGIN_MVector(builder, data)

def GCTDistanceFieldAddSPAN_M(builder, SPAN_M):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SPAN_M), 0)

def AddSPAN_M(builder, SPAN_M):
    GCTDistanceFieldAddSPAN_M(builder, SPAN_M)

def GCTDistanceFieldStartSPAN_MVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSPAN_MVector(builder, numElems):
    return GCTDistanceFieldStartSPAN_MVector(builder, numElems)

def GCTDistanceFieldCreateSPAN_MVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateSPAN_MVector(builder, data):
    GCTDistanceFieldCreateSPAN_MVector(builder, data)

def GCTDistanceFieldAddDISTANCE_SCALE_M(builder, DISTANCE_SCALE_M):
    builder.PrependFloat32Slot(3, DISTANCE_SCALE_M, 0.0)

def AddDISTANCE_SCALE_M(builder, DISTANCE_SCALE_M):
    GCTDistanceFieldAddDISTANCE_SCALE_M(builder, DISTANCE_SCALE_M)

def GCTDistanceFieldAddDENSITY_THRESHOLD(builder, DENSITY_THRESHOLD):
    builder.PrependFloat32Slot(4, DENSITY_THRESHOLD, 0.0)

def AddDENSITY_THRESHOLD(builder, DENSITY_THRESHOLD):
    GCTDistanceFieldAddDENSITY_THRESHOLD(builder, DENSITY_THRESHOLD)

def GCTDistanceFieldAddMEAN_AMPLITUDE(builder, MEAN_AMPLITUDE):
    builder.PrependFloat32Slot(5, MEAN_AMPLITUDE, 0.0)

def AddMEAN_AMPLITUDE(builder, MEAN_AMPLITUDE):
    GCTDistanceFieldAddMEAN_AMPLITUDE(builder, MEAN_AMPLITUDE)

def GCTDistanceFieldAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    GCTDistanceFieldAddDATA(builder, DATA)

def GCTDistanceFieldStartDATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDATAVector(builder, numElems):
    return GCTDistanceFieldStartDATAVector(builder, numElems)

def GCTDistanceFieldCreateDATAVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependInt8(item)
    return builder.EndVector()

def CreateDATAVector(builder, data):
    GCTDistanceFieldCreateDATAVector(builder, data)

def GCTDistanceFieldAddCHUNK_CID(builder, CHUNK_CID):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(CHUNK_CID), 0)

def AddCHUNK_CID(builder, CHUNK_CID):
    GCTDistanceFieldAddCHUNK_CID(builder, CHUNK_CID)

def GCTDistanceFieldEnd(builder):
    return builder.EndObject()

def End(builder):
    return GCTDistanceFieldEnd(builder)

try:
    from typing import List
except:
    pass

class GCTDistanceFieldT(object):

    # GCTDistanceFieldT
    def __init__(
        self,
        N = 0,
        ORIGIN_M = None,
        SPAN_M = None,
        DISTANCE_SCALE_M = 0.0,
        DENSITY_THRESHOLD = 0.0,
        MEAN_AMPLITUDE = 0.0,
        DATA = None,
        CHUNK_CID = None,
    ):
        self.N = N  # type: int
        self.ORIGIN_M = ORIGIN_M  # type: Optional[List[float]]
        self.SPAN_M = SPAN_M  # type: Optional[List[float]]
        self.DISTANCE_SCALE_M = DISTANCE_SCALE_M  # type: float
        self.DENSITY_THRESHOLD = DENSITY_THRESHOLD  # type: float
        self.MEAN_AMPLITUDE = MEAN_AMPLITUDE  # type: float
        self.DATA = DATA  # type: Optional[List[int]]
        self.CHUNK_CID = CHUNK_CID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpGctdistanceField = GCTDistanceField()
        tmpGctdistanceField.Init(buf, pos)
        return cls.InitFromObj(tmpGctdistanceField)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpGctdistanceField):
        x = GCTDistanceFieldT()
        x._UnPack(tmpGctdistanceField)
        return x

    # GCTDistanceFieldT
    def _UnPack(self, GCTDistanceField):
        if GCTDistanceField is None:
            return
        self.N = GCTDistanceField.N()
        if not GCTDistanceField.ORIGIN_MIsNone():
            if np is None:
                self.ORIGIN_M = []
                for i in range(GCTDistanceField.ORIGIN_MLength()):
                    self.ORIGIN_M.append(GCTDistanceField.ORIGIN_M(i))
            else:
                self.ORIGIN_M = GCTDistanceField.ORIGIN_MAsNumpy()
        if not GCTDistanceField.SPAN_MIsNone():
            if np is None:
                self.SPAN_M = []
                for i in range(GCTDistanceField.SPAN_MLength()):
                    self.SPAN_M.append(GCTDistanceField.SPAN_M(i))
            else:
                self.SPAN_M = GCTDistanceField.SPAN_MAsNumpy()
        self.DISTANCE_SCALE_M = GCTDistanceField.DISTANCE_SCALE_M()
        self.DENSITY_THRESHOLD = GCTDistanceField.DENSITY_THRESHOLD()
        self.MEAN_AMPLITUDE = GCTDistanceField.MEAN_AMPLITUDE()
        if not GCTDistanceField.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(GCTDistanceField.DATALength()):
                    self.DATA.append(GCTDistanceField.DATA(i))
            else:
                self.DATA = GCTDistanceField.DATAAsNumpy()
        self.CHUNK_CID = GCTDistanceField.CHUNK_CID()

    # GCTDistanceFieldT
    def Pack(self, builder):
        if self.ORIGIN_M is not None:
            if np is not None and type(self.ORIGIN_M) is np.ndarray:
                ORIGIN_M = builder.CreateNumpyVector(self.ORIGIN_M)
            else:
                GCTDistanceFieldStartORIGIN_MVector(builder, len(self.ORIGIN_M))
                for i in reversed(range(len(self.ORIGIN_M))):
                    builder.PrependFloat32(self.ORIGIN_M[i])
                ORIGIN_M = builder.EndVector()
        if self.SPAN_M is not None:
            if np is not None and type(self.SPAN_M) is np.ndarray:
                SPAN_M = builder.CreateNumpyVector(self.SPAN_M)
            else:
                GCTDistanceFieldStartSPAN_MVector(builder, len(self.SPAN_M))
                for i in reversed(range(len(self.SPAN_M))):
                    builder.PrependFloat32(self.SPAN_M[i])
                SPAN_M = builder.EndVector()
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                GCTDistanceFieldStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependByte(self.DATA[i])
                DATA = builder.EndVector()
        if self.CHUNK_CID is not None:
            CHUNK_CID = builder.CreateString(self.CHUNK_CID)
        GCTDistanceFieldStart(builder)
        GCTDistanceFieldAddN(builder, self.N)
        if self.ORIGIN_M is not None:
            GCTDistanceFieldAddORIGIN_M(builder, ORIGIN_M)
        if self.SPAN_M is not None:
            GCTDistanceFieldAddSPAN_M(builder, SPAN_M)
        GCTDistanceFieldAddDISTANCE_SCALE_M(builder, self.DISTANCE_SCALE_M)
        GCTDistanceFieldAddDENSITY_THRESHOLD(builder, self.DENSITY_THRESHOLD)
        GCTDistanceFieldAddMEAN_AMPLITUDE(builder, self.MEAN_AMPLITUDE)
        if self.DATA is not None:
            GCTDistanceFieldAddDATA(builder, DATA)
        if self.CHUNK_CID is not None:
            GCTDistanceFieldAddCHUNK_CID(builder, CHUNK_CID)
        GCTDistanceField = GCTDistanceFieldEnd(builder)
        return GCTDistanceField
