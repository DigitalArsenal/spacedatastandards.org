# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One variant: one cloud (or one tile's merged mixture) as a chain of
# levels of detail in its own frame. Instances place variants in the tile.
class GCTVariant(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = GCTVariant()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsGCTVariant(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def GCTVariantBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x47\x43\x54", size_prefixed=size_prefixed)

    # GCTVariant
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Producer-stable identifier of the variant within FIELD_ID.
    # GCTVariant
    def VARIANT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Where the levels live.
    # GCTVariant
    def SCOPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Content identifier of the variant's levels when SCOPE is
    # SHARED_LIBRARY. Two references with equal VARIANT_CID are the same
    # bytes.
    # GCTVariant
    def VARIANT_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Genus, where classified.
    # GCTVariant
    def GENUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Levels of detail, finest first, each conserving the mass of the one
    # before it. Absent when SCOPE is SHARED_LIBRARY.
    # GCTVariant
    def LEVELS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from GCTLevel import GCTLevel
            obj = GCTLevel()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # GCTVariant
    def LEVELSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTVariant
    def LEVELSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Axis-aligned bounds of the finest level's support in the variant
    # frame, metres: min x, y, z (3 values).
    # GCTVariant
    def BOUNDS_MIN_M(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # GCTVariant
    def BOUNDS_MIN_MAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # GCTVariant
    def BOUNDS_MIN_MLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTVariant
    def BOUNDS_MIN_MIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Axis-aligned bounds, max x, y, z (3 values).
    # GCTVariant
    def BOUNDS_MAX_M(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # GCTVariant
    def BOUNDS_MAX_MAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # GCTVariant
    def BOUNDS_MAX_MLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTVariant
    def BOUNDS_MAX_MIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Total mass of the finest level: the integral of amplitude over space,
    # AMPLITUDE_UNITS times cubic metres. Every level MUST match it within
    # MASS_TOLERANCE.
    # GCTVariant
    def MASS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Signed distance field of the surface. Optional.
    # GCTVariant
    def DISTANCE_FIELD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from GCTDistanceField import GCTDistanceField
            obj = GCTDistanceField()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Baked density volume for ray marching. Optional.
    # GCTVariant
    def DENSITY_VOLUME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from GCTDensityVolume import GCTDensityVolume
            obj = GCTDensityVolume()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Seed the producer authored the variant from, for reproducibility.
    # GCTVariant
    def AUTHORING_SEED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def GCTVariantStart(builder):
    builder.StartObject(11)

def Start(builder):
    GCTVariantStart(builder)

def GCTVariantAddVARIANT_ID(builder, VARIANT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(VARIANT_ID), 0)

def AddVARIANT_ID(builder, VARIANT_ID):
    GCTVariantAddVARIANT_ID(builder, VARIANT_ID)

def GCTVariantAddSCOPE(builder, SCOPE):
    builder.PrependInt8Slot(1, SCOPE, 0)

def AddSCOPE(builder, SCOPE):
    GCTVariantAddSCOPE(builder, SCOPE)

def GCTVariantAddVARIANT_CID(builder, VARIANT_CID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VARIANT_CID), 0)

def AddVARIANT_CID(builder, VARIANT_CID):
    GCTVariantAddVARIANT_CID(builder, VARIANT_CID)

def GCTVariantAddGENUS(builder, GENUS):
    builder.PrependInt8Slot(3, GENUS, 0)

def AddGENUS(builder, GENUS):
    GCTVariantAddGENUS(builder, GENUS)

def GCTVariantAddLEVELS(builder, LEVELS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(LEVELS), 0)

def AddLEVELS(builder, LEVELS):
    GCTVariantAddLEVELS(builder, LEVELS)

def GCTVariantStartLEVELSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartLEVELSVector(builder, numElems):
    return GCTVariantStartLEVELSVector(builder, numElems)

def GCTVariantCreateLEVELSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateLEVELSVector(builder, data):
    GCTVariantCreateLEVELSVector(builder, data)

def GCTVariantAddBOUNDS_MIN_M(builder, BOUNDS_MIN_M):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(BOUNDS_MIN_M), 0)

def AddBOUNDS_MIN_M(builder, BOUNDS_MIN_M):
    GCTVariantAddBOUNDS_MIN_M(builder, BOUNDS_MIN_M)

def GCTVariantStartBOUNDS_MIN_MVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartBOUNDS_MIN_MVector(builder, numElems):
    return GCTVariantStartBOUNDS_MIN_MVector(builder, numElems)

def GCTVariantCreateBOUNDS_MIN_MVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateBOUNDS_MIN_MVector(builder, data):
    GCTVariantCreateBOUNDS_MIN_MVector(builder, data)

def GCTVariantAddBOUNDS_MAX_M(builder, BOUNDS_MAX_M):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(BOUNDS_MAX_M), 0)

def AddBOUNDS_MAX_M(builder, BOUNDS_MAX_M):
    GCTVariantAddBOUNDS_MAX_M(builder, BOUNDS_MAX_M)

def GCTVariantStartBOUNDS_MAX_MVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartBOUNDS_MAX_MVector(builder, numElems):
    return GCTVariantStartBOUNDS_MAX_MVector(builder, numElems)

def GCTVariantCreateBOUNDS_MAX_MVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateBOUNDS_MAX_MVector(builder, data):
    GCTVariantCreateBOUNDS_MAX_MVector(builder, data)

def GCTVariantAddMASS(builder, MASS):
    builder.PrependFloat64Slot(7, MASS, 0.0)

def AddMASS(builder, MASS):
    GCTVariantAddMASS(builder, MASS)

def GCTVariantAddDISTANCE_FIELD(builder, DISTANCE_FIELD):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(DISTANCE_FIELD), 0)

def AddDISTANCE_FIELD(builder, DISTANCE_FIELD):
    GCTVariantAddDISTANCE_FIELD(builder, DISTANCE_FIELD)

def GCTVariantAddDENSITY_VOLUME(builder, DENSITY_VOLUME):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(DENSITY_VOLUME), 0)

def AddDENSITY_VOLUME(builder, DENSITY_VOLUME):
    GCTVariantAddDENSITY_VOLUME(builder, DENSITY_VOLUME)

def GCTVariantAddAUTHORING_SEED(builder, AUTHORING_SEED):
    builder.PrependUint32Slot(10, AUTHORING_SEED, 0)

def AddAUTHORING_SEED(builder, AUTHORING_SEED):
    GCTVariantAddAUTHORING_SEED(builder, AUTHORING_SEED)

def GCTVariantEnd(builder):
    return builder.EndObject()

def End(builder):
    return GCTVariantEnd(builder)

import GCTDensityVolume
import GCTDistanceField
import GCTLevel
try:
    from typing import List, Optional
except:
    pass

class GCTVariantT(object):

    # GCTVariantT
    def __init__(
        self,
        VARIANT_ID = None,
        SCOPE = 0,
        VARIANT_CID = None,
        GENUS = 0,
        LEVELS = None,
        BOUNDS_MIN_M = None,
        BOUNDS_MAX_M = None,
        MASS = 0.0,
        DISTANCE_FIELD = None,
        DENSITY_VOLUME = None,
        AUTHORING_SEED = 0,
    ):
        self.VARIANT_ID = VARIANT_ID  # type: Optional[str]
        self.SCOPE = SCOPE  # type: int
        self.VARIANT_CID = VARIANT_CID  # type: Optional[str]
        self.GENUS = GENUS  # type: int
        self.LEVELS = LEVELS  # type: Optional[List[GCTLevel.GCTLevelT]]
        self.BOUNDS_MIN_M = BOUNDS_MIN_M  # type: Optional[List[float]]
        self.BOUNDS_MAX_M = BOUNDS_MAX_M  # type: Optional[List[float]]
        self.MASS = MASS  # type: float
        self.DISTANCE_FIELD = DISTANCE_FIELD  # type: Optional[GCTDistanceField.GCTDistanceFieldT]
        self.DENSITY_VOLUME = DENSITY_VOLUME  # type: Optional[GCTDensityVolume.GCTDensityVolumeT]
        self.AUTHORING_SEED = AUTHORING_SEED  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpGctvariant = GCTVariant()
        tmpGctvariant.Init(buf, pos)
        return cls.InitFromObj(tmpGctvariant)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpGctvariant):
        x = GCTVariantT()
        x._UnPack(tmpGctvariant)
        return x

    # GCTVariantT
    def _UnPack(self, GCTVariant):
        if GCTVariant is None:
            return
        self.VARIANT_ID = GCTVariant.VARIANT_ID()
        self.SCOPE = GCTVariant.SCOPE()
        self.VARIANT_CID = GCTVariant.VARIANT_CID()
        self.GENUS = GCTVariant.GENUS()
        if not GCTVariant.LEVELSIsNone():
            self.LEVELS = []
            for i in range(GCTVariant.LEVELSLength()):
                if GCTVariant.LEVELS(i) is None:
                    self.LEVELS.append(None)
                else:
                    gCTLevel_ = GCTLevel.GCTLevelT.InitFromObj(GCTVariant.LEVELS(i))
                    self.LEVELS.append(gCTLevel_)
        if not GCTVariant.BOUNDS_MIN_MIsNone():
            if np is None:
                self.BOUNDS_MIN_M = []
                for i in range(GCTVariant.BOUNDS_MIN_MLength()):
                    self.BOUNDS_MIN_M.append(GCTVariant.BOUNDS_MIN_M(i))
            else:
                self.BOUNDS_MIN_M = GCTVariant.BOUNDS_MIN_MAsNumpy()
        if not GCTVariant.BOUNDS_MAX_MIsNone():
            if np is None:
                self.BOUNDS_MAX_M = []
                for i in range(GCTVariant.BOUNDS_MAX_MLength()):
                    self.BOUNDS_MAX_M.append(GCTVariant.BOUNDS_MAX_M(i))
            else:
                self.BOUNDS_MAX_M = GCTVariant.BOUNDS_MAX_MAsNumpy()
        self.MASS = GCTVariant.MASS()
        if GCTVariant.DISTANCE_FIELD() is not None:
            self.DISTANCE_FIELD = GCTDistanceField.GCTDistanceFieldT.InitFromObj(GCTVariant.DISTANCE_FIELD())
        if GCTVariant.DENSITY_VOLUME() is not None:
            self.DENSITY_VOLUME = GCTDensityVolume.GCTDensityVolumeT.InitFromObj(GCTVariant.DENSITY_VOLUME())
        self.AUTHORING_SEED = GCTVariant.AUTHORING_SEED()

    # GCTVariantT
    def Pack(self, builder):
        if self.VARIANT_ID is not None:
            VARIANT_ID = builder.CreateString(self.VARIANT_ID)
        if self.VARIANT_CID is not None:
            VARIANT_CID = builder.CreateString(self.VARIANT_CID)
        if self.LEVELS is not None:
            LEVELSlist = []
            for i in range(len(self.LEVELS)):
                LEVELSlist.append(self.LEVELS[i].Pack(builder))
            GCTVariantStartLEVELSVector(builder, len(self.LEVELS))
            for i in reversed(range(len(self.LEVELS))):
                builder.PrependUOffsetTRelative(LEVELSlist[i])
            LEVELS = builder.EndVector()
        if self.BOUNDS_MIN_M is not None:
            if np is not None and type(self.BOUNDS_MIN_M) is np.ndarray:
                BOUNDS_MIN_M = builder.CreateNumpyVector(self.BOUNDS_MIN_M)
            else:
                GCTVariantStartBOUNDS_MIN_MVector(builder, len(self.BOUNDS_MIN_M))
                for i in reversed(range(len(self.BOUNDS_MIN_M))):
                    builder.PrependFloat32(self.BOUNDS_MIN_M[i])
                BOUNDS_MIN_M = builder.EndVector()
        if self.BOUNDS_MAX_M is not None:
            if np is not None and type(self.BOUNDS_MAX_M) is np.ndarray:
                BOUNDS_MAX_M = builder.CreateNumpyVector(self.BOUNDS_MAX_M)
            else:
                GCTVariantStartBOUNDS_MAX_MVector(builder, len(self.BOUNDS_MAX_M))
                for i in reversed(range(len(self.BOUNDS_MAX_M))):
                    builder.PrependFloat32(self.BOUNDS_MAX_M[i])
                BOUNDS_MAX_M = builder.EndVector()
        if self.DISTANCE_FIELD is not None:
            DISTANCE_FIELD = self.DISTANCE_FIELD.Pack(builder)
        if self.DENSITY_VOLUME is not None:
            DENSITY_VOLUME = self.DENSITY_VOLUME.Pack(builder)
        GCTVariantStart(builder)
        if self.VARIANT_ID is not None:
            GCTVariantAddVARIANT_ID(builder, VARIANT_ID)
        GCTVariantAddSCOPE(builder, self.SCOPE)
        if self.VARIANT_CID is not None:
            GCTVariantAddVARIANT_CID(builder, VARIANT_CID)
        GCTVariantAddGENUS(builder, self.GENUS)
        if self.LEVELS is not None:
            GCTVariantAddLEVELS(builder, LEVELS)
        if self.BOUNDS_MIN_M is not None:
            GCTVariantAddBOUNDS_MIN_M(builder, BOUNDS_MIN_M)
        if self.BOUNDS_MAX_M is not None:
            GCTVariantAddBOUNDS_MAX_M(builder, BOUNDS_MAX_M)
        GCTVariantAddMASS(builder, self.MASS)
        if self.DISTANCE_FIELD is not None:
            GCTVariantAddDISTANCE_FIELD(builder, DISTANCE_FIELD)
        if self.DENSITY_VOLUME is not None:
            GCTVariantAddDENSITY_VOLUME(builder, DENSITY_VOLUME)
        GCTVariantAddAUTHORING_SEED(builder, self.AUTHORING_SEED)
        GCTVariant = GCTVariantEnd(builder)
        return GCTVariant
