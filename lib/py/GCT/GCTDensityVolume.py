# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A baked density volume of a variant on the same cube as its distance
# field, for ray marching: N^3 samples of amplitude in AMPLITUDE_UNITS,
# float16, x fastest. Optional.
class GCTDensityVolume(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = GCTDensityVolume()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsGCTDensityVolume(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def GCTDensityVolumeBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x47\x43\x54", size_prefixed=size_prefixed)

    # GCTDensityVolume
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Voxels per axis.
    # GCTDensityVolume
    def N(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Origin of voxel (0, 0, 0) in the variant frame, metres (3 values).
    # GCTDensityVolume
    def ORIGIN_M(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # GCTDensityVolume
    def ORIGIN_MAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # GCTDensityVolume
    def ORIGIN_MLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTDensityVolume
    def ORIGIN_MIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Extent of the cube along each axis, metres (3 values).
    # GCTDensityVolume
    def SPAN_M(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # GCTDensityVolume
    def SPAN_MAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # GCTDensityVolume
    def SPAN_MLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTDensityVolume
    def SPAN_MIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # N^3 IEEE 754 half-precision samples as their 16-bit patterns, x
    # fastest, then y, then z. Absent when the volume travels as the chunk
    # named by CHUNK_CID.
    # GCTDensityVolume
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 2))
        return 0

    # GCTDensityVolume
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint16Flags, o)
        return 0

    # GCTDensityVolume
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GCTDensityVolume
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Content identifier of the volume when DATA is absent.
    # GCTDensityVolume
    def CHUNK_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def GCTDensityVolumeStart(builder):
    builder.StartObject(5)

def Start(builder):
    GCTDensityVolumeStart(builder)

def GCTDensityVolumeAddN(builder, N):
    builder.PrependUint16Slot(0, N, 0)

def AddN(builder, N):
    GCTDensityVolumeAddN(builder, N)

def GCTDensityVolumeAddORIGIN_M(builder, ORIGIN_M):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ORIGIN_M), 0)

def AddORIGIN_M(builder, ORIGIN_M):
    GCTDensityVolumeAddORIGIN_M(builder, ORIGIN_M)

def GCTDensityVolumeStartORIGIN_MVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartORIGIN_MVector(builder, numElems):
    return GCTDensityVolumeStartORIGIN_MVector(builder, numElems)

def GCTDensityVolumeCreateORIGIN_MVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateORIGIN_MVector(builder, data):
    GCTDensityVolumeCreateORIGIN_MVector(builder, data)

def GCTDensityVolumeAddSPAN_M(builder, SPAN_M):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SPAN_M), 0)

def AddSPAN_M(builder, SPAN_M):
    GCTDensityVolumeAddSPAN_M(builder, SPAN_M)

def GCTDensityVolumeStartSPAN_MVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSPAN_MVector(builder, numElems):
    return GCTDensityVolumeStartSPAN_MVector(builder, numElems)

def GCTDensityVolumeCreateSPAN_MVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateSPAN_MVector(builder, data):
    GCTDensityVolumeCreateSPAN_MVector(builder, data)

def GCTDensityVolumeAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    GCTDensityVolumeAddDATA(builder, DATA)

def GCTDensityVolumeStartDATAVector(builder, numElems):
    return builder.StartVector(2, numElems, 2)

def StartDATAVector(builder, numElems):
    return GCTDensityVolumeStartDATAVector(builder, numElems)

def GCTDensityVolumeCreateDATAVector(builder, data):
    data = list(data)
    builder.StartVector(2, len(data), 2)
    for item in reversed(data):
        builder.PrependUint16(item)
    return builder.EndVector()

def CreateDATAVector(builder, data):
    GCTDensityVolumeCreateDATAVector(builder, data)

def GCTDensityVolumeAddCHUNK_CID(builder, CHUNK_CID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(CHUNK_CID), 0)

def AddCHUNK_CID(builder, CHUNK_CID):
    GCTDensityVolumeAddCHUNK_CID(builder, CHUNK_CID)

def GCTDensityVolumeEnd(builder):
    return builder.EndObject()

def End(builder):
    return GCTDensityVolumeEnd(builder)

try:
    from typing import List
except:
    pass

class GCTDensityVolumeT(object):

    # GCTDensityVolumeT
    def __init__(
        self,
        N = 0,
        ORIGIN_M = None,
        SPAN_M = None,
        DATA = None,
        CHUNK_CID = None,
    ):
        self.N = N  # type: int
        self.ORIGIN_M = ORIGIN_M  # type: Optional[List[float]]
        self.SPAN_M = SPAN_M  # type: Optional[List[float]]
        self.DATA = DATA  # type: Optional[List[int]]
        self.CHUNK_CID = CHUNK_CID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpGctdensityVolume = GCTDensityVolume()
        tmpGctdensityVolume.Init(buf, pos)
        return cls.InitFromObj(tmpGctdensityVolume)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpGctdensityVolume):
        x = GCTDensityVolumeT()
        x._UnPack(tmpGctdensityVolume)
        return x

    # GCTDensityVolumeT
    def _UnPack(self, GCTDensityVolume):
        if GCTDensityVolume is None:
            return
        self.N = GCTDensityVolume.N()
        if not GCTDensityVolume.ORIGIN_MIsNone():
            if np is None:
                self.ORIGIN_M = []
                for i in range(GCTDensityVolume.ORIGIN_MLength()):
                    self.ORIGIN_M.append(GCTDensityVolume.ORIGIN_M(i))
            else:
                self.ORIGIN_M = GCTDensityVolume.ORIGIN_MAsNumpy()
        if not GCTDensityVolume.SPAN_MIsNone():
            if np is None:
                self.SPAN_M = []
                for i in range(GCTDensityVolume.SPAN_MLength()):
                    self.SPAN_M.append(GCTDensityVolume.SPAN_M(i))
            else:
                self.SPAN_M = GCTDensityVolume.SPAN_MAsNumpy()
        if not GCTDensityVolume.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(GCTDensityVolume.DATALength()):
                    self.DATA.append(GCTDensityVolume.DATA(i))
            else:
                self.DATA = GCTDensityVolume.DATAAsNumpy()
        self.CHUNK_CID = GCTDensityVolume.CHUNK_CID()

    # GCTDensityVolumeT
    def Pack(self, builder):
        if self.ORIGIN_M is not None:
            if np is not None and type(self.ORIGIN_M) is np.ndarray:
                ORIGIN_M = builder.CreateNumpyVector(self.ORIGIN_M)
            else:
                GCTDensityVolumeStartORIGIN_MVector(builder, len(self.ORIGIN_M))
                for i in reversed(range(len(self.ORIGIN_M))):
                    builder.PrependFloat32(self.ORIGIN_M[i])
                ORIGIN_M = builder.EndVector()
        if self.SPAN_M is not None:
            if np is not None and type(self.SPAN_M) is np.ndarray:
                SPAN_M = builder.CreateNumpyVector(self.SPAN_M)
            else:
                GCTDensityVolumeStartSPAN_MVector(builder, len(self.SPAN_M))
                for i in reversed(range(len(self.SPAN_M))):
                    builder.PrependFloat32(self.SPAN_M[i])
                SPAN_M = builder.EndVector()
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                GCTDensityVolumeStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependUint16(self.DATA[i])
                DATA = builder.EndVector()
        if self.CHUNK_CID is not None:
            CHUNK_CID = builder.CreateString(self.CHUNK_CID)
        GCTDensityVolumeStart(builder)
        GCTDensityVolumeAddN(builder, self.N)
        if self.ORIGIN_M is not None:
            GCTDensityVolumeAddORIGIN_M(builder, ORIGIN_M)
        if self.SPAN_M is not None:
            GCTDensityVolumeAddSPAN_M(builder, SPAN_M)
        if self.DATA is not None:
            GCTDensityVolumeAddDATA(builder, DATA)
        if self.CHUNK_CID is not None:
            GCTDensityVolumeAddCHUNK_CID(builder, CHUNK_CID)
        GCTDensityVolume = GCTDensityVolumeEnd(builder)
        return GCTDensityVolume
