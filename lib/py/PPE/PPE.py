# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Polynomial Ephemeris — top-level message containing metadata and
# one or more polynomial coefficient records for a single space object.
#
# A PPE message may contain position records, orbital element records, or both.
# Records should be ordered chronologically by EPOCH_MID and should collectively
# cover the time span [START_TIME, STOP_TIME] without gaps.
class PPE(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PPE()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPPE(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PPEBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x50\x45", size_prefixed=size_prefixed)

    # PPE
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Plain-text comments.
    # PPE
    def COMMENT(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # PPE
    def COMMENTLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPE
    def COMMENTIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Space object identification.
    # PPE
    def OBJECT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CAT import CAT
            obj = CAT()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Origin of the reference frame (e.g., EARTH, MOON, MARS).
    # PPE
    def CENTER_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference frame for position/velocity coefficients.
    # PPE
    def REFERENCE_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from RFM import RFM
            obj = RFM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Time system used for all epochs in this message.
    # PPE
    def TIME_SYSTEM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Start of the total time span covered by this ephemeris (ISO 8601).
    # PPE
    def START_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # End of the total time span covered by this ephemeris (ISO 8601).
    # PPE
    def STOP_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Default polynomial basis type for all records in this message.
    # Individual records may override this with their own BASIS_TYPE field.
    # PPE
    def DEFAULT_BASIS_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Array of position polynomial records.
    # Each record covers a time segment; together they span [START_TIME, STOP_TIME].
    # PPE
    def POSITION_RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PPEPositionRecord import PPEPositionRecord
            obj = PPEPositionRecord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PPE
    def POSITION_RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPE
    def POSITION_RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # Array of orbital element polynomial records.
    # Each record covers a time segment; together they span [START_TIME, STOP_TIME].
    # PPE
    def ORBITAL_ELEMENT_RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PPEOrbitalElementRecord import PPEOrbitalElementRecord
            obj = PPEOrbitalElementRecord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PPE
    def ORBITAL_ELEMENT_RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPE
    def ORBITAL_ELEMENT_RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # Generating ephemeris source (e.g., "JPL DE440", "HPOP v2.1", "Basilisk chebyPosEphem").
    # PPE
    def EPHEMERIS_SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Fit span in seconds used to generate each polynomial segment.
    # Informational; actual spans are in individual records.
    # PPE
    def NOMINAL_SEGMENT_SPAN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Nominal number of coefficients per segment.
    # Informational; actual counts are in individual records.
    # PPE
    def NOMINAL_NUM_COEFFICIENTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

def PPEStart(builder):
    builder.StartObject(13)

def Start(builder):
    PPEStart(builder)

def PPEAddCOMMENT(builder, COMMENT):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(COMMENT), 0)

def AddCOMMENT(builder, COMMENT):
    PPEAddCOMMENT(builder, COMMENT)

def PPEStartCOMMENTVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCOMMENTVector(builder, numElems):
    return PPEStartCOMMENTVector(builder, numElems)

def PPEAddOBJECT(builder, OBJECT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT), 0)

def AddOBJECT(builder, OBJECT):
    PPEAddOBJECT(builder, OBJECT)

def PPEAddCENTER_NAME(builder, CENTER_NAME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(CENTER_NAME), 0)

def AddCENTER_NAME(builder, CENTER_NAME):
    PPEAddCENTER_NAME(builder, CENTER_NAME)

def PPEAddREFERENCE_FRAME(builder, REFERENCE_FRAME):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(REFERENCE_FRAME), 0)

def AddREFERENCE_FRAME(builder, REFERENCE_FRAME):
    PPEAddREFERENCE_FRAME(builder, REFERENCE_FRAME)

def PPEAddTIME_SYSTEM(builder, TIME_SYSTEM):
    builder.PrependInt8Slot(4, TIME_SYSTEM, 0)

def AddTIME_SYSTEM(builder, TIME_SYSTEM):
    PPEAddTIME_SYSTEM(builder, TIME_SYSTEM)

def PPEAddSTART_TIME(builder, START_TIME):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(START_TIME), 0)

def AddSTART_TIME(builder, START_TIME):
    PPEAddSTART_TIME(builder, START_TIME)

def PPEAddSTOP_TIME(builder, STOP_TIME):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(STOP_TIME), 0)

def AddSTOP_TIME(builder, STOP_TIME):
    PPEAddSTOP_TIME(builder, STOP_TIME)

def PPEAddDEFAULT_BASIS_TYPE(builder, DEFAULT_BASIS_TYPE):
    builder.PrependInt8Slot(7, DEFAULT_BASIS_TYPE, 0)

def AddDEFAULT_BASIS_TYPE(builder, DEFAULT_BASIS_TYPE):
    PPEAddDEFAULT_BASIS_TYPE(builder, DEFAULT_BASIS_TYPE)

def PPEAddPOSITION_RECORDS(builder, POSITION_RECORDS):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(POSITION_RECORDS), 0)

def AddPOSITION_RECORDS(builder, POSITION_RECORDS):
    PPEAddPOSITION_RECORDS(builder, POSITION_RECORDS)

def PPEStartPOSITION_RECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPOSITION_RECORDSVector(builder, numElems):
    return PPEStartPOSITION_RECORDSVector(builder, numElems)

def PPEAddORBITAL_ELEMENT_RECORDS(builder, ORBITAL_ELEMENT_RECORDS):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(ORBITAL_ELEMENT_RECORDS), 0)

def AddORBITAL_ELEMENT_RECORDS(builder, ORBITAL_ELEMENT_RECORDS):
    PPEAddORBITAL_ELEMENT_RECORDS(builder, ORBITAL_ELEMENT_RECORDS)

def PPEStartORBITAL_ELEMENT_RECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartORBITAL_ELEMENT_RECORDSVector(builder, numElems):
    return PPEStartORBITAL_ELEMENT_RECORDSVector(builder, numElems)

def PPEAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(EPHEMERIS_SOURCE), 0)

def AddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    PPEAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)

def PPEAddNOMINAL_SEGMENT_SPAN(builder, NOMINAL_SEGMENT_SPAN):
    builder.PrependFloat64Slot(11, NOMINAL_SEGMENT_SPAN, 0.0)

def AddNOMINAL_SEGMENT_SPAN(builder, NOMINAL_SEGMENT_SPAN):
    PPEAddNOMINAL_SEGMENT_SPAN(builder, NOMINAL_SEGMENT_SPAN)

def PPEAddNOMINAL_NUM_COEFFICIENTS(builder, NOMINAL_NUM_COEFFICIENTS):
    builder.PrependUint16Slot(12, NOMINAL_NUM_COEFFICIENTS, 0)

def AddNOMINAL_NUM_COEFFICIENTS(builder, NOMINAL_NUM_COEFFICIENTS):
    PPEAddNOMINAL_NUM_COEFFICIENTS(builder, NOMINAL_NUM_COEFFICIENTS)

def PPEEnd(builder):
    return builder.EndObject()

def End(builder):
    return PPEEnd(builder)

import CAT
import PPEOrbitalElementRecord
import PPEPositionRecord
import RFM
try:
    from typing import List, Optional
except:
    pass

class PPET(object):

    # PPET
    def __init__(self):
        self.COMMENT = None  # type: List[str]
        self.OBJECT = None  # type: Optional[CAT.CATT]
        self.CENTER_NAME = None  # type: str
        self.REFERENCE_FRAME = None  # type: Optional[RFM.RFMT]
        self.TIME_SYSTEM = 0  # type: int
        self.START_TIME = None  # type: str
        self.STOP_TIME = None  # type: str
        self.DEFAULT_BASIS_TYPE = 0  # type: int
        self.POSITION_RECORDS = None  # type: List[PPEPositionRecord.PPEPositionRecordT]
        self.ORBITAL_ELEMENT_RECORDS = None  # type: List[PPEOrbitalElementRecord.PPEOrbitalElementRecordT]
        self.EPHEMERIS_SOURCE = None  # type: str
        self.NOMINAL_SEGMENT_SPAN = 0.0  # type: float
        self.NOMINAL_NUM_COEFFICIENTS = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        PPE = PPE()
        PPE.Init(buf, pos)
        return cls.InitFromObj(PPE)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, PPE):
        x = PPET()
        x._UnPack(PPE)
        return x

    # PPET
    def _UnPack(self, PPE):
        if PPE is None:
            return
        if not PPE.COMMENTIsNone():
            self.COMMENT = []
            for i in range(PPE.COMMENTLength()):
                self.COMMENT.append(PPE.COMMENT(i))
        if PPE.OBJECT() is not None:
            self.OBJECT = CAT.CATT.InitFromObj(PPE.OBJECT())
        self.CENTER_NAME = PPE.CENTER_NAME()
        if PPE.REFERENCE_FRAME() is not None:
            self.REFERENCE_FRAME = RFM.RFMT.InitFromObj(PPE.REFERENCE_FRAME())
        self.TIME_SYSTEM = PPE.TIME_SYSTEM()
        self.START_TIME = PPE.START_TIME()
        self.STOP_TIME = PPE.STOP_TIME()
        self.DEFAULT_BASIS_TYPE = PPE.DEFAULT_BASIS_TYPE()
        if not PPE.POSITION_RECORDSIsNone():
            self.POSITION_RECORDS = []
            for i in range(PPE.POSITION_RECORDSLength()):
                if PPE.POSITION_RECORDS(i) is None:
                    self.POSITION_RECORDS.append(None)
                else:
                    pPEPositionRecord_ = PPEPositionRecord.PPEPositionRecordT.InitFromObj(PPE.POSITION_RECORDS(i))
                    self.POSITION_RECORDS.append(pPEPositionRecord_)
        if not PPE.ORBITAL_ELEMENT_RECORDSIsNone():
            self.ORBITAL_ELEMENT_RECORDS = []
            for i in range(PPE.ORBITAL_ELEMENT_RECORDSLength()):
                if PPE.ORBITAL_ELEMENT_RECORDS(i) is None:
                    self.ORBITAL_ELEMENT_RECORDS.append(None)
                else:
                    pPEOrbitalElementRecord_ = PPEOrbitalElementRecord.PPEOrbitalElementRecordT.InitFromObj(PPE.ORBITAL_ELEMENT_RECORDS(i))
                    self.ORBITAL_ELEMENT_RECORDS.append(pPEOrbitalElementRecord_)
        self.EPHEMERIS_SOURCE = PPE.EPHEMERIS_SOURCE()
        self.NOMINAL_SEGMENT_SPAN = PPE.NOMINAL_SEGMENT_SPAN()
        self.NOMINAL_NUM_COEFFICIENTS = PPE.NOMINAL_NUM_COEFFICIENTS()

    # PPET
    def Pack(self, builder):
        if self.COMMENT is not None:
            COMMENTlist = []
            for i in range(len(self.COMMENT)):
                COMMENTlist.append(builder.CreateString(self.COMMENT[i]))
            PPEStartCOMMENTVector(builder, len(self.COMMENT))
            for i in reversed(range(len(self.COMMENT))):
                builder.PrependUOffsetTRelative(COMMENTlist[i])
            COMMENT = builder.EndVector()
        if self.OBJECT is not None:
            OBJECT = self.OBJECT.Pack(builder)
        if self.CENTER_NAME is not None:
            CENTER_NAME = builder.CreateString(self.CENTER_NAME)
        if self.REFERENCE_FRAME is not None:
            REFERENCE_FRAME = self.REFERENCE_FRAME.Pack(builder)
        if self.START_TIME is not None:
            START_TIME = builder.CreateString(self.START_TIME)
        if self.STOP_TIME is not None:
            STOP_TIME = builder.CreateString(self.STOP_TIME)
        if self.POSITION_RECORDS is not None:
            POSITION_RECORDSlist = []
            for i in range(len(self.POSITION_RECORDS)):
                POSITION_RECORDSlist.append(self.POSITION_RECORDS[i].Pack(builder))
            PPEStartPOSITION_RECORDSVector(builder, len(self.POSITION_RECORDS))
            for i in reversed(range(len(self.POSITION_RECORDS))):
                builder.PrependUOffsetTRelative(POSITION_RECORDSlist[i])
            POSITION_RECORDS = builder.EndVector()
        if self.ORBITAL_ELEMENT_RECORDS is not None:
            ORBITAL_ELEMENT_RECORDSlist = []
            for i in range(len(self.ORBITAL_ELEMENT_RECORDS)):
                ORBITAL_ELEMENT_RECORDSlist.append(self.ORBITAL_ELEMENT_RECORDS[i].Pack(builder))
            PPEStartORBITAL_ELEMENT_RECORDSVector(builder, len(self.ORBITAL_ELEMENT_RECORDS))
            for i in reversed(range(len(self.ORBITAL_ELEMENT_RECORDS))):
                builder.PrependUOffsetTRelative(ORBITAL_ELEMENT_RECORDSlist[i])
            ORBITAL_ELEMENT_RECORDS = builder.EndVector()
        if self.EPHEMERIS_SOURCE is not None:
            EPHEMERIS_SOURCE = builder.CreateString(self.EPHEMERIS_SOURCE)
        PPEStart(builder)
        if self.COMMENT is not None:
            PPEAddCOMMENT(builder, COMMENT)
        if self.OBJECT is not None:
            PPEAddOBJECT(builder, OBJECT)
        if self.CENTER_NAME is not None:
            PPEAddCENTER_NAME(builder, CENTER_NAME)
        if self.REFERENCE_FRAME is not None:
            PPEAddREFERENCE_FRAME(builder, REFERENCE_FRAME)
        PPEAddTIME_SYSTEM(builder, self.TIME_SYSTEM)
        if self.START_TIME is not None:
            PPEAddSTART_TIME(builder, START_TIME)
        if self.STOP_TIME is not None:
            PPEAddSTOP_TIME(builder, STOP_TIME)
        PPEAddDEFAULT_BASIS_TYPE(builder, self.DEFAULT_BASIS_TYPE)
        if self.POSITION_RECORDS is not None:
            PPEAddPOSITION_RECORDS(builder, POSITION_RECORDS)
        if self.ORBITAL_ELEMENT_RECORDS is not None:
            PPEAddORBITAL_ELEMENT_RECORDS(builder, ORBITAL_ELEMENT_RECORDS)
        if self.EPHEMERIS_SOURCE is not None:
            PPEAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)
        PPEAddNOMINAL_SEGMENT_SPAN(builder, self.NOMINAL_SEGMENT_SPAN)
        PPEAddNOMINAL_NUM_COEFFICIENTS(builder, self.NOMINAL_NUM_COEFFICIENTS)
        PPE = PPEEnd(builder)
        return PPE
