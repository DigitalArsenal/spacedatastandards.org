# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PRWEphemerisResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWEphemerisResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWEphemerisResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWEphemerisResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWEphemerisResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWEphemerisResult
    def TARGET_NAIF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # PRWEphemerisResult
    def CENTER_NAIF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Explicit ICRF axes and named NAIF origin; FRM state in SI m/m/s.
    # PRWEphemerisResult
    def STATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWResidentState import PRWResidentState
            obj = PRWResidentState()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWEphemerisResult
    def EPHEMERIS_SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PRWEphemerisResultStart(builder):
    builder.StartObject(4)

def Start(builder):
    PRWEphemerisResultStart(builder)

def PRWEphemerisResultAddTARGET_NAIF_ID(builder, TARGET_NAIF_ID):
    builder.PrependInt32Slot(0, TARGET_NAIF_ID, 0)

def AddTARGET_NAIF_ID(builder, TARGET_NAIF_ID):
    PRWEphemerisResultAddTARGET_NAIF_ID(builder, TARGET_NAIF_ID)

def PRWEphemerisResultAddCENTER_NAIF_ID(builder, CENTER_NAIF_ID):
    builder.PrependInt32Slot(1, CENTER_NAIF_ID, 0)

def AddCENTER_NAIF_ID(builder, CENTER_NAIF_ID):
    PRWEphemerisResultAddCENTER_NAIF_ID(builder, CENTER_NAIF_ID)

def PRWEphemerisResultAddSTATE(builder, STATE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(STATE), 0)

def AddSTATE(builder, STATE):
    PRWEphemerisResultAddSTATE(builder, STATE)

def PRWEphemerisResultAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(EPHEMERIS_SOURCE), 0)

def AddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    PRWEphemerisResultAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)

def PRWEphemerisResultEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWEphemerisResultEnd(builder)

import PRWResidentState
try:
    from typing import Optional
except:
    pass

class PRWEphemerisResultT(object):

    # PRWEphemerisResultT
    def __init__(
        self,
        TARGET_NAIF_ID = 0,
        CENTER_NAIF_ID = 0,
        STATE = None,
        EPHEMERIS_SOURCE = None,
    ):
        self.TARGET_NAIF_ID = TARGET_NAIF_ID  # type: int
        self.CENTER_NAIF_ID = CENTER_NAIF_ID  # type: int
        self.STATE = STATE  # type: Optional[PRWResidentState.PRWResidentStateT]
        self.EPHEMERIS_SOURCE = EPHEMERIS_SOURCE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwephemerisResult = PRWEphemerisResult()
        tmpPrwephemerisResult.Init(buf, pos)
        return cls.InitFromObj(tmpPrwephemerisResult)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwephemerisResult):
        x = PRWEphemerisResultT()
        x._UnPack(tmpPrwephemerisResult)
        return x

    # PRWEphemerisResultT
    def _UnPack(self, PRWEphemerisResult):
        if PRWEphemerisResult is None:
            return
        self.TARGET_NAIF_ID = PRWEphemerisResult.TARGET_NAIF_ID()
        self.CENTER_NAIF_ID = PRWEphemerisResult.CENTER_NAIF_ID()
        if PRWEphemerisResult.STATE() is not None:
            self.STATE = PRWResidentState.PRWResidentStateT.InitFromObj(PRWEphemerisResult.STATE())
        self.EPHEMERIS_SOURCE = PRWEphemerisResult.EPHEMERIS_SOURCE()

    # PRWEphemerisResultT
    def Pack(self, builder):
        if self.STATE is not None:
            STATE = self.STATE.Pack(builder)
        if self.EPHEMERIS_SOURCE is not None:
            EPHEMERIS_SOURCE = builder.CreateString(self.EPHEMERIS_SOURCE)
        PRWEphemerisResultStart(builder)
        PRWEphemerisResultAddTARGET_NAIF_ID(builder, self.TARGET_NAIF_ID)
        PRWEphemerisResultAddCENTER_NAIF_ID(builder, self.CENTER_NAIF_ID)
        if self.STATE is not None:
            PRWEphemerisResultAddSTATE(builder, STATE)
        if self.EPHEMERIS_SOURCE is not None:
            PRWEphemerisResultAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)
        PRWEphemerisResult = PRWEphemerisResultEnd(builder)
        return PRWEphemerisResult
