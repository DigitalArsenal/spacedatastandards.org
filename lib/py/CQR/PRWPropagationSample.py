# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PRWPropagationSample(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWPropagationSample()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWPropagationSample(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWPropagationSampleBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWPropagationSample
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWPropagationSample
    def STATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWResidentState import PRWResidentState
            obj = PRWResidentState()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Both STMs cumulative from request initial epoch; SI state units.
    # PRWPropagationSample
    def STM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWStateMatrix import PRWStateMatrix
            obj = PRWStateMatrix()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWPropagationSample
    def MASS_STM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWStateMatrix import PRWStateMatrix
            obj = PRWStateMatrix()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWPropagationSample
    def COVARIANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWStateMatrix import PRWStateMatrix
            obj = PRWStateMatrix()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWPropagationSample
    def MASS_COVARIANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWStateMatrix import PRWStateMatrix
            obj = PRWStateMatrix()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWPropagationSample
    def ACCEPTED_STEPS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # PRWPropagationSample
    def REJECTED_STEPS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # PRWPropagationSample
    def BURNS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PRWBurnReport import PRWBurnReport
            obj = PRWBurnReport()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWPropagationSample
    def BURNSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWPropagationSample
    def BURNSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

def PRWPropagationSampleStart(builder):
    builder.StartObject(8)

def Start(builder):
    PRWPropagationSampleStart(builder)

def PRWPropagationSampleAddSTATE(builder, STATE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(STATE), 0)

def AddSTATE(builder, STATE):
    PRWPropagationSampleAddSTATE(builder, STATE)

def PRWPropagationSampleAddSTM(builder, STM):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(STM), 0)

def AddSTM(builder, STM):
    PRWPropagationSampleAddSTM(builder, STM)

def PRWPropagationSampleAddMASS_STM(builder, MASS_STM):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MASS_STM), 0)

def AddMASS_STM(builder, MASS_STM):
    PRWPropagationSampleAddMASS_STM(builder, MASS_STM)

def PRWPropagationSampleAddCOVARIANCE(builder, COVARIANCE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(COVARIANCE), 0)

def AddCOVARIANCE(builder, COVARIANCE):
    PRWPropagationSampleAddCOVARIANCE(builder, COVARIANCE)

def PRWPropagationSampleAddMASS_COVARIANCE(builder, MASS_COVARIANCE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(MASS_COVARIANCE), 0)

def AddMASS_COVARIANCE(builder, MASS_COVARIANCE):
    PRWPropagationSampleAddMASS_COVARIANCE(builder, MASS_COVARIANCE)

def PRWPropagationSampleAddACCEPTED_STEPS(builder, ACCEPTED_STEPS):
    builder.PrependUint64Slot(5, ACCEPTED_STEPS, 0)

def AddACCEPTED_STEPS(builder, ACCEPTED_STEPS):
    PRWPropagationSampleAddACCEPTED_STEPS(builder, ACCEPTED_STEPS)

def PRWPropagationSampleAddREJECTED_STEPS(builder, REJECTED_STEPS):
    builder.PrependUint64Slot(6, REJECTED_STEPS, 0)

def AddREJECTED_STEPS(builder, REJECTED_STEPS):
    PRWPropagationSampleAddREJECTED_STEPS(builder, REJECTED_STEPS)

def PRWPropagationSampleAddBURNS(builder, BURNS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(BURNS), 0)

def AddBURNS(builder, BURNS):
    PRWPropagationSampleAddBURNS(builder, BURNS)

def PRWPropagationSampleStartBURNSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartBURNSVector(builder, numElems):
    return PRWPropagationSampleStartBURNSVector(builder, numElems)

def PRWPropagationSampleCreateBURNSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateBURNSVector(builder, data):
    PRWPropagationSampleCreateBURNSVector(builder, data)

def PRWPropagationSampleEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWPropagationSampleEnd(builder)

import PRWBurnReport
import PRWResidentState
import PRWStateMatrix
try:
    from typing import List, Optional
except:
    pass

class PRWPropagationSampleT(object):

    # PRWPropagationSampleT
    def __init__(
        self,
        STATE = None,
        STM = None,
        MASS_STM = None,
        COVARIANCE = None,
        MASS_COVARIANCE = None,
        ACCEPTED_STEPS = 0,
        REJECTED_STEPS = 0,
        BURNS = None,
    ):
        self.STATE = STATE  # type: Optional[PRWResidentState.PRWResidentStateT]
        self.STM = STM  # type: Optional[PRWStateMatrix.PRWStateMatrixT]
        self.MASS_STM = MASS_STM  # type: Optional[PRWStateMatrix.PRWStateMatrixT]
        self.COVARIANCE = COVARIANCE  # type: Optional[PRWStateMatrix.PRWStateMatrixT]
        self.MASS_COVARIANCE = MASS_COVARIANCE  # type: Optional[PRWStateMatrix.PRWStateMatrixT]
        self.ACCEPTED_STEPS = ACCEPTED_STEPS  # type: int
        self.REJECTED_STEPS = REJECTED_STEPS  # type: int
        self.BURNS = BURNS  # type: Optional[List[PRWBurnReport.PRWBurnReportT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwpropagationSample = PRWPropagationSample()
        tmpPrwpropagationSample.Init(buf, pos)
        return cls.InitFromObj(tmpPrwpropagationSample)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwpropagationSample):
        x = PRWPropagationSampleT()
        x._UnPack(tmpPrwpropagationSample)
        return x

    # PRWPropagationSampleT
    def _UnPack(self, PRWPropagationSample):
        if PRWPropagationSample is None:
            return
        if PRWPropagationSample.STATE() is not None:
            self.STATE = PRWResidentState.PRWResidentStateT.InitFromObj(PRWPropagationSample.STATE())
        if PRWPropagationSample.STM() is not None:
            self.STM = PRWStateMatrix.PRWStateMatrixT.InitFromObj(PRWPropagationSample.STM())
        if PRWPropagationSample.MASS_STM() is not None:
            self.MASS_STM = PRWStateMatrix.PRWStateMatrixT.InitFromObj(PRWPropagationSample.MASS_STM())
        if PRWPropagationSample.COVARIANCE() is not None:
            self.COVARIANCE = PRWStateMatrix.PRWStateMatrixT.InitFromObj(PRWPropagationSample.COVARIANCE())
        if PRWPropagationSample.MASS_COVARIANCE() is not None:
            self.MASS_COVARIANCE = PRWStateMatrix.PRWStateMatrixT.InitFromObj(PRWPropagationSample.MASS_COVARIANCE())
        self.ACCEPTED_STEPS = PRWPropagationSample.ACCEPTED_STEPS()
        self.REJECTED_STEPS = PRWPropagationSample.REJECTED_STEPS()
        if not PRWPropagationSample.BURNSIsNone():
            self.BURNS = []
            for i in range(PRWPropagationSample.BURNSLength()):
                if PRWPropagationSample.BURNS(i) is None:
                    self.BURNS.append(None)
                else:
                    pRWBurnReport_ = PRWBurnReport.PRWBurnReportT.InitFromObj(PRWPropagationSample.BURNS(i))
                    self.BURNS.append(pRWBurnReport_)

    # PRWPropagationSampleT
    def Pack(self, builder):
        if self.STATE is not None:
            STATE = self.STATE.Pack(builder)
        if self.STM is not None:
            STM = self.STM.Pack(builder)
        if self.MASS_STM is not None:
            MASS_STM = self.MASS_STM.Pack(builder)
        if self.COVARIANCE is not None:
            COVARIANCE = self.COVARIANCE.Pack(builder)
        if self.MASS_COVARIANCE is not None:
            MASS_COVARIANCE = self.MASS_COVARIANCE.Pack(builder)
        if self.BURNS is not None:
            BURNSlist = []
            for i in range(len(self.BURNS)):
                BURNSlist.append(self.BURNS[i].Pack(builder))
            PRWPropagationSampleStartBURNSVector(builder, len(self.BURNS))
            for i in reversed(range(len(self.BURNS))):
                builder.PrependUOffsetTRelative(BURNSlist[i])
            BURNS = builder.EndVector()
        PRWPropagationSampleStart(builder)
        if self.STATE is not None:
            PRWPropagationSampleAddSTATE(builder, STATE)
        if self.STM is not None:
            PRWPropagationSampleAddSTM(builder, STM)
        if self.MASS_STM is not None:
            PRWPropagationSampleAddMASS_STM(builder, MASS_STM)
        if self.COVARIANCE is not None:
            PRWPropagationSampleAddCOVARIANCE(builder, COVARIANCE)
        if self.MASS_COVARIANCE is not None:
            PRWPropagationSampleAddMASS_COVARIANCE(builder, MASS_COVARIANCE)
        PRWPropagationSampleAddACCEPTED_STEPS(builder, self.ACCEPTED_STEPS)
        PRWPropagationSampleAddREJECTED_STEPS(builder, self.REJECTED_STEPS)
        if self.BURNS is not None:
            PRWPropagationSampleAddBURNS(builder, BURNS)
        PRWPropagationSample = PRWPropagationSampleEnd(builder)
        return PRWPropagationSample
