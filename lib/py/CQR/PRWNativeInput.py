# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Native container bytes wholly inside the FlatBuffer; not an NCD trailer.
class PRWNativeInput(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWNativeInput()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWNativeInput(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWNativeInputBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWNativeInput
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWNativeInput
    def DESCRIPTOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from NCD import NCD
            obj = NCD()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Exactly DESCRIPTOR.SOURCE_BYTE_LENGTH bytes; verify declared hash.
    # PRWNativeInput
    def CONTENT(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # PRWNativeInput
    def CONTENTAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # PRWNativeInput
    def CONTENTLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWNativeInput
    def CONTENTIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def PRWNativeInputStart(builder):
    builder.StartObject(2)

def Start(builder):
    PRWNativeInputStart(builder)

def PRWNativeInputAddDESCRIPTOR(builder, DESCRIPTOR):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTOR), 0)

def AddDESCRIPTOR(builder, DESCRIPTOR):
    PRWNativeInputAddDESCRIPTOR(builder, DESCRIPTOR)

def PRWNativeInputAddCONTENT(builder, CONTENT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT), 0)

def AddCONTENT(builder, CONTENT):
    PRWNativeInputAddCONTENT(builder, CONTENT)

def PRWNativeInputStartCONTENTVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCONTENTVector(builder, numElems):
    return PRWNativeInputStartCONTENTVector(builder, numElems)

def PRWNativeInputCreateCONTENTVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCONTENTVector(builder, data):
    PRWNativeInputCreateCONTENTVector(builder, data)

def PRWNativeInputEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWNativeInputEnd(builder)

import NCD
try:
    from typing import List, Optional
except:
    pass

class PRWNativeInputT(object):

    # PRWNativeInputT
    def __init__(
        self,
        DESCRIPTOR = None,
        CONTENT = None,
    ):
        self.DESCRIPTOR = DESCRIPTOR  # type: Optional[NCD.NCDT]
        self.CONTENT = CONTENT  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwnativeInput = PRWNativeInput()
        tmpPrwnativeInput.Init(buf, pos)
        return cls.InitFromObj(tmpPrwnativeInput)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwnativeInput):
        x = PRWNativeInputT()
        x._UnPack(tmpPrwnativeInput)
        return x

    # PRWNativeInputT
    def _UnPack(self, PRWNativeInput):
        if PRWNativeInput is None:
            return
        if PRWNativeInput.DESCRIPTOR() is not None:
            self.DESCRIPTOR = NCD.NCDT.InitFromObj(PRWNativeInput.DESCRIPTOR())
        if not PRWNativeInput.CONTENTIsNone():
            if np is None:
                self.CONTENT = []
                for i in range(PRWNativeInput.CONTENTLength()):
                    self.CONTENT.append(PRWNativeInput.CONTENT(i))
            else:
                self.CONTENT = PRWNativeInput.CONTENTAsNumpy()

    # PRWNativeInputT
    def Pack(self, builder):
        if self.DESCRIPTOR is not None:
            DESCRIPTOR = self.DESCRIPTOR.Pack(builder)
        if self.CONTENT is not None:
            if np is not None and type(self.CONTENT) is np.ndarray:
                CONTENT = builder.CreateNumpyVector(self.CONTENT)
            else:
                PRWNativeInputStartCONTENTVector(builder, len(self.CONTENT))
                for i in reversed(range(len(self.CONTENT))):
                    builder.PrependUint8(self.CONTENT[i])
                CONTENT = builder.EndVector()
        PRWNativeInputStart(builder)
        if self.DESCRIPTOR is not None:
            PRWNativeInputAddDESCRIPTOR(builder, DESCRIPTOR)
        if self.CONTENT is not None:
            PRWNativeInputAddCONTENT(builder, CONTENT)
        PRWNativeInput = PRWNativeInputEnd(builder)
        return PRWNativeInput
