# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Typed replacement of a bare raw-text port. Content is UTF-8 with no BOM;
# XML declaration, if present, must agree. Content must be nonempty CDM KVN
# or CDM XML matching SERIALIZATION; reject UNSPECIFIED and other documents.
# Not an arbitrary JSON container.
class CQRNativeDocument(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CQRNativeDocument()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCQRNativeDocument(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CQRNativeDocumentBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x51\x52", size_prefixed=size_prefixed)

    # CQRNativeDocument
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CQRNativeDocument
    def SERIALIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # CQRNativeDocument
    def CONTENT(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CQRNativeDocument
    def CONTENTAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # CQRNativeDocument
    def CONTENTLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CQRNativeDocument
    def CONTENTIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def CQRNativeDocumentStart(builder):
    builder.StartObject(2)

def Start(builder):
    CQRNativeDocumentStart(builder)

def CQRNativeDocumentAddSERIALIZATION(builder, SERIALIZATION):
    builder.PrependUint8Slot(0, SERIALIZATION, 0)

def AddSERIALIZATION(builder, SERIALIZATION):
    CQRNativeDocumentAddSERIALIZATION(builder, SERIALIZATION)

def CQRNativeDocumentAddCONTENT(builder, CONTENT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT), 0)

def AddCONTENT(builder, CONTENT):
    CQRNativeDocumentAddCONTENT(builder, CONTENT)

def CQRNativeDocumentStartCONTENTVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCONTENTVector(builder, numElems):
    return CQRNativeDocumentStartCONTENTVector(builder, numElems)

def CQRNativeDocumentCreateCONTENTVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCONTENTVector(builder, data):
    CQRNativeDocumentCreateCONTENTVector(builder, data)

def CQRNativeDocumentEnd(builder):
    return builder.EndObject()

def End(builder):
    return CQRNativeDocumentEnd(builder)

try:
    from typing import List
except:
    pass

class CQRNativeDocumentT(object):

    # CQRNativeDocumentT
    def __init__(
        self,
        SERIALIZATION = 0,
        CONTENT = None,
    ):
        self.SERIALIZATION = SERIALIZATION  # type: int
        self.CONTENT = CONTENT  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCqrnativeDocument = CQRNativeDocument()
        tmpCqrnativeDocument.Init(buf, pos)
        return cls.InitFromObj(tmpCqrnativeDocument)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCqrnativeDocument):
        x = CQRNativeDocumentT()
        x._UnPack(tmpCqrnativeDocument)
        return x

    # CQRNativeDocumentT
    def _UnPack(self, CQRNativeDocument):
        if CQRNativeDocument is None:
            return
        self.SERIALIZATION = CQRNativeDocument.SERIALIZATION()
        if not CQRNativeDocument.CONTENTIsNone():
            if np is None:
                self.CONTENT = []
                for i in range(CQRNativeDocument.CONTENTLength()):
                    self.CONTENT.append(CQRNativeDocument.CONTENT(i))
            else:
                self.CONTENT = CQRNativeDocument.CONTENTAsNumpy()

    # CQRNativeDocumentT
    def Pack(self, builder):
        if self.CONTENT is not None:
            if np is not None and type(self.CONTENT) is np.ndarray:
                CONTENT = builder.CreateNumpyVector(self.CONTENT)
            else:
                CQRNativeDocumentStartCONTENTVector(builder, len(self.CONTENT))
                for i in reversed(range(len(self.CONTENT))):
                    builder.PrependUint8(self.CONTENT[i])
                CONTENT = builder.EndVector()
        CQRNativeDocumentStart(builder)
        CQRNativeDocumentAddSERIALIZATION(builder, self.SERIALIZATION)
        if self.CONTENT is not None:
            CQRNativeDocumentAddCONTENT(builder, CONTENT)
        CQRNativeDocument = CQRNativeDocumentEnd(builder)
        return CQRNativeDocument
