# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Proves control of a DNS domain by the same HD wallet as the signing key —
# the EPM-record mirror of the DNS TXT wire form (which is deliberately
# NON-SDS: a base64 FlatBuffer would not fit a 255-byte TXT record). The
# canonical statement is 6 fixed LF-terminated lines prefixed
# "sdn-domain-proof/1", and it verifies by BYTE-REPLAY of the verbatim
# SIGNED_PAYLOAD (ChainProof precedent) — never by JCS.
class DomainProof(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DomainProof()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDomainProof(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DomainProofBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x50\x4D", size_prefixed=size_prefixed)

    # DomainProof
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Fully-qualified domain name the proof binds (e.g., "node.example.org")
    # DomainProof
    def DOMAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Public key for this proof (hex-encoded)
    # DomainProof
    def PUBLIC_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # BIP-32 / SLIP-10 derivation path of the proving key. The signer is the
    # Ed25519 EPM/publication key at m/44'/0'/0'/0'/0'
    # DomainProof
    def KEY_PATH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Signature over the attestation payload (hex-encoded)
    # DomainProof
    def SIGNATURE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The canonical payload that was signed, verbatim (hex-encoded)
    # DomainProof
    def SIGNED_PAYLOAD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Signature algorithm. ABSENT means ed25519
    # DomainProof
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Signature encoding format. ABSENT means the canonical encoding of
    # ALGORITHM (raw-ed25519 for ed25519)
    # DomainProof
    def ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def DomainProofStart(builder):
    builder.StartObject(7)

def Start(builder):
    DomainProofStart(builder)

def DomainProofAddDOMAIN(builder, DOMAIN):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(DOMAIN), 0)

def AddDOMAIN(builder, DOMAIN):
    DomainProofAddDOMAIN(builder, DOMAIN)

def DomainProofAddPUBLIC_KEY(builder, PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PUBLIC_KEY), 0)

def AddPUBLIC_KEY(builder, PUBLIC_KEY):
    DomainProofAddPUBLIC_KEY(builder, PUBLIC_KEY)

def DomainProofAddKEY_PATH(builder, KEY_PATH):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_PATH), 0)

def AddKEY_PATH(builder, KEY_PATH):
    DomainProofAddKEY_PATH(builder, KEY_PATH)

def DomainProofAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    DomainProofAddSIGNATURE(builder, SIGNATURE)

def DomainProofAddSIGNED_PAYLOAD(builder, SIGNED_PAYLOAD):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNED_PAYLOAD), 0)

def AddSIGNED_PAYLOAD(builder, SIGNED_PAYLOAD):
    DomainProofAddSIGNED_PAYLOAD(builder, SIGNED_PAYLOAD)

def DomainProofAddALGORITHM(builder, ALGORITHM):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ALGORITHM), 0)

def AddALGORITHM(builder, ALGORITHM):
    DomainProofAddALGORITHM(builder, ALGORITHM)

def DomainProofAddENCODING(builder, ENCODING):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ENCODING), 0)

def AddENCODING(builder, ENCODING):
    DomainProofAddENCODING(builder, ENCODING)

def DomainProofEnd(builder):
    return builder.EndObject()

def End(builder):
    return DomainProofEnd(builder)


class DomainProofT(object):

    # DomainProofT
    def __init__(
        self,
        DOMAIN = None,
        PUBLIC_KEY = None,
        KEY_PATH = None,
        SIGNATURE = None,
        SIGNED_PAYLOAD = None,
        ALGORITHM = None,
        ENCODING = None,
    ):
        self.DOMAIN = DOMAIN  # type: Optional[str]
        self.PUBLIC_KEY = PUBLIC_KEY  # type: Optional[str]
        self.KEY_PATH = KEY_PATH  # type: Optional[str]
        self.SIGNATURE = SIGNATURE  # type: Optional[str]
        self.SIGNED_PAYLOAD = SIGNED_PAYLOAD  # type: Optional[str]
        self.ALGORITHM = ALGORITHM  # type: Optional[str]
        self.ENCODING = ENCODING  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpDomainProof = DomainProof()
        tmpDomainProof.Init(buf, pos)
        return cls.InitFromObj(tmpDomainProof)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpDomainProof):
        x = DomainProofT()
        x._UnPack(tmpDomainProof)
        return x

    # DomainProofT
    def _UnPack(self, DomainProof):
        if DomainProof is None:
            return
        self.DOMAIN = DomainProof.DOMAIN()
        self.PUBLIC_KEY = DomainProof.PUBLIC_KEY()
        self.KEY_PATH = DomainProof.KEY_PATH()
        self.SIGNATURE = DomainProof.SIGNATURE()
        self.SIGNED_PAYLOAD = DomainProof.SIGNED_PAYLOAD()
        self.ALGORITHM = DomainProof.ALGORITHM()
        self.ENCODING = DomainProof.ENCODING()

    # DomainProofT
    def Pack(self, builder):
        if self.DOMAIN is not None:
            DOMAIN = builder.CreateString(self.DOMAIN)
        if self.PUBLIC_KEY is not None:
            PUBLIC_KEY = builder.CreateString(self.PUBLIC_KEY)
        if self.KEY_PATH is not None:
            KEY_PATH = builder.CreateString(self.KEY_PATH)
        if self.SIGNATURE is not None:
            SIGNATURE = builder.CreateString(self.SIGNATURE)
        if self.SIGNED_PAYLOAD is not None:
            SIGNED_PAYLOAD = builder.CreateString(self.SIGNED_PAYLOAD)
        if self.ALGORITHM is not None:
            ALGORITHM = builder.CreateString(self.ALGORITHM)
        if self.ENCODING is not None:
            ENCODING = builder.CreateString(self.ENCODING)
        DomainProofStart(builder)
        if self.DOMAIN is not None:
            DomainProofAddDOMAIN(builder, DOMAIN)
        if self.PUBLIC_KEY is not None:
            DomainProofAddPUBLIC_KEY(builder, PUBLIC_KEY)
        if self.KEY_PATH is not None:
            DomainProofAddKEY_PATH(builder, KEY_PATH)
        if self.SIGNATURE is not None:
            DomainProofAddSIGNATURE(builder, SIGNATURE)
        if self.SIGNED_PAYLOAD is not None:
            DomainProofAddSIGNED_PAYLOAD(builder, SIGNED_PAYLOAD)
        if self.ALGORITHM is not None:
            DomainProofAddALGORITHM(builder, ALGORITHM)
        if self.ENCODING is not None:
            DomainProofAddENCODING(builder, ENCODING)
        DomainProof = DomainProofEnd(builder)
        return DomainProof
