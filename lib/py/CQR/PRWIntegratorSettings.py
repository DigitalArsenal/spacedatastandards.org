# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Units are SI. A scalar legacy tolerance must be expanded by the adapter.
class PRWIntegratorSettings(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWIntegratorSettings()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWIntegratorSettings(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWIntegratorSettingsBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWIntegratorSettings
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWIntegratorSettings
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # PRWIntegratorSettings
    def INITIAL_STEP_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 60.0

    # PRWIntegratorSettings
    def MINIMUM_STEP_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1.0

    # PRWIntegratorSettings
    def MAXIMUM_STEP_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 3600.0

    # Six or seven positive tolerances, respectively m, m/s and optional kg.
    # PRWIntegratorSettings
    def ABSOLUTE_TOLERANCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PRWIntegratorSettings
    def ABSOLUTE_TOLERANCESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PRWIntegratorSettings
    def ABSOLUTE_TOLERANCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWIntegratorSettings
    def ABSOLUTE_TOLERANCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # PRWIntegratorSettings
    def RELATIVE_TOLERANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1e-12

    # PRWIntegratorSettings
    def MAXIMUM_STEPS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 100000

def PRWIntegratorSettingsStart(builder):
    builder.StartObject(7)

def Start(builder):
    PRWIntegratorSettingsStart(builder)

def PRWIntegratorSettingsAddALGORITHM(builder, ALGORITHM):
    builder.PrependUint8Slot(0, ALGORITHM, 0)

def AddALGORITHM(builder, ALGORITHM):
    PRWIntegratorSettingsAddALGORITHM(builder, ALGORITHM)

def PRWIntegratorSettingsAddINITIAL_STEP_SECONDS(builder, INITIAL_STEP_SECONDS):
    builder.PrependFloat64Slot(1, INITIAL_STEP_SECONDS, 60.0)

def AddINITIAL_STEP_SECONDS(builder, INITIAL_STEP_SECONDS):
    PRWIntegratorSettingsAddINITIAL_STEP_SECONDS(builder, INITIAL_STEP_SECONDS)

def PRWIntegratorSettingsAddMINIMUM_STEP_SECONDS(builder, MINIMUM_STEP_SECONDS):
    builder.PrependFloat64Slot(2, MINIMUM_STEP_SECONDS, 1.0)

def AddMINIMUM_STEP_SECONDS(builder, MINIMUM_STEP_SECONDS):
    PRWIntegratorSettingsAddMINIMUM_STEP_SECONDS(builder, MINIMUM_STEP_SECONDS)

def PRWIntegratorSettingsAddMAXIMUM_STEP_SECONDS(builder, MAXIMUM_STEP_SECONDS):
    builder.PrependFloat64Slot(3, MAXIMUM_STEP_SECONDS, 3600.0)

def AddMAXIMUM_STEP_SECONDS(builder, MAXIMUM_STEP_SECONDS):
    PRWIntegratorSettingsAddMAXIMUM_STEP_SECONDS(builder, MAXIMUM_STEP_SECONDS)

def PRWIntegratorSettingsAddABSOLUTE_TOLERANCES(builder, ABSOLUTE_TOLERANCES):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ABSOLUTE_TOLERANCES), 0)

def AddABSOLUTE_TOLERANCES(builder, ABSOLUTE_TOLERANCES):
    PRWIntegratorSettingsAddABSOLUTE_TOLERANCES(builder, ABSOLUTE_TOLERANCES)

def PRWIntegratorSettingsStartABSOLUTE_TOLERANCESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartABSOLUTE_TOLERANCESVector(builder, numElems):
    return PRWIntegratorSettingsStartABSOLUTE_TOLERANCESVector(builder, numElems)

def PRWIntegratorSettingsCreateABSOLUTE_TOLERANCESVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateABSOLUTE_TOLERANCESVector(builder, data):
    PRWIntegratorSettingsCreateABSOLUTE_TOLERANCESVector(builder, data)

def PRWIntegratorSettingsAddRELATIVE_TOLERANCE(builder, RELATIVE_TOLERANCE):
    builder.PrependFloat64Slot(5, RELATIVE_TOLERANCE, 1e-12)

def AddRELATIVE_TOLERANCE(builder, RELATIVE_TOLERANCE):
    PRWIntegratorSettingsAddRELATIVE_TOLERANCE(builder, RELATIVE_TOLERANCE)

def PRWIntegratorSettingsAddMAXIMUM_STEPS(builder, MAXIMUM_STEPS):
    builder.PrependUint32Slot(6, MAXIMUM_STEPS, 100000)

def AddMAXIMUM_STEPS(builder, MAXIMUM_STEPS):
    PRWIntegratorSettingsAddMAXIMUM_STEPS(builder, MAXIMUM_STEPS)

def PRWIntegratorSettingsEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWIntegratorSettingsEnd(builder)

try:
    from typing import List
except:
    pass

class PRWIntegratorSettingsT(object):

    # PRWIntegratorSettingsT
    def __init__(
        self,
        ALGORITHM = 0,
        INITIAL_STEP_SECONDS = 60.0,
        MINIMUM_STEP_SECONDS = 1.0,
        MAXIMUM_STEP_SECONDS = 3600.0,
        ABSOLUTE_TOLERANCES = None,
        RELATIVE_TOLERANCE = 1e-12,
        MAXIMUM_STEPS = 100000,
    ):
        self.ALGORITHM = ALGORITHM  # type: int
        self.INITIAL_STEP_SECONDS = INITIAL_STEP_SECONDS  # type: float
        self.MINIMUM_STEP_SECONDS = MINIMUM_STEP_SECONDS  # type: float
        self.MAXIMUM_STEP_SECONDS = MAXIMUM_STEP_SECONDS  # type: float
        self.ABSOLUTE_TOLERANCES = ABSOLUTE_TOLERANCES  # type: Optional[List[float]]
        self.RELATIVE_TOLERANCE = RELATIVE_TOLERANCE  # type: float
        self.MAXIMUM_STEPS = MAXIMUM_STEPS  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwintegratorSettings = PRWIntegratorSettings()
        tmpPrwintegratorSettings.Init(buf, pos)
        return cls.InitFromObj(tmpPrwintegratorSettings)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwintegratorSettings):
        x = PRWIntegratorSettingsT()
        x._UnPack(tmpPrwintegratorSettings)
        return x

    # PRWIntegratorSettingsT
    def _UnPack(self, PRWIntegratorSettings):
        if PRWIntegratorSettings is None:
            return
        self.ALGORITHM = PRWIntegratorSettings.ALGORITHM()
        self.INITIAL_STEP_SECONDS = PRWIntegratorSettings.INITIAL_STEP_SECONDS()
        self.MINIMUM_STEP_SECONDS = PRWIntegratorSettings.MINIMUM_STEP_SECONDS()
        self.MAXIMUM_STEP_SECONDS = PRWIntegratorSettings.MAXIMUM_STEP_SECONDS()
        if not PRWIntegratorSettings.ABSOLUTE_TOLERANCESIsNone():
            if np is None:
                self.ABSOLUTE_TOLERANCES = []
                for i in range(PRWIntegratorSettings.ABSOLUTE_TOLERANCESLength()):
                    self.ABSOLUTE_TOLERANCES.append(PRWIntegratorSettings.ABSOLUTE_TOLERANCES(i))
            else:
                self.ABSOLUTE_TOLERANCES = PRWIntegratorSettings.ABSOLUTE_TOLERANCESAsNumpy()
        self.RELATIVE_TOLERANCE = PRWIntegratorSettings.RELATIVE_TOLERANCE()
        self.MAXIMUM_STEPS = PRWIntegratorSettings.MAXIMUM_STEPS()

    # PRWIntegratorSettingsT
    def Pack(self, builder):
        if self.ABSOLUTE_TOLERANCES is not None:
            if np is not None and type(self.ABSOLUTE_TOLERANCES) is np.ndarray:
                ABSOLUTE_TOLERANCES = builder.CreateNumpyVector(self.ABSOLUTE_TOLERANCES)
            else:
                PRWIntegratorSettingsStartABSOLUTE_TOLERANCESVector(builder, len(self.ABSOLUTE_TOLERANCES))
                for i in reversed(range(len(self.ABSOLUTE_TOLERANCES))):
                    builder.PrependFloat64(self.ABSOLUTE_TOLERANCES[i])
                ABSOLUTE_TOLERANCES = builder.EndVector()
        PRWIntegratorSettingsStart(builder)
        PRWIntegratorSettingsAddALGORITHM(builder, self.ALGORITHM)
        PRWIntegratorSettingsAddINITIAL_STEP_SECONDS(builder, self.INITIAL_STEP_SECONDS)
        PRWIntegratorSettingsAddMINIMUM_STEP_SECONDS(builder, self.MINIMUM_STEP_SECONDS)
        PRWIntegratorSettingsAddMAXIMUM_STEP_SECONDS(builder, self.MAXIMUM_STEP_SECONDS)
        if self.ABSOLUTE_TOLERANCES is not None:
            PRWIntegratorSettingsAddABSOLUTE_TOLERANCES(builder, ABSOLUTE_TOLERANCES)
        PRWIntegratorSettingsAddRELATIVE_TOLERANCE(builder, self.RELATIVE_TOLERANCE)
        PRWIntegratorSettingsAddMAXIMUM_STEPS(builder, self.MAXIMUM_STEPS)
        PRWIntegratorSettings = PRWIntegratorSettingsEnd(builder)
        return PRWIntegratorSettings
