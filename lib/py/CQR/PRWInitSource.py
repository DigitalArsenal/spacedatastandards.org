# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PRWInitSource(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWInitSource()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWInitSource(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWInitSourceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWInitSource
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Wire kind identifier for BYTES.
    # PRWInitSource
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Optional SDS file_identifier for BYTES (`$OMM`, `$OCM`, `$OEM`, `$PPE`).
    # PRWInitSource
    def FILE_IDENTIFIER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Encoded source record as a FlatBuffer (consumed per KIND).
    # PRWInitSource
    def BYTES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # PRWInitSource
    def BYTESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # PRWInitSource
    def BYTESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWInitSource
    def BYTESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Convenience inline form when KIND == KEPLERIAN.
    # PRWInitSource
    def KEPLERIAN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWKeplerianElements import PRWKeplerianElements
            obj = PRWKeplerianElements()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Convenience inline form when KIND == TLE.
    # PRWInitSource
    def TLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWTleLines import PRWTleLines
            obj = PRWTleLines()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def PRWInitSourceStart(builder):
    builder.StartObject(5)

def Start(builder):
    PRWInitSourceStart(builder)

def PRWInitSourceAddKIND(builder, KIND):
    builder.PrependUint8Slot(0, KIND, 0)

def AddKIND(builder, KIND):
    PRWInitSourceAddKIND(builder, KIND)

def PRWInitSourceAddFILE_IDENTIFIER(builder, FILE_IDENTIFIER):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(FILE_IDENTIFIER), 0)

def AddFILE_IDENTIFIER(builder, FILE_IDENTIFIER):
    PRWInitSourceAddFILE_IDENTIFIER(builder, FILE_IDENTIFIER)

def PRWInitSourceAddBYTES(builder, BYTES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(BYTES), 0)

def AddBYTES(builder, BYTES):
    PRWInitSourceAddBYTES(builder, BYTES)

def PRWInitSourceStartBYTESVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartBYTESVector(builder, numElems):
    return PRWInitSourceStartBYTESVector(builder, numElems)

def PRWInitSourceCreateBYTESVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateBYTESVector(builder, data):
    PRWInitSourceCreateBYTESVector(builder, data)

def PRWInitSourceAddKEPLERIAN(builder, KEPLERIAN):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(KEPLERIAN), 0)

def AddKEPLERIAN(builder, KEPLERIAN):
    PRWInitSourceAddKEPLERIAN(builder, KEPLERIAN)

def PRWInitSourceAddTLE(builder, TLE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(TLE), 0)

def AddTLE(builder, TLE):
    PRWInitSourceAddTLE(builder, TLE)

def PRWInitSourceEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWInitSourceEnd(builder)

import PRWKeplerianElements
import PRWTleLines
try:
    from typing import List, Optional
except:
    pass

class PRWInitSourceT(object):

    # PRWInitSourceT
    def __init__(
        self,
        KIND = 0,
        FILE_IDENTIFIER = None,
        BYTES = None,
        KEPLERIAN = None,
        TLE = None,
    ):
        self.KIND = KIND  # type: int
        self.FILE_IDENTIFIER = FILE_IDENTIFIER  # type: Optional[str]
        self.BYTES = BYTES  # type: Optional[List[int]]
        self.KEPLERIAN = KEPLERIAN  # type: Optional[PRWKeplerianElements.PRWKeplerianElementsT]
        self.TLE = TLE  # type: Optional[PRWTleLines.PRWTleLinesT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwinitSource = PRWInitSource()
        tmpPrwinitSource.Init(buf, pos)
        return cls.InitFromObj(tmpPrwinitSource)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwinitSource):
        x = PRWInitSourceT()
        x._UnPack(tmpPrwinitSource)
        return x

    # PRWInitSourceT
    def _UnPack(self, PRWInitSource):
        if PRWInitSource is None:
            return
        self.KIND = PRWInitSource.KIND()
        self.FILE_IDENTIFIER = PRWInitSource.FILE_IDENTIFIER()
        if not PRWInitSource.BYTESIsNone():
            if np is None:
                self.BYTES = []
                for i in range(PRWInitSource.BYTESLength()):
                    self.BYTES.append(PRWInitSource.BYTES(i))
            else:
                self.BYTES = PRWInitSource.BYTESAsNumpy()
        if PRWInitSource.KEPLERIAN() is not None:
            self.KEPLERIAN = PRWKeplerianElements.PRWKeplerianElementsT.InitFromObj(PRWInitSource.KEPLERIAN())
        if PRWInitSource.TLE() is not None:
            self.TLE = PRWTleLines.PRWTleLinesT.InitFromObj(PRWInitSource.TLE())

    # PRWInitSourceT
    def Pack(self, builder):
        if self.FILE_IDENTIFIER is not None:
            FILE_IDENTIFIER = builder.CreateString(self.FILE_IDENTIFIER)
        if self.BYTES is not None:
            if np is not None and type(self.BYTES) is np.ndarray:
                BYTES = builder.CreateNumpyVector(self.BYTES)
            else:
                PRWInitSourceStartBYTESVector(builder, len(self.BYTES))
                for i in reversed(range(len(self.BYTES))):
                    builder.PrependUint8(self.BYTES[i])
                BYTES = builder.EndVector()
        if self.KEPLERIAN is not None:
            KEPLERIAN = self.KEPLERIAN.Pack(builder)
        if self.TLE is not None:
            TLE = self.TLE.Pack(builder)
        PRWInitSourceStart(builder)
        PRWInitSourceAddKIND(builder, self.KIND)
        if self.FILE_IDENTIFIER is not None:
            PRWInitSourceAddFILE_IDENTIFIER(builder, FILE_IDENTIFIER)
        if self.BYTES is not None:
            PRWInitSourceAddBYTES(builder, BYTES)
        if self.KEPLERIAN is not None:
            PRWInitSourceAddKEPLERIAN(builder, KEPLERIAN)
        if self.TLE is not None:
            PRWInitSourceAddTLE(builder, TLE)
        PRWInitSource = PRWInitSourceEnd(builder)
        return PRWInitSource
