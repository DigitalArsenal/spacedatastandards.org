# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Asset admitted by a value or token predicate.
class TRPAsset(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRPAsset()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRPAsset(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRPAssetBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x50", size_prefixed=size_prefixed)

    # TRPAsset
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CAIP-2 style chain identifier, e.g. "eip155:1" or
    # "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp". Vendor and organization names
    # are forbidden; a chain is named only by its identifier.
    # TRPAsset
    def CHAIN_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Chain-local token address, or empty for the chain's native asset.
    # TRPAsset
    def TOKEN_ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Published token symbol used for display and matching.
    # TRPAsset
    def TOKEN_SYMBOL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of decimal places used by the smallest-unit integer balance.
    # TRPAsset
    def DECIMALS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def TRPAssetStart(builder):
    builder.StartObject(4)

def Start(builder):
    TRPAssetStart(builder)

def TRPAssetAddCHAIN_ID(builder, CHAIN_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CHAIN_ID), 0)

def AddCHAIN_ID(builder, CHAIN_ID):
    TRPAssetAddCHAIN_ID(builder, CHAIN_ID)

def TRPAssetAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(TOKEN_ADDRESS), 0)

def AddTOKEN_ADDRESS(builder, TOKEN_ADDRESS):
    TRPAssetAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS)

def TRPAssetAddTOKEN_SYMBOL(builder, TOKEN_SYMBOL):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TOKEN_SYMBOL), 0)

def AddTOKEN_SYMBOL(builder, TOKEN_SYMBOL):
    TRPAssetAddTOKEN_SYMBOL(builder, TOKEN_SYMBOL)

def TRPAssetAddDECIMALS(builder, DECIMALS):
    builder.PrependUint8Slot(3, DECIMALS, 0)

def AddDECIMALS(builder, DECIMALS):
    TRPAssetAddDECIMALS(builder, DECIMALS)

def TRPAssetEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRPAssetEnd(builder)


class TRPAssetT(object):

    # TRPAssetT
    def __init__(
        self,
        CHAIN_ID = None,
        TOKEN_ADDRESS = None,
        TOKEN_SYMBOL = None,
        DECIMALS = 0,
    ):
        self.CHAIN_ID = CHAIN_ID  # type: Optional[str]
        self.TOKEN_ADDRESS = TOKEN_ADDRESS  # type: Optional[str]
        self.TOKEN_SYMBOL = TOKEN_SYMBOL  # type: Optional[str]
        self.DECIMALS = DECIMALS  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrpasset = TRPAsset()
        tmpTrpasset.Init(buf, pos)
        return cls.InitFromObj(tmpTrpasset)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrpasset):
        x = TRPAssetT()
        x._UnPack(tmpTrpasset)
        return x

    # TRPAssetT
    def _UnPack(self, TRPAsset):
        if TRPAsset is None:
            return
        self.CHAIN_ID = TRPAsset.CHAIN_ID()
        self.TOKEN_ADDRESS = TRPAsset.TOKEN_ADDRESS()
        self.TOKEN_SYMBOL = TRPAsset.TOKEN_SYMBOL()
        self.DECIMALS = TRPAsset.DECIMALS()

    # TRPAssetT
    def Pack(self, builder):
        if self.CHAIN_ID is not None:
            CHAIN_ID = builder.CreateString(self.CHAIN_ID)
        if self.TOKEN_ADDRESS is not None:
            TOKEN_ADDRESS = builder.CreateString(self.TOKEN_ADDRESS)
        if self.TOKEN_SYMBOL is not None:
            TOKEN_SYMBOL = builder.CreateString(self.TOKEN_SYMBOL)
        TRPAssetStart(builder)
        if self.CHAIN_ID is not None:
            TRPAssetAddCHAIN_ID(builder, CHAIN_ID)
        if self.TOKEN_ADDRESS is not None:
            TRPAssetAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS)
        if self.TOKEN_SYMBOL is not None:
            TRPAssetAddTOKEN_SYMBOL(builder, TOKEN_SYMBOL)
        TRPAssetAddDECIMALS(builder, self.DECIMALS)
        TRPAsset = TRPAssetEnd(builder)
        return TRPAsset
