# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Recursive boolean group for a compound trust rule set.
class TRPGroup(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRPGroup()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRPGroup(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRPGroupBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x50", size_prefixed=size_prefixed)

    # TRPGroup
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier unique within the policy.
    # TRPGroup
    def GROUP_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Boolean operation applied across direct predicates and child groups.
    # TRPGroup
    def COMBINATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Predicates evaluated directly within this group.
    # TRPGroup
    def PREDICATES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TRPPredicate import TRPPredicate
            obj = TRPPredicate()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TRPGroup
    def PREDICATESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRPGroup
    def PREDICATESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Nested groups used to express compound rule sets.
    # TRPGroup
    def GROUPS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TRPGroup import TRPGroup
            obj = TRPGroup()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TRPGroup
    def GROUPSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRPGroup
    def GROUPSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def TRPGroupStart(builder):
    builder.StartObject(4)

def Start(builder):
    TRPGroupStart(builder)

def TRPGroupAddGROUP_ID(builder, GROUP_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(GROUP_ID), 0)

def AddGROUP_ID(builder, GROUP_ID):
    TRPGroupAddGROUP_ID(builder, GROUP_ID)

def TRPGroupAddCOMBINATOR(builder, COMBINATOR):
    builder.PrependInt8Slot(1, COMBINATOR, 0)

def AddCOMBINATOR(builder, COMBINATOR):
    TRPGroupAddCOMBINATOR(builder, COMBINATOR)

def TRPGroupAddPREDICATES(builder, PREDICATES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PREDICATES), 0)

def AddPREDICATES(builder, PREDICATES):
    TRPGroupAddPREDICATES(builder, PREDICATES)

def TRPGroupStartPREDICATESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPREDICATESVector(builder, numElems):
    return TRPGroupStartPREDICATESVector(builder, numElems)

def TRPGroupCreatePREDICATESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePREDICATESVector(builder, data):
    TRPGroupCreatePREDICATESVector(builder, data)

def TRPGroupAddGROUPS(builder, GROUPS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(GROUPS), 0)

def AddGROUPS(builder, GROUPS):
    TRPGroupAddGROUPS(builder, GROUPS)

def TRPGroupStartGROUPSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartGROUPSVector(builder, numElems):
    return TRPGroupStartGROUPSVector(builder, numElems)

def TRPGroupCreateGROUPSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateGROUPSVector(builder, data):
    TRPGroupCreateGROUPSVector(builder, data)

def TRPGroupEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRPGroupEnd(builder)

import TRPPredicate
try:
    from typing import List
except:
    pass

class TRPGroupT(object):

    # TRPGroupT
    def __init__(
        self,
        GROUP_ID = None,
        COMBINATOR = 0,
        PREDICATES = None,
        GROUPS = None,
    ):
        self.GROUP_ID = GROUP_ID  # type: Optional[str]
        self.COMBINATOR = COMBINATOR  # type: int
        self.PREDICATES = PREDICATES  # type: Optional[List[TRPPredicate.TRPPredicateT]]
        self.GROUPS = GROUPS  # type: Optional[List[TRPGroup.TRPGroupT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrpgroup = TRPGroup()
        tmpTrpgroup.Init(buf, pos)
        return cls.InitFromObj(tmpTrpgroup)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrpgroup):
        x = TRPGroupT()
        x._UnPack(tmpTrpgroup)
        return x

    # TRPGroupT
    def _UnPack(self, TRPGroup):
        if TRPGroup is None:
            return
        self.GROUP_ID = TRPGroup.GROUP_ID()
        self.COMBINATOR = TRPGroup.COMBINATOR()
        if not TRPGroup.PREDICATESIsNone():
            self.PREDICATES = []
            for i in range(TRPGroup.PREDICATESLength()):
                if TRPGroup.PREDICATES(i) is None:
                    self.PREDICATES.append(None)
                else:
                    tRPPredicate_ = TRPPredicate.TRPPredicateT.InitFromObj(TRPGroup.PREDICATES(i))
                    self.PREDICATES.append(tRPPredicate_)
        if not TRPGroup.GROUPSIsNone():
            self.GROUPS = []
            for i in range(TRPGroup.GROUPSLength()):
                if TRPGroup.GROUPS(i) is None:
                    self.GROUPS.append(None)
                else:
                    tRPGroup_ = TRPGroup.TRPGroupT.InitFromObj(TRPGroup.GROUPS(i))
                    self.GROUPS.append(tRPGroup_)

    # TRPGroupT
    def Pack(self, builder):
        if self.GROUP_ID is not None:
            GROUP_ID = builder.CreateString(self.GROUP_ID)
        if self.PREDICATES is not None:
            PREDICATESlist = []
            for i in range(len(self.PREDICATES)):
                PREDICATESlist.append(self.PREDICATES[i].Pack(builder))
            TRPGroupStartPREDICATESVector(builder, len(self.PREDICATES))
            for i in reversed(range(len(self.PREDICATES))):
                builder.PrependUOffsetTRelative(PREDICATESlist[i])
            PREDICATES = builder.EndVector()
        if self.GROUPS is not None:
            GROUPSlist = []
            for i in range(len(self.GROUPS)):
                GROUPSlist.append(self.GROUPS[i].Pack(builder))
            TRPGroupStartGROUPSVector(builder, len(self.GROUPS))
            for i in reversed(range(len(self.GROUPS))):
                builder.PrependUOffsetTRelative(GROUPSlist[i])
            GROUPS = builder.EndVector()
        TRPGroupStart(builder)
        if self.GROUP_ID is not None:
            TRPGroupAddGROUP_ID(builder, GROUP_ID)
        TRPGroupAddCOMBINATOR(builder, self.COMBINATOR)
        if self.PREDICATES is not None:
            TRPGroupAddPREDICATES(builder, PREDICATES)
        if self.GROUPS is not None:
            TRPGroupAddGROUPS(builder, GROUPS)
        TRPGroup = TRPGroupEnd(builder)
        return TRPGroup
