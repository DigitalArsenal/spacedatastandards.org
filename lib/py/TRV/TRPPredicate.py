# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One independently measurable predicate in a trust rule policy.
class TRPPredicate(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRPPredicate()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRPPredicate(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRPPredicateBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x50", size_prefixed=size_prefixed)

    # TRPPredicate
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier unique within the policy.
    # TRPPredicate
    def PREDICATE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Predicate operation to evaluate.
    # TRPPredicate
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Minimum qualifying value expressed as a uint64 in the smallest unit.
    # TRPPredicate
    def MIN_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Currency or unit used to compare normalized values.
    # TRPPredicate
    def VALUE_CURRENCY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Minimum continuous holding duration in seconds.
    # TRPPredicate
    def MIN_HELD_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Assets admitted by this predicate.
    # TRPPredicate
    def ASSETS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TRPAsset import TRPAsset
            obj = TRPAsset()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TRPPredicate
    def ASSETSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRPPredicate
    def ASSETSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # X, the number of named trusters that must match.
    # TRPPredicate
    def REQUIRED_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # The N named `$TRE` trusters considered by a trusted-connections predicate.
    # TRPPredicate
    def TRUSTER_IDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # TRPPredicate
    def TRUSTER_IDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRPPredicate
    def TRUSTER_IDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Minimum accepted `$TRE.WEIGHT` for a matching trust edge.
    # TRPPredicate
    def MIN_EDGE_WEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def TRPPredicateStart(builder):
    builder.StartObject(9)

def Start(builder):
    TRPPredicateStart(builder)

def TRPPredicateAddPREDICATE_ID(builder, PREDICATE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PREDICATE_ID), 0)

def AddPREDICATE_ID(builder, PREDICATE_ID):
    TRPPredicateAddPREDICATE_ID(builder, PREDICATE_ID)

def TRPPredicateAddKIND(builder, KIND):
    builder.PrependInt8Slot(1, KIND, 0)

def AddKIND(builder, KIND):
    TRPPredicateAddKIND(builder, KIND)

def TRPPredicateAddMIN_VALUE(builder, MIN_VALUE):
    builder.PrependUint64Slot(2, MIN_VALUE, 0)

def AddMIN_VALUE(builder, MIN_VALUE):
    TRPPredicateAddMIN_VALUE(builder, MIN_VALUE)

def TRPPredicateAddVALUE_CURRENCY(builder, VALUE_CURRENCY):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(VALUE_CURRENCY), 0)

def AddVALUE_CURRENCY(builder, VALUE_CURRENCY):
    TRPPredicateAddVALUE_CURRENCY(builder, VALUE_CURRENCY)

def TRPPredicateAddMIN_HELD_SECONDS(builder, MIN_HELD_SECONDS):
    builder.PrependUint64Slot(4, MIN_HELD_SECONDS, 0)

def AddMIN_HELD_SECONDS(builder, MIN_HELD_SECONDS):
    TRPPredicateAddMIN_HELD_SECONDS(builder, MIN_HELD_SECONDS)

def TRPPredicateAddASSETS(builder, ASSETS):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ASSETS), 0)

def AddASSETS(builder, ASSETS):
    TRPPredicateAddASSETS(builder, ASSETS)

def TRPPredicateStartASSETSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartASSETSVector(builder, numElems):
    return TRPPredicateStartASSETSVector(builder, numElems)

def TRPPredicateCreateASSETSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateASSETSVector(builder, data):
    TRPPredicateCreateASSETSVector(builder, data)

def TRPPredicateAddREQUIRED_COUNT(builder, REQUIRED_COUNT):
    builder.PrependUint32Slot(6, REQUIRED_COUNT, 0)

def AddREQUIRED_COUNT(builder, REQUIRED_COUNT):
    TRPPredicateAddREQUIRED_COUNT(builder, REQUIRED_COUNT)

def TRPPredicateAddTRUSTER_IDS(builder, TRUSTER_IDS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(TRUSTER_IDS), 0)

def AddTRUSTER_IDS(builder, TRUSTER_IDS):
    TRPPredicateAddTRUSTER_IDS(builder, TRUSTER_IDS)

def TRPPredicateStartTRUSTER_IDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTRUSTER_IDSVector(builder, numElems):
    return TRPPredicateStartTRUSTER_IDSVector(builder, numElems)

def TRPPredicateCreateTRUSTER_IDSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateTRUSTER_IDSVector(builder, data):
    TRPPredicateCreateTRUSTER_IDSVector(builder, data)

def TRPPredicateAddMIN_EDGE_WEIGHT(builder, MIN_EDGE_WEIGHT):
    builder.PrependFloat64Slot(8, MIN_EDGE_WEIGHT, 0.0)

def AddMIN_EDGE_WEIGHT(builder, MIN_EDGE_WEIGHT):
    TRPPredicateAddMIN_EDGE_WEIGHT(builder, MIN_EDGE_WEIGHT)

def TRPPredicateEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRPPredicateEnd(builder)

import TRPAsset
try:
    from typing import List
except:
    pass

class TRPPredicateT(object):

    # TRPPredicateT
    def __init__(
        self,
        PREDICATE_ID = None,
        KIND = 0,
        MIN_VALUE = 0,
        VALUE_CURRENCY = None,
        MIN_HELD_SECONDS = 0,
        ASSETS = None,
        REQUIRED_COUNT = 0,
        TRUSTER_IDS = None,
        MIN_EDGE_WEIGHT = 0.0,
    ):
        self.PREDICATE_ID = PREDICATE_ID  # type: Optional[str]
        self.KIND = KIND  # type: int
        self.MIN_VALUE = MIN_VALUE  # type: int
        self.VALUE_CURRENCY = VALUE_CURRENCY  # type: Optional[str]
        self.MIN_HELD_SECONDS = MIN_HELD_SECONDS  # type: int
        self.ASSETS = ASSETS  # type: Optional[List[TRPAsset.TRPAssetT]]
        self.REQUIRED_COUNT = REQUIRED_COUNT  # type: int
        self.TRUSTER_IDS = TRUSTER_IDS  # type: Optional[List[Optional[str]]]
        self.MIN_EDGE_WEIGHT = MIN_EDGE_WEIGHT  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrppredicate = TRPPredicate()
        tmpTrppredicate.Init(buf, pos)
        return cls.InitFromObj(tmpTrppredicate)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrppredicate):
        x = TRPPredicateT()
        x._UnPack(tmpTrppredicate)
        return x

    # TRPPredicateT
    def _UnPack(self, TRPPredicate):
        if TRPPredicate is None:
            return
        self.PREDICATE_ID = TRPPredicate.PREDICATE_ID()
        self.KIND = TRPPredicate.KIND()
        self.MIN_VALUE = TRPPredicate.MIN_VALUE()
        self.VALUE_CURRENCY = TRPPredicate.VALUE_CURRENCY()
        self.MIN_HELD_SECONDS = TRPPredicate.MIN_HELD_SECONDS()
        if not TRPPredicate.ASSETSIsNone():
            self.ASSETS = []
            for i in range(TRPPredicate.ASSETSLength()):
                if TRPPredicate.ASSETS(i) is None:
                    self.ASSETS.append(None)
                else:
                    tRPAsset_ = TRPAsset.TRPAssetT.InitFromObj(TRPPredicate.ASSETS(i))
                    self.ASSETS.append(tRPAsset_)
        self.REQUIRED_COUNT = TRPPredicate.REQUIRED_COUNT()
        if not TRPPredicate.TRUSTER_IDSIsNone():
            self.TRUSTER_IDS = []
            for i in range(TRPPredicate.TRUSTER_IDSLength()):
                self.TRUSTER_IDS.append(TRPPredicate.TRUSTER_IDS(i))
        self.MIN_EDGE_WEIGHT = TRPPredicate.MIN_EDGE_WEIGHT()

    # TRPPredicateT
    def Pack(self, builder):
        if self.PREDICATE_ID is not None:
            PREDICATE_ID = builder.CreateString(self.PREDICATE_ID)
        if self.VALUE_CURRENCY is not None:
            VALUE_CURRENCY = builder.CreateString(self.VALUE_CURRENCY)
        if self.ASSETS is not None:
            ASSETSlist = []
            for i in range(len(self.ASSETS)):
                ASSETSlist.append(self.ASSETS[i].Pack(builder))
            TRPPredicateStartASSETSVector(builder, len(self.ASSETS))
            for i in reversed(range(len(self.ASSETS))):
                builder.PrependUOffsetTRelative(ASSETSlist[i])
            ASSETS = builder.EndVector()
        if self.TRUSTER_IDS is not None:
            TRUSTER_IDSlist = []
            for i in range(len(self.TRUSTER_IDS)):
                TRUSTER_IDSlist.append(builder.CreateString(self.TRUSTER_IDS[i]))
            TRPPredicateStartTRUSTER_IDSVector(builder, len(self.TRUSTER_IDS))
            for i in reversed(range(len(self.TRUSTER_IDS))):
                builder.PrependUOffsetTRelative(TRUSTER_IDSlist[i])
            TRUSTER_IDS = builder.EndVector()
        TRPPredicateStart(builder)
        if self.PREDICATE_ID is not None:
            TRPPredicateAddPREDICATE_ID(builder, PREDICATE_ID)
        TRPPredicateAddKIND(builder, self.KIND)
        TRPPredicateAddMIN_VALUE(builder, self.MIN_VALUE)
        if self.VALUE_CURRENCY is not None:
            TRPPredicateAddVALUE_CURRENCY(builder, VALUE_CURRENCY)
        TRPPredicateAddMIN_HELD_SECONDS(builder, self.MIN_HELD_SECONDS)
        if self.ASSETS is not None:
            TRPPredicateAddASSETS(builder, ASSETS)
        TRPPredicateAddREQUIRED_COUNT(builder, self.REQUIRED_COUNT)
        if self.TRUSTER_IDS is not None:
            TRPPredicateAddTRUSTER_IDS(builder, TRUSTER_IDS)
        TRPPredicateAddMIN_EDGE_WEIGHT(builder, self.MIN_EDGE_WEIGHT)
        TRPPredicate = TRPPredicateEnd(builder)
        return TRPPredicate
