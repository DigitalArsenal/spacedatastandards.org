# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Chain evidence supporting a value-based predicate result.
class TRVBondEvidence(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRVBondEvidence()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRVBondEvidence(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRVBondEvidenceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x56", size_prefixed=size_prefixed)

    # TRVBondEvidence
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Chain identifier using the same identifier-only convention as TRPAsset.
    # TRVBondEvidence
    def CHAIN_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Chain-local address whose balance was evaluated.
    # TRVBondEvidence
    def ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Chain-local token address, or empty for the chain's native asset.
    # TRVBondEvidence
    def TOKEN_ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Observed uint64 balance expressed in the asset's smallest unit.
    # TRVBondEvidence
    def BALANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Number of decimal places used by BALANCE.
    # TRVBondEvidence
    def DECIMALS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Currency or unit used for normalized valuation.
    # TRVBondEvidence
    def VALUE_CURRENCY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Balance normalized into VALUE_CURRENCY.
    # TRVBondEvidence
    def NORMALIZED_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Unix timestamp in milliseconds from which the balance is proven to have
    # been continuously held, established from chain history.
    # TRVBondEvidence
    def HELD_SINCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix timestamp in milliseconds when the balance was observed.
    # TRVBondEvidence
    def OBSERVED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Chain-specific block identifier anchoring the observation.
    # TRVBondEvidence
    def BLOCK_REFERENCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Replayable query or query description used to obtain the evidence.
    # TRVBondEvidence
    def SOURCE_QUERY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TRVBondEvidenceStart(builder):
    builder.StartObject(11)

def Start(builder):
    TRVBondEvidenceStart(builder)

def TRVBondEvidenceAddCHAIN_ID(builder, CHAIN_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CHAIN_ID), 0)

def AddCHAIN_ID(builder, CHAIN_ID):
    TRVBondEvidenceAddCHAIN_ID(builder, CHAIN_ID)

def TRVBondEvidenceAddADDRESS(builder, ADDRESS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ADDRESS), 0)

def AddADDRESS(builder, ADDRESS):
    TRVBondEvidenceAddADDRESS(builder, ADDRESS)

def TRVBondEvidenceAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TOKEN_ADDRESS), 0)

def AddTOKEN_ADDRESS(builder, TOKEN_ADDRESS):
    TRVBondEvidenceAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS)

def TRVBondEvidenceAddBALANCE(builder, BALANCE):
    builder.PrependUint64Slot(3, BALANCE, 0)

def AddBALANCE(builder, BALANCE):
    TRVBondEvidenceAddBALANCE(builder, BALANCE)

def TRVBondEvidenceAddDECIMALS(builder, DECIMALS):
    builder.PrependUint8Slot(4, DECIMALS, 0)

def AddDECIMALS(builder, DECIMALS):
    TRVBondEvidenceAddDECIMALS(builder, DECIMALS)

def TRVBondEvidenceAddVALUE_CURRENCY(builder, VALUE_CURRENCY):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(VALUE_CURRENCY), 0)

def AddVALUE_CURRENCY(builder, VALUE_CURRENCY):
    TRVBondEvidenceAddVALUE_CURRENCY(builder, VALUE_CURRENCY)

def TRVBondEvidenceAddNORMALIZED_VALUE(builder, NORMALIZED_VALUE):
    builder.PrependFloat64Slot(6, NORMALIZED_VALUE, 0.0)

def AddNORMALIZED_VALUE(builder, NORMALIZED_VALUE):
    TRVBondEvidenceAddNORMALIZED_VALUE(builder, NORMALIZED_VALUE)

def TRVBondEvidenceAddHELD_SINCE(builder, HELD_SINCE):
    builder.PrependUint64Slot(7, HELD_SINCE, 0)

def AddHELD_SINCE(builder, HELD_SINCE):
    TRVBondEvidenceAddHELD_SINCE(builder, HELD_SINCE)

def TRVBondEvidenceAddOBSERVED_AT(builder, OBSERVED_AT):
    builder.PrependUint64Slot(8, OBSERVED_AT, 0)

def AddOBSERVED_AT(builder, OBSERVED_AT):
    TRVBondEvidenceAddOBSERVED_AT(builder, OBSERVED_AT)

def TRVBondEvidenceAddBLOCK_REFERENCE(builder, BLOCK_REFERENCE):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(BLOCK_REFERENCE), 0)

def AddBLOCK_REFERENCE(builder, BLOCK_REFERENCE):
    TRVBondEvidenceAddBLOCK_REFERENCE(builder, BLOCK_REFERENCE)

def TRVBondEvidenceAddSOURCE_QUERY(builder, SOURCE_QUERY):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_QUERY), 0)

def AddSOURCE_QUERY(builder, SOURCE_QUERY):
    TRVBondEvidenceAddSOURCE_QUERY(builder, SOURCE_QUERY)

def TRVBondEvidenceEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRVBondEvidenceEnd(builder)


class TRVBondEvidenceT(object):

    # TRVBondEvidenceT
    def __init__(
        self,
        CHAIN_ID = None,
        ADDRESS = None,
        TOKEN_ADDRESS = None,
        BALANCE = 0,
        DECIMALS = 0,
        VALUE_CURRENCY = None,
        NORMALIZED_VALUE = 0.0,
        HELD_SINCE = 0,
        OBSERVED_AT = 0,
        BLOCK_REFERENCE = None,
        SOURCE_QUERY = None,
    ):
        self.CHAIN_ID = CHAIN_ID  # type: Optional[str]
        self.ADDRESS = ADDRESS  # type: Optional[str]
        self.TOKEN_ADDRESS = TOKEN_ADDRESS  # type: Optional[str]
        self.BALANCE = BALANCE  # type: int
        self.DECIMALS = DECIMALS  # type: int
        self.VALUE_CURRENCY = VALUE_CURRENCY  # type: Optional[str]
        self.NORMALIZED_VALUE = NORMALIZED_VALUE  # type: float
        self.HELD_SINCE = HELD_SINCE  # type: int
        self.OBSERVED_AT = OBSERVED_AT  # type: int
        self.BLOCK_REFERENCE = BLOCK_REFERENCE  # type: Optional[str]
        self.SOURCE_QUERY = SOURCE_QUERY  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrvbondEvidence = TRVBondEvidence()
        tmpTrvbondEvidence.Init(buf, pos)
        return cls.InitFromObj(tmpTrvbondEvidence)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrvbondEvidence):
        x = TRVBondEvidenceT()
        x._UnPack(tmpTrvbondEvidence)
        return x

    # TRVBondEvidenceT
    def _UnPack(self, TRVBondEvidence):
        if TRVBondEvidence is None:
            return
        self.CHAIN_ID = TRVBondEvidence.CHAIN_ID()
        self.ADDRESS = TRVBondEvidence.ADDRESS()
        self.TOKEN_ADDRESS = TRVBondEvidence.TOKEN_ADDRESS()
        self.BALANCE = TRVBondEvidence.BALANCE()
        self.DECIMALS = TRVBondEvidence.DECIMALS()
        self.VALUE_CURRENCY = TRVBondEvidence.VALUE_CURRENCY()
        self.NORMALIZED_VALUE = TRVBondEvidence.NORMALIZED_VALUE()
        self.HELD_SINCE = TRVBondEvidence.HELD_SINCE()
        self.OBSERVED_AT = TRVBondEvidence.OBSERVED_AT()
        self.BLOCK_REFERENCE = TRVBondEvidence.BLOCK_REFERENCE()
        self.SOURCE_QUERY = TRVBondEvidence.SOURCE_QUERY()

    # TRVBondEvidenceT
    def Pack(self, builder):
        if self.CHAIN_ID is not None:
            CHAIN_ID = builder.CreateString(self.CHAIN_ID)
        if self.ADDRESS is not None:
            ADDRESS = builder.CreateString(self.ADDRESS)
        if self.TOKEN_ADDRESS is not None:
            TOKEN_ADDRESS = builder.CreateString(self.TOKEN_ADDRESS)
        if self.VALUE_CURRENCY is not None:
            VALUE_CURRENCY = builder.CreateString(self.VALUE_CURRENCY)
        if self.BLOCK_REFERENCE is not None:
            BLOCK_REFERENCE = builder.CreateString(self.BLOCK_REFERENCE)
        if self.SOURCE_QUERY is not None:
            SOURCE_QUERY = builder.CreateString(self.SOURCE_QUERY)
        TRVBondEvidenceStart(builder)
        if self.CHAIN_ID is not None:
            TRVBondEvidenceAddCHAIN_ID(builder, CHAIN_ID)
        if self.ADDRESS is not None:
            TRVBondEvidenceAddADDRESS(builder, ADDRESS)
        if self.TOKEN_ADDRESS is not None:
            TRVBondEvidenceAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS)
        TRVBondEvidenceAddBALANCE(builder, self.BALANCE)
        TRVBondEvidenceAddDECIMALS(builder, self.DECIMALS)
        if self.VALUE_CURRENCY is not None:
            TRVBondEvidenceAddVALUE_CURRENCY(builder, VALUE_CURRENCY)
        TRVBondEvidenceAddNORMALIZED_VALUE(builder, self.NORMALIZED_VALUE)
        TRVBondEvidenceAddHELD_SINCE(builder, self.HELD_SINCE)
        TRVBondEvidenceAddOBSERVED_AT(builder, self.OBSERVED_AT)
        if self.BLOCK_REFERENCE is not None:
            TRVBondEvidenceAddBLOCK_REFERENCE(builder, BLOCK_REFERENCE)
        if self.SOURCE_QUERY is not None:
            TRVBondEvidenceAddSOURCE_QUERY(builder, SOURCE_QUERY)
        TRVBondEvidence = TRVBondEvidenceEnd(builder)
        return TRVBondEvidence
