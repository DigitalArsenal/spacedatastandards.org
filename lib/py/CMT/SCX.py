# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Chain Settlement / Smart-Contract Descriptor. The forward description of the
# exact established splitter contract/program a buyer's wallet targets so
# payment and platform commission settle atomically on-chain. Referenced by
# `$CMT.CONTRACTS` and echoed into a store `checkout` quote. Commission is
# realized by the splitter contract itself when the buyer pays it; there is no
# off-chain or manual settlement step.
class SCX(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCX()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCX(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCXBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x58", size_prefixed=size_prefixed)

    # SCX
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Settlement chain identifier, e.g. "ethereum", "solana", "bitcoin"
    # (matches `$PUR.PAYMENT_CHAIN`)
    # SCX
    def CHAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # EVM chainId; 0 for non-EVM chains
    # SCX
    def CHAIN_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # On-chain settlement mechanism
    # SCX
    def SETTLEMENT_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Established split standard id, e.g. "0xsplits.v2" (ETH) or the SOL
    # splitter program name. An audited off-the-shelf standard, not a custom
    # contract.
    # SCX
    def SPLITTER_STANDARD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Deployed splitter contract address (ETH) or splitter program id (SOL) the
    # buyer's payment routes into
    # SCX
    def SPLITTER_ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The specific split configuration (recipient set + basis points) registered
    # with the splitter
    # SCX
    def SPLIT_CONFIG_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ERC-20 / SPL mint address for stablecoin settlement; empty = native asset
    # SCX
    def TOKEN_ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Token symbol, e.g. "USDC"
    # SCX
    def TOKEN_SYMBOL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Token decimals, e.g. 6 for USDC
    # SCX
    def TOKEN_DECIMALS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # The recipient set the split configuration encodes (platform + publisher…),
    # mirrored here for independent verification
    # SCX
    def SPLIT_RECIPIENTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SplitRecipient import SplitRecipient
            obj = SplitRecipient()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCX
    def SPLIT_RECIPIENTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCX
    def SPLIT_RECIPIENTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # Confirmations required before the payment is accepted
    # SCX
    def MIN_CONFIRMATIONS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # How the order reference is bound on-chain for replay-safe matching
    # SCX
    def MEMO_SCHEME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def SCXStart(builder):
    builder.StartObject(12)

def Start(builder):
    SCXStart(builder)

def SCXAddCHAIN(builder, CHAIN):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CHAIN), 0)

def AddCHAIN(builder, CHAIN):
    SCXAddCHAIN(builder, CHAIN)

def SCXAddCHAIN_ID(builder, CHAIN_ID):
    builder.PrependUint64Slot(1, CHAIN_ID, 0)

def AddCHAIN_ID(builder, CHAIN_ID):
    SCXAddCHAIN_ID(builder, CHAIN_ID)

def SCXAddSETTLEMENT_KIND(builder, SETTLEMENT_KIND):
    builder.PrependInt8Slot(2, SETTLEMENT_KIND, 0)

def AddSETTLEMENT_KIND(builder, SETTLEMENT_KIND):
    SCXAddSETTLEMENT_KIND(builder, SETTLEMENT_KIND)

def SCXAddSPLITTER_STANDARD(builder, SPLITTER_STANDARD):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SPLITTER_STANDARD), 0)

def AddSPLITTER_STANDARD(builder, SPLITTER_STANDARD):
    SCXAddSPLITTER_STANDARD(builder, SPLITTER_STANDARD)

def SCXAddSPLITTER_ADDRESS(builder, SPLITTER_ADDRESS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SPLITTER_ADDRESS), 0)

def AddSPLITTER_ADDRESS(builder, SPLITTER_ADDRESS):
    SCXAddSPLITTER_ADDRESS(builder, SPLITTER_ADDRESS)

def SCXAddSPLIT_CONFIG_ID(builder, SPLIT_CONFIG_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SPLIT_CONFIG_ID), 0)

def AddSPLIT_CONFIG_ID(builder, SPLIT_CONFIG_ID):
    SCXAddSPLIT_CONFIG_ID(builder, SPLIT_CONFIG_ID)

def SCXAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(TOKEN_ADDRESS), 0)

def AddTOKEN_ADDRESS(builder, TOKEN_ADDRESS):
    SCXAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS)

def SCXAddTOKEN_SYMBOL(builder, TOKEN_SYMBOL):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(TOKEN_SYMBOL), 0)

def AddTOKEN_SYMBOL(builder, TOKEN_SYMBOL):
    SCXAddTOKEN_SYMBOL(builder, TOKEN_SYMBOL)

def SCXAddTOKEN_DECIMALS(builder, TOKEN_DECIMALS):
    builder.PrependUint8Slot(8, TOKEN_DECIMALS, 0)

def AddTOKEN_DECIMALS(builder, TOKEN_DECIMALS):
    SCXAddTOKEN_DECIMALS(builder, TOKEN_DECIMALS)

def SCXAddSPLIT_RECIPIENTS(builder, SPLIT_RECIPIENTS):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(SPLIT_RECIPIENTS), 0)

def AddSPLIT_RECIPIENTS(builder, SPLIT_RECIPIENTS):
    SCXAddSPLIT_RECIPIENTS(builder, SPLIT_RECIPIENTS)

def SCXStartSPLIT_RECIPIENTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSPLIT_RECIPIENTSVector(builder, numElems):
    return SCXStartSPLIT_RECIPIENTSVector(builder, numElems)

def SCXCreateSPLIT_RECIPIENTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSPLIT_RECIPIENTSVector(builder, data):
    SCXCreateSPLIT_RECIPIENTSVector(builder, data)

def SCXAddMIN_CONFIRMATIONS(builder, MIN_CONFIRMATIONS):
    builder.PrependUint32Slot(10, MIN_CONFIRMATIONS, 0)

def AddMIN_CONFIRMATIONS(builder, MIN_CONFIRMATIONS):
    SCXAddMIN_CONFIRMATIONS(builder, MIN_CONFIRMATIONS)

def SCXAddMEMO_SCHEME(builder, MEMO_SCHEME):
    builder.PrependInt8Slot(11, MEMO_SCHEME, 0)

def AddMEMO_SCHEME(builder, MEMO_SCHEME):
    SCXAddMEMO_SCHEME(builder, MEMO_SCHEME)

def SCXEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCXEnd(builder)

import SplitRecipient
try:
    from typing import List
except:
    pass

class SCXT(object):

    # SCXT
    def __init__(
        self,
        CHAIN = None,
        CHAIN_ID = 0,
        SETTLEMENT_KIND = 0,
        SPLITTER_STANDARD = None,
        SPLITTER_ADDRESS = None,
        SPLIT_CONFIG_ID = None,
        TOKEN_ADDRESS = None,
        TOKEN_SYMBOL = None,
        TOKEN_DECIMALS = 0,
        SPLIT_RECIPIENTS = None,
        MIN_CONFIRMATIONS = 0,
        MEMO_SCHEME = 0,
    ):
        self.CHAIN = CHAIN  # type: Optional[str]
        self.CHAIN_ID = CHAIN_ID  # type: int
        self.SETTLEMENT_KIND = SETTLEMENT_KIND  # type: int
        self.SPLITTER_STANDARD = SPLITTER_STANDARD  # type: Optional[str]
        self.SPLITTER_ADDRESS = SPLITTER_ADDRESS  # type: Optional[str]
        self.SPLIT_CONFIG_ID = SPLIT_CONFIG_ID  # type: Optional[str]
        self.TOKEN_ADDRESS = TOKEN_ADDRESS  # type: Optional[str]
        self.TOKEN_SYMBOL = TOKEN_SYMBOL  # type: Optional[str]
        self.TOKEN_DECIMALS = TOKEN_DECIMALS  # type: int
        self.SPLIT_RECIPIENTS = SPLIT_RECIPIENTS  # type: Optional[List[SplitRecipient.SplitRecipientT]]
        self.MIN_CONFIRMATIONS = MIN_CONFIRMATIONS  # type: int
        self.MEMO_SCHEME = MEMO_SCHEME  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScx = SCX()
        tmpScx.Init(buf, pos)
        return cls.InitFromObj(tmpScx)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScx):
        x = SCXT()
        x._UnPack(tmpScx)
        return x

    # SCXT
    def _UnPack(self, SCX):
        if SCX is None:
            return
        self.CHAIN = SCX.CHAIN()
        self.CHAIN_ID = SCX.CHAIN_ID()
        self.SETTLEMENT_KIND = SCX.SETTLEMENT_KIND()
        self.SPLITTER_STANDARD = SCX.SPLITTER_STANDARD()
        self.SPLITTER_ADDRESS = SCX.SPLITTER_ADDRESS()
        self.SPLIT_CONFIG_ID = SCX.SPLIT_CONFIG_ID()
        self.TOKEN_ADDRESS = SCX.TOKEN_ADDRESS()
        self.TOKEN_SYMBOL = SCX.TOKEN_SYMBOL()
        self.TOKEN_DECIMALS = SCX.TOKEN_DECIMALS()
        if not SCX.SPLIT_RECIPIENTSIsNone():
            self.SPLIT_RECIPIENTS = []
            for i in range(SCX.SPLIT_RECIPIENTSLength()):
                if SCX.SPLIT_RECIPIENTS(i) is None:
                    self.SPLIT_RECIPIENTS.append(None)
                else:
                    splitRecipient_ = SplitRecipient.SplitRecipientT.InitFromObj(SCX.SPLIT_RECIPIENTS(i))
                    self.SPLIT_RECIPIENTS.append(splitRecipient_)
        self.MIN_CONFIRMATIONS = SCX.MIN_CONFIRMATIONS()
        self.MEMO_SCHEME = SCX.MEMO_SCHEME()

    # SCXT
    def Pack(self, builder):
        if self.CHAIN is not None:
            CHAIN = builder.CreateString(self.CHAIN)
        if self.SPLITTER_STANDARD is not None:
            SPLITTER_STANDARD = builder.CreateString(self.SPLITTER_STANDARD)
        if self.SPLITTER_ADDRESS is not None:
            SPLITTER_ADDRESS = builder.CreateString(self.SPLITTER_ADDRESS)
        if self.SPLIT_CONFIG_ID is not None:
            SPLIT_CONFIG_ID = builder.CreateString(self.SPLIT_CONFIG_ID)
        if self.TOKEN_ADDRESS is not None:
            TOKEN_ADDRESS = builder.CreateString(self.TOKEN_ADDRESS)
        if self.TOKEN_SYMBOL is not None:
            TOKEN_SYMBOL = builder.CreateString(self.TOKEN_SYMBOL)
        if self.SPLIT_RECIPIENTS is not None:
            SPLIT_RECIPIENTSlist = []
            for i in range(len(self.SPLIT_RECIPIENTS)):
                SPLIT_RECIPIENTSlist.append(self.SPLIT_RECIPIENTS[i].Pack(builder))
            SCXStartSPLIT_RECIPIENTSVector(builder, len(self.SPLIT_RECIPIENTS))
            for i in reversed(range(len(self.SPLIT_RECIPIENTS))):
                builder.PrependUOffsetTRelative(SPLIT_RECIPIENTSlist[i])
            SPLIT_RECIPIENTS = builder.EndVector()
        SCXStart(builder)
        if self.CHAIN is not None:
            SCXAddCHAIN(builder, CHAIN)
        SCXAddCHAIN_ID(builder, self.CHAIN_ID)
        SCXAddSETTLEMENT_KIND(builder, self.SETTLEMENT_KIND)
        if self.SPLITTER_STANDARD is not None:
            SCXAddSPLITTER_STANDARD(builder, SPLITTER_STANDARD)
        if self.SPLITTER_ADDRESS is not None:
            SCXAddSPLITTER_ADDRESS(builder, SPLITTER_ADDRESS)
        if self.SPLIT_CONFIG_ID is not None:
            SCXAddSPLIT_CONFIG_ID(builder, SPLIT_CONFIG_ID)
        if self.TOKEN_ADDRESS is not None:
            SCXAddTOKEN_ADDRESS(builder, TOKEN_ADDRESS)
        if self.TOKEN_SYMBOL is not None:
            SCXAddTOKEN_SYMBOL(builder, TOKEN_SYMBOL)
        SCXAddTOKEN_DECIMALS(builder, self.TOKEN_DECIMALS)
        if self.SPLIT_RECIPIENTS is not None:
            SCXAddSPLIT_RECIPIENTS(builder, SPLIT_RECIPIENTS)
        SCXAddMIN_CONFIRMATIONS(builder, self.MIN_CONFIRMATIONS)
        SCXAddMEMO_SCHEME(builder, self.MEMO_SCHEME)
        SCX = SCXEnd(builder)
        return SCX
