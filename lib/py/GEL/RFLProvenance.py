# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Where this sample set came from, stated once for the record.
class RFLProvenance(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RFLProvenance()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRFLProvenance(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RFLProvenanceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x46\x4C", size_prefixed=size_prefixed)

    # RFLProvenance
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # RFLProvenance
    def METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # The producing system, dataset or measurement campaign. Required: a
    # budget time series with no stated source is not publishable.
    # RFLProvenance
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Replayable query or scenario reference that regenerates this set.
    # RFLProvenance
    def SOURCE_QUERY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Identifier of the propagator that produced the geometry columns. A record
    # whose METHOD is MODELED or HYBRID and which carries geometry columns MUST
    # name it: geometry is only as good as the propagator, and the propagator is
    # a pluggable choice, never an assumed default.
    # RFLProvenance
    def PROPAGATOR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFLProvenance
    def PROPAGATOR_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Force model or configuration identifier handed to the propagator.
    # RFLProvenance
    def PROPAGATOR_CONFIGURATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Uniform sampling step, seconds. Absent when SAMPLE_EPOCHS are irregular.
    # RFLProvenance
    def SAMPLING_STEP_S(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Per-term model bindings.
    # RFLProvenance
    def MODELS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from RFLModelBinding import RFLModelBinding
            obj = RFLModelBinding()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # RFLProvenance
    def MODELSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RFLProvenance
    def MODELSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Weather or environment dataset the atmospheric terms were evaluated
    # against, when they were not evaluated against a statistical model.
    # RFLProvenance
    def ENVIRONMENT_DATASET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Terrain or surface dataset the blockage terms were evaluated against.
    # RFLProvenance
    def TERRAIN_DATASET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Unix ms this set was computed or recorded.
    # RFLProvenance
    def COMPUTED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix ms the upstream inputs were retrieved.
    # RFLProvenance
    def RETRIEVED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Licence the source imposes on redistribution. An empty LICENSE means
    # UNKNOWN TERMS — never public domain, never a grant to redistribute.
    # RFLProvenance
    def LICENSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # True when the licence forbids commercial use of this set.
    # RFLProvenance
    def NON_COMMERCIAL_ONLY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Attribution the licence requires this record to carry downstream.
    # RFLProvenance
    def CITATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Primary `$PLG.PLUGIN_ID` / `$PMM.MODULE_ID` that produced this record.
    # MODELS remains the per-budget-term attribution surface.
    # RFLProvenance
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFLProvenance
    def MODULE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Content hash of the exact producing WASM artifact.
    # RFLProvenance
    def MODULE_CONTENT_HASH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def RFLProvenanceStart(builder):
    builder.StartObject(18)

def Start(builder):
    RFLProvenanceStart(builder)

def RFLProvenanceAddMETHOD(builder, METHOD):
    builder.PrependInt8Slot(0, METHOD, 0)

def AddMETHOD(builder, METHOD):
    RFLProvenanceAddMETHOD(builder, METHOD)

def RFLProvenanceAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    RFLProvenanceAddSOURCE(builder, SOURCE)

def RFLProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_QUERY), 0)

def AddSOURCE_QUERY(builder, SOURCE_QUERY):
    RFLProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY)

def RFLProvenanceAddPROPAGATOR_ID(builder, PROPAGATOR_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(PROPAGATOR_ID), 0)

def AddPROPAGATOR_ID(builder, PROPAGATOR_ID):
    RFLProvenanceAddPROPAGATOR_ID(builder, PROPAGATOR_ID)

def RFLProvenanceAddPROPAGATOR_VERSION(builder, PROPAGATOR_VERSION):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PROPAGATOR_VERSION), 0)

def AddPROPAGATOR_VERSION(builder, PROPAGATOR_VERSION):
    RFLProvenanceAddPROPAGATOR_VERSION(builder, PROPAGATOR_VERSION)

def RFLProvenanceAddPROPAGATOR_CONFIGURATION(builder, PROPAGATOR_CONFIGURATION):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PROPAGATOR_CONFIGURATION), 0)

def AddPROPAGATOR_CONFIGURATION(builder, PROPAGATOR_CONFIGURATION):
    RFLProvenanceAddPROPAGATOR_CONFIGURATION(builder, PROPAGATOR_CONFIGURATION)

def RFLProvenanceAddSAMPLING_STEP_S(builder, SAMPLING_STEP_S):
    builder.PrependFloat64Slot(6, SAMPLING_STEP_S, 0.0)

def AddSAMPLING_STEP_S(builder, SAMPLING_STEP_S):
    RFLProvenanceAddSAMPLING_STEP_S(builder, SAMPLING_STEP_S)

def RFLProvenanceAddMODELS(builder, MODELS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(MODELS), 0)

def AddMODELS(builder, MODELS):
    RFLProvenanceAddMODELS(builder, MODELS)

def RFLProvenanceStartMODELSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartMODELSVector(builder, numElems):
    return RFLProvenanceStartMODELSVector(builder, numElems)

def RFLProvenanceCreateMODELSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateMODELSVector(builder, data):
    RFLProvenanceCreateMODELSVector(builder, data)

def RFLProvenanceAddENVIRONMENT_DATASET(builder, ENVIRONMENT_DATASET):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(ENVIRONMENT_DATASET), 0)

def AddENVIRONMENT_DATASET(builder, ENVIRONMENT_DATASET):
    RFLProvenanceAddENVIRONMENT_DATASET(builder, ENVIRONMENT_DATASET)

def RFLProvenanceAddTERRAIN_DATASET(builder, TERRAIN_DATASET):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(TERRAIN_DATASET), 0)

def AddTERRAIN_DATASET(builder, TERRAIN_DATASET):
    RFLProvenanceAddTERRAIN_DATASET(builder, TERRAIN_DATASET)

def RFLProvenanceAddCOMPUTED_AT(builder, COMPUTED_AT):
    builder.PrependUint64Slot(10, COMPUTED_AT, 0)

def AddCOMPUTED_AT(builder, COMPUTED_AT):
    RFLProvenanceAddCOMPUTED_AT(builder, COMPUTED_AT)

def RFLProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT):
    builder.PrependUint64Slot(11, RETRIEVED_AT, 0)

def AddRETRIEVED_AT(builder, RETRIEVED_AT):
    RFLProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT)

def RFLProvenanceAddLICENSE(builder, LICENSE):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE), 0)

def AddLICENSE(builder, LICENSE):
    RFLProvenanceAddLICENSE(builder, LICENSE)

def RFLProvenanceAddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY):
    builder.PrependBoolSlot(13, NON_COMMERCIAL_ONLY, 0)

def AddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY):
    RFLProvenanceAddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY)

def RFLProvenanceAddCITATION(builder, CITATION):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(CITATION), 0)

def AddCITATION(builder, CITATION):
    RFLProvenanceAddCITATION(builder, CITATION)

def RFLProvenanceAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    RFLProvenanceAddMODULE_ID(builder, MODULE_ID)

def RFLProvenanceAddMODULE_VERSION(builder, MODULE_VERSION):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_VERSION), 0)

def AddMODULE_VERSION(builder, MODULE_VERSION):
    RFLProvenanceAddMODULE_VERSION(builder, MODULE_VERSION)

def RFLProvenanceAddMODULE_CONTENT_HASH(builder, MODULE_CONTENT_HASH):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_CONTENT_HASH), 0)

def AddMODULE_CONTENT_HASH(builder, MODULE_CONTENT_HASH):
    RFLProvenanceAddMODULE_CONTENT_HASH(builder, MODULE_CONTENT_HASH)

def RFLProvenanceEnd(builder):
    return builder.EndObject()

def End(builder):
    return RFLProvenanceEnd(builder)

import RFLModelBinding
try:
    from typing import List
except:
    pass

class RFLProvenanceT(object):

    # RFLProvenanceT
    def __init__(
        self,
        METHOD = 0,
        SOURCE = None,
        SOURCE_QUERY = None,
        PROPAGATOR_ID = None,
        PROPAGATOR_VERSION = None,
        PROPAGATOR_CONFIGURATION = None,
        SAMPLING_STEP_S = 0.0,
        MODELS = None,
        ENVIRONMENT_DATASET = None,
        TERRAIN_DATASET = None,
        COMPUTED_AT = 0,
        RETRIEVED_AT = 0,
        LICENSE = None,
        NON_COMMERCIAL_ONLY = False,
        CITATION = None,
        MODULE_ID = None,
        MODULE_VERSION = None,
        MODULE_CONTENT_HASH = None,
    ):
        self.METHOD = METHOD  # type: int
        self.SOURCE = SOURCE  # type: Optional[str]
        self.SOURCE_QUERY = SOURCE_QUERY  # type: Optional[str]
        self.PROPAGATOR_ID = PROPAGATOR_ID  # type: Optional[str]
        self.PROPAGATOR_VERSION = PROPAGATOR_VERSION  # type: Optional[str]
        self.PROPAGATOR_CONFIGURATION = PROPAGATOR_CONFIGURATION  # type: Optional[str]
        self.SAMPLING_STEP_S = SAMPLING_STEP_S  # type: float
        self.MODELS = MODELS  # type: Optional[List[RFLModelBinding.RFLModelBindingT]]
        self.ENVIRONMENT_DATASET = ENVIRONMENT_DATASET  # type: Optional[str]
        self.TERRAIN_DATASET = TERRAIN_DATASET  # type: Optional[str]
        self.COMPUTED_AT = COMPUTED_AT  # type: int
        self.RETRIEVED_AT = RETRIEVED_AT  # type: int
        self.LICENSE = LICENSE  # type: Optional[str]
        self.NON_COMMERCIAL_ONLY = NON_COMMERCIAL_ONLY  # type: bool
        self.CITATION = CITATION  # type: Optional[str]
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.MODULE_VERSION = MODULE_VERSION  # type: Optional[str]
        self.MODULE_CONTENT_HASH = MODULE_CONTENT_HASH  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpRflprovenance = RFLProvenance()
        tmpRflprovenance.Init(buf, pos)
        return cls.InitFromObj(tmpRflprovenance)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpRflprovenance):
        x = RFLProvenanceT()
        x._UnPack(tmpRflprovenance)
        return x

    # RFLProvenanceT
    def _UnPack(self, RFLProvenance):
        if RFLProvenance is None:
            return
        self.METHOD = RFLProvenance.METHOD()
        self.SOURCE = RFLProvenance.SOURCE()
        self.SOURCE_QUERY = RFLProvenance.SOURCE_QUERY()
        self.PROPAGATOR_ID = RFLProvenance.PROPAGATOR_ID()
        self.PROPAGATOR_VERSION = RFLProvenance.PROPAGATOR_VERSION()
        self.PROPAGATOR_CONFIGURATION = RFLProvenance.PROPAGATOR_CONFIGURATION()
        self.SAMPLING_STEP_S = RFLProvenance.SAMPLING_STEP_S()
        if not RFLProvenance.MODELSIsNone():
            self.MODELS = []
            for i in range(RFLProvenance.MODELSLength()):
                if RFLProvenance.MODELS(i) is None:
                    self.MODELS.append(None)
                else:
                    rFLModelBinding_ = RFLModelBinding.RFLModelBindingT.InitFromObj(RFLProvenance.MODELS(i))
                    self.MODELS.append(rFLModelBinding_)
        self.ENVIRONMENT_DATASET = RFLProvenance.ENVIRONMENT_DATASET()
        self.TERRAIN_DATASET = RFLProvenance.TERRAIN_DATASET()
        self.COMPUTED_AT = RFLProvenance.COMPUTED_AT()
        self.RETRIEVED_AT = RFLProvenance.RETRIEVED_AT()
        self.LICENSE = RFLProvenance.LICENSE()
        self.NON_COMMERCIAL_ONLY = RFLProvenance.NON_COMMERCIAL_ONLY()
        self.CITATION = RFLProvenance.CITATION()
        self.MODULE_ID = RFLProvenance.MODULE_ID()
        self.MODULE_VERSION = RFLProvenance.MODULE_VERSION()
        self.MODULE_CONTENT_HASH = RFLProvenance.MODULE_CONTENT_HASH()

    # RFLProvenanceT
    def Pack(self, builder):
        if self.SOURCE is not None:
            SOURCE = builder.CreateString(self.SOURCE)
        if self.SOURCE_QUERY is not None:
            SOURCE_QUERY = builder.CreateString(self.SOURCE_QUERY)
        if self.PROPAGATOR_ID is not None:
            PROPAGATOR_ID = builder.CreateString(self.PROPAGATOR_ID)
        if self.PROPAGATOR_VERSION is not None:
            PROPAGATOR_VERSION = builder.CreateString(self.PROPAGATOR_VERSION)
        if self.PROPAGATOR_CONFIGURATION is not None:
            PROPAGATOR_CONFIGURATION = builder.CreateString(self.PROPAGATOR_CONFIGURATION)
        if self.MODELS is not None:
            MODELSlist = []
            for i in range(len(self.MODELS)):
                MODELSlist.append(self.MODELS[i].Pack(builder))
            RFLProvenanceStartMODELSVector(builder, len(self.MODELS))
            for i in reversed(range(len(self.MODELS))):
                builder.PrependUOffsetTRelative(MODELSlist[i])
            MODELS = builder.EndVector()
        if self.ENVIRONMENT_DATASET is not None:
            ENVIRONMENT_DATASET = builder.CreateString(self.ENVIRONMENT_DATASET)
        if self.TERRAIN_DATASET is not None:
            TERRAIN_DATASET = builder.CreateString(self.TERRAIN_DATASET)
        if self.LICENSE is not None:
            LICENSE = builder.CreateString(self.LICENSE)
        if self.CITATION is not None:
            CITATION = builder.CreateString(self.CITATION)
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.MODULE_VERSION is not None:
            MODULE_VERSION = builder.CreateString(self.MODULE_VERSION)
        if self.MODULE_CONTENT_HASH is not None:
            MODULE_CONTENT_HASH = builder.CreateString(self.MODULE_CONTENT_HASH)
        RFLProvenanceStart(builder)
        RFLProvenanceAddMETHOD(builder, self.METHOD)
        if self.SOURCE is not None:
            RFLProvenanceAddSOURCE(builder, SOURCE)
        if self.SOURCE_QUERY is not None:
            RFLProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY)
        if self.PROPAGATOR_ID is not None:
            RFLProvenanceAddPROPAGATOR_ID(builder, PROPAGATOR_ID)
        if self.PROPAGATOR_VERSION is not None:
            RFLProvenanceAddPROPAGATOR_VERSION(builder, PROPAGATOR_VERSION)
        if self.PROPAGATOR_CONFIGURATION is not None:
            RFLProvenanceAddPROPAGATOR_CONFIGURATION(builder, PROPAGATOR_CONFIGURATION)
        RFLProvenanceAddSAMPLING_STEP_S(builder, self.SAMPLING_STEP_S)
        if self.MODELS is not None:
            RFLProvenanceAddMODELS(builder, MODELS)
        if self.ENVIRONMENT_DATASET is not None:
            RFLProvenanceAddENVIRONMENT_DATASET(builder, ENVIRONMENT_DATASET)
        if self.TERRAIN_DATASET is not None:
            RFLProvenanceAddTERRAIN_DATASET(builder, TERRAIN_DATASET)
        RFLProvenanceAddCOMPUTED_AT(builder, self.COMPUTED_AT)
        RFLProvenanceAddRETRIEVED_AT(builder, self.RETRIEVED_AT)
        if self.LICENSE is not None:
            RFLProvenanceAddLICENSE(builder, LICENSE)
        RFLProvenanceAddNON_COMMERCIAL_ONLY(builder, self.NON_COMMERCIAL_ONLY)
        if self.CITATION is not None:
            RFLProvenanceAddCITATION(builder, CITATION)
        if self.MODULE_ID is not None:
            RFLProvenanceAddMODULE_ID(builder, MODULE_ID)
        if self.MODULE_VERSION is not None:
            RFLProvenanceAddMODULE_VERSION(builder, MODULE_VERSION)
        if self.MODULE_CONTENT_HASH is not None:
            RFLProvenanceAddMODULE_CONTENT_HASH(builder, MODULE_CONTENT_HASH)
        RFLProvenance = RFLProvenanceEnd(builder)
        return RFLProvenance
