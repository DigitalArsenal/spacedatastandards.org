# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# On-Orbit Solar Array
class OOS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OOS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOOS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OOSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x4F\x53", size_prefixed=size_prefixed)

    # OOS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Unique identifier
    # OOS
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference to parent on-orbit object
    # OOS
    def ID_ON_ORBIT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference to solar array specification
    # OOS
    def ID_SOLAR_ARRAY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Solar array name or designation
    # OOS
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Solar cell type (e.g., SILICON, GAAS, MULTI_JUNCTION, THIN_FILM, PEROVSKITE)
    # OOS
    def CELL_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of solar arrays of this type
    # OOS
    def QUANTITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Total array area in square meters
    # OOS
    def AREA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Beginning of life power output in Watts
    # OOS
    def POWER_BOL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # End of life power output in Watts
    # OOS
    def POWER_EOL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Conversion efficiency as fraction (0.0-1.0)
    # OOS
    def EFFICIENCY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Degradation rate per year as fraction
    # OOS
    def DEGRADATION_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Number of panels per array
    # OOS
    def NUM_PANELS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Whether the array is deployable
    # OOS
    def DEPLOYABLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Whether the array is articulable/tracking
    # OOS
    def TRACKING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Array mass in kg
    # OOS
    def MASS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Additional notes
    # OOS
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def OOSStart(builder):
    builder.StartObject(16)

def Start(builder):
    OOSStart(builder)

def OOSAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    OOSAddID(builder, ID)

def OOSAddID_ON_ORBIT(builder, ID_ON_ORBIT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ID_ON_ORBIT), 0)

def AddID_ON_ORBIT(builder, ID_ON_ORBIT):
    OOSAddID_ON_ORBIT(builder, ID_ON_ORBIT)

def OOSAddID_SOLAR_ARRAY(builder, ID_SOLAR_ARRAY):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ID_SOLAR_ARRAY), 0)

def AddID_SOLAR_ARRAY(builder, ID_SOLAR_ARRAY):
    OOSAddID_SOLAR_ARRAY(builder, ID_SOLAR_ARRAY)

def OOSAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    OOSAddNAME(builder, NAME)

def OOSAddCELL_TYPE(builder, CELL_TYPE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(CELL_TYPE), 0)

def AddCELL_TYPE(builder, CELL_TYPE):
    OOSAddCELL_TYPE(builder, CELL_TYPE)

def OOSAddQUANTITY(builder, QUANTITY):
    builder.PrependUint32Slot(5, QUANTITY, 0)

def AddQUANTITY(builder, QUANTITY):
    OOSAddQUANTITY(builder, QUANTITY)

def OOSAddAREA(builder, AREA):
    builder.PrependFloat64Slot(6, AREA, 0.0)

def AddAREA(builder, AREA):
    OOSAddAREA(builder, AREA)

def OOSAddPOWER_BOL(builder, POWER_BOL):
    builder.PrependFloat64Slot(7, POWER_BOL, 0.0)

def AddPOWER_BOL(builder, POWER_BOL):
    OOSAddPOWER_BOL(builder, POWER_BOL)

def OOSAddPOWER_EOL(builder, POWER_EOL):
    builder.PrependFloat64Slot(8, POWER_EOL, 0.0)

def AddPOWER_EOL(builder, POWER_EOL):
    OOSAddPOWER_EOL(builder, POWER_EOL)

def OOSAddEFFICIENCY(builder, EFFICIENCY):
    builder.PrependFloat64Slot(9, EFFICIENCY, 0.0)

def AddEFFICIENCY(builder, EFFICIENCY):
    OOSAddEFFICIENCY(builder, EFFICIENCY)

def OOSAddDEGRADATION_RATE(builder, DEGRADATION_RATE):
    builder.PrependFloat64Slot(10, DEGRADATION_RATE, 0.0)

def AddDEGRADATION_RATE(builder, DEGRADATION_RATE):
    OOSAddDEGRADATION_RATE(builder, DEGRADATION_RATE)

def OOSAddNUM_PANELS(builder, NUM_PANELS):
    builder.PrependUint32Slot(11, NUM_PANELS, 0)

def AddNUM_PANELS(builder, NUM_PANELS):
    OOSAddNUM_PANELS(builder, NUM_PANELS)

def OOSAddDEPLOYABLE(builder, DEPLOYABLE):
    builder.PrependBoolSlot(12, DEPLOYABLE, 0)

def AddDEPLOYABLE(builder, DEPLOYABLE):
    OOSAddDEPLOYABLE(builder, DEPLOYABLE)

def OOSAddTRACKING(builder, TRACKING):
    builder.PrependBoolSlot(13, TRACKING, 0)

def AddTRACKING(builder, TRACKING):
    OOSAddTRACKING(builder, TRACKING)

def OOSAddMASS(builder, MASS):
    builder.PrependFloat64Slot(14, MASS, 0.0)

def AddMASS(builder, MASS):
    OOSAddMASS(builder, MASS)

def OOSAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    OOSAddNOTES(builder, NOTES)

def OOSEnd(builder):
    return builder.EndObject()

def End(builder):
    return OOSEnd(builder)


class OOST(object):

    # OOST
    def __init__(self):
        self.ID = None  # type: str
        self.ID_ON_ORBIT = None  # type: str
        self.ID_SOLAR_ARRAY = None  # type: str
        self.NAME = None  # type: str
        self.CELL_TYPE = None  # type: str
        self.QUANTITY = 0  # type: int
        self.AREA = 0.0  # type: float
        self.POWER_BOL = 0.0  # type: float
        self.POWER_EOL = 0.0  # type: float
        self.EFFICIENCY = 0.0  # type: float
        self.DEGRADATION_RATE = 0.0  # type: float
        self.NUM_PANELS = 0  # type: int
        self.DEPLOYABLE = False  # type: bool
        self.TRACKING = False  # type: bool
        self.MASS = 0.0  # type: float
        self.NOTES = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        OOS = OOS()
        OOS.Init(buf, pos)
        return cls.InitFromObj(OOS)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, OOS):
        x = OOST()
        x._UnPack(OOS)
        return x

    # OOST
    def _UnPack(self, OOS):
        if OOS is None:
            return
        self.ID = OOS.ID()
        self.ID_ON_ORBIT = OOS.ID_ON_ORBIT()
        self.ID_SOLAR_ARRAY = OOS.ID_SOLAR_ARRAY()
        self.NAME = OOS.NAME()
        self.CELL_TYPE = OOS.CELL_TYPE()
        self.QUANTITY = OOS.QUANTITY()
        self.AREA = OOS.AREA()
        self.POWER_BOL = OOS.POWER_BOL()
        self.POWER_EOL = OOS.POWER_EOL()
        self.EFFICIENCY = OOS.EFFICIENCY()
        self.DEGRADATION_RATE = OOS.DEGRADATION_RATE()
        self.NUM_PANELS = OOS.NUM_PANELS()
        self.DEPLOYABLE = OOS.DEPLOYABLE()
        self.TRACKING = OOS.TRACKING()
        self.MASS = OOS.MASS()
        self.NOTES = OOS.NOTES()

    # OOST
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.ID_ON_ORBIT is not None:
            ID_ON_ORBIT = builder.CreateString(self.ID_ON_ORBIT)
        if self.ID_SOLAR_ARRAY is not None:
            ID_SOLAR_ARRAY = builder.CreateString(self.ID_SOLAR_ARRAY)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.CELL_TYPE is not None:
            CELL_TYPE = builder.CreateString(self.CELL_TYPE)
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        OOSStart(builder)
        if self.ID is not None:
            OOSAddID(builder, ID)
        if self.ID_ON_ORBIT is not None:
            OOSAddID_ON_ORBIT(builder, ID_ON_ORBIT)
        if self.ID_SOLAR_ARRAY is not None:
            OOSAddID_SOLAR_ARRAY(builder, ID_SOLAR_ARRAY)
        if self.NAME is not None:
            OOSAddNAME(builder, NAME)
        if self.CELL_TYPE is not None:
            OOSAddCELL_TYPE(builder, CELL_TYPE)
        OOSAddQUANTITY(builder, self.QUANTITY)
        OOSAddAREA(builder, self.AREA)
        OOSAddPOWER_BOL(builder, self.POWER_BOL)
        OOSAddPOWER_EOL(builder, self.POWER_EOL)
        OOSAddEFFICIENCY(builder, self.EFFICIENCY)
        OOSAddDEGRADATION_RATE(builder, self.DEGRADATION_RATE)
        OOSAddNUM_PANELS(builder, self.NUM_PANELS)
        OOSAddDEPLOYABLE(builder, self.DEPLOYABLE)
        OOSAddTRACKING(builder, self.TRACKING)
        OOSAddMASS(builder, self.MASS)
        if self.NOTES is not None:
            OOSAddNOTES(builder, NOTES)
        OOS = OOSEnd(builder)
        return OOS
