# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Pre/post-maneuver orbital state
class mnvOrbitalState(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = mnvOrbitalState()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsmnvOrbitalState(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def mnvOrbitalStateBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4D\x4E\x56", size_prefixed=size_prefixed)

    # mnvOrbitalState
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Element set identifier
    # mnvOrbitalState
    def ID_ELSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Element set data reference
    # mnvOrbitalState
    def ELSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # State vector identifier
    # mnvOrbitalState
    def ID_STATE_VECTOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # State vector data reference
    # mnvOrbitalState
    def STATE_VECTOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Position X (km)
    # mnvOrbitalState
    def POS_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Position Y (km)
    # mnvOrbitalState
    def POS_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Position Z (km)
    # mnvOrbitalState
    def POS_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Velocity X (km/s)
    # mnvOrbitalState
    def VEL_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Velocity Y (km/s)
    # mnvOrbitalState
    def VEL_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Velocity Z (km/s)
    # mnvOrbitalState
    def VEL_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Radiation pressure coefficient (Cr)
    # mnvOrbitalState
    def RADIATION_PRESS_COEFF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Ballistic coefficient (m^2/kg)
    # mnvOrbitalState
    def BALLISTIC_COEFF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Apogee altitude (km)
    # mnvOrbitalState
    def APOGEE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Perigee altitude (km)
    # mnvOrbitalState
    def PERIGEE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Inclination (degrees)
    # mnvOrbitalState
    def INCLINATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Eccentricity
    # mnvOrbitalState
    def ECCENTRICITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Orbital period (minutes)
    # mnvOrbitalState
    def PERIOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Right ascension of ascending node (degrees)
    # mnvOrbitalState
    def RAAN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Semi-major axis (km)
    # mnvOrbitalState
    def SMA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # GEO longitude (degrees east)
    # mnvOrbitalState
    def GEO_LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Longitude drift rate (degrees/day)
    # mnvOrbitalState
    def DRIFT_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Position uncertainty U (km, 1-sigma)
    # mnvOrbitalState
    def SIGMA_U(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Position uncertainty V (km, 1-sigma)
    # mnvOrbitalState
    def SIGMA_V(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Position uncertainty W (km, 1-sigma)
    # mnvOrbitalState
    def SIGMA_W(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def mnvOrbitalStateStart(builder):
    builder.StartObject(24)

def Start(builder):
    mnvOrbitalStateStart(builder)

def mnvOrbitalStateAddID_ELSET(builder, ID_ELSET):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID_ELSET), 0)

def AddID_ELSET(builder, ID_ELSET):
    mnvOrbitalStateAddID_ELSET(builder, ID_ELSET)

def mnvOrbitalStateAddELSET(builder, ELSET):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ELSET), 0)

def AddELSET(builder, ELSET):
    mnvOrbitalStateAddELSET(builder, ELSET)

def mnvOrbitalStateAddID_STATE_VECTOR(builder, ID_STATE_VECTOR):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ID_STATE_VECTOR), 0)

def AddID_STATE_VECTOR(builder, ID_STATE_VECTOR):
    mnvOrbitalStateAddID_STATE_VECTOR(builder, ID_STATE_VECTOR)

def mnvOrbitalStateAddSTATE_VECTOR(builder, STATE_VECTOR):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(STATE_VECTOR), 0)

def AddSTATE_VECTOR(builder, STATE_VECTOR):
    mnvOrbitalStateAddSTATE_VECTOR(builder, STATE_VECTOR)

def mnvOrbitalStateAddPOS_X(builder, POS_X):
    builder.PrependFloat64Slot(4, POS_X, 0.0)

def AddPOS_X(builder, POS_X):
    mnvOrbitalStateAddPOS_X(builder, POS_X)

def mnvOrbitalStateAddPOS_Y(builder, POS_Y):
    builder.PrependFloat64Slot(5, POS_Y, 0.0)

def AddPOS_Y(builder, POS_Y):
    mnvOrbitalStateAddPOS_Y(builder, POS_Y)

def mnvOrbitalStateAddPOS_Z(builder, POS_Z):
    builder.PrependFloat64Slot(6, POS_Z, 0.0)

def AddPOS_Z(builder, POS_Z):
    mnvOrbitalStateAddPOS_Z(builder, POS_Z)

def mnvOrbitalStateAddVEL_X(builder, VEL_X):
    builder.PrependFloat64Slot(7, VEL_X, 0.0)

def AddVEL_X(builder, VEL_X):
    mnvOrbitalStateAddVEL_X(builder, VEL_X)

def mnvOrbitalStateAddVEL_Y(builder, VEL_Y):
    builder.PrependFloat64Slot(8, VEL_Y, 0.0)

def AddVEL_Y(builder, VEL_Y):
    mnvOrbitalStateAddVEL_Y(builder, VEL_Y)

def mnvOrbitalStateAddVEL_Z(builder, VEL_Z):
    builder.PrependFloat64Slot(9, VEL_Z, 0.0)

def AddVEL_Z(builder, VEL_Z):
    mnvOrbitalStateAddVEL_Z(builder, VEL_Z)

def mnvOrbitalStateAddRADIATION_PRESS_COEFF(builder, RADIATION_PRESS_COEFF):
    builder.PrependFloat64Slot(10, RADIATION_PRESS_COEFF, 0.0)

def AddRADIATION_PRESS_COEFF(builder, RADIATION_PRESS_COEFF):
    mnvOrbitalStateAddRADIATION_PRESS_COEFF(builder, RADIATION_PRESS_COEFF)

def mnvOrbitalStateAddBALLISTIC_COEFF(builder, BALLISTIC_COEFF):
    builder.PrependFloat64Slot(11, BALLISTIC_COEFF, 0.0)

def AddBALLISTIC_COEFF(builder, BALLISTIC_COEFF):
    mnvOrbitalStateAddBALLISTIC_COEFF(builder, BALLISTIC_COEFF)

def mnvOrbitalStateAddAPOGEE(builder, APOGEE):
    builder.PrependFloat64Slot(12, APOGEE, 0.0)

def AddAPOGEE(builder, APOGEE):
    mnvOrbitalStateAddAPOGEE(builder, APOGEE)

def mnvOrbitalStateAddPERIGEE(builder, PERIGEE):
    builder.PrependFloat64Slot(13, PERIGEE, 0.0)

def AddPERIGEE(builder, PERIGEE):
    mnvOrbitalStateAddPERIGEE(builder, PERIGEE)

def mnvOrbitalStateAddINCLINATION(builder, INCLINATION):
    builder.PrependFloat64Slot(14, INCLINATION, 0.0)

def AddINCLINATION(builder, INCLINATION):
    mnvOrbitalStateAddINCLINATION(builder, INCLINATION)

def mnvOrbitalStateAddECCENTRICITY(builder, ECCENTRICITY):
    builder.PrependFloat64Slot(15, ECCENTRICITY, 0.0)

def AddECCENTRICITY(builder, ECCENTRICITY):
    mnvOrbitalStateAddECCENTRICITY(builder, ECCENTRICITY)

def mnvOrbitalStateAddPERIOD(builder, PERIOD):
    builder.PrependFloat64Slot(16, PERIOD, 0.0)

def AddPERIOD(builder, PERIOD):
    mnvOrbitalStateAddPERIOD(builder, PERIOD)

def mnvOrbitalStateAddRAAN(builder, RAAN):
    builder.PrependFloat64Slot(17, RAAN, 0.0)

def AddRAAN(builder, RAAN):
    mnvOrbitalStateAddRAAN(builder, RAAN)

def mnvOrbitalStateAddSMA(builder, SMA):
    builder.PrependFloat64Slot(18, SMA, 0.0)

def AddSMA(builder, SMA):
    mnvOrbitalStateAddSMA(builder, SMA)

def mnvOrbitalStateAddGEO_LONGITUDE(builder, GEO_LONGITUDE):
    builder.PrependFloat64Slot(19, GEO_LONGITUDE, 0.0)

def AddGEO_LONGITUDE(builder, GEO_LONGITUDE):
    mnvOrbitalStateAddGEO_LONGITUDE(builder, GEO_LONGITUDE)

def mnvOrbitalStateAddDRIFT_RATE(builder, DRIFT_RATE):
    builder.PrependFloat64Slot(20, DRIFT_RATE, 0.0)

def AddDRIFT_RATE(builder, DRIFT_RATE):
    mnvOrbitalStateAddDRIFT_RATE(builder, DRIFT_RATE)

def mnvOrbitalStateAddSIGMA_U(builder, SIGMA_U):
    builder.PrependFloat64Slot(21, SIGMA_U, 0.0)

def AddSIGMA_U(builder, SIGMA_U):
    mnvOrbitalStateAddSIGMA_U(builder, SIGMA_U)

def mnvOrbitalStateAddSIGMA_V(builder, SIGMA_V):
    builder.PrependFloat64Slot(22, SIGMA_V, 0.0)

def AddSIGMA_V(builder, SIGMA_V):
    mnvOrbitalStateAddSIGMA_V(builder, SIGMA_V)

def mnvOrbitalStateAddSIGMA_W(builder, SIGMA_W):
    builder.PrependFloat64Slot(23, SIGMA_W, 0.0)

def AddSIGMA_W(builder, SIGMA_W):
    mnvOrbitalStateAddSIGMA_W(builder, SIGMA_W)

def mnvOrbitalStateEnd(builder):
    return builder.EndObject()

def End(builder):
    return mnvOrbitalStateEnd(builder)


class mnvOrbitalStateT(object):

    # mnvOrbitalStateT
    def __init__(self):
        self.ID_ELSET = None  # type: str
        self.ELSET = None  # type: str
        self.ID_STATE_VECTOR = None  # type: str
        self.STATE_VECTOR = None  # type: str
        self.POS_X = 0.0  # type: float
        self.POS_Y = 0.0  # type: float
        self.POS_Z = 0.0  # type: float
        self.VEL_X = 0.0  # type: float
        self.VEL_Y = 0.0  # type: float
        self.VEL_Z = 0.0  # type: float
        self.RADIATION_PRESS_COEFF = 0.0  # type: float
        self.BALLISTIC_COEFF = 0.0  # type: float
        self.APOGEE = 0.0  # type: float
        self.PERIGEE = 0.0  # type: float
        self.INCLINATION = 0.0  # type: float
        self.ECCENTRICITY = 0.0  # type: float
        self.PERIOD = 0.0  # type: float
        self.RAAN = 0.0  # type: float
        self.SMA = 0.0  # type: float
        self.GEO_LONGITUDE = 0.0  # type: float
        self.DRIFT_RATE = 0.0  # type: float
        self.SIGMA_U = 0.0  # type: float
        self.SIGMA_V = 0.0  # type: float
        self.SIGMA_W = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        mnvOrbitalState = mnvOrbitalState()
        mnvOrbitalState.Init(buf, pos)
        return cls.InitFromObj(mnvOrbitalState)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, mnvOrbitalState):
        x = mnvOrbitalStateT()
        x._UnPack(mnvOrbitalState)
        return x

    # mnvOrbitalStateT
    def _UnPack(self, mnvOrbitalState):
        if mnvOrbitalState is None:
            return
        self.ID_ELSET = mnvOrbitalState.ID_ELSET()
        self.ELSET = mnvOrbitalState.ELSET()
        self.ID_STATE_VECTOR = mnvOrbitalState.ID_STATE_VECTOR()
        self.STATE_VECTOR = mnvOrbitalState.STATE_VECTOR()
        self.POS_X = mnvOrbitalState.POS_X()
        self.POS_Y = mnvOrbitalState.POS_Y()
        self.POS_Z = mnvOrbitalState.POS_Z()
        self.VEL_X = mnvOrbitalState.VEL_X()
        self.VEL_Y = mnvOrbitalState.VEL_Y()
        self.VEL_Z = mnvOrbitalState.VEL_Z()
        self.RADIATION_PRESS_COEFF = mnvOrbitalState.RADIATION_PRESS_COEFF()
        self.BALLISTIC_COEFF = mnvOrbitalState.BALLISTIC_COEFF()
        self.APOGEE = mnvOrbitalState.APOGEE()
        self.PERIGEE = mnvOrbitalState.PERIGEE()
        self.INCLINATION = mnvOrbitalState.INCLINATION()
        self.ECCENTRICITY = mnvOrbitalState.ECCENTRICITY()
        self.PERIOD = mnvOrbitalState.PERIOD()
        self.RAAN = mnvOrbitalState.RAAN()
        self.SMA = mnvOrbitalState.SMA()
        self.GEO_LONGITUDE = mnvOrbitalState.GEO_LONGITUDE()
        self.DRIFT_RATE = mnvOrbitalState.DRIFT_RATE()
        self.SIGMA_U = mnvOrbitalState.SIGMA_U()
        self.SIGMA_V = mnvOrbitalState.SIGMA_V()
        self.SIGMA_W = mnvOrbitalState.SIGMA_W()

    # mnvOrbitalStateT
    def Pack(self, builder):
        if self.ID_ELSET is not None:
            ID_ELSET = builder.CreateString(self.ID_ELSET)
        if self.ELSET is not None:
            ELSET = builder.CreateString(self.ELSET)
        if self.ID_STATE_VECTOR is not None:
            ID_STATE_VECTOR = builder.CreateString(self.ID_STATE_VECTOR)
        if self.STATE_VECTOR is not None:
            STATE_VECTOR = builder.CreateString(self.STATE_VECTOR)
        mnvOrbitalStateStart(builder)
        if self.ID_ELSET is not None:
            mnvOrbitalStateAddID_ELSET(builder, ID_ELSET)
        if self.ELSET is not None:
            mnvOrbitalStateAddELSET(builder, ELSET)
        if self.ID_STATE_VECTOR is not None:
            mnvOrbitalStateAddID_STATE_VECTOR(builder, ID_STATE_VECTOR)
        if self.STATE_VECTOR is not None:
            mnvOrbitalStateAddSTATE_VECTOR(builder, STATE_VECTOR)
        mnvOrbitalStateAddPOS_X(builder, self.POS_X)
        mnvOrbitalStateAddPOS_Y(builder, self.POS_Y)
        mnvOrbitalStateAddPOS_Z(builder, self.POS_Z)
        mnvOrbitalStateAddVEL_X(builder, self.VEL_X)
        mnvOrbitalStateAddVEL_Y(builder, self.VEL_Y)
        mnvOrbitalStateAddVEL_Z(builder, self.VEL_Z)
        mnvOrbitalStateAddRADIATION_PRESS_COEFF(builder, self.RADIATION_PRESS_COEFF)
        mnvOrbitalStateAddBALLISTIC_COEFF(builder, self.BALLISTIC_COEFF)
        mnvOrbitalStateAddAPOGEE(builder, self.APOGEE)
        mnvOrbitalStateAddPERIGEE(builder, self.PERIGEE)
        mnvOrbitalStateAddINCLINATION(builder, self.INCLINATION)
        mnvOrbitalStateAddECCENTRICITY(builder, self.ECCENTRICITY)
        mnvOrbitalStateAddPERIOD(builder, self.PERIOD)
        mnvOrbitalStateAddRAAN(builder, self.RAAN)
        mnvOrbitalStateAddSMA(builder, self.SMA)
        mnvOrbitalStateAddGEO_LONGITUDE(builder, self.GEO_LONGITUDE)
        mnvOrbitalStateAddDRIFT_RATE(builder, self.DRIFT_RATE)
        mnvOrbitalStateAddSIGMA_U(builder, self.SIGMA_U)
        mnvOrbitalStateAddSIGMA_V(builder, self.SIGMA_V)
        mnvOrbitalStateAddSIGMA_W(builder, self.SIGMA_W)
        mnvOrbitalState = mnvOrbitalStateEnd(builder)
        return mnvOrbitalState
