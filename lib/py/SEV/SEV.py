# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Space Environment Observation Detail
class SEV(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SEV()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSEV(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SEVBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x45\x56", size_prefixed=size_prefixed)

    # SEV
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SEV
    def OB_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SEV
    def OB_UO_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SEV
    def OB_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SEV
    def OB_STRING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SEV
    def OB_ARRAY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # SEV
    def OB_ARRAYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SEV
    def OB_ARRAYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # SEV
    def OB_BOOL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # SEV
    def OB_QUALITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SEV
    def OB_DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def SEVStart(builder):
    builder.StartObject(8)

def Start(builder):
    SEVStart(builder)

def SEVAddOB_TYPE(builder, OB_TYPE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(OB_TYPE), 0)

def AddOB_TYPE(builder, OB_TYPE):
    SEVAddOB_TYPE(builder, OB_TYPE)

def SEVAddOB_UO_M(builder, OB_UO_M):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(OB_UO_M), 0)

def AddOB_UO_M(builder, OB_UO_M):
    SEVAddOB_UO_M(builder, OB_UO_M)

def SEVAddOB_VALUE(builder, OB_VALUE):
    builder.PrependFloat64Slot(2, OB_VALUE, 0.0)

def AddOB_VALUE(builder, OB_VALUE):
    SEVAddOB_VALUE(builder, OB_VALUE)

def SEVAddOB_STRING(builder, OB_STRING):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(OB_STRING), 0)

def AddOB_STRING(builder, OB_STRING):
    SEVAddOB_STRING(builder, OB_STRING)

def SEVAddOB_ARRAY(builder, OB_ARRAY):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(OB_ARRAY), 0)

def AddOB_ARRAY(builder, OB_ARRAY):
    SEVAddOB_ARRAY(builder, OB_ARRAY)

def SEVStartOB_ARRAYVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartOB_ARRAYVector(builder, numElems):
    return SEVStartOB_ARRAYVector(builder, numElems)

def SEVAddOB_BOOL(builder, OB_BOOL):
    builder.PrependBoolSlot(5, OB_BOOL, 0)

def AddOB_BOOL(builder, OB_BOOL):
    SEVAddOB_BOOL(builder, OB_BOOL)

def SEVAddOB_QUALITY(builder, OB_QUALITY):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(OB_QUALITY), 0)

def AddOB_QUALITY(builder, OB_QUALITY):
    SEVAddOB_QUALITY(builder, OB_QUALITY)

def SEVAddOB_DESCRIPTION(builder, OB_DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(OB_DESCRIPTION), 0)

def AddOB_DESCRIPTION(builder, OB_DESCRIPTION):
    SEVAddOB_DESCRIPTION(builder, OB_DESCRIPTION)

def SEVEnd(builder):
    return builder.EndObject()

def End(builder):
    return SEVEnd(builder)

try:
    from typing import List
except:
    pass

class SEVT(object):

    # SEVT
    def __init__(self):
        self.OB_TYPE = None  # type: str
        self.OB_UO_M = None  # type: str
        self.OB_VALUE = 0.0  # type: float
        self.OB_STRING = None  # type: str
        self.OB_ARRAY = None  # type: List[str]
        self.OB_BOOL = False  # type: bool
        self.OB_QUALITY = None  # type: str
        self.OB_DESCRIPTION = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        SEV = SEV()
        SEV.Init(buf, pos)
        return cls.InitFromObj(SEV)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, SEV):
        x = SEVT()
        x._UnPack(SEV)
        return x

    # SEVT
    def _UnPack(self, SEV):
        if SEV is None:
            return
        self.OB_TYPE = SEV.OB_TYPE()
        self.OB_UO_M = SEV.OB_UO_M()
        self.OB_VALUE = SEV.OB_VALUE()
        self.OB_STRING = SEV.OB_STRING()
        if not SEV.OB_ARRAYIsNone():
            self.OB_ARRAY = []
            for i in range(SEV.OB_ARRAYLength()):
                self.OB_ARRAY.append(SEV.OB_ARRAY(i))
        self.OB_BOOL = SEV.OB_BOOL()
        self.OB_QUALITY = SEV.OB_QUALITY()
        self.OB_DESCRIPTION = SEV.OB_DESCRIPTION()

    # SEVT
    def Pack(self, builder):
        if self.OB_TYPE is not None:
            OB_TYPE = builder.CreateString(self.OB_TYPE)
        if self.OB_UO_M is not None:
            OB_UO_M = builder.CreateString(self.OB_UO_M)
        if self.OB_STRING is not None:
            OB_STRING = builder.CreateString(self.OB_STRING)
        if self.OB_ARRAY is not None:
            OB_ARRAYlist = []
            for i in range(len(self.OB_ARRAY)):
                OB_ARRAYlist.append(builder.CreateString(self.OB_ARRAY[i]))
            SEVStartOB_ARRAYVector(builder, len(self.OB_ARRAY))
            for i in reversed(range(len(self.OB_ARRAY))):
                builder.PrependUOffsetTRelative(OB_ARRAYlist[i])
            OB_ARRAY = builder.EndVector()
        if self.OB_QUALITY is not None:
            OB_QUALITY = builder.CreateString(self.OB_QUALITY)
        if self.OB_DESCRIPTION is not None:
            OB_DESCRIPTION = builder.CreateString(self.OB_DESCRIPTION)
        SEVStart(builder)
        if self.OB_TYPE is not None:
            SEVAddOB_TYPE(builder, OB_TYPE)
        if self.OB_UO_M is not None:
            SEVAddOB_UO_M(builder, OB_UO_M)
        SEVAddOB_VALUE(builder, self.OB_VALUE)
        if self.OB_STRING is not None:
            SEVAddOB_STRING(builder, OB_STRING)
        if self.OB_ARRAY is not None:
            SEVAddOB_ARRAY(builder, OB_ARRAY)
        SEVAddOB_BOOL(builder, self.OB_BOOL)
        if self.OB_QUALITY is not None:
            SEVAddOB_QUALITY(builder, OB_QUALITY)
        if self.OB_DESCRIPTION is not None:
            SEVAddOB_DESCRIPTION(builder, OB_DESCRIPTION)
        SEV = SEVEnd(builder)
        return SEV
