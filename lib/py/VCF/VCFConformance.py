# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Everything needed to decide whether this card is servable, and whether two
# implementations produced the same bytes.
class VCFConformance(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VCFConformance()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVCFConformance(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VCFConformanceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x43\x46", size_prefixed=size_prefixed)

    # VCFConformance
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Card format version token verbatim as emitted, e.g. `3.0`.
    # VCFConformance
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Identifier of the emission profile the card conforms to, verbatim, naming
    # the annex revision the publisher implemented.
    # VCFConformance
    def PROFILE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Line terminator the card text uses.
    # VCFConformance
    def LINE_ENDING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Octet width beyond which a content line is folded onto continuation lines.
    # 0 means the publisher folded nothing, which is non-canonical.
    # VCFConformance
    def FOLD_OCTET_WIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Octet length of `CARD` exactly as emitted, folding and terminators
    # included. This is the number a serving surface budgets against.
    # VCFConformance
    def BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Octet length the same card would have with folding removed. Carried so a
    # folding disagreement is detectable without unfolding the text.
    # VCFConformance
    def UNFOLDED_BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Octet ceiling of the surface this card was produced for. 0 means the
    # publisher states no ceiling; it never means a ceiling of zero.
    # VCFConformance
    def BYTE_BUDGET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Whether BYTE_LENGTH is within BYTE_BUDGET. False with a non-zero budget
    # means the card is a valid record that is NOT servable on that surface.
    # VCFConformance
    def WITHIN_BYTE_BUDGET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # SHA-256 of `CARD` as 64 lowercase hexadecimal characters. This is the
    # value two implementations compare to prove byte-identical projection.
    # VCFConformance
    def CARD_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of property rows emitted, unfolded. Carried so a truncated card is
    # detectable before parsing.
    # VCFConformance
    def PROPERTY_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Whether the card carries a complete verification chain: a signing-key row,
    # an encryption-key row, and a profile-signature row, all present in the
    # UNFOLDED text. A card without all three cannot verify its own subject and
    # is not servable as an identity card.
    # VCFConformance
    def CARRIES_VERIFICATION_CHAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def VCFConformanceStart(builder):
    builder.StartObject(11)

def Start(builder):
    VCFConformanceStart(builder)

def VCFConformanceAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    VCFConformanceAddVERSION(builder, VERSION)

def VCFConformanceAddPROFILE(builder, PROFILE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PROFILE), 0)

def AddPROFILE(builder, PROFILE):
    VCFConformanceAddPROFILE(builder, PROFILE)

def VCFConformanceAddLINE_ENDING(builder, LINE_ENDING):
    builder.PrependInt8Slot(2, LINE_ENDING, 0)

def AddLINE_ENDING(builder, LINE_ENDING):
    VCFConformanceAddLINE_ENDING(builder, LINE_ENDING)

def VCFConformanceAddFOLD_OCTET_WIDTH(builder, FOLD_OCTET_WIDTH):
    builder.PrependUint32Slot(3, FOLD_OCTET_WIDTH, 0)

def AddFOLD_OCTET_WIDTH(builder, FOLD_OCTET_WIDTH):
    VCFConformanceAddFOLD_OCTET_WIDTH(builder, FOLD_OCTET_WIDTH)

def VCFConformanceAddBYTE_LENGTH(builder, BYTE_LENGTH):
    builder.PrependUint32Slot(4, BYTE_LENGTH, 0)

def AddBYTE_LENGTH(builder, BYTE_LENGTH):
    VCFConformanceAddBYTE_LENGTH(builder, BYTE_LENGTH)

def VCFConformanceAddUNFOLDED_BYTE_LENGTH(builder, UNFOLDED_BYTE_LENGTH):
    builder.PrependUint32Slot(5, UNFOLDED_BYTE_LENGTH, 0)

def AddUNFOLDED_BYTE_LENGTH(builder, UNFOLDED_BYTE_LENGTH):
    VCFConformanceAddUNFOLDED_BYTE_LENGTH(builder, UNFOLDED_BYTE_LENGTH)

def VCFConformanceAddBYTE_BUDGET(builder, BYTE_BUDGET):
    builder.PrependUint32Slot(6, BYTE_BUDGET, 0)

def AddBYTE_BUDGET(builder, BYTE_BUDGET):
    VCFConformanceAddBYTE_BUDGET(builder, BYTE_BUDGET)

def VCFConformanceAddWITHIN_BYTE_BUDGET(builder, WITHIN_BYTE_BUDGET):
    builder.PrependBoolSlot(7, WITHIN_BYTE_BUDGET, 0)

def AddWITHIN_BYTE_BUDGET(builder, WITHIN_BYTE_BUDGET):
    VCFConformanceAddWITHIN_BYTE_BUDGET(builder, WITHIN_BYTE_BUDGET)

def VCFConformanceAddCARD_SHA256(builder, CARD_SHA256):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(CARD_SHA256), 0)

def AddCARD_SHA256(builder, CARD_SHA256):
    VCFConformanceAddCARD_SHA256(builder, CARD_SHA256)

def VCFConformanceAddPROPERTY_COUNT(builder, PROPERTY_COUNT):
    builder.PrependUint32Slot(9, PROPERTY_COUNT, 0)

def AddPROPERTY_COUNT(builder, PROPERTY_COUNT):
    VCFConformanceAddPROPERTY_COUNT(builder, PROPERTY_COUNT)

def VCFConformanceAddCARRIES_VERIFICATION_CHAIN(builder, CARRIES_VERIFICATION_CHAIN):
    builder.PrependBoolSlot(10, CARRIES_VERIFICATION_CHAIN, 0)

def AddCARRIES_VERIFICATION_CHAIN(builder, CARRIES_VERIFICATION_CHAIN):
    VCFConformanceAddCARRIES_VERIFICATION_CHAIN(builder, CARRIES_VERIFICATION_CHAIN)

def VCFConformanceEnd(builder):
    return builder.EndObject()

def End(builder):
    return VCFConformanceEnd(builder)


class VCFConformanceT(object):

    # VCFConformanceT
    def __init__(
        self,
        VERSION = None,
        PROFILE = None,
        LINE_ENDING = 0,
        FOLD_OCTET_WIDTH = 0,
        BYTE_LENGTH = 0,
        UNFOLDED_BYTE_LENGTH = 0,
        BYTE_BUDGET = 0,
        WITHIN_BYTE_BUDGET = False,
        CARD_SHA256 = None,
        PROPERTY_COUNT = 0,
        CARRIES_VERIFICATION_CHAIN = False,
    ):
        self.VERSION = VERSION  # type: Optional[str]
        self.PROFILE = PROFILE  # type: Optional[str]
        self.LINE_ENDING = LINE_ENDING  # type: int
        self.FOLD_OCTET_WIDTH = FOLD_OCTET_WIDTH  # type: int
        self.BYTE_LENGTH = BYTE_LENGTH  # type: int
        self.UNFOLDED_BYTE_LENGTH = UNFOLDED_BYTE_LENGTH  # type: int
        self.BYTE_BUDGET = BYTE_BUDGET  # type: int
        self.WITHIN_BYTE_BUDGET = WITHIN_BYTE_BUDGET  # type: bool
        self.CARD_SHA256 = CARD_SHA256  # type: Optional[str]
        self.PROPERTY_COUNT = PROPERTY_COUNT  # type: int
        self.CARRIES_VERIFICATION_CHAIN = CARRIES_VERIFICATION_CHAIN  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVcfconformance = VCFConformance()
        tmpVcfconformance.Init(buf, pos)
        return cls.InitFromObj(tmpVcfconformance)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVcfconformance):
        x = VCFConformanceT()
        x._UnPack(tmpVcfconformance)
        return x

    # VCFConformanceT
    def _UnPack(self, VCFConformance):
        if VCFConformance is None:
            return
        self.VERSION = VCFConformance.VERSION()
        self.PROFILE = VCFConformance.PROFILE()
        self.LINE_ENDING = VCFConformance.LINE_ENDING()
        self.FOLD_OCTET_WIDTH = VCFConformance.FOLD_OCTET_WIDTH()
        self.BYTE_LENGTH = VCFConformance.BYTE_LENGTH()
        self.UNFOLDED_BYTE_LENGTH = VCFConformance.UNFOLDED_BYTE_LENGTH()
        self.BYTE_BUDGET = VCFConformance.BYTE_BUDGET()
        self.WITHIN_BYTE_BUDGET = VCFConformance.WITHIN_BYTE_BUDGET()
        self.CARD_SHA256 = VCFConformance.CARD_SHA256()
        self.PROPERTY_COUNT = VCFConformance.PROPERTY_COUNT()
        self.CARRIES_VERIFICATION_CHAIN = VCFConformance.CARRIES_VERIFICATION_CHAIN()

    # VCFConformanceT
    def Pack(self, builder):
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        if self.PROFILE is not None:
            PROFILE = builder.CreateString(self.PROFILE)
        if self.CARD_SHA256 is not None:
            CARD_SHA256 = builder.CreateString(self.CARD_SHA256)
        VCFConformanceStart(builder)
        if self.VERSION is not None:
            VCFConformanceAddVERSION(builder, VERSION)
        if self.PROFILE is not None:
            VCFConformanceAddPROFILE(builder, PROFILE)
        VCFConformanceAddLINE_ENDING(builder, self.LINE_ENDING)
        VCFConformanceAddFOLD_OCTET_WIDTH(builder, self.FOLD_OCTET_WIDTH)
        VCFConformanceAddBYTE_LENGTH(builder, self.BYTE_LENGTH)
        VCFConformanceAddUNFOLDED_BYTE_LENGTH(builder, self.UNFOLDED_BYTE_LENGTH)
        VCFConformanceAddBYTE_BUDGET(builder, self.BYTE_BUDGET)
        VCFConformanceAddWITHIN_BYTE_BUDGET(builder, self.WITHIN_BYTE_BUDGET)
        if self.CARD_SHA256 is not None:
            VCFConformanceAddCARD_SHA256(builder, CARD_SHA256)
        VCFConformanceAddPROPERTY_COUNT(builder, self.PROPERTY_COUNT)
        VCFConformanceAddCARRIES_VERIFICATION_CHAIN(builder, self.CARRIES_VERIFICATION_CHAIN)
        VCFConformance = VCFConformanceEnd(builder)
        return VCFConformance
