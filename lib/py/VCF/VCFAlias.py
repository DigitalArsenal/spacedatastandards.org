# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One identity address row: the machine-readable half of the card.
class VCFAlias(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VCFAlias()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVCFAlias(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VCFAliasBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x43\x46", size_prefixed=size_prefixed)

    # VCFAlias
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # What the row carries.
    # VCFAlias
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # The row's kind label verbatim as emitted — the leftmost domain label.
    # Authoritative when KIND is OTHER.
    # VCFAlias
    def KIND_TOKEN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Everything left of the separator in the emitted address, verbatim.
    # VCFAlias
    def LOCAL_PART(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Everything right of the separator in the emitted address, verbatim. This
    # is publisher data: the identity domain of the deployment that produced the
    # card, never a value this standard fixes.
    # VCFAlias
    def DOMAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The complete emitted address, `LOCAL_PART` then the separator then
    # `DOMAIN`, verbatim. Carried whole because it is what the importer stores
    # and what a consumer matches on.
    # VCFAlias
    def ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Transport encoding of LOCAL_PART verbatim and lowercase. An address
    # local part has a hard octet cap, so raw hexadecimal key material does not
    # fit and a compact binary-to-text encoding is REQUIRED for key rows.
    # VCFAlias
    def ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Cryptographic algorithm the row's key or signature belongs to, verbatim as
    # the profile states it. A key without a named algorithm is not verifiable.
    # VCFAlias
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The profile's published public key value verbatim, for key rows. The SAME
    # bytes LOCAL_PART transport-encodes, carried in the profile's own spelling
    # so a consumer can join card to profile without decoding. NEVER an extended
    # public key and NEVER a derivation path.
    # VCFAlias
    def PUBLIC_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Ledger token verbatim for LEDGER_ACCOUNT rows.
    # VCFAlias
    def CHAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Whether this row survives into the COMPACT form.
    # VCFAlias
    def ON_COMPACT_CARD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Octet length of the complete UNFOLDED row as emitted.
    # VCFAlias
    def OCTET_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def VCFAliasStart(builder):
    builder.StartObject(11)

def Start(builder):
    VCFAliasStart(builder)

def VCFAliasAddKIND(builder, KIND):
    builder.PrependInt8Slot(0, KIND, 0)

def AddKIND(builder, KIND):
    VCFAliasAddKIND(builder, KIND)

def VCFAliasAddKIND_TOKEN(builder, KIND_TOKEN):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(KIND_TOKEN), 0)

def AddKIND_TOKEN(builder, KIND_TOKEN):
    VCFAliasAddKIND_TOKEN(builder, KIND_TOKEN)

def VCFAliasAddLOCAL_PART(builder, LOCAL_PART):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(LOCAL_PART), 0)

def AddLOCAL_PART(builder, LOCAL_PART):
    VCFAliasAddLOCAL_PART(builder, LOCAL_PART)

def VCFAliasAddDOMAIN(builder, DOMAIN):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(DOMAIN), 0)

def AddDOMAIN(builder, DOMAIN):
    VCFAliasAddDOMAIN(builder, DOMAIN)

def VCFAliasAddADDRESS(builder, ADDRESS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ADDRESS), 0)

def AddADDRESS(builder, ADDRESS):
    VCFAliasAddADDRESS(builder, ADDRESS)

def VCFAliasAddENCODING(builder, ENCODING):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ENCODING), 0)

def AddENCODING(builder, ENCODING):
    VCFAliasAddENCODING(builder, ENCODING)

def VCFAliasAddALGORITHM(builder, ALGORITHM):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ALGORITHM), 0)

def AddALGORITHM(builder, ALGORITHM):
    VCFAliasAddALGORITHM(builder, ALGORITHM)

def VCFAliasAddPUBLIC_KEY(builder, PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(PUBLIC_KEY), 0)

def AddPUBLIC_KEY(builder, PUBLIC_KEY):
    VCFAliasAddPUBLIC_KEY(builder, PUBLIC_KEY)

def VCFAliasAddCHAIN(builder, CHAIN):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(CHAIN), 0)

def AddCHAIN(builder, CHAIN):
    VCFAliasAddCHAIN(builder, CHAIN)

def VCFAliasAddON_COMPACT_CARD(builder, ON_COMPACT_CARD):
    builder.PrependBoolSlot(9, ON_COMPACT_CARD, 0)

def AddON_COMPACT_CARD(builder, ON_COMPACT_CARD):
    VCFAliasAddON_COMPACT_CARD(builder, ON_COMPACT_CARD)

def VCFAliasAddOCTET_LENGTH(builder, OCTET_LENGTH):
    builder.PrependUint32Slot(10, OCTET_LENGTH, 0)

def AddOCTET_LENGTH(builder, OCTET_LENGTH):
    VCFAliasAddOCTET_LENGTH(builder, OCTET_LENGTH)

def VCFAliasEnd(builder):
    return builder.EndObject()

def End(builder):
    return VCFAliasEnd(builder)


class VCFAliasT(object):

    # VCFAliasT
    def __init__(
        self,
        KIND = 0,
        KIND_TOKEN = None,
        LOCAL_PART = None,
        DOMAIN = None,
        ADDRESS = None,
        ENCODING = None,
        ALGORITHM = None,
        PUBLIC_KEY = None,
        CHAIN = None,
        ON_COMPACT_CARD = False,
        OCTET_LENGTH = 0,
    ):
        self.KIND = KIND  # type: int
        self.KIND_TOKEN = KIND_TOKEN  # type: Optional[str]
        self.LOCAL_PART = LOCAL_PART  # type: Optional[str]
        self.DOMAIN = DOMAIN  # type: Optional[str]
        self.ADDRESS = ADDRESS  # type: Optional[str]
        self.ENCODING = ENCODING  # type: Optional[str]
        self.ALGORITHM = ALGORITHM  # type: Optional[str]
        self.PUBLIC_KEY = PUBLIC_KEY  # type: Optional[str]
        self.CHAIN = CHAIN  # type: Optional[str]
        self.ON_COMPACT_CARD = ON_COMPACT_CARD  # type: bool
        self.OCTET_LENGTH = OCTET_LENGTH  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVcfalias = VCFAlias()
        tmpVcfalias.Init(buf, pos)
        return cls.InitFromObj(tmpVcfalias)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVcfalias):
        x = VCFAliasT()
        x._UnPack(tmpVcfalias)
        return x

    # VCFAliasT
    def _UnPack(self, VCFAlias):
        if VCFAlias is None:
            return
        self.KIND = VCFAlias.KIND()
        self.KIND_TOKEN = VCFAlias.KIND_TOKEN()
        self.LOCAL_PART = VCFAlias.LOCAL_PART()
        self.DOMAIN = VCFAlias.DOMAIN()
        self.ADDRESS = VCFAlias.ADDRESS()
        self.ENCODING = VCFAlias.ENCODING()
        self.ALGORITHM = VCFAlias.ALGORITHM()
        self.PUBLIC_KEY = VCFAlias.PUBLIC_KEY()
        self.CHAIN = VCFAlias.CHAIN()
        self.ON_COMPACT_CARD = VCFAlias.ON_COMPACT_CARD()
        self.OCTET_LENGTH = VCFAlias.OCTET_LENGTH()

    # VCFAliasT
    def Pack(self, builder):
        if self.KIND_TOKEN is not None:
            KIND_TOKEN = builder.CreateString(self.KIND_TOKEN)
        if self.LOCAL_PART is not None:
            LOCAL_PART = builder.CreateString(self.LOCAL_PART)
        if self.DOMAIN is not None:
            DOMAIN = builder.CreateString(self.DOMAIN)
        if self.ADDRESS is not None:
            ADDRESS = builder.CreateString(self.ADDRESS)
        if self.ENCODING is not None:
            ENCODING = builder.CreateString(self.ENCODING)
        if self.ALGORITHM is not None:
            ALGORITHM = builder.CreateString(self.ALGORITHM)
        if self.PUBLIC_KEY is not None:
            PUBLIC_KEY = builder.CreateString(self.PUBLIC_KEY)
        if self.CHAIN is not None:
            CHAIN = builder.CreateString(self.CHAIN)
        VCFAliasStart(builder)
        VCFAliasAddKIND(builder, self.KIND)
        if self.KIND_TOKEN is not None:
            VCFAliasAddKIND_TOKEN(builder, KIND_TOKEN)
        if self.LOCAL_PART is not None:
            VCFAliasAddLOCAL_PART(builder, LOCAL_PART)
        if self.DOMAIN is not None:
            VCFAliasAddDOMAIN(builder, DOMAIN)
        if self.ADDRESS is not None:
            VCFAliasAddADDRESS(builder, ADDRESS)
        if self.ENCODING is not None:
            VCFAliasAddENCODING(builder, ENCODING)
        if self.ALGORITHM is not None:
            VCFAliasAddALGORITHM(builder, ALGORITHM)
        if self.PUBLIC_KEY is not None:
            VCFAliasAddPUBLIC_KEY(builder, PUBLIC_KEY)
        if self.CHAIN is not None:
            VCFAliasAddCHAIN(builder, CHAIN)
        VCFAliasAddON_COMPACT_CARD(builder, self.ON_COMPACT_CARD)
        VCFAliasAddOCTET_LENGTH(builder, self.OCTET_LENGTH)
        VCFAlias = VCFAliasEnd(builder)
        return VCFAlias
