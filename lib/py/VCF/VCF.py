# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# vCard Projection Card
class VCF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VCF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVCF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VCFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x43\x46", size_prefixed=size_prefixed)

    # VCF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher-stable identifier of this card. Together with the publishing key
    # it is the card's identity across every republication.
    # VCF
    def CARD_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Which canonical emission form this record carries.
    # VCF
    def FORM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # The emitted card text VERBATIM: every octet an importer will read,
    # including folding and line terminators. Authoritative over PROPERTIES and
    # ALIASES in every disagreement.
    # VCF
    def CARD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Media type the card is served as, verbatim, including its charset
    # parameter.
    # VCF
    def MEDIA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The profile this card projects and the provenance binding them.
    # VCF
    def SUBJECT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VCFSubject import VCFSubject
            obj = VCFSubject()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # The publisher's parse of `CARD`, in canonical emission order.
    # VCF
    def PROPERTIES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from VCFProperty import VCFProperty
            obj = VCFProperty()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VCF
    def PROPERTIESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VCF
    def PROPERTIESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # The identity address rows carried by `CARD`, in canonical emission order.
    # VCF
    def ALIASES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from VCFAlias import VCFAlias
            obj = VCFAlias()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VCF
    def ALIASESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VCF
    def ALIASESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # How the card carries a portrait, if at all.
    # VCF
    def PHOTO_POLICY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Locator of the portrait content under URI_REFERENCE, verbatim as emitted.
    # Empty under every other policy.
    # VCF
    def PHOTO_URI(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Media type of the portrait content verbatim, when one is carried.
    # VCF
    def PHOTO_MEDIA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Emission facts and the digest two implementations compare.
    # VCF
    def CONFORMANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VCFConformance import VCFConformance
            obj = VCFConformance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Reverse-DNS identifier of the component that produced the card, so two
    # projectors are distinguishable when their bytes differ.
    # VCF
    def PRODUCER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Build or release identifier of that component verbatim.
    # VCF
    def PRODUCER_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp at which the card was projected.
    # VCF
    def GENERATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp after which a consumer re-projects
    # from the profile rather than serving this card. Empty means no expiry.
    # VCF
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `namespace:value` facets, sorted and deduplicated. See TAG GRAMMAR above.
    # VCF
    def TAGS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # VCF
    def TAGSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VCF
    def TAGSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        return o == 0

    # Publisher note about this card; never a substitute for a field.
    # VCF
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def VCFStart(builder):
    builder.StartObject(17)

def Start(builder):
    VCFStart(builder)

def VCFAddCARD_ID(builder, CARD_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CARD_ID), 0)

def AddCARD_ID(builder, CARD_ID):
    VCFAddCARD_ID(builder, CARD_ID)

def VCFAddFORM(builder, FORM):
    builder.PrependInt8Slot(1, FORM, 0)

def AddFORM(builder, FORM):
    VCFAddFORM(builder, FORM)

def VCFAddCARD(builder, CARD):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(CARD), 0)

def AddCARD(builder, CARD):
    VCFAddCARD(builder, CARD)

def VCFAddMEDIA_TYPE(builder, MEDIA_TYPE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MEDIA_TYPE), 0)

def AddMEDIA_TYPE(builder, MEDIA_TYPE):
    VCFAddMEDIA_TYPE(builder, MEDIA_TYPE)

def VCFAddSUBJECT(builder, SUBJECT):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SUBJECT), 0)

def AddSUBJECT(builder, SUBJECT):
    VCFAddSUBJECT(builder, SUBJECT)

def VCFAddPROPERTIES(builder, PROPERTIES):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PROPERTIES), 0)

def AddPROPERTIES(builder, PROPERTIES):
    VCFAddPROPERTIES(builder, PROPERTIES)

def VCFStartPROPERTIESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPROPERTIESVector(builder, numElems):
    return VCFStartPROPERTIESVector(builder, numElems)

def VCFCreatePROPERTIESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePROPERTIESVector(builder, data):
    VCFCreatePROPERTIESVector(builder, data)

def VCFAddALIASES(builder, ALIASES):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ALIASES), 0)

def AddALIASES(builder, ALIASES):
    VCFAddALIASES(builder, ALIASES)

def VCFStartALIASESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartALIASESVector(builder, numElems):
    return VCFStartALIASESVector(builder, numElems)

def VCFCreateALIASESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateALIASESVector(builder, data):
    VCFCreateALIASESVector(builder, data)

def VCFAddPHOTO_POLICY(builder, PHOTO_POLICY):
    builder.PrependInt8Slot(7, PHOTO_POLICY, 0)

def AddPHOTO_POLICY(builder, PHOTO_POLICY):
    VCFAddPHOTO_POLICY(builder, PHOTO_POLICY)

def VCFAddPHOTO_URI(builder, PHOTO_URI):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(PHOTO_URI), 0)

def AddPHOTO_URI(builder, PHOTO_URI):
    VCFAddPHOTO_URI(builder, PHOTO_URI)

def VCFAddPHOTO_MEDIA_TYPE(builder, PHOTO_MEDIA_TYPE):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(PHOTO_MEDIA_TYPE), 0)

def AddPHOTO_MEDIA_TYPE(builder, PHOTO_MEDIA_TYPE):
    VCFAddPHOTO_MEDIA_TYPE(builder, PHOTO_MEDIA_TYPE)

def VCFAddCONFORMANCE(builder, CONFORMANCE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(CONFORMANCE), 0)

def AddCONFORMANCE(builder, CONFORMANCE):
    VCFAddCONFORMANCE(builder, CONFORMANCE)

def VCFAddPRODUCER_ID(builder, PRODUCER_ID):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(PRODUCER_ID), 0)

def AddPRODUCER_ID(builder, PRODUCER_ID):
    VCFAddPRODUCER_ID(builder, PRODUCER_ID)

def VCFAddPRODUCER_VERSION(builder, PRODUCER_VERSION):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(PRODUCER_VERSION), 0)

def AddPRODUCER_VERSION(builder, PRODUCER_VERSION):
    VCFAddPRODUCER_VERSION(builder, PRODUCER_VERSION)

def VCFAddGENERATED_AT(builder, GENERATED_AT):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(GENERATED_AT), 0)

def AddGENERATED_AT(builder, GENERATED_AT):
    VCFAddGENERATED_AT(builder, GENERATED_AT)

def VCFAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(EXPIRES_AT), 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    VCFAddEXPIRES_AT(builder, EXPIRES_AT)

def VCFAddTAGS(builder, TAGS):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(TAGS), 0)

def AddTAGS(builder, TAGS):
    VCFAddTAGS(builder, TAGS)

def VCFStartTAGSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTAGSVector(builder, numElems):
    return VCFStartTAGSVector(builder, numElems)

def VCFCreateTAGSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateTAGSVector(builder, data):
    VCFCreateTAGSVector(builder, data)

def VCFAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    VCFAddNOTES(builder, NOTES)

def VCFEnd(builder):
    return builder.EndObject()

def End(builder):
    return VCFEnd(builder)

import VCFAlias
import VCFConformance
import VCFProperty
import VCFSubject
try:
    from typing import List, Optional
except:
    pass

class VCFT(object):

    # VCFT
    def __init__(
        self,
        CARD_ID = None,
        FORM = 0,
        CARD = None,
        MEDIA_TYPE = None,
        SUBJECT = None,
        PROPERTIES = None,
        ALIASES = None,
        PHOTO_POLICY = 0,
        PHOTO_URI = None,
        PHOTO_MEDIA_TYPE = None,
        CONFORMANCE = None,
        PRODUCER_ID = None,
        PRODUCER_VERSION = None,
        GENERATED_AT = None,
        EXPIRES_AT = None,
        TAGS = None,
        NOTES = None,
    ):
        self.CARD_ID = CARD_ID  # type: Optional[str]
        self.FORM = FORM  # type: int
        self.CARD = CARD  # type: Optional[str]
        self.MEDIA_TYPE = MEDIA_TYPE  # type: Optional[str]
        self.SUBJECT = SUBJECT  # type: Optional[VCFSubject.VCFSubjectT]
        self.PROPERTIES = PROPERTIES  # type: Optional[List[VCFProperty.VCFPropertyT]]
        self.ALIASES = ALIASES  # type: Optional[List[VCFAlias.VCFAliasT]]
        self.PHOTO_POLICY = PHOTO_POLICY  # type: int
        self.PHOTO_URI = PHOTO_URI  # type: Optional[str]
        self.PHOTO_MEDIA_TYPE = PHOTO_MEDIA_TYPE  # type: Optional[str]
        self.CONFORMANCE = CONFORMANCE  # type: Optional[VCFConformance.VCFConformanceT]
        self.PRODUCER_ID = PRODUCER_ID  # type: Optional[str]
        self.PRODUCER_VERSION = PRODUCER_VERSION  # type: Optional[str]
        self.GENERATED_AT = GENERATED_AT  # type: Optional[str]
        self.EXPIRES_AT = EXPIRES_AT  # type: Optional[str]
        self.TAGS = TAGS  # type: Optional[List[Optional[str]]]
        self.NOTES = NOTES  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVcf = VCF()
        tmpVcf.Init(buf, pos)
        return cls.InitFromObj(tmpVcf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVcf):
        x = VCFT()
        x._UnPack(tmpVcf)
        return x

    # VCFT
    def _UnPack(self, VCF):
        if VCF is None:
            return
        self.CARD_ID = VCF.CARD_ID()
        self.FORM = VCF.FORM()
        self.CARD = VCF.CARD()
        self.MEDIA_TYPE = VCF.MEDIA_TYPE()
        if VCF.SUBJECT() is not None:
            self.SUBJECT = VCFSubject.VCFSubjectT.InitFromObj(VCF.SUBJECT())
        if not VCF.PROPERTIESIsNone():
            self.PROPERTIES = []
            for i in range(VCF.PROPERTIESLength()):
                if VCF.PROPERTIES(i) is None:
                    self.PROPERTIES.append(None)
                else:
                    vCFProperty_ = VCFProperty.VCFPropertyT.InitFromObj(VCF.PROPERTIES(i))
                    self.PROPERTIES.append(vCFProperty_)
        if not VCF.ALIASESIsNone():
            self.ALIASES = []
            for i in range(VCF.ALIASESLength()):
                if VCF.ALIASES(i) is None:
                    self.ALIASES.append(None)
                else:
                    vCFAlias_ = VCFAlias.VCFAliasT.InitFromObj(VCF.ALIASES(i))
                    self.ALIASES.append(vCFAlias_)
        self.PHOTO_POLICY = VCF.PHOTO_POLICY()
        self.PHOTO_URI = VCF.PHOTO_URI()
        self.PHOTO_MEDIA_TYPE = VCF.PHOTO_MEDIA_TYPE()
        if VCF.CONFORMANCE() is not None:
            self.CONFORMANCE = VCFConformance.VCFConformanceT.InitFromObj(VCF.CONFORMANCE())
        self.PRODUCER_ID = VCF.PRODUCER_ID()
        self.PRODUCER_VERSION = VCF.PRODUCER_VERSION()
        self.GENERATED_AT = VCF.GENERATED_AT()
        self.EXPIRES_AT = VCF.EXPIRES_AT()
        if not VCF.TAGSIsNone():
            self.TAGS = []
            for i in range(VCF.TAGSLength()):
                self.TAGS.append(VCF.TAGS(i))
        self.NOTES = VCF.NOTES()

    # VCFT
    def Pack(self, builder):
        if self.CARD_ID is not None:
            CARD_ID = builder.CreateString(self.CARD_ID)
        if self.CARD is not None:
            CARD = builder.CreateString(self.CARD)
        if self.MEDIA_TYPE is not None:
            MEDIA_TYPE = builder.CreateString(self.MEDIA_TYPE)
        if self.SUBJECT is not None:
            SUBJECT = self.SUBJECT.Pack(builder)
        if self.PROPERTIES is not None:
            PROPERTIESlist = []
            for i in range(len(self.PROPERTIES)):
                PROPERTIESlist.append(self.PROPERTIES[i].Pack(builder))
            VCFStartPROPERTIESVector(builder, len(self.PROPERTIES))
            for i in reversed(range(len(self.PROPERTIES))):
                builder.PrependUOffsetTRelative(PROPERTIESlist[i])
            PROPERTIES = builder.EndVector()
        if self.ALIASES is not None:
            ALIASESlist = []
            for i in range(len(self.ALIASES)):
                ALIASESlist.append(self.ALIASES[i].Pack(builder))
            VCFStartALIASESVector(builder, len(self.ALIASES))
            for i in reversed(range(len(self.ALIASES))):
                builder.PrependUOffsetTRelative(ALIASESlist[i])
            ALIASES = builder.EndVector()
        if self.PHOTO_URI is not None:
            PHOTO_URI = builder.CreateString(self.PHOTO_URI)
        if self.PHOTO_MEDIA_TYPE is not None:
            PHOTO_MEDIA_TYPE = builder.CreateString(self.PHOTO_MEDIA_TYPE)
        if self.CONFORMANCE is not None:
            CONFORMANCE = self.CONFORMANCE.Pack(builder)
        if self.PRODUCER_ID is not None:
            PRODUCER_ID = builder.CreateString(self.PRODUCER_ID)
        if self.PRODUCER_VERSION is not None:
            PRODUCER_VERSION = builder.CreateString(self.PRODUCER_VERSION)
        if self.GENERATED_AT is not None:
            GENERATED_AT = builder.CreateString(self.GENERATED_AT)
        if self.EXPIRES_AT is not None:
            EXPIRES_AT = builder.CreateString(self.EXPIRES_AT)
        if self.TAGS is not None:
            TAGSlist = []
            for i in range(len(self.TAGS)):
                TAGSlist.append(builder.CreateString(self.TAGS[i]))
            VCFStartTAGSVector(builder, len(self.TAGS))
            for i in reversed(range(len(self.TAGS))):
                builder.PrependUOffsetTRelative(TAGSlist[i])
            TAGS = builder.EndVector()
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        VCFStart(builder)
        if self.CARD_ID is not None:
            VCFAddCARD_ID(builder, CARD_ID)
        VCFAddFORM(builder, self.FORM)
        if self.CARD is not None:
            VCFAddCARD(builder, CARD)
        if self.MEDIA_TYPE is not None:
            VCFAddMEDIA_TYPE(builder, MEDIA_TYPE)
        if self.SUBJECT is not None:
            VCFAddSUBJECT(builder, SUBJECT)
        if self.PROPERTIES is not None:
            VCFAddPROPERTIES(builder, PROPERTIES)
        if self.ALIASES is not None:
            VCFAddALIASES(builder, ALIASES)
        VCFAddPHOTO_POLICY(builder, self.PHOTO_POLICY)
        if self.PHOTO_URI is not None:
            VCFAddPHOTO_URI(builder, PHOTO_URI)
        if self.PHOTO_MEDIA_TYPE is not None:
            VCFAddPHOTO_MEDIA_TYPE(builder, PHOTO_MEDIA_TYPE)
        if self.CONFORMANCE is not None:
            VCFAddCONFORMANCE(builder, CONFORMANCE)
        if self.PRODUCER_ID is not None:
            VCFAddPRODUCER_ID(builder, PRODUCER_ID)
        if self.PRODUCER_VERSION is not None:
            VCFAddPRODUCER_VERSION(builder, PRODUCER_VERSION)
        if self.GENERATED_AT is not None:
            VCFAddGENERATED_AT(builder, GENERATED_AT)
        if self.EXPIRES_AT is not None:
            VCFAddEXPIRES_AT(builder, EXPIRES_AT)
        if self.TAGS is not None:
            VCFAddTAGS(builder, TAGS)
        if self.NOTES is not None:
            VCFAddNOTES(builder, NOTES)
        VCF = VCFEnd(builder)
        return VCF
