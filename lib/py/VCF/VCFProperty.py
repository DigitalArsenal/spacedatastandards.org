# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One emitted card property, as the publisher parsed its own output.
class VCFProperty(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VCFProperty()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVCFProperty(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VCFPropertyBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x43\x46", size_prefixed=size_prefixed)

    # VCFProperty
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Property name verbatim and upper-case, e.g. `EMAIL`.
    # VCFProperty
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Parameter tokens verbatim in emission order, each a single `name=value`
    # pair. A parameter the emitter wrote without a value is carried verbatim.
    # VCFProperty
    def PARAMETERS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # VCFProperty
    def PARAMETERSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VCFProperty
    def PARAMETERSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Property value with the text format's escaping REMOVED, so a consumer
    # reads a value rather than re-implementing an unescaper. `CARD` retains the
    # escaped form.
    # VCFProperty
    def VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Grouping prefix the property was emitted under, when the emitter used one.
    # VCFProperty
    def GROUP(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Zero-based position of this property in the canonical emission order for
    # this FORM. Two conforming emitters agree on this number.
    # VCFProperty
    def ORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Octet length of the property's complete UNFOLDED content line, excluding
    # the line terminator. This is the number folding is applied against.
    # VCFProperty
    def OCTET_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def VCFPropertyStart(builder):
    builder.StartObject(6)

def Start(builder):
    VCFPropertyStart(builder)

def VCFPropertyAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    VCFPropertyAddNAME(builder, NAME)

def VCFPropertyAddPARAMETERS(builder, PARAMETERS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PARAMETERS), 0)

def AddPARAMETERS(builder, PARAMETERS):
    VCFPropertyAddPARAMETERS(builder, PARAMETERS)

def VCFPropertyStartPARAMETERSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPARAMETERSVector(builder, numElems):
    return VCFPropertyStartPARAMETERSVector(builder, numElems)

def VCFPropertyCreatePARAMETERSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePARAMETERSVector(builder, data):
    VCFPropertyCreatePARAMETERSVector(builder, data)

def VCFPropertyAddVALUE(builder, VALUE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VALUE), 0)

def AddVALUE(builder, VALUE):
    VCFPropertyAddVALUE(builder, VALUE)

def VCFPropertyAddGROUP(builder, GROUP):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(GROUP), 0)

def AddGROUP(builder, GROUP):
    VCFPropertyAddGROUP(builder, GROUP)

def VCFPropertyAddORDER(builder, ORDER):
    builder.PrependUint32Slot(4, ORDER, 0)

def AddORDER(builder, ORDER):
    VCFPropertyAddORDER(builder, ORDER)

def VCFPropertyAddOCTET_LENGTH(builder, OCTET_LENGTH):
    builder.PrependUint32Slot(5, OCTET_LENGTH, 0)

def AddOCTET_LENGTH(builder, OCTET_LENGTH):
    VCFPropertyAddOCTET_LENGTH(builder, OCTET_LENGTH)

def VCFPropertyEnd(builder):
    return builder.EndObject()

def End(builder):
    return VCFPropertyEnd(builder)

try:
    from typing import List
except:
    pass

class VCFPropertyT(object):

    # VCFPropertyT
    def __init__(
        self,
        NAME = None,
        PARAMETERS = None,
        VALUE = None,
        GROUP = None,
        ORDER = 0,
        OCTET_LENGTH = 0,
    ):
        self.NAME = NAME  # type: Optional[str]
        self.PARAMETERS = PARAMETERS  # type: Optional[List[Optional[str]]]
        self.VALUE = VALUE  # type: Optional[str]
        self.GROUP = GROUP  # type: Optional[str]
        self.ORDER = ORDER  # type: int
        self.OCTET_LENGTH = OCTET_LENGTH  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVcfproperty = VCFProperty()
        tmpVcfproperty.Init(buf, pos)
        return cls.InitFromObj(tmpVcfproperty)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVcfproperty):
        x = VCFPropertyT()
        x._UnPack(tmpVcfproperty)
        return x

    # VCFPropertyT
    def _UnPack(self, VCFProperty):
        if VCFProperty is None:
            return
        self.NAME = VCFProperty.NAME()
        if not VCFProperty.PARAMETERSIsNone():
            self.PARAMETERS = []
            for i in range(VCFProperty.PARAMETERSLength()):
                self.PARAMETERS.append(VCFProperty.PARAMETERS(i))
        self.VALUE = VCFProperty.VALUE()
        self.GROUP = VCFProperty.GROUP()
        self.ORDER = VCFProperty.ORDER()
        self.OCTET_LENGTH = VCFProperty.OCTET_LENGTH()

    # VCFPropertyT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.PARAMETERS is not None:
            PARAMETERSlist = []
            for i in range(len(self.PARAMETERS)):
                PARAMETERSlist.append(builder.CreateString(self.PARAMETERS[i]))
            VCFPropertyStartPARAMETERSVector(builder, len(self.PARAMETERS))
            for i in reversed(range(len(self.PARAMETERS))):
                builder.PrependUOffsetTRelative(PARAMETERSlist[i])
            PARAMETERS = builder.EndVector()
        if self.VALUE is not None:
            VALUE = builder.CreateString(self.VALUE)
        if self.GROUP is not None:
            GROUP = builder.CreateString(self.GROUP)
        VCFPropertyStart(builder)
        if self.NAME is not None:
            VCFPropertyAddNAME(builder, NAME)
        if self.PARAMETERS is not None:
            VCFPropertyAddPARAMETERS(builder, PARAMETERS)
        if self.VALUE is not None:
            VCFPropertyAddVALUE(builder, VALUE)
        if self.GROUP is not None:
            VCFPropertyAddGROUP(builder, GROUP)
        VCFPropertyAddORDER(builder, self.ORDER)
        VCFPropertyAddOCTET_LENGTH(builder, self.OCTET_LENGTH)
        VCFProperty = VCFPropertyEnd(builder)
        return VCFProperty
