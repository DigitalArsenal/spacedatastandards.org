# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Plaintext carried, size-prefixed, inside RPC.CIPHERTEXT.
class RPCBody(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RPCBody()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRPCBody(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RPCBodyBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x50\x43", size_prefixed=size_prefixed)

    # RPCBody
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Request method verb (for example GET, POST). Empty on a response.
    # RPCBody
    def METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Request route, path plus optional query. Empty on a response.
    # RPCBody
    def ROUTE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Request or response payload bytes.
    # RPCBody
    def BODY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # RPCBody
    def BODYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # RPCBody
    def BODYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RPCBody
    def BODYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Four-character file identifier of BODY when BODY is a FlatBuffer,
    # `$` included (for example "$EPM"). Empty when BODY is not a FlatBuffer.
    # RPCBody
    def BODY_FILE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Response status code. Zero on a request.
    # RPCBody
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Identifier of the signer's key; MUST identify RPC.SIGNER_PUBLIC_KEY.
    # RPCBody
    def SIGNER_KEY_ID(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # RPCBody
    def SIGNER_KEY_IDAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # RPCBody
    def SIGNER_KEY_IDLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RPCBody
    def SIGNER_KEY_IDIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Request only: public key the response is sealed to.
    # RPCBody
    def REPLY_KEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # RPCBody
    def REPLY_KEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # RPCBody
    def REPLY_KEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RPCBody
    def REPLY_KEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Response only: RPC.NONCE of the request being answered.
    # RPCBody
    def REQUEST_NONCE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # RPCBody
    def REQUEST_NONCEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # RPCBody
    def REQUEST_NONCELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RPCBody
    def REQUEST_NONCEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Response only: SHA-256 (32 bytes) of the complete size-prefixed request
    # RPC FlatBuffer as received, signatures included.
    # RPCBody
    def REQUEST_DIGEST(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # RPCBody
    def REQUEST_DIGESTAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # RPCBody
    def REQUEST_DIGESTLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RPCBody
    def REQUEST_DIGESTIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

def RPCBodyStart(builder):
    builder.StartObject(9)

def Start(builder):
    RPCBodyStart(builder)

def RPCBodyAddMETHOD(builder, METHOD):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(METHOD), 0)

def AddMETHOD(builder, METHOD):
    RPCBodyAddMETHOD(builder, METHOD)

def RPCBodyAddROUTE(builder, ROUTE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ROUTE), 0)

def AddROUTE(builder, ROUTE):
    RPCBodyAddROUTE(builder, ROUTE)

def RPCBodyAddBODY(builder, BODY):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(BODY), 0)

def AddBODY(builder, BODY):
    RPCBodyAddBODY(builder, BODY)

def RPCBodyStartBODYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartBODYVector(builder, numElems):
    return RPCBodyStartBODYVector(builder, numElems)

def RPCBodyCreateBODYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateBODYVector(builder, data):
    RPCBodyCreateBODYVector(builder, data)

def RPCBodyAddBODY_FILE_ID(builder, BODY_FILE_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(BODY_FILE_ID), 0)

def AddBODY_FILE_ID(builder, BODY_FILE_ID):
    RPCBodyAddBODY_FILE_ID(builder, BODY_FILE_ID)

def RPCBodyAddSTATUS(builder, STATUS):
    builder.PrependUint16Slot(4, STATUS, 0)

def AddSTATUS(builder, STATUS):
    RPCBodyAddSTATUS(builder, STATUS)

def RPCBodyAddSIGNER_KEY_ID(builder, SIGNER_KEY_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNER_KEY_ID), 0)

def AddSIGNER_KEY_ID(builder, SIGNER_KEY_ID):
    RPCBodyAddSIGNER_KEY_ID(builder, SIGNER_KEY_ID)

def RPCBodyStartSIGNER_KEY_IDVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNER_KEY_IDVector(builder, numElems):
    return RPCBodyStartSIGNER_KEY_IDVector(builder, numElems)

def RPCBodyCreateSIGNER_KEY_IDVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNER_KEY_IDVector(builder, data):
    RPCBodyCreateSIGNER_KEY_IDVector(builder, data)

def RPCBodyAddREPLY_KEY(builder, REPLY_KEY):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(REPLY_KEY), 0)

def AddREPLY_KEY(builder, REPLY_KEY):
    RPCBodyAddREPLY_KEY(builder, REPLY_KEY)

def RPCBodyStartREPLY_KEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartREPLY_KEYVector(builder, numElems):
    return RPCBodyStartREPLY_KEYVector(builder, numElems)

def RPCBodyCreateREPLY_KEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateREPLY_KEYVector(builder, data):
    RPCBodyCreateREPLY_KEYVector(builder, data)

def RPCBodyAddREQUEST_NONCE(builder, REQUEST_NONCE):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_NONCE), 0)

def AddREQUEST_NONCE(builder, REQUEST_NONCE):
    RPCBodyAddREQUEST_NONCE(builder, REQUEST_NONCE)

def RPCBodyStartREQUEST_NONCEVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartREQUEST_NONCEVector(builder, numElems):
    return RPCBodyStartREQUEST_NONCEVector(builder, numElems)

def RPCBodyCreateREQUEST_NONCEVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateREQUEST_NONCEVector(builder, data):
    RPCBodyCreateREQUEST_NONCEVector(builder, data)

def RPCBodyAddREQUEST_DIGEST(builder, REQUEST_DIGEST):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_DIGEST), 0)

def AddREQUEST_DIGEST(builder, REQUEST_DIGEST):
    RPCBodyAddREQUEST_DIGEST(builder, REQUEST_DIGEST)

def RPCBodyStartREQUEST_DIGESTVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartREQUEST_DIGESTVector(builder, numElems):
    return RPCBodyStartREQUEST_DIGESTVector(builder, numElems)

def RPCBodyCreateREQUEST_DIGESTVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateREQUEST_DIGESTVector(builder, data):
    RPCBodyCreateREQUEST_DIGESTVector(builder, data)

def RPCBodyEnd(builder):
    return builder.EndObject()

def End(builder):
    return RPCBodyEnd(builder)

try:
    from typing import List
except:
    pass

class RPCBodyT(object):

    # RPCBodyT
    def __init__(
        self,
        METHOD = None,
        ROUTE = None,
        BODY = None,
        BODY_FILE_ID = None,
        STATUS = 0,
        SIGNER_KEY_ID = None,
        REPLY_KEY = None,
        REQUEST_NONCE = None,
        REQUEST_DIGEST = None,
    ):
        self.METHOD = METHOD  # type: Optional[str]
        self.ROUTE = ROUTE  # type: Optional[str]
        self.BODY = BODY  # type: Optional[List[int]]
        self.BODY_FILE_ID = BODY_FILE_ID  # type: Optional[str]
        self.STATUS = STATUS  # type: int
        self.SIGNER_KEY_ID = SIGNER_KEY_ID  # type: Optional[List[int]]
        self.REPLY_KEY = REPLY_KEY  # type: Optional[List[int]]
        self.REQUEST_NONCE = REQUEST_NONCE  # type: Optional[List[int]]
        self.REQUEST_DIGEST = REQUEST_DIGEST  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpRpcbody = RPCBody()
        tmpRpcbody.Init(buf, pos)
        return cls.InitFromObj(tmpRpcbody)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpRpcbody):
        x = RPCBodyT()
        x._UnPack(tmpRpcbody)
        return x

    # RPCBodyT
    def _UnPack(self, RPCBody):
        if RPCBody is None:
            return
        self.METHOD = RPCBody.METHOD()
        self.ROUTE = RPCBody.ROUTE()
        if not RPCBody.BODYIsNone():
            if np is None:
                self.BODY = []
                for i in range(RPCBody.BODYLength()):
                    self.BODY.append(RPCBody.BODY(i))
            else:
                self.BODY = RPCBody.BODYAsNumpy()
        self.BODY_FILE_ID = RPCBody.BODY_FILE_ID()
        self.STATUS = RPCBody.STATUS()
        if not RPCBody.SIGNER_KEY_IDIsNone():
            if np is None:
                self.SIGNER_KEY_ID = []
                for i in range(RPCBody.SIGNER_KEY_IDLength()):
                    self.SIGNER_KEY_ID.append(RPCBody.SIGNER_KEY_ID(i))
            else:
                self.SIGNER_KEY_ID = RPCBody.SIGNER_KEY_IDAsNumpy()
        if not RPCBody.REPLY_KEYIsNone():
            if np is None:
                self.REPLY_KEY = []
                for i in range(RPCBody.REPLY_KEYLength()):
                    self.REPLY_KEY.append(RPCBody.REPLY_KEY(i))
            else:
                self.REPLY_KEY = RPCBody.REPLY_KEYAsNumpy()
        if not RPCBody.REQUEST_NONCEIsNone():
            if np is None:
                self.REQUEST_NONCE = []
                for i in range(RPCBody.REQUEST_NONCELength()):
                    self.REQUEST_NONCE.append(RPCBody.REQUEST_NONCE(i))
            else:
                self.REQUEST_NONCE = RPCBody.REQUEST_NONCEAsNumpy()
        if not RPCBody.REQUEST_DIGESTIsNone():
            if np is None:
                self.REQUEST_DIGEST = []
                for i in range(RPCBody.REQUEST_DIGESTLength()):
                    self.REQUEST_DIGEST.append(RPCBody.REQUEST_DIGEST(i))
            else:
                self.REQUEST_DIGEST = RPCBody.REQUEST_DIGESTAsNumpy()

    # RPCBodyT
    def Pack(self, builder):
        if self.METHOD is not None:
            METHOD = builder.CreateString(self.METHOD)
        if self.ROUTE is not None:
            ROUTE = builder.CreateString(self.ROUTE)
        if self.BODY is not None:
            if np is not None and type(self.BODY) is np.ndarray:
                BODY = builder.CreateNumpyVector(self.BODY)
            else:
                RPCBodyStartBODYVector(builder, len(self.BODY))
                for i in reversed(range(len(self.BODY))):
                    builder.PrependUint8(self.BODY[i])
                BODY = builder.EndVector()
        if self.BODY_FILE_ID is not None:
            BODY_FILE_ID = builder.CreateString(self.BODY_FILE_ID)
        if self.SIGNER_KEY_ID is not None:
            if np is not None and type(self.SIGNER_KEY_ID) is np.ndarray:
                SIGNER_KEY_ID = builder.CreateNumpyVector(self.SIGNER_KEY_ID)
            else:
                RPCBodyStartSIGNER_KEY_IDVector(builder, len(self.SIGNER_KEY_ID))
                for i in reversed(range(len(self.SIGNER_KEY_ID))):
                    builder.PrependUint8(self.SIGNER_KEY_ID[i])
                SIGNER_KEY_ID = builder.EndVector()
        if self.REPLY_KEY is not None:
            if np is not None and type(self.REPLY_KEY) is np.ndarray:
                REPLY_KEY = builder.CreateNumpyVector(self.REPLY_KEY)
            else:
                RPCBodyStartREPLY_KEYVector(builder, len(self.REPLY_KEY))
                for i in reversed(range(len(self.REPLY_KEY))):
                    builder.PrependUint8(self.REPLY_KEY[i])
                REPLY_KEY = builder.EndVector()
        if self.REQUEST_NONCE is not None:
            if np is not None and type(self.REQUEST_NONCE) is np.ndarray:
                REQUEST_NONCE = builder.CreateNumpyVector(self.REQUEST_NONCE)
            else:
                RPCBodyStartREQUEST_NONCEVector(builder, len(self.REQUEST_NONCE))
                for i in reversed(range(len(self.REQUEST_NONCE))):
                    builder.PrependUint8(self.REQUEST_NONCE[i])
                REQUEST_NONCE = builder.EndVector()
        if self.REQUEST_DIGEST is not None:
            if np is not None and type(self.REQUEST_DIGEST) is np.ndarray:
                REQUEST_DIGEST = builder.CreateNumpyVector(self.REQUEST_DIGEST)
            else:
                RPCBodyStartREQUEST_DIGESTVector(builder, len(self.REQUEST_DIGEST))
                for i in reversed(range(len(self.REQUEST_DIGEST))):
                    builder.PrependUint8(self.REQUEST_DIGEST[i])
                REQUEST_DIGEST = builder.EndVector()
        RPCBodyStart(builder)
        if self.METHOD is not None:
            RPCBodyAddMETHOD(builder, METHOD)
        if self.ROUTE is not None:
            RPCBodyAddROUTE(builder, ROUTE)
        if self.BODY is not None:
            RPCBodyAddBODY(builder, BODY)
        if self.BODY_FILE_ID is not None:
            RPCBodyAddBODY_FILE_ID(builder, BODY_FILE_ID)
        RPCBodyAddSTATUS(builder, self.STATUS)
        if self.SIGNER_KEY_ID is not None:
            RPCBodyAddSIGNER_KEY_ID(builder, SIGNER_KEY_ID)
        if self.REPLY_KEY is not None:
            RPCBodyAddREPLY_KEY(builder, REPLY_KEY)
        if self.REQUEST_NONCE is not None:
            RPCBodyAddREQUEST_NONCE(builder, REQUEST_NONCE)
        if self.REQUEST_DIGEST is not None:
            RPCBodyAddREQUEST_DIGEST(builder, REQUEST_DIGEST)
        RPCBody = RPCBodyEnd(builder)
        return RPCBody
