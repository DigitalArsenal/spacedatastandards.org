# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Licensing proof message
class LPF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LPF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLPF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def LPFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4C\x50\x46", size_prefixed=size_prefixed)

    # LPF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Message type
    # LPF
    def MESSAGE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Unique request identifier
    # LPF
    def REQUEST_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Canonical module identifier
    # LPF
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Optional requested module version
    # LPF
    def MODULE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requester peer identifier
    # LPF
    def REQUESTER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requester account identity or wallet xpub
    # LPF
    def REQUESTER_XPUB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requested domain proved for policy evaluation
    # LPF
    def REQUESTED_DOMAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requested timeout in milliseconds
    # LPF
    def REQUESTED_TIMEOUT_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Requester ephemeral X25519 public key
    # LPF
    def REQUESTER_EPHEMERAL_PUBKEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LPF
    def REQUESTER_EPHEMERAL_PUBKEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LPF
    def REQUESTER_EPHEMERAL_PUBKEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LPF
    def REQUESTER_EPHEMERAL_PUBKEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # Provider-issued challenge nonce being proved
    # LPF
    def CHALLENGE_NONCE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LPF
    def CHALLENGE_NONCEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LPF
    def CHALLENGE_NONCELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LPF
    def CHALLENGE_NONCEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # Challenge expiration time being acknowledged
    # LPF
    def CHALLENGE_EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Provider peer identifier that issued the challenge
    # LPF
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Detached requester signature over the challenge payload
    # LPF
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LPF
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LPF
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LPF
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

    # Requester signing public key
    # LPF
    def SIGNING_PUBKEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LPF
    def SIGNING_PUBKEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LPF
    def SIGNING_PUBKEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LPF
    def SIGNING_PUBKEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        return o == 0

    # Proof timestamp in milliseconds since epoch
    # LPF
    def TIMESTAMP_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Structured rejection code
    # LPF
    def REJECTION_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Structured rejection message
    # LPF
    def REJECTION_MESSAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def LPFStart(builder):
    builder.StartObject(17)

def Start(builder):
    LPFStart(builder)

def LPFAddMESSAGE_TYPE(builder, MESSAGE_TYPE):
    builder.PrependInt8Slot(0, MESSAGE_TYPE, 0)

def AddMESSAGE_TYPE(builder, MESSAGE_TYPE):
    LPFAddMESSAGE_TYPE(builder, MESSAGE_TYPE)

def LPFAddREQUEST_ID(builder, REQUEST_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_ID), 0)

def AddREQUEST_ID(builder, REQUEST_ID):
    LPFAddREQUEST_ID(builder, REQUEST_ID)

def LPFAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    LPFAddMODULE_ID(builder, MODULE_ID)

def LPFAddMODULE_VERSION(builder, MODULE_VERSION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_VERSION), 0)

def AddMODULE_VERSION(builder, MODULE_VERSION):
    LPFAddMODULE_VERSION(builder, MODULE_VERSION)

def LPFAddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTER_PEER_ID), 0)

def AddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID):
    LPFAddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID)

def LPFAddREQUESTER_XPUB(builder, REQUESTER_XPUB):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTER_XPUB), 0)

def AddREQUESTER_XPUB(builder, REQUESTER_XPUB):
    LPFAddREQUESTER_XPUB(builder, REQUESTER_XPUB)

def LPFAddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTED_DOMAIN), 0)

def AddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN):
    LPFAddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN)

def LPFAddREQUESTED_TIMEOUT_MS(builder, REQUESTED_TIMEOUT_MS):
    builder.PrependUint64Slot(7, REQUESTED_TIMEOUT_MS, 0)

def AddREQUESTED_TIMEOUT_MS(builder, REQUESTED_TIMEOUT_MS):
    LPFAddREQUESTED_TIMEOUT_MS(builder, REQUESTED_TIMEOUT_MS)

def LPFAddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTER_EPHEMERAL_PUBKEY), 0)

def AddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY):
    LPFAddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY)

def LPFStartREQUESTER_EPHEMERAL_PUBKEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartREQUESTER_EPHEMERAL_PUBKEYVector(builder, numElems):
    return LPFStartREQUESTER_EPHEMERAL_PUBKEYVector(builder, numElems)

def LPFCreateREQUESTER_EPHEMERAL_PUBKEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateREQUESTER_EPHEMERAL_PUBKEYVector(builder, data):
    LPFCreateREQUESTER_EPHEMERAL_PUBKEYVector(builder, data)

def LPFAddCHALLENGE_NONCE(builder, CHALLENGE_NONCE):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(CHALLENGE_NONCE), 0)

def AddCHALLENGE_NONCE(builder, CHALLENGE_NONCE):
    LPFAddCHALLENGE_NONCE(builder, CHALLENGE_NONCE)

def LPFStartCHALLENGE_NONCEVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCHALLENGE_NONCEVector(builder, numElems):
    return LPFStartCHALLENGE_NONCEVector(builder, numElems)

def LPFCreateCHALLENGE_NONCEVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCHALLENGE_NONCEVector(builder, data):
    LPFCreateCHALLENGE_NONCEVector(builder, data)

def LPFAddCHALLENGE_EXPIRES_AT(builder, CHALLENGE_EXPIRES_AT):
    builder.PrependUint64Slot(10, CHALLENGE_EXPIRES_AT, 0)

def AddCHALLENGE_EXPIRES_AT(builder, CHALLENGE_EXPIRES_AT):
    LPFAddCHALLENGE_EXPIRES_AT(builder, CHALLENGE_EXPIRES_AT)

def LPFAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    LPFAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def LPFAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    LPFAddSIGNATURE(builder, SIGNATURE)

def LPFStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return LPFStartSIGNATUREVector(builder, numElems)

def LPFCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    LPFCreateSIGNATUREVector(builder, data)

def LPFAddSIGNING_PUBKEY(builder, SIGNING_PUBKEY):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNING_PUBKEY), 0)

def AddSIGNING_PUBKEY(builder, SIGNING_PUBKEY):
    LPFAddSIGNING_PUBKEY(builder, SIGNING_PUBKEY)

def LPFStartSIGNING_PUBKEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNING_PUBKEYVector(builder, numElems):
    return LPFStartSIGNING_PUBKEYVector(builder, numElems)

def LPFCreateSIGNING_PUBKEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNING_PUBKEYVector(builder, data):
    LPFCreateSIGNING_PUBKEYVector(builder, data)

def LPFAddTIMESTAMP_MS(builder, TIMESTAMP_MS):
    builder.PrependUint64Slot(14, TIMESTAMP_MS, 0)

def AddTIMESTAMP_MS(builder, TIMESTAMP_MS):
    LPFAddTIMESTAMP_MS(builder, TIMESTAMP_MS)

def LPFAddREJECTION_CODE(builder, REJECTION_CODE):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(REJECTION_CODE), 0)

def AddREJECTION_CODE(builder, REJECTION_CODE):
    LPFAddREJECTION_CODE(builder, REJECTION_CODE)

def LPFAddREJECTION_MESSAGE(builder, REJECTION_MESSAGE):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(REJECTION_MESSAGE), 0)

def AddREJECTION_MESSAGE(builder, REJECTION_MESSAGE):
    LPFAddREJECTION_MESSAGE(builder, REJECTION_MESSAGE)

def LPFEnd(builder):
    return builder.EndObject()

def End(builder):
    return LPFEnd(builder)

try:
    from typing import List
except:
    pass

class LPFT(object):

    # LPFT
    def __init__(
        self,
        MESSAGE_TYPE = 0,
        REQUEST_ID = None,
        MODULE_ID = None,
        MODULE_VERSION = None,
        REQUESTER_PEER_ID = None,
        REQUESTER_XPUB = None,
        REQUESTED_DOMAIN = None,
        REQUESTED_TIMEOUT_MS = 0,
        REQUESTER_EPHEMERAL_PUBKEY = None,
        CHALLENGE_NONCE = None,
        CHALLENGE_EXPIRES_AT = 0,
        PROVIDER_PEER_ID = None,
        SIGNATURE = None,
        SIGNING_PUBKEY = None,
        TIMESTAMP_MS = 0,
        REJECTION_CODE = None,
        REJECTION_MESSAGE = None,
    ):
        self.MESSAGE_TYPE = MESSAGE_TYPE  # type: int
        self.REQUEST_ID = REQUEST_ID  # type: Optional[str]
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.MODULE_VERSION = MODULE_VERSION  # type: Optional[str]
        self.REQUESTER_PEER_ID = REQUESTER_PEER_ID  # type: Optional[str]
        self.REQUESTER_XPUB = REQUESTER_XPUB  # type: Optional[str]
        self.REQUESTED_DOMAIN = REQUESTED_DOMAIN  # type: Optional[str]
        self.REQUESTED_TIMEOUT_MS = REQUESTED_TIMEOUT_MS  # type: int
        self.REQUESTER_EPHEMERAL_PUBKEY = REQUESTER_EPHEMERAL_PUBKEY  # type: Optional[List[int]]
        self.CHALLENGE_NONCE = CHALLENGE_NONCE  # type: Optional[List[int]]
        self.CHALLENGE_EXPIRES_AT = CHALLENGE_EXPIRES_AT  # type: int
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]
        self.SIGNING_PUBKEY = SIGNING_PUBKEY  # type: Optional[List[int]]
        self.TIMESTAMP_MS = TIMESTAMP_MS  # type: int
        self.REJECTION_CODE = REJECTION_CODE  # type: Optional[str]
        self.REJECTION_MESSAGE = REJECTION_MESSAGE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpLpf = LPF()
        tmpLpf.Init(buf, pos)
        return cls.InitFromObj(tmpLpf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpLpf):
        x = LPFT()
        x._UnPack(tmpLpf)
        return x

    # LPFT
    def _UnPack(self, LPF):
        if LPF is None:
            return
        self.MESSAGE_TYPE = LPF.MESSAGE_TYPE()
        self.REQUEST_ID = LPF.REQUEST_ID()
        self.MODULE_ID = LPF.MODULE_ID()
        self.MODULE_VERSION = LPF.MODULE_VERSION()
        self.REQUESTER_PEER_ID = LPF.REQUESTER_PEER_ID()
        self.REQUESTER_XPUB = LPF.REQUESTER_XPUB()
        self.REQUESTED_DOMAIN = LPF.REQUESTED_DOMAIN()
        self.REQUESTED_TIMEOUT_MS = LPF.REQUESTED_TIMEOUT_MS()
        if not LPF.REQUESTER_EPHEMERAL_PUBKEYIsNone():
            if np is None:
                self.REQUESTER_EPHEMERAL_PUBKEY = []
                for i in range(LPF.REQUESTER_EPHEMERAL_PUBKEYLength()):
                    self.REQUESTER_EPHEMERAL_PUBKEY.append(LPF.REQUESTER_EPHEMERAL_PUBKEY(i))
            else:
                self.REQUESTER_EPHEMERAL_PUBKEY = LPF.REQUESTER_EPHEMERAL_PUBKEYAsNumpy()
        if not LPF.CHALLENGE_NONCEIsNone():
            if np is None:
                self.CHALLENGE_NONCE = []
                for i in range(LPF.CHALLENGE_NONCELength()):
                    self.CHALLENGE_NONCE.append(LPF.CHALLENGE_NONCE(i))
            else:
                self.CHALLENGE_NONCE = LPF.CHALLENGE_NONCEAsNumpy()
        self.CHALLENGE_EXPIRES_AT = LPF.CHALLENGE_EXPIRES_AT()
        self.PROVIDER_PEER_ID = LPF.PROVIDER_PEER_ID()
        if not LPF.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(LPF.SIGNATURELength()):
                    self.SIGNATURE.append(LPF.SIGNATURE(i))
            else:
                self.SIGNATURE = LPF.SIGNATUREAsNumpy()
        if not LPF.SIGNING_PUBKEYIsNone():
            if np is None:
                self.SIGNING_PUBKEY = []
                for i in range(LPF.SIGNING_PUBKEYLength()):
                    self.SIGNING_PUBKEY.append(LPF.SIGNING_PUBKEY(i))
            else:
                self.SIGNING_PUBKEY = LPF.SIGNING_PUBKEYAsNumpy()
        self.TIMESTAMP_MS = LPF.TIMESTAMP_MS()
        self.REJECTION_CODE = LPF.REJECTION_CODE()
        self.REJECTION_MESSAGE = LPF.REJECTION_MESSAGE()

    # LPFT
    def Pack(self, builder):
        if self.REQUEST_ID is not None:
            REQUEST_ID = builder.CreateString(self.REQUEST_ID)
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.MODULE_VERSION is not None:
            MODULE_VERSION = builder.CreateString(self.MODULE_VERSION)
        if self.REQUESTER_PEER_ID is not None:
            REQUESTER_PEER_ID = builder.CreateString(self.REQUESTER_PEER_ID)
        if self.REQUESTER_XPUB is not None:
            REQUESTER_XPUB = builder.CreateString(self.REQUESTER_XPUB)
        if self.REQUESTED_DOMAIN is not None:
            REQUESTED_DOMAIN = builder.CreateString(self.REQUESTED_DOMAIN)
        if self.REQUESTER_EPHEMERAL_PUBKEY is not None:
            if np is not None and type(self.REQUESTER_EPHEMERAL_PUBKEY) is np.ndarray:
                REQUESTER_EPHEMERAL_PUBKEY = builder.CreateNumpyVector(self.REQUESTER_EPHEMERAL_PUBKEY)
            else:
                LPFStartREQUESTER_EPHEMERAL_PUBKEYVector(builder, len(self.REQUESTER_EPHEMERAL_PUBKEY))
                for i in reversed(range(len(self.REQUESTER_EPHEMERAL_PUBKEY))):
                    builder.PrependUint8(self.REQUESTER_EPHEMERAL_PUBKEY[i])
                REQUESTER_EPHEMERAL_PUBKEY = builder.EndVector()
        if self.CHALLENGE_NONCE is not None:
            if np is not None and type(self.CHALLENGE_NONCE) is np.ndarray:
                CHALLENGE_NONCE = builder.CreateNumpyVector(self.CHALLENGE_NONCE)
            else:
                LPFStartCHALLENGE_NONCEVector(builder, len(self.CHALLENGE_NONCE))
                for i in reversed(range(len(self.CHALLENGE_NONCE))):
                    builder.PrependUint8(self.CHALLENGE_NONCE[i])
                CHALLENGE_NONCE = builder.EndVector()
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                LPFStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        if self.SIGNING_PUBKEY is not None:
            if np is not None and type(self.SIGNING_PUBKEY) is np.ndarray:
                SIGNING_PUBKEY = builder.CreateNumpyVector(self.SIGNING_PUBKEY)
            else:
                LPFStartSIGNING_PUBKEYVector(builder, len(self.SIGNING_PUBKEY))
                for i in reversed(range(len(self.SIGNING_PUBKEY))):
                    builder.PrependUint8(self.SIGNING_PUBKEY[i])
                SIGNING_PUBKEY = builder.EndVector()
        if self.REJECTION_CODE is not None:
            REJECTION_CODE = builder.CreateString(self.REJECTION_CODE)
        if self.REJECTION_MESSAGE is not None:
            REJECTION_MESSAGE = builder.CreateString(self.REJECTION_MESSAGE)
        LPFStart(builder)
        LPFAddMESSAGE_TYPE(builder, self.MESSAGE_TYPE)
        if self.REQUEST_ID is not None:
            LPFAddREQUEST_ID(builder, REQUEST_ID)
        if self.MODULE_ID is not None:
            LPFAddMODULE_ID(builder, MODULE_ID)
        if self.MODULE_VERSION is not None:
            LPFAddMODULE_VERSION(builder, MODULE_VERSION)
        if self.REQUESTER_PEER_ID is not None:
            LPFAddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID)
        if self.REQUESTER_XPUB is not None:
            LPFAddREQUESTER_XPUB(builder, REQUESTER_XPUB)
        if self.REQUESTED_DOMAIN is not None:
            LPFAddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN)
        LPFAddREQUESTED_TIMEOUT_MS(builder, self.REQUESTED_TIMEOUT_MS)
        if self.REQUESTER_EPHEMERAL_PUBKEY is not None:
            LPFAddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY)
        if self.CHALLENGE_NONCE is not None:
            LPFAddCHALLENGE_NONCE(builder, CHALLENGE_NONCE)
        LPFAddCHALLENGE_EXPIRES_AT(builder, self.CHALLENGE_EXPIRES_AT)
        if self.PROVIDER_PEER_ID is not None:
            LPFAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.SIGNATURE is not None:
            LPFAddSIGNATURE(builder, SIGNATURE)
        if self.SIGNING_PUBKEY is not None:
            LPFAddSIGNING_PUBKEY(builder, SIGNING_PUBKEY)
        LPFAddTIMESTAMP_MS(builder, self.TIMESTAMP_MS)
        if self.REJECTION_CODE is not None:
            LPFAddREJECTION_CODE(builder, REJECTION_CODE)
        if self.REJECTION_MESSAGE is not None:
            LPFAddREJECTION_MESSAGE(builder, REJECTION_MESSAGE)
        LPF = LPFEnd(builder)
        return LPF
