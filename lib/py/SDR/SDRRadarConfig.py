# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Radar sensor configuration.
class SDRRadarConfig(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDRRadarConfig()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDRRadarConfig(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDRRadarConfigBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x52", size_prefixed=size_prefixed)

    # SDRRadarConfig
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Operating frequency (Hz).
    # SDRRadarConfig
    def FREQUENCY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Transmit power (W).
    # SDRRadarConfig
    def POWER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Antenna gain (dB).
    # SDRRadarConfig
    def GAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Receiver bandwidth (Hz).
    # SDRRadarConfig
    def BANDWIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Noise figure (dB).
    # SDRRadarConfig
    def NOISE_FIGURE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # System losses (dB).
    # SDRRadarConfig
    def SYSTEM_LOSS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Pulse repetition frequency (Hz).
    # SDRRadarConfig
    def PRF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Pulse width (seconds).
    # SDRRadarConfig
    def PULSE_WIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Detection threshold SNR (dB).
    # SDRRadarConfig
    def DETECTION_THRESHOLD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Minimum detection range (meters).
    # SDRRadarConfig
    def MIN_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum detection range (meters).
    # SDRRadarConfig
    def MAX_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SDRRadarConfigStart(builder):
    builder.StartObject(11)

def Start(builder):
    SDRRadarConfigStart(builder)

def SDRRadarConfigAddFREQUENCY(builder, FREQUENCY):
    builder.PrependFloat64Slot(0, FREQUENCY, 0.0)

def AddFREQUENCY(builder, FREQUENCY):
    SDRRadarConfigAddFREQUENCY(builder, FREQUENCY)

def SDRRadarConfigAddPOWER(builder, POWER):
    builder.PrependFloat64Slot(1, POWER, 0.0)

def AddPOWER(builder, POWER):
    SDRRadarConfigAddPOWER(builder, POWER)

def SDRRadarConfigAddGAIN(builder, GAIN):
    builder.PrependFloat64Slot(2, GAIN, 0.0)

def AddGAIN(builder, GAIN):
    SDRRadarConfigAddGAIN(builder, GAIN)

def SDRRadarConfigAddBANDWIDTH(builder, BANDWIDTH):
    builder.PrependFloat64Slot(3, BANDWIDTH, 0.0)

def AddBANDWIDTH(builder, BANDWIDTH):
    SDRRadarConfigAddBANDWIDTH(builder, BANDWIDTH)

def SDRRadarConfigAddNOISE_FIGURE(builder, NOISE_FIGURE):
    builder.PrependFloat64Slot(4, NOISE_FIGURE, 0.0)

def AddNOISE_FIGURE(builder, NOISE_FIGURE):
    SDRRadarConfigAddNOISE_FIGURE(builder, NOISE_FIGURE)

def SDRRadarConfigAddSYSTEM_LOSS(builder, SYSTEM_LOSS):
    builder.PrependFloat64Slot(5, SYSTEM_LOSS, 0.0)

def AddSYSTEM_LOSS(builder, SYSTEM_LOSS):
    SDRRadarConfigAddSYSTEM_LOSS(builder, SYSTEM_LOSS)

def SDRRadarConfigAddPRF(builder, PRF):
    builder.PrependFloat64Slot(6, PRF, 0.0)

def AddPRF(builder, PRF):
    SDRRadarConfigAddPRF(builder, PRF)

def SDRRadarConfigAddPULSE_WIDTH(builder, PULSE_WIDTH):
    builder.PrependFloat64Slot(7, PULSE_WIDTH, 0.0)

def AddPULSE_WIDTH(builder, PULSE_WIDTH):
    SDRRadarConfigAddPULSE_WIDTH(builder, PULSE_WIDTH)

def SDRRadarConfigAddDETECTION_THRESHOLD(builder, DETECTION_THRESHOLD):
    builder.PrependFloat64Slot(8, DETECTION_THRESHOLD, 0.0)

def AddDETECTION_THRESHOLD(builder, DETECTION_THRESHOLD):
    SDRRadarConfigAddDETECTION_THRESHOLD(builder, DETECTION_THRESHOLD)

def SDRRadarConfigAddMIN_RANGE(builder, MIN_RANGE):
    builder.PrependFloat64Slot(9, MIN_RANGE, 0.0)

def AddMIN_RANGE(builder, MIN_RANGE):
    SDRRadarConfigAddMIN_RANGE(builder, MIN_RANGE)

def SDRRadarConfigAddMAX_RANGE(builder, MAX_RANGE):
    builder.PrependFloat64Slot(10, MAX_RANGE, 0.0)

def AddMAX_RANGE(builder, MAX_RANGE):
    SDRRadarConfigAddMAX_RANGE(builder, MAX_RANGE)

def SDRRadarConfigEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDRRadarConfigEnd(builder)


class SDRRadarConfigT(object):

    # SDRRadarConfigT
    def __init__(
        self,
        FREQUENCY = 0.0,
        POWER = 0.0,
        GAIN = 0.0,
        BANDWIDTH = 0.0,
        NOISE_FIGURE = 0.0,
        SYSTEM_LOSS = 0.0,
        PRF = 0.0,
        PULSE_WIDTH = 0.0,
        DETECTION_THRESHOLD = 0.0,
        MIN_RANGE = 0.0,
        MAX_RANGE = 0.0,
    ):
        self.FREQUENCY = FREQUENCY  # type: float
        self.POWER = POWER  # type: float
        self.GAIN = GAIN  # type: float
        self.BANDWIDTH = BANDWIDTH  # type: float
        self.NOISE_FIGURE = NOISE_FIGURE  # type: float
        self.SYSTEM_LOSS = SYSTEM_LOSS  # type: float
        self.PRF = PRF  # type: float
        self.PULSE_WIDTH = PULSE_WIDTH  # type: float
        self.DETECTION_THRESHOLD = DETECTION_THRESHOLD  # type: float
        self.MIN_RANGE = MIN_RANGE  # type: float
        self.MAX_RANGE = MAX_RANGE  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdrradarConfig = SDRRadarConfig()
        tmpSdrradarConfig.Init(buf, pos)
        return cls.InitFromObj(tmpSdrradarConfig)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdrradarConfig):
        x = SDRRadarConfigT()
        x._UnPack(tmpSdrradarConfig)
        return x

    # SDRRadarConfigT
    def _UnPack(self, SDRRadarConfig):
        if SDRRadarConfig is None:
            return
        self.FREQUENCY = SDRRadarConfig.FREQUENCY()
        self.POWER = SDRRadarConfig.POWER()
        self.GAIN = SDRRadarConfig.GAIN()
        self.BANDWIDTH = SDRRadarConfig.BANDWIDTH()
        self.NOISE_FIGURE = SDRRadarConfig.NOISE_FIGURE()
        self.SYSTEM_LOSS = SDRRadarConfig.SYSTEM_LOSS()
        self.PRF = SDRRadarConfig.PRF()
        self.PULSE_WIDTH = SDRRadarConfig.PULSE_WIDTH()
        self.DETECTION_THRESHOLD = SDRRadarConfig.DETECTION_THRESHOLD()
        self.MIN_RANGE = SDRRadarConfig.MIN_RANGE()
        self.MAX_RANGE = SDRRadarConfig.MAX_RANGE()

    # SDRRadarConfigT
    def Pack(self, builder):
        SDRRadarConfigStart(builder)
        SDRRadarConfigAddFREQUENCY(builder, self.FREQUENCY)
        SDRRadarConfigAddPOWER(builder, self.POWER)
        SDRRadarConfigAddGAIN(builder, self.GAIN)
        SDRRadarConfigAddBANDWIDTH(builder, self.BANDWIDTH)
        SDRRadarConfigAddNOISE_FIGURE(builder, self.NOISE_FIGURE)
        SDRRadarConfigAddSYSTEM_LOSS(builder, self.SYSTEM_LOSS)
        SDRRadarConfigAddPRF(builder, self.PRF)
        SDRRadarConfigAddPULSE_WIDTH(builder, self.PULSE_WIDTH)
        SDRRadarConfigAddDETECTION_THRESHOLD(builder, self.DETECTION_THRESHOLD)
        SDRRadarConfigAddMIN_RANGE(builder, self.MIN_RANGE)
        SDRRadarConfigAddMAX_RANGE(builder, self.MAX_RANGE)
        SDRRadarConfig = SDRRadarConfigEnd(builder)
        return SDRRadarConfig
