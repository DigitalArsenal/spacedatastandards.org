# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Transponder Channel
class commsChannel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = commsChannel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAscommsChannel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def commsChannelBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4D\x53", size_prefixed=size_prefixed)

    # commsChannel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Channel identifier
    # commsChannel
    def CHANNEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Channel name
    # commsChannel
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Uplink frequency in MHz
    # commsChannel
    def UPLINK_FREQ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Downlink frequency in MHz
    # commsChannel
    def DOWNLINK_FREQ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Channel bandwidth in MHz
    # commsChannel
    def BANDWIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Modulation type
    # commsChannel
    def MODULATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Data rate in Mbps
    # commsChannel
    def DATA_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Encryption method
    # commsChannel
    def ENCRYPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Forward error correction coding rate (e.g., 0.5, 0.75)
    # commsChannel
    def FEC_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Channel power in dBW
    # commsChannel
    def POWER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def commsChannelStart(builder):
    builder.StartObject(10)

def Start(builder):
    commsChannelStart(builder)

def commsChannelAddCHANNEL_ID(builder, CHANNEL_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CHANNEL_ID), 0)

def AddCHANNEL_ID(builder, CHANNEL_ID):
    commsChannelAddCHANNEL_ID(builder, CHANNEL_ID)

def commsChannelAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    commsChannelAddNAME(builder, NAME)

def commsChannelAddUPLINK_FREQ(builder, UPLINK_FREQ):
    builder.PrependFloat64Slot(2, UPLINK_FREQ, 0.0)

def AddUPLINK_FREQ(builder, UPLINK_FREQ):
    commsChannelAddUPLINK_FREQ(builder, UPLINK_FREQ)

def commsChannelAddDOWNLINK_FREQ(builder, DOWNLINK_FREQ):
    builder.PrependFloat64Slot(3, DOWNLINK_FREQ, 0.0)

def AddDOWNLINK_FREQ(builder, DOWNLINK_FREQ):
    commsChannelAddDOWNLINK_FREQ(builder, DOWNLINK_FREQ)

def commsChannelAddBANDWIDTH(builder, BANDWIDTH):
    builder.PrependFloat64Slot(4, BANDWIDTH, 0.0)

def AddBANDWIDTH(builder, BANDWIDTH):
    commsChannelAddBANDWIDTH(builder, BANDWIDTH)

def commsChannelAddMODULATION(builder, MODULATION):
    builder.PrependInt8Slot(5, MODULATION, 0)

def AddMODULATION(builder, MODULATION):
    commsChannelAddMODULATION(builder, MODULATION)

def commsChannelAddDATA_RATE(builder, DATA_RATE):
    builder.PrependFloat64Slot(6, DATA_RATE, 0.0)

def AddDATA_RATE(builder, DATA_RATE):
    commsChannelAddDATA_RATE(builder, DATA_RATE)

def commsChannelAddENCRYPTION(builder, ENCRYPTION):
    builder.PrependInt8Slot(7, ENCRYPTION, 0)

def AddENCRYPTION(builder, ENCRYPTION):
    commsChannelAddENCRYPTION(builder, ENCRYPTION)

def commsChannelAddFEC_RATE(builder, FEC_RATE):
    builder.PrependFloat64Slot(8, FEC_RATE, 0.0)

def AddFEC_RATE(builder, FEC_RATE):
    commsChannelAddFEC_RATE(builder, FEC_RATE)

def commsChannelAddPOWER(builder, POWER):
    builder.PrependFloat64Slot(9, POWER, 0.0)

def AddPOWER(builder, POWER):
    commsChannelAddPOWER(builder, POWER)

def commsChannelEnd(builder):
    return builder.EndObject()

def End(builder):
    return commsChannelEnd(builder)


class commsChannelT(object):

    # commsChannelT
    def __init__(self):
        self.CHANNEL_ID = None  # type: str
        self.NAME = None  # type: str
        self.UPLINK_FREQ = 0.0  # type: float
        self.DOWNLINK_FREQ = 0.0  # type: float
        self.BANDWIDTH = 0.0  # type: float
        self.MODULATION = 0  # type: int
        self.DATA_RATE = 0.0  # type: float
        self.ENCRYPTION = 0  # type: int
        self.FEC_RATE = 0.0  # type: float
        self.POWER = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        commsChannel = commsChannel()
        commsChannel.Init(buf, pos)
        return cls.InitFromObj(commsChannel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, commsChannel):
        x = commsChannelT()
        x._UnPack(commsChannel)
        return x

    # commsChannelT
    def _UnPack(self, commsChannel):
        if commsChannel is None:
            return
        self.CHANNEL_ID = commsChannel.CHANNEL_ID()
        self.NAME = commsChannel.NAME()
        self.UPLINK_FREQ = commsChannel.UPLINK_FREQ()
        self.DOWNLINK_FREQ = commsChannel.DOWNLINK_FREQ()
        self.BANDWIDTH = commsChannel.BANDWIDTH()
        self.MODULATION = commsChannel.MODULATION()
        self.DATA_RATE = commsChannel.DATA_RATE()
        self.ENCRYPTION = commsChannel.ENCRYPTION()
        self.FEC_RATE = commsChannel.FEC_RATE()
        self.POWER = commsChannel.POWER()

    # commsChannelT
    def Pack(self, builder):
        if self.CHANNEL_ID is not None:
            CHANNEL_ID = builder.CreateString(self.CHANNEL_ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        commsChannelStart(builder)
        if self.CHANNEL_ID is not None:
            commsChannelAddCHANNEL_ID(builder, CHANNEL_ID)
        if self.NAME is not None:
            commsChannelAddNAME(builder, NAME)
        commsChannelAddUPLINK_FREQ(builder, self.UPLINK_FREQ)
        commsChannelAddDOWNLINK_FREQ(builder, self.DOWNLINK_FREQ)
        commsChannelAddBANDWIDTH(builder, self.BANDWIDTH)
        commsChannelAddMODULATION(builder, self.MODULATION)
        commsChannelAddDATA_RATE(builder, self.DATA_RATE)
        commsChannelAddENCRYPTION(builder, self.ENCRYPTION)
        commsChannelAddFEC_RATE(builder, self.FEC_RATE)
        commsChannelAddPOWER(builder, self.POWER)
        commsChannel = commsChannelEnd(builder)
        return commsChannel
