# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TDMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TDMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTDMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TDMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x44\x4D", size_prefixed=size_prefixed)

    # TDMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TDMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TDM import TDM
            obj = TDM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TDMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TDMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def TDMCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    TDMCOLLECTIONStart(builder)

def TDMCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    TDMCOLLECTIONAddRECORDS(builder, RECORDS)

def TDMCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return TDMCOLLECTIONStartRECORDSVector(builder, numElems)

def TDMCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return TDMCOLLECTIONEnd(builder)

import TDM
try:
    from typing import List
except:
    pass

class TDMCOLLECTIONT(object):

    # TDMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[TDM.TDMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        TDMCOLLECTION = TDMCOLLECTION()
        TDMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(TDMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, TDMCOLLECTION):
        x = TDMCOLLECTIONT()
        x._UnPack(TDMCOLLECTION)
        return x

    # TDMCOLLECTIONT
    def _UnPack(self, TDMCOLLECTION):
        if TDMCOLLECTION is None:
            return
        if not TDMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(TDMCOLLECTION.RECORDSLength()):
                if TDMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    tDM_ = TDM.TDMT.InitFromObj(TDMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(tDM_)

    # TDMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            TDMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        TDMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            TDMCOLLECTIONAddRECORDS(builder, RECORDS)
        TDMCOLLECTION = TDMCOLLECTIONEnd(builder)
        return TDMCOLLECTION
