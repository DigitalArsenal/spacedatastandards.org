# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Ionospheric Electron Density Profile
class ionoDensityProfile(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ionoDensityProfile()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsionoDensityProfile(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ionoDensityProfileBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x4F\x4E", size_prefixed=size_prefixed)

    # ionoDensityProfile
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Observation epoch (ISO 8601)
    # ionoDensityProfile
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Altitudes in km
    # ionoDensityProfile
    def ALTITUDES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ionoDensityProfile
    def ALTITUDESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # ionoDensityProfile
    def ALTITUDESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ionoDensityProfile
    def ALTITUDESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Electron densities at each altitude in electrons/m^3
    # ionoDensityProfile
    def DENSITIES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ionoDensityProfile
    def DENSITIESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # ionoDensityProfile
    def DENSITIESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ionoDensityProfile
    def DENSITIESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def ionoDensityProfileStart(builder):
    builder.StartObject(3)

def Start(builder):
    ionoDensityProfileStart(builder)

def ionoDensityProfileAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    ionoDensityProfileAddEPOCH(builder, EPOCH)

def ionoDensityProfileAddALTITUDES(builder, ALTITUDES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ALTITUDES), 0)

def AddALTITUDES(builder, ALTITUDES):
    ionoDensityProfileAddALTITUDES(builder, ALTITUDES)

def ionoDensityProfileStartALTITUDESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartALTITUDESVector(builder, numElems):
    return ionoDensityProfileStartALTITUDESVector(builder, numElems)

def ionoDensityProfileAddDENSITIES(builder, DENSITIES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DENSITIES), 0)

def AddDENSITIES(builder, DENSITIES):
    ionoDensityProfileAddDENSITIES(builder, DENSITIES)

def ionoDensityProfileStartDENSITIESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartDENSITIESVector(builder, numElems):
    return ionoDensityProfileStartDENSITIESVector(builder, numElems)

def ionoDensityProfileEnd(builder):
    return builder.EndObject()

def End(builder):
    return ionoDensityProfileEnd(builder)

try:
    from typing import List
except:
    pass

class ionoDensityProfileT(object):

    # ionoDensityProfileT
    def __init__(self):
        self.EPOCH = None  # type: str
        self.ALTITUDES = None  # type: List[float]
        self.DENSITIES = None  # type: List[float]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ionoDensityProfile = ionoDensityProfile()
        ionoDensityProfile.Init(buf, pos)
        return cls.InitFromObj(ionoDensityProfile)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ionoDensityProfile):
        x = ionoDensityProfileT()
        x._UnPack(ionoDensityProfile)
        return x

    # ionoDensityProfileT
    def _UnPack(self, ionoDensityProfile):
        if ionoDensityProfile is None:
            return
        self.EPOCH = ionoDensityProfile.EPOCH()
        if not ionoDensityProfile.ALTITUDESIsNone():
            if np is None:
                self.ALTITUDES = []
                for i in range(ionoDensityProfile.ALTITUDESLength()):
                    self.ALTITUDES.append(ionoDensityProfile.ALTITUDES(i))
            else:
                self.ALTITUDES = ionoDensityProfile.ALTITUDESAsNumpy()
        if not ionoDensityProfile.DENSITIESIsNone():
            if np is None:
                self.DENSITIES = []
                for i in range(ionoDensityProfile.DENSITIESLength()):
                    self.DENSITIES.append(ionoDensityProfile.DENSITIES(i))
            else:
                self.DENSITIES = ionoDensityProfile.DENSITIESAsNumpy()

    # ionoDensityProfileT
    def Pack(self, builder):
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        if self.ALTITUDES is not None:
            if np is not None and type(self.ALTITUDES) is np.ndarray:
                ALTITUDES = builder.CreateNumpyVector(self.ALTITUDES)
            else:
                ionoDensityProfileStartALTITUDESVector(builder, len(self.ALTITUDES))
                for i in reversed(range(len(self.ALTITUDES))):
                    builder.PrependFloat64(self.ALTITUDES[i])
                ALTITUDES = builder.EndVector()
        if self.DENSITIES is not None:
            if np is not None and type(self.DENSITIES) is np.ndarray:
                DENSITIES = builder.CreateNumpyVector(self.DENSITIES)
            else:
                ionoDensityProfileStartDENSITIESVector(builder, len(self.DENSITIES))
                for i in reversed(range(len(self.DENSITIES))):
                    builder.PrependFloat64(self.DENSITIES[i])
                DENSITIES = builder.EndVector()
        ionoDensityProfileStart(builder)
        if self.EPOCH is not None:
            ionoDensityProfileAddEPOCH(builder, EPOCH)
        if self.ALTITUDES is not None:
            ionoDensityProfileAddALTITUDES(builder, ALTITUDES)
        if self.DENSITIES is not None:
            ionoDensityProfileAddDENSITIES(builder, DENSITIES)
        ionoDensityProfile = ionoDensityProfileEnd(builder)
        return ionoDensityProfile
