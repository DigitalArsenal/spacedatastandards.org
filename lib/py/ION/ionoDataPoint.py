# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Ionospheric Observation Data Point
class ionoDataPoint(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ionoDataPoint()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsionoDataPoint(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ionoDataPointBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x4F\x4E", size_prefixed=size_prefixed)

    # ionoDataPoint
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Observation epoch (ISO 8601)
    # ionoDataPoint
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Ionospheric layer
    # ionoDataPoint
    def LAYER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Critical frequency in MHz
    # ionoDataPoint
    def CRITICAL_FREQ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Virtual height in km
    # ionoDataPoint
    def VIRTUAL_HEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # True height in km
    # ionoDataPoint
    def TRUE_HEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Peak electron density in electrons/m^3
    # ionoDataPoint
    def PEAK_DENSITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Total electron content in TECU
    # ionoDataPoint
    def TEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TEC uncertainty in TECU
    # ionoDataPoint
    def TEC_UNCERTAINTY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def ionoDataPointStart(builder):
    builder.StartObject(8)

def Start(builder):
    ionoDataPointStart(builder)

def ionoDataPointAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    ionoDataPointAddEPOCH(builder, EPOCH)

def ionoDataPointAddLAYER(builder, LAYER):
    builder.PrependInt8Slot(1, LAYER, 0)

def AddLAYER(builder, LAYER):
    ionoDataPointAddLAYER(builder, LAYER)

def ionoDataPointAddCRITICAL_FREQ(builder, CRITICAL_FREQ):
    builder.PrependFloat64Slot(2, CRITICAL_FREQ, 0.0)

def AddCRITICAL_FREQ(builder, CRITICAL_FREQ):
    ionoDataPointAddCRITICAL_FREQ(builder, CRITICAL_FREQ)

def ionoDataPointAddVIRTUAL_HEIGHT(builder, VIRTUAL_HEIGHT):
    builder.PrependFloat64Slot(3, VIRTUAL_HEIGHT, 0.0)

def AddVIRTUAL_HEIGHT(builder, VIRTUAL_HEIGHT):
    ionoDataPointAddVIRTUAL_HEIGHT(builder, VIRTUAL_HEIGHT)

def ionoDataPointAddTRUE_HEIGHT(builder, TRUE_HEIGHT):
    builder.PrependFloat64Slot(4, TRUE_HEIGHT, 0.0)

def AddTRUE_HEIGHT(builder, TRUE_HEIGHT):
    ionoDataPointAddTRUE_HEIGHT(builder, TRUE_HEIGHT)

def ionoDataPointAddPEAK_DENSITY(builder, PEAK_DENSITY):
    builder.PrependFloat64Slot(5, PEAK_DENSITY, 0.0)

def AddPEAK_DENSITY(builder, PEAK_DENSITY):
    ionoDataPointAddPEAK_DENSITY(builder, PEAK_DENSITY)

def ionoDataPointAddTEC(builder, TEC):
    builder.PrependFloat64Slot(6, TEC, 0.0)

def AddTEC(builder, TEC):
    ionoDataPointAddTEC(builder, TEC)

def ionoDataPointAddTEC_UNCERTAINTY(builder, TEC_UNCERTAINTY):
    builder.PrependFloat64Slot(7, TEC_UNCERTAINTY, 0.0)

def AddTEC_UNCERTAINTY(builder, TEC_UNCERTAINTY):
    ionoDataPointAddTEC_UNCERTAINTY(builder, TEC_UNCERTAINTY)

def ionoDataPointEnd(builder):
    return builder.EndObject()

def End(builder):
    return ionoDataPointEnd(builder)


class ionoDataPointT(object):

    # ionoDataPointT
    def __init__(self):
        self.EPOCH = None  # type: str
        self.LAYER = 0  # type: int
        self.CRITICAL_FREQ = 0.0  # type: float
        self.VIRTUAL_HEIGHT = 0.0  # type: float
        self.TRUE_HEIGHT = 0.0  # type: float
        self.PEAK_DENSITY = 0.0  # type: float
        self.TEC = 0.0  # type: float
        self.TEC_UNCERTAINTY = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ionoDataPoint = ionoDataPoint()
        ionoDataPoint.Init(buf, pos)
        return cls.InitFromObj(ionoDataPoint)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ionoDataPoint):
        x = ionoDataPointT()
        x._UnPack(ionoDataPoint)
        return x

    # ionoDataPointT
    def _UnPack(self, ionoDataPoint):
        if ionoDataPoint is None:
            return
        self.EPOCH = ionoDataPoint.EPOCH()
        self.LAYER = ionoDataPoint.LAYER()
        self.CRITICAL_FREQ = ionoDataPoint.CRITICAL_FREQ()
        self.VIRTUAL_HEIGHT = ionoDataPoint.VIRTUAL_HEIGHT()
        self.TRUE_HEIGHT = ionoDataPoint.TRUE_HEIGHT()
        self.PEAK_DENSITY = ionoDataPoint.PEAK_DENSITY()
        self.TEC = ionoDataPoint.TEC()
        self.TEC_UNCERTAINTY = ionoDataPoint.TEC_UNCERTAINTY()

    # ionoDataPointT
    def Pack(self, builder):
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        ionoDataPointStart(builder)
        if self.EPOCH is not None:
            ionoDataPointAddEPOCH(builder, EPOCH)
        ionoDataPointAddLAYER(builder, self.LAYER)
        ionoDataPointAddCRITICAL_FREQ(builder, self.CRITICAL_FREQ)
        ionoDataPointAddVIRTUAL_HEIGHT(builder, self.VIRTUAL_HEIGHT)
        ionoDataPointAddTRUE_HEIGHT(builder, self.TRUE_HEIGHT)
        ionoDataPointAddPEAK_DENSITY(builder, self.PEAK_DENSITY)
        ionoDataPointAddTEC(builder, self.TEC)
        ionoDataPointAddTEC_UNCERTAINTY(builder, self.TEC_UNCERTAINTY)
        ionoDataPoint = ionoDataPointEnd(builder)
        return ionoDataPoint
