# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TRHAttestation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRHAttestation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRHAttestation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRHAttestationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x48", size_prefixed=size_prefixed)

    # TRHAttestation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TRHAttestation
    def SIGNING_PUBLIC_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TRHAttestation
    def SIGNED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TRHAttestation
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRHAttestation
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TRHAttestation
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRHAttestation
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # TRHAttestation
    def CANONICAL_JSON_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRHAttestation
    def CANONICAL_JSON_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TRHAttestation
    def CANONICAL_JSON_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRHAttestation
    def CANONICAL_JSON_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def TRHAttestationStart(builder):
    builder.StartObject(4)

def Start(builder):
    TRHAttestationStart(builder)

def TRHAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNING_PUBLIC_KEY), 0)

def AddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    TRHAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)

def TRHAttestationAddSIGNED_AT(builder, SIGNED_AT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNED_AT), 0)

def AddSIGNED_AT(builder, SIGNED_AT):
    TRHAttestationAddSIGNED_AT(builder, SIGNED_AT)

def TRHAttestationAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    TRHAttestationAddSIGNATURE(builder, SIGNATURE)

def TRHAttestationStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return TRHAttestationStartSIGNATUREVector(builder, numElems)

def TRHAttestationCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    TRHAttestationCreateSIGNATUREVector(builder, data)

def TRHAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CANONICAL_JSON_SIGNATURE), 0)

def AddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE):
    TRHAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE)

def TRHAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCANONICAL_JSON_SIGNATUREVector(builder, numElems):
    return TRHAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, numElems)

def TRHAttestationCreateCANONICAL_JSON_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCANONICAL_JSON_SIGNATUREVector(builder, data):
    TRHAttestationCreateCANONICAL_JSON_SIGNATUREVector(builder, data)

def TRHAttestationEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRHAttestationEnd(builder)

try:
    from typing import List
except:
    pass

class TRHAttestationT(object):

    # TRHAttestationT
    def __init__(
        self,
        SIGNING_PUBLIC_KEY = None,
        SIGNED_AT = None,
        SIGNATURE = None,
        CANONICAL_JSON_SIGNATURE = None,
    ):
        self.SIGNING_PUBLIC_KEY = SIGNING_PUBLIC_KEY  # type: Optional[str]
        self.SIGNED_AT = SIGNED_AT  # type: Optional[str]
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]
        self.CANONICAL_JSON_SIGNATURE = CANONICAL_JSON_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrhattestation = TRHAttestation()
        tmpTrhattestation.Init(buf, pos)
        return cls.InitFromObj(tmpTrhattestation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrhattestation):
        x = TRHAttestationT()
        x._UnPack(tmpTrhattestation)
        return x

    # TRHAttestationT
    def _UnPack(self, TRHAttestation):
        if TRHAttestation is None:
            return
        self.SIGNING_PUBLIC_KEY = TRHAttestation.SIGNING_PUBLIC_KEY()
        self.SIGNED_AT = TRHAttestation.SIGNED_AT()
        if not TRHAttestation.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(TRHAttestation.SIGNATURELength()):
                    self.SIGNATURE.append(TRHAttestation.SIGNATURE(i))
            else:
                self.SIGNATURE = TRHAttestation.SIGNATUREAsNumpy()
        if not TRHAttestation.CANONICAL_JSON_SIGNATUREIsNone():
            if np is None:
                self.CANONICAL_JSON_SIGNATURE = []
                for i in range(TRHAttestation.CANONICAL_JSON_SIGNATURELength()):
                    self.CANONICAL_JSON_SIGNATURE.append(TRHAttestation.CANONICAL_JSON_SIGNATURE(i))
            else:
                self.CANONICAL_JSON_SIGNATURE = TRHAttestation.CANONICAL_JSON_SIGNATUREAsNumpy()

    # TRHAttestationT
    def Pack(self, builder):
        if self.SIGNING_PUBLIC_KEY is not None:
            SIGNING_PUBLIC_KEY = builder.CreateString(self.SIGNING_PUBLIC_KEY)
        if self.SIGNED_AT is not None:
            SIGNED_AT = builder.CreateString(self.SIGNED_AT)
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                TRHAttestationStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        if self.CANONICAL_JSON_SIGNATURE is not None:
            if np is not None and type(self.CANONICAL_JSON_SIGNATURE) is np.ndarray:
                CANONICAL_JSON_SIGNATURE = builder.CreateNumpyVector(self.CANONICAL_JSON_SIGNATURE)
            else:
                TRHAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, len(self.CANONICAL_JSON_SIGNATURE))
                for i in reversed(range(len(self.CANONICAL_JSON_SIGNATURE))):
                    builder.PrependUint8(self.CANONICAL_JSON_SIGNATURE[i])
                CANONICAL_JSON_SIGNATURE = builder.EndVector()
        TRHAttestationStart(builder)
        if self.SIGNING_PUBLIC_KEY is not None:
            TRHAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)
        if self.SIGNED_AT is not None:
            TRHAttestationAddSIGNED_AT(builder, SIGNED_AT)
        if self.SIGNATURE is not None:
            TRHAttestationAddSIGNATURE(builder, SIGNATURE)
        if self.CANONICAL_JSON_SIGNATURE is not None:
            TRHAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE)
        TRHAttestation = TRHAttestationEnd(builder)
        return TRHAttestation
