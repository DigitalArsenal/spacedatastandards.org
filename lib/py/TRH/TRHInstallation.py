# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TRHInstallation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRHInstallation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRHInstallation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRHInstallationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x48", size_prefixed=size_prefixed)

    # TRHInstallation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TRHInstallation
    def OWNER_OBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TRHInstallation
    def HARDWARE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TRHHardwareRef import TRHHardwareRef
            obj = TRHHardwareRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TRHInstallation
    def HARDWARELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRHInstallation
    def HARDWAREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # TRHInstallation
    def CLOCK_BIAS_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TRHInstallation
    def CLOCK_DRIFT_SECONDS_PER_SECOND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def TRHInstallationStart(builder):
    builder.StartObject(4)

def Start(builder):
    TRHInstallationStart(builder)

def TRHInstallationAddOWNER_OBJECT_ID(builder, OWNER_OBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(OWNER_OBJECT_ID), 0)

def AddOWNER_OBJECT_ID(builder, OWNER_OBJECT_ID):
    TRHInstallationAddOWNER_OBJECT_ID(builder, OWNER_OBJECT_ID)

def TRHInstallationAddHARDWARE(builder, HARDWARE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(HARDWARE), 0)

def AddHARDWARE(builder, HARDWARE):
    TRHInstallationAddHARDWARE(builder, HARDWARE)

def TRHInstallationStartHARDWAREVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartHARDWAREVector(builder, numElems):
    return TRHInstallationStartHARDWAREVector(builder, numElems)

def TRHInstallationCreateHARDWAREVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateHARDWAREVector(builder, data):
    TRHInstallationCreateHARDWAREVector(builder, data)

def TRHInstallationAddCLOCK_BIAS_SECONDS(builder, CLOCK_BIAS_SECONDS):
    builder.PrependFloat64Slot(2, CLOCK_BIAS_SECONDS, 0.0)

def AddCLOCK_BIAS_SECONDS(builder, CLOCK_BIAS_SECONDS):
    TRHInstallationAddCLOCK_BIAS_SECONDS(builder, CLOCK_BIAS_SECONDS)

def TRHInstallationAddCLOCK_DRIFT_SECONDS_PER_SECOND(builder, CLOCK_DRIFT_SECONDS_PER_SECOND):
    builder.PrependFloat64Slot(3, CLOCK_DRIFT_SECONDS_PER_SECOND, 0.0)

def AddCLOCK_DRIFT_SECONDS_PER_SECOND(builder, CLOCK_DRIFT_SECONDS_PER_SECOND):
    TRHInstallationAddCLOCK_DRIFT_SECONDS_PER_SECOND(builder, CLOCK_DRIFT_SECONDS_PER_SECOND)

def TRHInstallationEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRHInstallationEnd(builder)

import TRHHardwareRef
try:
    from typing import List
except:
    pass

class TRHInstallationT(object):

    # TRHInstallationT
    def __init__(
        self,
        OWNER_OBJECT_ID = None,
        HARDWARE = None,
        CLOCK_BIAS_SECONDS = 0.0,
        CLOCK_DRIFT_SECONDS_PER_SECOND = 0.0,
    ):
        self.OWNER_OBJECT_ID = OWNER_OBJECT_ID  # type: Optional[str]
        self.HARDWARE = HARDWARE  # type: Optional[List[TRHHardwareRef.TRHHardwareRefT]]
        self.CLOCK_BIAS_SECONDS = CLOCK_BIAS_SECONDS  # type: float
        self.CLOCK_DRIFT_SECONDS_PER_SECOND = CLOCK_DRIFT_SECONDS_PER_SECOND  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrhinstallation = TRHInstallation()
        tmpTrhinstallation.Init(buf, pos)
        return cls.InitFromObj(tmpTrhinstallation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrhinstallation):
        x = TRHInstallationT()
        x._UnPack(tmpTrhinstallation)
        return x

    # TRHInstallationT
    def _UnPack(self, TRHInstallation):
        if TRHInstallation is None:
            return
        self.OWNER_OBJECT_ID = TRHInstallation.OWNER_OBJECT_ID()
        if not TRHInstallation.HARDWAREIsNone():
            self.HARDWARE = []
            for i in range(TRHInstallation.HARDWARELength()):
                if TRHInstallation.HARDWARE(i) is None:
                    self.HARDWARE.append(None)
                else:
                    tRHHardwareRef_ = TRHHardwareRef.TRHHardwareRefT.InitFromObj(TRHInstallation.HARDWARE(i))
                    self.HARDWARE.append(tRHHardwareRef_)
        self.CLOCK_BIAS_SECONDS = TRHInstallation.CLOCK_BIAS_SECONDS()
        self.CLOCK_DRIFT_SECONDS_PER_SECOND = TRHInstallation.CLOCK_DRIFT_SECONDS_PER_SECOND()

    # TRHInstallationT
    def Pack(self, builder):
        if self.OWNER_OBJECT_ID is not None:
            OWNER_OBJECT_ID = builder.CreateString(self.OWNER_OBJECT_ID)
        if self.HARDWARE is not None:
            HARDWARElist = []
            for i in range(len(self.HARDWARE)):
                HARDWARElist.append(self.HARDWARE[i].Pack(builder))
            TRHInstallationStartHARDWAREVector(builder, len(self.HARDWARE))
            for i in reversed(range(len(self.HARDWARE))):
                builder.PrependUOffsetTRelative(HARDWARElist[i])
            HARDWARE = builder.EndVector()
        TRHInstallationStart(builder)
        if self.OWNER_OBJECT_ID is not None:
            TRHInstallationAddOWNER_OBJECT_ID(builder, OWNER_OBJECT_ID)
        if self.HARDWARE is not None:
            TRHInstallationAddHARDWARE(builder, HARDWARE)
        TRHInstallationAddCLOCK_BIAS_SECONDS(builder, self.CLOCK_BIAS_SECONDS)
        TRHInstallationAddCLOCK_DRIFT_SECONDS_PER_SECOND(builder, self.CLOCK_DRIFT_SECONDS_PER_SECOND)
        TRHInstallation = TRHInstallationEnd(builder)
        return TRHInstallation
