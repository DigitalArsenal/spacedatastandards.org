# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TRHTransmitter(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRHTransmitter()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRHTransmitter(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRHTransmitterBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x48", size_prefixed=size_prefixed)

    # TRHTransmitter
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TRHTransmitter
    def HARDWARE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TRHTransmitter
    def FREQUENCY_HZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TRHTransmitter
    def POWER_WATTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TRHTransmitter
    def MODULATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TRHTransmitter
    def HARDWARE_DELAY_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TRHTransmitter
    def ANTENNA_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TRHTransmitterStart(builder):
    builder.StartObject(6)

def Start(builder):
    TRHTransmitterStart(builder)

def TRHTransmitterAddHARDWARE_ID(builder, HARDWARE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(HARDWARE_ID), 0)

def AddHARDWARE_ID(builder, HARDWARE_ID):
    TRHTransmitterAddHARDWARE_ID(builder, HARDWARE_ID)

def TRHTransmitterAddFREQUENCY_HZ(builder, FREQUENCY_HZ):
    builder.PrependFloat64Slot(1, FREQUENCY_HZ, 0.0)

def AddFREQUENCY_HZ(builder, FREQUENCY_HZ):
    TRHTransmitterAddFREQUENCY_HZ(builder, FREQUENCY_HZ)

def TRHTransmitterAddPOWER_WATTS(builder, POWER_WATTS):
    builder.PrependFloat64Slot(2, POWER_WATTS, 0.0)

def AddPOWER_WATTS(builder, POWER_WATTS):
    TRHTransmitterAddPOWER_WATTS(builder, POWER_WATTS)

def TRHTransmitterAddMODULATION(builder, MODULATION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MODULATION), 0)

def AddMODULATION(builder, MODULATION):
    TRHTransmitterAddMODULATION(builder, MODULATION)

def TRHTransmitterAddHARDWARE_DELAY_SECONDS(builder, HARDWARE_DELAY_SECONDS):
    builder.PrependFloat64Slot(4, HARDWARE_DELAY_SECONDS, 0.0)

def AddHARDWARE_DELAY_SECONDS(builder, HARDWARE_DELAY_SECONDS):
    TRHTransmitterAddHARDWARE_DELAY_SECONDS(builder, HARDWARE_DELAY_SECONDS)

def TRHTransmitterAddANTENNA_ID(builder, ANTENNA_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ANTENNA_ID), 0)

def AddANTENNA_ID(builder, ANTENNA_ID):
    TRHTransmitterAddANTENNA_ID(builder, ANTENNA_ID)

def TRHTransmitterEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRHTransmitterEnd(builder)


class TRHTransmitterT(object):

    # TRHTransmitterT
    def __init__(
        self,
        HARDWARE_ID = None,
        FREQUENCY_HZ = 0.0,
        POWER_WATTS = 0.0,
        MODULATION = None,
        HARDWARE_DELAY_SECONDS = 0.0,
        ANTENNA_ID = None,
    ):
        self.HARDWARE_ID = HARDWARE_ID  # type: Optional[str]
        self.FREQUENCY_HZ = FREQUENCY_HZ  # type: float
        self.POWER_WATTS = POWER_WATTS  # type: float
        self.MODULATION = MODULATION  # type: Optional[str]
        self.HARDWARE_DELAY_SECONDS = HARDWARE_DELAY_SECONDS  # type: float
        self.ANTENNA_ID = ANTENNA_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrhtransmitter = TRHTransmitter()
        tmpTrhtransmitter.Init(buf, pos)
        return cls.InitFromObj(tmpTrhtransmitter)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrhtransmitter):
        x = TRHTransmitterT()
        x._UnPack(tmpTrhtransmitter)
        return x

    # TRHTransmitterT
    def _UnPack(self, TRHTransmitter):
        if TRHTransmitter is None:
            return
        self.HARDWARE_ID = TRHTransmitter.HARDWARE_ID()
        self.FREQUENCY_HZ = TRHTransmitter.FREQUENCY_HZ()
        self.POWER_WATTS = TRHTransmitter.POWER_WATTS()
        self.MODULATION = TRHTransmitter.MODULATION()
        self.HARDWARE_DELAY_SECONDS = TRHTransmitter.HARDWARE_DELAY_SECONDS()
        self.ANTENNA_ID = TRHTransmitter.ANTENNA_ID()

    # TRHTransmitterT
    def Pack(self, builder):
        if self.HARDWARE_ID is not None:
            HARDWARE_ID = builder.CreateString(self.HARDWARE_ID)
        if self.MODULATION is not None:
            MODULATION = builder.CreateString(self.MODULATION)
        if self.ANTENNA_ID is not None:
            ANTENNA_ID = builder.CreateString(self.ANTENNA_ID)
        TRHTransmitterStart(builder)
        if self.HARDWARE_ID is not None:
            TRHTransmitterAddHARDWARE_ID(builder, HARDWARE_ID)
        TRHTransmitterAddFREQUENCY_HZ(builder, self.FREQUENCY_HZ)
        TRHTransmitterAddPOWER_WATTS(builder, self.POWER_WATTS)
        if self.MODULATION is not None:
            TRHTransmitterAddMODULATION(builder, MODULATION)
        TRHTransmitterAddHARDWARE_DELAY_SECONDS(builder, self.HARDWARE_DELAY_SECONDS)
        if self.ANTENNA_ID is not None:
            TRHTransmitterAddANTENNA_ID(builder, ANTENNA_ID)
        TRHTransmitter = TRHTransmitterEnd(builder)
        return TRHTransmitter
