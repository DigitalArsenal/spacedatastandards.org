# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class EOPCollection(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EOPCollection()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEOPCollection(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def EOPCollectionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x4F\x50", size_prefixed=size_prefixed)

    # EOPCollection
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EOPCollection
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from EOP import EOP
            obj = EOP()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # EOPCollection
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EOPCollection
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def EOPCollectionStart(builder):
    builder.StartObject(1)

def Start(builder):
    EOPCollectionStart(builder)

def EOPCollectionAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    EOPCollectionAddRECORDS(builder, RECORDS)

def EOPCollectionStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return EOPCollectionStartRECORDSVector(builder, numElems)

def EOPCollectionEnd(builder):
    return builder.EndObject()

def End(builder):
    return EOPCollectionEnd(builder)

import EOP
try:
    from typing import List
except:
    pass

class EOPCollectionT(object):

    # EOPCollectionT
    def __init__(self):
        self.RECORDS = None  # type: List[EOP.EOPT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        eopcollection = EOPCollection()
        eopcollection.Init(buf, pos)
        return cls.InitFromObj(eopcollection)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, eopcollection):
        x = EOPCollectionT()
        x._UnPack(eopcollection)
        return x

    # EOPCollectionT
    def _UnPack(self, eopcollection):
        if eopcollection is None:
            return
        if not eopcollection.RECORDSIsNone():
            self.RECORDS = []
            for i in range(eopcollection.RECORDSLength()):
                if eopcollection.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    eOP_ = EOP.EOPT.InitFromObj(eopcollection.RECORDS(i))
                    self.RECORDS.append(eOP_)

    # EOPCollectionT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            EOPCollectionStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        EOPCollectionStart(builder)
        if self.RECORDS is not None:
            EOPCollectionAddRECORDS(builder, RECORDS)
        eopcollection = EOPCollectionEnd(builder)
        return eopcollection
