# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Canonicalization rule applied before hashing or signature verification.
class CanonicalizationRule(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CanonicalizationRule()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCanonicalizationRule(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CanonicalizationRuleBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4D\x42\x4C", size_prefixed=size_prefixed)

    # CanonicalizationRule
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Schema version for the canonicalization contract.
    # CanonicalizationRule
    def version(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 1

    # Strip any custom section whose name starts with this prefix before
    # hashing the module for signature verification.
    # CanonicalizationRule
    def stripped_custom_section_prefix(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Logical container name used for the module bundle record within the
    # delivery artifact.
    # CanonicalizationRule
    def bundle_section_name(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Hash function identifier, for example `sha256`.
    # CanonicalizationRule
    def hash_algorithm(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def CanonicalizationRuleStart(builder):
    builder.StartObject(4)

def Start(builder):
    CanonicalizationRuleStart(builder)

def CanonicalizationRuleAddversion(builder, version):
    builder.PrependUint16Slot(0, version, 1)

def Addversion(builder, version):
    CanonicalizationRuleAddversion(builder, version)

def CanonicalizationRuleAddstripped_custom_section_prefix(builder, stripped_custom_section_prefix):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(stripped_custom_section_prefix), 0)

def Addstripped_custom_section_prefix(builder, stripped_custom_section_prefix):
    CanonicalizationRuleAddstripped_custom_section_prefix(builder, stripped_custom_section_prefix)

def CanonicalizationRuleAddbundle_section_name(builder, bundle_section_name):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(bundle_section_name), 0)

def Addbundle_section_name(builder, bundle_section_name):
    CanonicalizationRuleAddbundle_section_name(builder, bundle_section_name)

def CanonicalizationRuleAddhash_algorithm(builder, hash_algorithm):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(hash_algorithm), 0)

def Addhash_algorithm(builder, hash_algorithm):
    CanonicalizationRuleAddhash_algorithm(builder, hash_algorithm)

def CanonicalizationRuleEnd(builder):
    return builder.EndObject()

def End(builder):
    return CanonicalizationRuleEnd(builder)


class CanonicalizationRuleT(object):

    # CanonicalizationRuleT
    def __init__(
        self,
        version = 1,
        stripped_custom_section_prefix = None,
        bundle_section_name = None,
        hash_algorithm = None,
    ):
        self.version = version  # type: int
        self.stripped_custom_section_prefix = stripped_custom_section_prefix  # type: Optional[str]
        self.bundle_section_name = bundle_section_name  # type: Optional[str]
        self.hash_algorithm = hash_algorithm  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCanonicalizationRule = CanonicalizationRule()
        tmpCanonicalizationRule.Init(buf, pos)
        return cls.InitFromObj(tmpCanonicalizationRule)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCanonicalizationRule):
        x = CanonicalizationRuleT()
        x._UnPack(tmpCanonicalizationRule)
        return x

    # CanonicalizationRuleT
    def _UnPack(self, CanonicalizationRule):
        if CanonicalizationRule is None:
            return
        self.version = CanonicalizationRule.version()
        self.stripped_custom_section_prefix = CanonicalizationRule.stripped_custom_section_prefix()
        self.bundle_section_name = CanonicalizationRule.bundle_section_name()
        self.hash_algorithm = CanonicalizationRule.hash_algorithm()

    # CanonicalizationRuleT
    def Pack(self, builder):
        if self.stripped_custom_section_prefix is not None:
            stripped_custom_section_prefix = builder.CreateString(self.stripped_custom_section_prefix)
        if self.bundle_section_name is not None:
            bundle_section_name = builder.CreateString(self.bundle_section_name)
        if self.hash_algorithm is not None:
            hash_algorithm = builder.CreateString(self.hash_algorithm)
        CanonicalizationRuleStart(builder)
        CanonicalizationRuleAddversion(builder, self.version)
        if self.stripped_custom_section_prefix is not None:
            CanonicalizationRuleAddstripped_custom_section_prefix(builder, stripped_custom_section_prefix)
        if self.bundle_section_name is not None:
            CanonicalizationRuleAddbundle_section_name(builder, bundle_section_name)
        if self.hash_algorithm is not None:
            CanonicalizationRuleAddhash_algorithm(builder, hash_algorithm)
        CanonicalizationRule = CanonicalizationRuleEnd(builder)
        return CanonicalizationRule
