# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Metadata stored in the REC trailer for one module delivery artifact.
class MBL(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MBL()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMBL(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def MBLBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4D\x42\x4C", size_prefixed=size_prefixed)

    # MBL
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Bundle schema version.
    # MBL
    def bundle_version(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 1

    # Human-readable package format label.
    # MBL
    def module_format(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Canonicalization rule used to compute module hashes.
    # MBL
    def canonicalization(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CanonicalizationRule import CanonicalizationRule
            obj = CanonicalizationRule()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SHA-256 of the wasm module after stripping SDS-owned custom sections.
    # MBL
    def canonical_module_hash(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # MBL
    def canonical_module_hashAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # MBL
    def canonical_module_hashLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MBL
    def canonical_module_hashIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # SHA-256 of the canonical plugin manifest bytes.
    # MBL
    def manifest_hash(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # MBL
    def manifest_hashAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # MBL
    def manifest_hashLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MBL
    def manifest_hashIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Legacy ABI export retained for backward compatibility.
    # MBL
    def manifest_export_symbol(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Legacy ABI export retained for backward compatibility.
    # MBL
    def manifest_size_symbol(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Payloads embedded in the bundle.
    # MBL
    def entries(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ModuleBundleEntry import ModuleBundleEntry
            obj = ModuleBundleEntry()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MBL
    def entriesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MBL
    def entriesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

def MBLStart(builder):
    builder.StartObject(8)

def Start(builder):
    MBLStart(builder)

def MBLAddbundle_version(builder, bundle_version):
    builder.PrependUint16Slot(0, bundle_version, 1)

def Addbundle_version(builder, bundle_version):
    MBLAddbundle_version(builder, bundle_version)

def MBLAddmodule_format(builder, module_format):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(module_format), 0)

def Addmodule_format(builder, module_format):
    MBLAddmodule_format(builder, module_format)

def MBLAddcanonicalization(builder, canonicalization):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(canonicalization), 0)

def Addcanonicalization(builder, canonicalization):
    MBLAddcanonicalization(builder, canonicalization)

def MBLAddcanonical_module_hash(builder, canonical_module_hash):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(canonical_module_hash), 0)

def Addcanonical_module_hash(builder, canonical_module_hash):
    MBLAddcanonical_module_hash(builder, canonical_module_hash)

def MBLStartcanonical_module_hashVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def Startcanonical_module_hashVector(builder, numElems):
    return MBLStartcanonical_module_hashVector(builder, numElems)

def MBLCreatecanonical_module_hashVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def Createcanonical_module_hashVector(builder, data):
    MBLCreatecanonical_module_hashVector(builder, data)

def MBLAddmanifest_hash(builder, manifest_hash):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(manifest_hash), 0)

def Addmanifest_hash(builder, manifest_hash):
    MBLAddmanifest_hash(builder, manifest_hash)

def MBLStartmanifest_hashVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def Startmanifest_hashVector(builder, numElems):
    return MBLStartmanifest_hashVector(builder, numElems)

def MBLCreatemanifest_hashVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def Createmanifest_hashVector(builder, data):
    MBLCreatemanifest_hashVector(builder, data)

def MBLAddmanifest_export_symbol(builder, manifest_export_symbol):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(manifest_export_symbol), 0)

def Addmanifest_export_symbol(builder, manifest_export_symbol):
    MBLAddmanifest_export_symbol(builder, manifest_export_symbol)

def MBLAddmanifest_size_symbol(builder, manifest_size_symbol):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(manifest_size_symbol), 0)

def Addmanifest_size_symbol(builder, manifest_size_symbol):
    MBLAddmanifest_size_symbol(builder, manifest_size_symbol)

def MBLAddentries(builder, entries):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(entries), 0)

def Addentries(builder, entries):
    MBLAddentries(builder, entries)

def MBLStartentriesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartentriesVector(builder, numElems):
    return MBLStartentriesVector(builder, numElems)

def MBLCreateentriesVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateentriesVector(builder, data):
    MBLCreateentriesVector(builder, data)

def MBLEnd(builder):
    return builder.EndObject()

def End(builder):
    return MBLEnd(builder)

import CanonicalizationRule
import ModuleBundleEntry
try:
    from typing import List, Optional
except:
    pass

class MBLT(object):

    # MBLT
    def __init__(
        self,
        bundle_version = 1,
        module_format = None,
        canonicalization = None,
        canonical_module_hash = None,
        manifest_hash = None,
        manifest_export_symbol = None,
        manifest_size_symbol = None,
        entries = None,
    ):
        self.bundle_version = bundle_version  # type: int
        self.module_format = module_format  # type: Optional[str]
        self.canonicalization = canonicalization  # type: Optional[CanonicalizationRule.CanonicalizationRuleT]
        self.canonical_module_hash = canonical_module_hash  # type: Optional[List[int]]
        self.manifest_hash = manifest_hash  # type: Optional[List[int]]
        self.manifest_export_symbol = manifest_export_symbol  # type: Optional[str]
        self.manifest_size_symbol = manifest_size_symbol  # type: Optional[str]
        self.entries = entries  # type: Optional[List[ModuleBundleEntry.ModuleBundleEntryT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpMbl = MBL()
        tmpMbl.Init(buf, pos)
        return cls.InitFromObj(tmpMbl)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpMbl):
        x = MBLT()
        x._UnPack(tmpMbl)
        return x

    # MBLT
    def _UnPack(self, MBL):
        if MBL is None:
            return
        self.bundle_version = MBL.bundle_version()
        self.module_format = MBL.module_format()
        if MBL.canonicalization() is not None:
            self.canonicalization = CanonicalizationRule.CanonicalizationRuleT.InitFromObj(MBL.canonicalization())
        if not MBL.canonical_module_hashIsNone():
            if np is None:
                self.canonical_module_hash = []
                for i in range(MBL.canonical_module_hashLength()):
                    self.canonical_module_hash.append(MBL.canonical_module_hash(i))
            else:
                self.canonical_module_hash = MBL.canonical_module_hashAsNumpy()
        if not MBL.manifest_hashIsNone():
            if np is None:
                self.manifest_hash = []
                for i in range(MBL.manifest_hashLength()):
                    self.manifest_hash.append(MBL.manifest_hash(i))
            else:
                self.manifest_hash = MBL.manifest_hashAsNumpy()
        self.manifest_export_symbol = MBL.manifest_export_symbol()
        self.manifest_size_symbol = MBL.manifest_size_symbol()
        if not MBL.entriesIsNone():
            self.entries = []
            for i in range(MBL.entriesLength()):
                if MBL.entries(i) is None:
                    self.entries.append(None)
                else:
                    moduleBundleEntry_ = ModuleBundleEntry.ModuleBundleEntryT.InitFromObj(MBL.entries(i))
                    self.entries.append(moduleBundleEntry_)

    # MBLT
    def Pack(self, builder):
        if self.module_format is not None:
            module_format = builder.CreateString(self.module_format)
        if self.canonicalization is not None:
            canonicalization = self.canonicalization.Pack(builder)
        if self.canonical_module_hash is not None:
            if np is not None and type(self.canonical_module_hash) is np.ndarray:
                canonical_module_hash = builder.CreateNumpyVector(self.canonical_module_hash)
            else:
                MBLStartcanonical_module_hashVector(builder, len(self.canonical_module_hash))
                for i in reversed(range(len(self.canonical_module_hash))):
                    builder.PrependUint8(self.canonical_module_hash[i])
                canonical_module_hash = builder.EndVector()
        if self.manifest_hash is not None:
            if np is not None and type(self.manifest_hash) is np.ndarray:
                manifest_hash = builder.CreateNumpyVector(self.manifest_hash)
            else:
                MBLStartmanifest_hashVector(builder, len(self.manifest_hash))
                for i in reversed(range(len(self.manifest_hash))):
                    builder.PrependUint8(self.manifest_hash[i])
                manifest_hash = builder.EndVector()
        if self.manifest_export_symbol is not None:
            manifest_export_symbol = builder.CreateString(self.manifest_export_symbol)
        if self.manifest_size_symbol is not None:
            manifest_size_symbol = builder.CreateString(self.manifest_size_symbol)
        if self.entries is not None:
            entrieslist = []
            for i in range(len(self.entries)):
                entrieslist.append(self.entries[i].Pack(builder))
            MBLStartentriesVector(builder, len(self.entries))
            for i in reversed(range(len(self.entries))):
                builder.PrependUOffsetTRelative(entrieslist[i])
            entries = builder.EndVector()
        MBLStart(builder)
        MBLAddbundle_version(builder, self.bundle_version)
        if self.module_format is not None:
            MBLAddmodule_format(builder, module_format)
        if self.canonicalization is not None:
            MBLAddcanonicalization(builder, canonicalization)
        if self.canonical_module_hash is not None:
            MBLAddcanonical_module_hash(builder, canonical_module_hash)
        if self.manifest_hash is not None:
            MBLAddmanifest_hash(builder, manifest_hash)
        if self.manifest_export_symbol is not None:
            MBLAddmanifest_export_symbol(builder, manifest_export_symbol)
        if self.manifest_size_symbol is not None:
            MBLAddmanifest_size_symbol(builder, manifest_size_symbol)
        if self.entries is not None:
            MBLAddentries(builder, entries)
        MBL = MBLEnd(builder)
        return MBL
