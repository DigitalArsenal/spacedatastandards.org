# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class EOOCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EOOCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEOOCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def EOOCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x4F\x4F", size_prefixed=size_prefixed)

    # EOOCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EOOCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from EOO import EOO
            obj = EOO()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # EOOCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EOOCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def EOOCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    EOOCOLLECTIONStart(builder)

def EOOCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    EOOCOLLECTIONAddRECORDS(builder, RECORDS)

def EOOCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return EOOCOLLECTIONStartRECORDSVector(builder, numElems)

def EOOCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return EOOCOLLECTIONEnd(builder)

import EOO
try:
    from typing import List
except:
    pass

class EOOCOLLECTIONT(object):

    # EOOCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[EOO.EOOT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        EOOCOLLECTION = EOOCOLLECTION()
        EOOCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(EOOCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, EOOCOLLECTION):
        x = EOOCOLLECTIONT()
        x._UnPack(EOOCOLLECTION)
        return x

    # EOOCOLLECTIONT
    def _UnPack(self, EOOCOLLECTION):
        if EOOCOLLECTION is None:
            return
        if not EOOCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(EOOCOLLECTION.RECORDSLength()):
                if EOOCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    eOO_ = EOO.EOOT.InitFromObj(EOOCOLLECTION.RECORDS(i))
                    self.RECORDS.append(eOO_)

    # EOOCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            EOOCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        EOOCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            EOOCOLLECTIONAddRECORDS(builder, RECORDS)
        EOOCOLLECTION = EOOCOLLECTIONEnd(builder)
        return EOOCOLLECTION
