# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SPWCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SPWCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSPWCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SPWCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x50\x57", size_prefixed=size_prefixed)

    # SPWCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SPWCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SPW import SPW
            obj = SPW()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SPWCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SPWCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def SPWCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    SPWCOLLECTIONStart(builder)

def SPWCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    SPWCOLLECTIONAddRECORDS(builder, RECORDS)

def SPWCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return SPWCOLLECTIONStartRECORDSVector(builder, numElems)

def SPWCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return SPWCOLLECTIONEnd(builder)

import SPW
try:
    from typing import List
except:
    pass

class SPWCOLLECTIONT(object):

    # SPWCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[SPW.SPWT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        SPWCOLLECTION = SPWCOLLECTION()
        SPWCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(SPWCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, SPWCOLLECTION):
        x = SPWCOLLECTIONT()
        x._UnPack(SPWCOLLECTION)
        return x

    # SPWCOLLECTIONT
    def _UnPack(self, SPWCOLLECTION):
        if SPWCOLLECTION is None:
            return
        if not SPWCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(SPWCOLLECTION.RECORDSLength()):
                if SPWCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    sPW_ = SPW.SPWT.InitFromObj(SPWCOLLECTION.RECORDS(i))
                    self.RECORDS.append(sPW_)

    # SPWCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            SPWCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        SPWCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            SPWCOLLECTIONAddRECORDS(builder, RECORDS)
        SPWCOLLECTION = SPWCOLLECTIONEnd(builder)
        return SPWCOLLECTION
