# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Sensor Runtime Wire envelope — carries either a batch request or a batch
# response across the runtime boundary.
class SNW(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SNW()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSNW(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SNWBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x4E\x57", size_prefixed=size_prefixed)

    # SNW
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SNW
    def BATCH_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SNWBatchRequest import SNWBatchRequest
            obj = SNWBatchRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SNW
    def BATCH_RESPONSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SNWBatchResponse import SNWBatchResponse
            obj = SNWBatchResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def SNWStart(builder):
    builder.StartObject(2)

def Start(builder):
    SNWStart(builder)

def SNWAddBATCH_REQUEST(builder, BATCH_REQUEST):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(BATCH_REQUEST), 0)

def AddBATCH_REQUEST(builder, BATCH_REQUEST):
    SNWAddBATCH_REQUEST(builder, BATCH_REQUEST)

def SNWAddBATCH_RESPONSE(builder, BATCH_RESPONSE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(BATCH_RESPONSE), 0)

def AddBATCH_RESPONSE(builder, BATCH_RESPONSE):
    SNWAddBATCH_RESPONSE(builder, BATCH_RESPONSE)

def SNWEnd(builder):
    return builder.EndObject()

def End(builder):
    return SNWEnd(builder)

import SNWBatchRequest
import SNWBatchResponse
try:
    from typing import Optional
except:
    pass

class SNWT(object):

    # SNWT
    def __init__(
        self,
        BATCH_REQUEST = None,
        BATCH_RESPONSE = None,
    ):
        self.BATCH_REQUEST = BATCH_REQUEST  # type: Optional[SNWBatchRequest.SNWBatchRequestT]
        self.BATCH_RESPONSE = BATCH_RESPONSE  # type: Optional[SNWBatchResponse.SNWBatchResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSnw = SNW()
        tmpSnw.Init(buf, pos)
        return cls.InitFromObj(tmpSnw)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSnw):
        x = SNWT()
        x._UnPack(tmpSnw)
        return x

    # SNWT
    def _UnPack(self, SNW):
        if SNW is None:
            return
        if SNW.BATCH_REQUEST() is not None:
            self.BATCH_REQUEST = SNWBatchRequest.SNWBatchRequestT.InitFromObj(SNW.BATCH_REQUEST())
        if SNW.BATCH_RESPONSE() is not None:
            self.BATCH_RESPONSE = SNWBatchResponse.SNWBatchResponseT.InitFromObj(SNW.BATCH_RESPONSE())

    # SNWT
    def Pack(self, builder):
        if self.BATCH_REQUEST is not None:
            BATCH_REQUEST = self.BATCH_REQUEST.Pack(builder)
        if self.BATCH_RESPONSE is not None:
            BATCH_RESPONSE = self.BATCH_RESPONSE.Pack(builder)
        SNWStart(builder)
        if self.BATCH_REQUEST is not None:
            SNWAddBATCH_REQUEST(builder, BATCH_REQUEST)
        if self.BATCH_RESPONSE is not None:
            SNWAddBATCH_RESPONSE(builder, BATCH_RESPONSE)
        SNW = SNWEnd(builder)
        return SNW
