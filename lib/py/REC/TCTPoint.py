# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One point along a track.
class TCTPoint(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TCTPoint()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTCTPoint(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TCTPointBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x43\x54", size_prefixed=size_prefixed)

    # TCTPoint
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Time the point is valid at, Unix milliseconds UTC.
    # TCTPoint
    def VALID_TIME_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Lead from INIT_TIME_MS, hours; 0 for observed tracks.
    # TCTPoint
    def LEAD_HOURS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Centre latitude, degrees north.
    # TCTPoint
    def LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Centre longitude, degrees east in [-180, 180).
    # TCTPoint
    def LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum sustained wind over WIND_AVERAGING_PERIOD_S, m/s
    # (1 kt = 1852/3600 m/s).
    # TCTPoint
    def MAX_SUSTAINED_WIND_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Minimum central pressure, pascal (1 hPa = 100 Pa).
    # TCTPoint
    def MIN_CENTRAL_PRESSURE_PA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Radius of maximum wind, km; negative when not reported.
    # TCTPoint
    def RADIUS_MAX_WIND_KM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Wind radii at each reported threshold.
    # TCTPoint
    def RADII(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TCTRadii import TCTRadii
            obj = TCTRadii()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TCTPoint
    def RADIILength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TCTPoint
    def RADIIIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Intensity category at this point.
    # TCTPoint
    def CATEGORY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Probability in [0, 1] that the system exists at this point; 1 for
    # observed tracks and unconditional forecasts.
    # TCTPoint
    def EXISTENCE_PROBABILITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 1.0

    # Direction of motion, degrees clockwise from north; negative when not
    # reported.
    # TCTPoint
    def MOTION_DIRECTION_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Speed of motion, m/s; negative when not reported.
    # TCTPoint
    def MOTION_SPEED_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

def TCTPointStart(builder):
    builder.StartObject(12)

def Start(builder):
    TCTPointStart(builder)

def TCTPointAddVALID_TIME_MS(builder, VALID_TIME_MS):
    builder.PrependUint64Slot(0, VALID_TIME_MS, 0)

def AddVALID_TIME_MS(builder, VALID_TIME_MS):
    TCTPointAddVALID_TIME_MS(builder, VALID_TIME_MS)

def TCTPointAddLEAD_HOURS(builder, LEAD_HOURS):
    builder.PrependFloat32Slot(1, LEAD_HOURS, 0.0)

def AddLEAD_HOURS(builder, LEAD_HOURS):
    TCTPointAddLEAD_HOURS(builder, LEAD_HOURS)

def TCTPointAddLATITUDE(builder, LATITUDE):
    builder.PrependFloat64Slot(2, LATITUDE, 0.0)

def AddLATITUDE(builder, LATITUDE):
    TCTPointAddLATITUDE(builder, LATITUDE)

def TCTPointAddLONGITUDE(builder, LONGITUDE):
    builder.PrependFloat64Slot(3, LONGITUDE, 0.0)

def AddLONGITUDE(builder, LONGITUDE):
    TCTPointAddLONGITUDE(builder, LONGITUDE)

def TCTPointAddMAX_SUSTAINED_WIND_MS(builder, MAX_SUSTAINED_WIND_MS):
    builder.PrependFloat32Slot(4, MAX_SUSTAINED_WIND_MS, 0.0)

def AddMAX_SUSTAINED_WIND_MS(builder, MAX_SUSTAINED_WIND_MS):
    TCTPointAddMAX_SUSTAINED_WIND_MS(builder, MAX_SUSTAINED_WIND_MS)

def TCTPointAddMIN_CENTRAL_PRESSURE_PA(builder, MIN_CENTRAL_PRESSURE_PA):
    builder.PrependFloat32Slot(5, MIN_CENTRAL_PRESSURE_PA, 0.0)

def AddMIN_CENTRAL_PRESSURE_PA(builder, MIN_CENTRAL_PRESSURE_PA):
    TCTPointAddMIN_CENTRAL_PRESSURE_PA(builder, MIN_CENTRAL_PRESSURE_PA)

def TCTPointAddRADIUS_MAX_WIND_KM(builder, RADIUS_MAX_WIND_KM):
    builder.PrependFloat32Slot(6, RADIUS_MAX_WIND_KM, 0.0)

def AddRADIUS_MAX_WIND_KM(builder, RADIUS_MAX_WIND_KM):
    TCTPointAddRADIUS_MAX_WIND_KM(builder, RADIUS_MAX_WIND_KM)

def TCTPointAddRADII(builder, RADII):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(RADII), 0)

def AddRADII(builder, RADII):
    TCTPointAddRADII(builder, RADII)

def TCTPointStartRADIIVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRADIIVector(builder, numElems):
    return TCTPointStartRADIIVector(builder, numElems)

def TCTPointCreateRADIIVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateRADIIVector(builder, data):
    TCTPointCreateRADIIVector(builder, data)

def TCTPointAddCATEGORY(builder, CATEGORY):
    builder.PrependInt8Slot(8, CATEGORY, 0)

def AddCATEGORY(builder, CATEGORY):
    TCTPointAddCATEGORY(builder, CATEGORY)

def TCTPointAddEXISTENCE_PROBABILITY(builder, EXISTENCE_PROBABILITY):
    builder.PrependFloat32Slot(9, EXISTENCE_PROBABILITY, 1.0)

def AddEXISTENCE_PROBABILITY(builder, EXISTENCE_PROBABILITY):
    TCTPointAddEXISTENCE_PROBABILITY(builder, EXISTENCE_PROBABILITY)

def TCTPointAddMOTION_DIRECTION_DEG(builder, MOTION_DIRECTION_DEG):
    builder.PrependFloat32Slot(10, MOTION_DIRECTION_DEG, 0.0)

def AddMOTION_DIRECTION_DEG(builder, MOTION_DIRECTION_DEG):
    TCTPointAddMOTION_DIRECTION_DEG(builder, MOTION_DIRECTION_DEG)

def TCTPointAddMOTION_SPEED_MS(builder, MOTION_SPEED_MS):
    builder.PrependFloat32Slot(11, MOTION_SPEED_MS, 0.0)

def AddMOTION_SPEED_MS(builder, MOTION_SPEED_MS):
    TCTPointAddMOTION_SPEED_MS(builder, MOTION_SPEED_MS)

def TCTPointEnd(builder):
    return builder.EndObject()

def End(builder):
    return TCTPointEnd(builder)

import TCTRadii
try:
    from typing import List
except:
    pass

class TCTPointT(object):

    # TCTPointT
    def __init__(
        self,
        VALID_TIME_MS = 0,
        LEAD_HOURS = 0.0,
        LATITUDE = 0.0,
        LONGITUDE = 0.0,
        MAX_SUSTAINED_WIND_MS = 0.0,
        MIN_CENTRAL_PRESSURE_PA = 0.0,
        RADIUS_MAX_WIND_KM = 0.0,
        RADII = None,
        CATEGORY = 0,
        EXISTENCE_PROBABILITY = 1.0,
        MOTION_DIRECTION_DEG = 0.0,
        MOTION_SPEED_MS = 0.0,
    ):
        self.VALID_TIME_MS = VALID_TIME_MS  # type: int
        self.LEAD_HOURS = LEAD_HOURS  # type: float
        self.LATITUDE = LATITUDE  # type: float
        self.LONGITUDE = LONGITUDE  # type: float
        self.MAX_SUSTAINED_WIND_MS = MAX_SUSTAINED_WIND_MS  # type: float
        self.MIN_CENTRAL_PRESSURE_PA = MIN_CENTRAL_PRESSURE_PA  # type: float
        self.RADIUS_MAX_WIND_KM = RADIUS_MAX_WIND_KM  # type: float
        self.RADII = RADII  # type: Optional[List[TCTRadii.TCTRadiiT]]
        self.CATEGORY = CATEGORY  # type: int
        self.EXISTENCE_PROBABILITY = EXISTENCE_PROBABILITY  # type: float
        self.MOTION_DIRECTION_DEG = MOTION_DIRECTION_DEG  # type: float
        self.MOTION_SPEED_MS = MOTION_SPEED_MS  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTctpoint = TCTPoint()
        tmpTctpoint.Init(buf, pos)
        return cls.InitFromObj(tmpTctpoint)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTctpoint):
        x = TCTPointT()
        x._UnPack(tmpTctpoint)
        return x

    # TCTPointT
    def _UnPack(self, TCTPoint):
        if TCTPoint is None:
            return
        self.VALID_TIME_MS = TCTPoint.VALID_TIME_MS()
        self.LEAD_HOURS = TCTPoint.LEAD_HOURS()
        self.LATITUDE = TCTPoint.LATITUDE()
        self.LONGITUDE = TCTPoint.LONGITUDE()
        self.MAX_SUSTAINED_WIND_MS = TCTPoint.MAX_SUSTAINED_WIND_MS()
        self.MIN_CENTRAL_PRESSURE_PA = TCTPoint.MIN_CENTRAL_PRESSURE_PA()
        self.RADIUS_MAX_WIND_KM = TCTPoint.RADIUS_MAX_WIND_KM()
        if not TCTPoint.RADIIIsNone():
            self.RADII = []
            for i in range(TCTPoint.RADIILength()):
                if TCTPoint.RADII(i) is None:
                    self.RADII.append(None)
                else:
                    tCTRadii_ = TCTRadii.TCTRadiiT.InitFromObj(TCTPoint.RADII(i))
                    self.RADII.append(tCTRadii_)
        self.CATEGORY = TCTPoint.CATEGORY()
        self.EXISTENCE_PROBABILITY = TCTPoint.EXISTENCE_PROBABILITY()
        self.MOTION_DIRECTION_DEG = TCTPoint.MOTION_DIRECTION_DEG()
        self.MOTION_SPEED_MS = TCTPoint.MOTION_SPEED_MS()

    # TCTPointT
    def Pack(self, builder):
        if self.RADII is not None:
            RADIIlist = []
            for i in range(len(self.RADII)):
                RADIIlist.append(self.RADII[i].Pack(builder))
            TCTPointStartRADIIVector(builder, len(self.RADII))
            for i in reversed(range(len(self.RADII))):
                builder.PrependUOffsetTRelative(RADIIlist[i])
            RADII = builder.EndVector()
        TCTPointStart(builder)
        TCTPointAddVALID_TIME_MS(builder, self.VALID_TIME_MS)
        TCTPointAddLEAD_HOURS(builder, self.LEAD_HOURS)
        TCTPointAddLATITUDE(builder, self.LATITUDE)
        TCTPointAddLONGITUDE(builder, self.LONGITUDE)
        TCTPointAddMAX_SUSTAINED_WIND_MS(builder, self.MAX_SUSTAINED_WIND_MS)
        TCTPointAddMIN_CENTRAL_PRESSURE_PA(builder, self.MIN_CENTRAL_PRESSURE_PA)
        TCTPointAddRADIUS_MAX_WIND_KM(builder, self.RADIUS_MAX_WIND_KM)
        if self.RADII is not None:
            TCTPointAddRADII(builder, RADII)
        TCTPointAddCATEGORY(builder, self.CATEGORY)
        TCTPointAddEXISTENCE_PROBABILITY(builder, self.EXISTENCE_PROBABILITY)
        TCTPointAddMOTION_DIRECTION_DEG(builder, self.MOTION_DIRECTION_DEG)
        TCTPointAddMOTION_SPEED_MS(builder, self.MOTION_SPEED_MS)
        TCTPoint = TCTPointEnd(builder)
        return TCTPoint
