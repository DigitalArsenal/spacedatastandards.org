# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Object Physical Properties - the sourced physical description of one space
# object and, when one exists, of the exact 3D asset that represents it.
#
# Division of labour with the neighbouring standards. $CAT is catalogue
# identity and orbit, and its RCS, SIZE and MASS scalars are unattributed
# convenience fields carried from the catalogue publisher. $BUS is the bus
# DESIGN, shared by every object built on it. $VAM is the asset manifest: the
# bytes, the licence, the review and the geometry metrics. $PNL is the
# articulated panel model consumed by solar-radiation-pressure propagation.
# $OPP is the per-object physical truth in which EVERY value names its own
# source, epoch and method. $OPP never restates orbital elements and never
# duplicates bus-design values; a per-object value that came from the bus
# design is stated here with METHOD INFERRED_FROM_FAMILY so a consumer can see
# that it is an approximation.
#
# Joins. NORAD_CAT_ID and OBJECT_ID join to $CAT. BUS_ID joins to $BUS.
# ASSET joins to the exact reviewed $VAM variant. ENTITY_ID is the stable
# non-catalogue key shared with $VAM for objects that have no catalogue entry.
#
# Never-invent law, enforced by the shape rather than by prose: a physical
# quantity exists only as an OPPQuantity and OPPQuantity.PROVENANCE is
# required, so there is no encoding for a mass, dimension, area or
# cross-section that does not name where it came from. Unknown values are
# absent. A consumer that finds a field missing has learned that no admissible
# source published it.
class OPP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OPP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOPP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OPPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x50\x50", size_prefixed=size_prefixed)

    # OPP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier for this record.
    # OPP
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SemVer 2.0.0 record-format version, not a content revision.
    # OPP
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $CAT.NORAD_CAT_ID verbatim. Zero when the object has no catalogue number.
    # OPP
    def NORAD_CAT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # $CAT.OBJECT_ID verbatim: the COSPAR international designator.
    # OPP
    def OBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $CAT.OBJECT_NAME verbatim, for display only. Never a join key.
    # OPP
    def OBJECT_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Stable non-catalogue entity key, such as an asset catalogue entity path.
    # MUST equal ASSET.ENTITY_ID when ASSET is present.
    # OPP
    def ENTITY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Join key to the $BUS record describing this object's bus design; holds
    # that record's BUS.ID verbatim. Empty when the bus is unknown or has no
    # $BUS record.
    # OPP
    def BUS_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Bus or platform family name verbatim from BUS_FAMILY_PROVENANCE.SOURCE,
    # for objects whose family is known but has no $BUS record.
    # OPP
    def BUS_FAMILY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provenance of BUS_FAMILY. Present whenever BUS_FAMILY is nonempty.
    # OPP
    def BUS_FAMILY_PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPProvenance import OPPProvenance
            obj = OPPProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Manufacturer or prime contractor as stated by MANUFACTURER_PROVENANCE.
    # OPP
    def MANUFACTURER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provenance of MANUFACTURER. Present whenever MANUFACTURER is nonempty.
    # OPP
    def MANUFACTURER_PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPProvenance import OPPProvenance
            obj = OPPProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPP
    def MASS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPMass import OPPMass
            obj = OPPMass()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPP
    def DIMENSIONS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPDimensions import OPPDimensions
            obj = OPPDimensions()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPP
    def AREAS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPAreas import OPPAreas
            obj = OPPAreas()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # One entry per band, polarization and aspect the sources publish. Empty
    # means no source published a radar cross-section for this object.
    # OPP
    def RADAR_CROSS_SECTIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from OPPRadarCrossSection import OPPRadarCrossSection
            obj = OPPRadarCrossSection()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPP
    def RADAR_CROSS_SECTIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # OPP
    def RADAR_CROSS_SECTIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

    # Visual magnitude, UNITS "mag", with the observing conditions in
    # PROVENANCE.NOTES.
    # OPP
    def VISUAL_MAGNITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Material and surface inventory.
    # OPP
    def SURFACES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from OPPSurface import OPPSurface
            obj = OPPSurface()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPP
    def SURFACESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # OPP
    def SURFACESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        return o == 0

    # Binding to the exact reviewed 3D asset this record accompanies. Absent for
    # an object with no asset; the physical values remain valid without it.
    # OPP
    def ASSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPAssetRef import OPPAssetRef
            obj = OPPAssetRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Every source consulted while building this record, including sources that
    # yielded no value. Lets a consumer distinguish "not looked for" from
    # "looked for and not published".
    # OPP
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from OPPProvenance import OPPProvenance
            obj = OPPProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPP
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # OPP
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        return o == 0

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ) when
    # the record was created.
    # OPP
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ) when
    # the record was last updated.
    # OPP
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # CIDv1 containing a multihash of the exact superseded $OPP bytes.
    # OPP
    def SUPERSEDES_OPP_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # OPP
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def OPPStart(builder):
    builder.StartObject(23)

def Start(builder):
    OPPStart(builder)

def OPPAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    OPPAddID(builder, ID)

def OPPAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    OPPAddVERSION(builder, VERSION)

def OPPAddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    builder.PrependUint32Slot(2, NORAD_CAT_ID, 0)

def AddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    OPPAddNORAD_CAT_ID(builder, NORAD_CAT_ID)

def OPPAddOBJECT_ID(builder, OBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT_ID), 0)

def AddOBJECT_ID(builder, OBJECT_ID):
    OPPAddOBJECT_ID(builder, OBJECT_ID)

def OPPAddOBJECT_NAME(builder, OBJECT_NAME):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT_NAME), 0)

def AddOBJECT_NAME(builder, OBJECT_NAME):
    OPPAddOBJECT_NAME(builder, OBJECT_NAME)

def OPPAddENTITY_ID(builder, ENTITY_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ENTITY_ID), 0)

def AddENTITY_ID(builder, ENTITY_ID):
    OPPAddENTITY_ID(builder, ENTITY_ID)

def OPPAddBUS_ID(builder, BUS_ID):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(BUS_ID), 0)

def AddBUS_ID(builder, BUS_ID):
    OPPAddBUS_ID(builder, BUS_ID)

def OPPAddBUS_FAMILY(builder, BUS_FAMILY):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(BUS_FAMILY), 0)

def AddBUS_FAMILY(builder, BUS_FAMILY):
    OPPAddBUS_FAMILY(builder, BUS_FAMILY)

def OPPAddBUS_FAMILY_PROVENANCE(builder, BUS_FAMILY_PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(BUS_FAMILY_PROVENANCE), 0)

def AddBUS_FAMILY_PROVENANCE(builder, BUS_FAMILY_PROVENANCE):
    OPPAddBUS_FAMILY_PROVENANCE(builder, BUS_FAMILY_PROVENANCE)

def OPPAddMANUFACTURER(builder, MANUFACTURER):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(MANUFACTURER), 0)

def AddMANUFACTURER(builder, MANUFACTURER):
    OPPAddMANUFACTURER(builder, MANUFACTURER)

def OPPAddMANUFACTURER_PROVENANCE(builder, MANUFACTURER_PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(MANUFACTURER_PROVENANCE), 0)

def AddMANUFACTURER_PROVENANCE(builder, MANUFACTURER_PROVENANCE):
    OPPAddMANUFACTURER_PROVENANCE(builder, MANUFACTURER_PROVENANCE)

def OPPAddMASS(builder, MASS):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(MASS), 0)

def AddMASS(builder, MASS):
    OPPAddMASS(builder, MASS)

def OPPAddDIMENSIONS(builder, DIMENSIONS):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(DIMENSIONS), 0)

def AddDIMENSIONS(builder, DIMENSIONS):
    OPPAddDIMENSIONS(builder, DIMENSIONS)

def OPPAddAREAS(builder, AREAS):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(AREAS), 0)

def AddAREAS(builder, AREAS):
    OPPAddAREAS(builder, AREAS)

def OPPAddRADAR_CROSS_SECTIONS(builder, RADAR_CROSS_SECTIONS):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(RADAR_CROSS_SECTIONS), 0)

def AddRADAR_CROSS_SECTIONS(builder, RADAR_CROSS_SECTIONS):
    OPPAddRADAR_CROSS_SECTIONS(builder, RADAR_CROSS_SECTIONS)

def OPPStartRADAR_CROSS_SECTIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRADAR_CROSS_SECTIONSVector(builder, numElems):
    return OPPStartRADAR_CROSS_SECTIONSVector(builder, numElems)

def OPPCreateRADAR_CROSS_SECTIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateRADAR_CROSS_SECTIONSVector(builder, data):
    OPPCreateRADAR_CROSS_SECTIONSVector(builder, data)

def OPPAddVISUAL_MAGNITUDE(builder, VISUAL_MAGNITUDE):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(VISUAL_MAGNITUDE), 0)

def AddVISUAL_MAGNITUDE(builder, VISUAL_MAGNITUDE):
    OPPAddVISUAL_MAGNITUDE(builder, VISUAL_MAGNITUDE)

def OPPAddSURFACES(builder, SURFACES):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(SURFACES), 0)

def AddSURFACES(builder, SURFACES):
    OPPAddSURFACES(builder, SURFACES)

def OPPStartSURFACESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSURFACESVector(builder, numElems):
    return OPPStartSURFACESVector(builder, numElems)

def OPPCreateSURFACESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSURFACESVector(builder, data):
    OPPCreateSURFACESVector(builder, data)

def OPPAddASSET(builder, ASSET):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(ASSET), 0)

def AddASSET(builder, ASSET):
    OPPAddASSET(builder, ASSET)

def OPPAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    OPPAddSOURCES(builder, SOURCES)

def OPPStartSOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCESVector(builder, numElems):
    return OPPStartSOURCESVector(builder, numElems)

def OPPCreateSOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCESVector(builder, data):
    OPPCreateSOURCESVector(builder, data)

def OPPAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUOffsetTRelativeSlot(19, flatbuffers.number_types.UOffsetTFlags.py_type(CREATED_AT), 0)

def AddCREATED_AT(builder, CREATED_AT):
    OPPAddCREATED_AT(builder, CREATED_AT)

def OPPAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUOffsetTRelativeSlot(20, flatbuffers.number_types.UOffsetTFlags.py_type(UPDATED_AT), 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    OPPAddUPDATED_AT(builder, UPDATED_AT)

def OPPAddSUPERSEDES_OPP_CID(builder, SUPERSEDES_OPP_CID):
    builder.PrependUOffsetTRelativeSlot(21, flatbuffers.number_types.UOffsetTFlags.py_type(SUPERSEDES_OPP_CID), 0)

def AddSUPERSEDES_OPP_CID(builder, SUPERSEDES_OPP_CID):
    OPPAddSUPERSEDES_OPP_CID(builder, SUPERSEDES_OPP_CID)

def OPPAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(22, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    OPPAddNOTES(builder, NOTES)

def OPPEnd(builder):
    return builder.EndObject()

def End(builder):
    return OPPEnd(builder)

import OPPAreas
import OPPAssetRef
import OPPDimensions
import OPPMass
import OPPProvenance
import OPPQuantity
import OPPRadarCrossSection
import OPPSurface
try:
    from typing import List, Optional
except:
    pass

class OPPT(object):

    # OPPT
    def __init__(
        self,
        ID = None,
        VERSION = None,
        NORAD_CAT_ID = 0,
        OBJECT_ID = None,
        OBJECT_NAME = None,
        ENTITY_ID = None,
        BUS_ID = None,
        BUS_FAMILY = None,
        BUS_FAMILY_PROVENANCE = None,
        MANUFACTURER = None,
        MANUFACTURER_PROVENANCE = None,
        MASS = None,
        DIMENSIONS = None,
        AREAS = None,
        RADAR_CROSS_SECTIONS = None,
        VISUAL_MAGNITUDE = None,
        SURFACES = None,
        ASSET = None,
        SOURCES = None,
        CREATED_AT = None,
        UPDATED_AT = None,
        SUPERSEDES_OPP_CID = None,
        NOTES = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.VERSION = VERSION  # type: Optional[str]
        self.NORAD_CAT_ID = NORAD_CAT_ID  # type: int
        self.OBJECT_ID = OBJECT_ID  # type: Optional[str]
        self.OBJECT_NAME = OBJECT_NAME  # type: Optional[str]
        self.ENTITY_ID = ENTITY_ID  # type: Optional[str]
        self.BUS_ID = BUS_ID  # type: Optional[str]
        self.BUS_FAMILY = BUS_FAMILY  # type: Optional[str]
        self.BUS_FAMILY_PROVENANCE = BUS_FAMILY_PROVENANCE  # type: Optional[OPPProvenance.OPPProvenanceT]
        self.MANUFACTURER = MANUFACTURER  # type: Optional[str]
        self.MANUFACTURER_PROVENANCE = MANUFACTURER_PROVENANCE  # type: Optional[OPPProvenance.OPPProvenanceT]
        self.MASS = MASS  # type: Optional[OPPMass.OPPMassT]
        self.DIMENSIONS = DIMENSIONS  # type: Optional[OPPDimensions.OPPDimensionsT]
        self.AREAS = AREAS  # type: Optional[OPPAreas.OPPAreasT]
        self.RADAR_CROSS_SECTIONS = RADAR_CROSS_SECTIONS  # type: Optional[List[OPPRadarCrossSection.OPPRadarCrossSectionT]]
        self.VISUAL_MAGNITUDE = VISUAL_MAGNITUDE  # type: Optional[OPPQuantity.OPPQuantityT]
        self.SURFACES = SURFACES  # type: Optional[List[OPPSurface.OPPSurfaceT]]
        self.ASSET = ASSET  # type: Optional[OPPAssetRef.OPPAssetRefT]
        self.SOURCES = SOURCES  # type: Optional[List[OPPProvenance.OPPProvenanceT]]
        self.CREATED_AT = CREATED_AT  # type: Optional[str]
        self.UPDATED_AT = UPDATED_AT  # type: Optional[str]
        self.SUPERSEDES_OPP_CID = SUPERSEDES_OPP_CID  # type: Optional[str]
        self.NOTES = NOTES  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOpp = OPP()
        tmpOpp.Init(buf, pos)
        return cls.InitFromObj(tmpOpp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOpp):
        x = OPPT()
        x._UnPack(tmpOpp)
        return x

    # OPPT
    def _UnPack(self, OPP):
        if OPP is None:
            return
        self.ID = OPP.ID()
        self.VERSION = OPP.VERSION()
        self.NORAD_CAT_ID = OPP.NORAD_CAT_ID()
        self.OBJECT_ID = OPP.OBJECT_ID()
        self.OBJECT_NAME = OPP.OBJECT_NAME()
        self.ENTITY_ID = OPP.ENTITY_ID()
        self.BUS_ID = OPP.BUS_ID()
        self.BUS_FAMILY = OPP.BUS_FAMILY()
        if OPP.BUS_FAMILY_PROVENANCE() is not None:
            self.BUS_FAMILY_PROVENANCE = OPPProvenance.OPPProvenanceT.InitFromObj(OPP.BUS_FAMILY_PROVENANCE())
        self.MANUFACTURER = OPP.MANUFACTURER()
        if OPP.MANUFACTURER_PROVENANCE() is not None:
            self.MANUFACTURER_PROVENANCE = OPPProvenance.OPPProvenanceT.InitFromObj(OPP.MANUFACTURER_PROVENANCE())
        if OPP.MASS() is not None:
            self.MASS = OPPMass.OPPMassT.InitFromObj(OPP.MASS())
        if OPP.DIMENSIONS() is not None:
            self.DIMENSIONS = OPPDimensions.OPPDimensionsT.InitFromObj(OPP.DIMENSIONS())
        if OPP.AREAS() is not None:
            self.AREAS = OPPAreas.OPPAreasT.InitFromObj(OPP.AREAS())
        if not OPP.RADAR_CROSS_SECTIONSIsNone():
            self.RADAR_CROSS_SECTIONS = []
            for i in range(OPP.RADAR_CROSS_SECTIONSLength()):
                if OPP.RADAR_CROSS_SECTIONS(i) is None:
                    self.RADAR_CROSS_SECTIONS.append(None)
                else:
                    oPPRadarCrossSection_ = OPPRadarCrossSection.OPPRadarCrossSectionT.InitFromObj(OPP.RADAR_CROSS_SECTIONS(i))
                    self.RADAR_CROSS_SECTIONS.append(oPPRadarCrossSection_)
        if OPP.VISUAL_MAGNITUDE() is not None:
            self.VISUAL_MAGNITUDE = OPPQuantity.OPPQuantityT.InitFromObj(OPP.VISUAL_MAGNITUDE())
        if not OPP.SURFACESIsNone():
            self.SURFACES = []
            for i in range(OPP.SURFACESLength()):
                if OPP.SURFACES(i) is None:
                    self.SURFACES.append(None)
                else:
                    oPPSurface_ = OPPSurface.OPPSurfaceT.InitFromObj(OPP.SURFACES(i))
                    self.SURFACES.append(oPPSurface_)
        if OPP.ASSET() is not None:
            self.ASSET = OPPAssetRef.OPPAssetRefT.InitFromObj(OPP.ASSET())
        if not OPP.SOURCESIsNone():
            self.SOURCES = []
            for i in range(OPP.SOURCESLength()):
                if OPP.SOURCES(i) is None:
                    self.SOURCES.append(None)
                else:
                    oPPProvenance_ = OPPProvenance.OPPProvenanceT.InitFromObj(OPP.SOURCES(i))
                    self.SOURCES.append(oPPProvenance_)
        self.CREATED_AT = OPP.CREATED_AT()
        self.UPDATED_AT = OPP.UPDATED_AT()
        self.SUPERSEDES_OPP_CID = OPP.SUPERSEDES_OPP_CID()
        self.NOTES = OPP.NOTES()

    # OPPT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        if self.OBJECT_ID is not None:
            OBJECT_ID = builder.CreateString(self.OBJECT_ID)
        if self.OBJECT_NAME is not None:
            OBJECT_NAME = builder.CreateString(self.OBJECT_NAME)
        if self.ENTITY_ID is not None:
            ENTITY_ID = builder.CreateString(self.ENTITY_ID)
        if self.BUS_ID is not None:
            BUS_ID = builder.CreateString(self.BUS_ID)
        if self.BUS_FAMILY is not None:
            BUS_FAMILY = builder.CreateString(self.BUS_FAMILY)
        if self.BUS_FAMILY_PROVENANCE is not None:
            BUS_FAMILY_PROVENANCE = self.BUS_FAMILY_PROVENANCE.Pack(builder)
        if self.MANUFACTURER is not None:
            MANUFACTURER = builder.CreateString(self.MANUFACTURER)
        if self.MANUFACTURER_PROVENANCE is not None:
            MANUFACTURER_PROVENANCE = self.MANUFACTURER_PROVENANCE.Pack(builder)
        if self.MASS is not None:
            MASS = self.MASS.Pack(builder)
        if self.DIMENSIONS is not None:
            DIMENSIONS = self.DIMENSIONS.Pack(builder)
        if self.AREAS is not None:
            AREAS = self.AREAS.Pack(builder)
        if self.RADAR_CROSS_SECTIONS is not None:
            RADAR_CROSS_SECTIONSlist = []
            for i in range(len(self.RADAR_CROSS_SECTIONS)):
                RADAR_CROSS_SECTIONSlist.append(self.RADAR_CROSS_SECTIONS[i].Pack(builder))
            OPPStartRADAR_CROSS_SECTIONSVector(builder, len(self.RADAR_CROSS_SECTIONS))
            for i in reversed(range(len(self.RADAR_CROSS_SECTIONS))):
                builder.PrependUOffsetTRelative(RADAR_CROSS_SECTIONSlist[i])
            RADAR_CROSS_SECTIONS = builder.EndVector()
        if self.VISUAL_MAGNITUDE is not None:
            VISUAL_MAGNITUDE = self.VISUAL_MAGNITUDE.Pack(builder)
        if self.SURFACES is not None:
            SURFACESlist = []
            for i in range(len(self.SURFACES)):
                SURFACESlist.append(self.SURFACES[i].Pack(builder))
            OPPStartSURFACESVector(builder, len(self.SURFACES))
            for i in reversed(range(len(self.SURFACES))):
                builder.PrependUOffsetTRelative(SURFACESlist[i])
            SURFACES = builder.EndVector()
        if self.ASSET is not None:
            ASSET = self.ASSET.Pack(builder)
        if self.SOURCES is not None:
            SOURCESlist = []
            for i in range(len(self.SOURCES)):
                SOURCESlist.append(self.SOURCES[i].Pack(builder))
            OPPStartSOURCESVector(builder, len(self.SOURCES))
            for i in reversed(range(len(self.SOURCES))):
                builder.PrependUOffsetTRelative(SOURCESlist[i])
            SOURCES = builder.EndVector()
        if self.CREATED_AT is not None:
            CREATED_AT = builder.CreateString(self.CREATED_AT)
        if self.UPDATED_AT is not None:
            UPDATED_AT = builder.CreateString(self.UPDATED_AT)
        if self.SUPERSEDES_OPP_CID is not None:
            SUPERSEDES_OPP_CID = builder.CreateString(self.SUPERSEDES_OPP_CID)
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        OPPStart(builder)
        if self.ID is not None:
            OPPAddID(builder, ID)
        if self.VERSION is not None:
            OPPAddVERSION(builder, VERSION)
        OPPAddNORAD_CAT_ID(builder, self.NORAD_CAT_ID)
        if self.OBJECT_ID is not None:
            OPPAddOBJECT_ID(builder, OBJECT_ID)
        if self.OBJECT_NAME is not None:
            OPPAddOBJECT_NAME(builder, OBJECT_NAME)
        if self.ENTITY_ID is not None:
            OPPAddENTITY_ID(builder, ENTITY_ID)
        if self.BUS_ID is not None:
            OPPAddBUS_ID(builder, BUS_ID)
        if self.BUS_FAMILY is not None:
            OPPAddBUS_FAMILY(builder, BUS_FAMILY)
        if self.BUS_FAMILY_PROVENANCE is not None:
            OPPAddBUS_FAMILY_PROVENANCE(builder, BUS_FAMILY_PROVENANCE)
        if self.MANUFACTURER is not None:
            OPPAddMANUFACTURER(builder, MANUFACTURER)
        if self.MANUFACTURER_PROVENANCE is not None:
            OPPAddMANUFACTURER_PROVENANCE(builder, MANUFACTURER_PROVENANCE)
        if self.MASS is not None:
            OPPAddMASS(builder, MASS)
        if self.DIMENSIONS is not None:
            OPPAddDIMENSIONS(builder, DIMENSIONS)
        if self.AREAS is not None:
            OPPAddAREAS(builder, AREAS)
        if self.RADAR_CROSS_SECTIONS is not None:
            OPPAddRADAR_CROSS_SECTIONS(builder, RADAR_CROSS_SECTIONS)
        if self.VISUAL_MAGNITUDE is not None:
            OPPAddVISUAL_MAGNITUDE(builder, VISUAL_MAGNITUDE)
        if self.SURFACES is not None:
            OPPAddSURFACES(builder, SURFACES)
        if self.ASSET is not None:
            OPPAddASSET(builder, ASSET)
        if self.SOURCES is not None:
            OPPAddSOURCES(builder, SOURCES)
        if self.CREATED_AT is not None:
            OPPAddCREATED_AT(builder, CREATED_AT)
        if self.UPDATED_AT is not None:
            OPPAddUPDATED_AT(builder, UPDATED_AT)
        if self.SUPERSEDES_OPP_CID is not None:
            OPPAddSUPERSEDES_OPP_CID(builder, SUPERSEDES_OPP_CID)
        if self.NOTES is not None:
            OPPAddNOTES(builder, NOTES)
        OPP = OPPEnd(builder)
        return OPP
