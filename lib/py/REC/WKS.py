# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Workspace — named scene snapshot + FlatSQL query state + share grants
class WKS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = WKS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsWKS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def WKSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x57\x4B\x53", size_prefixed=size_prefixed)

    # WKS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable workspace identifier
    # WKS
    def WORKSPACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Display name
    # WKS
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Display description
    # WKS
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # IPFS CID of the owning `$EPM`
    # WKS
    def OWNER_EPM_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$APP.ID` of the application this workspace belongs to
    # WKS
    def APP_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The named `$CZM` scene snapshot
    # WKS
    def SCENE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CZM import CZM
            obj = CZM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FlatSQL query state captured with the scene
    # WKS
    def QUERIES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from WKSQuery import WKSQuery
            obj = WKSQuery()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # WKS
    def QUERIESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # WKS
    def QUERIESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Share grants to other `$EPM` identities
    # WKS
    def SHARE_GRANTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ACL import ACL
            obj = ACL()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # WKS
    def SHARE_GRANTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # WKS
    def SHARE_GRANTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Unix ms the workspace was created
    # WKS
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix ms the workspace was last updated
    # WKS
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Ed25519 signature by the owner `$EPM`
    # WKS
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # WKS
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # WKS
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # WKS
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

def WKSStart(builder):
    builder.StartObject(11)

def Start(builder):
    WKSStart(builder)

def WKSAddWORKSPACE_ID(builder, WORKSPACE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(WORKSPACE_ID), 0)

def AddWORKSPACE_ID(builder, WORKSPACE_ID):
    WKSAddWORKSPACE_ID(builder, WORKSPACE_ID)

def WKSAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    WKSAddNAME(builder, NAME)

def WKSAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    WKSAddDESCRIPTION(builder, DESCRIPTION)

def WKSAddOWNER_EPM_CID(builder, OWNER_EPM_CID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(OWNER_EPM_CID), 0)

def AddOWNER_EPM_CID(builder, OWNER_EPM_CID):
    WKSAddOWNER_EPM_CID(builder, OWNER_EPM_CID)

def WKSAddAPP_ID(builder, APP_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(APP_ID), 0)

def AddAPP_ID(builder, APP_ID):
    WKSAddAPP_ID(builder, APP_ID)

def WKSAddSCENE(builder, SCENE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SCENE), 0)

def AddSCENE(builder, SCENE):
    WKSAddSCENE(builder, SCENE)

def WKSAddQUERIES(builder, QUERIES):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(QUERIES), 0)

def AddQUERIES(builder, QUERIES):
    WKSAddQUERIES(builder, QUERIES)

def WKSStartQUERIESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartQUERIESVector(builder, numElems):
    return WKSStartQUERIESVector(builder, numElems)

def WKSCreateQUERIESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateQUERIESVector(builder, data):
    WKSCreateQUERIESVector(builder, data)

def WKSAddSHARE_GRANTS(builder, SHARE_GRANTS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(SHARE_GRANTS), 0)

def AddSHARE_GRANTS(builder, SHARE_GRANTS):
    WKSAddSHARE_GRANTS(builder, SHARE_GRANTS)

def WKSStartSHARE_GRANTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSHARE_GRANTSVector(builder, numElems):
    return WKSStartSHARE_GRANTSVector(builder, numElems)

def WKSCreateSHARE_GRANTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSHARE_GRANTSVector(builder, data):
    WKSCreateSHARE_GRANTSVector(builder, data)

def WKSAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUint64Slot(8, CREATED_AT, 0)

def AddCREATED_AT(builder, CREATED_AT):
    WKSAddCREATED_AT(builder, CREATED_AT)

def WKSAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUint64Slot(9, UPDATED_AT, 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    WKSAddUPDATED_AT(builder, UPDATED_AT)

def WKSAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    WKSAddSIGNATURE(builder, SIGNATURE)

def WKSStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return WKSStartSIGNATUREVector(builder, numElems)

def WKSCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    WKSCreateSIGNATUREVector(builder, data)

def WKSEnd(builder):
    return builder.EndObject()

def End(builder):
    return WKSEnd(builder)

import ACL
import CZM
import WKSQuery
try:
    from typing import List, Optional
except:
    pass

class WKST(object):

    # WKST
    def __init__(
        self,
        WORKSPACE_ID = None,
        NAME = None,
        DESCRIPTION = None,
        OWNER_EPM_CID = None,
        APP_ID = None,
        SCENE = None,
        QUERIES = None,
        SHARE_GRANTS = None,
        CREATED_AT = 0,
        UPDATED_AT = 0,
        SIGNATURE = None,
    ):
        self.WORKSPACE_ID = WORKSPACE_ID  # type: Optional[str]
        self.NAME = NAME  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.OWNER_EPM_CID = OWNER_EPM_CID  # type: Optional[str]
        self.APP_ID = APP_ID  # type: Optional[str]
        self.SCENE = SCENE  # type: Optional[CZM.CZMT]
        self.QUERIES = QUERIES  # type: Optional[List[WKSQuery.WKSQueryT]]
        self.SHARE_GRANTS = SHARE_GRANTS  # type: Optional[List[ACL.ACLT]]
        self.CREATED_AT = CREATED_AT  # type: int
        self.UPDATED_AT = UPDATED_AT  # type: int
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpWks = WKS()
        tmpWks.Init(buf, pos)
        return cls.InitFromObj(tmpWks)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpWks):
        x = WKST()
        x._UnPack(tmpWks)
        return x

    # WKST
    def _UnPack(self, WKS):
        if WKS is None:
            return
        self.WORKSPACE_ID = WKS.WORKSPACE_ID()
        self.NAME = WKS.NAME()
        self.DESCRIPTION = WKS.DESCRIPTION()
        self.OWNER_EPM_CID = WKS.OWNER_EPM_CID()
        self.APP_ID = WKS.APP_ID()
        if WKS.SCENE() is not None:
            self.SCENE = CZM.CZMT.InitFromObj(WKS.SCENE())
        if not WKS.QUERIESIsNone():
            self.QUERIES = []
            for i in range(WKS.QUERIESLength()):
                if WKS.QUERIES(i) is None:
                    self.QUERIES.append(None)
                else:
                    wKSQuery_ = WKSQuery.WKSQueryT.InitFromObj(WKS.QUERIES(i))
                    self.QUERIES.append(wKSQuery_)
        if not WKS.SHARE_GRANTSIsNone():
            self.SHARE_GRANTS = []
            for i in range(WKS.SHARE_GRANTSLength()):
                if WKS.SHARE_GRANTS(i) is None:
                    self.SHARE_GRANTS.append(None)
                else:
                    aCL_ = ACL.ACLT.InitFromObj(WKS.SHARE_GRANTS(i))
                    self.SHARE_GRANTS.append(aCL_)
        self.CREATED_AT = WKS.CREATED_AT()
        self.UPDATED_AT = WKS.UPDATED_AT()
        if not WKS.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(WKS.SIGNATURELength()):
                    self.SIGNATURE.append(WKS.SIGNATURE(i))
            else:
                self.SIGNATURE = WKS.SIGNATUREAsNumpy()

    # WKST
    def Pack(self, builder):
        if self.WORKSPACE_ID is not None:
            WORKSPACE_ID = builder.CreateString(self.WORKSPACE_ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.OWNER_EPM_CID is not None:
            OWNER_EPM_CID = builder.CreateString(self.OWNER_EPM_CID)
        if self.APP_ID is not None:
            APP_ID = builder.CreateString(self.APP_ID)
        if self.SCENE is not None:
            SCENE = self.SCENE.Pack(builder)
        if self.QUERIES is not None:
            QUERIESlist = []
            for i in range(len(self.QUERIES)):
                QUERIESlist.append(self.QUERIES[i].Pack(builder))
            WKSStartQUERIESVector(builder, len(self.QUERIES))
            for i in reversed(range(len(self.QUERIES))):
                builder.PrependUOffsetTRelative(QUERIESlist[i])
            QUERIES = builder.EndVector()
        if self.SHARE_GRANTS is not None:
            SHARE_GRANTSlist = []
            for i in range(len(self.SHARE_GRANTS)):
                SHARE_GRANTSlist.append(self.SHARE_GRANTS[i].Pack(builder))
            WKSStartSHARE_GRANTSVector(builder, len(self.SHARE_GRANTS))
            for i in reversed(range(len(self.SHARE_GRANTS))):
                builder.PrependUOffsetTRelative(SHARE_GRANTSlist[i])
            SHARE_GRANTS = builder.EndVector()
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                WKSStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        WKSStart(builder)
        if self.WORKSPACE_ID is not None:
            WKSAddWORKSPACE_ID(builder, WORKSPACE_ID)
        if self.NAME is not None:
            WKSAddNAME(builder, NAME)
        if self.DESCRIPTION is not None:
            WKSAddDESCRIPTION(builder, DESCRIPTION)
        if self.OWNER_EPM_CID is not None:
            WKSAddOWNER_EPM_CID(builder, OWNER_EPM_CID)
        if self.APP_ID is not None:
            WKSAddAPP_ID(builder, APP_ID)
        if self.SCENE is not None:
            WKSAddSCENE(builder, SCENE)
        if self.QUERIES is not None:
            WKSAddQUERIES(builder, QUERIES)
        if self.SHARE_GRANTS is not None:
            WKSAddSHARE_GRANTS(builder, SHARE_GRANTS)
        WKSAddCREATED_AT(builder, self.CREATED_AT)
        WKSAddUPDATED_AT(builder, self.UPDATED_AT)
        if self.SIGNATURE is not None:
            WKSAddSIGNATURE(builder, SIGNATURE)
        WKS = WKSEnd(builder)
        return WKS
