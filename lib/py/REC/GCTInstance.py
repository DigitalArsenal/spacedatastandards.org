# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One placement of a variant in the tile frame. The transform applied to a
# variant-frame point p is: scale by (SCALE_X, SCALE_Y, SCALE_Z), mirror per
# SYMMETRY, rotate by YAW_RAD about +Z, translate by (X_M, Y_M, Z_M). The
# aligned-binary INSTANCES section carries these fields in this order as
# float32.
class GCTInstance(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = GCTInstance()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsGCTInstance(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def GCTInstanceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x47\x43\x54", size_prefixed=size_prefixed)

    # GCTInstance
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Index into GCT.VARIANTS.
    # GCTInstance
    def VARIANT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Position of the variant's origin in the tile frame, metres east.
    # GCTInstance
    def X_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Metres north.
    # GCTInstance
    def Y_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Metres up (above the tile's ENU origin).
    # GCTInstance
    def Z_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Scale along the variant frame's axes; 1 is the variant as authored.
    # GCTInstance
    def SCALE_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 1.0

    # GCTInstance
    def SCALE_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 1.0

    # GCTInstance
    def SCALE_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 1.0

    # Rotation about +Z, radians, counter-clockwise from east.
    # GCTInstance
    def YAW_RAD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # One of eight axis mirror symmetries applied before the yaw: bit 0
    # mirrors x, bit 1 mirrors y, bit 2 swaps x and y.
    # GCTInstance
    def SYMMETRY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Strength of the consumer's per-instance erosion and warp in [0, 1] (0:
    # the variant exactly as authored). Values above 2 select a shared deck
    # gap field with hole fraction VALUE - 2, per the producer's convention
    # stated in PRODUCER_NOTES.
    # GCTInstance
    def EROSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Producer-stable identity of the cloud this instance belongs to, equal
    # across tiles, levels and valid times while the cloud persists — the
    # key a consumer fades and tracks by. 0 means unstated.
    # GCTInstance
    def CLOUD_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Cluster the instance belongs to (a chain of cumulus, a tower), for
    # grouping; 0 means none.
    # GCTInstance
    def CLUSTER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Shape error, metres: the silhouette distance between this placement
    # and the cloud it stands in for. 0 means the instance IS the cloud (a
    # unique variant); a class stand-in placed for a chain of five cumulus
    # carries half the member spacing. A consumer that refines below the
    # shape error requests the unique variant.
    # GCTInstance
    def SHAPE_ERROR_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # Width of the cloud as placed, metres, for size-dependent decisions.
    # GCTInstance
    def WIDTH_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

def GCTInstanceStart(builder):
    builder.StartObject(14)

def Start(builder):
    GCTInstanceStart(builder)

def GCTInstanceAddVARIANT(builder, VARIANT):
    builder.PrependUint32Slot(0, VARIANT, 0)

def AddVARIANT(builder, VARIANT):
    GCTInstanceAddVARIANT(builder, VARIANT)

def GCTInstanceAddX_M(builder, X_M):
    builder.PrependFloat32Slot(1, X_M, 0.0)

def AddX_M(builder, X_M):
    GCTInstanceAddX_M(builder, X_M)

def GCTInstanceAddY_M(builder, Y_M):
    builder.PrependFloat32Slot(2, Y_M, 0.0)

def AddY_M(builder, Y_M):
    GCTInstanceAddY_M(builder, Y_M)

def GCTInstanceAddZ_M(builder, Z_M):
    builder.PrependFloat32Slot(3, Z_M, 0.0)

def AddZ_M(builder, Z_M):
    GCTInstanceAddZ_M(builder, Z_M)

def GCTInstanceAddSCALE_X(builder, SCALE_X):
    builder.PrependFloat32Slot(4, SCALE_X, 1.0)

def AddSCALE_X(builder, SCALE_X):
    GCTInstanceAddSCALE_X(builder, SCALE_X)

def GCTInstanceAddSCALE_Y(builder, SCALE_Y):
    builder.PrependFloat32Slot(5, SCALE_Y, 1.0)

def AddSCALE_Y(builder, SCALE_Y):
    GCTInstanceAddSCALE_Y(builder, SCALE_Y)

def GCTInstanceAddSCALE_Z(builder, SCALE_Z):
    builder.PrependFloat32Slot(6, SCALE_Z, 1.0)

def AddSCALE_Z(builder, SCALE_Z):
    GCTInstanceAddSCALE_Z(builder, SCALE_Z)

def GCTInstanceAddYAW_RAD(builder, YAW_RAD):
    builder.PrependFloat32Slot(7, YAW_RAD, 0.0)

def AddYAW_RAD(builder, YAW_RAD):
    GCTInstanceAddYAW_RAD(builder, YAW_RAD)

def GCTInstanceAddSYMMETRY(builder, SYMMETRY):
    builder.PrependUint8Slot(8, SYMMETRY, 0)

def AddSYMMETRY(builder, SYMMETRY):
    GCTInstanceAddSYMMETRY(builder, SYMMETRY)

def GCTInstanceAddEROSION(builder, EROSION):
    builder.PrependFloat32Slot(9, EROSION, 0.0)

def AddEROSION(builder, EROSION):
    GCTInstanceAddEROSION(builder, EROSION)

def GCTInstanceAddCLOUD_ID(builder, CLOUD_ID):
    builder.PrependUint64Slot(10, CLOUD_ID, 0)

def AddCLOUD_ID(builder, CLOUD_ID):
    GCTInstanceAddCLOUD_ID(builder, CLOUD_ID)

def GCTInstanceAddCLUSTER_ID(builder, CLUSTER_ID):
    builder.PrependUint32Slot(11, CLUSTER_ID, 0)

def AddCLUSTER_ID(builder, CLUSTER_ID):
    GCTInstanceAddCLUSTER_ID(builder, CLUSTER_ID)

def GCTInstanceAddSHAPE_ERROR_M(builder, SHAPE_ERROR_M):
    builder.PrependFloat32Slot(12, SHAPE_ERROR_M, 0.0)

def AddSHAPE_ERROR_M(builder, SHAPE_ERROR_M):
    GCTInstanceAddSHAPE_ERROR_M(builder, SHAPE_ERROR_M)

def GCTInstanceAddWIDTH_M(builder, WIDTH_M):
    builder.PrependFloat32Slot(13, WIDTH_M, 0.0)

def AddWIDTH_M(builder, WIDTH_M):
    GCTInstanceAddWIDTH_M(builder, WIDTH_M)

def GCTInstanceEnd(builder):
    return builder.EndObject()

def End(builder):
    return GCTInstanceEnd(builder)


class GCTInstanceT(object):

    # GCTInstanceT
    def __init__(
        self,
        VARIANT = 0,
        X_M = 0.0,
        Y_M = 0.0,
        Z_M = 0.0,
        SCALE_X = 1.0,
        SCALE_Y = 1.0,
        SCALE_Z = 1.0,
        YAW_RAD = 0.0,
        SYMMETRY = 0,
        EROSION = 0.0,
        CLOUD_ID = 0,
        CLUSTER_ID = 0,
        SHAPE_ERROR_M = 0.0,
        WIDTH_M = 0.0,
    ):
        self.VARIANT = VARIANT  # type: int
        self.X_M = X_M  # type: float
        self.Y_M = Y_M  # type: float
        self.Z_M = Z_M  # type: float
        self.SCALE_X = SCALE_X  # type: float
        self.SCALE_Y = SCALE_Y  # type: float
        self.SCALE_Z = SCALE_Z  # type: float
        self.YAW_RAD = YAW_RAD  # type: float
        self.SYMMETRY = SYMMETRY  # type: int
        self.EROSION = EROSION  # type: float
        self.CLOUD_ID = CLOUD_ID  # type: int
        self.CLUSTER_ID = CLUSTER_ID  # type: int
        self.SHAPE_ERROR_M = SHAPE_ERROR_M  # type: float
        self.WIDTH_M = WIDTH_M  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpGctinstance = GCTInstance()
        tmpGctinstance.Init(buf, pos)
        return cls.InitFromObj(tmpGctinstance)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpGctinstance):
        x = GCTInstanceT()
        x._UnPack(tmpGctinstance)
        return x

    # GCTInstanceT
    def _UnPack(self, GCTInstance):
        if GCTInstance is None:
            return
        self.VARIANT = GCTInstance.VARIANT()
        self.X_M = GCTInstance.X_M()
        self.Y_M = GCTInstance.Y_M()
        self.Z_M = GCTInstance.Z_M()
        self.SCALE_X = GCTInstance.SCALE_X()
        self.SCALE_Y = GCTInstance.SCALE_Y()
        self.SCALE_Z = GCTInstance.SCALE_Z()
        self.YAW_RAD = GCTInstance.YAW_RAD()
        self.SYMMETRY = GCTInstance.SYMMETRY()
        self.EROSION = GCTInstance.EROSION()
        self.CLOUD_ID = GCTInstance.CLOUD_ID()
        self.CLUSTER_ID = GCTInstance.CLUSTER_ID()
        self.SHAPE_ERROR_M = GCTInstance.SHAPE_ERROR_M()
        self.WIDTH_M = GCTInstance.WIDTH_M()

    # GCTInstanceT
    def Pack(self, builder):
        GCTInstanceStart(builder)
        GCTInstanceAddVARIANT(builder, self.VARIANT)
        GCTInstanceAddX_M(builder, self.X_M)
        GCTInstanceAddY_M(builder, self.Y_M)
        GCTInstanceAddZ_M(builder, self.Z_M)
        GCTInstanceAddSCALE_X(builder, self.SCALE_X)
        GCTInstanceAddSCALE_Y(builder, self.SCALE_Y)
        GCTInstanceAddSCALE_Z(builder, self.SCALE_Z)
        GCTInstanceAddYAW_RAD(builder, self.YAW_RAD)
        GCTInstanceAddSYMMETRY(builder, self.SYMMETRY)
        GCTInstanceAddEROSION(builder, self.EROSION)
        GCTInstanceAddCLOUD_ID(builder, self.CLOUD_ID)
        GCTInstanceAddCLUSTER_ID(builder, self.CLUSTER_ID)
        GCTInstanceAddSHAPE_ERROR_M(builder, self.SHAPE_ERROR_M)
        GCTInstanceAddWIDTH_M(builder, self.WIDTH_M)
        GCTInstance = GCTInstanceEnd(builder)
        return GCTInstance
