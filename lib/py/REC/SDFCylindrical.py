# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Cylindrical SDF parameters.
class SDFCylindrical(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDFCylindrical()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDFCylindrical(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFCylindricalBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDFCylindrical
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDFCylindrical
    def RADIUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SDFCylindrical
    def HALF_HEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SDFCylindricalStart(builder):
    builder.StartObject(2)

def Start(builder):
    SDFCylindricalStart(builder)

def SDFCylindricalAddRADIUS(builder, RADIUS):
    builder.PrependFloat64Slot(0, RADIUS, 0.0)

def AddRADIUS(builder, RADIUS):
    SDFCylindricalAddRADIUS(builder, RADIUS)

def SDFCylindricalAddHALF_HEIGHT(builder, HALF_HEIGHT):
    builder.PrependFloat64Slot(1, HALF_HEIGHT, 0.0)

def AddHALF_HEIGHT(builder, HALF_HEIGHT):
    SDFCylindricalAddHALF_HEIGHT(builder, HALF_HEIGHT)

def SDFCylindricalEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFCylindricalEnd(builder)


class SDFCylindricalT(object):

    # SDFCylindricalT
    def __init__(
        self,
        RADIUS = 0.0,
        HALF_HEIGHT = 0.0,
    ):
        self.RADIUS = RADIUS  # type: float
        self.HALF_HEIGHT = HALF_HEIGHT  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfcylindrical = SDFCylindrical()
        tmpSdfcylindrical.Init(buf, pos)
        return cls.InitFromObj(tmpSdfcylindrical)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfcylindrical):
        x = SDFCylindricalT()
        x._UnPack(tmpSdfcylindrical)
        return x

    # SDFCylindricalT
    def _UnPack(self, SDFCylindrical):
        if SDFCylindrical is None:
            return
        self.RADIUS = SDFCylindrical.RADIUS()
        self.HALF_HEIGHT = SDFCylindrical.HALF_HEIGHT()

    # SDFCylindricalT
    def Pack(self, builder):
        SDFCylindricalStart(builder)
        SDFCylindricalAddRADIUS(builder, self.RADIUS)
        SDFCylindricalAddHALF_HEIGHT(builder, self.HALF_HEIGHT)
        SDFCylindrical = SDFCylindricalEnd(builder)
        return SDFCylindrical
