# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One UI page of the app. Exactly one of the two delivery mechanisms must
# be populated: inline CONTENT (with ENCODING describing its string form),
# or MODULE_ID plus URL for a page served by a member module. Inline pages
# must be fully self-contained — CSS and JS inlined, all assets embedded
# as data URIs, zero external requests.
class APPUIPage(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = APPUIPage()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAPPUIPage(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def APPUIPageBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x50\x50", size_prefixed=size_prefixed)

    # APPUIPage
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # App-local stable reference for this page. Required, unique within the
    # manifest.
    # APPUIPage
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Display title. Falls back to the app NAME when empty.
    # APPUIPage
    def TITLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable summary. Falls back to the app DESCRIPTION when empty.
    # APPUIPage
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Launcher icon identifier or inline data URI.
    # APPUIPage
    def ICON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Launcher accent color, CSS color syntax.
    # APPUIPage
    def COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Launcher text color, CSS color syntax.
    # APPUIPage
    def TEXT_COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Inlined, self-contained page in the string form declared by ENCODING.
    # Empty when the page is module-served via MODULE_ID and URL.
    # APPUIPage
    def CONTENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # String form of CONTENT.
    # APPUIPage
    def ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # IANA media type of the decoded page bytes, for example text/html.
    # APPUIPage
    def MEDIA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # 64 lowercase hexadecimal characters encoding SHA-256 of the decoded
    # page bytes, so a launcher can verify inline content after decoding.
    # APPUIPage
    def CONTENT_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # True for the page the launcher opens first. Exactly one page in an
    # app must set this when UI is present.
    # APPUIPage
    def ENTRY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # When module-served, must equal an APPModuleRef.ID in the same
    # manifest — the member module that serves this page.
    # APPUIPage
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # When module-served, the path or entrypoint the module serves the page
    # at.
    # APPUIPage
    def URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def APPUIPageStart(builder):
    builder.StartObject(13)

def Start(builder):
    APPUIPageStart(builder)

def APPUIPageAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    APPUIPageAddID(builder, ID)

def APPUIPageAddTITLE(builder, TITLE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(TITLE), 0)

def AddTITLE(builder, TITLE):
    APPUIPageAddTITLE(builder, TITLE)

def APPUIPageAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    APPUIPageAddDESCRIPTION(builder, DESCRIPTION)

def APPUIPageAddICON(builder, ICON):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ICON), 0)

def AddICON(builder, ICON):
    APPUIPageAddICON(builder, ICON)

def APPUIPageAddCOLOR(builder, COLOR):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(COLOR), 0)

def AddCOLOR(builder, COLOR):
    APPUIPageAddCOLOR(builder, COLOR)

def APPUIPageAddTEXT_COLOR(builder, TEXT_COLOR):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(TEXT_COLOR), 0)

def AddTEXT_COLOR(builder, TEXT_COLOR):
    APPUIPageAddTEXT_COLOR(builder, TEXT_COLOR)

def APPUIPageAddCONTENT(builder, CONTENT):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT), 0)

def AddCONTENT(builder, CONTENT):
    APPUIPageAddCONTENT(builder, CONTENT)

def APPUIPageAddENCODING(builder, ENCODING):
    builder.PrependUint8Slot(7, ENCODING, 0)

def AddENCODING(builder, ENCODING):
    APPUIPageAddENCODING(builder, ENCODING)

def APPUIPageAddMEDIA_TYPE(builder, MEDIA_TYPE):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(MEDIA_TYPE), 0)

def AddMEDIA_TYPE(builder, MEDIA_TYPE):
    APPUIPageAddMEDIA_TYPE(builder, MEDIA_TYPE)

def APPUIPageAddCONTENT_SHA256(builder, CONTENT_SHA256):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT_SHA256), 0)

def AddCONTENT_SHA256(builder, CONTENT_SHA256):
    APPUIPageAddCONTENT_SHA256(builder, CONTENT_SHA256)

def APPUIPageAddENTRY(builder, ENTRY):
    builder.PrependBoolSlot(10, ENTRY, 0)

def AddENTRY(builder, ENTRY):
    APPUIPageAddENTRY(builder, ENTRY)

def APPUIPageAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    APPUIPageAddMODULE_ID(builder, MODULE_ID)

def APPUIPageAddURL(builder, URL):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(URL), 0)

def AddURL(builder, URL):
    APPUIPageAddURL(builder, URL)

def APPUIPageEnd(builder):
    return builder.EndObject()

def End(builder):
    return APPUIPageEnd(builder)


class APPUIPageT(object):

    # APPUIPageT
    def __init__(
        self,
        ID = None,
        TITLE = None,
        DESCRIPTION = None,
        ICON = None,
        COLOR = None,
        TEXT_COLOR = None,
        CONTENT = None,
        ENCODING = 0,
        MEDIA_TYPE = None,
        CONTENT_SHA256 = None,
        ENTRY = False,
        MODULE_ID = None,
        URL = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.TITLE = TITLE  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.ICON = ICON  # type: Optional[str]
        self.COLOR = COLOR  # type: Optional[str]
        self.TEXT_COLOR = TEXT_COLOR  # type: Optional[str]
        self.CONTENT = CONTENT  # type: Optional[str]
        self.ENCODING = ENCODING  # type: int
        self.MEDIA_TYPE = MEDIA_TYPE  # type: Optional[str]
        self.CONTENT_SHA256 = CONTENT_SHA256  # type: Optional[str]
        self.ENTRY = ENTRY  # type: bool
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.URL = URL  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAppuipage = APPUIPage()
        tmpAppuipage.Init(buf, pos)
        return cls.InitFromObj(tmpAppuipage)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAppuipage):
        x = APPUIPageT()
        x._UnPack(tmpAppuipage)
        return x

    # APPUIPageT
    def _UnPack(self, APPUIPage):
        if APPUIPage is None:
            return
        self.ID = APPUIPage.ID()
        self.TITLE = APPUIPage.TITLE()
        self.DESCRIPTION = APPUIPage.DESCRIPTION()
        self.ICON = APPUIPage.ICON()
        self.COLOR = APPUIPage.COLOR()
        self.TEXT_COLOR = APPUIPage.TEXT_COLOR()
        self.CONTENT = APPUIPage.CONTENT()
        self.ENCODING = APPUIPage.ENCODING()
        self.MEDIA_TYPE = APPUIPage.MEDIA_TYPE()
        self.CONTENT_SHA256 = APPUIPage.CONTENT_SHA256()
        self.ENTRY = APPUIPage.ENTRY()
        self.MODULE_ID = APPUIPage.MODULE_ID()
        self.URL = APPUIPage.URL()

    # APPUIPageT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.TITLE is not None:
            TITLE = builder.CreateString(self.TITLE)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.ICON is not None:
            ICON = builder.CreateString(self.ICON)
        if self.COLOR is not None:
            COLOR = builder.CreateString(self.COLOR)
        if self.TEXT_COLOR is not None:
            TEXT_COLOR = builder.CreateString(self.TEXT_COLOR)
        if self.CONTENT is not None:
            CONTENT = builder.CreateString(self.CONTENT)
        if self.MEDIA_TYPE is not None:
            MEDIA_TYPE = builder.CreateString(self.MEDIA_TYPE)
        if self.CONTENT_SHA256 is not None:
            CONTENT_SHA256 = builder.CreateString(self.CONTENT_SHA256)
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.URL is not None:
            URL = builder.CreateString(self.URL)
        APPUIPageStart(builder)
        if self.ID is not None:
            APPUIPageAddID(builder, ID)
        if self.TITLE is not None:
            APPUIPageAddTITLE(builder, TITLE)
        if self.DESCRIPTION is not None:
            APPUIPageAddDESCRIPTION(builder, DESCRIPTION)
        if self.ICON is not None:
            APPUIPageAddICON(builder, ICON)
        if self.COLOR is not None:
            APPUIPageAddCOLOR(builder, COLOR)
        if self.TEXT_COLOR is not None:
            APPUIPageAddTEXT_COLOR(builder, TEXT_COLOR)
        if self.CONTENT is not None:
            APPUIPageAddCONTENT(builder, CONTENT)
        APPUIPageAddENCODING(builder, self.ENCODING)
        if self.MEDIA_TYPE is not None:
            APPUIPageAddMEDIA_TYPE(builder, MEDIA_TYPE)
        if self.CONTENT_SHA256 is not None:
            APPUIPageAddCONTENT_SHA256(builder, CONTENT_SHA256)
        APPUIPageAddENTRY(builder, self.ENTRY)
        if self.MODULE_ID is not None:
            APPUIPageAddMODULE_ID(builder, MODULE_ID)
        if self.URL is not None:
            APPUIPageAddURL(builder, URL)
        APPUIPage = APPUIPageEnd(builder)
        return APPUIPage
