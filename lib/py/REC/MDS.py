# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Mission Design Solution Set — a set of candidate trajectories produced by
# solving a mission design broad search, typically a Pareto front over
# competing objectives (total delta-V against total flight time).
#
# SDS had no multi-candidate container of any kind before this record. The
# closest existing carrier for a single planned burn is OCM.Maneuver, whose
# payload is untyped DATA:[string] with MAN_UNITS:[string] and which has no
# v-infinity, turn angle, flyby periapsis radius, or C3.
class MDS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MDS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMDS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def MDSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4D\x44\x53", size_prefixed=size_prefixed)

    # MDS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier for this solution set.
    # MDS
    def SOLUTION_SET_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PROBLEM_ID of the $MDP this set solves. A solution set whose problem
    # definition is unknown is not reproducible.
    # MDS
    def PROBLEM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Objectives the front is expressed over, e.g.
    # ["TOTAL_DELTA_V_KM_S", "TOTAL_FLIGHT_TIME_DAYS"]. Names MUST be field
    # names of MDSCandidate.
    # MDS
    def OBJECTIVES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # MDS
    def OBJECTIVESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MDS
    def OBJECTIVESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # The candidates.
    # MDS
    def CANDIDATES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from MDSCandidate import MDSCandidate
            obj = MDSCandidate()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MDS
    def CANDIDATESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MDS
    def CANDIDATESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Solver identity and version that produced the set.
    # MDS
    def SOLVER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # MDS
    def SOLVER_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Ephemeris source used, e.g. a SPICE kernel set identifier.
    # MDS
    def EPHEMERIS_SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # When the set was generated, ISO 8601.
    # MDS
    def CREATION_DATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Free-form notes.
    # MDS
    def COMMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def MDSStart(builder):
    builder.StartObject(9)

def Start(builder):
    MDSStart(builder)

def MDSAddSOLUTION_SET_ID(builder, SOLUTION_SET_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SOLUTION_SET_ID), 0)

def AddSOLUTION_SET_ID(builder, SOLUTION_SET_ID):
    MDSAddSOLUTION_SET_ID(builder, SOLUTION_SET_ID)

def MDSAddPROBLEM_ID(builder, PROBLEM_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PROBLEM_ID), 0)

def AddPROBLEM_ID(builder, PROBLEM_ID):
    MDSAddPROBLEM_ID(builder, PROBLEM_ID)

def MDSAddOBJECTIVES(builder, OBJECTIVES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECTIVES), 0)

def AddOBJECTIVES(builder, OBJECTIVES):
    MDSAddOBJECTIVES(builder, OBJECTIVES)

def MDSStartOBJECTIVESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartOBJECTIVESVector(builder, numElems):
    return MDSStartOBJECTIVESVector(builder, numElems)

def MDSCreateOBJECTIVESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateOBJECTIVESVector(builder, data):
    MDSCreateOBJECTIVESVector(builder, data)

def MDSAddCANDIDATES(builder, CANDIDATES):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CANDIDATES), 0)

def AddCANDIDATES(builder, CANDIDATES):
    MDSAddCANDIDATES(builder, CANDIDATES)

def MDSStartCANDIDATESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCANDIDATESVector(builder, numElems):
    return MDSStartCANDIDATESVector(builder, numElems)

def MDSCreateCANDIDATESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCANDIDATESVector(builder, data):
    MDSCreateCANDIDATESVector(builder, data)

def MDSAddSOLVER(builder, SOLVER):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SOLVER), 0)

def AddSOLVER(builder, SOLVER):
    MDSAddSOLVER(builder, SOLVER)

def MDSAddSOLVER_VERSION(builder, SOLVER_VERSION):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SOLVER_VERSION), 0)

def AddSOLVER_VERSION(builder, SOLVER_VERSION):
    MDSAddSOLVER_VERSION(builder, SOLVER_VERSION)

def MDSAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(EPHEMERIS_SOURCE), 0)

def AddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    MDSAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)

def MDSAddCREATION_DATE(builder, CREATION_DATE):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(CREATION_DATE), 0)

def AddCREATION_DATE(builder, CREATION_DATE):
    MDSAddCREATION_DATE(builder, CREATION_DATE)

def MDSAddCOMMENT(builder, COMMENT):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(COMMENT), 0)

def AddCOMMENT(builder, COMMENT):
    MDSAddCOMMENT(builder, COMMENT)

def MDSEnd(builder):
    return builder.EndObject()

def End(builder):
    return MDSEnd(builder)

import MDSCandidate
try:
    from typing import List
except:
    pass

class MDST(object):

    # MDST
    def __init__(
        self,
        SOLUTION_SET_ID = None,
        PROBLEM_ID = None,
        OBJECTIVES = None,
        CANDIDATES = None,
        SOLVER = None,
        SOLVER_VERSION = None,
        EPHEMERIS_SOURCE = None,
        CREATION_DATE = None,
        COMMENT = None,
    ):
        self.SOLUTION_SET_ID = SOLUTION_SET_ID  # type: Optional[str]
        self.PROBLEM_ID = PROBLEM_ID  # type: Optional[str]
        self.OBJECTIVES = OBJECTIVES  # type: Optional[List[Optional[str]]]
        self.CANDIDATES = CANDIDATES  # type: Optional[List[MDSCandidate.MDSCandidateT]]
        self.SOLVER = SOLVER  # type: Optional[str]
        self.SOLVER_VERSION = SOLVER_VERSION  # type: Optional[str]
        self.EPHEMERIS_SOURCE = EPHEMERIS_SOURCE  # type: Optional[str]
        self.CREATION_DATE = CREATION_DATE  # type: Optional[str]
        self.COMMENT = COMMENT  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpMds = MDS()
        tmpMds.Init(buf, pos)
        return cls.InitFromObj(tmpMds)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpMds):
        x = MDST()
        x._UnPack(tmpMds)
        return x

    # MDST
    def _UnPack(self, MDS):
        if MDS is None:
            return
        self.SOLUTION_SET_ID = MDS.SOLUTION_SET_ID()
        self.PROBLEM_ID = MDS.PROBLEM_ID()
        if not MDS.OBJECTIVESIsNone():
            self.OBJECTIVES = []
            for i in range(MDS.OBJECTIVESLength()):
                self.OBJECTIVES.append(MDS.OBJECTIVES(i))
        if not MDS.CANDIDATESIsNone():
            self.CANDIDATES = []
            for i in range(MDS.CANDIDATESLength()):
                if MDS.CANDIDATES(i) is None:
                    self.CANDIDATES.append(None)
                else:
                    mDSCandidate_ = MDSCandidate.MDSCandidateT.InitFromObj(MDS.CANDIDATES(i))
                    self.CANDIDATES.append(mDSCandidate_)
        self.SOLVER = MDS.SOLVER()
        self.SOLVER_VERSION = MDS.SOLVER_VERSION()
        self.EPHEMERIS_SOURCE = MDS.EPHEMERIS_SOURCE()
        self.CREATION_DATE = MDS.CREATION_DATE()
        self.COMMENT = MDS.COMMENT()

    # MDST
    def Pack(self, builder):
        if self.SOLUTION_SET_ID is not None:
            SOLUTION_SET_ID = builder.CreateString(self.SOLUTION_SET_ID)
        if self.PROBLEM_ID is not None:
            PROBLEM_ID = builder.CreateString(self.PROBLEM_ID)
        if self.OBJECTIVES is not None:
            OBJECTIVESlist = []
            for i in range(len(self.OBJECTIVES)):
                OBJECTIVESlist.append(builder.CreateString(self.OBJECTIVES[i]))
            MDSStartOBJECTIVESVector(builder, len(self.OBJECTIVES))
            for i in reversed(range(len(self.OBJECTIVES))):
                builder.PrependUOffsetTRelative(OBJECTIVESlist[i])
            OBJECTIVES = builder.EndVector()
        if self.CANDIDATES is not None:
            CANDIDATESlist = []
            for i in range(len(self.CANDIDATES)):
                CANDIDATESlist.append(self.CANDIDATES[i].Pack(builder))
            MDSStartCANDIDATESVector(builder, len(self.CANDIDATES))
            for i in reversed(range(len(self.CANDIDATES))):
                builder.PrependUOffsetTRelative(CANDIDATESlist[i])
            CANDIDATES = builder.EndVector()
        if self.SOLVER is not None:
            SOLVER = builder.CreateString(self.SOLVER)
        if self.SOLVER_VERSION is not None:
            SOLVER_VERSION = builder.CreateString(self.SOLVER_VERSION)
        if self.EPHEMERIS_SOURCE is not None:
            EPHEMERIS_SOURCE = builder.CreateString(self.EPHEMERIS_SOURCE)
        if self.CREATION_DATE is not None:
            CREATION_DATE = builder.CreateString(self.CREATION_DATE)
        if self.COMMENT is not None:
            COMMENT = builder.CreateString(self.COMMENT)
        MDSStart(builder)
        if self.SOLUTION_SET_ID is not None:
            MDSAddSOLUTION_SET_ID(builder, SOLUTION_SET_ID)
        if self.PROBLEM_ID is not None:
            MDSAddPROBLEM_ID(builder, PROBLEM_ID)
        if self.OBJECTIVES is not None:
            MDSAddOBJECTIVES(builder, OBJECTIVES)
        if self.CANDIDATES is not None:
            MDSAddCANDIDATES(builder, CANDIDATES)
        if self.SOLVER is not None:
            MDSAddSOLVER(builder, SOLVER)
        if self.SOLVER_VERSION is not None:
            MDSAddSOLVER_VERSION(builder, SOLVER_VERSION)
        if self.EPHEMERIS_SOURCE is not None:
            MDSAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)
        if self.CREATION_DATE is not None:
            MDSAddCREATION_DATE(builder, CREATION_DATE)
        if self.COMMENT is not None:
            MDSAddCOMMENT(builder, COMMENT)
        MDS = MDSEnd(builder)
        return MDS
