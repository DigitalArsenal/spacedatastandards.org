# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One aggregated quantity for the parent record's key.
#
# A metric is a DISTRIBUTION, not a scalar. There is deliberately no
# `VALUE: double` on this table: a source that publishes only a median
# encodes exactly one CNPStatistic with STATISTIC MEDIAN, and a consumer can
# then never mistake it for a mean.
class CNPMetric(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CNPMetric()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCNPMetric(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CNPMetricBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4E\x50", size_prefixed=size_prefixed)

    # CNPMetric
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Quantity aggregated.
    # CNPMetric
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 10

    # The source's own name for this metric, verbatim. REQUIRED to be
    # non-empty when KIND is OTHER; recommended always, because it is what
    # makes the mapping auditable.
    # CNPMetric
    def SOURCE_METRIC_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Units of every VALUE under STATISTICS. NORMATIVE and required: "Mbps",
    # "kbps", "ms", "percent", "fraction", "count". Units are never silently
    # converted on ingest; a source publishing kbps encodes kbps.
    # CNPMetric
    def UNITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The distribution. At least one entry; a metric with none is malformed.
    # CNPMetric
    def STATISTICS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from CNPStatistic import CNPStatistic
            obj = CNPStatistic()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CNPMetric
    def STATISTICSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CNPMetric
    def STATISTICSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Number of individual measurements aggregated. 0 means the source did
    # not publish a sample count — it never means zero measurements.
    # CNPMetric
    def SAMPLE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Number of distinct clients, terminals or probes contributing. 0 means
    # unpublished.
    # CNPMetric
    def CLIENT_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Where this metric came from and under what terms.
    # CNPMetric
    def PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CNPProvenance import CNPProvenance
            obj = CNPProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def CNPMetricStart(builder):
    builder.StartObject(7)

def Start(builder):
    CNPMetricStart(builder)

def CNPMetricAddKIND(builder, KIND):
    builder.PrependInt8Slot(0, KIND, 10)

def AddKIND(builder, KIND):
    CNPMetricAddKIND(builder, KIND)

def CNPMetricAddSOURCE_METRIC_NAME(builder, SOURCE_METRIC_NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_METRIC_NAME), 0)

def AddSOURCE_METRIC_NAME(builder, SOURCE_METRIC_NAME):
    CNPMetricAddSOURCE_METRIC_NAME(builder, SOURCE_METRIC_NAME)

def CNPMetricAddUNITS(builder, UNITS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(UNITS), 0)

def AddUNITS(builder, UNITS):
    CNPMetricAddUNITS(builder, UNITS)

def CNPMetricAddSTATISTICS(builder, STATISTICS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(STATISTICS), 0)

def AddSTATISTICS(builder, STATISTICS):
    CNPMetricAddSTATISTICS(builder, STATISTICS)

def CNPMetricStartSTATISTICSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSTATISTICSVector(builder, numElems):
    return CNPMetricStartSTATISTICSVector(builder, numElems)

def CNPMetricCreateSTATISTICSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSTATISTICSVector(builder, data):
    CNPMetricCreateSTATISTICSVector(builder, data)

def CNPMetricAddSAMPLE_COUNT(builder, SAMPLE_COUNT):
    builder.PrependUint64Slot(4, SAMPLE_COUNT, 0)

def AddSAMPLE_COUNT(builder, SAMPLE_COUNT):
    CNPMetricAddSAMPLE_COUNT(builder, SAMPLE_COUNT)

def CNPMetricAddCLIENT_COUNT(builder, CLIENT_COUNT):
    builder.PrependUint64Slot(5, CLIENT_COUNT, 0)

def AddCLIENT_COUNT(builder, CLIENT_COUNT):
    CNPMetricAddCLIENT_COUNT(builder, CLIENT_COUNT)

def CNPMetricAddPROVENANCE(builder, PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PROVENANCE), 0)

def AddPROVENANCE(builder, PROVENANCE):
    CNPMetricAddPROVENANCE(builder, PROVENANCE)

def CNPMetricEnd(builder):
    return builder.EndObject()

def End(builder):
    return CNPMetricEnd(builder)

import CNPProvenance
import CNPStatistic
try:
    from typing import List, Optional
except:
    pass

class CNPMetricT(object):

    # CNPMetricT
    def __init__(
        self,
        KIND = 10,
        SOURCE_METRIC_NAME = None,
        UNITS = None,
        STATISTICS = None,
        SAMPLE_COUNT = 0,
        CLIENT_COUNT = 0,
        PROVENANCE = None,
    ):
        self.KIND = KIND  # type: int
        self.SOURCE_METRIC_NAME = SOURCE_METRIC_NAME  # type: Optional[str]
        self.UNITS = UNITS  # type: Optional[str]
        self.STATISTICS = STATISTICS  # type: Optional[List[CNPStatistic.CNPStatisticT]]
        self.SAMPLE_COUNT = SAMPLE_COUNT  # type: int
        self.CLIENT_COUNT = CLIENT_COUNT  # type: int
        self.PROVENANCE = PROVENANCE  # type: Optional[CNPProvenance.CNPProvenanceT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCnpmetric = CNPMetric()
        tmpCnpmetric.Init(buf, pos)
        return cls.InitFromObj(tmpCnpmetric)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCnpmetric):
        x = CNPMetricT()
        x._UnPack(tmpCnpmetric)
        return x

    # CNPMetricT
    def _UnPack(self, CNPMetric):
        if CNPMetric is None:
            return
        self.KIND = CNPMetric.KIND()
        self.SOURCE_METRIC_NAME = CNPMetric.SOURCE_METRIC_NAME()
        self.UNITS = CNPMetric.UNITS()
        if not CNPMetric.STATISTICSIsNone():
            self.STATISTICS = []
            for i in range(CNPMetric.STATISTICSLength()):
                if CNPMetric.STATISTICS(i) is None:
                    self.STATISTICS.append(None)
                else:
                    cNPStatistic_ = CNPStatistic.CNPStatisticT.InitFromObj(CNPMetric.STATISTICS(i))
                    self.STATISTICS.append(cNPStatistic_)
        self.SAMPLE_COUNT = CNPMetric.SAMPLE_COUNT()
        self.CLIENT_COUNT = CNPMetric.CLIENT_COUNT()
        if CNPMetric.PROVENANCE() is not None:
            self.PROVENANCE = CNPProvenance.CNPProvenanceT.InitFromObj(CNPMetric.PROVENANCE())

    # CNPMetricT
    def Pack(self, builder):
        if self.SOURCE_METRIC_NAME is not None:
            SOURCE_METRIC_NAME = builder.CreateString(self.SOURCE_METRIC_NAME)
        if self.UNITS is not None:
            UNITS = builder.CreateString(self.UNITS)
        if self.STATISTICS is not None:
            STATISTICSlist = []
            for i in range(len(self.STATISTICS)):
                STATISTICSlist.append(self.STATISTICS[i].Pack(builder))
            CNPMetricStartSTATISTICSVector(builder, len(self.STATISTICS))
            for i in reversed(range(len(self.STATISTICS))):
                builder.PrependUOffsetTRelative(STATISTICSlist[i])
            STATISTICS = builder.EndVector()
        if self.PROVENANCE is not None:
            PROVENANCE = self.PROVENANCE.Pack(builder)
        CNPMetricStart(builder)
        CNPMetricAddKIND(builder, self.KIND)
        if self.SOURCE_METRIC_NAME is not None:
            CNPMetricAddSOURCE_METRIC_NAME(builder, SOURCE_METRIC_NAME)
        if self.UNITS is not None:
            CNPMetricAddUNITS(builder, UNITS)
        if self.STATISTICS is not None:
            CNPMetricAddSTATISTICS(builder, STATISTICS)
        CNPMetricAddSAMPLE_COUNT(builder, self.SAMPLE_COUNT)
        CNPMetricAddCLIENT_COUNT(builder, self.CLIENT_COUNT)
        if self.PROVENANCE is not None:
            CNPMetricAddPROVENANCE(builder, PROVENANCE)
        CNPMetric = CNPMetricEnd(builder)
        return CNPMetric
