# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Runtime counters for one installed module or flow.
class NDSModuleStat(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NDSModuleStat()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNDSModuleStat(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NDSModuleStatBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x44\x53", size_prefixed=size_prefixed)

    # NDSModuleStat
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # NDSModuleStat
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Runtime state, e.g. "running", "installed", "error".
    # NDSModuleStat
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # NDSModuleStat
    def RUN_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # NDSModuleStat
    def ERROR_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # Unix seconds; 0 = never.
    # NDSModuleStat
    def LAST_RUN_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # Unix seconds; 0 = not scheduled.
    # NDSModuleStat
    def NEXT_RUN_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

def NDSModuleStatStart(builder):
    builder.StartObject(6)

def Start(builder):
    NDSModuleStatStart(builder)

def NDSModuleStatAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    NDSModuleStatAddMODULE_ID(builder, MODULE_ID)

def NDSModuleStatAddSTATUS(builder, STATUS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(STATUS), 0)

def AddSTATUS(builder, STATUS):
    NDSModuleStatAddSTATUS(builder, STATUS)

def NDSModuleStatAddRUN_COUNT(builder, RUN_COUNT):
    builder.PrependInt64Slot(2, RUN_COUNT, 0)

def AddRUN_COUNT(builder, RUN_COUNT):
    NDSModuleStatAddRUN_COUNT(builder, RUN_COUNT)

def NDSModuleStatAddERROR_COUNT(builder, ERROR_COUNT):
    builder.PrependInt64Slot(3, ERROR_COUNT, 0)

def AddERROR_COUNT(builder, ERROR_COUNT):
    NDSModuleStatAddERROR_COUNT(builder, ERROR_COUNT)

def NDSModuleStatAddLAST_RUN_AT(builder, LAST_RUN_AT):
    builder.PrependInt64Slot(4, LAST_RUN_AT, 0)

def AddLAST_RUN_AT(builder, LAST_RUN_AT):
    NDSModuleStatAddLAST_RUN_AT(builder, LAST_RUN_AT)

def NDSModuleStatAddNEXT_RUN_AT(builder, NEXT_RUN_AT):
    builder.PrependInt64Slot(5, NEXT_RUN_AT, 0)

def AddNEXT_RUN_AT(builder, NEXT_RUN_AT):
    NDSModuleStatAddNEXT_RUN_AT(builder, NEXT_RUN_AT)

def NDSModuleStatEnd(builder):
    return builder.EndObject()

def End(builder):
    return NDSModuleStatEnd(builder)


class NDSModuleStatT(object):

    # NDSModuleStatT
    def __init__(
        self,
        MODULE_ID = None,
        STATUS = None,
        RUN_COUNT = 0,
        ERROR_COUNT = 0,
        LAST_RUN_AT = 0,
        NEXT_RUN_AT = 0,
    ):
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.STATUS = STATUS  # type: Optional[str]
        self.RUN_COUNT = RUN_COUNT  # type: int
        self.ERROR_COUNT = ERROR_COUNT  # type: int
        self.LAST_RUN_AT = LAST_RUN_AT  # type: int
        self.NEXT_RUN_AT = NEXT_RUN_AT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNdsmoduleStat = NDSModuleStat()
        tmpNdsmoduleStat.Init(buf, pos)
        return cls.InitFromObj(tmpNdsmoduleStat)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNdsmoduleStat):
        x = NDSModuleStatT()
        x._UnPack(tmpNdsmoduleStat)
        return x

    # NDSModuleStatT
    def _UnPack(self, NDSModuleStat):
        if NDSModuleStat is None:
            return
        self.MODULE_ID = NDSModuleStat.MODULE_ID()
        self.STATUS = NDSModuleStat.STATUS()
        self.RUN_COUNT = NDSModuleStat.RUN_COUNT()
        self.ERROR_COUNT = NDSModuleStat.ERROR_COUNT()
        self.LAST_RUN_AT = NDSModuleStat.LAST_RUN_AT()
        self.NEXT_RUN_AT = NDSModuleStat.NEXT_RUN_AT()

    # NDSModuleStatT
    def Pack(self, builder):
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.STATUS is not None:
            STATUS = builder.CreateString(self.STATUS)
        NDSModuleStatStart(builder)
        if self.MODULE_ID is not None:
            NDSModuleStatAddMODULE_ID(builder, MODULE_ID)
        if self.STATUS is not None:
            NDSModuleStatAddSTATUS(builder, STATUS)
        NDSModuleStatAddRUN_COUNT(builder, self.RUN_COUNT)
        NDSModuleStatAddERROR_COUNT(builder, self.ERROR_COUNT)
        NDSModuleStatAddLAST_RUN_AT(builder, self.LAST_RUN_AT)
        NDSModuleStatAddNEXT_RUN_AT(builder, self.NEXT_RUN_AT)
        NDSModuleStat = NDSModuleStatEnd(builder)
        return NDSModuleStat
