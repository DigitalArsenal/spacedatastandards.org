# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PRWDescribeRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWDescribeRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWDescribeRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWDescribeRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWDescribeRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWDescribeRequest
    def INSTANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWInstance import PRWInstance
            obj = PRWInstance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWDescribeRequest
    def SEGMENT_SET_HANDLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # PRWDescribeRequest
    def SOURCE_HANDLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # PRWDescribeRequest
    def SOURCE_HANDLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # PRWDescribeRequest
    def SOURCE_HANDLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWDescribeRequest
    def SOURCE_HANDLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def PRWDescribeRequestStart(builder):
    builder.StartObject(3)

def Start(builder):
    PRWDescribeRequestStart(builder)

def PRWDescribeRequestAddINSTANCE(builder, INSTANCE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(INSTANCE), 0)

def AddINSTANCE(builder, INSTANCE):
    PRWDescribeRequestAddINSTANCE(builder, INSTANCE)

def PRWDescribeRequestAddSEGMENT_SET_HANDLE(builder, SEGMENT_SET_HANDLE):
    builder.PrependUint32Slot(1, SEGMENT_SET_HANDLE, 0)

def AddSEGMENT_SET_HANDLE(builder, SEGMENT_SET_HANDLE):
    PRWDescribeRequestAddSEGMENT_SET_HANDLE(builder, SEGMENT_SET_HANDLE)

def PRWDescribeRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_HANDLES), 0)

def AddSOURCE_HANDLES(builder, SOURCE_HANDLES):
    PRWDescribeRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES)

def PRWDescribeRequestStartSOURCE_HANDLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCE_HANDLESVector(builder, numElems):
    return PRWDescribeRequestStartSOURCE_HANDLESVector(builder, numElems)

def PRWDescribeRequestCreateSOURCE_HANDLESVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateSOURCE_HANDLESVector(builder, data):
    PRWDescribeRequestCreateSOURCE_HANDLESVector(builder, data)

def PRWDescribeRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWDescribeRequestEnd(builder)

import PRWInstance
try:
    from typing import List, Optional
except:
    pass

class PRWDescribeRequestT(object):

    # PRWDescribeRequestT
    def __init__(
        self,
        INSTANCE = None,
        SEGMENT_SET_HANDLE = 0,
        SOURCE_HANDLES = None,
    ):
        self.INSTANCE = INSTANCE  # type: Optional[PRWInstance.PRWInstanceT]
        self.SEGMENT_SET_HANDLE = SEGMENT_SET_HANDLE  # type: int
        self.SOURCE_HANDLES = SOURCE_HANDLES  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwdescribeRequest = PRWDescribeRequest()
        tmpPrwdescribeRequest.Init(buf, pos)
        return cls.InitFromObj(tmpPrwdescribeRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwdescribeRequest):
        x = PRWDescribeRequestT()
        x._UnPack(tmpPrwdescribeRequest)
        return x

    # PRWDescribeRequestT
    def _UnPack(self, PRWDescribeRequest):
        if PRWDescribeRequest is None:
            return
        if PRWDescribeRequest.INSTANCE() is not None:
            self.INSTANCE = PRWInstance.PRWInstanceT.InitFromObj(PRWDescribeRequest.INSTANCE())
        self.SEGMENT_SET_HANDLE = PRWDescribeRequest.SEGMENT_SET_HANDLE()
        if not PRWDescribeRequest.SOURCE_HANDLESIsNone():
            if np is None:
                self.SOURCE_HANDLES = []
                for i in range(PRWDescribeRequest.SOURCE_HANDLESLength()):
                    self.SOURCE_HANDLES.append(PRWDescribeRequest.SOURCE_HANDLES(i))
            else:
                self.SOURCE_HANDLES = PRWDescribeRequest.SOURCE_HANDLESAsNumpy()

    # PRWDescribeRequestT
    def Pack(self, builder):
        if self.INSTANCE is not None:
            INSTANCE = self.INSTANCE.Pack(builder)
        if self.SOURCE_HANDLES is not None:
            if np is not None and type(self.SOURCE_HANDLES) is np.ndarray:
                SOURCE_HANDLES = builder.CreateNumpyVector(self.SOURCE_HANDLES)
            else:
                PRWDescribeRequestStartSOURCE_HANDLESVector(builder, len(self.SOURCE_HANDLES))
                for i in reversed(range(len(self.SOURCE_HANDLES))):
                    builder.PrependUint32(self.SOURCE_HANDLES[i])
                SOURCE_HANDLES = builder.EndVector()
        PRWDescribeRequestStart(builder)
        if self.INSTANCE is not None:
            PRWDescribeRequestAddINSTANCE(builder, INSTANCE)
        PRWDescribeRequestAddSEGMENT_SET_HANDLE(builder, self.SEGMENT_SET_HANDLE)
        if self.SOURCE_HANDLES is not None:
            PRWDescribeRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES)
        PRWDescribeRequest = PRWDescribeRequestEnd(builder)
        return PRWDescribeRequest
