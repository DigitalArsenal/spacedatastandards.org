# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# The publisher's attestation over this profile, in BOTH ratified forms.
#
# PRESENCE IS THE STATEMENT. A profile is SIGNED if and only if this table
# is present; a profile whose `ATTESTATION` is absent is UNSIGNED, and a
# consumer marks it as such. There is deliberately no boolean saying so: a
# self-asserted "signed" flag is attacker-controlled and proves nothing,
# while presence of the table is checkable.
#
# FAIL CLOSED. When this table is present, every field below MUST be
# present and BOTH signatures MUST verify independently against
# `SIGNING_PUBLIC_KEY`. A profile whose attestation is incomplete, or
# whose either signature fails, is REJECTED — it is NEVER downgraded to
# "unsigned and therefore acceptable", which would let stripping one
# signature launder a tampered profile past an importer that admits
# unsigned input.
class BPFAttestation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BPFAttestation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBPFAttestation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BPFAttestationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x50\x46", size_prefixed=size_prefixed)

    # BPFAttestation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # The signer's LITERAL Ed25519 public key, 64 lowercase hexadecimal
    # characters. A derivation ancestor is structurally absent from this
    # standard: publishing one lets any holder of a child key walk the
    # signer's whole key tree. Rotation is republication.
    # BPFAttestation
    def SIGNING_PUBLIC_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
    # at which the attestation was produced.
    # BPFAttestation
    def SIGNED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Ed25519 signature over the size-prefixed FlatBuffer with both 64-byte
    # signature payloads zeroed while preserving their vectors and offsets.
    # BPFAttestation
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # BPFAttestation
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # BPFAttestation
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BPFAttestation
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Ed25519 signature over canonical JSON with IDL field order and
    # capitalization, no insignificant whitespace, and both signature fields
    # omitted.
    # BPFAttestation
    def CANONICAL_JSON_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # BPFAttestation
    def CANONICAL_JSON_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # BPFAttestation
    def CANONICAL_JSON_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BPFAttestation
    def CANONICAL_JSON_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def BPFAttestationStart(builder):
    builder.StartObject(4)

def Start(builder):
    BPFAttestationStart(builder)

def BPFAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNING_PUBLIC_KEY), 0)

def AddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    BPFAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)

def BPFAttestationAddSIGNED_AT(builder, SIGNED_AT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNED_AT), 0)

def AddSIGNED_AT(builder, SIGNED_AT):
    BPFAttestationAddSIGNED_AT(builder, SIGNED_AT)

def BPFAttestationAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    BPFAttestationAddSIGNATURE(builder, SIGNATURE)

def BPFAttestationStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return BPFAttestationStartSIGNATUREVector(builder, numElems)

def BPFAttestationCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    BPFAttestationCreateSIGNATUREVector(builder, data)

def BPFAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CANONICAL_JSON_SIGNATURE), 0)

def AddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE):
    BPFAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE)

def BPFAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCANONICAL_JSON_SIGNATUREVector(builder, numElems):
    return BPFAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, numElems)

def BPFAttestationCreateCANONICAL_JSON_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCANONICAL_JSON_SIGNATUREVector(builder, data):
    BPFAttestationCreateCANONICAL_JSON_SIGNATUREVector(builder, data)

def BPFAttestationEnd(builder):
    return builder.EndObject()

def End(builder):
    return BPFAttestationEnd(builder)

try:
    from typing import List
except:
    pass

class BPFAttestationT(object):

    # BPFAttestationT
    def __init__(
        self,
        SIGNING_PUBLIC_KEY = None,
        SIGNED_AT = None,
        SIGNATURE = None,
        CANONICAL_JSON_SIGNATURE = None,
    ):
        self.SIGNING_PUBLIC_KEY = SIGNING_PUBLIC_KEY  # type: Optional[str]
        self.SIGNED_AT = SIGNED_AT  # type: Optional[str]
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]
        self.CANONICAL_JSON_SIGNATURE = CANONICAL_JSON_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpBpfattestation = BPFAttestation()
        tmpBpfattestation.Init(buf, pos)
        return cls.InitFromObj(tmpBpfattestation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpBpfattestation):
        x = BPFAttestationT()
        x._UnPack(tmpBpfattestation)
        return x

    # BPFAttestationT
    def _UnPack(self, BPFAttestation):
        if BPFAttestation is None:
            return
        self.SIGNING_PUBLIC_KEY = BPFAttestation.SIGNING_PUBLIC_KEY()
        self.SIGNED_AT = BPFAttestation.SIGNED_AT()
        if not BPFAttestation.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(BPFAttestation.SIGNATURELength()):
                    self.SIGNATURE.append(BPFAttestation.SIGNATURE(i))
            else:
                self.SIGNATURE = BPFAttestation.SIGNATUREAsNumpy()
        if not BPFAttestation.CANONICAL_JSON_SIGNATUREIsNone():
            if np is None:
                self.CANONICAL_JSON_SIGNATURE = []
                for i in range(BPFAttestation.CANONICAL_JSON_SIGNATURELength()):
                    self.CANONICAL_JSON_SIGNATURE.append(BPFAttestation.CANONICAL_JSON_SIGNATURE(i))
            else:
                self.CANONICAL_JSON_SIGNATURE = BPFAttestation.CANONICAL_JSON_SIGNATUREAsNumpy()

    # BPFAttestationT
    def Pack(self, builder):
        if self.SIGNING_PUBLIC_KEY is not None:
            SIGNING_PUBLIC_KEY = builder.CreateString(self.SIGNING_PUBLIC_KEY)
        if self.SIGNED_AT is not None:
            SIGNED_AT = builder.CreateString(self.SIGNED_AT)
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                BPFAttestationStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        if self.CANONICAL_JSON_SIGNATURE is not None:
            if np is not None and type(self.CANONICAL_JSON_SIGNATURE) is np.ndarray:
                CANONICAL_JSON_SIGNATURE = builder.CreateNumpyVector(self.CANONICAL_JSON_SIGNATURE)
            else:
                BPFAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, len(self.CANONICAL_JSON_SIGNATURE))
                for i in reversed(range(len(self.CANONICAL_JSON_SIGNATURE))):
                    builder.PrependUint8(self.CANONICAL_JSON_SIGNATURE[i])
                CANONICAL_JSON_SIGNATURE = builder.EndVector()
        BPFAttestationStart(builder)
        if self.SIGNING_PUBLIC_KEY is not None:
            BPFAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)
        if self.SIGNED_AT is not None:
            BPFAttestationAddSIGNED_AT(builder, SIGNED_AT)
        if self.SIGNATURE is not None:
            BPFAttestationAddSIGNATURE(builder, SIGNATURE)
        if self.CANONICAL_JSON_SIGNATURE is not None:
            BPFAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE)
        BPFAttestation = BPFAttestationEnd(builder)
        return BPFAttestation
