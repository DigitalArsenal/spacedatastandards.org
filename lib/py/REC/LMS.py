# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Lambert boundary-value solve request.
class LMS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LMS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLMS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def LMSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4C\x4D\x53", size_prefixed=size_prefixed)

    # LMS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Caller-supplied request identifier for result correlation.
    # LMS
    def REQUEST_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Initial position X component (km).
    # LMS
    def R1_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Initial position Y component (km).
    # LMS
    def R1_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Initial position Z component (km).
    # LMS
    def R1_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Final position X component (km).
    # LMS
    def R2_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Final position Y component (km).
    # LMS
    def R2_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Final position Z component (km).
    # LMS
    def R2_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Time of flight (seconds). Must be greater than zero.
    # LMS
    def TOF_SEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Central-body gravitational parameter (km^3/s^2). Must be greater than zero.
    # LMS
    def MU_KM3_S2(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Short-way or long-way transfer selection.
    # LMS
    def TRANSFER_WAY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Maximum complete revolutions to search. Zero means single-revolution only.
    # LMS
    def MAX_REVS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Inertial reference frame label for both position vectors (for example GCRF, EME2000, ICRF).
    # LMS
    def REF_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Optional epoch/time-scale label for provenance. The solver uses TOF, not epoch, internally.
    # LMS
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Request flags reserved for future compatibility.
    # LMS
    def FLAGS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

def LMSStart(builder):
    builder.StartObject(14)

def Start(builder):
    LMSStart(builder)

def LMSAddREQUEST_ID(builder, REQUEST_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_ID), 0)

def AddREQUEST_ID(builder, REQUEST_ID):
    LMSAddREQUEST_ID(builder, REQUEST_ID)

def LMSAddR1_X(builder, R1_X):
    builder.PrependFloat64Slot(1, R1_X, 0.0)

def AddR1_X(builder, R1_X):
    LMSAddR1_X(builder, R1_X)

def LMSAddR1_Y(builder, R1_Y):
    builder.PrependFloat64Slot(2, R1_Y, 0.0)

def AddR1_Y(builder, R1_Y):
    LMSAddR1_Y(builder, R1_Y)

def LMSAddR1_Z(builder, R1_Z):
    builder.PrependFloat64Slot(3, R1_Z, 0.0)

def AddR1_Z(builder, R1_Z):
    LMSAddR1_Z(builder, R1_Z)

def LMSAddR2_X(builder, R2_X):
    builder.PrependFloat64Slot(4, R2_X, 0.0)

def AddR2_X(builder, R2_X):
    LMSAddR2_X(builder, R2_X)

def LMSAddR2_Y(builder, R2_Y):
    builder.PrependFloat64Slot(5, R2_Y, 0.0)

def AddR2_Y(builder, R2_Y):
    LMSAddR2_Y(builder, R2_Y)

def LMSAddR2_Z(builder, R2_Z):
    builder.PrependFloat64Slot(6, R2_Z, 0.0)

def AddR2_Z(builder, R2_Z):
    LMSAddR2_Z(builder, R2_Z)

def LMSAddTOF_SEC(builder, TOF_SEC):
    builder.PrependFloat64Slot(7, TOF_SEC, 0.0)

def AddTOF_SEC(builder, TOF_SEC):
    LMSAddTOF_SEC(builder, TOF_SEC)

def LMSAddMU_KM3_S2(builder, MU_KM3_S2):
    builder.PrependFloat64Slot(8, MU_KM3_S2, 0.0)

def AddMU_KM3_S2(builder, MU_KM3_S2):
    LMSAddMU_KM3_S2(builder, MU_KM3_S2)

def LMSAddTRANSFER_WAY(builder, TRANSFER_WAY):
    builder.PrependInt8Slot(9, TRANSFER_WAY, 0)

def AddTRANSFER_WAY(builder, TRANSFER_WAY):
    LMSAddTRANSFER_WAY(builder, TRANSFER_WAY)

def LMSAddMAX_REVS(builder, MAX_REVS):
    builder.PrependUint16Slot(10, MAX_REVS, 0)

def AddMAX_REVS(builder, MAX_REVS):
    LMSAddMAX_REVS(builder, MAX_REVS)

def LMSAddREF_FRAME(builder, REF_FRAME):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(REF_FRAME), 0)

def AddREF_FRAME(builder, REF_FRAME):
    LMSAddREF_FRAME(builder, REF_FRAME)

def LMSAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    LMSAddEPOCH(builder, EPOCH)

def LMSAddFLAGS(builder, FLAGS):
    builder.PrependUint16Slot(13, FLAGS, 0)

def AddFLAGS(builder, FLAGS):
    LMSAddFLAGS(builder, FLAGS)

def LMSEnd(builder):
    return builder.EndObject()

def End(builder):
    return LMSEnd(builder)


class LMST(object):

    # LMST
    def __init__(
        self,
        REQUEST_ID = None,
        R1_X = 0.0,
        R1_Y = 0.0,
        R1_Z = 0.0,
        R2_X = 0.0,
        R2_Y = 0.0,
        R2_Z = 0.0,
        TOF_SEC = 0.0,
        MU_KM3_S2 = 0.0,
        TRANSFER_WAY = 0,
        MAX_REVS = 0,
        REF_FRAME = None,
        EPOCH = None,
        FLAGS = 0,
    ):
        self.REQUEST_ID = REQUEST_ID  # type: Optional[str]
        self.R1_X = R1_X  # type: float
        self.R1_Y = R1_Y  # type: float
        self.R1_Z = R1_Z  # type: float
        self.R2_X = R2_X  # type: float
        self.R2_Y = R2_Y  # type: float
        self.R2_Z = R2_Z  # type: float
        self.TOF_SEC = TOF_SEC  # type: float
        self.MU_KM3_S2 = MU_KM3_S2  # type: float
        self.TRANSFER_WAY = TRANSFER_WAY  # type: int
        self.MAX_REVS = MAX_REVS  # type: int
        self.REF_FRAME = REF_FRAME  # type: Optional[str]
        self.EPOCH = EPOCH  # type: Optional[str]
        self.FLAGS = FLAGS  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpLms = LMS()
        tmpLms.Init(buf, pos)
        return cls.InitFromObj(tmpLms)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpLms):
        x = LMST()
        x._UnPack(tmpLms)
        return x

    # LMST
    def _UnPack(self, LMS):
        if LMS is None:
            return
        self.REQUEST_ID = LMS.REQUEST_ID()
        self.R1_X = LMS.R1_X()
        self.R1_Y = LMS.R1_Y()
        self.R1_Z = LMS.R1_Z()
        self.R2_X = LMS.R2_X()
        self.R2_Y = LMS.R2_Y()
        self.R2_Z = LMS.R2_Z()
        self.TOF_SEC = LMS.TOF_SEC()
        self.MU_KM3_S2 = LMS.MU_KM3_S2()
        self.TRANSFER_WAY = LMS.TRANSFER_WAY()
        self.MAX_REVS = LMS.MAX_REVS()
        self.REF_FRAME = LMS.REF_FRAME()
        self.EPOCH = LMS.EPOCH()
        self.FLAGS = LMS.FLAGS()

    # LMST
    def Pack(self, builder):
        if self.REQUEST_ID is not None:
            REQUEST_ID = builder.CreateString(self.REQUEST_ID)
        if self.REF_FRAME is not None:
            REF_FRAME = builder.CreateString(self.REF_FRAME)
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        LMSStart(builder)
        if self.REQUEST_ID is not None:
            LMSAddREQUEST_ID(builder, REQUEST_ID)
        LMSAddR1_X(builder, self.R1_X)
        LMSAddR1_Y(builder, self.R1_Y)
        LMSAddR1_Z(builder, self.R1_Z)
        LMSAddR2_X(builder, self.R2_X)
        LMSAddR2_Y(builder, self.R2_Y)
        LMSAddR2_Z(builder, self.R2_Z)
        LMSAddTOF_SEC(builder, self.TOF_SEC)
        LMSAddMU_KM3_S2(builder, self.MU_KM3_S2)
        LMSAddTRANSFER_WAY(builder, self.TRANSFER_WAY)
        LMSAddMAX_REVS(builder, self.MAX_REVS)
        if self.REF_FRAME is not None:
            LMSAddREF_FRAME(builder, REF_FRAME)
        if self.EPOCH is not None:
            LMSAddEPOCH(builder, EPOCH)
        LMSAddFLAGS(builder, self.FLAGS)
        LMS = LMSEnd(builder)
        return LMS
