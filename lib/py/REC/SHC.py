# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Spherical-Harmonic Coefficient Set — an actual gravity field, as opposed to
# the NAME of one.
#
# This is deliberately NOT $GRV. $GRV is a model SELECTOR: it names a model,
# bounds its degree/order, toggles third-body and tidal effects, and carries
# scalar J2..J6. It cannot carry a coefficient set, and a named model without
# its coefficients is not interchange — it is a citation.
#
# Coefficients are carried as four parallel arrays of equal length, indexed
# together: entry i is the coefficient at DEGREES[i], ORDERS[i] with cosine
# term C[i] and sine term S[i]. Parallel arrays rather than a table per
# coefficient because a 200x200 field is ~40,000 coefficients and a
# table-per-coefficient encoding is pathological in both size and decode cost.
class SHC(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SHC()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSHC(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SHCBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x48\x43", size_prefixed=size_prefixed)

    # SHC
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Identifier of the coefficient set, e.g. "EGM2008", "GGGRX1200".
    # SHC
    def MODEL_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Body the field describes, as a NAIF integer ID (399 = Earth, 301 = Moon,
    # 499 = Mars). NAIF ID is normative; BODY_NAME is advisory. Note that a
    # barycenter (e.g. 4) is NOT the body (e.g. 499).
    # SHC
    def CENTRAL_BODY_NAIF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Advisory body name.
    # SHC
    def BODY_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Gravitational parameter of the body, m^3/s^2. Part of the set: a field's
    # coefficients are only meaningful with the GM they were solved against.
    # SHC
    def GM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Reference radius of the expansion, metres. Also part of the set.
    # SHC
    def REFERENCE_RADIUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum degree and order present in the arrays.
    # SHC
    def MAX_DEGREE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # SHC
    def MAX_ORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Normalization convention. A set published without this is unusable;
    # consumers MUST reject UNSPECIFIED rather than assuming a convention.
    # SHC
    def NORMALIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Permanent-tide system. Consumers MUST reject UNSPECIFIED for any
    # application sensitive at the 1e-9 level in C20.
    # SHC
    def PERMANENT_TIDE_SYSTEM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Epoch the static coefficients are referenced to, ISO 8601. Required when
    # VARIABLE_TERMS is present.
    # SHC
    def REFERENCE_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Parallel coefficient arrays. DEGREES, ORDERS, C and S MUST have identical
    # lengths; a record where they do not is invalid.
    # SHC
    def DEGREES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 2))
        return 0

    # SHC
    def DEGREESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint16Flags, o)
        return 0

    # SHC
    def DEGREESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHC
    def DEGREESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # SHC
    def ORDERS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 2))
        return 0

    # SHC
    def ORDERSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint16Flags, o)
        return 0

    # SHC
    def ORDERSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHC
    def ORDERSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # SHC
    def C(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SHC
    def CAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SHC
    def CLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHC
    def CIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

    # SHC
    def S(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SHC
    def SAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SHC
    def SLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHC
    def SIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        return o == 0

    # Optional 1-sigma uncertainties, parallel to C and S when present.
    # SHC
    def C_SIGMA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SHC
    def C_SIGMAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SHC
    def C_SIGMALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHC
    def C_SIGMAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

    # SHC
    def S_SIGMA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SHC
    def S_SIGMAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SHC
    def S_SIGMALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHC
    def S_SIGMAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        return o == 0

    # Optional time-variable terms applied on top of the static field.
    # SHC
    def VARIABLE_TERMS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SHCVariableTerm import SHCVariableTerm
            obj = SHCVariableTerm()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SHC
    def VARIABLE_TERMSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SHC
    def VARIABLE_TERMSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        return o == 0

    # Tide-free/zero-tide conversion already applied, publication reference, or
    # any other provenance needed to reproduce the set.
    # SHC
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Citation for the field, e.g. the paper or data-centre DOI.
    # SHC
    def CITATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def SHCStart(builder):
    builder.StartObject(19)

def Start(builder):
    SHCStart(builder)

def SHCAddMODEL_NAME(builder, MODEL_NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(MODEL_NAME), 0)

def AddMODEL_NAME(builder, MODEL_NAME):
    SHCAddMODEL_NAME(builder, MODEL_NAME)

def SHCAddCENTRAL_BODY_NAIF_ID(builder, CENTRAL_BODY_NAIF_ID):
    builder.PrependInt32Slot(1, CENTRAL_BODY_NAIF_ID, 0)

def AddCENTRAL_BODY_NAIF_ID(builder, CENTRAL_BODY_NAIF_ID):
    SHCAddCENTRAL_BODY_NAIF_ID(builder, CENTRAL_BODY_NAIF_ID)

def SHCAddBODY_NAME(builder, BODY_NAME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(BODY_NAME), 0)

def AddBODY_NAME(builder, BODY_NAME):
    SHCAddBODY_NAME(builder, BODY_NAME)

def SHCAddGM(builder, GM):
    builder.PrependFloat64Slot(3, GM, 0.0)

def AddGM(builder, GM):
    SHCAddGM(builder, GM)

def SHCAddREFERENCE_RADIUS(builder, REFERENCE_RADIUS):
    builder.PrependFloat64Slot(4, REFERENCE_RADIUS, 0.0)

def AddREFERENCE_RADIUS(builder, REFERENCE_RADIUS):
    SHCAddREFERENCE_RADIUS(builder, REFERENCE_RADIUS)

def SHCAddMAX_DEGREE(builder, MAX_DEGREE):
    builder.PrependUint16Slot(5, MAX_DEGREE, 0)

def AddMAX_DEGREE(builder, MAX_DEGREE):
    SHCAddMAX_DEGREE(builder, MAX_DEGREE)

def SHCAddMAX_ORDER(builder, MAX_ORDER):
    builder.PrependUint16Slot(6, MAX_ORDER, 0)

def AddMAX_ORDER(builder, MAX_ORDER):
    SHCAddMAX_ORDER(builder, MAX_ORDER)

def SHCAddNORMALIZATION(builder, NORMALIZATION):
    builder.PrependUint8Slot(7, NORMALIZATION, 0)

def AddNORMALIZATION(builder, NORMALIZATION):
    SHCAddNORMALIZATION(builder, NORMALIZATION)

def SHCAddPERMANENT_TIDE_SYSTEM(builder, PERMANENT_TIDE_SYSTEM):
    builder.PrependUint8Slot(8, PERMANENT_TIDE_SYSTEM, 0)

def AddPERMANENT_TIDE_SYSTEM(builder, PERMANENT_TIDE_SYSTEM):
    SHCAddPERMANENT_TIDE_SYSTEM(builder, PERMANENT_TIDE_SYSTEM)

def SHCAddREFERENCE_EPOCH(builder, REFERENCE_EPOCH):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(REFERENCE_EPOCH), 0)

def AddREFERENCE_EPOCH(builder, REFERENCE_EPOCH):
    SHCAddREFERENCE_EPOCH(builder, REFERENCE_EPOCH)

def SHCAddDEGREES(builder, DEGREES):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(DEGREES), 0)

def AddDEGREES(builder, DEGREES):
    SHCAddDEGREES(builder, DEGREES)

def SHCStartDEGREESVector(builder, numElems):
    return builder.StartVector(2, numElems, 2)

def StartDEGREESVector(builder, numElems):
    return SHCStartDEGREESVector(builder, numElems)

def SHCCreateDEGREESVector(builder, data):
    data = list(data)
    builder.StartVector(2, len(data), 2)
    for item in reversed(data):
        builder.PrependUint16(item)
    return builder.EndVector()

def CreateDEGREESVector(builder, data):
    SHCCreateDEGREESVector(builder, data)

def SHCAddORDERS(builder, ORDERS):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(ORDERS), 0)

def AddORDERS(builder, ORDERS):
    SHCAddORDERS(builder, ORDERS)

def SHCStartORDERSVector(builder, numElems):
    return builder.StartVector(2, numElems, 2)

def StartORDERSVector(builder, numElems):
    return SHCStartORDERSVector(builder, numElems)

def SHCCreateORDERSVector(builder, data):
    data = list(data)
    builder.StartVector(2, len(data), 2)
    for item in reversed(data):
        builder.PrependUint16(item)
    return builder.EndVector()

def CreateORDERSVector(builder, data):
    SHCCreateORDERSVector(builder, data)

def SHCAddC(builder, C):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(C), 0)

def AddC(builder, C):
    SHCAddC(builder, C)

def SHCStartCVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartCVector(builder, numElems):
    return SHCStartCVector(builder, numElems)

def SHCCreateCVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateCVector(builder, data):
    SHCCreateCVector(builder, data)

def SHCAddS(builder, S):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(S), 0)

def AddS(builder, S):
    SHCAddS(builder, S)

def SHCStartSVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSVector(builder, numElems):
    return SHCStartSVector(builder, numElems)

def SHCCreateSVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSVector(builder, data):
    SHCCreateSVector(builder, data)

def SHCAddC_SIGMA(builder, C_SIGMA):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(C_SIGMA), 0)

def AddC_SIGMA(builder, C_SIGMA):
    SHCAddC_SIGMA(builder, C_SIGMA)

def SHCStartC_SIGMAVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartC_SIGMAVector(builder, numElems):
    return SHCStartC_SIGMAVector(builder, numElems)

def SHCCreateC_SIGMAVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateC_SIGMAVector(builder, data):
    SHCCreateC_SIGMAVector(builder, data)

def SHCAddS_SIGMA(builder, S_SIGMA):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(S_SIGMA), 0)

def AddS_SIGMA(builder, S_SIGMA):
    SHCAddS_SIGMA(builder, S_SIGMA)

def SHCStartS_SIGMAVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartS_SIGMAVector(builder, numElems):
    return SHCStartS_SIGMAVector(builder, numElems)

def SHCCreateS_SIGMAVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateS_SIGMAVector(builder, data):
    SHCCreateS_SIGMAVector(builder, data)

def SHCAddVARIABLE_TERMS(builder, VARIABLE_TERMS):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(VARIABLE_TERMS), 0)

def AddVARIABLE_TERMS(builder, VARIABLE_TERMS):
    SHCAddVARIABLE_TERMS(builder, VARIABLE_TERMS)

def SHCStartVARIABLE_TERMSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartVARIABLE_TERMSVector(builder, numElems):
    return SHCStartVARIABLE_TERMSVector(builder, numElems)

def SHCCreateVARIABLE_TERMSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateVARIABLE_TERMSVector(builder, data):
    SHCCreateVARIABLE_TERMSVector(builder, data)

def SHCAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    SHCAddSOURCE(builder, SOURCE)

def SHCAddCITATION(builder, CITATION):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(CITATION), 0)

def AddCITATION(builder, CITATION):
    SHCAddCITATION(builder, CITATION)

def SHCEnd(builder):
    return builder.EndObject()

def End(builder):
    return SHCEnd(builder)

import SHCVariableTerm
try:
    from typing import List
except:
    pass

class SHCT(object):

    # SHCT
    def __init__(
        self,
        MODEL_NAME = None,
        CENTRAL_BODY_NAIF_ID = 0,
        BODY_NAME = None,
        GM = 0.0,
        REFERENCE_RADIUS = 0.0,
        MAX_DEGREE = 0,
        MAX_ORDER = 0,
        NORMALIZATION = 0,
        PERMANENT_TIDE_SYSTEM = 0,
        REFERENCE_EPOCH = None,
        DEGREES = None,
        ORDERS = None,
        C = None,
        S = None,
        C_SIGMA = None,
        S_SIGMA = None,
        VARIABLE_TERMS = None,
        SOURCE = None,
        CITATION = None,
    ):
        self.MODEL_NAME = MODEL_NAME  # type: Optional[str]
        self.CENTRAL_BODY_NAIF_ID = CENTRAL_BODY_NAIF_ID  # type: int
        self.BODY_NAME = BODY_NAME  # type: Optional[str]
        self.GM = GM  # type: float
        self.REFERENCE_RADIUS = REFERENCE_RADIUS  # type: float
        self.MAX_DEGREE = MAX_DEGREE  # type: int
        self.MAX_ORDER = MAX_ORDER  # type: int
        self.NORMALIZATION = NORMALIZATION  # type: int
        self.PERMANENT_TIDE_SYSTEM = PERMANENT_TIDE_SYSTEM  # type: int
        self.REFERENCE_EPOCH = REFERENCE_EPOCH  # type: Optional[str]
        self.DEGREES = DEGREES  # type: Optional[List[int]]
        self.ORDERS = ORDERS  # type: Optional[List[int]]
        self.C = C  # type: Optional[List[float]]
        self.S = S  # type: Optional[List[float]]
        self.C_SIGMA = C_SIGMA  # type: Optional[List[float]]
        self.S_SIGMA = S_SIGMA  # type: Optional[List[float]]
        self.VARIABLE_TERMS = VARIABLE_TERMS  # type: Optional[List[SHCVariableTerm.SHCVariableTermT]]
        self.SOURCE = SOURCE  # type: Optional[str]
        self.CITATION = CITATION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpShc = SHC()
        tmpShc.Init(buf, pos)
        return cls.InitFromObj(tmpShc)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpShc):
        x = SHCT()
        x._UnPack(tmpShc)
        return x

    # SHCT
    def _UnPack(self, SHC):
        if SHC is None:
            return
        self.MODEL_NAME = SHC.MODEL_NAME()
        self.CENTRAL_BODY_NAIF_ID = SHC.CENTRAL_BODY_NAIF_ID()
        self.BODY_NAME = SHC.BODY_NAME()
        self.GM = SHC.GM()
        self.REFERENCE_RADIUS = SHC.REFERENCE_RADIUS()
        self.MAX_DEGREE = SHC.MAX_DEGREE()
        self.MAX_ORDER = SHC.MAX_ORDER()
        self.NORMALIZATION = SHC.NORMALIZATION()
        self.PERMANENT_TIDE_SYSTEM = SHC.PERMANENT_TIDE_SYSTEM()
        self.REFERENCE_EPOCH = SHC.REFERENCE_EPOCH()
        if not SHC.DEGREESIsNone():
            if np is None:
                self.DEGREES = []
                for i in range(SHC.DEGREESLength()):
                    self.DEGREES.append(SHC.DEGREES(i))
            else:
                self.DEGREES = SHC.DEGREESAsNumpy()
        if not SHC.ORDERSIsNone():
            if np is None:
                self.ORDERS = []
                for i in range(SHC.ORDERSLength()):
                    self.ORDERS.append(SHC.ORDERS(i))
            else:
                self.ORDERS = SHC.ORDERSAsNumpy()
        if not SHC.CIsNone():
            if np is None:
                self.C = []
                for i in range(SHC.CLength()):
                    self.C.append(SHC.C(i))
            else:
                self.C = SHC.CAsNumpy()
        if not SHC.SIsNone():
            if np is None:
                self.S = []
                for i in range(SHC.SLength()):
                    self.S.append(SHC.S(i))
            else:
                self.S = SHC.SAsNumpy()
        if not SHC.C_SIGMAIsNone():
            if np is None:
                self.C_SIGMA = []
                for i in range(SHC.C_SIGMALength()):
                    self.C_SIGMA.append(SHC.C_SIGMA(i))
            else:
                self.C_SIGMA = SHC.C_SIGMAAsNumpy()
        if not SHC.S_SIGMAIsNone():
            if np is None:
                self.S_SIGMA = []
                for i in range(SHC.S_SIGMALength()):
                    self.S_SIGMA.append(SHC.S_SIGMA(i))
            else:
                self.S_SIGMA = SHC.S_SIGMAAsNumpy()
        if not SHC.VARIABLE_TERMSIsNone():
            self.VARIABLE_TERMS = []
            for i in range(SHC.VARIABLE_TERMSLength()):
                if SHC.VARIABLE_TERMS(i) is None:
                    self.VARIABLE_TERMS.append(None)
                else:
                    sHCVariableTerm_ = SHCVariableTerm.SHCVariableTermT.InitFromObj(SHC.VARIABLE_TERMS(i))
                    self.VARIABLE_TERMS.append(sHCVariableTerm_)
        self.SOURCE = SHC.SOURCE()
        self.CITATION = SHC.CITATION()

    # SHCT
    def Pack(self, builder):
        if self.MODEL_NAME is not None:
            MODEL_NAME = builder.CreateString(self.MODEL_NAME)
        if self.BODY_NAME is not None:
            BODY_NAME = builder.CreateString(self.BODY_NAME)
        if self.REFERENCE_EPOCH is not None:
            REFERENCE_EPOCH = builder.CreateString(self.REFERENCE_EPOCH)
        if self.DEGREES is not None:
            if np is not None and type(self.DEGREES) is np.ndarray:
                DEGREES = builder.CreateNumpyVector(self.DEGREES)
            else:
                SHCStartDEGREESVector(builder, len(self.DEGREES))
                for i in reversed(range(len(self.DEGREES))):
                    builder.PrependUint16(self.DEGREES[i])
                DEGREES = builder.EndVector()
        if self.ORDERS is not None:
            if np is not None and type(self.ORDERS) is np.ndarray:
                ORDERS = builder.CreateNumpyVector(self.ORDERS)
            else:
                SHCStartORDERSVector(builder, len(self.ORDERS))
                for i in reversed(range(len(self.ORDERS))):
                    builder.PrependUint16(self.ORDERS[i])
                ORDERS = builder.EndVector()
        if self.C is not None:
            if np is not None and type(self.C) is np.ndarray:
                C = builder.CreateNumpyVector(self.C)
            else:
                SHCStartCVector(builder, len(self.C))
                for i in reversed(range(len(self.C))):
                    builder.PrependFloat64(self.C[i])
                C = builder.EndVector()
        if self.S is not None:
            if np is not None and type(self.S) is np.ndarray:
                S = builder.CreateNumpyVector(self.S)
            else:
                SHCStartSVector(builder, len(self.S))
                for i in reversed(range(len(self.S))):
                    builder.PrependFloat64(self.S[i])
                S = builder.EndVector()
        if self.C_SIGMA is not None:
            if np is not None and type(self.C_SIGMA) is np.ndarray:
                C_SIGMA = builder.CreateNumpyVector(self.C_SIGMA)
            else:
                SHCStartC_SIGMAVector(builder, len(self.C_SIGMA))
                for i in reversed(range(len(self.C_SIGMA))):
                    builder.PrependFloat64(self.C_SIGMA[i])
                C_SIGMA = builder.EndVector()
        if self.S_SIGMA is not None:
            if np is not None and type(self.S_SIGMA) is np.ndarray:
                S_SIGMA = builder.CreateNumpyVector(self.S_SIGMA)
            else:
                SHCStartS_SIGMAVector(builder, len(self.S_SIGMA))
                for i in reversed(range(len(self.S_SIGMA))):
                    builder.PrependFloat64(self.S_SIGMA[i])
                S_SIGMA = builder.EndVector()
        if self.VARIABLE_TERMS is not None:
            VARIABLE_TERMSlist = []
            for i in range(len(self.VARIABLE_TERMS)):
                VARIABLE_TERMSlist.append(self.VARIABLE_TERMS[i].Pack(builder))
            SHCStartVARIABLE_TERMSVector(builder, len(self.VARIABLE_TERMS))
            for i in reversed(range(len(self.VARIABLE_TERMS))):
                builder.PrependUOffsetTRelative(VARIABLE_TERMSlist[i])
            VARIABLE_TERMS = builder.EndVector()
        if self.SOURCE is not None:
            SOURCE = builder.CreateString(self.SOURCE)
        if self.CITATION is not None:
            CITATION = builder.CreateString(self.CITATION)
        SHCStart(builder)
        if self.MODEL_NAME is not None:
            SHCAddMODEL_NAME(builder, MODEL_NAME)
        SHCAddCENTRAL_BODY_NAIF_ID(builder, self.CENTRAL_BODY_NAIF_ID)
        if self.BODY_NAME is not None:
            SHCAddBODY_NAME(builder, BODY_NAME)
        SHCAddGM(builder, self.GM)
        SHCAddREFERENCE_RADIUS(builder, self.REFERENCE_RADIUS)
        SHCAddMAX_DEGREE(builder, self.MAX_DEGREE)
        SHCAddMAX_ORDER(builder, self.MAX_ORDER)
        SHCAddNORMALIZATION(builder, self.NORMALIZATION)
        SHCAddPERMANENT_TIDE_SYSTEM(builder, self.PERMANENT_TIDE_SYSTEM)
        if self.REFERENCE_EPOCH is not None:
            SHCAddREFERENCE_EPOCH(builder, REFERENCE_EPOCH)
        if self.DEGREES is not None:
            SHCAddDEGREES(builder, DEGREES)
        if self.ORDERS is not None:
            SHCAddORDERS(builder, ORDERS)
        if self.C is not None:
            SHCAddC(builder, C)
        if self.S is not None:
            SHCAddS(builder, S)
        if self.C_SIGMA is not None:
            SHCAddC_SIGMA(builder, C_SIGMA)
        if self.S_SIGMA is not None:
            SHCAddS_SIGMA(builder, S_SIGMA)
        if self.VARIABLE_TERMS is not None:
            SHCAddVARIABLE_TERMS(builder, VARIABLE_TERMS)
        if self.SOURCE is not None:
            SHCAddSOURCE(builder, SOURCE)
        if self.CITATION is not None:
            SHCAddCITATION(builder, CITATION)
        SHC = SHCEnd(builder)
        return SHC
