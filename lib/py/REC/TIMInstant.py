# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Numeric or textual instant tagged with its time system and representation.
class TIMInstant(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TIMInstant()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTIMInstant(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TIMInstantBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x49\x4D", size_prefixed=size_prefixed)

    # TIMInstant
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Time system for this instant.
    # TIMInstant
    def TIME_SYSTEM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Interpretation of JULIAN_DATE, SECONDS, and ISO8601.
    # TIMInstant
    def EPOCH_FORMAT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Julian Date or Modified Julian Date day value, selected by EPOCH_FORMAT.
    # TIMInstant
    def JULIAN_DATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Seconds for UNIX_SECONDS, GPS_SECONDS, GNSS_WEEK_SECONDS, or MISSION_ELAPSED_SECONDS.
    # TIMInstant
    def SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ISO 8601 timestamp text for ISO8601 inputs or display outputs.
    # TIMInstant
    def ISO8601(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Additional nanoseconds beyond the fractional scalar/text value.
    # TIMInstant
    def SUBSECOND_NANOS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Optional application-defined epoch label for MET, MRT, or SCLK values.
    # TIMInstant
    def EPOCH_LABEL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # GNSS week number when EPOCH_FORMAT is GNSS_WEEK_SECONDS.
    # TIMInstant
    def GNSS_WEEK(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Whether GNSS_ROLLOVER_REFERENCE_ISO8601 should be applied.
    # TIMInstant
    def HAS_GNSS_ROLLOVER_REFERENCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Optional Orekit-style GNSS week rollover reference timestamp.
    # TIMInstant
    def GNSS_ROLLOVER_REFERENCE_ISO8601(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # CCSDS time-code payload when EPOCH_FORMAT is CCSDS_TIME_CODE.
    # TIMInstant
    def CCSDS_TIME_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TIMCcsdsTimeCode import TIMCcsdsTimeCode
            obj = TIMCcsdsTimeCode()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def TIMInstantStart(builder):
    builder.StartObject(11)

def Start(builder):
    TIMInstantStart(builder)

def TIMInstantAddTIME_SYSTEM(builder, TIME_SYSTEM):
    builder.PrependInt8Slot(0, TIME_SYSTEM, 0)

def AddTIME_SYSTEM(builder, TIME_SYSTEM):
    TIMInstantAddTIME_SYSTEM(builder, TIME_SYSTEM)

def TIMInstantAddEPOCH_FORMAT(builder, EPOCH_FORMAT):
    builder.PrependInt8Slot(1, EPOCH_FORMAT, 0)

def AddEPOCH_FORMAT(builder, EPOCH_FORMAT):
    TIMInstantAddEPOCH_FORMAT(builder, EPOCH_FORMAT)

def TIMInstantAddJULIAN_DATE(builder, JULIAN_DATE):
    builder.PrependFloat64Slot(2, JULIAN_DATE, 0.0)

def AddJULIAN_DATE(builder, JULIAN_DATE):
    TIMInstantAddJULIAN_DATE(builder, JULIAN_DATE)

def TIMInstantAddSECONDS(builder, SECONDS):
    builder.PrependFloat64Slot(3, SECONDS, 0.0)

def AddSECONDS(builder, SECONDS):
    TIMInstantAddSECONDS(builder, SECONDS)

def TIMInstantAddISO8601(builder, ISO8601):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ISO8601), 0)

def AddISO8601(builder, ISO8601):
    TIMInstantAddISO8601(builder, ISO8601)

def TIMInstantAddSUBSECOND_NANOS(builder, SUBSECOND_NANOS):
    builder.PrependInt32Slot(5, SUBSECOND_NANOS, 0)

def AddSUBSECOND_NANOS(builder, SUBSECOND_NANOS):
    TIMInstantAddSUBSECOND_NANOS(builder, SUBSECOND_NANOS)

def TIMInstantAddEPOCH_LABEL(builder, EPOCH_LABEL):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH_LABEL), 0)

def AddEPOCH_LABEL(builder, EPOCH_LABEL):
    TIMInstantAddEPOCH_LABEL(builder, EPOCH_LABEL)

def TIMInstantAddGNSS_WEEK(builder, GNSS_WEEK):
    builder.PrependInt32Slot(7, GNSS_WEEK, 0)

def AddGNSS_WEEK(builder, GNSS_WEEK):
    TIMInstantAddGNSS_WEEK(builder, GNSS_WEEK)

def TIMInstantAddHAS_GNSS_ROLLOVER_REFERENCE(builder, HAS_GNSS_ROLLOVER_REFERENCE):
    builder.PrependBoolSlot(8, HAS_GNSS_ROLLOVER_REFERENCE, 0)

def AddHAS_GNSS_ROLLOVER_REFERENCE(builder, HAS_GNSS_ROLLOVER_REFERENCE):
    TIMInstantAddHAS_GNSS_ROLLOVER_REFERENCE(builder, HAS_GNSS_ROLLOVER_REFERENCE)

def TIMInstantAddGNSS_ROLLOVER_REFERENCE_ISO8601(builder, GNSS_ROLLOVER_REFERENCE_ISO8601):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(GNSS_ROLLOVER_REFERENCE_ISO8601), 0)

def AddGNSS_ROLLOVER_REFERENCE_ISO8601(builder, GNSS_ROLLOVER_REFERENCE_ISO8601):
    TIMInstantAddGNSS_ROLLOVER_REFERENCE_ISO8601(builder, GNSS_ROLLOVER_REFERENCE_ISO8601)

def TIMInstantAddCCSDS_TIME_CODE(builder, CCSDS_TIME_CODE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(CCSDS_TIME_CODE), 0)

def AddCCSDS_TIME_CODE(builder, CCSDS_TIME_CODE):
    TIMInstantAddCCSDS_TIME_CODE(builder, CCSDS_TIME_CODE)

def TIMInstantEnd(builder):
    return builder.EndObject()

def End(builder):
    return TIMInstantEnd(builder)

import TIMCcsdsTimeCode
try:
    from typing import Optional
except:
    pass

class TIMInstantT(object):

    # TIMInstantT
    def __init__(
        self,
        TIME_SYSTEM = 0,
        EPOCH_FORMAT = 0,
        JULIAN_DATE = 0.0,
        SECONDS = 0.0,
        ISO8601 = None,
        SUBSECOND_NANOS = 0,
        EPOCH_LABEL = None,
        GNSS_WEEK = 0,
        HAS_GNSS_ROLLOVER_REFERENCE = False,
        GNSS_ROLLOVER_REFERENCE_ISO8601 = None,
        CCSDS_TIME_CODE = None,
    ):
        self.TIME_SYSTEM = TIME_SYSTEM  # type: int
        self.EPOCH_FORMAT = EPOCH_FORMAT  # type: int
        self.JULIAN_DATE = JULIAN_DATE  # type: float
        self.SECONDS = SECONDS  # type: float
        self.ISO8601 = ISO8601  # type: Optional[str]
        self.SUBSECOND_NANOS = SUBSECOND_NANOS  # type: int
        self.EPOCH_LABEL = EPOCH_LABEL  # type: Optional[str]
        self.GNSS_WEEK = GNSS_WEEK  # type: int
        self.HAS_GNSS_ROLLOVER_REFERENCE = HAS_GNSS_ROLLOVER_REFERENCE  # type: bool
        self.GNSS_ROLLOVER_REFERENCE_ISO8601 = GNSS_ROLLOVER_REFERENCE_ISO8601  # type: Optional[str]
        self.CCSDS_TIME_CODE = CCSDS_TIME_CODE  # type: Optional[TIMCcsdsTimeCode.TIMCcsdsTimeCodeT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTiminstant = TIMInstant()
        tmpTiminstant.Init(buf, pos)
        return cls.InitFromObj(tmpTiminstant)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTiminstant):
        x = TIMInstantT()
        x._UnPack(tmpTiminstant)
        return x

    # TIMInstantT
    def _UnPack(self, TIMInstant):
        if TIMInstant is None:
            return
        self.TIME_SYSTEM = TIMInstant.TIME_SYSTEM()
        self.EPOCH_FORMAT = TIMInstant.EPOCH_FORMAT()
        self.JULIAN_DATE = TIMInstant.JULIAN_DATE()
        self.SECONDS = TIMInstant.SECONDS()
        self.ISO8601 = TIMInstant.ISO8601()
        self.SUBSECOND_NANOS = TIMInstant.SUBSECOND_NANOS()
        self.EPOCH_LABEL = TIMInstant.EPOCH_LABEL()
        self.GNSS_WEEK = TIMInstant.GNSS_WEEK()
        self.HAS_GNSS_ROLLOVER_REFERENCE = TIMInstant.HAS_GNSS_ROLLOVER_REFERENCE()
        self.GNSS_ROLLOVER_REFERENCE_ISO8601 = TIMInstant.GNSS_ROLLOVER_REFERENCE_ISO8601()
        if TIMInstant.CCSDS_TIME_CODE() is not None:
            self.CCSDS_TIME_CODE = TIMCcsdsTimeCode.TIMCcsdsTimeCodeT.InitFromObj(TIMInstant.CCSDS_TIME_CODE())

    # TIMInstantT
    def Pack(self, builder):
        if self.ISO8601 is not None:
            ISO8601 = builder.CreateString(self.ISO8601)
        if self.EPOCH_LABEL is not None:
            EPOCH_LABEL = builder.CreateString(self.EPOCH_LABEL)
        if self.GNSS_ROLLOVER_REFERENCE_ISO8601 is not None:
            GNSS_ROLLOVER_REFERENCE_ISO8601 = builder.CreateString(self.GNSS_ROLLOVER_REFERENCE_ISO8601)
        if self.CCSDS_TIME_CODE is not None:
            CCSDS_TIME_CODE = self.CCSDS_TIME_CODE.Pack(builder)
        TIMInstantStart(builder)
        TIMInstantAddTIME_SYSTEM(builder, self.TIME_SYSTEM)
        TIMInstantAddEPOCH_FORMAT(builder, self.EPOCH_FORMAT)
        TIMInstantAddJULIAN_DATE(builder, self.JULIAN_DATE)
        TIMInstantAddSECONDS(builder, self.SECONDS)
        if self.ISO8601 is not None:
            TIMInstantAddISO8601(builder, ISO8601)
        TIMInstantAddSUBSECOND_NANOS(builder, self.SUBSECOND_NANOS)
        if self.EPOCH_LABEL is not None:
            TIMInstantAddEPOCH_LABEL(builder, EPOCH_LABEL)
        TIMInstantAddGNSS_WEEK(builder, self.GNSS_WEEK)
        TIMInstantAddHAS_GNSS_ROLLOVER_REFERENCE(builder, self.HAS_GNSS_ROLLOVER_REFERENCE)
        if self.GNSS_ROLLOVER_REFERENCE_ISO8601 is not None:
            TIMInstantAddGNSS_ROLLOVER_REFERENCE_ISO8601(builder, GNSS_ROLLOVER_REFERENCE_ISO8601)
        if self.CCSDS_TIME_CODE is not None:
            TIMInstantAddCCSDS_TIME_CODE(builder, CCSDS_TIME_CODE)
        TIMInstant = TIMInstantEnd(builder)
        return TIMInstant
