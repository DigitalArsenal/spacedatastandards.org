# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Completeness-capable signed index over a dataset update.
class DPMCompletenessIndex(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DPMCompletenessIndex()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDPMCompletenessIndex(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DPMCompletenessIndexBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x44\x50\x4D", size_prefixed=size_prefixed)

    # DPMCompletenessIndex
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable index name, e.g. file_id, norad_cat_id, epoch, source_batch. Every
    # completeness-verifiable dataset update SHOULD include a file_id index so
    # subscribers can prove that all returned records belong to the announced
    # FILE_ID partition.
    # DPMCompletenessIndex
    def INDEX_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Deterministic ordering expression for the index. Providers and
    # subscribers MUST use this ordering when building or verifying range
    # proofs. A query is completeness-verifiable only when its predicate can be
    # expressed against one or more declared indexes.
    # DPMCompletenessIndex
    def CANONICAL_ORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SHA-256 or Merkle root of the ordered index, lowercase hex. This root is
    # signed by the DPM provider signature and is the verifier's commitment for
    # inclusion and range-completeness proofs. To verify a provider-mediated
    # response, the subscriber recomputes each returned leaf, walks the supplied
    # sibling hashes using MERKLE_PROFILE, confirms the root equals INDEX_ROOT,
    # and confirms any range-boundary proofs required by CANONICAL_ORDER.
    # DPMCompletenessIndex
    def INDEX_ROOT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Hash profile for leaves and internal nodes, e.g.
    # SDN-MERKLE-SHA256-v1. Profiles define domain separators, leaf material,
    # pair ordering, duplicate handling, and odd-leaf promotion.
    # DPMCompletenessIndex
    def MERKLE_PROFILE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Whether this index can prove that no matching records were omitted for a
    # supported range query. Inclusion proofs alone prove authenticity, not
    # completeness.
    # DPMCompletenessIndex
    def SUPPORTS_RANGE_COMPLETENESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def DPMCompletenessIndexStart(builder):
    builder.StartObject(5)

def Start(builder):
    DPMCompletenessIndexStart(builder)

def DPMCompletenessIndexAddINDEX_NAME(builder, INDEX_NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(INDEX_NAME), 0)

def AddINDEX_NAME(builder, INDEX_NAME):
    DPMCompletenessIndexAddINDEX_NAME(builder, INDEX_NAME)

def DPMCompletenessIndexAddCANONICAL_ORDER(builder, CANONICAL_ORDER):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CANONICAL_ORDER), 0)

def AddCANONICAL_ORDER(builder, CANONICAL_ORDER):
    DPMCompletenessIndexAddCANONICAL_ORDER(builder, CANONICAL_ORDER)

def DPMCompletenessIndexAddINDEX_ROOT(builder, INDEX_ROOT):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(INDEX_ROOT), 0)

def AddINDEX_ROOT(builder, INDEX_ROOT):
    DPMCompletenessIndexAddINDEX_ROOT(builder, INDEX_ROOT)

def DPMCompletenessIndexAddMERKLE_PROFILE(builder, MERKLE_PROFILE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MERKLE_PROFILE), 0)

def AddMERKLE_PROFILE(builder, MERKLE_PROFILE):
    DPMCompletenessIndexAddMERKLE_PROFILE(builder, MERKLE_PROFILE)

def DPMCompletenessIndexAddSUPPORTS_RANGE_COMPLETENESS(builder, SUPPORTS_RANGE_COMPLETENESS):
    builder.PrependBoolSlot(4, SUPPORTS_RANGE_COMPLETENESS, 0)

def AddSUPPORTS_RANGE_COMPLETENESS(builder, SUPPORTS_RANGE_COMPLETENESS):
    DPMCompletenessIndexAddSUPPORTS_RANGE_COMPLETENESS(builder, SUPPORTS_RANGE_COMPLETENESS)

def DPMCompletenessIndexEnd(builder):
    return builder.EndObject()

def End(builder):
    return DPMCompletenessIndexEnd(builder)


class DPMCompletenessIndexT(object):

    # DPMCompletenessIndexT
    def __init__(
        self,
        INDEX_NAME = None,
        CANONICAL_ORDER = None,
        INDEX_ROOT = None,
        MERKLE_PROFILE = None,
        SUPPORTS_RANGE_COMPLETENESS = False,
    ):
        self.INDEX_NAME = INDEX_NAME  # type: Optional[str]
        self.CANONICAL_ORDER = CANONICAL_ORDER  # type: Optional[str]
        self.INDEX_ROOT = INDEX_ROOT  # type: Optional[str]
        self.MERKLE_PROFILE = MERKLE_PROFILE  # type: Optional[str]
        self.SUPPORTS_RANGE_COMPLETENESS = SUPPORTS_RANGE_COMPLETENESS  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpDpmcompletenessIndex = DPMCompletenessIndex()
        tmpDpmcompletenessIndex.Init(buf, pos)
        return cls.InitFromObj(tmpDpmcompletenessIndex)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpDpmcompletenessIndex):
        x = DPMCompletenessIndexT()
        x._UnPack(tmpDpmcompletenessIndex)
        return x

    # DPMCompletenessIndexT
    def _UnPack(self, DPMCompletenessIndex):
        if DPMCompletenessIndex is None:
            return
        self.INDEX_NAME = DPMCompletenessIndex.INDEX_NAME()
        self.CANONICAL_ORDER = DPMCompletenessIndex.CANONICAL_ORDER()
        self.INDEX_ROOT = DPMCompletenessIndex.INDEX_ROOT()
        self.MERKLE_PROFILE = DPMCompletenessIndex.MERKLE_PROFILE()
        self.SUPPORTS_RANGE_COMPLETENESS = DPMCompletenessIndex.SUPPORTS_RANGE_COMPLETENESS()

    # DPMCompletenessIndexT
    def Pack(self, builder):
        if self.INDEX_NAME is not None:
            INDEX_NAME = builder.CreateString(self.INDEX_NAME)
        if self.CANONICAL_ORDER is not None:
            CANONICAL_ORDER = builder.CreateString(self.CANONICAL_ORDER)
        if self.INDEX_ROOT is not None:
            INDEX_ROOT = builder.CreateString(self.INDEX_ROOT)
        if self.MERKLE_PROFILE is not None:
            MERKLE_PROFILE = builder.CreateString(self.MERKLE_PROFILE)
        DPMCompletenessIndexStart(builder)
        if self.INDEX_NAME is not None:
            DPMCompletenessIndexAddINDEX_NAME(builder, INDEX_NAME)
        if self.CANONICAL_ORDER is not None:
            DPMCompletenessIndexAddCANONICAL_ORDER(builder, CANONICAL_ORDER)
        if self.INDEX_ROOT is not None:
            DPMCompletenessIndexAddINDEX_ROOT(builder, INDEX_ROOT)
        if self.MERKLE_PROFILE is not None:
            DPMCompletenessIndexAddMERKLE_PROFILE(builder, MERKLE_PROFILE)
        DPMCompletenessIndexAddSUPPORTS_RANGE_COMPLETENESS(builder, self.SUPPORTS_RANGE_COMPLETENESS)
        DPMCompletenessIndex = DPMCompletenessIndexEnd(builder)
        return DPMCompletenessIndex
