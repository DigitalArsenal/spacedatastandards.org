# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Generic sensor configuration.
class SDRSensorConfig(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDRSensorConfig()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDRSensorConfig(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDRSensorConfigBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x52", size_prefixed=size_prefixed)

    # SDRSensorConfig
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDRSensorConfig
    def CATEGORY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Sensor position in ECEF (meters) — X.
    # SDRSensorConfig
    def POS_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Sensor position in ECEF (meters) — Y.
    # SDRSensorConfig
    def POS_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Sensor position in ECEF (meters) — Z.
    # SDRSensorConfig
    def POS_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Sensor orientation as quaternion (W, X, Y, Z).
    # SDRSensorConfig
    def ORIENT_W(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SDRSensorConfig
    def ORIENT_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SDRSensorConfig
    def ORIENT_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SDRSensorConfig
    def ORIENT_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Radar-specific config when CATEGORY == RADAR.
    # SDRSensorConfig
    def RADAR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SDRRadarConfig import SDRRadarConfig
            obj = SDRRadarConfig()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Operational flags.
    # SDRSensorConfig
    def FLAGS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def SDRSensorConfigStart(builder):
    builder.StartObject(10)

def Start(builder):
    SDRSensorConfigStart(builder)

def SDRSensorConfigAddCATEGORY(builder, CATEGORY):
    builder.PrependUint8Slot(0, CATEGORY, 0)

def AddCATEGORY(builder, CATEGORY):
    SDRSensorConfigAddCATEGORY(builder, CATEGORY)

def SDRSensorConfigAddPOS_X(builder, POS_X):
    builder.PrependFloat64Slot(1, POS_X, 0.0)

def AddPOS_X(builder, POS_X):
    SDRSensorConfigAddPOS_X(builder, POS_X)

def SDRSensorConfigAddPOS_Y(builder, POS_Y):
    builder.PrependFloat64Slot(2, POS_Y, 0.0)

def AddPOS_Y(builder, POS_Y):
    SDRSensorConfigAddPOS_Y(builder, POS_Y)

def SDRSensorConfigAddPOS_Z(builder, POS_Z):
    builder.PrependFloat64Slot(3, POS_Z, 0.0)

def AddPOS_Z(builder, POS_Z):
    SDRSensorConfigAddPOS_Z(builder, POS_Z)

def SDRSensorConfigAddORIENT_W(builder, ORIENT_W):
    builder.PrependFloat64Slot(4, ORIENT_W, 0.0)

def AddORIENT_W(builder, ORIENT_W):
    SDRSensorConfigAddORIENT_W(builder, ORIENT_W)

def SDRSensorConfigAddORIENT_X(builder, ORIENT_X):
    builder.PrependFloat64Slot(5, ORIENT_X, 0.0)

def AddORIENT_X(builder, ORIENT_X):
    SDRSensorConfigAddORIENT_X(builder, ORIENT_X)

def SDRSensorConfigAddORIENT_Y(builder, ORIENT_Y):
    builder.PrependFloat64Slot(6, ORIENT_Y, 0.0)

def AddORIENT_Y(builder, ORIENT_Y):
    SDRSensorConfigAddORIENT_Y(builder, ORIENT_Y)

def SDRSensorConfigAddORIENT_Z(builder, ORIENT_Z):
    builder.PrependFloat64Slot(7, ORIENT_Z, 0.0)

def AddORIENT_Z(builder, ORIENT_Z):
    SDRSensorConfigAddORIENT_Z(builder, ORIENT_Z)

def SDRSensorConfigAddRADAR(builder, RADAR):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(RADAR), 0)

def AddRADAR(builder, RADAR):
    SDRSensorConfigAddRADAR(builder, RADAR)

def SDRSensorConfigAddFLAGS(builder, FLAGS):
    builder.PrependUint32Slot(9, FLAGS, 0)

def AddFLAGS(builder, FLAGS):
    SDRSensorConfigAddFLAGS(builder, FLAGS)

def SDRSensorConfigEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDRSensorConfigEnd(builder)

import SDRRadarConfig
try:
    from typing import Optional
except:
    pass

class SDRSensorConfigT(object):

    # SDRSensorConfigT
    def __init__(
        self,
        CATEGORY = 0,
        POS_X = 0.0,
        POS_Y = 0.0,
        POS_Z = 0.0,
        ORIENT_W = 0.0,
        ORIENT_X = 0.0,
        ORIENT_Y = 0.0,
        ORIENT_Z = 0.0,
        RADAR = None,
        FLAGS = 0,
    ):
        self.CATEGORY = CATEGORY  # type: int
        self.POS_X = POS_X  # type: float
        self.POS_Y = POS_Y  # type: float
        self.POS_Z = POS_Z  # type: float
        self.ORIENT_W = ORIENT_W  # type: float
        self.ORIENT_X = ORIENT_X  # type: float
        self.ORIENT_Y = ORIENT_Y  # type: float
        self.ORIENT_Z = ORIENT_Z  # type: float
        self.RADAR = RADAR  # type: Optional[SDRRadarConfig.SDRRadarConfigT]
        self.FLAGS = FLAGS  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdrsensorConfig = SDRSensorConfig()
        tmpSdrsensorConfig.Init(buf, pos)
        return cls.InitFromObj(tmpSdrsensorConfig)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdrsensorConfig):
        x = SDRSensorConfigT()
        x._UnPack(tmpSdrsensorConfig)
        return x

    # SDRSensorConfigT
    def _UnPack(self, SDRSensorConfig):
        if SDRSensorConfig is None:
            return
        self.CATEGORY = SDRSensorConfig.CATEGORY()
        self.POS_X = SDRSensorConfig.POS_X()
        self.POS_Y = SDRSensorConfig.POS_Y()
        self.POS_Z = SDRSensorConfig.POS_Z()
        self.ORIENT_W = SDRSensorConfig.ORIENT_W()
        self.ORIENT_X = SDRSensorConfig.ORIENT_X()
        self.ORIENT_Y = SDRSensorConfig.ORIENT_Y()
        self.ORIENT_Z = SDRSensorConfig.ORIENT_Z()
        if SDRSensorConfig.RADAR() is not None:
            self.RADAR = SDRRadarConfig.SDRRadarConfigT.InitFromObj(SDRSensorConfig.RADAR())
        self.FLAGS = SDRSensorConfig.FLAGS()

    # SDRSensorConfigT
    def Pack(self, builder):
        if self.RADAR is not None:
            RADAR = self.RADAR.Pack(builder)
        SDRSensorConfigStart(builder)
        SDRSensorConfigAddCATEGORY(builder, self.CATEGORY)
        SDRSensorConfigAddPOS_X(builder, self.POS_X)
        SDRSensorConfigAddPOS_Y(builder, self.POS_Y)
        SDRSensorConfigAddPOS_Z(builder, self.POS_Z)
        SDRSensorConfigAddORIENT_W(builder, self.ORIENT_W)
        SDRSensorConfigAddORIENT_X(builder, self.ORIENT_X)
        SDRSensorConfigAddORIENT_Y(builder, self.ORIENT_Y)
        SDRSensorConfigAddORIENT_Z(builder, self.ORIENT_Z)
        if self.RADAR is not None:
            SDRSensorConfigAddRADAR(builder, RADAR)
        SDRSensorConfigAddFLAGS(builder, self.FLAGS)
        SDRSensorConfig = SDRSensorConfigEnd(builder)
        return SDRSensorConfig
