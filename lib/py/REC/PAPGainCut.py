# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One one-dimensional gain cut through the synthesized pattern.
class PAPGainCut(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PAPGainCut()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPAPGainCut(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PAPGainCutBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x41\x50", size_prefixed=size_prefixed)

    # PAPGainCut
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PAPGainCut
    def CUT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PAPGainCut
    def AXIS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # PAPGainCut
    def FIXED_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPGainCut
    def ANGLES_DEG(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PAPGainCut
    def ANGLES_DEGAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PAPGainCut
    def ANGLES_DEGLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PAPGainCut
    def ANGLES_DEGIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # PAPGainCut
    def GAIN_DBI(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PAPGainCut
    def GAIN_DBIAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PAPGainCut
    def GAIN_DBILength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PAPGainCut
    def GAIN_DBIIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def PAPGainCutStart(builder):
    builder.StartObject(5)

def Start(builder):
    PAPGainCutStart(builder)

def PAPGainCutAddCUT_ID(builder, CUT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CUT_ID), 0)

def AddCUT_ID(builder, CUT_ID):
    PAPGainCutAddCUT_ID(builder, CUT_ID)

def PAPGainCutAddAXIS(builder, AXIS):
    builder.PrependInt8Slot(1, AXIS, 0)

def AddAXIS(builder, AXIS):
    PAPGainCutAddAXIS(builder, AXIS)

def PAPGainCutAddFIXED_ANGLE_DEG(builder, FIXED_ANGLE_DEG):
    builder.PrependFloat64Slot(2, FIXED_ANGLE_DEG, 0.0)

def AddFIXED_ANGLE_DEG(builder, FIXED_ANGLE_DEG):
    PAPGainCutAddFIXED_ANGLE_DEG(builder, FIXED_ANGLE_DEG)

def PAPGainCutAddANGLES_DEG(builder, ANGLES_DEG):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ANGLES_DEG), 0)

def AddANGLES_DEG(builder, ANGLES_DEG):
    PAPGainCutAddANGLES_DEG(builder, ANGLES_DEG)

def PAPGainCutStartANGLES_DEGVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartANGLES_DEGVector(builder, numElems):
    return PAPGainCutStartANGLES_DEGVector(builder, numElems)

def PAPGainCutCreateANGLES_DEGVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateANGLES_DEGVector(builder, data):
    PAPGainCutCreateANGLES_DEGVector(builder, data)

def PAPGainCutAddGAIN_DBI(builder, GAIN_DBI):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(GAIN_DBI), 0)

def AddGAIN_DBI(builder, GAIN_DBI):
    PAPGainCutAddGAIN_DBI(builder, GAIN_DBI)

def PAPGainCutStartGAIN_DBIVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartGAIN_DBIVector(builder, numElems):
    return PAPGainCutStartGAIN_DBIVector(builder, numElems)

def PAPGainCutCreateGAIN_DBIVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateGAIN_DBIVector(builder, data):
    PAPGainCutCreateGAIN_DBIVector(builder, data)

def PAPGainCutEnd(builder):
    return builder.EndObject()

def End(builder):
    return PAPGainCutEnd(builder)

try:
    from typing import List
except:
    pass

class PAPGainCutT(object):

    # PAPGainCutT
    def __init__(
        self,
        CUT_ID = None,
        AXIS = 0,
        FIXED_ANGLE_DEG = 0.0,
        ANGLES_DEG = None,
        GAIN_DBI = None,
    ):
        self.CUT_ID = CUT_ID  # type: Optional[str]
        self.AXIS = AXIS  # type: int
        self.FIXED_ANGLE_DEG = FIXED_ANGLE_DEG  # type: float
        self.ANGLES_DEG = ANGLES_DEG  # type: Optional[List[float]]
        self.GAIN_DBI = GAIN_DBI  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPapgainCut = PAPGainCut()
        tmpPapgainCut.Init(buf, pos)
        return cls.InitFromObj(tmpPapgainCut)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPapgainCut):
        x = PAPGainCutT()
        x._UnPack(tmpPapgainCut)
        return x

    # PAPGainCutT
    def _UnPack(self, PAPGainCut):
        if PAPGainCut is None:
            return
        self.CUT_ID = PAPGainCut.CUT_ID()
        self.AXIS = PAPGainCut.AXIS()
        self.FIXED_ANGLE_DEG = PAPGainCut.FIXED_ANGLE_DEG()
        if not PAPGainCut.ANGLES_DEGIsNone():
            if np is None:
                self.ANGLES_DEG = []
                for i in range(PAPGainCut.ANGLES_DEGLength()):
                    self.ANGLES_DEG.append(PAPGainCut.ANGLES_DEG(i))
            else:
                self.ANGLES_DEG = PAPGainCut.ANGLES_DEGAsNumpy()
        if not PAPGainCut.GAIN_DBIIsNone():
            if np is None:
                self.GAIN_DBI = []
                for i in range(PAPGainCut.GAIN_DBILength()):
                    self.GAIN_DBI.append(PAPGainCut.GAIN_DBI(i))
            else:
                self.GAIN_DBI = PAPGainCut.GAIN_DBIAsNumpy()

    # PAPGainCutT
    def Pack(self, builder):
        if self.CUT_ID is not None:
            CUT_ID = builder.CreateString(self.CUT_ID)
        if self.ANGLES_DEG is not None:
            if np is not None and type(self.ANGLES_DEG) is np.ndarray:
                ANGLES_DEG = builder.CreateNumpyVector(self.ANGLES_DEG)
            else:
                PAPGainCutStartANGLES_DEGVector(builder, len(self.ANGLES_DEG))
                for i in reversed(range(len(self.ANGLES_DEG))):
                    builder.PrependFloat64(self.ANGLES_DEG[i])
                ANGLES_DEG = builder.EndVector()
        if self.GAIN_DBI is not None:
            if np is not None and type(self.GAIN_DBI) is np.ndarray:
                GAIN_DBI = builder.CreateNumpyVector(self.GAIN_DBI)
            else:
                PAPGainCutStartGAIN_DBIVector(builder, len(self.GAIN_DBI))
                for i in reversed(range(len(self.GAIN_DBI))):
                    builder.PrependFloat64(self.GAIN_DBI[i])
                GAIN_DBI = builder.EndVector()
        PAPGainCutStart(builder)
        if self.CUT_ID is not None:
            PAPGainCutAddCUT_ID(builder, CUT_ID)
        PAPGainCutAddAXIS(builder, self.AXIS)
        PAPGainCutAddFIXED_ANGLE_DEG(builder, self.FIXED_ANGLE_DEG)
        if self.ANGLES_DEG is not None:
            PAPGainCutAddANGLES_DEG(builder, ANGLES_DEG)
        if self.GAIN_DBI is not None:
            PAPGainCutAddGAIN_DBI(builder, GAIN_DBI)
        PAPGainCut = PAPGainCutEnd(builder)
        return PAPGainCut
