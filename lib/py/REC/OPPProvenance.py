# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Provenance for exactly one physical value.
#
# This table is the mechanism by which the never-invent-data law is enforced
# in the IDL rather than in prose: no physical quantity in an $OPP can be
# expressed without one of these attached, so a value with no admissible
# source cannot be encoded at all. Unknown values are ABSENT. They are never
# zero, never a placeholder, and never carried forward from a different
# object.
class OPPProvenance(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OPPProvenance()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOPPProvenance(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OPPProvenanceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x50\x50", size_prefixed=size_prefixed)

    # OPPProvenance
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher of the value, named as the publisher names itself: "ESA DISCOS",
    # "CelesTrak SATCAT", "Gunter's Space Page", "NASA", the operator's name.
    # OPPProvenance
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The source's own identifier for the record this value was read from, such
    # as a DISCOS object id. Verbatim, never normalized.
    # OPPProvenance
    def SOURCE_RECORD_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Deep link to the exact source record when the source publishes one.
    # OPPProvenance
    def SOURCE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ) of the
    # instant the value describes: the epoch at which the property held. A
    # design or as-built value that does not change over time omits EPOCH.
    # OPPProvenance
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ) when
    # this record retrieved the value from SOURCE.
    # OPPProvenance
    def RETRIEVED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The source's own revision, edition or publication marker, verbatim.
    # OPPProvenance
    def SOURCE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # OPPProvenance
    def METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Terms under which SOURCE permits redistribution of the value, verbatim.
    # OPPProvenance
    def LICENSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Attribution string the source requires be displayed with the value.
    # OPPProvenance
    def ATTRIBUTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # 64 lowercase hexadecimal characters encoding SHA-256 of the exact source
    # payload bytes the value was parsed from.
    # OPPProvenance
    def SOURCE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Free text qualifying the value, such as the source's own caveat.
    # OPPProvenance
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def OPPProvenanceStart(builder):
    builder.StartObject(11)

def Start(builder):
    OPPProvenanceStart(builder)

def OPPProvenanceAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    OPPProvenanceAddSOURCE(builder, SOURCE)

def OPPProvenanceAddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_RECORD_ID), 0)

def AddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID):
    OPPProvenanceAddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID)

def OPPProvenanceAddSOURCE_URL(builder, SOURCE_URL):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_URL), 0)

def AddSOURCE_URL(builder, SOURCE_URL):
    OPPProvenanceAddSOURCE_URL(builder, SOURCE_URL)

def OPPProvenanceAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    OPPProvenanceAddEPOCH(builder, EPOCH)

def OPPProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(RETRIEVED_AT), 0)

def AddRETRIEVED_AT(builder, RETRIEVED_AT):
    OPPProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT)

def OPPProvenanceAddSOURCE_VERSION(builder, SOURCE_VERSION):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_VERSION), 0)

def AddSOURCE_VERSION(builder, SOURCE_VERSION):
    OPPProvenanceAddSOURCE_VERSION(builder, SOURCE_VERSION)

def OPPProvenanceAddMETHOD(builder, METHOD):
    builder.PrependInt8Slot(6, METHOD, 0)

def AddMETHOD(builder, METHOD):
    OPPProvenanceAddMETHOD(builder, METHOD)

def OPPProvenanceAddLICENSE(builder, LICENSE):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE), 0)

def AddLICENSE(builder, LICENSE):
    OPPProvenanceAddLICENSE(builder, LICENSE)

def OPPProvenanceAddATTRIBUTION(builder, ATTRIBUTION):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(ATTRIBUTION), 0)

def AddATTRIBUTION(builder, ATTRIBUTION):
    OPPProvenanceAddATTRIBUTION(builder, ATTRIBUTION)

def OPPProvenanceAddSOURCE_SHA256(builder, SOURCE_SHA256):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_SHA256), 0)

def AddSOURCE_SHA256(builder, SOURCE_SHA256):
    OPPProvenanceAddSOURCE_SHA256(builder, SOURCE_SHA256)

def OPPProvenanceAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    OPPProvenanceAddNOTES(builder, NOTES)

def OPPProvenanceEnd(builder):
    return builder.EndObject()

def End(builder):
    return OPPProvenanceEnd(builder)


class OPPProvenanceT(object):

    # OPPProvenanceT
    def __init__(
        self,
        SOURCE = None,
        SOURCE_RECORD_ID = None,
        SOURCE_URL = None,
        EPOCH = None,
        RETRIEVED_AT = None,
        SOURCE_VERSION = None,
        METHOD = 0,
        LICENSE = None,
        ATTRIBUTION = None,
        SOURCE_SHA256 = None,
        NOTES = None,
    ):
        self.SOURCE = SOURCE  # type: Optional[str]
        self.SOURCE_RECORD_ID = SOURCE_RECORD_ID  # type: Optional[str]
        self.SOURCE_URL = SOURCE_URL  # type: Optional[str]
        self.EPOCH = EPOCH  # type: Optional[str]
        self.RETRIEVED_AT = RETRIEVED_AT  # type: Optional[str]
        self.SOURCE_VERSION = SOURCE_VERSION  # type: Optional[str]
        self.METHOD = METHOD  # type: int
        self.LICENSE = LICENSE  # type: Optional[str]
        self.ATTRIBUTION = ATTRIBUTION  # type: Optional[str]
        self.SOURCE_SHA256 = SOURCE_SHA256  # type: Optional[str]
        self.NOTES = NOTES  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOppprovenance = OPPProvenance()
        tmpOppprovenance.Init(buf, pos)
        return cls.InitFromObj(tmpOppprovenance)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOppprovenance):
        x = OPPProvenanceT()
        x._UnPack(tmpOppprovenance)
        return x

    # OPPProvenanceT
    def _UnPack(self, OPPProvenance):
        if OPPProvenance is None:
            return
        self.SOURCE = OPPProvenance.SOURCE()
        self.SOURCE_RECORD_ID = OPPProvenance.SOURCE_RECORD_ID()
        self.SOURCE_URL = OPPProvenance.SOURCE_URL()
        self.EPOCH = OPPProvenance.EPOCH()
        self.RETRIEVED_AT = OPPProvenance.RETRIEVED_AT()
        self.SOURCE_VERSION = OPPProvenance.SOURCE_VERSION()
        self.METHOD = OPPProvenance.METHOD()
        self.LICENSE = OPPProvenance.LICENSE()
        self.ATTRIBUTION = OPPProvenance.ATTRIBUTION()
        self.SOURCE_SHA256 = OPPProvenance.SOURCE_SHA256()
        self.NOTES = OPPProvenance.NOTES()

    # OPPProvenanceT
    def Pack(self, builder):
        if self.SOURCE is not None:
            SOURCE = builder.CreateString(self.SOURCE)
        if self.SOURCE_RECORD_ID is not None:
            SOURCE_RECORD_ID = builder.CreateString(self.SOURCE_RECORD_ID)
        if self.SOURCE_URL is not None:
            SOURCE_URL = builder.CreateString(self.SOURCE_URL)
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        if self.RETRIEVED_AT is not None:
            RETRIEVED_AT = builder.CreateString(self.RETRIEVED_AT)
        if self.SOURCE_VERSION is not None:
            SOURCE_VERSION = builder.CreateString(self.SOURCE_VERSION)
        if self.LICENSE is not None:
            LICENSE = builder.CreateString(self.LICENSE)
        if self.ATTRIBUTION is not None:
            ATTRIBUTION = builder.CreateString(self.ATTRIBUTION)
        if self.SOURCE_SHA256 is not None:
            SOURCE_SHA256 = builder.CreateString(self.SOURCE_SHA256)
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        OPPProvenanceStart(builder)
        if self.SOURCE is not None:
            OPPProvenanceAddSOURCE(builder, SOURCE)
        if self.SOURCE_RECORD_ID is not None:
            OPPProvenanceAddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID)
        if self.SOURCE_URL is not None:
            OPPProvenanceAddSOURCE_URL(builder, SOURCE_URL)
        if self.EPOCH is not None:
            OPPProvenanceAddEPOCH(builder, EPOCH)
        if self.RETRIEVED_AT is not None:
            OPPProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT)
        if self.SOURCE_VERSION is not None:
            OPPProvenanceAddSOURCE_VERSION(builder, SOURCE_VERSION)
        OPPProvenanceAddMETHOD(builder, self.METHOD)
        if self.LICENSE is not None:
            OPPProvenanceAddLICENSE(builder, LICENSE)
        if self.ATTRIBUTION is not None:
            OPPProvenanceAddATTRIBUTION(builder, ATTRIBUTION)
        if self.SOURCE_SHA256 is not None:
            OPPProvenanceAddSOURCE_SHA256(builder, SOURCE_SHA256)
        if self.NOTES is not None:
            OPPProvenanceAddNOTES(builder, NOTES)
        OPPProvenance = OPPProvenanceEnd(builder)
        return OPPProvenance
