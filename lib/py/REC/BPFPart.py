# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One variable part of the build template and whether this profile embeds
# it.
class BPFPart(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BPFPart()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBPFPart(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BPFPartBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x50\x46", size_prefixed=size_prefixed)

    # BPFPart
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Template slot identifier this entry configures, as published by the
    # template's part manifest. Required, unique within the profile.
    # BPFPart
    def PART_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Capability class of the part.
    # BPFPart
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # True when the part is embedded in the produced artifact.
    # BPFPart
    def INCLUDED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # 64 lowercase hexadecimal characters encoding SHA-256 of the part's
    # bytes as published by the template, so a composed artifact can be
    # verified against the profile that produced it.
    # BPFPart
    def CONTENT_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Size of the part's bytes in octets, as published by the template.
    # BPFPart
    def BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Human-readable summary.
    # BPFPart
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def BPFPartStart(builder):
    builder.StartObject(6)

def Start(builder):
    BPFPartStart(builder)

def BPFPartAddPART_ID(builder, PART_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PART_ID), 0)

def AddPART_ID(builder, PART_ID):
    BPFPartAddPART_ID(builder, PART_ID)

def BPFPartAddKIND(builder, KIND):
    builder.PrependInt8Slot(1, KIND, 0)

def AddKIND(builder, KIND):
    BPFPartAddKIND(builder, KIND)

def BPFPartAddINCLUDED(builder, INCLUDED):
    builder.PrependBoolSlot(2, INCLUDED, 0)

def AddINCLUDED(builder, INCLUDED):
    BPFPartAddINCLUDED(builder, INCLUDED)

def BPFPartAddCONTENT_SHA256(builder, CONTENT_SHA256):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT_SHA256), 0)

def AddCONTENT_SHA256(builder, CONTENT_SHA256):
    BPFPartAddCONTENT_SHA256(builder, CONTENT_SHA256)

def BPFPartAddBYTE_LENGTH(builder, BYTE_LENGTH):
    builder.PrependUint64Slot(4, BYTE_LENGTH, 0)

def AddBYTE_LENGTH(builder, BYTE_LENGTH):
    BPFPartAddBYTE_LENGTH(builder, BYTE_LENGTH)

def BPFPartAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    BPFPartAddDESCRIPTION(builder, DESCRIPTION)

def BPFPartEnd(builder):
    return builder.EndObject()

def End(builder):
    return BPFPartEnd(builder)


class BPFPartT(object):

    # BPFPartT
    def __init__(
        self,
        PART_ID = None,
        KIND = 0,
        INCLUDED = False,
        CONTENT_SHA256 = None,
        BYTE_LENGTH = 0,
        DESCRIPTION = None,
    ):
        self.PART_ID = PART_ID  # type: Optional[str]
        self.KIND = KIND  # type: int
        self.INCLUDED = INCLUDED  # type: bool
        self.CONTENT_SHA256 = CONTENT_SHA256  # type: Optional[str]
        self.BYTE_LENGTH = BYTE_LENGTH  # type: int
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpBpfpart = BPFPart()
        tmpBpfpart.Init(buf, pos)
        return cls.InitFromObj(tmpBpfpart)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpBpfpart):
        x = BPFPartT()
        x._UnPack(tmpBpfpart)
        return x

    # BPFPartT
    def _UnPack(self, BPFPart):
        if BPFPart is None:
            return
        self.PART_ID = BPFPart.PART_ID()
        self.KIND = BPFPart.KIND()
        self.INCLUDED = BPFPart.INCLUDED()
        self.CONTENT_SHA256 = BPFPart.CONTENT_SHA256()
        self.BYTE_LENGTH = BPFPart.BYTE_LENGTH()
        self.DESCRIPTION = BPFPart.DESCRIPTION()

    # BPFPartT
    def Pack(self, builder):
        if self.PART_ID is not None:
            PART_ID = builder.CreateString(self.PART_ID)
        if self.CONTENT_SHA256 is not None:
            CONTENT_SHA256 = builder.CreateString(self.CONTENT_SHA256)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        BPFPartStart(builder)
        if self.PART_ID is not None:
            BPFPartAddPART_ID(builder, PART_ID)
        BPFPartAddKIND(builder, self.KIND)
        BPFPartAddINCLUDED(builder, self.INCLUDED)
        if self.CONTENT_SHA256 is not None:
            BPFPartAddCONTENT_SHA256(builder, CONTENT_SHA256)
        BPFPartAddBYTE_LENGTH(builder, self.BYTE_LENGTH)
        if self.DESCRIPTION is not None:
            BPFPartAddDESCRIPTION(builder, DESCRIPTION)
        BPFPart = BPFPartEnd(builder)
        return BPFPart
