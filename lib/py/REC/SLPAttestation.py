# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SLPAttestation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SLPAttestation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSLPAttestation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SLPAttestationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x4C\x50", size_prefixed=size_prefixed)

    # SLPAttestation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Literal Ed25519 public key, encoded as 64 lowercase hexadecimal characters.
    # SLPAttestation
    def SIGNING_PUBLIC_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SLPAttestation
    def SIGNED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Ed25519 signature over the size-prefixed FlatBuffer with both signature
    # payloads zeroed while preserving their vectors and offsets.
    # SLPAttestation
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SLPAttestation
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SLPAttestation
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SLPAttestation
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Ed25519 signature over canonical JSON in IDL field order and capitalization,
    # with both signature fields omitted.
    # SLPAttestation
    def CANONICAL_JSON_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SLPAttestation
    def CANONICAL_JSON_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SLPAttestation
    def CANONICAL_JSON_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SLPAttestation
    def CANONICAL_JSON_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def SLPAttestationStart(builder):
    builder.StartObject(4)

def Start(builder):
    SLPAttestationStart(builder)

def SLPAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNING_PUBLIC_KEY), 0)

def AddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    SLPAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)

def SLPAttestationAddSIGNED_AT(builder, SIGNED_AT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNED_AT), 0)

def AddSIGNED_AT(builder, SIGNED_AT):
    SLPAttestationAddSIGNED_AT(builder, SIGNED_AT)

def SLPAttestationAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    SLPAttestationAddSIGNATURE(builder, SIGNATURE)

def SLPAttestationStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return SLPAttestationStartSIGNATUREVector(builder, numElems)

def SLPAttestationCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    SLPAttestationCreateSIGNATUREVector(builder, data)

def SLPAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CANONICAL_JSON_SIGNATURE), 0)

def AddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE):
    SLPAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE)

def SLPAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCANONICAL_JSON_SIGNATUREVector(builder, numElems):
    return SLPAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, numElems)

def SLPAttestationCreateCANONICAL_JSON_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCANONICAL_JSON_SIGNATUREVector(builder, data):
    SLPAttestationCreateCANONICAL_JSON_SIGNATUREVector(builder, data)

def SLPAttestationEnd(builder):
    return builder.EndObject()

def End(builder):
    return SLPAttestationEnd(builder)

try:
    from typing import List
except:
    pass

class SLPAttestationT(object):

    # SLPAttestationT
    def __init__(
        self,
        SIGNING_PUBLIC_KEY = None,
        SIGNED_AT = None,
        SIGNATURE = None,
        CANONICAL_JSON_SIGNATURE = None,
    ):
        self.SIGNING_PUBLIC_KEY = SIGNING_PUBLIC_KEY  # type: Optional[str]
        self.SIGNED_AT = SIGNED_AT  # type: Optional[str]
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]
        self.CANONICAL_JSON_SIGNATURE = CANONICAL_JSON_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSlpattestation = SLPAttestation()
        tmpSlpattestation.Init(buf, pos)
        return cls.InitFromObj(tmpSlpattestation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSlpattestation):
        x = SLPAttestationT()
        x._UnPack(tmpSlpattestation)
        return x

    # SLPAttestationT
    def _UnPack(self, SLPAttestation):
        if SLPAttestation is None:
            return
        self.SIGNING_PUBLIC_KEY = SLPAttestation.SIGNING_PUBLIC_KEY()
        self.SIGNED_AT = SLPAttestation.SIGNED_AT()
        if not SLPAttestation.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(SLPAttestation.SIGNATURELength()):
                    self.SIGNATURE.append(SLPAttestation.SIGNATURE(i))
            else:
                self.SIGNATURE = SLPAttestation.SIGNATUREAsNumpy()
        if not SLPAttestation.CANONICAL_JSON_SIGNATUREIsNone():
            if np is None:
                self.CANONICAL_JSON_SIGNATURE = []
                for i in range(SLPAttestation.CANONICAL_JSON_SIGNATURELength()):
                    self.CANONICAL_JSON_SIGNATURE.append(SLPAttestation.CANONICAL_JSON_SIGNATURE(i))
            else:
                self.CANONICAL_JSON_SIGNATURE = SLPAttestation.CANONICAL_JSON_SIGNATUREAsNumpy()

    # SLPAttestationT
    def Pack(self, builder):
        if self.SIGNING_PUBLIC_KEY is not None:
            SIGNING_PUBLIC_KEY = builder.CreateString(self.SIGNING_PUBLIC_KEY)
        if self.SIGNED_AT is not None:
            SIGNED_AT = builder.CreateString(self.SIGNED_AT)
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                SLPAttestationStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        if self.CANONICAL_JSON_SIGNATURE is not None:
            if np is not None and type(self.CANONICAL_JSON_SIGNATURE) is np.ndarray:
                CANONICAL_JSON_SIGNATURE = builder.CreateNumpyVector(self.CANONICAL_JSON_SIGNATURE)
            else:
                SLPAttestationStartCANONICAL_JSON_SIGNATUREVector(builder, len(self.CANONICAL_JSON_SIGNATURE))
                for i in reversed(range(len(self.CANONICAL_JSON_SIGNATURE))):
                    builder.PrependUint8(self.CANONICAL_JSON_SIGNATURE[i])
                CANONICAL_JSON_SIGNATURE = builder.EndVector()
        SLPAttestationStart(builder)
        if self.SIGNING_PUBLIC_KEY is not None:
            SLPAttestationAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)
        if self.SIGNED_AT is not None:
            SLPAttestationAddSIGNED_AT(builder, SIGNED_AT)
        if self.SIGNATURE is not None:
            SLPAttestationAddSIGNATURE(builder, SIGNATURE)
        if self.CANONICAL_JSON_SIGNATURE is not None:
            SLPAttestationAddCANONICAL_JSON_SIGNATURE(builder, CANONICAL_JSON_SIGNATURE)
        SLPAttestation = SLPAttestationEnd(builder)
        return SLPAttestation
