# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PRWExecutionResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWExecutionResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWExecutionResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWExecutionResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWExecutionResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWExecutionResult
    def FINAL_SAMPLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWPropagationSample import PRWPropagationSample
            obj = PRWPropagationSample()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWExecutionResult
    def SAMPLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PRWPropagationSample import PRWPropagationSample
            obj = PRWPropagationSample()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWExecutionResult
    def SAMPLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWExecutionResult
    def SAMPLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # PRWExecutionResult
    def ELAPSED_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PRWExecutionResult
    def EPHEMERIS_SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PRWExecutionResult
    def STM_TECHNIQUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # PRWExecutionResult
    def DENSITY_TREATMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def PRWExecutionResultStart(builder):
    builder.StartObject(6)

def Start(builder):
    PRWExecutionResultStart(builder)

def PRWExecutionResultAddFINAL_SAMPLE(builder, FINAL_SAMPLE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(FINAL_SAMPLE), 0)

def AddFINAL_SAMPLE(builder, FINAL_SAMPLE):
    PRWExecutionResultAddFINAL_SAMPLE(builder, FINAL_SAMPLE)

def PRWExecutionResultAddSAMPLES(builder, SAMPLES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SAMPLES), 0)

def AddSAMPLES(builder, SAMPLES):
    PRWExecutionResultAddSAMPLES(builder, SAMPLES)

def PRWExecutionResultStartSAMPLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSAMPLESVector(builder, numElems):
    return PRWExecutionResultStartSAMPLESVector(builder, numElems)

def PRWExecutionResultCreateSAMPLESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSAMPLESVector(builder, data):
    PRWExecutionResultCreateSAMPLESVector(builder, data)

def PRWExecutionResultAddELAPSED_SECONDS(builder, ELAPSED_SECONDS):
    builder.PrependFloat64Slot(2, ELAPSED_SECONDS, 0.0)

def AddELAPSED_SECONDS(builder, ELAPSED_SECONDS):
    PRWExecutionResultAddELAPSED_SECONDS(builder, ELAPSED_SECONDS)

def PRWExecutionResultAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(EPHEMERIS_SOURCE), 0)

def AddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE):
    PRWExecutionResultAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)

def PRWExecutionResultAddSTM_TECHNIQUE(builder, STM_TECHNIQUE):
    builder.PrependUint8Slot(4, STM_TECHNIQUE, 0)

def AddSTM_TECHNIQUE(builder, STM_TECHNIQUE):
    PRWExecutionResultAddSTM_TECHNIQUE(builder, STM_TECHNIQUE)

def PRWExecutionResultAddDENSITY_TREATMENT(builder, DENSITY_TREATMENT):
    builder.PrependUint8Slot(5, DENSITY_TREATMENT, 0)

def AddDENSITY_TREATMENT(builder, DENSITY_TREATMENT):
    PRWExecutionResultAddDENSITY_TREATMENT(builder, DENSITY_TREATMENT)

def PRWExecutionResultEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWExecutionResultEnd(builder)

import PRWPropagationSample
try:
    from typing import List, Optional
except:
    pass

class PRWExecutionResultT(object):

    # PRWExecutionResultT
    def __init__(
        self,
        FINAL_SAMPLE = None,
        SAMPLES = None,
        ELAPSED_SECONDS = 0.0,
        EPHEMERIS_SOURCE = None,
        STM_TECHNIQUE = 0,
        DENSITY_TREATMENT = 0,
    ):
        self.FINAL_SAMPLE = FINAL_SAMPLE  # type: Optional[PRWPropagationSample.PRWPropagationSampleT]
        self.SAMPLES = SAMPLES  # type: Optional[List[PRWPropagationSample.PRWPropagationSampleT]]
        self.ELAPSED_SECONDS = ELAPSED_SECONDS  # type: float
        self.EPHEMERIS_SOURCE = EPHEMERIS_SOURCE  # type: Optional[str]
        self.STM_TECHNIQUE = STM_TECHNIQUE  # type: int
        self.DENSITY_TREATMENT = DENSITY_TREATMENT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwexecutionResult = PRWExecutionResult()
        tmpPrwexecutionResult.Init(buf, pos)
        return cls.InitFromObj(tmpPrwexecutionResult)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwexecutionResult):
        x = PRWExecutionResultT()
        x._UnPack(tmpPrwexecutionResult)
        return x

    # PRWExecutionResultT
    def _UnPack(self, PRWExecutionResult):
        if PRWExecutionResult is None:
            return
        if PRWExecutionResult.FINAL_SAMPLE() is not None:
            self.FINAL_SAMPLE = PRWPropagationSample.PRWPropagationSampleT.InitFromObj(PRWExecutionResult.FINAL_SAMPLE())
        if not PRWExecutionResult.SAMPLESIsNone():
            self.SAMPLES = []
            for i in range(PRWExecutionResult.SAMPLESLength()):
                if PRWExecutionResult.SAMPLES(i) is None:
                    self.SAMPLES.append(None)
                else:
                    pRWPropagationSample_ = PRWPropagationSample.PRWPropagationSampleT.InitFromObj(PRWExecutionResult.SAMPLES(i))
                    self.SAMPLES.append(pRWPropagationSample_)
        self.ELAPSED_SECONDS = PRWExecutionResult.ELAPSED_SECONDS()
        self.EPHEMERIS_SOURCE = PRWExecutionResult.EPHEMERIS_SOURCE()
        self.STM_TECHNIQUE = PRWExecutionResult.STM_TECHNIQUE()
        self.DENSITY_TREATMENT = PRWExecutionResult.DENSITY_TREATMENT()

    # PRWExecutionResultT
    def Pack(self, builder):
        if self.FINAL_SAMPLE is not None:
            FINAL_SAMPLE = self.FINAL_SAMPLE.Pack(builder)
        if self.SAMPLES is not None:
            SAMPLESlist = []
            for i in range(len(self.SAMPLES)):
                SAMPLESlist.append(self.SAMPLES[i].Pack(builder))
            PRWExecutionResultStartSAMPLESVector(builder, len(self.SAMPLES))
            for i in reversed(range(len(self.SAMPLES))):
                builder.PrependUOffsetTRelative(SAMPLESlist[i])
            SAMPLES = builder.EndVector()
        if self.EPHEMERIS_SOURCE is not None:
            EPHEMERIS_SOURCE = builder.CreateString(self.EPHEMERIS_SOURCE)
        PRWExecutionResultStart(builder)
        if self.FINAL_SAMPLE is not None:
            PRWExecutionResultAddFINAL_SAMPLE(builder, FINAL_SAMPLE)
        if self.SAMPLES is not None:
            PRWExecutionResultAddSAMPLES(builder, SAMPLES)
        PRWExecutionResultAddELAPSED_SECONDS(builder, self.ELAPSED_SECONDS)
        if self.EPHEMERIS_SOURCE is not None:
            PRWExecutionResultAddEPHEMERIS_SOURCE(builder, EPHEMERIS_SOURCE)
        PRWExecutionResultAddSTM_TECHNIQUE(builder, self.STM_TECHNIQUE)
        PRWExecutionResultAddDENSITY_TREATMENT(builder, self.DENSITY_TREATMENT)
        PRWExecutionResult = PRWExecutionResultEnd(builder)
        return PRWExecutionResult
