# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Path reconstruction request.
class FPCPathRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FPCPathRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFPCPathRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FPCPathRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x46\x50\x43", size_prefixed=size_prefixed)

    # FPCPathRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Target vertex index.
    # FPCPathRequest
    def TARGET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def FPCPathRequestStart(builder):
    builder.StartObject(1)

def Start(builder):
    FPCPathRequestStart(builder)

def FPCPathRequestAddTARGET(builder, TARGET):
    builder.PrependUint32Slot(0, TARGET, 0)

def AddTARGET(builder, TARGET):
    FPCPathRequestAddTARGET(builder, TARGET)

def FPCPathRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return FPCPathRequestEnd(builder)


class FPCPathRequestT(object):

    # FPCPathRequestT
    def __init__(
        self,
        TARGET = 0,
    ):
        self.TARGET = TARGET  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpFpcpathRequest = FPCPathRequest()
        tmpFpcpathRequest.Init(buf, pos)
        return cls.InitFromObj(tmpFpcpathRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpFpcpathRequest):
        x = FPCPathRequestT()
        x._UnPack(tmpFpcpathRequest)
        return x

    # FPCPathRequestT
    def _UnPack(self, FPCPathRequest):
        if FPCPathRequest is None:
            return
        self.TARGET = FPCPathRequest.TARGET()

    # FPCPathRequestT
    def Pack(self, builder):
        FPCPathRequestStart(builder)
        FPCPathRequestAddTARGET(builder, self.TARGET)
        FPCPathRequest = FPCPathRequestEnd(builder)
        return FPCPathRequest
