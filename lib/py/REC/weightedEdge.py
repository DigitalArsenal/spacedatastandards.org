# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Weighted directed edge (16 bytes, 8-byte aligned for zero-copy ingestion).
class weightedEdge(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 16

    # weightedEdge
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # weightedEdge
    def SRC(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # weightedEdge
    def DST(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))
    # weightedEdge
    def WEIGHT(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))

def CreateweightedEdge(builder, SRC, DST, WEIGHT):
    builder.Prep(8, 16)
    builder.PrependFloat64(WEIGHT)
    builder.PrependUint32(DST)
    builder.PrependUint32(SRC)
    return builder.Offset()


class weightedEdgeT(object):

    # weightedEdgeT
    def __init__(
        self,
        SRC = 0,
        DST = 0,
        WEIGHT = 0.0,
    ):
        self.SRC = SRC  # type: int
        self.DST = DST  # type: int
        self.WEIGHT = WEIGHT  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpWeightedEdge = weightedEdge()
        tmpWeightedEdge.Init(buf, pos)
        return cls.InitFromObj(tmpWeightedEdge)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpWeightedEdge):
        x = weightedEdgeT()
        x._UnPack(tmpWeightedEdge)
        return x

    # weightedEdgeT
    def _UnPack(self, weightedEdge):
        if weightedEdge is None:
            return
        self.SRC = weightedEdge.SRC()
        self.DST = weightedEdge.DST()
        self.WEIGHT = weightedEdge.WEIGHT()

    # weightedEdgeT
    def Pack(self, builder):
        return CreateweightedEdge(builder, self.SRC, self.DST, self.WEIGHT)
