# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# B-spline interpolation envelope.
class BSP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BSP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBSP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BSPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x42\x53\x50", size_prefixed=size_prefixed)

    # BSP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # B-spline interpolation request.
    # BSP
    def INTERPOLATION_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from BSPInterpolationRequest import BSPInterpolationRequest
            obj = BSPInterpolationRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # B-spline interpolation result.
    # BSP
    def INTERPOLATION_RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from BSPInterpolationResult import BSPInterpolationResult
            obj = BSPInterpolationResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def BSPStart(builder):
    builder.StartObject(2)

def Start(builder):
    BSPStart(builder)

def BSPAddINTERPOLATION_REQUEST(builder, INTERPOLATION_REQUEST):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(INTERPOLATION_REQUEST), 0)

def AddINTERPOLATION_REQUEST(builder, INTERPOLATION_REQUEST):
    BSPAddINTERPOLATION_REQUEST(builder, INTERPOLATION_REQUEST)

def BSPAddINTERPOLATION_RESULT(builder, INTERPOLATION_RESULT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(INTERPOLATION_RESULT), 0)

def AddINTERPOLATION_RESULT(builder, INTERPOLATION_RESULT):
    BSPAddINTERPOLATION_RESULT(builder, INTERPOLATION_RESULT)

def BSPEnd(builder):
    return builder.EndObject()

def End(builder):
    return BSPEnd(builder)

import BSPInterpolationRequest
import BSPInterpolationResult
try:
    from typing import Optional
except:
    pass

class BSPT(object):

    # BSPT
    def __init__(
        self,
        INTERPOLATION_REQUEST = None,
        INTERPOLATION_RESULT = None,
    ):
        self.INTERPOLATION_REQUEST = INTERPOLATION_REQUEST  # type: Optional[BSPInterpolationRequest.BSPInterpolationRequestT]
        self.INTERPOLATION_RESULT = INTERPOLATION_RESULT  # type: Optional[BSPInterpolationResult.BSPInterpolationResultT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpBsp = BSP()
        tmpBsp.Init(buf, pos)
        return cls.InitFromObj(tmpBsp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpBsp):
        x = BSPT()
        x._UnPack(tmpBsp)
        return x

    # BSPT
    def _UnPack(self, BSP):
        if BSP is None:
            return
        if BSP.INTERPOLATION_REQUEST() is not None:
            self.INTERPOLATION_REQUEST = BSPInterpolationRequest.BSPInterpolationRequestT.InitFromObj(BSP.INTERPOLATION_REQUEST())
        if BSP.INTERPOLATION_RESULT() is not None:
            self.INTERPOLATION_RESULT = BSPInterpolationResult.BSPInterpolationResultT.InitFromObj(BSP.INTERPOLATION_RESULT())

    # BSPT
    def Pack(self, builder):
        if self.INTERPOLATION_REQUEST is not None:
            INTERPOLATION_REQUEST = self.INTERPOLATION_REQUEST.Pack(builder)
        if self.INTERPOLATION_RESULT is not None:
            INTERPOLATION_RESULT = self.INTERPOLATION_RESULT.Pack(builder)
        BSPStart(builder)
        if self.INTERPOLATION_REQUEST is not None:
            BSPAddINTERPOLATION_REQUEST(builder, INTERPOLATION_REQUEST)
        if self.INTERPOLATION_RESULT is not None:
            BSPAddINTERPOLATION_RESULT(builder, INTERPOLATION_RESULT)
        BSP = BSPEnd(builder)
        return BSP
