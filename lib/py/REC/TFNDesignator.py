# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One designator the facility answers to in a named coding system.
class TFNDesignator(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TFNDesignator()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTFNDesignator(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TFNDesignatorBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x46\x4E", size_prefixed=size_prefixed)

    # TFNDesignator
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Coding-system token, verbatim (the system's own name). Never a field
    # name and never normalized by the publisher.
    # TFNDesignator
    def SCHEME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The designator within that system, verbatim.
    # TFNDesignator
    def VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TFNDesignatorStart(builder):
    builder.StartObject(2)

def Start(builder):
    TFNDesignatorStart(builder)

def TFNDesignatorAddSCHEME(builder, SCHEME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEME), 0)

def AddSCHEME(builder, SCHEME):
    TFNDesignatorAddSCHEME(builder, SCHEME)

def TFNDesignatorAddVALUE(builder, VALUE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VALUE), 0)

def AddVALUE(builder, VALUE):
    TFNDesignatorAddVALUE(builder, VALUE)

def TFNDesignatorEnd(builder):
    return builder.EndObject()

def End(builder):
    return TFNDesignatorEnd(builder)


class TFNDesignatorT(object):

    # TFNDesignatorT
    def __init__(
        self,
        SCHEME = None,
        VALUE = None,
    ):
        self.SCHEME = SCHEME  # type: Optional[str]
        self.VALUE = VALUE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTfndesignator = TFNDesignator()
        tmpTfndesignator.Init(buf, pos)
        return cls.InitFromObj(tmpTfndesignator)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTfndesignator):
        x = TFNDesignatorT()
        x._UnPack(tmpTfndesignator)
        return x

    # TFNDesignatorT
    def _UnPack(self, TFNDesignator):
        if TFNDesignator is None:
            return
        self.SCHEME = TFNDesignator.SCHEME()
        self.VALUE = TFNDesignator.VALUE()

    # TFNDesignatorT
    def Pack(self, builder):
        if self.SCHEME is not None:
            SCHEME = builder.CreateString(self.SCHEME)
        if self.VALUE is not None:
            VALUE = builder.CreateString(self.VALUE)
        TFNDesignatorStart(builder)
        if self.SCHEME is not None:
            TFNDesignatorAddSCHEME(builder, SCHEME)
        if self.VALUE is not None:
            TFNDesignatorAddVALUE(builder, VALUE)
        TFNDesignator = TFNDesignatorEnd(builder)
        return TFNDesignator
