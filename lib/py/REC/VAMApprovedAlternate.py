# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Exact approved alternate asset bytes and reviewed state.
class VAMApprovedAlternate(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VAMApprovedAlternate()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVAMApprovedAlternate(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VAMApprovedAlternateBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x41\x4D", size_prefixed=size_prefixed)

    # VAMApprovedAlternate
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # VAMApprovedAlternate
    def VARIANT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Nonempty CIDv1 identifying the exact approved alternate bytes.
    # VAMApprovedAlternate
    def CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SHA-256 of the exact approved alternate bytes as 64 lowercase hexadecimal characters.
    # VAMApprovedAlternate
    def BYTE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # VAMApprovedAlternate
    def REVIEWED_TRANSFORM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VAMTransform import VAMTransform
            obj = VAMTransform()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VAMApprovedAlternate
    def RANK(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def VAMApprovedAlternateStart(builder):
    builder.StartObject(5)

def Start(builder):
    VAMApprovedAlternateStart(builder)

def VAMApprovedAlternateAddVARIANT_ID(builder, VARIANT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(VARIANT_ID), 0)

def AddVARIANT_ID(builder, VARIANT_ID):
    VAMApprovedAlternateAddVARIANT_ID(builder, VARIANT_ID)

def VAMApprovedAlternateAddCID(builder, CID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CID), 0)

def AddCID(builder, CID):
    VAMApprovedAlternateAddCID(builder, CID)

def VAMApprovedAlternateAddBYTE_SHA256(builder, BYTE_SHA256):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(BYTE_SHA256), 0)

def AddBYTE_SHA256(builder, BYTE_SHA256):
    VAMApprovedAlternateAddBYTE_SHA256(builder, BYTE_SHA256)

def VAMApprovedAlternateAddREVIEWED_TRANSFORM(builder, REVIEWED_TRANSFORM):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(REVIEWED_TRANSFORM), 0)

def AddREVIEWED_TRANSFORM(builder, REVIEWED_TRANSFORM):
    VAMApprovedAlternateAddREVIEWED_TRANSFORM(builder, REVIEWED_TRANSFORM)

def VAMApprovedAlternateAddRANK(builder, RANK):
    builder.PrependUint32Slot(4, RANK, 0)

def AddRANK(builder, RANK):
    VAMApprovedAlternateAddRANK(builder, RANK)

def VAMApprovedAlternateEnd(builder):
    return builder.EndObject()

def End(builder):
    return VAMApprovedAlternateEnd(builder)

import VAMTransform
try:
    from typing import Optional
except:
    pass

class VAMApprovedAlternateT(object):

    # VAMApprovedAlternateT
    def __init__(
        self,
        VARIANT_ID = None,
        CID = None,
        BYTE_SHA256 = None,
        REVIEWED_TRANSFORM = None,
        RANK = 0,
    ):
        self.VARIANT_ID = VARIANT_ID  # type: Optional[str]
        self.CID = CID  # type: Optional[str]
        self.BYTE_SHA256 = BYTE_SHA256  # type: Optional[str]
        self.REVIEWED_TRANSFORM = REVIEWED_TRANSFORM  # type: Optional[VAMTransform.VAMTransformT]
        self.RANK = RANK  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVamapprovedAlternate = VAMApprovedAlternate()
        tmpVamapprovedAlternate.Init(buf, pos)
        return cls.InitFromObj(tmpVamapprovedAlternate)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVamapprovedAlternate):
        x = VAMApprovedAlternateT()
        x._UnPack(tmpVamapprovedAlternate)
        return x

    # VAMApprovedAlternateT
    def _UnPack(self, VAMApprovedAlternate):
        if VAMApprovedAlternate is None:
            return
        self.VARIANT_ID = VAMApprovedAlternate.VARIANT_ID()
        self.CID = VAMApprovedAlternate.CID()
        self.BYTE_SHA256 = VAMApprovedAlternate.BYTE_SHA256()
        if VAMApprovedAlternate.REVIEWED_TRANSFORM() is not None:
            self.REVIEWED_TRANSFORM = VAMTransform.VAMTransformT.InitFromObj(VAMApprovedAlternate.REVIEWED_TRANSFORM())
        self.RANK = VAMApprovedAlternate.RANK()

    # VAMApprovedAlternateT
    def Pack(self, builder):
        if self.VARIANT_ID is not None:
            VARIANT_ID = builder.CreateString(self.VARIANT_ID)
        if self.CID is not None:
            CID = builder.CreateString(self.CID)
        if self.BYTE_SHA256 is not None:
            BYTE_SHA256 = builder.CreateString(self.BYTE_SHA256)
        if self.REVIEWED_TRANSFORM is not None:
            REVIEWED_TRANSFORM = self.REVIEWED_TRANSFORM.Pack(builder)
        VAMApprovedAlternateStart(builder)
        if self.VARIANT_ID is not None:
            VAMApprovedAlternateAddVARIANT_ID(builder, VARIANT_ID)
        if self.CID is not None:
            VAMApprovedAlternateAddCID(builder, CID)
        if self.BYTE_SHA256 is not None:
            VAMApprovedAlternateAddBYTE_SHA256(builder, BYTE_SHA256)
        if self.REVIEWED_TRANSFORM is not None:
            VAMApprovedAlternateAddREVIEWED_TRANSFORM(builder, REVIEWED_TRANSFORM)
        VAMApprovedAlternateAddRANK(builder, self.RANK)
        VAMApprovedAlternate = VAMApprovedAlternateEnd(builder)
        return VAMApprovedAlternate
