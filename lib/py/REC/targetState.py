# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Target state input (40 bytes, 8-byte aligned).
class targetState(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 40

    # targetState
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Target position X in ECEF (meters).
    # targetState
    def POS_X(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # Target position Y in ECEF (meters).
    # targetState
    def POS_Y(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))
    # Target position Z in ECEF (meters).
    # targetState
    def POS_Z(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(16))
    # Radar cross section (m^2).
    # targetState
    def RCS(self): return self._tab.Get(flatbuffers.number_types.Float64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(24))
    # Unique target identifier.
    # targetState
    def TARGET_ID(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(32))
    # Target flags (reserved).
    # targetState
    def FLAGS(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(36))

def CreatetargetState(builder, POS_X, POS_Y, POS_Z, RCS, TARGET_ID, FLAGS):
    builder.Prep(8, 40)
    builder.PrependUint32(FLAGS)
    builder.PrependUint32(TARGET_ID)
    builder.PrependFloat64(RCS)
    builder.PrependFloat64(POS_Z)
    builder.PrependFloat64(POS_Y)
    builder.PrependFloat64(POS_X)
    return builder.Offset()


class targetStateT(object):

    # targetStateT
    def __init__(
        self,
        POS_X = 0.0,
        POS_Y = 0.0,
        POS_Z = 0.0,
        RCS = 0.0,
        TARGET_ID = 0,
        FLAGS = 0,
    ):
        self.POS_X = POS_X  # type: float
        self.POS_Y = POS_Y  # type: float
        self.POS_Z = POS_Z  # type: float
        self.RCS = RCS  # type: float
        self.TARGET_ID = TARGET_ID  # type: int
        self.FLAGS = FLAGS  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTargetState = targetState()
        tmpTargetState.Init(buf, pos)
        return cls.InitFromObj(tmpTargetState)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTargetState):
        x = targetStateT()
        x._UnPack(tmpTargetState)
        return x

    # targetStateT
    def _UnPack(self, targetState):
        if targetState is None:
            return
        self.POS_X = targetState.POS_X()
        self.POS_Y = targetState.POS_Y()
        self.POS_Z = targetState.POS_Z()
        self.RCS = targetState.RCS()
        self.TARGET_ID = targetState.TARGET_ID()
        self.FLAGS = targetState.FLAGS()

    # targetStateT
    def Pack(self, builder):
        return CreatetargetState(builder, self.POS_X, self.POS_Y, self.POS_Z, self.RCS, self.TARGET_ID, self.FLAGS)
