# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One uplink transmitter frequency ramp applying over a closed time interval.
#
# SDS EXTENSION beyond CCSDS 503.0-B-1: the base standard carries a single
# TRANSMIT_FREQ_1 per segment, which cannot express a ramped uplink. Deep-space
# radiometric archives carry an explicit ramp table and the Doppler observables
# are NOT reconstructible without it. Ramps are optional; a record that omits
# TRANSMIT_RAMPS is exactly a CCSDS-conformant TDM.
#
# Frequency over the interval is the linear polynomial
#   f(t) = FREQUENCY_HZ + FREQUENCY_RATE_HZ_PER_S * (t - REFERENCE_TIME)
# which is the common form of the two archive representations: DSN ODF
# (TRK-2-18, table 3-5) supplies ramp start frequency, ramp rate, and ramp
# start/end time, so REFERENCE_TIME equals START_TIME; ESA IFMS supplies a
# ramp reference time with constant and linear transmission-frequency terms,
# so REFERENCE_TIME is that reference time and may precede START_TIME.
# Producers MUST set REFERENCE_TIME explicitly rather than letting a consumer
# assume which convention was used.
class TDMTransmitRamp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TDMTransmitRamp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTDMTransmitRamp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TDMTransmitRampBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x44\x4D", size_prefixed=size_prefixed)

    # TDMTransmitRamp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Start of the interval over which this ramp applies, ISO 8601.
    # TDMTransmitRamp
    def START_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # End of the interval over which this ramp applies, ISO 8601.
    # TDMTransmitRamp
    def END_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Epoch at which FREQUENCY_HZ is the instantaneous value, ISO 8601.
    # TDMTransmitRamp
    def REFERENCE_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Transmitted frequency at REFERENCE_TIME, Hz.
    # TDMTransmitRamp
    def FREQUENCY_HZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Constant ramp rate over the interval, Hz per second.
    # TDMTransmitRamp
    def FREQUENCY_RATE_HZ_PER_S(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Identifier of the transmitting station the ramp applies to. Must match
    # the PARTICIPANT_n naming used by the segment when both are present.
    # TDMTransmitRamp
    def TRANSMITTING_STATION_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Uplink band the ramp applies to, using the same vocabulary as
    # TRANSMIT_BAND (e.g. "S", "X", "Ka", "Ku").
    # TDMTransmitRamp
    def TRANSMIT_BAND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TDMTransmitRampStart(builder):
    builder.StartObject(7)

def Start(builder):
    TDMTransmitRampStart(builder)

def TDMTransmitRampAddSTART_TIME(builder, START_TIME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(START_TIME), 0)

def AddSTART_TIME(builder, START_TIME):
    TDMTransmitRampAddSTART_TIME(builder, START_TIME)

def TDMTransmitRampAddEND_TIME(builder, END_TIME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(END_TIME), 0)

def AddEND_TIME(builder, END_TIME):
    TDMTransmitRampAddEND_TIME(builder, END_TIME)

def TDMTransmitRampAddREFERENCE_TIME(builder, REFERENCE_TIME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(REFERENCE_TIME), 0)

def AddREFERENCE_TIME(builder, REFERENCE_TIME):
    TDMTransmitRampAddREFERENCE_TIME(builder, REFERENCE_TIME)

def TDMTransmitRampAddFREQUENCY_HZ(builder, FREQUENCY_HZ):
    builder.PrependFloat64Slot(3, FREQUENCY_HZ, 0.0)

def AddFREQUENCY_HZ(builder, FREQUENCY_HZ):
    TDMTransmitRampAddFREQUENCY_HZ(builder, FREQUENCY_HZ)

def TDMTransmitRampAddFREQUENCY_RATE_HZ_PER_S(builder, FREQUENCY_RATE_HZ_PER_S):
    builder.PrependFloat64Slot(4, FREQUENCY_RATE_HZ_PER_S, 0.0)

def AddFREQUENCY_RATE_HZ_PER_S(builder, FREQUENCY_RATE_HZ_PER_S):
    TDMTransmitRampAddFREQUENCY_RATE_HZ_PER_S(builder, FREQUENCY_RATE_HZ_PER_S)

def TDMTransmitRampAddTRANSMITTING_STATION_ID(builder, TRANSMITTING_STATION_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(TRANSMITTING_STATION_ID), 0)

def AddTRANSMITTING_STATION_ID(builder, TRANSMITTING_STATION_ID):
    TDMTransmitRampAddTRANSMITTING_STATION_ID(builder, TRANSMITTING_STATION_ID)

def TDMTransmitRampAddTRANSMIT_BAND(builder, TRANSMIT_BAND):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(TRANSMIT_BAND), 0)

def AddTRANSMIT_BAND(builder, TRANSMIT_BAND):
    TDMTransmitRampAddTRANSMIT_BAND(builder, TRANSMIT_BAND)

def TDMTransmitRampEnd(builder):
    return builder.EndObject()

def End(builder):
    return TDMTransmitRampEnd(builder)


class TDMTransmitRampT(object):

    # TDMTransmitRampT
    def __init__(
        self,
        START_TIME = None,
        END_TIME = None,
        REFERENCE_TIME = None,
        FREQUENCY_HZ = 0.0,
        FREQUENCY_RATE_HZ_PER_S = 0.0,
        TRANSMITTING_STATION_ID = None,
        TRANSMIT_BAND = None,
    ):
        self.START_TIME = START_TIME  # type: Optional[str]
        self.END_TIME = END_TIME  # type: Optional[str]
        self.REFERENCE_TIME = REFERENCE_TIME  # type: Optional[str]
        self.FREQUENCY_HZ = FREQUENCY_HZ  # type: float
        self.FREQUENCY_RATE_HZ_PER_S = FREQUENCY_RATE_HZ_PER_S  # type: float
        self.TRANSMITTING_STATION_ID = TRANSMITTING_STATION_ID  # type: Optional[str]
        self.TRANSMIT_BAND = TRANSMIT_BAND  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTdmtransmitRamp = TDMTransmitRamp()
        tmpTdmtransmitRamp.Init(buf, pos)
        return cls.InitFromObj(tmpTdmtransmitRamp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTdmtransmitRamp):
        x = TDMTransmitRampT()
        x._UnPack(tmpTdmtransmitRamp)
        return x

    # TDMTransmitRampT
    def _UnPack(self, TDMTransmitRamp):
        if TDMTransmitRamp is None:
            return
        self.START_TIME = TDMTransmitRamp.START_TIME()
        self.END_TIME = TDMTransmitRamp.END_TIME()
        self.REFERENCE_TIME = TDMTransmitRamp.REFERENCE_TIME()
        self.FREQUENCY_HZ = TDMTransmitRamp.FREQUENCY_HZ()
        self.FREQUENCY_RATE_HZ_PER_S = TDMTransmitRamp.FREQUENCY_RATE_HZ_PER_S()
        self.TRANSMITTING_STATION_ID = TDMTransmitRamp.TRANSMITTING_STATION_ID()
        self.TRANSMIT_BAND = TDMTransmitRamp.TRANSMIT_BAND()

    # TDMTransmitRampT
    def Pack(self, builder):
        if self.START_TIME is not None:
            START_TIME = builder.CreateString(self.START_TIME)
        if self.END_TIME is not None:
            END_TIME = builder.CreateString(self.END_TIME)
        if self.REFERENCE_TIME is not None:
            REFERENCE_TIME = builder.CreateString(self.REFERENCE_TIME)
        if self.TRANSMITTING_STATION_ID is not None:
            TRANSMITTING_STATION_ID = builder.CreateString(self.TRANSMITTING_STATION_ID)
        if self.TRANSMIT_BAND is not None:
            TRANSMIT_BAND = builder.CreateString(self.TRANSMIT_BAND)
        TDMTransmitRampStart(builder)
        if self.START_TIME is not None:
            TDMTransmitRampAddSTART_TIME(builder, START_TIME)
        if self.END_TIME is not None:
            TDMTransmitRampAddEND_TIME(builder, END_TIME)
        if self.REFERENCE_TIME is not None:
            TDMTransmitRampAddREFERENCE_TIME(builder, REFERENCE_TIME)
        TDMTransmitRampAddFREQUENCY_HZ(builder, self.FREQUENCY_HZ)
        TDMTransmitRampAddFREQUENCY_RATE_HZ_PER_S(builder, self.FREQUENCY_RATE_HZ_PER_S)
        if self.TRANSMITTING_STATION_ID is not None:
            TDMTransmitRampAddTRANSMITTING_STATION_ID(builder, TRANSMITTING_STATION_ID)
        if self.TRANSMIT_BAND is not None:
            TDMTransmitRampAddTRANSMIT_BAND(builder, TRANSMIT_BAND)
        TDMTransmitRamp = TDMTransmitRampEnd(builder)
        return TDMTransmitRamp
