# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Impulsive change at TDB epoch; delta velocity is m/s in the named basis.
class PRWImpulse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWImpulse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWImpulse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWImpulseBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWImpulse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWImpulse
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TIMInstant import TIMInstant
            obj = TIMInstant()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWImpulse
    def DELTA_V(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FRMVector3 import FRMVector3
            obj = FRMVector3()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWImpulse
    def VECTOR_BASIS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def PRWImpulseStart(builder):
    builder.StartObject(3)

def Start(builder):
    PRWImpulseStart(builder)

def PRWImpulseAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    PRWImpulseAddEPOCH(builder, EPOCH)

def PRWImpulseAddDELTA_V(builder, DELTA_V):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(DELTA_V), 0)

def AddDELTA_V(builder, DELTA_V):
    PRWImpulseAddDELTA_V(builder, DELTA_V)

def PRWImpulseAddVECTOR_BASIS(builder, VECTOR_BASIS):
    builder.PrependUint8Slot(2, VECTOR_BASIS, 0)

def AddVECTOR_BASIS(builder, VECTOR_BASIS):
    PRWImpulseAddVECTOR_BASIS(builder, VECTOR_BASIS)

def PRWImpulseEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWImpulseEnd(builder)

import FRMVector3
import TIMInstant
try:
    from typing import Optional
except:
    pass

class PRWImpulseT(object):

    # PRWImpulseT
    def __init__(
        self,
        EPOCH = None,
        DELTA_V = None,
        VECTOR_BASIS = 0,
    ):
        self.EPOCH = EPOCH  # type: Optional[TIMInstant.TIMInstantT]
        self.DELTA_V = DELTA_V  # type: Optional[FRMVector3.FRMVector3T]
        self.VECTOR_BASIS = VECTOR_BASIS  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwimpulse = PRWImpulse()
        tmpPrwimpulse.Init(buf, pos)
        return cls.InitFromObj(tmpPrwimpulse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwimpulse):
        x = PRWImpulseT()
        x._UnPack(tmpPrwimpulse)
        return x

    # PRWImpulseT
    def _UnPack(self, PRWImpulse):
        if PRWImpulse is None:
            return
        if PRWImpulse.EPOCH() is not None:
            self.EPOCH = TIMInstant.TIMInstantT.InitFromObj(PRWImpulse.EPOCH())
        if PRWImpulse.DELTA_V() is not None:
            self.DELTA_V = FRMVector3.FRMVector3T.InitFromObj(PRWImpulse.DELTA_V())
        self.VECTOR_BASIS = PRWImpulse.VECTOR_BASIS()

    # PRWImpulseT
    def Pack(self, builder):
        if self.EPOCH is not None:
            EPOCH = self.EPOCH.Pack(builder)
        if self.DELTA_V is not None:
            DELTA_V = self.DELTA_V.Pack(builder)
        PRWImpulseStart(builder)
        if self.EPOCH is not None:
            PRWImpulseAddEPOCH(builder, EPOCH)
        if self.DELTA_V is not None:
            PRWImpulseAddDELTA_V(builder, DELTA_V)
        PRWImpulseAddVECTOR_BASIS(builder, self.VECTOR_BASIS)
        PRWImpulse = PRWImpulseEnd(builder)
        return PRWImpulse
