# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Access Interval
class ACI(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ACI()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsACI(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ACIBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x43\x49", size_prefixed=size_prefixed)

    # ACI
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier of this interval set.
    # ACI
    def ACI_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Display name.
    # ACI
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Analysis or scenario grouping identifier shared with the `$RFL` samples
    # and `$CVP` geometry of the same study.
    # ACI
    def SCENARIO_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$RFL.RFL_ID` these intervals were derived from, when they were derived
    # from a published sample set. Absent for measured or scheduled intervals.
    # ACI
    def RFL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The intervals.
    # ACI
    def INTERVALS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ACIInterval import ACIInterval
            obj = ACIInterval()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ACI
    def INTERVALSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ACI
    def INTERVALSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Analysis window, seconds since 1970-01-01T00:00:00 in TIME_SYSTEM. An
    # interval set states its window so "no intervals" can be distinguished
    # from "not analysed".
    # ACI
    def WINDOW_START(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ACI
    def WINDOW_STOP(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Time scale all epochs in this record are expressed in. Defaults to UTC
    # explicitly: `timingStandard` ordinal 0 is GMST.
    # ACI
    def TIME_SYSTEM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 11

    # Provenance. Required.
    # ACI
    def PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from ACIProvenance import ACIProvenance
            obj = ACIProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Unix ms this record was serialized.
    # ACI
    def COMPUTED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # `$EPM` identifier of the producing node.
    # ACI
    def PRODUCER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Ed25519 signature by the producing `$EPM`.
    # ACI
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # ACI
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # ACI
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ACI
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

def ACIStart(builder):
    builder.StartObject(12)

def Start(builder):
    ACIStart(builder)

def ACIAddACI_ID(builder, ACI_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ACI_ID), 0)

def AddACI_ID(builder, ACI_ID):
    ACIAddACI_ID(builder, ACI_ID)

def ACIAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    ACIAddNAME(builder, NAME)

def ACIAddSCENARIO_ID(builder, SCENARIO_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SCENARIO_ID), 0)

def AddSCENARIO_ID(builder, SCENARIO_ID):
    ACIAddSCENARIO_ID(builder, SCENARIO_ID)

def ACIAddRFL_ID(builder, RFL_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(RFL_ID), 0)

def AddRFL_ID(builder, RFL_ID):
    ACIAddRFL_ID(builder, RFL_ID)

def ACIAddINTERVALS(builder, INTERVALS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(INTERVALS), 0)

def AddINTERVALS(builder, INTERVALS):
    ACIAddINTERVALS(builder, INTERVALS)

def ACIStartINTERVALSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartINTERVALSVector(builder, numElems):
    return ACIStartINTERVALSVector(builder, numElems)

def ACICreateINTERVALSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateINTERVALSVector(builder, data):
    ACICreateINTERVALSVector(builder, data)

def ACIAddWINDOW_START(builder, WINDOW_START):
    builder.PrependFloat64Slot(5, WINDOW_START, 0.0)

def AddWINDOW_START(builder, WINDOW_START):
    ACIAddWINDOW_START(builder, WINDOW_START)

def ACIAddWINDOW_STOP(builder, WINDOW_STOP):
    builder.PrependFloat64Slot(6, WINDOW_STOP, 0.0)

def AddWINDOW_STOP(builder, WINDOW_STOP):
    ACIAddWINDOW_STOP(builder, WINDOW_STOP)

def ACIAddTIME_SYSTEM(builder, TIME_SYSTEM):
    builder.PrependInt8Slot(7, TIME_SYSTEM, 11)

def AddTIME_SYSTEM(builder, TIME_SYSTEM):
    ACIAddTIME_SYSTEM(builder, TIME_SYSTEM)

def ACIAddPROVENANCE(builder, PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(PROVENANCE), 0)

def AddPROVENANCE(builder, PROVENANCE):
    ACIAddPROVENANCE(builder, PROVENANCE)

def ACIAddCOMPUTED_AT(builder, COMPUTED_AT):
    builder.PrependUint64Slot(9, COMPUTED_AT, 0)

def AddCOMPUTED_AT(builder, COMPUTED_AT):
    ACIAddCOMPUTED_AT(builder, COMPUTED_AT)

def ACIAddPRODUCER_ID(builder, PRODUCER_ID):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(PRODUCER_ID), 0)

def AddPRODUCER_ID(builder, PRODUCER_ID):
    ACIAddPRODUCER_ID(builder, PRODUCER_ID)

def ACIAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    ACIAddSIGNATURE(builder, SIGNATURE)

def ACIStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return ACIStartSIGNATUREVector(builder, numElems)

def ACICreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    ACICreateSIGNATUREVector(builder, data)

def ACIEnd(builder):
    return builder.EndObject()

def End(builder):
    return ACIEnd(builder)

import ACIInterval
import ACIProvenance
try:
    from typing import List, Optional
except:
    pass

class ACIT(object):

    # ACIT
    def __init__(
        self,
        ACI_ID = None,
        NAME = None,
        SCENARIO_ID = None,
        RFL_ID = None,
        INTERVALS = None,
        WINDOW_START = 0.0,
        WINDOW_STOP = 0.0,
        TIME_SYSTEM = 11,
        PROVENANCE = None,
        COMPUTED_AT = 0,
        PRODUCER_ID = None,
        SIGNATURE = None,
    ):
        self.ACI_ID = ACI_ID  # type: Optional[str]
        self.NAME = NAME  # type: Optional[str]
        self.SCENARIO_ID = SCENARIO_ID  # type: Optional[str]
        self.RFL_ID = RFL_ID  # type: Optional[str]
        self.INTERVALS = INTERVALS  # type: Optional[List[ACIInterval.ACIIntervalT]]
        self.WINDOW_START = WINDOW_START  # type: float
        self.WINDOW_STOP = WINDOW_STOP  # type: float
        self.TIME_SYSTEM = TIME_SYSTEM  # type: int
        self.PROVENANCE = PROVENANCE  # type: Optional[ACIProvenance.ACIProvenanceT]
        self.COMPUTED_AT = COMPUTED_AT  # type: int
        self.PRODUCER_ID = PRODUCER_ID  # type: Optional[str]
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAci = ACI()
        tmpAci.Init(buf, pos)
        return cls.InitFromObj(tmpAci)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAci):
        x = ACIT()
        x._UnPack(tmpAci)
        return x

    # ACIT
    def _UnPack(self, ACI):
        if ACI is None:
            return
        self.ACI_ID = ACI.ACI_ID()
        self.NAME = ACI.NAME()
        self.SCENARIO_ID = ACI.SCENARIO_ID()
        self.RFL_ID = ACI.RFL_ID()
        if not ACI.INTERVALSIsNone():
            self.INTERVALS = []
            for i in range(ACI.INTERVALSLength()):
                if ACI.INTERVALS(i) is None:
                    self.INTERVALS.append(None)
                else:
                    aCIInterval_ = ACIInterval.ACIIntervalT.InitFromObj(ACI.INTERVALS(i))
                    self.INTERVALS.append(aCIInterval_)
        self.WINDOW_START = ACI.WINDOW_START()
        self.WINDOW_STOP = ACI.WINDOW_STOP()
        self.TIME_SYSTEM = ACI.TIME_SYSTEM()
        if ACI.PROVENANCE() is not None:
            self.PROVENANCE = ACIProvenance.ACIProvenanceT.InitFromObj(ACI.PROVENANCE())
        self.COMPUTED_AT = ACI.COMPUTED_AT()
        self.PRODUCER_ID = ACI.PRODUCER_ID()
        if not ACI.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(ACI.SIGNATURELength()):
                    self.SIGNATURE.append(ACI.SIGNATURE(i))
            else:
                self.SIGNATURE = ACI.SIGNATUREAsNumpy()

    # ACIT
    def Pack(self, builder):
        if self.ACI_ID is not None:
            ACI_ID = builder.CreateString(self.ACI_ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.SCENARIO_ID is not None:
            SCENARIO_ID = builder.CreateString(self.SCENARIO_ID)
        if self.RFL_ID is not None:
            RFL_ID = builder.CreateString(self.RFL_ID)
        if self.INTERVALS is not None:
            INTERVALSlist = []
            for i in range(len(self.INTERVALS)):
                INTERVALSlist.append(self.INTERVALS[i].Pack(builder))
            ACIStartINTERVALSVector(builder, len(self.INTERVALS))
            for i in reversed(range(len(self.INTERVALS))):
                builder.PrependUOffsetTRelative(INTERVALSlist[i])
            INTERVALS = builder.EndVector()
        if self.PROVENANCE is not None:
            PROVENANCE = self.PROVENANCE.Pack(builder)
        if self.PRODUCER_ID is not None:
            PRODUCER_ID = builder.CreateString(self.PRODUCER_ID)
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                ACIStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        ACIStart(builder)
        if self.ACI_ID is not None:
            ACIAddACI_ID(builder, ACI_ID)
        if self.NAME is not None:
            ACIAddNAME(builder, NAME)
        if self.SCENARIO_ID is not None:
            ACIAddSCENARIO_ID(builder, SCENARIO_ID)
        if self.RFL_ID is not None:
            ACIAddRFL_ID(builder, RFL_ID)
        if self.INTERVALS is not None:
            ACIAddINTERVALS(builder, INTERVALS)
        ACIAddWINDOW_START(builder, self.WINDOW_START)
        ACIAddWINDOW_STOP(builder, self.WINDOW_STOP)
        ACIAddTIME_SYSTEM(builder, self.TIME_SYSTEM)
        if self.PROVENANCE is not None:
            ACIAddPROVENANCE(builder, PROVENANCE)
        ACIAddCOMPUTED_AT(builder, self.COMPUTED_AT)
        if self.PRODUCER_ID is not None:
            ACIAddPRODUCER_ID(builder, PRODUCER_ID)
        if self.SIGNATURE is not None:
            ACIAddSIGNATURE(builder, SIGNATURE)
        ACI = ACIEnd(builder)
        return ACI
