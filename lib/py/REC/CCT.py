# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# $CCT — Capability Category Taxonomy.
#
# The ratified set of capability classes that distributable units (modules,
# applications, composed flows) are classified under, together with the labels
# and routes every consuming surface renders. One published $CCT is the single
# source of truth shared by a storefront, a library, a search index and the
# unit manifests themselves.
#
# Division of labour: `$CCT` = the category vocabulary and its presentation;
# `$PLG` = one module's listing, which cites categories by code; `$APP` = one
# application's manifest, which cites categories by code; `$PMM` = which
# modules a provider serves; `$STO`/`$STF` = commerce.
class CCT(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CCT()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCCT(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CCTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x43\x54", size_prefixed=size_prefixed)

    # CCT
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier of this taxonomy publication.
    # CCT
    def TAXONOMY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SemVer 2.0.0 version of the taxonomy content.
    # CCT
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Unix seconds when this taxonomy revision was issued.
    # CCT
    def ISSUED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Human-readable title of the taxonomy.
    # CCT
    def TITLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The ratified categories. Every capabilityClass member a consumer may
    # encounter SHOULD appear exactly once; a code appearing twice is invalid.
    # CCT
    def CATEGORIES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from CCTCategory import CCTCategory
            obj = CCTCategory()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CCT
    def CATEGORIESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CCT
    def CATEGORIESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Optional per-category item counts as observed over a named catalogue.
    # Absent means counts are computed by the consumer.
    # CCT
    def ROLLUPS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from CCTCategoryRollup import CCTCategoryRollup
            obj = CCTCategoryRollup()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CCT
    def ROLLUPSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CCT
    def ROLLUPSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Signature from the publishing node key over the canonical taxonomy bytes.
    # CCT
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CCT
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # CCT
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CCT
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def CCTStart(builder):
    builder.StartObject(7)

def Start(builder):
    CCTStart(builder)

def CCTAddTAXONOMY_ID(builder, TAXONOMY_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(TAXONOMY_ID), 0)

def AddTAXONOMY_ID(builder, TAXONOMY_ID):
    CCTAddTAXONOMY_ID(builder, TAXONOMY_ID)

def CCTAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    CCTAddVERSION(builder, VERSION)

def CCTAddISSUED_AT(builder, ISSUED_AT):
    builder.PrependUint64Slot(2, ISSUED_AT, 0)

def AddISSUED_AT(builder, ISSUED_AT):
    CCTAddISSUED_AT(builder, ISSUED_AT)

def CCTAddTITLE(builder, TITLE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(TITLE), 0)

def AddTITLE(builder, TITLE):
    CCTAddTITLE(builder, TITLE)

def CCTAddCATEGORIES(builder, CATEGORIES):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(CATEGORIES), 0)

def AddCATEGORIES(builder, CATEGORIES):
    CCTAddCATEGORIES(builder, CATEGORIES)

def CCTStartCATEGORIESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCATEGORIESVector(builder, numElems):
    return CCTStartCATEGORIESVector(builder, numElems)

def CCTCreateCATEGORIESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCATEGORIESVector(builder, data):
    CCTCreateCATEGORIESVector(builder, data)

def CCTAddROLLUPS(builder, ROLLUPS):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ROLLUPS), 0)

def AddROLLUPS(builder, ROLLUPS):
    CCTAddROLLUPS(builder, ROLLUPS)

def CCTStartROLLUPSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartROLLUPSVector(builder, numElems):
    return CCTStartROLLUPSVector(builder, numElems)

def CCTCreateROLLUPSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateROLLUPSVector(builder, data):
    CCTCreateROLLUPSVector(builder, data)

def CCTAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    CCTAddSIGNATURE(builder, SIGNATURE)

def CCTStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return CCTStartSIGNATUREVector(builder, numElems)

def CCTCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    CCTCreateSIGNATUREVector(builder, data)

def CCTEnd(builder):
    return builder.EndObject()

def End(builder):
    return CCTEnd(builder)

import CCTCategory
import CCTCategoryRollup
try:
    from typing import List
except:
    pass

class CCTT(object):

    # CCTT
    def __init__(
        self,
        TAXONOMY_ID = None,
        VERSION = None,
        ISSUED_AT = 0,
        TITLE = None,
        CATEGORIES = None,
        ROLLUPS = None,
        SIGNATURE = None,
    ):
        self.TAXONOMY_ID = TAXONOMY_ID  # type: Optional[str]
        self.VERSION = VERSION  # type: Optional[str]
        self.ISSUED_AT = ISSUED_AT  # type: int
        self.TITLE = TITLE  # type: Optional[str]
        self.CATEGORIES = CATEGORIES  # type: Optional[List[CCTCategory.CCTCategoryT]]
        self.ROLLUPS = ROLLUPS  # type: Optional[List[CCTCategoryRollup.CCTCategoryRollupT]]
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCct = CCT()
        tmpCct.Init(buf, pos)
        return cls.InitFromObj(tmpCct)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCct):
        x = CCTT()
        x._UnPack(tmpCct)
        return x

    # CCTT
    def _UnPack(self, CCT):
        if CCT is None:
            return
        self.TAXONOMY_ID = CCT.TAXONOMY_ID()
        self.VERSION = CCT.VERSION()
        self.ISSUED_AT = CCT.ISSUED_AT()
        self.TITLE = CCT.TITLE()
        if not CCT.CATEGORIESIsNone():
            self.CATEGORIES = []
            for i in range(CCT.CATEGORIESLength()):
                if CCT.CATEGORIES(i) is None:
                    self.CATEGORIES.append(None)
                else:
                    cCTCategory_ = CCTCategory.CCTCategoryT.InitFromObj(CCT.CATEGORIES(i))
                    self.CATEGORIES.append(cCTCategory_)
        if not CCT.ROLLUPSIsNone():
            self.ROLLUPS = []
            for i in range(CCT.ROLLUPSLength()):
                if CCT.ROLLUPS(i) is None:
                    self.ROLLUPS.append(None)
                else:
                    cCTCategoryRollup_ = CCTCategoryRollup.CCTCategoryRollupT.InitFromObj(CCT.ROLLUPS(i))
                    self.ROLLUPS.append(cCTCategoryRollup_)
        if not CCT.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(CCT.SIGNATURELength()):
                    self.SIGNATURE.append(CCT.SIGNATURE(i))
            else:
                self.SIGNATURE = CCT.SIGNATUREAsNumpy()

    # CCTT
    def Pack(self, builder):
        if self.TAXONOMY_ID is not None:
            TAXONOMY_ID = builder.CreateString(self.TAXONOMY_ID)
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        if self.TITLE is not None:
            TITLE = builder.CreateString(self.TITLE)
        if self.CATEGORIES is not None:
            CATEGORIESlist = []
            for i in range(len(self.CATEGORIES)):
                CATEGORIESlist.append(self.CATEGORIES[i].Pack(builder))
            CCTStartCATEGORIESVector(builder, len(self.CATEGORIES))
            for i in reversed(range(len(self.CATEGORIES))):
                builder.PrependUOffsetTRelative(CATEGORIESlist[i])
            CATEGORIES = builder.EndVector()
        if self.ROLLUPS is not None:
            ROLLUPSlist = []
            for i in range(len(self.ROLLUPS)):
                ROLLUPSlist.append(self.ROLLUPS[i].Pack(builder))
            CCTStartROLLUPSVector(builder, len(self.ROLLUPS))
            for i in reversed(range(len(self.ROLLUPS))):
                builder.PrependUOffsetTRelative(ROLLUPSlist[i])
            ROLLUPS = builder.EndVector()
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                CCTStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        CCTStart(builder)
        if self.TAXONOMY_ID is not None:
            CCTAddTAXONOMY_ID(builder, TAXONOMY_ID)
        if self.VERSION is not None:
            CCTAddVERSION(builder, VERSION)
        CCTAddISSUED_AT(builder, self.ISSUED_AT)
        if self.TITLE is not None:
            CCTAddTITLE(builder, TITLE)
        if self.CATEGORIES is not None:
            CCTAddCATEGORIES(builder, CATEGORIES)
        if self.ROLLUPS is not None:
            CCTAddROLLUPS(builder, ROLLUPS)
        if self.SIGNATURE is not None:
            CCTAddSIGNATURE(builder, SIGNATURE)
        CCT = CCTEnd(builder)
        return CCT
