# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Compressed Packet Stream — a run of fixed-length CCSDS packets losslessly
# compressed as one stateful stream (CCSDS 124.0-B-1 POCKET+). Decoded output
# is a sequence of PACKET_COUNT packets of PACKET_LENGTH bytes, each of which
# projects to an $SPP record. This record carries a compressed packet stream
# only; it is not a general-purpose compression envelope for SDS records.
class CPS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CPS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCPS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CPSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x50\x53", size_prefixed=size_prefixed)

    # CPS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Compression algorithm used to produce COMPRESSED_DATA.
    # CPS
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
    # defined only for a fixed packet length; a length change starts a new
    # stream and therefore a new $CPS record.
    # CPS
    def PACKET_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
    # this is the only output-size bound a decoder has. The compressed
    # bitstream alone does not bound the decoded size, and a compression-ratio
    # guess is unsafe in both directions. Decoders MUST reject a record whose
    # PACKET_COUNT is zero, and MUST allocate exactly
    # PACKET_COUNT * PACKET_LENGTH bytes of output.
    # CPS
    def PACKET_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
    # a window depth, NOT a retransmission interval — it does not by itself
    # configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
    # REFERENCE_PERIOD, which are three independent periods.
    # CPS
    def ROBUSTNESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
    # MASK_SEND_PERIOD, and REFERENCE_PERIOD.
    # CPS
    def MASK_UPDATE_PERIOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # POCKET+ mask send period (ft), in packets — how often the change mask is
    # transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
    # REFERENCE_PERIOD.
    # CPS
    def MASK_SEND_PERIOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # POCKET+ reference packet retransmission period (rt), in packets.
    # Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
    # CPS
    def REFERENCE_PERIOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
    # ft=1, i.e. it transmits a full reference packet and a full mask, so this
    # record decodes standalone without its predecessor. There is no
    # synchronization flag in the POCKET+ bitstream itself: this field is the
    # producer's assertion about the first packet, and defaults to false.
    # CPS
    def SELF_SYNCHRONIZING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Producer-defined stream identity. Records sharing STREAM_ID decode in
    # SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
    # poisons every subsequent segment up to the next segment with
    # SELF_SYNCHRONIZING = true.
    # CPS
    def STREAM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Zero-based index of this segment within STREAM_ID.
    # CPS
    def SEGMENT_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # CCSDS Application Process Identifier of the packets, when known.
    # CPS
    def APID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Spacecraft identifier of the packets, when known.
    # CPS
    def SPACECRAFT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # UTC epoch of the first packet in the segment, ISO 8601.
    # CPS
    def START_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # UTC epoch of the last packet in the segment, ISO 8601.
    # CPS
    def STOP_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
    # packet is individually octet-aligned; padding is per PACKET, never per
    # stream. Bit-concatenating packets diverges from the reference
    # implementation and MUST NOT be done.
    # CPS
    def COMPRESSED_DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CPS
    def COMPRESSED_DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # CPS
    def COMPRESSED_DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CPS
    def COMPRESSED_DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

    # PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
    # output length without recomputing the product from a corrupt header.
    # CPS
    def UNCOMPRESSED_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
    # CPS
    def UNCOMPRESSED_CRC32(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def CPSStart(builder):
    builder.StartObject(17)

def Start(builder):
    CPSStart(builder)

def CPSAddALGORITHM(builder, ALGORITHM):
    builder.PrependUint8Slot(0, ALGORITHM, 0)

def AddALGORITHM(builder, ALGORITHM):
    CPSAddALGORITHM(builder, ALGORITHM)

def CPSAddPACKET_LENGTH(builder, PACKET_LENGTH):
    builder.PrependUint16Slot(1, PACKET_LENGTH, 0)

def AddPACKET_LENGTH(builder, PACKET_LENGTH):
    CPSAddPACKET_LENGTH(builder, PACKET_LENGTH)

def CPSAddPACKET_COUNT(builder, PACKET_COUNT):
    builder.PrependUint32Slot(2, PACKET_COUNT, 0)

def AddPACKET_COUNT(builder, PACKET_COUNT):
    CPSAddPACKET_COUNT(builder, PACKET_COUNT)

def CPSAddROBUSTNESS(builder, ROBUSTNESS):
    builder.PrependUint8Slot(3, ROBUSTNESS, 0)

def AddROBUSTNESS(builder, ROBUSTNESS):
    CPSAddROBUSTNESS(builder, ROBUSTNESS)

def CPSAddMASK_UPDATE_PERIOD(builder, MASK_UPDATE_PERIOD):
    builder.PrependUint16Slot(4, MASK_UPDATE_PERIOD, 0)

def AddMASK_UPDATE_PERIOD(builder, MASK_UPDATE_PERIOD):
    CPSAddMASK_UPDATE_PERIOD(builder, MASK_UPDATE_PERIOD)

def CPSAddMASK_SEND_PERIOD(builder, MASK_SEND_PERIOD):
    builder.PrependUint16Slot(5, MASK_SEND_PERIOD, 0)

def AddMASK_SEND_PERIOD(builder, MASK_SEND_PERIOD):
    CPSAddMASK_SEND_PERIOD(builder, MASK_SEND_PERIOD)

def CPSAddREFERENCE_PERIOD(builder, REFERENCE_PERIOD):
    builder.PrependUint16Slot(6, REFERENCE_PERIOD, 0)

def AddREFERENCE_PERIOD(builder, REFERENCE_PERIOD):
    CPSAddREFERENCE_PERIOD(builder, REFERENCE_PERIOD)

def CPSAddSELF_SYNCHRONIZING(builder, SELF_SYNCHRONIZING):
    builder.PrependBoolSlot(7, SELF_SYNCHRONIZING, 0)

def AddSELF_SYNCHRONIZING(builder, SELF_SYNCHRONIZING):
    CPSAddSELF_SYNCHRONIZING(builder, SELF_SYNCHRONIZING)

def CPSAddSTREAM_ID(builder, STREAM_ID):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(STREAM_ID), 0)

def AddSTREAM_ID(builder, STREAM_ID):
    CPSAddSTREAM_ID(builder, STREAM_ID)

def CPSAddSEGMENT_INDEX(builder, SEGMENT_INDEX):
    builder.PrependUint32Slot(9, SEGMENT_INDEX, 0)

def AddSEGMENT_INDEX(builder, SEGMENT_INDEX):
    CPSAddSEGMENT_INDEX(builder, SEGMENT_INDEX)

def CPSAddAPID(builder, APID):
    builder.PrependUint16Slot(10, APID, 0)

def AddAPID(builder, APID):
    CPSAddAPID(builder, APID)

def CPSAddSPACECRAFT_ID(builder, SPACECRAFT_ID):
    builder.PrependUint16Slot(11, SPACECRAFT_ID, 0)

def AddSPACECRAFT_ID(builder, SPACECRAFT_ID):
    CPSAddSPACECRAFT_ID(builder, SPACECRAFT_ID)

def CPSAddSTART_EPOCH(builder, START_EPOCH):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(START_EPOCH), 0)

def AddSTART_EPOCH(builder, START_EPOCH):
    CPSAddSTART_EPOCH(builder, START_EPOCH)

def CPSAddSTOP_EPOCH(builder, STOP_EPOCH):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(STOP_EPOCH), 0)

def AddSTOP_EPOCH(builder, STOP_EPOCH):
    CPSAddSTOP_EPOCH(builder, STOP_EPOCH)

def CPSAddCOMPRESSED_DATA(builder, COMPRESSED_DATA):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(COMPRESSED_DATA), 0)

def AddCOMPRESSED_DATA(builder, COMPRESSED_DATA):
    CPSAddCOMPRESSED_DATA(builder, COMPRESSED_DATA)

def CPSStartCOMPRESSED_DATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCOMPRESSED_DATAVector(builder, numElems):
    return CPSStartCOMPRESSED_DATAVector(builder, numElems)

def CPSCreateCOMPRESSED_DATAVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCOMPRESSED_DATAVector(builder, data):
    CPSCreateCOMPRESSED_DATAVector(builder, data)

def CPSAddUNCOMPRESSED_LENGTH(builder, UNCOMPRESSED_LENGTH):
    builder.PrependUint32Slot(15, UNCOMPRESSED_LENGTH, 0)

def AddUNCOMPRESSED_LENGTH(builder, UNCOMPRESSED_LENGTH):
    CPSAddUNCOMPRESSED_LENGTH(builder, UNCOMPRESSED_LENGTH)

def CPSAddUNCOMPRESSED_CRC32(builder, UNCOMPRESSED_CRC32):
    builder.PrependUint32Slot(16, UNCOMPRESSED_CRC32, 0)

def AddUNCOMPRESSED_CRC32(builder, UNCOMPRESSED_CRC32):
    CPSAddUNCOMPRESSED_CRC32(builder, UNCOMPRESSED_CRC32)

def CPSEnd(builder):
    return builder.EndObject()

def End(builder):
    return CPSEnd(builder)

try:
    from typing import List
except:
    pass

class CPST(object):

    # CPST
    def __init__(
        self,
        ALGORITHM = 0,
        PACKET_LENGTH = 0,
        PACKET_COUNT = 0,
        ROBUSTNESS = 0,
        MASK_UPDATE_PERIOD = 0,
        MASK_SEND_PERIOD = 0,
        REFERENCE_PERIOD = 0,
        SELF_SYNCHRONIZING = False,
        STREAM_ID = None,
        SEGMENT_INDEX = 0,
        APID = 0,
        SPACECRAFT_ID = 0,
        START_EPOCH = None,
        STOP_EPOCH = None,
        COMPRESSED_DATA = None,
        UNCOMPRESSED_LENGTH = 0,
        UNCOMPRESSED_CRC32 = 0,
    ):
        self.ALGORITHM = ALGORITHM  # type: int
        self.PACKET_LENGTH = PACKET_LENGTH  # type: int
        self.PACKET_COUNT = PACKET_COUNT  # type: int
        self.ROBUSTNESS = ROBUSTNESS  # type: int
        self.MASK_UPDATE_PERIOD = MASK_UPDATE_PERIOD  # type: int
        self.MASK_SEND_PERIOD = MASK_SEND_PERIOD  # type: int
        self.REFERENCE_PERIOD = REFERENCE_PERIOD  # type: int
        self.SELF_SYNCHRONIZING = SELF_SYNCHRONIZING  # type: bool
        self.STREAM_ID = STREAM_ID  # type: Optional[str]
        self.SEGMENT_INDEX = SEGMENT_INDEX  # type: int
        self.APID = APID  # type: int
        self.SPACECRAFT_ID = SPACECRAFT_ID  # type: int
        self.START_EPOCH = START_EPOCH  # type: Optional[str]
        self.STOP_EPOCH = STOP_EPOCH  # type: Optional[str]
        self.COMPRESSED_DATA = COMPRESSED_DATA  # type: Optional[List[int]]
        self.UNCOMPRESSED_LENGTH = UNCOMPRESSED_LENGTH  # type: int
        self.UNCOMPRESSED_CRC32 = UNCOMPRESSED_CRC32  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCps = CPS()
        tmpCps.Init(buf, pos)
        return cls.InitFromObj(tmpCps)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCps):
        x = CPST()
        x._UnPack(tmpCps)
        return x

    # CPST
    def _UnPack(self, CPS):
        if CPS is None:
            return
        self.ALGORITHM = CPS.ALGORITHM()
        self.PACKET_LENGTH = CPS.PACKET_LENGTH()
        self.PACKET_COUNT = CPS.PACKET_COUNT()
        self.ROBUSTNESS = CPS.ROBUSTNESS()
        self.MASK_UPDATE_PERIOD = CPS.MASK_UPDATE_PERIOD()
        self.MASK_SEND_PERIOD = CPS.MASK_SEND_PERIOD()
        self.REFERENCE_PERIOD = CPS.REFERENCE_PERIOD()
        self.SELF_SYNCHRONIZING = CPS.SELF_SYNCHRONIZING()
        self.STREAM_ID = CPS.STREAM_ID()
        self.SEGMENT_INDEX = CPS.SEGMENT_INDEX()
        self.APID = CPS.APID()
        self.SPACECRAFT_ID = CPS.SPACECRAFT_ID()
        self.START_EPOCH = CPS.START_EPOCH()
        self.STOP_EPOCH = CPS.STOP_EPOCH()
        if not CPS.COMPRESSED_DATAIsNone():
            if np is None:
                self.COMPRESSED_DATA = []
                for i in range(CPS.COMPRESSED_DATALength()):
                    self.COMPRESSED_DATA.append(CPS.COMPRESSED_DATA(i))
            else:
                self.COMPRESSED_DATA = CPS.COMPRESSED_DATAAsNumpy()
        self.UNCOMPRESSED_LENGTH = CPS.UNCOMPRESSED_LENGTH()
        self.UNCOMPRESSED_CRC32 = CPS.UNCOMPRESSED_CRC32()

    # CPST
    def Pack(self, builder):
        if self.STREAM_ID is not None:
            STREAM_ID = builder.CreateString(self.STREAM_ID)
        if self.START_EPOCH is not None:
            START_EPOCH = builder.CreateString(self.START_EPOCH)
        if self.STOP_EPOCH is not None:
            STOP_EPOCH = builder.CreateString(self.STOP_EPOCH)
        if self.COMPRESSED_DATA is not None:
            if np is not None and type(self.COMPRESSED_DATA) is np.ndarray:
                COMPRESSED_DATA = builder.CreateNumpyVector(self.COMPRESSED_DATA)
            else:
                CPSStartCOMPRESSED_DATAVector(builder, len(self.COMPRESSED_DATA))
                for i in reversed(range(len(self.COMPRESSED_DATA))):
                    builder.PrependUint8(self.COMPRESSED_DATA[i])
                COMPRESSED_DATA = builder.EndVector()
        CPSStart(builder)
        CPSAddALGORITHM(builder, self.ALGORITHM)
        CPSAddPACKET_LENGTH(builder, self.PACKET_LENGTH)
        CPSAddPACKET_COUNT(builder, self.PACKET_COUNT)
        CPSAddROBUSTNESS(builder, self.ROBUSTNESS)
        CPSAddMASK_UPDATE_PERIOD(builder, self.MASK_UPDATE_PERIOD)
        CPSAddMASK_SEND_PERIOD(builder, self.MASK_SEND_PERIOD)
        CPSAddREFERENCE_PERIOD(builder, self.REFERENCE_PERIOD)
        CPSAddSELF_SYNCHRONIZING(builder, self.SELF_SYNCHRONIZING)
        if self.STREAM_ID is not None:
            CPSAddSTREAM_ID(builder, STREAM_ID)
        CPSAddSEGMENT_INDEX(builder, self.SEGMENT_INDEX)
        CPSAddAPID(builder, self.APID)
        CPSAddSPACECRAFT_ID(builder, self.SPACECRAFT_ID)
        if self.START_EPOCH is not None:
            CPSAddSTART_EPOCH(builder, START_EPOCH)
        if self.STOP_EPOCH is not None:
            CPSAddSTOP_EPOCH(builder, STOP_EPOCH)
        if self.COMPRESSED_DATA is not None:
            CPSAddCOMPRESSED_DATA(builder, COMPRESSED_DATA)
        CPSAddUNCOMPRESSED_LENGTH(builder, self.UNCOMPRESSED_LENGTH)
        CPSAddUNCOMPRESSED_CRC32(builder, self.UNCOMPRESSED_CRC32)
        CPS = CPSEnd(builder)
        return CPS
