# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Where a block of bytes lives and how it is verified. The payload lane is
# CID-first: a tile record addresses its bytes so an epoch can be verified
# rather than trusted, and carries them inline only when the block is small
# enough that a second fetch costs more than it saves. Exactly one of CID or
# BYTES is the authority for a given ref; when both are present the bytes
# MUST hash to the CID.
class DTTPayloadRef(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DTTPayloadRef()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDTTPayloadRef(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DTTPayloadRefBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x44\x54\x54", size_prefixed=size_prefixed)

    # DTTPayloadRef
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Content identifier of the block in the dataset lane.
    # DTTPayloadRef
    def CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The block inline. Reserved for small blocks; a tile pyramid is
    # distributed by CID, never by embedding megabytes per record.
    # DTTPayloadRef
    def BYTES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # DTTPayloadRef
    def BYTESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # DTTPayloadRef
    def BYTESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DTTPayloadRef
    def BYTESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Length of the block in bytes, whether inline or referenced, so a
    # consumer can budget a fetch before making it.
    # DTTPayloadRef
    def SIZE_BYTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Multihash of the block, lowercase hexadecimal, when the publisher states
    # one independently of the CID.
    # DTTPayloadRef
    def DIGEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Content encoding applied to the block before hashing, verbatim, when the
    # bytes are stored compressed.
    # DTTPayloadRef
    def CONTENT_ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Media type of the block as the publisher serves it.
    # DTTPayloadRef
    def MEDIA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def DTTPayloadRefStart(builder):
    builder.StartObject(6)

def Start(builder):
    DTTPayloadRefStart(builder)

def DTTPayloadRefAddCID(builder, CID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CID), 0)

def AddCID(builder, CID):
    DTTPayloadRefAddCID(builder, CID)

def DTTPayloadRefAddBYTES(builder, BYTES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(BYTES), 0)

def AddBYTES(builder, BYTES):
    DTTPayloadRefAddBYTES(builder, BYTES)

def DTTPayloadRefStartBYTESVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartBYTESVector(builder, numElems):
    return DTTPayloadRefStartBYTESVector(builder, numElems)

def DTTPayloadRefCreateBYTESVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateBYTESVector(builder, data):
    DTTPayloadRefCreateBYTESVector(builder, data)

def DTTPayloadRefAddSIZE_BYTES(builder, SIZE_BYTES):
    builder.PrependUint64Slot(2, SIZE_BYTES, 0)

def AddSIZE_BYTES(builder, SIZE_BYTES):
    DTTPayloadRefAddSIZE_BYTES(builder, SIZE_BYTES)

def DTTPayloadRefAddDIGEST(builder, DIGEST):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(DIGEST), 0)

def AddDIGEST(builder, DIGEST):
    DTTPayloadRefAddDIGEST(builder, DIGEST)

def DTTPayloadRefAddCONTENT_ENCODING(builder, CONTENT_ENCODING):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT_ENCODING), 0)

def AddCONTENT_ENCODING(builder, CONTENT_ENCODING):
    DTTPayloadRefAddCONTENT_ENCODING(builder, CONTENT_ENCODING)

def DTTPayloadRefAddMEDIA_TYPE(builder, MEDIA_TYPE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(MEDIA_TYPE), 0)

def AddMEDIA_TYPE(builder, MEDIA_TYPE):
    DTTPayloadRefAddMEDIA_TYPE(builder, MEDIA_TYPE)

def DTTPayloadRefEnd(builder):
    return builder.EndObject()

def End(builder):
    return DTTPayloadRefEnd(builder)

try:
    from typing import List
except:
    pass

class DTTPayloadRefT(object):

    # DTTPayloadRefT
    def __init__(
        self,
        CID = None,
        BYTES = None,
        SIZE_BYTES = 0,
        DIGEST = None,
        CONTENT_ENCODING = None,
        MEDIA_TYPE = None,
    ):
        self.CID = CID  # type: Optional[str]
        self.BYTES = BYTES  # type: Optional[List[int]]
        self.SIZE_BYTES = SIZE_BYTES  # type: int
        self.DIGEST = DIGEST  # type: Optional[str]
        self.CONTENT_ENCODING = CONTENT_ENCODING  # type: Optional[str]
        self.MEDIA_TYPE = MEDIA_TYPE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpDttpayloadRef = DTTPayloadRef()
        tmpDttpayloadRef.Init(buf, pos)
        return cls.InitFromObj(tmpDttpayloadRef)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpDttpayloadRef):
        x = DTTPayloadRefT()
        x._UnPack(tmpDttpayloadRef)
        return x

    # DTTPayloadRefT
    def _UnPack(self, DTTPayloadRef):
        if DTTPayloadRef is None:
            return
        self.CID = DTTPayloadRef.CID()
        if not DTTPayloadRef.BYTESIsNone():
            if np is None:
                self.BYTES = []
                for i in range(DTTPayloadRef.BYTESLength()):
                    self.BYTES.append(DTTPayloadRef.BYTES(i))
            else:
                self.BYTES = DTTPayloadRef.BYTESAsNumpy()
        self.SIZE_BYTES = DTTPayloadRef.SIZE_BYTES()
        self.DIGEST = DTTPayloadRef.DIGEST()
        self.CONTENT_ENCODING = DTTPayloadRef.CONTENT_ENCODING()
        self.MEDIA_TYPE = DTTPayloadRef.MEDIA_TYPE()

    # DTTPayloadRefT
    def Pack(self, builder):
        if self.CID is not None:
            CID = builder.CreateString(self.CID)
        if self.BYTES is not None:
            if np is not None and type(self.BYTES) is np.ndarray:
                BYTES = builder.CreateNumpyVector(self.BYTES)
            else:
                DTTPayloadRefStartBYTESVector(builder, len(self.BYTES))
                for i in reversed(range(len(self.BYTES))):
                    builder.PrependUint8(self.BYTES[i])
                BYTES = builder.EndVector()
        if self.DIGEST is not None:
            DIGEST = builder.CreateString(self.DIGEST)
        if self.CONTENT_ENCODING is not None:
            CONTENT_ENCODING = builder.CreateString(self.CONTENT_ENCODING)
        if self.MEDIA_TYPE is not None:
            MEDIA_TYPE = builder.CreateString(self.MEDIA_TYPE)
        DTTPayloadRefStart(builder)
        if self.CID is not None:
            DTTPayloadRefAddCID(builder, CID)
        if self.BYTES is not None:
            DTTPayloadRefAddBYTES(builder, BYTES)
        DTTPayloadRefAddSIZE_BYTES(builder, self.SIZE_BYTES)
        if self.DIGEST is not None:
            DTTPayloadRefAddDIGEST(builder, DIGEST)
        if self.CONTENT_ENCODING is not None:
            DTTPayloadRefAddCONTENT_ENCODING(builder, CONTENT_ENCODING)
        if self.MEDIA_TYPE is not None:
            DTTPayloadRefAddMEDIA_TYPE(builder, MEDIA_TYPE)
        DTTPayloadRef = DTTPayloadRefEnd(builder)
        return DTTPayloadRef
