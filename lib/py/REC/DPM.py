# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Dataset Publication Manifest binding data/index CIDs, query replay,
# source hashes, schema hashes, encryption metadata, provider-mediated query
# protocols, completeness roots, and provider signature.
class DPM(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DPM()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDPM(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DPMBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x44\x50\x4D", size_prefixed=size_prefixed)

    # DPM
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Manifest format version.
    # DPM
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Stable dataset identifier.
    # DPM
    def DATASET_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Dataset update or batch identifier.
    # DPM
    def UPDATE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Canonical publication/update partition identity. FILE_ID is the key used
    # everywhere a subscriber, provider, PNM, asset, entitlement, cache, audit
    # log, or query protocol refers to this exact update. It is not merely a
    # human filename and it is not the FlatBuffer file_identifier. For
    # completeness-verifiable streams, all returned records MUST belong to this
    # FILE_ID and prove inclusion under this DPM's signed roots, normally through
    # a declared file_id completeness index. Use this field for all update
    # addressing instead of inventing per-protocol IDs; provider responses,
    # Merkle leaves, proof paths, PNM.FILE_ID, and DPMAsset.FILE_ID must all bind
    # to the same value.
    # DPM
    def FILE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider peer ID.
    # DPM
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider EPM CID.
    # DPM
    def PROVIDER_EPM_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Publication timestamp in ISO 8601 UTC.
    # DPM
    def PUBLISH_TIMESTAMP(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Published shard, index, and auxiliary artifacts.
    # DPM
    def ASSETS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from DPMAsset import DPMAsset
            obj = DPMAsset()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # DPM
    def ASSETSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DPM
    def ASSETSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Source batches used to build the dataset.
    # DPM
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from DPMSourceBatch import DPMSourceBatch
            obj = DPMSourceBatch()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # DPM
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DPM
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # Replayable query binding.
    # DPM
    def QUERY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from DPMQueryBinding import DPMQueryBinding
            obj = DPMQueryBinding()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Signed completeness-capable indexes. Inclusion proofs prove that returned
    # records are authentic members of DATA_ROOT. Range-completeness proofs also
    # prove that no matching records were omitted, but only for predicates
    # expressible against these declared indexes.
    # DPM
    def INDEXES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from DPMCompletenessIndex import DPMCompletenessIndex
            obj = DPMCompletenessIndex()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # DPM
    def INDEXESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DPM
    def INDEXESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # Encryption/key metadata.
    # DPM
    def ENCRYPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from DPMEncryptionBinding import DPMEncryptionBinding
            obj = DPMEncryptionBinding()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Provider signature over the canonical manifest bytes or manifest digest.
    # DPM
    def PROVIDER_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # DPM
    def PROVIDER_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # DPM
    def PROVIDER_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DPM
    def PROVIDER_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

    # Signature algorithm, e.g. Ed25519.
    # DPM
    def SIGNATURE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def DPMStart(builder):
    builder.StartObject(14)

def Start(builder):
    DPMStart(builder)

def DPMAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    DPMAddVERSION(builder, VERSION)

def DPMAddDATASET_ID(builder, DATASET_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(DATASET_ID), 0)

def AddDATASET_ID(builder, DATASET_ID):
    DPMAddDATASET_ID(builder, DATASET_ID)

def DPMAddUPDATE_ID(builder, UPDATE_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(UPDATE_ID), 0)

def AddUPDATE_ID(builder, UPDATE_ID):
    DPMAddUPDATE_ID(builder, UPDATE_ID)

def DPMAddFILE_ID(builder, FILE_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(FILE_ID), 0)

def AddFILE_ID(builder, FILE_ID):
    DPMAddFILE_ID(builder, FILE_ID)

def DPMAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    DPMAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def DPMAddPROVIDER_EPM_CID(builder, PROVIDER_EPM_CID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_EPM_CID), 0)

def AddPROVIDER_EPM_CID(builder, PROVIDER_EPM_CID):
    DPMAddPROVIDER_EPM_CID(builder, PROVIDER_EPM_CID)

def DPMAddPUBLISH_TIMESTAMP(builder, PUBLISH_TIMESTAMP):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PUBLISH_TIMESTAMP), 0)

def AddPUBLISH_TIMESTAMP(builder, PUBLISH_TIMESTAMP):
    DPMAddPUBLISH_TIMESTAMP(builder, PUBLISH_TIMESTAMP)

def DPMAddASSETS(builder, ASSETS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(ASSETS), 0)

def AddASSETS(builder, ASSETS):
    DPMAddASSETS(builder, ASSETS)

def DPMStartASSETSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartASSETSVector(builder, numElems):
    return DPMStartASSETSVector(builder, numElems)

def DPMCreateASSETSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateASSETSVector(builder, data):
    DPMCreateASSETSVector(builder, data)

def DPMAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    DPMAddSOURCES(builder, SOURCES)

def DPMStartSOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCESVector(builder, numElems):
    return DPMStartSOURCESVector(builder, numElems)

def DPMCreateSOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCESVector(builder, data):
    DPMCreateSOURCESVector(builder, data)

def DPMAddQUERY(builder, QUERY):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(QUERY), 0)

def AddQUERY(builder, QUERY):
    DPMAddQUERY(builder, QUERY)

def DPMAddINDEXES(builder, INDEXES):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(INDEXES), 0)

def AddINDEXES(builder, INDEXES):
    DPMAddINDEXES(builder, INDEXES)

def DPMStartINDEXESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartINDEXESVector(builder, numElems):
    return DPMStartINDEXESVector(builder, numElems)

def DPMCreateINDEXESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateINDEXESVector(builder, data):
    DPMCreateINDEXESVector(builder, data)

def DPMAddENCRYPTION(builder, ENCRYPTION):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(ENCRYPTION), 0)

def AddENCRYPTION(builder, ENCRYPTION):
    DPMAddENCRYPTION(builder, ENCRYPTION)

def DPMAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_SIGNATURE), 0)

def AddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    DPMAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)

def DPMStartPROVIDER_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPROVIDER_SIGNATUREVector(builder, numElems):
    return DPMStartPROVIDER_SIGNATUREVector(builder, numElems)

def DPMCreatePROVIDER_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePROVIDER_SIGNATUREVector(builder, data):
    DPMCreatePROVIDER_SIGNATUREVector(builder, data)

def DPMAddSIGNATURE_TYPE(builder, SIGNATURE_TYPE):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE_TYPE), 0)

def AddSIGNATURE_TYPE(builder, SIGNATURE_TYPE):
    DPMAddSIGNATURE_TYPE(builder, SIGNATURE_TYPE)

def DPMEnd(builder):
    return builder.EndObject()

def End(builder):
    return DPMEnd(builder)

import DPMAsset
import DPMCompletenessIndex
import DPMEncryptionBinding
import DPMQueryBinding
import DPMSourceBatch
try:
    from typing import List, Optional
except:
    pass

class DPMT(object):

    # DPMT
    def __init__(
        self,
        VERSION = None,
        DATASET_ID = None,
        UPDATE_ID = None,
        FILE_ID = None,
        PROVIDER_PEER_ID = None,
        PROVIDER_EPM_CID = None,
        PUBLISH_TIMESTAMP = None,
        ASSETS = None,
        SOURCES = None,
        QUERY = None,
        INDEXES = None,
        ENCRYPTION = None,
        PROVIDER_SIGNATURE = None,
        SIGNATURE_TYPE = None,
    ):
        self.VERSION = VERSION  # type: Optional[str]
        self.DATASET_ID = DATASET_ID  # type: Optional[str]
        self.UPDATE_ID = UPDATE_ID  # type: Optional[str]
        self.FILE_ID = FILE_ID  # type: Optional[str]
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.PROVIDER_EPM_CID = PROVIDER_EPM_CID  # type: Optional[str]
        self.PUBLISH_TIMESTAMP = PUBLISH_TIMESTAMP  # type: Optional[str]
        self.ASSETS = ASSETS  # type: Optional[List[DPMAsset.DPMAssetT]]
        self.SOURCES = SOURCES  # type: Optional[List[DPMSourceBatch.DPMSourceBatchT]]
        self.QUERY = QUERY  # type: Optional[DPMQueryBinding.DPMQueryBindingT]
        self.INDEXES = INDEXES  # type: Optional[List[DPMCompletenessIndex.DPMCompletenessIndexT]]
        self.ENCRYPTION = ENCRYPTION  # type: Optional[DPMEncryptionBinding.DPMEncryptionBindingT]
        self.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE  # type: Optional[List[int]]
        self.SIGNATURE_TYPE = SIGNATURE_TYPE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpDpm = DPM()
        tmpDpm.Init(buf, pos)
        return cls.InitFromObj(tmpDpm)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpDpm):
        x = DPMT()
        x._UnPack(tmpDpm)
        return x

    # DPMT
    def _UnPack(self, DPM):
        if DPM is None:
            return
        self.VERSION = DPM.VERSION()
        self.DATASET_ID = DPM.DATASET_ID()
        self.UPDATE_ID = DPM.UPDATE_ID()
        self.FILE_ID = DPM.FILE_ID()
        self.PROVIDER_PEER_ID = DPM.PROVIDER_PEER_ID()
        self.PROVIDER_EPM_CID = DPM.PROVIDER_EPM_CID()
        self.PUBLISH_TIMESTAMP = DPM.PUBLISH_TIMESTAMP()
        if not DPM.ASSETSIsNone():
            self.ASSETS = []
            for i in range(DPM.ASSETSLength()):
                if DPM.ASSETS(i) is None:
                    self.ASSETS.append(None)
                else:
                    dPMAsset_ = DPMAsset.DPMAssetT.InitFromObj(DPM.ASSETS(i))
                    self.ASSETS.append(dPMAsset_)
        if not DPM.SOURCESIsNone():
            self.SOURCES = []
            for i in range(DPM.SOURCESLength()):
                if DPM.SOURCES(i) is None:
                    self.SOURCES.append(None)
                else:
                    dPMSourceBatch_ = DPMSourceBatch.DPMSourceBatchT.InitFromObj(DPM.SOURCES(i))
                    self.SOURCES.append(dPMSourceBatch_)
        if DPM.QUERY() is not None:
            self.QUERY = DPMQueryBinding.DPMQueryBindingT.InitFromObj(DPM.QUERY())
        if not DPM.INDEXESIsNone():
            self.INDEXES = []
            for i in range(DPM.INDEXESLength()):
                if DPM.INDEXES(i) is None:
                    self.INDEXES.append(None)
                else:
                    dPMCompletenessIndex_ = DPMCompletenessIndex.DPMCompletenessIndexT.InitFromObj(DPM.INDEXES(i))
                    self.INDEXES.append(dPMCompletenessIndex_)
        if DPM.ENCRYPTION() is not None:
            self.ENCRYPTION = DPMEncryptionBinding.DPMEncryptionBindingT.InitFromObj(DPM.ENCRYPTION())
        if not DPM.PROVIDER_SIGNATUREIsNone():
            if np is None:
                self.PROVIDER_SIGNATURE = []
                for i in range(DPM.PROVIDER_SIGNATURELength()):
                    self.PROVIDER_SIGNATURE.append(DPM.PROVIDER_SIGNATURE(i))
            else:
                self.PROVIDER_SIGNATURE = DPM.PROVIDER_SIGNATUREAsNumpy()
        self.SIGNATURE_TYPE = DPM.SIGNATURE_TYPE()

    # DPMT
    def Pack(self, builder):
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        if self.DATASET_ID is not None:
            DATASET_ID = builder.CreateString(self.DATASET_ID)
        if self.UPDATE_ID is not None:
            UPDATE_ID = builder.CreateString(self.UPDATE_ID)
        if self.FILE_ID is not None:
            FILE_ID = builder.CreateString(self.FILE_ID)
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.PROVIDER_EPM_CID is not None:
            PROVIDER_EPM_CID = builder.CreateString(self.PROVIDER_EPM_CID)
        if self.PUBLISH_TIMESTAMP is not None:
            PUBLISH_TIMESTAMP = builder.CreateString(self.PUBLISH_TIMESTAMP)
        if self.ASSETS is not None:
            ASSETSlist = []
            for i in range(len(self.ASSETS)):
                ASSETSlist.append(self.ASSETS[i].Pack(builder))
            DPMStartASSETSVector(builder, len(self.ASSETS))
            for i in reversed(range(len(self.ASSETS))):
                builder.PrependUOffsetTRelative(ASSETSlist[i])
            ASSETS = builder.EndVector()
        if self.SOURCES is not None:
            SOURCESlist = []
            for i in range(len(self.SOURCES)):
                SOURCESlist.append(self.SOURCES[i].Pack(builder))
            DPMStartSOURCESVector(builder, len(self.SOURCES))
            for i in reversed(range(len(self.SOURCES))):
                builder.PrependUOffsetTRelative(SOURCESlist[i])
            SOURCES = builder.EndVector()
        if self.QUERY is not None:
            QUERY = self.QUERY.Pack(builder)
        if self.INDEXES is not None:
            INDEXESlist = []
            for i in range(len(self.INDEXES)):
                INDEXESlist.append(self.INDEXES[i].Pack(builder))
            DPMStartINDEXESVector(builder, len(self.INDEXES))
            for i in reversed(range(len(self.INDEXES))):
                builder.PrependUOffsetTRelative(INDEXESlist[i])
            INDEXES = builder.EndVector()
        if self.ENCRYPTION is not None:
            ENCRYPTION = self.ENCRYPTION.Pack(builder)
        if self.PROVIDER_SIGNATURE is not None:
            if np is not None and type(self.PROVIDER_SIGNATURE) is np.ndarray:
                PROVIDER_SIGNATURE = builder.CreateNumpyVector(self.PROVIDER_SIGNATURE)
            else:
                DPMStartPROVIDER_SIGNATUREVector(builder, len(self.PROVIDER_SIGNATURE))
                for i in reversed(range(len(self.PROVIDER_SIGNATURE))):
                    builder.PrependUint8(self.PROVIDER_SIGNATURE[i])
                PROVIDER_SIGNATURE = builder.EndVector()
        if self.SIGNATURE_TYPE is not None:
            SIGNATURE_TYPE = builder.CreateString(self.SIGNATURE_TYPE)
        DPMStart(builder)
        if self.VERSION is not None:
            DPMAddVERSION(builder, VERSION)
        if self.DATASET_ID is not None:
            DPMAddDATASET_ID(builder, DATASET_ID)
        if self.UPDATE_ID is not None:
            DPMAddUPDATE_ID(builder, UPDATE_ID)
        if self.FILE_ID is not None:
            DPMAddFILE_ID(builder, FILE_ID)
        if self.PROVIDER_PEER_ID is not None:
            DPMAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.PROVIDER_EPM_CID is not None:
            DPMAddPROVIDER_EPM_CID(builder, PROVIDER_EPM_CID)
        if self.PUBLISH_TIMESTAMP is not None:
            DPMAddPUBLISH_TIMESTAMP(builder, PUBLISH_TIMESTAMP)
        if self.ASSETS is not None:
            DPMAddASSETS(builder, ASSETS)
        if self.SOURCES is not None:
            DPMAddSOURCES(builder, SOURCES)
        if self.QUERY is not None:
            DPMAddQUERY(builder, QUERY)
        if self.INDEXES is not None:
            DPMAddINDEXES(builder, INDEXES)
        if self.ENCRYPTION is not None:
            DPMAddENCRYPTION(builder, ENCRYPTION)
        if self.PROVIDER_SIGNATURE is not None:
            DPMAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)
        if self.SIGNATURE_TYPE is not None:
            DPMAddSIGNATURE_TYPE(builder, SIGNATURE_TYPE)
        DPM = DPMEnd(builder)
        return DPM
