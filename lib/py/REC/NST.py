# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Node Status Record - the set of peers a node reports.
class NST(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NST()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNST(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NSTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x53\x54", size_prefixed=size_prefixed)

    # NST
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # NST
    def NODES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from NSTNode import NSTNode
            obj = NSTNode()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NST
    def NODESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NST
    def NODESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Unix milliseconds when this set was generated.
    # NST
    def GENERATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # Peer identifier of the emitting node.
    # NST
    def SOURCE_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def NSTStart(builder):
    builder.StartObject(3)

def Start(builder):
    NSTStart(builder)

def NSTAddNODES(builder, NODES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NODES), 0)

def AddNODES(builder, NODES):
    NSTAddNODES(builder, NODES)

def NSTStartNODESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartNODESVector(builder, numElems):
    return NSTStartNODESVector(builder, numElems)

def NSTCreateNODESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateNODESVector(builder, data):
    NSTCreateNODESVector(builder, data)

def NSTAddGENERATED_AT(builder, GENERATED_AT):
    builder.PrependInt64Slot(1, GENERATED_AT, 0)

def AddGENERATED_AT(builder, GENERATED_AT):
    NSTAddGENERATED_AT(builder, GENERATED_AT)

def NSTAddSOURCE_PEER_ID(builder, SOURCE_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_PEER_ID), 0)

def AddSOURCE_PEER_ID(builder, SOURCE_PEER_ID):
    NSTAddSOURCE_PEER_ID(builder, SOURCE_PEER_ID)

def NSTEnd(builder):
    return builder.EndObject()

def End(builder):
    return NSTEnd(builder)

import NSTNode
try:
    from typing import List
except:
    pass

class NSTT(object):

    # NSTT
    def __init__(
        self,
        NODES = None,
        GENERATED_AT = 0,
        SOURCE_PEER_ID = None,
    ):
        self.NODES = NODES  # type: Optional[List[NSTNode.NSTNodeT]]
        self.GENERATED_AT = GENERATED_AT  # type: int
        self.SOURCE_PEER_ID = SOURCE_PEER_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNst = NST()
        tmpNst.Init(buf, pos)
        return cls.InitFromObj(tmpNst)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNst):
        x = NSTT()
        x._UnPack(tmpNst)
        return x

    # NSTT
    def _UnPack(self, NST):
        if NST is None:
            return
        if not NST.NODESIsNone():
            self.NODES = []
            for i in range(NST.NODESLength()):
                if NST.NODES(i) is None:
                    self.NODES.append(None)
                else:
                    nSTNode_ = NSTNode.NSTNodeT.InitFromObj(NST.NODES(i))
                    self.NODES.append(nSTNode_)
        self.GENERATED_AT = NST.GENERATED_AT()
        self.SOURCE_PEER_ID = NST.SOURCE_PEER_ID()

    # NSTT
    def Pack(self, builder):
        if self.NODES is not None:
            NODESlist = []
            for i in range(len(self.NODES)):
                NODESlist.append(self.NODES[i].Pack(builder))
            NSTStartNODESVector(builder, len(self.NODES))
            for i in reversed(range(len(self.NODES))):
                builder.PrependUOffsetTRelative(NODESlist[i])
            NODES = builder.EndVector()
        if self.SOURCE_PEER_ID is not None:
            SOURCE_PEER_ID = builder.CreateString(self.SOURCE_PEER_ID)
        NSTStart(builder)
        if self.NODES is not None:
            NSTAddNODES(builder, NODES)
        NSTAddGENERATED_AT(builder, self.GENERATED_AT)
        if self.SOURCE_PEER_ID is not None:
            NSTAddSOURCE_PEER_ID(builder, SOURCE_PEER_ID)
        NST = NSTEnd(builder)
        return NST
