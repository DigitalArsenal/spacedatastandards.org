# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# ONE CCSDS TDM observation: a single data-section line.
#
# A TDM data section is NOT a uniform grid. Every line in every published
# example (CCSDS 503.0-B-2 Cor.1 annex E, 18 figures) has the literal form
#
#     KEYWORD = <epoch> <value>
#
# and the epochs are chosen INDEPENDENTLY PER KEYWORD. Figure E-17 (ground
# based radar tracking with RCS) is decisive: RANGE, ANGLE_1, ANGLE_2 and
# CARRIER_POWER share epochs, while the final RCS line repeats the EARLIER
# epoch 2011-05-11T10:26:33.7008 after a 10:26:33.9686 line. No single
# OBSERVATION_START_TIME + i * OBSERVATION_STEP_SIZE grid can express that,
# which is why the parallel observation arrays on the TDM root cannot
# round-trip a conformant TDM.
#
# KEYWORD is the CCSDS data keyword verbatim and case-exact, e.g. "RANGE",
# "ANGLE_1", "CARRIER_POWER", "RCS", "DOPPLER_INTEGRATED", "PR_N0",
# "RECEIVE_FREQ_1", "TRANSMIT_PHASE_CT_1", "VLBI_DELAY", "DOR", "MAG",
# "CLOCK_BIAS", "STEC", "TROPO_DRY", "PRESSURE". Carrying the keyword rather
# than a fixed field per observable is what lets this form transport the whole
# 503.0-B-2 data vocabulary, including keywords a future issue adds, without
# a wire change and without inventing a name the standard does not define.
#
# Units are those the standard assigns to that keyword; RANGE additionally
# depends on RANGE_UNITS, which is metadata, not data.
class TDMObservation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TDMObservation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTDMObservation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TDMObservationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x44\x4D", size_prefixed=size_prefixed)

    # TDMObservation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CCSDS data keyword, verbatim and case-exact.
    # TDMObservation
    def KEYWORD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Epoch of this single observation, ISO 8601, in the segment TIME_SYSTEM.
    # TDMObservation
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Observed value.
    # TDMObservation
    def VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def TDMObservationStart(builder):
    builder.StartObject(3)

def Start(builder):
    TDMObservationStart(builder)

def TDMObservationAddKEYWORD(builder, KEYWORD):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(KEYWORD), 0)

def AddKEYWORD(builder, KEYWORD):
    TDMObservationAddKEYWORD(builder, KEYWORD)

def TDMObservationAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    TDMObservationAddEPOCH(builder, EPOCH)

def TDMObservationAddVALUE(builder, VALUE):
    builder.PrependFloat64Slot(2, VALUE, 0.0)

def AddVALUE(builder, VALUE):
    TDMObservationAddVALUE(builder, VALUE)

def TDMObservationEnd(builder):
    return builder.EndObject()

def End(builder):
    return TDMObservationEnd(builder)


class TDMObservationT(object):

    # TDMObservationT
    def __init__(
        self,
        KEYWORD = None,
        EPOCH = None,
        VALUE = 0.0,
    ):
        self.KEYWORD = KEYWORD  # type: Optional[str]
        self.EPOCH = EPOCH  # type: Optional[str]
        self.VALUE = VALUE  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTdmobservation = TDMObservation()
        tmpTdmobservation.Init(buf, pos)
        return cls.InitFromObj(tmpTdmobservation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTdmobservation):
        x = TDMObservationT()
        x._UnPack(tmpTdmobservation)
        return x

    # TDMObservationT
    def _UnPack(self, TDMObservation):
        if TDMObservation is None:
            return
        self.KEYWORD = TDMObservation.KEYWORD()
        self.EPOCH = TDMObservation.EPOCH()
        self.VALUE = TDMObservation.VALUE()

    # TDMObservationT
    def Pack(self, builder):
        if self.KEYWORD is not None:
            KEYWORD = builder.CreateString(self.KEYWORD)
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        TDMObservationStart(builder)
        if self.KEYWORD is not None:
            TDMObservationAddKEYWORD(builder, KEYWORD)
        if self.EPOCH is not None:
            TDMObservationAddEPOCH(builder, EPOCH)
        TDMObservationAddVALUE(builder, self.VALUE)
        TDMObservation = TDMObservationEnd(builder)
        return TDMObservation
