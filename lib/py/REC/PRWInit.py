# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Propagator initialization request — assigns TLE / OMM / Keplerian /
# polynomial inputs to entity handles.
#
# The actual per-entity source record is carried inline as raw bytes
# tagged with SOURCE_KIND and, where useful, a SDS file_identifier.
# Callers encode `OMM`, `OCM`, or a one-off Keplerian set and pass the
# bytes verbatim; the propagator uses SOURCE_KIND to decide how to
# consume them.
class PRWInit(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWInit()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWInit(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWInitBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWInit
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Entity handles to assign results to (same order as SOURCES[]).
    # PRWInit
    def ENTITY_HANDLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # PRWInit
    def ENTITY_HANDLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # PRWInit
    def ENTITY_HANDLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWInit
    def ENTITY_HANDLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Per-entity source records, encoded as SDS FlatBuffers.
    # PRWInit
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PRWInitSource import PRWInitSource
            obj = PRWInitSource()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWInit
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWInit
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def PRWInitStart(builder):
    builder.StartObject(2)

def Start(builder):
    PRWInitStart(builder)

def PRWInitAddENTITY_HANDLES(builder, ENTITY_HANDLES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ENTITY_HANDLES), 0)

def AddENTITY_HANDLES(builder, ENTITY_HANDLES):
    PRWInitAddENTITY_HANDLES(builder, ENTITY_HANDLES)

def PRWInitStartENTITY_HANDLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartENTITY_HANDLESVector(builder, numElems):
    return PRWInitStartENTITY_HANDLESVector(builder, numElems)

def PRWInitCreateENTITY_HANDLESVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateENTITY_HANDLESVector(builder, data):
    PRWInitCreateENTITY_HANDLESVector(builder, data)

def PRWInitAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    PRWInitAddSOURCES(builder, SOURCES)

def PRWInitStartSOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCESVector(builder, numElems):
    return PRWInitStartSOURCESVector(builder, numElems)

def PRWInitCreateSOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCESVector(builder, data):
    PRWInitCreateSOURCESVector(builder, data)

def PRWInitEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWInitEnd(builder)

import PRWInitSource
try:
    from typing import List
except:
    pass

class PRWInitT(object):

    # PRWInitT
    def __init__(
        self,
        ENTITY_HANDLES = None,
        SOURCES = None,
    ):
        self.ENTITY_HANDLES = ENTITY_HANDLES  # type: Optional[List[int]]
        self.SOURCES = SOURCES  # type: Optional[List[PRWInitSource.PRWInitSourceT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwinit = PRWInit()
        tmpPrwinit.Init(buf, pos)
        return cls.InitFromObj(tmpPrwinit)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwinit):
        x = PRWInitT()
        x._UnPack(tmpPrwinit)
        return x

    # PRWInitT
    def _UnPack(self, PRWInit):
        if PRWInit is None:
            return
        if not PRWInit.ENTITY_HANDLESIsNone():
            if np is None:
                self.ENTITY_HANDLES = []
                for i in range(PRWInit.ENTITY_HANDLESLength()):
                    self.ENTITY_HANDLES.append(PRWInit.ENTITY_HANDLES(i))
            else:
                self.ENTITY_HANDLES = PRWInit.ENTITY_HANDLESAsNumpy()
        if not PRWInit.SOURCESIsNone():
            self.SOURCES = []
            for i in range(PRWInit.SOURCESLength()):
                if PRWInit.SOURCES(i) is None:
                    self.SOURCES.append(None)
                else:
                    pRWInitSource_ = PRWInitSource.PRWInitSourceT.InitFromObj(PRWInit.SOURCES(i))
                    self.SOURCES.append(pRWInitSource_)

    # PRWInitT
    def Pack(self, builder):
        if self.ENTITY_HANDLES is not None:
            if np is not None and type(self.ENTITY_HANDLES) is np.ndarray:
                ENTITY_HANDLES = builder.CreateNumpyVector(self.ENTITY_HANDLES)
            else:
                PRWInitStartENTITY_HANDLESVector(builder, len(self.ENTITY_HANDLES))
                for i in reversed(range(len(self.ENTITY_HANDLES))):
                    builder.PrependUint32(self.ENTITY_HANDLES[i])
                ENTITY_HANDLES = builder.EndVector()
        if self.SOURCES is not None:
            SOURCESlist = []
            for i in range(len(self.SOURCES)):
                SOURCESlist.append(self.SOURCES[i].Pack(builder))
            PRWInitStartSOURCESVector(builder, len(self.SOURCES))
            for i in reversed(range(len(self.SOURCES))):
                builder.PrependUOffsetTRelative(SOURCESlist[i])
            SOURCES = builder.EndVector()
        PRWInitStart(builder)
        if self.ENTITY_HANDLES is not None:
            PRWInitAddENTITY_HANDLES(builder, ENTITY_HANDLES)
        if self.SOURCES is not None:
            PRWInitAddSOURCES(builder, SOURCES)
        PRWInit = PRWInitEnd(builder)
        return PRWInit
