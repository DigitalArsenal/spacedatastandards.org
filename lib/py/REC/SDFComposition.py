# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Constructive composition of two SDFs.
class SDFComposition(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDFComposition()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDFComposition(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFCompositionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDFComposition
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDFComposition
    def OPERATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # First operand (index into SDF.COMPONENTS[]).
    # SDFComposition
    def OPERAND_A(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Second operand (index into SDF.COMPONENTS[]).
    # SDFComposition
    def OPERAND_B(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Blend radius for smooth operations.
    # SDFComposition
    def BLEND_RADIUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SDFCompositionStart(builder):
    builder.StartObject(4)

def Start(builder):
    SDFCompositionStart(builder)

def SDFCompositionAddOPERATION(builder, OPERATION):
    builder.PrependUint8Slot(0, OPERATION, 0)

def AddOPERATION(builder, OPERATION):
    SDFCompositionAddOPERATION(builder, OPERATION)

def SDFCompositionAddOPERAND_A(builder, OPERAND_A):
    builder.PrependUint32Slot(1, OPERAND_A, 0)

def AddOPERAND_A(builder, OPERAND_A):
    SDFCompositionAddOPERAND_A(builder, OPERAND_A)

def SDFCompositionAddOPERAND_B(builder, OPERAND_B):
    builder.PrependUint32Slot(2, OPERAND_B, 0)

def AddOPERAND_B(builder, OPERAND_B):
    SDFCompositionAddOPERAND_B(builder, OPERAND_B)

def SDFCompositionAddBLEND_RADIUS(builder, BLEND_RADIUS):
    builder.PrependFloat64Slot(3, BLEND_RADIUS, 0.0)

def AddBLEND_RADIUS(builder, BLEND_RADIUS):
    SDFCompositionAddBLEND_RADIUS(builder, BLEND_RADIUS)

def SDFCompositionEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFCompositionEnd(builder)


class SDFCompositionT(object):

    # SDFCompositionT
    def __init__(
        self,
        OPERATION = 0,
        OPERAND_A = 0,
        OPERAND_B = 0,
        BLEND_RADIUS = 0.0,
    ):
        self.OPERATION = OPERATION  # type: int
        self.OPERAND_A = OPERAND_A  # type: int
        self.OPERAND_B = OPERAND_B  # type: int
        self.BLEND_RADIUS = BLEND_RADIUS  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfcomposition = SDFComposition()
        tmpSdfcomposition.Init(buf, pos)
        return cls.InitFromObj(tmpSdfcomposition)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfcomposition):
        x = SDFCompositionT()
        x._UnPack(tmpSdfcomposition)
        return x

    # SDFCompositionT
    def _UnPack(self, SDFComposition):
        if SDFComposition is None:
            return
        self.OPERATION = SDFComposition.OPERATION()
        self.OPERAND_A = SDFComposition.OPERAND_A()
        self.OPERAND_B = SDFComposition.OPERAND_B()
        self.BLEND_RADIUS = SDFComposition.BLEND_RADIUS()

    # SDFCompositionT
    def Pack(self, builder):
        SDFCompositionStart(builder)
        SDFCompositionAddOPERATION(builder, self.OPERATION)
        SDFCompositionAddOPERAND_A(builder, self.OPERAND_A)
        SDFCompositionAddOPERAND_B(builder, self.OPERAND_B)
        SDFCompositionAddBLEND_RADIUS(builder, self.BLEND_RADIUS)
        SDFComposition = SDFCompositionEnd(builder)
        return SDFComposition
