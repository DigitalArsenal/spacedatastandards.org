# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Startup readiness message.
class SCCReady(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCCReady()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCCReady(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCCReadyBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x43", size_prefixed=size_prefixed)

    # SCCReady
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCCReady
    def READY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return True

def SCCReadyStart(builder):
    builder.StartObject(1)

def Start(builder):
    SCCReadyStart(builder)

def SCCReadyAddREADY(builder, READY):
    builder.PrependBoolSlot(0, READY, 1)

def AddREADY(builder, READY):
    SCCReadyAddREADY(builder, READY)

def SCCReadyEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCCReadyEnd(builder)


class SCCReadyT(object):

    # SCCReadyT
    def __init__(
        self,
        READY = True,
    ):
        self.READY = READY  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSccready = SCCReady()
        tmpSccready.Init(buf, pos)
        return cls.InitFromObj(tmpSccready)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSccready):
        x = SCCReadyT()
        x._UnPack(tmpSccready)
        return x

    # SCCReadyT
    def _UnPack(self, SCCReady):
        if SCCReady is None:
            return
        self.READY = SCCReady.READY()

    # SCCReadyT
    def Pack(self, builder):
        SCCReadyStart(builder)
        SCCReadyAddREADY(builder, self.READY)
        SCCReady = SCCReadyEnd(builder)
        return SCCReady
