# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Conical SDF parameters.
class SDFConical(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDFConical()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDFConical(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFConicalBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDFConical
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDFConical
    def HALF_ANGLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Inner half-angle for hollow cones (0 == solid).
    # SDFConical
    def INNER_HALF_ANGLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Minimum range (inner cap distance).
    # SDFConical
    def MIN_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum range (outer cap distance).
    # SDFConical
    def MAX_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Use a flat cap (true) or a spherical cap (false).
    # SDFConical
    def FLAT_CAP(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def SDFConicalStart(builder):
    builder.StartObject(5)

def Start(builder):
    SDFConicalStart(builder)

def SDFConicalAddHALF_ANGLE(builder, HALF_ANGLE):
    builder.PrependFloat64Slot(0, HALF_ANGLE, 0.0)

def AddHALF_ANGLE(builder, HALF_ANGLE):
    SDFConicalAddHALF_ANGLE(builder, HALF_ANGLE)

def SDFConicalAddINNER_HALF_ANGLE(builder, INNER_HALF_ANGLE):
    builder.PrependFloat64Slot(1, INNER_HALF_ANGLE, 0.0)

def AddINNER_HALF_ANGLE(builder, INNER_HALF_ANGLE):
    SDFConicalAddINNER_HALF_ANGLE(builder, INNER_HALF_ANGLE)

def SDFConicalAddMIN_RANGE(builder, MIN_RANGE):
    builder.PrependFloat64Slot(2, MIN_RANGE, 0.0)

def AddMIN_RANGE(builder, MIN_RANGE):
    SDFConicalAddMIN_RANGE(builder, MIN_RANGE)

def SDFConicalAddMAX_RANGE(builder, MAX_RANGE):
    builder.PrependFloat64Slot(3, MAX_RANGE, 0.0)

def AddMAX_RANGE(builder, MAX_RANGE):
    SDFConicalAddMAX_RANGE(builder, MAX_RANGE)

def SDFConicalAddFLAT_CAP(builder, FLAT_CAP):
    builder.PrependBoolSlot(4, FLAT_CAP, 0)

def AddFLAT_CAP(builder, FLAT_CAP):
    SDFConicalAddFLAT_CAP(builder, FLAT_CAP)

def SDFConicalEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFConicalEnd(builder)


class SDFConicalT(object):

    # SDFConicalT
    def __init__(
        self,
        HALF_ANGLE = 0.0,
        INNER_HALF_ANGLE = 0.0,
        MIN_RANGE = 0.0,
        MAX_RANGE = 0.0,
        FLAT_CAP = False,
    ):
        self.HALF_ANGLE = HALF_ANGLE  # type: float
        self.INNER_HALF_ANGLE = INNER_HALF_ANGLE  # type: float
        self.MIN_RANGE = MIN_RANGE  # type: float
        self.MAX_RANGE = MAX_RANGE  # type: float
        self.FLAT_CAP = FLAT_CAP  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfconical = SDFConical()
        tmpSdfconical.Init(buf, pos)
        return cls.InitFromObj(tmpSdfconical)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfconical):
        x = SDFConicalT()
        x._UnPack(tmpSdfconical)
        return x

    # SDFConicalT
    def _UnPack(self, SDFConical):
        if SDFConical is None:
            return
        self.HALF_ANGLE = SDFConical.HALF_ANGLE()
        self.INNER_HALF_ANGLE = SDFConical.INNER_HALF_ANGLE()
        self.MIN_RANGE = SDFConical.MIN_RANGE()
        self.MAX_RANGE = SDFConical.MAX_RANGE()
        self.FLAT_CAP = SDFConical.FLAT_CAP()

    # SDFConicalT
    def Pack(self, builder):
        SDFConicalStart(builder)
        SDFConicalAddHALF_ANGLE(builder, self.HALF_ANGLE)
        SDFConicalAddINNER_HALF_ANGLE(builder, self.INNER_HALF_ANGLE)
        SDFConicalAddMIN_RANGE(builder, self.MIN_RANGE)
        SDFConicalAddMAX_RANGE(builder, self.MAX_RANGE)
        SDFConicalAddFLAT_CAP(builder, self.FLAT_CAP)
        SDFConical = SDFConicalEnd(builder)
        return SDFConical
