# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Trust Rule Policy - Signed compound criteria for evaluating one subject.
class TRP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x50", size_prefixed=size_prefixed)

    # TRP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable policy identifier.
    # TRP
    def POLICY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable policy name.
    # TRP
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable policy description.
    # TRP
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Root of the compound rule tree.
    # TRP
    def ROOT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TRPGroup import TRPGroup
            obj = TRPGroup()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Periodic evaluation cadence in milliseconds. The 10000 default is the
    # 0.1 Hz baseline and is configurable by the policy author.
    # TRP
    def EVALUATION_INTERVAL_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 10000

    # Event names that may trigger evaluation, e.g. "trust-edge", "trust-node",
    # "entity-profile", or "chain-balance".
    # TRP
    def EVENT_SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # TRP
    def EVENT_SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRP
    def EVENT_SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Whether evaluators should currently apply this policy.
    # TRP
    def ACTIVE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Unix timestamp in milliseconds when this policy was created.
    # TRP
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix timestamp in milliseconds when this policy was last updated.
    # TRP
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Peer identifier of the evaluator that issued this policy record.
    # TRP
    def EVALUATOR_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Evaluator signature over the policy record.
    # TRP
    def EVALUATOR_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRP
    def EVALUATOR_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TRP
    def EVALUATOR_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRP
    def EVALUATOR_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

def TRPStart(builder):
    builder.StartObject(11)

def Start(builder):
    TRPStart(builder)

def TRPAddPOLICY_ID(builder, POLICY_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(POLICY_ID), 0)

def AddPOLICY_ID(builder, POLICY_ID):
    TRPAddPOLICY_ID(builder, POLICY_ID)

def TRPAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    TRPAddNAME(builder, NAME)

def TRPAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    TRPAddDESCRIPTION(builder, DESCRIPTION)

def TRPAddROOT(builder, ROOT):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ROOT), 0)

def AddROOT(builder, ROOT):
    TRPAddROOT(builder, ROOT)

def TRPAddEVALUATION_INTERVAL_MS(builder, EVALUATION_INTERVAL_MS):
    builder.PrependUint32Slot(4, EVALUATION_INTERVAL_MS, 10000)

def AddEVALUATION_INTERVAL_MS(builder, EVALUATION_INTERVAL_MS):
    TRPAddEVALUATION_INTERVAL_MS(builder, EVALUATION_INTERVAL_MS)

def TRPAddEVENT_SOURCES(builder, EVENT_SOURCES):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(EVENT_SOURCES), 0)

def AddEVENT_SOURCES(builder, EVENT_SOURCES):
    TRPAddEVENT_SOURCES(builder, EVENT_SOURCES)

def TRPStartEVENT_SOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartEVENT_SOURCESVector(builder, numElems):
    return TRPStartEVENT_SOURCESVector(builder, numElems)

def TRPCreateEVENT_SOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateEVENT_SOURCESVector(builder, data):
    TRPCreateEVENT_SOURCESVector(builder, data)

def TRPAddACTIVE(builder, ACTIVE):
    builder.PrependBoolSlot(6, ACTIVE, 0)

def AddACTIVE(builder, ACTIVE):
    TRPAddACTIVE(builder, ACTIVE)

def TRPAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUint64Slot(7, CREATED_AT, 0)

def AddCREATED_AT(builder, CREATED_AT):
    TRPAddCREATED_AT(builder, CREATED_AT)

def TRPAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUint64Slot(8, UPDATED_AT, 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    TRPAddUPDATED_AT(builder, UPDATED_AT)

def TRPAddEVALUATOR_PEER_ID(builder, EVALUATOR_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(EVALUATOR_PEER_ID), 0)

def AddEVALUATOR_PEER_ID(builder, EVALUATOR_PEER_ID):
    TRPAddEVALUATOR_PEER_ID(builder, EVALUATOR_PEER_ID)

def TRPAddEVALUATOR_SIGNATURE(builder, EVALUATOR_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(EVALUATOR_SIGNATURE), 0)

def AddEVALUATOR_SIGNATURE(builder, EVALUATOR_SIGNATURE):
    TRPAddEVALUATOR_SIGNATURE(builder, EVALUATOR_SIGNATURE)

def TRPStartEVALUATOR_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartEVALUATOR_SIGNATUREVector(builder, numElems):
    return TRPStartEVALUATOR_SIGNATUREVector(builder, numElems)

def TRPCreateEVALUATOR_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateEVALUATOR_SIGNATUREVector(builder, data):
    TRPCreateEVALUATOR_SIGNATUREVector(builder, data)

def TRPEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRPEnd(builder)

import TRPGroup
try:
    from typing import List, Optional
except:
    pass

class TRPT(object):

    # TRPT
    def __init__(
        self,
        POLICY_ID = None,
        NAME = None,
        DESCRIPTION = None,
        ROOT = None,
        EVALUATION_INTERVAL_MS = 10000,
        EVENT_SOURCES = None,
        ACTIVE = False,
        CREATED_AT = 0,
        UPDATED_AT = 0,
        EVALUATOR_PEER_ID = None,
        EVALUATOR_SIGNATURE = None,
    ):
        self.POLICY_ID = POLICY_ID  # type: Optional[str]
        self.NAME = NAME  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.ROOT = ROOT  # type: Optional[TRPGroup.TRPGroupT]
        self.EVALUATION_INTERVAL_MS = EVALUATION_INTERVAL_MS  # type: int
        self.EVENT_SOURCES = EVENT_SOURCES  # type: Optional[List[Optional[str]]]
        self.ACTIVE = ACTIVE  # type: bool
        self.CREATED_AT = CREATED_AT  # type: int
        self.UPDATED_AT = UPDATED_AT  # type: int
        self.EVALUATOR_PEER_ID = EVALUATOR_PEER_ID  # type: Optional[str]
        self.EVALUATOR_SIGNATURE = EVALUATOR_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrp = TRP()
        tmpTrp.Init(buf, pos)
        return cls.InitFromObj(tmpTrp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrp):
        x = TRPT()
        x._UnPack(tmpTrp)
        return x

    # TRPT
    def _UnPack(self, TRP):
        if TRP is None:
            return
        self.POLICY_ID = TRP.POLICY_ID()
        self.NAME = TRP.NAME()
        self.DESCRIPTION = TRP.DESCRIPTION()
        if TRP.ROOT() is not None:
            self.ROOT = TRPGroup.TRPGroupT.InitFromObj(TRP.ROOT())
        self.EVALUATION_INTERVAL_MS = TRP.EVALUATION_INTERVAL_MS()
        if not TRP.EVENT_SOURCESIsNone():
            self.EVENT_SOURCES = []
            for i in range(TRP.EVENT_SOURCESLength()):
                self.EVENT_SOURCES.append(TRP.EVENT_SOURCES(i))
        self.ACTIVE = TRP.ACTIVE()
        self.CREATED_AT = TRP.CREATED_AT()
        self.UPDATED_AT = TRP.UPDATED_AT()
        self.EVALUATOR_PEER_ID = TRP.EVALUATOR_PEER_ID()
        if not TRP.EVALUATOR_SIGNATUREIsNone():
            if np is None:
                self.EVALUATOR_SIGNATURE = []
                for i in range(TRP.EVALUATOR_SIGNATURELength()):
                    self.EVALUATOR_SIGNATURE.append(TRP.EVALUATOR_SIGNATURE(i))
            else:
                self.EVALUATOR_SIGNATURE = TRP.EVALUATOR_SIGNATUREAsNumpy()

    # TRPT
    def Pack(self, builder):
        if self.POLICY_ID is not None:
            POLICY_ID = builder.CreateString(self.POLICY_ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.ROOT is not None:
            ROOT = self.ROOT.Pack(builder)
        if self.EVENT_SOURCES is not None:
            EVENT_SOURCESlist = []
            for i in range(len(self.EVENT_SOURCES)):
                EVENT_SOURCESlist.append(builder.CreateString(self.EVENT_SOURCES[i]))
            TRPStartEVENT_SOURCESVector(builder, len(self.EVENT_SOURCES))
            for i in reversed(range(len(self.EVENT_SOURCES))):
                builder.PrependUOffsetTRelative(EVENT_SOURCESlist[i])
            EVENT_SOURCES = builder.EndVector()
        if self.EVALUATOR_PEER_ID is not None:
            EVALUATOR_PEER_ID = builder.CreateString(self.EVALUATOR_PEER_ID)
        if self.EVALUATOR_SIGNATURE is not None:
            if np is not None and type(self.EVALUATOR_SIGNATURE) is np.ndarray:
                EVALUATOR_SIGNATURE = builder.CreateNumpyVector(self.EVALUATOR_SIGNATURE)
            else:
                TRPStartEVALUATOR_SIGNATUREVector(builder, len(self.EVALUATOR_SIGNATURE))
                for i in reversed(range(len(self.EVALUATOR_SIGNATURE))):
                    builder.PrependUint8(self.EVALUATOR_SIGNATURE[i])
                EVALUATOR_SIGNATURE = builder.EndVector()
        TRPStart(builder)
        if self.POLICY_ID is not None:
            TRPAddPOLICY_ID(builder, POLICY_ID)
        if self.NAME is not None:
            TRPAddNAME(builder, NAME)
        if self.DESCRIPTION is not None:
            TRPAddDESCRIPTION(builder, DESCRIPTION)
        if self.ROOT is not None:
            TRPAddROOT(builder, ROOT)
        TRPAddEVALUATION_INTERVAL_MS(builder, self.EVALUATION_INTERVAL_MS)
        if self.EVENT_SOURCES is not None:
            TRPAddEVENT_SOURCES(builder, EVENT_SOURCES)
        TRPAddACTIVE(builder, self.ACTIVE)
        TRPAddCREATED_AT(builder, self.CREATED_AT)
        TRPAddUPDATED_AT(builder, self.UPDATED_AT)
        if self.EVALUATOR_PEER_ID is not None:
            TRPAddEVALUATOR_PEER_ID(builder, EVALUATOR_PEER_ID)
        if self.EVALUATOR_SIGNATURE is not None:
            TRPAddEVALUATOR_SIGNATURE(builder, EVALUATOR_SIGNATURE)
        TRP = TRPEnd(builder)
        return TRP
