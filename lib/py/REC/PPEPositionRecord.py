# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A single time-segment record of polynomial coefficients for Cartesian position
# (and optionally velocity). Coefficients are stored per axis.
#
# To evaluate position at time t within this record's validity window:
#   1. Compute normalized time: tau = (t - EPOCH_MID) / EPOCH_HALF_SPAN
#      where tau is in [-1, +1].
#   2. Evaluate the polynomial basis of degree (NUM_COEFFICIENTS - 1) using
#      the coefficients for each axis.
#
# Velocity coefficients, if present, follow the same evaluation procedure.
# If HAS_VELOCITY_COEFFICIENTS is false, velocity can be obtained by
# analytically differentiating the position polynomial.
class PPEPositionRecord(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PPEPositionRecord()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPPEPositionRecord(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PPEPositionRecordBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x50\x45", size_prefixed=size_prefixed)

    # PPEPositionRecord
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Midpoint epoch of this record's validity window (ISO 8601 UTC or TDB).
    # Together with EPOCH_HALF_SPAN, defines the time interval:
    #   [EPOCH_MID - EPOCH_HALF_SPAN, EPOCH_MID + EPOCH_HALF_SPAN]
    # PPEPositionRecord
    def EPOCH_MID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Half-span of the validity window in seconds.
    # The full span is 2 * EPOCH_HALF_SPAN seconds centered on EPOCH_MID.
    # PPEPositionRecord
    def EPOCH_HALF_SPAN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Number of polynomial coefficients per axis.
    # The polynomial degree is (NUM_COEFFICIENTS - 1).
    # Typical values: 8-32 for high-fidelity ephemeris fits.
    # PPEPositionRecord
    def NUM_COEFFICIENTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Polynomial basis type for interpreting the coefficient arrays.
    # PPEPositionRecord
    def BASIS_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Position coefficients for X-axis (km).
    # Length must equal NUM_COEFFICIENTS. Ordered c0, c1, ..., c_{N-1}.
    # PPEPositionRecord
    def POS_COEFF_X(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PPEPositionRecord
    def POS_COEFF_XAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PPEPositionRecord
    def POS_COEFF_XLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPEPositionRecord
    def POS_COEFF_XIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Position coefficients for Y-axis (km).
    # Length must equal NUM_COEFFICIENTS.
    # PPEPositionRecord
    def POS_COEFF_Y(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PPEPositionRecord
    def POS_COEFF_YAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PPEPositionRecord
    def POS_COEFF_YLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPEPositionRecord
    def POS_COEFF_YIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Position coefficients for Z-axis (km).
    # Length must equal NUM_COEFFICIENTS.
    # PPEPositionRecord
    def POS_COEFF_Z(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PPEPositionRecord
    def POS_COEFF_ZAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PPEPositionRecord
    def POS_COEFF_ZLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPEPositionRecord
    def POS_COEFF_ZIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Whether explicit velocity coefficients are provided.
    # If false, velocity should be derived by differentiating the position polynomial.
    # PPEPositionRecord
    def HAS_VELOCITY_COEFFICIENTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Velocity coefficients for X-axis (km/s). Optional.
    # Length must equal NUM_COEFFICIENTS if present.
    # PPEPositionRecord
    def VEL_COEFF_X(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PPEPositionRecord
    def VEL_COEFF_XAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PPEPositionRecord
    def VEL_COEFF_XLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPEPositionRecord
    def VEL_COEFF_XIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # Velocity coefficients for Y-axis (km/s). Optional.
    # PPEPositionRecord
    def VEL_COEFF_Y(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PPEPositionRecord
    def VEL_COEFF_YAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PPEPositionRecord
    def VEL_COEFF_YLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPEPositionRecord
    def VEL_COEFF_YIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # Velocity coefficients for Z-axis (km/s). Optional.
    # PPEPositionRecord
    def VEL_COEFF_Z(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PPEPositionRecord
    def VEL_COEFF_ZAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PPEPositionRecord
    def VEL_COEFF_ZLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PPEPositionRecord
    def VEL_COEFF_ZIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # Maximum position fit residual over this segment (km). Optional quality metric.
    # PPEPositionRecord
    def MAX_POSITION_RESIDUAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RMS position fit residual over this segment (km). Optional quality metric.
    # PPEPositionRecord
    def RMS_POSITION_RESIDUAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def PPEPositionRecordStart(builder):
    builder.StartObject(13)

def Start(builder):
    PPEPositionRecordStart(builder)

def PPEPositionRecordAddEPOCH_MID(builder, EPOCH_MID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH_MID), 0)

def AddEPOCH_MID(builder, EPOCH_MID):
    PPEPositionRecordAddEPOCH_MID(builder, EPOCH_MID)

def PPEPositionRecordAddEPOCH_HALF_SPAN(builder, EPOCH_HALF_SPAN):
    builder.PrependFloat64Slot(1, EPOCH_HALF_SPAN, 0.0)

def AddEPOCH_HALF_SPAN(builder, EPOCH_HALF_SPAN):
    PPEPositionRecordAddEPOCH_HALF_SPAN(builder, EPOCH_HALF_SPAN)

def PPEPositionRecordAddNUM_COEFFICIENTS(builder, NUM_COEFFICIENTS):
    builder.PrependUint16Slot(2, NUM_COEFFICIENTS, 0)

def AddNUM_COEFFICIENTS(builder, NUM_COEFFICIENTS):
    PPEPositionRecordAddNUM_COEFFICIENTS(builder, NUM_COEFFICIENTS)

def PPEPositionRecordAddBASIS_TYPE(builder, BASIS_TYPE):
    builder.PrependInt8Slot(3, BASIS_TYPE, 0)

def AddBASIS_TYPE(builder, BASIS_TYPE):
    PPEPositionRecordAddBASIS_TYPE(builder, BASIS_TYPE)

def PPEPositionRecordAddPOS_COEFF_X(builder, POS_COEFF_X):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(POS_COEFF_X), 0)

def AddPOS_COEFF_X(builder, POS_COEFF_X):
    PPEPositionRecordAddPOS_COEFF_X(builder, POS_COEFF_X)

def PPEPositionRecordStartPOS_COEFF_XVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartPOS_COEFF_XVector(builder, numElems):
    return PPEPositionRecordStartPOS_COEFF_XVector(builder, numElems)

def PPEPositionRecordCreatePOS_COEFF_XVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreatePOS_COEFF_XVector(builder, data):
    PPEPositionRecordCreatePOS_COEFF_XVector(builder, data)

def PPEPositionRecordAddPOS_COEFF_Y(builder, POS_COEFF_Y):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(POS_COEFF_Y), 0)

def AddPOS_COEFF_Y(builder, POS_COEFF_Y):
    PPEPositionRecordAddPOS_COEFF_Y(builder, POS_COEFF_Y)

def PPEPositionRecordStartPOS_COEFF_YVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartPOS_COEFF_YVector(builder, numElems):
    return PPEPositionRecordStartPOS_COEFF_YVector(builder, numElems)

def PPEPositionRecordCreatePOS_COEFF_YVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreatePOS_COEFF_YVector(builder, data):
    PPEPositionRecordCreatePOS_COEFF_YVector(builder, data)

def PPEPositionRecordAddPOS_COEFF_Z(builder, POS_COEFF_Z):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(POS_COEFF_Z), 0)

def AddPOS_COEFF_Z(builder, POS_COEFF_Z):
    PPEPositionRecordAddPOS_COEFF_Z(builder, POS_COEFF_Z)

def PPEPositionRecordStartPOS_COEFF_ZVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartPOS_COEFF_ZVector(builder, numElems):
    return PPEPositionRecordStartPOS_COEFF_ZVector(builder, numElems)

def PPEPositionRecordCreatePOS_COEFF_ZVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreatePOS_COEFF_ZVector(builder, data):
    PPEPositionRecordCreatePOS_COEFF_ZVector(builder, data)

def PPEPositionRecordAddHAS_VELOCITY_COEFFICIENTS(builder, HAS_VELOCITY_COEFFICIENTS):
    builder.PrependBoolSlot(7, HAS_VELOCITY_COEFFICIENTS, 0)

def AddHAS_VELOCITY_COEFFICIENTS(builder, HAS_VELOCITY_COEFFICIENTS):
    PPEPositionRecordAddHAS_VELOCITY_COEFFICIENTS(builder, HAS_VELOCITY_COEFFICIENTS)

def PPEPositionRecordAddVEL_COEFF_X(builder, VEL_COEFF_X):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(VEL_COEFF_X), 0)

def AddVEL_COEFF_X(builder, VEL_COEFF_X):
    PPEPositionRecordAddVEL_COEFF_X(builder, VEL_COEFF_X)

def PPEPositionRecordStartVEL_COEFF_XVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartVEL_COEFF_XVector(builder, numElems):
    return PPEPositionRecordStartVEL_COEFF_XVector(builder, numElems)

def PPEPositionRecordCreateVEL_COEFF_XVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateVEL_COEFF_XVector(builder, data):
    PPEPositionRecordCreateVEL_COEFF_XVector(builder, data)

def PPEPositionRecordAddVEL_COEFF_Y(builder, VEL_COEFF_Y):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(VEL_COEFF_Y), 0)

def AddVEL_COEFF_Y(builder, VEL_COEFF_Y):
    PPEPositionRecordAddVEL_COEFF_Y(builder, VEL_COEFF_Y)

def PPEPositionRecordStartVEL_COEFF_YVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartVEL_COEFF_YVector(builder, numElems):
    return PPEPositionRecordStartVEL_COEFF_YVector(builder, numElems)

def PPEPositionRecordCreateVEL_COEFF_YVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateVEL_COEFF_YVector(builder, data):
    PPEPositionRecordCreateVEL_COEFF_YVector(builder, data)

def PPEPositionRecordAddVEL_COEFF_Z(builder, VEL_COEFF_Z):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(VEL_COEFF_Z), 0)

def AddVEL_COEFF_Z(builder, VEL_COEFF_Z):
    PPEPositionRecordAddVEL_COEFF_Z(builder, VEL_COEFF_Z)

def PPEPositionRecordStartVEL_COEFF_ZVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartVEL_COEFF_ZVector(builder, numElems):
    return PPEPositionRecordStartVEL_COEFF_ZVector(builder, numElems)

def PPEPositionRecordCreateVEL_COEFF_ZVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateVEL_COEFF_ZVector(builder, data):
    PPEPositionRecordCreateVEL_COEFF_ZVector(builder, data)

def PPEPositionRecordAddMAX_POSITION_RESIDUAL(builder, MAX_POSITION_RESIDUAL):
    builder.PrependFloat64Slot(11, MAX_POSITION_RESIDUAL, 0.0)

def AddMAX_POSITION_RESIDUAL(builder, MAX_POSITION_RESIDUAL):
    PPEPositionRecordAddMAX_POSITION_RESIDUAL(builder, MAX_POSITION_RESIDUAL)

def PPEPositionRecordAddRMS_POSITION_RESIDUAL(builder, RMS_POSITION_RESIDUAL):
    builder.PrependFloat64Slot(12, RMS_POSITION_RESIDUAL, 0.0)

def AddRMS_POSITION_RESIDUAL(builder, RMS_POSITION_RESIDUAL):
    PPEPositionRecordAddRMS_POSITION_RESIDUAL(builder, RMS_POSITION_RESIDUAL)

def PPEPositionRecordEnd(builder):
    return builder.EndObject()

def End(builder):
    return PPEPositionRecordEnd(builder)

try:
    from typing import List
except:
    pass

class PPEPositionRecordT(object):

    # PPEPositionRecordT
    def __init__(
        self,
        EPOCH_MID = None,
        EPOCH_HALF_SPAN = 0.0,
        NUM_COEFFICIENTS = 0,
        BASIS_TYPE = 0,
        POS_COEFF_X = None,
        POS_COEFF_Y = None,
        POS_COEFF_Z = None,
        HAS_VELOCITY_COEFFICIENTS = False,
        VEL_COEFF_X = None,
        VEL_COEFF_Y = None,
        VEL_COEFF_Z = None,
        MAX_POSITION_RESIDUAL = 0.0,
        RMS_POSITION_RESIDUAL = 0.0,
    ):
        self.EPOCH_MID = EPOCH_MID  # type: Optional[str]
        self.EPOCH_HALF_SPAN = EPOCH_HALF_SPAN  # type: float
        self.NUM_COEFFICIENTS = NUM_COEFFICIENTS  # type: int
        self.BASIS_TYPE = BASIS_TYPE  # type: int
        self.POS_COEFF_X = POS_COEFF_X  # type: Optional[List[float]]
        self.POS_COEFF_Y = POS_COEFF_Y  # type: Optional[List[float]]
        self.POS_COEFF_Z = POS_COEFF_Z  # type: Optional[List[float]]
        self.HAS_VELOCITY_COEFFICIENTS = HAS_VELOCITY_COEFFICIENTS  # type: bool
        self.VEL_COEFF_X = VEL_COEFF_X  # type: Optional[List[float]]
        self.VEL_COEFF_Y = VEL_COEFF_Y  # type: Optional[List[float]]
        self.VEL_COEFF_Z = VEL_COEFF_Z  # type: Optional[List[float]]
        self.MAX_POSITION_RESIDUAL = MAX_POSITION_RESIDUAL  # type: float
        self.RMS_POSITION_RESIDUAL = RMS_POSITION_RESIDUAL  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPpepositionRecord = PPEPositionRecord()
        tmpPpepositionRecord.Init(buf, pos)
        return cls.InitFromObj(tmpPpepositionRecord)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPpepositionRecord):
        x = PPEPositionRecordT()
        x._UnPack(tmpPpepositionRecord)
        return x

    # PPEPositionRecordT
    def _UnPack(self, PPEPositionRecord):
        if PPEPositionRecord is None:
            return
        self.EPOCH_MID = PPEPositionRecord.EPOCH_MID()
        self.EPOCH_HALF_SPAN = PPEPositionRecord.EPOCH_HALF_SPAN()
        self.NUM_COEFFICIENTS = PPEPositionRecord.NUM_COEFFICIENTS()
        self.BASIS_TYPE = PPEPositionRecord.BASIS_TYPE()
        if not PPEPositionRecord.POS_COEFF_XIsNone():
            if np is None:
                self.POS_COEFF_X = []
                for i in range(PPEPositionRecord.POS_COEFF_XLength()):
                    self.POS_COEFF_X.append(PPEPositionRecord.POS_COEFF_X(i))
            else:
                self.POS_COEFF_X = PPEPositionRecord.POS_COEFF_XAsNumpy()
        if not PPEPositionRecord.POS_COEFF_YIsNone():
            if np is None:
                self.POS_COEFF_Y = []
                for i in range(PPEPositionRecord.POS_COEFF_YLength()):
                    self.POS_COEFF_Y.append(PPEPositionRecord.POS_COEFF_Y(i))
            else:
                self.POS_COEFF_Y = PPEPositionRecord.POS_COEFF_YAsNumpy()
        if not PPEPositionRecord.POS_COEFF_ZIsNone():
            if np is None:
                self.POS_COEFF_Z = []
                for i in range(PPEPositionRecord.POS_COEFF_ZLength()):
                    self.POS_COEFF_Z.append(PPEPositionRecord.POS_COEFF_Z(i))
            else:
                self.POS_COEFF_Z = PPEPositionRecord.POS_COEFF_ZAsNumpy()
        self.HAS_VELOCITY_COEFFICIENTS = PPEPositionRecord.HAS_VELOCITY_COEFFICIENTS()
        if not PPEPositionRecord.VEL_COEFF_XIsNone():
            if np is None:
                self.VEL_COEFF_X = []
                for i in range(PPEPositionRecord.VEL_COEFF_XLength()):
                    self.VEL_COEFF_X.append(PPEPositionRecord.VEL_COEFF_X(i))
            else:
                self.VEL_COEFF_X = PPEPositionRecord.VEL_COEFF_XAsNumpy()
        if not PPEPositionRecord.VEL_COEFF_YIsNone():
            if np is None:
                self.VEL_COEFF_Y = []
                for i in range(PPEPositionRecord.VEL_COEFF_YLength()):
                    self.VEL_COEFF_Y.append(PPEPositionRecord.VEL_COEFF_Y(i))
            else:
                self.VEL_COEFF_Y = PPEPositionRecord.VEL_COEFF_YAsNumpy()
        if not PPEPositionRecord.VEL_COEFF_ZIsNone():
            if np is None:
                self.VEL_COEFF_Z = []
                for i in range(PPEPositionRecord.VEL_COEFF_ZLength()):
                    self.VEL_COEFF_Z.append(PPEPositionRecord.VEL_COEFF_Z(i))
            else:
                self.VEL_COEFF_Z = PPEPositionRecord.VEL_COEFF_ZAsNumpy()
        self.MAX_POSITION_RESIDUAL = PPEPositionRecord.MAX_POSITION_RESIDUAL()
        self.RMS_POSITION_RESIDUAL = PPEPositionRecord.RMS_POSITION_RESIDUAL()

    # PPEPositionRecordT
    def Pack(self, builder):
        if self.EPOCH_MID is not None:
            EPOCH_MID = builder.CreateString(self.EPOCH_MID)
        if self.POS_COEFF_X is not None:
            if np is not None and type(self.POS_COEFF_X) is np.ndarray:
                POS_COEFF_X = builder.CreateNumpyVector(self.POS_COEFF_X)
            else:
                PPEPositionRecordStartPOS_COEFF_XVector(builder, len(self.POS_COEFF_X))
                for i in reversed(range(len(self.POS_COEFF_X))):
                    builder.PrependFloat64(self.POS_COEFF_X[i])
                POS_COEFF_X = builder.EndVector()
        if self.POS_COEFF_Y is not None:
            if np is not None and type(self.POS_COEFF_Y) is np.ndarray:
                POS_COEFF_Y = builder.CreateNumpyVector(self.POS_COEFF_Y)
            else:
                PPEPositionRecordStartPOS_COEFF_YVector(builder, len(self.POS_COEFF_Y))
                for i in reversed(range(len(self.POS_COEFF_Y))):
                    builder.PrependFloat64(self.POS_COEFF_Y[i])
                POS_COEFF_Y = builder.EndVector()
        if self.POS_COEFF_Z is not None:
            if np is not None and type(self.POS_COEFF_Z) is np.ndarray:
                POS_COEFF_Z = builder.CreateNumpyVector(self.POS_COEFF_Z)
            else:
                PPEPositionRecordStartPOS_COEFF_ZVector(builder, len(self.POS_COEFF_Z))
                for i in reversed(range(len(self.POS_COEFF_Z))):
                    builder.PrependFloat64(self.POS_COEFF_Z[i])
                POS_COEFF_Z = builder.EndVector()
        if self.VEL_COEFF_X is not None:
            if np is not None and type(self.VEL_COEFF_X) is np.ndarray:
                VEL_COEFF_X = builder.CreateNumpyVector(self.VEL_COEFF_X)
            else:
                PPEPositionRecordStartVEL_COEFF_XVector(builder, len(self.VEL_COEFF_X))
                for i in reversed(range(len(self.VEL_COEFF_X))):
                    builder.PrependFloat64(self.VEL_COEFF_X[i])
                VEL_COEFF_X = builder.EndVector()
        if self.VEL_COEFF_Y is not None:
            if np is not None and type(self.VEL_COEFF_Y) is np.ndarray:
                VEL_COEFF_Y = builder.CreateNumpyVector(self.VEL_COEFF_Y)
            else:
                PPEPositionRecordStartVEL_COEFF_YVector(builder, len(self.VEL_COEFF_Y))
                for i in reversed(range(len(self.VEL_COEFF_Y))):
                    builder.PrependFloat64(self.VEL_COEFF_Y[i])
                VEL_COEFF_Y = builder.EndVector()
        if self.VEL_COEFF_Z is not None:
            if np is not None and type(self.VEL_COEFF_Z) is np.ndarray:
                VEL_COEFF_Z = builder.CreateNumpyVector(self.VEL_COEFF_Z)
            else:
                PPEPositionRecordStartVEL_COEFF_ZVector(builder, len(self.VEL_COEFF_Z))
                for i in reversed(range(len(self.VEL_COEFF_Z))):
                    builder.PrependFloat64(self.VEL_COEFF_Z[i])
                VEL_COEFF_Z = builder.EndVector()
        PPEPositionRecordStart(builder)
        if self.EPOCH_MID is not None:
            PPEPositionRecordAddEPOCH_MID(builder, EPOCH_MID)
        PPEPositionRecordAddEPOCH_HALF_SPAN(builder, self.EPOCH_HALF_SPAN)
        PPEPositionRecordAddNUM_COEFFICIENTS(builder, self.NUM_COEFFICIENTS)
        PPEPositionRecordAddBASIS_TYPE(builder, self.BASIS_TYPE)
        if self.POS_COEFF_X is not None:
            PPEPositionRecordAddPOS_COEFF_X(builder, POS_COEFF_X)
        if self.POS_COEFF_Y is not None:
            PPEPositionRecordAddPOS_COEFF_Y(builder, POS_COEFF_Y)
        if self.POS_COEFF_Z is not None:
            PPEPositionRecordAddPOS_COEFF_Z(builder, POS_COEFF_Z)
        PPEPositionRecordAddHAS_VELOCITY_COEFFICIENTS(builder, self.HAS_VELOCITY_COEFFICIENTS)
        if self.VEL_COEFF_X is not None:
            PPEPositionRecordAddVEL_COEFF_X(builder, VEL_COEFF_X)
        if self.VEL_COEFF_Y is not None:
            PPEPositionRecordAddVEL_COEFF_Y(builder, VEL_COEFF_Y)
        if self.VEL_COEFF_Z is not None:
            PPEPositionRecordAddVEL_COEFF_Z(builder, VEL_COEFF_Z)
        PPEPositionRecordAddMAX_POSITION_RESIDUAL(builder, self.MAX_POSITION_RESIDUAL)
        PPEPositionRecordAddRMS_POSITION_RESIDUAL(builder, self.RMS_POSITION_RESIDUAL)
        PPEPositionRecord = PPEPositionRecordEnd(builder)
        return PPEPositionRecord
