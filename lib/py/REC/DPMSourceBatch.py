# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Source batch metadata bound into the dataset publication.
class DPMSourceBatch(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DPMSourceBatch()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDPMSourceBatch(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DPMSourceBatchBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x44\x50\x4D", size_prefixed=size_prefixed)

    # DPMSourceBatch
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Provider-controlled source name.
    # DPMSourceBatch
    def SOURCE_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Original source URL.
    # DPMSourceBatch
    def SOURCE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SHA-256 hash of raw source bytes, lowercase hex.
    # DPMSourceBatch
    def SOURCE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # HTTP ETag observed during retrieval.
    # DPMSourceBatch
    def HTTP_ETAG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # HTTP Last-Modified value observed during retrieval.
    # DPMSourceBatch
    def HTTP_LAST_MODIFIED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Retrieval timestamp in ISO 8601 UTC.
    # DPMSourceBatch
    def RETRIEVED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Parser or transform version.
    # DPMSourceBatch
    def PARSER_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of normalized records emitted from this source.
    # DPMSourceBatch
    def RECORD_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Warnings or policy notes produced during normalization.
    # DPMSourceBatch
    def WARNINGS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # DPMSourceBatch
    def WARNINGSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DPMSourceBatch
    def WARNINGSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

def DPMSourceBatchStart(builder):
    builder.StartObject(9)

def Start(builder):
    DPMSourceBatchStart(builder)

def DPMSourceBatchAddSOURCE_NAME(builder, SOURCE_NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_NAME), 0)

def AddSOURCE_NAME(builder, SOURCE_NAME):
    DPMSourceBatchAddSOURCE_NAME(builder, SOURCE_NAME)

def DPMSourceBatchAddSOURCE_URL(builder, SOURCE_URL):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_URL), 0)

def AddSOURCE_URL(builder, SOURCE_URL):
    DPMSourceBatchAddSOURCE_URL(builder, SOURCE_URL)

def DPMSourceBatchAddSOURCE_SHA256(builder, SOURCE_SHA256):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_SHA256), 0)

def AddSOURCE_SHA256(builder, SOURCE_SHA256):
    DPMSourceBatchAddSOURCE_SHA256(builder, SOURCE_SHA256)

def DPMSourceBatchAddHTTP_ETAG(builder, HTTP_ETAG):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(HTTP_ETAG), 0)

def AddHTTP_ETAG(builder, HTTP_ETAG):
    DPMSourceBatchAddHTTP_ETAG(builder, HTTP_ETAG)

def DPMSourceBatchAddHTTP_LAST_MODIFIED(builder, HTTP_LAST_MODIFIED):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(HTTP_LAST_MODIFIED), 0)

def AddHTTP_LAST_MODIFIED(builder, HTTP_LAST_MODIFIED):
    DPMSourceBatchAddHTTP_LAST_MODIFIED(builder, HTTP_LAST_MODIFIED)

def DPMSourceBatchAddRETRIEVED_AT(builder, RETRIEVED_AT):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(RETRIEVED_AT), 0)

def AddRETRIEVED_AT(builder, RETRIEVED_AT):
    DPMSourceBatchAddRETRIEVED_AT(builder, RETRIEVED_AT)

def DPMSourceBatchAddPARSER_VERSION(builder, PARSER_VERSION):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PARSER_VERSION), 0)

def AddPARSER_VERSION(builder, PARSER_VERSION):
    DPMSourceBatchAddPARSER_VERSION(builder, PARSER_VERSION)

def DPMSourceBatchAddRECORD_COUNT(builder, RECORD_COUNT):
    builder.PrependUint64Slot(7, RECORD_COUNT, 0)

def AddRECORD_COUNT(builder, RECORD_COUNT):
    DPMSourceBatchAddRECORD_COUNT(builder, RECORD_COUNT)

def DPMSourceBatchAddWARNINGS(builder, WARNINGS):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(WARNINGS), 0)

def AddWARNINGS(builder, WARNINGS):
    DPMSourceBatchAddWARNINGS(builder, WARNINGS)

def DPMSourceBatchStartWARNINGSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartWARNINGSVector(builder, numElems):
    return DPMSourceBatchStartWARNINGSVector(builder, numElems)

def DPMSourceBatchCreateWARNINGSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateWARNINGSVector(builder, data):
    DPMSourceBatchCreateWARNINGSVector(builder, data)

def DPMSourceBatchEnd(builder):
    return builder.EndObject()

def End(builder):
    return DPMSourceBatchEnd(builder)

try:
    from typing import List
except:
    pass

class DPMSourceBatchT(object):

    # DPMSourceBatchT
    def __init__(
        self,
        SOURCE_NAME = None,
        SOURCE_URL = None,
        SOURCE_SHA256 = None,
        HTTP_ETAG = None,
        HTTP_LAST_MODIFIED = None,
        RETRIEVED_AT = None,
        PARSER_VERSION = None,
        RECORD_COUNT = 0,
        WARNINGS = None,
    ):
        self.SOURCE_NAME = SOURCE_NAME  # type: Optional[str]
        self.SOURCE_URL = SOURCE_URL  # type: Optional[str]
        self.SOURCE_SHA256 = SOURCE_SHA256  # type: Optional[str]
        self.HTTP_ETAG = HTTP_ETAG  # type: Optional[str]
        self.HTTP_LAST_MODIFIED = HTTP_LAST_MODIFIED  # type: Optional[str]
        self.RETRIEVED_AT = RETRIEVED_AT  # type: Optional[str]
        self.PARSER_VERSION = PARSER_VERSION  # type: Optional[str]
        self.RECORD_COUNT = RECORD_COUNT  # type: int
        self.WARNINGS = WARNINGS  # type: Optional[List[Optional[str]]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpDpmsourceBatch = DPMSourceBatch()
        tmpDpmsourceBatch.Init(buf, pos)
        return cls.InitFromObj(tmpDpmsourceBatch)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpDpmsourceBatch):
        x = DPMSourceBatchT()
        x._UnPack(tmpDpmsourceBatch)
        return x

    # DPMSourceBatchT
    def _UnPack(self, DPMSourceBatch):
        if DPMSourceBatch is None:
            return
        self.SOURCE_NAME = DPMSourceBatch.SOURCE_NAME()
        self.SOURCE_URL = DPMSourceBatch.SOURCE_URL()
        self.SOURCE_SHA256 = DPMSourceBatch.SOURCE_SHA256()
        self.HTTP_ETAG = DPMSourceBatch.HTTP_ETAG()
        self.HTTP_LAST_MODIFIED = DPMSourceBatch.HTTP_LAST_MODIFIED()
        self.RETRIEVED_AT = DPMSourceBatch.RETRIEVED_AT()
        self.PARSER_VERSION = DPMSourceBatch.PARSER_VERSION()
        self.RECORD_COUNT = DPMSourceBatch.RECORD_COUNT()
        if not DPMSourceBatch.WARNINGSIsNone():
            self.WARNINGS = []
            for i in range(DPMSourceBatch.WARNINGSLength()):
                self.WARNINGS.append(DPMSourceBatch.WARNINGS(i))

    # DPMSourceBatchT
    def Pack(self, builder):
        if self.SOURCE_NAME is not None:
            SOURCE_NAME = builder.CreateString(self.SOURCE_NAME)
        if self.SOURCE_URL is not None:
            SOURCE_URL = builder.CreateString(self.SOURCE_URL)
        if self.SOURCE_SHA256 is not None:
            SOURCE_SHA256 = builder.CreateString(self.SOURCE_SHA256)
        if self.HTTP_ETAG is not None:
            HTTP_ETAG = builder.CreateString(self.HTTP_ETAG)
        if self.HTTP_LAST_MODIFIED is not None:
            HTTP_LAST_MODIFIED = builder.CreateString(self.HTTP_LAST_MODIFIED)
        if self.RETRIEVED_AT is not None:
            RETRIEVED_AT = builder.CreateString(self.RETRIEVED_AT)
        if self.PARSER_VERSION is not None:
            PARSER_VERSION = builder.CreateString(self.PARSER_VERSION)
        if self.WARNINGS is not None:
            WARNINGSlist = []
            for i in range(len(self.WARNINGS)):
                WARNINGSlist.append(builder.CreateString(self.WARNINGS[i]))
            DPMSourceBatchStartWARNINGSVector(builder, len(self.WARNINGS))
            for i in reversed(range(len(self.WARNINGS))):
                builder.PrependUOffsetTRelative(WARNINGSlist[i])
            WARNINGS = builder.EndVector()
        DPMSourceBatchStart(builder)
        if self.SOURCE_NAME is not None:
            DPMSourceBatchAddSOURCE_NAME(builder, SOURCE_NAME)
        if self.SOURCE_URL is not None:
            DPMSourceBatchAddSOURCE_URL(builder, SOURCE_URL)
        if self.SOURCE_SHA256 is not None:
            DPMSourceBatchAddSOURCE_SHA256(builder, SOURCE_SHA256)
        if self.HTTP_ETAG is not None:
            DPMSourceBatchAddHTTP_ETAG(builder, HTTP_ETAG)
        if self.HTTP_LAST_MODIFIED is not None:
            DPMSourceBatchAddHTTP_LAST_MODIFIED(builder, HTTP_LAST_MODIFIED)
        if self.RETRIEVED_AT is not None:
            DPMSourceBatchAddRETRIEVED_AT(builder, RETRIEVED_AT)
        if self.PARSER_VERSION is not None:
            DPMSourceBatchAddPARSER_VERSION(builder, PARSER_VERSION)
        DPMSourceBatchAddRECORD_COUNT(builder, self.RECORD_COUNT)
        if self.WARNINGS is not None:
            DPMSourceBatchAddWARNINGS(builder, WARNINGS)
        DPMSourceBatch = DPMSourceBatchEnd(builder)
        return DPMSourceBatch
