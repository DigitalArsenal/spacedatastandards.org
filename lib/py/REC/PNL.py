# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Panelled (box-wing) Spacecraft Macro Model — the surface geometry and
# optical properties needed to compute non-conservative forces (solar
# radiation pressure, atmospheric drag, thermal re-radiation) on one object.
#
# This is deliberately NOT $VAM, $BUS or $PHY. $VAM ranks visual assets for
# rendering and a render mesh is not a force model; $BUS/$OOD are descriptive
# inventory; $PHY is simulation configuration (integration method, force-type
# toggles). None of them carries per-panel area, normal, and optical
# coefficients.
class PNL(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PNL()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPNL(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PNLBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4E\x4C", size_prefixed=size_prefixed)

    # PNL
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Object this macro model describes.
    # PNL
    def OBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Advisory object name.
    # PNL
    def OBJECT_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Satellite catalog number, when the object is catalogued.
    # PNL
    def NORAD_CAT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Body-fixed frame the panel normals are expressed in, e.g. "SC_BODY".
    # PNL
    def BODY_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Total spacecraft mass the model corresponds to, kilograms.
    # PNL
    def TOTAL_MASS_KG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Epoch the model is valid at, ISO 8601. Mass and panel configuration
    # change over a mission; a macro model without an epoch is ambiguous.
    # PNL
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The panels.
    # PNL
    def PANELS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PNLPanel import PNLPanel
            obj = PNLPanel()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PNL
    def PANELSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PNL
    def PANELSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Reference cross-sectional area used by a consumer that falls back to a
    # cannonball model, square metres.
    # PNL
    def REFERENCE_AREA_M2(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Provenance of the geometry and the optical coefficients.
    # PNL
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PNLStart(builder):
    builder.StartObject(9)

def Start(builder):
    PNLStart(builder)

def PNLAddOBJECT_ID(builder, OBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT_ID), 0)

def AddOBJECT_ID(builder, OBJECT_ID):
    PNLAddOBJECT_ID(builder, OBJECT_ID)

def PNLAddOBJECT_NAME(builder, OBJECT_NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT_NAME), 0)

def AddOBJECT_NAME(builder, OBJECT_NAME):
    PNLAddOBJECT_NAME(builder, OBJECT_NAME)

def PNLAddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    builder.PrependUint32Slot(2, NORAD_CAT_ID, 0)

def AddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    PNLAddNORAD_CAT_ID(builder, NORAD_CAT_ID)

def PNLAddBODY_FRAME(builder, BODY_FRAME):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(BODY_FRAME), 0)

def AddBODY_FRAME(builder, BODY_FRAME):
    PNLAddBODY_FRAME(builder, BODY_FRAME)

def PNLAddTOTAL_MASS_KG(builder, TOTAL_MASS_KG):
    builder.PrependFloat64Slot(4, TOTAL_MASS_KG, 0.0)

def AddTOTAL_MASS_KG(builder, TOTAL_MASS_KG):
    PNLAddTOTAL_MASS_KG(builder, TOTAL_MASS_KG)

def PNLAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    PNLAddEPOCH(builder, EPOCH)

def PNLAddPANELS(builder, PANELS):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PANELS), 0)

def AddPANELS(builder, PANELS):
    PNLAddPANELS(builder, PANELS)

def PNLStartPANELSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPANELSVector(builder, numElems):
    return PNLStartPANELSVector(builder, numElems)

def PNLCreatePANELSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePANELSVector(builder, data):
    PNLCreatePANELSVector(builder, data)

def PNLAddREFERENCE_AREA_M2(builder, REFERENCE_AREA_M2):
    builder.PrependFloat64Slot(7, REFERENCE_AREA_M2, 0.0)

def AddREFERENCE_AREA_M2(builder, REFERENCE_AREA_M2):
    PNLAddREFERENCE_AREA_M2(builder, REFERENCE_AREA_M2)

def PNLAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    PNLAddSOURCE(builder, SOURCE)

def PNLEnd(builder):
    return builder.EndObject()

def End(builder):
    return PNLEnd(builder)

import PNLPanel
try:
    from typing import List
except:
    pass

class PNLT(object):

    # PNLT
    def __init__(
        self,
        OBJECT_ID = None,
        OBJECT_NAME = None,
        NORAD_CAT_ID = 0,
        BODY_FRAME = None,
        TOTAL_MASS_KG = 0.0,
        EPOCH = None,
        PANELS = None,
        REFERENCE_AREA_M2 = 0.0,
        SOURCE = None,
    ):
        self.OBJECT_ID = OBJECT_ID  # type: Optional[str]
        self.OBJECT_NAME = OBJECT_NAME  # type: Optional[str]
        self.NORAD_CAT_ID = NORAD_CAT_ID  # type: int
        self.BODY_FRAME = BODY_FRAME  # type: Optional[str]
        self.TOTAL_MASS_KG = TOTAL_MASS_KG  # type: float
        self.EPOCH = EPOCH  # type: Optional[str]
        self.PANELS = PANELS  # type: Optional[List[PNLPanel.PNLPanelT]]
        self.REFERENCE_AREA_M2 = REFERENCE_AREA_M2  # type: float
        self.SOURCE = SOURCE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPnl = PNL()
        tmpPnl.Init(buf, pos)
        return cls.InitFromObj(tmpPnl)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPnl):
        x = PNLT()
        x._UnPack(tmpPnl)
        return x

    # PNLT
    def _UnPack(self, PNL):
        if PNL is None:
            return
        self.OBJECT_ID = PNL.OBJECT_ID()
        self.OBJECT_NAME = PNL.OBJECT_NAME()
        self.NORAD_CAT_ID = PNL.NORAD_CAT_ID()
        self.BODY_FRAME = PNL.BODY_FRAME()
        self.TOTAL_MASS_KG = PNL.TOTAL_MASS_KG()
        self.EPOCH = PNL.EPOCH()
        if not PNL.PANELSIsNone():
            self.PANELS = []
            for i in range(PNL.PANELSLength()):
                if PNL.PANELS(i) is None:
                    self.PANELS.append(None)
                else:
                    pNLPanel_ = PNLPanel.PNLPanelT.InitFromObj(PNL.PANELS(i))
                    self.PANELS.append(pNLPanel_)
        self.REFERENCE_AREA_M2 = PNL.REFERENCE_AREA_M2()
        self.SOURCE = PNL.SOURCE()

    # PNLT
    def Pack(self, builder):
        if self.OBJECT_ID is not None:
            OBJECT_ID = builder.CreateString(self.OBJECT_ID)
        if self.OBJECT_NAME is not None:
            OBJECT_NAME = builder.CreateString(self.OBJECT_NAME)
        if self.BODY_FRAME is not None:
            BODY_FRAME = builder.CreateString(self.BODY_FRAME)
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        if self.PANELS is not None:
            PANELSlist = []
            for i in range(len(self.PANELS)):
                PANELSlist.append(self.PANELS[i].Pack(builder))
            PNLStartPANELSVector(builder, len(self.PANELS))
            for i in reversed(range(len(self.PANELS))):
                builder.PrependUOffsetTRelative(PANELSlist[i])
            PANELS = builder.EndVector()
        if self.SOURCE is not None:
            SOURCE = builder.CreateString(self.SOURCE)
        PNLStart(builder)
        if self.OBJECT_ID is not None:
            PNLAddOBJECT_ID(builder, OBJECT_ID)
        if self.OBJECT_NAME is not None:
            PNLAddOBJECT_NAME(builder, OBJECT_NAME)
        PNLAddNORAD_CAT_ID(builder, self.NORAD_CAT_ID)
        if self.BODY_FRAME is not None:
            PNLAddBODY_FRAME(builder, BODY_FRAME)
        PNLAddTOTAL_MASS_KG(builder, self.TOTAL_MASS_KG)
        if self.EPOCH is not None:
            PNLAddEPOCH(builder, EPOCH)
        if self.PANELS is not None:
            PNLAddPANELS(builder, PANELS)
        PNLAddREFERENCE_AREA_M2(builder, self.REFERENCE_AREA_M2)
        if self.SOURCE is not None:
            PNLAddSOURCE(builder, SOURCE)
        PNL = PNLEnd(builder)
        return PNL
