# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Vehicle Endurance Profile
class VEP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VEP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVEP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VEPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x45\x50", size_prefixed=size_prefixed)

    # VEP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher-stable identifier for this profile record.
    # VEP
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Publisher-stable identifier of the vehicle CLASS this profile models.
    # Required: every profile is at least class-level.
    # VEP
    def VEHICLE_CLASS_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Verbatim vehicle type token refining the class, when this profile is
    # a per-type refinement.
    # VEP
    def VEHICLE_TYPE_DESIGNATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Domain the vehicle operates in. The `$TRK` enum reused verbatim;
    # ordinal 0 is SPACE, so it is defaulted to UNKNOWN explicitly.
    # VEP
    def ENVIRONMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 5

    # Kind of energy store.
    # VEP
    def ENERGY_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Usable energy store capacity, in CAPACITY_UNITS.
    # VEP
    def CAPACITY_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Units of CAPACITY_VALUE, verbatim. The same unit keys
    # SPECIFIC_RANGE_M_PER_UNIT and the replenishment transfer rates.
    # VEP
    def CAPACITY_UNITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Fraction of capacity [0-1] held as reserve; endurance computations
    # spend down to the reserve, never through it.
    # VEP
    def RESERVE_FRACTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # The burn-rate table, keyed regime x altitude x speed. Required, and
    # never a single collapsed scalar.
    # VEP
    def PERFORMANCE_POINTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from VEPPerformancePoint import VEPPerformancePoint
            obj = VEPPerformancePoint()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VEP
    def PERFORMANCE_POINTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VEP
    def PERFORMANCE_POINTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # How a consumer interpolates between the points.
    # VEP
    def INTERPOLATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Maximum endurance, seconds, as the source publishes it.
    # VEP
    def MAXIMUM_ENDURANCE_S(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum range, metres, as the source publishes it.
    # VEP
    def MAXIMUM_RANGE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Best-loiter speed, metres per second.
    # VEP
    def LOITER_SPEED_MPS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Service ceiling, metres above mean sea level.
    # VEP
    def SERVICE_CEILING_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Replenishment options available to this class or type.
    # VEP
    def REPLENISHMENT(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from VEPReplenishment import VEPReplenishment
            obj = VEPReplenishment()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VEP
    def REPLENISHMENTLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VEP
    def REPLENISHMENTIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

    # `$VEP.ID` of the parent profile this one refines — the class-to-type
    # specialization chain. Absent for a class-level root profile.
    # VEP
    def REFINES_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Dataset lineage and licence. Required.
    # VEP
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VEPProvenance import VEPProvenance
            obj = VEPProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def VEPStart(builder):
    builder.StartObject(17)

def Start(builder):
    VEPStart(builder)

def VEPAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    VEPAddID(builder, ID)

def VEPAddVEHICLE_CLASS_ID(builder, VEHICLE_CLASS_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VEHICLE_CLASS_ID), 0)

def AddVEHICLE_CLASS_ID(builder, VEHICLE_CLASS_ID):
    VEPAddVEHICLE_CLASS_ID(builder, VEHICLE_CLASS_ID)

def VEPAddVEHICLE_TYPE_DESIGNATOR(builder, VEHICLE_TYPE_DESIGNATOR):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VEHICLE_TYPE_DESIGNATOR), 0)

def AddVEHICLE_TYPE_DESIGNATOR(builder, VEHICLE_TYPE_DESIGNATOR):
    VEPAddVEHICLE_TYPE_DESIGNATOR(builder, VEHICLE_TYPE_DESIGNATOR)

def VEPAddENVIRONMENT(builder, ENVIRONMENT):
    builder.PrependInt8Slot(3, ENVIRONMENT, 5)

def AddENVIRONMENT(builder, ENVIRONMENT):
    VEPAddENVIRONMENT(builder, ENVIRONMENT)

def VEPAddENERGY_TYPE(builder, ENERGY_TYPE):
    builder.PrependInt8Slot(4, ENERGY_TYPE, 0)

def AddENERGY_TYPE(builder, ENERGY_TYPE):
    VEPAddENERGY_TYPE(builder, ENERGY_TYPE)

def VEPAddCAPACITY_VALUE(builder, CAPACITY_VALUE):
    builder.PrependFloat64Slot(5, CAPACITY_VALUE, 0.0)

def AddCAPACITY_VALUE(builder, CAPACITY_VALUE):
    VEPAddCAPACITY_VALUE(builder, CAPACITY_VALUE)

def VEPAddCAPACITY_UNITS(builder, CAPACITY_UNITS):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(CAPACITY_UNITS), 0)

def AddCAPACITY_UNITS(builder, CAPACITY_UNITS):
    VEPAddCAPACITY_UNITS(builder, CAPACITY_UNITS)

def VEPAddRESERVE_FRACTION(builder, RESERVE_FRACTION):
    builder.PrependFloat64Slot(7, RESERVE_FRACTION, 0.0)

def AddRESERVE_FRACTION(builder, RESERVE_FRACTION):
    VEPAddRESERVE_FRACTION(builder, RESERVE_FRACTION)

def VEPAddPERFORMANCE_POINTS(builder, PERFORMANCE_POINTS):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(PERFORMANCE_POINTS), 0)

def AddPERFORMANCE_POINTS(builder, PERFORMANCE_POINTS):
    VEPAddPERFORMANCE_POINTS(builder, PERFORMANCE_POINTS)

def VEPStartPERFORMANCE_POINTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPERFORMANCE_POINTSVector(builder, numElems):
    return VEPStartPERFORMANCE_POINTSVector(builder, numElems)

def VEPCreatePERFORMANCE_POINTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePERFORMANCE_POINTSVector(builder, data):
    VEPCreatePERFORMANCE_POINTSVector(builder, data)

def VEPAddINTERPOLATION(builder, INTERPOLATION):
    builder.PrependInt8Slot(9, INTERPOLATION, 0)

def AddINTERPOLATION(builder, INTERPOLATION):
    VEPAddINTERPOLATION(builder, INTERPOLATION)

def VEPAddMAXIMUM_ENDURANCE_S(builder, MAXIMUM_ENDURANCE_S):
    builder.PrependFloat64Slot(10, MAXIMUM_ENDURANCE_S, 0.0)

def AddMAXIMUM_ENDURANCE_S(builder, MAXIMUM_ENDURANCE_S):
    VEPAddMAXIMUM_ENDURANCE_S(builder, MAXIMUM_ENDURANCE_S)

def VEPAddMAXIMUM_RANGE_M(builder, MAXIMUM_RANGE_M):
    builder.PrependFloat64Slot(11, MAXIMUM_RANGE_M, 0.0)

def AddMAXIMUM_RANGE_M(builder, MAXIMUM_RANGE_M):
    VEPAddMAXIMUM_RANGE_M(builder, MAXIMUM_RANGE_M)

def VEPAddLOITER_SPEED_MPS(builder, LOITER_SPEED_MPS):
    builder.PrependFloat64Slot(12, LOITER_SPEED_MPS, 0.0)

def AddLOITER_SPEED_MPS(builder, LOITER_SPEED_MPS):
    VEPAddLOITER_SPEED_MPS(builder, LOITER_SPEED_MPS)

def VEPAddSERVICE_CEILING_M(builder, SERVICE_CEILING_M):
    builder.PrependFloat64Slot(13, SERVICE_CEILING_M, 0.0)

def AddSERVICE_CEILING_M(builder, SERVICE_CEILING_M):
    VEPAddSERVICE_CEILING_M(builder, SERVICE_CEILING_M)

def VEPAddREPLENISHMENT(builder, REPLENISHMENT):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(REPLENISHMENT), 0)

def AddREPLENISHMENT(builder, REPLENISHMENT):
    VEPAddREPLENISHMENT(builder, REPLENISHMENT)

def VEPStartREPLENISHMENTVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartREPLENISHMENTVector(builder, numElems):
    return VEPStartREPLENISHMENTVector(builder, numElems)

def VEPCreateREPLENISHMENTVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateREPLENISHMENTVector(builder, data):
    VEPCreateREPLENISHMENTVector(builder, data)

def VEPAddREFINES_ID(builder, REFINES_ID):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(REFINES_ID), 0)

def AddREFINES_ID(builder, REFINES_ID):
    VEPAddREFINES_ID(builder, REFINES_ID)

def VEPAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    VEPAddSOURCE(builder, SOURCE)

def VEPEnd(builder):
    return builder.EndObject()

def End(builder):
    return VEPEnd(builder)

import VEPPerformancePoint
import VEPProvenance
import VEPReplenishment
try:
    from typing import List, Optional
except:
    pass

class VEPT(object):

    # VEPT
    def __init__(
        self,
        ID = None,
        VEHICLE_CLASS_ID = None,
        VEHICLE_TYPE_DESIGNATOR = None,
        ENVIRONMENT = 5,
        ENERGY_TYPE = 0,
        CAPACITY_VALUE = 0.0,
        CAPACITY_UNITS = None,
        RESERVE_FRACTION = 0.0,
        PERFORMANCE_POINTS = None,
        INTERPOLATION = 0,
        MAXIMUM_ENDURANCE_S = 0.0,
        MAXIMUM_RANGE_M = 0.0,
        LOITER_SPEED_MPS = 0.0,
        SERVICE_CEILING_M = 0.0,
        REPLENISHMENT = None,
        REFINES_ID = None,
        SOURCE = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.VEHICLE_CLASS_ID = VEHICLE_CLASS_ID  # type: Optional[str]
        self.VEHICLE_TYPE_DESIGNATOR = VEHICLE_TYPE_DESIGNATOR  # type: Optional[str]
        self.ENVIRONMENT = ENVIRONMENT  # type: int
        self.ENERGY_TYPE = ENERGY_TYPE  # type: int
        self.CAPACITY_VALUE = CAPACITY_VALUE  # type: float
        self.CAPACITY_UNITS = CAPACITY_UNITS  # type: Optional[str]
        self.RESERVE_FRACTION = RESERVE_FRACTION  # type: float
        self.PERFORMANCE_POINTS = PERFORMANCE_POINTS  # type: Optional[List[VEPPerformancePoint.VEPPerformancePointT]]
        self.INTERPOLATION = INTERPOLATION  # type: int
        self.MAXIMUM_ENDURANCE_S = MAXIMUM_ENDURANCE_S  # type: float
        self.MAXIMUM_RANGE_M = MAXIMUM_RANGE_M  # type: float
        self.LOITER_SPEED_MPS = LOITER_SPEED_MPS  # type: float
        self.SERVICE_CEILING_M = SERVICE_CEILING_M  # type: float
        self.REPLENISHMENT = REPLENISHMENT  # type: Optional[List[VEPReplenishment.VEPReplenishmentT]]
        self.REFINES_ID = REFINES_ID  # type: Optional[str]
        self.SOURCE = SOURCE  # type: Optional[VEPProvenance.VEPProvenanceT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVep = VEP()
        tmpVep.Init(buf, pos)
        return cls.InitFromObj(tmpVep)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVep):
        x = VEPT()
        x._UnPack(tmpVep)
        return x

    # VEPT
    def _UnPack(self, VEP):
        if VEP is None:
            return
        self.ID = VEP.ID()
        self.VEHICLE_CLASS_ID = VEP.VEHICLE_CLASS_ID()
        self.VEHICLE_TYPE_DESIGNATOR = VEP.VEHICLE_TYPE_DESIGNATOR()
        self.ENVIRONMENT = VEP.ENVIRONMENT()
        self.ENERGY_TYPE = VEP.ENERGY_TYPE()
        self.CAPACITY_VALUE = VEP.CAPACITY_VALUE()
        self.CAPACITY_UNITS = VEP.CAPACITY_UNITS()
        self.RESERVE_FRACTION = VEP.RESERVE_FRACTION()
        if not VEP.PERFORMANCE_POINTSIsNone():
            self.PERFORMANCE_POINTS = []
            for i in range(VEP.PERFORMANCE_POINTSLength()):
                if VEP.PERFORMANCE_POINTS(i) is None:
                    self.PERFORMANCE_POINTS.append(None)
                else:
                    vEPPerformancePoint_ = VEPPerformancePoint.VEPPerformancePointT.InitFromObj(VEP.PERFORMANCE_POINTS(i))
                    self.PERFORMANCE_POINTS.append(vEPPerformancePoint_)
        self.INTERPOLATION = VEP.INTERPOLATION()
        self.MAXIMUM_ENDURANCE_S = VEP.MAXIMUM_ENDURANCE_S()
        self.MAXIMUM_RANGE_M = VEP.MAXIMUM_RANGE_M()
        self.LOITER_SPEED_MPS = VEP.LOITER_SPEED_MPS()
        self.SERVICE_CEILING_M = VEP.SERVICE_CEILING_M()
        if not VEP.REPLENISHMENTIsNone():
            self.REPLENISHMENT = []
            for i in range(VEP.REPLENISHMENTLength()):
                if VEP.REPLENISHMENT(i) is None:
                    self.REPLENISHMENT.append(None)
                else:
                    vEPReplenishment_ = VEPReplenishment.VEPReplenishmentT.InitFromObj(VEP.REPLENISHMENT(i))
                    self.REPLENISHMENT.append(vEPReplenishment_)
        self.REFINES_ID = VEP.REFINES_ID()
        if VEP.SOURCE() is not None:
            self.SOURCE = VEPProvenance.VEPProvenanceT.InitFromObj(VEP.SOURCE())

    # VEPT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.VEHICLE_CLASS_ID is not None:
            VEHICLE_CLASS_ID = builder.CreateString(self.VEHICLE_CLASS_ID)
        if self.VEHICLE_TYPE_DESIGNATOR is not None:
            VEHICLE_TYPE_DESIGNATOR = builder.CreateString(self.VEHICLE_TYPE_DESIGNATOR)
        if self.CAPACITY_UNITS is not None:
            CAPACITY_UNITS = builder.CreateString(self.CAPACITY_UNITS)
        if self.PERFORMANCE_POINTS is not None:
            PERFORMANCE_POINTSlist = []
            for i in range(len(self.PERFORMANCE_POINTS)):
                PERFORMANCE_POINTSlist.append(self.PERFORMANCE_POINTS[i].Pack(builder))
            VEPStartPERFORMANCE_POINTSVector(builder, len(self.PERFORMANCE_POINTS))
            for i in reversed(range(len(self.PERFORMANCE_POINTS))):
                builder.PrependUOffsetTRelative(PERFORMANCE_POINTSlist[i])
            PERFORMANCE_POINTS = builder.EndVector()
        if self.REPLENISHMENT is not None:
            REPLENISHMENTlist = []
            for i in range(len(self.REPLENISHMENT)):
                REPLENISHMENTlist.append(self.REPLENISHMENT[i].Pack(builder))
            VEPStartREPLENISHMENTVector(builder, len(self.REPLENISHMENT))
            for i in reversed(range(len(self.REPLENISHMENT))):
                builder.PrependUOffsetTRelative(REPLENISHMENTlist[i])
            REPLENISHMENT = builder.EndVector()
        if self.REFINES_ID is not None:
            REFINES_ID = builder.CreateString(self.REFINES_ID)
        if self.SOURCE is not None:
            SOURCE = self.SOURCE.Pack(builder)
        VEPStart(builder)
        if self.ID is not None:
            VEPAddID(builder, ID)
        if self.VEHICLE_CLASS_ID is not None:
            VEPAddVEHICLE_CLASS_ID(builder, VEHICLE_CLASS_ID)
        if self.VEHICLE_TYPE_DESIGNATOR is not None:
            VEPAddVEHICLE_TYPE_DESIGNATOR(builder, VEHICLE_TYPE_DESIGNATOR)
        VEPAddENVIRONMENT(builder, self.ENVIRONMENT)
        VEPAddENERGY_TYPE(builder, self.ENERGY_TYPE)
        VEPAddCAPACITY_VALUE(builder, self.CAPACITY_VALUE)
        if self.CAPACITY_UNITS is not None:
            VEPAddCAPACITY_UNITS(builder, CAPACITY_UNITS)
        VEPAddRESERVE_FRACTION(builder, self.RESERVE_FRACTION)
        if self.PERFORMANCE_POINTS is not None:
            VEPAddPERFORMANCE_POINTS(builder, PERFORMANCE_POINTS)
        VEPAddINTERPOLATION(builder, self.INTERPOLATION)
        VEPAddMAXIMUM_ENDURANCE_S(builder, self.MAXIMUM_ENDURANCE_S)
        VEPAddMAXIMUM_RANGE_M(builder, self.MAXIMUM_RANGE_M)
        VEPAddLOITER_SPEED_MPS(builder, self.LOITER_SPEED_MPS)
        VEPAddSERVICE_CEILING_M(builder, self.SERVICE_CEILING_M)
        if self.REPLENISHMENT is not None:
            VEPAddREPLENISHMENT(builder, REPLENISHMENT)
        if self.REFINES_ID is not None:
            VEPAddREFINES_ID(builder, REFINES_ID)
        if self.SOURCE is not None:
            VEPAddSOURCE(builder, SOURCE)
        VEP = VEPEnd(builder)
        return VEP
