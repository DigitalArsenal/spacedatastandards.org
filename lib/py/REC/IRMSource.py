# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# The source object a job is reading, and the validators that prove a resumed
# read is reading the same bytes.
class IRMSource(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IRMSource()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIRMSource(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IRMSourceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x52\x4D", size_prefixed=size_prefixed)

    # IRMSource
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Replayable locator of the source object, verbatim.
    # IRMSource
    def SOURCE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Source-native object, export, or product identifier verbatim, when the
    # provider names one independently of its locator.
    # IRMSource
    def SOURCE_OBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Content identifier pinning the exact source bytes, when the source is
    # content-addressed.
    # IRMSource
    def SOURCE_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Media type of the source object verbatim as the provider states it.
    # IRMSource
    def MEDIA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Total length of the source object in bytes. 0 means the provider did not
    # state a length; it never means an empty object.
    # IRMSource
    def TOTAL_BYTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Opaque entity validator verbatim as the provider published it.
    # IRMSource
    def ENTITY_TAG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider-stated last-modification timestamp verbatim.
    # IRMSource
    def LAST_MODIFIED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SHA-256 of the complete source object as 64 lowercase hexadecimal
    # characters, when the provider publishes one. Never computed from a partial
    # read.
    # IRMSource
    def SOURCE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider-stated edition, revision, or release marker verbatim.
    # IRMSource
    def SOURCE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # What the publisher concluded when it last compared validators.
    # IRMSource
    def VALIDATOR_MATCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # RFC 3339 UTC fixed-millisecond timestamp of that comparison.
    # IRMSource
    def VALIDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def IRMSourceStart(builder):
    builder.StartObject(11)

def Start(builder):
    IRMSourceStart(builder)

def IRMSourceAddSOURCE_URL(builder, SOURCE_URL):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_URL), 0)

def AddSOURCE_URL(builder, SOURCE_URL):
    IRMSourceAddSOURCE_URL(builder, SOURCE_URL)

def IRMSourceAddSOURCE_OBJECT_ID(builder, SOURCE_OBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_OBJECT_ID), 0)

def AddSOURCE_OBJECT_ID(builder, SOURCE_OBJECT_ID):
    IRMSourceAddSOURCE_OBJECT_ID(builder, SOURCE_OBJECT_ID)

def IRMSourceAddSOURCE_CID(builder, SOURCE_CID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_CID), 0)

def AddSOURCE_CID(builder, SOURCE_CID):
    IRMSourceAddSOURCE_CID(builder, SOURCE_CID)

def IRMSourceAddMEDIA_TYPE(builder, MEDIA_TYPE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MEDIA_TYPE), 0)

def AddMEDIA_TYPE(builder, MEDIA_TYPE):
    IRMSourceAddMEDIA_TYPE(builder, MEDIA_TYPE)

def IRMSourceAddTOTAL_BYTES(builder, TOTAL_BYTES):
    builder.PrependUint64Slot(4, TOTAL_BYTES, 0)

def AddTOTAL_BYTES(builder, TOTAL_BYTES):
    IRMSourceAddTOTAL_BYTES(builder, TOTAL_BYTES)

def IRMSourceAddENTITY_TAG(builder, ENTITY_TAG):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ENTITY_TAG), 0)

def AddENTITY_TAG(builder, ENTITY_TAG):
    IRMSourceAddENTITY_TAG(builder, ENTITY_TAG)

def IRMSourceAddLAST_MODIFIED(builder, LAST_MODIFIED):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(LAST_MODIFIED), 0)

def AddLAST_MODIFIED(builder, LAST_MODIFIED):
    IRMSourceAddLAST_MODIFIED(builder, LAST_MODIFIED)

def IRMSourceAddSOURCE_SHA256(builder, SOURCE_SHA256):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_SHA256), 0)

def AddSOURCE_SHA256(builder, SOURCE_SHA256):
    IRMSourceAddSOURCE_SHA256(builder, SOURCE_SHA256)

def IRMSourceAddSOURCE_VERSION(builder, SOURCE_VERSION):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_VERSION), 0)

def AddSOURCE_VERSION(builder, SOURCE_VERSION):
    IRMSourceAddSOURCE_VERSION(builder, SOURCE_VERSION)

def IRMSourceAddVALIDATOR_MATCH(builder, VALIDATOR_MATCH):
    builder.PrependInt8Slot(9, VALIDATOR_MATCH, 0)

def AddVALIDATOR_MATCH(builder, VALIDATOR_MATCH):
    IRMSourceAddVALIDATOR_MATCH(builder, VALIDATOR_MATCH)

def IRMSourceAddVALIDATED_AT(builder, VALIDATED_AT):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(VALIDATED_AT), 0)

def AddVALIDATED_AT(builder, VALIDATED_AT):
    IRMSourceAddVALIDATED_AT(builder, VALIDATED_AT)

def IRMSourceEnd(builder):
    return builder.EndObject()

def End(builder):
    return IRMSourceEnd(builder)


class IRMSourceT(object):

    # IRMSourceT
    def __init__(
        self,
        SOURCE_URL = None,
        SOURCE_OBJECT_ID = None,
        SOURCE_CID = None,
        MEDIA_TYPE = None,
        TOTAL_BYTES = 0,
        ENTITY_TAG = None,
        LAST_MODIFIED = None,
        SOURCE_SHA256 = None,
        SOURCE_VERSION = None,
        VALIDATOR_MATCH = 0,
        VALIDATED_AT = None,
    ):
        self.SOURCE_URL = SOURCE_URL  # type: Optional[str]
        self.SOURCE_OBJECT_ID = SOURCE_OBJECT_ID  # type: Optional[str]
        self.SOURCE_CID = SOURCE_CID  # type: Optional[str]
        self.MEDIA_TYPE = MEDIA_TYPE  # type: Optional[str]
        self.TOTAL_BYTES = TOTAL_BYTES  # type: int
        self.ENTITY_TAG = ENTITY_TAG  # type: Optional[str]
        self.LAST_MODIFIED = LAST_MODIFIED  # type: Optional[str]
        self.SOURCE_SHA256 = SOURCE_SHA256  # type: Optional[str]
        self.SOURCE_VERSION = SOURCE_VERSION  # type: Optional[str]
        self.VALIDATOR_MATCH = VALIDATOR_MATCH  # type: int
        self.VALIDATED_AT = VALIDATED_AT  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIrmsource = IRMSource()
        tmpIrmsource.Init(buf, pos)
        return cls.InitFromObj(tmpIrmsource)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIrmsource):
        x = IRMSourceT()
        x._UnPack(tmpIrmsource)
        return x

    # IRMSourceT
    def _UnPack(self, IRMSource):
        if IRMSource is None:
            return
        self.SOURCE_URL = IRMSource.SOURCE_URL()
        self.SOURCE_OBJECT_ID = IRMSource.SOURCE_OBJECT_ID()
        self.SOURCE_CID = IRMSource.SOURCE_CID()
        self.MEDIA_TYPE = IRMSource.MEDIA_TYPE()
        self.TOTAL_BYTES = IRMSource.TOTAL_BYTES()
        self.ENTITY_TAG = IRMSource.ENTITY_TAG()
        self.LAST_MODIFIED = IRMSource.LAST_MODIFIED()
        self.SOURCE_SHA256 = IRMSource.SOURCE_SHA256()
        self.SOURCE_VERSION = IRMSource.SOURCE_VERSION()
        self.VALIDATOR_MATCH = IRMSource.VALIDATOR_MATCH()
        self.VALIDATED_AT = IRMSource.VALIDATED_AT()

    # IRMSourceT
    def Pack(self, builder):
        if self.SOURCE_URL is not None:
            SOURCE_URL = builder.CreateString(self.SOURCE_URL)
        if self.SOURCE_OBJECT_ID is not None:
            SOURCE_OBJECT_ID = builder.CreateString(self.SOURCE_OBJECT_ID)
        if self.SOURCE_CID is not None:
            SOURCE_CID = builder.CreateString(self.SOURCE_CID)
        if self.MEDIA_TYPE is not None:
            MEDIA_TYPE = builder.CreateString(self.MEDIA_TYPE)
        if self.ENTITY_TAG is not None:
            ENTITY_TAG = builder.CreateString(self.ENTITY_TAG)
        if self.LAST_MODIFIED is not None:
            LAST_MODIFIED = builder.CreateString(self.LAST_MODIFIED)
        if self.SOURCE_SHA256 is not None:
            SOURCE_SHA256 = builder.CreateString(self.SOURCE_SHA256)
        if self.SOURCE_VERSION is not None:
            SOURCE_VERSION = builder.CreateString(self.SOURCE_VERSION)
        if self.VALIDATED_AT is not None:
            VALIDATED_AT = builder.CreateString(self.VALIDATED_AT)
        IRMSourceStart(builder)
        if self.SOURCE_URL is not None:
            IRMSourceAddSOURCE_URL(builder, SOURCE_URL)
        if self.SOURCE_OBJECT_ID is not None:
            IRMSourceAddSOURCE_OBJECT_ID(builder, SOURCE_OBJECT_ID)
        if self.SOURCE_CID is not None:
            IRMSourceAddSOURCE_CID(builder, SOURCE_CID)
        if self.MEDIA_TYPE is not None:
            IRMSourceAddMEDIA_TYPE(builder, MEDIA_TYPE)
        IRMSourceAddTOTAL_BYTES(builder, self.TOTAL_BYTES)
        if self.ENTITY_TAG is not None:
            IRMSourceAddENTITY_TAG(builder, ENTITY_TAG)
        if self.LAST_MODIFIED is not None:
            IRMSourceAddLAST_MODIFIED(builder, LAST_MODIFIED)
        if self.SOURCE_SHA256 is not None:
            IRMSourceAddSOURCE_SHA256(builder, SOURCE_SHA256)
        if self.SOURCE_VERSION is not None:
            IRMSourceAddSOURCE_VERSION(builder, SOURCE_VERSION)
        IRMSourceAddVALIDATOR_MATCH(builder, self.VALIDATOR_MATCH)
        if self.VALIDATED_AT is not None:
            IRMSourceAddVALIDATED_AT(builder, VALIDATED_AT)
        IRMSource = IRMSourceEnd(builder)
        return IRMSource
