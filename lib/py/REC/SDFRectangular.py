# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Rectangular (pyramidal) SDF parameters.
class SDFRectangular(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDFRectangular()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDFRectangular(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDFRectangularBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x46", size_prefixed=size_prefixed)

    # SDFRectangular
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDFRectangular
    def X_HALF_ANGLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SDFRectangular
    def Y_HALF_ANGLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SDFRectangular
    def MIN_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SDFRectangular
    def MAX_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SDFRectangularStart(builder):
    builder.StartObject(4)

def Start(builder):
    SDFRectangularStart(builder)

def SDFRectangularAddX_HALF_ANGLE(builder, X_HALF_ANGLE):
    builder.PrependFloat64Slot(0, X_HALF_ANGLE, 0.0)

def AddX_HALF_ANGLE(builder, X_HALF_ANGLE):
    SDFRectangularAddX_HALF_ANGLE(builder, X_HALF_ANGLE)

def SDFRectangularAddY_HALF_ANGLE(builder, Y_HALF_ANGLE):
    builder.PrependFloat64Slot(1, Y_HALF_ANGLE, 0.0)

def AddY_HALF_ANGLE(builder, Y_HALF_ANGLE):
    SDFRectangularAddY_HALF_ANGLE(builder, Y_HALF_ANGLE)

def SDFRectangularAddMIN_RANGE(builder, MIN_RANGE):
    builder.PrependFloat64Slot(2, MIN_RANGE, 0.0)

def AddMIN_RANGE(builder, MIN_RANGE):
    SDFRectangularAddMIN_RANGE(builder, MIN_RANGE)

def SDFRectangularAddMAX_RANGE(builder, MAX_RANGE):
    builder.PrependFloat64Slot(3, MAX_RANGE, 0.0)

def AddMAX_RANGE(builder, MAX_RANGE):
    SDFRectangularAddMAX_RANGE(builder, MAX_RANGE)

def SDFRectangularEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDFRectangularEnd(builder)


class SDFRectangularT(object):

    # SDFRectangularT
    def __init__(
        self,
        X_HALF_ANGLE = 0.0,
        Y_HALF_ANGLE = 0.0,
        MIN_RANGE = 0.0,
        MAX_RANGE = 0.0,
    ):
        self.X_HALF_ANGLE = X_HALF_ANGLE  # type: float
        self.Y_HALF_ANGLE = Y_HALF_ANGLE  # type: float
        self.MIN_RANGE = MIN_RANGE  # type: float
        self.MAX_RANGE = MAX_RANGE  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdfrectangular = SDFRectangular()
        tmpSdfrectangular.Init(buf, pos)
        return cls.InitFromObj(tmpSdfrectangular)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdfrectangular):
        x = SDFRectangularT()
        x._UnPack(tmpSdfrectangular)
        return x

    # SDFRectangularT
    def _UnPack(self, SDFRectangular):
        if SDFRectangular is None:
            return
        self.X_HALF_ANGLE = SDFRectangular.X_HALF_ANGLE()
        self.Y_HALF_ANGLE = SDFRectangular.Y_HALF_ANGLE()
        self.MIN_RANGE = SDFRectangular.MIN_RANGE()
        self.MAX_RANGE = SDFRectangular.MAX_RANGE()

    # SDFRectangularT
    def Pack(self, builder):
        SDFRectangularStart(builder)
        SDFRectangularAddX_HALF_ANGLE(builder, self.X_HALF_ANGLE)
        SDFRectangularAddY_HALF_ANGLE(builder, self.Y_HALF_ANGLE)
        SDFRectangularAddMIN_RANGE(builder, self.MIN_RANGE)
        SDFRectangularAddMAX_RANGE(builder, self.MAX_RANGE)
        SDFRectangular = SDFRectangularEnd(builder)
        return SDFRectangular
