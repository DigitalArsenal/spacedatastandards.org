# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One SDS record type the app produces and/or consumes. Names an existing
# spacedatastandards.org schema by its established code; the app manifest
# defines no data schemas of its own.
class APPDataRef(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = APPDataRef()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAPPDataRef(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def APPDataRefBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x50\x50", size_prefixed=size_prefixed)

    # APPDataRef
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # App-local stable reference for this data binding. Required, unique
    # within the manifest.
    # APPDataRef
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Existing SDS schema code, for example OMM, CDM, or EPM. Required.
    # APPDataRef
    def SDS_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Data flow relative to the app.
    # APPDataRef
    def DIRECTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # When present, must equal an APPModuleRef.ID in the same manifest — the
    # member module responsible for this data binding.
    # APPDataRef
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable summary.
    # APPDataRef
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def APPDataRefStart(builder):
    builder.StartObject(5)

def Start(builder):
    APPDataRefStart(builder)

def APPDataRefAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    APPDataRefAddID(builder, ID)

def APPDataRefAddSDS_TYPE(builder, SDS_TYPE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SDS_TYPE), 0)

def AddSDS_TYPE(builder, SDS_TYPE):
    APPDataRefAddSDS_TYPE(builder, SDS_TYPE)

def APPDataRefAddDIRECTION(builder, DIRECTION):
    builder.PrependUint8Slot(2, DIRECTION, 0)

def AddDIRECTION(builder, DIRECTION):
    APPDataRefAddDIRECTION(builder, DIRECTION)

def APPDataRefAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    APPDataRefAddMODULE_ID(builder, MODULE_ID)

def APPDataRefAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    APPDataRefAddDESCRIPTION(builder, DESCRIPTION)

def APPDataRefEnd(builder):
    return builder.EndObject()

def End(builder):
    return APPDataRefEnd(builder)


class APPDataRefT(object):

    # APPDataRefT
    def __init__(
        self,
        ID = None,
        SDS_TYPE = None,
        DIRECTION = 0,
        MODULE_ID = None,
        DESCRIPTION = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.SDS_TYPE = SDS_TYPE  # type: Optional[str]
        self.DIRECTION = DIRECTION  # type: int
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAppdataRef = APPDataRef()
        tmpAppdataRef.Init(buf, pos)
        return cls.InitFromObj(tmpAppdataRef)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAppdataRef):
        x = APPDataRefT()
        x._UnPack(tmpAppdataRef)
        return x

    # APPDataRefT
    def _UnPack(self, APPDataRef):
        if APPDataRef is None:
            return
        self.ID = APPDataRef.ID()
        self.SDS_TYPE = APPDataRef.SDS_TYPE()
        self.DIRECTION = APPDataRef.DIRECTION()
        self.MODULE_ID = APPDataRef.MODULE_ID()
        self.DESCRIPTION = APPDataRef.DESCRIPTION()

    # APPDataRefT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.SDS_TYPE is not None:
            SDS_TYPE = builder.CreateString(self.SDS_TYPE)
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        APPDataRefStart(builder)
        if self.ID is not None:
            APPDataRefAddID(builder, ID)
        if self.SDS_TYPE is not None:
            APPDataRefAddSDS_TYPE(builder, SDS_TYPE)
        APPDataRefAddDIRECTION(builder, self.DIRECTION)
        if self.MODULE_ID is not None:
            APPDataRefAddMODULE_ID(builder, MODULE_ID)
        if self.DESCRIPTION is not None:
            APPDataRefAddDESCRIPTION(builder, DESCRIPTION)
        APPDataRef = APPDataRefEnd(builder)
        return APPDataRef
