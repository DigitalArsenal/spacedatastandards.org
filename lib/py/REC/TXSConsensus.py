# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# How one facility's independent source reports were reduced to the single
# published position, and how strongly those reports agreed.
#
# Agreement is CORROBORATION and is never authority: five compilations that
# copied one another agree perfectly and establish nothing. Read this table
# beside POSITION_AUTHORITY, never instead of it.
class TXSConsensus(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TXSConsensus()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTXSConsensus(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TXSConsensusBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x58\x53", size_prefixed=size_prefixed)

    # TXSConsensus
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TXSConsensus
    def METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Sources queried for this facility, including those that returned nothing.
    # Never fewer than the number of SOURCES entries.
    # TXSConsensus
    def PROVIDERS_CONSULTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Sources whose reported position fell within the publisher's agreement
    # threshold. A value of 1 means the published position rests on a single
    # report and was never independently corroborated.
    # TXSConsensus
    def PROVIDERS_AGREEING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Sources whose reported position fell OUTSIDE that threshold and were not
    # reconciled. Non-zero is the machine-readable form of a contested
    # position and is published alongside POSITION_DISPUTED.
    # TXSConsensus
    def PROVIDERS_DISAGREEING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # PROVIDER_ID of the report the merge selected, copied verbatim from the
    # matching SOURCES entry. Absent when METHOD is CENTROID: a computed
    # position belongs to no single source.
    # TXSConsensus
    def WINNING_PROVIDER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Greatest surface distance in metres between any two agreeing reported
    # positions. Zero is meaningful only when PROVIDERS_AGREEING is 1.
    # TXSConsensus
    def POSITION_SPREAD_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # The publisher's own confidence in the merged position, on [0,1]. Never a
    # source's figure and never a probability from a model this standard does
    # not name.
    # TXSConsensus
    def CONFIDENCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFC 3339 UTC fixed-millisecond instant at which the merge was performed.
    # TXSConsensus
    def MERGED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TXSConsensusStart(builder):
    builder.StartObject(8)

def Start(builder):
    TXSConsensusStart(builder)

def TXSConsensusAddMETHOD(builder, METHOD):
    builder.PrependInt8Slot(0, METHOD, 0)

def AddMETHOD(builder, METHOD):
    TXSConsensusAddMETHOD(builder, METHOD)

def TXSConsensusAddPROVIDERS_CONSULTED(builder, PROVIDERS_CONSULTED):
    builder.PrependUint32Slot(1, PROVIDERS_CONSULTED, 0)

def AddPROVIDERS_CONSULTED(builder, PROVIDERS_CONSULTED):
    TXSConsensusAddPROVIDERS_CONSULTED(builder, PROVIDERS_CONSULTED)

def TXSConsensusAddPROVIDERS_AGREEING(builder, PROVIDERS_AGREEING):
    builder.PrependUint32Slot(2, PROVIDERS_AGREEING, 0)

def AddPROVIDERS_AGREEING(builder, PROVIDERS_AGREEING):
    TXSConsensusAddPROVIDERS_AGREEING(builder, PROVIDERS_AGREEING)

def TXSConsensusAddPROVIDERS_DISAGREEING(builder, PROVIDERS_DISAGREEING):
    builder.PrependUint32Slot(3, PROVIDERS_DISAGREEING, 0)

def AddPROVIDERS_DISAGREEING(builder, PROVIDERS_DISAGREEING):
    TXSConsensusAddPROVIDERS_DISAGREEING(builder, PROVIDERS_DISAGREEING)

def TXSConsensusAddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(WINNING_PROVIDER_ID), 0)

def AddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID):
    TXSConsensusAddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID)

def TXSConsensusAddPOSITION_SPREAD_M(builder, POSITION_SPREAD_M):
    builder.PrependFloat64Slot(5, POSITION_SPREAD_M, 0.0)

def AddPOSITION_SPREAD_M(builder, POSITION_SPREAD_M):
    TXSConsensusAddPOSITION_SPREAD_M(builder, POSITION_SPREAD_M)

def TXSConsensusAddCONFIDENCE(builder, CONFIDENCE):
    builder.PrependFloat64Slot(6, CONFIDENCE, 0.0)

def AddCONFIDENCE(builder, CONFIDENCE):
    TXSConsensusAddCONFIDENCE(builder, CONFIDENCE)

def TXSConsensusAddMERGED_AT(builder, MERGED_AT):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(MERGED_AT), 0)

def AddMERGED_AT(builder, MERGED_AT):
    TXSConsensusAddMERGED_AT(builder, MERGED_AT)

def TXSConsensusEnd(builder):
    return builder.EndObject()

def End(builder):
    return TXSConsensusEnd(builder)


class TXSConsensusT(object):

    # TXSConsensusT
    def __init__(
        self,
        METHOD = 0,
        PROVIDERS_CONSULTED = 0,
        PROVIDERS_AGREEING = 0,
        PROVIDERS_DISAGREEING = 0,
        WINNING_PROVIDER_ID = None,
        POSITION_SPREAD_M = 0.0,
        CONFIDENCE = 0.0,
        MERGED_AT = None,
    ):
        self.METHOD = METHOD  # type: int
        self.PROVIDERS_CONSULTED = PROVIDERS_CONSULTED  # type: int
        self.PROVIDERS_AGREEING = PROVIDERS_AGREEING  # type: int
        self.PROVIDERS_DISAGREEING = PROVIDERS_DISAGREEING  # type: int
        self.WINNING_PROVIDER_ID = WINNING_PROVIDER_ID  # type: Optional[str]
        self.POSITION_SPREAD_M = POSITION_SPREAD_M  # type: float
        self.CONFIDENCE = CONFIDENCE  # type: float
        self.MERGED_AT = MERGED_AT  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTxsconsensus = TXSConsensus()
        tmpTxsconsensus.Init(buf, pos)
        return cls.InitFromObj(tmpTxsconsensus)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTxsconsensus):
        x = TXSConsensusT()
        x._UnPack(tmpTxsconsensus)
        return x

    # TXSConsensusT
    def _UnPack(self, TXSConsensus):
        if TXSConsensus is None:
            return
        self.METHOD = TXSConsensus.METHOD()
        self.PROVIDERS_CONSULTED = TXSConsensus.PROVIDERS_CONSULTED()
        self.PROVIDERS_AGREEING = TXSConsensus.PROVIDERS_AGREEING()
        self.PROVIDERS_DISAGREEING = TXSConsensus.PROVIDERS_DISAGREEING()
        self.WINNING_PROVIDER_ID = TXSConsensus.WINNING_PROVIDER_ID()
        self.POSITION_SPREAD_M = TXSConsensus.POSITION_SPREAD_M()
        self.CONFIDENCE = TXSConsensus.CONFIDENCE()
        self.MERGED_AT = TXSConsensus.MERGED_AT()

    # TXSConsensusT
    def Pack(self, builder):
        if self.WINNING_PROVIDER_ID is not None:
            WINNING_PROVIDER_ID = builder.CreateString(self.WINNING_PROVIDER_ID)
        if self.MERGED_AT is not None:
            MERGED_AT = builder.CreateString(self.MERGED_AT)
        TXSConsensusStart(builder)
        TXSConsensusAddMETHOD(builder, self.METHOD)
        TXSConsensusAddPROVIDERS_CONSULTED(builder, self.PROVIDERS_CONSULTED)
        TXSConsensusAddPROVIDERS_AGREEING(builder, self.PROVIDERS_AGREEING)
        TXSConsensusAddPROVIDERS_DISAGREEING(builder, self.PROVIDERS_DISAGREEING)
        if self.WINNING_PROVIDER_ID is not None:
            TXSConsensusAddWINNING_PROVIDER_ID(builder, WINNING_PROVIDER_ID)
        TXSConsensusAddPOSITION_SPREAD_M(builder, self.POSITION_SPREAD_M)
        TXSConsensusAddCONFIDENCE(builder, self.CONFIDENCE)
        if self.MERGED_AT is not None:
            TXSConsensusAddMERGED_AT(builder, MERGED_AT)
        TXSConsensus = TXSConsensusEnd(builder)
        return TXSConsensus
