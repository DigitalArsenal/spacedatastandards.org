# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One scalar physical quantity, its unit and its own provenance.
#
# PROVENANCE is required: an OPPQuantity that exists names where it came from.
# Presence of the table is the signal that a value exists, so VALUE zero is a
# real zero (0 dBsm is 1 m2) and an unknown quantity omits the whole table.
class OPPQuantity(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OPPQuantity()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOPPQuantity(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OPPQuantityBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x50\x50", size_prefixed=size_prefixed)

    # OPPQuantity
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # OPPQuantity
    def VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Unit symbol verbatim, never silently converted: "kg", "m", "m2", "m2/kg",
    # "kg/m2", "dBsm", "W", "mag". A converted value states the unit it was
    # converted to and records the conversion in PROVENANCE.NOTES with
    # PROVENANCE.METHOD DERIVED.
    # OPPQuantity
    def UNITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # 1-sigma uncertainty in UNITS. Zero means the source stated no uncertainty;
    # a genuine stated sigma is never exactly zero.
    # OPPQuantity
    def SIGMA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # OPPQuantity
    def PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPProvenance import OPPProvenance
            obj = OPPProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def OPPQuantityStart(builder):
    builder.StartObject(4)

def Start(builder):
    OPPQuantityStart(builder)

def OPPQuantityAddVALUE(builder, VALUE):
    builder.PrependFloat64Slot(0, VALUE, 0.0)

def AddVALUE(builder, VALUE):
    OPPQuantityAddVALUE(builder, VALUE)

def OPPQuantityAddUNITS(builder, UNITS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(UNITS), 0)

def AddUNITS(builder, UNITS):
    OPPQuantityAddUNITS(builder, UNITS)

def OPPQuantityAddSIGMA(builder, SIGMA):
    builder.PrependFloat64Slot(2, SIGMA, 0.0)

def AddSIGMA(builder, SIGMA):
    OPPQuantityAddSIGMA(builder, SIGMA)

def OPPQuantityAddPROVENANCE(builder, PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(PROVENANCE), 0)

def AddPROVENANCE(builder, PROVENANCE):
    OPPQuantityAddPROVENANCE(builder, PROVENANCE)

def OPPQuantityEnd(builder):
    return builder.EndObject()

def End(builder):
    return OPPQuantityEnd(builder)

import OPPProvenance
try:
    from typing import Optional
except:
    pass

class OPPQuantityT(object):

    # OPPQuantityT
    def __init__(
        self,
        VALUE = 0.0,
        UNITS = None,
        SIGMA = 0.0,
        PROVENANCE = None,
    ):
        self.VALUE = VALUE  # type: float
        self.UNITS = UNITS  # type: Optional[str]
        self.SIGMA = SIGMA  # type: float
        self.PROVENANCE = PROVENANCE  # type: Optional[OPPProvenance.OPPProvenanceT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOppquantity = OPPQuantity()
        tmpOppquantity.Init(buf, pos)
        return cls.InitFromObj(tmpOppquantity)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOppquantity):
        x = OPPQuantityT()
        x._UnPack(tmpOppquantity)
        return x

    # OPPQuantityT
    def _UnPack(self, OPPQuantity):
        if OPPQuantity is None:
            return
        self.VALUE = OPPQuantity.VALUE()
        self.UNITS = OPPQuantity.UNITS()
        self.SIGMA = OPPQuantity.SIGMA()
        if OPPQuantity.PROVENANCE() is not None:
            self.PROVENANCE = OPPProvenance.OPPProvenanceT.InitFromObj(OPPQuantity.PROVENANCE())

    # OPPQuantityT
    def Pack(self, builder):
        if self.UNITS is not None:
            UNITS = builder.CreateString(self.UNITS)
        if self.PROVENANCE is not None:
            PROVENANCE = self.PROVENANCE.Pack(builder)
        OPPQuantityStart(builder)
        OPPQuantityAddVALUE(builder, self.VALUE)
        if self.UNITS is not None:
            OPPQuantityAddUNITS(builder, UNITS)
        OPPQuantityAddSIGMA(builder, self.SIGMA)
        if self.PROVENANCE is not None:
            OPPQuantityAddPROVENANCE(builder, PROVENANCE)
        OPPQuantity = OPPQuantityEnd(builder)
        return OPPQuantity
