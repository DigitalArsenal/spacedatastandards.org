# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Crypto-native Subscription Authorization
class SUB(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SUB()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSUB(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SUBBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x55\x42", size_prefixed=size_prefixed)

    # SUB
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable subscription identifier
    # SUB
    def SUBSCRIPTION_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference to the durable entitlement (`$ENT.ENTITLEMENT_ID`)
    # SUB
    def ENTITLEMENT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Listing this subscription pays for (`$PLG.PLUGIN_ID` / `$STF.LISTING_ID`)
    # SUB
    def LISTING_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # On-chain recurring mechanism
    # SUB
    def AUTH_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Settlement descriptor (chain, splitter, token) that charges each period
    # SUB
    def CHAIN_SETTLEMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCX import SCX
            obj = SCX()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Billing period length in days (mirrors `$PLG.SUBSCRIPTION_PERIOD_DAYS`)
    # SUB
    def PERIOD_DAYS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Charge per period in the smallest unit of CURRENCY
    # SUB
    def AMOUNT_PER_PERIOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Currency / token symbol, e.g. "USDC"
    # SUB
    def CURRENCY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Unix ms of the next scheduled charge
    # SUB
    def NEXT_CHARGE_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Maximum number of periods; 0 = until cancelled
    # SUB
    def MAX_PERIODS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Subscription lifecycle status
    # SUB
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Buyer's Ed25519 authorization signature over the terms
    # SUB
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SUB
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SUB
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SUB
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

def SUBStart(builder):
    builder.StartObject(12)

def Start(builder):
    SUBStart(builder)

def SUBAddSUBSCRIPTION_ID(builder, SUBSCRIPTION_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SUBSCRIPTION_ID), 0)

def AddSUBSCRIPTION_ID(builder, SUBSCRIPTION_ID):
    SUBAddSUBSCRIPTION_ID(builder, SUBSCRIPTION_ID)

def SUBAddENTITLEMENT_ID(builder, ENTITLEMENT_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ENTITLEMENT_ID), 0)

def AddENTITLEMENT_ID(builder, ENTITLEMENT_ID):
    SUBAddENTITLEMENT_ID(builder, ENTITLEMENT_ID)

def SUBAddLISTING_ID(builder, LISTING_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(LISTING_ID), 0)

def AddLISTING_ID(builder, LISTING_ID):
    SUBAddLISTING_ID(builder, LISTING_ID)

def SUBAddAUTH_KIND(builder, AUTH_KIND):
    builder.PrependInt8Slot(3, AUTH_KIND, 0)

def AddAUTH_KIND(builder, AUTH_KIND):
    SUBAddAUTH_KIND(builder, AUTH_KIND)

def SUBAddCHAIN_SETTLEMENT(builder, CHAIN_SETTLEMENT):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(CHAIN_SETTLEMENT), 0)

def AddCHAIN_SETTLEMENT(builder, CHAIN_SETTLEMENT):
    SUBAddCHAIN_SETTLEMENT(builder, CHAIN_SETTLEMENT)

def SUBAddPERIOD_DAYS(builder, PERIOD_DAYS):
    builder.PrependUint32Slot(5, PERIOD_DAYS, 0)

def AddPERIOD_DAYS(builder, PERIOD_DAYS):
    SUBAddPERIOD_DAYS(builder, PERIOD_DAYS)

def SUBAddAMOUNT_PER_PERIOD(builder, AMOUNT_PER_PERIOD):
    builder.PrependUint64Slot(6, AMOUNT_PER_PERIOD, 0)

def AddAMOUNT_PER_PERIOD(builder, AMOUNT_PER_PERIOD):
    SUBAddAMOUNT_PER_PERIOD(builder, AMOUNT_PER_PERIOD)

def SUBAddCURRENCY(builder, CURRENCY):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(CURRENCY), 0)

def AddCURRENCY(builder, CURRENCY):
    SUBAddCURRENCY(builder, CURRENCY)

def SUBAddNEXT_CHARGE_AT(builder, NEXT_CHARGE_AT):
    builder.PrependUint64Slot(8, NEXT_CHARGE_AT, 0)

def AddNEXT_CHARGE_AT(builder, NEXT_CHARGE_AT):
    SUBAddNEXT_CHARGE_AT(builder, NEXT_CHARGE_AT)

def SUBAddMAX_PERIODS(builder, MAX_PERIODS):
    builder.PrependUint32Slot(9, MAX_PERIODS, 0)

def AddMAX_PERIODS(builder, MAX_PERIODS):
    SUBAddMAX_PERIODS(builder, MAX_PERIODS)

def SUBAddSTATUS(builder, STATUS):
    builder.PrependInt8Slot(10, STATUS, 0)

def AddSTATUS(builder, STATUS):
    SUBAddSTATUS(builder, STATUS)

def SUBAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    SUBAddSIGNATURE(builder, SIGNATURE)

def SUBStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return SUBStartSIGNATUREVector(builder, numElems)

def SUBCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    SUBCreateSIGNATUREVector(builder, data)

def SUBEnd(builder):
    return builder.EndObject()

def End(builder):
    return SUBEnd(builder)

import SCX
try:
    from typing import List, Optional
except:
    pass

class SUBT(object):

    # SUBT
    def __init__(
        self,
        SUBSCRIPTION_ID = None,
        ENTITLEMENT_ID = None,
        LISTING_ID = None,
        AUTH_KIND = 0,
        CHAIN_SETTLEMENT = None,
        PERIOD_DAYS = 0,
        AMOUNT_PER_PERIOD = 0,
        CURRENCY = None,
        NEXT_CHARGE_AT = 0,
        MAX_PERIODS = 0,
        STATUS = 0,
        SIGNATURE = None,
    ):
        self.SUBSCRIPTION_ID = SUBSCRIPTION_ID  # type: Optional[str]
        self.ENTITLEMENT_ID = ENTITLEMENT_ID  # type: Optional[str]
        self.LISTING_ID = LISTING_ID  # type: Optional[str]
        self.AUTH_KIND = AUTH_KIND  # type: int
        self.CHAIN_SETTLEMENT = CHAIN_SETTLEMENT  # type: Optional[SCX.SCXT]
        self.PERIOD_DAYS = PERIOD_DAYS  # type: int
        self.AMOUNT_PER_PERIOD = AMOUNT_PER_PERIOD  # type: int
        self.CURRENCY = CURRENCY  # type: Optional[str]
        self.NEXT_CHARGE_AT = NEXT_CHARGE_AT  # type: int
        self.MAX_PERIODS = MAX_PERIODS  # type: int
        self.STATUS = STATUS  # type: int
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSub = SUB()
        tmpSub.Init(buf, pos)
        return cls.InitFromObj(tmpSub)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSub):
        x = SUBT()
        x._UnPack(tmpSub)
        return x

    # SUBT
    def _UnPack(self, SUB):
        if SUB is None:
            return
        self.SUBSCRIPTION_ID = SUB.SUBSCRIPTION_ID()
        self.ENTITLEMENT_ID = SUB.ENTITLEMENT_ID()
        self.LISTING_ID = SUB.LISTING_ID()
        self.AUTH_KIND = SUB.AUTH_KIND()
        if SUB.CHAIN_SETTLEMENT() is not None:
            self.CHAIN_SETTLEMENT = SCX.SCXT.InitFromObj(SUB.CHAIN_SETTLEMENT())
        self.PERIOD_DAYS = SUB.PERIOD_DAYS()
        self.AMOUNT_PER_PERIOD = SUB.AMOUNT_PER_PERIOD()
        self.CURRENCY = SUB.CURRENCY()
        self.NEXT_CHARGE_AT = SUB.NEXT_CHARGE_AT()
        self.MAX_PERIODS = SUB.MAX_PERIODS()
        self.STATUS = SUB.STATUS()
        if not SUB.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(SUB.SIGNATURELength()):
                    self.SIGNATURE.append(SUB.SIGNATURE(i))
            else:
                self.SIGNATURE = SUB.SIGNATUREAsNumpy()

    # SUBT
    def Pack(self, builder):
        if self.SUBSCRIPTION_ID is not None:
            SUBSCRIPTION_ID = builder.CreateString(self.SUBSCRIPTION_ID)
        if self.ENTITLEMENT_ID is not None:
            ENTITLEMENT_ID = builder.CreateString(self.ENTITLEMENT_ID)
        if self.LISTING_ID is not None:
            LISTING_ID = builder.CreateString(self.LISTING_ID)
        if self.CHAIN_SETTLEMENT is not None:
            CHAIN_SETTLEMENT = self.CHAIN_SETTLEMENT.Pack(builder)
        if self.CURRENCY is not None:
            CURRENCY = builder.CreateString(self.CURRENCY)
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                SUBStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        SUBStart(builder)
        if self.SUBSCRIPTION_ID is not None:
            SUBAddSUBSCRIPTION_ID(builder, SUBSCRIPTION_ID)
        if self.ENTITLEMENT_ID is not None:
            SUBAddENTITLEMENT_ID(builder, ENTITLEMENT_ID)
        if self.LISTING_ID is not None:
            SUBAddLISTING_ID(builder, LISTING_ID)
        SUBAddAUTH_KIND(builder, self.AUTH_KIND)
        if self.CHAIN_SETTLEMENT is not None:
            SUBAddCHAIN_SETTLEMENT(builder, CHAIN_SETTLEMENT)
        SUBAddPERIOD_DAYS(builder, self.PERIOD_DAYS)
        SUBAddAMOUNT_PER_PERIOD(builder, self.AMOUNT_PER_PERIOD)
        if self.CURRENCY is not None:
            SUBAddCURRENCY(builder, CURRENCY)
        SUBAddNEXT_CHARGE_AT(builder, self.NEXT_CHARGE_AT)
        SUBAddMAX_PERIODS(builder, self.MAX_PERIODS)
        SUBAddSTATUS(builder, self.STATUS)
        if self.SIGNATURE is not None:
            SUBAddSIGNATURE(builder, SIGNATURE)
        SUB = SUBEnd(builder)
        return SUB
