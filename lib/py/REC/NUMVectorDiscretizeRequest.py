# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Request for component-wise vector discretization.
class NUMVectorDiscretizeRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NUMVectorDiscretizeRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNUMVectorDiscretizeRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NUMVectorDiscretizeRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x55\x4D", size_prefixed=size_prefixed)

    # NUMVectorDiscretizeRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Input vector to quantize.
    # NUMVectorDiscretizeRequest
    def STATE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMVectorDiscretizeRequest
    def STATEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMVectorDiscretizeRequest
    def STATELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMVectorDiscretizeRequest
    def STATEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Per-component least-significant-bit size. Length must match STATE.
    # NUMVectorDiscretizeRequest
    def LSB(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMVectorDiscretizeRequest
    def LSBAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMVectorDiscretizeRequest
    def LSBLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMVectorDiscretizeRequest
    def LSBIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Rounding direction used before the original sign is restored.
    # NUMVectorDiscretizeRequest
    def ROUND_DIRECTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # When true, PREVIOUS_ERROR is added to STATE before discretization.
    # NUMVectorDiscretizeRequest
    def CARRY_ERROR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Optional prior discretization error vector from an earlier result.
    # NUMVectorDiscretizeRequest
    def PREVIOUS_ERROR(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMVectorDiscretizeRequest
    def PREVIOUS_ERRORAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMVectorDiscretizeRequest
    def PREVIOUS_ERRORLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMVectorDiscretizeRequest
    def PREVIOUS_ERRORIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Optional trace/correlation identifier.
    # NUMVectorDiscretizeRequest
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def NUMVectorDiscretizeRequestStart(builder):
    builder.StartObject(6)

def Start(builder):
    NUMVectorDiscretizeRequestStart(builder)

def NUMVectorDiscretizeRequestAddSTATE(builder, STATE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(STATE), 0)

def AddSTATE(builder, STATE):
    NUMVectorDiscretizeRequestAddSTATE(builder, STATE)

def NUMVectorDiscretizeRequestStartSTATEVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSTATEVector(builder, numElems):
    return NUMVectorDiscretizeRequestStartSTATEVector(builder, numElems)

def NUMVectorDiscretizeRequestCreateSTATEVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSTATEVector(builder, data):
    NUMVectorDiscretizeRequestCreateSTATEVector(builder, data)

def NUMVectorDiscretizeRequestAddLSB(builder, LSB):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(LSB), 0)

def AddLSB(builder, LSB):
    NUMVectorDiscretizeRequestAddLSB(builder, LSB)

def NUMVectorDiscretizeRequestStartLSBVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartLSBVector(builder, numElems):
    return NUMVectorDiscretizeRequestStartLSBVector(builder, numElems)

def NUMVectorDiscretizeRequestCreateLSBVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateLSBVector(builder, data):
    NUMVectorDiscretizeRequestCreateLSBVector(builder, data)

def NUMVectorDiscretizeRequestAddROUND_DIRECTION(builder, ROUND_DIRECTION):
    builder.PrependInt8Slot(2, ROUND_DIRECTION, 0)

def AddROUND_DIRECTION(builder, ROUND_DIRECTION):
    NUMVectorDiscretizeRequestAddROUND_DIRECTION(builder, ROUND_DIRECTION)

def NUMVectorDiscretizeRequestAddCARRY_ERROR(builder, CARRY_ERROR):
    builder.PrependBoolSlot(3, CARRY_ERROR, 0)

def AddCARRY_ERROR(builder, CARRY_ERROR):
    NUMVectorDiscretizeRequestAddCARRY_ERROR(builder, CARRY_ERROR)

def NUMVectorDiscretizeRequestAddPREVIOUS_ERROR(builder, PREVIOUS_ERROR):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PREVIOUS_ERROR), 0)

def AddPREVIOUS_ERROR(builder, PREVIOUS_ERROR):
    NUMVectorDiscretizeRequestAddPREVIOUS_ERROR(builder, PREVIOUS_ERROR)

def NUMVectorDiscretizeRequestStartPREVIOUS_ERRORVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartPREVIOUS_ERRORVector(builder, numElems):
    return NUMVectorDiscretizeRequestStartPREVIOUS_ERRORVector(builder, numElems)

def NUMVectorDiscretizeRequestCreatePREVIOUS_ERRORVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreatePREVIOUS_ERRORVector(builder, data):
    NUMVectorDiscretizeRequestCreatePREVIOUS_ERRORVector(builder, data)

def NUMVectorDiscretizeRequestAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(TRACE_ID), 0)

def AddTRACE_ID(builder, TRACE_ID):
    NUMVectorDiscretizeRequestAddTRACE_ID(builder, TRACE_ID)

def NUMVectorDiscretizeRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return NUMVectorDiscretizeRequestEnd(builder)

try:
    from typing import List
except:
    pass

class NUMVectorDiscretizeRequestT(object):

    # NUMVectorDiscretizeRequestT
    def __init__(
        self,
        STATE = None,
        LSB = None,
        ROUND_DIRECTION = 0,
        CARRY_ERROR = False,
        PREVIOUS_ERROR = None,
        TRACE_ID = None,
    ):
        self.STATE = STATE  # type: Optional[List[float]]
        self.LSB = LSB  # type: Optional[List[float]]
        self.ROUND_DIRECTION = ROUND_DIRECTION  # type: int
        self.CARRY_ERROR = CARRY_ERROR  # type: bool
        self.PREVIOUS_ERROR = PREVIOUS_ERROR  # type: Optional[List[float]]
        self.TRACE_ID = TRACE_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNumvectorDiscretizeRequest = NUMVectorDiscretizeRequest()
        tmpNumvectorDiscretizeRequest.Init(buf, pos)
        return cls.InitFromObj(tmpNumvectorDiscretizeRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNumvectorDiscretizeRequest):
        x = NUMVectorDiscretizeRequestT()
        x._UnPack(tmpNumvectorDiscretizeRequest)
        return x

    # NUMVectorDiscretizeRequestT
    def _UnPack(self, NUMVectorDiscretizeRequest):
        if NUMVectorDiscretizeRequest is None:
            return
        if not NUMVectorDiscretizeRequest.STATEIsNone():
            if np is None:
                self.STATE = []
                for i in range(NUMVectorDiscretizeRequest.STATELength()):
                    self.STATE.append(NUMVectorDiscretizeRequest.STATE(i))
            else:
                self.STATE = NUMVectorDiscretizeRequest.STATEAsNumpy()
        if not NUMVectorDiscretizeRequest.LSBIsNone():
            if np is None:
                self.LSB = []
                for i in range(NUMVectorDiscretizeRequest.LSBLength()):
                    self.LSB.append(NUMVectorDiscretizeRequest.LSB(i))
            else:
                self.LSB = NUMVectorDiscretizeRequest.LSBAsNumpy()
        self.ROUND_DIRECTION = NUMVectorDiscretizeRequest.ROUND_DIRECTION()
        self.CARRY_ERROR = NUMVectorDiscretizeRequest.CARRY_ERROR()
        if not NUMVectorDiscretizeRequest.PREVIOUS_ERRORIsNone():
            if np is None:
                self.PREVIOUS_ERROR = []
                for i in range(NUMVectorDiscretizeRequest.PREVIOUS_ERRORLength()):
                    self.PREVIOUS_ERROR.append(NUMVectorDiscretizeRequest.PREVIOUS_ERROR(i))
            else:
                self.PREVIOUS_ERROR = NUMVectorDiscretizeRequest.PREVIOUS_ERRORAsNumpy()
        self.TRACE_ID = NUMVectorDiscretizeRequest.TRACE_ID()

    # NUMVectorDiscretizeRequestT
    def Pack(self, builder):
        if self.STATE is not None:
            if np is not None and type(self.STATE) is np.ndarray:
                STATE = builder.CreateNumpyVector(self.STATE)
            else:
                NUMVectorDiscretizeRequestStartSTATEVector(builder, len(self.STATE))
                for i in reversed(range(len(self.STATE))):
                    builder.PrependFloat64(self.STATE[i])
                STATE = builder.EndVector()
        if self.LSB is not None:
            if np is not None and type(self.LSB) is np.ndarray:
                LSB = builder.CreateNumpyVector(self.LSB)
            else:
                NUMVectorDiscretizeRequestStartLSBVector(builder, len(self.LSB))
                for i in reversed(range(len(self.LSB))):
                    builder.PrependFloat64(self.LSB[i])
                LSB = builder.EndVector()
        if self.PREVIOUS_ERROR is not None:
            if np is not None and type(self.PREVIOUS_ERROR) is np.ndarray:
                PREVIOUS_ERROR = builder.CreateNumpyVector(self.PREVIOUS_ERROR)
            else:
                NUMVectorDiscretizeRequestStartPREVIOUS_ERRORVector(builder, len(self.PREVIOUS_ERROR))
                for i in reversed(range(len(self.PREVIOUS_ERROR))):
                    builder.PrependFloat64(self.PREVIOUS_ERROR[i])
                PREVIOUS_ERROR = builder.EndVector()
        if self.TRACE_ID is not None:
            TRACE_ID = builder.CreateString(self.TRACE_ID)
        NUMVectorDiscretizeRequestStart(builder)
        if self.STATE is not None:
            NUMVectorDiscretizeRequestAddSTATE(builder, STATE)
        if self.LSB is not None:
            NUMVectorDiscretizeRequestAddLSB(builder, LSB)
        NUMVectorDiscretizeRequestAddROUND_DIRECTION(builder, self.ROUND_DIRECTION)
        NUMVectorDiscretizeRequestAddCARRY_ERROR(builder, self.CARRY_ERROR)
        if self.PREVIOUS_ERROR is not None:
            NUMVectorDiscretizeRequestAddPREVIOUS_ERROR(builder, PREVIOUS_ERROR)
        if self.TRACE_ID is not None:
            NUMVectorDiscretizeRequestAddTRACE_ID(builder, TRACE_ID)
        NUMVectorDiscretizeRequest = NUMVectorDiscretizeRequestEnd(builder)
        return NUMVectorDiscretizeRequest
