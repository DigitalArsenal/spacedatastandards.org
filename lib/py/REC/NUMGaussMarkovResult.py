# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Result of one Basilisk-compatible Gauss-Markov random sequence.
class NUMGaussMarkovResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NUMGaussMarkovResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNUMGaussMarkovResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NUMGaussMarkovResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x55\x4D", size_prefixed=size_prefixed)

    # NUMGaussMarkovResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # NUMGaussMarkovResult
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # NUMGaussMarkovResult
    def ERROR_MESSAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Post-warmup per-state sample means.
    # NUMGaussMarkovResult
    def MEAN(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMGaussMarkovResult
    def MEANAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMGaussMarkovResult
    def MEANLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMGaussMarkovResult
    def MEANIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Post-warmup per-state sample standard deviations with n - 1 denominator.
    # NUMGaussMarkovResult
    def STANDARD_DEVIATION(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMGaussMarkovResult
    def STANDARD_DEVIATIONAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMGaussMarkovResult
    def STANDARD_DEVIATIONLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMGaussMarkovResult
    def STANDARD_DEVIATIONIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Post-warmup per-state minima.
    # NUMGaussMarkovResult
    def MINIMUM(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMGaussMarkovResult
    def MINIMUMAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMGaussMarkovResult
    def MINIMUMLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMGaussMarkovResult
    def MINIMUMIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Post-warmup per-state maxima.
    # NUMGaussMarkovResult
    def MAXIMUM(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMGaussMarkovResult
    def MAXIMUMAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMGaussMarkovResult
    def MAXIMUMLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMGaussMarkovResult
    def MAXIMUMIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # State after the final generated sample.
    # NUMGaussMarkovResult
    def FINAL_STATE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMGaussMarkovResult
    def FINAL_STATEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMGaussMarkovResult
    def FINAL_STATELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMGaussMarkovResult
    def FINAL_STATEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Optional flattened sample-major state history.
    # NUMGaussMarkovResult
    def SAMPLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NUMGaussMarkovResult
    def SAMPLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NUMGaussMarkovResult
    def SAMPLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NUMGaussMarkovResult
    def SAMPLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Caller trace/correlation identifier copied from the request when present.
    # NUMGaussMarkovResult
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def NUMGaussMarkovResultStart(builder):
    builder.StartObject(9)

def Start(builder):
    NUMGaussMarkovResultStart(builder)

def NUMGaussMarkovResultAddSTATUS(builder, STATUS):
    builder.PrependInt8Slot(0, STATUS, 0)

def AddSTATUS(builder, STATUS):
    NUMGaussMarkovResultAddSTATUS(builder, STATUS)

def NUMGaussMarkovResultAddERROR_MESSAGE(builder, ERROR_MESSAGE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ERROR_MESSAGE), 0)

def AddERROR_MESSAGE(builder, ERROR_MESSAGE):
    NUMGaussMarkovResultAddERROR_MESSAGE(builder, ERROR_MESSAGE)

def NUMGaussMarkovResultAddMEAN(builder, MEAN):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MEAN), 0)

def AddMEAN(builder, MEAN):
    NUMGaussMarkovResultAddMEAN(builder, MEAN)

def NUMGaussMarkovResultStartMEANVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartMEANVector(builder, numElems):
    return NUMGaussMarkovResultStartMEANVector(builder, numElems)

def NUMGaussMarkovResultCreateMEANVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateMEANVector(builder, data):
    NUMGaussMarkovResultCreateMEANVector(builder, data)

def NUMGaussMarkovResultAddSTANDARD_DEVIATION(builder, STANDARD_DEVIATION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(STANDARD_DEVIATION), 0)

def AddSTANDARD_DEVIATION(builder, STANDARD_DEVIATION):
    NUMGaussMarkovResultAddSTANDARD_DEVIATION(builder, STANDARD_DEVIATION)

def NUMGaussMarkovResultStartSTANDARD_DEVIATIONVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSTANDARD_DEVIATIONVector(builder, numElems):
    return NUMGaussMarkovResultStartSTANDARD_DEVIATIONVector(builder, numElems)

def NUMGaussMarkovResultCreateSTANDARD_DEVIATIONVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSTANDARD_DEVIATIONVector(builder, data):
    NUMGaussMarkovResultCreateSTANDARD_DEVIATIONVector(builder, data)

def NUMGaussMarkovResultAddMINIMUM(builder, MINIMUM):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(MINIMUM), 0)

def AddMINIMUM(builder, MINIMUM):
    NUMGaussMarkovResultAddMINIMUM(builder, MINIMUM)

def NUMGaussMarkovResultStartMINIMUMVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartMINIMUMVector(builder, numElems):
    return NUMGaussMarkovResultStartMINIMUMVector(builder, numElems)

def NUMGaussMarkovResultCreateMINIMUMVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateMINIMUMVector(builder, data):
    NUMGaussMarkovResultCreateMINIMUMVector(builder, data)

def NUMGaussMarkovResultAddMAXIMUM(builder, MAXIMUM):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(MAXIMUM), 0)

def AddMAXIMUM(builder, MAXIMUM):
    NUMGaussMarkovResultAddMAXIMUM(builder, MAXIMUM)

def NUMGaussMarkovResultStartMAXIMUMVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartMAXIMUMVector(builder, numElems):
    return NUMGaussMarkovResultStartMAXIMUMVector(builder, numElems)

def NUMGaussMarkovResultCreateMAXIMUMVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateMAXIMUMVector(builder, data):
    NUMGaussMarkovResultCreateMAXIMUMVector(builder, data)

def NUMGaussMarkovResultAddFINAL_STATE(builder, FINAL_STATE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(FINAL_STATE), 0)

def AddFINAL_STATE(builder, FINAL_STATE):
    NUMGaussMarkovResultAddFINAL_STATE(builder, FINAL_STATE)

def NUMGaussMarkovResultStartFINAL_STATEVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartFINAL_STATEVector(builder, numElems):
    return NUMGaussMarkovResultStartFINAL_STATEVector(builder, numElems)

def NUMGaussMarkovResultCreateFINAL_STATEVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateFINAL_STATEVector(builder, data):
    NUMGaussMarkovResultCreateFINAL_STATEVector(builder, data)

def NUMGaussMarkovResultAddSAMPLES(builder, SAMPLES):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(SAMPLES), 0)

def AddSAMPLES(builder, SAMPLES):
    NUMGaussMarkovResultAddSAMPLES(builder, SAMPLES)

def NUMGaussMarkovResultStartSAMPLESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSAMPLESVector(builder, numElems):
    return NUMGaussMarkovResultStartSAMPLESVector(builder, numElems)

def NUMGaussMarkovResultCreateSAMPLESVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSAMPLESVector(builder, data):
    NUMGaussMarkovResultCreateSAMPLESVector(builder, data)

def NUMGaussMarkovResultAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(TRACE_ID), 0)

def AddTRACE_ID(builder, TRACE_ID):
    NUMGaussMarkovResultAddTRACE_ID(builder, TRACE_ID)

def NUMGaussMarkovResultEnd(builder):
    return builder.EndObject()

def End(builder):
    return NUMGaussMarkovResultEnd(builder)

try:
    from typing import List
except:
    pass

class NUMGaussMarkovResultT(object):

    # NUMGaussMarkovResultT
    def __init__(
        self,
        STATUS = 0,
        ERROR_MESSAGE = None,
        MEAN = None,
        STANDARD_DEVIATION = None,
        MINIMUM = None,
        MAXIMUM = None,
        FINAL_STATE = None,
        SAMPLES = None,
        TRACE_ID = None,
    ):
        self.STATUS = STATUS  # type: int
        self.ERROR_MESSAGE = ERROR_MESSAGE  # type: Optional[str]
        self.MEAN = MEAN  # type: Optional[List[float]]
        self.STANDARD_DEVIATION = STANDARD_DEVIATION  # type: Optional[List[float]]
        self.MINIMUM = MINIMUM  # type: Optional[List[float]]
        self.MAXIMUM = MAXIMUM  # type: Optional[List[float]]
        self.FINAL_STATE = FINAL_STATE  # type: Optional[List[float]]
        self.SAMPLES = SAMPLES  # type: Optional[List[float]]
        self.TRACE_ID = TRACE_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNumgaussMarkovResult = NUMGaussMarkovResult()
        tmpNumgaussMarkovResult.Init(buf, pos)
        return cls.InitFromObj(tmpNumgaussMarkovResult)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNumgaussMarkovResult):
        x = NUMGaussMarkovResultT()
        x._UnPack(tmpNumgaussMarkovResult)
        return x

    # NUMGaussMarkovResultT
    def _UnPack(self, NUMGaussMarkovResult):
        if NUMGaussMarkovResult is None:
            return
        self.STATUS = NUMGaussMarkovResult.STATUS()
        self.ERROR_MESSAGE = NUMGaussMarkovResult.ERROR_MESSAGE()
        if not NUMGaussMarkovResult.MEANIsNone():
            if np is None:
                self.MEAN = []
                for i in range(NUMGaussMarkovResult.MEANLength()):
                    self.MEAN.append(NUMGaussMarkovResult.MEAN(i))
            else:
                self.MEAN = NUMGaussMarkovResult.MEANAsNumpy()
        if not NUMGaussMarkovResult.STANDARD_DEVIATIONIsNone():
            if np is None:
                self.STANDARD_DEVIATION = []
                for i in range(NUMGaussMarkovResult.STANDARD_DEVIATIONLength()):
                    self.STANDARD_DEVIATION.append(NUMGaussMarkovResult.STANDARD_DEVIATION(i))
            else:
                self.STANDARD_DEVIATION = NUMGaussMarkovResult.STANDARD_DEVIATIONAsNumpy()
        if not NUMGaussMarkovResult.MINIMUMIsNone():
            if np is None:
                self.MINIMUM = []
                for i in range(NUMGaussMarkovResult.MINIMUMLength()):
                    self.MINIMUM.append(NUMGaussMarkovResult.MINIMUM(i))
            else:
                self.MINIMUM = NUMGaussMarkovResult.MINIMUMAsNumpy()
        if not NUMGaussMarkovResult.MAXIMUMIsNone():
            if np is None:
                self.MAXIMUM = []
                for i in range(NUMGaussMarkovResult.MAXIMUMLength()):
                    self.MAXIMUM.append(NUMGaussMarkovResult.MAXIMUM(i))
            else:
                self.MAXIMUM = NUMGaussMarkovResult.MAXIMUMAsNumpy()
        if not NUMGaussMarkovResult.FINAL_STATEIsNone():
            if np is None:
                self.FINAL_STATE = []
                for i in range(NUMGaussMarkovResult.FINAL_STATELength()):
                    self.FINAL_STATE.append(NUMGaussMarkovResult.FINAL_STATE(i))
            else:
                self.FINAL_STATE = NUMGaussMarkovResult.FINAL_STATEAsNumpy()
        if not NUMGaussMarkovResult.SAMPLESIsNone():
            if np is None:
                self.SAMPLES = []
                for i in range(NUMGaussMarkovResult.SAMPLESLength()):
                    self.SAMPLES.append(NUMGaussMarkovResult.SAMPLES(i))
            else:
                self.SAMPLES = NUMGaussMarkovResult.SAMPLESAsNumpy()
        self.TRACE_ID = NUMGaussMarkovResult.TRACE_ID()

    # NUMGaussMarkovResultT
    def Pack(self, builder):
        if self.ERROR_MESSAGE is not None:
            ERROR_MESSAGE = builder.CreateString(self.ERROR_MESSAGE)
        if self.MEAN is not None:
            if np is not None and type(self.MEAN) is np.ndarray:
                MEAN = builder.CreateNumpyVector(self.MEAN)
            else:
                NUMGaussMarkovResultStartMEANVector(builder, len(self.MEAN))
                for i in reversed(range(len(self.MEAN))):
                    builder.PrependFloat64(self.MEAN[i])
                MEAN = builder.EndVector()
        if self.STANDARD_DEVIATION is not None:
            if np is not None and type(self.STANDARD_DEVIATION) is np.ndarray:
                STANDARD_DEVIATION = builder.CreateNumpyVector(self.STANDARD_DEVIATION)
            else:
                NUMGaussMarkovResultStartSTANDARD_DEVIATIONVector(builder, len(self.STANDARD_DEVIATION))
                for i in reversed(range(len(self.STANDARD_DEVIATION))):
                    builder.PrependFloat64(self.STANDARD_DEVIATION[i])
                STANDARD_DEVIATION = builder.EndVector()
        if self.MINIMUM is not None:
            if np is not None and type(self.MINIMUM) is np.ndarray:
                MINIMUM = builder.CreateNumpyVector(self.MINIMUM)
            else:
                NUMGaussMarkovResultStartMINIMUMVector(builder, len(self.MINIMUM))
                for i in reversed(range(len(self.MINIMUM))):
                    builder.PrependFloat64(self.MINIMUM[i])
                MINIMUM = builder.EndVector()
        if self.MAXIMUM is not None:
            if np is not None and type(self.MAXIMUM) is np.ndarray:
                MAXIMUM = builder.CreateNumpyVector(self.MAXIMUM)
            else:
                NUMGaussMarkovResultStartMAXIMUMVector(builder, len(self.MAXIMUM))
                for i in reversed(range(len(self.MAXIMUM))):
                    builder.PrependFloat64(self.MAXIMUM[i])
                MAXIMUM = builder.EndVector()
        if self.FINAL_STATE is not None:
            if np is not None and type(self.FINAL_STATE) is np.ndarray:
                FINAL_STATE = builder.CreateNumpyVector(self.FINAL_STATE)
            else:
                NUMGaussMarkovResultStartFINAL_STATEVector(builder, len(self.FINAL_STATE))
                for i in reversed(range(len(self.FINAL_STATE))):
                    builder.PrependFloat64(self.FINAL_STATE[i])
                FINAL_STATE = builder.EndVector()
        if self.SAMPLES is not None:
            if np is not None and type(self.SAMPLES) is np.ndarray:
                SAMPLES = builder.CreateNumpyVector(self.SAMPLES)
            else:
                NUMGaussMarkovResultStartSAMPLESVector(builder, len(self.SAMPLES))
                for i in reversed(range(len(self.SAMPLES))):
                    builder.PrependFloat64(self.SAMPLES[i])
                SAMPLES = builder.EndVector()
        if self.TRACE_ID is not None:
            TRACE_ID = builder.CreateString(self.TRACE_ID)
        NUMGaussMarkovResultStart(builder)
        NUMGaussMarkovResultAddSTATUS(builder, self.STATUS)
        if self.ERROR_MESSAGE is not None:
            NUMGaussMarkovResultAddERROR_MESSAGE(builder, ERROR_MESSAGE)
        if self.MEAN is not None:
            NUMGaussMarkovResultAddMEAN(builder, MEAN)
        if self.STANDARD_DEVIATION is not None:
            NUMGaussMarkovResultAddSTANDARD_DEVIATION(builder, STANDARD_DEVIATION)
        if self.MINIMUM is not None:
            NUMGaussMarkovResultAddMINIMUM(builder, MINIMUM)
        if self.MAXIMUM is not None:
            NUMGaussMarkovResultAddMAXIMUM(builder, MAXIMUM)
        if self.FINAL_STATE is not None:
            NUMGaussMarkovResultAddFINAL_STATE(builder, FINAL_STATE)
        if self.SAMPLES is not None:
            NUMGaussMarkovResultAddSAMPLES(builder, SAMPLES)
        if self.TRACE_ID is not None:
            NUMGaussMarkovResultAddTRACE_ID(builder, TRACE_ID)
        NUMGaussMarkovResult = NUMGaussMarkovResultEnd(builder)
        return NUMGaussMarkovResult
