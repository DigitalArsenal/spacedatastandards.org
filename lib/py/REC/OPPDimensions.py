# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Physical envelope. Every extent is a sourced quantity. Extents measured off
# 3D-asset geometry carry PROVENANCE.METHOD MODEL_DERIVED and describe the
# model, not the flight article.
class OPPDimensions(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OPPDimensions()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOPPDimensions(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OPPDimensionsBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x50\x50", size_prefixed=size_prefixed)

    # OPPDimensions
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Extents along the object's own body axes [m].
    # OPPDimensions
    def BODY_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPPDimensions
    def BODY_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPPDimensions
    def BODY_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Envelope terms as an object database publishes them [m].
    # OPPDimensions
    def HEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPPDimensions
    def WIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPPDimensions
    def DEPTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OPPDimensions
    def DIAMETER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Largest extent with appendages stowed [m].
    # OPPDimensions
    def SPAN_STOWED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Largest extent with appendages deployed [m].
    # OPPDimensions
    def SPAN_DEPLOYED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Gross geometric shape verbatim from the source, such as a shape string
    # of the form "Box + 1 Pan" or "Cyl". Never parsed into geometry.
    # OPPDimensions
    def SHAPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provenance of SHAPE. Present whenever SHAPE is nonempty.
    # OPPDimensions
    def SHAPE_PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPProvenance import OPPProvenance
            obj = OPPProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def OPPDimensionsStart(builder):
    builder.StartObject(11)

def Start(builder):
    OPPDimensionsStart(builder)

def OPPDimensionsAddBODY_X(builder, BODY_X):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(BODY_X), 0)

def AddBODY_X(builder, BODY_X):
    OPPDimensionsAddBODY_X(builder, BODY_X)

def OPPDimensionsAddBODY_Y(builder, BODY_Y):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(BODY_Y), 0)

def AddBODY_Y(builder, BODY_Y):
    OPPDimensionsAddBODY_Y(builder, BODY_Y)

def OPPDimensionsAddBODY_Z(builder, BODY_Z):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(BODY_Z), 0)

def AddBODY_Z(builder, BODY_Z):
    OPPDimensionsAddBODY_Z(builder, BODY_Z)

def OPPDimensionsAddHEIGHT(builder, HEIGHT):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(HEIGHT), 0)

def AddHEIGHT(builder, HEIGHT):
    OPPDimensionsAddHEIGHT(builder, HEIGHT)

def OPPDimensionsAddWIDTH(builder, WIDTH):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(WIDTH), 0)

def AddWIDTH(builder, WIDTH):
    OPPDimensionsAddWIDTH(builder, WIDTH)

def OPPDimensionsAddDEPTH(builder, DEPTH):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(DEPTH), 0)

def AddDEPTH(builder, DEPTH):
    OPPDimensionsAddDEPTH(builder, DEPTH)

def OPPDimensionsAddDIAMETER(builder, DIAMETER):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(DIAMETER), 0)

def AddDIAMETER(builder, DIAMETER):
    OPPDimensionsAddDIAMETER(builder, DIAMETER)

def OPPDimensionsAddSPAN_STOWED(builder, SPAN_STOWED):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(SPAN_STOWED), 0)

def AddSPAN_STOWED(builder, SPAN_STOWED):
    OPPDimensionsAddSPAN_STOWED(builder, SPAN_STOWED)

def OPPDimensionsAddSPAN_DEPLOYED(builder, SPAN_DEPLOYED):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(SPAN_DEPLOYED), 0)

def AddSPAN_DEPLOYED(builder, SPAN_DEPLOYED):
    OPPDimensionsAddSPAN_DEPLOYED(builder, SPAN_DEPLOYED)

def OPPDimensionsAddSHAPE(builder, SHAPE):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(SHAPE), 0)

def AddSHAPE(builder, SHAPE):
    OPPDimensionsAddSHAPE(builder, SHAPE)

def OPPDimensionsAddSHAPE_PROVENANCE(builder, SHAPE_PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(SHAPE_PROVENANCE), 0)

def AddSHAPE_PROVENANCE(builder, SHAPE_PROVENANCE):
    OPPDimensionsAddSHAPE_PROVENANCE(builder, SHAPE_PROVENANCE)

def OPPDimensionsEnd(builder):
    return builder.EndObject()

def End(builder):
    return OPPDimensionsEnd(builder)

import OPPProvenance
import OPPQuantity
try:
    from typing import Optional
except:
    pass

class OPPDimensionsT(object):

    # OPPDimensionsT
    def __init__(
        self,
        BODY_X = None,
        BODY_Y = None,
        BODY_Z = None,
        HEIGHT = None,
        WIDTH = None,
        DEPTH = None,
        DIAMETER = None,
        SPAN_STOWED = None,
        SPAN_DEPLOYED = None,
        SHAPE = None,
        SHAPE_PROVENANCE = None,
    ):
        self.BODY_X = BODY_X  # type: Optional[OPPQuantity.OPPQuantityT]
        self.BODY_Y = BODY_Y  # type: Optional[OPPQuantity.OPPQuantityT]
        self.BODY_Z = BODY_Z  # type: Optional[OPPQuantity.OPPQuantityT]
        self.HEIGHT = HEIGHT  # type: Optional[OPPQuantity.OPPQuantityT]
        self.WIDTH = WIDTH  # type: Optional[OPPQuantity.OPPQuantityT]
        self.DEPTH = DEPTH  # type: Optional[OPPQuantity.OPPQuantityT]
        self.DIAMETER = DIAMETER  # type: Optional[OPPQuantity.OPPQuantityT]
        self.SPAN_STOWED = SPAN_STOWED  # type: Optional[OPPQuantity.OPPQuantityT]
        self.SPAN_DEPLOYED = SPAN_DEPLOYED  # type: Optional[OPPQuantity.OPPQuantityT]
        self.SHAPE = SHAPE  # type: Optional[str]
        self.SHAPE_PROVENANCE = SHAPE_PROVENANCE  # type: Optional[OPPProvenance.OPPProvenanceT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOppdimensions = OPPDimensions()
        tmpOppdimensions.Init(buf, pos)
        return cls.InitFromObj(tmpOppdimensions)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOppdimensions):
        x = OPPDimensionsT()
        x._UnPack(tmpOppdimensions)
        return x

    # OPPDimensionsT
    def _UnPack(self, OPPDimensions):
        if OPPDimensions is None:
            return
        if OPPDimensions.BODY_X() is not None:
            self.BODY_X = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.BODY_X())
        if OPPDimensions.BODY_Y() is not None:
            self.BODY_Y = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.BODY_Y())
        if OPPDimensions.BODY_Z() is not None:
            self.BODY_Z = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.BODY_Z())
        if OPPDimensions.HEIGHT() is not None:
            self.HEIGHT = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.HEIGHT())
        if OPPDimensions.WIDTH() is not None:
            self.WIDTH = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.WIDTH())
        if OPPDimensions.DEPTH() is not None:
            self.DEPTH = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.DEPTH())
        if OPPDimensions.DIAMETER() is not None:
            self.DIAMETER = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.DIAMETER())
        if OPPDimensions.SPAN_STOWED() is not None:
            self.SPAN_STOWED = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.SPAN_STOWED())
        if OPPDimensions.SPAN_DEPLOYED() is not None:
            self.SPAN_DEPLOYED = OPPQuantity.OPPQuantityT.InitFromObj(OPPDimensions.SPAN_DEPLOYED())
        self.SHAPE = OPPDimensions.SHAPE()
        if OPPDimensions.SHAPE_PROVENANCE() is not None:
            self.SHAPE_PROVENANCE = OPPProvenance.OPPProvenanceT.InitFromObj(OPPDimensions.SHAPE_PROVENANCE())

    # OPPDimensionsT
    def Pack(self, builder):
        if self.BODY_X is not None:
            BODY_X = self.BODY_X.Pack(builder)
        if self.BODY_Y is not None:
            BODY_Y = self.BODY_Y.Pack(builder)
        if self.BODY_Z is not None:
            BODY_Z = self.BODY_Z.Pack(builder)
        if self.HEIGHT is not None:
            HEIGHT = self.HEIGHT.Pack(builder)
        if self.WIDTH is not None:
            WIDTH = self.WIDTH.Pack(builder)
        if self.DEPTH is not None:
            DEPTH = self.DEPTH.Pack(builder)
        if self.DIAMETER is not None:
            DIAMETER = self.DIAMETER.Pack(builder)
        if self.SPAN_STOWED is not None:
            SPAN_STOWED = self.SPAN_STOWED.Pack(builder)
        if self.SPAN_DEPLOYED is not None:
            SPAN_DEPLOYED = self.SPAN_DEPLOYED.Pack(builder)
        if self.SHAPE is not None:
            SHAPE = builder.CreateString(self.SHAPE)
        if self.SHAPE_PROVENANCE is not None:
            SHAPE_PROVENANCE = self.SHAPE_PROVENANCE.Pack(builder)
        OPPDimensionsStart(builder)
        if self.BODY_X is not None:
            OPPDimensionsAddBODY_X(builder, BODY_X)
        if self.BODY_Y is not None:
            OPPDimensionsAddBODY_Y(builder, BODY_Y)
        if self.BODY_Z is not None:
            OPPDimensionsAddBODY_Z(builder, BODY_Z)
        if self.HEIGHT is not None:
            OPPDimensionsAddHEIGHT(builder, HEIGHT)
        if self.WIDTH is not None:
            OPPDimensionsAddWIDTH(builder, WIDTH)
        if self.DEPTH is not None:
            OPPDimensionsAddDEPTH(builder, DEPTH)
        if self.DIAMETER is not None:
            OPPDimensionsAddDIAMETER(builder, DIAMETER)
        if self.SPAN_STOWED is not None:
            OPPDimensionsAddSPAN_STOWED(builder, SPAN_STOWED)
        if self.SPAN_DEPLOYED is not None:
            OPPDimensionsAddSPAN_DEPLOYED(builder, SPAN_DEPLOYED)
        if self.SHAPE is not None:
            OPPDimensionsAddSHAPE(builder, SHAPE)
        if self.SHAPE_PROVENANCE is not None:
            OPPDimensionsAddSHAPE_PROVENANCE(builder, SHAPE_PROVENANCE)
        OPPDimensions = OPPDimensionsEnd(builder)
        return OPPDimensions
