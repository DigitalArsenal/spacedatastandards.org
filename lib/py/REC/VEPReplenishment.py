# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One replenishment option: how the energy store is reset.
class VEPReplenishment(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VEPReplenishment()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVEPReplenishment(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VEPReplenishmentBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x45\x50", size_prefixed=size_prefixed)

    # VEPReplenishment
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # `$TFN.ID` of a facility where this replenishment is available. Absent
    # for a replenishment that is not facility-bound.
    # VEPReplenishment
    def FACILITY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Typical duration of the replenishment, seconds.
    # VEPReplenishment
    def DURATION_S(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Transfer rate, in TRANSFER_RATE_UNITS.
    # VEPReplenishment
    def TRANSFER_RATE_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Units of TRANSFER_RATE_VALUE, verbatim.
    # VEPReplenishment
    def TRANSFER_RATE_UNITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Replenishment happens while the vehicle is underway (aerial
    # refuelling, underway replenishment).
    # VEPReplenishment
    def IN_MOTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def VEPReplenishmentStart(builder):
    builder.StartObject(5)

def Start(builder):
    VEPReplenishmentStart(builder)

def VEPReplenishmentAddFACILITY_ID(builder, FACILITY_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(FACILITY_ID), 0)

def AddFACILITY_ID(builder, FACILITY_ID):
    VEPReplenishmentAddFACILITY_ID(builder, FACILITY_ID)

def VEPReplenishmentAddDURATION_S(builder, DURATION_S):
    builder.PrependFloat64Slot(1, DURATION_S, 0.0)

def AddDURATION_S(builder, DURATION_S):
    VEPReplenishmentAddDURATION_S(builder, DURATION_S)

def VEPReplenishmentAddTRANSFER_RATE_VALUE(builder, TRANSFER_RATE_VALUE):
    builder.PrependFloat64Slot(2, TRANSFER_RATE_VALUE, 0.0)

def AddTRANSFER_RATE_VALUE(builder, TRANSFER_RATE_VALUE):
    VEPReplenishmentAddTRANSFER_RATE_VALUE(builder, TRANSFER_RATE_VALUE)

def VEPReplenishmentAddTRANSFER_RATE_UNITS(builder, TRANSFER_RATE_UNITS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(TRANSFER_RATE_UNITS), 0)

def AddTRANSFER_RATE_UNITS(builder, TRANSFER_RATE_UNITS):
    VEPReplenishmentAddTRANSFER_RATE_UNITS(builder, TRANSFER_RATE_UNITS)

def VEPReplenishmentAddIN_MOTION(builder, IN_MOTION):
    builder.PrependBoolSlot(4, IN_MOTION, 0)

def AddIN_MOTION(builder, IN_MOTION):
    VEPReplenishmentAddIN_MOTION(builder, IN_MOTION)

def VEPReplenishmentEnd(builder):
    return builder.EndObject()

def End(builder):
    return VEPReplenishmentEnd(builder)


class VEPReplenishmentT(object):

    # VEPReplenishmentT
    def __init__(
        self,
        FACILITY_ID = None,
        DURATION_S = 0.0,
        TRANSFER_RATE_VALUE = 0.0,
        TRANSFER_RATE_UNITS = None,
        IN_MOTION = False,
    ):
        self.FACILITY_ID = FACILITY_ID  # type: Optional[str]
        self.DURATION_S = DURATION_S  # type: float
        self.TRANSFER_RATE_VALUE = TRANSFER_RATE_VALUE  # type: float
        self.TRANSFER_RATE_UNITS = TRANSFER_RATE_UNITS  # type: Optional[str]
        self.IN_MOTION = IN_MOTION  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVepreplenishment = VEPReplenishment()
        tmpVepreplenishment.Init(buf, pos)
        return cls.InitFromObj(tmpVepreplenishment)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVepreplenishment):
        x = VEPReplenishmentT()
        x._UnPack(tmpVepreplenishment)
        return x

    # VEPReplenishmentT
    def _UnPack(self, VEPReplenishment):
        if VEPReplenishment is None:
            return
        self.FACILITY_ID = VEPReplenishment.FACILITY_ID()
        self.DURATION_S = VEPReplenishment.DURATION_S()
        self.TRANSFER_RATE_VALUE = VEPReplenishment.TRANSFER_RATE_VALUE()
        self.TRANSFER_RATE_UNITS = VEPReplenishment.TRANSFER_RATE_UNITS()
        self.IN_MOTION = VEPReplenishment.IN_MOTION()

    # VEPReplenishmentT
    def Pack(self, builder):
        if self.FACILITY_ID is not None:
            FACILITY_ID = builder.CreateString(self.FACILITY_ID)
        if self.TRANSFER_RATE_UNITS is not None:
            TRANSFER_RATE_UNITS = builder.CreateString(self.TRANSFER_RATE_UNITS)
        VEPReplenishmentStart(builder)
        if self.FACILITY_ID is not None:
            VEPReplenishmentAddFACILITY_ID(builder, FACILITY_ID)
        VEPReplenishmentAddDURATION_S(builder, self.DURATION_S)
        VEPReplenishmentAddTRANSFER_RATE_VALUE(builder, self.TRANSFER_RATE_VALUE)
        if self.TRANSFER_RATE_UNITS is not None:
            VEPReplenishmentAddTRANSFER_RATE_UNITS(builder, TRANSFER_RATE_UNITS)
        VEPReplenishmentAddIN_MOTION(builder, self.IN_MOTION)
        VEPReplenishment = VEPReplenishmentEnd(builder)
        return VEPReplenishment
