# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Scenario Controls - message-bus envelope for scenario setup, state
# requests/responses, startup readiness, and asset-picker delegation.
class SCC(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCC()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCC(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCCBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x43", size_prefixed=size_prefixed)

    # SCC
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCC
    def MESSAGE_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # SCC
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SCC
    def SCENARIOS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCN import SCN
            obj = SCN()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCC
    def SCENARIOSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCC
    def SCENARIOSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # SCC
    def SCENARIO(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCN import SCN
            obj = SCN()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCC
    def REQUEST_STATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCCRequestState import SCCRequestState
            obj = SCCRequestState()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCC
    def STATE_RESPONSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCCStateResponse import SCCStateResponse
            obj = SCCStateResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCC
    def ASSET_PICKER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCCAssetPicker import SCCAssetPicker
            obj = SCCAssetPicker()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCC
    def READY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCCReady import SCCReady
            obj = SCCReady()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def SCCStart(builder):
    builder.StartObject(8)

def Start(builder):
    SCCStart(builder)

def SCCAddMESSAGE_KIND(builder, MESSAGE_KIND):
    builder.PrependInt8Slot(0, MESSAGE_KIND, 0)

def AddMESSAGE_KIND(builder, MESSAGE_KIND):
    SCCAddMESSAGE_KIND(builder, MESSAGE_KIND)

def SCCAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(TRACE_ID), 0)

def AddTRACE_ID(builder, TRACE_ID):
    SCCAddTRACE_ID(builder, TRACE_ID)

def SCCAddSCENARIOS(builder, SCENARIOS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SCENARIOS), 0)

def AddSCENARIOS(builder, SCENARIOS):
    SCCAddSCENARIOS(builder, SCENARIOS)

def SCCStartSCENARIOSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSCENARIOSVector(builder, numElems):
    return SCCStartSCENARIOSVector(builder, numElems)

def SCCCreateSCENARIOSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSCENARIOSVector(builder, data):
    SCCCreateSCENARIOSVector(builder, data)

def SCCAddSCENARIO(builder, SCENARIO):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SCENARIO), 0)

def AddSCENARIO(builder, SCENARIO):
    SCCAddSCENARIO(builder, SCENARIO)

def SCCAddREQUEST_STATE(builder, REQUEST_STATE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_STATE), 0)

def AddREQUEST_STATE(builder, REQUEST_STATE):
    SCCAddREQUEST_STATE(builder, REQUEST_STATE)

def SCCAddSTATE_RESPONSE(builder, STATE_RESPONSE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(STATE_RESPONSE), 0)

def AddSTATE_RESPONSE(builder, STATE_RESPONSE):
    SCCAddSTATE_RESPONSE(builder, STATE_RESPONSE)

def SCCAddASSET_PICKER(builder, ASSET_PICKER):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ASSET_PICKER), 0)

def AddASSET_PICKER(builder, ASSET_PICKER):
    SCCAddASSET_PICKER(builder, ASSET_PICKER)

def SCCAddREADY(builder, READY):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(READY), 0)

def AddREADY(builder, READY):
    SCCAddREADY(builder, READY)

def SCCEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCCEnd(builder)

import SCCAssetPicker
import SCCReady
import SCCRequestState
import SCCStateResponse
import SCN
try:
    from typing import List, Optional
except:
    pass

class SCCT(object):

    # SCCT
    def __init__(
        self,
        MESSAGE_KIND = 0,
        TRACE_ID = None,
        SCENARIOS = None,
        SCENARIO = None,
        REQUEST_STATE = None,
        STATE_RESPONSE = None,
        ASSET_PICKER = None,
        READY = None,
    ):
        self.MESSAGE_KIND = MESSAGE_KIND  # type: int
        self.TRACE_ID = TRACE_ID  # type: Optional[str]
        self.SCENARIOS = SCENARIOS  # type: Optional[List[SCN.SCNT]]
        self.SCENARIO = SCENARIO  # type: Optional[SCN.SCNT]
        self.REQUEST_STATE = REQUEST_STATE  # type: Optional[SCCRequestState.SCCRequestStateT]
        self.STATE_RESPONSE = STATE_RESPONSE  # type: Optional[SCCStateResponse.SCCStateResponseT]
        self.ASSET_PICKER = ASSET_PICKER  # type: Optional[SCCAssetPicker.SCCAssetPickerT]
        self.READY = READY  # type: Optional[SCCReady.SCCReadyT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScc = SCC()
        tmpScc.Init(buf, pos)
        return cls.InitFromObj(tmpScc)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScc):
        x = SCCT()
        x._UnPack(tmpScc)
        return x

    # SCCT
    def _UnPack(self, SCC):
        if SCC is None:
            return
        self.MESSAGE_KIND = SCC.MESSAGE_KIND()
        self.TRACE_ID = SCC.TRACE_ID()
        if not SCC.SCENARIOSIsNone():
            self.SCENARIOS = []
            for i in range(SCC.SCENARIOSLength()):
                if SCC.SCENARIOS(i) is None:
                    self.SCENARIOS.append(None)
                else:
                    sCN_ = SCN.SCNT.InitFromObj(SCC.SCENARIOS(i))
                    self.SCENARIOS.append(sCN_)
        if SCC.SCENARIO() is not None:
            self.SCENARIO = SCN.SCNT.InitFromObj(SCC.SCENARIO())
        if SCC.REQUEST_STATE() is not None:
            self.REQUEST_STATE = SCCRequestState.SCCRequestStateT.InitFromObj(SCC.REQUEST_STATE())
        if SCC.STATE_RESPONSE() is not None:
            self.STATE_RESPONSE = SCCStateResponse.SCCStateResponseT.InitFromObj(SCC.STATE_RESPONSE())
        if SCC.ASSET_PICKER() is not None:
            self.ASSET_PICKER = SCCAssetPicker.SCCAssetPickerT.InitFromObj(SCC.ASSET_PICKER())
        if SCC.READY() is not None:
            self.READY = SCCReady.SCCReadyT.InitFromObj(SCC.READY())

    # SCCT
    def Pack(self, builder):
        if self.TRACE_ID is not None:
            TRACE_ID = builder.CreateString(self.TRACE_ID)
        if self.SCENARIOS is not None:
            SCENARIOSlist = []
            for i in range(len(self.SCENARIOS)):
                SCENARIOSlist.append(self.SCENARIOS[i].Pack(builder))
            SCCStartSCENARIOSVector(builder, len(self.SCENARIOS))
            for i in reversed(range(len(self.SCENARIOS))):
                builder.PrependUOffsetTRelative(SCENARIOSlist[i])
            SCENARIOS = builder.EndVector()
        if self.SCENARIO is not None:
            SCENARIO = self.SCENARIO.Pack(builder)
        if self.REQUEST_STATE is not None:
            REQUEST_STATE = self.REQUEST_STATE.Pack(builder)
        if self.STATE_RESPONSE is not None:
            STATE_RESPONSE = self.STATE_RESPONSE.Pack(builder)
        if self.ASSET_PICKER is not None:
            ASSET_PICKER = self.ASSET_PICKER.Pack(builder)
        if self.READY is not None:
            READY = self.READY.Pack(builder)
        SCCStart(builder)
        SCCAddMESSAGE_KIND(builder, self.MESSAGE_KIND)
        if self.TRACE_ID is not None:
            SCCAddTRACE_ID(builder, TRACE_ID)
        if self.SCENARIOS is not None:
            SCCAddSCENARIOS(builder, SCENARIOS)
        if self.SCENARIO is not None:
            SCCAddSCENARIO(builder, SCENARIO)
        if self.REQUEST_STATE is not None:
            SCCAddREQUEST_STATE(builder, REQUEST_STATE)
        if self.STATE_RESPONSE is not None:
            SCCAddSTATE_RESPONSE(builder, STATE_RESPONSE)
        if self.ASSET_PICKER is not None:
            SCCAddASSET_PICKER(builder, ASSET_PICKER)
        if self.READY is not None:
            SCCAddREADY(builder, READY)
        SCC = SCCEnd(builder)
        return SCC
