# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Response envelope emitted by `plugin_invoke_stream`.
class PIVResponse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PIVResponse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPIVResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PIVResponseBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x49\x56", size_prefixed=size_prefixed)

    # PIVResponse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Method-specific status code. Zero conventionally indicates success.
    # PIVResponse
    def STATUS_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Coarse envelope status.
    # PIVResponse
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # True when the method yielded before fully draining queued work.
    # PIVResponse
    def YIELDED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Outstanding frame count remaining after this response. Non-zero
    # only when STATUS == YIELDED.
    # PIVResponse
    def BACKLOG_REMAINING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Output frames. Each TAB carries schema identity and wire format.
    # PIVResponse
    def OUTPUTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TAB import TAB
            obj = TAB()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PIVResponse
    def OUTPUTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PIVResponse
    def OUTPUTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Optional arena backing OUTPUTS payload bodies.
    # PIVResponse
    def PAYLOAD_ARENA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # PIVResponse
    def PAYLOAD_ARENAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # PIVResponse
    def PAYLOAD_ARENALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PIVResponse
    def PAYLOAD_ARENAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Stable machine-readable error code when STATUS == FAILED.
    # PIVResponse
    def ERROR_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable diagnostic when STATUS == FAILED.
    # PIVResponse
    def ERROR_MESSAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Echo of the request's TRACE_ID for correlation.
    # PIVResponse
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def PIVResponseStart(builder):
    builder.StartObject(9)

def Start(builder):
    PIVResponseStart(builder)

def PIVResponseAddSTATUS_CODE(builder, STATUS_CODE):
    builder.PrependInt32Slot(0, STATUS_CODE, 0)

def AddSTATUS_CODE(builder, STATUS_CODE):
    PIVResponseAddSTATUS_CODE(builder, STATUS_CODE)

def PIVResponseAddSTATUS(builder, STATUS):
    builder.PrependInt32Slot(1, STATUS, 0)

def AddSTATUS(builder, STATUS):
    PIVResponseAddSTATUS(builder, STATUS)

def PIVResponseAddYIELDED(builder, YIELDED):
    builder.PrependBoolSlot(2, YIELDED, 0)

def AddYIELDED(builder, YIELDED):
    PIVResponseAddYIELDED(builder, YIELDED)

def PIVResponseAddBACKLOG_REMAINING(builder, BACKLOG_REMAINING):
    builder.PrependUint32Slot(3, BACKLOG_REMAINING, 0)

def AddBACKLOG_REMAINING(builder, BACKLOG_REMAINING):
    PIVResponseAddBACKLOG_REMAINING(builder, BACKLOG_REMAINING)

def PIVResponseAddOUTPUTS(builder, OUTPUTS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(OUTPUTS), 0)

def AddOUTPUTS(builder, OUTPUTS):
    PIVResponseAddOUTPUTS(builder, OUTPUTS)

def PIVResponseStartOUTPUTSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartOUTPUTSVector(builder, numElems):
    return PIVResponseStartOUTPUTSVector(builder, numElems)

def PIVResponseCreateOUTPUTSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateOUTPUTSVector(builder, data):
    PIVResponseCreateOUTPUTSVector(builder, data)

def PIVResponseAddPAYLOAD_ARENA(builder, PAYLOAD_ARENA):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD_ARENA), 0)

def AddPAYLOAD_ARENA(builder, PAYLOAD_ARENA):
    PIVResponseAddPAYLOAD_ARENA(builder, PAYLOAD_ARENA)

def PIVResponseStartPAYLOAD_ARENAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPAYLOAD_ARENAVector(builder, numElems):
    return PIVResponseStartPAYLOAD_ARENAVector(builder, numElems)

def PIVResponseCreatePAYLOAD_ARENAVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePAYLOAD_ARENAVector(builder, data):
    PIVResponseCreatePAYLOAD_ARENAVector(builder, data)

def PIVResponseAddERROR_CODE(builder, ERROR_CODE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ERROR_CODE), 0)

def AddERROR_CODE(builder, ERROR_CODE):
    PIVResponseAddERROR_CODE(builder, ERROR_CODE)

def PIVResponseAddERROR_MESSAGE(builder, ERROR_MESSAGE):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(ERROR_MESSAGE), 0)

def AddERROR_MESSAGE(builder, ERROR_MESSAGE):
    PIVResponseAddERROR_MESSAGE(builder, ERROR_MESSAGE)

def PIVResponseAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUint64Slot(8, TRACE_ID, 0)

def AddTRACE_ID(builder, TRACE_ID):
    PIVResponseAddTRACE_ID(builder, TRACE_ID)

def PIVResponseEnd(builder):
    return builder.EndObject()

def End(builder):
    return PIVResponseEnd(builder)

import TAB
try:
    from typing import List
except:
    pass

class PIVResponseT(object):

    # PIVResponseT
    def __init__(
        self,
        STATUS_CODE = 0,
        STATUS = 0,
        YIELDED = False,
        BACKLOG_REMAINING = 0,
        OUTPUTS = None,
        PAYLOAD_ARENA = None,
        ERROR_CODE = None,
        ERROR_MESSAGE = None,
        TRACE_ID = 0,
    ):
        self.STATUS_CODE = STATUS_CODE  # type: int
        self.STATUS = STATUS  # type: int
        self.YIELDED = YIELDED  # type: bool
        self.BACKLOG_REMAINING = BACKLOG_REMAINING  # type: int
        self.OUTPUTS = OUTPUTS  # type: Optional[List[TAB.TABT]]
        self.PAYLOAD_ARENA = PAYLOAD_ARENA  # type: Optional[List[int]]
        self.ERROR_CODE = ERROR_CODE  # type: Optional[str]
        self.ERROR_MESSAGE = ERROR_MESSAGE  # type: Optional[str]
        self.TRACE_ID = TRACE_ID  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPivresponse = PIVResponse()
        tmpPivresponse.Init(buf, pos)
        return cls.InitFromObj(tmpPivresponse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPivresponse):
        x = PIVResponseT()
        x._UnPack(tmpPivresponse)
        return x

    # PIVResponseT
    def _UnPack(self, PIVResponse):
        if PIVResponse is None:
            return
        self.STATUS_CODE = PIVResponse.STATUS_CODE()
        self.STATUS = PIVResponse.STATUS()
        self.YIELDED = PIVResponse.YIELDED()
        self.BACKLOG_REMAINING = PIVResponse.BACKLOG_REMAINING()
        if not PIVResponse.OUTPUTSIsNone():
            self.OUTPUTS = []
            for i in range(PIVResponse.OUTPUTSLength()):
                if PIVResponse.OUTPUTS(i) is None:
                    self.OUTPUTS.append(None)
                else:
                    tAB_ = TAB.TABT.InitFromObj(PIVResponse.OUTPUTS(i))
                    self.OUTPUTS.append(tAB_)
        if not PIVResponse.PAYLOAD_ARENAIsNone():
            if np is None:
                self.PAYLOAD_ARENA = []
                for i in range(PIVResponse.PAYLOAD_ARENALength()):
                    self.PAYLOAD_ARENA.append(PIVResponse.PAYLOAD_ARENA(i))
            else:
                self.PAYLOAD_ARENA = PIVResponse.PAYLOAD_ARENAAsNumpy()
        self.ERROR_CODE = PIVResponse.ERROR_CODE()
        self.ERROR_MESSAGE = PIVResponse.ERROR_MESSAGE()
        self.TRACE_ID = PIVResponse.TRACE_ID()

    # PIVResponseT
    def Pack(self, builder):
        if self.OUTPUTS is not None:
            OUTPUTSlist = []
            for i in range(len(self.OUTPUTS)):
                OUTPUTSlist.append(self.OUTPUTS[i].Pack(builder))
            PIVResponseStartOUTPUTSVector(builder, len(self.OUTPUTS))
            for i in reversed(range(len(self.OUTPUTS))):
                builder.PrependUOffsetTRelative(OUTPUTSlist[i])
            OUTPUTS = builder.EndVector()
        if self.PAYLOAD_ARENA is not None:
            if np is not None and type(self.PAYLOAD_ARENA) is np.ndarray:
                PAYLOAD_ARENA = builder.CreateNumpyVector(self.PAYLOAD_ARENA)
            else:
                PIVResponseStartPAYLOAD_ARENAVector(builder, len(self.PAYLOAD_ARENA))
                for i in reversed(range(len(self.PAYLOAD_ARENA))):
                    builder.PrependUint8(self.PAYLOAD_ARENA[i])
                PAYLOAD_ARENA = builder.EndVector()
        if self.ERROR_CODE is not None:
            ERROR_CODE = builder.CreateString(self.ERROR_CODE)
        if self.ERROR_MESSAGE is not None:
            ERROR_MESSAGE = builder.CreateString(self.ERROR_MESSAGE)
        PIVResponseStart(builder)
        PIVResponseAddSTATUS_CODE(builder, self.STATUS_CODE)
        PIVResponseAddSTATUS(builder, self.STATUS)
        PIVResponseAddYIELDED(builder, self.YIELDED)
        PIVResponseAddBACKLOG_REMAINING(builder, self.BACKLOG_REMAINING)
        if self.OUTPUTS is not None:
            PIVResponseAddOUTPUTS(builder, OUTPUTS)
        if self.PAYLOAD_ARENA is not None:
            PIVResponseAddPAYLOAD_ARENA(builder, PAYLOAD_ARENA)
        if self.ERROR_CODE is not None:
            PIVResponseAddERROR_CODE(builder, ERROR_CODE)
        if self.ERROR_MESSAGE is not None:
            PIVResponseAddERROR_MESSAGE(builder, ERROR_MESSAGE)
        PIVResponseAddTRACE_ID(builder, self.TRACE_ID)
        PIVResponse = PIVResponseEnd(builder)
        return PIVResponse
