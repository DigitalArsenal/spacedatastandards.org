# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Publisher Key-Broker Descriptor
class PKB(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PKB()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPKB(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PKBBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4B\x42", size_prefixed=size_prefixed)

    # PKB
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable broker identity, referenced by `$PLG`/`$STO`/`$PUR`/`$LGR`
    # PKB
    def BROKER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # IPFS CID of the publisher `$EPM` that operates this broker
    # PKB
    def PUBLISHER_EPM_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # libp2p peer id of the publisher node hosting the broker
    # PKB
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # libp2p protocol the broker answers on
    # (e.g. "/space-data-network/module-delivery/1.0.0")
    # PKB
    def PROTOCOL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Dialable multiformat addresses for the broker
    # PKB
    def MULTIFORMAT_ADDRESSES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # PKB
    def MULTIFORMAT_ADDRESSESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PKB
    def MULTIFORMAT_ADDRESSESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # The broker's public key (from the key server's get_public_key) so the
    # buyer can pin it
    # PKB
    def BROKER_PUBLIC_KEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # PKB
    def BROKER_PUBLIC_KEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # PKB
    def BROKER_PUBLIC_KEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PKB
    def BROKER_PUBLIC_KEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Custody model (default: full publisher custody)
    # PKB
    def BROKER_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Store-maintained liveness status
    # PKB
    def LIVENESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Unix ms of the last successful probe/heartbeat
    # PKB
    def LAST_HEARTBEAT_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Expected heartbeat cadence in ms
    # PKB
    def HEARTBEAT_INTERVAL_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix ms the store first saw this broker
    # PKB
    def FIRST_SEEN_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix ms this descriptor was last updated
    # PKB
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Ed25519 signature by the publisher `$EPM` over the self-attested endpoint
    # fields; re-verified by the store's live probe
    # PKB
    def SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # PKB
    def SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # PKB
    def SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PKB
    def SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

def PKBStart(builder):
    builder.StartObject(13)

def Start(builder):
    PKBStart(builder)

def PKBAddBROKER_ID(builder, BROKER_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(BROKER_ID), 0)

def AddBROKER_ID(builder, BROKER_ID):
    PKBAddBROKER_ID(builder, BROKER_ID)

def PKBAddPUBLISHER_EPM_CID(builder, PUBLISHER_EPM_CID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PUBLISHER_EPM_CID), 0)

def AddPUBLISHER_EPM_CID(builder, PUBLISHER_EPM_CID):
    PKBAddPUBLISHER_EPM_CID(builder, PUBLISHER_EPM_CID)

def PKBAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    PKBAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def PKBAddPROTOCOL_ID(builder, PROTOCOL_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(PROTOCOL_ID), 0)

def AddPROTOCOL_ID(builder, PROTOCOL_ID):
    PKBAddPROTOCOL_ID(builder, PROTOCOL_ID)

def PKBAddMULTIFORMAT_ADDRESSES(builder, MULTIFORMAT_ADDRESSES):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(MULTIFORMAT_ADDRESSES), 0)

def AddMULTIFORMAT_ADDRESSES(builder, MULTIFORMAT_ADDRESSES):
    PKBAddMULTIFORMAT_ADDRESSES(builder, MULTIFORMAT_ADDRESSES)

def PKBStartMULTIFORMAT_ADDRESSESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartMULTIFORMAT_ADDRESSESVector(builder, numElems):
    return PKBStartMULTIFORMAT_ADDRESSESVector(builder, numElems)

def PKBCreateMULTIFORMAT_ADDRESSESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateMULTIFORMAT_ADDRESSESVector(builder, data):
    PKBCreateMULTIFORMAT_ADDRESSESVector(builder, data)

def PKBAddBROKER_PUBLIC_KEY(builder, BROKER_PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(BROKER_PUBLIC_KEY), 0)

def AddBROKER_PUBLIC_KEY(builder, BROKER_PUBLIC_KEY):
    PKBAddBROKER_PUBLIC_KEY(builder, BROKER_PUBLIC_KEY)

def PKBStartBROKER_PUBLIC_KEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartBROKER_PUBLIC_KEYVector(builder, numElems):
    return PKBStartBROKER_PUBLIC_KEYVector(builder, numElems)

def PKBCreateBROKER_PUBLIC_KEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateBROKER_PUBLIC_KEYVector(builder, data):
    PKBCreateBROKER_PUBLIC_KEYVector(builder, data)

def PKBAddBROKER_KIND(builder, BROKER_KIND):
    builder.PrependInt8Slot(6, BROKER_KIND, 0)

def AddBROKER_KIND(builder, BROKER_KIND):
    PKBAddBROKER_KIND(builder, BROKER_KIND)

def PKBAddLIVENESS(builder, LIVENESS):
    builder.PrependInt8Slot(7, LIVENESS, 0)

def AddLIVENESS(builder, LIVENESS):
    PKBAddLIVENESS(builder, LIVENESS)

def PKBAddLAST_HEARTBEAT_AT(builder, LAST_HEARTBEAT_AT):
    builder.PrependUint64Slot(8, LAST_HEARTBEAT_AT, 0)

def AddLAST_HEARTBEAT_AT(builder, LAST_HEARTBEAT_AT):
    PKBAddLAST_HEARTBEAT_AT(builder, LAST_HEARTBEAT_AT)

def PKBAddHEARTBEAT_INTERVAL_MS(builder, HEARTBEAT_INTERVAL_MS):
    builder.PrependUint64Slot(9, HEARTBEAT_INTERVAL_MS, 0)

def AddHEARTBEAT_INTERVAL_MS(builder, HEARTBEAT_INTERVAL_MS):
    PKBAddHEARTBEAT_INTERVAL_MS(builder, HEARTBEAT_INTERVAL_MS)

def PKBAddFIRST_SEEN_AT(builder, FIRST_SEEN_AT):
    builder.PrependUint64Slot(10, FIRST_SEEN_AT, 0)

def AddFIRST_SEEN_AT(builder, FIRST_SEEN_AT):
    PKBAddFIRST_SEEN_AT(builder, FIRST_SEEN_AT)

def PKBAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUint64Slot(11, UPDATED_AT, 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    PKBAddUPDATED_AT(builder, UPDATED_AT)

def PKBAddSIGNATURE(builder, SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE), 0)

def AddSIGNATURE(builder, SIGNATURE):
    PKBAddSIGNATURE(builder, SIGNATURE)

def PKBStartSIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSIGNATUREVector(builder, numElems):
    return PKBStartSIGNATUREVector(builder, numElems)

def PKBCreateSIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSIGNATUREVector(builder, data):
    PKBCreateSIGNATUREVector(builder, data)

def PKBEnd(builder):
    return builder.EndObject()

def End(builder):
    return PKBEnd(builder)

try:
    from typing import List
except:
    pass

class PKBT(object):

    # PKBT
    def __init__(
        self,
        BROKER_ID = None,
        PUBLISHER_EPM_CID = None,
        PROVIDER_PEER_ID = None,
        PROTOCOL_ID = None,
        MULTIFORMAT_ADDRESSES = None,
        BROKER_PUBLIC_KEY = None,
        BROKER_KIND = 0,
        LIVENESS = 0,
        LAST_HEARTBEAT_AT = 0,
        HEARTBEAT_INTERVAL_MS = 0,
        FIRST_SEEN_AT = 0,
        UPDATED_AT = 0,
        SIGNATURE = None,
    ):
        self.BROKER_ID = BROKER_ID  # type: Optional[str]
        self.PUBLISHER_EPM_CID = PUBLISHER_EPM_CID  # type: Optional[str]
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.PROTOCOL_ID = PROTOCOL_ID  # type: Optional[str]
        self.MULTIFORMAT_ADDRESSES = MULTIFORMAT_ADDRESSES  # type: Optional[List[Optional[str]]]
        self.BROKER_PUBLIC_KEY = BROKER_PUBLIC_KEY  # type: Optional[List[int]]
        self.BROKER_KIND = BROKER_KIND  # type: int
        self.LIVENESS = LIVENESS  # type: int
        self.LAST_HEARTBEAT_AT = LAST_HEARTBEAT_AT  # type: int
        self.HEARTBEAT_INTERVAL_MS = HEARTBEAT_INTERVAL_MS  # type: int
        self.FIRST_SEEN_AT = FIRST_SEEN_AT  # type: int
        self.UPDATED_AT = UPDATED_AT  # type: int
        self.SIGNATURE = SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPkb = PKB()
        tmpPkb.Init(buf, pos)
        return cls.InitFromObj(tmpPkb)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPkb):
        x = PKBT()
        x._UnPack(tmpPkb)
        return x

    # PKBT
    def _UnPack(self, PKB):
        if PKB is None:
            return
        self.BROKER_ID = PKB.BROKER_ID()
        self.PUBLISHER_EPM_CID = PKB.PUBLISHER_EPM_CID()
        self.PROVIDER_PEER_ID = PKB.PROVIDER_PEER_ID()
        self.PROTOCOL_ID = PKB.PROTOCOL_ID()
        if not PKB.MULTIFORMAT_ADDRESSESIsNone():
            self.MULTIFORMAT_ADDRESSES = []
            for i in range(PKB.MULTIFORMAT_ADDRESSESLength()):
                self.MULTIFORMAT_ADDRESSES.append(PKB.MULTIFORMAT_ADDRESSES(i))
        if not PKB.BROKER_PUBLIC_KEYIsNone():
            if np is None:
                self.BROKER_PUBLIC_KEY = []
                for i in range(PKB.BROKER_PUBLIC_KEYLength()):
                    self.BROKER_PUBLIC_KEY.append(PKB.BROKER_PUBLIC_KEY(i))
            else:
                self.BROKER_PUBLIC_KEY = PKB.BROKER_PUBLIC_KEYAsNumpy()
        self.BROKER_KIND = PKB.BROKER_KIND()
        self.LIVENESS = PKB.LIVENESS()
        self.LAST_HEARTBEAT_AT = PKB.LAST_HEARTBEAT_AT()
        self.HEARTBEAT_INTERVAL_MS = PKB.HEARTBEAT_INTERVAL_MS()
        self.FIRST_SEEN_AT = PKB.FIRST_SEEN_AT()
        self.UPDATED_AT = PKB.UPDATED_AT()
        if not PKB.SIGNATUREIsNone():
            if np is None:
                self.SIGNATURE = []
                for i in range(PKB.SIGNATURELength()):
                    self.SIGNATURE.append(PKB.SIGNATURE(i))
            else:
                self.SIGNATURE = PKB.SIGNATUREAsNumpy()

    # PKBT
    def Pack(self, builder):
        if self.BROKER_ID is not None:
            BROKER_ID = builder.CreateString(self.BROKER_ID)
        if self.PUBLISHER_EPM_CID is not None:
            PUBLISHER_EPM_CID = builder.CreateString(self.PUBLISHER_EPM_CID)
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.PROTOCOL_ID is not None:
            PROTOCOL_ID = builder.CreateString(self.PROTOCOL_ID)
        if self.MULTIFORMAT_ADDRESSES is not None:
            MULTIFORMAT_ADDRESSESlist = []
            for i in range(len(self.MULTIFORMAT_ADDRESSES)):
                MULTIFORMAT_ADDRESSESlist.append(builder.CreateString(self.MULTIFORMAT_ADDRESSES[i]))
            PKBStartMULTIFORMAT_ADDRESSESVector(builder, len(self.MULTIFORMAT_ADDRESSES))
            for i in reversed(range(len(self.MULTIFORMAT_ADDRESSES))):
                builder.PrependUOffsetTRelative(MULTIFORMAT_ADDRESSESlist[i])
            MULTIFORMAT_ADDRESSES = builder.EndVector()
        if self.BROKER_PUBLIC_KEY is not None:
            if np is not None and type(self.BROKER_PUBLIC_KEY) is np.ndarray:
                BROKER_PUBLIC_KEY = builder.CreateNumpyVector(self.BROKER_PUBLIC_KEY)
            else:
                PKBStartBROKER_PUBLIC_KEYVector(builder, len(self.BROKER_PUBLIC_KEY))
                for i in reversed(range(len(self.BROKER_PUBLIC_KEY))):
                    builder.PrependUint8(self.BROKER_PUBLIC_KEY[i])
                BROKER_PUBLIC_KEY = builder.EndVector()
        if self.SIGNATURE is not None:
            if np is not None and type(self.SIGNATURE) is np.ndarray:
                SIGNATURE = builder.CreateNumpyVector(self.SIGNATURE)
            else:
                PKBStartSIGNATUREVector(builder, len(self.SIGNATURE))
                for i in reversed(range(len(self.SIGNATURE))):
                    builder.PrependUint8(self.SIGNATURE[i])
                SIGNATURE = builder.EndVector()
        PKBStart(builder)
        if self.BROKER_ID is not None:
            PKBAddBROKER_ID(builder, BROKER_ID)
        if self.PUBLISHER_EPM_CID is not None:
            PKBAddPUBLISHER_EPM_CID(builder, PUBLISHER_EPM_CID)
        if self.PROVIDER_PEER_ID is not None:
            PKBAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.PROTOCOL_ID is not None:
            PKBAddPROTOCOL_ID(builder, PROTOCOL_ID)
        if self.MULTIFORMAT_ADDRESSES is not None:
            PKBAddMULTIFORMAT_ADDRESSES(builder, MULTIFORMAT_ADDRESSES)
        if self.BROKER_PUBLIC_KEY is not None:
            PKBAddBROKER_PUBLIC_KEY(builder, BROKER_PUBLIC_KEY)
        PKBAddBROKER_KIND(builder, self.BROKER_KIND)
        PKBAddLIVENESS(builder, self.LIVENESS)
        PKBAddLAST_HEARTBEAT_AT(builder, self.LAST_HEARTBEAT_AT)
        PKBAddHEARTBEAT_INTERVAL_MS(builder, self.HEARTBEAT_INTERVAL_MS)
        PKBAddFIRST_SEEN_AT(builder, self.FIRST_SEEN_AT)
        PKBAddUPDATED_AT(builder, self.UPDATED_AT)
        if self.SIGNATURE is not None:
            PKBAddSIGNATURE(builder, SIGNATURE)
        PKB = PKBEnd(builder)
        return PKB
