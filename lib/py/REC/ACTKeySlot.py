# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One purpose-key slot of an operator.
class ACTKeySlot(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ACTKeySlot()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsACTKeySlot(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ACTKeySlotBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x43\x54", size_prefixed=size_prefixed)

    # ACTKeySlot
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Slot name, e.g. "signing", "encryption", "publication".
    # ACTKeySlot
    def SLOT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Derivation path currently in use.
    # ACTKeySlot
    def KEY_PATH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Derivation path the next rotation will use.
    # ACTKeySlot
    def NEXT_PATH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ACTKeySlot
    def ROTATABLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Key algorithm, e.g. "secp256k1", "ed25519", "x25519".
    # ACTKeySlot
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def ACTKeySlotStart(builder):
    builder.StartObject(5)

def Start(builder):
    ACTKeySlotStart(builder)

def ACTKeySlotAddSLOT(builder, SLOT):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SLOT), 0)

def AddSLOT(builder, SLOT):
    ACTKeySlotAddSLOT(builder, SLOT)

def ACTKeySlotAddKEY_PATH(builder, KEY_PATH):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_PATH), 0)

def AddKEY_PATH(builder, KEY_PATH):
    ACTKeySlotAddKEY_PATH(builder, KEY_PATH)

def ACTKeySlotAddNEXT_PATH(builder, NEXT_PATH):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(NEXT_PATH), 0)

def AddNEXT_PATH(builder, NEXT_PATH):
    ACTKeySlotAddNEXT_PATH(builder, NEXT_PATH)

def ACTKeySlotAddROTATABLE(builder, ROTATABLE):
    builder.PrependBoolSlot(3, ROTATABLE, 0)

def AddROTATABLE(builder, ROTATABLE):
    ACTKeySlotAddROTATABLE(builder, ROTATABLE)

def ACTKeySlotAddALGORITHM(builder, ALGORITHM):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ALGORITHM), 0)

def AddALGORITHM(builder, ALGORITHM):
    ACTKeySlotAddALGORITHM(builder, ALGORITHM)

def ACTKeySlotEnd(builder):
    return builder.EndObject()

def End(builder):
    return ACTKeySlotEnd(builder)


class ACTKeySlotT(object):

    # ACTKeySlotT
    def __init__(
        self,
        SLOT = None,
        KEY_PATH = None,
        NEXT_PATH = None,
        ROTATABLE = False,
        ALGORITHM = None,
    ):
        self.SLOT = SLOT  # type: Optional[str]
        self.KEY_PATH = KEY_PATH  # type: Optional[str]
        self.NEXT_PATH = NEXT_PATH  # type: Optional[str]
        self.ROTATABLE = ROTATABLE  # type: bool
        self.ALGORITHM = ALGORITHM  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpActkeySlot = ACTKeySlot()
        tmpActkeySlot.Init(buf, pos)
        return cls.InitFromObj(tmpActkeySlot)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpActkeySlot):
        x = ACTKeySlotT()
        x._UnPack(tmpActkeySlot)
        return x

    # ACTKeySlotT
    def _UnPack(self, ACTKeySlot):
        if ACTKeySlot is None:
            return
        self.SLOT = ACTKeySlot.SLOT()
        self.KEY_PATH = ACTKeySlot.KEY_PATH()
        self.NEXT_PATH = ACTKeySlot.NEXT_PATH()
        self.ROTATABLE = ACTKeySlot.ROTATABLE()
        self.ALGORITHM = ACTKeySlot.ALGORITHM()

    # ACTKeySlotT
    def Pack(self, builder):
        if self.SLOT is not None:
            SLOT = builder.CreateString(self.SLOT)
        if self.KEY_PATH is not None:
            KEY_PATH = builder.CreateString(self.KEY_PATH)
        if self.NEXT_PATH is not None:
            NEXT_PATH = builder.CreateString(self.NEXT_PATH)
        if self.ALGORITHM is not None:
            ALGORITHM = builder.CreateString(self.ALGORITHM)
        ACTKeySlotStart(builder)
        if self.SLOT is not None:
            ACTKeySlotAddSLOT(builder, SLOT)
        if self.KEY_PATH is not None:
            ACTKeySlotAddKEY_PATH(builder, KEY_PATH)
        if self.NEXT_PATH is not None:
            ACTKeySlotAddNEXT_PATH(builder, NEXT_PATH)
        ACTKeySlotAddROTATABLE(builder, self.ROTATABLE)
        if self.ALGORITHM is not None:
            ACTKeySlotAddALGORITHM(builder, ALGORITHM)
        ACTKeySlot = ACTKeySlotEnd(builder)
        return ACTKeySlot
