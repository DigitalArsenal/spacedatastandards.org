# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TRHAntenna(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRHAntenna()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRHAntenna(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRHAntennaBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x48", size_prefixed=size_prefixed)

    # TRHAntenna
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TRHAntenna
    def HARDWARE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TRHAntenna
    def FREQUENCY_HZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TRHAntenna
    def BANDWIDTH_HZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TRHAntenna
    def GAIN_DBI(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TRHAntenna
    def PHASE_CENTER_DELAY_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def TRHAntennaStart(builder):
    builder.StartObject(5)

def Start(builder):
    TRHAntennaStart(builder)

def TRHAntennaAddHARDWARE_ID(builder, HARDWARE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(HARDWARE_ID), 0)

def AddHARDWARE_ID(builder, HARDWARE_ID):
    TRHAntennaAddHARDWARE_ID(builder, HARDWARE_ID)

def TRHAntennaAddFREQUENCY_HZ(builder, FREQUENCY_HZ):
    builder.PrependFloat64Slot(1, FREQUENCY_HZ, 0.0)

def AddFREQUENCY_HZ(builder, FREQUENCY_HZ):
    TRHAntennaAddFREQUENCY_HZ(builder, FREQUENCY_HZ)

def TRHAntennaAddBANDWIDTH_HZ(builder, BANDWIDTH_HZ):
    builder.PrependFloat64Slot(2, BANDWIDTH_HZ, 0.0)

def AddBANDWIDTH_HZ(builder, BANDWIDTH_HZ):
    TRHAntennaAddBANDWIDTH_HZ(builder, BANDWIDTH_HZ)

def TRHAntennaAddGAIN_DBI(builder, GAIN_DBI):
    builder.PrependFloat64Slot(3, GAIN_DBI, 0.0)

def AddGAIN_DBI(builder, GAIN_DBI):
    TRHAntennaAddGAIN_DBI(builder, GAIN_DBI)

def TRHAntennaAddPHASE_CENTER_DELAY_SECONDS(builder, PHASE_CENTER_DELAY_SECONDS):
    builder.PrependFloat64Slot(4, PHASE_CENTER_DELAY_SECONDS, 0.0)

def AddPHASE_CENTER_DELAY_SECONDS(builder, PHASE_CENTER_DELAY_SECONDS):
    TRHAntennaAddPHASE_CENTER_DELAY_SECONDS(builder, PHASE_CENTER_DELAY_SECONDS)

def TRHAntennaEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRHAntennaEnd(builder)


class TRHAntennaT(object):

    # TRHAntennaT
    def __init__(
        self,
        HARDWARE_ID = None,
        FREQUENCY_HZ = 0.0,
        BANDWIDTH_HZ = 0.0,
        GAIN_DBI = 0.0,
        PHASE_CENTER_DELAY_SECONDS = 0.0,
    ):
        self.HARDWARE_ID = HARDWARE_ID  # type: Optional[str]
        self.FREQUENCY_HZ = FREQUENCY_HZ  # type: float
        self.BANDWIDTH_HZ = BANDWIDTH_HZ  # type: float
        self.GAIN_DBI = GAIN_DBI  # type: float
        self.PHASE_CENTER_DELAY_SECONDS = PHASE_CENTER_DELAY_SECONDS  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrhantenna = TRHAntenna()
        tmpTrhantenna.Init(buf, pos)
        return cls.InitFromObj(tmpTrhantenna)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrhantenna):
        x = TRHAntennaT()
        x._UnPack(tmpTrhantenna)
        return x

    # TRHAntennaT
    def _UnPack(self, TRHAntenna):
        if TRHAntenna is None:
            return
        self.HARDWARE_ID = TRHAntenna.HARDWARE_ID()
        self.FREQUENCY_HZ = TRHAntenna.FREQUENCY_HZ()
        self.BANDWIDTH_HZ = TRHAntenna.BANDWIDTH_HZ()
        self.GAIN_DBI = TRHAntenna.GAIN_DBI()
        self.PHASE_CENTER_DELAY_SECONDS = TRHAntenna.PHASE_CENTER_DELAY_SECONDS()

    # TRHAntennaT
    def Pack(self, builder):
        if self.HARDWARE_ID is not None:
            HARDWARE_ID = builder.CreateString(self.HARDWARE_ID)
        TRHAntennaStart(builder)
        if self.HARDWARE_ID is not None:
            TRHAntennaAddHARDWARE_ID(builder, HARDWARE_ID)
        TRHAntennaAddFREQUENCY_HZ(builder, self.FREQUENCY_HZ)
        TRHAntennaAddBANDWIDTH_HZ(builder, self.BANDWIDTH_HZ)
        TRHAntennaAddGAIN_DBI(builder, self.GAIN_DBI)
        TRHAntennaAddPHASE_CENTER_DELAY_SECONDS(builder, self.PHASE_CENTER_DELAY_SECONDS)
        TRHAntenna = TRHAntennaEnd(builder)
        return TRHAntenna
