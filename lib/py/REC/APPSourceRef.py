# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One upstream data source the app depends on.
class APPSourceRef(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = APPSourceRef()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAPPSourceRef(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def APPSourceRefBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x50\x50", size_prefixed=size_prefixed)

    # APPSourceRef
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # App-local stable reference for this source. Required, unique within
    # the manifest.
    # APPSourceRef
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Classifies REF.
    # APPSourceRef
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Source identifier: an APPModuleRef.ID when KIND is MODULE, otherwise a
    # URL or dataset identifier. Required.
    # APPSourceRef
    def REF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable summary.
    # APPSourceRef
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def APPSourceRefStart(builder):
    builder.StartObject(4)

def Start(builder):
    APPSourceRefStart(builder)

def APPSourceRefAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    APPSourceRefAddID(builder, ID)

def APPSourceRefAddKIND(builder, KIND):
    builder.PrependUint8Slot(1, KIND, 0)

def AddKIND(builder, KIND):
    APPSourceRefAddKIND(builder, KIND)

def APPSourceRefAddREF(builder, REF):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(REF), 0)

def AddREF(builder, REF):
    APPSourceRefAddREF(builder, REF)

def APPSourceRefAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    APPSourceRefAddDESCRIPTION(builder, DESCRIPTION)

def APPSourceRefEnd(builder):
    return builder.EndObject()

def End(builder):
    return APPSourceRefEnd(builder)


class APPSourceRefT(object):

    # APPSourceRefT
    def __init__(
        self,
        ID = None,
        KIND = 0,
        REF = None,
        DESCRIPTION = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.KIND = KIND  # type: int
        self.REF = REF  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAppsourceRef = APPSourceRef()
        tmpAppsourceRef.Init(buf, pos)
        return cls.InitFromObj(tmpAppsourceRef)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAppsourceRef):
        x = APPSourceRefT()
        x._UnPack(tmpAppsourceRef)
        return x

    # APPSourceRefT
    def _UnPack(self, APPSourceRef):
        if APPSourceRef is None:
            return
        self.ID = APPSourceRef.ID()
        self.KIND = APPSourceRef.KIND()
        self.REF = APPSourceRef.REF()
        self.DESCRIPTION = APPSourceRef.DESCRIPTION()

    # APPSourceRefT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.REF is not None:
            REF = builder.CreateString(self.REF)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        APPSourceRefStart(builder)
        if self.ID is not None:
            APPSourceRefAddID(builder, ID)
        APPSourceRefAddKIND(builder, self.KIND)
        if self.REF is not None:
            APPSourceRefAddREF(builder, REF)
        if self.DESCRIPTION is not None:
            APPSourceRefAddDESCRIPTION(builder, DESCRIPTION)
        APPSourceRef = APPSourceRefEnd(builder)
        return APPSourceRef
