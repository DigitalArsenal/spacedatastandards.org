# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Terrestrial Transmitter Site.
class TXS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TXS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTXS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TXSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x58\x53", size_prefixed=size_prefixed)

    # TXS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher-stable identifier for this facility, unique within its record
    # set. Never a source's own identifier and never a coordinate string.
    # TXS
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Winning source's native facility identifier, copied verbatim from the
    # SOURCES entry named by CONSENSUS.WINNING_PROVIDER_ID. Never a substitute
    # for ID and never stable across sources.
    # TXS
    def NATIVE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Facility or structure name verbatim when a source publishes one. Never
    # synthesized from coordinates, licensee, or nearest settlement.
    # TXS
    def SITE_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Station identification the facility as a whole answers to, verbatim: a
    # callsign, an assigned station identifier, or the identifier an observer
    # community records. Per-emission identity lives in
    # EMISSIONS[].STATION_IDENTITY.
    # TXS
    def STATION_IDENTITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Licensee, operator or responsible administration name verbatim as the
    # cited source states it. A display name, never an identifier: consumers
    # must not treat it as a stable key.
    # TXS
    def LICENSEE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Licence, assignment or authorization identifier verbatim.
    # TXS
    def LICENSE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Primary service class of the facility. Individual emissions may state
    # their own.
    # TXS
    def SERVICE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # TXS
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # ISO 3166-1 alpha-2 country code, uppercase, as the source states it.
    # TXS
    def COUNTRY_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Administrative subdivision or locality verbatim as the source states it.
    # TXS
    def ADMIN_AREA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Published facility position, decimal degrees on WGS 84, produced by the
    # merge described in CONSENSUS. ABSENT when no source publishes a position:
    # a region centroid is never substituted. Per-source positions before
    # reconciliation live in SOURCES[].REPORTED_LATITUDE/REPORTED_LONGITUDE and
    # are never overwritten by this value.
    # TXS
    def LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TXS
    def LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # The class of authority behind LATITUDE/LONGITUDE. A consumer that renders
    # a position without reading this field is rendering an unlabelled claim.
    # TXS
    def POSITION_AUTHORITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Horizontal envelope in metres within which the facility is asserted to
    # lie, as the publisher states it. Zero means unpublished — never that the
    # position is exact.
    # TXS
    def POSITION_UNCERTAINTY_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Credible consulted sources place the facility in materially different
    # locations, or a source states the position is contested. The published
    # coordinate remains the publisher's best merge; this flag forbids a
    # consumer from presenting it as settled.
    # TXS
    def POSITION_DISPUTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Positions the facility is asserted to have vacated, most recent first.
    # TXS
    def PRIOR_POSITIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TXSPriorPosition import TXSPriorPosition
            obj = TXSPriorPosition()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TXS
    def PRIOR_POSITIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TXS
    def PRIOR_POSITIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        return o == 0

    # Ground elevation at the facility, metres above mean sea level as the
    # source states it. Zero means unpublished.
    # TXS
    def GROUND_ELEVATION_AMSL_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Overall height of the antenna-supporting structure above ground level,
    # metres. Distinct from any emission's antenna height. Zero means
    # unpublished.
    # TXS
    def STRUCTURE_HEIGHT_AGL_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TXS
    def STRUCTURE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Number of antenna-supporting structures at the facility. Zero means
    # unpublished.
    # TXS
    def STRUCTURE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Every emission the facility is documented to radiate.
    # TXS
    def EMISSIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TXSEmission import TXSEmission
            obj = TXSEmission()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TXS
    def EMISSIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TXS
    def EMISSIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        return o == 0

    # RFC 3339 UTC fixed-millisecond bounds of the documentation or observation
    # window backing the published record.
    # TXS
    def FIRST_OBSERVED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TXS
    def LAST_OBSERVED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Every source consulted for this facility, including sources that returned
    # nothing. Required: a facility with no named sources is not publishable.
    # TXS
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TXSProvenance import TXSProvenance
            obj = TXSProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TXS
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TXS
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        return o == 0

    # How the entries in SOURCES were reduced to the published position.
    # Required: a published position always states the rule that produced it,
    # including SINGLE_SOURCE.
    # TXS
    def CONSENSUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TXSConsensus import TXSConsensus
            obj = TXSConsensus()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def TXSStart(builder):
    builder.StartObject(25)

def Start(builder):
    TXSStart(builder)

def TXSAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    TXSAddID(builder, ID)

def TXSAddNATIVE_ID(builder, NATIVE_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NATIVE_ID), 0)

def AddNATIVE_ID(builder, NATIVE_ID):
    TXSAddNATIVE_ID(builder, NATIVE_ID)

def TXSAddSITE_NAME(builder, SITE_NAME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SITE_NAME), 0)

def AddSITE_NAME(builder, SITE_NAME):
    TXSAddSITE_NAME(builder, SITE_NAME)

def TXSAddSTATION_IDENTITY(builder, STATION_IDENTITY):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(STATION_IDENTITY), 0)

def AddSTATION_IDENTITY(builder, STATION_IDENTITY):
    TXSAddSTATION_IDENTITY(builder, STATION_IDENTITY)

def TXSAddLICENSEE(builder, LICENSEE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSEE), 0)

def AddLICENSEE(builder, LICENSEE):
    TXSAddLICENSEE(builder, LICENSEE)

def TXSAddLICENSE_ID(builder, LICENSE_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE_ID), 0)

def AddLICENSE_ID(builder, LICENSE_ID):
    TXSAddLICENSE_ID(builder, LICENSE_ID)

def TXSAddSERVICE(builder, SERVICE):
    builder.PrependInt8Slot(6, SERVICE, 0)

def AddSERVICE(builder, SERVICE):
    TXSAddSERVICE(builder, SERVICE)

def TXSAddSTATUS(builder, STATUS):
    builder.PrependInt8Slot(7, STATUS, 0)

def AddSTATUS(builder, STATUS):
    TXSAddSTATUS(builder, STATUS)

def TXSAddCOUNTRY_CODE(builder, COUNTRY_CODE):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(COUNTRY_CODE), 0)

def AddCOUNTRY_CODE(builder, COUNTRY_CODE):
    TXSAddCOUNTRY_CODE(builder, COUNTRY_CODE)

def TXSAddADMIN_AREA(builder, ADMIN_AREA):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(ADMIN_AREA), 0)

def AddADMIN_AREA(builder, ADMIN_AREA):
    TXSAddADMIN_AREA(builder, ADMIN_AREA)

def TXSAddLATITUDE(builder, LATITUDE):
    builder.PrependFloat64Slot(10, LATITUDE, 0.0)

def AddLATITUDE(builder, LATITUDE):
    TXSAddLATITUDE(builder, LATITUDE)

def TXSAddLONGITUDE(builder, LONGITUDE):
    builder.PrependFloat64Slot(11, LONGITUDE, 0.0)

def AddLONGITUDE(builder, LONGITUDE):
    TXSAddLONGITUDE(builder, LONGITUDE)

def TXSAddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY):
    builder.PrependInt8Slot(12, POSITION_AUTHORITY, 0)

def AddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY):
    TXSAddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY)

def TXSAddPOSITION_UNCERTAINTY_M(builder, POSITION_UNCERTAINTY_M):
    builder.PrependFloat64Slot(13, POSITION_UNCERTAINTY_M, 0.0)

def AddPOSITION_UNCERTAINTY_M(builder, POSITION_UNCERTAINTY_M):
    TXSAddPOSITION_UNCERTAINTY_M(builder, POSITION_UNCERTAINTY_M)

def TXSAddPOSITION_DISPUTED(builder, POSITION_DISPUTED):
    builder.PrependBoolSlot(14, POSITION_DISPUTED, 0)

def AddPOSITION_DISPUTED(builder, POSITION_DISPUTED):
    TXSAddPOSITION_DISPUTED(builder, POSITION_DISPUTED)

def TXSAddPRIOR_POSITIONS(builder, PRIOR_POSITIONS):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(PRIOR_POSITIONS), 0)

def AddPRIOR_POSITIONS(builder, PRIOR_POSITIONS):
    TXSAddPRIOR_POSITIONS(builder, PRIOR_POSITIONS)

def TXSStartPRIOR_POSITIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPRIOR_POSITIONSVector(builder, numElems):
    return TXSStartPRIOR_POSITIONSVector(builder, numElems)

def TXSCreatePRIOR_POSITIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePRIOR_POSITIONSVector(builder, data):
    TXSCreatePRIOR_POSITIONSVector(builder, data)

def TXSAddGROUND_ELEVATION_AMSL_M(builder, GROUND_ELEVATION_AMSL_M):
    builder.PrependFloat64Slot(16, GROUND_ELEVATION_AMSL_M, 0.0)

def AddGROUND_ELEVATION_AMSL_M(builder, GROUND_ELEVATION_AMSL_M):
    TXSAddGROUND_ELEVATION_AMSL_M(builder, GROUND_ELEVATION_AMSL_M)

def TXSAddSTRUCTURE_HEIGHT_AGL_M(builder, STRUCTURE_HEIGHT_AGL_M):
    builder.PrependFloat64Slot(17, STRUCTURE_HEIGHT_AGL_M, 0.0)

def AddSTRUCTURE_HEIGHT_AGL_M(builder, STRUCTURE_HEIGHT_AGL_M):
    TXSAddSTRUCTURE_HEIGHT_AGL_M(builder, STRUCTURE_HEIGHT_AGL_M)

def TXSAddSTRUCTURE(builder, STRUCTURE):
    builder.PrependInt8Slot(18, STRUCTURE, 0)

def AddSTRUCTURE(builder, STRUCTURE):
    TXSAddSTRUCTURE(builder, STRUCTURE)

def TXSAddSTRUCTURE_COUNT(builder, STRUCTURE_COUNT):
    builder.PrependUint32Slot(19, STRUCTURE_COUNT, 0)

def AddSTRUCTURE_COUNT(builder, STRUCTURE_COUNT):
    TXSAddSTRUCTURE_COUNT(builder, STRUCTURE_COUNT)

def TXSAddEMISSIONS(builder, EMISSIONS):
    builder.PrependUOffsetTRelativeSlot(20, flatbuffers.number_types.UOffsetTFlags.py_type(EMISSIONS), 0)

def AddEMISSIONS(builder, EMISSIONS):
    TXSAddEMISSIONS(builder, EMISSIONS)

def TXSStartEMISSIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartEMISSIONSVector(builder, numElems):
    return TXSStartEMISSIONSVector(builder, numElems)

def TXSCreateEMISSIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateEMISSIONSVector(builder, data):
    TXSCreateEMISSIONSVector(builder, data)

def TXSAddFIRST_OBSERVED(builder, FIRST_OBSERVED):
    builder.PrependUOffsetTRelativeSlot(21, flatbuffers.number_types.UOffsetTFlags.py_type(FIRST_OBSERVED), 0)

def AddFIRST_OBSERVED(builder, FIRST_OBSERVED):
    TXSAddFIRST_OBSERVED(builder, FIRST_OBSERVED)

def TXSAddLAST_OBSERVED(builder, LAST_OBSERVED):
    builder.PrependUOffsetTRelativeSlot(22, flatbuffers.number_types.UOffsetTFlags.py_type(LAST_OBSERVED), 0)

def AddLAST_OBSERVED(builder, LAST_OBSERVED):
    TXSAddLAST_OBSERVED(builder, LAST_OBSERVED)

def TXSAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(23, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    TXSAddSOURCES(builder, SOURCES)

def TXSStartSOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCESVector(builder, numElems):
    return TXSStartSOURCESVector(builder, numElems)

def TXSCreateSOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCESVector(builder, data):
    TXSCreateSOURCESVector(builder, data)

def TXSAddCONSENSUS(builder, CONSENSUS):
    builder.PrependUOffsetTRelativeSlot(24, flatbuffers.number_types.UOffsetTFlags.py_type(CONSENSUS), 0)

def AddCONSENSUS(builder, CONSENSUS):
    TXSAddCONSENSUS(builder, CONSENSUS)

def TXSEnd(builder):
    return builder.EndObject()

def End(builder):
    return TXSEnd(builder)

import TXSConsensus
import TXSEmission
import TXSPriorPosition
import TXSProvenance
try:
    from typing import List, Optional
except:
    pass

class TXST(object):

    # TXST
    def __init__(
        self,
        ID = None,
        NATIVE_ID = None,
        SITE_NAME = None,
        STATION_IDENTITY = None,
        LICENSEE = None,
        LICENSE_ID = None,
        SERVICE = 0,
        STATUS = 0,
        COUNTRY_CODE = None,
        ADMIN_AREA = None,
        LATITUDE = 0.0,
        LONGITUDE = 0.0,
        POSITION_AUTHORITY = 0,
        POSITION_UNCERTAINTY_M = 0.0,
        POSITION_DISPUTED = False,
        PRIOR_POSITIONS = None,
        GROUND_ELEVATION_AMSL_M = 0.0,
        STRUCTURE_HEIGHT_AGL_M = 0.0,
        STRUCTURE = 0,
        STRUCTURE_COUNT = 0,
        EMISSIONS = None,
        FIRST_OBSERVED = None,
        LAST_OBSERVED = None,
        SOURCES = None,
        CONSENSUS = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.NATIVE_ID = NATIVE_ID  # type: Optional[str]
        self.SITE_NAME = SITE_NAME  # type: Optional[str]
        self.STATION_IDENTITY = STATION_IDENTITY  # type: Optional[str]
        self.LICENSEE = LICENSEE  # type: Optional[str]
        self.LICENSE_ID = LICENSE_ID  # type: Optional[str]
        self.SERVICE = SERVICE  # type: int
        self.STATUS = STATUS  # type: int
        self.COUNTRY_CODE = COUNTRY_CODE  # type: Optional[str]
        self.ADMIN_AREA = ADMIN_AREA  # type: Optional[str]
        self.LATITUDE = LATITUDE  # type: float
        self.LONGITUDE = LONGITUDE  # type: float
        self.POSITION_AUTHORITY = POSITION_AUTHORITY  # type: int
        self.POSITION_UNCERTAINTY_M = POSITION_UNCERTAINTY_M  # type: float
        self.POSITION_DISPUTED = POSITION_DISPUTED  # type: bool
        self.PRIOR_POSITIONS = PRIOR_POSITIONS  # type: Optional[List[TXSPriorPosition.TXSPriorPositionT]]
        self.GROUND_ELEVATION_AMSL_M = GROUND_ELEVATION_AMSL_M  # type: float
        self.STRUCTURE_HEIGHT_AGL_M = STRUCTURE_HEIGHT_AGL_M  # type: float
        self.STRUCTURE = STRUCTURE  # type: int
        self.STRUCTURE_COUNT = STRUCTURE_COUNT  # type: int
        self.EMISSIONS = EMISSIONS  # type: Optional[List[TXSEmission.TXSEmissionT]]
        self.FIRST_OBSERVED = FIRST_OBSERVED  # type: Optional[str]
        self.LAST_OBSERVED = LAST_OBSERVED  # type: Optional[str]
        self.SOURCES = SOURCES  # type: Optional[List[TXSProvenance.TXSProvenanceT]]
        self.CONSENSUS = CONSENSUS  # type: Optional[TXSConsensus.TXSConsensusT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTxs = TXS()
        tmpTxs.Init(buf, pos)
        return cls.InitFromObj(tmpTxs)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTxs):
        x = TXST()
        x._UnPack(tmpTxs)
        return x

    # TXST
    def _UnPack(self, TXS):
        if TXS is None:
            return
        self.ID = TXS.ID()
        self.NATIVE_ID = TXS.NATIVE_ID()
        self.SITE_NAME = TXS.SITE_NAME()
        self.STATION_IDENTITY = TXS.STATION_IDENTITY()
        self.LICENSEE = TXS.LICENSEE()
        self.LICENSE_ID = TXS.LICENSE_ID()
        self.SERVICE = TXS.SERVICE()
        self.STATUS = TXS.STATUS()
        self.COUNTRY_CODE = TXS.COUNTRY_CODE()
        self.ADMIN_AREA = TXS.ADMIN_AREA()
        self.LATITUDE = TXS.LATITUDE()
        self.LONGITUDE = TXS.LONGITUDE()
        self.POSITION_AUTHORITY = TXS.POSITION_AUTHORITY()
        self.POSITION_UNCERTAINTY_M = TXS.POSITION_UNCERTAINTY_M()
        self.POSITION_DISPUTED = TXS.POSITION_DISPUTED()
        if not TXS.PRIOR_POSITIONSIsNone():
            self.PRIOR_POSITIONS = []
            for i in range(TXS.PRIOR_POSITIONSLength()):
                if TXS.PRIOR_POSITIONS(i) is None:
                    self.PRIOR_POSITIONS.append(None)
                else:
                    tXSPriorPosition_ = TXSPriorPosition.TXSPriorPositionT.InitFromObj(TXS.PRIOR_POSITIONS(i))
                    self.PRIOR_POSITIONS.append(tXSPriorPosition_)
        self.GROUND_ELEVATION_AMSL_M = TXS.GROUND_ELEVATION_AMSL_M()
        self.STRUCTURE_HEIGHT_AGL_M = TXS.STRUCTURE_HEIGHT_AGL_M()
        self.STRUCTURE = TXS.STRUCTURE()
        self.STRUCTURE_COUNT = TXS.STRUCTURE_COUNT()
        if not TXS.EMISSIONSIsNone():
            self.EMISSIONS = []
            for i in range(TXS.EMISSIONSLength()):
                if TXS.EMISSIONS(i) is None:
                    self.EMISSIONS.append(None)
                else:
                    tXSEmission_ = TXSEmission.TXSEmissionT.InitFromObj(TXS.EMISSIONS(i))
                    self.EMISSIONS.append(tXSEmission_)
        self.FIRST_OBSERVED = TXS.FIRST_OBSERVED()
        self.LAST_OBSERVED = TXS.LAST_OBSERVED()
        if not TXS.SOURCESIsNone():
            self.SOURCES = []
            for i in range(TXS.SOURCESLength()):
                if TXS.SOURCES(i) is None:
                    self.SOURCES.append(None)
                else:
                    tXSProvenance_ = TXSProvenance.TXSProvenanceT.InitFromObj(TXS.SOURCES(i))
                    self.SOURCES.append(tXSProvenance_)
        if TXS.CONSENSUS() is not None:
            self.CONSENSUS = TXSConsensus.TXSConsensusT.InitFromObj(TXS.CONSENSUS())

    # TXST
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.NATIVE_ID is not None:
            NATIVE_ID = builder.CreateString(self.NATIVE_ID)
        if self.SITE_NAME is not None:
            SITE_NAME = builder.CreateString(self.SITE_NAME)
        if self.STATION_IDENTITY is not None:
            STATION_IDENTITY = builder.CreateString(self.STATION_IDENTITY)
        if self.LICENSEE is not None:
            LICENSEE = builder.CreateString(self.LICENSEE)
        if self.LICENSE_ID is not None:
            LICENSE_ID = builder.CreateString(self.LICENSE_ID)
        if self.COUNTRY_CODE is not None:
            COUNTRY_CODE = builder.CreateString(self.COUNTRY_CODE)
        if self.ADMIN_AREA is not None:
            ADMIN_AREA = builder.CreateString(self.ADMIN_AREA)
        if self.PRIOR_POSITIONS is not None:
            PRIOR_POSITIONSlist = []
            for i in range(len(self.PRIOR_POSITIONS)):
                PRIOR_POSITIONSlist.append(self.PRIOR_POSITIONS[i].Pack(builder))
            TXSStartPRIOR_POSITIONSVector(builder, len(self.PRIOR_POSITIONS))
            for i in reversed(range(len(self.PRIOR_POSITIONS))):
                builder.PrependUOffsetTRelative(PRIOR_POSITIONSlist[i])
            PRIOR_POSITIONS = builder.EndVector()
        if self.EMISSIONS is not None:
            EMISSIONSlist = []
            for i in range(len(self.EMISSIONS)):
                EMISSIONSlist.append(self.EMISSIONS[i].Pack(builder))
            TXSStartEMISSIONSVector(builder, len(self.EMISSIONS))
            for i in reversed(range(len(self.EMISSIONS))):
                builder.PrependUOffsetTRelative(EMISSIONSlist[i])
            EMISSIONS = builder.EndVector()
        if self.FIRST_OBSERVED is not None:
            FIRST_OBSERVED = builder.CreateString(self.FIRST_OBSERVED)
        if self.LAST_OBSERVED is not None:
            LAST_OBSERVED = builder.CreateString(self.LAST_OBSERVED)
        if self.SOURCES is not None:
            SOURCESlist = []
            for i in range(len(self.SOURCES)):
                SOURCESlist.append(self.SOURCES[i].Pack(builder))
            TXSStartSOURCESVector(builder, len(self.SOURCES))
            for i in reversed(range(len(self.SOURCES))):
                builder.PrependUOffsetTRelative(SOURCESlist[i])
            SOURCES = builder.EndVector()
        if self.CONSENSUS is not None:
            CONSENSUS = self.CONSENSUS.Pack(builder)
        TXSStart(builder)
        if self.ID is not None:
            TXSAddID(builder, ID)
        if self.NATIVE_ID is not None:
            TXSAddNATIVE_ID(builder, NATIVE_ID)
        if self.SITE_NAME is not None:
            TXSAddSITE_NAME(builder, SITE_NAME)
        if self.STATION_IDENTITY is not None:
            TXSAddSTATION_IDENTITY(builder, STATION_IDENTITY)
        if self.LICENSEE is not None:
            TXSAddLICENSEE(builder, LICENSEE)
        if self.LICENSE_ID is not None:
            TXSAddLICENSE_ID(builder, LICENSE_ID)
        TXSAddSERVICE(builder, self.SERVICE)
        TXSAddSTATUS(builder, self.STATUS)
        if self.COUNTRY_CODE is not None:
            TXSAddCOUNTRY_CODE(builder, COUNTRY_CODE)
        if self.ADMIN_AREA is not None:
            TXSAddADMIN_AREA(builder, ADMIN_AREA)
        TXSAddLATITUDE(builder, self.LATITUDE)
        TXSAddLONGITUDE(builder, self.LONGITUDE)
        TXSAddPOSITION_AUTHORITY(builder, self.POSITION_AUTHORITY)
        TXSAddPOSITION_UNCERTAINTY_M(builder, self.POSITION_UNCERTAINTY_M)
        TXSAddPOSITION_DISPUTED(builder, self.POSITION_DISPUTED)
        if self.PRIOR_POSITIONS is not None:
            TXSAddPRIOR_POSITIONS(builder, PRIOR_POSITIONS)
        TXSAddGROUND_ELEVATION_AMSL_M(builder, self.GROUND_ELEVATION_AMSL_M)
        TXSAddSTRUCTURE_HEIGHT_AGL_M(builder, self.STRUCTURE_HEIGHT_AGL_M)
        TXSAddSTRUCTURE(builder, self.STRUCTURE)
        TXSAddSTRUCTURE_COUNT(builder, self.STRUCTURE_COUNT)
        if self.EMISSIONS is not None:
            TXSAddEMISSIONS(builder, EMISSIONS)
        if self.FIRST_OBSERVED is not None:
            TXSAddFIRST_OBSERVED(builder, FIRST_OBSERVED)
        if self.LAST_OBSERVED is not None:
            TXSAddLAST_OBSERVED(builder, LAST_OBSERVED)
        if self.SOURCES is not None:
            TXSAddSOURCES(builder, SOURCES)
        if self.CONSENSUS is not None:
            TXSAddCONSENSUS(builder, CONSENSUS)
        TXS = TXSEnd(builder)
        return TXS
