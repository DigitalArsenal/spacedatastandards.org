# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Resident index construction reuses canonical sources, samples and PPE.
# SAMPLED_STATES consumes sampled OEM records in SOURCES[].EPHEMERIS;
# POLYNOMIAL_SEGMENTS consumes SEGMENTS/PPE. Handles are validated against
# INSTANCE identity/generation; they are never persisted process addresses.
class CQRIndexRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CQRIndexRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCQRIndexRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CQRIndexRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x51\x52", size_prefixed=size_prefixed)

    # CQRIndexRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CQRIndexRequest
    def INSTANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWInstance import PRWInstance
            obj = PRWInstance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CQRIndexRequest
    def CATALOG_HANDLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # CQRIndexRequest
    def SOURCE_HANDLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # CQRIndexRequest
    def SOURCE_HANDLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # CQRIndexRequest
    def SOURCE_HANDLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CQRIndexRequest
    def SOURCE_HANDLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # CQRIndexRequest
    def SEGMENT_SET_HANDLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # CQRIndexRequest
    def PRIMARY_SOURCE_HANDLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # CQRIndexRequest
    def PRIMARY_SOURCE_HANDLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # CQRIndexRequest
    def PRIMARY_SOURCE_HANDLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CQRIndexRequest
    def PRIMARY_SOURCE_HANDLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # CQRIndexRequest
    def INDEX_CONTENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # CQRIndexRequest
    def REFINEMENT_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # CQRIndexRequest
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from CQRObjectSource import CQRObjectSource
            obj = CQRObjectSource()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CQRIndexRequest
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CQRIndexRequest
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # CQRIndexRequest
    def SEGMENTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWDescribeResult import PRWDescribeResult
            obj = PRWDescribeResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def CQRIndexRequestStart(builder):
    builder.StartObject(9)

def Start(builder):
    CQRIndexRequestStart(builder)

def CQRIndexRequestAddINSTANCE(builder, INSTANCE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(INSTANCE), 0)

def AddINSTANCE(builder, INSTANCE):
    CQRIndexRequestAddINSTANCE(builder, INSTANCE)

def CQRIndexRequestAddCATALOG_HANDLE(builder, CATALOG_HANDLE):
    builder.PrependUint32Slot(1, CATALOG_HANDLE, 0)

def AddCATALOG_HANDLE(builder, CATALOG_HANDLE):
    CQRIndexRequestAddCATALOG_HANDLE(builder, CATALOG_HANDLE)

def CQRIndexRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_HANDLES), 0)

def AddSOURCE_HANDLES(builder, SOURCE_HANDLES):
    CQRIndexRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES)

def CQRIndexRequestStartSOURCE_HANDLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCE_HANDLESVector(builder, numElems):
    return CQRIndexRequestStartSOURCE_HANDLESVector(builder, numElems)

def CQRIndexRequestCreateSOURCE_HANDLESVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateSOURCE_HANDLESVector(builder, data):
    CQRIndexRequestCreateSOURCE_HANDLESVector(builder, data)

def CQRIndexRequestAddSEGMENT_SET_HANDLE(builder, SEGMENT_SET_HANDLE):
    builder.PrependUint32Slot(3, SEGMENT_SET_HANDLE, 0)

def AddSEGMENT_SET_HANDLE(builder, SEGMENT_SET_HANDLE):
    CQRIndexRequestAddSEGMENT_SET_HANDLE(builder, SEGMENT_SET_HANDLE)

def CQRIndexRequestAddPRIMARY_SOURCE_HANDLES(builder, PRIMARY_SOURCE_HANDLES):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PRIMARY_SOURCE_HANDLES), 0)

def AddPRIMARY_SOURCE_HANDLES(builder, PRIMARY_SOURCE_HANDLES):
    CQRIndexRequestAddPRIMARY_SOURCE_HANDLES(builder, PRIMARY_SOURCE_HANDLES)

def CQRIndexRequestStartPRIMARY_SOURCE_HANDLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPRIMARY_SOURCE_HANDLESVector(builder, numElems):
    return CQRIndexRequestStartPRIMARY_SOURCE_HANDLESVector(builder, numElems)

def CQRIndexRequestCreatePRIMARY_SOURCE_HANDLESVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreatePRIMARY_SOURCE_HANDLESVector(builder, data):
    CQRIndexRequestCreatePRIMARY_SOURCE_HANDLESVector(builder, data)

def CQRIndexRequestAddINDEX_CONTENT(builder, INDEX_CONTENT):
    builder.PrependUint8Slot(5, INDEX_CONTENT, 0)

def AddINDEX_CONTENT(builder, INDEX_CONTENT):
    CQRIndexRequestAddINDEX_CONTENT(builder, INDEX_CONTENT)

def CQRIndexRequestAddREFINEMENT_MODE(builder, REFINEMENT_MODE):
    builder.PrependUint8Slot(6, REFINEMENT_MODE, 0)

def AddREFINEMENT_MODE(builder, REFINEMENT_MODE):
    CQRIndexRequestAddREFINEMENT_MODE(builder, REFINEMENT_MODE)

def CQRIndexRequestAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    CQRIndexRequestAddSOURCES(builder, SOURCES)

def CQRIndexRequestStartSOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCESVector(builder, numElems):
    return CQRIndexRequestStartSOURCESVector(builder, numElems)

def CQRIndexRequestCreateSOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCESVector(builder, data):
    CQRIndexRequestCreateSOURCESVector(builder, data)

def CQRIndexRequestAddSEGMENTS(builder, SEGMENTS):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(SEGMENTS), 0)

def AddSEGMENTS(builder, SEGMENTS):
    CQRIndexRequestAddSEGMENTS(builder, SEGMENTS)

def CQRIndexRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return CQRIndexRequestEnd(builder)

import CQRObjectSource
import PRWDescribeResult
import PRWInstance
try:
    from typing import List, Optional
except:
    pass

class CQRIndexRequestT(object):

    # CQRIndexRequestT
    def __init__(
        self,
        INSTANCE = None,
        CATALOG_HANDLE = 0,
        SOURCE_HANDLES = None,
        SEGMENT_SET_HANDLE = 0,
        PRIMARY_SOURCE_HANDLES = None,
        INDEX_CONTENT = 0,
        REFINEMENT_MODE = 0,
        SOURCES = None,
        SEGMENTS = None,
    ):
        self.INSTANCE = INSTANCE  # type: Optional[PRWInstance.PRWInstanceT]
        self.CATALOG_HANDLE = CATALOG_HANDLE  # type: int
        self.SOURCE_HANDLES = SOURCE_HANDLES  # type: Optional[List[int]]
        self.SEGMENT_SET_HANDLE = SEGMENT_SET_HANDLE  # type: int
        self.PRIMARY_SOURCE_HANDLES = PRIMARY_SOURCE_HANDLES  # type: Optional[List[int]]
        self.INDEX_CONTENT = INDEX_CONTENT  # type: int
        self.REFINEMENT_MODE = REFINEMENT_MODE  # type: int
        self.SOURCES = SOURCES  # type: Optional[List[CQRObjectSource.CQRObjectSourceT]]
        self.SEGMENTS = SEGMENTS  # type: Optional[PRWDescribeResult.PRWDescribeResultT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCqrindexRequest = CQRIndexRequest()
        tmpCqrindexRequest.Init(buf, pos)
        return cls.InitFromObj(tmpCqrindexRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCqrindexRequest):
        x = CQRIndexRequestT()
        x._UnPack(tmpCqrindexRequest)
        return x

    # CQRIndexRequestT
    def _UnPack(self, CQRIndexRequest):
        if CQRIndexRequest is None:
            return
        if CQRIndexRequest.INSTANCE() is not None:
            self.INSTANCE = PRWInstance.PRWInstanceT.InitFromObj(CQRIndexRequest.INSTANCE())
        self.CATALOG_HANDLE = CQRIndexRequest.CATALOG_HANDLE()
        if not CQRIndexRequest.SOURCE_HANDLESIsNone():
            if np is None:
                self.SOURCE_HANDLES = []
                for i in range(CQRIndexRequest.SOURCE_HANDLESLength()):
                    self.SOURCE_HANDLES.append(CQRIndexRequest.SOURCE_HANDLES(i))
            else:
                self.SOURCE_HANDLES = CQRIndexRequest.SOURCE_HANDLESAsNumpy()
        self.SEGMENT_SET_HANDLE = CQRIndexRequest.SEGMENT_SET_HANDLE()
        if not CQRIndexRequest.PRIMARY_SOURCE_HANDLESIsNone():
            if np is None:
                self.PRIMARY_SOURCE_HANDLES = []
                for i in range(CQRIndexRequest.PRIMARY_SOURCE_HANDLESLength()):
                    self.PRIMARY_SOURCE_HANDLES.append(CQRIndexRequest.PRIMARY_SOURCE_HANDLES(i))
            else:
                self.PRIMARY_SOURCE_HANDLES = CQRIndexRequest.PRIMARY_SOURCE_HANDLESAsNumpy()
        self.INDEX_CONTENT = CQRIndexRequest.INDEX_CONTENT()
        self.REFINEMENT_MODE = CQRIndexRequest.REFINEMENT_MODE()
        if not CQRIndexRequest.SOURCESIsNone():
            self.SOURCES = []
            for i in range(CQRIndexRequest.SOURCESLength()):
                if CQRIndexRequest.SOURCES(i) is None:
                    self.SOURCES.append(None)
                else:
                    cQRObjectSource_ = CQRObjectSource.CQRObjectSourceT.InitFromObj(CQRIndexRequest.SOURCES(i))
                    self.SOURCES.append(cQRObjectSource_)
        if CQRIndexRequest.SEGMENTS() is not None:
            self.SEGMENTS = PRWDescribeResult.PRWDescribeResultT.InitFromObj(CQRIndexRequest.SEGMENTS())

    # CQRIndexRequestT
    def Pack(self, builder):
        if self.INSTANCE is not None:
            INSTANCE = self.INSTANCE.Pack(builder)
        if self.SOURCE_HANDLES is not None:
            if np is not None and type(self.SOURCE_HANDLES) is np.ndarray:
                SOURCE_HANDLES = builder.CreateNumpyVector(self.SOURCE_HANDLES)
            else:
                CQRIndexRequestStartSOURCE_HANDLESVector(builder, len(self.SOURCE_HANDLES))
                for i in reversed(range(len(self.SOURCE_HANDLES))):
                    builder.PrependUint32(self.SOURCE_HANDLES[i])
                SOURCE_HANDLES = builder.EndVector()
        if self.PRIMARY_SOURCE_HANDLES is not None:
            if np is not None and type(self.PRIMARY_SOURCE_HANDLES) is np.ndarray:
                PRIMARY_SOURCE_HANDLES = builder.CreateNumpyVector(self.PRIMARY_SOURCE_HANDLES)
            else:
                CQRIndexRequestStartPRIMARY_SOURCE_HANDLESVector(builder, len(self.PRIMARY_SOURCE_HANDLES))
                for i in reversed(range(len(self.PRIMARY_SOURCE_HANDLES))):
                    builder.PrependUint32(self.PRIMARY_SOURCE_HANDLES[i])
                PRIMARY_SOURCE_HANDLES = builder.EndVector()
        if self.SOURCES is not None:
            SOURCESlist = []
            for i in range(len(self.SOURCES)):
                SOURCESlist.append(self.SOURCES[i].Pack(builder))
            CQRIndexRequestStartSOURCESVector(builder, len(self.SOURCES))
            for i in reversed(range(len(self.SOURCES))):
                builder.PrependUOffsetTRelative(SOURCESlist[i])
            SOURCES = builder.EndVector()
        if self.SEGMENTS is not None:
            SEGMENTS = self.SEGMENTS.Pack(builder)
        CQRIndexRequestStart(builder)
        if self.INSTANCE is not None:
            CQRIndexRequestAddINSTANCE(builder, INSTANCE)
        CQRIndexRequestAddCATALOG_HANDLE(builder, self.CATALOG_HANDLE)
        if self.SOURCE_HANDLES is not None:
            CQRIndexRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES)
        CQRIndexRequestAddSEGMENT_SET_HANDLE(builder, self.SEGMENT_SET_HANDLE)
        if self.PRIMARY_SOURCE_HANDLES is not None:
            CQRIndexRequestAddPRIMARY_SOURCE_HANDLES(builder, PRIMARY_SOURCE_HANDLES)
        CQRIndexRequestAddINDEX_CONTENT(builder, self.INDEX_CONTENT)
        CQRIndexRequestAddREFINEMENT_MODE(builder, self.REFINEMENT_MODE)
        if self.SOURCES is not None:
            CQRIndexRequestAddSOURCES(builder, SOURCES)
        if self.SEGMENTS is not None:
            CQRIndexRequestAddSEGMENTS(builder, SEGMENTS)
        CQRIndexRequest = CQRIndexRequestEnd(builder)
        return CQRIndexRequest
