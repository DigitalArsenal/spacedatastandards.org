# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Geography an aggregate is keyed to.
#
# A separate table so an absent REGION means "the source did not key by
# geography", which is different from GLOBAL ("the source aggregated across
# all geographies") and different again from an empty CODE.
class CNPRegion(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CNPRegion()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCNPRegion(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CNPRegionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4E\x50", size_prefixed=size_prefixed)

    # CNPRegion
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Granularity of CODE.
    # CNPRegion
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 6

    # The region key, verbatim from the source in the coding scheme KIND
    # names. Never re-coded between schemes on ingest.
    # CNPRegion
    def CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable region name as the source publishes it. Joins to
    # $CTR for country identity when KIND is COUNTRY.
    # CNPRegion
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def CNPRegionStart(builder):
    builder.StartObject(3)

def Start(builder):
    CNPRegionStart(builder)

def CNPRegionAddKIND(builder, KIND):
    builder.PrependInt8Slot(0, KIND, 6)

def AddKIND(builder, KIND):
    CNPRegionAddKIND(builder, KIND)

def CNPRegionAddCODE(builder, CODE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CODE), 0)

def AddCODE(builder, CODE):
    CNPRegionAddCODE(builder, CODE)

def CNPRegionAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    CNPRegionAddNAME(builder, NAME)

def CNPRegionEnd(builder):
    return builder.EndObject()

def End(builder):
    return CNPRegionEnd(builder)


class CNPRegionT(object):

    # CNPRegionT
    def __init__(
        self,
        KIND = 6,
        CODE = None,
        NAME = None,
    ):
        self.KIND = KIND  # type: int
        self.CODE = CODE  # type: Optional[str]
        self.NAME = NAME  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCnpregion = CNPRegion()
        tmpCnpregion.Init(buf, pos)
        return cls.InitFromObj(tmpCnpregion)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCnpregion):
        x = CNPRegionT()
        x._UnPack(tmpCnpregion)
        return x

    # CNPRegionT
    def _UnPack(self, CNPRegion):
        if CNPRegion is None:
            return
        self.KIND = CNPRegion.KIND()
        self.CODE = CNPRegion.CODE()
        self.NAME = CNPRegion.NAME()

    # CNPRegionT
    def Pack(self, builder):
        if self.CODE is not None:
            CODE = builder.CreateString(self.CODE)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        CNPRegionStart(builder)
        CNPRegionAddKIND(builder, self.KIND)
        if self.CODE is not None:
            CNPRegionAddCODE(builder, CODE)
        if self.NAME is not None:
            CNPRegionAddNAME(builder, NAME)
        CNPRegion = CNPRegionEnd(builder)
        return CNPRegion
