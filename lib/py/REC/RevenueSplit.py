# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One recipient's share of a sale, expressed in basis points.
class RevenueSplit(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RevenueSplit()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRevenueSplit(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RevenueSplitBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4D\x54", size_prefixed=size_prefixed)

    # RevenueSplit
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # The payout recipient
    # RevenueSplit
    def RECIPIENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SplitRecipient import SplitRecipient
            obj = SplitRecipient()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Share in basis points (1/100 of a percent); all splits plus the platform
    # commission sum to 10000
    # RevenueSplit
    def BPS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # The role this recipient plays
    # RevenueSplit
    def ROLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def RevenueSplitStart(builder):
    builder.StartObject(3)

def Start(builder):
    RevenueSplitStart(builder)

def RevenueSplitAddRECIPIENT(builder, RECIPIENT):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECIPIENT), 0)

def AddRECIPIENT(builder, RECIPIENT):
    RevenueSplitAddRECIPIENT(builder, RECIPIENT)

def RevenueSplitAddBPS(builder, BPS):
    builder.PrependUint16Slot(1, BPS, 0)

def AddBPS(builder, BPS):
    RevenueSplitAddBPS(builder, BPS)

def RevenueSplitAddROLE(builder, ROLE):
    builder.PrependInt8Slot(2, ROLE, 0)

def AddROLE(builder, ROLE):
    RevenueSplitAddROLE(builder, ROLE)

def RevenueSplitEnd(builder):
    return builder.EndObject()

def End(builder):
    return RevenueSplitEnd(builder)

import SplitRecipient
try:
    from typing import Optional
except:
    pass

class RevenueSplitT(object):

    # RevenueSplitT
    def __init__(
        self,
        RECIPIENT = None,
        BPS = 0,
        ROLE = 0,
    ):
        self.RECIPIENT = RECIPIENT  # type: Optional[SplitRecipient.SplitRecipientT]
        self.BPS = BPS  # type: int
        self.ROLE = ROLE  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpRevenueSplit = RevenueSplit()
        tmpRevenueSplit.Init(buf, pos)
        return cls.InitFromObj(tmpRevenueSplit)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpRevenueSplit):
        x = RevenueSplitT()
        x._UnPack(tmpRevenueSplit)
        return x

    # RevenueSplitT
    def _UnPack(self, RevenueSplit):
        if RevenueSplit is None:
            return
        if RevenueSplit.RECIPIENT() is not None:
            self.RECIPIENT = SplitRecipient.SplitRecipientT.InitFromObj(RevenueSplit.RECIPIENT())
        self.BPS = RevenueSplit.BPS()
        self.ROLE = RevenueSplit.ROLE()

    # RevenueSplitT
    def Pack(self, builder):
        if self.RECIPIENT is not None:
            RECIPIENT = self.RECIPIENT.Pack(builder)
        RevenueSplitStart(builder)
        if self.RECIPIENT is not None:
            RevenueSplitAddRECIPIENT(builder, RECIPIENT)
        RevenueSplitAddBPS(builder, self.BPS)
        RevenueSplitAddROLE(builder, self.ROLE)
        RevenueSplit = RevenueSplitEnd(builder)
        return RevenueSplit
