# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Propagator Runtime Wire — envelope that carries either an init request,
# a batch request, or a batch response across a runtime boundary.
class PRW(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRW()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRW(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRW
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRW
    def INIT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWInit import PRWInit
            obj = PRWInit()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRW
    def BATCH_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWBatchRequest import PRWBatchRequest
            obj = PRWBatchRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRW
    def BATCH_RESPONSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWBatchResponse import PRWBatchResponse
            obj = PRWBatchResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def PRWStart(builder):
    builder.StartObject(3)

def Start(builder):
    PRWStart(builder)

def PRWAddINIT(builder, INIT):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(INIT), 0)

def AddINIT(builder, INIT):
    PRWAddINIT(builder, INIT)

def PRWAddBATCH_REQUEST(builder, BATCH_REQUEST):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(BATCH_REQUEST), 0)

def AddBATCH_REQUEST(builder, BATCH_REQUEST):
    PRWAddBATCH_REQUEST(builder, BATCH_REQUEST)

def PRWAddBATCH_RESPONSE(builder, BATCH_RESPONSE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(BATCH_RESPONSE), 0)

def AddBATCH_RESPONSE(builder, BATCH_RESPONSE):
    PRWAddBATCH_RESPONSE(builder, BATCH_RESPONSE)

def PRWEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWEnd(builder)

import PRWBatchRequest
import PRWBatchResponse
import PRWInit
try:
    from typing import Optional
except:
    pass

class PRWT(object):

    # PRWT
    def __init__(
        self,
        INIT = None,
        BATCH_REQUEST = None,
        BATCH_RESPONSE = None,
    ):
        self.INIT = INIT  # type: Optional[PRWInit.PRWInitT]
        self.BATCH_REQUEST = BATCH_REQUEST  # type: Optional[PRWBatchRequest.PRWBatchRequestT]
        self.BATCH_RESPONSE = BATCH_RESPONSE  # type: Optional[PRWBatchResponse.PRWBatchResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrw = PRW()
        tmpPrw.Init(buf, pos)
        return cls.InitFromObj(tmpPrw)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrw):
        x = PRWT()
        x._UnPack(tmpPrw)
        return x

    # PRWT
    def _UnPack(self, PRW):
        if PRW is None:
            return
        if PRW.INIT() is not None:
            self.INIT = PRWInit.PRWInitT.InitFromObj(PRW.INIT())
        if PRW.BATCH_REQUEST() is not None:
            self.BATCH_REQUEST = PRWBatchRequest.PRWBatchRequestT.InitFromObj(PRW.BATCH_REQUEST())
        if PRW.BATCH_RESPONSE() is not None:
            self.BATCH_RESPONSE = PRWBatchResponse.PRWBatchResponseT.InitFromObj(PRW.BATCH_RESPONSE())

    # PRWT
    def Pack(self, builder):
        if self.INIT is not None:
            INIT = self.INIT.Pack(builder)
        if self.BATCH_REQUEST is not None:
            BATCH_REQUEST = self.BATCH_REQUEST.Pack(builder)
        if self.BATCH_RESPONSE is not None:
            BATCH_RESPONSE = self.BATCH_RESPONSE.Pack(builder)
        PRWStart(builder)
        if self.INIT is not None:
            PRWAddINIT(builder, INIT)
        if self.BATCH_REQUEST is not None:
            PRWAddBATCH_REQUEST(builder, BATCH_REQUEST)
        if self.BATCH_RESPONSE is not None:
            PRWAddBATCH_RESPONSE(builder, BATCH_RESPONSE)
        PRW = PRWEnd(builder)
        return PRW
