# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Geodetic position of the receiver at capture time.
#
# A separate table because latitude/longitude 0,0 is a VALID coordinate: an
# absent GEOLOCATION means "the source published no position", which a
# zero-defaulted scalar pair could never express.
class IQCGeolocation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IQCGeolocation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIQCGeolocation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IQCGeolocationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x51\x43", size_prefixed=size_prefixed)

    # IQCGeolocation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Geodetic latitude, degrees, WGS-84, positive north.
    # IQCGeolocation
    def LATITUDE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Geodetic longitude, degrees, WGS-84, positive east.
    # IQCGeolocation
    def LONGITUDE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Height above the WGS-84 ellipsoid, metres. Absent in most SigMF
    # recordings; SigMF `core:geolocation` is a GeoJSON Point whose third
    # ordinate is optional.
    # IQCGeolocation
    def ALTITUDE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Reported position uncertainty, metres, when the source states one.
    # IQCGeolocation
    def UNCERTAINTY_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # How the position was obtained, verbatim from the source (e.g. "GPS",
    # "station registry", "author-supplied"). Never inferred.
    # IQCGeolocation
    def METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def IQCGeolocationStart(builder):
    builder.StartObject(5)

def Start(builder):
    IQCGeolocationStart(builder)

def IQCGeolocationAddLATITUDE_DEG(builder, LATITUDE_DEG):
    builder.PrependFloat64Slot(0, LATITUDE_DEG, 0.0)

def AddLATITUDE_DEG(builder, LATITUDE_DEG):
    IQCGeolocationAddLATITUDE_DEG(builder, LATITUDE_DEG)

def IQCGeolocationAddLONGITUDE_DEG(builder, LONGITUDE_DEG):
    builder.PrependFloat64Slot(1, LONGITUDE_DEG, 0.0)

def AddLONGITUDE_DEG(builder, LONGITUDE_DEG):
    IQCGeolocationAddLONGITUDE_DEG(builder, LONGITUDE_DEG)

def IQCGeolocationAddALTITUDE_M(builder, ALTITUDE_M):
    builder.PrependFloat64Slot(2, ALTITUDE_M, 0.0)

def AddALTITUDE_M(builder, ALTITUDE_M):
    IQCGeolocationAddALTITUDE_M(builder, ALTITUDE_M)

def IQCGeolocationAddUNCERTAINTY_M(builder, UNCERTAINTY_M):
    builder.PrependFloat64Slot(3, UNCERTAINTY_M, 0.0)

def AddUNCERTAINTY_M(builder, UNCERTAINTY_M):
    IQCGeolocationAddUNCERTAINTY_M(builder, UNCERTAINTY_M)

def IQCGeolocationAddMETHOD(builder, METHOD):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(METHOD), 0)

def AddMETHOD(builder, METHOD):
    IQCGeolocationAddMETHOD(builder, METHOD)

def IQCGeolocationEnd(builder):
    return builder.EndObject()

def End(builder):
    return IQCGeolocationEnd(builder)


class IQCGeolocationT(object):

    # IQCGeolocationT
    def __init__(
        self,
        LATITUDE_DEG = 0.0,
        LONGITUDE_DEG = 0.0,
        ALTITUDE_M = 0.0,
        UNCERTAINTY_M = 0.0,
        METHOD = None,
    ):
        self.LATITUDE_DEG = LATITUDE_DEG  # type: float
        self.LONGITUDE_DEG = LONGITUDE_DEG  # type: float
        self.ALTITUDE_M = ALTITUDE_M  # type: float
        self.UNCERTAINTY_M = UNCERTAINTY_M  # type: float
        self.METHOD = METHOD  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIqcgeolocation = IQCGeolocation()
        tmpIqcgeolocation.Init(buf, pos)
        return cls.InitFromObj(tmpIqcgeolocation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIqcgeolocation):
        x = IQCGeolocationT()
        x._UnPack(tmpIqcgeolocation)
        return x

    # IQCGeolocationT
    def _UnPack(self, IQCGeolocation):
        if IQCGeolocation is None:
            return
        self.LATITUDE_DEG = IQCGeolocation.LATITUDE_DEG()
        self.LONGITUDE_DEG = IQCGeolocation.LONGITUDE_DEG()
        self.ALTITUDE_M = IQCGeolocation.ALTITUDE_M()
        self.UNCERTAINTY_M = IQCGeolocation.UNCERTAINTY_M()
        self.METHOD = IQCGeolocation.METHOD()

    # IQCGeolocationT
    def Pack(self, builder):
        if self.METHOD is not None:
            METHOD = builder.CreateString(self.METHOD)
        IQCGeolocationStart(builder)
        IQCGeolocationAddLATITUDE_DEG(builder, self.LATITUDE_DEG)
        IQCGeolocationAddLONGITUDE_DEG(builder, self.LONGITUDE_DEG)
        IQCGeolocationAddALTITUDE_M(builder, self.ALTITUDE_M)
        IQCGeolocationAddUNCERTAINTY_M(builder, self.UNCERTAINTY_M)
        if self.METHOD is not None:
            IQCGeolocationAddMETHOD(builder, METHOD)
        IQCGeolocation = IQCGeolocationEnd(builder)
        return IQCGeolocation
