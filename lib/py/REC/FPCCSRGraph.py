# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Pre-built CSR graph ingestion request.
class FPCCSRGraph(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FPCCSRGraph()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFPCCSRGraph(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FPCCSRGraphBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x46\x50\x43", size_prefixed=size_prefixed)

    # FPCCSRGraph
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Number of vertices in the graph.
    # FPCCSRGraph
    def VERTEX_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # CSR row offsets (V + 1 entries).
    # FPCCSRGraph
    def OFFSETS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # FPCCSRGraph
    def OFFSETSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # FPCCSRGraph
    def OFFSETSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FPCCSRGraph
    def OFFSETSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # CSR destination indices.
    # FPCCSRGraph
    def DESTINATIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # FPCCSRGraph
    def DESTINATIONSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # FPCCSRGraph
    def DESTINATIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FPCCSRGraph
    def DESTINATIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # CSR edge weights.
    # FPCCSRGraph
    def WEIGHTS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # FPCCSRGraph
    def WEIGHTSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # FPCCSRGraph
    def WEIGHTSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FPCCSRGraph
    def WEIGHTSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def FPCCSRGraphStart(builder):
    builder.StartObject(4)

def Start(builder):
    FPCCSRGraphStart(builder)

def FPCCSRGraphAddVERTEX_COUNT(builder, VERTEX_COUNT):
    builder.PrependUint32Slot(0, VERTEX_COUNT, 0)

def AddVERTEX_COUNT(builder, VERTEX_COUNT):
    FPCCSRGraphAddVERTEX_COUNT(builder, VERTEX_COUNT)

def FPCCSRGraphAddOFFSETS(builder, OFFSETS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(OFFSETS), 0)

def AddOFFSETS(builder, OFFSETS):
    FPCCSRGraphAddOFFSETS(builder, OFFSETS)

def FPCCSRGraphStartOFFSETSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartOFFSETSVector(builder, numElems):
    return FPCCSRGraphStartOFFSETSVector(builder, numElems)

def FPCCSRGraphCreateOFFSETSVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateOFFSETSVector(builder, data):
    FPCCSRGraphCreateOFFSETSVector(builder, data)

def FPCCSRGraphAddDESTINATIONS(builder, DESTINATIONS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(DESTINATIONS), 0)

def AddDESTINATIONS(builder, DESTINATIONS):
    FPCCSRGraphAddDESTINATIONS(builder, DESTINATIONS)

def FPCCSRGraphStartDESTINATIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDESTINATIONSVector(builder, numElems):
    return FPCCSRGraphStartDESTINATIONSVector(builder, numElems)

def FPCCSRGraphCreateDESTINATIONSVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateDESTINATIONSVector(builder, data):
    FPCCSRGraphCreateDESTINATIONSVector(builder, data)

def FPCCSRGraphAddWEIGHTS(builder, WEIGHTS):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(WEIGHTS), 0)

def AddWEIGHTS(builder, WEIGHTS):
    FPCCSRGraphAddWEIGHTS(builder, WEIGHTS)

def FPCCSRGraphStartWEIGHTSVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartWEIGHTSVector(builder, numElems):
    return FPCCSRGraphStartWEIGHTSVector(builder, numElems)

def FPCCSRGraphCreateWEIGHTSVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateWEIGHTSVector(builder, data):
    FPCCSRGraphCreateWEIGHTSVector(builder, data)

def FPCCSRGraphEnd(builder):
    return builder.EndObject()

def End(builder):
    return FPCCSRGraphEnd(builder)

try:
    from typing import List
except:
    pass

class FPCCSRGraphT(object):

    # FPCCSRGraphT
    def __init__(
        self,
        VERTEX_COUNT = 0,
        OFFSETS = None,
        DESTINATIONS = None,
        WEIGHTS = None,
    ):
        self.VERTEX_COUNT = VERTEX_COUNT  # type: int
        self.OFFSETS = OFFSETS  # type: Optional[List[int]]
        self.DESTINATIONS = DESTINATIONS  # type: Optional[List[int]]
        self.WEIGHTS = WEIGHTS  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpFpccsrgraph = FPCCSRGraph()
        tmpFpccsrgraph.Init(buf, pos)
        return cls.InitFromObj(tmpFpccsrgraph)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpFpccsrgraph):
        x = FPCCSRGraphT()
        x._UnPack(tmpFpccsrgraph)
        return x

    # FPCCSRGraphT
    def _UnPack(self, FPCCSRGraph):
        if FPCCSRGraph is None:
            return
        self.VERTEX_COUNT = FPCCSRGraph.VERTEX_COUNT()
        if not FPCCSRGraph.OFFSETSIsNone():
            if np is None:
                self.OFFSETS = []
                for i in range(FPCCSRGraph.OFFSETSLength()):
                    self.OFFSETS.append(FPCCSRGraph.OFFSETS(i))
            else:
                self.OFFSETS = FPCCSRGraph.OFFSETSAsNumpy()
        if not FPCCSRGraph.DESTINATIONSIsNone():
            if np is None:
                self.DESTINATIONS = []
                for i in range(FPCCSRGraph.DESTINATIONSLength()):
                    self.DESTINATIONS.append(FPCCSRGraph.DESTINATIONS(i))
            else:
                self.DESTINATIONS = FPCCSRGraph.DESTINATIONSAsNumpy()
        if not FPCCSRGraph.WEIGHTSIsNone():
            if np is None:
                self.WEIGHTS = []
                for i in range(FPCCSRGraph.WEIGHTSLength()):
                    self.WEIGHTS.append(FPCCSRGraph.WEIGHTS(i))
            else:
                self.WEIGHTS = FPCCSRGraph.WEIGHTSAsNumpy()

    # FPCCSRGraphT
    def Pack(self, builder):
        if self.OFFSETS is not None:
            if np is not None and type(self.OFFSETS) is np.ndarray:
                OFFSETS = builder.CreateNumpyVector(self.OFFSETS)
            else:
                FPCCSRGraphStartOFFSETSVector(builder, len(self.OFFSETS))
                for i in reversed(range(len(self.OFFSETS))):
                    builder.PrependUint32(self.OFFSETS[i])
                OFFSETS = builder.EndVector()
        if self.DESTINATIONS is not None:
            if np is not None and type(self.DESTINATIONS) is np.ndarray:
                DESTINATIONS = builder.CreateNumpyVector(self.DESTINATIONS)
            else:
                FPCCSRGraphStartDESTINATIONSVector(builder, len(self.DESTINATIONS))
                for i in reversed(range(len(self.DESTINATIONS))):
                    builder.PrependUint32(self.DESTINATIONS[i])
                DESTINATIONS = builder.EndVector()
        if self.WEIGHTS is not None:
            if np is not None and type(self.WEIGHTS) is np.ndarray:
                WEIGHTS = builder.CreateNumpyVector(self.WEIGHTS)
            else:
                FPCCSRGraphStartWEIGHTSVector(builder, len(self.WEIGHTS))
                for i in reversed(range(len(self.WEIGHTS))):
                    builder.PrependFloat64(self.WEIGHTS[i])
                WEIGHTS = builder.EndVector()
        FPCCSRGraphStart(builder)
        FPCCSRGraphAddVERTEX_COUNT(builder, self.VERTEX_COUNT)
        if self.OFFSETS is not None:
            FPCCSRGraphAddOFFSETS(builder, OFFSETS)
        if self.DESTINATIONS is not None:
            FPCCSRGraphAddDESTINATIONS(builder, DESTINATIONS)
        if self.WEIGHTS is not None:
            FPCCSRGraphAddWEIGHTS(builder, WEIGHTS)
        FPCCSRGraph = FPCCSRGraphEnd(builder)
        return FPCCSRGraph
