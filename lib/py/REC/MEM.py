# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Measurement Error Model. JSON projections preserve these IDL-capitalized
# field names exactly. The FlatBuffer signature covers the size-prefixed
# buffer with both signature vectors zeroed; the canonical-JSON signature
# covers RFC 8785 canonical JSON with both signature fields omitted.
class MEM(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MEM()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMEM(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def MEMBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4D\x45\x4D", size_prefixed=size_prefixed)

    # MEM
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MEM
    def ERROR_MODELS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from MEMErrorModel import MEMErrorModel
            obj = MEMErrorModel()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MEM
    def ERROR_MODELSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MEM
    def ERROR_MODELSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # MEM
    def EDITING_LEDGER(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from MEMEditingDecision import MEMEditingDecision
            obj = MEMEditingDecision()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MEM
    def EDITING_LEDGERLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MEM
    def EDITING_LEDGERIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # MEM
    def RANDOM_SEED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # MEM
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # MEM
    def ATTESTATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from MEMAttestation import MEMAttestation
            obj = MEMAttestation()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def MEMStart(builder):
    builder.StartObject(5)

def Start(builder):
    MEMStart(builder)

def MEMAddERROR_MODELS(builder, ERROR_MODELS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ERROR_MODELS), 0)

def AddERROR_MODELS(builder, ERROR_MODELS):
    MEMAddERROR_MODELS(builder, ERROR_MODELS)

def MEMStartERROR_MODELSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartERROR_MODELSVector(builder, numElems):
    return MEMStartERROR_MODELSVector(builder, numElems)

def MEMCreateERROR_MODELSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateERROR_MODELSVector(builder, data):
    MEMCreateERROR_MODELSVector(builder, data)

def MEMAddEDITING_LEDGER(builder, EDITING_LEDGER):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(EDITING_LEDGER), 0)

def AddEDITING_LEDGER(builder, EDITING_LEDGER):
    MEMAddEDITING_LEDGER(builder, EDITING_LEDGER)

def MEMStartEDITING_LEDGERVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartEDITING_LEDGERVector(builder, numElems):
    return MEMStartEDITING_LEDGERVector(builder, numElems)

def MEMCreateEDITING_LEDGERVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateEDITING_LEDGERVector(builder, data):
    MEMCreateEDITING_LEDGERVector(builder, data)

def MEMAddRANDOM_SEED(builder, RANDOM_SEED):
    builder.PrependUint64Slot(2, RANDOM_SEED, 0)

def AddRANDOM_SEED(builder, RANDOM_SEED):
    MEMAddRANDOM_SEED(builder, RANDOM_SEED)

def MEMAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(TRACE_ID), 0)

def AddTRACE_ID(builder, TRACE_ID):
    MEMAddTRACE_ID(builder, TRACE_ID)

def MEMAddATTESTATION(builder, ATTESTATION):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ATTESTATION), 0)

def AddATTESTATION(builder, ATTESTATION):
    MEMAddATTESTATION(builder, ATTESTATION)

def MEMEnd(builder):
    return builder.EndObject()

def End(builder):
    return MEMEnd(builder)

import MEMAttestation
import MEMEditingDecision
import MEMErrorModel
try:
    from typing import List, Optional
except:
    pass

class MEMT(object):

    # MEMT
    def __init__(
        self,
        ERROR_MODELS = None,
        EDITING_LEDGER = None,
        RANDOM_SEED = 0,
        TRACE_ID = None,
        ATTESTATION = None,
    ):
        self.ERROR_MODELS = ERROR_MODELS  # type: Optional[List[MEMErrorModel.MEMErrorModelT]]
        self.EDITING_LEDGER = EDITING_LEDGER  # type: Optional[List[MEMEditingDecision.MEMEditingDecisionT]]
        self.RANDOM_SEED = RANDOM_SEED  # type: int
        self.TRACE_ID = TRACE_ID  # type: Optional[str]
        self.ATTESTATION = ATTESTATION  # type: Optional[MEMAttestation.MEMAttestationT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpMem = MEM()
        tmpMem.Init(buf, pos)
        return cls.InitFromObj(tmpMem)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpMem):
        x = MEMT()
        x._UnPack(tmpMem)
        return x

    # MEMT
    def _UnPack(self, MEM):
        if MEM is None:
            return
        if not MEM.ERROR_MODELSIsNone():
            self.ERROR_MODELS = []
            for i in range(MEM.ERROR_MODELSLength()):
                if MEM.ERROR_MODELS(i) is None:
                    self.ERROR_MODELS.append(None)
                else:
                    mEMErrorModel_ = MEMErrorModel.MEMErrorModelT.InitFromObj(MEM.ERROR_MODELS(i))
                    self.ERROR_MODELS.append(mEMErrorModel_)
        if not MEM.EDITING_LEDGERIsNone():
            self.EDITING_LEDGER = []
            for i in range(MEM.EDITING_LEDGERLength()):
                if MEM.EDITING_LEDGER(i) is None:
                    self.EDITING_LEDGER.append(None)
                else:
                    mEMEditingDecision_ = MEMEditingDecision.MEMEditingDecisionT.InitFromObj(MEM.EDITING_LEDGER(i))
                    self.EDITING_LEDGER.append(mEMEditingDecision_)
        self.RANDOM_SEED = MEM.RANDOM_SEED()
        self.TRACE_ID = MEM.TRACE_ID()
        if MEM.ATTESTATION() is not None:
            self.ATTESTATION = MEMAttestation.MEMAttestationT.InitFromObj(MEM.ATTESTATION())

    # MEMT
    def Pack(self, builder):
        if self.ERROR_MODELS is not None:
            ERROR_MODELSlist = []
            for i in range(len(self.ERROR_MODELS)):
                ERROR_MODELSlist.append(self.ERROR_MODELS[i].Pack(builder))
            MEMStartERROR_MODELSVector(builder, len(self.ERROR_MODELS))
            for i in reversed(range(len(self.ERROR_MODELS))):
                builder.PrependUOffsetTRelative(ERROR_MODELSlist[i])
            ERROR_MODELS = builder.EndVector()
        if self.EDITING_LEDGER is not None:
            EDITING_LEDGERlist = []
            for i in range(len(self.EDITING_LEDGER)):
                EDITING_LEDGERlist.append(self.EDITING_LEDGER[i].Pack(builder))
            MEMStartEDITING_LEDGERVector(builder, len(self.EDITING_LEDGER))
            for i in reversed(range(len(self.EDITING_LEDGER))):
                builder.PrependUOffsetTRelative(EDITING_LEDGERlist[i])
            EDITING_LEDGER = builder.EndVector()
        if self.TRACE_ID is not None:
            TRACE_ID = builder.CreateString(self.TRACE_ID)
        if self.ATTESTATION is not None:
            ATTESTATION = self.ATTESTATION.Pack(builder)
        MEMStart(builder)
        if self.ERROR_MODELS is not None:
            MEMAddERROR_MODELS(builder, ERROR_MODELS)
        if self.EDITING_LEDGER is not None:
            MEMAddEDITING_LEDGER(builder, EDITING_LEDGER)
        MEMAddRANDOM_SEED(builder, self.RANDOM_SEED)
        if self.TRACE_ID is not None:
            MEMAddTRACE_ID(builder, TRACE_ID)
        if self.ATTESTATION is not None:
            MEMAddATTESTATION(builder, ATTESTATION)
        MEM = MEMEnd(builder)
        return MEM
