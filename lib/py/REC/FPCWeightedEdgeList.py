# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Bulk weighted-edge ingestion request.
class FPCWeightedEdgeList(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FPCWeightedEdgeList()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFPCWeightedEdgeList(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FPCWeightedEdgeListBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x46\x50\x43", size_prefixed=size_prefixed)

    # FPCWeightedEdgeList
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Optional vertex capacity for implicit graph creation.
    # FPCWeightedEdgeList
    def VERTEX_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Weighted directed edge records.
    # FPCWeightedEdgeList
    def EDGES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 16
            from weightedEdge import weightedEdge
            obj = weightedEdge()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FPCWeightedEdgeList
    def EDGESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FPCWeightedEdgeList
    def EDGESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Build a CSR representation after ingest.
    # FPCWeightedEdgeList
    def BUILD_GRAPH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def FPCWeightedEdgeListStart(builder):
    builder.StartObject(3)

def Start(builder):
    FPCWeightedEdgeListStart(builder)

def FPCWeightedEdgeListAddVERTEX_COUNT(builder, VERTEX_COUNT):
    builder.PrependUint32Slot(0, VERTEX_COUNT, 0)

def AddVERTEX_COUNT(builder, VERTEX_COUNT):
    FPCWeightedEdgeListAddVERTEX_COUNT(builder, VERTEX_COUNT)

def FPCWeightedEdgeListAddEDGES(builder, EDGES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(EDGES), 0)

def AddEDGES(builder, EDGES):
    FPCWeightedEdgeListAddEDGES(builder, EDGES)

def FPCWeightedEdgeListStartEDGESVector(builder, numElems):
    return builder.StartVector(16, numElems, 8)

def StartEDGESVector(builder, numElems):
    return FPCWeightedEdgeListStartEDGESVector(builder, numElems)

def FPCWeightedEdgeListCreateEDGESVector(builder, data):
    data = list(data)
    builder.StartVector(16, len(data), 8)
    for item in reversed(data):
        item.Pack(builder)
    return builder.EndVector()

def CreateEDGESVector(builder, data):
    FPCWeightedEdgeListCreateEDGESVector(builder, data)

def FPCWeightedEdgeListAddBUILD_GRAPH(builder, BUILD_GRAPH):
    builder.PrependBoolSlot(2, BUILD_GRAPH, 0)

def AddBUILD_GRAPH(builder, BUILD_GRAPH):
    FPCWeightedEdgeListAddBUILD_GRAPH(builder, BUILD_GRAPH)

def FPCWeightedEdgeListEnd(builder):
    return builder.EndObject()

def End(builder):
    return FPCWeightedEdgeListEnd(builder)

import weightedEdge
try:
    from typing import List
except:
    pass

class FPCWeightedEdgeListT(object):

    # FPCWeightedEdgeListT
    def __init__(
        self,
        VERTEX_COUNT = 0,
        EDGES = None,
        BUILD_GRAPH = False,
    ):
        self.VERTEX_COUNT = VERTEX_COUNT  # type: int
        self.EDGES = EDGES  # type: Optional[List[weightedEdge.weightedEdgeT]]
        self.BUILD_GRAPH = BUILD_GRAPH  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpFpcweightedEdgeList = FPCWeightedEdgeList()
        tmpFpcweightedEdgeList.Init(buf, pos)
        return cls.InitFromObj(tmpFpcweightedEdgeList)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpFpcweightedEdgeList):
        x = FPCWeightedEdgeListT()
        x._UnPack(tmpFpcweightedEdgeList)
        return x

    # FPCWeightedEdgeListT
    def _UnPack(self, FPCWeightedEdgeList):
        if FPCWeightedEdgeList is None:
            return
        self.VERTEX_COUNT = FPCWeightedEdgeList.VERTEX_COUNT()
        if not FPCWeightedEdgeList.EDGESIsNone():
            self.EDGES = []
            for i in range(FPCWeightedEdgeList.EDGESLength()):
                if FPCWeightedEdgeList.EDGES(i) is None:
                    self.EDGES.append(None)
                else:
                    weightedEdge_ = weightedEdge.weightedEdgeT.InitFromObj(FPCWeightedEdgeList.EDGES(i))
                    self.EDGES.append(weightedEdge_)
        self.BUILD_GRAPH = FPCWeightedEdgeList.BUILD_GRAPH()

    # FPCWeightedEdgeListT
    def Pack(self, builder):
        if self.EDGES is not None:
            FPCWeightedEdgeListStartEDGESVector(builder, len(self.EDGES))
            for i in reversed(range(len(self.EDGES))):
                self.EDGES[i].Pack(builder)
            EDGES = builder.EndVector()
        FPCWeightedEdgeListStart(builder)
        FPCWeightedEdgeListAddVERTEX_COUNT(builder, self.VERTEX_COUNT)
        if self.EDGES is not None:
            FPCWeightedEdgeListAddEDGES(builder, EDGES)
        FPCWeightedEdgeListAddBUILD_GRAPH(builder, self.BUILD_GRAPH)
        FPCWeightedEdgeList = FPCWeightedEdgeListEnd(builder)
        return FPCWeightedEdgeList
