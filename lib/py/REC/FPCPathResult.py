# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Path reconstruction result.
class FPCPathResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FPCPathResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFPCPathResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FPCPathResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x46\x50\x43", size_prefixed=size_prefixed)

    # FPCPathResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Source vertex index from the latest SSSP solve.
    # FPCPathResult
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Requested target vertex index.
    # FPCPathResult
    def TARGET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Whether a path exists from SOURCE to TARGET.
    # FPCPathResult
    def REACHABLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Total distance from SOURCE to TARGET (Infinity if unreachable).
    # FPCPathResult
    def DISTANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Vertex sequence from SOURCE to TARGET.
    # FPCPathResult
    def PATH(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # FPCPathResult
    def PATHAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # FPCPathResult
    def PATHLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FPCPathResult
    def PATHIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def FPCPathResultStart(builder):
    builder.StartObject(5)

def Start(builder):
    FPCPathResultStart(builder)

def FPCPathResultAddSOURCE(builder, SOURCE):
    builder.PrependUint32Slot(0, SOURCE, 0)

def AddSOURCE(builder, SOURCE):
    FPCPathResultAddSOURCE(builder, SOURCE)

def FPCPathResultAddTARGET(builder, TARGET):
    builder.PrependUint32Slot(1, TARGET, 0)

def AddTARGET(builder, TARGET):
    FPCPathResultAddTARGET(builder, TARGET)

def FPCPathResultAddREACHABLE(builder, REACHABLE):
    builder.PrependBoolSlot(2, REACHABLE, 0)

def AddREACHABLE(builder, REACHABLE):
    FPCPathResultAddREACHABLE(builder, REACHABLE)

def FPCPathResultAddDISTANCE(builder, DISTANCE):
    builder.PrependFloat64Slot(3, DISTANCE, 0.0)

def AddDISTANCE(builder, DISTANCE):
    FPCPathResultAddDISTANCE(builder, DISTANCE)

def FPCPathResultAddPATH(builder, PATH):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PATH), 0)

def AddPATH(builder, PATH):
    FPCPathResultAddPATH(builder, PATH)

def FPCPathResultStartPATHVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPATHVector(builder, numElems):
    return FPCPathResultStartPATHVector(builder, numElems)

def FPCPathResultCreatePATHVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreatePATHVector(builder, data):
    FPCPathResultCreatePATHVector(builder, data)

def FPCPathResultEnd(builder):
    return builder.EndObject()

def End(builder):
    return FPCPathResultEnd(builder)

try:
    from typing import List
except:
    pass

class FPCPathResultT(object):

    # FPCPathResultT
    def __init__(
        self,
        SOURCE = 0,
        TARGET = 0,
        REACHABLE = False,
        DISTANCE = 0.0,
        PATH = None,
    ):
        self.SOURCE = SOURCE  # type: int
        self.TARGET = TARGET  # type: int
        self.REACHABLE = REACHABLE  # type: bool
        self.DISTANCE = DISTANCE  # type: float
        self.PATH = PATH  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpFpcpathResult = FPCPathResult()
        tmpFpcpathResult.Init(buf, pos)
        return cls.InitFromObj(tmpFpcpathResult)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpFpcpathResult):
        x = FPCPathResultT()
        x._UnPack(tmpFpcpathResult)
        return x

    # FPCPathResultT
    def _UnPack(self, FPCPathResult):
        if FPCPathResult is None:
            return
        self.SOURCE = FPCPathResult.SOURCE()
        self.TARGET = FPCPathResult.TARGET()
        self.REACHABLE = FPCPathResult.REACHABLE()
        self.DISTANCE = FPCPathResult.DISTANCE()
        if not FPCPathResult.PATHIsNone():
            if np is None:
                self.PATH = []
                for i in range(FPCPathResult.PATHLength()):
                    self.PATH.append(FPCPathResult.PATH(i))
            else:
                self.PATH = FPCPathResult.PATHAsNumpy()

    # FPCPathResultT
    def Pack(self, builder):
        if self.PATH is not None:
            if np is not None and type(self.PATH) is np.ndarray:
                PATH = builder.CreateNumpyVector(self.PATH)
            else:
                FPCPathResultStartPATHVector(builder, len(self.PATH))
                for i in reversed(range(len(self.PATH))):
                    builder.PrependUint32(self.PATH[i])
                PATH = builder.EndVector()
        FPCPathResultStart(builder)
        FPCPathResultAddSOURCE(builder, self.SOURCE)
        FPCPathResultAddTARGET(builder, self.TARGET)
        FPCPathResultAddREACHABLE(builder, self.REACHABLE)
        FPCPathResultAddDISTANCE(builder, self.DISTANCE)
        if self.PATH is not None:
            FPCPathResultAddPATH(builder, PATH)
        FPCPathResult = FPCPathResultEnd(builder)
        return FPCPathResult
