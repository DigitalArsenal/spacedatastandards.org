# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Ingest Resume Mark
class IRM(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IRM()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIRM(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IRMBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x52\x4D", size_prefixed=size_prefixed)

    # IRM
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher-stable identifier of the ingest job. Together with the publishing
    # key it is the job's identity across every mark it emits.
    # IRM
    def JOB_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Monotonic version of this mark within JOB_ID. Highest wins per publishing
    # key. Never reused, never decremented.
    # IRM
    def SEQUENCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Publisher-stable identifier of the data provider being read, verbatim.
    # Publisher data: it may name a real provider.
    # IRM
    def PROVIDER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reverse-DNS identifier of the ingest module or flow that owns the job, so
    # two pipelines reading one provider do not share a resume point.
    # IRM
    def INGESTOR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The source object and its validators.
    # IRM
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from IRMSource import IRMSource
            obj = IRMSource()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Lifecycle of the job.
    # IRM
    def STATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # How byte positions in this mark are addressed.
    # IRM
    def RANGE_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Byte offset at which the next read begins, in the SOURCE's own byte stream:
    # the compressed stream when DECODE_CONTEXT.COMPRESSION is non-empty, in
    # which case it is a record boundary only in company with the decoder state,
    # window and partial record. On an uncompressed source it is ALWAYS a
    # complete-record boundary, corrected backwards from the previous chunk's
    # byte budget. 0 is a valid value and means the job has committed nothing yet.
    # IRM
    def NEXT_OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Zero-based index of the next chunk to read. With PART_INDEX addressing this
    # is the only position.
    # IRM
    def NEXT_CHUNK_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Byte budget the publisher reads per chunk. Records the ceiling the job
    # actually ran under, which a host may have clamped below the configured one.
    # IRM
    def CHUNK_BYTE_BUDGET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Everything needed to decode the chunk at NEXT_OFFSET. REQUIRED in practice
    # whenever NEXT_OFFSET is past the source's own header; a consumer that finds
    # it absent there restarts from offset 0 rather than decoding blind.
    # IRM
    def DECODE_CONTEXT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from IRMDecodeContext import IRMDecodeContext
            obj = IRMDecodeContext()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # The chunk this mark was advanced from — the last one durably committed.
    # IRM
    def LAST_CHUNK(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from IRMChunk import IRMChunk
            obj = IRMChunk()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Chunks durably committed by this job so far.
    # IRM
    def CHUNKS_COMMITTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Source bytes accounted for by committed chunks.
    # IRM
    def BYTES_COMMITTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Records durably committed by this job so far, across every chunk.
    # IRM
    def RECORDS_COMMITTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Four-character standard code of the records this job writes, `$` included,
    # e.g. `$TBS`. Never a union ordinal.
    # IRM
    def TARGET_STANDARD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reconciliation mode the storage lane was called with, verbatim, e.g.
    # `append`. A chunked job never reconciles per batch: reconciling would delete
    # every earlier chunk as the next one arrived.
    # IRM
    def RECONCILE_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Deduplication or merge policy the job applied, verbatim.
    # IRM
    def MERGE_POLICY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The last unrecovered error, when there is one.
    # IRM
    def FAULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from IRMFault import IRMFault
            obj = IRMFault()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # RFC 3339 UTC fixed-millisecond timestamp at which the job was created.
    # IRM
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp at which the first chunk was read.
    # IRM
    def STARTED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp at which this mark was published.
    # With JOB_ID and SEQUENCE this is the version key; SEQUENCE breaks ties.
    # IRM
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp at which the job reached COMPLETE.
    # IRM
    def COMPLETED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp after which a consumer treats the
    # mark as stale and re-validates the source before resuming. Empty means the
    # publisher states no expiry.
    # IRM
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `namespace:value` facets, sorted and deduplicated. See TAG GRAMMAR above.
    # IRM
    def TAGS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # IRM
    def TAGSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # IRM
    def TAGSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        return o == 0

    # Publisher note about this job; never a substitute for a field.
    # IRM
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(54))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def IRMStart(builder):
    builder.StartObject(26)

def Start(builder):
    IRMStart(builder)

def IRMAddJOB_ID(builder, JOB_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(JOB_ID), 0)

def AddJOB_ID(builder, JOB_ID):
    IRMAddJOB_ID(builder, JOB_ID)

def IRMAddSEQUENCE(builder, SEQUENCE):
    builder.PrependUint64Slot(1, SEQUENCE, 0)

def AddSEQUENCE(builder, SEQUENCE):
    IRMAddSEQUENCE(builder, SEQUENCE)

def IRMAddPROVIDER_ID(builder, PROVIDER_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_ID), 0)

def AddPROVIDER_ID(builder, PROVIDER_ID):
    IRMAddPROVIDER_ID(builder, PROVIDER_ID)

def IRMAddINGESTOR_ID(builder, INGESTOR_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(INGESTOR_ID), 0)

def AddINGESTOR_ID(builder, INGESTOR_ID):
    IRMAddINGESTOR_ID(builder, INGESTOR_ID)

def IRMAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    IRMAddSOURCE(builder, SOURCE)

def IRMAddSTATE(builder, STATE):
    builder.PrependInt8Slot(5, STATE, 0)

def AddSTATE(builder, STATE):
    IRMAddSTATE(builder, STATE)

def IRMAddRANGE_MODE(builder, RANGE_MODE):
    builder.PrependInt8Slot(6, RANGE_MODE, 0)

def AddRANGE_MODE(builder, RANGE_MODE):
    IRMAddRANGE_MODE(builder, RANGE_MODE)

def IRMAddNEXT_OFFSET(builder, NEXT_OFFSET):
    builder.PrependUint64Slot(7, NEXT_OFFSET, 0)

def AddNEXT_OFFSET(builder, NEXT_OFFSET):
    IRMAddNEXT_OFFSET(builder, NEXT_OFFSET)

def IRMAddNEXT_CHUNK_INDEX(builder, NEXT_CHUNK_INDEX):
    builder.PrependUint32Slot(8, NEXT_CHUNK_INDEX, 0)

def AddNEXT_CHUNK_INDEX(builder, NEXT_CHUNK_INDEX):
    IRMAddNEXT_CHUNK_INDEX(builder, NEXT_CHUNK_INDEX)

def IRMAddCHUNK_BYTE_BUDGET(builder, CHUNK_BYTE_BUDGET):
    builder.PrependUint64Slot(9, CHUNK_BYTE_BUDGET, 0)

def AddCHUNK_BYTE_BUDGET(builder, CHUNK_BYTE_BUDGET):
    IRMAddCHUNK_BYTE_BUDGET(builder, CHUNK_BYTE_BUDGET)

def IRMAddDECODE_CONTEXT(builder, DECODE_CONTEXT):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(DECODE_CONTEXT), 0)

def AddDECODE_CONTEXT(builder, DECODE_CONTEXT):
    IRMAddDECODE_CONTEXT(builder, DECODE_CONTEXT)

def IRMAddLAST_CHUNK(builder, LAST_CHUNK):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(LAST_CHUNK), 0)

def AddLAST_CHUNK(builder, LAST_CHUNK):
    IRMAddLAST_CHUNK(builder, LAST_CHUNK)

def IRMAddCHUNKS_COMMITTED(builder, CHUNKS_COMMITTED):
    builder.PrependUint32Slot(12, CHUNKS_COMMITTED, 0)

def AddCHUNKS_COMMITTED(builder, CHUNKS_COMMITTED):
    IRMAddCHUNKS_COMMITTED(builder, CHUNKS_COMMITTED)

def IRMAddBYTES_COMMITTED(builder, BYTES_COMMITTED):
    builder.PrependUint64Slot(13, BYTES_COMMITTED, 0)

def AddBYTES_COMMITTED(builder, BYTES_COMMITTED):
    IRMAddBYTES_COMMITTED(builder, BYTES_COMMITTED)

def IRMAddRECORDS_COMMITTED(builder, RECORDS_COMMITTED):
    builder.PrependUint64Slot(14, RECORDS_COMMITTED, 0)

def AddRECORDS_COMMITTED(builder, RECORDS_COMMITTED):
    IRMAddRECORDS_COMMITTED(builder, RECORDS_COMMITTED)

def IRMAddTARGET_STANDARD(builder, TARGET_STANDARD):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(TARGET_STANDARD), 0)

def AddTARGET_STANDARD(builder, TARGET_STANDARD):
    IRMAddTARGET_STANDARD(builder, TARGET_STANDARD)

def IRMAddRECONCILE_MODE(builder, RECONCILE_MODE):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(RECONCILE_MODE), 0)

def AddRECONCILE_MODE(builder, RECONCILE_MODE):
    IRMAddRECONCILE_MODE(builder, RECONCILE_MODE)

def IRMAddMERGE_POLICY(builder, MERGE_POLICY):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(MERGE_POLICY), 0)

def AddMERGE_POLICY(builder, MERGE_POLICY):
    IRMAddMERGE_POLICY(builder, MERGE_POLICY)

def IRMAddFAULT(builder, FAULT):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(FAULT), 0)

def AddFAULT(builder, FAULT):
    IRMAddFAULT(builder, FAULT)

def IRMAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUOffsetTRelativeSlot(19, flatbuffers.number_types.UOffsetTFlags.py_type(CREATED_AT), 0)

def AddCREATED_AT(builder, CREATED_AT):
    IRMAddCREATED_AT(builder, CREATED_AT)

def IRMAddSTARTED_AT(builder, STARTED_AT):
    builder.PrependUOffsetTRelativeSlot(20, flatbuffers.number_types.UOffsetTFlags.py_type(STARTED_AT), 0)

def AddSTARTED_AT(builder, STARTED_AT):
    IRMAddSTARTED_AT(builder, STARTED_AT)

def IRMAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUOffsetTRelativeSlot(21, flatbuffers.number_types.UOffsetTFlags.py_type(UPDATED_AT), 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    IRMAddUPDATED_AT(builder, UPDATED_AT)

def IRMAddCOMPLETED_AT(builder, COMPLETED_AT):
    builder.PrependUOffsetTRelativeSlot(22, flatbuffers.number_types.UOffsetTFlags.py_type(COMPLETED_AT), 0)

def AddCOMPLETED_AT(builder, COMPLETED_AT):
    IRMAddCOMPLETED_AT(builder, COMPLETED_AT)

def IRMAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUOffsetTRelativeSlot(23, flatbuffers.number_types.UOffsetTFlags.py_type(EXPIRES_AT), 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    IRMAddEXPIRES_AT(builder, EXPIRES_AT)

def IRMAddTAGS(builder, TAGS):
    builder.PrependUOffsetTRelativeSlot(24, flatbuffers.number_types.UOffsetTFlags.py_type(TAGS), 0)

def AddTAGS(builder, TAGS):
    IRMAddTAGS(builder, TAGS)

def IRMStartTAGSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTAGSVector(builder, numElems):
    return IRMStartTAGSVector(builder, numElems)

def IRMCreateTAGSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateTAGSVector(builder, data):
    IRMCreateTAGSVector(builder, data)

def IRMAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(25, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    IRMAddNOTES(builder, NOTES)

def IRMEnd(builder):
    return builder.EndObject()

def End(builder):
    return IRMEnd(builder)

import IRMChunk
import IRMDecodeContext
import IRMFault
import IRMSource
try:
    from typing import List, Optional
except:
    pass

class IRMT(object):

    # IRMT
    def __init__(
        self,
        JOB_ID = None,
        SEQUENCE = 0,
        PROVIDER_ID = None,
        INGESTOR_ID = None,
        SOURCE = None,
        STATE = 0,
        RANGE_MODE = 0,
        NEXT_OFFSET = 0,
        NEXT_CHUNK_INDEX = 0,
        CHUNK_BYTE_BUDGET = 0,
        DECODE_CONTEXT = None,
        LAST_CHUNK = None,
        CHUNKS_COMMITTED = 0,
        BYTES_COMMITTED = 0,
        RECORDS_COMMITTED = 0,
        TARGET_STANDARD = None,
        RECONCILE_MODE = None,
        MERGE_POLICY = None,
        FAULT = None,
        CREATED_AT = None,
        STARTED_AT = None,
        UPDATED_AT = None,
        COMPLETED_AT = None,
        EXPIRES_AT = None,
        TAGS = None,
        NOTES = None,
    ):
        self.JOB_ID = JOB_ID  # type: Optional[str]
        self.SEQUENCE = SEQUENCE  # type: int
        self.PROVIDER_ID = PROVIDER_ID  # type: Optional[str]
        self.INGESTOR_ID = INGESTOR_ID  # type: Optional[str]
        self.SOURCE = SOURCE  # type: Optional[IRMSource.IRMSourceT]
        self.STATE = STATE  # type: int
        self.RANGE_MODE = RANGE_MODE  # type: int
        self.NEXT_OFFSET = NEXT_OFFSET  # type: int
        self.NEXT_CHUNK_INDEX = NEXT_CHUNK_INDEX  # type: int
        self.CHUNK_BYTE_BUDGET = CHUNK_BYTE_BUDGET  # type: int
        self.DECODE_CONTEXT = DECODE_CONTEXT  # type: Optional[IRMDecodeContext.IRMDecodeContextT]
        self.LAST_CHUNK = LAST_CHUNK  # type: Optional[IRMChunk.IRMChunkT]
        self.CHUNKS_COMMITTED = CHUNKS_COMMITTED  # type: int
        self.BYTES_COMMITTED = BYTES_COMMITTED  # type: int
        self.RECORDS_COMMITTED = RECORDS_COMMITTED  # type: int
        self.TARGET_STANDARD = TARGET_STANDARD  # type: Optional[str]
        self.RECONCILE_MODE = RECONCILE_MODE  # type: Optional[str]
        self.MERGE_POLICY = MERGE_POLICY  # type: Optional[str]
        self.FAULT = FAULT  # type: Optional[IRMFault.IRMFaultT]
        self.CREATED_AT = CREATED_AT  # type: Optional[str]
        self.STARTED_AT = STARTED_AT  # type: Optional[str]
        self.UPDATED_AT = UPDATED_AT  # type: Optional[str]
        self.COMPLETED_AT = COMPLETED_AT  # type: Optional[str]
        self.EXPIRES_AT = EXPIRES_AT  # type: Optional[str]
        self.TAGS = TAGS  # type: Optional[List[Optional[str]]]
        self.NOTES = NOTES  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIrm = IRM()
        tmpIrm.Init(buf, pos)
        return cls.InitFromObj(tmpIrm)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIrm):
        x = IRMT()
        x._UnPack(tmpIrm)
        return x

    # IRMT
    def _UnPack(self, IRM):
        if IRM is None:
            return
        self.JOB_ID = IRM.JOB_ID()
        self.SEQUENCE = IRM.SEQUENCE()
        self.PROVIDER_ID = IRM.PROVIDER_ID()
        self.INGESTOR_ID = IRM.INGESTOR_ID()
        if IRM.SOURCE() is not None:
            self.SOURCE = IRMSource.IRMSourceT.InitFromObj(IRM.SOURCE())
        self.STATE = IRM.STATE()
        self.RANGE_MODE = IRM.RANGE_MODE()
        self.NEXT_OFFSET = IRM.NEXT_OFFSET()
        self.NEXT_CHUNK_INDEX = IRM.NEXT_CHUNK_INDEX()
        self.CHUNK_BYTE_BUDGET = IRM.CHUNK_BYTE_BUDGET()
        if IRM.DECODE_CONTEXT() is not None:
            self.DECODE_CONTEXT = IRMDecodeContext.IRMDecodeContextT.InitFromObj(IRM.DECODE_CONTEXT())
        if IRM.LAST_CHUNK() is not None:
            self.LAST_CHUNK = IRMChunk.IRMChunkT.InitFromObj(IRM.LAST_CHUNK())
        self.CHUNKS_COMMITTED = IRM.CHUNKS_COMMITTED()
        self.BYTES_COMMITTED = IRM.BYTES_COMMITTED()
        self.RECORDS_COMMITTED = IRM.RECORDS_COMMITTED()
        self.TARGET_STANDARD = IRM.TARGET_STANDARD()
        self.RECONCILE_MODE = IRM.RECONCILE_MODE()
        self.MERGE_POLICY = IRM.MERGE_POLICY()
        if IRM.FAULT() is not None:
            self.FAULT = IRMFault.IRMFaultT.InitFromObj(IRM.FAULT())
        self.CREATED_AT = IRM.CREATED_AT()
        self.STARTED_AT = IRM.STARTED_AT()
        self.UPDATED_AT = IRM.UPDATED_AT()
        self.COMPLETED_AT = IRM.COMPLETED_AT()
        self.EXPIRES_AT = IRM.EXPIRES_AT()
        if not IRM.TAGSIsNone():
            self.TAGS = []
            for i in range(IRM.TAGSLength()):
                self.TAGS.append(IRM.TAGS(i))
        self.NOTES = IRM.NOTES()

    # IRMT
    def Pack(self, builder):
        if self.JOB_ID is not None:
            JOB_ID = builder.CreateString(self.JOB_ID)
        if self.PROVIDER_ID is not None:
            PROVIDER_ID = builder.CreateString(self.PROVIDER_ID)
        if self.INGESTOR_ID is not None:
            INGESTOR_ID = builder.CreateString(self.INGESTOR_ID)
        if self.SOURCE is not None:
            SOURCE = self.SOURCE.Pack(builder)
        if self.DECODE_CONTEXT is not None:
            DECODE_CONTEXT = self.DECODE_CONTEXT.Pack(builder)
        if self.LAST_CHUNK is not None:
            LAST_CHUNK = self.LAST_CHUNK.Pack(builder)
        if self.TARGET_STANDARD is not None:
            TARGET_STANDARD = builder.CreateString(self.TARGET_STANDARD)
        if self.RECONCILE_MODE is not None:
            RECONCILE_MODE = builder.CreateString(self.RECONCILE_MODE)
        if self.MERGE_POLICY is not None:
            MERGE_POLICY = builder.CreateString(self.MERGE_POLICY)
        if self.FAULT is not None:
            FAULT = self.FAULT.Pack(builder)
        if self.CREATED_AT is not None:
            CREATED_AT = builder.CreateString(self.CREATED_AT)
        if self.STARTED_AT is not None:
            STARTED_AT = builder.CreateString(self.STARTED_AT)
        if self.UPDATED_AT is not None:
            UPDATED_AT = builder.CreateString(self.UPDATED_AT)
        if self.COMPLETED_AT is not None:
            COMPLETED_AT = builder.CreateString(self.COMPLETED_AT)
        if self.EXPIRES_AT is not None:
            EXPIRES_AT = builder.CreateString(self.EXPIRES_AT)
        if self.TAGS is not None:
            TAGSlist = []
            for i in range(len(self.TAGS)):
                TAGSlist.append(builder.CreateString(self.TAGS[i]))
            IRMStartTAGSVector(builder, len(self.TAGS))
            for i in reversed(range(len(self.TAGS))):
                builder.PrependUOffsetTRelative(TAGSlist[i])
            TAGS = builder.EndVector()
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        IRMStart(builder)
        if self.JOB_ID is not None:
            IRMAddJOB_ID(builder, JOB_ID)
        IRMAddSEQUENCE(builder, self.SEQUENCE)
        if self.PROVIDER_ID is not None:
            IRMAddPROVIDER_ID(builder, PROVIDER_ID)
        if self.INGESTOR_ID is not None:
            IRMAddINGESTOR_ID(builder, INGESTOR_ID)
        if self.SOURCE is not None:
            IRMAddSOURCE(builder, SOURCE)
        IRMAddSTATE(builder, self.STATE)
        IRMAddRANGE_MODE(builder, self.RANGE_MODE)
        IRMAddNEXT_OFFSET(builder, self.NEXT_OFFSET)
        IRMAddNEXT_CHUNK_INDEX(builder, self.NEXT_CHUNK_INDEX)
        IRMAddCHUNK_BYTE_BUDGET(builder, self.CHUNK_BYTE_BUDGET)
        if self.DECODE_CONTEXT is not None:
            IRMAddDECODE_CONTEXT(builder, DECODE_CONTEXT)
        if self.LAST_CHUNK is not None:
            IRMAddLAST_CHUNK(builder, LAST_CHUNK)
        IRMAddCHUNKS_COMMITTED(builder, self.CHUNKS_COMMITTED)
        IRMAddBYTES_COMMITTED(builder, self.BYTES_COMMITTED)
        IRMAddRECORDS_COMMITTED(builder, self.RECORDS_COMMITTED)
        if self.TARGET_STANDARD is not None:
            IRMAddTARGET_STANDARD(builder, TARGET_STANDARD)
        if self.RECONCILE_MODE is not None:
            IRMAddRECONCILE_MODE(builder, RECONCILE_MODE)
        if self.MERGE_POLICY is not None:
            IRMAddMERGE_POLICY(builder, MERGE_POLICY)
        if self.FAULT is not None:
            IRMAddFAULT(builder, FAULT)
        if self.CREATED_AT is not None:
            IRMAddCREATED_AT(builder, CREATED_AT)
        if self.STARTED_AT is not None:
            IRMAddSTARTED_AT(builder, STARTED_AT)
        if self.UPDATED_AT is not None:
            IRMAddUPDATED_AT(builder, UPDATED_AT)
        if self.COMPLETED_AT is not None:
            IRMAddCOMPLETED_AT(builder, COMPLETED_AT)
        if self.EXPIRES_AT is not None:
            IRMAddEXPIRES_AT(builder, EXPIRES_AT)
        if self.TAGS is not None:
            IRMAddTAGS(builder, TAGS)
        if self.NOTES is not None:
            IRMAddNOTES(builder, NOTES)
        IRM = IRMEnd(builder)
        return IRM
