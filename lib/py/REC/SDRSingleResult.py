# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Compact single-frame detection result.
class SDRSingleResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDRSingleResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDRSingleResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDRSingleResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x52", size_prefixed=size_prefixed)

    # SDRSingleResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SDRSingleResult
    def DETECTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Range to target (meters).
    # SDRSingleResult
    def RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Signal-to-noise ratio (dB).
    # SDRSingleResult
    def SNR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Azimuth angle to target (radians).
    # SDRSingleResult
    def AZIMUTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Elevation angle to target (radians).
    # SDRSingleResult
    def ELEVATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Range rate / Doppler velocity (m/s).
    # SDRSingleResult
    def RANGE_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Detection timestamp (milliseconds since epoch).
    # SDRSingleResult
    def TIMESTAMP_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Track identifier (0 if not tracking).
    # SDRSingleResult
    def TRACK_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def SDRSingleResultStart(builder):
    builder.StartObject(8)

def Start(builder):
    SDRSingleResultStart(builder)

def SDRSingleResultAddDETECTED(builder, DETECTED):
    builder.PrependBoolSlot(0, DETECTED, 0)

def AddDETECTED(builder, DETECTED):
    SDRSingleResultAddDETECTED(builder, DETECTED)

def SDRSingleResultAddRANGE(builder, RANGE):
    builder.PrependFloat64Slot(1, RANGE, 0.0)

def AddRANGE(builder, RANGE):
    SDRSingleResultAddRANGE(builder, RANGE)

def SDRSingleResultAddSNR(builder, SNR):
    builder.PrependFloat64Slot(2, SNR, 0.0)

def AddSNR(builder, SNR):
    SDRSingleResultAddSNR(builder, SNR)

def SDRSingleResultAddAZIMUTH(builder, AZIMUTH):
    builder.PrependFloat64Slot(3, AZIMUTH, 0.0)

def AddAZIMUTH(builder, AZIMUTH):
    SDRSingleResultAddAZIMUTH(builder, AZIMUTH)

def SDRSingleResultAddELEVATION(builder, ELEVATION):
    builder.PrependFloat64Slot(4, ELEVATION, 0.0)

def AddELEVATION(builder, ELEVATION):
    SDRSingleResultAddELEVATION(builder, ELEVATION)

def SDRSingleResultAddRANGE_RATE(builder, RANGE_RATE):
    builder.PrependFloat64Slot(5, RANGE_RATE, 0.0)

def AddRANGE_RATE(builder, RANGE_RATE):
    SDRSingleResultAddRANGE_RATE(builder, RANGE_RATE)

def SDRSingleResultAddTIMESTAMP_MS(builder, TIMESTAMP_MS):
    builder.PrependUint64Slot(6, TIMESTAMP_MS, 0)

def AddTIMESTAMP_MS(builder, TIMESTAMP_MS):
    SDRSingleResultAddTIMESTAMP_MS(builder, TIMESTAMP_MS)

def SDRSingleResultAddTRACK_ID(builder, TRACK_ID):
    builder.PrependUint32Slot(7, TRACK_ID, 0)

def AddTRACK_ID(builder, TRACK_ID):
    SDRSingleResultAddTRACK_ID(builder, TRACK_ID)

def SDRSingleResultEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDRSingleResultEnd(builder)


class SDRSingleResultT(object):

    # SDRSingleResultT
    def __init__(
        self,
        DETECTED = False,
        RANGE = 0.0,
        SNR = 0.0,
        AZIMUTH = 0.0,
        ELEVATION = 0.0,
        RANGE_RATE = 0.0,
        TIMESTAMP_MS = 0,
        TRACK_ID = 0,
    ):
        self.DETECTED = DETECTED  # type: bool
        self.RANGE = RANGE  # type: float
        self.SNR = SNR  # type: float
        self.AZIMUTH = AZIMUTH  # type: float
        self.ELEVATION = ELEVATION  # type: float
        self.RANGE_RATE = RANGE_RATE  # type: float
        self.TIMESTAMP_MS = TIMESTAMP_MS  # type: int
        self.TRACK_ID = TRACK_ID  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSdrsingleResult = SDRSingleResult()
        tmpSdrsingleResult.Init(buf, pos)
        return cls.InitFromObj(tmpSdrsingleResult)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSdrsingleResult):
        x = SDRSingleResultT()
        x._UnPack(tmpSdrsingleResult)
        return x

    # SDRSingleResultT
    def _UnPack(self, SDRSingleResult):
        if SDRSingleResult is None:
            return
        self.DETECTED = SDRSingleResult.DETECTED()
        self.RANGE = SDRSingleResult.RANGE()
        self.SNR = SDRSingleResult.SNR()
        self.AZIMUTH = SDRSingleResult.AZIMUTH()
        self.ELEVATION = SDRSingleResult.ELEVATION()
        self.RANGE_RATE = SDRSingleResult.RANGE_RATE()
        self.TIMESTAMP_MS = SDRSingleResult.TIMESTAMP_MS()
        self.TRACK_ID = SDRSingleResult.TRACK_ID()

    # SDRSingleResultT
    def Pack(self, builder):
        SDRSingleResultStart(builder)
        SDRSingleResultAddDETECTED(builder, self.DETECTED)
        SDRSingleResultAddRANGE(builder, self.RANGE)
        SDRSingleResultAddSNR(builder, self.SNR)
        SDRSingleResultAddAZIMUTH(builder, self.AZIMUTH)
        SDRSingleResultAddELEVATION(builder, self.ELEVATION)
        SDRSingleResultAddRANGE_RATE(builder, self.RANGE_RATE)
        SDRSingleResultAddTIMESTAMP_MS(builder, self.TIMESTAMP_MS)
        SDRSingleResultAddTRACK_ID(builder, self.TRACK_ID)
        SDRSingleResult = SDRSingleResultEnd(builder)
        return SDRSingleResult
