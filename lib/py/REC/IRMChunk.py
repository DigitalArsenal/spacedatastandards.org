# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One chunk that was read and durably committed.
class IRMChunk(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IRMChunk()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIRMChunk(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IRMChunkBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x49\x52\x4D", size_prefixed=size_prefixed)

    # IRMChunk
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Zero-based position of the chunk in the job's read sequence.
    # IRMChunk
    def CHUNK_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Publisher-stable batch identifier the chunk's records were stored under,
    # verbatim, so a consumer can join the mark to the stored rows.
    # IRMChunk
    def BATCH_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # First byte of the range that was read.
    # IRMChunk
    def RANGE_FIRST_BYTE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Last byte of the range that was read. Interpreted per RANGE_MODE:
    # inclusive under INCLUSIVE_BYTE_RANGE, exclusive under OFFSET_LENGTH.
    # IRMChunk
    def RANGE_LAST_BYTE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Bytes the read actually returned, which may be shorter than the range
    # requested.
    # IRMChunk
    def BYTE_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SHA-256 of the chunk's bytes as 64 lowercase hexadecimal characters.
    # IRMChunk
    def CHUNK_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Records the decoder produced from the chunk.
    # IRMChunk
    def RECORDS_DECODED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Records the storage lane reported as durably committed. A chunk whose
    # stored count is 0 is not marked at all.
    # IRMChunk
    def RECORDS_STORED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Records the storage lane rejected or collapsed as duplicates.
    # IRMChunk
    def RECORDS_REJECTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Number of read attempts this chunk cost, including the successful one.
    # IRMChunk
    def ATTEMPTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # RFC 3339 UTC fixed-millisecond timestamp at which the storage lane
    # confirmed the commit.
    # IRMChunk
    def COMMITTED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def IRMChunkStart(builder):
    builder.StartObject(11)

def Start(builder):
    IRMChunkStart(builder)

def IRMChunkAddCHUNK_INDEX(builder, CHUNK_INDEX):
    builder.PrependUint32Slot(0, CHUNK_INDEX, 0)

def AddCHUNK_INDEX(builder, CHUNK_INDEX):
    IRMChunkAddCHUNK_INDEX(builder, CHUNK_INDEX)

def IRMChunkAddBATCH_ID(builder, BATCH_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(BATCH_ID), 0)

def AddBATCH_ID(builder, BATCH_ID):
    IRMChunkAddBATCH_ID(builder, BATCH_ID)

def IRMChunkAddRANGE_FIRST_BYTE(builder, RANGE_FIRST_BYTE):
    builder.PrependUint64Slot(2, RANGE_FIRST_BYTE, 0)

def AddRANGE_FIRST_BYTE(builder, RANGE_FIRST_BYTE):
    IRMChunkAddRANGE_FIRST_BYTE(builder, RANGE_FIRST_BYTE)

def IRMChunkAddRANGE_LAST_BYTE(builder, RANGE_LAST_BYTE):
    builder.PrependUint64Slot(3, RANGE_LAST_BYTE, 0)

def AddRANGE_LAST_BYTE(builder, RANGE_LAST_BYTE):
    IRMChunkAddRANGE_LAST_BYTE(builder, RANGE_LAST_BYTE)

def IRMChunkAddBYTE_LENGTH(builder, BYTE_LENGTH):
    builder.PrependUint64Slot(4, BYTE_LENGTH, 0)

def AddBYTE_LENGTH(builder, BYTE_LENGTH):
    IRMChunkAddBYTE_LENGTH(builder, BYTE_LENGTH)

def IRMChunkAddCHUNK_SHA256(builder, CHUNK_SHA256):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(CHUNK_SHA256), 0)

def AddCHUNK_SHA256(builder, CHUNK_SHA256):
    IRMChunkAddCHUNK_SHA256(builder, CHUNK_SHA256)

def IRMChunkAddRECORDS_DECODED(builder, RECORDS_DECODED):
    builder.PrependUint32Slot(6, RECORDS_DECODED, 0)

def AddRECORDS_DECODED(builder, RECORDS_DECODED):
    IRMChunkAddRECORDS_DECODED(builder, RECORDS_DECODED)

def IRMChunkAddRECORDS_STORED(builder, RECORDS_STORED):
    builder.PrependUint32Slot(7, RECORDS_STORED, 0)

def AddRECORDS_STORED(builder, RECORDS_STORED):
    IRMChunkAddRECORDS_STORED(builder, RECORDS_STORED)

def IRMChunkAddRECORDS_REJECTED(builder, RECORDS_REJECTED):
    builder.PrependUint32Slot(8, RECORDS_REJECTED, 0)

def AddRECORDS_REJECTED(builder, RECORDS_REJECTED):
    IRMChunkAddRECORDS_REJECTED(builder, RECORDS_REJECTED)

def IRMChunkAddATTEMPTS(builder, ATTEMPTS):
    builder.PrependUint32Slot(9, ATTEMPTS, 0)

def AddATTEMPTS(builder, ATTEMPTS):
    IRMChunkAddATTEMPTS(builder, ATTEMPTS)

def IRMChunkAddCOMMITTED_AT(builder, COMMITTED_AT):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(COMMITTED_AT), 0)

def AddCOMMITTED_AT(builder, COMMITTED_AT):
    IRMChunkAddCOMMITTED_AT(builder, COMMITTED_AT)

def IRMChunkEnd(builder):
    return builder.EndObject()

def End(builder):
    return IRMChunkEnd(builder)


class IRMChunkT(object):

    # IRMChunkT
    def __init__(
        self,
        CHUNK_INDEX = 0,
        BATCH_ID = None,
        RANGE_FIRST_BYTE = 0,
        RANGE_LAST_BYTE = 0,
        BYTE_LENGTH = 0,
        CHUNK_SHA256 = None,
        RECORDS_DECODED = 0,
        RECORDS_STORED = 0,
        RECORDS_REJECTED = 0,
        ATTEMPTS = 0,
        COMMITTED_AT = None,
    ):
        self.CHUNK_INDEX = CHUNK_INDEX  # type: int
        self.BATCH_ID = BATCH_ID  # type: Optional[str]
        self.RANGE_FIRST_BYTE = RANGE_FIRST_BYTE  # type: int
        self.RANGE_LAST_BYTE = RANGE_LAST_BYTE  # type: int
        self.BYTE_LENGTH = BYTE_LENGTH  # type: int
        self.CHUNK_SHA256 = CHUNK_SHA256  # type: Optional[str]
        self.RECORDS_DECODED = RECORDS_DECODED  # type: int
        self.RECORDS_STORED = RECORDS_STORED  # type: int
        self.RECORDS_REJECTED = RECORDS_REJECTED  # type: int
        self.ATTEMPTS = ATTEMPTS  # type: int
        self.COMMITTED_AT = COMMITTED_AT  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpIrmchunk = IRMChunk()
        tmpIrmchunk.Init(buf, pos)
        return cls.InitFromObj(tmpIrmchunk)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpIrmchunk):
        x = IRMChunkT()
        x._UnPack(tmpIrmchunk)
        return x

    # IRMChunkT
    def _UnPack(self, IRMChunk):
        if IRMChunk is None:
            return
        self.CHUNK_INDEX = IRMChunk.CHUNK_INDEX()
        self.BATCH_ID = IRMChunk.BATCH_ID()
        self.RANGE_FIRST_BYTE = IRMChunk.RANGE_FIRST_BYTE()
        self.RANGE_LAST_BYTE = IRMChunk.RANGE_LAST_BYTE()
        self.BYTE_LENGTH = IRMChunk.BYTE_LENGTH()
        self.CHUNK_SHA256 = IRMChunk.CHUNK_SHA256()
        self.RECORDS_DECODED = IRMChunk.RECORDS_DECODED()
        self.RECORDS_STORED = IRMChunk.RECORDS_STORED()
        self.RECORDS_REJECTED = IRMChunk.RECORDS_REJECTED()
        self.ATTEMPTS = IRMChunk.ATTEMPTS()
        self.COMMITTED_AT = IRMChunk.COMMITTED_AT()

    # IRMChunkT
    def Pack(self, builder):
        if self.BATCH_ID is not None:
            BATCH_ID = builder.CreateString(self.BATCH_ID)
        if self.CHUNK_SHA256 is not None:
            CHUNK_SHA256 = builder.CreateString(self.CHUNK_SHA256)
        if self.COMMITTED_AT is not None:
            COMMITTED_AT = builder.CreateString(self.COMMITTED_AT)
        IRMChunkStart(builder)
        IRMChunkAddCHUNK_INDEX(builder, self.CHUNK_INDEX)
        if self.BATCH_ID is not None:
            IRMChunkAddBATCH_ID(builder, BATCH_ID)
        IRMChunkAddRANGE_FIRST_BYTE(builder, self.RANGE_FIRST_BYTE)
        IRMChunkAddRANGE_LAST_BYTE(builder, self.RANGE_LAST_BYTE)
        IRMChunkAddBYTE_LENGTH(builder, self.BYTE_LENGTH)
        if self.CHUNK_SHA256 is not None:
            IRMChunkAddCHUNK_SHA256(builder, CHUNK_SHA256)
        IRMChunkAddRECORDS_DECODED(builder, self.RECORDS_DECODED)
        IRMChunkAddRECORDS_STORED(builder, self.RECORDS_STORED)
        IRMChunkAddRECORDS_REJECTED(builder, self.RECORDS_REJECTED)
        IRMChunkAddATTEMPTS(builder, self.ATTEMPTS)
        if self.COMMITTED_AT is not None:
            IRMChunkAddCOMMITTED_AT(builder, COMMITTED_AT)
        IRMChunk = IRMChunkEnd(builder)
        return IRMChunk
