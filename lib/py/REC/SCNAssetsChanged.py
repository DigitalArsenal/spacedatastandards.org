# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Scenario asset-change notification payload.
class SCNAssetsChanged(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCNAssetsChanged()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCNAssetsChanged(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCNAssetsChangedBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x4E", size_prefixed=size_prefixed)

    # SCNAssetsChanged
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Satellite references added by the asset-change event.
    # SCNAssetsChanged
    def ADDED_SATELLITES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCNReference import SCNReference
            obj = SCNReference()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNAssetsChanged
    def ADDED_SATELLITESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNAssetsChanged
    def ADDED_SATELLITESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Ground-site references added by the asset-change event.
    # SCNAssetsChanged
    def ADDED_GROUND_SITES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCNReference import SCNReference
            obj = SCNReference()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNAssetsChanged
    def ADDED_GROUND_SITESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNAssetsChanged
    def ADDED_GROUND_SITESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Satellite references removed by the asset-change event.
    # SCNAssetsChanged
    def REMOVED_SATELLITES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCNReference import SCNReference
            obj = SCNReference()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNAssetsChanged
    def REMOVED_SATELLITESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNAssetsChanged
    def REMOVED_SATELLITESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Ground-site references removed by the asset-change event.
    # SCNAssetsChanged
    def REMOVED_GROUND_SITES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCNReference import SCNReference
            obj = SCNReference()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNAssetsChanged
    def REMOVED_GROUND_SITESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNAssetsChanged
    def REMOVED_GROUND_SITESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Current satellite references after the asset-change event.
    # SCNAssetsChanged
    def SATELLITES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCNReference import SCNReference
            obj = SCNReference()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNAssetsChanged
    def SATELLITESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNAssetsChanged
    def SATELLITESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Current ground-site references after the asset-change event.
    # SCNAssetsChanged
    def GROUND_SITES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCNReference import SCNReference
            obj = SCNReference()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNAssetsChanged
    def GROUND_SITESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNAssetsChanged
    def GROUND_SITESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Current full reference set after the asset-change event.
    # SCNAssetsChanged
    def REFERENCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCNReference import SCNReference
            obj = SCNReference()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCNAssetsChanged
    def REFERENCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCNAssetsChanged
    def REFERENCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def SCNAssetsChangedStart(builder):
    builder.StartObject(7)

def Start(builder):
    SCNAssetsChangedStart(builder)

def SCNAssetsChangedAddADDED_SATELLITES(builder, ADDED_SATELLITES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ADDED_SATELLITES), 0)

def AddADDED_SATELLITES(builder, ADDED_SATELLITES):
    SCNAssetsChangedAddADDED_SATELLITES(builder, ADDED_SATELLITES)

def SCNAssetsChangedStartADDED_SATELLITESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartADDED_SATELLITESVector(builder, numElems):
    return SCNAssetsChangedStartADDED_SATELLITESVector(builder, numElems)

def SCNAssetsChangedCreateADDED_SATELLITESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateADDED_SATELLITESVector(builder, data):
    SCNAssetsChangedCreateADDED_SATELLITESVector(builder, data)

def SCNAssetsChangedAddADDED_GROUND_SITES(builder, ADDED_GROUND_SITES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ADDED_GROUND_SITES), 0)

def AddADDED_GROUND_SITES(builder, ADDED_GROUND_SITES):
    SCNAssetsChangedAddADDED_GROUND_SITES(builder, ADDED_GROUND_SITES)

def SCNAssetsChangedStartADDED_GROUND_SITESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartADDED_GROUND_SITESVector(builder, numElems):
    return SCNAssetsChangedStartADDED_GROUND_SITESVector(builder, numElems)

def SCNAssetsChangedCreateADDED_GROUND_SITESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateADDED_GROUND_SITESVector(builder, data):
    SCNAssetsChangedCreateADDED_GROUND_SITESVector(builder, data)

def SCNAssetsChangedAddREMOVED_SATELLITES(builder, REMOVED_SATELLITES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(REMOVED_SATELLITES), 0)

def AddREMOVED_SATELLITES(builder, REMOVED_SATELLITES):
    SCNAssetsChangedAddREMOVED_SATELLITES(builder, REMOVED_SATELLITES)

def SCNAssetsChangedStartREMOVED_SATELLITESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartREMOVED_SATELLITESVector(builder, numElems):
    return SCNAssetsChangedStartREMOVED_SATELLITESVector(builder, numElems)

def SCNAssetsChangedCreateREMOVED_SATELLITESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateREMOVED_SATELLITESVector(builder, data):
    SCNAssetsChangedCreateREMOVED_SATELLITESVector(builder, data)

def SCNAssetsChangedAddREMOVED_GROUND_SITES(builder, REMOVED_GROUND_SITES):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(REMOVED_GROUND_SITES), 0)

def AddREMOVED_GROUND_SITES(builder, REMOVED_GROUND_SITES):
    SCNAssetsChangedAddREMOVED_GROUND_SITES(builder, REMOVED_GROUND_SITES)

def SCNAssetsChangedStartREMOVED_GROUND_SITESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartREMOVED_GROUND_SITESVector(builder, numElems):
    return SCNAssetsChangedStartREMOVED_GROUND_SITESVector(builder, numElems)

def SCNAssetsChangedCreateREMOVED_GROUND_SITESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateREMOVED_GROUND_SITESVector(builder, data):
    SCNAssetsChangedCreateREMOVED_GROUND_SITESVector(builder, data)

def SCNAssetsChangedAddSATELLITES(builder, SATELLITES):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SATELLITES), 0)

def AddSATELLITES(builder, SATELLITES):
    SCNAssetsChangedAddSATELLITES(builder, SATELLITES)

def SCNAssetsChangedStartSATELLITESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSATELLITESVector(builder, numElems):
    return SCNAssetsChangedStartSATELLITESVector(builder, numElems)

def SCNAssetsChangedCreateSATELLITESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSATELLITESVector(builder, data):
    SCNAssetsChangedCreateSATELLITESVector(builder, data)

def SCNAssetsChangedAddGROUND_SITES(builder, GROUND_SITES):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(GROUND_SITES), 0)

def AddGROUND_SITES(builder, GROUND_SITES):
    SCNAssetsChangedAddGROUND_SITES(builder, GROUND_SITES)

def SCNAssetsChangedStartGROUND_SITESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartGROUND_SITESVector(builder, numElems):
    return SCNAssetsChangedStartGROUND_SITESVector(builder, numElems)

def SCNAssetsChangedCreateGROUND_SITESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateGROUND_SITESVector(builder, data):
    SCNAssetsChangedCreateGROUND_SITESVector(builder, data)

def SCNAssetsChangedAddREFERENCES(builder, REFERENCES):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(REFERENCES), 0)

def AddREFERENCES(builder, REFERENCES):
    SCNAssetsChangedAddREFERENCES(builder, REFERENCES)

def SCNAssetsChangedStartREFERENCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartREFERENCESVector(builder, numElems):
    return SCNAssetsChangedStartREFERENCESVector(builder, numElems)

def SCNAssetsChangedCreateREFERENCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateREFERENCESVector(builder, data):
    SCNAssetsChangedCreateREFERENCESVector(builder, data)

def SCNAssetsChangedEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCNAssetsChangedEnd(builder)

import SCNReference
try:
    from typing import List
except:
    pass

class SCNAssetsChangedT(object):

    # SCNAssetsChangedT
    def __init__(
        self,
        ADDED_SATELLITES = None,
        ADDED_GROUND_SITES = None,
        REMOVED_SATELLITES = None,
        REMOVED_GROUND_SITES = None,
        SATELLITES = None,
        GROUND_SITES = None,
        REFERENCES = None,
    ):
        self.ADDED_SATELLITES = ADDED_SATELLITES  # type: Optional[List[SCNReference.SCNReferenceT]]
        self.ADDED_GROUND_SITES = ADDED_GROUND_SITES  # type: Optional[List[SCNReference.SCNReferenceT]]
        self.REMOVED_SATELLITES = REMOVED_SATELLITES  # type: Optional[List[SCNReference.SCNReferenceT]]
        self.REMOVED_GROUND_SITES = REMOVED_GROUND_SITES  # type: Optional[List[SCNReference.SCNReferenceT]]
        self.SATELLITES = SATELLITES  # type: Optional[List[SCNReference.SCNReferenceT]]
        self.GROUND_SITES = GROUND_SITES  # type: Optional[List[SCNReference.SCNReferenceT]]
        self.REFERENCES = REFERENCES  # type: Optional[List[SCNReference.SCNReferenceT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScnassetsChanged = SCNAssetsChanged()
        tmpScnassetsChanged.Init(buf, pos)
        return cls.InitFromObj(tmpScnassetsChanged)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScnassetsChanged):
        x = SCNAssetsChangedT()
        x._UnPack(tmpScnassetsChanged)
        return x

    # SCNAssetsChangedT
    def _UnPack(self, SCNAssetsChanged):
        if SCNAssetsChanged is None:
            return
        if not SCNAssetsChanged.ADDED_SATELLITESIsNone():
            self.ADDED_SATELLITES = []
            for i in range(SCNAssetsChanged.ADDED_SATELLITESLength()):
                if SCNAssetsChanged.ADDED_SATELLITES(i) is None:
                    self.ADDED_SATELLITES.append(None)
                else:
                    sCNReference_ = SCNReference.SCNReferenceT.InitFromObj(SCNAssetsChanged.ADDED_SATELLITES(i))
                    self.ADDED_SATELLITES.append(sCNReference_)
        if not SCNAssetsChanged.ADDED_GROUND_SITESIsNone():
            self.ADDED_GROUND_SITES = []
            for i in range(SCNAssetsChanged.ADDED_GROUND_SITESLength()):
                if SCNAssetsChanged.ADDED_GROUND_SITES(i) is None:
                    self.ADDED_GROUND_SITES.append(None)
                else:
                    sCNReference_ = SCNReference.SCNReferenceT.InitFromObj(SCNAssetsChanged.ADDED_GROUND_SITES(i))
                    self.ADDED_GROUND_SITES.append(sCNReference_)
        if not SCNAssetsChanged.REMOVED_SATELLITESIsNone():
            self.REMOVED_SATELLITES = []
            for i in range(SCNAssetsChanged.REMOVED_SATELLITESLength()):
                if SCNAssetsChanged.REMOVED_SATELLITES(i) is None:
                    self.REMOVED_SATELLITES.append(None)
                else:
                    sCNReference_ = SCNReference.SCNReferenceT.InitFromObj(SCNAssetsChanged.REMOVED_SATELLITES(i))
                    self.REMOVED_SATELLITES.append(sCNReference_)
        if not SCNAssetsChanged.REMOVED_GROUND_SITESIsNone():
            self.REMOVED_GROUND_SITES = []
            for i in range(SCNAssetsChanged.REMOVED_GROUND_SITESLength()):
                if SCNAssetsChanged.REMOVED_GROUND_SITES(i) is None:
                    self.REMOVED_GROUND_SITES.append(None)
                else:
                    sCNReference_ = SCNReference.SCNReferenceT.InitFromObj(SCNAssetsChanged.REMOVED_GROUND_SITES(i))
                    self.REMOVED_GROUND_SITES.append(sCNReference_)
        if not SCNAssetsChanged.SATELLITESIsNone():
            self.SATELLITES = []
            for i in range(SCNAssetsChanged.SATELLITESLength()):
                if SCNAssetsChanged.SATELLITES(i) is None:
                    self.SATELLITES.append(None)
                else:
                    sCNReference_ = SCNReference.SCNReferenceT.InitFromObj(SCNAssetsChanged.SATELLITES(i))
                    self.SATELLITES.append(sCNReference_)
        if not SCNAssetsChanged.GROUND_SITESIsNone():
            self.GROUND_SITES = []
            for i in range(SCNAssetsChanged.GROUND_SITESLength()):
                if SCNAssetsChanged.GROUND_SITES(i) is None:
                    self.GROUND_SITES.append(None)
                else:
                    sCNReference_ = SCNReference.SCNReferenceT.InitFromObj(SCNAssetsChanged.GROUND_SITES(i))
                    self.GROUND_SITES.append(sCNReference_)
        if not SCNAssetsChanged.REFERENCESIsNone():
            self.REFERENCES = []
            for i in range(SCNAssetsChanged.REFERENCESLength()):
                if SCNAssetsChanged.REFERENCES(i) is None:
                    self.REFERENCES.append(None)
                else:
                    sCNReference_ = SCNReference.SCNReferenceT.InitFromObj(SCNAssetsChanged.REFERENCES(i))
                    self.REFERENCES.append(sCNReference_)

    # SCNAssetsChangedT
    def Pack(self, builder):
        if self.ADDED_SATELLITES is not None:
            ADDED_SATELLITESlist = []
            for i in range(len(self.ADDED_SATELLITES)):
                ADDED_SATELLITESlist.append(self.ADDED_SATELLITES[i].Pack(builder))
            SCNAssetsChangedStartADDED_SATELLITESVector(builder, len(self.ADDED_SATELLITES))
            for i in reversed(range(len(self.ADDED_SATELLITES))):
                builder.PrependUOffsetTRelative(ADDED_SATELLITESlist[i])
            ADDED_SATELLITES = builder.EndVector()
        if self.ADDED_GROUND_SITES is not None:
            ADDED_GROUND_SITESlist = []
            for i in range(len(self.ADDED_GROUND_SITES)):
                ADDED_GROUND_SITESlist.append(self.ADDED_GROUND_SITES[i].Pack(builder))
            SCNAssetsChangedStartADDED_GROUND_SITESVector(builder, len(self.ADDED_GROUND_SITES))
            for i in reversed(range(len(self.ADDED_GROUND_SITES))):
                builder.PrependUOffsetTRelative(ADDED_GROUND_SITESlist[i])
            ADDED_GROUND_SITES = builder.EndVector()
        if self.REMOVED_SATELLITES is not None:
            REMOVED_SATELLITESlist = []
            for i in range(len(self.REMOVED_SATELLITES)):
                REMOVED_SATELLITESlist.append(self.REMOVED_SATELLITES[i].Pack(builder))
            SCNAssetsChangedStartREMOVED_SATELLITESVector(builder, len(self.REMOVED_SATELLITES))
            for i in reversed(range(len(self.REMOVED_SATELLITES))):
                builder.PrependUOffsetTRelative(REMOVED_SATELLITESlist[i])
            REMOVED_SATELLITES = builder.EndVector()
        if self.REMOVED_GROUND_SITES is not None:
            REMOVED_GROUND_SITESlist = []
            for i in range(len(self.REMOVED_GROUND_SITES)):
                REMOVED_GROUND_SITESlist.append(self.REMOVED_GROUND_SITES[i].Pack(builder))
            SCNAssetsChangedStartREMOVED_GROUND_SITESVector(builder, len(self.REMOVED_GROUND_SITES))
            for i in reversed(range(len(self.REMOVED_GROUND_SITES))):
                builder.PrependUOffsetTRelative(REMOVED_GROUND_SITESlist[i])
            REMOVED_GROUND_SITES = builder.EndVector()
        if self.SATELLITES is not None:
            SATELLITESlist = []
            for i in range(len(self.SATELLITES)):
                SATELLITESlist.append(self.SATELLITES[i].Pack(builder))
            SCNAssetsChangedStartSATELLITESVector(builder, len(self.SATELLITES))
            for i in reversed(range(len(self.SATELLITES))):
                builder.PrependUOffsetTRelative(SATELLITESlist[i])
            SATELLITES = builder.EndVector()
        if self.GROUND_SITES is not None:
            GROUND_SITESlist = []
            for i in range(len(self.GROUND_SITES)):
                GROUND_SITESlist.append(self.GROUND_SITES[i].Pack(builder))
            SCNAssetsChangedStartGROUND_SITESVector(builder, len(self.GROUND_SITES))
            for i in reversed(range(len(self.GROUND_SITES))):
                builder.PrependUOffsetTRelative(GROUND_SITESlist[i])
            GROUND_SITES = builder.EndVector()
        if self.REFERENCES is not None:
            REFERENCESlist = []
            for i in range(len(self.REFERENCES)):
                REFERENCESlist.append(self.REFERENCES[i].Pack(builder))
            SCNAssetsChangedStartREFERENCESVector(builder, len(self.REFERENCES))
            for i in reversed(range(len(self.REFERENCES))):
                builder.PrependUOffsetTRelative(REFERENCESlist[i])
            REFERENCES = builder.EndVector()
        SCNAssetsChangedStart(builder)
        if self.ADDED_SATELLITES is not None:
            SCNAssetsChangedAddADDED_SATELLITES(builder, ADDED_SATELLITES)
        if self.ADDED_GROUND_SITES is not None:
            SCNAssetsChangedAddADDED_GROUND_SITES(builder, ADDED_GROUND_SITES)
        if self.REMOVED_SATELLITES is not None:
            SCNAssetsChangedAddREMOVED_SATELLITES(builder, REMOVED_SATELLITES)
        if self.REMOVED_GROUND_SITES is not None:
            SCNAssetsChangedAddREMOVED_GROUND_SITES(builder, REMOVED_GROUND_SITES)
        if self.SATELLITES is not None:
            SCNAssetsChangedAddSATELLITES(builder, SATELLITES)
        if self.GROUND_SITES is not None:
            SCNAssetsChangedAddGROUND_SITES(builder, GROUND_SITES)
        if self.REFERENCES is not None:
            SCNAssetsChangedAddREFERENCES(builder, REFERENCES)
        SCNAssetsChanged = SCNAssetsChangedEnd(builder)
        return SCNAssetsChanged
