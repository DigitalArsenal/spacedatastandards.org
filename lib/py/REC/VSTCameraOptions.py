# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Camera options for a space-domain scenario viewer.
class VSTCameraOptions(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VSTCameraOptions()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVSTCameraOptions(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def VSTCameraOptionsBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x56\x53\x54", size_prefixed=size_prefixed)

    # VSTCameraOptions
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Camera frame relative to the focused target or default scene.
    # VSTCameraOptions
    def CAMERA_FRAME_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Satellite body alignment mode when focused on a satellite.
    # VSTCameraOptions
    def SATELLITE_ALIGNMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Camera distance from the current target in kilometers.
    # VSTCameraOptions
    def DISTANCE_FROM_TARGET_KM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Camera orientation offset.
    # VSTCameraOptions
    def ROTATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from VSTCameraRotation import VSTCameraRotation
            obj = VSTCameraRotation()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def VSTCameraOptionsStart(builder):
    builder.StartObject(4)

def Start(builder):
    VSTCameraOptionsStart(builder)

def VSTCameraOptionsAddCAMERA_FRAME_MODE(builder, CAMERA_FRAME_MODE):
    builder.PrependInt8Slot(0, CAMERA_FRAME_MODE, 0)

def AddCAMERA_FRAME_MODE(builder, CAMERA_FRAME_MODE):
    VSTCameraOptionsAddCAMERA_FRAME_MODE(builder, CAMERA_FRAME_MODE)

def VSTCameraOptionsAddSATELLITE_ALIGNMENT(builder, SATELLITE_ALIGNMENT):
    builder.PrependInt8Slot(1, SATELLITE_ALIGNMENT, 0)

def AddSATELLITE_ALIGNMENT(builder, SATELLITE_ALIGNMENT):
    VSTCameraOptionsAddSATELLITE_ALIGNMENT(builder, SATELLITE_ALIGNMENT)

def VSTCameraOptionsAddDISTANCE_FROM_TARGET_KM(builder, DISTANCE_FROM_TARGET_KM):
    builder.PrependFloat64Slot(2, DISTANCE_FROM_TARGET_KM, 0.0)

def AddDISTANCE_FROM_TARGET_KM(builder, DISTANCE_FROM_TARGET_KM):
    VSTCameraOptionsAddDISTANCE_FROM_TARGET_KM(builder, DISTANCE_FROM_TARGET_KM)

def VSTCameraOptionsAddROTATION(builder, ROTATION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ROTATION), 0)

def AddROTATION(builder, ROTATION):
    VSTCameraOptionsAddROTATION(builder, ROTATION)

def VSTCameraOptionsEnd(builder):
    return builder.EndObject()

def End(builder):
    return VSTCameraOptionsEnd(builder)

import VSTCameraRotation
try:
    from typing import Optional
except:
    pass

class VSTCameraOptionsT(object):

    # VSTCameraOptionsT
    def __init__(
        self,
        CAMERA_FRAME_MODE = 0,
        SATELLITE_ALIGNMENT = 0,
        DISTANCE_FROM_TARGET_KM = 0.0,
        ROTATION = None,
    ):
        self.CAMERA_FRAME_MODE = CAMERA_FRAME_MODE  # type: int
        self.SATELLITE_ALIGNMENT = SATELLITE_ALIGNMENT  # type: int
        self.DISTANCE_FROM_TARGET_KM = DISTANCE_FROM_TARGET_KM  # type: float
        self.ROTATION = ROTATION  # type: Optional[VSTCameraRotation.VSTCameraRotationT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpVstcameraOptions = VSTCameraOptions()
        tmpVstcameraOptions.Init(buf, pos)
        return cls.InitFromObj(tmpVstcameraOptions)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpVstcameraOptions):
        x = VSTCameraOptionsT()
        x._UnPack(tmpVstcameraOptions)
        return x

    # VSTCameraOptionsT
    def _UnPack(self, VSTCameraOptions):
        if VSTCameraOptions is None:
            return
        self.CAMERA_FRAME_MODE = VSTCameraOptions.CAMERA_FRAME_MODE()
        self.SATELLITE_ALIGNMENT = VSTCameraOptions.SATELLITE_ALIGNMENT()
        self.DISTANCE_FROM_TARGET_KM = VSTCameraOptions.DISTANCE_FROM_TARGET_KM()
        if VSTCameraOptions.ROTATION() is not None:
            self.ROTATION = VSTCameraRotation.VSTCameraRotationT.InitFromObj(VSTCameraOptions.ROTATION())

    # VSTCameraOptionsT
    def Pack(self, builder):
        if self.ROTATION is not None:
            ROTATION = self.ROTATION.Pack(builder)
        VSTCameraOptionsStart(builder)
        VSTCameraOptionsAddCAMERA_FRAME_MODE(builder, self.CAMERA_FRAME_MODE)
        VSTCameraOptionsAddSATELLITE_ALIGNMENT(builder, self.SATELLITE_ALIGNMENT)
        VSTCameraOptionsAddDISTANCE_FROM_TARGET_KM(builder, self.DISTANCE_FROM_TARGET_KM)
        if self.ROTATION is not None:
            VSTCameraOptionsAddROTATION(builder, ROTATION)
        VSTCameraOptions = VSTCameraOptionsEnd(builder)
        return VSTCameraOptions
