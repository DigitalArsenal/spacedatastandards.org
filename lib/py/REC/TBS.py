# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Terrestrial Base Station Site.
#
# One record describes ONE terrestrial cellular base-station site as
# reconciled from every provider the publisher consulted. It is the wire
# record for terrestrial mobile-network infrastructure, and it is not an
# emitter model ($RFE), a band specification ($RFB), a geodetic tracking
# station ($GST), a satellite ground station ($SIT), or an observation.
#
# A site cannot be re-serialized unattributed. SOURCES and CONSENSUS are both
# required, so every published position carries the providers it came from and
# the rule that reconciled them; a consumer that strips attribution has to
# leave the standard to do it. Unknown values are ABSENT — never zero, never a
# placeholder, never carried across from a neighbouring site.
#
# Identity is ID, the publisher's own. The network addressing fields are the
# mobile network's, are assigned by its operator, and are reused worldwide;
# none of them is a global key on its own.
class TBS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TBS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTBS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TBSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x42\x53", size_prefixed=size_prefixed)

    # TBS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher-stable identifier for this site, unique within its record set.
    # Recommended form: "<mcc>-<mnc>-<area-code>-<cell-id>".
    # TBS
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Winning provider's native site identifier, copied verbatim from the
    # SOURCES entry named by CONSENSUS.WINNING_PROVIDER_ID. Never a substitute
    # for ID and never stable across providers.
    # TBS
    def NATIVE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TBS
    def RADIO(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 6

    # Mobile country code from the international mobile-subscriber identity
    # numbering plan. Zero means the source published none.
    # TBS
    def MCC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Mobile network code within MCC. Carried as an integer, so decimal leading
    # zeros are NOT preserved: a two- versus three-digit network code is
    # distinguished by the numbering plan of MCC, and the source's own text is
    # retained verbatim in TBSProvenance.NATIVE_ID. Zero means unpublished.
    # TBS
    def MNC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Location area code, used by GSM-, CDMA- and UMTS-class access. Zero means
    # unpublished. A site published with a location area code does not also
    # carry a tracking area code; publishing both asserts two access
    # generations for one cell.
    # TBS
    def LAC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Tracking area code, used by LTE- and NR-class access. Zero means
    # unpublished.
    # TBS
    def TAC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Cell identity verbatim from the source. A string because width, radix and
    # leading zeros are significant and differ by access class: a 16-bit cell
    # identity, a 28-bit evolved cell identity and a 36-bit new-radio cell
    # identity are not interchangeable integers, and this field never converts
    # between them.
    # TBS
    def CELL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reconciled site position in decimal degrees on WGS 84, produced by the
    # merge described in CONSENSUS. Per-provider positions before
    # reconciliation live in SOURCES[].REPORTED_LATITUDE/REPORTED_LONGITUDE and
    # are never overwritten by this value.
    # TBS
    def LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TBS
    def LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Radius in metres of the area within which the site's observations were
    # collected — a positional-uncertainty envelope, not a transmission range,
    # not a coverage guarantee, and never converted from another unit. Zero
    # means unpublished.
    # TBS
    def RANGE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Independent observations behind the winning report. Zero means the source
    # publishes no observation count, never that the site was never observed.
    # TBS
    def SAMPLES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Mean received signal strength in dBm across those observations, as the
    # source publishes it. Carried in dBm unconverted; a consumer needing watts
    # converts at the join, explicitly. Zero means unpublished.
    # TBS
    def AVERAGE_SIGNAL_DBM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFC 3339 UTC fixed-millisecond bounds of the observation window backing
    # the winning report.
    # TBS
    def FIRST_OBSERVED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TBS
    def LAST_OBSERVED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Network operator name verbatim as the cited source states it. A display
    # name, never an identifier: consumers must not treat it as a stable key.
    # TBS
    def OPERATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Operating centre frequency in MHz, following the $RFB convention. A
    # consumer joining to $IQC, which carries hertz, converts at the join,
    # explicitly. Zero means unpublished.
    # TBS
    def FREQUENCY_MHZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Site or structure name verbatim when the source publishes one. Never
    # synthesized from coordinates, operator, or cell identity.
    # TBS
    def SITE_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ISO 3166-1 alpha-2 country code, uppercase, as the source states it.
    # Derived from MCC only when the source itself derives it.
    # TBS
    def COUNTRY_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Every provider consulted for this site, including providers that returned
    # nothing. Required: a site with no named sources is not publishable.
    # TBS
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TBSProvenance import TBSProvenance
            obj = TBSProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TBS
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TBS
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        return o == 0

    # How the entries in SOURCES were reduced to LATITUDE/LONGITUDE. Required:
    # a published position always states the rule that produced it, including
    # SINGLE_SOURCE.
    # TBS
    def CONSENSUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TBSConsensus import TBSConsensus
            obj = TBSConsensus()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def TBSStart(builder):
    builder.StartObject(21)

def Start(builder):
    TBSStart(builder)

def TBSAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    TBSAddID(builder, ID)

def TBSAddNATIVE_ID(builder, NATIVE_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NATIVE_ID), 0)

def AddNATIVE_ID(builder, NATIVE_ID):
    TBSAddNATIVE_ID(builder, NATIVE_ID)

def TBSAddRADIO(builder, RADIO):
    builder.PrependInt8Slot(2, RADIO, 6)

def AddRADIO(builder, RADIO):
    TBSAddRADIO(builder, RADIO)

def TBSAddMCC(builder, MCC):
    builder.PrependUint32Slot(3, MCC, 0)

def AddMCC(builder, MCC):
    TBSAddMCC(builder, MCC)

def TBSAddMNC(builder, MNC):
    builder.PrependUint32Slot(4, MNC, 0)

def AddMNC(builder, MNC):
    TBSAddMNC(builder, MNC)

def TBSAddLAC(builder, LAC):
    builder.PrependUint32Slot(5, LAC, 0)

def AddLAC(builder, LAC):
    TBSAddLAC(builder, LAC)

def TBSAddTAC(builder, TAC):
    builder.PrependUint32Slot(6, TAC, 0)

def AddTAC(builder, TAC):
    TBSAddTAC(builder, TAC)

def TBSAddCELL_ID(builder, CELL_ID):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(CELL_ID), 0)

def AddCELL_ID(builder, CELL_ID):
    TBSAddCELL_ID(builder, CELL_ID)

def TBSAddLATITUDE(builder, LATITUDE):
    builder.PrependFloat64Slot(8, LATITUDE, 0.0)

def AddLATITUDE(builder, LATITUDE):
    TBSAddLATITUDE(builder, LATITUDE)

def TBSAddLONGITUDE(builder, LONGITUDE):
    builder.PrependFloat64Slot(9, LONGITUDE, 0.0)

def AddLONGITUDE(builder, LONGITUDE):
    TBSAddLONGITUDE(builder, LONGITUDE)

def TBSAddRANGE_M(builder, RANGE_M):
    builder.PrependFloat64Slot(10, RANGE_M, 0.0)

def AddRANGE_M(builder, RANGE_M):
    TBSAddRANGE_M(builder, RANGE_M)

def TBSAddSAMPLES(builder, SAMPLES):
    builder.PrependUint32Slot(11, SAMPLES, 0)

def AddSAMPLES(builder, SAMPLES):
    TBSAddSAMPLES(builder, SAMPLES)

def TBSAddAVERAGE_SIGNAL_DBM(builder, AVERAGE_SIGNAL_DBM):
    builder.PrependFloat64Slot(12, AVERAGE_SIGNAL_DBM, 0.0)

def AddAVERAGE_SIGNAL_DBM(builder, AVERAGE_SIGNAL_DBM):
    TBSAddAVERAGE_SIGNAL_DBM(builder, AVERAGE_SIGNAL_DBM)

def TBSAddFIRST_OBSERVED(builder, FIRST_OBSERVED):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(FIRST_OBSERVED), 0)

def AddFIRST_OBSERVED(builder, FIRST_OBSERVED):
    TBSAddFIRST_OBSERVED(builder, FIRST_OBSERVED)

def TBSAddLAST_OBSERVED(builder, LAST_OBSERVED):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(LAST_OBSERVED), 0)

def AddLAST_OBSERVED(builder, LAST_OBSERVED):
    TBSAddLAST_OBSERVED(builder, LAST_OBSERVED)

def TBSAddOPERATOR(builder, OPERATOR):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(OPERATOR), 0)

def AddOPERATOR(builder, OPERATOR):
    TBSAddOPERATOR(builder, OPERATOR)

def TBSAddFREQUENCY_MHZ(builder, FREQUENCY_MHZ):
    builder.PrependFloat64Slot(16, FREQUENCY_MHZ, 0.0)

def AddFREQUENCY_MHZ(builder, FREQUENCY_MHZ):
    TBSAddFREQUENCY_MHZ(builder, FREQUENCY_MHZ)

def TBSAddSITE_NAME(builder, SITE_NAME):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(SITE_NAME), 0)

def AddSITE_NAME(builder, SITE_NAME):
    TBSAddSITE_NAME(builder, SITE_NAME)

def TBSAddCOUNTRY_CODE(builder, COUNTRY_CODE):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(COUNTRY_CODE), 0)

def AddCOUNTRY_CODE(builder, COUNTRY_CODE):
    TBSAddCOUNTRY_CODE(builder, COUNTRY_CODE)

def TBSAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(19, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    TBSAddSOURCES(builder, SOURCES)

def TBSStartSOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCESVector(builder, numElems):
    return TBSStartSOURCESVector(builder, numElems)

def TBSCreateSOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCESVector(builder, data):
    TBSCreateSOURCESVector(builder, data)

def TBSAddCONSENSUS(builder, CONSENSUS):
    builder.PrependUOffsetTRelativeSlot(20, flatbuffers.number_types.UOffsetTFlags.py_type(CONSENSUS), 0)

def AddCONSENSUS(builder, CONSENSUS):
    TBSAddCONSENSUS(builder, CONSENSUS)

def TBSEnd(builder):
    return builder.EndObject()

def End(builder):
    return TBSEnd(builder)

import TBSConsensus
import TBSProvenance
try:
    from typing import List, Optional
except:
    pass

class TBST(object):

    # TBST
    def __init__(
        self,
        ID = None,
        NATIVE_ID = None,
        RADIO = 6,
        MCC = 0,
        MNC = 0,
        LAC = 0,
        TAC = 0,
        CELL_ID = None,
        LATITUDE = 0.0,
        LONGITUDE = 0.0,
        RANGE_M = 0.0,
        SAMPLES = 0,
        AVERAGE_SIGNAL_DBM = 0.0,
        FIRST_OBSERVED = None,
        LAST_OBSERVED = None,
        OPERATOR = None,
        FREQUENCY_MHZ = 0.0,
        SITE_NAME = None,
        COUNTRY_CODE = None,
        SOURCES = None,
        CONSENSUS = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.NATIVE_ID = NATIVE_ID  # type: Optional[str]
        self.RADIO = RADIO  # type: int
        self.MCC = MCC  # type: int
        self.MNC = MNC  # type: int
        self.LAC = LAC  # type: int
        self.TAC = TAC  # type: int
        self.CELL_ID = CELL_ID  # type: Optional[str]
        self.LATITUDE = LATITUDE  # type: float
        self.LONGITUDE = LONGITUDE  # type: float
        self.RANGE_M = RANGE_M  # type: float
        self.SAMPLES = SAMPLES  # type: int
        self.AVERAGE_SIGNAL_DBM = AVERAGE_SIGNAL_DBM  # type: float
        self.FIRST_OBSERVED = FIRST_OBSERVED  # type: Optional[str]
        self.LAST_OBSERVED = LAST_OBSERVED  # type: Optional[str]
        self.OPERATOR = OPERATOR  # type: Optional[str]
        self.FREQUENCY_MHZ = FREQUENCY_MHZ  # type: float
        self.SITE_NAME = SITE_NAME  # type: Optional[str]
        self.COUNTRY_CODE = COUNTRY_CODE  # type: Optional[str]
        self.SOURCES = SOURCES  # type: Optional[List[TBSProvenance.TBSProvenanceT]]
        self.CONSENSUS = CONSENSUS  # type: Optional[TBSConsensus.TBSConsensusT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTbs = TBS()
        tmpTbs.Init(buf, pos)
        return cls.InitFromObj(tmpTbs)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTbs):
        x = TBST()
        x._UnPack(tmpTbs)
        return x

    # TBST
    def _UnPack(self, TBS):
        if TBS is None:
            return
        self.ID = TBS.ID()
        self.NATIVE_ID = TBS.NATIVE_ID()
        self.RADIO = TBS.RADIO()
        self.MCC = TBS.MCC()
        self.MNC = TBS.MNC()
        self.LAC = TBS.LAC()
        self.TAC = TBS.TAC()
        self.CELL_ID = TBS.CELL_ID()
        self.LATITUDE = TBS.LATITUDE()
        self.LONGITUDE = TBS.LONGITUDE()
        self.RANGE_M = TBS.RANGE_M()
        self.SAMPLES = TBS.SAMPLES()
        self.AVERAGE_SIGNAL_DBM = TBS.AVERAGE_SIGNAL_DBM()
        self.FIRST_OBSERVED = TBS.FIRST_OBSERVED()
        self.LAST_OBSERVED = TBS.LAST_OBSERVED()
        self.OPERATOR = TBS.OPERATOR()
        self.FREQUENCY_MHZ = TBS.FREQUENCY_MHZ()
        self.SITE_NAME = TBS.SITE_NAME()
        self.COUNTRY_CODE = TBS.COUNTRY_CODE()
        if not TBS.SOURCESIsNone():
            self.SOURCES = []
            for i in range(TBS.SOURCESLength()):
                if TBS.SOURCES(i) is None:
                    self.SOURCES.append(None)
                else:
                    tBSProvenance_ = TBSProvenance.TBSProvenanceT.InitFromObj(TBS.SOURCES(i))
                    self.SOURCES.append(tBSProvenance_)
        if TBS.CONSENSUS() is not None:
            self.CONSENSUS = TBSConsensus.TBSConsensusT.InitFromObj(TBS.CONSENSUS())

    # TBST
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.NATIVE_ID is not None:
            NATIVE_ID = builder.CreateString(self.NATIVE_ID)
        if self.CELL_ID is not None:
            CELL_ID = builder.CreateString(self.CELL_ID)
        if self.FIRST_OBSERVED is not None:
            FIRST_OBSERVED = builder.CreateString(self.FIRST_OBSERVED)
        if self.LAST_OBSERVED is not None:
            LAST_OBSERVED = builder.CreateString(self.LAST_OBSERVED)
        if self.OPERATOR is not None:
            OPERATOR = builder.CreateString(self.OPERATOR)
        if self.SITE_NAME is not None:
            SITE_NAME = builder.CreateString(self.SITE_NAME)
        if self.COUNTRY_CODE is not None:
            COUNTRY_CODE = builder.CreateString(self.COUNTRY_CODE)
        if self.SOURCES is not None:
            SOURCESlist = []
            for i in range(len(self.SOURCES)):
                SOURCESlist.append(self.SOURCES[i].Pack(builder))
            TBSStartSOURCESVector(builder, len(self.SOURCES))
            for i in reversed(range(len(self.SOURCES))):
                builder.PrependUOffsetTRelative(SOURCESlist[i])
            SOURCES = builder.EndVector()
        if self.CONSENSUS is not None:
            CONSENSUS = self.CONSENSUS.Pack(builder)
        TBSStart(builder)
        if self.ID is not None:
            TBSAddID(builder, ID)
        if self.NATIVE_ID is not None:
            TBSAddNATIVE_ID(builder, NATIVE_ID)
        TBSAddRADIO(builder, self.RADIO)
        TBSAddMCC(builder, self.MCC)
        TBSAddMNC(builder, self.MNC)
        TBSAddLAC(builder, self.LAC)
        TBSAddTAC(builder, self.TAC)
        if self.CELL_ID is not None:
            TBSAddCELL_ID(builder, CELL_ID)
        TBSAddLATITUDE(builder, self.LATITUDE)
        TBSAddLONGITUDE(builder, self.LONGITUDE)
        TBSAddRANGE_M(builder, self.RANGE_M)
        TBSAddSAMPLES(builder, self.SAMPLES)
        TBSAddAVERAGE_SIGNAL_DBM(builder, self.AVERAGE_SIGNAL_DBM)
        if self.FIRST_OBSERVED is not None:
            TBSAddFIRST_OBSERVED(builder, FIRST_OBSERVED)
        if self.LAST_OBSERVED is not None:
            TBSAddLAST_OBSERVED(builder, LAST_OBSERVED)
        if self.OPERATOR is not None:
            TBSAddOPERATOR(builder, OPERATOR)
        TBSAddFREQUENCY_MHZ(builder, self.FREQUENCY_MHZ)
        if self.SITE_NAME is not None:
            TBSAddSITE_NAME(builder, SITE_NAME)
        if self.COUNTRY_CODE is not None:
            TBSAddCOUNTRY_CODE(builder, COUNTRY_CODE)
        if self.SOURCES is not None:
            TBSAddSOURCES(builder, SOURCES)
        if self.CONSENSUS is not None:
            TBSAddCONSENSUS(builder, CONSENSUS)
        TBS = TBSEnd(builder)
        return TBS
