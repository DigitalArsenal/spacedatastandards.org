# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Stripe material
class CZMStripeMaterial(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CZMStripeMaterial()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCZMStripeMaterial(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CZMStripeMaterialBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x5A\x4D", size_prefixed=size_prefixed)

    # CZMStripeMaterial
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stripe orientation
    # CZMStripeMaterial
    def ORIENTATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Even color
    # CZMStripeMaterial
    def EVEN_COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CZMColor import CZMColor
            obj = CZMColor()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Odd color
    # CZMStripeMaterial
    def ODD_COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CZMColor import CZMColor
            obj = CZMColor()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Offset
    # CZMStripeMaterial
    def OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Number of times to repeat
    # CZMStripeMaterial
    def REPEAT_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def CZMStripeMaterialStart(builder):
    builder.StartObject(5)

def Start(builder):
    CZMStripeMaterialStart(builder)

def CZMStripeMaterialAddORIENTATION(builder, ORIENTATION):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ORIENTATION), 0)

def AddORIENTATION(builder, ORIENTATION):
    CZMStripeMaterialAddORIENTATION(builder, ORIENTATION)

def CZMStripeMaterialAddEVEN_COLOR(builder, EVEN_COLOR):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(EVEN_COLOR), 0)

def AddEVEN_COLOR(builder, EVEN_COLOR):
    CZMStripeMaterialAddEVEN_COLOR(builder, EVEN_COLOR)

def CZMStripeMaterialAddODD_COLOR(builder, ODD_COLOR):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ODD_COLOR), 0)

def AddODD_COLOR(builder, ODD_COLOR):
    CZMStripeMaterialAddODD_COLOR(builder, ODD_COLOR)

def CZMStripeMaterialAddOFFSET(builder, OFFSET):
    builder.PrependFloat64Slot(3, OFFSET, 0.0)

def AddOFFSET(builder, OFFSET):
    CZMStripeMaterialAddOFFSET(builder, OFFSET)

def CZMStripeMaterialAddREPEAT_COUNT(builder, REPEAT_COUNT):
    builder.PrependFloat64Slot(4, REPEAT_COUNT, 0.0)

def AddREPEAT_COUNT(builder, REPEAT_COUNT):
    CZMStripeMaterialAddREPEAT_COUNT(builder, REPEAT_COUNT)

def CZMStripeMaterialEnd(builder):
    return builder.EndObject()

def End(builder):
    return CZMStripeMaterialEnd(builder)

import CZMColor
try:
    from typing import Optional
except:
    pass

class CZMStripeMaterialT(object):

    # CZMStripeMaterialT
    def __init__(self):
        self.ORIENTATION = None  # type: str
        self.EVEN_COLOR = None  # type: Optional[CZMColor.CZMColorT]
        self.ODD_COLOR = None  # type: Optional[CZMColor.CZMColorT]
        self.OFFSET = 0.0  # type: float
        self.REPEAT_COUNT = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        czmstripeMaterial = CZMStripeMaterial()
        czmstripeMaterial.Init(buf, pos)
        return cls.InitFromObj(czmstripeMaterial)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, czmstripeMaterial):
        x = CZMStripeMaterialT()
        x._UnPack(czmstripeMaterial)
        return x

    # CZMStripeMaterialT
    def _UnPack(self, czmstripeMaterial):
        if czmstripeMaterial is None:
            return
        self.ORIENTATION = czmstripeMaterial.ORIENTATION()
        if czmstripeMaterial.EVEN_COLOR() is not None:
            self.EVEN_COLOR = CZMColor.CZMColorT.InitFromObj(czmstripeMaterial.EVEN_COLOR())
        if czmstripeMaterial.ODD_COLOR() is not None:
            self.ODD_COLOR = CZMColor.CZMColorT.InitFromObj(czmstripeMaterial.ODD_COLOR())
        self.OFFSET = czmstripeMaterial.OFFSET()
        self.REPEAT_COUNT = czmstripeMaterial.REPEAT_COUNT()

    # CZMStripeMaterialT
    def Pack(self, builder):
        if self.ORIENTATION is not None:
            ORIENTATION = builder.CreateString(self.ORIENTATION)
        if self.EVEN_COLOR is not None:
            EVEN_COLOR = self.EVEN_COLOR.Pack(builder)
        if self.ODD_COLOR is not None:
            ODD_COLOR = self.ODD_COLOR.Pack(builder)
        CZMStripeMaterialStart(builder)
        if self.ORIENTATION is not None:
            CZMStripeMaterialAddORIENTATION(builder, ORIENTATION)
        if self.EVEN_COLOR is not None:
            CZMStripeMaterialAddEVEN_COLOR(builder, EVEN_COLOR)
        if self.ODD_COLOR is not None:
            CZMStripeMaterialAddODD_COLOR(builder, ODD_COLOR)
        CZMStripeMaterialAddOFFSET(builder, self.OFFSET)
        CZMStripeMaterialAddREPEAT_COUNT(builder, self.REPEAT_COUNT)
        czmstripeMaterial = CZMStripeMaterialEnd(builder)
        return czmstripeMaterial
