# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Checkerboard material
class CZMCheckerboardMaterial(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CZMCheckerboardMaterial()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCZMCheckerboardMaterial(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CZMCheckerboardMaterialBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x5A\x4D", size_prefixed=size_prefixed)

    # CZMCheckerboardMaterial
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Even color
    # CZMCheckerboardMaterial
    def EVEN_COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CZMColor import CZMColor
            obj = CZMColor()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Odd color
    # CZMCheckerboardMaterial
    def ODD_COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CZMColor import CZMColor
            obj = CZMColor()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Repeat X
    # CZMCheckerboardMaterial
    def REPEAT_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Repeat Y
    # CZMCheckerboardMaterial
    def REPEAT_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def CZMCheckerboardMaterialStart(builder):
    builder.StartObject(4)

def Start(builder):
    CZMCheckerboardMaterialStart(builder)

def CZMCheckerboardMaterialAddEVEN_COLOR(builder, EVEN_COLOR):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EVEN_COLOR), 0)

def AddEVEN_COLOR(builder, EVEN_COLOR):
    CZMCheckerboardMaterialAddEVEN_COLOR(builder, EVEN_COLOR)

def CZMCheckerboardMaterialAddODD_COLOR(builder, ODD_COLOR):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ODD_COLOR), 0)

def AddODD_COLOR(builder, ODD_COLOR):
    CZMCheckerboardMaterialAddODD_COLOR(builder, ODD_COLOR)

def CZMCheckerboardMaterialAddREPEAT_X(builder, REPEAT_X):
    builder.PrependFloat64Slot(2, REPEAT_X, 0.0)

def AddREPEAT_X(builder, REPEAT_X):
    CZMCheckerboardMaterialAddREPEAT_X(builder, REPEAT_X)

def CZMCheckerboardMaterialAddREPEAT_Y(builder, REPEAT_Y):
    builder.PrependFloat64Slot(3, REPEAT_Y, 0.0)

def AddREPEAT_Y(builder, REPEAT_Y):
    CZMCheckerboardMaterialAddREPEAT_Y(builder, REPEAT_Y)

def CZMCheckerboardMaterialEnd(builder):
    return builder.EndObject()

def End(builder):
    return CZMCheckerboardMaterialEnd(builder)

import CZMColor
try:
    from typing import Optional
except:
    pass

class CZMCheckerboardMaterialT(object):

    # CZMCheckerboardMaterialT
    def __init__(self):
        self.EVEN_COLOR = None  # type: Optional[CZMColor.CZMColorT]
        self.ODD_COLOR = None  # type: Optional[CZMColor.CZMColorT]
        self.REPEAT_X = 0.0  # type: float
        self.REPEAT_Y = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        czmcheckerboardMaterial = CZMCheckerboardMaterial()
        czmcheckerboardMaterial.Init(buf, pos)
        return cls.InitFromObj(czmcheckerboardMaterial)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, czmcheckerboardMaterial):
        x = CZMCheckerboardMaterialT()
        x._UnPack(czmcheckerboardMaterial)
        return x

    # CZMCheckerboardMaterialT
    def _UnPack(self, czmcheckerboardMaterial):
        if czmcheckerboardMaterial is None:
            return
        if czmcheckerboardMaterial.EVEN_COLOR() is not None:
            self.EVEN_COLOR = CZMColor.CZMColorT.InitFromObj(czmcheckerboardMaterial.EVEN_COLOR())
        if czmcheckerboardMaterial.ODD_COLOR() is not None:
            self.ODD_COLOR = CZMColor.CZMColorT.InitFromObj(czmcheckerboardMaterial.ODD_COLOR())
        self.REPEAT_X = czmcheckerboardMaterial.REPEAT_X()
        self.REPEAT_Y = czmcheckerboardMaterial.REPEAT_Y()

    # CZMCheckerboardMaterialT
    def Pack(self, builder):
        if self.EVEN_COLOR is not None:
            EVEN_COLOR = self.EVEN_COLOR.Pack(builder)
        if self.ODD_COLOR is not None:
            ODD_COLOR = self.ODD_COLOR.Pack(builder)
        CZMCheckerboardMaterialStart(builder)
        if self.EVEN_COLOR is not None:
            CZMCheckerboardMaterialAddEVEN_COLOR(builder, EVEN_COLOR)
        if self.ODD_COLOR is not None:
            CZMCheckerboardMaterialAddODD_COLOR(builder, ODD_COLOR)
        CZMCheckerboardMaterialAddREPEAT_X(builder, self.REPEAT_X)
        CZMCheckerboardMaterialAddREPEAT_Y(builder, self.REPEAT_Y)
        czmcheckerboardMaterial = CZMCheckerboardMaterialEnd(builder)
        return czmcheckerboardMaterial
