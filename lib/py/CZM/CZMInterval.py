# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Time interval for time-dynamic properties
class CZMInterval(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CZMInterval()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCZMInterval(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CZMIntervalBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x5A\x4D", size_prefixed=size_prefixed)

    # CZMInterval
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ISO 8601 interval string (e.g. "2012-03-15T10:00:00Z/2012-03-16T10:00:00Z")
    # CZMInterval
    def INTERVAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def CZMIntervalStart(builder):
    builder.StartObject(1)

def Start(builder):
    CZMIntervalStart(builder)

def CZMIntervalAddINTERVAL(builder, INTERVAL):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(INTERVAL), 0)

def AddINTERVAL(builder, INTERVAL):
    CZMIntervalAddINTERVAL(builder, INTERVAL)

def CZMIntervalEnd(builder):
    return builder.EndObject()

def End(builder):
    return CZMIntervalEnd(builder)


class CZMIntervalT(object):

    # CZMIntervalT
    def __init__(self):
        self.INTERVAL = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        czminterval = CZMInterval()
        czminterval.Init(buf, pos)
        return cls.InitFromObj(czminterval)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, czminterval):
        x = CZMIntervalT()
        x._UnPack(czminterval)
        return x

    # CZMIntervalT
    def _UnPack(self, czminterval):
        if czminterval is None:
            return
        self.INTERVAL = czminterval.INTERVAL()

    # CZMIntervalT
    def Pack(self, builder):
        if self.INTERVAL is not None:
            INTERVAL = builder.CreateString(self.INTERVAL)
        CZMIntervalStart(builder)
        if self.INTERVAL is not None:
            CZMIntervalAddINTERVAL(builder, INTERVAL)
        czminterval = CZMIntervalEnd(builder)
        return czminterval
