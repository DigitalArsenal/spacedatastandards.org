# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Solid color material
class CZMSolidColorMaterial(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CZMSolidColorMaterial()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCZMSolidColorMaterial(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CZMSolidColorMaterialBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x5A\x4D", size_prefixed=size_prefixed)

    # CZMSolidColorMaterial
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Color
    # CZMSolidColorMaterial
    def COLOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from CZMColor import CZMColor
            obj = CZMColor()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def CZMSolidColorMaterialStart(builder):
    builder.StartObject(1)

def Start(builder):
    CZMSolidColorMaterialStart(builder)

def CZMSolidColorMaterialAddCOLOR(builder, COLOR):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(COLOR), 0)

def AddCOLOR(builder, COLOR):
    CZMSolidColorMaterialAddCOLOR(builder, COLOR)

def CZMSolidColorMaterialEnd(builder):
    return builder.EndObject()

def End(builder):
    return CZMSolidColorMaterialEnd(builder)

import CZMColor
try:
    from typing import Optional
except:
    pass

class CZMSolidColorMaterialT(object):

    # CZMSolidColorMaterialT
    def __init__(self):
        self.COLOR = None  # type: Optional[CZMColor.CZMColorT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        czmsolidColorMaterial = CZMSolidColorMaterial()
        czmsolidColorMaterial.Init(buf, pos)
        return cls.InitFromObj(czmsolidColorMaterial)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, czmsolidColorMaterial):
        x = CZMSolidColorMaterialT()
        x._UnPack(czmsolidColorMaterial)
        return x

    # CZMSolidColorMaterialT
    def _UnPack(self, czmsolidColorMaterial):
        if czmsolidColorMaterial is None:
            return
        if czmsolidColorMaterial.COLOR() is not None:
            self.COLOR = CZMColor.CZMColorT.InitFromObj(czmsolidColorMaterial.COLOR())

    # CZMSolidColorMaterialT
    def Pack(self, builder):
        if self.COLOR is not None:
            COLOR = self.COLOR.Pack(builder)
        CZMSolidColorMaterialStart(builder)
        if self.COLOR is not None:
            CZMSolidColorMaterialAddCOLOR(builder, COLOR)
        czmsolidColorMaterial = CZMSolidColorMaterialEnd(builder)
        return czmsolidColorMaterial
