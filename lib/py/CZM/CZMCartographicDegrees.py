# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Cartographic position (longitude, latitude, height in radians/meters)
class CZMCartographicDegrees(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CZMCartographicDegrees()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCZMCartographicDegrees(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CZMCartographicDegreesBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x5A\x4D", size_prefixed=size_prefixed)

    # CZMCartographicDegrees
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Longitude in degrees
    # CZMCartographicDegrees
    def LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Latitude in degrees
    # CZMCartographicDegrees
    def LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Height in meters above WGS84 ellipsoid
    # CZMCartographicDegrees
    def HEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def CZMCartographicDegreesStart(builder):
    builder.StartObject(3)

def Start(builder):
    CZMCartographicDegreesStart(builder)

def CZMCartographicDegreesAddLONGITUDE(builder, LONGITUDE):
    builder.PrependFloat64Slot(0, LONGITUDE, 0.0)

def AddLONGITUDE(builder, LONGITUDE):
    CZMCartographicDegreesAddLONGITUDE(builder, LONGITUDE)

def CZMCartographicDegreesAddLATITUDE(builder, LATITUDE):
    builder.PrependFloat64Slot(1, LATITUDE, 0.0)

def AddLATITUDE(builder, LATITUDE):
    CZMCartographicDegreesAddLATITUDE(builder, LATITUDE)

def CZMCartographicDegreesAddHEIGHT(builder, HEIGHT):
    builder.PrependFloat64Slot(2, HEIGHT, 0.0)

def AddHEIGHT(builder, HEIGHT):
    CZMCartographicDegreesAddHEIGHT(builder, HEIGHT)

def CZMCartographicDegreesEnd(builder):
    return builder.EndObject()

def End(builder):
    return CZMCartographicDegreesEnd(builder)


class CZMCartographicDegreesT(object):

    # CZMCartographicDegreesT
    def __init__(self):
        self.LONGITUDE = 0.0  # type: float
        self.LATITUDE = 0.0  # type: float
        self.HEIGHT = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        czmcartographicDegrees = CZMCartographicDegrees()
        czmcartographicDegrees.Init(buf, pos)
        return cls.InitFromObj(czmcartographicDegrees)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, czmcartographicDegrees):
        x = CZMCartographicDegreesT()
        x._UnPack(czmcartographicDegrees)
        return x

    # CZMCartographicDegreesT
    def _UnPack(self, czmcartographicDegrees):
        if czmcartographicDegrees is None:
            return
        self.LONGITUDE = czmcartographicDegrees.LONGITUDE()
        self.LATITUDE = czmcartographicDegrees.LATITUDE()
        self.HEIGHT = czmcartographicDegrees.HEIGHT()

    # CZMCartographicDegreesT
    def Pack(self, builder):
        CZMCartographicDegreesStart(builder)
        CZMCartographicDegreesAddLONGITUDE(builder, self.LONGITUDE)
        CZMCartographicDegreesAddLATITUDE(builder, self.LATITUDE)
        CZMCartographicDegreesAddHEIGHT(builder, self.HEIGHT)
        czmcartographicDegrees = CZMCartographicDegreesEnd(builder)
        return czmcartographicDegrees
