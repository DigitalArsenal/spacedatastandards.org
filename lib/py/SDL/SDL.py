# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Space Data Link Security (CCSDS 355.0-B-1)
class SDL(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SDL()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSDL(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SDLBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x44\x4C", size_prefixed=size_prefixed)

    # SDL
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Security Parameter Index
    # SDL
    def SPI(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Initialization vector length in bytes
    # SDL
    def IV_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Initialization vector
    # SDL
    def IV(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SDL
    def IVAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SDL
    def IVLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDL
    def IVIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # MAC length in bytes
    # SDL
    def MAC_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Message authentication code
    # SDL
    def MAC(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SDL
    def MACAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SDL
    def MACLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDL
    def MACIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Pad length
    # SDL
    def PAD_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Security payload
    # SDL
    def PAYLOAD(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SDL
    def PAYLOADAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SDL
    def PAYLOADLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SDL
    def PAYLOADIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def SDLStart(builder):
    builder.StartObject(7)

def Start(builder):
    SDLStart(builder)

def SDLAddSPI(builder, SPI):
    builder.PrependUint16Slot(0, SPI, 0)

def AddSPI(builder, SPI):
    SDLAddSPI(builder, SPI)

def SDLAddIV_LENGTH(builder, IV_LENGTH):
    builder.PrependUint8Slot(1, IV_LENGTH, 0)

def AddIV_LENGTH(builder, IV_LENGTH):
    SDLAddIV_LENGTH(builder, IV_LENGTH)

def SDLAddIV(builder, IV):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(IV), 0)

def AddIV(builder, IV):
    SDLAddIV(builder, IV)

def SDLStartIVVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartIVVector(builder, numElems):
    return SDLStartIVVector(builder, numElems)

def SDLAddMAC_LENGTH(builder, MAC_LENGTH):
    builder.PrependUint8Slot(3, MAC_LENGTH, 0)

def AddMAC_LENGTH(builder, MAC_LENGTH):
    SDLAddMAC_LENGTH(builder, MAC_LENGTH)

def SDLAddMAC(builder, MAC):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(MAC), 0)

def AddMAC(builder, MAC):
    SDLAddMAC(builder, MAC)

def SDLStartMACVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartMACVector(builder, numElems):
    return SDLStartMACVector(builder, numElems)

def SDLAddPAD_LENGTH(builder, PAD_LENGTH):
    builder.PrependUint8Slot(5, PAD_LENGTH, 0)

def AddPAD_LENGTH(builder, PAD_LENGTH):
    SDLAddPAD_LENGTH(builder, PAD_LENGTH)

def SDLAddPAYLOAD(builder, PAYLOAD):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD), 0)

def AddPAYLOAD(builder, PAYLOAD):
    SDLAddPAYLOAD(builder, PAYLOAD)

def SDLStartPAYLOADVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPAYLOADVector(builder, numElems):
    return SDLStartPAYLOADVector(builder, numElems)

def SDLEnd(builder):
    return builder.EndObject()

def End(builder):
    return SDLEnd(builder)

try:
    from typing import List
except:
    pass

class SDLT(object):

    # SDLT
    def __init__(self):
        self.SPI = 0  # type: int
        self.IV_LENGTH = 0  # type: int
        self.IV = None  # type: List[int]
        self.MAC_LENGTH = 0  # type: int
        self.MAC = None  # type: List[int]
        self.PAD_LENGTH = 0  # type: int
        self.PAYLOAD = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        SDL = SDL()
        SDL.Init(buf, pos)
        return cls.InitFromObj(SDL)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, SDL):
        x = SDLT()
        x._UnPack(SDL)
        return x

    # SDLT
    def _UnPack(self, SDL):
        if SDL is None:
            return
        self.SPI = SDL.SPI()
        self.IV_LENGTH = SDL.IV_LENGTH()
        if not SDL.IVIsNone():
            if np is None:
                self.IV = []
                for i in range(SDL.IVLength()):
                    self.IV.append(SDL.IV(i))
            else:
                self.IV = SDL.IVAsNumpy()
        self.MAC_LENGTH = SDL.MAC_LENGTH()
        if not SDL.MACIsNone():
            if np is None:
                self.MAC = []
                for i in range(SDL.MACLength()):
                    self.MAC.append(SDL.MAC(i))
            else:
                self.MAC = SDL.MACAsNumpy()
        self.PAD_LENGTH = SDL.PAD_LENGTH()
        if not SDL.PAYLOADIsNone():
            if np is None:
                self.PAYLOAD = []
                for i in range(SDL.PAYLOADLength()):
                    self.PAYLOAD.append(SDL.PAYLOAD(i))
            else:
                self.PAYLOAD = SDL.PAYLOADAsNumpy()

    # SDLT
    def Pack(self, builder):
        if self.IV is not None:
            if np is not None and type(self.IV) is np.ndarray:
                IV = builder.CreateNumpyVector(self.IV)
            else:
                SDLStartIVVector(builder, len(self.IV))
                for i in reversed(range(len(self.IV))):
                    builder.PrependUint8(self.IV[i])
                IV = builder.EndVector()
        if self.MAC is not None:
            if np is not None and type(self.MAC) is np.ndarray:
                MAC = builder.CreateNumpyVector(self.MAC)
            else:
                SDLStartMACVector(builder, len(self.MAC))
                for i in reversed(range(len(self.MAC))):
                    builder.PrependUint8(self.MAC[i])
                MAC = builder.EndVector()
        if self.PAYLOAD is not None:
            if np is not None and type(self.PAYLOAD) is np.ndarray:
                PAYLOAD = builder.CreateNumpyVector(self.PAYLOAD)
            else:
                SDLStartPAYLOADVector(builder, len(self.PAYLOAD))
                for i in reversed(range(len(self.PAYLOAD))):
                    builder.PrependUint8(self.PAYLOAD[i])
                PAYLOAD = builder.EndVector()
        SDLStart(builder)
        SDLAddSPI(builder, self.SPI)
        SDLAddIV_LENGTH(builder, self.IV_LENGTH)
        if self.IV is not None:
            SDLAddIV(builder, IV)
        SDLAddMAC_LENGTH(builder, self.MAC_LENGTH)
        if self.MAC is not None:
            SDLAddMAC(builder, MAC)
        SDLAddPAD_LENGTH(builder, self.PAD_LENGTH)
        if self.PAYLOAD is not None:
            SDLAddPAYLOAD(builder, PAYLOAD)
        SDL = SDLEnd(builder)
        return SDL
