# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Cursor on Target Event
class COT(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = COT()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCOT(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def COTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4F\x54", size_prefixed=size_prefixed)

    # COT
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Schema version
    # COT
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Globally unique event identifier
    # COT
    def UID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # CoT event type (dot-delimited MIL-STD-2525/APP-6 hierarchy)
    # e.g. "a-f-G-U-C" = atom, friend, Ground, Unit, Combat
    # COT
    def TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # How the event was generated (e.g. "m-g" = machine GPS)
    # COT
    def HOW(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Time the event was generated (ISO 8601)
    # COT
    def TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Time the event information was valid (ISO 8601)
    # COT
    def START(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Time the event information is no longer valid (ISO 8601)
    # COT
    def STALE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Access control marking
    # COT
    def ACCESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Quality of service
    # COT
    def QOS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Operational status
    # COT
    def OPEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Point location
    # COT
    def POINT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from COTPoint import COTPoint
            obj = COTPoint()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Detailed event information
    # COT
    def DETAIL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from COTDetail import COTDetail
            obj = COTDetail()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def COTStart(builder):
    builder.StartObject(12)

def Start(builder):
    COTStart(builder)

def COTAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    COTAddVERSION(builder, VERSION)

def COTAddUID(builder, UID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(UID), 0)

def AddUID(builder, UID):
    COTAddUID(builder, UID)

def COTAddTYPE(builder, TYPE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TYPE), 0)

def AddTYPE(builder, TYPE):
    COTAddTYPE(builder, TYPE)

def COTAddHOW(builder, HOW):
    builder.PrependInt8Slot(3, HOW, 0)

def AddHOW(builder, HOW):
    COTAddHOW(builder, HOW)

def COTAddTIME(builder, TIME):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(TIME), 0)

def AddTIME(builder, TIME):
    COTAddTIME(builder, TIME)

def COTAddSTART(builder, START):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(START), 0)

def AddSTART(builder, START):
    COTAddSTART(builder, START)

def COTAddSTALE(builder, STALE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(STALE), 0)

def AddSTALE(builder, STALE):
    COTAddSTALE(builder, STALE)

def COTAddACCESS(builder, ACCESS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(ACCESS), 0)

def AddACCESS(builder, ACCESS):
    COTAddACCESS(builder, ACCESS)

def COTAddQOS(builder, QOS):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(QOS), 0)

def AddQOS(builder, QOS):
    COTAddQOS(builder, QOS)

def COTAddOPEX(builder, OPEX):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(OPEX), 0)

def AddOPEX(builder, OPEX):
    COTAddOPEX(builder, OPEX)

def COTAddPOINT(builder, POINT):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(POINT), 0)

def AddPOINT(builder, POINT):
    COTAddPOINT(builder, POINT)

def COTAddDETAIL(builder, DETAIL):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(DETAIL), 0)

def AddDETAIL(builder, DETAIL):
    COTAddDETAIL(builder, DETAIL)

def COTEnd(builder):
    return builder.EndObject()

def End(builder):
    return COTEnd(builder)

import COTDetail
import COTPoint
try:
    from typing import Optional
except:
    pass

class COTT(object):

    # COTT
    def __init__(self):
        self.VERSION = None  # type: str
        self.UID = None  # type: str
        self.TYPE = None  # type: str
        self.HOW = 0  # type: int
        self.TIME = None  # type: str
        self.START = None  # type: str
        self.STALE = None  # type: str
        self.ACCESS = None  # type: str
        self.QOS = None  # type: str
        self.OPEX = None  # type: str
        self.POINT = None  # type: Optional[COTPoint.COTPointT]
        self.DETAIL = None  # type: Optional[COTDetail.COTDetailT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        COT = COT()
        COT.Init(buf, pos)
        return cls.InitFromObj(COT)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, COT):
        x = COTT()
        x._UnPack(COT)
        return x

    # COTT
    def _UnPack(self, COT):
        if COT is None:
            return
        self.VERSION = COT.VERSION()
        self.UID = COT.UID()
        self.TYPE = COT.TYPE()
        self.HOW = COT.HOW()
        self.TIME = COT.TIME()
        self.START = COT.START()
        self.STALE = COT.STALE()
        self.ACCESS = COT.ACCESS()
        self.QOS = COT.QOS()
        self.OPEX = COT.OPEX()
        if COT.POINT() is not None:
            self.POINT = COTPoint.COTPointT.InitFromObj(COT.POINT())
        if COT.DETAIL() is not None:
            self.DETAIL = COTDetail.COTDetailT.InitFromObj(COT.DETAIL())

    # COTT
    def Pack(self, builder):
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        if self.UID is not None:
            UID = builder.CreateString(self.UID)
        if self.TYPE is not None:
            TYPE = builder.CreateString(self.TYPE)
        if self.TIME is not None:
            TIME = builder.CreateString(self.TIME)
        if self.START is not None:
            START = builder.CreateString(self.START)
        if self.STALE is not None:
            STALE = builder.CreateString(self.STALE)
        if self.ACCESS is not None:
            ACCESS = builder.CreateString(self.ACCESS)
        if self.QOS is not None:
            QOS = builder.CreateString(self.QOS)
        if self.OPEX is not None:
            OPEX = builder.CreateString(self.OPEX)
        if self.POINT is not None:
            POINT = self.POINT.Pack(builder)
        if self.DETAIL is not None:
            DETAIL = self.DETAIL.Pack(builder)
        COTStart(builder)
        if self.VERSION is not None:
            COTAddVERSION(builder, VERSION)
        if self.UID is not None:
            COTAddUID(builder, UID)
        if self.TYPE is not None:
            COTAddTYPE(builder, TYPE)
        COTAddHOW(builder, self.HOW)
        if self.TIME is not None:
            COTAddTIME(builder, TIME)
        if self.START is not None:
            COTAddSTART(builder, START)
        if self.STALE is not None:
            COTAddSTALE(builder, STALE)
        if self.ACCESS is not None:
            COTAddACCESS(builder, ACCESS)
        if self.QOS is not None:
            COTAddQOS(builder, QOS)
        if self.OPEX is not None:
            COTAddOPEX(builder, OPEX)
        if self.POINT is not None:
            COTAddPOINT(builder, POINT)
        if self.DETAIL is not None:
            COTAddDETAIL(builder, DETAIL)
        COT = COTEnd(builder)
        return COT
