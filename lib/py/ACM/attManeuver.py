# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Attitude Maneuver
class attManeuver(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = attManeuver()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsattManeuver(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def attManeuverBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x43\x4D", size_prefixed=size_prefixed)

    # attManeuver
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Maneuver epoch start (ISO 8601)
    # attManeuver
    def MAN_EPOCH_START(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Duration in seconds
    # attManeuver
    def DURATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Reference frame
    # attManeuver
    def REF_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Torque about body X in N*m
    # attManeuver
    def TOR_1(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Torque about body Y in N*m
    # attManeuver
    def TOR_2(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Torque about body Z in N*m
    # attManeuver
    def TOR_3(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def attManeuverStart(builder):
    builder.StartObject(6)

def Start(builder):
    attManeuverStart(builder)

def attManeuverAddMAN_EPOCH_START(builder, MAN_EPOCH_START):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(MAN_EPOCH_START), 0)

def AddMAN_EPOCH_START(builder, MAN_EPOCH_START):
    attManeuverAddMAN_EPOCH_START(builder, MAN_EPOCH_START)

def attManeuverAddDURATION(builder, DURATION):
    builder.PrependFloat64Slot(1, DURATION, 0.0)

def AddDURATION(builder, DURATION):
    attManeuverAddDURATION(builder, DURATION)

def attManeuverAddREF_FRAME(builder, REF_FRAME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(REF_FRAME), 0)

def AddREF_FRAME(builder, REF_FRAME):
    attManeuverAddREF_FRAME(builder, REF_FRAME)

def attManeuverAddTOR_1(builder, TOR_1):
    builder.PrependFloat64Slot(3, TOR_1, 0.0)

def AddTOR_1(builder, TOR_1):
    attManeuverAddTOR_1(builder, TOR_1)

def attManeuverAddTOR_2(builder, TOR_2):
    builder.PrependFloat64Slot(4, TOR_2, 0.0)

def AddTOR_2(builder, TOR_2):
    attManeuverAddTOR_2(builder, TOR_2)

def attManeuverAddTOR_3(builder, TOR_3):
    builder.PrependFloat64Slot(5, TOR_3, 0.0)

def AddTOR_3(builder, TOR_3):
    attManeuverAddTOR_3(builder, TOR_3)

def attManeuverEnd(builder):
    return builder.EndObject()

def End(builder):
    return attManeuverEnd(builder)


class attManeuverT(object):

    # attManeuverT
    def __init__(self):
        self.MAN_EPOCH_START = None  # type: str
        self.DURATION = 0.0  # type: float
        self.REF_FRAME = None  # type: str
        self.TOR_1 = 0.0  # type: float
        self.TOR_2 = 0.0  # type: float
        self.TOR_3 = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        attManeuver = attManeuver()
        attManeuver.Init(buf, pos)
        return cls.InitFromObj(attManeuver)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, attManeuver):
        x = attManeuverT()
        x._UnPack(attManeuver)
        return x

    # attManeuverT
    def _UnPack(self, attManeuver):
        if attManeuver is None:
            return
        self.MAN_EPOCH_START = attManeuver.MAN_EPOCH_START()
        self.DURATION = attManeuver.DURATION()
        self.REF_FRAME = attManeuver.REF_FRAME()
        self.TOR_1 = attManeuver.TOR_1()
        self.TOR_2 = attManeuver.TOR_2()
        self.TOR_3 = attManeuver.TOR_3()

    # attManeuverT
    def Pack(self, builder):
        if self.MAN_EPOCH_START is not None:
            MAN_EPOCH_START = builder.CreateString(self.MAN_EPOCH_START)
        if self.REF_FRAME is not None:
            REF_FRAME = builder.CreateString(self.REF_FRAME)
        attManeuverStart(builder)
        if self.MAN_EPOCH_START is not None:
            attManeuverAddMAN_EPOCH_START(builder, MAN_EPOCH_START)
        attManeuverAddDURATION(builder, self.DURATION)
        if self.REF_FRAME is not None:
            attManeuverAddREF_FRAME(builder, REF_FRAME)
        attManeuverAddTOR_1(builder, self.TOR_1)
        attManeuverAddTOR_2(builder, self.TOR_2)
        attManeuverAddTOR_3(builder, self.TOR_3)
        attManeuver = attManeuverEnd(builder)
        return attManeuver
