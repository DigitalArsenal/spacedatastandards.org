# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Attitude Covariance
class attCovariance(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = attCovariance()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsattCovariance(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def attCovarianceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x43\x4D", size_prefixed=size_prefixed)

    # attCovariance
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Covariance type
    # attCovariance
    def COV_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Reference frame
    # attCovariance
    def COV_REF_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Epoch (ISO 8601)
    # attCovariance
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Upper-triangular covariance matrix elements (row-major)
    # attCovariance
    def COV(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # attCovariance
    def COVAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # attCovariance
    def COVLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # attCovariance
    def COVIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def attCovarianceStart(builder):
    builder.StartObject(4)

def Start(builder):
    attCovarianceStart(builder)

def attCovarianceAddCOV_TYPE(builder, COV_TYPE):
    builder.PrependInt8Slot(0, COV_TYPE, 0)

def AddCOV_TYPE(builder, COV_TYPE):
    attCovarianceAddCOV_TYPE(builder, COV_TYPE)

def attCovarianceAddCOV_REF_FRAME(builder, COV_REF_FRAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(COV_REF_FRAME), 0)

def AddCOV_REF_FRAME(builder, COV_REF_FRAME):
    attCovarianceAddCOV_REF_FRAME(builder, COV_REF_FRAME)

def attCovarianceAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    attCovarianceAddEPOCH(builder, EPOCH)

def attCovarianceAddCOV(builder, COV):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(COV), 0)

def AddCOV(builder, COV):
    attCovarianceAddCOV(builder, COV)

def attCovarianceStartCOVVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartCOVVector(builder, numElems):
    return attCovarianceStartCOVVector(builder, numElems)

def attCovarianceEnd(builder):
    return builder.EndObject()

def End(builder):
    return attCovarianceEnd(builder)

try:
    from typing import List
except:
    pass

class attCovarianceT(object):

    # attCovarianceT
    def __init__(self):
        self.COV_TYPE = 0  # type: int
        self.COV_REF_FRAME = None  # type: str
        self.EPOCH = None  # type: str
        self.COV = None  # type: List[float]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        attCovariance = attCovariance()
        attCovariance.Init(buf, pos)
        return cls.InitFromObj(attCovariance)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, attCovariance):
        x = attCovarianceT()
        x._UnPack(attCovariance)
        return x

    # attCovarianceT
    def _UnPack(self, attCovariance):
        if attCovariance is None:
            return
        self.COV_TYPE = attCovariance.COV_TYPE()
        self.COV_REF_FRAME = attCovariance.COV_REF_FRAME()
        self.EPOCH = attCovariance.EPOCH()
        if not attCovariance.COVIsNone():
            if np is None:
                self.COV = []
                for i in range(attCovariance.COVLength()):
                    self.COV.append(attCovariance.COV(i))
            else:
                self.COV = attCovariance.COVAsNumpy()

    # attCovarianceT
    def Pack(self, builder):
        if self.COV_REF_FRAME is not None:
            COV_REF_FRAME = builder.CreateString(self.COV_REF_FRAME)
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        if self.COV is not None:
            if np is not None and type(self.COV) is np.ndarray:
                COV = builder.CreateNumpyVector(self.COV)
            else:
                attCovarianceStartCOVVector(builder, len(self.COV))
                for i in reversed(range(len(self.COV))):
                    builder.PrependFloat64(self.COV[i])
                COV = builder.EndVector()
        attCovarianceStart(builder)
        attCovarianceAddCOV_TYPE(builder, self.COV_TYPE)
        if self.COV_REF_FRAME is not None:
            attCovarianceAddCOV_REF_FRAME(builder, COV_REF_FRAME)
        if self.EPOCH is not None:
            attCovarianceAddEPOCH(builder, EPOCH)
        if self.COV is not None:
            attCovarianceAddCOV(builder, COV)
        attCovariance = attCovarianceEnd(builder)
        return attCovariance
