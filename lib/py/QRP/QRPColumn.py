# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One column descriptor of a page, in record field order.
class QRPColumn(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = QRPColumn()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsQRPColumn(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def QRPColumnBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x51\x52\x50", size_prefixed=size_prefixed)

    # QRPColumn
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # QRPColumn
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Scalar type of the field as declared in the standard, e.g. "string",
    # "double", "int64", "bool", "enum".
    # QRPColumn
    def TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Unit of the field when the standard declares one, else empty.
    # QRPColumn
    def UNIT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # True when the serving node can order the page by this column.
    # QRPColumn
    def SORTABLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # True when the serving node holds an index on this column.
    # QRPColumn
    def INDEXED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def QRPColumnStart(builder):
    builder.StartObject(5)

def Start(builder):
    QRPColumnStart(builder)

def QRPColumnAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    QRPColumnAddNAME(builder, NAME)

def QRPColumnAddTYPE(builder, TYPE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(TYPE), 0)

def AddTYPE(builder, TYPE):
    QRPColumnAddTYPE(builder, TYPE)

def QRPColumnAddUNIT(builder, UNIT):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(UNIT), 0)

def AddUNIT(builder, UNIT):
    QRPColumnAddUNIT(builder, UNIT)

def QRPColumnAddSORTABLE(builder, SORTABLE):
    builder.PrependBoolSlot(3, SORTABLE, 0)

def AddSORTABLE(builder, SORTABLE):
    QRPColumnAddSORTABLE(builder, SORTABLE)

def QRPColumnAddINDEXED(builder, INDEXED):
    builder.PrependBoolSlot(4, INDEXED, 0)

def AddINDEXED(builder, INDEXED):
    QRPColumnAddINDEXED(builder, INDEXED)

def QRPColumnEnd(builder):
    return builder.EndObject()

def End(builder):
    return QRPColumnEnd(builder)


class QRPColumnT(object):

    # QRPColumnT
    def __init__(
        self,
        NAME = None,
        TYPE = None,
        UNIT = None,
        SORTABLE = False,
        INDEXED = False,
    ):
        self.NAME = NAME  # type: Optional[str]
        self.TYPE = TYPE  # type: Optional[str]
        self.UNIT = UNIT  # type: Optional[str]
        self.SORTABLE = SORTABLE  # type: bool
        self.INDEXED = INDEXED  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpQrpcolumn = QRPColumn()
        tmpQrpcolumn.Init(buf, pos)
        return cls.InitFromObj(tmpQrpcolumn)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpQrpcolumn):
        x = QRPColumnT()
        x._UnPack(tmpQrpcolumn)
        return x

    # QRPColumnT
    def _UnPack(self, QRPColumn):
        if QRPColumn is None:
            return
        self.NAME = QRPColumn.NAME()
        self.TYPE = QRPColumn.TYPE()
        self.UNIT = QRPColumn.UNIT()
        self.SORTABLE = QRPColumn.SORTABLE()
        self.INDEXED = QRPColumn.INDEXED()

    # QRPColumnT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.TYPE is not None:
            TYPE = builder.CreateString(self.TYPE)
        if self.UNIT is not None:
            UNIT = builder.CreateString(self.UNIT)
        QRPColumnStart(builder)
        if self.NAME is not None:
            QRPColumnAddNAME(builder, NAME)
        if self.TYPE is not None:
            QRPColumnAddTYPE(builder, TYPE)
        if self.UNIT is not None:
            QRPColumnAddUNIT(builder, UNIT)
        QRPColumnAddSORTABLE(builder, self.SORTABLE)
        QRPColumnAddINDEXED(builder, self.INDEXED)
        QRPColumn = QRPColumnEnd(builder)
        return QRPColumn
