# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One field filter of a request.
class QRPFilter(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = QRPFilter()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsQRPFilter(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def QRPFilterBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x51\x52\x50", size_prefixed=size_prefixed)

    # QRPFilter
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # QRPFilter
    def FIELD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # QRPFilter
    def OP(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Value as text; for In, values separated by commas.
    # QRPFilter
    def VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def QRPFilterStart(builder):
    builder.StartObject(3)

def Start(builder):
    QRPFilterStart(builder)

def QRPFilterAddFIELD(builder, FIELD):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(FIELD), 0)

def AddFIELD(builder, FIELD):
    QRPFilterAddFIELD(builder, FIELD)

def QRPFilterAddOP(builder, OP):
    builder.PrependInt8Slot(1, OP, 0)

def AddOP(builder, OP):
    QRPFilterAddOP(builder, OP)

def QRPFilterAddVALUE(builder, VALUE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VALUE), 0)

def AddVALUE(builder, VALUE):
    QRPFilterAddVALUE(builder, VALUE)

def QRPFilterEnd(builder):
    return builder.EndObject()

def End(builder):
    return QRPFilterEnd(builder)


class QRPFilterT(object):

    # QRPFilterT
    def __init__(
        self,
        FIELD = None,
        OP = 0,
        VALUE = None,
    ):
        self.FIELD = FIELD  # type: Optional[str]
        self.OP = OP  # type: int
        self.VALUE = VALUE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpQrpfilter = QRPFilter()
        tmpQrpfilter.Init(buf, pos)
        return cls.InitFromObj(tmpQrpfilter)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpQrpfilter):
        x = QRPFilterT()
        x._UnPack(tmpQrpfilter)
        return x

    # QRPFilterT
    def _UnPack(self, QRPFilter):
        if QRPFilter is None:
            return
        self.FIELD = QRPFilter.FIELD()
        self.OP = QRPFilter.OP()
        self.VALUE = QRPFilter.VALUE()

    # QRPFilterT
    def Pack(self, builder):
        if self.FIELD is not None:
            FIELD = builder.CreateString(self.FIELD)
        if self.VALUE is not None:
            VALUE = builder.CreateString(self.VALUE)
        QRPFilterStart(builder)
        if self.FIELD is not None:
            QRPFilterAddFIELD(builder, FIELD)
        QRPFilterAddOP(builder, self.OP)
        if self.VALUE is not None:
            QRPFilterAddVALUE(builder, VALUE)
        QRPFilter = QRPFilterEnd(builder)
        return QRPFilter
