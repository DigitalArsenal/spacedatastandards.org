# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Sensor contribution to an orbit determination solution
class odSensorContribution(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = odSensorContribution()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsodSensorContribution(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def odSensorContributionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x42\x44", size_prefixed=size_prefixed)

    # odSensorContribution
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Sensor identifier
    # odSensorContribution
    def SENSOR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Original sensor identifier
    # odSensorContribution
    def ORIG_SENSOR_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of accepted observations from this sensor
    # odSensorContribution
    def NUM_ACCEPTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Number of rejected observations from this sensor
    # odSensorContribution
    def NUM_REJECTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Weighted RMS for this sensor's observations
    # odSensorContribution
    def WRMS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Observation types from this sensor
    # odSensorContribution
    def OB_TYPES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # odSensorContribution
    def OB_TYPESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # odSensorContribution
    def OB_TYPESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

def odSensorContributionStart(builder):
    builder.StartObject(6)

def Start(builder):
    odSensorContributionStart(builder)

def odSensorContributionAddSENSOR_ID(builder, SENSOR_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SENSOR_ID), 0)

def AddSENSOR_ID(builder, SENSOR_ID):
    odSensorContributionAddSENSOR_ID(builder, SENSOR_ID)

def odSensorContributionAddORIG_SENSOR_ID(builder, ORIG_SENSOR_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ORIG_SENSOR_ID), 0)

def AddORIG_SENSOR_ID(builder, ORIG_SENSOR_ID):
    odSensorContributionAddORIG_SENSOR_ID(builder, ORIG_SENSOR_ID)

def odSensorContributionAddNUM_ACCEPTED(builder, NUM_ACCEPTED):
    builder.PrependUint32Slot(2, NUM_ACCEPTED, 0)

def AddNUM_ACCEPTED(builder, NUM_ACCEPTED):
    odSensorContributionAddNUM_ACCEPTED(builder, NUM_ACCEPTED)

def odSensorContributionAddNUM_REJECTED(builder, NUM_REJECTED):
    builder.PrependUint32Slot(3, NUM_REJECTED, 0)

def AddNUM_REJECTED(builder, NUM_REJECTED):
    odSensorContributionAddNUM_REJECTED(builder, NUM_REJECTED)

def odSensorContributionAddWRMS(builder, WRMS):
    builder.PrependFloat64Slot(4, WRMS, 0.0)

def AddWRMS(builder, WRMS):
    odSensorContributionAddWRMS(builder, WRMS)

def odSensorContributionAddOB_TYPES(builder, OB_TYPES):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(OB_TYPES), 0)

def AddOB_TYPES(builder, OB_TYPES):
    odSensorContributionAddOB_TYPES(builder, OB_TYPES)

def odSensorContributionStartOB_TYPESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartOB_TYPESVector(builder, numElems):
    return odSensorContributionStartOB_TYPESVector(builder, numElems)

def odSensorContributionEnd(builder):
    return builder.EndObject()

def End(builder):
    return odSensorContributionEnd(builder)

try:
    from typing import List
except:
    pass

class odSensorContributionT(object):

    # odSensorContributionT
    def __init__(self):
        self.SENSOR_ID = None  # type: str
        self.ORIG_SENSOR_ID = None  # type: str
        self.NUM_ACCEPTED = 0  # type: int
        self.NUM_REJECTED = 0  # type: int
        self.WRMS = 0.0  # type: float
        self.OB_TYPES = None  # type: List[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        odSensorContribution = odSensorContribution()
        odSensorContribution.Init(buf, pos)
        return cls.InitFromObj(odSensorContribution)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, odSensorContribution):
        x = odSensorContributionT()
        x._UnPack(odSensorContribution)
        return x

    # odSensorContributionT
    def _UnPack(self, odSensorContribution):
        if odSensorContribution is None:
            return
        self.SENSOR_ID = odSensorContribution.SENSOR_ID()
        self.ORIG_SENSOR_ID = odSensorContribution.ORIG_SENSOR_ID()
        self.NUM_ACCEPTED = odSensorContribution.NUM_ACCEPTED()
        self.NUM_REJECTED = odSensorContribution.NUM_REJECTED()
        self.WRMS = odSensorContribution.WRMS()
        if not odSensorContribution.OB_TYPESIsNone():
            self.OB_TYPES = []
            for i in range(odSensorContribution.OB_TYPESLength()):
                self.OB_TYPES.append(odSensorContribution.OB_TYPES(i))

    # odSensorContributionT
    def Pack(self, builder):
        if self.SENSOR_ID is not None:
            SENSOR_ID = builder.CreateString(self.SENSOR_ID)
        if self.ORIG_SENSOR_ID is not None:
            ORIG_SENSOR_ID = builder.CreateString(self.ORIG_SENSOR_ID)
        if self.OB_TYPES is not None:
            OB_TYPESlist = []
            for i in range(len(self.OB_TYPES)):
                OB_TYPESlist.append(builder.CreateString(self.OB_TYPES[i]))
            odSensorContributionStartOB_TYPESVector(builder, len(self.OB_TYPES))
            for i in reversed(range(len(self.OB_TYPES))):
                builder.PrependUOffsetTRelative(OB_TYPESlist[i])
            OB_TYPES = builder.EndVector()
        odSensorContributionStart(builder)
        if self.SENSOR_ID is not None:
            odSensorContributionAddSENSOR_ID(builder, SENSOR_ID)
        if self.ORIG_SENSOR_ID is not None:
            odSensorContributionAddORIG_SENSOR_ID(builder, ORIG_SENSOR_ID)
        odSensorContributionAddNUM_ACCEPTED(builder, self.NUM_ACCEPTED)
        odSensorContributionAddNUM_REJECTED(builder, self.NUM_REJECTED)
        odSensorContributionAddWRMS(builder, self.WRMS)
        if self.OB_TYPES is not None:
            odSensorContributionAddOB_TYPES(builder, OB_TYPES)
        odSensorContribution = odSensorContributionEnd(builder)
        return odSensorContribution
