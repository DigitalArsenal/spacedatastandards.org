# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Coordinate Systems
class CRD(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CRD()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCRD(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CRDBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x52\x44", size_prefixed=size_prefixed)

    # CRD
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CRD
    def X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # CRD
    def Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # CRD
    def Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # CRD
    def VX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # CRD
    def VY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # CRD
    def VZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # CRD
    def FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # CRD
    def ELLIPSOID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # CRD
    def RESERVED(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CRD
    def RESERVEDAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # CRD
    def RESERVEDLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CRD
    def RESERVEDIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

def CRDStart(builder):
    builder.StartObject(9)

def Start(builder):
    CRDStart(builder)

def CRDAddX(builder, X):
    builder.PrependFloat64Slot(0, X, 0.0)

def AddX(builder, X):
    CRDAddX(builder, X)

def CRDAddY(builder, Y):
    builder.PrependFloat64Slot(1, Y, 0.0)

def AddY(builder, Y):
    CRDAddY(builder, Y)

def CRDAddZ(builder, Z):
    builder.PrependFloat64Slot(2, Z, 0.0)

def AddZ(builder, Z):
    CRDAddZ(builder, Z)

def CRDAddVX(builder, VX):
    builder.PrependFloat64Slot(3, VX, 0.0)

def AddVX(builder, VX):
    CRDAddVX(builder, VX)

def CRDAddVY(builder, VY):
    builder.PrependFloat64Slot(4, VY, 0.0)

def AddVY(builder, VY):
    CRDAddVY(builder, VY)

def CRDAddVZ(builder, VZ):
    builder.PrependFloat64Slot(5, VZ, 0.0)

def AddVZ(builder, VZ):
    CRDAddVZ(builder, VZ)

def CRDAddFRAME(builder, FRAME):
    builder.PrependUint8Slot(6, FRAME, 0)

def AddFRAME(builder, FRAME):
    CRDAddFRAME(builder, FRAME)

def CRDAddELLIPSOID(builder, ELLIPSOID):
    builder.PrependUint8Slot(7, ELLIPSOID, 0)

def AddELLIPSOID(builder, ELLIPSOID):
    CRDAddELLIPSOID(builder, ELLIPSOID)

def CRDAddRESERVED(builder, RESERVED):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(RESERVED), 0)

def AddRESERVED(builder, RESERVED):
    CRDAddRESERVED(builder, RESERVED)

def CRDStartRESERVEDVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartRESERVEDVector(builder, numElems):
    return CRDStartRESERVEDVector(builder, numElems)

def CRDEnd(builder):
    return builder.EndObject()

def End(builder):
    return CRDEnd(builder)

try:
    from typing import List
except:
    pass

class CRDT(object):

    # CRDT
    def __init__(self):
        self.X = 0.0  # type: float
        self.Y = 0.0  # type: float
        self.Z = 0.0  # type: float
        self.VX = 0.0  # type: float
        self.VY = 0.0  # type: float
        self.VZ = 0.0  # type: float
        self.FRAME = 0  # type: int
        self.ELLIPSOID = 0  # type: int
        self.RESERVED = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        CRD = CRD()
        CRD.Init(buf, pos)
        return cls.InitFromObj(CRD)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, CRD):
        x = CRDT()
        x._UnPack(CRD)
        return x

    # CRDT
    def _UnPack(self, CRD):
        if CRD is None:
            return
        self.X = CRD.X()
        self.Y = CRD.Y()
        self.Z = CRD.Z()
        self.VX = CRD.VX()
        self.VY = CRD.VY()
        self.VZ = CRD.VZ()
        self.FRAME = CRD.FRAME()
        self.ELLIPSOID = CRD.ELLIPSOID()
        if not CRD.RESERVEDIsNone():
            if np is None:
                self.RESERVED = []
                for i in range(CRD.RESERVEDLength()):
                    self.RESERVED.append(CRD.RESERVED(i))
            else:
                self.RESERVED = CRD.RESERVEDAsNumpy()

    # CRDT
    def Pack(self, builder):
        if self.RESERVED is not None:
            if np is not None and type(self.RESERVED) is np.ndarray:
                RESERVED = builder.CreateNumpyVector(self.RESERVED)
            else:
                CRDStartRESERVEDVector(builder, len(self.RESERVED))
                for i in reversed(range(len(self.RESERVED))):
                    builder.PrependUint8(self.RESERVED[i])
                RESERVED = builder.EndVector()
        CRDStart(builder)
        CRDAddX(builder, self.X)
        CRDAddY(builder, self.Y)
        CRDAddZ(builder, self.Z)
        CRDAddVX(builder, self.VX)
        CRDAddVY(builder, self.VY)
        CRDAddVZ(builder, self.VZ)
        CRDAddFRAME(builder, self.FRAME)
        CRDAddELLIPSOID(builder, self.ELLIPSOID)
        if self.RESERVED is not None:
            CRDAddRESERVED(builder, RESERVED)
        CRD = CRDEnd(builder)
        return CRD
