# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Standards Record Index — local wrapper for a standards payload's identity
# and mutation metadata, participating in shared FlatSQL query and cascade
# paths across plugins that read the same record universe.
class SRI(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SRI()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSRI(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SRIBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x52\x49", size_prefixed=size_prefixed)

    # SRI
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable host-local record key for one standards payload.
    # SRI
    def RECORD_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SDS schema family for the record (`OMM`, `CAT`, `OEM`, `CDM`, etc.).
    # SRI
    def SCHEMA_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Attachment role when one standards message produces multiple entity records.
    # SRI
    def ROLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Ingest path that registered this record (`stream-*`, `manual-api`, etc.).
    # SRI
    def ATTACHED_VIA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # High-level payload representation retained by the mutation registry.
    # SRI
    def PAYLOAD_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Milliseconds since epoch when the local mutation record was last refreshed.
    # SRI
    def UPDATED_AT_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Reserved for forward-compatible growth.
    # SRI
    def RESERVED(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SRI
    def RESERVEDAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SRI
    def RESERVEDLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SRI
    def RESERVEDIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def SRIStart(builder):
    builder.StartObject(7)

def Start(builder):
    SRIStart(builder)

def SRIAddRECORD_KEY(builder, RECORD_KEY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORD_KEY), 0)

def AddRECORD_KEY(builder, RECORD_KEY):
    SRIAddRECORD_KEY(builder, RECORD_KEY)

def SRIAddSCHEMA_NAME(builder, SCHEMA_NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_NAME), 0)

def AddSCHEMA_NAME(builder, SCHEMA_NAME):
    SRIAddSCHEMA_NAME(builder, SCHEMA_NAME)

def SRIAddROLE(builder, ROLE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ROLE), 0)

def AddROLE(builder, ROLE):
    SRIAddROLE(builder, ROLE)

def SRIAddATTACHED_VIA(builder, ATTACHED_VIA):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(ATTACHED_VIA), 0)

def AddATTACHED_VIA(builder, ATTACHED_VIA):
    SRIAddATTACHED_VIA(builder, ATTACHED_VIA)

def SRIAddPAYLOAD_KIND(builder, PAYLOAD_KIND):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD_KIND), 0)

def AddPAYLOAD_KIND(builder, PAYLOAD_KIND):
    SRIAddPAYLOAD_KIND(builder, PAYLOAD_KIND)

def SRIAddUPDATED_AT_MS(builder, UPDATED_AT_MS):
    builder.PrependFloat64Slot(5, UPDATED_AT_MS, 0.0)

def AddUPDATED_AT_MS(builder, UPDATED_AT_MS):
    SRIAddUPDATED_AT_MS(builder, UPDATED_AT_MS)

def SRIAddRESERVED(builder, RESERVED):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(RESERVED), 0)

def AddRESERVED(builder, RESERVED):
    SRIAddRESERVED(builder, RESERVED)

def SRIStartRESERVEDVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartRESERVEDVector(builder, numElems):
    return SRIStartRESERVEDVector(builder, numElems)

def SRICreateRESERVEDVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateRESERVEDVector(builder, data):
    SRICreateRESERVEDVector(builder, data)

def SRIEnd(builder):
    return builder.EndObject()

def End(builder):
    return SRIEnd(builder)

try:
    from typing import List
except:
    pass

class SRIT(object):

    # SRIT
    def __init__(
        self,
        RECORD_KEY = None,
        SCHEMA_NAME = None,
        ROLE = None,
        ATTACHED_VIA = None,
        PAYLOAD_KIND = None,
        UPDATED_AT_MS = 0.0,
        RESERVED = None,
    ):
        self.RECORD_KEY = RECORD_KEY  # type: Optional[str]
        self.SCHEMA_NAME = SCHEMA_NAME  # type: Optional[str]
        self.ROLE = ROLE  # type: Optional[str]
        self.ATTACHED_VIA = ATTACHED_VIA  # type: Optional[str]
        self.PAYLOAD_KIND = PAYLOAD_KIND  # type: Optional[str]
        self.UPDATED_AT_MS = UPDATED_AT_MS  # type: float
        self.RESERVED = RESERVED  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSri = SRI()
        tmpSri.Init(buf, pos)
        return cls.InitFromObj(tmpSri)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSri):
        x = SRIT()
        x._UnPack(tmpSri)
        return x

    # SRIT
    def _UnPack(self, SRI):
        if SRI is None:
            return
        self.RECORD_KEY = SRI.RECORD_KEY()
        self.SCHEMA_NAME = SRI.SCHEMA_NAME()
        self.ROLE = SRI.ROLE()
        self.ATTACHED_VIA = SRI.ATTACHED_VIA()
        self.PAYLOAD_KIND = SRI.PAYLOAD_KIND()
        self.UPDATED_AT_MS = SRI.UPDATED_AT_MS()
        if not SRI.RESERVEDIsNone():
            if np is None:
                self.RESERVED = []
                for i in range(SRI.RESERVEDLength()):
                    self.RESERVED.append(SRI.RESERVED(i))
            else:
                self.RESERVED = SRI.RESERVEDAsNumpy()

    # SRIT
    def Pack(self, builder):
        if self.RECORD_KEY is not None:
            RECORD_KEY = builder.CreateString(self.RECORD_KEY)
        if self.SCHEMA_NAME is not None:
            SCHEMA_NAME = builder.CreateString(self.SCHEMA_NAME)
        if self.ROLE is not None:
            ROLE = builder.CreateString(self.ROLE)
        if self.ATTACHED_VIA is not None:
            ATTACHED_VIA = builder.CreateString(self.ATTACHED_VIA)
        if self.PAYLOAD_KIND is not None:
            PAYLOAD_KIND = builder.CreateString(self.PAYLOAD_KIND)
        if self.RESERVED is not None:
            if np is not None and type(self.RESERVED) is np.ndarray:
                RESERVED = builder.CreateNumpyVector(self.RESERVED)
            else:
                SRIStartRESERVEDVector(builder, len(self.RESERVED))
                for i in reversed(range(len(self.RESERVED))):
                    builder.PrependUint8(self.RESERVED[i])
                RESERVED = builder.EndVector()
        SRIStart(builder)
        if self.RECORD_KEY is not None:
            SRIAddRECORD_KEY(builder, RECORD_KEY)
        if self.SCHEMA_NAME is not None:
            SRIAddSCHEMA_NAME(builder, SCHEMA_NAME)
        if self.ROLE is not None:
            SRIAddROLE(builder, ROLE)
        if self.ATTACHED_VIA is not None:
            SRIAddATTACHED_VIA(builder, ATTACHED_VIA)
        if self.PAYLOAD_KIND is not None:
            SRIAddPAYLOAD_KIND(builder, PAYLOAD_KIND)
        SRIAddUPDATED_AT_MS(builder, self.UPDATED_AT_MS)
        if self.RESERVED is not None:
            SRIAddRESERVED(builder, RESERVED)
        SRI = SRIEnd(builder)
        return SRI
