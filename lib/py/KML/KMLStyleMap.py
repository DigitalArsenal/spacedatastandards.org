# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Style map (normal/highlight pair)
class KMLStyleMap(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLStyleMap()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLStyleMap(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLStyleMapBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLStyleMap
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Style map identifier
    # KMLStyleMap
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Pairs
    # KMLStyleMap
    def PAIRS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from KMLStyleMapPair import KMLStyleMapPair
            obj = KMLStyleMapPair()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # KMLStyleMap
    def PAIRSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KMLStyleMap
    def PAIRSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def KMLStyleMapStart(builder):
    builder.StartObject(2)

def Start(builder):
    KMLStyleMapStart(builder)

def KMLStyleMapAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    KMLStyleMapAddID(builder, ID)

def KMLStyleMapAddPAIRS(builder, PAIRS):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PAIRS), 0)

def AddPAIRS(builder, PAIRS):
    KMLStyleMapAddPAIRS(builder, PAIRS)

def KMLStyleMapStartPAIRSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPAIRSVector(builder, numElems):
    return KMLStyleMapStartPAIRSVector(builder, numElems)

def KMLStyleMapEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLStyleMapEnd(builder)

import KMLStyleMapPair
try:
    from typing import List
except:
    pass

class KMLStyleMapT(object):

    # KMLStyleMapT
    def __init__(self):
        self.ID = None  # type: str
        self.PAIRS = None  # type: List[KMLStyleMapPair.KMLStyleMapPairT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmlstyleMap = KMLStyleMap()
        kmlstyleMap.Init(buf, pos)
        return cls.InitFromObj(kmlstyleMap)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmlstyleMap):
        x = KMLStyleMapT()
        x._UnPack(kmlstyleMap)
        return x

    # KMLStyleMapT
    def _UnPack(self, kmlstyleMap):
        if kmlstyleMap is None:
            return
        self.ID = kmlstyleMap.ID()
        if not kmlstyleMap.PAIRSIsNone():
            self.PAIRS = []
            for i in range(kmlstyleMap.PAIRSLength()):
                if kmlstyleMap.PAIRS(i) is None:
                    self.PAIRS.append(None)
                else:
                    kMLStyleMapPair_ = KMLStyleMapPair.KMLStyleMapPairT.InitFromObj(kmlstyleMap.PAIRS(i))
                    self.PAIRS.append(kMLStyleMapPair_)

    # KMLStyleMapT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.PAIRS is not None:
            PAIRSlist = []
            for i in range(len(self.PAIRS)):
                PAIRSlist.append(self.PAIRS[i].Pack(builder))
            KMLStyleMapStartPAIRSVector(builder, len(self.PAIRS))
            for i in reversed(range(len(self.PAIRS))):
                builder.PrependUOffsetTRelative(PAIRSlist[i])
            PAIRS = builder.EndVector()
        KMLStyleMapStart(builder)
        if self.ID is not None:
            KMLStyleMapAddID(builder, ID)
        if self.PAIRS is not None:
            KMLStyleMapAddPAIRS(builder, PAIRS)
        kmlstyleMap = KMLStyleMapEnd(builder)
        return kmlstyleMap
