# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Point geometry
class KMLPoint(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLPoint()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLPoint(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLPointBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLPoint
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Coordinate
    # KMLPoint
    def COORDINATES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from KMLCoordinate import KMLCoordinate
            obj = KMLCoordinate()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Altitude mode
    # KMLPoint
    def ALTITUDE_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Whether to extrude to ground
    # KMLPoint
    def EXTRUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def KMLPointStart(builder):
    builder.StartObject(3)

def Start(builder):
    KMLPointStart(builder)

def KMLPointAddCOORDINATES(builder, COORDINATES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(COORDINATES), 0)

def AddCOORDINATES(builder, COORDINATES):
    KMLPointAddCOORDINATES(builder, COORDINATES)

def KMLPointAddALTITUDE_MODE(builder, ALTITUDE_MODE):
    builder.PrependInt8Slot(1, ALTITUDE_MODE, 0)

def AddALTITUDE_MODE(builder, ALTITUDE_MODE):
    KMLPointAddALTITUDE_MODE(builder, ALTITUDE_MODE)

def KMLPointAddEXTRUDE(builder, EXTRUDE):
    builder.PrependBoolSlot(2, EXTRUDE, 0)

def AddEXTRUDE(builder, EXTRUDE):
    KMLPointAddEXTRUDE(builder, EXTRUDE)

def KMLPointEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLPointEnd(builder)

import KMLCoordinate
try:
    from typing import Optional
except:
    pass

class KMLPointT(object):

    # KMLPointT
    def __init__(self):
        self.COORDINATES = None  # type: Optional[KMLCoordinate.KMLCoordinateT]
        self.ALTITUDE_MODE = 0  # type: int
        self.EXTRUDE = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmlpoint = KMLPoint()
        kmlpoint.Init(buf, pos)
        return cls.InitFromObj(kmlpoint)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmlpoint):
        x = KMLPointT()
        x._UnPack(kmlpoint)
        return x

    # KMLPointT
    def _UnPack(self, kmlpoint):
        if kmlpoint is None:
            return
        if kmlpoint.COORDINATES() is not None:
            self.COORDINATES = KMLCoordinate.KMLCoordinateT.InitFromObj(kmlpoint.COORDINATES())
        self.ALTITUDE_MODE = kmlpoint.ALTITUDE_MODE()
        self.EXTRUDE = kmlpoint.EXTRUDE()

    # KMLPointT
    def Pack(self, builder):
        if self.COORDINATES is not None:
            COORDINATES = self.COORDINATES.Pack(builder)
        KMLPointStart(builder)
        if self.COORDINATES is not None:
            KMLPointAddCOORDINATES(builder, COORDINATES)
        KMLPointAddALTITUDE_MODE(builder, self.ALTITUDE_MODE)
        KMLPointAddEXTRUDE(builder, self.EXTRUDE)
        kmlpoint = KMLPointEnd(builder)
        return kmlpoint
