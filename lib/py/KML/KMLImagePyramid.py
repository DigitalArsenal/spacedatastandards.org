# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# ImagePyramid for PhotoOverlay
class KMLImagePyramid(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLImagePyramid()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLImagePyramid(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLImagePyramidBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLImagePyramid
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Tile size in pixels
    # KMLImagePyramid
    def TILE_SIZE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Maximum image width
    # KMLImagePyramid
    def MAX_WIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Maximum image height
    # KMLImagePyramid
    def MAX_HEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Grid origin
    # KMLImagePyramid
    def GRID_ORIGIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def KMLImagePyramidStart(builder):
    builder.StartObject(4)

def Start(builder):
    KMLImagePyramidStart(builder)

def KMLImagePyramidAddTILE_SIZE(builder, TILE_SIZE):
    builder.PrependInt32Slot(0, TILE_SIZE, 0)

def AddTILE_SIZE(builder, TILE_SIZE):
    KMLImagePyramidAddTILE_SIZE(builder, TILE_SIZE)

def KMLImagePyramidAddMAX_WIDTH(builder, MAX_WIDTH):
    builder.PrependInt32Slot(1, MAX_WIDTH, 0)

def AddMAX_WIDTH(builder, MAX_WIDTH):
    KMLImagePyramidAddMAX_WIDTH(builder, MAX_WIDTH)

def KMLImagePyramidAddMAX_HEIGHT(builder, MAX_HEIGHT):
    builder.PrependInt32Slot(2, MAX_HEIGHT, 0)

def AddMAX_HEIGHT(builder, MAX_HEIGHT):
    KMLImagePyramidAddMAX_HEIGHT(builder, MAX_HEIGHT)

def KMLImagePyramidAddGRID_ORIGIN(builder, GRID_ORIGIN):
    builder.PrependInt8Slot(3, GRID_ORIGIN, 0)

def AddGRID_ORIGIN(builder, GRID_ORIGIN):
    KMLImagePyramidAddGRID_ORIGIN(builder, GRID_ORIGIN)

def KMLImagePyramidEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLImagePyramidEnd(builder)


class KMLImagePyramidT(object):

    # KMLImagePyramidT
    def __init__(self):
        self.TILE_SIZE = 0  # type: int
        self.MAX_WIDTH = 0  # type: int
        self.MAX_HEIGHT = 0  # type: int
        self.GRID_ORIGIN = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmlimagePyramid = KMLImagePyramid()
        kmlimagePyramid.Init(buf, pos)
        return cls.InitFromObj(kmlimagePyramid)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmlimagePyramid):
        x = KMLImagePyramidT()
        x._UnPack(kmlimagePyramid)
        return x

    # KMLImagePyramidT
    def _UnPack(self, kmlimagePyramid):
        if kmlimagePyramid is None:
            return
        self.TILE_SIZE = kmlimagePyramid.TILE_SIZE()
        self.MAX_WIDTH = kmlimagePyramid.MAX_WIDTH()
        self.MAX_HEIGHT = kmlimagePyramid.MAX_HEIGHT()
        self.GRID_ORIGIN = kmlimagePyramid.GRID_ORIGIN()

    # KMLImagePyramidT
    def Pack(self, builder):
        KMLImagePyramidStart(builder)
        KMLImagePyramidAddTILE_SIZE(builder, self.TILE_SIZE)
        KMLImagePyramidAddMAX_WIDTH(builder, self.MAX_WIDTH)
        KMLImagePyramidAddMAX_HEIGHT(builder, self.MAX_HEIGHT)
        KMLImagePyramidAddGRID_ORIGIN(builder, self.GRID_ORIGIN)
        kmlimagePyramid = KMLImagePyramidEnd(builder)
        return kmlimagePyramid
