# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Polygon geometry
class KMLPolygon(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLPolygon()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLPolygon(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLPolygonBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLPolygon
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Outer boundary
    # KMLPolygon
    def OUTER_BOUNDARY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from KMLLinearRing import KMLLinearRing
            obj = KMLLinearRing()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Inner boundaries (holes)
    # KMLPolygon
    def INNER_BOUNDARIES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from KMLLinearRing import KMLLinearRing
            obj = KMLLinearRing()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # KMLPolygon
    def INNER_BOUNDARIESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KMLPolygon
    def INNER_BOUNDARIESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Altitude mode
    # KMLPolygon
    def ALTITUDE_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Whether to extrude to ground
    # KMLPolygon
    def EXTRUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Whether to tessellate
    # KMLPolygon
    def TESSELLATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def KMLPolygonStart(builder):
    builder.StartObject(5)

def Start(builder):
    KMLPolygonStart(builder)

def KMLPolygonAddOUTER_BOUNDARY(builder, OUTER_BOUNDARY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(OUTER_BOUNDARY), 0)

def AddOUTER_BOUNDARY(builder, OUTER_BOUNDARY):
    KMLPolygonAddOUTER_BOUNDARY(builder, OUTER_BOUNDARY)

def KMLPolygonAddINNER_BOUNDARIES(builder, INNER_BOUNDARIES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(INNER_BOUNDARIES), 0)

def AddINNER_BOUNDARIES(builder, INNER_BOUNDARIES):
    KMLPolygonAddINNER_BOUNDARIES(builder, INNER_BOUNDARIES)

def KMLPolygonStartINNER_BOUNDARIESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartINNER_BOUNDARIESVector(builder, numElems):
    return KMLPolygonStartINNER_BOUNDARIESVector(builder, numElems)

def KMLPolygonAddALTITUDE_MODE(builder, ALTITUDE_MODE):
    builder.PrependInt8Slot(2, ALTITUDE_MODE, 0)

def AddALTITUDE_MODE(builder, ALTITUDE_MODE):
    KMLPolygonAddALTITUDE_MODE(builder, ALTITUDE_MODE)

def KMLPolygonAddEXTRUDE(builder, EXTRUDE):
    builder.PrependBoolSlot(3, EXTRUDE, 0)

def AddEXTRUDE(builder, EXTRUDE):
    KMLPolygonAddEXTRUDE(builder, EXTRUDE)

def KMLPolygonAddTESSELLATE(builder, TESSELLATE):
    builder.PrependBoolSlot(4, TESSELLATE, 0)

def AddTESSELLATE(builder, TESSELLATE):
    KMLPolygonAddTESSELLATE(builder, TESSELLATE)

def KMLPolygonEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLPolygonEnd(builder)

import KMLLinearRing
try:
    from typing import List, Optional
except:
    pass

class KMLPolygonT(object):

    # KMLPolygonT
    def __init__(self):
        self.OUTER_BOUNDARY = None  # type: Optional[KMLLinearRing.KMLLinearRingT]
        self.INNER_BOUNDARIES = None  # type: List[KMLLinearRing.KMLLinearRingT]
        self.ALTITUDE_MODE = 0  # type: int
        self.EXTRUDE = False  # type: bool
        self.TESSELLATE = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmlpolygon = KMLPolygon()
        kmlpolygon.Init(buf, pos)
        return cls.InitFromObj(kmlpolygon)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmlpolygon):
        x = KMLPolygonT()
        x._UnPack(kmlpolygon)
        return x

    # KMLPolygonT
    def _UnPack(self, kmlpolygon):
        if kmlpolygon is None:
            return
        if kmlpolygon.OUTER_BOUNDARY() is not None:
            self.OUTER_BOUNDARY = KMLLinearRing.KMLLinearRingT.InitFromObj(kmlpolygon.OUTER_BOUNDARY())
        if not kmlpolygon.INNER_BOUNDARIESIsNone():
            self.INNER_BOUNDARIES = []
            for i in range(kmlpolygon.INNER_BOUNDARIESLength()):
                if kmlpolygon.INNER_BOUNDARIES(i) is None:
                    self.INNER_BOUNDARIES.append(None)
                else:
                    kMLLinearRing_ = KMLLinearRing.KMLLinearRingT.InitFromObj(kmlpolygon.INNER_BOUNDARIES(i))
                    self.INNER_BOUNDARIES.append(kMLLinearRing_)
        self.ALTITUDE_MODE = kmlpolygon.ALTITUDE_MODE()
        self.EXTRUDE = kmlpolygon.EXTRUDE()
        self.TESSELLATE = kmlpolygon.TESSELLATE()

    # KMLPolygonT
    def Pack(self, builder):
        if self.OUTER_BOUNDARY is not None:
            OUTER_BOUNDARY = self.OUTER_BOUNDARY.Pack(builder)
        if self.INNER_BOUNDARIES is not None:
            INNER_BOUNDARIESlist = []
            for i in range(len(self.INNER_BOUNDARIES)):
                INNER_BOUNDARIESlist.append(self.INNER_BOUNDARIES[i].Pack(builder))
            KMLPolygonStartINNER_BOUNDARIESVector(builder, len(self.INNER_BOUNDARIES))
            for i in reversed(range(len(self.INNER_BOUNDARIES))):
                builder.PrependUOffsetTRelative(INNER_BOUNDARIESlist[i])
            INNER_BOUNDARIES = builder.EndVector()
        KMLPolygonStart(builder)
        if self.OUTER_BOUNDARY is not None:
            KMLPolygonAddOUTER_BOUNDARY(builder, OUTER_BOUNDARY)
        if self.INNER_BOUNDARIES is not None:
            KMLPolygonAddINNER_BOUNDARIES(builder, INNER_BOUNDARIES)
        KMLPolygonAddALTITUDE_MODE(builder, self.ALTITUDE_MODE)
        KMLPolygonAddEXTRUDE(builder, self.EXTRUDE)
        KMLPolygonAddTESSELLATE(builder, self.TESSELLATE)
        kmlpolygon = KMLPolygonEnd(builder)
        return kmlpolygon
