# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# gx:MultiTrack
class KMLMultiTrack(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLMultiTrack()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLMultiTrack(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLMultiTrackBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLMultiTrack
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Altitude mode
    # KMLMultiTrack
    def ALTITUDE_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Whether to interpolate between tracks
    # KMLMultiTrack
    def INTERPOLATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Child tracks
    # KMLMultiTrack
    def TRACKS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from KMLTrack import KMLTrack
            obj = KMLTrack()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # KMLMultiTrack
    def TRACKSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KMLMultiTrack
    def TRACKSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def KMLMultiTrackStart(builder):
    builder.StartObject(3)

def Start(builder):
    KMLMultiTrackStart(builder)

def KMLMultiTrackAddALTITUDE_MODE(builder, ALTITUDE_MODE):
    builder.PrependInt8Slot(0, ALTITUDE_MODE, 0)

def AddALTITUDE_MODE(builder, ALTITUDE_MODE):
    KMLMultiTrackAddALTITUDE_MODE(builder, ALTITUDE_MODE)

def KMLMultiTrackAddINTERPOLATE(builder, INTERPOLATE):
    builder.PrependBoolSlot(1, INTERPOLATE, 0)

def AddINTERPOLATE(builder, INTERPOLATE):
    KMLMultiTrackAddINTERPOLATE(builder, INTERPOLATE)

def KMLMultiTrackAddTRACKS(builder, TRACKS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TRACKS), 0)

def AddTRACKS(builder, TRACKS):
    KMLMultiTrackAddTRACKS(builder, TRACKS)

def KMLMultiTrackStartTRACKSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTRACKSVector(builder, numElems):
    return KMLMultiTrackStartTRACKSVector(builder, numElems)

def KMLMultiTrackEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLMultiTrackEnd(builder)

import KMLTrack
try:
    from typing import List
except:
    pass

class KMLMultiTrackT(object):

    # KMLMultiTrackT
    def __init__(self):
        self.ALTITUDE_MODE = 0  # type: int
        self.INTERPOLATE = False  # type: bool
        self.TRACKS = None  # type: List[KMLTrack.KMLTrackT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmlmultiTrack = KMLMultiTrack()
        kmlmultiTrack.Init(buf, pos)
        return cls.InitFromObj(kmlmultiTrack)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmlmultiTrack):
        x = KMLMultiTrackT()
        x._UnPack(kmlmultiTrack)
        return x

    # KMLMultiTrackT
    def _UnPack(self, kmlmultiTrack):
        if kmlmultiTrack is None:
            return
        self.ALTITUDE_MODE = kmlmultiTrack.ALTITUDE_MODE()
        self.INTERPOLATE = kmlmultiTrack.INTERPOLATE()
        if not kmlmultiTrack.TRACKSIsNone():
            self.TRACKS = []
            for i in range(kmlmultiTrack.TRACKSLength()):
                if kmlmultiTrack.TRACKS(i) is None:
                    self.TRACKS.append(None)
                else:
                    kMLTrack_ = KMLTrack.KMLTrackT.InitFromObj(kmlmultiTrack.TRACKS(i))
                    self.TRACKS.append(kMLTrack_)

    # KMLMultiTrackT
    def Pack(self, builder):
        if self.TRACKS is not None:
            TRACKSlist = []
            for i in range(len(self.TRACKS)):
                TRACKSlist.append(self.TRACKS[i].Pack(builder))
            KMLMultiTrackStartTRACKSVector(builder, len(self.TRACKS))
            for i in reversed(range(len(self.TRACKS))):
                builder.PrependUOffsetTRelative(TRACKSlist[i])
            TRACKS = builder.EndVector()
        KMLMultiTrackStart(builder)
        KMLMultiTrackAddALTITUDE_MODE(builder, self.ALTITUDE_MODE)
        KMLMultiTrackAddINTERPOLATE(builder, self.INTERPOLATE)
        if self.TRACKS is not None:
            KMLMultiTrackAddTRACKS(builder, TRACKS)
        kmlmultiTrack = KMLMultiTrackEnd(builder)
        return kmlmultiTrack
