# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# TimeStamp
class KMLTimeStamp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLTimeStamp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLTimeStamp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLTimeStampBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLTimeStamp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Time (ISO 8601)
    # KMLTimeStamp
    def WHEN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def KMLTimeStampStart(builder):
    builder.StartObject(1)

def Start(builder):
    KMLTimeStampStart(builder)

def KMLTimeStampAddWHEN(builder, WHEN):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(WHEN), 0)

def AddWHEN(builder, WHEN):
    KMLTimeStampAddWHEN(builder, WHEN)

def KMLTimeStampEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLTimeStampEnd(builder)


class KMLTimeStampT(object):

    # KMLTimeStampT
    def __init__(self):
        self.WHEN = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmltimeStamp = KMLTimeStamp()
        kmltimeStamp.Init(buf, pos)
        return cls.InitFromObj(kmltimeStamp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmltimeStamp):
        x = KMLTimeStampT()
        x._UnPack(kmltimeStamp)
        return x

    # KMLTimeStampT
    def _UnPack(self, kmltimeStamp):
        if kmltimeStamp is None:
            return
        self.WHEN = kmltimeStamp.WHEN()

    # KMLTimeStampT
    def Pack(self, builder):
        if self.WHEN is not None:
            WHEN = builder.CreateString(self.WHEN)
        KMLTimeStampStart(builder)
        if self.WHEN is not None:
            KMLTimeStampAddWHEN(builder, WHEN)
        kmltimeStamp = KMLTimeStampEnd(builder)
        return kmltimeStamp
