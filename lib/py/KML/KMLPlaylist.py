# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# gx:Playlist
class KMLPlaylist(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLPlaylist()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLPlaylist(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLPlaylistBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLPlaylist
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Tour primitives
    # KMLPlaylist
    def PRIMITIVES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from KMLTourPrimitive import KMLTourPrimitive
            obj = KMLTourPrimitive()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # KMLPlaylist
    def PRIMITIVESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KMLPlaylist
    def PRIMITIVESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def KMLPlaylistStart(builder):
    builder.StartObject(1)

def Start(builder):
    KMLPlaylistStart(builder)

def KMLPlaylistAddPRIMITIVES(builder, PRIMITIVES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PRIMITIVES), 0)

def AddPRIMITIVES(builder, PRIMITIVES):
    KMLPlaylistAddPRIMITIVES(builder, PRIMITIVES)

def KMLPlaylistStartPRIMITIVESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPRIMITIVESVector(builder, numElems):
    return KMLPlaylistStartPRIMITIVESVector(builder, numElems)

def KMLPlaylistEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLPlaylistEnd(builder)

import KMLTourPrimitive
try:
    from typing import List
except:
    pass

class KMLPlaylistT(object):

    # KMLPlaylistT
    def __init__(self):
        self.PRIMITIVES = None  # type: List[KMLTourPrimitive.KMLTourPrimitiveT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmlplaylist = KMLPlaylist()
        kmlplaylist.Init(buf, pos)
        return cls.InitFromObj(kmlplaylist)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmlplaylist):
        x = KMLPlaylistT()
        x._UnPack(kmlplaylist)
        return x

    # KMLPlaylistT
    def _UnPack(self, kmlplaylist):
        if kmlplaylist is None:
            return
        if not kmlplaylist.PRIMITIVESIsNone():
            self.PRIMITIVES = []
            for i in range(kmlplaylist.PRIMITIVESLength()):
                if kmlplaylist.PRIMITIVES(i) is None:
                    self.PRIMITIVES.append(None)
                else:
                    kMLTourPrimitive_ = KMLTourPrimitive.KMLTourPrimitiveT.InitFromObj(kmlplaylist.PRIMITIVES(i))
                    self.PRIMITIVES.append(kMLTourPrimitive_)

    # KMLPlaylistT
    def Pack(self, builder):
        if self.PRIMITIVES is not None:
            PRIMITIVESlist = []
            for i in range(len(self.PRIMITIVES)):
                PRIMITIVESlist.append(self.PRIMITIVES[i].Pack(builder))
            KMLPlaylistStartPRIMITIVESVector(builder, len(self.PRIMITIVES))
            for i in reversed(range(len(self.PRIMITIVES))):
                builder.PrependUOffsetTRelative(PRIMITIVESlist[i])
            PRIMITIVES = builder.EndVector()
        KMLPlaylistStart(builder)
        if self.PRIMITIVES is not None:
            KMLPlaylistAddPRIMITIVES(builder, PRIMITIVES)
        kmlplaylist = KMLPlaylistEnd(builder)
        return kmlplaylist
