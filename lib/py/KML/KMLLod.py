# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Level of Detail parameters
class KMLLod(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KMLLod()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKMLLod(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def KMLLodBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4B\x4D\x4C", size_prefixed=size_prefixed)

    # KMLLod
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Minimum LOD pixels
    # KMLLod
    def MIN_LOD_PIXELS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum LOD pixels (-1 = infinite)
    # KMLLod
    def MAX_LOD_PIXELS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Minimum fade extent
    # KMLLod
    def MIN_FADE_EXTENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum fade extent
    # KMLLod
    def MAX_FADE_EXTENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def KMLLodStart(builder):
    builder.StartObject(4)

def Start(builder):
    KMLLodStart(builder)

def KMLLodAddMIN_LOD_PIXELS(builder, MIN_LOD_PIXELS):
    builder.PrependFloat64Slot(0, MIN_LOD_PIXELS, 0.0)

def AddMIN_LOD_PIXELS(builder, MIN_LOD_PIXELS):
    KMLLodAddMIN_LOD_PIXELS(builder, MIN_LOD_PIXELS)

def KMLLodAddMAX_LOD_PIXELS(builder, MAX_LOD_PIXELS):
    builder.PrependFloat64Slot(1, MAX_LOD_PIXELS, 0.0)

def AddMAX_LOD_PIXELS(builder, MAX_LOD_PIXELS):
    KMLLodAddMAX_LOD_PIXELS(builder, MAX_LOD_PIXELS)

def KMLLodAddMIN_FADE_EXTENT(builder, MIN_FADE_EXTENT):
    builder.PrependFloat64Slot(2, MIN_FADE_EXTENT, 0.0)

def AddMIN_FADE_EXTENT(builder, MIN_FADE_EXTENT):
    KMLLodAddMIN_FADE_EXTENT(builder, MIN_FADE_EXTENT)

def KMLLodAddMAX_FADE_EXTENT(builder, MAX_FADE_EXTENT):
    builder.PrependFloat64Slot(3, MAX_FADE_EXTENT, 0.0)

def AddMAX_FADE_EXTENT(builder, MAX_FADE_EXTENT):
    KMLLodAddMAX_FADE_EXTENT(builder, MAX_FADE_EXTENT)

def KMLLodEnd(builder):
    return builder.EndObject()

def End(builder):
    return KMLLodEnd(builder)


class KMLLodT(object):

    # KMLLodT
    def __init__(self):
        self.MIN_LOD_PIXELS = 0.0  # type: float
        self.MAX_LOD_PIXELS = 0.0  # type: float
        self.MIN_FADE_EXTENT = 0.0  # type: float
        self.MAX_FADE_EXTENT = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kmllod = KMLLod()
        kmllod.Init(buf, pos)
        return cls.InitFromObj(kmllod)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kmllod):
        x = KMLLodT()
        x._UnPack(kmllod)
        return x

    # KMLLodT
    def _UnPack(self, kmllod):
        if kmllod is None:
            return
        self.MIN_LOD_PIXELS = kmllod.MIN_LOD_PIXELS()
        self.MAX_LOD_PIXELS = kmllod.MAX_LOD_PIXELS()
        self.MIN_FADE_EXTENT = kmllod.MIN_FADE_EXTENT()
        self.MAX_FADE_EXTENT = kmllod.MAX_FADE_EXTENT()

    # KMLLodT
    def Pack(self, builder):
        KMLLodStart(builder)
        KMLLodAddMIN_LOD_PIXELS(builder, self.MIN_LOD_PIXELS)
        KMLLodAddMAX_LOD_PIXELS(builder, self.MAX_LOD_PIXELS)
        KMLLodAddMIN_FADE_EXTENT(builder, self.MIN_FADE_EXTENT)
        KMLLodAddMAX_FADE_EXTENT(builder, self.MAX_FADE_EXTENT)
        kmllod = KMLLodEnd(builder)
        return kmllod
