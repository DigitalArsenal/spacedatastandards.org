# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CDMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CDMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCDMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CDMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x44\x4D", size_prefixed=size_prefixed)

    # CDMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # A vector of CDM records
    # CDMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from CDM import CDM
            obj = CDM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CDMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CDMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def CDMCOLLECTIONStart(builder): builder.StartObject(1)
def Start(builder):
    return CDMCOLLECTIONStart(builder)
def CDMCOLLECTIONAddRECORDS(builder, RECORDS): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)
def AddRECORDS(builder, RECORDS):
    return CDMCOLLECTIONAddRECORDS(builder, RECORDS)
def CDMCOLLECTIONStartRECORDSVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartRECORDSVector(builder, numElems):
    return CDMCOLLECTIONStartRECORDSVector(builder, numElems)
def CDMCOLLECTIONEnd(builder): return builder.EndObject()
def End(builder):
    return CDMCOLLECTIONEnd(builder)
import CDM
try:
    from typing import List
except:
    pass

class CDMCOLLECTIONT(object):

    # CDMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[CDM.CDMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        CDMCOLLECTION = CDMCOLLECTION()
        CDMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(CDMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, CDMCOLLECTION):
        x = CDMCOLLECTIONT()
        x._UnPack(CDMCOLLECTION)
        return x

    # CDMCOLLECTIONT
    def _UnPack(self, CDMCOLLECTION):
        if CDMCOLLECTION is None:
            return
        if not CDMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(CDMCOLLECTION.RECORDSLength()):
                if CDMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    cDM_ = CDM.CDMT.InitFromObj(CDMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(cDM_)

    # CDMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            CDMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        CDMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            CDMCOLLECTIONAddRECORDS(builder, RECORDS)
        CDMCOLLECTION = CDMCOLLECTIONEnd(builder)
        return CDMCOLLECTION
