# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Sensor Tasking Plan
class sensorPlan(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = sensorPlan()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAssensorPlan(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def sensorPlanBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x45\x4E", size_prefixed=size_prefixed)

    # sensorPlan
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Plan start time (ISO 8601)
    # sensorPlan
    def START_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Plan end time (ISO 8601)
    # sensorPlan
    def END_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Target satellite number or designator
    # sensorPlan
    def TARGET_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Priority level (1=highest)
    # sensorPlan
    def PRIORITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Requested observation mode
    # sensorPlan
    def MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Minimum elevation in degrees
    # sensorPlan
    def MIN_ELEVATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum range in km
    # sensorPlan
    def MAX_RANGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def sensorPlanStart(builder):
    builder.StartObject(7)

def Start(builder):
    sensorPlanStart(builder)

def sensorPlanAddSTART_TIME(builder, START_TIME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(START_TIME), 0)

def AddSTART_TIME(builder, START_TIME):
    sensorPlanAddSTART_TIME(builder, START_TIME)

def sensorPlanAddEND_TIME(builder, END_TIME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(END_TIME), 0)

def AddEND_TIME(builder, END_TIME):
    sensorPlanAddEND_TIME(builder, END_TIME)

def sensorPlanAddTARGET_ID(builder, TARGET_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TARGET_ID), 0)

def AddTARGET_ID(builder, TARGET_ID):
    sensorPlanAddTARGET_ID(builder, TARGET_ID)

def sensorPlanAddPRIORITY(builder, PRIORITY):
    builder.PrependUint8Slot(3, PRIORITY, 0)

def AddPRIORITY(builder, PRIORITY):
    sensorPlanAddPRIORITY(builder, PRIORITY)

def sensorPlanAddMODE(builder, MODE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(MODE), 0)

def AddMODE(builder, MODE):
    sensorPlanAddMODE(builder, MODE)

def sensorPlanAddMIN_ELEVATION(builder, MIN_ELEVATION):
    builder.PrependFloat64Slot(5, MIN_ELEVATION, 0.0)

def AddMIN_ELEVATION(builder, MIN_ELEVATION):
    sensorPlanAddMIN_ELEVATION(builder, MIN_ELEVATION)

def sensorPlanAddMAX_RANGE(builder, MAX_RANGE):
    builder.PrependFloat64Slot(6, MAX_RANGE, 0.0)

def AddMAX_RANGE(builder, MAX_RANGE):
    sensorPlanAddMAX_RANGE(builder, MAX_RANGE)

def sensorPlanEnd(builder):
    return builder.EndObject()

def End(builder):
    return sensorPlanEnd(builder)


class sensorPlanT(object):

    # sensorPlanT
    def __init__(self):
        self.START_TIME = None  # type: str
        self.END_TIME = None  # type: str
        self.TARGET_ID = None  # type: str
        self.PRIORITY = 0  # type: int
        self.MODE = None  # type: str
        self.MIN_ELEVATION = 0.0  # type: float
        self.MAX_RANGE = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        sensorPlan = sensorPlan()
        sensorPlan.Init(buf, pos)
        return cls.InitFromObj(sensorPlan)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, sensorPlan):
        x = sensorPlanT()
        x._UnPack(sensorPlan)
        return x

    # sensorPlanT
    def _UnPack(self, sensorPlan):
        if sensorPlan is None:
            return
        self.START_TIME = sensorPlan.START_TIME()
        self.END_TIME = sensorPlan.END_TIME()
        self.TARGET_ID = sensorPlan.TARGET_ID()
        self.PRIORITY = sensorPlan.PRIORITY()
        self.MODE = sensorPlan.MODE()
        self.MIN_ELEVATION = sensorPlan.MIN_ELEVATION()
        self.MAX_RANGE = sensorPlan.MAX_RANGE()

    # sensorPlanT
    def Pack(self, builder):
        if self.START_TIME is not None:
            START_TIME = builder.CreateString(self.START_TIME)
        if self.END_TIME is not None:
            END_TIME = builder.CreateString(self.END_TIME)
        if self.TARGET_ID is not None:
            TARGET_ID = builder.CreateString(self.TARGET_ID)
        if self.MODE is not None:
            MODE = builder.CreateString(self.MODE)
        sensorPlanStart(builder)
        if self.START_TIME is not None:
            sensorPlanAddSTART_TIME(builder, START_TIME)
        if self.END_TIME is not None:
            sensorPlanAddEND_TIME(builder, END_TIME)
        if self.TARGET_ID is not None:
            sensorPlanAddTARGET_ID(builder, TARGET_ID)
        sensorPlanAddPRIORITY(builder, self.PRIORITY)
        if self.MODE is not None:
            sensorPlanAddMODE(builder, MODE)
        sensorPlanAddMIN_ELEVATION(builder, self.MIN_ELEVATION)
        sensorPlanAddMAX_RANGE(builder, self.MAX_RANGE)
        sensorPlan = sensorPlanEnd(builder)
        return sensorPlan
