# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Sensor Management
class SEN(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SEN()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSEN(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SENBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x45\x4E", size_prefixed=size_prefixed)

    # SEN
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Unique sensor identifier
    # SEN
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Sensor name
    # SEN
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Sensor type
    # SEN
    def TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Current operational status
    # SEN
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Site identifier
    # SEN
    def SITE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Geodetic latitude in degrees
    # SEN
    def LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Geodetic longitude in degrees
    # SEN
    def LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Altitude in meters above WGS-84
    # SEN
    def ALTITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Last status update (ISO 8601)
    # SEN
    def STATUS_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Maintenance history
    # SEN
    def MAINTENANCE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from sensorMaintenanceEvent import sensorMaintenanceEvent
            obj = sensorMaintenanceEvent()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SEN
    def MAINTENANCELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SEN
    def MAINTENANCEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # Observation plans/taskings
    # SEN
    def PLANS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from sensorPlan import sensorPlan
            obj = sensorPlan()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SEN
    def PLANSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SEN
    def PLANSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # Operational statistics
    # SEN
    def STATISTICS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from sensorStats import sensorStats
            obj = sensorStats()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SEN
    def STATISTICSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SEN
    def STATISTICSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # Additional notes
    # SEN
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def SENStart(builder):
    builder.StartObject(13)

def Start(builder):
    SENStart(builder)

def SENAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    SENAddID(builder, ID)

def SENAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    SENAddNAME(builder, NAME)

def SENAddTYPE(builder, TYPE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TYPE), 0)

def AddTYPE(builder, TYPE):
    SENAddTYPE(builder, TYPE)

def SENAddSTATUS(builder, STATUS):
    builder.PrependInt8Slot(3, STATUS, 0)

def AddSTATUS(builder, STATUS):
    SENAddSTATUS(builder, STATUS)

def SENAddSITE_ID(builder, SITE_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SITE_ID), 0)

def AddSITE_ID(builder, SITE_ID):
    SENAddSITE_ID(builder, SITE_ID)

def SENAddLATITUDE(builder, LATITUDE):
    builder.PrependFloat64Slot(5, LATITUDE, 0.0)

def AddLATITUDE(builder, LATITUDE):
    SENAddLATITUDE(builder, LATITUDE)

def SENAddLONGITUDE(builder, LONGITUDE):
    builder.PrependFloat64Slot(6, LONGITUDE, 0.0)

def AddLONGITUDE(builder, LONGITUDE):
    SENAddLONGITUDE(builder, LONGITUDE)

def SENAddALTITUDE(builder, ALTITUDE):
    builder.PrependFloat64Slot(7, ALTITUDE, 0.0)

def AddALTITUDE(builder, ALTITUDE):
    SENAddALTITUDE(builder, ALTITUDE)

def SENAddSTATUS_TIME(builder, STATUS_TIME):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(STATUS_TIME), 0)

def AddSTATUS_TIME(builder, STATUS_TIME):
    SENAddSTATUS_TIME(builder, STATUS_TIME)

def SENAddMAINTENANCE(builder, MAINTENANCE):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(MAINTENANCE), 0)

def AddMAINTENANCE(builder, MAINTENANCE):
    SENAddMAINTENANCE(builder, MAINTENANCE)

def SENStartMAINTENANCEVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartMAINTENANCEVector(builder, numElems):
    return SENStartMAINTENANCEVector(builder, numElems)

def SENAddPLANS(builder, PLANS):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(PLANS), 0)

def AddPLANS(builder, PLANS):
    SENAddPLANS(builder, PLANS)

def SENStartPLANSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPLANSVector(builder, numElems):
    return SENStartPLANSVector(builder, numElems)

def SENAddSTATISTICS(builder, STATISTICS):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(STATISTICS), 0)

def AddSTATISTICS(builder, STATISTICS):
    SENAddSTATISTICS(builder, STATISTICS)

def SENStartSTATISTICSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSTATISTICSVector(builder, numElems):
    return SENStartSTATISTICSVector(builder, numElems)

def SENAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    SENAddNOTES(builder, NOTES)

def SENEnd(builder):
    return builder.EndObject()

def End(builder):
    return SENEnd(builder)

import sensorMaintenanceEvent
import sensorPlan
import sensorStats
try:
    from typing import List
except:
    pass

class SENT(object):

    # SENT
    def __init__(self):
        self.ID = None  # type: str
        self.NAME = None  # type: str
        self.TYPE = None  # type: str
        self.STATUS = 0  # type: int
        self.SITE_ID = None  # type: str
        self.LATITUDE = 0.0  # type: float
        self.LONGITUDE = 0.0  # type: float
        self.ALTITUDE = 0.0  # type: float
        self.STATUS_TIME = None  # type: str
        self.MAINTENANCE = None  # type: List[sensorMaintenanceEvent.sensorMaintenanceEventT]
        self.PLANS = None  # type: List[sensorPlan.sensorPlanT]
        self.STATISTICS = None  # type: List[sensorStats.sensorStatsT]
        self.NOTES = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        SEN = SEN()
        SEN.Init(buf, pos)
        return cls.InitFromObj(SEN)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, SEN):
        x = SENT()
        x._UnPack(SEN)
        return x

    # SENT
    def _UnPack(self, SEN):
        if SEN is None:
            return
        self.ID = SEN.ID()
        self.NAME = SEN.NAME()
        self.TYPE = SEN.TYPE()
        self.STATUS = SEN.STATUS()
        self.SITE_ID = SEN.SITE_ID()
        self.LATITUDE = SEN.LATITUDE()
        self.LONGITUDE = SEN.LONGITUDE()
        self.ALTITUDE = SEN.ALTITUDE()
        self.STATUS_TIME = SEN.STATUS_TIME()
        if not SEN.MAINTENANCEIsNone():
            self.MAINTENANCE = []
            for i in range(SEN.MAINTENANCELength()):
                if SEN.MAINTENANCE(i) is None:
                    self.MAINTENANCE.append(None)
                else:
                    sensorMaintenanceEvent_ = sensorMaintenanceEvent.sensorMaintenanceEventT.InitFromObj(SEN.MAINTENANCE(i))
                    self.MAINTENANCE.append(sensorMaintenanceEvent_)
        if not SEN.PLANSIsNone():
            self.PLANS = []
            for i in range(SEN.PLANSLength()):
                if SEN.PLANS(i) is None:
                    self.PLANS.append(None)
                else:
                    sensorPlan_ = sensorPlan.sensorPlanT.InitFromObj(SEN.PLANS(i))
                    self.PLANS.append(sensorPlan_)
        if not SEN.STATISTICSIsNone():
            self.STATISTICS = []
            for i in range(SEN.STATISTICSLength()):
                if SEN.STATISTICS(i) is None:
                    self.STATISTICS.append(None)
                else:
                    sensorStats_ = sensorStats.sensorStatsT.InitFromObj(SEN.STATISTICS(i))
                    self.STATISTICS.append(sensorStats_)
        self.NOTES = SEN.NOTES()

    # SENT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.TYPE is not None:
            TYPE = builder.CreateString(self.TYPE)
        if self.SITE_ID is not None:
            SITE_ID = builder.CreateString(self.SITE_ID)
        if self.STATUS_TIME is not None:
            STATUS_TIME = builder.CreateString(self.STATUS_TIME)
        if self.MAINTENANCE is not None:
            MAINTENANCElist = []
            for i in range(len(self.MAINTENANCE)):
                MAINTENANCElist.append(self.MAINTENANCE[i].Pack(builder))
            SENStartMAINTENANCEVector(builder, len(self.MAINTENANCE))
            for i in reversed(range(len(self.MAINTENANCE))):
                builder.PrependUOffsetTRelative(MAINTENANCElist[i])
            MAINTENANCE = builder.EndVector()
        if self.PLANS is not None:
            PLANSlist = []
            for i in range(len(self.PLANS)):
                PLANSlist.append(self.PLANS[i].Pack(builder))
            SENStartPLANSVector(builder, len(self.PLANS))
            for i in reversed(range(len(self.PLANS))):
                builder.PrependUOffsetTRelative(PLANSlist[i])
            PLANS = builder.EndVector()
        if self.STATISTICS is not None:
            STATISTICSlist = []
            for i in range(len(self.STATISTICS)):
                STATISTICSlist.append(self.STATISTICS[i].Pack(builder))
            SENStartSTATISTICSVector(builder, len(self.STATISTICS))
            for i in reversed(range(len(self.STATISTICS))):
                builder.PrependUOffsetTRelative(STATISTICSlist[i])
            STATISTICS = builder.EndVector()
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        SENStart(builder)
        if self.ID is not None:
            SENAddID(builder, ID)
        if self.NAME is not None:
            SENAddNAME(builder, NAME)
        if self.TYPE is not None:
            SENAddTYPE(builder, TYPE)
        SENAddSTATUS(builder, self.STATUS)
        if self.SITE_ID is not None:
            SENAddSITE_ID(builder, SITE_ID)
        SENAddLATITUDE(builder, self.LATITUDE)
        SENAddLONGITUDE(builder, self.LONGITUDE)
        SENAddALTITUDE(builder, self.ALTITUDE)
        if self.STATUS_TIME is not None:
            SENAddSTATUS_TIME(builder, STATUS_TIME)
        if self.MAINTENANCE is not None:
            SENAddMAINTENANCE(builder, MAINTENANCE)
        if self.PLANS is not None:
            SENAddPLANS(builder, PLANS)
        if self.STATISTICS is not None:
            SENAddSTATISTICS(builder, STATISTICS)
        if self.NOTES is not None:
            SENAddNOTES(builder, NOTES)
        SEN = SENEnd(builder)
        return SEN
