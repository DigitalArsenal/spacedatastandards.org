# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Event Locator
#
# One request and one report for every class of located event: shadow
# passage, site visibility, sensor-field-of-view intrusion, apsis passage,
# node crossing, and the generic condition on any $PCE catalog parameter.
# Every class is the same pair of an event function and a refined root, so a
# new class is a configuration table and a vocabulary member, never a new
# record. Entry and exit epochs, duration, the occulting or observed body and
# the parameter values captured at each event's instants are reported in one
# shape, and the predicate of the generic class is $PCE's
# PCEParameterCondition rather than a second spelling of the same idea.
#
# Angles are radians, durations seconds, epochs ISO 8601 in a named time
# system.
class EVL(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EVL()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEVL(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def EVLBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x56\x4C", size_prefixed=size_prefixed)

    # EVL
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EVL
    def LOCATION_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from EVLEventLocationRequest import EVLEventLocationRequest
            obj = EVLEventLocationRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # EVL
    def EVENT_REPORT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from EVLEventReport import EVLEventReport
            obj = EVLEventReport()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def EVLStart(builder):
    builder.StartObject(2)

def Start(builder):
    EVLStart(builder)

def EVLAddLOCATION_REQUEST(builder, LOCATION_REQUEST):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(LOCATION_REQUEST), 0)

def AddLOCATION_REQUEST(builder, LOCATION_REQUEST):
    EVLAddLOCATION_REQUEST(builder, LOCATION_REQUEST)

def EVLAddEVENT_REPORT(builder, EVENT_REPORT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(EVENT_REPORT), 0)

def AddEVENT_REPORT(builder, EVENT_REPORT):
    EVLAddEVENT_REPORT(builder, EVENT_REPORT)

def EVLEnd(builder):
    return builder.EndObject()

def End(builder):
    return EVLEnd(builder)

import EVLEventLocationRequest
import EVLEventReport
try:
    from typing import Optional
except:
    pass

class EVLT(object):

    # EVLT
    def __init__(
        self,
        LOCATION_REQUEST = None,
        EVENT_REPORT = None,
    ):
        self.LOCATION_REQUEST = LOCATION_REQUEST  # type: Optional[EVLEventLocationRequest.EVLEventLocationRequestT]
        self.EVENT_REPORT = EVENT_REPORT  # type: Optional[EVLEventReport.EVLEventReportT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpEvl = EVL()
        tmpEvl.Init(buf, pos)
        return cls.InitFromObj(tmpEvl)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpEvl):
        x = EVLT()
        x._UnPack(tmpEvl)
        return x

    # EVLT
    def _UnPack(self, EVL):
        if EVL is None:
            return
        if EVL.LOCATION_REQUEST() is not None:
            self.LOCATION_REQUEST = EVLEventLocationRequest.EVLEventLocationRequestT.InitFromObj(EVL.LOCATION_REQUEST())
        if EVL.EVENT_REPORT() is not None:
            self.EVENT_REPORT = EVLEventReport.EVLEventReportT.InitFromObj(EVL.EVENT_REPORT())

    # EVLT
    def Pack(self, builder):
        if self.LOCATION_REQUEST is not None:
            LOCATION_REQUEST = self.LOCATION_REQUEST.Pack(builder)
        if self.EVENT_REPORT is not None:
            EVENT_REPORT = self.EVENT_REPORT.Pack(builder)
        EVLStart(builder)
        if self.LOCATION_REQUEST is not None:
            EVLAddLOCATION_REQUEST(builder, LOCATION_REQUEST)
        if self.EVENT_REPORT is not None:
            EVLAddEVENT_REPORT(builder, EVENT_REPORT)
        EVL = EVLEnd(builder)
        return EVL
