# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Parameter values captured at one instant inside an event.
class EVLEventSample(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EVLEventSample()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEVLEventSample(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def EVLEventSampleBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x56\x4C", size_prefixed=size_prefixed)

    # EVLEventSample
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EVLEventSample
    def POINT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # EVLEventSample
    def SAMPLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PCEParameterSample import PCEParameterSample
            obj = PCEParameterSample()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def EVLEventSampleStart(builder):
    builder.StartObject(2)

def Start(builder):
    EVLEventSampleStart(builder)

def EVLEventSampleAddPOINT(builder, POINT):
    builder.PrependUint8Slot(0, POINT, 0)

def AddPOINT(builder, POINT):
    EVLEventSampleAddPOINT(builder, POINT)

def EVLEventSampleAddSAMPLE(builder, SAMPLE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SAMPLE), 0)

def AddSAMPLE(builder, SAMPLE):
    EVLEventSampleAddSAMPLE(builder, SAMPLE)

def EVLEventSampleEnd(builder):
    return builder.EndObject()

def End(builder):
    return EVLEventSampleEnd(builder)

import PCEParameterSample
try:
    from typing import Optional
except:
    pass

class EVLEventSampleT(object):

    # EVLEventSampleT
    def __init__(
        self,
        POINT = 0,
        SAMPLE = None,
    ):
        self.POINT = POINT  # type: int
        self.SAMPLE = SAMPLE  # type: Optional[PCEParameterSample.PCEParameterSampleT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpEvleventSample = EVLEventSample()
        tmpEvleventSample.Init(buf, pos)
        return cls.InitFromObj(tmpEvleventSample)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpEvleventSample):
        x = EVLEventSampleT()
        x._UnPack(tmpEvleventSample)
        return x

    # EVLEventSampleT
    def _UnPack(self, EVLEventSample):
        if EVLEventSample is None:
            return
        self.POINT = EVLEventSample.POINT()
        if EVLEventSample.SAMPLE() is not None:
            self.SAMPLE = PCEParameterSample.PCEParameterSampleT.InitFromObj(EVLEventSample.SAMPLE())

    # EVLEventSampleT
    def Pack(self, builder):
        if self.SAMPLE is not None:
            SAMPLE = self.SAMPLE.Pack(builder)
        EVLEventSampleStart(builder)
        EVLEventSampleAddPOINT(builder, self.POINT)
        if self.SAMPLE is not None:
            EVLEventSampleAddSAMPLE(builder, SAMPLE)
        EVLEventSample = EVLEventSampleEnd(builder)
        return EVLEventSample
