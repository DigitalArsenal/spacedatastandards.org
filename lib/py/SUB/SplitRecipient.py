# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Recipient of an on-chain revenue split. The identity binds to an `$EPM`
# via CHAIN_PROOFS so a payout address is provably the recipient's.
class SplitRecipient(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SplitRecipient()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSplitRecipient(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SplitRecipientBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x58", size_prefixed=size_prefixed)

    # SplitRecipient
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # IPFS CID of the recipient's Entity Profile Message (`$EPM`)
    # SplitRecipient
    def EPM_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Settlement chain, e.g. "ethereum", "solana", "bitcoin"
    # SplitRecipient
    def CHAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # On-chain payout address (EVM 0x / SOL base58 / BTC address)
    # SplitRecipient
    def ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Account xpub the address derives from
    # SplitRecipient
    def XPUB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def SplitRecipientStart(builder):
    builder.StartObject(4)

def Start(builder):
    SplitRecipientStart(builder)

def SplitRecipientAddEPM_CID(builder, EPM_CID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EPM_CID), 0)

def AddEPM_CID(builder, EPM_CID):
    SplitRecipientAddEPM_CID(builder, EPM_CID)

def SplitRecipientAddCHAIN(builder, CHAIN):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CHAIN), 0)

def AddCHAIN(builder, CHAIN):
    SplitRecipientAddCHAIN(builder, CHAIN)

def SplitRecipientAddADDRESS(builder, ADDRESS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ADDRESS), 0)

def AddADDRESS(builder, ADDRESS):
    SplitRecipientAddADDRESS(builder, ADDRESS)

def SplitRecipientAddXPUB(builder, XPUB):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(XPUB), 0)

def AddXPUB(builder, XPUB):
    SplitRecipientAddXPUB(builder, XPUB)

def SplitRecipientEnd(builder):
    return builder.EndObject()

def End(builder):
    return SplitRecipientEnd(builder)


class SplitRecipientT(object):

    # SplitRecipientT
    def __init__(
        self,
        EPM_CID = None,
        CHAIN = None,
        ADDRESS = None,
        XPUB = None,
    ):
        self.EPM_CID = EPM_CID  # type: Optional[str]
        self.CHAIN = CHAIN  # type: Optional[str]
        self.ADDRESS = ADDRESS  # type: Optional[str]
        self.XPUB = XPUB  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpSplitRecipient = SplitRecipient()
        tmpSplitRecipient.Init(buf, pos)
        return cls.InitFromObj(tmpSplitRecipient)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpSplitRecipient):
        x = SplitRecipientT()
        x._UnPack(tmpSplitRecipient)
        return x

    # SplitRecipientT
    def _UnPack(self, SplitRecipient):
        if SplitRecipient is None:
            return
        self.EPM_CID = SplitRecipient.EPM_CID()
        self.CHAIN = SplitRecipient.CHAIN()
        self.ADDRESS = SplitRecipient.ADDRESS()
        self.XPUB = SplitRecipient.XPUB()

    # SplitRecipientT
    def Pack(self, builder):
        if self.EPM_CID is not None:
            EPM_CID = builder.CreateString(self.EPM_CID)
        if self.CHAIN is not None:
            CHAIN = builder.CreateString(self.CHAIN)
        if self.ADDRESS is not None:
            ADDRESS = builder.CreateString(self.ADDRESS)
        if self.XPUB is not None:
            XPUB = builder.CreateString(self.XPUB)
        SplitRecipientStart(builder)
        if self.EPM_CID is not None:
            SplitRecipientAddEPM_CID(builder, EPM_CID)
        if self.CHAIN is not None:
            SplitRecipientAddCHAIN(builder, CHAIN)
        if self.ADDRESS is not None:
            SplitRecipientAddADDRESS(builder, ADDRESS)
        if self.XPUB is not None:
            SplitRecipientAddXPUB(builder, XPUB)
        SplitRecipient = SplitRecipientEnd(builder)
        return SplitRecipient
