# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Mass breakdown. Each component is independently sourced; a wet mass and a
# dry mass may legitimately come from different publishers and epochs.
class OPPMass(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OPPMass()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOPPMass(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OPPMassBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x50\x50", size_prefixed=size_prefixed)

    # OPPMass
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Mass without propellant or consumables [kg].
    # OPPMass
    def DRY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Mass with propellant, at the stated epoch [kg].
    # OPPMass
    def WET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Mass at separation from the launch vehicle [kg].
    # OPPMass
    def LAUNCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Propellant mass at the stated epoch [kg].
    # OPPMass
    def PROPELLANT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Payload mass carried by the bus [kg].
    # OPPMass
    def PAYLOAD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Best current estimate of mass at the record's epoch [kg].
    # OPPMass
    def CURRENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def OPPMassStart(builder):
    builder.StartObject(6)

def Start(builder):
    OPPMassStart(builder)

def OPPMassAddDRY(builder, DRY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(DRY), 0)

def AddDRY(builder, DRY):
    OPPMassAddDRY(builder, DRY)

def OPPMassAddWET(builder, WET):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(WET), 0)

def AddWET(builder, WET):
    OPPMassAddWET(builder, WET)

def OPPMassAddLAUNCH(builder, LAUNCH):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(LAUNCH), 0)

def AddLAUNCH(builder, LAUNCH):
    OPPMassAddLAUNCH(builder, LAUNCH)

def OPPMassAddPROPELLANT(builder, PROPELLANT):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(PROPELLANT), 0)

def AddPROPELLANT(builder, PROPELLANT):
    OPPMassAddPROPELLANT(builder, PROPELLANT)

def OPPMassAddPAYLOAD(builder, PAYLOAD):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD), 0)

def AddPAYLOAD(builder, PAYLOAD):
    OPPMassAddPAYLOAD(builder, PAYLOAD)

def OPPMassAddCURRENT(builder, CURRENT):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(CURRENT), 0)

def AddCURRENT(builder, CURRENT):
    OPPMassAddCURRENT(builder, CURRENT)

def OPPMassEnd(builder):
    return builder.EndObject()

def End(builder):
    return OPPMassEnd(builder)

import OPPQuantity
try:
    from typing import Optional
except:
    pass

class OPPMassT(object):

    # OPPMassT
    def __init__(
        self,
        DRY = None,
        WET = None,
        LAUNCH = None,
        PROPELLANT = None,
        PAYLOAD = None,
        CURRENT = None,
    ):
        self.DRY = DRY  # type: Optional[OPPQuantity.OPPQuantityT]
        self.WET = WET  # type: Optional[OPPQuantity.OPPQuantityT]
        self.LAUNCH = LAUNCH  # type: Optional[OPPQuantity.OPPQuantityT]
        self.PROPELLANT = PROPELLANT  # type: Optional[OPPQuantity.OPPQuantityT]
        self.PAYLOAD = PAYLOAD  # type: Optional[OPPQuantity.OPPQuantityT]
        self.CURRENT = CURRENT  # type: Optional[OPPQuantity.OPPQuantityT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOppmass = OPPMass()
        tmpOppmass.Init(buf, pos)
        return cls.InitFromObj(tmpOppmass)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOppmass):
        x = OPPMassT()
        x._UnPack(tmpOppmass)
        return x

    # OPPMassT
    def _UnPack(self, OPPMass):
        if OPPMass is None:
            return
        if OPPMass.DRY() is not None:
            self.DRY = OPPQuantity.OPPQuantityT.InitFromObj(OPPMass.DRY())
        if OPPMass.WET() is not None:
            self.WET = OPPQuantity.OPPQuantityT.InitFromObj(OPPMass.WET())
        if OPPMass.LAUNCH() is not None:
            self.LAUNCH = OPPQuantity.OPPQuantityT.InitFromObj(OPPMass.LAUNCH())
        if OPPMass.PROPELLANT() is not None:
            self.PROPELLANT = OPPQuantity.OPPQuantityT.InitFromObj(OPPMass.PROPELLANT())
        if OPPMass.PAYLOAD() is not None:
            self.PAYLOAD = OPPQuantity.OPPQuantityT.InitFromObj(OPPMass.PAYLOAD())
        if OPPMass.CURRENT() is not None:
            self.CURRENT = OPPQuantity.OPPQuantityT.InitFromObj(OPPMass.CURRENT())

    # OPPMassT
    def Pack(self, builder):
        if self.DRY is not None:
            DRY = self.DRY.Pack(builder)
        if self.WET is not None:
            WET = self.WET.Pack(builder)
        if self.LAUNCH is not None:
            LAUNCH = self.LAUNCH.Pack(builder)
        if self.PROPELLANT is not None:
            PROPELLANT = self.PROPELLANT.Pack(builder)
        if self.PAYLOAD is not None:
            PAYLOAD = self.PAYLOAD.Pack(builder)
        if self.CURRENT is not None:
            CURRENT = self.CURRENT.Pack(builder)
        OPPMassStart(builder)
        if self.DRY is not None:
            OPPMassAddDRY(builder, DRY)
        if self.WET is not None:
            OPPMassAddWET(builder, WET)
        if self.LAUNCH is not None:
            OPPMassAddLAUNCH(builder, LAUNCH)
        if self.PROPELLANT is not None:
            OPPMassAddPROPELLANT(builder, PROPELLANT)
        if self.PAYLOAD is not None:
            OPPMassAddPAYLOAD(builder, PAYLOAD)
        if self.CURRENT is not None:
            OPPMassAddCURRENT(builder, CURRENT)
        OPPMass = OPPMassEnd(builder)
        return OPPMass
