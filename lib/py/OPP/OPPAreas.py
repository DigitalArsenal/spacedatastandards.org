# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Projected areas and the ratios derived from them.
class OPPAreas(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OPPAreas()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOPPAreas(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OPPAreasBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x50\x50", size_prefixed=size_prefixed)

    # OPPAreas
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Average projected cross-sectional area over all aspects [m2].
    # OPPAreas
    def CROSS_SECTIONAL_AVERAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Smallest projected cross-sectional area [m2].
    # OPPAreas
    def CROSS_SECTIONAL_MINIMUM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Largest projected cross-sectional area [m2].
    # OPPAreas
    def CROSS_SECTIONAL_MAXIMUM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Total solar-array area [m2].
    # OPPAreas
    def SOLAR_ARRAY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Area-to-mass ratio [m2/kg], only when a source publishes one directly or
    # the publisher derives it with PROVENANCE.METHOD DERIVED.
    # OPPAreas
    def AREA_TO_MASS_RATIO(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Ballistic coefficient [kg/m2].
    # OPPAreas
    def BALLISTIC_COEFFICIENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from OPPQuantity import OPPQuantity
            obj = OPPQuantity()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def OPPAreasStart(builder):
    builder.StartObject(6)

def Start(builder):
    OPPAreasStart(builder)

def OPPAreasAddCROSS_SECTIONAL_AVERAGE(builder, CROSS_SECTIONAL_AVERAGE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CROSS_SECTIONAL_AVERAGE), 0)

def AddCROSS_SECTIONAL_AVERAGE(builder, CROSS_SECTIONAL_AVERAGE):
    OPPAreasAddCROSS_SECTIONAL_AVERAGE(builder, CROSS_SECTIONAL_AVERAGE)

def OPPAreasAddCROSS_SECTIONAL_MINIMUM(builder, CROSS_SECTIONAL_MINIMUM):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(CROSS_SECTIONAL_MINIMUM), 0)

def AddCROSS_SECTIONAL_MINIMUM(builder, CROSS_SECTIONAL_MINIMUM):
    OPPAreasAddCROSS_SECTIONAL_MINIMUM(builder, CROSS_SECTIONAL_MINIMUM)

def OPPAreasAddCROSS_SECTIONAL_MAXIMUM(builder, CROSS_SECTIONAL_MAXIMUM):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(CROSS_SECTIONAL_MAXIMUM), 0)

def AddCROSS_SECTIONAL_MAXIMUM(builder, CROSS_SECTIONAL_MAXIMUM):
    OPPAreasAddCROSS_SECTIONAL_MAXIMUM(builder, CROSS_SECTIONAL_MAXIMUM)

def OPPAreasAddSOLAR_ARRAY(builder, SOLAR_ARRAY):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SOLAR_ARRAY), 0)

def AddSOLAR_ARRAY(builder, SOLAR_ARRAY):
    OPPAreasAddSOLAR_ARRAY(builder, SOLAR_ARRAY)

def OPPAreasAddAREA_TO_MASS_RATIO(builder, AREA_TO_MASS_RATIO):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(AREA_TO_MASS_RATIO), 0)

def AddAREA_TO_MASS_RATIO(builder, AREA_TO_MASS_RATIO):
    OPPAreasAddAREA_TO_MASS_RATIO(builder, AREA_TO_MASS_RATIO)

def OPPAreasAddBALLISTIC_COEFFICIENT(builder, BALLISTIC_COEFFICIENT):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(BALLISTIC_COEFFICIENT), 0)

def AddBALLISTIC_COEFFICIENT(builder, BALLISTIC_COEFFICIENT):
    OPPAreasAddBALLISTIC_COEFFICIENT(builder, BALLISTIC_COEFFICIENT)

def OPPAreasEnd(builder):
    return builder.EndObject()

def End(builder):
    return OPPAreasEnd(builder)

import OPPQuantity
try:
    from typing import Optional
except:
    pass

class OPPAreasT(object):

    # OPPAreasT
    def __init__(
        self,
        CROSS_SECTIONAL_AVERAGE = None,
        CROSS_SECTIONAL_MINIMUM = None,
        CROSS_SECTIONAL_MAXIMUM = None,
        SOLAR_ARRAY = None,
        AREA_TO_MASS_RATIO = None,
        BALLISTIC_COEFFICIENT = None,
    ):
        self.CROSS_SECTIONAL_AVERAGE = CROSS_SECTIONAL_AVERAGE  # type: Optional[OPPQuantity.OPPQuantityT]
        self.CROSS_SECTIONAL_MINIMUM = CROSS_SECTIONAL_MINIMUM  # type: Optional[OPPQuantity.OPPQuantityT]
        self.CROSS_SECTIONAL_MAXIMUM = CROSS_SECTIONAL_MAXIMUM  # type: Optional[OPPQuantity.OPPQuantityT]
        self.SOLAR_ARRAY = SOLAR_ARRAY  # type: Optional[OPPQuantity.OPPQuantityT]
        self.AREA_TO_MASS_RATIO = AREA_TO_MASS_RATIO  # type: Optional[OPPQuantity.OPPQuantityT]
        self.BALLISTIC_COEFFICIENT = BALLISTIC_COEFFICIENT  # type: Optional[OPPQuantity.OPPQuantityT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOppareas = OPPAreas()
        tmpOppareas.Init(buf, pos)
        return cls.InitFromObj(tmpOppareas)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOppareas):
        x = OPPAreasT()
        x._UnPack(tmpOppareas)
        return x

    # OPPAreasT
    def _UnPack(self, OPPAreas):
        if OPPAreas is None:
            return
        if OPPAreas.CROSS_SECTIONAL_AVERAGE() is not None:
            self.CROSS_SECTIONAL_AVERAGE = OPPQuantity.OPPQuantityT.InitFromObj(OPPAreas.CROSS_SECTIONAL_AVERAGE())
        if OPPAreas.CROSS_SECTIONAL_MINIMUM() is not None:
            self.CROSS_SECTIONAL_MINIMUM = OPPQuantity.OPPQuantityT.InitFromObj(OPPAreas.CROSS_SECTIONAL_MINIMUM())
        if OPPAreas.CROSS_SECTIONAL_MAXIMUM() is not None:
            self.CROSS_SECTIONAL_MAXIMUM = OPPQuantity.OPPQuantityT.InitFromObj(OPPAreas.CROSS_SECTIONAL_MAXIMUM())
        if OPPAreas.SOLAR_ARRAY() is not None:
            self.SOLAR_ARRAY = OPPQuantity.OPPQuantityT.InitFromObj(OPPAreas.SOLAR_ARRAY())
        if OPPAreas.AREA_TO_MASS_RATIO() is not None:
            self.AREA_TO_MASS_RATIO = OPPQuantity.OPPQuantityT.InitFromObj(OPPAreas.AREA_TO_MASS_RATIO())
        if OPPAreas.BALLISTIC_COEFFICIENT() is not None:
            self.BALLISTIC_COEFFICIENT = OPPQuantity.OPPQuantityT.InitFromObj(OPPAreas.BALLISTIC_COEFFICIENT())

    # OPPAreasT
    def Pack(self, builder):
        if self.CROSS_SECTIONAL_AVERAGE is not None:
            CROSS_SECTIONAL_AVERAGE = self.CROSS_SECTIONAL_AVERAGE.Pack(builder)
        if self.CROSS_SECTIONAL_MINIMUM is not None:
            CROSS_SECTIONAL_MINIMUM = self.CROSS_SECTIONAL_MINIMUM.Pack(builder)
        if self.CROSS_SECTIONAL_MAXIMUM is not None:
            CROSS_SECTIONAL_MAXIMUM = self.CROSS_SECTIONAL_MAXIMUM.Pack(builder)
        if self.SOLAR_ARRAY is not None:
            SOLAR_ARRAY = self.SOLAR_ARRAY.Pack(builder)
        if self.AREA_TO_MASS_RATIO is not None:
            AREA_TO_MASS_RATIO = self.AREA_TO_MASS_RATIO.Pack(builder)
        if self.BALLISTIC_COEFFICIENT is not None:
            BALLISTIC_COEFFICIENT = self.BALLISTIC_COEFFICIENT.Pack(builder)
        OPPAreasStart(builder)
        if self.CROSS_SECTIONAL_AVERAGE is not None:
            OPPAreasAddCROSS_SECTIONAL_AVERAGE(builder, CROSS_SECTIONAL_AVERAGE)
        if self.CROSS_SECTIONAL_MINIMUM is not None:
            OPPAreasAddCROSS_SECTIONAL_MINIMUM(builder, CROSS_SECTIONAL_MINIMUM)
        if self.CROSS_SECTIONAL_MAXIMUM is not None:
            OPPAreasAddCROSS_SECTIONAL_MAXIMUM(builder, CROSS_SECTIONAL_MAXIMUM)
        if self.SOLAR_ARRAY is not None:
            OPPAreasAddSOLAR_ARRAY(builder, SOLAR_ARRAY)
        if self.AREA_TO_MASS_RATIO is not None:
            OPPAreasAddAREA_TO_MASS_RATIO(builder, AREA_TO_MASS_RATIO)
        if self.BALLISTIC_COEFFICIENT is not None:
            OPPAreasAddBALLISTIC_COEFFICIENT(builder, BALLISTIC_COEFFICIENT)
        OPPAreas = OPPAreasEnd(builder)
        return OPPAreas
