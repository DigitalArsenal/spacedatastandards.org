# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Binding to the exact 3D asset bytes this record accompanies.
#
# Every field is copied verbatim from the $VAM that governs the asset, so an
# $OPP is valid for exactly one reviewed variant. Different bytes are a
# different asset: mint a new $OPP and set SUPERSEDES_OPP_CID rather than
# repointing an existing one.
class OPPAssetRef(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OPPAssetRef()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOPPAssetRef(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OPPAssetRefBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x50\x50", size_prefixed=size_prefixed)

    # OPPAssetRef
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # $VAM.ID verbatim.
    # OPPAssetRef
    def VAM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAM.VERSION verbatim.
    # OPPAssetRef
    def VAM_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # CIDv1 containing a multihash of the exact $VAM bytes.
    # OPPAssetRef
    def VAM_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAMVariant.ID verbatim: the specific glTF or GLB this record describes.
    # OPPAssetRef
    def VARIANT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAMVariant.BYTE_SHA256 verbatim: 64 lowercase hexadecimal characters
    # encoding SHA-256 of the exact asset file bytes.
    # OPPAssetRef
    def ASSET_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAMVariant.CID verbatim.
    # OPPAssetRef
    def ASSET_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAMVariant.FILE_NAME verbatim.
    # OPPAssetRef
    def ASSET_FILE_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAMVariant.MEDIA_TYPE verbatim.
    # OPPAssetRef
    def ASSET_MEDIA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAMVariant.GLTF_VERSION verbatim.
    # OPPAssetRef
    def GLTF_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # $VAM.ENTITY_ID verbatim. MUST equal OPP.ENTITY_ID.
    # OPPAssetRef
    def ENTITY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # True only when the asset geometry was authored or rescaled to the physical
    # dimensions in this record. False, the default, means the asset is
    # representative and its geometry is not a source of physical dimensions.
    # OPPAssetRef
    def GEOMETRY_MATCHES_PHYSICAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Metres per asset unit for the named variant. Restates
    # $VAMTransform.METERS_PER_SOURCE_UNIT for consumers that read only $OPP.
    # OPPAssetRef
    def METERS_PER_ASSET_UNIT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1.0

def OPPAssetRefStart(builder):
    builder.StartObject(12)

def Start(builder):
    OPPAssetRefStart(builder)

def OPPAssetRefAddVAM_ID(builder, VAM_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(VAM_ID), 0)

def AddVAM_ID(builder, VAM_ID):
    OPPAssetRefAddVAM_ID(builder, VAM_ID)

def OPPAssetRefAddVAM_VERSION(builder, VAM_VERSION):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VAM_VERSION), 0)

def AddVAM_VERSION(builder, VAM_VERSION):
    OPPAssetRefAddVAM_VERSION(builder, VAM_VERSION)

def OPPAssetRefAddVAM_CID(builder, VAM_CID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VAM_CID), 0)

def AddVAM_CID(builder, VAM_CID):
    OPPAssetRefAddVAM_CID(builder, VAM_CID)

def OPPAssetRefAddVARIANT_ID(builder, VARIANT_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(VARIANT_ID), 0)

def AddVARIANT_ID(builder, VARIANT_ID):
    OPPAssetRefAddVARIANT_ID(builder, VARIANT_ID)

def OPPAssetRefAddASSET_SHA256(builder, ASSET_SHA256):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ASSET_SHA256), 0)

def AddASSET_SHA256(builder, ASSET_SHA256):
    OPPAssetRefAddASSET_SHA256(builder, ASSET_SHA256)

def OPPAssetRefAddASSET_CID(builder, ASSET_CID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ASSET_CID), 0)

def AddASSET_CID(builder, ASSET_CID):
    OPPAssetRefAddASSET_CID(builder, ASSET_CID)

def OPPAssetRefAddASSET_FILE_NAME(builder, ASSET_FILE_NAME):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ASSET_FILE_NAME), 0)

def AddASSET_FILE_NAME(builder, ASSET_FILE_NAME):
    OPPAssetRefAddASSET_FILE_NAME(builder, ASSET_FILE_NAME)

def OPPAssetRefAddASSET_MEDIA_TYPE(builder, ASSET_MEDIA_TYPE):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(ASSET_MEDIA_TYPE), 0)

def AddASSET_MEDIA_TYPE(builder, ASSET_MEDIA_TYPE):
    OPPAssetRefAddASSET_MEDIA_TYPE(builder, ASSET_MEDIA_TYPE)

def OPPAssetRefAddGLTF_VERSION(builder, GLTF_VERSION):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(GLTF_VERSION), 0)

def AddGLTF_VERSION(builder, GLTF_VERSION):
    OPPAssetRefAddGLTF_VERSION(builder, GLTF_VERSION)

def OPPAssetRefAddENTITY_ID(builder, ENTITY_ID):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(ENTITY_ID), 0)

def AddENTITY_ID(builder, ENTITY_ID):
    OPPAssetRefAddENTITY_ID(builder, ENTITY_ID)

def OPPAssetRefAddGEOMETRY_MATCHES_PHYSICAL(builder, GEOMETRY_MATCHES_PHYSICAL):
    builder.PrependBoolSlot(10, GEOMETRY_MATCHES_PHYSICAL, 0)

def AddGEOMETRY_MATCHES_PHYSICAL(builder, GEOMETRY_MATCHES_PHYSICAL):
    OPPAssetRefAddGEOMETRY_MATCHES_PHYSICAL(builder, GEOMETRY_MATCHES_PHYSICAL)

def OPPAssetRefAddMETERS_PER_ASSET_UNIT(builder, METERS_PER_ASSET_UNIT):
    builder.PrependFloat64Slot(11, METERS_PER_ASSET_UNIT, 1.0)

def AddMETERS_PER_ASSET_UNIT(builder, METERS_PER_ASSET_UNIT):
    OPPAssetRefAddMETERS_PER_ASSET_UNIT(builder, METERS_PER_ASSET_UNIT)

def OPPAssetRefEnd(builder):
    return builder.EndObject()

def End(builder):
    return OPPAssetRefEnd(builder)


class OPPAssetRefT(object):

    # OPPAssetRefT
    def __init__(
        self,
        VAM_ID = None,
        VAM_VERSION = None,
        VAM_CID = None,
        VARIANT_ID = None,
        ASSET_SHA256 = None,
        ASSET_CID = None,
        ASSET_FILE_NAME = None,
        ASSET_MEDIA_TYPE = None,
        GLTF_VERSION = None,
        ENTITY_ID = None,
        GEOMETRY_MATCHES_PHYSICAL = False,
        METERS_PER_ASSET_UNIT = 1.0,
    ):
        self.VAM_ID = VAM_ID  # type: Optional[str]
        self.VAM_VERSION = VAM_VERSION  # type: Optional[str]
        self.VAM_CID = VAM_CID  # type: Optional[str]
        self.VARIANT_ID = VARIANT_ID  # type: Optional[str]
        self.ASSET_SHA256 = ASSET_SHA256  # type: Optional[str]
        self.ASSET_CID = ASSET_CID  # type: Optional[str]
        self.ASSET_FILE_NAME = ASSET_FILE_NAME  # type: Optional[str]
        self.ASSET_MEDIA_TYPE = ASSET_MEDIA_TYPE  # type: Optional[str]
        self.GLTF_VERSION = GLTF_VERSION  # type: Optional[str]
        self.ENTITY_ID = ENTITY_ID  # type: Optional[str]
        self.GEOMETRY_MATCHES_PHYSICAL = GEOMETRY_MATCHES_PHYSICAL  # type: bool
        self.METERS_PER_ASSET_UNIT = METERS_PER_ASSET_UNIT  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOppassetRef = OPPAssetRef()
        tmpOppassetRef.Init(buf, pos)
        return cls.InitFromObj(tmpOppassetRef)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOppassetRef):
        x = OPPAssetRefT()
        x._UnPack(tmpOppassetRef)
        return x

    # OPPAssetRefT
    def _UnPack(self, OPPAssetRef):
        if OPPAssetRef is None:
            return
        self.VAM_ID = OPPAssetRef.VAM_ID()
        self.VAM_VERSION = OPPAssetRef.VAM_VERSION()
        self.VAM_CID = OPPAssetRef.VAM_CID()
        self.VARIANT_ID = OPPAssetRef.VARIANT_ID()
        self.ASSET_SHA256 = OPPAssetRef.ASSET_SHA256()
        self.ASSET_CID = OPPAssetRef.ASSET_CID()
        self.ASSET_FILE_NAME = OPPAssetRef.ASSET_FILE_NAME()
        self.ASSET_MEDIA_TYPE = OPPAssetRef.ASSET_MEDIA_TYPE()
        self.GLTF_VERSION = OPPAssetRef.GLTF_VERSION()
        self.ENTITY_ID = OPPAssetRef.ENTITY_ID()
        self.GEOMETRY_MATCHES_PHYSICAL = OPPAssetRef.GEOMETRY_MATCHES_PHYSICAL()
        self.METERS_PER_ASSET_UNIT = OPPAssetRef.METERS_PER_ASSET_UNIT()

    # OPPAssetRefT
    def Pack(self, builder):
        if self.VAM_ID is not None:
            VAM_ID = builder.CreateString(self.VAM_ID)
        if self.VAM_VERSION is not None:
            VAM_VERSION = builder.CreateString(self.VAM_VERSION)
        if self.VAM_CID is not None:
            VAM_CID = builder.CreateString(self.VAM_CID)
        if self.VARIANT_ID is not None:
            VARIANT_ID = builder.CreateString(self.VARIANT_ID)
        if self.ASSET_SHA256 is not None:
            ASSET_SHA256 = builder.CreateString(self.ASSET_SHA256)
        if self.ASSET_CID is not None:
            ASSET_CID = builder.CreateString(self.ASSET_CID)
        if self.ASSET_FILE_NAME is not None:
            ASSET_FILE_NAME = builder.CreateString(self.ASSET_FILE_NAME)
        if self.ASSET_MEDIA_TYPE is not None:
            ASSET_MEDIA_TYPE = builder.CreateString(self.ASSET_MEDIA_TYPE)
        if self.GLTF_VERSION is not None:
            GLTF_VERSION = builder.CreateString(self.GLTF_VERSION)
        if self.ENTITY_ID is not None:
            ENTITY_ID = builder.CreateString(self.ENTITY_ID)
        OPPAssetRefStart(builder)
        if self.VAM_ID is not None:
            OPPAssetRefAddVAM_ID(builder, VAM_ID)
        if self.VAM_VERSION is not None:
            OPPAssetRefAddVAM_VERSION(builder, VAM_VERSION)
        if self.VAM_CID is not None:
            OPPAssetRefAddVAM_CID(builder, VAM_CID)
        if self.VARIANT_ID is not None:
            OPPAssetRefAddVARIANT_ID(builder, VARIANT_ID)
        if self.ASSET_SHA256 is not None:
            OPPAssetRefAddASSET_SHA256(builder, ASSET_SHA256)
        if self.ASSET_CID is not None:
            OPPAssetRefAddASSET_CID(builder, ASSET_CID)
        if self.ASSET_FILE_NAME is not None:
            OPPAssetRefAddASSET_FILE_NAME(builder, ASSET_FILE_NAME)
        if self.ASSET_MEDIA_TYPE is not None:
            OPPAssetRefAddASSET_MEDIA_TYPE(builder, ASSET_MEDIA_TYPE)
        if self.GLTF_VERSION is not None:
            OPPAssetRefAddGLTF_VERSION(builder, GLTF_VERSION)
        if self.ENTITY_ID is not None:
            OPPAssetRefAddENTITY_ID(builder, ENTITY_ID)
        OPPAssetRefAddGEOMETRY_MATCHES_PHYSICAL(builder, self.GEOMETRY_MATCHES_PHYSICAL)
        OPPAssetRefAddMETERS_PER_ASSET_UNIT(builder, self.METERS_PER_ASSET_UNIT)
        OPPAssetRef = OPPAssetRefEnd(builder)
        return OPPAssetRef
