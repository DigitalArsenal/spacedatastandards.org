# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# On-Orbit Antenna
class OOA(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OOA()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOOA(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OOABufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x4F\x41", size_prefixed=size_prefixed)

    # OOA
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Unique identifier
    # OOA
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference to parent on-orbit object
    # OOA
    def ID_ON_ORBIT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference to antenna specification
    # OOA
    def ID_ANTENNA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Antenna name or designation
    # OOA
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Antenna type (e.g., PARABOLIC, PHASED_ARRAY, HORN, HELICAL, DIPOLE, PATCH, YAGI)
    # OOA
    def ANTENNA_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of antennas of this type
    # OOA
    def QUANTITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Operating frequency band (e.g., UHF, L, S, C, X, Ku, Ka, V, W)
    # OOA
    def BAND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Minimum operating frequency in MHz
    # OOA
    def FREQ_MIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Maximum operating frequency in MHz
    # OOA
    def FREQ_MAX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Antenna gain in dBi
    # OOA
    def GAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Antenna diameter or aperture in meters
    # OOA
    def APERTURE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Beamwidth in degrees
    # OOA
    def BEAMWIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Polarization type (e.g., LINEAR, CIRCULAR, DUAL, RHCP, LHCP)
    # OOA
    def POLARIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Whether the antenna is steerable
    # OOA
    def STEERABLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Maximum slew rate in degrees per second
    # OOA
    def SLEW_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Purpose or function (e.g., TT_C, PAYLOAD, CROSSLINK, GPS)
    # OOA
    def PURPOSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Additional notes
    # OOA
    def NOTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def OOAStart(builder):
    builder.StartObject(17)

def Start(builder):
    OOAStart(builder)

def OOAAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    OOAAddID(builder, ID)

def OOAAddID_ON_ORBIT(builder, ID_ON_ORBIT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ID_ON_ORBIT), 0)

def AddID_ON_ORBIT(builder, ID_ON_ORBIT):
    OOAAddID_ON_ORBIT(builder, ID_ON_ORBIT)

def OOAAddID_ANTENNA(builder, ID_ANTENNA):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ID_ANTENNA), 0)

def AddID_ANTENNA(builder, ID_ANTENNA):
    OOAAddID_ANTENNA(builder, ID_ANTENNA)

def OOAAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    OOAAddNAME(builder, NAME)

def OOAAddANTENNA_TYPE(builder, ANTENNA_TYPE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ANTENNA_TYPE), 0)

def AddANTENNA_TYPE(builder, ANTENNA_TYPE):
    OOAAddANTENNA_TYPE(builder, ANTENNA_TYPE)

def OOAAddQUANTITY(builder, QUANTITY):
    builder.PrependUint32Slot(5, QUANTITY, 0)

def AddQUANTITY(builder, QUANTITY):
    OOAAddQUANTITY(builder, QUANTITY)

def OOAAddBAND(builder, BAND):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(BAND), 0)

def AddBAND(builder, BAND):
    OOAAddBAND(builder, BAND)

def OOAAddFREQ_MIN(builder, FREQ_MIN):
    builder.PrependFloat64Slot(7, FREQ_MIN, 0.0)

def AddFREQ_MIN(builder, FREQ_MIN):
    OOAAddFREQ_MIN(builder, FREQ_MIN)

def OOAAddFREQ_MAX(builder, FREQ_MAX):
    builder.PrependFloat64Slot(8, FREQ_MAX, 0.0)

def AddFREQ_MAX(builder, FREQ_MAX):
    OOAAddFREQ_MAX(builder, FREQ_MAX)

def OOAAddGAIN(builder, GAIN):
    builder.PrependFloat64Slot(9, GAIN, 0.0)

def AddGAIN(builder, GAIN):
    OOAAddGAIN(builder, GAIN)

def OOAAddAPERTURE(builder, APERTURE):
    builder.PrependFloat64Slot(10, APERTURE, 0.0)

def AddAPERTURE(builder, APERTURE):
    OOAAddAPERTURE(builder, APERTURE)

def OOAAddBEAMWIDTH(builder, BEAMWIDTH):
    builder.PrependFloat64Slot(11, BEAMWIDTH, 0.0)

def AddBEAMWIDTH(builder, BEAMWIDTH):
    OOAAddBEAMWIDTH(builder, BEAMWIDTH)

def OOAAddPOLARIZATION(builder, POLARIZATION):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(POLARIZATION), 0)

def AddPOLARIZATION(builder, POLARIZATION):
    OOAAddPOLARIZATION(builder, POLARIZATION)

def OOAAddSTEERABLE(builder, STEERABLE):
    builder.PrependBoolSlot(13, STEERABLE, 0)

def AddSTEERABLE(builder, STEERABLE):
    OOAAddSTEERABLE(builder, STEERABLE)

def OOAAddSLEW_RATE(builder, SLEW_RATE):
    builder.PrependFloat64Slot(14, SLEW_RATE, 0.0)

def AddSLEW_RATE(builder, SLEW_RATE):
    OOAAddSLEW_RATE(builder, SLEW_RATE)

def OOAAddPURPOSE(builder, PURPOSE):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(PURPOSE), 0)

def AddPURPOSE(builder, PURPOSE):
    OOAAddPURPOSE(builder, PURPOSE)

def OOAAddNOTES(builder, NOTES):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(NOTES), 0)

def AddNOTES(builder, NOTES):
    OOAAddNOTES(builder, NOTES)

def OOAEnd(builder):
    return builder.EndObject()

def End(builder):
    return OOAEnd(builder)


class OOAT(object):

    # OOAT
    def __init__(self):
        self.ID = None  # type: str
        self.ID_ON_ORBIT = None  # type: str
        self.ID_ANTENNA = None  # type: str
        self.NAME = None  # type: str
        self.ANTENNA_TYPE = None  # type: str
        self.QUANTITY = 0  # type: int
        self.BAND = None  # type: str
        self.FREQ_MIN = 0.0  # type: float
        self.FREQ_MAX = 0.0  # type: float
        self.GAIN = 0.0  # type: float
        self.APERTURE = 0.0  # type: float
        self.BEAMWIDTH = 0.0  # type: float
        self.POLARIZATION = None  # type: str
        self.STEERABLE = False  # type: bool
        self.SLEW_RATE = 0.0  # type: float
        self.PURPOSE = None  # type: str
        self.NOTES = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        OOA = OOA()
        OOA.Init(buf, pos)
        return cls.InitFromObj(OOA)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, OOA):
        x = OOAT()
        x._UnPack(OOA)
        return x

    # OOAT
    def _UnPack(self, OOA):
        if OOA is None:
            return
        self.ID = OOA.ID()
        self.ID_ON_ORBIT = OOA.ID_ON_ORBIT()
        self.ID_ANTENNA = OOA.ID_ANTENNA()
        self.NAME = OOA.NAME()
        self.ANTENNA_TYPE = OOA.ANTENNA_TYPE()
        self.QUANTITY = OOA.QUANTITY()
        self.BAND = OOA.BAND()
        self.FREQ_MIN = OOA.FREQ_MIN()
        self.FREQ_MAX = OOA.FREQ_MAX()
        self.GAIN = OOA.GAIN()
        self.APERTURE = OOA.APERTURE()
        self.BEAMWIDTH = OOA.BEAMWIDTH()
        self.POLARIZATION = OOA.POLARIZATION()
        self.STEERABLE = OOA.STEERABLE()
        self.SLEW_RATE = OOA.SLEW_RATE()
        self.PURPOSE = OOA.PURPOSE()
        self.NOTES = OOA.NOTES()

    # OOAT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.ID_ON_ORBIT is not None:
            ID_ON_ORBIT = builder.CreateString(self.ID_ON_ORBIT)
        if self.ID_ANTENNA is not None:
            ID_ANTENNA = builder.CreateString(self.ID_ANTENNA)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.ANTENNA_TYPE is not None:
            ANTENNA_TYPE = builder.CreateString(self.ANTENNA_TYPE)
        if self.BAND is not None:
            BAND = builder.CreateString(self.BAND)
        if self.POLARIZATION is not None:
            POLARIZATION = builder.CreateString(self.POLARIZATION)
        if self.PURPOSE is not None:
            PURPOSE = builder.CreateString(self.PURPOSE)
        if self.NOTES is not None:
            NOTES = builder.CreateString(self.NOTES)
        OOAStart(builder)
        if self.ID is not None:
            OOAAddID(builder, ID)
        if self.ID_ON_ORBIT is not None:
            OOAAddID_ON_ORBIT(builder, ID_ON_ORBIT)
        if self.ID_ANTENNA is not None:
            OOAAddID_ANTENNA(builder, ID_ANTENNA)
        if self.NAME is not None:
            OOAAddNAME(builder, NAME)
        if self.ANTENNA_TYPE is not None:
            OOAAddANTENNA_TYPE(builder, ANTENNA_TYPE)
        OOAAddQUANTITY(builder, self.QUANTITY)
        if self.BAND is not None:
            OOAAddBAND(builder, BAND)
        OOAAddFREQ_MIN(builder, self.FREQ_MIN)
        OOAAddFREQ_MAX(builder, self.FREQ_MAX)
        OOAAddGAIN(builder, self.GAIN)
        OOAAddAPERTURE(builder, self.APERTURE)
        OOAAddBEAMWIDTH(builder, self.BEAMWIDTH)
        if self.POLARIZATION is not None:
            OOAAddPOLARIZATION(builder, POLARIZATION)
        OOAAddSTEERABLE(builder, self.STEERABLE)
        OOAAddSLEW_RATE(builder, self.SLEW_RATE)
        if self.PURPOSE is not None:
            OOAAddPURPOSE(builder, PURPOSE)
        if self.NOTES is not None:
            OOAAddNOTES(builder, NOTES)
        OOA = OOAEnd(builder)
        return OOA
