# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Reflection and transmission at one incidence angle and polarization. The
# angular dependence of a real surface cannot be carried by a single
# normal-incidence number.
class RFSReflectionEntry(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RFSReflectionEntry()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRFSReflectionEntry(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RFSReflectionEntryBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x46\x53", size_prefixed=size_prefixed)

    # RFSReflectionEntry
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Frequency this entry is stated at, megahertz.
    # RFSReflectionEntry
    def FREQUENCY_MHZ(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Angle from the surface normal, degrees [0-90).
    # RFSReflectionEntry
    def INCIDENCE_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFSReflectionEntry
    def POLARIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Magnitude of the field reflection coefficient [0-1].
    # RFSReflectionEntry
    def REFLECTION_COEFFICIENT_MAGNITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Phase of the field reflection coefficient, degrees.
    # RFSReflectionEntry
    def REFLECTION_COEFFICIENT_PHASE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Reflection loss, dB (positive).
    # RFSReflectionEntry
    def REFLECTION_LOSS_DB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Loss through the material at this angle, dB (positive).
    # RFSReflectionEntry
    def TRANSMISSION_LOSS_DB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Power removed from the specular direction by surface roughness, dB.
    # RFSReflectionEntry
    def SCATTERING_LOSS_DB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def RFSReflectionEntryStart(builder):
    builder.StartObject(8)

def Start(builder):
    RFSReflectionEntryStart(builder)

def RFSReflectionEntryAddFREQUENCY_MHZ(builder, FREQUENCY_MHZ):
    builder.PrependFloat64Slot(0, FREQUENCY_MHZ, 0.0)

def AddFREQUENCY_MHZ(builder, FREQUENCY_MHZ):
    RFSReflectionEntryAddFREQUENCY_MHZ(builder, FREQUENCY_MHZ)

def RFSReflectionEntryAddINCIDENCE_ANGLE_DEG(builder, INCIDENCE_ANGLE_DEG):
    builder.PrependFloat64Slot(1, INCIDENCE_ANGLE_DEG, 0.0)

def AddINCIDENCE_ANGLE_DEG(builder, INCIDENCE_ANGLE_DEG):
    RFSReflectionEntryAddINCIDENCE_ANGLE_DEG(builder, INCIDENCE_ANGLE_DEG)

def RFSReflectionEntryAddPOLARIZATION(builder, POLARIZATION):
    builder.PrependInt8Slot(2, POLARIZATION, 0)

def AddPOLARIZATION(builder, POLARIZATION):
    RFSReflectionEntryAddPOLARIZATION(builder, POLARIZATION)

def RFSReflectionEntryAddREFLECTION_COEFFICIENT_MAGNITUDE(builder, REFLECTION_COEFFICIENT_MAGNITUDE):
    builder.PrependFloat64Slot(3, REFLECTION_COEFFICIENT_MAGNITUDE, 0.0)

def AddREFLECTION_COEFFICIENT_MAGNITUDE(builder, REFLECTION_COEFFICIENT_MAGNITUDE):
    RFSReflectionEntryAddREFLECTION_COEFFICIENT_MAGNITUDE(builder, REFLECTION_COEFFICIENT_MAGNITUDE)

def RFSReflectionEntryAddREFLECTION_COEFFICIENT_PHASE_DEG(builder, REFLECTION_COEFFICIENT_PHASE_DEG):
    builder.PrependFloat64Slot(4, REFLECTION_COEFFICIENT_PHASE_DEG, 0.0)

def AddREFLECTION_COEFFICIENT_PHASE_DEG(builder, REFLECTION_COEFFICIENT_PHASE_DEG):
    RFSReflectionEntryAddREFLECTION_COEFFICIENT_PHASE_DEG(builder, REFLECTION_COEFFICIENT_PHASE_DEG)

def RFSReflectionEntryAddREFLECTION_LOSS_DB(builder, REFLECTION_LOSS_DB):
    builder.PrependFloat64Slot(5, REFLECTION_LOSS_DB, 0.0)

def AddREFLECTION_LOSS_DB(builder, REFLECTION_LOSS_DB):
    RFSReflectionEntryAddREFLECTION_LOSS_DB(builder, REFLECTION_LOSS_DB)

def RFSReflectionEntryAddTRANSMISSION_LOSS_DB(builder, TRANSMISSION_LOSS_DB):
    builder.PrependFloat64Slot(6, TRANSMISSION_LOSS_DB, 0.0)

def AddTRANSMISSION_LOSS_DB(builder, TRANSMISSION_LOSS_DB):
    RFSReflectionEntryAddTRANSMISSION_LOSS_DB(builder, TRANSMISSION_LOSS_DB)

def RFSReflectionEntryAddSCATTERING_LOSS_DB(builder, SCATTERING_LOSS_DB):
    builder.PrependFloat64Slot(7, SCATTERING_LOSS_DB, 0.0)

def AddSCATTERING_LOSS_DB(builder, SCATTERING_LOSS_DB):
    RFSReflectionEntryAddSCATTERING_LOSS_DB(builder, SCATTERING_LOSS_DB)

def RFSReflectionEntryEnd(builder):
    return builder.EndObject()

def End(builder):
    return RFSReflectionEntryEnd(builder)


class RFSReflectionEntryT(object):

    # RFSReflectionEntryT
    def __init__(
        self,
        FREQUENCY_MHZ = 0.0,
        INCIDENCE_ANGLE_DEG = 0.0,
        POLARIZATION = 0,
        REFLECTION_COEFFICIENT_MAGNITUDE = 0.0,
        REFLECTION_COEFFICIENT_PHASE_DEG = 0.0,
        REFLECTION_LOSS_DB = 0.0,
        TRANSMISSION_LOSS_DB = 0.0,
        SCATTERING_LOSS_DB = 0.0,
    ):
        self.FREQUENCY_MHZ = FREQUENCY_MHZ  # type: float
        self.INCIDENCE_ANGLE_DEG = INCIDENCE_ANGLE_DEG  # type: float
        self.POLARIZATION = POLARIZATION  # type: int
        self.REFLECTION_COEFFICIENT_MAGNITUDE = REFLECTION_COEFFICIENT_MAGNITUDE  # type: float
        self.REFLECTION_COEFFICIENT_PHASE_DEG = REFLECTION_COEFFICIENT_PHASE_DEG  # type: float
        self.REFLECTION_LOSS_DB = REFLECTION_LOSS_DB  # type: float
        self.TRANSMISSION_LOSS_DB = TRANSMISSION_LOSS_DB  # type: float
        self.SCATTERING_LOSS_DB = SCATTERING_LOSS_DB  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpRfsreflectionEntry = RFSReflectionEntry()
        tmpRfsreflectionEntry.Init(buf, pos)
        return cls.InitFromObj(tmpRfsreflectionEntry)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpRfsreflectionEntry):
        x = RFSReflectionEntryT()
        x._UnPack(tmpRfsreflectionEntry)
        return x

    # RFSReflectionEntryT
    def _UnPack(self, RFSReflectionEntry):
        if RFSReflectionEntry is None:
            return
        self.FREQUENCY_MHZ = RFSReflectionEntry.FREQUENCY_MHZ()
        self.INCIDENCE_ANGLE_DEG = RFSReflectionEntry.INCIDENCE_ANGLE_DEG()
        self.POLARIZATION = RFSReflectionEntry.POLARIZATION()
        self.REFLECTION_COEFFICIENT_MAGNITUDE = RFSReflectionEntry.REFLECTION_COEFFICIENT_MAGNITUDE()
        self.REFLECTION_COEFFICIENT_PHASE_DEG = RFSReflectionEntry.REFLECTION_COEFFICIENT_PHASE_DEG()
        self.REFLECTION_LOSS_DB = RFSReflectionEntry.REFLECTION_LOSS_DB()
        self.TRANSMISSION_LOSS_DB = RFSReflectionEntry.TRANSMISSION_LOSS_DB()
        self.SCATTERING_LOSS_DB = RFSReflectionEntry.SCATTERING_LOSS_DB()

    # RFSReflectionEntryT
    def Pack(self, builder):
        RFSReflectionEntryStart(builder)
        RFSReflectionEntryAddFREQUENCY_MHZ(builder, self.FREQUENCY_MHZ)
        RFSReflectionEntryAddINCIDENCE_ANGLE_DEG(builder, self.INCIDENCE_ANGLE_DEG)
        RFSReflectionEntryAddPOLARIZATION(builder, self.POLARIZATION)
        RFSReflectionEntryAddREFLECTION_COEFFICIENT_MAGNITUDE(builder, self.REFLECTION_COEFFICIENT_MAGNITUDE)
        RFSReflectionEntryAddREFLECTION_COEFFICIENT_PHASE_DEG(builder, self.REFLECTION_COEFFICIENT_PHASE_DEG)
        RFSReflectionEntryAddREFLECTION_LOSS_DB(builder, self.REFLECTION_LOSS_DB)
        RFSReflectionEntryAddTRANSMISSION_LOSS_DB(builder, self.TRANSMISSION_LOSS_DB)
        RFSReflectionEntryAddSCATTERING_LOSS_DB(builder, self.SCATTERING_LOSS_DB)
        RFSReflectionEntry = RFSReflectionEntryEnd(builder)
        return RFSReflectionEntry
