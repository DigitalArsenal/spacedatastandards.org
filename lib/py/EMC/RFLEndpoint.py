# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One endpoint of a link. Identity ONLY — position, antenna geometry and band
# capability are NOT copied here; they are joined from the standard named by
# ENDPOINT_KIND. A copied position silently goes stale against its source.
class RFLEndpoint(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RFLEndpoint()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRFLEndpoint(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RFLEndpointBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x46\x4C", size_prefixed=size_prefixed)

    # RFLEndpoint
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Producer-stable identifier for this endpoint within the record.
    # RFLEndpoint
    def ENDPOINT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFLEndpoint
    def ENDPOINT_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Human-readable name.
    # RFLEndpoint
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$GST.STATION_ID` when ENDPOINT_KIND is GEODETIC_TRACKING_STATION.
    # RFLEndpoint
    def GST_STATION_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$SIT` identifier when ENDPOINT_KIND is SATELLITE_GROUND_STATION.
    # RFLEndpoint
    def SIT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$TBS.SITE_ID` when ENDPOINT_KIND is TERRESTRIAL_SITE.
    # RFLEndpoint
    def TBS_SITE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$CAT.NORAD_CAT_ID` when the endpoint is an on-orbit object. 0 when
    # unbound — never a placeholder catalogue number.
    # RFLEndpoint
    def NORAD_CAT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # International designator, `$CAT.OBJECT_ID`.
    # RFLEndpoint
    def OBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$RFB.ID` — the band specification (frequency, bandwidth, EIRP, gains,
    # polarization) this endpoint transmits or receives on.
    # RFLEndpoint
    def RFB_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$RFE.ID` — the parametric emitter description, when one exists.
    # RFLEndpoint
    def RFE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$IQC` capture identifier, when the samples were derived from a recording.
    # RFLEndpoint
    def IQC_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Beam identifier within the emitter.
    # RFLEndpoint
    def ID_BEAM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Channel or carrier identifier within the beam.
    # RFLEndpoint
    def CHANNEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Polarization actually used on this link. Defaults to UNKNOWN explicitly:
    # `rfPolarization` ordinal 0 is LHCP, so an unset field would otherwise
    # decode as a real polarization.
    # RFLEndpoint
    def POLARIZATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 6

def RFLEndpointStart(builder):
    builder.StartObject(14)

def Start(builder):
    RFLEndpointStart(builder)

def RFLEndpointAddENDPOINT_ID(builder, ENDPOINT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ENDPOINT_ID), 0)

def AddENDPOINT_ID(builder, ENDPOINT_ID):
    RFLEndpointAddENDPOINT_ID(builder, ENDPOINT_ID)

def RFLEndpointAddENDPOINT_KIND(builder, ENDPOINT_KIND):
    builder.PrependInt8Slot(1, ENDPOINT_KIND, 0)

def AddENDPOINT_KIND(builder, ENDPOINT_KIND):
    RFLEndpointAddENDPOINT_KIND(builder, ENDPOINT_KIND)

def RFLEndpointAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    RFLEndpointAddNAME(builder, NAME)

def RFLEndpointAddGST_STATION_ID(builder, GST_STATION_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(GST_STATION_ID), 0)

def AddGST_STATION_ID(builder, GST_STATION_ID):
    RFLEndpointAddGST_STATION_ID(builder, GST_STATION_ID)

def RFLEndpointAddSIT_ID(builder, SIT_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SIT_ID), 0)

def AddSIT_ID(builder, SIT_ID):
    RFLEndpointAddSIT_ID(builder, SIT_ID)

def RFLEndpointAddTBS_SITE_ID(builder, TBS_SITE_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(TBS_SITE_ID), 0)

def AddTBS_SITE_ID(builder, TBS_SITE_ID):
    RFLEndpointAddTBS_SITE_ID(builder, TBS_SITE_ID)

def RFLEndpointAddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    builder.PrependUint32Slot(6, NORAD_CAT_ID, 0)

def AddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    RFLEndpointAddNORAD_CAT_ID(builder, NORAD_CAT_ID)

def RFLEndpointAddOBJECT_ID(builder, OBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT_ID), 0)

def AddOBJECT_ID(builder, OBJECT_ID):
    RFLEndpointAddOBJECT_ID(builder, OBJECT_ID)

def RFLEndpointAddRFB_ID(builder, RFB_ID):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(RFB_ID), 0)

def AddRFB_ID(builder, RFB_ID):
    RFLEndpointAddRFB_ID(builder, RFB_ID)

def RFLEndpointAddRFE_ID(builder, RFE_ID):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(RFE_ID), 0)

def AddRFE_ID(builder, RFE_ID):
    RFLEndpointAddRFE_ID(builder, RFE_ID)

def RFLEndpointAddIQC_ID(builder, IQC_ID):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(IQC_ID), 0)

def AddIQC_ID(builder, IQC_ID):
    RFLEndpointAddIQC_ID(builder, IQC_ID)

def RFLEndpointAddID_BEAM(builder, ID_BEAM):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(ID_BEAM), 0)

def AddID_BEAM(builder, ID_BEAM):
    RFLEndpointAddID_BEAM(builder, ID_BEAM)

def RFLEndpointAddCHANNEL_ID(builder, CHANNEL_ID):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(CHANNEL_ID), 0)

def AddCHANNEL_ID(builder, CHANNEL_ID):
    RFLEndpointAddCHANNEL_ID(builder, CHANNEL_ID)

def RFLEndpointAddPOLARIZATION(builder, POLARIZATION):
    builder.PrependInt8Slot(13, POLARIZATION, 6)

def AddPOLARIZATION(builder, POLARIZATION):
    RFLEndpointAddPOLARIZATION(builder, POLARIZATION)

def RFLEndpointEnd(builder):
    return builder.EndObject()

def End(builder):
    return RFLEndpointEnd(builder)


class RFLEndpointT(object):

    # RFLEndpointT
    def __init__(
        self,
        ENDPOINT_ID = None,
        ENDPOINT_KIND = 0,
        NAME = None,
        GST_STATION_ID = None,
        SIT_ID = None,
        TBS_SITE_ID = None,
        NORAD_CAT_ID = 0,
        OBJECT_ID = None,
        RFB_ID = None,
        RFE_ID = None,
        IQC_ID = None,
        ID_BEAM = None,
        CHANNEL_ID = None,
        POLARIZATION = 6,
    ):
        self.ENDPOINT_ID = ENDPOINT_ID  # type: Optional[str]
        self.ENDPOINT_KIND = ENDPOINT_KIND  # type: int
        self.NAME = NAME  # type: Optional[str]
        self.GST_STATION_ID = GST_STATION_ID  # type: Optional[str]
        self.SIT_ID = SIT_ID  # type: Optional[str]
        self.TBS_SITE_ID = TBS_SITE_ID  # type: Optional[str]
        self.NORAD_CAT_ID = NORAD_CAT_ID  # type: int
        self.OBJECT_ID = OBJECT_ID  # type: Optional[str]
        self.RFB_ID = RFB_ID  # type: Optional[str]
        self.RFE_ID = RFE_ID  # type: Optional[str]
        self.IQC_ID = IQC_ID  # type: Optional[str]
        self.ID_BEAM = ID_BEAM  # type: Optional[str]
        self.CHANNEL_ID = CHANNEL_ID  # type: Optional[str]
        self.POLARIZATION = POLARIZATION  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpRflendpoint = RFLEndpoint()
        tmpRflendpoint.Init(buf, pos)
        return cls.InitFromObj(tmpRflendpoint)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpRflendpoint):
        x = RFLEndpointT()
        x._UnPack(tmpRflendpoint)
        return x

    # RFLEndpointT
    def _UnPack(self, RFLEndpoint):
        if RFLEndpoint is None:
            return
        self.ENDPOINT_ID = RFLEndpoint.ENDPOINT_ID()
        self.ENDPOINT_KIND = RFLEndpoint.ENDPOINT_KIND()
        self.NAME = RFLEndpoint.NAME()
        self.GST_STATION_ID = RFLEndpoint.GST_STATION_ID()
        self.SIT_ID = RFLEndpoint.SIT_ID()
        self.TBS_SITE_ID = RFLEndpoint.TBS_SITE_ID()
        self.NORAD_CAT_ID = RFLEndpoint.NORAD_CAT_ID()
        self.OBJECT_ID = RFLEndpoint.OBJECT_ID()
        self.RFB_ID = RFLEndpoint.RFB_ID()
        self.RFE_ID = RFLEndpoint.RFE_ID()
        self.IQC_ID = RFLEndpoint.IQC_ID()
        self.ID_BEAM = RFLEndpoint.ID_BEAM()
        self.CHANNEL_ID = RFLEndpoint.CHANNEL_ID()
        self.POLARIZATION = RFLEndpoint.POLARIZATION()

    # RFLEndpointT
    def Pack(self, builder):
        if self.ENDPOINT_ID is not None:
            ENDPOINT_ID = builder.CreateString(self.ENDPOINT_ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.GST_STATION_ID is not None:
            GST_STATION_ID = builder.CreateString(self.GST_STATION_ID)
        if self.SIT_ID is not None:
            SIT_ID = builder.CreateString(self.SIT_ID)
        if self.TBS_SITE_ID is not None:
            TBS_SITE_ID = builder.CreateString(self.TBS_SITE_ID)
        if self.OBJECT_ID is not None:
            OBJECT_ID = builder.CreateString(self.OBJECT_ID)
        if self.RFB_ID is not None:
            RFB_ID = builder.CreateString(self.RFB_ID)
        if self.RFE_ID is not None:
            RFE_ID = builder.CreateString(self.RFE_ID)
        if self.IQC_ID is not None:
            IQC_ID = builder.CreateString(self.IQC_ID)
        if self.ID_BEAM is not None:
            ID_BEAM = builder.CreateString(self.ID_BEAM)
        if self.CHANNEL_ID is not None:
            CHANNEL_ID = builder.CreateString(self.CHANNEL_ID)
        RFLEndpointStart(builder)
        if self.ENDPOINT_ID is not None:
            RFLEndpointAddENDPOINT_ID(builder, ENDPOINT_ID)
        RFLEndpointAddENDPOINT_KIND(builder, self.ENDPOINT_KIND)
        if self.NAME is not None:
            RFLEndpointAddNAME(builder, NAME)
        if self.GST_STATION_ID is not None:
            RFLEndpointAddGST_STATION_ID(builder, GST_STATION_ID)
        if self.SIT_ID is not None:
            RFLEndpointAddSIT_ID(builder, SIT_ID)
        if self.TBS_SITE_ID is not None:
            RFLEndpointAddTBS_SITE_ID(builder, TBS_SITE_ID)
        RFLEndpointAddNORAD_CAT_ID(builder, self.NORAD_CAT_ID)
        if self.OBJECT_ID is not None:
            RFLEndpointAddOBJECT_ID(builder, OBJECT_ID)
        if self.RFB_ID is not None:
            RFLEndpointAddRFB_ID(builder, RFB_ID)
        if self.RFE_ID is not None:
            RFLEndpointAddRFE_ID(builder, RFE_ID)
        if self.IQC_ID is not None:
            RFLEndpointAddIQC_ID(builder, IQC_ID)
        if self.ID_BEAM is not None:
            RFLEndpointAddID_BEAM(builder, ID_BEAM)
        if self.CHANNEL_ID is not None:
            RFLEndpointAddCHANNEL_ID(builder, CHANNEL_ID)
        RFLEndpointAddPOLARIZATION(builder, self.POLARIZATION)
        RFLEndpoint = RFLEndpointEnd(builder)
        return RFLEndpoint
