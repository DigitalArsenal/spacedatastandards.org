# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One unit of the page's data contract: a named flow describing what data
# enters or leaves the running page, the SDS standard it carries, how it is
# transported, and — when applicable — the loaded module method port bound
# to it. Standards-only rule: every flow payload is an SDS record (a
# canonical size-prefixed FlatBuffer) or a CID pointing at one; this table
# defines no data shapes of its own, it only references an existing
# spacedatastandards.org schema by its established code. Locators are
# content-addressed and IPFS-first.
class APPDataflow(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = APPDataflow()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAPPDataflow(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def APPDataflowBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x50\x50", size_prefixed=size_prefixed)

    # APPDataflow
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # App-local stable name for this flow. Required, unique within the
    # manifest.
    # APPDataflow
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Which way the payload crosses the page boundary at runtime.
    # APPDataflow
    def DIRECTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Existing SDS schema code carried by this flow, for example OMM, OEM, or
    # PNM. Required. Mirrors APPDataRef.SDS_TYPE but named for the standard
    # the flow carries; the app defines no schema of its own.
    # APPDataflow
    def SDS_SCHEMA(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Transport that moves the payload. Defaults to IPFS_CID per the
    # content-addressed, IPFS-first rule.
    # APPDataflow
    def TRANSPORT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Where to fetch or reach the payload, interpreted per TRANSPORT: a CID
    # for IPFS_CID, a gossip topic name for PUBSUB_TOPIC, or a same-origin
    # route template for GATEWAY_ROUTE.
    # APPDataflow
    def LOCATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # When present, must equal an APPModuleRef.ID in the same manifest — the
    # loaded module that produces or consumes this flow. Binds the flow to a
    # specific module method port together with METHOD_ID and PORT_ID.
    # APPDataflow
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # When present, the PLG.PLGMethodManifest.METHOD_ID on MODULE_ID that
    # this flow is bound to.
    # APPDataflow
    def METHOD_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # When present, the PLG.PLGPortManifest.PORT_ID on METHOD_ID that this
    # flow is bound to.
    # APPDataflow
    def PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # String/compression form of the payload as it crosses the channel,
    # reusing the page content-encoding vocabulary. For flows carrying
    # canonical SDS FlatBuffer bytes (or a CID string), UTF8 denotes the raw
    # bytes/string with no extra wrapper; BASE64, BASE64_GZIP, and
    # BASE64_BROTLI denote a base64 text wrapper (optionally compressed)
    # applied when the channel is text-only.
    # APPDataflow
    def CONTENT_ENCODING(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Human-readable summary.
    # APPDataflow
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def APPDataflowStart(builder):
    builder.StartObject(10)

def Start(builder):
    APPDataflowStart(builder)

def APPDataflowAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    APPDataflowAddNAME(builder, NAME)

def APPDataflowAddDIRECTION(builder, DIRECTION):
    builder.PrependUint8Slot(1, DIRECTION, 0)

def AddDIRECTION(builder, DIRECTION):
    APPDataflowAddDIRECTION(builder, DIRECTION)

def APPDataflowAddSDS_SCHEMA(builder, SDS_SCHEMA):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SDS_SCHEMA), 0)

def AddSDS_SCHEMA(builder, SDS_SCHEMA):
    APPDataflowAddSDS_SCHEMA(builder, SDS_SCHEMA)

def APPDataflowAddTRANSPORT(builder, TRANSPORT):
    builder.PrependUint8Slot(3, TRANSPORT, 0)

def AddTRANSPORT(builder, TRANSPORT):
    APPDataflowAddTRANSPORT(builder, TRANSPORT)

def APPDataflowAddLOCATOR(builder, LOCATOR):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(LOCATOR), 0)

def AddLOCATOR(builder, LOCATOR):
    APPDataflowAddLOCATOR(builder, LOCATOR)

def APPDataflowAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    APPDataflowAddMODULE_ID(builder, MODULE_ID)

def APPDataflowAddMETHOD_ID(builder, METHOD_ID):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(METHOD_ID), 0)

def AddMETHOD_ID(builder, METHOD_ID):
    APPDataflowAddMETHOD_ID(builder, METHOD_ID)

def APPDataflowAddPORT_ID(builder, PORT_ID):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(PORT_ID), 0)

def AddPORT_ID(builder, PORT_ID):
    APPDataflowAddPORT_ID(builder, PORT_ID)

def APPDataflowAddCONTENT_ENCODING(builder, CONTENT_ENCODING):
    builder.PrependUint8Slot(8, CONTENT_ENCODING, 0)

def AddCONTENT_ENCODING(builder, CONTENT_ENCODING):
    APPDataflowAddCONTENT_ENCODING(builder, CONTENT_ENCODING)

def APPDataflowAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    APPDataflowAddDESCRIPTION(builder, DESCRIPTION)

def APPDataflowEnd(builder):
    return builder.EndObject()

def End(builder):
    return APPDataflowEnd(builder)


class APPDataflowT(object):

    # APPDataflowT
    def __init__(
        self,
        NAME = None,
        DIRECTION = 0,
        SDS_SCHEMA = None,
        TRANSPORT = 0,
        LOCATOR = None,
        MODULE_ID = None,
        METHOD_ID = None,
        PORT_ID = None,
        CONTENT_ENCODING = 0,
        DESCRIPTION = None,
    ):
        self.NAME = NAME  # type: Optional[str]
        self.DIRECTION = DIRECTION  # type: int
        self.SDS_SCHEMA = SDS_SCHEMA  # type: Optional[str]
        self.TRANSPORT = TRANSPORT  # type: int
        self.LOCATOR = LOCATOR  # type: Optional[str]
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.METHOD_ID = METHOD_ID  # type: Optional[str]
        self.PORT_ID = PORT_ID  # type: Optional[str]
        self.CONTENT_ENCODING = CONTENT_ENCODING  # type: int
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAppdataflow = APPDataflow()
        tmpAppdataflow.Init(buf, pos)
        return cls.InitFromObj(tmpAppdataflow)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAppdataflow):
        x = APPDataflowT()
        x._UnPack(tmpAppdataflow)
        return x

    # APPDataflowT
    def _UnPack(self, APPDataflow):
        if APPDataflow is None:
            return
        self.NAME = APPDataflow.NAME()
        self.DIRECTION = APPDataflow.DIRECTION()
        self.SDS_SCHEMA = APPDataflow.SDS_SCHEMA()
        self.TRANSPORT = APPDataflow.TRANSPORT()
        self.LOCATOR = APPDataflow.LOCATOR()
        self.MODULE_ID = APPDataflow.MODULE_ID()
        self.METHOD_ID = APPDataflow.METHOD_ID()
        self.PORT_ID = APPDataflow.PORT_ID()
        self.CONTENT_ENCODING = APPDataflow.CONTENT_ENCODING()
        self.DESCRIPTION = APPDataflow.DESCRIPTION()

    # APPDataflowT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.SDS_SCHEMA is not None:
            SDS_SCHEMA = builder.CreateString(self.SDS_SCHEMA)
        if self.LOCATOR is not None:
            LOCATOR = builder.CreateString(self.LOCATOR)
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.METHOD_ID is not None:
            METHOD_ID = builder.CreateString(self.METHOD_ID)
        if self.PORT_ID is not None:
            PORT_ID = builder.CreateString(self.PORT_ID)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        APPDataflowStart(builder)
        if self.NAME is not None:
            APPDataflowAddNAME(builder, NAME)
        APPDataflowAddDIRECTION(builder, self.DIRECTION)
        if self.SDS_SCHEMA is not None:
            APPDataflowAddSDS_SCHEMA(builder, SDS_SCHEMA)
        APPDataflowAddTRANSPORT(builder, self.TRANSPORT)
        if self.LOCATOR is not None:
            APPDataflowAddLOCATOR(builder, LOCATOR)
        if self.MODULE_ID is not None:
            APPDataflowAddMODULE_ID(builder, MODULE_ID)
        if self.METHOD_ID is not None:
            APPDataflowAddMETHOD_ID(builder, METHOD_ID)
        if self.PORT_ID is not None:
            APPDataflowAddPORT_ID(builder, PORT_ID)
        APPDataflowAddCONTENT_ENCODING(builder, self.CONTENT_ENCODING)
        if self.DESCRIPTION is not None:
            APPDataflowAddDESCRIPTION(builder, DESCRIPTION)
        APPDataflow = APPDataflowEnd(builder)
        return APPDataflow
