# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One member WASM module of the app. References module identity; never
# embeds the module artifact itself (delivery is the module-bundle lane).
class APPModuleRef(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = APPModuleRef()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAPPModuleRef(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def APPModuleRefBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x50\x50", size_prefixed=size_prefixed)

    # APPModuleRef
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # App-local stable reference for this module. Required, unique within
    # the manifest.
    # APPModuleRef
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Plugin identifier of the module as published in its plugin listing.
    # APPModuleRef
    def PLUGIN_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # 64 lowercase hexadecimal characters encoding SHA-256 of the module's
    # portable (pre-AOT, publication-trailer-stripped) WASM bytes. This is
    # the identity capability policies and signature policies key on.
    # APPModuleRef
    def CONTENT_HASH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Module version expected by the app.
    # APPModuleRef
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Free-text launcher hint, for example primary, worker, or ui-host.
    # APPModuleRef
    def ROLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable summary.
    # APPModuleRef
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Per-invocation wall-clock budget override in milliseconds. Zero means
    # the host runtime default applies.
    # APPModuleRef
    def MAX_WALL_CLOCK_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Per-invocation execution-cost (fuel) budget override. Zero means the
    # host runtime default applies.
    # APPModuleRef
    def MAX_COST_UNITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Linear-memory ceiling override in 64 KiB WASM pages. Zero means the
    # host runtime default applies.
    # APPModuleRef
    def MAX_MEMORY_PAGES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Where this module is instantiated. PAGE or BOTH means the module also
    # loads in the browser: the page resolves its bytes by CONTENT_HASH over
    # IPFS and instantiates it through the SAME isomorphic module-sdk harness
    # ABI the SDN nodes use (manifest + plugin_invoke_stream) — never through
    # a bespoke page-only loader. Defaults to NODE to preserve the prior
    # node-only behavior of manifests written before this field existed.
    # APPModuleRef
    def RUNTIME_TARGET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def APPModuleRefStart(builder):
    builder.StartObject(10)

def Start(builder):
    APPModuleRefStart(builder)

def APPModuleRefAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    APPModuleRefAddID(builder, ID)

def APPModuleRefAddPLUGIN_ID(builder, PLUGIN_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PLUGIN_ID), 0)

def AddPLUGIN_ID(builder, PLUGIN_ID):
    APPModuleRefAddPLUGIN_ID(builder, PLUGIN_ID)

def APPModuleRefAddCONTENT_HASH(builder, CONTENT_HASH):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT_HASH), 0)

def AddCONTENT_HASH(builder, CONTENT_HASH):
    APPModuleRefAddCONTENT_HASH(builder, CONTENT_HASH)

def APPModuleRefAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    APPModuleRefAddVERSION(builder, VERSION)

def APPModuleRefAddROLE(builder, ROLE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ROLE), 0)

def AddROLE(builder, ROLE):
    APPModuleRefAddROLE(builder, ROLE)

def APPModuleRefAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    APPModuleRefAddDESCRIPTION(builder, DESCRIPTION)

def APPModuleRefAddMAX_WALL_CLOCK_MS(builder, MAX_WALL_CLOCK_MS):
    builder.PrependUint64Slot(6, MAX_WALL_CLOCK_MS, 0)

def AddMAX_WALL_CLOCK_MS(builder, MAX_WALL_CLOCK_MS):
    APPModuleRefAddMAX_WALL_CLOCK_MS(builder, MAX_WALL_CLOCK_MS)

def APPModuleRefAddMAX_COST_UNITS(builder, MAX_COST_UNITS):
    builder.PrependUint64Slot(7, MAX_COST_UNITS, 0)

def AddMAX_COST_UNITS(builder, MAX_COST_UNITS):
    APPModuleRefAddMAX_COST_UNITS(builder, MAX_COST_UNITS)

def APPModuleRefAddMAX_MEMORY_PAGES(builder, MAX_MEMORY_PAGES):
    builder.PrependUint32Slot(8, MAX_MEMORY_PAGES, 0)

def AddMAX_MEMORY_PAGES(builder, MAX_MEMORY_PAGES):
    APPModuleRefAddMAX_MEMORY_PAGES(builder, MAX_MEMORY_PAGES)

def APPModuleRefAddRUNTIME_TARGET(builder, RUNTIME_TARGET):
    builder.PrependUint8Slot(9, RUNTIME_TARGET, 0)

def AddRUNTIME_TARGET(builder, RUNTIME_TARGET):
    APPModuleRefAddRUNTIME_TARGET(builder, RUNTIME_TARGET)

def APPModuleRefEnd(builder):
    return builder.EndObject()

def End(builder):
    return APPModuleRefEnd(builder)


class APPModuleRefT(object):

    # APPModuleRefT
    def __init__(
        self,
        ID = None,
        PLUGIN_ID = None,
        CONTENT_HASH = None,
        VERSION = None,
        ROLE = None,
        DESCRIPTION = None,
        MAX_WALL_CLOCK_MS = 0,
        MAX_COST_UNITS = 0,
        MAX_MEMORY_PAGES = 0,
        RUNTIME_TARGET = 0,
    ):
        self.ID = ID  # type: Optional[str]
        self.PLUGIN_ID = PLUGIN_ID  # type: Optional[str]
        self.CONTENT_HASH = CONTENT_HASH  # type: Optional[str]
        self.VERSION = VERSION  # type: Optional[str]
        self.ROLE = ROLE  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.MAX_WALL_CLOCK_MS = MAX_WALL_CLOCK_MS  # type: int
        self.MAX_COST_UNITS = MAX_COST_UNITS  # type: int
        self.MAX_MEMORY_PAGES = MAX_MEMORY_PAGES  # type: int
        self.RUNTIME_TARGET = RUNTIME_TARGET  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAppmoduleRef = APPModuleRef()
        tmpAppmoduleRef.Init(buf, pos)
        return cls.InitFromObj(tmpAppmoduleRef)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAppmoduleRef):
        x = APPModuleRefT()
        x._UnPack(tmpAppmoduleRef)
        return x

    # APPModuleRefT
    def _UnPack(self, APPModuleRef):
        if APPModuleRef is None:
            return
        self.ID = APPModuleRef.ID()
        self.PLUGIN_ID = APPModuleRef.PLUGIN_ID()
        self.CONTENT_HASH = APPModuleRef.CONTENT_HASH()
        self.VERSION = APPModuleRef.VERSION()
        self.ROLE = APPModuleRef.ROLE()
        self.DESCRIPTION = APPModuleRef.DESCRIPTION()
        self.MAX_WALL_CLOCK_MS = APPModuleRef.MAX_WALL_CLOCK_MS()
        self.MAX_COST_UNITS = APPModuleRef.MAX_COST_UNITS()
        self.MAX_MEMORY_PAGES = APPModuleRef.MAX_MEMORY_PAGES()
        self.RUNTIME_TARGET = APPModuleRef.RUNTIME_TARGET()

    # APPModuleRefT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.PLUGIN_ID is not None:
            PLUGIN_ID = builder.CreateString(self.PLUGIN_ID)
        if self.CONTENT_HASH is not None:
            CONTENT_HASH = builder.CreateString(self.CONTENT_HASH)
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        if self.ROLE is not None:
            ROLE = builder.CreateString(self.ROLE)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        APPModuleRefStart(builder)
        if self.ID is not None:
            APPModuleRefAddID(builder, ID)
        if self.PLUGIN_ID is not None:
            APPModuleRefAddPLUGIN_ID(builder, PLUGIN_ID)
        if self.CONTENT_HASH is not None:
            APPModuleRefAddCONTENT_HASH(builder, CONTENT_HASH)
        if self.VERSION is not None:
            APPModuleRefAddVERSION(builder, VERSION)
        if self.ROLE is not None:
            APPModuleRefAddROLE(builder, ROLE)
        if self.DESCRIPTION is not None:
            APPModuleRefAddDESCRIPTION(builder, DESCRIPTION)
        APPModuleRefAddMAX_WALL_CLOCK_MS(builder, self.MAX_WALL_CLOCK_MS)
        APPModuleRefAddMAX_COST_UNITS(builder, self.MAX_COST_UNITS)
        APPModuleRefAddMAX_MEMORY_PAGES(builder, self.MAX_MEMORY_PAGES)
        APPModuleRefAddRUNTIME_TARGET(builder, self.RUNTIME_TARGET)
        APPModuleRef = APPModuleRefEnd(builder)
        return APPModuleRef
