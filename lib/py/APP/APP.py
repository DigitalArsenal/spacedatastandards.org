# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Application Package Manifest — one launchable app.
class APP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = APP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAPP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def APPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x50\x50", size_prefixed=size_prefixed)

    # APP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable app identity, unique per publisher. Required.
    # APP
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Display name.
    # APP
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SemVer 2.0.0 app version.
    # APP
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable summary.
    # APP
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Member WASM modules. Referential integrity: every MODULE_ID and every
    # MODULE-kind REF elsewhere in the manifest must resolve into this list.
    # APP
    def MODULES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from APPModuleRef import APPModuleRef
            obj = APPModuleRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # APP
    def MODULESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # APP
    def MODULESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # SDS record types produced and consumed.
    # APP
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from APPDataRef import APPDataRef
            obj = APPDataRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # APP
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # APP
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Upstream sources depended on.
    # APP
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from APPSourceRef import APPSourceRef
            obj = APPSourceRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # APP
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # APP
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # UI pages. Exactly one entry page when nonempty.
    # APP
    def UI(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from APPUIPage import APPUIPage
            obj = APPUIPage()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # APP
    def UILength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # APP
    def UIIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
    # when the manifest was created.
    # APP
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
    # when the manifest was last updated.
    # APP
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def APPStart(builder):
    builder.StartObject(10)

def Start(builder):
    APPStart(builder)

def APPAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    APPAddID(builder, ID)

def APPAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    APPAddNAME(builder, NAME)

def APPAddVERSION(builder, VERSION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VERSION), 0)

def AddVERSION(builder, VERSION):
    APPAddVERSION(builder, VERSION)

def APPAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    APPAddDESCRIPTION(builder, DESCRIPTION)

def APPAddMODULES(builder, MODULES):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(MODULES), 0)

def AddMODULES(builder, MODULES):
    APPAddMODULES(builder, MODULES)

def APPStartMODULESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartMODULESVector(builder, numElems):
    return APPStartMODULESVector(builder, numElems)

def APPCreateMODULESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateMODULESVector(builder, data):
    APPCreateMODULESVector(builder, data)

def APPAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    APPAddDATA(builder, DATA)

def APPStartDATAVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDATAVector(builder, numElems):
    return APPStartDATAVector(builder, numElems)

def APPCreateDATAVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateDATAVector(builder, data):
    APPCreateDATAVector(builder, data)

def APPAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    APPAddSOURCES(builder, SOURCES)

def APPStartSOURCESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCESVector(builder, numElems):
    return APPStartSOURCESVector(builder, numElems)

def APPCreateSOURCESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSOURCESVector(builder, data):
    APPCreateSOURCESVector(builder, data)

def APPAddUI(builder, UI):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(UI), 0)

def AddUI(builder, UI):
    APPAddUI(builder, UI)

def APPStartUIVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartUIVector(builder, numElems):
    return APPStartUIVector(builder, numElems)

def APPCreateUIVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateUIVector(builder, data):
    APPCreateUIVector(builder, data)

def APPAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(CREATED_AT), 0)

def AddCREATED_AT(builder, CREATED_AT):
    APPAddCREATED_AT(builder, CREATED_AT)

def APPAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(UPDATED_AT), 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    APPAddUPDATED_AT(builder, UPDATED_AT)

def APPEnd(builder):
    return builder.EndObject()

def End(builder):
    return APPEnd(builder)

import APPDataRef
import APPModuleRef
import APPSourceRef
import APPUIPage
try:
    from typing import List
except:
    pass

class APPT(object):

    # APPT
    def __init__(
        self,
        ID = None,
        NAME = None,
        VERSION = None,
        DESCRIPTION = None,
        MODULES = None,
        DATA = None,
        SOURCES = None,
        UI = None,
        CREATED_AT = None,
        UPDATED_AT = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.NAME = NAME  # type: Optional[str]
        self.VERSION = VERSION  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.MODULES = MODULES  # type: Optional[List[APPModuleRef.APPModuleRefT]]
        self.DATA = DATA  # type: Optional[List[APPDataRef.APPDataRefT]]
        self.SOURCES = SOURCES  # type: Optional[List[APPSourceRef.APPSourceRefT]]
        self.UI = UI  # type: Optional[List[APPUIPage.APPUIPageT]]
        self.CREATED_AT = CREATED_AT  # type: Optional[str]
        self.UPDATED_AT = UPDATED_AT  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpApp = APP()
        tmpApp.Init(buf, pos)
        return cls.InitFromObj(tmpApp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpApp):
        x = APPT()
        x._UnPack(tmpApp)
        return x

    # APPT
    def _UnPack(self, APP):
        if APP is None:
            return
        self.ID = APP.ID()
        self.NAME = APP.NAME()
        self.VERSION = APP.VERSION()
        self.DESCRIPTION = APP.DESCRIPTION()
        if not APP.MODULESIsNone():
            self.MODULES = []
            for i in range(APP.MODULESLength()):
                if APP.MODULES(i) is None:
                    self.MODULES.append(None)
                else:
                    aPPModuleRef_ = APPModuleRef.APPModuleRefT.InitFromObj(APP.MODULES(i))
                    self.MODULES.append(aPPModuleRef_)
        if not APP.DATAIsNone():
            self.DATA = []
            for i in range(APP.DATALength()):
                if APP.DATA(i) is None:
                    self.DATA.append(None)
                else:
                    aPPDataRef_ = APPDataRef.APPDataRefT.InitFromObj(APP.DATA(i))
                    self.DATA.append(aPPDataRef_)
        if not APP.SOURCESIsNone():
            self.SOURCES = []
            for i in range(APP.SOURCESLength()):
                if APP.SOURCES(i) is None:
                    self.SOURCES.append(None)
                else:
                    aPPSourceRef_ = APPSourceRef.APPSourceRefT.InitFromObj(APP.SOURCES(i))
                    self.SOURCES.append(aPPSourceRef_)
        if not APP.UIIsNone():
            self.UI = []
            for i in range(APP.UILength()):
                if APP.UI(i) is None:
                    self.UI.append(None)
                else:
                    aPPUIPage_ = APPUIPage.APPUIPageT.InitFromObj(APP.UI(i))
                    self.UI.append(aPPUIPage_)
        self.CREATED_AT = APP.CREATED_AT()
        self.UPDATED_AT = APP.UPDATED_AT()

    # APPT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.VERSION is not None:
            VERSION = builder.CreateString(self.VERSION)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.MODULES is not None:
            MODULESlist = []
            for i in range(len(self.MODULES)):
                MODULESlist.append(self.MODULES[i].Pack(builder))
            APPStartMODULESVector(builder, len(self.MODULES))
            for i in reversed(range(len(self.MODULES))):
                builder.PrependUOffsetTRelative(MODULESlist[i])
            MODULES = builder.EndVector()
        if self.DATA is not None:
            DATAlist = []
            for i in range(len(self.DATA)):
                DATAlist.append(self.DATA[i].Pack(builder))
            APPStartDATAVector(builder, len(self.DATA))
            for i in reversed(range(len(self.DATA))):
                builder.PrependUOffsetTRelative(DATAlist[i])
            DATA = builder.EndVector()
        if self.SOURCES is not None:
            SOURCESlist = []
            for i in range(len(self.SOURCES)):
                SOURCESlist.append(self.SOURCES[i].Pack(builder))
            APPStartSOURCESVector(builder, len(self.SOURCES))
            for i in reversed(range(len(self.SOURCES))):
                builder.PrependUOffsetTRelative(SOURCESlist[i])
            SOURCES = builder.EndVector()
        if self.UI is not None:
            UIlist = []
            for i in range(len(self.UI)):
                UIlist.append(self.UI[i].Pack(builder))
            APPStartUIVector(builder, len(self.UI))
            for i in reversed(range(len(self.UI))):
                builder.PrependUOffsetTRelative(UIlist[i])
            UI = builder.EndVector()
        if self.CREATED_AT is not None:
            CREATED_AT = builder.CreateString(self.CREATED_AT)
        if self.UPDATED_AT is not None:
            UPDATED_AT = builder.CreateString(self.UPDATED_AT)
        APPStart(builder)
        if self.ID is not None:
            APPAddID(builder, ID)
        if self.NAME is not None:
            APPAddNAME(builder, NAME)
        if self.VERSION is not None:
            APPAddVERSION(builder, VERSION)
        if self.DESCRIPTION is not None:
            APPAddDESCRIPTION(builder, DESCRIPTION)
        if self.MODULES is not None:
            APPAddMODULES(builder, MODULES)
        if self.DATA is not None:
            APPAddDATA(builder, DATA)
        if self.SOURCES is not None:
            APPAddSOURCES(builder, SOURCES)
        if self.UI is not None:
            APPAddUI(builder, UI)
        if self.CREATED_AT is not None:
            APPAddCREATED_AT(builder, CREATED_AT)
        if self.UPDATED_AT is not None:
            APPAddUPDATED_AT(builder, UPDATED_AT)
        APP = APPEnd(builder)
        return APP
