# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One ratified category in the taxonomy.
#
# DISPLAY_NAME, SUMMARY and SLUG are required: a category cannot be published
# without the label a surface will render, the sentence a browse row will
# show, and the route a link will target. This is what stops each consumer
# from inventing its own wording for the same code.
class CCTCategory(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CCTCategory()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCCTCategory(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CCTCategoryBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x43\x54", size_prefixed=size_prefixed)

    # CCTCategory
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # The ratified category code. This is the join key every consumer uses.
    # CCTCategory
    def CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Canonical human-readable label, rendered verbatim. MUST equal the display
    # name stated in the CODE member's doc comment.
    # CCTCategory
    def DISPLAY_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # One-sentence description shown on browse rows and category headers.
    # CCTCategory
    def SUMMARY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Route-safe identifier: the CODE identifier lowercased with `_` replaced by
    # `-`. Published explicitly rather than derived so every surface routes
    # identically.
    # CCTCategory
    def SLUG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Longer editorial description for a category landing page.
    # CCTCategory
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Parent category for hierarchical browse. UNSPECIFIED means this is a
    # top-level category. A category MUST NOT name itself as its parent.
    # CCTCategory
    def PARENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Presentation order within its parent, ascending. Ties break on
    # DISPLAY_NAME.
    # CCTCategory
    def SORT_ORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Search synonyms and alternate phrasings that resolve to this category.
    # Feeds type-ahead; never rendered as the category label.
    # CCTCategory
    def KEYWORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # CCTCategory
    def KEYWORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CCTCategory
    def KEYWORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Key of a self-hosted icon or capsule asset for this category. A KEY, not a
    # URL: consuming node surfaces load zero external-origin bytes, so the
    # consumer resolves this against its own local asset set.
    # CCTCategory
    def ICON_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def CCTCategoryStart(builder):
    builder.StartObject(9)

def Start(builder):
    CCTCategoryStart(builder)

def CCTCategoryAddCODE(builder, CODE):
    builder.PrependUint8Slot(0, CODE, 0)

def AddCODE(builder, CODE):
    CCTCategoryAddCODE(builder, CODE)

def CCTCategoryAddDISPLAY_NAME(builder, DISPLAY_NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(DISPLAY_NAME), 0)

def AddDISPLAY_NAME(builder, DISPLAY_NAME):
    CCTCategoryAddDISPLAY_NAME(builder, DISPLAY_NAME)

def CCTCategoryAddSUMMARY(builder, SUMMARY):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SUMMARY), 0)

def AddSUMMARY(builder, SUMMARY):
    CCTCategoryAddSUMMARY(builder, SUMMARY)

def CCTCategoryAddSLUG(builder, SLUG):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SLUG), 0)

def AddSLUG(builder, SLUG):
    CCTCategoryAddSLUG(builder, SLUG)

def CCTCategoryAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    CCTCategoryAddDESCRIPTION(builder, DESCRIPTION)

def CCTCategoryAddPARENT(builder, PARENT):
    builder.PrependUint8Slot(5, PARENT, 0)

def AddPARENT(builder, PARENT):
    CCTCategoryAddPARENT(builder, PARENT)

def CCTCategoryAddSORT_ORDER(builder, SORT_ORDER):
    builder.PrependUint16Slot(6, SORT_ORDER, 0)

def AddSORT_ORDER(builder, SORT_ORDER):
    CCTCategoryAddSORT_ORDER(builder, SORT_ORDER)

def CCTCategoryAddKEYWORDS(builder, KEYWORDS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(KEYWORDS), 0)

def AddKEYWORDS(builder, KEYWORDS):
    CCTCategoryAddKEYWORDS(builder, KEYWORDS)

def CCTCategoryStartKEYWORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartKEYWORDSVector(builder, numElems):
    return CCTCategoryStartKEYWORDSVector(builder, numElems)

def CCTCategoryCreateKEYWORDSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateKEYWORDSVector(builder, data):
    CCTCategoryCreateKEYWORDSVector(builder, data)

def CCTCategoryAddICON_KEY(builder, ICON_KEY):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(ICON_KEY), 0)

def AddICON_KEY(builder, ICON_KEY):
    CCTCategoryAddICON_KEY(builder, ICON_KEY)

def CCTCategoryEnd(builder):
    return builder.EndObject()

def End(builder):
    return CCTCategoryEnd(builder)

try:
    from typing import List
except:
    pass

class CCTCategoryT(object):

    # CCTCategoryT
    def __init__(
        self,
        CODE = 0,
        DISPLAY_NAME = None,
        SUMMARY = None,
        SLUG = None,
        DESCRIPTION = None,
        PARENT = 0,
        SORT_ORDER = 0,
        KEYWORDS = None,
        ICON_KEY = None,
    ):
        self.CODE = CODE  # type: int
        self.DISPLAY_NAME = DISPLAY_NAME  # type: Optional[str]
        self.SUMMARY = SUMMARY  # type: Optional[str]
        self.SLUG = SLUG  # type: Optional[str]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]
        self.PARENT = PARENT  # type: int
        self.SORT_ORDER = SORT_ORDER  # type: int
        self.KEYWORDS = KEYWORDS  # type: Optional[List[Optional[str]]]
        self.ICON_KEY = ICON_KEY  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCctcategory = CCTCategory()
        tmpCctcategory.Init(buf, pos)
        return cls.InitFromObj(tmpCctcategory)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCctcategory):
        x = CCTCategoryT()
        x._UnPack(tmpCctcategory)
        return x

    # CCTCategoryT
    def _UnPack(self, CCTCategory):
        if CCTCategory is None:
            return
        self.CODE = CCTCategory.CODE()
        self.DISPLAY_NAME = CCTCategory.DISPLAY_NAME()
        self.SUMMARY = CCTCategory.SUMMARY()
        self.SLUG = CCTCategory.SLUG()
        self.DESCRIPTION = CCTCategory.DESCRIPTION()
        self.PARENT = CCTCategory.PARENT()
        self.SORT_ORDER = CCTCategory.SORT_ORDER()
        if not CCTCategory.KEYWORDSIsNone():
            self.KEYWORDS = []
            for i in range(CCTCategory.KEYWORDSLength()):
                self.KEYWORDS.append(CCTCategory.KEYWORDS(i))
        self.ICON_KEY = CCTCategory.ICON_KEY()

    # CCTCategoryT
    def Pack(self, builder):
        if self.DISPLAY_NAME is not None:
            DISPLAY_NAME = builder.CreateString(self.DISPLAY_NAME)
        if self.SUMMARY is not None:
            SUMMARY = builder.CreateString(self.SUMMARY)
        if self.SLUG is not None:
            SLUG = builder.CreateString(self.SLUG)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        if self.KEYWORDS is not None:
            KEYWORDSlist = []
            for i in range(len(self.KEYWORDS)):
                KEYWORDSlist.append(builder.CreateString(self.KEYWORDS[i]))
            CCTCategoryStartKEYWORDSVector(builder, len(self.KEYWORDS))
            for i in reversed(range(len(self.KEYWORDS))):
                builder.PrependUOffsetTRelative(KEYWORDSlist[i])
            KEYWORDS = builder.EndVector()
        if self.ICON_KEY is not None:
            ICON_KEY = builder.CreateString(self.ICON_KEY)
        CCTCategoryStart(builder)
        CCTCategoryAddCODE(builder, self.CODE)
        if self.DISPLAY_NAME is not None:
            CCTCategoryAddDISPLAY_NAME(builder, DISPLAY_NAME)
        if self.SUMMARY is not None:
            CCTCategoryAddSUMMARY(builder, SUMMARY)
        if self.SLUG is not None:
            CCTCategoryAddSLUG(builder, SLUG)
        if self.DESCRIPTION is not None:
            CCTCategoryAddDESCRIPTION(builder, DESCRIPTION)
        CCTCategoryAddPARENT(builder, self.PARENT)
        CCTCategoryAddSORT_ORDER(builder, self.SORT_ORDER)
        if self.KEYWORDS is not None:
            CCTCategoryAddKEYWORDS(builder, KEYWORDS)
        if self.ICON_KEY is not None:
            CCTCategoryAddICON_KEY(builder, ICON_KEY)
        CCTCategory = CCTCategoryEnd(builder)
        return CCTCategory
