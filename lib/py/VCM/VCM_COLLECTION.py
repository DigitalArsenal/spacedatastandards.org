# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Collection of VCM records
class VCM_COLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VCM_COLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVCM_COLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # VCM_COLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # VCM_COLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from VCM import VCM
            obj = VCM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VCM_COLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VCM_COLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def VCM_COLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    VCM_COLLECTIONStart(builder)

def VCM_COLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    VCM_COLLECTIONAddRECORDS(builder, RECORDS)

def VCM_COLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return VCM_COLLECTIONStartRECORDSVector(builder, numElems)

def VCM_COLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return VCM_COLLECTIONEnd(builder)

import VCM
try:
    from typing import List
except:
    pass

class VCM_COLLECTIONT(object):

    # VCM_COLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[VCM.VCMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        VCM_COLLECTION = VCM_COLLECTION()
        VCM_COLLECTION.Init(buf, pos)
        return cls.InitFromObj(VCM_COLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, VCM_COLLECTION):
        x = VCM_COLLECTIONT()
        x._UnPack(VCM_COLLECTION)
        return x

    # VCM_COLLECTIONT
    def _UnPack(self, VCM_COLLECTION):
        if VCM_COLLECTION is None:
            return
        if not VCM_COLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(VCM_COLLECTION.RECORDSLength()):
                if VCM_COLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    vCM_ = VCM.VCMT.InitFromObj(VCM_COLLECTION.RECORDS(i))
                    self.RECORDS.append(vCM_)

    # VCM_COLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            VCM_COLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        VCM_COLLECTIONStart(builder)
        if self.RECORDS is not None:
            VCM_COLLECTIONAddRECORDS(builder, RECORDS)
        VCM_COLLECTION = VCM_COLLECTIONEnd(builder)
        return VCM_COLLECTION
