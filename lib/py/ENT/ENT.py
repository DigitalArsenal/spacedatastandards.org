# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Entitlement - Provider subscription or account entitlement state.
#
# This is durable operational state for a provider account, not a plugin
# license key. It records the current authoritative entitlement for an SDN
# account identity and can be projected into subscription, billing, and
# access-control surfaces.
class ENT(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ENT()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsENT(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ENTBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x4E\x54", size_prefixed=size_prefixed)

    # ENT
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable entitlement identifier.
    # ENT
    def ENTITLEMENT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Account identity or wallet xpub.
    # ENT
    def XPUB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Current SDN peer ID bound to the entitlement.
    # ENT
    def PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider plan code, e.g. "free", "explorer", or "enterprise".
    # ENT
    def PLAN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Structured entitlement lifecycle status.
    # ENT
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Original provider status string when finer detail is needed.
    # ENT
    def STATUS_TEXT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Fiat processor customer identifier.
    # ENT
    def STRIPE_CUSTOMER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Fiat processor subscription identifier.
    # ENT
    def STRIPE_SUBSCRIPTION_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Unix timestamp when the entitlement expires (0 = no expiry).
    # ENT
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix timestamp when the entitlement was created.
    # ENT
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix timestamp when the entitlement was last updated.
    # ENT
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Peer ID of the entitlement issuer/provider.
    # ENT
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider signature over the entitlement record.
    # ENT
    def PROVIDER_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # ENT
    def PROVIDER_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # ENT
    def PROVIDER_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ENT
    def PROVIDER_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

def ENTStart(builder):
    builder.StartObject(13)

def Start(builder):
    ENTStart(builder)

def ENTAddENTITLEMENT_ID(builder, ENTITLEMENT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ENTITLEMENT_ID), 0)

def AddENTITLEMENT_ID(builder, ENTITLEMENT_ID):
    ENTAddENTITLEMENT_ID(builder, ENTITLEMENT_ID)

def ENTAddXPUB(builder, XPUB):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(XPUB), 0)

def AddXPUB(builder, XPUB):
    ENTAddXPUB(builder, XPUB)

def ENTAddPEER_ID(builder, PEER_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PEER_ID), 0)

def AddPEER_ID(builder, PEER_ID):
    ENTAddPEER_ID(builder, PEER_ID)

def ENTAddPLAN(builder, PLAN):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(PLAN), 0)

def AddPLAN(builder, PLAN):
    ENTAddPLAN(builder, PLAN)

def ENTAddSTATUS(builder, STATUS):
    builder.PrependInt8Slot(4, STATUS, 0)

def AddSTATUS(builder, STATUS):
    ENTAddSTATUS(builder, STATUS)

def ENTAddSTATUS_TEXT(builder, STATUS_TEXT):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(STATUS_TEXT), 0)

def AddSTATUS_TEXT(builder, STATUS_TEXT):
    ENTAddSTATUS_TEXT(builder, STATUS_TEXT)

def ENTAddSTRIPE_CUSTOMER_ID(builder, STRIPE_CUSTOMER_ID):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(STRIPE_CUSTOMER_ID), 0)

def AddSTRIPE_CUSTOMER_ID(builder, STRIPE_CUSTOMER_ID):
    ENTAddSTRIPE_CUSTOMER_ID(builder, STRIPE_CUSTOMER_ID)

def ENTAddSTRIPE_SUBSCRIPTION_ID(builder, STRIPE_SUBSCRIPTION_ID):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(STRIPE_SUBSCRIPTION_ID), 0)

def AddSTRIPE_SUBSCRIPTION_ID(builder, STRIPE_SUBSCRIPTION_ID):
    ENTAddSTRIPE_SUBSCRIPTION_ID(builder, STRIPE_SUBSCRIPTION_ID)

def ENTAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(8, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    ENTAddEXPIRES_AT(builder, EXPIRES_AT)

def ENTAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUint64Slot(9, CREATED_AT, 0)

def AddCREATED_AT(builder, CREATED_AT):
    ENTAddCREATED_AT(builder, CREATED_AT)

def ENTAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUint64Slot(10, UPDATED_AT, 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    ENTAddUPDATED_AT(builder, UPDATED_AT)

def ENTAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    ENTAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def ENTAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_SIGNATURE), 0)

def AddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    ENTAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)

def ENTStartPROVIDER_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPROVIDER_SIGNATUREVector(builder, numElems):
    return ENTStartPROVIDER_SIGNATUREVector(builder, numElems)

def ENTCreatePROVIDER_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePROVIDER_SIGNATUREVector(builder, data):
    ENTCreatePROVIDER_SIGNATUREVector(builder, data)

def ENTEnd(builder):
    return builder.EndObject()

def End(builder):
    return ENTEnd(builder)

try:
    from typing import List
except:
    pass

class ENTT(object):

    # ENTT
    def __init__(
        self,
        ENTITLEMENT_ID = None,
        XPUB = None,
        PEER_ID = None,
        PLAN = None,
        STATUS = 0,
        STATUS_TEXT = None,
        STRIPE_CUSTOMER_ID = None,
        STRIPE_SUBSCRIPTION_ID = None,
        EXPIRES_AT = 0,
        CREATED_AT = 0,
        UPDATED_AT = 0,
        PROVIDER_PEER_ID = None,
        PROVIDER_SIGNATURE = None,
    ):
        self.ENTITLEMENT_ID = ENTITLEMENT_ID  # type: Optional[str]
        self.XPUB = XPUB  # type: Optional[str]
        self.PEER_ID = PEER_ID  # type: Optional[str]
        self.PLAN = PLAN  # type: Optional[str]
        self.STATUS = STATUS  # type: int
        self.STATUS_TEXT = STATUS_TEXT  # type: Optional[str]
        self.STRIPE_CUSTOMER_ID = STRIPE_CUSTOMER_ID  # type: Optional[str]
        self.STRIPE_SUBSCRIPTION_ID = STRIPE_SUBSCRIPTION_ID  # type: Optional[str]
        self.EXPIRES_AT = EXPIRES_AT  # type: int
        self.CREATED_AT = CREATED_AT  # type: int
        self.UPDATED_AT = UPDATED_AT  # type: int
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpEnt = ENT()
        tmpEnt.Init(buf, pos)
        return cls.InitFromObj(tmpEnt)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpEnt):
        x = ENTT()
        x._UnPack(tmpEnt)
        return x

    # ENTT
    def _UnPack(self, ENT):
        if ENT is None:
            return
        self.ENTITLEMENT_ID = ENT.ENTITLEMENT_ID()
        self.XPUB = ENT.XPUB()
        self.PEER_ID = ENT.PEER_ID()
        self.PLAN = ENT.PLAN()
        self.STATUS = ENT.STATUS()
        self.STATUS_TEXT = ENT.STATUS_TEXT()
        self.STRIPE_CUSTOMER_ID = ENT.STRIPE_CUSTOMER_ID()
        self.STRIPE_SUBSCRIPTION_ID = ENT.STRIPE_SUBSCRIPTION_ID()
        self.EXPIRES_AT = ENT.EXPIRES_AT()
        self.CREATED_AT = ENT.CREATED_AT()
        self.UPDATED_AT = ENT.UPDATED_AT()
        self.PROVIDER_PEER_ID = ENT.PROVIDER_PEER_ID()
        if not ENT.PROVIDER_SIGNATUREIsNone():
            if np is None:
                self.PROVIDER_SIGNATURE = []
                for i in range(ENT.PROVIDER_SIGNATURELength()):
                    self.PROVIDER_SIGNATURE.append(ENT.PROVIDER_SIGNATURE(i))
            else:
                self.PROVIDER_SIGNATURE = ENT.PROVIDER_SIGNATUREAsNumpy()

    # ENTT
    def Pack(self, builder):
        if self.ENTITLEMENT_ID is not None:
            ENTITLEMENT_ID = builder.CreateString(self.ENTITLEMENT_ID)
        if self.XPUB is not None:
            XPUB = builder.CreateString(self.XPUB)
        if self.PEER_ID is not None:
            PEER_ID = builder.CreateString(self.PEER_ID)
        if self.PLAN is not None:
            PLAN = builder.CreateString(self.PLAN)
        if self.STATUS_TEXT is not None:
            STATUS_TEXT = builder.CreateString(self.STATUS_TEXT)
        if self.STRIPE_CUSTOMER_ID is not None:
            STRIPE_CUSTOMER_ID = builder.CreateString(self.STRIPE_CUSTOMER_ID)
        if self.STRIPE_SUBSCRIPTION_ID is not None:
            STRIPE_SUBSCRIPTION_ID = builder.CreateString(self.STRIPE_SUBSCRIPTION_ID)
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.PROVIDER_SIGNATURE is not None:
            if np is not None and type(self.PROVIDER_SIGNATURE) is np.ndarray:
                PROVIDER_SIGNATURE = builder.CreateNumpyVector(self.PROVIDER_SIGNATURE)
            else:
                ENTStartPROVIDER_SIGNATUREVector(builder, len(self.PROVIDER_SIGNATURE))
                for i in reversed(range(len(self.PROVIDER_SIGNATURE))):
                    builder.PrependUint8(self.PROVIDER_SIGNATURE[i])
                PROVIDER_SIGNATURE = builder.EndVector()
        ENTStart(builder)
        if self.ENTITLEMENT_ID is not None:
            ENTAddENTITLEMENT_ID(builder, ENTITLEMENT_ID)
        if self.XPUB is not None:
            ENTAddXPUB(builder, XPUB)
        if self.PEER_ID is not None:
            ENTAddPEER_ID(builder, PEER_ID)
        if self.PLAN is not None:
            ENTAddPLAN(builder, PLAN)
        ENTAddSTATUS(builder, self.STATUS)
        if self.STATUS_TEXT is not None:
            ENTAddSTATUS_TEXT(builder, STATUS_TEXT)
        if self.STRIPE_CUSTOMER_ID is not None:
            ENTAddSTRIPE_CUSTOMER_ID(builder, STRIPE_CUSTOMER_ID)
        if self.STRIPE_SUBSCRIPTION_ID is not None:
            ENTAddSTRIPE_SUBSCRIPTION_ID(builder, STRIPE_SUBSCRIPTION_ID)
        ENTAddEXPIRES_AT(builder, self.EXPIRES_AT)
        ENTAddCREATED_AT(builder, self.CREATED_AT)
        ENTAddUPDATED_AT(builder, self.UPDATED_AT)
        if self.PROVIDER_PEER_ID is not None:
            ENTAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.PROVIDER_SIGNATURE is not None:
            ENTAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)
        ENT = ENTEnd(builder)
        return ENT
