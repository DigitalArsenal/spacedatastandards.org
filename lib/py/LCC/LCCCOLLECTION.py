# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class LCCCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LCCCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLCCCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def LCCCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4C\x43\x43", size_prefixed=size_prefixed)

    # LCCCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # LCCCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from LCC import LCC
            obj = LCC()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # LCCCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LCCCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def LCCCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    LCCCOLLECTIONStart(builder)

def LCCCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    LCCCOLLECTIONAddRECORDS(builder, RECORDS)

def LCCCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return LCCCOLLECTIONStartRECORDSVector(builder, numElems)

def LCCCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return LCCCOLLECTIONEnd(builder)

import LCC
try:
    from typing import List
except:
    pass

class LCCCOLLECTIONT(object):

    # LCCCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[LCC.LCCT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        LCCCOLLECTION = LCCCOLLECTION()
        LCCCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(LCCCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, LCCCOLLECTION):
        x = LCCCOLLECTIONT()
        x._UnPack(LCCCOLLECTION)
        return x

    # LCCCOLLECTIONT
    def _UnPack(self, LCCCOLLECTION):
        if LCCCOLLECTION is None:
            return
        if not LCCCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(LCCCOLLECTION.RECORDSLength()):
                if LCCCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    lCC_ = LCC.LCCT.InitFromObj(LCCCOLLECTION.RECORDS(i))
                    self.RECORDS.append(lCC_)

    # LCCCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            LCCCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        LCCCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            LCCCOLLECTIONAddRECORDS(builder, RECORDS)
        LCCCOLLECTION = LCCCOLLECTIONEnd(builder)
        return LCCCOLLECTION
