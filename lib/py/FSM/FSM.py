# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class FSM(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FSM()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFSM(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FSMBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x46\x53\x4D", size_prefixed=size_prefixed)

    # FSM
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FSM
    def MESSAGE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def LISTING_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def STREAM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def SCHEMA_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def SCHEMA_HASH(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSM
    def SCHEMA_HASHAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSM
    def SCHEMA_HASHLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSM
    def SCHEMA_HASHIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # FSM
    def POLICY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def POLICY_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 1

    # FSM
    def KEY_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def SEQUENCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSM
    def PRODUCED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSM
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FSM
    def SUBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # FSM
    def FIELDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FieldStreamValue import FieldStreamValue
            obj = FieldStreamValue()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FSM
    def FIELDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSM
    def FIELDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        return o == 0

    # FSM
    def PAYLOAD_HASH(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSM
    def PAYLOAD_HASHAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSM
    def PAYLOAD_HASHLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSM
    def PAYLOAD_HASHIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

    # FSM
    def PREVIOUS_MESSAGE_HASH(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSM
    def PREVIOUS_MESSAGE_HASHAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSM
    def PREVIOUS_MESSAGE_HASHLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSM
    def PREVIOUS_MESSAGE_HASHIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        return o == 0

    # FSM
    def PROVIDER_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # FSM
    def PROVIDER_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # FSM
    def PROVIDER_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FSM
    def PROVIDER_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        return o == 0

def FSMStart(builder):
    builder.StartObject(17)

def Start(builder):
    FSMStart(builder)

def FSMAddMESSAGE_ID(builder, MESSAGE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(MESSAGE_ID), 0)

def AddMESSAGE_ID(builder, MESSAGE_ID):
    FSMAddMESSAGE_ID(builder, MESSAGE_ID)

def FSMAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    FSMAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def FSMAddLISTING_ID(builder, LISTING_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(LISTING_ID), 0)

def AddLISTING_ID(builder, LISTING_ID):
    FSMAddLISTING_ID(builder, LISTING_ID)

def FSMAddSTREAM_ID(builder, STREAM_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(STREAM_ID), 0)

def AddSTREAM_ID(builder, STREAM_ID):
    FSMAddSTREAM_ID(builder, STREAM_ID)

def FSMAddSCHEMA_CODE(builder, SCHEMA_CODE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_CODE), 0)

def AddSCHEMA_CODE(builder, SCHEMA_CODE):
    FSMAddSCHEMA_CODE(builder, SCHEMA_CODE)

def FSMAddSCHEMA_HASH(builder, SCHEMA_HASH):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SCHEMA_HASH), 0)

def AddSCHEMA_HASH(builder, SCHEMA_HASH):
    FSMAddSCHEMA_HASH(builder, SCHEMA_HASH)

def FSMStartSCHEMA_HASHVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSCHEMA_HASHVector(builder, numElems):
    return FSMStartSCHEMA_HASHVector(builder, numElems)

def FSMCreateSCHEMA_HASHVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateSCHEMA_HASHVector(builder, data):
    FSMCreateSCHEMA_HASHVector(builder, data)

def FSMAddPOLICY_ID(builder, POLICY_ID):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(POLICY_ID), 0)

def AddPOLICY_ID(builder, POLICY_ID):
    FSMAddPOLICY_ID(builder, POLICY_ID)

def FSMAddPOLICY_VERSION(builder, POLICY_VERSION):
    builder.PrependUint32Slot(7, POLICY_VERSION, 1)

def AddPOLICY_VERSION(builder, POLICY_VERSION):
    FSMAddPOLICY_VERSION(builder, POLICY_VERSION)

def FSMAddKEY_EPOCH(builder, KEY_EPOCH):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(KEY_EPOCH), 0)

def AddKEY_EPOCH(builder, KEY_EPOCH):
    FSMAddKEY_EPOCH(builder, KEY_EPOCH)

def FSMAddSEQUENCE(builder, SEQUENCE):
    builder.PrependUint64Slot(9, SEQUENCE, 0)

def AddSEQUENCE(builder, SEQUENCE):
    FSMAddSEQUENCE(builder, SEQUENCE)

def FSMAddPRODUCED_AT(builder, PRODUCED_AT):
    builder.PrependUint64Slot(10, PRODUCED_AT, 0)

def AddPRODUCED_AT(builder, PRODUCED_AT):
    FSMAddPRODUCED_AT(builder, PRODUCED_AT)

def FSMAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(11, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    FSMAddEXPIRES_AT(builder, EXPIRES_AT)

def FSMAddSUBJECT_ID(builder, SUBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(SUBJECT_ID), 0)

def AddSUBJECT_ID(builder, SUBJECT_ID):
    FSMAddSUBJECT_ID(builder, SUBJECT_ID)

def FSMAddFIELDS(builder, FIELDS):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(FIELDS), 0)

def AddFIELDS(builder, FIELDS):
    FSMAddFIELDS(builder, FIELDS)

def FSMStartFIELDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartFIELDSVector(builder, numElems):
    return FSMStartFIELDSVector(builder, numElems)

def FSMCreateFIELDSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateFIELDSVector(builder, data):
    FSMCreateFIELDSVector(builder, data)

def FSMAddPAYLOAD_HASH(builder, PAYLOAD_HASH):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD_HASH), 0)

def AddPAYLOAD_HASH(builder, PAYLOAD_HASH):
    FSMAddPAYLOAD_HASH(builder, PAYLOAD_HASH)

def FSMStartPAYLOAD_HASHVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPAYLOAD_HASHVector(builder, numElems):
    return FSMStartPAYLOAD_HASHVector(builder, numElems)

def FSMCreatePAYLOAD_HASHVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePAYLOAD_HASHVector(builder, data):
    FSMCreatePAYLOAD_HASHVector(builder, data)

def FSMAddPREVIOUS_MESSAGE_HASH(builder, PREVIOUS_MESSAGE_HASH):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(PREVIOUS_MESSAGE_HASH), 0)

def AddPREVIOUS_MESSAGE_HASH(builder, PREVIOUS_MESSAGE_HASH):
    FSMAddPREVIOUS_MESSAGE_HASH(builder, PREVIOUS_MESSAGE_HASH)

def FSMStartPREVIOUS_MESSAGE_HASHVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPREVIOUS_MESSAGE_HASHVector(builder, numElems):
    return FSMStartPREVIOUS_MESSAGE_HASHVector(builder, numElems)

def FSMCreatePREVIOUS_MESSAGE_HASHVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePREVIOUS_MESSAGE_HASHVector(builder, data):
    FSMCreatePREVIOUS_MESSAGE_HASHVector(builder, data)

def FSMAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_SIGNATURE), 0)

def AddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    FSMAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)

def FSMStartPROVIDER_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPROVIDER_SIGNATUREVector(builder, numElems):
    return FSMStartPROVIDER_SIGNATUREVector(builder, numElems)

def FSMCreatePROVIDER_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePROVIDER_SIGNATUREVector(builder, data):
    FSMCreatePROVIDER_SIGNATUREVector(builder, data)

def FSMEnd(builder):
    return builder.EndObject()

def End(builder):
    return FSMEnd(builder)

import FieldStreamValue
try:
    from typing import List
except:
    pass

class FSMT(object):

    # FSMT
    def __init__(
        self,
        MESSAGE_ID = None,
        PROVIDER_PEER_ID = None,
        LISTING_ID = None,
        STREAM_ID = None,
        SCHEMA_CODE = None,
        SCHEMA_HASH = None,
        POLICY_ID = None,
        POLICY_VERSION = 1,
        KEY_EPOCH = None,
        SEQUENCE = 0,
        PRODUCED_AT = 0,
        EXPIRES_AT = 0,
        SUBJECT_ID = None,
        FIELDS = None,
        PAYLOAD_HASH = None,
        PREVIOUS_MESSAGE_HASH = None,
        PROVIDER_SIGNATURE = None,
    ):
        self.MESSAGE_ID = MESSAGE_ID  # type: Optional[str]
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.LISTING_ID = LISTING_ID  # type: Optional[str]
        self.STREAM_ID = STREAM_ID  # type: Optional[str]
        self.SCHEMA_CODE = SCHEMA_CODE  # type: Optional[str]
        self.SCHEMA_HASH = SCHEMA_HASH  # type: Optional[List[int]]
        self.POLICY_ID = POLICY_ID  # type: Optional[str]
        self.POLICY_VERSION = POLICY_VERSION  # type: int
        self.KEY_EPOCH = KEY_EPOCH  # type: Optional[str]
        self.SEQUENCE = SEQUENCE  # type: int
        self.PRODUCED_AT = PRODUCED_AT  # type: int
        self.EXPIRES_AT = EXPIRES_AT  # type: int
        self.SUBJECT_ID = SUBJECT_ID  # type: Optional[str]
        self.FIELDS = FIELDS  # type: Optional[List[FieldStreamValue.FieldStreamValueT]]
        self.PAYLOAD_HASH = PAYLOAD_HASH  # type: Optional[List[int]]
        self.PREVIOUS_MESSAGE_HASH = PREVIOUS_MESSAGE_HASH  # type: Optional[List[int]]
        self.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpFsm = FSM()
        tmpFsm.Init(buf, pos)
        return cls.InitFromObj(tmpFsm)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpFsm):
        x = FSMT()
        x._UnPack(tmpFsm)
        return x

    # FSMT
    def _UnPack(self, FSM):
        if FSM is None:
            return
        self.MESSAGE_ID = FSM.MESSAGE_ID()
        self.PROVIDER_PEER_ID = FSM.PROVIDER_PEER_ID()
        self.LISTING_ID = FSM.LISTING_ID()
        self.STREAM_ID = FSM.STREAM_ID()
        self.SCHEMA_CODE = FSM.SCHEMA_CODE()
        if not FSM.SCHEMA_HASHIsNone():
            if np is None:
                self.SCHEMA_HASH = []
                for i in range(FSM.SCHEMA_HASHLength()):
                    self.SCHEMA_HASH.append(FSM.SCHEMA_HASH(i))
            else:
                self.SCHEMA_HASH = FSM.SCHEMA_HASHAsNumpy()
        self.POLICY_ID = FSM.POLICY_ID()
        self.POLICY_VERSION = FSM.POLICY_VERSION()
        self.KEY_EPOCH = FSM.KEY_EPOCH()
        self.SEQUENCE = FSM.SEQUENCE()
        self.PRODUCED_AT = FSM.PRODUCED_AT()
        self.EXPIRES_AT = FSM.EXPIRES_AT()
        self.SUBJECT_ID = FSM.SUBJECT_ID()
        if not FSM.FIELDSIsNone():
            self.FIELDS = []
            for i in range(FSM.FIELDSLength()):
                if FSM.FIELDS(i) is None:
                    self.FIELDS.append(None)
                else:
                    fieldStreamValue_ = FieldStreamValue.FieldStreamValueT.InitFromObj(FSM.FIELDS(i))
                    self.FIELDS.append(fieldStreamValue_)
        if not FSM.PAYLOAD_HASHIsNone():
            if np is None:
                self.PAYLOAD_HASH = []
                for i in range(FSM.PAYLOAD_HASHLength()):
                    self.PAYLOAD_HASH.append(FSM.PAYLOAD_HASH(i))
            else:
                self.PAYLOAD_HASH = FSM.PAYLOAD_HASHAsNumpy()
        if not FSM.PREVIOUS_MESSAGE_HASHIsNone():
            if np is None:
                self.PREVIOUS_MESSAGE_HASH = []
                for i in range(FSM.PREVIOUS_MESSAGE_HASHLength()):
                    self.PREVIOUS_MESSAGE_HASH.append(FSM.PREVIOUS_MESSAGE_HASH(i))
            else:
                self.PREVIOUS_MESSAGE_HASH = FSM.PREVIOUS_MESSAGE_HASHAsNumpy()
        if not FSM.PROVIDER_SIGNATUREIsNone():
            if np is None:
                self.PROVIDER_SIGNATURE = []
                for i in range(FSM.PROVIDER_SIGNATURELength()):
                    self.PROVIDER_SIGNATURE.append(FSM.PROVIDER_SIGNATURE(i))
            else:
                self.PROVIDER_SIGNATURE = FSM.PROVIDER_SIGNATUREAsNumpy()

    # FSMT
    def Pack(self, builder):
        if self.MESSAGE_ID is not None:
            MESSAGE_ID = builder.CreateString(self.MESSAGE_ID)
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.LISTING_ID is not None:
            LISTING_ID = builder.CreateString(self.LISTING_ID)
        if self.STREAM_ID is not None:
            STREAM_ID = builder.CreateString(self.STREAM_ID)
        if self.SCHEMA_CODE is not None:
            SCHEMA_CODE = builder.CreateString(self.SCHEMA_CODE)
        if self.SCHEMA_HASH is not None:
            if np is not None and type(self.SCHEMA_HASH) is np.ndarray:
                SCHEMA_HASH = builder.CreateNumpyVector(self.SCHEMA_HASH)
            else:
                FSMStartSCHEMA_HASHVector(builder, len(self.SCHEMA_HASH))
                for i in reversed(range(len(self.SCHEMA_HASH))):
                    builder.PrependUint8(self.SCHEMA_HASH[i])
                SCHEMA_HASH = builder.EndVector()
        if self.POLICY_ID is not None:
            POLICY_ID = builder.CreateString(self.POLICY_ID)
        if self.KEY_EPOCH is not None:
            KEY_EPOCH = builder.CreateString(self.KEY_EPOCH)
        if self.SUBJECT_ID is not None:
            SUBJECT_ID = builder.CreateString(self.SUBJECT_ID)
        if self.FIELDS is not None:
            FIELDSlist = []
            for i in range(len(self.FIELDS)):
                FIELDSlist.append(self.FIELDS[i].Pack(builder))
            FSMStartFIELDSVector(builder, len(self.FIELDS))
            for i in reversed(range(len(self.FIELDS))):
                builder.PrependUOffsetTRelative(FIELDSlist[i])
            FIELDS = builder.EndVector()
        if self.PAYLOAD_HASH is not None:
            if np is not None and type(self.PAYLOAD_HASH) is np.ndarray:
                PAYLOAD_HASH = builder.CreateNumpyVector(self.PAYLOAD_HASH)
            else:
                FSMStartPAYLOAD_HASHVector(builder, len(self.PAYLOAD_HASH))
                for i in reversed(range(len(self.PAYLOAD_HASH))):
                    builder.PrependUint8(self.PAYLOAD_HASH[i])
                PAYLOAD_HASH = builder.EndVector()
        if self.PREVIOUS_MESSAGE_HASH is not None:
            if np is not None and type(self.PREVIOUS_MESSAGE_HASH) is np.ndarray:
                PREVIOUS_MESSAGE_HASH = builder.CreateNumpyVector(self.PREVIOUS_MESSAGE_HASH)
            else:
                FSMStartPREVIOUS_MESSAGE_HASHVector(builder, len(self.PREVIOUS_MESSAGE_HASH))
                for i in reversed(range(len(self.PREVIOUS_MESSAGE_HASH))):
                    builder.PrependUint8(self.PREVIOUS_MESSAGE_HASH[i])
                PREVIOUS_MESSAGE_HASH = builder.EndVector()
        if self.PROVIDER_SIGNATURE is not None:
            if np is not None and type(self.PROVIDER_SIGNATURE) is np.ndarray:
                PROVIDER_SIGNATURE = builder.CreateNumpyVector(self.PROVIDER_SIGNATURE)
            else:
                FSMStartPROVIDER_SIGNATUREVector(builder, len(self.PROVIDER_SIGNATURE))
                for i in reversed(range(len(self.PROVIDER_SIGNATURE))):
                    builder.PrependUint8(self.PROVIDER_SIGNATURE[i])
                PROVIDER_SIGNATURE = builder.EndVector()
        FSMStart(builder)
        if self.MESSAGE_ID is not None:
            FSMAddMESSAGE_ID(builder, MESSAGE_ID)
        if self.PROVIDER_PEER_ID is not None:
            FSMAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.LISTING_ID is not None:
            FSMAddLISTING_ID(builder, LISTING_ID)
        if self.STREAM_ID is not None:
            FSMAddSTREAM_ID(builder, STREAM_ID)
        if self.SCHEMA_CODE is not None:
            FSMAddSCHEMA_CODE(builder, SCHEMA_CODE)
        if self.SCHEMA_HASH is not None:
            FSMAddSCHEMA_HASH(builder, SCHEMA_HASH)
        if self.POLICY_ID is not None:
            FSMAddPOLICY_ID(builder, POLICY_ID)
        FSMAddPOLICY_VERSION(builder, self.POLICY_VERSION)
        if self.KEY_EPOCH is not None:
            FSMAddKEY_EPOCH(builder, KEY_EPOCH)
        FSMAddSEQUENCE(builder, self.SEQUENCE)
        FSMAddPRODUCED_AT(builder, self.PRODUCED_AT)
        FSMAddEXPIRES_AT(builder, self.EXPIRES_AT)
        if self.SUBJECT_ID is not None:
            FSMAddSUBJECT_ID(builder, SUBJECT_ID)
        if self.FIELDS is not None:
            FSMAddFIELDS(builder, FIELDS)
        if self.PAYLOAD_HASH is not None:
            FSMAddPAYLOAD_HASH(builder, PAYLOAD_HASH)
        if self.PREVIOUS_MESSAGE_HASH is not None:
            FSMAddPREVIOUS_MESSAGE_HASH(builder, PREVIOUS_MESSAGE_HASH)
        if self.PROVIDER_SIGNATURE is not None:
            FSMAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)
        FSM = FSMEnd(builder)
        return FSM
