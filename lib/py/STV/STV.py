# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# State Vector
class STV(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = STV()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSTV(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def STVBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x54\x56", size_prefixed=size_prefixed)

    # STV
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # STV
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # STV
    def POS_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # STV
    def POS_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # STV
    def POS_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # STV
    def VEL_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # STV
    def VEL_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # STV
    def VEL_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # STV
    def REF_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # STV
    def FLAGS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # STV
    def RESERVED(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # STV
    def RESERVEDAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # STV
    def RESERVEDLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # STV
    def RESERVEDIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

def STVStart(builder):
    builder.StartObject(10)

def Start(builder):
    STVStart(builder)

def STVAddEPOCH(builder, EPOCH):
    builder.PrependFloat64Slot(0, EPOCH, 0.0)

def AddEPOCH(builder, EPOCH):
    STVAddEPOCH(builder, EPOCH)

def STVAddPOS_X(builder, POS_X):
    builder.PrependFloat64Slot(1, POS_X, 0.0)

def AddPOS_X(builder, POS_X):
    STVAddPOS_X(builder, POS_X)

def STVAddPOS_Y(builder, POS_Y):
    builder.PrependFloat64Slot(2, POS_Y, 0.0)

def AddPOS_Y(builder, POS_Y):
    STVAddPOS_Y(builder, POS_Y)

def STVAddPOS_Z(builder, POS_Z):
    builder.PrependFloat64Slot(3, POS_Z, 0.0)

def AddPOS_Z(builder, POS_Z):
    STVAddPOS_Z(builder, POS_Z)

def STVAddVEL_X(builder, VEL_X):
    builder.PrependFloat64Slot(4, VEL_X, 0.0)

def AddVEL_X(builder, VEL_X):
    STVAddVEL_X(builder, VEL_X)

def STVAddVEL_Y(builder, VEL_Y):
    builder.PrependFloat64Slot(5, VEL_Y, 0.0)

def AddVEL_Y(builder, VEL_Y):
    STVAddVEL_Y(builder, VEL_Y)

def STVAddVEL_Z(builder, VEL_Z):
    builder.PrependFloat64Slot(6, VEL_Z, 0.0)

def AddVEL_Z(builder, VEL_Z):
    STVAddVEL_Z(builder, VEL_Z)

def STVAddREF_FRAME(builder, REF_FRAME):
    builder.PrependUint8Slot(7, REF_FRAME, 0)

def AddREF_FRAME(builder, REF_FRAME):
    STVAddREF_FRAME(builder, REF_FRAME)

def STVAddFLAGS(builder, FLAGS):
    builder.PrependUint8Slot(8, FLAGS, 0)

def AddFLAGS(builder, FLAGS):
    STVAddFLAGS(builder, FLAGS)

def STVAddRESERVED(builder, RESERVED):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(RESERVED), 0)

def AddRESERVED(builder, RESERVED):
    STVAddRESERVED(builder, RESERVED)

def STVStartRESERVEDVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartRESERVEDVector(builder, numElems):
    return STVStartRESERVEDVector(builder, numElems)

def STVEnd(builder):
    return builder.EndObject()

def End(builder):
    return STVEnd(builder)

try:
    from typing import List
except:
    pass

class STVT(object):

    # STVT
    def __init__(self):
        self.EPOCH = 0.0  # type: float
        self.POS_X = 0.0  # type: float
        self.POS_Y = 0.0  # type: float
        self.POS_Z = 0.0  # type: float
        self.VEL_X = 0.0  # type: float
        self.VEL_Y = 0.0  # type: float
        self.VEL_Z = 0.0  # type: float
        self.REF_FRAME = 0  # type: int
        self.FLAGS = 0  # type: int
        self.RESERVED = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        STV = STV()
        STV.Init(buf, pos)
        return cls.InitFromObj(STV)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, STV):
        x = STVT()
        x._UnPack(STV)
        return x

    # STVT
    def _UnPack(self, STV):
        if STV is None:
            return
        self.EPOCH = STV.EPOCH()
        self.POS_X = STV.POS_X()
        self.POS_Y = STV.POS_Y()
        self.POS_Z = STV.POS_Z()
        self.VEL_X = STV.VEL_X()
        self.VEL_Y = STV.VEL_Y()
        self.VEL_Z = STV.VEL_Z()
        self.REF_FRAME = STV.REF_FRAME()
        self.FLAGS = STV.FLAGS()
        if not STV.RESERVEDIsNone():
            if np is None:
                self.RESERVED = []
                for i in range(STV.RESERVEDLength()):
                    self.RESERVED.append(STV.RESERVED(i))
            else:
                self.RESERVED = STV.RESERVEDAsNumpy()

    # STVT
    def Pack(self, builder):
        if self.RESERVED is not None:
            if np is not None and type(self.RESERVED) is np.ndarray:
                RESERVED = builder.CreateNumpyVector(self.RESERVED)
            else:
                STVStartRESERVEDVector(builder, len(self.RESERVED))
                for i in reversed(range(len(self.RESERVED))):
                    builder.PrependUint8(self.RESERVED[i])
                RESERVED = builder.EndVector()
        STVStart(builder)
        STVAddEPOCH(builder, self.EPOCH)
        STVAddPOS_X(builder, self.POS_X)
        STVAddPOS_Y(builder, self.POS_Y)
        STVAddPOS_Z(builder, self.POS_Z)
        STVAddVEL_X(builder, self.VEL_X)
        STVAddVEL_Y(builder, self.VEL_Y)
        STVAddVEL_Z(builder, self.VEL_Z)
        STVAddREF_FRAME(builder, self.REF_FRAME)
        STVAddFLAGS(builder, self.FLAGS)
        if self.RESERVED is not None:
            STVAddRESERVED(builder, RESERVED)
        STV = STVEnd(builder)
        return STV
