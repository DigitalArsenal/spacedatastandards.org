# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Replayable provenance for ONE source's report of ONE facility, emission, or
# scheduled transmission.
#
# Every source consulted gets its own entry, including sources that returned
# nothing, so a consumer can tell "not looked for" from "looked for and
# returned nothing". Source identity is DATA: PROVIDER_ID and AUTHORITY carry
# verbatim what that source states about itself.
class TXSProvenance(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TXSProvenance()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTXSProvenance(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TXSProvenanceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x58\x53", size_prefixed=size_prefixed)

    # TXSProvenance
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Publisher-stable, lowercase identifier for the consulted source, unique
    # within the publisher's own source registry. Never a display name and
    # never a URL.
    # TXSProvenance
    def PROVIDER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The authority under which that source publishes, verbatim as the source
    # states it: a spectrum regulator, an international coordination register,
    # a licensee, a monitoring service, or an observer compilation. Never
    # inferred by the publisher.
    # TXSProvenance
    def AUTHORITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Replayable URL identifying the source material.
    # TXSProvenance
    def SOURCE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The exact query, filter, or request parameters used, so the same rows can
    # be recovered by repeating the fetch.
    # TXSProvenance
    def SOURCE_QUERY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFC 3339 UTC fixed-millisecond retrieval timestamp.
    # TXSProvenance
    def RETRIEVED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Redistribution terms verbatim. Required: there is no encoding for a
    # redistributed record whose terms are unstated. A source that publishes no
    # terms is recorded as publishing no terms; unknown terms are never public
    # domain and never an implied grant.
    # TXSProvenance
    def LICENSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # URL of the full licence text as that source publishes it.
    # TXSProvenance
    def LICENSE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Attribution text the licence requires a redistributor to display.
    # TXSProvenance
    def ATTRIBUTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The licence forbids commercial use. Rides PER SOURCE because one record
    # may merge an open-terms lane beside a non-commercial cross-check, and the
    # strictest consulted lane governs any derived product.
    # TXSProvenance
    def NON_COMMERCIAL_ONLY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # This source's own row, record, licence or file identifier, verbatim.
    # TXSProvenance
    def NATIVE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The class of authority this particular source speaks with about position.
    # Published per source because a record may merge a regulator lane with a
    # community lane; the record-level POSITION_AUTHORITY states which class the
    # PUBLISHED coordinate came from.
    # TXSProvenance
    def POSITION_AUTHORITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Position this source reported, in decimal degrees on WGS 84, before
    # reconciliation. Retained unrounded so the merge stays auditable. Absent
    # when the source was consulted and reported no position.
    # TXSProvenance
    def REPORTED_LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TXSProvenance
    def REPORTED_LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # This source's report materially contributed to the published record.
    # False marks a source that was consulted and either returned nothing or
    # was outvoted; its entry is published either way.
    # TXSProvenance
    def CONTRIBUTED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def TXSProvenanceStart(builder):
    builder.StartObject(14)

def Start(builder):
    TXSProvenanceStart(builder)

def TXSProvenanceAddPROVIDER_ID(builder, PROVIDER_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_ID), 0)

def AddPROVIDER_ID(builder, PROVIDER_ID):
    TXSProvenanceAddPROVIDER_ID(builder, PROVIDER_ID)

def TXSProvenanceAddAUTHORITY(builder, AUTHORITY):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(AUTHORITY), 0)

def AddAUTHORITY(builder, AUTHORITY):
    TXSProvenanceAddAUTHORITY(builder, AUTHORITY)

def TXSProvenanceAddSOURCE_URL(builder, SOURCE_URL):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_URL), 0)

def AddSOURCE_URL(builder, SOURCE_URL):
    TXSProvenanceAddSOURCE_URL(builder, SOURCE_URL)

def TXSProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_QUERY), 0)

def AddSOURCE_QUERY(builder, SOURCE_QUERY):
    TXSProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY)

def TXSProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(RETRIEVED_AT), 0)

def AddRETRIEVED_AT(builder, RETRIEVED_AT):
    TXSProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT)

def TXSProvenanceAddLICENSE(builder, LICENSE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE), 0)

def AddLICENSE(builder, LICENSE):
    TXSProvenanceAddLICENSE(builder, LICENSE)

def TXSProvenanceAddLICENSE_URL(builder, LICENSE_URL):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE_URL), 0)

def AddLICENSE_URL(builder, LICENSE_URL):
    TXSProvenanceAddLICENSE_URL(builder, LICENSE_URL)

def TXSProvenanceAddATTRIBUTION(builder, ATTRIBUTION):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(ATTRIBUTION), 0)

def AddATTRIBUTION(builder, ATTRIBUTION):
    TXSProvenanceAddATTRIBUTION(builder, ATTRIBUTION)

def TXSProvenanceAddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY):
    builder.PrependBoolSlot(8, NON_COMMERCIAL_ONLY, 0)

def AddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY):
    TXSProvenanceAddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY)

def TXSProvenanceAddNATIVE_ID(builder, NATIVE_ID):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(NATIVE_ID), 0)

def AddNATIVE_ID(builder, NATIVE_ID):
    TXSProvenanceAddNATIVE_ID(builder, NATIVE_ID)

def TXSProvenanceAddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY):
    builder.PrependInt8Slot(10, POSITION_AUTHORITY, 0)

def AddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY):
    TXSProvenanceAddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY)

def TXSProvenanceAddREPORTED_LATITUDE(builder, REPORTED_LATITUDE):
    builder.PrependFloat64Slot(11, REPORTED_LATITUDE, 0.0)

def AddREPORTED_LATITUDE(builder, REPORTED_LATITUDE):
    TXSProvenanceAddREPORTED_LATITUDE(builder, REPORTED_LATITUDE)

def TXSProvenanceAddREPORTED_LONGITUDE(builder, REPORTED_LONGITUDE):
    builder.PrependFloat64Slot(12, REPORTED_LONGITUDE, 0.0)

def AddREPORTED_LONGITUDE(builder, REPORTED_LONGITUDE):
    TXSProvenanceAddREPORTED_LONGITUDE(builder, REPORTED_LONGITUDE)

def TXSProvenanceAddCONTRIBUTED(builder, CONTRIBUTED):
    builder.PrependBoolSlot(13, CONTRIBUTED, 0)

def AddCONTRIBUTED(builder, CONTRIBUTED):
    TXSProvenanceAddCONTRIBUTED(builder, CONTRIBUTED)

def TXSProvenanceEnd(builder):
    return builder.EndObject()

def End(builder):
    return TXSProvenanceEnd(builder)


class TXSProvenanceT(object):

    # TXSProvenanceT
    def __init__(
        self,
        PROVIDER_ID = None,
        AUTHORITY = None,
        SOURCE_URL = None,
        SOURCE_QUERY = None,
        RETRIEVED_AT = None,
        LICENSE = None,
        LICENSE_URL = None,
        ATTRIBUTION = None,
        NON_COMMERCIAL_ONLY = False,
        NATIVE_ID = None,
        POSITION_AUTHORITY = 0,
        REPORTED_LATITUDE = 0.0,
        REPORTED_LONGITUDE = 0.0,
        CONTRIBUTED = False,
    ):
        self.PROVIDER_ID = PROVIDER_ID  # type: Optional[str]
        self.AUTHORITY = AUTHORITY  # type: Optional[str]
        self.SOURCE_URL = SOURCE_URL  # type: Optional[str]
        self.SOURCE_QUERY = SOURCE_QUERY  # type: Optional[str]
        self.RETRIEVED_AT = RETRIEVED_AT  # type: Optional[str]
        self.LICENSE = LICENSE  # type: Optional[str]
        self.LICENSE_URL = LICENSE_URL  # type: Optional[str]
        self.ATTRIBUTION = ATTRIBUTION  # type: Optional[str]
        self.NON_COMMERCIAL_ONLY = NON_COMMERCIAL_ONLY  # type: bool
        self.NATIVE_ID = NATIVE_ID  # type: Optional[str]
        self.POSITION_AUTHORITY = POSITION_AUTHORITY  # type: int
        self.REPORTED_LATITUDE = REPORTED_LATITUDE  # type: float
        self.REPORTED_LONGITUDE = REPORTED_LONGITUDE  # type: float
        self.CONTRIBUTED = CONTRIBUTED  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTxsprovenance = TXSProvenance()
        tmpTxsprovenance.Init(buf, pos)
        return cls.InitFromObj(tmpTxsprovenance)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTxsprovenance):
        x = TXSProvenanceT()
        x._UnPack(tmpTxsprovenance)
        return x

    # TXSProvenanceT
    def _UnPack(self, TXSProvenance):
        if TXSProvenance is None:
            return
        self.PROVIDER_ID = TXSProvenance.PROVIDER_ID()
        self.AUTHORITY = TXSProvenance.AUTHORITY()
        self.SOURCE_URL = TXSProvenance.SOURCE_URL()
        self.SOURCE_QUERY = TXSProvenance.SOURCE_QUERY()
        self.RETRIEVED_AT = TXSProvenance.RETRIEVED_AT()
        self.LICENSE = TXSProvenance.LICENSE()
        self.LICENSE_URL = TXSProvenance.LICENSE_URL()
        self.ATTRIBUTION = TXSProvenance.ATTRIBUTION()
        self.NON_COMMERCIAL_ONLY = TXSProvenance.NON_COMMERCIAL_ONLY()
        self.NATIVE_ID = TXSProvenance.NATIVE_ID()
        self.POSITION_AUTHORITY = TXSProvenance.POSITION_AUTHORITY()
        self.REPORTED_LATITUDE = TXSProvenance.REPORTED_LATITUDE()
        self.REPORTED_LONGITUDE = TXSProvenance.REPORTED_LONGITUDE()
        self.CONTRIBUTED = TXSProvenance.CONTRIBUTED()

    # TXSProvenanceT
    def Pack(self, builder):
        if self.PROVIDER_ID is not None:
            PROVIDER_ID = builder.CreateString(self.PROVIDER_ID)
        if self.AUTHORITY is not None:
            AUTHORITY = builder.CreateString(self.AUTHORITY)
        if self.SOURCE_URL is not None:
            SOURCE_URL = builder.CreateString(self.SOURCE_URL)
        if self.SOURCE_QUERY is not None:
            SOURCE_QUERY = builder.CreateString(self.SOURCE_QUERY)
        if self.RETRIEVED_AT is not None:
            RETRIEVED_AT = builder.CreateString(self.RETRIEVED_AT)
        if self.LICENSE is not None:
            LICENSE = builder.CreateString(self.LICENSE)
        if self.LICENSE_URL is not None:
            LICENSE_URL = builder.CreateString(self.LICENSE_URL)
        if self.ATTRIBUTION is not None:
            ATTRIBUTION = builder.CreateString(self.ATTRIBUTION)
        if self.NATIVE_ID is not None:
            NATIVE_ID = builder.CreateString(self.NATIVE_ID)
        TXSProvenanceStart(builder)
        if self.PROVIDER_ID is not None:
            TXSProvenanceAddPROVIDER_ID(builder, PROVIDER_ID)
        if self.AUTHORITY is not None:
            TXSProvenanceAddAUTHORITY(builder, AUTHORITY)
        if self.SOURCE_URL is not None:
            TXSProvenanceAddSOURCE_URL(builder, SOURCE_URL)
        if self.SOURCE_QUERY is not None:
            TXSProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY)
        if self.RETRIEVED_AT is not None:
            TXSProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT)
        if self.LICENSE is not None:
            TXSProvenanceAddLICENSE(builder, LICENSE)
        if self.LICENSE_URL is not None:
            TXSProvenanceAddLICENSE_URL(builder, LICENSE_URL)
        if self.ATTRIBUTION is not None:
            TXSProvenanceAddATTRIBUTION(builder, ATTRIBUTION)
        TXSProvenanceAddNON_COMMERCIAL_ONLY(builder, self.NON_COMMERCIAL_ONLY)
        if self.NATIVE_ID is not None:
            TXSProvenanceAddNATIVE_ID(builder, NATIVE_ID)
        TXSProvenanceAddPOSITION_AUTHORITY(builder, self.POSITION_AUTHORITY)
        TXSProvenanceAddREPORTED_LATITUDE(builder, self.REPORTED_LATITUDE)
        TXSProvenanceAddREPORTED_LONGITUDE(builder, self.REPORTED_LONGITUDE)
        TXSProvenanceAddCONTRIBUTED(builder, self.CONTRIBUTED)
        TXSProvenance = TXSProvenanceEnd(builder)
        return TXSProvenance
