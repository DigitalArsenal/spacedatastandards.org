# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A position the facility is asserted to have occupied and vacated, retained
# so a relocation never silently rewrites history.
class TXSPriorPosition(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TXSPriorPosition()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTXSPriorPosition(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TXSPriorPositionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x58\x53", size_prefixed=size_prefixed)

    # TXSPriorPosition
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Decimal degrees on WGS 84.
    # TXSPriorPosition
    def LATITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # TXSPriorPosition
    def LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Class of authority behind THIS coordinate, which is frequently weaker
    # than the authority behind the current one.
    # TXSPriorPosition
    def POSITION_AUTHORITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # One-sigma-equivalent horizontal envelope in metres, as the source states
    # it. Zero means unpublished.
    # TXSPriorPosition
    def POSITION_UNCERTAINTY_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFC 3339 UTC bounds over which the facility is asserted to have occupied
    # this position. Either bound is absent when unpublished.
    # TXSPriorPosition
    def VALID_FROM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TXSPriorPosition
    def VALID_UNTIL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PROVIDER_ID of the SOURCES entry asserting this position.
    # TXSPriorPosition
    def PROVIDER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TXSPriorPositionStart(builder):
    builder.StartObject(7)

def Start(builder):
    TXSPriorPositionStart(builder)

def TXSPriorPositionAddLATITUDE(builder, LATITUDE):
    builder.PrependFloat64Slot(0, LATITUDE, 0.0)

def AddLATITUDE(builder, LATITUDE):
    TXSPriorPositionAddLATITUDE(builder, LATITUDE)

def TXSPriorPositionAddLONGITUDE(builder, LONGITUDE):
    builder.PrependFloat64Slot(1, LONGITUDE, 0.0)

def AddLONGITUDE(builder, LONGITUDE):
    TXSPriorPositionAddLONGITUDE(builder, LONGITUDE)

def TXSPriorPositionAddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY):
    builder.PrependInt8Slot(2, POSITION_AUTHORITY, 0)

def AddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY):
    TXSPriorPositionAddPOSITION_AUTHORITY(builder, POSITION_AUTHORITY)

def TXSPriorPositionAddPOSITION_UNCERTAINTY_M(builder, POSITION_UNCERTAINTY_M):
    builder.PrependFloat64Slot(3, POSITION_UNCERTAINTY_M, 0.0)

def AddPOSITION_UNCERTAINTY_M(builder, POSITION_UNCERTAINTY_M):
    TXSPriorPositionAddPOSITION_UNCERTAINTY_M(builder, POSITION_UNCERTAINTY_M)

def TXSPriorPositionAddVALID_FROM(builder, VALID_FROM):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(VALID_FROM), 0)

def AddVALID_FROM(builder, VALID_FROM):
    TXSPriorPositionAddVALID_FROM(builder, VALID_FROM)

def TXSPriorPositionAddVALID_UNTIL(builder, VALID_UNTIL):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(VALID_UNTIL), 0)

def AddVALID_UNTIL(builder, VALID_UNTIL):
    TXSPriorPositionAddVALID_UNTIL(builder, VALID_UNTIL)

def TXSPriorPositionAddPROVIDER_ID(builder, PROVIDER_ID):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_ID), 0)

def AddPROVIDER_ID(builder, PROVIDER_ID):
    TXSPriorPositionAddPROVIDER_ID(builder, PROVIDER_ID)

def TXSPriorPositionEnd(builder):
    return builder.EndObject()

def End(builder):
    return TXSPriorPositionEnd(builder)


class TXSPriorPositionT(object):

    # TXSPriorPositionT
    def __init__(
        self,
        LATITUDE = 0.0,
        LONGITUDE = 0.0,
        POSITION_AUTHORITY = 0,
        POSITION_UNCERTAINTY_M = 0.0,
        VALID_FROM = None,
        VALID_UNTIL = None,
        PROVIDER_ID = None,
    ):
        self.LATITUDE = LATITUDE  # type: float
        self.LONGITUDE = LONGITUDE  # type: float
        self.POSITION_AUTHORITY = POSITION_AUTHORITY  # type: int
        self.POSITION_UNCERTAINTY_M = POSITION_UNCERTAINTY_M  # type: float
        self.VALID_FROM = VALID_FROM  # type: Optional[str]
        self.VALID_UNTIL = VALID_UNTIL  # type: Optional[str]
        self.PROVIDER_ID = PROVIDER_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTxspriorPosition = TXSPriorPosition()
        tmpTxspriorPosition.Init(buf, pos)
        return cls.InitFromObj(tmpTxspriorPosition)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTxspriorPosition):
        x = TXSPriorPositionT()
        x._UnPack(tmpTxspriorPosition)
        return x

    # TXSPriorPositionT
    def _UnPack(self, TXSPriorPosition):
        if TXSPriorPosition is None:
            return
        self.LATITUDE = TXSPriorPosition.LATITUDE()
        self.LONGITUDE = TXSPriorPosition.LONGITUDE()
        self.POSITION_AUTHORITY = TXSPriorPosition.POSITION_AUTHORITY()
        self.POSITION_UNCERTAINTY_M = TXSPriorPosition.POSITION_UNCERTAINTY_M()
        self.VALID_FROM = TXSPriorPosition.VALID_FROM()
        self.VALID_UNTIL = TXSPriorPosition.VALID_UNTIL()
        self.PROVIDER_ID = TXSPriorPosition.PROVIDER_ID()

    # TXSPriorPositionT
    def Pack(self, builder):
        if self.VALID_FROM is not None:
            VALID_FROM = builder.CreateString(self.VALID_FROM)
        if self.VALID_UNTIL is not None:
            VALID_UNTIL = builder.CreateString(self.VALID_UNTIL)
        if self.PROVIDER_ID is not None:
            PROVIDER_ID = builder.CreateString(self.PROVIDER_ID)
        TXSPriorPositionStart(builder)
        TXSPriorPositionAddLATITUDE(builder, self.LATITUDE)
        TXSPriorPositionAddLONGITUDE(builder, self.LONGITUDE)
        TXSPriorPositionAddPOSITION_AUTHORITY(builder, self.POSITION_AUTHORITY)
        TXSPriorPositionAddPOSITION_UNCERTAINTY_M(builder, self.POSITION_UNCERTAINTY_M)
        if self.VALID_FROM is not None:
            TXSPriorPositionAddVALID_FROM(builder, VALID_FROM)
        if self.VALID_UNTIL is not None:
            TXSPriorPositionAddVALID_UNTIL(builder, VALID_UNTIL)
        if self.PROVIDER_ID is not None:
            TXSPriorPositionAddPROVIDER_ID(builder, PROVIDER_ID)
        TXSPriorPosition = TXSPriorPositionEnd(builder)
        return TXSPriorPosition
