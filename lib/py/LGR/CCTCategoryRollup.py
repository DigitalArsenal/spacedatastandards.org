# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# An observed count of catalogue items in one category.
#
# Counts are VOLATILE and are never part of the ratified taxonomy itself. A
# rollup exists only with COUNTED_AT and SOURCE_CATALOG_ID, so a published
# count can never omit when it was taken or what it was taken over. A consumer
# that needs a live number computes it from the items; a consumer rendering a
# published rollup MUST show it as of COUNTED_AT.
class CCTCategoryRollup(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CCTCategoryRollup()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCCTCategoryRollup(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CCTCategoryRollupBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x43\x54", size_prefixed=size_prefixed)

    # CCTCategoryRollup
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Category being counted.
    # CCTCategoryRollup
    def CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Number of catalogue items whose PRIMARY_CATEGORY or CATEGORIES include
    # CODE, counted over SOURCE_CATALOG_ID at COUNTED_AT.
    # CCTCategoryRollup
    def ITEM_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Identifier of the catalogue the count was taken over.
    # CCTCategoryRollup
    def SOURCE_CATALOG_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Unix seconds when the count was taken.
    # CCTCategoryRollup
    def COUNTED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def CCTCategoryRollupStart(builder):
    builder.StartObject(4)

def Start(builder):
    CCTCategoryRollupStart(builder)

def CCTCategoryRollupAddCODE(builder, CODE):
    builder.PrependUint8Slot(0, CODE, 0)

def AddCODE(builder, CODE):
    CCTCategoryRollupAddCODE(builder, CODE)

def CCTCategoryRollupAddITEM_COUNT(builder, ITEM_COUNT):
    builder.PrependUint32Slot(1, ITEM_COUNT, 0)

def AddITEM_COUNT(builder, ITEM_COUNT):
    CCTCategoryRollupAddITEM_COUNT(builder, ITEM_COUNT)

def CCTCategoryRollupAddSOURCE_CATALOG_ID(builder, SOURCE_CATALOG_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_CATALOG_ID), 0)

def AddSOURCE_CATALOG_ID(builder, SOURCE_CATALOG_ID):
    CCTCategoryRollupAddSOURCE_CATALOG_ID(builder, SOURCE_CATALOG_ID)

def CCTCategoryRollupAddCOUNTED_AT(builder, COUNTED_AT):
    builder.PrependUint64Slot(3, COUNTED_AT, 0)

def AddCOUNTED_AT(builder, COUNTED_AT):
    CCTCategoryRollupAddCOUNTED_AT(builder, COUNTED_AT)

def CCTCategoryRollupEnd(builder):
    return builder.EndObject()

def End(builder):
    return CCTCategoryRollupEnd(builder)


class CCTCategoryRollupT(object):

    # CCTCategoryRollupT
    def __init__(
        self,
        CODE = 0,
        ITEM_COUNT = 0,
        SOURCE_CATALOG_ID = None,
        COUNTED_AT = 0,
    ):
        self.CODE = CODE  # type: int
        self.ITEM_COUNT = ITEM_COUNT  # type: int
        self.SOURCE_CATALOG_ID = SOURCE_CATALOG_ID  # type: Optional[str]
        self.COUNTED_AT = COUNTED_AT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCctcategoryRollup = CCTCategoryRollup()
        tmpCctcategoryRollup.Init(buf, pos)
        return cls.InitFromObj(tmpCctcategoryRollup)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCctcategoryRollup):
        x = CCTCategoryRollupT()
        x._UnPack(tmpCctcategoryRollup)
        return x

    # CCTCategoryRollupT
    def _UnPack(self, CCTCategoryRollup):
        if CCTCategoryRollup is None:
            return
        self.CODE = CCTCategoryRollup.CODE()
        self.ITEM_COUNT = CCTCategoryRollup.ITEM_COUNT()
        self.SOURCE_CATALOG_ID = CCTCategoryRollup.SOURCE_CATALOG_ID()
        self.COUNTED_AT = CCTCategoryRollup.COUNTED_AT()

    # CCTCategoryRollupT
    def Pack(self, builder):
        if self.SOURCE_CATALOG_ID is not None:
            SOURCE_CATALOG_ID = builder.CreateString(self.SOURCE_CATALOG_ID)
        CCTCategoryRollupStart(builder)
        CCTCategoryRollupAddCODE(builder, self.CODE)
        CCTCategoryRollupAddITEM_COUNT(builder, self.ITEM_COUNT)
        if self.SOURCE_CATALOG_ID is not None:
            CCTCategoryRollupAddSOURCE_CATALOG_ID(builder, SOURCE_CATALOG_ID)
        CCTCategoryRollupAddCOUNTED_AT(builder, self.COUNTED_AT)
        CCTCategoryRollup = CCTCategoryRollupEnd(builder)
        return CCTCategoryRollup
