# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Licensing grant message
class LGR(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LGR()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLGR(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def LGRBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4C\x47\x52", size_prefixed=size_prefixed)

    # LGR
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Message type
    # LGR
    def MESSAGE_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Unique request identifier
    # LGR
    def REQUEST_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Canonical module identifier
    # LGR
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Optional module version
    # LGR
    def MODULE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requester peer identifier
    # LGR
    def REQUESTER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requester account identity or wallet xpub
    # LGR
    def REQUESTER_XPUB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requested domain
    # LGR
    def REQUESTED_DOMAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Requested timeout in milliseconds
    # LGR
    def REQUESTED_TIMEOUT_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Granted domain
    # LGR
    def GRANTED_DOMAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Granted timeout in milliseconds
    # LGR
    def GRANTED_TIMEOUT_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Grant expiration time in milliseconds since epoch
    # LGR
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Required scope for the publication
    # LGR
    def REQUIRED_SCOPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Grant status string
    # LGR
    def GRANT_STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Structured denial reason
    # LGR
    def DENIAL_REASON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Issued capability token bytes
    # LGR
    def CAPABILITY_TOKEN(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LGR
    def CAPABILITY_TOKENAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LGR
    def CAPABILITY_TOKENLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LGR
    def CAPABILITY_TOKENIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

    # Published module descriptor
    # LGR
    def MODULE_DESCRIPTOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PLG import PLG
            obj = PLG()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Encryption header for the recipient-specific wrapped content-key payload.
    # LGR
    def WRAPPED_CONTENT_KEY_HEADER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from ENC import ENC
            obj = ENC()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Encrypted FlatBuffer payload containing the recipient-specific content key
    # material. The payload currently uses `$KMF` semantics and is decrypted
    # using `WRAPPED_CONTENT_KEY_HEADER` before reading the key bytes.
    # LGR
    def WRAPPED_CONTENT_KEY_PAYLOAD(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LGR
    def WRAPPED_CONTENT_KEY_PAYLOADAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LGR
    def WRAPPED_CONTENT_KEY_PAYLOADLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LGR
    def WRAPPED_CONTENT_KEY_PAYLOADIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        return o == 0

    # Provider public key used to verify the grant signature
    # LGR
    def GRANT_VERIFIER_PUBKEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LGR
    def GRANT_VERIFIER_PUBKEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LGR
    def GRANT_VERIFIER_PUBKEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LGR
    def GRANT_VERIFIER_PUBKEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        return o == 0

    # Provider signature over the grant
    # LGR
    def PROVIDER_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LGR
    def PROVIDER_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LGR
    def PROVIDER_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LGR
    def PROVIDER_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        return o == 0

def LGRStart(builder):
    builder.StartObject(20)

def Start(builder):
    LGRStart(builder)

def LGRAddMESSAGE_TYPE(builder, MESSAGE_TYPE):
    builder.PrependInt8Slot(0, MESSAGE_TYPE, 0)

def AddMESSAGE_TYPE(builder, MESSAGE_TYPE):
    LGRAddMESSAGE_TYPE(builder, MESSAGE_TYPE)

def LGRAddREQUEST_ID(builder, REQUEST_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_ID), 0)

def AddREQUEST_ID(builder, REQUEST_ID):
    LGRAddREQUEST_ID(builder, REQUEST_ID)

def LGRAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    LGRAddMODULE_ID(builder, MODULE_ID)

def LGRAddMODULE_VERSION(builder, MODULE_VERSION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_VERSION), 0)

def AddMODULE_VERSION(builder, MODULE_VERSION):
    LGRAddMODULE_VERSION(builder, MODULE_VERSION)

def LGRAddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTER_PEER_ID), 0)

def AddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID):
    LGRAddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID)

def LGRAddREQUESTER_XPUB(builder, REQUESTER_XPUB):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTER_XPUB), 0)

def AddREQUESTER_XPUB(builder, REQUESTER_XPUB):
    LGRAddREQUESTER_XPUB(builder, REQUESTER_XPUB)

def LGRAddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTED_DOMAIN), 0)

def AddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN):
    LGRAddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN)

def LGRAddREQUESTED_TIMEOUT_MS(builder, REQUESTED_TIMEOUT_MS):
    builder.PrependUint64Slot(7, REQUESTED_TIMEOUT_MS, 0)

def AddREQUESTED_TIMEOUT_MS(builder, REQUESTED_TIMEOUT_MS):
    LGRAddREQUESTED_TIMEOUT_MS(builder, REQUESTED_TIMEOUT_MS)

def LGRAddGRANTED_DOMAIN(builder, GRANTED_DOMAIN):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(GRANTED_DOMAIN), 0)

def AddGRANTED_DOMAIN(builder, GRANTED_DOMAIN):
    LGRAddGRANTED_DOMAIN(builder, GRANTED_DOMAIN)

def LGRAddGRANTED_TIMEOUT_MS(builder, GRANTED_TIMEOUT_MS):
    builder.PrependUint64Slot(9, GRANTED_TIMEOUT_MS, 0)

def AddGRANTED_TIMEOUT_MS(builder, GRANTED_TIMEOUT_MS):
    LGRAddGRANTED_TIMEOUT_MS(builder, GRANTED_TIMEOUT_MS)

def LGRAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(10, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    LGRAddEXPIRES_AT(builder, EXPIRES_AT)

def LGRAddREQUIRED_SCOPE(builder, REQUIRED_SCOPE):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(REQUIRED_SCOPE), 0)

def AddREQUIRED_SCOPE(builder, REQUIRED_SCOPE):
    LGRAddREQUIRED_SCOPE(builder, REQUIRED_SCOPE)

def LGRAddGRANT_STATUS(builder, GRANT_STATUS):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(GRANT_STATUS), 0)

def AddGRANT_STATUS(builder, GRANT_STATUS):
    LGRAddGRANT_STATUS(builder, GRANT_STATUS)

def LGRAddDENIAL_REASON(builder, DENIAL_REASON):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(DENIAL_REASON), 0)

def AddDENIAL_REASON(builder, DENIAL_REASON):
    LGRAddDENIAL_REASON(builder, DENIAL_REASON)

def LGRAddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(CAPABILITY_TOKEN), 0)

def AddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN):
    LGRAddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN)

def LGRStartCAPABILITY_TOKENVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCAPABILITY_TOKENVector(builder, numElems):
    return LGRStartCAPABILITY_TOKENVector(builder, numElems)

def LGRCreateCAPABILITY_TOKENVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCAPABILITY_TOKENVector(builder, data):
    LGRCreateCAPABILITY_TOKENVector(builder, data)

def LGRAddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_DESCRIPTOR), 0)

def AddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR):
    LGRAddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR)

def LGRAddWRAPPED_CONTENT_KEY_HEADER(builder, WRAPPED_CONTENT_KEY_HEADER):
    builder.PrependUOffsetTRelativeSlot(16, flatbuffers.number_types.UOffsetTFlags.py_type(WRAPPED_CONTENT_KEY_HEADER), 0)

def AddWRAPPED_CONTENT_KEY_HEADER(builder, WRAPPED_CONTENT_KEY_HEADER):
    LGRAddWRAPPED_CONTENT_KEY_HEADER(builder, WRAPPED_CONTENT_KEY_HEADER)

def LGRAddWRAPPED_CONTENT_KEY_PAYLOAD(builder, WRAPPED_CONTENT_KEY_PAYLOAD):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(WRAPPED_CONTENT_KEY_PAYLOAD), 0)

def AddWRAPPED_CONTENT_KEY_PAYLOAD(builder, WRAPPED_CONTENT_KEY_PAYLOAD):
    LGRAddWRAPPED_CONTENT_KEY_PAYLOAD(builder, WRAPPED_CONTENT_KEY_PAYLOAD)

def LGRStartWRAPPED_CONTENT_KEY_PAYLOADVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartWRAPPED_CONTENT_KEY_PAYLOADVector(builder, numElems):
    return LGRStartWRAPPED_CONTENT_KEY_PAYLOADVector(builder, numElems)

def LGRCreateWRAPPED_CONTENT_KEY_PAYLOADVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateWRAPPED_CONTENT_KEY_PAYLOADVector(builder, data):
    LGRCreateWRAPPED_CONTENT_KEY_PAYLOADVector(builder, data)

def LGRAddGRANT_VERIFIER_PUBKEY(builder, GRANT_VERIFIER_PUBKEY):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(GRANT_VERIFIER_PUBKEY), 0)

def AddGRANT_VERIFIER_PUBKEY(builder, GRANT_VERIFIER_PUBKEY):
    LGRAddGRANT_VERIFIER_PUBKEY(builder, GRANT_VERIFIER_PUBKEY)

def LGRStartGRANT_VERIFIER_PUBKEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartGRANT_VERIFIER_PUBKEYVector(builder, numElems):
    return LGRStartGRANT_VERIFIER_PUBKEYVector(builder, numElems)

def LGRCreateGRANT_VERIFIER_PUBKEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateGRANT_VERIFIER_PUBKEYVector(builder, data):
    LGRCreateGRANT_VERIFIER_PUBKEYVector(builder, data)

def LGRAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(19, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_SIGNATURE), 0)

def AddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    LGRAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)

def LGRStartPROVIDER_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPROVIDER_SIGNATUREVector(builder, numElems):
    return LGRStartPROVIDER_SIGNATUREVector(builder, numElems)

def LGRCreatePROVIDER_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePROVIDER_SIGNATUREVector(builder, data):
    LGRCreatePROVIDER_SIGNATUREVector(builder, data)

def LGREnd(builder):
    return builder.EndObject()

def End(builder):
    return LGREnd(builder)

import ENC
import PLG
try:
    from typing import List, Optional
except:
    pass

class LGRT(object):

    # LGRT
    def __init__(
        self,
        MESSAGE_TYPE = 0,
        REQUEST_ID = None,
        MODULE_ID = None,
        MODULE_VERSION = None,
        REQUESTER_PEER_ID = None,
        REQUESTER_XPUB = None,
        REQUESTED_DOMAIN = None,
        REQUESTED_TIMEOUT_MS = 0,
        GRANTED_DOMAIN = None,
        GRANTED_TIMEOUT_MS = 0,
        EXPIRES_AT = 0,
        REQUIRED_SCOPE = None,
        GRANT_STATUS = None,
        DENIAL_REASON = None,
        CAPABILITY_TOKEN = None,
        MODULE_DESCRIPTOR = None,
        WRAPPED_CONTENT_KEY_HEADER = None,
        WRAPPED_CONTENT_KEY_PAYLOAD = None,
        GRANT_VERIFIER_PUBKEY = None,
        PROVIDER_SIGNATURE = None,
    ):
        self.MESSAGE_TYPE = MESSAGE_TYPE  # type: int
        self.REQUEST_ID = REQUEST_ID  # type: Optional[str]
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.MODULE_VERSION = MODULE_VERSION  # type: Optional[str]
        self.REQUESTER_PEER_ID = REQUESTER_PEER_ID  # type: Optional[str]
        self.REQUESTER_XPUB = REQUESTER_XPUB  # type: Optional[str]
        self.REQUESTED_DOMAIN = REQUESTED_DOMAIN  # type: Optional[str]
        self.REQUESTED_TIMEOUT_MS = REQUESTED_TIMEOUT_MS  # type: int
        self.GRANTED_DOMAIN = GRANTED_DOMAIN  # type: Optional[str]
        self.GRANTED_TIMEOUT_MS = GRANTED_TIMEOUT_MS  # type: int
        self.EXPIRES_AT = EXPIRES_AT  # type: int
        self.REQUIRED_SCOPE = REQUIRED_SCOPE  # type: Optional[str]
        self.GRANT_STATUS = GRANT_STATUS  # type: Optional[str]
        self.DENIAL_REASON = DENIAL_REASON  # type: Optional[str]
        self.CAPABILITY_TOKEN = CAPABILITY_TOKEN  # type: Optional[List[int]]
        self.MODULE_DESCRIPTOR = MODULE_DESCRIPTOR  # type: Optional[PLG.PLGT]
        self.WRAPPED_CONTENT_KEY_HEADER = WRAPPED_CONTENT_KEY_HEADER  # type: Optional[ENC.ENCT]
        self.WRAPPED_CONTENT_KEY_PAYLOAD = WRAPPED_CONTENT_KEY_PAYLOAD  # type: Optional[List[int]]
        self.GRANT_VERIFIER_PUBKEY = GRANT_VERIFIER_PUBKEY  # type: Optional[List[int]]
        self.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpLgr = LGR()
        tmpLgr.Init(buf, pos)
        return cls.InitFromObj(tmpLgr)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpLgr):
        x = LGRT()
        x._UnPack(tmpLgr)
        return x

    # LGRT
    def _UnPack(self, LGR):
        if LGR is None:
            return
        self.MESSAGE_TYPE = LGR.MESSAGE_TYPE()
        self.REQUEST_ID = LGR.REQUEST_ID()
        self.MODULE_ID = LGR.MODULE_ID()
        self.MODULE_VERSION = LGR.MODULE_VERSION()
        self.REQUESTER_PEER_ID = LGR.REQUESTER_PEER_ID()
        self.REQUESTER_XPUB = LGR.REQUESTER_XPUB()
        self.REQUESTED_DOMAIN = LGR.REQUESTED_DOMAIN()
        self.REQUESTED_TIMEOUT_MS = LGR.REQUESTED_TIMEOUT_MS()
        self.GRANTED_DOMAIN = LGR.GRANTED_DOMAIN()
        self.GRANTED_TIMEOUT_MS = LGR.GRANTED_TIMEOUT_MS()
        self.EXPIRES_AT = LGR.EXPIRES_AT()
        self.REQUIRED_SCOPE = LGR.REQUIRED_SCOPE()
        self.GRANT_STATUS = LGR.GRANT_STATUS()
        self.DENIAL_REASON = LGR.DENIAL_REASON()
        if not LGR.CAPABILITY_TOKENIsNone():
            if np is None:
                self.CAPABILITY_TOKEN = []
                for i in range(LGR.CAPABILITY_TOKENLength()):
                    self.CAPABILITY_TOKEN.append(LGR.CAPABILITY_TOKEN(i))
            else:
                self.CAPABILITY_TOKEN = LGR.CAPABILITY_TOKENAsNumpy()
        if LGR.MODULE_DESCRIPTOR() is not None:
            self.MODULE_DESCRIPTOR = PLG.PLGT.InitFromObj(LGR.MODULE_DESCRIPTOR())
        if LGR.WRAPPED_CONTENT_KEY_HEADER() is not None:
            self.WRAPPED_CONTENT_KEY_HEADER = ENC.ENCT.InitFromObj(LGR.WRAPPED_CONTENT_KEY_HEADER())
        if not LGR.WRAPPED_CONTENT_KEY_PAYLOADIsNone():
            if np is None:
                self.WRAPPED_CONTENT_KEY_PAYLOAD = []
                for i in range(LGR.WRAPPED_CONTENT_KEY_PAYLOADLength()):
                    self.WRAPPED_CONTENT_KEY_PAYLOAD.append(LGR.WRAPPED_CONTENT_KEY_PAYLOAD(i))
            else:
                self.WRAPPED_CONTENT_KEY_PAYLOAD = LGR.WRAPPED_CONTENT_KEY_PAYLOADAsNumpy()
        if not LGR.GRANT_VERIFIER_PUBKEYIsNone():
            if np is None:
                self.GRANT_VERIFIER_PUBKEY = []
                for i in range(LGR.GRANT_VERIFIER_PUBKEYLength()):
                    self.GRANT_VERIFIER_PUBKEY.append(LGR.GRANT_VERIFIER_PUBKEY(i))
            else:
                self.GRANT_VERIFIER_PUBKEY = LGR.GRANT_VERIFIER_PUBKEYAsNumpy()
        if not LGR.PROVIDER_SIGNATUREIsNone():
            if np is None:
                self.PROVIDER_SIGNATURE = []
                for i in range(LGR.PROVIDER_SIGNATURELength()):
                    self.PROVIDER_SIGNATURE.append(LGR.PROVIDER_SIGNATURE(i))
            else:
                self.PROVIDER_SIGNATURE = LGR.PROVIDER_SIGNATUREAsNumpy()

    # LGRT
    def Pack(self, builder):
        if self.REQUEST_ID is not None:
            REQUEST_ID = builder.CreateString(self.REQUEST_ID)
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.MODULE_VERSION is not None:
            MODULE_VERSION = builder.CreateString(self.MODULE_VERSION)
        if self.REQUESTER_PEER_ID is not None:
            REQUESTER_PEER_ID = builder.CreateString(self.REQUESTER_PEER_ID)
        if self.REQUESTER_XPUB is not None:
            REQUESTER_XPUB = builder.CreateString(self.REQUESTER_XPUB)
        if self.REQUESTED_DOMAIN is not None:
            REQUESTED_DOMAIN = builder.CreateString(self.REQUESTED_DOMAIN)
        if self.GRANTED_DOMAIN is not None:
            GRANTED_DOMAIN = builder.CreateString(self.GRANTED_DOMAIN)
        if self.REQUIRED_SCOPE is not None:
            REQUIRED_SCOPE = builder.CreateString(self.REQUIRED_SCOPE)
        if self.GRANT_STATUS is not None:
            GRANT_STATUS = builder.CreateString(self.GRANT_STATUS)
        if self.DENIAL_REASON is not None:
            DENIAL_REASON = builder.CreateString(self.DENIAL_REASON)
        if self.CAPABILITY_TOKEN is not None:
            if np is not None and type(self.CAPABILITY_TOKEN) is np.ndarray:
                CAPABILITY_TOKEN = builder.CreateNumpyVector(self.CAPABILITY_TOKEN)
            else:
                LGRStartCAPABILITY_TOKENVector(builder, len(self.CAPABILITY_TOKEN))
                for i in reversed(range(len(self.CAPABILITY_TOKEN))):
                    builder.PrependUint8(self.CAPABILITY_TOKEN[i])
                CAPABILITY_TOKEN = builder.EndVector()
        if self.MODULE_DESCRIPTOR is not None:
            MODULE_DESCRIPTOR = self.MODULE_DESCRIPTOR.Pack(builder)
        if self.WRAPPED_CONTENT_KEY_HEADER is not None:
            WRAPPED_CONTENT_KEY_HEADER = self.WRAPPED_CONTENT_KEY_HEADER.Pack(builder)
        if self.WRAPPED_CONTENT_KEY_PAYLOAD is not None:
            if np is not None and type(self.WRAPPED_CONTENT_KEY_PAYLOAD) is np.ndarray:
                WRAPPED_CONTENT_KEY_PAYLOAD = builder.CreateNumpyVector(self.WRAPPED_CONTENT_KEY_PAYLOAD)
            else:
                LGRStartWRAPPED_CONTENT_KEY_PAYLOADVector(builder, len(self.WRAPPED_CONTENT_KEY_PAYLOAD))
                for i in reversed(range(len(self.WRAPPED_CONTENT_KEY_PAYLOAD))):
                    builder.PrependUint8(self.WRAPPED_CONTENT_KEY_PAYLOAD[i])
                WRAPPED_CONTENT_KEY_PAYLOAD = builder.EndVector()
        if self.GRANT_VERIFIER_PUBKEY is not None:
            if np is not None and type(self.GRANT_VERIFIER_PUBKEY) is np.ndarray:
                GRANT_VERIFIER_PUBKEY = builder.CreateNumpyVector(self.GRANT_VERIFIER_PUBKEY)
            else:
                LGRStartGRANT_VERIFIER_PUBKEYVector(builder, len(self.GRANT_VERIFIER_PUBKEY))
                for i in reversed(range(len(self.GRANT_VERIFIER_PUBKEY))):
                    builder.PrependUint8(self.GRANT_VERIFIER_PUBKEY[i])
                GRANT_VERIFIER_PUBKEY = builder.EndVector()
        if self.PROVIDER_SIGNATURE is not None:
            if np is not None and type(self.PROVIDER_SIGNATURE) is np.ndarray:
                PROVIDER_SIGNATURE = builder.CreateNumpyVector(self.PROVIDER_SIGNATURE)
            else:
                LGRStartPROVIDER_SIGNATUREVector(builder, len(self.PROVIDER_SIGNATURE))
                for i in reversed(range(len(self.PROVIDER_SIGNATURE))):
                    builder.PrependUint8(self.PROVIDER_SIGNATURE[i])
                PROVIDER_SIGNATURE = builder.EndVector()
        LGRStart(builder)
        LGRAddMESSAGE_TYPE(builder, self.MESSAGE_TYPE)
        if self.REQUEST_ID is not None:
            LGRAddREQUEST_ID(builder, REQUEST_ID)
        if self.MODULE_ID is not None:
            LGRAddMODULE_ID(builder, MODULE_ID)
        if self.MODULE_VERSION is not None:
            LGRAddMODULE_VERSION(builder, MODULE_VERSION)
        if self.REQUESTER_PEER_ID is not None:
            LGRAddREQUESTER_PEER_ID(builder, REQUESTER_PEER_ID)
        if self.REQUESTER_XPUB is not None:
            LGRAddREQUESTER_XPUB(builder, REQUESTER_XPUB)
        if self.REQUESTED_DOMAIN is not None:
            LGRAddREQUESTED_DOMAIN(builder, REQUESTED_DOMAIN)
        LGRAddREQUESTED_TIMEOUT_MS(builder, self.REQUESTED_TIMEOUT_MS)
        if self.GRANTED_DOMAIN is not None:
            LGRAddGRANTED_DOMAIN(builder, GRANTED_DOMAIN)
        LGRAddGRANTED_TIMEOUT_MS(builder, self.GRANTED_TIMEOUT_MS)
        LGRAddEXPIRES_AT(builder, self.EXPIRES_AT)
        if self.REQUIRED_SCOPE is not None:
            LGRAddREQUIRED_SCOPE(builder, REQUIRED_SCOPE)
        if self.GRANT_STATUS is not None:
            LGRAddGRANT_STATUS(builder, GRANT_STATUS)
        if self.DENIAL_REASON is not None:
            LGRAddDENIAL_REASON(builder, DENIAL_REASON)
        if self.CAPABILITY_TOKEN is not None:
            LGRAddCAPABILITY_TOKEN(builder, CAPABILITY_TOKEN)
        if self.MODULE_DESCRIPTOR is not None:
            LGRAddMODULE_DESCRIPTOR(builder, MODULE_DESCRIPTOR)
        if self.WRAPPED_CONTENT_KEY_HEADER is not None:
            LGRAddWRAPPED_CONTENT_KEY_HEADER(builder, WRAPPED_CONTENT_KEY_HEADER)
        if self.WRAPPED_CONTENT_KEY_PAYLOAD is not None:
            LGRAddWRAPPED_CONTENT_KEY_PAYLOAD(builder, WRAPPED_CONTENT_KEY_PAYLOAD)
        if self.GRANT_VERIFIER_PUBKEY is not None:
            LGRAddGRANT_VERIFIER_PUBKEY(builder, GRANT_VERIFIER_PUBKEY)
        if self.PROVIDER_SIGNATURE is not None:
            LGRAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)
        LGR = LGREnd(builder)
        return LGR
