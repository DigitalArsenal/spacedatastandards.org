# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Exact validated SDS and representation contract bound into a signed flow
# edge. CANONICAL_TYPE and ALIGNED_TYPE describe the same logical schema;
# ALIGNED_TYPE carries its fixed layout in TAB.FlatBufferTypeRef.
class PLGFlowEdgeContract(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PLGFlowEdgeContract()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPLGFlowEdgeContract(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PLGFlowEdgeContractBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # PLGFlowEdgeContract
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Canonical SDS identity carried by the edge. NOT `required`: an edge may
    # be opaque by design (see OPAQUE), and a signer must never be forced to
    # invent an identity to satisfy the schema. A contract MUST carry exactly
    # one of CANONICAL_TYPE or OPAQUE = true; a contract with neither, or with
    # both, is invalid and MUST be rejected by the compiler that signs the flow.
    # PLGFlowEdgeContract
    def CANONICAL_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FlatBufferTypeRef import FlatBufferTypeRef
            obj = FlatBufferTypeRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PLGFlowEdgeContract
    def ALIGNED_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FlatBufferTypeRef import FlatBufferTypeRef
            obj = FlatBufferTypeRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PLGFlowEdgeContract
    def CANONICAL_FALLBACK_AVAILABLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return True

    # PLGFlowEdgeContract
    def ALIGNED_ELIGIBLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # PLGFlowEdgeContract
    def ROUTE_POLICY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # The edge carries bytes with no SDS identity BY DESIGN — an
    # application-blind host-capability adapter (an HTTP body, a raw file
    # chunk) or a timer TICK frame with no payload at all. This is a deliberate
    # signed assertion of opacity, which is why it is an explicit flag rather
    # than an absent CANONICAL_TYPE: a missing type must stay distinguishable
    # from a declared-opaque one. An opaque edge is ineligible for the aligned
    # route, so ALIGNED_ELIGIBLE MUST be false when OPAQUE is true.
    # PLGFlowEdgeContract
    def OPAQUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def PLGFlowEdgeContractStart(builder):
    builder.StartObject(6)

def Start(builder):
    PLGFlowEdgeContractStart(builder)

def PLGFlowEdgeContractAddCANONICAL_TYPE(builder, CANONICAL_TYPE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CANONICAL_TYPE), 0)

def AddCANONICAL_TYPE(builder, CANONICAL_TYPE):
    PLGFlowEdgeContractAddCANONICAL_TYPE(builder, CANONICAL_TYPE)

def PLGFlowEdgeContractAddALIGNED_TYPE(builder, ALIGNED_TYPE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ALIGNED_TYPE), 0)

def AddALIGNED_TYPE(builder, ALIGNED_TYPE):
    PLGFlowEdgeContractAddALIGNED_TYPE(builder, ALIGNED_TYPE)

def PLGFlowEdgeContractAddCANONICAL_FALLBACK_AVAILABLE(builder, CANONICAL_FALLBACK_AVAILABLE):
    builder.PrependBoolSlot(2, CANONICAL_FALLBACK_AVAILABLE, 1)

def AddCANONICAL_FALLBACK_AVAILABLE(builder, CANONICAL_FALLBACK_AVAILABLE):
    PLGFlowEdgeContractAddCANONICAL_FALLBACK_AVAILABLE(builder, CANONICAL_FALLBACK_AVAILABLE)

def PLGFlowEdgeContractAddALIGNED_ELIGIBLE(builder, ALIGNED_ELIGIBLE):
    builder.PrependBoolSlot(3, ALIGNED_ELIGIBLE, 0)

def AddALIGNED_ELIGIBLE(builder, ALIGNED_ELIGIBLE):
    PLGFlowEdgeContractAddALIGNED_ELIGIBLE(builder, ALIGNED_ELIGIBLE)

def PLGFlowEdgeContractAddROUTE_POLICY(builder, ROUTE_POLICY):
    builder.PrependUint8Slot(4, ROUTE_POLICY, 0)

def AddROUTE_POLICY(builder, ROUTE_POLICY):
    PLGFlowEdgeContractAddROUTE_POLICY(builder, ROUTE_POLICY)

def PLGFlowEdgeContractAddOPAQUE(builder, OPAQUE):
    builder.PrependBoolSlot(5, OPAQUE, 0)

def AddOPAQUE(builder, OPAQUE):
    PLGFlowEdgeContractAddOPAQUE(builder, OPAQUE)

def PLGFlowEdgeContractEnd(builder):
    return builder.EndObject()

def End(builder):
    return PLGFlowEdgeContractEnd(builder)

import FlatBufferTypeRef
try:
    from typing import Optional
except:
    pass

class PLGFlowEdgeContractT(object):

    # PLGFlowEdgeContractT
    def __init__(
        self,
        CANONICAL_TYPE = None,
        ALIGNED_TYPE = None,
        CANONICAL_FALLBACK_AVAILABLE = True,
        ALIGNED_ELIGIBLE = False,
        ROUTE_POLICY = 0,
        OPAQUE = False,
    ):
        self.CANONICAL_TYPE = CANONICAL_TYPE  # type: Optional[FlatBufferTypeRef.FlatBufferTypeRefT]
        self.ALIGNED_TYPE = ALIGNED_TYPE  # type: Optional[FlatBufferTypeRef.FlatBufferTypeRefT]
        self.CANONICAL_FALLBACK_AVAILABLE = CANONICAL_FALLBACK_AVAILABLE  # type: bool
        self.ALIGNED_ELIGIBLE = ALIGNED_ELIGIBLE  # type: bool
        self.ROUTE_POLICY = ROUTE_POLICY  # type: int
        self.OPAQUE = OPAQUE  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPlgflowEdgeContract = PLGFlowEdgeContract()
        tmpPlgflowEdgeContract.Init(buf, pos)
        return cls.InitFromObj(tmpPlgflowEdgeContract)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPlgflowEdgeContract):
        x = PLGFlowEdgeContractT()
        x._UnPack(tmpPlgflowEdgeContract)
        return x

    # PLGFlowEdgeContractT
    def _UnPack(self, PLGFlowEdgeContract):
        if PLGFlowEdgeContract is None:
            return
        if PLGFlowEdgeContract.CANONICAL_TYPE() is not None:
            self.CANONICAL_TYPE = FlatBufferTypeRef.FlatBufferTypeRefT.InitFromObj(PLGFlowEdgeContract.CANONICAL_TYPE())
        if PLGFlowEdgeContract.ALIGNED_TYPE() is not None:
            self.ALIGNED_TYPE = FlatBufferTypeRef.FlatBufferTypeRefT.InitFromObj(PLGFlowEdgeContract.ALIGNED_TYPE())
        self.CANONICAL_FALLBACK_AVAILABLE = PLGFlowEdgeContract.CANONICAL_FALLBACK_AVAILABLE()
        self.ALIGNED_ELIGIBLE = PLGFlowEdgeContract.ALIGNED_ELIGIBLE()
        self.ROUTE_POLICY = PLGFlowEdgeContract.ROUTE_POLICY()
        self.OPAQUE = PLGFlowEdgeContract.OPAQUE()

    # PLGFlowEdgeContractT
    def Pack(self, builder):
        if self.CANONICAL_TYPE is not None:
            CANONICAL_TYPE = self.CANONICAL_TYPE.Pack(builder)
        if self.ALIGNED_TYPE is not None:
            ALIGNED_TYPE = self.ALIGNED_TYPE.Pack(builder)
        PLGFlowEdgeContractStart(builder)
        if self.CANONICAL_TYPE is not None:
            PLGFlowEdgeContractAddCANONICAL_TYPE(builder, CANONICAL_TYPE)
        if self.ALIGNED_TYPE is not None:
            PLGFlowEdgeContractAddALIGNED_TYPE(builder, ALIGNED_TYPE)
        PLGFlowEdgeContractAddCANONICAL_FALLBACK_AVAILABLE(builder, self.CANONICAL_FALLBACK_AVAILABLE)
        PLGFlowEdgeContractAddALIGNED_ELIGIBLE(builder, self.ALIGNED_ELIGIBLE)
        PLGFlowEdgeContractAddROUTE_POLICY(builder, self.ROUTE_POLICY)
        PLGFlowEdgeContractAddOPAQUE(builder, self.OPAQUE)
        PLGFlowEdgeContract = PLGFlowEdgeContractEnd(builder)
        return PLGFlowEdgeContract
