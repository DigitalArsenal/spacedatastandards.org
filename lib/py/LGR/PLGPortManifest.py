# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One input or output port on a method.
class PLGPortManifest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PLGPortManifest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPLGPortManifest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PLGPortManifestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # PLGPortManifest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable port identifier within the method.
    # PLGPortManifest
    def PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Human-readable name for UIs.
    # PLGPortManifest
    def DISPLAY_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Type sets accepted on this port.
    # PLGPortManifest
    def ACCEPTED_TYPE_SETS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from PLGAcceptedTypeSet import PLGAcceptedTypeSet
            obj = PLGAcceptedTypeSet()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PLGPortManifest
    def ACCEPTED_TYPE_SETSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PLGPortManifest
    def ACCEPTED_TYPE_SETSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # Minimum number of streams that must be connected.
    # PLGPortManifest
    def MIN_STREAMS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 1

    # Maximum number of streams that may be connected.
    # PLGPortManifest
    def MAX_STREAMS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 1

    # Whether the port must be connected for invocation.
    # PLGPortManifest
    def REQUIRED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return True

    # Optional human-readable description.
    # PLGPortManifest
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PLGPortManifestStart(builder):
    builder.StartObject(7)

def Start(builder):
    PLGPortManifestStart(builder)

def PLGPortManifestAddPORT_ID(builder, PORT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PORT_ID), 0)

def AddPORT_ID(builder, PORT_ID):
    PLGPortManifestAddPORT_ID(builder, PORT_ID)

def PLGPortManifestAddDISPLAY_NAME(builder, DISPLAY_NAME):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(DISPLAY_NAME), 0)

def AddDISPLAY_NAME(builder, DISPLAY_NAME):
    PLGPortManifestAddDISPLAY_NAME(builder, DISPLAY_NAME)

def PLGPortManifestAddACCEPTED_TYPE_SETS(builder, ACCEPTED_TYPE_SETS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ACCEPTED_TYPE_SETS), 0)

def AddACCEPTED_TYPE_SETS(builder, ACCEPTED_TYPE_SETS):
    PLGPortManifestAddACCEPTED_TYPE_SETS(builder, ACCEPTED_TYPE_SETS)

def PLGPortManifestStartACCEPTED_TYPE_SETSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartACCEPTED_TYPE_SETSVector(builder, numElems):
    return PLGPortManifestStartACCEPTED_TYPE_SETSVector(builder, numElems)

def PLGPortManifestCreateACCEPTED_TYPE_SETSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateACCEPTED_TYPE_SETSVector(builder, data):
    PLGPortManifestCreateACCEPTED_TYPE_SETSVector(builder, data)

def PLGPortManifestAddMIN_STREAMS(builder, MIN_STREAMS):
    builder.PrependUint16Slot(3, MIN_STREAMS, 1)

def AddMIN_STREAMS(builder, MIN_STREAMS):
    PLGPortManifestAddMIN_STREAMS(builder, MIN_STREAMS)

def PLGPortManifestAddMAX_STREAMS(builder, MAX_STREAMS):
    builder.PrependUint16Slot(4, MAX_STREAMS, 1)

def AddMAX_STREAMS(builder, MAX_STREAMS):
    PLGPortManifestAddMAX_STREAMS(builder, MAX_STREAMS)

def PLGPortManifestAddREQUIRED(builder, REQUIRED):
    builder.PrependBoolSlot(5, REQUIRED, 1)

def AddREQUIRED(builder, REQUIRED):
    PLGPortManifestAddREQUIRED(builder, REQUIRED)

def PLGPortManifestAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    PLGPortManifestAddDESCRIPTION(builder, DESCRIPTION)

def PLGPortManifestEnd(builder):
    return builder.EndObject()

def End(builder):
    return PLGPortManifestEnd(builder)

import PLGAcceptedTypeSet
try:
    from typing import List
except:
    pass

class PLGPortManifestT(object):

    # PLGPortManifestT
    def __init__(
        self,
        PORT_ID = None,
        DISPLAY_NAME = None,
        ACCEPTED_TYPE_SETS = None,
        MIN_STREAMS = 1,
        MAX_STREAMS = 1,
        REQUIRED = True,
        DESCRIPTION = None,
    ):
        self.PORT_ID = PORT_ID  # type: Optional[str]
        self.DISPLAY_NAME = DISPLAY_NAME  # type: Optional[str]
        self.ACCEPTED_TYPE_SETS = ACCEPTED_TYPE_SETS  # type: Optional[List[PLGAcceptedTypeSet.PLGAcceptedTypeSetT]]
        self.MIN_STREAMS = MIN_STREAMS  # type: int
        self.MAX_STREAMS = MAX_STREAMS  # type: int
        self.REQUIRED = REQUIRED  # type: bool
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPlgportManifest = PLGPortManifest()
        tmpPlgportManifest.Init(buf, pos)
        return cls.InitFromObj(tmpPlgportManifest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPlgportManifest):
        x = PLGPortManifestT()
        x._UnPack(tmpPlgportManifest)
        return x

    # PLGPortManifestT
    def _UnPack(self, PLGPortManifest):
        if PLGPortManifest is None:
            return
        self.PORT_ID = PLGPortManifest.PORT_ID()
        self.DISPLAY_NAME = PLGPortManifest.DISPLAY_NAME()
        if not PLGPortManifest.ACCEPTED_TYPE_SETSIsNone():
            self.ACCEPTED_TYPE_SETS = []
            for i in range(PLGPortManifest.ACCEPTED_TYPE_SETSLength()):
                if PLGPortManifest.ACCEPTED_TYPE_SETS(i) is None:
                    self.ACCEPTED_TYPE_SETS.append(None)
                else:
                    pLGAcceptedTypeSet_ = PLGAcceptedTypeSet.PLGAcceptedTypeSetT.InitFromObj(PLGPortManifest.ACCEPTED_TYPE_SETS(i))
                    self.ACCEPTED_TYPE_SETS.append(pLGAcceptedTypeSet_)
        self.MIN_STREAMS = PLGPortManifest.MIN_STREAMS()
        self.MAX_STREAMS = PLGPortManifest.MAX_STREAMS()
        self.REQUIRED = PLGPortManifest.REQUIRED()
        self.DESCRIPTION = PLGPortManifest.DESCRIPTION()

    # PLGPortManifestT
    def Pack(self, builder):
        if self.PORT_ID is not None:
            PORT_ID = builder.CreateString(self.PORT_ID)
        if self.DISPLAY_NAME is not None:
            DISPLAY_NAME = builder.CreateString(self.DISPLAY_NAME)
        if self.ACCEPTED_TYPE_SETS is not None:
            ACCEPTED_TYPE_SETSlist = []
            for i in range(len(self.ACCEPTED_TYPE_SETS)):
                ACCEPTED_TYPE_SETSlist.append(self.ACCEPTED_TYPE_SETS[i].Pack(builder))
            PLGPortManifestStartACCEPTED_TYPE_SETSVector(builder, len(self.ACCEPTED_TYPE_SETS))
            for i in reversed(range(len(self.ACCEPTED_TYPE_SETS))):
                builder.PrependUOffsetTRelative(ACCEPTED_TYPE_SETSlist[i])
            ACCEPTED_TYPE_SETS = builder.EndVector()
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        PLGPortManifestStart(builder)
        if self.PORT_ID is not None:
            PLGPortManifestAddPORT_ID(builder, PORT_ID)
        if self.DISPLAY_NAME is not None:
            PLGPortManifestAddDISPLAY_NAME(builder, DISPLAY_NAME)
        if self.ACCEPTED_TYPE_SETS is not None:
            PLGPortManifestAddACCEPTED_TYPE_SETS(builder, ACCEPTED_TYPE_SETS)
        PLGPortManifestAddMIN_STREAMS(builder, self.MIN_STREAMS)
        PLGPortManifestAddMAX_STREAMS(builder, self.MAX_STREAMS)
        PLGPortManifestAddREQUIRED(builder, self.REQUIRED)
        if self.DESCRIPTION is not None:
            PLGPortManifestAddDESCRIPTION(builder, DESCRIPTION)
        PLGPortManifest = PLGPortManifestEnd(builder)
        return PLGPortManifest
