# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One flow trigger (e.g. a host timer or HTTP route) that starts a drain.
class PLGFlowTrigger(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PLGFlowTrigger()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPLGFlowTrigger(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PLGFlowTriggerBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # PLGFlowTrigger
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable trigger identifier
    # PLGFlowTrigger
    def TRIGGER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Trigger kind, e.g. "timer", "http"
    # PLGFlowTrigger
    def KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Trigger source, e.g. "host-cron"
    # PLGFlowTrigger
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Default firing interval in milliseconds (timer triggers)
    # PLGFlowTrigger
    def DEFAULT_INTERVAL_MS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Mounted HTTP path (http triggers)
    # PLGFlowTrigger
    def HTTP_PATH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PLGFlowTriggerStart(builder):
    builder.StartObject(5)

def Start(builder):
    PLGFlowTriggerStart(builder)

def PLGFlowTriggerAddTRIGGER_ID(builder, TRIGGER_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(TRIGGER_ID), 0)

def AddTRIGGER_ID(builder, TRIGGER_ID):
    PLGFlowTriggerAddTRIGGER_ID(builder, TRIGGER_ID)

def PLGFlowTriggerAddKIND(builder, KIND):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(KIND), 0)

def AddKIND(builder, KIND):
    PLGFlowTriggerAddKIND(builder, KIND)

def PLGFlowTriggerAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    PLGFlowTriggerAddSOURCE(builder, SOURCE)

def PLGFlowTriggerAddDEFAULT_INTERVAL_MS(builder, DEFAULT_INTERVAL_MS):
    builder.PrependUint64Slot(3, DEFAULT_INTERVAL_MS, 0)

def AddDEFAULT_INTERVAL_MS(builder, DEFAULT_INTERVAL_MS):
    PLGFlowTriggerAddDEFAULT_INTERVAL_MS(builder, DEFAULT_INTERVAL_MS)

def PLGFlowTriggerAddHTTP_PATH(builder, HTTP_PATH):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(HTTP_PATH), 0)

def AddHTTP_PATH(builder, HTTP_PATH):
    PLGFlowTriggerAddHTTP_PATH(builder, HTTP_PATH)

def PLGFlowTriggerEnd(builder):
    return builder.EndObject()

def End(builder):
    return PLGFlowTriggerEnd(builder)


class PLGFlowTriggerT(object):

    # PLGFlowTriggerT
    def __init__(
        self,
        TRIGGER_ID = None,
        KIND = None,
        SOURCE = None,
        DEFAULT_INTERVAL_MS = 0,
        HTTP_PATH = None,
    ):
        self.TRIGGER_ID = TRIGGER_ID  # type: Optional[str]
        self.KIND = KIND  # type: Optional[str]
        self.SOURCE = SOURCE  # type: Optional[str]
        self.DEFAULT_INTERVAL_MS = DEFAULT_INTERVAL_MS  # type: int
        self.HTTP_PATH = HTTP_PATH  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPlgflowTrigger = PLGFlowTrigger()
        tmpPlgflowTrigger.Init(buf, pos)
        return cls.InitFromObj(tmpPlgflowTrigger)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPlgflowTrigger):
        x = PLGFlowTriggerT()
        x._UnPack(tmpPlgflowTrigger)
        return x

    # PLGFlowTriggerT
    def _UnPack(self, PLGFlowTrigger):
        if PLGFlowTrigger is None:
            return
        self.TRIGGER_ID = PLGFlowTrigger.TRIGGER_ID()
        self.KIND = PLGFlowTrigger.KIND()
        self.SOURCE = PLGFlowTrigger.SOURCE()
        self.DEFAULT_INTERVAL_MS = PLGFlowTrigger.DEFAULT_INTERVAL_MS()
        self.HTTP_PATH = PLGFlowTrigger.HTTP_PATH()

    # PLGFlowTriggerT
    def Pack(self, builder):
        if self.TRIGGER_ID is not None:
            TRIGGER_ID = builder.CreateString(self.TRIGGER_ID)
        if self.KIND is not None:
            KIND = builder.CreateString(self.KIND)
        if self.SOURCE is not None:
            SOURCE = builder.CreateString(self.SOURCE)
        if self.HTTP_PATH is not None:
            HTTP_PATH = builder.CreateString(self.HTTP_PATH)
        PLGFlowTriggerStart(builder)
        if self.TRIGGER_ID is not None:
            PLGFlowTriggerAddTRIGGER_ID(builder, TRIGGER_ID)
        if self.KIND is not None:
            PLGFlowTriggerAddKIND(builder, KIND)
        if self.SOURCE is not None:
            PLGFlowTriggerAddSOURCE(builder, SOURCE)
        PLGFlowTriggerAddDEFAULT_INTERVAL_MS(builder, self.DEFAULT_INTERVAL_MS)
        if self.HTTP_PATH is not None:
            PLGFlowTriggerAddHTTP_PATH(builder, HTTP_PATH)
        PLGFlowTrigger = PLGFlowTriggerEnd(builder)
        return PLGFlowTrigger
