# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Where and what this material is attached to. A material record with no
# geometry describes a substance; a material record WITH geometry describes
# an obstacle a solver can intersect.
class RFSGeometryRef(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RFSGeometryRef()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRFSGeometryRef(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RFSGeometryRefBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x46\x53", size_prefixed=size_prefixed)

    # RFSGeometryRef
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Producer-stable identifier of this attachment.
    # RFSGeometryRef
    def GEOMETRY_REF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFSGeometryRef
    def GEOMETRY_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # `$VAM` asset identity, copied VERBATIM including the byte hash so the
    # material can never drift onto a different revision of the mesh.
    # RFSGeometryRef
    def VAM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFSGeometryRef
    def VARIANT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFSGeometryRef
    def ASSET_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Mesh and primitive inside the asset.
    # RFSGeometryRef
    def MESH_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFSGeometryRef
    def PRIMITIVE_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return -1

    # glTF material slot the properties apply to, matching the
    # `$OPP.OPPSurface` join fields.
    # RFSGeometryRef
    def GLTF_MATERIAL_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFSGeometryRef
    def GLTF_MATERIAL_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return -1

    # Explicit face indexes, when the material covers part of a mesh.
    # RFSGeometryRef
    def FACE_INDEXES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # RFSGeometryRef
    def FACE_INDEXESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # RFSGeometryRef
    def FACE_INDEXESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RFSGeometryRef
    def FACE_INDEXESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # `$OPP.OPPSurface` surface identifier.
    # RFSGeometryRef
    def OPP_SURFACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$PNL` panel identifier.
    # RFSGeometryRef
    def PANEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$CAT` identity, when the obstacle is a catalogued object. 0 when
    # unbound.
    # RFSGeometryRef
    def NORAD_CAT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # RFSGeometryRef
    def OBJECT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$TBS.SITE_ID`, when the obstacle is a structure at a terrestrial site.
    # RFSGeometryRef
    def TBS_SITE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Producer's own feature identifier for a building, wall or terrain patch.
    # RFSGeometryRef
    def FEATURE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Primitive and its dimensions in metres, when GEOMETRY_KIND is
    # ANALYTIC_SHAPE. The dimension order is the primitive's own convention and
    # MUST be stated in SHAPE_DIMENSION_NAMES.
    # RFSGeometryRef
    def ANALYTIC_SHAPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # RFSGeometryRef
    def SHAPE_DIMENSIONS_M(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # RFSGeometryRef
    def SHAPE_DIMENSIONS_MAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # RFSGeometryRef
    def SHAPE_DIMENSIONS_MLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RFSGeometryRef
    def SHAPE_DIMENSIONS_MIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        return o == 0

    # RFSGeometryRef
    def SHAPE_DIMENSION_NAMES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # RFSGeometryRef
    def SHAPE_DIMENSION_NAMESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RFSGeometryRef
    def SHAPE_DIMENSION_NAMESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        return o == 0

    # Geodetic placement of a fixed obstacle, degrees and metres. Absent for
    # an obstacle whose position is joined from `$CAT`, `$TBS` or `$VAM`.
    # RFSGeometryRef
    def LATITUDE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFSGeometryRef
    def LONGITUDE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFSGeometryRef
    def ALTITUDE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Height above ground of a fixed structure, metres.
    # RFSGeometryRef
    def HEIGHT_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Orientation of the surface normal, degrees east of north and above the
    # horizon. A wall that faces the wrong way blocks the wrong link.
    # RFSGeometryRef
    def NORMAL_AZIMUTH_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # RFSGeometryRef
    def NORMAL_ELEVATION_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def RFSGeometryRefStart(builder):
    builder.StartObject(25)

def Start(builder):
    RFSGeometryRefStart(builder)

def RFSGeometryRefAddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(GEOMETRY_REF_ID), 0)

def AddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID):
    RFSGeometryRefAddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID)

def RFSGeometryRefAddGEOMETRY_KIND(builder, GEOMETRY_KIND):
    builder.PrependInt8Slot(1, GEOMETRY_KIND, 0)

def AddGEOMETRY_KIND(builder, GEOMETRY_KIND):
    RFSGeometryRefAddGEOMETRY_KIND(builder, GEOMETRY_KIND)

def RFSGeometryRefAddVAM_ID(builder, VAM_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VAM_ID), 0)

def AddVAM_ID(builder, VAM_ID):
    RFSGeometryRefAddVAM_ID(builder, VAM_ID)

def RFSGeometryRefAddVARIANT_ID(builder, VARIANT_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(VARIANT_ID), 0)

def AddVARIANT_ID(builder, VARIANT_ID):
    RFSGeometryRefAddVARIANT_ID(builder, VARIANT_ID)

def RFSGeometryRefAddASSET_SHA256(builder, ASSET_SHA256):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(ASSET_SHA256), 0)

def AddASSET_SHA256(builder, ASSET_SHA256):
    RFSGeometryRefAddASSET_SHA256(builder, ASSET_SHA256)

def RFSGeometryRefAddMESH_NAME(builder, MESH_NAME):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(MESH_NAME), 0)

def AddMESH_NAME(builder, MESH_NAME):
    RFSGeometryRefAddMESH_NAME(builder, MESH_NAME)

def RFSGeometryRefAddPRIMITIVE_INDEX(builder, PRIMITIVE_INDEX):
    builder.PrependInt32Slot(6, PRIMITIVE_INDEX, -1)

def AddPRIMITIVE_INDEX(builder, PRIMITIVE_INDEX):
    RFSGeometryRefAddPRIMITIVE_INDEX(builder, PRIMITIVE_INDEX)

def RFSGeometryRefAddGLTF_MATERIAL_NAME(builder, GLTF_MATERIAL_NAME):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(GLTF_MATERIAL_NAME), 0)

def AddGLTF_MATERIAL_NAME(builder, GLTF_MATERIAL_NAME):
    RFSGeometryRefAddGLTF_MATERIAL_NAME(builder, GLTF_MATERIAL_NAME)

def RFSGeometryRefAddGLTF_MATERIAL_INDEX(builder, GLTF_MATERIAL_INDEX):
    builder.PrependInt32Slot(8, GLTF_MATERIAL_INDEX, -1)

def AddGLTF_MATERIAL_INDEX(builder, GLTF_MATERIAL_INDEX):
    RFSGeometryRefAddGLTF_MATERIAL_INDEX(builder, GLTF_MATERIAL_INDEX)

def RFSGeometryRefAddFACE_INDEXES(builder, FACE_INDEXES):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(FACE_INDEXES), 0)

def AddFACE_INDEXES(builder, FACE_INDEXES):
    RFSGeometryRefAddFACE_INDEXES(builder, FACE_INDEXES)

def RFSGeometryRefStartFACE_INDEXESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartFACE_INDEXESVector(builder, numElems):
    return RFSGeometryRefStartFACE_INDEXESVector(builder, numElems)

def RFSGeometryRefCreateFACE_INDEXESVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateFACE_INDEXESVector(builder, data):
    RFSGeometryRefCreateFACE_INDEXESVector(builder, data)

def RFSGeometryRefAddOPP_SURFACE_ID(builder, OPP_SURFACE_ID):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(OPP_SURFACE_ID), 0)

def AddOPP_SURFACE_ID(builder, OPP_SURFACE_ID):
    RFSGeometryRefAddOPP_SURFACE_ID(builder, OPP_SURFACE_ID)

def RFSGeometryRefAddPANEL_ID(builder, PANEL_ID):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(PANEL_ID), 0)

def AddPANEL_ID(builder, PANEL_ID):
    RFSGeometryRefAddPANEL_ID(builder, PANEL_ID)

def RFSGeometryRefAddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    builder.PrependUint32Slot(12, NORAD_CAT_ID, 0)

def AddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    RFSGeometryRefAddNORAD_CAT_ID(builder, NORAD_CAT_ID)

def RFSGeometryRefAddOBJECT_ID(builder, OBJECT_ID):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(OBJECT_ID), 0)

def AddOBJECT_ID(builder, OBJECT_ID):
    RFSGeometryRefAddOBJECT_ID(builder, OBJECT_ID)

def RFSGeometryRefAddTBS_SITE_ID(builder, TBS_SITE_ID):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(TBS_SITE_ID), 0)

def AddTBS_SITE_ID(builder, TBS_SITE_ID):
    RFSGeometryRefAddTBS_SITE_ID(builder, TBS_SITE_ID)

def RFSGeometryRefAddFEATURE_ID(builder, FEATURE_ID):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(FEATURE_ID), 0)

def AddFEATURE_ID(builder, FEATURE_ID):
    RFSGeometryRefAddFEATURE_ID(builder, FEATURE_ID)

def RFSGeometryRefAddANALYTIC_SHAPE(builder, ANALYTIC_SHAPE):
    builder.PrependInt8Slot(16, ANALYTIC_SHAPE, 0)

def AddANALYTIC_SHAPE(builder, ANALYTIC_SHAPE):
    RFSGeometryRefAddANALYTIC_SHAPE(builder, ANALYTIC_SHAPE)

def RFSGeometryRefAddSHAPE_DIMENSIONS_M(builder, SHAPE_DIMENSIONS_M):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(SHAPE_DIMENSIONS_M), 0)

def AddSHAPE_DIMENSIONS_M(builder, SHAPE_DIMENSIONS_M):
    RFSGeometryRefAddSHAPE_DIMENSIONS_M(builder, SHAPE_DIMENSIONS_M)

def RFSGeometryRefStartSHAPE_DIMENSIONS_MVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSHAPE_DIMENSIONS_MVector(builder, numElems):
    return RFSGeometryRefStartSHAPE_DIMENSIONS_MVector(builder, numElems)

def RFSGeometryRefCreateSHAPE_DIMENSIONS_MVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSHAPE_DIMENSIONS_MVector(builder, data):
    RFSGeometryRefCreateSHAPE_DIMENSIONS_MVector(builder, data)

def RFSGeometryRefAddSHAPE_DIMENSION_NAMES(builder, SHAPE_DIMENSION_NAMES):
    builder.PrependUOffsetTRelativeSlot(18, flatbuffers.number_types.UOffsetTFlags.py_type(SHAPE_DIMENSION_NAMES), 0)

def AddSHAPE_DIMENSION_NAMES(builder, SHAPE_DIMENSION_NAMES):
    RFSGeometryRefAddSHAPE_DIMENSION_NAMES(builder, SHAPE_DIMENSION_NAMES)

def RFSGeometryRefStartSHAPE_DIMENSION_NAMESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSHAPE_DIMENSION_NAMESVector(builder, numElems):
    return RFSGeometryRefStartSHAPE_DIMENSION_NAMESVector(builder, numElems)

def RFSGeometryRefCreateSHAPE_DIMENSION_NAMESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateSHAPE_DIMENSION_NAMESVector(builder, data):
    RFSGeometryRefCreateSHAPE_DIMENSION_NAMESVector(builder, data)

def RFSGeometryRefAddLATITUDE_DEG(builder, LATITUDE_DEG):
    builder.PrependFloat64Slot(19, LATITUDE_DEG, 0.0)

def AddLATITUDE_DEG(builder, LATITUDE_DEG):
    RFSGeometryRefAddLATITUDE_DEG(builder, LATITUDE_DEG)

def RFSGeometryRefAddLONGITUDE_DEG(builder, LONGITUDE_DEG):
    builder.PrependFloat64Slot(20, LONGITUDE_DEG, 0.0)

def AddLONGITUDE_DEG(builder, LONGITUDE_DEG):
    RFSGeometryRefAddLONGITUDE_DEG(builder, LONGITUDE_DEG)

def RFSGeometryRefAddALTITUDE_M(builder, ALTITUDE_M):
    builder.PrependFloat64Slot(21, ALTITUDE_M, 0.0)

def AddALTITUDE_M(builder, ALTITUDE_M):
    RFSGeometryRefAddALTITUDE_M(builder, ALTITUDE_M)

def RFSGeometryRefAddHEIGHT_M(builder, HEIGHT_M):
    builder.PrependFloat64Slot(22, HEIGHT_M, 0.0)

def AddHEIGHT_M(builder, HEIGHT_M):
    RFSGeometryRefAddHEIGHT_M(builder, HEIGHT_M)

def RFSGeometryRefAddNORMAL_AZIMUTH_DEG(builder, NORMAL_AZIMUTH_DEG):
    builder.PrependFloat64Slot(23, NORMAL_AZIMUTH_DEG, 0.0)

def AddNORMAL_AZIMUTH_DEG(builder, NORMAL_AZIMUTH_DEG):
    RFSGeometryRefAddNORMAL_AZIMUTH_DEG(builder, NORMAL_AZIMUTH_DEG)

def RFSGeometryRefAddNORMAL_ELEVATION_DEG(builder, NORMAL_ELEVATION_DEG):
    builder.PrependFloat64Slot(24, NORMAL_ELEVATION_DEG, 0.0)

def AddNORMAL_ELEVATION_DEG(builder, NORMAL_ELEVATION_DEG):
    RFSGeometryRefAddNORMAL_ELEVATION_DEG(builder, NORMAL_ELEVATION_DEG)

def RFSGeometryRefEnd(builder):
    return builder.EndObject()

def End(builder):
    return RFSGeometryRefEnd(builder)

try:
    from typing import List
except:
    pass

class RFSGeometryRefT(object):

    # RFSGeometryRefT
    def __init__(
        self,
        GEOMETRY_REF_ID = None,
        GEOMETRY_KIND = 0,
        VAM_ID = None,
        VARIANT_ID = None,
        ASSET_SHA256 = None,
        MESH_NAME = None,
        PRIMITIVE_INDEX = -1,
        GLTF_MATERIAL_NAME = None,
        GLTF_MATERIAL_INDEX = -1,
        FACE_INDEXES = None,
        OPP_SURFACE_ID = None,
        PANEL_ID = None,
        NORAD_CAT_ID = 0,
        OBJECT_ID = None,
        TBS_SITE_ID = None,
        FEATURE_ID = None,
        ANALYTIC_SHAPE = 0,
        SHAPE_DIMENSIONS_M = None,
        SHAPE_DIMENSION_NAMES = None,
        LATITUDE_DEG = 0.0,
        LONGITUDE_DEG = 0.0,
        ALTITUDE_M = 0.0,
        HEIGHT_M = 0.0,
        NORMAL_AZIMUTH_DEG = 0.0,
        NORMAL_ELEVATION_DEG = 0.0,
    ):
        self.GEOMETRY_REF_ID = GEOMETRY_REF_ID  # type: Optional[str]
        self.GEOMETRY_KIND = GEOMETRY_KIND  # type: int
        self.VAM_ID = VAM_ID  # type: Optional[str]
        self.VARIANT_ID = VARIANT_ID  # type: Optional[str]
        self.ASSET_SHA256 = ASSET_SHA256  # type: Optional[str]
        self.MESH_NAME = MESH_NAME  # type: Optional[str]
        self.PRIMITIVE_INDEX = PRIMITIVE_INDEX  # type: int
        self.GLTF_MATERIAL_NAME = GLTF_MATERIAL_NAME  # type: Optional[str]
        self.GLTF_MATERIAL_INDEX = GLTF_MATERIAL_INDEX  # type: int
        self.FACE_INDEXES = FACE_INDEXES  # type: Optional[List[int]]
        self.OPP_SURFACE_ID = OPP_SURFACE_ID  # type: Optional[str]
        self.PANEL_ID = PANEL_ID  # type: Optional[str]
        self.NORAD_CAT_ID = NORAD_CAT_ID  # type: int
        self.OBJECT_ID = OBJECT_ID  # type: Optional[str]
        self.TBS_SITE_ID = TBS_SITE_ID  # type: Optional[str]
        self.FEATURE_ID = FEATURE_ID  # type: Optional[str]
        self.ANALYTIC_SHAPE = ANALYTIC_SHAPE  # type: int
        self.SHAPE_DIMENSIONS_M = SHAPE_DIMENSIONS_M  # type: Optional[List[float]]
        self.SHAPE_DIMENSION_NAMES = SHAPE_DIMENSION_NAMES  # type: Optional[List[Optional[str]]]
        self.LATITUDE_DEG = LATITUDE_DEG  # type: float
        self.LONGITUDE_DEG = LONGITUDE_DEG  # type: float
        self.ALTITUDE_M = ALTITUDE_M  # type: float
        self.HEIGHT_M = HEIGHT_M  # type: float
        self.NORMAL_AZIMUTH_DEG = NORMAL_AZIMUTH_DEG  # type: float
        self.NORMAL_ELEVATION_DEG = NORMAL_ELEVATION_DEG  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpRfsgeometryRef = RFSGeometryRef()
        tmpRfsgeometryRef.Init(buf, pos)
        return cls.InitFromObj(tmpRfsgeometryRef)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpRfsgeometryRef):
        x = RFSGeometryRefT()
        x._UnPack(tmpRfsgeometryRef)
        return x

    # RFSGeometryRefT
    def _UnPack(self, RFSGeometryRef):
        if RFSGeometryRef is None:
            return
        self.GEOMETRY_REF_ID = RFSGeometryRef.GEOMETRY_REF_ID()
        self.GEOMETRY_KIND = RFSGeometryRef.GEOMETRY_KIND()
        self.VAM_ID = RFSGeometryRef.VAM_ID()
        self.VARIANT_ID = RFSGeometryRef.VARIANT_ID()
        self.ASSET_SHA256 = RFSGeometryRef.ASSET_SHA256()
        self.MESH_NAME = RFSGeometryRef.MESH_NAME()
        self.PRIMITIVE_INDEX = RFSGeometryRef.PRIMITIVE_INDEX()
        self.GLTF_MATERIAL_NAME = RFSGeometryRef.GLTF_MATERIAL_NAME()
        self.GLTF_MATERIAL_INDEX = RFSGeometryRef.GLTF_MATERIAL_INDEX()
        if not RFSGeometryRef.FACE_INDEXESIsNone():
            if np is None:
                self.FACE_INDEXES = []
                for i in range(RFSGeometryRef.FACE_INDEXESLength()):
                    self.FACE_INDEXES.append(RFSGeometryRef.FACE_INDEXES(i))
            else:
                self.FACE_INDEXES = RFSGeometryRef.FACE_INDEXESAsNumpy()
        self.OPP_SURFACE_ID = RFSGeometryRef.OPP_SURFACE_ID()
        self.PANEL_ID = RFSGeometryRef.PANEL_ID()
        self.NORAD_CAT_ID = RFSGeometryRef.NORAD_CAT_ID()
        self.OBJECT_ID = RFSGeometryRef.OBJECT_ID()
        self.TBS_SITE_ID = RFSGeometryRef.TBS_SITE_ID()
        self.FEATURE_ID = RFSGeometryRef.FEATURE_ID()
        self.ANALYTIC_SHAPE = RFSGeometryRef.ANALYTIC_SHAPE()
        if not RFSGeometryRef.SHAPE_DIMENSIONS_MIsNone():
            if np is None:
                self.SHAPE_DIMENSIONS_M = []
                for i in range(RFSGeometryRef.SHAPE_DIMENSIONS_MLength()):
                    self.SHAPE_DIMENSIONS_M.append(RFSGeometryRef.SHAPE_DIMENSIONS_M(i))
            else:
                self.SHAPE_DIMENSIONS_M = RFSGeometryRef.SHAPE_DIMENSIONS_MAsNumpy()
        if not RFSGeometryRef.SHAPE_DIMENSION_NAMESIsNone():
            self.SHAPE_DIMENSION_NAMES = []
            for i in range(RFSGeometryRef.SHAPE_DIMENSION_NAMESLength()):
                self.SHAPE_DIMENSION_NAMES.append(RFSGeometryRef.SHAPE_DIMENSION_NAMES(i))
        self.LATITUDE_DEG = RFSGeometryRef.LATITUDE_DEG()
        self.LONGITUDE_DEG = RFSGeometryRef.LONGITUDE_DEG()
        self.ALTITUDE_M = RFSGeometryRef.ALTITUDE_M()
        self.HEIGHT_M = RFSGeometryRef.HEIGHT_M()
        self.NORMAL_AZIMUTH_DEG = RFSGeometryRef.NORMAL_AZIMUTH_DEG()
        self.NORMAL_ELEVATION_DEG = RFSGeometryRef.NORMAL_ELEVATION_DEG()

    # RFSGeometryRefT
    def Pack(self, builder):
        if self.GEOMETRY_REF_ID is not None:
            GEOMETRY_REF_ID = builder.CreateString(self.GEOMETRY_REF_ID)
        if self.VAM_ID is not None:
            VAM_ID = builder.CreateString(self.VAM_ID)
        if self.VARIANT_ID is not None:
            VARIANT_ID = builder.CreateString(self.VARIANT_ID)
        if self.ASSET_SHA256 is not None:
            ASSET_SHA256 = builder.CreateString(self.ASSET_SHA256)
        if self.MESH_NAME is not None:
            MESH_NAME = builder.CreateString(self.MESH_NAME)
        if self.GLTF_MATERIAL_NAME is not None:
            GLTF_MATERIAL_NAME = builder.CreateString(self.GLTF_MATERIAL_NAME)
        if self.FACE_INDEXES is not None:
            if np is not None and type(self.FACE_INDEXES) is np.ndarray:
                FACE_INDEXES = builder.CreateNumpyVector(self.FACE_INDEXES)
            else:
                RFSGeometryRefStartFACE_INDEXESVector(builder, len(self.FACE_INDEXES))
                for i in reversed(range(len(self.FACE_INDEXES))):
                    builder.PrependUint32(self.FACE_INDEXES[i])
                FACE_INDEXES = builder.EndVector()
        if self.OPP_SURFACE_ID is not None:
            OPP_SURFACE_ID = builder.CreateString(self.OPP_SURFACE_ID)
        if self.PANEL_ID is not None:
            PANEL_ID = builder.CreateString(self.PANEL_ID)
        if self.OBJECT_ID is not None:
            OBJECT_ID = builder.CreateString(self.OBJECT_ID)
        if self.TBS_SITE_ID is not None:
            TBS_SITE_ID = builder.CreateString(self.TBS_SITE_ID)
        if self.FEATURE_ID is not None:
            FEATURE_ID = builder.CreateString(self.FEATURE_ID)
        if self.SHAPE_DIMENSIONS_M is not None:
            if np is not None and type(self.SHAPE_DIMENSIONS_M) is np.ndarray:
                SHAPE_DIMENSIONS_M = builder.CreateNumpyVector(self.SHAPE_DIMENSIONS_M)
            else:
                RFSGeometryRefStartSHAPE_DIMENSIONS_MVector(builder, len(self.SHAPE_DIMENSIONS_M))
                for i in reversed(range(len(self.SHAPE_DIMENSIONS_M))):
                    builder.PrependFloat64(self.SHAPE_DIMENSIONS_M[i])
                SHAPE_DIMENSIONS_M = builder.EndVector()
        if self.SHAPE_DIMENSION_NAMES is not None:
            SHAPE_DIMENSION_NAMESlist = []
            for i in range(len(self.SHAPE_DIMENSION_NAMES)):
                SHAPE_DIMENSION_NAMESlist.append(builder.CreateString(self.SHAPE_DIMENSION_NAMES[i]))
            RFSGeometryRefStartSHAPE_DIMENSION_NAMESVector(builder, len(self.SHAPE_DIMENSION_NAMES))
            for i in reversed(range(len(self.SHAPE_DIMENSION_NAMES))):
                builder.PrependUOffsetTRelative(SHAPE_DIMENSION_NAMESlist[i])
            SHAPE_DIMENSION_NAMES = builder.EndVector()
        RFSGeometryRefStart(builder)
        if self.GEOMETRY_REF_ID is not None:
            RFSGeometryRefAddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID)
        RFSGeometryRefAddGEOMETRY_KIND(builder, self.GEOMETRY_KIND)
        if self.VAM_ID is not None:
            RFSGeometryRefAddVAM_ID(builder, VAM_ID)
        if self.VARIANT_ID is not None:
            RFSGeometryRefAddVARIANT_ID(builder, VARIANT_ID)
        if self.ASSET_SHA256 is not None:
            RFSGeometryRefAddASSET_SHA256(builder, ASSET_SHA256)
        if self.MESH_NAME is not None:
            RFSGeometryRefAddMESH_NAME(builder, MESH_NAME)
        RFSGeometryRefAddPRIMITIVE_INDEX(builder, self.PRIMITIVE_INDEX)
        if self.GLTF_MATERIAL_NAME is not None:
            RFSGeometryRefAddGLTF_MATERIAL_NAME(builder, GLTF_MATERIAL_NAME)
        RFSGeometryRefAddGLTF_MATERIAL_INDEX(builder, self.GLTF_MATERIAL_INDEX)
        if self.FACE_INDEXES is not None:
            RFSGeometryRefAddFACE_INDEXES(builder, FACE_INDEXES)
        if self.OPP_SURFACE_ID is not None:
            RFSGeometryRefAddOPP_SURFACE_ID(builder, OPP_SURFACE_ID)
        if self.PANEL_ID is not None:
            RFSGeometryRefAddPANEL_ID(builder, PANEL_ID)
        RFSGeometryRefAddNORAD_CAT_ID(builder, self.NORAD_CAT_ID)
        if self.OBJECT_ID is not None:
            RFSGeometryRefAddOBJECT_ID(builder, OBJECT_ID)
        if self.TBS_SITE_ID is not None:
            RFSGeometryRefAddTBS_SITE_ID(builder, TBS_SITE_ID)
        if self.FEATURE_ID is not None:
            RFSGeometryRefAddFEATURE_ID(builder, FEATURE_ID)
        RFSGeometryRefAddANALYTIC_SHAPE(builder, self.ANALYTIC_SHAPE)
        if self.SHAPE_DIMENSIONS_M is not None:
            RFSGeometryRefAddSHAPE_DIMENSIONS_M(builder, SHAPE_DIMENSIONS_M)
        if self.SHAPE_DIMENSION_NAMES is not None:
            RFSGeometryRefAddSHAPE_DIMENSION_NAMES(builder, SHAPE_DIMENSION_NAMES)
        RFSGeometryRefAddLATITUDE_DEG(builder, self.LATITUDE_DEG)
        RFSGeometryRefAddLONGITUDE_DEG(builder, self.LONGITUDE_DEG)
        RFSGeometryRefAddALTITUDE_M(builder, self.ALTITUDE_M)
        RFSGeometryRefAddHEIGHT_M(builder, self.HEIGHT_M)
        RFSGeometryRefAddNORMAL_AZIMUTH_DEG(builder, self.NORMAL_AZIMUTH_DEG)
        RFSGeometryRefAddNORMAL_ELEVATION_DEG(builder, self.NORMAL_ELEVATION_DEG)
        RFSGeometryRef = RFSGeometryRefEnd(builder)
        return RFSGeometryRef
