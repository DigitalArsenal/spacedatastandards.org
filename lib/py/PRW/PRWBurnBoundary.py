# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Scheduled time gates an optional condition. At least one must be present.
class PRWBurnBoundary(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWBurnBoundary()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWBurnBoundary(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWBurnBoundaryBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWBurnBoundary
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWBurnBoundary
    def ELAPSED_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # True when ELAPSED_SECONDS carries a value; false means absent.
    # PRWBurnBoundary
    def HAS_ELAPSED_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # SI PCE predicate; direction and goal tolerance explicitly populated.
    # PRWBurnBoundary
    def CONDITION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PCEParameterCondition import PCEParameterCondition
            obj = PCEParameterCondition()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def PRWBurnBoundaryStart(builder):
    builder.StartObject(3)

def Start(builder):
    PRWBurnBoundaryStart(builder)

def PRWBurnBoundaryAddELAPSED_SECONDS(builder, ELAPSED_SECONDS):
    builder.PrependFloat64Slot(0, ELAPSED_SECONDS, 0.0)

def AddELAPSED_SECONDS(builder, ELAPSED_SECONDS):
    PRWBurnBoundaryAddELAPSED_SECONDS(builder, ELAPSED_SECONDS)

def PRWBurnBoundaryAddHAS_ELAPSED_SECONDS(builder, HAS_ELAPSED_SECONDS):
    builder.PrependBoolSlot(1, HAS_ELAPSED_SECONDS, 0)

def AddHAS_ELAPSED_SECONDS(builder, HAS_ELAPSED_SECONDS):
    PRWBurnBoundaryAddHAS_ELAPSED_SECONDS(builder, HAS_ELAPSED_SECONDS)

def PRWBurnBoundaryAddCONDITION(builder, CONDITION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(CONDITION), 0)

def AddCONDITION(builder, CONDITION):
    PRWBurnBoundaryAddCONDITION(builder, CONDITION)

def PRWBurnBoundaryEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWBurnBoundaryEnd(builder)

import PCEParameterCondition
try:
    from typing import Optional
except:
    pass

class PRWBurnBoundaryT(object):

    # PRWBurnBoundaryT
    def __init__(
        self,
        ELAPSED_SECONDS = 0.0,
        HAS_ELAPSED_SECONDS = False,
        CONDITION = None,
    ):
        self.ELAPSED_SECONDS = ELAPSED_SECONDS  # type: float
        self.HAS_ELAPSED_SECONDS = HAS_ELAPSED_SECONDS  # type: bool
        self.CONDITION = CONDITION  # type: Optional[PCEParameterCondition.PCEParameterConditionT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwburnBoundary = PRWBurnBoundary()
        tmpPrwburnBoundary.Init(buf, pos)
        return cls.InitFromObj(tmpPrwburnBoundary)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwburnBoundary):
        x = PRWBurnBoundaryT()
        x._UnPack(tmpPrwburnBoundary)
        return x

    # PRWBurnBoundaryT
    def _UnPack(self, PRWBurnBoundary):
        if PRWBurnBoundary is None:
            return
        self.ELAPSED_SECONDS = PRWBurnBoundary.ELAPSED_SECONDS()
        self.HAS_ELAPSED_SECONDS = PRWBurnBoundary.HAS_ELAPSED_SECONDS()
        if PRWBurnBoundary.CONDITION() is not None:
            self.CONDITION = PCEParameterCondition.PCEParameterConditionT.InitFromObj(PRWBurnBoundary.CONDITION())

    # PRWBurnBoundaryT
    def Pack(self, builder):
        if self.CONDITION is not None:
            CONDITION = self.CONDITION.Pack(builder)
        PRWBurnBoundaryStart(builder)
        PRWBurnBoundaryAddELAPSED_SECONDS(builder, self.ELAPSED_SECONDS)
        PRWBurnBoundaryAddHAS_ELAPSED_SECONDS(builder, self.HAS_ELAPSED_SECONDS)
        if self.CONDITION is not None:
            PRWBurnBoundaryAddCONDITION(builder, CONDITION)
        PRWBurnBoundary = PRWBurnBoundaryEnd(builder)
        return PRWBurnBoundary
