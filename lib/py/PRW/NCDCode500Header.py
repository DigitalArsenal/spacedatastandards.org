# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Header words of a fixed-record binary ephemeris container.
class NCDCode500Header(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NCDCode500Header()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNCDCode500Header(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NCDCode500HeaderBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x43\x44", size_prefixed=size_prefixed)

    # NCDCode500Header
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Object name as recorded in the header.
    # NCDCode500Header
    def SATELLITE_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Container-internal tape or file identifier.
    # NCDCode500Header
    def TAPE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Time system indicator as recorded.
    # NCDCode500Header
    def TIME_SYSTEM_INDICATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Coordinate system indicator as recorded.
    # NCDCode500Header
    def COORDINATE_SYSTEM_INDICATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Coverage start, ISO 8601.
    # NCDCode500Header
    def START_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Coverage stop, ISO 8601.
    # NCDCode500Header
    def STOP_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Uniform step between data records, seconds.
    # NCDCode500Header
    def EPOCH_STEP_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # The header record's packed words in file order, undecoded. A reader that
    # understands a word decodes it; a rewriter reproduces the record exactly
    # without having to.
    # NCDCode500Header
    def HEADER_WORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # NCDCode500Header
    def HEADER_WORDSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # NCDCode500Header
    def HEADER_WORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NCDCode500Header
    def HEADER_WORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

def NCDCode500HeaderStart(builder):
    builder.StartObject(8)

def Start(builder):
    NCDCode500HeaderStart(builder)

def NCDCode500HeaderAddSATELLITE_NAME(builder, SATELLITE_NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SATELLITE_NAME), 0)

def AddSATELLITE_NAME(builder, SATELLITE_NAME):
    NCDCode500HeaderAddSATELLITE_NAME(builder, SATELLITE_NAME)

def NCDCode500HeaderAddTAPE_ID(builder, TAPE_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(TAPE_ID), 0)

def AddTAPE_ID(builder, TAPE_ID):
    NCDCode500HeaderAddTAPE_ID(builder, TAPE_ID)

def NCDCode500HeaderAddTIME_SYSTEM_INDICATOR(builder, TIME_SYSTEM_INDICATOR):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(TIME_SYSTEM_INDICATOR), 0)

def AddTIME_SYSTEM_INDICATOR(builder, TIME_SYSTEM_INDICATOR):
    NCDCode500HeaderAddTIME_SYSTEM_INDICATOR(builder, TIME_SYSTEM_INDICATOR)

def NCDCode500HeaderAddCOORDINATE_SYSTEM_INDICATOR(builder, COORDINATE_SYSTEM_INDICATOR):
    builder.PrependInt32Slot(3, COORDINATE_SYSTEM_INDICATOR, 0)

def AddCOORDINATE_SYSTEM_INDICATOR(builder, COORDINATE_SYSTEM_INDICATOR):
    NCDCode500HeaderAddCOORDINATE_SYSTEM_INDICATOR(builder, COORDINATE_SYSTEM_INDICATOR)

def NCDCode500HeaderAddSTART_EPOCH(builder, START_EPOCH):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(START_EPOCH), 0)

def AddSTART_EPOCH(builder, START_EPOCH):
    NCDCode500HeaderAddSTART_EPOCH(builder, START_EPOCH)

def NCDCode500HeaderAddSTOP_EPOCH(builder, STOP_EPOCH):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(STOP_EPOCH), 0)

def AddSTOP_EPOCH(builder, STOP_EPOCH):
    NCDCode500HeaderAddSTOP_EPOCH(builder, STOP_EPOCH)

def NCDCode500HeaderAddEPOCH_STEP_SECONDS(builder, EPOCH_STEP_SECONDS):
    builder.PrependFloat64Slot(6, EPOCH_STEP_SECONDS, 0.0)

def AddEPOCH_STEP_SECONDS(builder, EPOCH_STEP_SECONDS):
    NCDCode500HeaderAddEPOCH_STEP_SECONDS(builder, EPOCH_STEP_SECONDS)

def NCDCode500HeaderAddHEADER_WORDS(builder, HEADER_WORDS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(HEADER_WORDS), 0)

def AddHEADER_WORDS(builder, HEADER_WORDS):
    NCDCode500HeaderAddHEADER_WORDS(builder, HEADER_WORDS)

def NCDCode500HeaderStartHEADER_WORDSVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartHEADER_WORDSVector(builder, numElems):
    return NCDCode500HeaderStartHEADER_WORDSVector(builder, numElems)

def NCDCode500HeaderCreateHEADER_WORDSVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateHEADER_WORDSVector(builder, data):
    NCDCode500HeaderCreateHEADER_WORDSVector(builder, data)

def NCDCode500HeaderEnd(builder):
    return builder.EndObject()

def End(builder):
    return NCDCode500HeaderEnd(builder)

try:
    from typing import List
except:
    pass

class NCDCode500HeaderT(object):

    # NCDCode500HeaderT
    def __init__(
        self,
        SATELLITE_NAME = None,
        TAPE_ID = None,
        TIME_SYSTEM_INDICATOR = None,
        COORDINATE_SYSTEM_INDICATOR = 0,
        START_EPOCH = None,
        STOP_EPOCH = None,
        EPOCH_STEP_SECONDS = 0.0,
        HEADER_WORDS = None,
    ):
        self.SATELLITE_NAME = SATELLITE_NAME  # type: Optional[str]
        self.TAPE_ID = TAPE_ID  # type: Optional[str]
        self.TIME_SYSTEM_INDICATOR = TIME_SYSTEM_INDICATOR  # type: Optional[str]
        self.COORDINATE_SYSTEM_INDICATOR = COORDINATE_SYSTEM_INDICATOR  # type: int
        self.START_EPOCH = START_EPOCH  # type: Optional[str]
        self.STOP_EPOCH = STOP_EPOCH  # type: Optional[str]
        self.EPOCH_STEP_SECONDS = EPOCH_STEP_SECONDS  # type: float
        self.HEADER_WORDS = HEADER_WORDS  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNcdcode500Header = NCDCode500Header()
        tmpNcdcode500Header.Init(buf, pos)
        return cls.InitFromObj(tmpNcdcode500Header)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNcdcode500Header):
        x = NCDCode500HeaderT()
        x._UnPack(tmpNcdcode500Header)
        return x

    # NCDCode500HeaderT
    def _UnPack(self, NCDCode500Header):
        if NCDCode500Header is None:
            return
        self.SATELLITE_NAME = NCDCode500Header.SATELLITE_NAME()
        self.TAPE_ID = NCDCode500Header.TAPE_ID()
        self.TIME_SYSTEM_INDICATOR = NCDCode500Header.TIME_SYSTEM_INDICATOR()
        self.COORDINATE_SYSTEM_INDICATOR = NCDCode500Header.COORDINATE_SYSTEM_INDICATOR()
        self.START_EPOCH = NCDCode500Header.START_EPOCH()
        self.STOP_EPOCH = NCDCode500Header.STOP_EPOCH()
        self.EPOCH_STEP_SECONDS = NCDCode500Header.EPOCH_STEP_SECONDS()
        if not NCDCode500Header.HEADER_WORDSIsNone():
            if np is None:
                self.HEADER_WORDS = []
                for i in range(NCDCode500Header.HEADER_WORDSLength()):
                    self.HEADER_WORDS.append(NCDCode500Header.HEADER_WORDS(i))
            else:
                self.HEADER_WORDS = NCDCode500Header.HEADER_WORDSAsNumpy()

    # NCDCode500HeaderT
    def Pack(self, builder):
        if self.SATELLITE_NAME is not None:
            SATELLITE_NAME = builder.CreateString(self.SATELLITE_NAME)
        if self.TAPE_ID is not None:
            TAPE_ID = builder.CreateString(self.TAPE_ID)
        if self.TIME_SYSTEM_INDICATOR is not None:
            TIME_SYSTEM_INDICATOR = builder.CreateString(self.TIME_SYSTEM_INDICATOR)
        if self.START_EPOCH is not None:
            START_EPOCH = builder.CreateString(self.START_EPOCH)
        if self.STOP_EPOCH is not None:
            STOP_EPOCH = builder.CreateString(self.STOP_EPOCH)
        if self.HEADER_WORDS is not None:
            if np is not None and type(self.HEADER_WORDS) is np.ndarray:
                HEADER_WORDS = builder.CreateNumpyVector(self.HEADER_WORDS)
            else:
                NCDCode500HeaderStartHEADER_WORDSVector(builder, len(self.HEADER_WORDS))
                for i in reversed(range(len(self.HEADER_WORDS))):
                    builder.PrependFloat64(self.HEADER_WORDS[i])
                HEADER_WORDS = builder.EndVector()
        NCDCode500HeaderStart(builder)
        if self.SATELLITE_NAME is not None:
            NCDCode500HeaderAddSATELLITE_NAME(builder, SATELLITE_NAME)
        if self.TAPE_ID is not None:
            NCDCode500HeaderAddTAPE_ID(builder, TAPE_ID)
        if self.TIME_SYSTEM_INDICATOR is not None:
            NCDCode500HeaderAddTIME_SYSTEM_INDICATOR(builder, TIME_SYSTEM_INDICATOR)
        NCDCode500HeaderAddCOORDINATE_SYSTEM_INDICATOR(builder, self.COORDINATE_SYSTEM_INDICATOR)
        if self.START_EPOCH is not None:
            NCDCode500HeaderAddSTART_EPOCH(builder, START_EPOCH)
        if self.STOP_EPOCH is not None:
            NCDCode500HeaderAddSTOP_EPOCH(builder, STOP_EPOCH)
        NCDCode500HeaderAddEPOCH_STEP_SECONDS(builder, self.EPOCH_STEP_SECONDS)
        if self.HEADER_WORDS is not None:
            NCDCode500HeaderAddHEADER_WORDS(builder, HEADER_WORDS)
        NCDCode500Header = NCDCode500HeaderEnd(builder)
        return NCDCode500Header
