# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Row-major square matrix on [x,y,z,vx,vy,vz,(mass)], SI state units.
# Covariance entries have row*column units; STM entries row/column units.
class PRWStateMatrix(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWStateMatrix()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWStateMatrix(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWStateMatrixBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWStateMatrix
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWStateMatrix
    def DIMENSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # PRWStateMatrix
    def VALUES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # PRWStateMatrix
    def VALUESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # PRWStateMatrix
    def VALUESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWStateMatrix
    def VALUESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def PRWStateMatrixStart(builder):
    builder.StartObject(2)

def Start(builder):
    PRWStateMatrixStart(builder)

def PRWStateMatrixAddDIMENSION(builder, DIMENSION):
    builder.PrependUint8Slot(0, DIMENSION, 0)

def AddDIMENSION(builder, DIMENSION):
    PRWStateMatrixAddDIMENSION(builder, DIMENSION)

def PRWStateMatrixAddVALUES(builder, VALUES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VALUES), 0)

def AddVALUES(builder, VALUES):
    PRWStateMatrixAddVALUES(builder, VALUES)

def PRWStateMatrixStartVALUESVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartVALUESVector(builder, numElems):
    return PRWStateMatrixStartVALUESVector(builder, numElems)

def PRWStateMatrixCreateVALUESVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateVALUESVector(builder, data):
    PRWStateMatrixCreateVALUESVector(builder, data)

def PRWStateMatrixEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWStateMatrixEnd(builder)

try:
    from typing import List
except:
    pass

class PRWStateMatrixT(object):

    # PRWStateMatrixT
    def __init__(
        self,
        DIMENSION = 0,
        VALUES = None,
    ):
        self.DIMENSION = DIMENSION  # type: int
        self.VALUES = VALUES  # type: Optional[List[float]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwstateMatrix = PRWStateMatrix()
        tmpPrwstateMatrix.Init(buf, pos)
        return cls.InitFromObj(tmpPrwstateMatrix)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwstateMatrix):
        x = PRWStateMatrixT()
        x._UnPack(tmpPrwstateMatrix)
        return x

    # PRWStateMatrixT
    def _UnPack(self, PRWStateMatrix):
        if PRWStateMatrix is None:
            return
        self.DIMENSION = PRWStateMatrix.DIMENSION()
        if not PRWStateMatrix.VALUESIsNone():
            if np is None:
                self.VALUES = []
                for i in range(PRWStateMatrix.VALUESLength()):
                    self.VALUES.append(PRWStateMatrix.VALUES(i))
            else:
                self.VALUES = PRWStateMatrix.VALUESAsNumpy()

    # PRWStateMatrixT
    def Pack(self, builder):
        if self.VALUES is not None:
            if np is not None and type(self.VALUES) is np.ndarray:
                VALUES = builder.CreateNumpyVector(self.VALUES)
            else:
                PRWStateMatrixStartVALUESVector(builder, len(self.VALUES))
                for i in reversed(range(len(self.VALUES))):
                    builder.PrependFloat64(self.VALUES[i])
                VALUES = builder.EndVector()
        PRWStateMatrixStart(builder)
        PRWStateMatrixAddDIMENSION(builder, self.DIMENSION)
        if self.VALUES is not None:
            PRWStateMatrixAddVALUES(builder, VALUES)
        PRWStateMatrix = PRWStateMatrixEnd(builder)
        return PRWStateMatrix
