# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Batch propagation request — propagate every entity in ENTITY_HANDLES[] to
# EPOCH and write the result StateVector stream to OUTPUT_OFFSET in the
# shared arena.
class PRWBatchRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWBatchRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWBatchRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWBatchRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWBatchRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Target epoch as a Julian date (TIME_SYSTEM is configured on the host).
    # PRWBatchRequest
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Entity handles to propagate (empty = all initialized entities).
    # PRWBatchRequest
    def ENTITY_HANDLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # PRWBatchRequest
    def ENTITY_HANDLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # PRWBatchRequest
    def ENTITY_HANDLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWBatchRequest
    def ENTITY_HANDLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Output buffer offset in the arena where the StateVector stream begins.
    # PRWBatchRequest
    def OUTPUT_OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Maximum entities to process in this call (0 = unbounded).
    # PRWBatchRequest
    def MAX_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Target reference frame for the output state stream. Matches enum
    # values in SDS `RFM`. If zero, the propagator chooses its native frame.
    # PRWBatchRequest
    def TARGET_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PRWBatchRequestStart(builder):
    builder.StartObject(5)

def Start(builder):
    PRWBatchRequestStart(builder)

def PRWBatchRequestAddEPOCH(builder, EPOCH):
    builder.PrependFloat64Slot(0, EPOCH, 0.0)

def AddEPOCH(builder, EPOCH):
    PRWBatchRequestAddEPOCH(builder, EPOCH)

def PRWBatchRequestAddENTITY_HANDLES(builder, ENTITY_HANDLES):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ENTITY_HANDLES), 0)

def AddENTITY_HANDLES(builder, ENTITY_HANDLES):
    PRWBatchRequestAddENTITY_HANDLES(builder, ENTITY_HANDLES)

def PRWBatchRequestStartENTITY_HANDLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartENTITY_HANDLESVector(builder, numElems):
    return PRWBatchRequestStartENTITY_HANDLESVector(builder, numElems)

def PRWBatchRequestCreateENTITY_HANDLESVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateENTITY_HANDLESVector(builder, data):
    PRWBatchRequestCreateENTITY_HANDLESVector(builder, data)

def PRWBatchRequestAddOUTPUT_OFFSET(builder, OUTPUT_OFFSET):
    builder.PrependUint32Slot(2, OUTPUT_OFFSET, 0)

def AddOUTPUT_OFFSET(builder, OUTPUT_OFFSET):
    PRWBatchRequestAddOUTPUT_OFFSET(builder, OUTPUT_OFFSET)

def PRWBatchRequestAddMAX_COUNT(builder, MAX_COUNT):
    builder.PrependUint32Slot(3, MAX_COUNT, 0)

def AddMAX_COUNT(builder, MAX_COUNT):
    PRWBatchRequestAddMAX_COUNT(builder, MAX_COUNT)

def PRWBatchRequestAddTARGET_FRAME(builder, TARGET_FRAME):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(TARGET_FRAME), 0)

def AddTARGET_FRAME(builder, TARGET_FRAME):
    PRWBatchRequestAddTARGET_FRAME(builder, TARGET_FRAME)

def PRWBatchRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWBatchRequestEnd(builder)

try:
    from typing import List
except:
    pass

class PRWBatchRequestT(object):

    # PRWBatchRequestT
    def __init__(
        self,
        EPOCH = 0.0,
        ENTITY_HANDLES = None,
        OUTPUT_OFFSET = 0,
        MAX_COUNT = 0,
        TARGET_FRAME = None,
    ):
        self.EPOCH = EPOCH  # type: float
        self.ENTITY_HANDLES = ENTITY_HANDLES  # type: Optional[List[int]]
        self.OUTPUT_OFFSET = OUTPUT_OFFSET  # type: int
        self.MAX_COUNT = MAX_COUNT  # type: int
        self.TARGET_FRAME = TARGET_FRAME  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwbatchRequest = PRWBatchRequest()
        tmpPrwbatchRequest.Init(buf, pos)
        return cls.InitFromObj(tmpPrwbatchRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwbatchRequest):
        x = PRWBatchRequestT()
        x._UnPack(tmpPrwbatchRequest)
        return x

    # PRWBatchRequestT
    def _UnPack(self, PRWBatchRequest):
        if PRWBatchRequest is None:
            return
        self.EPOCH = PRWBatchRequest.EPOCH()
        if not PRWBatchRequest.ENTITY_HANDLESIsNone():
            if np is None:
                self.ENTITY_HANDLES = []
                for i in range(PRWBatchRequest.ENTITY_HANDLESLength()):
                    self.ENTITY_HANDLES.append(PRWBatchRequest.ENTITY_HANDLES(i))
            else:
                self.ENTITY_HANDLES = PRWBatchRequest.ENTITY_HANDLESAsNumpy()
        self.OUTPUT_OFFSET = PRWBatchRequest.OUTPUT_OFFSET()
        self.MAX_COUNT = PRWBatchRequest.MAX_COUNT()
        self.TARGET_FRAME = PRWBatchRequest.TARGET_FRAME()

    # PRWBatchRequestT
    def Pack(self, builder):
        if self.ENTITY_HANDLES is not None:
            if np is not None and type(self.ENTITY_HANDLES) is np.ndarray:
                ENTITY_HANDLES = builder.CreateNumpyVector(self.ENTITY_HANDLES)
            else:
                PRWBatchRequestStartENTITY_HANDLESVector(builder, len(self.ENTITY_HANDLES))
                for i in reversed(range(len(self.ENTITY_HANDLES))):
                    builder.PrependUint32(self.ENTITY_HANDLES[i])
                ENTITY_HANDLES = builder.EndVector()
        if self.TARGET_FRAME is not None:
            TARGET_FRAME = builder.CreateString(self.TARGET_FRAME)
        PRWBatchRequestStart(builder)
        PRWBatchRequestAddEPOCH(builder, self.EPOCH)
        if self.ENTITY_HANDLES is not None:
            PRWBatchRequestAddENTITY_HANDLES(builder, ENTITY_HANDLES)
        PRWBatchRequestAddOUTPUT_OFFSET(builder, self.OUTPUT_OFFSET)
        PRWBatchRequestAddMAX_COUNT(builder, self.MAX_COUNT)
        if self.TARGET_FRAME is not None:
            PRWBatchRequestAddTARGET_FRAME(builder, TARGET_FRAME)
        PRWBatchRequest = PRWBatchRequestEnd(builder)
        return PRWBatchRequest
