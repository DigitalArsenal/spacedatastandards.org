# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# All epochs explicit; duration seconds >=0; empty handles means all resident.
class PRWPrepareRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWPrepareRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWPrepareRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWPrepareRequestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWPrepareRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PRWPrepareRequest
    def INSTANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PRWInstance import PRWInstance
            obj = PRWInstance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWPrepareRequest
    def CATALOG_HANDLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # PRWPrepareRequest
    def SOURCE_HANDLES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # PRWPrepareRequest
    def SOURCE_HANDLESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # PRWPrepareRequest
    def SOURCE_HANDLESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWPrepareRequest
    def SOURCE_HANDLESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # PRWPrepareRequest
    def START_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TIMInstant import TIMInstant
            obj = TIMInstant()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PRWPrepareRequest
    def DURATION_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Provider-advertised profile name; unrecognized nonempty values fail.
    # PRWPrepareRequest
    def PROFILE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PRWPrepareRequestStart(builder):
    builder.StartObject(6)

def Start(builder):
    PRWPrepareRequestStart(builder)

def PRWPrepareRequestAddINSTANCE(builder, INSTANCE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(INSTANCE), 0)

def AddINSTANCE(builder, INSTANCE):
    PRWPrepareRequestAddINSTANCE(builder, INSTANCE)

def PRWPrepareRequestAddCATALOG_HANDLE(builder, CATALOG_HANDLE):
    builder.PrependUint32Slot(1, CATALOG_HANDLE, 0)

def AddCATALOG_HANDLE(builder, CATALOG_HANDLE):
    PRWPrepareRequestAddCATALOG_HANDLE(builder, CATALOG_HANDLE)

def PRWPrepareRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_HANDLES), 0)

def AddSOURCE_HANDLES(builder, SOURCE_HANDLES):
    PRWPrepareRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES)

def PRWPrepareRequestStartSOURCE_HANDLESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSOURCE_HANDLESVector(builder, numElems):
    return PRWPrepareRequestStartSOURCE_HANDLESVector(builder, numElems)

def PRWPrepareRequestCreateSOURCE_HANDLESVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateSOURCE_HANDLESVector(builder, data):
    PRWPrepareRequestCreateSOURCE_HANDLESVector(builder, data)

def PRWPrepareRequestAddSTART_EPOCH(builder, START_EPOCH):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(START_EPOCH), 0)

def AddSTART_EPOCH(builder, START_EPOCH):
    PRWPrepareRequestAddSTART_EPOCH(builder, START_EPOCH)

def PRWPrepareRequestAddDURATION_SECONDS(builder, DURATION_SECONDS):
    builder.PrependFloat64Slot(4, DURATION_SECONDS, 0.0)

def AddDURATION_SECONDS(builder, DURATION_SECONDS):
    PRWPrepareRequestAddDURATION_SECONDS(builder, DURATION_SECONDS)

def PRWPrepareRequestAddPROFILE(builder, PROFILE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PROFILE), 0)

def AddPROFILE(builder, PROFILE):
    PRWPrepareRequestAddPROFILE(builder, PROFILE)

def PRWPrepareRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWPrepareRequestEnd(builder)

import PRWInstance
import TIMInstant
try:
    from typing import List, Optional
except:
    pass

class PRWPrepareRequestT(object):

    # PRWPrepareRequestT
    def __init__(
        self,
        INSTANCE = None,
        CATALOG_HANDLE = 0,
        SOURCE_HANDLES = None,
        START_EPOCH = None,
        DURATION_SECONDS = 0.0,
        PROFILE = None,
    ):
        self.INSTANCE = INSTANCE  # type: Optional[PRWInstance.PRWInstanceT]
        self.CATALOG_HANDLE = CATALOG_HANDLE  # type: int
        self.SOURCE_HANDLES = SOURCE_HANDLES  # type: Optional[List[int]]
        self.START_EPOCH = START_EPOCH  # type: Optional[TIMInstant.TIMInstantT]
        self.DURATION_SECONDS = DURATION_SECONDS  # type: float
        self.PROFILE = PROFILE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwprepareRequest = PRWPrepareRequest()
        tmpPrwprepareRequest.Init(buf, pos)
        return cls.InitFromObj(tmpPrwprepareRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwprepareRequest):
        x = PRWPrepareRequestT()
        x._UnPack(tmpPrwprepareRequest)
        return x

    # PRWPrepareRequestT
    def _UnPack(self, PRWPrepareRequest):
        if PRWPrepareRequest is None:
            return
        if PRWPrepareRequest.INSTANCE() is not None:
            self.INSTANCE = PRWInstance.PRWInstanceT.InitFromObj(PRWPrepareRequest.INSTANCE())
        self.CATALOG_HANDLE = PRWPrepareRequest.CATALOG_HANDLE()
        if not PRWPrepareRequest.SOURCE_HANDLESIsNone():
            if np is None:
                self.SOURCE_HANDLES = []
                for i in range(PRWPrepareRequest.SOURCE_HANDLESLength()):
                    self.SOURCE_HANDLES.append(PRWPrepareRequest.SOURCE_HANDLES(i))
            else:
                self.SOURCE_HANDLES = PRWPrepareRequest.SOURCE_HANDLESAsNumpy()
        if PRWPrepareRequest.START_EPOCH() is not None:
            self.START_EPOCH = TIMInstant.TIMInstantT.InitFromObj(PRWPrepareRequest.START_EPOCH())
        self.DURATION_SECONDS = PRWPrepareRequest.DURATION_SECONDS()
        self.PROFILE = PRWPrepareRequest.PROFILE()

    # PRWPrepareRequestT
    def Pack(self, builder):
        if self.INSTANCE is not None:
            INSTANCE = self.INSTANCE.Pack(builder)
        if self.SOURCE_HANDLES is not None:
            if np is not None and type(self.SOURCE_HANDLES) is np.ndarray:
                SOURCE_HANDLES = builder.CreateNumpyVector(self.SOURCE_HANDLES)
            else:
                PRWPrepareRequestStartSOURCE_HANDLESVector(builder, len(self.SOURCE_HANDLES))
                for i in reversed(range(len(self.SOURCE_HANDLES))):
                    builder.PrependUint32(self.SOURCE_HANDLES[i])
                SOURCE_HANDLES = builder.EndVector()
        if self.START_EPOCH is not None:
            START_EPOCH = self.START_EPOCH.Pack(builder)
        if self.PROFILE is not None:
            PROFILE = builder.CreateString(self.PROFILE)
        PRWPrepareRequestStart(builder)
        if self.INSTANCE is not None:
            PRWPrepareRequestAddINSTANCE(builder, INSTANCE)
        PRWPrepareRequestAddCATALOG_HANDLE(builder, self.CATALOG_HANDLE)
        if self.SOURCE_HANDLES is not None:
            PRWPrepareRequestAddSOURCE_HANDLES(builder, SOURCE_HANDLES)
        if self.START_EPOCH is not None:
            PRWPrepareRequestAddSTART_EPOCH(builder, START_EPOCH)
        PRWPrepareRequestAddDURATION_SECONDS(builder, self.DURATION_SECONDS)
        if self.PROFILE is not None:
            PRWPrepareRequestAddPROFILE(builder, PROFILE)
        PRWPrepareRequest = PRWPrepareRequestEnd(builder)
        return PRWPrepareRequest
