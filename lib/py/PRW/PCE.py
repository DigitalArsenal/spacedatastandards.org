# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Parameter Catalog and Evaluation
#
# The named calculation parameters an analysis resolves through: their owner
# class, unit, data type, frame dependency and per-publisher availability
# (CATALOG), a request and result for evaluating named parameters on states
# at epochs in a stated coordinate system (EVALUATION_REQUEST /
# EVALUATION_RESULT), and propagate-to-condition, which is a predicate on one
# of those same names rather than a vocabulary of its own (STOP_REQUEST /
# STOP_REPORT). Stopping conditions, targeting goals, report columns and plot
# series all resolve names here, so a name means one thing everywhere.
#
# Angles are radians, lengths metres, durations seconds, masses kilograms.
# A buffer carries the members its exchange needs; a catalog publication and
# an evaluation exchange are both complete $PCE records.
class PCE(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PCE()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPCE(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PCEBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x43\x45", size_prefixed=size_prefixed)

    # PCE
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PCE
    def CATALOG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PCEParameterCatalog import PCEParameterCatalog
            obj = PCEParameterCatalog()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PCE
    def EVALUATION_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PCEEvaluationRequest import PCEEvaluationRequest
            obj = PCEEvaluationRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PCE
    def EVALUATION_RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PCEEvaluationResult import PCEEvaluationResult
            obj = PCEEvaluationResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PCE
    def STOP_REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PCEStopRequest import PCEStopRequest
            obj = PCEStopRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PCE
    def STOP_REPORT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from PCEStopReport import PCEStopReport
            obj = PCEStopReport()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def PCEStart(builder):
    builder.StartObject(5)

def Start(builder):
    PCEStart(builder)

def PCEAddCATALOG(builder, CATALOG):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(CATALOG), 0)

def AddCATALOG(builder, CATALOG):
    PCEAddCATALOG(builder, CATALOG)

def PCEAddEVALUATION_REQUEST(builder, EVALUATION_REQUEST):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(EVALUATION_REQUEST), 0)

def AddEVALUATION_REQUEST(builder, EVALUATION_REQUEST):
    PCEAddEVALUATION_REQUEST(builder, EVALUATION_REQUEST)

def PCEAddEVALUATION_RESULT(builder, EVALUATION_RESULT):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(EVALUATION_RESULT), 0)

def AddEVALUATION_RESULT(builder, EVALUATION_RESULT):
    PCEAddEVALUATION_RESULT(builder, EVALUATION_RESULT)

def PCEAddSTOP_REQUEST(builder, STOP_REQUEST):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(STOP_REQUEST), 0)

def AddSTOP_REQUEST(builder, STOP_REQUEST):
    PCEAddSTOP_REQUEST(builder, STOP_REQUEST)

def PCEAddSTOP_REPORT(builder, STOP_REPORT):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(STOP_REPORT), 0)

def AddSTOP_REPORT(builder, STOP_REPORT):
    PCEAddSTOP_REPORT(builder, STOP_REPORT)

def PCEEnd(builder):
    return builder.EndObject()

def End(builder):
    return PCEEnd(builder)

import PCEEvaluationRequest
import PCEEvaluationResult
import PCEParameterCatalog
import PCEStopReport
import PCEStopRequest
try:
    from typing import Optional
except:
    pass

class PCET(object):

    # PCET
    def __init__(
        self,
        CATALOG = None,
        EVALUATION_REQUEST = None,
        EVALUATION_RESULT = None,
        STOP_REQUEST = None,
        STOP_REPORT = None,
    ):
        self.CATALOG = CATALOG  # type: Optional[PCEParameterCatalog.PCEParameterCatalogT]
        self.EVALUATION_REQUEST = EVALUATION_REQUEST  # type: Optional[PCEEvaluationRequest.PCEEvaluationRequestT]
        self.EVALUATION_RESULT = EVALUATION_RESULT  # type: Optional[PCEEvaluationResult.PCEEvaluationResultT]
        self.STOP_REQUEST = STOP_REQUEST  # type: Optional[PCEStopRequest.PCEStopRequestT]
        self.STOP_REPORT = STOP_REPORT  # type: Optional[PCEStopReport.PCEStopReportT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPce = PCE()
        tmpPce.Init(buf, pos)
        return cls.InitFromObj(tmpPce)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPce):
        x = PCET()
        x._UnPack(tmpPce)
        return x

    # PCET
    def _UnPack(self, PCE):
        if PCE is None:
            return
        if PCE.CATALOG() is not None:
            self.CATALOG = PCEParameterCatalog.PCEParameterCatalogT.InitFromObj(PCE.CATALOG())
        if PCE.EVALUATION_REQUEST() is not None:
            self.EVALUATION_REQUEST = PCEEvaluationRequest.PCEEvaluationRequestT.InitFromObj(PCE.EVALUATION_REQUEST())
        if PCE.EVALUATION_RESULT() is not None:
            self.EVALUATION_RESULT = PCEEvaluationResult.PCEEvaluationResultT.InitFromObj(PCE.EVALUATION_RESULT())
        if PCE.STOP_REQUEST() is not None:
            self.STOP_REQUEST = PCEStopRequest.PCEStopRequestT.InitFromObj(PCE.STOP_REQUEST())
        if PCE.STOP_REPORT() is not None:
            self.STOP_REPORT = PCEStopReport.PCEStopReportT.InitFromObj(PCE.STOP_REPORT())

    # PCET
    def Pack(self, builder):
        if self.CATALOG is not None:
            CATALOG = self.CATALOG.Pack(builder)
        if self.EVALUATION_REQUEST is not None:
            EVALUATION_REQUEST = self.EVALUATION_REQUEST.Pack(builder)
        if self.EVALUATION_RESULT is not None:
            EVALUATION_RESULT = self.EVALUATION_RESULT.Pack(builder)
        if self.STOP_REQUEST is not None:
            STOP_REQUEST = self.STOP_REQUEST.Pack(builder)
        if self.STOP_REPORT is not None:
            STOP_REPORT = self.STOP_REPORT.Pack(builder)
        PCEStart(builder)
        if self.CATALOG is not None:
            PCEAddCATALOG(builder, CATALOG)
        if self.EVALUATION_REQUEST is not None:
            PCEAddEVALUATION_REQUEST(builder, EVALUATION_REQUEST)
        if self.EVALUATION_RESULT is not None:
            PCEAddEVALUATION_RESULT(builder, EVALUATION_RESULT)
        if self.STOP_REQUEST is not None:
            PCEAddSTOP_REQUEST(builder, STOP_REQUEST)
        if self.STOP_REPORT is not None:
            PCEAddSTOP_REPORT(builder, STOP_REPORT)
        PCE = PCEEnd(builder)
        return PCE
