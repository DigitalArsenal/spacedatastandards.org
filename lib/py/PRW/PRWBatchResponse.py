# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Batch propagation response header — describes the stream written to
# OUTPUT_OFFSET and reports errors. The stream itself is
# `STATE_VECTOR_SIZE`-tuple rows in the SDS `OCM` STATE_DATA layout.
class PRWBatchResponse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRWBatchResponse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRWBatchResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRWBatchResponseBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x57", size_prefixed=size_prefixed)

    # PRWBatchResponse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Number of state vectors written.
    # PRWBatchResponse
    def COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Offset in the arena where the state-vector stream begins.
    # PRWBatchResponse
    def OUTPUT_OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Components per state vector (6 = PV, 9 = PVA). Mirrors OCM.STATE_VECTOR_SIZE.
    # PRWBatchResponse
    def STATE_VECTOR_SIZE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Reference frame for the stream (SDS `RFM`-compatible string tag).
    # PRWBatchResponse
    def REFERENCE_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Per-entity status flags (same cardinality as COUNT).
    # PRWBatchResponse
    def FLAGS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # PRWBatchResponse
    def FLAGSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # PRWBatchResponse
    def FLAGSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PRWBatchResponse
    def FLAGSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Error code (0 == OK).
    # PRWBatchResponse
    def ERROR_CODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Optional error message when ERROR_CODE != OK.
    # PRWBatchResponse
    def ERROR_MESSAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PRWBatchResponseStart(builder):
    builder.StartObject(7)

def Start(builder):
    PRWBatchResponseStart(builder)

def PRWBatchResponseAddCOUNT(builder, COUNT):
    builder.PrependUint32Slot(0, COUNT, 0)

def AddCOUNT(builder, COUNT):
    PRWBatchResponseAddCOUNT(builder, COUNT)

def PRWBatchResponseAddOUTPUT_OFFSET(builder, OUTPUT_OFFSET):
    builder.PrependUint32Slot(1, OUTPUT_OFFSET, 0)

def AddOUTPUT_OFFSET(builder, OUTPUT_OFFSET):
    PRWBatchResponseAddOUTPUT_OFFSET(builder, OUTPUT_OFFSET)

def PRWBatchResponseAddSTATE_VECTOR_SIZE(builder, STATE_VECTOR_SIZE):
    builder.PrependUint8Slot(2, STATE_VECTOR_SIZE, 0)

def AddSTATE_VECTOR_SIZE(builder, STATE_VECTOR_SIZE):
    PRWBatchResponseAddSTATE_VECTOR_SIZE(builder, STATE_VECTOR_SIZE)

def PRWBatchResponseAddREFERENCE_FRAME(builder, REFERENCE_FRAME):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(REFERENCE_FRAME), 0)

def AddREFERENCE_FRAME(builder, REFERENCE_FRAME):
    PRWBatchResponseAddREFERENCE_FRAME(builder, REFERENCE_FRAME)

def PRWBatchResponseAddFLAGS(builder, FLAGS):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(FLAGS), 0)

def AddFLAGS(builder, FLAGS):
    PRWBatchResponseAddFLAGS(builder, FLAGS)

def PRWBatchResponseStartFLAGSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartFLAGSVector(builder, numElems):
    return PRWBatchResponseStartFLAGSVector(builder, numElems)

def PRWBatchResponseCreateFLAGSVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateFLAGSVector(builder, data):
    PRWBatchResponseCreateFLAGSVector(builder, data)

def PRWBatchResponseAddERROR_CODE(builder, ERROR_CODE):
    builder.PrependInt32Slot(5, ERROR_CODE, 0)

def AddERROR_CODE(builder, ERROR_CODE):
    PRWBatchResponseAddERROR_CODE(builder, ERROR_CODE)

def PRWBatchResponseAddERROR_MESSAGE(builder, ERROR_MESSAGE):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(ERROR_MESSAGE), 0)

def AddERROR_MESSAGE(builder, ERROR_MESSAGE):
    PRWBatchResponseAddERROR_MESSAGE(builder, ERROR_MESSAGE)

def PRWBatchResponseEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRWBatchResponseEnd(builder)

try:
    from typing import List
except:
    pass

class PRWBatchResponseT(object):

    # PRWBatchResponseT
    def __init__(
        self,
        COUNT = 0,
        OUTPUT_OFFSET = 0,
        STATE_VECTOR_SIZE = 0,
        REFERENCE_FRAME = None,
        FLAGS = None,
        ERROR_CODE = 0,
        ERROR_MESSAGE = None,
    ):
        self.COUNT = COUNT  # type: int
        self.OUTPUT_OFFSET = OUTPUT_OFFSET  # type: int
        self.STATE_VECTOR_SIZE = STATE_VECTOR_SIZE  # type: int
        self.REFERENCE_FRAME = REFERENCE_FRAME  # type: Optional[str]
        self.FLAGS = FLAGS  # type: Optional[List[int]]
        self.ERROR_CODE = ERROR_CODE  # type: int
        self.ERROR_MESSAGE = ERROR_MESSAGE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrwbatchResponse = PRWBatchResponse()
        tmpPrwbatchResponse.Init(buf, pos)
        return cls.InitFromObj(tmpPrwbatchResponse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrwbatchResponse):
        x = PRWBatchResponseT()
        x._UnPack(tmpPrwbatchResponse)
        return x

    # PRWBatchResponseT
    def _UnPack(self, PRWBatchResponse):
        if PRWBatchResponse is None:
            return
        self.COUNT = PRWBatchResponse.COUNT()
        self.OUTPUT_OFFSET = PRWBatchResponse.OUTPUT_OFFSET()
        self.STATE_VECTOR_SIZE = PRWBatchResponse.STATE_VECTOR_SIZE()
        self.REFERENCE_FRAME = PRWBatchResponse.REFERENCE_FRAME()
        if not PRWBatchResponse.FLAGSIsNone():
            if np is None:
                self.FLAGS = []
                for i in range(PRWBatchResponse.FLAGSLength()):
                    self.FLAGS.append(PRWBatchResponse.FLAGS(i))
            else:
                self.FLAGS = PRWBatchResponse.FLAGSAsNumpy()
        self.ERROR_CODE = PRWBatchResponse.ERROR_CODE()
        self.ERROR_MESSAGE = PRWBatchResponse.ERROR_MESSAGE()

    # PRWBatchResponseT
    def Pack(self, builder):
        if self.REFERENCE_FRAME is not None:
            REFERENCE_FRAME = builder.CreateString(self.REFERENCE_FRAME)
        if self.FLAGS is not None:
            if np is not None and type(self.FLAGS) is np.ndarray:
                FLAGS = builder.CreateNumpyVector(self.FLAGS)
            else:
                PRWBatchResponseStartFLAGSVector(builder, len(self.FLAGS))
                for i in reversed(range(len(self.FLAGS))):
                    builder.PrependUint32(self.FLAGS[i])
                FLAGS = builder.EndVector()
        if self.ERROR_MESSAGE is not None:
            ERROR_MESSAGE = builder.CreateString(self.ERROR_MESSAGE)
        PRWBatchResponseStart(builder)
        PRWBatchResponseAddCOUNT(builder, self.COUNT)
        PRWBatchResponseAddOUTPUT_OFFSET(builder, self.OUTPUT_OFFSET)
        PRWBatchResponseAddSTATE_VECTOR_SIZE(builder, self.STATE_VECTOR_SIZE)
        if self.REFERENCE_FRAME is not None:
            PRWBatchResponseAddREFERENCE_FRAME(builder, REFERENCE_FRAME)
        if self.FLAGS is not None:
            PRWBatchResponseAddFLAGS(builder, FLAGS)
        PRWBatchResponseAddERROR_CODE(builder, self.ERROR_CODE)
        if self.ERROR_MESSAGE is not None:
            PRWBatchResponseAddERROR_MESSAGE(builder, ERROR_MESSAGE)
        PRWBatchResponse = PRWBatchResponseEnd(builder)
        return PRWBatchResponse
