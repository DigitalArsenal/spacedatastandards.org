# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One independently addressed segment inside a native container.
#
# A segmented binary ephemeris is not a single span: each segment has its own
# body pair, frame, numeric type and time bounds, and a reader must choose
# among them. $OEM carries the STATES; this carries the segment DESCRIPTOR
# that says which states came from where and what must be written back.
class NCDSegmentDescriptor(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NCDSegmentDescriptor()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNCDSegmentDescriptor(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def NCDSegmentDescriptorBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4E\x43\x44", size_prefixed=size_prefixed)

    # NCDSegmentDescriptor
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Segment name as recorded in the container.
    # NCDSegmentDescriptor
    def NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Integer body code of the segment target.
    # NCDSegmentDescriptor
    def TARGET_NAIF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Integer body code of the segment centre.
    # NCDSegmentDescriptor
    def CENTER_NAIF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Integer reference frame code of the segment.
    # NCDSegmentDescriptor
    def FRAME_NAIF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Reference frame name as recorded in the container.
    # NCDSegmentDescriptor
    def FRAME_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Numeric segment data type, verbatim. The evaluation rule is a property of
    # this number (for example, Chebyshev position with fixed interval, or
    # discrete states with Lagrange interpolation); it is carried as the
    # container's own integer so a rewrite is exact.
    # NCDSegmentDescriptor
    def SEGMENT_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # Segment coverage start, ISO 8601.
    # NCDSegmentDescriptor
    def START_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Segment coverage stop, ISO 8601.
    # NCDSegmentDescriptor
    def STOP_EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Segment coverage start, seconds past J2000 in the barycentric dynamical
    # time scale, as stored in the container.
    # NCDSegmentDescriptor
    def START_SECONDS_PAST_J2000_TDB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Segment coverage stop, same scale.
    # NCDSegmentDescriptor
    def STOP_SECONDS_PAST_J2000_TDB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # First addressed element of the segment inside the file.
    # NCDSegmentDescriptor
    def INITIAL_ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Last addressed element of the segment inside the file.
    # NCDSegmentDescriptor
    def FINAL_ADDRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Polynomial degree or record size the segment type parameterises, when the
    # type carries one.
    # NCDSegmentDescriptor
    def POLYNOMIAL_DEGREE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def NCDSegmentDescriptorStart(builder):
    builder.StartObject(13)

def Start(builder):
    NCDSegmentDescriptorStart(builder)

def NCDSegmentDescriptorAddNAME(builder, NAME):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NAME), 0)

def AddNAME(builder, NAME):
    NCDSegmentDescriptorAddNAME(builder, NAME)

def NCDSegmentDescriptorAddTARGET_NAIF_ID(builder, TARGET_NAIF_ID):
    builder.PrependInt32Slot(1, TARGET_NAIF_ID, 0)

def AddTARGET_NAIF_ID(builder, TARGET_NAIF_ID):
    NCDSegmentDescriptorAddTARGET_NAIF_ID(builder, TARGET_NAIF_ID)

def NCDSegmentDescriptorAddCENTER_NAIF_ID(builder, CENTER_NAIF_ID):
    builder.PrependInt32Slot(2, CENTER_NAIF_ID, 0)

def AddCENTER_NAIF_ID(builder, CENTER_NAIF_ID):
    NCDSegmentDescriptorAddCENTER_NAIF_ID(builder, CENTER_NAIF_ID)

def NCDSegmentDescriptorAddFRAME_NAIF_ID(builder, FRAME_NAIF_ID):
    builder.PrependInt32Slot(3, FRAME_NAIF_ID, 0)

def AddFRAME_NAIF_ID(builder, FRAME_NAIF_ID):
    NCDSegmentDescriptorAddFRAME_NAIF_ID(builder, FRAME_NAIF_ID)

def NCDSegmentDescriptorAddFRAME_NAME(builder, FRAME_NAME):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(FRAME_NAME), 0)

def AddFRAME_NAME(builder, FRAME_NAME):
    NCDSegmentDescriptorAddFRAME_NAME(builder, FRAME_NAME)

def NCDSegmentDescriptorAddSEGMENT_TYPE(builder, SEGMENT_TYPE):
    builder.PrependInt32Slot(5, SEGMENT_TYPE, 0)

def AddSEGMENT_TYPE(builder, SEGMENT_TYPE):
    NCDSegmentDescriptorAddSEGMENT_TYPE(builder, SEGMENT_TYPE)

def NCDSegmentDescriptorAddSTART_EPOCH(builder, START_EPOCH):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(START_EPOCH), 0)

def AddSTART_EPOCH(builder, START_EPOCH):
    NCDSegmentDescriptorAddSTART_EPOCH(builder, START_EPOCH)

def NCDSegmentDescriptorAddSTOP_EPOCH(builder, STOP_EPOCH):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(STOP_EPOCH), 0)

def AddSTOP_EPOCH(builder, STOP_EPOCH):
    NCDSegmentDescriptorAddSTOP_EPOCH(builder, STOP_EPOCH)

def NCDSegmentDescriptorAddSTART_SECONDS_PAST_J2000_TDB(builder, START_SECONDS_PAST_J2000_TDB):
    builder.PrependFloat64Slot(8, START_SECONDS_PAST_J2000_TDB, 0.0)

def AddSTART_SECONDS_PAST_J2000_TDB(builder, START_SECONDS_PAST_J2000_TDB):
    NCDSegmentDescriptorAddSTART_SECONDS_PAST_J2000_TDB(builder, START_SECONDS_PAST_J2000_TDB)

def NCDSegmentDescriptorAddSTOP_SECONDS_PAST_J2000_TDB(builder, STOP_SECONDS_PAST_J2000_TDB):
    builder.PrependFloat64Slot(9, STOP_SECONDS_PAST_J2000_TDB, 0.0)

def AddSTOP_SECONDS_PAST_J2000_TDB(builder, STOP_SECONDS_PAST_J2000_TDB):
    NCDSegmentDescriptorAddSTOP_SECONDS_PAST_J2000_TDB(builder, STOP_SECONDS_PAST_J2000_TDB)

def NCDSegmentDescriptorAddINITIAL_ADDRESS(builder, INITIAL_ADDRESS):
    builder.PrependUint64Slot(10, INITIAL_ADDRESS, 0)

def AddINITIAL_ADDRESS(builder, INITIAL_ADDRESS):
    NCDSegmentDescriptorAddINITIAL_ADDRESS(builder, INITIAL_ADDRESS)

def NCDSegmentDescriptorAddFINAL_ADDRESS(builder, FINAL_ADDRESS):
    builder.PrependUint64Slot(11, FINAL_ADDRESS, 0)

def AddFINAL_ADDRESS(builder, FINAL_ADDRESS):
    NCDSegmentDescriptorAddFINAL_ADDRESS(builder, FINAL_ADDRESS)

def NCDSegmentDescriptorAddPOLYNOMIAL_DEGREE(builder, POLYNOMIAL_DEGREE):
    builder.PrependUint32Slot(12, POLYNOMIAL_DEGREE, 0)

def AddPOLYNOMIAL_DEGREE(builder, POLYNOMIAL_DEGREE):
    NCDSegmentDescriptorAddPOLYNOMIAL_DEGREE(builder, POLYNOMIAL_DEGREE)

def NCDSegmentDescriptorEnd(builder):
    return builder.EndObject()

def End(builder):
    return NCDSegmentDescriptorEnd(builder)


class NCDSegmentDescriptorT(object):

    # NCDSegmentDescriptorT
    def __init__(
        self,
        NAME = None,
        TARGET_NAIF_ID = 0,
        CENTER_NAIF_ID = 0,
        FRAME_NAIF_ID = 0,
        FRAME_NAME = None,
        SEGMENT_TYPE = 0,
        START_EPOCH = None,
        STOP_EPOCH = None,
        START_SECONDS_PAST_J2000_TDB = 0.0,
        STOP_SECONDS_PAST_J2000_TDB = 0.0,
        INITIAL_ADDRESS = 0,
        FINAL_ADDRESS = 0,
        POLYNOMIAL_DEGREE = 0,
    ):
        self.NAME = NAME  # type: Optional[str]
        self.TARGET_NAIF_ID = TARGET_NAIF_ID  # type: int
        self.CENTER_NAIF_ID = CENTER_NAIF_ID  # type: int
        self.FRAME_NAIF_ID = FRAME_NAIF_ID  # type: int
        self.FRAME_NAME = FRAME_NAME  # type: Optional[str]
        self.SEGMENT_TYPE = SEGMENT_TYPE  # type: int
        self.START_EPOCH = START_EPOCH  # type: Optional[str]
        self.STOP_EPOCH = STOP_EPOCH  # type: Optional[str]
        self.START_SECONDS_PAST_J2000_TDB = START_SECONDS_PAST_J2000_TDB  # type: float
        self.STOP_SECONDS_PAST_J2000_TDB = STOP_SECONDS_PAST_J2000_TDB  # type: float
        self.INITIAL_ADDRESS = INITIAL_ADDRESS  # type: int
        self.FINAL_ADDRESS = FINAL_ADDRESS  # type: int
        self.POLYNOMIAL_DEGREE = POLYNOMIAL_DEGREE  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpNcdsegmentDescriptor = NCDSegmentDescriptor()
        tmpNcdsegmentDescriptor.Init(buf, pos)
        return cls.InitFromObj(tmpNcdsegmentDescriptor)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpNcdsegmentDescriptor):
        x = NCDSegmentDescriptorT()
        x._UnPack(tmpNcdsegmentDescriptor)
        return x

    # NCDSegmentDescriptorT
    def _UnPack(self, NCDSegmentDescriptor):
        if NCDSegmentDescriptor is None:
            return
        self.NAME = NCDSegmentDescriptor.NAME()
        self.TARGET_NAIF_ID = NCDSegmentDescriptor.TARGET_NAIF_ID()
        self.CENTER_NAIF_ID = NCDSegmentDescriptor.CENTER_NAIF_ID()
        self.FRAME_NAIF_ID = NCDSegmentDescriptor.FRAME_NAIF_ID()
        self.FRAME_NAME = NCDSegmentDescriptor.FRAME_NAME()
        self.SEGMENT_TYPE = NCDSegmentDescriptor.SEGMENT_TYPE()
        self.START_EPOCH = NCDSegmentDescriptor.START_EPOCH()
        self.STOP_EPOCH = NCDSegmentDescriptor.STOP_EPOCH()
        self.START_SECONDS_PAST_J2000_TDB = NCDSegmentDescriptor.START_SECONDS_PAST_J2000_TDB()
        self.STOP_SECONDS_PAST_J2000_TDB = NCDSegmentDescriptor.STOP_SECONDS_PAST_J2000_TDB()
        self.INITIAL_ADDRESS = NCDSegmentDescriptor.INITIAL_ADDRESS()
        self.FINAL_ADDRESS = NCDSegmentDescriptor.FINAL_ADDRESS()
        self.POLYNOMIAL_DEGREE = NCDSegmentDescriptor.POLYNOMIAL_DEGREE()

    # NCDSegmentDescriptorT
    def Pack(self, builder):
        if self.NAME is not None:
            NAME = builder.CreateString(self.NAME)
        if self.FRAME_NAME is not None:
            FRAME_NAME = builder.CreateString(self.FRAME_NAME)
        if self.START_EPOCH is not None:
            START_EPOCH = builder.CreateString(self.START_EPOCH)
        if self.STOP_EPOCH is not None:
            STOP_EPOCH = builder.CreateString(self.STOP_EPOCH)
        NCDSegmentDescriptorStart(builder)
        if self.NAME is not None:
            NCDSegmentDescriptorAddNAME(builder, NAME)
        NCDSegmentDescriptorAddTARGET_NAIF_ID(builder, self.TARGET_NAIF_ID)
        NCDSegmentDescriptorAddCENTER_NAIF_ID(builder, self.CENTER_NAIF_ID)
        NCDSegmentDescriptorAddFRAME_NAIF_ID(builder, self.FRAME_NAIF_ID)
        if self.FRAME_NAME is not None:
            NCDSegmentDescriptorAddFRAME_NAME(builder, FRAME_NAME)
        NCDSegmentDescriptorAddSEGMENT_TYPE(builder, self.SEGMENT_TYPE)
        if self.START_EPOCH is not None:
            NCDSegmentDescriptorAddSTART_EPOCH(builder, START_EPOCH)
        if self.STOP_EPOCH is not None:
            NCDSegmentDescriptorAddSTOP_EPOCH(builder, STOP_EPOCH)
        NCDSegmentDescriptorAddSTART_SECONDS_PAST_J2000_TDB(builder, self.START_SECONDS_PAST_J2000_TDB)
        NCDSegmentDescriptorAddSTOP_SECONDS_PAST_J2000_TDB(builder, self.STOP_SECONDS_PAST_J2000_TDB)
        NCDSegmentDescriptorAddINITIAL_ADDRESS(builder, self.INITIAL_ADDRESS)
        NCDSegmentDescriptorAddFINAL_ADDRESS(builder, self.FINAL_ADDRESS)
        NCDSegmentDescriptorAddPOLYNOMIAL_DEGREE(builder, self.POLYNOMIAL_DEGREE)
        NCDSegmentDescriptor = NCDSegmentDescriptorEnd(builder)
        return NCDSegmentDescriptor
