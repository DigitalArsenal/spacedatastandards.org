# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# GEO Drift History Record
class driftRecord(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = driftRecord()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsdriftRecord(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def driftRecordBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x44\x46\x48", size_prefixed=size_prefixed)

    # driftRecord
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Epoch of drift measurement (ISO 8601)
    # driftRecord
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Longitude drift rate in degrees/day
    # driftRecord
    def DRIFT_RATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Mean longitude in degrees East
    # driftRecord
    def MEAN_LONGITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Longitude oscillation amplitude in degrees
    # driftRecord
    def LONGITUDE_AMPLITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Eccentricity
    # driftRecord
    def ECCENTRICITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Inclination in degrees
    # driftRecord
    def INCLINATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def driftRecordStart(builder):
    builder.StartObject(6)

def Start(builder):
    driftRecordStart(builder)

def driftRecordAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    driftRecordAddEPOCH(builder, EPOCH)

def driftRecordAddDRIFT_RATE(builder, DRIFT_RATE):
    builder.PrependFloat64Slot(1, DRIFT_RATE, 0.0)

def AddDRIFT_RATE(builder, DRIFT_RATE):
    driftRecordAddDRIFT_RATE(builder, DRIFT_RATE)

def driftRecordAddMEAN_LONGITUDE(builder, MEAN_LONGITUDE):
    builder.PrependFloat64Slot(2, MEAN_LONGITUDE, 0.0)

def AddMEAN_LONGITUDE(builder, MEAN_LONGITUDE):
    driftRecordAddMEAN_LONGITUDE(builder, MEAN_LONGITUDE)

def driftRecordAddLONGITUDE_AMPLITUDE(builder, LONGITUDE_AMPLITUDE):
    builder.PrependFloat64Slot(3, LONGITUDE_AMPLITUDE, 0.0)

def AddLONGITUDE_AMPLITUDE(builder, LONGITUDE_AMPLITUDE):
    driftRecordAddLONGITUDE_AMPLITUDE(builder, LONGITUDE_AMPLITUDE)

def driftRecordAddECCENTRICITY(builder, ECCENTRICITY):
    builder.PrependFloat64Slot(4, ECCENTRICITY, 0.0)

def AddECCENTRICITY(builder, ECCENTRICITY):
    driftRecordAddECCENTRICITY(builder, ECCENTRICITY)

def driftRecordAddINCLINATION(builder, INCLINATION):
    builder.PrependFloat64Slot(5, INCLINATION, 0.0)

def AddINCLINATION(builder, INCLINATION):
    driftRecordAddINCLINATION(builder, INCLINATION)

def driftRecordEnd(builder):
    return builder.EndObject()

def End(builder):
    return driftRecordEnd(builder)


class driftRecordT(object):

    # driftRecordT
    def __init__(self):
        self.EPOCH = None  # type: str
        self.DRIFT_RATE = 0.0  # type: float
        self.MEAN_LONGITUDE = 0.0  # type: float
        self.LONGITUDE_AMPLITUDE = 0.0  # type: float
        self.ECCENTRICITY = 0.0  # type: float
        self.INCLINATION = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        driftRecord = driftRecord()
        driftRecord.Init(buf, pos)
        return cls.InitFromObj(driftRecord)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, driftRecord):
        x = driftRecordT()
        x._UnPack(driftRecord)
        return x

    # driftRecordT
    def _UnPack(self, driftRecord):
        if driftRecord is None:
            return
        self.EPOCH = driftRecord.EPOCH()
        self.DRIFT_RATE = driftRecord.DRIFT_RATE()
        self.MEAN_LONGITUDE = driftRecord.MEAN_LONGITUDE()
        self.LONGITUDE_AMPLITUDE = driftRecord.LONGITUDE_AMPLITUDE()
        self.ECCENTRICITY = driftRecord.ECCENTRICITY()
        self.INCLINATION = driftRecord.INCLINATION()

    # driftRecordT
    def Pack(self, builder):
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        driftRecordStart(builder)
        if self.EPOCH is not None:
            driftRecordAddEPOCH(builder, EPOCH)
        driftRecordAddDRIFT_RATE(builder, self.DRIFT_RATE)
        driftRecordAddMEAN_LONGITUDE(builder, self.MEAN_LONGITUDE)
        driftRecordAddLONGITUDE_AMPLITUDE(builder, self.LONGITUDE_AMPLITUDE)
        driftRecordAddECCENTRICITY(builder, self.ECCENTRICITY)
        driftRecordAddINCLINATION(builder, self.INCLINATION)
        driftRecord = driftRecordEnd(builder)
        return driftRecord
