# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One host capability dependency (richer form of PluginCapability).
class PLGHostCapability(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PLGHostCapability()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPLGHostCapability(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PLGHostCapabilityBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4C\x47", size_prefixed=size_prefixed)

    # PLGHostCapability
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PLGHostCapability
    def CAPABILITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # PLGHostCapability
    def SCOPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PLGHostCapability
    def REQUIRED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return True

    # PLGHostCapability
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PLGHostCapabilityStart(builder):
    builder.StartObject(4)

def Start(builder):
    PLGHostCapabilityStart(builder)

def PLGHostCapabilityAddCAPABILITY(builder, CAPABILITY):
    builder.PrependUint16Slot(0, CAPABILITY, 0)

def AddCAPABILITY(builder, CAPABILITY):
    PLGHostCapabilityAddCAPABILITY(builder, CAPABILITY)

def PLGHostCapabilityAddSCOPE(builder, SCOPE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SCOPE), 0)

def AddSCOPE(builder, SCOPE):
    PLGHostCapabilityAddSCOPE(builder, SCOPE)

def PLGHostCapabilityAddREQUIRED(builder, REQUIRED):
    builder.PrependBoolSlot(2, REQUIRED, 1)

def AddREQUIRED(builder, REQUIRED):
    PLGHostCapabilityAddREQUIRED(builder, REQUIRED)

def PLGHostCapabilityAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    PLGHostCapabilityAddDESCRIPTION(builder, DESCRIPTION)

def PLGHostCapabilityEnd(builder):
    return builder.EndObject()

def End(builder):
    return PLGHostCapabilityEnd(builder)


class PLGHostCapabilityT(object):

    # PLGHostCapabilityT
    def __init__(
        self,
        CAPABILITY = 0,
        SCOPE = None,
        REQUIRED = True,
        DESCRIPTION = None,
    ):
        self.CAPABILITY = CAPABILITY  # type: int
        self.SCOPE = SCOPE  # type: Optional[str]
        self.REQUIRED = REQUIRED  # type: bool
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPlghostCapability = PLGHostCapability()
        tmpPlghostCapability.Init(buf, pos)
        return cls.InitFromObj(tmpPlghostCapability)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPlghostCapability):
        x = PLGHostCapabilityT()
        x._UnPack(tmpPlghostCapability)
        return x

    # PLGHostCapabilityT
    def _UnPack(self, PLGHostCapability):
        if PLGHostCapability is None:
            return
        self.CAPABILITY = PLGHostCapability.CAPABILITY()
        self.SCOPE = PLGHostCapability.SCOPE()
        self.REQUIRED = PLGHostCapability.REQUIRED()
        self.DESCRIPTION = PLGHostCapability.DESCRIPTION()

    # PLGHostCapabilityT
    def Pack(self, builder):
        if self.SCOPE is not None:
            SCOPE = builder.CreateString(self.SCOPE)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        PLGHostCapabilityStart(builder)
        PLGHostCapabilityAddCAPABILITY(builder, self.CAPABILITY)
        if self.SCOPE is not None:
            PLGHostCapabilityAddSCOPE(builder, SCOPE)
        PLGHostCapabilityAddREQUIRED(builder, self.REQUIRED)
        if self.DESCRIPTION is not None:
            PLGHostCapabilityAddDESCRIPTION(builder, DESCRIPTION)
        PLGHostCapability = PLGHostCapabilityEnd(builder)
        return PLGHostCapability
