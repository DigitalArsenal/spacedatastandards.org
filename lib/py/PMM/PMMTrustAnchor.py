# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# The single key identity every signature in this manifest resolves to, and
# the two external anchors that give that key its weight: the DNS proof that
# binds it to the domain, and the Adversarial-Security bond that prices
# trust in it.
class PMMTrustAnchor(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PMMTrustAnchor()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPMMTrustAnchor(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PMMTrustAnchorBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4D\x4D", size_prefixed=size_prefixed)

    # PMMTrustAnchor
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Domain the node key is bound to. MUST equal `PMM.PROVIDER_DOMAIN` and
    # MUST equal the origin the manifest was fetched from.
    # PMMTrustAnchor
    def PROVIDER_DOMAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # libp2p peer id of the provider node.
    # PMMTrustAnchor
    def NODE_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # BIP-32 account xpub of the node identity, verbatim as advertised in the
    # node's `$EPM`.
    # PMMTrustAnchor
    def NODE_XPUB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Hex-encoded public key that verifies `PMM.SIGNATURE` and every
    # `PMMModuleEntry.ARTIFACT_SIGNATURE`, derived from the node key.
    # PMMTrustAnchor
    def SIGNING_PUBLIC_KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # HD derivation path of `SIGNING_PUBLIC_KEY` under `NODE_XPUB`.
    # PMMTrustAnchor
    def SIGNING_KEY_PATH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Signature algorithm identifier, e.g. "ed25519",
    # "secp256k1-compact-ethereum". Empty means "ed25519".
    # PMMTrustAnchor
    def SIGNATURE_ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # IPFS CID of the provider node's `$EPM`.
    # PMMTrustAnchor
    def EPM_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Fully-qualified DNS name of the TXT record carrying the signed
    # domain-to-nodekey proof, e.g. "_sdnkey.example.org". A verifying client
    # resolves this itself (DNS-over-HTTPS) rather than trusting the copy
    # below.
    # PMMTrustAnchor
    def DNS_PROOF_RECORD_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Verbatim TXT value of that proof, carried only so a client can detect
    # disagreement with what DNS returns. The resolved record always wins.
    # PMMTrustAnchor
    def DNS_PROOF_TXT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The canonical statement the DNS proof signs, verbatim, when the proof
    # format keeps it out of the TXT value.
    # PMMTrustAnchor
    def DNS_PROOF_STATEMENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Chain addresses derived from this same node key that hold the provider's
    # Adversarial-Security bond. Each entry proves, through `ChainProof`, that
    # the address derives from the key that signed this manifest.
    # PMMTrustAnchor
    def BOND_ADDRESSES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ChainProof import ChainProof
            obj = ChainProof()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PMMTrustAnchor
    def BOND_ADDRESSESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PMMTrustAnchor
    def BOND_ADDRESSESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # Optional URL of a machine-readable bond attestation (bonded value,
    # duration unspent) for clients that price trust rather than gate on it.
    # PMMTrustAnchor
    def BOND_ATTESTATION_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PMMTrustAnchorStart(builder):
    builder.StartObject(12)

def Start(builder):
    PMMTrustAnchorStart(builder)

def PMMTrustAnchorAddPROVIDER_DOMAIN(builder, PROVIDER_DOMAIN):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_DOMAIN), 0)

def AddPROVIDER_DOMAIN(builder, PROVIDER_DOMAIN):
    PMMTrustAnchorAddPROVIDER_DOMAIN(builder, PROVIDER_DOMAIN)

def PMMTrustAnchorAddNODE_PEER_ID(builder, NODE_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(NODE_PEER_ID), 0)

def AddNODE_PEER_ID(builder, NODE_PEER_ID):
    PMMTrustAnchorAddNODE_PEER_ID(builder, NODE_PEER_ID)

def PMMTrustAnchorAddNODE_XPUB(builder, NODE_XPUB):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(NODE_XPUB), 0)

def AddNODE_XPUB(builder, NODE_XPUB):
    PMMTrustAnchorAddNODE_XPUB(builder, NODE_XPUB)

def PMMTrustAnchorAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNING_PUBLIC_KEY), 0)

def AddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY):
    PMMTrustAnchorAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)

def PMMTrustAnchorAddSIGNING_KEY_PATH(builder, SIGNING_KEY_PATH):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNING_KEY_PATH), 0)

def AddSIGNING_KEY_PATH(builder, SIGNING_KEY_PATH):
    PMMTrustAnchorAddSIGNING_KEY_PATH(builder, SIGNING_KEY_PATH)

def PMMTrustAnchorAddSIGNATURE_ALGORITHM(builder, SIGNATURE_ALGORITHM):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SIGNATURE_ALGORITHM), 0)

def AddSIGNATURE_ALGORITHM(builder, SIGNATURE_ALGORITHM):
    PMMTrustAnchorAddSIGNATURE_ALGORITHM(builder, SIGNATURE_ALGORITHM)

def PMMTrustAnchorAddEPM_CID(builder, EPM_CID):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(EPM_CID), 0)

def AddEPM_CID(builder, EPM_CID):
    PMMTrustAnchorAddEPM_CID(builder, EPM_CID)

def PMMTrustAnchorAddDNS_PROOF_RECORD_NAME(builder, DNS_PROOF_RECORD_NAME):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(DNS_PROOF_RECORD_NAME), 0)

def AddDNS_PROOF_RECORD_NAME(builder, DNS_PROOF_RECORD_NAME):
    PMMTrustAnchorAddDNS_PROOF_RECORD_NAME(builder, DNS_PROOF_RECORD_NAME)

def PMMTrustAnchorAddDNS_PROOF_TXT(builder, DNS_PROOF_TXT):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(DNS_PROOF_TXT), 0)

def AddDNS_PROOF_TXT(builder, DNS_PROOF_TXT):
    PMMTrustAnchorAddDNS_PROOF_TXT(builder, DNS_PROOF_TXT)

def PMMTrustAnchorAddDNS_PROOF_STATEMENT(builder, DNS_PROOF_STATEMENT):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(DNS_PROOF_STATEMENT), 0)

def AddDNS_PROOF_STATEMENT(builder, DNS_PROOF_STATEMENT):
    PMMTrustAnchorAddDNS_PROOF_STATEMENT(builder, DNS_PROOF_STATEMENT)

def PMMTrustAnchorAddBOND_ADDRESSES(builder, BOND_ADDRESSES):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(BOND_ADDRESSES), 0)

def AddBOND_ADDRESSES(builder, BOND_ADDRESSES):
    PMMTrustAnchorAddBOND_ADDRESSES(builder, BOND_ADDRESSES)

def PMMTrustAnchorStartBOND_ADDRESSESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartBOND_ADDRESSESVector(builder, numElems):
    return PMMTrustAnchorStartBOND_ADDRESSESVector(builder, numElems)

def PMMTrustAnchorCreateBOND_ADDRESSESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateBOND_ADDRESSESVector(builder, data):
    PMMTrustAnchorCreateBOND_ADDRESSESVector(builder, data)

def PMMTrustAnchorAddBOND_ATTESTATION_URL(builder, BOND_ATTESTATION_URL):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(BOND_ATTESTATION_URL), 0)

def AddBOND_ATTESTATION_URL(builder, BOND_ATTESTATION_URL):
    PMMTrustAnchorAddBOND_ATTESTATION_URL(builder, BOND_ATTESTATION_URL)

def PMMTrustAnchorEnd(builder):
    return builder.EndObject()

def End(builder):
    return PMMTrustAnchorEnd(builder)

import ChainProof
try:
    from typing import List
except:
    pass

class PMMTrustAnchorT(object):

    # PMMTrustAnchorT
    def __init__(
        self,
        PROVIDER_DOMAIN = None,
        NODE_PEER_ID = None,
        NODE_XPUB = None,
        SIGNING_PUBLIC_KEY = None,
        SIGNING_KEY_PATH = None,
        SIGNATURE_ALGORITHM = None,
        EPM_CID = None,
        DNS_PROOF_RECORD_NAME = None,
        DNS_PROOF_TXT = None,
        DNS_PROOF_STATEMENT = None,
        BOND_ADDRESSES = None,
        BOND_ATTESTATION_URL = None,
    ):
        self.PROVIDER_DOMAIN = PROVIDER_DOMAIN  # type: Optional[str]
        self.NODE_PEER_ID = NODE_PEER_ID  # type: Optional[str]
        self.NODE_XPUB = NODE_XPUB  # type: Optional[str]
        self.SIGNING_PUBLIC_KEY = SIGNING_PUBLIC_KEY  # type: Optional[str]
        self.SIGNING_KEY_PATH = SIGNING_KEY_PATH  # type: Optional[str]
        self.SIGNATURE_ALGORITHM = SIGNATURE_ALGORITHM  # type: Optional[str]
        self.EPM_CID = EPM_CID  # type: Optional[str]
        self.DNS_PROOF_RECORD_NAME = DNS_PROOF_RECORD_NAME  # type: Optional[str]
        self.DNS_PROOF_TXT = DNS_PROOF_TXT  # type: Optional[str]
        self.DNS_PROOF_STATEMENT = DNS_PROOF_STATEMENT  # type: Optional[str]
        self.BOND_ADDRESSES = BOND_ADDRESSES  # type: Optional[List[ChainProof.ChainProofT]]
        self.BOND_ATTESTATION_URL = BOND_ATTESTATION_URL  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPmmtrustAnchor = PMMTrustAnchor()
        tmpPmmtrustAnchor.Init(buf, pos)
        return cls.InitFromObj(tmpPmmtrustAnchor)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPmmtrustAnchor):
        x = PMMTrustAnchorT()
        x._UnPack(tmpPmmtrustAnchor)
        return x

    # PMMTrustAnchorT
    def _UnPack(self, PMMTrustAnchor):
        if PMMTrustAnchor is None:
            return
        self.PROVIDER_DOMAIN = PMMTrustAnchor.PROVIDER_DOMAIN()
        self.NODE_PEER_ID = PMMTrustAnchor.NODE_PEER_ID()
        self.NODE_XPUB = PMMTrustAnchor.NODE_XPUB()
        self.SIGNING_PUBLIC_KEY = PMMTrustAnchor.SIGNING_PUBLIC_KEY()
        self.SIGNING_KEY_PATH = PMMTrustAnchor.SIGNING_KEY_PATH()
        self.SIGNATURE_ALGORITHM = PMMTrustAnchor.SIGNATURE_ALGORITHM()
        self.EPM_CID = PMMTrustAnchor.EPM_CID()
        self.DNS_PROOF_RECORD_NAME = PMMTrustAnchor.DNS_PROOF_RECORD_NAME()
        self.DNS_PROOF_TXT = PMMTrustAnchor.DNS_PROOF_TXT()
        self.DNS_PROOF_STATEMENT = PMMTrustAnchor.DNS_PROOF_STATEMENT()
        if not PMMTrustAnchor.BOND_ADDRESSESIsNone():
            self.BOND_ADDRESSES = []
            for i in range(PMMTrustAnchor.BOND_ADDRESSESLength()):
                if PMMTrustAnchor.BOND_ADDRESSES(i) is None:
                    self.BOND_ADDRESSES.append(None)
                else:
                    chainProof_ = ChainProof.ChainProofT.InitFromObj(PMMTrustAnchor.BOND_ADDRESSES(i))
                    self.BOND_ADDRESSES.append(chainProof_)
        self.BOND_ATTESTATION_URL = PMMTrustAnchor.BOND_ATTESTATION_URL()

    # PMMTrustAnchorT
    def Pack(self, builder):
        if self.PROVIDER_DOMAIN is not None:
            PROVIDER_DOMAIN = builder.CreateString(self.PROVIDER_DOMAIN)
        if self.NODE_PEER_ID is not None:
            NODE_PEER_ID = builder.CreateString(self.NODE_PEER_ID)
        if self.NODE_XPUB is not None:
            NODE_XPUB = builder.CreateString(self.NODE_XPUB)
        if self.SIGNING_PUBLIC_KEY is not None:
            SIGNING_PUBLIC_KEY = builder.CreateString(self.SIGNING_PUBLIC_KEY)
        if self.SIGNING_KEY_PATH is not None:
            SIGNING_KEY_PATH = builder.CreateString(self.SIGNING_KEY_PATH)
        if self.SIGNATURE_ALGORITHM is not None:
            SIGNATURE_ALGORITHM = builder.CreateString(self.SIGNATURE_ALGORITHM)
        if self.EPM_CID is not None:
            EPM_CID = builder.CreateString(self.EPM_CID)
        if self.DNS_PROOF_RECORD_NAME is not None:
            DNS_PROOF_RECORD_NAME = builder.CreateString(self.DNS_PROOF_RECORD_NAME)
        if self.DNS_PROOF_TXT is not None:
            DNS_PROOF_TXT = builder.CreateString(self.DNS_PROOF_TXT)
        if self.DNS_PROOF_STATEMENT is not None:
            DNS_PROOF_STATEMENT = builder.CreateString(self.DNS_PROOF_STATEMENT)
        if self.BOND_ADDRESSES is not None:
            BOND_ADDRESSESlist = []
            for i in range(len(self.BOND_ADDRESSES)):
                BOND_ADDRESSESlist.append(self.BOND_ADDRESSES[i].Pack(builder))
            PMMTrustAnchorStartBOND_ADDRESSESVector(builder, len(self.BOND_ADDRESSES))
            for i in reversed(range(len(self.BOND_ADDRESSES))):
                builder.PrependUOffsetTRelative(BOND_ADDRESSESlist[i])
            BOND_ADDRESSES = builder.EndVector()
        if self.BOND_ATTESTATION_URL is not None:
            BOND_ATTESTATION_URL = builder.CreateString(self.BOND_ATTESTATION_URL)
        PMMTrustAnchorStart(builder)
        if self.PROVIDER_DOMAIN is not None:
            PMMTrustAnchorAddPROVIDER_DOMAIN(builder, PROVIDER_DOMAIN)
        if self.NODE_PEER_ID is not None:
            PMMTrustAnchorAddNODE_PEER_ID(builder, NODE_PEER_ID)
        if self.NODE_XPUB is not None:
            PMMTrustAnchorAddNODE_XPUB(builder, NODE_XPUB)
        if self.SIGNING_PUBLIC_KEY is not None:
            PMMTrustAnchorAddSIGNING_PUBLIC_KEY(builder, SIGNING_PUBLIC_KEY)
        if self.SIGNING_KEY_PATH is not None:
            PMMTrustAnchorAddSIGNING_KEY_PATH(builder, SIGNING_KEY_PATH)
        if self.SIGNATURE_ALGORITHM is not None:
            PMMTrustAnchorAddSIGNATURE_ALGORITHM(builder, SIGNATURE_ALGORITHM)
        if self.EPM_CID is not None:
            PMMTrustAnchorAddEPM_CID(builder, EPM_CID)
        if self.DNS_PROOF_RECORD_NAME is not None:
            PMMTrustAnchorAddDNS_PROOF_RECORD_NAME(builder, DNS_PROOF_RECORD_NAME)
        if self.DNS_PROOF_TXT is not None:
            PMMTrustAnchorAddDNS_PROOF_TXT(builder, DNS_PROOF_TXT)
        if self.DNS_PROOF_STATEMENT is not None:
            PMMTrustAnchorAddDNS_PROOF_STATEMENT(builder, DNS_PROOF_STATEMENT)
        if self.BOND_ADDRESSES is not None:
            PMMTrustAnchorAddBOND_ADDRESSES(builder, BOND_ADDRESSES)
        if self.BOND_ATTESTATION_URL is not None:
            PMMTrustAnchorAddBOND_ATTESTATION_URL(builder, BOND_ATTESTATION_URL)
        PMMTrustAnchor = PMMTrustAnchorEnd(builder)
        return PMMTrustAnchor
