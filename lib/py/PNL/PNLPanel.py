# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One flat panel of a macro model.
#
# The reflection coefficients describe how incident radiation is disposed of:
# SPECULAR_REFLECTIVITY + DIFFUSE_REFLECTIVITY + ABSORPTIVITY should sum to
# 1 for a physically consistent panel. Producers SHOULD state the source of
# the optical properties in the parent record's SOURCE field; a panel model
# whose coefficients have no provenance is worse than none.
class PNLPanel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PNLPanel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPNLPanel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PNLPanelBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x4E\x4C", size_prefixed=size_prefixed)

    # PNLPanel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable panel identifier within the model.
    # PNLPanel
    def PANEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Panel area, square metres.
    # PNLPanel
    def AREA_M2(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Outward panel normal in the body frame, unit vector.
    # PNLPanel
    def NORMAL_X(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PNLPanel
    def NORMAL_Y(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PNLPanel
    def NORMAL_Z(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Orientation behaviour of the panel.
    # PNLPanel
    def TRACKING_MODE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 1

    # Fraction of incident radiation reflected specularly.
    # PNLPanel
    def SPECULAR_REFLECTIVITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Fraction reflected diffusely.
    # PNLPanel
    def DIFFUSE_REFLECTIVITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Fraction absorbed.
    # PNLPanel
    def ABSORPTIVITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Infrared emissivity of the panel surface.
    # PNLPanel
    def EMISSIVITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # True when absorbed energy is re-radiated instantaneously (no thermal
    # lag). False means the consumer must model thermal inertia.
    # PNLPanel
    def INSTANTANEOUS_RERADIATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return True

    # Panel drag coefficient, when the model is used for aerodynamic forces.
    # PNLPanel
    def DRAG_COEFFICIENT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Material name, for traceability back to a materials table.
    # PNLPanel
    def MATERIAL_NAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PNLPanelStart(builder):
    builder.StartObject(13)

def Start(builder):
    PNLPanelStart(builder)

def PNLPanelAddPANEL_ID(builder, PANEL_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(PANEL_ID), 0)

def AddPANEL_ID(builder, PANEL_ID):
    PNLPanelAddPANEL_ID(builder, PANEL_ID)

def PNLPanelAddAREA_M2(builder, AREA_M2):
    builder.PrependFloat64Slot(1, AREA_M2, 0.0)

def AddAREA_M2(builder, AREA_M2):
    PNLPanelAddAREA_M2(builder, AREA_M2)

def PNLPanelAddNORMAL_X(builder, NORMAL_X):
    builder.PrependFloat64Slot(2, NORMAL_X, 0.0)

def AddNORMAL_X(builder, NORMAL_X):
    PNLPanelAddNORMAL_X(builder, NORMAL_X)

def PNLPanelAddNORMAL_Y(builder, NORMAL_Y):
    builder.PrependFloat64Slot(3, NORMAL_Y, 0.0)

def AddNORMAL_Y(builder, NORMAL_Y):
    PNLPanelAddNORMAL_Y(builder, NORMAL_Y)

def PNLPanelAddNORMAL_Z(builder, NORMAL_Z):
    builder.PrependFloat64Slot(4, NORMAL_Z, 0.0)

def AddNORMAL_Z(builder, NORMAL_Z):
    PNLPanelAddNORMAL_Z(builder, NORMAL_Z)

def PNLPanelAddTRACKING_MODE(builder, TRACKING_MODE):
    builder.PrependUint8Slot(5, TRACKING_MODE, 1)

def AddTRACKING_MODE(builder, TRACKING_MODE):
    PNLPanelAddTRACKING_MODE(builder, TRACKING_MODE)

def PNLPanelAddSPECULAR_REFLECTIVITY(builder, SPECULAR_REFLECTIVITY):
    builder.PrependFloat64Slot(6, SPECULAR_REFLECTIVITY, 0.0)

def AddSPECULAR_REFLECTIVITY(builder, SPECULAR_REFLECTIVITY):
    PNLPanelAddSPECULAR_REFLECTIVITY(builder, SPECULAR_REFLECTIVITY)

def PNLPanelAddDIFFUSE_REFLECTIVITY(builder, DIFFUSE_REFLECTIVITY):
    builder.PrependFloat64Slot(7, DIFFUSE_REFLECTIVITY, 0.0)

def AddDIFFUSE_REFLECTIVITY(builder, DIFFUSE_REFLECTIVITY):
    PNLPanelAddDIFFUSE_REFLECTIVITY(builder, DIFFUSE_REFLECTIVITY)

def PNLPanelAddABSORPTIVITY(builder, ABSORPTIVITY):
    builder.PrependFloat64Slot(8, ABSORPTIVITY, 0.0)

def AddABSORPTIVITY(builder, ABSORPTIVITY):
    PNLPanelAddABSORPTIVITY(builder, ABSORPTIVITY)

def PNLPanelAddEMISSIVITY(builder, EMISSIVITY):
    builder.PrependFloat64Slot(9, EMISSIVITY, 0.0)

def AddEMISSIVITY(builder, EMISSIVITY):
    PNLPanelAddEMISSIVITY(builder, EMISSIVITY)

def PNLPanelAddINSTANTANEOUS_RERADIATION(builder, INSTANTANEOUS_RERADIATION):
    builder.PrependBoolSlot(10, INSTANTANEOUS_RERADIATION, 1)

def AddINSTANTANEOUS_RERADIATION(builder, INSTANTANEOUS_RERADIATION):
    PNLPanelAddINSTANTANEOUS_RERADIATION(builder, INSTANTANEOUS_RERADIATION)

def PNLPanelAddDRAG_COEFFICIENT(builder, DRAG_COEFFICIENT):
    builder.PrependFloat64Slot(11, DRAG_COEFFICIENT, 0.0)

def AddDRAG_COEFFICIENT(builder, DRAG_COEFFICIENT):
    PNLPanelAddDRAG_COEFFICIENT(builder, DRAG_COEFFICIENT)

def PNLPanelAddMATERIAL_NAME(builder, MATERIAL_NAME):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(MATERIAL_NAME), 0)

def AddMATERIAL_NAME(builder, MATERIAL_NAME):
    PNLPanelAddMATERIAL_NAME(builder, MATERIAL_NAME)

def PNLPanelEnd(builder):
    return builder.EndObject()

def End(builder):
    return PNLPanelEnd(builder)


class PNLPanelT(object):

    # PNLPanelT
    def __init__(
        self,
        PANEL_ID = None,
        AREA_M2 = 0.0,
        NORMAL_X = 0.0,
        NORMAL_Y = 0.0,
        NORMAL_Z = 0.0,
        TRACKING_MODE = 1,
        SPECULAR_REFLECTIVITY = 0.0,
        DIFFUSE_REFLECTIVITY = 0.0,
        ABSORPTIVITY = 0.0,
        EMISSIVITY = 0.0,
        INSTANTANEOUS_RERADIATION = True,
        DRAG_COEFFICIENT = 0.0,
        MATERIAL_NAME = None,
    ):
        self.PANEL_ID = PANEL_ID  # type: Optional[str]
        self.AREA_M2 = AREA_M2  # type: float
        self.NORMAL_X = NORMAL_X  # type: float
        self.NORMAL_Y = NORMAL_Y  # type: float
        self.NORMAL_Z = NORMAL_Z  # type: float
        self.TRACKING_MODE = TRACKING_MODE  # type: int
        self.SPECULAR_REFLECTIVITY = SPECULAR_REFLECTIVITY  # type: float
        self.DIFFUSE_REFLECTIVITY = DIFFUSE_REFLECTIVITY  # type: float
        self.ABSORPTIVITY = ABSORPTIVITY  # type: float
        self.EMISSIVITY = EMISSIVITY  # type: float
        self.INSTANTANEOUS_RERADIATION = INSTANTANEOUS_RERADIATION  # type: bool
        self.DRAG_COEFFICIENT = DRAG_COEFFICIENT  # type: float
        self.MATERIAL_NAME = MATERIAL_NAME  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPnlpanel = PNLPanel()
        tmpPnlpanel.Init(buf, pos)
        return cls.InitFromObj(tmpPnlpanel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPnlpanel):
        x = PNLPanelT()
        x._UnPack(tmpPnlpanel)
        return x

    # PNLPanelT
    def _UnPack(self, PNLPanel):
        if PNLPanel is None:
            return
        self.PANEL_ID = PNLPanel.PANEL_ID()
        self.AREA_M2 = PNLPanel.AREA_M2()
        self.NORMAL_X = PNLPanel.NORMAL_X()
        self.NORMAL_Y = PNLPanel.NORMAL_Y()
        self.NORMAL_Z = PNLPanel.NORMAL_Z()
        self.TRACKING_MODE = PNLPanel.TRACKING_MODE()
        self.SPECULAR_REFLECTIVITY = PNLPanel.SPECULAR_REFLECTIVITY()
        self.DIFFUSE_REFLECTIVITY = PNLPanel.DIFFUSE_REFLECTIVITY()
        self.ABSORPTIVITY = PNLPanel.ABSORPTIVITY()
        self.EMISSIVITY = PNLPanel.EMISSIVITY()
        self.INSTANTANEOUS_RERADIATION = PNLPanel.INSTANTANEOUS_RERADIATION()
        self.DRAG_COEFFICIENT = PNLPanel.DRAG_COEFFICIENT()
        self.MATERIAL_NAME = PNLPanel.MATERIAL_NAME()

    # PNLPanelT
    def Pack(self, builder):
        if self.PANEL_ID is not None:
            PANEL_ID = builder.CreateString(self.PANEL_ID)
        if self.MATERIAL_NAME is not None:
            MATERIAL_NAME = builder.CreateString(self.MATERIAL_NAME)
        PNLPanelStart(builder)
        if self.PANEL_ID is not None:
            PNLPanelAddPANEL_ID(builder, PANEL_ID)
        PNLPanelAddAREA_M2(builder, self.AREA_M2)
        PNLPanelAddNORMAL_X(builder, self.NORMAL_X)
        PNLPanelAddNORMAL_Y(builder, self.NORMAL_Y)
        PNLPanelAddNORMAL_Z(builder, self.NORMAL_Z)
        PNLPanelAddTRACKING_MODE(builder, self.TRACKING_MODE)
        PNLPanelAddSPECULAR_REFLECTIVITY(builder, self.SPECULAR_REFLECTIVITY)
        PNLPanelAddDIFFUSE_REFLECTIVITY(builder, self.DIFFUSE_REFLECTIVITY)
        PNLPanelAddABSORPTIVITY(builder, self.ABSORPTIVITY)
        PNLPanelAddEMISSIVITY(builder, self.EMISSIVITY)
        PNLPanelAddINSTANTANEOUS_RERADIATION(builder, self.INSTANTANEOUS_RERADIATION)
        PNLPanelAddDRAG_COEFFICIENT(builder, self.DRAG_COEFFICIENT)
        if self.MATERIAL_NAME is not None:
            PNLPanelAddMATERIAL_NAME(builder, MATERIAL_NAME)
        PNLPanel = PNLPanelEnd(builder)
        return PNLPanel
