# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Trust Node Record - Durable node membership state for an SDN trust graph.
#
# TNR records preserve isolated trust graph nodes that do not currently have
# an active trust edge. The current graph is a projection over the latest TNR
# and TRE records, where deleted records are tombstones.
class TNR(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TNR()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTNR(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TNRBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x4E\x52", size_prefixed=size_prefixed)

    # TNR
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable trust graph node identifier, normally an SDN peer ID or account id.
    # TNR
    def NODE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Unix timestamp in milliseconds when this node record was created.
    # TNR
    def CREATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Unix timestamp in milliseconds when this node record was last updated.
    # TNR
    def UPDATED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # True when this record tombstones the node from the current graph.
    # TNR
    def DELETED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Peer ID of the node/provider that issued this trust graph record.
    # TNR
    def PROVIDER_PEER_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider signature over the trust node record.
    # TNR
    def PROVIDER_SIGNATURE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TNR
    def PROVIDER_SIGNATUREAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TNR
    def PROVIDER_SIGNATURELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TNR
    def PROVIDER_SIGNATUREIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

def TNRStart(builder):
    builder.StartObject(6)

def Start(builder):
    TNRStart(builder)

def TNRAddNODE_ID(builder, NODE_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(NODE_ID), 0)

def AddNODE_ID(builder, NODE_ID):
    TNRAddNODE_ID(builder, NODE_ID)

def TNRAddCREATED_AT(builder, CREATED_AT):
    builder.PrependUint64Slot(1, CREATED_AT, 0)

def AddCREATED_AT(builder, CREATED_AT):
    TNRAddCREATED_AT(builder, CREATED_AT)

def TNRAddUPDATED_AT(builder, UPDATED_AT):
    builder.PrependUint64Slot(2, UPDATED_AT, 0)

def AddUPDATED_AT(builder, UPDATED_AT):
    TNRAddUPDATED_AT(builder, UPDATED_AT)

def TNRAddDELETED(builder, DELETED):
    builder.PrependBoolSlot(3, DELETED, 0)

def AddDELETED(builder, DELETED):
    TNRAddDELETED(builder, DELETED)

def TNRAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_PEER_ID), 0)

def AddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID):
    TNRAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)

def TNRAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_SIGNATURE), 0)

def AddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE):
    TNRAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)

def TNRStartPROVIDER_SIGNATUREVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPROVIDER_SIGNATUREVector(builder, numElems):
    return TNRStartPROVIDER_SIGNATUREVector(builder, numElems)

def TNRCreatePROVIDER_SIGNATUREVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePROVIDER_SIGNATUREVector(builder, data):
    TNRCreatePROVIDER_SIGNATUREVector(builder, data)

def TNREnd(builder):
    return builder.EndObject()

def End(builder):
    return TNREnd(builder)

try:
    from typing import List
except:
    pass

class TNRT(object):

    # TNRT
    def __init__(
        self,
        NODE_ID = None,
        CREATED_AT = 0,
        UPDATED_AT = 0,
        DELETED = False,
        PROVIDER_PEER_ID = None,
        PROVIDER_SIGNATURE = None,
    ):
        self.NODE_ID = NODE_ID  # type: Optional[str]
        self.CREATED_AT = CREATED_AT  # type: int
        self.UPDATED_AT = UPDATED_AT  # type: int
        self.DELETED = DELETED  # type: bool
        self.PROVIDER_PEER_ID = PROVIDER_PEER_ID  # type: Optional[str]
        self.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTnr = TNR()
        tmpTnr.Init(buf, pos)
        return cls.InitFromObj(tmpTnr)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTnr):
        x = TNRT()
        x._UnPack(tmpTnr)
        return x

    # TNRT
    def _UnPack(self, TNR):
        if TNR is None:
            return
        self.NODE_ID = TNR.NODE_ID()
        self.CREATED_AT = TNR.CREATED_AT()
        self.UPDATED_AT = TNR.UPDATED_AT()
        self.DELETED = TNR.DELETED()
        self.PROVIDER_PEER_ID = TNR.PROVIDER_PEER_ID()
        if not TNR.PROVIDER_SIGNATUREIsNone():
            if np is None:
                self.PROVIDER_SIGNATURE = []
                for i in range(TNR.PROVIDER_SIGNATURELength()):
                    self.PROVIDER_SIGNATURE.append(TNR.PROVIDER_SIGNATURE(i))
            else:
                self.PROVIDER_SIGNATURE = TNR.PROVIDER_SIGNATUREAsNumpy()

    # TNRT
    def Pack(self, builder):
        if self.NODE_ID is not None:
            NODE_ID = builder.CreateString(self.NODE_ID)
        if self.PROVIDER_PEER_ID is not None:
            PROVIDER_PEER_ID = builder.CreateString(self.PROVIDER_PEER_ID)
        if self.PROVIDER_SIGNATURE is not None:
            if np is not None and type(self.PROVIDER_SIGNATURE) is np.ndarray:
                PROVIDER_SIGNATURE = builder.CreateNumpyVector(self.PROVIDER_SIGNATURE)
            else:
                TNRStartPROVIDER_SIGNATUREVector(builder, len(self.PROVIDER_SIGNATURE))
                for i in reversed(range(len(self.PROVIDER_SIGNATURE))):
                    builder.PrependUint8(self.PROVIDER_SIGNATURE[i])
                PROVIDER_SIGNATURE = builder.EndVector()
        TNRStart(builder)
        if self.NODE_ID is not None:
            TNRAddNODE_ID(builder, NODE_ID)
        TNRAddCREATED_AT(builder, self.CREATED_AT)
        TNRAddUPDATED_AT(builder, self.UPDATED_AT)
        TNRAddDELETED(builder, self.DELETED)
        if self.PROVIDER_PEER_ID is not None:
            TNRAddPROVIDER_PEER_ID(builder, PROVIDER_PEER_ID)
        if self.PROVIDER_SIGNATURE is not None:
            TNRAddPROVIDER_SIGNATURE(builder, PROVIDER_SIGNATURE)
        TNR = TNREnd(builder)
        return TNR
