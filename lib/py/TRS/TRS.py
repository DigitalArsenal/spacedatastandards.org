# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Terrain raster solve envelope.
class TRS(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRS()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRS(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRSBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x53", size_prefixed=size_prefixed)

    # TRS
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TRS
    def REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TRSRequest import TRSRequest
            obj = TRSRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TRS
    def RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TRSResult import TRSResult
            obj = TRSResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def TRSStart(builder):
    builder.StartObject(2)

def Start(builder):
    TRSStart(builder)

def TRSAddREQUEST(builder, REQUEST):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST), 0)

def AddREQUEST(builder, REQUEST):
    TRSAddREQUEST(builder, REQUEST)

def TRSAddRESULT(builder, RESULT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(RESULT), 0)

def AddRESULT(builder, RESULT):
    TRSAddRESULT(builder, RESULT)

def TRSEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRSEnd(builder)

import TRSRequest
import TRSResult
try:
    from typing import Optional
except:
    pass

class TRST(object):

    # TRST
    def __init__(
        self,
        REQUEST = None,
        RESULT = None,
    ):
        self.REQUEST = REQUEST  # type: Optional[TRSRequest.TRSRequestT]
        self.RESULT = RESULT  # type: Optional[TRSResult.TRSResultT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrs = TRS()
        tmpTrs.Init(buf, pos)
        return cls.InitFromObj(tmpTrs)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrs):
        x = TRST()
        x._UnPack(tmpTrs)
        return x

    # TRST
    def _UnPack(self, TRS):
        if TRS is None:
            return
        if TRS.REQUEST() is not None:
            self.REQUEST = TRSRequest.TRSRequestT.InitFromObj(TRS.REQUEST())
        if TRS.RESULT() is not None:
            self.RESULT = TRSResult.TRSResultT.InitFromObj(TRS.RESULT())

    # TRST
    def Pack(self, builder):
        if self.REQUEST is not None:
            REQUEST = self.REQUEST.Pack(builder)
        if self.RESULT is not None:
            RESULT = self.RESULT.Pack(builder)
        TRSStart(builder)
        if self.REQUEST is not None:
            TRSAddREQUEST(builder, REQUEST)
        if self.RESULT is not None:
            TRSAddRESULT(builder, RESULT)
        TRS = TRSEnd(builder)
        return TRS
