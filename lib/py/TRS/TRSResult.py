# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One terrain raster solve result. On STATUS = OK every layer below is
# present with exactly GRID_WIDTH * GRID_HEIGHT row-major entries. On any
# other STATUS the result carries NO layers — never a partial raster.
class TRSResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRSResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRSResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRSResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x53", size_prefixed=size_prefixed)

    # TRSResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TRSResult
    def STATUS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # On REFUSED or INVALID_INPUT: names the exact gap or defect. Empty on
    # OK.
    # TRSResult
    def ERROR_MESSAGE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Output raster dimensions the layers are stated for, echoed from the
    # request.
    # TRSResult
    def GRID_WIDTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # TRSResult
    def GRID_HEIGHT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # 1 where the geometric ray from the emitter clears the obstruction
    # surface, else 0.
    # TRSResult
    def VISIBLE_MASK(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRSResult
    def VISIBLE_MASKAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TRSResult
    def VISIBLE_MASKLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def VISIBLE_MASKIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Complement of VISIBLE_MASK.
    # TRSResult
    def OCCLUDED_MASK(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRSResult
    def OCCLUDED_MASKAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TRSResult
    def OCCLUDED_MASKLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def OCCLUDED_MASKIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # 1 where LINK_MARGIN_DB > 0, else 0.
    # TRSResult
    def POSITIVE_LINK_MASK(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRSResult
    def POSITIVE_LINK_MASKAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TRSResult
    def POSITIVE_LINK_MASKLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def POSITIVE_LINK_MASKIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Path classification of every cell from the dominant-edge diffraction
    # parameter — see trsPathClass.
    # TRSResult
    def PATH_CLASS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRSResult
    def PATH_CLASSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int8Flags, o)
        return 0

    # TRSResult
    def PATH_CLASSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def PATH_CLASSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # Total path loss including every applied term, dB (positive).
    # TRSResult
    def PATH_LOSS_DB(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # TRSResult
    def PATH_LOSS_DBAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # TRSResult
    def PATH_LOSS_DBLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def PATH_LOSS_DBIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # Loss above free space — what the terrain cost the cell, dB.
    # TRSResult
    def EXCESS_LOSS_DB(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # TRSResult
    def EXCESS_LOSS_DBAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # TRSResult
    def EXCESS_LOSS_DBLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def EXCESS_LOSS_DBIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # Link budget margin, dB.
    # TRSResult
    def LINK_MARGIN_DB(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # TRSResult
    def LINK_MARGIN_DBAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # TRSResult
    def LINK_MARGIN_DBLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def LINK_MARGIN_DBIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # ITU-R P.526-15 eq. (29) diffraction parameter v of the dominant edge.
    # Exactly -10.0 where no finite v exists (an unobstructed, Fresnel-clear
    # path) — the layer's stated no-finite-v encoding, carried verbatim so
    # the FlatBuffer and aligned-binary forms stay byte-comparable.
    # TRSResult
    def DIFFRACTION_PARAMETER(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # TRSResult
    def DIFFRACTION_PARAMETERAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # TRSResult
    def DIFFRACTION_PARAMETERLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def DIFFRACTION_PARAMETERIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # Specular ground-bounce contribution, dB. All zeros outside the
    # SPECULAR_SINGLE_BOUNCE tier.
    # TRSResult
    def REFLECTION_CONTRIBUTION_DB(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # TRSResult
    def REFLECTION_CONTRIBUTION_DBAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # TRSResult
    def REFLECTION_CONTRIBUTION_DBLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRSResult
    def REFLECTION_CONTRIBUTION_DBIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

    # Caller trace/correlation identifier copied from the request when
    # present.
    # TRSResult
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TRSResultStart(builder):
    builder.StartObject(14)

def Start(builder):
    TRSResultStart(builder)

def TRSResultAddSTATUS(builder, STATUS):
    builder.PrependInt8Slot(0, STATUS, 0)

def AddSTATUS(builder, STATUS):
    TRSResultAddSTATUS(builder, STATUS)

def TRSResultAddERROR_MESSAGE(builder, ERROR_MESSAGE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ERROR_MESSAGE), 0)

def AddERROR_MESSAGE(builder, ERROR_MESSAGE):
    TRSResultAddERROR_MESSAGE(builder, ERROR_MESSAGE)

def TRSResultAddGRID_WIDTH(builder, GRID_WIDTH):
    builder.PrependUint32Slot(2, GRID_WIDTH, 0)

def AddGRID_WIDTH(builder, GRID_WIDTH):
    TRSResultAddGRID_WIDTH(builder, GRID_WIDTH)

def TRSResultAddGRID_HEIGHT(builder, GRID_HEIGHT):
    builder.PrependUint32Slot(3, GRID_HEIGHT, 0)

def AddGRID_HEIGHT(builder, GRID_HEIGHT):
    TRSResultAddGRID_HEIGHT(builder, GRID_HEIGHT)

def TRSResultAddVISIBLE_MASK(builder, VISIBLE_MASK):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(VISIBLE_MASK), 0)

def AddVISIBLE_MASK(builder, VISIBLE_MASK):
    TRSResultAddVISIBLE_MASK(builder, VISIBLE_MASK)

def TRSResultStartVISIBLE_MASKVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartVISIBLE_MASKVector(builder, numElems):
    return TRSResultStartVISIBLE_MASKVector(builder, numElems)

def TRSResultCreateVISIBLE_MASKVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateVISIBLE_MASKVector(builder, data):
    TRSResultCreateVISIBLE_MASKVector(builder, data)

def TRSResultAddOCCLUDED_MASK(builder, OCCLUDED_MASK):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(OCCLUDED_MASK), 0)

def AddOCCLUDED_MASK(builder, OCCLUDED_MASK):
    TRSResultAddOCCLUDED_MASK(builder, OCCLUDED_MASK)

def TRSResultStartOCCLUDED_MASKVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartOCCLUDED_MASKVector(builder, numElems):
    return TRSResultStartOCCLUDED_MASKVector(builder, numElems)

def TRSResultCreateOCCLUDED_MASKVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateOCCLUDED_MASKVector(builder, data):
    TRSResultCreateOCCLUDED_MASKVector(builder, data)

def TRSResultAddPOSITIVE_LINK_MASK(builder, POSITIVE_LINK_MASK):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(POSITIVE_LINK_MASK), 0)

def AddPOSITIVE_LINK_MASK(builder, POSITIVE_LINK_MASK):
    TRSResultAddPOSITIVE_LINK_MASK(builder, POSITIVE_LINK_MASK)

def TRSResultStartPOSITIVE_LINK_MASKVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPOSITIVE_LINK_MASKVector(builder, numElems):
    return TRSResultStartPOSITIVE_LINK_MASKVector(builder, numElems)

def TRSResultCreatePOSITIVE_LINK_MASKVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePOSITIVE_LINK_MASKVector(builder, data):
    TRSResultCreatePOSITIVE_LINK_MASKVector(builder, data)

def TRSResultAddPATH_CLASS(builder, PATH_CLASS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(PATH_CLASS), 0)

def AddPATH_CLASS(builder, PATH_CLASS):
    TRSResultAddPATH_CLASS(builder, PATH_CLASS)

def TRSResultStartPATH_CLASSVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPATH_CLASSVector(builder, numElems):
    return TRSResultStartPATH_CLASSVector(builder, numElems)

def TRSResultCreatePATH_CLASSVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependInt8(item)
    return builder.EndVector()

def CreatePATH_CLASSVector(builder, data):
    TRSResultCreatePATH_CLASSVector(builder, data)

def TRSResultAddPATH_LOSS_DB(builder, PATH_LOSS_DB):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(PATH_LOSS_DB), 0)

def AddPATH_LOSS_DB(builder, PATH_LOSS_DB):
    TRSResultAddPATH_LOSS_DB(builder, PATH_LOSS_DB)

def TRSResultStartPATH_LOSS_DBVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPATH_LOSS_DBVector(builder, numElems):
    return TRSResultStartPATH_LOSS_DBVector(builder, numElems)

def TRSResultCreatePATH_LOSS_DBVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreatePATH_LOSS_DBVector(builder, data):
    TRSResultCreatePATH_LOSS_DBVector(builder, data)

def TRSResultAddEXCESS_LOSS_DB(builder, EXCESS_LOSS_DB):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(EXCESS_LOSS_DB), 0)

def AddEXCESS_LOSS_DB(builder, EXCESS_LOSS_DB):
    TRSResultAddEXCESS_LOSS_DB(builder, EXCESS_LOSS_DB)

def TRSResultStartEXCESS_LOSS_DBVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartEXCESS_LOSS_DBVector(builder, numElems):
    return TRSResultStartEXCESS_LOSS_DBVector(builder, numElems)

def TRSResultCreateEXCESS_LOSS_DBVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateEXCESS_LOSS_DBVector(builder, data):
    TRSResultCreateEXCESS_LOSS_DBVector(builder, data)

def TRSResultAddLINK_MARGIN_DB(builder, LINK_MARGIN_DB):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(LINK_MARGIN_DB), 0)

def AddLINK_MARGIN_DB(builder, LINK_MARGIN_DB):
    TRSResultAddLINK_MARGIN_DB(builder, LINK_MARGIN_DB)

def TRSResultStartLINK_MARGIN_DBVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartLINK_MARGIN_DBVector(builder, numElems):
    return TRSResultStartLINK_MARGIN_DBVector(builder, numElems)

def TRSResultCreateLINK_MARGIN_DBVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateLINK_MARGIN_DBVector(builder, data):
    TRSResultCreateLINK_MARGIN_DBVector(builder, data)

def TRSResultAddDIFFRACTION_PARAMETER(builder, DIFFRACTION_PARAMETER):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(DIFFRACTION_PARAMETER), 0)

def AddDIFFRACTION_PARAMETER(builder, DIFFRACTION_PARAMETER):
    TRSResultAddDIFFRACTION_PARAMETER(builder, DIFFRACTION_PARAMETER)

def TRSResultStartDIFFRACTION_PARAMETERVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDIFFRACTION_PARAMETERVector(builder, numElems):
    return TRSResultStartDIFFRACTION_PARAMETERVector(builder, numElems)

def TRSResultCreateDIFFRACTION_PARAMETERVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateDIFFRACTION_PARAMETERVector(builder, data):
    TRSResultCreateDIFFRACTION_PARAMETERVector(builder, data)

def TRSResultAddREFLECTION_CONTRIBUTION_DB(builder, REFLECTION_CONTRIBUTION_DB):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(REFLECTION_CONTRIBUTION_DB), 0)

def AddREFLECTION_CONTRIBUTION_DB(builder, REFLECTION_CONTRIBUTION_DB):
    TRSResultAddREFLECTION_CONTRIBUTION_DB(builder, REFLECTION_CONTRIBUTION_DB)

def TRSResultStartREFLECTION_CONTRIBUTION_DBVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartREFLECTION_CONTRIBUTION_DBVector(builder, numElems):
    return TRSResultStartREFLECTION_CONTRIBUTION_DBVector(builder, numElems)

def TRSResultCreateREFLECTION_CONTRIBUTION_DBVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependFloat32(item)
    return builder.EndVector()

def CreateREFLECTION_CONTRIBUTION_DBVector(builder, data):
    TRSResultCreateREFLECTION_CONTRIBUTION_DBVector(builder, data)

def TRSResultAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(TRACE_ID), 0)

def AddTRACE_ID(builder, TRACE_ID):
    TRSResultAddTRACE_ID(builder, TRACE_ID)

def TRSResultEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRSResultEnd(builder)

try:
    from typing import List
except:
    pass

class TRSResultT(object):

    # TRSResultT
    def __init__(
        self,
        STATUS = 0,
        ERROR_MESSAGE = None,
        GRID_WIDTH = 0,
        GRID_HEIGHT = 0,
        VISIBLE_MASK = None,
        OCCLUDED_MASK = None,
        POSITIVE_LINK_MASK = None,
        PATH_CLASS = None,
        PATH_LOSS_DB = None,
        EXCESS_LOSS_DB = None,
        LINK_MARGIN_DB = None,
        DIFFRACTION_PARAMETER = None,
        REFLECTION_CONTRIBUTION_DB = None,
        TRACE_ID = None,
    ):
        self.STATUS = STATUS  # type: int
        self.ERROR_MESSAGE = ERROR_MESSAGE  # type: Optional[str]
        self.GRID_WIDTH = GRID_WIDTH  # type: int
        self.GRID_HEIGHT = GRID_HEIGHT  # type: int
        self.VISIBLE_MASK = VISIBLE_MASK  # type: Optional[List[int]]
        self.OCCLUDED_MASK = OCCLUDED_MASK  # type: Optional[List[int]]
        self.POSITIVE_LINK_MASK = POSITIVE_LINK_MASK  # type: Optional[List[int]]
        self.PATH_CLASS = PATH_CLASS  # type: Optional[List[int]]
        self.PATH_LOSS_DB = PATH_LOSS_DB  # type: Optional[List[float]]
        self.EXCESS_LOSS_DB = EXCESS_LOSS_DB  # type: Optional[List[float]]
        self.LINK_MARGIN_DB = LINK_MARGIN_DB  # type: Optional[List[float]]
        self.DIFFRACTION_PARAMETER = DIFFRACTION_PARAMETER  # type: Optional[List[float]]
        self.REFLECTION_CONTRIBUTION_DB = REFLECTION_CONTRIBUTION_DB  # type: Optional[List[float]]
        self.TRACE_ID = TRACE_ID  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpTrsresult = TRSResult()
        tmpTrsresult.Init(buf, pos)
        return cls.InitFromObj(tmpTrsresult)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpTrsresult):
        x = TRSResultT()
        x._UnPack(tmpTrsresult)
        return x

    # TRSResultT
    def _UnPack(self, TRSResult):
        if TRSResult is None:
            return
        self.STATUS = TRSResult.STATUS()
        self.ERROR_MESSAGE = TRSResult.ERROR_MESSAGE()
        self.GRID_WIDTH = TRSResult.GRID_WIDTH()
        self.GRID_HEIGHT = TRSResult.GRID_HEIGHT()
        if not TRSResult.VISIBLE_MASKIsNone():
            if np is None:
                self.VISIBLE_MASK = []
                for i in range(TRSResult.VISIBLE_MASKLength()):
                    self.VISIBLE_MASK.append(TRSResult.VISIBLE_MASK(i))
            else:
                self.VISIBLE_MASK = TRSResult.VISIBLE_MASKAsNumpy()
        if not TRSResult.OCCLUDED_MASKIsNone():
            if np is None:
                self.OCCLUDED_MASK = []
                for i in range(TRSResult.OCCLUDED_MASKLength()):
                    self.OCCLUDED_MASK.append(TRSResult.OCCLUDED_MASK(i))
            else:
                self.OCCLUDED_MASK = TRSResult.OCCLUDED_MASKAsNumpy()
        if not TRSResult.POSITIVE_LINK_MASKIsNone():
            if np is None:
                self.POSITIVE_LINK_MASK = []
                for i in range(TRSResult.POSITIVE_LINK_MASKLength()):
                    self.POSITIVE_LINK_MASK.append(TRSResult.POSITIVE_LINK_MASK(i))
            else:
                self.POSITIVE_LINK_MASK = TRSResult.POSITIVE_LINK_MASKAsNumpy()
        if not TRSResult.PATH_CLASSIsNone():
            if np is None:
                self.PATH_CLASS = []
                for i in range(TRSResult.PATH_CLASSLength()):
                    self.PATH_CLASS.append(TRSResult.PATH_CLASS(i))
            else:
                self.PATH_CLASS = TRSResult.PATH_CLASSAsNumpy()
        if not TRSResult.PATH_LOSS_DBIsNone():
            if np is None:
                self.PATH_LOSS_DB = []
                for i in range(TRSResult.PATH_LOSS_DBLength()):
                    self.PATH_LOSS_DB.append(TRSResult.PATH_LOSS_DB(i))
            else:
                self.PATH_LOSS_DB = TRSResult.PATH_LOSS_DBAsNumpy()
        if not TRSResult.EXCESS_LOSS_DBIsNone():
            if np is None:
                self.EXCESS_LOSS_DB = []
                for i in range(TRSResult.EXCESS_LOSS_DBLength()):
                    self.EXCESS_LOSS_DB.append(TRSResult.EXCESS_LOSS_DB(i))
            else:
                self.EXCESS_LOSS_DB = TRSResult.EXCESS_LOSS_DBAsNumpy()
        if not TRSResult.LINK_MARGIN_DBIsNone():
            if np is None:
                self.LINK_MARGIN_DB = []
                for i in range(TRSResult.LINK_MARGIN_DBLength()):
                    self.LINK_MARGIN_DB.append(TRSResult.LINK_MARGIN_DB(i))
            else:
                self.LINK_MARGIN_DB = TRSResult.LINK_MARGIN_DBAsNumpy()
        if not TRSResult.DIFFRACTION_PARAMETERIsNone():
            if np is None:
                self.DIFFRACTION_PARAMETER = []
                for i in range(TRSResult.DIFFRACTION_PARAMETERLength()):
                    self.DIFFRACTION_PARAMETER.append(TRSResult.DIFFRACTION_PARAMETER(i))
            else:
                self.DIFFRACTION_PARAMETER = TRSResult.DIFFRACTION_PARAMETERAsNumpy()
        if not TRSResult.REFLECTION_CONTRIBUTION_DBIsNone():
            if np is None:
                self.REFLECTION_CONTRIBUTION_DB = []
                for i in range(TRSResult.REFLECTION_CONTRIBUTION_DBLength()):
                    self.REFLECTION_CONTRIBUTION_DB.append(TRSResult.REFLECTION_CONTRIBUTION_DB(i))
            else:
                self.REFLECTION_CONTRIBUTION_DB = TRSResult.REFLECTION_CONTRIBUTION_DBAsNumpy()
        self.TRACE_ID = TRSResult.TRACE_ID()

    # TRSResultT
    def Pack(self, builder):
        if self.ERROR_MESSAGE is not None:
            ERROR_MESSAGE = builder.CreateString(self.ERROR_MESSAGE)
        if self.VISIBLE_MASK is not None:
            if np is not None and type(self.VISIBLE_MASK) is np.ndarray:
                VISIBLE_MASK = builder.CreateNumpyVector(self.VISIBLE_MASK)
            else:
                TRSResultStartVISIBLE_MASKVector(builder, len(self.VISIBLE_MASK))
                for i in reversed(range(len(self.VISIBLE_MASK))):
                    builder.PrependUint8(self.VISIBLE_MASK[i])
                VISIBLE_MASK = builder.EndVector()
        if self.OCCLUDED_MASK is not None:
            if np is not None and type(self.OCCLUDED_MASK) is np.ndarray:
                OCCLUDED_MASK = builder.CreateNumpyVector(self.OCCLUDED_MASK)
            else:
                TRSResultStartOCCLUDED_MASKVector(builder, len(self.OCCLUDED_MASK))
                for i in reversed(range(len(self.OCCLUDED_MASK))):
                    builder.PrependUint8(self.OCCLUDED_MASK[i])
                OCCLUDED_MASK = builder.EndVector()
        if self.POSITIVE_LINK_MASK is not None:
            if np is not None and type(self.POSITIVE_LINK_MASK) is np.ndarray:
                POSITIVE_LINK_MASK = builder.CreateNumpyVector(self.POSITIVE_LINK_MASK)
            else:
                TRSResultStartPOSITIVE_LINK_MASKVector(builder, len(self.POSITIVE_LINK_MASK))
                for i in reversed(range(len(self.POSITIVE_LINK_MASK))):
                    builder.PrependUint8(self.POSITIVE_LINK_MASK[i])
                POSITIVE_LINK_MASK = builder.EndVector()
        if self.PATH_CLASS is not None:
            if np is not None and type(self.PATH_CLASS) is np.ndarray:
                PATH_CLASS = builder.CreateNumpyVector(self.PATH_CLASS)
            else:
                TRSResultStartPATH_CLASSVector(builder, len(self.PATH_CLASS))
                for i in reversed(range(len(self.PATH_CLASS))):
                    builder.PrependByte(self.PATH_CLASS[i])
                PATH_CLASS = builder.EndVector()
        if self.PATH_LOSS_DB is not None:
            if np is not None and type(self.PATH_LOSS_DB) is np.ndarray:
                PATH_LOSS_DB = builder.CreateNumpyVector(self.PATH_LOSS_DB)
            else:
                TRSResultStartPATH_LOSS_DBVector(builder, len(self.PATH_LOSS_DB))
                for i in reversed(range(len(self.PATH_LOSS_DB))):
                    builder.PrependFloat32(self.PATH_LOSS_DB[i])
                PATH_LOSS_DB = builder.EndVector()
        if self.EXCESS_LOSS_DB is not None:
            if np is not None and type(self.EXCESS_LOSS_DB) is np.ndarray:
                EXCESS_LOSS_DB = builder.CreateNumpyVector(self.EXCESS_LOSS_DB)
            else:
                TRSResultStartEXCESS_LOSS_DBVector(builder, len(self.EXCESS_LOSS_DB))
                for i in reversed(range(len(self.EXCESS_LOSS_DB))):
                    builder.PrependFloat32(self.EXCESS_LOSS_DB[i])
                EXCESS_LOSS_DB = builder.EndVector()
        if self.LINK_MARGIN_DB is not None:
            if np is not None and type(self.LINK_MARGIN_DB) is np.ndarray:
                LINK_MARGIN_DB = builder.CreateNumpyVector(self.LINK_MARGIN_DB)
            else:
                TRSResultStartLINK_MARGIN_DBVector(builder, len(self.LINK_MARGIN_DB))
                for i in reversed(range(len(self.LINK_MARGIN_DB))):
                    builder.PrependFloat32(self.LINK_MARGIN_DB[i])
                LINK_MARGIN_DB = builder.EndVector()
        if self.DIFFRACTION_PARAMETER is not None:
            if np is not None and type(self.DIFFRACTION_PARAMETER) is np.ndarray:
                DIFFRACTION_PARAMETER = builder.CreateNumpyVector(self.DIFFRACTION_PARAMETER)
            else:
                TRSResultStartDIFFRACTION_PARAMETERVector(builder, len(self.DIFFRACTION_PARAMETER))
                for i in reversed(range(len(self.DIFFRACTION_PARAMETER))):
                    builder.PrependFloat32(self.DIFFRACTION_PARAMETER[i])
                DIFFRACTION_PARAMETER = builder.EndVector()
        if self.REFLECTION_CONTRIBUTION_DB is not None:
            if np is not None and type(self.REFLECTION_CONTRIBUTION_DB) is np.ndarray:
                REFLECTION_CONTRIBUTION_DB = builder.CreateNumpyVector(self.REFLECTION_CONTRIBUTION_DB)
            else:
                TRSResultStartREFLECTION_CONTRIBUTION_DBVector(builder, len(self.REFLECTION_CONTRIBUTION_DB))
                for i in reversed(range(len(self.REFLECTION_CONTRIBUTION_DB))):
                    builder.PrependFloat32(self.REFLECTION_CONTRIBUTION_DB[i])
                REFLECTION_CONTRIBUTION_DB = builder.EndVector()
        if self.TRACE_ID is not None:
            TRACE_ID = builder.CreateString(self.TRACE_ID)
        TRSResultStart(builder)
        TRSResultAddSTATUS(builder, self.STATUS)
        if self.ERROR_MESSAGE is not None:
            TRSResultAddERROR_MESSAGE(builder, ERROR_MESSAGE)
        TRSResultAddGRID_WIDTH(builder, self.GRID_WIDTH)
        TRSResultAddGRID_HEIGHT(builder, self.GRID_HEIGHT)
        if self.VISIBLE_MASK is not None:
            TRSResultAddVISIBLE_MASK(builder, VISIBLE_MASK)
        if self.OCCLUDED_MASK is not None:
            TRSResultAddOCCLUDED_MASK(builder, OCCLUDED_MASK)
        if self.POSITIVE_LINK_MASK is not None:
            TRSResultAddPOSITIVE_LINK_MASK(builder, POSITIVE_LINK_MASK)
        if self.PATH_CLASS is not None:
            TRSResultAddPATH_CLASS(builder, PATH_CLASS)
        if self.PATH_LOSS_DB is not None:
            TRSResultAddPATH_LOSS_DB(builder, PATH_LOSS_DB)
        if self.EXCESS_LOSS_DB is not None:
            TRSResultAddEXCESS_LOSS_DB(builder, EXCESS_LOSS_DB)
        if self.LINK_MARGIN_DB is not None:
            TRSResultAddLINK_MARGIN_DB(builder, LINK_MARGIN_DB)
        if self.DIFFRACTION_PARAMETER is not None:
            TRSResultAddDIFFRACTION_PARAMETER(builder, DIFFRACTION_PARAMETER)
        if self.REFLECTION_CONTRIBUTION_DB is not None:
            TRSResultAddREFLECTION_CONTRIBUTION_DB(builder, REFLECTION_CONTRIBUTION_DB)
        if self.TRACE_ID is not None:
            TRSResultAddTRACE_ID(builder, TRACE_ID)
        TRSResult = TRSResultEnd(builder)
        return TRSResult
