# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SCVCoverageGrid(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCVCoverageGrid()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCVCoverageGrid(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCVCoverageGridBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x56", size_prefixed=size_prefixed)

    # SCVCoverageGrid
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCVCoverageGrid
    def GRID_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SCVCoverageGrid
    def DOMAIN(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # SCVCoverageGrid
    def FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # SCVCoverageGrid
    def MIN_LAT_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCoverageGrid
    def MAX_LAT_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCoverageGrid
    def MIN_LON_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCoverageGrid
    def MAX_LON_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCoverageGrid
    def LAT_STEP_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCoverageGrid
    def LON_STEP_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCoverageGrid
    def ALTITUDE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCoverageGrid
    def CELL_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # SCVCoverageGrid
    def RESOLUTION_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SCVCoverageGridStart(builder):
    builder.StartObject(12)

def Start(builder):
    SCVCoverageGridStart(builder)

def SCVCoverageGridAddGRID_ID(builder, GRID_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(GRID_ID), 0)

def AddGRID_ID(builder, GRID_ID):
    SCVCoverageGridAddGRID_ID(builder, GRID_ID)

def SCVCoverageGridAddDOMAIN(builder, DOMAIN):
    builder.PrependUint8Slot(1, DOMAIN, 0)

def AddDOMAIN(builder, DOMAIN):
    SCVCoverageGridAddDOMAIN(builder, DOMAIN)

def SCVCoverageGridAddFRAME(builder, FRAME):
    builder.PrependUint8Slot(2, FRAME, 0)

def AddFRAME(builder, FRAME):
    SCVCoverageGridAddFRAME(builder, FRAME)

def SCVCoverageGridAddMIN_LAT_DEG(builder, MIN_LAT_DEG):
    builder.PrependFloat64Slot(3, MIN_LAT_DEG, 0.0)

def AddMIN_LAT_DEG(builder, MIN_LAT_DEG):
    SCVCoverageGridAddMIN_LAT_DEG(builder, MIN_LAT_DEG)

def SCVCoverageGridAddMAX_LAT_DEG(builder, MAX_LAT_DEG):
    builder.PrependFloat64Slot(4, MAX_LAT_DEG, 0.0)

def AddMAX_LAT_DEG(builder, MAX_LAT_DEG):
    SCVCoverageGridAddMAX_LAT_DEG(builder, MAX_LAT_DEG)

def SCVCoverageGridAddMIN_LON_DEG(builder, MIN_LON_DEG):
    builder.PrependFloat64Slot(5, MIN_LON_DEG, 0.0)

def AddMIN_LON_DEG(builder, MIN_LON_DEG):
    SCVCoverageGridAddMIN_LON_DEG(builder, MIN_LON_DEG)

def SCVCoverageGridAddMAX_LON_DEG(builder, MAX_LON_DEG):
    builder.PrependFloat64Slot(6, MAX_LON_DEG, 0.0)

def AddMAX_LON_DEG(builder, MAX_LON_DEG):
    SCVCoverageGridAddMAX_LON_DEG(builder, MAX_LON_DEG)

def SCVCoverageGridAddLAT_STEP_DEG(builder, LAT_STEP_DEG):
    builder.PrependFloat64Slot(7, LAT_STEP_DEG, 0.0)

def AddLAT_STEP_DEG(builder, LAT_STEP_DEG):
    SCVCoverageGridAddLAT_STEP_DEG(builder, LAT_STEP_DEG)

def SCVCoverageGridAddLON_STEP_DEG(builder, LON_STEP_DEG):
    builder.PrependFloat64Slot(8, LON_STEP_DEG, 0.0)

def AddLON_STEP_DEG(builder, LON_STEP_DEG):
    SCVCoverageGridAddLON_STEP_DEG(builder, LON_STEP_DEG)

def SCVCoverageGridAddALTITUDE_M(builder, ALTITUDE_M):
    builder.PrependFloat64Slot(9, ALTITUDE_M, 0.0)

def AddALTITUDE_M(builder, ALTITUDE_M):
    SCVCoverageGridAddALTITUDE_M(builder, ALTITUDE_M)

def SCVCoverageGridAddCELL_COUNT(builder, CELL_COUNT):
    builder.PrependUint32Slot(10, CELL_COUNT, 0)

def AddCELL_COUNT(builder, CELL_COUNT):
    SCVCoverageGridAddCELL_COUNT(builder, CELL_COUNT)

def SCVCoverageGridAddRESOLUTION_DEG(builder, RESOLUTION_DEG):
    builder.PrependFloat64Slot(11, RESOLUTION_DEG, 0.0)

def AddRESOLUTION_DEG(builder, RESOLUTION_DEG):
    SCVCoverageGridAddRESOLUTION_DEG(builder, RESOLUTION_DEG)

def SCVCoverageGridEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCVCoverageGridEnd(builder)


class SCVCoverageGridT(object):

    # SCVCoverageGridT
    def __init__(
        self,
        GRID_ID = None,
        DOMAIN = 0,
        FRAME = 0,
        MIN_LAT_DEG = 0.0,
        MAX_LAT_DEG = 0.0,
        MIN_LON_DEG = 0.0,
        MAX_LON_DEG = 0.0,
        LAT_STEP_DEG = 0.0,
        LON_STEP_DEG = 0.0,
        ALTITUDE_M = 0.0,
        CELL_COUNT = 0,
        RESOLUTION_DEG = 0.0,
    ):
        self.GRID_ID = GRID_ID  # type: Optional[str]
        self.DOMAIN = DOMAIN  # type: int
        self.FRAME = FRAME  # type: int
        self.MIN_LAT_DEG = MIN_LAT_DEG  # type: float
        self.MAX_LAT_DEG = MAX_LAT_DEG  # type: float
        self.MIN_LON_DEG = MIN_LON_DEG  # type: float
        self.MAX_LON_DEG = MAX_LON_DEG  # type: float
        self.LAT_STEP_DEG = LAT_STEP_DEG  # type: float
        self.LON_STEP_DEG = LON_STEP_DEG  # type: float
        self.ALTITUDE_M = ALTITUDE_M  # type: float
        self.CELL_COUNT = CELL_COUNT  # type: int
        self.RESOLUTION_DEG = RESOLUTION_DEG  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScvcoverageGrid = SCVCoverageGrid()
        tmpScvcoverageGrid.Init(buf, pos)
        return cls.InitFromObj(tmpScvcoverageGrid)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScvcoverageGrid):
        x = SCVCoverageGridT()
        x._UnPack(tmpScvcoverageGrid)
        return x

    # SCVCoverageGridT
    def _UnPack(self, SCVCoverageGrid):
        if SCVCoverageGrid is None:
            return
        self.GRID_ID = SCVCoverageGrid.GRID_ID()
        self.DOMAIN = SCVCoverageGrid.DOMAIN()
        self.FRAME = SCVCoverageGrid.FRAME()
        self.MIN_LAT_DEG = SCVCoverageGrid.MIN_LAT_DEG()
        self.MAX_LAT_DEG = SCVCoverageGrid.MAX_LAT_DEG()
        self.MIN_LON_DEG = SCVCoverageGrid.MIN_LON_DEG()
        self.MAX_LON_DEG = SCVCoverageGrid.MAX_LON_DEG()
        self.LAT_STEP_DEG = SCVCoverageGrid.LAT_STEP_DEG()
        self.LON_STEP_DEG = SCVCoverageGrid.LON_STEP_DEG()
        self.ALTITUDE_M = SCVCoverageGrid.ALTITUDE_M()
        self.CELL_COUNT = SCVCoverageGrid.CELL_COUNT()
        self.RESOLUTION_DEG = SCVCoverageGrid.RESOLUTION_DEG()

    # SCVCoverageGridT
    def Pack(self, builder):
        if self.GRID_ID is not None:
            GRID_ID = builder.CreateString(self.GRID_ID)
        SCVCoverageGridStart(builder)
        if self.GRID_ID is not None:
            SCVCoverageGridAddGRID_ID(builder, GRID_ID)
        SCVCoverageGridAddDOMAIN(builder, self.DOMAIN)
        SCVCoverageGridAddFRAME(builder, self.FRAME)
        SCVCoverageGridAddMIN_LAT_DEG(builder, self.MIN_LAT_DEG)
        SCVCoverageGridAddMAX_LAT_DEG(builder, self.MAX_LAT_DEG)
        SCVCoverageGridAddMIN_LON_DEG(builder, self.MIN_LON_DEG)
        SCVCoverageGridAddMAX_LON_DEG(builder, self.MAX_LON_DEG)
        SCVCoverageGridAddLAT_STEP_DEG(builder, self.LAT_STEP_DEG)
        SCVCoverageGridAddLON_STEP_DEG(builder, self.LON_STEP_DEG)
        SCVCoverageGridAddALTITUDE_M(builder, self.ALTITUDE_M)
        SCVCoverageGridAddCELL_COUNT(builder, self.CELL_COUNT)
        SCVCoverageGridAddRESOLUTION_DEG(builder, self.RESOLUTION_DEG)
        SCVCoverageGrid = SCVCoverageGridEnd(builder)
        return SCVCoverageGrid
