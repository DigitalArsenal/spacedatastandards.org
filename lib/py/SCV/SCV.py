# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SCV(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCV()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCV(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCVBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x56", size_prefixed=size_prefixed)

    # SCV
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCV
    def ENVELOPE_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # SCV
    def REQUEST(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCVCoverageRequest import SCVCoverageRequest
            obj = SCVCoverageRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCV
    def PROGRESS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCVProgress import SCVProgress
            obj = SCVProgress()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCV
    def CANCEL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCVCancel import SCVCancel
            obj = SCVCancel()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCV
    def RESULT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCVResult import SCVResult
            obj = SCVResult()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCV
    def GEOMETRY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCVPackedGeometryChunk import SCVPackedGeometryChunk
            obj = SCVPackedGeometryChunk()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCV
    def RASTER_PRODUCTS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from SCVPackedRasterProducts import SCVPackedRasterProducts
            obj = SCVPackedRasterProducts()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def SCVStart(builder):
    builder.StartObject(7)

def Start(builder):
    SCVStart(builder)

def SCVAddENVELOPE_KIND(builder, ENVELOPE_KIND):
    builder.PrependUint8Slot(0, ENVELOPE_KIND, 0)

def AddENVELOPE_KIND(builder, ENVELOPE_KIND):
    SCVAddENVELOPE_KIND(builder, ENVELOPE_KIND)

def SCVAddREQUEST(builder, REQUEST):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST), 0)

def AddREQUEST(builder, REQUEST):
    SCVAddREQUEST(builder, REQUEST)

def SCVAddPROGRESS(builder, PROGRESS):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(PROGRESS), 0)

def AddPROGRESS(builder, PROGRESS):
    SCVAddPROGRESS(builder, PROGRESS)

def SCVAddCANCEL(builder, CANCEL):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CANCEL), 0)

def AddCANCEL(builder, CANCEL):
    SCVAddCANCEL(builder, CANCEL)

def SCVAddRESULT(builder, RESULT):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(RESULT), 0)

def AddRESULT(builder, RESULT):
    SCVAddRESULT(builder, RESULT)

def SCVAddGEOMETRY(builder, GEOMETRY):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(GEOMETRY), 0)

def AddGEOMETRY(builder, GEOMETRY):
    SCVAddGEOMETRY(builder, GEOMETRY)

def SCVAddRASTER_PRODUCTS(builder, RASTER_PRODUCTS):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(RASTER_PRODUCTS), 0)

def AddRASTER_PRODUCTS(builder, RASTER_PRODUCTS):
    SCVAddRASTER_PRODUCTS(builder, RASTER_PRODUCTS)

def SCVEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCVEnd(builder)

import SCVCancel
import SCVCoverageRequest
import SCVPackedGeometryChunk
import SCVPackedRasterProducts
import SCVProgress
import SCVResult
try:
    from typing import Optional
except:
    pass

class SCVT(object):

    # SCVT
    def __init__(
        self,
        ENVELOPE_KIND = 0,
        REQUEST = None,
        PROGRESS = None,
        CANCEL = None,
        RESULT = None,
        GEOMETRY = None,
        RASTER_PRODUCTS = None,
    ):
        self.ENVELOPE_KIND = ENVELOPE_KIND  # type: int
        self.REQUEST = REQUEST  # type: Optional[SCVCoverageRequest.SCVCoverageRequestT]
        self.PROGRESS = PROGRESS  # type: Optional[SCVProgress.SCVProgressT]
        self.CANCEL = CANCEL  # type: Optional[SCVCancel.SCVCancelT]
        self.RESULT = RESULT  # type: Optional[SCVResult.SCVResultT]
        self.GEOMETRY = GEOMETRY  # type: Optional[SCVPackedGeometryChunk.SCVPackedGeometryChunkT]
        self.RASTER_PRODUCTS = RASTER_PRODUCTS  # type: Optional[SCVPackedRasterProducts.SCVPackedRasterProductsT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScv = SCV()
        tmpScv.Init(buf, pos)
        return cls.InitFromObj(tmpScv)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScv):
        x = SCVT()
        x._UnPack(tmpScv)
        return x

    # SCVT
    def _UnPack(self, SCV):
        if SCV is None:
            return
        self.ENVELOPE_KIND = SCV.ENVELOPE_KIND()
        if SCV.REQUEST() is not None:
            self.REQUEST = SCVCoverageRequest.SCVCoverageRequestT.InitFromObj(SCV.REQUEST())
        if SCV.PROGRESS() is not None:
            self.PROGRESS = SCVProgress.SCVProgressT.InitFromObj(SCV.PROGRESS())
        if SCV.CANCEL() is not None:
            self.CANCEL = SCVCancel.SCVCancelT.InitFromObj(SCV.CANCEL())
        if SCV.RESULT() is not None:
            self.RESULT = SCVResult.SCVResultT.InitFromObj(SCV.RESULT())
        if SCV.GEOMETRY() is not None:
            self.GEOMETRY = SCVPackedGeometryChunk.SCVPackedGeometryChunkT.InitFromObj(SCV.GEOMETRY())
        if SCV.RASTER_PRODUCTS() is not None:
            self.RASTER_PRODUCTS = SCVPackedRasterProducts.SCVPackedRasterProductsT.InitFromObj(SCV.RASTER_PRODUCTS())

    # SCVT
    def Pack(self, builder):
        if self.REQUEST is not None:
            REQUEST = self.REQUEST.Pack(builder)
        if self.PROGRESS is not None:
            PROGRESS = self.PROGRESS.Pack(builder)
        if self.CANCEL is not None:
            CANCEL = self.CANCEL.Pack(builder)
        if self.RESULT is not None:
            RESULT = self.RESULT.Pack(builder)
        if self.GEOMETRY is not None:
            GEOMETRY = self.GEOMETRY.Pack(builder)
        if self.RASTER_PRODUCTS is not None:
            RASTER_PRODUCTS = self.RASTER_PRODUCTS.Pack(builder)
        SCVStart(builder)
        SCVAddENVELOPE_KIND(builder, self.ENVELOPE_KIND)
        if self.REQUEST is not None:
            SCVAddREQUEST(builder, REQUEST)
        if self.PROGRESS is not None:
            SCVAddPROGRESS(builder, PROGRESS)
        if self.CANCEL is not None:
            SCVAddCANCEL(builder, CANCEL)
        if self.RESULT is not None:
            SCVAddRESULT(builder, RESULT)
        if self.GEOMETRY is not None:
            SCVAddGEOMETRY(builder, GEOMETRY)
        if self.RASTER_PRODUCTS is not None:
            SCVAddRASTER_PRODUCTS(builder, RASTER_PRODUCTS)
        SCV = SCVEnd(builder)
        return SCV
