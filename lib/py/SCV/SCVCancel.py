# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SCVCancel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCVCancel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCVCancel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCVCancelBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x56", size_prefixed=size_prefixed)

    # SCVCancel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCVCancel
    def JOB_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SCVCancel
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # SCVCancel
    def REASON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SCVCancel
    def CANCEL_AFTER_WINDOW_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def SCVCancelStart(builder):
    builder.StartObject(4)

def Start(builder):
    SCVCancelStart(builder)

def SCVCancelAddJOB_ID(builder, JOB_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(JOB_ID), 0)

def AddJOB_ID(builder, JOB_ID):
    SCVCancelAddJOB_ID(builder, JOB_ID)

def SCVCancelAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUint64Slot(1, TRACE_ID, 0)

def AddTRACE_ID(builder, TRACE_ID):
    SCVCancelAddTRACE_ID(builder, TRACE_ID)

def SCVCancelAddREASON(builder, REASON):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(REASON), 0)

def AddREASON(builder, REASON):
    SCVCancelAddREASON(builder, REASON)

def SCVCancelAddCANCEL_AFTER_WINDOW_INDEX(builder, CANCEL_AFTER_WINDOW_INDEX):
    builder.PrependUint32Slot(3, CANCEL_AFTER_WINDOW_INDEX, 0)

def AddCANCEL_AFTER_WINDOW_INDEX(builder, CANCEL_AFTER_WINDOW_INDEX):
    SCVCancelAddCANCEL_AFTER_WINDOW_INDEX(builder, CANCEL_AFTER_WINDOW_INDEX)

def SCVCancelEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCVCancelEnd(builder)


class SCVCancelT(object):

    # SCVCancelT
    def __init__(
        self,
        JOB_ID = None,
        TRACE_ID = 0,
        REASON = None,
        CANCEL_AFTER_WINDOW_INDEX = 0,
    ):
        self.JOB_ID = JOB_ID  # type: Optional[str]
        self.TRACE_ID = TRACE_ID  # type: int
        self.REASON = REASON  # type: Optional[str]
        self.CANCEL_AFTER_WINDOW_INDEX = CANCEL_AFTER_WINDOW_INDEX  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScvcancel = SCVCancel()
        tmpScvcancel.Init(buf, pos)
        return cls.InitFromObj(tmpScvcancel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScvcancel):
        x = SCVCancelT()
        x._UnPack(tmpScvcancel)
        return x

    # SCVCancelT
    def _UnPack(self, SCVCancel):
        if SCVCancel is None:
            return
        self.JOB_ID = SCVCancel.JOB_ID()
        self.TRACE_ID = SCVCancel.TRACE_ID()
        self.REASON = SCVCancel.REASON()
        self.CANCEL_AFTER_WINDOW_INDEX = SCVCancel.CANCEL_AFTER_WINDOW_INDEX()

    # SCVCancelT
    def Pack(self, builder):
        if self.JOB_ID is not None:
            JOB_ID = builder.CreateString(self.JOB_ID)
        if self.REASON is not None:
            REASON = builder.CreateString(self.REASON)
        SCVCancelStart(builder)
        if self.JOB_ID is not None:
            SCVCancelAddJOB_ID(builder, JOB_ID)
        SCVCancelAddTRACE_ID(builder, self.TRACE_ID)
        if self.REASON is not None:
            SCVCancelAddREASON(builder, REASON)
        SCVCancelAddCANCEL_AFTER_WINDOW_INDEX(builder, self.CANCEL_AFTER_WINDOW_INDEX)
        SCVCancel = SCVCancelEnd(builder)
        return SCVCancel
