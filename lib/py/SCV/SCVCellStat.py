# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SCVCellStat(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCVCellStat()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCVCellStat(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCVCellStatBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x56", size_prefixed=size_prefixed)

    # SCVCellStat
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCVCellStat
    def CELL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # SCVCellStat
    def LATITUDE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def LONGITUDE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def ALTITUDE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def COVERED_WINDOW_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # SCVCellStat
    def TOTAL_WINDOW_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # SCVCellStat
    def COVERAGE_FRACTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def MEAN_REVISIT_SEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def MAX_GAP_SEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def MEAN_GAP_SEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def REDUNDANCY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVCellStat
    def SENSOR_IDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # SCVCellStat
    def SENSOR_IDSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # SCVCellStat
    def SENSOR_IDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCVCellStat
    def SENSOR_IDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # SCVCellStat
    def SENSOR_BITSET_WORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SCVCellStat
    def SENSOR_BITSET_WORDSAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint64Flags, o)
        return 0

    # SCVCellStat
    def SENSOR_BITSET_WORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCVCellStat
    def SENSOR_BITSET_WORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

def SCVCellStatStart(builder):
    builder.StartObject(13)

def Start(builder):
    SCVCellStatStart(builder)

def SCVCellStatAddCELL_ID(builder, CELL_ID):
    builder.PrependUint32Slot(0, CELL_ID, 0)

def AddCELL_ID(builder, CELL_ID):
    SCVCellStatAddCELL_ID(builder, CELL_ID)

def SCVCellStatAddLATITUDE_DEG(builder, LATITUDE_DEG):
    builder.PrependFloat64Slot(1, LATITUDE_DEG, 0.0)

def AddLATITUDE_DEG(builder, LATITUDE_DEG):
    SCVCellStatAddLATITUDE_DEG(builder, LATITUDE_DEG)

def SCVCellStatAddLONGITUDE_DEG(builder, LONGITUDE_DEG):
    builder.PrependFloat64Slot(2, LONGITUDE_DEG, 0.0)

def AddLONGITUDE_DEG(builder, LONGITUDE_DEG):
    SCVCellStatAddLONGITUDE_DEG(builder, LONGITUDE_DEG)

def SCVCellStatAddALTITUDE_M(builder, ALTITUDE_M):
    builder.PrependFloat64Slot(3, ALTITUDE_M, 0.0)

def AddALTITUDE_M(builder, ALTITUDE_M):
    SCVCellStatAddALTITUDE_M(builder, ALTITUDE_M)

def SCVCellStatAddCOVERED_WINDOW_COUNT(builder, COVERED_WINDOW_COUNT):
    builder.PrependUint32Slot(4, COVERED_WINDOW_COUNT, 0)

def AddCOVERED_WINDOW_COUNT(builder, COVERED_WINDOW_COUNT):
    SCVCellStatAddCOVERED_WINDOW_COUNT(builder, COVERED_WINDOW_COUNT)

def SCVCellStatAddTOTAL_WINDOW_COUNT(builder, TOTAL_WINDOW_COUNT):
    builder.PrependUint32Slot(5, TOTAL_WINDOW_COUNT, 0)

def AddTOTAL_WINDOW_COUNT(builder, TOTAL_WINDOW_COUNT):
    SCVCellStatAddTOTAL_WINDOW_COUNT(builder, TOTAL_WINDOW_COUNT)

def SCVCellStatAddCOVERAGE_FRACTION(builder, COVERAGE_FRACTION):
    builder.PrependFloat64Slot(6, COVERAGE_FRACTION, 0.0)

def AddCOVERAGE_FRACTION(builder, COVERAGE_FRACTION):
    SCVCellStatAddCOVERAGE_FRACTION(builder, COVERAGE_FRACTION)

def SCVCellStatAddMEAN_REVISIT_SEC(builder, MEAN_REVISIT_SEC):
    builder.PrependFloat64Slot(7, MEAN_REVISIT_SEC, 0.0)

def AddMEAN_REVISIT_SEC(builder, MEAN_REVISIT_SEC):
    SCVCellStatAddMEAN_REVISIT_SEC(builder, MEAN_REVISIT_SEC)

def SCVCellStatAddMAX_GAP_SEC(builder, MAX_GAP_SEC):
    builder.PrependFloat64Slot(8, MAX_GAP_SEC, 0.0)

def AddMAX_GAP_SEC(builder, MAX_GAP_SEC):
    SCVCellStatAddMAX_GAP_SEC(builder, MAX_GAP_SEC)

def SCVCellStatAddMEAN_GAP_SEC(builder, MEAN_GAP_SEC):
    builder.PrependFloat64Slot(9, MEAN_GAP_SEC, 0.0)

def AddMEAN_GAP_SEC(builder, MEAN_GAP_SEC):
    SCVCellStatAddMEAN_GAP_SEC(builder, MEAN_GAP_SEC)

def SCVCellStatAddREDUNDANCY(builder, REDUNDANCY):
    builder.PrependFloat64Slot(10, REDUNDANCY, 0.0)

def AddREDUNDANCY(builder, REDUNDANCY):
    SCVCellStatAddREDUNDANCY(builder, REDUNDANCY)

def SCVCellStatAddSENSOR_IDS(builder, SENSOR_IDS):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(SENSOR_IDS), 0)

def AddSENSOR_IDS(builder, SENSOR_IDS):
    SCVCellStatAddSENSOR_IDS(builder, SENSOR_IDS)

def SCVCellStatStartSENSOR_IDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSENSOR_IDSVector(builder, numElems):
    return SCVCellStatStartSENSOR_IDSVector(builder, numElems)

def SCVCellStatCreateSENSOR_IDSVector(builder, data):
    data = list(data)
    builder.StartVector(4, len(data), 4)
    for item in reversed(data):
        builder.PrependUint32(item)
    return builder.EndVector()

def CreateSENSOR_IDSVector(builder, data):
    SCVCellStatCreateSENSOR_IDSVector(builder, data)

def SCVCellStatAddSENSOR_BITSET_WORDS(builder, SENSOR_BITSET_WORDS):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(SENSOR_BITSET_WORDS), 0)

def AddSENSOR_BITSET_WORDS(builder, SENSOR_BITSET_WORDS):
    SCVCellStatAddSENSOR_BITSET_WORDS(builder, SENSOR_BITSET_WORDS)

def SCVCellStatStartSENSOR_BITSET_WORDSVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSENSOR_BITSET_WORDSVector(builder, numElems):
    return SCVCellStatStartSENSOR_BITSET_WORDSVector(builder, numElems)

def SCVCellStatCreateSENSOR_BITSET_WORDSVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependUint64(item)
    return builder.EndVector()

def CreateSENSOR_BITSET_WORDSVector(builder, data):
    SCVCellStatCreateSENSOR_BITSET_WORDSVector(builder, data)

def SCVCellStatEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCVCellStatEnd(builder)

try:
    from typing import List
except:
    pass

class SCVCellStatT(object):

    # SCVCellStatT
    def __init__(
        self,
        CELL_ID = 0,
        LATITUDE_DEG = 0.0,
        LONGITUDE_DEG = 0.0,
        ALTITUDE_M = 0.0,
        COVERED_WINDOW_COUNT = 0,
        TOTAL_WINDOW_COUNT = 0,
        COVERAGE_FRACTION = 0.0,
        MEAN_REVISIT_SEC = 0.0,
        MAX_GAP_SEC = 0.0,
        MEAN_GAP_SEC = 0.0,
        REDUNDANCY = 0.0,
        SENSOR_IDS = None,
        SENSOR_BITSET_WORDS = None,
    ):
        self.CELL_ID = CELL_ID  # type: int
        self.LATITUDE_DEG = LATITUDE_DEG  # type: float
        self.LONGITUDE_DEG = LONGITUDE_DEG  # type: float
        self.ALTITUDE_M = ALTITUDE_M  # type: float
        self.COVERED_WINDOW_COUNT = COVERED_WINDOW_COUNT  # type: int
        self.TOTAL_WINDOW_COUNT = TOTAL_WINDOW_COUNT  # type: int
        self.COVERAGE_FRACTION = COVERAGE_FRACTION  # type: float
        self.MEAN_REVISIT_SEC = MEAN_REVISIT_SEC  # type: float
        self.MAX_GAP_SEC = MAX_GAP_SEC  # type: float
        self.MEAN_GAP_SEC = MEAN_GAP_SEC  # type: float
        self.REDUNDANCY = REDUNDANCY  # type: float
        self.SENSOR_IDS = SENSOR_IDS  # type: Optional[List[int]]
        self.SENSOR_BITSET_WORDS = SENSOR_BITSET_WORDS  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScvcellStat = SCVCellStat()
        tmpScvcellStat.Init(buf, pos)
        return cls.InitFromObj(tmpScvcellStat)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScvcellStat):
        x = SCVCellStatT()
        x._UnPack(tmpScvcellStat)
        return x

    # SCVCellStatT
    def _UnPack(self, SCVCellStat):
        if SCVCellStat is None:
            return
        self.CELL_ID = SCVCellStat.CELL_ID()
        self.LATITUDE_DEG = SCVCellStat.LATITUDE_DEG()
        self.LONGITUDE_DEG = SCVCellStat.LONGITUDE_DEG()
        self.ALTITUDE_M = SCVCellStat.ALTITUDE_M()
        self.COVERED_WINDOW_COUNT = SCVCellStat.COVERED_WINDOW_COUNT()
        self.TOTAL_WINDOW_COUNT = SCVCellStat.TOTAL_WINDOW_COUNT()
        self.COVERAGE_FRACTION = SCVCellStat.COVERAGE_FRACTION()
        self.MEAN_REVISIT_SEC = SCVCellStat.MEAN_REVISIT_SEC()
        self.MAX_GAP_SEC = SCVCellStat.MAX_GAP_SEC()
        self.MEAN_GAP_SEC = SCVCellStat.MEAN_GAP_SEC()
        self.REDUNDANCY = SCVCellStat.REDUNDANCY()
        if not SCVCellStat.SENSOR_IDSIsNone():
            if np is None:
                self.SENSOR_IDS = []
                for i in range(SCVCellStat.SENSOR_IDSLength()):
                    self.SENSOR_IDS.append(SCVCellStat.SENSOR_IDS(i))
            else:
                self.SENSOR_IDS = SCVCellStat.SENSOR_IDSAsNumpy()
        if not SCVCellStat.SENSOR_BITSET_WORDSIsNone():
            if np is None:
                self.SENSOR_BITSET_WORDS = []
                for i in range(SCVCellStat.SENSOR_BITSET_WORDSLength()):
                    self.SENSOR_BITSET_WORDS.append(SCVCellStat.SENSOR_BITSET_WORDS(i))
            else:
                self.SENSOR_BITSET_WORDS = SCVCellStat.SENSOR_BITSET_WORDSAsNumpy()

    # SCVCellStatT
    def Pack(self, builder):
        if self.SENSOR_IDS is not None:
            if np is not None and type(self.SENSOR_IDS) is np.ndarray:
                SENSOR_IDS = builder.CreateNumpyVector(self.SENSOR_IDS)
            else:
                SCVCellStatStartSENSOR_IDSVector(builder, len(self.SENSOR_IDS))
                for i in reversed(range(len(self.SENSOR_IDS))):
                    builder.PrependUint32(self.SENSOR_IDS[i])
                SENSOR_IDS = builder.EndVector()
        if self.SENSOR_BITSET_WORDS is not None:
            if np is not None and type(self.SENSOR_BITSET_WORDS) is np.ndarray:
                SENSOR_BITSET_WORDS = builder.CreateNumpyVector(self.SENSOR_BITSET_WORDS)
            else:
                SCVCellStatStartSENSOR_BITSET_WORDSVector(builder, len(self.SENSOR_BITSET_WORDS))
                for i in reversed(range(len(self.SENSOR_BITSET_WORDS))):
                    builder.PrependUint64(self.SENSOR_BITSET_WORDS[i])
                SENSOR_BITSET_WORDS = builder.EndVector()
        SCVCellStatStart(builder)
        SCVCellStatAddCELL_ID(builder, self.CELL_ID)
        SCVCellStatAddLATITUDE_DEG(builder, self.LATITUDE_DEG)
        SCVCellStatAddLONGITUDE_DEG(builder, self.LONGITUDE_DEG)
        SCVCellStatAddALTITUDE_M(builder, self.ALTITUDE_M)
        SCVCellStatAddCOVERED_WINDOW_COUNT(builder, self.COVERED_WINDOW_COUNT)
        SCVCellStatAddTOTAL_WINDOW_COUNT(builder, self.TOTAL_WINDOW_COUNT)
        SCVCellStatAddCOVERAGE_FRACTION(builder, self.COVERAGE_FRACTION)
        SCVCellStatAddMEAN_REVISIT_SEC(builder, self.MEAN_REVISIT_SEC)
        SCVCellStatAddMAX_GAP_SEC(builder, self.MAX_GAP_SEC)
        SCVCellStatAddMEAN_GAP_SEC(builder, self.MEAN_GAP_SEC)
        SCVCellStatAddREDUNDANCY(builder, self.REDUNDANCY)
        if self.SENSOR_IDS is not None:
            SCVCellStatAddSENSOR_IDS(builder, SENSOR_IDS)
        if self.SENSOR_BITSET_WORDS is not None:
            SCVCellStatAddSENSOR_BITSET_WORDS(builder, SENSOR_BITSET_WORDS)
        SCVCellStat = SCVCellStatEnd(builder)
        return SCVCellStat
