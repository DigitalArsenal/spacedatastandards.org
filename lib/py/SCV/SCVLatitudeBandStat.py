# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SCVLatitudeBandStat(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCVLatitudeBandStat()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCVLatitudeBandStat(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCVLatitudeBandStatBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x56", size_prefixed=size_prefixed)

    # SCVLatitudeBandStat
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCVLatitudeBandStat
    def MIN_LAT_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVLatitudeBandStat
    def MAX_LAT_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVLatitudeBandStat
    def COVERAGE_FRACTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVLatitudeBandStat
    def MEAN_REVISIT_SEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVLatitudeBandStat
    def MAX_GAP_SEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVLatitudeBandStat
    def MEAN_REDUNDANCY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def SCVLatitudeBandStatStart(builder):
    builder.StartObject(6)

def Start(builder):
    SCVLatitudeBandStatStart(builder)

def SCVLatitudeBandStatAddMIN_LAT_DEG(builder, MIN_LAT_DEG):
    builder.PrependFloat64Slot(0, MIN_LAT_DEG, 0.0)

def AddMIN_LAT_DEG(builder, MIN_LAT_DEG):
    SCVLatitudeBandStatAddMIN_LAT_DEG(builder, MIN_LAT_DEG)

def SCVLatitudeBandStatAddMAX_LAT_DEG(builder, MAX_LAT_DEG):
    builder.PrependFloat64Slot(1, MAX_LAT_DEG, 0.0)

def AddMAX_LAT_DEG(builder, MAX_LAT_DEG):
    SCVLatitudeBandStatAddMAX_LAT_DEG(builder, MAX_LAT_DEG)

def SCVLatitudeBandStatAddCOVERAGE_FRACTION(builder, COVERAGE_FRACTION):
    builder.PrependFloat64Slot(2, COVERAGE_FRACTION, 0.0)

def AddCOVERAGE_FRACTION(builder, COVERAGE_FRACTION):
    SCVLatitudeBandStatAddCOVERAGE_FRACTION(builder, COVERAGE_FRACTION)

def SCVLatitudeBandStatAddMEAN_REVISIT_SEC(builder, MEAN_REVISIT_SEC):
    builder.PrependFloat64Slot(3, MEAN_REVISIT_SEC, 0.0)

def AddMEAN_REVISIT_SEC(builder, MEAN_REVISIT_SEC):
    SCVLatitudeBandStatAddMEAN_REVISIT_SEC(builder, MEAN_REVISIT_SEC)

def SCVLatitudeBandStatAddMAX_GAP_SEC(builder, MAX_GAP_SEC):
    builder.PrependFloat64Slot(4, MAX_GAP_SEC, 0.0)

def AddMAX_GAP_SEC(builder, MAX_GAP_SEC):
    SCVLatitudeBandStatAddMAX_GAP_SEC(builder, MAX_GAP_SEC)

def SCVLatitudeBandStatAddMEAN_REDUNDANCY(builder, MEAN_REDUNDANCY):
    builder.PrependFloat64Slot(5, MEAN_REDUNDANCY, 0.0)

def AddMEAN_REDUNDANCY(builder, MEAN_REDUNDANCY):
    SCVLatitudeBandStatAddMEAN_REDUNDANCY(builder, MEAN_REDUNDANCY)

def SCVLatitudeBandStatEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCVLatitudeBandStatEnd(builder)


class SCVLatitudeBandStatT(object):

    # SCVLatitudeBandStatT
    def __init__(
        self,
        MIN_LAT_DEG = 0.0,
        MAX_LAT_DEG = 0.0,
        COVERAGE_FRACTION = 0.0,
        MEAN_REVISIT_SEC = 0.0,
        MAX_GAP_SEC = 0.0,
        MEAN_REDUNDANCY = 0.0,
    ):
        self.MIN_LAT_DEG = MIN_LAT_DEG  # type: float
        self.MAX_LAT_DEG = MAX_LAT_DEG  # type: float
        self.COVERAGE_FRACTION = COVERAGE_FRACTION  # type: float
        self.MEAN_REVISIT_SEC = MEAN_REVISIT_SEC  # type: float
        self.MAX_GAP_SEC = MAX_GAP_SEC  # type: float
        self.MEAN_REDUNDANCY = MEAN_REDUNDANCY  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScvlatitudeBandStat = SCVLatitudeBandStat()
        tmpScvlatitudeBandStat.Init(buf, pos)
        return cls.InitFromObj(tmpScvlatitudeBandStat)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScvlatitudeBandStat):
        x = SCVLatitudeBandStatT()
        x._UnPack(tmpScvlatitudeBandStat)
        return x

    # SCVLatitudeBandStatT
    def _UnPack(self, SCVLatitudeBandStat):
        if SCVLatitudeBandStat is None:
            return
        self.MIN_LAT_DEG = SCVLatitudeBandStat.MIN_LAT_DEG()
        self.MAX_LAT_DEG = SCVLatitudeBandStat.MAX_LAT_DEG()
        self.COVERAGE_FRACTION = SCVLatitudeBandStat.COVERAGE_FRACTION()
        self.MEAN_REVISIT_SEC = SCVLatitudeBandStat.MEAN_REVISIT_SEC()
        self.MAX_GAP_SEC = SCVLatitudeBandStat.MAX_GAP_SEC()
        self.MEAN_REDUNDANCY = SCVLatitudeBandStat.MEAN_REDUNDANCY()

    # SCVLatitudeBandStatT
    def Pack(self, builder):
        SCVLatitudeBandStatStart(builder)
        SCVLatitudeBandStatAddMIN_LAT_DEG(builder, self.MIN_LAT_DEG)
        SCVLatitudeBandStatAddMAX_LAT_DEG(builder, self.MAX_LAT_DEG)
        SCVLatitudeBandStatAddCOVERAGE_FRACTION(builder, self.COVERAGE_FRACTION)
        SCVLatitudeBandStatAddMEAN_REVISIT_SEC(builder, self.MEAN_REVISIT_SEC)
        SCVLatitudeBandStatAddMAX_GAP_SEC(builder, self.MAX_GAP_SEC)
        SCVLatitudeBandStatAddMEAN_REDUNDANCY(builder, self.MEAN_REDUNDANCY)
        SCVLatitudeBandStat = SCVLatitudeBandStatEnd(builder)
        return SCVLatitudeBandStat
