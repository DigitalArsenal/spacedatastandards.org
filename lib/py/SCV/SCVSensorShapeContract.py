# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SCVSensorShapeContract(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SCVSensorShapeContract()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSCVSensorShapeContract(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SCVSensorShapeContractBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x43\x56", size_prefixed=size_prefixed)

    # SCVSensorShapeContract
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SCVSensorShapeContract
    def SHAPE_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # SCVSensorShapeContract
    def AXIS_CONVENTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # SCVSensorShapeContract
    def RANGE_BOUNDARY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # SCVSensorShapeContract
    def OUTER_HALF_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def INNER_HALF_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def MIN_CLOCK_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def MAX_CLOCK_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 360.0

    # SCVSensorShapeContract
    def X_HALF_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def Y_HALF_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def INNER_LOOK_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def OUTER_LOOK_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def SAR_SAMPLING_DENSITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def MIN_RANGE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def MAX_RANGE_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # SCVSensorShapeContract
    def POLYGON_VERTICES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from SCVVec3 import SCVVec3
            obj = SCVVec3()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SCVSensorShapeContract
    def POLYGON_VERTICESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SCVSensorShapeContract
    def POLYGON_VERTICESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

    # SCVSensorShapeContract
    def POLYGON_FRAME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def SCVSensorShapeContractStart(builder):
    builder.StartObject(16)

def Start(builder):
    SCVSensorShapeContractStart(builder)

def SCVSensorShapeContractAddSHAPE_KIND(builder, SHAPE_KIND):
    builder.PrependUint8Slot(0, SHAPE_KIND, 0)

def AddSHAPE_KIND(builder, SHAPE_KIND):
    SCVSensorShapeContractAddSHAPE_KIND(builder, SHAPE_KIND)

def SCVSensorShapeContractAddAXIS_CONVENTION(builder, AXIS_CONVENTION):
    builder.PrependUint8Slot(1, AXIS_CONVENTION, 0)

def AddAXIS_CONVENTION(builder, AXIS_CONVENTION):
    SCVSensorShapeContractAddAXIS_CONVENTION(builder, AXIS_CONVENTION)

def SCVSensorShapeContractAddRANGE_BOUNDARY(builder, RANGE_BOUNDARY):
    builder.PrependUint8Slot(2, RANGE_BOUNDARY, 0)

def AddRANGE_BOUNDARY(builder, RANGE_BOUNDARY):
    SCVSensorShapeContractAddRANGE_BOUNDARY(builder, RANGE_BOUNDARY)

def SCVSensorShapeContractAddOUTER_HALF_ANGLE_DEG(builder, OUTER_HALF_ANGLE_DEG):
    builder.PrependFloat64Slot(3, OUTER_HALF_ANGLE_DEG, 0.0)

def AddOUTER_HALF_ANGLE_DEG(builder, OUTER_HALF_ANGLE_DEG):
    SCVSensorShapeContractAddOUTER_HALF_ANGLE_DEG(builder, OUTER_HALF_ANGLE_DEG)

def SCVSensorShapeContractAddINNER_HALF_ANGLE_DEG(builder, INNER_HALF_ANGLE_DEG):
    builder.PrependFloat64Slot(4, INNER_HALF_ANGLE_DEG, 0.0)

def AddINNER_HALF_ANGLE_DEG(builder, INNER_HALF_ANGLE_DEG):
    SCVSensorShapeContractAddINNER_HALF_ANGLE_DEG(builder, INNER_HALF_ANGLE_DEG)

def SCVSensorShapeContractAddMIN_CLOCK_ANGLE_DEG(builder, MIN_CLOCK_ANGLE_DEG):
    builder.PrependFloat64Slot(5, MIN_CLOCK_ANGLE_DEG, 0.0)

def AddMIN_CLOCK_ANGLE_DEG(builder, MIN_CLOCK_ANGLE_DEG):
    SCVSensorShapeContractAddMIN_CLOCK_ANGLE_DEG(builder, MIN_CLOCK_ANGLE_DEG)

def SCVSensorShapeContractAddMAX_CLOCK_ANGLE_DEG(builder, MAX_CLOCK_ANGLE_DEG):
    builder.PrependFloat64Slot(6, MAX_CLOCK_ANGLE_DEG, 360.0)

def AddMAX_CLOCK_ANGLE_DEG(builder, MAX_CLOCK_ANGLE_DEG):
    SCVSensorShapeContractAddMAX_CLOCK_ANGLE_DEG(builder, MAX_CLOCK_ANGLE_DEG)

def SCVSensorShapeContractAddX_HALF_ANGLE_DEG(builder, X_HALF_ANGLE_DEG):
    builder.PrependFloat64Slot(7, X_HALF_ANGLE_DEG, 0.0)

def AddX_HALF_ANGLE_DEG(builder, X_HALF_ANGLE_DEG):
    SCVSensorShapeContractAddX_HALF_ANGLE_DEG(builder, X_HALF_ANGLE_DEG)

def SCVSensorShapeContractAddY_HALF_ANGLE_DEG(builder, Y_HALF_ANGLE_DEG):
    builder.PrependFloat64Slot(8, Y_HALF_ANGLE_DEG, 0.0)

def AddY_HALF_ANGLE_DEG(builder, Y_HALF_ANGLE_DEG):
    SCVSensorShapeContractAddY_HALF_ANGLE_DEG(builder, Y_HALF_ANGLE_DEG)

def SCVSensorShapeContractAddINNER_LOOK_ANGLE_DEG(builder, INNER_LOOK_ANGLE_DEG):
    builder.PrependFloat64Slot(9, INNER_LOOK_ANGLE_DEG, 0.0)

def AddINNER_LOOK_ANGLE_DEG(builder, INNER_LOOK_ANGLE_DEG):
    SCVSensorShapeContractAddINNER_LOOK_ANGLE_DEG(builder, INNER_LOOK_ANGLE_DEG)

def SCVSensorShapeContractAddOUTER_LOOK_ANGLE_DEG(builder, OUTER_LOOK_ANGLE_DEG):
    builder.PrependFloat64Slot(10, OUTER_LOOK_ANGLE_DEG, 0.0)

def AddOUTER_LOOK_ANGLE_DEG(builder, OUTER_LOOK_ANGLE_DEG):
    SCVSensorShapeContractAddOUTER_LOOK_ANGLE_DEG(builder, OUTER_LOOK_ANGLE_DEG)

def SCVSensorShapeContractAddSAR_SAMPLING_DENSITY(builder, SAR_SAMPLING_DENSITY):
    builder.PrependFloat64Slot(11, SAR_SAMPLING_DENSITY, 0.0)

def AddSAR_SAMPLING_DENSITY(builder, SAR_SAMPLING_DENSITY):
    SCVSensorShapeContractAddSAR_SAMPLING_DENSITY(builder, SAR_SAMPLING_DENSITY)

def SCVSensorShapeContractAddMIN_RANGE_M(builder, MIN_RANGE_M):
    builder.PrependFloat64Slot(12, MIN_RANGE_M, 0.0)

def AddMIN_RANGE_M(builder, MIN_RANGE_M):
    SCVSensorShapeContractAddMIN_RANGE_M(builder, MIN_RANGE_M)

def SCVSensorShapeContractAddMAX_RANGE_M(builder, MAX_RANGE_M):
    builder.PrependFloat64Slot(13, MAX_RANGE_M, 0.0)

def AddMAX_RANGE_M(builder, MAX_RANGE_M):
    SCVSensorShapeContractAddMAX_RANGE_M(builder, MAX_RANGE_M)

def SCVSensorShapeContractAddPOLYGON_VERTICES(builder, POLYGON_VERTICES):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(POLYGON_VERTICES), 0)

def AddPOLYGON_VERTICES(builder, POLYGON_VERTICES):
    SCVSensorShapeContractAddPOLYGON_VERTICES(builder, POLYGON_VERTICES)

def SCVSensorShapeContractStartPOLYGON_VERTICESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPOLYGON_VERTICESVector(builder, numElems):
    return SCVSensorShapeContractStartPOLYGON_VERTICESVector(builder, numElems)

def SCVSensorShapeContractCreatePOLYGON_VERTICESVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreatePOLYGON_VERTICESVector(builder, data):
    SCVSensorShapeContractCreatePOLYGON_VERTICESVector(builder, data)

def SCVSensorShapeContractAddPOLYGON_FRAME(builder, POLYGON_FRAME):
    builder.PrependUint8Slot(15, POLYGON_FRAME, 0)

def AddPOLYGON_FRAME(builder, POLYGON_FRAME):
    SCVSensorShapeContractAddPOLYGON_FRAME(builder, POLYGON_FRAME)

def SCVSensorShapeContractEnd(builder):
    return builder.EndObject()

def End(builder):
    return SCVSensorShapeContractEnd(builder)

import SCVVec3
try:
    from typing import List
except:
    pass

class SCVSensorShapeContractT(object):

    # SCVSensorShapeContractT
    def __init__(
        self,
        SHAPE_KIND = 0,
        AXIS_CONVENTION = 0,
        RANGE_BOUNDARY = 0,
        OUTER_HALF_ANGLE_DEG = 0.0,
        INNER_HALF_ANGLE_DEG = 0.0,
        MIN_CLOCK_ANGLE_DEG = 0.0,
        MAX_CLOCK_ANGLE_DEG = 360.0,
        X_HALF_ANGLE_DEG = 0.0,
        Y_HALF_ANGLE_DEG = 0.0,
        INNER_LOOK_ANGLE_DEG = 0.0,
        OUTER_LOOK_ANGLE_DEG = 0.0,
        SAR_SAMPLING_DENSITY = 0.0,
        MIN_RANGE_M = 0.0,
        MAX_RANGE_M = 0.0,
        POLYGON_VERTICES = None,
        POLYGON_FRAME = 0,
    ):
        self.SHAPE_KIND = SHAPE_KIND  # type: int
        self.AXIS_CONVENTION = AXIS_CONVENTION  # type: int
        self.RANGE_BOUNDARY = RANGE_BOUNDARY  # type: int
        self.OUTER_HALF_ANGLE_DEG = OUTER_HALF_ANGLE_DEG  # type: float
        self.INNER_HALF_ANGLE_DEG = INNER_HALF_ANGLE_DEG  # type: float
        self.MIN_CLOCK_ANGLE_DEG = MIN_CLOCK_ANGLE_DEG  # type: float
        self.MAX_CLOCK_ANGLE_DEG = MAX_CLOCK_ANGLE_DEG  # type: float
        self.X_HALF_ANGLE_DEG = X_HALF_ANGLE_DEG  # type: float
        self.Y_HALF_ANGLE_DEG = Y_HALF_ANGLE_DEG  # type: float
        self.INNER_LOOK_ANGLE_DEG = INNER_LOOK_ANGLE_DEG  # type: float
        self.OUTER_LOOK_ANGLE_DEG = OUTER_LOOK_ANGLE_DEG  # type: float
        self.SAR_SAMPLING_DENSITY = SAR_SAMPLING_DENSITY  # type: float
        self.MIN_RANGE_M = MIN_RANGE_M  # type: float
        self.MAX_RANGE_M = MAX_RANGE_M  # type: float
        self.POLYGON_VERTICES = POLYGON_VERTICES  # type: Optional[List[SCVVec3.SCVVec3T]]
        self.POLYGON_FRAME = POLYGON_FRAME  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpScvsensorShapeContract = SCVSensorShapeContract()
        tmpScvsensorShapeContract.Init(buf, pos)
        return cls.InitFromObj(tmpScvsensorShapeContract)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpScvsensorShapeContract):
        x = SCVSensorShapeContractT()
        x._UnPack(tmpScvsensorShapeContract)
        return x

    # SCVSensorShapeContractT
    def _UnPack(self, SCVSensorShapeContract):
        if SCVSensorShapeContract is None:
            return
        self.SHAPE_KIND = SCVSensorShapeContract.SHAPE_KIND()
        self.AXIS_CONVENTION = SCVSensorShapeContract.AXIS_CONVENTION()
        self.RANGE_BOUNDARY = SCVSensorShapeContract.RANGE_BOUNDARY()
        self.OUTER_HALF_ANGLE_DEG = SCVSensorShapeContract.OUTER_HALF_ANGLE_DEG()
        self.INNER_HALF_ANGLE_DEG = SCVSensorShapeContract.INNER_HALF_ANGLE_DEG()
        self.MIN_CLOCK_ANGLE_DEG = SCVSensorShapeContract.MIN_CLOCK_ANGLE_DEG()
        self.MAX_CLOCK_ANGLE_DEG = SCVSensorShapeContract.MAX_CLOCK_ANGLE_DEG()
        self.X_HALF_ANGLE_DEG = SCVSensorShapeContract.X_HALF_ANGLE_DEG()
        self.Y_HALF_ANGLE_DEG = SCVSensorShapeContract.Y_HALF_ANGLE_DEG()
        self.INNER_LOOK_ANGLE_DEG = SCVSensorShapeContract.INNER_LOOK_ANGLE_DEG()
        self.OUTER_LOOK_ANGLE_DEG = SCVSensorShapeContract.OUTER_LOOK_ANGLE_DEG()
        self.SAR_SAMPLING_DENSITY = SCVSensorShapeContract.SAR_SAMPLING_DENSITY()
        self.MIN_RANGE_M = SCVSensorShapeContract.MIN_RANGE_M()
        self.MAX_RANGE_M = SCVSensorShapeContract.MAX_RANGE_M()
        if not SCVSensorShapeContract.POLYGON_VERTICESIsNone():
            self.POLYGON_VERTICES = []
            for i in range(SCVSensorShapeContract.POLYGON_VERTICESLength()):
                if SCVSensorShapeContract.POLYGON_VERTICES(i) is None:
                    self.POLYGON_VERTICES.append(None)
                else:
                    sCVVec3_ = SCVVec3.SCVVec3T.InitFromObj(SCVSensorShapeContract.POLYGON_VERTICES(i))
                    self.POLYGON_VERTICES.append(sCVVec3_)
        self.POLYGON_FRAME = SCVSensorShapeContract.POLYGON_FRAME()

    # SCVSensorShapeContractT
    def Pack(self, builder):
        if self.POLYGON_VERTICES is not None:
            POLYGON_VERTICESlist = []
            for i in range(len(self.POLYGON_VERTICES)):
                POLYGON_VERTICESlist.append(self.POLYGON_VERTICES[i].Pack(builder))
            SCVSensorShapeContractStartPOLYGON_VERTICESVector(builder, len(self.POLYGON_VERTICES))
            for i in reversed(range(len(self.POLYGON_VERTICES))):
                builder.PrependUOffsetTRelative(POLYGON_VERTICESlist[i])
            POLYGON_VERTICES = builder.EndVector()
        SCVSensorShapeContractStart(builder)
        SCVSensorShapeContractAddSHAPE_KIND(builder, self.SHAPE_KIND)
        SCVSensorShapeContractAddAXIS_CONVENTION(builder, self.AXIS_CONVENTION)
        SCVSensorShapeContractAddRANGE_BOUNDARY(builder, self.RANGE_BOUNDARY)
        SCVSensorShapeContractAddOUTER_HALF_ANGLE_DEG(builder, self.OUTER_HALF_ANGLE_DEG)
        SCVSensorShapeContractAddINNER_HALF_ANGLE_DEG(builder, self.INNER_HALF_ANGLE_DEG)
        SCVSensorShapeContractAddMIN_CLOCK_ANGLE_DEG(builder, self.MIN_CLOCK_ANGLE_DEG)
        SCVSensorShapeContractAddMAX_CLOCK_ANGLE_DEG(builder, self.MAX_CLOCK_ANGLE_DEG)
        SCVSensorShapeContractAddX_HALF_ANGLE_DEG(builder, self.X_HALF_ANGLE_DEG)
        SCVSensorShapeContractAddY_HALF_ANGLE_DEG(builder, self.Y_HALF_ANGLE_DEG)
        SCVSensorShapeContractAddINNER_LOOK_ANGLE_DEG(builder, self.INNER_LOOK_ANGLE_DEG)
        SCVSensorShapeContractAddOUTER_LOOK_ANGLE_DEG(builder, self.OUTER_LOOK_ANGLE_DEG)
        SCVSensorShapeContractAddSAR_SAMPLING_DENSITY(builder, self.SAR_SAMPLING_DENSITY)
        SCVSensorShapeContractAddMIN_RANGE_M(builder, self.MIN_RANGE_M)
        SCVSensorShapeContractAddMAX_RANGE_M(builder, self.MAX_RANGE_M)
        if self.POLYGON_VERTICES is not None:
            SCVSensorShapeContractAddPOLYGON_VERTICES(builder, POLYGON_VERTICES)
        SCVSensorShapeContractAddPOLYGON_FRAME(builder, self.POLYGON_FRAME)
        SCVSensorShapeContract = SCVSensorShapeContractEnd(builder)
        return SCVSensorShapeContract
