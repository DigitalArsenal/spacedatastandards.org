# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Wrapped module content key
class LWK(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LWK()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLWK(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def LWKBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4C\x57\x4B", size_prefixed=size_prefixed)

    # LWK
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Unique request identifier
    # LWK
    def REQUEST_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Canonical module identifier
    # LWK
    def MODULE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Optional module version
    # LWK
    def MODULE_VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Provider-local content key identifier
    # LWK
    def CONTENT_KEY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Recipient key identifier or fingerprint
    # LWK
    def RECIPIENT_KEY_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Wrapped-key algorithm
    # LWK
    def ALGORITHM(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Requester ephemeral X25519 public key
    # LWK
    def REQUESTER_EPHEMERAL_PUBKEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LWK
    def REQUESTER_EPHEMERAL_PUBKEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LWK
    def REQUESTER_EPHEMERAL_PUBKEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LWK
    def REQUESTER_EPHEMERAL_PUBKEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # Provider ephemeral X25519 public key
    # LWK
    def PROVIDER_EPHEMERAL_PUBKEY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LWK
    def PROVIDER_EPHEMERAL_PUBKEYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LWK
    def PROVIDER_EPHEMERAL_PUBKEYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LWK
    def PROVIDER_EPHEMERAL_PUBKEYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # HKDF salt
    # LWK
    def HKDF_SALT(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LWK
    def HKDF_SALTAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LWK
    def HKDF_SALTLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LWK
    def HKDF_SALTIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # AES-GCM IV
    # LWK
    def IV(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LWK
    def IVAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LWK
    def IVLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LWK
    def IVIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # Wrapped key ciphertext
    # LWK
    def CIPHERTEXT(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LWK
    def CIPHERTEXTAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LWK
    def CIPHERTEXTLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LWK
    def CIPHERTEXTIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # AES-GCM authentication tag
    # LWK
    def TAG(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # LWK
    def TAGAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # LWK
    def TAGLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LWK
    def TAGIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # Envelope expiration time in milliseconds since epoch
    # LWK
    def EXPIRES_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def LWKStart(builder):
    builder.StartObject(13)

def Start(builder):
    LWKStart(builder)

def LWKAddREQUEST_ID(builder, REQUEST_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(REQUEST_ID), 0)

def AddREQUEST_ID(builder, REQUEST_ID):
    LWKAddREQUEST_ID(builder, REQUEST_ID)

def LWKAddMODULE_ID(builder, MODULE_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_ID), 0)

def AddMODULE_ID(builder, MODULE_ID):
    LWKAddMODULE_ID(builder, MODULE_ID)

def LWKAddMODULE_VERSION(builder, MODULE_VERSION):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MODULE_VERSION), 0)

def AddMODULE_VERSION(builder, MODULE_VERSION):
    LWKAddMODULE_VERSION(builder, MODULE_VERSION)

def LWKAddCONTENT_KEY_ID(builder, CONTENT_KEY_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CONTENT_KEY_ID), 0)

def AddCONTENT_KEY_ID(builder, CONTENT_KEY_ID):
    LWKAddCONTENT_KEY_ID(builder, CONTENT_KEY_ID)

def LWKAddRECIPIENT_KEY_ID(builder, RECIPIENT_KEY_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(RECIPIENT_KEY_ID), 0)

def AddRECIPIENT_KEY_ID(builder, RECIPIENT_KEY_ID):
    LWKAddRECIPIENT_KEY_ID(builder, RECIPIENT_KEY_ID)

def LWKAddALGORITHM(builder, ALGORITHM):
    builder.PrependInt8Slot(5, ALGORITHM, 0)

def AddALGORITHM(builder, ALGORITHM):
    LWKAddALGORITHM(builder, ALGORITHM)

def LWKAddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(REQUESTER_EPHEMERAL_PUBKEY), 0)

def AddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY):
    LWKAddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY)

def LWKStartREQUESTER_EPHEMERAL_PUBKEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartREQUESTER_EPHEMERAL_PUBKEYVector(builder, numElems):
    return LWKStartREQUESTER_EPHEMERAL_PUBKEYVector(builder, numElems)

def LWKCreateREQUESTER_EPHEMERAL_PUBKEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateREQUESTER_EPHEMERAL_PUBKEYVector(builder, data):
    LWKCreateREQUESTER_EPHEMERAL_PUBKEYVector(builder, data)

def LWKAddPROVIDER_EPHEMERAL_PUBKEY(builder, PROVIDER_EPHEMERAL_PUBKEY):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(PROVIDER_EPHEMERAL_PUBKEY), 0)

def AddPROVIDER_EPHEMERAL_PUBKEY(builder, PROVIDER_EPHEMERAL_PUBKEY):
    LWKAddPROVIDER_EPHEMERAL_PUBKEY(builder, PROVIDER_EPHEMERAL_PUBKEY)

def LWKStartPROVIDER_EPHEMERAL_PUBKEYVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPROVIDER_EPHEMERAL_PUBKEYVector(builder, numElems):
    return LWKStartPROVIDER_EPHEMERAL_PUBKEYVector(builder, numElems)

def LWKCreatePROVIDER_EPHEMERAL_PUBKEYVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePROVIDER_EPHEMERAL_PUBKEYVector(builder, data):
    LWKCreatePROVIDER_EPHEMERAL_PUBKEYVector(builder, data)

def LWKAddHKDF_SALT(builder, HKDF_SALT):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(HKDF_SALT), 0)

def AddHKDF_SALT(builder, HKDF_SALT):
    LWKAddHKDF_SALT(builder, HKDF_SALT)

def LWKStartHKDF_SALTVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartHKDF_SALTVector(builder, numElems):
    return LWKStartHKDF_SALTVector(builder, numElems)

def LWKCreateHKDF_SALTVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateHKDF_SALTVector(builder, data):
    LWKCreateHKDF_SALTVector(builder, data)

def LWKAddIV(builder, IV):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(IV), 0)

def AddIV(builder, IV):
    LWKAddIV(builder, IV)

def LWKStartIVVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartIVVector(builder, numElems):
    return LWKStartIVVector(builder, numElems)

def LWKCreateIVVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateIVVector(builder, data):
    LWKCreateIVVector(builder, data)

def LWKAddCIPHERTEXT(builder, CIPHERTEXT):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(CIPHERTEXT), 0)

def AddCIPHERTEXT(builder, CIPHERTEXT):
    LWKAddCIPHERTEXT(builder, CIPHERTEXT)

def LWKStartCIPHERTEXTVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartCIPHERTEXTVector(builder, numElems):
    return LWKStartCIPHERTEXTVector(builder, numElems)

def LWKCreateCIPHERTEXTVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateCIPHERTEXTVector(builder, data):
    LWKCreateCIPHERTEXTVector(builder, data)

def LWKAddTAG(builder, TAG):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(TAG), 0)

def AddTAG(builder, TAG):
    LWKAddTAG(builder, TAG)

def LWKStartTAGVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartTAGVector(builder, numElems):
    return LWKStartTAGVector(builder, numElems)

def LWKCreateTAGVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreateTAGVector(builder, data):
    LWKCreateTAGVector(builder, data)

def LWKAddEXPIRES_AT(builder, EXPIRES_AT):
    builder.PrependUint64Slot(12, EXPIRES_AT, 0)

def AddEXPIRES_AT(builder, EXPIRES_AT):
    LWKAddEXPIRES_AT(builder, EXPIRES_AT)

def LWKEnd(builder):
    return builder.EndObject()

def End(builder):
    return LWKEnd(builder)

try:
    from typing import List
except:
    pass

class LWKT(object):

    # LWKT
    def __init__(
        self,
        REQUEST_ID = None,
        MODULE_ID = None,
        MODULE_VERSION = None,
        CONTENT_KEY_ID = None,
        RECIPIENT_KEY_ID = None,
        ALGORITHM = 0,
        REQUESTER_EPHEMERAL_PUBKEY = None,
        PROVIDER_EPHEMERAL_PUBKEY = None,
        HKDF_SALT = None,
        IV = None,
        CIPHERTEXT = None,
        TAG = None,
        EXPIRES_AT = 0,
    ):
        self.REQUEST_ID = REQUEST_ID  # type: Optional[str]
        self.MODULE_ID = MODULE_ID  # type: Optional[str]
        self.MODULE_VERSION = MODULE_VERSION  # type: Optional[str]
        self.CONTENT_KEY_ID = CONTENT_KEY_ID  # type: Optional[str]
        self.RECIPIENT_KEY_ID = RECIPIENT_KEY_ID  # type: Optional[str]
        self.ALGORITHM = ALGORITHM  # type: int
        self.REQUESTER_EPHEMERAL_PUBKEY = REQUESTER_EPHEMERAL_PUBKEY  # type: Optional[List[int]]
        self.PROVIDER_EPHEMERAL_PUBKEY = PROVIDER_EPHEMERAL_PUBKEY  # type: Optional[List[int]]
        self.HKDF_SALT = HKDF_SALT  # type: Optional[List[int]]
        self.IV = IV  # type: Optional[List[int]]
        self.CIPHERTEXT = CIPHERTEXT  # type: Optional[List[int]]
        self.TAG = TAG  # type: Optional[List[int]]
        self.EXPIRES_AT = EXPIRES_AT  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpLwk = LWK()
        tmpLwk.Init(buf, pos)
        return cls.InitFromObj(tmpLwk)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpLwk):
        x = LWKT()
        x._UnPack(tmpLwk)
        return x

    # LWKT
    def _UnPack(self, LWK):
        if LWK is None:
            return
        self.REQUEST_ID = LWK.REQUEST_ID()
        self.MODULE_ID = LWK.MODULE_ID()
        self.MODULE_VERSION = LWK.MODULE_VERSION()
        self.CONTENT_KEY_ID = LWK.CONTENT_KEY_ID()
        self.RECIPIENT_KEY_ID = LWK.RECIPIENT_KEY_ID()
        self.ALGORITHM = LWK.ALGORITHM()
        if not LWK.REQUESTER_EPHEMERAL_PUBKEYIsNone():
            if np is None:
                self.REQUESTER_EPHEMERAL_PUBKEY = []
                for i in range(LWK.REQUESTER_EPHEMERAL_PUBKEYLength()):
                    self.REQUESTER_EPHEMERAL_PUBKEY.append(LWK.REQUESTER_EPHEMERAL_PUBKEY(i))
            else:
                self.REQUESTER_EPHEMERAL_PUBKEY = LWK.REQUESTER_EPHEMERAL_PUBKEYAsNumpy()
        if not LWK.PROVIDER_EPHEMERAL_PUBKEYIsNone():
            if np is None:
                self.PROVIDER_EPHEMERAL_PUBKEY = []
                for i in range(LWK.PROVIDER_EPHEMERAL_PUBKEYLength()):
                    self.PROVIDER_EPHEMERAL_PUBKEY.append(LWK.PROVIDER_EPHEMERAL_PUBKEY(i))
            else:
                self.PROVIDER_EPHEMERAL_PUBKEY = LWK.PROVIDER_EPHEMERAL_PUBKEYAsNumpy()
        if not LWK.HKDF_SALTIsNone():
            if np is None:
                self.HKDF_SALT = []
                for i in range(LWK.HKDF_SALTLength()):
                    self.HKDF_SALT.append(LWK.HKDF_SALT(i))
            else:
                self.HKDF_SALT = LWK.HKDF_SALTAsNumpy()
        if not LWK.IVIsNone():
            if np is None:
                self.IV = []
                for i in range(LWK.IVLength()):
                    self.IV.append(LWK.IV(i))
            else:
                self.IV = LWK.IVAsNumpy()
        if not LWK.CIPHERTEXTIsNone():
            if np is None:
                self.CIPHERTEXT = []
                for i in range(LWK.CIPHERTEXTLength()):
                    self.CIPHERTEXT.append(LWK.CIPHERTEXT(i))
            else:
                self.CIPHERTEXT = LWK.CIPHERTEXTAsNumpy()
        if not LWK.TAGIsNone():
            if np is None:
                self.TAG = []
                for i in range(LWK.TAGLength()):
                    self.TAG.append(LWK.TAG(i))
            else:
                self.TAG = LWK.TAGAsNumpy()
        self.EXPIRES_AT = LWK.EXPIRES_AT()

    # LWKT
    def Pack(self, builder):
        if self.REQUEST_ID is not None:
            REQUEST_ID = builder.CreateString(self.REQUEST_ID)
        if self.MODULE_ID is not None:
            MODULE_ID = builder.CreateString(self.MODULE_ID)
        if self.MODULE_VERSION is not None:
            MODULE_VERSION = builder.CreateString(self.MODULE_VERSION)
        if self.CONTENT_KEY_ID is not None:
            CONTENT_KEY_ID = builder.CreateString(self.CONTENT_KEY_ID)
        if self.RECIPIENT_KEY_ID is not None:
            RECIPIENT_KEY_ID = builder.CreateString(self.RECIPIENT_KEY_ID)
        if self.REQUESTER_EPHEMERAL_PUBKEY is not None:
            if np is not None and type(self.REQUESTER_EPHEMERAL_PUBKEY) is np.ndarray:
                REQUESTER_EPHEMERAL_PUBKEY = builder.CreateNumpyVector(self.REQUESTER_EPHEMERAL_PUBKEY)
            else:
                LWKStartREQUESTER_EPHEMERAL_PUBKEYVector(builder, len(self.REQUESTER_EPHEMERAL_PUBKEY))
                for i in reversed(range(len(self.REQUESTER_EPHEMERAL_PUBKEY))):
                    builder.PrependUint8(self.REQUESTER_EPHEMERAL_PUBKEY[i])
                REQUESTER_EPHEMERAL_PUBKEY = builder.EndVector()
        if self.PROVIDER_EPHEMERAL_PUBKEY is not None:
            if np is not None and type(self.PROVIDER_EPHEMERAL_PUBKEY) is np.ndarray:
                PROVIDER_EPHEMERAL_PUBKEY = builder.CreateNumpyVector(self.PROVIDER_EPHEMERAL_PUBKEY)
            else:
                LWKStartPROVIDER_EPHEMERAL_PUBKEYVector(builder, len(self.PROVIDER_EPHEMERAL_PUBKEY))
                for i in reversed(range(len(self.PROVIDER_EPHEMERAL_PUBKEY))):
                    builder.PrependUint8(self.PROVIDER_EPHEMERAL_PUBKEY[i])
                PROVIDER_EPHEMERAL_PUBKEY = builder.EndVector()
        if self.HKDF_SALT is not None:
            if np is not None and type(self.HKDF_SALT) is np.ndarray:
                HKDF_SALT = builder.CreateNumpyVector(self.HKDF_SALT)
            else:
                LWKStartHKDF_SALTVector(builder, len(self.HKDF_SALT))
                for i in reversed(range(len(self.HKDF_SALT))):
                    builder.PrependUint8(self.HKDF_SALT[i])
                HKDF_SALT = builder.EndVector()
        if self.IV is not None:
            if np is not None and type(self.IV) is np.ndarray:
                IV = builder.CreateNumpyVector(self.IV)
            else:
                LWKStartIVVector(builder, len(self.IV))
                for i in reversed(range(len(self.IV))):
                    builder.PrependUint8(self.IV[i])
                IV = builder.EndVector()
        if self.CIPHERTEXT is not None:
            if np is not None and type(self.CIPHERTEXT) is np.ndarray:
                CIPHERTEXT = builder.CreateNumpyVector(self.CIPHERTEXT)
            else:
                LWKStartCIPHERTEXTVector(builder, len(self.CIPHERTEXT))
                for i in reversed(range(len(self.CIPHERTEXT))):
                    builder.PrependUint8(self.CIPHERTEXT[i])
                CIPHERTEXT = builder.EndVector()
        if self.TAG is not None:
            if np is not None and type(self.TAG) is np.ndarray:
                TAG = builder.CreateNumpyVector(self.TAG)
            else:
                LWKStartTAGVector(builder, len(self.TAG))
                for i in reversed(range(len(self.TAG))):
                    builder.PrependUint8(self.TAG[i])
                TAG = builder.EndVector()
        LWKStart(builder)
        if self.REQUEST_ID is not None:
            LWKAddREQUEST_ID(builder, REQUEST_ID)
        if self.MODULE_ID is not None:
            LWKAddMODULE_ID(builder, MODULE_ID)
        if self.MODULE_VERSION is not None:
            LWKAddMODULE_VERSION(builder, MODULE_VERSION)
        if self.CONTENT_KEY_ID is not None:
            LWKAddCONTENT_KEY_ID(builder, CONTENT_KEY_ID)
        if self.RECIPIENT_KEY_ID is not None:
            LWKAddRECIPIENT_KEY_ID(builder, RECIPIENT_KEY_ID)
        LWKAddALGORITHM(builder, self.ALGORITHM)
        if self.REQUESTER_EPHEMERAL_PUBKEY is not None:
            LWKAddREQUESTER_EPHEMERAL_PUBKEY(builder, REQUESTER_EPHEMERAL_PUBKEY)
        if self.PROVIDER_EPHEMERAL_PUBKEY is not None:
            LWKAddPROVIDER_EPHEMERAL_PUBKEY(builder, PROVIDER_EPHEMERAL_PUBKEY)
        if self.HKDF_SALT is not None:
            LWKAddHKDF_SALT(builder, HKDF_SALT)
        if self.IV is not None:
            LWKAddIV(builder, IV)
        if self.CIPHERTEXT is not None:
            LWKAddCIPHERTEXT(builder, CIPHERTEXT)
        if self.TAG is not None:
            LWKAddTAG(builder, TAG)
        LWKAddEXPIRES_AT(builder, self.EXPIRES_AT)
        LWK = LWKEnd(builder)
        return LWK
