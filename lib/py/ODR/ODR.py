# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Estimation Run Report. This is the canonical estimation-family result
# model. STATE_COVARIANCE is row-major with COVARIANCE_DIMENSION squared
# entries and is also published as the $OCM named by OCM_CONTENT_ID. The
# estimator report never substitutes a propagator name for its input port.
class ODR(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ODR()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsODR(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ODRBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x44\x52", size_prefixed=size_prefixed)

    # ODR
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ODR
    def RUN_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODR
    def STARTED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODR
    def COMPLETED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODR
    def CONFIGURATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from ODRSolverConfiguration import ODRSolverConfiguration
            obj = ODRSolverConfiguration()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODR
    def CONVERGENCE_REASON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # ODR
    def ITERATIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ODRIterationStatistics import ODRIterationStatistics
            obj = ODRIterationStatistics()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODR
    def ITERATIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODR
    def ITERATIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # ODR
    def EDITED_OBSERVATIONS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ODREditedObservation import ODREditedObservation
            obj = ODREditedObservation()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODR
    def EDITED_OBSERVATIONSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODR
    def EDITED_OBSERVATIONSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # ODR
    def ESTIMATED_PARAMETERS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ODREstimatedParameter import ODREstimatedParameter
            obj = ODREstimatedParameter()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODR
    def ESTIMATED_PARAMETERSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODR
    def ESTIMATED_PARAMETERSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # ODR
    def ESTIMATED_EPOCH_STATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FRMStateVector import FRMStateVector
            obj = FRMStateVector()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODR
    def COVARIANCE_DIMENSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # ODR
    def STATE_COVARIANCE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ODR
    def STATE_COVARIANCEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # ODR
    def STATE_COVARIANCELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODR
    def STATE_COVARIANCEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # ODR
    def OCM_CONTENT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODR
    def FILTER_HISTORY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from ODRFilterEpoch import ODRFilterEpoch
            obj = ODRFilterEpoch()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODR
    def FILTER_HISTORYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODR
    def FILTER_HISTORYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

    # ODR
    def RESIDUAL_RMS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ODR
    def TRACE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODR
    def ATTESTATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from ODRAttestation import ODRAttestation
            obj = ODRAttestation()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def ODRStart(builder):
    builder.StartObject(16)

def Start(builder):
    ODRStart(builder)

def ODRAddRUN_ID(builder, RUN_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RUN_ID), 0)

def AddRUN_ID(builder, RUN_ID):
    ODRAddRUN_ID(builder, RUN_ID)

def ODRAddSTARTED_AT(builder, STARTED_AT):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(STARTED_AT), 0)

def AddSTARTED_AT(builder, STARTED_AT):
    ODRAddSTARTED_AT(builder, STARTED_AT)

def ODRAddCOMPLETED_AT(builder, COMPLETED_AT):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(COMPLETED_AT), 0)

def AddCOMPLETED_AT(builder, COMPLETED_AT):
    ODRAddCOMPLETED_AT(builder, COMPLETED_AT)

def ODRAddCONFIGURATION(builder, CONFIGURATION):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(CONFIGURATION), 0)

def AddCONFIGURATION(builder, CONFIGURATION):
    ODRAddCONFIGURATION(builder, CONFIGURATION)

def ODRAddCONVERGENCE_REASON(builder, CONVERGENCE_REASON):
    builder.PrependUint8Slot(4, CONVERGENCE_REASON, 0)

def AddCONVERGENCE_REASON(builder, CONVERGENCE_REASON):
    ODRAddCONVERGENCE_REASON(builder, CONVERGENCE_REASON)

def ODRAddITERATIONS(builder, ITERATIONS):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(ITERATIONS), 0)

def AddITERATIONS(builder, ITERATIONS):
    ODRAddITERATIONS(builder, ITERATIONS)

def ODRStartITERATIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartITERATIONSVector(builder, numElems):
    return ODRStartITERATIONSVector(builder, numElems)

def ODRCreateITERATIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateITERATIONSVector(builder, data):
    ODRCreateITERATIONSVector(builder, data)

def ODRAddEDITED_OBSERVATIONS(builder, EDITED_OBSERVATIONS):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(EDITED_OBSERVATIONS), 0)

def AddEDITED_OBSERVATIONS(builder, EDITED_OBSERVATIONS):
    ODRAddEDITED_OBSERVATIONS(builder, EDITED_OBSERVATIONS)

def ODRStartEDITED_OBSERVATIONSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartEDITED_OBSERVATIONSVector(builder, numElems):
    return ODRStartEDITED_OBSERVATIONSVector(builder, numElems)

def ODRCreateEDITED_OBSERVATIONSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateEDITED_OBSERVATIONSVector(builder, data):
    ODRCreateEDITED_OBSERVATIONSVector(builder, data)

def ODRAddESTIMATED_PARAMETERS(builder, ESTIMATED_PARAMETERS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(ESTIMATED_PARAMETERS), 0)

def AddESTIMATED_PARAMETERS(builder, ESTIMATED_PARAMETERS):
    ODRAddESTIMATED_PARAMETERS(builder, ESTIMATED_PARAMETERS)

def ODRStartESTIMATED_PARAMETERSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartESTIMATED_PARAMETERSVector(builder, numElems):
    return ODRStartESTIMATED_PARAMETERSVector(builder, numElems)

def ODRCreateESTIMATED_PARAMETERSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateESTIMATED_PARAMETERSVector(builder, data):
    ODRCreateESTIMATED_PARAMETERSVector(builder, data)

def ODRAddESTIMATED_EPOCH_STATE(builder, ESTIMATED_EPOCH_STATE):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(ESTIMATED_EPOCH_STATE), 0)

def AddESTIMATED_EPOCH_STATE(builder, ESTIMATED_EPOCH_STATE):
    ODRAddESTIMATED_EPOCH_STATE(builder, ESTIMATED_EPOCH_STATE)

def ODRAddCOVARIANCE_DIMENSION(builder, COVARIANCE_DIMENSION):
    builder.PrependUint32Slot(9, COVARIANCE_DIMENSION, 0)

def AddCOVARIANCE_DIMENSION(builder, COVARIANCE_DIMENSION):
    ODRAddCOVARIANCE_DIMENSION(builder, COVARIANCE_DIMENSION)

def ODRAddSTATE_COVARIANCE(builder, STATE_COVARIANCE):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(STATE_COVARIANCE), 0)

def AddSTATE_COVARIANCE(builder, STATE_COVARIANCE):
    ODRAddSTATE_COVARIANCE(builder, STATE_COVARIANCE)

def ODRStartSTATE_COVARIANCEVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSTATE_COVARIANCEVector(builder, numElems):
    return ODRStartSTATE_COVARIANCEVector(builder, numElems)

def ODRCreateSTATE_COVARIANCEVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSTATE_COVARIANCEVector(builder, data):
    ODRCreateSTATE_COVARIANCEVector(builder, data)

def ODRAddOCM_CONTENT_ID(builder, OCM_CONTENT_ID):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(OCM_CONTENT_ID), 0)

def AddOCM_CONTENT_ID(builder, OCM_CONTENT_ID):
    ODRAddOCM_CONTENT_ID(builder, OCM_CONTENT_ID)

def ODRAddFILTER_HISTORY(builder, FILTER_HISTORY):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(FILTER_HISTORY), 0)

def AddFILTER_HISTORY(builder, FILTER_HISTORY):
    ODRAddFILTER_HISTORY(builder, FILTER_HISTORY)

def ODRStartFILTER_HISTORYVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartFILTER_HISTORYVector(builder, numElems):
    return ODRStartFILTER_HISTORYVector(builder, numElems)

def ODRCreateFILTER_HISTORYVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateFILTER_HISTORYVector(builder, data):
    ODRCreateFILTER_HISTORYVector(builder, data)

def ODRAddRESIDUAL_RMS(builder, RESIDUAL_RMS):
    builder.PrependFloat64Slot(13, RESIDUAL_RMS, 0.0)

def AddRESIDUAL_RMS(builder, RESIDUAL_RMS):
    ODRAddRESIDUAL_RMS(builder, RESIDUAL_RMS)

def ODRAddTRACE_ID(builder, TRACE_ID):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(TRACE_ID), 0)

def AddTRACE_ID(builder, TRACE_ID):
    ODRAddTRACE_ID(builder, TRACE_ID)

def ODRAddATTESTATION(builder, ATTESTATION):
    builder.PrependUOffsetTRelativeSlot(15, flatbuffers.number_types.UOffsetTFlags.py_type(ATTESTATION), 0)

def AddATTESTATION(builder, ATTESTATION):
    ODRAddATTESTATION(builder, ATTESTATION)

def ODREnd(builder):
    return builder.EndObject()

def End(builder):
    return ODREnd(builder)

import FRMStateVector
import ODRAttestation
import ODREditedObservation
import ODREstimatedParameter
import ODRFilterEpoch
import ODRIterationStatistics
import ODRSolverConfiguration
try:
    from typing import List, Optional
except:
    pass

class ODRT(object):

    # ODRT
    def __init__(
        self,
        RUN_ID = None,
        STARTED_AT = None,
        COMPLETED_AT = None,
        CONFIGURATION = None,
        CONVERGENCE_REASON = 0,
        ITERATIONS = None,
        EDITED_OBSERVATIONS = None,
        ESTIMATED_PARAMETERS = None,
        ESTIMATED_EPOCH_STATE = None,
        COVARIANCE_DIMENSION = 0,
        STATE_COVARIANCE = None,
        OCM_CONTENT_ID = None,
        FILTER_HISTORY = None,
        RESIDUAL_RMS = 0.0,
        TRACE_ID = None,
        ATTESTATION = None,
    ):
        self.RUN_ID = RUN_ID  # type: Optional[str]
        self.STARTED_AT = STARTED_AT  # type: Optional[str]
        self.COMPLETED_AT = COMPLETED_AT  # type: Optional[str]
        self.CONFIGURATION = CONFIGURATION  # type: Optional[ODRSolverConfiguration.ODRSolverConfigurationT]
        self.CONVERGENCE_REASON = CONVERGENCE_REASON  # type: int
        self.ITERATIONS = ITERATIONS  # type: Optional[List[ODRIterationStatistics.ODRIterationStatisticsT]]
        self.EDITED_OBSERVATIONS = EDITED_OBSERVATIONS  # type: Optional[List[ODREditedObservation.ODREditedObservationT]]
        self.ESTIMATED_PARAMETERS = ESTIMATED_PARAMETERS  # type: Optional[List[ODREstimatedParameter.ODREstimatedParameterT]]
        self.ESTIMATED_EPOCH_STATE = ESTIMATED_EPOCH_STATE  # type: Optional[FRMStateVector.FRMStateVectorT]
        self.COVARIANCE_DIMENSION = COVARIANCE_DIMENSION  # type: int
        self.STATE_COVARIANCE = STATE_COVARIANCE  # type: Optional[List[float]]
        self.OCM_CONTENT_ID = OCM_CONTENT_ID  # type: Optional[str]
        self.FILTER_HISTORY = FILTER_HISTORY  # type: Optional[List[ODRFilterEpoch.ODRFilterEpochT]]
        self.RESIDUAL_RMS = RESIDUAL_RMS  # type: float
        self.TRACE_ID = TRACE_ID  # type: Optional[str]
        self.ATTESTATION = ATTESTATION  # type: Optional[ODRAttestation.ODRAttestationT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOdr = ODR()
        tmpOdr.Init(buf, pos)
        return cls.InitFromObj(tmpOdr)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOdr):
        x = ODRT()
        x._UnPack(tmpOdr)
        return x

    # ODRT
    def _UnPack(self, ODR):
        if ODR is None:
            return
        self.RUN_ID = ODR.RUN_ID()
        self.STARTED_AT = ODR.STARTED_AT()
        self.COMPLETED_AT = ODR.COMPLETED_AT()
        if ODR.CONFIGURATION() is not None:
            self.CONFIGURATION = ODRSolverConfiguration.ODRSolverConfigurationT.InitFromObj(ODR.CONFIGURATION())
        self.CONVERGENCE_REASON = ODR.CONVERGENCE_REASON()
        if not ODR.ITERATIONSIsNone():
            self.ITERATIONS = []
            for i in range(ODR.ITERATIONSLength()):
                if ODR.ITERATIONS(i) is None:
                    self.ITERATIONS.append(None)
                else:
                    oDRIterationStatistics_ = ODRIterationStatistics.ODRIterationStatisticsT.InitFromObj(ODR.ITERATIONS(i))
                    self.ITERATIONS.append(oDRIterationStatistics_)
        if not ODR.EDITED_OBSERVATIONSIsNone():
            self.EDITED_OBSERVATIONS = []
            for i in range(ODR.EDITED_OBSERVATIONSLength()):
                if ODR.EDITED_OBSERVATIONS(i) is None:
                    self.EDITED_OBSERVATIONS.append(None)
                else:
                    oDREditedObservation_ = ODREditedObservation.ODREditedObservationT.InitFromObj(ODR.EDITED_OBSERVATIONS(i))
                    self.EDITED_OBSERVATIONS.append(oDREditedObservation_)
        if not ODR.ESTIMATED_PARAMETERSIsNone():
            self.ESTIMATED_PARAMETERS = []
            for i in range(ODR.ESTIMATED_PARAMETERSLength()):
                if ODR.ESTIMATED_PARAMETERS(i) is None:
                    self.ESTIMATED_PARAMETERS.append(None)
                else:
                    oDREstimatedParameter_ = ODREstimatedParameter.ODREstimatedParameterT.InitFromObj(ODR.ESTIMATED_PARAMETERS(i))
                    self.ESTIMATED_PARAMETERS.append(oDREstimatedParameter_)
        if ODR.ESTIMATED_EPOCH_STATE() is not None:
            self.ESTIMATED_EPOCH_STATE = FRMStateVector.FRMStateVectorT.InitFromObj(ODR.ESTIMATED_EPOCH_STATE())
        self.COVARIANCE_DIMENSION = ODR.COVARIANCE_DIMENSION()
        if not ODR.STATE_COVARIANCEIsNone():
            if np is None:
                self.STATE_COVARIANCE = []
                for i in range(ODR.STATE_COVARIANCELength()):
                    self.STATE_COVARIANCE.append(ODR.STATE_COVARIANCE(i))
            else:
                self.STATE_COVARIANCE = ODR.STATE_COVARIANCEAsNumpy()
        self.OCM_CONTENT_ID = ODR.OCM_CONTENT_ID()
        if not ODR.FILTER_HISTORYIsNone():
            self.FILTER_HISTORY = []
            for i in range(ODR.FILTER_HISTORYLength()):
                if ODR.FILTER_HISTORY(i) is None:
                    self.FILTER_HISTORY.append(None)
                else:
                    oDRFilterEpoch_ = ODRFilterEpoch.ODRFilterEpochT.InitFromObj(ODR.FILTER_HISTORY(i))
                    self.FILTER_HISTORY.append(oDRFilterEpoch_)
        self.RESIDUAL_RMS = ODR.RESIDUAL_RMS()
        self.TRACE_ID = ODR.TRACE_ID()
        if ODR.ATTESTATION() is not None:
            self.ATTESTATION = ODRAttestation.ODRAttestationT.InitFromObj(ODR.ATTESTATION())

    # ODRT
    def Pack(self, builder):
        if self.RUN_ID is not None:
            RUN_ID = builder.CreateString(self.RUN_ID)
        if self.STARTED_AT is not None:
            STARTED_AT = builder.CreateString(self.STARTED_AT)
        if self.COMPLETED_AT is not None:
            COMPLETED_AT = builder.CreateString(self.COMPLETED_AT)
        if self.CONFIGURATION is not None:
            CONFIGURATION = self.CONFIGURATION.Pack(builder)
        if self.ITERATIONS is not None:
            ITERATIONSlist = []
            for i in range(len(self.ITERATIONS)):
                ITERATIONSlist.append(self.ITERATIONS[i].Pack(builder))
            ODRStartITERATIONSVector(builder, len(self.ITERATIONS))
            for i in reversed(range(len(self.ITERATIONS))):
                builder.PrependUOffsetTRelative(ITERATIONSlist[i])
            ITERATIONS = builder.EndVector()
        if self.EDITED_OBSERVATIONS is not None:
            EDITED_OBSERVATIONSlist = []
            for i in range(len(self.EDITED_OBSERVATIONS)):
                EDITED_OBSERVATIONSlist.append(self.EDITED_OBSERVATIONS[i].Pack(builder))
            ODRStartEDITED_OBSERVATIONSVector(builder, len(self.EDITED_OBSERVATIONS))
            for i in reversed(range(len(self.EDITED_OBSERVATIONS))):
                builder.PrependUOffsetTRelative(EDITED_OBSERVATIONSlist[i])
            EDITED_OBSERVATIONS = builder.EndVector()
        if self.ESTIMATED_PARAMETERS is not None:
            ESTIMATED_PARAMETERSlist = []
            for i in range(len(self.ESTIMATED_PARAMETERS)):
                ESTIMATED_PARAMETERSlist.append(self.ESTIMATED_PARAMETERS[i].Pack(builder))
            ODRStartESTIMATED_PARAMETERSVector(builder, len(self.ESTIMATED_PARAMETERS))
            for i in reversed(range(len(self.ESTIMATED_PARAMETERS))):
                builder.PrependUOffsetTRelative(ESTIMATED_PARAMETERSlist[i])
            ESTIMATED_PARAMETERS = builder.EndVector()
        if self.ESTIMATED_EPOCH_STATE is not None:
            ESTIMATED_EPOCH_STATE = self.ESTIMATED_EPOCH_STATE.Pack(builder)
        if self.STATE_COVARIANCE is not None:
            if np is not None and type(self.STATE_COVARIANCE) is np.ndarray:
                STATE_COVARIANCE = builder.CreateNumpyVector(self.STATE_COVARIANCE)
            else:
                ODRStartSTATE_COVARIANCEVector(builder, len(self.STATE_COVARIANCE))
                for i in reversed(range(len(self.STATE_COVARIANCE))):
                    builder.PrependFloat64(self.STATE_COVARIANCE[i])
                STATE_COVARIANCE = builder.EndVector()
        if self.OCM_CONTENT_ID is not None:
            OCM_CONTENT_ID = builder.CreateString(self.OCM_CONTENT_ID)
        if self.FILTER_HISTORY is not None:
            FILTER_HISTORYlist = []
            for i in range(len(self.FILTER_HISTORY)):
                FILTER_HISTORYlist.append(self.FILTER_HISTORY[i].Pack(builder))
            ODRStartFILTER_HISTORYVector(builder, len(self.FILTER_HISTORY))
            for i in reversed(range(len(self.FILTER_HISTORY))):
                builder.PrependUOffsetTRelative(FILTER_HISTORYlist[i])
            FILTER_HISTORY = builder.EndVector()
        if self.TRACE_ID is not None:
            TRACE_ID = builder.CreateString(self.TRACE_ID)
        if self.ATTESTATION is not None:
            ATTESTATION = self.ATTESTATION.Pack(builder)
        ODRStart(builder)
        if self.RUN_ID is not None:
            ODRAddRUN_ID(builder, RUN_ID)
        if self.STARTED_AT is not None:
            ODRAddSTARTED_AT(builder, STARTED_AT)
        if self.COMPLETED_AT is not None:
            ODRAddCOMPLETED_AT(builder, COMPLETED_AT)
        if self.CONFIGURATION is not None:
            ODRAddCONFIGURATION(builder, CONFIGURATION)
        ODRAddCONVERGENCE_REASON(builder, self.CONVERGENCE_REASON)
        if self.ITERATIONS is not None:
            ODRAddITERATIONS(builder, ITERATIONS)
        if self.EDITED_OBSERVATIONS is not None:
            ODRAddEDITED_OBSERVATIONS(builder, EDITED_OBSERVATIONS)
        if self.ESTIMATED_PARAMETERS is not None:
            ODRAddESTIMATED_PARAMETERS(builder, ESTIMATED_PARAMETERS)
        if self.ESTIMATED_EPOCH_STATE is not None:
            ODRAddESTIMATED_EPOCH_STATE(builder, ESTIMATED_EPOCH_STATE)
        ODRAddCOVARIANCE_DIMENSION(builder, self.COVARIANCE_DIMENSION)
        if self.STATE_COVARIANCE is not None:
            ODRAddSTATE_COVARIANCE(builder, STATE_COVARIANCE)
        if self.OCM_CONTENT_ID is not None:
            ODRAddOCM_CONTENT_ID(builder, OCM_CONTENT_ID)
        if self.FILTER_HISTORY is not None:
            ODRAddFILTER_HISTORY(builder, FILTER_HISTORY)
        ODRAddRESIDUAL_RMS(builder, self.RESIDUAL_RMS)
        if self.TRACE_ID is not None:
            ODRAddTRACE_ID(builder, TRACE_ID)
        if self.ATTESTATION is not None:
            ODRAddATTESTATION(builder, ATTESTATION)
        ODR = ODREnd(builder)
        return ODR
