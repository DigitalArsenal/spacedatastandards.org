# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ODREditedObservation(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ODREditedObservation()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsODREditedObservation(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ODREditedObservationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x44\x52", size_prefixed=size_prefixed)

    # ODREditedObservation
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ODREditedObservation
    def OBSERVATION_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODREditedObservation
    def ITERATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # ODREditedObservation
    def PREFIT_RESIDUAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ODREditedObservation
    def NORMALIZED_RESIDUAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ODREditedObservation
    def REASON(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def ODREditedObservationStart(builder):
    builder.StartObject(5)

def Start(builder):
    ODREditedObservationStart(builder)

def ODREditedObservationAddOBSERVATION_ID(builder, OBSERVATION_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(OBSERVATION_ID), 0)

def AddOBSERVATION_ID(builder, OBSERVATION_ID):
    ODREditedObservationAddOBSERVATION_ID(builder, OBSERVATION_ID)

def ODREditedObservationAddITERATION(builder, ITERATION):
    builder.PrependUint32Slot(1, ITERATION, 0)

def AddITERATION(builder, ITERATION):
    ODREditedObservationAddITERATION(builder, ITERATION)

def ODREditedObservationAddPREFIT_RESIDUAL(builder, PREFIT_RESIDUAL):
    builder.PrependFloat64Slot(2, PREFIT_RESIDUAL, 0.0)

def AddPREFIT_RESIDUAL(builder, PREFIT_RESIDUAL):
    ODREditedObservationAddPREFIT_RESIDUAL(builder, PREFIT_RESIDUAL)

def ODREditedObservationAddNORMALIZED_RESIDUAL(builder, NORMALIZED_RESIDUAL):
    builder.PrependFloat64Slot(3, NORMALIZED_RESIDUAL, 0.0)

def AddNORMALIZED_RESIDUAL(builder, NORMALIZED_RESIDUAL):
    ODREditedObservationAddNORMALIZED_RESIDUAL(builder, NORMALIZED_RESIDUAL)

def ODREditedObservationAddREASON(builder, REASON):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(REASON), 0)

def AddREASON(builder, REASON):
    ODREditedObservationAddREASON(builder, REASON)

def ODREditedObservationEnd(builder):
    return builder.EndObject()

def End(builder):
    return ODREditedObservationEnd(builder)


class ODREditedObservationT(object):

    # ODREditedObservationT
    def __init__(
        self,
        OBSERVATION_ID = None,
        ITERATION = 0,
        PREFIT_RESIDUAL = 0.0,
        NORMALIZED_RESIDUAL = 0.0,
        REASON = None,
    ):
        self.OBSERVATION_ID = OBSERVATION_ID  # type: Optional[str]
        self.ITERATION = ITERATION  # type: int
        self.PREFIT_RESIDUAL = PREFIT_RESIDUAL  # type: float
        self.NORMALIZED_RESIDUAL = NORMALIZED_RESIDUAL  # type: float
        self.REASON = REASON  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOdreditedObservation = ODREditedObservation()
        tmpOdreditedObservation.Init(buf, pos)
        return cls.InitFromObj(tmpOdreditedObservation)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOdreditedObservation):
        x = ODREditedObservationT()
        x._UnPack(tmpOdreditedObservation)
        return x

    # ODREditedObservationT
    def _UnPack(self, ODREditedObservation):
        if ODREditedObservation is None:
            return
        self.OBSERVATION_ID = ODREditedObservation.OBSERVATION_ID()
        self.ITERATION = ODREditedObservation.ITERATION()
        self.PREFIT_RESIDUAL = ODREditedObservation.PREFIT_RESIDUAL()
        self.NORMALIZED_RESIDUAL = ODREditedObservation.NORMALIZED_RESIDUAL()
        self.REASON = ODREditedObservation.REASON()

    # ODREditedObservationT
    def Pack(self, builder):
        if self.OBSERVATION_ID is not None:
            OBSERVATION_ID = builder.CreateString(self.OBSERVATION_ID)
        if self.REASON is not None:
            REASON = builder.CreateString(self.REASON)
        ODREditedObservationStart(builder)
        if self.OBSERVATION_ID is not None:
            ODREditedObservationAddOBSERVATION_ID(builder, OBSERVATION_ID)
        ODREditedObservationAddITERATION(builder, self.ITERATION)
        ODREditedObservationAddPREFIT_RESIDUAL(builder, self.PREFIT_RESIDUAL)
        ODREditedObservationAddNORMALIZED_RESIDUAL(builder, self.NORMALIZED_RESIDUAL)
        if self.REASON is not None:
            ODREditedObservationAddREASON(builder, REASON)
        ODREditedObservation = ODREditedObservationEnd(builder)
        return ODREditedObservation
