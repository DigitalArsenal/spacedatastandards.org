# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ODRSolverConfiguration(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ODRSolverConfiguration()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsODRSolverConfiguration(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ODRSolverConfigurationBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x44\x52", size_prefixed=size_prefixed)

    # ODRSolverConfiguration
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ODRSolverConfiguration
    def ESTIMATOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # ODRSolverConfiguration
    def MAXIMUM_ITERATIONS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # ODRSolverConfiguration
    def STATE_CONVERGENCE_TOLERANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ODRSolverConfiguration
    def RMS_CONVERGENCE_TOLERANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ODRSolverConfiguration
    def SIGMA_EDIT_THRESHOLD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ODRSolverConfiguration
    def PROCESS_NOISE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # ODRSolverConfiguration
    def PROCESS_NOISE_SPECTRAL_DENSITY(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ODRSolverConfiguration
    def PROCESS_NOISE_SPECTRAL_DENSITYAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # ODRSolverConfiguration
    def PROCESS_NOISE_SPECTRAL_DENSITYLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODRSolverConfiguration
    def PROCESS_NOISE_SPECTRAL_DENSITYIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # ODRSolverConfiguration
    def DYNAMIC_MODEL_CORRELATION_TIME_SECONDS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ODRSolverConfiguration
    def PROPAGATOR_PORT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODRSolverConfiguration
    def PROPAGATOR_CAPABILITY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def ODRSolverConfigurationStart(builder):
    builder.StartObject(10)

def Start(builder):
    ODRSolverConfigurationStart(builder)

def ODRSolverConfigurationAddESTIMATOR(builder, ESTIMATOR):
    builder.PrependUint8Slot(0, ESTIMATOR, 0)

def AddESTIMATOR(builder, ESTIMATOR):
    ODRSolverConfigurationAddESTIMATOR(builder, ESTIMATOR)

def ODRSolverConfigurationAddMAXIMUM_ITERATIONS(builder, MAXIMUM_ITERATIONS):
    builder.PrependUint32Slot(1, MAXIMUM_ITERATIONS, 0)

def AddMAXIMUM_ITERATIONS(builder, MAXIMUM_ITERATIONS):
    ODRSolverConfigurationAddMAXIMUM_ITERATIONS(builder, MAXIMUM_ITERATIONS)

def ODRSolverConfigurationAddSTATE_CONVERGENCE_TOLERANCE(builder, STATE_CONVERGENCE_TOLERANCE):
    builder.PrependFloat64Slot(2, STATE_CONVERGENCE_TOLERANCE, 0.0)

def AddSTATE_CONVERGENCE_TOLERANCE(builder, STATE_CONVERGENCE_TOLERANCE):
    ODRSolverConfigurationAddSTATE_CONVERGENCE_TOLERANCE(builder, STATE_CONVERGENCE_TOLERANCE)

def ODRSolverConfigurationAddRMS_CONVERGENCE_TOLERANCE(builder, RMS_CONVERGENCE_TOLERANCE):
    builder.PrependFloat64Slot(3, RMS_CONVERGENCE_TOLERANCE, 0.0)

def AddRMS_CONVERGENCE_TOLERANCE(builder, RMS_CONVERGENCE_TOLERANCE):
    ODRSolverConfigurationAddRMS_CONVERGENCE_TOLERANCE(builder, RMS_CONVERGENCE_TOLERANCE)

def ODRSolverConfigurationAddSIGMA_EDIT_THRESHOLD(builder, SIGMA_EDIT_THRESHOLD):
    builder.PrependFloat64Slot(4, SIGMA_EDIT_THRESHOLD, 0.0)

def AddSIGMA_EDIT_THRESHOLD(builder, SIGMA_EDIT_THRESHOLD):
    ODRSolverConfigurationAddSIGMA_EDIT_THRESHOLD(builder, SIGMA_EDIT_THRESHOLD)

def ODRSolverConfigurationAddPROCESS_NOISE(builder, PROCESS_NOISE):
    builder.PrependUint8Slot(5, PROCESS_NOISE, 0)

def AddPROCESS_NOISE(builder, PROCESS_NOISE):
    ODRSolverConfigurationAddPROCESS_NOISE(builder, PROCESS_NOISE)

def ODRSolverConfigurationAddPROCESS_NOISE_SPECTRAL_DENSITY(builder, PROCESS_NOISE_SPECTRAL_DENSITY):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(PROCESS_NOISE_SPECTRAL_DENSITY), 0)

def AddPROCESS_NOISE_SPECTRAL_DENSITY(builder, PROCESS_NOISE_SPECTRAL_DENSITY):
    ODRSolverConfigurationAddPROCESS_NOISE_SPECTRAL_DENSITY(builder, PROCESS_NOISE_SPECTRAL_DENSITY)

def ODRSolverConfigurationStartPROCESS_NOISE_SPECTRAL_DENSITYVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartPROCESS_NOISE_SPECTRAL_DENSITYVector(builder, numElems):
    return ODRSolverConfigurationStartPROCESS_NOISE_SPECTRAL_DENSITYVector(builder, numElems)

def ODRSolverConfigurationCreatePROCESS_NOISE_SPECTRAL_DENSITYVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreatePROCESS_NOISE_SPECTRAL_DENSITYVector(builder, data):
    ODRSolverConfigurationCreatePROCESS_NOISE_SPECTRAL_DENSITYVector(builder, data)

def ODRSolverConfigurationAddDYNAMIC_MODEL_CORRELATION_TIME_SECONDS(builder, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS):
    builder.PrependFloat64Slot(7, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS, 0.0)

def AddDYNAMIC_MODEL_CORRELATION_TIME_SECONDS(builder, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS):
    ODRSolverConfigurationAddDYNAMIC_MODEL_CORRELATION_TIME_SECONDS(builder, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS)

def ODRSolverConfigurationAddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(PROPAGATOR_PORT_ID), 0)

def AddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID):
    ODRSolverConfigurationAddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID)

def ODRSolverConfigurationAddPROPAGATOR_CAPABILITY(builder, PROPAGATOR_CAPABILITY):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(PROPAGATOR_CAPABILITY), 0)

def AddPROPAGATOR_CAPABILITY(builder, PROPAGATOR_CAPABILITY):
    ODRSolverConfigurationAddPROPAGATOR_CAPABILITY(builder, PROPAGATOR_CAPABILITY)

def ODRSolverConfigurationEnd(builder):
    return builder.EndObject()

def End(builder):
    return ODRSolverConfigurationEnd(builder)

try:
    from typing import List
except:
    pass

class ODRSolverConfigurationT(object):

    # ODRSolverConfigurationT
    def __init__(
        self,
        ESTIMATOR = 0,
        MAXIMUM_ITERATIONS = 0,
        STATE_CONVERGENCE_TOLERANCE = 0.0,
        RMS_CONVERGENCE_TOLERANCE = 0.0,
        SIGMA_EDIT_THRESHOLD = 0.0,
        PROCESS_NOISE = 0,
        PROCESS_NOISE_SPECTRAL_DENSITY = None,
        DYNAMIC_MODEL_CORRELATION_TIME_SECONDS = 0.0,
        PROPAGATOR_PORT_ID = None,
        PROPAGATOR_CAPABILITY = None,
    ):
        self.ESTIMATOR = ESTIMATOR  # type: int
        self.MAXIMUM_ITERATIONS = MAXIMUM_ITERATIONS  # type: int
        self.STATE_CONVERGENCE_TOLERANCE = STATE_CONVERGENCE_TOLERANCE  # type: float
        self.RMS_CONVERGENCE_TOLERANCE = RMS_CONVERGENCE_TOLERANCE  # type: float
        self.SIGMA_EDIT_THRESHOLD = SIGMA_EDIT_THRESHOLD  # type: float
        self.PROCESS_NOISE = PROCESS_NOISE  # type: int
        self.PROCESS_NOISE_SPECTRAL_DENSITY = PROCESS_NOISE_SPECTRAL_DENSITY  # type: Optional[List[float]]
        self.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS = DYNAMIC_MODEL_CORRELATION_TIME_SECONDS  # type: float
        self.PROPAGATOR_PORT_ID = PROPAGATOR_PORT_ID  # type: Optional[str]
        self.PROPAGATOR_CAPABILITY = PROPAGATOR_CAPABILITY  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOdrsolverConfiguration = ODRSolverConfiguration()
        tmpOdrsolverConfiguration.Init(buf, pos)
        return cls.InitFromObj(tmpOdrsolverConfiguration)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOdrsolverConfiguration):
        x = ODRSolverConfigurationT()
        x._UnPack(tmpOdrsolverConfiguration)
        return x

    # ODRSolverConfigurationT
    def _UnPack(self, ODRSolverConfiguration):
        if ODRSolverConfiguration is None:
            return
        self.ESTIMATOR = ODRSolverConfiguration.ESTIMATOR()
        self.MAXIMUM_ITERATIONS = ODRSolverConfiguration.MAXIMUM_ITERATIONS()
        self.STATE_CONVERGENCE_TOLERANCE = ODRSolverConfiguration.STATE_CONVERGENCE_TOLERANCE()
        self.RMS_CONVERGENCE_TOLERANCE = ODRSolverConfiguration.RMS_CONVERGENCE_TOLERANCE()
        self.SIGMA_EDIT_THRESHOLD = ODRSolverConfiguration.SIGMA_EDIT_THRESHOLD()
        self.PROCESS_NOISE = ODRSolverConfiguration.PROCESS_NOISE()
        if not ODRSolverConfiguration.PROCESS_NOISE_SPECTRAL_DENSITYIsNone():
            if np is None:
                self.PROCESS_NOISE_SPECTRAL_DENSITY = []
                for i in range(ODRSolverConfiguration.PROCESS_NOISE_SPECTRAL_DENSITYLength()):
                    self.PROCESS_NOISE_SPECTRAL_DENSITY.append(ODRSolverConfiguration.PROCESS_NOISE_SPECTRAL_DENSITY(i))
            else:
                self.PROCESS_NOISE_SPECTRAL_DENSITY = ODRSolverConfiguration.PROCESS_NOISE_SPECTRAL_DENSITYAsNumpy()
        self.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS = ODRSolverConfiguration.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS()
        self.PROPAGATOR_PORT_ID = ODRSolverConfiguration.PROPAGATOR_PORT_ID()
        self.PROPAGATOR_CAPABILITY = ODRSolverConfiguration.PROPAGATOR_CAPABILITY()

    # ODRSolverConfigurationT
    def Pack(self, builder):
        if self.PROCESS_NOISE_SPECTRAL_DENSITY is not None:
            if np is not None and type(self.PROCESS_NOISE_SPECTRAL_DENSITY) is np.ndarray:
                PROCESS_NOISE_SPECTRAL_DENSITY = builder.CreateNumpyVector(self.PROCESS_NOISE_SPECTRAL_DENSITY)
            else:
                ODRSolverConfigurationStartPROCESS_NOISE_SPECTRAL_DENSITYVector(builder, len(self.PROCESS_NOISE_SPECTRAL_DENSITY))
                for i in reversed(range(len(self.PROCESS_NOISE_SPECTRAL_DENSITY))):
                    builder.PrependFloat64(self.PROCESS_NOISE_SPECTRAL_DENSITY[i])
                PROCESS_NOISE_SPECTRAL_DENSITY = builder.EndVector()
        if self.PROPAGATOR_PORT_ID is not None:
            PROPAGATOR_PORT_ID = builder.CreateString(self.PROPAGATOR_PORT_ID)
        if self.PROPAGATOR_CAPABILITY is not None:
            PROPAGATOR_CAPABILITY = builder.CreateString(self.PROPAGATOR_CAPABILITY)
        ODRSolverConfigurationStart(builder)
        ODRSolverConfigurationAddESTIMATOR(builder, self.ESTIMATOR)
        ODRSolverConfigurationAddMAXIMUM_ITERATIONS(builder, self.MAXIMUM_ITERATIONS)
        ODRSolverConfigurationAddSTATE_CONVERGENCE_TOLERANCE(builder, self.STATE_CONVERGENCE_TOLERANCE)
        ODRSolverConfigurationAddRMS_CONVERGENCE_TOLERANCE(builder, self.RMS_CONVERGENCE_TOLERANCE)
        ODRSolverConfigurationAddSIGMA_EDIT_THRESHOLD(builder, self.SIGMA_EDIT_THRESHOLD)
        ODRSolverConfigurationAddPROCESS_NOISE(builder, self.PROCESS_NOISE)
        if self.PROCESS_NOISE_SPECTRAL_DENSITY is not None:
            ODRSolverConfigurationAddPROCESS_NOISE_SPECTRAL_DENSITY(builder, PROCESS_NOISE_SPECTRAL_DENSITY)
        ODRSolverConfigurationAddDYNAMIC_MODEL_CORRELATION_TIME_SECONDS(builder, self.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS)
        if self.PROPAGATOR_PORT_ID is not None:
            ODRSolverConfigurationAddPROPAGATOR_PORT_ID(builder, PROPAGATOR_PORT_ID)
        if self.PROPAGATOR_CAPABILITY is not None:
            ODRSolverConfigurationAddPROPAGATOR_CAPABILITY(builder, PROPAGATOR_CAPABILITY)
        ODRSolverConfiguration = ODRSolverConfigurationEnd(builder)
        return ODRSolverConfiguration
