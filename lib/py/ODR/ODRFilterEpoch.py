# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ODRFilterEpoch(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ODRFilterEpoch()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsODRFilterEpoch(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ODRFilterEpochBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x4F\x44\x52", size_prefixed=size_prefixed)

    # ODRFilterEpoch
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ODRFilterEpoch
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ODRFilterEpoch
    def FILTERED_STATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FRMStateVector import FRMStateVector
            obj = FRMStateVector()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODRFilterEpoch
    def FILTERED_COVARIANCE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ODRFilterEpoch
    def FILTERED_COVARIANCEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # ODRFilterEpoch
    def FILTERED_COVARIANCELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODRFilterEpoch
    def FILTERED_COVARIANCEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # ODRFilterEpoch
    def SMOOTHED_STATE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FRMStateVector import FRMStateVector
            obj = FRMStateVector()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ODRFilterEpoch
    def SMOOTHED_COVARIANCE(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ODRFilterEpoch
    def SMOOTHED_COVARIANCEAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # ODRFilterEpoch
    def SMOOTHED_COVARIANCELength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ODRFilterEpoch
    def SMOOTHED_COVARIANCEIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # ODRFilterEpoch
    def NORMALIZED_INNOVATION_SQUARED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def ODRFilterEpochStart(builder):
    builder.StartObject(6)

def Start(builder):
    ODRFilterEpochStart(builder)

def ODRFilterEpochAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    ODRFilterEpochAddEPOCH(builder, EPOCH)

def ODRFilterEpochAddFILTERED_STATE(builder, FILTERED_STATE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(FILTERED_STATE), 0)

def AddFILTERED_STATE(builder, FILTERED_STATE):
    ODRFilterEpochAddFILTERED_STATE(builder, FILTERED_STATE)

def ODRFilterEpochAddFILTERED_COVARIANCE(builder, FILTERED_COVARIANCE):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(FILTERED_COVARIANCE), 0)

def AddFILTERED_COVARIANCE(builder, FILTERED_COVARIANCE):
    ODRFilterEpochAddFILTERED_COVARIANCE(builder, FILTERED_COVARIANCE)

def ODRFilterEpochStartFILTERED_COVARIANCEVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartFILTERED_COVARIANCEVector(builder, numElems):
    return ODRFilterEpochStartFILTERED_COVARIANCEVector(builder, numElems)

def ODRFilterEpochCreateFILTERED_COVARIANCEVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateFILTERED_COVARIANCEVector(builder, data):
    ODRFilterEpochCreateFILTERED_COVARIANCEVector(builder, data)

def ODRFilterEpochAddSMOOTHED_STATE(builder, SMOOTHED_STATE):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SMOOTHED_STATE), 0)

def AddSMOOTHED_STATE(builder, SMOOTHED_STATE):
    ODRFilterEpochAddSMOOTHED_STATE(builder, SMOOTHED_STATE)

def ODRFilterEpochAddSMOOTHED_COVARIANCE(builder, SMOOTHED_COVARIANCE):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SMOOTHED_COVARIANCE), 0)

def AddSMOOTHED_COVARIANCE(builder, SMOOTHED_COVARIANCE):
    ODRFilterEpochAddSMOOTHED_COVARIANCE(builder, SMOOTHED_COVARIANCE)

def ODRFilterEpochStartSMOOTHED_COVARIANCEVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartSMOOTHED_COVARIANCEVector(builder, numElems):
    return ODRFilterEpochStartSMOOTHED_COVARIANCEVector(builder, numElems)

def ODRFilterEpochCreateSMOOTHED_COVARIANCEVector(builder, data):
    data = list(data)
    builder.StartVector(8, len(data), 8)
    for item in reversed(data):
        builder.PrependFloat64(item)
    return builder.EndVector()

def CreateSMOOTHED_COVARIANCEVector(builder, data):
    ODRFilterEpochCreateSMOOTHED_COVARIANCEVector(builder, data)

def ODRFilterEpochAddNORMALIZED_INNOVATION_SQUARED(builder, NORMALIZED_INNOVATION_SQUARED):
    builder.PrependFloat64Slot(5, NORMALIZED_INNOVATION_SQUARED, 0.0)

def AddNORMALIZED_INNOVATION_SQUARED(builder, NORMALIZED_INNOVATION_SQUARED):
    ODRFilterEpochAddNORMALIZED_INNOVATION_SQUARED(builder, NORMALIZED_INNOVATION_SQUARED)

def ODRFilterEpochEnd(builder):
    return builder.EndObject()

def End(builder):
    return ODRFilterEpochEnd(builder)

import FRMStateVector
try:
    from typing import List, Optional
except:
    pass

class ODRFilterEpochT(object):

    # ODRFilterEpochT
    def __init__(
        self,
        EPOCH = None,
        FILTERED_STATE = None,
        FILTERED_COVARIANCE = None,
        SMOOTHED_STATE = None,
        SMOOTHED_COVARIANCE = None,
        NORMALIZED_INNOVATION_SQUARED = 0.0,
    ):
        self.EPOCH = EPOCH  # type: Optional[str]
        self.FILTERED_STATE = FILTERED_STATE  # type: Optional[FRMStateVector.FRMStateVectorT]
        self.FILTERED_COVARIANCE = FILTERED_COVARIANCE  # type: Optional[List[float]]
        self.SMOOTHED_STATE = SMOOTHED_STATE  # type: Optional[FRMStateVector.FRMStateVectorT]
        self.SMOOTHED_COVARIANCE = SMOOTHED_COVARIANCE  # type: Optional[List[float]]
        self.NORMALIZED_INNOVATION_SQUARED = NORMALIZED_INNOVATION_SQUARED  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpOdrfilterEpoch = ODRFilterEpoch()
        tmpOdrfilterEpoch.Init(buf, pos)
        return cls.InitFromObj(tmpOdrfilterEpoch)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpOdrfilterEpoch):
        x = ODRFilterEpochT()
        x._UnPack(tmpOdrfilterEpoch)
        return x

    # ODRFilterEpochT
    def _UnPack(self, ODRFilterEpoch):
        if ODRFilterEpoch is None:
            return
        self.EPOCH = ODRFilterEpoch.EPOCH()
        if ODRFilterEpoch.FILTERED_STATE() is not None:
            self.FILTERED_STATE = FRMStateVector.FRMStateVectorT.InitFromObj(ODRFilterEpoch.FILTERED_STATE())
        if not ODRFilterEpoch.FILTERED_COVARIANCEIsNone():
            if np is None:
                self.FILTERED_COVARIANCE = []
                for i in range(ODRFilterEpoch.FILTERED_COVARIANCELength()):
                    self.FILTERED_COVARIANCE.append(ODRFilterEpoch.FILTERED_COVARIANCE(i))
            else:
                self.FILTERED_COVARIANCE = ODRFilterEpoch.FILTERED_COVARIANCEAsNumpy()
        if ODRFilterEpoch.SMOOTHED_STATE() is not None:
            self.SMOOTHED_STATE = FRMStateVector.FRMStateVectorT.InitFromObj(ODRFilterEpoch.SMOOTHED_STATE())
        if not ODRFilterEpoch.SMOOTHED_COVARIANCEIsNone():
            if np is None:
                self.SMOOTHED_COVARIANCE = []
                for i in range(ODRFilterEpoch.SMOOTHED_COVARIANCELength()):
                    self.SMOOTHED_COVARIANCE.append(ODRFilterEpoch.SMOOTHED_COVARIANCE(i))
            else:
                self.SMOOTHED_COVARIANCE = ODRFilterEpoch.SMOOTHED_COVARIANCEAsNumpy()
        self.NORMALIZED_INNOVATION_SQUARED = ODRFilterEpoch.NORMALIZED_INNOVATION_SQUARED()

    # ODRFilterEpochT
    def Pack(self, builder):
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        if self.FILTERED_STATE is not None:
            FILTERED_STATE = self.FILTERED_STATE.Pack(builder)
        if self.FILTERED_COVARIANCE is not None:
            if np is not None and type(self.FILTERED_COVARIANCE) is np.ndarray:
                FILTERED_COVARIANCE = builder.CreateNumpyVector(self.FILTERED_COVARIANCE)
            else:
                ODRFilterEpochStartFILTERED_COVARIANCEVector(builder, len(self.FILTERED_COVARIANCE))
                for i in reversed(range(len(self.FILTERED_COVARIANCE))):
                    builder.PrependFloat64(self.FILTERED_COVARIANCE[i])
                FILTERED_COVARIANCE = builder.EndVector()
        if self.SMOOTHED_STATE is not None:
            SMOOTHED_STATE = self.SMOOTHED_STATE.Pack(builder)
        if self.SMOOTHED_COVARIANCE is not None:
            if np is not None and type(self.SMOOTHED_COVARIANCE) is np.ndarray:
                SMOOTHED_COVARIANCE = builder.CreateNumpyVector(self.SMOOTHED_COVARIANCE)
            else:
                ODRFilterEpochStartSMOOTHED_COVARIANCEVector(builder, len(self.SMOOTHED_COVARIANCE))
                for i in reversed(range(len(self.SMOOTHED_COVARIANCE))):
                    builder.PrependFloat64(self.SMOOTHED_COVARIANCE[i])
                SMOOTHED_COVARIANCE = builder.EndVector()
        ODRFilterEpochStart(builder)
        if self.EPOCH is not None:
            ODRFilterEpochAddEPOCH(builder, EPOCH)
        if self.FILTERED_STATE is not None:
            ODRFilterEpochAddFILTERED_STATE(builder, FILTERED_STATE)
        if self.FILTERED_COVARIANCE is not None:
            ODRFilterEpochAddFILTERED_COVARIANCE(builder, FILTERED_COVARIANCE)
        if self.SMOOTHED_STATE is not None:
            ODRFilterEpochAddSMOOTHED_STATE(builder, SMOOTHED_STATE)
        if self.SMOOTHED_COVARIANCE is not None:
            ODRFilterEpochAddSMOOTHED_COVARIANCE(builder, SMOOTHED_COVARIANCE)
        ODRFilterEpochAddNORMALIZED_INNOVATION_SQUARED(builder, self.NORMALIZED_INNOVATION_SQUARED)
        ODRFilterEpoch = ODRFilterEpochEnd(builder)
        return ODRFilterEpoch
