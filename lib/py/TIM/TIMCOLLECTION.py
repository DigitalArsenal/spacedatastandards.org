# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TIMCOLLECTION(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TIMCOLLECTION()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTIMCOLLECTION(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TIMCOLLECTIONBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x49\x4D", size_prefixed=size_prefixed)

    # TIMCOLLECTION
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TIMCOLLECTION
    def RECORDS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TIM import TIM
            obj = TIM()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TIMCOLLECTION
    def RECORDSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TIMCOLLECTION
    def RECORDSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def TIMCOLLECTIONStart(builder):
    builder.StartObject(1)

def Start(builder):
    TIMCOLLECTIONStart(builder)

def TIMCOLLECTIONAddRECORDS(builder, RECORDS):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(RECORDS), 0)

def AddRECORDS(builder, RECORDS):
    TIMCOLLECTIONAddRECORDS(builder, RECORDS)

def TIMCOLLECTIONStartRECORDSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRECORDSVector(builder, numElems):
    return TIMCOLLECTIONStartRECORDSVector(builder, numElems)

def TIMCOLLECTIONEnd(builder):
    return builder.EndObject()

def End(builder):
    return TIMCOLLECTIONEnd(builder)

import TIM
try:
    from typing import List
except:
    pass

class TIMCOLLECTIONT(object):

    # TIMCOLLECTIONT
    def __init__(self):
        self.RECORDS = None  # type: List[TIM.TIMT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        TIMCOLLECTION = TIMCOLLECTION()
        TIMCOLLECTION.Init(buf, pos)
        return cls.InitFromObj(TIMCOLLECTION)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, TIMCOLLECTION):
        x = TIMCOLLECTIONT()
        x._UnPack(TIMCOLLECTION)
        return x

    # TIMCOLLECTIONT
    def _UnPack(self, TIMCOLLECTION):
        if TIMCOLLECTION is None:
            return
        if not TIMCOLLECTION.RECORDSIsNone():
            self.RECORDS = []
            for i in range(TIMCOLLECTION.RECORDSLength()):
                if TIMCOLLECTION.RECORDS(i) is None:
                    self.RECORDS.append(None)
                else:
                    tIM_ = TIM.TIMT.InitFromObj(TIMCOLLECTION.RECORDS(i))
                    self.RECORDS.append(tIM_)

    # TIMCOLLECTIONT
    def Pack(self, builder):
        if self.RECORDS is not None:
            RECORDSlist = []
            for i in range(len(self.RECORDS)):
                RECORDSlist.append(self.RECORDS[i].Pack(builder))
            TIMCOLLECTIONStartRECORDSVector(builder, len(self.RECORDS))
            for i in reversed(range(len(self.RECORDS))):
                builder.PrependUOffsetTRelative(RECORDSlist[i])
            RECORDS = builder.EndVector()
        TIMCOLLECTIONStart(builder)
        if self.RECORDS is not None:
            TIMCOLLECTIONAddRECORDS(builder, RECORDS)
        TIMCOLLECTION = TIMCOLLECTIONEnd(builder)
        return TIMCOLLECTION
