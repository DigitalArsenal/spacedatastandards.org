# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One array element and its applied complex weight.
class PAPElement(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PAPElement()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPAPElement(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PAPElementBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x41\x50", size_prefixed=size_prefixed)

    # PAPElement
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PAPElement
    def ELEMENT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PAPElement
    def POSITION_X_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPElement
    def POSITION_Y_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPElement
    def POSITION_Z_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPElement
    def WEIGHT_REAL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPElement
    def WEIGHT_IMAGINARY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPElement
    def AMPLITUDE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPElement
    def PHASE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # PAPElement
    def ELEMENT_PATTERN_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PAPElement
    def FAILURE_FLAGS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # PAPElement
    def FAILED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def PAPElementStart(builder):
    builder.StartObject(11)

def Start(builder):
    PAPElementStart(builder)

def PAPElementAddELEMENT_ID(builder, ELEMENT_ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ELEMENT_ID), 0)

def AddELEMENT_ID(builder, ELEMENT_ID):
    PAPElementAddELEMENT_ID(builder, ELEMENT_ID)

def PAPElementAddPOSITION_X_M(builder, POSITION_X_M):
    builder.PrependFloat64Slot(1, POSITION_X_M, 0.0)

def AddPOSITION_X_M(builder, POSITION_X_M):
    PAPElementAddPOSITION_X_M(builder, POSITION_X_M)

def PAPElementAddPOSITION_Y_M(builder, POSITION_Y_M):
    builder.PrependFloat64Slot(2, POSITION_Y_M, 0.0)

def AddPOSITION_Y_M(builder, POSITION_Y_M):
    PAPElementAddPOSITION_Y_M(builder, POSITION_Y_M)

def PAPElementAddPOSITION_Z_M(builder, POSITION_Z_M):
    builder.PrependFloat64Slot(3, POSITION_Z_M, 0.0)

def AddPOSITION_Z_M(builder, POSITION_Z_M):
    PAPElementAddPOSITION_Z_M(builder, POSITION_Z_M)

def PAPElementAddWEIGHT_REAL(builder, WEIGHT_REAL):
    builder.PrependFloat64Slot(4, WEIGHT_REAL, 0.0)

def AddWEIGHT_REAL(builder, WEIGHT_REAL):
    PAPElementAddWEIGHT_REAL(builder, WEIGHT_REAL)

def PAPElementAddWEIGHT_IMAGINARY(builder, WEIGHT_IMAGINARY):
    builder.PrependFloat64Slot(5, WEIGHT_IMAGINARY, 0.0)

def AddWEIGHT_IMAGINARY(builder, WEIGHT_IMAGINARY):
    PAPElementAddWEIGHT_IMAGINARY(builder, WEIGHT_IMAGINARY)

def PAPElementAddAMPLITUDE(builder, AMPLITUDE):
    builder.PrependFloat64Slot(6, AMPLITUDE, 0.0)

def AddAMPLITUDE(builder, AMPLITUDE):
    PAPElementAddAMPLITUDE(builder, AMPLITUDE)

def PAPElementAddPHASE_DEG(builder, PHASE_DEG):
    builder.PrependFloat64Slot(7, PHASE_DEG, 0.0)

def AddPHASE_DEG(builder, PHASE_DEG):
    PAPElementAddPHASE_DEG(builder, PHASE_DEG)

def PAPElementAddELEMENT_PATTERN_ID(builder, ELEMENT_PATTERN_ID):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(ELEMENT_PATTERN_ID), 0)

def AddELEMENT_PATTERN_ID(builder, ELEMENT_PATTERN_ID):
    PAPElementAddELEMENT_PATTERN_ID(builder, ELEMENT_PATTERN_ID)

def PAPElementAddFAILURE_FLAGS(builder, FAILURE_FLAGS):
    builder.PrependUint32Slot(9, FAILURE_FLAGS, 0)

def AddFAILURE_FLAGS(builder, FAILURE_FLAGS):
    PAPElementAddFAILURE_FLAGS(builder, FAILURE_FLAGS)

def PAPElementAddFAILED(builder, FAILED):
    builder.PrependBoolSlot(10, FAILED, 0)

def AddFAILED(builder, FAILED):
    PAPElementAddFAILED(builder, FAILED)

def PAPElementEnd(builder):
    return builder.EndObject()

def End(builder):
    return PAPElementEnd(builder)


class PAPElementT(object):

    # PAPElementT
    def __init__(
        self,
        ELEMENT_ID = None,
        POSITION_X_M = 0.0,
        POSITION_Y_M = 0.0,
        POSITION_Z_M = 0.0,
        WEIGHT_REAL = 0.0,
        WEIGHT_IMAGINARY = 0.0,
        AMPLITUDE = 0.0,
        PHASE_DEG = 0.0,
        ELEMENT_PATTERN_ID = None,
        FAILURE_FLAGS = 0,
        FAILED = False,
    ):
        self.ELEMENT_ID = ELEMENT_ID  # type: Optional[str]
        self.POSITION_X_M = POSITION_X_M  # type: float
        self.POSITION_Y_M = POSITION_Y_M  # type: float
        self.POSITION_Z_M = POSITION_Z_M  # type: float
        self.WEIGHT_REAL = WEIGHT_REAL  # type: float
        self.WEIGHT_IMAGINARY = WEIGHT_IMAGINARY  # type: float
        self.AMPLITUDE = AMPLITUDE  # type: float
        self.PHASE_DEG = PHASE_DEG  # type: float
        self.ELEMENT_PATTERN_ID = ELEMENT_PATTERN_ID  # type: Optional[str]
        self.FAILURE_FLAGS = FAILURE_FLAGS  # type: int
        self.FAILED = FAILED  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPapelement = PAPElement()
        tmpPapelement.Init(buf, pos)
        return cls.InitFromObj(tmpPapelement)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPapelement):
        x = PAPElementT()
        x._UnPack(tmpPapelement)
        return x

    # PAPElementT
    def _UnPack(self, PAPElement):
        if PAPElement is None:
            return
        self.ELEMENT_ID = PAPElement.ELEMENT_ID()
        self.POSITION_X_M = PAPElement.POSITION_X_M()
        self.POSITION_Y_M = PAPElement.POSITION_Y_M()
        self.POSITION_Z_M = PAPElement.POSITION_Z_M()
        self.WEIGHT_REAL = PAPElement.WEIGHT_REAL()
        self.WEIGHT_IMAGINARY = PAPElement.WEIGHT_IMAGINARY()
        self.AMPLITUDE = PAPElement.AMPLITUDE()
        self.PHASE_DEG = PAPElement.PHASE_DEG()
        self.ELEMENT_PATTERN_ID = PAPElement.ELEMENT_PATTERN_ID()
        self.FAILURE_FLAGS = PAPElement.FAILURE_FLAGS()
        self.FAILED = PAPElement.FAILED()

    # PAPElementT
    def Pack(self, builder):
        if self.ELEMENT_ID is not None:
            ELEMENT_ID = builder.CreateString(self.ELEMENT_ID)
        if self.ELEMENT_PATTERN_ID is not None:
            ELEMENT_PATTERN_ID = builder.CreateString(self.ELEMENT_PATTERN_ID)
        PAPElementStart(builder)
        if self.ELEMENT_ID is not None:
            PAPElementAddELEMENT_ID(builder, ELEMENT_ID)
        PAPElementAddPOSITION_X_M(builder, self.POSITION_X_M)
        PAPElementAddPOSITION_Y_M(builder, self.POSITION_Y_M)
        PAPElementAddPOSITION_Z_M(builder, self.POSITION_Z_M)
        PAPElementAddWEIGHT_REAL(builder, self.WEIGHT_REAL)
        PAPElementAddWEIGHT_IMAGINARY(builder, self.WEIGHT_IMAGINARY)
        PAPElementAddAMPLITUDE(builder, self.AMPLITUDE)
        PAPElementAddPHASE_DEG(builder, self.PHASE_DEG)
        if self.ELEMENT_PATTERN_ID is not None:
            PAPElementAddELEMENT_PATTERN_ID(builder, ELEMENT_PATTERN_ID)
        PAPElementAddFAILURE_FLAGS(builder, self.FAILURE_FLAGS)
        PAPElementAddFAILED(builder, self.FAILED)
        PAPElement = PAPElementEnd(builder)
        return PAPElement
