# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One obstacle interaction on one sample's path. SPARSE: entries exist only
# where an interaction was computed, so a clear path is never encoded as a
# row of zeroes.
#
# This is the join that makes an arbitrary 3D object a first-class part of
# the link budget: MATERIAL_ID names the `$RFS` record whose permittivity,
# conductivity, penetration loss and roughness produced LOSS_DB, and
# GEOMETRY_REF_ID names the exact mesh, face set, panel or analytic shape
# inside it that was hit.
class RFLObstruction(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RFLObstruction()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRFLObstruction(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def RFLObstructionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x52\x46\x4C", size_prefixed=size_prefixed)

    # RFLObstruction
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Index into the sample arrays this interaction applies to.
    # RFLObstruction
    def SAMPLE_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Producer's identifier for the obstacle instance.
    # RFLObstruction
    def OBSTACLE_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$RFS.MATERIAL_ID` of the surface that was hit. An obstruction loss with
    # no material is an unattributed number: a metal panel and a plasterboard
    # wall of the same size do entirely different things to a link.
    # RFLObstruction
    def MATERIAL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$RFS.RFSGeometryRef.GEOMETRY_REF_ID` of the exact geometry hit.
    # RFLObstruction
    def GEOMETRY_REF_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$VAM` asset identity of the obstacle mesh, when the obstacle is a
    # reviewed 3D asset.
    # RFLObstruction
    def VAM_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RFLObstruction
    def VARIANT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # `$CAT.NORAD_CAT_ID` when the obstacle is a catalogued object (an
    # occulting spacecraft, a structure on another vehicle). 0 when unbound.
    # RFLObstruction
    def NORAD_CAT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # RFLObstruction
    def INTERACTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Angle between the incident ray and the surface normal, degrees [0-90).
    # RFLObstruction
    def INCIDENCE_ANGLE_DEG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Path length travelled INSIDE the material, metres. With
    # `$RFS.ATTENUATION_DB_PER_M` this is what makes penetration loss
    # reproducible instead of a stated constant.
    # RFLObstruction
    def PATH_LENGTH_IN_MATERIAL_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Loss this interaction contributed, dB (positive).
    # RFLObstruction
    def LOSS_DB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Extra propagation delay of a reflected or scattered component, seconds.
    # RFLObstruction
    def EXCESS_DELAY_S(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Fraction of the first Fresnel zone still clear [0-1]. 1 means the zone is
    # untouched.
    # RFLObstruction
    def FRESNEL_CLEARANCE_FRACTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Distance from the transmitter to the interaction point, metres.
    # RFLObstruction
    def DISTANCE_FROM_TRANSMITTER_M(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def RFLObstructionStart(builder):
    builder.StartObject(14)

def Start(builder):
    RFLObstructionStart(builder)

def RFLObstructionAddSAMPLE_INDEX(builder, SAMPLE_INDEX):
    builder.PrependUint32Slot(0, SAMPLE_INDEX, 0)

def AddSAMPLE_INDEX(builder, SAMPLE_INDEX):
    RFLObstructionAddSAMPLE_INDEX(builder, SAMPLE_INDEX)

def RFLObstructionAddOBSTACLE_ID(builder, OBSTACLE_ID):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(OBSTACLE_ID), 0)

def AddOBSTACLE_ID(builder, OBSTACLE_ID):
    RFLObstructionAddOBSTACLE_ID(builder, OBSTACLE_ID)

def RFLObstructionAddMATERIAL_ID(builder, MATERIAL_ID):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(MATERIAL_ID), 0)

def AddMATERIAL_ID(builder, MATERIAL_ID):
    RFLObstructionAddMATERIAL_ID(builder, MATERIAL_ID)

def RFLObstructionAddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(GEOMETRY_REF_ID), 0)

def AddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID):
    RFLObstructionAddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID)

def RFLObstructionAddVAM_ID(builder, VAM_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(VAM_ID), 0)

def AddVAM_ID(builder, VAM_ID):
    RFLObstructionAddVAM_ID(builder, VAM_ID)

def RFLObstructionAddVARIANT_ID(builder, VARIANT_ID):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(VARIANT_ID), 0)

def AddVARIANT_ID(builder, VARIANT_ID):
    RFLObstructionAddVARIANT_ID(builder, VARIANT_ID)

def RFLObstructionAddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    builder.PrependUint32Slot(6, NORAD_CAT_ID, 0)

def AddNORAD_CAT_ID(builder, NORAD_CAT_ID):
    RFLObstructionAddNORAD_CAT_ID(builder, NORAD_CAT_ID)

def RFLObstructionAddINTERACTION(builder, INTERACTION):
    builder.PrependInt8Slot(7, INTERACTION, 0)

def AddINTERACTION(builder, INTERACTION):
    RFLObstructionAddINTERACTION(builder, INTERACTION)

def RFLObstructionAddINCIDENCE_ANGLE_DEG(builder, INCIDENCE_ANGLE_DEG):
    builder.PrependFloat64Slot(8, INCIDENCE_ANGLE_DEG, 0.0)

def AddINCIDENCE_ANGLE_DEG(builder, INCIDENCE_ANGLE_DEG):
    RFLObstructionAddINCIDENCE_ANGLE_DEG(builder, INCIDENCE_ANGLE_DEG)

def RFLObstructionAddPATH_LENGTH_IN_MATERIAL_M(builder, PATH_LENGTH_IN_MATERIAL_M):
    builder.PrependFloat64Slot(9, PATH_LENGTH_IN_MATERIAL_M, 0.0)

def AddPATH_LENGTH_IN_MATERIAL_M(builder, PATH_LENGTH_IN_MATERIAL_M):
    RFLObstructionAddPATH_LENGTH_IN_MATERIAL_M(builder, PATH_LENGTH_IN_MATERIAL_M)

def RFLObstructionAddLOSS_DB(builder, LOSS_DB):
    builder.PrependFloat64Slot(10, LOSS_DB, 0.0)

def AddLOSS_DB(builder, LOSS_DB):
    RFLObstructionAddLOSS_DB(builder, LOSS_DB)

def RFLObstructionAddEXCESS_DELAY_S(builder, EXCESS_DELAY_S):
    builder.PrependFloat64Slot(11, EXCESS_DELAY_S, 0.0)

def AddEXCESS_DELAY_S(builder, EXCESS_DELAY_S):
    RFLObstructionAddEXCESS_DELAY_S(builder, EXCESS_DELAY_S)

def RFLObstructionAddFRESNEL_CLEARANCE_FRACTION(builder, FRESNEL_CLEARANCE_FRACTION):
    builder.PrependFloat64Slot(12, FRESNEL_CLEARANCE_FRACTION, 0.0)

def AddFRESNEL_CLEARANCE_FRACTION(builder, FRESNEL_CLEARANCE_FRACTION):
    RFLObstructionAddFRESNEL_CLEARANCE_FRACTION(builder, FRESNEL_CLEARANCE_FRACTION)

def RFLObstructionAddDISTANCE_FROM_TRANSMITTER_M(builder, DISTANCE_FROM_TRANSMITTER_M):
    builder.PrependFloat64Slot(13, DISTANCE_FROM_TRANSMITTER_M, 0.0)

def AddDISTANCE_FROM_TRANSMITTER_M(builder, DISTANCE_FROM_TRANSMITTER_M):
    RFLObstructionAddDISTANCE_FROM_TRANSMITTER_M(builder, DISTANCE_FROM_TRANSMITTER_M)

def RFLObstructionEnd(builder):
    return builder.EndObject()

def End(builder):
    return RFLObstructionEnd(builder)


class RFLObstructionT(object):

    # RFLObstructionT
    def __init__(
        self,
        SAMPLE_INDEX = 0,
        OBSTACLE_ID = None,
        MATERIAL_ID = None,
        GEOMETRY_REF_ID = None,
        VAM_ID = None,
        VARIANT_ID = None,
        NORAD_CAT_ID = 0,
        INTERACTION = 0,
        INCIDENCE_ANGLE_DEG = 0.0,
        PATH_LENGTH_IN_MATERIAL_M = 0.0,
        LOSS_DB = 0.0,
        EXCESS_DELAY_S = 0.0,
        FRESNEL_CLEARANCE_FRACTION = 0.0,
        DISTANCE_FROM_TRANSMITTER_M = 0.0,
    ):
        self.SAMPLE_INDEX = SAMPLE_INDEX  # type: int
        self.OBSTACLE_ID = OBSTACLE_ID  # type: Optional[str]
        self.MATERIAL_ID = MATERIAL_ID  # type: Optional[str]
        self.GEOMETRY_REF_ID = GEOMETRY_REF_ID  # type: Optional[str]
        self.VAM_ID = VAM_ID  # type: Optional[str]
        self.VARIANT_ID = VARIANT_ID  # type: Optional[str]
        self.NORAD_CAT_ID = NORAD_CAT_ID  # type: int
        self.INTERACTION = INTERACTION  # type: int
        self.INCIDENCE_ANGLE_DEG = INCIDENCE_ANGLE_DEG  # type: float
        self.PATH_LENGTH_IN_MATERIAL_M = PATH_LENGTH_IN_MATERIAL_M  # type: float
        self.LOSS_DB = LOSS_DB  # type: float
        self.EXCESS_DELAY_S = EXCESS_DELAY_S  # type: float
        self.FRESNEL_CLEARANCE_FRACTION = FRESNEL_CLEARANCE_FRACTION  # type: float
        self.DISTANCE_FROM_TRANSMITTER_M = DISTANCE_FROM_TRANSMITTER_M  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpRflobstruction = RFLObstruction()
        tmpRflobstruction.Init(buf, pos)
        return cls.InitFromObj(tmpRflobstruction)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpRflobstruction):
        x = RFLObstructionT()
        x._UnPack(tmpRflobstruction)
        return x

    # RFLObstructionT
    def _UnPack(self, RFLObstruction):
        if RFLObstruction is None:
            return
        self.SAMPLE_INDEX = RFLObstruction.SAMPLE_INDEX()
        self.OBSTACLE_ID = RFLObstruction.OBSTACLE_ID()
        self.MATERIAL_ID = RFLObstruction.MATERIAL_ID()
        self.GEOMETRY_REF_ID = RFLObstruction.GEOMETRY_REF_ID()
        self.VAM_ID = RFLObstruction.VAM_ID()
        self.VARIANT_ID = RFLObstruction.VARIANT_ID()
        self.NORAD_CAT_ID = RFLObstruction.NORAD_CAT_ID()
        self.INTERACTION = RFLObstruction.INTERACTION()
        self.INCIDENCE_ANGLE_DEG = RFLObstruction.INCIDENCE_ANGLE_DEG()
        self.PATH_LENGTH_IN_MATERIAL_M = RFLObstruction.PATH_LENGTH_IN_MATERIAL_M()
        self.LOSS_DB = RFLObstruction.LOSS_DB()
        self.EXCESS_DELAY_S = RFLObstruction.EXCESS_DELAY_S()
        self.FRESNEL_CLEARANCE_FRACTION = RFLObstruction.FRESNEL_CLEARANCE_FRACTION()
        self.DISTANCE_FROM_TRANSMITTER_M = RFLObstruction.DISTANCE_FROM_TRANSMITTER_M()

    # RFLObstructionT
    def Pack(self, builder):
        if self.OBSTACLE_ID is not None:
            OBSTACLE_ID = builder.CreateString(self.OBSTACLE_ID)
        if self.MATERIAL_ID is not None:
            MATERIAL_ID = builder.CreateString(self.MATERIAL_ID)
        if self.GEOMETRY_REF_ID is not None:
            GEOMETRY_REF_ID = builder.CreateString(self.GEOMETRY_REF_ID)
        if self.VAM_ID is not None:
            VAM_ID = builder.CreateString(self.VAM_ID)
        if self.VARIANT_ID is not None:
            VARIANT_ID = builder.CreateString(self.VARIANT_ID)
        RFLObstructionStart(builder)
        RFLObstructionAddSAMPLE_INDEX(builder, self.SAMPLE_INDEX)
        if self.OBSTACLE_ID is not None:
            RFLObstructionAddOBSTACLE_ID(builder, OBSTACLE_ID)
        if self.MATERIAL_ID is not None:
            RFLObstructionAddMATERIAL_ID(builder, MATERIAL_ID)
        if self.GEOMETRY_REF_ID is not None:
            RFLObstructionAddGEOMETRY_REF_ID(builder, GEOMETRY_REF_ID)
        if self.VAM_ID is not None:
            RFLObstructionAddVAM_ID(builder, VAM_ID)
        if self.VARIANT_ID is not None:
            RFLObstructionAddVARIANT_ID(builder, VARIANT_ID)
        RFLObstructionAddNORAD_CAT_ID(builder, self.NORAD_CAT_ID)
        RFLObstructionAddINTERACTION(builder, self.INTERACTION)
        RFLObstructionAddINCIDENCE_ANGLE_DEG(builder, self.INCIDENCE_ANGLE_DEG)
        RFLObstructionAddPATH_LENGTH_IN_MATERIAL_M(builder, self.PATH_LENGTH_IN_MATERIAL_M)
        RFLObstructionAddLOSS_DB(builder, self.LOSS_DB)
        RFLObstructionAddEXCESS_DELAY_S(builder, self.EXCESS_DELAY_S)
        RFLObstructionAddFRESNEL_CLEARANCE_FRACTION(builder, self.FRESNEL_CLEARANCE_FRACTION)
        RFLObstructionAddDISTANCE_FROM_TRANSMITTER_M(builder, self.DISTANCE_FROM_TRANSMITTER_M)
        RFLObstruction = RFLObstructionEnd(builder)
        return RFLObstruction
