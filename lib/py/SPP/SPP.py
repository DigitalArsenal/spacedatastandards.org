# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Space Packet Protocol (CCSDS 133.0-B-1)
class SPP(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SPP()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSPP(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def SPPBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x53\x50\x50", size_prefixed=size_prefixed)

    # SPP
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Packet version number
    # SPP
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Packet type (TM or TC)
    # SPP
    def PACKET_TYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Secondary header flag
    # SPP
    def SEC_HDR_FLAG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Application Process Identifier
    # SPP
    def APID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Sequence flags (00=continuation, 01=first, 10=last, 11=standalone)
    # SPP
    def SEQUENCE_FLAGS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Sequence count
    # SPP
    def SEQUENCE_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Data length minus 1
    # SPP
    def DATA_LENGTH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Packet data zone
    # SPP
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SPP
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SPP
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SPP
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

def SPPStart(builder):
    builder.StartObject(8)

def Start(builder):
    SPPStart(builder)

def SPPAddVERSION(builder, VERSION):
    builder.PrependUint8Slot(0, VERSION, 0)

def AddVERSION(builder, VERSION):
    SPPAddVERSION(builder, VERSION)

def SPPAddPACKET_TYPE(builder, PACKET_TYPE):
    builder.PrependInt8Slot(1, PACKET_TYPE, 0)

def AddPACKET_TYPE(builder, PACKET_TYPE):
    SPPAddPACKET_TYPE(builder, PACKET_TYPE)

def SPPAddSEC_HDR_FLAG(builder, SEC_HDR_FLAG):
    builder.PrependBoolSlot(2, SEC_HDR_FLAG, 0)

def AddSEC_HDR_FLAG(builder, SEC_HDR_FLAG):
    SPPAddSEC_HDR_FLAG(builder, SEC_HDR_FLAG)

def SPPAddAPID(builder, APID):
    builder.PrependUint16Slot(3, APID, 0)

def AddAPID(builder, APID):
    SPPAddAPID(builder, APID)

def SPPAddSEQUENCE_FLAGS(builder, SEQUENCE_FLAGS):
    builder.PrependUint8Slot(4, SEQUENCE_FLAGS, 0)

def AddSEQUENCE_FLAGS(builder, SEQUENCE_FLAGS):
    SPPAddSEQUENCE_FLAGS(builder, SEQUENCE_FLAGS)

def SPPAddSEQUENCE_COUNT(builder, SEQUENCE_COUNT):
    builder.PrependUint16Slot(5, SEQUENCE_COUNT, 0)

def AddSEQUENCE_COUNT(builder, SEQUENCE_COUNT):
    SPPAddSEQUENCE_COUNT(builder, SEQUENCE_COUNT)

def SPPAddDATA_LENGTH(builder, DATA_LENGTH):
    builder.PrependUint16Slot(6, DATA_LENGTH, 0)

def AddDATA_LENGTH(builder, DATA_LENGTH):
    SPPAddDATA_LENGTH(builder, DATA_LENGTH)

def SPPAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    SPPAddDATA(builder, DATA)

def SPPStartDATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDATAVector(builder, numElems):
    return SPPStartDATAVector(builder, numElems)

def SPPEnd(builder):
    return builder.EndObject()

def End(builder):
    return SPPEnd(builder)

try:
    from typing import List
except:
    pass

class SPPT(object):

    # SPPT
    def __init__(self):
        self.VERSION = 0  # type: int
        self.PACKET_TYPE = 0  # type: int
        self.SEC_HDR_FLAG = False  # type: bool
        self.APID = 0  # type: int
        self.SEQUENCE_FLAGS = 0  # type: int
        self.SEQUENCE_COUNT = 0  # type: int
        self.DATA_LENGTH = 0  # type: int
        self.DATA = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        SPP = SPP()
        SPP.Init(buf, pos)
        return cls.InitFromObj(SPP)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, SPP):
        x = SPPT()
        x._UnPack(SPP)
        return x

    # SPPT
    def _UnPack(self, SPP):
        if SPP is None:
            return
        self.VERSION = SPP.VERSION()
        self.PACKET_TYPE = SPP.PACKET_TYPE()
        self.SEC_HDR_FLAG = SPP.SEC_HDR_FLAG()
        self.APID = SPP.APID()
        self.SEQUENCE_FLAGS = SPP.SEQUENCE_FLAGS()
        self.SEQUENCE_COUNT = SPP.SEQUENCE_COUNT()
        self.DATA_LENGTH = SPP.DATA_LENGTH()
        if not SPP.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(SPP.DATALength()):
                    self.DATA.append(SPP.DATA(i))
            else:
                self.DATA = SPP.DATAAsNumpy()

    # SPPT
    def Pack(self, builder):
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                SPPStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependUint8(self.DATA[i])
                DATA = builder.EndVector()
        SPPStart(builder)
        SPPAddVERSION(builder, self.VERSION)
        SPPAddPACKET_TYPE(builder, self.PACKET_TYPE)
        SPPAddSEC_HDR_FLAG(builder, self.SEC_HDR_FLAG)
        SPPAddAPID(builder, self.APID)
        SPPAddSEQUENCE_FLAGS(builder, self.SEQUENCE_FLAGS)
        SPPAddSEQUENCE_COUNT(builder, self.SEQUENCE_COUNT)
        SPPAddDATA_LENGTH(builder, self.DATA_LENGTH)
        if self.DATA is not None:
            SPPAddDATA(builder, DATA)
        SPP = SPPEnd(builder)
        return SPP
