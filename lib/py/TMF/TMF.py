# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Telemetry Transfer Frame (CCSDS 132.0-B-2)
class TMF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TMF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTMF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TMFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x4D\x46", size_prefixed=size_prefixed)

    # TMF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Transfer frame version
    # TMF
    def VERSION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Spacecraft identifier
    # TMF
    def SPACECRAFT_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Virtual channel identifier
    # TMF
    def VIRTUAL_CHANNEL_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Operational control field flag
    # TMF
    def OCF_FLAG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Master channel frame count
    # TMF
    def MASTER_FRAME_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Virtual channel frame count
    # TMF
    def VIRTUAL_FRAME_COUNT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Secondary header flag
    # TMF
    def SEC_HDR_FLAG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Synchronization flag
    # TMF
    def SYNCH_FLAG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Packet order flag
    # TMF
    def PACKET_ORDER_FLAG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Segment length identifier
    # TMF
    def SEGMENT_LENGTH_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # First header pointer
    # TMF
    def FIRST_HDR_POINTER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # Data field
    # TMF
    def DATA(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TMF
    def DATAAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TMF
    def DATALength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TMF
    def DATAIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # Operational control field
    # TMF
    def OCF(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TMF
    def OCFAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # TMF
    def OCFLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TMF
    def OCFIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

    # Frame error control field
    # TMF
    def FECF(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

def TMFStart(builder):
    builder.StartObject(14)

def Start(builder):
    TMFStart(builder)

def TMFAddVERSION(builder, VERSION):
    builder.PrependUint8Slot(0, VERSION, 0)

def AddVERSION(builder, VERSION):
    TMFAddVERSION(builder, VERSION)

def TMFAddSPACECRAFT_ID(builder, SPACECRAFT_ID):
    builder.PrependUint16Slot(1, SPACECRAFT_ID, 0)

def AddSPACECRAFT_ID(builder, SPACECRAFT_ID):
    TMFAddSPACECRAFT_ID(builder, SPACECRAFT_ID)

def TMFAddVIRTUAL_CHANNEL_ID(builder, VIRTUAL_CHANNEL_ID):
    builder.PrependUint8Slot(2, VIRTUAL_CHANNEL_ID, 0)

def AddVIRTUAL_CHANNEL_ID(builder, VIRTUAL_CHANNEL_ID):
    TMFAddVIRTUAL_CHANNEL_ID(builder, VIRTUAL_CHANNEL_ID)

def TMFAddOCF_FLAG(builder, OCF_FLAG):
    builder.PrependBoolSlot(3, OCF_FLAG, 0)

def AddOCF_FLAG(builder, OCF_FLAG):
    TMFAddOCF_FLAG(builder, OCF_FLAG)

def TMFAddMASTER_FRAME_COUNT(builder, MASTER_FRAME_COUNT):
    builder.PrependUint8Slot(4, MASTER_FRAME_COUNT, 0)

def AddMASTER_FRAME_COUNT(builder, MASTER_FRAME_COUNT):
    TMFAddMASTER_FRAME_COUNT(builder, MASTER_FRAME_COUNT)

def TMFAddVIRTUAL_FRAME_COUNT(builder, VIRTUAL_FRAME_COUNT):
    builder.PrependUint8Slot(5, VIRTUAL_FRAME_COUNT, 0)

def AddVIRTUAL_FRAME_COUNT(builder, VIRTUAL_FRAME_COUNT):
    TMFAddVIRTUAL_FRAME_COUNT(builder, VIRTUAL_FRAME_COUNT)

def TMFAddSEC_HDR_FLAG(builder, SEC_HDR_FLAG):
    builder.PrependBoolSlot(6, SEC_HDR_FLAG, 0)

def AddSEC_HDR_FLAG(builder, SEC_HDR_FLAG):
    TMFAddSEC_HDR_FLAG(builder, SEC_HDR_FLAG)

def TMFAddSYNCH_FLAG(builder, SYNCH_FLAG):
    builder.PrependBoolSlot(7, SYNCH_FLAG, 0)

def AddSYNCH_FLAG(builder, SYNCH_FLAG):
    TMFAddSYNCH_FLAG(builder, SYNCH_FLAG)

def TMFAddPACKET_ORDER_FLAG(builder, PACKET_ORDER_FLAG):
    builder.PrependBoolSlot(8, PACKET_ORDER_FLAG, 0)

def AddPACKET_ORDER_FLAG(builder, PACKET_ORDER_FLAG):
    TMFAddPACKET_ORDER_FLAG(builder, PACKET_ORDER_FLAG)

def TMFAddSEGMENT_LENGTH_ID(builder, SEGMENT_LENGTH_ID):
    builder.PrependUint8Slot(9, SEGMENT_LENGTH_ID, 0)

def AddSEGMENT_LENGTH_ID(builder, SEGMENT_LENGTH_ID):
    TMFAddSEGMENT_LENGTH_ID(builder, SEGMENT_LENGTH_ID)

def TMFAddFIRST_HDR_POINTER(builder, FIRST_HDR_POINTER):
    builder.PrependUint16Slot(10, FIRST_HDR_POINTER, 0)

def AddFIRST_HDR_POINTER(builder, FIRST_HDR_POINTER):
    TMFAddFIRST_HDR_POINTER(builder, FIRST_HDR_POINTER)

def TMFAddDATA(builder, DATA):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(DATA), 0)

def AddDATA(builder, DATA):
    TMFAddDATA(builder, DATA)

def TMFStartDATAVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDATAVector(builder, numElems):
    return TMFStartDATAVector(builder, numElems)

def TMFAddOCF(builder, OCF):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(OCF), 0)

def AddOCF(builder, OCF):
    TMFAddOCF(builder, OCF)

def TMFStartOCFVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartOCFVector(builder, numElems):
    return TMFStartOCFVector(builder, numElems)

def TMFAddFECF(builder, FECF):
    builder.PrependUint16Slot(13, FECF, 0)

def AddFECF(builder, FECF):
    TMFAddFECF(builder, FECF)

def TMFEnd(builder):
    return builder.EndObject()

def End(builder):
    return TMFEnd(builder)

try:
    from typing import List
except:
    pass

class TMFT(object):

    # TMFT
    def __init__(self):
        self.VERSION = 0  # type: int
        self.SPACECRAFT_ID = 0  # type: int
        self.VIRTUAL_CHANNEL_ID = 0  # type: int
        self.OCF_FLAG = False  # type: bool
        self.MASTER_FRAME_COUNT = 0  # type: int
        self.VIRTUAL_FRAME_COUNT = 0  # type: int
        self.SEC_HDR_FLAG = False  # type: bool
        self.SYNCH_FLAG = False  # type: bool
        self.PACKET_ORDER_FLAG = False  # type: bool
        self.SEGMENT_LENGTH_ID = 0  # type: int
        self.FIRST_HDR_POINTER = 0  # type: int
        self.DATA = None  # type: List[int]
        self.OCF = None  # type: List[int]
        self.FECF = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        TMF = TMF()
        TMF.Init(buf, pos)
        return cls.InitFromObj(TMF)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, TMF):
        x = TMFT()
        x._UnPack(TMF)
        return x

    # TMFT
    def _UnPack(self, TMF):
        if TMF is None:
            return
        self.VERSION = TMF.VERSION()
        self.SPACECRAFT_ID = TMF.SPACECRAFT_ID()
        self.VIRTUAL_CHANNEL_ID = TMF.VIRTUAL_CHANNEL_ID()
        self.OCF_FLAG = TMF.OCF_FLAG()
        self.MASTER_FRAME_COUNT = TMF.MASTER_FRAME_COUNT()
        self.VIRTUAL_FRAME_COUNT = TMF.VIRTUAL_FRAME_COUNT()
        self.SEC_HDR_FLAG = TMF.SEC_HDR_FLAG()
        self.SYNCH_FLAG = TMF.SYNCH_FLAG()
        self.PACKET_ORDER_FLAG = TMF.PACKET_ORDER_FLAG()
        self.SEGMENT_LENGTH_ID = TMF.SEGMENT_LENGTH_ID()
        self.FIRST_HDR_POINTER = TMF.FIRST_HDR_POINTER()
        if not TMF.DATAIsNone():
            if np is None:
                self.DATA = []
                for i in range(TMF.DATALength()):
                    self.DATA.append(TMF.DATA(i))
            else:
                self.DATA = TMF.DATAAsNumpy()
        if not TMF.OCFIsNone():
            if np is None:
                self.OCF = []
                for i in range(TMF.OCFLength()):
                    self.OCF.append(TMF.OCF(i))
            else:
                self.OCF = TMF.OCFAsNumpy()
        self.FECF = TMF.FECF()

    # TMFT
    def Pack(self, builder):
        if self.DATA is not None:
            if np is not None and type(self.DATA) is np.ndarray:
                DATA = builder.CreateNumpyVector(self.DATA)
            else:
                TMFStartDATAVector(builder, len(self.DATA))
                for i in reversed(range(len(self.DATA))):
                    builder.PrependUint8(self.DATA[i])
                DATA = builder.EndVector()
        if self.OCF is not None:
            if np is not None and type(self.OCF) is np.ndarray:
                OCF = builder.CreateNumpyVector(self.OCF)
            else:
                TMFStartOCFVector(builder, len(self.OCF))
                for i in reversed(range(len(self.OCF))):
                    builder.PrependUint8(self.OCF[i])
                OCF = builder.EndVector()
        TMFStart(builder)
        TMFAddVERSION(builder, self.VERSION)
        TMFAddSPACECRAFT_ID(builder, self.SPACECRAFT_ID)
        TMFAddVIRTUAL_CHANNEL_ID(builder, self.VIRTUAL_CHANNEL_ID)
        TMFAddOCF_FLAG(builder, self.OCF_FLAG)
        TMFAddMASTER_FRAME_COUNT(builder, self.MASTER_FRAME_COUNT)
        TMFAddVIRTUAL_FRAME_COUNT(builder, self.VIRTUAL_FRAME_COUNT)
        TMFAddSEC_HDR_FLAG(builder, self.SEC_HDR_FLAG)
        TMFAddSYNCH_FLAG(builder, self.SYNCH_FLAG)
        TMFAddPACKET_ORDER_FLAG(builder, self.PACKET_ORDER_FLAG)
        TMFAddSEGMENT_LENGTH_ID(builder, self.SEGMENT_LENGTH_ID)
        TMFAddFIRST_HDR_POINTER(builder, self.FIRST_HDR_POINTER)
        if self.DATA is not None:
            TMFAddDATA(builder, DATA)
        if self.OCF is not None:
            TMFAddOCF(builder, OCF)
        TMFAddFECF(builder, self.FECF)
        TMF = TMFEnd(builder)
        return TMF
