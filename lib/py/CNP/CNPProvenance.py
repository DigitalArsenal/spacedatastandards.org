# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Where one metric came from and under what terms it may be republished.
class CNPProvenance(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CNPProvenance()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCNPProvenance(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CNPProvenanceBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x43\x4E\x50", size_prefixed=size_prefixed)

    # CNPProvenance
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Name of the organization or programme publishing the underlying
    # measurements, carried verbatim as that publisher states it.
    # CNPProvenance
    def SOURCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # URL of the dataset, endpoint or landing page.
    # CNPProvenance
    def SOURCE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Dataset, table or API path queried, verbatim, e.g. a fully qualified
    # warehouse table name or a REST route.
    # CNPProvenance
    def SOURCE_DATASET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # The exact query or request that produced these numbers, so the
    # aggregate can be replayed rather than trusted.
    # CNPProvenance
    def SOURCE_QUERY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Source's own identifier for the returned batch, when it issues one.
    # CNPProvenance
    def SOURCE_RECORD_ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SHA-256 of the raw source bytes this aggregate was computed from,
    # lowercase hex.
    # CNPProvenance
    def SOURCE_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ISO 8601 UTC time the source was queried.
    # CNPProvenance
    def RETRIEVED_AT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # How the underlying observations were produced.
    # CNPProvenance
    def METHOD(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 8

    # Measurement server, vantage point or probe set, when the source names
    # one. A throughput number without a vantage is not comparable to one
    # with a different vantage.
    # CNPProvenance
    def MEASUREMENT_SERVER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SPDX identifier when the source states one, otherwise the licence name
    # verbatim. EMPTY MEANS UNKNOWN TERMS — never public domain, and never a
    # grant to republish.
    # CNPProvenance
    def LICENSE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # URL of the licence text.
    # CNPProvenance
    def LICENSE_URL(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Attribution string the licence requires this record and every derived
    # product to carry downstream.
    # CNPProvenance
    def ATTRIBUTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # True when the licence forbids commercial use (e.g. CC BY-NC 4.0). A
    # consumer building a paid surface MUST honour this flag; it is carried
    # per-source because one $CNP may mix a CC0 lane with a CC BY-NC
    # cross-check.
    # CNPProvenance
    def NON_COMMERCIAL_ONLY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def CNPProvenanceStart(builder):
    builder.StartObject(13)

def Start(builder):
    CNPProvenanceStart(builder)

def CNPProvenanceAddSOURCE(builder, SOURCE):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE), 0)

def AddSOURCE(builder, SOURCE):
    CNPProvenanceAddSOURCE(builder, SOURCE)

def CNPProvenanceAddSOURCE_URL(builder, SOURCE_URL):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_URL), 0)

def AddSOURCE_URL(builder, SOURCE_URL):
    CNPProvenanceAddSOURCE_URL(builder, SOURCE_URL)

def CNPProvenanceAddSOURCE_DATASET(builder, SOURCE_DATASET):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_DATASET), 0)

def AddSOURCE_DATASET(builder, SOURCE_DATASET):
    CNPProvenanceAddSOURCE_DATASET(builder, SOURCE_DATASET)

def CNPProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_QUERY), 0)

def AddSOURCE_QUERY(builder, SOURCE_QUERY):
    CNPProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY)

def CNPProvenanceAddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_RECORD_ID), 0)

def AddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID):
    CNPProvenanceAddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID)

def CNPProvenanceAddSOURCE_SHA256(builder, SOURCE_SHA256):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCE_SHA256), 0)

def AddSOURCE_SHA256(builder, SOURCE_SHA256):
    CNPProvenanceAddSOURCE_SHA256(builder, SOURCE_SHA256)

def CNPProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(RETRIEVED_AT), 0)

def AddRETRIEVED_AT(builder, RETRIEVED_AT):
    CNPProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT)

def CNPProvenanceAddMETHOD(builder, METHOD):
    builder.PrependInt8Slot(7, METHOD, 8)

def AddMETHOD(builder, METHOD):
    CNPProvenanceAddMETHOD(builder, METHOD)

def CNPProvenanceAddMEASUREMENT_SERVER(builder, MEASUREMENT_SERVER):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(MEASUREMENT_SERVER), 0)

def AddMEASUREMENT_SERVER(builder, MEASUREMENT_SERVER):
    CNPProvenanceAddMEASUREMENT_SERVER(builder, MEASUREMENT_SERVER)

def CNPProvenanceAddLICENSE(builder, LICENSE):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE), 0)

def AddLICENSE(builder, LICENSE):
    CNPProvenanceAddLICENSE(builder, LICENSE)

def CNPProvenanceAddLICENSE_URL(builder, LICENSE_URL):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(LICENSE_URL), 0)

def AddLICENSE_URL(builder, LICENSE_URL):
    CNPProvenanceAddLICENSE_URL(builder, LICENSE_URL)

def CNPProvenanceAddATTRIBUTION(builder, ATTRIBUTION):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(ATTRIBUTION), 0)

def AddATTRIBUTION(builder, ATTRIBUTION):
    CNPProvenanceAddATTRIBUTION(builder, ATTRIBUTION)

def CNPProvenanceAddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY):
    builder.PrependBoolSlot(12, NON_COMMERCIAL_ONLY, 0)

def AddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY):
    CNPProvenanceAddNON_COMMERCIAL_ONLY(builder, NON_COMMERCIAL_ONLY)

def CNPProvenanceEnd(builder):
    return builder.EndObject()

def End(builder):
    return CNPProvenanceEnd(builder)


class CNPProvenanceT(object):

    # CNPProvenanceT
    def __init__(
        self,
        SOURCE = None,
        SOURCE_URL = None,
        SOURCE_DATASET = None,
        SOURCE_QUERY = None,
        SOURCE_RECORD_ID = None,
        SOURCE_SHA256 = None,
        RETRIEVED_AT = None,
        METHOD = 8,
        MEASUREMENT_SERVER = None,
        LICENSE = None,
        LICENSE_URL = None,
        ATTRIBUTION = None,
        NON_COMMERCIAL_ONLY = False,
    ):
        self.SOURCE = SOURCE  # type: Optional[str]
        self.SOURCE_URL = SOURCE_URL  # type: Optional[str]
        self.SOURCE_DATASET = SOURCE_DATASET  # type: Optional[str]
        self.SOURCE_QUERY = SOURCE_QUERY  # type: Optional[str]
        self.SOURCE_RECORD_ID = SOURCE_RECORD_ID  # type: Optional[str]
        self.SOURCE_SHA256 = SOURCE_SHA256  # type: Optional[str]
        self.RETRIEVED_AT = RETRIEVED_AT  # type: Optional[str]
        self.METHOD = METHOD  # type: int
        self.MEASUREMENT_SERVER = MEASUREMENT_SERVER  # type: Optional[str]
        self.LICENSE = LICENSE  # type: Optional[str]
        self.LICENSE_URL = LICENSE_URL  # type: Optional[str]
        self.ATTRIBUTION = ATTRIBUTION  # type: Optional[str]
        self.NON_COMMERCIAL_ONLY = NON_COMMERCIAL_ONLY  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpCnpprovenance = CNPProvenance()
        tmpCnpprovenance.Init(buf, pos)
        return cls.InitFromObj(tmpCnpprovenance)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpCnpprovenance):
        x = CNPProvenanceT()
        x._UnPack(tmpCnpprovenance)
        return x

    # CNPProvenanceT
    def _UnPack(self, CNPProvenance):
        if CNPProvenance is None:
            return
        self.SOURCE = CNPProvenance.SOURCE()
        self.SOURCE_URL = CNPProvenance.SOURCE_URL()
        self.SOURCE_DATASET = CNPProvenance.SOURCE_DATASET()
        self.SOURCE_QUERY = CNPProvenance.SOURCE_QUERY()
        self.SOURCE_RECORD_ID = CNPProvenance.SOURCE_RECORD_ID()
        self.SOURCE_SHA256 = CNPProvenance.SOURCE_SHA256()
        self.RETRIEVED_AT = CNPProvenance.RETRIEVED_AT()
        self.METHOD = CNPProvenance.METHOD()
        self.MEASUREMENT_SERVER = CNPProvenance.MEASUREMENT_SERVER()
        self.LICENSE = CNPProvenance.LICENSE()
        self.LICENSE_URL = CNPProvenance.LICENSE_URL()
        self.ATTRIBUTION = CNPProvenance.ATTRIBUTION()
        self.NON_COMMERCIAL_ONLY = CNPProvenance.NON_COMMERCIAL_ONLY()

    # CNPProvenanceT
    def Pack(self, builder):
        if self.SOURCE is not None:
            SOURCE = builder.CreateString(self.SOURCE)
        if self.SOURCE_URL is not None:
            SOURCE_URL = builder.CreateString(self.SOURCE_URL)
        if self.SOURCE_DATASET is not None:
            SOURCE_DATASET = builder.CreateString(self.SOURCE_DATASET)
        if self.SOURCE_QUERY is not None:
            SOURCE_QUERY = builder.CreateString(self.SOURCE_QUERY)
        if self.SOURCE_RECORD_ID is not None:
            SOURCE_RECORD_ID = builder.CreateString(self.SOURCE_RECORD_ID)
        if self.SOURCE_SHA256 is not None:
            SOURCE_SHA256 = builder.CreateString(self.SOURCE_SHA256)
        if self.RETRIEVED_AT is not None:
            RETRIEVED_AT = builder.CreateString(self.RETRIEVED_AT)
        if self.MEASUREMENT_SERVER is not None:
            MEASUREMENT_SERVER = builder.CreateString(self.MEASUREMENT_SERVER)
        if self.LICENSE is not None:
            LICENSE = builder.CreateString(self.LICENSE)
        if self.LICENSE_URL is not None:
            LICENSE_URL = builder.CreateString(self.LICENSE_URL)
        if self.ATTRIBUTION is not None:
            ATTRIBUTION = builder.CreateString(self.ATTRIBUTION)
        CNPProvenanceStart(builder)
        if self.SOURCE is not None:
            CNPProvenanceAddSOURCE(builder, SOURCE)
        if self.SOURCE_URL is not None:
            CNPProvenanceAddSOURCE_URL(builder, SOURCE_URL)
        if self.SOURCE_DATASET is not None:
            CNPProvenanceAddSOURCE_DATASET(builder, SOURCE_DATASET)
        if self.SOURCE_QUERY is not None:
            CNPProvenanceAddSOURCE_QUERY(builder, SOURCE_QUERY)
        if self.SOURCE_RECORD_ID is not None:
            CNPProvenanceAddSOURCE_RECORD_ID(builder, SOURCE_RECORD_ID)
        if self.SOURCE_SHA256 is not None:
            CNPProvenanceAddSOURCE_SHA256(builder, SOURCE_SHA256)
        if self.RETRIEVED_AT is not None:
            CNPProvenanceAddRETRIEVED_AT(builder, RETRIEVED_AT)
        CNPProvenanceAddMETHOD(builder, self.METHOD)
        if self.MEASUREMENT_SERVER is not None:
            CNPProvenanceAddMEASUREMENT_SERVER(builder, MEASUREMENT_SERVER)
        if self.LICENSE is not None:
            CNPProvenanceAddLICENSE(builder, LICENSE)
        if self.LICENSE_URL is not None:
            CNPProvenanceAddLICENSE_URL(builder, LICENSE_URL)
        if self.ATTRIBUTION is not None:
            CNPProvenanceAddATTRIBUTION(builder, ATTRIBUTION)
        CNPProvenanceAddNON_COMMERCIAL_ONLY(builder, self.NON_COMMERCIAL_ONLY)
        CNPProvenance = CNPProvenanceEnd(builder)
        return CNPProvenance
