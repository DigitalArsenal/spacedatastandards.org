# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Terrain Models
class TRN(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TRN()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTRN(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TRNBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x54\x52\x4E", size_prefixed=size_prefixed)

    # TRN
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TRN
    def SOURCES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # TRN
    def SOURCESAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int8Flags, o)
        return 0

    # TRN
    def SOURCESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TRN
    def SOURCESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # TRN
    def INTERPOLATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 1

    # TRN
    def CACHE_ENABLED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return True

    # TRN
    def MAX_CACHE_TILES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 100

    # TRN
    def VERTICAL_EXAGGERATION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1.0

def TRNStart(builder):
    builder.StartObject(5)

def Start(builder):
    TRNStart(builder)

def TRNAddSOURCES(builder, SOURCES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(SOURCES), 0)

def AddSOURCES(builder, SOURCES):
    TRNAddSOURCES(builder, SOURCES)

def TRNStartSOURCESVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartSOURCESVector(builder, numElems):
    return TRNStartSOURCESVector(builder, numElems)

def TRNAddINTERPOLATION(builder, INTERPOLATION):
    builder.PrependInt8Slot(1, INTERPOLATION, 1)

def AddINTERPOLATION(builder, INTERPOLATION):
    TRNAddINTERPOLATION(builder, INTERPOLATION)

def TRNAddCACHE_ENABLED(builder, CACHE_ENABLED):
    builder.PrependBoolSlot(2, CACHE_ENABLED, 1)

def AddCACHE_ENABLED(builder, CACHE_ENABLED):
    TRNAddCACHE_ENABLED(builder, CACHE_ENABLED)

def TRNAddMAX_CACHE_TILES(builder, MAX_CACHE_TILES):
    builder.PrependUint16Slot(3, MAX_CACHE_TILES, 100)

def AddMAX_CACHE_TILES(builder, MAX_CACHE_TILES):
    TRNAddMAX_CACHE_TILES(builder, MAX_CACHE_TILES)

def TRNAddVERTICAL_EXAGGERATION(builder, VERTICAL_EXAGGERATION):
    builder.PrependFloat64Slot(4, VERTICAL_EXAGGERATION, 1.0)

def AddVERTICAL_EXAGGERATION(builder, VERTICAL_EXAGGERATION):
    TRNAddVERTICAL_EXAGGERATION(builder, VERTICAL_EXAGGERATION)

def TRNEnd(builder):
    return builder.EndObject()

def End(builder):
    return TRNEnd(builder)

try:
    from typing import List
except:
    pass

class TRNT(object):

    # TRNT
    def __init__(self):
        self.SOURCES = None  # type: List[int]
        self.INTERPOLATION = 1  # type: int
        self.CACHE_ENABLED = True  # type: bool
        self.MAX_CACHE_TILES = 100  # type: int
        self.VERTICAL_EXAGGERATION = 1.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        TRN = TRN()
        TRN.Init(buf, pos)
        return cls.InitFromObj(TRN)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, TRN):
        x = TRNT()
        x._UnPack(TRN)
        return x

    # TRNT
    def _UnPack(self, TRN):
        if TRN is None:
            return
        if not TRN.SOURCESIsNone():
            if np is None:
                self.SOURCES = []
                for i in range(TRN.SOURCESLength()):
                    self.SOURCES.append(TRN.SOURCES(i))
            else:
                self.SOURCES = TRN.SOURCESAsNumpy()
        self.INTERPOLATION = TRN.INTERPOLATION()
        self.CACHE_ENABLED = TRN.CACHE_ENABLED()
        self.MAX_CACHE_TILES = TRN.MAX_CACHE_TILES()
        self.VERTICAL_EXAGGERATION = TRN.VERTICAL_EXAGGERATION()

    # TRNT
    def Pack(self, builder):
        if self.SOURCES is not None:
            if np is not None and type(self.SOURCES) is np.ndarray:
                SOURCES = builder.CreateNumpyVector(self.SOURCES)
            else:
                TRNStartSOURCESVector(builder, len(self.SOURCES))
                for i in reversed(range(len(self.SOURCES))):
                    builder.PrependByte(self.SOURCES[i])
                SOURCES = builder.EndVector()
        TRNStart(builder)
        if self.SOURCES is not None:
            TRNAddSOURCES(builder, SOURCES)
        TRNAddINTERPOLATION(builder, self.INTERPOLATION)
        TRNAddCACHE_ENABLED(builder, self.CACHE_ENABLED)
        TRNAddMAX_CACHE_TILES(builder, self.MAX_CACHE_TILES)
        TRNAddVERTICAL_EXAGGERATION(builder, self.VERTICAL_EXAGGERATION)
        TRN = TRNEnd(builder)
        return TRN
