# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# String metadata key/value attached to a peer registry entry.
class PRRMetadataEntry(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PRRMetadataEntry()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPRRMetadataEntry(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PRRMetadataEntryBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x50\x52\x52", size_prefixed=size_prefixed)

    # PRRMetadataEntry
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Metadata key.
    # PRRMetadataEntry
    def KEY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Metadata value.
    # PRRMetadataEntry
    def VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def PRRMetadataEntryStart(builder):
    builder.StartObject(2)

def Start(builder):
    PRRMetadataEntryStart(builder)

def PRRMetadataEntryAddKEY(builder, KEY):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(KEY), 0)

def AddKEY(builder, KEY):
    PRRMetadataEntryAddKEY(builder, KEY)

def PRRMetadataEntryAddVALUE(builder, VALUE):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(VALUE), 0)

def AddVALUE(builder, VALUE):
    PRRMetadataEntryAddVALUE(builder, VALUE)

def PRRMetadataEntryEnd(builder):
    return builder.EndObject()

def End(builder):
    return PRRMetadataEntryEnd(builder)


class PRRMetadataEntryT(object):

    # PRRMetadataEntryT
    def __init__(
        self,
        KEY = None,
        VALUE = None,
    ):
        self.KEY = KEY  # type: Optional[str]
        self.VALUE = VALUE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpPrrmetadataEntry = PRRMetadataEntry()
        tmpPrrmetadataEntry.Init(buf, pos)
        return cls.InitFromObj(tmpPrrmetadataEntry)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpPrrmetadataEntry):
        x = PRRMetadataEntryT()
        x._UnPack(tmpPrrmetadataEntry)
        return x

    # PRRMetadataEntryT
    def _UnPack(self, PRRMetadataEntry):
        if PRRMetadataEntry is None:
            return
        self.KEY = PRRMetadataEntry.KEY()
        self.VALUE = PRRMetadataEntry.VALUE()

    # PRRMetadataEntryT
    def Pack(self, builder):
        if self.KEY is not None:
            KEY = builder.CreateString(self.KEY)
        if self.VALUE is not None:
            VALUE = builder.CreateString(self.VALUE)
        PRRMetadataEntryStart(builder)
        if self.KEY is not None:
            PRRMetadataEntryAddKEY(builder, KEY)
        if self.VALUE is not None:
            PRRMetadataEntryAddVALUE(builder, VALUE)
        PRRMetadataEntry = PRRMetadataEntryEnd(builder)
        return PRRMetadataEntry
