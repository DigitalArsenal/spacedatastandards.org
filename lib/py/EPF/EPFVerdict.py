# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# FlatSQL-queryable compliance verdict consumed directly by `$RPT`.
class EPFVerdict(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EPFVerdict()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEPFVerdict(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def EPFVerdictBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x45\x50\x46", size_prefixed=size_prefixed)

    # EPFVerdict
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EPFVerdict
    def LIMIT_KIND(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # EPFVerdict
    def COMPLIANT(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Worst margin to the applicable limit, dB. Positive means compliant.
    # EPFVerdict
    def MARGIN_DB(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # EPFVerdict
    def PERCENT_TIME_EXCEEDED(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # EPFVerdict
    def WORST_SAMPLE_INDEX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # EPFVerdict
    def NOTE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def EPFVerdictStart(builder):
    builder.StartObject(6)

def Start(builder):
    EPFVerdictStart(builder)

def EPFVerdictAddLIMIT_KIND(builder, LIMIT_KIND):
    builder.PrependInt8Slot(0, LIMIT_KIND, 0)

def AddLIMIT_KIND(builder, LIMIT_KIND):
    EPFVerdictAddLIMIT_KIND(builder, LIMIT_KIND)

def EPFVerdictAddCOMPLIANT(builder, COMPLIANT):
    builder.PrependBoolSlot(1, COMPLIANT, 0)

def AddCOMPLIANT(builder, COMPLIANT):
    EPFVerdictAddCOMPLIANT(builder, COMPLIANT)

def EPFVerdictAddMARGIN_DB(builder, MARGIN_DB):
    builder.PrependFloat64Slot(2, MARGIN_DB, 0.0)

def AddMARGIN_DB(builder, MARGIN_DB):
    EPFVerdictAddMARGIN_DB(builder, MARGIN_DB)

def EPFVerdictAddPERCENT_TIME_EXCEEDED(builder, PERCENT_TIME_EXCEEDED):
    builder.PrependFloat64Slot(3, PERCENT_TIME_EXCEEDED, 0.0)

def AddPERCENT_TIME_EXCEEDED(builder, PERCENT_TIME_EXCEEDED):
    EPFVerdictAddPERCENT_TIME_EXCEEDED(builder, PERCENT_TIME_EXCEEDED)

def EPFVerdictAddWORST_SAMPLE_INDEX(builder, WORST_SAMPLE_INDEX):
    builder.PrependUint32Slot(4, WORST_SAMPLE_INDEX, 0)

def AddWORST_SAMPLE_INDEX(builder, WORST_SAMPLE_INDEX):
    EPFVerdictAddWORST_SAMPLE_INDEX(builder, WORST_SAMPLE_INDEX)

def EPFVerdictAddNOTE(builder, NOTE):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(NOTE), 0)

def AddNOTE(builder, NOTE):
    EPFVerdictAddNOTE(builder, NOTE)

def EPFVerdictEnd(builder):
    return builder.EndObject()

def End(builder):
    return EPFVerdictEnd(builder)


class EPFVerdictT(object):

    # EPFVerdictT
    def __init__(
        self,
        LIMIT_KIND = 0,
        COMPLIANT = False,
        MARGIN_DB = 0.0,
        PERCENT_TIME_EXCEEDED = 0.0,
        WORST_SAMPLE_INDEX = 0,
        NOTE = None,
    ):
        self.LIMIT_KIND = LIMIT_KIND  # type: int
        self.COMPLIANT = COMPLIANT  # type: bool
        self.MARGIN_DB = MARGIN_DB  # type: float
        self.PERCENT_TIME_EXCEEDED = PERCENT_TIME_EXCEEDED  # type: float
        self.WORST_SAMPLE_INDEX = WORST_SAMPLE_INDEX  # type: int
        self.NOTE = NOTE  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpEpfverdict = EPFVerdict()
        tmpEpfverdict.Init(buf, pos)
        return cls.InitFromObj(tmpEpfverdict)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpEpfverdict):
        x = EPFVerdictT()
        x._UnPack(tmpEpfverdict)
        return x

    # EPFVerdictT
    def _UnPack(self, EPFVerdict):
        if EPFVerdict is None:
            return
        self.LIMIT_KIND = EPFVerdict.LIMIT_KIND()
        self.COMPLIANT = EPFVerdict.COMPLIANT()
        self.MARGIN_DB = EPFVerdict.MARGIN_DB()
        self.PERCENT_TIME_EXCEEDED = EPFVerdict.PERCENT_TIME_EXCEEDED()
        self.WORST_SAMPLE_INDEX = EPFVerdict.WORST_SAMPLE_INDEX()
        self.NOTE = EPFVerdict.NOTE()

    # EPFVerdictT
    def Pack(self, builder):
        if self.NOTE is not None:
            NOTE = builder.CreateString(self.NOTE)
        EPFVerdictStart(builder)
        EPFVerdictAddLIMIT_KIND(builder, self.LIMIT_KIND)
        EPFVerdictAddCOMPLIANT(builder, self.COMPLIANT)
        EPFVerdictAddMARGIN_DB(builder, self.MARGIN_DB)
        EPFVerdictAddPERCENT_TIME_EXCEEDED(builder, self.PERCENT_TIME_EXCEEDED)
        EPFVerdictAddWORST_SAMPLE_INDEX(builder, self.WORST_SAMPLE_INDEX)
        if self.NOTE is not None:
            EPFVerdictAddNOTE(builder, NOTE)
        EPFVerdict = EPFVerdictEnd(builder)
        return EPFVerdict
