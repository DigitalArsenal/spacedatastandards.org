# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# GeoJSON FeatureCollection
class GJN(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = GJN()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsGJN(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def GJNBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x47\x4A\x4E", size_prefixed=size_prefixed)

    # GJN
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Features in the collection
    # GJN
    def FEATURES(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from GJNFeature import GJNFeature
            obj = GJNFeature()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # GJN
    def FEATURESLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # GJN
    def FEATURESIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Bounding box (optional)
    # GJN
    def BBOX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from GJNBoundingBox import GJNBoundingBox
            obj = GJNBoundingBox()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def GJNStart(builder):
    builder.StartObject(2)

def Start(builder):
    GJNStart(builder)

def GJNAddFEATURES(builder, FEATURES):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(FEATURES), 0)

def AddFEATURES(builder, FEATURES):
    GJNAddFEATURES(builder, FEATURES)

def GJNStartFEATURESVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartFEATURESVector(builder, numElems):
    return GJNStartFEATURESVector(builder, numElems)

def GJNAddBBOX(builder, BBOX):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(BBOX), 0)

def AddBBOX(builder, BBOX):
    GJNAddBBOX(builder, BBOX)

def GJNEnd(builder):
    return builder.EndObject()

def End(builder):
    return GJNEnd(builder)

import GJNBoundingBox
import GJNFeature
try:
    from typing import List, Optional
except:
    pass

class GJNT(object):

    # GJNT
    def __init__(self):
        self.FEATURES = None  # type: List[GJNFeature.GJNFeatureT]
        self.BBOX = None  # type: Optional[GJNBoundingBox.GJNBoundingBoxT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        GJN = GJN()
        GJN.Init(buf, pos)
        return cls.InitFromObj(GJN)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, GJN):
        x = GJNT()
        x._UnPack(GJN)
        return x

    # GJNT
    def _UnPack(self, GJN):
        if GJN is None:
            return
        if not GJN.FEATURESIsNone():
            self.FEATURES = []
            for i in range(GJN.FEATURESLength()):
                if GJN.FEATURES(i) is None:
                    self.FEATURES.append(None)
                else:
                    gJNFeature_ = GJNFeature.GJNFeatureT.InitFromObj(GJN.FEATURES(i))
                    self.FEATURES.append(gJNFeature_)
        if GJN.BBOX() is not None:
            self.BBOX = GJNBoundingBox.GJNBoundingBoxT.InitFromObj(GJN.BBOX())

    # GJNT
    def Pack(self, builder):
        if self.FEATURES is not None:
            FEATURESlist = []
            for i in range(len(self.FEATURES)):
                FEATURESlist.append(self.FEATURES[i].Pack(builder))
            GJNStartFEATURESVector(builder, len(self.FEATURES))
            for i in reversed(range(len(self.FEATURES))):
                builder.PrependUOffsetTRelative(FEATURESlist[i])
            FEATURES = builder.EndVector()
        if self.BBOX is not None:
            BBOX = self.BBOX.Pack(builder)
        GJNStart(builder)
        if self.FEATURES is not None:
            GJNAddFEATURES(builder, FEATURES)
        if self.BBOX is not None:
            GJNAddBBOX(builder, BBOX)
        GJN = GJNEnd(builder)
        return GJN
