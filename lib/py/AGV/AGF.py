# automatically generated by the FlatBuffers compiler, do not modify

# namespace:

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Atmospheric Gridded Field
class AGF(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AGF()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAGF(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AGFBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x24\x41\x47\x46", size_prefixed=size_prefixed)

    # AGF
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Stable identifier of this field record.
    # AGF
    def ID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Nominal time the field represents, ISO 8601 UTC. For an observation this
    # is the observation time; for a forecast it is the validity time.
    # AGF
    def EPOCH(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Half-open validity window [VALID_START, VALID_END), ISO 8601 UTC. A
    # field is a sample of a continuous atmosphere and is not valid forever.
    # AGF
    def VALID_START(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # AGF
    def VALID_END(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Model initialisation time, ISO 8601 UTC. ABSENT for an observation — an
    # observation has no forecast reference time, and an empty string here is
    # what distinguishes the two cases.
    # AGF
    def FORECAST_REFERENCE_TIME(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Seconds from FORECAST_REFERENCE_TIME to EPOCH. Meaningful ONLY when
    # FORECAST_REFERENCE_TIME is present; NaN otherwise. It is never inferred
    # to be zero for an observation.
    # AGF
    def FORECAST_LEAD_S(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # The physical quantity. It does NOT imply units.
    # AGF
    def VARIABLE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # UDUNITS-compatible units of the SCALED physical value. Required: the
    # enum names a quantity, this names its scale, and a field without it is
    # uninterpretable.
    # AGF
    def UNITS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Category meanings for a categorical VARIABLE such as CLOUD_MASK, indexed
    # by raw value. Empty for continuous variables.
    # AGF
    def CATEGORY_LABELS(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # AGF
    def CATEGORY_LABELSLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AGF
    def CATEGORY_LABELSIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # Element type of the packed array.
    # AGF
    def DTYPE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Sentinel marking a cell with no data, in RAW units, before scaling.
    # AGF
    def FILL_VALUE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # physical = raw * SCALE_FACTOR + ADD_OFFSET, applied only to non-fill
    # cells. Defaults are the identity packing.
    # AGF
    def SCALE_FACTOR(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 1.0

    # AGF
    def ADD_OFFSET(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Number of columns and rows. The payload holds exactly NX * NY elements.
    # AGF
    def NX(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # AGF
    def NY(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Order of the NY rows within the payload.
    # AGF
    def ROW_ORDER(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(34))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Projection family of the grid.
    # AGF
    def PROJECTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(36))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Explicit, authoritative georeferencing parameters.
    # AGF
    def PROJECTION_PARAMETERS(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(38))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from AGFProjectionParameters import AGFProjectionParameters
            obj = AGFProjectionParameters()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Codec applied to the packed array.
    # AGF
    def PAYLOAD_CODEC(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(40))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Inline post-codec bytes. LEGAL ONLY when PAYLOAD_SIZE_BYTES is at most
    # 1048576. MUST be absent when PAYLOAD_CID is present.
    # AGF
    def PAYLOAD(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # AGF
    def PAYLOADAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # AGF
    def PAYLOADLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AGF
    def PAYLOADIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(42))
        return o == 0

    # Content-addressed reference to the identical post-codec byte string.
    # MUST be absent when PAYLOAD is present, and MUST be used when the
    # post-codec size exceeds 1048576 bytes.
    # AGF
    def PAYLOAD_CID(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(44))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Lowercase hex SHA-256 of the post-codec byte string. REQUIRED in BOTH
    # payload modes — it is what proves an inline and a fetched payload are
    # the same object.
    # AGF
    def PAYLOAD_SHA256(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(46))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Length of the post-codec byte string, bytes. REQUIRED in BOTH payload
    # modes; it is what makes the 1 MiB inline rule checkable without
    # decoding. Zero is never valid for a populated field.
    # AGF
    def PAYLOAD_SIZE_BYTES(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(48))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Where the field came from. Required.
    # AGF
    def PROVENANCE(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(50))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from AGFProvenance import AGFProvenance
            obj = AGFProvenance()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Free-text description of the field, not of its vendor.
    # AGF
    def DESCRIPTION(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(52))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def AGFStart(builder):
    builder.StartObject(25)

def Start(builder):
    AGFStart(builder)

def AGFAddID(builder, ID):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ID), 0)

def AddID(builder, ID):
    AGFAddID(builder, ID)

def AGFAddEPOCH(builder, EPOCH):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(EPOCH), 0)

def AddEPOCH(builder, EPOCH):
    AGFAddEPOCH(builder, EPOCH)

def AGFAddVALID_START(builder, VALID_START):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(VALID_START), 0)

def AddVALID_START(builder, VALID_START):
    AGFAddVALID_START(builder, VALID_START)

def AGFAddVALID_END(builder, VALID_END):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(VALID_END), 0)

def AddVALID_END(builder, VALID_END):
    AGFAddVALID_END(builder, VALID_END)

def AGFAddFORECAST_REFERENCE_TIME(builder, FORECAST_REFERENCE_TIME):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(FORECAST_REFERENCE_TIME), 0)

def AddFORECAST_REFERENCE_TIME(builder, FORECAST_REFERENCE_TIME):
    AGFAddFORECAST_REFERENCE_TIME(builder, FORECAST_REFERENCE_TIME)

def AGFAddFORECAST_LEAD_S(builder, FORECAST_LEAD_S):
    builder.PrependFloat64Slot(5, FORECAST_LEAD_S, 0.0)

def AddFORECAST_LEAD_S(builder, FORECAST_LEAD_S):
    AGFAddFORECAST_LEAD_S(builder, FORECAST_LEAD_S)

def AGFAddVARIABLE(builder, VARIABLE):
    builder.PrependInt8Slot(6, VARIABLE, 0)

def AddVARIABLE(builder, VARIABLE):
    AGFAddVARIABLE(builder, VARIABLE)

def AGFAddUNITS(builder, UNITS):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(UNITS), 0)

def AddUNITS(builder, UNITS):
    AGFAddUNITS(builder, UNITS)

def AGFAddCATEGORY_LABELS(builder, CATEGORY_LABELS):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(CATEGORY_LABELS), 0)

def AddCATEGORY_LABELS(builder, CATEGORY_LABELS):
    AGFAddCATEGORY_LABELS(builder, CATEGORY_LABELS)

def AGFStartCATEGORY_LABELSVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCATEGORY_LABELSVector(builder, numElems):
    return AGFStartCATEGORY_LABELSVector(builder, numElems)

def AGFCreateCATEGORY_LABELSVector(builder, data):
    return builder.CreateVectorOfTables(data)

def CreateCATEGORY_LABELSVector(builder, data):
    AGFCreateCATEGORY_LABELSVector(builder, data)

def AGFAddDTYPE(builder, DTYPE):
    builder.PrependInt8Slot(9, DTYPE, 0)

def AddDTYPE(builder, DTYPE):
    AGFAddDTYPE(builder, DTYPE)

def AGFAddFILL_VALUE(builder, FILL_VALUE):
    builder.PrependFloat64Slot(10, FILL_VALUE, 0.0)

def AddFILL_VALUE(builder, FILL_VALUE):
    AGFAddFILL_VALUE(builder, FILL_VALUE)

def AGFAddSCALE_FACTOR(builder, SCALE_FACTOR):
    builder.PrependFloat64Slot(11, SCALE_FACTOR, 1.0)

def AddSCALE_FACTOR(builder, SCALE_FACTOR):
    AGFAddSCALE_FACTOR(builder, SCALE_FACTOR)

def AGFAddADD_OFFSET(builder, ADD_OFFSET):
    builder.PrependFloat64Slot(12, ADD_OFFSET, 0.0)

def AddADD_OFFSET(builder, ADD_OFFSET):
    AGFAddADD_OFFSET(builder, ADD_OFFSET)

def AGFAddNX(builder, NX):
    builder.PrependUint32Slot(13, NX, 0)

def AddNX(builder, NX):
    AGFAddNX(builder, NX)

def AGFAddNY(builder, NY):
    builder.PrependUint32Slot(14, NY, 0)

def AddNY(builder, NY):
    AGFAddNY(builder, NY)

def AGFAddROW_ORDER(builder, ROW_ORDER):
    builder.PrependInt8Slot(15, ROW_ORDER, 0)

def AddROW_ORDER(builder, ROW_ORDER):
    AGFAddROW_ORDER(builder, ROW_ORDER)

def AGFAddPROJECTION(builder, PROJECTION):
    builder.PrependInt8Slot(16, PROJECTION, 0)

def AddPROJECTION(builder, PROJECTION):
    AGFAddPROJECTION(builder, PROJECTION)

def AGFAddPROJECTION_PARAMETERS(builder, PROJECTION_PARAMETERS):
    builder.PrependUOffsetTRelativeSlot(17, flatbuffers.number_types.UOffsetTFlags.py_type(PROJECTION_PARAMETERS), 0)

def AddPROJECTION_PARAMETERS(builder, PROJECTION_PARAMETERS):
    AGFAddPROJECTION_PARAMETERS(builder, PROJECTION_PARAMETERS)

def AGFAddPAYLOAD_CODEC(builder, PAYLOAD_CODEC):
    builder.PrependInt8Slot(18, PAYLOAD_CODEC, 0)

def AddPAYLOAD_CODEC(builder, PAYLOAD_CODEC):
    AGFAddPAYLOAD_CODEC(builder, PAYLOAD_CODEC)

def AGFAddPAYLOAD(builder, PAYLOAD):
    builder.PrependUOffsetTRelativeSlot(19, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD), 0)

def AddPAYLOAD(builder, PAYLOAD):
    AGFAddPAYLOAD(builder, PAYLOAD)

def AGFStartPAYLOADVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartPAYLOADVector(builder, numElems):
    return AGFStartPAYLOADVector(builder, numElems)

def AGFCreatePAYLOADVector(builder, data):
    data = list(data)
    builder.StartVector(1, len(data), 1)
    for item in reversed(data):
        builder.PrependUint8(item)
    return builder.EndVector()

def CreatePAYLOADVector(builder, data):
    AGFCreatePAYLOADVector(builder, data)

def AGFAddPAYLOAD_CID(builder, PAYLOAD_CID):
    builder.PrependUOffsetTRelativeSlot(20, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD_CID), 0)

def AddPAYLOAD_CID(builder, PAYLOAD_CID):
    AGFAddPAYLOAD_CID(builder, PAYLOAD_CID)

def AGFAddPAYLOAD_SHA256(builder, PAYLOAD_SHA256):
    builder.PrependUOffsetTRelativeSlot(21, flatbuffers.number_types.UOffsetTFlags.py_type(PAYLOAD_SHA256), 0)

def AddPAYLOAD_SHA256(builder, PAYLOAD_SHA256):
    AGFAddPAYLOAD_SHA256(builder, PAYLOAD_SHA256)

def AGFAddPAYLOAD_SIZE_BYTES(builder, PAYLOAD_SIZE_BYTES):
    builder.PrependUint64Slot(22, PAYLOAD_SIZE_BYTES, 0)

def AddPAYLOAD_SIZE_BYTES(builder, PAYLOAD_SIZE_BYTES):
    AGFAddPAYLOAD_SIZE_BYTES(builder, PAYLOAD_SIZE_BYTES)

def AGFAddPROVENANCE(builder, PROVENANCE):
    builder.PrependUOffsetTRelativeSlot(23, flatbuffers.number_types.UOffsetTFlags.py_type(PROVENANCE), 0)

def AddPROVENANCE(builder, PROVENANCE):
    AGFAddPROVENANCE(builder, PROVENANCE)

def AGFAddDESCRIPTION(builder, DESCRIPTION):
    builder.PrependUOffsetTRelativeSlot(24, flatbuffers.number_types.UOffsetTFlags.py_type(DESCRIPTION), 0)

def AddDESCRIPTION(builder, DESCRIPTION):
    AGFAddDESCRIPTION(builder, DESCRIPTION)

def AGFEnd(builder):
    return builder.EndObject()

def End(builder):
    return AGFEnd(builder)

import AGFProjectionParameters
import AGFProvenance
try:
    from typing import List, Optional
except:
    pass

class AGFT(object):

    # AGFT
    def __init__(
        self,
        ID = None,
        EPOCH = None,
        VALID_START = None,
        VALID_END = None,
        FORECAST_REFERENCE_TIME = None,
        FORECAST_LEAD_S = 0.0,
        VARIABLE = 0,
        UNITS = None,
        CATEGORY_LABELS = None,
        DTYPE = 0,
        FILL_VALUE = 0.0,
        SCALE_FACTOR = 1.0,
        ADD_OFFSET = 0.0,
        NX = 0,
        NY = 0,
        ROW_ORDER = 0,
        PROJECTION = 0,
        PROJECTION_PARAMETERS = None,
        PAYLOAD_CODEC = 0,
        PAYLOAD = None,
        PAYLOAD_CID = None,
        PAYLOAD_SHA256 = None,
        PAYLOAD_SIZE_BYTES = 0,
        PROVENANCE = None,
        DESCRIPTION = None,
    ):
        self.ID = ID  # type: Optional[str]
        self.EPOCH = EPOCH  # type: Optional[str]
        self.VALID_START = VALID_START  # type: Optional[str]
        self.VALID_END = VALID_END  # type: Optional[str]
        self.FORECAST_REFERENCE_TIME = FORECAST_REFERENCE_TIME  # type: Optional[str]
        self.FORECAST_LEAD_S = FORECAST_LEAD_S  # type: float
        self.VARIABLE = VARIABLE  # type: int
        self.UNITS = UNITS  # type: Optional[str]
        self.CATEGORY_LABELS = CATEGORY_LABELS  # type: Optional[List[Optional[str]]]
        self.DTYPE = DTYPE  # type: int
        self.FILL_VALUE = FILL_VALUE  # type: float
        self.SCALE_FACTOR = SCALE_FACTOR  # type: float
        self.ADD_OFFSET = ADD_OFFSET  # type: float
        self.NX = NX  # type: int
        self.NY = NY  # type: int
        self.ROW_ORDER = ROW_ORDER  # type: int
        self.PROJECTION = PROJECTION  # type: int
        self.PROJECTION_PARAMETERS = PROJECTION_PARAMETERS  # type: Optional[AGFProjectionParameters.AGFProjectionParametersT]
        self.PAYLOAD_CODEC = PAYLOAD_CODEC  # type: int
        self.PAYLOAD = PAYLOAD  # type: Optional[List[int]]
        self.PAYLOAD_CID = PAYLOAD_CID  # type: Optional[str]
        self.PAYLOAD_SHA256 = PAYLOAD_SHA256  # type: Optional[str]
        self.PAYLOAD_SIZE_BYTES = PAYLOAD_SIZE_BYTES  # type: int
        self.PROVENANCE = PROVENANCE  # type: Optional[AGFProvenance.AGFProvenanceT]
        self.DESCRIPTION = DESCRIPTION  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tmpAgf = AGF()
        tmpAgf.Init(buf, pos)
        return cls.InitFromObj(tmpAgf)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tmpAgf):
        x = AGFT()
        x._UnPack(tmpAgf)
        return x

    # AGFT
    def _UnPack(self, AGF):
        if AGF is None:
            return
        self.ID = AGF.ID()
        self.EPOCH = AGF.EPOCH()
        self.VALID_START = AGF.VALID_START()
        self.VALID_END = AGF.VALID_END()
        self.FORECAST_REFERENCE_TIME = AGF.FORECAST_REFERENCE_TIME()
        self.FORECAST_LEAD_S = AGF.FORECAST_LEAD_S()
        self.VARIABLE = AGF.VARIABLE()
        self.UNITS = AGF.UNITS()
        if not AGF.CATEGORY_LABELSIsNone():
            self.CATEGORY_LABELS = []
            for i in range(AGF.CATEGORY_LABELSLength()):
                self.CATEGORY_LABELS.append(AGF.CATEGORY_LABELS(i))
        self.DTYPE = AGF.DTYPE()
        self.FILL_VALUE = AGF.FILL_VALUE()
        self.SCALE_FACTOR = AGF.SCALE_FACTOR()
        self.ADD_OFFSET = AGF.ADD_OFFSET()
        self.NX = AGF.NX()
        self.NY = AGF.NY()
        self.ROW_ORDER = AGF.ROW_ORDER()
        self.PROJECTION = AGF.PROJECTION()
        if AGF.PROJECTION_PARAMETERS() is not None:
            self.PROJECTION_PARAMETERS = AGFProjectionParameters.AGFProjectionParametersT.InitFromObj(AGF.PROJECTION_PARAMETERS())
        self.PAYLOAD_CODEC = AGF.PAYLOAD_CODEC()
        if not AGF.PAYLOADIsNone():
            if np is None:
                self.PAYLOAD = []
                for i in range(AGF.PAYLOADLength()):
                    self.PAYLOAD.append(AGF.PAYLOAD(i))
            else:
                self.PAYLOAD = AGF.PAYLOADAsNumpy()
        self.PAYLOAD_CID = AGF.PAYLOAD_CID()
        self.PAYLOAD_SHA256 = AGF.PAYLOAD_SHA256()
        self.PAYLOAD_SIZE_BYTES = AGF.PAYLOAD_SIZE_BYTES()
        if AGF.PROVENANCE() is not None:
            self.PROVENANCE = AGFProvenance.AGFProvenanceT.InitFromObj(AGF.PROVENANCE())
        self.DESCRIPTION = AGF.DESCRIPTION()

    # AGFT
    def Pack(self, builder):
        if self.ID is not None:
            ID = builder.CreateString(self.ID)
        if self.EPOCH is not None:
            EPOCH = builder.CreateString(self.EPOCH)
        if self.VALID_START is not None:
            VALID_START = builder.CreateString(self.VALID_START)
        if self.VALID_END is not None:
            VALID_END = builder.CreateString(self.VALID_END)
        if self.FORECAST_REFERENCE_TIME is not None:
            FORECAST_REFERENCE_TIME = builder.CreateString(self.FORECAST_REFERENCE_TIME)
        if self.UNITS is not None:
            UNITS = builder.CreateString(self.UNITS)
        if self.CATEGORY_LABELS is not None:
            CATEGORY_LABELSlist = []
            for i in range(len(self.CATEGORY_LABELS)):
                CATEGORY_LABELSlist.append(builder.CreateString(self.CATEGORY_LABELS[i]))
            AGFStartCATEGORY_LABELSVector(builder, len(self.CATEGORY_LABELS))
            for i in reversed(range(len(self.CATEGORY_LABELS))):
                builder.PrependUOffsetTRelative(CATEGORY_LABELSlist[i])
            CATEGORY_LABELS = builder.EndVector()
        if self.PROJECTION_PARAMETERS is not None:
            PROJECTION_PARAMETERS = self.PROJECTION_PARAMETERS.Pack(builder)
        if self.PAYLOAD is not None:
            if np is not None and type(self.PAYLOAD) is np.ndarray:
                PAYLOAD = builder.CreateNumpyVector(self.PAYLOAD)
            else:
                AGFStartPAYLOADVector(builder, len(self.PAYLOAD))
                for i in reversed(range(len(self.PAYLOAD))):
                    builder.PrependUint8(self.PAYLOAD[i])
                PAYLOAD = builder.EndVector()
        if self.PAYLOAD_CID is not None:
            PAYLOAD_CID = builder.CreateString(self.PAYLOAD_CID)
        if self.PAYLOAD_SHA256 is not None:
            PAYLOAD_SHA256 = builder.CreateString(self.PAYLOAD_SHA256)
        if self.PROVENANCE is not None:
            PROVENANCE = self.PROVENANCE.Pack(builder)
        if self.DESCRIPTION is not None:
            DESCRIPTION = builder.CreateString(self.DESCRIPTION)
        AGFStart(builder)
        if self.ID is not None:
            AGFAddID(builder, ID)
        if self.EPOCH is not None:
            AGFAddEPOCH(builder, EPOCH)
        if self.VALID_START is not None:
            AGFAddVALID_START(builder, VALID_START)
        if self.VALID_END is not None:
            AGFAddVALID_END(builder, VALID_END)
        if self.FORECAST_REFERENCE_TIME is not None:
            AGFAddFORECAST_REFERENCE_TIME(builder, FORECAST_REFERENCE_TIME)
        AGFAddFORECAST_LEAD_S(builder, self.FORECAST_LEAD_S)
        AGFAddVARIABLE(builder, self.VARIABLE)
        if self.UNITS is not None:
            AGFAddUNITS(builder, UNITS)
        if self.CATEGORY_LABELS is not None:
            AGFAddCATEGORY_LABELS(builder, CATEGORY_LABELS)
        AGFAddDTYPE(builder, self.DTYPE)
        AGFAddFILL_VALUE(builder, self.FILL_VALUE)
        AGFAddSCALE_FACTOR(builder, self.SCALE_FACTOR)
        AGFAddADD_OFFSET(builder, self.ADD_OFFSET)
        AGFAddNX(builder, self.NX)
        AGFAddNY(builder, self.NY)
        AGFAddROW_ORDER(builder, self.ROW_ORDER)
        AGFAddPROJECTION(builder, self.PROJECTION)
        if self.PROJECTION_PARAMETERS is not None:
            AGFAddPROJECTION_PARAMETERS(builder, PROJECTION_PARAMETERS)
        AGFAddPAYLOAD_CODEC(builder, self.PAYLOAD_CODEC)
        if self.PAYLOAD is not None:
            AGFAddPAYLOAD(builder, PAYLOAD)
        if self.PAYLOAD_CID is not None:
            AGFAddPAYLOAD_CID(builder, PAYLOAD_CID)
        if self.PAYLOAD_SHA256 is not None:
            AGFAddPAYLOAD_SHA256(builder, PAYLOAD_SHA256)
        AGFAddPAYLOAD_SIZE_BYTES(builder, self.PAYLOAD_SIZE_BYTES)
        if self.PROVENANCE is not None:
            AGFAddPROVENANCE(builder, PROVENANCE)
        if self.DESCRIPTION is not None:
            AGFAddDESCRIPTION(builder, DESCRIPTION)
        AGF = AGFEnd(builder)
        return AGF
