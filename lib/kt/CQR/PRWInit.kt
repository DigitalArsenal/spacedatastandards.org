// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector
import com.google.flatbuffers.BooleanVector
import com.google.flatbuffers.ByteVector
import com.google.flatbuffers.Constants
import com.google.flatbuffers.DoubleVector
import com.google.flatbuffers.FlatBufferBuilder
import com.google.flatbuffers.FloatVector
import com.google.flatbuffers.LongVector
import com.google.flatbuffers.StringVector
import com.google.flatbuffers.Struct
import com.google.flatbuffers.Table
import com.google.flatbuffers.UnionVector
import java.nio.ByteBuffer
import java.nio.ByteOrder
import kotlin.math.sign

/**
 * Propagator initialization request — assigns TLE / OMM / Keplerian /
 * polynomial inputs to entity handles.
 *
 * The actual per-entity source record is carried inline as raw bytes
 * tagged with SOURCE_KIND and, where useful, a SDS file_identifier.
 * Callers encode `OMM`, `OCM`, or a one-off Keplerian set and pass the
 * bytes verbatim; the propagator uses SOURCE_KIND to decide how to
 * consume them.
 */
@Suppress("unused")
class PRWInit : Table() {

    fun __init(_i: Int, _bb: ByteBuffer)  {
        __reset(_i, _bb)
    }
    fun __assign(_i: Int, _bb: ByteBuffer) : PRWInit {
        __init(_i, _bb)
        return this
    }
    /**
     * Entity handles to assign results to (same order as SOURCES[]).
     */
    fun entityHandles(j: Int) : UInt {
        val o = __offset(4)
        return if (o != 0) {
            bb.getInt(__vector(o) + j * 4).toUInt()
        } else {
            0u
        }
    }
    val entityHandlesLength : Int
        get() {
            val o = __offset(4); return if (o != 0) __vector_len(o) else 0
        }
    val entityHandlesAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(4, 4)
    fun entityHandlesInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 4, 4)
    /**
     * Per-entity source records, encoded as SDS FlatBuffers.
     */
    fun sources(j: Int) : PRWInitSource? = sources(PRWInitSource(), j)
    fun sources(obj: PRWInitSource, j: Int) : PRWInitSource? {
        val o = __offset(6)
        return if (o != 0) {
            obj.__assign(__indirect(__vector(o) + j * 4), bb)
        } else {
            null
        }
    }
    val sourcesLength : Int
        get() {
            val o = __offset(6); return if (o != 0) __vector_len(o) else 0
        }
    companion object {
        fun validateVersion() = Constants.FLATBUFFERS_25_12_19()
        fun getRootAsPRWInit(_bb: ByteBuffer): PRWInit = getRootAsPRWInit(_bb, PRWInit())
        fun getRootAsPRWInit(_bb: ByteBuffer, obj: PRWInit): PRWInit {
            _bb.order(ByteOrder.LITTLE_ENDIAN)
            return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb))
        }
        fun createPRWInit(builder: FlatBufferBuilder, entityHandlesOffset: Int, sourcesOffset: Int) : Int {
            builder.startTable(2)
            addSOURCES(builder, sourcesOffset)
            addENTITYHANDLES(builder, entityHandlesOffset)
            return endPRWInit(builder)
        }
        fun startPRWInit(builder: FlatBufferBuilder) = builder.startTable(2)
        fun addENTITYHANDLES(builder: FlatBufferBuilder, entityHandles: Int) = builder.addOffset(0, entityHandles, 0)
        @kotlin.ExperimentalUnsignedTypes
        fun createEntityHandlesVector(builder: FlatBufferBuilder, data: UIntArray) : Int {
            builder.startVector(4, data.size, 4)
            for (i in data.size - 1 downTo 0) {
                builder.addInt(data[i].toInt())
            }
            return builder.endVector()
        }
        fun startEntityHandlesVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(4, numElems, 4)
        fun addSOURCES(builder: FlatBufferBuilder, sources: Int) = builder.addOffset(1, sources, 0)
        fun createSourcesVector(builder: FlatBufferBuilder, data: IntArray) : Int {
            builder.startVector(4, data.size, 4)
            for (i in data.size - 1 downTo 0) {
                builder.addOffset(data[i])
            }
            return builder.endVector()
        }
        fun startSourcesVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(4, numElems, 4)
        fun endPRWInit(builder: FlatBufferBuilder) : Int {
            val o = builder.endTable()
            return o
        }
    }
}
