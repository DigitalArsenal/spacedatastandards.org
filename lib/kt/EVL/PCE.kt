// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector
import com.google.flatbuffers.BooleanVector
import com.google.flatbuffers.ByteVector
import com.google.flatbuffers.Constants
import com.google.flatbuffers.DoubleVector
import com.google.flatbuffers.FlatBufferBuilder
import com.google.flatbuffers.FloatVector
import com.google.flatbuffers.LongVector
import com.google.flatbuffers.StringVector
import com.google.flatbuffers.Struct
import com.google.flatbuffers.Table
import com.google.flatbuffers.UnionVector
import java.nio.ByteBuffer
import java.nio.ByteOrder
import kotlin.math.sign

/**
 * Parameter Catalog and Evaluation
 *
 * The named calculation parameters an analysis resolves through: their owner
 * class, unit, data type, frame dependency and per-publisher availability
 * (CATALOG), a request and result for evaluating named parameters on states
 * at epochs in a stated coordinate system (EVALUATION_REQUEST /
 * EVALUATION_RESULT), and propagate-to-condition, which is a predicate on one
 * of those same names rather than a vocabulary of its own (STOP_REQUEST /
 * STOP_REPORT). Stopping conditions, targeting goals, report columns and plot
 * series all resolve names here, so a name means one thing everywhere.
 *
 * Angles are radians, lengths metres, durations seconds, masses kilograms.
 * A buffer carries the members its exchange needs; a catalog publication and
 * an evaluation exchange are both complete $PCE records.
 */
@Suppress("unused")
class PCE : Table() {

    fun __init(_i: Int, _bb: ByteBuffer)  {
        __reset(_i, _bb)
    }
    fun __assign(_i: Int, _bb: ByteBuffer) : PCE {
        __init(_i, _bb)
        return this
    }
    val catalog : PCEParameterCatalog? get() = catalog(PCEParameterCatalog())
    fun catalog(obj: PCEParameterCatalog) : PCEParameterCatalog? {
        val o = __offset(4)
        return if (o != 0) {
            obj.__assign(__indirect(o + bb_pos), bb)
        } else {
            null
        }
    }
    val evaluationRequest : PCEEvaluationRequest? get() = evaluationRequest(PCEEvaluationRequest())
    fun evaluationRequest(obj: PCEEvaluationRequest) : PCEEvaluationRequest? {
        val o = __offset(6)
        return if (o != 0) {
            obj.__assign(__indirect(o + bb_pos), bb)
        } else {
            null
        }
    }
    val evaluationResult : PCEEvaluationResult? get() = evaluationResult(PCEEvaluationResult())
    fun evaluationResult(obj: PCEEvaluationResult) : PCEEvaluationResult? {
        val o = __offset(8)
        return if (o != 0) {
            obj.__assign(__indirect(o + bb_pos), bb)
        } else {
            null
        }
    }
    val stopRequest : PCEStopRequest? get() = stopRequest(PCEStopRequest())
    fun stopRequest(obj: PCEStopRequest) : PCEStopRequest? {
        val o = __offset(10)
        return if (o != 0) {
            obj.__assign(__indirect(o + bb_pos), bb)
        } else {
            null
        }
    }
    val stopReport : PCEStopReport? get() = stopReport(PCEStopReport())
    fun stopReport(obj: PCEStopReport) : PCEStopReport? {
        val o = __offset(12)
        return if (o != 0) {
            obj.__assign(__indirect(o + bb_pos), bb)
        } else {
            null
        }
    }
    companion object {
        fun validateVersion() = Constants.FLATBUFFERS_25_12_19()
        fun getRootAsPCE(_bb: ByteBuffer): PCE = getRootAsPCE(_bb, PCE())
        fun getRootAsPCE(_bb: ByteBuffer, obj: PCE): PCE {
            _bb.order(ByteOrder.LITTLE_ENDIAN)
            return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb))
        }
        fun PCEBufferHasIdentifier(_bb: ByteBuffer) : Boolean = __has_identifier(_bb, "$PCE")
        fun createPCE(builder: FlatBufferBuilder, catalogOffset: Int, evaluationRequestOffset: Int, evaluationResultOffset: Int, stopRequestOffset: Int, stopReportOffset: Int) : Int {
            builder.startTable(5)
            addSTOPREPORT(builder, stopReportOffset)
            addSTOPREQUEST(builder, stopRequestOffset)
            addEVALUATIONRESULT(builder, evaluationResultOffset)
            addEVALUATIONREQUEST(builder, evaluationRequestOffset)
            addCATALOG(builder, catalogOffset)
            return endPCE(builder)
        }
        fun startPCE(builder: FlatBufferBuilder) = builder.startTable(5)
        fun addCATALOG(builder: FlatBufferBuilder, catalog: Int) = builder.addOffset(0, catalog, 0)
        fun addEVALUATIONREQUEST(builder: FlatBufferBuilder, evaluationRequest: Int) = builder.addOffset(1, evaluationRequest, 0)
        fun addEVALUATIONRESULT(builder: FlatBufferBuilder, evaluationResult: Int) = builder.addOffset(2, evaluationResult, 0)
        fun addSTOPREQUEST(builder: FlatBufferBuilder, stopRequest: Int) = builder.addOffset(3, stopRequest, 0)
        fun addSTOPREPORT(builder: FlatBufferBuilder, stopReport: Int) = builder.addOffset(4, stopReport, 0)
        fun endPCE(builder: FlatBufferBuilder) : Int {
            val o = builder.endTable()
            return o
        }
        fun finishPCEBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finish(offset, "$PCE")
        fun finishSizePrefixedPCEBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finishSizePrefixed(offset, "$PCE")
    }
}
