// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector
import com.google.flatbuffers.BooleanVector
import com.google.flatbuffers.ByteVector
import com.google.flatbuffers.Constants
import com.google.flatbuffers.DoubleVector
import com.google.flatbuffers.FlatBufferBuilder
import com.google.flatbuffers.FloatVector
import com.google.flatbuffers.LongVector
import com.google.flatbuffers.StringVector
import com.google.flatbuffers.Struct
import com.google.flatbuffers.Table
import com.google.flatbuffers.UnionVector
import java.nio.ByteBuffer
import java.nio.ByteOrder
import kotlin.math.sign

/**
 * Terrestrial Base Station Site.
 *
 * One record describes ONE terrestrial cellular base-station site as
 * reconciled from every provider the publisher consulted. It is the wire
 * record for terrestrial mobile-network infrastructure, and it is not an
 * emitter model ($RFE), a band specification ($RFB), a geodetic tracking
 * station ($GST), a satellite ground station ($SIT), or an observation.
 *
 * A site cannot be re-serialized unattributed. SOURCES and CONSENSUS are both
 * required, so every published position carries the providers it came from and
 * the rule that reconciled them; a consumer that strips attribution has to
 * leave the standard to do it. Unknown values are ABSENT — never zero, never a
 * placeholder, never carried across from a neighbouring site.
 *
 * Identity is ID, the publisher's own. The network addressing fields are the
 * mobile network's, are assigned by its operator, and are reused worldwide;
 * none of them is a global key on its own.
 */
@Suppress("unused")
class TBS : Table() {

    fun __init(_i: Int, _bb: ByteBuffer)  {
        __reset(_i, _bb)
    }
    fun __assign(_i: Int, _bb: ByteBuffer) : TBS {
        __init(_i, _bb)
        return this
    }
    /**
     * Publisher-stable identifier for this site, unique within its record set.
     * Recommended form: "<mcc>-<mnc>-<area-code>-<cell-id>".
     */
    val id : String
        get() {
            val o = __offset(4)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                throw AssertionError("No value for (required) field id")
            }
        }
    val idAsByteBuffer : ByteBuffer get() = __vector_as_bytebuffer(4, 1)
    fun idInByteBuffer(_bb: ByteBuffer) : ByteBuffer = __vector_in_bytebuffer(_bb, 4, 1)
    /**
     * Winning provider's native site identifier, copied verbatim from the
     * SOURCES entry named by CONSENSUS.WINNING_PROVIDER_ID. Never a substitute
     * for ID and never stable across providers.
     */
    val nativeId : String?
        get() {
            val o = __offset(6)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val nativeIdAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(6, 1)
    fun nativeIdInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 6, 1)
    val radio : Byte
        get() {
            val o = __offset(8)
            return if(o != 0) bb.get(o + bb_pos) else 6
        }
    /**
     * Mobile country code from the international mobile-subscriber identity
     * numbering plan. Zero means the source published none.
     */
    val mcc : UInt
        get() {
            val o = __offset(10)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * Mobile network code within MCC. Carried as an integer, so decimal leading
     * zeros are NOT preserved: a two- versus three-digit network code is
     * distinguished by the numbering plan of MCC, and the source's own text is
     * retained verbatim in TBSProvenance.NATIVE_ID. Zero means unpublished.
     */
    val mnc : UInt
        get() {
            val o = __offset(12)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * Location area code, used by GSM-, CDMA- and UMTS-class access. Zero means
     * unpublished. A site published with a location area code does not also
     * carry a tracking area code; publishing both asserts two access
     * generations for one cell.
     */
    val lac : UInt
        get() {
            val o = __offset(14)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * Tracking area code, used by LTE- and NR-class access. Zero means
     * unpublished.
     */
    val tac : UInt
        get() {
            val o = __offset(16)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * Cell identity verbatim from the source. A string because width, radix and
     * leading zeros are significant and differ by access class: a 16-bit cell
     * identity, a 28-bit evolved cell identity and a 36-bit new-radio cell
     * identity are not interchangeable integers, and this field never converts
     * between them.
     */
    val cellId : String?
        get() {
            val o = __offset(18)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val cellIdAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(18, 1)
    fun cellIdInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 18, 1)
    /**
     * Reconciled site position in decimal degrees on WGS 84, produced by the
     * merge described in CONSENSUS. Per-provider positions before
     * reconciliation live in SOURCES[].REPORTED_LATITUDE/REPORTED_LONGITUDE and
     * are never overwritten by this value.
     */
    val latitude : Double
        get() {
            val o = __offset(20)
            return if(o != 0) bb.getDouble(o + bb_pos) else 0.0
        }
    val longitude : Double
        get() {
            val o = __offset(22)
            return if(o != 0) bb.getDouble(o + bb_pos) else 0.0
        }
    /**
     * Radius in metres of the area within which the site's observations were
     * collected — a positional-uncertainty envelope, not a transmission range,
     * not a coverage guarantee, and never converted from another unit. Zero
     * means unpublished.
     */
    val rangeM : Double
        get() {
            val o = __offset(24)
            return if(o != 0) bb.getDouble(o + bb_pos) else 0.0
        }
    /**
     * Independent observations behind the winning report. Zero means the source
     * publishes no observation count, never that the site was never observed.
     */
    val samples : UInt
        get() {
            val o = __offset(26)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * Mean received signal strength in dBm across those observations, as the
     * source publishes it. Carried in dBm unconverted; a consumer needing watts
     * converts at the join, explicitly. Zero means unpublished.
     */
    val averageSignalDbm : Double
        get() {
            val o = __offset(28)
            return if(o != 0) bb.getDouble(o + bb_pos) else 0.0
        }
    /**
     * RFC 3339 UTC fixed-millisecond bounds of the observation window backing
     * the winning report.
     */
    val firstObserved : String?
        get() {
            val o = __offset(30)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val firstObservedAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(30, 1)
    fun firstObservedInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 30, 1)
    val lastObserved : String?
        get() {
            val o = __offset(32)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val lastObservedAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(32, 1)
    fun lastObservedInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 32, 1)
    /**
     * Network operator name verbatim as the cited source states it. A display
     * name, never an identifier: consumers must not treat it as a stable key.
     */
    val operator : String?
        get() {
            val o = __offset(34)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val operatorAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(34, 1)
    fun operatorInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 34, 1)
    /**
     * Operating centre frequency in MHz, following the $RFB convention. A
     * consumer joining to $IQC, which carries hertz, converts at the join,
     * explicitly. Zero means unpublished.
     */
    val frequencyMhz : Double
        get() {
            val o = __offset(36)
            return if(o != 0) bb.getDouble(o + bb_pos) else 0.0
        }
    /**
     * Site or structure name verbatim when the source publishes one. Never
     * synthesized from coordinates, operator, or cell identity.
     */
    val siteName : String?
        get() {
            val o = __offset(38)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val siteNameAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(38, 1)
    fun siteNameInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 38, 1)
    /**
     * ISO 3166-1 alpha-2 country code, uppercase, as the source states it.
     * Derived from MCC only when the source itself derives it.
     */
    val countryCode : String?
        get() {
            val o = __offset(40)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val countryCodeAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(40, 1)
    fun countryCodeInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 40, 1)
    /**
     * Every provider consulted for this site, including providers that returned
     * nothing. Required: a site with no named sources is not publishable.
     */
    fun sources(j: Int) : TBSProvenance = sources(TBSProvenance(), j)
    fun sources(obj: TBSProvenance, j: Int) : TBSProvenance {
        val o = __offset(42)
        return if (o != 0) {
            obj.__assign(__indirect(__vector(o) + j * 4), bb)
        } else {
            throw IndexOutOfBoundsException("Index out of range: $j, vector sources is empty")
        }
    }
    val sourcesLength : Int
        get() {
            val o = __offset(42); return if (o != 0) __vector_len(o) else 0
        }
    /**
     * How the entries in SOURCES were reduced to LATITUDE/LONGITUDE. Required:
     * a published position always states the rule that produced it, including
     * SINGLE_SOURCE.
     */
    val consensus : TBSConsensus get() = consensus(TBSConsensus())
    fun consensus(obj: TBSConsensus) : TBSConsensus {
        val o = __offset(44)
        return if (o != 0) {
            obj.__assign(__indirect(o + bb_pos), bb)
        } else {
            throw AssertionError("No value for (required) field consensus")
        }
    }
    companion object {
        fun validateVersion() = Constants.FLATBUFFERS_25_12_19()
        fun getRootAsTBS(_bb: ByteBuffer): TBS = getRootAsTBS(_bb, TBS())
        fun getRootAsTBS(_bb: ByteBuffer, obj: TBS): TBS {
            _bb.order(ByteOrder.LITTLE_ENDIAN)
            return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb))
        }
        fun TBSBufferHasIdentifier(_bb: ByteBuffer) : Boolean = __has_identifier(_bb, "$TBS")
        fun createTBS(builder: FlatBufferBuilder, idOffset: Int, nativeIdOffset: Int, radio: Byte, mcc: UInt, mnc: UInt, lac: UInt, tac: UInt, cellIdOffset: Int, latitude: Double, longitude: Double, rangeM: Double, samples: UInt, averageSignalDbm: Double, firstObservedOffset: Int, lastObservedOffset: Int, operatorOffset: Int, frequencyMhz: Double, siteNameOffset: Int, countryCodeOffset: Int, sourcesOffset: Int, consensusOffset: Int) : Int {
            builder.startTable(21)
            addFREQUENCYMHZ(builder, frequencyMhz)
            addAVERAGESIGNALDBM(builder, averageSignalDbm)
            addRANGEM(builder, rangeM)
            addLONGITUDE(builder, longitude)
            addLATITUDE(builder, latitude)
            addCONSENSUS(builder, consensusOffset)
            addSOURCES(builder, sourcesOffset)
            addCOUNTRYCODE(builder, countryCodeOffset)
            addSITENAME(builder, siteNameOffset)
            addOPERATOR(builder, operatorOffset)
            addLASTOBSERVED(builder, lastObservedOffset)
            addFIRSTOBSERVED(builder, firstObservedOffset)
            addSAMPLES(builder, samples)
            addCELLID(builder, cellIdOffset)
            addTAC(builder, tac)
            addLAC(builder, lac)
            addMNC(builder, mnc)
            addMCC(builder, mcc)
            addNATIVEID(builder, nativeIdOffset)
            addID(builder, idOffset)
            addRADIO(builder, radio)
            return endTBS(builder)
        }
        fun startTBS(builder: FlatBufferBuilder) = builder.startTable(21)
        fun addID(builder: FlatBufferBuilder, id: Int) = builder.addOffset(0, id, 0)
        fun addNATIVEID(builder: FlatBufferBuilder, nativeId: Int) = builder.addOffset(1, nativeId, 0)
        fun addRADIO(builder: FlatBufferBuilder, radio: Byte) = builder.addByte(2, radio, 6)
        fun addMCC(builder: FlatBufferBuilder, mcc: UInt) = builder.addInt(3, mcc.toInt(), 0)
        fun addMNC(builder: FlatBufferBuilder, mnc: UInt) = builder.addInt(4, mnc.toInt(), 0)
        fun addLAC(builder: FlatBufferBuilder, lac: UInt) = builder.addInt(5, lac.toInt(), 0)
        fun addTAC(builder: FlatBufferBuilder, tac: UInt) = builder.addInt(6, tac.toInt(), 0)
        fun addCELLID(builder: FlatBufferBuilder, cellId: Int) = builder.addOffset(7, cellId, 0)
        fun addLATITUDE(builder: FlatBufferBuilder, latitude: Double) = builder.addDouble(8, latitude, 0.0)
        fun addLONGITUDE(builder: FlatBufferBuilder, longitude: Double) = builder.addDouble(9, longitude, 0.0)
        fun addRANGEM(builder: FlatBufferBuilder, rangeM: Double) = builder.addDouble(10, rangeM, 0.0)
        fun addSAMPLES(builder: FlatBufferBuilder, samples: UInt) = builder.addInt(11, samples.toInt(), 0)
        fun addAVERAGESIGNALDBM(builder: FlatBufferBuilder, averageSignalDbm: Double) = builder.addDouble(12, averageSignalDbm, 0.0)
        fun addFIRSTOBSERVED(builder: FlatBufferBuilder, firstObserved: Int) = builder.addOffset(13, firstObserved, 0)
        fun addLASTOBSERVED(builder: FlatBufferBuilder, lastObserved: Int) = builder.addOffset(14, lastObserved, 0)
        fun addOPERATOR(builder: FlatBufferBuilder, operator: Int) = builder.addOffset(15, operator, 0)
        fun addFREQUENCYMHZ(builder: FlatBufferBuilder, frequencyMhz: Double) = builder.addDouble(16, frequencyMhz, 0.0)
        fun addSITENAME(builder: FlatBufferBuilder, siteName: Int) = builder.addOffset(17, siteName, 0)
        fun addCOUNTRYCODE(builder: FlatBufferBuilder, countryCode: Int) = builder.addOffset(18, countryCode, 0)
        fun addSOURCES(builder: FlatBufferBuilder, sources: Int) = builder.addOffset(19, sources, 0)
        fun createSourcesVector(builder: FlatBufferBuilder, data: IntArray) : Int {
            builder.startVector(4, data.size, 4)
            for (i in data.size - 1 downTo 0) {
                builder.addOffset(data[i])
            }
            return builder.endVector()
        }
        fun startSourcesVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(4, numElems, 4)
        fun addCONSENSUS(builder: FlatBufferBuilder, consensus: Int) = builder.addOffset(20, consensus, 0)
        fun endTBS(builder: FlatBufferBuilder) : Int {
            val o = builder.endTable()
                builder.required(o, 4)
                builder.required(o, 42)
                builder.required(o, 44)
            return o
        }
        fun finishTBSBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finish(offset, "$TBS")
        fun finishSizePrefixedTBSBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finishSizePrefixed(offset, "$TBS")
    }
}
