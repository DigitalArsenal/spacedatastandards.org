// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector
import com.google.flatbuffers.BooleanVector
import com.google.flatbuffers.ByteVector
import com.google.flatbuffers.Constants
import com.google.flatbuffers.DoubleVector
import com.google.flatbuffers.FlatBufferBuilder
import com.google.flatbuffers.FloatVector
import com.google.flatbuffers.LongVector
import com.google.flatbuffers.StringVector
import com.google.flatbuffers.Struct
import com.google.flatbuffers.Table
import com.google.flatbuffers.UnionVector
import java.nio.ByteBuffer
import java.nio.ByteOrder
import kotlin.math.sign

/**
 * $CCT — Capability Category Taxonomy.
 *
 * The ratified set of capability classes that distributable units (modules,
 * applications, composed flows) are classified under, together with the labels
 * and routes every consuming surface renders. One published $CCT is the single
 * source of truth shared by a storefront, a library, a search index and the
 * unit manifests themselves.
 *
 * Division of labour: `$CCT` = the category vocabulary and its presentation;
 * `$PLG` = one module's listing, which cites categories by code; `$APP` = one
 * application's manifest, which cites categories by code; `$PMM` = which
 * modules a provider serves; `$STO`/`$STF` = commerce.
 */
@Suppress("unused")
class CCT : Table() {

    fun __init(_i: Int, _bb: ByteBuffer)  {
        __reset(_i, _bb)
    }
    fun __assign(_i: Int, _bb: ByteBuffer) : CCT {
        __init(_i, _bb)
        return this
    }
    /**
     * Stable identifier of this taxonomy publication.
     */
    val taxonomyId : String
        get() {
            val o = __offset(4)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                throw AssertionError("No value for (required) field taxonomyId")
            }
        }
    val taxonomyIdAsByteBuffer : ByteBuffer get() = __vector_as_bytebuffer(4, 1)
    fun taxonomyIdInByteBuffer(_bb: ByteBuffer) : ByteBuffer = __vector_in_bytebuffer(_bb, 4, 1)
    /**
     * SemVer 2.0.0 version of the taxonomy content.
     */
    val version : String
        get() {
            val o = __offset(6)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                throw AssertionError("No value for (required) field version")
            }
        }
    val versionAsByteBuffer : ByteBuffer get() = __vector_as_bytebuffer(6, 1)
    fun versionInByteBuffer(_bb: ByteBuffer) : ByteBuffer = __vector_in_bytebuffer(_bb, 6, 1)
    /**
     * Unix seconds when this taxonomy revision was issued.
     */
    val issuedAt : ULong
        get() {
            val o = __offset(8)
            return if(o != 0) bb.getLong(o + bb_pos).toULong() else 0UL
        }
    /**
     * Human-readable title of the taxonomy.
     */
    val title : String?
        get() {
            val o = __offset(10)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val titleAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(10, 1)
    fun titleInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 10, 1)
    /**
     * The ratified categories. Every capabilityClass member a consumer may
     * encounter SHOULD appear exactly once; a code appearing twice is invalid.
     */
    fun categories(j: Int) : CCTCategory = categories(CCTCategory(), j)
    fun categories(obj: CCTCategory, j: Int) : CCTCategory {
        val o = __offset(12)
        return if (o != 0) {
            obj.__assign(__indirect(__vector(o) + j * 4), bb)
        } else {
            throw IndexOutOfBoundsException("Index out of range: $j, vector categories is empty")
        }
    }
    val categoriesLength : Int
        get() {
            val o = __offset(12); return if (o != 0) __vector_len(o) else 0
        }
    /**
     * Optional per-category item counts as observed over a named catalogue.
     * Absent means counts are computed by the consumer.
     */
    fun rollups(j: Int) : CCTCategoryRollup? = rollups(CCTCategoryRollup(), j)
    fun rollups(obj: CCTCategoryRollup, j: Int) : CCTCategoryRollup? {
        val o = __offset(14)
        return if (o != 0) {
            obj.__assign(__indirect(__vector(o) + j * 4), bb)
        } else {
            null
        }
    }
    val rollupsLength : Int
        get() {
            val o = __offset(14); return if (o != 0) __vector_len(o) else 0
        }
    /**
     * Signature from the publishing node key over the canonical taxonomy bytes.
     */
    fun signature(j: Int) : UByte {
        val o = __offset(16)
        return if (o != 0) {
            bb.get(__vector(o) + j * 1).toUByte()
        } else {
            0u
        }
    }
    val signatureLength : Int
        get() {
            val o = __offset(16); return if (o != 0) __vector_len(o) else 0
        }
    val signatureAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(16, 1)
    fun signatureInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 16, 1)
    companion object {
        fun validateVersion() = Constants.FLATBUFFERS_25_12_19()
        fun getRootAsCCT(_bb: ByteBuffer): CCT = getRootAsCCT(_bb, CCT())
        fun getRootAsCCT(_bb: ByteBuffer, obj: CCT): CCT {
            _bb.order(ByteOrder.LITTLE_ENDIAN)
            return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb))
        }
        fun CCTBufferHasIdentifier(_bb: ByteBuffer) : Boolean = __has_identifier(_bb, "$CCT")
        fun createCCT(builder: FlatBufferBuilder, taxonomyIdOffset: Int, versionOffset: Int, issuedAt: ULong, titleOffset: Int, categoriesOffset: Int, rollupsOffset: Int, signatureOffset: Int) : Int {
            builder.startTable(7)
            addISSUEDAT(builder, issuedAt)
            addSIGNATURE(builder, signatureOffset)
            addROLLUPS(builder, rollupsOffset)
            addCATEGORIES(builder, categoriesOffset)
            addTITLE(builder, titleOffset)
            addVERSION(builder, versionOffset)
            addTAXONOMYID(builder, taxonomyIdOffset)
            return endCCT(builder)
        }
        fun startCCT(builder: FlatBufferBuilder) = builder.startTable(7)
        fun addTAXONOMYID(builder: FlatBufferBuilder, taxonomyId: Int) = builder.addOffset(0, taxonomyId, 0)
        fun addVERSION(builder: FlatBufferBuilder, version: Int) = builder.addOffset(1, version, 0)
        fun addISSUEDAT(builder: FlatBufferBuilder, issuedAt: ULong) = builder.addLong(2, issuedAt.toLong(), 0)
        fun addTITLE(builder: FlatBufferBuilder, title: Int) = builder.addOffset(3, title, 0)
        fun addCATEGORIES(builder: FlatBufferBuilder, categories: Int) = builder.addOffset(4, categories, 0)
        fun createCategoriesVector(builder: FlatBufferBuilder, data: IntArray) : Int {
            builder.startVector(4, data.size, 4)
            for (i in data.size - 1 downTo 0) {
                builder.addOffset(data[i])
            }
            return builder.endVector()
        }
        fun startCategoriesVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(4, numElems, 4)
        fun addROLLUPS(builder: FlatBufferBuilder, rollups: Int) = builder.addOffset(5, rollups, 0)
        fun createRollupsVector(builder: FlatBufferBuilder, data: IntArray) : Int {
            builder.startVector(4, data.size, 4)
            for (i in data.size - 1 downTo 0) {
                builder.addOffset(data[i])
            }
            return builder.endVector()
        }
        fun startRollupsVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(4, numElems, 4)
        fun addSIGNATURE(builder: FlatBufferBuilder, signature: Int) = builder.addOffset(6, signature, 0)
        @kotlin.ExperimentalUnsignedTypes
        fun createSignatureVector(builder: FlatBufferBuilder, data: UByteArray) : Int {
            builder.startVector(1, data.size, 1)
            for (i in data.size - 1 downTo 0) {
                builder.addByte(data[i].toByte())
            }
            return builder.endVector()
        }
        fun startSignatureVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(1, numElems, 1)
        fun endCCT(builder: FlatBufferBuilder) : Int {
            val o = builder.endTable()
                builder.required(o, 4)
                builder.required(o, 6)
                builder.required(o, 12)
            return o
        }
        fun finishCCTBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finish(offset, "$CCT")
        fun finishSizePrefixedCCTBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finishSizePrefixed(offset, "$CCT")
    }
}
