// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector
import com.google.flatbuffers.BooleanVector
import com.google.flatbuffers.ByteVector
import com.google.flatbuffers.Constants
import com.google.flatbuffers.DoubleVector
import com.google.flatbuffers.FlatBufferBuilder
import com.google.flatbuffers.FloatVector
import com.google.flatbuffers.LongVector
import com.google.flatbuffers.StringVector
import com.google.flatbuffers.Struct
import com.google.flatbuffers.Table
import com.google.flatbuffers.UnionVector
import java.nio.ByteBuffer
import java.nio.ByteOrder
import kotlin.math.sign

/**
 * Result of one Basilisk-compatible Gauss-Markov random sequence.
 */
@Suppress("unused")
class NUMGaussMarkovResult : Table() {

    fun __init(_i: Int, _bb: ByteBuffer)  {
        __reset(_i, _bb)
    }
    fun __assign(_i: Int, _bb: ByteBuffer) : NUMGaussMarkovResult {
        __init(_i, _bb)
        return this
    }
    val status : Byte
        get() {
            val o = __offset(4)
            return if(o != 0) bb.get(o + bb_pos) else 0
        }
    val errorMessage : String?
        get() {
            val o = __offset(6)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val errorMessageAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(6, 1)
    fun errorMessageInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 6, 1)
    /**
     * Post-warmup per-state sample means.
     */
    fun mean(j: Int) : Double {
        val o = __offset(8)
        return if (o != 0) {
            bb.getDouble(__vector(o) + j * 8)
        } else {
            0.0
        }
    }
    val meanLength : Int
        get() {
            val o = __offset(8); return if (o != 0) __vector_len(o) else 0
        }
    val meanAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(8, 8)
    fun meanInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 8, 8)
    /**
     * Post-warmup per-state sample standard deviations with n - 1 denominator.
     */
    fun standardDeviation(j: Int) : Double {
        val o = __offset(10)
        return if (o != 0) {
            bb.getDouble(__vector(o) + j * 8)
        } else {
            0.0
        }
    }
    val standardDeviationLength : Int
        get() {
            val o = __offset(10); return if (o != 0) __vector_len(o) else 0
        }
    val standardDeviationAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(10, 8)
    fun standardDeviationInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 10, 8)
    /**
     * Post-warmup per-state minima.
     */
    fun minimum(j: Int) : Double {
        val o = __offset(12)
        return if (o != 0) {
            bb.getDouble(__vector(o) + j * 8)
        } else {
            0.0
        }
    }
    val minimumLength : Int
        get() {
            val o = __offset(12); return if (o != 0) __vector_len(o) else 0
        }
    val minimumAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(12, 8)
    fun minimumInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 12, 8)
    /**
     * Post-warmup per-state maxima.
     */
    fun maximum(j: Int) : Double {
        val o = __offset(14)
        return if (o != 0) {
            bb.getDouble(__vector(o) + j * 8)
        } else {
            0.0
        }
    }
    val maximumLength : Int
        get() {
            val o = __offset(14); return if (o != 0) __vector_len(o) else 0
        }
    val maximumAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(14, 8)
    fun maximumInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 14, 8)
    /**
     * State after the final generated sample.
     */
    fun finalState(j: Int) : Double {
        val o = __offset(16)
        return if (o != 0) {
            bb.getDouble(__vector(o) + j * 8)
        } else {
            0.0
        }
    }
    val finalStateLength : Int
        get() {
            val o = __offset(16); return if (o != 0) __vector_len(o) else 0
        }
    val finalStateAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(16, 8)
    fun finalStateInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 16, 8)
    /**
     * Optional flattened sample-major state history.
     */
    fun samples(j: Int) : Double {
        val o = __offset(18)
        return if (o != 0) {
            bb.getDouble(__vector(o) + j * 8)
        } else {
            0.0
        }
    }
    val samplesLength : Int
        get() {
            val o = __offset(18); return if (o != 0) __vector_len(o) else 0
        }
    val samplesAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(18, 8)
    fun samplesInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 18, 8)
    /**
     * Caller trace/correlation identifier copied from the request when present.
     */
    val traceId : String?
        get() {
            val o = __offset(20)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val traceIdAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(20, 1)
    fun traceIdInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 20, 1)
    companion object {
        fun validateVersion() = Constants.FLATBUFFERS_25_12_19()
        fun getRootAsNUMGaussMarkovResult(_bb: ByteBuffer): NUMGaussMarkovResult = getRootAsNUMGaussMarkovResult(_bb, NUMGaussMarkovResult())
        fun getRootAsNUMGaussMarkovResult(_bb: ByteBuffer, obj: NUMGaussMarkovResult): NUMGaussMarkovResult {
            _bb.order(ByteOrder.LITTLE_ENDIAN)
            return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb))
        }
        fun createNUMGaussMarkovResult(builder: FlatBufferBuilder, status: Byte, errorMessageOffset: Int, meanOffset: Int, standardDeviationOffset: Int, minimumOffset: Int, maximumOffset: Int, finalStateOffset: Int, samplesOffset: Int, traceIdOffset: Int) : Int {
            builder.startTable(9)
            addTRACEID(builder, traceIdOffset)
            addSAMPLES(builder, samplesOffset)
            addFINALSTATE(builder, finalStateOffset)
            addMAXIMUM(builder, maximumOffset)
            addMINIMUM(builder, minimumOffset)
            addSTANDARDDEVIATION(builder, standardDeviationOffset)
            addMEAN(builder, meanOffset)
            addERRORMESSAGE(builder, errorMessageOffset)
            addSTATUS(builder, status)
            return endNUMGaussMarkovResult(builder)
        }
        fun startNUMGaussMarkovResult(builder: FlatBufferBuilder) = builder.startTable(9)
        fun addSTATUS(builder: FlatBufferBuilder, status: Byte) = builder.addByte(0, status, 0)
        fun addERRORMESSAGE(builder: FlatBufferBuilder, errorMessage: Int) = builder.addOffset(1, errorMessage, 0)
        fun addMEAN(builder: FlatBufferBuilder, mean: Int) = builder.addOffset(2, mean, 0)
        fun createMeanVector(builder: FlatBufferBuilder, data: DoubleArray) : Int {
            builder.startVector(8, data.size, 8)
            for (i in data.size - 1 downTo 0) {
                builder.addDouble(data[i])
            }
            return builder.endVector()
        }
        fun startMeanVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(8, numElems, 8)
        fun addSTANDARDDEVIATION(builder: FlatBufferBuilder, standardDeviation: Int) = builder.addOffset(3, standardDeviation, 0)
        fun createStandardDeviationVector(builder: FlatBufferBuilder, data: DoubleArray) : Int {
            builder.startVector(8, data.size, 8)
            for (i in data.size - 1 downTo 0) {
                builder.addDouble(data[i])
            }
            return builder.endVector()
        }
        fun startStandardDeviationVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(8, numElems, 8)
        fun addMINIMUM(builder: FlatBufferBuilder, minimum: Int) = builder.addOffset(4, minimum, 0)
        fun createMinimumVector(builder: FlatBufferBuilder, data: DoubleArray) : Int {
            builder.startVector(8, data.size, 8)
            for (i in data.size - 1 downTo 0) {
                builder.addDouble(data[i])
            }
            return builder.endVector()
        }
        fun startMinimumVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(8, numElems, 8)
        fun addMAXIMUM(builder: FlatBufferBuilder, maximum: Int) = builder.addOffset(5, maximum, 0)
        fun createMaximumVector(builder: FlatBufferBuilder, data: DoubleArray) : Int {
            builder.startVector(8, data.size, 8)
            for (i in data.size - 1 downTo 0) {
                builder.addDouble(data[i])
            }
            return builder.endVector()
        }
        fun startMaximumVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(8, numElems, 8)
        fun addFINALSTATE(builder: FlatBufferBuilder, finalState: Int) = builder.addOffset(6, finalState, 0)
        fun createFinalStateVector(builder: FlatBufferBuilder, data: DoubleArray) : Int {
            builder.startVector(8, data.size, 8)
            for (i in data.size - 1 downTo 0) {
                builder.addDouble(data[i])
            }
            return builder.endVector()
        }
        fun startFinalStateVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(8, numElems, 8)
        fun addSAMPLES(builder: FlatBufferBuilder, samples: Int) = builder.addOffset(7, samples, 0)
        fun createSamplesVector(builder: FlatBufferBuilder, data: DoubleArray) : Int {
            builder.startVector(8, data.size, 8)
            for (i in data.size - 1 downTo 0) {
                builder.addDouble(data[i])
            }
            return builder.endVector()
        }
        fun startSamplesVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(8, numElems, 8)
        fun addTRACEID(builder: FlatBufferBuilder, traceId: Int) = builder.addOffset(8, traceId, 0)
        fun endNUMGaussMarkovResult(builder: FlatBufferBuilder) : Int {
            val o = builder.endTable()
            return o
        }
    }
}
