// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector
import com.google.flatbuffers.BooleanVector
import com.google.flatbuffers.ByteVector
import com.google.flatbuffers.Constants
import com.google.flatbuffers.DoubleVector
import com.google.flatbuffers.FlatBufferBuilder
import com.google.flatbuffers.FloatVector
import com.google.flatbuffers.LongVector
import com.google.flatbuffers.StringVector
import com.google.flatbuffers.Struct
import com.google.flatbuffers.Table
import com.google.flatbuffers.UnionVector
import java.nio.ByteBuffer
import java.nio.ByteOrder
import kotlin.math.sign

/**
 * Compressed Packet Stream — a run of fixed-length CCSDS packets losslessly
 * compressed as one stateful stream (CCSDS 124.0-B-1 POCKET+). Decoded output
 * is a sequence of PACKET_COUNT packets of PACKET_LENGTH bytes, each of which
 * projects to an $SPP record. This record carries a compressed packet stream
 * only; it is not a general-purpose compression envelope for SDS records.
 */
@Suppress("unused")
class CPS : Table() {

    fun __init(_i: Int, _bb: ByteBuffer)  {
        __reset(_i, _bb)
    }
    fun __assign(_i: Int, _bb: ByteBuffer) : CPS {
        __init(_i, _bb)
        return this
    }
    /**
     * Compression algorithm used to produce COMPRESSED_DATA.
     */
    val algorithm : UByte
        get() {
            val o = __offset(4)
            return if(o != 0) bb.get(o + bb_pos).toUByte() else 0u
        }
    /**
     * Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
     * defined only for a fixed packet length; a length change starts a new
     * stream and therefore a new $CPS record.
     */
    val packetLength : UShort
        get() {
            val o = __offset(6)
            return if(o != 0) bb.getShort(o + bb_pos).toUShort() else 0u
        }
    /**
     * Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
     * this is the only output-size bound a decoder has. The compressed
     * bitstream alone does not bound the decoded size, and a compression-ratio
     * guess is unsafe in both directions. Decoders MUST reject a record whose
     * PACKET_COUNT is zero, and MUST allocate exactly
     * PACKET_COUNT * PACKET_LENGTH bytes of output.
     */
    val packetCount : UInt
        get() {
            val o = __offset(8)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
     * a window depth, NOT a retransmission interval — it does not by itself
     * configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
     * REFERENCE_PERIOD, which are three independent periods.
     */
    val robustness : UByte
        get() {
            val o = __offset(10)
            return if(o != 0) bb.get(o + bb_pos).toUByte() else 0u
        }
    /**
     * POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
     * MASK_SEND_PERIOD, and REFERENCE_PERIOD.
     */
    val maskUpdatePeriod : UShort
        get() {
            val o = __offset(12)
            return if(o != 0) bb.getShort(o + bb_pos).toUShort() else 0u
        }
    /**
     * POCKET+ mask send period (ft), in packets — how often the change mask is
     * transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
     * REFERENCE_PERIOD.
     */
    val maskSendPeriod : UShort
        get() {
            val o = __offset(14)
            return if(o != 0) bb.getShort(o + bb_pos).toUShort() else 0u
        }
    /**
     * POCKET+ reference packet retransmission period (rt), in packets.
     * Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
     */
    val referencePeriod : UShort
        get() {
            val o = __offset(16)
            return if(o != 0) bb.getShort(o + bb_pos).toUShort() else 0u
        }
    /**
     * True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
     * ft=1, i.e. it transmits a full reference packet and a full mask, so this
     * record decodes standalone without its predecessor. There is no
     * synchronization flag in the POCKET+ bitstream itself: this field is the
     * producer's assertion about the first packet, and defaults to false.
     */
    val selfSynchronizing : Boolean
        get() {
            val o = __offset(18)
            return if(o != 0) 0.toByte() != bb.get(o + bb_pos) else false
        }
    /**
     * Producer-defined stream identity. Records sharing STREAM_ID decode in
     * SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
     * poisons every subsequent segment up to the next segment with
     * SELF_SYNCHRONIZING = true.
     */
    val streamId : String?
        get() {
            val o = __offset(20)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val streamIdAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(20, 1)
    fun streamIdInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 20, 1)
    /**
     * Zero-based index of this segment within STREAM_ID.
     */
    val segmentIndex : UInt
        get() {
            val o = __offset(22)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * CCSDS Application Process Identifier of the packets, when known.
     */
    val apid : UShort
        get() {
            val o = __offset(24)
            return if(o != 0) bb.getShort(o + bb_pos).toUShort() else 0u
        }
    /**
     * Spacecraft identifier of the packets, when known.
     */
    val spacecraftId : UShort
        get() {
            val o = __offset(26)
            return if(o != 0) bb.getShort(o + bb_pos).toUShort() else 0u
        }
    /**
     * UTC epoch of the first packet in the segment, ISO 8601.
     */
    val startEpoch : String?
        get() {
            val o = __offset(28)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val startEpochAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(28, 1)
    fun startEpochInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 28, 1)
    /**
     * UTC epoch of the last packet in the segment, ISO 8601.
     */
    val stopEpoch : String?
        get() {
            val o = __offset(30)
            return if (o != 0) {
                __string(o + bb_pos)
            } else {
                null
            }
        }
    val stopEpochAsByteBuffer : ByteBuffer? get() = __vector_as_bytebuffer(30, 1)
    fun stopEpochInByteBuffer(_bb: ByteBuffer) : ByteBuffer? = __vector_in_bytebuffer(_bb, 30, 1)
    /**
     * The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
     * packet is individually octet-aligned; padding is per PACKET, never per
     * stream. Bit-concatenating packets diverges from the reference
     * implementation and MUST NOT be done.
     */
    fun compressedData(j: Int) : UByte {
        val o = __offset(32)
        return if (o != 0) {
            bb.get(__vector(o) + j * 1).toUByte()
        } else {
            throw IndexOutOfBoundsException("Index out of range: $j, vector compressedData is empty")
        }
    }
    val compressedDataLength : Int
        get() {
            val o = __offset(32); return if (o != 0) __vector_len(o) else 0
        }
    val compressedDataAsByteBuffer : ByteBuffer get() = __vector_as_bytebuffer(32, 1)
    fun compressedDataInByteBuffer(_bb: ByteBuffer) : ByteBuffer = __vector_in_bytebuffer(_bb, 32, 1)
    /**
     * PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
     * output length without recomputing the product from a corrupt header.
     */
    val uncompressedLength : UInt
        get() {
            val o = __offset(34)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    /**
     * CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
     */
    val uncompressedCrc32 : UInt
        get() {
            val o = __offset(36)
            return if(o != 0) bb.getInt(o + bb_pos).toUInt() else 0u
        }
    companion object {
        fun validateVersion() = Constants.FLATBUFFERS_25_12_19()
        fun getRootAsCPS(_bb: ByteBuffer): CPS = getRootAsCPS(_bb, CPS())
        fun getRootAsCPS(_bb: ByteBuffer, obj: CPS): CPS {
            _bb.order(ByteOrder.LITTLE_ENDIAN)
            return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb))
        }
        fun CPSBufferHasIdentifier(_bb: ByteBuffer) : Boolean = __has_identifier(_bb, "$CPS")
        fun createCPS(builder: FlatBufferBuilder, algorithm: UByte, packetLength: UShort, packetCount: UInt, robustness: UByte, maskUpdatePeriod: UShort, maskSendPeriod: UShort, referencePeriod: UShort, selfSynchronizing: Boolean, streamIdOffset: Int, segmentIndex: UInt, apid: UShort, spacecraftId: UShort, startEpochOffset: Int, stopEpochOffset: Int, compressedDataOffset: Int, uncompressedLength: UInt, uncompressedCrc32: UInt) : Int {
            builder.startTable(17)
            addUNCOMPRESSEDCRC32(builder, uncompressedCrc32)
            addUNCOMPRESSEDLENGTH(builder, uncompressedLength)
            addCOMPRESSEDDATA(builder, compressedDataOffset)
            addSTOPEPOCH(builder, stopEpochOffset)
            addSTARTEPOCH(builder, startEpochOffset)
            addSEGMENTINDEX(builder, segmentIndex)
            addSTREAMID(builder, streamIdOffset)
            addPACKETCOUNT(builder, packetCount)
            addSPACECRAFTID(builder, spacecraftId)
            addAPID(builder, apid)
            addREFERENCEPERIOD(builder, referencePeriod)
            addMASKSENDPERIOD(builder, maskSendPeriod)
            addMASKUPDATEPERIOD(builder, maskUpdatePeriod)
            addPACKETLENGTH(builder, packetLength)
            addSELFSYNCHRONIZING(builder, selfSynchronizing)
            addROBUSTNESS(builder, robustness)
            addALGORITHM(builder, algorithm)
            return endCPS(builder)
        }
        fun startCPS(builder: FlatBufferBuilder) = builder.startTable(17)
        fun addALGORITHM(builder: FlatBufferBuilder, algorithm: UByte) = builder.addByte(0, algorithm.toByte(), 0)
        fun addPACKETLENGTH(builder: FlatBufferBuilder, packetLength: UShort) = builder.addShort(1, packetLength.toShort(), 0)
        fun addPACKETCOUNT(builder: FlatBufferBuilder, packetCount: UInt) = builder.addInt(2, packetCount.toInt(), 0)
        fun addROBUSTNESS(builder: FlatBufferBuilder, robustness: UByte) = builder.addByte(3, robustness.toByte(), 0)
        fun addMASKUPDATEPERIOD(builder: FlatBufferBuilder, maskUpdatePeriod: UShort) = builder.addShort(4, maskUpdatePeriod.toShort(), 0)
        fun addMASKSENDPERIOD(builder: FlatBufferBuilder, maskSendPeriod: UShort) = builder.addShort(5, maskSendPeriod.toShort(), 0)
        fun addREFERENCEPERIOD(builder: FlatBufferBuilder, referencePeriod: UShort) = builder.addShort(6, referencePeriod.toShort(), 0)
        fun addSELFSYNCHRONIZING(builder: FlatBufferBuilder, selfSynchronizing: Boolean) = builder.addBoolean(7, selfSynchronizing, false)
        fun addSTREAMID(builder: FlatBufferBuilder, streamId: Int) = builder.addOffset(8, streamId, 0)
        fun addSEGMENTINDEX(builder: FlatBufferBuilder, segmentIndex: UInt) = builder.addInt(9, segmentIndex.toInt(), 0)
        fun addAPID(builder: FlatBufferBuilder, apid: UShort) = builder.addShort(10, apid.toShort(), 0)
        fun addSPACECRAFTID(builder: FlatBufferBuilder, spacecraftId: UShort) = builder.addShort(11, spacecraftId.toShort(), 0)
        fun addSTARTEPOCH(builder: FlatBufferBuilder, startEpoch: Int) = builder.addOffset(12, startEpoch, 0)
        fun addSTOPEPOCH(builder: FlatBufferBuilder, stopEpoch: Int) = builder.addOffset(13, stopEpoch, 0)
        fun addCOMPRESSEDDATA(builder: FlatBufferBuilder, compressedData: Int) = builder.addOffset(14, compressedData, 0)
        @kotlin.ExperimentalUnsignedTypes
        fun createCompressedDataVector(builder: FlatBufferBuilder, data: UByteArray) : Int {
            builder.startVector(1, data.size, 1)
            for (i in data.size - 1 downTo 0) {
                builder.addByte(data[i].toByte())
            }
            return builder.endVector()
        }
        fun startCompressedDataVector(builder: FlatBufferBuilder, numElems: Int) = builder.startVector(1, numElems, 1)
        fun addUNCOMPRESSEDLENGTH(builder: FlatBufferBuilder, uncompressedLength: UInt) = builder.addInt(15, uncompressedLength.toInt(), 0)
        fun addUNCOMPRESSEDCRC32(builder: FlatBufferBuilder, uncompressedCrc32: UInt) = builder.addInt(16, uncompressedCrc32.toInt(), 0)
        fun endCPS(builder: FlatBufferBuilder) : Int {
            val o = builder.endTable()
                builder.required(o, 32)
            return o
        }
        fun finishCPSBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finish(offset, "$CPS")
        fun finishSizePrefixedCPSBuffer(builder: FlatBufferBuilder, offset: Int) = builder.finishSizePrefixed(offset, "$CPS")
    }
}
