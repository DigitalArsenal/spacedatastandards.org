// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * A row that answers a CONE or FIELD_OF_VIEW query.
 */
@SuppressWarnings("unused")
public final class SKRHit extends Struct {
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public SKRHit __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  public long ROW() { return (long)bb.getInt(bb_pos + 0) & 0xFFFFFFFFL; }
  /**
   * SKTPoint FLAGS bits for the row.
   */
  public long FLAGS() { return (long)bb.getInt(bb_pos + 4) & 0xFFFFFFFFL; }
  public double RA_DEG() { return bb.getDouble(bb_pos + 8); }
  public double DEC_DEG() { return bb.getDouble(bb_pos + 16); }
  /**
   * Angle from the centre or boresight.
   */
  public double SEPARATION_DEG() { return bb.getDouble(bb_pos + 24); }
  /**
   * Field coordinates: angles along the field's x and y axes (the
   * standard coordinates of a cone query). NaN behind the tangent plane.
   */
  public double FIELD_X_DEG() { return bb.getDouble(bb_pos + 32); }
  public double FIELD_Y_DEG() { return bb.getDouble(bb_pos + 40); }
  /**
   * Magnitude in the query's band; NaN when unknown.
   */
  public float MAG() { return bb.getFloat(bb_pos + 48); }
  public long RESERVED() { return (long)bb.getInt(bb_pos + 52) & 0xFFFFFFFFL; }

  public static int createSKRHit(FlatBufferBuilder builder, long ROW, long FLAGS, double RA_DEG, double DEC_DEG, double SEPARATION_DEG, double FIELD_X_DEG, double FIELD_Y_DEG, float MAG, long RESERVED) {
    builder.prep(8, 56);
    builder.putInt((int) RESERVED);
    builder.putFloat(MAG);
    builder.putDouble(FIELD_Y_DEG);
    builder.putDouble(FIELD_X_DEG);
    builder.putDouble(SEPARATION_DEG);
    builder.putDouble(DEC_DEG);
    builder.putDouble(RA_DEG);
    builder.putInt((int) FLAGS);
    builder.putInt((int) ROW);
    return builder.offset();
  }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public SKRHit get(int j) { return get(new SKRHit(), j); }
    public SKRHit get(SKRHit obj, int j) {  return obj.__assign(__element(j), bb); }
  }
}
