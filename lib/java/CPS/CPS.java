// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * Compressed Packet Stream — a run of fixed-length CCSDS packets losslessly
 * compressed as one stateful stream (CCSDS 124.0-B-1 POCKET+). Decoded output
 * is a sequence of PACKET_COUNT packets of PACKET_LENGTH bytes, each of which
 * projects to an $SPP record. This record carries a compressed packet stream
 * only; it is not a general-purpose compression envelope for SDS records.
 */
@SuppressWarnings("unused")
public final class CPS extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_12_19(); }
  public static CPS getRootAsCPS(ByteBuffer _bb) { return getRootAsCPS(_bb, new CPS()); }
  public static CPS getRootAsCPS(ByteBuffer _bb, CPS obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public static boolean CPSBufferHasIdentifier(ByteBuffer _bb) { return __has_identifier(_bb, "$CPS"); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public CPS __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /**
   * Compression algorithm used to produce COMPRESSED_DATA.
   */
  public int ALGORITHM() { int o = __offset(4); return o != 0 ? bb.get(o + bb_pos) & 0xFF : 0; }
  /**
   * Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
   * defined only for a fixed packet length; a length change starts a new
   * stream and therefore a new $CPS record.
   */
  public int PACKET_LENGTH() { int o = __offset(6); return o != 0 ? bb.getShort(o + bb_pos) & 0xFFFF : 0; }
  /**
   * Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
   * this is the only output-size bound a decoder has. The compressed
   * bitstream alone does not bound the decoded size, and a compression-ratio
   * guess is unsafe in both directions. Decoders MUST reject a record whose
   * PACKET_COUNT is zero, and MUST allocate exactly
   * PACKET_COUNT * PACKET_LENGTH bytes of output.
   */
  public long PACKET_COUNT() { int o = __offset(8); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
   * a window depth, NOT a retransmission interval — it does not by itself
   * configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
   * REFERENCE_PERIOD, which are three independent periods.
   */
  public int ROBUSTNESS() { int o = __offset(10); return o != 0 ? bb.get(o + bb_pos) & 0xFF : 0; }
  /**
   * POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
   * MASK_SEND_PERIOD, and REFERENCE_PERIOD.
   */
  public int MASK_UPDATE_PERIOD() { int o = __offset(12); return o != 0 ? bb.getShort(o + bb_pos) & 0xFFFF : 0; }
  /**
   * POCKET+ mask send period (ft), in packets — how often the change mask is
   * transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
   * REFERENCE_PERIOD.
   */
  public int MASK_SEND_PERIOD() { int o = __offset(14); return o != 0 ? bb.getShort(o + bb_pos) & 0xFFFF : 0; }
  /**
   * POCKET+ reference packet retransmission period (rt), in packets.
   * Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
   */
  public int REFERENCE_PERIOD() { int o = __offset(16); return o != 0 ? bb.getShort(o + bb_pos) & 0xFFFF : 0; }
  /**
   * True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
   * ft=1, i.e. it transmits a full reference packet and a full mask, so this
   * record decodes standalone without its predecessor. There is no
   * synchronization flag in the POCKET+ bitstream itself: this field is the
   * producer's assertion about the first packet, and defaults to false.
   */
  public boolean SELF_SYNCHRONIZING() { int o = __offset(18); return o != 0 ? 0!=bb.get(o + bb_pos) : false; }
  /**
   * Producer-defined stream identity. Records sharing STREAM_ID decode in
   * SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
   * poisons every subsequent segment up to the next segment with
   * SELF_SYNCHRONIZING = true.
   */
  public String STREAM_ID() { int o = __offset(20); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer STREAM_IDAsByteBuffer() { return __vector_as_bytebuffer(20, 1); }
  public ByteBuffer STREAM_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 20, 1); }
  /**
   * Zero-based index of this segment within STREAM_ID.
   */
  public long SEGMENT_INDEX() { int o = __offset(22); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * CCSDS Application Process Identifier of the packets, when known.
   */
  public int APID() { int o = __offset(24); return o != 0 ? bb.getShort(o + bb_pos) & 0xFFFF : 0; }
  /**
   * Spacecraft identifier of the packets, when known.
   */
  public int SPACECRAFT_ID() { int o = __offset(26); return o != 0 ? bb.getShort(o + bb_pos) & 0xFFFF : 0; }
  /**
   * UTC epoch of the first packet in the segment, ISO 8601.
   */
  public String START_EPOCH() { int o = __offset(28); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer START_EPOCHAsByteBuffer() { return __vector_as_bytebuffer(28, 1); }
  public ByteBuffer START_EPOCHInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 28, 1); }
  /**
   * UTC epoch of the last packet in the segment, ISO 8601.
   */
  public String STOP_EPOCH() { int o = __offset(30); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer STOP_EPOCHAsByteBuffer() { return __vector_as_bytebuffer(30, 1); }
  public ByteBuffer STOP_EPOCHInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 30, 1); }
  /**
   * The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
   * packet is individually octet-aligned; padding is per PACKET, never per
   * stream. Bit-concatenating packets diverges from the reference
   * implementation and MUST NOT be done.
   */
  public int COMPRESSED_DATA(int j) { int o = __offset(32); return o != 0 ? bb.get(__vector(o) + j * 1) & 0xFF : 0; }
  public int COMPRESSED_DATALength() { int o = __offset(32); return o != 0 ? __vector_len(o) : 0; }
  public ByteVector compressedDataVector() { return compressedDataVector(new ByteVector()); }
  public ByteVector compressedDataVector(ByteVector obj) { int o = __offset(32); return o != 0 ? obj.__assign(__vector(o), bb) : null; }
  public ByteBuffer COMPRESSED_DATAAsByteBuffer() { return __vector_as_bytebuffer(32, 1); }
  public ByteBuffer COMPRESSED_DATAInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 32, 1); }
  /**
   * PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
   * output length without recomputing the product from a corrupt header.
   */
  public long UNCOMPRESSED_LENGTH() { int o = __offset(34); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
   */
  public long UNCOMPRESSED_CRC32() { int o = __offset(36); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }

  public static int createCPS(FlatBufferBuilder builder,
      int ALGORITHM,
      int PACKET_LENGTH,
      long PACKET_COUNT,
      int ROBUSTNESS,
      int MASK_UPDATE_PERIOD,
      int MASK_SEND_PERIOD,
      int REFERENCE_PERIOD,
      boolean SELF_SYNCHRONIZING,
      int STREAM_IDOffset,
      long SEGMENT_INDEX,
      int APID,
      int SPACECRAFT_ID,
      int START_EPOCHOffset,
      int STOP_EPOCHOffset,
      int COMPRESSED_DATAOffset,
      long UNCOMPRESSED_LENGTH,
      long UNCOMPRESSED_CRC32) {
    builder.startTable(17);
    CPS.addUncompressedCrc32(builder, UNCOMPRESSED_CRC32);
    CPS.addUncompressedLength(builder, UNCOMPRESSED_LENGTH);
    CPS.addCompressedData(builder, COMPRESSED_DATAOffset);
    CPS.addStopEpoch(builder, STOP_EPOCHOffset);
    CPS.addStartEpoch(builder, START_EPOCHOffset);
    CPS.addSegmentIndex(builder, SEGMENT_INDEX);
    CPS.addStreamId(builder, STREAM_IDOffset);
    CPS.addPacketCount(builder, PACKET_COUNT);
    CPS.addSpacecraftId(builder, SPACECRAFT_ID);
    CPS.addApid(builder, APID);
    CPS.addReferencePeriod(builder, REFERENCE_PERIOD);
    CPS.addMaskSendPeriod(builder, MASK_SEND_PERIOD);
    CPS.addMaskUpdatePeriod(builder, MASK_UPDATE_PERIOD);
    CPS.addPacketLength(builder, PACKET_LENGTH);
    CPS.addSelfSynchronizing(builder, SELF_SYNCHRONIZING);
    CPS.addRobustness(builder, ROBUSTNESS);
    CPS.addAlgorithm(builder, ALGORITHM);
    return CPS.endCPS(builder);
  }

  public static void startCPS(FlatBufferBuilder builder) { builder.startTable(17); }
  public static void addAlgorithm(FlatBufferBuilder builder, int ALGORITHM) { builder.addByte(0, (byte) ALGORITHM, (byte) 0); }
  public static void addPacketLength(FlatBufferBuilder builder, int PACKET_LENGTH) { builder.addShort(1, (short) PACKET_LENGTH, (short) 0); }
  public static void addPacketCount(FlatBufferBuilder builder, long PACKET_COUNT) { builder.addInt(2, (int) PACKET_COUNT, (int) 0L); }
  public static void addRobustness(FlatBufferBuilder builder, int ROBUSTNESS) { builder.addByte(3, (byte) ROBUSTNESS, (byte) 0); }
  public static void addMaskUpdatePeriod(FlatBufferBuilder builder, int MASK_UPDATE_PERIOD) { builder.addShort(4, (short) MASK_UPDATE_PERIOD, (short) 0); }
  public static void addMaskSendPeriod(FlatBufferBuilder builder, int MASK_SEND_PERIOD) { builder.addShort(5, (short) MASK_SEND_PERIOD, (short) 0); }
  public static void addReferencePeriod(FlatBufferBuilder builder, int REFERENCE_PERIOD) { builder.addShort(6, (short) REFERENCE_PERIOD, (short) 0); }
  public static void addSelfSynchronizing(FlatBufferBuilder builder, boolean SELF_SYNCHRONIZING) { builder.addBoolean(7, SELF_SYNCHRONIZING, false); }
  public static void addStreamId(FlatBufferBuilder builder, int STREAM_IDOffset) { builder.addOffset(8, STREAM_IDOffset, 0); }
  public static void addSegmentIndex(FlatBufferBuilder builder, long SEGMENT_INDEX) { builder.addInt(9, (int) SEGMENT_INDEX, (int) 0L); }
  public static void addApid(FlatBufferBuilder builder, int APID) { builder.addShort(10, (short) APID, (short) 0); }
  public static void addSpacecraftId(FlatBufferBuilder builder, int SPACECRAFT_ID) { builder.addShort(11, (short) SPACECRAFT_ID, (short) 0); }
  public static void addStartEpoch(FlatBufferBuilder builder, int START_EPOCHOffset) { builder.addOffset(12, START_EPOCHOffset, 0); }
  public static void addStopEpoch(FlatBufferBuilder builder, int STOP_EPOCHOffset) { builder.addOffset(13, STOP_EPOCHOffset, 0); }
  public static void addCompressedData(FlatBufferBuilder builder, int COMPRESSED_DATAOffset) { builder.addOffset(14, COMPRESSED_DATAOffset, 0); }
  public static int createCompressedDataVector(FlatBufferBuilder builder, byte[] data) { return builder.createByteVector(data); }
  public static int createCompressedDataVector(FlatBufferBuilder builder, ByteBuffer data) { return builder.createByteVector(data); }
  public static void startCompressedDataVector(FlatBufferBuilder builder, int numElems) { builder.startVector(1, numElems, 1); }
  public static void addUncompressedLength(FlatBufferBuilder builder, long UNCOMPRESSED_LENGTH) { builder.addInt(15, (int) UNCOMPRESSED_LENGTH, (int) 0L); }
  public static void addUncompressedCrc32(FlatBufferBuilder builder, long UNCOMPRESSED_CRC32) { builder.addInt(16, (int) UNCOMPRESSED_CRC32, (int) 0L); }
  public static int endCPS(FlatBufferBuilder builder) {
    int o = builder.endTable();
    builder.required(o, 32);  // COMPRESSED_DATA
    return o;
  }
  public static void finishCPSBuffer(FlatBufferBuilder builder, int offset) { builder.finish(offset, "$CPS"); }
  public static void finishSizePrefixedCPSBuffer(FlatBufferBuilder builder, int offset) { builder.finishSizePrefixed(offset, "$CPS"); }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public CPS get(int j) { return get(new CPS(), j); }
    public CPS get(CPS obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
  }
}
