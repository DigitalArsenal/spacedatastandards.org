// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * Everything needed to decide whether this card is servable, and whether two
 * implementations produced the same bytes.
 */
@SuppressWarnings("unused")
public final class VCFConformance extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_12_19(); }
  public static VCFConformance getRootAsVCFConformance(ByteBuffer _bb) { return getRootAsVCFConformance(_bb, new VCFConformance()); }
  public static VCFConformance getRootAsVCFConformance(ByteBuffer _bb, VCFConformance obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public VCFConformance __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /**
   * Card format version token verbatim as emitted, e.g. `3.0`.
   */
  public String VERSION() { int o = __offset(4); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer VERSIONAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer VERSIONInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
  /**
   * Identifier of the emission profile the card conforms to, verbatim, naming
   * the annex revision the publisher implemented.
   */
  public String PROFILE() { int o = __offset(6); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer PROFILEAsByteBuffer() { return __vector_as_bytebuffer(6, 1); }
  public ByteBuffer PROFILEInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 6, 1); }
  /**
   * Line terminator the card text uses.
   */
  public byte LINE_ENDING() { int o = __offset(8); return o != 0 ? bb.get(o + bb_pos) : 0; }
  /**
   * Octet width beyond which a content line is folded onto continuation lines.
   * 0 means the publisher folded nothing, which is non-canonical.
   */
  public long FOLD_OCTET_WIDTH() { int o = __offset(10); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Octet length of `CARD` exactly as emitted, folding and terminators
   * included. This is the number a serving surface budgets against.
   */
  public long BYTE_LENGTH() { int o = __offset(12); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Octet length the same card would have with folding removed. Carried so a
   * folding disagreement is detectable without unfolding the text.
   */
  public long UNFOLDED_BYTE_LENGTH() { int o = __offset(14); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Octet ceiling of the surface this card was produced for. 0 means the
   * publisher states no ceiling; it never means a ceiling of zero.
   */
  public long BYTE_BUDGET() { int o = __offset(16); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Whether BYTE_LENGTH is within BYTE_BUDGET. False with a non-zero budget
   * means the card is a valid record that is NOT servable on that surface.
   */
  public boolean WITHIN_BYTE_BUDGET() { int o = __offset(18); return o != 0 ? 0!=bb.get(o + bb_pos) : false; }
  /**
   * SHA-256 of `CARD` as 64 lowercase hexadecimal characters. This is the
   * value two implementations compare to prove byte-identical projection.
   */
  public String CARD_SHA256() { int o = __offset(20); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer CARD_SHA256AsByteBuffer() { return __vector_as_bytebuffer(20, 1); }
  public ByteBuffer CARD_SHA256InByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 20, 1); }
  /**
   * Number of property rows emitted, unfolded. Carried so a truncated card is
   * detectable before parsing.
   */
  public long PROPERTY_COUNT() { int o = __offset(22); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Whether the card carries a complete verification chain: a signing-key row,
   * an encryption-key row, and a profile-signature row, all present in the
   * UNFOLDED text. A card without all three cannot verify its own subject and
   * is not servable as an identity card.
   */
  public boolean CARRIES_VERIFICATION_CHAIN() { int o = __offset(24); return o != 0 ? 0!=bb.get(o + bb_pos) : false; }

  public static int createVCFConformance(FlatBufferBuilder builder,
      int VERSIONOffset,
      int PROFILEOffset,
      byte LINE_ENDING,
      long FOLD_OCTET_WIDTH,
      long BYTE_LENGTH,
      long UNFOLDED_BYTE_LENGTH,
      long BYTE_BUDGET,
      boolean WITHIN_BYTE_BUDGET,
      int CARD_SHA256Offset,
      long PROPERTY_COUNT,
      boolean CARRIES_VERIFICATION_CHAIN) {
    builder.startTable(11);
    VCFConformance.addPropertyCount(builder, PROPERTY_COUNT);
    VCFConformance.addCardSha256(builder, CARD_SHA256Offset);
    VCFConformance.addByteBudget(builder, BYTE_BUDGET);
    VCFConformance.addUnfoldedByteLength(builder, UNFOLDED_BYTE_LENGTH);
    VCFConformance.addByteLength(builder, BYTE_LENGTH);
    VCFConformance.addFoldOctetWidth(builder, FOLD_OCTET_WIDTH);
    VCFConformance.addProfile(builder, PROFILEOffset);
    VCFConformance.addVersion(builder, VERSIONOffset);
    VCFConformance.addCarriesVerificationChain(builder, CARRIES_VERIFICATION_CHAIN);
    VCFConformance.addWithinByteBudget(builder, WITHIN_BYTE_BUDGET);
    VCFConformance.addLineEnding(builder, LINE_ENDING);
    return VCFConformance.endVCFConformance(builder);
  }

  public static void startVCFConformance(FlatBufferBuilder builder) { builder.startTable(11); }
  public static void addVersion(FlatBufferBuilder builder, int VERSIONOffset) { builder.addOffset(0, VERSIONOffset, 0); }
  public static void addProfile(FlatBufferBuilder builder, int PROFILEOffset) { builder.addOffset(1, PROFILEOffset, 0); }
  public static void addLineEnding(FlatBufferBuilder builder, byte LINE_ENDING) { builder.addByte(2, LINE_ENDING, 0); }
  public static void addFoldOctetWidth(FlatBufferBuilder builder, long FOLD_OCTET_WIDTH) { builder.addInt(3, (int) FOLD_OCTET_WIDTH, (int) 0L); }
  public static void addByteLength(FlatBufferBuilder builder, long BYTE_LENGTH) { builder.addInt(4, (int) BYTE_LENGTH, (int) 0L); }
  public static void addUnfoldedByteLength(FlatBufferBuilder builder, long UNFOLDED_BYTE_LENGTH) { builder.addInt(5, (int) UNFOLDED_BYTE_LENGTH, (int) 0L); }
  public static void addByteBudget(FlatBufferBuilder builder, long BYTE_BUDGET) { builder.addInt(6, (int) BYTE_BUDGET, (int) 0L); }
  public static void addWithinByteBudget(FlatBufferBuilder builder, boolean WITHIN_BYTE_BUDGET) { builder.addBoolean(7, WITHIN_BYTE_BUDGET, false); }
  public static void addCardSha256(FlatBufferBuilder builder, int CARD_SHA256Offset) { builder.addOffset(8, CARD_SHA256Offset, 0); }
  public static void addPropertyCount(FlatBufferBuilder builder, long PROPERTY_COUNT) { builder.addInt(9, (int) PROPERTY_COUNT, (int) 0L); }
  public static void addCarriesVerificationChain(FlatBufferBuilder builder, boolean CARRIES_VERIFICATION_CHAIN) { builder.addBoolean(10, CARRIES_VERIFICATION_CHAIN, false); }
  public static int endVCFConformance(FlatBufferBuilder builder) {
    int o = builder.endTable();
    builder.required(o, 4);  // VERSION
    return o;
  }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public VCFConformance get(int j) { return get(new VCFConformance(), j); }
    public VCFConformance get(VCFConformance obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
  }
}
