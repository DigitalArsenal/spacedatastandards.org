// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * The runtime restriction the produced artifact enforces on itself.
 *
 * AUTHORIZATION. A host is authorized when it equals an `ALLOWED_DOMAINS`
 * entry, OR ends with an `ALLOWED_TLDS` entry, OR equals a `DEV_DOMAINS`
 * entry. Suffix rules are DOT-ANCHORED: `.mil` admits `x.mil` and `a.b.mil`
 * and admits neither `mil` nor `evilmil`; a suffix rule never matches a
 * host that merely contains it (`x.mil.example` is refused).
 *
 * UNLOCKED IS EXPLICIT. `ALLOWED_DOMAINS` empty AND `ALLOWED_TLDS` empty
 * means every host is authorized. This is a stated, allowed configuration,
 * which is why this table is required on `BPF`: an absent lock is never
 * read as an unlocked one.
 *
 * EXPIRY IS INDEPENDENT of authorization: an unlocked profile still
 * expires. There is deliberately no stored expiry instant — expiry is
 * `COMPILED_AT_MS + TTL_DAYS * 86400000` and a second, storable copy of it
 * could disagree with the two values that produce it.
 */
@SuppressWarnings("unused")
public final class BPFRuntimeLock extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_12_19(); }
  public static BPFRuntimeLock getRootAsBPFRuntimeLock(ByteBuffer _bb) { return getRootAsBPFRuntimeLock(_bb, new BPFRuntimeLock()); }
  public static BPFRuntimeLock getRootAsBPFRuntimeLock(ByteBuffer _bb, BPFRuntimeLock obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public BPFRuntimeLock __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /**
   * Exact hostnames authorized to run the produced artifact. Empty
   * together with `ALLOWED_TLDS` means unrestricted.
   */
  public String ALLOWED_DOMAINS(int j) { int o = __offset(4); return o != 0 ? __string(__vector(o) + j * 4) : null; }
  public int ALLOWED_DOMAINSLength() { int o = __offset(4); return o != 0 ? __vector_len(o) : 0; }
  public StringVector allowedDomainsVector() { return allowedDomainsVector(new StringVector()); }
  public StringVector allowedDomainsVector(StringVector obj) { int o = __offset(4); return o != 0 ? obj.__assign(__vector(o), 4, bb) : null; }
  /**
   * Dot-anchored hostname suffix rules, each written with its leading dot,
   * for example `.gov`. Empty together with `ALLOWED_DOMAINS` means
   * unrestricted.
   */
  public String ALLOWED_TLDS(int j) { int o = __offset(6); return o != 0 ? __string(__vector(o) + j * 4) : null; }
  public int ALLOWED_TLDSLength() { int o = __offset(6); return o != 0 ? __vector_len(o) : 0; }
  public StringVector allowedTldsVector() { return allowedTldsVector(new StringVector()); }
  public StringVector allowedTldsVector(StringVector obj) { int o = __offset(6); return o != 0 ? obj.__assign(__vector(o), 4, bb) : null; }
  /**
   * Development hosts authorized in addition to the rules above, so a
   * locked profile remains runnable in a development context. Kept
   * separate from `ALLOWED_DOMAINS` because a development exemption and a
   * deployment authorization are different facts: folded together, a
   * development host would be indistinguishable from a deployment grant
   * and would silently persist into a locked artifact.
   */
  public String DEV_DOMAINS(int j) { int o = __offset(8); return o != 0 ? __string(__vector(o) + j * 4) : null; }
  public int DEV_DOMAINSLength() { int o = __offset(8); return o != 0 ? __vector_len(o) : 0; }
  public StringVector devDomainsVector() { return devDomainsVector(new StringVector()); }
  public StringVector devDomainsVector(StringVector obj) { int o = __offset(8); return o != 0 ? obj.__assign(__vector(o), 4, bb) : null; }
  /**
   * Lifetime of the produced artifact in days from `COMPILED_AT_MS`.
   * Normative range 1 through 365 inclusive; 180 when unstated. A value of
   * 0 or above 365 is invalid and MUST be rejected, never clamped.
   */
  public long TTL_DAYS() { int o = __offset(10); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 180L; }
  /**
   * Milliseconds since the Unix epoch at which the artifact was composed.
   * 0 means the profile has not been used to compose an artifact yet and
   * carries no expiry; the composing step stamps it.
   */
  public long COMPILED_AT_MS() { int o = __offset(12); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }

  public static int createBPFRuntimeLock(FlatBufferBuilder builder,
      int ALLOWED_DOMAINSOffset,
      int ALLOWED_TLDSOffset,
      int DEV_DOMAINSOffset,
      long TTL_DAYS,
      long COMPILED_AT_MS) {
    builder.startTable(5);
    BPFRuntimeLock.addCompiledAtMs(builder, COMPILED_AT_MS);
    BPFRuntimeLock.addTtlDays(builder, TTL_DAYS);
    BPFRuntimeLock.addDevDomains(builder, DEV_DOMAINSOffset);
    BPFRuntimeLock.addAllowedTlds(builder, ALLOWED_TLDSOffset);
    BPFRuntimeLock.addAllowedDomains(builder, ALLOWED_DOMAINSOffset);
    return BPFRuntimeLock.endBPFRuntimeLock(builder);
  }

  public static void startBPFRuntimeLock(FlatBufferBuilder builder) { builder.startTable(5); }
  public static void addAllowedDomains(FlatBufferBuilder builder, int ALLOWED_DOMAINSOffset) { builder.addOffset(0, ALLOWED_DOMAINSOffset, 0); }
  public static int createAllowedDomainsVector(FlatBufferBuilder builder, int[] data) { builder.startVector(4, data.length, 4); for (int i = data.length - 1; i >= 0; i--) builder.addOffset(data[i]); return builder.endVector(); }
  public static void startAllowedDomainsVector(FlatBufferBuilder builder, int numElems) { builder.startVector(4, numElems, 4); }
  public static void addAllowedTlds(FlatBufferBuilder builder, int ALLOWED_TLDSOffset) { builder.addOffset(1, ALLOWED_TLDSOffset, 0); }
  public static int createAllowedTldsVector(FlatBufferBuilder builder, int[] data) { builder.startVector(4, data.length, 4); for (int i = data.length - 1; i >= 0; i--) builder.addOffset(data[i]); return builder.endVector(); }
  public static void startAllowedTldsVector(FlatBufferBuilder builder, int numElems) { builder.startVector(4, numElems, 4); }
  public static void addDevDomains(FlatBufferBuilder builder, int DEV_DOMAINSOffset) { builder.addOffset(2, DEV_DOMAINSOffset, 0); }
  public static int createDevDomainsVector(FlatBufferBuilder builder, int[] data) { builder.startVector(4, data.length, 4); for (int i = data.length - 1; i >= 0; i--) builder.addOffset(data[i]); return builder.endVector(); }
  public static void startDevDomainsVector(FlatBufferBuilder builder, int numElems) { builder.startVector(4, numElems, 4); }
  public static void addTtlDays(FlatBufferBuilder builder, long TTL_DAYS) { builder.addInt(3, (int) TTL_DAYS, (int) 180L); }
  public static void addCompiledAtMs(FlatBufferBuilder builder, long COMPILED_AT_MS) { builder.addLong(4, COMPILED_AT_MS, 0L); }
  public static int endBPFRuntimeLock(FlatBufferBuilder builder) {
    int o = builder.endTable();
    return o;
  }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public BPFRuntimeLock get(int j) { return get(new BPFRuntimeLock(), j); }
    public BPFRuntimeLock get(BPFRuntimeLock obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
  }
}
