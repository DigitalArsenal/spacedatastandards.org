// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

@SuppressWarnings("unused")
public final class SCVCoverageGrid extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_12_19(); }
  public static SCVCoverageGrid getRootAsSCVCoverageGrid(ByteBuffer _bb) { return getRootAsSCVCoverageGrid(_bb, new SCVCoverageGrid()); }
  public static SCVCoverageGrid getRootAsSCVCoverageGrid(ByteBuffer _bb, SCVCoverageGrid obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public SCVCoverageGrid __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  public String GRID_ID() { int o = __offset(4); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer GRID_IDAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer GRID_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
  public int DOMAIN() { int o = __offset(6); return o != 0 ? bb.get(o + bb_pos) & 0xFF : 0; }
  public int FRAME() { int o = __offset(8); return o != 0 ? bb.get(o + bb_pos) & 0xFF : 0; }
  public double MIN_LAT_DEG() { int o = __offset(10); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public double MAX_LAT_DEG() { int o = __offset(12); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public double MIN_LON_DEG() { int o = __offset(14); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public double MAX_LON_DEG() { int o = __offset(16); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public double LAT_STEP_DEG() { int o = __offset(18); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public double LON_STEP_DEG() { int o = __offset(20); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public double ALTITUDE_M() { int o = __offset(22); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public long CELL_COUNT() { int o = __offset(24); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  public double RESOLUTION_DEG() { int o = __offset(26); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }

  public static int createSCVCoverageGrid(FlatBufferBuilder builder,
      int GRID_IDOffset,
      int DOMAIN,
      int FRAME,
      double MIN_LAT_DEG,
      double MAX_LAT_DEG,
      double MIN_LON_DEG,
      double MAX_LON_DEG,
      double LAT_STEP_DEG,
      double LON_STEP_DEG,
      double ALTITUDE_M,
      long CELL_COUNT,
      double RESOLUTION_DEG) {
    builder.startTable(12);
    SCVCoverageGrid.addResolutionDeg(builder, RESOLUTION_DEG);
    SCVCoverageGrid.addAltitudeM(builder, ALTITUDE_M);
    SCVCoverageGrid.addLonStepDeg(builder, LON_STEP_DEG);
    SCVCoverageGrid.addLatStepDeg(builder, LAT_STEP_DEG);
    SCVCoverageGrid.addMaxLonDeg(builder, MAX_LON_DEG);
    SCVCoverageGrid.addMinLonDeg(builder, MIN_LON_DEG);
    SCVCoverageGrid.addMaxLatDeg(builder, MAX_LAT_DEG);
    SCVCoverageGrid.addMinLatDeg(builder, MIN_LAT_DEG);
    SCVCoverageGrid.addCellCount(builder, CELL_COUNT);
    SCVCoverageGrid.addGridId(builder, GRID_IDOffset);
    SCVCoverageGrid.addFrame(builder, FRAME);
    SCVCoverageGrid.addDomain(builder, DOMAIN);
    return SCVCoverageGrid.endSCVCoverageGrid(builder);
  }

  public static void startSCVCoverageGrid(FlatBufferBuilder builder) { builder.startTable(12); }
  public static void addGridId(FlatBufferBuilder builder, int GRID_IDOffset) { builder.addOffset(0, GRID_IDOffset, 0); }
  public static void addDomain(FlatBufferBuilder builder, int DOMAIN) { builder.addByte(1, (byte) DOMAIN, (byte) 0); }
  public static void addFrame(FlatBufferBuilder builder, int FRAME) { builder.addByte(2, (byte) FRAME, (byte) 0); }
  public static void addMinLatDeg(FlatBufferBuilder builder, double MIN_LAT_DEG) { builder.addDouble(3, MIN_LAT_DEG, 0.0); }
  public static void addMaxLatDeg(FlatBufferBuilder builder, double MAX_LAT_DEG) { builder.addDouble(4, MAX_LAT_DEG, 0.0); }
  public static void addMinLonDeg(FlatBufferBuilder builder, double MIN_LON_DEG) { builder.addDouble(5, MIN_LON_DEG, 0.0); }
  public static void addMaxLonDeg(FlatBufferBuilder builder, double MAX_LON_DEG) { builder.addDouble(6, MAX_LON_DEG, 0.0); }
  public static void addLatStepDeg(FlatBufferBuilder builder, double LAT_STEP_DEG) { builder.addDouble(7, LAT_STEP_DEG, 0.0); }
  public static void addLonStepDeg(FlatBufferBuilder builder, double LON_STEP_DEG) { builder.addDouble(8, LON_STEP_DEG, 0.0); }
  public static void addAltitudeM(FlatBufferBuilder builder, double ALTITUDE_M) { builder.addDouble(9, ALTITUDE_M, 0.0); }
  public static void addCellCount(FlatBufferBuilder builder, long CELL_COUNT) { builder.addInt(10, (int) CELL_COUNT, (int) 0L); }
  public static void addResolutionDeg(FlatBufferBuilder builder, double RESOLUTION_DEG) { builder.addDouble(11, RESOLUTION_DEG, 0.0); }
  public static int endSCVCoverageGrid(FlatBufferBuilder builder) {
    int o = builder.endTable();
    return o;
  }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public SCVCoverageGrid get(int j) { return get(new SCVCoverageGrid(), j); }
    public SCVCoverageGrid get(SCVCoverageGrid obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
  }
}
