// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * Publisher Key-Broker Descriptor
 */
@SuppressWarnings("unused")
public final class PKB extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_12_19(); }
  public static PKB getRootAsPKB(ByteBuffer _bb) { return getRootAsPKB(_bb, new PKB()); }
  public static PKB getRootAsPKB(ByteBuffer _bb, PKB obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public static boolean PKBBufferHasIdentifier(ByteBuffer _bb) { return __has_identifier(_bb, "$PKB"); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public PKB __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /**
   * Stable broker identity, referenced by `$PLG`/`$STO`/`$PUR`/`$LGR`
   */
  public String BROKER_ID() { int o = __offset(4); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer BROKER_IDAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer BROKER_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
  /**
   * IPFS CID of the publisher `$EPM` that operates this broker
   */
  public String PUBLISHER_EPM_CID() { int o = __offset(6); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer PUBLISHER_EPM_CIDAsByteBuffer() { return __vector_as_bytebuffer(6, 1); }
  public ByteBuffer PUBLISHER_EPM_CIDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 6, 1); }
  /**
   * libp2p peer id of the publisher node hosting the broker
   */
  public String PROVIDER_PEER_ID() { int o = __offset(8); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer PROVIDER_PEER_IDAsByteBuffer() { return __vector_as_bytebuffer(8, 1); }
  public ByteBuffer PROVIDER_PEER_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 8, 1); }
  /**
   * libp2p protocol the broker answers on
   * (e.g. "/space-data-network/module-delivery/1.0.0")
   */
  public String PROTOCOL_ID() { int o = __offset(10); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer PROTOCOL_IDAsByteBuffer() { return __vector_as_bytebuffer(10, 1); }
  public ByteBuffer PROTOCOL_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 10, 1); }
  /**
   * Dialable multiformat addresses for the broker
   */
  public String MULTIFORMAT_ADDRESSES(int j) { int o = __offset(12); return o != 0 ? __string(__vector(o) + j * 4) : null; }
  public int MULTIFORMAT_ADDRESSESLength() { int o = __offset(12); return o != 0 ? __vector_len(o) : 0; }
  public StringVector multiformatAddressesVector() { return multiformatAddressesVector(new StringVector()); }
  public StringVector multiformatAddressesVector(StringVector obj) { int o = __offset(12); return o != 0 ? obj.__assign(__vector(o), 4, bb) : null; }
  /**
   * The broker's public key (from the key server's get_public_key) so the
   * buyer can pin it
   */
  public int BROKER_PUBLIC_KEY(int j) { int o = __offset(14); return o != 0 ? bb.get(__vector(o) + j * 1) & 0xFF : 0; }
  public int BROKER_PUBLIC_KEYLength() { int o = __offset(14); return o != 0 ? __vector_len(o) : 0; }
  public ByteVector brokerPublicKeyVector() { return brokerPublicKeyVector(new ByteVector()); }
  public ByteVector brokerPublicKeyVector(ByteVector obj) { int o = __offset(14); return o != 0 ? obj.__assign(__vector(o), bb) : null; }
  public ByteBuffer BROKER_PUBLIC_KEYAsByteBuffer() { return __vector_as_bytebuffer(14, 1); }
  public ByteBuffer BROKER_PUBLIC_KEYInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 14, 1); }
  /**
   * Custody model (default: full publisher custody)
   */
  public byte BROKER_KIND() { int o = __offset(16); return o != 0 ? bb.get(o + bb_pos) : 0; }
  /**
   * Store-maintained liveness status
   */
  public byte LIVENESS() { int o = __offset(18); return o != 0 ? bb.get(o + bb_pos) : 0; }
  /**
   * Unix ms of the last successful probe/heartbeat
   */
  public long LAST_HEARTBEAT_AT() { int o = __offset(20); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }
  /**
   * Expected heartbeat cadence in ms
   */
  public long HEARTBEAT_INTERVAL_MS() { int o = __offset(22); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }
  /**
   * Unix ms the store first saw this broker
   */
  public long FIRST_SEEN_AT() { int o = __offset(24); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }
  /**
   * Unix ms this descriptor was last updated
   */
  public long UPDATED_AT() { int o = __offset(26); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }
  /**
   * Ed25519 signature by the publisher `$EPM` over the self-attested endpoint
   * fields; re-verified by the store's live probe
   */
  public int SIGNATURE(int j) { int o = __offset(28); return o != 0 ? bb.get(__vector(o) + j * 1) & 0xFF : 0; }
  public int SIGNATURELength() { int o = __offset(28); return o != 0 ? __vector_len(o) : 0; }
  public ByteVector signatureVector() { return signatureVector(new ByteVector()); }
  public ByteVector signatureVector(ByteVector obj) { int o = __offset(28); return o != 0 ? obj.__assign(__vector(o), bb) : null; }
  public ByteBuffer SIGNATUREAsByteBuffer() { return __vector_as_bytebuffer(28, 1); }
  public ByteBuffer SIGNATUREInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 28, 1); }

  public static int createPKB(FlatBufferBuilder builder,
      int BROKER_IDOffset,
      int PUBLISHER_EPM_CIDOffset,
      int PROVIDER_PEER_IDOffset,
      int PROTOCOL_IDOffset,
      int MULTIFORMAT_ADDRESSESOffset,
      int BROKER_PUBLIC_KEYOffset,
      byte BROKER_KIND,
      byte LIVENESS,
      long LAST_HEARTBEAT_AT,
      long HEARTBEAT_INTERVAL_MS,
      long FIRST_SEEN_AT,
      long UPDATED_AT,
      int SIGNATUREOffset) {
    builder.startTable(13);
    PKB.addUpdatedAt(builder, UPDATED_AT);
    PKB.addFirstSeenAt(builder, FIRST_SEEN_AT);
    PKB.addHeartbeatIntervalMs(builder, HEARTBEAT_INTERVAL_MS);
    PKB.addLastHeartbeatAt(builder, LAST_HEARTBEAT_AT);
    PKB.addSignature(builder, SIGNATUREOffset);
    PKB.addBrokerPublicKey(builder, BROKER_PUBLIC_KEYOffset);
    PKB.addMultiformatAddresses(builder, MULTIFORMAT_ADDRESSESOffset);
    PKB.addProtocolId(builder, PROTOCOL_IDOffset);
    PKB.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
    PKB.addPublisherEpmCid(builder, PUBLISHER_EPM_CIDOffset);
    PKB.addBrokerId(builder, BROKER_IDOffset);
    PKB.addLiveness(builder, LIVENESS);
    PKB.addBrokerKind(builder, BROKER_KIND);
    return PKB.endPKB(builder);
  }

  public static void startPKB(FlatBufferBuilder builder) { builder.startTable(13); }
  public static void addBrokerId(FlatBufferBuilder builder, int BROKER_IDOffset) { builder.addOffset(0, BROKER_IDOffset, 0); }
  public static void addPublisherEpmCid(FlatBufferBuilder builder, int PUBLISHER_EPM_CIDOffset) { builder.addOffset(1, PUBLISHER_EPM_CIDOffset, 0); }
  public static void addProviderPeerId(FlatBufferBuilder builder, int PROVIDER_PEER_IDOffset) { builder.addOffset(2, PROVIDER_PEER_IDOffset, 0); }
  public static void addProtocolId(FlatBufferBuilder builder, int PROTOCOL_IDOffset) { builder.addOffset(3, PROTOCOL_IDOffset, 0); }
  public static void addMultiformatAddresses(FlatBufferBuilder builder, int MULTIFORMAT_ADDRESSESOffset) { builder.addOffset(4, MULTIFORMAT_ADDRESSESOffset, 0); }
  public static int createMultiformatAddressesVector(FlatBufferBuilder builder, int[] data) { builder.startVector(4, data.length, 4); for (int i = data.length - 1; i >= 0; i--) builder.addOffset(data[i]); return builder.endVector(); }
  public static void startMultiformatAddressesVector(FlatBufferBuilder builder, int numElems) { builder.startVector(4, numElems, 4); }
  public static void addBrokerPublicKey(FlatBufferBuilder builder, int BROKER_PUBLIC_KEYOffset) { builder.addOffset(5, BROKER_PUBLIC_KEYOffset, 0); }
  public static int createBrokerPublicKeyVector(FlatBufferBuilder builder, byte[] data) { return builder.createByteVector(data); }
  public static int createBrokerPublicKeyVector(FlatBufferBuilder builder, ByteBuffer data) { return builder.createByteVector(data); }
  public static void startBrokerPublicKeyVector(FlatBufferBuilder builder, int numElems) { builder.startVector(1, numElems, 1); }
  public static void addBrokerKind(FlatBufferBuilder builder, byte BROKER_KIND) { builder.addByte(6, BROKER_KIND, 0); }
  public static void addLiveness(FlatBufferBuilder builder, byte LIVENESS) { builder.addByte(7, LIVENESS, 0); }
  public static void addLastHeartbeatAt(FlatBufferBuilder builder, long LAST_HEARTBEAT_AT) { builder.addLong(8, LAST_HEARTBEAT_AT, 0L); }
  public static void addHeartbeatIntervalMs(FlatBufferBuilder builder, long HEARTBEAT_INTERVAL_MS) { builder.addLong(9, HEARTBEAT_INTERVAL_MS, 0L); }
  public static void addFirstSeenAt(FlatBufferBuilder builder, long FIRST_SEEN_AT) { builder.addLong(10, FIRST_SEEN_AT, 0L); }
  public static void addUpdatedAt(FlatBufferBuilder builder, long UPDATED_AT) { builder.addLong(11, UPDATED_AT, 0L); }
  public static void addSignature(FlatBufferBuilder builder, int SIGNATUREOffset) { builder.addOffset(12, SIGNATUREOffset, 0); }
  public static int createSignatureVector(FlatBufferBuilder builder, byte[] data) { return builder.createByteVector(data); }
  public static int createSignatureVector(FlatBufferBuilder builder, ByteBuffer data) { return builder.createByteVector(data); }
  public static void startSignatureVector(FlatBufferBuilder builder, int numElems) { builder.startVector(1, numElems, 1); }
  public static int endPKB(FlatBufferBuilder builder) {
    int o = builder.endTable();
    builder.required(o, 4);  // BROKER_ID
    return o;
  }
  public static void finishPKBBuffer(FlatBufferBuilder builder, int offset) { builder.finish(offset, "$PKB"); }
  public static void finishSizePrefixedPKBBuffer(FlatBufferBuilder builder, int offset) { builder.finishSizePrefixed(offset, "$PKB"); }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public PKB get(int j) { return get(new PKB(), j); }
    public PKB get(PKB obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
  }
}
