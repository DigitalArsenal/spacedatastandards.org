// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * Terrestrial Base Station Site.
 *
 * One record describes ONE terrestrial cellular base-station site as
 * reconciled from every provider the publisher consulted. It is the wire
 * record for terrestrial mobile-network infrastructure, and it is not an
 * emitter model ($RFE), a band specification ($RFB), a geodetic tracking
 * station ($GST), a satellite ground station ($SIT), or an observation.
 *
 * A site cannot be re-serialized unattributed. SOURCES and CONSENSUS are both
 * required, so every published position carries the providers it came from and
 * the rule that reconciled them; a consumer that strips attribution has to
 * leave the standard to do it. Unknown values are ABSENT — never zero, never a
 * placeholder, never carried across from a neighbouring site.
 *
 * Identity is ID, the publisher's own. The network addressing fields are the
 * mobile network's, are assigned by its operator, and are reused worldwide;
 * none of them is a global key on its own.
 */
@SuppressWarnings("unused")
public final class TBS extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_12_19(); }
  public static TBS getRootAsTBS(ByteBuffer _bb) { return getRootAsTBS(_bb, new TBS()); }
  public static TBS getRootAsTBS(ByteBuffer _bb, TBS obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public static boolean TBSBufferHasIdentifier(ByteBuffer _bb) { return __has_identifier(_bb, "$TBS"); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public TBS __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /**
   * Publisher-stable identifier for this site, unique within its record set.
   * Recommended form: "<mcc>-<mnc>-<area-code>-<cell-id>".
   */
  public String ID() { int o = __offset(4); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer IDAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
  /**
   * Winning provider's native site identifier, copied verbatim from the
   * SOURCES entry named by CONSENSUS.WINNING_PROVIDER_ID. Never a substitute
   * for ID and never stable across providers.
   */
  public String NATIVE_ID() { int o = __offset(6); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer NATIVE_IDAsByteBuffer() { return __vector_as_bytebuffer(6, 1); }
  public ByteBuffer NATIVE_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 6, 1); }
  public byte RADIO() { int o = __offset(8); return o != 0 ? bb.get(o + bb_pos) : 6; }
  /**
   * Mobile country code from the international mobile-subscriber identity
   * numbering plan. Zero means the source published none.
   */
  public long MCC() { int o = __offset(10); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Mobile network code within MCC. Carried as an integer, so decimal leading
   * zeros are NOT preserved: a two- versus three-digit network code is
   * distinguished by the numbering plan of MCC, and the source's own text is
   * retained verbatim in TBSProvenance.NATIVE_ID. Zero means unpublished.
   */
  public long MNC() { int o = __offset(12); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Location area code, used by GSM-, CDMA- and UMTS-class access. Zero means
   * unpublished. A site published with a location area code does not also
   * carry a tracking area code; publishing both asserts two access
   * generations for one cell.
   */
  public long LAC() { int o = __offset(14); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Tracking area code, used by LTE- and NR-class access. Zero means
   * unpublished.
   */
  public long TAC() { int o = __offset(16); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Cell identity verbatim from the source. A string because width, radix and
   * leading zeros are significant and differ by access class: a 16-bit cell
   * identity, a 28-bit evolved cell identity and a 36-bit new-radio cell
   * identity are not interchangeable integers, and this field never converts
   * between them.
   */
  public String CELL_ID() { int o = __offset(18); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer CELL_IDAsByteBuffer() { return __vector_as_bytebuffer(18, 1); }
  public ByteBuffer CELL_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 18, 1); }
  /**
   * Reconciled site position in decimal degrees on WGS 84, produced by the
   * merge described in CONSENSUS. Per-provider positions before
   * reconciliation live in SOURCES[].REPORTED_LATITUDE/REPORTED_LONGITUDE and
   * are never overwritten by this value.
   */
  public double LATITUDE() { int o = __offset(20); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  public double LONGITUDE() { int o = __offset(22); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  /**
   * Radius in metres of the area within which the site's observations were
   * collected — a positional-uncertainty envelope, not a transmission range,
   * not a coverage guarantee, and never converted from another unit. Zero
   * means unpublished.
   */
  public double RANGE_M() { int o = __offset(24); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  /**
   * Independent observations behind the winning report. Zero means the source
   * publishes no observation count, never that the site was never observed.
   */
  public long SAMPLES() { int o = __offset(26); return o != 0 ? (long)bb.getInt(o + bb_pos) & 0xFFFFFFFFL : 0L; }
  /**
   * Mean received signal strength in dBm across those observations, as the
   * source publishes it. Carried in dBm unconverted; a consumer needing watts
   * converts at the join, explicitly. Zero means unpublished.
   */
  public double AVERAGE_SIGNAL_DBM() { int o = __offset(28); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  /**
   * RFC 3339 UTC fixed-millisecond bounds of the observation window backing
   * the winning report.
   */
  public String FIRST_OBSERVED() { int o = __offset(30); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer FIRST_OBSERVEDAsByteBuffer() { return __vector_as_bytebuffer(30, 1); }
  public ByteBuffer FIRST_OBSERVEDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 30, 1); }
  public String LAST_OBSERVED() { int o = __offset(32); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer LAST_OBSERVEDAsByteBuffer() { return __vector_as_bytebuffer(32, 1); }
  public ByteBuffer LAST_OBSERVEDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 32, 1); }
  /**
   * Network operator name verbatim as the cited source states it. A display
   * name, never an identifier: consumers must not treat it as a stable key.
   */
  public String OPERATOR() { int o = __offset(34); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer OPERATORAsByteBuffer() { return __vector_as_bytebuffer(34, 1); }
  public ByteBuffer OPERATORInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 34, 1); }
  /**
   * Operating centre frequency in MHz, following the $RFB convention. A
   * consumer joining to $IQC, which carries hertz, converts at the join,
   * explicitly. Zero means unpublished.
   */
  public double FREQUENCY_MHZ() { int o = __offset(36); return o != 0 ? bb.getDouble(o + bb_pos) : 0.0; }
  /**
   * Site or structure name verbatim when the source publishes one. Never
   * synthesized from coordinates, operator, or cell identity.
   */
  public String SITE_NAME() { int o = __offset(38); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer SITE_NAMEAsByteBuffer() { return __vector_as_bytebuffer(38, 1); }
  public ByteBuffer SITE_NAMEInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 38, 1); }
  /**
   * ISO 3166-1 alpha-2 country code, uppercase, as the source states it.
   * Derived from MCC only when the source itself derives it.
   */
  public String COUNTRY_CODE() { int o = __offset(40); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer COUNTRY_CODEAsByteBuffer() { return __vector_as_bytebuffer(40, 1); }
  public ByteBuffer COUNTRY_CODEInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 40, 1); }
  /**
   * Every provider consulted for this site, including providers that returned
   * nothing. Required: a site with no named sources is not publishable.
   */
  public TBSProvenance SOURCES(int j) { return SOURCES(new TBSProvenance(), j); }
  public TBSProvenance SOURCES(TBSProvenance obj, int j) { int o = __offset(42); return o != 0 ? obj.__assign(__indirect(__vector(o) + j * 4), bb) : null; }
  public int SOURCESLength() { int o = __offset(42); return o != 0 ? __vector_len(o) : 0; }
  public TBSProvenance.Vector sourcesVector() { return sourcesVector(new TBSProvenance.Vector()); }
  public TBSProvenance.Vector sourcesVector(TBSProvenance.Vector obj) { int o = __offset(42); return o != 0 ? obj.__assign(__vector(o), 4, bb) : null; }
  /**
   * How the entries in SOURCES were reduced to LATITUDE/LONGITUDE. Required:
   * a published position always states the rule that produced it, including
   * SINGLE_SOURCE.
   */
  public TBSConsensus CONSENSUS() { return CONSENSUS(new TBSConsensus()); }
  public TBSConsensus CONSENSUS(TBSConsensus obj) { int o = __offset(44); return o != 0 ? obj.__assign(__indirect(o + bb_pos), bb) : null; }

  public static int createTBS(FlatBufferBuilder builder,
      int IDOffset,
      int NATIVE_IDOffset,
      byte RADIO,
      long MCC,
      long MNC,
      long LAC,
      long TAC,
      int CELL_IDOffset,
      double LATITUDE,
      double LONGITUDE,
      double RANGE_M,
      long SAMPLES,
      double AVERAGE_SIGNAL_DBM,
      int FIRST_OBSERVEDOffset,
      int LAST_OBSERVEDOffset,
      int OPERATOROffset,
      double FREQUENCY_MHZ,
      int SITE_NAMEOffset,
      int COUNTRY_CODEOffset,
      int SOURCESOffset,
      int CONSENSUSOffset) {
    builder.startTable(21);
    TBS.addFrequencyMhz(builder, FREQUENCY_MHZ);
    TBS.addAverageSignalDbm(builder, AVERAGE_SIGNAL_DBM);
    TBS.addRangeM(builder, RANGE_M);
    TBS.addLongitude(builder, LONGITUDE);
    TBS.addLatitude(builder, LATITUDE);
    TBS.addConsensus(builder, CONSENSUSOffset);
    TBS.addSources(builder, SOURCESOffset);
    TBS.addCountryCode(builder, COUNTRY_CODEOffset);
    TBS.addSiteName(builder, SITE_NAMEOffset);
    TBS.addOperator(builder, OPERATOROffset);
    TBS.addLastObserved(builder, LAST_OBSERVEDOffset);
    TBS.addFirstObserved(builder, FIRST_OBSERVEDOffset);
    TBS.addSamples(builder, SAMPLES);
    TBS.addCellId(builder, CELL_IDOffset);
    TBS.addTac(builder, TAC);
    TBS.addLac(builder, LAC);
    TBS.addMnc(builder, MNC);
    TBS.addMcc(builder, MCC);
    TBS.addNativeId(builder, NATIVE_IDOffset);
    TBS.addId(builder, IDOffset);
    TBS.addRadio(builder, RADIO);
    return TBS.endTBS(builder);
  }

  public static void startTBS(FlatBufferBuilder builder) { builder.startTable(21); }
  public static void addId(FlatBufferBuilder builder, int IDOffset) { builder.addOffset(0, IDOffset, 0); }
  public static void addNativeId(FlatBufferBuilder builder, int NATIVE_IDOffset) { builder.addOffset(1, NATIVE_IDOffset, 0); }
  public static void addRadio(FlatBufferBuilder builder, byte RADIO) { builder.addByte(2, RADIO, 6); }
  public static void addMcc(FlatBufferBuilder builder, long MCC) { builder.addInt(3, (int) MCC, (int) 0L); }
  public static void addMnc(FlatBufferBuilder builder, long MNC) { builder.addInt(4, (int) MNC, (int) 0L); }
  public static void addLac(FlatBufferBuilder builder, long LAC) { builder.addInt(5, (int) LAC, (int) 0L); }
  public static void addTac(FlatBufferBuilder builder, long TAC) { builder.addInt(6, (int) TAC, (int) 0L); }
  public static void addCellId(FlatBufferBuilder builder, int CELL_IDOffset) { builder.addOffset(7, CELL_IDOffset, 0); }
  public static void addLatitude(FlatBufferBuilder builder, double LATITUDE) { builder.addDouble(8, LATITUDE, 0.0); }
  public static void addLongitude(FlatBufferBuilder builder, double LONGITUDE) { builder.addDouble(9, LONGITUDE, 0.0); }
  public static void addRangeM(FlatBufferBuilder builder, double RANGE_M) { builder.addDouble(10, RANGE_M, 0.0); }
  public static void addSamples(FlatBufferBuilder builder, long SAMPLES) { builder.addInt(11, (int) SAMPLES, (int) 0L); }
  public static void addAverageSignalDbm(FlatBufferBuilder builder, double AVERAGE_SIGNAL_DBM) { builder.addDouble(12, AVERAGE_SIGNAL_DBM, 0.0); }
  public static void addFirstObserved(FlatBufferBuilder builder, int FIRST_OBSERVEDOffset) { builder.addOffset(13, FIRST_OBSERVEDOffset, 0); }
  public static void addLastObserved(FlatBufferBuilder builder, int LAST_OBSERVEDOffset) { builder.addOffset(14, LAST_OBSERVEDOffset, 0); }
  public static void addOperator(FlatBufferBuilder builder, int OPERATOROffset) { builder.addOffset(15, OPERATOROffset, 0); }
  public static void addFrequencyMhz(FlatBufferBuilder builder, double FREQUENCY_MHZ) { builder.addDouble(16, FREQUENCY_MHZ, 0.0); }
  public static void addSiteName(FlatBufferBuilder builder, int SITE_NAMEOffset) { builder.addOffset(17, SITE_NAMEOffset, 0); }
  public static void addCountryCode(FlatBufferBuilder builder, int COUNTRY_CODEOffset) { builder.addOffset(18, COUNTRY_CODEOffset, 0); }
  public static void addSources(FlatBufferBuilder builder, int SOURCESOffset) { builder.addOffset(19, SOURCESOffset, 0); }
  public static int createSourcesVector(FlatBufferBuilder builder, int[] data) { builder.startVector(4, data.length, 4); for (int i = data.length - 1; i >= 0; i--) builder.addOffset(data[i]); return builder.endVector(); }
  public static void startSourcesVector(FlatBufferBuilder builder, int numElems) { builder.startVector(4, numElems, 4); }
  public static void addConsensus(FlatBufferBuilder builder, int CONSENSUSOffset) { builder.addOffset(20, CONSENSUSOffset, 0); }
  public static int endTBS(FlatBufferBuilder builder) {
    int o = builder.endTable();
    builder.required(o, 4);  // ID
    builder.required(o, 42);  // SOURCES
    builder.required(o, 44);  // CONSENSUS
    return o;
  }
  public static void finishTBSBuffer(FlatBufferBuilder builder, int offset) { builder.finish(offset, "$TBS"); }
  public static void finishSizePrefixedTBSBuffer(FlatBufferBuilder builder, int offset) { builder.finishSizePrefixed(offset, "$TBS"); }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public TBS get(int j) { return get(new TBS(), j); }
    public TBS get(TBS obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
  }
}
