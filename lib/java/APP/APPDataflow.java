// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * One unit of the page's data contract: a named flow describing what data
 * enters or leaves the running page, the SDS standard it carries, how it is
 * transported, and — when applicable — the loaded module method port bound
 * to it. Standards-only rule: every flow payload is an SDS record (a
 * canonical size-prefixed FlatBuffer) or a CID pointing at one; this table
 * defines no data shapes of its own, it only references an existing
 * spacedatastandards.org schema by its established code. Locators are
 * content-addressed and IPFS-first.
 */
@SuppressWarnings("unused")
public final class APPDataflow extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_12_19(); }
  public static APPDataflow getRootAsAPPDataflow(ByteBuffer _bb) { return getRootAsAPPDataflow(_bb, new APPDataflow()); }
  public static APPDataflow getRootAsAPPDataflow(ByteBuffer _bb, APPDataflow obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public APPDataflow __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  /**
   * App-local stable name for this flow. Required, unique within the
   * manifest.
   */
  public String NAME() { int o = __offset(4); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer NAMEAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer NAMEInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
  /**
   * Which way the payload crosses the page boundary at runtime.
   */
  public int DIRECTION() { int o = __offset(6); return o != 0 ? bb.get(o + bb_pos) & 0xFF : 0; }
  /**
   * Existing SDS schema code carried by this flow, for example OMM, OEM, or
   * PNM. Required. Mirrors APPDataRef.SDS_TYPE but named for the standard
   * the flow carries; the app defines no schema of its own.
   */
  public String SDS_SCHEMA() { int o = __offset(8); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer SDS_SCHEMAAsByteBuffer() { return __vector_as_bytebuffer(8, 1); }
  public ByteBuffer SDS_SCHEMAInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 8, 1); }
  /**
   * Transport that moves the payload. Defaults to IPFS_CID per the
   * content-addressed, IPFS-first rule.
   */
  public int TRANSPORT() { int o = __offset(10); return o != 0 ? bb.get(o + bb_pos) & 0xFF : 0; }
  /**
   * Where to fetch or reach the payload, interpreted per TRANSPORT: a CID
   * for IPFS_CID, a gossip topic name for PUBSUB_TOPIC, or a same-origin
   * route template for GATEWAY_ROUTE.
   */
  public String LOCATOR() { int o = __offset(12); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer LOCATORAsByteBuffer() { return __vector_as_bytebuffer(12, 1); }
  public ByteBuffer LOCATORInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 12, 1); }
  /**
   * When present, must equal an APPModuleRef.ID in the same manifest — the
   * loaded module that produces or consumes this flow. Binds the flow to a
   * specific module method port together with METHOD_ID and PORT_ID.
   */
  public String MODULE_ID() { int o = __offset(14); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer MODULE_IDAsByteBuffer() { return __vector_as_bytebuffer(14, 1); }
  public ByteBuffer MODULE_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 14, 1); }
  /**
   * When present, the PLG.PLGMethodManifest.METHOD_ID on MODULE_ID that
   * this flow is bound to.
   */
  public String METHOD_ID() { int o = __offset(16); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer METHOD_IDAsByteBuffer() { return __vector_as_bytebuffer(16, 1); }
  public ByteBuffer METHOD_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 16, 1); }
  /**
   * When present, the PLG.PLGPortManifest.PORT_ID on METHOD_ID that this
   * flow is bound to.
   */
  public String PORT_ID() { int o = __offset(18); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer PORT_IDAsByteBuffer() { return __vector_as_bytebuffer(18, 1); }
  public ByteBuffer PORT_IDInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 18, 1); }
  /**
   * String/compression form of the payload as it crosses the channel,
   * reusing the page content-encoding vocabulary. For flows carrying
   * canonical SDS FlatBuffer bytes (or a CID string), UTF8 denotes the raw
   * bytes/string with no extra wrapper; BASE64, BASE64_GZIP, and
   * BASE64_BROTLI denote a base64 text wrapper (optionally compressed)
   * applied when the channel is text-only.
   */
  public int CONTENT_ENCODING() { int o = __offset(20); return o != 0 ? bb.get(o + bb_pos) & 0xFF : 0; }
  /**
   * Human-readable summary.
   */
  public String DESCRIPTION() { int o = __offset(22); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer DESCRIPTIONAsByteBuffer() { return __vector_as_bytebuffer(22, 1); }
  public ByteBuffer DESCRIPTIONInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 22, 1); }

  public static int createAPPDataflow(FlatBufferBuilder builder,
      int NAMEOffset,
      int DIRECTION,
      int SDS_SCHEMAOffset,
      int TRANSPORT,
      int LOCATOROffset,
      int MODULE_IDOffset,
      int METHOD_IDOffset,
      int PORT_IDOffset,
      int CONTENT_ENCODING,
      int DESCRIPTIONOffset) {
    builder.startTable(10);
    APPDataflow.addDescription(builder, DESCRIPTIONOffset);
    APPDataflow.addPortId(builder, PORT_IDOffset);
    APPDataflow.addMethodId(builder, METHOD_IDOffset);
    APPDataflow.addModuleId(builder, MODULE_IDOffset);
    APPDataflow.addLocator(builder, LOCATOROffset);
    APPDataflow.addSdsSchema(builder, SDS_SCHEMAOffset);
    APPDataflow.addName(builder, NAMEOffset);
    APPDataflow.addContentEncoding(builder, CONTENT_ENCODING);
    APPDataflow.addTransport(builder, TRANSPORT);
    APPDataflow.addDirection(builder, DIRECTION);
    return APPDataflow.endAPPDataflow(builder);
  }

  public static void startAPPDataflow(FlatBufferBuilder builder) { builder.startTable(10); }
  public static void addName(FlatBufferBuilder builder, int NAMEOffset) { builder.addOffset(NAMEOffset); builder.slot(0); }
  public static void addDirection(FlatBufferBuilder builder, int DIRECTION) { builder.addByte(1, (byte) DIRECTION, (byte) 0); }
  public static void addSdsSchema(FlatBufferBuilder builder, int SDS_SCHEMAOffset) { builder.addOffset(2, SDS_SCHEMAOffset, 0); }
  public static void addTransport(FlatBufferBuilder builder, int TRANSPORT) { builder.addByte(3, (byte) TRANSPORT, (byte) 0); }
  public static void addLocator(FlatBufferBuilder builder, int LOCATOROffset) { builder.addOffset(4, LOCATOROffset, 0); }
  public static void addModuleId(FlatBufferBuilder builder, int MODULE_IDOffset) { builder.addOffset(5, MODULE_IDOffset, 0); }
  public static void addMethodId(FlatBufferBuilder builder, int METHOD_IDOffset) { builder.addOffset(6, METHOD_IDOffset, 0); }
  public static void addPortId(FlatBufferBuilder builder, int PORT_IDOffset) { builder.addOffset(7, PORT_IDOffset, 0); }
  public static void addContentEncoding(FlatBufferBuilder builder, int CONTENT_ENCODING) { builder.addByte(8, (byte) CONTENT_ENCODING, (byte) 0); }
  public static void addDescription(FlatBufferBuilder builder, int DESCRIPTIONOffset) { builder.addOffset(9, DESCRIPTIONOffset, 0); }
  public static int endAPPDataflow(FlatBufferBuilder builder) {
    int o = builder.endTable();
    builder.required(o, 4);  // NAME
    builder.required(o, 8);  // SDS_SCHEMA
    return o;
  }

  @Override
  protected int keysCompare(Integer o1, Integer o2, ByteBuffer _bb) { return compareStrings(__offset(4, o1, _bb), __offset(4, o2, _bb), _bb); }

  public static APPDataflow __lookup_by_key(APPDataflow obj, int vectorLocation, String key, ByteBuffer bb) {
    byte[] byteKey = key.getBytes(java.nio.charset.StandardCharsets.UTF_8);
    int span = bb.getInt(vectorLocation - 4);
    int start = 0;
    while (span != 0) {
      int middle = span / 2;
      int tableOffset = __indirect(vectorLocation + 4 * (start + middle), bb);
      int comp = compareStrings(__offset(4, bb.capacity() - tableOffset, bb), byteKey, bb);
      if (comp > 0) {
        span = middle;
      } else if (comp < 0) {
        middle++;
        start += middle;
        span -= middle;
      } else {
        return (obj == null ? new APPDataflow() : obj).__assign(tableOffset, bb);
      }
    }
    return null;
  }

  public static final class Vector extends BaseVector {
    public Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public APPDataflow get(int j) { return get(new APPDataflow(), j); }
    public APPDataflow get(APPDataflow obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
    public APPDataflow getByKey(String key) {  return __lookup_by_key(null, __vector(), key, bb); }
    public APPDataflow getByKey(APPDataflow obj, String key) {  return __lookup_by_key(obj, __vector(), key, bb); }
  }
}
