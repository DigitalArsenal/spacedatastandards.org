// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { ACWConstraint } from './ACWConstraint.js';
import { acwConstraintOperator } from './acwConstraintOperator.js';
/**
 * A boolean composition of constraints and nested sets.
 */
export class ACWConstraintSet {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsACWConstraintSet(bb, obj) {
        return (obj || new ACWConstraintSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsACWConstraintSet(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ACWConstraintSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    OPERATOR() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : acwConstraintOperator.ALL_OF;
    }
    CONSTRAINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new ACWConstraint()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    constraintsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Nested sets, combined with the same OPERATOR.
     */
    SETS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new ACWConstraintSet()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    setsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    LABEL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startACWConstraintSet(builder) {
        builder.startObject(4);
    }
    static addOperator(builder, OPERATOR) {
        builder.addFieldInt8(0, OPERATOR, acwConstraintOperator.ALL_OF);
    }
    static addConstraints(builder, CONSTRAINTSOffset) {
        builder.addFieldOffset(1, CONSTRAINTSOffset, 0);
    }
    static createConstraintsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startConstraintsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSets(builder, SETSOffset) {
        builder.addFieldOffset(2, SETSOffset, 0);
    }
    static createSetsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSetsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addLabel(builder, LABELOffset) {
        builder.addFieldOffset(3, LABELOffset, 0);
    }
    static endACWConstraintSet(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createACWConstraintSet(builder, OPERATOR, CONSTRAINTSOffset, SETSOffset, LABELOffset) {
        ACWConstraintSet.startACWConstraintSet(builder);
        ACWConstraintSet.addOperator(builder, OPERATOR);
        ACWConstraintSet.addConstraints(builder, CONSTRAINTSOffset);
        ACWConstraintSet.addSets(builder, SETSOffset);
        ACWConstraintSet.addLabel(builder, LABELOffset);
        return ACWConstraintSet.endACWConstraintSet(builder);
    }
    unpack() {
        return new ACWConstraintSetT(this.OPERATOR(), this.bb.createObjList(this.CONSTRAINTS.bind(this), this.constraintsLength()), this.bb.createObjList(this.SETS.bind(this), this.setsLength()), this.LABEL());
    }
    unpackTo(_o) {
        _o.OPERATOR = this.OPERATOR();
        _o.CONSTRAINTS = this.bb.createObjList(this.CONSTRAINTS.bind(this), this.constraintsLength());
        _o.SETS = this.bb.createObjList(this.SETS.bind(this), this.setsLength());
        _o.LABEL = this.LABEL();
    }
}
export class ACWConstraintSetT {
    constructor(OPERATOR = acwConstraintOperator.ALL_OF, CONSTRAINTS = [], SETS = [], LABEL = null) {
        this.OPERATOR = OPERATOR;
        this.CONSTRAINTS = CONSTRAINTS;
        this.SETS = SETS;
        this.LABEL = LABEL;
    }
    pack(builder) {
        const CONSTRAINTS = ACWConstraintSet.createConstraintsVector(builder, builder.createObjectOffsetList(this.CONSTRAINTS));
        const SETS = ACWConstraintSet.createSetsVector(builder, builder.createObjectOffsetList(this.SETS));
        const LABEL = (this.LABEL !== null ? builder.createString(this.LABEL) : 0);
        return ACWConstraintSet.createACWConstraintSet(builder, this.OPERATOR, CONSTRAINTS, SETS, LABEL);
    }
}
