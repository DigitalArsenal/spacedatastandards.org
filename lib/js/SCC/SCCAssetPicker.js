// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { assetCatalogTab } from './assetCatalogTab.js';
/**
 * Asset-picker request or acknowledgement.
 */
export class SCCAssetPicker {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCCAssetPicker(bb, obj) {
        return (obj || new SCCAssetPicker()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCCAssetPicker(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCCAssetPicker()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    REQUEST_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Initial catalog tab to display in the asset picker.
     */
    CATALOG_TAB() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : assetCatalogTab.UNKNOWN;
    }
    /**
     * True when the receiver accepted or completed the picker request.
     */
    ACKNOWLEDGED() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startSCCAssetPicker(builder) {
        builder.startObject(3);
    }
    static addRequestId(builder, REQUEST_IDOffset) {
        builder.addFieldOffset(0, REQUEST_IDOffset, 0);
    }
    static addCatalogTab(builder, CATALOG_TAB) {
        builder.addFieldInt8(1, CATALOG_TAB, assetCatalogTab.UNKNOWN);
    }
    static addAcknowledged(builder, ACKNOWLEDGED) {
        builder.addFieldInt8(2, +ACKNOWLEDGED, +false);
    }
    static endSCCAssetPicker(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCCAssetPicker(builder, REQUEST_IDOffset, CATALOG_TAB, ACKNOWLEDGED) {
        SCCAssetPicker.startSCCAssetPicker(builder);
        SCCAssetPicker.addRequestId(builder, REQUEST_IDOffset);
        SCCAssetPicker.addCatalogTab(builder, CATALOG_TAB);
        SCCAssetPicker.addAcknowledged(builder, ACKNOWLEDGED);
        return SCCAssetPicker.endSCCAssetPicker(builder);
    }
    unpack() {
        return new SCCAssetPickerT(this.REQUEST_ID(), this.CATALOG_TAB(), this.ACKNOWLEDGED());
    }
    unpackTo(_o) {
        _o.REQUEST_ID = this.REQUEST_ID();
        _o.CATALOG_TAB = this.CATALOG_TAB();
        _o.ACKNOWLEDGED = this.ACKNOWLEDGED();
    }
}
export class SCCAssetPickerT {
    constructor(REQUEST_ID = null, CATALOG_TAB = assetCatalogTab.UNKNOWN, ACKNOWLEDGED = false) {
        this.REQUEST_ID = REQUEST_ID;
        this.CATALOG_TAB = CATALOG_TAB;
        this.ACKNOWLEDGED = ACKNOWLEDGED;
    }
    pack(builder) {
        const REQUEST_ID = (this.REQUEST_ID !== null ? builder.createString(this.REQUEST_ID) : 0);
        return SCCAssetPicker.createSCCAssetPicker(builder, REQUEST_ID, this.CATALOG_TAB, this.ACKNOWLEDGED);
    }
}
