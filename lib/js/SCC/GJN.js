// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GJNBoundingBox } from './GJNBoundingBox.js';
import { GJNFeature } from './GJNFeature.js';
/**
 * GeoJSON FeatureCollection
 */
export class GJN {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGJN(bb, obj) {
        return (obj || new GJN()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGJN(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GJN()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$GJN');
    }
    /**
     * Features in the collection
     */
    FEATURES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new GJNFeature()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    featuresLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Bounding box (optional)
     */
    BBOX(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new GJNBoundingBox()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startGJN(builder) {
        builder.startObject(2);
    }
    static addFeatures(builder, FEATURESOffset) {
        builder.addFieldOffset(0, FEATURESOffset, 0);
    }
    static createFeaturesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startFeaturesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addBbox(builder, BBOXOffset) {
        builder.addFieldOffset(1, BBOXOffset, 0);
    }
    static endGJN(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishGJNBuffer(builder, offset) {
        builder.finish(offset, '$GJN');
    }
    static finishSizePrefixedGJNBuffer(builder, offset) {
        builder.finish(offset, '$GJN', true);
    }
    unpack() {
        return new GJNT(this.bb.createObjList(this.FEATURES.bind(this), this.featuresLength()), (this.BBOX() !== null ? this.BBOX().unpack() : null));
    }
    unpackTo(_o) {
        _o.FEATURES = this.bb.createObjList(this.FEATURES.bind(this), this.featuresLength());
        _o.BBOX = (this.BBOX() !== null ? this.BBOX().unpack() : null);
    }
}
export class GJNT {
    constructor(FEATURES = [], BBOX = null) {
        this.FEATURES = FEATURES;
        this.BBOX = BBOX;
    }
    pack(builder) {
        const FEATURES = GJN.createFeaturesVector(builder, builder.createObjectOffsetList(this.FEATURES));
        const BBOX = (this.BBOX !== null ? this.BBOX.pack(builder) : 0);
        GJN.startGJN(builder);
        GJN.addFeatures(builder, FEATURES);
        GJN.addBbox(builder, BBOX);
        return GJN.endGJN(builder);
    }
}
