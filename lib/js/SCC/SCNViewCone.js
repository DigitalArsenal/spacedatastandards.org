// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Sensor or site view-cone overlay used by scenario tools.
 */
export class SCNViewCone {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCNViewCone(bb, obj) {
        return (obj || new SCNViewCone()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCNViewCone(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCNViewCone()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Minimum elevation angle in degrees.
     */
    MIN_ELEVATION() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum elevation angle in degrees.
     */
    MAX_ELEVATION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Minimum azimuth angle in degrees.
     */
    MIN_AZIMUTH() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum azimuth angle in degrees.
     */
    MAX_AZIMUTH() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum view-cone range in kilometers.
     */
    MAX_RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Half angle in degrees for cone-style overlays.
     */
    HALF_ANGLE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    DYNAMIC_RANGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startSCNViewCone(builder) {
        builder.startObject(7);
    }
    static addMinElevation(builder, MIN_ELEVATION) {
        builder.addFieldFloat64(0, MIN_ELEVATION, 0.0);
    }
    static addMaxElevation(builder, MAX_ELEVATION) {
        builder.addFieldFloat64(1, MAX_ELEVATION, 0.0);
    }
    static addMinAzimuth(builder, MIN_AZIMUTH) {
        builder.addFieldFloat64(2, MIN_AZIMUTH, 0.0);
    }
    static addMaxAzimuth(builder, MAX_AZIMUTH) {
        builder.addFieldFloat64(3, MAX_AZIMUTH, 0.0);
    }
    static addMaxRange(builder, MAX_RANGE) {
        builder.addFieldFloat64(4, MAX_RANGE, 0.0);
    }
    static addHalfAngle(builder, HALF_ANGLE) {
        builder.addFieldFloat64(5, HALF_ANGLE, 0.0);
    }
    static addDynamicRange(builder, DYNAMIC_RANGEOffset) {
        builder.addFieldOffset(6, DYNAMIC_RANGEOffset, 0);
    }
    static endSCNViewCone(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCNViewCone(builder, MIN_ELEVATION, MAX_ELEVATION, MIN_AZIMUTH, MAX_AZIMUTH, MAX_RANGE, HALF_ANGLE, DYNAMIC_RANGEOffset) {
        SCNViewCone.startSCNViewCone(builder);
        SCNViewCone.addMinElevation(builder, MIN_ELEVATION);
        SCNViewCone.addMaxElevation(builder, MAX_ELEVATION);
        SCNViewCone.addMinAzimuth(builder, MIN_AZIMUTH);
        SCNViewCone.addMaxAzimuth(builder, MAX_AZIMUTH);
        SCNViewCone.addMaxRange(builder, MAX_RANGE);
        SCNViewCone.addHalfAngle(builder, HALF_ANGLE);
        SCNViewCone.addDynamicRange(builder, DYNAMIC_RANGEOffset);
        return SCNViewCone.endSCNViewCone(builder);
    }
    unpack() {
        return new SCNViewConeT(this.MIN_ELEVATION(), this.MAX_ELEVATION(), this.MIN_AZIMUTH(), this.MAX_AZIMUTH(), this.MAX_RANGE(), this.HALF_ANGLE(), this.DYNAMIC_RANGE());
    }
    unpackTo(_o) {
        _o.MIN_ELEVATION = this.MIN_ELEVATION();
        _o.MAX_ELEVATION = this.MAX_ELEVATION();
        _o.MIN_AZIMUTH = this.MIN_AZIMUTH();
        _o.MAX_AZIMUTH = this.MAX_AZIMUTH();
        _o.MAX_RANGE = this.MAX_RANGE();
        _o.HALF_ANGLE = this.HALF_ANGLE();
        _o.DYNAMIC_RANGE = this.DYNAMIC_RANGE();
    }
}
export class SCNViewConeT {
    constructor(MIN_ELEVATION = 0.0, MAX_ELEVATION = 0.0, MIN_AZIMUTH = 0.0, MAX_AZIMUTH = 0.0, MAX_RANGE = 0.0, HALF_ANGLE = 0.0, DYNAMIC_RANGE = null) {
        this.MIN_ELEVATION = MIN_ELEVATION;
        this.MAX_ELEVATION = MAX_ELEVATION;
        this.MIN_AZIMUTH = MIN_AZIMUTH;
        this.MAX_AZIMUTH = MAX_AZIMUTH;
        this.MAX_RANGE = MAX_RANGE;
        this.HALF_ANGLE = HALF_ANGLE;
        this.DYNAMIC_RANGE = DYNAMIC_RANGE;
    }
    pack(builder) {
        const DYNAMIC_RANGE = (this.DYNAMIC_RANGE !== null ? builder.createString(this.DYNAMIC_RANGE) : 0);
        return SCNViewCone.createSCNViewCone(builder, this.MIN_ELEVATION, this.MAX_ELEVATION, this.MIN_AZIMUTH, this.MAX_AZIMUTH, this.MAX_RANGE, this.HALF_ANGLE, DYNAMIC_RANGE);
    }
}
