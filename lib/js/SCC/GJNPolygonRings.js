// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GJNLinearRing } from './GJNLinearRing.js';
/**
 * A polygon represented as an array of rings (outer boundary + holes)
 */
export class GJNPolygonRings {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGJNPolygonRings(bb, obj) {
        return (obj || new GJNPolygonRings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGJNPolygonRings(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GJNPolygonRings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Rings: first is outer boundary, rest are holes
     */
    RINGS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new GJNLinearRing()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    ringsLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startGJNPolygonRings(builder) {
        builder.startObject(1);
    }
    static addRings(builder, RINGSOffset) {
        builder.addFieldOffset(0, RINGSOffset, 0);
    }
    static createRingsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRingsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endGJNPolygonRings(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGJNPolygonRings(builder, RINGSOffset) {
        GJNPolygonRings.startGJNPolygonRings(builder);
        GJNPolygonRings.addRings(builder, RINGSOffset);
        return GJNPolygonRings.endGJNPolygonRings(builder);
    }
    unpack() {
        return new GJNPolygonRingsT(this.bb.createObjList(this.RINGS.bind(this), this.ringsLength()));
    }
    unpackTo(_o) {
        _o.RINGS = this.bb.createObjList(this.RINGS.bind(this), this.ringsLength());
    }
}
export class GJNPolygonRingsT {
    constructor(RINGS = []) {
        this.RINGS = RINGS;
    }
    pack(builder) {
        const RINGS = GJNPolygonRings.createRingsVector(builder, builder.createObjectOffsetList(this.RINGS));
        return GJNPolygonRings.createGJNPolygonRings(builder, RINGS);
    }
}
