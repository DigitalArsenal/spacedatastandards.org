// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GJNPosition } from './GJNPosition.js';
/**
 * Time-tagged point or annotation shown in a scenario.
 */
export class SCNPointOfInterest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCNPointOfInterest(bb, obj) {
        return (obj || new SCNPointOfInterest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCNPointOfInterest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCNPointOfInterest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Seconds before the epoch when highlighting begins.
     */
    HIGHLIGHT_BEFORE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Seconds after the epoch when highlighting remains active.
     */
    HIGHLIGHT_AFTER() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    COLOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * WGS84 geodetic position for the point of interest.
     */
    POSITION(obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new GJNPosition()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startSCNPointOfInterest(builder) {
        builder.startObject(7);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(1, DESCRIPTIONOffset, 0);
    }
    static addEpoch(builder, EPOCHOffset) {
        builder.addFieldOffset(2, EPOCHOffset, 0);
    }
    static addHighlightBefore(builder, HIGHLIGHT_BEFORE) {
        builder.addFieldFloat64(3, HIGHLIGHT_BEFORE, 0.0);
    }
    static addHighlightAfter(builder, HIGHLIGHT_AFTER) {
        builder.addFieldFloat64(4, HIGHLIGHT_AFTER, 0.0);
    }
    static addColor(builder, COLOROffset) {
        builder.addFieldOffset(5, COLOROffset, 0);
    }
    static addPosition(builder, POSITIONOffset) {
        builder.addFieldOffset(6, POSITIONOffset, 0);
    }
    static endSCNPointOfInterest(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new SCNPointOfInterestT(this.NAME(), this.DESCRIPTION(), this.EPOCH(), this.HIGHLIGHT_BEFORE(), this.HIGHLIGHT_AFTER(), this.COLOR(), (this.POSITION() !== null ? this.POSITION().unpack() : null));
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.DESCRIPTION = this.DESCRIPTION();
        _o.EPOCH = this.EPOCH();
        _o.HIGHLIGHT_BEFORE = this.HIGHLIGHT_BEFORE();
        _o.HIGHLIGHT_AFTER = this.HIGHLIGHT_AFTER();
        _o.COLOR = this.COLOR();
        _o.POSITION = (this.POSITION() !== null ? this.POSITION().unpack() : null);
    }
}
export class SCNPointOfInterestT {
    constructor(NAME = null, DESCRIPTION = null, EPOCH = null, HIGHLIGHT_BEFORE = 0.0, HIGHLIGHT_AFTER = 0.0, COLOR = null, POSITION = null) {
        this.NAME = NAME;
        this.DESCRIPTION = DESCRIPTION;
        this.EPOCH = EPOCH;
        this.HIGHLIGHT_BEFORE = HIGHLIGHT_BEFORE;
        this.HIGHLIGHT_AFTER = HIGHLIGHT_AFTER;
        this.COLOR = COLOR;
        this.POSITION = POSITION;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH) : 0);
        const COLOR = (this.COLOR !== null ? builder.createString(this.COLOR) : 0);
        const POSITION = (this.POSITION !== null ? this.POSITION.pack(builder) : 0);
        SCNPointOfInterest.startSCNPointOfInterest(builder);
        SCNPointOfInterest.addName(builder, NAME);
        SCNPointOfInterest.addDescription(builder, DESCRIPTION);
        SCNPointOfInterest.addEpoch(builder, EPOCH);
        SCNPointOfInterest.addHighlightBefore(builder, this.HIGHLIGHT_BEFORE);
        SCNPointOfInterest.addHighlightAfter(builder, this.HIGHLIGHT_AFTER);
        SCNPointOfInterest.addColor(builder, COLOR);
        SCNPointOfInterest.addPosition(builder, POSITION);
        return SCNPointOfInterest.endSCNPointOfInterest(builder);
    }
}
