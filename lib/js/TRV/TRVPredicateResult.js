// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { TRVBondEvidence } from './TRVBondEvidence.js';
import { trpPredicateKind } from './trpPredicateKind.js';
/**
 * Outcome and evidence for one policy predicate.
 */
export class TRVPredicateResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRVPredicateResult(bb, obj) {
        return (obj || new TRVPredicateResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRVPredicateResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRVPredicateResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    PREDICATE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Predicate operation copied from the evaluated policy.
     */
    KIND() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : trpPredicateKind.MinValueLocked;
    }
    /**
     * Whether this predicate passed.
     */
    PASSED() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Value measured by the evaluator.
     */
    MEASURED_VALUE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Threshold required by the evaluated predicate.
     */
    REQUIRED_VALUE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Chain evidence used by value and duration predicates.
     */
    BOND_EVIDENCE(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new TRVBondEvidence()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    bondEvidenceLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    TRUSTER_IDS_MATCHED(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    trusterIdsMatchedLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    EVIDENCE_TEXT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startTRVPredicateResult(builder) {
        builder.startObject(8);
    }
    static addPredicateId(builder, PREDICATE_IDOffset) {
        builder.addFieldOffset(0, PREDICATE_IDOffset, 0);
    }
    static addKind(builder, KIND) {
        builder.addFieldInt8(1, KIND, trpPredicateKind.MinValueLocked);
    }
    static addPassed(builder, PASSED) {
        builder.addFieldInt8(2, +PASSED, +false);
    }
    static addMeasuredValue(builder, MEASURED_VALUE) {
        builder.addFieldFloat64(3, MEASURED_VALUE, 0.0);
    }
    static addRequiredValue(builder, REQUIRED_VALUE) {
        builder.addFieldFloat64(4, REQUIRED_VALUE, 0.0);
    }
    static addBondEvidence(builder, BOND_EVIDENCEOffset) {
        builder.addFieldOffset(5, BOND_EVIDENCEOffset, 0);
    }
    static createBondEvidenceVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startBondEvidenceVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTrusterIdsMatched(builder, TRUSTER_IDS_MATCHEDOffset) {
        builder.addFieldOffset(6, TRUSTER_IDS_MATCHEDOffset, 0);
    }
    static createTrusterIdsMatchedVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTrusterIdsMatchedVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addEvidenceText(builder, EVIDENCE_TEXTOffset) {
        builder.addFieldOffset(7, EVIDENCE_TEXTOffset, 0);
    }
    static endTRVPredicateResult(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createTRVPredicateResult(builder, PREDICATE_IDOffset, KIND, PASSED, MEASURED_VALUE, REQUIRED_VALUE, BOND_EVIDENCEOffset, TRUSTER_IDS_MATCHEDOffset, EVIDENCE_TEXTOffset) {
        TRVPredicateResult.startTRVPredicateResult(builder);
        TRVPredicateResult.addPredicateId(builder, PREDICATE_IDOffset);
        TRVPredicateResult.addKind(builder, KIND);
        TRVPredicateResult.addPassed(builder, PASSED);
        TRVPredicateResult.addMeasuredValue(builder, MEASURED_VALUE);
        TRVPredicateResult.addRequiredValue(builder, REQUIRED_VALUE);
        TRVPredicateResult.addBondEvidence(builder, BOND_EVIDENCEOffset);
        TRVPredicateResult.addTrusterIdsMatched(builder, TRUSTER_IDS_MATCHEDOffset);
        TRVPredicateResult.addEvidenceText(builder, EVIDENCE_TEXTOffset);
        return TRVPredicateResult.endTRVPredicateResult(builder);
    }
    unpack() {
        return new TRVPredicateResultT(this.PREDICATE_ID(), this.KIND(), this.PASSED(), this.MEASURED_VALUE(), this.REQUIRED_VALUE(), this.bb.createObjList(this.BOND_EVIDENCE.bind(this), this.bondEvidenceLength()), this.bb.createScalarList(this.TRUSTER_IDS_MATCHED.bind(this), this.trusterIdsMatchedLength()), this.EVIDENCE_TEXT());
    }
    unpackTo(_o) {
        _o.PREDICATE_ID = this.PREDICATE_ID();
        _o.KIND = this.KIND();
        _o.PASSED = this.PASSED();
        _o.MEASURED_VALUE = this.MEASURED_VALUE();
        _o.REQUIRED_VALUE = this.REQUIRED_VALUE();
        _o.BOND_EVIDENCE = this.bb.createObjList(this.BOND_EVIDENCE.bind(this), this.bondEvidenceLength());
        _o.TRUSTER_IDS_MATCHED = this.bb.createScalarList(this.TRUSTER_IDS_MATCHED.bind(this), this.trusterIdsMatchedLength());
        _o.EVIDENCE_TEXT = this.EVIDENCE_TEXT();
    }
}
export class TRVPredicateResultT {
    constructor(PREDICATE_ID = null, KIND = trpPredicateKind.MinValueLocked, PASSED = false, MEASURED_VALUE = 0.0, REQUIRED_VALUE = 0.0, BOND_EVIDENCE = [], TRUSTER_IDS_MATCHED = [], EVIDENCE_TEXT = null) {
        this.PREDICATE_ID = PREDICATE_ID;
        this.KIND = KIND;
        this.PASSED = PASSED;
        this.MEASURED_VALUE = MEASURED_VALUE;
        this.REQUIRED_VALUE = REQUIRED_VALUE;
        this.BOND_EVIDENCE = BOND_EVIDENCE;
        this.TRUSTER_IDS_MATCHED = TRUSTER_IDS_MATCHED;
        this.EVIDENCE_TEXT = EVIDENCE_TEXT;
    }
    pack(builder) {
        const PREDICATE_ID = (this.PREDICATE_ID !== null ? builder.createString(this.PREDICATE_ID) : 0);
        const BOND_EVIDENCE = TRVPredicateResult.createBondEvidenceVector(builder, builder.createObjectOffsetList(this.BOND_EVIDENCE));
        const TRUSTER_IDS_MATCHED = TRVPredicateResult.createTrusterIdsMatchedVector(builder, builder.createObjectOffsetList(this.TRUSTER_IDS_MATCHED));
        const EVIDENCE_TEXT = (this.EVIDENCE_TEXT !== null ? builder.createString(this.EVIDENCE_TEXT) : 0);
        return TRVPredicateResult.createTRVPredicateResult(builder, PREDICATE_ID, this.KIND, this.PASSED, this.MEASURED_VALUE, this.REQUIRED_VALUE, BOND_EVIDENCE, TRUSTER_IDS_MATCHED, EVIDENCE_TEXT);
    }
}
