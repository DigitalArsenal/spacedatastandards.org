// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { TRVPredicateResult } from './TRVPredicateResult.js';
/**
 * Trust Rule Verdict.
 *
 * A TRV records one reproducible evaluation of a `$TRP` policy for a subject.
 * This record replaces ad-hoc trust status events.
 */
export class TRV {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRV(bb, obj) {
        return (obj || new TRV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRV(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$TRV');
    }
    VERDICT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    POLICY_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SUBJECT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    /**
     * Whether the complete policy passed.
     */
    PASSED() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Pass state produced by the immediately preceding evaluation.
     */
    PREVIOUS_PASSED() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Score produced by this evaluation.
     */
    SCORE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Score produced by the immediately preceding evaluation.
     */
    PREVIOUS_SCORE() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Per-predicate results and their evidence.
     */
    RESULTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new TRVPredicateResult()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    resultsLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    TRIGGER(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Unix timestamp in milliseconds when the evaluation completed.
     */
    EVALUATED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    EVALUATOR_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Evaluator signature over the verdict record.
     */
    EVALUATOR_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    evaluatorSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    evaluatorSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startTRV(builder) {
        builder.startObject(12);
    }
    static addVerdictId(builder, VERDICT_IDOffset) {
        builder.addFieldOffset(0, VERDICT_IDOffset, 0);
    }
    static addPolicyId(builder, POLICY_IDOffset) {
        builder.addFieldOffset(1, POLICY_IDOffset, 0);
    }
    static addSubjectId(builder, SUBJECT_IDOffset) {
        builder.addFieldOffset(2, SUBJECT_IDOffset, 0);
    }
    static addPassed(builder, PASSED) {
        builder.addFieldInt8(3, +PASSED, +false);
    }
    static addPreviousPassed(builder, PREVIOUS_PASSED) {
        builder.addFieldInt8(4, +PREVIOUS_PASSED, +false);
    }
    static addScore(builder, SCORE) {
        builder.addFieldFloat64(5, SCORE, 0.0);
    }
    static addPreviousScore(builder, PREVIOUS_SCORE) {
        builder.addFieldFloat64(6, PREVIOUS_SCORE, 0.0);
    }
    static addResults(builder, RESULTSOffset) {
        builder.addFieldOffset(7, RESULTSOffset, 0);
    }
    static createResultsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startResultsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTrigger(builder, TRIGGEROffset) {
        builder.addFieldOffset(8, TRIGGEROffset, 0);
    }
    static addEvaluatedAt(builder, EVALUATED_AT) {
        builder.addFieldInt64(9, EVALUATED_AT, BigInt('0'));
    }
    static addEvaluatorPeerId(builder, EVALUATOR_PEER_IDOffset) {
        builder.addFieldOffset(10, EVALUATOR_PEER_IDOffset, 0);
    }
    static addEvaluatorSignature(builder, EVALUATOR_SIGNATUREOffset) {
        builder.addFieldOffset(11, EVALUATOR_SIGNATUREOffset, 0);
    }
    static createEvaluatorSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startEvaluatorSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endTRV(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // VERDICT_ID
        builder.requiredField(offset, 6); // POLICY_ID
        builder.requiredField(offset, 8); // SUBJECT_ID
        return offset;
    }
    static finishTRVBuffer(builder, offset) {
        builder.finish(offset, '$TRV');
    }
    static finishSizePrefixedTRVBuffer(builder, offset) {
        builder.finish(offset, '$TRV', true);
    }
    static createTRV(builder, VERDICT_IDOffset, POLICY_IDOffset, SUBJECT_IDOffset, PASSED, PREVIOUS_PASSED, SCORE, PREVIOUS_SCORE, RESULTSOffset, TRIGGEROffset, EVALUATED_AT, EVALUATOR_PEER_IDOffset, EVALUATOR_SIGNATUREOffset) {
        TRV.startTRV(builder);
        TRV.addVerdictId(builder, VERDICT_IDOffset);
        TRV.addPolicyId(builder, POLICY_IDOffset);
        TRV.addSubjectId(builder, SUBJECT_IDOffset);
        TRV.addPassed(builder, PASSED);
        TRV.addPreviousPassed(builder, PREVIOUS_PASSED);
        TRV.addScore(builder, SCORE);
        TRV.addPreviousScore(builder, PREVIOUS_SCORE);
        TRV.addResults(builder, RESULTSOffset);
        TRV.addTrigger(builder, TRIGGEROffset);
        TRV.addEvaluatedAt(builder, EVALUATED_AT);
        TRV.addEvaluatorPeerId(builder, EVALUATOR_PEER_IDOffset);
        TRV.addEvaluatorSignature(builder, EVALUATOR_SIGNATUREOffset);
        return TRV.endTRV(builder);
    }
    unpack() {
        return new TRVT(this.VERDICT_ID(), this.POLICY_ID(), this.SUBJECT_ID(), this.PASSED(), this.PREVIOUS_PASSED(), this.SCORE(), this.PREVIOUS_SCORE(), this.bb.createObjList(this.RESULTS.bind(this), this.resultsLength()), this.TRIGGER(), this.EVALUATED_AT(), this.EVALUATOR_PEER_ID(), this.bb.createScalarList(this.EVALUATOR_SIGNATURE.bind(this), this.evaluatorSignatureLength()));
    }
    unpackTo(_o) {
        _o.VERDICT_ID = this.VERDICT_ID();
        _o.POLICY_ID = this.POLICY_ID();
        _o.SUBJECT_ID = this.SUBJECT_ID();
        _o.PASSED = this.PASSED();
        _o.PREVIOUS_PASSED = this.PREVIOUS_PASSED();
        _o.SCORE = this.SCORE();
        _o.PREVIOUS_SCORE = this.PREVIOUS_SCORE();
        _o.RESULTS = this.bb.createObjList(this.RESULTS.bind(this), this.resultsLength());
        _o.TRIGGER = this.TRIGGER();
        _o.EVALUATED_AT = this.EVALUATED_AT();
        _o.EVALUATOR_PEER_ID = this.EVALUATOR_PEER_ID();
        _o.EVALUATOR_SIGNATURE = this.bb.createScalarList(this.EVALUATOR_SIGNATURE.bind(this), this.evaluatorSignatureLength());
    }
}
export class TRVT {
    constructor(VERDICT_ID = null, POLICY_ID = null, SUBJECT_ID = null, PASSED = false, PREVIOUS_PASSED = false, SCORE = 0.0, PREVIOUS_SCORE = 0.0, RESULTS = [], TRIGGER = null, EVALUATED_AT = BigInt('0'), EVALUATOR_PEER_ID = null, EVALUATOR_SIGNATURE = []) {
        this.VERDICT_ID = VERDICT_ID;
        this.POLICY_ID = POLICY_ID;
        this.SUBJECT_ID = SUBJECT_ID;
        this.PASSED = PASSED;
        this.PREVIOUS_PASSED = PREVIOUS_PASSED;
        this.SCORE = SCORE;
        this.PREVIOUS_SCORE = PREVIOUS_SCORE;
        this.RESULTS = RESULTS;
        this.TRIGGER = TRIGGER;
        this.EVALUATED_AT = EVALUATED_AT;
        this.EVALUATOR_PEER_ID = EVALUATOR_PEER_ID;
        this.EVALUATOR_SIGNATURE = EVALUATOR_SIGNATURE;
    }
    pack(builder) {
        const VERDICT_ID = (this.VERDICT_ID !== null ? builder.createString(this.VERDICT_ID) : 0);
        const POLICY_ID = (this.POLICY_ID !== null ? builder.createString(this.POLICY_ID) : 0);
        const SUBJECT_ID = (this.SUBJECT_ID !== null ? builder.createString(this.SUBJECT_ID) : 0);
        const RESULTS = TRV.createResultsVector(builder, builder.createObjectOffsetList(this.RESULTS));
        const TRIGGER = (this.TRIGGER !== null ? builder.createString(this.TRIGGER) : 0);
        const EVALUATOR_PEER_ID = (this.EVALUATOR_PEER_ID !== null ? builder.createString(this.EVALUATOR_PEER_ID) : 0);
        const EVALUATOR_SIGNATURE = TRV.createEvaluatorSignatureVector(builder, this.EVALUATOR_SIGNATURE);
        return TRV.createTRV(builder, VERDICT_ID, POLICY_ID, SUBJECT_ID, this.PASSED, this.PREVIOUS_PASSED, this.SCORE, this.PREVIOUS_SCORE, RESULTS, TRIGGER, this.EVALUATED_AT, EVALUATOR_PEER_ID, EVALUATOR_SIGNATURE);
    }
}
