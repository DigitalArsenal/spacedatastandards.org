// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { CZMColor } from './CZMColor.js';
import { CZMHeightReference } from './CZMHeightReference.js';
/**
 * Model properties (glTF)
 */
export class CZMModel {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCZMModel(bb, obj) {
        return (obj || new CZMModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCZMModel(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CZMModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Whether the model is displayed
     */
    SHOW() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    GLTF(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Scale factor
     */
    SCALE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Minimum pixel size
     */
    MINIMUM_PIXEL_SIZE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum scale
     */
    MAXIMUM_SCALE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Height reference
     */
    HEIGHT_REFERENCE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : CZMHeightReference.NONE;
    }
    /**
     * Color tint
     */
    COLOR(obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new CZMColor()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Whether to incrementally load textures
     */
    INCREMENTALLY_LOAD_TEXTURES() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Whether to run animations
     */
    RUN_ANIMATIONS() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    SHADOWS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Silhouette color
     */
    SILHOUETTE_COLOR(obj) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? (obj || new CZMColor()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Silhouette size in pixels
     */
    SILHOUETTE_SIZE() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    COLOR_BLEND_MODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Color blend amount (0-1)
     */
    COLOR_BLEND_AMOUNT() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startCZMModel(builder) {
        builder.startObject(14);
    }
    static addShow(builder, SHOW) {
        builder.addFieldInt8(0, +SHOW, +false);
    }
    static addGltf(builder, GLTFOffset) {
        builder.addFieldOffset(1, GLTFOffset, 0);
    }
    static addScale(builder, SCALE) {
        builder.addFieldFloat64(2, SCALE, 0.0);
    }
    static addMinimumPixelSize(builder, MINIMUM_PIXEL_SIZE) {
        builder.addFieldFloat64(3, MINIMUM_PIXEL_SIZE, 0.0);
    }
    static addMaximumScale(builder, MAXIMUM_SCALE) {
        builder.addFieldFloat64(4, MAXIMUM_SCALE, 0.0);
    }
    static addHeightReference(builder, HEIGHT_REFERENCE) {
        builder.addFieldInt8(5, HEIGHT_REFERENCE, CZMHeightReference.NONE);
    }
    static addColor(builder, COLOROffset) {
        builder.addFieldOffset(6, COLOROffset, 0);
    }
    static addIncrementallyLoadTextures(builder, INCREMENTALLY_LOAD_TEXTURES) {
        builder.addFieldInt8(7, +INCREMENTALLY_LOAD_TEXTURES, +false);
    }
    static addRunAnimations(builder, RUN_ANIMATIONS) {
        builder.addFieldInt8(8, +RUN_ANIMATIONS, +false);
    }
    static addShadows(builder, SHADOWSOffset) {
        builder.addFieldOffset(9, SHADOWSOffset, 0);
    }
    static addSilhouetteColor(builder, SILHOUETTE_COLOROffset) {
        builder.addFieldOffset(10, SILHOUETTE_COLOROffset, 0);
    }
    static addSilhouetteSize(builder, SILHOUETTE_SIZE) {
        builder.addFieldFloat64(11, SILHOUETTE_SIZE, 0.0);
    }
    static addColorBlendMode(builder, COLOR_BLEND_MODEOffset) {
        builder.addFieldOffset(12, COLOR_BLEND_MODEOffset, 0);
    }
    static addColorBlendAmount(builder, COLOR_BLEND_AMOUNT) {
        builder.addFieldFloat64(13, COLOR_BLEND_AMOUNT, 0.0);
    }
    static endCZMModel(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new CZMModelT(this.SHOW(), this.GLTF(), this.SCALE(), this.MINIMUM_PIXEL_SIZE(), this.MAXIMUM_SCALE(), this.HEIGHT_REFERENCE(), (this.COLOR() !== null ? this.COLOR().unpack() : null), this.INCREMENTALLY_LOAD_TEXTURES(), this.RUN_ANIMATIONS(), this.SHADOWS(), (this.SILHOUETTE_COLOR() !== null ? this.SILHOUETTE_COLOR().unpack() : null), this.SILHOUETTE_SIZE(), this.COLOR_BLEND_MODE(), this.COLOR_BLEND_AMOUNT());
    }
    unpackTo(_o) {
        _o.SHOW = this.SHOW();
        _o.GLTF = this.GLTF();
        _o.SCALE = this.SCALE();
        _o.MINIMUM_PIXEL_SIZE = this.MINIMUM_PIXEL_SIZE();
        _o.MAXIMUM_SCALE = this.MAXIMUM_SCALE();
        _o.HEIGHT_REFERENCE = this.HEIGHT_REFERENCE();
        _o.COLOR = (this.COLOR() !== null ? this.COLOR().unpack() : null);
        _o.INCREMENTALLY_LOAD_TEXTURES = this.INCREMENTALLY_LOAD_TEXTURES();
        _o.RUN_ANIMATIONS = this.RUN_ANIMATIONS();
        _o.SHADOWS = this.SHADOWS();
        _o.SILHOUETTE_COLOR = (this.SILHOUETTE_COLOR() !== null ? this.SILHOUETTE_COLOR().unpack() : null);
        _o.SILHOUETTE_SIZE = this.SILHOUETTE_SIZE();
        _o.COLOR_BLEND_MODE = this.COLOR_BLEND_MODE();
        _o.COLOR_BLEND_AMOUNT = this.COLOR_BLEND_AMOUNT();
    }
}
export class CZMModelT {
    constructor(SHOW = false, GLTF = null, SCALE = 0.0, MINIMUM_PIXEL_SIZE = 0.0, MAXIMUM_SCALE = 0.0, HEIGHT_REFERENCE = CZMHeightReference.NONE, COLOR = null, INCREMENTALLY_LOAD_TEXTURES = false, RUN_ANIMATIONS = false, SHADOWS = null, SILHOUETTE_COLOR = null, SILHOUETTE_SIZE = 0.0, COLOR_BLEND_MODE = null, COLOR_BLEND_AMOUNT = 0.0) {
        this.SHOW = SHOW;
        this.GLTF = GLTF;
        this.SCALE = SCALE;
        this.MINIMUM_PIXEL_SIZE = MINIMUM_PIXEL_SIZE;
        this.MAXIMUM_SCALE = MAXIMUM_SCALE;
        this.HEIGHT_REFERENCE = HEIGHT_REFERENCE;
        this.COLOR = COLOR;
        this.INCREMENTALLY_LOAD_TEXTURES = INCREMENTALLY_LOAD_TEXTURES;
        this.RUN_ANIMATIONS = RUN_ANIMATIONS;
        this.SHADOWS = SHADOWS;
        this.SILHOUETTE_COLOR = SILHOUETTE_COLOR;
        this.SILHOUETTE_SIZE = SILHOUETTE_SIZE;
        this.COLOR_BLEND_MODE = COLOR_BLEND_MODE;
        this.COLOR_BLEND_AMOUNT = COLOR_BLEND_AMOUNT;
    }
    pack(builder) {
        const GLTF = (this.GLTF !== null ? builder.createString(this.GLTF) : 0);
        const COLOR = (this.COLOR !== null ? this.COLOR.pack(builder) : 0);
        const SHADOWS = (this.SHADOWS !== null ? builder.createString(this.SHADOWS) : 0);
        const SILHOUETTE_COLOR = (this.SILHOUETTE_COLOR !== null ? this.SILHOUETTE_COLOR.pack(builder) : 0);
        const COLOR_BLEND_MODE = (this.COLOR_BLEND_MODE !== null ? builder.createString(this.COLOR_BLEND_MODE) : 0);
        CZMModel.startCZMModel(builder);
        CZMModel.addShow(builder, this.SHOW);
        CZMModel.addGltf(builder, GLTF);
        CZMModel.addScale(builder, this.SCALE);
        CZMModel.addMinimumPixelSize(builder, this.MINIMUM_PIXEL_SIZE);
        CZMModel.addMaximumScale(builder, this.MAXIMUM_SCALE);
        CZMModel.addHeightReference(builder, this.HEIGHT_REFERENCE);
        CZMModel.addColor(builder, COLOR);
        CZMModel.addIncrementallyLoadTextures(builder, this.INCREMENTALLY_LOAD_TEXTURES);
        CZMModel.addRunAnimations(builder, this.RUN_ANIMATIONS);
        CZMModel.addShadows(builder, SHADOWS);
        CZMModel.addSilhouetteColor(builder, SILHOUETTE_COLOR);
        CZMModel.addSilhouetteSize(builder, this.SILHOUETTE_SIZE);
        CZMModel.addColorBlendMode(builder, COLOR_BLEND_MODE);
        CZMModel.addColorBlendAmount(builder, this.COLOR_BLEND_AMOUNT);
        return CZMModel.endCZMModel(builder);
    }
}
