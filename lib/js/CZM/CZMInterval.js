// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Time interval for time-dynamic properties
 */
export class CZMInterval {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCZMInterval(bb, obj) {
        return (obj || new CZMInterval()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCZMInterval(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CZMInterval()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    INTERVAL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startCZMInterval(builder) {
        builder.startObject(1);
    }
    static addInterval(builder, INTERVALOffset) {
        builder.addFieldOffset(0, INTERVALOffset, 0);
    }
    static endCZMInterval(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createCZMInterval(builder, INTERVALOffset) {
        CZMInterval.startCZMInterval(builder);
        CZMInterval.addInterval(builder, INTERVALOffset);
        return CZMInterval.endCZMInterval(builder);
    }
    unpack() {
        return new CZMIntervalT(this.INTERVAL());
    }
    unpackTo(_o) {
        _o.INTERVAL = this.INTERVAL();
    }
}
export class CZMIntervalT {
    constructor(INTERVAL = null) {
        this.INTERVAL = INTERVAL;
    }
    pack(builder) {
        const INTERVAL = (this.INTERVAL !== null ? builder.createString(this.INTERVAL) : 0);
        return CZMInterval.createCZMInterval(builder, INTERVAL);
    }
}
