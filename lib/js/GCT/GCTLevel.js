// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { gctLevelDerivation } from './gctLevelDerivation.js';
import { gctSampleEncoding } from './gctSampleEncoding.js';
/**
 * One level of detail of one variant: a Gaussian mixture in the variant's
 * own frame (metres, origin at the variant's reference point, Z up, base of
 * the cloud near Z = 0). Levels are ordered finest first. Every per-Gaussian
 * vector holds exactly 4 * GAUSSIAN_COUNT float32 values — one RGBA texel
 * per Gaussian — in the same Gaussian order.
 */
export class GCTLevel {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGCTLevel(bb, obj) {
        return (obj || new GCTLevel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGCTLevel(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GCTLevel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Number of Gaussians in this level.
     */
    GAUSSIAN_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * The cell this level was merged on, metres: the level's geometric error
     * in the variant frame. Multiply by the instance scale in use.
     */
    MERGE_CELL_M() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat32(this.bb_pos + offset) : 0.0;
    }
    /**
     * How this level was produced.
     */
    DERIVATION() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : gctLevelDerivation.UNSPECIFIED;
    }
    /**
     * Where the samples live.
     */
    ENCODING() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : gctSampleEncoding.INLINE_FLOAT32;
    }
    CHUNK_CID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Encoded chunk length in bytes when ENCODING is CONTENT_ADDRESSED_CHUNK.
     */
    CHUNK_BYTE_LENGTH() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Per Gaussian: x, y, z of the centre, metres in the variant frame; w =
     * amplitude in AMPLITUDE_UNITS at the centre.
     */
    CENTERS(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    centersLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    centersArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Per Gaussian: standard deviation along the variant frame's x, y, z
     * axes, metres (axis-aligned; the instance yaw rotates the frame); w = a
     * per-Gaussian seed in [0, 1) for stochastic shading, 0 when unused.
     */
    SIGMAS(index) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    sigmasLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sigmasArray() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * OPTIONAL. Per Gaussian: x, y, z of the field's boundary normal at the
     * centre (unit vector scaled by a boundary weight in [0, 1]: 0 deep
     * inside, 1 at a crisp condensation interface); w = the local coarse
     * density in [0, 1]. Absent when not computed.
     */
    BOUNDARY(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    boundaryLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    boundaryArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * OPTIONAL. Per Gaussian: x, y, z of a finer-scale surface normal that
     * adds relief to BOUNDARY; w unused (0). Absent when not computed.
     */
    FINE_NORMAL(index) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    fineNormalLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    fineNormalArray() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * OPTIONAL. Per Gaussian: optical depth of the field from the centre
     * outward along +x, +y, +z (dimensionless); w unused (0). Absent when
     * not computed.
     */
    OPTICAL_DEPTH_POS(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    opticalDepthPosLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    opticalDepthPosArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * OPTIONAL. Per Gaussian: optical depth along -x, -y, -z; w unused (0).
     * Absent when not computed.
     */
    OPTICAL_DEPTH_NEG(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    opticalDepthNegLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    opticalDepthNegArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startGCTLevel(builder) {
        builder.startObject(12);
    }
    static addGaussianCount(builder, GAUSSIAN_COUNT) {
        builder.addFieldInt32(0, GAUSSIAN_COUNT, 0);
    }
    static addMergeCellM(builder, MERGE_CELL_M) {
        builder.addFieldFloat32(1, MERGE_CELL_M, 0.0);
    }
    static addDerivation(builder, DERIVATION) {
        builder.addFieldInt8(2, DERIVATION, gctLevelDerivation.UNSPECIFIED);
    }
    static addEncoding(builder, ENCODING) {
        builder.addFieldInt8(3, ENCODING, gctSampleEncoding.INLINE_FLOAT32);
    }
    static addChunkCid(builder, CHUNK_CIDOffset) {
        builder.addFieldOffset(4, CHUNK_CIDOffset, 0);
    }
    static addChunkByteLength(builder, CHUNK_BYTE_LENGTH) {
        builder.addFieldInt64(5, CHUNK_BYTE_LENGTH, BigInt('0'));
    }
    static addCenters(builder, CENTERSOffset) {
        builder.addFieldOffset(6, CENTERSOffset, 0);
    }
    static createCentersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startCentersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSigmas(builder, SIGMASOffset) {
        builder.addFieldOffset(7, SIGMASOffset, 0);
    }
    static createSigmasVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startSigmasVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addBoundary(builder, BOUNDARYOffset) {
        builder.addFieldOffset(8, BOUNDARYOffset, 0);
    }
    static createBoundaryVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startBoundaryVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addFineNormal(builder, FINE_NORMALOffset) {
        builder.addFieldOffset(9, FINE_NORMALOffset, 0);
    }
    static createFineNormalVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startFineNormalVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addOpticalDepthPos(builder, OPTICAL_DEPTH_POSOffset) {
        builder.addFieldOffset(10, OPTICAL_DEPTH_POSOffset, 0);
    }
    static createOpticalDepthPosVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startOpticalDepthPosVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addOpticalDepthNeg(builder, OPTICAL_DEPTH_NEGOffset) {
        builder.addFieldOffset(11, OPTICAL_DEPTH_NEGOffset, 0);
    }
    static createOpticalDepthNegVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startOpticalDepthNegVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endGCTLevel(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGCTLevel(builder, GAUSSIAN_COUNT, MERGE_CELL_M, DERIVATION, ENCODING, CHUNK_CIDOffset, CHUNK_BYTE_LENGTH, CENTERSOffset, SIGMASOffset, BOUNDARYOffset, FINE_NORMALOffset, OPTICAL_DEPTH_POSOffset, OPTICAL_DEPTH_NEGOffset) {
        GCTLevel.startGCTLevel(builder);
        GCTLevel.addGaussianCount(builder, GAUSSIAN_COUNT);
        GCTLevel.addMergeCellM(builder, MERGE_CELL_M);
        GCTLevel.addDerivation(builder, DERIVATION);
        GCTLevel.addEncoding(builder, ENCODING);
        GCTLevel.addChunkCid(builder, CHUNK_CIDOffset);
        GCTLevel.addChunkByteLength(builder, CHUNK_BYTE_LENGTH);
        GCTLevel.addCenters(builder, CENTERSOffset);
        GCTLevel.addSigmas(builder, SIGMASOffset);
        GCTLevel.addBoundary(builder, BOUNDARYOffset);
        GCTLevel.addFineNormal(builder, FINE_NORMALOffset);
        GCTLevel.addOpticalDepthPos(builder, OPTICAL_DEPTH_POSOffset);
        GCTLevel.addOpticalDepthNeg(builder, OPTICAL_DEPTH_NEGOffset);
        return GCTLevel.endGCTLevel(builder);
    }
    unpack() {
        return new GCTLevelT(this.GAUSSIAN_COUNT(), this.MERGE_CELL_M(), this.DERIVATION(), this.ENCODING(), this.CHUNK_CID(), this.CHUNK_BYTE_LENGTH(), this.bb.createScalarList(this.CENTERS.bind(this), this.centersLength()), this.bb.createScalarList(this.SIGMAS.bind(this), this.sigmasLength()), this.bb.createScalarList(this.BOUNDARY.bind(this), this.boundaryLength()), this.bb.createScalarList(this.FINE_NORMAL.bind(this), this.fineNormalLength()), this.bb.createScalarList(this.OPTICAL_DEPTH_POS.bind(this), this.opticalDepthPosLength()), this.bb.createScalarList(this.OPTICAL_DEPTH_NEG.bind(this), this.opticalDepthNegLength()));
    }
    unpackTo(_o) {
        _o.GAUSSIAN_COUNT = this.GAUSSIAN_COUNT();
        _o.MERGE_CELL_M = this.MERGE_CELL_M();
        _o.DERIVATION = this.DERIVATION();
        _o.ENCODING = this.ENCODING();
        _o.CHUNK_CID = this.CHUNK_CID();
        _o.CHUNK_BYTE_LENGTH = this.CHUNK_BYTE_LENGTH();
        _o.CENTERS = this.bb.createScalarList(this.CENTERS.bind(this), this.centersLength());
        _o.SIGMAS = this.bb.createScalarList(this.SIGMAS.bind(this), this.sigmasLength());
        _o.BOUNDARY = this.bb.createScalarList(this.BOUNDARY.bind(this), this.boundaryLength());
        _o.FINE_NORMAL = this.bb.createScalarList(this.FINE_NORMAL.bind(this), this.fineNormalLength());
        _o.OPTICAL_DEPTH_POS = this.bb.createScalarList(this.OPTICAL_DEPTH_POS.bind(this), this.opticalDepthPosLength());
        _o.OPTICAL_DEPTH_NEG = this.bb.createScalarList(this.OPTICAL_DEPTH_NEG.bind(this), this.opticalDepthNegLength());
    }
}
export class GCTLevelT {
    constructor(GAUSSIAN_COUNT = 0, MERGE_CELL_M = 0.0, DERIVATION = gctLevelDerivation.UNSPECIFIED, ENCODING = gctSampleEncoding.INLINE_FLOAT32, CHUNK_CID = null, CHUNK_BYTE_LENGTH = BigInt('0'), CENTERS = [], SIGMAS = [], BOUNDARY = [], FINE_NORMAL = [], OPTICAL_DEPTH_POS = [], OPTICAL_DEPTH_NEG = []) {
        this.GAUSSIAN_COUNT = GAUSSIAN_COUNT;
        this.MERGE_CELL_M = MERGE_CELL_M;
        this.DERIVATION = DERIVATION;
        this.ENCODING = ENCODING;
        this.CHUNK_CID = CHUNK_CID;
        this.CHUNK_BYTE_LENGTH = CHUNK_BYTE_LENGTH;
        this.CENTERS = CENTERS;
        this.SIGMAS = SIGMAS;
        this.BOUNDARY = BOUNDARY;
        this.FINE_NORMAL = FINE_NORMAL;
        this.OPTICAL_DEPTH_POS = OPTICAL_DEPTH_POS;
        this.OPTICAL_DEPTH_NEG = OPTICAL_DEPTH_NEG;
    }
    pack(builder) {
        const CHUNK_CID = (this.CHUNK_CID !== null ? builder.createString(this.CHUNK_CID) : 0);
        const CENTERS = GCTLevel.createCentersVector(builder, this.CENTERS);
        const SIGMAS = GCTLevel.createSigmasVector(builder, this.SIGMAS);
        const BOUNDARY = GCTLevel.createBoundaryVector(builder, this.BOUNDARY);
        const FINE_NORMAL = GCTLevel.createFineNormalVector(builder, this.FINE_NORMAL);
        const OPTICAL_DEPTH_POS = GCTLevel.createOpticalDepthPosVector(builder, this.OPTICAL_DEPTH_POS);
        const OPTICAL_DEPTH_NEG = GCTLevel.createOpticalDepthNegVector(builder, this.OPTICAL_DEPTH_NEG);
        return GCTLevel.createGCTLevel(builder, this.GAUSSIAN_COUNT, this.MERGE_CELL_M, this.DERIVATION, this.ENCODING, CHUNK_CID, this.CHUNK_BYTE_LENGTH, CENTERS, SIGMAS, BOUNDARY, FINE_NORMAL, OPTICAL_DEPTH_POS, OPTICAL_DEPTH_NEG);
    }
}
