// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * A signed distance field of a variant's surface (the isosurface of the
 * mixture at DENSITY_THRESHOLD), on a cube of N^3 voxels over the variant's
 * bounds, stored as bytes: distance / DISTANCE_SCALE_M, negative inside,
 * clamped to [-128, 127]. Optional; a consumer without it sphere-traces
 * nothing and lights the variant from an atlas instead.
 */
export class GCTDistanceField {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGCTDistanceField(bb, obj) {
        return (obj || new GCTDistanceField()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGCTDistanceField(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GCTDistanceField()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Voxels per axis.
     */
    N() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Origin of voxel (0, 0, 0) in the variant frame, metres (3 values).
     */
    ORIGIN_M(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    originMLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    originMArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Extent of the cube along each axis, metres (3 values).
     */
    SPAN_M(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    spanMLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    spanMArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Metres per byte step of DATA.
     */
    DISTANCE_SCALE_M() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat32(this.bb_pos + offset) : 0.0;
    }
    /**
     * Density threshold, in AMPLITUDE_UNITS, that defines the surface.
     */
    DENSITY_THRESHOLD() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat32(this.bb_pos + offset) : 0.0;
    }
    /**
     * Mean amplitude over the occupied volume, AMPLITUDE_UNITS.
     */
    MEAN_AMPLITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat32(this.bb_pos + offset) : 0.0;
    }
    /**
     * N^3 signed bytes, x fastest, then y, then z. Absent when the field
     * travels as the chunk named by CHUNK_CID.
     */
    DATA(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    dataArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Int8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    CHUNK_CID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startGCTDistanceField(builder) {
        builder.startObject(8);
    }
    static addN(builder, N) {
        builder.addFieldInt16(0, N, 0);
    }
    static addOriginM(builder, ORIGIN_MOffset) {
        builder.addFieldOffset(1, ORIGIN_MOffset, 0);
    }
    static createOriginMVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startOriginMVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSpanM(builder, SPAN_MOffset) {
        builder.addFieldOffset(2, SPAN_MOffset, 0);
    }
    static createSpanMVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startSpanMVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addDistanceScaleM(builder, DISTANCE_SCALE_M) {
        builder.addFieldFloat32(3, DISTANCE_SCALE_M, 0.0);
    }
    static addDensityThreshold(builder, DENSITY_THRESHOLD) {
        builder.addFieldFloat32(4, DENSITY_THRESHOLD, 0.0);
    }
    static addMeanAmplitude(builder, MEAN_AMPLITUDE) {
        builder.addFieldFloat32(5, MEAN_AMPLITUDE, 0.0);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(6, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addChunkCid(builder, CHUNK_CIDOffset) {
        builder.addFieldOffset(7, CHUNK_CIDOffset, 0);
    }
    static endGCTDistanceField(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGCTDistanceField(builder, N, ORIGIN_MOffset, SPAN_MOffset, DISTANCE_SCALE_M, DENSITY_THRESHOLD, MEAN_AMPLITUDE, DATAOffset, CHUNK_CIDOffset) {
        GCTDistanceField.startGCTDistanceField(builder);
        GCTDistanceField.addN(builder, N);
        GCTDistanceField.addOriginM(builder, ORIGIN_MOffset);
        GCTDistanceField.addSpanM(builder, SPAN_MOffset);
        GCTDistanceField.addDistanceScaleM(builder, DISTANCE_SCALE_M);
        GCTDistanceField.addDensityThreshold(builder, DENSITY_THRESHOLD);
        GCTDistanceField.addMeanAmplitude(builder, MEAN_AMPLITUDE);
        GCTDistanceField.addData(builder, DATAOffset);
        GCTDistanceField.addChunkCid(builder, CHUNK_CIDOffset);
        return GCTDistanceField.endGCTDistanceField(builder);
    }
    unpack() {
        return new GCTDistanceFieldT(this.N(), this.bb.createScalarList(this.ORIGIN_M.bind(this), this.originMLength()), this.bb.createScalarList(this.SPAN_M.bind(this), this.spanMLength()), this.DISTANCE_SCALE_M(), this.DENSITY_THRESHOLD(), this.MEAN_AMPLITUDE(), this.bb.createScalarList(this.DATA.bind(this), this.dataLength()), this.CHUNK_CID());
    }
    unpackTo(_o) {
        _o.N = this.N();
        _o.ORIGIN_M = this.bb.createScalarList(this.ORIGIN_M.bind(this), this.originMLength());
        _o.SPAN_M = this.bb.createScalarList(this.SPAN_M.bind(this), this.spanMLength());
        _o.DISTANCE_SCALE_M = this.DISTANCE_SCALE_M();
        _o.DENSITY_THRESHOLD = this.DENSITY_THRESHOLD();
        _o.MEAN_AMPLITUDE = this.MEAN_AMPLITUDE();
        _o.DATA = this.bb.createScalarList(this.DATA.bind(this), this.dataLength());
        _o.CHUNK_CID = this.CHUNK_CID();
    }
}
export class GCTDistanceFieldT {
    constructor(N = 0, ORIGIN_M = [], SPAN_M = [], DISTANCE_SCALE_M = 0.0, DENSITY_THRESHOLD = 0.0, MEAN_AMPLITUDE = 0.0, DATA = [], CHUNK_CID = null) {
        this.N = N;
        this.ORIGIN_M = ORIGIN_M;
        this.SPAN_M = SPAN_M;
        this.DISTANCE_SCALE_M = DISTANCE_SCALE_M;
        this.DENSITY_THRESHOLD = DENSITY_THRESHOLD;
        this.MEAN_AMPLITUDE = MEAN_AMPLITUDE;
        this.DATA = DATA;
        this.CHUNK_CID = CHUNK_CID;
    }
    pack(builder) {
        const ORIGIN_M = GCTDistanceField.createOriginMVector(builder, this.ORIGIN_M);
        const SPAN_M = GCTDistanceField.createSpanMVector(builder, this.SPAN_M);
        const DATA = GCTDistanceField.createDataVector(builder, this.DATA);
        const CHUNK_CID = (this.CHUNK_CID !== null ? builder.createString(this.CHUNK_CID) : 0);
        return GCTDistanceField.createGCTDistanceField(builder, this.N, ORIGIN_M, SPAN_M, this.DISTANCE_SCALE_M, this.DENSITY_THRESHOLD, this.MEAN_AMPLITUDE, DATA, CHUNK_CID);
    }
}
