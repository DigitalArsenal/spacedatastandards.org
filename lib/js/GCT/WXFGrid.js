// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { wxfGridKind } from './wxfGridKind.js';
/**
 * Regular grid the samples are laid out on. Samples are row-major with
 * latitude as the outer index: cell (i, j) for 0 <= i < NLAT, 0 <= j < NLON
 * sits at VALUES[i * NLON + j] and is centred at
 * (LAT0 + i * DLAT, LON0 + j * DLON). DLAT is negative when rows run north
 * to south. Longitudes are in [-180, 180) or [0, 360) exactly as the
 * producer publishes them; a consumer normalises.
 */
export class WXFGrid {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsWXFGrid(bb, obj) {
        return (obj || new WXFGrid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsWXFGrid(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new WXFGrid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Grid geometry.
     */
    KIND() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : wxfGridKind.RegularLatLon;
    }
    /**
     * Latitude of the first row's cell centre, degrees north.
     */
    LAT0() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Longitude of the first column's cell centre, degrees east.
     */
    LON0() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Latitude step between rows, degrees (signed).
     */
    DLAT() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Longitude step between columns, degrees (signed).
     */
    DLON() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Number of rows (latitudes).
     */
    NLAT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Number of columns (longitudes).
     */
    NLON() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Whether the last column wraps to the first (global grid).
     */
    PERIODIC_LON() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startWXFGrid(builder) {
        builder.startObject(8);
    }
    static addKind(builder, KIND) {
        builder.addFieldInt8(0, KIND, wxfGridKind.RegularLatLon);
    }
    static addLat0(builder, LAT0) {
        builder.addFieldFloat64(1, LAT0, 0.0);
    }
    static addLon0(builder, LON0) {
        builder.addFieldFloat64(2, LON0, 0.0);
    }
    static addDlat(builder, DLAT) {
        builder.addFieldFloat64(3, DLAT, 0.0);
    }
    static addDlon(builder, DLON) {
        builder.addFieldFloat64(4, DLON, 0.0);
    }
    static addNlat(builder, NLAT) {
        builder.addFieldInt32(5, NLAT, 0);
    }
    static addNlon(builder, NLON) {
        builder.addFieldInt32(6, NLON, 0);
    }
    static addPeriodicLon(builder, PERIODIC_LON) {
        builder.addFieldInt8(7, +PERIODIC_LON, +false);
    }
    static endWXFGrid(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createWXFGrid(builder, KIND, LAT0, LON0, DLAT, DLON, NLAT, NLON, PERIODIC_LON) {
        WXFGrid.startWXFGrid(builder);
        WXFGrid.addKind(builder, KIND);
        WXFGrid.addLat0(builder, LAT0);
        WXFGrid.addLon0(builder, LON0);
        WXFGrid.addDlat(builder, DLAT);
        WXFGrid.addDlon(builder, DLON);
        WXFGrid.addNlat(builder, NLAT);
        WXFGrid.addNlon(builder, NLON);
        WXFGrid.addPeriodicLon(builder, PERIODIC_LON);
        return WXFGrid.endWXFGrid(builder);
    }
    unpack() {
        return new WXFGridT(this.KIND(), this.LAT0(), this.LON0(), this.DLAT(), this.DLON(), this.NLAT(), this.NLON(), this.PERIODIC_LON());
    }
    unpackTo(_o) {
        _o.KIND = this.KIND();
        _o.LAT0 = this.LAT0();
        _o.LON0 = this.LON0();
        _o.DLAT = this.DLAT();
        _o.DLON = this.DLON();
        _o.NLAT = this.NLAT();
        _o.NLON = this.NLON();
        _o.PERIODIC_LON = this.PERIODIC_LON();
    }
}
export class WXFGridT {
    constructor(KIND = wxfGridKind.RegularLatLon, LAT0 = 0.0, LON0 = 0.0, DLAT = 0.0, DLON = 0.0, NLAT = 0, NLON = 0, PERIODIC_LON = false) {
        this.KIND = KIND;
        this.LAT0 = LAT0;
        this.LON0 = LON0;
        this.DLAT = DLAT;
        this.DLON = DLON;
        this.NLAT = NLAT;
        this.NLON = NLON;
        this.PERIODIC_LON = PERIODIC_LON;
    }
    pack(builder) {
        return WXFGrid.createWXFGrid(builder, this.KIND, this.LAT0, this.LON0, this.DLAT, this.DLON, this.NLAT, this.NLON, this.PERIODIC_LON);
    }
}
