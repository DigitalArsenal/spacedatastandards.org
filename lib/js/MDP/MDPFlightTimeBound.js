// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Time-of-flight bound between two encounters. Adjacent stages give per-leg
 * bounds; non-adjacent stages give cumulative bounds such as
 * "Earth to Mars to Earth within 3 years".
 */
export class MDPFlightTimeBound {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsMDPFlightTimeBound(bb, obj) {
        return (obj || new MDPFlightTimeBound()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsMDPFlightTimeBound(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new MDPFlightTimeBound()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Stage indices the bound spans. TO_STAGE_INDEX > FROM_STAGE_INDEX.
     */
    FROM_STAGE_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TO_STAGE_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Inclusive bounds on elapsed time between those two encounters, days.
     */
    MIN_DAYS() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_DAYS() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startMDPFlightTimeBound(builder) {
        builder.startObject(4);
    }
    static addFromStageIndex(builder, FROM_STAGE_INDEX) {
        builder.addFieldInt32(0, FROM_STAGE_INDEX, 0);
    }
    static addToStageIndex(builder, TO_STAGE_INDEX) {
        builder.addFieldInt32(1, TO_STAGE_INDEX, 0);
    }
    static addMinDays(builder, MIN_DAYS) {
        builder.addFieldFloat64(2, MIN_DAYS, 0.0);
    }
    static addMaxDays(builder, MAX_DAYS) {
        builder.addFieldFloat64(3, MAX_DAYS, 0.0);
    }
    static endMDPFlightTimeBound(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createMDPFlightTimeBound(builder, FROM_STAGE_INDEX, TO_STAGE_INDEX, MIN_DAYS, MAX_DAYS) {
        MDPFlightTimeBound.startMDPFlightTimeBound(builder);
        MDPFlightTimeBound.addFromStageIndex(builder, FROM_STAGE_INDEX);
        MDPFlightTimeBound.addToStageIndex(builder, TO_STAGE_INDEX);
        MDPFlightTimeBound.addMinDays(builder, MIN_DAYS);
        MDPFlightTimeBound.addMaxDays(builder, MAX_DAYS);
        return MDPFlightTimeBound.endMDPFlightTimeBound(builder);
    }
    unpack() {
        return new MDPFlightTimeBoundT(this.FROM_STAGE_INDEX(), this.TO_STAGE_INDEX(), this.MIN_DAYS(), this.MAX_DAYS());
    }
    unpackTo(_o) {
        _o.FROM_STAGE_INDEX = this.FROM_STAGE_INDEX();
        _o.TO_STAGE_INDEX = this.TO_STAGE_INDEX();
        _o.MIN_DAYS = this.MIN_DAYS();
        _o.MAX_DAYS = this.MAX_DAYS();
    }
}
export class MDPFlightTimeBoundT {
    constructor(FROM_STAGE_INDEX = 0, TO_STAGE_INDEX = 0, MIN_DAYS = 0.0, MAX_DAYS = 0.0) {
        this.FROM_STAGE_INDEX = FROM_STAGE_INDEX;
        this.TO_STAGE_INDEX = TO_STAGE_INDEX;
        this.MIN_DAYS = MIN_DAYS;
        this.MAX_DAYS = MAX_DAYS;
    }
    pack(builder) {
        return MDPFlightTimeBound.createMDPFlightTimeBound(builder, this.FROM_STAGE_INDEX, this.TO_STAGE_INDEX, this.MIN_DAYS, this.MAX_DAYS);
    }
}
