// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One encounter stage of the body sequence: the set of bodies admissible at
 * this position in the sequence, and the epoch grid searched for it.
 *
 * Bodies are named by NAIF integer ID, which is normative. SDS carries no
 * body enum and $GRV.CentralBody is gravity-model-scoped and incomplete, so it
 * must not be repurposed here. Note that a barycenter and a body center are
 * different objects (4 is the Mars barycenter, 499 is Mars) and the
 * distinction is operationally load-bearing.
 */
export class MDPEncounterStage {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsMDPEncounterStage(bb, obj) {
        return (obj || new MDPEncounterStage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsMDPEncounterStage(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new MDPEncounterStage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Zero-based position in the ordered sequence.
     */
    STAGE_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * NAIF integer IDs admissible at this stage. A single entry pins the body.
     */
    ALLOWED_BODY_NAIF_IDS(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    allowedBodyNaifIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    allowedBodyNaifIdsArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Int32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    ALLOWED_BODY_NAMES(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    allowedBodyNamesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    EPOCH_WINDOW_START(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EPOCH_WINDOW_END(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Epoch grid step searched within the window, days.
     */
    EPOCH_GRID_STEP_DAYS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Bounds on hyperbolic excess speed at this encounter, km/s.
     */
    V_INFINITY_MIN_KM_S() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    V_INFINITY_MAX_KM_S() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Minimum admissible flyby altitude above the body's reference surface, km.
     */
    MIN_FLYBY_ALTITUDE_KM() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Cap on powered-flyby delta-V applied at this encounter, km/s.
     */
    MAX_POWERED_FLYBY_DELTA_V_KM_S() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startMDPEncounterStage(builder) {
        builder.startObject(10);
    }
    static addStageIndex(builder, STAGE_INDEX) {
        builder.addFieldInt32(0, STAGE_INDEX, 0);
    }
    static addAllowedBodyNaifIds(builder, ALLOWED_BODY_NAIF_IDSOffset) {
        builder.addFieldOffset(1, ALLOWED_BODY_NAIF_IDSOffset, 0);
    }
    static createAllowedBodyNaifIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startAllowedBodyNaifIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAllowedBodyNames(builder, ALLOWED_BODY_NAMESOffset) {
        builder.addFieldOffset(2, ALLOWED_BODY_NAMESOffset, 0);
    }
    static createAllowedBodyNamesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAllowedBodyNamesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addEpochWindowStart(builder, EPOCH_WINDOW_STARTOffset) {
        builder.addFieldOffset(3, EPOCH_WINDOW_STARTOffset, 0);
    }
    static addEpochWindowEnd(builder, EPOCH_WINDOW_ENDOffset) {
        builder.addFieldOffset(4, EPOCH_WINDOW_ENDOffset, 0);
    }
    static addEpochGridStepDays(builder, EPOCH_GRID_STEP_DAYS) {
        builder.addFieldFloat64(5, EPOCH_GRID_STEP_DAYS, 0.0);
    }
    static addVInfinityMinKmS(builder, V_INFINITY_MIN_KM_S) {
        builder.addFieldFloat64(6, V_INFINITY_MIN_KM_S, 0.0);
    }
    static addVInfinityMaxKmS(builder, V_INFINITY_MAX_KM_S) {
        builder.addFieldFloat64(7, V_INFINITY_MAX_KM_S, 0.0);
    }
    static addMinFlybyAltitudeKm(builder, MIN_FLYBY_ALTITUDE_KM) {
        builder.addFieldFloat64(8, MIN_FLYBY_ALTITUDE_KM, 0.0);
    }
    static addMaxPoweredFlybyDeltaVKmS(builder, MAX_POWERED_FLYBY_DELTA_V_KM_S) {
        builder.addFieldFloat64(9, MAX_POWERED_FLYBY_DELTA_V_KM_S, 0.0);
    }
    static endMDPEncounterStage(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createMDPEncounterStage(builder, STAGE_INDEX, ALLOWED_BODY_NAIF_IDSOffset, ALLOWED_BODY_NAMESOffset, EPOCH_WINDOW_STARTOffset, EPOCH_WINDOW_ENDOffset, EPOCH_GRID_STEP_DAYS, V_INFINITY_MIN_KM_S, V_INFINITY_MAX_KM_S, MIN_FLYBY_ALTITUDE_KM, MAX_POWERED_FLYBY_DELTA_V_KM_S) {
        MDPEncounterStage.startMDPEncounterStage(builder);
        MDPEncounterStage.addStageIndex(builder, STAGE_INDEX);
        MDPEncounterStage.addAllowedBodyNaifIds(builder, ALLOWED_BODY_NAIF_IDSOffset);
        MDPEncounterStage.addAllowedBodyNames(builder, ALLOWED_BODY_NAMESOffset);
        MDPEncounterStage.addEpochWindowStart(builder, EPOCH_WINDOW_STARTOffset);
        MDPEncounterStage.addEpochWindowEnd(builder, EPOCH_WINDOW_ENDOffset);
        MDPEncounterStage.addEpochGridStepDays(builder, EPOCH_GRID_STEP_DAYS);
        MDPEncounterStage.addVInfinityMinKmS(builder, V_INFINITY_MIN_KM_S);
        MDPEncounterStage.addVInfinityMaxKmS(builder, V_INFINITY_MAX_KM_S);
        MDPEncounterStage.addMinFlybyAltitudeKm(builder, MIN_FLYBY_ALTITUDE_KM);
        MDPEncounterStage.addMaxPoweredFlybyDeltaVKmS(builder, MAX_POWERED_FLYBY_DELTA_V_KM_S);
        return MDPEncounterStage.endMDPEncounterStage(builder);
    }
    unpack() {
        return new MDPEncounterStageT(this.STAGE_INDEX(), this.bb.createScalarList(this.ALLOWED_BODY_NAIF_IDS.bind(this), this.allowedBodyNaifIdsLength()), this.bb.createScalarList(this.ALLOWED_BODY_NAMES.bind(this), this.allowedBodyNamesLength()), this.EPOCH_WINDOW_START(), this.EPOCH_WINDOW_END(), this.EPOCH_GRID_STEP_DAYS(), this.V_INFINITY_MIN_KM_S(), this.V_INFINITY_MAX_KM_S(), this.MIN_FLYBY_ALTITUDE_KM(), this.MAX_POWERED_FLYBY_DELTA_V_KM_S());
    }
    unpackTo(_o) {
        _o.STAGE_INDEX = this.STAGE_INDEX();
        _o.ALLOWED_BODY_NAIF_IDS = this.bb.createScalarList(this.ALLOWED_BODY_NAIF_IDS.bind(this), this.allowedBodyNaifIdsLength());
        _o.ALLOWED_BODY_NAMES = this.bb.createScalarList(this.ALLOWED_BODY_NAMES.bind(this), this.allowedBodyNamesLength());
        _o.EPOCH_WINDOW_START = this.EPOCH_WINDOW_START();
        _o.EPOCH_WINDOW_END = this.EPOCH_WINDOW_END();
        _o.EPOCH_GRID_STEP_DAYS = this.EPOCH_GRID_STEP_DAYS();
        _o.V_INFINITY_MIN_KM_S = this.V_INFINITY_MIN_KM_S();
        _o.V_INFINITY_MAX_KM_S = this.V_INFINITY_MAX_KM_S();
        _o.MIN_FLYBY_ALTITUDE_KM = this.MIN_FLYBY_ALTITUDE_KM();
        _o.MAX_POWERED_FLYBY_DELTA_V_KM_S = this.MAX_POWERED_FLYBY_DELTA_V_KM_S();
    }
}
export class MDPEncounterStageT {
    constructor(STAGE_INDEX = 0, ALLOWED_BODY_NAIF_IDS = [], ALLOWED_BODY_NAMES = [], EPOCH_WINDOW_START = null, EPOCH_WINDOW_END = null, EPOCH_GRID_STEP_DAYS = 0.0, V_INFINITY_MIN_KM_S = 0.0, V_INFINITY_MAX_KM_S = 0.0, MIN_FLYBY_ALTITUDE_KM = 0.0, MAX_POWERED_FLYBY_DELTA_V_KM_S = 0.0) {
        this.STAGE_INDEX = STAGE_INDEX;
        this.ALLOWED_BODY_NAIF_IDS = ALLOWED_BODY_NAIF_IDS;
        this.ALLOWED_BODY_NAMES = ALLOWED_BODY_NAMES;
        this.EPOCH_WINDOW_START = EPOCH_WINDOW_START;
        this.EPOCH_WINDOW_END = EPOCH_WINDOW_END;
        this.EPOCH_GRID_STEP_DAYS = EPOCH_GRID_STEP_DAYS;
        this.V_INFINITY_MIN_KM_S = V_INFINITY_MIN_KM_S;
        this.V_INFINITY_MAX_KM_S = V_INFINITY_MAX_KM_S;
        this.MIN_FLYBY_ALTITUDE_KM = MIN_FLYBY_ALTITUDE_KM;
        this.MAX_POWERED_FLYBY_DELTA_V_KM_S = MAX_POWERED_FLYBY_DELTA_V_KM_S;
    }
    pack(builder) {
        const ALLOWED_BODY_NAIF_IDS = MDPEncounterStage.createAllowedBodyNaifIdsVector(builder, this.ALLOWED_BODY_NAIF_IDS);
        const ALLOWED_BODY_NAMES = MDPEncounterStage.createAllowedBodyNamesVector(builder, builder.createObjectOffsetList(this.ALLOWED_BODY_NAMES));
        const EPOCH_WINDOW_START = (this.EPOCH_WINDOW_START !== null ? builder.createString(this.EPOCH_WINDOW_START) : 0);
        const EPOCH_WINDOW_END = (this.EPOCH_WINDOW_END !== null ? builder.createString(this.EPOCH_WINDOW_END) : 0);
        return MDPEncounterStage.createMDPEncounterStage(builder, this.STAGE_INDEX, ALLOWED_BODY_NAIF_IDS, ALLOWED_BODY_NAMES, EPOCH_WINDOW_START, EPOCH_WINDOW_END, this.EPOCH_GRID_STEP_DAYS, this.V_INFINITY_MIN_KM_S, this.V_INFINITY_MAX_KM_S, this.MIN_FLYBY_ALTITUDE_KM, this.MAX_POWERED_FLYBY_DELTA_V_KM_S);
    }
}
