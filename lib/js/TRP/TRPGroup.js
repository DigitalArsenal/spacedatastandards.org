// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { TRPPredicate } from './TRPPredicate.js';
import { trpCombinator } from './trpCombinator.js';
/**
 * Recursive boolean group for a compound trust rule set.
 */
export class TRPGroup {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRPGroup(bb, obj) {
        return (obj || new TRPGroup()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRPGroup(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRPGroup()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    GROUP_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Boolean operation applied across direct predicates and child groups.
     */
    COMBINATOR() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : trpCombinator.All;
    }
    /**
     * Predicates evaluated directly within this group.
     */
    PREDICATES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new TRPPredicate()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    predicatesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Nested groups used to express compound rule sets.
     */
    GROUPS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new TRPGroup()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    groupsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startTRPGroup(builder) {
        builder.startObject(4);
    }
    static addGroupId(builder, GROUP_IDOffset) {
        builder.addFieldOffset(0, GROUP_IDOffset, 0);
    }
    static addCombinator(builder, COMBINATOR) {
        builder.addFieldInt8(1, COMBINATOR, trpCombinator.All);
    }
    static addPredicates(builder, PREDICATESOffset) {
        builder.addFieldOffset(2, PREDICATESOffset, 0);
    }
    static createPredicatesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPredicatesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addGroups(builder, GROUPSOffset) {
        builder.addFieldOffset(3, GROUPSOffset, 0);
    }
    static createGroupsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startGroupsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endTRPGroup(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createTRPGroup(builder, GROUP_IDOffset, COMBINATOR, PREDICATESOffset, GROUPSOffset) {
        TRPGroup.startTRPGroup(builder);
        TRPGroup.addGroupId(builder, GROUP_IDOffset);
        TRPGroup.addCombinator(builder, COMBINATOR);
        TRPGroup.addPredicates(builder, PREDICATESOffset);
        TRPGroup.addGroups(builder, GROUPSOffset);
        return TRPGroup.endTRPGroup(builder);
    }
    unpack() {
        return new TRPGroupT(this.GROUP_ID(), this.COMBINATOR(), this.bb.createObjList(this.PREDICATES.bind(this), this.predicatesLength()), this.bb.createObjList(this.GROUPS.bind(this), this.groupsLength()));
    }
    unpackTo(_o) {
        _o.GROUP_ID = this.GROUP_ID();
        _o.COMBINATOR = this.COMBINATOR();
        _o.PREDICATES = this.bb.createObjList(this.PREDICATES.bind(this), this.predicatesLength());
        _o.GROUPS = this.bb.createObjList(this.GROUPS.bind(this), this.groupsLength());
    }
}
export class TRPGroupT {
    constructor(GROUP_ID = null, COMBINATOR = trpCombinator.All, PREDICATES = [], GROUPS = []) {
        this.GROUP_ID = GROUP_ID;
        this.COMBINATOR = COMBINATOR;
        this.PREDICATES = PREDICATES;
        this.GROUPS = GROUPS;
    }
    pack(builder) {
        const GROUP_ID = (this.GROUP_ID !== null ? builder.createString(this.GROUP_ID) : 0);
        const PREDICATES = TRPGroup.createPredicatesVector(builder, builder.createObjectOffsetList(this.PREDICATES));
        const GROUPS = TRPGroup.createGroupsVector(builder, builder.createObjectOffsetList(this.GROUPS));
        return TRPGroup.createTRPGroup(builder, GROUP_ID, this.COMBINATOR, PREDICATES, GROUPS);
    }
}
