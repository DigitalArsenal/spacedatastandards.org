// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { VAMVector3 } from './VAMVector3.js';
/**
 * Geometry, material, texture, and bounds measurements.
 */
export class VAMMetrics {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsVAMMetrics(bb, obj) {
        return (obj || new VAMMetrics()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsVAMMetrics(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new VAMMetrics()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    TRIANGLE_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    VERTEX_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    PRIMITIVE_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    DRAW_CALL_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    MATERIAL_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TEXTURE_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TEXTURE_BYTES() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    ANIMATION_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    BOUNDING_BOX_MIN(obj) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? (obj || new VAMVector3()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    BOUNDING_BOX_MAX(obj) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? (obj || new VAMVector3()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    DIMENSIONS_METERS(obj) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? (obj || new VAMVector3()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startVAMMetrics(builder) {
        builder.startObject(11);
    }
    static addTriangleCount(builder, TRIANGLE_COUNT) {
        builder.addFieldInt64(0, TRIANGLE_COUNT, BigInt('0'));
    }
    static addVertexCount(builder, VERTEX_COUNT) {
        builder.addFieldInt64(1, VERTEX_COUNT, BigInt('0'));
    }
    static addPrimitiveCount(builder, PRIMITIVE_COUNT) {
        builder.addFieldInt32(2, PRIMITIVE_COUNT, 0);
    }
    static addDrawCallCount(builder, DRAW_CALL_COUNT) {
        builder.addFieldInt32(3, DRAW_CALL_COUNT, 0);
    }
    static addMaterialCount(builder, MATERIAL_COUNT) {
        builder.addFieldInt32(4, MATERIAL_COUNT, 0);
    }
    static addTextureCount(builder, TEXTURE_COUNT) {
        builder.addFieldInt32(5, TEXTURE_COUNT, 0);
    }
    static addTextureBytes(builder, TEXTURE_BYTES) {
        builder.addFieldInt64(6, TEXTURE_BYTES, BigInt('0'));
    }
    static addAnimationCount(builder, ANIMATION_COUNT) {
        builder.addFieldInt32(7, ANIMATION_COUNT, 0);
    }
    static addBoundingBoxMin(builder, BOUNDING_BOX_MINOffset) {
        builder.addFieldOffset(8, BOUNDING_BOX_MINOffset, 0);
    }
    static addBoundingBoxMax(builder, BOUNDING_BOX_MAXOffset) {
        builder.addFieldOffset(9, BOUNDING_BOX_MAXOffset, 0);
    }
    static addDimensionsMeters(builder, DIMENSIONS_METERSOffset) {
        builder.addFieldOffset(10, DIMENSIONS_METERSOffset, 0);
    }
    static endVAMMetrics(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new VAMMetricsT(this.TRIANGLE_COUNT(), this.VERTEX_COUNT(), this.PRIMITIVE_COUNT(), this.DRAW_CALL_COUNT(), this.MATERIAL_COUNT(), this.TEXTURE_COUNT(), this.TEXTURE_BYTES(), this.ANIMATION_COUNT(), (this.BOUNDING_BOX_MIN() !== null ? this.BOUNDING_BOX_MIN().unpack() : null), (this.BOUNDING_BOX_MAX() !== null ? this.BOUNDING_BOX_MAX().unpack() : null), (this.DIMENSIONS_METERS() !== null ? this.DIMENSIONS_METERS().unpack() : null));
    }
    unpackTo(_o) {
        _o.TRIANGLE_COUNT = this.TRIANGLE_COUNT();
        _o.VERTEX_COUNT = this.VERTEX_COUNT();
        _o.PRIMITIVE_COUNT = this.PRIMITIVE_COUNT();
        _o.DRAW_CALL_COUNT = this.DRAW_CALL_COUNT();
        _o.MATERIAL_COUNT = this.MATERIAL_COUNT();
        _o.TEXTURE_COUNT = this.TEXTURE_COUNT();
        _o.TEXTURE_BYTES = this.TEXTURE_BYTES();
        _o.ANIMATION_COUNT = this.ANIMATION_COUNT();
        _o.BOUNDING_BOX_MIN = (this.BOUNDING_BOX_MIN() !== null ? this.BOUNDING_BOX_MIN().unpack() : null);
        _o.BOUNDING_BOX_MAX = (this.BOUNDING_BOX_MAX() !== null ? this.BOUNDING_BOX_MAX().unpack() : null);
        _o.DIMENSIONS_METERS = (this.DIMENSIONS_METERS() !== null ? this.DIMENSIONS_METERS().unpack() : null);
    }
}
export class VAMMetricsT {
    constructor(TRIANGLE_COUNT = BigInt('0'), VERTEX_COUNT = BigInt('0'), PRIMITIVE_COUNT = 0, DRAW_CALL_COUNT = 0, MATERIAL_COUNT = 0, TEXTURE_COUNT = 0, TEXTURE_BYTES = BigInt('0'), ANIMATION_COUNT = 0, BOUNDING_BOX_MIN = null, BOUNDING_BOX_MAX = null, DIMENSIONS_METERS = null) {
        this.TRIANGLE_COUNT = TRIANGLE_COUNT;
        this.VERTEX_COUNT = VERTEX_COUNT;
        this.PRIMITIVE_COUNT = PRIMITIVE_COUNT;
        this.DRAW_CALL_COUNT = DRAW_CALL_COUNT;
        this.MATERIAL_COUNT = MATERIAL_COUNT;
        this.TEXTURE_COUNT = TEXTURE_COUNT;
        this.TEXTURE_BYTES = TEXTURE_BYTES;
        this.ANIMATION_COUNT = ANIMATION_COUNT;
        this.BOUNDING_BOX_MIN = BOUNDING_BOX_MIN;
        this.BOUNDING_BOX_MAX = BOUNDING_BOX_MAX;
        this.DIMENSIONS_METERS = DIMENSIONS_METERS;
    }
    pack(builder) {
        const BOUNDING_BOX_MIN = (this.BOUNDING_BOX_MIN !== null ? this.BOUNDING_BOX_MIN.pack(builder) : 0);
        const BOUNDING_BOX_MAX = (this.BOUNDING_BOX_MAX !== null ? this.BOUNDING_BOX_MAX.pack(builder) : 0);
        const DIMENSIONS_METERS = (this.DIMENSIONS_METERS !== null ? this.DIMENSIONS_METERS.pack(builder) : 0);
        VAMMetrics.startVAMMetrics(builder);
        VAMMetrics.addTriangleCount(builder, this.TRIANGLE_COUNT);
        VAMMetrics.addVertexCount(builder, this.VERTEX_COUNT);
        VAMMetrics.addPrimitiveCount(builder, this.PRIMITIVE_COUNT);
        VAMMetrics.addDrawCallCount(builder, this.DRAW_CALL_COUNT);
        VAMMetrics.addMaterialCount(builder, this.MATERIAL_COUNT);
        VAMMetrics.addTextureCount(builder, this.TEXTURE_COUNT);
        VAMMetrics.addTextureBytes(builder, this.TEXTURE_BYTES);
        VAMMetrics.addAnimationCount(builder, this.ANIMATION_COUNT);
        VAMMetrics.addBoundingBoxMin(builder, BOUNDING_BOX_MIN);
        VAMMetrics.addBoundingBoxMax(builder, BOUNDING_BOX_MAX);
        VAMMetrics.addDimensionsMeters(builder, DIMENSIONS_METERS);
        return VAMMetrics.endVAMMetrics(builder);
    }
}
