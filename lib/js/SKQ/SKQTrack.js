// automatically generated by the FlatBuffers compiler, do not modify
/**
 * A body's apparent path across the sky over an interval, and its disc.
 */
export class SKQTrack {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    /**
     * Direction of the body's centre at the start of the interval.
     */
    START_RA_DEG() {
        return this.bb.readFloat64(this.bb_pos);
    }
    START_DEC_DEG() {
        return this.bb.readFloat64(this.bb_pos + 8);
    }
    /**
     * Direction at the end; equal to the start for an instant.
     */
    END_RA_DEG() {
        return this.bb.readFloat64(this.bb_pos + 16);
    }
    END_DEC_DEG() {
        return this.bb.readFloat64(this.bb_pos + 24);
    }
    /**
     * Angular radius of the body's disc.
     */
    RADIUS_DEG() {
        return this.bb.readFloat64(this.bb_pos + 32);
    }
    /**
     * Caller's identifier for the body, echoed in the result.
     */
    BODY_ID() {
        return this.bb.readUint32(this.bb_pos + 40);
    }
    RESERVED() {
        return this.bb.readUint32(this.bb_pos + 44);
    }
    static sizeOf() {
        return 48;
    }
    static createSKQTrack(builder, START_RA_DEG, START_DEC_DEG, END_RA_DEG, END_DEC_DEG, RADIUS_DEG, BODY_ID, RESERVED) {
        builder.prep(8, 48);
        builder.writeInt32(RESERVED);
        builder.writeInt32(BODY_ID);
        builder.writeFloat64(RADIUS_DEG);
        builder.writeFloat64(END_DEC_DEG);
        builder.writeFloat64(END_RA_DEG);
        builder.writeFloat64(START_DEC_DEG);
        builder.writeFloat64(START_RA_DEG);
        return builder.offset();
    }
    unpack() {
        return new SKQTrackT(this.START_RA_DEG(), this.START_DEC_DEG(), this.END_RA_DEG(), this.END_DEC_DEG(), this.RADIUS_DEG(), this.BODY_ID(), this.RESERVED());
    }
    unpackTo(_o) {
        _o.START_RA_DEG = this.START_RA_DEG();
        _o.START_DEC_DEG = this.START_DEC_DEG();
        _o.END_RA_DEG = this.END_RA_DEG();
        _o.END_DEC_DEG = this.END_DEC_DEG();
        _o.RADIUS_DEG = this.RADIUS_DEG();
        _o.BODY_ID = this.BODY_ID();
        _o.RESERVED = this.RESERVED();
    }
}
export class SKQTrackT {
    constructor(START_RA_DEG = 0.0, START_DEC_DEG = 0.0, END_RA_DEG = 0.0, END_DEC_DEG = 0.0, RADIUS_DEG = 0.0, BODY_ID = 0, RESERVED = 0) {
        this.START_RA_DEG = START_RA_DEG;
        this.START_DEC_DEG = START_DEC_DEG;
        this.END_RA_DEG = END_RA_DEG;
        this.END_DEC_DEG = END_DEC_DEG;
        this.RADIUS_DEG = RADIUS_DEG;
        this.BODY_ID = BODY_ID;
        this.RESERVED = RESERVED;
    }
    pack(builder) {
        return SKQTrack.createSKQTrack(builder, this.START_RA_DEG, this.START_DEC_DEG, this.END_RA_DEG, this.END_DEC_DEG, this.RADIUS_DEG, this.BODY_ID, this.RESERVED);
    }
}
