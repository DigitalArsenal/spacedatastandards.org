// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PCEParameterCondition } from './PCEParameterCondition.js';
/**
 * Scheduled time gates an optional condition. At least one must be present.
 */
export class PRWBurnBoundary {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWBurnBoundary(bb, obj) {
        return (obj || new PRWBurnBoundary()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWBurnBoundary(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWBurnBoundary()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    ELAPSED_SECONDS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * True when ELAPSED_SECONDS carries a value; false means absent.
     */
    HAS_ELAPSED_SECONDS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * SI PCE predicate; direction and goal tolerance explicitly populated.
     */
    CONDITION(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new PCEParameterCondition()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startPRWBurnBoundary(builder) {
        builder.startObject(3);
    }
    static addElapsedSeconds(builder, ELAPSED_SECONDS) {
        builder.addFieldFloat64(0, ELAPSED_SECONDS, 0.0);
    }
    static addHasElapsedSeconds(builder, HAS_ELAPSED_SECONDS) {
        builder.addFieldInt8(1, +HAS_ELAPSED_SECONDS, +false);
    }
    static addCondition(builder, CONDITIONOffset) {
        builder.addFieldOffset(2, CONDITIONOffset, 0);
    }
    static endPRWBurnBoundary(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new PRWBurnBoundaryT(this.ELAPSED_SECONDS(), this.HAS_ELAPSED_SECONDS(), (this.CONDITION() !== null ? this.CONDITION().unpack() : null));
    }
    unpackTo(_o) {
        _o.ELAPSED_SECONDS = this.ELAPSED_SECONDS();
        _o.HAS_ELAPSED_SECONDS = this.HAS_ELAPSED_SECONDS();
        _o.CONDITION = (this.CONDITION() !== null ? this.CONDITION().unpack() : null);
    }
}
export class PRWBurnBoundaryT {
    constructor(ELAPSED_SECONDS = 0.0, HAS_ELAPSED_SECONDS = false, CONDITION = null) {
        this.ELAPSED_SECONDS = ELAPSED_SECONDS;
        this.HAS_ELAPSED_SECONDS = HAS_ELAPSED_SECONDS;
        this.CONDITION = CONDITION;
    }
    pack(builder) {
        const CONDITION = (this.CONDITION !== null ? this.CONDITION.pack(builder) : 0);
        PRWBurnBoundary.startPRWBurnBoundary(builder);
        PRWBurnBoundary.addElapsedSeconds(builder, this.ELAPSED_SECONDS);
        PRWBurnBoundary.addHasElapsedSeconds(builder, this.HAS_ELAPSED_SECONDS);
        PRWBurnBoundary.addCondition(builder, CONDITION);
        return PRWBurnBoundary.endPRWBurnBoundary(builder);
    }
}
