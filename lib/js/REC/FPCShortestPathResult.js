// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Single-source shortest-path result.
 */
export class FPCShortestPathResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsFPCShortestPathResult(bb, obj) {
        return (obj || new FPCShortestPathResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsFPCShortestPathResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FPCShortestPathResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Source vertex index.
     */
    SOURCE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Number of vertices in the solved graph.
     */
    VERTEX_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Number of reachable vertices (finite distances).
     */
    REACHABLE_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Distance array (length == VERTEX_COUNT).
     */
    DISTANCES(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    distancesLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    distancesArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Predecessor array (length == VERTEX_COUNT; UINT32_MAX means none / source).
     */
    PREDECESSORS(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    predecessorsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    predecessorsArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? new Uint32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startFPCShortestPathResult(builder) {
        builder.startObject(5);
    }
    static addSource(builder, SOURCE) {
        builder.addFieldInt32(0, SOURCE, 0);
    }
    static addVertexCount(builder, VERTEX_COUNT) {
        builder.addFieldInt32(1, VERTEX_COUNT, 0);
    }
    static addReachableCount(builder, REACHABLE_COUNT) {
        builder.addFieldInt32(2, REACHABLE_COUNT, 0);
    }
    static addDistances(builder, DISTANCESOffset) {
        builder.addFieldOffset(3, DISTANCESOffset, 0);
    }
    static createDistancesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startDistancesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addPredecessors(builder, PREDECESSORSOffset) {
        builder.addFieldOffset(4, PREDECESSORSOffset, 0);
    }
    static createPredecessorsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startPredecessorsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endFPCShortestPathResult(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createFPCShortestPathResult(builder, SOURCE, VERTEX_COUNT, REACHABLE_COUNT, DISTANCESOffset, PREDECESSORSOffset) {
        FPCShortestPathResult.startFPCShortestPathResult(builder);
        FPCShortestPathResult.addSource(builder, SOURCE);
        FPCShortestPathResult.addVertexCount(builder, VERTEX_COUNT);
        FPCShortestPathResult.addReachableCount(builder, REACHABLE_COUNT);
        FPCShortestPathResult.addDistances(builder, DISTANCESOffset);
        FPCShortestPathResult.addPredecessors(builder, PREDECESSORSOffset);
        return FPCShortestPathResult.endFPCShortestPathResult(builder);
    }
    unpack() {
        return new FPCShortestPathResultT(this.SOURCE(), this.VERTEX_COUNT(), this.REACHABLE_COUNT(), this.bb.createScalarList(this.DISTANCES.bind(this), this.distancesLength()), this.bb.createScalarList(this.PREDECESSORS.bind(this), this.predecessorsLength()));
    }
    unpackTo(_o) {
        _o.SOURCE = this.SOURCE();
        _o.VERTEX_COUNT = this.VERTEX_COUNT();
        _o.REACHABLE_COUNT = this.REACHABLE_COUNT();
        _o.DISTANCES = this.bb.createScalarList(this.DISTANCES.bind(this), this.distancesLength());
        _o.PREDECESSORS = this.bb.createScalarList(this.PREDECESSORS.bind(this), this.predecessorsLength());
    }
}
export class FPCShortestPathResultT {
    constructor(SOURCE = 0, VERTEX_COUNT = 0, REACHABLE_COUNT = 0, DISTANCES = [], PREDECESSORS = []) {
        this.SOURCE = SOURCE;
        this.VERTEX_COUNT = VERTEX_COUNT;
        this.REACHABLE_COUNT = REACHABLE_COUNT;
        this.DISTANCES = DISTANCES;
        this.PREDECESSORS = PREDECESSORS;
    }
    pack(builder) {
        const DISTANCES = FPCShortestPathResult.createDistancesVector(builder, this.DISTANCES);
        const PREDECESSORS = FPCShortestPathResult.createPredecessorsVector(builder, this.PREDECESSORS);
        return FPCShortestPathResult.createFPCShortestPathResult(builder, this.SOURCE, this.VERTEX_COUNT, this.REACHABLE_COUNT, DISTANCES, PREDECESSORS);
    }
}
