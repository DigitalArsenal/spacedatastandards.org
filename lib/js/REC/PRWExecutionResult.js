// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PRWPropagationSample } from './PRWPropagationSample.js';
import { prwDensityTreatment } from './prwDensityTreatment.js';
import { prwDerivativeTechnique } from './prwDerivativeTechnique.js';
export class PRWExecutionResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWExecutionResult(bb, obj) {
        return (obj || new PRWExecutionResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWExecutionResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWExecutionResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    FINAL_SAMPLE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new PRWPropagationSample()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    SAMPLES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new PRWPropagationSample()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    samplesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ELAPSED_SECONDS() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    EPHEMERIS_SOURCE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    STM_TECHNIQUE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : prwDerivativeTechnique.UNSPECIFIED;
    }
    DENSITY_TREATMENT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : prwDensityTreatment.UNSPECIFIED;
    }
    static startPRWExecutionResult(builder) {
        builder.startObject(6);
    }
    static addFinalSample(builder, FINAL_SAMPLEOffset) {
        builder.addFieldOffset(0, FINAL_SAMPLEOffset, 0);
    }
    static addSamples(builder, SAMPLESOffset) {
        builder.addFieldOffset(1, SAMPLESOffset, 0);
    }
    static createSamplesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSamplesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addElapsedSeconds(builder, ELAPSED_SECONDS) {
        builder.addFieldFloat64(2, ELAPSED_SECONDS, 0.0);
    }
    static addEphemerisSource(builder, EPHEMERIS_SOURCEOffset) {
        builder.addFieldOffset(3, EPHEMERIS_SOURCEOffset, 0);
    }
    static addStmTechnique(builder, STM_TECHNIQUE) {
        builder.addFieldInt8(4, STM_TECHNIQUE, prwDerivativeTechnique.UNSPECIFIED);
    }
    static addDensityTreatment(builder, DENSITY_TREATMENT) {
        builder.addFieldInt8(5, DENSITY_TREATMENT, prwDensityTreatment.UNSPECIFIED);
    }
    static endPRWExecutionResult(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // FINAL_SAMPLE
        builder.requiredField(offset, 10); // EPHEMERIS_SOURCE
        return offset;
    }
    static createPRWExecutionResult(builder, FINAL_SAMPLEOffset, SAMPLESOffset, ELAPSED_SECONDS, EPHEMERIS_SOURCEOffset, STM_TECHNIQUE, DENSITY_TREATMENT) {
        PRWExecutionResult.startPRWExecutionResult(builder);
        PRWExecutionResult.addFinalSample(builder, FINAL_SAMPLEOffset);
        PRWExecutionResult.addSamples(builder, SAMPLESOffset);
        PRWExecutionResult.addElapsedSeconds(builder, ELAPSED_SECONDS);
        PRWExecutionResult.addEphemerisSource(builder, EPHEMERIS_SOURCEOffset);
        PRWExecutionResult.addStmTechnique(builder, STM_TECHNIQUE);
        PRWExecutionResult.addDensityTreatment(builder, DENSITY_TREATMENT);
        return PRWExecutionResult.endPRWExecutionResult(builder);
    }
    unpack() {
        return new PRWExecutionResultT((this.FINAL_SAMPLE() !== null ? this.FINAL_SAMPLE().unpack() : null), this.bb.createObjList(this.SAMPLES.bind(this), this.samplesLength()), this.ELAPSED_SECONDS(), this.EPHEMERIS_SOURCE(), this.STM_TECHNIQUE(), this.DENSITY_TREATMENT());
    }
    unpackTo(_o) {
        _o.FINAL_SAMPLE = (this.FINAL_SAMPLE() !== null ? this.FINAL_SAMPLE().unpack() : null);
        _o.SAMPLES = this.bb.createObjList(this.SAMPLES.bind(this), this.samplesLength());
        _o.ELAPSED_SECONDS = this.ELAPSED_SECONDS();
        _o.EPHEMERIS_SOURCE = this.EPHEMERIS_SOURCE();
        _o.STM_TECHNIQUE = this.STM_TECHNIQUE();
        _o.DENSITY_TREATMENT = this.DENSITY_TREATMENT();
    }
}
export class PRWExecutionResultT {
    constructor(FINAL_SAMPLE = null, SAMPLES = [], ELAPSED_SECONDS = 0.0, EPHEMERIS_SOURCE = null, STM_TECHNIQUE = prwDerivativeTechnique.UNSPECIFIED, DENSITY_TREATMENT = prwDensityTreatment.UNSPECIFIED) {
        this.FINAL_SAMPLE = FINAL_SAMPLE;
        this.SAMPLES = SAMPLES;
        this.ELAPSED_SECONDS = ELAPSED_SECONDS;
        this.EPHEMERIS_SOURCE = EPHEMERIS_SOURCE;
        this.STM_TECHNIQUE = STM_TECHNIQUE;
        this.DENSITY_TREATMENT = DENSITY_TREATMENT;
    }
    pack(builder) {
        const FINAL_SAMPLE = (this.FINAL_SAMPLE !== null ? this.FINAL_SAMPLE.pack(builder) : 0);
        const SAMPLES = PRWExecutionResult.createSamplesVector(builder, builder.createObjectOffsetList(this.SAMPLES));
        const EPHEMERIS_SOURCE = (this.EPHEMERIS_SOURCE !== null ? builder.createString(this.EPHEMERIS_SOURCE) : 0);
        return PRWExecutionResult.createPRWExecutionResult(builder, FINAL_SAMPLE, SAMPLES, this.ELAPSED_SECONDS, EPHEMERIS_SOURCE, this.STM_TECHNIQUE, this.DENSITY_TREATMENT);
    }
}
