// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SCVCoverageGrid } from './SCVCoverageGrid.js';
import { SCVMemoryRegion } from './SCVMemoryRegion.js';
import { SCVPackedRasterBand } from './SCVPackedRasterBand.js';
import { SCVTimeGrid } from './SCVTimeGrid.js';
export class SCVPackedRasterProducts {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCVPackedRasterProducts(bb, obj) {
        return (obj || new SCVPackedRasterProducts()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCVPackedRasterProducts(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCVPackedRasterProducts()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    JOB_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TRACE_ID() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    GRID(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new SCVCoverageGrid()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    TIME_GRID(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new SCVTimeGrid()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    ROWS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    COLUMNS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    CELL_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    BUCKET_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    WORDS_PER_BUCKET() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    MEMORY_REGIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? (obj || new SCVMemoryRegion()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    memoryRegionsLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    BANDS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? (obj || new SCVPackedRasterBand()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    bandsLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startSCVPackedRasterProducts(builder) {
        builder.startObject(11);
    }
    static addJobId(builder, JOB_IDOffset) {
        builder.addFieldOffset(0, JOB_IDOffset, 0);
    }
    static addTraceId(builder, TRACE_ID) {
        builder.addFieldInt64(1, TRACE_ID, BigInt('0'));
    }
    static addGrid(builder, GRIDOffset) {
        builder.addFieldOffset(2, GRIDOffset, 0);
    }
    static addTimeGrid(builder, TIME_GRIDOffset) {
        builder.addFieldOffset(3, TIME_GRIDOffset, 0);
    }
    static addRows(builder, ROWS) {
        builder.addFieldInt32(4, ROWS, 0);
    }
    static addColumns(builder, COLUMNS) {
        builder.addFieldInt32(5, COLUMNS, 0);
    }
    static addCellCount(builder, CELL_COUNT) {
        builder.addFieldInt32(6, CELL_COUNT, 0);
    }
    static addBucketCount(builder, BUCKET_COUNT) {
        builder.addFieldInt32(7, BUCKET_COUNT, 0);
    }
    static addWordsPerBucket(builder, WORDS_PER_BUCKET) {
        builder.addFieldInt32(8, WORDS_PER_BUCKET, 0);
    }
    static addMemoryRegions(builder, MEMORY_REGIONSOffset) {
        builder.addFieldOffset(9, MEMORY_REGIONSOffset, 0);
    }
    static createMemoryRegionsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startMemoryRegionsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addBands(builder, BANDSOffset) {
        builder.addFieldOffset(10, BANDSOffset, 0);
    }
    static createBandsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startBandsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endSCVPackedRasterProducts(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new SCVPackedRasterProductsT(this.JOB_ID(), this.TRACE_ID(), (this.GRID() !== null ? this.GRID().unpack() : null), (this.TIME_GRID() !== null ? this.TIME_GRID().unpack() : null), this.ROWS(), this.COLUMNS(), this.CELL_COUNT(), this.BUCKET_COUNT(), this.WORDS_PER_BUCKET(), this.bb.createObjList(this.MEMORY_REGIONS.bind(this), this.memoryRegionsLength()), this.bb.createObjList(this.BANDS.bind(this), this.bandsLength()));
    }
    unpackTo(_o) {
        _o.JOB_ID = this.JOB_ID();
        _o.TRACE_ID = this.TRACE_ID();
        _o.GRID = (this.GRID() !== null ? this.GRID().unpack() : null);
        _o.TIME_GRID = (this.TIME_GRID() !== null ? this.TIME_GRID().unpack() : null);
        _o.ROWS = this.ROWS();
        _o.COLUMNS = this.COLUMNS();
        _o.CELL_COUNT = this.CELL_COUNT();
        _o.BUCKET_COUNT = this.BUCKET_COUNT();
        _o.WORDS_PER_BUCKET = this.WORDS_PER_BUCKET();
        _o.MEMORY_REGIONS = this.bb.createObjList(this.MEMORY_REGIONS.bind(this), this.memoryRegionsLength());
        _o.BANDS = this.bb.createObjList(this.BANDS.bind(this), this.bandsLength());
    }
}
export class SCVPackedRasterProductsT {
    constructor(JOB_ID = null, TRACE_ID = BigInt('0'), GRID = null, TIME_GRID = null, ROWS = 0, COLUMNS = 0, CELL_COUNT = 0, BUCKET_COUNT = 0, WORDS_PER_BUCKET = 0, MEMORY_REGIONS = [], BANDS = []) {
        this.JOB_ID = JOB_ID;
        this.TRACE_ID = TRACE_ID;
        this.GRID = GRID;
        this.TIME_GRID = TIME_GRID;
        this.ROWS = ROWS;
        this.COLUMNS = COLUMNS;
        this.CELL_COUNT = CELL_COUNT;
        this.BUCKET_COUNT = BUCKET_COUNT;
        this.WORDS_PER_BUCKET = WORDS_PER_BUCKET;
        this.MEMORY_REGIONS = MEMORY_REGIONS;
        this.BANDS = BANDS;
    }
    pack(builder) {
        const JOB_ID = (this.JOB_ID !== null ? builder.createString(this.JOB_ID) : 0);
        const GRID = (this.GRID !== null ? this.GRID.pack(builder) : 0);
        const TIME_GRID = (this.TIME_GRID !== null ? this.TIME_GRID.pack(builder) : 0);
        const MEMORY_REGIONS = SCVPackedRasterProducts.createMemoryRegionsVector(builder, builder.createObjectOffsetList(this.MEMORY_REGIONS));
        const BANDS = SCVPackedRasterProducts.createBandsVector(builder, builder.createObjectOffsetList(this.BANDS));
        SCVPackedRasterProducts.startSCVPackedRasterProducts(builder);
        SCVPackedRasterProducts.addJobId(builder, JOB_ID);
        SCVPackedRasterProducts.addTraceId(builder, this.TRACE_ID);
        SCVPackedRasterProducts.addGrid(builder, GRID);
        SCVPackedRasterProducts.addTimeGrid(builder, TIME_GRID);
        SCVPackedRasterProducts.addRows(builder, this.ROWS);
        SCVPackedRasterProducts.addColumns(builder, this.COLUMNS);
        SCVPackedRasterProducts.addCellCount(builder, this.CELL_COUNT);
        SCVPackedRasterProducts.addBucketCount(builder, this.BUCKET_COUNT);
        SCVPackedRasterProducts.addWordsPerBucket(builder, this.WORDS_PER_BUCKET);
        SCVPackedRasterProducts.addMemoryRegions(builder, MEMORY_REGIONS);
        SCVPackedRasterProducts.addBands(builder, BANDS);
        return SCVPackedRasterProducts.endSCVPackedRasterProducts(builder);
    }
}
