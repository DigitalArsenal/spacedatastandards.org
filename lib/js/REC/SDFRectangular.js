// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Rectangular (pyramidal) SDF parameters.
 */
export class SDFRectangular {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSDFRectangular(bb, obj) {
        return (obj || new SDFRectangular()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSDFRectangular(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SDFRectangular()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    X_HALF_ANGLE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    Y_HALF_ANGLE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MIN_RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startSDFRectangular(builder) {
        builder.startObject(4);
    }
    static addXHalfAngle(builder, X_HALF_ANGLE) {
        builder.addFieldFloat64(0, X_HALF_ANGLE, 0.0);
    }
    static addYHalfAngle(builder, Y_HALF_ANGLE) {
        builder.addFieldFloat64(1, Y_HALF_ANGLE, 0.0);
    }
    static addMinRange(builder, MIN_RANGE) {
        builder.addFieldFloat64(2, MIN_RANGE, 0.0);
    }
    static addMaxRange(builder, MAX_RANGE) {
        builder.addFieldFloat64(3, MAX_RANGE, 0.0);
    }
    static endSDFRectangular(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSDFRectangular(builder, X_HALF_ANGLE, Y_HALF_ANGLE, MIN_RANGE, MAX_RANGE) {
        SDFRectangular.startSDFRectangular(builder);
        SDFRectangular.addXHalfAngle(builder, X_HALF_ANGLE);
        SDFRectangular.addYHalfAngle(builder, Y_HALF_ANGLE);
        SDFRectangular.addMinRange(builder, MIN_RANGE);
        SDFRectangular.addMaxRange(builder, MAX_RANGE);
        return SDFRectangular.endSDFRectangular(builder);
    }
    unpack() {
        return new SDFRectangularT(this.X_HALF_ANGLE(), this.Y_HALF_ANGLE(), this.MIN_RANGE(), this.MAX_RANGE());
    }
    unpackTo(_o) {
        _o.X_HALF_ANGLE = this.X_HALF_ANGLE();
        _o.Y_HALF_ANGLE = this.Y_HALF_ANGLE();
        _o.MIN_RANGE = this.MIN_RANGE();
        _o.MAX_RANGE = this.MAX_RANGE();
    }
}
export class SDFRectangularT {
    constructor(X_HALF_ANGLE = 0.0, Y_HALF_ANGLE = 0.0, MIN_RANGE = 0.0, MAX_RANGE = 0.0) {
        this.X_HALF_ANGLE = X_HALF_ANGLE;
        this.Y_HALF_ANGLE = Y_HALF_ANGLE;
        this.MIN_RANGE = MIN_RANGE;
        this.MAX_RANGE = MAX_RANGE;
    }
    pack(builder) {
        return SDFRectangular.createSDFRectangular(builder, this.X_HALF_ANGLE, this.Y_HALF_ANGLE, this.MIN_RANGE, this.MAX_RANGE);
    }
}
