// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { BooleanOperatorType } from './BooleanOperatorType.js';
import { ParameterComparison } from './ParameterComparison.js';
/**
 * Boolean expression combining comparisons
 */
export class BooleanExpression {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsBooleanExpression(bb, obj) {
        return (obj || new BooleanExpression()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsBooleanExpression(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new BooleanExpression()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Boolean operator
     */
    OPERATOR() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : BooleanOperatorType.AND;
    }
    /**
     * Comparison conditions
     */
    CONDITIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new ParameterComparison()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    conditionsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Nested expressions
     */
    EXPRESSIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new BooleanExpression()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    expressionsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startBooleanExpression(builder) {
        builder.startObject(3);
    }
    static addOperator(builder, OPERATOR) {
        builder.addFieldInt8(0, OPERATOR, BooleanOperatorType.AND);
    }
    static addConditions(builder, CONDITIONSOffset) {
        builder.addFieldOffset(1, CONDITIONSOffset, 0);
    }
    static createConditionsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startConditionsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addExpressions(builder, EXPRESSIONSOffset) {
        builder.addFieldOffset(2, EXPRESSIONSOffset, 0);
    }
    static createExpressionsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startExpressionsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endBooleanExpression(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createBooleanExpression(builder, OPERATOR, CONDITIONSOffset, EXPRESSIONSOffset) {
        BooleanExpression.startBooleanExpression(builder);
        BooleanExpression.addOperator(builder, OPERATOR);
        BooleanExpression.addConditions(builder, CONDITIONSOffset);
        BooleanExpression.addExpressions(builder, EXPRESSIONSOffset);
        return BooleanExpression.endBooleanExpression(builder);
    }
    unpack() {
        return new BooleanExpressionT(this.OPERATOR(), this.bb.createObjList(this.CONDITIONS.bind(this), this.conditionsLength()), this.bb.createObjList(this.EXPRESSIONS.bind(this), this.expressionsLength()));
    }
    unpackTo(_o) {
        _o.OPERATOR = this.OPERATOR();
        _o.CONDITIONS = this.bb.createObjList(this.CONDITIONS.bind(this), this.conditionsLength());
        _o.EXPRESSIONS = this.bb.createObjList(this.EXPRESSIONS.bind(this), this.expressionsLength());
    }
}
export class BooleanExpressionT {
    constructor(OPERATOR = BooleanOperatorType.AND, CONDITIONS = [], EXPRESSIONS = []) {
        this.OPERATOR = OPERATOR;
        this.CONDITIONS = CONDITIONS;
        this.EXPRESSIONS = EXPRESSIONS;
    }
    pack(builder) {
        const CONDITIONS = BooleanExpression.createConditionsVector(builder, builder.createObjectOffsetList(this.CONDITIONS));
        const EXPRESSIONS = BooleanExpression.createExpressionsVector(builder, builder.createObjectOffsetList(this.EXPRESSIONS));
        return BooleanExpression.createBooleanExpression(builder, this.OPERATOR, CONDITIONS, EXPRESSIONS);
    }
}
