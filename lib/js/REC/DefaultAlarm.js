// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { EnumerationAlarm } from './EnumerationAlarm.js';
import { StaticAlarmRanges } from './StaticAlarmRanges.js';
/**
 * Default alarm definition
 */
export class DefaultAlarm {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsDefaultAlarm(bb, obj) {
        return (obj || new DefaultAlarm()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsDefaultAlarm(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new DefaultAlarm()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Minimum violations before alarm triggers
     */
    MIN_VIOLATIONS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Static numeric alarm ranges
     */
    STATIC_ALARM_RANGES(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new StaticAlarmRanges()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Enumeration alarms for enumerated types
     */
    ENUMERATION_ALARMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new EnumerationAlarm()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    enumerationAlarmsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startDefaultAlarm(builder) {
        builder.startObject(3);
    }
    static addMinViolations(builder, MIN_VIOLATIONS) {
        builder.addFieldInt16(0, MIN_VIOLATIONS, 0);
    }
    static addStaticAlarmRanges(builder, STATIC_ALARM_RANGESOffset) {
        builder.addFieldOffset(1, STATIC_ALARM_RANGESOffset, 0);
    }
    static addEnumerationAlarms(builder, ENUMERATION_ALARMSOffset) {
        builder.addFieldOffset(2, ENUMERATION_ALARMSOffset, 0);
    }
    static createEnumerationAlarmsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startEnumerationAlarmsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endDefaultAlarm(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new DefaultAlarmT(this.MIN_VIOLATIONS(), (this.STATIC_ALARM_RANGES() !== null ? this.STATIC_ALARM_RANGES().unpack() : null), this.bb.createObjList(this.ENUMERATION_ALARMS.bind(this), this.enumerationAlarmsLength()));
    }
    unpackTo(_o) {
        _o.MIN_VIOLATIONS = this.MIN_VIOLATIONS();
        _o.STATIC_ALARM_RANGES = (this.STATIC_ALARM_RANGES() !== null ? this.STATIC_ALARM_RANGES().unpack() : null);
        _o.ENUMERATION_ALARMS = this.bb.createObjList(this.ENUMERATION_ALARMS.bind(this), this.enumerationAlarmsLength());
    }
}
export class DefaultAlarmT {
    constructor(MIN_VIOLATIONS = 0, STATIC_ALARM_RANGES = null, ENUMERATION_ALARMS = []) {
        this.MIN_VIOLATIONS = MIN_VIOLATIONS;
        this.STATIC_ALARM_RANGES = STATIC_ALARM_RANGES;
        this.ENUMERATION_ALARMS = ENUMERATION_ALARMS;
    }
    pack(builder) {
        const STATIC_ALARM_RANGES = (this.STATIC_ALARM_RANGES !== null ? this.STATIC_ALARM_RANGES.pack(builder) : 0);
        const ENUMERATION_ALARMS = DefaultAlarm.createEnumerationAlarmsVector(builder, builder.createObjectOffsetList(this.ENUMERATION_ALARMS));
        DefaultAlarm.startDefaultAlarm(builder);
        DefaultAlarm.addMinViolations(builder, this.MIN_VIOLATIONS);
        DefaultAlarm.addStaticAlarmRanges(builder, STATIC_ALARM_RANGES);
        DefaultAlarm.addEnumerationAlarms(builder, ENUMERATION_ALARMS);
        return DefaultAlarm.endDefaultAlarm(builder);
    }
}
