// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { scvCoordinateFrame } from './scvCoordinateFrame.js';
import { scvGeometryDomain } from './scvGeometryDomain.js';
export class SCVCoverageGrid {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCVCoverageGrid(bb, obj) {
        return (obj || new SCVCoverageGrid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCVCoverageGrid(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCVCoverageGrid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    GRID_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DOMAIN() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : scvGeometryDomain.SURFACE;
    }
    FRAME() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : scvCoordinateFrame.UNKNOWN;
    }
    MIN_LAT_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_LAT_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MIN_LON_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_LON_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    LAT_STEP_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    LON_STEP_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    ALTITUDE_M() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    CELL_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    RESOLUTION_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startSCVCoverageGrid(builder) {
        builder.startObject(12);
    }
    static addGridId(builder, GRID_IDOffset) {
        builder.addFieldOffset(0, GRID_IDOffset, 0);
    }
    static addDomain(builder, DOMAIN) {
        builder.addFieldInt8(1, DOMAIN, scvGeometryDomain.SURFACE);
    }
    static addFrame(builder, FRAME) {
        builder.addFieldInt8(2, FRAME, scvCoordinateFrame.UNKNOWN);
    }
    static addMinLatDeg(builder, MIN_LAT_DEG) {
        builder.addFieldFloat64(3, MIN_LAT_DEG, 0.0);
    }
    static addMaxLatDeg(builder, MAX_LAT_DEG) {
        builder.addFieldFloat64(4, MAX_LAT_DEG, 0.0);
    }
    static addMinLonDeg(builder, MIN_LON_DEG) {
        builder.addFieldFloat64(5, MIN_LON_DEG, 0.0);
    }
    static addMaxLonDeg(builder, MAX_LON_DEG) {
        builder.addFieldFloat64(6, MAX_LON_DEG, 0.0);
    }
    static addLatStepDeg(builder, LAT_STEP_DEG) {
        builder.addFieldFloat64(7, LAT_STEP_DEG, 0.0);
    }
    static addLonStepDeg(builder, LON_STEP_DEG) {
        builder.addFieldFloat64(8, LON_STEP_DEG, 0.0);
    }
    static addAltitudeM(builder, ALTITUDE_M) {
        builder.addFieldFloat64(9, ALTITUDE_M, 0.0);
    }
    static addCellCount(builder, CELL_COUNT) {
        builder.addFieldInt32(10, CELL_COUNT, 0);
    }
    static addResolutionDeg(builder, RESOLUTION_DEG) {
        builder.addFieldFloat64(11, RESOLUTION_DEG, 0.0);
    }
    static endSCVCoverageGrid(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCVCoverageGrid(builder, GRID_IDOffset, DOMAIN, FRAME, MIN_LAT_DEG, MAX_LAT_DEG, MIN_LON_DEG, MAX_LON_DEG, LAT_STEP_DEG, LON_STEP_DEG, ALTITUDE_M, CELL_COUNT, RESOLUTION_DEG) {
        SCVCoverageGrid.startSCVCoverageGrid(builder);
        SCVCoverageGrid.addGridId(builder, GRID_IDOffset);
        SCVCoverageGrid.addDomain(builder, DOMAIN);
        SCVCoverageGrid.addFrame(builder, FRAME);
        SCVCoverageGrid.addMinLatDeg(builder, MIN_LAT_DEG);
        SCVCoverageGrid.addMaxLatDeg(builder, MAX_LAT_DEG);
        SCVCoverageGrid.addMinLonDeg(builder, MIN_LON_DEG);
        SCVCoverageGrid.addMaxLonDeg(builder, MAX_LON_DEG);
        SCVCoverageGrid.addLatStepDeg(builder, LAT_STEP_DEG);
        SCVCoverageGrid.addLonStepDeg(builder, LON_STEP_DEG);
        SCVCoverageGrid.addAltitudeM(builder, ALTITUDE_M);
        SCVCoverageGrid.addCellCount(builder, CELL_COUNT);
        SCVCoverageGrid.addResolutionDeg(builder, RESOLUTION_DEG);
        return SCVCoverageGrid.endSCVCoverageGrid(builder);
    }
    unpack() {
        return new SCVCoverageGridT(this.GRID_ID(), this.DOMAIN(), this.FRAME(), this.MIN_LAT_DEG(), this.MAX_LAT_DEG(), this.MIN_LON_DEG(), this.MAX_LON_DEG(), this.LAT_STEP_DEG(), this.LON_STEP_DEG(), this.ALTITUDE_M(), this.CELL_COUNT(), this.RESOLUTION_DEG());
    }
    unpackTo(_o) {
        _o.GRID_ID = this.GRID_ID();
        _o.DOMAIN = this.DOMAIN();
        _o.FRAME = this.FRAME();
        _o.MIN_LAT_DEG = this.MIN_LAT_DEG();
        _o.MAX_LAT_DEG = this.MAX_LAT_DEG();
        _o.MIN_LON_DEG = this.MIN_LON_DEG();
        _o.MAX_LON_DEG = this.MAX_LON_DEG();
        _o.LAT_STEP_DEG = this.LAT_STEP_DEG();
        _o.LON_STEP_DEG = this.LON_STEP_DEG();
        _o.ALTITUDE_M = this.ALTITUDE_M();
        _o.CELL_COUNT = this.CELL_COUNT();
        _o.RESOLUTION_DEG = this.RESOLUTION_DEG();
    }
}
export class SCVCoverageGridT {
    constructor(GRID_ID = null, DOMAIN = scvGeometryDomain.SURFACE, FRAME = scvCoordinateFrame.UNKNOWN, MIN_LAT_DEG = 0.0, MAX_LAT_DEG = 0.0, MIN_LON_DEG = 0.0, MAX_LON_DEG = 0.0, LAT_STEP_DEG = 0.0, LON_STEP_DEG = 0.0, ALTITUDE_M = 0.0, CELL_COUNT = 0, RESOLUTION_DEG = 0.0) {
        this.GRID_ID = GRID_ID;
        this.DOMAIN = DOMAIN;
        this.FRAME = FRAME;
        this.MIN_LAT_DEG = MIN_LAT_DEG;
        this.MAX_LAT_DEG = MAX_LAT_DEG;
        this.MIN_LON_DEG = MIN_LON_DEG;
        this.MAX_LON_DEG = MAX_LON_DEG;
        this.LAT_STEP_DEG = LAT_STEP_DEG;
        this.LON_STEP_DEG = LON_STEP_DEG;
        this.ALTITUDE_M = ALTITUDE_M;
        this.CELL_COUNT = CELL_COUNT;
        this.RESOLUTION_DEG = RESOLUTION_DEG;
    }
    pack(builder) {
        const GRID_ID = (this.GRID_ID !== null ? builder.createString(this.GRID_ID) : 0);
        return SCVCoverageGrid.createSCVCoverageGrid(builder, GRID_ID, this.DOMAIN, this.FRAME, this.MIN_LAT_DEG, this.MAX_LAT_DEG, this.MIN_LON_DEG, this.MAX_LON_DEG, this.LAT_STEP_DEG, this.LON_STEP_DEG, this.ALTITUDE_M, this.CELL_COUNT, this.RESOLUTION_DEG);
    }
}
