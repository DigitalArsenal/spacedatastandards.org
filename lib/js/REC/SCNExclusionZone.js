// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GJNGeometry } from './GJNGeometry.js';
import { GJNPosition } from './GJNPosition.js';
/**
 * Scenario exclusion zone. BOUNDARY carries the canonical geospatial shape
 * when available; POINTS preserves simple WGS84 polygon imports.
 */
export class SCNExclusionZone {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCNExclusionZone(bb, obj) {
        return (obj || new SCNExclusionZone()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCNExclusionZone(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCNExclusionZone()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    LABEL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FILL_COLOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    LABEL_COLOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * True when the exclusion-zone polygon should be filled.
     */
    IS_FILLED() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Simple WGS84 polygon points for imported zones.
     */
    POINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new GJNPosition()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    pointsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Canonical GeoJSON geometry for the exclusion zone.
     */
    BOUNDARY(obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new GJNGeometry()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startSCNExclusionZone(builder) {
        builder.startObject(6);
    }
    static addLabel(builder, LABELOffset) {
        builder.addFieldOffset(0, LABELOffset, 0);
    }
    static addFillColor(builder, FILL_COLOROffset) {
        builder.addFieldOffset(1, FILL_COLOROffset, 0);
    }
    static addLabelColor(builder, LABEL_COLOROffset) {
        builder.addFieldOffset(2, LABEL_COLOROffset, 0);
    }
    static addIsFilled(builder, IS_FILLED) {
        builder.addFieldInt8(3, +IS_FILLED, +false);
    }
    static addPoints(builder, POINTSOffset) {
        builder.addFieldOffset(4, POINTSOffset, 0);
    }
    static createPointsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPointsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addBoundary(builder, BOUNDARYOffset) {
        builder.addFieldOffset(5, BOUNDARYOffset, 0);
    }
    static endSCNExclusionZone(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new SCNExclusionZoneT(this.LABEL(), this.FILL_COLOR(), this.LABEL_COLOR(), this.IS_FILLED(), this.bb.createObjList(this.POINTS.bind(this), this.pointsLength()), (this.BOUNDARY() !== null ? this.BOUNDARY().unpack() : null));
    }
    unpackTo(_o) {
        _o.LABEL = this.LABEL();
        _o.FILL_COLOR = this.FILL_COLOR();
        _o.LABEL_COLOR = this.LABEL_COLOR();
        _o.IS_FILLED = this.IS_FILLED();
        _o.POINTS = this.bb.createObjList(this.POINTS.bind(this), this.pointsLength());
        _o.BOUNDARY = (this.BOUNDARY() !== null ? this.BOUNDARY().unpack() : null);
    }
}
export class SCNExclusionZoneT {
    constructor(LABEL = null, FILL_COLOR = null, LABEL_COLOR = null, IS_FILLED = false, POINTS = [], BOUNDARY = null) {
        this.LABEL = LABEL;
        this.FILL_COLOR = FILL_COLOR;
        this.LABEL_COLOR = LABEL_COLOR;
        this.IS_FILLED = IS_FILLED;
        this.POINTS = POINTS;
        this.BOUNDARY = BOUNDARY;
    }
    pack(builder) {
        const LABEL = (this.LABEL !== null ? builder.createString(this.LABEL) : 0);
        const FILL_COLOR = (this.FILL_COLOR !== null ? builder.createString(this.FILL_COLOR) : 0);
        const LABEL_COLOR = (this.LABEL_COLOR !== null ? builder.createString(this.LABEL_COLOR) : 0);
        const POINTS = SCNExclusionZone.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));
        const BOUNDARY = (this.BOUNDARY !== null ? this.BOUNDARY.pack(builder) : 0);
        SCNExclusionZone.startSCNExclusionZone(builder);
        SCNExclusionZone.addLabel(builder, LABEL);
        SCNExclusionZone.addFillColor(builder, FILL_COLOR);
        SCNExclusionZone.addLabelColor(builder, LABEL_COLOR);
        SCNExclusionZone.addIsFilled(builder, this.IS_FILLED);
        SCNExclusionZone.addPoints(builder, POINTS);
        SCNExclusionZone.addBoundary(builder, BOUNDARY);
        return SCNExclusionZone.endSCNExclusionZone(builder);
    }
}
