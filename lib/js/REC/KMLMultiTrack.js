// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLTrack } from './KMLTrack.js';
/**
 * gx:MultiTrack
 */
export class KMLMultiTrack {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLMultiTrack(bb, obj) {
        return (obj || new KMLMultiTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLMultiTrack(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLMultiTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Altitude mode
     */
    ALTITUDE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
    }
    /**
     * Whether to interpolate between tracks
     */
    INTERPOLATE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Child tracks
     */
    TRACKS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new KMLTrack()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    tracksLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startKMLMultiTrack(builder) {
        builder.startObject(3);
    }
    static addAltitudeMode(builder, ALTITUDE_MODE) {
        builder.addFieldInt8(0, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
    }
    static addInterpolate(builder, INTERPOLATE) {
        builder.addFieldInt8(1, +INTERPOLATE, +false);
    }
    static addTracks(builder, TRACKSOffset) {
        builder.addFieldOffset(2, TRACKSOffset, 0);
    }
    static createTracksVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTracksVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endKMLMultiTrack(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLMultiTrack(builder, ALTITUDE_MODE, INTERPOLATE, TRACKSOffset) {
        KMLMultiTrack.startKMLMultiTrack(builder);
        KMLMultiTrack.addAltitudeMode(builder, ALTITUDE_MODE);
        KMLMultiTrack.addInterpolate(builder, INTERPOLATE);
        KMLMultiTrack.addTracks(builder, TRACKSOffset);
        return KMLMultiTrack.endKMLMultiTrack(builder);
    }
    unpack() {
        return new KMLMultiTrackT(this.ALTITUDE_MODE(), this.INTERPOLATE(), this.bb.createObjList(this.TRACKS.bind(this), this.tracksLength()));
    }
    unpackTo(_o) {
        _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
        _o.INTERPOLATE = this.INTERPOLATE();
        _o.TRACKS = this.bb.createObjList(this.TRACKS.bind(this), this.tracksLength());
    }
}
export class KMLMultiTrackT {
    constructor(ALTITUDE_MODE = KMLAltitudeMode.CLAMP_TO_GROUND, INTERPOLATE = false, TRACKS = []) {
        this.ALTITUDE_MODE = ALTITUDE_MODE;
        this.INTERPOLATE = INTERPOLATE;
        this.TRACKS = TRACKS;
    }
    pack(builder) {
        const TRACKS = KMLMultiTrack.createTracksVector(builder, builder.createObjectOffsetList(this.TRACKS));
        return KMLMultiTrack.createKMLMultiTrack(builder, this.ALTITUDE_MODE, this.INTERPOLATE, TRACKS);
    }
}
