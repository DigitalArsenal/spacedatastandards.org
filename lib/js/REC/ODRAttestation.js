// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class ODRAttestation {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsODRAttestation(bb, obj) {
        return (obj || new ODRAttestation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsODRAttestation(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ODRAttestation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SIGNING_PUBLIC_KEY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SIGNED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    CANONICAL_JSON_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    canonicalJsonSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    canonicalJsonSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    static startODRAttestation(builder) {
        builder.startObject(4);
    }
    static addSigningPublicKey(builder, SIGNING_PUBLIC_KEYOffset) {
        builder.addFieldOffset(0, SIGNING_PUBLIC_KEYOffset, 0);
    }
    static addSignedAt(builder, SIGNED_ATOffset) {
        builder.addFieldOffset(1, SIGNED_ATOffset, 0);
    }
    static addSignature(builder, SIGNATUREOffset) {
        builder.addFieldOffset(2, SIGNATUREOffset, 0);
    }
    static createSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATUREOffset) {
        builder.addFieldOffset(3, CANONICAL_JSON_SIGNATUREOffset, 0);
    }
    static createCanonicalJsonSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startCanonicalJsonSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endODRAttestation(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // SIGNING_PUBLIC_KEY
        builder.requiredField(offset, 8); // SIGNATURE
        builder.requiredField(offset, 10); // CANONICAL_JSON_SIGNATURE
        return offset;
    }
    static createODRAttestation(builder, SIGNING_PUBLIC_KEYOffset, SIGNED_ATOffset, SIGNATUREOffset, CANONICAL_JSON_SIGNATUREOffset) {
        ODRAttestation.startODRAttestation(builder);
        ODRAttestation.addSigningPublicKey(builder, SIGNING_PUBLIC_KEYOffset);
        ODRAttestation.addSignedAt(builder, SIGNED_ATOffset);
        ODRAttestation.addSignature(builder, SIGNATUREOffset);
        ODRAttestation.addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATUREOffset);
        return ODRAttestation.endODRAttestation(builder);
    }
    unpack() {
        return new ODRAttestationT(this.SIGNING_PUBLIC_KEY(), this.SIGNED_AT(), this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength()), this.bb.createScalarList(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength()));
    }
    unpackTo(_o) {
        _o.SIGNING_PUBLIC_KEY = this.SIGNING_PUBLIC_KEY();
        _o.SIGNED_AT = this.SIGNED_AT();
        _o.SIGNATURE = this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength());
        _o.CANONICAL_JSON_SIGNATURE = this.bb.createScalarList(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength());
    }
}
export class ODRAttestationT {
    constructor(SIGNING_PUBLIC_KEY = null, SIGNED_AT = null, SIGNATURE = [], CANONICAL_JSON_SIGNATURE = []) {
        this.SIGNING_PUBLIC_KEY = SIGNING_PUBLIC_KEY;
        this.SIGNED_AT = SIGNED_AT;
        this.SIGNATURE = SIGNATURE;
        this.CANONICAL_JSON_SIGNATURE = CANONICAL_JSON_SIGNATURE;
    }
    pack(builder) {
        const SIGNING_PUBLIC_KEY = (this.SIGNING_PUBLIC_KEY !== null ? builder.createString(this.SIGNING_PUBLIC_KEY) : 0);
        const SIGNED_AT = (this.SIGNED_AT !== null ? builder.createString(this.SIGNED_AT) : 0);
        const SIGNATURE = ODRAttestation.createSignatureVector(builder, this.SIGNATURE);
        const CANONICAL_JSON_SIGNATURE = ODRAttestation.createCanonicalJsonSignatureVector(builder, this.CANONICAL_JSON_SIGNATURE);
        return ODRAttestation.createODRAttestation(builder, SIGNING_PUBLIC_KEY, SIGNED_AT, SIGNATURE, CANONICAL_JSON_SIGNATURE);
    }
}
