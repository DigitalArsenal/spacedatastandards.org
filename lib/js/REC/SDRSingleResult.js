// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Compact single-frame detection result.
 */
export class SDRSingleResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSDRSingleResult(bb, obj) {
        return (obj || new SDRSingleResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSDRSingleResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SDRSingleResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    DETECTED() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Range to target (meters).
     */
    RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Signal-to-noise ratio (dB).
     */
    SNR() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Azimuth angle to target (radians).
     */
    AZIMUTH() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Elevation angle to target (radians).
     */
    ELEVATION() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Range rate / Doppler velocity (m/s).
     */
    RANGE_RATE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Detection timestamp (milliseconds since epoch).
     */
    TIMESTAMP_MS() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Track identifier (0 if not tracking).
     */
    TRACK_ID() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    static startSDRSingleResult(builder) {
        builder.startObject(8);
    }
    static addDetected(builder, DETECTED) {
        builder.addFieldInt8(0, +DETECTED, +false);
    }
    static addRange(builder, RANGE) {
        builder.addFieldFloat64(1, RANGE, 0.0);
    }
    static addSnr(builder, SNR) {
        builder.addFieldFloat64(2, SNR, 0.0);
    }
    static addAzimuth(builder, AZIMUTH) {
        builder.addFieldFloat64(3, AZIMUTH, 0.0);
    }
    static addElevation(builder, ELEVATION) {
        builder.addFieldFloat64(4, ELEVATION, 0.0);
    }
    static addRangeRate(builder, RANGE_RATE) {
        builder.addFieldFloat64(5, RANGE_RATE, 0.0);
    }
    static addTimestampMs(builder, TIMESTAMP_MS) {
        builder.addFieldInt64(6, TIMESTAMP_MS, BigInt('0'));
    }
    static addTrackId(builder, TRACK_ID) {
        builder.addFieldInt32(7, TRACK_ID, 0);
    }
    static endSDRSingleResult(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSDRSingleResult(builder, DETECTED, RANGE, SNR, AZIMUTH, ELEVATION, RANGE_RATE, TIMESTAMP_MS, TRACK_ID) {
        SDRSingleResult.startSDRSingleResult(builder);
        SDRSingleResult.addDetected(builder, DETECTED);
        SDRSingleResult.addRange(builder, RANGE);
        SDRSingleResult.addSnr(builder, SNR);
        SDRSingleResult.addAzimuth(builder, AZIMUTH);
        SDRSingleResult.addElevation(builder, ELEVATION);
        SDRSingleResult.addRangeRate(builder, RANGE_RATE);
        SDRSingleResult.addTimestampMs(builder, TIMESTAMP_MS);
        SDRSingleResult.addTrackId(builder, TRACK_ID);
        return SDRSingleResult.endSDRSingleResult(builder);
    }
    unpack() {
        return new SDRSingleResultT(this.DETECTED(), this.RANGE(), this.SNR(), this.AZIMUTH(), this.ELEVATION(), this.RANGE_RATE(), this.TIMESTAMP_MS(), this.TRACK_ID());
    }
    unpackTo(_o) {
        _o.DETECTED = this.DETECTED();
        _o.RANGE = this.RANGE();
        _o.SNR = this.SNR();
        _o.AZIMUTH = this.AZIMUTH();
        _o.ELEVATION = this.ELEVATION();
        _o.RANGE_RATE = this.RANGE_RATE();
        _o.TIMESTAMP_MS = this.TIMESTAMP_MS();
        _o.TRACK_ID = this.TRACK_ID();
    }
}
export class SDRSingleResultT {
    constructor(DETECTED = false, RANGE = 0.0, SNR = 0.0, AZIMUTH = 0.0, ELEVATION = 0.0, RANGE_RATE = 0.0, TIMESTAMP_MS = BigInt('0'), TRACK_ID = 0) {
        this.DETECTED = DETECTED;
        this.RANGE = RANGE;
        this.SNR = SNR;
        this.AZIMUTH = AZIMUTH;
        this.ELEVATION = ELEVATION;
        this.RANGE_RATE = RANGE_RATE;
        this.TIMESTAMP_MS = TIMESTAMP_MS;
        this.TRACK_ID = TRACK_ID;
    }
    pack(builder) {
        return SDRSingleResult.createSDRSingleResult(builder, this.DETECTED, this.RANGE, this.SNR, this.AZIMUTH, this.ELEVATION, this.RANGE_RATE, this.TIMESTAMP_MS, this.TRACK_ID);
    }
}
