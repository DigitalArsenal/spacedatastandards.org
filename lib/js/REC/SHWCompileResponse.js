// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SHWUniform } from './SHWUniform.js';
/**
 * Compile response — whether the shader compiled, with diagnostics.
 */
export class SHWCompileResponse {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSHWCompileResponse(bb, obj) {
        return (obj || new SHWCompileResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSHWCompileResponse(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SHWCompileResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SHADER_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Whether compilation succeeded.
     */
    COMPILE_SUCCESS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    INFO_LOG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Optional compiled binary (if the compiler emits one).
     */
    COMPILED_BINARY(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    compiledBinaryLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    compiledBinaryArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Echo of the uniforms the compiler actually linked.
     */
    LINKED_UNIFORMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new SHWUniform()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    linkedUniformsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startSHWCompileResponse(builder) {
        builder.startObject(5);
    }
    static addShaderName(builder, SHADER_NAMEOffset) {
        builder.addFieldOffset(0, SHADER_NAMEOffset, 0);
    }
    static addCompileSuccess(builder, COMPILE_SUCCESS) {
        builder.addFieldInt8(1, +COMPILE_SUCCESS, +false);
    }
    static addInfoLog(builder, INFO_LOGOffset) {
        builder.addFieldOffset(2, INFO_LOGOffset, 0);
    }
    static addCompiledBinary(builder, COMPILED_BINARYOffset) {
        builder.addFieldOffset(3, COMPILED_BINARYOffset, 0);
    }
    static createCompiledBinaryVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startCompiledBinaryVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addLinkedUniforms(builder, LINKED_UNIFORMSOffset) {
        builder.addFieldOffset(4, LINKED_UNIFORMSOffset, 0);
    }
    static createLinkedUniformsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startLinkedUniformsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endSHWCompileResponse(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSHWCompileResponse(builder, SHADER_NAMEOffset, COMPILE_SUCCESS, INFO_LOGOffset, COMPILED_BINARYOffset, LINKED_UNIFORMSOffset) {
        SHWCompileResponse.startSHWCompileResponse(builder);
        SHWCompileResponse.addShaderName(builder, SHADER_NAMEOffset);
        SHWCompileResponse.addCompileSuccess(builder, COMPILE_SUCCESS);
        SHWCompileResponse.addInfoLog(builder, INFO_LOGOffset);
        SHWCompileResponse.addCompiledBinary(builder, COMPILED_BINARYOffset);
        SHWCompileResponse.addLinkedUniforms(builder, LINKED_UNIFORMSOffset);
        return SHWCompileResponse.endSHWCompileResponse(builder);
    }
    unpack() {
        return new SHWCompileResponseT(this.SHADER_NAME(), this.COMPILE_SUCCESS(), this.INFO_LOG(), this.bb.createScalarList(this.COMPILED_BINARY.bind(this), this.compiledBinaryLength()), this.bb.createObjList(this.LINKED_UNIFORMS.bind(this), this.linkedUniformsLength()));
    }
    unpackTo(_o) {
        _o.SHADER_NAME = this.SHADER_NAME();
        _o.COMPILE_SUCCESS = this.COMPILE_SUCCESS();
        _o.INFO_LOG = this.INFO_LOG();
        _o.COMPILED_BINARY = this.bb.createScalarList(this.COMPILED_BINARY.bind(this), this.compiledBinaryLength());
        _o.LINKED_UNIFORMS = this.bb.createObjList(this.LINKED_UNIFORMS.bind(this), this.linkedUniformsLength());
    }
}
export class SHWCompileResponseT {
    constructor(SHADER_NAME = null, COMPILE_SUCCESS = false, INFO_LOG = null, COMPILED_BINARY = [], LINKED_UNIFORMS = []) {
        this.SHADER_NAME = SHADER_NAME;
        this.COMPILE_SUCCESS = COMPILE_SUCCESS;
        this.INFO_LOG = INFO_LOG;
        this.COMPILED_BINARY = COMPILED_BINARY;
        this.LINKED_UNIFORMS = LINKED_UNIFORMS;
    }
    pack(builder) {
        const SHADER_NAME = (this.SHADER_NAME !== null ? builder.createString(this.SHADER_NAME) : 0);
        const INFO_LOG = (this.INFO_LOG !== null ? builder.createString(this.INFO_LOG) : 0);
        const COMPILED_BINARY = SHWCompileResponse.createCompiledBinaryVector(builder, this.COMPILED_BINARY);
        const LINKED_UNIFORMS = SHWCompileResponse.createLinkedUniformsVector(builder, builder.createObjectOffsetList(this.LINKED_UNIFORMS));
        return SHWCompileResponse.createSHWCompileResponse(builder, SHADER_NAME, this.COMPILE_SUCCESS, INFO_LOG, COMPILED_BINARY, LINKED_UNIFORMS);
    }
}
