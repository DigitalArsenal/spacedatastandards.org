// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { stateRequestKind } from './stateRequestKind.js';
/**
 * Request current state from a scenario controls host.
 */
export class SCCRequestState {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCCRequestState(bb, obj) {
        return (obj || new SCCRequestState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCCRequestState(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCCRequestState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    REQUEST_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * State payload requested by the caller.
     */
    REQUEST_KIND() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : stateRequestKind.UNKNOWN;
    }
    static startSCCRequestState(builder) {
        builder.startObject(2);
    }
    static addRequestId(builder, REQUEST_IDOffset) {
        builder.addFieldOffset(0, REQUEST_IDOffset, 0);
    }
    static addRequestKind(builder, REQUEST_KIND) {
        builder.addFieldInt8(1, REQUEST_KIND, stateRequestKind.UNKNOWN);
    }
    static endSCCRequestState(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCCRequestState(builder, REQUEST_IDOffset, REQUEST_KIND) {
        SCCRequestState.startSCCRequestState(builder);
        SCCRequestState.addRequestId(builder, REQUEST_IDOffset);
        SCCRequestState.addRequestKind(builder, REQUEST_KIND);
        return SCCRequestState.endSCCRequestState(builder);
    }
    unpack() {
        return new SCCRequestStateT(this.REQUEST_ID(), this.REQUEST_KIND());
    }
    unpackTo(_o) {
        _o.REQUEST_ID = this.REQUEST_ID();
        _o.REQUEST_KIND = this.REQUEST_KIND();
    }
}
export class SCCRequestStateT {
    constructor(REQUEST_ID = null, REQUEST_KIND = stateRequestKind.UNKNOWN) {
        this.REQUEST_ID = REQUEST_ID;
        this.REQUEST_KIND = REQUEST_KIND;
    }
    pack(builder) {
        const REQUEST_ID = (this.REQUEST_ID !== null ? builder.createString(this.REQUEST_ID) : 0);
        return SCCRequestState.createSCCRequestState(builder, REQUEST_ID, this.REQUEST_KIND);
    }
}
