// automatically generated by the FlatBuffers compiler, do not modify
/**
 * A row that answers a CONE or FIELD_OF_VIEW query.
 */
export class SKRHit {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    ROW() {
        return this.bb.readUint32(this.bb_pos);
    }
    /**
     * SKTPoint FLAGS bits for the row.
     */
    FLAGS() {
        return this.bb.readUint32(this.bb_pos + 4);
    }
    RA_DEG() {
        return this.bb.readFloat64(this.bb_pos + 8);
    }
    DEC_DEG() {
        return this.bb.readFloat64(this.bb_pos + 16);
    }
    /**
     * Angle from the centre or boresight.
     */
    SEPARATION_DEG() {
        return this.bb.readFloat64(this.bb_pos + 24);
    }
    /**
     * Field coordinates: angles along the field's x and y axes (the
     * standard coordinates of a cone query). NaN behind the tangent plane.
     */
    FIELD_X_DEG() {
        return this.bb.readFloat64(this.bb_pos + 32);
    }
    FIELD_Y_DEG() {
        return this.bb.readFloat64(this.bb_pos + 40);
    }
    /**
     * Magnitude in the query's band; NaN when unknown.
     */
    MAG() {
        return this.bb.readFloat32(this.bb_pos + 48);
    }
    RESERVED() {
        return this.bb.readUint32(this.bb_pos + 52);
    }
    static sizeOf() {
        return 56;
    }
    static createSKRHit(builder, ROW, FLAGS, RA_DEG, DEC_DEG, SEPARATION_DEG, FIELD_X_DEG, FIELD_Y_DEG, MAG, RESERVED) {
        builder.prep(8, 56);
        builder.writeInt32(RESERVED);
        builder.writeFloat32(MAG);
        builder.writeFloat64(FIELD_Y_DEG);
        builder.writeFloat64(FIELD_X_DEG);
        builder.writeFloat64(SEPARATION_DEG);
        builder.writeFloat64(DEC_DEG);
        builder.writeFloat64(RA_DEG);
        builder.writeInt32(FLAGS);
        builder.writeInt32(ROW);
        return builder.offset();
    }
    unpack() {
        return new SKRHitT(this.ROW(), this.FLAGS(), this.RA_DEG(), this.DEC_DEG(), this.SEPARATION_DEG(), this.FIELD_X_DEG(), this.FIELD_Y_DEG(), this.MAG(), this.RESERVED());
    }
    unpackTo(_o) {
        _o.ROW = this.ROW();
        _o.FLAGS = this.FLAGS();
        _o.RA_DEG = this.RA_DEG();
        _o.DEC_DEG = this.DEC_DEG();
        _o.SEPARATION_DEG = this.SEPARATION_DEG();
        _o.FIELD_X_DEG = this.FIELD_X_DEG();
        _o.FIELD_Y_DEG = this.FIELD_Y_DEG();
        _o.MAG = this.MAG();
        _o.RESERVED = this.RESERVED();
    }
}
export class SKRHitT {
    constructor(ROW = 0, FLAGS = 0, RA_DEG = 0.0, DEC_DEG = 0.0, SEPARATION_DEG = 0.0, FIELD_X_DEG = 0.0, FIELD_Y_DEG = 0.0, MAG = 0.0, RESERVED = 0) {
        this.ROW = ROW;
        this.FLAGS = FLAGS;
        this.RA_DEG = RA_DEG;
        this.DEC_DEG = DEC_DEG;
        this.SEPARATION_DEG = SEPARATION_DEG;
        this.FIELD_X_DEG = FIELD_X_DEG;
        this.FIELD_Y_DEG = FIELD_Y_DEG;
        this.MAG = MAG;
        this.RESERVED = RESERVED;
    }
    pack(builder) {
        return SKRHit.createSKRHit(builder, this.ROW, this.FLAGS, this.RA_DEG, this.DEC_DEG, this.SEPARATION_DEG, this.FIELD_X_DEG, this.FIELD_Y_DEG, this.MAG, this.RESERVED);
    }
}
