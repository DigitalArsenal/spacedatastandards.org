// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Counters for the node's trust rule evaluation engine.
 */
export class NDSTrustEngineStat {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNDSTrustEngineStat(bb, obj) {
        return (obj || new NDSTrustEngineStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNDSTrustEngineStat(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NDSTrustEngineStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Minimum interval between evaluation passes, milliseconds.
     */
    MIN_INTERVAL_MS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    RUNS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix seconds; 0 = never.
     */
    LAST_RUN_AT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    static startNDSTrustEngineStat(builder) {
        builder.startObject(3);
    }
    static addMinIntervalMs(builder, MIN_INTERVAL_MS) {
        builder.addFieldInt64(0, MIN_INTERVAL_MS, BigInt('0'));
    }
    static addRuns(builder, RUNS) {
        builder.addFieldInt64(1, RUNS, BigInt('0'));
    }
    static addLastRunAt(builder, LAST_RUN_AT) {
        builder.addFieldInt64(2, LAST_RUN_AT, BigInt('0'));
    }
    static endNDSTrustEngineStat(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNDSTrustEngineStat(builder, MIN_INTERVAL_MS, RUNS, LAST_RUN_AT) {
        NDSTrustEngineStat.startNDSTrustEngineStat(builder);
        NDSTrustEngineStat.addMinIntervalMs(builder, MIN_INTERVAL_MS);
        NDSTrustEngineStat.addRuns(builder, RUNS);
        NDSTrustEngineStat.addLastRunAt(builder, LAST_RUN_AT);
        return NDSTrustEngineStat.endNDSTrustEngineStat(builder);
    }
    unpack() {
        return new NDSTrustEngineStatT(this.MIN_INTERVAL_MS(), this.RUNS(), this.LAST_RUN_AT());
    }
    unpackTo(_o) {
        _o.MIN_INTERVAL_MS = this.MIN_INTERVAL_MS();
        _o.RUNS = this.RUNS();
        _o.LAST_RUN_AT = this.LAST_RUN_AT();
    }
}
export class NDSTrustEngineStatT {
    constructor(MIN_INTERVAL_MS = BigInt('0'), RUNS = BigInt('0'), LAST_RUN_AT = BigInt('0')) {
        this.MIN_INTERVAL_MS = MIN_INTERVAL_MS;
        this.RUNS = RUNS;
        this.LAST_RUN_AT = LAST_RUN_AT;
    }
    pack(builder) {
        return NDSTrustEngineStat.createNDSTrustEngineStat(builder, this.MIN_INTERVAL_MS, this.RUNS, this.LAST_RUN_AT);
    }
}
