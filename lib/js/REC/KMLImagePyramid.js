// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLGridOrigin } from './KMLGridOrigin.js';
/**
 * ImagePyramid for PhotoOverlay
 */
export class KMLImagePyramid {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLImagePyramid(bb, obj) {
        return (obj || new KMLImagePyramid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLImagePyramid(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLImagePyramid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Tile size in pixels
     */
    TILE_SIZE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    /**
     * Maximum image width
     */
    MAX_WIDTH() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    /**
     * Maximum image height
     */
    MAX_HEIGHT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    /**
     * Grid origin
     */
    GRID_ORIGIN() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLGridOrigin.LOWER_LEFT;
    }
    static startKMLImagePyramid(builder) {
        builder.startObject(4);
    }
    static addTileSize(builder, TILE_SIZE) {
        builder.addFieldInt32(0, TILE_SIZE, 0);
    }
    static addMaxWidth(builder, MAX_WIDTH) {
        builder.addFieldInt32(1, MAX_WIDTH, 0);
    }
    static addMaxHeight(builder, MAX_HEIGHT) {
        builder.addFieldInt32(2, MAX_HEIGHT, 0);
    }
    static addGridOrigin(builder, GRID_ORIGIN) {
        builder.addFieldInt8(3, GRID_ORIGIN, KMLGridOrigin.LOWER_LEFT);
    }
    static endKMLImagePyramid(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLImagePyramid(builder, TILE_SIZE, MAX_WIDTH, MAX_HEIGHT, GRID_ORIGIN) {
        KMLImagePyramid.startKMLImagePyramid(builder);
        KMLImagePyramid.addTileSize(builder, TILE_SIZE);
        KMLImagePyramid.addMaxWidth(builder, MAX_WIDTH);
        KMLImagePyramid.addMaxHeight(builder, MAX_HEIGHT);
        KMLImagePyramid.addGridOrigin(builder, GRID_ORIGIN);
        return KMLImagePyramid.endKMLImagePyramid(builder);
    }
    unpack() {
        return new KMLImagePyramidT(this.TILE_SIZE(), this.MAX_WIDTH(), this.MAX_HEIGHT(), this.GRID_ORIGIN());
    }
    unpackTo(_o) {
        _o.TILE_SIZE = this.TILE_SIZE();
        _o.MAX_WIDTH = this.MAX_WIDTH();
        _o.MAX_HEIGHT = this.MAX_HEIGHT();
        _o.GRID_ORIGIN = this.GRID_ORIGIN();
    }
}
export class KMLImagePyramidT {
    constructor(TILE_SIZE = 0, MAX_WIDTH = 0, MAX_HEIGHT = 0, GRID_ORIGIN = KMLGridOrigin.LOWER_LEFT) {
        this.TILE_SIZE = TILE_SIZE;
        this.MAX_WIDTH = MAX_WIDTH;
        this.MAX_HEIGHT = MAX_HEIGHT;
        this.GRID_ORIGIN = GRID_ORIGIN;
    }
    pack(builder) {
        return KMLImagePyramid.createKMLImagePyramid(builder, this.TILE_SIZE, this.MAX_WIDTH, this.MAX_HEIGHT, this.GRID_ORIGIN);
    }
}
