// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One flow trigger (e.g. a host timer or HTTP route) that starts a drain.
 */
export class PLGFlowTrigger {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPLGFlowTrigger(bb, obj) {
        return (obj || new PLGFlowTrigger()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPLGFlowTrigger(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PLGFlowTrigger()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    TRIGGER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    KIND(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Default firing interval in milliseconds (timer triggers)
     */
    DEFAULT_INTERVAL_MS() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    HTTP_PATH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startPLGFlowTrigger(builder) {
        builder.startObject(5);
    }
    static addTriggerId(builder, TRIGGER_IDOffset) {
        builder.addFieldOffset(0, TRIGGER_IDOffset, 0);
    }
    static addKind(builder, KINDOffset) {
        builder.addFieldOffset(1, KINDOffset, 0);
    }
    static addSource(builder, SOURCEOffset) {
        builder.addFieldOffset(2, SOURCEOffset, 0);
    }
    static addDefaultIntervalMs(builder, DEFAULT_INTERVAL_MS) {
        builder.addFieldInt64(3, DEFAULT_INTERVAL_MS, BigInt('0'));
    }
    static addHttpPath(builder, HTTP_PATHOffset) {
        builder.addFieldOffset(4, HTTP_PATHOffset, 0);
    }
    static endPLGFlowTrigger(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // TRIGGER_ID
        return offset;
    }
    static createPLGFlowTrigger(builder, TRIGGER_IDOffset, KINDOffset, SOURCEOffset, DEFAULT_INTERVAL_MS, HTTP_PATHOffset) {
        PLGFlowTrigger.startPLGFlowTrigger(builder);
        PLGFlowTrigger.addTriggerId(builder, TRIGGER_IDOffset);
        PLGFlowTrigger.addKind(builder, KINDOffset);
        PLGFlowTrigger.addSource(builder, SOURCEOffset);
        PLGFlowTrigger.addDefaultIntervalMs(builder, DEFAULT_INTERVAL_MS);
        PLGFlowTrigger.addHttpPath(builder, HTTP_PATHOffset);
        return PLGFlowTrigger.endPLGFlowTrigger(builder);
    }
    unpack() {
        return new PLGFlowTriggerT(this.TRIGGER_ID(), this.KIND(), this.SOURCE(), this.DEFAULT_INTERVAL_MS(), this.HTTP_PATH());
    }
    unpackTo(_o) {
        _o.TRIGGER_ID = this.TRIGGER_ID();
        _o.KIND = this.KIND();
        _o.SOURCE = this.SOURCE();
        _o.DEFAULT_INTERVAL_MS = this.DEFAULT_INTERVAL_MS();
        _o.HTTP_PATH = this.HTTP_PATH();
    }
}
export class PLGFlowTriggerT {
    constructor(TRIGGER_ID = null, KIND = null, SOURCE = null, DEFAULT_INTERVAL_MS = BigInt('0'), HTTP_PATH = null) {
        this.TRIGGER_ID = TRIGGER_ID;
        this.KIND = KIND;
        this.SOURCE = SOURCE;
        this.DEFAULT_INTERVAL_MS = DEFAULT_INTERVAL_MS;
        this.HTTP_PATH = HTTP_PATH;
    }
    pack(builder) {
        const TRIGGER_ID = (this.TRIGGER_ID !== null ? builder.createString(this.TRIGGER_ID) : 0);
        const KIND = (this.KIND !== null ? builder.createString(this.KIND) : 0);
        const SOURCE = (this.SOURCE !== null ? builder.createString(this.SOURCE) : 0);
        const HTTP_PATH = (this.HTTP_PATH !== null ? builder.createString(this.HTTP_PATH) : 0);
        return PLGFlowTrigger.createPLGFlowTrigger(builder, TRIGGER_ID, KIND, SOURCE, this.DEFAULT_INTERVAL_MS, HTTP_PATH);
    }
}
