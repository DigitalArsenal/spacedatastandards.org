// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KRF } from './KRF.js';
import { licensingConfigMessageType } from './licensingConfigMessageType.js';
import { licensingConfigRole } from './licensingConfigRole.js';
/**
 * Licensing Configuration Frame
 */
export class LCF {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsLCF(bb, obj) {
        return (obj || new LCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsLCF(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new LCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$LCF');
    }
    /**
     * Distinguishes configuration inputs from runtime status outputs.
     */
    MESSAGE_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : licensingConfigMessageType.Configure;
    }
    /**
     * Indicates whether the configuration/status applies to server/provider or requester/client behavior.
     */
    ROLE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : licensingConfigRole.Provider;
    }
    PROVIDER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Host-managed long-lived signing key used for grant signatures.
     */
    PROVIDER_SIGNING_KEY(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new KRF()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Host-managed wrapping key used to protect per-publication content keys for requesters.
     */
    PROVIDER_WRAPPING_KEY(obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new KRF()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Optional requester signing key reference for requester-hosted flows.
     */
    REQUESTER_SIGNING_KEY(obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new KRF()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Logical active publication/grant key version.
     */
    ACTIVE_KEY_VERSION() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Runtime expiry in unix milliseconds, or 0 if unbounded.
     */
    EXPIRES_AT() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Maximum accepted requester/provider clock skew in milliseconds.
     */
    MAX_CLOCK_SKEW_MS() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Challenge time-to-live in milliseconds.
     */
    CHALLENGE_TTL_MS() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Optional capability token bytes returned with successful grants.
     */
    CAPABILITY_TOKEN(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    capabilityTokenLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    capabilityTokenArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Whether the runtime has been initialized successfully.
     */
    INITIALIZED() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Whether the runtime needs issuer key rotation or refreshed configuration.
     */
    NEEDS_ROTATION() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    STATUS_CODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    STATUS_MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startLCF(builder) {
        builder.startObject(15);
    }
    static addMessageType(builder, MESSAGE_TYPE) {
        builder.addFieldInt8(0, MESSAGE_TYPE, licensingConfigMessageType.Configure);
    }
    static addRole(builder, ROLE) {
        builder.addFieldInt8(1, ROLE, licensingConfigRole.Provider);
    }
    static addProviderPeerId(builder, PROVIDER_PEER_IDOffset) {
        builder.addFieldOffset(2, PROVIDER_PEER_IDOffset, 0);
    }
    static addProviderSigningKey(builder, PROVIDER_SIGNING_KEYOffset) {
        builder.addFieldOffset(3, PROVIDER_SIGNING_KEYOffset, 0);
    }
    static addProviderWrappingKey(builder, PROVIDER_WRAPPING_KEYOffset) {
        builder.addFieldOffset(4, PROVIDER_WRAPPING_KEYOffset, 0);
    }
    static addRequesterSigningKey(builder, REQUESTER_SIGNING_KEYOffset) {
        builder.addFieldOffset(5, REQUESTER_SIGNING_KEYOffset, 0);
    }
    static addActiveKeyVersion(builder, ACTIVE_KEY_VERSION) {
        builder.addFieldInt32(6, ACTIVE_KEY_VERSION, 0);
    }
    static addExpiresAt(builder, EXPIRES_AT) {
        builder.addFieldInt64(7, EXPIRES_AT, BigInt('0'));
    }
    static addMaxClockSkewMs(builder, MAX_CLOCK_SKEW_MS) {
        builder.addFieldInt64(8, MAX_CLOCK_SKEW_MS, BigInt('0'));
    }
    static addChallengeTtlMs(builder, CHALLENGE_TTL_MS) {
        builder.addFieldInt64(9, CHALLENGE_TTL_MS, BigInt('0'));
    }
    static addCapabilityToken(builder, CAPABILITY_TOKENOffset) {
        builder.addFieldOffset(10, CAPABILITY_TOKENOffset, 0);
    }
    static createCapabilityTokenVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startCapabilityTokenVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addInitialized(builder, INITIALIZED) {
        builder.addFieldInt8(11, +INITIALIZED, +false);
    }
    static addNeedsRotation(builder, NEEDS_ROTATION) {
        builder.addFieldInt8(12, +NEEDS_ROTATION, +false);
    }
    static addStatusCode(builder, STATUS_CODEOffset) {
        builder.addFieldOffset(13, STATUS_CODEOffset, 0);
    }
    static addStatusMessage(builder, STATUS_MESSAGEOffset) {
        builder.addFieldOffset(14, STATUS_MESSAGEOffset, 0);
    }
    static endLCF(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishLCFBuffer(builder, offset) {
        builder.finish(offset, '$LCF');
    }
    static finishSizePrefixedLCFBuffer(builder, offset) {
        builder.finish(offset, '$LCF', true);
    }
    unpack() {
        return new LCFT(this.MESSAGE_TYPE(), this.ROLE(), this.PROVIDER_PEER_ID(), (this.PROVIDER_SIGNING_KEY() !== null ? this.PROVIDER_SIGNING_KEY().unpack() : null), (this.PROVIDER_WRAPPING_KEY() !== null ? this.PROVIDER_WRAPPING_KEY().unpack() : null), (this.REQUESTER_SIGNING_KEY() !== null ? this.REQUESTER_SIGNING_KEY().unpack() : null), this.ACTIVE_KEY_VERSION(), this.EXPIRES_AT(), this.MAX_CLOCK_SKEW_MS(), this.CHALLENGE_TTL_MS(), this.bb.createScalarList(this.CAPABILITY_TOKEN.bind(this), this.capabilityTokenLength()), this.INITIALIZED(), this.NEEDS_ROTATION(), this.STATUS_CODE(), this.STATUS_MESSAGE());
    }
    unpackTo(_o) {
        _o.MESSAGE_TYPE = this.MESSAGE_TYPE();
        _o.ROLE = this.ROLE();
        _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
        _o.PROVIDER_SIGNING_KEY = (this.PROVIDER_SIGNING_KEY() !== null ? this.PROVIDER_SIGNING_KEY().unpack() : null);
        _o.PROVIDER_WRAPPING_KEY = (this.PROVIDER_WRAPPING_KEY() !== null ? this.PROVIDER_WRAPPING_KEY().unpack() : null);
        _o.REQUESTER_SIGNING_KEY = (this.REQUESTER_SIGNING_KEY() !== null ? this.REQUESTER_SIGNING_KEY().unpack() : null);
        _o.ACTIVE_KEY_VERSION = this.ACTIVE_KEY_VERSION();
        _o.EXPIRES_AT = this.EXPIRES_AT();
        _o.MAX_CLOCK_SKEW_MS = this.MAX_CLOCK_SKEW_MS();
        _o.CHALLENGE_TTL_MS = this.CHALLENGE_TTL_MS();
        _o.CAPABILITY_TOKEN = this.bb.createScalarList(this.CAPABILITY_TOKEN.bind(this), this.capabilityTokenLength());
        _o.INITIALIZED = this.INITIALIZED();
        _o.NEEDS_ROTATION = this.NEEDS_ROTATION();
        _o.STATUS_CODE = this.STATUS_CODE();
        _o.STATUS_MESSAGE = this.STATUS_MESSAGE();
    }
}
export class LCFT {
    constructor(MESSAGE_TYPE = licensingConfigMessageType.Configure, ROLE = licensingConfigRole.Provider, PROVIDER_PEER_ID = null, PROVIDER_SIGNING_KEY = null, PROVIDER_WRAPPING_KEY = null, REQUESTER_SIGNING_KEY = null, ACTIVE_KEY_VERSION = 0, EXPIRES_AT = BigInt('0'), MAX_CLOCK_SKEW_MS = BigInt('0'), CHALLENGE_TTL_MS = BigInt('0'), CAPABILITY_TOKEN = [], INITIALIZED = false, NEEDS_ROTATION = false, STATUS_CODE = null, STATUS_MESSAGE = null) {
        this.MESSAGE_TYPE = MESSAGE_TYPE;
        this.ROLE = ROLE;
        this.PROVIDER_PEER_ID = PROVIDER_PEER_ID;
        this.PROVIDER_SIGNING_KEY = PROVIDER_SIGNING_KEY;
        this.PROVIDER_WRAPPING_KEY = PROVIDER_WRAPPING_KEY;
        this.REQUESTER_SIGNING_KEY = REQUESTER_SIGNING_KEY;
        this.ACTIVE_KEY_VERSION = ACTIVE_KEY_VERSION;
        this.EXPIRES_AT = EXPIRES_AT;
        this.MAX_CLOCK_SKEW_MS = MAX_CLOCK_SKEW_MS;
        this.CHALLENGE_TTL_MS = CHALLENGE_TTL_MS;
        this.CAPABILITY_TOKEN = CAPABILITY_TOKEN;
        this.INITIALIZED = INITIALIZED;
        this.NEEDS_ROTATION = NEEDS_ROTATION;
        this.STATUS_CODE = STATUS_CODE;
        this.STATUS_MESSAGE = STATUS_MESSAGE;
    }
    pack(builder) {
        const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID) : 0);
        const PROVIDER_SIGNING_KEY = (this.PROVIDER_SIGNING_KEY !== null ? this.PROVIDER_SIGNING_KEY.pack(builder) : 0);
        const PROVIDER_WRAPPING_KEY = (this.PROVIDER_WRAPPING_KEY !== null ? this.PROVIDER_WRAPPING_KEY.pack(builder) : 0);
        const REQUESTER_SIGNING_KEY = (this.REQUESTER_SIGNING_KEY !== null ? this.REQUESTER_SIGNING_KEY.pack(builder) : 0);
        const CAPABILITY_TOKEN = LCF.createCapabilityTokenVector(builder, this.CAPABILITY_TOKEN);
        const STATUS_CODE = (this.STATUS_CODE !== null ? builder.createString(this.STATUS_CODE) : 0);
        const STATUS_MESSAGE = (this.STATUS_MESSAGE !== null ? builder.createString(this.STATUS_MESSAGE) : 0);
        LCF.startLCF(builder);
        LCF.addMessageType(builder, this.MESSAGE_TYPE);
        LCF.addRole(builder, this.ROLE);
        LCF.addProviderPeerId(builder, PROVIDER_PEER_ID);
        LCF.addProviderSigningKey(builder, PROVIDER_SIGNING_KEY);
        LCF.addProviderWrappingKey(builder, PROVIDER_WRAPPING_KEY);
        LCF.addRequesterSigningKey(builder, REQUESTER_SIGNING_KEY);
        LCF.addActiveKeyVersion(builder, this.ACTIVE_KEY_VERSION);
        LCF.addExpiresAt(builder, this.EXPIRES_AT);
        LCF.addMaxClockSkewMs(builder, this.MAX_CLOCK_SKEW_MS);
        LCF.addChallengeTtlMs(builder, this.CHALLENGE_TTL_MS);
        LCF.addCapabilityToken(builder, CAPABILITY_TOKEN);
        LCF.addInitialized(builder, this.INITIALIZED);
        LCF.addNeedsRotation(builder, this.NEEDS_ROTATION);
        LCF.addStatusCode(builder, STATUS_CODE);
        LCF.addStatusMessage(builder, STATUS_MESSAGE);
        return LCF.endLCF(builder);
    }
}
