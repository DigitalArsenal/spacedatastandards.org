// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Radar sensor configuration.
 */
export class SDRRadarConfig {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSDRRadarConfig(bb, obj) {
        return (obj || new SDRRadarConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSDRRadarConfig(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SDRRadarConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Operating frequency (Hz).
     */
    FREQUENCY() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Transmit power (W).
     */
    POWER() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Antenna gain (dB).
     */
    GAIN() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Receiver bandwidth (Hz).
     */
    BANDWIDTH() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Noise figure (dB).
     */
    NOISE_FIGURE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * System losses (dB).
     */
    SYSTEM_LOSS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Pulse repetition frequency (Hz).
     */
    PRF() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Pulse width (seconds).
     */
    PULSE_WIDTH() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Detection threshold SNR (dB).
     */
    DETECTION_THRESHOLD() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Minimum detection range (meters).
     */
    MIN_RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum detection range (meters).
     */
    MAX_RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startSDRRadarConfig(builder) {
        builder.startObject(11);
    }
    static addFrequency(builder, FREQUENCY) {
        builder.addFieldFloat64(0, FREQUENCY, 0.0);
    }
    static addPower(builder, POWER) {
        builder.addFieldFloat64(1, POWER, 0.0);
    }
    static addGain(builder, GAIN) {
        builder.addFieldFloat64(2, GAIN, 0.0);
    }
    static addBandwidth(builder, BANDWIDTH) {
        builder.addFieldFloat64(3, BANDWIDTH, 0.0);
    }
    static addNoiseFigure(builder, NOISE_FIGURE) {
        builder.addFieldFloat64(4, NOISE_FIGURE, 0.0);
    }
    static addSystemLoss(builder, SYSTEM_LOSS) {
        builder.addFieldFloat64(5, SYSTEM_LOSS, 0.0);
    }
    static addPrf(builder, PRF) {
        builder.addFieldFloat64(6, PRF, 0.0);
    }
    static addPulseWidth(builder, PULSE_WIDTH) {
        builder.addFieldFloat64(7, PULSE_WIDTH, 0.0);
    }
    static addDetectionThreshold(builder, DETECTION_THRESHOLD) {
        builder.addFieldFloat64(8, DETECTION_THRESHOLD, 0.0);
    }
    static addMinRange(builder, MIN_RANGE) {
        builder.addFieldFloat64(9, MIN_RANGE, 0.0);
    }
    static addMaxRange(builder, MAX_RANGE) {
        builder.addFieldFloat64(10, MAX_RANGE, 0.0);
    }
    static endSDRRadarConfig(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSDRRadarConfig(builder, FREQUENCY, POWER, GAIN, BANDWIDTH, NOISE_FIGURE, SYSTEM_LOSS, PRF, PULSE_WIDTH, DETECTION_THRESHOLD, MIN_RANGE, MAX_RANGE) {
        SDRRadarConfig.startSDRRadarConfig(builder);
        SDRRadarConfig.addFrequency(builder, FREQUENCY);
        SDRRadarConfig.addPower(builder, POWER);
        SDRRadarConfig.addGain(builder, GAIN);
        SDRRadarConfig.addBandwidth(builder, BANDWIDTH);
        SDRRadarConfig.addNoiseFigure(builder, NOISE_FIGURE);
        SDRRadarConfig.addSystemLoss(builder, SYSTEM_LOSS);
        SDRRadarConfig.addPrf(builder, PRF);
        SDRRadarConfig.addPulseWidth(builder, PULSE_WIDTH);
        SDRRadarConfig.addDetectionThreshold(builder, DETECTION_THRESHOLD);
        SDRRadarConfig.addMinRange(builder, MIN_RANGE);
        SDRRadarConfig.addMaxRange(builder, MAX_RANGE);
        return SDRRadarConfig.endSDRRadarConfig(builder);
    }
    unpack() {
        return new SDRRadarConfigT(this.FREQUENCY(), this.POWER(), this.GAIN(), this.BANDWIDTH(), this.NOISE_FIGURE(), this.SYSTEM_LOSS(), this.PRF(), this.PULSE_WIDTH(), this.DETECTION_THRESHOLD(), this.MIN_RANGE(), this.MAX_RANGE());
    }
    unpackTo(_o) {
        _o.FREQUENCY = this.FREQUENCY();
        _o.POWER = this.POWER();
        _o.GAIN = this.GAIN();
        _o.BANDWIDTH = this.BANDWIDTH();
        _o.NOISE_FIGURE = this.NOISE_FIGURE();
        _o.SYSTEM_LOSS = this.SYSTEM_LOSS();
        _o.PRF = this.PRF();
        _o.PULSE_WIDTH = this.PULSE_WIDTH();
        _o.DETECTION_THRESHOLD = this.DETECTION_THRESHOLD();
        _o.MIN_RANGE = this.MIN_RANGE();
        _o.MAX_RANGE = this.MAX_RANGE();
    }
}
export class SDRRadarConfigT {
    constructor(FREQUENCY = 0.0, POWER = 0.0, GAIN = 0.0, BANDWIDTH = 0.0, NOISE_FIGURE = 0.0, SYSTEM_LOSS = 0.0, PRF = 0.0, PULSE_WIDTH = 0.0, DETECTION_THRESHOLD = 0.0, MIN_RANGE = 0.0, MAX_RANGE = 0.0) {
        this.FREQUENCY = FREQUENCY;
        this.POWER = POWER;
        this.GAIN = GAIN;
        this.BANDWIDTH = BANDWIDTH;
        this.NOISE_FIGURE = NOISE_FIGURE;
        this.SYSTEM_LOSS = SYSTEM_LOSS;
        this.PRF = PRF;
        this.PULSE_WIDTH = PULSE_WIDTH;
        this.DETECTION_THRESHOLD = DETECTION_THRESHOLD;
        this.MIN_RANGE = MIN_RANGE;
        this.MAX_RANGE = MAX_RANGE;
    }
    pack(builder) {
        return SDRRadarConfig.createSDRRadarConfig(builder, this.FREQUENCY, this.POWER, this.GAIN, this.BANDWIDTH, this.NOISE_FIGURE, this.SYSTEM_LOSS, this.PRF, this.PULSE_WIDTH, this.DETECTION_THRESHOLD, this.MIN_RANGE, this.MAX_RANGE);
    }
}
