// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { beamContourPoint } from './beamContourPoint.js';
/**
 * Beam Contour (iso-gain boundary)
 */
export class beamContour {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsbeamContour(bb, obj) {
        return (obj || new beamContour()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsbeamContour(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new beamContour()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    CONTOUR_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Gain level in dBi
     */
    GAIN_LEVEL() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Contour boundary points
     */
    POINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new beamContourPoint()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    pointsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startbeamContour(builder) {
        builder.startObject(3);
    }
    static addContourId(builder, CONTOUR_IDOffset) {
        builder.addFieldOffset(0, CONTOUR_IDOffset, 0);
    }
    static addGainLevel(builder, GAIN_LEVEL) {
        builder.addFieldFloat64(1, GAIN_LEVEL, 0.0);
    }
    static addPoints(builder, POINTSOffset) {
        builder.addFieldOffset(2, POINTSOffset, 0);
    }
    static createPointsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPointsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endbeamContour(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createbeamContour(builder, CONTOUR_IDOffset, GAIN_LEVEL, POINTSOffset) {
        beamContour.startbeamContour(builder);
        beamContour.addContourId(builder, CONTOUR_IDOffset);
        beamContour.addGainLevel(builder, GAIN_LEVEL);
        beamContour.addPoints(builder, POINTSOffset);
        return beamContour.endbeamContour(builder);
    }
    unpack() {
        return new beamContourT(this.CONTOUR_ID(), this.GAIN_LEVEL(), this.bb.createObjList(this.POINTS.bind(this), this.pointsLength()));
    }
    unpackTo(_o) {
        _o.CONTOUR_ID = this.CONTOUR_ID();
        _o.GAIN_LEVEL = this.GAIN_LEVEL();
        _o.POINTS = this.bb.createObjList(this.POINTS.bind(this), this.pointsLength());
    }
}
export class beamContourT {
    constructor(CONTOUR_ID = null, GAIN_LEVEL = 0.0, POINTS = []) {
        this.CONTOUR_ID = CONTOUR_ID;
        this.GAIN_LEVEL = GAIN_LEVEL;
        this.POINTS = POINTS;
    }
    pack(builder) {
        const CONTOUR_ID = (this.CONTOUR_ID !== null ? builder.createString(this.CONTOUR_ID) : 0);
        const POINTS = beamContour.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));
        return beamContour.createbeamContour(builder, CONTOUR_ID, this.GAIN_LEVEL, POINTS);
    }
}
