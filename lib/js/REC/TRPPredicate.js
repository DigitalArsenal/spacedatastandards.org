// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { TRPAsset } from './TRPAsset.js';
import { trpPredicateKind } from './trpPredicateKind.js';
/**
 * One independently measurable predicate in a trust rule policy.
 */
export class TRPPredicate {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRPPredicate(bb, obj) {
        return (obj || new TRPPredicate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRPPredicate(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRPPredicate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    PREDICATE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Predicate operation to evaluate.
     */
    KIND() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : trpPredicateKind.MinValueLocked;
    }
    /**
     * Minimum qualifying value expressed as a uint64 in the smallest unit.
     */
    MIN_VALUE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    VALUE_CURRENCY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Minimum continuous holding duration in seconds.
     */
    MIN_HELD_SECONDS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Assets admitted by this predicate.
     */
    ASSETS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new TRPAsset()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    assetsLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * X, the number of named trusters that must match.
     */
    REQUIRED_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TRUSTER_IDS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    trusterIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Minimum accepted `$TRE.WEIGHT` for a matching trust edge.
     */
    MIN_EDGE_WEIGHT() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startTRPPredicate(builder) {
        builder.startObject(9);
    }
    static addPredicateId(builder, PREDICATE_IDOffset) {
        builder.addFieldOffset(0, PREDICATE_IDOffset, 0);
    }
    static addKind(builder, KIND) {
        builder.addFieldInt8(1, KIND, trpPredicateKind.MinValueLocked);
    }
    static addMinValue(builder, MIN_VALUE) {
        builder.addFieldInt64(2, MIN_VALUE, BigInt('0'));
    }
    static addValueCurrency(builder, VALUE_CURRENCYOffset) {
        builder.addFieldOffset(3, VALUE_CURRENCYOffset, 0);
    }
    static addMinHeldSeconds(builder, MIN_HELD_SECONDS) {
        builder.addFieldInt64(4, MIN_HELD_SECONDS, BigInt('0'));
    }
    static addAssets(builder, ASSETSOffset) {
        builder.addFieldOffset(5, ASSETSOffset, 0);
    }
    static createAssetsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAssetsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addRequiredCount(builder, REQUIRED_COUNT) {
        builder.addFieldInt32(6, REQUIRED_COUNT, 0);
    }
    static addTrusterIds(builder, TRUSTER_IDSOffset) {
        builder.addFieldOffset(7, TRUSTER_IDSOffset, 0);
    }
    static createTrusterIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTrusterIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addMinEdgeWeight(builder, MIN_EDGE_WEIGHT) {
        builder.addFieldFloat64(8, MIN_EDGE_WEIGHT, 0.0);
    }
    static endTRPPredicate(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createTRPPredicate(builder, PREDICATE_IDOffset, KIND, MIN_VALUE, VALUE_CURRENCYOffset, MIN_HELD_SECONDS, ASSETSOffset, REQUIRED_COUNT, TRUSTER_IDSOffset, MIN_EDGE_WEIGHT) {
        TRPPredicate.startTRPPredicate(builder);
        TRPPredicate.addPredicateId(builder, PREDICATE_IDOffset);
        TRPPredicate.addKind(builder, KIND);
        TRPPredicate.addMinValue(builder, MIN_VALUE);
        TRPPredicate.addValueCurrency(builder, VALUE_CURRENCYOffset);
        TRPPredicate.addMinHeldSeconds(builder, MIN_HELD_SECONDS);
        TRPPredicate.addAssets(builder, ASSETSOffset);
        TRPPredicate.addRequiredCount(builder, REQUIRED_COUNT);
        TRPPredicate.addTrusterIds(builder, TRUSTER_IDSOffset);
        TRPPredicate.addMinEdgeWeight(builder, MIN_EDGE_WEIGHT);
        return TRPPredicate.endTRPPredicate(builder);
    }
    unpack() {
        return new TRPPredicateT(this.PREDICATE_ID(), this.KIND(), this.MIN_VALUE(), this.VALUE_CURRENCY(), this.MIN_HELD_SECONDS(), this.bb.createObjList(this.ASSETS.bind(this), this.assetsLength()), this.REQUIRED_COUNT(), this.bb.createScalarList(this.TRUSTER_IDS.bind(this), this.trusterIdsLength()), this.MIN_EDGE_WEIGHT());
    }
    unpackTo(_o) {
        _o.PREDICATE_ID = this.PREDICATE_ID();
        _o.KIND = this.KIND();
        _o.MIN_VALUE = this.MIN_VALUE();
        _o.VALUE_CURRENCY = this.VALUE_CURRENCY();
        _o.MIN_HELD_SECONDS = this.MIN_HELD_SECONDS();
        _o.ASSETS = this.bb.createObjList(this.ASSETS.bind(this), this.assetsLength());
        _o.REQUIRED_COUNT = this.REQUIRED_COUNT();
        _o.TRUSTER_IDS = this.bb.createScalarList(this.TRUSTER_IDS.bind(this), this.trusterIdsLength());
        _o.MIN_EDGE_WEIGHT = this.MIN_EDGE_WEIGHT();
    }
}
export class TRPPredicateT {
    constructor(PREDICATE_ID = null, KIND = trpPredicateKind.MinValueLocked, MIN_VALUE = BigInt('0'), VALUE_CURRENCY = null, MIN_HELD_SECONDS = BigInt('0'), ASSETS = [], REQUIRED_COUNT = 0, TRUSTER_IDS = [], MIN_EDGE_WEIGHT = 0.0) {
        this.PREDICATE_ID = PREDICATE_ID;
        this.KIND = KIND;
        this.MIN_VALUE = MIN_VALUE;
        this.VALUE_CURRENCY = VALUE_CURRENCY;
        this.MIN_HELD_SECONDS = MIN_HELD_SECONDS;
        this.ASSETS = ASSETS;
        this.REQUIRED_COUNT = REQUIRED_COUNT;
        this.TRUSTER_IDS = TRUSTER_IDS;
        this.MIN_EDGE_WEIGHT = MIN_EDGE_WEIGHT;
    }
    pack(builder) {
        const PREDICATE_ID = (this.PREDICATE_ID !== null ? builder.createString(this.PREDICATE_ID) : 0);
        const VALUE_CURRENCY = (this.VALUE_CURRENCY !== null ? builder.createString(this.VALUE_CURRENCY) : 0);
        const ASSETS = TRPPredicate.createAssetsVector(builder, builder.createObjectOffsetList(this.ASSETS));
        const TRUSTER_IDS = TRPPredicate.createTrusterIdsVector(builder, builder.createObjectOffsetList(this.TRUSTER_IDS));
        return TRPPredicate.createTRPPredicate(builder, PREDICATE_ID, this.KIND, this.MIN_VALUE, VALUE_CURRENCY, this.MIN_HELD_SECONDS, ASSETS, this.REQUIRED_COUNT, TRUSTER_IDS, this.MIN_EDGE_WEIGHT);
    }
}
