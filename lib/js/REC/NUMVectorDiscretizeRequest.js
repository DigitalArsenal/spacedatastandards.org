// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { numDiscretizeRoundDirection } from './numDiscretizeRoundDirection.js';
/**
 * Request for component-wise vector discretization.
 */
export class NUMVectorDiscretizeRequest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNUMVectorDiscretizeRequest(bb, obj) {
        return (obj || new NUMVectorDiscretizeRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNUMVectorDiscretizeRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NUMVectorDiscretizeRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Input vector to quantize.
     */
    STATE(index) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    stateLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    stateArray() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Per-component least-significant-bit size. Length must match STATE.
     */
    LSB(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    lsbLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    lsbArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Rounding direction used before the original sign is restored.
     */
    ROUND_DIRECTION() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : numDiscretizeRoundDirection.TO_ZERO;
    }
    /**
     * When true, PREVIOUS_ERROR is added to STATE before discretization.
     */
    CARRY_ERROR() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Optional prior discretization error vector from an earlier result.
     */
    PREVIOUS_ERROR(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    previousErrorLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    previousErrorArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startNUMVectorDiscretizeRequest(builder) {
        builder.startObject(6);
    }
    static addState(builder, STATEOffset) {
        builder.addFieldOffset(0, STATEOffset, 0);
    }
    static createStateVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startStateVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addLsb(builder, LSBOffset) {
        builder.addFieldOffset(1, LSBOffset, 0);
    }
    static createLsbVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startLsbVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addRoundDirection(builder, ROUND_DIRECTION) {
        builder.addFieldInt8(2, ROUND_DIRECTION, numDiscretizeRoundDirection.TO_ZERO);
    }
    static addCarryError(builder, CARRY_ERROR) {
        builder.addFieldInt8(3, +CARRY_ERROR, +false);
    }
    static addPreviousError(builder, PREVIOUS_ERROROffset) {
        builder.addFieldOffset(4, PREVIOUS_ERROROffset, 0);
    }
    static createPreviousErrorVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startPreviousErrorVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(5, TRACE_IDOffset, 0);
    }
    static endNUMVectorDiscretizeRequest(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNUMVectorDiscretizeRequest(builder, STATEOffset, LSBOffset, ROUND_DIRECTION, CARRY_ERROR, PREVIOUS_ERROROffset, TRACE_IDOffset) {
        NUMVectorDiscretizeRequest.startNUMVectorDiscretizeRequest(builder);
        NUMVectorDiscretizeRequest.addState(builder, STATEOffset);
        NUMVectorDiscretizeRequest.addLsb(builder, LSBOffset);
        NUMVectorDiscretizeRequest.addRoundDirection(builder, ROUND_DIRECTION);
        NUMVectorDiscretizeRequest.addCarryError(builder, CARRY_ERROR);
        NUMVectorDiscretizeRequest.addPreviousError(builder, PREVIOUS_ERROROffset);
        NUMVectorDiscretizeRequest.addTraceId(builder, TRACE_IDOffset);
        return NUMVectorDiscretizeRequest.endNUMVectorDiscretizeRequest(builder);
    }
    unpack() {
        return new NUMVectorDiscretizeRequestT(this.bb.createScalarList(this.STATE.bind(this), this.stateLength()), this.bb.createScalarList(this.LSB.bind(this), this.lsbLength()), this.ROUND_DIRECTION(), this.CARRY_ERROR(), this.bb.createScalarList(this.PREVIOUS_ERROR.bind(this), this.previousErrorLength()), this.TRACE_ID());
    }
    unpackTo(_o) {
        _o.STATE = this.bb.createScalarList(this.STATE.bind(this), this.stateLength());
        _o.LSB = this.bb.createScalarList(this.LSB.bind(this), this.lsbLength());
        _o.ROUND_DIRECTION = this.ROUND_DIRECTION();
        _o.CARRY_ERROR = this.CARRY_ERROR();
        _o.PREVIOUS_ERROR = this.bb.createScalarList(this.PREVIOUS_ERROR.bind(this), this.previousErrorLength());
        _o.TRACE_ID = this.TRACE_ID();
    }
}
export class NUMVectorDiscretizeRequestT {
    constructor(STATE = [], LSB = [], ROUND_DIRECTION = numDiscretizeRoundDirection.TO_ZERO, CARRY_ERROR = false, PREVIOUS_ERROR = [], TRACE_ID = null) {
        this.STATE = STATE;
        this.LSB = LSB;
        this.ROUND_DIRECTION = ROUND_DIRECTION;
        this.CARRY_ERROR = CARRY_ERROR;
        this.PREVIOUS_ERROR = PREVIOUS_ERROR;
        this.TRACE_ID = TRACE_ID;
    }
    pack(builder) {
        const STATE = NUMVectorDiscretizeRequest.createStateVector(builder, this.STATE);
        const LSB = NUMVectorDiscretizeRequest.createLsbVector(builder, this.LSB);
        const PREVIOUS_ERROR = NUMVectorDiscretizeRequest.createPreviousErrorVector(builder, this.PREVIOUS_ERROR);
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        return NUMVectorDiscretizeRequest.createNUMVectorDiscretizeRequest(builder, STATE, LSB, this.ROUND_DIRECTION, this.CARRY_ERROR, PREVIOUS_ERROR, TRACE_ID);
    }
}
