// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * RGBA color
 */
export class CZMColor {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCZMColor(bb, obj) {
        return (obj || new CZMColor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCZMColor(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CZMColor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Red component (0-255)
     */
    RED() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Green component (0-255)
     */
    GREEN() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Blue component (0-255)
     */
    BLUE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Alpha component (0-255)
     */
    ALPHA() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    static startCZMColor(builder) {
        builder.startObject(4);
    }
    static addRed(builder, RED) {
        builder.addFieldInt8(0, RED, 0);
    }
    static addGreen(builder, GREEN) {
        builder.addFieldInt8(1, GREEN, 0);
    }
    static addBlue(builder, BLUE) {
        builder.addFieldInt8(2, BLUE, 0);
    }
    static addAlpha(builder, ALPHA) {
        builder.addFieldInt8(3, ALPHA, 0);
    }
    static endCZMColor(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createCZMColor(builder, RED, GREEN, BLUE, ALPHA) {
        CZMColor.startCZMColor(builder);
        CZMColor.addRed(builder, RED);
        CZMColor.addGreen(builder, GREEN);
        CZMColor.addBlue(builder, BLUE);
        CZMColor.addAlpha(builder, ALPHA);
        return CZMColor.endCZMColor(builder);
    }
    unpack() {
        return new CZMColorT(this.RED(), this.GREEN(), this.BLUE(), this.ALPHA());
    }
    unpackTo(_o) {
        _o.RED = this.RED();
        _o.GREEN = this.GREEN();
        _o.BLUE = this.BLUE();
        _o.ALPHA = this.ALPHA();
    }
}
export class CZMColorT {
    constructor(RED = 0, GREEN = 0, BLUE = 0, ALPHA = 0) {
        this.RED = RED;
        this.GREEN = GREEN;
        this.BLUE = BLUE;
        this.ALPHA = ALPHA;
    }
    pack(builder) {
        return CZMColor.createCZMColor(builder, this.RED, this.GREEN, this.BLUE, this.ALPHA);
    }
}
