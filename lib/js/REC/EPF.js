// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { EPFContributor } from './EPFContributor.js';
import { EPFVerdict } from './EPFVerdict.js';
import { RFLProvenance } from './RFLProvenance.js';
import { epfLimitCategory } from './epfLimitCategory.js';
/**
 * Aggregate Interference and Flux-Density Compliance
 */
export class EPF {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsEPF(bb, obj) {
        return (obj || new EPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsEPF(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new EPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$EPF');
    }
    EPF_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SCENARIO_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RFL_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ACI_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    VICTIM_ENDPOINT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    LIMIT_KIND() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : epfLimitCategory.UNSPECIFIED;
    }
    LIMIT_MODEL_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    VALUE_UNITS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SAMPLE_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    SAMPLE_EPOCHS(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    sampleEpochsLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sampleEpochsArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    SAMPLE_VALUES(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    sampleValuesLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sampleValuesArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    SAMPLE_LIMIT_VALUES(index) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    sampleLimitValuesLength() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sampleLimitValuesArray() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    /**
     * Positive means the sample complies with its limit.
     */
    SAMPLE_MARGIN_DB(index) {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    sampleMarginDbLength() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sampleMarginDbArray() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    SAMPLE_EXCEEDS_LIMIT(index) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? !!this.bb.readInt8(this.bb.__vector(this.bb_pos + offset) + index) : false;
    }
    sampleExceedsLimitLength() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sampleExceedsLimitArray() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return new Int8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    CONTRIBUTORS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? (obj || new EPFContributor()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    contributorsLength() {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Empirical distribution values and their percent-time exceedance. Arrays
     * are index-aligned and ordered as stated by provenance.
     */
    CDF_VALUES(index) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    cdfValuesLength() {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    cdfValuesArray() {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    CDF_PERCENT_TIME_EXCEEDED(index) {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    cdfPercentTimeExceededLength() {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    cdfPercentTimeExceededArray() {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    VERDICT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 40);
        return offset ? (obj || new EPFVerdict()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    PROVENANCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 42);
        return offset ? (obj || new RFLProvenance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    COMPUTED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 44);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    PRODUCER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    /**
     * Ed25519 signature over the size-prefixed FlatBuffer with both 64-byte
     * signature payloads zeroed while preserving their vectors and offsets.
     */
    SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 48);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 48);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 48);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    /**
     * Ed25519 signature over canonical JSON with IDL field order and
     * capitalization, no insignificant whitespace, and both signature fields
     * omitted.
     */
    CANONICAL_JSON_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 50);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    canonicalJsonSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 50);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    canonicalJsonSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 50);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    static startEPF(builder) {
        builder.startObject(24);
    }
    static addEpfId(builder, EPF_IDOffset) {
        builder.addFieldOffset(0, EPF_IDOffset, 0);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(1, NAMEOffset, 0);
    }
    static addScenarioId(builder, SCENARIO_IDOffset) {
        builder.addFieldOffset(2, SCENARIO_IDOffset, 0);
    }
    static addRflId(builder, RFL_IDOffset) {
        builder.addFieldOffset(3, RFL_IDOffset, 0);
    }
    static addAciId(builder, ACI_IDOffset) {
        builder.addFieldOffset(4, ACI_IDOffset, 0);
    }
    static addVictimEndpointId(builder, VICTIM_ENDPOINT_IDOffset) {
        builder.addFieldOffset(5, VICTIM_ENDPOINT_IDOffset, 0);
    }
    static addLimitKind(builder, LIMIT_KIND) {
        builder.addFieldInt8(6, LIMIT_KIND, epfLimitCategory.UNSPECIFIED);
    }
    static addLimitModelName(builder, LIMIT_MODEL_NAMEOffset) {
        builder.addFieldOffset(7, LIMIT_MODEL_NAMEOffset, 0);
    }
    static addValueUnits(builder, VALUE_UNITSOffset) {
        builder.addFieldOffset(8, VALUE_UNITSOffset, 0);
    }
    static addSampleCount(builder, SAMPLE_COUNT) {
        builder.addFieldInt32(9, SAMPLE_COUNT, 0);
    }
    static addSampleEpochs(builder, SAMPLE_EPOCHSOffset) {
        builder.addFieldOffset(10, SAMPLE_EPOCHSOffset, 0);
    }
    static createSampleEpochsVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startSampleEpochsVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addSampleValues(builder, SAMPLE_VALUESOffset) {
        builder.addFieldOffset(11, SAMPLE_VALUESOffset, 0);
    }
    static createSampleValuesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startSampleValuesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addSampleLimitValues(builder, SAMPLE_LIMIT_VALUESOffset) {
        builder.addFieldOffset(12, SAMPLE_LIMIT_VALUESOffset, 0);
    }
    static createSampleLimitValuesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startSampleLimitValuesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addSampleMarginDb(builder, SAMPLE_MARGIN_DBOffset) {
        builder.addFieldOffset(13, SAMPLE_MARGIN_DBOffset, 0);
    }
    static createSampleMarginDbVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startSampleMarginDbVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addSampleExceedsLimit(builder, SAMPLE_EXCEEDS_LIMITOffset) {
        builder.addFieldOffset(14, SAMPLE_EXCEEDS_LIMITOffset, 0);
    }
    static createSampleExceedsLimitVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(+data[i]);
        }
        return builder.endVector();
    }
    static startSampleExceedsLimitVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addContributors(builder, CONTRIBUTORSOffset) {
        builder.addFieldOffset(15, CONTRIBUTORSOffset, 0);
    }
    static createContributorsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startContributorsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCdfValues(builder, CDF_VALUESOffset) {
        builder.addFieldOffset(16, CDF_VALUESOffset, 0);
    }
    static createCdfValuesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCdfValuesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addCdfPercentTimeExceeded(builder, CDF_PERCENT_TIME_EXCEEDEDOffset) {
        builder.addFieldOffset(17, CDF_PERCENT_TIME_EXCEEDEDOffset, 0);
    }
    static createCdfPercentTimeExceededVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCdfPercentTimeExceededVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addVerdict(builder, VERDICTOffset) {
        builder.addFieldOffset(18, VERDICTOffset, 0);
    }
    static addProvenance(builder, PROVENANCEOffset) {
        builder.addFieldOffset(19, PROVENANCEOffset, 0);
    }
    static addComputedAt(builder, COMPUTED_AT) {
        builder.addFieldInt64(20, COMPUTED_AT, BigInt('0'));
    }
    static addProducerId(builder, PRODUCER_IDOffset) {
        builder.addFieldOffset(21, PRODUCER_IDOffset, 0);
    }
    static addSignature(builder, SIGNATUREOffset) {
        builder.addFieldOffset(22, SIGNATUREOffset, 0);
    }
    static createSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATUREOffset) {
        builder.addFieldOffset(23, CANONICAL_JSON_SIGNATUREOffset, 0);
    }
    static createCanonicalJsonSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startCanonicalJsonSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endEPF(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // EPF_ID
        builder.requiredField(offset, 20); // VALUE_UNITS
        builder.requiredField(offset, 24); // SAMPLE_EPOCHS
        builder.requiredField(offset, 26); // SAMPLE_VALUES
        builder.requiredField(offset, 28); // SAMPLE_LIMIT_VALUES
        builder.requiredField(offset, 30); // SAMPLE_MARGIN_DB
        builder.requiredField(offset, 32); // SAMPLE_EXCEEDS_LIMIT
        builder.requiredField(offset, 36); // CDF_VALUES
        builder.requiredField(offset, 38); // CDF_PERCENT_TIME_EXCEEDED
        builder.requiredField(offset, 40); // VERDICT
        builder.requiredField(offset, 42); // PROVENANCE
        builder.requiredField(offset, 46); // PRODUCER_ID
        builder.requiredField(offset, 48); // SIGNATURE
        builder.requiredField(offset, 50); // CANONICAL_JSON_SIGNATURE
        return offset;
    }
    static finishEPFBuffer(builder, offset) {
        builder.finish(offset, '$EPF');
    }
    static finishSizePrefixedEPFBuffer(builder, offset) {
        builder.finish(offset, '$EPF', true);
    }
    unpack() {
        return new EPFT(this.EPF_ID(), this.NAME(), this.SCENARIO_ID(), this.RFL_ID(), this.ACI_ID(), this.VICTIM_ENDPOINT_ID(), this.LIMIT_KIND(), this.LIMIT_MODEL_NAME(), this.VALUE_UNITS(), this.SAMPLE_COUNT(), this.bb.createScalarList(this.SAMPLE_EPOCHS.bind(this), this.sampleEpochsLength()), this.bb.createScalarList(this.SAMPLE_VALUES.bind(this), this.sampleValuesLength()), this.bb.createScalarList(this.SAMPLE_LIMIT_VALUES.bind(this), this.sampleLimitValuesLength()), this.bb.createScalarList(this.SAMPLE_MARGIN_DB.bind(this), this.sampleMarginDbLength()), this.bb.createScalarList(this.SAMPLE_EXCEEDS_LIMIT.bind(this), this.sampleExceedsLimitLength()), this.bb.createObjList(this.CONTRIBUTORS.bind(this), this.contributorsLength()), this.bb.createScalarList(this.CDF_VALUES.bind(this), this.cdfValuesLength()), this.bb.createScalarList(this.CDF_PERCENT_TIME_EXCEEDED.bind(this), this.cdfPercentTimeExceededLength()), (this.VERDICT() !== null ? this.VERDICT().unpack() : null), (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null), this.COMPUTED_AT(), this.PRODUCER_ID(), this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength()), this.bb.createScalarList(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength()));
    }
    unpackTo(_o) {
        _o.EPF_ID = this.EPF_ID();
        _o.NAME = this.NAME();
        _o.SCENARIO_ID = this.SCENARIO_ID();
        _o.RFL_ID = this.RFL_ID();
        _o.ACI_ID = this.ACI_ID();
        _o.VICTIM_ENDPOINT_ID = this.VICTIM_ENDPOINT_ID();
        _o.LIMIT_KIND = this.LIMIT_KIND();
        _o.LIMIT_MODEL_NAME = this.LIMIT_MODEL_NAME();
        _o.VALUE_UNITS = this.VALUE_UNITS();
        _o.SAMPLE_COUNT = this.SAMPLE_COUNT();
        _o.SAMPLE_EPOCHS = this.bb.createScalarList(this.SAMPLE_EPOCHS.bind(this), this.sampleEpochsLength());
        _o.SAMPLE_VALUES = this.bb.createScalarList(this.SAMPLE_VALUES.bind(this), this.sampleValuesLength());
        _o.SAMPLE_LIMIT_VALUES = this.bb.createScalarList(this.SAMPLE_LIMIT_VALUES.bind(this), this.sampleLimitValuesLength());
        _o.SAMPLE_MARGIN_DB = this.bb.createScalarList(this.SAMPLE_MARGIN_DB.bind(this), this.sampleMarginDbLength());
        _o.SAMPLE_EXCEEDS_LIMIT = this.bb.createScalarList(this.SAMPLE_EXCEEDS_LIMIT.bind(this), this.sampleExceedsLimitLength());
        _o.CONTRIBUTORS = this.bb.createObjList(this.CONTRIBUTORS.bind(this), this.contributorsLength());
        _o.CDF_VALUES = this.bb.createScalarList(this.CDF_VALUES.bind(this), this.cdfValuesLength());
        _o.CDF_PERCENT_TIME_EXCEEDED = this.bb.createScalarList(this.CDF_PERCENT_TIME_EXCEEDED.bind(this), this.cdfPercentTimeExceededLength());
        _o.VERDICT = (this.VERDICT() !== null ? this.VERDICT().unpack() : null);
        _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null);
        _o.COMPUTED_AT = this.COMPUTED_AT();
        _o.PRODUCER_ID = this.PRODUCER_ID();
        _o.SIGNATURE = this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength());
        _o.CANONICAL_JSON_SIGNATURE = this.bb.createScalarList(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength());
    }
}
export class EPFT {
    constructor(EPF_ID = null, NAME = null, SCENARIO_ID = null, RFL_ID = null, ACI_ID = null, VICTIM_ENDPOINT_ID = null, LIMIT_KIND = epfLimitCategory.UNSPECIFIED, LIMIT_MODEL_NAME = null, VALUE_UNITS = null, SAMPLE_COUNT = 0, SAMPLE_EPOCHS = [], SAMPLE_VALUES = [], SAMPLE_LIMIT_VALUES = [], SAMPLE_MARGIN_DB = [], SAMPLE_EXCEEDS_LIMIT = [], CONTRIBUTORS = [], CDF_VALUES = [], CDF_PERCENT_TIME_EXCEEDED = [], VERDICT = null, PROVENANCE = null, COMPUTED_AT = BigInt('0'), PRODUCER_ID = null, SIGNATURE = [], CANONICAL_JSON_SIGNATURE = []) {
        this.EPF_ID = EPF_ID;
        this.NAME = NAME;
        this.SCENARIO_ID = SCENARIO_ID;
        this.RFL_ID = RFL_ID;
        this.ACI_ID = ACI_ID;
        this.VICTIM_ENDPOINT_ID = VICTIM_ENDPOINT_ID;
        this.LIMIT_KIND = LIMIT_KIND;
        this.LIMIT_MODEL_NAME = LIMIT_MODEL_NAME;
        this.VALUE_UNITS = VALUE_UNITS;
        this.SAMPLE_COUNT = SAMPLE_COUNT;
        this.SAMPLE_EPOCHS = SAMPLE_EPOCHS;
        this.SAMPLE_VALUES = SAMPLE_VALUES;
        this.SAMPLE_LIMIT_VALUES = SAMPLE_LIMIT_VALUES;
        this.SAMPLE_MARGIN_DB = SAMPLE_MARGIN_DB;
        this.SAMPLE_EXCEEDS_LIMIT = SAMPLE_EXCEEDS_LIMIT;
        this.CONTRIBUTORS = CONTRIBUTORS;
        this.CDF_VALUES = CDF_VALUES;
        this.CDF_PERCENT_TIME_EXCEEDED = CDF_PERCENT_TIME_EXCEEDED;
        this.VERDICT = VERDICT;
        this.PROVENANCE = PROVENANCE;
        this.COMPUTED_AT = COMPUTED_AT;
        this.PRODUCER_ID = PRODUCER_ID;
        this.SIGNATURE = SIGNATURE;
        this.CANONICAL_JSON_SIGNATURE = CANONICAL_JSON_SIGNATURE;
    }
    pack(builder) {
        const EPF_ID = (this.EPF_ID !== null ? builder.createString(this.EPF_ID) : 0);
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const SCENARIO_ID = (this.SCENARIO_ID !== null ? builder.createString(this.SCENARIO_ID) : 0);
        const RFL_ID = (this.RFL_ID !== null ? builder.createString(this.RFL_ID) : 0);
        const ACI_ID = (this.ACI_ID !== null ? builder.createString(this.ACI_ID) : 0);
        const VICTIM_ENDPOINT_ID = (this.VICTIM_ENDPOINT_ID !== null ? builder.createString(this.VICTIM_ENDPOINT_ID) : 0);
        const LIMIT_MODEL_NAME = (this.LIMIT_MODEL_NAME !== null ? builder.createString(this.LIMIT_MODEL_NAME) : 0);
        const VALUE_UNITS = (this.VALUE_UNITS !== null ? builder.createString(this.VALUE_UNITS) : 0);
        const SAMPLE_EPOCHS = EPF.createSampleEpochsVector(builder, this.SAMPLE_EPOCHS);
        const SAMPLE_VALUES = EPF.createSampleValuesVector(builder, this.SAMPLE_VALUES);
        const SAMPLE_LIMIT_VALUES = EPF.createSampleLimitValuesVector(builder, this.SAMPLE_LIMIT_VALUES);
        const SAMPLE_MARGIN_DB = EPF.createSampleMarginDbVector(builder, this.SAMPLE_MARGIN_DB);
        const SAMPLE_EXCEEDS_LIMIT = EPF.createSampleExceedsLimitVector(builder, this.SAMPLE_EXCEEDS_LIMIT);
        const CONTRIBUTORS = EPF.createContributorsVector(builder, builder.createObjectOffsetList(this.CONTRIBUTORS));
        const CDF_VALUES = EPF.createCdfValuesVector(builder, this.CDF_VALUES);
        const CDF_PERCENT_TIME_EXCEEDED = EPF.createCdfPercentTimeExceededVector(builder, this.CDF_PERCENT_TIME_EXCEEDED);
        const VERDICT = (this.VERDICT !== null ? this.VERDICT.pack(builder) : 0);
        const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE.pack(builder) : 0);
        const PRODUCER_ID = (this.PRODUCER_ID !== null ? builder.createString(this.PRODUCER_ID) : 0);
        const SIGNATURE = EPF.createSignatureVector(builder, this.SIGNATURE);
        const CANONICAL_JSON_SIGNATURE = EPF.createCanonicalJsonSignatureVector(builder, this.CANONICAL_JSON_SIGNATURE);
        EPF.startEPF(builder);
        EPF.addEpfId(builder, EPF_ID);
        EPF.addName(builder, NAME);
        EPF.addScenarioId(builder, SCENARIO_ID);
        EPF.addRflId(builder, RFL_ID);
        EPF.addAciId(builder, ACI_ID);
        EPF.addVictimEndpointId(builder, VICTIM_ENDPOINT_ID);
        EPF.addLimitKind(builder, this.LIMIT_KIND);
        EPF.addLimitModelName(builder, LIMIT_MODEL_NAME);
        EPF.addValueUnits(builder, VALUE_UNITS);
        EPF.addSampleCount(builder, this.SAMPLE_COUNT);
        EPF.addSampleEpochs(builder, SAMPLE_EPOCHS);
        EPF.addSampleValues(builder, SAMPLE_VALUES);
        EPF.addSampleLimitValues(builder, SAMPLE_LIMIT_VALUES);
        EPF.addSampleMarginDb(builder, SAMPLE_MARGIN_DB);
        EPF.addSampleExceedsLimit(builder, SAMPLE_EXCEEDS_LIMIT);
        EPF.addContributors(builder, CONTRIBUTORS);
        EPF.addCdfValues(builder, CDF_VALUES);
        EPF.addCdfPercentTimeExceeded(builder, CDF_PERCENT_TIME_EXCEEDED);
        EPF.addVerdict(builder, VERDICT);
        EPF.addProvenance(builder, PROVENANCE);
        EPF.addComputedAt(builder, this.COMPUTED_AT);
        EPF.addProducerId(builder, PRODUCER_ID);
        EPF.addSignature(builder, SIGNATURE);
        EPF.addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATURE);
        return EPF.endEPF(builder);
    }
}
