// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { entitlementLifecycleStatus } from './entitlementLifecycleStatus.js';
/**
 * Entitlement - Provider subscription or account entitlement state.
 *
 * This is durable operational state for a provider account, not a plugin
 * license key. It records the current authoritative entitlement for an SDN
 * account identity and can be projected into subscription, billing, and
 * access-control surfaces.
 */
export class ENT {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsENT(bb, obj) {
        return (obj || new ENT()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsENT(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ENT()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$ENT');
    }
    ENTITLEMENT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    XPUB(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PLAN(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Structured entitlement lifecycle status.
     */
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : entitlementLifecycleStatus.Active;
    }
    STATUS_TEXT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    STRIPE_CUSTOMER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    STRIPE_SUBSCRIPTION_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Unix timestamp when the entitlement expires (0 = no expiry).
     */
    EXPIRES_AT() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix timestamp when the entitlement was created.
     */
    CREATED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix timestamp when the entitlement was last updated.
     */
    UPDATED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    PROVIDER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Provider signature over the entitlement record.
     */
    PROVIDER_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    providerSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    providerSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startENT(builder) {
        builder.startObject(13);
    }
    static addEntitlementId(builder, ENTITLEMENT_IDOffset) {
        builder.addFieldOffset(0, ENTITLEMENT_IDOffset, 0);
    }
    static addXpub(builder, XPUBOffset) {
        builder.addFieldOffset(1, XPUBOffset, 0);
    }
    static addPeerId(builder, PEER_IDOffset) {
        builder.addFieldOffset(2, PEER_IDOffset, 0);
    }
    static addPlan(builder, PLANOffset) {
        builder.addFieldOffset(3, PLANOffset, 0);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(4, STATUS, entitlementLifecycleStatus.Active);
    }
    static addStatusText(builder, STATUS_TEXTOffset) {
        builder.addFieldOffset(5, STATUS_TEXTOffset, 0);
    }
    static addStripeCustomerId(builder, STRIPE_CUSTOMER_IDOffset) {
        builder.addFieldOffset(6, STRIPE_CUSTOMER_IDOffset, 0);
    }
    static addStripeSubscriptionId(builder, STRIPE_SUBSCRIPTION_IDOffset) {
        builder.addFieldOffset(7, STRIPE_SUBSCRIPTION_IDOffset, 0);
    }
    static addExpiresAt(builder, EXPIRES_AT) {
        builder.addFieldInt64(8, EXPIRES_AT, BigInt('0'));
    }
    static addCreatedAt(builder, CREATED_AT) {
        builder.addFieldInt64(9, CREATED_AT, BigInt('0'));
    }
    static addUpdatedAt(builder, UPDATED_AT) {
        builder.addFieldInt64(10, UPDATED_AT, BigInt('0'));
    }
    static addProviderPeerId(builder, PROVIDER_PEER_IDOffset) {
        builder.addFieldOffset(11, PROVIDER_PEER_IDOffset, 0);
    }
    static addProviderSignature(builder, PROVIDER_SIGNATUREOffset) {
        builder.addFieldOffset(12, PROVIDER_SIGNATUREOffset, 0);
    }
    static createProviderSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startProviderSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endENT(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // XPUB
        return offset;
    }
    static finishENTBuffer(builder, offset) {
        builder.finish(offset, '$ENT');
    }
    static finishSizePrefixedENTBuffer(builder, offset) {
        builder.finish(offset, '$ENT', true);
    }
    static createENT(builder, ENTITLEMENT_IDOffset, XPUBOffset, PEER_IDOffset, PLANOffset, STATUS, STATUS_TEXTOffset, STRIPE_CUSTOMER_IDOffset, STRIPE_SUBSCRIPTION_IDOffset, EXPIRES_AT, CREATED_AT, UPDATED_AT, PROVIDER_PEER_IDOffset, PROVIDER_SIGNATUREOffset) {
        ENT.startENT(builder);
        ENT.addEntitlementId(builder, ENTITLEMENT_IDOffset);
        ENT.addXpub(builder, XPUBOffset);
        ENT.addPeerId(builder, PEER_IDOffset);
        ENT.addPlan(builder, PLANOffset);
        ENT.addStatus(builder, STATUS);
        ENT.addStatusText(builder, STATUS_TEXTOffset);
        ENT.addStripeCustomerId(builder, STRIPE_CUSTOMER_IDOffset);
        ENT.addStripeSubscriptionId(builder, STRIPE_SUBSCRIPTION_IDOffset);
        ENT.addExpiresAt(builder, EXPIRES_AT);
        ENT.addCreatedAt(builder, CREATED_AT);
        ENT.addUpdatedAt(builder, UPDATED_AT);
        ENT.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
        ENT.addProviderSignature(builder, PROVIDER_SIGNATUREOffset);
        return ENT.endENT(builder);
    }
    unpack() {
        return new ENTT(this.ENTITLEMENT_ID(), this.XPUB(), this.PEER_ID(), this.PLAN(), this.STATUS(), this.STATUS_TEXT(), this.STRIPE_CUSTOMER_ID(), this.STRIPE_SUBSCRIPTION_ID(), this.EXPIRES_AT(), this.CREATED_AT(), this.UPDATED_AT(), this.PROVIDER_PEER_ID(), this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength()));
    }
    unpackTo(_o) {
        _o.ENTITLEMENT_ID = this.ENTITLEMENT_ID();
        _o.XPUB = this.XPUB();
        _o.PEER_ID = this.PEER_ID();
        _o.PLAN = this.PLAN();
        _o.STATUS = this.STATUS();
        _o.STATUS_TEXT = this.STATUS_TEXT();
        _o.STRIPE_CUSTOMER_ID = this.STRIPE_CUSTOMER_ID();
        _o.STRIPE_SUBSCRIPTION_ID = this.STRIPE_SUBSCRIPTION_ID();
        _o.EXPIRES_AT = this.EXPIRES_AT();
        _o.CREATED_AT = this.CREATED_AT();
        _o.UPDATED_AT = this.UPDATED_AT();
        _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
        _o.PROVIDER_SIGNATURE = this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength());
    }
}
export class ENTT {
    constructor(ENTITLEMENT_ID = null, XPUB = null, PEER_ID = null, PLAN = null, STATUS = entitlementLifecycleStatus.Active, STATUS_TEXT = null, STRIPE_CUSTOMER_ID = null, STRIPE_SUBSCRIPTION_ID = null, EXPIRES_AT = BigInt('0'), CREATED_AT = BigInt('0'), UPDATED_AT = BigInt('0'), PROVIDER_PEER_ID = null, PROVIDER_SIGNATURE = []) {
        this.ENTITLEMENT_ID = ENTITLEMENT_ID;
        this.XPUB = XPUB;
        this.PEER_ID = PEER_ID;
        this.PLAN = PLAN;
        this.STATUS = STATUS;
        this.STATUS_TEXT = STATUS_TEXT;
        this.STRIPE_CUSTOMER_ID = STRIPE_CUSTOMER_ID;
        this.STRIPE_SUBSCRIPTION_ID = STRIPE_SUBSCRIPTION_ID;
        this.EXPIRES_AT = EXPIRES_AT;
        this.CREATED_AT = CREATED_AT;
        this.UPDATED_AT = UPDATED_AT;
        this.PROVIDER_PEER_ID = PROVIDER_PEER_ID;
        this.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE;
    }
    pack(builder) {
        const ENTITLEMENT_ID = (this.ENTITLEMENT_ID !== null ? builder.createString(this.ENTITLEMENT_ID) : 0);
        const XPUB = (this.XPUB !== null ? builder.createString(this.XPUB) : 0);
        const PEER_ID = (this.PEER_ID !== null ? builder.createString(this.PEER_ID) : 0);
        const PLAN = (this.PLAN !== null ? builder.createString(this.PLAN) : 0);
        const STATUS_TEXT = (this.STATUS_TEXT !== null ? builder.createString(this.STATUS_TEXT) : 0);
        const STRIPE_CUSTOMER_ID = (this.STRIPE_CUSTOMER_ID !== null ? builder.createString(this.STRIPE_CUSTOMER_ID) : 0);
        const STRIPE_SUBSCRIPTION_ID = (this.STRIPE_SUBSCRIPTION_ID !== null ? builder.createString(this.STRIPE_SUBSCRIPTION_ID) : 0);
        const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID) : 0);
        const PROVIDER_SIGNATURE = ENT.createProviderSignatureVector(builder, this.PROVIDER_SIGNATURE);
        return ENT.createENT(builder, ENTITLEMENT_ID, XPUB, PEER_ID, PLAN, this.STATUS, STATUS_TEXT, STRIPE_CUSTOMER_ID, STRIPE_SUBSCRIPTION_ID, this.EXPIRES_AT, this.CREATED_AT, this.UPDATED_AT, PROVIDER_PEER_ID, PROVIDER_SIGNATURE);
    }
}
