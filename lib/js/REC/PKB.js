// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { brokerCustodyModel } from './brokerCustodyModel.js';
import { brokerLiveness } from './brokerLiveness.js';
/**
 * Publisher Key-Broker Descriptor
 */
export class PKB {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPKB(bb, obj) {
        return (obj || new PKB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPKB(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PKB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$PKB');
    }
    BROKER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    PUBLISHER_EPM_CID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROVIDER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROTOCOL_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MULTIFORMAT_ADDRESSES(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    multiformatAddressesLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * The broker's public key (from the key server's get_public_key) so the
     * buyer can pin it
     */
    BROKER_PUBLIC_KEY(index) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    brokerPublicKeyLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    brokerPublicKeyArray() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Custody model (default: full publisher custody)
     */
    BROKER_KIND() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : brokerCustodyModel.SelfHosted;
    }
    /**
     * Store-maintained liveness status
     */
    LIVENESS() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : brokerLiveness.Unknown;
    }
    /**
     * Unix ms of the last successful probe/heartbeat
     */
    LAST_HEARTBEAT_AT() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Expected heartbeat cadence in ms
     */
    HEARTBEAT_INTERVAL_MS() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix ms the store first saw this broker
     */
    FIRST_SEEN_AT() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix ms this descriptor was last updated
     */
    UPDATED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Ed25519 signature by the publisher `$EPM` over the self-attested endpoint
     * fields; re-verified by the store's live probe
     */
    SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startPKB(builder) {
        builder.startObject(13);
    }
    static addBrokerId(builder, BROKER_IDOffset) {
        builder.addFieldOffset(0, BROKER_IDOffset, 0);
    }
    static addPublisherEpmCid(builder, PUBLISHER_EPM_CIDOffset) {
        builder.addFieldOffset(1, PUBLISHER_EPM_CIDOffset, 0);
    }
    static addProviderPeerId(builder, PROVIDER_PEER_IDOffset) {
        builder.addFieldOffset(2, PROVIDER_PEER_IDOffset, 0);
    }
    static addProtocolId(builder, PROTOCOL_IDOffset) {
        builder.addFieldOffset(3, PROTOCOL_IDOffset, 0);
    }
    static addMultiformatAddresses(builder, MULTIFORMAT_ADDRESSESOffset) {
        builder.addFieldOffset(4, MULTIFORMAT_ADDRESSESOffset, 0);
    }
    static createMultiformatAddressesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startMultiformatAddressesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addBrokerPublicKey(builder, BROKER_PUBLIC_KEYOffset) {
        builder.addFieldOffset(5, BROKER_PUBLIC_KEYOffset, 0);
    }
    static createBrokerPublicKeyVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startBrokerPublicKeyVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addBrokerKind(builder, BROKER_KIND) {
        builder.addFieldInt8(6, BROKER_KIND, brokerCustodyModel.SelfHosted);
    }
    static addLiveness(builder, LIVENESS) {
        builder.addFieldInt8(7, LIVENESS, brokerLiveness.Unknown);
    }
    static addLastHeartbeatAt(builder, LAST_HEARTBEAT_AT) {
        builder.addFieldInt64(8, LAST_HEARTBEAT_AT, BigInt('0'));
    }
    static addHeartbeatIntervalMs(builder, HEARTBEAT_INTERVAL_MS) {
        builder.addFieldInt64(9, HEARTBEAT_INTERVAL_MS, BigInt('0'));
    }
    static addFirstSeenAt(builder, FIRST_SEEN_AT) {
        builder.addFieldInt64(10, FIRST_SEEN_AT, BigInt('0'));
    }
    static addUpdatedAt(builder, UPDATED_AT) {
        builder.addFieldInt64(11, UPDATED_AT, BigInt('0'));
    }
    static addSignature(builder, SIGNATUREOffset) {
        builder.addFieldOffset(12, SIGNATUREOffset, 0);
    }
    static createSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endPKB(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // BROKER_ID
        return offset;
    }
    static finishPKBBuffer(builder, offset) {
        builder.finish(offset, '$PKB');
    }
    static finishSizePrefixedPKBBuffer(builder, offset) {
        builder.finish(offset, '$PKB', true);
    }
    static createPKB(builder, BROKER_IDOffset, PUBLISHER_EPM_CIDOffset, PROVIDER_PEER_IDOffset, PROTOCOL_IDOffset, MULTIFORMAT_ADDRESSESOffset, BROKER_PUBLIC_KEYOffset, BROKER_KIND, LIVENESS, LAST_HEARTBEAT_AT, HEARTBEAT_INTERVAL_MS, FIRST_SEEN_AT, UPDATED_AT, SIGNATUREOffset) {
        PKB.startPKB(builder);
        PKB.addBrokerId(builder, BROKER_IDOffset);
        PKB.addPublisherEpmCid(builder, PUBLISHER_EPM_CIDOffset);
        PKB.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
        PKB.addProtocolId(builder, PROTOCOL_IDOffset);
        PKB.addMultiformatAddresses(builder, MULTIFORMAT_ADDRESSESOffset);
        PKB.addBrokerPublicKey(builder, BROKER_PUBLIC_KEYOffset);
        PKB.addBrokerKind(builder, BROKER_KIND);
        PKB.addLiveness(builder, LIVENESS);
        PKB.addLastHeartbeatAt(builder, LAST_HEARTBEAT_AT);
        PKB.addHeartbeatIntervalMs(builder, HEARTBEAT_INTERVAL_MS);
        PKB.addFirstSeenAt(builder, FIRST_SEEN_AT);
        PKB.addUpdatedAt(builder, UPDATED_AT);
        PKB.addSignature(builder, SIGNATUREOffset);
        return PKB.endPKB(builder);
    }
    unpack() {
        return new PKBT(this.BROKER_ID(), this.PUBLISHER_EPM_CID(), this.PROVIDER_PEER_ID(), this.PROTOCOL_ID(), this.bb.createScalarList(this.MULTIFORMAT_ADDRESSES.bind(this), this.multiformatAddressesLength()), this.bb.createScalarList(this.BROKER_PUBLIC_KEY.bind(this), this.brokerPublicKeyLength()), this.BROKER_KIND(), this.LIVENESS(), this.LAST_HEARTBEAT_AT(), this.HEARTBEAT_INTERVAL_MS(), this.FIRST_SEEN_AT(), this.UPDATED_AT(), this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength()));
    }
    unpackTo(_o) {
        _o.BROKER_ID = this.BROKER_ID();
        _o.PUBLISHER_EPM_CID = this.PUBLISHER_EPM_CID();
        _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
        _o.PROTOCOL_ID = this.PROTOCOL_ID();
        _o.MULTIFORMAT_ADDRESSES = this.bb.createScalarList(this.MULTIFORMAT_ADDRESSES.bind(this), this.multiformatAddressesLength());
        _o.BROKER_PUBLIC_KEY = this.bb.createScalarList(this.BROKER_PUBLIC_KEY.bind(this), this.brokerPublicKeyLength());
        _o.BROKER_KIND = this.BROKER_KIND();
        _o.LIVENESS = this.LIVENESS();
        _o.LAST_HEARTBEAT_AT = this.LAST_HEARTBEAT_AT();
        _o.HEARTBEAT_INTERVAL_MS = this.HEARTBEAT_INTERVAL_MS();
        _o.FIRST_SEEN_AT = this.FIRST_SEEN_AT();
        _o.UPDATED_AT = this.UPDATED_AT();
        _o.SIGNATURE = this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength());
    }
}
export class PKBT {
    constructor(BROKER_ID = null, PUBLISHER_EPM_CID = null, PROVIDER_PEER_ID = null, PROTOCOL_ID = null, MULTIFORMAT_ADDRESSES = [], BROKER_PUBLIC_KEY = [], BROKER_KIND = brokerCustodyModel.SelfHosted, LIVENESS = brokerLiveness.Unknown, LAST_HEARTBEAT_AT = BigInt('0'), HEARTBEAT_INTERVAL_MS = BigInt('0'), FIRST_SEEN_AT = BigInt('0'), UPDATED_AT = BigInt('0'), SIGNATURE = []) {
        this.BROKER_ID = BROKER_ID;
        this.PUBLISHER_EPM_CID = PUBLISHER_EPM_CID;
        this.PROVIDER_PEER_ID = PROVIDER_PEER_ID;
        this.PROTOCOL_ID = PROTOCOL_ID;
        this.MULTIFORMAT_ADDRESSES = MULTIFORMAT_ADDRESSES;
        this.BROKER_PUBLIC_KEY = BROKER_PUBLIC_KEY;
        this.BROKER_KIND = BROKER_KIND;
        this.LIVENESS = LIVENESS;
        this.LAST_HEARTBEAT_AT = LAST_HEARTBEAT_AT;
        this.HEARTBEAT_INTERVAL_MS = HEARTBEAT_INTERVAL_MS;
        this.FIRST_SEEN_AT = FIRST_SEEN_AT;
        this.UPDATED_AT = UPDATED_AT;
        this.SIGNATURE = SIGNATURE;
    }
    pack(builder) {
        const BROKER_ID = (this.BROKER_ID !== null ? builder.createString(this.BROKER_ID) : 0);
        const PUBLISHER_EPM_CID = (this.PUBLISHER_EPM_CID !== null ? builder.createString(this.PUBLISHER_EPM_CID) : 0);
        const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID) : 0);
        const PROTOCOL_ID = (this.PROTOCOL_ID !== null ? builder.createString(this.PROTOCOL_ID) : 0);
        const MULTIFORMAT_ADDRESSES = PKB.createMultiformatAddressesVector(builder, builder.createObjectOffsetList(this.MULTIFORMAT_ADDRESSES));
        const BROKER_PUBLIC_KEY = PKB.createBrokerPublicKeyVector(builder, this.BROKER_PUBLIC_KEY);
        const SIGNATURE = PKB.createSignatureVector(builder, this.SIGNATURE);
        return PKB.createPKB(builder, BROKER_ID, PUBLISHER_EPM_CID, PROVIDER_PEER_ID, PROTOCOL_ID, MULTIFORMAT_ADDRESSES, BROKER_PUBLIC_KEY, this.BROKER_KIND, this.LIVENESS, this.LAST_HEARTBEAT_AT, this.HEARTBEAT_INTERVAL_MS, this.FIRST_SEEN_AT, this.UPDATED_AT, SIGNATURE);
    }
}
