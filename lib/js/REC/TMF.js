// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Telemetry Transfer Frame (CCSDS 132.0-B-2)
 */
export class TMF {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTMF(bb, obj) {
        return (obj || new TMF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTMF(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TMF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$TMF');
    }
    /**
     * Transfer frame version
     */
    VERSION() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Spacecraft identifier
     */
    SPACECRAFT_ID() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Virtual channel identifier
     */
    VIRTUAL_CHANNEL_ID() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Operational control field flag
     */
    OCF_FLAG() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Master channel frame count
     */
    MASTER_FRAME_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Virtual channel frame count
     */
    VIRTUAL_FRAME_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Secondary header flag
     */
    SEC_HDR_FLAG() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Synchronization flag
     */
    SYNCH_FLAG() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Packet order flag
     */
    PACKET_ORDER_FLAG() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Segment length identifier
     */
    SEGMENT_LENGTH_ID() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * First header pointer
     */
    FIRST_HDR_POINTER() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Data field
     */
    DATA(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    dataArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Operational control field
     */
    OCF(index) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    ocfLength() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ocfArray() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Frame error control field
     */
    FECF() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    static startTMF(builder) {
        builder.startObject(14);
    }
    static addVersion(builder, VERSION) {
        builder.addFieldInt8(0, VERSION, 0);
    }
    static addSpacecraftId(builder, SPACECRAFT_ID) {
        builder.addFieldInt16(1, SPACECRAFT_ID, 0);
    }
    static addVirtualChannelId(builder, VIRTUAL_CHANNEL_ID) {
        builder.addFieldInt8(2, VIRTUAL_CHANNEL_ID, 0);
    }
    static addOcfFlag(builder, OCF_FLAG) {
        builder.addFieldInt8(3, +OCF_FLAG, +false);
    }
    static addMasterFrameCount(builder, MASTER_FRAME_COUNT) {
        builder.addFieldInt8(4, MASTER_FRAME_COUNT, 0);
    }
    static addVirtualFrameCount(builder, VIRTUAL_FRAME_COUNT) {
        builder.addFieldInt8(5, VIRTUAL_FRAME_COUNT, 0);
    }
    static addSecHdrFlag(builder, SEC_HDR_FLAG) {
        builder.addFieldInt8(6, +SEC_HDR_FLAG, +false);
    }
    static addSynchFlag(builder, SYNCH_FLAG) {
        builder.addFieldInt8(7, +SYNCH_FLAG, +false);
    }
    static addPacketOrderFlag(builder, PACKET_ORDER_FLAG) {
        builder.addFieldInt8(8, +PACKET_ORDER_FLAG, +false);
    }
    static addSegmentLengthId(builder, SEGMENT_LENGTH_ID) {
        builder.addFieldInt8(9, SEGMENT_LENGTH_ID, 0);
    }
    static addFirstHdrPointer(builder, FIRST_HDR_POINTER) {
        builder.addFieldInt16(10, FIRST_HDR_POINTER, 0);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(11, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addOcf(builder, OCFOffset) {
        builder.addFieldOffset(12, OCFOffset, 0);
    }
    static createOcfVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startOcfVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addFecf(builder, FECF) {
        builder.addFieldInt16(13, FECF, 0);
    }
    static endTMF(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishTMFBuffer(builder, offset) {
        builder.finish(offset, '$TMF');
    }
    static finishSizePrefixedTMFBuffer(builder, offset) {
        builder.finish(offset, '$TMF', true);
    }
    static createTMF(builder, VERSION, SPACECRAFT_ID, VIRTUAL_CHANNEL_ID, OCF_FLAG, MASTER_FRAME_COUNT, VIRTUAL_FRAME_COUNT, SEC_HDR_FLAG, SYNCH_FLAG, PACKET_ORDER_FLAG, SEGMENT_LENGTH_ID, FIRST_HDR_POINTER, DATAOffset, OCFOffset, FECF) {
        TMF.startTMF(builder);
        TMF.addVersion(builder, VERSION);
        TMF.addSpacecraftId(builder, SPACECRAFT_ID);
        TMF.addVirtualChannelId(builder, VIRTUAL_CHANNEL_ID);
        TMF.addOcfFlag(builder, OCF_FLAG);
        TMF.addMasterFrameCount(builder, MASTER_FRAME_COUNT);
        TMF.addVirtualFrameCount(builder, VIRTUAL_FRAME_COUNT);
        TMF.addSecHdrFlag(builder, SEC_HDR_FLAG);
        TMF.addSynchFlag(builder, SYNCH_FLAG);
        TMF.addPacketOrderFlag(builder, PACKET_ORDER_FLAG);
        TMF.addSegmentLengthId(builder, SEGMENT_LENGTH_ID);
        TMF.addFirstHdrPointer(builder, FIRST_HDR_POINTER);
        TMF.addData(builder, DATAOffset);
        TMF.addOcf(builder, OCFOffset);
        TMF.addFecf(builder, FECF);
        return TMF.endTMF(builder);
    }
    unpack() {
        return new TMFT(this.VERSION(), this.SPACECRAFT_ID(), this.VIRTUAL_CHANNEL_ID(), this.OCF_FLAG(), this.MASTER_FRAME_COUNT(), this.VIRTUAL_FRAME_COUNT(), this.SEC_HDR_FLAG(), this.SYNCH_FLAG(), this.PACKET_ORDER_FLAG(), this.SEGMENT_LENGTH_ID(), this.FIRST_HDR_POINTER(), this.bb.createScalarList(this.DATA.bind(this), this.dataLength()), this.bb.createScalarList(this.OCF.bind(this), this.ocfLength()), this.FECF());
    }
    unpackTo(_o) {
        _o.VERSION = this.VERSION();
        _o.SPACECRAFT_ID = this.SPACECRAFT_ID();
        _o.VIRTUAL_CHANNEL_ID = this.VIRTUAL_CHANNEL_ID();
        _o.OCF_FLAG = this.OCF_FLAG();
        _o.MASTER_FRAME_COUNT = this.MASTER_FRAME_COUNT();
        _o.VIRTUAL_FRAME_COUNT = this.VIRTUAL_FRAME_COUNT();
        _o.SEC_HDR_FLAG = this.SEC_HDR_FLAG();
        _o.SYNCH_FLAG = this.SYNCH_FLAG();
        _o.PACKET_ORDER_FLAG = this.PACKET_ORDER_FLAG();
        _o.SEGMENT_LENGTH_ID = this.SEGMENT_LENGTH_ID();
        _o.FIRST_HDR_POINTER = this.FIRST_HDR_POINTER();
        _o.DATA = this.bb.createScalarList(this.DATA.bind(this), this.dataLength());
        _o.OCF = this.bb.createScalarList(this.OCF.bind(this), this.ocfLength());
        _o.FECF = this.FECF();
    }
}
export class TMFT {
    constructor(VERSION = 0, SPACECRAFT_ID = 0, VIRTUAL_CHANNEL_ID = 0, OCF_FLAG = false, MASTER_FRAME_COUNT = 0, VIRTUAL_FRAME_COUNT = 0, SEC_HDR_FLAG = false, SYNCH_FLAG = false, PACKET_ORDER_FLAG = false, SEGMENT_LENGTH_ID = 0, FIRST_HDR_POINTER = 0, DATA = [], OCF = [], FECF = 0) {
        this.VERSION = VERSION;
        this.SPACECRAFT_ID = SPACECRAFT_ID;
        this.VIRTUAL_CHANNEL_ID = VIRTUAL_CHANNEL_ID;
        this.OCF_FLAG = OCF_FLAG;
        this.MASTER_FRAME_COUNT = MASTER_FRAME_COUNT;
        this.VIRTUAL_FRAME_COUNT = VIRTUAL_FRAME_COUNT;
        this.SEC_HDR_FLAG = SEC_HDR_FLAG;
        this.SYNCH_FLAG = SYNCH_FLAG;
        this.PACKET_ORDER_FLAG = PACKET_ORDER_FLAG;
        this.SEGMENT_LENGTH_ID = SEGMENT_LENGTH_ID;
        this.FIRST_HDR_POINTER = FIRST_HDR_POINTER;
        this.DATA = DATA;
        this.OCF = OCF;
        this.FECF = FECF;
    }
    pack(builder) {
        const DATA = TMF.createDataVector(builder, this.DATA);
        const OCF = TMF.createOcfVector(builder, this.OCF);
        return TMF.createTMF(builder, this.VERSION, this.SPACECRAFT_ID, this.VIRTUAL_CHANNEL_ID, this.OCF_FLAG, this.MASTER_FRAME_COUNT, this.VIRTUAL_FRAME_COUNT, this.SEC_HDR_FLAG, this.SYNCH_FLAG, this.PACKET_ORDER_FLAG, this.SEGMENT_LENGTH_ID, this.FIRST_HDR_POINTER, DATA, OCF, this.FECF);
    }
}
