// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Ionospheric Electron Density Profile
 */
export class ionoDensityProfile {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsionoDensityProfile(bb, obj) {
        return (obj || new ionoDensityProfile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsionoDensityProfile(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ionoDensityProfile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Altitudes in km
     */
    ALTITUDES(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    altitudesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    altitudesArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Electron densities at each altitude in electrons/m^3
     */
    DENSITIES(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    densitiesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    densitiesArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startionoDensityProfile(builder) {
        builder.startObject(3);
    }
    static addEpoch(builder, EPOCHOffset) {
        builder.addFieldOffset(0, EPOCHOffset, 0);
    }
    static addAltitudes(builder, ALTITUDESOffset) {
        builder.addFieldOffset(1, ALTITUDESOffset, 0);
    }
    static createAltitudesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startAltitudesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addDensities(builder, DENSITIESOffset) {
        builder.addFieldOffset(2, DENSITIESOffset, 0);
    }
    static createDensitiesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startDensitiesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endionoDensityProfile(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createionoDensityProfile(builder, EPOCHOffset, ALTITUDESOffset, DENSITIESOffset) {
        ionoDensityProfile.startionoDensityProfile(builder);
        ionoDensityProfile.addEpoch(builder, EPOCHOffset);
        ionoDensityProfile.addAltitudes(builder, ALTITUDESOffset);
        ionoDensityProfile.addDensities(builder, DENSITIESOffset);
        return ionoDensityProfile.endionoDensityProfile(builder);
    }
    unpack() {
        return new ionoDensityProfileT(this.EPOCH(), this.bb.createScalarList(this.ALTITUDES.bind(this), this.altitudesLength()), this.bb.createScalarList(this.DENSITIES.bind(this), this.densitiesLength()));
    }
    unpackTo(_o) {
        _o.EPOCH = this.EPOCH();
        _o.ALTITUDES = this.bb.createScalarList(this.ALTITUDES.bind(this), this.altitudesLength());
        _o.DENSITIES = this.bb.createScalarList(this.DENSITIES.bind(this), this.densitiesLength());
    }
}
export class ionoDensityProfileT {
    constructor(EPOCH = null, ALTITUDES = [], DENSITIES = []) {
        this.EPOCH = EPOCH;
        this.ALTITUDES = ALTITUDES;
        this.DENSITIES = DENSITIES;
    }
    pack(builder) {
        const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH) : 0);
        const ALTITUDES = ionoDensityProfile.createAltitudesVector(builder, this.ALTITUDES);
        const DENSITIES = ionoDensityProfile.createDensitiesVector(builder, this.DENSITIES);
        return ionoDensityProfile.createionoDensityProfile(builder, EPOCH, ALTITUDES, DENSITIES);
    }
}
