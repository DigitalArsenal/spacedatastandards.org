// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { ACWBlackoutWindow } from './ACWBlackoutWindow.js';
import { ACWStateSample } from './ACWStateSample.js';
/**
 * A moving observer (spacecraft) given as pre-sampled Earth-fixed states, in
 * the same frame and time scale as ACWRequest.STATES.
 */
export class ACWObserverTrajectory {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsACWObserverTrajectory(bb, obj) {
        return (obj || new ACWObserverTrajectory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsACWObserverTrajectory(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ACWObserverTrajectory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    OBSERVER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    STATES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new ACWStateSample()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    statesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Observer-specific unavailable intervals.
     */
    BLACKOUT_WINDOWS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new ACWBlackoutWindow()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    blackoutWindowsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startACWObserverTrajectory(builder) {
        builder.startObject(4);
    }
    static addObserverId(builder, OBSERVER_IDOffset) {
        builder.addFieldOffset(0, OBSERVER_IDOffset, 0);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(1, NAMEOffset, 0);
    }
    static addStates(builder, STATESOffset) {
        builder.addFieldOffset(2, STATESOffset, 0);
    }
    static createStatesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startStatesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addBlackoutWindows(builder, BLACKOUT_WINDOWSOffset) {
        builder.addFieldOffset(3, BLACKOUT_WINDOWSOffset, 0);
    }
    static createBlackoutWindowsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startBlackoutWindowsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endACWObserverTrajectory(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createACWObserverTrajectory(builder, OBSERVER_IDOffset, NAMEOffset, STATESOffset, BLACKOUT_WINDOWSOffset) {
        ACWObserverTrajectory.startACWObserverTrajectory(builder);
        ACWObserverTrajectory.addObserverId(builder, OBSERVER_IDOffset);
        ACWObserverTrajectory.addName(builder, NAMEOffset);
        ACWObserverTrajectory.addStates(builder, STATESOffset);
        ACWObserverTrajectory.addBlackoutWindows(builder, BLACKOUT_WINDOWSOffset);
        return ACWObserverTrajectory.endACWObserverTrajectory(builder);
    }
    unpack() {
        return new ACWObserverTrajectoryT(this.OBSERVER_ID(), this.NAME(), this.bb.createObjList(this.STATES.bind(this), this.statesLength()), this.bb.createObjList(this.BLACKOUT_WINDOWS.bind(this), this.blackoutWindowsLength()));
    }
    unpackTo(_o) {
        _o.OBSERVER_ID = this.OBSERVER_ID();
        _o.NAME = this.NAME();
        _o.STATES = this.bb.createObjList(this.STATES.bind(this), this.statesLength());
        _o.BLACKOUT_WINDOWS = this.bb.createObjList(this.BLACKOUT_WINDOWS.bind(this), this.blackoutWindowsLength());
    }
}
export class ACWObserverTrajectoryT {
    constructor(OBSERVER_ID = null, NAME = null, STATES = [], BLACKOUT_WINDOWS = []) {
        this.OBSERVER_ID = OBSERVER_ID;
        this.NAME = NAME;
        this.STATES = STATES;
        this.BLACKOUT_WINDOWS = BLACKOUT_WINDOWS;
    }
    pack(builder) {
        const OBSERVER_ID = (this.OBSERVER_ID !== null ? builder.createString(this.OBSERVER_ID) : 0);
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const STATES = ACWObserverTrajectory.createStatesVector(builder, builder.createObjectOffsetList(this.STATES));
        const BLACKOUT_WINDOWS = ACWObserverTrajectory.createBlackoutWindowsVector(builder, builder.createObjectOffsetList(this.BLACKOUT_WINDOWS));
        return ACWObserverTrajectory.createACWObserverTrajectory(builder, OBSERVER_ID, NAME, STATES, BLACKOUT_WINDOWS);
    }
}
