// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { ReferenceLocationType } from './ReferenceLocationType.js';
/**
 * Location specification within a container
 */
export class LocationInContainer {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsLocationInContainer(bb, obj) {
        return (obj || new LocationInContainer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsLocationInContainer(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new LocationInContainer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Reference location type
     */
    REFERENCE_LOCATION() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : ReferenceLocationType.PREVIOUS_ENTRY;
    }
    /**
     * Offset in bits from reference location
     */
    OFFSET_IN_BITS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    static startLocationInContainer(builder) {
        builder.startObject(2);
    }
    static addReferenceLocation(builder, REFERENCE_LOCATION) {
        builder.addFieldInt8(0, REFERENCE_LOCATION, ReferenceLocationType.PREVIOUS_ENTRY);
    }
    static addOffsetInBits(builder, OFFSET_IN_BITS) {
        builder.addFieldInt32(1, OFFSET_IN_BITS, 0);
    }
    static endLocationInContainer(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createLocationInContainer(builder, REFERENCE_LOCATION, OFFSET_IN_BITS) {
        LocationInContainer.startLocationInContainer(builder);
        LocationInContainer.addReferenceLocation(builder, REFERENCE_LOCATION);
        LocationInContainer.addOffsetInBits(builder, OFFSET_IN_BITS);
        return LocationInContainer.endLocationInContainer(builder);
    }
    unpack() {
        return new LocationInContainerT(this.REFERENCE_LOCATION(), this.OFFSET_IN_BITS());
    }
    unpackTo(_o) {
        _o.REFERENCE_LOCATION = this.REFERENCE_LOCATION();
        _o.OFFSET_IN_BITS = this.OFFSET_IN_BITS();
    }
}
export class LocationInContainerT {
    constructor(REFERENCE_LOCATION = ReferenceLocationType.PREVIOUS_ENTRY, OFFSET_IN_BITS = 0) {
        this.REFERENCE_LOCATION = REFERENCE_LOCATION;
        this.OFFSET_IN_BITS = OFFSET_IN_BITS;
    }
    pack(builder) {
        return LocationInContainer.createLocationInContainer(builder, this.REFERENCE_LOCATION, this.OFFSET_IN_BITS);
    }
}
