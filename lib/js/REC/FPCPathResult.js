// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Path reconstruction result.
 */
export class FPCPathResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsFPCPathResult(bb, obj) {
        return (obj || new FPCPathResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsFPCPathResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FPCPathResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Source vertex index from the latest SSSP solve.
     */
    SOURCE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Requested target vertex index.
     */
    TARGET() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Whether a path exists from SOURCE to TARGET.
     */
    REACHABLE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Total distance from SOURCE to TARGET (Infinity if unreachable).
     */
    DISTANCE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Vertex sequence from SOURCE to TARGET.
     */
    PATH(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    pathLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    pathArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? new Uint32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startFPCPathResult(builder) {
        builder.startObject(5);
    }
    static addSource(builder, SOURCE) {
        builder.addFieldInt32(0, SOURCE, 0);
    }
    static addTarget(builder, TARGET) {
        builder.addFieldInt32(1, TARGET, 0);
    }
    static addReachable(builder, REACHABLE) {
        builder.addFieldInt8(2, +REACHABLE, +false);
    }
    static addDistance(builder, DISTANCE) {
        builder.addFieldFloat64(3, DISTANCE, 0.0);
    }
    static addPath(builder, PATHOffset) {
        builder.addFieldOffset(4, PATHOffset, 0);
    }
    static createPathVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startPathVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endFPCPathResult(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createFPCPathResult(builder, SOURCE, TARGET, REACHABLE, DISTANCE, PATHOffset) {
        FPCPathResult.startFPCPathResult(builder);
        FPCPathResult.addSource(builder, SOURCE);
        FPCPathResult.addTarget(builder, TARGET);
        FPCPathResult.addReachable(builder, REACHABLE);
        FPCPathResult.addDistance(builder, DISTANCE);
        FPCPathResult.addPath(builder, PATHOffset);
        return FPCPathResult.endFPCPathResult(builder);
    }
    unpack() {
        return new FPCPathResultT(this.SOURCE(), this.TARGET(), this.REACHABLE(), this.DISTANCE(), this.bb.createScalarList(this.PATH.bind(this), this.pathLength()));
    }
    unpackTo(_o) {
        _o.SOURCE = this.SOURCE();
        _o.TARGET = this.TARGET();
        _o.REACHABLE = this.REACHABLE();
        _o.DISTANCE = this.DISTANCE();
        _o.PATH = this.bb.createScalarList(this.PATH.bind(this), this.pathLength());
    }
}
export class FPCPathResultT {
    constructor(SOURCE = 0, TARGET = 0, REACHABLE = false, DISTANCE = 0.0, PATH = []) {
        this.SOURCE = SOURCE;
        this.TARGET = TARGET;
        this.REACHABLE = REACHABLE;
        this.DISTANCE = DISTANCE;
        this.PATH = PATH;
    }
    pack(builder) {
        const PATH = FPCPathResult.createPathVector(builder, this.PATH);
        return FPCPathResult.createFPCPathResult(builder, this.SOURCE, this.TARGET, this.REACHABLE, this.DISTANCE, PATH);
    }
}
