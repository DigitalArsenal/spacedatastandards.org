// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Document header information
 */
export class XTCHeader {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsXTCHeader(bb, obj) {
        return (obj || new XTCHeader()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsXTCHeader(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new XTCHeader()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DATE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CLASSIFICATION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    VALIDATION_STATUS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    AUTHOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    NOTES(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    notesLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startXTCHeader(builder) {
        builder.startObject(6);
    }
    static addVersion(builder, VERSIONOffset) {
        builder.addFieldOffset(0, VERSIONOffset, 0);
    }
    static addDate(builder, DATEOffset) {
        builder.addFieldOffset(1, DATEOffset, 0);
    }
    static addClassification(builder, CLASSIFICATIONOffset) {
        builder.addFieldOffset(2, CLASSIFICATIONOffset, 0);
    }
    static addValidationStatus(builder, VALIDATION_STATUSOffset) {
        builder.addFieldOffset(3, VALIDATION_STATUSOffset, 0);
    }
    static addAuthor(builder, AUTHOROffset) {
        builder.addFieldOffset(4, AUTHOROffset, 0);
    }
    static addNotes(builder, NOTESOffset) {
        builder.addFieldOffset(5, NOTESOffset, 0);
    }
    static createNotesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startNotesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endXTCHeader(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createXTCHeader(builder, VERSIONOffset, DATEOffset, CLASSIFICATIONOffset, VALIDATION_STATUSOffset, AUTHOROffset, NOTESOffset) {
        XTCHeader.startXTCHeader(builder);
        XTCHeader.addVersion(builder, VERSIONOffset);
        XTCHeader.addDate(builder, DATEOffset);
        XTCHeader.addClassification(builder, CLASSIFICATIONOffset);
        XTCHeader.addValidationStatus(builder, VALIDATION_STATUSOffset);
        XTCHeader.addAuthor(builder, AUTHOROffset);
        XTCHeader.addNotes(builder, NOTESOffset);
        return XTCHeader.endXTCHeader(builder);
    }
    unpack() {
        return new XTCHeaderT(this.VERSION(), this.DATE(), this.CLASSIFICATION(), this.VALIDATION_STATUS(), this.AUTHOR(), this.bb.createScalarList(this.NOTES.bind(this), this.notesLength()));
    }
    unpackTo(_o) {
        _o.VERSION = this.VERSION();
        _o.DATE = this.DATE();
        _o.CLASSIFICATION = this.CLASSIFICATION();
        _o.VALIDATION_STATUS = this.VALIDATION_STATUS();
        _o.AUTHOR = this.AUTHOR();
        _o.NOTES = this.bb.createScalarList(this.NOTES.bind(this), this.notesLength());
    }
}
export class XTCHeaderT {
    constructor(VERSION = null, DATE = null, CLASSIFICATION = null, VALIDATION_STATUS = null, AUTHOR = null, NOTES = []) {
        this.VERSION = VERSION;
        this.DATE = DATE;
        this.CLASSIFICATION = CLASSIFICATION;
        this.VALIDATION_STATUS = VALIDATION_STATUS;
        this.AUTHOR = AUTHOR;
        this.NOTES = NOTES;
    }
    pack(builder) {
        const VERSION = (this.VERSION !== null ? builder.createString(this.VERSION) : 0);
        const DATE = (this.DATE !== null ? builder.createString(this.DATE) : 0);
        const CLASSIFICATION = (this.CLASSIFICATION !== null ? builder.createString(this.CLASSIFICATION) : 0);
        const VALIDATION_STATUS = (this.VALIDATION_STATUS !== null ? builder.createString(this.VALIDATION_STATUS) : 0);
        const AUTHOR = (this.AUTHOR !== null ? builder.createString(this.AUTHOR) : 0);
        const NOTES = XTCHeader.createNotesVector(builder, builder.createObjectOffsetList(this.NOTES));
        return XTCHeader.createXTCHeader(builder, VERSION, DATE, CLASSIFICATION, VALIDATION_STATUS, AUTHOR, NOTES);
    }
}
