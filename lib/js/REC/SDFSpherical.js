// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Spherical SDF parameters.
 */
export class SDFSpherical {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSDFSpherical(bb, obj) {
        return (obj || new SDFSpherical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSDFSpherical(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SDFSpherical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    RADIUS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Inner radius for hollow sphere (0 == solid).
     */
    INNER_RADIUS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startSDFSpherical(builder) {
        builder.startObject(2);
    }
    static addRadius(builder, RADIUS) {
        builder.addFieldFloat64(0, RADIUS, 0.0);
    }
    static addInnerRadius(builder, INNER_RADIUS) {
        builder.addFieldFloat64(1, INNER_RADIUS, 0.0);
    }
    static endSDFSpherical(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSDFSpherical(builder, RADIUS, INNER_RADIUS) {
        SDFSpherical.startSDFSpherical(builder);
        SDFSpherical.addRadius(builder, RADIUS);
        SDFSpherical.addInnerRadius(builder, INNER_RADIUS);
        return SDFSpherical.endSDFSpherical(builder);
    }
    unpack() {
        return new SDFSphericalT(this.RADIUS(), this.INNER_RADIUS());
    }
    unpackTo(_o) {
        _o.RADIUS = this.RADIUS();
        _o.INNER_RADIUS = this.INNER_RADIUS();
    }
}
export class SDFSphericalT {
    constructor(RADIUS = 0.0, INNER_RADIUS = 0.0) {
        this.RADIUS = RADIUS;
        this.INNER_RADIUS = INNER_RADIUS;
    }
    pack(builder) {
        return SDFSpherical.createSDFSpherical(builder, this.RADIUS, this.INNER_RADIUS);
    }
}
