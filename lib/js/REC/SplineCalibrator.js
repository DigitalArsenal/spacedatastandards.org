// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SplinePoint } from './SplinePoint.js';
/**
 * Spline calibrator - piecewise linear interpolation
 */
export class SplineCalibrator {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSplineCalibrator(bb, obj) {
        return (obj || new SplineCalibrator()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSplineCalibrator(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SplineCalibrator()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Spline points ordered by raw value
     */
    POINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new SplinePoint()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    pointsLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Extrapolate below minimum point
     */
    EXTRAPOLATE_LOW() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Extrapolate above maximum point
     */
    EXTRAPOLATE_HIGH() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startSplineCalibrator(builder) {
        builder.startObject(3);
    }
    static addPoints(builder, POINTSOffset) {
        builder.addFieldOffset(0, POINTSOffset, 0);
    }
    static createPointsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPointsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addExtrapolateLow(builder, EXTRAPOLATE_LOW) {
        builder.addFieldInt8(1, +EXTRAPOLATE_LOW, +false);
    }
    static addExtrapolateHigh(builder, EXTRAPOLATE_HIGH) {
        builder.addFieldInt8(2, +EXTRAPOLATE_HIGH, +false);
    }
    static endSplineCalibrator(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSplineCalibrator(builder, POINTSOffset, EXTRAPOLATE_LOW, EXTRAPOLATE_HIGH) {
        SplineCalibrator.startSplineCalibrator(builder);
        SplineCalibrator.addPoints(builder, POINTSOffset);
        SplineCalibrator.addExtrapolateLow(builder, EXTRAPOLATE_LOW);
        SplineCalibrator.addExtrapolateHigh(builder, EXTRAPOLATE_HIGH);
        return SplineCalibrator.endSplineCalibrator(builder);
    }
    unpack() {
        return new SplineCalibratorT(this.bb.createObjList(this.POINTS.bind(this), this.pointsLength()), this.EXTRAPOLATE_LOW(), this.EXTRAPOLATE_HIGH());
    }
    unpackTo(_o) {
        _o.POINTS = this.bb.createObjList(this.POINTS.bind(this), this.pointsLength());
        _o.EXTRAPOLATE_LOW = this.EXTRAPOLATE_LOW();
        _o.EXTRAPOLATE_HIGH = this.EXTRAPOLATE_HIGH();
    }
}
export class SplineCalibratorT {
    constructor(POINTS = [], EXTRAPOLATE_LOW = false, EXTRAPOLATE_HIGH = false) {
        this.POINTS = POINTS;
        this.EXTRAPOLATE_LOW = EXTRAPOLATE_LOW;
        this.EXTRAPOLATE_HIGH = EXTRAPOLATE_HIGH;
    }
    pack(builder) {
        const POINTS = SplineCalibrator.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));
        return SplineCalibrator.createSplineCalibrator(builder, POINTS, this.EXTRAPOLATE_LOW, this.EXTRAPOLATE_HIGH);
    }
}
