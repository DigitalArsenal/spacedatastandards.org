// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLLinearRing } from './KMLLinearRing.js';
/**
 * Polygon geometry
 */
export class KMLPolygon {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLPolygon(bb, obj) {
        return (obj || new KMLPolygon()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLPolygon(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLPolygon()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Outer boundary
     */
    OUTER_BOUNDARY(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new KMLLinearRing()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Inner boundaries (holes)
     */
    INNER_BOUNDARIES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new KMLLinearRing()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    innerBoundariesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Altitude mode
     */
    ALTITUDE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
    }
    /**
     * Whether to extrude to ground
     */
    EXTRUDE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Whether to tessellate
     */
    TESSELLATE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startKMLPolygon(builder) {
        builder.startObject(5);
    }
    static addOuterBoundary(builder, OUTER_BOUNDARYOffset) {
        builder.addFieldOffset(0, OUTER_BOUNDARYOffset, 0);
    }
    static addInnerBoundaries(builder, INNER_BOUNDARIESOffset) {
        builder.addFieldOffset(1, INNER_BOUNDARIESOffset, 0);
    }
    static createInnerBoundariesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startInnerBoundariesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAltitudeMode(builder, ALTITUDE_MODE) {
        builder.addFieldInt8(2, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
    }
    static addExtrude(builder, EXTRUDE) {
        builder.addFieldInt8(3, +EXTRUDE, +false);
    }
    static addTessellate(builder, TESSELLATE) {
        builder.addFieldInt8(4, +TESSELLATE, +false);
    }
    static endKMLPolygon(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLPolygon(builder, OUTER_BOUNDARYOffset, INNER_BOUNDARIESOffset, ALTITUDE_MODE, EXTRUDE, TESSELLATE) {
        KMLPolygon.startKMLPolygon(builder);
        KMLPolygon.addOuterBoundary(builder, OUTER_BOUNDARYOffset);
        KMLPolygon.addInnerBoundaries(builder, INNER_BOUNDARIESOffset);
        KMLPolygon.addAltitudeMode(builder, ALTITUDE_MODE);
        KMLPolygon.addExtrude(builder, EXTRUDE);
        KMLPolygon.addTessellate(builder, TESSELLATE);
        return KMLPolygon.endKMLPolygon(builder);
    }
    unpack() {
        return new KMLPolygonT((this.OUTER_BOUNDARY() !== null ? this.OUTER_BOUNDARY().unpack() : null), this.bb.createObjList(this.INNER_BOUNDARIES.bind(this), this.innerBoundariesLength()), this.ALTITUDE_MODE(), this.EXTRUDE(), this.TESSELLATE());
    }
    unpackTo(_o) {
        _o.OUTER_BOUNDARY = (this.OUTER_BOUNDARY() !== null ? this.OUTER_BOUNDARY().unpack() : null);
        _o.INNER_BOUNDARIES = this.bb.createObjList(this.INNER_BOUNDARIES.bind(this), this.innerBoundariesLength());
        _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
        _o.EXTRUDE = this.EXTRUDE();
        _o.TESSELLATE = this.TESSELLATE();
    }
}
export class KMLPolygonT {
    constructor(OUTER_BOUNDARY = null, INNER_BOUNDARIES = [], ALTITUDE_MODE = KMLAltitudeMode.CLAMP_TO_GROUND, EXTRUDE = false, TESSELLATE = false) {
        this.OUTER_BOUNDARY = OUTER_BOUNDARY;
        this.INNER_BOUNDARIES = INNER_BOUNDARIES;
        this.ALTITUDE_MODE = ALTITUDE_MODE;
        this.EXTRUDE = EXTRUDE;
        this.TESSELLATE = TESSELLATE;
    }
    pack(builder) {
        const OUTER_BOUNDARY = (this.OUTER_BOUNDARY !== null ? this.OUTER_BOUNDARY.pack(builder) : 0);
        const INNER_BOUNDARIES = KMLPolygon.createInnerBoundariesVector(builder, builder.createObjectOffsetList(this.INNER_BOUNDARIES));
        return KMLPolygon.createKMLPolygon(builder, OUTER_BOUNDARY, INNER_BOUNDARIES, this.ALTITUDE_MODE, this.EXTRUDE, this.TESSELLATE);
    }
}
