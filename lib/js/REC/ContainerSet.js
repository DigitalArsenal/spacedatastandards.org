// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SequenceContainer } from './SequenceContainer.js';
/**
 * Collection of sequence containers
 */
export class ContainerSet {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsContainerSet(bb, obj) {
        return (obj || new ContainerSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsContainerSet(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ContainerSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Sequence containers
     */
    CONTAINERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new SequenceContainer()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    containersLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startContainerSet(builder) {
        builder.startObject(1);
    }
    static addContainers(builder, CONTAINERSOffset) {
        builder.addFieldOffset(0, CONTAINERSOffset, 0);
    }
    static createContainersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startContainersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endContainerSet(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createContainerSet(builder, CONTAINERSOffset) {
        ContainerSet.startContainerSet(builder);
        ContainerSet.addContainers(builder, CONTAINERSOffset);
        return ContainerSet.endContainerSet(builder);
    }
    unpack() {
        return new ContainerSetT(this.bb.createObjList(this.CONTAINERS.bind(this), this.containersLength()));
    }
    unpackTo(_o) {
        _o.CONTAINERS = this.bb.createObjList(this.CONTAINERS.bind(this), this.containersLength());
    }
}
export class ContainerSetT {
    constructor(CONTAINERS = []) {
        this.CONTAINERS = CONTAINERS;
    }
    pack(builder) {
        const CONTAINERS = ContainerSet.createContainersVector(builder, builder.createObjectOffsetList(this.CONTAINERS));
        return ContainerSet.createContainerSet(builder, CONTAINERS);
    }
}
