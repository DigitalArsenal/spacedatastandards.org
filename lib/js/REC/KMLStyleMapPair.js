// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLStyleState } from './KMLStyleState.js';
/**
 * Style map pair
 */
export class KMLStyleMapPair {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLStyleMapPair(bb, obj) {
        return (obj || new KMLStyleMapPair()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLStyleMapPair(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLStyleMapPair()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * State (normal or highlight)
     */
    STATE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLStyleState.NORMAL;
    }
    STYLE_URL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startKMLStyleMapPair(builder) {
        builder.startObject(2);
    }
    static addState(builder, STATE) {
        builder.addFieldInt8(0, STATE, KMLStyleState.NORMAL);
    }
    static addStyleUrl(builder, STYLE_URLOffset) {
        builder.addFieldOffset(1, STYLE_URLOffset, 0);
    }
    static endKMLStyleMapPair(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLStyleMapPair(builder, STATE, STYLE_URLOffset) {
        KMLStyleMapPair.startKMLStyleMapPair(builder);
        KMLStyleMapPair.addState(builder, STATE);
        KMLStyleMapPair.addStyleUrl(builder, STYLE_URLOffset);
        return KMLStyleMapPair.endKMLStyleMapPair(builder);
    }
    unpack() {
        return new KMLStyleMapPairT(this.STATE(), this.STYLE_URL());
    }
    unpackTo(_o) {
        _o.STATE = this.STATE();
        _o.STYLE_URL = this.STYLE_URL();
    }
}
export class KMLStyleMapPairT {
    constructor(STATE = KMLStyleState.NORMAL, STYLE_URL = null) {
        this.STATE = STATE;
        this.STYLE_URL = STYLE_URL;
    }
    pack(builder) {
        const STYLE_URL = (this.STYLE_URL !== null ? builder.createString(this.STYLE_URL) : 0);
        return KMLStyleMapPair.createKMLStyleMapPair(builder, this.STATE, STYLE_URL);
    }
}
