// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SCX } from './SCX.js';
import { recurringChargeMethod } from './recurringChargeMethod.js';
import { subscriptionLifecycleStatus } from './subscriptionLifecycleStatus.js';
/**
 * Crypto-native Subscription Authorization
 */
export class SUB {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSUB(bb, obj) {
        return (obj || new SUB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSUB(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SUB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$SUB');
    }
    SUBSCRIPTION_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    ENTITLEMENT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    LISTING_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * On-chain recurring mechanism
     */
    AUTH_KIND() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : recurringChargeMethod.Erc20Allowance;
    }
    /**
     * Settlement descriptor (chain, splitter, token) that charges each period
     */
    CHAIN_SETTLEMENT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new SCX()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Billing period length in days (mirrors `$PLG.SUBSCRIPTION_PERIOD_DAYS`)
     */
    PERIOD_DAYS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Charge per period in the smallest unit of CURRENCY
     */
    AMOUNT_PER_PERIOD() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    CURRENCY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Unix ms of the next scheduled charge
     */
    NEXT_CHARGE_AT() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Maximum number of periods; 0 = until cancelled
     */
    MAX_PERIODS() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Subscription lifecycle status
     */
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : subscriptionLifecycleStatus.Active;
    }
    /**
     * Buyer's Ed25519 authorization signature over the terms
     */
    SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startSUB(builder) {
        builder.startObject(12);
    }
    static addSubscriptionId(builder, SUBSCRIPTION_IDOffset) {
        builder.addFieldOffset(0, SUBSCRIPTION_IDOffset, 0);
    }
    static addEntitlementId(builder, ENTITLEMENT_IDOffset) {
        builder.addFieldOffset(1, ENTITLEMENT_IDOffset, 0);
    }
    static addListingId(builder, LISTING_IDOffset) {
        builder.addFieldOffset(2, LISTING_IDOffset, 0);
    }
    static addAuthKind(builder, AUTH_KIND) {
        builder.addFieldInt8(3, AUTH_KIND, recurringChargeMethod.Erc20Allowance);
    }
    static addChainSettlement(builder, CHAIN_SETTLEMENTOffset) {
        builder.addFieldOffset(4, CHAIN_SETTLEMENTOffset, 0);
    }
    static addPeriodDays(builder, PERIOD_DAYS) {
        builder.addFieldInt32(5, PERIOD_DAYS, 0);
    }
    static addAmountPerPeriod(builder, AMOUNT_PER_PERIOD) {
        builder.addFieldInt64(6, AMOUNT_PER_PERIOD, BigInt('0'));
    }
    static addCurrency(builder, CURRENCYOffset) {
        builder.addFieldOffset(7, CURRENCYOffset, 0);
    }
    static addNextChargeAt(builder, NEXT_CHARGE_AT) {
        builder.addFieldInt64(8, NEXT_CHARGE_AT, BigInt('0'));
    }
    static addMaxPeriods(builder, MAX_PERIODS) {
        builder.addFieldInt32(9, MAX_PERIODS, 0);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(10, STATUS, subscriptionLifecycleStatus.Active);
    }
    static addSignature(builder, SIGNATUREOffset) {
        builder.addFieldOffset(11, SIGNATUREOffset, 0);
    }
    static createSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endSUB(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // SUBSCRIPTION_ID
        return offset;
    }
    static finishSUBBuffer(builder, offset) {
        builder.finish(offset, '$SUB');
    }
    static finishSizePrefixedSUBBuffer(builder, offset) {
        builder.finish(offset, '$SUB', true);
    }
    unpack() {
        return new SUBT(this.SUBSCRIPTION_ID(), this.ENTITLEMENT_ID(), this.LISTING_ID(), this.AUTH_KIND(), (this.CHAIN_SETTLEMENT() !== null ? this.CHAIN_SETTLEMENT().unpack() : null), this.PERIOD_DAYS(), this.AMOUNT_PER_PERIOD(), this.CURRENCY(), this.NEXT_CHARGE_AT(), this.MAX_PERIODS(), this.STATUS(), this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength()));
    }
    unpackTo(_o) {
        _o.SUBSCRIPTION_ID = this.SUBSCRIPTION_ID();
        _o.ENTITLEMENT_ID = this.ENTITLEMENT_ID();
        _o.LISTING_ID = this.LISTING_ID();
        _o.AUTH_KIND = this.AUTH_KIND();
        _o.CHAIN_SETTLEMENT = (this.CHAIN_SETTLEMENT() !== null ? this.CHAIN_SETTLEMENT().unpack() : null);
        _o.PERIOD_DAYS = this.PERIOD_DAYS();
        _o.AMOUNT_PER_PERIOD = this.AMOUNT_PER_PERIOD();
        _o.CURRENCY = this.CURRENCY();
        _o.NEXT_CHARGE_AT = this.NEXT_CHARGE_AT();
        _o.MAX_PERIODS = this.MAX_PERIODS();
        _o.STATUS = this.STATUS();
        _o.SIGNATURE = this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength());
    }
}
export class SUBT {
    constructor(SUBSCRIPTION_ID = null, ENTITLEMENT_ID = null, LISTING_ID = null, AUTH_KIND = recurringChargeMethod.Erc20Allowance, CHAIN_SETTLEMENT = null, PERIOD_DAYS = 0, AMOUNT_PER_PERIOD = BigInt('0'), CURRENCY = null, NEXT_CHARGE_AT = BigInt('0'), MAX_PERIODS = 0, STATUS = subscriptionLifecycleStatus.Active, SIGNATURE = []) {
        this.SUBSCRIPTION_ID = SUBSCRIPTION_ID;
        this.ENTITLEMENT_ID = ENTITLEMENT_ID;
        this.LISTING_ID = LISTING_ID;
        this.AUTH_KIND = AUTH_KIND;
        this.CHAIN_SETTLEMENT = CHAIN_SETTLEMENT;
        this.PERIOD_DAYS = PERIOD_DAYS;
        this.AMOUNT_PER_PERIOD = AMOUNT_PER_PERIOD;
        this.CURRENCY = CURRENCY;
        this.NEXT_CHARGE_AT = NEXT_CHARGE_AT;
        this.MAX_PERIODS = MAX_PERIODS;
        this.STATUS = STATUS;
        this.SIGNATURE = SIGNATURE;
    }
    pack(builder) {
        const SUBSCRIPTION_ID = (this.SUBSCRIPTION_ID !== null ? builder.createString(this.SUBSCRIPTION_ID) : 0);
        const ENTITLEMENT_ID = (this.ENTITLEMENT_ID !== null ? builder.createString(this.ENTITLEMENT_ID) : 0);
        const LISTING_ID = (this.LISTING_ID !== null ? builder.createString(this.LISTING_ID) : 0);
        const CHAIN_SETTLEMENT = (this.CHAIN_SETTLEMENT !== null ? this.CHAIN_SETTLEMENT.pack(builder) : 0);
        const CURRENCY = (this.CURRENCY !== null ? builder.createString(this.CURRENCY) : 0);
        const SIGNATURE = SUB.createSignatureVector(builder, this.SIGNATURE);
        SUB.startSUB(builder);
        SUB.addSubscriptionId(builder, SUBSCRIPTION_ID);
        SUB.addEntitlementId(builder, ENTITLEMENT_ID);
        SUB.addListingId(builder, LISTING_ID);
        SUB.addAuthKind(builder, this.AUTH_KIND);
        SUB.addChainSettlement(builder, CHAIN_SETTLEMENT);
        SUB.addPeriodDays(builder, this.PERIOD_DAYS);
        SUB.addAmountPerPeriod(builder, this.AMOUNT_PER_PERIOD);
        SUB.addCurrency(builder, CURRENCY);
        SUB.addNextChargeAt(builder, this.NEXT_CHARGE_AT);
        SUB.addMaxPeriods(builder, this.MAX_PERIODS);
        SUB.addStatus(builder, this.STATUS);
        SUB.addSignature(builder, SIGNATURE);
        return SUB.endSUB(builder);
    }
}
