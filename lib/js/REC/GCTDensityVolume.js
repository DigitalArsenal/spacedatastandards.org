// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * A baked density volume of a variant on the same cube as its distance
 * field, for ray marching: N^3 samples of amplitude in AMPLITUDE_UNITS,
 * float16, x fastest. Optional.
 */
export class GCTDensityVolume {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGCTDensityVolume(bb, obj) {
        return (obj || new GCTDensityVolume()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGCTDensityVolume(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GCTDensityVolume()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Voxels per axis.
     */
    N() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Origin of voxel (0, 0, 0) in the variant frame, metres (3 values).
     */
    ORIGIN_M(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    originMLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    originMArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Extent of the cube along each axis, metres (3 values).
     */
    SPAN_M(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    spanMLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    spanMArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Float32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * N^3 IEEE 754 half-precision samples as their 16-bit patterns, x
     * fastest, then y, then z. Absent when the volume travels as the chunk
     * named by CHUNK_CID.
     */
    DATA(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint16(this.bb.__vector(this.bb_pos + offset) + index * 2) : 0;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    dataArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Uint16Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    CHUNK_CID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startGCTDensityVolume(builder) {
        builder.startObject(5);
    }
    static addN(builder, N) {
        builder.addFieldInt16(0, N, 0);
    }
    static addOriginM(builder, ORIGIN_MOffset) {
        builder.addFieldOffset(1, ORIGIN_MOffset, 0);
    }
    static createOriginMVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startOriginMVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSpanM(builder, SPAN_MOffset) {
        builder.addFieldOffset(2, SPAN_MOffset, 0);
    }
    static createSpanMVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat32(data[i]);
        }
        return builder.endVector();
    }
    static startSpanMVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(3, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(2, data.length, 2);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt16(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(2, numElems, 2);
    }
    static addChunkCid(builder, CHUNK_CIDOffset) {
        builder.addFieldOffset(4, CHUNK_CIDOffset, 0);
    }
    static endGCTDensityVolume(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGCTDensityVolume(builder, N, ORIGIN_MOffset, SPAN_MOffset, DATAOffset, CHUNK_CIDOffset) {
        GCTDensityVolume.startGCTDensityVolume(builder);
        GCTDensityVolume.addN(builder, N);
        GCTDensityVolume.addOriginM(builder, ORIGIN_MOffset);
        GCTDensityVolume.addSpanM(builder, SPAN_MOffset);
        GCTDensityVolume.addData(builder, DATAOffset);
        GCTDensityVolume.addChunkCid(builder, CHUNK_CIDOffset);
        return GCTDensityVolume.endGCTDensityVolume(builder);
    }
    unpack() {
        return new GCTDensityVolumeT(this.N(), this.bb.createScalarList(this.ORIGIN_M.bind(this), this.originMLength()), this.bb.createScalarList(this.SPAN_M.bind(this), this.spanMLength()), this.bb.createScalarList(this.DATA.bind(this), this.dataLength()), this.CHUNK_CID());
    }
    unpackTo(_o) {
        _o.N = this.N();
        _o.ORIGIN_M = this.bb.createScalarList(this.ORIGIN_M.bind(this), this.originMLength());
        _o.SPAN_M = this.bb.createScalarList(this.SPAN_M.bind(this), this.spanMLength());
        _o.DATA = this.bb.createScalarList(this.DATA.bind(this), this.dataLength());
        _o.CHUNK_CID = this.CHUNK_CID();
    }
}
export class GCTDensityVolumeT {
    constructor(N = 0, ORIGIN_M = [], SPAN_M = [], DATA = [], CHUNK_CID = null) {
        this.N = N;
        this.ORIGIN_M = ORIGIN_M;
        this.SPAN_M = SPAN_M;
        this.DATA = DATA;
        this.CHUNK_CID = CHUNK_CID;
    }
    pack(builder) {
        const ORIGIN_M = GCTDensityVolume.createOriginMVector(builder, this.ORIGIN_M);
        const SPAN_M = GCTDensityVolume.createSpanMVector(builder, this.SPAN_M);
        const DATA = GCTDensityVolume.createDataVector(builder, this.DATA);
        const CHUNK_CID = (this.CHUNK_CID !== null ? builder.createString(this.CHUNK_CID) : 0);
        return GCTDensityVolume.createGCTDensityVolume(builder, this.N, ORIGIN_M, SPAN_M, DATA, CHUNK_CID);
    }
}
