// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { CustomStream } from './CustomStream.js';
import { FixedFrameStream } from './FixedFrameStream.js';
import { VariableFrameStream } from './VariableFrameStream.js';
/**
 * Collection of streams
 */
export class StreamSet {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsStreamSet(bb, obj) {
        return (obj || new StreamSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsStreamSet(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new StreamSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Fixed frame streams
     */
    FIXED_FRAME_STREAMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new FixedFrameStream()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    fixedFrameStreamsLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Variable frame streams
     */
    VARIABLE_FRAME_STREAMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new VariableFrameStream()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    variableFrameStreamsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Custom streams
     */
    CUSTOM_STREAMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new CustomStream()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    customStreamsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startStreamSet(builder) {
        builder.startObject(3);
    }
    static addFixedFrameStreams(builder, FIXED_FRAME_STREAMSOffset) {
        builder.addFieldOffset(0, FIXED_FRAME_STREAMSOffset, 0);
    }
    static createFixedFrameStreamsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startFixedFrameStreamsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addVariableFrameStreams(builder, VARIABLE_FRAME_STREAMSOffset) {
        builder.addFieldOffset(1, VARIABLE_FRAME_STREAMSOffset, 0);
    }
    static createVariableFrameStreamsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startVariableFrameStreamsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCustomStreams(builder, CUSTOM_STREAMSOffset) {
        builder.addFieldOffset(2, CUSTOM_STREAMSOffset, 0);
    }
    static createCustomStreamsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startCustomStreamsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endStreamSet(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createStreamSet(builder, FIXED_FRAME_STREAMSOffset, VARIABLE_FRAME_STREAMSOffset, CUSTOM_STREAMSOffset) {
        StreamSet.startStreamSet(builder);
        StreamSet.addFixedFrameStreams(builder, FIXED_FRAME_STREAMSOffset);
        StreamSet.addVariableFrameStreams(builder, VARIABLE_FRAME_STREAMSOffset);
        StreamSet.addCustomStreams(builder, CUSTOM_STREAMSOffset);
        return StreamSet.endStreamSet(builder);
    }
    unpack() {
        return new StreamSetT(this.bb.createObjList(this.FIXED_FRAME_STREAMS.bind(this), this.fixedFrameStreamsLength()), this.bb.createObjList(this.VARIABLE_FRAME_STREAMS.bind(this), this.variableFrameStreamsLength()), this.bb.createObjList(this.CUSTOM_STREAMS.bind(this), this.customStreamsLength()));
    }
    unpackTo(_o) {
        _o.FIXED_FRAME_STREAMS = this.bb.createObjList(this.FIXED_FRAME_STREAMS.bind(this), this.fixedFrameStreamsLength());
        _o.VARIABLE_FRAME_STREAMS = this.bb.createObjList(this.VARIABLE_FRAME_STREAMS.bind(this), this.variableFrameStreamsLength());
        _o.CUSTOM_STREAMS = this.bb.createObjList(this.CUSTOM_STREAMS.bind(this), this.customStreamsLength());
    }
}
export class StreamSetT {
    constructor(FIXED_FRAME_STREAMS = [], VARIABLE_FRAME_STREAMS = [], CUSTOM_STREAMS = []) {
        this.FIXED_FRAME_STREAMS = FIXED_FRAME_STREAMS;
        this.VARIABLE_FRAME_STREAMS = VARIABLE_FRAME_STREAMS;
        this.CUSTOM_STREAMS = CUSTOM_STREAMS;
    }
    pack(builder) {
        const FIXED_FRAME_STREAMS = StreamSet.createFixedFrameStreamsVector(builder, builder.createObjectOffsetList(this.FIXED_FRAME_STREAMS));
        const VARIABLE_FRAME_STREAMS = StreamSet.createVariableFrameStreamsVector(builder, builder.createObjectOffsetList(this.VARIABLE_FRAME_STREAMS));
        const CUSTOM_STREAMS = StreamSet.createCustomStreamsVector(builder, builder.createObjectOffsetList(this.CUSTOM_STREAMS));
        return StreamSet.createStreamSet(builder, FIXED_FRAME_STREAMS, VARIABLE_FRAME_STREAMS, CUSTOM_STREAMS);
    }
}
