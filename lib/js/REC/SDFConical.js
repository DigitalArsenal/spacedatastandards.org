// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Conical SDF parameters.
 */
export class SDFConical {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSDFConical(bb, obj) {
        return (obj || new SDFConical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSDFConical(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SDFConical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    HALF_ANGLE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Inner half-angle for hollow cones (0 == solid).
     */
    INNER_HALF_ANGLE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Minimum range (inner cap distance).
     */
    MIN_RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum range (outer cap distance).
     */
    MAX_RANGE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Use a flat cap (true) or a spherical cap (false).
     */
    FLAT_CAP() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startSDFConical(builder) {
        builder.startObject(5);
    }
    static addHalfAngle(builder, HALF_ANGLE) {
        builder.addFieldFloat64(0, HALF_ANGLE, 0.0);
    }
    static addInnerHalfAngle(builder, INNER_HALF_ANGLE) {
        builder.addFieldFloat64(1, INNER_HALF_ANGLE, 0.0);
    }
    static addMinRange(builder, MIN_RANGE) {
        builder.addFieldFloat64(2, MIN_RANGE, 0.0);
    }
    static addMaxRange(builder, MAX_RANGE) {
        builder.addFieldFloat64(3, MAX_RANGE, 0.0);
    }
    static addFlatCap(builder, FLAT_CAP) {
        builder.addFieldInt8(4, +FLAT_CAP, +false);
    }
    static endSDFConical(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSDFConical(builder, HALF_ANGLE, INNER_HALF_ANGLE, MIN_RANGE, MAX_RANGE, FLAT_CAP) {
        SDFConical.startSDFConical(builder);
        SDFConical.addHalfAngle(builder, HALF_ANGLE);
        SDFConical.addInnerHalfAngle(builder, INNER_HALF_ANGLE);
        SDFConical.addMinRange(builder, MIN_RANGE);
        SDFConical.addMaxRange(builder, MAX_RANGE);
        SDFConical.addFlatCap(builder, FLAT_CAP);
        return SDFConical.endSDFConical(builder);
    }
    unpack() {
        return new SDFConicalT(this.HALF_ANGLE(), this.INNER_HALF_ANGLE(), this.MIN_RANGE(), this.MAX_RANGE(), this.FLAT_CAP());
    }
    unpackTo(_o) {
        _o.HALF_ANGLE = this.HALF_ANGLE();
        _o.INNER_HALF_ANGLE = this.INNER_HALF_ANGLE();
        _o.MIN_RANGE = this.MIN_RANGE();
        _o.MAX_RANGE = this.MAX_RANGE();
        _o.FLAT_CAP = this.FLAT_CAP();
    }
}
export class SDFConicalT {
    constructor(HALF_ANGLE = 0.0, INNER_HALF_ANGLE = 0.0, MIN_RANGE = 0.0, MAX_RANGE = 0.0, FLAT_CAP = false) {
        this.HALF_ANGLE = HALF_ANGLE;
        this.INNER_HALF_ANGLE = INNER_HALF_ANGLE;
        this.MIN_RANGE = MIN_RANGE;
        this.MAX_RANGE = MAX_RANGE;
        this.FLAT_CAP = FLAT_CAP;
    }
    pack(builder) {
        return SDFConical.createSDFConical(builder, this.HALF_ANGLE, this.INNER_HALF_ANGLE, this.MIN_RANGE, this.MAX_RANGE, this.FLAT_CAP);
    }
}
