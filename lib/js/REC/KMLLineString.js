// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLCoordinate } from './KMLCoordinate.js';
/**
 * LineString geometry
 */
export class KMLLineString {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLLineString(bb, obj) {
        return (obj || new KMLLineString()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLLineString(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLLineString()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Coordinates
     */
    COORDINATES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new KMLCoordinate()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    coordinatesLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Altitude mode
     */
    ALTITUDE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
    }
    /**
     * Whether to extrude to ground
     */
    EXTRUDE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Whether to tessellate (follow terrain)
     */
    TESSELLATE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startKMLLineString(builder) {
        builder.startObject(4);
    }
    static addCoordinates(builder, COORDINATESOffset) {
        builder.addFieldOffset(0, COORDINATESOffset, 0);
    }
    static createCoordinatesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startCoordinatesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAltitudeMode(builder, ALTITUDE_MODE) {
        builder.addFieldInt8(1, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
    }
    static addExtrude(builder, EXTRUDE) {
        builder.addFieldInt8(2, +EXTRUDE, +false);
    }
    static addTessellate(builder, TESSELLATE) {
        builder.addFieldInt8(3, +TESSELLATE, +false);
    }
    static endKMLLineString(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLLineString(builder, COORDINATESOffset, ALTITUDE_MODE, EXTRUDE, TESSELLATE) {
        KMLLineString.startKMLLineString(builder);
        KMLLineString.addCoordinates(builder, COORDINATESOffset);
        KMLLineString.addAltitudeMode(builder, ALTITUDE_MODE);
        KMLLineString.addExtrude(builder, EXTRUDE);
        KMLLineString.addTessellate(builder, TESSELLATE);
        return KMLLineString.endKMLLineString(builder);
    }
    unpack() {
        return new KMLLineStringT(this.bb.createObjList(this.COORDINATES.bind(this), this.coordinatesLength()), this.ALTITUDE_MODE(), this.EXTRUDE(), this.TESSELLATE());
    }
    unpackTo(_o) {
        _o.COORDINATES = this.bb.createObjList(this.COORDINATES.bind(this), this.coordinatesLength());
        _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
        _o.EXTRUDE = this.EXTRUDE();
        _o.TESSELLATE = this.TESSELLATE();
    }
}
export class KMLLineStringT {
    constructor(COORDINATES = [], ALTITUDE_MODE = KMLAltitudeMode.CLAMP_TO_GROUND, EXTRUDE = false, TESSELLATE = false) {
        this.COORDINATES = COORDINATES;
        this.ALTITUDE_MODE = ALTITUDE_MODE;
        this.EXTRUDE = EXTRUDE;
        this.TESSELLATE = TESSELLATE;
    }
    pack(builder) {
        const COORDINATES = KMLLineString.createCoordinatesVector(builder, builder.createObjectOffsetList(this.COORDINATES));
        return KMLLineString.createKMLLineString(builder, COORDINATES, this.ALTITUDE_MODE, this.EXTRUDE, this.TESSELLATE);
    }
}
