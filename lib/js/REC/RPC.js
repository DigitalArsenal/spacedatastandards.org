// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { ENC } from './ENC.js';
import { rpcDirection } from './rpcDirection.js';
/**
 * Sealed Remote Procedure Call envelope (outer, plaintext, signed).
 */
export class RPC {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsRPC(bb, obj) {
        return (obj || new RPC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsRPC(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new RPC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$RPC');
    }
    /**
     * Envelope version. This document defines version 1.
     */
    VERSION() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 1;
    }
    /**
     * Request or response.
     */
    DIRECTION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : rpcDirection.Request;
    }
    /**
     * Encryption header for CIPHERTEXT. CONTEXT is "RPC/1|request" or
     * "RPC/1|response" matching DIRECTION.
     */
    ENCRYPTION(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new ENC()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    SESSION_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Optional identifier of the sender's key (routing hint only; the binding
     * identity is SIGNER_PUBLIC_KEY checked against RPCBody.SIGNER_KEY_ID).
     */
    SENDER_KEY_ID(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    senderKeyIdLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    senderKeyIdArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Sender clock at signing, milliseconds since the Unix epoch (UTC).
     */
    TIMESTAMP() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Random 16-byte nonce, unique per envelope.
     */
    NONCE(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    nonceLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    nonceArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Encrypted size-prefixed RPCBody.
     */
    CIPHERTEXT(index) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    ciphertextLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ciphertextArray() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    /**
     * Ed25519 public key (32 bytes) that verifies both signatures.
     */
    SIGNER_PUBLIC_KEY(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signerPublicKeyLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signerPublicKeyArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    SIGNATURE_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Ed25519 signature over the size-prefixed FlatBuffer with both 64-byte
     * signature payloads zeroed while preserving their vectors and offsets.
     */
    SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    /**
     * Ed25519 signature over canonical JSON with IDL field order and
     * capitalization, no insignificant whitespace, and both signature fields
     * omitted.
     */
    CANONICAL_JSON_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    canonicalJsonSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    canonicalJsonSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    static startRPC(builder) {
        builder.startObject(12);
    }
    static addVersion(builder, VERSION) {
        builder.addFieldInt8(0, VERSION, 1);
    }
    static addDirection(builder, DIRECTION) {
        builder.addFieldInt8(1, DIRECTION, rpcDirection.Request);
    }
    static addEncryption(builder, ENCRYPTIONOffset) {
        builder.addFieldOffset(2, ENCRYPTIONOffset, 0);
    }
    static addSessionId(builder, SESSION_IDOffset) {
        builder.addFieldOffset(3, SESSION_IDOffset, 0);
    }
    static addSenderKeyId(builder, SENDER_KEY_IDOffset) {
        builder.addFieldOffset(4, SENDER_KEY_IDOffset, 0);
    }
    static createSenderKeyIdVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSenderKeyIdVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addTimestamp(builder, TIMESTAMP) {
        builder.addFieldInt64(5, TIMESTAMP, BigInt('0'));
    }
    static addNonce(builder, NONCEOffset) {
        builder.addFieldOffset(6, NONCEOffset, 0);
    }
    static createNonceVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startNonceVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addCiphertext(builder, CIPHERTEXTOffset) {
        builder.addFieldOffset(7, CIPHERTEXTOffset, 0);
    }
    static createCiphertextVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startCiphertextVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addSignerPublicKey(builder, SIGNER_PUBLIC_KEYOffset) {
        builder.addFieldOffset(8, SIGNER_PUBLIC_KEYOffset, 0);
    }
    static createSignerPublicKeyVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignerPublicKeyVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addSignatureType(builder, SIGNATURE_TYPEOffset) {
        builder.addFieldOffset(9, SIGNATURE_TYPEOffset, 0);
    }
    static addSignature(builder, SIGNATUREOffset) {
        builder.addFieldOffset(10, SIGNATUREOffset, 0);
    }
    static createSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATUREOffset) {
        builder.addFieldOffset(11, CANONICAL_JSON_SIGNATUREOffset, 0);
    }
    static createCanonicalJsonSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startCanonicalJsonSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endRPC(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 8); // ENCRYPTION
        builder.requiredField(offset, 18); // CIPHERTEXT
        builder.requiredField(offset, 20); // SIGNER_PUBLIC_KEY
        builder.requiredField(offset, 24); // SIGNATURE
        builder.requiredField(offset, 26); // CANONICAL_JSON_SIGNATURE
        return offset;
    }
    static finishRPCBuffer(builder, offset) {
        builder.finish(offset, '$RPC');
    }
    static finishSizePrefixedRPCBuffer(builder, offset) {
        builder.finish(offset, '$RPC', true);
    }
    unpack() {
        return new RPCT(this.VERSION(), this.DIRECTION(), (this.ENCRYPTION() !== null ? this.ENCRYPTION().unpack() : null), this.SESSION_ID(), this.bb.createScalarList(this.SENDER_KEY_ID.bind(this), this.senderKeyIdLength()), this.TIMESTAMP(), this.bb.createScalarList(this.NONCE.bind(this), this.nonceLength()), this.bb.createScalarList(this.CIPHERTEXT.bind(this), this.ciphertextLength()), this.bb.createScalarList(this.SIGNER_PUBLIC_KEY.bind(this), this.signerPublicKeyLength()), this.SIGNATURE_TYPE(), this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength()), this.bb.createScalarList(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength()));
    }
    unpackTo(_o) {
        _o.VERSION = this.VERSION();
        _o.DIRECTION = this.DIRECTION();
        _o.ENCRYPTION = (this.ENCRYPTION() !== null ? this.ENCRYPTION().unpack() : null);
        _o.SESSION_ID = this.SESSION_ID();
        _o.SENDER_KEY_ID = this.bb.createScalarList(this.SENDER_KEY_ID.bind(this), this.senderKeyIdLength());
        _o.TIMESTAMP = this.TIMESTAMP();
        _o.NONCE = this.bb.createScalarList(this.NONCE.bind(this), this.nonceLength());
        _o.CIPHERTEXT = this.bb.createScalarList(this.CIPHERTEXT.bind(this), this.ciphertextLength());
        _o.SIGNER_PUBLIC_KEY = this.bb.createScalarList(this.SIGNER_PUBLIC_KEY.bind(this), this.signerPublicKeyLength());
        _o.SIGNATURE_TYPE = this.SIGNATURE_TYPE();
        _o.SIGNATURE = this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength());
        _o.CANONICAL_JSON_SIGNATURE = this.bb.createScalarList(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength());
    }
}
export class RPCT {
    constructor(VERSION = 1, DIRECTION = rpcDirection.Request, ENCRYPTION = null, SESSION_ID = null, SENDER_KEY_ID = [], TIMESTAMP = BigInt('0'), NONCE = [], CIPHERTEXT = [], SIGNER_PUBLIC_KEY = [], SIGNATURE_TYPE = null, SIGNATURE = [], CANONICAL_JSON_SIGNATURE = []) {
        this.VERSION = VERSION;
        this.DIRECTION = DIRECTION;
        this.ENCRYPTION = ENCRYPTION;
        this.SESSION_ID = SESSION_ID;
        this.SENDER_KEY_ID = SENDER_KEY_ID;
        this.TIMESTAMP = TIMESTAMP;
        this.NONCE = NONCE;
        this.CIPHERTEXT = CIPHERTEXT;
        this.SIGNER_PUBLIC_KEY = SIGNER_PUBLIC_KEY;
        this.SIGNATURE_TYPE = SIGNATURE_TYPE;
        this.SIGNATURE = SIGNATURE;
        this.CANONICAL_JSON_SIGNATURE = CANONICAL_JSON_SIGNATURE;
    }
    pack(builder) {
        const ENCRYPTION = (this.ENCRYPTION !== null ? this.ENCRYPTION.pack(builder) : 0);
        const SESSION_ID = (this.SESSION_ID !== null ? builder.createString(this.SESSION_ID) : 0);
        const SENDER_KEY_ID = RPC.createSenderKeyIdVector(builder, this.SENDER_KEY_ID);
        const NONCE = RPC.createNonceVector(builder, this.NONCE);
        const CIPHERTEXT = RPC.createCiphertextVector(builder, this.CIPHERTEXT);
        const SIGNER_PUBLIC_KEY = RPC.createSignerPublicKeyVector(builder, this.SIGNER_PUBLIC_KEY);
        const SIGNATURE_TYPE = (this.SIGNATURE_TYPE !== null ? builder.createString(this.SIGNATURE_TYPE) : 0);
        const SIGNATURE = RPC.createSignatureVector(builder, this.SIGNATURE);
        const CANONICAL_JSON_SIGNATURE = RPC.createCanonicalJsonSignatureVector(builder, this.CANONICAL_JSON_SIGNATURE);
        RPC.startRPC(builder);
        RPC.addVersion(builder, this.VERSION);
        RPC.addDirection(builder, this.DIRECTION);
        RPC.addEncryption(builder, ENCRYPTION);
        RPC.addSessionId(builder, SESSION_ID);
        RPC.addSenderKeyId(builder, SENDER_KEY_ID);
        RPC.addTimestamp(builder, this.TIMESTAMP);
        RPC.addNonce(builder, NONCE);
        RPC.addCiphertext(builder, CIPHERTEXT);
        RPC.addSignerPublicKey(builder, SIGNER_PUBLIC_KEY);
        RPC.addSignatureType(builder, SIGNATURE_TYPE);
        RPC.addSignature(builder, SIGNATURE);
        RPC.addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATURE);
        return RPC.endRPC(builder);
    }
}
