// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { ACWAccessWindow } from './ACWAccessWindow.js';
import { acwResultStatus } from './acwResultStatus.js';
/**
 * Result for one access-window compute request.
 */
export class ACWResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsACWResult(bb, obj) {
        return (obj || new ACWResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsACWResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ACWResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : acwResultStatus.OK;
    }
    ERROR_MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    WINDOWS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new ACWAccessWindow()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    windowsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startACWResult(builder) {
        builder.startObject(4);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(0, STATUS, acwResultStatus.OK);
    }
    static addErrorMessage(builder, ERROR_MESSAGEOffset) {
        builder.addFieldOffset(1, ERROR_MESSAGEOffset, 0);
    }
    static addWindows(builder, WINDOWSOffset) {
        builder.addFieldOffset(2, WINDOWSOffset, 0);
    }
    static createWindowsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startWindowsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(3, TRACE_IDOffset, 0);
    }
    static endACWResult(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createACWResult(builder, STATUS, ERROR_MESSAGEOffset, WINDOWSOffset, TRACE_IDOffset) {
        ACWResult.startACWResult(builder);
        ACWResult.addStatus(builder, STATUS);
        ACWResult.addErrorMessage(builder, ERROR_MESSAGEOffset);
        ACWResult.addWindows(builder, WINDOWSOffset);
        ACWResult.addTraceId(builder, TRACE_IDOffset);
        return ACWResult.endACWResult(builder);
    }
    unpack() {
        return new ACWResultT(this.STATUS(), this.ERROR_MESSAGE(), this.bb.createObjList(this.WINDOWS.bind(this), this.windowsLength()), this.TRACE_ID());
    }
    unpackTo(_o) {
        _o.STATUS = this.STATUS();
        _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
        _o.WINDOWS = this.bb.createObjList(this.WINDOWS.bind(this), this.windowsLength());
        _o.TRACE_ID = this.TRACE_ID();
    }
}
export class ACWResultT {
    constructor(STATUS = acwResultStatus.OK, ERROR_MESSAGE = null, WINDOWS = [], TRACE_ID = null) {
        this.STATUS = STATUS;
        this.ERROR_MESSAGE = ERROR_MESSAGE;
        this.WINDOWS = WINDOWS;
        this.TRACE_ID = TRACE_ID;
    }
    pack(builder) {
        const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE) : 0);
        const WINDOWS = ACWResult.createWindowsVector(builder, builder.createObjectOffsetList(this.WINDOWS));
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        return ACWResult.createACWResult(builder, this.STATUS, ERROR_MESSAGE, WINDOWS, TRACE_ID);
    }
}
