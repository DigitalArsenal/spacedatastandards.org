// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { CustomAlgorithm } from './CustomAlgorithm.js';
import { MathAlgorithm } from './MathAlgorithm.js';
/**
 * Collection of algorithms
 */
export class AlgorithmSet {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAlgorithmSet(bb, obj) {
        return (obj || new AlgorithmSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAlgorithmSet(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new AlgorithmSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Custom algorithms
     */
    CUSTOM_ALGORITHMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new CustomAlgorithm()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    customAlgorithmsLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Math algorithms
     */
    MATH_ALGORITHMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new MathAlgorithm()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    mathAlgorithmsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startAlgorithmSet(builder) {
        builder.startObject(2);
    }
    static addCustomAlgorithms(builder, CUSTOM_ALGORITHMSOffset) {
        builder.addFieldOffset(0, CUSTOM_ALGORITHMSOffset, 0);
    }
    static createCustomAlgorithmsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startCustomAlgorithmsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addMathAlgorithms(builder, MATH_ALGORITHMSOffset) {
        builder.addFieldOffset(1, MATH_ALGORITHMSOffset, 0);
    }
    static createMathAlgorithmsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startMathAlgorithmsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endAlgorithmSet(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createAlgorithmSet(builder, CUSTOM_ALGORITHMSOffset, MATH_ALGORITHMSOffset) {
        AlgorithmSet.startAlgorithmSet(builder);
        AlgorithmSet.addCustomAlgorithms(builder, CUSTOM_ALGORITHMSOffset);
        AlgorithmSet.addMathAlgorithms(builder, MATH_ALGORITHMSOffset);
        return AlgorithmSet.endAlgorithmSet(builder);
    }
    unpack() {
        return new AlgorithmSetT(this.bb.createObjList(this.CUSTOM_ALGORITHMS.bind(this), this.customAlgorithmsLength()), this.bb.createObjList(this.MATH_ALGORITHMS.bind(this), this.mathAlgorithmsLength()));
    }
    unpackTo(_o) {
        _o.CUSTOM_ALGORITHMS = this.bb.createObjList(this.CUSTOM_ALGORITHMS.bind(this), this.customAlgorithmsLength());
        _o.MATH_ALGORITHMS = this.bb.createObjList(this.MATH_ALGORITHMS.bind(this), this.mathAlgorithmsLength());
    }
}
export class AlgorithmSetT {
    constructor(CUSTOM_ALGORITHMS = [], MATH_ALGORITHMS = []) {
        this.CUSTOM_ALGORITHMS = CUSTOM_ALGORITHMS;
        this.MATH_ALGORITHMS = MATH_ALGORITHMS;
    }
    pack(builder) {
        const CUSTOM_ALGORITHMS = AlgorithmSet.createCustomAlgorithmsVector(builder, builder.createObjectOffsetList(this.CUSTOM_ALGORITHMS));
        const MATH_ALGORITHMS = AlgorithmSet.createMathAlgorithmsVector(builder, builder.createObjectOffsetList(this.MATH_ALGORITHMS));
        return AlgorithmSet.createAlgorithmSet(builder, CUSTOM_ALGORITHMS, MATH_ALGORITHMS);
    }
}
