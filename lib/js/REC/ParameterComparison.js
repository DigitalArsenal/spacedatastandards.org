// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { ComparisonOperator } from './ComparisonOperator.js';
/**
 * Parameter comparison for match criteria
 */
export class ParameterComparison {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsParameterComparison(bb, obj) {
        return (obj || new ParameterComparison()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsParameterComparison(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ParameterComparison()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    PARAMETER_REF(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Comparison operator
     */
    OPERATOR() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : ComparisonOperator.EQ;
    }
    VALUE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Use calibrated value (true) or raw value (false)
     */
    USE_CALIBRATED_VALUE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : true;
    }
    static startParameterComparison(builder) {
        builder.startObject(4);
    }
    static addParameterRef(builder, PARAMETER_REFOffset) {
        builder.addFieldOffset(0, PARAMETER_REFOffset, 0);
    }
    static addOperator(builder, OPERATOR) {
        builder.addFieldInt8(1, OPERATOR, ComparisonOperator.EQ);
    }
    static addValue(builder, VALUEOffset) {
        builder.addFieldOffset(2, VALUEOffset, 0);
    }
    static addUseCalibratedValue(builder, USE_CALIBRATED_VALUE) {
        builder.addFieldInt8(3, +USE_CALIBRATED_VALUE, +true);
    }
    static endParameterComparison(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createParameterComparison(builder, PARAMETER_REFOffset, OPERATOR, VALUEOffset, USE_CALIBRATED_VALUE) {
        ParameterComparison.startParameterComparison(builder);
        ParameterComparison.addParameterRef(builder, PARAMETER_REFOffset);
        ParameterComparison.addOperator(builder, OPERATOR);
        ParameterComparison.addValue(builder, VALUEOffset);
        ParameterComparison.addUseCalibratedValue(builder, USE_CALIBRATED_VALUE);
        return ParameterComparison.endParameterComparison(builder);
    }
    unpack() {
        return new ParameterComparisonT(this.PARAMETER_REF(), this.OPERATOR(), this.VALUE(), this.USE_CALIBRATED_VALUE());
    }
    unpackTo(_o) {
        _o.PARAMETER_REF = this.PARAMETER_REF();
        _o.OPERATOR = this.OPERATOR();
        _o.VALUE = this.VALUE();
        _o.USE_CALIBRATED_VALUE = this.USE_CALIBRATED_VALUE();
    }
}
export class ParameterComparisonT {
    constructor(PARAMETER_REF = null, OPERATOR = ComparisonOperator.EQ, VALUE = null, USE_CALIBRATED_VALUE = true) {
        this.PARAMETER_REF = PARAMETER_REF;
        this.OPERATOR = OPERATOR;
        this.VALUE = VALUE;
        this.USE_CALIBRATED_VALUE = USE_CALIBRATED_VALUE;
    }
    pack(builder) {
        const PARAMETER_REF = (this.PARAMETER_REF !== null ? builder.createString(this.PARAMETER_REF) : 0);
        const VALUE = (this.VALUE !== null ? builder.createString(this.VALUE) : 0);
        return ParameterComparison.createParameterComparison(builder, PARAMETER_REF, this.OPERATOR, VALUE, this.USE_CALIBRATED_VALUE);
    }
}
