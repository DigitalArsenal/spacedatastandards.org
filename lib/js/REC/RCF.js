// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { rcfPduType } from './rcfPduType.js';
/**
 * Return Channel Frames Service (CCSDS 913.5-B-2)
 */
export class RCF {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsRCF(bb, obj) {
        return (obj || new RCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsRCF(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new RCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$RCF');
    }
    /**
     * PDU type
     */
    PDU_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : rcfPduType.BIND_INVOCATION;
    }
    INITIATOR_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RESPONDER_PORT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Service type
     */
    SERVICE_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Version number
     */
    VERSION() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Invoke ID
     */
    INVOKE_ID() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Spacecraft ID filter
     */
    SPACECRAFT_ID() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Virtual channel ID filter
     */
    VIRTUAL_CHANNEL_ID() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    /**
     * Data
     */
    DATA(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    dataArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startRCF(builder) {
        builder.startObject(9);
    }
    static addPduType(builder, PDU_TYPE) {
        builder.addFieldInt8(0, PDU_TYPE, rcfPduType.BIND_INVOCATION);
    }
    static addInitiatorId(builder, INITIATOR_IDOffset) {
        builder.addFieldOffset(1, INITIATOR_IDOffset, 0);
    }
    static addResponderPortId(builder, RESPONDER_PORT_IDOffset) {
        builder.addFieldOffset(2, RESPONDER_PORT_IDOffset, 0);
    }
    static addServiceType(builder, SERVICE_TYPE) {
        builder.addFieldInt8(3, SERVICE_TYPE, 0);
    }
    static addVersion(builder, VERSION) {
        builder.addFieldInt16(4, VERSION, 0);
    }
    static addInvokeId(builder, INVOKE_ID) {
        builder.addFieldInt32(5, INVOKE_ID, 0);
    }
    static addSpacecraftId(builder, SPACECRAFT_ID) {
        builder.addFieldInt16(6, SPACECRAFT_ID, 0);
    }
    static addVirtualChannelId(builder, VIRTUAL_CHANNEL_ID) {
        builder.addFieldInt8(7, VIRTUAL_CHANNEL_ID, 0);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(8, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endRCF(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishRCFBuffer(builder, offset) {
        builder.finish(offset, '$RCF');
    }
    static finishSizePrefixedRCFBuffer(builder, offset) {
        builder.finish(offset, '$RCF', true);
    }
    static createRCF(builder, PDU_TYPE, INITIATOR_IDOffset, RESPONDER_PORT_IDOffset, SERVICE_TYPE, VERSION, INVOKE_ID, SPACECRAFT_ID, VIRTUAL_CHANNEL_ID, DATAOffset) {
        RCF.startRCF(builder);
        RCF.addPduType(builder, PDU_TYPE);
        RCF.addInitiatorId(builder, INITIATOR_IDOffset);
        RCF.addResponderPortId(builder, RESPONDER_PORT_IDOffset);
        RCF.addServiceType(builder, SERVICE_TYPE);
        RCF.addVersion(builder, VERSION);
        RCF.addInvokeId(builder, INVOKE_ID);
        RCF.addSpacecraftId(builder, SPACECRAFT_ID);
        RCF.addVirtualChannelId(builder, VIRTUAL_CHANNEL_ID);
        RCF.addData(builder, DATAOffset);
        return RCF.endRCF(builder);
    }
    unpack() {
        return new RCFT(this.PDU_TYPE(), this.INITIATOR_ID(), this.RESPONDER_PORT_ID(), this.SERVICE_TYPE(), this.VERSION(), this.INVOKE_ID(), this.SPACECRAFT_ID(), this.VIRTUAL_CHANNEL_ID(), this.bb.createScalarList(this.DATA.bind(this), this.dataLength()));
    }
    unpackTo(_o) {
        _o.PDU_TYPE = this.PDU_TYPE();
        _o.INITIATOR_ID = this.INITIATOR_ID();
        _o.RESPONDER_PORT_ID = this.RESPONDER_PORT_ID();
        _o.SERVICE_TYPE = this.SERVICE_TYPE();
        _o.VERSION = this.VERSION();
        _o.INVOKE_ID = this.INVOKE_ID();
        _o.SPACECRAFT_ID = this.SPACECRAFT_ID();
        _o.VIRTUAL_CHANNEL_ID = this.VIRTUAL_CHANNEL_ID();
        _o.DATA = this.bb.createScalarList(this.DATA.bind(this), this.dataLength());
    }
}
export class RCFT {
    constructor(PDU_TYPE = rcfPduType.BIND_INVOCATION, INITIATOR_ID = null, RESPONDER_PORT_ID = null, SERVICE_TYPE = 0, VERSION = 0, INVOKE_ID = 0, SPACECRAFT_ID = 0, VIRTUAL_CHANNEL_ID = 0, DATA = []) {
        this.PDU_TYPE = PDU_TYPE;
        this.INITIATOR_ID = INITIATOR_ID;
        this.RESPONDER_PORT_ID = RESPONDER_PORT_ID;
        this.SERVICE_TYPE = SERVICE_TYPE;
        this.VERSION = VERSION;
        this.INVOKE_ID = INVOKE_ID;
        this.SPACECRAFT_ID = SPACECRAFT_ID;
        this.VIRTUAL_CHANNEL_ID = VIRTUAL_CHANNEL_ID;
        this.DATA = DATA;
    }
    pack(builder) {
        const INITIATOR_ID = (this.INITIATOR_ID !== null ? builder.createString(this.INITIATOR_ID) : 0);
        const RESPONDER_PORT_ID = (this.RESPONDER_PORT_ID !== null ? builder.createString(this.RESPONDER_PORT_ID) : 0);
        const DATA = RCF.createDataVector(builder, this.DATA);
        return RCF.createRCF(builder, this.PDU_TYPE, INITIATOR_ID, RESPONDER_PORT_ID, this.SERVICE_TYPE, this.VERSION, this.INVOKE_ID, this.SPACECRAFT_ID, this.VIRTUAL_CHANNEL_ID, DATA);
    }
}
