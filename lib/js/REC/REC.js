// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { Record } from './Record.js';
/**
 * Collection of Standard Records
 */
export class REC {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsREC(bb, obj) {
        return (obj || new REC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsREC(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new REC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$REC');
    }
    version(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Array of heterogeneous records from any supported standard
     */
    RECORDS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new Record()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    recordsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startREC(builder) {
        builder.startObject(2);
    }
    static addVersion(builder, versionOffset) {
        builder.addFieldOffset(0, versionOffset, 0);
    }
    static addRecords(builder, RECORDSOffset) {
        builder.addFieldOffset(1, RECORDSOffset, 0);
    }
    static createRecordsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRecordsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endREC(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishRECBuffer(builder, offset) {
        builder.finish(offset, '$REC');
    }
    static finishSizePrefixedRECBuffer(builder, offset) {
        builder.finish(offset, '$REC', true);
    }
    static createREC(builder, versionOffset, RECORDSOffset) {
        REC.startREC(builder);
        REC.addVersion(builder, versionOffset);
        REC.addRecords(builder, RECORDSOffset);
        return REC.endREC(builder);
    }
    unpack() {
        return new RECT(this.version(), this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength()));
    }
    unpackTo(_o) {
        _o.version = this.version();
        _o.RECORDS = this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength());
    }
}
export class RECT {
    constructor(version = null, RECORDS = []) {
        this.version = version;
        this.RECORDS = RECORDS;
    }
    pack(builder) {
        const version = (this.version !== null ? builder.createString(this.version) : 0);
        const RECORDS = REC.createRecordsVector(builder, builder.createObjectOffsetList(this.RECORDS));
        return REC.createREC(builder, version, RECORDS);
    }
}
