// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One category tally.
 */
export class AGRCategory {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAGRCategory(bb, obj) {
        return (obj || new AGRCategory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAGRCategory(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new AGRCategory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    KEY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    SUM() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startAGRCategory(builder) {
        builder.startObject(3);
    }
    static addKey(builder, KEYOffset) {
        builder.addFieldOffset(0, KEYOffset, 0);
    }
    static addCount(builder, COUNT) {
        builder.addFieldInt64(1, COUNT, BigInt('0'));
    }
    static addSum(builder, SUM) {
        builder.addFieldFloat64(2, SUM, 0.0);
    }
    static endAGRCategory(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createAGRCategory(builder, KEYOffset, COUNT, SUM) {
        AGRCategory.startAGRCategory(builder);
        AGRCategory.addKey(builder, KEYOffset);
        AGRCategory.addCount(builder, COUNT);
        AGRCategory.addSum(builder, SUM);
        return AGRCategory.endAGRCategory(builder);
    }
    unpack() {
        return new AGRCategoryT(this.KEY(), this.COUNT(), this.SUM());
    }
    unpackTo(_o) {
        _o.KEY = this.KEY();
        _o.COUNT = this.COUNT();
        _o.SUM = this.SUM();
    }
}
export class AGRCategoryT {
    constructor(KEY = null, COUNT = BigInt('0'), SUM = 0.0) {
        this.KEY = KEY;
        this.COUNT = COUNT;
        this.SUM = SUM;
    }
    pack(builder) {
        const KEY = (this.KEY !== null ? builder.createString(this.KEY) : 0);
        return AGRCategory.createAGRCategory(builder, KEY, this.COUNT, this.SUM);
    }
}
