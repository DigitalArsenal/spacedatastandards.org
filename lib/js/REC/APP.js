// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { APPDataRef } from './APPDataRef.js';
import { APPDataflow } from './APPDataflow.js';
import { APPModuleRef } from './APPModuleRef.js';
import { APPSourceRef } from './APPSourceRef.js';
import { APPUIPage } from './APPUIPage.js';
/**
 * Application Package Manifest — one launchable app.
 */
export class APP {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAPP(bb, obj) {
        return (obj || new APP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAPP(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new APP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$APP');
    }
    ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Member WASM modules. Referential integrity: every MODULE_ID and every
     * MODULE-kind REF elsewhere in the manifest must resolve into this list.
     */
    MODULES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new APPModuleRef()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    modulesLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * SDS record types produced and consumed.
     */
    DATA(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new APPDataRef()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Upstream sources depended on.
     */
    SOURCES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new APPSourceRef()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    sourcesLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * UI pages. Exactly one entry page when nonempty.
     */
    UI(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new APPUIPage()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    uiLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    CREATED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    UPDATED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * The page's declarative data contract: what data enters and leaves the
     * running page and how. Referential integrity: every MODULE_ID here must
     * resolve into MODULES, and each MODULE_ID/METHOD_ID/PORT_ID triple must
     * name a method port advertised by that module's PLG manifest.
     */
    DATAFLOW(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? (obj || new APPDataflow()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    dataflowLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startAPP(builder) {
        builder.startObject(11);
    }
    static addId(builder, IDOffset) {
        builder.addFieldOffset(0, IDOffset, 0);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(1, NAMEOffset, 0);
    }
    static addVersion(builder, VERSIONOffset) {
        builder.addFieldOffset(2, VERSIONOffset, 0);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(3, DESCRIPTIONOffset, 0);
    }
    static addModules(builder, MODULESOffset) {
        builder.addFieldOffset(4, MODULESOffset, 0);
    }
    static createModulesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startModulesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(5, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSources(builder, SOURCESOffset) {
        builder.addFieldOffset(6, SOURCESOffset, 0);
    }
    static createSourcesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSourcesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addUi(builder, UIOffset) {
        builder.addFieldOffset(7, UIOffset, 0);
    }
    static createUiVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startUiVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCreatedAt(builder, CREATED_ATOffset) {
        builder.addFieldOffset(8, CREATED_ATOffset, 0);
    }
    static addUpdatedAt(builder, UPDATED_ATOffset) {
        builder.addFieldOffset(9, UPDATED_ATOffset, 0);
    }
    static addDataflow(builder, DATAFLOWOffset) {
        builder.addFieldOffset(10, DATAFLOWOffset, 0);
    }
    static createDataflowVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startDataflowVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endAPP(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // ID
        return offset;
    }
    static finishAPPBuffer(builder, offset) {
        builder.finish(offset, '$APP');
    }
    static finishSizePrefixedAPPBuffer(builder, offset) {
        builder.finish(offset, '$APP', true);
    }
    static createAPP(builder, IDOffset, NAMEOffset, VERSIONOffset, DESCRIPTIONOffset, MODULESOffset, DATAOffset, SOURCESOffset, UIOffset, CREATED_ATOffset, UPDATED_ATOffset, DATAFLOWOffset) {
        APP.startAPP(builder);
        APP.addId(builder, IDOffset);
        APP.addName(builder, NAMEOffset);
        APP.addVersion(builder, VERSIONOffset);
        APP.addDescription(builder, DESCRIPTIONOffset);
        APP.addModules(builder, MODULESOffset);
        APP.addData(builder, DATAOffset);
        APP.addSources(builder, SOURCESOffset);
        APP.addUi(builder, UIOffset);
        APP.addCreatedAt(builder, CREATED_ATOffset);
        APP.addUpdatedAt(builder, UPDATED_ATOffset);
        APP.addDataflow(builder, DATAFLOWOffset);
        return APP.endAPP(builder);
    }
    unpack() {
        return new APPT(this.ID(), this.NAME(), this.VERSION(), this.DESCRIPTION(), this.bb.createObjList(this.MODULES.bind(this), this.modulesLength()), this.bb.createObjList(this.DATA.bind(this), this.dataLength()), this.bb.createObjList(this.SOURCES.bind(this), this.sourcesLength()), this.bb.createObjList(this.UI.bind(this), this.uiLength()), this.CREATED_AT(), this.UPDATED_AT(), this.bb.createObjList(this.DATAFLOW.bind(this), this.dataflowLength()));
    }
    unpackTo(_o) {
        _o.ID = this.ID();
        _o.NAME = this.NAME();
        _o.VERSION = this.VERSION();
        _o.DESCRIPTION = this.DESCRIPTION();
        _o.MODULES = this.bb.createObjList(this.MODULES.bind(this), this.modulesLength());
        _o.DATA = this.bb.createObjList(this.DATA.bind(this), this.dataLength());
        _o.SOURCES = this.bb.createObjList(this.SOURCES.bind(this), this.sourcesLength());
        _o.UI = this.bb.createObjList(this.UI.bind(this), this.uiLength());
        _o.CREATED_AT = this.CREATED_AT();
        _o.UPDATED_AT = this.UPDATED_AT();
        _o.DATAFLOW = this.bb.createObjList(this.DATAFLOW.bind(this), this.dataflowLength());
    }
}
export class APPT {
    constructor(ID = null, NAME = null, VERSION = null, DESCRIPTION = null, MODULES = [], DATA = [], SOURCES = [], UI = [], CREATED_AT = null, UPDATED_AT = null, DATAFLOW = []) {
        this.ID = ID;
        this.NAME = NAME;
        this.VERSION = VERSION;
        this.DESCRIPTION = DESCRIPTION;
        this.MODULES = MODULES;
        this.DATA = DATA;
        this.SOURCES = SOURCES;
        this.UI = UI;
        this.CREATED_AT = CREATED_AT;
        this.UPDATED_AT = UPDATED_AT;
        this.DATAFLOW = DATAFLOW;
    }
    pack(builder) {
        const ID = (this.ID !== null ? builder.createString(this.ID) : 0);
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const VERSION = (this.VERSION !== null ? builder.createString(this.VERSION) : 0);
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        const MODULES = APP.createModulesVector(builder, builder.createObjectOffsetList(this.MODULES));
        const DATA = APP.createDataVector(builder, builder.createObjectOffsetList(this.DATA));
        const SOURCES = APP.createSourcesVector(builder, builder.createObjectOffsetList(this.SOURCES));
        const UI = APP.createUiVector(builder, builder.createObjectOffsetList(this.UI));
        const CREATED_AT = (this.CREATED_AT !== null ? builder.createString(this.CREATED_AT) : 0);
        const UPDATED_AT = (this.UPDATED_AT !== null ? builder.createString(this.UPDATED_AT) : 0);
        const DATAFLOW = APP.createDataflowVector(builder, builder.createObjectOffsetList(this.DATAFLOW));
        return APP.createAPP(builder, ID, NAME, VERSION, DESCRIPTION, MODULES, DATA, SOURCES, UI, CREATED_AT, UPDATED_AT, DATAFLOW);
    }
}
