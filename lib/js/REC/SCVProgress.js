// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { scvResultState } from './scvResultState.js';
export class SCVProgress {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCVProgress(bb, obj) {
        return (obj || new SCVProgress()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCVProgress(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCVProgress()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    JOB_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TRACE_ID() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : scvResultState.OK;
    }
    COMPLETED_SENSORS() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TOTAL_SENSORS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    COMPLETED_WINDOWS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TOTAL_WINDOWS() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    BACKLOG_REMAINING() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    COMPLETION_FRACTION() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startSCVProgress(builder) {
        builder.startObject(10);
    }
    static addJobId(builder, JOB_IDOffset) {
        builder.addFieldOffset(0, JOB_IDOffset, 0);
    }
    static addTraceId(builder, TRACE_ID) {
        builder.addFieldInt64(1, TRACE_ID, BigInt('0'));
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(2, STATUS, scvResultState.OK);
    }
    static addCompletedSensors(builder, COMPLETED_SENSORS) {
        builder.addFieldInt32(3, COMPLETED_SENSORS, 0);
    }
    static addTotalSensors(builder, TOTAL_SENSORS) {
        builder.addFieldInt32(4, TOTAL_SENSORS, 0);
    }
    static addCompletedWindows(builder, COMPLETED_WINDOWS) {
        builder.addFieldInt32(5, COMPLETED_WINDOWS, 0);
    }
    static addTotalWindows(builder, TOTAL_WINDOWS) {
        builder.addFieldInt32(6, TOTAL_WINDOWS, 0);
    }
    static addBacklogRemaining(builder, BACKLOG_REMAINING) {
        builder.addFieldInt32(7, BACKLOG_REMAINING, 0);
    }
    static addCompletionFraction(builder, COMPLETION_FRACTION) {
        builder.addFieldFloat64(8, COMPLETION_FRACTION, 0.0);
    }
    static addMessage(builder, MESSAGEOffset) {
        builder.addFieldOffset(9, MESSAGEOffset, 0);
    }
    static endSCVProgress(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCVProgress(builder, JOB_IDOffset, TRACE_ID, STATUS, COMPLETED_SENSORS, TOTAL_SENSORS, COMPLETED_WINDOWS, TOTAL_WINDOWS, BACKLOG_REMAINING, COMPLETION_FRACTION, MESSAGEOffset) {
        SCVProgress.startSCVProgress(builder);
        SCVProgress.addJobId(builder, JOB_IDOffset);
        SCVProgress.addTraceId(builder, TRACE_ID);
        SCVProgress.addStatus(builder, STATUS);
        SCVProgress.addCompletedSensors(builder, COMPLETED_SENSORS);
        SCVProgress.addTotalSensors(builder, TOTAL_SENSORS);
        SCVProgress.addCompletedWindows(builder, COMPLETED_WINDOWS);
        SCVProgress.addTotalWindows(builder, TOTAL_WINDOWS);
        SCVProgress.addBacklogRemaining(builder, BACKLOG_REMAINING);
        SCVProgress.addCompletionFraction(builder, COMPLETION_FRACTION);
        SCVProgress.addMessage(builder, MESSAGEOffset);
        return SCVProgress.endSCVProgress(builder);
    }
    unpack() {
        return new SCVProgressT(this.JOB_ID(), this.TRACE_ID(), this.STATUS(), this.COMPLETED_SENSORS(), this.TOTAL_SENSORS(), this.COMPLETED_WINDOWS(), this.TOTAL_WINDOWS(), this.BACKLOG_REMAINING(), this.COMPLETION_FRACTION(), this.MESSAGE());
    }
    unpackTo(_o) {
        _o.JOB_ID = this.JOB_ID();
        _o.TRACE_ID = this.TRACE_ID();
        _o.STATUS = this.STATUS();
        _o.COMPLETED_SENSORS = this.COMPLETED_SENSORS();
        _o.TOTAL_SENSORS = this.TOTAL_SENSORS();
        _o.COMPLETED_WINDOWS = this.COMPLETED_WINDOWS();
        _o.TOTAL_WINDOWS = this.TOTAL_WINDOWS();
        _o.BACKLOG_REMAINING = this.BACKLOG_REMAINING();
        _o.COMPLETION_FRACTION = this.COMPLETION_FRACTION();
        _o.MESSAGE = this.MESSAGE();
    }
}
export class SCVProgressT {
    constructor(JOB_ID = null, TRACE_ID = BigInt('0'), STATUS = scvResultState.OK, COMPLETED_SENSORS = 0, TOTAL_SENSORS = 0, COMPLETED_WINDOWS = 0, TOTAL_WINDOWS = 0, BACKLOG_REMAINING = 0, COMPLETION_FRACTION = 0.0, MESSAGE = null) {
        this.JOB_ID = JOB_ID;
        this.TRACE_ID = TRACE_ID;
        this.STATUS = STATUS;
        this.COMPLETED_SENSORS = COMPLETED_SENSORS;
        this.TOTAL_SENSORS = TOTAL_SENSORS;
        this.COMPLETED_WINDOWS = COMPLETED_WINDOWS;
        this.TOTAL_WINDOWS = TOTAL_WINDOWS;
        this.BACKLOG_REMAINING = BACKLOG_REMAINING;
        this.COMPLETION_FRACTION = COMPLETION_FRACTION;
        this.MESSAGE = MESSAGE;
    }
    pack(builder) {
        const JOB_ID = (this.JOB_ID !== null ? builder.createString(this.JOB_ID) : 0);
        const MESSAGE = (this.MESSAGE !== null ? builder.createString(this.MESSAGE) : 0);
        return SCVProgress.createSCVProgress(builder, JOB_ID, this.TRACE_ID, this.STATUS, this.COMPLETED_SENSORS, this.TOTAL_SENSORS, this.COMPLETED_WINDOWS, this.TOTAL_WINDOWS, this.BACKLOG_REMAINING, this.COMPLETION_FRACTION, MESSAGE);
    }
}
