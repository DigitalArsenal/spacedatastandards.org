// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Standards Record Index — local wrapper for a standards payload's identity
 * and mutation metadata, participating in shared FlatSQL query and cascade
 * paths across plugins that read the same record universe.
 */
export class SRI {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSRI(bb, obj) {
        return (obj || new SRI()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSRI(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SRI()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$SRI');
    }
    RECORD_KEY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SCHEMA_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ROLE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ATTACHED_VIA(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PAYLOAD_KIND(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Milliseconds since epoch when the local mutation record was last refreshed.
     */
    UPDATED_AT_MS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Reserved for forward-compatible growth.
     */
    RESERVED(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    reservedLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    reservedArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startSRI(builder) {
        builder.startObject(7);
    }
    static addRecordKey(builder, RECORD_KEYOffset) {
        builder.addFieldOffset(0, RECORD_KEYOffset, 0);
    }
    static addSchemaName(builder, SCHEMA_NAMEOffset) {
        builder.addFieldOffset(1, SCHEMA_NAMEOffset, 0);
    }
    static addRole(builder, ROLEOffset) {
        builder.addFieldOffset(2, ROLEOffset, 0);
    }
    static addAttachedVia(builder, ATTACHED_VIAOffset) {
        builder.addFieldOffset(3, ATTACHED_VIAOffset, 0);
    }
    static addPayloadKind(builder, PAYLOAD_KINDOffset) {
        builder.addFieldOffset(4, PAYLOAD_KINDOffset, 0);
    }
    static addUpdatedAtMs(builder, UPDATED_AT_MS) {
        builder.addFieldFloat64(5, UPDATED_AT_MS, 0.0);
    }
    static addReserved(builder, RESERVEDOffset) {
        builder.addFieldOffset(6, RESERVEDOffset, 0);
    }
    static createReservedVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startReservedVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endSRI(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishSRIBuffer(builder, offset) {
        builder.finish(offset, '$SRI');
    }
    static finishSizePrefixedSRIBuffer(builder, offset) {
        builder.finish(offset, '$SRI', true);
    }
    static createSRI(builder, RECORD_KEYOffset, SCHEMA_NAMEOffset, ROLEOffset, ATTACHED_VIAOffset, PAYLOAD_KINDOffset, UPDATED_AT_MS, RESERVEDOffset) {
        SRI.startSRI(builder);
        SRI.addRecordKey(builder, RECORD_KEYOffset);
        SRI.addSchemaName(builder, SCHEMA_NAMEOffset);
        SRI.addRole(builder, ROLEOffset);
        SRI.addAttachedVia(builder, ATTACHED_VIAOffset);
        SRI.addPayloadKind(builder, PAYLOAD_KINDOffset);
        SRI.addUpdatedAtMs(builder, UPDATED_AT_MS);
        SRI.addReserved(builder, RESERVEDOffset);
        return SRI.endSRI(builder);
    }
    unpack() {
        return new SRIT(this.RECORD_KEY(), this.SCHEMA_NAME(), this.ROLE(), this.ATTACHED_VIA(), this.PAYLOAD_KIND(), this.UPDATED_AT_MS(), this.bb.createScalarList(this.RESERVED.bind(this), this.reservedLength()));
    }
    unpackTo(_o) {
        _o.RECORD_KEY = this.RECORD_KEY();
        _o.SCHEMA_NAME = this.SCHEMA_NAME();
        _o.ROLE = this.ROLE();
        _o.ATTACHED_VIA = this.ATTACHED_VIA();
        _o.PAYLOAD_KIND = this.PAYLOAD_KIND();
        _o.UPDATED_AT_MS = this.UPDATED_AT_MS();
        _o.RESERVED = this.bb.createScalarList(this.RESERVED.bind(this), this.reservedLength());
    }
}
export class SRIT {
    constructor(RECORD_KEY = null, SCHEMA_NAME = null, ROLE = null, ATTACHED_VIA = null, PAYLOAD_KIND = null, UPDATED_AT_MS = 0.0, RESERVED = []) {
        this.RECORD_KEY = RECORD_KEY;
        this.SCHEMA_NAME = SCHEMA_NAME;
        this.ROLE = ROLE;
        this.ATTACHED_VIA = ATTACHED_VIA;
        this.PAYLOAD_KIND = PAYLOAD_KIND;
        this.UPDATED_AT_MS = UPDATED_AT_MS;
        this.RESERVED = RESERVED;
    }
    pack(builder) {
        const RECORD_KEY = (this.RECORD_KEY !== null ? builder.createString(this.RECORD_KEY) : 0);
        const SCHEMA_NAME = (this.SCHEMA_NAME !== null ? builder.createString(this.SCHEMA_NAME) : 0);
        const ROLE = (this.ROLE !== null ? builder.createString(this.ROLE) : 0);
        const ATTACHED_VIA = (this.ATTACHED_VIA !== null ? builder.createString(this.ATTACHED_VIA) : 0);
        const PAYLOAD_KIND = (this.PAYLOAD_KIND !== null ? builder.createString(this.PAYLOAD_KIND) : 0);
        const RESERVED = SRI.createReservedVector(builder, this.RESERVED);
        return SRI.createSRI(builder, RECORD_KEY, SCHEMA_NAME, ROLE, ATTACHED_VIA, PAYLOAD_KIND, this.UPDATED_AT_MS, RESERVED);
    }
}
