// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One purpose-key slot of an operator.
 */
export class ACTKeySlot {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsACTKeySlot(bb, obj) {
        return (obj || new ACTKeySlot()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsACTKeySlot(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ACTKeySlot()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SLOT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    KEY_PATH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    NEXT_PATH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ROTATABLE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    ALGORITHM(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startACTKeySlot(builder) {
        builder.startObject(5);
    }
    static addSlot(builder, SLOTOffset) {
        builder.addFieldOffset(0, SLOTOffset, 0);
    }
    static addKeyPath(builder, KEY_PATHOffset) {
        builder.addFieldOffset(1, KEY_PATHOffset, 0);
    }
    static addNextPath(builder, NEXT_PATHOffset) {
        builder.addFieldOffset(2, NEXT_PATHOffset, 0);
    }
    static addRotatable(builder, ROTATABLE) {
        builder.addFieldInt8(3, +ROTATABLE, +false);
    }
    static addAlgorithm(builder, ALGORITHMOffset) {
        builder.addFieldOffset(4, ALGORITHMOffset, 0);
    }
    static endACTKeySlot(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createACTKeySlot(builder, SLOTOffset, KEY_PATHOffset, NEXT_PATHOffset, ROTATABLE, ALGORITHMOffset) {
        ACTKeySlot.startACTKeySlot(builder);
        ACTKeySlot.addSlot(builder, SLOTOffset);
        ACTKeySlot.addKeyPath(builder, KEY_PATHOffset);
        ACTKeySlot.addNextPath(builder, NEXT_PATHOffset);
        ACTKeySlot.addRotatable(builder, ROTATABLE);
        ACTKeySlot.addAlgorithm(builder, ALGORITHMOffset);
        return ACTKeySlot.endACTKeySlot(builder);
    }
    unpack() {
        return new ACTKeySlotT(this.SLOT(), this.KEY_PATH(), this.NEXT_PATH(), this.ROTATABLE(), this.ALGORITHM());
    }
    unpackTo(_o) {
        _o.SLOT = this.SLOT();
        _o.KEY_PATH = this.KEY_PATH();
        _o.NEXT_PATH = this.NEXT_PATH();
        _o.ROTATABLE = this.ROTATABLE();
        _o.ALGORITHM = this.ALGORITHM();
    }
}
export class ACTKeySlotT {
    constructor(SLOT = null, KEY_PATH = null, NEXT_PATH = null, ROTATABLE = false, ALGORITHM = null) {
        this.SLOT = SLOT;
        this.KEY_PATH = KEY_PATH;
        this.NEXT_PATH = NEXT_PATH;
        this.ROTATABLE = ROTATABLE;
        this.ALGORITHM = ALGORITHM;
    }
    pack(builder) {
        const SLOT = (this.SLOT !== null ? builder.createString(this.SLOT) : 0);
        const KEY_PATH = (this.KEY_PATH !== null ? builder.createString(this.KEY_PATH) : 0);
        const NEXT_PATH = (this.NEXT_PATH !== null ? builder.createString(this.NEXT_PATH) : 0);
        const ALGORITHM = (this.ALGORITHM !== null ? builder.createString(this.ALGORITHM) : 0);
        return ACTKeySlot.createACTKeySlot(builder, SLOT, KEY_PATH, NEXT_PATH, this.ROTATABLE, ALGORITHM);
    }
}
