// automatically generated by the FlatBuffers compiler, do not modify
/**
 * A row a body's disc passes over.
 */
export class SKROccultation {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    BODY_ID() {
        return this.bb.readUint32(this.bb_pos);
    }
    ROW() {
        return this.bb.readUint32(this.bb_pos + 4);
    }
    /**
     * Closest approach of the body's centre to the row.
     */
    MIN_SEPARATION_DEG() {
        return this.bb.readFloat64(this.bb_pos + 8);
    }
    /**
     * Where along the track the closest approach falls, from 0 (start) to 1
     * (end).
     */
    TRACK_FRACTION() {
        return this.bb.readFloat64(this.bb_pos + 16);
    }
    MAG() {
        return this.bb.readFloat32(this.bb_pos + 24);
    }
    RESERVED() {
        return this.bb.readUint32(this.bb_pos + 28);
    }
    static sizeOf() {
        return 32;
    }
    static createSKROccultation(builder, BODY_ID, ROW, MIN_SEPARATION_DEG, TRACK_FRACTION, MAG, RESERVED) {
        builder.prep(8, 32);
        builder.writeInt32(RESERVED);
        builder.writeFloat32(MAG);
        builder.writeFloat64(TRACK_FRACTION);
        builder.writeFloat64(MIN_SEPARATION_DEG);
        builder.writeInt32(ROW);
        builder.writeInt32(BODY_ID);
        return builder.offset();
    }
    unpack() {
        return new SKROccultationT(this.BODY_ID(), this.ROW(), this.MIN_SEPARATION_DEG(), this.TRACK_FRACTION(), this.MAG(), this.RESERVED());
    }
    unpackTo(_o) {
        _o.BODY_ID = this.BODY_ID();
        _o.ROW = this.ROW();
        _o.MIN_SEPARATION_DEG = this.MIN_SEPARATION_DEG();
        _o.TRACK_FRACTION = this.TRACK_FRACTION();
        _o.MAG = this.MAG();
        _o.RESERVED = this.RESERVED();
    }
}
export class SKROccultationT {
    constructor(BODY_ID = 0, ROW = 0, MIN_SEPARATION_DEG = 0.0, TRACK_FRACTION = 0.0, MAG = 0.0, RESERVED = 0) {
        this.BODY_ID = BODY_ID;
        this.ROW = ROW;
        this.MIN_SEPARATION_DEG = MIN_SEPARATION_DEG;
        this.TRACK_FRACTION = TRACK_FRACTION;
        this.MAG = MAG;
        this.RESERVED = RESERVED;
    }
    pack(builder) {
        return SKROccultation.createSKROccultation(builder, this.BODY_ID, this.ROW, this.MIN_SEPARATION_DEG, this.TRACK_FRACTION, this.MAG, this.RESERVED);
    }
}
