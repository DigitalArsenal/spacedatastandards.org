// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * 3D Tileset
 */
export class CZMTileset {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCZMTileset(bb, obj) {
        return (obj || new CZMTileset()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCZMTileset(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CZMTileset()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Whether the tileset is displayed
     */
    SHOW() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    URI(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Maximum screen space error
     */
    MAXIMUM_SCREEN_SPACE_ERROR() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startCZMTileset(builder) {
        builder.startObject(3);
    }
    static addShow(builder, SHOW) {
        builder.addFieldInt8(0, +SHOW, +false);
    }
    static addUri(builder, URIOffset) {
        builder.addFieldOffset(1, URIOffset, 0);
    }
    static addMaximumScreenSpaceError(builder, MAXIMUM_SCREEN_SPACE_ERROR) {
        builder.addFieldFloat64(2, MAXIMUM_SCREEN_SPACE_ERROR, 0.0);
    }
    static endCZMTileset(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createCZMTileset(builder, SHOW, URIOffset, MAXIMUM_SCREEN_SPACE_ERROR) {
        CZMTileset.startCZMTileset(builder);
        CZMTileset.addShow(builder, SHOW);
        CZMTileset.addUri(builder, URIOffset);
        CZMTileset.addMaximumScreenSpaceError(builder, MAXIMUM_SCREEN_SPACE_ERROR);
        return CZMTileset.endCZMTileset(builder);
    }
    unpack() {
        return new CZMTilesetT(this.SHOW(), this.URI(), this.MAXIMUM_SCREEN_SPACE_ERROR());
    }
    unpackTo(_o) {
        _o.SHOW = this.SHOW();
        _o.URI = this.URI();
        _o.MAXIMUM_SCREEN_SPACE_ERROR = this.MAXIMUM_SCREEN_SPACE_ERROR();
    }
}
export class CZMTilesetT {
    constructor(SHOW = false, URI = null, MAXIMUM_SCREEN_SPACE_ERROR = 0.0) {
        this.SHOW = SHOW;
        this.URI = URI;
        this.MAXIMUM_SCREEN_SPACE_ERROR = MAXIMUM_SCREEN_SPACE_ERROR;
    }
    pack(builder) {
        const URI = (this.URI !== null ? builder.createString(this.URI) : 0);
        return CZMTileset.createCZMTileset(builder, this.SHOW, URI, this.MAXIMUM_SCREEN_SPACE_ERROR);
    }
}
