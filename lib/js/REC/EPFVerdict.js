// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { epfLimitCategory } from './epfLimitCategory.js';
/**
 * FlatSQL-queryable compliance verdict consumed directly by `$RPT`.
 */
export class EPFVerdict {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsEPFVerdict(bb, obj) {
        return (obj || new EPFVerdict()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsEPFVerdict(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new EPFVerdict()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    LIMIT_KIND() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : epfLimitCategory.UNSPECIFIED;
    }
    COMPLIANT() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Worst margin to the applicable limit, dB. Positive means compliant.
     */
    MARGIN_DB() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    PERCENT_TIME_EXCEEDED() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    WORST_SAMPLE_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    NOTE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startEPFVerdict(builder) {
        builder.startObject(6);
    }
    static addLimitKind(builder, LIMIT_KIND) {
        builder.addFieldInt8(0, LIMIT_KIND, epfLimitCategory.UNSPECIFIED);
    }
    static addCompliant(builder, COMPLIANT) {
        builder.addFieldInt8(1, +COMPLIANT, +false);
    }
    static addMarginDb(builder, MARGIN_DB) {
        builder.addFieldFloat64(2, MARGIN_DB, 0.0);
    }
    static addPercentTimeExceeded(builder, PERCENT_TIME_EXCEEDED) {
        builder.addFieldFloat64(3, PERCENT_TIME_EXCEEDED, 0.0);
    }
    static addWorstSampleIndex(builder, WORST_SAMPLE_INDEX) {
        builder.addFieldInt32(4, WORST_SAMPLE_INDEX, 0);
    }
    static addNote(builder, NOTEOffset) {
        builder.addFieldOffset(5, NOTEOffset, 0);
    }
    static endEPFVerdict(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createEPFVerdict(builder, LIMIT_KIND, COMPLIANT, MARGIN_DB, PERCENT_TIME_EXCEEDED, WORST_SAMPLE_INDEX, NOTEOffset) {
        EPFVerdict.startEPFVerdict(builder);
        EPFVerdict.addLimitKind(builder, LIMIT_KIND);
        EPFVerdict.addCompliant(builder, COMPLIANT);
        EPFVerdict.addMarginDb(builder, MARGIN_DB);
        EPFVerdict.addPercentTimeExceeded(builder, PERCENT_TIME_EXCEEDED);
        EPFVerdict.addWorstSampleIndex(builder, WORST_SAMPLE_INDEX);
        EPFVerdict.addNote(builder, NOTEOffset);
        return EPFVerdict.endEPFVerdict(builder);
    }
    unpack() {
        return new EPFVerdictT(this.LIMIT_KIND(), this.COMPLIANT(), this.MARGIN_DB(), this.PERCENT_TIME_EXCEEDED(), this.WORST_SAMPLE_INDEX(), this.NOTE());
    }
    unpackTo(_o) {
        _o.LIMIT_KIND = this.LIMIT_KIND();
        _o.COMPLIANT = this.COMPLIANT();
        _o.MARGIN_DB = this.MARGIN_DB();
        _o.PERCENT_TIME_EXCEEDED = this.PERCENT_TIME_EXCEEDED();
        _o.WORST_SAMPLE_INDEX = this.WORST_SAMPLE_INDEX();
        _o.NOTE = this.NOTE();
    }
}
export class EPFVerdictT {
    constructor(LIMIT_KIND = epfLimitCategory.UNSPECIFIED, COMPLIANT = false, MARGIN_DB = 0.0, PERCENT_TIME_EXCEEDED = 0.0, WORST_SAMPLE_INDEX = 0, NOTE = null) {
        this.LIMIT_KIND = LIMIT_KIND;
        this.COMPLIANT = COMPLIANT;
        this.MARGIN_DB = MARGIN_DB;
        this.PERCENT_TIME_EXCEEDED = PERCENT_TIME_EXCEEDED;
        this.WORST_SAMPLE_INDEX = WORST_SAMPLE_INDEX;
        this.NOTE = NOTE;
    }
    pack(builder) {
        const NOTE = (this.NOTE !== null ? builder.createString(this.NOTE) : 0);
        return EPFVerdict.createEPFVerdict(builder, this.LIMIT_KIND, this.COMPLIANT, this.MARGIN_DB, this.PERCENT_TIME_EXCEEDED, this.WORST_SAMPLE_INDEX, NOTE);
    }
}
