// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Binary data encoding specification
 */
export class BinaryDataEncoding {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsBinaryDataEncoding(bb, obj) {
        return (obj || new BinaryDataEncoding()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsBinaryDataEncoding(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new BinaryDataEncoding()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Fixed size in bits
     */
    SIZE_IN_BITS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Maximum size in bits (for variable length)
     */
    MAX_SIZE_IN_BITS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Leading size field bits (for variable length)
     */
    LEADING_SIZE_BITS() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    static startBinaryDataEncoding(builder) {
        builder.startObject(3);
    }
    static addSizeInBits(builder, SIZE_IN_BITS) {
        builder.addFieldInt16(0, SIZE_IN_BITS, 0);
    }
    static addMaxSizeInBits(builder, MAX_SIZE_IN_BITS) {
        builder.addFieldInt16(1, MAX_SIZE_IN_BITS, 0);
    }
    static addLeadingSizeBits(builder, LEADING_SIZE_BITS) {
        builder.addFieldInt8(2, LEADING_SIZE_BITS, 0);
    }
    static endBinaryDataEncoding(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createBinaryDataEncoding(builder, SIZE_IN_BITS, MAX_SIZE_IN_BITS, LEADING_SIZE_BITS) {
        BinaryDataEncoding.startBinaryDataEncoding(builder);
        BinaryDataEncoding.addSizeInBits(builder, SIZE_IN_BITS);
        BinaryDataEncoding.addMaxSizeInBits(builder, MAX_SIZE_IN_BITS);
        BinaryDataEncoding.addLeadingSizeBits(builder, LEADING_SIZE_BITS);
        return BinaryDataEncoding.endBinaryDataEncoding(builder);
    }
    unpack() {
        return new BinaryDataEncodingT(this.SIZE_IN_BITS(), this.MAX_SIZE_IN_BITS(), this.LEADING_SIZE_BITS());
    }
    unpackTo(_o) {
        _o.SIZE_IN_BITS = this.SIZE_IN_BITS();
        _o.MAX_SIZE_IN_BITS = this.MAX_SIZE_IN_BITS();
        _o.LEADING_SIZE_BITS = this.LEADING_SIZE_BITS();
    }
}
export class BinaryDataEncodingT {
    constructor(SIZE_IN_BITS = 0, MAX_SIZE_IN_BITS = 0, LEADING_SIZE_BITS = 0) {
        this.SIZE_IN_BITS = SIZE_IN_BITS;
        this.MAX_SIZE_IN_BITS = MAX_SIZE_IN_BITS;
        this.LEADING_SIZE_BITS = LEADING_SIZE_BITS;
    }
    pack(builder) {
        return BinaryDataEncoding.createBinaryDataEncoding(builder, this.SIZE_IN_BITS, this.MAX_SIZE_IN_BITS, this.LEADING_SIZE_BITS);
    }
}
