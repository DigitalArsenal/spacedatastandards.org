// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GPXLink } from './GPXLink.js';
import { GPXTrackSegment } from './GPXTrackSegment.js';
/**
 * Track (ordered list of track segments)
 */
export class GPXTrack {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGPXTrack(bb, obj) {
        return (obj || new GPXTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGPXTrack(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GPXTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    COMMENT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Links to additional information
     */
    LINKS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new GPXLink()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    linksLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Track number
     */
    NUMBER() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Track segments
     */
    SEGMENTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new GPXTrackSegment()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    segmentsLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startGPXTrack(builder) {
        builder.startObject(8);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addComment(builder, COMMENTOffset) {
        builder.addFieldOffset(1, COMMENTOffset, 0);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(2, DESCRIPTIONOffset, 0);
    }
    static addSource(builder, SOURCEOffset) {
        builder.addFieldOffset(3, SOURCEOffset, 0);
    }
    static addLinks(builder, LINKSOffset) {
        builder.addFieldOffset(4, LINKSOffset, 0);
    }
    static createLinksVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startLinksVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addNumber(builder, NUMBER) {
        builder.addFieldInt32(5, NUMBER, 0);
    }
    static addType(builder, TYPEOffset) {
        builder.addFieldOffset(6, TYPEOffset, 0);
    }
    static addSegments(builder, SEGMENTSOffset) {
        builder.addFieldOffset(7, SEGMENTSOffset, 0);
    }
    static createSegmentsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSegmentsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endGPXTrack(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGPXTrack(builder, NAMEOffset, COMMENTOffset, DESCRIPTIONOffset, SOURCEOffset, LINKSOffset, NUMBER, TYPEOffset, SEGMENTSOffset) {
        GPXTrack.startGPXTrack(builder);
        GPXTrack.addName(builder, NAMEOffset);
        GPXTrack.addComment(builder, COMMENTOffset);
        GPXTrack.addDescription(builder, DESCRIPTIONOffset);
        GPXTrack.addSource(builder, SOURCEOffset);
        GPXTrack.addLinks(builder, LINKSOffset);
        GPXTrack.addNumber(builder, NUMBER);
        GPXTrack.addType(builder, TYPEOffset);
        GPXTrack.addSegments(builder, SEGMENTSOffset);
        return GPXTrack.endGPXTrack(builder);
    }
    unpack() {
        return new GPXTrackT(this.NAME(), this.COMMENT(), this.DESCRIPTION(), this.SOURCE(), this.bb.createObjList(this.LINKS.bind(this), this.linksLength()), this.NUMBER(), this.TYPE(), this.bb.createObjList(this.SEGMENTS.bind(this), this.segmentsLength()));
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.COMMENT = this.COMMENT();
        _o.DESCRIPTION = this.DESCRIPTION();
        _o.SOURCE = this.SOURCE();
        _o.LINKS = this.bb.createObjList(this.LINKS.bind(this), this.linksLength());
        _o.NUMBER = this.NUMBER();
        _o.TYPE = this.TYPE();
        _o.SEGMENTS = this.bb.createObjList(this.SEGMENTS.bind(this), this.segmentsLength());
    }
}
export class GPXTrackT {
    constructor(NAME = null, COMMENT = null, DESCRIPTION = null, SOURCE = null, LINKS = [], NUMBER = 0, TYPE = null, SEGMENTS = []) {
        this.NAME = NAME;
        this.COMMENT = COMMENT;
        this.DESCRIPTION = DESCRIPTION;
        this.SOURCE = SOURCE;
        this.LINKS = LINKS;
        this.NUMBER = NUMBER;
        this.TYPE = TYPE;
        this.SEGMENTS = SEGMENTS;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const COMMENT = (this.COMMENT !== null ? builder.createString(this.COMMENT) : 0);
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        const SOURCE = (this.SOURCE !== null ? builder.createString(this.SOURCE) : 0);
        const LINKS = GPXTrack.createLinksVector(builder, builder.createObjectOffsetList(this.LINKS));
        const TYPE = (this.TYPE !== null ? builder.createString(this.TYPE) : 0);
        const SEGMENTS = GPXTrack.createSegmentsVector(builder, builder.createObjectOffsetList(this.SEGMENTS));
        return GPXTrack.createGPXTrack(builder, NAME, COMMENT, DESCRIPTION, SOURCE, LINKS, this.NUMBER, TYPE, SEGMENTS);
    }
}
