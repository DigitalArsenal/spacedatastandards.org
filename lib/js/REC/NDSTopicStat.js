// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Live traffic for one publish/subscribe topic.
 */
export class NDSTopicStat {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNDSTopicStat(bb, obj) {
        return (obj || new NDSTopicStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNDSTopicStat(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NDSTopicStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    TOPIC(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RATE_PER_MIN() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Unix seconds; 0 = not observed.
     */
    LAST_SEEN_AT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    SUBSCRIBED() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startNDSTopicStat(builder) {
        builder.startObject(4);
    }
    static addTopic(builder, TOPICOffset) {
        builder.addFieldOffset(0, TOPICOffset, 0);
    }
    static addRatePerMin(builder, RATE_PER_MIN) {
        builder.addFieldFloat64(1, RATE_PER_MIN, 0.0);
    }
    static addLastSeenAt(builder, LAST_SEEN_AT) {
        builder.addFieldInt64(2, LAST_SEEN_AT, BigInt('0'));
    }
    static addSubscribed(builder, SUBSCRIBED) {
        builder.addFieldInt8(3, +SUBSCRIBED, +false);
    }
    static endNDSTopicStat(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNDSTopicStat(builder, TOPICOffset, RATE_PER_MIN, LAST_SEEN_AT, SUBSCRIBED) {
        NDSTopicStat.startNDSTopicStat(builder);
        NDSTopicStat.addTopic(builder, TOPICOffset);
        NDSTopicStat.addRatePerMin(builder, RATE_PER_MIN);
        NDSTopicStat.addLastSeenAt(builder, LAST_SEEN_AT);
        NDSTopicStat.addSubscribed(builder, SUBSCRIBED);
        return NDSTopicStat.endNDSTopicStat(builder);
    }
    unpack() {
        return new NDSTopicStatT(this.TOPIC(), this.RATE_PER_MIN(), this.LAST_SEEN_AT(), this.SUBSCRIBED());
    }
    unpackTo(_o) {
        _o.TOPIC = this.TOPIC();
        _o.RATE_PER_MIN = this.RATE_PER_MIN();
        _o.LAST_SEEN_AT = this.LAST_SEEN_AT();
        _o.SUBSCRIBED = this.SUBSCRIBED();
    }
}
export class NDSTopicStatT {
    constructor(TOPIC = null, RATE_PER_MIN = 0.0, LAST_SEEN_AT = BigInt('0'), SUBSCRIBED = false) {
        this.TOPIC = TOPIC;
        this.RATE_PER_MIN = RATE_PER_MIN;
        this.LAST_SEEN_AT = LAST_SEEN_AT;
        this.SUBSCRIBED = SUBSCRIBED;
    }
    pack(builder) {
        const TOPIC = (this.TOPIC !== null ? builder.createString(this.TOPIC) : 0);
        return NDSTopicStat.createNDSTopicStat(builder, TOPIC, this.RATE_PER_MIN, this.LAST_SEEN_AT, this.SUBSCRIBED);
    }
}
