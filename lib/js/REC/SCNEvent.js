// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { EOO } from './EOO.js';
import { OMM } from './OMM.js';
import { RDO } from './RDO.js';
import { STV } from './STV.js';
/**
 * Imported scenario event payload composed from existing SDS records.
 */
export class SCNEvent {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCNEvent(bb, obj) {
        return (obj || new SCNEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCNEvent(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCNEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    EVENT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Element sets associated with the event.
     */
    ELSETS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new OMM()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    elsetsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * State vectors associated with the event.
     */
    STATES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new STV()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    statesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Electro-optical observations associated with the event.
     */
    EO_OBSERVATIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new EOO()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    eoObservationsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Radar observations associated with the event.
     */
    RADAR_OBSERVATIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new RDO()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    radarObservationsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startSCNEvent(builder) {
        builder.startObject(5);
    }
    static addEventId(builder, EVENT_IDOffset) {
        builder.addFieldOffset(0, EVENT_IDOffset, 0);
    }
    static addElsets(builder, ELSETSOffset) {
        builder.addFieldOffset(1, ELSETSOffset, 0);
    }
    static createElsetsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startElsetsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addStates(builder, STATESOffset) {
        builder.addFieldOffset(2, STATESOffset, 0);
    }
    static createStatesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startStatesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addEoObservations(builder, EO_OBSERVATIONSOffset) {
        builder.addFieldOffset(3, EO_OBSERVATIONSOffset, 0);
    }
    static createEoObservationsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startEoObservationsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addRadarObservations(builder, RADAR_OBSERVATIONSOffset) {
        builder.addFieldOffset(4, RADAR_OBSERVATIONSOffset, 0);
    }
    static createRadarObservationsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRadarObservationsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endSCNEvent(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCNEvent(builder, EVENT_IDOffset, ELSETSOffset, STATESOffset, EO_OBSERVATIONSOffset, RADAR_OBSERVATIONSOffset) {
        SCNEvent.startSCNEvent(builder);
        SCNEvent.addEventId(builder, EVENT_IDOffset);
        SCNEvent.addElsets(builder, ELSETSOffset);
        SCNEvent.addStates(builder, STATESOffset);
        SCNEvent.addEoObservations(builder, EO_OBSERVATIONSOffset);
        SCNEvent.addRadarObservations(builder, RADAR_OBSERVATIONSOffset);
        return SCNEvent.endSCNEvent(builder);
    }
    unpack() {
        return new SCNEventT(this.EVENT_ID(), this.bb.createObjList(this.ELSETS.bind(this), this.elsetsLength()), this.bb.createObjList(this.STATES.bind(this), this.statesLength()), this.bb.createObjList(this.EO_OBSERVATIONS.bind(this), this.eoObservationsLength()), this.bb.createObjList(this.RADAR_OBSERVATIONS.bind(this), this.radarObservationsLength()));
    }
    unpackTo(_o) {
        _o.EVENT_ID = this.EVENT_ID();
        _o.ELSETS = this.bb.createObjList(this.ELSETS.bind(this), this.elsetsLength());
        _o.STATES = this.bb.createObjList(this.STATES.bind(this), this.statesLength());
        _o.EO_OBSERVATIONS = this.bb.createObjList(this.EO_OBSERVATIONS.bind(this), this.eoObservationsLength());
        _o.RADAR_OBSERVATIONS = this.bb.createObjList(this.RADAR_OBSERVATIONS.bind(this), this.radarObservationsLength());
    }
}
export class SCNEventT {
    constructor(EVENT_ID = null, ELSETS = [], STATES = [], EO_OBSERVATIONS = [], RADAR_OBSERVATIONS = []) {
        this.EVENT_ID = EVENT_ID;
        this.ELSETS = ELSETS;
        this.STATES = STATES;
        this.EO_OBSERVATIONS = EO_OBSERVATIONS;
        this.RADAR_OBSERVATIONS = RADAR_OBSERVATIONS;
    }
    pack(builder) {
        const EVENT_ID = (this.EVENT_ID !== null ? builder.createString(this.EVENT_ID) : 0);
        const ELSETS = SCNEvent.createElsetsVector(builder, builder.createObjectOffsetList(this.ELSETS));
        const STATES = SCNEvent.createStatesVector(builder, builder.createObjectOffsetList(this.STATES));
        const EO_OBSERVATIONS = SCNEvent.createEoObservationsVector(builder, builder.createObjectOffsetList(this.EO_OBSERVATIONS));
        const RADAR_OBSERVATIONS = SCNEvent.createRadarObservationsVector(builder, builder.createObjectOffsetList(this.RADAR_OBSERVATIONS));
        return SCNEvent.createSCNEvent(builder, EVENT_ID, ELSETS, STATES, EO_OBSERVATIONS, RADAR_OBSERVATIONS);
    }
}
