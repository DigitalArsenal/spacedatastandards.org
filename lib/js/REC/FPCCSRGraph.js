// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Pre-built CSR graph ingestion request.
 */
export class FPCCSRGraph {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsFPCCSRGraph(bb, obj) {
        return (obj || new FPCCSRGraph()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsFPCCSRGraph(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FPCCSRGraph()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Number of vertices in the graph.
     */
    VERTEX_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * CSR row offsets (V + 1 entries).
     */
    OFFSETS(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    offsetsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    offsetsArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Uint32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * CSR destination indices.
     */
    DESTINATIONS(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    destinationsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    destinationsArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Uint32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * CSR edge weights.
     */
    WEIGHTS(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    weightsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    weightsArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startFPCCSRGraph(builder) {
        builder.startObject(4);
    }
    static addVertexCount(builder, VERTEX_COUNT) {
        builder.addFieldInt32(0, VERTEX_COUNT, 0);
    }
    static addOffsets(builder, OFFSETSOffset) {
        builder.addFieldOffset(1, OFFSETSOffset, 0);
    }
    static createOffsetsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startOffsetsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addDestinations(builder, DESTINATIONSOffset) {
        builder.addFieldOffset(2, DESTINATIONSOffset, 0);
    }
    static createDestinationsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startDestinationsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addWeights(builder, WEIGHTSOffset) {
        builder.addFieldOffset(3, WEIGHTSOffset, 0);
    }
    static createWeightsVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startWeightsVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endFPCCSRGraph(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createFPCCSRGraph(builder, VERTEX_COUNT, OFFSETSOffset, DESTINATIONSOffset, WEIGHTSOffset) {
        FPCCSRGraph.startFPCCSRGraph(builder);
        FPCCSRGraph.addVertexCount(builder, VERTEX_COUNT);
        FPCCSRGraph.addOffsets(builder, OFFSETSOffset);
        FPCCSRGraph.addDestinations(builder, DESTINATIONSOffset);
        FPCCSRGraph.addWeights(builder, WEIGHTSOffset);
        return FPCCSRGraph.endFPCCSRGraph(builder);
    }
    unpack() {
        return new FPCCSRGraphT(this.VERTEX_COUNT(), this.bb.createScalarList(this.OFFSETS.bind(this), this.offsetsLength()), this.bb.createScalarList(this.DESTINATIONS.bind(this), this.destinationsLength()), this.bb.createScalarList(this.WEIGHTS.bind(this), this.weightsLength()));
    }
    unpackTo(_o) {
        _o.VERTEX_COUNT = this.VERTEX_COUNT();
        _o.OFFSETS = this.bb.createScalarList(this.OFFSETS.bind(this), this.offsetsLength());
        _o.DESTINATIONS = this.bb.createScalarList(this.DESTINATIONS.bind(this), this.destinationsLength());
        _o.WEIGHTS = this.bb.createScalarList(this.WEIGHTS.bind(this), this.weightsLength());
    }
}
export class FPCCSRGraphT {
    constructor(VERTEX_COUNT = 0, OFFSETS = [], DESTINATIONS = [], WEIGHTS = []) {
        this.VERTEX_COUNT = VERTEX_COUNT;
        this.OFFSETS = OFFSETS;
        this.DESTINATIONS = DESTINATIONS;
        this.WEIGHTS = WEIGHTS;
    }
    pack(builder) {
        const OFFSETS = FPCCSRGraph.createOffsetsVector(builder, this.OFFSETS);
        const DESTINATIONS = FPCCSRGraph.createDestinationsVector(builder, this.DESTINATIONS);
        const WEIGHTS = FPCCSRGraph.createWeightsVector(builder, this.WEIGHTS);
        return FPCCSRGraph.createFPCCSRGraph(builder, this.VERTEX_COUNT, OFFSETS, DESTINATIONS, WEIGHTS);
    }
}
