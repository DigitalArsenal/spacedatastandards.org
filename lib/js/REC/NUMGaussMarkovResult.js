// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { numResultStatus } from './numResultStatus.js';
/**
 * Result of one Basilisk-compatible Gauss-Markov random sequence.
 */
export class NUMGaussMarkovResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNUMGaussMarkovResult(bb, obj) {
        return (obj || new NUMGaussMarkovResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNUMGaussMarkovResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NUMGaussMarkovResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : numResultStatus.OK;
    }
    ERROR_MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Post-warmup per-state sample means.
     */
    MEAN(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    meanLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    meanArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Post-warmup per-state sample standard deviations with n - 1 denominator.
     */
    STANDARD_DEVIATION(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    standardDeviationLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    standardDeviationArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Post-warmup per-state minima.
     */
    MINIMUM(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    minimumLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    minimumArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Post-warmup per-state maxima.
     */
    MAXIMUM(index) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    maximumLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    maximumArray() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * State after the final generated sample.
     */
    FINAL_STATE(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    finalStateLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    finalStateArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Optional flattened sample-major state history.
     */
    SAMPLES(index) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    samplesLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    samplesArray() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startNUMGaussMarkovResult(builder) {
        builder.startObject(9);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(0, STATUS, numResultStatus.OK);
    }
    static addErrorMessage(builder, ERROR_MESSAGEOffset) {
        builder.addFieldOffset(1, ERROR_MESSAGEOffset, 0);
    }
    static addMean(builder, MEANOffset) {
        builder.addFieldOffset(2, MEANOffset, 0);
    }
    static createMeanVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startMeanVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addStandardDeviation(builder, STANDARD_DEVIATIONOffset) {
        builder.addFieldOffset(3, STANDARD_DEVIATIONOffset, 0);
    }
    static createStandardDeviationVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startStandardDeviationVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addMinimum(builder, MINIMUMOffset) {
        builder.addFieldOffset(4, MINIMUMOffset, 0);
    }
    static createMinimumVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startMinimumVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addMaximum(builder, MAXIMUMOffset) {
        builder.addFieldOffset(5, MAXIMUMOffset, 0);
    }
    static createMaximumVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startMaximumVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addFinalState(builder, FINAL_STATEOffset) {
        builder.addFieldOffset(6, FINAL_STATEOffset, 0);
    }
    static createFinalStateVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startFinalStateVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addSamples(builder, SAMPLESOffset) {
        builder.addFieldOffset(7, SAMPLESOffset, 0);
    }
    static createSamplesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startSamplesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(8, TRACE_IDOffset, 0);
    }
    static endNUMGaussMarkovResult(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNUMGaussMarkovResult(builder, STATUS, ERROR_MESSAGEOffset, MEANOffset, STANDARD_DEVIATIONOffset, MINIMUMOffset, MAXIMUMOffset, FINAL_STATEOffset, SAMPLESOffset, TRACE_IDOffset) {
        NUMGaussMarkovResult.startNUMGaussMarkovResult(builder);
        NUMGaussMarkovResult.addStatus(builder, STATUS);
        NUMGaussMarkovResult.addErrorMessage(builder, ERROR_MESSAGEOffset);
        NUMGaussMarkovResult.addMean(builder, MEANOffset);
        NUMGaussMarkovResult.addStandardDeviation(builder, STANDARD_DEVIATIONOffset);
        NUMGaussMarkovResult.addMinimum(builder, MINIMUMOffset);
        NUMGaussMarkovResult.addMaximum(builder, MAXIMUMOffset);
        NUMGaussMarkovResult.addFinalState(builder, FINAL_STATEOffset);
        NUMGaussMarkovResult.addSamples(builder, SAMPLESOffset);
        NUMGaussMarkovResult.addTraceId(builder, TRACE_IDOffset);
        return NUMGaussMarkovResult.endNUMGaussMarkovResult(builder);
    }
    unpack() {
        return new NUMGaussMarkovResultT(this.STATUS(), this.ERROR_MESSAGE(), this.bb.createScalarList(this.MEAN.bind(this), this.meanLength()), this.bb.createScalarList(this.STANDARD_DEVIATION.bind(this), this.standardDeviationLength()), this.bb.createScalarList(this.MINIMUM.bind(this), this.minimumLength()), this.bb.createScalarList(this.MAXIMUM.bind(this), this.maximumLength()), this.bb.createScalarList(this.FINAL_STATE.bind(this), this.finalStateLength()), this.bb.createScalarList(this.SAMPLES.bind(this), this.samplesLength()), this.TRACE_ID());
    }
    unpackTo(_o) {
        _o.STATUS = this.STATUS();
        _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
        _o.MEAN = this.bb.createScalarList(this.MEAN.bind(this), this.meanLength());
        _o.STANDARD_DEVIATION = this.bb.createScalarList(this.STANDARD_DEVIATION.bind(this), this.standardDeviationLength());
        _o.MINIMUM = this.bb.createScalarList(this.MINIMUM.bind(this), this.minimumLength());
        _o.MAXIMUM = this.bb.createScalarList(this.MAXIMUM.bind(this), this.maximumLength());
        _o.FINAL_STATE = this.bb.createScalarList(this.FINAL_STATE.bind(this), this.finalStateLength());
        _o.SAMPLES = this.bb.createScalarList(this.SAMPLES.bind(this), this.samplesLength());
        _o.TRACE_ID = this.TRACE_ID();
    }
}
export class NUMGaussMarkovResultT {
    constructor(STATUS = numResultStatus.OK, ERROR_MESSAGE = null, MEAN = [], STANDARD_DEVIATION = [], MINIMUM = [], MAXIMUM = [], FINAL_STATE = [], SAMPLES = [], TRACE_ID = null) {
        this.STATUS = STATUS;
        this.ERROR_MESSAGE = ERROR_MESSAGE;
        this.MEAN = MEAN;
        this.STANDARD_DEVIATION = STANDARD_DEVIATION;
        this.MINIMUM = MINIMUM;
        this.MAXIMUM = MAXIMUM;
        this.FINAL_STATE = FINAL_STATE;
        this.SAMPLES = SAMPLES;
        this.TRACE_ID = TRACE_ID;
    }
    pack(builder) {
        const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE) : 0);
        const MEAN = NUMGaussMarkovResult.createMeanVector(builder, this.MEAN);
        const STANDARD_DEVIATION = NUMGaussMarkovResult.createStandardDeviationVector(builder, this.STANDARD_DEVIATION);
        const MINIMUM = NUMGaussMarkovResult.createMinimumVector(builder, this.MINIMUM);
        const MAXIMUM = NUMGaussMarkovResult.createMaximumVector(builder, this.MAXIMUM);
        const FINAL_STATE = NUMGaussMarkovResult.createFinalStateVector(builder, this.FINAL_STATE);
        const SAMPLES = NUMGaussMarkovResult.createSamplesVector(builder, this.SAMPLES);
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        return NUMGaussMarkovResult.createNUMGaussMarkovResult(builder, this.STATUS, ERROR_MESSAGE, MEAN, STANDARD_DEVIATION, MINIMUM, MAXIMUM, FINAL_STATE, SAMPLES, TRACE_ID);
    }
}
