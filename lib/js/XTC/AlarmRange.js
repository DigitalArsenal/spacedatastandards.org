// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Numeric alarm range
 */
export class AlarmRange {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAlarmRange(bb, obj) {
        return (obj || new AlarmRange()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAlarmRange(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new AlarmRange()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Minimum value (inclusive)
     */
    MIN_INCLUSIVE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum value (inclusive)
     */
    MAX_INCLUSIVE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Minimum value (exclusive)
     */
    MIN_EXCLUSIVE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum value (exclusive)
     */
    MAX_EXCLUSIVE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startAlarmRange(builder) {
        builder.startObject(4);
    }
    static addMinInclusive(builder, MIN_INCLUSIVE) {
        builder.addFieldFloat64(0, MIN_INCLUSIVE, 0.0);
    }
    static addMaxInclusive(builder, MAX_INCLUSIVE) {
        builder.addFieldFloat64(1, MAX_INCLUSIVE, 0.0);
    }
    static addMinExclusive(builder, MIN_EXCLUSIVE) {
        builder.addFieldFloat64(2, MIN_EXCLUSIVE, 0.0);
    }
    static addMaxExclusive(builder, MAX_EXCLUSIVE) {
        builder.addFieldFloat64(3, MAX_EXCLUSIVE, 0.0);
    }
    static endAlarmRange(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createAlarmRange(builder, MIN_INCLUSIVE, MAX_INCLUSIVE, MIN_EXCLUSIVE, MAX_EXCLUSIVE) {
        AlarmRange.startAlarmRange(builder);
        AlarmRange.addMinInclusive(builder, MIN_INCLUSIVE);
        AlarmRange.addMaxInclusive(builder, MAX_INCLUSIVE);
        AlarmRange.addMinExclusive(builder, MIN_EXCLUSIVE);
        AlarmRange.addMaxExclusive(builder, MAX_EXCLUSIVE);
        return AlarmRange.endAlarmRange(builder);
    }
    unpack() {
        return new AlarmRangeT(this.MIN_INCLUSIVE(), this.MAX_INCLUSIVE(), this.MIN_EXCLUSIVE(), this.MAX_EXCLUSIVE());
    }
    unpackTo(_o) {
        _o.MIN_INCLUSIVE = this.MIN_INCLUSIVE();
        _o.MAX_INCLUSIVE = this.MAX_INCLUSIVE();
        _o.MIN_EXCLUSIVE = this.MIN_EXCLUSIVE();
        _o.MAX_EXCLUSIVE = this.MAX_EXCLUSIVE();
    }
}
export class AlarmRangeT {
    constructor(MIN_INCLUSIVE = 0.0, MAX_INCLUSIVE = 0.0, MIN_EXCLUSIVE = 0.0, MAX_EXCLUSIVE = 0.0) {
        this.MIN_INCLUSIVE = MIN_INCLUSIVE;
        this.MAX_INCLUSIVE = MAX_INCLUSIVE;
        this.MIN_EXCLUSIVE = MIN_EXCLUSIVE;
        this.MAX_EXCLUSIVE = MAX_EXCLUSIVE;
    }
    pack(builder) {
        return AlarmRange.createAlarmRange(builder, this.MIN_INCLUSIVE, this.MAX_INCLUSIVE, this.MIN_EXCLUSIVE, this.MAX_EXCLUSIVE);
    }
}
