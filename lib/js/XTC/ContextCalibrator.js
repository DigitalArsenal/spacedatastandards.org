// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { MatchCriteria } from './MatchCriteria.js';
import { MathOperation } from './MathOperation.js';
import { PolynomialCalibrator } from './PolynomialCalibrator.js';
import { SplineCalibrator } from './SplineCalibrator.js';
/**
 * Context-dependent calibrator selection
 */
export class ContextCalibrator {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsContextCalibrator(bb, obj) {
        return (obj || new ContextCalibrator()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsContextCalibrator(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ContextCalibrator()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Match criteria for selecting this calibrator
     */
    MATCH_CRITERIA(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new MatchCriteria()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Polynomial calibrator (if used)
     */
    POLYNOMIAL(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new PolynomialCalibrator()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Spline calibrator (if used)
     */
    SPLINE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new SplineCalibrator()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Math operation calibrator (if used)
     */
    MATH_OPERATION(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new MathOperation()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startContextCalibrator(builder) {
        builder.startObject(4);
    }
    static addMatchCriteria(builder, MATCH_CRITERIAOffset) {
        builder.addFieldOffset(0, MATCH_CRITERIAOffset, 0);
    }
    static addPolynomial(builder, POLYNOMIALOffset) {
        builder.addFieldOffset(1, POLYNOMIALOffset, 0);
    }
    static addSpline(builder, SPLINEOffset) {
        builder.addFieldOffset(2, SPLINEOffset, 0);
    }
    static addMathOperation(builder, MATH_OPERATIONOffset) {
        builder.addFieldOffset(3, MATH_OPERATIONOffset, 0);
    }
    static endContextCalibrator(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new ContextCalibratorT((this.MATCH_CRITERIA() !== null ? this.MATCH_CRITERIA().unpack() : null), (this.POLYNOMIAL() !== null ? this.POLYNOMIAL().unpack() : null), (this.SPLINE() !== null ? this.SPLINE().unpack() : null), (this.MATH_OPERATION() !== null ? this.MATH_OPERATION().unpack() : null));
    }
    unpackTo(_o) {
        _o.MATCH_CRITERIA = (this.MATCH_CRITERIA() !== null ? this.MATCH_CRITERIA().unpack() : null);
        _o.POLYNOMIAL = (this.POLYNOMIAL() !== null ? this.POLYNOMIAL().unpack() : null);
        _o.SPLINE = (this.SPLINE() !== null ? this.SPLINE().unpack() : null);
        _o.MATH_OPERATION = (this.MATH_OPERATION() !== null ? this.MATH_OPERATION().unpack() : null);
    }
}
export class ContextCalibratorT {
    constructor(MATCH_CRITERIA = null, POLYNOMIAL = null, SPLINE = null, MATH_OPERATION = null) {
        this.MATCH_CRITERIA = MATCH_CRITERIA;
        this.POLYNOMIAL = POLYNOMIAL;
        this.SPLINE = SPLINE;
        this.MATH_OPERATION = MATH_OPERATION;
    }
    pack(builder) {
        const MATCH_CRITERIA = (this.MATCH_CRITERIA !== null ? this.MATCH_CRITERIA.pack(builder) : 0);
        const POLYNOMIAL = (this.POLYNOMIAL !== null ? this.POLYNOMIAL.pack(builder) : 0);
        const SPLINE = (this.SPLINE !== null ? this.SPLINE.pack(builder) : 0);
        const MATH_OPERATION = (this.MATH_OPERATION !== null ? this.MATH_OPERATION.pack(builder) : 0);
        ContextCalibrator.startContextCalibrator(builder);
        ContextCalibrator.addMatchCriteria(builder, MATCH_CRITERIA);
        ContextCalibrator.addPolynomial(builder, POLYNOMIAL);
        ContextCalibrator.addSpline(builder, SPLINE);
        ContextCalibrator.addMathOperation(builder, MATH_OPERATION);
        return ContextCalibrator.endContextCalibrator(builder);
    }
}
