// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { AlgorithmInput } from './AlgorithmInput.js';
import { AlgorithmOutput } from './AlgorithmOutput.js';
import { AlgorithmTrigger } from './AlgorithmTrigger.js';
/**
 * Custom algorithm definition
 */
export class CustomAlgorithm {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCustomAlgorithm(bb, obj) {
        return (obj || new CustomAlgorithm()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCustomAlgorithm(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CustomAlgorithm()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SHORT_DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    LONG_DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    LANGUAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ALGORITHM_TEXT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EXTERNAL_ALGORITHM_REF(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Input bindings
     */
    INPUTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new AlgorithmInput()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    inputsLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Output bindings
     */
    OUTPUTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new AlgorithmOutput()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    outputsLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Trigger conditions
     */
    TRIGGERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? (obj || new AlgorithmTrigger()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    triggersLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startCustomAlgorithm(builder) {
        builder.startObject(9);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addShortDescription(builder, SHORT_DESCRIPTIONOffset) {
        builder.addFieldOffset(1, SHORT_DESCRIPTIONOffset, 0);
    }
    static addLongDescription(builder, LONG_DESCRIPTIONOffset) {
        builder.addFieldOffset(2, LONG_DESCRIPTIONOffset, 0);
    }
    static addLanguage(builder, LANGUAGEOffset) {
        builder.addFieldOffset(3, LANGUAGEOffset, 0);
    }
    static addAlgorithmText(builder, ALGORITHM_TEXTOffset) {
        builder.addFieldOffset(4, ALGORITHM_TEXTOffset, 0);
    }
    static addExternalAlgorithmRef(builder, EXTERNAL_ALGORITHM_REFOffset) {
        builder.addFieldOffset(5, EXTERNAL_ALGORITHM_REFOffset, 0);
    }
    static addInputs(builder, INPUTSOffset) {
        builder.addFieldOffset(6, INPUTSOffset, 0);
    }
    static createInputsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startInputsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addOutputs(builder, OUTPUTSOffset) {
        builder.addFieldOffset(7, OUTPUTSOffset, 0);
    }
    static createOutputsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startOutputsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTriggers(builder, TRIGGERSOffset) {
        builder.addFieldOffset(8, TRIGGERSOffset, 0);
    }
    static createTriggersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTriggersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endCustomAlgorithm(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createCustomAlgorithm(builder, NAMEOffset, SHORT_DESCRIPTIONOffset, LONG_DESCRIPTIONOffset, LANGUAGEOffset, ALGORITHM_TEXTOffset, EXTERNAL_ALGORITHM_REFOffset, INPUTSOffset, OUTPUTSOffset, TRIGGERSOffset) {
        CustomAlgorithm.startCustomAlgorithm(builder);
        CustomAlgorithm.addName(builder, NAMEOffset);
        CustomAlgorithm.addShortDescription(builder, SHORT_DESCRIPTIONOffset);
        CustomAlgorithm.addLongDescription(builder, LONG_DESCRIPTIONOffset);
        CustomAlgorithm.addLanguage(builder, LANGUAGEOffset);
        CustomAlgorithm.addAlgorithmText(builder, ALGORITHM_TEXTOffset);
        CustomAlgorithm.addExternalAlgorithmRef(builder, EXTERNAL_ALGORITHM_REFOffset);
        CustomAlgorithm.addInputs(builder, INPUTSOffset);
        CustomAlgorithm.addOutputs(builder, OUTPUTSOffset);
        CustomAlgorithm.addTriggers(builder, TRIGGERSOffset);
        return CustomAlgorithm.endCustomAlgorithm(builder);
    }
    unpack() {
        return new CustomAlgorithmT(this.NAME(), this.SHORT_DESCRIPTION(), this.LONG_DESCRIPTION(), this.LANGUAGE(), this.ALGORITHM_TEXT(), this.EXTERNAL_ALGORITHM_REF(), this.bb.createObjList(this.INPUTS.bind(this), this.inputsLength()), this.bb.createObjList(this.OUTPUTS.bind(this), this.outputsLength()), this.bb.createObjList(this.TRIGGERS.bind(this), this.triggersLength()));
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.SHORT_DESCRIPTION = this.SHORT_DESCRIPTION();
        _o.LONG_DESCRIPTION = this.LONG_DESCRIPTION();
        _o.LANGUAGE = this.LANGUAGE();
        _o.ALGORITHM_TEXT = this.ALGORITHM_TEXT();
        _o.EXTERNAL_ALGORITHM_REF = this.EXTERNAL_ALGORITHM_REF();
        _o.INPUTS = this.bb.createObjList(this.INPUTS.bind(this), this.inputsLength());
        _o.OUTPUTS = this.bb.createObjList(this.OUTPUTS.bind(this), this.outputsLength());
        _o.TRIGGERS = this.bb.createObjList(this.TRIGGERS.bind(this), this.triggersLength());
    }
}
export class CustomAlgorithmT {
    constructor(NAME = null, SHORT_DESCRIPTION = null, LONG_DESCRIPTION = null, LANGUAGE = null, ALGORITHM_TEXT = null, EXTERNAL_ALGORITHM_REF = null, INPUTS = [], OUTPUTS = [], TRIGGERS = []) {
        this.NAME = NAME;
        this.SHORT_DESCRIPTION = SHORT_DESCRIPTION;
        this.LONG_DESCRIPTION = LONG_DESCRIPTION;
        this.LANGUAGE = LANGUAGE;
        this.ALGORITHM_TEXT = ALGORITHM_TEXT;
        this.EXTERNAL_ALGORITHM_REF = EXTERNAL_ALGORITHM_REF;
        this.INPUTS = INPUTS;
        this.OUTPUTS = OUTPUTS;
        this.TRIGGERS = TRIGGERS;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const SHORT_DESCRIPTION = (this.SHORT_DESCRIPTION !== null ? builder.createString(this.SHORT_DESCRIPTION) : 0);
        const LONG_DESCRIPTION = (this.LONG_DESCRIPTION !== null ? builder.createString(this.LONG_DESCRIPTION) : 0);
        const LANGUAGE = (this.LANGUAGE !== null ? builder.createString(this.LANGUAGE) : 0);
        const ALGORITHM_TEXT = (this.ALGORITHM_TEXT !== null ? builder.createString(this.ALGORITHM_TEXT) : 0);
        const EXTERNAL_ALGORITHM_REF = (this.EXTERNAL_ALGORITHM_REF !== null ? builder.createString(this.EXTERNAL_ALGORITHM_REF) : 0);
        const INPUTS = CustomAlgorithm.createInputsVector(builder, builder.createObjectOffsetList(this.INPUTS));
        const OUTPUTS = CustomAlgorithm.createOutputsVector(builder, builder.createObjectOffsetList(this.OUTPUTS));
        const TRIGGERS = CustomAlgorithm.createTriggersVector(builder, builder.createObjectOffsetList(this.TRIGGERS));
        return CustomAlgorithm.createCustomAlgorithm(builder, NAME, SHORT_DESCRIPTION, LONG_DESCRIPTION, LANGUAGE, ALGORITHM_TEXT, EXTERNAL_ALGORITHM_REF, INPUTS, OUTPUTS, TRIGGERS);
    }
}
