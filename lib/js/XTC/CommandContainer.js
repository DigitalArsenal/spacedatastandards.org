// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { BaseContainer } from './BaseContainer.js';
import { CommandContainerEntry } from './CommandContainerEntry.js';
/**
 * Command container definition
 */
export class CommandContainer {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCommandContainer(bb, obj) {
        return (obj || new CommandContainer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCommandContainer(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CommandContainer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Entry list
     */
    ENTRY_LIST(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new CommandContainerEntry()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    entryListLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Base container reference
     */
    BASE_CONTAINER(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new BaseContainer()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startCommandContainer(builder) {
        builder.startObject(3);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addEntryList(builder, ENTRY_LISTOffset) {
        builder.addFieldOffset(1, ENTRY_LISTOffset, 0);
    }
    static createEntryListVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startEntryListVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addBaseContainer(builder, BASE_CONTAINEROffset) {
        builder.addFieldOffset(2, BASE_CONTAINEROffset, 0);
    }
    static endCommandContainer(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new CommandContainerT(this.NAME(), this.bb.createObjList(this.ENTRY_LIST.bind(this), this.entryListLength()), (this.BASE_CONTAINER() !== null ? this.BASE_CONTAINER().unpack() : null));
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.ENTRY_LIST = this.bb.createObjList(this.ENTRY_LIST.bind(this), this.entryListLength());
        _o.BASE_CONTAINER = (this.BASE_CONTAINER() !== null ? this.BASE_CONTAINER().unpack() : null);
    }
}
export class CommandContainerT {
    constructor(NAME = null, ENTRY_LIST = [], BASE_CONTAINER = null) {
        this.NAME = NAME;
        this.ENTRY_LIST = ENTRY_LIST;
        this.BASE_CONTAINER = BASE_CONTAINER;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const ENTRY_LIST = CommandContainer.createEntryListVector(builder, builder.createObjectOffsetList(this.ENTRY_LIST));
        const BASE_CONTAINER = (this.BASE_CONTAINER !== null ? this.BASE_CONTAINER.pack(builder) : 0);
        CommandContainer.startCommandContainer(builder);
        CommandContainer.addName(builder, NAME);
        CommandContainer.addEntryList(builder, ENTRY_LIST);
        CommandContainer.addBaseContainer(builder, BASE_CONTAINER);
        return CommandContainer.endCommandContainer(builder);
    }
}
