// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Everything a resuming reader needs to decode a chunk that does not begin at
 * offset 0. Required whenever NEXT_OFFSET is past the source's own header.
 */
export class IRMDecodeContext {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsIRMDecodeContext(bb, obj) {
        return (obj || new IRMDecodeContext()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsIRMDecodeContext(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new IRMDecodeContext()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    FORMAT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    HEADER_LINE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Byte length the header occupied at the start of the source, including its
     * terminator, so a reader can tell a header-bearing prefix from a data row.
     */
    HEADER_BYTE_LENGTH() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    COLUMN_NAMES(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    columnNamesLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    DELIMITER(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    QUOTE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ESCAPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    LINE_TERMINATOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CHARSET(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    COMPRESSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Position in the DECODED stream corresponding to NEXT_OFFSET, when the
     * source is compressed. 0 with a non-empty COMPRESSION means the decoded
     * stream has not been advanced.
     */
    DECODED_OFFSET() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Serialized, pointer-free image of the decoder at the seam — the state a
     * codec with no restart point needs in order to have one. Opaque to every
     * consumer but a decoder whose stamps match exactly. Never partially applied.
     */
    DECODER_STATE(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    decoderStateLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    decoderStateArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    DECODER_STATE_FORMAT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DECODER_STATE_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Byte length DECODER_STATE is expected to have. Carried even though the
     * vector knows its own size, so a size disagreement is detectable BEFORE the
     * image is loaded into a differently shaped struct.
     */
    DECODER_STATE_BYTE_LENGTH() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    DECODER_STATE_SHA256(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DECODER_BUILD_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DECODER_STATE_MEDIA_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * The decoder's history window at the seam — the bytes that ARE the codec's
     * back-reference dictionary. Carried separately from DECODER_STATE because it
     * is bulk data of a size the codec fixes, not part of the struct image.
     */
    WINDOW(index) {
        const offset = this.bb.__offset(this.bb_pos, 40);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    windowLength() {
        const offset = this.bb.__offset(this.bb_pos, 40);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    windowArray() {
        const offset = this.bb.__offset(this.bb_pos, 40);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Byte length WINDOW is expected to have, for the same detect-before-load
     * reason as DECODER_STATE_BYTE_LENGTH.
     */
    WINDOW_BYTE_LENGTH() {
        const offset = this.bb.__offset(this.bb_pos, 42);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Offset into WINDOW at which the codec will next write, when the window is
     * a circular buffer whose contents cannot be interpreted without it.
     */
    WINDOW_POSITION() {
        const offset = this.bb.__offset(this.bb_pos, 44);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * The bytes of the record that straddles the seam: everything the previous
     * chunk decoded of a record it could not finish. A resuming decoder prepends
     * these to the next chunk's decoded output before parsing. Carried as bytes,
     * not text, because a seam may fall inside a multi-byte character.
     */
    PARTIAL_RECORD(index) {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    partialRecordLength() {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    partialRecordArray() {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startIRMDecodeContext(builder) {
        builder.startObject(22);
    }
    static addFormat(builder, FORMATOffset) {
        builder.addFieldOffset(0, FORMATOffset, 0);
    }
    static addHeaderLine(builder, HEADER_LINEOffset) {
        builder.addFieldOffset(1, HEADER_LINEOffset, 0);
    }
    static addHeaderByteLength(builder, HEADER_BYTE_LENGTH) {
        builder.addFieldInt64(2, HEADER_BYTE_LENGTH, BigInt('0'));
    }
    static addColumnNames(builder, COLUMN_NAMESOffset) {
        builder.addFieldOffset(3, COLUMN_NAMESOffset, 0);
    }
    static createColumnNamesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startColumnNamesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addDelimiter(builder, DELIMITEROffset) {
        builder.addFieldOffset(4, DELIMITEROffset, 0);
    }
    static addQuote(builder, QUOTEOffset) {
        builder.addFieldOffset(5, QUOTEOffset, 0);
    }
    static addEscape(builder, ESCAPEOffset) {
        builder.addFieldOffset(6, ESCAPEOffset, 0);
    }
    static addLineTerminator(builder, LINE_TERMINATOROffset) {
        builder.addFieldOffset(7, LINE_TERMINATOROffset, 0);
    }
    static addCharset(builder, CHARSETOffset) {
        builder.addFieldOffset(8, CHARSETOffset, 0);
    }
    static addCompression(builder, COMPRESSIONOffset) {
        builder.addFieldOffset(9, COMPRESSIONOffset, 0);
    }
    static addDecodedOffset(builder, DECODED_OFFSET) {
        builder.addFieldInt64(10, DECODED_OFFSET, BigInt('0'));
    }
    static addDecoderState(builder, DECODER_STATEOffset) {
        builder.addFieldOffset(11, DECODER_STATEOffset, 0);
    }
    static createDecoderStateVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startDecoderStateVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addDecoderStateFormat(builder, DECODER_STATE_FORMATOffset) {
        builder.addFieldOffset(12, DECODER_STATE_FORMATOffset, 0);
    }
    static addDecoderStateVersion(builder, DECODER_STATE_VERSIONOffset) {
        builder.addFieldOffset(13, DECODER_STATE_VERSIONOffset, 0);
    }
    static addDecoderStateByteLength(builder, DECODER_STATE_BYTE_LENGTH) {
        builder.addFieldInt64(14, DECODER_STATE_BYTE_LENGTH, BigInt('0'));
    }
    static addDecoderStateSha256(builder, DECODER_STATE_SHA256Offset) {
        builder.addFieldOffset(15, DECODER_STATE_SHA256Offset, 0);
    }
    static addDecoderBuildId(builder, DECODER_BUILD_IDOffset) {
        builder.addFieldOffset(16, DECODER_BUILD_IDOffset, 0);
    }
    static addDecoderStateMediaType(builder, DECODER_STATE_MEDIA_TYPEOffset) {
        builder.addFieldOffset(17, DECODER_STATE_MEDIA_TYPEOffset, 0);
    }
    static addWindow(builder, WINDOWOffset) {
        builder.addFieldOffset(18, WINDOWOffset, 0);
    }
    static createWindowVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startWindowVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addWindowByteLength(builder, WINDOW_BYTE_LENGTH) {
        builder.addFieldInt64(19, WINDOW_BYTE_LENGTH, BigInt('0'));
    }
    static addWindowPosition(builder, WINDOW_POSITION) {
        builder.addFieldInt64(20, WINDOW_POSITION, BigInt('0'));
    }
    static addPartialRecord(builder, PARTIAL_RECORDOffset) {
        builder.addFieldOffset(21, PARTIAL_RECORDOffset, 0);
    }
    static createPartialRecordVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startPartialRecordVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endIRMDecodeContext(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // FORMAT
        return offset;
    }
    static createIRMDecodeContext(builder, FORMATOffset, HEADER_LINEOffset, HEADER_BYTE_LENGTH, COLUMN_NAMESOffset, DELIMITEROffset, QUOTEOffset, ESCAPEOffset, LINE_TERMINATOROffset, CHARSETOffset, COMPRESSIONOffset, DECODED_OFFSET, DECODER_STATEOffset, DECODER_STATE_FORMATOffset, DECODER_STATE_VERSIONOffset, DECODER_STATE_BYTE_LENGTH, DECODER_STATE_SHA256Offset, DECODER_BUILD_IDOffset, DECODER_STATE_MEDIA_TYPEOffset, WINDOWOffset, WINDOW_BYTE_LENGTH, WINDOW_POSITION, PARTIAL_RECORDOffset) {
        IRMDecodeContext.startIRMDecodeContext(builder);
        IRMDecodeContext.addFormat(builder, FORMATOffset);
        IRMDecodeContext.addHeaderLine(builder, HEADER_LINEOffset);
        IRMDecodeContext.addHeaderByteLength(builder, HEADER_BYTE_LENGTH);
        IRMDecodeContext.addColumnNames(builder, COLUMN_NAMESOffset);
        IRMDecodeContext.addDelimiter(builder, DELIMITEROffset);
        IRMDecodeContext.addQuote(builder, QUOTEOffset);
        IRMDecodeContext.addEscape(builder, ESCAPEOffset);
        IRMDecodeContext.addLineTerminator(builder, LINE_TERMINATOROffset);
        IRMDecodeContext.addCharset(builder, CHARSETOffset);
        IRMDecodeContext.addCompression(builder, COMPRESSIONOffset);
        IRMDecodeContext.addDecodedOffset(builder, DECODED_OFFSET);
        IRMDecodeContext.addDecoderState(builder, DECODER_STATEOffset);
        IRMDecodeContext.addDecoderStateFormat(builder, DECODER_STATE_FORMATOffset);
        IRMDecodeContext.addDecoderStateVersion(builder, DECODER_STATE_VERSIONOffset);
        IRMDecodeContext.addDecoderStateByteLength(builder, DECODER_STATE_BYTE_LENGTH);
        IRMDecodeContext.addDecoderStateSha256(builder, DECODER_STATE_SHA256Offset);
        IRMDecodeContext.addDecoderBuildId(builder, DECODER_BUILD_IDOffset);
        IRMDecodeContext.addDecoderStateMediaType(builder, DECODER_STATE_MEDIA_TYPEOffset);
        IRMDecodeContext.addWindow(builder, WINDOWOffset);
        IRMDecodeContext.addWindowByteLength(builder, WINDOW_BYTE_LENGTH);
        IRMDecodeContext.addWindowPosition(builder, WINDOW_POSITION);
        IRMDecodeContext.addPartialRecord(builder, PARTIAL_RECORDOffset);
        return IRMDecodeContext.endIRMDecodeContext(builder);
    }
    unpack() {
        return new IRMDecodeContextT(this.FORMAT(), this.HEADER_LINE(), this.HEADER_BYTE_LENGTH(), this.bb.createScalarList(this.COLUMN_NAMES.bind(this), this.columnNamesLength()), this.DELIMITER(), this.QUOTE(), this.ESCAPE(), this.LINE_TERMINATOR(), this.CHARSET(), this.COMPRESSION(), this.DECODED_OFFSET(), this.bb.createScalarList(this.DECODER_STATE.bind(this), this.decoderStateLength()), this.DECODER_STATE_FORMAT(), this.DECODER_STATE_VERSION(), this.DECODER_STATE_BYTE_LENGTH(), this.DECODER_STATE_SHA256(), this.DECODER_BUILD_ID(), this.DECODER_STATE_MEDIA_TYPE(), this.bb.createScalarList(this.WINDOW.bind(this), this.windowLength()), this.WINDOW_BYTE_LENGTH(), this.WINDOW_POSITION(), this.bb.createScalarList(this.PARTIAL_RECORD.bind(this), this.partialRecordLength()));
    }
    unpackTo(_o) {
        _o.FORMAT = this.FORMAT();
        _o.HEADER_LINE = this.HEADER_LINE();
        _o.HEADER_BYTE_LENGTH = this.HEADER_BYTE_LENGTH();
        _o.COLUMN_NAMES = this.bb.createScalarList(this.COLUMN_NAMES.bind(this), this.columnNamesLength());
        _o.DELIMITER = this.DELIMITER();
        _o.QUOTE = this.QUOTE();
        _o.ESCAPE = this.ESCAPE();
        _o.LINE_TERMINATOR = this.LINE_TERMINATOR();
        _o.CHARSET = this.CHARSET();
        _o.COMPRESSION = this.COMPRESSION();
        _o.DECODED_OFFSET = this.DECODED_OFFSET();
        _o.DECODER_STATE = this.bb.createScalarList(this.DECODER_STATE.bind(this), this.decoderStateLength());
        _o.DECODER_STATE_FORMAT = this.DECODER_STATE_FORMAT();
        _o.DECODER_STATE_VERSION = this.DECODER_STATE_VERSION();
        _o.DECODER_STATE_BYTE_LENGTH = this.DECODER_STATE_BYTE_LENGTH();
        _o.DECODER_STATE_SHA256 = this.DECODER_STATE_SHA256();
        _o.DECODER_BUILD_ID = this.DECODER_BUILD_ID();
        _o.DECODER_STATE_MEDIA_TYPE = this.DECODER_STATE_MEDIA_TYPE();
        _o.WINDOW = this.bb.createScalarList(this.WINDOW.bind(this), this.windowLength());
        _o.WINDOW_BYTE_LENGTH = this.WINDOW_BYTE_LENGTH();
        _o.WINDOW_POSITION = this.WINDOW_POSITION();
        _o.PARTIAL_RECORD = this.bb.createScalarList(this.PARTIAL_RECORD.bind(this), this.partialRecordLength());
    }
}
export class IRMDecodeContextT {
    constructor(FORMAT = null, HEADER_LINE = null, HEADER_BYTE_LENGTH = BigInt('0'), COLUMN_NAMES = [], DELIMITER = null, QUOTE = null, ESCAPE = null, LINE_TERMINATOR = null, CHARSET = null, COMPRESSION = null, DECODED_OFFSET = BigInt('0'), DECODER_STATE = [], DECODER_STATE_FORMAT = null, DECODER_STATE_VERSION = null, DECODER_STATE_BYTE_LENGTH = BigInt('0'), DECODER_STATE_SHA256 = null, DECODER_BUILD_ID = null, DECODER_STATE_MEDIA_TYPE = null, WINDOW = [], WINDOW_BYTE_LENGTH = BigInt('0'), WINDOW_POSITION = BigInt('0'), PARTIAL_RECORD = []) {
        this.FORMAT = FORMAT;
        this.HEADER_LINE = HEADER_LINE;
        this.HEADER_BYTE_LENGTH = HEADER_BYTE_LENGTH;
        this.COLUMN_NAMES = COLUMN_NAMES;
        this.DELIMITER = DELIMITER;
        this.QUOTE = QUOTE;
        this.ESCAPE = ESCAPE;
        this.LINE_TERMINATOR = LINE_TERMINATOR;
        this.CHARSET = CHARSET;
        this.COMPRESSION = COMPRESSION;
        this.DECODED_OFFSET = DECODED_OFFSET;
        this.DECODER_STATE = DECODER_STATE;
        this.DECODER_STATE_FORMAT = DECODER_STATE_FORMAT;
        this.DECODER_STATE_VERSION = DECODER_STATE_VERSION;
        this.DECODER_STATE_BYTE_LENGTH = DECODER_STATE_BYTE_LENGTH;
        this.DECODER_STATE_SHA256 = DECODER_STATE_SHA256;
        this.DECODER_BUILD_ID = DECODER_BUILD_ID;
        this.DECODER_STATE_MEDIA_TYPE = DECODER_STATE_MEDIA_TYPE;
        this.WINDOW = WINDOW;
        this.WINDOW_BYTE_LENGTH = WINDOW_BYTE_LENGTH;
        this.WINDOW_POSITION = WINDOW_POSITION;
        this.PARTIAL_RECORD = PARTIAL_RECORD;
    }
    pack(builder) {
        const FORMAT = (this.FORMAT !== null ? builder.createString(this.FORMAT) : 0);
        const HEADER_LINE = (this.HEADER_LINE !== null ? builder.createString(this.HEADER_LINE) : 0);
        const COLUMN_NAMES = IRMDecodeContext.createColumnNamesVector(builder, builder.createObjectOffsetList(this.COLUMN_NAMES));
        const DELIMITER = (this.DELIMITER !== null ? builder.createString(this.DELIMITER) : 0);
        const QUOTE = (this.QUOTE !== null ? builder.createString(this.QUOTE) : 0);
        const ESCAPE = (this.ESCAPE !== null ? builder.createString(this.ESCAPE) : 0);
        const LINE_TERMINATOR = (this.LINE_TERMINATOR !== null ? builder.createString(this.LINE_TERMINATOR) : 0);
        const CHARSET = (this.CHARSET !== null ? builder.createString(this.CHARSET) : 0);
        const COMPRESSION = (this.COMPRESSION !== null ? builder.createString(this.COMPRESSION) : 0);
        const DECODER_STATE = IRMDecodeContext.createDecoderStateVector(builder, this.DECODER_STATE);
        const DECODER_STATE_FORMAT = (this.DECODER_STATE_FORMAT !== null ? builder.createString(this.DECODER_STATE_FORMAT) : 0);
        const DECODER_STATE_VERSION = (this.DECODER_STATE_VERSION !== null ? builder.createString(this.DECODER_STATE_VERSION) : 0);
        const DECODER_STATE_SHA256 = (this.DECODER_STATE_SHA256 !== null ? builder.createString(this.DECODER_STATE_SHA256) : 0);
        const DECODER_BUILD_ID = (this.DECODER_BUILD_ID !== null ? builder.createString(this.DECODER_BUILD_ID) : 0);
        const DECODER_STATE_MEDIA_TYPE = (this.DECODER_STATE_MEDIA_TYPE !== null ? builder.createString(this.DECODER_STATE_MEDIA_TYPE) : 0);
        const WINDOW = IRMDecodeContext.createWindowVector(builder, this.WINDOW);
        const PARTIAL_RECORD = IRMDecodeContext.createPartialRecordVector(builder, this.PARTIAL_RECORD);
        return IRMDecodeContext.createIRMDecodeContext(builder, FORMAT, HEADER_LINE, this.HEADER_BYTE_LENGTH, COLUMN_NAMES, DELIMITER, QUOTE, ESCAPE, LINE_TERMINATOR, CHARSET, COMPRESSION, this.DECODED_OFFSET, DECODER_STATE, DECODER_STATE_FORMAT, DECODER_STATE_VERSION, this.DECODER_STATE_BYTE_LENGTH, DECODER_STATE_SHA256, DECODER_BUILD_ID, DECODER_STATE_MEDIA_TYPE, WINDOW, this.WINDOW_BYTE_LENGTH, this.WINDOW_POSITION, PARTIAL_RECORD);
    }
}
