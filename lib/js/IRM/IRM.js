// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { IRMChunk } from './IRMChunk.js';
import { IRMDecodeContext } from './IRMDecodeContext.js';
import { IRMFault } from './IRMFault.js';
import { IRMSource } from './IRMSource.js';
import { irmJobState } from './irmJobState.js';
import { irmRangeMode } from './irmRangeMode.js';
/**
 * Ingest Resume Mark
 */
export class IRM {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsIRM(bb, obj) {
        return (obj || new IRM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsIRM(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new IRM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$IRM');
    }
    JOB_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    /**
     * Monotonic version of this mark within JOB_ID. Highest wins per publishing
     * key. Never reused, never decremented.
     */
    SEQUENCE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    PROVIDER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    INGESTOR_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * The source object and its validators.
     */
    SOURCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new IRMSource()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Lifecycle of the job.
     */
    STATE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : irmJobState.UNSPECIFIED;
    }
    /**
     * How byte positions in this mark are addressed.
     */
    RANGE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : irmRangeMode.UNSPECIFIED;
    }
    /**
     * Byte offset at which the next read begins, in the SOURCE's own byte stream:
     * the compressed stream when DECODE_CONTEXT.COMPRESSION is non-empty, in
     * which case it is a record boundary only in company with the decoder state,
     * window and partial record. On an uncompressed source it is ALWAYS a
     * complete-record boundary, corrected backwards from the previous chunk's
     * byte budget. 0 is a valid value and means the job has committed nothing yet.
     */
    NEXT_OFFSET() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Zero-based index of the next chunk to read. With PART_INDEX addressing this
     * is the only position.
     */
    NEXT_CHUNK_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Byte budget the publisher reads per chunk. Records the ceiling the job
     * actually ran under, which a host may have clamped below the configured one.
     */
    CHUNK_BYTE_BUDGET() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Everything needed to decode the chunk at NEXT_OFFSET. REQUIRED in practice
     * whenever NEXT_OFFSET is past the source's own header; a consumer that finds
     * it absent there restarts from offset 0 rather than decoding blind.
     */
    DECODE_CONTEXT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? (obj || new IRMDecodeContext()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * The chunk this mark was advanced from — the last one durably committed.
     */
    LAST_CHUNK(obj) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? (obj || new IRMChunk()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Chunks durably committed by this job so far.
     */
    CHUNKS_COMMITTED() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Source bytes accounted for by committed chunks.
     */
    BYTES_COMMITTED() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Records durably committed by this job so far, across every chunk.
     */
    RECORDS_COMMITTED() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    TARGET_STANDARD(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RECONCILE_MODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MERGE_POLICY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * The last unrecovered error, when there is one.
     */
    FAULT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 40);
        return offset ? (obj || new IRMFault()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    CREATED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 42);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    STARTED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 44);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    UPDATED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    COMPLETED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 48);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EXPIRES_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 50);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TAGS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 52);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    tagsLength() {
        const offset = this.bb.__offset(this.bb_pos, 52);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    NOTES(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 54);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startIRM(builder) {
        builder.startObject(26);
    }
    static addJobId(builder, JOB_IDOffset) {
        builder.addFieldOffset(0, JOB_IDOffset, 0);
    }
    static addSequence(builder, SEQUENCE) {
        builder.addFieldInt64(1, SEQUENCE, BigInt('0'));
    }
    static addProviderId(builder, PROVIDER_IDOffset) {
        builder.addFieldOffset(2, PROVIDER_IDOffset, 0);
    }
    static addIngestorId(builder, INGESTOR_IDOffset) {
        builder.addFieldOffset(3, INGESTOR_IDOffset, 0);
    }
    static addSource(builder, SOURCEOffset) {
        builder.addFieldOffset(4, SOURCEOffset, 0);
    }
    static addState(builder, STATE) {
        builder.addFieldInt8(5, STATE, irmJobState.UNSPECIFIED);
    }
    static addRangeMode(builder, RANGE_MODE) {
        builder.addFieldInt8(6, RANGE_MODE, irmRangeMode.UNSPECIFIED);
    }
    static addNextOffset(builder, NEXT_OFFSET) {
        builder.addFieldInt64(7, NEXT_OFFSET, BigInt('0'));
    }
    static addNextChunkIndex(builder, NEXT_CHUNK_INDEX) {
        builder.addFieldInt32(8, NEXT_CHUNK_INDEX, 0);
    }
    static addChunkByteBudget(builder, CHUNK_BYTE_BUDGET) {
        builder.addFieldInt64(9, CHUNK_BYTE_BUDGET, BigInt('0'));
    }
    static addDecodeContext(builder, DECODE_CONTEXTOffset) {
        builder.addFieldOffset(10, DECODE_CONTEXTOffset, 0);
    }
    static addLastChunk(builder, LAST_CHUNKOffset) {
        builder.addFieldOffset(11, LAST_CHUNKOffset, 0);
    }
    static addChunksCommitted(builder, CHUNKS_COMMITTED) {
        builder.addFieldInt32(12, CHUNKS_COMMITTED, 0);
    }
    static addBytesCommitted(builder, BYTES_COMMITTED) {
        builder.addFieldInt64(13, BYTES_COMMITTED, BigInt('0'));
    }
    static addRecordsCommitted(builder, RECORDS_COMMITTED) {
        builder.addFieldInt64(14, RECORDS_COMMITTED, BigInt('0'));
    }
    static addTargetStandard(builder, TARGET_STANDARDOffset) {
        builder.addFieldOffset(15, TARGET_STANDARDOffset, 0);
    }
    static addReconcileMode(builder, RECONCILE_MODEOffset) {
        builder.addFieldOffset(16, RECONCILE_MODEOffset, 0);
    }
    static addMergePolicy(builder, MERGE_POLICYOffset) {
        builder.addFieldOffset(17, MERGE_POLICYOffset, 0);
    }
    static addFault(builder, FAULTOffset) {
        builder.addFieldOffset(18, FAULTOffset, 0);
    }
    static addCreatedAt(builder, CREATED_ATOffset) {
        builder.addFieldOffset(19, CREATED_ATOffset, 0);
    }
    static addStartedAt(builder, STARTED_ATOffset) {
        builder.addFieldOffset(20, STARTED_ATOffset, 0);
    }
    static addUpdatedAt(builder, UPDATED_ATOffset) {
        builder.addFieldOffset(21, UPDATED_ATOffset, 0);
    }
    static addCompletedAt(builder, COMPLETED_ATOffset) {
        builder.addFieldOffset(22, COMPLETED_ATOffset, 0);
    }
    static addExpiresAt(builder, EXPIRES_ATOffset) {
        builder.addFieldOffset(23, EXPIRES_ATOffset, 0);
    }
    static addTags(builder, TAGSOffset) {
        builder.addFieldOffset(24, TAGSOffset, 0);
    }
    static createTagsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTagsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addNotes(builder, NOTESOffset) {
        builder.addFieldOffset(25, NOTESOffset, 0);
    }
    static endIRM(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // JOB_ID
        builder.requiredField(offset, 8); // PROVIDER_ID
        builder.requiredField(offset, 12); // SOURCE
        builder.requiredField(offset, 46); // UPDATED_AT
        return offset;
    }
    static finishIRMBuffer(builder, offset) {
        builder.finish(offset, '$IRM');
    }
    static finishSizePrefixedIRMBuffer(builder, offset) {
        builder.finish(offset, '$IRM', true);
    }
    unpack() {
        return new IRMT(this.JOB_ID(), this.SEQUENCE(), this.PROVIDER_ID(), this.INGESTOR_ID(), (this.SOURCE() !== null ? this.SOURCE().unpack() : null), this.STATE(), this.RANGE_MODE(), this.NEXT_OFFSET(), this.NEXT_CHUNK_INDEX(), this.CHUNK_BYTE_BUDGET(), (this.DECODE_CONTEXT() !== null ? this.DECODE_CONTEXT().unpack() : null), (this.LAST_CHUNK() !== null ? this.LAST_CHUNK().unpack() : null), this.CHUNKS_COMMITTED(), this.BYTES_COMMITTED(), this.RECORDS_COMMITTED(), this.TARGET_STANDARD(), this.RECONCILE_MODE(), this.MERGE_POLICY(), (this.FAULT() !== null ? this.FAULT().unpack() : null), this.CREATED_AT(), this.STARTED_AT(), this.UPDATED_AT(), this.COMPLETED_AT(), this.EXPIRES_AT(), this.bb.createScalarList(this.TAGS.bind(this), this.tagsLength()), this.NOTES());
    }
    unpackTo(_o) {
        _o.JOB_ID = this.JOB_ID();
        _o.SEQUENCE = this.SEQUENCE();
        _o.PROVIDER_ID = this.PROVIDER_ID();
        _o.INGESTOR_ID = this.INGESTOR_ID();
        _o.SOURCE = (this.SOURCE() !== null ? this.SOURCE().unpack() : null);
        _o.STATE = this.STATE();
        _o.RANGE_MODE = this.RANGE_MODE();
        _o.NEXT_OFFSET = this.NEXT_OFFSET();
        _o.NEXT_CHUNK_INDEX = this.NEXT_CHUNK_INDEX();
        _o.CHUNK_BYTE_BUDGET = this.CHUNK_BYTE_BUDGET();
        _o.DECODE_CONTEXT = (this.DECODE_CONTEXT() !== null ? this.DECODE_CONTEXT().unpack() : null);
        _o.LAST_CHUNK = (this.LAST_CHUNK() !== null ? this.LAST_CHUNK().unpack() : null);
        _o.CHUNKS_COMMITTED = this.CHUNKS_COMMITTED();
        _o.BYTES_COMMITTED = this.BYTES_COMMITTED();
        _o.RECORDS_COMMITTED = this.RECORDS_COMMITTED();
        _o.TARGET_STANDARD = this.TARGET_STANDARD();
        _o.RECONCILE_MODE = this.RECONCILE_MODE();
        _o.MERGE_POLICY = this.MERGE_POLICY();
        _o.FAULT = (this.FAULT() !== null ? this.FAULT().unpack() : null);
        _o.CREATED_AT = this.CREATED_AT();
        _o.STARTED_AT = this.STARTED_AT();
        _o.UPDATED_AT = this.UPDATED_AT();
        _o.COMPLETED_AT = this.COMPLETED_AT();
        _o.EXPIRES_AT = this.EXPIRES_AT();
        _o.TAGS = this.bb.createScalarList(this.TAGS.bind(this), this.tagsLength());
        _o.NOTES = this.NOTES();
    }
}
export class IRMT {
    constructor(JOB_ID = null, SEQUENCE = BigInt('0'), PROVIDER_ID = null, INGESTOR_ID = null, SOURCE = null, STATE = irmJobState.UNSPECIFIED, RANGE_MODE = irmRangeMode.UNSPECIFIED, NEXT_OFFSET = BigInt('0'), NEXT_CHUNK_INDEX = 0, CHUNK_BYTE_BUDGET = BigInt('0'), DECODE_CONTEXT = null, LAST_CHUNK = null, CHUNKS_COMMITTED = 0, BYTES_COMMITTED = BigInt('0'), RECORDS_COMMITTED = BigInt('0'), TARGET_STANDARD = null, RECONCILE_MODE = null, MERGE_POLICY = null, FAULT = null, CREATED_AT = null, STARTED_AT = null, UPDATED_AT = null, COMPLETED_AT = null, EXPIRES_AT = null, TAGS = [], NOTES = null) {
        this.JOB_ID = JOB_ID;
        this.SEQUENCE = SEQUENCE;
        this.PROVIDER_ID = PROVIDER_ID;
        this.INGESTOR_ID = INGESTOR_ID;
        this.SOURCE = SOURCE;
        this.STATE = STATE;
        this.RANGE_MODE = RANGE_MODE;
        this.NEXT_OFFSET = NEXT_OFFSET;
        this.NEXT_CHUNK_INDEX = NEXT_CHUNK_INDEX;
        this.CHUNK_BYTE_BUDGET = CHUNK_BYTE_BUDGET;
        this.DECODE_CONTEXT = DECODE_CONTEXT;
        this.LAST_CHUNK = LAST_CHUNK;
        this.CHUNKS_COMMITTED = CHUNKS_COMMITTED;
        this.BYTES_COMMITTED = BYTES_COMMITTED;
        this.RECORDS_COMMITTED = RECORDS_COMMITTED;
        this.TARGET_STANDARD = TARGET_STANDARD;
        this.RECONCILE_MODE = RECONCILE_MODE;
        this.MERGE_POLICY = MERGE_POLICY;
        this.FAULT = FAULT;
        this.CREATED_AT = CREATED_AT;
        this.STARTED_AT = STARTED_AT;
        this.UPDATED_AT = UPDATED_AT;
        this.COMPLETED_AT = COMPLETED_AT;
        this.EXPIRES_AT = EXPIRES_AT;
        this.TAGS = TAGS;
        this.NOTES = NOTES;
    }
    pack(builder) {
        const JOB_ID = (this.JOB_ID !== null ? builder.createString(this.JOB_ID) : 0);
        const PROVIDER_ID = (this.PROVIDER_ID !== null ? builder.createString(this.PROVIDER_ID) : 0);
        const INGESTOR_ID = (this.INGESTOR_ID !== null ? builder.createString(this.INGESTOR_ID) : 0);
        const SOURCE = (this.SOURCE !== null ? this.SOURCE.pack(builder) : 0);
        const DECODE_CONTEXT = (this.DECODE_CONTEXT !== null ? this.DECODE_CONTEXT.pack(builder) : 0);
        const LAST_CHUNK = (this.LAST_CHUNK !== null ? this.LAST_CHUNK.pack(builder) : 0);
        const TARGET_STANDARD = (this.TARGET_STANDARD !== null ? builder.createString(this.TARGET_STANDARD) : 0);
        const RECONCILE_MODE = (this.RECONCILE_MODE !== null ? builder.createString(this.RECONCILE_MODE) : 0);
        const MERGE_POLICY = (this.MERGE_POLICY !== null ? builder.createString(this.MERGE_POLICY) : 0);
        const FAULT = (this.FAULT !== null ? this.FAULT.pack(builder) : 0);
        const CREATED_AT = (this.CREATED_AT !== null ? builder.createString(this.CREATED_AT) : 0);
        const STARTED_AT = (this.STARTED_AT !== null ? builder.createString(this.STARTED_AT) : 0);
        const UPDATED_AT = (this.UPDATED_AT !== null ? builder.createString(this.UPDATED_AT) : 0);
        const COMPLETED_AT = (this.COMPLETED_AT !== null ? builder.createString(this.COMPLETED_AT) : 0);
        const EXPIRES_AT = (this.EXPIRES_AT !== null ? builder.createString(this.EXPIRES_AT) : 0);
        const TAGS = IRM.createTagsVector(builder, builder.createObjectOffsetList(this.TAGS));
        const NOTES = (this.NOTES !== null ? builder.createString(this.NOTES) : 0);
        IRM.startIRM(builder);
        IRM.addJobId(builder, JOB_ID);
        IRM.addSequence(builder, this.SEQUENCE);
        IRM.addProviderId(builder, PROVIDER_ID);
        IRM.addIngestorId(builder, INGESTOR_ID);
        IRM.addSource(builder, SOURCE);
        IRM.addState(builder, this.STATE);
        IRM.addRangeMode(builder, this.RANGE_MODE);
        IRM.addNextOffset(builder, this.NEXT_OFFSET);
        IRM.addNextChunkIndex(builder, this.NEXT_CHUNK_INDEX);
        IRM.addChunkByteBudget(builder, this.CHUNK_BYTE_BUDGET);
        IRM.addDecodeContext(builder, DECODE_CONTEXT);
        IRM.addLastChunk(builder, LAST_CHUNK);
        IRM.addChunksCommitted(builder, this.CHUNKS_COMMITTED);
        IRM.addBytesCommitted(builder, this.BYTES_COMMITTED);
        IRM.addRecordsCommitted(builder, this.RECORDS_COMMITTED);
        IRM.addTargetStandard(builder, TARGET_STANDARD);
        IRM.addReconcileMode(builder, RECONCILE_MODE);
        IRM.addMergePolicy(builder, MERGE_POLICY);
        IRM.addFault(builder, FAULT);
        IRM.addCreatedAt(builder, CREATED_AT);
        IRM.addStartedAt(builder, STARTED_AT);
        IRM.addUpdatedAt(builder, UPDATED_AT);
        IRM.addCompletedAt(builder, COMPLETED_AT);
        IRM.addExpiresAt(builder, EXPIRES_AT);
        IRM.addTags(builder, TAGS);
        IRM.addNotes(builder, NOTES);
        return IRM.endIRM(builder);
    }
}
