// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * The last unrecovered error, carried so a resumed run can refuse to loop.
 */
export class IRMFault {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsIRMFault(bb, obj) {
        return (obj || new IRMFault()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsIRMFault(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new IRMFault()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    CODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Zero-based index of the chunk the fault occurred on.
     */
    CHUNK_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Consecutive failed attempts at that chunk.
     */
    CONSECUTIVE_FAILURES() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    OCCURRED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RETRY_NOT_BEFORE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startIRMFault(builder) {
        builder.startObject(6);
    }
    static addCode(builder, CODEOffset) {
        builder.addFieldOffset(0, CODEOffset, 0);
    }
    static addMessage(builder, MESSAGEOffset) {
        builder.addFieldOffset(1, MESSAGEOffset, 0);
    }
    static addChunkIndex(builder, CHUNK_INDEX) {
        builder.addFieldInt32(2, CHUNK_INDEX, 0);
    }
    static addConsecutiveFailures(builder, CONSECUTIVE_FAILURES) {
        builder.addFieldInt32(3, CONSECUTIVE_FAILURES, 0);
    }
    static addOccurredAt(builder, OCCURRED_ATOffset) {
        builder.addFieldOffset(4, OCCURRED_ATOffset, 0);
    }
    static addRetryNotBefore(builder, RETRY_NOT_BEFOREOffset) {
        builder.addFieldOffset(5, RETRY_NOT_BEFOREOffset, 0);
    }
    static endIRMFault(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // CODE
        return offset;
    }
    static createIRMFault(builder, CODEOffset, MESSAGEOffset, CHUNK_INDEX, CONSECUTIVE_FAILURES, OCCURRED_ATOffset, RETRY_NOT_BEFOREOffset) {
        IRMFault.startIRMFault(builder);
        IRMFault.addCode(builder, CODEOffset);
        IRMFault.addMessage(builder, MESSAGEOffset);
        IRMFault.addChunkIndex(builder, CHUNK_INDEX);
        IRMFault.addConsecutiveFailures(builder, CONSECUTIVE_FAILURES);
        IRMFault.addOccurredAt(builder, OCCURRED_ATOffset);
        IRMFault.addRetryNotBefore(builder, RETRY_NOT_BEFOREOffset);
        return IRMFault.endIRMFault(builder);
    }
    unpack() {
        return new IRMFaultT(this.CODE(), this.MESSAGE(), this.CHUNK_INDEX(), this.CONSECUTIVE_FAILURES(), this.OCCURRED_AT(), this.RETRY_NOT_BEFORE());
    }
    unpackTo(_o) {
        _o.CODE = this.CODE();
        _o.MESSAGE = this.MESSAGE();
        _o.CHUNK_INDEX = this.CHUNK_INDEX();
        _o.CONSECUTIVE_FAILURES = this.CONSECUTIVE_FAILURES();
        _o.OCCURRED_AT = this.OCCURRED_AT();
        _o.RETRY_NOT_BEFORE = this.RETRY_NOT_BEFORE();
    }
}
export class IRMFaultT {
    constructor(CODE = null, MESSAGE = null, CHUNK_INDEX = 0, CONSECUTIVE_FAILURES = 0, OCCURRED_AT = null, RETRY_NOT_BEFORE = null) {
        this.CODE = CODE;
        this.MESSAGE = MESSAGE;
        this.CHUNK_INDEX = CHUNK_INDEX;
        this.CONSECUTIVE_FAILURES = CONSECUTIVE_FAILURES;
        this.OCCURRED_AT = OCCURRED_AT;
        this.RETRY_NOT_BEFORE = RETRY_NOT_BEFORE;
    }
    pack(builder) {
        const CODE = (this.CODE !== null ? builder.createString(this.CODE) : 0);
        const MESSAGE = (this.MESSAGE !== null ? builder.createString(this.MESSAGE) : 0);
        const OCCURRED_AT = (this.OCCURRED_AT !== null ? builder.createString(this.OCCURRED_AT) : 0);
        const RETRY_NOT_BEFORE = (this.RETRY_NOT_BEFORE !== null ? builder.createString(this.RETRY_NOT_BEFORE) : 0);
        return IRMFault.createIRMFault(builder, CODE, MESSAGE, this.CHUNK_INDEX, this.CONSECUTIVE_FAILURES, OCCURRED_AT, RETRY_NOT_BEFORE);
    }
}
