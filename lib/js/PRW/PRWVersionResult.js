// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Runtime diagnostic version; PLG remains the authoritative manifest identity.
 */
export class PRWVersionResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWVersionResult(bb, obj) {
        return (obj || new PRWVersionResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWVersionResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWVersionResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    MODULE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    static startPRWVersionResult(builder) {
        builder.startObject(2);
    }
    static addVersion(builder, VERSIONOffset) {
        builder.addFieldOffset(0, VERSIONOffset, 0);
    }
    static addModuleId(builder, MODULE_IDOffset) {
        builder.addFieldOffset(1, MODULE_IDOffset, 0);
    }
    static endPRWVersionResult(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // VERSION
        builder.requiredField(offset, 6); // MODULE_ID
        return offset;
    }
    static createPRWVersionResult(builder, VERSIONOffset, MODULE_IDOffset) {
        PRWVersionResult.startPRWVersionResult(builder);
        PRWVersionResult.addVersion(builder, VERSIONOffset);
        PRWVersionResult.addModuleId(builder, MODULE_IDOffset);
        return PRWVersionResult.endPRWVersionResult(builder);
    }
    unpack() {
        return new PRWVersionResultT(this.VERSION(), this.MODULE_ID());
    }
    unpackTo(_o) {
        _o.VERSION = this.VERSION();
        _o.MODULE_ID = this.MODULE_ID();
    }
}
export class PRWVersionResultT {
    constructor(VERSION = null, MODULE_ID = null) {
        this.VERSION = VERSION;
        this.MODULE_ID = MODULE_ID;
    }
    pack(builder) {
        const VERSION = (this.VERSION !== null ? builder.createString(this.VERSION) : 0);
        const MODULE_ID = (this.MODULE_ID !== null ? builder.createString(this.MODULE_ID) : 0);
        return PRWVersionResult.createPRWVersionResult(builder, VERSION, MODULE_ID);
    }
}
