// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Piecewise-linear throttle. Clock is seconds from initial propagation epoch.
 */
export class PRWThrottlePoint {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWThrottlePoint(bb, obj) {
        return (obj || new PRWThrottlePoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWThrottlePoint(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWThrottlePoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    ELAPSED_SECONDS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    FRACTION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startPRWThrottlePoint(builder) {
        builder.startObject(2);
    }
    static addElapsedSeconds(builder, ELAPSED_SECONDS) {
        builder.addFieldFloat64(0, ELAPSED_SECONDS, 0.0);
    }
    static addFraction(builder, FRACTION) {
        builder.addFieldFloat64(1, FRACTION, 0.0);
    }
    static endPRWThrottlePoint(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createPRWThrottlePoint(builder, ELAPSED_SECONDS, FRACTION) {
        PRWThrottlePoint.startPRWThrottlePoint(builder);
        PRWThrottlePoint.addElapsedSeconds(builder, ELAPSED_SECONDS);
        PRWThrottlePoint.addFraction(builder, FRACTION);
        return PRWThrottlePoint.endPRWThrottlePoint(builder);
    }
    unpack() {
        return new PRWThrottlePointT(this.ELAPSED_SECONDS(), this.FRACTION());
    }
    unpackTo(_o) {
        _o.ELAPSED_SECONDS = this.ELAPSED_SECONDS();
        _o.FRACTION = this.FRACTION();
    }
}
export class PRWThrottlePointT {
    constructor(ELAPSED_SECONDS = 0.0, FRACTION = 0.0) {
        this.ELAPSED_SECONDS = ELAPSED_SECONDS;
        this.FRACTION = FRACTION;
    }
    pack(builder) {
        return PRWThrottlePoint.createPRWThrottlePoint(builder, this.ELAPSED_SECONDS, this.FRACTION);
    }
}
