// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Row-major square matrix on [x,y,z,vx,vy,vz,(mass)], SI state units.
 * Covariance entries have row*column units; STM entries row/column units.
 */
export class PRWStateMatrix {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWStateMatrix(bb, obj) {
        return (obj || new PRWStateMatrix()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWStateMatrix(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWStateMatrix()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    DIMENSION() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    VALUES(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    valuesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    valuesArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    static startPRWStateMatrix(builder) {
        builder.startObject(2);
    }
    static addDimension(builder, DIMENSION) {
        builder.addFieldInt8(0, DIMENSION, 0);
    }
    static addValues(builder, VALUESOffset) {
        builder.addFieldOffset(1, VALUESOffset, 0);
    }
    static createValuesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startValuesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endPRWStateMatrix(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // VALUES
        return offset;
    }
    static createPRWStateMatrix(builder, DIMENSION, VALUESOffset) {
        PRWStateMatrix.startPRWStateMatrix(builder);
        PRWStateMatrix.addDimension(builder, DIMENSION);
        PRWStateMatrix.addValues(builder, VALUESOffset);
        return PRWStateMatrix.endPRWStateMatrix(builder);
    }
    unpack() {
        return new PRWStateMatrixT(this.DIMENSION(), this.bb.createScalarList(this.VALUES.bind(this), this.valuesLength()));
    }
    unpackTo(_o) {
        _o.DIMENSION = this.DIMENSION();
        _o.VALUES = this.bb.createScalarList(this.VALUES.bind(this), this.valuesLength());
    }
}
export class PRWStateMatrixT {
    constructor(DIMENSION = 0, VALUES = []) {
        this.DIMENSION = DIMENSION;
        this.VALUES = VALUES;
    }
    pack(builder) {
        const VALUES = PRWStateMatrix.createValuesVector(builder, this.VALUES);
        return PRWStateMatrix.createPRWStateMatrix(builder, this.DIMENSION, VALUES);
    }
}
