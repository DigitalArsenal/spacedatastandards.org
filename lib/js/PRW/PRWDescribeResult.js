// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PRWInstance } from './PRWInstance.js';
import { PRWTrajectorySource } from './PRWTrajectorySource.js';
export class PRWDescribeResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWDescribeResult(bb, obj) {
        return (obj || new PRWDescribeResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWDescribeResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWDescribeResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    INSTANCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new PRWInstance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    SEGMENT_SET_HANDLE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    SOURCES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new PRWTrajectorySource()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    sourcesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Chunking indexes SOURCES; no partial polynomial coefficient vector.
     */
    SOURCE_OFFSET() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    FINAL_CHUNK() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : true;
    }
    static startPRWDescribeResult(builder) {
        builder.startObject(5);
    }
    static addInstance(builder, INSTANCEOffset) {
        builder.addFieldOffset(0, INSTANCEOffset, 0);
    }
    static addSegmentSetHandle(builder, SEGMENT_SET_HANDLE) {
        builder.addFieldInt32(1, SEGMENT_SET_HANDLE, 0);
    }
    static addSources(builder, SOURCESOffset) {
        builder.addFieldOffset(2, SOURCESOffset, 0);
    }
    static createSourcesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSourcesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSourceOffset(builder, SOURCE_OFFSET) {
        builder.addFieldInt64(3, SOURCE_OFFSET, BigInt('0'));
    }
    static addFinalChunk(builder, FINAL_CHUNK) {
        builder.addFieldInt8(4, +FINAL_CHUNK, +true);
    }
    static endPRWDescribeResult(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // INSTANCE
        return offset;
    }
    static createPRWDescribeResult(builder, INSTANCEOffset, SEGMENT_SET_HANDLE, SOURCESOffset, SOURCE_OFFSET, FINAL_CHUNK) {
        PRWDescribeResult.startPRWDescribeResult(builder);
        PRWDescribeResult.addInstance(builder, INSTANCEOffset);
        PRWDescribeResult.addSegmentSetHandle(builder, SEGMENT_SET_HANDLE);
        PRWDescribeResult.addSources(builder, SOURCESOffset);
        PRWDescribeResult.addSourceOffset(builder, SOURCE_OFFSET);
        PRWDescribeResult.addFinalChunk(builder, FINAL_CHUNK);
        return PRWDescribeResult.endPRWDescribeResult(builder);
    }
    unpack() {
        return new PRWDescribeResultT((this.INSTANCE() !== null ? this.INSTANCE().unpack() : null), this.SEGMENT_SET_HANDLE(), this.bb.createObjList(this.SOURCES.bind(this), this.sourcesLength()), this.SOURCE_OFFSET(), this.FINAL_CHUNK());
    }
    unpackTo(_o) {
        _o.INSTANCE = (this.INSTANCE() !== null ? this.INSTANCE().unpack() : null);
        _o.SEGMENT_SET_HANDLE = this.SEGMENT_SET_HANDLE();
        _o.SOURCES = this.bb.createObjList(this.SOURCES.bind(this), this.sourcesLength());
        _o.SOURCE_OFFSET = this.SOURCE_OFFSET();
        _o.FINAL_CHUNK = this.FINAL_CHUNK();
    }
}
export class PRWDescribeResultT {
    constructor(INSTANCE = null, SEGMENT_SET_HANDLE = 0, SOURCES = [], SOURCE_OFFSET = BigInt('0'), FINAL_CHUNK = true) {
        this.INSTANCE = INSTANCE;
        this.SEGMENT_SET_HANDLE = SEGMENT_SET_HANDLE;
        this.SOURCES = SOURCES;
        this.SOURCE_OFFSET = SOURCE_OFFSET;
        this.FINAL_CHUNK = FINAL_CHUNK;
    }
    pack(builder) {
        const INSTANCE = (this.INSTANCE !== null ? this.INSTANCE.pack(builder) : 0);
        const SOURCES = PRWDescribeResult.createSourcesVector(builder, builder.createObjectOffsetList(this.SOURCES));
        return PRWDescribeResult.createPRWDescribeResult(builder, INSTANCE, this.SEGMENT_SET_HANDLE, SOURCES, this.SOURCE_OFFSET, this.FINAL_CHUNK);
    }
}
