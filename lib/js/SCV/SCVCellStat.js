// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class SCVCellStat {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCVCellStat(bb, obj) {
        return (obj || new SCVCellStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCVCellStat(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCVCellStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    CELL_ID() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    LATITUDE_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    LONGITUDE_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    ALTITUDE_M() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    COVERED_WINDOW_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TOTAL_WINDOW_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    COVERAGE_FRACTION() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MEAN_REVISIT_SEC() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_GAP_SEC() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MEAN_GAP_SEC() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    REDUNDANCY() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    SENSOR_IDS(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    sensorIdsLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sensorIdsArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Uint32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    SENSOR_BITSET_WORDS(index) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint64(this.bb.__vector(this.bb_pos + offset) + index * 8) : BigInt(0);
    }
    sensorBitsetWordsLength() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startSCVCellStat(builder) {
        builder.startObject(13);
    }
    static addCellId(builder, CELL_ID) {
        builder.addFieldInt32(0, CELL_ID, 0);
    }
    static addLatitudeDeg(builder, LATITUDE_DEG) {
        builder.addFieldFloat64(1, LATITUDE_DEG, 0.0);
    }
    static addLongitudeDeg(builder, LONGITUDE_DEG) {
        builder.addFieldFloat64(2, LONGITUDE_DEG, 0.0);
    }
    static addAltitudeM(builder, ALTITUDE_M) {
        builder.addFieldFloat64(3, ALTITUDE_M, 0.0);
    }
    static addCoveredWindowCount(builder, COVERED_WINDOW_COUNT) {
        builder.addFieldInt32(4, COVERED_WINDOW_COUNT, 0);
    }
    static addTotalWindowCount(builder, TOTAL_WINDOW_COUNT) {
        builder.addFieldInt32(5, TOTAL_WINDOW_COUNT, 0);
    }
    static addCoverageFraction(builder, COVERAGE_FRACTION) {
        builder.addFieldFloat64(6, COVERAGE_FRACTION, 0.0);
    }
    static addMeanRevisitSec(builder, MEAN_REVISIT_SEC) {
        builder.addFieldFloat64(7, MEAN_REVISIT_SEC, 0.0);
    }
    static addMaxGapSec(builder, MAX_GAP_SEC) {
        builder.addFieldFloat64(8, MAX_GAP_SEC, 0.0);
    }
    static addMeanGapSec(builder, MEAN_GAP_SEC) {
        builder.addFieldFloat64(9, MEAN_GAP_SEC, 0.0);
    }
    static addRedundancy(builder, REDUNDANCY) {
        builder.addFieldFloat64(10, REDUNDANCY, 0.0);
    }
    static addSensorIds(builder, SENSOR_IDSOffset) {
        builder.addFieldOffset(11, SENSOR_IDSOffset, 0);
    }
    static createSensorIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startSensorIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSensorBitsetWords(builder, SENSOR_BITSET_WORDSOffset) {
        builder.addFieldOffset(12, SENSOR_BITSET_WORDSOffset, 0);
    }
    static createSensorBitsetWordsVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt64(data[i]);
        }
        return builder.endVector();
    }
    static startSensorBitsetWordsVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endSCVCellStat(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCVCellStat(builder, CELL_ID, LATITUDE_DEG, LONGITUDE_DEG, ALTITUDE_M, COVERED_WINDOW_COUNT, TOTAL_WINDOW_COUNT, COVERAGE_FRACTION, MEAN_REVISIT_SEC, MAX_GAP_SEC, MEAN_GAP_SEC, REDUNDANCY, SENSOR_IDSOffset, SENSOR_BITSET_WORDSOffset) {
        SCVCellStat.startSCVCellStat(builder);
        SCVCellStat.addCellId(builder, CELL_ID);
        SCVCellStat.addLatitudeDeg(builder, LATITUDE_DEG);
        SCVCellStat.addLongitudeDeg(builder, LONGITUDE_DEG);
        SCVCellStat.addAltitudeM(builder, ALTITUDE_M);
        SCVCellStat.addCoveredWindowCount(builder, COVERED_WINDOW_COUNT);
        SCVCellStat.addTotalWindowCount(builder, TOTAL_WINDOW_COUNT);
        SCVCellStat.addCoverageFraction(builder, COVERAGE_FRACTION);
        SCVCellStat.addMeanRevisitSec(builder, MEAN_REVISIT_SEC);
        SCVCellStat.addMaxGapSec(builder, MAX_GAP_SEC);
        SCVCellStat.addMeanGapSec(builder, MEAN_GAP_SEC);
        SCVCellStat.addRedundancy(builder, REDUNDANCY);
        SCVCellStat.addSensorIds(builder, SENSOR_IDSOffset);
        SCVCellStat.addSensorBitsetWords(builder, SENSOR_BITSET_WORDSOffset);
        return SCVCellStat.endSCVCellStat(builder);
    }
    unpack() {
        return new SCVCellStatT(this.CELL_ID(), this.LATITUDE_DEG(), this.LONGITUDE_DEG(), this.ALTITUDE_M(), this.COVERED_WINDOW_COUNT(), this.TOTAL_WINDOW_COUNT(), this.COVERAGE_FRACTION(), this.MEAN_REVISIT_SEC(), this.MAX_GAP_SEC(), this.MEAN_GAP_SEC(), this.REDUNDANCY(), this.bb.createScalarList(this.SENSOR_IDS.bind(this), this.sensorIdsLength()), this.bb.createScalarList(this.SENSOR_BITSET_WORDS.bind(this), this.sensorBitsetWordsLength()));
    }
    unpackTo(_o) {
        _o.CELL_ID = this.CELL_ID();
        _o.LATITUDE_DEG = this.LATITUDE_DEG();
        _o.LONGITUDE_DEG = this.LONGITUDE_DEG();
        _o.ALTITUDE_M = this.ALTITUDE_M();
        _o.COVERED_WINDOW_COUNT = this.COVERED_WINDOW_COUNT();
        _o.TOTAL_WINDOW_COUNT = this.TOTAL_WINDOW_COUNT();
        _o.COVERAGE_FRACTION = this.COVERAGE_FRACTION();
        _o.MEAN_REVISIT_SEC = this.MEAN_REVISIT_SEC();
        _o.MAX_GAP_SEC = this.MAX_GAP_SEC();
        _o.MEAN_GAP_SEC = this.MEAN_GAP_SEC();
        _o.REDUNDANCY = this.REDUNDANCY();
        _o.SENSOR_IDS = this.bb.createScalarList(this.SENSOR_IDS.bind(this), this.sensorIdsLength());
        _o.SENSOR_BITSET_WORDS = this.bb.createScalarList(this.SENSOR_BITSET_WORDS.bind(this), this.sensorBitsetWordsLength());
    }
}
export class SCVCellStatT {
    constructor(CELL_ID = 0, LATITUDE_DEG = 0.0, LONGITUDE_DEG = 0.0, ALTITUDE_M = 0.0, COVERED_WINDOW_COUNT = 0, TOTAL_WINDOW_COUNT = 0, COVERAGE_FRACTION = 0.0, MEAN_REVISIT_SEC = 0.0, MAX_GAP_SEC = 0.0, MEAN_GAP_SEC = 0.0, REDUNDANCY = 0.0, SENSOR_IDS = [], SENSOR_BITSET_WORDS = []) {
        this.CELL_ID = CELL_ID;
        this.LATITUDE_DEG = LATITUDE_DEG;
        this.LONGITUDE_DEG = LONGITUDE_DEG;
        this.ALTITUDE_M = ALTITUDE_M;
        this.COVERED_WINDOW_COUNT = COVERED_WINDOW_COUNT;
        this.TOTAL_WINDOW_COUNT = TOTAL_WINDOW_COUNT;
        this.COVERAGE_FRACTION = COVERAGE_FRACTION;
        this.MEAN_REVISIT_SEC = MEAN_REVISIT_SEC;
        this.MAX_GAP_SEC = MAX_GAP_SEC;
        this.MEAN_GAP_SEC = MEAN_GAP_SEC;
        this.REDUNDANCY = REDUNDANCY;
        this.SENSOR_IDS = SENSOR_IDS;
        this.SENSOR_BITSET_WORDS = SENSOR_BITSET_WORDS;
    }
    pack(builder) {
        const SENSOR_IDS = SCVCellStat.createSensorIdsVector(builder, this.SENSOR_IDS);
        const SENSOR_BITSET_WORDS = SCVCellStat.createSensorBitsetWordsVector(builder, this.SENSOR_BITSET_WORDS);
        return SCVCellStat.createSCVCellStat(builder, this.CELL_ID, this.LATITUDE_DEG, this.LONGITUDE_DEG, this.ALTITUDE_M, this.COVERED_WINDOW_COUNT, this.TOTAL_WINDOW_COUNT, this.COVERAGE_FRACTION, this.MEAN_REVISIT_SEC, this.MAX_GAP_SEC, this.MEAN_GAP_SEC, this.REDUNDANCY, SENSOR_IDS, SENSOR_BITSET_WORDS);
    }
}
