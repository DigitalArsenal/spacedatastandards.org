// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class SCVCancel {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCVCancel(bb, obj) {
        return (obj || new SCVCancel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCVCancel(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCVCancel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    JOB_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TRACE_ID() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    REASON(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CANCEL_AFTER_WINDOW_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    static startSCVCancel(builder) {
        builder.startObject(4);
    }
    static addJobId(builder, JOB_IDOffset) {
        builder.addFieldOffset(0, JOB_IDOffset, 0);
    }
    static addTraceId(builder, TRACE_ID) {
        builder.addFieldInt64(1, TRACE_ID, BigInt('0'));
    }
    static addReason(builder, REASONOffset) {
        builder.addFieldOffset(2, REASONOffset, 0);
    }
    static addCancelAfterWindowIndex(builder, CANCEL_AFTER_WINDOW_INDEX) {
        builder.addFieldInt32(3, CANCEL_AFTER_WINDOW_INDEX, 0);
    }
    static endSCVCancel(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCVCancel(builder, JOB_IDOffset, TRACE_ID, REASONOffset, CANCEL_AFTER_WINDOW_INDEX) {
        SCVCancel.startSCVCancel(builder);
        SCVCancel.addJobId(builder, JOB_IDOffset);
        SCVCancel.addTraceId(builder, TRACE_ID);
        SCVCancel.addReason(builder, REASONOffset);
        SCVCancel.addCancelAfterWindowIndex(builder, CANCEL_AFTER_WINDOW_INDEX);
        return SCVCancel.endSCVCancel(builder);
    }
    unpack() {
        return new SCVCancelT(this.JOB_ID(), this.TRACE_ID(), this.REASON(), this.CANCEL_AFTER_WINDOW_INDEX());
    }
    unpackTo(_o) {
        _o.JOB_ID = this.JOB_ID();
        _o.TRACE_ID = this.TRACE_ID();
        _o.REASON = this.REASON();
        _o.CANCEL_AFTER_WINDOW_INDEX = this.CANCEL_AFTER_WINDOW_INDEX();
    }
}
export class SCVCancelT {
    constructor(JOB_ID = null, TRACE_ID = BigInt('0'), REASON = null, CANCEL_AFTER_WINDOW_INDEX = 0) {
        this.JOB_ID = JOB_ID;
        this.TRACE_ID = TRACE_ID;
        this.REASON = REASON;
        this.CANCEL_AFTER_WINDOW_INDEX = CANCEL_AFTER_WINDOW_INDEX;
    }
    pack(builder) {
        const JOB_ID = (this.JOB_ID !== null ? builder.createString(this.JOB_ID) : 0);
        const REASON = (this.REASON !== null ? builder.createString(this.REASON) : 0);
        return SCVCancel.createSCVCancel(builder, JOB_ID, this.TRACE_ID, REASON, this.CANCEL_AFTER_WINDOW_INDEX);
    }
}
