// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class SCVLatitudeBandStat {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCVLatitudeBandStat(bb, obj) {
        return (obj || new SCVLatitudeBandStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCVLatitudeBandStat(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCVLatitudeBandStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    MIN_LAT_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_LAT_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    COVERAGE_FRACTION() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MEAN_REVISIT_SEC() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_GAP_SEC() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MEAN_REDUNDANCY() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startSCVLatitudeBandStat(builder) {
        builder.startObject(6);
    }
    static addMinLatDeg(builder, MIN_LAT_DEG) {
        builder.addFieldFloat64(0, MIN_LAT_DEG, 0.0);
    }
    static addMaxLatDeg(builder, MAX_LAT_DEG) {
        builder.addFieldFloat64(1, MAX_LAT_DEG, 0.0);
    }
    static addCoverageFraction(builder, COVERAGE_FRACTION) {
        builder.addFieldFloat64(2, COVERAGE_FRACTION, 0.0);
    }
    static addMeanRevisitSec(builder, MEAN_REVISIT_SEC) {
        builder.addFieldFloat64(3, MEAN_REVISIT_SEC, 0.0);
    }
    static addMaxGapSec(builder, MAX_GAP_SEC) {
        builder.addFieldFloat64(4, MAX_GAP_SEC, 0.0);
    }
    static addMeanRedundancy(builder, MEAN_REDUNDANCY) {
        builder.addFieldFloat64(5, MEAN_REDUNDANCY, 0.0);
    }
    static endSCVLatitudeBandStat(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCVLatitudeBandStat(builder, MIN_LAT_DEG, MAX_LAT_DEG, COVERAGE_FRACTION, MEAN_REVISIT_SEC, MAX_GAP_SEC, MEAN_REDUNDANCY) {
        SCVLatitudeBandStat.startSCVLatitudeBandStat(builder);
        SCVLatitudeBandStat.addMinLatDeg(builder, MIN_LAT_DEG);
        SCVLatitudeBandStat.addMaxLatDeg(builder, MAX_LAT_DEG);
        SCVLatitudeBandStat.addCoverageFraction(builder, COVERAGE_FRACTION);
        SCVLatitudeBandStat.addMeanRevisitSec(builder, MEAN_REVISIT_SEC);
        SCVLatitudeBandStat.addMaxGapSec(builder, MAX_GAP_SEC);
        SCVLatitudeBandStat.addMeanRedundancy(builder, MEAN_REDUNDANCY);
        return SCVLatitudeBandStat.endSCVLatitudeBandStat(builder);
    }
    unpack() {
        return new SCVLatitudeBandStatT(this.MIN_LAT_DEG(), this.MAX_LAT_DEG(), this.COVERAGE_FRACTION(), this.MEAN_REVISIT_SEC(), this.MAX_GAP_SEC(), this.MEAN_REDUNDANCY());
    }
    unpackTo(_o) {
        _o.MIN_LAT_DEG = this.MIN_LAT_DEG();
        _o.MAX_LAT_DEG = this.MAX_LAT_DEG();
        _o.COVERAGE_FRACTION = this.COVERAGE_FRACTION();
        _o.MEAN_REVISIT_SEC = this.MEAN_REVISIT_SEC();
        _o.MAX_GAP_SEC = this.MAX_GAP_SEC();
        _o.MEAN_REDUNDANCY = this.MEAN_REDUNDANCY();
    }
}
export class SCVLatitudeBandStatT {
    constructor(MIN_LAT_DEG = 0.0, MAX_LAT_DEG = 0.0, COVERAGE_FRACTION = 0.0, MEAN_REVISIT_SEC = 0.0, MAX_GAP_SEC = 0.0, MEAN_REDUNDANCY = 0.0) {
        this.MIN_LAT_DEG = MIN_LAT_DEG;
        this.MAX_LAT_DEG = MAX_LAT_DEG;
        this.COVERAGE_FRACTION = COVERAGE_FRACTION;
        this.MEAN_REVISIT_SEC = MEAN_REVISIT_SEC;
        this.MAX_GAP_SEC = MAX_GAP_SEC;
        this.MEAN_REDUNDANCY = MEAN_REDUNDANCY;
    }
    pack(builder) {
        return SCVLatitudeBandStat.createSCVLatitudeBandStat(builder, this.MIN_LAT_DEG, this.MAX_LAT_DEG, this.COVERAGE_FRACTION, this.MEAN_REVISIT_SEC, this.MAX_GAP_SEC, this.MEAN_REDUNDANCY);
    }
}
