// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { weightedEdge } from './weightedEdge.js';
/**
 * Bulk weighted-edge ingestion request.
 */
export class FPCWeightedEdgeList {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsFPCWeightedEdgeList(bb, obj) {
        return (obj || new FPCWeightedEdgeList()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsFPCWeightedEdgeList(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FPCWeightedEdgeList()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Optional vertex capacity for implicit graph creation.
     */
    VERTEX_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Weighted directed edge records.
     */
    EDGES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new weightedEdge()).__init(this.bb.__vector(this.bb_pos + offset) + index * 16, this.bb) : null;
    }
    edgesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Build a CSR representation after ingest.
     */
    BUILD_GRAPH() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startFPCWeightedEdgeList(builder) {
        builder.startObject(3);
    }
    static addVertexCount(builder, VERTEX_COUNT) {
        builder.addFieldInt32(0, VERTEX_COUNT, 0);
    }
    static addEdges(builder, EDGESOffset) {
        builder.addFieldOffset(1, EDGESOffset, 0);
    }
    static startEdgesVector(builder, numElems) {
        builder.startVector(16, numElems, 8);
    }
    static addBuildGraph(builder, BUILD_GRAPH) {
        builder.addFieldInt8(2, +BUILD_GRAPH, +false);
    }
    static endFPCWeightedEdgeList(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createFPCWeightedEdgeList(builder, VERTEX_COUNT, EDGESOffset, BUILD_GRAPH) {
        FPCWeightedEdgeList.startFPCWeightedEdgeList(builder);
        FPCWeightedEdgeList.addVertexCount(builder, VERTEX_COUNT);
        FPCWeightedEdgeList.addEdges(builder, EDGESOffset);
        FPCWeightedEdgeList.addBuildGraph(builder, BUILD_GRAPH);
        return FPCWeightedEdgeList.endFPCWeightedEdgeList(builder);
    }
    unpack() {
        return new FPCWeightedEdgeListT(this.VERTEX_COUNT(), this.bb.createObjList(this.EDGES.bind(this), this.edgesLength()), this.BUILD_GRAPH());
    }
    unpackTo(_o) {
        _o.VERTEX_COUNT = this.VERTEX_COUNT();
        _o.EDGES = this.bb.createObjList(this.EDGES.bind(this), this.edgesLength());
        _o.BUILD_GRAPH = this.BUILD_GRAPH();
    }
}
export class FPCWeightedEdgeListT {
    constructor(VERTEX_COUNT = 0, EDGES = [], BUILD_GRAPH = false) {
        this.VERTEX_COUNT = VERTEX_COUNT;
        this.EDGES = EDGES;
        this.BUILD_GRAPH = BUILD_GRAPH;
    }
    pack(builder) {
        const EDGES = builder.createStructOffsetList(this.EDGES, FPCWeightedEdgeList.startEdgesVector);
        return FPCWeightedEdgeList.createFPCWeightedEdgeList(builder, this.VERTEX_COUNT, EDGES, this.BUILD_GRAPH);
    }
}
