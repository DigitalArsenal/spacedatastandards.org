// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { RFLModelBinding } from './RFLModelBinding.js';
import { rflMethod } from './rflMethod.js';
/**
 * Where this sample set came from, stated once for the record.
 */
export class RFLProvenance {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsRFLProvenance(bb, obj) {
        return (obj || new RFLProvenance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsRFLProvenance(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new RFLProvenance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    METHOD() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : rflMethod.UNSPECIFIED;
    }
    SOURCE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SOURCE_QUERY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROPAGATOR_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROPAGATOR_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROPAGATOR_CONFIGURATION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Uniform sampling step, seconds. Absent when SAMPLE_EPOCHS are irregular.
     */
    SAMPLING_STEP_S() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Per-term model bindings.
     */
    MODELS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new RFLModelBinding()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    modelsLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ENVIRONMENT_DATASET(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TERRAIN_DATASET(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Unix ms this set was computed or recorded.
     */
    COMPUTED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix ms the upstream inputs were retrieved.
     */
    RETRIEVED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    LICENSE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * True when the licence forbids commercial use of this set.
     */
    NON_COMMERCIAL_ONLY() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    CITATION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startRFLProvenance(builder) {
        builder.startObject(15);
    }
    static addMethod(builder, METHOD) {
        builder.addFieldInt8(0, METHOD, rflMethod.UNSPECIFIED);
    }
    static addSource(builder, SOURCEOffset) {
        builder.addFieldOffset(1, SOURCEOffset, 0);
    }
    static addSourceQuery(builder, SOURCE_QUERYOffset) {
        builder.addFieldOffset(2, SOURCE_QUERYOffset, 0);
    }
    static addPropagatorId(builder, PROPAGATOR_IDOffset) {
        builder.addFieldOffset(3, PROPAGATOR_IDOffset, 0);
    }
    static addPropagatorVersion(builder, PROPAGATOR_VERSIONOffset) {
        builder.addFieldOffset(4, PROPAGATOR_VERSIONOffset, 0);
    }
    static addPropagatorConfiguration(builder, PROPAGATOR_CONFIGURATIONOffset) {
        builder.addFieldOffset(5, PROPAGATOR_CONFIGURATIONOffset, 0);
    }
    static addSamplingStepS(builder, SAMPLING_STEP_S) {
        builder.addFieldFloat64(6, SAMPLING_STEP_S, 0.0);
    }
    static addModels(builder, MODELSOffset) {
        builder.addFieldOffset(7, MODELSOffset, 0);
    }
    static createModelsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startModelsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addEnvironmentDataset(builder, ENVIRONMENT_DATASETOffset) {
        builder.addFieldOffset(8, ENVIRONMENT_DATASETOffset, 0);
    }
    static addTerrainDataset(builder, TERRAIN_DATASETOffset) {
        builder.addFieldOffset(9, TERRAIN_DATASETOffset, 0);
    }
    static addComputedAt(builder, COMPUTED_AT) {
        builder.addFieldInt64(10, COMPUTED_AT, BigInt('0'));
    }
    static addRetrievedAt(builder, RETRIEVED_AT) {
        builder.addFieldInt64(11, RETRIEVED_AT, BigInt('0'));
    }
    static addLicense(builder, LICENSEOffset) {
        builder.addFieldOffset(12, LICENSEOffset, 0);
    }
    static addNonCommercialOnly(builder, NON_COMMERCIAL_ONLY) {
        builder.addFieldInt8(13, +NON_COMMERCIAL_ONLY, +false);
    }
    static addCitation(builder, CITATIONOffset) {
        builder.addFieldOffset(14, CITATIONOffset, 0);
    }
    static endRFLProvenance(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // SOURCE
        return offset;
    }
    static createRFLProvenance(builder, METHOD, SOURCEOffset, SOURCE_QUERYOffset, PROPAGATOR_IDOffset, PROPAGATOR_VERSIONOffset, PROPAGATOR_CONFIGURATIONOffset, SAMPLING_STEP_S, MODELSOffset, ENVIRONMENT_DATASETOffset, TERRAIN_DATASETOffset, COMPUTED_AT, RETRIEVED_AT, LICENSEOffset, NON_COMMERCIAL_ONLY, CITATIONOffset) {
        RFLProvenance.startRFLProvenance(builder);
        RFLProvenance.addMethod(builder, METHOD);
        RFLProvenance.addSource(builder, SOURCEOffset);
        RFLProvenance.addSourceQuery(builder, SOURCE_QUERYOffset);
        RFLProvenance.addPropagatorId(builder, PROPAGATOR_IDOffset);
        RFLProvenance.addPropagatorVersion(builder, PROPAGATOR_VERSIONOffset);
        RFLProvenance.addPropagatorConfiguration(builder, PROPAGATOR_CONFIGURATIONOffset);
        RFLProvenance.addSamplingStepS(builder, SAMPLING_STEP_S);
        RFLProvenance.addModels(builder, MODELSOffset);
        RFLProvenance.addEnvironmentDataset(builder, ENVIRONMENT_DATASETOffset);
        RFLProvenance.addTerrainDataset(builder, TERRAIN_DATASETOffset);
        RFLProvenance.addComputedAt(builder, COMPUTED_AT);
        RFLProvenance.addRetrievedAt(builder, RETRIEVED_AT);
        RFLProvenance.addLicense(builder, LICENSEOffset);
        RFLProvenance.addNonCommercialOnly(builder, NON_COMMERCIAL_ONLY);
        RFLProvenance.addCitation(builder, CITATIONOffset);
        return RFLProvenance.endRFLProvenance(builder);
    }
    unpack() {
        return new RFLProvenanceT(this.METHOD(), this.SOURCE(), this.SOURCE_QUERY(), this.PROPAGATOR_ID(), this.PROPAGATOR_VERSION(), this.PROPAGATOR_CONFIGURATION(), this.SAMPLING_STEP_S(), this.bb.createObjList(this.MODELS.bind(this), this.modelsLength()), this.ENVIRONMENT_DATASET(), this.TERRAIN_DATASET(), this.COMPUTED_AT(), this.RETRIEVED_AT(), this.LICENSE(), this.NON_COMMERCIAL_ONLY(), this.CITATION());
    }
    unpackTo(_o) {
        _o.METHOD = this.METHOD();
        _o.SOURCE = this.SOURCE();
        _o.SOURCE_QUERY = this.SOURCE_QUERY();
        _o.PROPAGATOR_ID = this.PROPAGATOR_ID();
        _o.PROPAGATOR_VERSION = this.PROPAGATOR_VERSION();
        _o.PROPAGATOR_CONFIGURATION = this.PROPAGATOR_CONFIGURATION();
        _o.SAMPLING_STEP_S = this.SAMPLING_STEP_S();
        _o.MODELS = this.bb.createObjList(this.MODELS.bind(this), this.modelsLength());
        _o.ENVIRONMENT_DATASET = this.ENVIRONMENT_DATASET();
        _o.TERRAIN_DATASET = this.TERRAIN_DATASET();
        _o.COMPUTED_AT = this.COMPUTED_AT();
        _o.RETRIEVED_AT = this.RETRIEVED_AT();
        _o.LICENSE = this.LICENSE();
        _o.NON_COMMERCIAL_ONLY = this.NON_COMMERCIAL_ONLY();
        _o.CITATION = this.CITATION();
    }
}
export class RFLProvenanceT {
    constructor(METHOD = rflMethod.UNSPECIFIED, SOURCE = null, SOURCE_QUERY = null, PROPAGATOR_ID = null, PROPAGATOR_VERSION = null, PROPAGATOR_CONFIGURATION = null, SAMPLING_STEP_S = 0.0, MODELS = [], ENVIRONMENT_DATASET = null, TERRAIN_DATASET = null, COMPUTED_AT = BigInt('0'), RETRIEVED_AT = BigInt('0'), LICENSE = null, NON_COMMERCIAL_ONLY = false, CITATION = null) {
        this.METHOD = METHOD;
        this.SOURCE = SOURCE;
        this.SOURCE_QUERY = SOURCE_QUERY;
        this.PROPAGATOR_ID = PROPAGATOR_ID;
        this.PROPAGATOR_VERSION = PROPAGATOR_VERSION;
        this.PROPAGATOR_CONFIGURATION = PROPAGATOR_CONFIGURATION;
        this.SAMPLING_STEP_S = SAMPLING_STEP_S;
        this.MODELS = MODELS;
        this.ENVIRONMENT_DATASET = ENVIRONMENT_DATASET;
        this.TERRAIN_DATASET = TERRAIN_DATASET;
        this.COMPUTED_AT = COMPUTED_AT;
        this.RETRIEVED_AT = RETRIEVED_AT;
        this.LICENSE = LICENSE;
        this.NON_COMMERCIAL_ONLY = NON_COMMERCIAL_ONLY;
        this.CITATION = CITATION;
    }
    pack(builder) {
        const SOURCE = (this.SOURCE !== null ? builder.createString(this.SOURCE) : 0);
        const SOURCE_QUERY = (this.SOURCE_QUERY !== null ? builder.createString(this.SOURCE_QUERY) : 0);
        const PROPAGATOR_ID = (this.PROPAGATOR_ID !== null ? builder.createString(this.PROPAGATOR_ID) : 0);
        const PROPAGATOR_VERSION = (this.PROPAGATOR_VERSION !== null ? builder.createString(this.PROPAGATOR_VERSION) : 0);
        const PROPAGATOR_CONFIGURATION = (this.PROPAGATOR_CONFIGURATION !== null ? builder.createString(this.PROPAGATOR_CONFIGURATION) : 0);
        const MODELS = RFLProvenance.createModelsVector(builder, builder.createObjectOffsetList(this.MODELS));
        const ENVIRONMENT_DATASET = (this.ENVIRONMENT_DATASET !== null ? builder.createString(this.ENVIRONMENT_DATASET) : 0);
        const TERRAIN_DATASET = (this.TERRAIN_DATASET !== null ? builder.createString(this.TERRAIN_DATASET) : 0);
        const LICENSE = (this.LICENSE !== null ? builder.createString(this.LICENSE) : 0);
        const CITATION = (this.CITATION !== null ? builder.createString(this.CITATION) : 0);
        return RFLProvenance.createRFLProvenance(builder, this.METHOD, SOURCE, SOURCE_QUERY, PROPAGATOR_ID, PROPAGATOR_VERSION, PROPAGATOR_CONFIGURATION, this.SAMPLING_STEP_S, MODELS, ENVIRONMENT_DATASET, TERRAIN_DATASET, this.COMPUTED_AT, this.RETRIEVED_AT, LICENSE, this.NON_COMMERCIAL_ONLY, CITATION);
    }
}
