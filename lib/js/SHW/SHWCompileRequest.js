// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SHWUniform } from './SHWUniform.js';
import { shaderInjectionPoint } from './shaderInjectionPoint.js';
import { shaderStage } from './shaderStage.js';
/**
 * Compile request — one shader source (typically a fragment of a
 * larger stage) plus metadata for the host splice.
 */
export class SHWCompileRequest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSHWCompileRequest(bb, obj) {
        return (obj || new SHWCompileRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSHWCompileRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SHWCompileRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SHADER_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Target GLSL stage.
     */
    SHADER_STAGE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : shaderStage.VERTEX;
    }
    /**
     * Intended injection point in the host pipeline.
     */
    SHADER_INJECTION_POINT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : shaderInjectionPoint.NONE;
    }
    SHADER_SOURCE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    GLSL_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Expected uniforms declared by the source.
     */
    SHADER_UNIFORMS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new SHWUniform()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    shaderUniformsLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startSHWCompileRequest(builder) {
        builder.startObject(6);
    }
    static addShaderName(builder, SHADER_NAMEOffset) {
        builder.addFieldOffset(0, SHADER_NAMEOffset, 0);
    }
    static addShaderStage(builder, SHADER_STAGE) {
        builder.addFieldInt8(1, SHADER_STAGE, shaderStage.VERTEX);
    }
    static addShaderInjectionPoint(builder, SHADER_INJECTION_POINT) {
        builder.addFieldInt8(2, SHADER_INJECTION_POINT, shaderInjectionPoint.NONE);
    }
    static addShaderSource(builder, SHADER_SOURCEOffset) {
        builder.addFieldOffset(3, SHADER_SOURCEOffset, 0);
    }
    static addGlslVersion(builder, GLSL_VERSIONOffset) {
        builder.addFieldOffset(4, GLSL_VERSIONOffset, 0);
    }
    static addShaderUniforms(builder, SHADER_UNIFORMSOffset) {
        builder.addFieldOffset(5, SHADER_UNIFORMSOffset, 0);
    }
    static createShaderUniformsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startShaderUniformsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endSHWCompileRequest(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSHWCompileRequest(builder, SHADER_NAMEOffset, SHADER_STAGE, SHADER_INJECTION_POINT, SHADER_SOURCEOffset, GLSL_VERSIONOffset, SHADER_UNIFORMSOffset) {
        SHWCompileRequest.startSHWCompileRequest(builder);
        SHWCompileRequest.addShaderName(builder, SHADER_NAMEOffset);
        SHWCompileRequest.addShaderStage(builder, SHADER_STAGE);
        SHWCompileRequest.addShaderInjectionPoint(builder, SHADER_INJECTION_POINT);
        SHWCompileRequest.addShaderSource(builder, SHADER_SOURCEOffset);
        SHWCompileRequest.addGlslVersion(builder, GLSL_VERSIONOffset);
        SHWCompileRequest.addShaderUniforms(builder, SHADER_UNIFORMSOffset);
        return SHWCompileRequest.endSHWCompileRequest(builder);
    }
    unpack() {
        return new SHWCompileRequestT(this.SHADER_NAME(), this.SHADER_STAGE(), this.SHADER_INJECTION_POINT(), this.SHADER_SOURCE(), this.GLSL_VERSION(), this.bb.createObjList(this.SHADER_UNIFORMS.bind(this), this.shaderUniformsLength()));
    }
    unpackTo(_o) {
        _o.SHADER_NAME = this.SHADER_NAME();
        _o.SHADER_STAGE = this.SHADER_STAGE();
        _o.SHADER_INJECTION_POINT = this.SHADER_INJECTION_POINT();
        _o.SHADER_SOURCE = this.SHADER_SOURCE();
        _o.GLSL_VERSION = this.GLSL_VERSION();
        _o.SHADER_UNIFORMS = this.bb.createObjList(this.SHADER_UNIFORMS.bind(this), this.shaderUniformsLength());
    }
}
export class SHWCompileRequestT {
    constructor(SHADER_NAME = null, SHADER_STAGE = shaderStage.VERTEX, SHADER_INJECTION_POINT = shaderInjectionPoint.NONE, SHADER_SOURCE = null, GLSL_VERSION = null, SHADER_UNIFORMS = []) {
        this.SHADER_NAME = SHADER_NAME;
        this.SHADER_STAGE = SHADER_STAGE;
        this.SHADER_INJECTION_POINT = SHADER_INJECTION_POINT;
        this.SHADER_SOURCE = SHADER_SOURCE;
        this.GLSL_VERSION = GLSL_VERSION;
        this.SHADER_UNIFORMS = SHADER_UNIFORMS;
    }
    pack(builder) {
        const SHADER_NAME = (this.SHADER_NAME !== null ? builder.createString(this.SHADER_NAME) : 0);
        const SHADER_SOURCE = (this.SHADER_SOURCE !== null ? builder.createString(this.SHADER_SOURCE) : 0);
        const GLSL_VERSION = (this.GLSL_VERSION !== null ? builder.createString(this.GLSL_VERSION) : 0);
        const SHADER_UNIFORMS = SHWCompileRequest.createShaderUniformsVector(builder, builder.createObjectOffsetList(this.SHADER_UNIFORMS));
        return SHWCompileRequest.createSHWCompileRequest(builder, SHADER_NAME, this.SHADER_STAGE, this.SHADER_INJECTION_POINT, SHADER_SOURCE, GLSL_VERSION, SHADER_UNIFORMS);
    }
}
