// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class ODREditedObservation {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsODREditedObservation(bb, obj) {
        return (obj || new ODREditedObservation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsODREditedObservation(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ODREditedObservation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    OBSERVATION_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    ITERATION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    PREFIT_RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    NORMALIZED_RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    REASON(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startODREditedObservation(builder) {
        builder.startObject(5);
    }
    static addObservationId(builder, OBSERVATION_IDOffset) {
        builder.addFieldOffset(0, OBSERVATION_IDOffset, 0);
    }
    static addIteration(builder, ITERATION) {
        builder.addFieldInt32(1, ITERATION, 0);
    }
    static addPrefitResidual(builder, PREFIT_RESIDUAL) {
        builder.addFieldFloat64(2, PREFIT_RESIDUAL, 0.0);
    }
    static addNormalizedResidual(builder, NORMALIZED_RESIDUAL) {
        builder.addFieldFloat64(3, NORMALIZED_RESIDUAL, 0.0);
    }
    static addReason(builder, REASONOffset) {
        builder.addFieldOffset(4, REASONOffset, 0);
    }
    static endODREditedObservation(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // OBSERVATION_ID
        return offset;
    }
    static createODREditedObservation(builder, OBSERVATION_IDOffset, ITERATION, PREFIT_RESIDUAL, NORMALIZED_RESIDUAL, REASONOffset) {
        ODREditedObservation.startODREditedObservation(builder);
        ODREditedObservation.addObservationId(builder, OBSERVATION_IDOffset);
        ODREditedObservation.addIteration(builder, ITERATION);
        ODREditedObservation.addPrefitResidual(builder, PREFIT_RESIDUAL);
        ODREditedObservation.addNormalizedResidual(builder, NORMALIZED_RESIDUAL);
        ODREditedObservation.addReason(builder, REASONOffset);
        return ODREditedObservation.endODREditedObservation(builder);
    }
    unpack() {
        return new ODREditedObservationT(this.OBSERVATION_ID(), this.ITERATION(), this.PREFIT_RESIDUAL(), this.NORMALIZED_RESIDUAL(), this.REASON());
    }
    unpackTo(_o) {
        _o.OBSERVATION_ID = this.OBSERVATION_ID();
        _o.ITERATION = this.ITERATION();
        _o.PREFIT_RESIDUAL = this.PREFIT_RESIDUAL();
        _o.NORMALIZED_RESIDUAL = this.NORMALIZED_RESIDUAL();
        _o.REASON = this.REASON();
    }
}
export class ODREditedObservationT {
    constructor(OBSERVATION_ID = null, ITERATION = 0, PREFIT_RESIDUAL = 0.0, NORMALIZED_RESIDUAL = 0.0, REASON = null) {
        this.OBSERVATION_ID = OBSERVATION_ID;
        this.ITERATION = ITERATION;
        this.PREFIT_RESIDUAL = PREFIT_RESIDUAL;
        this.NORMALIZED_RESIDUAL = NORMALIZED_RESIDUAL;
        this.REASON = REASON;
    }
    pack(builder) {
        const OBSERVATION_ID = (this.OBSERVATION_ID !== null ? builder.createString(this.OBSERVATION_ID) : 0);
        const REASON = (this.REASON !== null ? builder.createString(this.REASON) : 0);
        return ODREditedObservation.createODREditedObservation(builder, OBSERVATION_ID, this.ITERATION, this.PREFIT_RESIDUAL, this.NORMALIZED_RESIDUAL, REASON);
    }
}
