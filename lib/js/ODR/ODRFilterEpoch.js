// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { FRMStateVector } from './FRMStateVector.js';
export class ODRFilterEpoch {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsODRFilterEpoch(bb, obj) {
        return (obj || new ODRFilterEpoch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsODRFilterEpoch(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ODRFilterEpoch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FILTERED_STATE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new FRMStateVector()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    FILTERED_COVARIANCE(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    filteredCovarianceLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    filteredCovarianceArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    SMOOTHED_STATE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new FRMStateVector()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    SMOOTHED_COVARIANCE(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    smoothedCovarianceLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    smoothedCovarianceArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    NORMALIZED_INNOVATION_SQUARED() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startODRFilterEpoch(builder) {
        builder.startObject(6);
    }
    static addEpoch(builder, EPOCHOffset) {
        builder.addFieldOffset(0, EPOCHOffset, 0);
    }
    static addFilteredState(builder, FILTERED_STATEOffset) {
        builder.addFieldOffset(1, FILTERED_STATEOffset, 0);
    }
    static addFilteredCovariance(builder, FILTERED_COVARIANCEOffset) {
        builder.addFieldOffset(2, FILTERED_COVARIANCEOffset, 0);
    }
    static createFilteredCovarianceVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startFilteredCovarianceVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addSmoothedState(builder, SMOOTHED_STATEOffset) {
        builder.addFieldOffset(3, SMOOTHED_STATEOffset, 0);
    }
    static addSmoothedCovariance(builder, SMOOTHED_COVARIANCEOffset) {
        builder.addFieldOffset(4, SMOOTHED_COVARIANCEOffset, 0);
    }
    static createSmoothedCovarianceVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startSmoothedCovarianceVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addNormalizedInnovationSquared(builder, NORMALIZED_INNOVATION_SQUARED) {
        builder.addFieldFloat64(5, NORMALIZED_INNOVATION_SQUARED, 0.0);
    }
    static endODRFilterEpoch(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new ODRFilterEpochT(this.EPOCH(), (this.FILTERED_STATE() !== null ? this.FILTERED_STATE().unpack() : null), this.bb.createScalarList(this.FILTERED_COVARIANCE.bind(this), this.filteredCovarianceLength()), (this.SMOOTHED_STATE() !== null ? this.SMOOTHED_STATE().unpack() : null), this.bb.createScalarList(this.SMOOTHED_COVARIANCE.bind(this), this.smoothedCovarianceLength()), this.NORMALIZED_INNOVATION_SQUARED());
    }
    unpackTo(_o) {
        _o.EPOCH = this.EPOCH();
        _o.FILTERED_STATE = (this.FILTERED_STATE() !== null ? this.FILTERED_STATE().unpack() : null);
        _o.FILTERED_COVARIANCE = this.bb.createScalarList(this.FILTERED_COVARIANCE.bind(this), this.filteredCovarianceLength());
        _o.SMOOTHED_STATE = (this.SMOOTHED_STATE() !== null ? this.SMOOTHED_STATE().unpack() : null);
        _o.SMOOTHED_COVARIANCE = this.bb.createScalarList(this.SMOOTHED_COVARIANCE.bind(this), this.smoothedCovarianceLength());
        _o.NORMALIZED_INNOVATION_SQUARED = this.NORMALIZED_INNOVATION_SQUARED();
    }
}
export class ODRFilterEpochT {
    constructor(EPOCH = null, FILTERED_STATE = null, FILTERED_COVARIANCE = [], SMOOTHED_STATE = null, SMOOTHED_COVARIANCE = [], NORMALIZED_INNOVATION_SQUARED = 0.0) {
        this.EPOCH = EPOCH;
        this.FILTERED_STATE = FILTERED_STATE;
        this.FILTERED_COVARIANCE = FILTERED_COVARIANCE;
        this.SMOOTHED_STATE = SMOOTHED_STATE;
        this.SMOOTHED_COVARIANCE = SMOOTHED_COVARIANCE;
        this.NORMALIZED_INNOVATION_SQUARED = NORMALIZED_INNOVATION_SQUARED;
    }
    pack(builder) {
        const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH) : 0);
        const FILTERED_STATE = (this.FILTERED_STATE !== null ? this.FILTERED_STATE.pack(builder) : 0);
        const FILTERED_COVARIANCE = ODRFilterEpoch.createFilteredCovarianceVector(builder, this.FILTERED_COVARIANCE);
        const SMOOTHED_STATE = (this.SMOOTHED_STATE !== null ? this.SMOOTHED_STATE.pack(builder) : 0);
        const SMOOTHED_COVARIANCE = ODRFilterEpoch.createSmoothedCovarianceVector(builder, this.SMOOTHED_COVARIANCE);
        ODRFilterEpoch.startODRFilterEpoch(builder);
        ODRFilterEpoch.addEpoch(builder, EPOCH);
        ODRFilterEpoch.addFilteredState(builder, FILTERED_STATE);
        ODRFilterEpoch.addFilteredCovariance(builder, FILTERED_COVARIANCE);
        ODRFilterEpoch.addSmoothedState(builder, SMOOTHED_STATE);
        ODRFilterEpoch.addSmoothedCovariance(builder, SMOOTHED_COVARIANCE);
        ODRFilterEpoch.addNormalizedInnovationSquared(builder, this.NORMALIZED_INNOVATION_SQUARED);
        return ODRFilterEpoch.endODRFilterEpoch(builder);
    }
}
