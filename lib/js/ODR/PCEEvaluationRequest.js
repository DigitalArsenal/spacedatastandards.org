// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { FRMStateVector } from './FRMStateVector.js';
import { PCEEvaluationContext } from './PCEEvaluationContext.js';
import { PCEParameterRef } from './PCEParameterRef.js';
/**
 * Evaluate these parameters on these states. One state is a point query; a
 * run of states is a report or a plot series, and carries the same shape.
 */
export class PCEEvaluationRequest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPCEEvaluationRequest(bb, obj) {
        return (obj || new PCEEvaluationRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPCEEvaluationRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PCEEvaluationRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    CONTEXT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new PCEEvaluationContext()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    PARAMETERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new PCEParameterRef()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    parametersLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * States to evaluate at, in chronological order. Each carries its own
     * epoch and coordinate system name.
     */
    STATES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new FRMStateVector()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    statesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startPCEEvaluationRequest(builder) {
        builder.startObject(4);
    }
    static addContext(builder, CONTEXTOffset) {
        builder.addFieldOffset(0, CONTEXTOffset, 0);
    }
    static addParameters(builder, PARAMETERSOffset) {
        builder.addFieldOffset(1, PARAMETERSOffset, 0);
    }
    static createParametersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startParametersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addStates(builder, STATESOffset) {
        builder.addFieldOffset(2, STATESOffset, 0);
    }
    static createStatesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startStatesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(3, TRACE_IDOffset, 0);
    }
    static endPCEEvaluationRequest(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createPCEEvaluationRequest(builder, CONTEXTOffset, PARAMETERSOffset, STATESOffset, TRACE_IDOffset) {
        PCEEvaluationRequest.startPCEEvaluationRequest(builder);
        PCEEvaluationRequest.addContext(builder, CONTEXTOffset);
        PCEEvaluationRequest.addParameters(builder, PARAMETERSOffset);
        PCEEvaluationRequest.addStates(builder, STATESOffset);
        PCEEvaluationRequest.addTraceId(builder, TRACE_IDOffset);
        return PCEEvaluationRequest.endPCEEvaluationRequest(builder);
    }
    unpack() {
        return new PCEEvaluationRequestT((this.CONTEXT() !== null ? this.CONTEXT().unpack() : null), this.bb.createObjList(this.PARAMETERS.bind(this), this.parametersLength()), this.bb.createObjList(this.STATES.bind(this), this.statesLength()), this.TRACE_ID());
    }
    unpackTo(_o) {
        _o.CONTEXT = (this.CONTEXT() !== null ? this.CONTEXT().unpack() : null);
        _o.PARAMETERS = this.bb.createObjList(this.PARAMETERS.bind(this), this.parametersLength());
        _o.STATES = this.bb.createObjList(this.STATES.bind(this), this.statesLength());
        _o.TRACE_ID = this.TRACE_ID();
    }
}
export class PCEEvaluationRequestT {
    constructor(CONTEXT = null, PARAMETERS = [], STATES = [], TRACE_ID = null) {
        this.CONTEXT = CONTEXT;
        this.PARAMETERS = PARAMETERS;
        this.STATES = STATES;
        this.TRACE_ID = TRACE_ID;
    }
    pack(builder) {
        const CONTEXT = (this.CONTEXT !== null ? this.CONTEXT.pack(builder) : 0);
        const PARAMETERS = PCEEvaluationRequest.createParametersVector(builder, builder.createObjectOffsetList(this.PARAMETERS));
        const STATES = PCEEvaluationRequest.createStatesVector(builder, builder.createObjectOffsetList(this.STATES));
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        return PCEEvaluationRequest.createPCEEvaluationRequest(builder, CONTEXT, PARAMETERS, STATES, TRACE_ID);
    }
}
