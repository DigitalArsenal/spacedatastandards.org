// automatically generated by the FlatBuffers compiler, do not modify
import * as flatbuffers from 'flatbuffers';
import { SCHEMA_STANDARD } from './SCHEMA_STANDARD.js';
/**
 * Schema Manifest
 */
export class SCHEMA_MANIFEST {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCHEMA_MANIFEST(bb, obj) {
        return (obj || new SCHEMA_MANIFEST()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCHEMA_MANIFEST(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCHEMA_MANIFEST()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('SCMM');
    }
    version(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Standards Dictionary
     */
    RECORDS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new SCHEMA_STANDARD()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    recordsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startSCHEMA_MANIFEST(builder) {
        builder.startObject(2);
    }
    static addVersion(builder, versionOffset) {
        builder.addFieldOffset(0, versionOffset, 0);
    }
    static addRecords(builder, RECORDSOffset) {
        builder.addFieldOffset(1, RECORDSOffset, 0);
    }
    static createRecordsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRecordsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endSCHEMA_MANIFEST(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishSCHEMA_MANIFESTBuffer(builder, offset) {
        builder.finish(offset, 'SCMM');
    }
    static finishSizePrefixedSCHEMA_MANIFESTBuffer(builder, offset) {
        builder.finish(offset, 'SCMM', true);
    }
    static createSCHEMA_MANIFEST(builder, versionOffset, RECORDSOffset) {
        SCHEMA_MANIFEST.startSCHEMA_MANIFEST(builder);
        SCHEMA_MANIFEST.addVersion(builder, versionOffset);
        SCHEMA_MANIFEST.addRecords(builder, RECORDSOffset);
        return SCHEMA_MANIFEST.endSCHEMA_MANIFEST(builder);
    }
    unpack() {
        return new SCHEMA_MANIFESTT(this.version(), this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength()));
    }
    unpackTo(_o) {
        _o.version = this.version();
        _o.RECORDS = this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength());
    }
}
export class SCHEMA_MANIFESTT {
    constructor(version = null, RECORDS = []) {
        this.version = version;
        this.RECORDS = RECORDS;
    }
    pack(builder) {
        const version = (this.version !== null ? builder.createString(this.version) : 0);
        const RECORDS = SCHEMA_MANIFEST.createRecordsVector(builder, builder.createObjectOffsetList(this.RECORDS));
        return SCHEMA_MANIFEST.createSCHEMA_MANIFEST(builder, version, RECORDS);
    }
}
