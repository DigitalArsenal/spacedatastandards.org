// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SDFComponent } from './SDFComponent.js';
import { SDFComposition } from './SDFComposition.js';
import { sdfPrimitive } from './sdfPrimitive.js';
/**
 * Complete SDF configuration.
 */
export class SDFConfig {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSDFConfig(bb, obj) {
        return (obj || new SDFConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSDFConfig(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SDFConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Root primitive type (useful when COMPONENTS[] is empty).
     */
    PRIMITIVE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : sdfPrimitive.CONE;
    }
    /**
     * One or more components that may be combined via COMPOSITIONS[].
     */
    COMPONENTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new SDFComponent()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    componentsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Composition tree over COMPONENTS[].
     */
    COMPOSITIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new SDFComposition()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    compositionsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * World-to-local transform (4x4 column-major, 16 doubles).
     */
    INVERSE_TRANSFORM(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    inverseTransformLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    inverseTransformArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startSDFConfig(builder) {
        builder.startObject(4);
    }
    static addPrimitive(builder, PRIMITIVE) {
        builder.addFieldInt8(0, PRIMITIVE, sdfPrimitive.CONE);
    }
    static addComponents(builder, COMPONENTSOffset) {
        builder.addFieldOffset(1, COMPONENTSOffset, 0);
    }
    static createComponentsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startComponentsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCompositions(builder, COMPOSITIONSOffset) {
        builder.addFieldOffset(2, COMPOSITIONSOffset, 0);
    }
    static createCompositionsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startCompositionsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addInverseTransform(builder, INVERSE_TRANSFORMOffset) {
        builder.addFieldOffset(3, INVERSE_TRANSFORMOffset, 0);
    }
    static createInverseTransformVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startInverseTransformVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endSDFConfig(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSDFConfig(builder, PRIMITIVE, COMPONENTSOffset, COMPOSITIONSOffset, INVERSE_TRANSFORMOffset) {
        SDFConfig.startSDFConfig(builder);
        SDFConfig.addPrimitive(builder, PRIMITIVE);
        SDFConfig.addComponents(builder, COMPONENTSOffset);
        SDFConfig.addCompositions(builder, COMPOSITIONSOffset);
        SDFConfig.addInverseTransform(builder, INVERSE_TRANSFORMOffset);
        return SDFConfig.endSDFConfig(builder);
    }
    unpack() {
        return new SDFConfigT(this.PRIMITIVE(), this.bb.createObjList(this.COMPONENTS.bind(this), this.componentsLength()), this.bb.createObjList(this.COMPOSITIONS.bind(this), this.compositionsLength()), this.bb.createScalarList(this.INVERSE_TRANSFORM.bind(this), this.inverseTransformLength()));
    }
    unpackTo(_o) {
        _o.PRIMITIVE = this.PRIMITIVE();
        _o.COMPONENTS = this.bb.createObjList(this.COMPONENTS.bind(this), this.componentsLength());
        _o.COMPOSITIONS = this.bb.createObjList(this.COMPOSITIONS.bind(this), this.compositionsLength());
        _o.INVERSE_TRANSFORM = this.bb.createScalarList(this.INVERSE_TRANSFORM.bind(this), this.inverseTransformLength());
    }
}
export class SDFConfigT {
    constructor(PRIMITIVE = sdfPrimitive.CONE, COMPONENTS = [], COMPOSITIONS = [], INVERSE_TRANSFORM = []) {
        this.PRIMITIVE = PRIMITIVE;
        this.COMPONENTS = COMPONENTS;
        this.COMPOSITIONS = COMPOSITIONS;
        this.INVERSE_TRANSFORM = INVERSE_TRANSFORM;
    }
    pack(builder) {
        const COMPONENTS = SDFConfig.createComponentsVector(builder, builder.createObjectOffsetList(this.COMPONENTS));
        const COMPOSITIONS = SDFConfig.createCompositionsVector(builder, builder.createObjectOffsetList(this.COMPOSITIONS));
        const INVERSE_TRANSFORM = SDFConfig.createInverseTransformVector(builder, this.INVERSE_TRANSFORM);
        return SDFConfig.createSDFConfig(builder, this.PRIMITIVE, COMPONENTS, COMPOSITIONS, INVERSE_TRANSFORM);
    }
}
