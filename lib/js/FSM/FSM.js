// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { FieldStreamValue } from './FieldStreamValue.js';
export class FSM {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsFSM(bb, obj) {
        return (obj || new FSM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsFSM(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FSM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$FSM');
    }
    MESSAGE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    PROVIDER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    LISTING_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    STREAM_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SCHEMA_CODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SCHEMA_HASH(index) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    schemaHashLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    schemaHashArray() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    POLICY_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    POLICY_VERSION() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 1;
    }
    KEY_EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SEQUENCE() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    PRODUCED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    EXPIRES_AT() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    SUBJECT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FIELDS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? (obj || new FieldStreamValue()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    fieldsLength() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    PAYLOAD_HASH(index) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    payloadHashLength() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    payloadHashArray() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    PREVIOUS_MESSAGE_HASH(index) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    previousMessageHashLength() {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    previousMessageHashArray() {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    PROVIDER_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    providerSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    providerSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startFSM(builder) {
        builder.startObject(17);
    }
    static addMessageId(builder, MESSAGE_IDOffset) {
        builder.addFieldOffset(0, MESSAGE_IDOffset, 0);
    }
    static addProviderPeerId(builder, PROVIDER_PEER_IDOffset) {
        builder.addFieldOffset(1, PROVIDER_PEER_IDOffset, 0);
    }
    static addListingId(builder, LISTING_IDOffset) {
        builder.addFieldOffset(2, LISTING_IDOffset, 0);
    }
    static addStreamId(builder, STREAM_IDOffset) {
        builder.addFieldOffset(3, STREAM_IDOffset, 0);
    }
    static addSchemaCode(builder, SCHEMA_CODEOffset) {
        builder.addFieldOffset(4, SCHEMA_CODEOffset, 0);
    }
    static addSchemaHash(builder, SCHEMA_HASHOffset) {
        builder.addFieldOffset(5, SCHEMA_HASHOffset, 0);
    }
    static createSchemaHashVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSchemaHashVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addPolicyId(builder, POLICY_IDOffset) {
        builder.addFieldOffset(6, POLICY_IDOffset, 0);
    }
    static addPolicyVersion(builder, POLICY_VERSION) {
        builder.addFieldInt32(7, POLICY_VERSION, 1);
    }
    static addKeyEpoch(builder, KEY_EPOCHOffset) {
        builder.addFieldOffset(8, KEY_EPOCHOffset, 0);
    }
    static addSequence(builder, SEQUENCE) {
        builder.addFieldInt64(9, SEQUENCE, BigInt('0'));
    }
    static addProducedAt(builder, PRODUCED_AT) {
        builder.addFieldInt64(10, PRODUCED_AT, BigInt('0'));
    }
    static addExpiresAt(builder, EXPIRES_AT) {
        builder.addFieldInt64(11, EXPIRES_AT, BigInt('0'));
    }
    static addSubjectId(builder, SUBJECT_IDOffset) {
        builder.addFieldOffset(12, SUBJECT_IDOffset, 0);
    }
    static addFields(builder, FIELDSOffset) {
        builder.addFieldOffset(13, FIELDSOffset, 0);
    }
    static createFieldsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startFieldsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addPayloadHash(builder, PAYLOAD_HASHOffset) {
        builder.addFieldOffset(14, PAYLOAD_HASHOffset, 0);
    }
    static createPayloadHashVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startPayloadHashVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addPreviousMessageHash(builder, PREVIOUS_MESSAGE_HASHOffset) {
        builder.addFieldOffset(15, PREVIOUS_MESSAGE_HASHOffset, 0);
    }
    static createPreviousMessageHashVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startPreviousMessageHashVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addProviderSignature(builder, PROVIDER_SIGNATUREOffset) {
        builder.addFieldOffset(16, PROVIDER_SIGNATUREOffset, 0);
    }
    static createProviderSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startProviderSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endFSM(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // MESSAGE_ID
        builder.requiredField(offset, 6); // PROVIDER_PEER_ID
        builder.requiredField(offset, 8); // LISTING_ID
        builder.requiredField(offset, 10); // STREAM_ID
        builder.requiredField(offset, 12); // SCHEMA_CODE
        builder.requiredField(offset, 16); // POLICY_ID
        return offset;
    }
    static finishFSMBuffer(builder, offset) {
        builder.finish(offset, '$FSM');
    }
    static finishSizePrefixedFSMBuffer(builder, offset) {
        builder.finish(offset, '$FSM', true);
    }
    static createFSM(builder, MESSAGE_IDOffset, PROVIDER_PEER_IDOffset, LISTING_IDOffset, STREAM_IDOffset, SCHEMA_CODEOffset, SCHEMA_HASHOffset, POLICY_IDOffset, POLICY_VERSION, KEY_EPOCHOffset, SEQUENCE, PRODUCED_AT, EXPIRES_AT, SUBJECT_IDOffset, FIELDSOffset, PAYLOAD_HASHOffset, PREVIOUS_MESSAGE_HASHOffset, PROVIDER_SIGNATUREOffset) {
        FSM.startFSM(builder);
        FSM.addMessageId(builder, MESSAGE_IDOffset);
        FSM.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
        FSM.addListingId(builder, LISTING_IDOffset);
        FSM.addStreamId(builder, STREAM_IDOffset);
        FSM.addSchemaCode(builder, SCHEMA_CODEOffset);
        FSM.addSchemaHash(builder, SCHEMA_HASHOffset);
        FSM.addPolicyId(builder, POLICY_IDOffset);
        FSM.addPolicyVersion(builder, POLICY_VERSION);
        FSM.addKeyEpoch(builder, KEY_EPOCHOffset);
        FSM.addSequence(builder, SEQUENCE);
        FSM.addProducedAt(builder, PRODUCED_AT);
        FSM.addExpiresAt(builder, EXPIRES_AT);
        FSM.addSubjectId(builder, SUBJECT_IDOffset);
        FSM.addFields(builder, FIELDSOffset);
        FSM.addPayloadHash(builder, PAYLOAD_HASHOffset);
        FSM.addPreviousMessageHash(builder, PREVIOUS_MESSAGE_HASHOffset);
        FSM.addProviderSignature(builder, PROVIDER_SIGNATUREOffset);
        return FSM.endFSM(builder);
    }
    unpack() {
        return new FSMT(this.MESSAGE_ID(), this.PROVIDER_PEER_ID(), this.LISTING_ID(), this.STREAM_ID(), this.SCHEMA_CODE(), this.bb.createScalarList(this.SCHEMA_HASH.bind(this), this.schemaHashLength()), this.POLICY_ID(), this.POLICY_VERSION(), this.KEY_EPOCH(), this.SEQUENCE(), this.PRODUCED_AT(), this.EXPIRES_AT(), this.SUBJECT_ID(), this.bb.createObjList(this.FIELDS.bind(this), this.fieldsLength()), this.bb.createScalarList(this.PAYLOAD_HASH.bind(this), this.payloadHashLength()), this.bb.createScalarList(this.PREVIOUS_MESSAGE_HASH.bind(this), this.previousMessageHashLength()), this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength()));
    }
    unpackTo(_o) {
        _o.MESSAGE_ID = this.MESSAGE_ID();
        _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
        _o.LISTING_ID = this.LISTING_ID();
        _o.STREAM_ID = this.STREAM_ID();
        _o.SCHEMA_CODE = this.SCHEMA_CODE();
        _o.SCHEMA_HASH = this.bb.createScalarList(this.SCHEMA_HASH.bind(this), this.schemaHashLength());
        _o.POLICY_ID = this.POLICY_ID();
        _o.POLICY_VERSION = this.POLICY_VERSION();
        _o.KEY_EPOCH = this.KEY_EPOCH();
        _o.SEQUENCE = this.SEQUENCE();
        _o.PRODUCED_AT = this.PRODUCED_AT();
        _o.EXPIRES_AT = this.EXPIRES_AT();
        _o.SUBJECT_ID = this.SUBJECT_ID();
        _o.FIELDS = this.bb.createObjList(this.FIELDS.bind(this), this.fieldsLength());
        _o.PAYLOAD_HASH = this.bb.createScalarList(this.PAYLOAD_HASH.bind(this), this.payloadHashLength());
        _o.PREVIOUS_MESSAGE_HASH = this.bb.createScalarList(this.PREVIOUS_MESSAGE_HASH.bind(this), this.previousMessageHashLength());
        _o.PROVIDER_SIGNATURE = this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength());
    }
}
export class FSMT {
    constructor(MESSAGE_ID = null, PROVIDER_PEER_ID = null, LISTING_ID = null, STREAM_ID = null, SCHEMA_CODE = null, SCHEMA_HASH = [], POLICY_ID = null, POLICY_VERSION = 1, KEY_EPOCH = null, SEQUENCE = BigInt('0'), PRODUCED_AT = BigInt('0'), EXPIRES_AT = BigInt('0'), SUBJECT_ID = null, FIELDS = [], PAYLOAD_HASH = [], PREVIOUS_MESSAGE_HASH = [], PROVIDER_SIGNATURE = []) {
        this.MESSAGE_ID = MESSAGE_ID;
        this.PROVIDER_PEER_ID = PROVIDER_PEER_ID;
        this.LISTING_ID = LISTING_ID;
        this.STREAM_ID = STREAM_ID;
        this.SCHEMA_CODE = SCHEMA_CODE;
        this.SCHEMA_HASH = SCHEMA_HASH;
        this.POLICY_ID = POLICY_ID;
        this.POLICY_VERSION = POLICY_VERSION;
        this.KEY_EPOCH = KEY_EPOCH;
        this.SEQUENCE = SEQUENCE;
        this.PRODUCED_AT = PRODUCED_AT;
        this.EXPIRES_AT = EXPIRES_AT;
        this.SUBJECT_ID = SUBJECT_ID;
        this.FIELDS = FIELDS;
        this.PAYLOAD_HASH = PAYLOAD_HASH;
        this.PREVIOUS_MESSAGE_HASH = PREVIOUS_MESSAGE_HASH;
        this.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE;
    }
    pack(builder) {
        const MESSAGE_ID = (this.MESSAGE_ID !== null ? builder.createString(this.MESSAGE_ID) : 0);
        const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID) : 0);
        const LISTING_ID = (this.LISTING_ID !== null ? builder.createString(this.LISTING_ID) : 0);
        const STREAM_ID = (this.STREAM_ID !== null ? builder.createString(this.STREAM_ID) : 0);
        const SCHEMA_CODE = (this.SCHEMA_CODE !== null ? builder.createString(this.SCHEMA_CODE) : 0);
        const SCHEMA_HASH = FSM.createSchemaHashVector(builder, this.SCHEMA_HASH);
        const POLICY_ID = (this.POLICY_ID !== null ? builder.createString(this.POLICY_ID) : 0);
        const KEY_EPOCH = (this.KEY_EPOCH !== null ? builder.createString(this.KEY_EPOCH) : 0);
        const SUBJECT_ID = (this.SUBJECT_ID !== null ? builder.createString(this.SUBJECT_ID) : 0);
        const FIELDS = FSM.createFieldsVector(builder, builder.createObjectOffsetList(this.FIELDS));
        const PAYLOAD_HASH = FSM.createPayloadHashVector(builder, this.PAYLOAD_HASH);
        const PREVIOUS_MESSAGE_HASH = FSM.createPreviousMessageHashVector(builder, this.PREVIOUS_MESSAGE_HASH);
        const PROVIDER_SIGNATURE = FSM.createProviderSignatureVector(builder, this.PROVIDER_SIGNATURE);
        return FSM.createFSM(builder, MESSAGE_ID, PROVIDER_PEER_ID, LISTING_ID, STREAM_ID, SCHEMA_CODE, SCHEMA_HASH, POLICY_ID, this.POLICY_VERSION, KEY_EPOCH, this.SEQUENCE, this.PRODUCED_AT, this.EXPIRES_AT, SUBJECT_ID, FIELDS, PAYLOAD_HASH, PREVIOUS_MESSAGE_HASH, PROVIDER_SIGNATURE);
    }
}
