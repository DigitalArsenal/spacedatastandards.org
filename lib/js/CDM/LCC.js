// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { legacyCountryCode } from './legacyCountryCode.js';
/**
 * Legacy Country Code
 */
export class LCC {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsLCC(bb, obj) {
        return (obj || new LCC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsLCC(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new LCC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$LCC');
    }
    OWNER() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : legacyCountryCode.AB;
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * True while the code is in current use by the publishing catalogue.
     */
    ACTIVE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    SOURCE_URL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RETRIEVED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startLCC(builder) {
        builder.startObject(6);
    }
    static addOwner(builder, OWNER) {
        builder.addFieldInt8(0, OWNER, legacyCountryCode.AB);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(1, NAMEOffset, 0);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(2, DESCRIPTIONOffset, 0);
    }
    static addActive(builder, ACTIVE) {
        builder.addFieldInt8(3, +ACTIVE, +false);
    }
    static addSourceUrl(builder, SOURCE_URLOffset) {
        builder.addFieldOffset(4, SOURCE_URLOffset, 0);
    }
    static addRetrievedAt(builder, RETRIEVED_ATOffset) {
        builder.addFieldOffset(5, RETRIEVED_ATOffset, 0);
    }
    static endLCC(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishLCCBuffer(builder, offset) {
        builder.finish(offset, '$LCC');
    }
    static finishSizePrefixedLCCBuffer(builder, offset) {
        builder.finish(offset, '$LCC', true);
    }
    static createLCC(builder, OWNER, NAMEOffset, DESCRIPTIONOffset, ACTIVE, SOURCE_URLOffset, RETRIEVED_ATOffset) {
        LCC.startLCC(builder);
        LCC.addOwner(builder, OWNER);
        LCC.addName(builder, NAMEOffset);
        LCC.addDescription(builder, DESCRIPTIONOffset);
        LCC.addActive(builder, ACTIVE);
        LCC.addSourceUrl(builder, SOURCE_URLOffset);
        LCC.addRetrievedAt(builder, RETRIEVED_ATOffset);
        return LCC.endLCC(builder);
    }
    unpack() {
        return new LCCT(this.OWNER(), this.NAME(), this.DESCRIPTION(), this.ACTIVE(), this.SOURCE_URL(), this.RETRIEVED_AT());
    }
    unpackTo(_o) {
        _o.OWNER = this.OWNER();
        _o.NAME = this.NAME();
        _o.DESCRIPTION = this.DESCRIPTION();
        _o.ACTIVE = this.ACTIVE();
        _o.SOURCE_URL = this.SOURCE_URL();
        _o.RETRIEVED_AT = this.RETRIEVED_AT();
    }
}
export class LCCT {
    constructor(OWNER = legacyCountryCode.AB, NAME = null, DESCRIPTION = null, ACTIVE = false, SOURCE_URL = null, RETRIEVED_AT = null) {
        this.OWNER = OWNER;
        this.NAME = NAME;
        this.DESCRIPTION = DESCRIPTION;
        this.ACTIVE = ACTIVE;
        this.SOURCE_URL = SOURCE_URL;
        this.RETRIEVED_AT = RETRIEVED_AT;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        const SOURCE_URL = (this.SOURCE_URL !== null ? builder.createString(this.SOURCE_URL) : 0);
        const RETRIEVED_AT = (this.RETRIEVED_AT !== null ? builder.createString(this.RETRIEVED_AT) : 0);
        return LCC.createLCC(builder, this.OWNER, NAME, DESCRIPTION, this.ACTIVE, SOURCE_URL, RETRIEVED_AT);
    }
}
