// automatically generated by the FlatBuffers compiler, do not modify
import * as flatbuffers from 'flatbuffers';
/**
 * IPFS CID and Account Identifier
 */
export class IPFS_CID_ADDRESS {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsIPFS_CID_ADDRESS(bb, obj) {
        return (obj || new IPFS_CID_ADDRESS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsIPFS_CID_ADDRESS(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new IPFS_CID_ADDRESS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    IPFS_CID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    KEY_ADDRESS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startIPFS_CID_ADDRESS(builder) {
        builder.startObject(2);
    }
    static addIpfsCid(builder, IPFS_CIDOffset) {
        builder.addFieldOffset(0, IPFS_CIDOffset, 0);
    }
    static addKeyAddress(builder, KEY_ADDRESSOffset) {
        builder.addFieldOffset(1, KEY_ADDRESSOffset, 0);
    }
    static endIPFS_CID_ADDRESS(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createIPFS_CID_ADDRESS(builder, IPFS_CIDOffset, KEY_ADDRESSOffset) {
        IPFS_CID_ADDRESS.startIPFS_CID_ADDRESS(builder);
        IPFS_CID_ADDRESS.addIpfsCid(builder, IPFS_CIDOffset);
        IPFS_CID_ADDRESS.addKeyAddress(builder, KEY_ADDRESSOffset);
        return IPFS_CID_ADDRESS.endIPFS_CID_ADDRESS(builder);
    }
    unpack() {
        return new IPFS_CID_ADDRESST(this.IPFS_CID(), this.KEY_ADDRESS());
    }
    unpackTo(_o) {
        _o.IPFS_CID = this.IPFS_CID();
        _o.KEY_ADDRESS = this.KEY_ADDRESS();
    }
}
export class IPFS_CID_ADDRESST {
    constructor(IPFS_CID = null, KEY_ADDRESS = null) {
        this.IPFS_CID = IPFS_CID;
        this.KEY_ADDRESS = KEY_ADDRESS;
    }
    pack(builder) {
        const IPFS_CID = (this.IPFS_CID !== null ? builder.createString(this.IPFS_CID) : 0);
        const KEY_ADDRESS = (this.KEY_ADDRESS !== null ? builder.createString(this.KEY_ADDRESS) : 0);
        return IPFS_CID_ADDRESS.createIPFS_CID_ADDRESS(builder, IPFS_CID, KEY_ADDRESS);
    }
}
