// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { BPFAttestation } from './BPFAttestation.js';
import { BPFModule } from './BPFModule.js';
import { BPFPart } from './BPFPart.js';
import { BPFRuntimeLock } from './BPFRuntimeLock.js';
import { bpfLicenseMode } from './bpfLicenseMode.js';
/**
 * Build Profile — one saved, portable build configuration.
 *
 * PORTABILITY. The export unit is a `$REC` collection carrying this record
 * and, optionally, an `$APP` manifest that names it through an `APPDataRef`
 * with `SDS_TYPE: "BPF"`. The `$APP` wrapper is launcher metadata and
 * carries no authority; all authority over a profile lives in
 * `ATTESTATION`. A bare size-prefixed `$BPF` buffer is also a complete,
 * valid export.
 */
export class BPF {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsBPF(bb, obj) {
        return (obj || new BPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsBPF(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new BPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$BPF');
    }
    PROFILE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CREATED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    UPDATED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TEMPLATE_SHA256(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    /**
     * Template slots and their inclusion decisions. A slot the template
     * exposes and this vector omits is NOT included.
     */
    PARTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new BPFPart()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    partsLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Modules selected into the build and their protection tiers.
     */
    MODULES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new BPFModule()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    modulesLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * The runtime restriction the produced artifact enforces. Required, so
     * that an unlocked build is a stated configuration rather than a missing
     * one.
     */
    RUNTIME_LOCK(obj) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? (obj || new BPFRuntimeLock()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Distribution-control mode of the produced artifact.
     */
    LICENSE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : bpfLicenseMode.UNSPECIFIED;
    }
    /**
     * Publisher attestation. Absent means the profile is unsigned; see
     * `BPFAttestation` for the fail-closed rule when it is present.
     */
    ATTESTATION(obj) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? (obj || new BPFAttestation()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startBPF(builder) {
        builder.startObject(11);
    }
    static addProfileId(builder, PROFILE_IDOffset) {
        builder.addFieldOffset(0, PROFILE_IDOffset, 0);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(1, NAMEOffset, 0);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(2, DESCRIPTIONOffset, 0);
    }
    static addCreatedAt(builder, CREATED_ATOffset) {
        builder.addFieldOffset(3, CREATED_ATOffset, 0);
    }
    static addUpdatedAt(builder, UPDATED_ATOffset) {
        builder.addFieldOffset(4, UPDATED_ATOffset, 0);
    }
    static addTemplateSha256(builder, TEMPLATE_SHA256Offset) {
        builder.addFieldOffset(5, TEMPLATE_SHA256Offset, 0);
    }
    static addParts(builder, PARTSOffset) {
        builder.addFieldOffset(6, PARTSOffset, 0);
    }
    static createPartsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPartsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addModules(builder, MODULESOffset) {
        builder.addFieldOffset(7, MODULESOffset, 0);
    }
    static createModulesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startModulesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addRuntimeLock(builder, RUNTIME_LOCKOffset) {
        builder.addFieldOffset(8, RUNTIME_LOCKOffset, 0);
    }
    static addLicenseMode(builder, LICENSE_MODE) {
        builder.addFieldInt8(9, LICENSE_MODE, bpfLicenseMode.UNSPECIFIED);
    }
    static addAttestation(builder, ATTESTATIONOffset) {
        builder.addFieldOffset(10, ATTESTATIONOffset, 0);
    }
    static endBPF(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // PROFILE_ID
        builder.requiredField(offset, 6); // NAME
        builder.requiredField(offset, 14); // TEMPLATE_SHA256
        builder.requiredField(offset, 20); // RUNTIME_LOCK
        return offset;
    }
    static finishBPFBuffer(builder, offset) {
        builder.finish(offset, '$BPF');
    }
    static finishSizePrefixedBPFBuffer(builder, offset) {
        builder.finish(offset, '$BPF', true);
    }
    unpack() {
        return new BPFT(this.PROFILE_ID(), this.NAME(), this.DESCRIPTION(), this.CREATED_AT(), this.UPDATED_AT(), this.TEMPLATE_SHA256(), this.bb.createObjList(this.PARTS.bind(this), this.partsLength()), this.bb.createObjList(this.MODULES.bind(this), this.modulesLength()), (this.RUNTIME_LOCK() !== null ? this.RUNTIME_LOCK().unpack() : null), this.LICENSE_MODE(), (this.ATTESTATION() !== null ? this.ATTESTATION().unpack() : null));
    }
    unpackTo(_o) {
        _o.PROFILE_ID = this.PROFILE_ID();
        _o.NAME = this.NAME();
        _o.DESCRIPTION = this.DESCRIPTION();
        _o.CREATED_AT = this.CREATED_AT();
        _o.UPDATED_AT = this.UPDATED_AT();
        _o.TEMPLATE_SHA256 = this.TEMPLATE_SHA256();
        _o.PARTS = this.bb.createObjList(this.PARTS.bind(this), this.partsLength());
        _o.MODULES = this.bb.createObjList(this.MODULES.bind(this), this.modulesLength());
        _o.RUNTIME_LOCK = (this.RUNTIME_LOCK() !== null ? this.RUNTIME_LOCK().unpack() : null);
        _o.LICENSE_MODE = this.LICENSE_MODE();
        _o.ATTESTATION = (this.ATTESTATION() !== null ? this.ATTESTATION().unpack() : null);
    }
}
export class BPFT {
    constructor(PROFILE_ID = null, NAME = null, DESCRIPTION = null, CREATED_AT = null, UPDATED_AT = null, TEMPLATE_SHA256 = null, PARTS = [], MODULES = [], RUNTIME_LOCK = null, LICENSE_MODE = bpfLicenseMode.UNSPECIFIED, ATTESTATION = null) {
        this.PROFILE_ID = PROFILE_ID;
        this.NAME = NAME;
        this.DESCRIPTION = DESCRIPTION;
        this.CREATED_AT = CREATED_AT;
        this.UPDATED_AT = UPDATED_AT;
        this.TEMPLATE_SHA256 = TEMPLATE_SHA256;
        this.PARTS = PARTS;
        this.MODULES = MODULES;
        this.RUNTIME_LOCK = RUNTIME_LOCK;
        this.LICENSE_MODE = LICENSE_MODE;
        this.ATTESTATION = ATTESTATION;
    }
    pack(builder) {
        const PROFILE_ID = (this.PROFILE_ID !== null ? builder.createString(this.PROFILE_ID) : 0);
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        const CREATED_AT = (this.CREATED_AT !== null ? builder.createString(this.CREATED_AT) : 0);
        const UPDATED_AT = (this.UPDATED_AT !== null ? builder.createString(this.UPDATED_AT) : 0);
        const TEMPLATE_SHA256 = (this.TEMPLATE_SHA256 !== null ? builder.createString(this.TEMPLATE_SHA256) : 0);
        const PARTS = BPF.createPartsVector(builder, builder.createObjectOffsetList(this.PARTS));
        const MODULES = BPF.createModulesVector(builder, builder.createObjectOffsetList(this.MODULES));
        const RUNTIME_LOCK = (this.RUNTIME_LOCK !== null ? this.RUNTIME_LOCK.pack(builder) : 0);
        const ATTESTATION = (this.ATTESTATION !== null ? this.ATTESTATION.pack(builder) : 0);
        BPF.startBPF(builder);
        BPF.addProfileId(builder, PROFILE_ID);
        BPF.addName(builder, NAME);
        BPF.addDescription(builder, DESCRIPTION);
        BPF.addCreatedAt(builder, CREATED_AT);
        BPF.addUpdatedAt(builder, UPDATED_AT);
        BPF.addTemplateSha256(builder, TEMPLATE_SHA256);
        BPF.addParts(builder, PARTS);
        BPF.addModules(builder, MODULES);
        BPF.addRuntimeLock(builder, RUNTIME_LOCK);
        BPF.addLicenseMode(builder, this.LICENSE_MODE);
        BPF.addAttestation(builder, ATTESTATION);
        return BPF.endBPF(builder);
    }
}
