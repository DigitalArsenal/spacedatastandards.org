// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Electronic Warfare
 */
export class EWR {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsEWR(bb, obj) {
        return (obj || new EWR()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsEWR(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new EWR()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$EWR');
    }
    SYSTEM_ID() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    ENTITY_ID() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    ESM_ACTIVE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    ECM_ACTIVE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    RWR_ACTIVE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    ESM_CONTACTS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    esmContactsLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ACTIVE_JAMMING(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    activeJammingLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    COUNTERMEASURES(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    THREAT_WARNINGS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    threatWarningsLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    EMCON_LEVEL() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    LAST_UPDATE_MS() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    static startEWR(builder) {
        builder.startObject(11);
    }
    static addSystemId(builder, SYSTEM_ID) {
        builder.addFieldInt32(0, SYSTEM_ID, 0);
    }
    static addEntityId(builder, ENTITY_ID) {
        builder.addFieldInt32(1, ENTITY_ID, 0);
    }
    static addEsmActive(builder, ESM_ACTIVE) {
        builder.addFieldInt8(2, +ESM_ACTIVE, +false);
    }
    static addEcmActive(builder, ECM_ACTIVE) {
        builder.addFieldInt8(3, +ECM_ACTIVE, +false);
    }
    static addRwrActive(builder, RWR_ACTIVE) {
        builder.addFieldInt8(4, +RWR_ACTIVE, +false);
    }
    static addEsmContacts(builder, ESM_CONTACTSOffset) {
        builder.addFieldOffset(5, ESM_CONTACTSOffset, 0);
    }
    static createEsmContactsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startEsmContactsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addActiveJamming(builder, ACTIVE_JAMMINGOffset) {
        builder.addFieldOffset(6, ACTIVE_JAMMINGOffset, 0);
    }
    static createActiveJammingVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startActiveJammingVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCountermeasures(builder, COUNTERMEASURESOffset) {
        builder.addFieldOffset(7, COUNTERMEASURESOffset, 0);
    }
    static addThreatWarnings(builder, THREAT_WARNINGSOffset) {
        builder.addFieldOffset(8, THREAT_WARNINGSOffset, 0);
    }
    static createThreatWarningsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startThreatWarningsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addEmconLevel(builder, EMCON_LEVEL) {
        builder.addFieldInt8(9, EMCON_LEVEL, 0);
    }
    static addLastUpdateMs(builder, LAST_UPDATE_MS) {
        builder.addFieldInt64(10, LAST_UPDATE_MS, BigInt('0'));
    }
    static endEWR(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishEWRBuffer(builder, offset) {
        builder.finish(offset, '$EWR');
    }
    static finishSizePrefixedEWRBuffer(builder, offset) {
        builder.finish(offset, '$EWR', true);
    }
    static createEWR(builder, SYSTEM_ID, ENTITY_ID, ESM_ACTIVE, ECM_ACTIVE, RWR_ACTIVE, ESM_CONTACTSOffset, ACTIVE_JAMMINGOffset, COUNTERMEASURESOffset, THREAT_WARNINGSOffset, EMCON_LEVEL, LAST_UPDATE_MS) {
        EWR.startEWR(builder);
        EWR.addSystemId(builder, SYSTEM_ID);
        EWR.addEntityId(builder, ENTITY_ID);
        EWR.addEsmActive(builder, ESM_ACTIVE);
        EWR.addEcmActive(builder, ECM_ACTIVE);
        EWR.addRwrActive(builder, RWR_ACTIVE);
        EWR.addEsmContacts(builder, ESM_CONTACTSOffset);
        EWR.addActiveJamming(builder, ACTIVE_JAMMINGOffset);
        EWR.addCountermeasures(builder, COUNTERMEASURESOffset);
        EWR.addThreatWarnings(builder, THREAT_WARNINGSOffset);
        EWR.addEmconLevel(builder, EMCON_LEVEL);
        EWR.addLastUpdateMs(builder, LAST_UPDATE_MS);
        return EWR.endEWR(builder);
    }
    unpack() {
        return new EWRT(this.SYSTEM_ID(), this.ENTITY_ID(), this.ESM_ACTIVE(), this.ECM_ACTIVE(), this.RWR_ACTIVE(), this.bb.createScalarList(this.ESM_CONTACTS.bind(this), this.esmContactsLength()), this.bb.createScalarList(this.ACTIVE_JAMMING.bind(this), this.activeJammingLength()), this.COUNTERMEASURES(), this.bb.createScalarList(this.THREAT_WARNINGS.bind(this), this.threatWarningsLength()), this.EMCON_LEVEL(), this.LAST_UPDATE_MS());
    }
    unpackTo(_o) {
        _o.SYSTEM_ID = this.SYSTEM_ID();
        _o.ENTITY_ID = this.ENTITY_ID();
        _o.ESM_ACTIVE = this.ESM_ACTIVE();
        _o.ECM_ACTIVE = this.ECM_ACTIVE();
        _o.RWR_ACTIVE = this.RWR_ACTIVE();
        _o.ESM_CONTACTS = this.bb.createScalarList(this.ESM_CONTACTS.bind(this), this.esmContactsLength());
        _o.ACTIVE_JAMMING = this.bb.createScalarList(this.ACTIVE_JAMMING.bind(this), this.activeJammingLength());
        _o.COUNTERMEASURES = this.COUNTERMEASURES();
        _o.THREAT_WARNINGS = this.bb.createScalarList(this.THREAT_WARNINGS.bind(this), this.threatWarningsLength());
        _o.EMCON_LEVEL = this.EMCON_LEVEL();
        _o.LAST_UPDATE_MS = this.LAST_UPDATE_MS();
    }
}
export class EWRT {
    constructor(SYSTEM_ID = 0, ENTITY_ID = 0, ESM_ACTIVE = false, ECM_ACTIVE = false, RWR_ACTIVE = false, ESM_CONTACTS = [], ACTIVE_JAMMING = [], COUNTERMEASURES = null, THREAT_WARNINGS = [], EMCON_LEVEL = 0, LAST_UPDATE_MS = BigInt('0')) {
        this.SYSTEM_ID = SYSTEM_ID;
        this.ENTITY_ID = ENTITY_ID;
        this.ESM_ACTIVE = ESM_ACTIVE;
        this.ECM_ACTIVE = ECM_ACTIVE;
        this.RWR_ACTIVE = RWR_ACTIVE;
        this.ESM_CONTACTS = ESM_CONTACTS;
        this.ACTIVE_JAMMING = ACTIVE_JAMMING;
        this.COUNTERMEASURES = COUNTERMEASURES;
        this.THREAT_WARNINGS = THREAT_WARNINGS;
        this.EMCON_LEVEL = EMCON_LEVEL;
        this.LAST_UPDATE_MS = LAST_UPDATE_MS;
    }
    pack(builder) {
        const ESM_CONTACTS = EWR.createEsmContactsVector(builder, builder.createObjectOffsetList(this.ESM_CONTACTS));
        const ACTIVE_JAMMING = EWR.createActiveJammingVector(builder, builder.createObjectOffsetList(this.ACTIVE_JAMMING));
        const COUNTERMEASURES = (this.COUNTERMEASURES !== null ? builder.createString(this.COUNTERMEASURES) : 0);
        const THREAT_WARNINGS = EWR.createThreatWarningsVector(builder, builder.createObjectOffsetList(this.THREAT_WARNINGS));
        return EWR.createEWR(builder, this.SYSTEM_ID, this.ENTITY_ID, this.ESM_ACTIVE, this.ECM_ACTIVE, this.RWR_ACTIVE, ESM_CONTACTS, ACTIVE_JAMMING, COUNTERMEASURES, THREAT_WARNINGS, this.EMCON_LEVEL, this.LAST_UPDATE_MS);
    }
}
