// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Where a block of bytes lives and how it is verified. The payload lane is
 * CID-first: a tile record addresses its bytes so an epoch can be verified
 * rather than trusted, and carries them inline only when the block is small
 * enough that a second fetch costs more than it saves. Exactly one of CID or
 * BYTES is the authority for a given ref; when both are present the bytes
 * MUST hash to the CID.
 */
export class DTTPayloadRef {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsDTTPayloadRef(bb, obj) {
        return (obj || new DTTPayloadRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsDTTPayloadRef(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new DTTPayloadRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    CID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * The block inline. Reserved for small blocks; a tile pyramid is
     * distributed by CID, never by embedding megabytes per record.
     */
    BYTES(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    bytesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    bytesArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Length of the block in bytes, whether inline or referenced, so a
     * consumer can budget a fetch before making it.
     */
    SIZE_BYTES() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    DIGEST(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CONTENT_ENCODING(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MEDIA_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startDTTPayloadRef(builder) {
        builder.startObject(6);
    }
    static addCid(builder, CIDOffset) {
        builder.addFieldOffset(0, CIDOffset, 0);
    }
    static addBytes(builder, BYTESOffset) {
        builder.addFieldOffset(1, BYTESOffset, 0);
    }
    static createBytesVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startBytesVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addSizeBytes(builder, SIZE_BYTES) {
        builder.addFieldInt64(2, SIZE_BYTES, BigInt('0'));
    }
    static addDigest(builder, DIGESTOffset) {
        builder.addFieldOffset(3, DIGESTOffset, 0);
    }
    static addContentEncoding(builder, CONTENT_ENCODINGOffset) {
        builder.addFieldOffset(4, CONTENT_ENCODINGOffset, 0);
    }
    static addMediaType(builder, MEDIA_TYPEOffset) {
        builder.addFieldOffset(5, MEDIA_TYPEOffset, 0);
    }
    static endDTTPayloadRef(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createDTTPayloadRef(builder, CIDOffset, BYTESOffset, SIZE_BYTES, DIGESTOffset, CONTENT_ENCODINGOffset, MEDIA_TYPEOffset) {
        DTTPayloadRef.startDTTPayloadRef(builder);
        DTTPayloadRef.addCid(builder, CIDOffset);
        DTTPayloadRef.addBytes(builder, BYTESOffset);
        DTTPayloadRef.addSizeBytes(builder, SIZE_BYTES);
        DTTPayloadRef.addDigest(builder, DIGESTOffset);
        DTTPayloadRef.addContentEncoding(builder, CONTENT_ENCODINGOffset);
        DTTPayloadRef.addMediaType(builder, MEDIA_TYPEOffset);
        return DTTPayloadRef.endDTTPayloadRef(builder);
    }
    unpack() {
        return new DTTPayloadRefT(this.CID(), this.bb.createScalarList(this.BYTES.bind(this), this.bytesLength()), this.SIZE_BYTES(), this.DIGEST(), this.CONTENT_ENCODING(), this.MEDIA_TYPE());
    }
    unpackTo(_o) {
        _o.CID = this.CID();
        _o.BYTES = this.bb.createScalarList(this.BYTES.bind(this), this.bytesLength());
        _o.SIZE_BYTES = this.SIZE_BYTES();
        _o.DIGEST = this.DIGEST();
        _o.CONTENT_ENCODING = this.CONTENT_ENCODING();
        _o.MEDIA_TYPE = this.MEDIA_TYPE();
    }
}
export class DTTPayloadRefT {
    constructor(CID = null, BYTES = [], SIZE_BYTES = BigInt('0'), DIGEST = null, CONTENT_ENCODING = null, MEDIA_TYPE = null) {
        this.CID = CID;
        this.BYTES = BYTES;
        this.SIZE_BYTES = SIZE_BYTES;
        this.DIGEST = DIGEST;
        this.CONTENT_ENCODING = CONTENT_ENCODING;
        this.MEDIA_TYPE = MEDIA_TYPE;
    }
    pack(builder) {
        const CID = (this.CID !== null ? builder.createString(this.CID) : 0);
        const BYTES = DTTPayloadRef.createBytesVector(builder, this.BYTES);
        const DIGEST = (this.DIGEST !== null ? builder.createString(this.DIGEST) : 0);
        const CONTENT_ENCODING = (this.CONTENT_ENCODING !== null ? builder.createString(this.CONTENT_ENCODING) : 0);
        const MEDIA_TYPE = (this.MEDIA_TYPE !== null ? builder.createString(this.MEDIA_TYPE) : 0);
        return DTTPayloadRef.createDTTPayloadRef(builder, CID, BYTES, this.SIZE_BYTES, DIGEST, CONTENT_ENCODING, MEDIA_TYPE);
    }
}
