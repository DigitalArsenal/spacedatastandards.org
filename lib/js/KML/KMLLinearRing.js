// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLCoordinate } from './KMLCoordinate.js';
/**
 * LinearRing geometry
 */
export class KMLLinearRing {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLLinearRing(bb, obj) {
        return (obj || new KMLLinearRing()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLLinearRing(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLLinearRing()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Coordinates (first = last to close the ring)
     */
    COORDINATES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new KMLCoordinate()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    coordinatesLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Whether to extrude to ground
     */
    EXTRUDE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Whether to tessellate
     */
    TESSELLATE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Altitude mode
     */
    ALTITUDE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
    }
    static startKMLLinearRing(builder) {
        builder.startObject(4);
    }
    static addCoordinates(builder, COORDINATESOffset) {
        builder.addFieldOffset(0, COORDINATESOffset, 0);
    }
    static createCoordinatesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startCoordinatesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addExtrude(builder, EXTRUDE) {
        builder.addFieldInt8(1, +EXTRUDE, +false);
    }
    static addTessellate(builder, TESSELLATE) {
        builder.addFieldInt8(2, +TESSELLATE, +false);
    }
    static addAltitudeMode(builder, ALTITUDE_MODE) {
        builder.addFieldInt8(3, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
    }
    static endKMLLinearRing(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLLinearRing(builder, COORDINATESOffset, EXTRUDE, TESSELLATE, ALTITUDE_MODE) {
        KMLLinearRing.startKMLLinearRing(builder);
        KMLLinearRing.addCoordinates(builder, COORDINATESOffset);
        KMLLinearRing.addExtrude(builder, EXTRUDE);
        KMLLinearRing.addTessellate(builder, TESSELLATE);
        KMLLinearRing.addAltitudeMode(builder, ALTITUDE_MODE);
        return KMLLinearRing.endKMLLinearRing(builder);
    }
    unpack() {
        return new KMLLinearRingT(this.bb.createObjList(this.COORDINATES.bind(this), this.coordinatesLength()), this.EXTRUDE(), this.TESSELLATE(), this.ALTITUDE_MODE());
    }
    unpackTo(_o) {
        _o.COORDINATES = this.bb.createObjList(this.COORDINATES.bind(this), this.coordinatesLength());
        _o.EXTRUDE = this.EXTRUDE();
        _o.TESSELLATE = this.TESSELLATE();
        _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
    }
}
export class KMLLinearRingT {
    constructor(COORDINATES = [], EXTRUDE = false, TESSELLATE = false, ALTITUDE_MODE = KMLAltitudeMode.CLAMP_TO_GROUND) {
        this.COORDINATES = COORDINATES;
        this.EXTRUDE = EXTRUDE;
        this.TESSELLATE = TESSELLATE;
        this.ALTITUDE_MODE = ALTITUDE_MODE;
    }
    pack(builder) {
        const COORDINATES = KMLLinearRing.createCoordinatesVector(builder, builder.createObjectOffsetList(this.COORDINATES));
        return KMLLinearRing.createKMLLinearRing(builder, COORDINATES, this.EXTRUDE, this.TESSELLATE, this.ALTITUDE_MODE);
    }
}
