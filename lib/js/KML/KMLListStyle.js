// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLItemIcon } from './KMLItemIcon.js';
import { KMLListItemType } from './KMLListItemType.js';
/**
 * List style
 */
export class KMLListStyle {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLListStyle(bb, obj) {
        return (obj || new KMLListStyle()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLListStyle(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLListStyle()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * List item type
     */
    LIST_ITEM_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLListItemType.CHECK;
    }
    BG_COLOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Item icons
     */
    ITEM_ICONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new KMLItemIcon()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    itemIconsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Maximum snippet lines
     */
    MAX_SNIPPET_LINES() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    static startKMLListStyle(builder) {
        builder.startObject(4);
    }
    static addListItemType(builder, LIST_ITEM_TYPE) {
        builder.addFieldInt8(0, LIST_ITEM_TYPE, KMLListItemType.CHECK);
    }
    static addBgColor(builder, BG_COLOROffset) {
        builder.addFieldOffset(1, BG_COLOROffset, 0);
    }
    static addItemIcons(builder, ITEM_ICONSOffset) {
        builder.addFieldOffset(2, ITEM_ICONSOffset, 0);
    }
    static createItemIconsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startItemIconsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addMaxSnippetLines(builder, MAX_SNIPPET_LINES) {
        builder.addFieldInt32(3, MAX_SNIPPET_LINES, 0);
    }
    static endKMLListStyle(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLListStyle(builder, LIST_ITEM_TYPE, BG_COLOROffset, ITEM_ICONSOffset, MAX_SNIPPET_LINES) {
        KMLListStyle.startKMLListStyle(builder);
        KMLListStyle.addListItemType(builder, LIST_ITEM_TYPE);
        KMLListStyle.addBgColor(builder, BG_COLOROffset);
        KMLListStyle.addItemIcons(builder, ITEM_ICONSOffset);
        KMLListStyle.addMaxSnippetLines(builder, MAX_SNIPPET_LINES);
        return KMLListStyle.endKMLListStyle(builder);
    }
    unpack() {
        return new KMLListStyleT(this.LIST_ITEM_TYPE(), this.BG_COLOR(), this.bb.createObjList(this.ITEM_ICONS.bind(this), this.itemIconsLength()), this.MAX_SNIPPET_LINES());
    }
    unpackTo(_o) {
        _o.LIST_ITEM_TYPE = this.LIST_ITEM_TYPE();
        _o.BG_COLOR = this.BG_COLOR();
        _o.ITEM_ICONS = this.bb.createObjList(this.ITEM_ICONS.bind(this), this.itemIconsLength());
        _o.MAX_SNIPPET_LINES = this.MAX_SNIPPET_LINES();
    }
}
export class KMLListStyleT {
    constructor(LIST_ITEM_TYPE = KMLListItemType.CHECK, BG_COLOR = null, ITEM_ICONS = [], MAX_SNIPPET_LINES = 0) {
        this.LIST_ITEM_TYPE = LIST_ITEM_TYPE;
        this.BG_COLOR = BG_COLOR;
        this.ITEM_ICONS = ITEM_ICONS;
        this.MAX_SNIPPET_LINES = MAX_SNIPPET_LINES;
    }
    pack(builder) {
        const BG_COLOR = (this.BG_COLOR !== null ? builder.createString(this.BG_COLOR) : 0);
        const ITEM_ICONS = KMLListStyle.createItemIconsVector(builder, builder.createObjectOffsetList(this.ITEM_ICONS));
        return KMLListStyle.createKMLListStyle(builder, this.LIST_ITEM_TYPE, BG_COLOR, ITEM_ICONS, this.MAX_SNIPPET_LINES);
    }
}
