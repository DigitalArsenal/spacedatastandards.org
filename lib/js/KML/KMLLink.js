// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLRefreshMode } from './KMLRefreshMode.js';
import { KMLViewRefreshMode } from './KMLViewRefreshMode.js';
/**
 * Full Link element
 */
export class KMLLink {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLLink(bb, obj) {
        return (obj || new KMLLink()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLLink(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLLink()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    HREF(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Refresh mode
     */
    REFRESH_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLRefreshMode.ON_CHANGE;
    }
    /**
     * Refresh interval in seconds
     */
    REFRESH_INTERVAL() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * View refresh mode
     */
    VIEW_REFRESH_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLViewRefreshMode.NEVER;
    }
    /**
     * View refresh time in seconds
     */
    VIEW_REFRESH_TIME() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * View bound scale
     */
    VIEW_BOUND_SCALE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    VIEW_FORMAT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    HTTP_QUERY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startKMLLink(builder) {
        builder.startObject(8);
    }
    static addHref(builder, HREFOffset) {
        builder.addFieldOffset(0, HREFOffset, 0);
    }
    static addRefreshMode(builder, REFRESH_MODE) {
        builder.addFieldInt8(1, REFRESH_MODE, KMLRefreshMode.ON_CHANGE);
    }
    static addRefreshInterval(builder, REFRESH_INTERVAL) {
        builder.addFieldFloat64(2, REFRESH_INTERVAL, 0.0);
    }
    static addViewRefreshMode(builder, VIEW_REFRESH_MODE) {
        builder.addFieldInt8(3, VIEW_REFRESH_MODE, KMLViewRefreshMode.NEVER);
    }
    static addViewRefreshTime(builder, VIEW_REFRESH_TIME) {
        builder.addFieldFloat64(4, VIEW_REFRESH_TIME, 0.0);
    }
    static addViewBoundScale(builder, VIEW_BOUND_SCALE) {
        builder.addFieldFloat64(5, VIEW_BOUND_SCALE, 0.0);
    }
    static addViewFormat(builder, VIEW_FORMATOffset) {
        builder.addFieldOffset(6, VIEW_FORMATOffset, 0);
    }
    static addHttpQuery(builder, HTTP_QUERYOffset) {
        builder.addFieldOffset(7, HTTP_QUERYOffset, 0);
    }
    static endKMLLink(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLLink(builder, HREFOffset, REFRESH_MODE, REFRESH_INTERVAL, VIEW_REFRESH_MODE, VIEW_REFRESH_TIME, VIEW_BOUND_SCALE, VIEW_FORMATOffset, HTTP_QUERYOffset) {
        KMLLink.startKMLLink(builder);
        KMLLink.addHref(builder, HREFOffset);
        KMLLink.addRefreshMode(builder, REFRESH_MODE);
        KMLLink.addRefreshInterval(builder, REFRESH_INTERVAL);
        KMLLink.addViewRefreshMode(builder, VIEW_REFRESH_MODE);
        KMLLink.addViewRefreshTime(builder, VIEW_REFRESH_TIME);
        KMLLink.addViewBoundScale(builder, VIEW_BOUND_SCALE);
        KMLLink.addViewFormat(builder, VIEW_FORMATOffset);
        KMLLink.addHttpQuery(builder, HTTP_QUERYOffset);
        return KMLLink.endKMLLink(builder);
    }
    unpack() {
        return new KMLLinkT(this.HREF(), this.REFRESH_MODE(), this.REFRESH_INTERVAL(), this.VIEW_REFRESH_MODE(), this.VIEW_REFRESH_TIME(), this.VIEW_BOUND_SCALE(), this.VIEW_FORMAT(), this.HTTP_QUERY());
    }
    unpackTo(_o) {
        _o.HREF = this.HREF();
        _o.REFRESH_MODE = this.REFRESH_MODE();
        _o.REFRESH_INTERVAL = this.REFRESH_INTERVAL();
        _o.VIEW_REFRESH_MODE = this.VIEW_REFRESH_MODE();
        _o.VIEW_REFRESH_TIME = this.VIEW_REFRESH_TIME();
        _o.VIEW_BOUND_SCALE = this.VIEW_BOUND_SCALE();
        _o.VIEW_FORMAT = this.VIEW_FORMAT();
        _o.HTTP_QUERY = this.HTTP_QUERY();
    }
}
export class KMLLinkT {
    constructor(HREF = null, REFRESH_MODE = KMLRefreshMode.ON_CHANGE, REFRESH_INTERVAL = 0.0, VIEW_REFRESH_MODE = KMLViewRefreshMode.NEVER, VIEW_REFRESH_TIME = 0.0, VIEW_BOUND_SCALE = 0.0, VIEW_FORMAT = null, HTTP_QUERY = null) {
        this.HREF = HREF;
        this.REFRESH_MODE = REFRESH_MODE;
        this.REFRESH_INTERVAL = REFRESH_INTERVAL;
        this.VIEW_REFRESH_MODE = VIEW_REFRESH_MODE;
        this.VIEW_REFRESH_TIME = VIEW_REFRESH_TIME;
        this.VIEW_BOUND_SCALE = VIEW_BOUND_SCALE;
        this.VIEW_FORMAT = VIEW_FORMAT;
        this.HTTP_QUERY = HTTP_QUERY;
    }
    pack(builder) {
        const HREF = (this.HREF !== null ? builder.createString(this.HREF) : 0);
        const VIEW_FORMAT = (this.VIEW_FORMAT !== null ? builder.createString(this.VIEW_FORMAT) : 0);
        const HTTP_QUERY = (this.HTTP_QUERY !== null ? builder.createString(this.HTTP_QUERY) : 0);
        return KMLLink.createKMLLink(builder, HREF, this.REFRESH_MODE, this.REFRESH_INTERVAL, this.VIEW_REFRESH_MODE, this.VIEW_REFRESH_TIME, this.VIEW_BOUND_SCALE, VIEW_FORMAT, HTTP_QUERY);
    }
}
