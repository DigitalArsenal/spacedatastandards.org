// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Level of Detail parameters
 */
export class KMLLod {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLLod(bb, obj) {
        return (obj || new KMLLod()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLLod(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLLod()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Minimum LOD pixels
     */
    MIN_LOD_PIXELS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum LOD pixels (-1 = infinite)
     */
    MAX_LOD_PIXELS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Minimum fade extent
     */
    MIN_FADE_EXTENT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum fade extent
     */
    MAX_FADE_EXTENT() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startKMLLod(builder) {
        builder.startObject(4);
    }
    static addMinLodPixels(builder, MIN_LOD_PIXELS) {
        builder.addFieldFloat64(0, MIN_LOD_PIXELS, 0.0);
    }
    static addMaxLodPixels(builder, MAX_LOD_PIXELS) {
        builder.addFieldFloat64(1, MAX_LOD_PIXELS, 0.0);
    }
    static addMinFadeExtent(builder, MIN_FADE_EXTENT) {
        builder.addFieldFloat64(2, MIN_FADE_EXTENT, 0.0);
    }
    static addMaxFadeExtent(builder, MAX_FADE_EXTENT) {
        builder.addFieldFloat64(3, MAX_FADE_EXTENT, 0.0);
    }
    static endKMLLod(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLLod(builder, MIN_LOD_PIXELS, MAX_LOD_PIXELS, MIN_FADE_EXTENT, MAX_FADE_EXTENT) {
        KMLLod.startKMLLod(builder);
        KMLLod.addMinLodPixels(builder, MIN_LOD_PIXELS);
        KMLLod.addMaxLodPixels(builder, MAX_LOD_PIXELS);
        KMLLod.addMinFadeExtent(builder, MIN_FADE_EXTENT);
        KMLLod.addMaxFadeExtent(builder, MAX_FADE_EXTENT);
        return KMLLod.endKMLLod(builder);
    }
    unpack() {
        return new KMLLodT(this.MIN_LOD_PIXELS(), this.MAX_LOD_PIXELS(), this.MIN_FADE_EXTENT(), this.MAX_FADE_EXTENT());
    }
    unpackTo(_o) {
        _o.MIN_LOD_PIXELS = this.MIN_LOD_PIXELS();
        _o.MAX_LOD_PIXELS = this.MAX_LOD_PIXELS();
        _o.MIN_FADE_EXTENT = this.MIN_FADE_EXTENT();
        _o.MAX_FADE_EXTENT = this.MAX_FADE_EXTENT();
    }
}
export class KMLLodT {
    constructor(MIN_LOD_PIXELS = 0.0, MAX_LOD_PIXELS = 0.0, MIN_FADE_EXTENT = 0.0, MAX_FADE_EXTENT = 0.0) {
        this.MIN_LOD_PIXELS = MIN_LOD_PIXELS;
        this.MAX_LOD_PIXELS = MAX_LOD_PIXELS;
        this.MIN_FADE_EXTENT = MIN_FADE_EXTENT;
        this.MAX_FADE_EXTENT = MAX_FADE_EXTENT;
    }
    pack(builder) {
        return KMLLod.createKMLLod(builder, this.MIN_LOD_PIXELS, this.MAX_LOD_PIXELS, this.MIN_FADE_EXTENT, this.MAX_FADE_EXTENT);
    }
}
