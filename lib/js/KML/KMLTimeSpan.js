// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * TimeSpan
 */
export class KMLTimeSpan {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLTimeSpan(bb, obj) {
        return (obj || new KMLTimeSpan()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLTimeSpan(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLTimeSpan()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    BEGIN(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    END(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startKMLTimeSpan(builder) {
        builder.startObject(2);
    }
    static addBegin(builder, BEGINOffset) {
        builder.addFieldOffset(0, BEGINOffset, 0);
    }
    static addEnd(builder, ENDOffset) {
        builder.addFieldOffset(1, ENDOffset, 0);
    }
    static endKMLTimeSpan(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLTimeSpan(builder, BEGINOffset, ENDOffset) {
        KMLTimeSpan.startKMLTimeSpan(builder);
        KMLTimeSpan.addBegin(builder, BEGINOffset);
        KMLTimeSpan.addEnd(builder, ENDOffset);
        return KMLTimeSpan.endKMLTimeSpan(builder);
    }
    unpack() {
        return new KMLTimeSpanT(this.BEGIN(), this.END());
    }
    unpackTo(_o) {
        _o.BEGIN = this.BEGIN();
        _o.END = this.END();
    }
}
export class KMLTimeSpanT {
    constructor(BEGIN = null, END = null) {
        this.BEGIN = BEGIN;
        this.END = END;
    }
    pack(builder) {
        const BEGIN = (this.BEGIN !== null ? builder.createString(this.BEGIN) : 0);
        const END = (this.END !== null ? builder.createString(this.END) : 0);
        return KMLTimeSpan.createKMLTimeSpan(builder, BEGIN, END);
    }
}
