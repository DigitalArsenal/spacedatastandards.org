// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLAltitudeMode } from './KMLAltitudeMode.js';
/**
 * Camera viewpoint
 */
export class KMLCamera {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLCamera(bb, obj) {
        return (obj || new KMLCamera()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLCamera(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLCamera()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Camera longitude
     */
    LONGITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Camera latitude
     */
    LATITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Camera altitude
     */
    ALTITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Heading in degrees (0=North)
     */
    HEADING() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Tilt in degrees from vertical
     */
    TILT() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Roll in degrees
     */
    ROLL() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Altitude mode
     */
    ALTITUDE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
    }
    static startKMLCamera(builder) {
        builder.startObject(7);
    }
    static addLongitude(builder, LONGITUDE) {
        builder.addFieldFloat64(0, LONGITUDE, 0.0);
    }
    static addLatitude(builder, LATITUDE) {
        builder.addFieldFloat64(1, LATITUDE, 0.0);
    }
    static addAltitude(builder, ALTITUDE) {
        builder.addFieldFloat64(2, ALTITUDE, 0.0);
    }
    static addHeading(builder, HEADING) {
        builder.addFieldFloat64(3, HEADING, 0.0);
    }
    static addTilt(builder, TILT) {
        builder.addFieldFloat64(4, TILT, 0.0);
    }
    static addRoll(builder, ROLL) {
        builder.addFieldFloat64(5, ROLL, 0.0);
    }
    static addAltitudeMode(builder, ALTITUDE_MODE) {
        builder.addFieldInt8(6, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
    }
    static endKMLCamera(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLCamera(builder, LONGITUDE, LATITUDE, ALTITUDE, HEADING, TILT, ROLL, ALTITUDE_MODE) {
        KMLCamera.startKMLCamera(builder);
        KMLCamera.addLongitude(builder, LONGITUDE);
        KMLCamera.addLatitude(builder, LATITUDE);
        KMLCamera.addAltitude(builder, ALTITUDE);
        KMLCamera.addHeading(builder, HEADING);
        KMLCamera.addTilt(builder, TILT);
        KMLCamera.addRoll(builder, ROLL);
        KMLCamera.addAltitudeMode(builder, ALTITUDE_MODE);
        return KMLCamera.endKMLCamera(builder);
    }
    unpack() {
        return new KMLCameraT(this.LONGITUDE(), this.LATITUDE(), this.ALTITUDE(), this.HEADING(), this.TILT(), this.ROLL(), this.ALTITUDE_MODE());
    }
    unpackTo(_o) {
        _o.LONGITUDE = this.LONGITUDE();
        _o.LATITUDE = this.LATITUDE();
        _o.ALTITUDE = this.ALTITUDE();
        _o.HEADING = this.HEADING();
        _o.TILT = this.TILT();
        _o.ROLL = this.ROLL();
        _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
    }
}
export class KMLCameraT {
    constructor(LONGITUDE = 0.0, LATITUDE = 0.0, ALTITUDE = 0.0, HEADING = 0.0, TILT = 0.0, ROLL = 0.0, ALTITUDE_MODE = KMLAltitudeMode.CLAMP_TO_GROUND) {
        this.LONGITUDE = LONGITUDE;
        this.LATITUDE = LATITUDE;
        this.ALTITUDE = ALTITUDE;
        this.HEADING = HEADING;
        this.TILT = TILT;
        this.ROLL = ROLL;
        this.ALTITUDE_MODE = ALTITUDE_MODE;
    }
    pack(builder) {
        return KMLCamera.createKMLCamera(builder, this.LONGITUDE, this.LATITUDE, this.ALTITUDE, this.HEADING, this.TILT, this.ROLL, this.ALTITUDE_MODE);
    }
}
