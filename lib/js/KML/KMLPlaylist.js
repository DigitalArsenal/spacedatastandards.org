// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLTourPrimitive } from './KMLTourPrimitive.js';
/**
 * gx:Playlist
 */
export class KMLPlaylist {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLPlaylist(bb, obj) {
        return (obj || new KMLPlaylist()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLPlaylist(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLPlaylist()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Tour primitives
     */
    PRIMITIVES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new KMLTourPrimitive()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    primitivesLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startKMLPlaylist(builder) {
        builder.startObject(1);
    }
    static addPrimitives(builder, PRIMITIVESOffset) {
        builder.addFieldOffset(0, PRIMITIVESOffset, 0);
    }
    static createPrimitivesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPrimitivesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endKMLPlaylist(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createKMLPlaylist(builder, PRIMITIVESOffset) {
        KMLPlaylist.startKMLPlaylist(builder);
        KMLPlaylist.addPrimitives(builder, PRIMITIVESOffset);
        return KMLPlaylist.endKMLPlaylist(builder);
    }
    unpack() {
        return new KMLPlaylistT(this.bb.createObjList(this.PRIMITIVES.bind(this), this.primitivesLength()));
    }
    unpackTo(_o) {
        _o.PRIMITIVES = this.bb.createObjList(this.PRIMITIVES.bind(this), this.primitivesLength());
    }
}
export class KMLPlaylistT {
    constructor(PRIMITIVES = []) {
        this.PRIMITIVES = PRIMITIVES;
    }
    pack(builder) {
        const PRIMITIVES = KMLPlaylist.createPrimitivesVector(builder, builder.createObjectOffsetList(this.PRIMITIVES));
        return KMLPlaylist.createKMLPlaylist(builder, PRIMITIVES);
    }
}
