// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLCoordinate } from './KMLCoordinate.js';
import { KMLModel } from './KMLModel.js';
/**
 * gx:Track — time-stamped position track
 */
export class KMLTrack {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsKMLTrack(bb, obj) {
        return (obj || new KMLTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsKMLTrack(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new KMLTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Whether to extrude
     */
    EXTRUDE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Whether to tessellate
     */
    TESSELLATE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Altitude mode
     */
    ALTITUDE_MODE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
    }
    WHEN(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    whenLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Coordinates (lon lat alt per entry)
     */
    COORDS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new KMLCoordinate()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    coordsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Angles (heading tilt roll per entry)
     */
    ANGLES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new KMLCoordinate()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    anglesLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Model for track visualization
     */
    MODEL(obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new KMLModel()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startKMLTrack(builder) {
        builder.startObject(7);
    }
    static addExtrude(builder, EXTRUDE) {
        builder.addFieldInt8(0, +EXTRUDE, +false);
    }
    static addTessellate(builder, TESSELLATE) {
        builder.addFieldInt8(1, +TESSELLATE, +false);
    }
    static addAltitudeMode(builder, ALTITUDE_MODE) {
        builder.addFieldInt8(2, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
    }
    static addWhen(builder, WHENOffset) {
        builder.addFieldOffset(3, WHENOffset, 0);
    }
    static createWhenVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startWhenVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addCoords(builder, COORDSOffset) {
        builder.addFieldOffset(4, COORDSOffset, 0);
    }
    static createCoordsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startCoordsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAngles(builder, ANGLESOffset) {
        builder.addFieldOffset(5, ANGLESOffset, 0);
    }
    static createAnglesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAnglesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addModel(builder, MODELOffset) {
        builder.addFieldOffset(6, MODELOffset, 0);
    }
    static endKMLTrack(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new KMLTrackT(this.EXTRUDE(), this.TESSELLATE(), this.ALTITUDE_MODE(), this.bb.createScalarList(this.WHEN.bind(this), this.whenLength()), this.bb.createObjList(this.COORDS.bind(this), this.coordsLength()), this.bb.createObjList(this.ANGLES.bind(this), this.anglesLength()), (this.MODEL() !== null ? this.MODEL().unpack() : null));
    }
    unpackTo(_o) {
        _o.EXTRUDE = this.EXTRUDE();
        _o.TESSELLATE = this.TESSELLATE();
        _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
        _o.WHEN = this.bb.createScalarList(this.WHEN.bind(this), this.whenLength());
        _o.COORDS = this.bb.createObjList(this.COORDS.bind(this), this.coordsLength());
        _o.ANGLES = this.bb.createObjList(this.ANGLES.bind(this), this.anglesLength());
        _o.MODEL = (this.MODEL() !== null ? this.MODEL().unpack() : null);
    }
}
export class KMLTrackT {
    constructor(EXTRUDE = false, TESSELLATE = false, ALTITUDE_MODE = KMLAltitudeMode.CLAMP_TO_GROUND, WHEN = [], COORDS = [], ANGLES = [], MODEL = null) {
        this.EXTRUDE = EXTRUDE;
        this.TESSELLATE = TESSELLATE;
        this.ALTITUDE_MODE = ALTITUDE_MODE;
        this.WHEN = WHEN;
        this.COORDS = COORDS;
        this.ANGLES = ANGLES;
        this.MODEL = MODEL;
    }
    pack(builder) {
        const WHEN = KMLTrack.createWhenVector(builder, builder.createObjectOffsetList(this.WHEN));
        const COORDS = KMLTrack.createCoordsVector(builder, builder.createObjectOffsetList(this.COORDS));
        const ANGLES = KMLTrack.createAnglesVector(builder, builder.createObjectOffsetList(this.ANGLES));
        const MODEL = (this.MODEL !== null ? this.MODEL.pack(builder) : 0);
        KMLTrack.startKMLTrack(builder);
        KMLTrack.addExtrude(builder, this.EXTRUDE);
        KMLTrack.addTessellate(builder, this.TESSELLATE);
        KMLTrack.addAltitudeMode(builder, this.ALTITUDE_MODE);
        KMLTrack.addWhen(builder, WHEN);
        KMLTrack.addCoords(builder, COORDS);
        KMLTrack.addAngles(builder, ANGLES);
        KMLTrack.addModel(builder, MODEL);
        return KMLTrack.endKMLTrack(builder);
    }
}
