// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GJNPosition } from './GJNPosition.js';
/**
 * A linear ring is a closed LineString with 4+ positions (first = last)
 */
export class GJNLinearRing {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGJNLinearRing(bb, obj) {
        return (obj || new GJNLinearRing()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGJNLinearRing(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GJNLinearRing()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Ordered positions forming the ring
     */
    POSITIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new GJNPosition()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    positionsLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startGJNLinearRing(builder) {
        builder.startObject(1);
    }
    static addPositions(builder, POSITIONSOffset) {
        builder.addFieldOffset(0, POSITIONSOffset, 0);
    }
    static createPositionsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPositionsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endGJNLinearRing(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGJNLinearRing(builder, POSITIONSOffset) {
        GJNLinearRing.startGJNLinearRing(builder);
        GJNLinearRing.addPositions(builder, POSITIONSOffset);
        return GJNLinearRing.endGJNLinearRing(builder);
    }
    unpack() {
        return new GJNLinearRingT(this.bb.createObjList(this.POSITIONS.bind(this), this.positionsLength()));
    }
    unpackTo(_o) {
        _o.POSITIONS = this.bb.createObjList(this.POSITIONS.bind(this), this.positionsLength());
    }
}
export class GJNLinearRingT {
    constructor(POSITIONS = []) {
        this.POSITIONS = POSITIONS;
    }
    pack(builder) {
        const POSITIONS = GJNLinearRing.createPositionsVector(builder, builder.createObjectOffsetList(this.POSITIONS));
        return GJNLinearRing.createGJNLinearRing(builder, POSITIONS);
    }
}
