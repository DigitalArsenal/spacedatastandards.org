// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * A single position (longitude, latitude, optional altitude)
 */
export class GJNPosition {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGJNPosition(bb, obj) {
        return (obj || new GJNPosition()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGJNPosition(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GJNPosition()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Longitude in decimal degrees (WGS84)
     */
    LONGITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Latitude in decimal degrees (WGS84)
     */
    LATITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Altitude in meters above WGS84 ellipsoid (optional)
     */
    ALTITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * True if altitude was explicitly provided (distinguishes 0 from absent)
     */
    HAS_ALTITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startGJNPosition(builder) {
        builder.startObject(4);
    }
    static addLongitude(builder, LONGITUDE) {
        builder.addFieldFloat64(0, LONGITUDE, 0.0);
    }
    static addLatitude(builder, LATITUDE) {
        builder.addFieldFloat64(1, LATITUDE, 0.0);
    }
    static addAltitude(builder, ALTITUDE) {
        builder.addFieldFloat64(2, ALTITUDE, 0.0);
    }
    static addHasAltitude(builder, HAS_ALTITUDE) {
        builder.addFieldInt8(3, +HAS_ALTITUDE, +false);
    }
    static endGJNPosition(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGJNPosition(builder, LONGITUDE, LATITUDE, ALTITUDE, HAS_ALTITUDE) {
        GJNPosition.startGJNPosition(builder);
        GJNPosition.addLongitude(builder, LONGITUDE);
        GJNPosition.addLatitude(builder, LATITUDE);
        GJNPosition.addAltitude(builder, ALTITUDE);
        GJNPosition.addHasAltitude(builder, HAS_ALTITUDE);
        return GJNPosition.endGJNPosition(builder);
    }
    unpack() {
        return new GJNPositionT(this.LONGITUDE(), this.LATITUDE(), this.ALTITUDE(), this.HAS_ALTITUDE());
    }
    unpackTo(_o) {
        _o.LONGITUDE = this.LONGITUDE();
        _o.LATITUDE = this.LATITUDE();
        _o.ALTITUDE = this.ALTITUDE();
        _o.HAS_ALTITUDE = this.HAS_ALTITUDE();
    }
}
export class GJNPositionT {
    constructor(LONGITUDE = 0.0, LATITUDE = 0.0, ALTITUDE = 0.0, HAS_ALTITUDE = false) {
        this.LONGITUDE = LONGITUDE;
        this.LATITUDE = LATITUDE;
        this.ALTITUDE = ALTITUDE;
        this.HAS_ALTITUDE = HAS_ALTITUDE;
    }
    pack(builder) {
        return GJNPosition.createGJNPosition(builder, this.LONGITUDE, this.LATITUDE, this.ALTITUDE, this.HAS_ALTITUDE);
    }
}
