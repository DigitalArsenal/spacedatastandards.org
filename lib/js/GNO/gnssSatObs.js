// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { gnssConstellation } from './gnssConstellation.js';
import { gnssObsData } from './gnssObsData.js';
/**
 * GNSS Satellite Observation
 */
export class gnssSatObs {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsgnssSatObs(bb, obj) {
        return (obj || new gnssSatObs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsgnssSatObs(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new gnssSatObs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    GNSS_SAT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Constellation
     */
    CONSTELLATION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : gnssConstellation.GPS;
    }
    /**
     * Elevation angle in degrees
     */
    ELEVATION() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Azimuth angle in degrees
     */
    AZIMUTH() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Tracking status (0=not tracked, 1=tracking, 2=locked)
     */
    TRACKING_STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    /**
     * AGC state
     */
    AGC_STATE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    /**
     * Observations for this satellite
     */
    OBSERVATIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new gnssObsData()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    observationsLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startgnssSatObs(builder) {
        builder.startObject(7);
    }
    static addGnssSatId(builder, GNSS_SAT_IDOffset) {
        builder.addFieldOffset(0, GNSS_SAT_IDOffset, 0);
    }
    static addConstellation(builder, CONSTELLATION) {
        builder.addFieldInt8(1, CONSTELLATION, gnssConstellation.GPS);
    }
    static addElevation(builder, ELEVATION) {
        builder.addFieldFloat64(2, ELEVATION, 0.0);
    }
    static addAzimuth(builder, AZIMUTH) {
        builder.addFieldFloat64(3, AZIMUTH, 0.0);
    }
    static addTrackingStatus(builder, TRACKING_STATUS) {
        builder.addFieldInt32(4, TRACKING_STATUS, 0);
    }
    static addAgcState(builder, AGC_STATE) {
        builder.addFieldInt32(5, AGC_STATE, 0);
    }
    static addObservations(builder, OBSERVATIONSOffset) {
        builder.addFieldOffset(6, OBSERVATIONSOffset, 0);
    }
    static createObservationsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startObservationsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endgnssSatObs(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static creategnssSatObs(builder, GNSS_SAT_IDOffset, CONSTELLATION, ELEVATION, AZIMUTH, TRACKING_STATUS, AGC_STATE, OBSERVATIONSOffset) {
        gnssSatObs.startgnssSatObs(builder);
        gnssSatObs.addGnssSatId(builder, GNSS_SAT_IDOffset);
        gnssSatObs.addConstellation(builder, CONSTELLATION);
        gnssSatObs.addElevation(builder, ELEVATION);
        gnssSatObs.addAzimuth(builder, AZIMUTH);
        gnssSatObs.addTrackingStatus(builder, TRACKING_STATUS);
        gnssSatObs.addAgcState(builder, AGC_STATE);
        gnssSatObs.addObservations(builder, OBSERVATIONSOffset);
        return gnssSatObs.endgnssSatObs(builder);
    }
    unpack() {
        return new gnssSatObsT(this.GNSS_SAT_ID(), this.CONSTELLATION(), this.ELEVATION(), this.AZIMUTH(), this.TRACKING_STATUS(), this.AGC_STATE(), this.bb.createObjList(this.OBSERVATIONS.bind(this), this.observationsLength()));
    }
    unpackTo(_o) {
        _o.GNSS_SAT_ID = this.GNSS_SAT_ID();
        _o.CONSTELLATION = this.CONSTELLATION();
        _o.ELEVATION = this.ELEVATION();
        _o.AZIMUTH = this.AZIMUTH();
        _o.TRACKING_STATUS = this.TRACKING_STATUS();
        _o.AGC_STATE = this.AGC_STATE();
        _o.OBSERVATIONS = this.bb.createObjList(this.OBSERVATIONS.bind(this), this.observationsLength());
    }
}
export class gnssSatObsT {
    constructor(GNSS_SAT_ID = null, CONSTELLATION = gnssConstellation.GPS, ELEVATION = 0.0, AZIMUTH = 0.0, TRACKING_STATUS = 0, AGC_STATE = 0, OBSERVATIONS = []) {
        this.GNSS_SAT_ID = GNSS_SAT_ID;
        this.CONSTELLATION = CONSTELLATION;
        this.ELEVATION = ELEVATION;
        this.AZIMUTH = AZIMUTH;
        this.TRACKING_STATUS = TRACKING_STATUS;
        this.AGC_STATE = AGC_STATE;
        this.OBSERVATIONS = OBSERVATIONS;
    }
    pack(builder) {
        const GNSS_SAT_ID = (this.GNSS_SAT_ID !== null ? builder.createString(this.GNSS_SAT_ID) : 0);
        const OBSERVATIONS = gnssSatObs.createObservationsVector(builder, builder.createObjectOffsetList(this.OBSERVATIONS));
        return gnssSatObs.creategnssSatObs(builder, GNSS_SAT_ID, this.CONSTELLATION, this.ELEVATION, this.AZIMUTH, this.TRACKING_STATUS, this.AGC_STATE, OBSERVATIONS);
    }
}
