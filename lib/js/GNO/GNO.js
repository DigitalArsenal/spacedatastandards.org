// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * GNSS Observation
 */
export class GNO {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGNO(bb, obj) {
        return (obj || new GNO()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGNO(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GNO()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$GNO');
    }
    GNSS_SAT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TRACKING_STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    AGC_STATE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : 0;
    }
    OBS_CODE_SET(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    obsCodeSetLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    OB(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    obLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startGNO(builder) {
        builder.startObject(5);
    }
    static addGnssSatId(builder, GNSS_SAT_IDOffset) {
        builder.addFieldOffset(0, GNSS_SAT_IDOffset, 0);
    }
    static addTrackingStatus(builder, TRACKING_STATUS) {
        builder.addFieldInt32(1, TRACKING_STATUS, 0);
    }
    static addAgcState(builder, AGC_STATE) {
        builder.addFieldInt32(2, AGC_STATE, 0);
    }
    static addObsCodeSet(builder, OBS_CODE_SETOffset) {
        builder.addFieldOffset(3, OBS_CODE_SETOffset, 0);
    }
    static createObsCodeSetVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startObsCodeSetVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addOb(builder, OBOffset) {
        builder.addFieldOffset(4, OBOffset, 0);
    }
    static createObVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startObVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endGNO(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishGNOBuffer(builder, offset) {
        builder.finish(offset, '$GNO');
    }
    static finishSizePrefixedGNOBuffer(builder, offset) {
        builder.finish(offset, '$GNO', true);
    }
    static createGNO(builder, GNSS_SAT_IDOffset, TRACKING_STATUS, AGC_STATE, OBS_CODE_SETOffset, OBOffset) {
        GNO.startGNO(builder);
        GNO.addGnssSatId(builder, GNSS_SAT_IDOffset);
        GNO.addTrackingStatus(builder, TRACKING_STATUS);
        GNO.addAgcState(builder, AGC_STATE);
        GNO.addObsCodeSet(builder, OBS_CODE_SETOffset);
        GNO.addOb(builder, OBOffset);
        return GNO.endGNO(builder);
    }
    unpack() {
        return new GNOT(this.GNSS_SAT_ID(), this.TRACKING_STATUS(), this.AGC_STATE(), this.bb.createScalarList(this.OBS_CODE_SET.bind(this), this.obsCodeSetLength()), this.bb.createScalarList(this.OB.bind(this), this.obLength()));
    }
    unpackTo(_o) {
        _o.GNSS_SAT_ID = this.GNSS_SAT_ID();
        _o.TRACKING_STATUS = this.TRACKING_STATUS();
        _o.AGC_STATE = this.AGC_STATE();
        _o.OBS_CODE_SET = this.bb.createScalarList(this.OBS_CODE_SET.bind(this), this.obsCodeSetLength());
        _o.OB = this.bb.createScalarList(this.OB.bind(this), this.obLength());
    }
}
export class GNOT {
    constructor(GNSS_SAT_ID = null, TRACKING_STATUS = 0, AGC_STATE = 0, OBS_CODE_SET = [], OB = []) {
        this.GNSS_SAT_ID = GNSS_SAT_ID;
        this.TRACKING_STATUS = TRACKING_STATUS;
        this.AGC_STATE = AGC_STATE;
        this.OBS_CODE_SET = OBS_CODE_SET;
        this.OB = OB;
    }
    pack(builder) {
        const GNSS_SAT_ID = (this.GNSS_SAT_ID !== null ? builder.createString(this.GNSS_SAT_ID) : 0);
        const OBS_CODE_SET = GNO.createObsCodeSetVector(builder, builder.createObjectOffsetList(this.OBS_CODE_SET));
        const OB = GNO.createObVector(builder, builder.createObjectOffsetList(this.OB));
        return GNO.createGNO(builder, GNSS_SAT_ID, this.TRACKING_STATUS, this.AGC_STATE, OBS_CODE_SET, OB);
    }
}
