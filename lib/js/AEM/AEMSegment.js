// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { AEMAttitudeEntry } from './AEMAttitudeEntry.js';
export class AEMSegment {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAEMSegment(bb, obj) {
        return (obj || new AEMSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAEMSegment(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new AEMSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    OBJECT_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    OBJECT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    REF_FRAME_A(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    REF_FRAME_B(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ATTITUDE_DIR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TIME_SYSTEM(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ATTITUDE_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    START_TIME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    STOP_TIME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DATA(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? (obj || new AEMAttitudeEntry()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startAEMSegment(builder) {
        builder.startObject(10);
    }
    static addObjectName(builder, OBJECT_NAMEOffset) {
        builder.addFieldOffset(0, OBJECT_NAMEOffset, 0);
    }
    static addObjectId(builder, OBJECT_IDOffset) {
        builder.addFieldOffset(1, OBJECT_IDOffset, 0);
    }
    static addRefFrameA(builder, REF_FRAME_AOffset) {
        builder.addFieldOffset(2, REF_FRAME_AOffset, 0);
    }
    static addRefFrameB(builder, REF_FRAME_BOffset) {
        builder.addFieldOffset(3, REF_FRAME_BOffset, 0);
    }
    static addAttitudeDir(builder, ATTITUDE_DIROffset) {
        builder.addFieldOffset(4, ATTITUDE_DIROffset, 0);
    }
    static addTimeSystem(builder, TIME_SYSTEMOffset) {
        builder.addFieldOffset(5, TIME_SYSTEMOffset, 0);
    }
    static addAttitudeType(builder, ATTITUDE_TYPEOffset) {
        builder.addFieldOffset(6, ATTITUDE_TYPEOffset, 0);
    }
    static addStartTime(builder, START_TIMEOffset) {
        builder.addFieldOffset(7, START_TIMEOffset, 0);
    }
    static addStopTime(builder, STOP_TIMEOffset) {
        builder.addFieldOffset(8, STOP_TIMEOffset, 0);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(9, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endAEMSegment(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createAEMSegment(builder, OBJECT_NAMEOffset, OBJECT_IDOffset, REF_FRAME_AOffset, REF_FRAME_BOffset, ATTITUDE_DIROffset, TIME_SYSTEMOffset, ATTITUDE_TYPEOffset, START_TIMEOffset, STOP_TIMEOffset, DATAOffset) {
        AEMSegment.startAEMSegment(builder);
        AEMSegment.addObjectName(builder, OBJECT_NAMEOffset);
        AEMSegment.addObjectId(builder, OBJECT_IDOffset);
        AEMSegment.addRefFrameA(builder, REF_FRAME_AOffset);
        AEMSegment.addRefFrameB(builder, REF_FRAME_BOffset);
        AEMSegment.addAttitudeDir(builder, ATTITUDE_DIROffset);
        AEMSegment.addTimeSystem(builder, TIME_SYSTEMOffset);
        AEMSegment.addAttitudeType(builder, ATTITUDE_TYPEOffset);
        AEMSegment.addStartTime(builder, START_TIMEOffset);
        AEMSegment.addStopTime(builder, STOP_TIMEOffset);
        AEMSegment.addData(builder, DATAOffset);
        return AEMSegment.endAEMSegment(builder);
    }
    unpack() {
        return new AEMSegmentT(this.OBJECT_NAME(), this.OBJECT_ID(), this.REF_FRAME_A(), this.REF_FRAME_B(), this.ATTITUDE_DIR(), this.TIME_SYSTEM(), this.ATTITUDE_TYPE(), this.START_TIME(), this.STOP_TIME(), this.bb.createObjList(this.DATA.bind(this), this.dataLength()));
    }
    unpackTo(_o) {
        _o.OBJECT_NAME = this.OBJECT_NAME();
        _o.OBJECT_ID = this.OBJECT_ID();
        _o.REF_FRAME_A = this.REF_FRAME_A();
        _o.REF_FRAME_B = this.REF_FRAME_B();
        _o.ATTITUDE_DIR = this.ATTITUDE_DIR();
        _o.TIME_SYSTEM = this.TIME_SYSTEM();
        _o.ATTITUDE_TYPE = this.ATTITUDE_TYPE();
        _o.START_TIME = this.START_TIME();
        _o.STOP_TIME = this.STOP_TIME();
        _o.DATA = this.bb.createObjList(this.DATA.bind(this), this.dataLength());
    }
}
export class AEMSegmentT {
    constructor(OBJECT_NAME = null, OBJECT_ID = null, REF_FRAME_A = null, REF_FRAME_B = null, ATTITUDE_DIR = null, TIME_SYSTEM = null, ATTITUDE_TYPE = null, START_TIME = null, STOP_TIME = null, DATA = []) {
        this.OBJECT_NAME = OBJECT_NAME;
        this.OBJECT_ID = OBJECT_ID;
        this.REF_FRAME_A = REF_FRAME_A;
        this.REF_FRAME_B = REF_FRAME_B;
        this.ATTITUDE_DIR = ATTITUDE_DIR;
        this.TIME_SYSTEM = TIME_SYSTEM;
        this.ATTITUDE_TYPE = ATTITUDE_TYPE;
        this.START_TIME = START_TIME;
        this.STOP_TIME = STOP_TIME;
        this.DATA = DATA;
    }
    pack(builder) {
        const OBJECT_NAME = (this.OBJECT_NAME !== null ? builder.createString(this.OBJECT_NAME) : 0);
        const OBJECT_ID = (this.OBJECT_ID !== null ? builder.createString(this.OBJECT_ID) : 0);
        const REF_FRAME_A = (this.REF_FRAME_A !== null ? builder.createString(this.REF_FRAME_A) : 0);
        const REF_FRAME_B = (this.REF_FRAME_B !== null ? builder.createString(this.REF_FRAME_B) : 0);
        const ATTITUDE_DIR = (this.ATTITUDE_DIR !== null ? builder.createString(this.ATTITUDE_DIR) : 0);
        const TIME_SYSTEM = (this.TIME_SYSTEM !== null ? builder.createString(this.TIME_SYSTEM) : 0);
        const ATTITUDE_TYPE = (this.ATTITUDE_TYPE !== null ? builder.createString(this.ATTITUDE_TYPE) : 0);
        const START_TIME = (this.START_TIME !== null ? builder.createString(this.START_TIME) : 0);
        const STOP_TIME = (this.STOP_TIME !== null ? builder.createString(this.STOP_TIME) : 0);
        const DATA = AEMSegment.createDataVector(builder, builder.createObjectOffsetList(this.DATA));
        return AEMSegment.createAEMSegment(builder, OBJECT_NAME, OBJECT_ID, REF_FRAME_A, REF_FRAME_B, ATTITUDE_DIR, TIME_SYSTEM, ATTITUDE_TYPE, START_TIME, STOP_TIME, DATA);
    }
}
