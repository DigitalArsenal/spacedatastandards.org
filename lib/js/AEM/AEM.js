// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { AEMSegment } from './AEMSegment.js';
/**
 * Attitude Ephemeris Message
 */
export class AEM {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAEM(bb, obj) {
        return (obj || new AEM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAEM(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new AEM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$AEM');
    }
    CCSDS_AEM_VERS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CREATION_DATE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ORIGINATOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SEGMENTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new AEMSegment()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    segmentsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startAEM(builder) {
        builder.startObject(4);
    }
    static addCcsdsAemVers(builder, CCSDS_AEM_VERSOffset) {
        builder.addFieldOffset(0, CCSDS_AEM_VERSOffset, 0);
    }
    static addCreationDate(builder, CREATION_DATEOffset) {
        builder.addFieldOffset(1, CREATION_DATEOffset, 0);
    }
    static addOriginator(builder, ORIGINATOROffset) {
        builder.addFieldOffset(2, ORIGINATOROffset, 0);
    }
    static addSegments(builder, SEGMENTSOffset) {
        builder.addFieldOffset(3, SEGMENTSOffset, 0);
    }
    static createSegmentsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSegmentsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endAEM(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishAEMBuffer(builder, offset) {
        builder.finish(offset, '$AEM');
    }
    static finishSizePrefixedAEMBuffer(builder, offset) {
        builder.finish(offset, '$AEM', true);
    }
    static createAEM(builder, CCSDS_AEM_VERSOffset, CREATION_DATEOffset, ORIGINATOROffset, SEGMENTSOffset) {
        AEM.startAEM(builder);
        AEM.addCcsdsAemVers(builder, CCSDS_AEM_VERSOffset);
        AEM.addCreationDate(builder, CREATION_DATEOffset);
        AEM.addOriginator(builder, ORIGINATOROffset);
        AEM.addSegments(builder, SEGMENTSOffset);
        return AEM.endAEM(builder);
    }
    unpack() {
        return new AEMT(this.CCSDS_AEM_VERS(), this.CREATION_DATE(), this.ORIGINATOR(), this.bb.createObjList(this.SEGMENTS.bind(this), this.segmentsLength()));
    }
    unpackTo(_o) {
        _o.CCSDS_AEM_VERS = this.CCSDS_AEM_VERS();
        _o.CREATION_DATE = this.CREATION_DATE();
        _o.ORIGINATOR = this.ORIGINATOR();
        _o.SEGMENTS = this.bb.createObjList(this.SEGMENTS.bind(this), this.segmentsLength());
    }
}
export class AEMT {
    constructor(CCSDS_AEM_VERS = null, CREATION_DATE = null, ORIGINATOR = null, SEGMENTS = []) {
        this.CCSDS_AEM_VERS = CCSDS_AEM_VERS;
        this.CREATION_DATE = CREATION_DATE;
        this.ORIGINATOR = ORIGINATOR;
        this.SEGMENTS = SEGMENTS;
    }
    pack(builder) {
        const CCSDS_AEM_VERS = (this.CCSDS_AEM_VERS !== null ? builder.createString(this.CCSDS_AEM_VERS) : 0);
        const CREATION_DATE = (this.CREATION_DATE !== null ? builder.createString(this.CREATION_DATE) : 0);
        const ORIGINATOR = (this.ORIGINATOR !== null ? builder.createString(this.ORIGINATOR) : 0);
        const SEGMENTS = AEM.createSegmentsVector(builder, builder.createObjectOffsetList(this.SEGMENTS));
        return AEM.createAEM(builder, CCSDS_AEM_VERS, CREATION_DATE, ORIGINATOR, SEGMENTS);
    }
}
