// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { CZMDynValueType } from './CZMDynValueType.js';
import { CZMInterpolation } from './CZMInterpolation.js';
/**
 * Sampled time-varying data
 */
export class CZMDynSampled {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCZMDynSampled(bb, obj) {
        return (obj || new CZMDynSampled()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCZMDynSampled(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CZMDynSampled()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Value type determines stride through DATA
     */
    VALUE_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : CZMDynValueType.NUMBER;
    }
    /**
     * Interleaved [time, value(s), ...] — stride depends on VALUE_TYPE
     */
    DATA(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    dataArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Interpolation settings
     */
    INTERPOLATION(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new CZMInterpolation()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startCZMDynSampled(builder) {
        builder.startObject(4);
    }
    static addEpoch(builder, EPOCHOffset) {
        builder.addFieldOffset(0, EPOCHOffset, 0);
    }
    static addValueType(builder, VALUE_TYPE) {
        builder.addFieldInt8(1, VALUE_TYPE, CZMDynValueType.NUMBER);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(2, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addInterpolation(builder, INTERPOLATIONOffset) {
        builder.addFieldOffset(3, INTERPOLATIONOffset, 0);
    }
    static endCZMDynSampled(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new CZMDynSampledT(this.EPOCH(), this.VALUE_TYPE(), this.bb.createScalarList(this.DATA.bind(this), this.dataLength()), (this.INTERPOLATION() !== null ? this.INTERPOLATION().unpack() : null));
    }
    unpackTo(_o) {
        _o.EPOCH = this.EPOCH();
        _o.VALUE_TYPE = this.VALUE_TYPE();
        _o.DATA = this.bb.createScalarList(this.DATA.bind(this), this.dataLength());
        _o.INTERPOLATION = (this.INTERPOLATION() !== null ? this.INTERPOLATION().unpack() : null);
    }
}
export class CZMDynSampledT {
    constructor(EPOCH = null, VALUE_TYPE = CZMDynValueType.NUMBER, DATA = [], INTERPOLATION = null) {
        this.EPOCH = EPOCH;
        this.VALUE_TYPE = VALUE_TYPE;
        this.DATA = DATA;
        this.INTERPOLATION = INTERPOLATION;
    }
    pack(builder) {
        const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH) : 0);
        const DATA = CZMDynSampled.createDataVector(builder, this.DATA);
        const INTERPOLATION = (this.INTERPOLATION !== null ? this.INTERPOLATION.pack(builder) : 0);
        CZMDynSampled.startCZMDynSampled(builder);
        CZMDynSampled.addEpoch(builder, EPOCH);
        CZMDynSampled.addValueType(builder, this.VALUE_TYPE);
        CZMDynSampled.addData(builder, DATA);
        CZMDynSampled.addInterpolation(builder, INTERPOLATION);
        return CZMDynSampled.endCZMDynSampled(builder);
    }
}
