// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Polygon hole position list
 */
export class CZMPolygonHole {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCZMPolygonHole(bb, obj) {
        return (obj || new CZMPolygonHole()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCZMPolygonHole(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CZMPolygonHole()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Positions as cartographic degrees [lon, lat, height, ...]
     */
    POSITIONS_CARTOGRAPHIC_DEGREES(index) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    positionsCartographicDegreesLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    positionsCartographicDegreesArray() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Positions as Cartesian [X, Y, Z, ...]
     */
    POSITIONS_CARTESIAN(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    positionsCartesianLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    positionsCartesianArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startCZMPolygonHole(builder) {
        builder.startObject(2);
    }
    static addPositionsCartographicDegrees(builder, POSITIONS_CARTOGRAPHIC_DEGREESOffset) {
        builder.addFieldOffset(0, POSITIONS_CARTOGRAPHIC_DEGREESOffset, 0);
    }
    static createPositionsCartographicDegreesVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startPositionsCartographicDegreesVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addPositionsCartesian(builder, POSITIONS_CARTESIANOffset) {
        builder.addFieldOffset(1, POSITIONS_CARTESIANOffset, 0);
    }
    static createPositionsCartesianVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startPositionsCartesianVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endCZMPolygonHole(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createCZMPolygonHole(builder, POSITIONS_CARTOGRAPHIC_DEGREESOffset, POSITIONS_CARTESIANOffset) {
        CZMPolygonHole.startCZMPolygonHole(builder);
        CZMPolygonHole.addPositionsCartographicDegrees(builder, POSITIONS_CARTOGRAPHIC_DEGREESOffset);
        CZMPolygonHole.addPositionsCartesian(builder, POSITIONS_CARTESIANOffset);
        return CZMPolygonHole.endCZMPolygonHole(builder);
    }
    unpack() {
        return new CZMPolygonHoleT(this.bb.createScalarList(this.POSITIONS_CARTOGRAPHIC_DEGREES.bind(this), this.positionsCartographicDegreesLength()), this.bb.createScalarList(this.POSITIONS_CARTESIAN.bind(this), this.positionsCartesianLength()));
    }
    unpackTo(_o) {
        _o.POSITIONS_CARTOGRAPHIC_DEGREES = this.bb.createScalarList(this.POSITIONS_CARTOGRAPHIC_DEGREES.bind(this), this.positionsCartographicDegreesLength());
        _o.POSITIONS_CARTESIAN = this.bb.createScalarList(this.POSITIONS_CARTESIAN.bind(this), this.positionsCartesianLength());
    }
}
export class CZMPolygonHoleT {
    constructor(POSITIONS_CARTOGRAPHIC_DEGREES = [], POSITIONS_CARTESIAN = []) {
        this.POSITIONS_CARTOGRAPHIC_DEGREES = POSITIONS_CARTOGRAPHIC_DEGREES;
        this.POSITIONS_CARTESIAN = POSITIONS_CARTESIAN;
    }
    pack(builder) {
        const POSITIONS_CARTOGRAPHIC_DEGREES = CZMPolygonHole.createPositionsCartographicDegreesVector(builder, this.POSITIONS_CARTOGRAPHIC_DEGREES);
        const POSITIONS_CARTESIAN = CZMPolygonHole.createPositionsCartesianVector(builder, this.POSITIONS_CARTESIAN);
        return CZMPolygonHole.createCZMPolygonHole(builder, POSITIONS_CARTOGRAPHIC_DEGREES, POSITIONS_CARTESIAN);
    }
}
