// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { snwErrorCode } from './snwErrorCode.js';
/**
 * Batch target detection response header.
 */
export class SNWBatchResponse {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSNWBatchResponse(bb, obj) {
        return (obj || new SNWBatchResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSNWBatchResponse(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SNWBatchResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Number of detection records written to OUTPUT_OFFSET.
     */
    COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Number of targets whose SNR met the detection threshold.
     */
    DETECTED_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Byte offset where the detectionResult[] stream starts.
     */
    OUTPUT_OFFSET() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Error code (0 == OK).
     */
    ERROR_CODE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt32(this.bb_pos + offset) : snwErrorCode.OK;
    }
    ERROR_MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startSNWBatchResponse(builder) {
        builder.startObject(5);
    }
    static addCount(builder, COUNT) {
        builder.addFieldInt32(0, COUNT, 0);
    }
    static addDetectedCount(builder, DETECTED_COUNT) {
        builder.addFieldInt32(1, DETECTED_COUNT, 0);
    }
    static addOutputOffset(builder, OUTPUT_OFFSET) {
        builder.addFieldInt32(2, OUTPUT_OFFSET, 0);
    }
    static addErrorCode(builder, ERROR_CODE) {
        builder.addFieldInt32(3, ERROR_CODE, snwErrorCode.OK);
    }
    static addErrorMessage(builder, ERROR_MESSAGEOffset) {
        builder.addFieldOffset(4, ERROR_MESSAGEOffset, 0);
    }
    static endSNWBatchResponse(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSNWBatchResponse(builder, COUNT, DETECTED_COUNT, OUTPUT_OFFSET, ERROR_CODE, ERROR_MESSAGEOffset) {
        SNWBatchResponse.startSNWBatchResponse(builder);
        SNWBatchResponse.addCount(builder, COUNT);
        SNWBatchResponse.addDetectedCount(builder, DETECTED_COUNT);
        SNWBatchResponse.addOutputOffset(builder, OUTPUT_OFFSET);
        SNWBatchResponse.addErrorCode(builder, ERROR_CODE);
        SNWBatchResponse.addErrorMessage(builder, ERROR_MESSAGEOffset);
        return SNWBatchResponse.endSNWBatchResponse(builder);
    }
    unpack() {
        return new SNWBatchResponseT(this.COUNT(), this.DETECTED_COUNT(), this.OUTPUT_OFFSET(), this.ERROR_CODE(), this.ERROR_MESSAGE());
    }
    unpackTo(_o) {
        _o.COUNT = this.COUNT();
        _o.DETECTED_COUNT = this.DETECTED_COUNT();
        _o.OUTPUT_OFFSET = this.OUTPUT_OFFSET();
        _o.ERROR_CODE = this.ERROR_CODE();
        _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
    }
}
export class SNWBatchResponseT {
    constructor(COUNT = 0, DETECTED_COUNT = 0, OUTPUT_OFFSET = 0, ERROR_CODE = snwErrorCode.OK, ERROR_MESSAGE = null) {
        this.COUNT = COUNT;
        this.DETECTED_COUNT = DETECTED_COUNT;
        this.OUTPUT_OFFSET = OUTPUT_OFFSET;
        this.ERROR_CODE = ERROR_CODE;
        this.ERROR_MESSAGE = ERROR_MESSAGE;
    }
    pack(builder) {
        const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE) : 0);
        return SNWBatchResponse.createSNWBatchResponse(builder, this.COUNT, this.DETECTED_COUNT, this.OUTPUT_OFFSET, this.ERROR_CODE, ERROR_MESSAGE);
    }
}
