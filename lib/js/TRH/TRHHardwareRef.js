// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { trhHardwareKind } from './trhHardwareKind.js';
export class TRHHardwareRef {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRHHardwareRef(bb, obj) {
        return (obj || new TRHHardwareRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRHHardwareRef(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRHHardwareRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    HARDWARE_KIND() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : trhHardwareKind.UNSPECIFIED;
    }
    HARDWARE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    static startTRHHardwareRef(builder) {
        builder.startObject(2);
    }
    static addHardwareKind(builder, HARDWARE_KIND) {
        builder.addFieldInt8(0, HARDWARE_KIND, trhHardwareKind.UNSPECIFIED);
    }
    static addHardwareId(builder, HARDWARE_IDOffset) {
        builder.addFieldOffset(1, HARDWARE_IDOffset, 0);
    }
    static endTRHHardwareRef(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // HARDWARE_ID
        return offset;
    }
    static createTRHHardwareRef(builder, HARDWARE_KIND, HARDWARE_IDOffset) {
        TRHHardwareRef.startTRHHardwareRef(builder);
        TRHHardwareRef.addHardwareKind(builder, HARDWARE_KIND);
        TRHHardwareRef.addHardwareId(builder, HARDWARE_IDOffset);
        return TRHHardwareRef.endTRHHardwareRef(builder);
    }
    unpack() {
        return new TRHHardwareRefT(this.HARDWARE_KIND(), this.HARDWARE_ID());
    }
    unpackTo(_o) {
        _o.HARDWARE_KIND = this.HARDWARE_KIND();
        _o.HARDWARE_ID = this.HARDWARE_ID();
    }
}
export class TRHHardwareRefT {
    constructor(HARDWARE_KIND = trhHardwareKind.UNSPECIFIED, HARDWARE_ID = null) {
        this.HARDWARE_KIND = HARDWARE_KIND;
        this.HARDWARE_ID = HARDWARE_ID;
    }
    pack(builder) {
        const HARDWARE_ID = (this.HARDWARE_ID !== null ? builder.createString(this.HARDWARE_ID) : 0);
        return TRHHardwareRef.createTRHHardwareRef(builder, this.HARDWARE_KIND, HARDWARE_ID);
    }
}
