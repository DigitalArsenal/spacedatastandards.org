// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { TRHAntenna } from './TRHAntenna.js';
import { TRHAttestation } from './TRHAttestation.js';
import { TRHInstallation } from './TRHInstallation.js';
import { TRHReceiver } from './TRHReceiver.js';
import { TRHTransmitter } from './TRHTransmitter.js';
import { TRHTransponder } from './TRHTransponder.js';
/**
 * Tracking Hardware. Installations attach antennas, transmitters, receivers
 * and transponders to either a surface site or a tracked object. Delays are
 * seconds, frequencies hertz, temperatures kelvin and powers watts. JSON
 * projections preserve these IDL-capitalized field names exactly.
 */
export class TRH {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRH(bb, obj) {
        return (obj || new TRH()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRH(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRH()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$TRH');
    }
    ANTENNAS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new TRHAntenna()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    antennasLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    TRANSMITTERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new TRHTransmitter()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    transmittersLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    RECEIVERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new TRHReceiver()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    receiversLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    TRANSPONDERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new TRHTransponder()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    transpondersLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    INSTALLATIONS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new TRHInstallation()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    installationsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ATTESTATION(obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new TRHAttestation()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startTRH(builder) {
        builder.startObject(7);
    }
    static addAntennas(builder, ANTENNASOffset) {
        builder.addFieldOffset(0, ANTENNASOffset, 0);
    }
    static createAntennasVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAntennasVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTransmitters(builder, TRANSMITTERSOffset) {
        builder.addFieldOffset(1, TRANSMITTERSOffset, 0);
    }
    static createTransmittersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTransmittersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addReceivers(builder, RECEIVERSOffset) {
        builder.addFieldOffset(2, RECEIVERSOffset, 0);
    }
    static createReceiversVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startReceiversVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTransponders(builder, TRANSPONDERSOffset) {
        builder.addFieldOffset(3, TRANSPONDERSOffset, 0);
    }
    static createTranspondersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTranspondersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addInstallations(builder, INSTALLATIONSOffset) {
        builder.addFieldOffset(4, INSTALLATIONSOffset, 0);
    }
    static createInstallationsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startInstallationsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(5, TRACE_IDOffset, 0);
    }
    static addAttestation(builder, ATTESTATIONOffset) {
        builder.addFieldOffset(6, ATTESTATIONOffset, 0);
    }
    static endTRH(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishTRHBuffer(builder, offset) {
        builder.finish(offset, '$TRH');
    }
    static finishSizePrefixedTRHBuffer(builder, offset) {
        builder.finish(offset, '$TRH', true);
    }
    unpack() {
        return new TRHT(this.bb.createObjList(this.ANTENNAS.bind(this), this.antennasLength()), this.bb.createObjList(this.TRANSMITTERS.bind(this), this.transmittersLength()), this.bb.createObjList(this.RECEIVERS.bind(this), this.receiversLength()), this.bb.createObjList(this.TRANSPONDERS.bind(this), this.transpondersLength()), this.bb.createObjList(this.INSTALLATIONS.bind(this), this.installationsLength()), this.TRACE_ID(), (this.ATTESTATION() !== null ? this.ATTESTATION().unpack() : null));
    }
    unpackTo(_o) {
        _o.ANTENNAS = this.bb.createObjList(this.ANTENNAS.bind(this), this.antennasLength());
        _o.TRANSMITTERS = this.bb.createObjList(this.TRANSMITTERS.bind(this), this.transmittersLength());
        _o.RECEIVERS = this.bb.createObjList(this.RECEIVERS.bind(this), this.receiversLength());
        _o.TRANSPONDERS = this.bb.createObjList(this.TRANSPONDERS.bind(this), this.transpondersLength());
        _o.INSTALLATIONS = this.bb.createObjList(this.INSTALLATIONS.bind(this), this.installationsLength());
        _o.TRACE_ID = this.TRACE_ID();
        _o.ATTESTATION = (this.ATTESTATION() !== null ? this.ATTESTATION().unpack() : null);
    }
}
export class TRHT {
    constructor(ANTENNAS = [], TRANSMITTERS = [], RECEIVERS = [], TRANSPONDERS = [], INSTALLATIONS = [], TRACE_ID = null, ATTESTATION = null) {
        this.ANTENNAS = ANTENNAS;
        this.TRANSMITTERS = TRANSMITTERS;
        this.RECEIVERS = RECEIVERS;
        this.TRANSPONDERS = TRANSPONDERS;
        this.INSTALLATIONS = INSTALLATIONS;
        this.TRACE_ID = TRACE_ID;
        this.ATTESTATION = ATTESTATION;
    }
    pack(builder) {
        const ANTENNAS = TRH.createAntennasVector(builder, builder.createObjectOffsetList(this.ANTENNAS));
        const TRANSMITTERS = TRH.createTransmittersVector(builder, builder.createObjectOffsetList(this.TRANSMITTERS));
        const RECEIVERS = TRH.createReceiversVector(builder, builder.createObjectOffsetList(this.RECEIVERS));
        const TRANSPONDERS = TRH.createTranspondersVector(builder, builder.createObjectOffsetList(this.TRANSPONDERS));
        const INSTALLATIONS = TRH.createInstallationsVector(builder, builder.createObjectOffsetList(this.INSTALLATIONS));
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        const ATTESTATION = (this.ATTESTATION !== null ? this.ATTESTATION.pack(builder) : 0);
        TRH.startTRH(builder);
        TRH.addAntennas(builder, ANTENNAS);
        TRH.addTransmitters(builder, TRANSMITTERS);
        TRH.addReceivers(builder, RECEIVERS);
        TRH.addTransponders(builder, TRANSPONDERS);
        TRH.addInstallations(builder, INSTALLATIONS);
        TRH.addTraceId(builder, TRACE_ID);
        TRH.addAttestation(builder, ATTESTATION);
        return TRH.endTRH(builder);
    }
}
