// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
export class TRHTransmitter {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRHTransmitter(bb, obj) {
        return (obj || new TRHTransmitter()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRHTransmitter(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRHTransmitter()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    HARDWARE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    FREQUENCY_HZ() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    POWER_WATTS() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MODULATION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    HARDWARE_DELAY_SECONDS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    ANTENNA_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startTRHTransmitter(builder) {
        builder.startObject(6);
    }
    static addHardwareId(builder, HARDWARE_IDOffset) {
        builder.addFieldOffset(0, HARDWARE_IDOffset, 0);
    }
    static addFrequencyHz(builder, FREQUENCY_HZ) {
        builder.addFieldFloat64(1, FREQUENCY_HZ, 0.0);
    }
    static addPowerWatts(builder, POWER_WATTS) {
        builder.addFieldFloat64(2, POWER_WATTS, 0.0);
    }
    static addModulation(builder, MODULATIONOffset) {
        builder.addFieldOffset(3, MODULATIONOffset, 0);
    }
    static addHardwareDelaySeconds(builder, HARDWARE_DELAY_SECONDS) {
        builder.addFieldFloat64(4, HARDWARE_DELAY_SECONDS, 0.0);
    }
    static addAntennaId(builder, ANTENNA_IDOffset) {
        builder.addFieldOffset(5, ANTENNA_IDOffset, 0);
    }
    static endTRHTransmitter(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // HARDWARE_ID
        return offset;
    }
    static createTRHTransmitter(builder, HARDWARE_IDOffset, FREQUENCY_HZ, POWER_WATTS, MODULATIONOffset, HARDWARE_DELAY_SECONDS, ANTENNA_IDOffset) {
        TRHTransmitter.startTRHTransmitter(builder);
        TRHTransmitter.addHardwareId(builder, HARDWARE_IDOffset);
        TRHTransmitter.addFrequencyHz(builder, FREQUENCY_HZ);
        TRHTransmitter.addPowerWatts(builder, POWER_WATTS);
        TRHTransmitter.addModulation(builder, MODULATIONOffset);
        TRHTransmitter.addHardwareDelaySeconds(builder, HARDWARE_DELAY_SECONDS);
        TRHTransmitter.addAntennaId(builder, ANTENNA_IDOffset);
        return TRHTransmitter.endTRHTransmitter(builder);
    }
    unpack() {
        return new TRHTransmitterT(this.HARDWARE_ID(), this.FREQUENCY_HZ(), this.POWER_WATTS(), this.MODULATION(), this.HARDWARE_DELAY_SECONDS(), this.ANTENNA_ID());
    }
    unpackTo(_o) {
        _o.HARDWARE_ID = this.HARDWARE_ID();
        _o.FREQUENCY_HZ = this.FREQUENCY_HZ();
        _o.POWER_WATTS = this.POWER_WATTS();
        _o.MODULATION = this.MODULATION();
        _o.HARDWARE_DELAY_SECONDS = this.HARDWARE_DELAY_SECONDS();
        _o.ANTENNA_ID = this.ANTENNA_ID();
    }
}
export class TRHTransmitterT {
    constructor(HARDWARE_ID = null, FREQUENCY_HZ = 0.0, POWER_WATTS = 0.0, MODULATION = null, HARDWARE_DELAY_SECONDS = 0.0, ANTENNA_ID = null) {
        this.HARDWARE_ID = HARDWARE_ID;
        this.FREQUENCY_HZ = FREQUENCY_HZ;
        this.POWER_WATTS = POWER_WATTS;
        this.MODULATION = MODULATION;
        this.HARDWARE_DELAY_SECONDS = HARDWARE_DELAY_SECONDS;
        this.ANTENNA_ID = ANTENNA_ID;
    }
    pack(builder) {
        const HARDWARE_ID = (this.HARDWARE_ID !== null ? builder.createString(this.HARDWARE_ID) : 0);
        const MODULATION = (this.MODULATION !== null ? builder.createString(this.MODULATION) : 0);
        const ANTENNA_ID = (this.ANTENNA_ID !== null ? builder.createString(this.ANTENNA_ID) : 0);
        return TRHTransmitter.createTRHTransmitter(builder, HARDWARE_ID, this.FREQUENCY_HZ, this.POWER_WATTS, MODULATION, this.HARDWARE_DELAY_SECONDS, ANTENNA_ID);
    }
}
