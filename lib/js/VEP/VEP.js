// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { VEPPerformancePoint } from './VEPPerformancePoint.js';
import { VEPProvenance } from './VEPProvenance.js';
import { VEPReplenishment } from './VEPReplenishment.js';
import { trackEnvironment } from './trackEnvironment.js';
import { vepEnergyType } from './vepEnergyType.js';
import { vepInterpolation } from './vepInterpolation.js';
/**
 * Vehicle Endurance Profile
 */
export class VEP {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsVEP(bb, obj) {
        return (obj || new VEP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsVEP(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new VEP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$VEP');
    }
    ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    VEHICLE_CLASS_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    VEHICLE_TYPE_DESIGNATOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Domain the vehicle operates in. The `$TRK` enum reused verbatim;
     * ordinal 0 is SPACE, so it is defaulted to UNKNOWN explicitly.
     */
    ENVIRONMENT() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : trackEnvironment.UNKNOWN;
    }
    /**
     * Kind of energy store.
     */
    ENERGY_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : vepEnergyType.UNSPECIFIED;
    }
    /**
     * Usable energy store capacity, in CAPACITY_UNITS.
     */
    CAPACITY_VALUE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    CAPACITY_UNITS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Fraction of capacity [0-1] held as reserve; endurance computations
     * spend down to the reserve, never through it.
     */
    RESERVE_FRACTION() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * The burn-rate table, keyed regime x altitude x speed. Required, and
     * never a single collapsed scalar.
     */
    PERFORMANCE_POINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? (obj || new VEPPerformancePoint()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    performancePointsLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * How a consumer interpolates between the points.
     */
    INTERPOLATION() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : vepInterpolation.UNSPECIFIED;
    }
    /**
     * Maximum endurance, seconds, as the source publishes it.
     */
    MAXIMUM_ENDURANCE_S() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Maximum range, metres, as the source publishes it.
     */
    MAXIMUM_RANGE_M() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Best-loiter speed, metres per second.
     */
    LOITER_SPEED_MPS() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Service ceiling, metres above mean sea level.
     */
    SERVICE_CEILING_M() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Replenishment options available to this class or type.
     */
    REPLENISHMENT(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? (obj || new VEPReplenishment()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    replenishmentLength() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    REFINES_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Dataset lineage and licence. Required.
     */
    SOURCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? (obj || new VEPProvenance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startVEP(builder) {
        builder.startObject(17);
    }
    static addId(builder, IDOffset) {
        builder.addFieldOffset(0, IDOffset, 0);
    }
    static addVehicleClassId(builder, VEHICLE_CLASS_IDOffset) {
        builder.addFieldOffset(1, VEHICLE_CLASS_IDOffset, 0);
    }
    static addVehicleTypeDesignator(builder, VEHICLE_TYPE_DESIGNATOROffset) {
        builder.addFieldOffset(2, VEHICLE_TYPE_DESIGNATOROffset, 0);
    }
    static addEnvironment(builder, ENVIRONMENT) {
        builder.addFieldInt8(3, ENVIRONMENT, trackEnvironment.UNKNOWN);
    }
    static addEnergyType(builder, ENERGY_TYPE) {
        builder.addFieldInt8(4, ENERGY_TYPE, vepEnergyType.UNSPECIFIED);
    }
    static addCapacityValue(builder, CAPACITY_VALUE) {
        builder.addFieldFloat64(5, CAPACITY_VALUE, 0.0);
    }
    static addCapacityUnits(builder, CAPACITY_UNITSOffset) {
        builder.addFieldOffset(6, CAPACITY_UNITSOffset, 0);
    }
    static addReserveFraction(builder, RESERVE_FRACTION) {
        builder.addFieldFloat64(7, RESERVE_FRACTION, 0.0);
    }
    static addPerformancePoints(builder, PERFORMANCE_POINTSOffset) {
        builder.addFieldOffset(8, PERFORMANCE_POINTSOffset, 0);
    }
    static createPerformancePointsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPerformancePointsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addInterpolation(builder, INTERPOLATION) {
        builder.addFieldInt8(9, INTERPOLATION, vepInterpolation.UNSPECIFIED);
    }
    static addMaximumEnduranceS(builder, MAXIMUM_ENDURANCE_S) {
        builder.addFieldFloat64(10, MAXIMUM_ENDURANCE_S, 0.0);
    }
    static addMaximumRangeM(builder, MAXIMUM_RANGE_M) {
        builder.addFieldFloat64(11, MAXIMUM_RANGE_M, 0.0);
    }
    static addLoiterSpeedMps(builder, LOITER_SPEED_MPS) {
        builder.addFieldFloat64(12, LOITER_SPEED_MPS, 0.0);
    }
    static addServiceCeilingM(builder, SERVICE_CEILING_M) {
        builder.addFieldFloat64(13, SERVICE_CEILING_M, 0.0);
    }
    static addReplenishment(builder, REPLENISHMENTOffset) {
        builder.addFieldOffset(14, REPLENISHMENTOffset, 0);
    }
    static createReplenishmentVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startReplenishmentVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addRefinesId(builder, REFINES_IDOffset) {
        builder.addFieldOffset(15, REFINES_IDOffset, 0);
    }
    static addSource(builder, SOURCEOffset) {
        builder.addFieldOffset(16, SOURCEOffset, 0);
    }
    static endVEP(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // ID
        builder.requiredField(offset, 6); // VEHICLE_CLASS_ID
        builder.requiredField(offset, 20); // PERFORMANCE_POINTS
        builder.requiredField(offset, 36); // SOURCE
        return offset;
    }
    static finishVEPBuffer(builder, offset) {
        builder.finish(offset, '$VEP');
    }
    static finishSizePrefixedVEPBuffer(builder, offset) {
        builder.finish(offset, '$VEP', true);
    }
    unpack() {
        return new VEPT(this.ID(), this.VEHICLE_CLASS_ID(), this.VEHICLE_TYPE_DESIGNATOR(), this.ENVIRONMENT(), this.ENERGY_TYPE(), this.CAPACITY_VALUE(), this.CAPACITY_UNITS(), this.RESERVE_FRACTION(), this.bb.createObjList(this.PERFORMANCE_POINTS.bind(this), this.performancePointsLength()), this.INTERPOLATION(), this.MAXIMUM_ENDURANCE_S(), this.MAXIMUM_RANGE_M(), this.LOITER_SPEED_MPS(), this.SERVICE_CEILING_M(), this.bb.createObjList(this.REPLENISHMENT.bind(this), this.replenishmentLength()), this.REFINES_ID(), (this.SOURCE() !== null ? this.SOURCE().unpack() : null));
    }
    unpackTo(_o) {
        _o.ID = this.ID();
        _o.VEHICLE_CLASS_ID = this.VEHICLE_CLASS_ID();
        _o.VEHICLE_TYPE_DESIGNATOR = this.VEHICLE_TYPE_DESIGNATOR();
        _o.ENVIRONMENT = this.ENVIRONMENT();
        _o.ENERGY_TYPE = this.ENERGY_TYPE();
        _o.CAPACITY_VALUE = this.CAPACITY_VALUE();
        _o.CAPACITY_UNITS = this.CAPACITY_UNITS();
        _o.RESERVE_FRACTION = this.RESERVE_FRACTION();
        _o.PERFORMANCE_POINTS = this.bb.createObjList(this.PERFORMANCE_POINTS.bind(this), this.performancePointsLength());
        _o.INTERPOLATION = this.INTERPOLATION();
        _o.MAXIMUM_ENDURANCE_S = this.MAXIMUM_ENDURANCE_S();
        _o.MAXIMUM_RANGE_M = this.MAXIMUM_RANGE_M();
        _o.LOITER_SPEED_MPS = this.LOITER_SPEED_MPS();
        _o.SERVICE_CEILING_M = this.SERVICE_CEILING_M();
        _o.REPLENISHMENT = this.bb.createObjList(this.REPLENISHMENT.bind(this), this.replenishmentLength());
        _o.REFINES_ID = this.REFINES_ID();
        _o.SOURCE = (this.SOURCE() !== null ? this.SOURCE().unpack() : null);
    }
}
export class VEPT {
    constructor(ID = null, VEHICLE_CLASS_ID = null, VEHICLE_TYPE_DESIGNATOR = null, ENVIRONMENT = trackEnvironment.UNKNOWN, ENERGY_TYPE = vepEnergyType.UNSPECIFIED, CAPACITY_VALUE = 0.0, CAPACITY_UNITS = null, RESERVE_FRACTION = 0.0, PERFORMANCE_POINTS = [], INTERPOLATION = vepInterpolation.UNSPECIFIED, MAXIMUM_ENDURANCE_S = 0.0, MAXIMUM_RANGE_M = 0.0, LOITER_SPEED_MPS = 0.0, SERVICE_CEILING_M = 0.0, REPLENISHMENT = [], REFINES_ID = null, SOURCE = null) {
        this.ID = ID;
        this.VEHICLE_CLASS_ID = VEHICLE_CLASS_ID;
        this.VEHICLE_TYPE_DESIGNATOR = VEHICLE_TYPE_DESIGNATOR;
        this.ENVIRONMENT = ENVIRONMENT;
        this.ENERGY_TYPE = ENERGY_TYPE;
        this.CAPACITY_VALUE = CAPACITY_VALUE;
        this.CAPACITY_UNITS = CAPACITY_UNITS;
        this.RESERVE_FRACTION = RESERVE_FRACTION;
        this.PERFORMANCE_POINTS = PERFORMANCE_POINTS;
        this.INTERPOLATION = INTERPOLATION;
        this.MAXIMUM_ENDURANCE_S = MAXIMUM_ENDURANCE_S;
        this.MAXIMUM_RANGE_M = MAXIMUM_RANGE_M;
        this.LOITER_SPEED_MPS = LOITER_SPEED_MPS;
        this.SERVICE_CEILING_M = SERVICE_CEILING_M;
        this.REPLENISHMENT = REPLENISHMENT;
        this.REFINES_ID = REFINES_ID;
        this.SOURCE = SOURCE;
    }
    pack(builder) {
        const ID = (this.ID !== null ? builder.createString(this.ID) : 0);
        const VEHICLE_CLASS_ID = (this.VEHICLE_CLASS_ID !== null ? builder.createString(this.VEHICLE_CLASS_ID) : 0);
        const VEHICLE_TYPE_DESIGNATOR = (this.VEHICLE_TYPE_DESIGNATOR !== null ? builder.createString(this.VEHICLE_TYPE_DESIGNATOR) : 0);
        const CAPACITY_UNITS = (this.CAPACITY_UNITS !== null ? builder.createString(this.CAPACITY_UNITS) : 0);
        const PERFORMANCE_POINTS = VEP.createPerformancePointsVector(builder, builder.createObjectOffsetList(this.PERFORMANCE_POINTS));
        const REPLENISHMENT = VEP.createReplenishmentVector(builder, builder.createObjectOffsetList(this.REPLENISHMENT));
        const REFINES_ID = (this.REFINES_ID !== null ? builder.createString(this.REFINES_ID) : 0);
        const SOURCE = (this.SOURCE !== null ? this.SOURCE.pack(builder) : 0);
        VEP.startVEP(builder);
        VEP.addId(builder, ID);
        VEP.addVehicleClassId(builder, VEHICLE_CLASS_ID);
        VEP.addVehicleTypeDesignator(builder, VEHICLE_TYPE_DESIGNATOR);
        VEP.addEnvironment(builder, this.ENVIRONMENT);
        VEP.addEnergyType(builder, this.ENERGY_TYPE);
        VEP.addCapacityValue(builder, this.CAPACITY_VALUE);
        VEP.addCapacityUnits(builder, CAPACITY_UNITS);
        VEP.addReserveFraction(builder, this.RESERVE_FRACTION);
        VEP.addPerformancePoints(builder, PERFORMANCE_POINTS);
        VEP.addInterpolation(builder, this.INTERPOLATION);
        VEP.addMaximumEnduranceS(builder, this.MAXIMUM_ENDURANCE_S);
        VEP.addMaximumRangeM(builder, this.MAXIMUM_RANGE_M);
        VEP.addLoiterSpeedMps(builder, this.LOITER_SPEED_MPS);
        VEP.addServiceCeilingM(builder, this.SERVICE_CEILING_M);
        VEP.addReplenishment(builder, REPLENISHMENT);
        VEP.addRefinesId(builder, REFINES_ID);
        VEP.addSource(builder, SOURCE);
        return VEP.endVEP(builder);
    }
}
