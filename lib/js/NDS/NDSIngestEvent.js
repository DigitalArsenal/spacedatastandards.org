// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { ndsIngestEventKind } from './ndsIngestEventKind.js';
/**
 * One source-ingest transition surfaced to the dashboard.
 */
export class NDSIngestEvent {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNDSIngestEvent(bb, obj) {
        return (obj || new NDSIngestEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNDSIngestEvent(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NDSIngestEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    KIND() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : ndsIngestEventKind.Stall;
    }
    SCHEMA(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROVIDER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix seconds when the transition occurred.
     */
    AT() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    static startNDSIngestEvent(builder) {
        builder.startObject(7);
    }
    static addKind(builder, KIND) {
        builder.addFieldInt8(0, KIND, ndsIngestEventKind.Stall);
    }
    static addSchema(builder, SCHEMAOffset) {
        builder.addFieldOffset(1, SCHEMAOffset, 0);
    }
    static addProviderId(builder, PROVIDER_IDOffset) {
        builder.addFieldOffset(2, PROVIDER_IDOffset, 0);
    }
    static addSourceName(builder, SOURCE_NAMEOffset) {
        builder.addFieldOffset(3, SOURCE_NAMEOffset, 0);
    }
    static addMessage(builder, MESSAGEOffset) {
        builder.addFieldOffset(4, MESSAGEOffset, 0);
    }
    static addCount(builder, COUNT) {
        builder.addFieldInt64(5, COUNT, BigInt('0'));
    }
    static addAt(builder, AT) {
        builder.addFieldInt64(6, AT, BigInt('0'));
    }
    static endNDSIngestEvent(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNDSIngestEvent(builder, KIND, SCHEMAOffset, PROVIDER_IDOffset, SOURCE_NAMEOffset, MESSAGEOffset, COUNT, AT) {
        NDSIngestEvent.startNDSIngestEvent(builder);
        NDSIngestEvent.addKind(builder, KIND);
        NDSIngestEvent.addSchema(builder, SCHEMAOffset);
        NDSIngestEvent.addProviderId(builder, PROVIDER_IDOffset);
        NDSIngestEvent.addSourceName(builder, SOURCE_NAMEOffset);
        NDSIngestEvent.addMessage(builder, MESSAGEOffset);
        NDSIngestEvent.addCount(builder, COUNT);
        NDSIngestEvent.addAt(builder, AT);
        return NDSIngestEvent.endNDSIngestEvent(builder);
    }
    unpack() {
        return new NDSIngestEventT(this.KIND(), this.SCHEMA(), this.PROVIDER_ID(), this.SOURCE_NAME(), this.MESSAGE(), this.COUNT(), this.AT());
    }
    unpackTo(_o) {
        _o.KIND = this.KIND();
        _o.SCHEMA = this.SCHEMA();
        _o.PROVIDER_ID = this.PROVIDER_ID();
        _o.SOURCE_NAME = this.SOURCE_NAME();
        _o.MESSAGE = this.MESSAGE();
        _o.COUNT = this.COUNT();
        _o.AT = this.AT();
    }
}
export class NDSIngestEventT {
    constructor(KIND = ndsIngestEventKind.Stall, SCHEMA = null, PROVIDER_ID = null, SOURCE_NAME = null, MESSAGE = null, COUNT = BigInt('0'), AT = BigInt('0')) {
        this.KIND = KIND;
        this.SCHEMA = SCHEMA;
        this.PROVIDER_ID = PROVIDER_ID;
        this.SOURCE_NAME = SOURCE_NAME;
        this.MESSAGE = MESSAGE;
        this.COUNT = COUNT;
        this.AT = AT;
    }
    pack(builder) {
        const SCHEMA = (this.SCHEMA !== null ? builder.createString(this.SCHEMA) : 0);
        const PROVIDER_ID = (this.PROVIDER_ID !== null ? builder.createString(this.PROVIDER_ID) : 0);
        const SOURCE_NAME = (this.SOURCE_NAME !== null ? builder.createString(this.SOURCE_NAME) : 0);
        const MESSAGE = (this.MESSAGE !== null ? builder.createString(this.MESSAGE) : 0);
        return NDSIngestEvent.createNDSIngestEvent(builder, this.KIND, SCHEMA, PROVIDER_ID, SOURCE_NAME, MESSAGE, this.COUNT, this.AT);
    }
}
