// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One standard's record footprint in the node store.
 */
export class NDSSchemaStat {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNDSSchemaStat(bb, obj) {
        return (obj || new NDSSchemaStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNDSSchemaStat(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NDSSchemaStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SCHEMA(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RECORD_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    TOTAL_BYTES() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Newest record epoch held, Unix milliseconds; 0 = unknown.
     */
    NEWEST_EPOCH_MS() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Oldest record epoch held, Unix milliseconds; 0 = unknown.
     */
    OLDEST_EPOCH_MS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    static startNDSSchemaStat(builder) {
        builder.startObject(5);
    }
    static addSchema(builder, SCHEMAOffset) {
        builder.addFieldOffset(0, SCHEMAOffset, 0);
    }
    static addRecordCount(builder, RECORD_COUNT) {
        builder.addFieldInt64(1, RECORD_COUNT, BigInt('0'));
    }
    static addTotalBytes(builder, TOTAL_BYTES) {
        builder.addFieldInt64(2, TOTAL_BYTES, BigInt('0'));
    }
    static addNewestEpochMs(builder, NEWEST_EPOCH_MS) {
        builder.addFieldInt64(3, NEWEST_EPOCH_MS, BigInt('0'));
    }
    static addOldestEpochMs(builder, OLDEST_EPOCH_MS) {
        builder.addFieldInt64(4, OLDEST_EPOCH_MS, BigInt('0'));
    }
    static endNDSSchemaStat(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNDSSchemaStat(builder, SCHEMAOffset, RECORD_COUNT, TOTAL_BYTES, NEWEST_EPOCH_MS, OLDEST_EPOCH_MS) {
        NDSSchemaStat.startNDSSchemaStat(builder);
        NDSSchemaStat.addSchema(builder, SCHEMAOffset);
        NDSSchemaStat.addRecordCount(builder, RECORD_COUNT);
        NDSSchemaStat.addTotalBytes(builder, TOTAL_BYTES);
        NDSSchemaStat.addNewestEpochMs(builder, NEWEST_EPOCH_MS);
        NDSSchemaStat.addOldestEpochMs(builder, OLDEST_EPOCH_MS);
        return NDSSchemaStat.endNDSSchemaStat(builder);
    }
    unpack() {
        return new NDSSchemaStatT(this.SCHEMA(), this.RECORD_COUNT(), this.TOTAL_BYTES(), this.NEWEST_EPOCH_MS(), this.OLDEST_EPOCH_MS());
    }
    unpackTo(_o) {
        _o.SCHEMA = this.SCHEMA();
        _o.RECORD_COUNT = this.RECORD_COUNT();
        _o.TOTAL_BYTES = this.TOTAL_BYTES();
        _o.NEWEST_EPOCH_MS = this.NEWEST_EPOCH_MS();
        _o.OLDEST_EPOCH_MS = this.OLDEST_EPOCH_MS();
    }
}
export class NDSSchemaStatT {
    constructor(SCHEMA = null, RECORD_COUNT = BigInt('0'), TOTAL_BYTES = BigInt('0'), NEWEST_EPOCH_MS = BigInt('0'), OLDEST_EPOCH_MS = BigInt('0')) {
        this.SCHEMA = SCHEMA;
        this.RECORD_COUNT = RECORD_COUNT;
        this.TOTAL_BYTES = TOTAL_BYTES;
        this.NEWEST_EPOCH_MS = NEWEST_EPOCH_MS;
        this.OLDEST_EPOCH_MS = OLDEST_EPOCH_MS;
    }
    pack(builder) {
        const SCHEMA = (this.SCHEMA !== null ? builder.createString(this.SCHEMA) : 0);
        return NDSSchemaStat.createNDSSchemaStat(builder, SCHEMA, this.RECORD_COUNT, this.TOTAL_BYTES, this.NEWEST_EPOCH_MS, this.OLDEST_EPOCH_MS);
    }
}
