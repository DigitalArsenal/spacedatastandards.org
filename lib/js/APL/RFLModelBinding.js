// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { RFLModelParameter } from './RFLModelParameter.js';
import { rflBudgetTerm } from './rflBudgetTerm.js';
/**
 * Which model produced one term of the budget. A loss term without a model
 * binding is an unattributed number: rain attenuation from two different
 * recommendations differs by decibels, and a consumer must be able to tell
 * which one it is holding.
 */
export class RFLModelBinding {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsRFLModelBinding(bb, obj) {
        return (obj || new RFLModelBinding()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsRFLModelBinding(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new RFLModelBinding()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * The budget term this binding produced.
     */
    TERM() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : rflBudgetTerm.UNSPECIFIED;
    }
    MODEL_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    MODEL_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MODULE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MODULE_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MODULE_CONTENT_HASH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PARAMETERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new RFLModelParameter()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    parametersLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startRFLModelBinding(builder) {
        builder.startObject(7);
    }
    static addTerm(builder, TERM) {
        builder.addFieldInt8(0, TERM, rflBudgetTerm.UNSPECIFIED);
    }
    static addModelName(builder, MODEL_NAMEOffset) {
        builder.addFieldOffset(1, MODEL_NAMEOffset, 0);
    }
    static addModelVersion(builder, MODEL_VERSIONOffset) {
        builder.addFieldOffset(2, MODEL_VERSIONOffset, 0);
    }
    static addModuleId(builder, MODULE_IDOffset) {
        builder.addFieldOffset(3, MODULE_IDOffset, 0);
    }
    static addModuleVersion(builder, MODULE_VERSIONOffset) {
        builder.addFieldOffset(4, MODULE_VERSIONOffset, 0);
    }
    static addModuleContentHash(builder, MODULE_CONTENT_HASHOffset) {
        builder.addFieldOffset(5, MODULE_CONTENT_HASHOffset, 0);
    }
    static addParameters(builder, PARAMETERSOffset) {
        builder.addFieldOffset(6, PARAMETERSOffset, 0);
    }
    static createParametersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startParametersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endRFLModelBinding(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // MODEL_NAME
        return offset;
    }
    static createRFLModelBinding(builder, TERM, MODEL_NAMEOffset, MODEL_VERSIONOffset, MODULE_IDOffset, MODULE_VERSIONOffset, MODULE_CONTENT_HASHOffset, PARAMETERSOffset) {
        RFLModelBinding.startRFLModelBinding(builder);
        RFLModelBinding.addTerm(builder, TERM);
        RFLModelBinding.addModelName(builder, MODEL_NAMEOffset);
        RFLModelBinding.addModelVersion(builder, MODEL_VERSIONOffset);
        RFLModelBinding.addModuleId(builder, MODULE_IDOffset);
        RFLModelBinding.addModuleVersion(builder, MODULE_VERSIONOffset);
        RFLModelBinding.addModuleContentHash(builder, MODULE_CONTENT_HASHOffset);
        RFLModelBinding.addParameters(builder, PARAMETERSOffset);
        return RFLModelBinding.endRFLModelBinding(builder);
    }
    unpack() {
        return new RFLModelBindingT(this.TERM(), this.MODEL_NAME(), this.MODEL_VERSION(), this.MODULE_ID(), this.MODULE_VERSION(), this.MODULE_CONTENT_HASH(), this.bb.createObjList(this.PARAMETERS.bind(this), this.parametersLength()));
    }
    unpackTo(_o) {
        _o.TERM = this.TERM();
        _o.MODEL_NAME = this.MODEL_NAME();
        _o.MODEL_VERSION = this.MODEL_VERSION();
        _o.MODULE_ID = this.MODULE_ID();
        _o.MODULE_VERSION = this.MODULE_VERSION();
        _o.MODULE_CONTENT_HASH = this.MODULE_CONTENT_HASH();
        _o.PARAMETERS = this.bb.createObjList(this.PARAMETERS.bind(this), this.parametersLength());
    }
}
export class RFLModelBindingT {
    constructor(TERM = rflBudgetTerm.UNSPECIFIED, MODEL_NAME = null, MODEL_VERSION = null, MODULE_ID = null, MODULE_VERSION = null, MODULE_CONTENT_HASH = null, PARAMETERS = []) {
        this.TERM = TERM;
        this.MODEL_NAME = MODEL_NAME;
        this.MODEL_VERSION = MODEL_VERSION;
        this.MODULE_ID = MODULE_ID;
        this.MODULE_VERSION = MODULE_VERSION;
        this.MODULE_CONTENT_HASH = MODULE_CONTENT_HASH;
        this.PARAMETERS = PARAMETERS;
    }
    pack(builder) {
        const MODEL_NAME = (this.MODEL_NAME !== null ? builder.createString(this.MODEL_NAME) : 0);
        const MODEL_VERSION = (this.MODEL_VERSION !== null ? builder.createString(this.MODEL_VERSION) : 0);
        const MODULE_ID = (this.MODULE_ID !== null ? builder.createString(this.MODULE_ID) : 0);
        const MODULE_VERSION = (this.MODULE_VERSION !== null ? builder.createString(this.MODULE_VERSION) : 0);
        const MODULE_CONTENT_HASH = (this.MODULE_CONTENT_HASH !== null ? builder.createString(this.MODULE_CONTENT_HASH) : 0);
        const PARAMETERS = RFLModelBinding.createParametersVector(builder, builder.createObjectOffsetList(this.PARAMETERS));
        return RFLModelBinding.createRFLModelBinding(builder, this.TERM, MODEL_NAME, MODEL_VERSION, MODULE_ID, MODULE_VERSION, MODULE_CONTENT_HASH, PARAMETERS);
    }
}
