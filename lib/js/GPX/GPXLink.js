// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * GPX link to an external resource
 */
export class GPXLink {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGPXLink(bb, obj) {
        return (obj || new GPXLink()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGPXLink(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GPXLink()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    HREF(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TEXT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startGPXLink(builder) {
        builder.startObject(3);
    }
    static addHref(builder, HREFOffset) {
        builder.addFieldOffset(0, HREFOffset, 0);
    }
    static addText(builder, TEXTOffset) {
        builder.addFieldOffset(1, TEXTOffset, 0);
    }
    static addType(builder, TYPEOffset) {
        builder.addFieldOffset(2, TYPEOffset, 0);
    }
    static endGPXLink(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGPXLink(builder, HREFOffset, TEXTOffset, TYPEOffset) {
        GPXLink.startGPXLink(builder);
        GPXLink.addHref(builder, HREFOffset);
        GPXLink.addText(builder, TEXTOffset);
        GPXLink.addType(builder, TYPEOffset);
        return GPXLink.endGPXLink(builder);
    }
    unpack() {
        return new GPXLinkT(this.HREF(), this.TEXT(), this.TYPE());
    }
    unpackTo(_o) {
        _o.HREF = this.HREF();
        _o.TEXT = this.TEXT();
        _o.TYPE = this.TYPE();
    }
}
export class GPXLinkT {
    constructor(HREF = null, TEXT = null, TYPE = null) {
        this.HREF = HREF;
        this.TEXT = TEXT;
        this.TYPE = TYPE;
    }
    pack(builder) {
        const HREF = (this.HREF !== null ? builder.createString(this.HREF) : 0);
        const TEXT = (this.TEXT !== null ? builder.createString(this.TEXT) : 0);
        const TYPE = (this.TYPE !== null ? builder.createString(this.TYPE) : 0);
        return GPXLink.createGPXLink(builder, HREF, TEXT, TYPE);
    }
}
