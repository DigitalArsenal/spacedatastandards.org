// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GPXWaypoint } from './GPXWaypoint.js';
/**
 * Track segment (continuous span of track points)
 */
export class GPXTrackSegment {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGPXTrackSegment(bb, obj) {
        return (obj || new GPXTrackSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGPXTrackSegment(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GPXTrackSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Ordered track points in this segment
     */
    POINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new GPXWaypoint()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    pointsLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startGPXTrackSegment(builder) {
        builder.startObject(1);
    }
    static addPoints(builder, POINTSOffset) {
        builder.addFieldOffset(0, POINTSOffset, 0);
    }
    static createPointsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPointsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endGPXTrackSegment(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGPXTrackSegment(builder, POINTSOffset) {
        GPXTrackSegment.startGPXTrackSegment(builder);
        GPXTrackSegment.addPoints(builder, POINTSOffset);
        return GPXTrackSegment.endGPXTrackSegment(builder);
    }
    unpack() {
        return new GPXTrackSegmentT(this.bb.createObjList(this.POINTS.bind(this), this.pointsLength()));
    }
    unpackTo(_o) {
        _o.POINTS = this.bb.createObjList(this.POINTS.bind(this), this.pointsLength());
    }
}
export class GPXTrackSegmentT {
    constructor(POINTS = []) {
        this.POINTS = POINTS;
    }
    pack(builder) {
        const POINTS = GPXTrackSegment.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));
        return GPXTrackSegment.createGPXTrackSegment(builder, POINTS);
    }
}
