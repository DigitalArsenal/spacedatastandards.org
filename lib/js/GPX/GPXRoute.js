// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { GPXLink } from './GPXLink.js';
import { GPXWaypoint } from './GPXWaypoint.js';
/**
 * Route (ordered list of waypoints for navigation)
 */
export class GPXRoute {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsGPXRoute(bb, obj) {
        return (obj || new GPXRoute()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsGPXRoute(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new GPXRoute()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    COMMENT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Links to additional information
     */
    LINKS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new GPXLink()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    linksLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Route number
     */
    NUMBER() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Route points (ordered waypoints)
     */
    POINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new GPXWaypoint()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    pointsLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startGPXRoute(builder) {
        builder.startObject(8);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addComment(builder, COMMENTOffset) {
        builder.addFieldOffset(1, COMMENTOffset, 0);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(2, DESCRIPTIONOffset, 0);
    }
    static addSource(builder, SOURCEOffset) {
        builder.addFieldOffset(3, SOURCEOffset, 0);
    }
    static addLinks(builder, LINKSOffset) {
        builder.addFieldOffset(4, LINKSOffset, 0);
    }
    static createLinksVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startLinksVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addNumber(builder, NUMBER) {
        builder.addFieldInt32(5, NUMBER, 0);
    }
    static addType(builder, TYPEOffset) {
        builder.addFieldOffset(6, TYPEOffset, 0);
    }
    static addPoints(builder, POINTSOffset) {
        builder.addFieldOffset(7, POINTSOffset, 0);
    }
    static createPointsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPointsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endGPXRoute(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createGPXRoute(builder, NAMEOffset, COMMENTOffset, DESCRIPTIONOffset, SOURCEOffset, LINKSOffset, NUMBER, TYPEOffset, POINTSOffset) {
        GPXRoute.startGPXRoute(builder);
        GPXRoute.addName(builder, NAMEOffset);
        GPXRoute.addComment(builder, COMMENTOffset);
        GPXRoute.addDescription(builder, DESCRIPTIONOffset);
        GPXRoute.addSource(builder, SOURCEOffset);
        GPXRoute.addLinks(builder, LINKSOffset);
        GPXRoute.addNumber(builder, NUMBER);
        GPXRoute.addType(builder, TYPEOffset);
        GPXRoute.addPoints(builder, POINTSOffset);
        return GPXRoute.endGPXRoute(builder);
    }
    unpack() {
        return new GPXRouteT(this.NAME(), this.COMMENT(), this.DESCRIPTION(), this.SOURCE(), this.bb.createObjList(this.LINKS.bind(this), this.linksLength()), this.NUMBER(), this.TYPE(), this.bb.createObjList(this.POINTS.bind(this), this.pointsLength()));
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.COMMENT = this.COMMENT();
        _o.DESCRIPTION = this.DESCRIPTION();
        _o.SOURCE = this.SOURCE();
        _o.LINKS = this.bb.createObjList(this.LINKS.bind(this), this.linksLength());
        _o.NUMBER = this.NUMBER();
        _o.TYPE = this.TYPE();
        _o.POINTS = this.bb.createObjList(this.POINTS.bind(this), this.pointsLength());
    }
}
export class GPXRouteT {
    constructor(NAME = null, COMMENT = null, DESCRIPTION = null, SOURCE = null, LINKS = [], NUMBER = 0, TYPE = null, POINTS = []) {
        this.NAME = NAME;
        this.COMMENT = COMMENT;
        this.DESCRIPTION = DESCRIPTION;
        this.SOURCE = SOURCE;
        this.LINKS = LINKS;
        this.NUMBER = NUMBER;
        this.TYPE = TYPE;
        this.POINTS = POINTS;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const COMMENT = (this.COMMENT !== null ? builder.createString(this.COMMENT) : 0);
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        const SOURCE = (this.SOURCE !== null ? builder.createString(this.SOURCE) : 0);
        const LINKS = GPXRoute.createLinksVector(builder, builder.createObjectOffsetList(this.LINKS));
        const TYPE = (this.TYPE !== null ? builder.createString(this.TYPE) : 0);
        const POINTS = GPXRoute.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));
        return GPXRoute.createGPXRoute(builder, NAME, COMMENT, DESCRIPTION, SOURCE, LINKS, this.NUMBER, TYPE, POINTS);
    }
}
