// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { licensingWrappedKeyAlgorithm } from './licensingWrappedKeyAlgorithm.js';
/**
 * Wrapped module content key
 */
export class LWK {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsLWK(bb, obj) {
        return (obj || new LWK()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsLWK(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new LWK()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$LWK');
    }
    REQUEST_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    MODULE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    MODULE_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CONTENT_KEY_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RECIPIENT_KEY_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Wrapped-key algorithm
     */
    ALGORITHM() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : licensingWrappedKeyAlgorithm.X25519_HKDF_SHA256_AES_256_GCM;
    }
    /**
     * Requester ephemeral X25519 public key
     */
    REQUESTER_EPHEMERAL_PUBKEY(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    requesterEphemeralPubkeyLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    requesterEphemeralPubkeyArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Provider ephemeral X25519 public key
     */
    PROVIDER_EPHEMERAL_PUBKEY(index) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    providerEphemeralPubkeyLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    providerEphemeralPubkeyArray() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * HKDF salt
     */
    HKDF_SALT(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    hkdfSaltLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    hkdfSaltArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * AES-GCM IV
     */
    IV(index) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    ivLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ivArray() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Wrapped key ciphertext
     */
    CIPHERTEXT(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    ciphertextLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ciphertextArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * AES-GCM authentication tag
     */
    TAG(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    tagLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    tagArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Envelope expiration time in milliseconds since epoch
     */
    EXPIRES_AT() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    static startLWK(builder) {
        builder.startObject(13);
    }
    static addRequestId(builder, REQUEST_IDOffset) {
        builder.addFieldOffset(0, REQUEST_IDOffset, 0);
    }
    static addModuleId(builder, MODULE_IDOffset) {
        builder.addFieldOffset(1, MODULE_IDOffset, 0);
    }
    static addModuleVersion(builder, MODULE_VERSIONOffset) {
        builder.addFieldOffset(2, MODULE_VERSIONOffset, 0);
    }
    static addContentKeyId(builder, CONTENT_KEY_IDOffset) {
        builder.addFieldOffset(3, CONTENT_KEY_IDOffset, 0);
    }
    static addRecipientKeyId(builder, RECIPIENT_KEY_IDOffset) {
        builder.addFieldOffset(4, RECIPIENT_KEY_IDOffset, 0);
    }
    static addAlgorithm(builder, ALGORITHM) {
        builder.addFieldInt8(5, ALGORITHM, licensingWrappedKeyAlgorithm.X25519_HKDF_SHA256_AES_256_GCM);
    }
    static addRequesterEphemeralPubkey(builder, REQUESTER_EPHEMERAL_PUBKEYOffset) {
        builder.addFieldOffset(6, REQUESTER_EPHEMERAL_PUBKEYOffset, 0);
    }
    static createRequesterEphemeralPubkeyVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startRequesterEphemeralPubkeyVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addProviderEphemeralPubkey(builder, PROVIDER_EPHEMERAL_PUBKEYOffset) {
        builder.addFieldOffset(7, PROVIDER_EPHEMERAL_PUBKEYOffset, 0);
    }
    static createProviderEphemeralPubkeyVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startProviderEphemeralPubkeyVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addHkdfSalt(builder, HKDF_SALTOffset) {
        builder.addFieldOffset(8, HKDF_SALTOffset, 0);
    }
    static createHkdfSaltVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startHkdfSaltVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addIv(builder, IVOffset) {
        builder.addFieldOffset(9, IVOffset, 0);
    }
    static createIvVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startIvVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addCiphertext(builder, CIPHERTEXTOffset) {
        builder.addFieldOffset(10, CIPHERTEXTOffset, 0);
    }
    static createCiphertextVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startCiphertextVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addTag(builder, TAGOffset) {
        builder.addFieldOffset(11, TAGOffset, 0);
    }
    static createTagVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startTagVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addExpiresAt(builder, EXPIRES_AT) {
        builder.addFieldInt64(12, EXPIRES_AT, BigInt('0'));
    }
    static endLWK(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // REQUEST_ID
        builder.requiredField(offset, 6); // MODULE_ID
        return offset;
    }
    static finishLWKBuffer(builder, offset) {
        builder.finish(offset, '$LWK');
    }
    static finishSizePrefixedLWKBuffer(builder, offset) {
        builder.finish(offset, '$LWK', true);
    }
    static createLWK(builder, REQUEST_IDOffset, MODULE_IDOffset, MODULE_VERSIONOffset, CONTENT_KEY_IDOffset, RECIPIENT_KEY_IDOffset, ALGORITHM, REQUESTER_EPHEMERAL_PUBKEYOffset, PROVIDER_EPHEMERAL_PUBKEYOffset, HKDF_SALTOffset, IVOffset, CIPHERTEXTOffset, TAGOffset, EXPIRES_AT) {
        LWK.startLWK(builder);
        LWK.addRequestId(builder, REQUEST_IDOffset);
        LWK.addModuleId(builder, MODULE_IDOffset);
        LWK.addModuleVersion(builder, MODULE_VERSIONOffset);
        LWK.addContentKeyId(builder, CONTENT_KEY_IDOffset);
        LWK.addRecipientKeyId(builder, RECIPIENT_KEY_IDOffset);
        LWK.addAlgorithm(builder, ALGORITHM);
        LWK.addRequesterEphemeralPubkey(builder, REQUESTER_EPHEMERAL_PUBKEYOffset);
        LWK.addProviderEphemeralPubkey(builder, PROVIDER_EPHEMERAL_PUBKEYOffset);
        LWK.addHkdfSalt(builder, HKDF_SALTOffset);
        LWK.addIv(builder, IVOffset);
        LWK.addCiphertext(builder, CIPHERTEXTOffset);
        LWK.addTag(builder, TAGOffset);
        LWK.addExpiresAt(builder, EXPIRES_AT);
        return LWK.endLWK(builder);
    }
    unpack() {
        return new LWKT(this.REQUEST_ID(), this.MODULE_ID(), this.MODULE_VERSION(), this.CONTENT_KEY_ID(), this.RECIPIENT_KEY_ID(), this.ALGORITHM(), this.bb.createScalarList(this.REQUESTER_EPHEMERAL_PUBKEY.bind(this), this.requesterEphemeralPubkeyLength()), this.bb.createScalarList(this.PROVIDER_EPHEMERAL_PUBKEY.bind(this), this.providerEphemeralPubkeyLength()), this.bb.createScalarList(this.HKDF_SALT.bind(this), this.hkdfSaltLength()), this.bb.createScalarList(this.IV.bind(this), this.ivLength()), this.bb.createScalarList(this.CIPHERTEXT.bind(this), this.ciphertextLength()), this.bb.createScalarList(this.TAG.bind(this), this.tagLength()), this.EXPIRES_AT());
    }
    unpackTo(_o) {
        _o.REQUEST_ID = this.REQUEST_ID();
        _o.MODULE_ID = this.MODULE_ID();
        _o.MODULE_VERSION = this.MODULE_VERSION();
        _o.CONTENT_KEY_ID = this.CONTENT_KEY_ID();
        _o.RECIPIENT_KEY_ID = this.RECIPIENT_KEY_ID();
        _o.ALGORITHM = this.ALGORITHM();
        _o.REQUESTER_EPHEMERAL_PUBKEY = this.bb.createScalarList(this.REQUESTER_EPHEMERAL_PUBKEY.bind(this), this.requesterEphemeralPubkeyLength());
        _o.PROVIDER_EPHEMERAL_PUBKEY = this.bb.createScalarList(this.PROVIDER_EPHEMERAL_PUBKEY.bind(this), this.providerEphemeralPubkeyLength());
        _o.HKDF_SALT = this.bb.createScalarList(this.HKDF_SALT.bind(this), this.hkdfSaltLength());
        _o.IV = this.bb.createScalarList(this.IV.bind(this), this.ivLength());
        _o.CIPHERTEXT = this.bb.createScalarList(this.CIPHERTEXT.bind(this), this.ciphertextLength());
        _o.TAG = this.bb.createScalarList(this.TAG.bind(this), this.tagLength());
        _o.EXPIRES_AT = this.EXPIRES_AT();
    }
}
export class LWKT {
    constructor(REQUEST_ID = null, MODULE_ID = null, MODULE_VERSION = null, CONTENT_KEY_ID = null, RECIPIENT_KEY_ID = null, ALGORITHM = licensingWrappedKeyAlgorithm.X25519_HKDF_SHA256_AES_256_GCM, REQUESTER_EPHEMERAL_PUBKEY = [], PROVIDER_EPHEMERAL_PUBKEY = [], HKDF_SALT = [], IV = [], CIPHERTEXT = [], TAG = [], EXPIRES_AT = BigInt('0')) {
        this.REQUEST_ID = REQUEST_ID;
        this.MODULE_ID = MODULE_ID;
        this.MODULE_VERSION = MODULE_VERSION;
        this.CONTENT_KEY_ID = CONTENT_KEY_ID;
        this.RECIPIENT_KEY_ID = RECIPIENT_KEY_ID;
        this.ALGORITHM = ALGORITHM;
        this.REQUESTER_EPHEMERAL_PUBKEY = REQUESTER_EPHEMERAL_PUBKEY;
        this.PROVIDER_EPHEMERAL_PUBKEY = PROVIDER_EPHEMERAL_PUBKEY;
        this.HKDF_SALT = HKDF_SALT;
        this.IV = IV;
        this.CIPHERTEXT = CIPHERTEXT;
        this.TAG = TAG;
        this.EXPIRES_AT = EXPIRES_AT;
    }
    pack(builder) {
        const REQUEST_ID = (this.REQUEST_ID !== null ? builder.createString(this.REQUEST_ID) : 0);
        const MODULE_ID = (this.MODULE_ID !== null ? builder.createString(this.MODULE_ID) : 0);
        const MODULE_VERSION = (this.MODULE_VERSION !== null ? builder.createString(this.MODULE_VERSION) : 0);
        const CONTENT_KEY_ID = (this.CONTENT_KEY_ID !== null ? builder.createString(this.CONTENT_KEY_ID) : 0);
        const RECIPIENT_KEY_ID = (this.RECIPIENT_KEY_ID !== null ? builder.createString(this.RECIPIENT_KEY_ID) : 0);
        const REQUESTER_EPHEMERAL_PUBKEY = LWK.createRequesterEphemeralPubkeyVector(builder, this.REQUESTER_EPHEMERAL_PUBKEY);
        const PROVIDER_EPHEMERAL_PUBKEY = LWK.createProviderEphemeralPubkeyVector(builder, this.PROVIDER_EPHEMERAL_PUBKEY);
        const HKDF_SALT = LWK.createHkdfSaltVector(builder, this.HKDF_SALT);
        const IV = LWK.createIvVector(builder, this.IV);
        const CIPHERTEXT = LWK.createCiphertextVector(builder, this.CIPHERTEXT);
        const TAG = LWK.createTagVector(builder, this.TAG);
        return LWK.createLWK(builder, REQUEST_ID, MODULE_ID, MODULE_VERSION, CONTENT_KEY_ID, RECIPIENT_KEY_ID, this.ALGORITHM, REQUESTER_EPHEMERAL_PUBKEY, PROVIDER_EPHEMERAL_PUBKEY, HKDF_SALT, IV, CIPHERTEXT, TAG, this.EXPIRES_AT);
    }
}
