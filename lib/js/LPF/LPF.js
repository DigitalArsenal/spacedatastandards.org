// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { licensingProofMessageType } from './licensingProofMessageType.js';
/**
 * Licensing proof message
 */
export class LPF {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsLPF(bb, obj) {
        return (obj || new LPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsLPF(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new LPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$LPF');
    }
    /**
     * Message type
     */
    MESSAGE_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : licensingProofMessageType.ProofRequest;
    }
    REQUEST_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MODULE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MODULE_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    REQUESTER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    REQUESTER_XPUB(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    REQUESTED_DOMAIN(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Requested timeout in milliseconds
     */
    REQUESTED_TIMEOUT_MS() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Requester ephemeral X25519 public key
     */
    REQUESTER_EPHEMERAL_PUBKEY(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    requesterEphemeralPubkeyLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    requesterEphemeralPubkeyArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Provider-issued challenge nonce being proved
     */
    CHALLENGE_NONCE(index) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    challengeNonceLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    challengeNonceArray() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Challenge expiration time being acknowledged
     */
    CHALLENGE_EXPIRES_AT() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    PROVIDER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Detached requester signature over the challenge payload
     */
    SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Requester signing public key
     */
    SIGNING_PUBKEY(index) {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signingPubkeyLength() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signingPubkeyArray() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Proof timestamp in milliseconds since epoch
     */
    TIMESTAMP_MS() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    REJECTION_CODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    REJECTION_MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startLPF(builder) {
        builder.startObject(17);
    }
    static addMessageType(builder, MESSAGE_TYPE) {
        builder.addFieldInt8(0, MESSAGE_TYPE, licensingProofMessageType.ProofRequest);
    }
    static addRequestId(builder, REQUEST_IDOffset) {
        builder.addFieldOffset(1, REQUEST_IDOffset, 0);
    }
    static addModuleId(builder, MODULE_IDOffset) {
        builder.addFieldOffset(2, MODULE_IDOffset, 0);
    }
    static addModuleVersion(builder, MODULE_VERSIONOffset) {
        builder.addFieldOffset(3, MODULE_VERSIONOffset, 0);
    }
    static addRequesterPeerId(builder, REQUESTER_PEER_IDOffset) {
        builder.addFieldOffset(4, REQUESTER_PEER_IDOffset, 0);
    }
    static addRequesterXpub(builder, REQUESTER_XPUBOffset) {
        builder.addFieldOffset(5, REQUESTER_XPUBOffset, 0);
    }
    static addRequestedDomain(builder, REQUESTED_DOMAINOffset) {
        builder.addFieldOffset(6, REQUESTED_DOMAINOffset, 0);
    }
    static addRequestedTimeoutMs(builder, REQUESTED_TIMEOUT_MS) {
        builder.addFieldInt64(7, REQUESTED_TIMEOUT_MS, BigInt('0'));
    }
    static addRequesterEphemeralPubkey(builder, REQUESTER_EPHEMERAL_PUBKEYOffset) {
        builder.addFieldOffset(8, REQUESTER_EPHEMERAL_PUBKEYOffset, 0);
    }
    static createRequesterEphemeralPubkeyVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startRequesterEphemeralPubkeyVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addChallengeNonce(builder, CHALLENGE_NONCEOffset) {
        builder.addFieldOffset(9, CHALLENGE_NONCEOffset, 0);
    }
    static createChallengeNonceVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startChallengeNonceVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addChallengeExpiresAt(builder, CHALLENGE_EXPIRES_AT) {
        builder.addFieldInt64(10, CHALLENGE_EXPIRES_AT, BigInt('0'));
    }
    static addProviderPeerId(builder, PROVIDER_PEER_IDOffset) {
        builder.addFieldOffset(11, PROVIDER_PEER_IDOffset, 0);
    }
    static addSignature(builder, SIGNATUREOffset) {
        builder.addFieldOffset(12, SIGNATUREOffset, 0);
    }
    static createSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addSigningPubkey(builder, SIGNING_PUBKEYOffset) {
        builder.addFieldOffset(13, SIGNING_PUBKEYOffset, 0);
    }
    static createSigningPubkeyVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSigningPubkeyVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addTimestampMs(builder, TIMESTAMP_MS) {
        builder.addFieldInt64(14, TIMESTAMP_MS, BigInt('0'));
    }
    static addRejectionCode(builder, REJECTION_CODEOffset) {
        builder.addFieldOffset(15, REJECTION_CODEOffset, 0);
    }
    static addRejectionMessage(builder, REJECTION_MESSAGEOffset) {
        builder.addFieldOffset(16, REJECTION_MESSAGEOffset, 0);
    }
    static endLPF(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // REQUEST_ID
        builder.requiredField(offset, 8); // MODULE_ID
        return offset;
    }
    static finishLPFBuffer(builder, offset) {
        builder.finish(offset, '$LPF');
    }
    static finishSizePrefixedLPFBuffer(builder, offset) {
        builder.finish(offset, '$LPF', true);
    }
    static createLPF(builder, MESSAGE_TYPE, REQUEST_IDOffset, MODULE_IDOffset, MODULE_VERSIONOffset, REQUESTER_PEER_IDOffset, REQUESTER_XPUBOffset, REQUESTED_DOMAINOffset, REQUESTED_TIMEOUT_MS, REQUESTER_EPHEMERAL_PUBKEYOffset, CHALLENGE_NONCEOffset, CHALLENGE_EXPIRES_AT, PROVIDER_PEER_IDOffset, SIGNATUREOffset, SIGNING_PUBKEYOffset, TIMESTAMP_MS, REJECTION_CODEOffset, REJECTION_MESSAGEOffset) {
        LPF.startLPF(builder);
        LPF.addMessageType(builder, MESSAGE_TYPE);
        LPF.addRequestId(builder, REQUEST_IDOffset);
        LPF.addModuleId(builder, MODULE_IDOffset);
        LPF.addModuleVersion(builder, MODULE_VERSIONOffset);
        LPF.addRequesterPeerId(builder, REQUESTER_PEER_IDOffset);
        LPF.addRequesterXpub(builder, REQUESTER_XPUBOffset);
        LPF.addRequestedDomain(builder, REQUESTED_DOMAINOffset);
        LPF.addRequestedTimeoutMs(builder, REQUESTED_TIMEOUT_MS);
        LPF.addRequesterEphemeralPubkey(builder, REQUESTER_EPHEMERAL_PUBKEYOffset);
        LPF.addChallengeNonce(builder, CHALLENGE_NONCEOffset);
        LPF.addChallengeExpiresAt(builder, CHALLENGE_EXPIRES_AT);
        LPF.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
        LPF.addSignature(builder, SIGNATUREOffset);
        LPF.addSigningPubkey(builder, SIGNING_PUBKEYOffset);
        LPF.addTimestampMs(builder, TIMESTAMP_MS);
        LPF.addRejectionCode(builder, REJECTION_CODEOffset);
        LPF.addRejectionMessage(builder, REJECTION_MESSAGEOffset);
        return LPF.endLPF(builder);
    }
    unpack() {
        return new LPFT(this.MESSAGE_TYPE(), this.REQUEST_ID(), this.MODULE_ID(), this.MODULE_VERSION(), this.REQUESTER_PEER_ID(), this.REQUESTER_XPUB(), this.REQUESTED_DOMAIN(), this.REQUESTED_TIMEOUT_MS(), this.bb.createScalarList(this.REQUESTER_EPHEMERAL_PUBKEY.bind(this), this.requesterEphemeralPubkeyLength()), this.bb.createScalarList(this.CHALLENGE_NONCE.bind(this), this.challengeNonceLength()), this.CHALLENGE_EXPIRES_AT(), this.PROVIDER_PEER_ID(), this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength()), this.bb.createScalarList(this.SIGNING_PUBKEY.bind(this), this.signingPubkeyLength()), this.TIMESTAMP_MS(), this.REJECTION_CODE(), this.REJECTION_MESSAGE());
    }
    unpackTo(_o) {
        _o.MESSAGE_TYPE = this.MESSAGE_TYPE();
        _o.REQUEST_ID = this.REQUEST_ID();
        _o.MODULE_ID = this.MODULE_ID();
        _o.MODULE_VERSION = this.MODULE_VERSION();
        _o.REQUESTER_PEER_ID = this.REQUESTER_PEER_ID();
        _o.REQUESTER_XPUB = this.REQUESTER_XPUB();
        _o.REQUESTED_DOMAIN = this.REQUESTED_DOMAIN();
        _o.REQUESTED_TIMEOUT_MS = this.REQUESTED_TIMEOUT_MS();
        _o.REQUESTER_EPHEMERAL_PUBKEY = this.bb.createScalarList(this.REQUESTER_EPHEMERAL_PUBKEY.bind(this), this.requesterEphemeralPubkeyLength());
        _o.CHALLENGE_NONCE = this.bb.createScalarList(this.CHALLENGE_NONCE.bind(this), this.challengeNonceLength());
        _o.CHALLENGE_EXPIRES_AT = this.CHALLENGE_EXPIRES_AT();
        _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
        _o.SIGNATURE = this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength());
        _o.SIGNING_PUBKEY = this.bb.createScalarList(this.SIGNING_PUBKEY.bind(this), this.signingPubkeyLength());
        _o.TIMESTAMP_MS = this.TIMESTAMP_MS();
        _o.REJECTION_CODE = this.REJECTION_CODE();
        _o.REJECTION_MESSAGE = this.REJECTION_MESSAGE();
    }
}
export class LPFT {
    constructor(MESSAGE_TYPE = licensingProofMessageType.ProofRequest, REQUEST_ID = null, MODULE_ID = null, MODULE_VERSION = null, REQUESTER_PEER_ID = null, REQUESTER_XPUB = null, REQUESTED_DOMAIN = null, REQUESTED_TIMEOUT_MS = BigInt('0'), REQUESTER_EPHEMERAL_PUBKEY = [], CHALLENGE_NONCE = [], CHALLENGE_EXPIRES_AT = BigInt('0'), PROVIDER_PEER_ID = null, SIGNATURE = [], SIGNING_PUBKEY = [], TIMESTAMP_MS = BigInt('0'), REJECTION_CODE = null, REJECTION_MESSAGE = null) {
        this.MESSAGE_TYPE = MESSAGE_TYPE;
        this.REQUEST_ID = REQUEST_ID;
        this.MODULE_ID = MODULE_ID;
        this.MODULE_VERSION = MODULE_VERSION;
        this.REQUESTER_PEER_ID = REQUESTER_PEER_ID;
        this.REQUESTER_XPUB = REQUESTER_XPUB;
        this.REQUESTED_DOMAIN = REQUESTED_DOMAIN;
        this.REQUESTED_TIMEOUT_MS = REQUESTED_TIMEOUT_MS;
        this.REQUESTER_EPHEMERAL_PUBKEY = REQUESTER_EPHEMERAL_PUBKEY;
        this.CHALLENGE_NONCE = CHALLENGE_NONCE;
        this.CHALLENGE_EXPIRES_AT = CHALLENGE_EXPIRES_AT;
        this.PROVIDER_PEER_ID = PROVIDER_PEER_ID;
        this.SIGNATURE = SIGNATURE;
        this.SIGNING_PUBKEY = SIGNING_PUBKEY;
        this.TIMESTAMP_MS = TIMESTAMP_MS;
        this.REJECTION_CODE = REJECTION_CODE;
        this.REJECTION_MESSAGE = REJECTION_MESSAGE;
    }
    pack(builder) {
        const REQUEST_ID = (this.REQUEST_ID !== null ? builder.createString(this.REQUEST_ID) : 0);
        const MODULE_ID = (this.MODULE_ID !== null ? builder.createString(this.MODULE_ID) : 0);
        const MODULE_VERSION = (this.MODULE_VERSION !== null ? builder.createString(this.MODULE_VERSION) : 0);
        const REQUESTER_PEER_ID = (this.REQUESTER_PEER_ID !== null ? builder.createString(this.REQUESTER_PEER_ID) : 0);
        const REQUESTER_XPUB = (this.REQUESTER_XPUB !== null ? builder.createString(this.REQUESTER_XPUB) : 0);
        const REQUESTED_DOMAIN = (this.REQUESTED_DOMAIN !== null ? builder.createString(this.REQUESTED_DOMAIN) : 0);
        const REQUESTER_EPHEMERAL_PUBKEY = LPF.createRequesterEphemeralPubkeyVector(builder, this.REQUESTER_EPHEMERAL_PUBKEY);
        const CHALLENGE_NONCE = LPF.createChallengeNonceVector(builder, this.CHALLENGE_NONCE);
        const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID) : 0);
        const SIGNATURE = LPF.createSignatureVector(builder, this.SIGNATURE);
        const SIGNING_PUBKEY = LPF.createSigningPubkeyVector(builder, this.SIGNING_PUBKEY);
        const REJECTION_CODE = (this.REJECTION_CODE !== null ? builder.createString(this.REJECTION_CODE) : 0);
        const REJECTION_MESSAGE = (this.REJECTION_MESSAGE !== null ? builder.createString(this.REJECTION_MESSAGE) : 0);
        return LPF.createLPF(builder, this.MESSAGE_TYPE, REQUEST_ID, MODULE_ID, MODULE_VERSION, REQUESTER_PEER_ID, REQUESTER_XPUB, REQUESTED_DOMAIN, this.REQUESTED_TIMEOUT_MS, REQUESTER_EPHEMERAL_PUBKEY, CHALLENGE_NONCE, this.CHALLENGE_EXPIRES_AT, PROVIDER_PEER_ID, SIGNATURE, SIGNING_PUBKEY, this.TIMESTAMP_MS, REJECTION_CODE, REJECTION_MESSAGE);
    }
}
