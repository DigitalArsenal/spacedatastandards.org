// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { lambertSolutionBranch } from './lambertSolutionBranch.js';
import { lambertSolveState } from './lambertSolveState.js';
/**
 * Lambert solve result.
 */
export class LMO {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsLMO(bb, obj) {
        return (obj || new LMO()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsLMO(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new LMO()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$LMO');
    }
    REQUEST_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Solve status.
     */
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : lambertSolveState.OK;
    }
    ERROR_CODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ERROR_MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Single-revolution solution when STATUS is OK.
     */
    SINGLE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new lambertSolutionBranch()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Multi-revolution branches in ascending N_REVS order.
     */
    MULTI(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new lambertSolutionBranch()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    multiLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Highest revolution count that produced a feasible pair. Zero when no multi-rev branch is feasible.
     */
    MAX_FEASIBLE_REVS() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Result flags reserved for future compatibility.
     */
    FLAGS() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    static startLMO(builder) {
        builder.startObject(8);
    }
    static addRequestId(builder, REQUEST_IDOffset) {
        builder.addFieldOffset(0, REQUEST_IDOffset, 0);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(1, STATUS, lambertSolveState.OK);
    }
    static addErrorCode(builder, ERROR_CODEOffset) {
        builder.addFieldOffset(2, ERROR_CODEOffset, 0);
    }
    static addErrorMessage(builder, ERROR_MESSAGEOffset) {
        builder.addFieldOffset(3, ERROR_MESSAGEOffset, 0);
    }
    static addSingle(builder, SINGLEOffset) {
        builder.addFieldOffset(4, SINGLEOffset, 0);
    }
    static addMulti(builder, MULTIOffset) {
        builder.addFieldOffset(5, MULTIOffset, 0);
    }
    static createMultiVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startMultiVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addMaxFeasibleRevs(builder, MAX_FEASIBLE_REVS) {
        builder.addFieldInt16(6, MAX_FEASIBLE_REVS, 0);
    }
    static addFlags(builder, FLAGS) {
        builder.addFieldInt16(7, FLAGS, 0);
    }
    static endLMO(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishLMOBuffer(builder, offset) {
        builder.finish(offset, '$LMO');
    }
    static finishSizePrefixedLMOBuffer(builder, offset) {
        builder.finish(offset, '$LMO', true);
    }
    unpack() {
        return new LMOT(this.REQUEST_ID(), this.STATUS(), this.ERROR_CODE(), this.ERROR_MESSAGE(), (this.SINGLE() !== null ? this.SINGLE().unpack() : null), this.bb.createObjList(this.MULTI.bind(this), this.multiLength()), this.MAX_FEASIBLE_REVS(), this.FLAGS());
    }
    unpackTo(_o) {
        _o.REQUEST_ID = this.REQUEST_ID();
        _o.STATUS = this.STATUS();
        _o.ERROR_CODE = this.ERROR_CODE();
        _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
        _o.SINGLE = (this.SINGLE() !== null ? this.SINGLE().unpack() : null);
        _o.MULTI = this.bb.createObjList(this.MULTI.bind(this), this.multiLength());
        _o.MAX_FEASIBLE_REVS = this.MAX_FEASIBLE_REVS();
        _o.FLAGS = this.FLAGS();
    }
}
export class LMOT {
    constructor(REQUEST_ID = null, STATUS = lambertSolveState.OK, ERROR_CODE = null, ERROR_MESSAGE = null, SINGLE = null, MULTI = [], MAX_FEASIBLE_REVS = 0, FLAGS = 0) {
        this.REQUEST_ID = REQUEST_ID;
        this.STATUS = STATUS;
        this.ERROR_CODE = ERROR_CODE;
        this.ERROR_MESSAGE = ERROR_MESSAGE;
        this.SINGLE = SINGLE;
        this.MULTI = MULTI;
        this.MAX_FEASIBLE_REVS = MAX_FEASIBLE_REVS;
        this.FLAGS = FLAGS;
    }
    pack(builder) {
        const REQUEST_ID = (this.REQUEST_ID !== null ? builder.createString(this.REQUEST_ID) : 0);
        const ERROR_CODE = (this.ERROR_CODE !== null ? builder.createString(this.ERROR_CODE) : 0);
        const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE) : 0);
        const SINGLE = (this.SINGLE !== null ? this.SINGLE.pack(builder) : 0);
        const MULTI = LMO.createMultiVector(builder, builder.createObjectOffsetList(this.MULTI));
        LMO.startLMO(builder);
        LMO.addRequestId(builder, REQUEST_ID);
        LMO.addStatus(builder, this.STATUS);
        LMO.addErrorCode(builder, ERROR_CODE);
        LMO.addErrorMessage(builder, ERROR_MESSAGE);
        LMO.addSingle(builder, SINGLE);
        LMO.addMulti(builder, MULTI);
        LMO.addMaxFeasibleRevs(builder, this.MAX_FEASIBLE_REVS);
        LMO.addFlags(builder, this.FLAGS);
        return LMO.endLMO(builder);
    }
}
