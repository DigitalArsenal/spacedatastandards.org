// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { CNPProvenance } from './CNPProvenance.js';
import { CNPStatistic } from './CNPStatistic.js';
import { cnpMetricKind } from './cnpMetricKind.js';
/**
 * One aggregated quantity for the parent record's key.
 *
 * A metric is a DISTRIBUTION, not a scalar. There is deliberately no
 * `VALUE: double` on this table: a source that publishes only a median
 * encodes exactly one CNPStatistic with STATISTIC MEDIAN, and a consumer can
 * then never mistake it for a mean.
 */
export class CNPMetric {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCNPMetric(bb, obj) {
        return (obj || new CNPMetric()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCNPMetric(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CNPMetric()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Quantity aggregated.
     */
    KIND() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : cnpMetricKind.OTHER;
    }
    SOURCE_METRIC_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    UNITS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    /**
     * The distribution. At least one entry; a metric with none is malformed.
     */
    STATISTICS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new CNPStatistic()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    statisticsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Number of individual measurements aggregated. 0 means the source did
     * not publish a sample count — it never means zero measurements.
     */
    SAMPLE_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Number of distinct clients, terminals or probes contributing. 0 means
     * unpublished.
     */
    CLIENT_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Where this metric came from and under what terms.
     */
    PROVENANCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new CNPProvenance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startCNPMetric(builder) {
        builder.startObject(7);
    }
    static addKind(builder, KIND) {
        builder.addFieldInt8(0, KIND, cnpMetricKind.OTHER);
    }
    static addSourceMetricName(builder, SOURCE_METRIC_NAMEOffset) {
        builder.addFieldOffset(1, SOURCE_METRIC_NAMEOffset, 0);
    }
    static addUnits(builder, UNITSOffset) {
        builder.addFieldOffset(2, UNITSOffset, 0);
    }
    static addStatistics(builder, STATISTICSOffset) {
        builder.addFieldOffset(3, STATISTICSOffset, 0);
    }
    static createStatisticsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startStatisticsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSampleCount(builder, SAMPLE_COUNT) {
        builder.addFieldInt64(4, SAMPLE_COUNT, BigInt('0'));
    }
    static addClientCount(builder, CLIENT_COUNT) {
        builder.addFieldInt64(5, CLIENT_COUNT, BigInt('0'));
    }
    static addProvenance(builder, PROVENANCEOffset) {
        builder.addFieldOffset(6, PROVENANCEOffset, 0);
    }
    static endCNPMetric(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 8); // UNITS
        builder.requiredField(offset, 16); // PROVENANCE
        return offset;
    }
    unpack() {
        return new CNPMetricT(this.KIND(), this.SOURCE_METRIC_NAME(), this.UNITS(), this.bb.createObjList(this.STATISTICS.bind(this), this.statisticsLength()), this.SAMPLE_COUNT(), this.CLIENT_COUNT(), (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null));
    }
    unpackTo(_o) {
        _o.KIND = this.KIND();
        _o.SOURCE_METRIC_NAME = this.SOURCE_METRIC_NAME();
        _o.UNITS = this.UNITS();
        _o.STATISTICS = this.bb.createObjList(this.STATISTICS.bind(this), this.statisticsLength());
        _o.SAMPLE_COUNT = this.SAMPLE_COUNT();
        _o.CLIENT_COUNT = this.CLIENT_COUNT();
        _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null);
    }
}
export class CNPMetricT {
    constructor(KIND = cnpMetricKind.OTHER, SOURCE_METRIC_NAME = null, UNITS = null, STATISTICS = [], SAMPLE_COUNT = BigInt('0'), CLIENT_COUNT = BigInt('0'), PROVENANCE = null) {
        this.KIND = KIND;
        this.SOURCE_METRIC_NAME = SOURCE_METRIC_NAME;
        this.UNITS = UNITS;
        this.STATISTICS = STATISTICS;
        this.SAMPLE_COUNT = SAMPLE_COUNT;
        this.CLIENT_COUNT = CLIENT_COUNT;
        this.PROVENANCE = PROVENANCE;
    }
    pack(builder) {
        const SOURCE_METRIC_NAME = (this.SOURCE_METRIC_NAME !== null ? builder.createString(this.SOURCE_METRIC_NAME) : 0);
        const UNITS = (this.UNITS !== null ? builder.createString(this.UNITS) : 0);
        const STATISTICS = CNPMetric.createStatisticsVector(builder, builder.createObjectOffsetList(this.STATISTICS));
        const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE.pack(builder) : 0);
        CNPMetric.startCNPMetric(builder);
        CNPMetric.addKind(builder, this.KIND);
        CNPMetric.addSourceMetricName(builder, SOURCE_METRIC_NAME);
        CNPMetric.addUnits(builder, UNITS);
        CNPMetric.addStatistics(builder, STATISTICS);
        CNPMetric.addSampleCount(builder, this.SAMPLE_COUNT);
        CNPMetric.addClientCount(builder, this.CLIENT_COUNT);
        CNPMetric.addProvenance(builder, PROVENANCE);
        return CNPMetric.endCNPMetric(builder);
    }
}
