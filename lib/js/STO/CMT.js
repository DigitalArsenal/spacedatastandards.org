// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { RevenueSplit } from './RevenueSplit.js';
import { SCX } from './SCX.js';
import { SplitRecipient } from './SplitRecipient.js';
import { commissionEnforcement } from './commissionEnforcement.js';
import { commissionScope } from './commissionScope.js';
/**
 * Commission Terms / Revenue Split. Binds a listing, store, or publisher to a
 * platform commission and the on-chain split that enforces it. This record
 * makes "the platform earns on every transaction" auditable and portable
 * across store deployments. Commission is realized purely by the buyer's
 * payment routing through the on-chain splitter contract described in
 * CONTRACTS — never by a manual or off-chain settlement step.
 */
export class CMT {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCMT(bb, obj) {
        return (obj || new CMT()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCMT(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CMT()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$CMT');
    }
    TERMS_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    /**
     * What these terms cover
     */
    SCOPE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : commissionScope.Listing;
    }
    TARGET_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Platform commission in basis points (e.g. 500 = 5%)
     */
    COMMISSION_BPS() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Recipient splits; each in basis points, summing with the commission to 10000
     */
    SPLITS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new RevenueSplit()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    splitsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * The platform's per-chain payout identity (the commission recipient)
     */
    PLATFORM_RECIPIENT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new SplitRecipient()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * How the split is enforced (on-chain splitter vs provider-attested)
     */
    ENFORCEMENT() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : commissionEnforcement.SplitterContract;
    }
    /**
     * The established splitter contract(s)/program(s) that perform the split,
     * one per accepted chain/asset
     */
    CONTRACTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new SCX()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    contractsLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Unix ms when these terms take effect
     */
    EFFECTIVE_AT() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix ms when these terms expire, or 0 for no expiry
     */
    EXPIRES_AT() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Ed25519 signature by the store operator `$EPM`
     */
    SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    signatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    signatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startCMT(builder) {
        builder.startObject(11);
    }
    static addTermsId(builder, TERMS_IDOffset) {
        builder.addFieldOffset(0, TERMS_IDOffset, 0);
    }
    static addScope(builder, SCOPE) {
        builder.addFieldInt8(1, SCOPE, commissionScope.Listing);
    }
    static addTargetId(builder, TARGET_IDOffset) {
        builder.addFieldOffset(2, TARGET_IDOffset, 0);
    }
    static addCommissionBps(builder, COMMISSION_BPS) {
        builder.addFieldInt16(3, COMMISSION_BPS, 0);
    }
    static addSplits(builder, SPLITSOffset) {
        builder.addFieldOffset(4, SPLITSOffset, 0);
    }
    static createSplitsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSplitsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addPlatformRecipient(builder, PLATFORM_RECIPIENTOffset) {
        builder.addFieldOffset(5, PLATFORM_RECIPIENTOffset, 0);
    }
    static addEnforcement(builder, ENFORCEMENT) {
        builder.addFieldInt8(6, ENFORCEMENT, commissionEnforcement.SplitterContract);
    }
    static addContracts(builder, CONTRACTSOffset) {
        builder.addFieldOffset(7, CONTRACTSOffset, 0);
    }
    static createContractsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startContractsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addEffectiveAt(builder, EFFECTIVE_AT) {
        builder.addFieldInt64(8, EFFECTIVE_AT, BigInt('0'));
    }
    static addExpiresAt(builder, EXPIRES_AT) {
        builder.addFieldInt64(9, EXPIRES_AT, BigInt('0'));
    }
    static addSignature(builder, SIGNATUREOffset) {
        builder.addFieldOffset(10, SIGNATUREOffset, 0);
    }
    static createSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endCMT(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // TERMS_ID
        return offset;
    }
    static finishCMTBuffer(builder, offset) {
        builder.finish(offset, '$CMT');
    }
    static finishSizePrefixedCMTBuffer(builder, offset) {
        builder.finish(offset, '$CMT', true);
    }
    unpack() {
        return new CMTT(this.TERMS_ID(), this.SCOPE(), this.TARGET_ID(), this.COMMISSION_BPS(), this.bb.createObjList(this.SPLITS.bind(this), this.splitsLength()), (this.PLATFORM_RECIPIENT() !== null ? this.PLATFORM_RECIPIENT().unpack() : null), this.ENFORCEMENT(), this.bb.createObjList(this.CONTRACTS.bind(this), this.contractsLength()), this.EFFECTIVE_AT(), this.EXPIRES_AT(), this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength()));
    }
    unpackTo(_o) {
        _o.TERMS_ID = this.TERMS_ID();
        _o.SCOPE = this.SCOPE();
        _o.TARGET_ID = this.TARGET_ID();
        _o.COMMISSION_BPS = this.COMMISSION_BPS();
        _o.SPLITS = this.bb.createObjList(this.SPLITS.bind(this), this.splitsLength());
        _o.PLATFORM_RECIPIENT = (this.PLATFORM_RECIPIENT() !== null ? this.PLATFORM_RECIPIENT().unpack() : null);
        _o.ENFORCEMENT = this.ENFORCEMENT();
        _o.CONTRACTS = this.bb.createObjList(this.CONTRACTS.bind(this), this.contractsLength());
        _o.EFFECTIVE_AT = this.EFFECTIVE_AT();
        _o.EXPIRES_AT = this.EXPIRES_AT();
        _o.SIGNATURE = this.bb.createScalarList(this.SIGNATURE.bind(this), this.signatureLength());
    }
}
export class CMTT {
    constructor(TERMS_ID = null, SCOPE = commissionScope.Listing, TARGET_ID = null, COMMISSION_BPS = 0, SPLITS = [], PLATFORM_RECIPIENT = null, ENFORCEMENT = commissionEnforcement.SplitterContract, CONTRACTS = [], EFFECTIVE_AT = BigInt('0'), EXPIRES_AT = BigInt('0'), SIGNATURE = []) {
        this.TERMS_ID = TERMS_ID;
        this.SCOPE = SCOPE;
        this.TARGET_ID = TARGET_ID;
        this.COMMISSION_BPS = COMMISSION_BPS;
        this.SPLITS = SPLITS;
        this.PLATFORM_RECIPIENT = PLATFORM_RECIPIENT;
        this.ENFORCEMENT = ENFORCEMENT;
        this.CONTRACTS = CONTRACTS;
        this.EFFECTIVE_AT = EFFECTIVE_AT;
        this.EXPIRES_AT = EXPIRES_AT;
        this.SIGNATURE = SIGNATURE;
    }
    pack(builder) {
        const TERMS_ID = (this.TERMS_ID !== null ? builder.createString(this.TERMS_ID) : 0);
        const TARGET_ID = (this.TARGET_ID !== null ? builder.createString(this.TARGET_ID) : 0);
        const SPLITS = CMT.createSplitsVector(builder, builder.createObjectOffsetList(this.SPLITS));
        const PLATFORM_RECIPIENT = (this.PLATFORM_RECIPIENT !== null ? this.PLATFORM_RECIPIENT.pack(builder) : 0);
        const CONTRACTS = CMT.createContractsVector(builder, builder.createObjectOffsetList(this.CONTRACTS));
        const SIGNATURE = CMT.createSignatureVector(builder, this.SIGNATURE);
        CMT.startCMT(builder);
        CMT.addTermsId(builder, TERMS_ID);
        CMT.addScope(builder, this.SCOPE);
        CMT.addTargetId(builder, TARGET_ID);
        CMT.addCommissionBps(builder, this.COMMISSION_BPS);
        CMT.addSplits(builder, SPLITS);
        CMT.addPlatformRecipient(builder, PLATFORM_RECIPIENT);
        CMT.addEnforcement(builder, this.ENFORCEMENT);
        CMT.addContracts(builder, CONTRACTS);
        CMT.addEffectiveAt(builder, this.EFFECTIVE_AT);
        CMT.addExpiresAt(builder, this.EXPIRES_AT);
        CMT.addSignature(builder, SIGNATURE);
        return CMT.endCMT(builder);
    }
}
