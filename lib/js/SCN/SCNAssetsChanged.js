// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SCNReference } from './SCNReference.js';
/**
 * Scenario asset-change notification payload.
 */
export class SCNAssetsChanged {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCNAssetsChanged(bb, obj) {
        return (obj || new SCNAssetsChanged()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCNAssetsChanged(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCNAssetsChanged()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Satellite references added by the asset-change event.
     */
    ADDED_SATELLITES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new SCNReference()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    addedSatellitesLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Ground-site references added by the asset-change event.
     */
    ADDED_GROUND_SITES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new SCNReference()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    addedGroundSitesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Satellite references removed by the asset-change event.
     */
    REMOVED_SATELLITES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new SCNReference()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    removedSatellitesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Ground-site references removed by the asset-change event.
     */
    REMOVED_GROUND_SITES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new SCNReference()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    removedGroundSitesLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Current satellite references after the asset-change event.
     */
    SATELLITES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new SCNReference()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    satellitesLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Current ground-site references after the asset-change event.
     */
    GROUND_SITES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new SCNReference()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    groundSitesLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Current full reference set after the asset-change event.
     */
    REFERENCES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new SCNReference()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    referencesLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startSCNAssetsChanged(builder) {
        builder.startObject(7);
    }
    static addAddedSatellites(builder, ADDED_SATELLITESOffset) {
        builder.addFieldOffset(0, ADDED_SATELLITESOffset, 0);
    }
    static createAddedSatellitesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAddedSatellitesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAddedGroundSites(builder, ADDED_GROUND_SITESOffset) {
        builder.addFieldOffset(1, ADDED_GROUND_SITESOffset, 0);
    }
    static createAddedGroundSitesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAddedGroundSitesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addRemovedSatellites(builder, REMOVED_SATELLITESOffset) {
        builder.addFieldOffset(2, REMOVED_SATELLITESOffset, 0);
    }
    static createRemovedSatellitesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRemovedSatellitesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addRemovedGroundSites(builder, REMOVED_GROUND_SITESOffset) {
        builder.addFieldOffset(3, REMOVED_GROUND_SITESOffset, 0);
    }
    static createRemovedGroundSitesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRemovedGroundSitesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSatellites(builder, SATELLITESOffset) {
        builder.addFieldOffset(4, SATELLITESOffset, 0);
    }
    static createSatellitesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSatellitesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addGroundSites(builder, GROUND_SITESOffset) {
        builder.addFieldOffset(5, GROUND_SITESOffset, 0);
    }
    static createGroundSitesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startGroundSitesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addReferences(builder, REFERENCESOffset) {
        builder.addFieldOffset(6, REFERENCESOffset, 0);
    }
    static createReferencesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startReferencesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endSCNAssetsChanged(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createSCNAssetsChanged(builder, ADDED_SATELLITESOffset, ADDED_GROUND_SITESOffset, REMOVED_SATELLITESOffset, REMOVED_GROUND_SITESOffset, SATELLITESOffset, GROUND_SITESOffset, REFERENCESOffset) {
        SCNAssetsChanged.startSCNAssetsChanged(builder);
        SCNAssetsChanged.addAddedSatellites(builder, ADDED_SATELLITESOffset);
        SCNAssetsChanged.addAddedGroundSites(builder, ADDED_GROUND_SITESOffset);
        SCNAssetsChanged.addRemovedSatellites(builder, REMOVED_SATELLITESOffset);
        SCNAssetsChanged.addRemovedGroundSites(builder, REMOVED_GROUND_SITESOffset);
        SCNAssetsChanged.addSatellites(builder, SATELLITESOffset);
        SCNAssetsChanged.addGroundSites(builder, GROUND_SITESOffset);
        SCNAssetsChanged.addReferences(builder, REFERENCESOffset);
        return SCNAssetsChanged.endSCNAssetsChanged(builder);
    }
    unpack() {
        return new SCNAssetsChangedT(this.bb.createObjList(this.ADDED_SATELLITES.bind(this), this.addedSatellitesLength()), this.bb.createObjList(this.ADDED_GROUND_SITES.bind(this), this.addedGroundSitesLength()), this.bb.createObjList(this.REMOVED_SATELLITES.bind(this), this.removedSatellitesLength()), this.bb.createObjList(this.REMOVED_GROUND_SITES.bind(this), this.removedGroundSitesLength()), this.bb.createObjList(this.SATELLITES.bind(this), this.satellitesLength()), this.bb.createObjList(this.GROUND_SITES.bind(this), this.groundSitesLength()), this.bb.createObjList(this.REFERENCES.bind(this), this.referencesLength()));
    }
    unpackTo(_o) {
        _o.ADDED_SATELLITES = this.bb.createObjList(this.ADDED_SATELLITES.bind(this), this.addedSatellitesLength());
        _o.ADDED_GROUND_SITES = this.bb.createObjList(this.ADDED_GROUND_SITES.bind(this), this.addedGroundSitesLength());
        _o.REMOVED_SATELLITES = this.bb.createObjList(this.REMOVED_SATELLITES.bind(this), this.removedSatellitesLength());
        _o.REMOVED_GROUND_SITES = this.bb.createObjList(this.REMOVED_GROUND_SITES.bind(this), this.removedGroundSitesLength());
        _o.SATELLITES = this.bb.createObjList(this.SATELLITES.bind(this), this.satellitesLength());
        _o.GROUND_SITES = this.bb.createObjList(this.GROUND_SITES.bind(this), this.groundSitesLength());
        _o.REFERENCES = this.bb.createObjList(this.REFERENCES.bind(this), this.referencesLength());
    }
}
export class SCNAssetsChangedT {
    constructor(ADDED_SATELLITES = [], ADDED_GROUND_SITES = [], REMOVED_SATELLITES = [], REMOVED_GROUND_SITES = [], SATELLITES = [], GROUND_SITES = [], REFERENCES = []) {
        this.ADDED_SATELLITES = ADDED_SATELLITES;
        this.ADDED_GROUND_SITES = ADDED_GROUND_SITES;
        this.REMOVED_SATELLITES = REMOVED_SATELLITES;
        this.REMOVED_GROUND_SITES = REMOVED_GROUND_SITES;
        this.SATELLITES = SATELLITES;
        this.GROUND_SITES = GROUND_SITES;
        this.REFERENCES = REFERENCES;
    }
    pack(builder) {
        const ADDED_SATELLITES = SCNAssetsChanged.createAddedSatellitesVector(builder, builder.createObjectOffsetList(this.ADDED_SATELLITES));
        const ADDED_GROUND_SITES = SCNAssetsChanged.createAddedGroundSitesVector(builder, builder.createObjectOffsetList(this.ADDED_GROUND_SITES));
        const REMOVED_SATELLITES = SCNAssetsChanged.createRemovedSatellitesVector(builder, builder.createObjectOffsetList(this.REMOVED_SATELLITES));
        const REMOVED_GROUND_SITES = SCNAssetsChanged.createRemovedGroundSitesVector(builder, builder.createObjectOffsetList(this.REMOVED_GROUND_SITES));
        const SATELLITES = SCNAssetsChanged.createSatellitesVector(builder, builder.createObjectOffsetList(this.SATELLITES));
        const GROUND_SITES = SCNAssetsChanged.createGroundSitesVector(builder, builder.createObjectOffsetList(this.GROUND_SITES));
        const REFERENCES = SCNAssetsChanged.createReferencesVector(builder, builder.createObjectOffsetList(this.REFERENCES));
        return SCNAssetsChanged.createSCNAssetsChanged(builder, ADDED_SATELLITES, ADDED_GROUND_SITES, REMOVED_SATELLITES, REMOVED_GROUND_SITES, SATELLITES, GROUND_SITES, REFERENCES);
    }
}
