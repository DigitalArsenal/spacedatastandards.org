// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Tracking and Data Fusion
 */
export class TKG {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTKG(bb, obj) {
        return (obj || new TKG()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTKG(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TKG()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$TKG');
    }
    COMMAND(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FILTER_CONFIG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    IMM_CONFIG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MHT_CONFIG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    JPDA_CONFIG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FUSION_CONFIG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MEASUREMENTS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    INITIAL_STATE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TRACK_TO_UPDATE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TRACKS_TO_CORRELATE(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    tracksToCorrelateLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startTKG(builder) {
        builder.startObject(10);
    }
    static addCommand(builder, COMMANDOffset) {
        builder.addFieldOffset(0, COMMANDOffset, 0);
    }
    static addFilterConfig(builder, FILTER_CONFIGOffset) {
        builder.addFieldOffset(1, FILTER_CONFIGOffset, 0);
    }
    static addImmConfig(builder, IMM_CONFIGOffset) {
        builder.addFieldOffset(2, IMM_CONFIGOffset, 0);
    }
    static addMhtConfig(builder, MHT_CONFIGOffset) {
        builder.addFieldOffset(3, MHT_CONFIGOffset, 0);
    }
    static addJpdaConfig(builder, JPDA_CONFIGOffset) {
        builder.addFieldOffset(4, JPDA_CONFIGOffset, 0);
    }
    static addFusionConfig(builder, FUSION_CONFIGOffset) {
        builder.addFieldOffset(5, FUSION_CONFIGOffset, 0);
    }
    static addMeasurements(builder, MEASUREMENTSOffset) {
        builder.addFieldOffset(6, MEASUREMENTSOffset, 0);
    }
    static addInitialState(builder, INITIAL_STATEOffset) {
        builder.addFieldOffset(7, INITIAL_STATEOffset, 0);
    }
    static addTrackToUpdate(builder, TRACK_TO_UPDATEOffset) {
        builder.addFieldOffset(8, TRACK_TO_UPDATEOffset, 0);
    }
    static addTracksToCorrelate(builder, TRACKS_TO_CORRELATEOffset) {
        builder.addFieldOffset(9, TRACKS_TO_CORRELATEOffset, 0);
    }
    static createTracksToCorrelateVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTracksToCorrelateVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endTKG(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishTKGBuffer(builder, offset) {
        builder.finish(offset, '$TKG');
    }
    static finishSizePrefixedTKGBuffer(builder, offset) {
        builder.finish(offset, '$TKG', true);
    }
    static createTKG(builder, COMMANDOffset, FILTER_CONFIGOffset, IMM_CONFIGOffset, MHT_CONFIGOffset, JPDA_CONFIGOffset, FUSION_CONFIGOffset, MEASUREMENTSOffset, INITIAL_STATEOffset, TRACK_TO_UPDATEOffset, TRACKS_TO_CORRELATEOffset) {
        TKG.startTKG(builder);
        TKG.addCommand(builder, COMMANDOffset);
        TKG.addFilterConfig(builder, FILTER_CONFIGOffset);
        TKG.addImmConfig(builder, IMM_CONFIGOffset);
        TKG.addMhtConfig(builder, MHT_CONFIGOffset);
        TKG.addJpdaConfig(builder, JPDA_CONFIGOffset);
        TKG.addFusionConfig(builder, FUSION_CONFIGOffset);
        TKG.addMeasurements(builder, MEASUREMENTSOffset);
        TKG.addInitialState(builder, INITIAL_STATEOffset);
        TKG.addTrackToUpdate(builder, TRACK_TO_UPDATEOffset);
        TKG.addTracksToCorrelate(builder, TRACKS_TO_CORRELATEOffset);
        return TKG.endTKG(builder);
    }
    unpack() {
        return new TKGT(this.COMMAND(), this.FILTER_CONFIG(), this.IMM_CONFIG(), this.MHT_CONFIG(), this.JPDA_CONFIG(), this.FUSION_CONFIG(), this.MEASUREMENTS(), this.INITIAL_STATE(), this.TRACK_TO_UPDATE(), this.bb.createScalarList(this.TRACKS_TO_CORRELATE.bind(this), this.tracksToCorrelateLength()));
    }
    unpackTo(_o) {
        _o.COMMAND = this.COMMAND();
        _o.FILTER_CONFIG = this.FILTER_CONFIG();
        _o.IMM_CONFIG = this.IMM_CONFIG();
        _o.MHT_CONFIG = this.MHT_CONFIG();
        _o.JPDA_CONFIG = this.JPDA_CONFIG();
        _o.FUSION_CONFIG = this.FUSION_CONFIG();
        _o.MEASUREMENTS = this.MEASUREMENTS();
        _o.INITIAL_STATE = this.INITIAL_STATE();
        _o.TRACK_TO_UPDATE = this.TRACK_TO_UPDATE();
        _o.TRACKS_TO_CORRELATE = this.bb.createScalarList(this.TRACKS_TO_CORRELATE.bind(this), this.tracksToCorrelateLength());
    }
}
export class TKGT {
    constructor(COMMAND = null, FILTER_CONFIG = null, IMM_CONFIG = null, MHT_CONFIG = null, JPDA_CONFIG = null, FUSION_CONFIG = null, MEASUREMENTS = null, INITIAL_STATE = null, TRACK_TO_UPDATE = null, TRACKS_TO_CORRELATE = []) {
        this.COMMAND = COMMAND;
        this.FILTER_CONFIG = FILTER_CONFIG;
        this.IMM_CONFIG = IMM_CONFIG;
        this.MHT_CONFIG = MHT_CONFIG;
        this.JPDA_CONFIG = JPDA_CONFIG;
        this.FUSION_CONFIG = FUSION_CONFIG;
        this.MEASUREMENTS = MEASUREMENTS;
        this.INITIAL_STATE = INITIAL_STATE;
        this.TRACK_TO_UPDATE = TRACK_TO_UPDATE;
        this.TRACKS_TO_CORRELATE = TRACKS_TO_CORRELATE;
    }
    pack(builder) {
        const COMMAND = (this.COMMAND !== null ? builder.createString(this.COMMAND) : 0);
        const FILTER_CONFIG = (this.FILTER_CONFIG !== null ? builder.createString(this.FILTER_CONFIG) : 0);
        const IMM_CONFIG = (this.IMM_CONFIG !== null ? builder.createString(this.IMM_CONFIG) : 0);
        const MHT_CONFIG = (this.MHT_CONFIG !== null ? builder.createString(this.MHT_CONFIG) : 0);
        const JPDA_CONFIG = (this.JPDA_CONFIG !== null ? builder.createString(this.JPDA_CONFIG) : 0);
        const FUSION_CONFIG = (this.FUSION_CONFIG !== null ? builder.createString(this.FUSION_CONFIG) : 0);
        const MEASUREMENTS = (this.MEASUREMENTS !== null ? builder.createString(this.MEASUREMENTS) : 0);
        const INITIAL_STATE = (this.INITIAL_STATE !== null ? builder.createString(this.INITIAL_STATE) : 0);
        const TRACK_TO_UPDATE = (this.TRACK_TO_UPDATE !== null ? builder.createString(this.TRACK_TO_UPDATE) : 0);
        const TRACKS_TO_CORRELATE = TKG.createTracksToCorrelateVector(builder, builder.createObjectOffsetList(this.TRACKS_TO_CORRELATE));
        return TKG.createTKG(builder, COMMAND, FILTER_CONFIG, IMM_CONFIG, MHT_CONFIG, JPDA_CONFIG, FUSION_CONFIG, MEASUREMENTS, INITIAL_STATE, TRACK_TO_UPDATE, TRACKS_TO_CORRELATE);
    }
}
