// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PCEParameterDescriptor } from './PCEParameterDescriptor.js';
/**
 * The roster a named publisher offers, resolved as a whole. A consumer
 * caches this and resolves every later name against it.
 */
export class PCEParameterCatalog {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPCEParameterCatalog(bb, obj) {
        return (obj || new PCEParameterCatalog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPCEParameterCatalog(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PCEParameterCatalog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    CATALOG_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROVIDER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PUBLISHED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ENTRIES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new PCEParameterDescriptor()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    entriesLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startPCEParameterCatalog(builder) {
        builder.startObject(4);
    }
    static addCatalogId(builder, CATALOG_IDOffset) {
        builder.addFieldOffset(0, CATALOG_IDOffset, 0);
    }
    static addProviderId(builder, PROVIDER_IDOffset) {
        builder.addFieldOffset(1, PROVIDER_IDOffset, 0);
    }
    static addPublishedAt(builder, PUBLISHED_ATOffset) {
        builder.addFieldOffset(2, PUBLISHED_ATOffset, 0);
    }
    static addEntries(builder, ENTRIESOffset) {
        builder.addFieldOffset(3, ENTRIESOffset, 0);
    }
    static createEntriesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startEntriesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endPCEParameterCatalog(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createPCEParameterCatalog(builder, CATALOG_IDOffset, PROVIDER_IDOffset, PUBLISHED_ATOffset, ENTRIESOffset) {
        PCEParameterCatalog.startPCEParameterCatalog(builder);
        PCEParameterCatalog.addCatalogId(builder, CATALOG_IDOffset);
        PCEParameterCatalog.addProviderId(builder, PROVIDER_IDOffset);
        PCEParameterCatalog.addPublishedAt(builder, PUBLISHED_ATOffset);
        PCEParameterCatalog.addEntries(builder, ENTRIESOffset);
        return PCEParameterCatalog.endPCEParameterCatalog(builder);
    }
    unpack() {
        return new PCEParameterCatalogT(this.CATALOG_ID(), this.PROVIDER_ID(), this.PUBLISHED_AT(), this.bb.createObjList(this.ENTRIES.bind(this), this.entriesLength()));
    }
    unpackTo(_o) {
        _o.CATALOG_ID = this.CATALOG_ID();
        _o.PROVIDER_ID = this.PROVIDER_ID();
        _o.PUBLISHED_AT = this.PUBLISHED_AT();
        _o.ENTRIES = this.bb.createObjList(this.ENTRIES.bind(this), this.entriesLength());
    }
}
export class PCEParameterCatalogT {
    constructor(CATALOG_ID = null, PROVIDER_ID = null, PUBLISHED_AT = null, ENTRIES = []) {
        this.CATALOG_ID = CATALOG_ID;
        this.PROVIDER_ID = PROVIDER_ID;
        this.PUBLISHED_AT = PUBLISHED_AT;
        this.ENTRIES = ENTRIES;
    }
    pack(builder) {
        const CATALOG_ID = (this.CATALOG_ID !== null ? builder.createString(this.CATALOG_ID) : 0);
        const PROVIDER_ID = (this.PROVIDER_ID !== null ? builder.createString(this.PROVIDER_ID) : 0);
        const PUBLISHED_AT = (this.PUBLISHED_AT !== null ? builder.createString(this.PUBLISHED_AT) : 0);
        const ENTRIES = PCEParameterCatalog.createEntriesVector(builder, builder.createObjectOffsetList(this.ENTRIES));
        return PCEParameterCatalog.createPCEParameterCatalog(builder, CATALOG_ID, PROVIDER_ID, PUBLISHED_AT, ENTRIES);
    }
}
