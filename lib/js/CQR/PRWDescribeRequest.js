// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PRWInstance } from './PRWInstance.js';
export class PRWDescribeRequest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWDescribeRequest(bb, obj) {
        return (obj || new PRWDescribeRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWDescribeRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWDescribeRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    INSTANCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new PRWInstance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    SEGMENT_SET_HANDLE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    SOURCE_HANDLES(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    sourceHandlesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sourceHandlesArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Uint32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startPRWDescribeRequest(builder) {
        builder.startObject(3);
    }
    static addInstance(builder, INSTANCEOffset) {
        builder.addFieldOffset(0, INSTANCEOffset, 0);
    }
    static addSegmentSetHandle(builder, SEGMENT_SET_HANDLE) {
        builder.addFieldInt32(1, SEGMENT_SET_HANDLE, 0);
    }
    static addSourceHandles(builder, SOURCE_HANDLESOffset) {
        builder.addFieldOffset(2, SOURCE_HANDLESOffset, 0);
    }
    static createSourceHandlesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startSourceHandlesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endPRWDescribeRequest(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // INSTANCE
        return offset;
    }
    static createPRWDescribeRequest(builder, INSTANCEOffset, SEGMENT_SET_HANDLE, SOURCE_HANDLESOffset) {
        PRWDescribeRequest.startPRWDescribeRequest(builder);
        PRWDescribeRequest.addInstance(builder, INSTANCEOffset);
        PRWDescribeRequest.addSegmentSetHandle(builder, SEGMENT_SET_HANDLE);
        PRWDescribeRequest.addSourceHandles(builder, SOURCE_HANDLESOffset);
        return PRWDescribeRequest.endPRWDescribeRequest(builder);
    }
    unpack() {
        return new PRWDescribeRequestT((this.INSTANCE() !== null ? this.INSTANCE().unpack() : null), this.SEGMENT_SET_HANDLE(), this.bb.createScalarList(this.SOURCE_HANDLES.bind(this), this.sourceHandlesLength()));
    }
    unpackTo(_o) {
        _o.INSTANCE = (this.INSTANCE() !== null ? this.INSTANCE().unpack() : null);
        _o.SEGMENT_SET_HANDLE = this.SEGMENT_SET_HANDLE();
        _o.SOURCE_HANDLES = this.bb.createScalarList(this.SOURCE_HANDLES.bind(this), this.sourceHandlesLength());
    }
}
export class PRWDescribeRequestT {
    constructor(INSTANCE = null, SEGMENT_SET_HANDLE = 0, SOURCE_HANDLES = []) {
        this.INSTANCE = INSTANCE;
        this.SEGMENT_SET_HANDLE = SEGMENT_SET_HANDLE;
        this.SOURCE_HANDLES = SOURCE_HANDLES;
    }
    pack(builder) {
        const INSTANCE = (this.INSTANCE !== null ? this.INSTANCE.pack(builder) : 0);
        const SOURCE_HANDLES = PRWDescribeRequest.createSourceHandlesVector(builder, this.SOURCE_HANDLES);
        return PRWDescribeRequest.createPRWDescribeRequest(builder, INSTANCE, this.SEGMENT_SET_HANDLE, SOURCE_HANDLES);
    }
}
