// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PPE } from './PPE.js';
import { PRWFitQuality } from './PRWFitQuality.js';
/**
 * Existing PPE retains km/km/s, midpoint/time-system and coefficient meaning.
 */
export class PRWTrajectorySource {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWTrajectorySource(bb, obj) {
        return (obj || new PRWTrajectorySource()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWTrajectorySource(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWTrajectorySource()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SOURCE_HANDLE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    OBJECT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EPHEMERIS(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new PPE()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * One quality record per PPE.POSITION_RECORDS entry, same order.
     */
    SEGMENT_QUALITY(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new PRWFitQuality()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    segmentQualityLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startPRWTrajectorySource(builder) {
        builder.startObject(4);
    }
    static addSourceHandle(builder, SOURCE_HANDLE) {
        builder.addFieldInt32(0, SOURCE_HANDLE, 0);
    }
    static addObjectId(builder, OBJECT_IDOffset) {
        builder.addFieldOffset(1, OBJECT_IDOffset, 0);
    }
    static addEphemeris(builder, EPHEMERISOffset) {
        builder.addFieldOffset(2, EPHEMERISOffset, 0);
    }
    static addSegmentQuality(builder, SEGMENT_QUALITYOffset) {
        builder.addFieldOffset(3, SEGMENT_QUALITYOffset, 0);
    }
    static createSegmentQualityVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSegmentQualityVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endPRWTrajectorySource(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 8); // EPHEMERIS
        return offset;
    }
    unpack() {
        return new PRWTrajectorySourceT(this.SOURCE_HANDLE(), this.OBJECT_ID(), (this.EPHEMERIS() !== null ? this.EPHEMERIS().unpack() : null), this.bb.createObjList(this.SEGMENT_QUALITY.bind(this), this.segmentQualityLength()));
    }
    unpackTo(_o) {
        _o.SOURCE_HANDLE = this.SOURCE_HANDLE();
        _o.OBJECT_ID = this.OBJECT_ID();
        _o.EPHEMERIS = (this.EPHEMERIS() !== null ? this.EPHEMERIS().unpack() : null);
        _o.SEGMENT_QUALITY = this.bb.createObjList(this.SEGMENT_QUALITY.bind(this), this.segmentQualityLength());
    }
}
export class PRWTrajectorySourceT {
    constructor(SOURCE_HANDLE = 0, OBJECT_ID = null, EPHEMERIS = null, SEGMENT_QUALITY = []) {
        this.SOURCE_HANDLE = SOURCE_HANDLE;
        this.OBJECT_ID = OBJECT_ID;
        this.EPHEMERIS = EPHEMERIS;
        this.SEGMENT_QUALITY = SEGMENT_QUALITY;
    }
    pack(builder) {
        const OBJECT_ID = (this.OBJECT_ID !== null ? builder.createString(this.OBJECT_ID) : 0);
        const EPHEMERIS = (this.EPHEMERIS !== null ? this.EPHEMERIS.pack(builder) : 0);
        const SEGMENT_QUALITY = PRWTrajectorySource.createSegmentQualityVector(builder, builder.createObjectOffsetList(this.SEGMENT_QUALITY));
        PRWTrajectorySource.startPRWTrajectorySource(builder);
        PRWTrajectorySource.addSourceHandle(builder, this.SOURCE_HANDLE);
        PRWTrajectorySource.addObjectId(builder, OBJECT_ID);
        PRWTrajectorySource.addEphemeris(builder, EPHEMERIS);
        PRWTrajectorySource.addSegmentQuality(builder, SEGMENT_QUALITY);
        return PRWTrajectorySource.endPRWTrajectorySource(builder);
    }
}
