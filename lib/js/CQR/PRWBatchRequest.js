// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Batch propagation request — propagate every entity in ENTITY_HANDLES[] to
 * EPOCH and write the result StateVector stream to OUTPUT_OFFSET in the
 * shared arena.
 */
export class PRWBatchRequest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRWBatchRequest(bb, obj) {
        return (obj || new PRWBatchRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRWBatchRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRWBatchRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Target epoch as a Julian date (TIME_SYSTEM is configured on the host).
     */
    EPOCH() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Entity handles to propagate (empty = all initialized entities).
     */
    ENTITY_HANDLES(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb.__vector(this.bb_pos + offset) + index * 4) : 0;
    }
    entityHandlesLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    entityHandlesArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Uint32Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Output buffer offset in the arena where the StateVector stream begins.
     */
    OUTPUT_OFFSET() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Maximum entities to process in this call (0 = unbounded).
     */
    MAX_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TARGET_FRAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startPRWBatchRequest(builder) {
        builder.startObject(5);
    }
    static addEpoch(builder, EPOCH) {
        builder.addFieldFloat64(0, EPOCH, 0.0);
    }
    static addEntityHandles(builder, ENTITY_HANDLESOffset) {
        builder.addFieldOffset(1, ENTITY_HANDLESOffset, 0);
    }
    static createEntityHandlesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt32(data[i]);
        }
        return builder.endVector();
    }
    static startEntityHandlesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addOutputOffset(builder, OUTPUT_OFFSET) {
        builder.addFieldInt32(2, OUTPUT_OFFSET, 0);
    }
    static addMaxCount(builder, MAX_COUNT) {
        builder.addFieldInt32(3, MAX_COUNT, 0);
    }
    static addTargetFrame(builder, TARGET_FRAMEOffset) {
        builder.addFieldOffset(4, TARGET_FRAMEOffset, 0);
    }
    static endPRWBatchRequest(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createPRWBatchRequest(builder, EPOCH, ENTITY_HANDLESOffset, OUTPUT_OFFSET, MAX_COUNT, TARGET_FRAMEOffset) {
        PRWBatchRequest.startPRWBatchRequest(builder);
        PRWBatchRequest.addEpoch(builder, EPOCH);
        PRWBatchRequest.addEntityHandles(builder, ENTITY_HANDLESOffset);
        PRWBatchRequest.addOutputOffset(builder, OUTPUT_OFFSET);
        PRWBatchRequest.addMaxCount(builder, MAX_COUNT);
        PRWBatchRequest.addTargetFrame(builder, TARGET_FRAMEOffset);
        return PRWBatchRequest.endPRWBatchRequest(builder);
    }
    unpack() {
        return new PRWBatchRequestT(this.EPOCH(), this.bb.createScalarList(this.ENTITY_HANDLES.bind(this), this.entityHandlesLength()), this.OUTPUT_OFFSET(), this.MAX_COUNT(), this.TARGET_FRAME());
    }
    unpackTo(_o) {
        _o.EPOCH = this.EPOCH();
        _o.ENTITY_HANDLES = this.bb.createScalarList(this.ENTITY_HANDLES.bind(this), this.entityHandlesLength());
        _o.OUTPUT_OFFSET = this.OUTPUT_OFFSET();
        _o.MAX_COUNT = this.MAX_COUNT();
        _o.TARGET_FRAME = this.TARGET_FRAME();
    }
}
export class PRWBatchRequestT {
    constructor(EPOCH = 0.0, ENTITY_HANDLES = [], OUTPUT_OFFSET = 0, MAX_COUNT = 0, TARGET_FRAME = null) {
        this.EPOCH = EPOCH;
        this.ENTITY_HANDLES = ENTITY_HANDLES;
        this.OUTPUT_OFFSET = OUTPUT_OFFSET;
        this.MAX_COUNT = MAX_COUNT;
        this.TARGET_FRAME = TARGET_FRAME;
    }
    pack(builder) {
        const ENTITY_HANDLES = PRWBatchRequest.createEntityHandlesVector(builder, this.ENTITY_HANDLES);
        const TARGET_FRAME = (this.TARGET_FRAME !== null ? builder.createString(this.TARGET_FRAME) : 0);
        return PRWBatchRequest.createPRWBatchRequest(builder, this.EPOCH, ENTITY_HANDLES, this.OUTPUT_OFFSET, this.MAX_COUNT, TARGET_FRAME);
    }
}
