// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { cqrDocumentSyntax } from './cqrDocumentSyntax.js';
/**
 * Typed replacement of a bare raw-text port. Content is UTF-8 with no BOM;
 * XML declaration, if present, must agree. Content must be nonempty CDM KVN
 * or CDM XML matching SERIALIZATION; reject UNSPECIFIED and other documents.
 * Not an arbitrary JSON container.
 */
export class CQRNativeDocument {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCQRNativeDocument(bb, obj) {
        return (obj || new CQRNativeDocument()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCQRNativeDocument(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CQRNativeDocument()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SERIALIZATION() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : cqrDocumentSyntax.UNSPECIFIED;
    }
    CONTENT(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    contentLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    contentArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    static startCQRNativeDocument(builder) {
        builder.startObject(2);
    }
    static addSerialization(builder, SERIALIZATION) {
        builder.addFieldInt8(0, SERIALIZATION, cqrDocumentSyntax.UNSPECIFIED);
    }
    static addContent(builder, CONTENTOffset) {
        builder.addFieldOffset(1, CONTENTOffset, 0);
    }
    static createContentVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startContentVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endCQRNativeDocument(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // CONTENT
        return offset;
    }
    static createCQRNativeDocument(builder, SERIALIZATION, CONTENTOffset) {
        CQRNativeDocument.startCQRNativeDocument(builder);
        CQRNativeDocument.addSerialization(builder, SERIALIZATION);
        CQRNativeDocument.addContent(builder, CONTENTOffset);
        return CQRNativeDocument.endCQRNativeDocument(builder);
    }
    unpack() {
        return new CQRNativeDocumentT(this.SERIALIZATION(), this.bb.createScalarList(this.CONTENT.bind(this), this.contentLength()));
    }
    unpackTo(_o) {
        _o.SERIALIZATION = this.SERIALIZATION();
        _o.CONTENT = this.bb.createScalarList(this.CONTENT.bind(this), this.contentLength());
    }
}
export class CQRNativeDocumentT {
    constructor(SERIALIZATION = cqrDocumentSyntax.UNSPECIFIED, CONTENT = []) {
        this.SERIALIZATION = SERIALIZATION;
        this.CONTENT = CONTENT;
    }
    pack(builder) {
        const CONTENT = CQRNativeDocument.createContentVector(builder, this.CONTENT);
        return CQRNativeDocument.createCQRNativeDocument(builder, this.SERIALIZATION, CONTENT);
    }
}
