// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { papGainCutAxis } from './papGainCutAxis.js';
/**
 * One one-dimensional gain cut through the synthesized pattern.
 */
export class PAPGainCut {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPAPGainCut(bb, obj) {
        return (obj || new PAPGainCut()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPAPGainCut(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PAPGainCut()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    CUT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    AXIS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : papGainCutAxis.UNSPECIFIED;
    }
    FIXED_ANGLE_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    ANGLES_DEG(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    anglesDegLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    anglesDegArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    GAIN_DBI(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    gainDbiLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    gainDbiArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset));
    }
    static startPAPGainCut(builder) {
        builder.startObject(5);
    }
    static addCutId(builder, CUT_IDOffset) {
        builder.addFieldOffset(0, CUT_IDOffset, 0);
    }
    static addAxis(builder, AXIS) {
        builder.addFieldInt8(1, AXIS, papGainCutAxis.UNSPECIFIED);
    }
    static addFixedAngleDeg(builder, FIXED_ANGLE_DEG) {
        builder.addFieldFloat64(2, FIXED_ANGLE_DEG, 0.0);
    }
    static addAnglesDeg(builder, ANGLES_DEGOffset) {
        builder.addFieldOffset(3, ANGLES_DEGOffset, 0);
    }
    static createAnglesDegVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startAnglesDegVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addGainDbi(builder, GAIN_DBIOffset) {
        builder.addFieldOffset(4, GAIN_DBIOffset, 0);
    }
    static createGainDbiVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startGainDbiVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endPAPGainCut(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // CUT_ID
        builder.requiredField(offset, 10); // ANGLES_DEG
        builder.requiredField(offset, 12); // GAIN_DBI
        return offset;
    }
    static createPAPGainCut(builder, CUT_IDOffset, AXIS, FIXED_ANGLE_DEG, ANGLES_DEGOffset, GAIN_DBIOffset) {
        PAPGainCut.startPAPGainCut(builder);
        PAPGainCut.addCutId(builder, CUT_IDOffset);
        PAPGainCut.addAxis(builder, AXIS);
        PAPGainCut.addFixedAngleDeg(builder, FIXED_ANGLE_DEG);
        PAPGainCut.addAnglesDeg(builder, ANGLES_DEGOffset);
        PAPGainCut.addGainDbi(builder, GAIN_DBIOffset);
        return PAPGainCut.endPAPGainCut(builder);
    }
    unpack() {
        return new PAPGainCutT(this.CUT_ID(), this.AXIS(), this.FIXED_ANGLE_DEG(), this.bb.createScalarList(this.ANGLES_DEG.bind(this), this.anglesDegLength()), this.bb.createScalarList(this.GAIN_DBI.bind(this), this.gainDbiLength()));
    }
    unpackTo(_o) {
        _o.CUT_ID = this.CUT_ID();
        _o.AXIS = this.AXIS();
        _o.FIXED_ANGLE_DEG = this.FIXED_ANGLE_DEG();
        _o.ANGLES_DEG = this.bb.createScalarList(this.ANGLES_DEG.bind(this), this.anglesDegLength());
        _o.GAIN_DBI = this.bb.createScalarList(this.GAIN_DBI.bind(this), this.gainDbiLength());
    }
}
export class PAPGainCutT {
    constructor(CUT_ID = null, AXIS = papGainCutAxis.UNSPECIFIED, FIXED_ANGLE_DEG = 0.0, ANGLES_DEG = [], GAIN_DBI = []) {
        this.CUT_ID = CUT_ID;
        this.AXIS = AXIS;
        this.FIXED_ANGLE_DEG = FIXED_ANGLE_DEG;
        this.ANGLES_DEG = ANGLES_DEG;
        this.GAIN_DBI = GAIN_DBI;
    }
    pack(builder) {
        const CUT_ID = (this.CUT_ID !== null ? builder.createString(this.CUT_ID) : 0);
        const ANGLES_DEG = PAPGainCut.createAnglesDegVector(builder, this.ANGLES_DEG);
        const GAIN_DBI = PAPGainCut.createGainDbiVector(builder, this.GAIN_DBI);
        return PAPGainCut.createPAPGainCut(builder, CUT_ID, this.AXIS, this.FIXED_ANGLE_DEG, ANGLES_DEG, GAIN_DBI);
    }
}
