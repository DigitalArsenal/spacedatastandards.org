// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { flatSqlByteStreamKind } from './flatSqlByteStreamKind.js';
/**
 * FlatSQL Byte Stream — a bounded, typed chunk used for append/query and
 * opaque snapshot/WAL persistence. DATA carries at most one MiB so larger
 * inputs and snapshots are delivered as ordered chunks. The host may persist
 * canonical FSB bytes but must not interpret DATA as tables, rows, or SQL.
 */
export class FSB {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsFSB(bb, obj) {
        return (obj || new FSB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsFSB(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FSB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$FSB');
    }
    REQUEST_ID() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    KIND() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : flatSqlByteStreamKind.UNSPECIFIED;
    }
    CHUNK_SEQUENCE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    FINAL() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    TOTAL_BYTES() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    RECORD_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    COLUMN_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    SCHEMA_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FILE_IDENTIFIER(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DATA(index) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    dataLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    dataArray() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    SHA256(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    sha256Length() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sha256Array() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startFSB(builder) {
        builder.startObject(11);
    }
    static addRequestId(builder, REQUEST_ID) {
        builder.addFieldInt64(0, REQUEST_ID, BigInt('0'));
    }
    static addKind(builder, KIND) {
        builder.addFieldInt8(1, KIND, flatSqlByteStreamKind.UNSPECIFIED);
    }
    static addChunkSequence(builder, CHUNK_SEQUENCE) {
        builder.addFieldInt32(2, CHUNK_SEQUENCE, 0);
    }
    static addFinal(builder, FINAL) {
        builder.addFieldInt8(3, +FINAL, +false);
    }
    static addTotalBytes(builder, TOTAL_BYTES) {
        builder.addFieldInt64(4, TOTAL_BYTES, BigInt('0'));
    }
    static addRecordCount(builder, RECORD_COUNT) {
        builder.addFieldInt64(5, RECORD_COUNT, BigInt('0'));
    }
    static addColumnCount(builder, COLUMN_COUNT) {
        builder.addFieldInt32(6, COLUMN_COUNT, 0);
    }
    static addSchemaName(builder, SCHEMA_NAMEOffset) {
        builder.addFieldOffset(7, SCHEMA_NAMEOffset, 0);
    }
    static addFileIdentifier(builder, FILE_IDENTIFIEROffset) {
        builder.addFieldOffset(8, FILE_IDENTIFIEROffset, 0);
    }
    static addData(builder, DATAOffset) {
        builder.addFieldOffset(9, DATAOffset, 0);
    }
    static createDataVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startDataVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addSha256(builder, SHA256Offset) {
        builder.addFieldOffset(10, SHA256Offset, 0);
    }
    static createSha256Vector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSha256Vector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endFSB(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishFSBBuffer(builder, offset) {
        builder.finish(offset, '$FSB');
    }
    static finishSizePrefixedFSBBuffer(builder, offset) {
        builder.finish(offset, '$FSB', true);
    }
    static createFSB(builder, REQUEST_ID, KIND, CHUNK_SEQUENCE, FINAL, TOTAL_BYTES, RECORD_COUNT, COLUMN_COUNT, SCHEMA_NAMEOffset, FILE_IDENTIFIEROffset, DATAOffset, SHA256Offset) {
        FSB.startFSB(builder);
        FSB.addRequestId(builder, REQUEST_ID);
        FSB.addKind(builder, KIND);
        FSB.addChunkSequence(builder, CHUNK_SEQUENCE);
        FSB.addFinal(builder, FINAL);
        FSB.addTotalBytes(builder, TOTAL_BYTES);
        FSB.addRecordCount(builder, RECORD_COUNT);
        FSB.addColumnCount(builder, COLUMN_COUNT);
        FSB.addSchemaName(builder, SCHEMA_NAMEOffset);
        FSB.addFileIdentifier(builder, FILE_IDENTIFIEROffset);
        FSB.addData(builder, DATAOffset);
        FSB.addSha256(builder, SHA256Offset);
        return FSB.endFSB(builder);
    }
    unpack() {
        return new FSBT(this.REQUEST_ID(), this.KIND(), this.CHUNK_SEQUENCE(), this.FINAL(), this.TOTAL_BYTES(), this.RECORD_COUNT(), this.COLUMN_COUNT(), this.SCHEMA_NAME(), this.FILE_IDENTIFIER(), this.bb.createScalarList(this.DATA.bind(this), this.dataLength()), this.bb.createScalarList(this.SHA256.bind(this), this.sha256Length()));
    }
    unpackTo(_o) {
        _o.REQUEST_ID = this.REQUEST_ID();
        _o.KIND = this.KIND();
        _o.CHUNK_SEQUENCE = this.CHUNK_SEQUENCE();
        _o.FINAL = this.FINAL();
        _o.TOTAL_BYTES = this.TOTAL_BYTES();
        _o.RECORD_COUNT = this.RECORD_COUNT();
        _o.COLUMN_COUNT = this.COLUMN_COUNT();
        _o.SCHEMA_NAME = this.SCHEMA_NAME();
        _o.FILE_IDENTIFIER = this.FILE_IDENTIFIER();
        _o.DATA = this.bb.createScalarList(this.DATA.bind(this), this.dataLength());
        _o.SHA256 = this.bb.createScalarList(this.SHA256.bind(this), this.sha256Length());
    }
}
export class FSBT {
    constructor(REQUEST_ID = BigInt('0'), KIND = flatSqlByteStreamKind.UNSPECIFIED, CHUNK_SEQUENCE = 0, FINAL = false, TOTAL_BYTES = BigInt('0'), RECORD_COUNT = BigInt('0'), COLUMN_COUNT = 0, SCHEMA_NAME = null, FILE_IDENTIFIER = null, DATA = [], SHA256 = []) {
        this.REQUEST_ID = REQUEST_ID;
        this.KIND = KIND;
        this.CHUNK_SEQUENCE = CHUNK_SEQUENCE;
        this.FINAL = FINAL;
        this.TOTAL_BYTES = TOTAL_BYTES;
        this.RECORD_COUNT = RECORD_COUNT;
        this.COLUMN_COUNT = COLUMN_COUNT;
        this.SCHEMA_NAME = SCHEMA_NAME;
        this.FILE_IDENTIFIER = FILE_IDENTIFIER;
        this.DATA = DATA;
        this.SHA256 = SHA256;
    }
    pack(builder) {
        const SCHEMA_NAME = (this.SCHEMA_NAME !== null ? builder.createString(this.SCHEMA_NAME) : 0);
        const FILE_IDENTIFIER = (this.FILE_IDENTIFIER !== null ? builder.createString(this.FILE_IDENTIFIER) : 0);
        const DATA = FSB.createDataVector(builder, this.DATA);
        const SHA256 = FSB.createSha256Vector(builder, this.SHA256);
        return FSB.createFSB(builder, this.REQUEST_ID, this.KIND, this.CHUNK_SEQUENCE, this.FINAL, this.TOTAL_BYTES, this.RECORD_COUNT, this.COLUMN_COUNT, SCHEMA_NAME, FILE_IDENTIFIER, DATA, SHA256);
    }
}
