// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One column descriptor of a page, in record field order.
 */
export class QRPColumn {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsQRPColumn(bb, obj) {
        return (obj || new QRPColumn()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsQRPColumn(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new QRPColumn()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    UNIT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * True when the serving node can order the page by this column.
     */
    SORTABLE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * True when the serving node holds an index on this column.
     */
    INDEXED() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startQRPColumn(builder) {
        builder.startObject(5);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addType(builder, TYPEOffset) {
        builder.addFieldOffset(1, TYPEOffset, 0);
    }
    static addUnit(builder, UNITOffset) {
        builder.addFieldOffset(2, UNITOffset, 0);
    }
    static addSortable(builder, SORTABLE) {
        builder.addFieldInt8(3, +SORTABLE, +false);
    }
    static addIndexed(builder, INDEXED) {
        builder.addFieldInt8(4, +INDEXED, +false);
    }
    static endQRPColumn(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createQRPColumn(builder, NAMEOffset, TYPEOffset, UNITOffset, SORTABLE, INDEXED) {
        QRPColumn.startQRPColumn(builder);
        QRPColumn.addName(builder, NAMEOffset);
        QRPColumn.addType(builder, TYPEOffset);
        QRPColumn.addUnit(builder, UNITOffset);
        QRPColumn.addSortable(builder, SORTABLE);
        QRPColumn.addIndexed(builder, INDEXED);
        return QRPColumn.endQRPColumn(builder);
    }
    unpack() {
        return new QRPColumnT(this.NAME(), this.TYPE(), this.UNIT(), this.SORTABLE(), this.INDEXED());
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.TYPE = this.TYPE();
        _o.UNIT = this.UNIT();
        _o.SORTABLE = this.SORTABLE();
        _o.INDEXED = this.INDEXED();
    }
}
export class QRPColumnT {
    constructor(NAME = null, TYPE = null, UNIT = null, SORTABLE = false, INDEXED = false) {
        this.NAME = NAME;
        this.TYPE = TYPE;
        this.UNIT = UNIT;
        this.SORTABLE = SORTABLE;
        this.INDEXED = INDEXED;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const TYPE = (this.TYPE !== null ? builder.createString(this.TYPE) : 0);
        const UNIT = (this.UNIT !== null ? builder.createString(this.UNIT) : 0);
        return QRPColumn.createQRPColumn(builder, NAME, TYPE, UNIT, this.SORTABLE, this.INDEXED);
    }
}
