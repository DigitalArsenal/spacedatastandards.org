// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { QRPColumn } from './QRPColumn.js';
import { QRPFilter } from './QRPFilter.js';
import { qrpKind } from './qrpKind.js';
import { qrpStatus } from './qrpStatus.js';
/**
 * Query Page - request, page header or error frame.
 */
export class QRP {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsQRP(bb, obj) {
        return (obj || new QRP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsQRP(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new QRP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$QRP');
    }
    KIND() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : qrpKind.Request;
    }
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : qrpStatus.Ok;
    }
    ERROR_CODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Milliseconds a client should wait before retrying; 0 = no advice.
     */
    RETRY_AFTER_MS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    SCHEMA_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FILE_IDENTIFIER(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SQL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SEARCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    FILTERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? (obj || new QRPFilter()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    filtersLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    SORT_FIELD(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SORT_DESC() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    PROVIDER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    BATCH_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PRODUCER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ORIGIN_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Epoch window, Unix milliseconds; 0 = unbounded.
     */
    FROM_EPOCH_MS() {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    TO_EPOCH_MS() {
        const offset = this.bb.__offset(this.bb_pos, 40);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * 1-based page number requested or served.
     */
    PAGE() {
        const offset = this.bb.__offset(this.bb_pos, 42);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Page size requested or served.
     */
    LIMIT() {
        const offset = this.bb.__offset(this.bb_pos, 44);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    CURSOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    NEXT_CURSOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 48);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Record frames that follow this page header.
     */
    RECORD_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 50);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Records matching the selection when known; 0 = unknown.
     */
    TOTAL_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 52);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Records the serving node scanned to build this page.
     */
    SCANNED() {
        const offset = this.bb.__offset(this.bb_pos, 54);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Records the serving node holds for the standard.
     */
    STORED() {
        const offset = this.bb.__offset(this.bb_pos, 56);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * True when a bounded scan returned before covering every stored record.
     */
    PARTIAL() {
        const offset = this.bb.__offset(this.bb_pos, 58);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * True when the page was cut at a byte or row budget.
     */
    TRUNCATED() {
        const offset = this.bb.__offset(this.bb_pos, 60);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Unix milliseconds the serving numbers were last true.
     */
    AS_OF_MS() {
        const offset = this.bb.__offset(this.bb_pos, 62);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * True when the page was served from a stale snapshot.
     */
    STALE() {
        const offset = this.bb.__offset(this.bb_pos, 64);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Milliseconds the serving node spent producing the page.
     */
    ELAPSED_MS() {
        const offset = this.bb.__offset(this.bb_pos, 66);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Unix milliseconds when the page was generated.
     */
    GENERATED_AT_MS() {
        const offset = this.bb.__offset(this.bb_pos, 68);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    ETAG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 70);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 72);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ARCHIVE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 74);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    COLUMNS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 76);
        return offset ? (obj || new QRPColumn()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    columnsLength() {
        const offset = this.bb.__offset(this.bb_pos, 76);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startQRP(builder) {
        builder.startObject(37);
    }
    static addKind(builder, KIND) {
        builder.addFieldInt8(0, KIND, qrpKind.Request);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(1, STATUS, qrpStatus.Ok);
    }
    static addErrorCode(builder, ERROR_CODEOffset) {
        builder.addFieldOffset(2, ERROR_CODEOffset, 0);
    }
    static addMessage(builder, MESSAGEOffset) {
        builder.addFieldOffset(3, MESSAGEOffset, 0);
    }
    static addRetryAfterMs(builder, RETRY_AFTER_MS) {
        builder.addFieldInt32(4, RETRY_AFTER_MS, 0);
    }
    static addSchemaName(builder, SCHEMA_NAMEOffset) {
        builder.addFieldOffset(5, SCHEMA_NAMEOffset, 0);
    }
    static addFileIdentifier(builder, FILE_IDENTIFIEROffset) {
        builder.addFieldOffset(6, FILE_IDENTIFIEROffset, 0);
    }
    static addSql(builder, SQLOffset) {
        builder.addFieldOffset(7, SQLOffset, 0);
    }
    static addSearch(builder, SEARCHOffset) {
        builder.addFieldOffset(8, SEARCHOffset, 0);
    }
    static addFilters(builder, FILTERSOffset) {
        builder.addFieldOffset(9, FILTERSOffset, 0);
    }
    static createFiltersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startFiltersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addSortField(builder, SORT_FIELDOffset) {
        builder.addFieldOffset(10, SORT_FIELDOffset, 0);
    }
    static addSortDesc(builder, SORT_DESC) {
        builder.addFieldInt8(11, +SORT_DESC, +false);
    }
    static addProviderId(builder, PROVIDER_IDOffset) {
        builder.addFieldOffset(12, PROVIDER_IDOffset, 0);
    }
    static addSourceName(builder, SOURCE_NAMEOffset) {
        builder.addFieldOffset(13, SOURCE_NAMEOffset, 0);
    }
    static addBatchId(builder, BATCH_IDOffset) {
        builder.addFieldOffset(14, BATCH_IDOffset, 0);
    }
    static addProducerPeerId(builder, PRODUCER_PEER_IDOffset) {
        builder.addFieldOffset(15, PRODUCER_PEER_IDOffset, 0);
    }
    static addOriginId(builder, ORIGIN_IDOffset) {
        builder.addFieldOffset(16, ORIGIN_IDOffset, 0);
    }
    static addFromEpochMs(builder, FROM_EPOCH_MS) {
        builder.addFieldInt64(17, FROM_EPOCH_MS, BigInt('0'));
    }
    static addToEpochMs(builder, TO_EPOCH_MS) {
        builder.addFieldInt64(18, TO_EPOCH_MS, BigInt('0'));
    }
    static addPage(builder, PAGE) {
        builder.addFieldInt32(19, PAGE, 0);
    }
    static addLimit(builder, LIMIT) {
        builder.addFieldInt32(20, LIMIT, 0);
    }
    static addCursor(builder, CURSOROffset) {
        builder.addFieldOffset(21, CURSOROffset, 0);
    }
    static addNextCursor(builder, NEXT_CURSOROffset) {
        builder.addFieldOffset(22, NEXT_CURSOROffset, 0);
    }
    static addRecordCount(builder, RECORD_COUNT) {
        builder.addFieldInt32(23, RECORD_COUNT, 0);
    }
    static addTotalCount(builder, TOTAL_COUNT) {
        builder.addFieldInt64(24, TOTAL_COUNT, BigInt('0'));
    }
    static addScanned(builder, SCANNED) {
        builder.addFieldInt64(25, SCANNED, BigInt('0'));
    }
    static addStored(builder, STORED) {
        builder.addFieldInt64(26, STORED, BigInt('0'));
    }
    static addPartial(builder, PARTIAL) {
        builder.addFieldInt8(27, +PARTIAL, +false);
    }
    static addTruncated(builder, TRUNCATED) {
        builder.addFieldInt8(28, +TRUNCATED, +false);
    }
    static addAsOfMs(builder, AS_OF_MS) {
        builder.addFieldInt64(29, AS_OF_MS, BigInt('0'));
    }
    static addStale(builder, STALE) {
        builder.addFieldInt8(30, +STALE, +false);
    }
    static addElapsedMs(builder, ELAPSED_MS) {
        builder.addFieldInt32(31, ELAPSED_MS, 0);
    }
    static addGeneratedAtMs(builder, GENERATED_AT_MS) {
        builder.addFieldInt64(32, GENERATED_AT_MS, BigInt('0'));
    }
    static addEtag(builder, ETAGOffset) {
        builder.addFieldOffset(33, ETAGOffset, 0);
    }
    static addCid(builder, CIDOffset) {
        builder.addFieldOffset(34, CIDOffset, 0);
    }
    static addArchiveId(builder, ARCHIVE_IDOffset) {
        builder.addFieldOffset(35, ARCHIVE_IDOffset, 0);
    }
    static addColumns(builder, COLUMNSOffset) {
        builder.addFieldOffset(36, COLUMNSOffset, 0);
    }
    static createColumnsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startColumnsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endQRP(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishQRPBuffer(builder, offset) {
        builder.finish(offset, '$QRP');
    }
    static finishSizePrefixedQRPBuffer(builder, offset) {
        builder.finish(offset, '$QRP', true);
    }
    static createQRP(builder, KIND, STATUS, ERROR_CODEOffset, MESSAGEOffset, RETRY_AFTER_MS, SCHEMA_NAMEOffset, FILE_IDENTIFIEROffset, SQLOffset, SEARCHOffset, FILTERSOffset, SORT_FIELDOffset, SORT_DESC, PROVIDER_IDOffset, SOURCE_NAMEOffset, BATCH_IDOffset, PRODUCER_PEER_IDOffset, ORIGIN_IDOffset, FROM_EPOCH_MS, TO_EPOCH_MS, PAGE, LIMIT, CURSOROffset, NEXT_CURSOROffset, RECORD_COUNT, TOTAL_COUNT, SCANNED, STORED, PARTIAL, TRUNCATED, AS_OF_MS, STALE, ELAPSED_MS, GENERATED_AT_MS, ETAGOffset, CIDOffset, ARCHIVE_IDOffset, COLUMNSOffset) {
        QRP.startQRP(builder);
        QRP.addKind(builder, KIND);
        QRP.addStatus(builder, STATUS);
        QRP.addErrorCode(builder, ERROR_CODEOffset);
        QRP.addMessage(builder, MESSAGEOffset);
        QRP.addRetryAfterMs(builder, RETRY_AFTER_MS);
        QRP.addSchemaName(builder, SCHEMA_NAMEOffset);
        QRP.addFileIdentifier(builder, FILE_IDENTIFIEROffset);
        QRP.addSql(builder, SQLOffset);
        QRP.addSearch(builder, SEARCHOffset);
        QRP.addFilters(builder, FILTERSOffset);
        QRP.addSortField(builder, SORT_FIELDOffset);
        QRP.addSortDesc(builder, SORT_DESC);
        QRP.addProviderId(builder, PROVIDER_IDOffset);
        QRP.addSourceName(builder, SOURCE_NAMEOffset);
        QRP.addBatchId(builder, BATCH_IDOffset);
        QRP.addProducerPeerId(builder, PRODUCER_PEER_IDOffset);
        QRP.addOriginId(builder, ORIGIN_IDOffset);
        QRP.addFromEpochMs(builder, FROM_EPOCH_MS);
        QRP.addToEpochMs(builder, TO_EPOCH_MS);
        QRP.addPage(builder, PAGE);
        QRP.addLimit(builder, LIMIT);
        QRP.addCursor(builder, CURSOROffset);
        QRP.addNextCursor(builder, NEXT_CURSOROffset);
        QRP.addRecordCount(builder, RECORD_COUNT);
        QRP.addTotalCount(builder, TOTAL_COUNT);
        QRP.addScanned(builder, SCANNED);
        QRP.addStored(builder, STORED);
        QRP.addPartial(builder, PARTIAL);
        QRP.addTruncated(builder, TRUNCATED);
        QRP.addAsOfMs(builder, AS_OF_MS);
        QRP.addStale(builder, STALE);
        QRP.addElapsedMs(builder, ELAPSED_MS);
        QRP.addGeneratedAtMs(builder, GENERATED_AT_MS);
        QRP.addEtag(builder, ETAGOffset);
        QRP.addCid(builder, CIDOffset);
        QRP.addArchiveId(builder, ARCHIVE_IDOffset);
        QRP.addColumns(builder, COLUMNSOffset);
        return QRP.endQRP(builder);
    }
    unpack() {
        return new QRPT(this.KIND(), this.STATUS(), this.ERROR_CODE(), this.MESSAGE(), this.RETRY_AFTER_MS(), this.SCHEMA_NAME(), this.FILE_IDENTIFIER(), this.SQL(), this.SEARCH(), this.bb.createObjList(this.FILTERS.bind(this), this.filtersLength()), this.SORT_FIELD(), this.SORT_DESC(), this.PROVIDER_ID(), this.SOURCE_NAME(), this.BATCH_ID(), this.PRODUCER_PEER_ID(), this.ORIGIN_ID(), this.FROM_EPOCH_MS(), this.TO_EPOCH_MS(), this.PAGE(), this.LIMIT(), this.CURSOR(), this.NEXT_CURSOR(), this.RECORD_COUNT(), this.TOTAL_COUNT(), this.SCANNED(), this.STORED(), this.PARTIAL(), this.TRUNCATED(), this.AS_OF_MS(), this.STALE(), this.ELAPSED_MS(), this.GENERATED_AT_MS(), this.ETAG(), this.CID(), this.ARCHIVE_ID(), this.bb.createObjList(this.COLUMNS.bind(this), this.columnsLength()));
    }
    unpackTo(_o) {
        _o.KIND = this.KIND();
        _o.STATUS = this.STATUS();
        _o.ERROR_CODE = this.ERROR_CODE();
        _o.MESSAGE = this.MESSAGE();
        _o.RETRY_AFTER_MS = this.RETRY_AFTER_MS();
        _o.SCHEMA_NAME = this.SCHEMA_NAME();
        _o.FILE_IDENTIFIER = this.FILE_IDENTIFIER();
        _o.SQL = this.SQL();
        _o.SEARCH = this.SEARCH();
        _o.FILTERS = this.bb.createObjList(this.FILTERS.bind(this), this.filtersLength());
        _o.SORT_FIELD = this.SORT_FIELD();
        _o.SORT_DESC = this.SORT_DESC();
        _o.PROVIDER_ID = this.PROVIDER_ID();
        _o.SOURCE_NAME = this.SOURCE_NAME();
        _o.BATCH_ID = this.BATCH_ID();
        _o.PRODUCER_PEER_ID = this.PRODUCER_PEER_ID();
        _o.ORIGIN_ID = this.ORIGIN_ID();
        _o.FROM_EPOCH_MS = this.FROM_EPOCH_MS();
        _o.TO_EPOCH_MS = this.TO_EPOCH_MS();
        _o.PAGE = this.PAGE();
        _o.LIMIT = this.LIMIT();
        _o.CURSOR = this.CURSOR();
        _o.NEXT_CURSOR = this.NEXT_CURSOR();
        _o.RECORD_COUNT = this.RECORD_COUNT();
        _o.TOTAL_COUNT = this.TOTAL_COUNT();
        _o.SCANNED = this.SCANNED();
        _o.STORED = this.STORED();
        _o.PARTIAL = this.PARTIAL();
        _o.TRUNCATED = this.TRUNCATED();
        _o.AS_OF_MS = this.AS_OF_MS();
        _o.STALE = this.STALE();
        _o.ELAPSED_MS = this.ELAPSED_MS();
        _o.GENERATED_AT_MS = this.GENERATED_AT_MS();
        _o.ETAG = this.ETAG();
        _o.CID = this.CID();
        _o.ARCHIVE_ID = this.ARCHIVE_ID();
        _o.COLUMNS = this.bb.createObjList(this.COLUMNS.bind(this), this.columnsLength());
    }
}
export class QRPT {
    constructor(KIND = qrpKind.Request, STATUS = qrpStatus.Ok, ERROR_CODE = null, MESSAGE = null, RETRY_AFTER_MS = 0, SCHEMA_NAME = null, FILE_IDENTIFIER = null, SQL = null, SEARCH = null, FILTERS = [], SORT_FIELD = null, SORT_DESC = false, PROVIDER_ID = null, SOURCE_NAME = null, BATCH_ID = null, PRODUCER_PEER_ID = null, ORIGIN_ID = null, FROM_EPOCH_MS = BigInt('0'), TO_EPOCH_MS = BigInt('0'), PAGE = 0, LIMIT = 0, CURSOR = null, NEXT_CURSOR = null, RECORD_COUNT = 0, TOTAL_COUNT = BigInt('0'), SCANNED = BigInt('0'), STORED = BigInt('0'), PARTIAL = false, TRUNCATED = false, AS_OF_MS = BigInt('0'), STALE = false, ELAPSED_MS = 0, GENERATED_AT_MS = BigInt('0'), ETAG = null, CID = null, ARCHIVE_ID = null, COLUMNS = []) {
        this.KIND = KIND;
        this.STATUS = STATUS;
        this.ERROR_CODE = ERROR_CODE;
        this.MESSAGE = MESSAGE;
        this.RETRY_AFTER_MS = RETRY_AFTER_MS;
        this.SCHEMA_NAME = SCHEMA_NAME;
        this.FILE_IDENTIFIER = FILE_IDENTIFIER;
        this.SQL = SQL;
        this.SEARCH = SEARCH;
        this.FILTERS = FILTERS;
        this.SORT_FIELD = SORT_FIELD;
        this.SORT_DESC = SORT_DESC;
        this.PROVIDER_ID = PROVIDER_ID;
        this.SOURCE_NAME = SOURCE_NAME;
        this.BATCH_ID = BATCH_ID;
        this.PRODUCER_PEER_ID = PRODUCER_PEER_ID;
        this.ORIGIN_ID = ORIGIN_ID;
        this.FROM_EPOCH_MS = FROM_EPOCH_MS;
        this.TO_EPOCH_MS = TO_EPOCH_MS;
        this.PAGE = PAGE;
        this.LIMIT = LIMIT;
        this.CURSOR = CURSOR;
        this.NEXT_CURSOR = NEXT_CURSOR;
        this.RECORD_COUNT = RECORD_COUNT;
        this.TOTAL_COUNT = TOTAL_COUNT;
        this.SCANNED = SCANNED;
        this.STORED = STORED;
        this.PARTIAL = PARTIAL;
        this.TRUNCATED = TRUNCATED;
        this.AS_OF_MS = AS_OF_MS;
        this.STALE = STALE;
        this.ELAPSED_MS = ELAPSED_MS;
        this.GENERATED_AT_MS = GENERATED_AT_MS;
        this.ETAG = ETAG;
        this.CID = CID;
        this.ARCHIVE_ID = ARCHIVE_ID;
        this.COLUMNS = COLUMNS;
    }
    pack(builder) {
        const ERROR_CODE = (this.ERROR_CODE !== null ? builder.createString(this.ERROR_CODE) : 0);
        const MESSAGE = (this.MESSAGE !== null ? builder.createString(this.MESSAGE) : 0);
        const SCHEMA_NAME = (this.SCHEMA_NAME !== null ? builder.createString(this.SCHEMA_NAME) : 0);
        const FILE_IDENTIFIER = (this.FILE_IDENTIFIER !== null ? builder.createString(this.FILE_IDENTIFIER) : 0);
        const SQL = (this.SQL !== null ? builder.createString(this.SQL) : 0);
        const SEARCH = (this.SEARCH !== null ? builder.createString(this.SEARCH) : 0);
        const FILTERS = QRP.createFiltersVector(builder, builder.createObjectOffsetList(this.FILTERS));
        const SORT_FIELD = (this.SORT_FIELD !== null ? builder.createString(this.SORT_FIELD) : 0);
        const PROVIDER_ID = (this.PROVIDER_ID !== null ? builder.createString(this.PROVIDER_ID) : 0);
        const SOURCE_NAME = (this.SOURCE_NAME !== null ? builder.createString(this.SOURCE_NAME) : 0);
        const BATCH_ID = (this.BATCH_ID !== null ? builder.createString(this.BATCH_ID) : 0);
        const PRODUCER_PEER_ID = (this.PRODUCER_PEER_ID !== null ? builder.createString(this.PRODUCER_PEER_ID) : 0);
        const ORIGIN_ID = (this.ORIGIN_ID !== null ? builder.createString(this.ORIGIN_ID) : 0);
        const CURSOR = (this.CURSOR !== null ? builder.createString(this.CURSOR) : 0);
        const NEXT_CURSOR = (this.NEXT_CURSOR !== null ? builder.createString(this.NEXT_CURSOR) : 0);
        const ETAG = (this.ETAG !== null ? builder.createString(this.ETAG) : 0);
        const CID = (this.CID !== null ? builder.createString(this.CID) : 0);
        const ARCHIVE_ID = (this.ARCHIVE_ID !== null ? builder.createString(this.ARCHIVE_ID) : 0);
        const COLUMNS = QRP.createColumnsVector(builder, builder.createObjectOffsetList(this.COLUMNS));
        return QRP.createQRP(builder, this.KIND, this.STATUS, ERROR_CODE, MESSAGE, this.RETRY_AFTER_MS, SCHEMA_NAME, FILE_IDENTIFIER, SQL, SEARCH, FILTERS, SORT_FIELD, this.SORT_DESC, PROVIDER_ID, SOURCE_NAME, BATCH_ID, PRODUCER_PEER_ID, ORIGIN_ID, this.FROM_EPOCH_MS, this.TO_EPOCH_MS, this.PAGE, this.LIMIT, CURSOR, NEXT_CURSOR, this.RECORD_COUNT, this.TOTAL_COUNT, this.SCANNED, this.STORED, this.PARTIAL, this.TRUNCATED, this.AS_OF_MS, this.STALE, this.ELAPSED_MS, this.GENERATED_AT_MS, ETAG, CID, ARCHIVE_ID, COLUMNS);
    }
}
