// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { USR } from './USR.js';
/**
 * Program Description Message
 */
export class PRG {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRG(bb, obj) {
        return (obj || new PRG()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRG(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRG()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$PRG');
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    HD_KEY_PATH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MESSAGE_TYPES(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    messageTypesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Vector of users associated with the program, each user can have assigned message types
     */
    USERS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new USR()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    usersLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startPRG(builder) {
        builder.startObject(4);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addHdKeyPath(builder, HD_KEY_PATHOffset) {
        builder.addFieldOffset(1, HD_KEY_PATHOffset, 0);
    }
    static addMessageTypes(builder, MESSAGE_TYPESOffset) {
        builder.addFieldOffset(2, MESSAGE_TYPESOffset, 0);
    }
    static createMessageTypesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startMessageTypesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addUsers(builder, USERSOffset) {
        builder.addFieldOffset(3, USERSOffset, 0);
    }
    static createUsersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startUsersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endPRG(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishPRGBuffer(builder, offset) {
        builder.finish(offset, '$PRG');
    }
    static finishSizePrefixedPRGBuffer(builder, offset) {
        builder.finish(offset, '$PRG', true);
    }
    static createPRG(builder, NAMEOffset, HD_KEY_PATHOffset, MESSAGE_TYPESOffset, USERSOffset) {
        PRG.startPRG(builder);
        PRG.addName(builder, NAMEOffset);
        PRG.addHdKeyPath(builder, HD_KEY_PATHOffset);
        PRG.addMessageTypes(builder, MESSAGE_TYPESOffset);
        PRG.addUsers(builder, USERSOffset);
        return PRG.endPRG(builder);
    }
    unpack() {
        return new PRGT(this.NAME(), this.HD_KEY_PATH(), this.bb.createScalarList(this.MESSAGE_TYPES.bind(this), this.messageTypesLength()), this.bb.createObjList(this.USERS.bind(this), this.usersLength()));
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.HD_KEY_PATH = this.HD_KEY_PATH();
        _o.MESSAGE_TYPES = this.bb.createScalarList(this.MESSAGE_TYPES.bind(this), this.messageTypesLength());
        _o.USERS = this.bb.createObjList(this.USERS.bind(this), this.usersLength());
    }
}
export class PRGT {
    constructor(NAME = null, HD_KEY_PATH = null, MESSAGE_TYPES = [], USERS = []) {
        this.NAME = NAME;
        this.HD_KEY_PATH = HD_KEY_PATH;
        this.MESSAGE_TYPES = MESSAGE_TYPES;
        this.USERS = USERS;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const HD_KEY_PATH = (this.HD_KEY_PATH !== null ? builder.createString(this.HD_KEY_PATH) : 0);
        const MESSAGE_TYPES = PRG.createMessageTypesVector(builder, builder.createObjectOffsetList(this.MESSAGE_TYPES));
        const USERS = PRG.createUsersVector(builder, builder.createObjectOffsetList(this.USERS));
        return PRG.createPRG(builder, NAME, HD_KEY_PATH, MESSAGE_TYPES, USERS);
    }
}
