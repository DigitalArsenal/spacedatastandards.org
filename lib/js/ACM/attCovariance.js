// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { attCovType } from './attCovType.js';
/**
 * Attitude Covariance
 */
export class attCovariance {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsattCovariance(bb, obj) {
        return (obj || new attCovariance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsattCovariance(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new attCovariance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Covariance type
     */
    COV_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : attCovType.ANGLE;
    }
    COV_REF_FRAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Upper-triangular covariance matrix elements (row-major)
     */
    COV(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    covLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    covArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startattCovariance(builder) {
        builder.startObject(4);
    }
    static addCovType(builder, COV_TYPE) {
        builder.addFieldInt8(0, COV_TYPE, attCovType.ANGLE);
    }
    static addCovRefFrame(builder, COV_REF_FRAMEOffset) {
        builder.addFieldOffset(1, COV_REF_FRAMEOffset, 0);
    }
    static addEpoch(builder, EPOCHOffset) {
        builder.addFieldOffset(2, EPOCHOffset, 0);
    }
    static addCov(builder, COVOffset) {
        builder.addFieldOffset(3, COVOffset, 0);
    }
    static createCovVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCovVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static endattCovariance(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createattCovariance(builder, COV_TYPE, COV_REF_FRAMEOffset, EPOCHOffset, COVOffset) {
        attCovariance.startattCovariance(builder);
        attCovariance.addCovType(builder, COV_TYPE);
        attCovariance.addCovRefFrame(builder, COV_REF_FRAMEOffset);
        attCovariance.addEpoch(builder, EPOCHOffset);
        attCovariance.addCov(builder, COVOffset);
        return attCovariance.endattCovariance(builder);
    }
    unpack() {
        return new attCovarianceT(this.COV_TYPE(), this.COV_REF_FRAME(), this.EPOCH(), this.bb.createScalarList(this.COV.bind(this), this.covLength()));
    }
    unpackTo(_o) {
        _o.COV_TYPE = this.COV_TYPE();
        _o.COV_REF_FRAME = this.COV_REF_FRAME();
        _o.EPOCH = this.EPOCH();
        _o.COV = this.bb.createScalarList(this.COV.bind(this), this.covLength());
    }
}
export class attCovarianceT {
    constructor(COV_TYPE = attCovType.ANGLE, COV_REF_FRAME = null, EPOCH = null, COV = []) {
        this.COV_TYPE = COV_TYPE;
        this.COV_REF_FRAME = COV_REF_FRAME;
        this.EPOCH = EPOCH;
        this.COV = COV;
    }
    pack(builder) {
        const COV_REF_FRAME = (this.COV_REF_FRAME !== null ? builder.createString(this.COV_REF_FRAME) : 0);
        const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH) : 0);
        const COV = attCovariance.createCovVector(builder, this.COV);
        return attCovariance.createattCovariance(builder, this.COV_TYPE, COV_REF_FRAME, EPOCH, COV);
    }
}
