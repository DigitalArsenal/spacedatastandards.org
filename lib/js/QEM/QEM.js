// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { QEMTokenVector } from './QEMTokenVector.js';
import { cesNormalizationKind } from './cesNormalizationKind.js';
import { cesPoolingKind } from './cesPoolingKind.js';
import { cesVectorEncoding } from './cesVectorEncoding.js';
/**
 * Query Encoder Model.
 *
 * One QEM publishes the pruned static token-embedding table a lightweight
 * client (e.g. a browser) loads to encode a search query into the same
 * vector space a paired $CES shard's rows live in, without shipping a full
 * transformer. A consumer pools the rows of the query's tokens (per
 * POOLING_METHOD) into a query vector, then dot-products it against
 * CES.ROWS.
 *
 * A QEM is compatible with a CES only when MODEL_ID, MODEL_VERSION,
 * MODEL_WEIGHTS_SHA256, POOLING_METHOD and TEMPLATE_VERSION all match; a
 * mismatch on any one means the two vector spaces are not comparable.
 */
export class QEM {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsQEM(bb, obj) {
        return (obj || new QEM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsQEM(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new QEM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$QEM');
    }
    MODEL_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MODEL_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    MODEL_WEIGHTS_SHA256(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Sentence/document pooling method the encoder uses. Compared verbatim
     * against CES.POOLING_METHOD.
     */
    POOLING_METHOD() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : cesPoolingKind.MEAN;
    }
    TEMPLATE_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Fixed embedding dimensionality; must equal the paired CES.DIMENSIONS.
     */
    DIMENSIONS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Row element encoding used across TOKENS.
     */
    ENCODING() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : cesVectorEncoding.INT8;
    }
    /**
     * Row-vector normalization applied before quantization.
     */
    NORMALIZATION() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : cesNormalizationKind.L2;
    }
    /**
     * Number of tokens in the pruned vocabulary.
     */
    VOCAB_SIZE() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * One row per pruned vocabulary token.
     */
    TOKENS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? (obj || new QEMTokenVector()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    tokensLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startQEM(builder) {
        builder.startObject(10);
    }
    static addModelId(builder, MODEL_IDOffset) {
        builder.addFieldOffset(0, MODEL_IDOffset, 0);
    }
    static addModelVersion(builder, MODEL_VERSIONOffset) {
        builder.addFieldOffset(1, MODEL_VERSIONOffset, 0);
    }
    static addModelWeightsSha256(builder, MODEL_WEIGHTS_SHA256Offset) {
        builder.addFieldOffset(2, MODEL_WEIGHTS_SHA256Offset, 0);
    }
    static addPoolingMethod(builder, POOLING_METHOD) {
        builder.addFieldInt8(3, POOLING_METHOD, cesPoolingKind.MEAN);
    }
    static addTemplateVersion(builder, TEMPLATE_VERSIONOffset) {
        builder.addFieldOffset(4, TEMPLATE_VERSIONOffset, 0);
    }
    static addDimensions(builder, DIMENSIONS) {
        builder.addFieldInt32(5, DIMENSIONS, 0);
    }
    static addEncoding(builder, ENCODING) {
        builder.addFieldInt8(6, ENCODING, cesVectorEncoding.INT8);
    }
    static addNormalization(builder, NORMALIZATION) {
        builder.addFieldInt8(7, NORMALIZATION, cesNormalizationKind.L2);
    }
    static addVocabSize(builder, VOCAB_SIZE) {
        builder.addFieldInt32(8, VOCAB_SIZE, 0);
    }
    static addTokens(builder, TOKENSOffset) {
        builder.addFieldOffset(9, TOKENSOffset, 0);
    }
    static createTokensVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTokensVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endQEM(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishQEMBuffer(builder, offset) {
        builder.finish(offset, '$QEM');
    }
    static finishSizePrefixedQEMBuffer(builder, offset) {
        builder.finish(offset, '$QEM', true);
    }
    static createQEM(builder, MODEL_IDOffset, MODEL_VERSIONOffset, MODEL_WEIGHTS_SHA256Offset, POOLING_METHOD, TEMPLATE_VERSIONOffset, DIMENSIONS, ENCODING, NORMALIZATION, VOCAB_SIZE, TOKENSOffset) {
        QEM.startQEM(builder);
        QEM.addModelId(builder, MODEL_IDOffset);
        QEM.addModelVersion(builder, MODEL_VERSIONOffset);
        QEM.addModelWeightsSha256(builder, MODEL_WEIGHTS_SHA256Offset);
        QEM.addPoolingMethod(builder, POOLING_METHOD);
        QEM.addTemplateVersion(builder, TEMPLATE_VERSIONOffset);
        QEM.addDimensions(builder, DIMENSIONS);
        QEM.addEncoding(builder, ENCODING);
        QEM.addNormalization(builder, NORMALIZATION);
        QEM.addVocabSize(builder, VOCAB_SIZE);
        QEM.addTokens(builder, TOKENSOffset);
        return QEM.endQEM(builder);
    }
    unpack() {
        return new QEMT(this.MODEL_ID(), this.MODEL_VERSION(), this.MODEL_WEIGHTS_SHA256(), this.POOLING_METHOD(), this.TEMPLATE_VERSION(), this.DIMENSIONS(), this.ENCODING(), this.NORMALIZATION(), this.VOCAB_SIZE(), this.bb.createObjList(this.TOKENS.bind(this), this.tokensLength()));
    }
    unpackTo(_o) {
        _o.MODEL_ID = this.MODEL_ID();
        _o.MODEL_VERSION = this.MODEL_VERSION();
        _o.MODEL_WEIGHTS_SHA256 = this.MODEL_WEIGHTS_SHA256();
        _o.POOLING_METHOD = this.POOLING_METHOD();
        _o.TEMPLATE_VERSION = this.TEMPLATE_VERSION();
        _o.DIMENSIONS = this.DIMENSIONS();
        _o.ENCODING = this.ENCODING();
        _o.NORMALIZATION = this.NORMALIZATION();
        _o.VOCAB_SIZE = this.VOCAB_SIZE();
        _o.TOKENS = this.bb.createObjList(this.TOKENS.bind(this), this.tokensLength());
    }
}
export class QEMT {
    constructor(MODEL_ID = null, MODEL_VERSION = null, MODEL_WEIGHTS_SHA256 = null, POOLING_METHOD = cesPoolingKind.MEAN, TEMPLATE_VERSION = null, DIMENSIONS = 0, ENCODING = cesVectorEncoding.INT8, NORMALIZATION = cesNormalizationKind.L2, VOCAB_SIZE = 0, TOKENS = []) {
        this.MODEL_ID = MODEL_ID;
        this.MODEL_VERSION = MODEL_VERSION;
        this.MODEL_WEIGHTS_SHA256 = MODEL_WEIGHTS_SHA256;
        this.POOLING_METHOD = POOLING_METHOD;
        this.TEMPLATE_VERSION = TEMPLATE_VERSION;
        this.DIMENSIONS = DIMENSIONS;
        this.ENCODING = ENCODING;
        this.NORMALIZATION = NORMALIZATION;
        this.VOCAB_SIZE = VOCAB_SIZE;
        this.TOKENS = TOKENS;
    }
    pack(builder) {
        const MODEL_ID = (this.MODEL_ID !== null ? builder.createString(this.MODEL_ID) : 0);
        const MODEL_VERSION = (this.MODEL_VERSION !== null ? builder.createString(this.MODEL_VERSION) : 0);
        const MODEL_WEIGHTS_SHA256 = (this.MODEL_WEIGHTS_SHA256 !== null ? builder.createString(this.MODEL_WEIGHTS_SHA256) : 0);
        const TEMPLATE_VERSION = (this.TEMPLATE_VERSION !== null ? builder.createString(this.TEMPLATE_VERSION) : 0);
        const TOKENS = QEM.createTokensVector(builder, builder.createObjectOffsetList(this.TOKENS));
        return QEM.createQEM(builder, MODEL_ID, MODEL_VERSION, MODEL_WEIGHTS_SHA256, this.POOLING_METHOD, TEMPLATE_VERSION, this.DIMENSIONS, this.ENCODING, this.NORMALIZATION, this.VOCAB_SIZE, TOKENS);
    }
}
