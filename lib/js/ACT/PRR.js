// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PRRMetadataEntry } from './PRRMetadataEntry.js';
import { peerRegistryTrustCategory } from './peerRegistryTrustCategory.js';
export class PRR {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPRR(bb, obj) {
        return (obj || new PRR()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPRR(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PRR()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$PRR');
    }
    PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    MULTIFORMAT_ADDRESS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    multiformatAddressLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Trust level assigned to this peer by the local registry.
     */
    TRUST_LEVEL() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : peerRegistryTrustCategory.Standard;
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ORGANIZATION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    GROUPS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    groupsLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    NOTES(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Unix timestamp in milliseconds when this peer was added.
     */
    ADDED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix timestamp in milliseconds when this peer was last observed.
     */
    LAST_SEEN() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Unix timestamp in milliseconds when this peer last connected.
     */
    LAST_CONNECTED() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Number of successful connections observed by this node.
     */
    CONNECTION_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Number of messages received from this peer.
     */
    MESSAGES_RECEIVED() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Number of messages sent to this peer.
     */
    MESSAGES_SENT() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Number of bytes received from this peer.
     */
    BYTES_RECEIVED() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Number of bytes sent to this peer.
     */
    BYTES_SENT() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Optional EPM.fbs bytes for the peer identity profile.
     */
    EPM_DATA(index) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    epmDataLength() {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    epmDataArray() {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    VCARD_DATA(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Additional peer metadata as string key/value entries.
     */
    METADATA(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? (obj || new PRRMetadataEntry()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    metadataLength() {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Unix timestamp in milliseconds when this registry record was last updated.
     */
    UPDATED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 40);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * True when this record tombstones the peer from the current registry.
     */
    DELETED() {
        const offset = this.bb.__offset(this.bb_pos, 42);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    PROVIDER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 44);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Provider signature over the peer registry record.
     */
    PROVIDER_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    providerSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    providerSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 46);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startPRR(builder) {
        builder.startObject(22);
    }
    static addPeerId(builder, PEER_IDOffset) {
        builder.addFieldOffset(0, PEER_IDOffset, 0);
    }
    static addMultiformatAddress(builder, MULTIFORMAT_ADDRESSOffset) {
        builder.addFieldOffset(1, MULTIFORMAT_ADDRESSOffset, 0);
    }
    static createMultiformatAddressVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startMultiformatAddressVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addTrustLevel(builder, TRUST_LEVEL) {
        builder.addFieldInt8(2, TRUST_LEVEL, peerRegistryTrustCategory.Standard);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(3, NAMEOffset, 0);
    }
    static addOrganization(builder, ORGANIZATIONOffset) {
        builder.addFieldOffset(4, ORGANIZATIONOffset, 0);
    }
    static addGroups(builder, GROUPSOffset) {
        builder.addFieldOffset(5, GROUPSOffset, 0);
    }
    static createGroupsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startGroupsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addNotes(builder, NOTESOffset) {
        builder.addFieldOffset(6, NOTESOffset, 0);
    }
    static addAddedAt(builder, ADDED_AT) {
        builder.addFieldInt64(7, ADDED_AT, BigInt('0'));
    }
    static addLastSeen(builder, LAST_SEEN) {
        builder.addFieldInt64(8, LAST_SEEN, BigInt('0'));
    }
    static addLastConnected(builder, LAST_CONNECTED) {
        builder.addFieldInt64(9, LAST_CONNECTED, BigInt('0'));
    }
    static addConnectionCount(builder, CONNECTION_COUNT) {
        builder.addFieldInt64(10, CONNECTION_COUNT, BigInt('0'));
    }
    static addMessagesReceived(builder, MESSAGES_RECEIVED) {
        builder.addFieldInt64(11, MESSAGES_RECEIVED, BigInt('0'));
    }
    static addMessagesSent(builder, MESSAGES_SENT) {
        builder.addFieldInt64(12, MESSAGES_SENT, BigInt('0'));
    }
    static addBytesReceived(builder, BYTES_RECEIVED) {
        builder.addFieldInt64(13, BYTES_RECEIVED, BigInt('0'));
    }
    static addBytesSent(builder, BYTES_SENT) {
        builder.addFieldInt64(14, BYTES_SENT, BigInt('0'));
    }
    static addEpmData(builder, EPM_DATAOffset) {
        builder.addFieldOffset(15, EPM_DATAOffset, 0);
    }
    static createEpmDataVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startEpmDataVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addVcardData(builder, VCARD_DATAOffset) {
        builder.addFieldOffset(16, VCARD_DATAOffset, 0);
    }
    static addMetadata(builder, METADATAOffset) {
        builder.addFieldOffset(17, METADATAOffset, 0);
    }
    static createMetadataVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startMetadataVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addUpdatedAt(builder, UPDATED_AT) {
        builder.addFieldInt64(18, UPDATED_AT, BigInt('0'));
    }
    static addDeleted(builder, DELETED) {
        builder.addFieldInt8(19, +DELETED, +false);
    }
    static addProviderPeerId(builder, PROVIDER_PEER_IDOffset) {
        builder.addFieldOffset(20, PROVIDER_PEER_IDOffset, 0);
    }
    static addProviderSignature(builder, PROVIDER_SIGNATUREOffset) {
        builder.addFieldOffset(21, PROVIDER_SIGNATUREOffset, 0);
    }
    static createProviderSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startProviderSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endPRR(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // PEER_ID
        return offset;
    }
    static finishPRRBuffer(builder, offset) {
        builder.finish(offset, '$PRR');
    }
    static finishSizePrefixedPRRBuffer(builder, offset) {
        builder.finish(offset, '$PRR', true);
    }
    static createPRR(builder, PEER_IDOffset, MULTIFORMAT_ADDRESSOffset, TRUST_LEVEL, NAMEOffset, ORGANIZATIONOffset, GROUPSOffset, NOTESOffset, ADDED_AT, LAST_SEEN, LAST_CONNECTED, CONNECTION_COUNT, MESSAGES_RECEIVED, MESSAGES_SENT, BYTES_RECEIVED, BYTES_SENT, EPM_DATAOffset, VCARD_DATAOffset, METADATAOffset, UPDATED_AT, DELETED, PROVIDER_PEER_IDOffset, PROVIDER_SIGNATUREOffset) {
        PRR.startPRR(builder);
        PRR.addPeerId(builder, PEER_IDOffset);
        PRR.addMultiformatAddress(builder, MULTIFORMAT_ADDRESSOffset);
        PRR.addTrustLevel(builder, TRUST_LEVEL);
        PRR.addName(builder, NAMEOffset);
        PRR.addOrganization(builder, ORGANIZATIONOffset);
        PRR.addGroups(builder, GROUPSOffset);
        PRR.addNotes(builder, NOTESOffset);
        PRR.addAddedAt(builder, ADDED_AT);
        PRR.addLastSeen(builder, LAST_SEEN);
        PRR.addLastConnected(builder, LAST_CONNECTED);
        PRR.addConnectionCount(builder, CONNECTION_COUNT);
        PRR.addMessagesReceived(builder, MESSAGES_RECEIVED);
        PRR.addMessagesSent(builder, MESSAGES_SENT);
        PRR.addBytesReceived(builder, BYTES_RECEIVED);
        PRR.addBytesSent(builder, BYTES_SENT);
        PRR.addEpmData(builder, EPM_DATAOffset);
        PRR.addVcardData(builder, VCARD_DATAOffset);
        PRR.addMetadata(builder, METADATAOffset);
        PRR.addUpdatedAt(builder, UPDATED_AT);
        PRR.addDeleted(builder, DELETED);
        PRR.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
        PRR.addProviderSignature(builder, PROVIDER_SIGNATUREOffset);
        return PRR.endPRR(builder);
    }
    unpack() {
        return new PRRT(this.PEER_ID(), this.bb.createScalarList(this.MULTIFORMAT_ADDRESS.bind(this), this.multiformatAddressLength()), this.TRUST_LEVEL(), this.NAME(), this.ORGANIZATION(), this.bb.createScalarList(this.GROUPS.bind(this), this.groupsLength()), this.NOTES(), this.ADDED_AT(), this.LAST_SEEN(), this.LAST_CONNECTED(), this.CONNECTION_COUNT(), this.MESSAGES_RECEIVED(), this.MESSAGES_SENT(), this.BYTES_RECEIVED(), this.BYTES_SENT(), this.bb.createScalarList(this.EPM_DATA.bind(this), this.epmDataLength()), this.VCARD_DATA(), this.bb.createObjList(this.METADATA.bind(this), this.metadataLength()), this.UPDATED_AT(), this.DELETED(), this.PROVIDER_PEER_ID(), this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength()));
    }
    unpackTo(_o) {
        _o.PEER_ID = this.PEER_ID();
        _o.MULTIFORMAT_ADDRESS = this.bb.createScalarList(this.MULTIFORMAT_ADDRESS.bind(this), this.multiformatAddressLength());
        _o.TRUST_LEVEL = this.TRUST_LEVEL();
        _o.NAME = this.NAME();
        _o.ORGANIZATION = this.ORGANIZATION();
        _o.GROUPS = this.bb.createScalarList(this.GROUPS.bind(this), this.groupsLength());
        _o.NOTES = this.NOTES();
        _o.ADDED_AT = this.ADDED_AT();
        _o.LAST_SEEN = this.LAST_SEEN();
        _o.LAST_CONNECTED = this.LAST_CONNECTED();
        _o.CONNECTION_COUNT = this.CONNECTION_COUNT();
        _o.MESSAGES_RECEIVED = this.MESSAGES_RECEIVED();
        _o.MESSAGES_SENT = this.MESSAGES_SENT();
        _o.BYTES_RECEIVED = this.BYTES_RECEIVED();
        _o.BYTES_SENT = this.BYTES_SENT();
        _o.EPM_DATA = this.bb.createScalarList(this.EPM_DATA.bind(this), this.epmDataLength());
        _o.VCARD_DATA = this.VCARD_DATA();
        _o.METADATA = this.bb.createObjList(this.METADATA.bind(this), this.metadataLength());
        _o.UPDATED_AT = this.UPDATED_AT();
        _o.DELETED = this.DELETED();
        _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
        _o.PROVIDER_SIGNATURE = this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength());
    }
}
export class PRRT {
    constructor(PEER_ID = null, MULTIFORMAT_ADDRESS = [], TRUST_LEVEL = peerRegistryTrustCategory.Standard, NAME = null, ORGANIZATION = null, GROUPS = [], NOTES = null, ADDED_AT = BigInt('0'), LAST_SEEN = BigInt('0'), LAST_CONNECTED = BigInt('0'), CONNECTION_COUNT = BigInt('0'), MESSAGES_RECEIVED = BigInt('0'), MESSAGES_SENT = BigInt('0'), BYTES_RECEIVED = BigInt('0'), BYTES_SENT = BigInt('0'), EPM_DATA = [], VCARD_DATA = null, METADATA = [], UPDATED_AT = BigInt('0'), DELETED = false, PROVIDER_PEER_ID = null, PROVIDER_SIGNATURE = []) {
        this.PEER_ID = PEER_ID;
        this.MULTIFORMAT_ADDRESS = MULTIFORMAT_ADDRESS;
        this.TRUST_LEVEL = TRUST_LEVEL;
        this.NAME = NAME;
        this.ORGANIZATION = ORGANIZATION;
        this.GROUPS = GROUPS;
        this.NOTES = NOTES;
        this.ADDED_AT = ADDED_AT;
        this.LAST_SEEN = LAST_SEEN;
        this.LAST_CONNECTED = LAST_CONNECTED;
        this.CONNECTION_COUNT = CONNECTION_COUNT;
        this.MESSAGES_RECEIVED = MESSAGES_RECEIVED;
        this.MESSAGES_SENT = MESSAGES_SENT;
        this.BYTES_RECEIVED = BYTES_RECEIVED;
        this.BYTES_SENT = BYTES_SENT;
        this.EPM_DATA = EPM_DATA;
        this.VCARD_DATA = VCARD_DATA;
        this.METADATA = METADATA;
        this.UPDATED_AT = UPDATED_AT;
        this.DELETED = DELETED;
        this.PROVIDER_PEER_ID = PROVIDER_PEER_ID;
        this.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE;
    }
    pack(builder) {
        const PEER_ID = (this.PEER_ID !== null ? builder.createString(this.PEER_ID) : 0);
        const MULTIFORMAT_ADDRESS = PRR.createMultiformatAddressVector(builder, builder.createObjectOffsetList(this.MULTIFORMAT_ADDRESS));
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const ORGANIZATION = (this.ORGANIZATION !== null ? builder.createString(this.ORGANIZATION) : 0);
        const GROUPS = PRR.createGroupsVector(builder, builder.createObjectOffsetList(this.GROUPS));
        const NOTES = (this.NOTES !== null ? builder.createString(this.NOTES) : 0);
        const EPM_DATA = PRR.createEpmDataVector(builder, this.EPM_DATA);
        const VCARD_DATA = (this.VCARD_DATA !== null ? builder.createString(this.VCARD_DATA) : 0);
        const METADATA = PRR.createMetadataVector(builder, builder.createObjectOffsetList(this.METADATA));
        const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID) : 0);
        const PROVIDER_SIGNATURE = PRR.createProviderSignatureVector(builder, this.PROVIDER_SIGNATURE);
        return PRR.createPRR(builder, PEER_ID, MULTIFORMAT_ADDRESS, this.TRUST_LEVEL, NAME, ORGANIZATION, GROUPS, NOTES, this.ADDED_AT, this.LAST_SEEN, this.LAST_CONNECTED, this.CONNECTION_COUNT, this.MESSAGES_RECEIVED, this.MESSAGES_SENT, this.BYTES_RECEIVED, this.BYTES_SENT, EPM_DATA, VCARD_DATA, METADATA, this.UPDATED_AT, this.DELETED, PROVIDER_PEER_ID, PROVIDER_SIGNATURE);
    }
}
