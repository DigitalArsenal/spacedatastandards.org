// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { TerrainInterpolation } from './TerrainInterpolation.js';
/**
 * Terrain Models
 */
export class TRN {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsTRN(bb, obj) {
        return (obj || new TRN()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsTRN(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new TRN()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$TRN');
    }
    SOURCES(index) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    sourcesLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    sourcesArray() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? new Int8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    INTERPOLATION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : TerrainInterpolation.BILINEAR;
    }
    CACHE_ENABLED() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : true;
    }
    MAX_CACHE_TILES() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 100;
    }
    VERTICAL_EXAGGERATION() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 1.0;
    }
    static startTRN(builder) {
        builder.startObject(5);
    }
    static addSources(builder, SOURCESOffset) {
        builder.addFieldOffset(0, SOURCESOffset, 0);
    }
    static createSourcesVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSourcesVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addInterpolation(builder, INTERPOLATION) {
        builder.addFieldInt8(1, INTERPOLATION, TerrainInterpolation.BILINEAR);
    }
    static addCacheEnabled(builder, CACHE_ENABLED) {
        builder.addFieldInt8(2, +CACHE_ENABLED, +true);
    }
    static addMaxCacheTiles(builder, MAX_CACHE_TILES) {
        builder.addFieldInt16(3, MAX_CACHE_TILES, 100);
    }
    static addVerticalExaggeration(builder, VERTICAL_EXAGGERATION) {
        builder.addFieldFloat64(4, VERTICAL_EXAGGERATION, 1.0);
    }
    static endTRN(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishTRNBuffer(builder, offset) {
        builder.finish(offset, '$TRN');
    }
    static finishSizePrefixedTRNBuffer(builder, offset) {
        builder.finish(offset, '$TRN', true);
    }
    static createTRN(builder, SOURCESOffset, INTERPOLATION, CACHE_ENABLED, MAX_CACHE_TILES, VERTICAL_EXAGGERATION) {
        TRN.startTRN(builder);
        TRN.addSources(builder, SOURCESOffset);
        TRN.addInterpolation(builder, INTERPOLATION);
        TRN.addCacheEnabled(builder, CACHE_ENABLED);
        TRN.addMaxCacheTiles(builder, MAX_CACHE_TILES);
        TRN.addVerticalExaggeration(builder, VERTICAL_EXAGGERATION);
        return TRN.endTRN(builder);
    }
    unpack() {
        return new TRNT(this.bb.createScalarList(this.SOURCES.bind(this), this.sourcesLength()), this.INTERPOLATION(), this.CACHE_ENABLED(), this.MAX_CACHE_TILES(), this.VERTICAL_EXAGGERATION());
    }
    unpackTo(_o) {
        _o.SOURCES = this.bb.createScalarList(this.SOURCES.bind(this), this.sourcesLength());
        _o.INTERPOLATION = this.INTERPOLATION();
        _o.CACHE_ENABLED = this.CACHE_ENABLED();
        _o.MAX_CACHE_TILES = this.MAX_CACHE_TILES();
        _o.VERTICAL_EXAGGERATION = this.VERTICAL_EXAGGERATION();
    }
}
export class TRNT {
    constructor(SOURCES = [], INTERPOLATION = TerrainInterpolation.BILINEAR, CACHE_ENABLED = true, MAX_CACHE_TILES = 100, VERTICAL_EXAGGERATION = 1.0) {
        this.SOURCES = SOURCES;
        this.INTERPOLATION = INTERPOLATION;
        this.CACHE_ENABLED = CACHE_ENABLED;
        this.MAX_CACHE_TILES = MAX_CACHE_TILES;
        this.VERTICAL_EXAGGERATION = VERTICAL_EXAGGERATION;
    }
    pack(builder) {
        const SOURCES = TRN.createSourcesVector(builder, this.SOURCES);
        return TRN.createTRN(builder, SOURCES, this.INTERPOLATION, this.CACHE_ENABLED, this.MAX_CACHE_TILES, this.VERTICAL_EXAGGERATION);
    }
}
