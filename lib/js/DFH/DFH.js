// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { driftRecord } from './driftRecord.js';
/**
 * GEO Drift History
 */
export class DFH {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsDFH(bb, obj) {
        return (obj || new DFH()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsDFH(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new DFH()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$DFH');
    }
    ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Satellite number
     */
    SAT_NO() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    OBJECT_DESIGNATOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    OBJECT_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    START_TIME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    END_TIME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EFFECTIVE_UNTIL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Current drift rate in degrees/day
     */
    DRIFT_RATE() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Current mean longitude in degrees East
     */
    MEAN_LONGITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Longitude slot center in degrees East (if station-keeping)
     */
    SLOT_CENTER() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Longitude slot half-width in degrees
     */
    SLOT_HALF_WIDTH() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Whether object is actively station-keeping
     */
    STATION_KEEPING() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Historical drift records
     */
    RECORDS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? (obj || new driftRecord()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    recordsLength() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Number of records in history
     */
    NUM_RECORDS() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    NOTES(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startDFH(builder) {
        builder.startObject(15);
    }
    static addId(builder, IDOffset) {
        builder.addFieldOffset(0, IDOffset, 0);
    }
    static addSatNo(builder, SAT_NO) {
        builder.addFieldInt32(1, SAT_NO, 0);
    }
    static addObjectDesignator(builder, OBJECT_DESIGNATOROffset) {
        builder.addFieldOffset(2, OBJECT_DESIGNATOROffset, 0);
    }
    static addObjectName(builder, OBJECT_NAMEOffset) {
        builder.addFieldOffset(3, OBJECT_NAMEOffset, 0);
    }
    static addStartTime(builder, START_TIMEOffset) {
        builder.addFieldOffset(4, START_TIMEOffset, 0);
    }
    static addEndTime(builder, END_TIMEOffset) {
        builder.addFieldOffset(5, END_TIMEOffset, 0);
    }
    static addEffectiveUntil(builder, EFFECTIVE_UNTILOffset) {
        builder.addFieldOffset(6, EFFECTIVE_UNTILOffset, 0);
    }
    static addDriftRate(builder, DRIFT_RATE) {
        builder.addFieldFloat64(7, DRIFT_RATE, 0.0);
    }
    static addMeanLongitude(builder, MEAN_LONGITUDE) {
        builder.addFieldFloat64(8, MEAN_LONGITUDE, 0.0);
    }
    static addSlotCenter(builder, SLOT_CENTER) {
        builder.addFieldFloat64(9, SLOT_CENTER, 0.0);
    }
    static addSlotHalfWidth(builder, SLOT_HALF_WIDTH) {
        builder.addFieldFloat64(10, SLOT_HALF_WIDTH, 0.0);
    }
    static addStationKeeping(builder, STATION_KEEPING) {
        builder.addFieldInt8(11, +STATION_KEEPING, +false);
    }
    static addRecords(builder, RECORDSOffset) {
        builder.addFieldOffset(12, RECORDSOffset, 0);
    }
    static createRecordsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRecordsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addNumRecords(builder, NUM_RECORDS) {
        builder.addFieldInt32(13, NUM_RECORDS, 0);
    }
    static addNotes(builder, NOTESOffset) {
        builder.addFieldOffset(14, NOTESOffset, 0);
    }
    static endDFH(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishDFHBuffer(builder, offset) {
        builder.finish(offset, '$DFH');
    }
    static finishSizePrefixedDFHBuffer(builder, offset) {
        builder.finish(offset, '$DFH', true);
    }
    static createDFH(builder, IDOffset, SAT_NO, OBJECT_DESIGNATOROffset, OBJECT_NAMEOffset, START_TIMEOffset, END_TIMEOffset, EFFECTIVE_UNTILOffset, DRIFT_RATE, MEAN_LONGITUDE, SLOT_CENTER, SLOT_HALF_WIDTH, STATION_KEEPING, RECORDSOffset, NUM_RECORDS, NOTESOffset) {
        DFH.startDFH(builder);
        DFH.addId(builder, IDOffset);
        DFH.addSatNo(builder, SAT_NO);
        DFH.addObjectDesignator(builder, OBJECT_DESIGNATOROffset);
        DFH.addObjectName(builder, OBJECT_NAMEOffset);
        DFH.addStartTime(builder, START_TIMEOffset);
        DFH.addEndTime(builder, END_TIMEOffset);
        DFH.addEffectiveUntil(builder, EFFECTIVE_UNTILOffset);
        DFH.addDriftRate(builder, DRIFT_RATE);
        DFH.addMeanLongitude(builder, MEAN_LONGITUDE);
        DFH.addSlotCenter(builder, SLOT_CENTER);
        DFH.addSlotHalfWidth(builder, SLOT_HALF_WIDTH);
        DFH.addStationKeeping(builder, STATION_KEEPING);
        DFH.addRecords(builder, RECORDSOffset);
        DFH.addNumRecords(builder, NUM_RECORDS);
        DFH.addNotes(builder, NOTESOffset);
        return DFH.endDFH(builder);
    }
    unpack() {
        return new DFHT(this.ID(), this.SAT_NO(), this.OBJECT_DESIGNATOR(), this.OBJECT_NAME(), this.START_TIME(), this.END_TIME(), this.EFFECTIVE_UNTIL(), this.DRIFT_RATE(), this.MEAN_LONGITUDE(), this.SLOT_CENTER(), this.SLOT_HALF_WIDTH(), this.STATION_KEEPING(), this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength()), this.NUM_RECORDS(), this.NOTES());
    }
    unpackTo(_o) {
        _o.ID = this.ID();
        _o.SAT_NO = this.SAT_NO();
        _o.OBJECT_DESIGNATOR = this.OBJECT_DESIGNATOR();
        _o.OBJECT_NAME = this.OBJECT_NAME();
        _o.START_TIME = this.START_TIME();
        _o.END_TIME = this.END_TIME();
        _o.EFFECTIVE_UNTIL = this.EFFECTIVE_UNTIL();
        _o.DRIFT_RATE = this.DRIFT_RATE();
        _o.MEAN_LONGITUDE = this.MEAN_LONGITUDE();
        _o.SLOT_CENTER = this.SLOT_CENTER();
        _o.SLOT_HALF_WIDTH = this.SLOT_HALF_WIDTH();
        _o.STATION_KEEPING = this.STATION_KEEPING();
        _o.RECORDS = this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength());
        _o.NUM_RECORDS = this.NUM_RECORDS();
        _o.NOTES = this.NOTES();
    }
}
export class DFHT {
    constructor(ID = null, SAT_NO = 0, OBJECT_DESIGNATOR = null, OBJECT_NAME = null, START_TIME = null, END_TIME = null, EFFECTIVE_UNTIL = null, DRIFT_RATE = 0.0, MEAN_LONGITUDE = 0.0, SLOT_CENTER = 0.0, SLOT_HALF_WIDTH = 0.0, STATION_KEEPING = false, RECORDS = [], NUM_RECORDS = 0, NOTES = null) {
        this.ID = ID;
        this.SAT_NO = SAT_NO;
        this.OBJECT_DESIGNATOR = OBJECT_DESIGNATOR;
        this.OBJECT_NAME = OBJECT_NAME;
        this.START_TIME = START_TIME;
        this.END_TIME = END_TIME;
        this.EFFECTIVE_UNTIL = EFFECTIVE_UNTIL;
        this.DRIFT_RATE = DRIFT_RATE;
        this.MEAN_LONGITUDE = MEAN_LONGITUDE;
        this.SLOT_CENTER = SLOT_CENTER;
        this.SLOT_HALF_WIDTH = SLOT_HALF_WIDTH;
        this.STATION_KEEPING = STATION_KEEPING;
        this.RECORDS = RECORDS;
        this.NUM_RECORDS = NUM_RECORDS;
        this.NOTES = NOTES;
    }
    pack(builder) {
        const ID = (this.ID !== null ? builder.createString(this.ID) : 0);
        const OBJECT_DESIGNATOR = (this.OBJECT_DESIGNATOR !== null ? builder.createString(this.OBJECT_DESIGNATOR) : 0);
        const OBJECT_NAME = (this.OBJECT_NAME !== null ? builder.createString(this.OBJECT_NAME) : 0);
        const START_TIME = (this.START_TIME !== null ? builder.createString(this.START_TIME) : 0);
        const END_TIME = (this.END_TIME !== null ? builder.createString(this.END_TIME) : 0);
        const EFFECTIVE_UNTIL = (this.EFFECTIVE_UNTIL !== null ? builder.createString(this.EFFECTIVE_UNTIL) : 0);
        const RECORDS = DFH.createRecordsVector(builder, builder.createObjectOffsetList(this.RECORDS));
        const NOTES = (this.NOTES !== null ? builder.createString(this.NOTES) : 0);
        return DFH.createDFH(builder, ID, this.SAT_NO, OBJECT_DESIGNATOR, OBJECT_NAME, START_TIME, END_TIME, EFFECTIVE_UNTIL, this.DRIFT_RATE, this.MEAN_LONGITUDE, this.SLOT_CENTER, this.SLOT_HALF_WIDTH, this.STATION_KEEPING, RECORDS, this.NUM_RECORDS, NOTES);
    }
}
