// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One interfering contribution to one sample. SPARSE: entries exist only
 * where a contribution was computed, so a dense matrix is never forced into
 * existence to say "no interference computed".
 */
export class RFLInterferenceContribution {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsRFLInterferenceContribution(bb, obj) {
        return (obj || new RFLInterferenceContribution()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsRFLInterferenceContribution(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new RFLInterferenceContribution()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Index into the sample arrays this contribution applies to.
     */
    SAMPLE_INDEX() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    SOURCE_ENDPOINT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RFE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RFB_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Interfering object's catalogue number, 0 when unbound.
     */
    NORAD_CAT_ID() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Received interfering power at the victim receiver, dBW.
     */
    INTERFERENCE_POWER_DBW() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Angular separation between the wanted and interfering paths at the
     * victim receiver, degrees.
     */
    TOPOCENTRIC_SEPARATION_DEG() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Fraction of the victim's bandwidth overlapped by this emission [0-1].
     */
    SPECTRAL_OVERLAP_FRACTION() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Polarization isolation applied to this contribution, dB.
     */
    POLARIZATION_ISOLATION_DB() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Discrimination applied from the victim antenna pattern, dB.
     */
    ANTENNA_DISCRIMINATION_DB() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startRFLInterferenceContribution(builder) {
        builder.startObject(10);
    }
    static addSampleIndex(builder, SAMPLE_INDEX) {
        builder.addFieldInt32(0, SAMPLE_INDEX, 0);
    }
    static addSourceEndpointId(builder, SOURCE_ENDPOINT_IDOffset) {
        builder.addFieldOffset(1, SOURCE_ENDPOINT_IDOffset, 0);
    }
    static addRfeId(builder, RFE_IDOffset) {
        builder.addFieldOffset(2, RFE_IDOffset, 0);
    }
    static addRfbId(builder, RFB_IDOffset) {
        builder.addFieldOffset(3, RFB_IDOffset, 0);
    }
    static addNoradCatId(builder, NORAD_CAT_ID) {
        builder.addFieldInt32(4, NORAD_CAT_ID, 0);
    }
    static addInterferencePowerDbw(builder, INTERFERENCE_POWER_DBW) {
        builder.addFieldFloat64(5, INTERFERENCE_POWER_DBW, 0.0);
    }
    static addTopocentricSeparationDeg(builder, TOPOCENTRIC_SEPARATION_DEG) {
        builder.addFieldFloat64(6, TOPOCENTRIC_SEPARATION_DEG, 0.0);
    }
    static addSpectralOverlapFraction(builder, SPECTRAL_OVERLAP_FRACTION) {
        builder.addFieldFloat64(7, SPECTRAL_OVERLAP_FRACTION, 0.0);
    }
    static addPolarizationIsolationDb(builder, POLARIZATION_ISOLATION_DB) {
        builder.addFieldFloat64(8, POLARIZATION_ISOLATION_DB, 0.0);
    }
    static addAntennaDiscriminationDb(builder, ANTENNA_DISCRIMINATION_DB) {
        builder.addFieldFloat64(9, ANTENNA_DISCRIMINATION_DB, 0.0);
    }
    static endRFLInterferenceContribution(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createRFLInterferenceContribution(builder, SAMPLE_INDEX, SOURCE_ENDPOINT_IDOffset, RFE_IDOffset, RFB_IDOffset, NORAD_CAT_ID, INTERFERENCE_POWER_DBW, TOPOCENTRIC_SEPARATION_DEG, SPECTRAL_OVERLAP_FRACTION, POLARIZATION_ISOLATION_DB, ANTENNA_DISCRIMINATION_DB) {
        RFLInterferenceContribution.startRFLInterferenceContribution(builder);
        RFLInterferenceContribution.addSampleIndex(builder, SAMPLE_INDEX);
        RFLInterferenceContribution.addSourceEndpointId(builder, SOURCE_ENDPOINT_IDOffset);
        RFLInterferenceContribution.addRfeId(builder, RFE_IDOffset);
        RFLInterferenceContribution.addRfbId(builder, RFB_IDOffset);
        RFLInterferenceContribution.addNoradCatId(builder, NORAD_CAT_ID);
        RFLInterferenceContribution.addInterferencePowerDbw(builder, INTERFERENCE_POWER_DBW);
        RFLInterferenceContribution.addTopocentricSeparationDeg(builder, TOPOCENTRIC_SEPARATION_DEG);
        RFLInterferenceContribution.addSpectralOverlapFraction(builder, SPECTRAL_OVERLAP_FRACTION);
        RFLInterferenceContribution.addPolarizationIsolationDb(builder, POLARIZATION_ISOLATION_DB);
        RFLInterferenceContribution.addAntennaDiscriminationDb(builder, ANTENNA_DISCRIMINATION_DB);
        return RFLInterferenceContribution.endRFLInterferenceContribution(builder);
    }
    unpack() {
        return new RFLInterferenceContributionT(this.SAMPLE_INDEX(), this.SOURCE_ENDPOINT_ID(), this.RFE_ID(), this.RFB_ID(), this.NORAD_CAT_ID(), this.INTERFERENCE_POWER_DBW(), this.TOPOCENTRIC_SEPARATION_DEG(), this.SPECTRAL_OVERLAP_FRACTION(), this.POLARIZATION_ISOLATION_DB(), this.ANTENNA_DISCRIMINATION_DB());
    }
    unpackTo(_o) {
        _o.SAMPLE_INDEX = this.SAMPLE_INDEX();
        _o.SOURCE_ENDPOINT_ID = this.SOURCE_ENDPOINT_ID();
        _o.RFE_ID = this.RFE_ID();
        _o.RFB_ID = this.RFB_ID();
        _o.NORAD_CAT_ID = this.NORAD_CAT_ID();
        _o.INTERFERENCE_POWER_DBW = this.INTERFERENCE_POWER_DBW();
        _o.TOPOCENTRIC_SEPARATION_DEG = this.TOPOCENTRIC_SEPARATION_DEG();
        _o.SPECTRAL_OVERLAP_FRACTION = this.SPECTRAL_OVERLAP_FRACTION();
        _o.POLARIZATION_ISOLATION_DB = this.POLARIZATION_ISOLATION_DB();
        _o.ANTENNA_DISCRIMINATION_DB = this.ANTENNA_DISCRIMINATION_DB();
    }
}
export class RFLInterferenceContributionT {
    constructor(SAMPLE_INDEX = 0, SOURCE_ENDPOINT_ID = null, RFE_ID = null, RFB_ID = null, NORAD_CAT_ID = 0, INTERFERENCE_POWER_DBW = 0.0, TOPOCENTRIC_SEPARATION_DEG = 0.0, SPECTRAL_OVERLAP_FRACTION = 0.0, POLARIZATION_ISOLATION_DB = 0.0, ANTENNA_DISCRIMINATION_DB = 0.0) {
        this.SAMPLE_INDEX = SAMPLE_INDEX;
        this.SOURCE_ENDPOINT_ID = SOURCE_ENDPOINT_ID;
        this.RFE_ID = RFE_ID;
        this.RFB_ID = RFB_ID;
        this.NORAD_CAT_ID = NORAD_CAT_ID;
        this.INTERFERENCE_POWER_DBW = INTERFERENCE_POWER_DBW;
        this.TOPOCENTRIC_SEPARATION_DEG = TOPOCENTRIC_SEPARATION_DEG;
        this.SPECTRAL_OVERLAP_FRACTION = SPECTRAL_OVERLAP_FRACTION;
        this.POLARIZATION_ISOLATION_DB = POLARIZATION_ISOLATION_DB;
        this.ANTENNA_DISCRIMINATION_DB = ANTENNA_DISCRIMINATION_DB;
    }
    pack(builder) {
        const SOURCE_ENDPOINT_ID = (this.SOURCE_ENDPOINT_ID !== null ? builder.createString(this.SOURCE_ENDPOINT_ID) : 0);
        const RFE_ID = (this.RFE_ID !== null ? builder.createString(this.RFE_ID) : 0);
        const RFB_ID = (this.RFB_ID !== null ? builder.createString(this.RFB_ID) : 0);
        return RFLInterferenceContribution.createRFLInterferenceContribution(builder, this.SAMPLE_INDEX, SOURCE_ENDPOINT_ID, RFE_ID, RFB_ID, this.NORAD_CAT_ID, this.INTERFERENCE_POWER_DBW, this.TOPOCENTRIC_SEPARATION_DEG, this.SPECTRAL_OVERLAP_FRACTION, this.POLARIZATION_ISOLATION_DB, this.ANTENNA_DISCRIMINATION_DB);
    }
}
