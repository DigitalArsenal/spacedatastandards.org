// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { rfPolarization } from './rfPolarization.js';
import { rflEndpointKind } from './rflEndpointKind.js';
/**
 * One endpoint of a link. Identity ONLY — position, antenna geometry and band
 * capability are NOT copied here; they are joined from the standard named by
 * ENDPOINT_KIND. A copied position silently goes stale against its source.
 */
export class RFLEndpoint {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsRFLEndpoint(bb, obj) {
        return (obj || new RFLEndpoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsRFLEndpoint(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new RFLEndpoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    ENDPOINT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    ENDPOINT_KIND() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : rflEndpointKind.UNSPECIFIED;
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    GST_STATION_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SIT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TBS_SITE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * `$CAT.NORAD_CAT_ID` when the endpoint is an on-orbit object. 0 when
     * unbound — never a placeholder catalogue number.
     */
    NORAD_CAT_ID() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    OBJECT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RFB_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RFE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    IQC_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ID_BEAM(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CHANNEL_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Polarization actually used on this link. Defaults to UNKNOWN explicitly:
     * `rfPolarization` ordinal 0 is LHCP, so an unset field would otherwise
     * decode as a real polarization.
     */
    POLARIZATION() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : rfPolarization.UNKNOWN;
    }
    static startRFLEndpoint(builder) {
        builder.startObject(14);
    }
    static addEndpointId(builder, ENDPOINT_IDOffset) {
        builder.addFieldOffset(0, ENDPOINT_IDOffset, 0);
    }
    static addEndpointKind(builder, ENDPOINT_KIND) {
        builder.addFieldInt8(1, ENDPOINT_KIND, rflEndpointKind.UNSPECIFIED);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(2, NAMEOffset, 0);
    }
    static addGstStationId(builder, GST_STATION_IDOffset) {
        builder.addFieldOffset(3, GST_STATION_IDOffset, 0);
    }
    static addSitId(builder, SIT_IDOffset) {
        builder.addFieldOffset(4, SIT_IDOffset, 0);
    }
    static addTbsSiteId(builder, TBS_SITE_IDOffset) {
        builder.addFieldOffset(5, TBS_SITE_IDOffset, 0);
    }
    static addNoradCatId(builder, NORAD_CAT_ID) {
        builder.addFieldInt32(6, NORAD_CAT_ID, 0);
    }
    static addObjectId(builder, OBJECT_IDOffset) {
        builder.addFieldOffset(7, OBJECT_IDOffset, 0);
    }
    static addRfbId(builder, RFB_IDOffset) {
        builder.addFieldOffset(8, RFB_IDOffset, 0);
    }
    static addRfeId(builder, RFE_IDOffset) {
        builder.addFieldOffset(9, RFE_IDOffset, 0);
    }
    static addIqcId(builder, IQC_IDOffset) {
        builder.addFieldOffset(10, IQC_IDOffset, 0);
    }
    static addIdBeam(builder, ID_BEAMOffset) {
        builder.addFieldOffset(11, ID_BEAMOffset, 0);
    }
    static addChannelId(builder, CHANNEL_IDOffset) {
        builder.addFieldOffset(12, CHANNEL_IDOffset, 0);
    }
    static addPolarization(builder, POLARIZATION) {
        builder.addFieldInt8(13, POLARIZATION, rfPolarization.UNKNOWN);
    }
    static endRFLEndpoint(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // ENDPOINT_ID
        return offset;
    }
    static createRFLEndpoint(builder, ENDPOINT_IDOffset, ENDPOINT_KIND, NAMEOffset, GST_STATION_IDOffset, SIT_IDOffset, TBS_SITE_IDOffset, NORAD_CAT_ID, OBJECT_IDOffset, RFB_IDOffset, RFE_IDOffset, IQC_IDOffset, ID_BEAMOffset, CHANNEL_IDOffset, POLARIZATION) {
        RFLEndpoint.startRFLEndpoint(builder);
        RFLEndpoint.addEndpointId(builder, ENDPOINT_IDOffset);
        RFLEndpoint.addEndpointKind(builder, ENDPOINT_KIND);
        RFLEndpoint.addName(builder, NAMEOffset);
        RFLEndpoint.addGstStationId(builder, GST_STATION_IDOffset);
        RFLEndpoint.addSitId(builder, SIT_IDOffset);
        RFLEndpoint.addTbsSiteId(builder, TBS_SITE_IDOffset);
        RFLEndpoint.addNoradCatId(builder, NORAD_CAT_ID);
        RFLEndpoint.addObjectId(builder, OBJECT_IDOffset);
        RFLEndpoint.addRfbId(builder, RFB_IDOffset);
        RFLEndpoint.addRfeId(builder, RFE_IDOffset);
        RFLEndpoint.addIqcId(builder, IQC_IDOffset);
        RFLEndpoint.addIdBeam(builder, ID_BEAMOffset);
        RFLEndpoint.addChannelId(builder, CHANNEL_IDOffset);
        RFLEndpoint.addPolarization(builder, POLARIZATION);
        return RFLEndpoint.endRFLEndpoint(builder);
    }
    unpack() {
        return new RFLEndpointT(this.ENDPOINT_ID(), this.ENDPOINT_KIND(), this.NAME(), this.GST_STATION_ID(), this.SIT_ID(), this.TBS_SITE_ID(), this.NORAD_CAT_ID(), this.OBJECT_ID(), this.RFB_ID(), this.RFE_ID(), this.IQC_ID(), this.ID_BEAM(), this.CHANNEL_ID(), this.POLARIZATION());
    }
    unpackTo(_o) {
        _o.ENDPOINT_ID = this.ENDPOINT_ID();
        _o.ENDPOINT_KIND = this.ENDPOINT_KIND();
        _o.NAME = this.NAME();
        _o.GST_STATION_ID = this.GST_STATION_ID();
        _o.SIT_ID = this.SIT_ID();
        _o.TBS_SITE_ID = this.TBS_SITE_ID();
        _o.NORAD_CAT_ID = this.NORAD_CAT_ID();
        _o.OBJECT_ID = this.OBJECT_ID();
        _o.RFB_ID = this.RFB_ID();
        _o.RFE_ID = this.RFE_ID();
        _o.IQC_ID = this.IQC_ID();
        _o.ID_BEAM = this.ID_BEAM();
        _o.CHANNEL_ID = this.CHANNEL_ID();
        _o.POLARIZATION = this.POLARIZATION();
    }
}
export class RFLEndpointT {
    constructor(ENDPOINT_ID = null, ENDPOINT_KIND = rflEndpointKind.UNSPECIFIED, NAME = null, GST_STATION_ID = null, SIT_ID = null, TBS_SITE_ID = null, NORAD_CAT_ID = 0, OBJECT_ID = null, RFB_ID = null, RFE_ID = null, IQC_ID = null, ID_BEAM = null, CHANNEL_ID = null, POLARIZATION = rfPolarization.UNKNOWN) {
        this.ENDPOINT_ID = ENDPOINT_ID;
        this.ENDPOINT_KIND = ENDPOINT_KIND;
        this.NAME = NAME;
        this.GST_STATION_ID = GST_STATION_ID;
        this.SIT_ID = SIT_ID;
        this.TBS_SITE_ID = TBS_SITE_ID;
        this.NORAD_CAT_ID = NORAD_CAT_ID;
        this.OBJECT_ID = OBJECT_ID;
        this.RFB_ID = RFB_ID;
        this.RFE_ID = RFE_ID;
        this.IQC_ID = IQC_ID;
        this.ID_BEAM = ID_BEAM;
        this.CHANNEL_ID = CHANNEL_ID;
        this.POLARIZATION = POLARIZATION;
    }
    pack(builder) {
        const ENDPOINT_ID = (this.ENDPOINT_ID !== null ? builder.createString(this.ENDPOINT_ID) : 0);
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const GST_STATION_ID = (this.GST_STATION_ID !== null ? builder.createString(this.GST_STATION_ID) : 0);
        const SIT_ID = (this.SIT_ID !== null ? builder.createString(this.SIT_ID) : 0);
        const TBS_SITE_ID = (this.TBS_SITE_ID !== null ? builder.createString(this.TBS_SITE_ID) : 0);
        const OBJECT_ID = (this.OBJECT_ID !== null ? builder.createString(this.OBJECT_ID) : 0);
        const RFB_ID = (this.RFB_ID !== null ? builder.createString(this.RFB_ID) : 0);
        const RFE_ID = (this.RFE_ID !== null ? builder.createString(this.RFE_ID) : 0);
        const IQC_ID = (this.IQC_ID !== null ? builder.createString(this.IQC_ID) : 0);
        const ID_BEAM = (this.ID_BEAM !== null ? builder.createString(this.ID_BEAM) : 0);
        const CHANNEL_ID = (this.CHANNEL_ID !== null ? builder.createString(this.CHANNEL_ID) : 0);
        return RFLEndpoint.createRFLEndpoint(builder, ENDPOINT_ID, this.ENDPOINT_KIND, NAME, GST_STATION_ID, SIT_ID, TBS_SITE_ID, this.NORAD_CAT_ID, OBJECT_ID, RFB_ID, RFE_ID, IQC_ID, ID_BEAM, CHANNEL_ID, this.POLARIZATION);
    }
}
