// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Request for component-wise vector saturation.
 */
export class NUMVectorSaturateRequest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNUMVectorSaturateRequest(bb, obj) {
        return (obj || new NUMVectorSaturateRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNUMVectorSaturateRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NUMVectorSaturateRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Input vector to clamp.
     */
    STATE(index) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    stateLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    stateArray() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Per-component lower bounds. Length must match STATE.
     */
    LOWER_BOUNDS(index) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    lowerBoundsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    lowerBoundsArray() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Per-component upper bounds. Length must match STATE.
     */
    UPPER_BOUNDS(index) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    upperBoundsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    upperBoundsArray() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startNUMVectorSaturateRequest(builder) {
        builder.startObject(4);
    }
    static addState(builder, STATEOffset) {
        builder.addFieldOffset(0, STATEOffset, 0);
    }
    static createStateVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startStateVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addLowerBounds(builder, LOWER_BOUNDSOffset) {
        builder.addFieldOffset(1, LOWER_BOUNDSOffset, 0);
    }
    static createLowerBoundsVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startLowerBoundsVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addUpperBounds(builder, UPPER_BOUNDSOffset) {
        builder.addFieldOffset(2, UPPER_BOUNDSOffset, 0);
    }
    static createUpperBoundsVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startUpperBoundsVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(3, TRACE_IDOffset, 0);
    }
    static endNUMVectorSaturateRequest(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNUMVectorSaturateRequest(builder, STATEOffset, LOWER_BOUNDSOffset, UPPER_BOUNDSOffset, TRACE_IDOffset) {
        NUMVectorSaturateRequest.startNUMVectorSaturateRequest(builder);
        NUMVectorSaturateRequest.addState(builder, STATEOffset);
        NUMVectorSaturateRequest.addLowerBounds(builder, LOWER_BOUNDSOffset);
        NUMVectorSaturateRequest.addUpperBounds(builder, UPPER_BOUNDSOffset);
        NUMVectorSaturateRequest.addTraceId(builder, TRACE_IDOffset);
        return NUMVectorSaturateRequest.endNUMVectorSaturateRequest(builder);
    }
    unpack() {
        return new NUMVectorSaturateRequestT(this.bb.createScalarList(this.STATE.bind(this), this.stateLength()), this.bb.createScalarList(this.LOWER_BOUNDS.bind(this), this.lowerBoundsLength()), this.bb.createScalarList(this.UPPER_BOUNDS.bind(this), this.upperBoundsLength()), this.TRACE_ID());
    }
    unpackTo(_o) {
        _o.STATE = this.bb.createScalarList(this.STATE.bind(this), this.stateLength());
        _o.LOWER_BOUNDS = this.bb.createScalarList(this.LOWER_BOUNDS.bind(this), this.lowerBoundsLength());
        _o.UPPER_BOUNDS = this.bb.createScalarList(this.UPPER_BOUNDS.bind(this), this.upperBoundsLength());
        _o.TRACE_ID = this.TRACE_ID();
    }
}
export class NUMVectorSaturateRequestT {
    constructor(STATE = [], LOWER_BOUNDS = [], UPPER_BOUNDS = [], TRACE_ID = null) {
        this.STATE = STATE;
        this.LOWER_BOUNDS = LOWER_BOUNDS;
        this.UPPER_BOUNDS = UPPER_BOUNDS;
        this.TRACE_ID = TRACE_ID;
    }
    pack(builder) {
        const STATE = NUMVectorSaturateRequest.createStateVector(builder, this.STATE);
        const LOWER_BOUNDS = NUMVectorSaturateRequest.createLowerBoundsVector(builder, this.LOWER_BOUNDS);
        const UPPER_BOUNDS = NUMVectorSaturateRequest.createUpperBoundsVector(builder, this.UPPER_BOUNDS);
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        return NUMVectorSaturateRequest.createNUMVectorSaturateRequest(builder, STATE, LOWER_BOUNDS, UPPER_BOUNDS, TRACE_ID);
    }
}
