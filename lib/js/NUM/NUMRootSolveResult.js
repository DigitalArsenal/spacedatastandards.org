// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { numResultStatus } from './numResultStatus.js';
/**
 * Result of one scalar numerical root-solving operation.
 */
export class NUMRootSolveResult {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNUMRootSolveResult(bb, obj) {
        return (obj || new NUMRootSolveResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNUMRootSolveResult(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NUMRootSolveResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : numResultStatus.OK;
    }
    ERROR_MESSAGE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ROOT() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    ITERATIONS() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startNUMRootSolveResult(builder) {
        builder.startObject(6);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(0, STATUS, numResultStatus.OK);
    }
    static addErrorMessage(builder, ERROR_MESSAGEOffset) {
        builder.addFieldOffset(1, ERROR_MESSAGEOffset, 0);
    }
    static addRoot(builder, ROOT) {
        builder.addFieldFloat64(2, ROOT, 0.0);
    }
    static addResidual(builder, RESIDUAL) {
        builder.addFieldFloat64(3, RESIDUAL, 0.0);
    }
    static addIterations(builder, ITERATIONS) {
        builder.addFieldInt32(4, ITERATIONS, 0);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(5, TRACE_IDOffset, 0);
    }
    static endNUMRootSolveResult(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNUMRootSolveResult(builder, STATUS, ERROR_MESSAGEOffset, ROOT, RESIDUAL, ITERATIONS, TRACE_IDOffset) {
        NUMRootSolveResult.startNUMRootSolveResult(builder);
        NUMRootSolveResult.addStatus(builder, STATUS);
        NUMRootSolveResult.addErrorMessage(builder, ERROR_MESSAGEOffset);
        NUMRootSolveResult.addRoot(builder, ROOT);
        NUMRootSolveResult.addResidual(builder, RESIDUAL);
        NUMRootSolveResult.addIterations(builder, ITERATIONS);
        NUMRootSolveResult.addTraceId(builder, TRACE_IDOffset);
        return NUMRootSolveResult.endNUMRootSolveResult(builder);
    }
    unpack() {
        return new NUMRootSolveResultT(this.STATUS(), this.ERROR_MESSAGE(), this.ROOT(), this.RESIDUAL(), this.ITERATIONS(), this.TRACE_ID());
    }
    unpackTo(_o) {
        _o.STATUS = this.STATUS();
        _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
        _o.ROOT = this.ROOT();
        _o.RESIDUAL = this.RESIDUAL();
        _o.ITERATIONS = this.ITERATIONS();
        _o.TRACE_ID = this.TRACE_ID();
    }
}
export class NUMRootSolveResultT {
    constructor(STATUS = numResultStatus.OK, ERROR_MESSAGE = null, ROOT = 0.0, RESIDUAL = 0.0, ITERATIONS = 0, TRACE_ID = null) {
        this.STATUS = STATUS;
        this.ERROR_MESSAGE = ERROR_MESSAGE;
        this.ROOT = ROOT;
        this.RESIDUAL = RESIDUAL;
        this.ITERATIONS = ITERATIONS;
        this.TRACE_ID = TRACE_ID;
    }
    pack(builder) {
        const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE) : 0);
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        return NUMRootSolveResult.createNUMRootSolveResult(builder, this.STATUS, ERROR_MESSAGE, this.ROOT, this.RESIDUAL, this.ITERATIONS, TRACE_ID);
    }
}
