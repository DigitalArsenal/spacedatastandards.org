// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { numFunctionCode } from './numFunctionCode.js';
import { numOperationCode } from './numOperationCode.js';
/**
 * Request for one scalar numerical root-solving operation.
 */
export class NUMRootSolveRequest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsNUMRootSolveRequest(bb, obj) {
        return (obj || new NUMRootSolveRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsNUMRootSolveRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new NUMRootSolveRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    OPERATION() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : numOperationCode.UNKNOWN;
    }
    FUNCTION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : numFunctionCode.UNKNOWN;
    }
    INITIAL_ESTIMATE() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    TARGET_VALUE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    ACCURACY() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    MAX_ITERATIONS() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    TRACE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startNUMRootSolveRequest(builder) {
        builder.startObject(7);
    }
    static addOperation(builder, OPERATION) {
        builder.addFieldInt8(0, OPERATION, numOperationCode.UNKNOWN);
    }
    static addFunction(builder, FUNCTION) {
        builder.addFieldInt8(1, FUNCTION, numFunctionCode.UNKNOWN);
    }
    static addInitialEstimate(builder, INITIAL_ESTIMATE) {
        builder.addFieldFloat64(2, INITIAL_ESTIMATE, 0.0);
    }
    static addTargetValue(builder, TARGET_VALUE) {
        builder.addFieldFloat64(3, TARGET_VALUE, 0.0);
    }
    static addAccuracy(builder, ACCURACY) {
        builder.addFieldFloat64(4, ACCURACY, 0.0);
    }
    static addMaxIterations(builder, MAX_ITERATIONS) {
        builder.addFieldInt32(5, MAX_ITERATIONS, 0);
    }
    static addTraceId(builder, TRACE_IDOffset) {
        builder.addFieldOffset(6, TRACE_IDOffset, 0);
    }
    static endNUMRootSolveRequest(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createNUMRootSolveRequest(builder, OPERATION, FUNCTION, INITIAL_ESTIMATE, TARGET_VALUE, ACCURACY, MAX_ITERATIONS, TRACE_IDOffset) {
        NUMRootSolveRequest.startNUMRootSolveRequest(builder);
        NUMRootSolveRequest.addOperation(builder, OPERATION);
        NUMRootSolveRequest.addFunction(builder, FUNCTION);
        NUMRootSolveRequest.addInitialEstimate(builder, INITIAL_ESTIMATE);
        NUMRootSolveRequest.addTargetValue(builder, TARGET_VALUE);
        NUMRootSolveRequest.addAccuracy(builder, ACCURACY);
        NUMRootSolveRequest.addMaxIterations(builder, MAX_ITERATIONS);
        NUMRootSolveRequest.addTraceId(builder, TRACE_IDOffset);
        return NUMRootSolveRequest.endNUMRootSolveRequest(builder);
    }
    unpack() {
        return new NUMRootSolveRequestT(this.OPERATION(), this.FUNCTION(), this.INITIAL_ESTIMATE(), this.TARGET_VALUE(), this.ACCURACY(), this.MAX_ITERATIONS(), this.TRACE_ID());
    }
    unpackTo(_o) {
        _o.OPERATION = this.OPERATION();
        _o.FUNCTION = this.FUNCTION();
        _o.INITIAL_ESTIMATE = this.INITIAL_ESTIMATE();
        _o.TARGET_VALUE = this.TARGET_VALUE();
        _o.ACCURACY = this.ACCURACY();
        _o.MAX_ITERATIONS = this.MAX_ITERATIONS();
        _o.TRACE_ID = this.TRACE_ID();
    }
}
export class NUMRootSolveRequestT {
    constructor(OPERATION = numOperationCode.UNKNOWN, FUNCTION = numFunctionCode.UNKNOWN, INITIAL_ESTIMATE = 0.0, TARGET_VALUE = 0.0, ACCURACY = 0.0, MAX_ITERATIONS = 0, TRACE_ID = null) {
        this.OPERATION = OPERATION;
        this.FUNCTION = FUNCTION;
        this.INITIAL_ESTIMATE = INITIAL_ESTIMATE;
        this.TARGET_VALUE = TARGET_VALUE;
        this.ACCURACY = ACCURACY;
        this.MAX_ITERATIONS = MAX_ITERATIONS;
        this.TRACE_ID = TRACE_ID;
    }
    pack(builder) {
        const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID) : 0);
        return NUMRootSolveRequest.createNUMRootSolveRequest(builder, this.OPERATION, this.FUNCTION, this.INITIAL_ESTIMATE, this.TARGET_VALUE, this.ACCURACY, this.MAX_ITERATIONS, TRACE_ID);
    }
}
