// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { BEMHopSlot } from './BEMHopSlot.js';
import { BEMProvenance } from './BEMProvenance.js';
/**
 * Periodic beam-hopping schedule carried by a deployed beam descriptor.
 */
export class BEMHopSchedule {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsBEMHopSchedule(bb, obj) {
        return (obj || new BEMHopSchedule()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsBEMHopSchedule(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new BEMHopSchedule()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SCHEDULE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    /**
     * Seconds since 1970-01-01T00:00:00 UTC at which slot offsets begin.
     */
    EPOCH() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    PERIOD_S() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    REPEATS() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    SLOTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new BEMHopSlot()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    slotsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    PROVENANCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new BEMProvenance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startBEMHopSchedule(builder) {
        builder.startObject(6);
    }
    static addScheduleId(builder, SCHEDULE_IDOffset) {
        builder.addFieldOffset(0, SCHEDULE_IDOffset, 0);
    }
    static addEpoch(builder, EPOCH) {
        builder.addFieldFloat64(1, EPOCH, 0.0);
    }
    static addPeriodS(builder, PERIOD_S) {
        builder.addFieldFloat64(2, PERIOD_S, 0.0);
    }
    static addRepeats(builder, REPEATS) {
        builder.addFieldInt8(3, +REPEATS, +false);
    }
    static addSlots(builder, SLOTSOffset) {
        builder.addFieldOffset(4, SLOTSOffset, 0);
    }
    static createSlotsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSlotsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addProvenance(builder, PROVENANCEOffset) {
        builder.addFieldOffset(5, PROVENANCEOffset, 0);
    }
    static endBEMHopSchedule(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // SCHEDULE_ID
        builder.requiredField(offset, 12); // SLOTS
        builder.requiredField(offset, 14); // PROVENANCE
        return offset;
    }
    unpack() {
        return new BEMHopScheduleT(this.SCHEDULE_ID(), this.EPOCH(), this.PERIOD_S(), this.REPEATS(), this.bb.createObjList(this.SLOTS.bind(this), this.slotsLength()), (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null));
    }
    unpackTo(_o) {
        _o.SCHEDULE_ID = this.SCHEDULE_ID();
        _o.EPOCH = this.EPOCH();
        _o.PERIOD_S = this.PERIOD_S();
        _o.REPEATS = this.REPEATS();
        _o.SLOTS = this.bb.createObjList(this.SLOTS.bind(this), this.slotsLength());
        _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null);
    }
}
export class BEMHopScheduleT {
    constructor(SCHEDULE_ID = null, EPOCH = 0.0, PERIOD_S = 0.0, REPEATS = false, SLOTS = [], PROVENANCE = null) {
        this.SCHEDULE_ID = SCHEDULE_ID;
        this.EPOCH = EPOCH;
        this.PERIOD_S = PERIOD_S;
        this.REPEATS = REPEATS;
        this.SLOTS = SLOTS;
        this.PROVENANCE = PROVENANCE;
    }
    pack(builder) {
        const SCHEDULE_ID = (this.SCHEDULE_ID !== null ? builder.createString(this.SCHEDULE_ID) : 0);
        const SLOTS = BEMHopSchedule.createSlotsVector(builder, builder.createObjectOffsetList(this.SLOTS));
        const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE.pack(builder) : 0);
        BEMHopSchedule.startBEMHopSchedule(builder);
        BEMHopSchedule.addScheduleId(builder, SCHEDULE_ID);
        BEMHopSchedule.addEpoch(builder, this.EPOCH);
        BEMHopSchedule.addPeriodS(builder, this.PERIOD_S);
        BEMHopSchedule.addRepeats(builder, this.REPEATS);
        BEMHopSchedule.addSlots(builder, SLOTS);
        BEMHopSchedule.addProvenance(builder, PROVENANCE);
        return BEMHopSchedule.endBEMHopSchedule(builder);
    }
}
