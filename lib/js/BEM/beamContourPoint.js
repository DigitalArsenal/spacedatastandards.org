// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Beam Contour Point (gain pattern boundary)
 */
export class beamContourPoint {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsbeamContourPoint(bb, obj) {
        return (obj || new beamContourPoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsbeamContourPoint(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new beamContourPoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Latitude in degrees
     */
    LATITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Longitude in degrees
     */
    LONGITUDE() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Gain level in dBi at this contour
     */
    GAIN() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startbeamContourPoint(builder) {
        builder.startObject(3);
    }
    static addLatitude(builder, LATITUDE) {
        builder.addFieldFloat64(0, LATITUDE, 0.0);
    }
    static addLongitude(builder, LONGITUDE) {
        builder.addFieldFloat64(1, LONGITUDE, 0.0);
    }
    static addGain(builder, GAIN) {
        builder.addFieldFloat64(2, GAIN, 0.0);
    }
    static endbeamContourPoint(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createbeamContourPoint(builder, LATITUDE, LONGITUDE, GAIN) {
        beamContourPoint.startbeamContourPoint(builder);
        beamContourPoint.addLatitude(builder, LATITUDE);
        beamContourPoint.addLongitude(builder, LONGITUDE);
        beamContourPoint.addGain(builder, GAIN);
        return beamContourPoint.endbeamContourPoint(builder);
    }
    unpack() {
        return new beamContourPointT(this.LATITUDE(), this.LONGITUDE(), this.GAIN());
    }
    unpackTo(_o) {
        _o.LATITUDE = this.LATITUDE();
        _o.LONGITUDE = this.LONGITUDE();
        _o.GAIN = this.GAIN();
    }
}
export class beamContourPointT {
    constructor(LATITUDE = 0.0, LONGITUDE = 0.0, GAIN = 0.0) {
        this.LATITUDE = LATITUDE;
        this.LONGITUDE = LONGITUDE;
        this.GAIN = GAIN;
    }
    pack(builder) {
        return beamContourPoint.createbeamContourPoint(builder, this.LATITUDE, this.LONGITUDE, this.GAIN);
    }
}
