// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { KeyType } from './KeyType.js';
/**
 * Represents cryptographic key information.
 *
 * The publication paradigm is "xpub + derivation paths, not key material":
 * a verifier derives child public keys from XPUB and KEY_PATH wherever the
 * curve permits. That permission is asymmetric, PERMANENTLY AND BY DESIGN:
 *
 * - secp256k1 at a NON-hardened path (e.g., "m/44'/0'/0'/0/0") IS derivable
 *   from XPUB via BIP-32 public derivation, so such an entry may omit
 *   PUBLIC_KEY entirely and carry only {XPUB, KEY_PATH, KEY_TYPE, ALGORITHM}.
 * - ed25519 under SLIP-10 has NO public derivation at all — every ed25519
 *   child is hardened — so NO xpub can yield an ed25519 child public key,
 *   for anyone, ever. For ed25519 entries the published PUBLIC_KEY is
 *   authoritative and MUST remain present.
 *
 * A future revision that removes the published ed25519 PUBLIC_KEY would make
 * every ed25519-signed EPM unverifiable; the retained key is deliberate,
 * not a transitional leftover.
 */
export class CryptoKey {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsCryptoKey(bb, obj) {
        return (obj || new CryptoKey()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsCryptoKey(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new CryptoKey()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    PUBLIC_KEY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    XPUB(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PRIVATE_KEY(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    XPRIV(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    KEY_ADDRESS(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ADDRESS_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Type of the cryptographic key (signing or encryption)
     */
    KEY_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : KeyType.Signing;
    }
    KEY_PATH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ALGORITHM(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ENCODING(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startCryptoKey(builder) {
        builder.startObject(10);
    }
    static addPublicKey(builder, PUBLIC_KEYOffset) {
        builder.addFieldOffset(0, PUBLIC_KEYOffset, 0);
    }
    static addXpub(builder, XPUBOffset) {
        builder.addFieldOffset(1, XPUBOffset, 0);
    }
    static addPrivateKey(builder, PRIVATE_KEYOffset) {
        builder.addFieldOffset(2, PRIVATE_KEYOffset, 0);
    }
    static addXpriv(builder, XPRIVOffset) {
        builder.addFieldOffset(3, XPRIVOffset, 0);
    }
    static addKeyAddress(builder, KEY_ADDRESSOffset) {
        builder.addFieldOffset(4, KEY_ADDRESSOffset, 0);
    }
    static addAddressType(builder, ADDRESS_TYPEOffset) {
        builder.addFieldOffset(5, ADDRESS_TYPEOffset, 0);
    }
    static addKeyType(builder, KEY_TYPE) {
        builder.addFieldInt8(6, KEY_TYPE, KeyType.Signing);
    }
    static addKeyPath(builder, KEY_PATHOffset) {
        builder.addFieldOffset(7, KEY_PATHOffset, 0);
    }
    static addAlgorithm(builder, ALGORITHMOffset) {
        builder.addFieldOffset(8, ALGORITHMOffset, 0);
    }
    static addEncoding(builder, ENCODINGOffset) {
        builder.addFieldOffset(9, ENCODINGOffset, 0);
    }
    static endCryptoKey(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createCryptoKey(builder, PUBLIC_KEYOffset, XPUBOffset, PRIVATE_KEYOffset, XPRIVOffset, KEY_ADDRESSOffset, ADDRESS_TYPEOffset, KEY_TYPE, KEY_PATHOffset, ALGORITHMOffset, ENCODINGOffset) {
        CryptoKey.startCryptoKey(builder);
        CryptoKey.addPublicKey(builder, PUBLIC_KEYOffset);
        CryptoKey.addXpub(builder, XPUBOffset);
        CryptoKey.addPrivateKey(builder, PRIVATE_KEYOffset);
        CryptoKey.addXpriv(builder, XPRIVOffset);
        CryptoKey.addKeyAddress(builder, KEY_ADDRESSOffset);
        CryptoKey.addAddressType(builder, ADDRESS_TYPEOffset);
        CryptoKey.addKeyType(builder, KEY_TYPE);
        CryptoKey.addKeyPath(builder, KEY_PATHOffset);
        CryptoKey.addAlgorithm(builder, ALGORITHMOffset);
        CryptoKey.addEncoding(builder, ENCODINGOffset);
        return CryptoKey.endCryptoKey(builder);
    }
    unpack() {
        return new CryptoKeyT(this.PUBLIC_KEY(), this.XPUB(), this.PRIVATE_KEY(), this.XPRIV(), this.KEY_ADDRESS(), this.ADDRESS_TYPE(), this.KEY_TYPE(), this.KEY_PATH(), this.ALGORITHM(), this.ENCODING());
    }
    unpackTo(_o) {
        _o.PUBLIC_KEY = this.PUBLIC_KEY();
        _o.XPUB = this.XPUB();
        _o.PRIVATE_KEY = this.PRIVATE_KEY();
        _o.XPRIV = this.XPRIV();
        _o.KEY_ADDRESS = this.KEY_ADDRESS();
        _o.ADDRESS_TYPE = this.ADDRESS_TYPE();
        _o.KEY_TYPE = this.KEY_TYPE();
        _o.KEY_PATH = this.KEY_PATH();
        _o.ALGORITHM = this.ALGORITHM();
        _o.ENCODING = this.ENCODING();
    }
}
export class CryptoKeyT {
    constructor(PUBLIC_KEY = null, XPUB = null, PRIVATE_KEY = null, XPRIV = null, KEY_ADDRESS = null, ADDRESS_TYPE = null, KEY_TYPE = KeyType.Signing, KEY_PATH = null, ALGORITHM = null, ENCODING = null) {
        this.PUBLIC_KEY = PUBLIC_KEY;
        this.XPUB = XPUB;
        this.PRIVATE_KEY = PRIVATE_KEY;
        this.XPRIV = XPRIV;
        this.KEY_ADDRESS = KEY_ADDRESS;
        this.ADDRESS_TYPE = ADDRESS_TYPE;
        this.KEY_TYPE = KEY_TYPE;
        this.KEY_PATH = KEY_PATH;
        this.ALGORITHM = ALGORITHM;
        this.ENCODING = ENCODING;
    }
    pack(builder) {
        const PUBLIC_KEY = (this.PUBLIC_KEY !== null ? builder.createString(this.PUBLIC_KEY) : 0);
        const XPUB = (this.XPUB !== null ? builder.createString(this.XPUB) : 0);
        const PRIVATE_KEY = (this.PRIVATE_KEY !== null ? builder.createString(this.PRIVATE_KEY) : 0);
        const XPRIV = (this.XPRIV !== null ? builder.createString(this.XPRIV) : 0);
        const KEY_ADDRESS = (this.KEY_ADDRESS !== null ? builder.createString(this.KEY_ADDRESS) : 0);
        const ADDRESS_TYPE = (this.ADDRESS_TYPE !== null ? builder.createString(this.ADDRESS_TYPE) : 0);
        const KEY_PATH = (this.KEY_PATH !== null ? builder.createString(this.KEY_PATH) : 0);
        const ALGORITHM = (this.ALGORITHM !== null ? builder.createString(this.ALGORITHM) : 0);
        const ENCODING = (this.ENCODING !== null ? builder.createString(this.ENCODING) : 0);
        return CryptoKey.createCryptoKey(builder, PUBLIC_KEY, XPUB, PRIVATE_KEY, XPRIV, KEY_ADDRESS, ADDRESS_TYPE, this.KEY_TYPE, KEY_PATH, ALGORITHM, ENCODING);
    }
}
