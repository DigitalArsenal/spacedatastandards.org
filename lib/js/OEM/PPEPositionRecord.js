// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { polynomialBasisType } from './polynomialBasisType.js';
/**
 * A single time-segment record of polynomial coefficients for Cartesian position
 * (and optionally velocity). Coefficients are stored per axis.
 *
 * To evaluate position at time t within this record's validity window:
 *   1. Compute normalized time: tau = (t - EPOCH_MID) / EPOCH_HALF_SPAN
 *      where tau is in [-1, +1].
 *   2. Evaluate the polynomial basis of degree (NUM_COEFFICIENTS - 1) using
 *      the coefficients for each axis.
 *
 * Velocity coefficients, if present, follow the same evaluation procedure.
 * If HAS_VELOCITY_COEFFICIENTS is false, velocity can be obtained by
 * analytically differentiating the position polynomial.
 */
export class PPEPositionRecord {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPPEPositionRecord(bb, obj) {
        return (obj || new PPEPositionRecord()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPPEPositionRecord(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PPEPositionRecord()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    EPOCH_MID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Half-span of the validity window in seconds.
     * The full span is 2 * EPOCH_HALF_SPAN seconds centered on EPOCH_MID.
     */
    EPOCH_HALF_SPAN() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Number of polynomial coefficients per axis.
     * The polynomial degree is (NUM_COEFFICIENTS - 1).
     * Typical values: 8-32 for high-fidelity ephemeris fits.
     */
    NUM_COEFFICIENTS() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Polynomial basis type for interpreting the coefficient arrays.
     */
    BASIS_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : polynomialBasisType.CHEBYSHEV;
    }
    /**
     * Position coefficients for X-axis (km).
     * Length must equal NUM_COEFFICIENTS. Ordered c0, c1, ..., c_{N-1}.
     */
    POS_COEFF_X(index) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    posCoeffXLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    posCoeffXArray() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Position coefficients for Y-axis (km).
     * Length must equal NUM_COEFFICIENTS.
     */
    POS_COEFF_Y(index) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    posCoeffYLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    posCoeffYArray() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Position coefficients for Z-axis (km).
     * Length must equal NUM_COEFFICIENTS.
     */
    POS_COEFF_Z(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    posCoeffZLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    posCoeffZArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Whether explicit velocity coefficients are provided.
     * If false, velocity should be derived by differentiating the position polynomial.
     */
    HAS_VELOCITY_COEFFICIENTS() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    /**
     * Velocity coefficients for X-axis (km/s). Optional.
     * Length must equal NUM_COEFFICIENTS if present.
     */
    VEL_COEFF_X(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    velCoeffXLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    velCoeffXArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Velocity coefficients for Y-axis (km/s). Optional.
     */
    VEL_COEFF_Y(index) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    velCoeffYLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    velCoeffYArray() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Velocity coefficients for Z-axis (km/s). Optional.
     */
    VEL_COEFF_Z(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    velCoeffZLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    velCoeffZArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Maximum position fit residual over this segment (km). Optional quality metric.
     */
    MAX_POSITION_RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * RMS position fit residual over this segment (km). Optional quality metric.
     */
    RMS_POSITION_RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startPPEPositionRecord(builder) {
        builder.startObject(13);
    }
    static addEpochMid(builder, EPOCH_MIDOffset) {
        builder.addFieldOffset(0, EPOCH_MIDOffset, 0);
    }
    static addEpochHalfSpan(builder, EPOCH_HALF_SPAN) {
        builder.addFieldFloat64(1, EPOCH_HALF_SPAN, 0.0);
    }
    static addNumCoefficients(builder, NUM_COEFFICIENTS) {
        builder.addFieldInt16(2, NUM_COEFFICIENTS, 0);
    }
    static addBasisType(builder, BASIS_TYPE) {
        builder.addFieldInt8(3, BASIS_TYPE, polynomialBasisType.CHEBYSHEV);
    }
    static addPosCoeffX(builder, POS_COEFF_XOffset) {
        builder.addFieldOffset(4, POS_COEFF_XOffset, 0);
    }
    static createPosCoeffXVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startPosCoeffXVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addPosCoeffY(builder, POS_COEFF_YOffset) {
        builder.addFieldOffset(5, POS_COEFF_YOffset, 0);
    }
    static createPosCoeffYVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startPosCoeffYVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addPosCoeffZ(builder, POS_COEFF_ZOffset) {
        builder.addFieldOffset(6, POS_COEFF_ZOffset, 0);
    }
    static createPosCoeffZVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startPosCoeffZVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addHasVelocityCoefficients(builder, HAS_VELOCITY_COEFFICIENTS) {
        builder.addFieldInt8(7, +HAS_VELOCITY_COEFFICIENTS, +false);
    }
    static addVelCoeffX(builder, VEL_COEFF_XOffset) {
        builder.addFieldOffset(8, VEL_COEFF_XOffset, 0);
    }
    static createVelCoeffXVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startVelCoeffXVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addVelCoeffY(builder, VEL_COEFF_YOffset) {
        builder.addFieldOffset(9, VEL_COEFF_YOffset, 0);
    }
    static createVelCoeffYVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startVelCoeffYVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addVelCoeffZ(builder, VEL_COEFF_ZOffset) {
        builder.addFieldOffset(10, VEL_COEFF_ZOffset, 0);
    }
    static createVelCoeffZVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startVelCoeffZVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addMaxPositionResidual(builder, MAX_POSITION_RESIDUAL) {
        builder.addFieldFloat64(11, MAX_POSITION_RESIDUAL, 0.0);
    }
    static addRmsPositionResidual(builder, RMS_POSITION_RESIDUAL) {
        builder.addFieldFloat64(12, RMS_POSITION_RESIDUAL, 0.0);
    }
    static endPPEPositionRecord(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // EPOCH_MID
        builder.requiredField(offset, 12); // POS_COEFF_X
        builder.requiredField(offset, 14); // POS_COEFF_Y
        builder.requiredField(offset, 16); // POS_COEFF_Z
        return offset;
    }
    static createPPEPositionRecord(builder, EPOCH_MIDOffset, EPOCH_HALF_SPAN, NUM_COEFFICIENTS, BASIS_TYPE, POS_COEFF_XOffset, POS_COEFF_YOffset, POS_COEFF_ZOffset, HAS_VELOCITY_COEFFICIENTS, VEL_COEFF_XOffset, VEL_COEFF_YOffset, VEL_COEFF_ZOffset, MAX_POSITION_RESIDUAL, RMS_POSITION_RESIDUAL) {
        PPEPositionRecord.startPPEPositionRecord(builder);
        PPEPositionRecord.addEpochMid(builder, EPOCH_MIDOffset);
        PPEPositionRecord.addEpochHalfSpan(builder, EPOCH_HALF_SPAN);
        PPEPositionRecord.addNumCoefficients(builder, NUM_COEFFICIENTS);
        PPEPositionRecord.addBasisType(builder, BASIS_TYPE);
        PPEPositionRecord.addPosCoeffX(builder, POS_COEFF_XOffset);
        PPEPositionRecord.addPosCoeffY(builder, POS_COEFF_YOffset);
        PPEPositionRecord.addPosCoeffZ(builder, POS_COEFF_ZOffset);
        PPEPositionRecord.addHasVelocityCoefficients(builder, HAS_VELOCITY_COEFFICIENTS);
        PPEPositionRecord.addVelCoeffX(builder, VEL_COEFF_XOffset);
        PPEPositionRecord.addVelCoeffY(builder, VEL_COEFF_YOffset);
        PPEPositionRecord.addVelCoeffZ(builder, VEL_COEFF_ZOffset);
        PPEPositionRecord.addMaxPositionResidual(builder, MAX_POSITION_RESIDUAL);
        PPEPositionRecord.addRmsPositionResidual(builder, RMS_POSITION_RESIDUAL);
        return PPEPositionRecord.endPPEPositionRecord(builder);
    }
    unpack() {
        return new PPEPositionRecordT(this.EPOCH_MID(), this.EPOCH_HALF_SPAN(), this.NUM_COEFFICIENTS(), this.BASIS_TYPE(), this.bb.createScalarList(this.POS_COEFF_X.bind(this), this.posCoeffXLength()), this.bb.createScalarList(this.POS_COEFF_Y.bind(this), this.posCoeffYLength()), this.bb.createScalarList(this.POS_COEFF_Z.bind(this), this.posCoeffZLength()), this.HAS_VELOCITY_COEFFICIENTS(), this.bb.createScalarList(this.VEL_COEFF_X.bind(this), this.velCoeffXLength()), this.bb.createScalarList(this.VEL_COEFF_Y.bind(this), this.velCoeffYLength()), this.bb.createScalarList(this.VEL_COEFF_Z.bind(this), this.velCoeffZLength()), this.MAX_POSITION_RESIDUAL(), this.RMS_POSITION_RESIDUAL());
    }
    unpackTo(_o) {
        _o.EPOCH_MID = this.EPOCH_MID();
        _o.EPOCH_HALF_SPAN = this.EPOCH_HALF_SPAN();
        _o.NUM_COEFFICIENTS = this.NUM_COEFFICIENTS();
        _o.BASIS_TYPE = this.BASIS_TYPE();
        _o.POS_COEFF_X = this.bb.createScalarList(this.POS_COEFF_X.bind(this), this.posCoeffXLength());
        _o.POS_COEFF_Y = this.bb.createScalarList(this.POS_COEFF_Y.bind(this), this.posCoeffYLength());
        _o.POS_COEFF_Z = this.bb.createScalarList(this.POS_COEFF_Z.bind(this), this.posCoeffZLength());
        _o.HAS_VELOCITY_COEFFICIENTS = this.HAS_VELOCITY_COEFFICIENTS();
        _o.VEL_COEFF_X = this.bb.createScalarList(this.VEL_COEFF_X.bind(this), this.velCoeffXLength());
        _o.VEL_COEFF_Y = this.bb.createScalarList(this.VEL_COEFF_Y.bind(this), this.velCoeffYLength());
        _o.VEL_COEFF_Z = this.bb.createScalarList(this.VEL_COEFF_Z.bind(this), this.velCoeffZLength());
        _o.MAX_POSITION_RESIDUAL = this.MAX_POSITION_RESIDUAL();
        _o.RMS_POSITION_RESIDUAL = this.RMS_POSITION_RESIDUAL();
    }
}
export class PPEPositionRecordT {
    constructor(EPOCH_MID = null, EPOCH_HALF_SPAN = 0.0, NUM_COEFFICIENTS = 0, BASIS_TYPE = polynomialBasisType.CHEBYSHEV, POS_COEFF_X = [], POS_COEFF_Y = [], POS_COEFF_Z = [], HAS_VELOCITY_COEFFICIENTS = false, VEL_COEFF_X = [], VEL_COEFF_Y = [], VEL_COEFF_Z = [], MAX_POSITION_RESIDUAL = 0.0, RMS_POSITION_RESIDUAL = 0.0) {
        this.EPOCH_MID = EPOCH_MID;
        this.EPOCH_HALF_SPAN = EPOCH_HALF_SPAN;
        this.NUM_COEFFICIENTS = NUM_COEFFICIENTS;
        this.BASIS_TYPE = BASIS_TYPE;
        this.POS_COEFF_X = POS_COEFF_X;
        this.POS_COEFF_Y = POS_COEFF_Y;
        this.POS_COEFF_Z = POS_COEFF_Z;
        this.HAS_VELOCITY_COEFFICIENTS = HAS_VELOCITY_COEFFICIENTS;
        this.VEL_COEFF_X = VEL_COEFF_X;
        this.VEL_COEFF_Y = VEL_COEFF_Y;
        this.VEL_COEFF_Z = VEL_COEFF_Z;
        this.MAX_POSITION_RESIDUAL = MAX_POSITION_RESIDUAL;
        this.RMS_POSITION_RESIDUAL = RMS_POSITION_RESIDUAL;
    }
    pack(builder) {
        const EPOCH_MID = (this.EPOCH_MID !== null ? builder.createString(this.EPOCH_MID) : 0);
        const POS_COEFF_X = PPEPositionRecord.createPosCoeffXVector(builder, this.POS_COEFF_X);
        const POS_COEFF_Y = PPEPositionRecord.createPosCoeffYVector(builder, this.POS_COEFF_Y);
        const POS_COEFF_Z = PPEPositionRecord.createPosCoeffZVector(builder, this.POS_COEFF_Z);
        const VEL_COEFF_X = PPEPositionRecord.createVelCoeffXVector(builder, this.VEL_COEFF_X);
        const VEL_COEFF_Y = PPEPositionRecord.createVelCoeffYVector(builder, this.VEL_COEFF_Y);
        const VEL_COEFF_Z = PPEPositionRecord.createVelCoeffZVector(builder, this.VEL_COEFF_Z);
        return PPEPositionRecord.createPPEPositionRecord(builder, EPOCH_MID, this.EPOCH_HALF_SPAN, this.NUM_COEFFICIENTS, this.BASIS_TYPE, POS_COEFF_X, POS_COEFF_Y, POS_COEFF_Z, this.HAS_VELOCITY_COEFFICIENTS, VEL_COEFF_X, VEL_COEFF_Y, VEL_COEFF_Z, this.MAX_POSITION_RESIDUAL, this.RMS_POSITION_RESIDUAL);
    }
}
