// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { appContentEncoding } from './appContentEncoding.js';
/**
 * One UI page of the app. Exactly one of the two delivery mechanisms must
 * be populated: inline CONTENT (with ENCODING describing its string form),
 * or MODULE_ID plus URL for a page served by a member module. Inline pages
 * must be fully self-contained — CSS and JS inlined, all assets embedded
 * as data URIs, zero external requests.
 */
export class APPUIPage {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAPPUIPage(bb, obj) {
        return (obj || new APPUIPage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAPPUIPage(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new APPUIPage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    TITLE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ICON(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    COLOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TEXT_COLOR(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CONTENT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * String form of CONTENT.
     */
    ENCODING() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : appContentEncoding.UTF8;
    }
    MEDIA_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CONTENT_SHA256(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * True for the page the launcher opens first. Exactly one page in an
     * app must set this when UI is present.
     */
    ENTRY() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    MODULE_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    URL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startAPPUIPage(builder) {
        builder.startObject(13);
    }
    static addId(builder, IDOffset) {
        builder.addFieldOffset(0, IDOffset, 0);
    }
    static addTitle(builder, TITLEOffset) {
        builder.addFieldOffset(1, TITLEOffset, 0);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(2, DESCRIPTIONOffset, 0);
    }
    static addIcon(builder, ICONOffset) {
        builder.addFieldOffset(3, ICONOffset, 0);
    }
    static addColor(builder, COLOROffset) {
        builder.addFieldOffset(4, COLOROffset, 0);
    }
    static addTextColor(builder, TEXT_COLOROffset) {
        builder.addFieldOffset(5, TEXT_COLOROffset, 0);
    }
    static addContent(builder, CONTENTOffset) {
        builder.addFieldOffset(6, CONTENTOffset, 0);
    }
    static addEncoding(builder, ENCODING) {
        builder.addFieldInt8(7, ENCODING, appContentEncoding.UTF8);
    }
    static addMediaType(builder, MEDIA_TYPEOffset) {
        builder.addFieldOffset(8, MEDIA_TYPEOffset, 0);
    }
    static addContentSha256(builder, CONTENT_SHA256Offset) {
        builder.addFieldOffset(9, CONTENT_SHA256Offset, 0);
    }
    static addEntry(builder, ENTRY) {
        builder.addFieldInt8(10, +ENTRY, +false);
    }
    static addModuleId(builder, MODULE_IDOffset) {
        builder.addFieldOffset(11, MODULE_IDOffset, 0);
    }
    static addUrl(builder, URLOffset) {
        builder.addFieldOffset(12, URLOffset, 0);
    }
    static endAPPUIPage(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // ID
        return offset;
    }
    static createAPPUIPage(builder, IDOffset, TITLEOffset, DESCRIPTIONOffset, ICONOffset, COLOROffset, TEXT_COLOROffset, CONTENTOffset, ENCODING, MEDIA_TYPEOffset, CONTENT_SHA256Offset, ENTRY, MODULE_IDOffset, URLOffset) {
        APPUIPage.startAPPUIPage(builder);
        APPUIPage.addId(builder, IDOffset);
        APPUIPage.addTitle(builder, TITLEOffset);
        APPUIPage.addDescription(builder, DESCRIPTIONOffset);
        APPUIPage.addIcon(builder, ICONOffset);
        APPUIPage.addColor(builder, COLOROffset);
        APPUIPage.addTextColor(builder, TEXT_COLOROffset);
        APPUIPage.addContent(builder, CONTENTOffset);
        APPUIPage.addEncoding(builder, ENCODING);
        APPUIPage.addMediaType(builder, MEDIA_TYPEOffset);
        APPUIPage.addContentSha256(builder, CONTENT_SHA256Offset);
        APPUIPage.addEntry(builder, ENTRY);
        APPUIPage.addModuleId(builder, MODULE_IDOffset);
        APPUIPage.addUrl(builder, URLOffset);
        return APPUIPage.endAPPUIPage(builder);
    }
    unpack() {
        return new APPUIPageT(this.ID(), this.TITLE(), this.DESCRIPTION(), this.ICON(), this.COLOR(), this.TEXT_COLOR(), this.CONTENT(), this.ENCODING(), this.MEDIA_TYPE(), this.CONTENT_SHA256(), this.ENTRY(), this.MODULE_ID(), this.URL());
    }
    unpackTo(_o) {
        _o.ID = this.ID();
        _o.TITLE = this.TITLE();
        _o.DESCRIPTION = this.DESCRIPTION();
        _o.ICON = this.ICON();
        _o.COLOR = this.COLOR();
        _o.TEXT_COLOR = this.TEXT_COLOR();
        _o.CONTENT = this.CONTENT();
        _o.ENCODING = this.ENCODING();
        _o.MEDIA_TYPE = this.MEDIA_TYPE();
        _o.CONTENT_SHA256 = this.CONTENT_SHA256();
        _o.ENTRY = this.ENTRY();
        _o.MODULE_ID = this.MODULE_ID();
        _o.URL = this.URL();
    }
}
export class APPUIPageT {
    constructor(ID = null, TITLE = null, DESCRIPTION = null, ICON = null, COLOR = null, TEXT_COLOR = null, CONTENT = null, ENCODING = appContentEncoding.UTF8, MEDIA_TYPE = null, CONTENT_SHA256 = null, ENTRY = false, MODULE_ID = null, URL = null) {
        this.ID = ID;
        this.TITLE = TITLE;
        this.DESCRIPTION = DESCRIPTION;
        this.ICON = ICON;
        this.COLOR = COLOR;
        this.TEXT_COLOR = TEXT_COLOR;
        this.CONTENT = CONTENT;
        this.ENCODING = ENCODING;
        this.MEDIA_TYPE = MEDIA_TYPE;
        this.CONTENT_SHA256 = CONTENT_SHA256;
        this.ENTRY = ENTRY;
        this.MODULE_ID = MODULE_ID;
        this.URL = URL;
    }
    pack(builder) {
        const ID = (this.ID !== null ? builder.createString(this.ID) : 0);
        const TITLE = (this.TITLE !== null ? builder.createString(this.TITLE) : 0);
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        const ICON = (this.ICON !== null ? builder.createString(this.ICON) : 0);
        const COLOR = (this.COLOR !== null ? builder.createString(this.COLOR) : 0);
        const TEXT_COLOR = (this.TEXT_COLOR !== null ? builder.createString(this.TEXT_COLOR) : 0);
        const CONTENT = (this.CONTENT !== null ? builder.createString(this.CONTENT) : 0);
        const MEDIA_TYPE = (this.MEDIA_TYPE !== null ? builder.createString(this.MEDIA_TYPE) : 0);
        const CONTENT_SHA256 = (this.CONTENT_SHA256 !== null ? builder.createString(this.CONTENT_SHA256) : 0);
        const MODULE_ID = (this.MODULE_ID !== null ? builder.createString(this.MODULE_ID) : 0);
        const URL = (this.URL !== null ? builder.createString(this.URL) : 0);
        return APPUIPage.createAPPUIPage(builder, ID, TITLE, DESCRIPTION, ICON, COLOR, TEXT_COLOR, CONTENT, this.ENCODING, MEDIA_TYPE, CONTENT_SHA256, this.ENTRY, MODULE_ID, URL);
    }
}
