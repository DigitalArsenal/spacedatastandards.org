// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Sonar and Underwater Acoustics
 */
export class SON {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSON(bb, obj) {
        return (obj || new SON()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSON(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SON()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$SON');
    }
    COMMAND(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ACTIVE_CONFIG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PASSIVE_CONFIG(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TL_REQUEST(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ENVIRONMENT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TARGET_SIGNATURE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TMA_INPUT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TORPEDO_SEEKER(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startSON(builder) {
        builder.startObject(8);
    }
    static addCommand(builder, COMMANDOffset) {
        builder.addFieldOffset(0, COMMANDOffset, 0);
    }
    static addActiveConfig(builder, ACTIVE_CONFIGOffset) {
        builder.addFieldOffset(1, ACTIVE_CONFIGOffset, 0);
    }
    static addPassiveConfig(builder, PASSIVE_CONFIGOffset) {
        builder.addFieldOffset(2, PASSIVE_CONFIGOffset, 0);
    }
    static addTlRequest(builder, TL_REQUESTOffset) {
        builder.addFieldOffset(3, TL_REQUESTOffset, 0);
    }
    static addEnvironment(builder, ENVIRONMENTOffset) {
        builder.addFieldOffset(4, ENVIRONMENTOffset, 0);
    }
    static addTargetSignature(builder, TARGET_SIGNATUREOffset) {
        builder.addFieldOffset(5, TARGET_SIGNATUREOffset, 0);
    }
    static addTmaInput(builder, TMA_INPUTOffset) {
        builder.addFieldOffset(6, TMA_INPUTOffset, 0);
    }
    static addTorpedoSeeker(builder, TORPEDO_SEEKEROffset) {
        builder.addFieldOffset(7, TORPEDO_SEEKEROffset, 0);
    }
    static endSON(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishSONBuffer(builder, offset) {
        builder.finish(offset, '$SON');
    }
    static finishSizePrefixedSONBuffer(builder, offset) {
        builder.finish(offset, '$SON', true);
    }
    static createSON(builder, COMMANDOffset, ACTIVE_CONFIGOffset, PASSIVE_CONFIGOffset, TL_REQUESTOffset, ENVIRONMENTOffset, TARGET_SIGNATUREOffset, TMA_INPUTOffset, TORPEDO_SEEKEROffset) {
        SON.startSON(builder);
        SON.addCommand(builder, COMMANDOffset);
        SON.addActiveConfig(builder, ACTIVE_CONFIGOffset);
        SON.addPassiveConfig(builder, PASSIVE_CONFIGOffset);
        SON.addTlRequest(builder, TL_REQUESTOffset);
        SON.addEnvironment(builder, ENVIRONMENTOffset);
        SON.addTargetSignature(builder, TARGET_SIGNATUREOffset);
        SON.addTmaInput(builder, TMA_INPUTOffset);
        SON.addTorpedoSeeker(builder, TORPEDO_SEEKEROffset);
        return SON.endSON(builder);
    }
    unpack() {
        return new SONT(this.COMMAND(), this.ACTIVE_CONFIG(), this.PASSIVE_CONFIG(), this.TL_REQUEST(), this.ENVIRONMENT(), this.TARGET_SIGNATURE(), this.TMA_INPUT(), this.TORPEDO_SEEKER());
    }
    unpackTo(_o) {
        _o.COMMAND = this.COMMAND();
        _o.ACTIVE_CONFIG = this.ACTIVE_CONFIG();
        _o.PASSIVE_CONFIG = this.PASSIVE_CONFIG();
        _o.TL_REQUEST = this.TL_REQUEST();
        _o.ENVIRONMENT = this.ENVIRONMENT();
        _o.TARGET_SIGNATURE = this.TARGET_SIGNATURE();
        _o.TMA_INPUT = this.TMA_INPUT();
        _o.TORPEDO_SEEKER = this.TORPEDO_SEEKER();
    }
}
export class SONT {
    constructor(COMMAND = null, ACTIVE_CONFIG = null, PASSIVE_CONFIG = null, TL_REQUEST = null, ENVIRONMENT = null, TARGET_SIGNATURE = null, TMA_INPUT = null, TORPEDO_SEEKER = null) {
        this.COMMAND = COMMAND;
        this.ACTIVE_CONFIG = ACTIVE_CONFIG;
        this.PASSIVE_CONFIG = PASSIVE_CONFIG;
        this.TL_REQUEST = TL_REQUEST;
        this.ENVIRONMENT = ENVIRONMENT;
        this.TARGET_SIGNATURE = TARGET_SIGNATURE;
        this.TMA_INPUT = TMA_INPUT;
        this.TORPEDO_SEEKER = TORPEDO_SEEKER;
    }
    pack(builder) {
        const COMMAND = (this.COMMAND !== null ? builder.createString(this.COMMAND) : 0);
        const ACTIVE_CONFIG = (this.ACTIVE_CONFIG !== null ? builder.createString(this.ACTIVE_CONFIG) : 0);
        const PASSIVE_CONFIG = (this.PASSIVE_CONFIG !== null ? builder.createString(this.PASSIVE_CONFIG) : 0);
        const TL_REQUEST = (this.TL_REQUEST !== null ? builder.createString(this.TL_REQUEST) : 0);
        const ENVIRONMENT = (this.ENVIRONMENT !== null ? builder.createString(this.ENVIRONMENT) : 0);
        const TARGET_SIGNATURE = (this.TARGET_SIGNATURE !== null ? builder.createString(this.TARGET_SIGNATURE) : 0);
        const TMA_INPUT = (this.TMA_INPUT !== null ? builder.createString(this.TMA_INPUT) : 0);
        const TORPEDO_SEEKER = (this.TORPEDO_SEEKER !== null ? builder.createString(this.TORPEDO_SEEKER) : 0);
        return SON.createSON(builder, COMMAND, ACTIVE_CONFIG, PASSIVE_CONFIG, TL_REQUEST, ENVIRONMENT, TARGET_SIGNATURE, TMA_INPUT, TORPEDO_SEEKER);
    }
}
