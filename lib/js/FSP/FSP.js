// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { FieldStreamAudience } from './FieldStreamAudience.js';
import { FieldStreamRule } from './FieldStreamRule.js';
import { fieldStreamRevocationCategory } from './fieldStreamRevocationCategory.js';
export class FSP {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsFSP(bb, obj) {
        return (obj || new FSP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsFSP(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new FSP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$FSP');
    }
    POLICY_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    POLICY_VERSION() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 1;
    }
    PROVIDER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    LISTING_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    STREAM_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SCHEMA_CODE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SCHEMA_HASH(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    schemaHashLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    schemaHashArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    AUDIENCES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? (obj || new FieldStreamAudience()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    audiencesLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    RULES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? (obj || new FieldStreamRule()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    rulesLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    ALLOWED_OPERATIONS(index) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readInt8(this.bb.__vector(this.bb_pos + offset) + index) : null;
    }
    allowedOperationsLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    allowedOperationsArray() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? new Int8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    KEY_SCOPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    KEY_EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    VALID_FROM() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    EXPIRES_AT() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    REVOCATION_STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : fieldStreamRevocationCategory.Active;
    }
    REVOKED_AT() {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    REVOCATION_REASON(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PROVIDER_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    providerSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    providerSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 38);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startFSP(builder) {
        builder.startObject(18);
    }
    static addPolicyId(builder, POLICY_IDOffset) {
        builder.addFieldOffset(0, POLICY_IDOffset, 0);
    }
    static addPolicyVersion(builder, POLICY_VERSION) {
        builder.addFieldInt32(1, POLICY_VERSION, 1);
    }
    static addProviderPeerId(builder, PROVIDER_PEER_IDOffset) {
        builder.addFieldOffset(2, PROVIDER_PEER_IDOffset, 0);
    }
    static addListingId(builder, LISTING_IDOffset) {
        builder.addFieldOffset(3, LISTING_IDOffset, 0);
    }
    static addStreamId(builder, STREAM_IDOffset) {
        builder.addFieldOffset(4, STREAM_IDOffset, 0);
    }
    static addSchemaCode(builder, SCHEMA_CODEOffset) {
        builder.addFieldOffset(5, SCHEMA_CODEOffset, 0);
    }
    static addSchemaHash(builder, SCHEMA_HASHOffset) {
        builder.addFieldOffset(6, SCHEMA_HASHOffset, 0);
    }
    static createSchemaHashVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startSchemaHashVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addAudiences(builder, AUDIENCESOffset) {
        builder.addFieldOffset(7, AUDIENCESOffset, 0);
    }
    static createAudiencesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAudiencesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addRules(builder, RULESOffset) {
        builder.addFieldOffset(8, RULESOffset, 0);
    }
    static createRulesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRulesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAllowedOperations(builder, ALLOWED_OPERATIONSOffset) {
        builder.addFieldOffset(9, ALLOWED_OPERATIONSOffset, 0);
    }
    static createAllowedOperationsVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startAllowedOperationsVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addKeyScope(builder, KEY_SCOPEOffset) {
        builder.addFieldOffset(10, KEY_SCOPEOffset, 0);
    }
    static addKeyEpoch(builder, KEY_EPOCHOffset) {
        builder.addFieldOffset(11, KEY_EPOCHOffset, 0);
    }
    static addValidFrom(builder, VALID_FROM) {
        builder.addFieldInt64(12, VALID_FROM, BigInt('0'));
    }
    static addExpiresAt(builder, EXPIRES_AT) {
        builder.addFieldInt64(13, EXPIRES_AT, BigInt('0'));
    }
    static addRevocationStatus(builder, REVOCATION_STATUS) {
        builder.addFieldInt8(14, REVOCATION_STATUS, fieldStreamRevocationCategory.Active);
    }
    static addRevokedAt(builder, REVOKED_AT) {
        builder.addFieldInt64(15, REVOKED_AT, BigInt('0'));
    }
    static addRevocationReason(builder, REVOCATION_REASONOffset) {
        builder.addFieldOffset(16, REVOCATION_REASONOffset, 0);
    }
    static addProviderSignature(builder, PROVIDER_SIGNATUREOffset) {
        builder.addFieldOffset(17, PROVIDER_SIGNATUREOffset, 0);
    }
    static createProviderSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startProviderSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endFSP(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // POLICY_ID
        builder.requiredField(offset, 8); // PROVIDER_PEER_ID
        builder.requiredField(offset, 10); // LISTING_ID
        builder.requiredField(offset, 12); // STREAM_ID
        builder.requiredField(offset, 14); // SCHEMA_CODE
        return offset;
    }
    static finishFSPBuffer(builder, offset) {
        builder.finish(offset, '$FSP');
    }
    static finishSizePrefixedFSPBuffer(builder, offset) {
        builder.finish(offset, '$FSP', true);
    }
    static createFSP(builder, POLICY_IDOffset, POLICY_VERSION, PROVIDER_PEER_IDOffset, LISTING_IDOffset, STREAM_IDOffset, SCHEMA_CODEOffset, SCHEMA_HASHOffset, AUDIENCESOffset, RULESOffset, ALLOWED_OPERATIONSOffset, KEY_SCOPEOffset, KEY_EPOCHOffset, VALID_FROM, EXPIRES_AT, REVOCATION_STATUS, REVOKED_AT, REVOCATION_REASONOffset, PROVIDER_SIGNATUREOffset) {
        FSP.startFSP(builder);
        FSP.addPolicyId(builder, POLICY_IDOffset);
        FSP.addPolicyVersion(builder, POLICY_VERSION);
        FSP.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
        FSP.addListingId(builder, LISTING_IDOffset);
        FSP.addStreamId(builder, STREAM_IDOffset);
        FSP.addSchemaCode(builder, SCHEMA_CODEOffset);
        FSP.addSchemaHash(builder, SCHEMA_HASHOffset);
        FSP.addAudiences(builder, AUDIENCESOffset);
        FSP.addRules(builder, RULESOffset);
        FSP.addAllowedOperations(builder, ALLOWED_OPERATIONSOffset);
        FSP.addKeyScope(builder, KEY_SCOPEOffset);
        FSP.addKeyEpoch(builder, KEY_EPOCHOffset);
        FSP.addValidFrom(builder, VALID_FROM);
        FSP.addExpiresAt(builder, EXPIRES_AT);
        FSP.addRevocationStatus(builder, REVOCATION_STATUS);
        FSP.addRevokedAt(builder, REVOKED_AT);
        FSP.addRevocationReason(builder, REVOCATION_REASONOffset);
        FSP.addProviderSignature(builder, PROVIDER_SIGNATUREOffset);
        return FSP.endFSP(builder);
    }
    unpack() {
        return new FSPT(this.POLICY_ID(), this.POLICY_VERSION(), this.PROVIDER_PEER_ID(), this.LISTING_ID(), this.STREAM_ID(), this.SCHEMA_CODE(), this.bb.createScalarList(this.SCHEMA_HASH.bind(this), this.schemaHashLength()), this.bb.createObjList(this.AUDIENCES.bind(this), this.audiencesLength()), this.bb.createObjList(this.RULES.bind(this), this.rulesLength()), this.bb.createScalarList(this.ALLOWED_OPERATIONS.bind(this), this.allowedOperationsLength()), this.KEY_SCOPE(), this.KEY_EPOCH(), this.VALID_FROM(), this.EXPIRES_AT(), this.REVOCATION_STATUS(), this.REVOKED_AT(), this.REVOCATION_REASON(), this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength()));
    }
    unpackTo(_o) {
        _o.POLICY_ID = this.POLICY_ID();
        _o.POLICY_VERSION = this.POLICY_VERSION();
        _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
        _o.LISTING_ID = this.LISTING_ID();
        _o.STREAM_ID = this.STREAM_ID();
        _o.SCHEMA_CODE = this.SCHEMA_CODE();
        _o.SCHEMA_HASH = this.bb.createScalarList(this.SCHEMA_HASH.bind(this), this.schemaHashLength());
        _o.AUDIENCES = this.bb.createObjList(this.AUDIENCES.bind(this), this.audiencesLength());
        _o.RULES = this.bb.createObjList(this.RULES.bind(this), this.rulesLength());
        _o.ALLOWED_OPERATIONS = this.bb.createScalarList(this.ALLOWED_OPERATIONS.bind(this), this.allowedOperationsLength());
        _o.KEY_SCOPE = this.KEY_SCOPE();
        _o.KEY_EPOCH = this.KEY_EPOCH();
        _o.VALID_FROM = this.VALID_FROM();
        _o.EXPIRES_AT = this.EXPIRES_AT();
        _o.REVOCATION_STATUS = this.REVOCATION_STATUS();
        _o.REVOKED_AT = this.REVOKED_AT();
        _o.REVOCATION_REASON = this.REVOCATION_REASON();
        _o.PROVIDER_SIGNATURE = this.bb.createScalarList(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength());
    }
}
export class FSPT {
    constructor(POLICY_ID = null, POLICY_VERSION = 1, PROVIDER_PEER_ID = null, LISTING_ID = null, STREAM_ID = null, SCHEMA_CODE = null, SCHEMA_HASH = [], AUDIENCES = [], RULES = [], ALLOWED_OPERATIONS = [], KEY_SCOPE = null, KEY_EPOCH = null, VALID_FROM = BigInt('0'), EXPIRES_AT = BigInt('0'), REVOCATION_STATUS = fieldStreamRevocationCategory.Active, REVOKED_AT = BigInt('0'), REVOCATION_REASON = null, PROVIDER_SIGNATURE = []) {
        this.POLICY_ID = POLICY_ID;
        this.POLICY_VERSION = POLICY_VERSION;
        this.PROVIDER_PEER_ID = PROVIDER_PEER_ID;
        this.LISTING_ID = LISTING_ID;
        this.STREAM_ID = STREAM_ID;
        this.SCHEMA_CODE = SCHEMA_CODE;
        this.SCHEMA_HASH = SCHEMA_HASH;
        this.AUDIENCES = AUDIENCES;
        this.RULES = RULES;
        this.ALLOWED_OPERATIONS = ALLOWED_OPERATIONS;
        this.KEY_SCOPE = KEY_SCOPE;
        this.KEY_EPOCH = KEY_EPOCH;
        this.VALID_FROM = VALID_FROM;
        this.EXPIRES_AT = EXPIRES_AT;
        this.REVOCATION_STATUS = REVOCATION_STATUS;
        this.REVOKED_AT = REVOKED_AT;
        this.REVOCATION_REASON = REVOCATION_REASON;
        this.PROVIDER_SIGNATURE = PROVIDER_SIGNATURE;
    }
    pack(builder) {
        const POLICY_ID = (this.POLICY_ID !== null ? builder.createString(this.POLICY_ID) : 0);
        const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID) : 0);
        const LISTING_ID = (this.LISTING_ID !== null ? builder.createString(this.LISTING_ID) : 0);
        const STREAM_ID = (this.STREAM_ID !== null ? builder.createString(this.STREAM_ID) : 0);
        const SCHEMA_CODE = (this.SCHEMA_CODE !== null ? builder.createString(this.SCHEMA_CODE) : 0);
        const SCHEMA_HASH = FSP.createSchemaHashVector(builder, this.SCHEMA_HASH);
        const AUDIENCES = FSP.createAudiencesVector(builder, builder.createObjectOffsetList(this.AUDIENCES));
        const RULES = FSP.createRulesVector(builder, builder.createObjectOffsetList(this.RULES));
        const ALLOWED_OPERATIONS = FSP.createAllowedOperationsVector(builder, this.ALLOWED_OPERATIONS);
        const KEY_SCOPE = (this.KEY_SCOPE !== null ? builder.createString(this.KEY_SCOPE) : 0);
        const KEY_EPOCH = (this.KEY_EPOCH !== null ? builder.createString(this.KEY_EPOCH) : 0);
        const REVOCATION_REASON = (this.REVOCATION_REASON !== null ? builder.createString(this.REVOCATION_REASON) : 0);
        const PROVIDER_SIGNATURE = FSP.createProviderSignatureVector(builder, this.PROVIDER_SIGNATURE);
        return FSP.createFSP(builder, POLICY_ID, this.POLICY_VERSION, PROVIDER_PEER_ID, LISTING_ID, STREAM_ID, SCHEMA_CODE, SCHEMA_HASH, AUDIENCES, RULES, ALLOWED_OPERATIONS, KEY_SCOPE, KEY_EPOCH, this.VALID_FROM, this.EXPIRES_AT, this.REVOCATION_STATUS, this.REVOKED_AT, REVOCATION_REASON, PROVIDER_SIGNATURE);
    }
}
