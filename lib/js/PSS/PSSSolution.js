// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PSSConstraintValue } from './PSSConstraintValue.js';
import { PSSObjectiveValue } from './PSSObjectiveValue.js';
import { PSSProvenance } from './PSSProvenance.js';
import { PSSVariableValue } from './PSSVariableValue.js';
import { pssSolutionStatus } from './pssSolutionStatus.js';
export class PSSSolution {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPSSSolution(bb, obj) {
        return (obj || new PSSSolution()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPSSSolution(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PSSSolution()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SOLUTION_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    STATUS() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : pssSolutionStatus.UNSPECIFIED;
    }
    VARIABLES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new PSSVariableValue()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    variablesLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    OBJECTIVES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new PSSObjectiveValue()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    objectivesLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    CONSTRAINTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new PSSConstraintValue()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    constraintsLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    DOMINANCE_RANK() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    CROWDING_DISTANCE() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    VERIFY_BY_PROPAGATION_RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    PROVENANCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? (obj || new PSSProvenance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startPSSSolution(builder) {
        builder.startObject(9);
    }
    static addSolutionId(builder, SOLUTION_IDOffset) {
        builder.addFieldOffset(0, SOLUTION_IDOffset, 0);
    }
    static addStatus(builder, STATUS) {
        builder.addFieldInt8(1, STATUS, pssSolutionStatus.UNSPECIFIED);
    }
    static addVariables(builder, VARIABLESOffset) {
        builder.addFieldOffset(2, VARIABLESOffset, 0);
    }
    static createVariablesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startVariablesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addObjectives(builder, OBJECTIVESOffset) {
        builder.addFieldOffset(3, OBJECTIVESOffset, 0);
    }
    static createObjectivesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startObjectivesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addConstraints(builder, CONSTRAINTSOffset) {
        builder.addFieldOffset(4, CONSTRAINTSOffset, 0);
    }
    static createConstraintsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startConstraintsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addDominanceRank(builder, DOMINANCE_RANK) {
        builder.addFieldInt32(5, DOMINANCE_RANK, 0);
    }
    static addCrowdingDistance(builder, CROWDING_DISTANCE) {
        builder.addFieldFloat64(6, CROWDING_DISTANCE, 0.0);
    }
    static addVerifyByPropagationResidual(builder, VERIFY_BY_PROPAGATION_RESIDUAL) {
        builder.addFieldFloat64(7, VERIFY_BY_PROPAGATION_RESIDUAL, 0.0);
    }
    static addProvenance(builder, PROVENANCEOffset) {
        builder.addFieldOffset(8, PROVENANCEOffset, 0);
    }
    static endPSSSolution(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // SOLUTION_ID
        return offset;
    }
    unpack() {
        return new PSSSolutionT(this.SOLUTION_ID(), this.STATUS(), this.bb.createObjList(this.VARIABLES.bind(this), this.variablesLength()), this.bb.createObjList(this.OBJECTIVES.bind(this), this.objectivesLength()), this.bb.createObjList(this.CONSTRAINTS.bind(this), this.constraintsLength()), this.DOMINANCE_RANK(), this.CROWDING_DISTANCE(), this.VERIFY_BY_PROPAGATION_RESIDUAL(), (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null));
    }
    unpackTo(_o) {
        _o.SOLUTION_ID = this.SOLUTION_ID();
        _o.STATUS = this.STATUS();
        _o.VARIABLES = this.bb.createObjList(this.VARIABLES.bind(this), this.variablesLength());
        _o.OBJECTIVES = this.bb.createObjList(this.OBJECTIVES.bind(this), this.objectivesLength());
        _o.CONSTRAINTS = this.bb.createObjList(this.CONSTRAINTS.bind(this), this.constraintsLength());
        _o.DOMINANCE_RANK = this.DOMINANCE_RANK();
        _o.CROWDING_DISTANCE = this.CROWDING_DISTANCE();
        _o.VERIFY_BY_PROPAGATION_RESIDUAL = this.VERIFY_BY_PROPAGATION_RESIDUAL();
        _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE().unpack() : null);
    }
}
export class PSSSolutionT {
    constructor(SOLUTION_ID = null, STATUS = pssSolutionStatus.UNSPECIFIED, VARIABLES = [], OBJECTIVES = [], CONSTRAINTS = [], DOMINANCE_RANK = 0, CROWDING_DISTANCE = 0.0, VERIFY_BY_PROPAGATION_RESIDUAL = 0.0, PROVENANCE = null) {
        this.SOLUTION_ID = SOLUTION_ID;
        this.STATUS = STATUS;
        this.VARIABLES = VARIABLES;
        this.OBJECTIVES = OBJECTIVES;
        this.CONSTRAINTS = CONSTRAINTS;
        this.DOMINANCE_RANK = DOMINANCE_RANK;
        this.CROWDING_DISTANCE = CROWDING_DISTANCE;
        this.VERIFY_BY_PROPAGATION_RESIDUAL = VERIFY_BY_PROPAGATION_RESIDUAL;
        this.PROVENANCE = PROVENANCE;
    }
    pack(builder) {
        const SOLUTION_ID = (this.SOLUTION_ID !== null ? builder.createString(this.SOLUTION_ID) : 0);
        const VARIABLES = PSSSolution.createVariablesVector(builder, builder.createObjectOffsetList(this.VARIABLES));
        const OBJECTIVES = PSSSolution.createObjectivesVector(builder, builder.createObjectOffsetList(this.OBJECTIVES));
        const CONSTRAINTS = PSSSolution.createConstraintsVector(builder, builder.createObjectOffsetList(this.CONSTRAINTS));
        const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE.pack(builder) : 0);
        PSSSolution.startPSSSolution(builder);
        PSSSolution.addSolutionId(builder, SOLUTION_ID);
        PSSSolution.addStatus(builder, this.STATUS);
        PSSSolution.addVariables(builder, VARIABLES);
        PSSSolution.addObjectives(builder, OBJECTIVES);
        PSSSolution.addConstraints(builder, CONSTRAINTS);
        PSSSolution.addDominanceRank(builder, this.DOMINANCE_RANK);
        PSSSolution.addCrowdingDistance(builder, this.CROWDING_DISTANCE);
        PSSSolution.addVerifyByPropagationResidual(builder, this.VERIFY_BY_PROPAGATION_RESIDUAL);
        PSSSolution.addProvenance(builder, PROVENANCE);
        return PSSSolution.endPSSSolution(builder);
    }
}
