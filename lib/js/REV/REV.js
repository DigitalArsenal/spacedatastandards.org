// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * Review - User review of a storefront listing
 */
export class REV {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsREV(bb, obj) {
        return (obj || new REV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsREV(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new REV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$REV');
    }
    REVIEW_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    LISTING_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    REVIEWER_PEER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Rating from 1-5 stars
     */
    RATING() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : 0;
    }
    TITLE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    CONTENT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ACL_GRANT_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Unix timestamp of the review
     */
    TIMESTAMP() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Ed25519 signature from reviewer
     */
    REVIEWER_SIGNATURE(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    reviewerSignatureLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    reviewerSignatureArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    static startREV(builder) {
        builder.startObject(9);
    }
    static addReviewId(builder, REVIEW_IDOffset) {
        builder.addFieldOffset(0, REVIEW_IDOffset, 0);
    }
    static addListingId(builder, LISTING_IDOffset) {
        builder.addFieldOffset(1, LISTING_IDOffset, 0);
    }
    static addReviewerPeerId(builder, REVIEWER_PEER_IDOffset) {
        builder.addFieldOffset(2, REVIEWER_PEER_IDOffset, 0);
    }
    static addRating(builder, RATING) {
        builder.addFieldInt8(3, RATING, 0);
    }
    static addTitle(builder, TITLEOffset) {
        builder.addFieldOffset(4, TITLEOffset, 0);
    }
    static addContent(builder, CONTENTOffset) {
        builder.addFieldOffset(5, CONTENTOffset, 0);
    }
    static addAclGrantId(builder, ACL_GRANT_IDOffset) {
        builder.addFieldOffset(6, ACL_GRANT_IDOffset, 0);
    }
    static addTimestamp(builder, TIMESTAMP) {
        builder.addFieldInt64(7, TIMESTAMP, BigInt('0'));
    }
    static addReviewerSignature(builder, REVIEWER_SIGNATUREOffset) {
        builder.addFieldOffset(8, REVIEWER_SIGNATUREOffset, 0);
    }
    static createReviewerSignatureVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startReviewerSignatureVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static endREV(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // REVIEW_ID
        builder.requiredField(offset, 6); // LISTING_ID
        builder.requiredField(offset, 8); // REVIEWER_PEER_ID
        return offset;
    }
    static finishREVBuffer(builder, offset) {
        builder.finish(offset, '$REV');
    }
    static finishSizePrefixedREVBuffer(builder, offset) {
        builder.finish(offset, '$REV', true);
    }
    static createREV(builder, REVIEW_IDOffset, LISTING_IDOffset, REVIEWER_PEER_IDOffset, RATING, TITLEOffset, CONTENTOffset, ACL_GRANT_IDOffset, TIMESTAMP, REVIEWER_SIGNATUREOffset) {
        REV.startREV(builder);
        REV.addReviewId(builder, REVIEW_IDOffset);
        REV.addListingId(builder, LISTING_IDOffset);
        REV.addReviewerPeerId(builder, REVIEWER_PEER_IDOffset);
        REV.addRating(builder, RATING);
        REV.addTitle(builder, TITLEOffset);
        REV.addContent(builder, CONTENTOffset);
        REV.addAclGrantId(builder, ACL_GRANT_IDOffset);
        REV.addTimestamp(builder, TIMESTAMP);
        REV.addReviewerSignature(builder, REVIEWER_SIGNATUREOffset);
        return REV.endREV(builder);
    }
    unpack() {
        return new REVT(this.REVIEW_ID(), this.LISTING_ID(), this.REVIEWER_PEER_ID(), this.RATING(), this.TITLE(), this.CONTENT(), this.ACL_GRANT_ID(), this.TIMESTAMP(), this.bb.createScalarList(this.REVIEWER_SIGNATURE.bind(this), this.reviewerSignatureLength()));
    }
    unpackTo(_o) {
        _o.REVIEW_ID = this.REVIEW_ID();
        _o.LISTING_ID = this.LISTING_ID();
        _o.REVIEWER_PEER_ID = this.REVIEWER_PEER_ID();
        _o.RATING = this.RATING();
        _o.TITLE = this.TITLE();
        _o.CONTENT = this.CONTENT();
        _o.ACL_GRANT_ID = this.ACL_GRANT_ID();
        _o.TIMESTAMP = this.TIMESTAMP();
        _o.REVIEWER_SIGNATURE = this.bb.createScalarList(this.REVIEWER_SIGNATURE.bind(this), this.reviewerSignatureLength());
    }
}
export class REVT {
    constructor(REVIEW_ID = null, LISTING_ID = null, REVIEWER_PEER_ID = null, RATING = 0, TITLE = null, CONTENT = null, ACL_GRANT_ID = null, TIMESTAMP = BigInt('0'), REVIEWER_SIGNATURE = []) {
        this.REVIEW_ID = REVIEW_ID;
        this.LISTING_ID = LISTING_ID;
        this.REVIEWER_PEER_ID = REVIEWER_PEER_ID;
        this.RATING = RATING;
        this.TITLE = TITLE;
        this.CONTENT = CONTENT;
        this.ACL_GRANT_ID = ACL_GRANT_ID;
        this.TIMESTAMP = TIMESTAMP;
        this.REVIEWER_SIGNATURE = REVIEWER_SIGNATURE;
    }
    pack(builder) {
        const REVIEW_ID = (this.REVIEW_ID !== null ? builder.createString(this.REVIEW_ID) : 0);
        const LISTING_ID = (this.LISTING_ID !== null ? builder.createString(this.LISTING_ID) : 0);
        const REVIEWER_PEER_ID = (this.REVIEWER_PEER_ID !== null ? builder.createString(this.REVIEWER_PEER_ID) : 0);
        const TITLE = (this.TITLE !== null ? builder.createString(this.TITLE) : 0);
        const CONTENT = (this.CONTENT !== null ? builder.createString(this.CONTENT) : 0);
        const ACL_GRANT_ID = (this.ACL_GRANT_ID !== null ? builder.createString(this.ACL_GRANT_ID) : 0);
        const REVIEWER_SIGNATURE = REV.createReviewerSignatureVector(builder, this.REVIEWER_SIGNATURE);
        return REV.createREV(builder, REVIEW_ID, LISTING_ID, REVIEWER_PEER_ID, this.RATING, TITLE, CONTENT, ACL_GRANT_ID, this.TIMESTAMP, REVIEWER_SIGNATURE);
    }
}
