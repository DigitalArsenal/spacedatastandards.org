// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { anomalyType } from './anomalyType.js';
import { polynomialBasisType } from './polynomialBasisType.js';
import { sizeShapeType } from './sizeShapeType.js';
/**
 * A single time-segment record of polynomial coefficients for classical orbital elements.
 *
 * The six classical elements are:
 *   1. SMA or R_PERIAPSIS (size/shape) — see SIZE_SHAPE_TYPE
 *   2. Eccentricity (dimensionless)
 *   3. Inclination (degrees)
 *   4. Right Ascension of Ascending Node / RAAN (degrees)
 *   5. Argument of Periapsis (degrees)
 *   6. Anomaly (degrees) — see ANOMALY_TYPE
 *
 * Evaluation follows the same normalized-time procedure as PPEPositionRecord.
 */
export class PPEOrbitalElementRecord {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPPEOrbitalElementRecord(bb, obj) {
        return (obj || new PPEOrbitalElementRecord()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPPEOrbitalElementRecord(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PPEOrbitalElementRecord()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    EPOCH_MID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Half-span of the validity window in seconds.
     */
    EPOCH_HALF_SPAN() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * Number of polynomial coefficients per element.
     */
    NUM_COEFFICIENTS() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint16(this.bb_pos + offset) : 0;
    }
    /**
     * Polynomial basis type for interpreting the coefficient arrays.
     */
    BASIS_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : polynomialBasisType.CHEBYSHEV;
    }
    /**
     * Parameterization of the first orbital element (SMA vs R_PERIAPSIS).
     */
    SIZE_SHAPE_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : sizeShapeType.SMA;
    }
    /**
     * Anomaly type for the sixth orbital element.
     */
    ANOMALY_TYPE() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : anomalyType.TRUE_ANOMALY;
    }
    /**
     * Coefficients for SMA or radius of periapsis (km).
     * Length must equal NUM_COEFFICIENTS.
     */
    COEFF_SIZE_SHAPE(index) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    coeffSizeShapeLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    coeffSizeShapeArray() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Coefficients for eccentricity (dimensionless).
     * Length must equal NUM_COEFFICIENTS.
     */
    COEFF_ECCENTRICITY(index) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    coeffEccentricityLength() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    coeffEccentricityArray() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Coefficients for inclination (degrees).
     * Length must equal NUM_COEFFICIENTS.
     */
    COEFF_INCLINATION(index) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    coeffInclinationLength() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    coeffInclinationArray() {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Coefficients for RAAN (degrees).
     * Length must equal NUM_COEFFICIENTS.
     */
    COEFF_RAAN(index) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    coeffRaanLength() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    coeffRaanArray() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Coefficients for argument of periapsis (degrees).
     * Length must equal NUM_COEFFICIENTS.
     */
    COEFF_ARG_PERIAPSIS(index) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    coeffArgPeriapsisLength() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    coeffArgPeriapsisArray() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Coefficients for anomaly (degrees). See ANOMALY_TYPE for interpretation.
     * Length must equal NUM_COEFFICIENTS.
     */
    COEFF_ANOMALY(index) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.readFloat64(this.bb.__vector(this.bb_pos + offset) + index * 8) : 0;
    }
    coeffAnomalyLength() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    coeffAnomalyArray() {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? new Float64Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Maximum element fit residual over this segment. Optional quality metric.
     * Units depend on the element (km for SMA, degrees for angles, dimensionless for ecc).
     */
    MAX_ELEMENT_RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    /**
     * RMS element fit residual over this segment. Optional quality metric.
     */
    RMS_ELEMENT_RESIDUAL() {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.readFloat64(this.bb_pos + offset) : 0.0;
    }
    static startPPEOrbitalElementRecord(builder) {
        builder.startObject(14);
    }
    static addEpochMid(builder, EPOCH_MIDOffset) {
        builder.addFieldOffset(0, EPOCH_MIDOffset, 0);
    }
    static addEpochHalfSpan(builder, EPOCH_HALF_SPAN) {
        builder.addFieldFloat64(1, EPOCH_HALF_SPAN, 0.0);
    }
    static addNumCoefficients(builder, NUM_COEFFICIENTS) {
        builder.addFieldInt16(2, NUM_COEFFICIENTS, 0);
    }
    static addBasisType(builder, BASIS_TYPE) {
        builder.addFieldInt8(3, BASIS_TYPE, polynomialBasisType.CHEBYSHEV);
    }
    static addSizeShapeType(builder, SIZE_SHAPE_TYPE) {
        builder.addFieldInt8(4, SIZE_SHAPE_TYPE, sizeShapeType.SMA);
    }
    static addAnomalyType(builder, ANOMALY_TYPE) {
        builder.addFieldInt8(5, ANOMALY_TYPE, anomalyType.TRUE_ANOMALY);
    }
    static addCoeffSizeShape(builder, COEFF_SIZE_SHAPEOffset) {
        builder.addFieldOffset(6, COEFF_SIZE_SHAPEOffset, 0);
    }
    static createCoeffSizeShapeVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCoeffSizeShapeVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addCoeffEccentricity(builder, COEFF_ECCENTRICITYOffset) {
        builder.addFieldOffset(7, COEFF_ECCENTRICITYOffset, 0);
    }
    static createCoeffEccentricityVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCoeffEccentricityVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addCoeffInclination(builder, COEFF_INCLINATIONOffset) {
        builder.addFieldOffset(8, COEFF_INCLINATIONOffset, 0);
    }
    static createCoeffInclinationVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCoeffInclinationVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addCoeffRaan(builder, COEFF_RAANOffset) {
        builder.addFieldOffset(9, COEFF_RAANOffset, 0);
    }
    static createCoeffRaanVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCoeffRaanVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addCoeffArgPeriapsis(builder, COEFF_ARG_PERIAPSISOffset) {
        builder.addFieldOffset(10, COEFF_ARG_PERIAPSISOffset, 0);
    }
    static createCoeffArgPeriapsisVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCoeffArgPeriapsisVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addCoeffAnomaly(builder, COEFF_ANOMALYOffset) {
        builder.addFieldOffset(11, COEFF_ANOMALYOffset, 0);
    }
    static createCoeffAnomalyVector(builder, data) {
        builder.startVector(8, data.length, 8);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addFloat64(data[i]);
        }
        return builder.endVector();
    }
    static startCoeffAnomalyVector(builder, numElems) {
        builder.startVector(8, numElems, 8);
    }
    static addMaxElementResidual(builder, MAX_ELEMENT_RESIDUAL) {
        builder.addFieldFloat64(12, MAX_ELEMENT_RESIDUAL, 0.0);
    }
    static addRmsElementResidual(builder, RMS_ELEMENT_RESIDUAL) {
        builder.addFieldFloat64(13, RMS_ELEMENT_RESIDUAL, 0.0);
    }
    static endPPEOrbitalElementRecord(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // EPOCH_MID
        builder.requiredField(offset, 16); // COEFF_SIZE_SHAPE
        builder.requiredField(offset, 18); // COEFF_ECCENTRICITY
        builder.requiredField(offset, 20); // COEFF_INCLINATION
        builder.requiredField(offset, 22); // COEFF_RAAN
        builder.requiredField(offset, 24); // COEFF_ARG_PERIAPSIS
        builder.requiredField(offset, 26); // COEFF_ANOMALY
        return offset;
    }
    static createPPEOrbitalElementRecord(builder, EPOCH_MIDOffset, EPOCH_HALF_SPAN, NUM_COEFFICIENTS, BASIS_TYPE, SIZE_SHAPE_TYPE, ANOMALY_TYPE, COEFF_SIZE_SHAPEOffset, COEFF_ECCENTRICITYOffset, COEFF_INCLINATIONOffset, COEFF_RAANOffset, COEFF_ARG_PERIAPSISOffset, COEFF_ANOMALYOffset, MAX_ELEMENT_RESIDUAL, RMS_ELEMENT_RESIDUAL) {
        PPEOrbitalElementRecord.startPPEOrbitalElementRecord(builder);
        PPEOrbitalElementRecord.addEpochMid(builder, EPOCH_MIDOffset);
        PPEOrbitalElementRecord.addEpochHalfSpan(builder, EPOCH_HALF_SPAN);
        PPEOrbitalElementRecord.addNumCoefficients(builder, NUM_COEFFICIENTS);
        PPEOrbitalElementRecord.addBasisType(builder, BASIS_TYPE);
        PPEOrbitalElementRecord.addSizeShapeType(builder, SIZE_SHAPE_TYPE);
        PPEOrbitalElementRecord.addAnomalyType(builder, ANOMALY_TYPE);
        PPEOrbitalElementRecord.addCoeffSizeShape(builder, COEFF_SIZE_SHAPEOffset);
        PPEOrbitalElementRecord.addCoeffEccentricity(builder, COEFF_ECCENTRICITYOffset);
        PPEOrbitalElementRecord.addCoeffInclination(builder, COEFF_INCLINATIONOffset);
        PPEOrbitalElementRecord.addCoeffRaan(builder, COEFF_RAANOffset);
        PPEOrbitalElementRecord.addCoeffArgPeriapsis(builder, COEFF_ARG_PERIAPSISOffset);
        PPEOrbitalElementRecord.addCoeffAnomaly(builder, COEFF_ANOMALYOffset);
        PPEOrbitalElementRecord.addMaxElementResidual(builder, MAX_ELEMENT_RESIDUAL);
        PPEOrbitalElementRecord.addRmsElementResidual(builder, RMS_ELEMENT_RESIDUAL);
        return PPEOrbitalElementRecord.endPPEOrbitalElementRecord(builder);
    }
    unpack() {
        return new PPEOrbitalElementRecordT(this.EPOCH_MID(), this.EPOCH_HALF_SPAN(), this.NUM_COEFFICIENTS(), this.BASIS_TYPE(), this.SIZE_SHAPE_TYPE(), this.ANOMALY_TYPE(), this.bb.createScalarList(this.COEFF_SIZE_SHAPE.bind(this), this.coeffSizeShapeLength()), this.bb.createScalarList(this.COEFF_ECCENTRICITY.bind(this), this.coeffEccentricityLength()), this.bb.createScalarList(this.COEFF_INCLINATION.bind(this), this.coeffInclinationLength()), this.bb.createScalarList(this.COEFF_RAAN.bind(this), this.coeffRaanLength()), this.bb.createScalarList(this.COEFF_ARG_PERIAPSIS.bind(this), this.coeffArgPeriapsisLength()), this.bb.createScalarList(this.COEFF_ANOMALY.bind(this), this.coeffAnomalyLength()), this.MAX_ELEMENT_RESIDUAL(), this.RMS_ELEMENT_RESIDUAL());
    }
    unpackTo(_o) {
        _o.EPOCH_MID = this.EPOCH_MID();
        _o.EPOCH_HALF_SPAN = this.EPOCH_HALF_SPAN();
        _o.NUM_COEFFICIENTS = this.NUM_COEFFICIENTS();
        _o.BASIS_TYPE = this.BASIS_TYPE();
        _o.SIZE_SHAPE_TYPE = this.SIZE_SHAPE_TYPE();
        _o.ANOMALY_TYPE = this.ANOMALY_TYPE();
        _o.COEFF_SIZE_SHAPE = this.bb.createScalarList(this.COEFF_SIZE_SHAPE.bind(this), this.coeffSizeShapeLength());
        _o.COEFF_ECCENTRICITY = this.bb.createScalarList(this.COEFF_ECCENTRICITY.bind(this), this.coeffEccentricityLength());
        _o.COEFF_INCLINATION = this.bb.createScalarList(this.COEFF_INCLINATION.bind(this), this.coeffInclinationLength());
        _o.COEFF_RAAN = this.bb.createScalarList(this.COEFF_RAAN.bind(this), this.coeffRaanLength());
        _o.COEFF_ARG_PERIAPSIS = this.bb.createScalarList(this.COEFF_ARG_PERIAPSIS.bind(this), this.coeffArgPeriapsisLength());
        _o.COEFF_ANOMALY = this.bb.createScalarList(this.COEFF_ANOMALY.bind(this), this.coeffAnomalyLength());
        _o.MAX_ELEMENT_RESIDUAL = this.MAX_ELEMENT_RESIDUAL();
        _o.RMS_ELEMENT_RESIDUAL = this.RMS_ELEMENT_RESIDUAL();
    }
}
export class PPEOrbitalElementRecordT {
    constructor(EPOCH_MID = null, EPOCH_HALF_SPAN = 0.0, NUM_COEFFICIENTS = 0, BASIS_TYPE = polynomialBasisType.CHEBYSHEV, SIZE_SHAPE_TYPE = sizeShapeType.SMA, ANOMALY_TYPE = anomalyType.TRUE_ANOMALY, COEFF_SIZE_SHAPE = [], COEFF_ECCENTRICITY = [], COEFF_INCLINATION = [], COEFF_RAAN = [], COEFF_ARG_PERIAPSIS = [], COEFF_ANOMALY = [], MAX_ELEMENT_RESIDUAL = 0.0, RMS_ELEMENT_RESIDUAL = 0.0) {
        this.EPOCH_MID = EPOCH_MID;
        this.EPOCH_HALF_SPAN = EPOCH_HALF_SPAN;
        this.NUM_COEFFICIENTS = NUM_COEFFICIENTS;
        this.BASIS_TYPE = BASIS_TYPE;
        this.SIZE_SHAPE_TYPE = SIZE_SHAPE_TYPE;
        this.ANOMALY_TYPE = ANOMALY_TYPE;
        this.COEFF_SIZE_SHAPE = COEFF_SIZE_SHAPE;
        this.COEFF_ECCENTRICITY = COEFF_ECCENTRICITY;
        this.COEFF_INCLINATION = COEFF_INCLINATION;
        this.COEFF_RAAN = COEFF_RAAN;
        this.COEFF_ARG_PERIAPSIS = COEFF_ARG_PERIAPSIS;
        this.COEFF_ANOMALY = COEFF_ANOMALY;
        this.MAX_ELEMENT_RESIDUAL = MAX_ELEMENT_RESIDUAL;
        this.RMS_ELEMENT_RESIDUAL = RMS_ELEMENT_RESIDUAL;
    }
    pack(builder) {
        const EPOCH_MID = (this.EPOCH_MID !== null ? builder.createString(this.EPOCH_MID) : 0);
        const COEFF_SIZE_SHAPE = PPEOrbitalElementRecord.createCoeffSizeShapeVector(builder, this.COEFF_SIZE_SHAPE);
        const COEFF_ECCENTRICITY = PPEOrbitalElementRecord.createCoeffEccentricityVector(builder, this.COEFF_ECCENTRICITY);
        const COEFF_INCLINATION = PPEOrbitalElementRecord.createCoeffInclinationVector(builder, this.COEFF_INCLINATION);
        const COEFF_RAAN = PPEOrbitalElementRecord.createCoeffRaanVector(builder, this.COEFF_RAAN);
        const COEFF_ARG_PERIAPSIS = PPEOrbitalElementRecord.createCoeffArgPeriapsisVector(builder, this.COEFF_ARG_PERIAPSIS);
        const COEFF_ANOMALY = PPEOrbitalElementRecord.createCoeffAnomalyVector(builder, this.COEFF_ANOMALY);
        return PPEOrbitalElementRecord.createPPEOrbitalElementRecord(builder, EPOCH_MID, this.EPOCH_HALF_SPAN, this.NUM_COEFFICIENTS, this.BASIS_TYPE, this.SIZE_SHAPE_TYPE, this.ANOMALY_TYPE, COEFF_SIZE_SHAPE, COEFF_ECCENTRICITY, COEFF_INCLINATION, COEFF_RAAN, COEFF_ARG_PERIAPSIS, COEFF_ANOMALY, this.MAX_ELEMENT_RESIDUAL, this.RMS_ELEMENT_RESIDUAL);
    }
}
