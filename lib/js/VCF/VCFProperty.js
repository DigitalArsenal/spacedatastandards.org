// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
/**
 * One emitted card property, as the publisher parsed its own output.
 */
export class VCFProperty {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsVCFProperty(bb, obj) {
        return (obj || new VCFProperty()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsVCFProperty(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new VCFProperty()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    PARAMETERS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    parametersLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    VALUE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    GROUP(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Zero-based position of this property in the canonical emission order for
     * this FORM. Two conforming emitters agree on this number.
     */
    ORDER() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Octet length of the property's complete UNFOLDED content line, excluding
     * the line terminator. This is the number folding is applied against.
     */
    OCTET_LENGTH() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    static startVCFProperty(builder) {
        builder.startObject(6);
    }
    static addName(builder, NAMEOffset) {
        builder.addFieldOffset(0, NAMEOffset, 0);
    }
    static addParameters(builder, PARAMETERSOffset) {
        builder.addFieldOffset(1, PARAMETERSOffset, 0);
    }
    static createParametersVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startParametersVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addValue(builder, VALUEOffset) {
        builder.addFieldOffset(2, VALUEOffset, 0);
    }
    static addGroup(builder, GROUPOffset) {
        builder.addFieldOffset(3, GROUPOffset, 0);
    }
    static addOrder(builder, ORDER) {
        builder.addFieldInt32(4, ORDER, 0);
    }
    static addOctetLength(builder, OCTET_LENGTH) {
        builder.addFieldInt32(5, OCTET_LENGTH, 0);
    }
    static endVCFProperty(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // NAME
        return offset;
    }
    static createVCFProperty(builder, NAMEOffset, PARAMETERSOffset, VALUEOffset, GROUPOffset, ORDER, OCTET_LENGTH) {
        VCFProperty.startVCFProperty(builder);
        VCFProperty.addName(builder, NAMEOffset);
        VCFProperty.addParameters(builder, PARAMETERSOffset);
        VCFProperty.addValue(builder, VALUEOffset);
        VCFProperty.addGroup(builder, GROUPOffset);
        VCFProperty.addOrder(builder, ORDER);
        VCFProperty.addOctetLength(builder, OCTET_LENGTH);
        return VCFProperty.endVCFProperty(builder);
    }
    unpack() {
        return new VCFPropertyT(this.NAME(), this.bb.createScalarList(this.PARAMETERS.bind(this), this.parametersLength()), this.VALUE(), this.GROUP(), this.ORDER(), this.OCTET_LENGTH());
    }
    unpackTo(_o) {
        _o.NAME = this.NAME();
        _o.PARAMETERS = this.bb.createScalarList(this.PARAMETERS.bind(this), this.parametersLength());
        _o.VALUE = this.VALUE();
        _o.GROUP = this.GROUP();
        _o.ORDER = this.ORDER();
        _o.OCTET_LENGTH = this.OCTET_LENGTH();
    }
}
export class VCFPropertyT {
    constructor(NAME = null, PARAMETERS = [], VALUE = null, GROUP = null, ORDER = 0, OCTET_LENGTH = 0) {
        this.NAME = NAME;
        this.PARAMETERS = PARAMETERS;
        this.VALUE = VALUE;
        this.GROUP = GROUP;
        this.ORDER = ORDER;
        this.OCTET_LENGTH = OCTET_LENGTH;
    }
    pack(builder) {
        const NAME = (this.NAME !== null ? builder.createString(this.NAME) : 0);
        const PARAMETERS = VCFProperty.createParametersVector(builder, builder.createObjectOffsetList(this.PARAMETERS));
        const VALUE = (this.VALUE !== null ? builder.createString(this.VALUE) : 0);
        const GROUP = (this.GROUP !== null ? builder.createString(this.GROUP) : 0);
        return VCFProperty.createVCFProperty(builder, NAME, PARAMETERS, VALUE, GROUP, this.ORDER, this.OCTET_LENGTH);
    }
}
