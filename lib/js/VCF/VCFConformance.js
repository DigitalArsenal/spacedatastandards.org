// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { vcfLineEnding } from './vcfLineEnding.js';
/**
 * Everything needed to decide whether this card is servable, and whether two
 * implementations produced the same bytes.
 */
export class VCFConformance {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsVCFConformance(bb, obj) {
        return (obj || new VCFConformance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsVCFConformance(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new VCFConformance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    PROFILE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Line terminator the card text uses.
     */
    LINE_ENDING() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : vcfLineEnding.UNSPECIFIED;
    }
    /**
     * Octet width beyond which a content line is folded onto continuation lines.
     * 0 means the publisher folded nothing, which is non-canonical.
     */
    FOLD_OCTET_WIDTH() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Octet length of `CARD` exactly as emitted, folding and terminators
     * included. This is the number a serving surface budgets against.
     */
    BYTE_LENGTH() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Octet length the same card would have with folding removed. Carried so a
     * folding disagreement is detectable without unfolding the text.
     */
    UNFOLDED_BYTE_LENGTH() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Octet ceiling of the surface this card was produced for. 0 means the
     * publisher states no ceiling; it never means a ceiling of zero.
     */
    BYTE_BUDGET() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Whether BYTE_LENGTH is within BYTE_BUDGET. False with a non-zero budget
     * means the card is a valid record that is NOT servable on that surface.
     */
    WITHIN_BYTE_BUDGET() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    CARD_SHA256(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Number of property rows emitted, unfolded. Carried so a truncated card is
     * detectable before parsing.
     */
    PROPERTY_COUNT() {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    /**
     * Whether the card carries a complete verification chain: a signing-key row,
     * an encryption-key row, and a profile-signature row, all present in the
     * UNFOLDED text. A card without all three cannot verify its own subject and
     * is not servable as an identity card.
     */
    CARRIES_VERIFICATION_CHAIN() {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? !!this.bb.readInt8(this.bb_pos + offset) : false;
    }
    static startVCFConformance(builder) {
        builder.startObject(11);
    }
    static addVersion(builder, VERSIONOffset) {
        builder.addFieldOffset(0, VERSIONOffset, 0);
    }
    static addProfile(builder, PROFILEOffset) {
        builder.addFieldOffset(1, PROFILEOffset, 0);
    }
    static addLineEnding(builder, LINE_ENDING) {
        builder.addFieldInt8(2, LINE_ENDING, vcfLineEnding.UNSPECIFIED);
    }
    static addFoldOctetWidth(builder, FOLD_OCTET_WIDTH) {
        builder.addFieldInt32(3, FOLD_OCTET_WIDTH, 0);
    }
    static addByteLength(builder, BYTE_LENGTH) {
        builder.addFieldInt32(4, BYTE_LENGTH, 0);
    }
    static addUnfoldedByteLength(builder, UNFOLDED_BYTE_LENGTH) {
        builder.addFieldInt32(5, UNFOLDED_BYTE_LENGTH, 0);
    }
    static addByteBudget(builder, BYTE_BUDGET) {
        builder.addFieldInt32(6, BYTE_BUDGET, 0);
    }
    static addWithinByteBudget(builder, WITHIN_BYTE_BUDGET) {
        builder.addFieldInt8(7, +WITHIN_BYTE_BUDGET, +false);
    }
    static addCardSha256(builder, CARD_SHA256Offset) {
        builder.addFieldOffset(8, CARD_SHA256Offset, 0);
    }
    static addPropertyCount(builder, PROPERTY_COUNT) {
        builder.addFieldInt32(9, PROPERTY_COUNT, 0);
    }
    static addCarriesVerificationChain(builder, CARRIES_VERIFICATION_CHAIN) {
        builder.addFieldInt8(10, +CARRIES_VERIFICATION_CHAIN, +false);
    }
    static endVCFConformance(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // VERSION
        return offset;
    }
    static createVCFConformance(builder, VERSIONOffset, PROFILEOffset, LINE_ENDING, FOLD_OCTET_WIDTH, BYTE_LENGTH, UNFOLDED_BYTE_LENGTH, BYTE_BUDGET, WITHIN_BYTE_BUDGET, CARD_SHA256Offset, PROPERTY_COUNT, CARRIES_VERIFICATION_CHAIN) {
        VCFConformance.startVCFConformance(builder);
        VCFConformance.addVersion(builder, VERSIONOffset);
        VCFConformance.addProfile(builder, PROFILEOffset);
        VCFConformance.addLineEnding(builder, LINE_ENDING);
        VCFConformance.addFoldOctetWidth(builder, FOLD_OCTET_WIDTH);
        VCFConformance.addByteLength(builder, BYTE_LENGTH);
        VCFConformance.addUnfoldedByteLength(builder, UNFOLDED_BYTE_LENGTH);
        VCFConformance.addByteBudget(builder, BYTE_BUDGET);
        VCFConformance.addWithinByteBudget(builder, WITHIN_BYTE_BUDGET);
        VCFConformance.addCardSha256(builder, CARD_SHA256Offset);
        VCFConformance.addPropertyCount(builder, PROPERTY_COUNT);
        VCFConformance.addCarriesVerificationChain(builder, CARRIES_VERIFICATION_CHAIN);
        return VCFConformance.endVCFConformance(builder);
    }
    unpack() {
        return new VCFConformanceT(this.VERSION(), this.PROFILE(), this.LINE_ENDING(), this.FOLD_OCTET_WIDTH(), this.BYTE_LENGTH(), this.UNFOLDED_BYTE_LENGTH(), this.BYTE_BUDGET(), this.WITHIN_BYTE_BUDGET(), this.CARD_SHA256(), this.PROPERTY_COUNT(), this.CARRIES_VERIFICATION_CHAIN());
    }
    unpackTo(_o) {
        _o.VERSION = this.VERSION();
        _o.PROFILE = this.PROFILE();
        _o.LINE_ENDING = this.LINE_ENDING();
        _o.FOLD_OCTET_WIDTH = this.FOLD_OCTET_WIDTH();
        _o.BYTE_LENGTH = this.BYTE_LENGTH();
        _o.UNFOLDED_BYTE_LENGTH = this.UNFOLDED_BYTE_LENGTH();
        _o.BYTE_BUDGET = this.BYTE_BUDGET();
        _o.WITHIN_BYTE_BUDGET = this.WITHIN_BYTE_BUDGET();
        _o.CARD_SHA256 = this.CARD_SHA256();
        _o.PROPERTY_COUNT = this.PROPERTY_COUNT();
        _o.CARRIES_VERIFICATION_CHAIN = this.CARRIES_VERIFICATION_CHAIN();
    }
}
export class VCFConformanceT {
    constructor(VERSION = null, PROFILE = null, LINE_ENDING = vcfLineEnding.UNSPECIFIED, FOLD_OCTET_WIDTH = 0, BYTE_LENGTH = 0, UNFOLDED_BYTE_LENGTH = 0, BYTE_BUDGET = 0, WITHIN_BYTE_BUDGET = false, CARD_SHA256 = null, PROPERTY_COUNT = 0, CARRIES_VERIFICATION_CHAIN = false) {
        this.VERSION = VERSION;
        this.PROFILE = PROFILE;
        this.LINE_ENDING = LINE_ENDING;
        this.FOLD_OCTET_WIDTH = FOLD_OCTET_WIDTH;
        this.BYTE_LENGTH = BYTE_LENGTH;
        this.UNFOLDED_BYTE_LENGTH = UNFOLDED_BYTE_LENGTH;
        this.BYTE_BUDGET = BYTE_BUDGET;
        this.WITHIN_BYTE_BUDGET = WITHIN_BYTE_BUDGET;
        this.CARD_SHA256 = CARD_SHA256;
        this.PROPERTY_COUNT = PROPERTY_COUNT;
        this.CARRIES_VERIFICATION_CHAIN = CARRIES_VERIFICATION_CHAIN;
    }
    pack(builder) {
        const VERSION = (this.VERSION !== null ? builder.createString(this.VERSION) : 0);
        const PROFILE = (this.PROFILE !== null ? builder.createString(this.PROFILE) : 0);
        const CARD_SHA256 = (this.CARD_SHA256 !== null ? builder.createString(this.CARD_SHA256) : 0);
        return VCFConformance.createVCFConformance(builder, VERSION, PROFILE, this.LINE_ENDING, this.FOLD_OCTET_WIDTH, this.BYTE_LENGTH, this.UNFOLDED_BYTE_LENGTH, this.BYTE_BUDGET, this.WITHIN_BYTE_BUDGET, CARD_SHA256, this.PROPERTY_COUNT, this.CARRIES_VERIFICATION_CHAIN);
    }
}
