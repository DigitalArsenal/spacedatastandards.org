// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { VCFAlias } from './VCFAlias.js';
import { VCFConformance } from './VCFConformance.js';
import { VCFProperty } from './VCFProperty.js';
import { VCFSubject } from './VCFSubject.js';
import { vcfCardForm } from './vcfCardForm.js';
import { vcfPhotoPolicy } from './vcfPhotoPolicy.js';
/**
 * vCard Projection Card
 */
export class VCF {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsVCF(bb, obj) {
        return (obj || new VCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsVCF(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new VCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$VCF');
    }
    CARD_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Which canonical emission form this record carries.
     */
    FORM() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : vcfCardForm.UNSPECIFIED;
    }
    CARD(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    MEDIA_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * The profile this card projects and the provenance binding them.
     */
    SUBJECT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new VCFSubject()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * The publisher's parse of `CARD`, in canonical emission order.
     */
    PROPERTIES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new VCFProperty()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    propertiesLength() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * The identity address rows carried by `CARD`, in canonical emission order.
     */
    ALIASES(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new VCFAlias()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    aliasesLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * How the card carries a portrait, if at all.
     */
    PHOTO_POLICY() {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : vcfPhotoPolicy.UNSPECIFIED;
    }
    PHOTO_URI(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PHOTO_MEDIA_TYPE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Emission facts and the digest two implementations compare.
     */
    CONFORMANCE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? (obj || new VCFConformance()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    PRODUCER_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 26);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    PRODUCER_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 28);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    GENERATED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 30);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EXPIRES_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 32);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    TAGS(index, optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__string(this.bb.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
    }
    tagsLength() {
        const offset = this.bb.__offset(this.bb_pos, 34);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    NOTES(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 36);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startVCF(builder) {
        builder.startObject(17);
    }
    static addCardId(builder, CARD_IDOffset) {
        builder.addFieldOffset(0, CARD_IDOffset, 0);
    }
    static addForm(builder, FORM) {
        builder.addFieldInt8(1, FORM, vcfCardForm.UNSPECIFIED);
    }
    static addCard(builder, CARDOffset) {
        builder.addFieldOffset(2, CARDOffset, 0);
    }
    static addMediaType(builder, MEDIA_TYPEOffset) {
        builder.addFieldOffset(3, MEDIA_TYPEOffset, 0);
    }
    static addSubject(builder, SUBJECTOffset) {
        builder.addFieldOffset(4, SUBJECTOffset, 0);
    }
    static addProperties(builder, PROPERTIESOffset) {
        builder.addFieldOffset(5, PROPERTIESOffset, 0);
    }
    static createPropertiesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startPropertiesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAliases(builder, ALIASESOffset) {
        builder.addFieldOffset(6, ALIASESOffset, 0);
    }
    static createAliasesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAliasesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addPhotoPolicy(builder, PHOTO_POLICY) {
        builder.addFieldInt8(7, PHOTO_POLICY, vcfPhotoPolicy.UNSPECIFIED);
    }
    static addPhotoUri(builder, PHOTO_URIOffset) {
        builder.addFieldOffset(8, PHOTO_URIOffset, 0);
    }
    static addPhotoMediaType(builder, PHOTO_MEDIA_TYPEOffset) {
        builder.addFieldOffset(9, PHOTO_MEDIA_TYPEOffset, 0);
    }
    static addConformance(builder, CONFORMANCEOffset) {
        builder.addFieldOffset(10, CONFORMANCEOffset, 0);
    }
    static addProducerId(builder, PRODUCER_IDOffset) {
        builder.addFieldOffset(11, PRODUCER_IDOffset, 0);
    }
    static addProducerVersion(builder, PRODUCER_VERSIONOffset) {
        builder.addFieldOffset(12, PRODUCER_VERSIONOffset, 0);
    }
    static addGeneratedAt(builder, GENERATED_ATOffset) {
        builder.addFieldOffset(13, GENERATED_ATOffset, 0);
    }
    static addExpiresAt(builder, EXPIRES_ATOffset) {
        builder.addFieldOffset(14, EXPIRES_ATOffset, 0);
    }
    static addTags(builder, TAGSOffset) {
        builder.addFieldOffset(15, TAGSOffset, 0);
    }
    static createTagsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startTagsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addNotes(builder, NOTESOffset) {
        builder.addFieldOffset(16, NOTESOffset, 0);
    }
    static endVCF(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 8); // CARD
        return offset;
    }
    static finishVCFBuffer(builder, offset) {
        builder.finish(offset, '$VCF');
    }
    static finishSizePrefixedVCFBuffer(builder, offset) {
        builder.finish(offset, '$VCF', true);
    }
    unpack() {
        return new VCFT(this.CARD_ID(), this.FORM(), this.CARD(), this.MEDIA_TYPE(), (this.SUBJECT() !== null ? this.SUBJECT().unpack() : null), this.bb.createObjList(this.PROPERTIES.bind(this), this.propertiesLength()), this.bb.createObjList(this.ALIASES.bind(this), this.aliasesLength()), this.PHOTO_POLICY(), this.PHOTO_URI(), this.PHOTO_MEDIA_TYPE(), (this.CONFORMANCE() !== null ? this.CONFORMANCE().unpack() : null), this.PRODUCER_ID(), this.PRODUCER_VERSION(), this.GENERATED_AT(), this.EXPIRES_AT(), this.bb.createScalarList(this.TAGS.bind(this), this.tagsLength()), this.NOTES());
    }
    unpackTo(_o) {
        _o.CARD_ID = this.CARD_ID();
        _o.FORM = this.FORM();
        _o.CARD = this.CARD();
        _o.MEDIA_TYPE = this.MEDIA_TYPE();
        _o.SUBJECT = (this.SUBJECT() !== null ? this.SUBJECT().unpack() : null);
        _o.PROPERTIES = this.bb.createObjList(this.PROPERTIES.bind(this), this.propertiesLength());
        _o.ALIASES = this.bb.createObjList(this.ALIASES.bind(this), this.aliasesLength());
        _o.PHOTO_POLICY = this.PHOTO_POLICY();
        _o.PHOTO_URI = this.PHOTO_URI();
        _o.PHOTO_MEDIA_TYPE = this.PHOTO_MEDIA_TYPE();
        _o.CONFORMANCE = (this.CONFORMANCE() !== null ? this.CONFORMANCE().unpack() : null);
        _o.PRODUCER_ID = this.PRODUCER_ID();
        _o.PRODUCER_VERSION = this.PRODUCER_VERSION();
        _o.GENERATED_AT = this.GENERATED_AT();
        _o.EXPIRES_AT = this.EXPIRES_AT();
        _o.TAGS = this.bb.createScalarList(this.TAGS.bind(this), this.tagsLength());
        _o.NOTES = this.NOTES();
    }
}
export class VCFT {
    constructor(CARD_ID = null, FORM = vcfCardForm.UNSPECIFIED, CARD = null, MEDIA_TYPE = null, SUBJECT = null, PROPERTIES = [], ALIASES = [], PHOTO_POLICY = vcfPhotoPolicy.UNSPECIFIED, PHOTO_URI = null, PHOTO_MEDIA_TYPE = null, CONFORMANCE = null, PRODUCER_ID = null, PRODUCER_VERSION = null, GENERATED_AT = null, EXPIRES_AT = null, TAGS = [], NOTES = null) {
        this.CARD_ID = CARD_ID;
        this.FORM = FORM;
        this.CARD = CARD;
        this.MEDIA_TYPE = MEDIA_TYPE;
        this.SUBJECT = SUBJECT;
        this.PROPERTIES = PROPERTIES;
        this.ALIASES = ALIASES;
        this.PHOTO_POLICY = PHOTO_POLICY;
        this.PHOTO_URI = PHOTO_URI;
        this.PHOTO_MEDIA_TYPE = PHOTO_MEDIA_TYPE;
        this.CONFORMANCE = CONFORMANCE;
        this.PRODUCER_ID = PRODUCER_ID;
        this.PRODUCER_VERSION = PRODUCER_VERSION;
        this.GENERATED_AT = GENERATED_AT;
        this.EXPIRES_AT = EXPIRES_AT;
        this.TAGS = TAGS;
        this.NOTES = NOTES;
    }
    pack(builder) {
        const CARD_ID = (this.CARD_ID !== null ? builder.createString(this.CARD_ID) : 0);
        const CARD = (this.CARD !== null ? builder.createString(this.CARD) : 0);
        const MEDIA_TYPE = (this.MEDIA_TYPE !== null ? builder.createString(this.MEDIA_TYPE) : 0);
        const SUBJECT = (this.SUBJECT !== null ? this.SUBJECT.pack(builder) : 0);
        const PROPERTIES = VCF.createPropertiesVector(builder, builder.createObjectOffsetList(this.PROPERTIES));
        const ALIASES = VCF.createAliasesVector(builder, builder.createObjectOffsetList(this.ALIASES));
        const PHOTO_URI = (this.PHOTO_URI !== null ? builder.createString(this.PHOTO_URI) : 0);
        const PHOTO_MEDIA_TYPE = (this.PHOTO_MEDIA_TYPE !== null ? builder.createString(this.PHOTO_MEDIA_TYPE) : 0);
        const CONFORMANCE = (this.CONFORMANCE !== null ? this.CONFORMANCE.pack(builder) : 0);
        const PRODUCER_ID = (this.PRODUCER_ID !== null ? builder.createString(this.PRODUCER_ID) : 0);
        const PRODUCER_VERSION = (this.PRODUCER_VERSION !== null ? builder.createString(this.PRODUCER_VERSION) : 0);
        const GENERATED_AT = (this.GENERATED_AT !== null ? builder.createString(this.GENERATED_AT) : 0);
        const EXPIRES_AT = (this.EXPIRES_AT !== null ? builder.createString(this.EXPIRES_AT) : 0);
        const TAGS = VCF.createTagsVector(builder, builder.createObjectOffsetList(this.TAGS));
        const NOTES = (this.NOTES !== null ? builder.createString(this.NOTES) : 0);
        VCF.startVCF(builder);
        VCF.addCardId(builder, CARD_ID);
        VCF.addForm(builder, this.FORM);
        VCF.addCard(builder, CARD);
        VCF.addMediaType(builder, MEDIA_TYPE);
        VCF.addSubject(builder, SUBJECT);
        VCF.addProperties(builder, PROPERTIES);
        VCF.addAliases(builder, ALIASES);
        VCF.addPhotoPolicy(builder, this.PHOTO_POLICY);
        VCF.addPhotoUri(builder, PHOTO_URI);
        VCF.addPhotoMediaType(builder, PHOTO_MEDIA_TYPE);
        VCF.addConformance(builder, CONFORMANCE);
        VCF.addProducerId(builder, PRODUCER_ID);
        VCF.addProducerVersion(builder, PRODUCER_VERSION);
        VCF.addGeneratedAt(builder, GENERATED_AT);
        VCF.addExpiresAt(builder, EXPIRES_AT);
        VCF.addTags(builder, TAGS);
        VCF.addNotes(builder, NOTES);
        return VCF.endVCF(builder);
    }
}
