// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { PLGPortManifest } from './PLGPortManifest.js';
import { drainBehavior } from './drainBehavior.js';
/**
 * Canonical method declaration.
 */
export class PLGMethodManifest {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsPLGMethodManifest(bb, obj) {
        return (obj || new PLGMethodManifest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsPLGMethodManifest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new PLGMethodManifest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    METHOD_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    DISPLAY_NAME(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    INPUT_PORTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new PLGPortManifest()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    inputPortsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    OUTPUT_PORTS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new PLGPortManifest()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    outputPortsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    MAX_BATCH() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 1;
    }
    DRAIN_POLICY() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : drainBehavior.DRAIN_UNTIL_YIELD;
    }
    DESCRIPTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startPLGMethodManifest(builder) {
        builder.startObject(7);
    }
    static addMethodId(builder, METHOD_IDOffset) {
        builder.addFieldOffset(0, METHOD_IDOffset, 0);
    }
    static addDisplayName(builder, DISPLAY_NAMEOffset) {
        builder.addFieldOffset(1, DISPLAY_NAMEOffset, 0);
    }
    static addInputPorts(builder, INPUT_PORTSOffset) {
        builder.addFieldOffset(2, INPUT_PORTSOffset, 0);
    }
    static createInputPortsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startInputPortsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addOutputPorts(builder, OUTPUT_PORTSOffset) {
        builder.addFieldOffset(3, OUTPUT_PORTSOffset, 0);
    }
    static createOutputPortsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startOutputPortsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addMaxBatch(builder, MAX_BATCH) {
        builder.addFieldInt32(4, MAX_BATCH, 1);
    }
    static addDrainPolicy(builder, DRAIN_POLICY) {
        builder.addFieldInt8(5, DRAIN_POLICY, drainBehavior.DRAIN_UNTIL_YIELD);
    }
    static addDescription(builder, DESCRIPTIONOffset) {
        builder.addFieldOffset(6, DESCRIPTIONOffset, 0);
    }
    static endPLGMethodManifest(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // METHOD_ID
        return offset;
    }
    static createPLGMethodManifest(builder, METHOD_IDOffset, DISPLAY_NAMEOffset, INPUT_PORTSOffset, OUTPUT_PORTSOffset, MAX_BATCH, DRAIN_POLICY, DESCRIPTIONOffset) {
        PLGMethodManifest.startPLGMethodManifest(builder);
        PLGMethodManifest.addMethodId(builder, METHOD_IDOffset);
        PLGMethodManifest.addDisplayName(builder, DISPLAY_NAMEOffset);
        PLGMethodManifest.addInputPorts(builder, INPUT_PORTSOffset);
        PLGMethodManifest.addOutputPorts(builder, OUTPUT_PORTSOffset);
        PLGMethodManifest.addMaxBatch(builder, MAX_BATCH);
        PLGMethodManifest.addDrainPolicy(builder, DRAIN_POLICY);
        PLGMethodManifest.addDescription(builder, DESCRIPTIONOffset);
        return PLGMethodManifest.endPLGMethodManifest(builder);
    }
    unpack() {
        return new PLGMethodManifestT(this.METHOD_ID(), this.DISPLAY_NAME(), this.bb.createObjList(this.INPUT_PORTS.bind(this), this.inputPortsLength()), this.bb.createObjList(this.OUTPUT_PORTS.bind(this), this.outputPortsLength()), this.MAX_BATCH(), this.DRAIN_POLICY(), this.DESCRIPTION());
    }
    unpackTo(_o) {
        _o.METHOD_ID = this.METHOD_ID();
        _o.DISPLAY_NAME = this.DISPLAY_NAME();
        _o.INPUT_PORTS = this.bb.createObjList(this.INPUT_PORTS.bind(this), this.inputPortsLength());
        _o.OUTPUT_PORTS = this.bb.createObjList(this.OUTPUT_PORTS.bind(this), this.outputPortsLength());
        _o.MAX_BATCH = this.MAX_BATCH();
        _o.DRAIN_POLICY = this.DRAIN_POLICY();
        _o.DESCRIPTION = this.DESCRIPTION();
    }
}
export class PLGMethodManifestT {
    constructor(METHOD_ID = null, DISPLAY_NAME = null, INPUT_PORTS = [], OUTPUT_PORTS = [], MAX_BATCH = 1, DRAIN_POLICY = drainBehavior.DRAIN_UNTIL_YIELD, DESCRIPTION = null) {
        this.METHOD_ID = METHOD_ID;
        this.DISPLAY_NAME = DISPLAY_NAME;
        this.INPUT_PORTS = INPUT_PORTS;
        this.OUTPUT_PORTS = OUTPUT_PORTS;
        this.MAX_BATCH = MAX_BATCH;
        this.DRAIN_POLICY = DRAIN_POLICY;
        this.DESCRIPTION = DESCRIPTION;
    }
    pack(builder) {
        const METHOD_ID = (this.METHOD_ID !== null ? builder.createString(this.METHOD_ID) : 0);
        const DISPLAY_NAME = (this.DISPLAY_NAME !== null ? builder.createString(this.DISPLAY_NAME) : 0);
        const INPUT_PORTS = PLGMethodManifest.createInputPortsVector(builder, builder.createObjectOffsetList(this.INPUT_PORTS));
        const OUTPUT_PORTS = PLGMethodManifest.createOutputPortsVector(builder, builder.createObjectOffsetList(this.OUTPUT_PORTS));
        const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION) : 0);
        return PLGMethodManifest.createPLGMethodManifest(builder, METHOD_ID, DISPLAY_NAME, INPUT_PORTS, OUTPUT_PORTS, this.MAX_BATCH, this.DRAIN_POLICY, DESCRIPTION);
    }
}
