// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { OPPQuantity } from './OPPQuantity.js';
/**
 * Projected areas and the ratios derived from them.
 */
export class OPPAreas {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsOPPAreas(bb, obj) {
        return (obj || new OPPAreas()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsOPPAreas(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new OPPAreas()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Average projected cross-sectional area over all aspects [m2].
     */
    CROSS_SECTIONAL_AVERAGE(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new OPPQuantity()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Smallest projected cross-sectional area [m2].
     */
    CROSS_SECTIONAL_MINIMUM(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new OPPQuantity()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Largest projected cross-sectional area [m2].
     */
    CROSS_SECTIONAL_MAXIMUM(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new OPPQuantity()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Total solar-array area [m2].
     */
    SOLAR_ARRAY(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new OPPQuantity()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Area-to-mass ratio [m2/kg], only when a source publishes one directly or
     * the publisher derives it with PROVENANCE.METHOD DERIVED.
     */
    AREA_TO_MASS_RATIO(obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new OPPQuantity()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * Ballistic coefficient [kg/m2].
     */
    BALLISTIC_COEFFICIENT(obj) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? (obj || new OPPQuantity()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startOPPAreas(builder) {
        builder.startObject(6);
    }
    static addCrossSectionalAverage(builder, CROSS_SECTIONAL_AVERAGEOffset) {
        builder.addFieldOffset(0, CROSS_SECTIONAL_AVERAGEOffset, 0);
    }
    static addCrossSectionalMinimum(builder, CROSS_SECTIONAL_MINIMUMOffset) {
        builder.addFieldOffset(1, CROSS_SECTIONAL_MINIMUMOffset, 0);
    }
    static addCrossSectionalMaximum(builder, CROSS_SECTIONAL_MAXIMUMOffset) {
        builder.addFieldOffset(2, CROSS_SECTIONAL_MAXIMUMOffset, 0);
    }
    static addSolarArray(builder, SOLAR_ARRAYOffset) {
        builder.addFieldOffset(3, SOLAR_ARRAYOffset, 0);
    }
    static addAreaToMassRatio(builder, AREA_TO_MASS_RATIOOffset) {
        builder.addFieldOffset(4, AREA_TO_MASS_RATIOOffset, 0);
    }
    static addBallisticCoefficient(builder, BALLISTIC_COEFFICIENTOffset) {
        builder.addFieldOffset(5, BALLISTIC_COEFFICIENTOffset, 0);
    }
    static endOPPAreas(builder) {
        const offset = builder.endObject();
        return offset;
    }
    unpack() {
        return new OPPAreasT((this.CROSS_SECTIONAL_AVERAGE() !== null ? this.CROSS_SECTIONAL_AVERAGE().unpack() : null), (this.CROSS_SECTIONAL_MINIMUM() !== null ? this.CROSS_SECTIONAL_MINIMUM().unpack() : null), (this.CROSS_SECTIONAL_MAXIMUM() !== null ? this.CROSS_SECTIONAL_MAXIMUM().unpack() : null), (this.SOLAR_ARRAY() !== null ? this.SOLAR_ARRAY().unpack() : null), (this.AREA_TO_MASS_RATIO() !== null ? this.AREA_TO_MASS_RATIO().unpack() : null), (this.BALLISTIC_COEFFICIENT() !== null ? this.BALLISTIC_COEFFICIENT().unpack() : null));
    }
    unpackTo(_o) {
        _o.CROSS_SECTIONAL_AVERAGE = (this.CROSS_SECTIONAL_AVERAGE() !== null ? this.CROSS_SECTIONAL_AVERAGE().unpack() : null);
        _o.CROSS_SECTIONAL_MINIMUM = (this.CROSS_SECTIONAL_MINIMUM() !== null ? this.CROSS_SECTIONAL_MINIMUM().unpack() : null);
        _o.CROSS_SECTIONAL_MAXIMUM = (this.CROSS_SECTIONAL_MAXIMUM() !== null ? this.CROSS_SECTIONAL_MAXIMUM().unpack() : null);
        _o.SOLAR_ARRAY = (this.SOLAR_ARRAY() !== null ? this.SOLAR_ARRAY().unpack() : null);
        _o.AREA_TO_MASS_RATIO = (this.AREA_TO_MASS_RATIO() !== null ? this.AREA_TO_MASS_RATIO().unpack() : null);
        _o.BALLISTIC_COEFFICIENT = (this.BALLISTIC_COEFFICIENT() !== null ? this.BALLISTIC_COEFFICIENT().unpack() : null);
    }
}
export class OPPAreasT {
    constructor(CROSS_SECTIONAL_AVERAGE = null, CROSS_SECTIONAL_MINIMUM = null, CROSS_SECTIONAL_MAXIMUM = null, SOLAR_ARRAY = null, AREA_TO_MASS_RATIO = null, BALLISTIC_COEFFICIENT = null) {
        this.CROSS_SECTIONAL_AVERAGE = CROSS_SECTIONAL_AVERAGE;
        this.CROSS_SECTIONAL_MINIMUM = CROSS_SECTIONAL_MINIMUM;
        this.CROSS_SECTIONAL_MAXIMUM = CROSS_SECTIONAL_MAXIMUM;
        this.SOLAR_ARRAY = SOLAR_ARRAY;
        this.AREA_TO_MASS_RATIO = AREA_TO_MASS_RATIO;
        this.BALLISTIC_COEFFICIENT = BALLISTIC_COEFFICIENT;
    }
    pack(builder) {
        const CROSS_SECTIONAL_AVERAGE = (this.CROSS_SECTIONAL_AVERAGE !== null ? this.CROSS_SECTIONAL_AVERAGE.pack(builder) : 0);
        const CROSS_SECTIONAL_MINIMUM = (this.CROSS_SECTIONAL_MINIMUM !== null ? this.CROSS_SECTIONAL_MINIMUM.pack(builder) : 0);
        const CROSS_SECTIONAL_MAXIMUM = (this.CROSS_SECTIONAL_MAXIMUM !== null ? this.CROSS_SECTIONAL_MAXIMUM.pack(builder) : 0);
        const SOLAR_ARRAY = (this.SOLAR_ARRAY !== null ? this.SOLAR_ARRAY.pack(builder) : 0);
        const AREA_TO_MASS_RATIO = (this.AREA_TO_MASS_RATIO !== null ? this.AREA_TO_MASS_RATIO.pack(builder) : 0);
        const BALLISTIC_COEFFICIENT = (this.BALLISTIC_COEFFICIENT !== null ? this.BALLISTIC_COEFFICIENT.pack(builder) : 0);
        OPPAreas.startOPPAreas(builder);
        OPPAreas.addCrossSectionalAverage(builder, CROSS_SECTIONAL_AVERAGE);
        OPPAreas.addCrossSectionalMinimum(builder, CROSS_SECTIONAL_MINIMUM);
        OPPAreas.addCrossSectionalMaximum(builder, CROSS_SECTIONAL_MAXIMUM);
        OPPAreas.addSolarArray(builder, SOLAR_ARRAY);
        OPPAreas.addAreaToMassRatio(builder, AREA_TO_MASS_RATIO);
        OPPAreas.addBallisticCoefficient(builder, BALLISTIC_COEFFICIENT);
        return OPPAreas.endOPPAreas(builder);
    }
}
