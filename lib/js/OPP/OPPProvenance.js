// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { oppDeterminationMethod } from './oppDeterminationMethod.js';
/**
 * Provenance for exactly one physical value.
 *
 * This table is the mechanism by which the never-invent-data law is enforced
 * in the IDL rather than in prose: no physical quantity in an $OPP can be
 * expressed without one of these attached, so a value with no admissible
 * source cannot be encoded at all. Unknown values are ABSENT. They are never
 * zero, never a placeholder, and never carried forward from a different
 * object.
 */
export class OPPProvenance {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsOPPProvenance(bb, obj) {
        return (obj || new OPPProvenance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsOPPProvenance(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new OPPProvenance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    SOURCE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return this.bb.__string(this.bb_pos + offset, optionalEncoding);
    }
    SOURCE_RECORD_ID(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE_URL(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    EPOCH(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    RETRIEVED_AT(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    METHOD() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : oppDeterminationMethod.UNSPECIFIED;
    }
    LICENSE(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 18);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    ATTRIBUTION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 20);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    SOURCE_SHA256(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 22);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    NOTES(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 24);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    static startOPPProvenance(builder) {
        builder.startObject(11);
    }
    static addSource(builder, SOURCEOffset) {
        builder.addFieldOffset(0, SOURCEOffset, 0);
    }
    static addSourceRecordId(builder, SOURCE_RECORD_IDOffset) {
        builder.addFieldOffset(1, SOURCE_RECORD_IDOffset, 0);
    }
    static addSourceUrl(builder, SOURCE_URLOffset) {
        builder.addFieldOffset(2, SOURCE_URLOffset, 0);
    }
    static addEpoch(builder, EPOCHOffset) {
        builder.addFieldOffset(3, EPOCHOffset, 0);
    }
    static addRetrievedAt(builder, RETRIEVED_ATOffset) {
        builder.addFieldOffset(4, RETRIEVED_ATOffset, 0);
    }
    static addSourceVersion(builder, SOURCE_VERSIONOffset) {
        builder.addFieldOffset(5, SOURCE_VERSIONOffset, 0);
    }
    static addMethod(builder, METHOD) {
        builder.addFieldInt8(6, METHOD, oppDeterminationMethod.UNSPECIFIED);
    }
    static addLicense(builder, LICENSEOffset) {
        builder.addFieldOffset(7, LICENSEOffset, 0);
    }
    static addAttribution(builder, ATTRIBUTIONOffset) {
        builder.addFieldOffset(8, ATTRIBUTIONOffset, 0);
    }
    static addSourceSha256(builder, SOURCE_SHA256Offset) {
        builder.addFieldOffset(9, SOURCE_SHA256Offset, 0);
    }
    static addNotes(builder, NOTESOffset) {
        builder.addFieldOffset(10, NOTESOffset, 0);
    }
    static endOPPProvenance(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // SOURCE
        return offset;
    }
    static createOPPProvenance(builder, SOURCEOffset, SOURCE_RECORD_IDOffset, SOURCE_URLOffset, EPOCHOffset, RETRIEVED_ATOffset, SOURCE_VERSIONOffset, METHOD, LICENSEOffset, ATTRIBUTIONOffset, SOURCE_SHA256Offset, NOTESOffset) {
        OPPProvenance.startOPPProvenance(builder);
        OPPProvenance.addSource(builder, SOURCEOffset);
        OPPProvenance.addSourceRecordId(builder, SOURCE_RECORD_IDOffset);
        OPPProvenance.addSourceUrl(builder, SOURCE_URLOffset);
        OPPProvenance.addEpoch(builder, EPOCHOffset);
        OPPProvenance.addRetrievedAt(builder, RETRIEVED_ATOffset);
        OPPProvenance.addSourceVersion(builder, SOURCE_VERSIONOffset);
        OPPProvenance.addMethod(builder, METHOD);
        OPPProvenance.addLicense(builder, LICENSEOffset);
        OPPProvenance.addAttribution(builder, ATTRIBUTIONOffset);
        OPPProvenance.addSourceSha256(builder, SOURCE_SHA256Offset);
        OPPProvenance.addNotes(builder, NOTESOffset);
        return OPPProvenance.endOPPProvenance(builder);
    }
    unpack() {
        return new OPPProvenanceT(this.SOURCE(), this.SOURCE_RECORD_ID(), this.SOURCE_URL(), this.EPOCH(), this.RETRIEVED_AT(), this.SOURCE_VERSION(), this.METHOD(), this.LICENSE(), this.ATTRIBUTION(), this.SOURCE_SHA256(), this.NOTES());
    }
    unpackTo(_o) {
        _o.SOURCE = this.SOURCE();
        _o.SOURCE_RECORD_ID = this.SOURCE_RECORD_ID();
        _o.SOURCE_URL = this.SOURCE_URL();
        _o.EPOCH = this.EPOCH();
        _o.RETRIEVED_AT = this.RETRIEVED_AT();
        _o.SOURCE_VERSION = this.SOURCE_VERSION();
        _o.METHOD = this.METHOD();
        _o.LICENSE = this.LICENSE();
        _o.ATTRIBUTION = this.ATTRIBUTION();
        _o.SOURCE_SHA256 = this.SOURCE_SHA256();
        _o.NOTES = this.NOTES();
    }
}
export class OPPProvenanceT {
    constructor(SOURCE = null, SOURCE_RECORD_ID = null, SOURCE_URL = null, EPOCH = null, RETRIEVED_AT = null, SOURCE_VERSION = null, METHOD = oppDeterminationMethod.UNSPECIFIED, LICENSE = null, ATTRIBUTION = null, SOURCE_SHA256 = null, NOTES = null) {
        this.SOURCE = SOURCE;
        this.SOURCE_RECORD_ID = SOURCE_RECORD_ID;
        this.SOURCE_URL = SOURCE_URL;
        this.EPOCH = EPOCH;
        this.RETRIEVED_AT = RETRIEVED_AT;
        this.SOURCE_VERSION = SOURCE_VERSION;
        this.METHOD = METHOD;
        this.LICENSE = LICENSE;
        this.ATTRIBUTION = ATTRIBUTION;
        this.SOURCE_SHA256 = SOURCE_SHA256;
        this.NOTES = NOTES;
    }
    pack(builder) {
        const SOURCE = (this.SOURCE !== null ? builder.createString(this.SOURCE) : 0);
        const SOURCE_RECORD_ID = (this.SOURCE_RECORD_ID !== null ? builder.createString(this.SOURCE_RECORD_ID) : 0);
        const SOURCE_URL = (this.SOURCE_URL !== null ? builder.createString(this.SOURCE_URL) : 0);
        const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH) : 0);
        const RETRIEVED_AT = (this.RETRIEVED_AT !== null ? builder.createString(this.RETRIEVED_AT) : 0);
        const SOURCE_VERSION = (this.SOURCE_VERSION !== null ? builder.createString(this.SOURCE_VERSION) : 0);
        const LICENSE = (this.LICENSE !== null ? builder.createString(this.LICENSE) : 0);
        const ATTRIBUTION = (this.ATTRIBUTION !== null ? builder.createString(this.ATTRIBUTION) : 0);
        const SOURCE_SHA256 = (this.SOURCE_SHA256 !== null ? builder.createString(this.SOURCE_SHA256) : 0);
        const NOTES = (this.NOTES !== null ? builder.createString(this.NOTES) : 0);
        return OPPProvenance.createOPPProvenance(builder, SOURCE, SOURCE_RECORD_ID, SOURCE_URL, EPOCH, RETRIEVED_AT, SOURCE_VERSION, this.METHOD, LICENSE, ATTRIBUTION, SOURCE_SHA256, NOTES);
    }
}
