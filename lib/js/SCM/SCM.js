// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers';
import { SCHEMA_STANDARD } from './SCHEMA_STANDARD.js';
/**
 * Schema Manifest
 */
export class SCM {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsSCM(bb, obj) {
        return (obj || new SCM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsSCM(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new SCM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static bufferHasIdentifier(bb) {
        return bb.__has_identifier('$SCM');
    }
    version(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Standards Dictionary
     */
    RECORDS(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new SCHEMA_STANDARD()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    recordsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    STANDARDS_VERSION(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Unix milliseconds when this registry frame was generated.
     */
    GENERATED_AT_MS() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readInt64(this.bb_pos + offset) : BigInt('0');
    }
    static startSCM(builder) {
        builder.startObject(4);
    }
    static addVersion(builder, versionOffset) {
        builder.addFieldOffset(0, versionOffset, 0);
    }
    static addRecords(builder, RECORDSOffset) {
        builder.addFieldOffset(1, RECORDSOffset, 0);
    }
    static createRecordsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startRecordsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addStandardsVersion(builder, STANDARDS_VERSIONOffset) {
        builder.addFieldOffset(2, STANDARDS_VERSIONOffset, 0);
    }
    static addGeneratedAtMs(builder, GENERATED_AT_MS) {
        builder.addFieldInt64(3, GENERATED_AT_MS, BigInt('0'));
    }
    static endSCM(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static finishSCMBuffer(builder, offset) {
        builder.finish(offset, '$SCM');
    }
    static finishSizePrefixedSCMBuffer(builder, offset) {
        builder.finish(offset, '$SCM', true);
    }
    static createSCM(builder, versionOffset, RECORDSOffset, STANDARDS_VERSIONOffset, GENERATED_AT_MS) {
        SCM.startSCM(builder);
        SCM.addVersion(builder, versionOffset);
        SCM.addRecords(builder, RECORDSOffset);
        SCM.addStandardsVersion(builder, STANDARDS_VERSIONOffset);
        SCM.addGeneratedAtMs(builder, GENERATED_AT_MS);
        return SCM.endSCM(builder);
    }
    unpack() {
        return new SCMT(this.version(), this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength()), this.STANDARDS_VERSION(), this.GENERATED_AT_MS());
    }
    unpackTo(_o) {
        _o.version = this.version();
        _o.RECORDS = this.bb.createObjList(this.RECORDS.bind(this), this.recordsLength());
        _o.STANDARDS_VERSION = this.STANDARDS_VERSION();
        _o.GENERATED_AT_MS = this.GENERATED_AT_MS();
    }
}
export class SCMT {
    constructor(version = null, RECORDS = [], STANDARDS_VERSION = null, GENERATED_AT_MS = BigInt('0')) {
        this.version = version;
        this.RECORDS = RECORDS;
        this.STANDARDS_VERSION = STANDARDS_VERSION;
        this.GENERATED_AT_MS = GENERATED_AT_MS;
    }
    pack(builder) {
        const version = (this.version !== null ? builder.createString(this.version) : 0);
        const RECORDS = SCM.createRecordsVector(builder, builder.createObjectOffsetList(this.RECORDS));
        const STANDARDS_VERSION = (this.STANDARDS_VERSION !== null ? builder.createString(this.STANDARDS_VERSION) : 0);
        return SCM.createSCM(builder, version, RECORDS, STANDARDS_VERSION, this.GENERATED_AT_MS);
    }
}
