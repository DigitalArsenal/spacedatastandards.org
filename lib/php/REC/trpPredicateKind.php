<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Trust predicate evaluated by a rule policy. Append new values only; never
/// reorder or reuse existing values.
class trpPredicateKind
{
    /// A subject must hold at least the configured value.
    const MinValueLocked = 0;
    /// A subject must have held the configured value for a minimum duration.
    const ValueForDuration = 1;
    /// A subject's qualifying value must use an allowed asset.
    const AllowedTokens = 2;
    /// A subject must satisfy an X-of-N trusted-connection rule.
    const TrustedConnections = 3;

    private static $names = array(
        trpPredicateKind::MinValueLocked=>"MinValueLocked",
        trpPredicateKind::ValueForDuration=>"ValueForDuration",
        trpPredicateKind::AllowedTokens=>"AllowedTokens",
        trpPredicateKind::TrustedConnections=>"TrustedConnections",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
