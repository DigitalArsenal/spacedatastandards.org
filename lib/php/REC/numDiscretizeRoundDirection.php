<?php
// automatically generated by the FlatBuffers compiler, do not modify

class numDiscretizeRoundDirection
{
    /// Round magnitude toward zero before restoring sign.
    const TO_ZERO = 0;
    /// Round magnitude away from zero before restoring sign.
    const FROM_ZERO = 1;
    /// Round magnitude to the nearest least-significant-bit multiple before restoring sign.
    const NEAR = 2;

    private static $names = array(
        numDiscretizeRoundDirection::TO_ZERO=>"TO_ZERO",
        numDiscretizeRoundDirection::FROM_ZERO=>"FROM_ZERO",
        numDiscretizeRoundDirection::NEAR=>"NEAR",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
