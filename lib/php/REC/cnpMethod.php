<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// How the underlying observations were produced.
class cnpMethod
{
    /// M-Lab NDT7 measurement.
    const NDT7 = 0;
    /// A speed test other than NDT7 (Ookla, Cloudflare AIM, operator-native).
    const SPEED_TEST = 1;
    /// ICMP or UDP echo.
    const PING = 2;
    /// Path trace.
    const TRACEROUTE = 3;
    /// Telemetry read from the user terminal itself.
    const TERMINAL_TELEMETRY = 4;
    /// Bulk HTTP transfer.
    const HTTP_DOWNLOAD = 5;
    /// Passive observation of production traffic.
    const PASSIVE = 6;
    /// Modelled or simulated rather than measured. A modelled value is not a
    /// measurement and this member is how a consumer refuses to treat it as one.
    const MODELED = 7;
    /// The source states a method none of the above names.
    const OTHER = 8;

    private static $names = array(
        cnpMethod::NDT7=>"NDT7",
        cnpMethod::SPEED_TEST=>"SPEED_TEST",
        cnpMethod::PING=>"PING",
        cnpMethod::TRACEROUTE=>"TRACEROUTE",
        cnpMethod::TERMINAL_TELEMETRY=>"TERMINAL_TELEMETRY",
        cnpMethod::HTTP_DOWNLOAD=>"HTTP_DOWNLOAD",
        cnpMethod::PASSIVE=>"PASSIVE",
        cnpMethod::MODELED=>"MODELED",
        cnpMethod::OTHER=>"OTHER",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
