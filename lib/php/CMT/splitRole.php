<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Role a split recipient plays in the revenue division.
class splitRole
{
    /// The module/flow publisher (vendor)
    const Publisher = 0;
    /// The store operator platform (e.g. SpaceAware) — the commission recipient
    const Platform = 1;
    /// A referrer who drove the sale
    const Referrer = 2;
    /// A charitable recipient
    const Charity = 3;

    private static $names = array(
        splitRole::Publisher=>"Publisher",
        splitRole::Platform=>"Platform",
        splitRole::Referrer=>"Referrer",
        splitRole::Charity=>"Charity",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
