<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Physical quantity a map pixel states.
/// Append new values only; never reorder or reuse existing values.
class sktMapQuantity
{
    const UNSPECIFIED = 0;
    /// Surface brightness, MAP_UNITS per steradian or per square arcsecond as
    /// MAP_UNITS states.
    const SURFACE_BRIGHTNESS = 1;
    /// Brightness temperature, kelvin.
    const BRIGHTNESS_TEMPERATURE = 2;
    /// Detected counts per pixel.
    const COUNTS = 3;
    /// Counts per unit exposure (a rate map).
    const COUNT_RATE = 4;
    /// Exposure per pixel.
    const EXPOSURE = 5;
    /// Probability per pixel.
    const PROBABILITY = 6;
    /// Surface density of sources per pixel.
    const SOURCE_DENSITY = 7;
    /// Quantity named in MAP_QUANTITY_NAME.
    const OTHER = 8;

    private static $names = array(
        sktMapQuantity::UNSPECIFIED=>"UNSPECIFIED",
        sktMapQuantity::SURFACE_BRIGHTNESS=>"SURFACE_BRIGHTNESS",
        sktMapQuantity::BRIGHTNESS_TEMPERATURE=>"BRIGHTNESS_TEMPERATURE",
        sktMapQuantity::COUNTS=>"COUNTS",
        sktMapQuantity::COUNT_RATE=>"COUNT_RATE",
        sktMapQuantity::EXPOSURE=>"EXPOSURE",
        sktMapQuantity::PROBABILITY=>"PROBABILITY",
        sktMapQuantity::SOURCE_DENSITY=>"SOURCE_DENSITY",
        sktMapQuantity::OTHER=>"OTHER",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
