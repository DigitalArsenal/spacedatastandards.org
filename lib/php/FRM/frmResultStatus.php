<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Append new values only; never reorder or reuse existing values.
class frmResultStatus
{
    const OK = 0;
    const INVALID_INPUT = 1;
    const UNSUPPORTED_OPERATION = 2;
    /// The provider does not implement the requested axis set.
    const UNSUPPORTED_AXIS_TYPE = 3;
    /// The provider does not implement the requested element set.
    const UNSUPPORTED_STATE_REPRESENTATION = 4;
    /// The axis chain needs Earth-orientation data that was not supplied or
    /// does not span EPOCH. The provider MUST fail here rather than silently
    /// extrapolate.
    const MISSING_EOP_DATA = 5;
    /// The state is at a singularity of the requested element set (for example
    /// e -> 0 for Keplerian). The provider MUST report this rather than
    /// substitute a different element set.
    const SINGULAR_ELEMENT_SET = 6;

    private static $names = array(
        frmResultStatus::OK=>"OK",
        frmResultStatus::INVALID_INPUT=>"INVALID_INPUT",
        frmResultStatus::UNSUPPORTED_OPERATION=>"UNSUPPORTED_OPERATION",
        frmResultStatus::UNSUPPORTED_AXIS_TYPE=>"UNSUPPORTED_AXIS_TYPE",
        frmResultStatus::UNSUPPORTED_STATE_REPRESENTATION=>"UNSUPPORTED_STATE_REPRESENTATION",
        frmResultStatus::MISSING_EOP_DATA=>"MISSING_EOP_DATA",
        frmResultStatus::SINGULAR_ELEMENT_SET=>"SINGULAR_ELEMENT_SET",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
