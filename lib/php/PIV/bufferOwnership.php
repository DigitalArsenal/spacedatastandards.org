<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Buffer ownership contract advertised by a stream port.
class bufferOwnership
{
    /// Arena / host owns the backing bytes; receiver must not free.
    const HOST_OWNED = 0;
    /// Plugin owns the backing bytes; host must not free.
    const PLUGIN_OWNED = 1;
    /// Ownership transfers with the frame (hand-off semantics).
    const TRANSFERRED = 2;

    private static $names = array(
        bufferOwnership::HOST_OWNED=>"HOST_OWNED",
        bufferOwnership::PLUGIN_OWNED=>"PLUGIN_OWNED",
        bufferOwnership::TRANSFERRED=>"TRANSFERRED",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
