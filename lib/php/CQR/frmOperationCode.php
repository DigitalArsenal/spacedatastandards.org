<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Operations this record can request. Values 1-4 are the original
/// position-only planet-centred-inertial / planet-centred-fixed / geodetic
/// conversions and are FROZEN. Append new values only; never reorder.
class frmOperationCode
{
    const UNKNOWN = 0;
    const PCI_TO_PCPF = 1;
    const PCPF_TO_PCI = 2;
    const LLA_TO_PCPF = 3;
    const PCPF_TO_LLA = 4;
    /// Transform a full state (position AND velocity) from SOURCE_COORDINATE_SYSTEM
    /// to TARGET_COORDINATE_SYSTEM at EPOCH, expressed in TARGET_REPRESENTATION.
    const STATE_TRANSFORM = 5;
    /// Return only the rotation from SOURCE_COORDINATE_SYSTEM to
    /// TARGET_COORDINATE_SYSTEM at EPOCH, with its time derivative and the
    /// angular velocity of the target axes. No state is required.
    const FRAME_ROTATION = 6;
    /// Convert a state between element sets within ONE coordinate system.
    const STATE_REPRESENTATION_CONVERT = 7;

    private static $names = array(
        frmOperationCode::UNKNOWN=>"UNKNOWN",
        frmOperationCode::PCI_TO_PCPF=>"PCI_TO_PCPF",
        frmOperationCode::PCPF_TO_PCI=>"PCPF_TO_PCI",
        frmOperationCode::LLA_TO_PCPF=>"LLA_TO_PCPF",
        frmOperationCode::PCPF_TO_LLA=>"PCPF_TO_LLA",
        frmOperationCode::STATE_TRANSFORM=>"STATE_TRANSFORM",
        frmOperationCode::FRAME_ROTATION=>"FRAME_ROTATION",
        frmOperationCode::STATE_REPRESENTATION_CONVERT=>"STATE_REPRESENTATION_CONVERT",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
