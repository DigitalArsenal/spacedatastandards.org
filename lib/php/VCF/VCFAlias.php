<?php
// automatically generated by the FlatBuffers compiler, do not modify

use \Google\FlatBuffers\Struct;
use \Google\FlatBuffers\Table;
use \Google\FlatBuffers\ByteBuffer;
use \Google\FlatBuffers\FlatBufferBuilder;

/// One identity address row: the machine-readable half of the card.
class VCFAlias extends Table
{
    /**
     * @param ByteBuffer $bb
     * @return VCFAlias
     */
    public static function getRootAsVCFAlias(ByteBuffer $bb)
    {
        $obj = new VCFAlias();
        return ($obj->init($bb->getInt($bb->getPosition()) + $bb->getPosition(), $bb));
    }

    public static function VCFAliasIdentifier()
    {
        return "$VCF";
    }

    public static function VCFAliasBufferHasIdentifier(ByteBuffer $buf)
    {
        return self::__has_identifier($buf, self::VCFAliasIdentifier());
    }

    /**
     * @param int $_i offset
     * @param ByteBuffer $_bb
     * @return VCFAlias
     **/
    public function init($_i, ByteBuffer $_bb)
    {
        $this->bb_pos = $_i;
        $this->bb = $_bb;
        return $this;
    }

    /// What the row carries.
    /**
     * @return sbyte
     */
    public function getKIND()
    {
        $o = $this->__offset(4);
        return $o != 0 ? $this->bb->getSbyte($o + $this->bb_pos) : \vcfAliasKind::UNSPECIFIED;
    }

    /// The row's kind label verbatim as emitted — the leftmost domain label.
    /// Authoritative when KIND is OTHER.
    public function getKIND_TOKEN()
    {
        $o = $this->__offset(6);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Everything left of the separator in the emitted address, verbatim.
    public function getLOCAL_PART()
    {
        $o = $this->__offset(8);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Everything right of the separator in the emitted address, verbatim. This
    /// is publisher data: the identity domain of the deployment that produced the
    /// card, never a value this standard fixes.
    public function getDOMAIN()
    {
        $o = $this->__offset(10);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// The complete emitted address, `LOCAL_PART` then the separator then
    /// `DOMAIN`, verbatim. Carried whole because it is what the importer stores
    /// and what a consumer matches on.
    public function getADDRESS()
    {
        $o = $this->__offset(12);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Transport encoding of LOCAL_PART verbatim and lowercase. An address
    /// local part has a hard octet cap, so raw hexadecimal key material does not
    /// fit and a compact binary-to-text encoding is REQUIRED for key rows.
    public function getENCODING()
    {
        $o = $this->__offset(14);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Cryptographic algorithm the row's key or signature belongs to, verbatim as
    /// the profile states it. A key without a named algorithm is not verifiable.
    public function getALGORITHM()
    {
        $o = $this->__offset(16);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// The profile's published public key value verbatim, for key rows. The SAME
    /// bytes LOCAL_PART transport-encodes, carried in the profile's own spelling
    /// so a consumer can join card to profile without decoding. NEVER an extended
    /// public key and NEVER a derivation path.
    public function getPUBLIC_KEY()
    {
        $o = $this->__offset(18);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Ledger token verbatim for LEDGER_ACCOUNT rows.
    public function getCHAIN()
    {
        $o = $this->__offset(20);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Whether this row survives into the COMPACT form.
    /**
     * @return bool
     */
    public function getON_COMPACT_CARD()
    {
        $o = $this->__offset(22);
        return $o != 0 ? $this->bb->getBool($o + $this->bb_pos) : false;
    }

    /// Octet length of the complete UNFOLDED row as emitted.
    /**
     * @return uint
     */
    public function getOCTET_LENGTH()
    {
        $o = $this->__offset(24);
        return $o != 0 ? $this->bb->getUint($o + $this->bb_pos) : 0;
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return void
     */
    public static function startVCFAlias(FlatBufferBuilder $builder)
    {
        $builder->StartObject(11);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return VCFAlias
     */
    public static function createVCFAlias(FlatBufferBuilder $builder, $KIND, $KIND_TOKEN, $LOCAL_PART, $DOMAIN, $ADDRESS, $ENCODING, $ALGORITHM, $PUBLIC_KEY, $CHAIN, $ON_COMPACT_CARD, $OCTET_LENGTH)
    {
        $builder->startObject(11);
        self::addKIND($builder, $KIND);
        self::addKIND_TOKEN($builder, $KIND_TOKEN);
        self::addLOCAL_PART($builder, $LOCAL_PART);
        self::addDOMAIN($builder, $DOMAIN);
        self::addADDRESS($builder, $ADDRESS);
        self::addENCODING($builder, $ENCODING);
        self::addALGORITHM($builder, $ALGORITHM);
        self::addPUBLIC_KEY($builder, $PUBLIC_KEY);
        self::addCHAIN($builder, $CHAIN);
        self::addON_COMPACT_CARD($builder, $ON_COMPACT_CARD);
        self::addOCTET_LENGTH($builder, $OCTET_LENGTH);
        $o = $builder->endObject();
        $builder->required($o, 6);  // KIND_TOKEN
        $builder->required($o, 8);  // LOCAL_PART
        $builder->required($o, 10);  // DOMAIN
        $builder->required($o, 12);  // ADDRESS
        return $o;
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param sbyte
     * @return void
     */
    public static function addKIND(FlatBufferBuilder $builder, $KIND)
    {
        $builder->addSbyteX(0, $KIND, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addKIND_TOKEN(FlatBufferBuilder $builder, $KIND_TOKEN)
    {
        $builder->addOffsetX(1, $KIND_TOKEN, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addLOCAL_PART(FlatBufferBuilder $builder, $LOCAL_PART)
    {
        $builder->addOffsetX(2, $LOCAL_PART, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addDOMAIN(FlatBufferBuilder $builder, $DOMAIN)
    {
        $builder->addOffsetX(3, $DOMAIN, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addADDRESS(FlatBufferBuilder $builder, $ADDRESS)
    {
        $builder->addOffsetX(4, $ADDRESS, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addENCODING(FlatBufferBuilder $builder, $ENCODING)
    {
        $builder->addOffsetX(5, $ENCODING, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addALGORITHM(FlatBufferBuilder $builder, $ALGORITHM)
    {
        $builder->addOffsetX(6, $ALGORITHM, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addPUBLIC_KEY(FlatBufferBuilder $builder, $PUBLIC_KEY)
    {
        $builder->addOffsetX(7, $PUBLIC_KEY, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addCHAIN(FlatBufferBuilder $builder, $CHAIN)
    {
        $builder->addOffsetX(8, $CHAIN, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param bool
     * @return void
     */
    public static function addON_COMPACT_CARD(FlatBufferBuilder $builder, $ON_COMPACT_CARD)
    {
        $builder->addBoolX(9, $ON_COMPACT_CARD, false);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param uint
     * @return void
     */
    public static function addOCTET_LENGTH(FlatBufferBuilder $builder, $OCTET_LENGTH)
    {
        $builder->addUintX(10, $OCTET_LENGTH, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return int table offset
     */
    public static function endVCFAlias(FlatBufferBuilder $builder)
    {
        $o = $builder->endObject();
        $builder->required($o, 6);  // KIND_TOKEN
        $builder->required($o, 8);  // LOCAL_PART
        $builder->required($o, 10);  // DOMAIN
        $builder->required($o, 12);  // ADDRESS
        return $o;
    }
}
