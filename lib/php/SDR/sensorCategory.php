<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Sensor Detection Report — runtime I/O contract for sensor detection
/// plugins. Distinct from SNR (sensor hardware catalog) and SEN (sensor
/// maintenance log); SDR carries per-frame target states, configuration,
/// and detection results.
/// Sensor category for a report.
class sensorCategory
{
    const RADAR = 0;
    const ELECTRO_OPTICAL = 1;
    const INFRARED = 2;
    const ACOUSTIC = 3;
    const RF = 4;
    const LIDAR = 5;
    const CUSTOM = 255;

    private static $names = array(
        sensorCategory::RADAR=>"RADAR",
        sensorCategory::ELECTRO_OPTICAL=>"ELECTRO_OPTICAL",
        sensorCategory::INFRARED=>"INFRARED",
        sensorCategory::ACOUSTIC=>"ACOUSTIC",
        sensorCategory::RF=>"RF",
        sensorCategory::LIDAR=>"LIDAR",
        sensorCategory::CUSTOM=>"CUSTOM",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
