<?php
// automatically generated by the FlatBuffers compiler, do not modify

class launchDetectionType
{
    const IR_DETECT = 0;
    const RADAR_DETECT = 1;
    const OPTICAL_DETECT = 2;
    const ELINT_DETECT = 3;
    const COMBINED = 4;
    const UNKNOWN = 5;

    private static $names = array(
        launchDetectionType::IR_DETECT=>"IR_DETECT",
        launchDetectionType::RADAR_DETECT=>"RADAR_DETECT",
        launchDetectionType::OPTICAL_DETECT=>"OPTICAL_DETECT",
        launchDetectionType::ELINT_DETECT=>"ELINT_DETECT",
        launchDetectionType::COMBINED=>"COMBINED",
        launchDetectionType::UNKNOWN=>"UNKNOWN",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
