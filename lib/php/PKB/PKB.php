<?php
// automatically generated by the FlatBuffers compiler, do not modify

use \Google\FlatBuffers\Struct;
use \Google\FlatBuffers\Table;
use \Google\FlatBuffers\ByteBuffer;
use \Google\FlatBuffers\FlatBufferBuilder;

/// Publisher Key-Broker Descriptor
class PKB extends Table
{
    /**
     * @param ByteBuffer $bb
     * @return PKB
     */
    public static function getRootAsPKB(ByteBuffer $bb)
    {
        $obj = new PKB();
        return ($obj->init($bb->getInt($bb->getPosition()) + $bb->getPosition(), $bb));
    }

    public static function PKBIdentifier()
    {
        return "$PKB";
    }

    public static function PKBBufferHasIdentifier(ByteBuffer $buf)
    {
        return self::__has_identifier($buf, self::PKBIdentifier());
    }

    /**
     * @param int $_i offset
     * @param ByteBuffer $_bb
     * @return PKB
     **/
    public function init($_i, ByteBuffer $_bb)
    {
        $this->bb_pos = $_i;
        $this->bb = $_bb;
        return $this;
    }

    /// Stable broker identity, referenced by `$PLG`/`$STO`/`$PUR`/`$LGR`
    public function getBROKER_ID()
    {
        $o = $this->__offset(4);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// IPFS CID of the publisher `$EPM` that operates this broker
    public function getPUBLISHER_EPM_CID()
    {
        $o = $this->__offset(6);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// libp2p peer id of the publisher node hosting the broker
    public function getPROVIDER_PEER_ID()
    {
        $o = $this->__offset(8);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// libp2p protocol the broker answers on
    /// (e.g. "/space-data-network/module-delivery/1.0.0")
    public function getPROTOCOL_ID()
    {
        $o = $this->__offset(10);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Dialable multiformat addresses for the broker
    /**
     * @param int offset
     * @return string
     */
    public function getMULTIFORMAT_ADDRESSES($j)
    {
        $o = $this->__offset(12);
        return $o != 0 ? $this->__string($this->__vector($o) + $j * 4) : 0;
    }

    /**
     * @return int
     */
    public function getMULTIFORMAT_ADDRESSESLength()
    {
        $o = $this->__offset(12);
        return $o != 0 ? $this->__vector_len($o) : 0;
    }

    /// The broker's public key (from the key server's get_public_key) so the
    /// buyer can pin it
    /**
     * @param int offset
     * @return byte
     */
    public function getBROKER_PUBLIC_KEY($j)
    {
        $o = $this->__offset(14);
        return $o != 0 ? $this->bb->getByte($this->__vector($o) + $j * 1) : 0;
    }

    /**
     * @return int
     */
    public function getBROKER_PUBLIC_KEYLength()
    {
        $o = $this->__offset(14);
        return $o != 0 ? $this->__vector_len($o) : 0;
    }

    /**
     * @return string
     */
    public function getBROKER_PUBLIC_KEYBytes()
    {
        return $this->__vector_as_bytes(14);
    }

    /// Custody model (default: full publisher custody)
    /**
     * @return sbyte
     */
    public function getBROKER_KIND()
    {
        $o = $this->__offset(16);
        return $o != 0 ? $this->bb->getSbyte($o + $this->bb_pos) : \brokerCustodyModel::SelfHosted;
    }

    /// Store-maintained liveness status
    /**
     * @return sbyte
     */
    public function getLIVENESS()
    {
        $o = $this->__offset(18);
        return $o != 0 ? $this->bb->getSbyte($o + $this->bb_pos) : \brokerLiveness::Unknown;
    }

    /// Unix ms of the last successful probe/heartbeat
    /**
     * @return ulong
     */
    public function getLAST_HEARTBEAT_AT()
    {
        $o = $this->__offset(20);
        return $o != 0 ? $this->bb->getUlong($o + $this->bb_pos) : 0;
    }

    /// Expected heartbeat cadence in ms
    /**
     * @return ulong
     */
    public function getHEARTBEAT_INTERVAL_MS()
    {
        $o = $this->__offset(22);
        return $o != 0 ? $this->bb->getUlong($o + $this->bb_pos) : 0;
    }

    /// Unix ms the store first saw this broker
    /**
     * @return ulong
     */
    public function getFIRST_SEEN_AT()
    {
        $o = $this->__offset(24);
        return $o != 0 ? $this->bb->getUlong($o + $this->bb_pos) : 0;
    }

    /// Unix ms this descriptor was last updated
    /**
     * @return ulong
     */
    public function getUPDATED_AT()
    {
        $o = $this->__offset(26);
        return $o != 0 ? $this->bb->getUlong($o + $this->bb_pos) : 0;
    }

    /// Ed25519 signature by the publisher `$EPM` over the self-attested endpoint
    /// fields; re-verified by the store's live probe
    /**
     * @param int offset
     * @return byte
     */
    public function getSIGNATURE($j)
    {
        $o = $this->__offset(28);
        return $o != 0 ? $this->bb->getByte($this->__vector($o) + $j * 1) : 0;
    }

    /**
     * @return int
     */
    public function getSIGNATURELength()
    {
        $o = $this->__offset(28);
        return $o != 0 ? $this->__vector_len($o) : 0;
    }

    /**
     * @return string
     */
    public function getSIGNATUREBytes()
    {
        return $this->__vector_as_bytes(28);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return void
     */
    public static function startPKB(FlatBufferBuilder $builder)
    {
        $builder->StartObject(13);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return PKB
     */
    public static function createPKB(FlatBufferBuilder $builder, $BROKER_ID, $PUBLISHER_EPM_CID, $PROVIDER_PEER_ID, $PROTOCOL_ID, $MULTIFORMAT_ADDRESSES, $BROKER_PUBLIC_KEY, $BROKER_KIND, $LIVENESS, $LAST_HEARTBEAT_AT, $HEARTBEAT_INTERVAL_MS, $FIRST_SEEN_AT, $UPDATED_AT, $SIGNATURE)
    {
        $builder->startObject(13);
        self::addBROKER_ID($builder, $BROKER_ID);
        self::addPUBLISHER_EPM_CID($builder, $PUBLISHER_EPM_CID);
        self::addPROVIDER_PEER_ID($builder, $PROVIDER_PEER_ID);
        self::addPROTOCOL_ID($builder, $PROTOCOL_ID);
        self::addMULTIFORMAT_ADDRESSES($builder, $MULTIFORMAT_ADDRESSES);
        self::addBROKER_PUBLIC_KEY($builder, $BROKER_PUBLIC_KEY);
        self::addBROKER_KIND($builder, $BROKER_KIND);
        self::addLIVENESS($builder, $LIVENESS);
        self::addLAST_HEARTBEAT_AT($builder, $LAST_HEARTBEAT_AT);
        self::addHEARTBEAT_INTERVAL_MS($builder, $HEARTBEAT_INTERVAL_MS);
        self::addFIRST_SEEN_AT($builder, $FIRST_SEEN_AT);
        self::addUPDATED_AT($builder, $UPDATED_AT);
        self::addSIGNATURE($builder, $SIGNATURE);
        $o = $builder->endObject();
        $builder->required($o, 4);  // BROKER_ID
        return $o;
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addBROKER_ID(FlatBufferBuilder $builder, $BROKER_ID)
    {
        $builder->addOffsetX(0, $BROKER_ID, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addPUBLISHER_EPM_CID(FlatBufferBuilder $builder, $PUBLISHER_EPM_CID)
    {
        $builder->addOffsetX(1, $PUBLISHER_EPM_CID, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addPROVIDER_PEER_ID(FlatBufferBuilder $builder, $PROVIDER_PEER_ID)
    {
        $builder->addOffsetX(2, $PROVIDER_PEER_ID, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addPROTOCOL_ID(FlatBufferBuilder $builder, $PROTOCOL_ID)
    {
        $builder->addOffsetX(3, $PROTOCOL_ID, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param VectorOffset
     * @return void
     */
    public static function addMULTIFORMAT_ADDRESSES(FlatBufferBuilder $builder, $MULTIFORMAT_ADDRESSES)
    {
        $builder->addOffsetX(4, $MULTIFORMAT_ADDRESSES, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param array offset array
     * @return int vector offset
     */
    public static function createMULTIFORMAT_ADDRESSESVector(FlatBufferBuilder $builder, array $data)
    {
        $builder->startVector(4, count($data), 4);
        for ($i = count($data) - 1; $i >= 0; $i--) {
            $builder->putOffset($data[$i]);
        }
        return $builder->endVector();
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param int $numElems
     * @return void
     */
    public static function startMULTIFORMAT_ADDRESSESVector(FlatBufferBuilder $builder, $numElems)
    {
        $builder->startVector(4, $numElems, 4);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param VectorOffset
     * @return void
     */
    public static function addBROKER_PUBLIC_KEY(FlatBufferBuilder $builder, $BROKER_PUBLIC_KEY)
    {
        $builder->addOffsetX(5, $BROKER_PUBLIC_KEY, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param array offset array
     * @return int vector offset
     */
    public static function createBROKER_PUBLIC_KEYVector(FlatBufferBuilder $builder, array $data)
    {
        $builder->startVector(1, count($data), 1);
        for ($i = count($data) - 1; $i >= 0; $i--) {
            $builder->putByte($data[$i]);
        }
        return $builder->endVector();
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param int $numElems
     * @return void
     */
    public static function startBROKER_PUBLIC_KEYVector(FlatBufferBuilder $builder, $numElems)
    {
        $builder->startVector(1, $numElems, 1);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param sbyte
     * @return void
     */
    public static function addBROKER_KIND(FlatBufferBuilder $builder, $BROKER_KIND)
    {
        $builder->addSbyteX(6, $BROKER_KIND, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param sbyte
     * @return void
     */
    public static function addLIVENESS(FlatBufferBuilder $builder, $LIVENESS)
    {
        $builder->addSbyteX(7, $LIVENESS, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param ulong
     * @return void
     */
    public static function addLAST_HEARTBEAT_AT(FlatBufferBuilder $builder, $LAST_HEARTBEAT_AT)
    {
        $builder->addUlongX(8, $LAST_HEARTBEAT_AT, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param ulong
     * @return void
     */
    public static function addHEARTBEAT_INTERVAL_MS(FlatBufferBuilder $builder, $HEARTBEAT_INTERVAL_MS)
    {
        $builder->addUlongX(9, $HEARTBEAT_INTERVAL_MS, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param ulong
     * @return void
     */
    public static function addFIRST_SEEN_AT(FlatBufferBuilder $builder, $FIRST_SEEN_AT)
    {
        $builder->addUlongX(10, $FIRST_SEEN_AT, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param ulong
     * @return void
     */
    public static function addUPDATED_AT(FlatBufferBuilder $builder, $UPDATED_AT)
    {
        $builder->addUlongX(11, $UPDATED_AT, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param VectorOffset
     * @return void
     */
    public static function addSIGNATURE(FlatBufferBuilder $builder, $SIGNATURE)
    {
        $builder->addOffsetX(12, $SIGNATURE, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param array offset array
     * @return int vector offset
     */
    public static function createSIGNATUREVector(FlatBufferBuilder $builder, array $data)
    {
        $builder->startVector(1, count($data), 1);
        for ($i = count($data) - 1; $i >= 0; $i--) {
            $builder->putByte($data[$i]);
        }
        return $builder->endVector();
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param int $numElems
     * @return void
     */
    public static function startSIGNATUREVector(FlatBufferBuilder $builder, $numElems)
    {
        $builder->startVector(1, $numElems, 1);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return int table offset
     */
    public static function endPKB(FlatBufferBuilder $builder)
    {
        $o = $builder->endObject();
        $builder->required($o, 4);  // BROKER_ID
        return $o;
    }

    public static function finishPKBBuffer(FlatBufferBuilder $builder, $offset)
    {
        $builder->finish($offset, "$PKB");
    }
}
