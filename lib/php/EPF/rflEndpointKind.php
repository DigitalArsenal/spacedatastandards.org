<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// What kind of thing an endpoint is, which selects the standard it joins to.
class rflEndpointKind
{
    const UNSPECIFIED = 0;
    /// On-orbit object; joins `$CAT` by NORAD_CAT_ID / OBJECT_ID.
    const SPACECRAFT = 1;
    /// Geodetic tracking station; joins `$GST` by STATION_ID.
    const GEODETIC_TRACKING_STATION = 2;
    /// Satellite ground station / teleport; joins `$SIT`.
    const SATELLITE_GROUND_STATION = 3;
    /// Terrestrial base-station site; joins `$TBS`.
    const TERRESTRIAL_SITE = 4;
    /// User terminal, fixed or mobile.
    const USER_TERMINAL = 5;
    const AIRBORNE = 6;
    const MARITIME = 7;
    /// A relay payload acting as an intermediate endpoint of a multi-hop chain.
    const RELAY_PAYLOAD = 8;

    private static $names = array(
        rflEndpointKind::UNSPECIFIED=>"UNSPECIFIED",
        rflEndpointKind::SPACECRAFT=>"SPACECRAFT",
        rflEndpointKind::GEODETIC_TRACKING_STATION=>"GEODETIC_TRACKING_STATION",
        rflEndpointKind::SATELLITE_GROUND_STATION=>"SATELLITE_GROUND_STATION",
        rflEndpointKind::TERRESTRIAL_SITE=>"TERRESTRIAL_SITE",
        rflEndpointKind::USER_TERMINAL=>"USER_TERMINAL",
        rflEndpointKind::AIRBORNE=>"AIRBORNE",
        rflEndpointKind::MARITIME=>"MARITIME",
        rflEndpointKind::RELAY_PAYLOAD=>"RELAY_PAYLOAD",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
