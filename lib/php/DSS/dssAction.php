<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Action a client requests on a lane. Append new values only; never reorder
/// or reuse existing values.
class dssAction
{
    const None = 0;
    /// Run one bounded catch-up pass now.
    const Sync = 1;
    const Subscribe = 2;
    const Unsubscribe = 3;
    const Pin = 4;
    const Unpin = 5;
    /// Rebuild the local materialisation from pinned publications.
    const Hydrate = 6;

    private static $names = array(
        dssAction::None=>"None",
        dssAction::Sync=>"Sync",
        dssAction::Subscribe=>"Subscribe",
        dssAction::Unsubscribe=>"Unsubscribe",
        dssAction::Pin=>"Pin",
        dssAction::Unpin=>"Unpin",
        dssAction::Hydrate=>"Hydrate",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
