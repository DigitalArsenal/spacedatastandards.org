<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Retention policy for a lane on this node. Append new values only; never
/// reorder or reuse existing values.
class dssPinPolicy
{
    const None = 0;
    /// Keep a bounded local cache.
    const Cache = 1;
    /// Pin every publication.
    const Pin = 2;
    /// Pin and archive every publication.
    const Archive = 3;

    private static $names = array(
        dssPinPolicy::None=>"None",
        dssPinPolicy::Cache=>"Cache",
        dssPinPolicy::Pin=>"Pin",
        dssPinPolicy::Archive=>"Archive",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
