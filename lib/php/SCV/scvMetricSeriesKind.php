<?php
// automatically generated by the FlatBuffers compiler, do not modify

class scvMetricSeriesKind
{
    const PERCENT_COVERED = 0;
    const COVERED_CELL_COUNT = 1;
    const ACCESS_COUNT = 2;
    const CONTACT_DURATION_SECONDS = 3;
    const REVISIT_SECONDS = 4;
    const GAP_SECONDS = 5;
    const REDUNDANCY = 6;
    const OVERLAP_COUNT = 7;
    const LATITUDE_BAND_COVERAGE = 8;

    private static $names = array(
        scvMetricSeriesKind::PERCENT_COVERED=>"PERCENT_COVERED",
        scvMetricSeriesKind::COVERED_CELL_COUNT=>"COVERED_CELL_COUNT",
        scvMetricSeriesKind::ACCESS_COUNT=>"ACCESS_COUNT",
        scvMetricSeriesKind::CONTACT_DURATION_SECONDS=>"CONTACT_DURATION_SECONDS",
        scvMetricSeriesKind::REVISIT_SECONDS=>"REVISIT_SECONDS",
        scvMetricSeriesKind::GAP_SECONDS=>"GAP_SECONDS",
        scvMetricSeriesKind::REDUNDANCY=>"REDUNDANCY",
        scvMetricSeriesKind::OVERLAP_COUNT=>"OVERLAP_COUNT",
        scvMetricSeriesKind::LATITUDE_BAND_COVERAGE=>"LATITUDE_BAND_COVERAGE",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
