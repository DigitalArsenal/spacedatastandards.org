<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Constructive solid-geometry composition operation for SDFs.
class sdfOperation
{
    /// No composition (primitive only).
    const NONE = 0;
    /// Union (min of SDFs) — inside either.
    const UNION = 1;
    /// Intersection (max of SDFs) — inside both.
    const INTERSECTION = 2;
    /// Subtraction (A - B) — inside A but not B.
    const SUBTRACTION = 3;
    /// Smooth union with a blend radius.
    const SMOOTH_UNION = 4;

    private static $names = array(
        sdfOperation::NONE=>"NONE",
        sdfOperation::UNION=>"UNION",
        sdfOperation::INTERSECTION=>"INTERSECTION",
        sdfOperation::SUBTRACTION=>"SUBTRACTION",
        sdfOperation::SMOOTH_UNION=>"SMOOTH_UNION",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
