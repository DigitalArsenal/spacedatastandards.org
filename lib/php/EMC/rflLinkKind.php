<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Direction of a link relative to its endpoints.
///
/// This enum deliberately mirrors `$LKS.linkCategory` member-for-member with
/// an added `UNSPECIFIED` at ordinal 0. `linkCategory` places `UPLINK` at
/// ordinal 0, so an unset field there silently decodes as a real direction;
/// a durable analytical record may not carry that hazard.
class rflLinkKind
{
    const UNSPECIFIED = 0;
    const UPLINK = 1;
    const DOWNLINK = 2;
    const CROSSLINK = 3;
    const INTER_SATELLITE = 4;
    const GROUND_TO_GROUND = 5;
    const RELAY = 6;
    /// Service link to an end-user terminal.
    const USER_BEAM = 7;
    /// Gateway link carrying aggregated traffic to the network core.
    const FEEDER = 8;

    private static $names = array(
        rflLinkKind::UNSPECIFIED=>"UNSPECIFIED",
        rflLinkKind::UPLINK=>"UPLINK",
        rflLinkKind::DOWNLINK=>"DOWNLINK",
        rflLinkKind::CROSSLINK=>"CROSSLINK",
        rflLinkKind::INTER_SATELLITE=>"INTER_SATELLITE",
        rflLinkKind::GROUND_TO_GROUND=>"GROUND_TO_GROUND",
        rflLinkKind::RELAY=>"RELAY",
        rflLinkKind::USER_BEAM=>"USER_BEAM",
        rflLinkKind::FEEDER=>"FEEDER",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
