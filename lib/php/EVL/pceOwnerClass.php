<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Which kind of object owns a parameter. A parameter is meaningless without
/// its owner: the same symbol evaluated on a space object and on a thruster
/// are different quantities. Append new values only; never reorder or reuse
/// existing values.
class pceOwnerClass
{
    const UNSPECIFIED = 0;
    /// A tracked object with an orbital state.
    const SPACE_OBJECT = 1;
    /// A point with an ephemeris but no vehicle properties: a celestial body,
    /// a barycentre, or a libration point.
    const SPACE_POINT = 2;
    /// A fixed site on a body surface.
    const GROUND_SITE = 3;
    /// A thrust-producing device carried by a space object.
    const THRUSTER = 4;
    /// A propellant reservoir carried by a space object.
    const FUEL_TANK = 5;
    /// An instantaneous velocity change.
    const IMPULSIVE_BURN = 6;
    /// A thrust arc of finite duration.
    const FINITE_BURN = 7;
    /// An electrical power source and its bus.
    const POWER_SYSTEM = 8;
    /// The orientation state of a space object.
    const ATTITUDE_SYSTEM = 9;
    /// A differential-corrector or optimizer instance.
    const SOLVER = 10;
    /// A set of space objects propagated together.
    const OBJECT_GROUP = 11;

    private static $names = array(
        pceOwnerClass::UNSPECIFIED=>"UNSPECIFIED",
        pceOwnerClass::SPACE_OBJECT=>"SPACE_OBJECT",
        pceOwnerClass::SPACE_POINT=>"SPACE_POINT",
        pceOwnerClass::GROUND_SITE=>"GROUND_SITE",
        pceOwnerClass::THRUSTER=>"THRUSTER",
        pceOwnerClass::FUEL_TANK=>"FUEL_TANK",
        pceOwnerClass::IMPULSIVE_BURN=>"IMPULSIVE_BURN",
        pceOwnerClass::FINITE_BURN=>"FINITE_BURN",
        pceOwnerClass::POWER_SYSTEM=>"POWER_SYSTEM",
        pceOwnerClass::ATTITUDE_SYSTEM=>"ATTITUDE_SYSTEM",
        pceOwnerClass::SOLVER=>"SOLVER",
        pceOwnerClass::OBJECT_GROUP=>"OBJECT_GROUP",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
