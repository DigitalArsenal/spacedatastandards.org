<?php
// automatically generated by the FlatBuffers compiler, do not modify

use \Google\FlatBuffers\Struct;
use \Google\FlatBuffers\Table;
use \Google\FlatBuffers\ByteBuffer;
use \Google\FlatBuffers\FlatBufferBuilder;

/// The publisher's attestation over this profile, in BOTH ratified forms.
///
/// PRESENCE IS THE STATEMENT. A profile is SIGNED if and only if this table
/// is present; a profile whose `ATTESTATION` is absent is UNSIGNED, and a
/// consumer marks it as such. There is deliberately no boolean saying so: a
/// self-asserted "signed" flag is attacker-controlled and proves nothing,
/// while presence of the table is checkable.
///
/// FAIL CLOSED. When this table is present, every field below MUST be
/// present and BOTH signatures MUST verify independently against
/// `SIGNING_PUBLIC_KEY`. A profile whose attestation is incomplete, or
/// whose either signature fails, is REJECTED — it is NEVER downgraded to
/// "unsigned and therefore acceptable", which would let stripping one
/// signature launder a tampered profile past an importer that admits
/// unsigned input.
class BPFAttestation extends Table
{
    /**
     * @param ByteBuffer $bb
     * @return BPFAttestation
     */
    public static function getRootAsBPFAttestation(ByteBuffer $bb)
    {
        $obj = new BPFAttestation();
        return ($obj->init($bb->getInt($bb->getPosition()) + $bb->getPosition(), $bb));
    }

    public static function BPFAttestationIdentifier()
    {
        return "$BPF";
    }

    public static function BPFAttestationBufferHasIdentifier(ByteBuffer $buf)
    {
        return self::__has_identifier($buf, self::BPFAttestationIdentifier());
    }

    /**
     * @param int $_i offset
     * @param ByteBuffer $_bb
     * @return BPFAttestation
     **/
    public function init($_i, ByteBuffer $_bb)
    {
        $this->bb_pos = $_i;
        $this->bb = $_bb;
        return $this;
    }

    /// The signer's LITERAL Ed25519 public key, 64 lowercase hexadecimal
    /// characters. A derivation ancestor is structurally absent from this
    /// standard: publishing one lets any holder of a child key walk the
    /// signer's whole key tree. Rotation is republication.
    public function getSIGNING_PUBLIC_KEY()
    {
        $o = $this->__offset(4);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
    /// at which the attestation was produced.
    public function getSIGNED_AT()
    {
        $o = $this->__offset(6);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Ed25519 signature over the size-prefixed FlatBuffer with both 64-byte
    /// signature payloads zeroed while preserving their vectors and offsets.
    /**
     * @param int offset
     * @return byte
     */
    public function getSIGNATURE($j)
    {
        $o = $this->__offset(8);
        return $o != 0 ? $this->bb->getByte($this->__vector($o) + $j * 1) : 0;
    }

    /**
     * @return int
     */
    public function getSIGNATURELength()
    {
        $o = $this->__offset(8);
        return $o != 0 ? $this->__vector_len($o) : 0;
    }

    /**
     * @return string
     */
    public function getSIGNATUREBytes()
    {
        return $this->__vector_as_bytes(8);
    }

    /// Ed25519 signature over canonical JSON with IDL field order and
    /// capitalization, no insignificant whitespace, and both signature fields
    /// omitted.
    /**
     * @param int offset
     * @return byte
     */
    public function getCANONICAL_JSON_SIGNATURE($j)
    {
        $o = $this->__offset(10);
        return $o != 0 ? $this->bb->getByte($this->__vector($o) + $j * 1) : 0;
    }

    /**
     * @return int
     */
    public function getCANONICAL_JSON_SIGNATURELength()
    {
        $o = $this->__offset(10);
        return $o != 0 ? $this->__vector_len($o) : 0;
    }

    /**
     * @return string
     */
    public function getCANONICAL_JSON_SIGNATUREBytes()
    {
        return $this->__vector_as_bytes(10);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return void
     */
    public static function startBPFAttestation(FlatBufferBuilder $builder)
    {
        $builder->StartObject(4);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return BPFAttestation
     */
    public static function createBPFAttestation(FlatBufferBuilder $builder, $SIGNING_PUBLIC_KEY, $SIGNED_AT, $SIGNATURE, $CANONICAL_JSON_SIGNATURE)
    {
        $builder->startObject(4);
        self::addSIGNING_PUBLIC_KEY($builder, $SIGNING_PUBLIC_KEY);
        self::addSIGNED_AT($builder, $SIGNED_AT);
        self::addSIGNATURE($builder, $SIGNATURE);
        self::addCANONICAL_JSON_SIGNATURE($builder, $CANONICAL_JSON_SIGNATURE);
        $o = $builder->endObject();
        $builder->required($o, 4);  // SIGNING_PUBLIC_KEY
        $builder->required($o, 8);  // SIGNATURE
        $builder->required($o, 10);  // CANONICAL_JSON_SIGNATURE
        return $o;
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addSIGNING_PUBLIC_KEY(FlatBufferBuilder $builder, $SIGNING_PUBLIC_KEY)
    {
        $builder->addOffsetX(0, $SIGNING_PUBLIC_KEY, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addSIGNED_AT(FlatBufferBuilder $builder, $SIGNED_AT)
    {
        $builder->addOffsetX(1, $SIGNED_AT, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param VectorOffset
     * @return void
     */
    public static function addSIGNATURE(FlatBufferBuilder $builder, $SIGNATURE)
    {
        $builder->addOffsetX(2, $SIGNATURE, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param array offset array
     * @return int vector offset
     */
    public static function createSIGNATUREVector(FlatBufferBuilder $builder, array $data)
    {
        $builder->startVector(1, count($data), 1);
        for ($i = count($data) - 1; $i >= 0; $i--) {
            $builder->putByte($data[$i]);
        }
        return $builder->endVector();
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param int $numElems
     * @return void
     */
    public static function startSIGNATUREVector(FlatBufferBuilder $builder, $numElems)
    {
        $builder->startVector(1, $numElems, 1);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param VectorOffset
     * @return void
     */
    public static function addCANONICAL_JSON_SIGNATURE(FlatBufferBuilder $builder, $CANONICAL_JSON_SIGNATURE)
    {
        $builder->addOffsetX(3, $CANONICAL_JSON_SIGNATURE, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param array offset array
     * @return int vector offset
     */
    public static function createCANONICAL_JSON_SIGNATUREVector(FlatBufferBuilder $builder, array $data)
    {
        $builder->startVector(1, count($data), 1);
        for ($i = count($data) - 1; $i >= 0; $i--) {
            $builder->putByte($data[$i]);
        }
        return $builder->endVector();
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param int $numElems
     * @return void
     */
    public static function startCANONICAL_JSON_SIGNATUREVector(FlatBufferBuilder $builder, $numElems)
    {
        $builder->startVector(1, $numElems, 1);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return int table offset
     */
    public static function endBPFAttestation(FlatBufferBuilder $builder)
    {
        $o = $builder->endObject();
        $builder->required($o, 4);  // SIGNING_PUBLIC_KEY
        $builder->required($o, 8);  // SIGNATURE
        $builder->required($o, 10);  // CANONICAL_JSON_SIGNATURE
        return $o;
    }
}
