<?php
// automatically generated by the FlatBuffers compiler, do not modify

use \Google\FlatBuffers\Struct;
use \Google\FlatBuffers\Table;
use \Google\FlatBuffers\ByteBuffer;
use \Google\FlatBuffers\FlatBufferBuilder;

/// Completeness-capable signed index over a dataset update.
class DPMCompletenessIndex extends Table
{
    /**
     * @param ByteBuffer $bb
     * @return DPMCompletenessIndex
     */
    public static function getRootAsDPMCompletenessIndex(ByteBuffer $bb)
    {
        $obj = new DPMCompletenessIndex();
        return ($obj->init($bb->getInt($bb->getPosition()) + $bb->getPosition(), $bb));
    }

    public static function DPMCompletenessIndexIdentifier()
    {
        return "$DPM";
    }

    public static function DPMCompletenessIndexBufferHasIdentifier(ByteBuffer $buf)
    {
        return self::__has_identifier($buf, self::DPMCompletenessIndexIdentifier());
    }

    /**
     * @param int $_i offset
     * @param ByteBuffer $_bb
     * @return DPMCompletenessIndex
     **/
    public function init($_i, ByteBuffer $_bb)
    {
        $this->bb_pos = $_i;
        $this->bb = $_bb;
        return $this;
    }

    /// Stable index name, e.g. file_id, norad_cat_id, epoch, source_batch.
    public function getINDEX_NAME()
    {
        $o = $this->__offset(4);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Deterministic ordering expression for the index. Providers and
    /// subscribers MUST use this ordering when building or verifying range
    /// proofs. A query is completeness-verifiable only when its predicate can be
    /// expressed against one or more declared indexes.
    public function getCANONICAL_ORDER()
    {
        $o = $this->__offset(6);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// SHA-256 or Merkle root of the ordered index, lowercase hex. This root is
    /// signed by the DPM provider signature and is the verifier's commitment for
    /// inclusion and range-completeness proofs.
    public function getINDEX_ROOT()
    {
        $o = $this->__offset(8);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Hash profile for leaves and internal nodes, e.g.
    /// SDN-MERKLE-SHA256-v1. Profiles define domain separators, leaf material,
    /// pair ordering, duplicate handling, and odd-leaf promotion.
    public function getMERKLE_PROFILE()
    {
        $o = $this->__offset(10);
        return $o != 0 ? $this->__string($o + $this->bb_pos) : null;
    }

    /// Whether this index can prove that no matching records were omitted for a
    /// supported range query. Inclusion proofs alone prove authenticity, not
    /// completeness.
    /**
     * @return bool
     */
    public function getSUPPORTS_RANGE_COMPLETENESS()
    {
        $o = $this->__offset(12);
        return $o != 0 ? $this->bb->getBool($o + $this->bb_pos) : false;
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return void
     */
    public static function startDPMCompletenessIndex(FlatBufferBuilder $builder)
    {
        $builder->StartObject(5);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return DPMCompletenessIndex
     */
    public static function createDPMCompletenessIndex(FlatBufferBuilder $builder, $INDEX_NAME, $CANONICAL_ORDER, $INDEX_ROOT, $MERKLE_PROFILE, $SUPPORTS_RANGE_COMPLETENESS)
    {
        $builder->startObject(5);
        self::addINDEX_NAME($builder, $INDEX_NAME);
        self::addCANONICAL_ORDER($builder, $CANONICAL_ORDER);
        self::addINDEX_ROOT($builder, $INDEX_ROOT);
        self::addMERKLE_PROFILE($builder, $MERKLE_PROFILE);
        self::addSUPPORTS_RANGE_COMPLETENESS($builder, $SUPPORTS_RANGE_COMPLETENESS);
        $o = $builder->endObject();
        return $o;
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addINDEX_NAME(FlatBufferBuilder $builder, $INDEX_NAME)
    {
        $builder->addOffsetX(0, $INDEX_NAME, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addCANONICAL_ORDER(FlatBufferBuilder $builder, $CANONICAL_ORDER)
    {
        $builder->addOffsetX(1, $CANONICAL_ORDER, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addINDEX_ROOT(FlatBufferBuilder $builder, $INDEX_ROOT)
    {
        $builder->addOffsetX(2, $INDEX_ROOT, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param StringOffset
     * @return void
     */
    public static function addMERKLE_PROFILE(FlatBufferBuilder $builder, $MERKLE_PROFILE)
    {
        $builder->addOffsetX(3, $MERKLE_PROFILE, 0);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @param bool
     * @return void
     */
    public static function addSUPPORTS_RANGE_COMPLETENESS(FlatBufferBuilder $builder, $SUPPORTS_RANGE_COMPLETENESS)
    {
        $builder->addBoolX(4, $SUPPORTS_RANGE_COMPLETENESS, false);
    }

    /**
     * @param FlatBufferBuilder $builder
     * @return int table offset
     */
    public static function endDPMCompletenessIndex(FlatBufferBuilder $builder)
    {
        $o = $builder->endObject();
        return $o;
    }
}
