<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// On-chain settlement mechanism a buyer's payment targets.
class settlementMechanism
{
    /// Native-asset transfer to a plain address (no split contract)
    const NativeTransfer = 0;
    /// ERC-20 / SPL token transfer to a plain address
    const Erc20Transfer = 1;
    /// 0xSplits-style splitter contract on an EVM chain
    const EthSplitter = 2;
    /// Splitter program on Solana
    const SolSplitter = 3;
    /// Direct address payout (e.g. Bitcoin); split settled off-contract
    const DirectAddress = 4;

    private static $names = array(
        settlementMechanism::NativeTransfer=>"NativeTransfer",
        settlementMechanism::Erc20Transfer=>"Erc20Transfer",
        settlementMechanism::EthSplitter=>"EthSplitter",
        settlementMechanism::SolSplitter=>"SolSplitter",
        settlementMechanism::DirectAddress=>"DirectAddress",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
