<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// How the purchase reference (`$PUR.REQUEST_ID`) is bound on-chain so a
/// payment can be matched to an order replay-safely.
class memoBindingScheme
{
    /// No on-chain memo binding
    const NoMemo = 0;
    /// Bound in EVM transaction calldata
    const TxData = 1;
    /// Solana memo program instruction
    const SolMemo = 2;
    /// Bitcoin OP_RETURN output
    const OpReturn = 3;

    private static $names = array(
        memoBindingScheme::NoMemo=>"NoMemo",
        memoBindingScheme::TxData=>"TxData",
        memoBindingScheme::SolMemo=>"SolMemo",
        memoBindingScheme::OpReturn=>"OpReturn",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
