<?php
// automatically generated by the FlatBuffers compiler, do not modify

/// Statistical reduction a single number expresses.
///
/// Named `cnpReduction`, not `cnpStatistic`, so it can never differ from the
/// `CNPStatistic` table by case alone in any generated language tree.
class cnpReduction
{
    const MEAN = 0;
    const MEDIAN = 1;
    /// PERCENTILE_RANK carries which one.
    const PERCENTILE = 2;
    const MINIMUM = 3;
    const MAXIMUM = 4;
    const STANDARD_DEVIATION = 5;
    /// Number of observations, not a value of the metric.
    const COUNT = 6;

    private static $names = array(
        cnpReduction::MEAN=>"MEAN",
        cnpReduction::MEDIAN=>"MEDIAN",
        cnpReduction::PERCENTILE=>"PERCENTILE",
        cnpReduction::MINIMUM=>"MINIMUM",
        cnpReduction::MAXIMUM=>"MAXIMUM",
        cnpReduction::STANDARD_DEVIATION=>"STANDARD_DEVIATION",
        cnpReduction::COUNT=>"COUNT",
    );

    public static function Name($e)
    {
        if (!isset(self::$names[$e])) {
            throw new \Exception();
        }
        return self::$names[$e];
    }
}
