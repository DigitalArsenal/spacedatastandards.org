// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { PRWResidentState, PRWResidentStateT } from './PRWResidentState.js';


export class PRWEphemerisResult implements flatbuffers.IUnpackableObject<PRWEphemerisResultT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PRWEphemerisResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPRWEphemerisResult(bb:flatbuffers.ByteBuffer, obj?:PRWEphemerisResult):PRWEphemerisResult {
  return (obj || new PRWEphemerisResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPRWEphemerisResult(bb:flatbuffers.ByteBuffer, obj?:PRWEphemerisResult):PRWEphemerisResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PRWEphemerisResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

TARGET_NAIF_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

CENTER_NAIF_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

/**
 * Explicit ICRF axes and named NAIF origin; FRM state in SI m/m/s.
 */
STATE(obj?:PRWResidentState):PRWResidentState|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new PRWResidentState()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

EPHEMERIS_SOURCE():string
EPHEMERIS_SOURCE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
EPHEMERIS_SOURCE(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

static startPRWEphemerisResult(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addTargetNaifId(builder:flatbuffers.Builder, TARGET_NAIF_ID:number) {
  builder.addFieldInt32(0, TARGET_NAIF_ID, 0);
}

static addCenterNaifId(builder:flatbuffers.Builder, CENTER_NAIF_ID:number) {
  builder.addFieldInt32(1, CENTER_NAIF_ID, 0);
}

static addState(builder:flatbuffers.Builder, STATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, STATEOffset, 0);
}

static addEphemerisSource(builder:flatbuffers.Builder, EPHEMERIS_SOURCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, EPHEMERIS_SOURCEOffset, 0);
}

static endPRWEphemerisResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 8) // STATE
  builder.requiredField(offset, 10) // EPHEMERIS_SOURCE
  return offset;
}


unpack(): PRWEphemerisResultT {
  return new PRWEphemerisResultT(
    this.TARGET_NAIF_ID(),
    this.CENTER_NAIF_ID(),
    (this.STATE() !== null ? this.STATE()!.unpack() : null),
    this.EPHEMERIS_SOURCE()
  );
}


unpackTo(_o: PRWEphemerisResultT): void {
  _o.TARGET_NAIF_ID = this.TARGET_NAIF_ID();
  _o.CENTER_NAIF_ID = this.CENTER_NAIF_ID();
  _o.STATE = (this.STATE() !== null ? this.STATE()!.unpack() : null);
  _o.EPHEMERIS_SOURCE = this.EPHEMERIS_SOURCE();
}
}

export class PRWEphemerisResultT implements flatbuffers.IGeneratedObject {
constructor(
  public TARGET_NAIF_ID: number = 0,
  public CENTER_NAIF_ID: number = 0,
  public STATE: PRWResidentStateT|null = null,
  public EPHEMERIS_SOURCE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const STATE = (this.STATE !== null ? this.STATE!.pack(builder) : 0);
  const EPHEMERIS_SOURCE = (this.EPHEMERIS_SOURCE !== null ? builder.createString(this.EPHEMERIS_SOURCE!) : 0);

  PRWEphemerisResult.startPRWEphemerisResult(builder);
  PRWEphemerisResult.addTargetNaifId(builder, this.TARGET_NAIF_ID);
  PRWEphemerisResult.addCenterNaifId(builder, this.CENTER_NAIF_ID);
  PRWEphemerisResult.addState(builder, STATE);
  PRWEphemerisResult.addEphemerisSource(builder, EPHEMERIS_SOURCE);

  return PRWEphemerisResult.endPRWEphemerisResult(builder);
}
}
