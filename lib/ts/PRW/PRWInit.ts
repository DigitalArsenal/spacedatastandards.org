// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { PRWInitSource, PRWInitSourceT } from './PRWInitSource.js';


/**
 * Propagator initialization request — assigns TLE / OMM / Keplerian /
 * polynomial inputs to entity handles.
 *
 * The actual per-entity source record is carried inline as raw bytes
 * tagged with SOURCE_KIND and, where useful, a SDS file_identifier.
 * Callers encode `OMM`, `OCM`, or a one-off Keplerian set and pass the
 * bytes verbatim; the propagator uses SOURCE_KIND to decide how to
 * consume them.
 */
export class PRWInit implements flatbuffers.IUnpackableObject<PRWInitT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PRWInit {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPRWInit(bb:flatbuffers.ByteBuffer, obj?:PRWInit):PRWInit {
  return (obj || new PRWInit()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPRWInit(bb:flatbuffers.ByteBuffer, obj?:PRWInit):PRWInit {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PRWInit()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Entity handles to assign results to (same order as SOURCES[]).
 */
ENTITY_HANDLES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

entityHandlesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

entityHandlesArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Per-entity source records, encoded as SDS FlatBuffers.
 */
SOURCES(index: number, obj?:PRWInitSource):PRWInitSource|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new PRWInitSource()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

sourcesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startPRWInit(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addEntityHandles(builder:flatbuffers.Builder, ENTITY_HANDLESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, ENTITY_HANDLESOffset, 0);
}

static createEntityHandlesVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createEntityHandlesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createEntityHandlesVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startEntityHandlesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSources(builder:flatbuffers.Builder, SOURCESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, SOURCESOffset, 0);
}

static createSourcesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSourcesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endPRWInit(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPRWInit(builder:flatbuffers.Builder, ENTITY_HANDLESOffset:flatbuffers.Offset, SOURCESOffset:flatbuffers.Offset):flatbuffers.Offset {
  PRWInit.startPRWInit(builder);
  PRWInit.addEntityHandles(builder, ENTITY_HANDLESOffset);
  PRWInit.addSources(builder, SOURCESOffset);
  return PRWInit.endPRWInit(builder);
}

unpack(): PRWInitT {
  return new PRWInitT(
    this.bb!.createScalarList<number>(this.ENTITY_HANDLES.bind(this), this.entityHandlesLength()),
    this.bb!.createObjList<PRWInitSource, PRWInitSourceT>(this.SOURCES.bind(this), this.sourcesLength())
  );
}


unpackTo(_o: PRWInitT): void {
  _o.ENTITY_HANDLES = this.bb!.createScalarList<number>(this.ENTITY_HANDLES.bind(this), this.entityHandlesLength());
  _o.SOURCES = this.bb!.createObjList<PRWInitSource, PRWInitSourceT>(this.SOURCES.bind(this), this.sourcesLength());
}
}

export class PRWInitT implements flatbuffers.IGeneratedObject {
constructor(
  public ENTITY_HANDLES: (number)[] = [],
  public SOURCES: (PRWInitSourceT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ENTITY_HANDLES = PRWInit.createEntityHandlesVector(builder, this.ENTITY_HANDLES);
  const SOURCES = PRWInit.createSourcesVector(builder, builder.createObjectOffsetList(this.SOURCES));

  return PRWInit.createPRWInit(builder,
    ENTITY_HANDLES,
    SOURCES
  );
}
}
