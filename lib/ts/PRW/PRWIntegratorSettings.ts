// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { prwSolverAlgorithm } from './prwSolverAlgorithm.js';


/**
 * Units are SI. A scalar legacy tolerance must be expanded by the adapter.
 */
export class PRWIntegratorSettings implements flatbuffers.IUnpackableObject<PRWIntegratorSettingsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PRWIntegratorSettings {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPRWIntegratorSettings(bb:flatbuffers.ByteBuffer, obj?:PRWIntegratorSettings):PRWIntegratorSettings {
  return (obj || new PRWIntegratorSettings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPRWIntegratorSettings(bb:flatbuffers.ByteBuffer, obj?:PRWIntegratorSettings):PRWIntegratorSettings {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PRWIntegratorSettings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

ALGORITHM():prwSolverAlgorithm {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : prwSolverAlgorithm.UNSPECIFIED;
}

INITIAL_STEP_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 60.0;
}

MINIMUM_STEP_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 1.0;
}

MAXIMUM_STEP_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 3600.0;
}

/**
 * Six or seven positive tolerances, respectively m, m/s and optional kg.
 */
ABSOLUTE_TOLERANCES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

absoluteTolerancesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

absoluteTolerancesArray():Float64Array {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

RELATIVE_TOLERANCE():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 1e-12;
}

MAXIMUM_STEPS():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 100000;
}

static startPRWIntegratorSettings(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHM:prwSolverAlgorithm) {
  builder.addFieldInt8(0, ALGORITHM, prwSolverAlgorithm.UNSPECIFIED);
}

static addInitialStepSeconds(builder:flatbuffers.Builder, INITIAL_STEP_SECONDS:number) {
  builder.addFieldFloat64(1, INITIAL_STEP_SECONDS, 60.0);
}

static addMinimumStepSeconds(builder:flatbuffers.Builder, MINIMUM_STEP_SECONDS:number) {
  builder.addFieldFloat64(2, MINIMUM_STEP_SECONDS, 1.0);
}

static addMaximumStepSeconds(builder:flatbuffers.Builder, MAXIMUM_STEP_SECONDS:number) {
  builder.addFieldFloat64(3, MAXIMUM_STEP_SECONDS, 3600.0);
}

static addAbsoluteTolerances(builder:flatbuffers.Builder, ABSOLUTE_TOLERANCESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, ABSOLUTE_TOLERANCESOffset, 0);
}

static createAbsoluteTolerancesVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createAbsoluteTolerancesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createAbsoluteTolerancesVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startAbsoluteTolerancesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addRelativeTolerance(builder:flatbuffers.Builder, RELATIVE_TOLERANCE:number) {
  builder.addFieldFloat64(5, RELATIVE_TOLERANCE, 1e-12);
}

static addMaximumSteps(builder:flatbuffers.Builder, MAXIMUM_STEPS:number) {
  builder.addFieldInt32(6, MAXIMUM_STEPS, 100000);
}

static endPRWIntegratorSettings(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 12) // ABSOLUTE_TOLERANCES
  return offset;
}

static createPRWIntegratorSettings(builder:flatbuffers.Builder, ALGORITHM:prwSolverAlgorithm, INITIAL_STEP_SECONDS:number, MINIMUM_STEP_SECONDS:number, MAXIMUM_STEP_SECONDS:number, ABSOLUTE_TOLERANCESOffset:flatbuffers.Offset, RELATIVE_TOLERANCE:number, MAXIMUM_STEPS:number):flatbuffers.Offset {
  PRWIntegratorSettings.startPRWIntegratorSettings(builder);
  PRWIntegratorSettings.addAlgorithm(builder, ALGORITHM);
  PRWIntegratorSettings.addInitialStepSeconds(builder, INITIAL_STEP_SECONDS);
  PRWIntegratorSettings.addMinimumStepSeconds(builder, MINIMUM_STEP_SECONDS);
  PRWIntegratorSettings.addMaximumStepSeconds(builder, MAXIMUM_STEP_SECONDS);
  PRWIntegratorSettings.addAbsoluteTolerances(builder, ABSOLUTE_TOLERANCESOffset);
  PRWIntegratorSettings.addRelativeTolerance(builder, RELATIVE_TOLERANCE);
  PRWIntegratorSettings.addMaximumSteps(builder, MAXIMUM_STEPS);
  return PRWIntegratorSettings.endPRWIntegratorSettings(builder);
}

unpack(): PRWIntegratorSettingsT {
  return new PRWIntegratorSettingsT(
    this.ALGORITHM(),
    this.INITIAL_STEP_SECONDS(),
    this.MINIMUM_STEP_SECONDS(),
    this.MAXIMUM_STEP_SECONDS(),
    this.bb!.createScalarList<number>(this.ABSOLUTE_TOLERANCES.bind(this), this.absoluteTolerancesLength()),
    this.RELATIVE_TOLERANCE(),
    this.MAXIMUM_STEPS()
  );
}


unpackTo(_o: PRWIntegratorSettingsT): void {
  _o.ALGORITHM = this.ALGORITHM();
  _o.INITIAL_STEP_SECONDS = this.INITIAL_STEP_SECONDS();
  _o.MINIMUM_STEP_SECONDS = this.MINIMUM_STEP_SECONDS();
  _o.MAXIMUM_STEP_SECONDS = this.MAXIMUM_STEP_SECONDS();
  _o.ABSOLUTE_TOLERANCES = this.bb!.createScalarList<number>(this.ABSOLUTE_TOLERANCES.bind(this), this.absoluteTolerancesLength());
  _o.RELATIVE_TOLERANCE = this.RELATIVE_TOLERANCE();
  _o.MAXIMUM_STEPS = this.MAXIMUM_STEPS();
}
}

export class PRWIntegratorSettingsT implements flatbuffers.IGeneratedObject {
constructor(
  public ALGORITHM: prwSolverAlgorithm = prwSolverAlgorithm.UNSPECIFIED,
  public INITIAL_STEP_SECONDS: number = 60.0,
  public MINIMUM_STEP_SECONDS: number = 1.0,
  public MAXIMUM_STEP_SECONDS: number = 3600.0,
  public ABSOLUTE_TOLERANCES: (number)[] = [],
  public RELATIVE_TOLERANCE: number = 1e-12,
  public MAXIMUM_STEPS: number = 100000
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ABSOLUTE_TOLERANCES = PRWIntegratorSettings.createAbsoluteTolerancesVector(builder, this.ABSOLUTE_TOLERANCES);

  return PRWIntegratorSettings.createPRWIntegratorSettings(builder,
    this.ALGORITHM,
    this.INITIAL_STEP_SECONDS,
    this.MINIMUM_STEP_SECONDS,
    this.MAXIMUM_STEP_SECONDS,
    ABSOLUTE_TOLERANCES,
    this.RELATIVE_TOLERANCE,
    this.MAXIMUM_STEPS
  );
}
}
