// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { PRWKeplerianElements, PRWKeplerianElementsT } from './PRWKeplerianElements.js';
import { PRWTleLines, PRWTleLinesT } from './PRWTleLines.js';
import { prwSourceKind } from './prwSourceKind.js';


export class PRWInitSource implements flatbuffers.IUnpackableObject<PRWInitSourceT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PRWInitSource {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPRWInitSource(bb:flatbuffers.ByteBuffer, obj?:PRWInitSource):PRWInitSource {
  return (obj || new PRWInitSource()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPRWInitSource(bb:flatbuffers.ByteBuffer, obj?:PRWInitSource):PRWInitSource {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PRWInitSource()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Wire kind identifier for BYTES.
 */
KIND():prwSourceKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : prwSourceKind.OMM;
}

/**
 * Optional SDS file_identifier for BYTES (`$OMM`, `$OCM`, `$OEM`, `$PPE`).
 */
FILE_IDENTIFIER():string|null
FILE_IDENTIFIER(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
FILE_IDENTIFIER(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Encoded source record as a FlatBuffer (consumed per KIND).
 */
BYTES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

bytesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

bytesArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Convenience inline form when KIND == KEPLERIAN.
 */
KEPLERIAN(obj?:PRWKeplerianElements):PRWKeplerianElements|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new PRWKeplerianElements()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Convenience inline form when KIND == TLE.
 */
TLE(obj?:PRWTleLines):PRWTleLines|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new PRWTleLines()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startPRWInitSource(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addKind(builder:flatbuffers.Builder, KIND:prwSourceKind) {
  builder.addFieldInt8(0, KIND, prwSourceKind.OMM);
}

static addFileIdentifier(builder:flatbuffers.Builder, FILE_IDENTIFIEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, FILE_IDENTIFIEROffset, 0);
}

static addBytes(builder:flatbuffers.Builder, BYTESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, BYTESOffset, 0);
}

static createBytesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startBytesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addKeplerian(builder:flatbuffers.Builder, KEPLERIANOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, KEPLERIANOffset, 0);
}

static addTle(builder:flatbuffers.Builder, TLEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, TLEOffset, 0);
}

static endPRWInitSource(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): PRWInitSourceT {
  return new PRWInitSourceT(
    this.KIND(),
    this.FILE_IDENTIFIER(),
    this.bb!.createScalarList<number>(this.BYTES.bind(this), this.bytesLength()),
    (this.KEPLERIAN() !== null ? this.KEPLERIAN()!.unpack() : null),
    (this.TLE() !== null ? this.TLE()!.unpack() : null)
  );
}


unpackTo(_o: PRWInitSourceT): void {
  _o.KIND = this.KIND();
  _o.FILE_IDENTIFIER = this.FILE_IDENTIFIER();
  _o.BYTES = this.bb!.createScalarList<number>(this.BYTES.bind(this), this.bytesLength());
  _o.KEPLERIAN = (this.KEPLERIAN() !== null ? this.KEPLERIAN()!.unpack() : null);
  _o.TLE = (this.TLE() !== null ? this.TLE()!.unpack() : null);
}
}

export class PRWInitSourceT implements flatbuffers.IGeneratedObject {
constructor(
  public KIND: prwSourceKind = prwSourceKind.OMM,
  public FILE_IDENTIFIER: string|Uint8Array|null = null,
  public BYTES: (number)[] = [],
  public KEPLERIAN: PRWKeplerianElementsT|null = null,
  public TLE: PRWTleLinesT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const FILE_IDENTIFIER = (this.FILE_IDENTIFIER !== null ? builder.createString(this.FILE_IDENTIFIER!) : 0);
  const BYTES = PRWInitSource.createBytesVector(builder, this.BYTES);
  const KEPLERIAN = (this.KEPLERIAN !== null ? this.KEPLERIAN!.pack(builder) : 0);
  const TLE = (this.TLE !== null ? this.TLE!.pack(builder) : 0);

  PRWInitSource.startPRWInitSource(builder);
  PRWInitSource.addKind(builder, this.KIND);
  PRWInitSource.addFileIdentifier(builder, FILE_IDENTIFIER);
  PRWInitSource.addBytes(builder, BYTES);
  PRWInitSource.addKeplerian(builder, KEPLERIAN);
  PRWInitSource.addTle(builder, TLE);

  return PRWInitSource.endPRWInitSource(builder);
}
}
