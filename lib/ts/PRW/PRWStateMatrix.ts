// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Row-major square matrix on [x,y,z,vx,vy,vz,(mass)], SI state units.
 * Covariance entries have row*column units; STM entries row/column units.
 */
export class PRWStateMatrix implements flatbuffers.IUnpackableObject<PRWStateMatrixT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PRWStateMatrix {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPRWStateMatrix(bb:flatbuffers.ByteBuffer, obj?:PRWStateMatrix):PRWStateMatrix {
  return (obj || new PRWStateMatrix()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPRWStateMatrix(bb:flatbuffers.ByteBuffer, obj?:PRWStateMatrix):PRWStateMatrix {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PRWStateMatrix()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

DIMENSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

VALUES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

valuesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

valuesArray():Float64Array {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

static startPRWStateMatrix(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addDimension(builder:flatbuffers.Builder, DIMENSION:number) {
  builder.addFieldInt8(0, DIMENSION, 0);
}

static addValues(builder:flatbuffers.Builder, VALUESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, VALUESOffset, 0);
}

static createValuesVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createValuesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createValuesVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startValuesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static endPRWStateMatrix(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // VALUES
  return offset;
}

static createPRWStateMatrix(builder:flatbuffers.Builder, DIMENSION:number, VALUESOffset:flatbuffers.Offset):flatbuffers.Offset {
  PRWStateMatrix.startPRWStateMatrix(builder);
  PRWStateMatrix.addDimension(builder, DIMENSION);
  PRWStateMatrix.addValues(builder, VALUESOffset);
  return PRWStateMatrix.endPRWStateMatrix(builder);
}

unpack(): PRWStateMatrixT {
  return new PRWStateMatrixT(
    this.DIMENSION(),
    this.bb!.createScalarList<number>(this.VALUES.bind(this), this.valuesLength())
  );
}


unpackTo(_o: PRWStateMatrixT): void {
  _o.DIMENSION = this.DIMENSION();
  _o.VALUES = this.bb!.createScalarList<number>(this.VALUES.bind(this), this.valuesLength());
}
}

export class PRWStateMatrixT implements flatbuffers.IGeneratedObject {
constructor(
  public DIMENSION: number = 0,
  public VALUES: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const VALUES = PRWStateMatrix.createValuesVector(builder, this.VALUES);

  return PRWStateMatrix.createPRWStateMatrix(builder,
    this.DIMENSION,
    VALUES
  );
}
}
