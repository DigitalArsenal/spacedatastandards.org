// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Telemetry Transfer Frame (CCSDS 132.0-B-2)
 */
export class TMF implements flatbuffers.IUnpackableObject<TMFT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TMF {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTMF(bb:flatbuffers.ByteBuffer, obj?:TMF):TMF {
  return (obj || new TMF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTMF(bb:flatbuffers.ByteBuffer, obj?:TMF):TMF {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TMF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$TMF');
}

/**
 * Transfer frame version
 */
VERSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Spacecraft identifier
 */
SPACECRAFT_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Virtual channel identifier
 */
VIRTUAL_CHANNEL_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Operational control field flag
 */
OCF_FLAG():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Master channel frame count
 */
MASTER_FRAME_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Virtual channel frame count
 */
VIRTUAL_FRAME_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Secondary header flag
 */
SEC_HDR_FLAG():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Synchronization flag
 */
SYNCH_FLAG():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Packet order flag
 */
PACKET_ORDER_FLAG():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Segment length identifier
 */
SEGMENT_LENGTH_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * First header pointer
 */
FIRST_HDR_POINTER():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Data field
 */
DATA(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

dataLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

dataArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Operational control field
 */
OCF(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

ocfLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

ocfArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Frame error control field
 */
FECF():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

static startTMF(builder:flatbuffers.Builder) {
  builder.startObject(14);
}

static addVersion(builder:flatbuffers.Builder, VERSION:number) {
  builder.addFieldInt8(0, VERSION, 0);
}

static addSpacecraftId(builder:flatbuffers.Builder, SPACECRAFT_ID:number) {
  builder.addFieldInt16(1, SPACECRAFT_ID, 0);
}

static addVirtualChannelId(builder:flatbuffers.Builder, VIRTUAL_CHANNEL_ID:number) {
  builder.addFieldInt8(2, VIRTUAL_CHANNEL_ID, 0);
}

static addOcfFlag(builder:flatbuffers.Builder, OCF_FLAG:boolean) {
  builder.addFieldInt8(3, +OCF_FLAG, +false);
}

static addMasterFrameCount(builder:flatbuffers.Builder, MASTER_FRAME_COUNT:number) {
  builder.addFieldInt8(4, MASTER_FRAME_COUNT, 0);
}

static addVirtualFrameCount(builder:flatbuffers.Builder, VIRTUAL_FRAME_COUNT:number) {
  builder.addFieldInt8(5, VIRTUAL_FRAME_COUNT, 0);
}

static addSecHdrFlag(builder:flatbuffers.Builder, SEC_HDR_FLAG:boolean) {
  builder.addFieldInt8(6, +SEC_HDR_FLAG, +false);
}

static addSynchFlag(builder:flatbuffers.Builder, SYNCH_FLAG:boolean) {
  builder.addFieldInt8(7, +SYNCH_FLAG, +false);
}

static addPacketOrderFlag(builder:flatbuffers.Builder, PACKET_ORDER_FLAG:boolean) {
  builder.addFieldInt8(8, +PACKET_ORDER_FLAG, +false);
}

static addSegmentLengthId(builder:flatbuffers.Builder, SEGMENT_LENGTH_ID:number) {
  builder.addFieldInt8(9, SEGMENT_LENGTH_ID, 0);
}

static addFirstHdrPointer(builder:flatbuffers.Builder, FIRST_HDR_POINTER:number) {
  builder.addFieldInt16(10, FIRST_HDR_POINTER, 0);
}

static addData(builder:flatbuffers.Builder, DATAOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, DATAOffset, 0);
}

static createDataVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startDataVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addOcf(builder:flatbuffers.Builder, OCFOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, OCFOffset, 0);
}

static createOcfVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startOcfVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addFecf(builder:flatbuffers.Builder, FECF:number) {
  builder.addFieldInt16(13, FECF, 0);
}

static endTMF(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishTMFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TMF');
}

static finishSizePrefixedTMFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TMF', true);
}

static createTMF(builder:flatbuffers.Builder, VERSION:number, SPACECRAFT_ID:number, VIRTUAL_CHANNEL_ID:number, OCF_FLAG:boolean, MASTER_FRAME_COUNT:number, VIRTUAL_FRAME_COUNT:number, SEC_HDR_FLAG:boolean, SYNCH_FLAG:boolean, PACKET_ORDER_FLAG:boolean, SEGMENT_LENGTH_ID:number, FIRST_HDR_POINTER:number, DATAOffset:flatbuffers.Offset, OCFOffset:flatbuffers.Offset, FECF:number):flatbuffers.Offset {
  TMF.startTMF(builder);
  TMF.addVersion(builder, VERSION);
  TMF.addSpacecraftId(builder, SPACECRAFT_ID);
  TMF.addVirtualChannelId(builder, VIRTUAL_CHANNEL_ID);
  TMF.addOcfFlag(builder, OCF_FLAG);
  TMF.addMasterFrameCount(builder, MASTER_FRAME_COUNT);
  TMF.addVirtualFrameCount(builder, VIRTUAL_FRAME_COUNT);
  TMF.addSecHdrFlag(builder, SEC_HDR_FLAG);
  TMF.addSynchFlag(builder, SYNCH_FLAG);
  TMF.addPacketOrderFlag(builder, PACKET_ORDER_FLAG);
  TMF.addSegmentLengthId(builder, SEGMENT_LENGTH_ID);
  TMF.addFirstHdrPointer(builder, FIRST_HDR_POINTER);
  TMF.addData(builder, DATAOffset);
  TMF.addOcf(builder, OCFOffset);
  TMF.addFecf(builder, FECF);
  return TMF.endTMF(builder);
}

unpack(): TMFT {
  return new TMFT(
    this.VERSION(),
    this.SPACECRAFT_ID(),
    this.VIRTUAL_CHANNEL_ID(),
    this.OCF_FLAG(),
    this.MASTER_FRAME_COUNT(),
    this.VIRTUAL_FRAME_COUNT(),
    this.SEC_HDR_FLAG(),
    this.SYNCH_FLAG(),
    this.PACKET_ORDER_FLAG(),
    this.SEGMENT_LENGTH_ID(),
    this.FIRST_HDR_POINTER(),
    this.bb!.createScalarList<number>(this.DATA.bind(this), this.dataLength()),
    this.bb!.createScalarList<number>(this.OCF.bind(this), this.ocfLength()),
    this.FECF()
  );
}


unpackTo(_o: TMFT): void {
  _o.VERSION = this.VERSION();
  _o.SPACECRAFT_ID = this.SPACECRAFT_ID();
  _o.VIRTUAL_CHANNEL_ID = this.VIRTUAL_CHANNEL_ID();
  _o.OCF_FLAG = this.OCF_FLAG();
  _o.MASTER_FRAME_COUNT = this.MASTER_FRAME_COUNT();
  _o.VIRTUAL_FRAME_COUNT = this.VIRTUAL_FRAME_COUNT();
  _o.SEC_HDR_FLAG = this.SEC_HDR_FLAG();
  _o.SYNCH_FLAG = this.SYNCH_FLAG();
  _o.PACKET_ORDER_FLAG = this.PACKET_ORDER_FLAG();
  _o.SEGMENT_LENGTH_ID = this.SEGMENT_LENGTH_ID();
  _o.FIRST_HDR_POINTER = this.FIRST_HDR_POINTER();
  _o.DATA = this.bb!.createScalarList<number>(this.DATA.bind(this), this.dataLength());
  _o.OCF = this.bb!.createScalarList<number>(this.OCF.bind(this), this.ocfLength());
  _o.FECF = this.FECF();
}
}

export class TMFT implements flatbuffers.IGeneratedObject {
constructor(
  public VERSION: number = 0,
  public SPACECRAFT_ID: number = 0,
  public VIRTUAL_CHANNEL_ID: number = 0,
  public OCF_FLAG: boolean = false,
  public MASTER_FRAME_COUNT: number = 0,
  public VIRTUAL_FRAME_COUNT: number = 0,
  public SEC_HDR_FLAG: boolean = false,
  public SYNCH_FLAG: boolean = false,
  public PACKET_ORDER_FLAG: boolean = false,
  public SEGMENT_LENGTH_ID: number = 0,
  public FIRST_HDR_POINTER: number = 0,
  public DATA: (number)[] = [],
  public OCF: (number)[] = [],
  public FECF: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const DATA = TMF.createDataVector(builder, this.DATA);
  const OCF = TMF.createOcfVector(builder, this.OCF);

  return TMF.createTMF(builder,
    this.VERSION,
    this.SPACECRAFT_ID,
    this.VIRTUAL_CHANNEL_ID,
    this.OCF_FLAG,
    this.MASTER_FRAME_COUNT,
    this.VIRTUAL_FRAME_COUNT,
    this.SEC_HDR_FLAG,
    this.SYNCH_FLAG,
    this.PACKET_ORDER_FLAG,
    this.SEGMENT_LENGTH_ID,
    this.FIRST_HDR_POINTER,
    DATA,
    OCF,
    this.FECF
  );
}
}
