// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Completeness-capable signed index over a dataset update.
 */
export class DPMCompletenessIndex implements flatbuffers.IUnpackableObject<DPMCompletenessIndexT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DPMCompletenessIndex {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDPMCompletenessIndex(bb:flatbuffers.ByteBuffer, obj?:DPMCompletenessIndex):DPMCompletenessIndex {
  return (obj || new DPMCompletenessIndex()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDPMCompletenessIndex(bb:flatbuffers.ByteBuffer, obj?:DPMCompletenessIndex):DPMCompletenessIndex {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DPMCompletenessIndex()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Stable index name, e.g. file_id, norad_cat_id, epoch, source_batch. Every
 * completeness-verifiable dataset update SHOULD include a file_id index so
 * subscribers can prove that all returned records belong to the announced
 * FILE_ID partition.
 */
INDEX_NAME():string|null
INDEX_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
INDEX_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Deterministic ordering expression for the index. Providers and
 * subscribers MUST use this ordering when building or verifying range
 * proofs. A query is completeness-verifiable only when its predicate can be
 * expressed against one or more declared indexes.
 */
CANONICAL_ORDER():string|null
CANONICAL_ORDER(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CANONICAL_ORDER(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * SHA-256 or Merkle root of the ordered index, lowercase hex. This root is
 * signed by the DPM provider signature and is the verifier's commitment for
 * inclusion and range-completeness proofs. To verify a provider-mediated
 * response, the subscriber recomputes each returned leaf, walks the supplied
 * sibling hashes using MERKLE_PROFILE, confirms the root equals INDEX_ROOT,
 * and confirms any range-boundary proofs required by CANONICAL_ORDER.
 */
INDEX_ROOT():string|null
INDEX_ROOT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
INDEX_ROOT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Hash profile for leaves and internal nodes, e.g.
 * SDN-MERKLE-SHA256-v1. Profiles define domain separators, leaf material,
 * pair ordering, duplicate handling, and odd-leaf promotion.
 */
MERKLE_PROFILE():string|null
MERKLE_PROFILE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MERKLE_PROFILE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Whether this index can prove that no matching records were omitted for a
 * supported range query. Inclusion proofs alone prove authenticity, not
 * completeness.
 */
SUPPORTS_RANGE_COMPLETENESS():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startDPMCompletenessIndex(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addIndexName(builder:flatbuffers.Builder, INDEX_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, INDEX_NAMEOffset, 0);
}

static addCanonicalOrder(builder:flatbuffers.Builder, CANONICAL_ORDEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, CANONICAL_ORDEROffset, 0);
}

static addIndexRoot(builder:flatbuffers.Builder, INDEX_ROOTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, INDEX_ROOTOffset, 0);
}

static addMerkleProfile(builder:flatbuffers.Builder, MERKLE_PROFILEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, MERKLE_PROFILEOffset, 0);
}

static addSupportsRangeCompleteness(builder:flatbuffers.Builder, SUPPORTS_RANGE_COMPLETENESS:boolean) {
  builder.addFieldInt8(4, +SUPPORTS_RANGE_COMPLETENESS, +false);
}

static endDPMCompletenessIndex(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createDPMCompletenessIndex(builder:flatbuffers.Builder, INDEX_NAMEOffset:flatbuffers.Offset, CANONICAL_ORDEROffset:flatbuffers.Offset, INDEX_ROOTOffset:flatbuffers.Offset, MERKLE_PROFILEOffset:flatbuffers.Offset, SUPPORTS_RANGE_COMPLETENESS:boolean):flatbuffers.Offset {
  DPMCompletenessIndex.startDPMCompletenessIndex(builder);
  DPMCompletenessIndex.addIndexName(builder, INDEX_NAMEOffset);
  DPMCompletenessIndex.addCanonicalOrder(builder, CANONICAL_ORDEROffset);
  DPMCompletenessIndex.addIndexRoot(builder, INDEX_ROOTOffset);
  DPMCompletenessIndex.addMerkleProfile(builder, MERKLE_PROFILEOffset);
  DPMCompletenessIndex.addSupportsRangeCompleteness(builder, SUPPORTS_RANGE_COMPLETENESS);
  return DPMCompletenessIndex.endDPMCompletenessIndex(builder);
}

unpack(): DPMCompletenessIndexT {
  return new DPMCompletenessIndexT(
    this.INDEX_NAME(),
    this.CANONICAL_ORDER(),
    this.INDEX_ROOT(),
    this.MERKLE_PROFILE(),
    this.SUPPORTS_RANGE_COMPLETENESS()
  );
}


unpackTo(_o: DPMCompletenessIndexT): void {
  _o.INDEX_NAME = this.INDEX_NAME();
  _o.CANONICAL_ORDER = this.CANONICAL_ORDER();
  _o.INDEX_ROOT = this.INDEX_ROOT();
  _o.MERKLE_PROFILE = this.MERKLE_PROFILE();
  _o.SUPPORTS_RANGE_COMPLETENESS = this.SUPPORTS_RANGE_COMPLETENESS();
}
}

export class DPMCompletenessIndexT implements flatbuffers.IGeneratedObject {
constructor(
  public INDEX_NAME: string|Uint8Array|null = null,
  public CANONICAL_ORDER: string|Uint8Array|null = null,
  public INDEX_ROOT: string|Uint8Array|null = null,
  public MERKLE_PROFILE: string|Uint8Array|null = null,
  public SUPPORTS_RANGE_COMPLETENESS: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const INDEX_NAME = (this.INDEX_NAME !== null ? builder.createString(this.INDEX_NAME!) : 0);
  const CANONICAL_ORDER = (this.CANONICAL_ORDER !== null ? builder.createString(this.CANONICAL_ORDER!) : 0);
  const INDEX_ROOT = (this.INDEX_ROOT !== null ? builder.createString(this.INDEX_ROOT!) : 0);
  const MERKLE_PROFILE = (this.MERKLE_PROFILE !== null ? builder.createString(this.MERKLE_PROFILE!) : 0);

  return DPMCompletenessIndex.createDPMCompletenessIndex(builder,
    INDEX_NAME,
    CANONICAL_ORDER,
    INDEX_ROOT,
    MERKLE_PROFILE,
    this.SUPPORTS_RANGE_COMPLETENESS
  );
}
}
