// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { MDSCandidate, MDSCandidateT } from './MDSCandidate.js';


/**
 * Mission Design Solution Set — a set of candidate trajectories produced by
 * solving a mission design broad search, typically a Pareto front over
 * competing objectives (total delta-V against total flight time).
 *
 * SDS had no multi-candidate container of any kind before this record. The
 * closest existing carrier for a single planned burn is OCM.Maneuver, whose
 * payload is untyped DATA:[string] with MAN_UNITS:[string] and which has no
 * v-infinity, turn angle, flyby periapsis radius, or C3.
 */
export class MDS implements flatbuffers.IUnpackableObject<MDST> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MDS {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMDS(bb:flatbuffers.ByteBuffer, obj?:MDS):MDS {
  return (obj || new MDS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMDS(bb:flatbuffers.ByteBuffer, obj?:MDS):MDS {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MDS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$MDS');
}

/**
 * Stable identifier for this solution set.
 */
SOLUTION_SET_ID():string
SOLUTION_SET_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
SOLUTION_SET_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * PROBLEM_ID of the $MDP this set solves. A solution set whose problem
 * definition is unknown is not reproducible.
 */
PROBLEM_ID():string|null
PROBLEM_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROBLEM_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Objectives the front is expressed over, e.g.
 * ["TOTAL_DELTA_V_KM_S", "TOTAL_FLIGHT_TIME_DAYS"]. Names MUST be field
 * names of MDSCandidate.
 */
OBJECTIVES(index: number):string
OBJECTIVES(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
OBJECTIVES(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

objectivesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * The candidates.
 */
CANDIDATES(index: number, obj?:MDSCandidate):MDSCandidate|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new MDSCandidate()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

candidatesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Solver identity and version that produced the set.
 */
SOLVER():string|null
SOLVER(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SOLVER(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

SOLVER_VERSION():string|null
SOLVER_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SOLVER_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Ephemeris source used, e.g. a SPICE kernel set identifier.
 */
EPHEMERIS_SOURCE():string|null
EPHEMERIS_SOURCE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EPHEMERIS_SOURCE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * When the set was generated, ISO 8601.
 */
CREATION_DATE():string|null
CREATION_DATE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CREATION_DATE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Free-form notes.
 */
COMMENT():string|null
COMMENT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
COMMENT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startMDS(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addSolutionSetId(builder:flatbuffers.Builder, SOLUTION_SET_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SOLUTION_SET_IDOffset, 0);
}

static addProblemId(builder:flatbuffers.Builder, PROBLEM_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, PROBLEM_IDOffset, 0);
}

static addObjectives(builder:flatbuffers.Builder, OBJECTIVESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, OBJECTIVESOffset, 0);
}

static createObjectivesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startObjectivesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCandidates(builder:flatbuffers.Builder, CANDIDATESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, CANDIDATESOffset, 0);
}

static createCandidatesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCandidatesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSolver(builder:flatbuffers.Builder, SOLVEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, SOLVEROffset, 0);
}

static addSolverVersion(builder:flatbuffers.Builder, SOLVER_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, SOLVER_VERSIONOffset, 0);
}

static addEphemerisSource(builder:flatbuffers.Builder, EPHEMERIS_SOURCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, EPHEMERIS_SOURCEOffset, 0);
}

static addCreationDate(builder:flatbuffers.Builder, CREATION_DATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, CREATION_DATEOffset, 0);
}

static addComment(builder:flatbuffers.Builder, COMMENTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, COMMENTOffset, 0);
}

static endMDS(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // SOLUTION_SET_ID
  return offset;
}

static finishMDSBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MDS');
}

static finishSizePrefixedMDSBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MDS', true);
}

static createMDS(builder:flatbuffers.Builder, SOLUTION_SET_IDOffset:flatbuffers.Offset, PROBLEM_IDOffset:flatbuffers.Offset, OBJECTIVESOffset:flatbuffers.Offset, CANDIDATESOffset:flatbuffers.Offset, SOLVEROffset:flatbuffers.Offset, SOLVER_VERSIONOffset:flatbuffers.Offset, EPHEMERIS_SOURCEOffset:flatbuffers.Offset, CREATION_DATEOffset:flatbuffers.Offset, COMMENTOffset:flatbuffers.Offset):flatbuffers.Offset {
  MDS.startMDS(builder);
  MDS.addSolutionSetId(builder, SOLUTION_SET_IDOffset);
  MDS.addProblemId(builder, PROBLEM_IDOffset);
  MDS.addObjectives(builder, OBJECTIVESOffset);
  MDS.addCandidates(builder, CANDIDATESOffset);
  MDS.addSolver(builder, SOLVEROffset);
  MDS.addSolverVersion(builder, SOLVER_VERSIONOffset);
  MDS.addEphemerisSource(builder, EPHEMERIS_SOURCEOffset);
  MDS.addCreationDate(builder, CREATION_DATEOffset);
  MDS.addComment(builder, COMMENTOffset);
  return MDS.endMDS(builder);
}

unpack(): MDST {
  return new MDST(
    this.SOLUTION_SET_ID(),
    this.PROBLEM_ID(),
    this.bb!.createScalarList<string>(this.OBJECTIVES.bind(this), this.objectivesLength()),
    this.bb!.createObjList<MDSCandidate, MDSCandidateT>(this.CANDIDATES.bind(this), this.candidatesLength()),
    this.SOLVER(),
    this.SOLVER_VERSION(),
    this.EPHEMERIS_SOURCE(),
    this.CREATION_DATE(),
    this.COMMENT()
  );
}


unpackTo(_o: MDST): void {
  _o.SOLUTION_SET_ID = this.SOLUTION_SET_ID();
  _o.PROBLEM_ID = this.PROBLEM_ID();
  _o.OBJECTIVES = this.bb!.createScalarList<string>(this.OBJECTIVES.bind(this), this.objectivesLength());
  _o.CANDIDATES = this.bb!.createObjList<MDSCandidate, MDSCandidateT>(this.CANDIDATES.bind(this), this.candidatesLength());
  _o.SOLVER = this.SOLVER();
  _o.SOLVER_VERSION = this.SOLVER_VERSION();
  _o.EPHEMERIS_SOURCE = this.EPHEMERIS_SOURCE();
  _o.CREATION_DATE = this.CREATION_DATE();
  _o.COMMENT = this.COMMENT();
}
}

export class MDST implements flatbuffers.IGeneratedObject {
constructor(
  public SOLUTION_SET_ID: string|Uint8Array|null = null,
  public PROBLEM_ID: string|Uint8Array|null = null,
  public OBJECTIVES: (string)[] = [],
  public CANDIDATES: (MDSCandidateT)[] = [],
  public SOLVER: string|Uint8Array|null = null,
  public SOLVER_VERSION: string|Uint8Array|null = null,
  public EPHEMERIS_SOURCE: string|Uint8Array|null = null,
  public CREATION_DATE: string|Uint8Array|null = null,
  public COMMENT: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SOLUTION_SET_ID = (this.SOLUTION_SET_ID !== null ? builder.createString(this.SOLUTION_SET_ID!) : 0);
  const PROBLEM_ID = (this.PROBLEM_ID !== null ? builder.createString(this.PROBLEM_ID!) : 0);
  const OBJECTIVES = MDS.createObjectivesVector(builder, builder.createObjectOffsetList(this.OBJECTIVES));
  const CANDIDATES = MDS.createCandidatesVector(builder, builder.createObjectOffsetList(this.CANDIDATES));
  const SOLVER = (this.SOLVER !== null ? builder.createString(this.SOLVER!) : 0);
  const SOLVER_VERSION = (this.SOLVER_VERSION !== null ? builder.createString(this.SOLVER_VERSION!) : 0);
  const EPHEMERIS_SOURCE = (this.EPHEMERIS_SOURCE !== null ? builder.createString(this.EPHEMERIS_SOURCE!) : 0);
  const CREATION_DATE = (this.CREATION_DATE !== null ? builder.createString(this.CREATION_DATE!) : 0);
  const COMMENT = (this.COMMENT !== null ? builder.createString(this.COMMENT!) : 0);

  return MDS.createMDS(builder,
    SOLUTION_SET_ID,
    PROBLEM_ID,
    OBJECTIVES,
    CANDIDATES,
    SOLVER,
    SOLVER_VERSION,
    EPHEMERIS_SOURCE,
    CREATION_DATE,
    COMMENT
  );
}
}
