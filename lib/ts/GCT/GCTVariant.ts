// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GCTDensityVolume, GCTDensityVolumeT } from './GCTDensityVolume.js';
import { GCTDistanceField, GCTDistanceFieldT } from './GCTDistanceField.js';
import { GCTLevel, GCTLevelT } from './GCTLevel.js';
import { gctCloudGenus } from './gctCloudGenus.js';
import { gctVariantScope } from './gctVariantScope.js';


/**
 * One variant: one cloud (or one tile's merged mixture) as a chain of
 * levels of detail in its own frame. Instances place variants in the tile.
 */
export class GCTVariant implements flatbuffers.IUnpackableObject<GCTVariantT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GCTVariant {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGCTVariant(bb:flatbuffers.ByteBuffer, obj?:GCTVariant):GCTVariant {
  return (obj || new GCTVariant()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGCTVariant(bb:flatbuffers.ByteBuffer, obj?:GCTVariant):GCTVariant {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GCTVariant()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Producer-stable identifier of the variant within FIELD_ID.
 */
VARIANT_ID():string
VARIANT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
VARIANT_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Where the levels live.
 */
SCOPE():gctVariantScope {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : gctVariantScope.INLINE;
}

/**
 * Content identifier of the variant's levels when SCOPE is
 * SHARED_LIBRARY. Two references with equal VARIANT_CID are the same
 * bytes.
 */
VARIANT_CID():string|null
VARIANT_CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VARIANT_CID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Genus, where classified.
 */
GENUS():gctCloudGenus {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : gctCloudGenus.UNSPECIFIED;
}

/**
 * Levels of detail, finest first, each conserving the mass of the one
 * before it. Absent when SCOPE is SHARED_LIBRARY.
 */
LEVELS(index: number, obj?:GCTLevel):GCTLevel|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new GCTLevel()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

levelsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Axis-aligned bounds of the finest level's support in the variant
 * frame, metres: min x, y, z (3 values).
 */
BOUNDS_MIN_M(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

boundsMinMLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

boundsMinMArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Axis-aligned bounds, max x, y, z (3 values).
 */
BOUNDS_MAX_M(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

boundsMaxMLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

boundsMaxMArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Total mass of the finest level: the integral of amplitude over space,
 * AMPLITUDE_UNITS times cubic metres. Every level MUST match it within
 * MASS_TOLERANCE.
 */
MASS():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Signed distance field of the surface. Optional.
 */
DISTANCE_FIELD(obj?:GCTDistanceField):GCTDistanceField|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new GCTDistanceField()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Baked density volume for ray marching. Optional.
 */
DENSITY_VOLUME(obj?:GCTDensityVolume):GCTDensityVolume|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new GCTDensityVolume()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Seed the producer authored the variant from, for reproducibility.
 */
AUTHORING_SEED():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startGCTVariant(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addVariantId(builder:flatbuffers.Builder, VARIANT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, VARIANT_IDOffset, 0);
}

static addScope(builder:flatbuffers.Builder, SCOPE:gctVariantScope) {
  builder.addFieldInt8(1, SCOPE, gctVariantScope.INLINE);
}

static addVariantCid(builder:flatbuffers.Builder, VARIANT_CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, VARIANT_CIDOffset, 0);
}

static addGenus(builder:flatbuffers.Builder, GENUS:gctCloudGenus) {
  builder.addFieldInt8(3, GENUS, gctCloudGenus.UNSPECIFIED);
}

static addLevels(builder:flatbuffers.Builder, LEVELSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, LEVELSOffset, 0);
}

static createLevelsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startLevelsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBoundsMinM(builder:flatbuffers.Builder, BOUNDS_MIN_MOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, BOUNDS_MIN_MOffset, 0);
}

static createBoundsMinMVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createBoundsMinMVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createBoundsMinMVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startBoundsMinMVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBoundsMaxM(builder:flatbuffers.Builder, BOUNDS_MAX_MOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, BOUNDS_MAX_MOffset, 0);
}

static createBoundsMaxMVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createBoundsMaxMVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createBoundsMaxMVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startBoundsMaxMVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMass(builder:flatbuffers.Builder, MASS:number) {
  builder.addFieldFloat64(7, MASS, 0.0);
}

static addDistanceField(builder:flatbuffers.Builder, DISTANCE_FIELDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, DISTANCE_FIELDOffset, 0);
}

static addDensityVolume(builder:flatbuffers.Builder, DENSITY_VOLUMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, DENSITY_VOLUMEOffset, 0);
}

static addAuthoringSeed(builder:flatbuffers.Builder, AUTHORING_SEED:number) {
  builder.addFieldInt32(10, AUTHORING_SEED, 0);
}

static endGCTVariant(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // VARIANT_ID
  return offset;
}


unpack(): GCTVariantT {
  return new GCTVariantT(
    this.VARIANT_ID(),
    this.SCOPE(),
    this.VARIANT_CID(),
    this.GENUS(),
    this.bb!.createObjList<GCTLevel, GCTLevelT>(this.LEVELS.bind(this), this.levelsLength()),
    this.bb!.createScalarList<number>(this.BOUNDS_MIN_M.bind(this), this.boundsMinMLength()),
    this.bb!.createScalarList<number>(this.BOUNDS_MAX_M.bind(this), this.boundsMaxMLength()),
    this.MASS(),
    (this.DISTANCE_FIELD() !== null ? this.DISTANCE_FIELD()!.unpack() : null),
    (this.DENSITY_VOLUME() !== null ? this.DENSITY_VOLUME()!.unpack() : null),
    this.AUTHORING_SEED()
  );
}


unpackTo(_o: GCTVariantT): void {
  _o.VARIANT_ID = this.VARIANT_ID();
  _o.SCOPE = this.SCOPE();
  _o.VARIANT_CID = this.VARIANT_CID();
  _o.GENUS = this.GENUS();
  _o.LEVELS = this.bb!.createObjList<GCTLevel, GCTLevelT>(this.LEVELS.bind(this), this.levelsLength());
  _o.BOUNDS_MIN_M = this.bb!.createScalarList<number>(this.BOUNDS_MIN_M.bind(this), this.boundsMinMLength());
  _o.BOUNDS_MAX_M = this.bb!.createScalarList<number>(this.BOUNDS_MAX_M.bind(this), this.boundsMaxMLength());
  _o.MASS = this.MASS();
  _o.DISTANCE_FIELD = (this.DISTANCE_FIELD() !== null ? this.DISTANCE_FIELD()!.unpack() : null);
  _o.DENSITY_VOLUME = (this.DENSITY_VOLUME() !== null ? this.DENSITY_VOLUME()!.unpack() : null);
  _o.AUTHORING_SEED = this.AUTHORING_SEED();
}
}

export class GCTVariantT implements flatbuffers.IGeneratedObject {
constructor(
  public VARIANT_ID: string|Uint8Array|null = null,
  public SCOPE: gctVariantScope = gctVariantScope.INLINE,
  public VARIANT_CID: string|Uint8Array|null = null,
  public GENUS: gctCloudGenus = gctCloudGenus.UNSPECIFIED,
  public LEVELS: (GCTLevelT)[] = [],
  public BOUNDS_MIN_M: (number)[] = [],
  public BOUNDS_MAX_M: (number)[] = [],
  public MASS: number = 0.0,
  public DISTANCE_FIELD: GCTDistanceFieldT|null = null,
  public DENSITY_VOLUME: GCTDensityVolumeT|null = null,
  public AUTHORING_SEED: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const VARIANT_ID = (this.VARIANT_ID !== null ? builder.createString(this.VARIANT_ID!) : 0);
  const VARIANT_CID = (this.VARIANT_CID !== null ? builder.createString(this.VARIANT_CID!) : 0);
  const LEVELS = GCTVariant.createLevelsVector(builder, builder.createObjectOffsetList(this.LEVELS));
  const BOUNDS_MIN_M = GCTVariant.createBoundsMinMVector(builder, this.BOUNDS_MIN_M);
  const BOUNDS_MAX_M = GCTVariant.createBoundsMaxMVector(builder, this.BOUNDS_MAX_M);
  const DISTANCE_FIELD = (this.DISTANCE_FIELD !== null ? this.DISTANCE_FIELD!.pack(builder) : 0);
  const DENSITY_VOLUME = (this.DENSITY_VOLUME !== null ? this.DENSITY_VOLUME!.pack(builder) : 0);

  GCTVariant.startGCTVariant(builder);
  GCTVariant.addVariantId(builder, VARIANT_ID);
  GCTVariant.addScope(builder, this.SCOPE);
  GCTVariant.addVariantCid(builder, VARIANT_CID);
  GCTVariant.addGenus(builder, this.GENUS);
  GCTVariant.addLevels(builder, LEVELS);
  GCTVariant.addBoundsMinM(builder, BOUNDS_MIN_M);
  GCTVariant.addBoundsMaxM(builder, BOUNDS_MAX_M);
  GCTVariant.addMass(builder, this.MASS);
  GCTVariant.addDistanceField(builder, DISTANCE_FIELD);
  GCTVariant.addDensityVolume(builder, DENSITY_VOLUME);
  GCTVariant.addAuthoringSeed(builder, this.AUTHORING_SEED);

  return GCTVariant.endGCTVariant(builder);
}
}
