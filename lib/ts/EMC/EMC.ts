// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { EMCEndpointRef, EMCEndpointRefT } from './EMCEndpointRef.js';
import { EMCIntermodulationTerm, EMCIntermodulationTermT } from './EMCIntermodulationTerm.js';
import { RFLProvenance, RFLProvenanceT } from './RFLProvenance.js';
import { emcCouplingPath } from './emcCouplingPath.js';
import { emcMechanismCategory } from './emcMechanismCategory.js';
import { emcResolutionState } from './emcResolutionState.js';
import { rfsMaterialClass } from './rfsMaterialClass.js';


/**
 * Electromagnetic Compatibility Assessment
 */
export class EMC implements flatbuffers.IUnpackableObject<EMCT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EMC {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEMC(bb:flatbuffers.ByteBuffer, obj?:EMC):EMC {
  return (obj || new EMC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEMC(bb:flatbuffers.ByteBuffer, obj?:EMC):EMC {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EMC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$EMC');
}

EMC_ID():string
EMC_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
EMC_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

NAME():string|null
NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

SCENARIO_ID():string|null
SCENARIO_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SCENARIO_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

RFL_ID():string|null
RFL_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
RFL_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

VICTIM_ENDPOINT(obj?:EMCEndpointRef):EMCEndpointRef|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new EMCEndpointRef()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

AGGRESSOR_ENDPOINT(obj?:EMCEndpointRef):EMCEndpointRef|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new EMCEndpointRef()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

COUPLING_MECHANISM():emcCouplingPath {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : emcCouplingPath.UNSPECIFIED;
}

MECHANISM_CLASS():emcMechanismCategory {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : emcMechanismCategory.UNSPECIFIED;
}

/**
 * Order of the active or passive intermodulation product. 0 when the
 * mechanism is not intermodulation.
 */
INTERMODULATION_ORDER():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

INTERMODULATION_TERMS(index: number, obj?:EMCIntermodulationTerm):EMCIntermodulationTerm|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new EMCIntermodulationTerm()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

intermodulationTermsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

INTERMODULATION_PRODUCT_FREQUENCY_HZ():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

HARMONIC_ORDER():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * RFEEmissionMask.MASK_ID used for an emission-limit comparison.
 */
EMISSION_MASK_ID():string|null
EMISSION_MASK_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EMISSION_MASK_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * `$RFS.MATERIAL_ID` and class used for structural coupling.
 */
RFS_MATERIAL_ID():string|null
RFS_MATERIAL_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
RFS_MATERIAL_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

RFS_MATERIAL_CLASS():rfsMaterialClass {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : rfsMaterialClass.UNSPECIFIED;
}

ISOLATION_DB():number {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

COUPLED_POWER_DBW():number {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

VICTIM_LIMIT_DBW():number {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MARGIN_DB():number {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Positive when the victim limit is exceeded.
 */
EXCEEDANCE_DB():number {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

EXCEEDS_LIMIT():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 44);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

RESOLUTION():emcResolutionState {
  const offset = this.bb!.__offset(this.bb_pos, 46);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : emcResolutionState.UNSPECIFIED;
}

RESOLUTION_NOTE():string|null
RESOLUTION_NOTE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
RESOLUTION_NOTE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 48);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

PROVENANCE(obj?:RFLProvenance):RFLProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 50);
  return offset ? (obj || new RFLProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

COMPUTED_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 52);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

PRODUCER_ID():string
PRODUCER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
PRODUCER_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 54);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Ed25519 signature over the size-prefixed FlatBuffer with both 64-byte
 * signature payloads zeroed while preserving their vectors and offsets.
 */
SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 56);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 56);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signatureArray():Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 56);
  return new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

/**
 * Ed25519 signature over canonical JSON with IDL field order and
 * capitalization, no insignificant whitespace, and both signature fields
 * omitted.
 */
CANONICAL_JSON_SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 58);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

canonicalJsonSignatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 58);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

canonicalJsonSignatureArray():Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 58);
  return new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

static startEMC(builder:flatbuffers.Builder) {
  builder.startObject(28);
}

static addEmcId(builder:flatbuffers.Builder, EMC_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, EMC_IDOffset, 0);
}

static addName(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, NAMEOffset, 0);
}

static addScenarioId(builder:flatbuffers.Builder, SCENARIO_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, SCENARIO_IDOffset, 0);
}

static addRflId(builder:flatbuffers.Builder, RFL_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, RFL_IDOffset, 0);
}

static addVictimEndpoint(builder:flatbuffers.Builder, VICTIM_ENDPOINTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, VICTIM_ENDPOINTOffset, 0);
}

static addAggressorEndpoint(builder:flatbuffers.Builder, AGGRESSOR_ENDPOINTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, AGGRESSOR_ENDPOINTOffset, 0);
}

static addCouplingMechanism(builder:flatbuffers.Builder, COUPLING_MECHANISM:emcCouplingPath) {
  builder.addFieldInt8(6, COUPLING_MECHANISM, emcCouplingPath.UNSPECIFIED);
}

static addMechanismClass(builder:flatbuffers.Builder, MECHANISM_CLASS:emcMechanismCategory) {
  builder.addFieldInt8(7, MECHANISM_CLASS, emcMechanismCategory.UNSPECIFIED);
}

static addIntermodulationOrder(builder:flatbuffers.Builder, INTERMODULATION_ORDER:number) {
  builder.addFieldInt32(8, INTERMODULATION_ORDER, 0);
}

static addIntermodulationTerms(builder:flatbuffers.Builder, INTERMODULATION_TERMSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, INTERMODULATION_TERMSOffset, 0);
}

static createIntermodulationTermsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startIntermodulationTermsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addIntermodulationProductFrequencyHz(builder:flatbuffers.Builder, INTERMODULATION_PRODUCT_FREQUENCY_HZ:number) {
  builder.addFieldFloat64(10, INTERMODULATION_PRODUCT_FREQUENCY_HZ, 0.0);
}

static addHarmonicOrder(builder:flatbuffers.Builder, HARMONIC_ORDER:number) {
  builder.addFieldInt32(11, HARMONIC_ORDER, 0);
}

static addEmissionMaskId(builder:flatbuffers.Builder, EMISSION_MASK_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, EMISSION_MASK_IDOffset, 0);
}

static addRfsMaterialId(builder:flatbuffers.Builder, RFS_MATERIAL_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, RFS_MATERIAL_IDOffset, 0);
}

static addRfsMaterialClass(builder:flatbuffers.Builder, RFS_MATERIAL_CLASS:rfsMaterialClass) {
  builder.addFieldInt8(14, RFS_MATERIAL_CLASS, rfsMaterialClass.UNSPECIFIED);
}

static addIsolationDb(builder:flatbuffers.Builder, ISOLATION_DB:number) {
  builder.addFieldFloat64(15, ISOLATION_DB, 0.0);
}

static addCoupledPowerDbw(builder:flatbuffers.Builder, COUPLED_POWER_DBW:number) {
  builder.addFieldFloat64(16, COUPLED_POWER_DBW, 0.0);
}

static addVictimLimitDbw(builder:flatbuffers.Builder, VICTIM_LIMIT_DBW:number) {
  builder.addFieldFloat64(17, VICTIM_LIMIT_DBW, 0.0);
}

static addMarginDb(builder:flatbuffers.Builder, MARGIN_DB:number) {
  builder.addFieldFloat64(18, MARGIN_DB, 0.0);
}

static addExceedanceDb(builder:flatbuffers.Builder, EXCEEDANCE_DB:number) {
  builder.addFieldFloat64(19, EXCEEDANCE_DB, 0.0);
}

static addExceedsLimit(builder:flatbuffers.Builder, EXCEEDS_LIMIT:boolean) {
  builder.addFieldInt8(20, +EXCEEDS_LIMIT, +false);
}

static addResolution(builder:flatbuffers.Builder, RESOLUTION:emcResolutionState) {
  builder.addFieldInt8(21, RESOLUTION, emcResolutionState.UNSPECIFIED);
}

static addResolutionNote(builder:flatbuffers.Builder, RESOLUTION_NOTEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(22, RESOLUTION_NOTEOffset, 0);
}

static addProvenance(builder:flatbuffers.Builder, PROVENANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(23, PROVENANCEOffset, 0);
}

static addComputedAt(builder:flatbuffers.Builder, COMPUTED_AT:bigint) {
  builder.addFieldInt64(24, COMPUTED_AT, BigInt('0'));
}

static addProducerId(builder:flatbuffers.Builder, PRODUCER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(25, PRODUCER_IDOffset, 0);
}

static addSignature(builder:flatbuffers.Builder, SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(26, SIGNATUREOffset, 0);
}

static createSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addCanonicalJsonSignature(builder:flatbuffers.Builder, CANONICAL_JSON_SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(27, CANONICAL_JSON_SIGNATUREOffset, 0);
}

static createCanonicalJsonSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCanonicalJsonSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endEMC(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // EMC_ID
  builder.requiredField(offset, 12) // VICTIM_ENDPOINT
  builder.requiredField(offset, 14) // AGGRESSOR_ENDPOINT
  builder.requiredField(offset, 50) // PROVENANCE
  builder.requiredField(offset, 54) // PRODUCER_ID
  builder.requiredField(offset, 56) // SIGNATURE
  builder.requiredField(offset, 58) // CANONICAL_JSON_SIGNATURE
  return offset;
}

static finishEMCBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$EMC');
}

static finishSizePrefixedEMCBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$EMC', true);
}


unpack(): EMCT {
  return new EMCT(
    this.EMC_ID(),
    this.NAME(),
    this.SCENARIO_ID(),
    this.RFL_ID(),
    (this.VICTIM_ENDPOINT() !== null ? this.VICTIM_ENDPOINT()!.unpack() : null),
    (this.AGGRESSOR_ENDPOINT() !== null ? this.AGGRESSOR_ENDPOINT()!.unpack() : null),
    this.COUPLING_MECHANISM(),
    this.MECHANISM_CLASS(),
    this.INTERMODULATION_ORDER(),
    this.bb!.createObjList<EMCIntermodulationTerm, EMCIntermodulationTermT>(this.INTERMODULATION_TERMS.bind(this), this.intermodulationTermsLength()),
    this.INTERMODULATION_PRODUCT_FREQUENCY_HZ(),
    this.HARMONIC_ORDER(),
    this.EMISSION_MASK_ID(),
    this.RFS_MATERIAL_ID(),
    this.RFS_MATERIAL_CLASS(),
    this.ISOLATION_DB(),
    this.COUPLED_POWER_DBW(),
    this.VICTIM_LIMIT_DBW(),
    this.MARGIN_DB(),
    this.EXCEEDANCE_DB(),
    this.EXCEEDS_LIMIT(),
    this.RESOLUTION(),
    this.RESOLUTION_NOTE(),
    (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null),
    this.COMPUTED_AT(),
    this.PRODUCER_ID(),
    this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength()),
    this.bb!.createScalarList<number>(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength())
  );
}


unpackTo(_o: EMCT): void {
  _o.EMC_ID = this.EMC_ID();
  _o.NAME = this.NAME();
  _o.SCENARIO_ID = this.SCENARIO_ID();
  _o.RFL_ID = this.RFL_ID();
  _o.VICTIM_ENDPOINT = (this.VICTIM_ENDPOINT() !== null ? this.VICTIM_ENDPOINT()!.unpack() : null);
  _o.AGGRESSOR_ENDPOINT = (this.AGGRESSOR_ENDPOINT() !== null ? this.AGGRESSOR_ENDPOINT()!.unpack() : null);
  _o.COUPLING_MECHANISM = this.COUPLING_MECHANISM();
  _o.MECHANISM_CLASS = this.MECHANISM_CLASS();
  _o.INTERMODULATION_ORDER = this.INTERMODULATION_ORDER();
  _o.INTERMODULATION_TERMS = this.bb!.createObjList<EMCIntermodulationTerm, EMCIntermodulationTermT>(this.INTERMODULATION_TERMS.bind(this), this.intermodulationTermsLength());
  _o.INTERMODULATION_PRODUCT_FREQUENCY_HZ = this.INTERMODULATION_PRODUCT_FREQUENCY_HZ();
  _o.HARMONIC_ORDER = this.HARMONIC_ORDER();
  _o.EMISSION_MASK_ID = this.EMISSION_MASK_ID();
  _o.RFS_MATERIAL_ID = this.RFS_MATERIAL_ID();
  _o.RFS_MATERIAL_CLASS = this.RFS_MATERIAL_CLASS();
  _o.ISOLATION_DB = this.ISOLATION_DB();
  _o.COUPLED_POWER_DBW = this.COUPLED_POWER_DBW();
  _o.VICTIM_LIMIT_DBW = this.VICTIM_LIMIT_DBW();
  _o.MARGIN_DB = this.MARGIN_DB();
  _o.EXCEEDANCE_DB = this.EXCEEDANCE_DB();
  _o.EXCEEDS_LIMIT = this.EXCEEDS_LIMIT();
  _o.RESOLUTION = this.RESOLUTION();
  _o.RESOLUTION_NOTE = this.RESOLUTION_NOTE();
  _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null);
  _o.COMPUTED_AT = this.COMPUTED_AT();
  _o.PRODUCER_ID = this.PRODUCER_ID();
  _o.SIGNATURE = this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength());
  _o.CANONICAL_JSON_SIGNATURE = this.bb!.createScalarList<number>(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength());
}
}

export class EMCT implements flatbuffers.IGeneratedObject {
constructor(
  public EMC_ID: string|Uint8Array|null = null,
  public NAME: string|Uint8Array|null = null,
  public SCENARIO_ID: string|Uint8Array|null = null,
  public RFL_ID: string|Uint8Array|null = null,
  public VICTIM_ENDPOINT: EMCEndpointRefT|null = null,
  public AGGRESSOR_ENDPOINT: EMCEndpointRefT|null = null,
  public COUPLING_MECHANISM: emcCouplingPath = emcCouplingPath.UNSPECIFIED,
  public MECHANISM_CLASS: emcMechanismCategory = emcMechanismCategory.UNSPECIFIED,
  public INTERMODULATION_ORDER: number = 0,
  public INTERMODULATION_TERMS: (EMCIntermodulationTermT)[] = [],
  public INTERMODULATION_PRODUCT_FREQUENCY_HZ: number = 0.0,
  public HARMONIC_ORDER: number = 0,
  public EMISSION_MASK_ID: string|Uint8Array|null = null,
  public RFS_MATERIAL_ID: string|Uint8Array|null = null,
  public RFS_MATERIAL_CLASS: rfsMaterialClass = rfsMaterialClass.UNSPECIFIED,
  public ISOLATION_DB: number = 0.0,
  public COUPLED_POWER_DBW: number = 0.0,
  public VICTIM_LIMIT_DBW: number = 0.0,
  public MARGIN_DB: number = 0.0,
  public EXCEEDANCE_DB: number = 0.0,
  public EXCEEDS_LIMIT: boolean = false,
  public RESOLUTION: emcResolutionState = emcResolutionState.UNSPECIFIED,
  public RESOLUTION_NOTE: string|Uint8Array|null = null,
  public PROVENANCE: RFLProvenanceT|null = null,
  public COMPUTED_AT: bigint = BigInt('0'),
  public PRODUCER_ID: string|Uint8Array|null = null,
  public SIGNATURE: (number)[] = [],
  public CANONICAL_JSON_SIGNATURE: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const EMC_ID = (this.EMC_ID !== null ? builder.createString(this.EMC_ID!) : 0);
  const NAME = (this.NAME !== null ? builder.createString(this.NAME!) : 0);
  const SCENARIO_ID = (this.SCENARIO_ID !== null ? builder.createString(this.SCENARIO_ID!) : 0);
  const RFL_ID = (this.RFL_ID !== null ? builder.createString(this.RFL_ID!) : 0);
  const VICTIM_ENDPOINT = (this.VICTIM_ENDPOINT !== null ? this.VICTIM_ENDPOINT!.pack(builder) : 0);
  const AGGRESSOR_ENDPOINT = (this.AGGRESSOR_ENDPOINT !== null ? this.AGGRESSOR_ENDPOINT!.pack(builder) : 0);
  const INTERMODULATION_TERMS = EMC.createIntermodulationTermsVector(builder, builder.createObjectOffsetList(this.INTERMODULATION_TERMS));
  const EMISSION_MASK_ID = (this.EMISSION_MASK_ID !== null ? builder.createString(this.EMISSION_MASK_ID!) : 0);
  const RFS_MATERIAL_ID = (this.RFS_MATERIAL_ID !== null ? builder.createString(this.RFS_MATERIAL_ID!) : 0);
  const RESOLUTION_NOTE = (this.RESOLUTION_NOTE !== null ? builder.createString(this.RESOLUTION_NOTE!) : 0);
  const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE!.pack(builder) : 0);
  const PRODUCER_ID = (this.PRODUCER_ID !== null ? builder.createString(this.PRODUCER_ID!) : 0);
  const SIGNATURE = EMC.createSignatureVector(builder, this.SIGNATURE);
  const CANONICAL_JSON_SIGNATURE = EMC.createCanonicalJsonSignatureVector(builder, this.CANONICAL_JSON_SIGNATURE);

  EMC.startEMC(builder);
  EMC.addEmcId(builder, EMC_ID);
  EMC.addName(builder, NAME);
  EMC.addScenarioId(builder, SCENARIO_ID);
  EMC.addRflId(builder, RFL_ID);
  EMC.addVictimEndpoint(builder, VICTIM_ENDPOINT);
  EMC.addAggressorEndpoint(builder, AGGRESSOR_ENDPOINT);
  EMC.addCouplingMechanism(builder, this.COUPLING_MECHANISM);
  EMC.addMechanismClass(builder, this.MECHANISM_CLASS);
  EMC.addIntermodulationOrder(builder, this.INTERMODULATION_ORDER);
  EMC.addIntermodulationTerms(builder, INTERMODULATION_TERMS);
  EMC.addIntermodulationProductFrequencyHz(builder, this.INTERMODULATION_PRODUCT_FREQUENCY_HZ);
  EMC.addHarmonicOrder(builder, this.HARMONIC_ORDER);
  EMC.addEmissionMaskId(builder, EMISSION_MASK_ID);
  EMC.addRfsMaterialId(builder, RFS_MATERIAL_ID);
  EMC.addRfsMaterialClass(builder, this.RFS_MATERIAL_CLASS);
  EMC.addIsolationDb(builder, this.ISOLATION_DB);
  EMC.addCoupledPowerDbw(builder, this.COUPLED_POWER_DBW);
  EMC.addVictimLimitDbw(builder, this.VICTIM_LIMIT_DBW);
  EMC.addMarginDb(builder, this.MARGIN_DB);
  EMC.addExceedanceDb(builder, this.EXCEEDANCE_DB);
  EMC.addExceedsLimit(builder, this.EXCEEDS_LIMIT);
  EMC.addResolution(builder, this.RESOLUTION);
  EMC.addResolutionNote(builder, RESOLUTION_NOTE);
  EMC.addProvenance(builder, PROVENANCE);
  EMC.addComputedAt(builder, this.COMPUTED_AT);
  EMC.addProducerId(builder, PRODUCER_ID);
  EMC.addSignature(builder, SIGNATURE);
  EMC.addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATURE);

  return EMC.endEMC(builder);
}
}
