// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SHWUniform, SHWUniformT } from './SHWUniform.js';
import { glslStage } from './glslStage.js';
import { shaderHookPoint } from './shaderHookPoint.js';


/**
 * Compile request — one shader source (typically a fragment of a
 * larger stage) plus metadata for the host splice.
 */
export class SHWCompileRequest implements flatbuffers.IUnpackableObject<SHWCompileRequestT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SHWCompileRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSHWCompileRequest(bb:flatbuffers.ByteBuffer, obj?:SHWCompileRequest):SHWCompileRequest {
  return (obj || new SHWCompileRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSHWCompileRequest(bb:flatbuffers.ByteBuffer, obj?:SHWCompileRequest):SHWCompileRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SHWCompileRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Logical shader name (used in logs and uniform prefixing).
 */
SHADER_NAME():string|null
SHADER_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SHADER_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Target GLSL stage.
 */
SHADER_STAGE():glslStage {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : glslStage.VERTEX;
}

/**
 * Intended injection point in the host pipeline.
 */
SHADER_INJECTION_POINT():shaderHookPoint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : shaderHookPoint.NONE;
}

/**
 * GLSL source (or preprocessed chunk).
 */
SHADER_SOURCE():string|null
SHADER_SOURCE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SHADER_SOURCE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * GLSL version string (for example `300 es`).
 */
GLSL_VERSION():string|null
GLSL_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
GLSL_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Expected uniforms declared by the source.
 */
SHADER_UNIFORMS(index: number, obj?:SHWUniform):SHWUniform|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new SHWUniform()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

shaderUniformsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startSHWCompileRequest(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addShaderName(builder:flatbuffers.Builder, SHADER_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SHADER_NAMEOffset, 0);
}

static addShaderStage(builder:flatbuffers.Builder, SHADER_STAGE:glslStage) {
  builder.addFieldInt8(1, SHADER_STAGE, glslStage.VERTEX);
}

static addShaderInjectionPoint(builder:flatbuffers.Builder, SHADER_INJECTION_POINT:shaderHookPoint) {
  builder.addFieldInt8(2, SHADER_INJECTION_POINT, shaderHookPoint.NONE);
}

static addShaderSource(builder:flatbuffers.Builder, SHADER_SOURCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, SHADER_SOURCEOffset, 0);
}

static addGlslVersion(builder:flatbuffers.Builder, GLSL_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, GLSL_VERSIONOffset, 0);
}

static addShaderUniforms(builder:flatbuffers.Builder, SHADER_UNIFORMSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, SHADER_UNIFORMSOffset, 0);
}

static createShaderUniformsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startShaderUniformsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endSHWCompileRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSHWCompileRequest(builder:flatbuffers.Builder, SHADER_NAMEOffset:flatbuffers.Offset, SHADER_STAGE:glslStage, SHADER_INJECTION_POINT:shaderHookPoint, SHADER_SOURCEOffset:flatbuffers.Offset, GLSL_VERSIONOffset:flatbuffers.Offset, SHADER_UNIFORMSOffset:flatbuffers.Offset):flatbuffers.Offset {
  SHWCompileRequest.startSHWCompileRequest(builder);
  SHWCompileRequest.addShaderName(builder, SHADER_NAMEOffset);
  SHWCompileRequest.addShaderStage(builder, SHADER_STAGE);
  SHWCompileRequest.addShaderInjectionPoint(builder, SHADER_INJECTION_POINT);
  SHWCompileRequest.addShaderSource(builder, SHADER_SOURCEOffset);
  SHWCompileRequest.addGlslVersion(builder, GLSL_VERSIONOffset);
  SHWCompileRequest.addShaderUniforms(builder, SHADER_UNIFORMSOffset);
  return SHWCompileRequest.endSHWCompileRequest(builder);
}

unpack(): SHWCompileRequestT {
  return new SHWCompileRequestT(
    this.SHADER_NAME(),
    this.SHADER_STAGE(),
    this.SHADER_INJECTION_POINT(),
    this.SHADER_SOURCE(),
    this.GLSL_VERSION(),
    this.bb!.createObjList<SHWUniform, SHWUniformT>(this.SHADER_UNIFORMS.bind(this), this.shaderUniformsLength())
  );
}


unpackTo(_o: SHWCompileRequestT): void {
  _o.SHADER_NAME = this.SHADER_NAME();
  _o.SHADER_STAGE = this.SHADER_STAGE();
  _o.SHADER_INJECTION_POINT = this.SHADER_INJECTION_POINT();
  _o.SHADER_SOURCE = this.SHADER_SOURCE();
  _o.GLSL_VERSION = this.GLSL_VERSION();
  _o.SHADER_UNIFORMS = this.bb!.createObjList<SHWUniform, SHWUniformT>(this.SHADER_UNIFORMS.bind(this), this.shaderUniformsLength());
}
}

export class SHWCompileRequestT implements flatbuffers.IGeneratedObject {
constructor(
  public SHADER_NAME: string|Uint8Array|null = null,
  public SHADER_STAGE: glslStage = glslStage.VERTEX,
  public SHADER_INJECTION_POINT: shaderHookPoint = shaderHookPoint.NONE,
  public SHADER_SOURCE: string|Uint8Array|null = null,
  public GLSL_VERSION: string|Uint8Array|null = null,
  public SHADER_UNIFORMS: (SHWUniformT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SHADER_NAME = (this.SHADER_NAME !== null ? builder.createString(this.SHADER_NAME!) : 0);
  const SHADER_SOURCE = (this.SHADER_SOURCE !== null ? builder.createString(this.SHADER_SOURCE!) : 0);
  const GLSL_VERSION = (this.GLSL_VERSION !== null ? builder.createString(this.GLSL_VERSION!) : 0);
  const SHADER_UNIFORMS = SHWCompileRequest.createShaderUniformsVector(builder, builder.createObjectOffsetList(this.SHADER_UNIFORMS));

  return SHWCompileRequest.createSHWCompileRequest(builder,
    SHADER_NAME,
    this.SHADER_STAGE,
    this.SHADER_INJECTION_POINT,
    SHADER_SOURCE,
    GLSL_VERSION,
    SHADER_UNIFORMS
  );
}
}
