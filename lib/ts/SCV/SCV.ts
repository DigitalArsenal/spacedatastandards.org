// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SCVCancel, SCVCancelT } from './SCVCancel.js';
import { SCVCoverageRequest, SCVCoverageRequestT } from './SCVCoverageRequest.js';
import { SCVPackedGeometryChunk, SCVPackedGeometryChunkT } from './SCVPackedGeometryChunk.js';
import { SCVPackedRasterProducts, SCVPackedRasterProductsT } from './SCVPackedRasterProducts.js';
import { SCVProgress, SCVProgressT } from './SCVProgress.js';
import { SCVResult, SCVResultT } from './SCVResult.js';
import { scvEnvelopeKind } from './scvEnvelopeKind.js';


export class SCV implements flatbuffers.IUnpackableObject<SCVT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCV {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCV(bb:flatbuffers.ByteBuffer, obj?:SCV):SCV {
  return (obj || new SCV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCV(bb:flatbuffers.ByteBuffer, obj?:SCV):SCV {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$SCV');
}

ENVELOPE_KIND():scvEnvelopeKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : scvEnvelopeKind.REQUEST;
}

REQUEST(obj?:SCVCoverageRequest):SCVCoverageRequest|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new SCVCoverageRequest()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

PROGRESS(obj?:SCVProgress):SCVProgress|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new SCVProgress()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

CANCEL(obj?:SCVCancel):SCVCancel|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new SCVCancel()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

RESULT(obj?:SCVResult):SCVResult|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new SCVResult()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

GEOMETRY(obj?:SCVPackedGeometryChunk):SCVPackedGeometryChunk|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new SCVPackedGeometryChunk()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

RASTER_PRODUCTS(obj?:SCVPackedRasterProducts):SCVPackedRasterProducts|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new SCVPackedRasterProducts()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startSCV(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addEnvelopeKind(builder:flatbuffers.Builder, ENVELOPE_KIND:scvEnvelopeKind) {
  builder.addFieldInt8(0, ENVELOPE_KIND, scvEnvelopeKind.REQUEST);
}

static addRequest(builder:flatbuffers.Builder, REQUESTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, REQUESTOffset, 0);
}

static addProgress(builder:flatbuffers.Builder, PROGRESSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, PROGRESSOffset, 0);
}

static addCancel(builder:flatbuffers.Builder, CANCELOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, CANCELOffset, 0);
}

static addResult(builder:flatbuffers.Builder, RESULTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, RESULTOffset, 0);
}

static addGeometry(builder:flatbuffers.Builder, GEOMETRYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, GEOMETRYOffset, 0);
}

static addRasterProducts(builder:flatbuffers.Builder, RASTER_PRODUCTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, RASTER_PRODUCTSOffset, 0);
}

static endSCV(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishSCVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SCV');
}

static finishSizePrefixedSCVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SCV', true);
}


unpack(): SCVT {
  return new SCVT(
    this.ENVELOPE_KIND(),
    (this.REQUEST() !== null ? this.REQUEST()!.unpack() : null),
    (this.PROGRESS() !== null ? this.PROGRESS()!.unpack() : null),
    (this.CANCEL() !== null ? this.CANCEL()!.unpack() : null),
    (this.RESULT() !== null ? this.RESULT()!.unpack() : null),
    (this.GEOMETRY() !== null ? this.GEOMETRY()!.unpack() : null),
    (this.RASTER_PRODUCTS() !== null ? this.RASTER_PRODUCTS()!.unpack() : null)
  );
}


unpackTo(_o: SCVT): void {
  _o.ENVELOPE_KIND = this.ENVELOPE_KIND();
  _o.REQUEST = (this.REQUEST() !== null ? this.REQUEST()!.unpack() : null);
  _o.PROGRESS = (this.PROGRESS() !== null ? this.PROGRESS()!.unpack() : null);
  _o.CANCEL = (this.CANCEL() !== null ? this.CANCEL()!.unpack() : null);
  _o.RESULT = (this.RESULT() !== null ? this.RESULT()!.unpack() : null);
  _o.GEOMETRY = (this.GEOMETRY() !== null ? this.GEOMETRY()!.unpack() : null);
  _o.RASTER_PRODUCTS = (this.RASTER_PRODUCTS() !== null ? this.RASTER_PRODUCTS()!.unpack() : null);
}
}

export class SCVT implements flatbuffers.IGeneratedObject {
constructor(
  public ENVELOPE_KIND: scvEnvelopeKind = scvEnvelopeKind.REQUEST,
  public REQUEST: SCVCoverageRequestT|null = null,
  public PROGRESS: SCVProgressT|null = null,
  public CANCEL: SCVCancelT|null = null,
  public RESULT: SCVResultT|null = null,
  public GEOMETRY: SCVPackedGeometryChunkT|null = null,
  public RASTER_PRODUCTS: SCVPackedRasterProductsT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const REQUEST = (this.REQUEST !== null ? this.REQUEST!.pack(builder) : 0);
  const PROGRESS = (this.PROGRESS !== null ? this.PROGRESS!.pack(builder) : 0);
  const CANCEL = (this.CANCEL !== null ? this.CANCEL!.pack(builder) : 0);
  const RESULT = (this.RESULT !== null ? this.RESULT!.pack(builder) : 0);
  const GEOMETRY = (this.GEOMETRY !== null ? this.GEOMETRY!.pack(builder) : 0);
  const RASTER_PRODUCTS = (this.RASTER_PRODUCTS !== null ? this.RASTER_PRODUCTS!.pack(builder) : 0);

  SCV.startSCV(builder);
  SCV.addEnvelopeKind(builder, this.ENVELOPE_KIND);
  SCV.addRequest(builder, REQUEST);
  SCV.addProgress(builder, PROGRESS);
  SCV.addCancel(builder, CANCEL);
  SCV.addResult(builder, RESULT);
  SCV.addGeometry(builder, GEOMETRY);
  SCV.addRasterProducts(builder, RASTER_PRODUCTS);

  return SCV.endSCV(builder);
}
}
