// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { scvCoordinateFrame } from './scvCoordinateFrame.js';
import { scvGeometryDomain } from './scvGeometryDomain.js';


export class SCVCoverageGrid implements flatbuffers.IUnpackableObject<SCVCoverageGridT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCVCoverageGrid {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCVCoverageGrid(bb:flatbuffers.ByteBuffer, obj?:SCVCoverageGrid):SCVCoverageGrid {
  return (obj || new SCVCoverageGrid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCVCoverageGrid(bb:flatbuffers.ByteBuffer, obj?:SCVCoverageGrid):SCVCoverageGrid {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCVCoverageGrid()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

GRID_ID():string|null
GRID_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
GRID_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

DOMAIN():scvGeometryDomain {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : scvGeometryDomain.SURFACE;
}

FRAME():scvCoordinateFrame {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : scvCoordinateFrame.UNKNOWN;
}

MIN_LAT_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MAX_LAT_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MIN_LON_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MAX_LON_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

LAT_STEP_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

LON_STEP_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

ALTITUDE_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

CELL_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

RESOLUTION_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startSCVCoverageGrid(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addGridId(builder:flatbuffers.Builder, GRID_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, GRID_IDOffset, 0);
}

static addDomain(builder:flatbuffers.Builder, DOMAIN:scvGeometryDomain) {
  builder.addFieldInt8(1, DOMAIN, scvGeometryDomain.SURFACE);
}

static addFrame(builder:flatbuffers.Builder, FRAME:scvCoordinateFrame) {
  builder.addFieldInt8(2, FRAME, scvCoordinateFrame.UNKNOWN);
}

static addMinLatDeg(builder:flatbuffers.Builder, MIN_LAT_DEG:number) {
  builder.addFieldFloat64(3, MIN_LAT_DEG, 0.0);
}

static addMaxLatDeg(builder:flatbuffers.Builder, MAX_LAT_DEG:number) {
  builder.addFieldFloat64(4, MAX_LAT_DEG, 0.0);
}

static addMinLonDeg(builder:flatbuffers.Builder, MIN_LON_DEG:number) {
  builder.addFieldFloat64(5, MIN_LON_DEG, 0.0);
}

static addMaxLonDeg(builder:flatbuffers.Builder, MAX_LON_DEG:number) {
  builder.addFieldFloat64(6, MAX_LON_DEG, 0.0);
}

static addLatStepDeg(builder:flatbuffers.Builder, LAT_STEP_DEG:number) {
  builder.addFieldFloat64(7, LAT_STEP_DEG, 0.0);
}

static addLonStepDeg(builder:flatbuffers.Builder, LON_STEP_DEG:number) {
  builder.addFieldFloat64(8, LON_STEP_DEG, 0.0);
}

static addAltitudeM(builder:flatbuffers.Builder, ALTITUDE_M:number) {
  builder.addFieldFloat64(9, ALTITUDE_M, 0.0);
}

static addCellCount(builder:flatbuffers.Builder, CELL_COUNT:number) {
  builder.addFieldInt32(10, CELL_COUNT, 0);
}

static addResolutionDeg(builder:flatbuffers.Builder, RESOLUTION_DEG:number) {
  builder.addFieldFloat64(11, RESOLUTION_DEG, 0.0);
}

static endSCVCoverageGrid(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSCVCoverageGrid(builder:flatbuffers.Builder, GRID_IDOffset:flatbuffers.Offset, DOMAIN:scvGeometryDomain, FRAME:scvCoordinateFrame, MIN_LAT_DEG:number, MAX_LAT_DEG:number, MIN_LON_DEG:number, MAX_LON_DEG:number, LAT_STEP_DEG:number, LON_STEP_DEG:number, ALTITUDE_M:number, CELL_COUNT:number, RESOLUTION_DEG:number):flatbuffers.Offset {
  SCVCoverageGrid.startSCVCoverageGrid(builder);
  SCVCoverageGrid.addGridId(builder, GRID_IDOffset);
  SCVCoverageGrid.addDomain(builder, DOMAIN);
  SCVCoverageGrid.addFrame(builder, FRAME);
  SCVCoverageGrid.addMinLatDeg(builder, MIN_LAT_DEG);
  SCVCoverageGrid.addMaxLatDeg(builder, MAX_LAT_DEG);
  SCVCoverageGrid.addMinLonDeg(builder, MIN_LON_DEG);
  SCVCoverageGrid.addMaxLonDeg(builder, MAX_LON_DEG);
  SCVCoverageGrid.addLatStepDeg(builder, LAT_STEP_DEG);
  SCVCoverageGrid.addLonStepDeg(builder, LON_STEP_DEG);
  SCVCoverageGrid.addAltitudeM(builder, ALTITUDE_M);
  SCVCoverageGrid.addCellCount(builder, CELL_COUNT);
  SCVCoverageGrid.addResolutionDeg(builder, RESOLUTION_DEG);
  return SCVCoverageGrid.endSCVCoverageGrid(builder);
}

unpack(): SCVCoverageGridT {
  return new SCVCoverageGridT(
    this.GRID_ID(),
    this.DOMAIN(),
    this.FRAME(),
    this.MIN_LAT_DEG(),
    this.MAX_LAT_DEG(),
    this.MIN_LON_DEG(),
    this.MAX_LON_DEG(),
    this.LAT_STEP_DEG(),
    this.LON_STEP_DEG(),
    this.ALTITUDE_M(),
    this.CELL_COUNT(),
    this.RESOLUTION_DEG()
  );
}


unpackTo(_o: SCVCoverageGridT): void {
  _o.GRID_ID = this.GRID_ID();
  _o.DOMAIN = this.DOMAIN();
  _o.FRAME = this.FRAME();
  _o.MIN_LAT_DEG = this.MIN_LAT_DEG();
  _o.MAX_LAT_DEG = this.MAX_LAT_DEG();
  _o.MIN_LON_DEG = this.MIN_LON_DEG();
  _o.MAX_LON_DEG = this.MAX_LON_DEG();
  _o.LAT_STEP_DEG = this.LAT_STEP_DEG();
  _o.LON_STEP_DEG = this.LON_STEP_DEG();
  _o.ALTITUDE_M = this.ALTITUDE_M();
  _o.CELL_COUNT = this.CELL_COUNT();
  _o.RESOLUTION_DEG = this.RESOLUTION_DEG();
}
}

export class SCVCoverageGridT implements flatbuffers.IGeneratedObject {
constructor(
  public GRID_ID: string|Uint8Array|null = null,
  public DOMAIN: scvGeometryDomain = scvGeometryDomain.SURFACE,
  public FRAME: scvCoordinateFrame = scvCoordinateFrame.UNKNOWN,
  public MIN_LAT_DEG: number = 0.0,
  public MAX_LAT_DEG: number = 0.0,
  public MIN_LON_DEG: number = 0.0,
  public MAX_LON_DEG: number = 0.0,
  public LAT_STEP_DEG: number = 0.0,
  public LON_STEP_DEG: number = 0.0,
  public ALTITUDE_M: number = 0.0,
  public CELL_COUNT: number = 0,
  public RESOLUTION_DEG: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const GRID_ID = (this.GRID_ID !== null ? builder.createString(this.GRID_ID!) : 0);

  return SCVCoverageGrid.createSCVCoverageGrid(builder,
    GRID_ID,
    this.DOMAIN,
    this.FRAME,
    this.MIN_LAT_DEG,
    this.MAX_LAT_DEG,
    this.MIN_LON_DEG,
    this.MAX_LON_DEG,
    this.LAT_STEP_DEG,
    this.LON_STEP_DEG,
    this.ALTITUDE_M,
    this.CELL_COUNT,
    this.RESOLUTION_DEG
  );
}
}
