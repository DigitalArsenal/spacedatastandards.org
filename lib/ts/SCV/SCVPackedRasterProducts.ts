// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SCVCoverageGrid, SCVCoverageGridT } from './SCVCoverageGrid.js';
import { SCVMemoryRegion, SCVMemoryRegionT } from './SCVMemoryRegion.js';
import { SCVPackedRasterBand, SCVPackedRasterBandT } from './SCVPackedRasterBand.js';
import { SCVTimeGrid, SCVTimeGridT } from './SCVTimeGrid.js';


export class SCVPackedRasterProducts implements flatbuffers.IUnpackableObject<SCVPackedRasterProductsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCVPackedRasterProducts {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCVPackedRasterProducts(bb:flatbuffers.ByteBuffer, obj?:SCVPackedRasterProducts):SCVPackedRasterProducts {
  return (obj || new SCVPackedRasterProducts()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCVPackedRasterProducts(bb:flatbuffers.ByteBuffer, obj?:SCVPackedRasterProducts):SCVPackedRasterProducts {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCVPackedRasterProducts()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

JOB_ID():string|null
JOB_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
JOB_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

TRACE_ID():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

GRID(obj?:SCVCoverageGrid):SCVCoverageGrid|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new SCVCoverageGrid()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

TIME_GRID(obj?:SCVTimeGrid):SCVTimeGrid|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new SCVTimeGrid()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

ROWS():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

COLUMNS():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

CELL_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

BUCKET_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

WORDS_PER_BUCKET():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

MEMORY_REGIONS(index: number, obj?:SCVMemoryRegion):SCVMemoryRegion|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new SCVMemoryRegion()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

memoryRegionsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

BANDS(index: number, obj?:SCVPackedRasterBand):SCVPackedRasterBand|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? (obj || new SCVPackedRasterBand()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

bandsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startSCVPackedRasterProducts(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addJobId(builder:flatbuffers.Builder, JOB_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, JOB_IDOffset, 0);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_ID:bigint) {
  builder.addFieldInt64(1, TRACE_ID, BigInt('0'));
}

static addGrid(builder:flatbuffers.Builder, GRIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, GRIDOffset, 0);
}

static addTimeGrid(builder:flatbuffers.Builder, TIME_GRIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, TIME_GRIDOffset, 0);
}

static addRows(builder:flatbuffers.Builder, ROWS:number) {
  builder.addFieldInt32(4, ROWS, 0);
}

static addColumns(builder:flatbuffers.Builder, COLUMNS:number) {
  builder.addFieldInt32(5, COLUMNS, 0);
}

static addCellCount(builder:flatbuffers.Builder, CELL_COUNT:number) {
  builder.addFieldInt32(6, CELL_COUNT, 0);
}

static addBucketCount(builder:flatbuffers.Builder, BUCKET_COUNT:number) {
  builder.addFieldInt32(7, BUCKET_COUNT, 0);
}

static addWordsPerBucket(builder:flatbuffers.Builder, WORDS_PER_BUCKET:number) {
  builder.addFieldInt32(8, WORDS_PER_BUCKET, 0);
}

static addMemoryRegions(builder:flatbuffers.Builder, MEMORY_REGIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, MEMORY_REGIONSOffset, 0);
}

static createMemoryRegionsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startMemoryRegionsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBands(builder:flatbuffers.Builder, BANDSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, BANDSOffset, 0);
}

static createBandsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startBandsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endSCVPackedRasterProducts(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): SCVPackedRasterProductsT {
  return new SCVPackedRasterProductsT(
    this.JOB_ID(),
    this.TRACE_ID(),
    (this.GRID() !== null ? this.GRID()!.unpack() : null),
    (this.TIME_GRID() !== null ? this.TIME_GRID()!.unpack() : null),
    this.ROWS(),
    this.COLUMNS(),
    this.CELL_COUNT(),
    this.BUCKET_COUNT(),
    this.WORDS_PER_BUCKET(),
    this.bb!.createObjList<SCVMemoryRegion, SCVMemoryRegionT>(this.MEMORY_REGIONS.bind(this), this.memoryRegionsLength()),
    this.bb!.createObjList<SCVPackedRasterBand, SCVPackedRasterBandT>(this.BANDS.bind(this), this.bandsLength())
  );
}


unpackTo(_o: SCVPackedRasterProductsT): void {
  _o.JOB_ID = this.JOB_ID();
  _o.TRACE_ID = this.TRACE_ID();
  _o.GRID = (this.GRID() !== null ? this.GRID()!.unpack() : null);
  _o.TIME_GRID = (this.TIME_GRID() !== null ? this.TIME_GRID()!.unpack() : null);
  _o.ROWS = this.ROWS();
  _o.COLUMNS = this.COLUMNS();
  _o.CELL_COUNT = this.CELL_COUNT();
  _o.BUCKET_COUNT = this.BUCKET_COUNT();
  _o.WORDS_PER_BUCKET = this.WORDS_PER_BUCKET();
  _o.MEMORY_REGIONS = this.bb!.createObjList<SCVMemoryRegion, SCVMemoryRegionT>(this.MEMORY_REGIONS.bind(this), this.memoryRegionsLength());
  _o.BANDS = this.bb!.createObjList<SCVPackedRasterBand, SCVPackedRasterBandT>(this.BANDS.bind(this), this.bandsLength());
}
}

export class SCVPackedRasterProductsT implements flatbuffers.IGeneratedObject {
constructor(
  public JOB_ID: string|Uint8Array|null = null,
  public TRACE_ID: bigint = BigInt('0'),
  public GRID: SCVCoverageGridT|null = null,
  public TIME_GRID: SCVTimeGridT|null = null,
  public ROWS: number = 0,
  public COLUMNS: number = 0,
  public CELL_COUNT: number = 0,
  public BUCKET_COUNT: number = 0,
  public WORDS_PER_BUCKET: number = 0,
  public MEMORY_REGIONS: (SCVMemoryRegionT)[] = [],
  public BANDS: (SCVPackedRasterBandT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const JOB_ID = (this.JOB_ID !== null ? builder.createString(this.JOB_ID!) : 0);
  const GRID = (this.GRID !== null ? this.GRID!.pack(builder) : 0);
  const TIME_GRID = (this.TIME_GRID !== null ? this.TIME_GRID!.pack(builder) : 0);
  const MEMORY_REGIONS = SCVPackedRasterProducts.createMemoryRegionsVector(builder, builder.createObjectOffsetList(this.MEMORY_REGIONS));
  const BANDS = SCVPackedRasterProducts.createBandsVector(builder, builder.createObjectOffsetList(this.BANDS));

  SCVPackedRasterProducts.startSCVPackedRasterProducts(builder);
  SCVPackedRasterProducts.addJobId(builder, JOB_ID);
  SCVPackedRasterProducts.addTraceId(builder, this.TRACE_ID);
  SCVPackedRasterProducts.addGrid(builder, GRID);
  SCVPackedRasterProducts.addTimeGrid(builder, TIME_GRID);
  SCVPackedRasterProducts.addRows(builder, this.ROWS);
  SCVPackedRasterProducts.addColumns(builder, this.COLUMNS);
  SCVPackedRasterProducts.addCellCount(builder, this.CELL_COUNT);
  SCVPackedRasterProducts.addBucketCount(builder, this.BUCKET_COUNT);
  SCVPackedRasterProducts.addWordsPerBucket(builder, this.WORDS_PER_BUCKET);
  SCVPackedRasterProducts.addMemoryRegions(builder, MEMORY_REGIONS);
  SCVPackedRasterProducts.addBands(builder, BANDS);

  return SCVPackedRasterProducts.endSCVPackedRasterProducts(builder);
}
}
