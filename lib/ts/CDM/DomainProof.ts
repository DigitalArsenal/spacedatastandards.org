// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Proves control of a DNS domain by the same HD wallet as the signing key —
 * the EPM-record mirror of the DNS TXT wire form (which is deliberately
 * NON-SDS: a base64 FlatBuffer would not fit a 255-byte TXT record). The
 * canonical statement is 6 fixed LF-terminated lines prefixed
 * "sdn-domain-proof/1", and it verifies by BYTE-REPLAY of the verbatim
 * SIGNED_PAYLOAD (ChainProof precedent) — never by JCS.
 */
export class DomainProof implements flatbuffers.IUnpackableObject<DomainProofT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DomainProof {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDomainProof(bb:flatbuffers.ByteBuffer, obj?:DomainProof):DomainProof {
  return (obj || new DomainProof()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDomainProof(bb:flatbuffers.ByteBuffer, obj?:DomainProof):DomainProof {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DomainProof()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Fully-qualified domain name the proof binds (e.g., "sdn.spaceaware.io")
 */
DOMAIN():string|null
DOMAIN(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DOMAIN(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Public key for this proof (hex-encoded)
 */
PUBLIC_KEY():string|null
PUBLIC_KEY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PUBLIC_KEY(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * BIP-32 / SLIP-10 derivation path of the proving key. The signer is the
 * Ed25519 EPM/publication key at m/44'/0'/0'/0'/0'
 */
KEY_PATH():string|null
KEY_PATH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
KEY_PATH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Signature over the attestation payload (hex-encoded)
 */
SIGNATURE():string|null
SIGNATURE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SIGNATURE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The canonical payload that was signed, verbatim (hex-encoded)
 */
SIGNED_PAYLOAD():string|null
SIGNED_PAYLOAD(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SIGNED_PAYLOAD(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Signature algorithm. ABSENT means ed25519
 */
ALGORITHM():string|null
ALGORITHM(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ALGORITHM(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Signature encoding format. ABSENT means the canonical encoding of
 * ALGORITHM (raw-ed25519 for ed25519)
 */
ENCODING():string|null
ENCODING(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ENCODING(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startDomainProof(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addDomain(builder:flatbuffers.Builder, DOMAINOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, DOMAINOffset, 0);
}

static addPublicKey(builder:flatbuffers.Builder, PUBLIC_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, PUBLIC_KEYOffset, 0);
}

static addKeyPath(builder:flatbuffers.Builder, KEY_PATHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, KEY_PATHOffset, 0);
}

static addSignature(builder:flatbuffers.Builder, SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, SIGNATUREOffset, 0);
}

static addSignedPayload(builder:flatbuffers.Builder, SIGNED_PAYLOADOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, SIGNED_PAYLOADOffset, 0);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ALGORITHMOffset, 0);
}

static addEncoding(builder:flatbuffers.Builder, ENCODINGOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, ENCODINGOffset, 0);
}

static endDomainProof(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createDomainProof(builder:flatbuffers.Builder, DOMAINOffset:flatbuffers.Offset, PUBLIC_KEYOffset:flatbuffers.Offset, KEY_PATHOffset:flatbuffers.Offset, SIGNATUREOffset:flatbuffers.Offset, SIGNED_PAYLOADOffset:flatbuffers.Offset, ALGORITHMOffset:flatbuffers.Offset, ENCODINGOffset:flatbuffers.Offset):flatbuffers.Offset {
  DomainProof.startDomainProof(builder);
  DomainProof.addDomain(builder, DOMAINOffset);
  DomainProof.addPublicKey(builder, PUBLIC_KEYOffset);
  DomainProof.addKeyPath(builder, KEY_PATHOffset);
  DomainProof.addSignature(builder, SIGNATUREOffset);
  DomainProof.addSignedPayload(builder, SIGNED_PAYLOADOffset);
  DomainProof.addAlgorithm(builder, ALGORITHMOffset);
  DomainProof.addEncoding(builder, ENCODINGOffset);
  return DomainProof.endDomainProof(builder);
}

unpack(): DomainProofT {
  return new DomainProofT(
    this.DOMAIN(),
    this.PUBLIC_KEY(),
    this.KEY_PATH(),
    this.SIGNATURE(),
    this.SIGNED_PAYLOAD(),
    this.ALGORITHM(),
    this.ENCODING()
  );
}


unpackTo(_o: DomainProofT): void {
  _o.DOMAIN = this.DOMAIN();
  _o.PUBLIC_KEY = this.PUBLIC_KEY();
  _o.KEY_PATH = this.KEY_PATH();
  _o.SIGNATURE = this.SIGNATURE();
  _o.SIGNED_PAYLOAD = this.SIGNED_PAYLOAD();
  _o.ALGORITHM = this.ALGORITHM();
  _o.ENCODING = this.ENCODING();
}
}

export class DomainProofT implements flatbuffers.IGeneratedObject {
constructor(
  public DOMAIN: string|Uint8Array|null = null,
  public PUBLIC_KEY: string|Uint8Array|null = null,
  public KEY_PATH: string|Uint8Array|null = null,
  public SIGNATURE: string|Uint8Array|null = null,
  public SIGNED_PAYLOAD: string|Uint8Array|null = null,
  public ALGORITHM: string|Uint8Array|null = null,
  public ENCODING: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const DOMAIN = (this.DOMAIN !== null ? builder.createString(this.DOMAIN!) : 0);
  const PUBLIC_KEY = (this.PUBLIC_KEY !== null ? builder.createString(this.PUBLIC_KEY!) : 0);
  const KEY_PATH = (this.KEY_PATH !== null ? builder.createString(this.KEY_PATH!) : 0);
  const SIGNATURE = (this.SIGNATURE !== null ? builder.createString(this.SIGNATURE!) : 0);
  const SIGNED_PAYLOAD = (this.SIGNED_PAYLOAD !== null ? builder.createString(this.SIGNED_PAYLOAD!) : 0);
  const ALGORITHM = (this.ALGORITHM !== null ? builder.createString(this.ALGORITHM!) : 0);
  const ENCODING = (this.ENCODING !== null ? builder.createString(this.ENCODING!) : 0);

  return DomainProof.createDomainProof(builder,
    DOMAIN,
    PUBLIC_KEY,
    KEY_PATH,
    SIGNATURE,
    SIGNED_PAYLOAD,
    ALGORITHM,
    ENCODING
  );
}
}
