// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Reentry Ground Impact Prediction
 */
export class reentryImpact implements flatbuffers.IUnpackableObject<reentryImpactT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):reentryImpact {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsreentryImpact(bb:flatbuffers.ByteBuffer, obj?:reentryImpact):reentryImpact {
  return (obj || new reentryImpact()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsreentryImpact(bb:flatbuffers.ByteBuffer, obj?:reentryImpact):reentryImpact {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new reentryImpact()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Predicted impact epoch (ISO 8601)
 */
IMPACT_EPOCH():string|null
IMPACT_EPOCH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
IMPACT_EPOCH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Epoch uncertainty window in seconds
 */
EPOCH_UNCERTAINTY():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Impact latitude in degrees
 */
LATITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Impact longitude in degrees
 */
LONGITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Along-track uncertainty in km
 */
ALONG_TRACK_UNC():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Cross-track uncertainty in km
 */
CROSS_TRACK_UNC():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Impact probability (0.0-1.0)
 */
IMPACT_PROBABILITY():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startreentryImpact(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addImpactEpoch(builder:flatbuffers.Builder, IMPACT_EPOCHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, IMPACT_EPOCHOffset, 0);
}

static addEpochUncertainty(builder:flatbuffers.Builder, EPOCH_UNCERTAINTY:number) {
  builder.addFieldFloat64(1, EPOCH_UNCERTAINTY, 0.0);
}

static addLatitude(builder:flatbuffers.Builder, LATITUDE:number) {
  builder.addFieldFloat64(2, LATITUDE, 0.0);
}

static addLongitude(builder:flatbuffers.Builder, LONGITUDE:number) {
  builder.addFieldFloat64(3, LONGITUDE, 0.0);
}

static addAlongTrackUnc(builder:flatbuffers.Builder, ALONG_TRACK_UNC:number) {
  builder.addFieldFloat64(4, ALONG_TRACK_UNC, 0.0);
}

static addCrossTrackUnc(builder:flatbuffers.Builder, CROSS_TRACK_UNC:number) {
  builder.addFieldFloat64(5, CROSS_TRACK_UNC, 0.0);
}

static addImpactProbability(builder:flatbuffers.Builder, IMPACT_PROBABILITY:number) {
  builder.addFieldFloat64(6, IMPACT_PROBABILITY, 0.0);
}

static endreentryImpact(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createreentryImpact(builder:flatbuffers.Builder, IMPACT_EPOCHOffset:flatbuffers.Offset, EPOCH_UNCERTAINTY:number, LATITUDE:number, LONGITUDE:number, ALONG_TRACK_UNC:number, CROSS_TRACK_UNC:number, IMPACT_PROBABILITY:number):flatbuffers.Offset {
  reentryImpact.startreentryImpact(builder);
  reentryImpact.addImpactEpoch(builder, IMPACT_EPOCHOffset);
  reentryImpact.addEpochUncertainty(builder, EPOCH_UNCERTAINTY);
  reentryImpact.addLatitude(builder, LATITUDE);
  reentryImpact.addLongitude(builder, LONGITUDE);
  reentryImpact.addAlongTrackUnc(builder, ALONG_TRACK_UNC);
  reentryImpact.addCrossTrackUnc(builder, CROSS_TRACK_UNC);
  reentryImpact.addImpactProbability(builder, IMPACT_PROBABILITY);
  return reentryImpact.endreentryImpact(builder);
}

unpack(): reentryImpactT {
  return new reentryImpactT(
    this.IMPACT_EPOCH(),
    this.EPOCH_UNCERTAINTY(),
    this.LATITUDE(),
    this.LONGITUDE(),
    this.ALONG_TRACK_UNC(),
    this.CROSS_TRACK_UNC(),
    this.IMPACT_PROBABILITY()
  );
}


unpackTo(_o: reentryImpactT): void {
  _o.IMPACT_EPOCH = this.IMPACT_EPOCH();
  _o.EPOCH_UNCERTAINTY = this.EPOCH_UNCERTAINTY();
  _o.LATITUDE = this.LATITUDE();
  _o.LONGITUDE = this.LONGITUDE();
  _o.ALONG_TRACK_UNC = this.ALONG_TRACK_UNC();
  _o.CROSS_TRACK_UNC = this.CROSS_TRACK_UNC();
  _o.IMPACT_PROBABILITY = this.IMPACT_PROBABILITY();
}
}

export class reentryImpactT implements flatbuffers.IGeneratedObject {
constructor(
  public IMPACT_EPOCH: string|Uint8Array|null = null,
  public EPOCH_UNCERTAINTY: number = 0.0,
  public LATITUDE: number = 0.0,
  public LONGITUDE: number = 0.0,
  public ALONG_TRACK_UNC: number = 0.0,
  public CROSS_TRACK_UNC: number = 0.0,
  public IMPACT_PROBABILITY: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const IMPACT_EPOCH = (this.IMPACT_EPOCH !== null ? builder.createString(this.IMPACT_EPOCH!) : 0);

  return reentryImpact.createreentryImpact(builder,
    IMPACT_EPOCH,
    this.EPOCH_UNCERTAINTY,
    this.LATITUDE,
    this.LONGITUDE,
    this.ALONG_TRACK_UNC,
    this.CROSS_TRACK_UNC,
    this.IMPACT_PROBABILITY
  );
}
}
