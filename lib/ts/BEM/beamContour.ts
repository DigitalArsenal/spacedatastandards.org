// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { beamContourPoint, beamContourPointT } from './beamContourPoint.js';


/**
 * Beam Contour (iso-gain boundary)
 */
export class beamContour implements flatbuffers.IUnpackableObject<beamContourT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):beamContour {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsbeamContour(bb:flatbuffers.ByteBuffer, obj?:beamContour):beamContour {
  return (obj || new beamContour()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsbeamContour(bb:flatbuffers.ByteBuffer, obj?:beamContour):beamContour {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new beamContour()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Contour level identifier
 */
CONTOUR_ID():string|null
CONTOUR_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CONTOUR_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Gain level in dBi
 */
GAIN_LEVEL():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Contour boundary points
 */
POINTS(index: number, obj?:beamContourPoint):beamContourPoint|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new beamContourPoint()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

pointsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startbeamContour(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addContourId(builder:flatbuffers.Builder, CONTOUR_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, CONTOUR_IDOffset, 0);
}

static addGainLevel(builder:flatbuffers.Builder, GAIN_LEVEL:number) {
  builder.addFieldFloat64(1, GAIN_LEVEL, 0.0);
}

static addPoints(builder:flatbuffers.Builder, POINTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, POINTSOffset, 0);
}

static createPointsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPointsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endbeamContour(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createbeamContour(builder:flatbuffers.Builder, CONTOUR_IDOffset:flatbuffers.Offset, GAIN_LEVEL:number, POINTSOffset:flatbuffers.Offset):flatbuffers.Offset {
  beamContour.startbeamContour(builder);
  beamContour.addContourId(builder, CONTOUR_IDOffset);
  beamContour.addGainLevel(builder, GAIN_LEVEL);
  beamContour.addPoints(builder, POINTSOffset);
  return beamContour.endbeamContour(builder);
}

unpack(): beamContourT {
  return new beamContourT(
    this.CONTOUR_ID(),
    this.GAIN_LEVEL(),
    this.bb!.createObjList<beamContourPoint, beamContourPointT>(this.POINTS.bind(this), this.pointsLength())
  );
}


unpackTo(_o: beamContourT): void {
  _o.CONTOUR_ID = this.CONTOUR_ID();
  _o.GAIN_LEVEL = this.GAIN_LEVEL();
  _o.POINTS = this.bb!.createObjList<beamContourPoint, beamContourPointT>(this.POINTS.bind(this), this.pointsLength());
}
}

export class beamContourT implements flatbuffers.IGeneratedObject {
constructor(
  public CONTOUR_ID: string|Uint8Array|null = null,
  public GAIN_LEVEL: number = 0.0,
  public POINTS: (beamContourPointT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CONTOUR_ID = (this.CONTOUR_ID !== null ? builder.createString(this.CONTOUR_ID!) : 0);
  const POINTS = beamContour.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));

  return beamContour.createbeamContour(builder,
    CONTOUR_ID,
    this.GAIN_LEVEL,
    POINTS
  );
}
}
