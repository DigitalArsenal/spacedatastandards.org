// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { BEMHopSlot, BEMHopSlotT } from './BEMHopSlot.js';
import { BEMProvenance, BEMProvenanceT } from './BEMProvenance.js';


/**
 * Periodic beam-hopping schedule carried by a deployed beam descriptor.
 */
export class BEMHopSchedule implements flatbuffers.IUnpackableObject<BEMHopScheduleT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BEMHopSchedule {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBEMHopSchedule(bb:flatbuffers.ByteBuffer, obj?:BEMHopSchedule):BEMHopSchedule {
  return (obj || new BEMHopSchedule()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBEMHopSchedule(bb:flatbuffers.ByteBuffer, obj?:BEMHopSchedule):BEMHopSchedule {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BEMHopSchedule()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

SCHEDULE_ID():string
SCHEDULE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
SCHEDULE_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Seconds since 1970-01-01T00:00:00 UTC at which slot offsets begin.
 */
EPOCH():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

PERIOD_S():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

REPEATS():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

SLOTS(index: number, obj?:BEMHopSlot):BEMHopSlot|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new BEMHopSlot()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

slotsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

PROVENANCE(obj?:BEMProvenance):BEMProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new BEMProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startBEMHopSchedule(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addScheduleId(builder:flatbuffers.Builder, SCHEDULE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SCHEDULE_IDOffset, 0);
}

static addEpoch(builder:flatbuffers.Builder, EPOCH:number) {
  builder.addFieldFloat64(1, EPOCH, 0.0);
}

static addPeriodS(builder:flatbuffers.Builder, PERIOD_S:number) {
  builder.addFieldFloat64(2, PERIOD_S, 0.0);
}

static addRepeats(builder:flatbuffers.Builder, REPEATS:boolean) {
  builder.addFieldInt8(3, +REPEATS, +false);
}

static addSlots(builder:flatbuffers.Builder, SLOTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, SLOTSOffset, 0);
}

static createSlotsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSlotsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addProvenance(builder:flatbuffers.Builder, PROVENANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, PROVENANCEOffset, 0);
}

static endBEMHopSchedule(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // SCHEDULE_ID
  builder.requiredField(offset, 12) // SLOTS
  builder.requiredField(offset, 14) // PROVENANCE
  return offset;
}


unpack(): BEMHopScheduleT {
  return new BEMHopScheduleT(
    this.SCHEDULE_ID(),
    this.EPOCH(),
    this.PERIOD_S(),
    this.REPEATS(),
    this.bb!.createObjList<BEMHopSlot, BEMHopSlotT>(this.SLOTS.bind(this), this.slotsLength()),
    (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null)
  );
}


unpackTo(_o: BEMHopScheduleT): void {
  _o.SCHEDULE_ID = this.SCHEDULE_ID();
  _o.EPOCH = this.EPOCH();
  _o.PERIOD_S = this.PERIOD_S();
  _o.REPEATS = this.REPEATS();
  _o.SLOTS = this.bb!.createObjList<BEMHopSlot, BEMHopSlotT>(this.SLOTS.bind(this), this.slotsLength());
  _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null);
}
}

export class BEMHopScheduleT implements flatbuffers.IGeneratedObject {
constructor(
  public SCHEDULE_ID: string|Uint8Array|null = null,
  public EPOCH: number = 0.0,
  public PERIOD_S: number = 0.0,
  public REPEATS: boolean = false,
  public SLOTS: (BEMHopSlotT)[] = [],
  public PROVENANCE: BEMProvenanceT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SCHEDULE_ID = (this.SCHEDULE_ID !== null ? builder.createString(this.SCHEDULE_ID!) : 0);
  const SLOTS = BEMHopSchedule.createSlotsVector(builder, builder.createObjectOffsetList(this.SLOTS));
  const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE!.pack(builder) : 0);

  BEMHopSchedule.startBEMHopSchedule(builder);
  BEMHopSchedule.addScheduleId(builder, SCHEDULE_ID);
  BEMHopSchedule.addEpoch(builder, this.EPOCH);
  BEMHopSchedule.addPeriodS(builder, this.PERIOD_S);
  BEMHopSchedule.addRepeats(builder, this.REPEATS);
  BEMHopSchedule.addSlots(builder, SLOTS);
  BEMHopSchedule.addProvenance(builder, PROVENANCE);

  return BEMHopSchedule.endBEMHopSchedule(builder);
}
}
