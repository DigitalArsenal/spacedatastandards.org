// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Geodetic position of the receiver at capture time.
 *
 * A separate table because latitude/longitude 0,0 is a VALID coordinate: an
 * absent GEOLOCATION means "the source published no position", which a
 * zero-defaulted scalar pair could never express.
 */
export class IQCGeolocation implements flatbuffers.IUnpackableObject<IQCGeolocationT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IQCGeolocation {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIQCGeolocation(bb:flatbuffers.ByteBuffer, obj?:IQCGeolocation):IQCGeolocation {
  return (obj || new IQCGeolocation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIQCGeolocation(bb:flatbuffers.ByteBuffer, obj?:IQCGeolocation):IQCGeolocation {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IQCGeolocation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Geodetic latitude, degrees, WGS-84, positive north.
 */
LATITUDE_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Geodetic longitude, degrees, WGS-84, positive east.
 */
LONGITUDE_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Height above the WGS-84 ellipsoid, metres. Absent in most SigMF
 * recordings; SigMF `core:geolocation` is a GeoJSON Point whose third
 * ordinate is optional.
 */
ALTITUDE_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Reported position uncertainty, metres, when the source states one.
 */
UNCERTAINTY_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * How the position was obtained, verbatim from the source (e.g. "GPS",
 * "station registry", "author-supplied"). Never inferred.
 */
METHOD():string|null
METHOD(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
METHOD(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startIQCGeolocation(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addLatitudeDeg(builder:flatbuffers.Builder, LATITUDE_DEG:number) {
  builder.addFieldFloat64(0, LATITUDE_DEG, 0.0);
}

static addLongitudeDeg(builder:flatbuffers.Builder, LONGITUDE_DEG:number) {
  builder.addFieldFloat64(1, LONGITUDE_DEG, 0.0);
}

static addAltitudeM(builder:flatbuffers.Builder, ALTITUDE_M:number) {
  builder.addFieldFloat64(2, ALTITUDE_M, 0.0);
}

static addUncertaintyM(builder:flatbuffers.Builder, UNCERTAINTY_M:number) {
  builder.addFieldFloat64(3, UNCERTAINTY_M, 0.0);
}

static addMethod(builder:flatbuffers.Builder, METHODOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, METHODOffset, 0);
}

static endIQCGeolocation(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIQCGeolocation(builder:flatbuffers.Builder, LATITUDE_DEG:number, LONGITUDE_DEG:number, ALTITUDE_M:number, UNCERTAINTY_M:number, METHODOffset:flatbuffers.Offset):flatbuffers.Offset {
  IQCGeolocation.startIQCGeolocation(builder);
  IQCGeolocation.addLatitudeDeg(builder, LATITUDE_DEG);
  IQCGeolocation.addLongitudeDeg(builder, LONGITUDE_DEG);
  IQCGeolocation.addAltitudeM(builder, ALTITUDE_M);
  IQCGeolocation.addUncertaintyM(builder, UNCERTAINTY_M);
  IQCGeolocation.addMethod(builder, METHODOffset);
  return IQCGeolocation.endIQCGeolocation(builder);
}

unpack(): IQCGeolocationT {
  return new IQCGeolocationT(
    this.LATITUDE_DEG(),
    this.LONGITUDE_DEG(),
    this.ALTITUDE_M(),
    this.UNCERTAINTY_M(),
    this.METHOD()
  );
}


unpackTo(_o: IQCGeolocationT): void {
  _o.LATITUDE_DEG = this.LATITUDE_DEG();
  _o.LONGITUDE_DEG = this.LONGITUDE_DEG();
  _o.ALTITUDE_M = this.ALTITUDE_M();
  _o.UNCERTAINTY_M = this.UNCERTAINTY_M();
  _o.METHOD = this.METHOD();
}
}

export class IQCGeolocationT implements flatbuffers.IGeneratedObject {
constructor(
  public LATITUDE_DEG: number = 0.0,
  public LONGITUDE_DEG: number = 0.0,
  public ALTITUDE_M: number = 0.0,
  public UNCERTAINTY_M: number = 0.0,
  public METHOD: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const METHOD = (this.METHOD !== null ? builder.createString(this.METHOD!) : 0);

  return IQCGeolocation.createIQCGeolocation(builder,
    this.LATITUDE_DEG,
    this.LONGITUDE_DEG,
    this.ALTITUDE_M,
    this.UNCERTAINTY_M,
    METHOD
  );
}
}
