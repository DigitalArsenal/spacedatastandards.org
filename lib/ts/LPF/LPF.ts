// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { licensingProofMessageType } from './licensingProofMessageType.js';


/**
 * Licensing proof message
 */
export class LPF implements flatbuffers.IUnpackableObject<LPFT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LPF {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLPF(bb:flatbuffers.ByteBuffer, obj?:LPF):LPF {
  return (obj || new LPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLPF(bb:flatbuffers.ByteBuffer, obj?:LPF):LPF {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$LPF');
}

/**
 * Message type
 */
MESSAGE_TYPE():licensingProofMessageType {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : licensingProofMessageType.ProofRequest;
}

/**
 * Unique request identifier
 */
REQUEST_ID():string|null
REQUEST_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REQUEST_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Canonical module identifier
 */
MODULE_ID():string|null
MODULE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MODULE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Optional requested module version
 */
MODULE_VERSION():string|null
MODULE_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MODULE_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Requester peer identifier
 */
REQUESTER_PEER_ID():string|null
REQUESTER_PEER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REQUESTER_PEER_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Requester account identity or wallet xpub
 */
REQUESTER_XPUB():string|null
REQUESTER_XPUB(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REQUESTER_XPUB(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Requested domain proved for policy evaluation
 */
REQUESTED_DOMAIN():string|null
REQUESTED_DOMAIN(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REQUESTED_DOMAIN(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Requested timeout in milliseconds
 */
REQUESTED_TIMEOUT_MS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Requester ephemeral X25519 public key
 */
REQUESTER_EPHEMERAL_PUBKEY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

requesterEphemeralPubkeyLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

requesterEphemeralPubkeyArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Provider-issued challenge nonce being proved
 */
CHALLENGE_NONCE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

challengeNonceLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

challengeNonceArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Challenge expiration time being acknowledged
 */
CHALLENGE_EXPIRES_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Provider peer identifier that issued the challenge
 */
PROVIDER_PEER_ID():string|null
PROVIDER_PEER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROVIDER_PEER_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Detached requester signature over the challenge payload
 */
SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signatureArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Requester signing public key
 */
SIGNING_PUBKEY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signingPubkeyLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signingPubkeyArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Proof timestamp in milliseconds since epoch
 */
TIMESTAMP_MS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Structured rejection code
 */
REJECTION_CODE():string|null
REJECTION_CODE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REJECTION_CODE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Structured rejection message
 */
REJECTION_MESSAGE():string|null
REJECTION_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REJECTION_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startLPF(builder:flatbuffers.Builder) {
  builder.startObject(17);
}

static addMessageType(builder:flatbuffers.Builder, MESSAGE_TYPE:licensingProofMessageType) {
  builder.addFieldInt8(0, MESSAGE_TYPE, licensingProofMessageType.ProofRequest);
}

static addRequestId(builder:flatbuffers.Builder, REQUEST_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, REQUEST_IDOffset, 0);
}

static addModuleId(builder:flatbuffers.Builder, MODULE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, MODULE_IDOffset, 0);
}

static addModuleVersion(builder:flatbuffers.Builder, MODULE_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, MODULE_VERSIONOffset, 0);
}

static addRequesterPeerId(builder:flatbuffers.Builder, REQUESTER_PEER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, REQUESTER_PEER_IDOffset, 0);
}

static addRequesterXpub(builder:flatbuffers.Builder, REQUESTER_XPUBOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, REQUESTER_XPUBOffset, 0);
}

static addRequestedDomain(builder:flatbuffers.Builder, REQUESTED_DOMAINOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, REQUESTED_DOMAINOffset, 0);
}

static addRequestedTimeoutMs(builder:flatbuffers.Builder, REQUESTED_TIMEOUT_MS:bigint) {
  builder.addFieldInt64(7, REQUESTED_TIMEOUT_MS, BigInt('0'));
}

static addRequesterEphemeralPubkey(builder:flatbuffers.Builder, REQUESTER_EPHEMERAL_PUBKEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, REQUESTER_EPHEMERAL_PUBKEYOffset, 0);
}

static createRequesterEphemeralPubkeyVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startRequesterEphemeralPubkeyVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addChallengeNonce(builder:flatbuffers.Builder, CHALLENGE_NONCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, CHALLENGE_NONCEOffset, 0);
}

static createChallengeNonceVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startChallengeNonceVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addChallengeExpiresAt(builder:flatbuffers.Builder, CHALLENGE_EXPIRES_AT:bigint) {
  builder.addFieldInt64(10, CHALLENGE_EXPIRES_AT, BigInt('0'));
}

static addProviderPeerId(builder:flatbuffers.Builder, PROVIDER_PEER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, PROVIDER_PEER_IDOffset, 0);
}

static addSignature(builder:flatbuffers.Builder, SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, SIGNATUREOffset, 0);
}

static createSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addSigningPubkey(builder:flatbuffers.Builder, SIGNING_PUBKEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, SIGNING_PUBKEYOffset, 0);
}

static createSigningPubkeyVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSigningPubkeyVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addTimestampMs(builder:flatbuffers.Builder, TIMESTAMP_MS:bigint) {
  builder.addFieldInt64(14, TIMESTAMP_MS, BigInt('0'));
}

static addRejectionCode(builder:flatbuffers.Builder, REJECTION_CODEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(15, REJECTION_CODEOffset, 0);
}

static addRejectionMessage(builder:flatbuffers.Builder, REJECTION_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(16, REJECTION_MESSAGEOffset, 0);
}

static endLPF(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // REQUEST_ID
  builder.requiredField(offset, 8) // MODULE_ID
  return offset;
}

static finishLPFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$LPF');
}

static finishSizePrefixedLPFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$LPF', true);
}

static createLPF(builder:flatbuffers.Builder, MESSAGE_TYPE:licensingProofMessageType, REQUEST_IDOffset:flatbuffers.Offset, MODULE_IDOffset:flatbuffers.Offset, MODULE_VERSIONOffset:flatbuffers.Offset, REQUESTER_PEER_IDOffset:flatbuffers.Offset, REQUESTER_XPUBOffset:flatbuffers.Offset, REQUESTED_DOMAINOffset:flatbuffers.Offset, REQUESTED_TIMEOUT_MS:bigint, REQUESTER_EPHEMERAL_PUBKEYOffset:flatbuffers.Offset, CHALLENGE_NONCEOffset:flatbuffers.Offset, CHALLENGE_EXPIRES_AT:bigint, PROVIDER_PEER_IDOffset:flatbuffers.Offset, SIGNATUREOffset:flatbuffers.Offset, SIGNING_PUBKEYOffset:flatbuffers.Offset, TIMESTAMP_MS:bigint, REJECTION_CODEOffset:flatbuffers.Offset, REJECTION_MESSAGEOffset:flatbuffers.Offset):flatbuffers.Offset {
  LPF.startLPF(builder);
  LPF.addMessageType(builder, MESSAGE_TYPE);
  LPF.addRequestId(builder, REQUEST_IDOffset);
  LPF.addModuleId(builder, MODULE_IDOffset);
  LPF.addModuleVersion(builder, MODULE_VERSIONOffset);
  LPF.addRequesterPeerId(builder, REQUESTER_PEER_IDOffset);
  LPF.addRequesterXpub(builder, REQUESTER_XPUBOffset);
  LPF.addRequestedDomain(builder, REQUESTED_DOMAINOffset);
  LPF.addRequestedTimeoutMs(builder, REQUESTED_TIMEOUT_MS);
  LPF.addRequesterEphemeralPubkey(builder, REQUESTER_EPHEMERAL_PUBKEYOffset);
  LPF.addChallengeNonce(builder, CHALLENGE_NONCEOffset);
  LPF.addChallengeExpiresAt(builder, CHALLENGE_EXPIRES_AT);
  LPF.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
  LPF.addSignature(builder, SIGNATUREOffset);
  LPF.addSigningPubkey(builder, SIGNING_PUBKEYOffset);
  LPF.addTimestampMs(builder, TIMESTAMP_MS);
  LPF.addRejectionCode(builder, REJECTION_CODEOffset);
  LPF.addRejectionMessage(builder, REJECTION_MESSAGEOffset);
  return LPF.endLPF(builder);
}

unpack(): LPFT {
  return new LPFT(
    this.MESSAGE_TYPE(),
    this.REQUEST_ID(),
    this.MODULE_ID(),
    this.MODULE_VERSION(),
    this.REQUESTER_PEER_ID(),
    this.REQUESTER_XPUB(),
    this.REQUESTED_DOMAIN(),
    this.REQUESTED_TIMEOUT_MS(),
    this.bb!.createScalarList<number>(this.REQUESTER_EPHEMERAL_PUBKEY.bind(this), this.requesterEphemeralPubkeyLength()),
    this.bb!.createScalarList<number>(this.CHALLENGE_NONCE.bind(this), this.challengeNonceLength()),
    this.CHALLENGE_EXPIRES_AT(),
    this.PROVIDER_PEER_ID(),
    this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength()),
    this.bb!.createScalarList<number>(this.SIGNING_PUBKEY.bind(this), this.signingPubkeyLength()),
    this.TIMESTAMP_MS(),
    this.REJECTION_CODE(),
    this.REJECTION_MESSAGE()
  );
}


unpackTo(_o: LPFT): void {
  _o.MESSAGE_TYPE = this.MESSAGE_TYPE();
  _o.REQUEST_ID = this.REQUEST_ID();
  _o.MODULE_ID = this.MODULE_ID();
  _o.MODULE_VERSION = this.MODULE_VERSION();
  _o.REQUESTER_PEER_ID = this.REQUESTER_PEER_ID();
  _o.REQUESTER_XPUB = this.REQUESTER_XPUB();
  _o.REQUESTED_DOMAIN = this.REQUESTED_DOMAIN();
  _o.REQUESTED_TIMEOUT_MS = this.REQUESTED_TIMEOUT_MS();
  _o.REQUESTER_EPHEMERAL_PUBKEY = this.bb!.createScalarList<number>(this.REQUESTER_EPHEMERAL_PUBKEY.bind(this), this.requesterEphemeralPubkeyLength());
  _o.CHALLENGE_NONCE = this.bb!.createScalarList<number>(this.CHALLENGE_NONCE.bind(this), this.challengeNonceLength());
  _o.CHALLENGE_EXPIRES_AT = this.CHALLENGE_EXPIRES_AT();
  _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
  _o.SIGNATURE = this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength());
  _o.SIGNING_PUBKEY = this.bb!.createScalarList<number>(this.SIGNING_PUBKEY.bind(this), this.signingPubkeyLength());
  _o.TIMESTAMP_MS = this.TIMESTAMP_MS();
  _o.REJECTION_CODE = this.REJECTION_CODE();
  _o.REJECTION_MESSAGE = this.REJECTION_MESSAGE();
}
}

export class LPFT implements flatbuffers.IGeneratedObject {
constructor(
  public MESSAGE_TYPE: licensingProofMessageType = licensingProofMessageType.ProofRequest,
  public REQUEST_ID: string|Uint8Array|null = null,
  public MODULE_ID: string|Uint8Array|null = null,
  public MODULE_VERSION: string|Uint8Array|null = null,
  public REQUESTER_PEER_ID: string|Uint8Array|null = null,
  public REQUESTER_XPUB: string|Uint8Array|null = null,
  public REQUESTED_DOMAIN: string|Uint8Array|null = null,
  public REQUESTED_TIMEOUT_MS: bigint = BigInt('0'),
  public REQUESTER_EPHEMERAL_PUBKEY: (number)[] = [],
  public CHALLENGE_NONCE: (number)[] = [],
  public CHALLENGE_EXPIRES_AT: bigint = BigInt('0'),
  public PROVIDER_PEER_ID: string|Uint8Array|null = null,
  public SIGNATURE: (number)[] = [],
  public SIGNING_PUBKEY: (number)[] = [],
  public TIMESTAMP_MS: bigint = BigInt('0'),
  public REJECTION_CODE: string|Uint8Array|null = null,
  public REJECTION_MESSAGE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const REQUEST_ID = (this.REQUEST_ID !== null ? builder.createString(this.REQUEST_ID!) : 0);
  const MODULE_ID = (this.MODULE_ID !== null ? builder.createString(this.MODULE_ID!) : 0);
  const MODULE_VERSION = (this.MODULE_VERSION !== null ? builder.createString(this.MODULE_VERSION!) : 0);
  const REQUESTER_PEER_ID = (this.REQUESTER_PEER_ID !== null ? builder.createString(this.REQUESTER_PEER_ID!) : 0);
  const REQUESTER_XPUB = (this.REQUESTER_XPUB !== null ? builder.createString(this.REQUESTER_XPUB!) : 0);
  const REQUESTED_DOMAIN = (this.REQUESTED_DOMAIN !== null ? builder.createString(this.REQUESTED_DOMAIN!) : 0);
  const REQUESTER_EPHEMERAL_PUBKEY = LPF.createRequesterEphemeralPubkeyVector(builder, this.REQUESTER_EPHEMERAL_PUBKEY);
  const CHALLENGE_NONCE = LPF.createChallengeNonceVector(builder, this.CHALLENGE_NONCE);
  const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID!) : 0);
  const SIGNATURE = LPF.createSignatureVector(builder, this.SIGNATURE);
  const SIGNING_PUBKEY = LPF.createSigningPubkeyVector(builder, this.SIGNING_PUBKEY);
  const REJECTION_CODE = (this.REJECTION_CODE !== null ? builder.createString(this.REJECTION_CODE!) : 0);
  const REJECTION_MESSAGE = (this.REJECTION_MESSAGE !== null ? builder.createString(this.REJECTION_MESSAGE!) : 0);

  return LPF.createLPF(builder,
    this.MESSAGE_TYPE,
    REQUEST_ID,
    MODULE_ID,
    MODULE_VERSION,
    REQUESTER_PEER_ID,
    REQUESTER_XPUB,
    REQUESTED_DOMAIN,
    this.REQUESTED_TIMEOUT_MS,
    REQUESTER_EPHEMERAL_PUBKEY,
    CHALLENGE_NONCE,
    this.CHALLENGE_EXPIRES_AT,
    PROVIDER_PEER_ID,
    SIGNATURE,
    SIGNING_PUBKEY,
    this.TIMESTAMP_MS,
    REJECTION_CODE,
    REJECTION_MESSAGE
  );
}
}
