// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { CAT, CATT } from './CAT.js';
import { PPEOrbitalElementRecord, PPEOrbitalElementRecordT } from './PPEOrbitalElementRecord.js';
import { PPEPositionRecord, PPEPositionRecordT } from './PPEPositionRecord.js';
import { RFM, RFMT } from './RFM.js';
import { polynomialBasisType } from './polynomialBasisType.js';
import { timeSystem } from './timeSystem.js';


/**
 * Polynomial Ephemeris — top-level message containing metadata and
 * one or more polynomial coefficient records for a single space object.
 *
 * A PPE message may contain position records, orbital element records, or both.
 * Records should be ordered chronologically by EPOCH_MID and should collectively
 * cover the time span [START_TIME, STOP_TIME] without gaps.
 */
export class PPE implements flatbuffers.IUnpackableObject<PPET> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PPE {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPPE(bb:flatbuffers.ByteBuffer, obj?:PPE):PPE {
  return (obj || new PPE()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPPE(bb:flatbuffers.ByteBuffer, obj?:PPE):PPE {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PPE()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$PPE');
}

/**
 * Plain-text comments.
 */
COMMENT(index: number):string
COMMENT(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
COMMENT(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

commentLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Space object identification.
 */
OBJECT(obj?:CAT):CAT|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new CAT()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Origin of the reference frame (e.g., EARTH, MOON, MARS).
 */
CENTER_NAME():string|null
CENTER_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CENTER_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Reference frame for position/velocity coefficients.
 */
REFERENCE_FRAME(obj?:RFM):RFM|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new RFM()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Time system used for all epochs in this message.
 */
TIME_SYSTEM():timeSystem {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : timeSystem.GMST;
}

/**
 * Start of the total time span covered by this ephemeris (ISO 8601).
 */
START_TIME():string|null
START_TIME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
START_TIME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * End of the total time span covered by this ephemeris (ISO 8601).
 */
STOP_TIME():string|null
STOP_TIME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
STOP_TIME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Default polynomial basis type for all records in this message.
 * Individual records may override this with their own BASIS_TYPE field.
 */
DEFAULT_BASIS_TYPE():polynomialBasisType {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : polynomialBasisType.CHEBYSHEV;
}

/**
 * Array of position polynomial records.
 * Each record covers a time segment; together they span [START_TIME, STOP_TIME].
 */
POSITION_RECORDS(index: number, obj?:PPEPositionRecord):PPEPositionRecord|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new PPEPositionRecord()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

positionRecordsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Array of orbital element polynomial records.
 * Each record covers a time segment; together they span [START_TIME, STOP_TIME].
 */
ORBITAL_ELEMENT_RECORDS(index: number, obj?:PPEOrbitalElementRecord):PPEOrbitalElementRecord|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new PPEOrbitalElementRecord()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

orbitalElementRecordsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Generating ephemeris source (e.g., "JPL DE440", "HPOP v2.1", "Basilisk chebyPosEphem").
 */
EPHEMERIS_SOURCE():string|null
EPHEMERIS_SOURCE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EPHEMERIS_SOURCE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Fit span in seconds used to generate each polynomial segment.
 * Informational; actual spans are in individual records.
 */
NOMINAL_SEGMENT_SPAN():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Nominal number of coefficients per segment.
 * Informational; actual counts are in individual records.
 */
NOMINAL_NUM_COEFFICIENTS():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

static startPPE(builder:flatbuffers.Builder) {
  builder.startObject(13);
}

static addComment(builder:flatbuffers.Builder, COMMENTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, COMMENTOffset, 0);
}

static createCommentVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCommentVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addObject(builder:flatbuffers.Builder, OBJECTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, OBJECTOffset, 0);
}

static addCenterName(builder:flatbuffers.Builder, CENTER_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, CENTER_NAMEOffset, 0);
}

static addReferenceFrame(builder:flatbuffers.Builder, REFERENCE_FRAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, REFERENCE_FRAMEOffset, 0);
}

static addTimeSystem(builder:flatbuffers.Builder, TIME_SYSTEM:timeSystem) {
  builder.addFieldInt8(4, TIME_SYSTEM, timeSystem.GMST);
}

static addStartTime(builder:flatbuffers.Builder, START_TIMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, START_TIMEOffset, 0);
}

static addStopTime(builder:flatbuffers.Builder, STOP_TIMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, STOP_TIMEOffset, 0);
}

static addDefaultBasisType(builder:flatbuffers.Builder, DEFAULT_BASIS_TYPE:polynomialBasisType) {
  builder.addFieldInt8(7, DEFAULT_BASIS_TYPE, polynomialBasisType.CHEBYSHEV);
}

static addPositionRecords(builder:flatbuffers.Builder, POSITION_RECORDSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, POSITION_RECORDSOffset, 0);
}

static createPositionRecordsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPositionRecordsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOrbitalElementRecords(builder:flatbuffers.Builder, ORBITAL_ELEMENT_RECORDSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, ORBITAL_ELEMENT_RECORDSOffset, 0);
}

static createOrbitalElementRecordsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startOrbitalElementRecordsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEphemerisSource(builder:flatbuffers.Builder, EPHEMERIS_SOURCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, EPHEMERIS_SOURCEOffset, 0);
}

static addNominalSegmentSpan(builder:flatbuffers.Builder, NOMINAL_SEGMENT_SPAN:number) {
  builder.addFieldFloat64(11, NOMINAL_SEGMENT_SPAN, 0.0);
}

static addNominalNumCoefficients(builder:flatbuffers.Builder, NOMINAL_NUM_COEFFICIENTS:number) {
  builder.addFieldInt16(12, NOMINAL_NUM_COEFFICIENTS, 0);
}

static endPPE(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishPPEBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$PPE');
}

static finishSizePrefixedPPEBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$PPE', true);
}


unpack(): PPET {
  return new PPET(
    this.bb!.createScalarList<string>(this.COMMENT.bind(this), this.commentLength()),
    (this.OBJECT() !== null ? this.OBJECT()!.unpack() : null),
    this.CENTER_NAME(),
    (this.REFERENCE_FRAME() !== null ? this.REFERENCE_FRAME()!.unpack() : null),
    this.TIME_SYSTEM(),
    this.START_TIME(),
    this.STOP_TIME(),
    this.DEFAULT_BASIS_TYPE(),
    this.bb!.createObjList<PPEPositionRecord, PPEPositionRecordT>(this.POSITION_RECORDS.bind(this), this.positionRecordsLength()),
    this.bb!.createObjList<PPEOrbitalElementRecord, PPEOrbitalElementRecordT>(this.ORBITAL_ELEMENT_RECORDS.bind(this), this.orbitalElementRecordsLength()),
    this.EPHEMERIS_SOURCE(),
    this.NOMINAL_SEGMENT_SPAN(),
    this.NOMINAL_NUM_COEFFICIENTS()
  );
}


unpackTo(_o: PPET): void {
  _o.COMMENT = this.bb!.createScalarList<string>(this.COMMENT.bind(this), this.commentLength());
  _o.OBJECT = (this.OBJECT() !== null ? this.OBJECT()!.unpack() : null);
  _o.CENTER_NAME = this.CENTER_NAME();
  _o.REFERENCE_FRAME = (this.REFERENCE_FRAME() !== null ? this.REFERENCE_FRAME()!.unpack() : null);
  _o.TIME_SYSTEM = this.TIME_SYSTEM();
  _o.START_TIME = this.START_TIME();
  _o.STOP_TIME = this.STOP_TIME();
  _o.DEFAULT_BASIS_TYPE = this.DEFAULT_BASIS_TYPE();
  _o.POSITION_RECORDS = this.bb!.createObjList<PPEPositionRecord, PPEPositionRecordT>(this.POSITION_RECORDS.bind(this), this.positionRecordsLength());
  _o.ORBITAL_ELEMENT_RECORDS = this.bb!.createObjList<PPEOrbitalElementRecord, PPEOrbitalElementRecordT>(this.ORBITAL_ELEMENT_RECORDS.bind(this), this.orbitalElementRecordsLength());
  _o.EPHEMERIS_SOURCE = this.EPHEMERIS_SOURCE();
  _o.NOMINAL_SEGMENT_SPAN = this.NOMINAL_SEGMENT_SPAN();
  _o.NOMINAL_NUM_COEFFICIENTS = this.NOMINAL_NUM_COEFFICIENTS();
}
}

export class PPET implements flatbuffers.IGeneratedObject {
constructor(
  public COMMENT: (string)[] = [],
  public OBJECT: CATT|null = null,
  public CENTER_NAME: string|Uint8Array|null = null,
  public REFERENCE_FRAME: RFMT|null = null,
  public TIME_SYSTEM: timeSystem = timeSystem.GMST,
  public START_TIME: string|Uint8Array|null = null,
  public STOP_TIME: string|Uint8Array|null = null,
  public DEFAULT_BASIS_TYPE: polynomialBasisType = polynomialBasisType.CHEBYSHEV,
  public POSITION_RECORDS: (PPEPositionRecordT)[] = [],
  public ORBITAL_ELEMENT_RECORDS: (PPEOrbitalElementRecordT)[] = [],
  public EPHEMERIS_SOURCE: string|Uint8Array|null = null,
  public NOMINAL_SEGMENT_SPAN: number = 0.0,
  public NOMINAL_NUM_COEFFICIENTS: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const COMMENT = PPE.createCommentVector(builder, builder.createObjectOffsetList(this.COMMENT));
  const OBJECT = (this.OBJECT !== null ? this.OBJECT!.pack(builder) : 0);
  const CENTER_NAME = (this.CENTER_NAME !== null ? builder.createString(this.CENTER_NAME!) : 0);
  const REFERENCE_FRAME = (this.REFERENCE_FRAME !== null ? this.REFERENCE_FRAME!.pack(builder) : 0);
  const START_TIME = (this.START_TIME !== null ? builder.createString(this.START_TIME!) : 0);
  const STOP_TIME = (this.STOP_TIME !== null ? builder.createString(this.STOP_TIME!) : 0);
  const POSITION_RECORDS = PPE.createPositionRecordsVector(builder, builder.createObjectOffsetList(this.POSITION_RECORDS));
  const ORBITAL_ELEMENT_RECORDS = PPE.createOrbitalElementRecordsVector(builder, builder.createObjectOffsetList(this.ORBITAL_ELEMENT_RECORDS));
  const EPHEMERIS_SOURCE = (this.EPHEMERIS_SOURCE !== null ? builder.createString(this.EPHEMERIS_SOURCE!) : 0);

  PPE.startPPE(builder);
  PPE.addComment(builder, COMMENT);
  PPE.addObject(builder, OBJECT);
  PPE.addCenterName(builder, CENTER_NAME);
  PPE.addReferenceFrame(builder, REFERENCE_FRAME);
  PPE.addTimeSystem(builder, this.TIME_SYSTEM);
  PPE.addStartTime(builder, START_TIME);
  PPE.addStopTime(builder, STOP_TIME);
  PPE.addDefaultBasisType(builder, this.DEFAULT_BASIS_TYPE);
  PPE.addPositionRecords(builder, POSITION_RECORDS);
  PPE.addOrbitalElementRecords(builder, ORBITAL_ELEMENT_RECORDS);
  PPE.addEphemerisSource(builder, EPHEMERIS_SOURCE);
  PPE.addNominalSegmentSpan(builder, this.NOMINAL_SEGMENT_SPAN);
  PPE.addNominalNumCoefficients(builder, this.NOMINAL_NUM_COEFFICIENTS);

  return PPE.endPPE(builder);
}
}
