// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One name a place is known by, beside its primary NAME. Every attribute is
 * carried as the gazetteer publishes it; a consumer choosing a display name
 * applies its own policy over these flags and never has the choice baked in
 * by the publisher.
 */
export class GNPName implements flatbuffers.IUnpackableObject<GNPNameT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GNPName {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGNPName(bb:flatbuffers.ByteBuffer, obj?:GNPName):GNPName {
  return (obj || new GNPName()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGNPName(bb:flatbuffers.ByteBuffer, obj?:GNPName):GNPName {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GNPName()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * The alternate name verbatim, in its own script, unnormalized.
 */
NAME():string
NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
NAME(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * ISO 639 language tag when the gazetteer states one. A gazetteer may also
 * state a non-language tag for a link or a postal, IATA, ICAO, or similar
 * coded name; that token is carried verbatim rather than discarded.
 */
LANGUAGE():string|null
LANGUAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
LANGUAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The gazetteer marks this as the preferred name for its language.
 */
IS_PREFERRED():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * The gazetteer marks this as a short form.
 */
IS_SHORT():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * The gazetteer marks this as colloquial or unofficial.
 */
IS_COLLOQUIAL():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * The gazetteer marks this name as no longer in use.
 */
IS_HISTORIC():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Period the name was in use, verbatim as the gazetteer states it. Not
 * normalized to RFC 3339: gazetteers publish partial and era-qualified
 * dates here, and coercion would invent precision.
 */
VALID_FROM():string|null
VALID_FROM(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VALID_FROM(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

VALID_TO():string|null
VALID_TO(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VALID_TO(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startGNPName(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addName(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, NAMEOffset, 0);
}

static addLanguage(builder:flatbuffers.Builder, LANGUAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, LANGUAGEOffset, 0);
}

static addIsPreferred(builder:flatbuffers.Builder, IS_PREFERRED:boolean) {
  builder.addFieldInt8(2, +IS_PREFERRED, +false);
}

static addIsShort(builder:flatbuffers.Builder, IS_SHORT:boolean) {
  builder.addFieldInt8(3, +IS_SHORT, +false);
}

static addIsColloquial(builder:flatbuffers.Builder, IS_COLLOQUIAL:boolean) {
  builder.addFieldInt8(4, +IS_COLLOQUIAL, +false);
}

static addIsHistoric(builder:flatbuffers.Builder, IS_HISTORIC:boolean) {
  builder.addFieldInt8(5, +IS_HISTORIC, +false);
}

static addValidFrom(builder:flatbuffers.Builder, VALID_FROMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, VALID_FROMOffset, 0);
}

static addValidTo(builder:flatbuffers.Builder, VALID_TOOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, VALID_TOOffset, 0);
}

static endGNPName(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // NAME
  return offset;
}

static createGNPName(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset, LANGUAGEOffset:flatbuffers.Offset, IS_PREFERRED:boolean, IS_SHORT:boolean, IS_COLLOQUIAL:boolean, IS_HISTORIC:boolean, VALID_FROMOffset:flatbuffers.Offset, VALID_TOOffset:flatbuffers.Offset):flatbuffers.Offset {
  GNPName.startGNPName(builder);
  GNPName.addName(builder, NAMEOffset);
  GNPName.addLanguage(builder, LANGUAGEOffset);
  GNPName.addIsPreferred(builder, IS_PREFERRED);
  GNPName.addIsShort(builder, IS_SHORT);
  GNPName.addIsColloquial(builder, IS_COLLOQUIAL);
  GNPName.addIsHistoric(builder, IS_HISTORIC);
  GNPName.addValidFrom(builder, VALID_FROMOffset);
  GNPName.addValidTo(builder, VALID_TOOffset);
  return GNPName.endGNPName(builder);
}

unpack(): GNPNameT {
  return new GNPNameT(
    this.NAME(),
    this.LANGUAGE(),
    this.IS_PREFERRED(),
    this.IS_SHORT(),
    this.IS_COLLOQUIAL(),
    this.IS_HISTORIC(),
    this.VALID_FROM(),
    this.VALID_TO()
  );
}


unpackTo(_o: GNPNameT): void {
  _o.NAME = this.NAME();
  _o.LANGUAGE = this.LANGUAGE();
  _o.IS_PREFERRED = this.IS_PREFERRED();
  _o.IS_SHORT = this.IS_SHORT();
  _o.IS_COLLOQUIAL = this.IS_COLLOQUIAL();
  _o.IS_HISTORIC = this.IS_HISTORIC();
  _o.VALID_FROM = this.VALID_FROM();
  _o.VALID_TO = this.VALID_TO();
}
}

export class GNPNameT implements flatbuffers.IGeneratedObject {
constructor(
  public NAME: string|Uint8Array|null = null,
  public LANGUAGE: string|Uint8Array|null = null,
  public IS_PREFERRED: boolean = false,
  public IS_SHORT: boolean = false,
  public IS_COLLOQUIAL: boolean = false,
  public IS_HISTORIC: boolean = false,
  public VALID_FROM: string|Uint8Array|null = null,
  public VALID_TO: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const NAME = (this.NAME !== null ? builder.createString(this.NAME!) : 0);
  const LANGUAGE = (this.LANGUAGE !== null ? builder.createString(this.LANGUAGE!) : 0);
  const VALID_FROM = (this.VALID_FROM !== null ? builder.createString(this.VALID_FROM!) : 0);
  const VALID_TO = (this.VALID_TO !== null ? builder.createString(this.VALID_TO!) : 0);

  return GNPName.createGNPName(builder,
    NAME,
    LANGUAGE,
    this.IS_PREFERRED,
    this.IS_SHORT,
    this.IS_COLLOQUIAL,
    this.IS_HISTORIC,
    VALID_FROM,
    VALID_TO
  );
}
}
