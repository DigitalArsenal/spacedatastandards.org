// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Plaintext carried, size-prefixed, inside RPC.CIPHERTEXT.
 */
export class RPCBody implements flatbuffers.IUnpackableObject<RPCBodyT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RPCBody {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRPCBody(bb:flatbuffers.ByteBuffer, obj?:RPCBody):RPCBody {
  return (obj || new RPCBody()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRPCBody(bb:flatbuffers.ByteBuffer, obj?:RPCBody):RPCBody {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RPCBody()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Request method verb (for example GET, POST). Empty on a response.
 */
METHOD():string|null
METHOD(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
METHOD(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Request route, path plus optional query. Empty on a response.
 */
ROUTE():string|null
ROUTE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ROUTE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Request or response payload bytes.
 */
BODY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

bodyLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

bodyArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Four-character file identifier of BODY when BODY is a FlatBuffer,
 * `$` included (for example "$EPM"). Empty when BODY is not a FlatBuffer.
 */
BODY_FILE_ID():string|null
BODY_FILE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BODY_FILE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Response status code. Zero on a request.
 */
STATUS():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Identifier of the signer's key; MUST identify RPC.SIGNER_PUBLIC_KEY.
 */
SIGNER_KEY_ID(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signerKeyIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signerKeyIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Request only: public key the response is sealed to.
 */
REPLY_KEY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

replyKeyLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

replyKeyArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Response only: RPC.NONCE of the request being answered.
 */
REQUEST_NONCE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

requestNonceLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

requestNonceArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Response only: SHA-256 (32 bytes) of the complete size-prefixed request
 * RPC FlatBuffer as received, signatures included.
 */
REQUEST_DIGEST(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

requestDigestLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

requestDigestArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startRPCBody(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addMethod(builder:flatbuffers.Builder, METHODOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, METHODOffset, 0);
}

static addRoute(builder:flatbuffers.Builder, ROUTEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ROUTEOffset, 0);
}

static addBody(builder:flatbuffers.Builder, BODYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, BODYOffset, 0);
}

static createBodyVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startBodyVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addBodyFileId(builder:flatbuffers.Builder, BODY_FILE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, BODY_FILE_IDOffset, 0);
}

static addStatus(builder:flatbuffers.Builder, STATUS:number) {
  builder.addFieldInt16(4, STATUS, 0);
}

static addSignerKeyId(builder:flatbuffers.Builder, SIGNER_KEY_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, SIGNER_KEY_IDOffset, 0);
}

static createSignerKeyIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSignerKeyIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addReplyKey(builder:flatbuffers.Builder, REPLY_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, REPLY_KEYOffset, 0);
}

static createReplyKeyVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startReplyKeyVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addRequestNonce(builder:flatbuffers.Builder, REQUEST_NONCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, REQUEST_NONCEOffset, 0);
}

static createRequestNonceVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startRequestNonceVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addRequestDigest(builder:flatbuffers.Builder, REQUEST_DIGESTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, REQUEST_DIGESTOffset, 0);
}

static createRequestDigestVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startRequestDigestVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endRPCBody(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRPCBody(builder:flatbuffers.Builder, METHODOffset:flatbuffers.Offset, ROUTEOffset:flatbuffers.Offset, BODYOffset:flatbuffers.Offset, BODY_FILE_IDOffset:flatbuffers.Offset, STATUS:number, SIGNER_KEY_IDOffset:flatbuffers.Offset, REPLY_KEYOffset:flatbuffers.Offset, REQUEST_NONCEOffset:flatbuffers.Offset, REQUEST_DIGESTOffset:flatbuffers.Offset):flatbuffers.Offset {
  RPCBody.startRPCBody(builder);
  RPCBody.addMethod(builder, METHODOffset);
  RPCBody.addRoute(builder, ROUTEOffset);
  RPCBody.addBody(builder, BODYOffset);
  RPCBody.addBodyFileId(builder, BODY_FILE_IDOffset);
  RPCBody.addStatus(builder, STATUS);
  RPCBody.addSignerKeyId(builder, SIGNER_KEY_IDOffset);
  RPCBody.addReplyKey(builder, REPLY_KEYOffset);
  RPCBody.addRequestNonce(builder, REQUEST_NONCEOffset);
  RPCBody.addRequestDigest(builder, REQUEST_DIGESTOffset);
  return RPCBody.endRPCBody(builder);
}

unpack(): RPCBodyT {
  return new RPCBodyT(
    this.METHOD(),
    this.ROUTE(),
    this.bb!.createScalarList<number>(this.BODY.bind(this), this.bodyLength()),
    this.BODY_FILE_ID(),
    this.STATUS(),
    this.bb!.createScalarList<number>(this.SIGNER_KEY_ID.bind(this), this.signerKeyIdLength()),
    this.bb!.createScalarList<number>(this.REPLY_KEY.bind(this), this.replyKeyLength()),
    this.bb!.createScalarList<number>(this.REQUEST_NONCE.bind(this), this.requestNonceLength()),
    this.bb!.createScalarList<number>(this.REQUEST_DIGEST.bind(this), this.requestDigestLength())
  );
}


unpackTo(_o: RPCBodyT): void {
  _o.METHOD = this.METHOD();
  _o.ROUTE = this.ROUTE();
  _o.BODY = this.bb!.createScalarList<number>(this.BODY.bind(this), this.bodyLength());
  _o.BODY_FILE_ID = this.BODY_FILE_ID();
  _o.STATUS = this.STATUS();
  _o.SIGNER_KEY_ID = this.bb!.createScalarList<number>(this.SIGNER_KEY_ID.bind(this), this.signerKeyIdLength());
  _o.REPLY_KEY = this.bb!.createScalarList<number>(this.REPLY_KEY.bind(this), this.replyKeyLength());
  _o.REQUEST_NONCE = this.bb!.createScalarList<number>(this.REQUEST_NONCE.bind(this), this.requestNonceLength());
  _o.REQUEST_DIGEST = this.bb!.createScalarList<number>(this.REQUEST_DIGEST.bind(this), this.requestDigestLength());
}
}

export class RPCBodyT implements flatbuffers.IGeneratedObject {
constructor(
  public METHOD: string|Uint8Array|null = null,
  public ROUTE: string|Uint8Array|null = null,
  public BODY: (number)[] = [],
  public BODY_FILE_ID: string|Uint8Array|null = null,
  public STATUS: number = 0,
  public SIGNER_KEY_ID: (number)[] = [],
  public REPLY_KEY: (number)[] = [],
  public REQUEST_NONCE: (number)[] = [],
  public REQUEST_DIGEST: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const METHOD = (this.METHOD !== null ? builder.createString(this.METHOD!) : 0);
  const ROUTE = (this.ROUTE !== null ? builder.createString(this.ROUTE!) : 0);
  const BODY = RPCBody.createBodyVector(builder, this.BODY);
  const BODY_FILE_ID = (this.BODY_FILE_ID !== null ? builder.createString(this.BODY_FILE_ID!) : 0);
  const SIGNER_KEY_ID = RPCBody.createSignerKeyIdVector(builder, this.SIGNER_KEY_ID);
  const REPLY_KEY = RPCBody.createReplyKeyVector(builder, this.REPLY_KEY);
  const REQUEST_NONCE = RPCBody.createRequestNonceVector(builder, this.REQUEST_NONCE);
  const REQUEST_DIGEST = RPCBody.createRequestDigestVector(builder, this.REQUEST_DIGEST);

  return RPCBody.createRPCBody(builder,
    METHOD,
    ROUTE,
    BODY,
    BODY_FILE_ID,
    this.STATUS,
    SIGNER_KEY_ID,
    REPLY_KEY,
    REQUEST_NONCE,
    REQUEST_DIGEST
  );
}
}
