// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { CNPProvenance, CNPProvenanceT } from './CNPProvenance.js';
import { CNPStatistic, CNPStatisticT } from './CNPStatistic.js';
import { cnpMetricKind } from './cnpMetricKind.js';


/**
 * One aggregated quantity for the parent record's key.
 *
 * A metric is a DISTRIBUTION, not a scalar. There is deliberately no
 * `VALUE: double` on this table: a source that publishes only a median
 * encodes exactly one CNPStatistic with STATISTIC MEDIAN, and a consumer can
 * then never mistake it for a mean.
 */
export class CNPMetric implements flatbuffers.IUnpackableObject<CNPMetricT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CNPMetric {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCNPMetric(bb:flatbuffers.ByteBuffer, obj?:CNPMetric):CNPMetric {
  return (obj || new CNPMetric()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCNPMetric(bb:flatbuffers.ByteBuffer, obj?:CNPMetric):CNPMetric {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CNPMetric()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Quantity aggregated.
 */
KIND():cnpMetricKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : cnpMetricKind.OTHER;
}

/**
 * The source's own name for this metric, verbatim. REQUIRED to be
 * non-empty when KIND is OTHER; recommended always, because it is what
 * makes the mapping auditable.
 */
SOURCE_METRIC_NAME():string|null
SOURCE_METRIC_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SOURCE_METRIC_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Units of every VALUE under STATISTICS. NORMATIVE and required: "Mbps",
 * "kbps", "ms", "percent", "fraction", "count". Units are never silently
 * converted on ingest; a source publishing kbps encodes kbps.
 */
UNITS():string
UNITS(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
UNITS(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * The distribution. At least one entry; a metric with none is malformed.
 */
STATISTICS(index: number, obj?:CNPStatistic):CNPStatistic|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new CNPStatistic()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

statisticsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Number of individual measurements aggregated. 0 means the source did
 * not publish a sample count — it never means zero measurements.
 */
SAMPLE_COUNT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Number of distinct clients, terminals or probes contributing. 0 means
 * unpublished.
 */
CLIENT_COUNT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Where this metric came from and under what terms.
 */
PROVENANCE(obj?:CNPProvenance):CNPProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new CNPProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startCNPMetric(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addKind(builder:flatbuffers.Builder, KIND:cnpMetricKind) {
  builder.addFieldInt8(0, KIND, cnpMetricKind.OTHER);
}

static addSourceMetricName(builder:flatbuffers.Builder, SOURCE_METRIC_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, SOURCE_METRIC_NAMEOffset, 0);
}

static addUnits(builder:flatbuffers.Builder, UNITSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, UNITSOffset, 0);
}

static addStatistics(builder:flatbuffers.Builder, STATISTICSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, STATISTICSOffset, 0);
}

static createStatisticsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startStatisticsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSampleCount(builder:flatbuffers.Builder, SAMPLE_COUNT:bigint) {
  builder.addFieldInt64(4, SAMPLE_COUNT, BigInt('0'));
}

static addClientCount(builder:flatbuffers.Builder, CLIENT_COUNT:bigint) {
  builder.addFieldInt64(5, CLIENT_COUNT, BigInt('0'));
}

static addProvenance(builder:flatbuffers.Builder, PROVENANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, PROVENANCEOffset, 0);
}

static endCNPMetric(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 8) // UNITS
  builder.requiredField(offset, 16) // PROVENANCE
  return offset;
}


unpack(): CNPMetricT {
  return new CNPMetricT(
    this.KIND(),
    this.SOURCE_METRIC_NAME(),
    this.UNITS(),
    this.bb!.createObjList<CNPStatistic, CNPStatisticT>(this.STATISTICS.bind(this), this.statisticsLength()),
    this.SAMPLE_COUNT(),
    this.CLIENT_COUNT(),
    (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null)
  );
}


unpackTo(_o: CNPMetricT): void {
  _o.KIND = this.KIND();
  _o.SOURCE_METRIC_NAME = this.SOURCE_METRIC_NAME();
  _o.UNITS = this.UNITS();
  _o.STATISTICS = this.bb!.createObjList<CNPStatistic, CNPStatisticT>(this.STATISTICS.bind(this), this.statisticsLength());
  _o.SAMPLE_COUNT = this.SAMPLE_COUNT();
  _o.CLIENT_COUNT = this.CLIENT_COUNT();
  _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null);
}
}

export class CNPMetricT implements flatbuffers.IGeneratedObject {
constructor(
  public KIND: cnpMetricKind = cnpMetricKind.OTHER,
  public SOURCE_METRIC_NAME: string|Uint8Array|null = null,
  public UNITS: string|Uint8Array|null = null,
  public STATISTICS: (CNPStatisticT)[] = [],
  public SAMPLE_COUNT: bigint = BigInt('0'),
  public CLIENT_COUNT: bigint = BigInt('0'),
  public PROVENANCE: CNPProvenanceT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SOURCE_METRIC_NAME = (this.SOURCE_METRIC_NAME !== null ? builder.createString(this.SOURCE_METRIC_NAME!) : 0);
  const UNITS = (this.UNITS !== null ? builder.createString(this.UNITS!) : 0);
  const STATISTICS = CNPMetric.createStatisticsVector(builder, builder.createObjectOffsetList(this.STATISTICS));
  const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE!.pack(builder) : 0);

  CNPMetric.startCNPMetric(builder);
  CNPMetric.addKind(builder, this.KIND);
  CNPMetric.addSourceMetricName(builder, SOURCE_METRIC_NAME);
  CNPMetric.addUnits(builder, UNITS);
  CNPMetric.addStatistics(builder, STATISTICS);
  CNPMetric.addSampleCount(builder, this.SAMPLE_COUNT);
  CNPMetric.addClientCount(builder, this.CLIENT_COUNT);
  CNPMetric.addProvenance(builder, PROVENANCE);

  return CNPMetric.endCNPMetric(builder);
}
}
