// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TAB, TABT } from './TAB.js';


/**
 * Request envelope carried by `plugin_invoke_stream`.
 */
export class PIVRequest implements flatbuffers.IUnpackableObject<PIVRequestT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PIVRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPIVRequest(bb:flatbuffers.ByteBuffer, obj?:PIVRequest):PIVRequest {
  return (obj || new PIVRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPIVRequest(bb:flatbuffers.ByteBuffer, obj?:PIVRequest):PIVRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PIVRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Stable method identifier from PLG.METHODS[].METHOD_ID.
 */
METHOD_ID():string
METHOD_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
METHOD_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Input frames. Each TAB.TYPE_REF names the payload schema and each
 * TAB.WIRE_FORMAT selects FLATBUFFER or ALIGNED_BINARY. TAB.OFFSET
 * is arena-relative into PAYLOAD_ARENA (or an absolute pointer when
 * PAYLOAD_ARENA is empty).
 */
INPUTS(index: number, obj?:TAB):TAB|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new TAB()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

inputsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Optional arena backing the INPUTS payload bodies. Empty means the
 * caller manages arena state out-of-band.
 */
PAYLOAD_ARENA(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

payloadArenaLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

payloadArenaArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Optional trace identifier for request correlation.
 */
TRACE_ID():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Optional yield-budget hint. Plugins MAY pause queued work after
 * draining this many output frames and return YIELDED for the caller
 * to resume. 0 = no hint.
 */
OUTPUT_STREAM_CAP():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startPIVRequest(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addMethodId(builder:flatbuffers.Builder, METHOD_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, METHOD_IDOffset, 0);
}

static addInputs(builder:flatbuffers.Builder, INPUTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, INPUTSOffset, 0);
}

static createInputsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startInputsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addPayloadArena(builder:flatbuffers.Builder, PAYLOAD_ARENAOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, PAYLOAD_ARENAOffset, 0);
}

static createPayloadArenaVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPayloadArenaVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_ID:bigint) {
  builder.addFieldInt64(3, TRACE_ID, BigInt('0'));
}

static addOutputStreamCap(builder:flatbuffers.Builder, OUTPUT_STREAM_CAP:number) {
  builder.addFieldInt32(4, OUTPUT_STREAM_CAP, 0);
}

static endPIVRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // METHOD_ID
  return offset;
}

static createPIVRequest(builder:flatbuffers.Builder, METHOD_IDOffset:flatbuffers.Offset, INPUTSOffset:flatbuffers.Offset, PAYLOAD_ARENAOffset:flatbuffers.Offset, TRACE_ID:bigint, OUTPUT_STREAM_CAP:number):flatbuffers.Offset {
  PIVRequest.startPIVRequest(builder);
  PIVRequest.addMethodId(builder, METHOD_IDOffset);
  PIVRequest.addInputs(builder, INPUTSOffset);
  PIVRequest.addPayloadArena(builder, PAYLOAD_ARENAOffset);
  PIVRequest.addTraceId(builder, TRACE_ID);
  PIVRequest.addOutputStreamCap(builder, OUTPUT_STREAM_CAP);
  return PIVRequest.endPIVRequest(builder);
}

unpack(): PIVRequestT {
  return new PIVRequestT(
    this.METHOD_ID(),
    this.bb!.createObjList<TAB, TABT>(this.INPUTS.bind(this), this.inputsLength()),
    this.bb!.createScalarList<number>(this.PAYLOAD_ARENA.bind(this), this.payloadArenaLength()),
    this.TRACE_ID(),
    this.OUTPUT_STREAM_CAP()
  );
}


unpackTo(_o: PIVRequestT): void {
  _o.METHOD_ID = this.METHOD_ID();
  _o.INPUTS = this.bb!.createObjList<TAB, TABT>(this.INPUTS.bind(this), this.inputsLength());
  _o.PAYLOAD_ARENA = this.bb!.createScalarList<number>(this.PAYLOAD_ARENA.bind(this), this.payloadArenaLength());
  _o.TRACE_ID = this.TRACE_ID();
  _o.OUTPUT_STREAM_CAP = this.OUTPUT_STREAM_CAP();
}
}

export class PIVRequestT implements flatbuffers.IGeneratedObject {
constructor(
  public METHOD_ID: string|Uint8Array|null = null,
  public INPUTS: (TABT)[] = [],
  public PAYLOAD_ARENA: (number)[] = [],
  public TRACE_ID: bigint = BigInt('0'),
  public OUTPUT_STREAM_CAP: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const METHOD_ID = (this.METHOD_ID !== null ? builder.createString(this.METHOD_ID!) : 0);
  const INPUTS = PIVRequest.createInputsVector(builder, builder.createObjectOffsetList(this.INPUTS));
  const PAYLOAD_ARENA = PIVRequest.createPayloadArenaVector(builder, this.PAYLOAD_ARENA);

  return PIVRequest.createPIVRequest(builder,
    METHOD_ID,
    INPUTS,
    PAYLOAD_ARENA,
    this.TRACE_ID,
    this.OUTPUT_STREAM_CAP
  );
}
}
