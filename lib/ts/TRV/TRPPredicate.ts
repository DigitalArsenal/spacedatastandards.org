// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TRPAsset, TRPAssetT } from './TRPAsset.js';
import { trpPredicateKind } from './trpPredicateKind.js';


/**
 * One independently measurable predicate in a trust rule policy.
 */
export class TRPPredicate implements flatbuffers.IUnpackableObject<TRPPredicateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TRPPredicate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTRPPredicate(bb:flatbuffers.ByteBuffer, obj?:TRPPredicate):TRPPredicate {
  return (obj || new TRPPredicate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTRPPredicate(bb:flatbuffers.ByteBuffer, obj?:TRPPredicate):TRPPredicate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TRPPredicate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Stable identifier unique within the policy.
 */
PREDICATE_ID():string|null
PREDICATE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PREDICATE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Predicate operation to evaluate.
 */
KIND():trpPredicateKind {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : trpPredicateKind.MinValueLocked;
}

/**
 * Minimum qualifying value expressed as a uint64 in the smallest unit.
 */
MIN_VALUE():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Currency or unit used to compare normalized values.
 */
VALUE_CURRENCY():string|null
VALUE_CURRENCY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VALUE_CURRENCY(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Minimum continuous holding duration in seconds.
 */
MIN_HELD_SECONDS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Assets admitted by this predicate.
 */
ASSETS(index: number, obj?:TRPAsset):TRPAsset|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new TRPAsset()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

assetsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * X, the number of named trusters that must match.
 */
REQUIRED_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * The N named `$TRE` trusters considered by a trusted-connections predicate.
 */
TRUSTER_IDS(index: number):string
TRUSTER_IDS(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
TRUSTER_IDS(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

trusterIdsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Minimum accepted `$TRE.WEIGHT` for a matching trust edge.
 */
MIN_EDGE_WEIGHT():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startTRPPredicate(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addPredicateId(builder:flatbuffers.Builder, PREDICATE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, PREDICATE_IDOffset, 0);
}

static addKind(builder:flatbuffers.Builder, KIND:trpPredicateKind) {
  builder.addFieldInt8(1, KIND, trpPredicateKind.MinValueLocked);
}

static addMinValue(builder:flatbuffers.Builder, MIN_VALUE:bigint) {
  builder.addFieldInt64(2, MIN_VALUE, BigInt('0'));
}

static addValueCurrency(builder:flatbuffers.Builder, VALUE_CURRENCYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, VALUE_CURRENCYOffset, 0);
}

static addMinHeldSeconds(builder:flatbuffers.Builder, MIN_HELD_SECONDS:bigint) {
  builder.addFieldInt64(4, MIN_HELD_SECONDS, BigInt('0'));
}

static addAssets(builder:flatbuffers.Builder, ASSETSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ASSETSOffset, 0);
}

static createAssetsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAssetsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addRequiredCount(builder:flatbuffers.Builder, REQUIRED_COUNT:number) {
  builder.addFieldInt32(6, REQUIRED_COUNT, 0);
}

static addTrusterIds(builder:flatbuffers.Builder, TRUSTER_IDSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, TRUSTER_IDSOffset, 0);
}

static createTrusterIdsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTrusterIdsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMinEdgeWeight(builder:flatbuffers.Builder, MIN_EDGE_WEIGHT:number) {
  builder.addFieldFloat64(8, MIN_EDGE_WEIGHT, 0.0);
}

static endTRPPredicate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTRPPredicate(builder:flatbuffers.Builder, PREDICATE_IDOffset:flatbuffers.Offset, KIND:trpPredicateKind, MIN_VALUE:bigint, VALUE_CURRENCYOffset:flatbuffers.Offset, MIN_HELD_SECONDS:bigint, ASSETSOffset:flatbuffers.Offset, REQUIRED_COUNT:number, TRUSTER_IDSOffset:flatbuffers.Offset, MIN_EDGE_WEIGHT:number):flatbuffers.Offset {
  TRPPredicate.startTRPPredicate(builder);
  TRPPredicate.addPredicateId(builder, PREDICATE_IDOffset);
  TRPPredicate.addKind(builder, KIND);
  TRPPredicate.addMinValue(builder, MIN_VALUE);
  TRPPredicate.addValueCurrency(builder, VALUE_CURRENCYOffset);
  TRPPredicate.addMinHeldSeconds(builder, MIN_HELD_SECONDS);
  TRPPredicate.addAssets(builder, ASSETSOffset);
  TRPPredicate.addRequiredCount(builder, REQUIRED_COUNT);
  TRPPredicate.addTrusterIds(builder, TRUSTER_IDSOffset);
  TRPPredicate.addMinEdgeWeight(builder, MIN_EDGE_WEIGHT);
  return TRPPredicate.endTRPPredicate(builder);
}

unpack(): TRPPredicateT {
  return new TRPPredicateT(
    this.PREDICATE_ID(),
    this.KIND(),
    this.MIN_VALUE(),
    this.VALUE_CURRENCY(),
    this.MIN_HELD_SECONDS(),
    this.bb!.createObjList<TRPAsset, TRPAssetT>(this.ASSETS.bind(this), this.assetsLength()),
    this.REQUIRED_COUNT(),
    this.bb!.createScalarList<string>(this.TRUSTER_IDS.bind(this), this.trusterIdsLength()),
    this.MIN_EDGE_WEIGHT()
  );
}


unpackTo(_o: TRPPredicateT): void {
  _o.PREDICATE_ID = this.PREDICATE_ID();
  _o.KIND = this.KIND();
  _o.MIN_VALUE = this.MIN_VALUE();
  _o.VALUE_CURRENCY = this.VALUE_CURRENCY();
  _o.MIN_HELD_SECONDS = this.MIN_HELD_SECONDS();
  _o.ASSETS = this.bb!.createObjList<TRPAsset, TRPAssetT>(this.ASSETS.bind(this), this.assetsLength());
  _o.REQUIRED_COUNT = this.REQUIRED_COUNT();
  _o.TRUSTER_IDS = this.bb!.createScalarList<string>(this.TRUSTER_IDS.bind(this), this.trusterIdsLength());
  _o.MIN_EDGE_WEIGHT = this.MIN_EDGE_WEIGHT();
}
}

export class TRPPredicateT implements flatbuffers.IGeneratedObject {
constructor(
  public PREDICATE_ID: string|Uint8Array|null = null,
  public KIND: trpPredicateKind = trpPredicateKind.MinValueLocked,
  public MIN_VALUE: bigint = BigInt('0'),
  public VALUE_CURRENCY: string|Uint8Array|null = null,
  public MIN_HELD_SECONDS: bigint = BigInt('0'),
  public ASSETS: (TRPAssetT)[] = [],
  public REQUIRED_COUNT: number = 0,
  public TRUSTER_IDS: (string)[] = [],
  public MIN_EDGE_WEIGHT: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PREDICATE_ID = (this.PREDICATE_ID !== null ? builder.createString(this.PREDICATE_ID!) : 0);
  const VALUE_CURRENCY = (this.VALUE_CURRENCY !== null ? builder.createString(this.VALUE_CURRENCY!) : 0);
  const ASSETS = TRPPredicate.createAssetsVector(builder, builder.createObjectOffsetList(this.ASSETS));
  const TRUSTER_IDS = TRPPredicate.createTrusterIdsVector(builder, builder.createObjectOffsetList(this.TRUSTER_IDS));

  return TRPPredicate.createTRPPredicate(builder,
    PREDICATE_ID,
    this.KIND,
    this.MIN_VALUE,
    VALUE_CURRENCY,
    this.MIN_HELD_SECONDS,
    ASSETS,
    this.REQUIRED_COUNT,
    TRUSTER_IDS,
    this.MIN_EDGE_WEIGHT
  );
}
}
