// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Asset admitted by a value or token predicate.
 */
export class TRPAsset implements flatbuffers.IUnpackableObject<TRPAssetT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TRPAsset {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTRPAsset(bb:flatbuffers.ByteBuffer, obj?:TRPAsset):TRPAsset {
  return (obj || new TRPAsset()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTRPAsset(bb:flatbuffers.ByteBuffer, obj?:TRPAsset):TRPAsset {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TRPAsset()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * CAIP-2 style chain identifier, e.g. "eip155:1" or
 * "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp". Vendor and organization names
 * are forbidden; a chain is named only by its identifier.
 */
CHAIN_ID():string|null
CHAIN_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CHAIN_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Chain-local token address, or empty for the chain's native asset.
 */
TOKEN_ADDRESS():string|null
TOKEN_ADDRESS(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TOKEN_ADDRESS(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Published token symbol used for display and matching.
 */
TOKEN_SYMBOL():string|null
TOKEN_SYMBOL(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TOKEN_SYMBOL(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Number of decimal places used by the smallest-unit integer balance.
 */
DECIMALS():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startTRPAsset(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addChainId(builder:flatbuffers.Builder, CHAIN_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, CHAIN_IDOffset, 0);
}

static addTokenAddress(builder:flatbuffers.Builder, TOKEN_ADDRESSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, TOKEN_ADDRESSOffset, 0);
}

static addTokenSymbol(builder:flatbuffers.Builder, TOKEN_SYMBOLOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, TOKEN_SYMBOLOffset, 0);
}

static addDecimals(builder:flatbuffers.Builder, DECIMALS:number) {
  builder.addFieldInt8(3, DECIMALS, 0);
}

static endTRPAsset(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTRPAsset(builder:flatbuffers.Builder, CHAIN_IDOffset:flatbuffers.Offset, TOKEN_ADDRESSOffset:flatbuffers.Offset, TOKEN_SYMBOLOffset:flatbuffers.Offset, DECIMALS:number):flatbuffers.Offset {
  TRPAsset.startTRPAsset(builder);
  TRPAsset.addChainId(builder, CHAIN_IDOffset);
  TRPAsset.addTokenAddress(builder, TOKEN_ADDRESSOffset);
  TRPAsset.addTokenSymbol(builder, TOKEN_SYMBOLOffset);
  TRPAsset.addDecimals(builder, DECIMALS);
  return TRPAsset.endTRPAsset(builder);
}

unpack(): TRPAssetT {
  return new TRPAssetT(
    this.CHAIN_ID(),
    this.TOKEN_ADDRESS(),
    this.TOKEN_SYMBOL(),
    this.DECIMALS()
  );
}


unpackTo(_o: TRPAssetT): void {
  _o.CHAIN_ID = this.CHAIN_ID();
  _o.TOKEN_ADDRESS = this.TOKEN_ADDRESS();
  _o.TOKEN_SYMBOL = this.TOKEN_SYMBOL();
  _o.DECIMALS = this.DECIMALS();
}
}

export class TRPAssetT implements flatbuffers.IGeneratedObject {
constructor(
  public CHAIN_ID: string|Uint8Array|null = null,
  public TOKEN_ADDRESS: string|Uint8Array|null = null,
  public TOKEN_SYMBOL: string|Uint8Array|null = null,
  public DECIMALS: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CHAIN_ID = (this.CHAIN_ID !== null ? builder.createString(this.CHAIN_ID!) : 0);
  const TOKEN_ADDRESS = (this.TOKEN_ADDRESS !== null ? builder.createString(this.TOKEN_ADDRESS!) : 0);
  const TOKEN_SYMBOL = (this.TOKEN_SYMBOL !== null ? builder.createString(this.TOKEN_SYMBOL!) : 0);

  return TRPAsset.createTRPAsset(builder,
    CHAIN_ID,
    TOKEN_ADDRESS,
    TOKEN_SYMBOL,
    this.DECIMALS
  );
}
}
