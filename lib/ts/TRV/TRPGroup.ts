// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TRPPredicate, TRPPredicateT } from './TRPPredicate.js';
import { trpCombinator } from './trpCombinator.js';


/**
 * Recursive boolean group for a compound trust rule set.
 */
export class TRPGroup implements flatbuffers.IUnpackableObject<TRPGroupT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TRPGroup {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTRPGroup(bb:flatbuffers.ByteBuffer, obj?:TRPGroup):TRPGroup {
  return (obj || new TRPGroup()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTRPGroup(bb:flatbuffers.ByteBuffer, obj?:TRPGroup):TRPGroup {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TRPGroup()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Stable identifier unique within the policy.
 */
GROUP_ID():string|null
GROUP_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
GROUP_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Boolean operation applied across direct predicates and child groups.
 */
COMBINATOR():trpCombinator {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : trpCombinator.All;
}

/**
 * Predicates evaluated directly within this group.
 */
PREDICATES(index: number, obj?:TRPPredicate):TRPPredicate|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new TRPPredicate()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

predicatesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Nested groups used to express compound rule sets.
 */
GROUPS(index: number, obj?:TRPGroup):TRPGroup|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new TRPGroup()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

groupsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startTRPGroup(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addGroupId(builder:flatbuffers.Builder, GROUP_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, GROUP_IDOffset, 0);
}

static addCombinator(builder:flatbuffers.Builder, COMBINATOR:trpCombinator) {
  builder.addFieldInt8(1, COMBINATOR, trpCombinator.All);
}

static addPredicates(builder:flatbuffers.Builder, PREDICATESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, PREDICATESOffset, 0);
}

static createPredicatesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPredicatesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addGroups(builder:flatbuffers.Builder, GROUPSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, GROUPSOffset, 0);
}

static createGroupsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startGroupsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endTRPGroup(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTRPGroup(builder:flatbuffers.Builder, GROUP_IDOffset:flatbuffers.Offset, COMBINATOR:trpCombinator, PREDICATESOffset:flatbuffers.Offset, GROUPSOffset:flatbuffers.Offset):flatbuffers.Offset {
  TRPGroup.startTRPGroup(builder);
  TRPGroup.addGroupId(builder, GROUP_IDOffset);
  TRPGroup.addCombinator(builder, COMBINATOR);
  TRPGroup.addPredicates(builder, PREDICATESOffset);
  TRPGroup.addGroups(builder, GROUPSOffset);
  return TRPGroup.endTRPGroup(builder);
}

unpack(): TRPGroupT {
  return new TRPGroupT(
    this.GROUP_ID(),
    this.COMBINATOR(),
    this.bb!.createObjList<TRPPredicate, TRPPredicateT>(this.PREDICATES.bind(this), this.predicatesLength()),
    this.bb!.createObjList<TRPGroup, TRPGroupT>(this.GROUPS.bind(this), this.groupsLength())
  );
}


unpackTo(_o: TRPGroupT): void {
  _o.GROUP_ID = this.GROUP_ID();
  _o.COMBINATOR = this.COMBINATOR();
  _o.PREDICATES = this.bb!.createObjList<TRPPredicate, TRPPredicateT>(this.PREDICATES.bind(this), this.predicatesLength());
  _o.GROUPS = this.bb!.createObjList<TRPGroup, TRPGroupT>(this.GROUPS.bind(this), this.groupsLength());
}
}

export class TRPGroupT implements flatbuffers.IGeneratedObject {
constructor(
  public GROUP_ID: string|Uint8Array|null = null,
  public COMBINATOR: trpCombinator = trpCombinator.All,
  public PREDICATES: (TRPPredicateT)[] = [],
  public GROUPS: (TRPGroupT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const GROUP_ID = (this.GROUP_ID !== null ? builder.createString(this.GROUP_ID!) : 0);
  const PREDICATES = TRPGroup.createPredicatesVector(builder, builder.createObjectOffsetList(this.PREDICATES));
  const GROUPS = TRPGroup.createGroupsVector(builder, builder.createObjectOffsetList(this.GROUPS));

  return TRPGroup.createTRPGroup(builder,
    GROUP_ID,
    this.COMBINATOR,
    PREDICATES,
    GROUPS
  );
}
}
