// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TCTRadii, TCTRadiiT } from './TCTRadii.js';
import { tctIntensityCategory } from './tctIntensityCategory.js';


/**
 * One point along a track.
 */
export class TCTPoint implements flatbuffers.IUnpackableObject<TCTPointT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TCTPoint {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTCTPoint(bb:flatbuffers.ByteBuffer, obj?:TCTPoint):TCTPoint {
  return (obj || new TCTPoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTCTPoint(bb:flatbuffers.ByteBuffer, obj?:TCTPoint):TCTPoint {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TCTPoint()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Time the point is valid at, Unix milliseconds UTC.
 */
VALID_TIME_MS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Lead from INIT_TIME_MS, hours; 0 for observed tracks.
 */
LEAD_HOURS():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Centre latitude, degrees north.
 */
LATITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Centre longitude, degrees east in [-180, 180).
 */
LONGITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Maximum sustained wind over WIND_AVERAGING_PERIOD_S, m/s
 * (1 kt = 1852/3600 m/s).
 */
MAX_SUSTAINED_WIND_MS():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Minimum central pressure, pascal (1 hPa = 100 Pa).
 */
MIN_CENTRAL_PRESSURE_PA():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Radius of maximum wind, km; negative when not reported.
 */
RADIUS_MAX_WIND_KM():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Wind radii at each reported threshold.
 */
RADII(index: number, obj?:TCTRadii):TCTRadii|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? (obj || new TCTRadii()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

radiiLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Intensity category at this point.
 */
CATEGORY():tctIntensityCategory {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : tctIntensityCategory.Unspecified;
}

/**
 * Probability in [0, 1] that the system exists at this point; 1 for
 * observed tracks and unconditional forecasts.
 */
EXISTENCE_PROBABILITY():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

/**
 * Direction of motion, degrees clockwise from north; negative when not
 * reported.
 */
MOTION_DIRECTION_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Speed of motion, m/s; negative when not reported.
 */
MOTION_SPEED_MS():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

static startTCTPoint(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addValidTimeMs(builder:flatbuffers.Builder, VALID_TIME_MS:bigint) {
  builder.addFieldInt64(0, VALID_TIME_MS, BigInt('0'));
}

static addLeadHours(builder:flatbuffers.Builder, LEAD_HOURS:number) {
  builder.addFieldFloat32(1, LEAD_HOURS, 0.0);
}

static addLatitude(builder:flatbuffers.Builder, LATITUDE:number) {
  builder.addFieldFloat64(2, LATITUDE, 0.0);
}

static addLongitude(builder:flatbuffers.Builder, LONGITUDE:number) {
  builder.addFieldFloat64(3, LONGITUDE, 0.0);
}

static addMaxSustainedWindMs(builder:flatbuffers.Builder, MAX_SUSTAINED_WIND_MS:number) {
  builder.addFieldFloat32(4, MAX_SUSTAINED_WIND_MS, 0.0);
}

static addMinCentralPressurePa(builder:flatbuffers.Builder, MIN_CENTRAL_PRESSURE_PA:number) {
  builder.addFieldFloat32(5, MIN_CENTRAL_PRESSURE_PA, 0.0);
}

static addRadiusMaxWindKm(builder:flatbuffers.Builder, RADIUS_MAX_WIND_KM:number) {
  builder.addFieldFloat32(6, RADIUS_MAX_WIND_KM, 0.0);
}

static addRadii(builder:flatbuffers.Builder, RADIIOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, RADIIOffset, 0);
}

static createRadiiVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startRadiiVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCategory(builder:flatbuffers.Builder, CATEGORY:tctIntensityCategory) {
  builder.addFieldInt8(8, CATEGORY, tctIntensityCategory.Unspecified);
}

static addExistenceProbability(builder:flatbuffers.Builder, EXISTENCE_PROBABILITY:number) {
  builder.addFieldFloat32(9, EXISTENCE_PROBABILITY, 1.0);
}

static addMotionDirectionDeg(builder:flatbuffers.Builder, MOTION_DIRECTION_DEG:number) {
  builder.addFieldFloat32(10, MOTION_DIRECTION_DEG, 0.0);
}

static addMotionSpeedMs(builder:flatbuffers.Builder, MOTION_SPEED_MS:number) {
  builder.addFieldFloat32(11, MOTION_SPEED_MS, 0.0);
}

static endTCTPoint(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTCTPoint(builder:flatbuffers.Builder, VALID_TIME_MS:bigint, LEAD_HOURS:number, LATITUDE:number, LONGITUDE:number, MAX_SUSTAINED_WIND_MS:number, MIN_CENTRAL_PRESSURE_PA:number, RADIUS_MAX_WIND_KM:number, RADIIOffset:flatbuffers.Offset, CATEGORY:tctIntensityCategory, EXISTENCE_PROBABILITY:number, MOTION_DIRECTION_DEG:number, MOTION_SPEED_MS:number):flatbuffers.Offset {
  TCTPoint.startTCTPoint(builder);
  TCTPoint.addValidTimeMs(builder, VALID_TIME_MS);
  TCTPoint.addLeadHours(builder, LEAD_HOURS);
  TCTPoint.addLatitude(builder, LATITUDE);
  TCTPoint.addLongitude(builder, LONGITUDE);
  TCTPoint.addMaxSustainedWindMs(builder, MAX_SUSTAINED_WIND_MS);
  TCTPoint.addMinCentralPressurePa(builder, MIN_CENTRAL_PRESSURE_PA);
  TCTPoint.addRadiusMaxWindKm(builder, RADIUS_MAX_WIND_KM);
  TCTPoint.addRadii(builder, RADIIOffset);
  TCTPoint.addCategory(builder, CATEGORY);
  TCTPoint.addExistenceProbability(builder, EXISTENCE_PROBABILITY);
  TCTPoint.addMotionDirectionDeg(builder, MOTION_DIRECTION_DEG);
  TCTPoint.addMotionSpeedMs(builder, MOTION_SPEED_MS);
  return TCTPoint.endTCTPoint(builder);
}

unpack(): TCTPointT {
  return new TCTPointT(
    this.VALID_TIME_MS(),
    this.LEAD_HOURS(),
    this.LATITUDE(),
    this.LONGITUDE(),
    this.MAX_SUSTAINED_WIND_MS(),
    this.MIN_CENTRAL_PRESSURE_PA(),
    this.RADIUS_MAX_WIND_KM(),
    this.bb!.createObjList<TCTRadii, TCTRadiiT>(this.RADII.bind(this), this.radiiLength()),
    this.CATEGORY(),
    this.EXISTENCE_PROBABILITY(),
    this.MOTION_DIRECTION_DEG(),
    this.MOTION_SPEED_MS()
  );
}


unpackTo(_o: TCTPointT): void {
  _o.VALID_TIME_MS = this.VALID_TIME_MS();
  _o.LEAD_HOURS = this.LEAD_HOURS();
  _o.LATITUDE = this.LATITUDE();
  _o.LONGITUDE = this.LONGITUDE();
  _o.MAX_SUSTAINED_WIND_MS = this.MAX_SUSTAINED_WIND_MS();
  _o.MIN_CENTRAL_PRESSURE_PA = this.MIN_CENTRAL_PRESSURE_PA();
  _o.RADIUS_MAX_WIND_KM = this.RADIUS_MAX_WIND_KM();
  _o.RADII = this.bb!.createObjList<TCTRadii, TCTRadiiT>(this.RADII.bind(this), this.radiiLength());
  _o.CATEGORY = this.CATEGORY();
  _o.EXISTENCE_PROBABILITY = this.EXISTENCE_PROBABILITY();
  _o.MOTION_DIRECTION_DEG = this.MOTION_DIRECTION_DEG();
  _o.MOTION_SPEED_MS = this.MOTION_SPEED_MS();
}
}

export class TCTPointT implements flatbuffers.IGeneratedObject {
constructor(
  public VALID_TIME_MS: bigint = BigInt('0'),
  public LEAD_HOURS: number = 0.0,
  public LATITUDE: number = 0.0,
  public LONGITUDE: number = 0.0,
  public MAX_SUSTAINED_WIND_MS: number = 0.0,
  public MIN_CENTRAL_PRESSURE_PA: number = 0.0,
  public RADIUS_MAX_WIND_KM: number = 0.0,
  public RADII: (TCTRadiiT)[] = [],
  public CATEGORY: tctIntensityCategory = tctIntensityCategory.Unspecified,
  public EXISTENCE_PROBABILITY: number = 1.0,
  public MOTION_DIRECTION_DEG: number = 0.0,
  public MOTION_SPEED_MS: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const RADII = TCTPoint.createRadiiVector(builder, builder.createObjectOffsetList(this.RADII));

  return TCTPoint.createTCTPoint(builder,
    this.VALID_TIME_MS,
    this.LEAD_HOURS,
    this.LATITUDE,
    this.LONGITUDE,
    this.MAX_SUSTAINED_WIND_MS,
    this.MIN_CENTRAL_PRESSURE_PA,
    this.RADIUS_MAX_WIND_KM,
    RADII,
    this.CATEGORY,
    this.EXISTENCE_PROBABILITY,
    this.MOTION_DIRECTION_DEG,
    this.MOTION_SPEED_MS
  );
}
}
