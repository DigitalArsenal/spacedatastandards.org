// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { VCFAlias, VCFAliasT } from './VCFAlias.js';
import { VCFConformance, VCFConformanceT } from './VCFConformance.js';
import { VCFProperty, VCFPropertyT } from './VCFProperty.js';
import { VCFSubject, VCFSubjectT } from './VCFSubject.js';
import { vcfCardForm } from './vcfCardForm.js';
import { vcfPhotoPolicy } from './vcfPhotoPolicy.js';


/**
 * vCard Projection Card
 */
export class VCF implements flatbuffers.IUnpackableObject<VCFT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VCF {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVCF(bb:flatbuffers.ByteBuffer, obj?:VCF):VCF {
  return (obj || new VCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVCF(bb:flatbuffers.ByteBuffer, obj?:VCF):VCF {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$VCF');
}

/**
 * Publisher-stable identifier of this card. Together with the publishing key
 * it is the card's identity across every republication.
 */
CARD_ID():string|null
CARD_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CARD_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Which canonical emission form this record carries.
 */
FORM():vcfCardForm {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : vcfCardForm.UNSPECIFIED;
}

/**
 * The emitted card text VERBATIM: every octet an importer will read,
 * including folding and line terminators. Authoritative over PROPERTIES and
 * ALIASES in every disagreement.
 */
CARD():string
CARD(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
CARD(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Media type the card is served as, verbatim, including its charset
 * parameter.
 */
MEDIA_TYPE():string|null
MEDIA_TYPE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MEDIA_TYPE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The profile this card projects and the provenance binding them.
 */
SUBJECT(obj?:VCFSubject):VCFSubject|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new VCFSubject()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * The publisher's parse of `CARD`, in canonical emission order.
 */
PROPERTIES(index: number, obj?:VCFProperty):VCFProperty|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new VCFProperty()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

propertiesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * The identity address rows carried by `CARD`, in canonical emission order.
 */
ALIASES(index: number, obj?:VCFAlias):VCFAlias|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new VCFAlias()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

aliasesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * How the card carries a portrait, if at all.
 */
PHOTO_POLICY():vcfPhotoPolicy {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : vcfPhotoPolicy.UNSPECIFIED;
}

/**
 * Locator of the portrait content under URI_REFERENCE, verbatim as emitted.
 * Empty under every other policy.
 */
PHOTO_URI():string|null
PHOTO_URI(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PHOTO_URI(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Media type of the portrait content verbatim, when one is carried.
 */
PHOTO_MEDIA_TYPE():string|null
PHOTO_MEDIA_TYPE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PHOTO_MEDIA_TYPE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Emission facts and the digest two implementations compare.
 */
CONFORMANCE(obj?:VCFConformance):VCFConformance|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? (obj || new VCFConformance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Reverse-DNS identifier of the component that produced the card, so two
 * projectors are distinguishable when their bytes differ.
 */
PRODUCER_ID():string|null
PRODUCER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PRODUCER_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Build or release identifier of that component verbatim.
 */
PRODUCER_VERSION():string|null
PRODUCER_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PRODUCER_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp at which the card was projected.
 */
GENERATED_AT():string|null
GENERATED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
GENERATED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp after which a consumer re-projects
 * from the profile rather than serving this card. Empty means no expiry.
 */
EXPIRES_AT():string|null
EXPIRES_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EXPIRES_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * `namespace:value` facets, sorted and deduplicated. See TAG GRAMMAR above.
 */
TAGS(index: number):string
TAGS(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
TAGS(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

tagsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Publisher note about this card; never a substitute for a field.
 */
NOTES():string|null
NOTES(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NOTES(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startVCF(builder:flatbuffers.Builder) {
  builder.startObject(17);
}

static addCardId(builder:flatbuffers.Builder, CARD_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, CARD_IDOffset, 0);
}

static addForm(builder:flatbuffers.Builder, FORM:vcfCardForm) {
  builder.addFieldInt8(1, FORM, vcfCardForm.UNSPECIFIED);
}

static addCard(builder:flatbuffers.Builder, CARDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, CARDOffset, 0);
}

static addMediaType(builder:flatbuffers.Builder, MEDIA_TYPEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, MEDIA_TYPEOffset, 0);
}

static addSubject(builder:flatbuffers.Builder, SUBJECTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, SUBJECTOffset, 0);
}

static addProperties(builder:flatbuffers.Builder, PROPERTIESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, PROPERTIESOffset, 0);
}

static createPropertiesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPropertiesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addAliases(builder:flatbuffers.Builder, ALIASESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, ALIASESOffset, 0);
}

static createAliasesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAliasesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addPhotoPolicy(builder:flatbuffers.Builder, PHOTO_POLICY:vcfPhotoPolicy) {
  builder.addFieldInt8(7, PHOTO_POLICY, vcfPhotoPolicy.UNSPECIFIED);
}

static addPhotoUri(builder:flatbuffers.Builder, PHOTO_URIOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, PHOTO_URIOffset, 0);
}

static addPhotoMediaType(builder:flatbuffers.Builder, PHOTO_MEDIA_TYPEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, PHOTO_MEDIA_TYPEOffset, 0);
}

static addConformance(builder:flatbuffers.Builder, CONFORMANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, CONFORMANCEOffset, 0);
}

static addProducerId(builder:flatbuffers.Builder, PRODUCER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, PRODUCER_IDOffset, 0);
}

static addProducerVersion(builder:flatbuffers.Builder, PRODUCER_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, PRODUCER_VERSIONOffset, 0);
}

static addGeneratedAt(builder:flatbuffers.Builder, GENERATED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, GENERATED_ATOffset, 0);
}

static addExpiresAt(builder:flatbuffers.Builder, EXPIRES_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, EXPIRES_ATOffset, 0);
}

static addTags(builder:flatbuffers.Builder, TAGSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(15, TAGSOffset, 0);
}

static createTagsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTagsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addNotes(builder:flatbuffers.Builder, NOTESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(16, NOTESOffset, 0);
}

static endVCF(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 8) // CARD
  return offset;
}

static finishVCFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$VCF');
}

static finishSizePrefixedVCFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$VCF', true);
}


unpack(): VCFT {
  return new VCFT(
    this.CARD_ID(),
    this.FORM(),
    this.CARD(),
    this.MEDIA_TYPE(),
    (this.SUBJECT() !== null ? this.SUBJECT()!.unpack() : null),
    this.bb!.createObjList<VCFProperty, VCFPropertyT>(this.PROPERTIES.bind(this), this.propertiesLength()),
    this.bb!.createObjList<VCFAlias, VCFAliasT>(this.ALIASES.bind(this), this.aliasesLength()),
    this.PHOTO_POLICY(),
    this.PHOTO_URI(),
    this.PHOTO_MEDIA_TYPE(),
    (this.CONFORMANCE() !== null ? this.CONFORMANCE()!.unpack() : null),
    this.PRODUCER_ID(),
    this.PRODUCER_VERSION(),
    this.GENERATED_AT(),
    this.EXPIRES_AT(),
    this.bb!.createScalarList<string>(this.TAGS.bind(this), this.tagsLength()),
    this.NOTES()
  );
}


unpackTo(_o: VCFT): void {
  _o.CARD_ID = this.CARD_ID();
  _o.FORM = this.FORM();
  _o.CARD = this.CARD();
  _o.MEDIA_TYPE = this.MEDIA_TYPE();
  _o.SUBJECT = (this.SUBJECT() !== null ? this.SUBJECT()!.unpack() : null);
  _o.PROPERTIES = this.bb!.createObjList<VCFProperty, VCFPropertyT>(this.PROPERTIES.bind(this), this.propertiesLength());
  _o.ALIASES = this.bb!.createObjList<VCFAlias, VCFAliasT>(this.ALIASES.bind(this), this.aliasesLength());
  _o.PHOTO_POLICY = this.PHOTO_POLICY();
  _o.PHOTO_URI = this.PHOTO_URI();
  _o.PHOTO_MEDIA_TYPE = this.PHOTO_MEDIA_TYPE();
  _o.CONFORMANCE = (this.CONFORMANCE() !== null ? this.CONFORMANCE()!.unpack() : null);
  _o.PRODUCER_ID = this.PRODUCER_ID();
  _o.PRODUCER_VERSION = this.PRODUCER_VERSION();
  _o.GENERATED_AT = this.GENERATED_AT();
  _o.EXPIRES_AT = this.EXPIRES_AT();
  _o.TAGS = this.bb!.createScalarList<string>(this.TAGS.bind(this), this.tagsLength());
  _o.NOTES = this.NOTES();
}
}

export class VCFT implements flatbuffers.IGeneratedObject {
constructor(
  public CARD_ID: string|Uint8Array|null = null,
  public FORM: vcfCardForm = vcfCardForm.UNSPECIFIED,
  public CARD: string|Uint8Array|null = null,
  public MEDIA_TYPE: string|Uint8Array|null = null,
  public SUBJECT: VCFSubjectT|null = null,
  public PROPERTIES: (VCFPropertyT)[] = [],
  public ALIASES: (VCFAliasT)[] = [],
  public PHOTO_POLICY: vcfPhotoPolicy = vcfPhotoPolicy.UNSPECIFIED,
  public PHOTO_URI: string|Uint8Array|null = null,
  public PHOTO_MEDIA_TYPE: string|Uint8Array|null = null,
  public CONFORMANCE: VCFConformanceT|null = null,
  public PRODUCER_ID: string|Uint8Array|null = null,
  public PRODUCER_VERSION: string|Uint8Array|null = null,
  public GENERATED_AT: string|Uint8Array|null = null,
  public EXPIRES_AT: string|Uint8Array|null = null,
  public TAGS: (string)[] = [],
  public NOTES: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CARD_ID = (this.CARD_ID !== null ? builder.createString(this.CARD_ID!) : 0);
  const CARD = (this.CARD !== null ? builder.createString(this.CARD!) : 0);
  const MEDIA_TYPE = (this.MEDIA_TYPE !== null ? builder.createString(this.MEDIA_TYPE!) : 0);
  const SUBJECT = (this.SUBJECT !== null ? this.SUBJECT!.pack(builder) : 0);
  const PROPERTIES = VCF.createPropertiesVector(builder, builder.createObjectOffsetList(this.PROPERTIES));
  const ALIASES = VCF.createAliasesVector(builder, builder.createObjectOffsetList(this.ALIASES));
  const PHOTO_URI = (this.PHOTO_URI !== null ? builder.createString(this.PHOTO_URI!) : 0);
  const PHOTO_MEDIA_TYPE = (this.PHOTO_MEDIA_TYPE !== null ? builder.createString(this.PHOTO_MEDIA_TYPE!) : 0);
  const CONFORMANCE = (this.CONFORMANCE !== null ? this.CONFORMANCE!.pack(builder) : 0);
  const PRODUCER_ID = (this.PRODUCER_ID !== null ? builder.createString(this.PRODUCER_ID!) : 0);
  const PRODUCER_VERSION = (this.PRODUCER_VERSION !== null ? builder.createString(this.PRODUCER_VERSION!) : 0);
  const GENERATED_AT = (this.GENERATED_AT !== null ? builder.createString(this.GENERATED_AT!) : 0);
  const EXPIRES_AT = (this.EXPIRES_AT !== null ? builder.createString(this.EXPIRES_AT!) : 0);
  const TAGS = VCF.createTagsVector(builder, builder.createObjectOffsetList(this.TAGS));
  const NOTES = (this.NOTES !== null ? builder.createString(this.NOTES!) : 0);

  VCF.startVCF(builder);
  VCF.addCardId(builder, CARD_ID);
  VCF.addForm(builder, this.FORM);
  VCF.addCard(builder, CARD);
  VCF.addMediaType(builder, MEDIA_TYPE);
  VCF.addSubject(builder, SUBJECT);
  VCF.addProperties(builder, PROPERTIES);
  VCF.addAliases(builder, ALIASES);
  VCF.addPhotoPolicy(builder, this.PHOTO_POLICY);
  VCF.addPhotoUri(builder, PHOTO_URI);
  VCF.addPhotoMediaType(builder, PHOTO_MEDIA_TYPE);
  VCF.addConformance(builder, CONFORMANCE);
  VCF.addProducerId(builder, PRODUCER_ID);
  VCF.addProducerVersion(builder, PRODUCER_VERSION);
  VCF.addGeneratedAt(builder, GENERATED_AT);
  VCF.addExpiresAt(builder, EXPIRES_AT);
  VCF.addTags(builder, TAGS);
  VCF.addNotes(builder, NOTES);

  return VCF.endVCF(builder);
}
}
