// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { vcfAliasKind } from './vcfAliasKind.js';


/**
 * One identity address row: the machine-readable half of the card.
 */
export class VCFAlias implements flatbuffers.IUnpackableObject<VCFAliasT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VCFAlias {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVCFAlias(bb:flatbuffers.ByteBuffer, obj?:VCFAlias):VCFAlias {
  return (obj || new VCFAlias()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVCFAlias(bb:flatbuffers.ByteBuffer, obj?:VCFAlias):VCFAlias {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VCFAlias()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * What the row carries.
 */
KIND():vcfAliasKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : vcfAliasKind.UNSPECIFIED;
}

/**
 * The row's kind label verbatim as emitted — the leftmost domain label.
 * Authoritative when KIND is OTHER.
 */
KIND_TOKEN():string
KIND_TOKEN(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
KIND_TOKEN(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Everything left of the separator in the emitted address, verbatim.
 */
LOCAL_PART():string
LOCAL_PART(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
LOCAL_PART(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Everything right of the separator in the emitted address, verbatim. This
 * is publisher data: the identity domain of the deployment that produced the
 * card, never a value this standard fixes.
 */
DOMAIN():string
DOMAIN(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
DOMAIN(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * The complete emitted address, `LOCAL_PART` then the separator then
 * `DOMAIN`, verbatim. Carried whole because it is what the importer stores
 * and what a consumer matches on.
 */
ADDRESS():string
ADDRESS(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
ADDRESS(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Transport encoding of LOCAL_PART verbatim and lowercase. An address
 * local part has a hard octet cap, so raw hexadecimal key material does not
 * fit and a compact binary-to-text encoding is REQUIRED for key rows.
 */
ENCODING():string|null
ENCODING(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ENCODING(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Cryptographic algorithm the row's key or signature belongs to, verbatim as
 * the profile states it. A key without a named algorithm is not verifiable.
 */
ALGORITHM():string|null
ALGORITHM(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ALGORITHM(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The profile's published public key value verbatim, for key rows. The SAME
 * bytes LOCAL_PART transport-encodes, carried in the profile's own spelling
 * so a consumer can join card to profile without decoding. NEVER an extended
 * public key and NEVER a derivation path.
 */
PUBLIC_KEY():string|null
PUBLIC_KEY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PUBLIC_KEY(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Ledger token verbatim for LEDGER_ACCOUNT rows.
 */
CHAIN():string|null
CHAIN(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CHAIN(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Whether this row survives into the COMPACT form.
 */
ON_COMPACT_CARD():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Octet length of the complete UNFOLDED row as emitted.
 */
OCTET_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startVCFAlias(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addKind(builder:flatbuffers.Builder, KIND:vcfAliasKind) {
  builder.addFieldInt8(0, KIND, vcfAliasKind.UNSPECIFIED);
}

static addKindToken(builder:flatbuffers.Builder, KIND_TOKENOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, KIND_TOKENOffset, 0);
}

static addLocalPart(builder:flatbuffers.Builder, LOCAL_PARTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, LOCAL_PARTOffset, 0);
}

static addDomain(builder:flatbuffers.Builder, DOMAINOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, DOMAINOffset, 0);
}

static addAddress(builder:flatbuffers.Builder, ADDRESSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, ADDRESSOffset, 0);
}

static addEncoding(builder:flatbuffers.Builder, ENCODINGOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ENCODINGOffset, 0);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, ALGORITHMOffset, 0);
}

static addPublicKey(builder:flatbuffers.Builder, PUBLIC_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, PUBLIC_KEYOffset, 0);
}

static addChain(builder:flatbuffers.Builder, CHAINOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, CHAINOffset, 0);
}

static addOnCompactCard(builder:flatbuffers.Builder, ON_COMPACT_CARD:boolean) {
  builder.addFieldInt8(9, +ON_COMPACT_CARD, +false);
}

static addOctetLength(builder:flatbuffers.Builder, OCTET_LENGTH:number) {
  builder.addFieldInt32(10, OCTET_LENGTH, 0);
}

static endVCFAlias(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // KIND_TOKEN
  builder.requiredField(offset, 8) // LOCAL_PART
  builder.requiredField(offset, 10) // DOMAIN
  builder.requiredField(offset, 12) // ADDRESS
  return offset;
}

static createVCFAlias(builder:flatbuffers.Builder, KIND:vcfAliasKind, KIND_TOKENOffset:flatbuffers.Offset, LOCAL_PARTOffset:flatbuffers.Offset, DOMAINOffset:flatbuffers.Offset, ADDRESSOffset:flatbuffers.Offset, ENCODINGOffset:flatbuffers.Offset, ALGORITHMOffset:flatbuffers.Offset, PUBLIC_KEYOffset:flatbuffers.Offset, CHAINOffset:flatbuffers.Offset, ON_COMPACT_CARD:boolean, OCTET_LENGTH:number):flatbuffers.Offset {
  VCFAlias.startVCFAlias(builder);
  VCFAlias.addKind(builder, KIND);
  VCFAlias.addKindToken(builder, KIND_TOKENOffset);
  VCFAlias.addLocalPart(builder, LOCAL_PARTOffset);
  VCFAlias.addDomain(builder, DOMAINOffset);
  VCFAlias.addAddress(builder, ADDRESSOffset);
  VCFAlias.addEncoding(builder, ENCODINGOffset);
  VCFAlias.addAlgorithm(builder, ALGORITHMOffset);
  VCFAlias.addPublicKey(builder, PUBLIC_KEYOffset);
  VCFAlias.addChain(builder, CHAINOffset);
  VCFAlias.addOnCompactCard(builder, ON_COMPACT_CARD);
  VCFAlias.addOctetLength(builder, OCTET_LENGTH);
  return VCFAlias.endVCFAlias(builder);
}

unpack(): VCFAliasT {
  return new VCFAliasT(
    this.KIND(),
    this.KIND_TOKEN(),
    this.LOCAL_PART(),
    this.DOMAIN(),
    this.ADDRESS(),
    this.ENCODING(),
    this.ALGORITHM(),
    this.PUBLIC_KEY(),
    this.CHAIN(),
    this.ON_COMPACT_CARD(),
    this.OCTET_LENGTH()
  );
}


unpackTo(_o: VCFAliasT): void {
  _o.KIND = this.KIND();
  _o.KIND_TOKEN = this.KIND_TOKEN();
  _o.LOCAL_PART = this.LOCAL_PART();
  _o.DOMAIN = this.DOMAIN();
  _o.ADDRESS = this.ADDRESS();
  _o.ENCODING = this.ENCODING();
  _o.ALGORITHM = this.ALGORITHM();
  _o.PUBLIC_KEY = this.PUBLIC_KEY();
  _o.CHAIN = this.CHAIN();
  _o.ON_COMPACT_CARD = this.ON_COMPACT_CARD();
  _o.OCTET_LENGTH = this.OCTET_LENGTH();
}
}

export class VCFAliasT implements flatbuffers.IGeneratedObject {
constructor(
  public KIND: vcfAliasKind = vcfAliasKind.UNSPECIFIED,
  public KIND_TOKEN: string|Uint8Array|null = null,
  public LOCAL_PART: string|Uint8Array|null = null,
  public DOMAIN: string|Uint8Array|null = null,
  public ADDRESS: string|Uint8Array|null = null,
  public ENCODING: string|Uint8Array|null = null,
  public ALGORITHM: string|Uint8Array|null = null,
  public PUBLIC_KEY: string|Uint8Array|null = null,
  public CHAIN: string|Uint8Array|null = null,
  public ON_COMPACT_CARD: boolean = false,
  public OCTET_LENGTH: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const KIND_TOKEN = (this.KIND_TOKEN !== null ? builder.createString(this.KIND_TOKEN!) : 0);
  const LOCAL_PART = (this.LOCAL_PART !== null ? builder.createString(this.LOCAL_PART!) : 0);
  const DOMAIN = (this.DOMAIN !== null ? builder.createString(this.DOMAIN!) : 0);
  const ADDRESS = (this.ADDRESS !== null ? builder.createString(this.ADDRESS!) : 0);
  const ENCODING = (this.ENCODING !== null ? builder.createString(this.ENCODING!) : 0);
  const ALGORITHM = (this.ALGORITHM !== null ? builder.createString(this.ALGORITHM!) : 0);
  const PUBLIC_KEY = (this.PUBLIC_KEY !== null ? builder.createString(this.PUBLIC_KEY!) : 0);
  const CHAIN = (this.CHAIN !== null ? builder.createString(this.CHAIN!) : 0);

  return VCFAlias.createVCFAlias(builder,
    this.KIND,
    KIND_TOKEN,
    LOCAL_PART,
    DOMAIN,
    ADDRESS,
    ENCODING,
    ALGORITHM,
    PUBLIC_KEY,
    CHAIN,
    this.ON_COMPACT_CARD,
    this.OCTET_LENGTH
  );
}
}
