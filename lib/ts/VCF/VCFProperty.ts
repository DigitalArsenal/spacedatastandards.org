// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One emitted card property, as the publisher parsed its own output.
 */
export class VCFProperty implements flatbuffers.IUnpackableObject<VCFPropertyT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VCFProperty {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVCFProperty(bb:flatbuffers.ByteBuffer, obj?:VCFProperty):VCFProperty {
  return (obj || new VCFProperty()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVCFProperty(bb:flatbuffers.ByteBuffer, obj?:VCFProperty):VCFProperty {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VCFProperty()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Property name verbatim and upper-case, e.g. `EMAIL`.
 */
NAME():string
NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
NAME(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Parameter tokens verbatim in emission order, each a single `name=value`
 * pair. A parameter the emitter wrote without a value is carried verbatim.
 */
PARAMETERS(index: number):string
PARAMETERS(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
PARAMETERS(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

parametersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Property value with the text format's escaping REMOVED, so a consumer
 * reads a value rather than re-implementing an unescaper. `CARD` retains the
 * escaped form.
 */
VALUE():string|null
VALUE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VALUE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Grouping prefix the property was emitted under, when the emitter used one.
 */
GROUP():string|null
GROUP(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
GROUP(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Zero-based position of this property in the canonical emission order for
 * this FORM. Two conforming emitters agree on this number.
 */
ORDER():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Octet length of the property's complete UNFOLDED content line, excluding
 * the line terminator. This is the number folding is applied against.
 */
OCTET_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startVCFProperty(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addName(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, NAMEOffset, 0);
}

static addParameters(builder:flatbuffers.Builder, PARAMETERSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, PARAMETERSOffset, 0);
}

static createParametersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startParametersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addValue(builder:flatbuffers.Builder, VALUEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, VALUEOffset, 0);
}

static addGroup(builder:flatbuffers.Builder, GROUPOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, GROUPOffset, 0);
}

static addOrder(builder:flatbuffers.Builder, ORDER:number) {
  builder.addFieldInt32(4, ORDER, 0);
}

static addOctetLength(builder:flatbuffers.Builder, OCTET_LENGTH:number) {
  builder.addFieldInt32(5, OCTET_LENGTH, 0);
}

static endVCFProperty(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // NAME
  return offset;
}

static createVCFProperty(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset, PARAMETERSOffset:flatbuffers.Offset, VALUEOffset:flatbuffers.Offset, GROUPOffset:flatbuffers.Offset, ORDER:number, OCTET_LENGTH:number):flatbuffers.Offset {
  VCFProperty.startVCFProperty(builder);
  VCFProperty.addName(builder, NAMEOffset);
  VCFProperty.addParameters(builder, PARAMETERSOffset);
  VCFProperty.addValue(builder, VALUEOffset);
  VCFProperty.addGroup(builder, GROUPOffset);
  VCFProperty.addOrder(builder, ORDER);
  VCFProperty.addOctetLength(builder, OCTET_LENGTH);
  return VCFProperty.endVCFProperty(builder);
}

unpack(): VCFPropertyT {
  return new VCFPropertyT(
    this.NAME(),
    this.bb!.createScalarList<string>(this.PARAMETERS.bind(this), this.parametersLength()),
    this.VALUE(),
    this.GROUP(),
    this.ORDER(),
    this.OCTET_LENGTH()
  );
}


unpackTo(_o: VCFPropertyT): void {
  _o.NAME = this.NAME();
  _o.PARAMETERS = this.bb!.createScalarList<string>(this.PARAMETERS.bind(this), this.parametersLength());
  _o.VALUE = this.VALUE();
  _o.GROUP = this.GROUP();
  _o.ORDER = this.ORDER();
  _o.OCTET_LENGTH = this.OCTET_LENGTH();
}
}

export class VCFPropertyT implements flatbuffers.IGeneratedObject {
constructor(
  public NAME: string|Uint8Array|null = null,
  public PARAMETERS: (string)[] = [],
  public VALUE: string|Uint8Array|null = null,
  public GROUP: string|Uint8Array|null = null,
  public ORDER: number = 0,
  public OCTET_LENGTH: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const NAME = (this.NAME !== null ? builder.createString(this.NAME!) : 0);
  const PARAMETERS = VCFProperty.createParametersVector(builder, builder.createObjectOffsetList(this.PARAMETERS));
  const VALUE = (this.VALUE !== null ? builder.createString(this.VALUE!) : 0);
  const GROUP = (this.GROUP !== null ? builder.createString(this.GROUP!) : 0);

  return VCFProperty.createVCFProperty(builder,
    NAME,
    PARAMETERS,
    VALUE,
    GROUP,
    this.ORDER,
    this.OCTET_LENGTH
  );
}
}
