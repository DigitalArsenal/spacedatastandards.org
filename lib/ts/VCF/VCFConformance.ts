// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { vcfLineEnding } from './vcfLineEnding.js';


/**
 * Everything needed to decide whether this card is servable, and whether two
 * implementations produced the same bytes.
 */
export class VCFConformance implements flatbuffers.IUnpackableObject<VCFConformanceT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VCFConformance {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVCFConformance(bb:flatbuffers.ByteBuffer, obj?:VCFConformance):VCFConformance {
  return (obj || new VCFConformance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVCFConformance(bb:flatbuffers.ByteBuffer, obj?:VCFConformance):VCFConformance {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VCFConformance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Card format version token verbatim as emitted, e.g. `3.0`.
 */
VERSION():string
VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
VERSION(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Identifier of the emission profile the card conforms to, verbatim, naming
 * the annex revision the publisher implemented.
 */
PROFILE():string|null
PROFILE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROFILE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Line terminator the card text uses.
 */
LINE_ENDING():vcfLineEnding {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : vcfLineEnding.UNSPECIFIED;
}

/**
 * Octet width beyond which a content line is folded onto continuation lines.
 * 0 means the publisher folded nothing, which is non-canonical.
 */
FOLD_OCTET_WIDTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Octet length of `CARD` exactly as emitted, folding and terminators
 * included. This is the number a serving surface budgets against.
 */
BYTE_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Octet length the same card would have with folding removed. Carried so a
 * folding disagreement is detectable without unfolding the text.
 */
UNFOLDED_BYTE_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Octet ceiling of the surface this card was produced for. 0 means the
 * publisher states no ceiling; it never means a ceiling of zero.
 */
BYTE_BUDGET():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Whether BYTE_LENGTH is within BYTE_BUDGET. False with a non-zero budget
 * means the card is a valid record that is NOT servable on that surface.
 */
WITHIN_BYTE_BUDGET():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * SHA-256 of `CARD` as 64 lowercase hexadecimal characters. This is the
 * value two implementations compare to prove byte-identical projection.
 */
CARD_SHA256():string|null
CARD_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CARD_SHA256(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Number of property rows emitted, unfolded. Carried so a truncated card is
 * detectable before parsing.
 */
PROPERTY_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Whether the card carries a complete verification chain: a signing-key row,
 * an encryption-key row, and a profile-signature row, all present in the
 * UNFOLDED text. A card without all three cannot verify its own subject and
 * is not servable as an identity card.
 */
CARRIES_VERIFICATION_CHAIN():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startVCFConformance(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addVersion(builder:flatbuffers.Builder, VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, VERSIONOffset, 0);
}

static addProfile(builder:flatbuffers.Builder, PROFILEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, PROFILEOffset, 0);
}

static addLineEnding(builder:flatbuffers.Builder, LINE_ENDING:vcfLineEnding) {
  builder.addFieldInt8(2, LINE_ENDING, vcfLineEnding.UNSPECIFIED);
}

static addFoldOctetWidth(builder:flatbuffers.Builder, FOLD_OCTET_WIDTH:number) {
  builder.addFieldInt32(3, FOLD_OCTET_WIDTH, 0);
}

static addByteLength(builder:flatbuffers.Builder, BYTE_LENGTH:number) {
  builder.addFieldInt32(4, BYTE_LENGTH, 0);
}

static addUnfoldedByteLength(builder:flatbuffers.Builder, UNFOLDED_BYTE_LENGTH:number) {
  builder.addFieldInt32(5, UNFOLDED_BYTE_LENGTH, 0);
}

static addByteBudget(builder:flatbuffers.Builder, BYTE_BUDGET:number) {
  builder.addFieldInt32(6, BYTE_BUDGET, 0);
}

static addWithinByteBudget(builder:flatbuffers.Builder, WITHIN_BYTE_BUDGET:boolean) {
  builder.addFieldInt8(7, +WITHIN_BYTE_BUDGET, +false);
}

static addCardSha256(builder:flatbuffers.Builder, CARD_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(8, CARD_SHA256Offset, 0);
}

static addPropertyCount(builder:flatbuffers.Builder, PROPERTY_COUNT:number) {
  builder.addFieldInt32(9, PROPERTY_COUNT, 0);
}

static addCarriesVerificationChain(builder:flatbuffers.Builder, CARRIES_VERIFICATION_CHAIN:boolean) {
  builder.addFieldInt8(10, +CARRIES_VERIFICATION_CHAIN, +false);
}

static endVCFConformance(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // VERSION
  return offset;
}

static createVCFConformance(builder:flatbuffers.Builder, VERSIONOffset:flatbuffers.Offset, PROFILEOffset:flatbuffers.Offset, LINE_ENDING:vcfLineEnding, FOLD_OCTET_WIDTH:number, BYTE_LENGTH:number, UNFOLDED_BYTE_LENGTH:number, BYTE_BUDGET:number, WITHIN_BYTE_BUDGET:boolean, CARD_SHA256Offset:flatbuffers.Offset, PROPERTY_COUNT:number, CARRIES_VERIFICATION_CHAIN:boolean):flatbuffers.Offset {
  VCFConformance.startVCFConformance(builder);
  VCFConformance.addVersion(builder, VERSIONOffset);
  VCFConformance.addProfile(builder, PROFILEOffset);
  VCFConformance.addLineEnding(builder, LINE_ENDING);
  VCFConformance.addFoldOctetWidth(builder, FOLD_OCTET_WIDTH);
  VCFConformance.addByteLength(builder, BYTE_LENGTH);
  VCFConformance.addUnfoldedByteLength(builder, UNFOLDED_BYTE_LENGTH);
  VCFConformance.addByteBudget(builder, BYTE_BUDGET);
  VCFConformance.addWithinByteBudget(builder, WITHIN_BYTE_BUDGET);
  VCFConformance.addCardSha256(builder, CARD_SHA256Offset);
  VCFConformance.addPropertyCount(builder, PROPERTY_COUNT);
  VCFConformance.addCarriesVerificationChain(builder, CARRIES_VERIFICATION_CHAIN);
  return VCFConformance.endVCFConformance(builder);
}

unpack(): VCFConformanceT {
  return new VCFConformanceT(
    this.VERSION(),
    this.PROFILE(),
    this.LINE_ENDING(),
    this.FOLD_OCTET_WIDTH(),
    this.BYTE_LENGTH(),
    this.UNFOLDED_BYTE_LENGTH(),
    this.BYTE_BUDGET(),
    this.WITHIN_BYTE_BUDGET(),
    this.CARD_SHA256(),
    this.PROPERTY_COUNT(),
    this.CARRIES_VERIFICATION_CHAIN()
  );
}


unpackTo(_o: VCFConformanceT): void {
  _o.VERSION = this.VERSION();
  _o.PROFILE = this.PROFILE();
  _o.LINE_ENDING = this.LINE_ENDING();
  _o.FOLD_OCTET_WIDTH = this.FOLD_OCTET_WIDTH();
  _o.BYTE_LENGTH = this.BYTE_LENGTH();
  _o.UNFOLDED_BYTE_LENGTH = this.UNFOLDED_BYTE_LENGTH();
  _o.BYTE_BUDGET = this.BYTE_BUDGET();
  _o.WITHIN_BYTE_BUDGET = this.WITHIN_BYTE_BUDGET();
  _o.CARD_SHA256 = this.CARD_SHA256();
  _o.PROPERTY_COUNT = this.PROPERTY_COUNT();
  _o.CARRIES_VERIFICATION_CHAIN = this.CARRIES_VERIFICATION_CHAIN();
}
}

export class VCFConformanceT implements flatbuffers.IGeneratedObject {
constructor(
  public VERSION: string|Uint8Array|null = null,
  public PROFILE: string|Uint8Array|null = null,
  public LINE_ENDING: vcfLineEnding = vcfLineEnding.UNSPECIFIED,
  public FOLD_OCTET_WIDTH: number = 0,
  public BYTE_LENGTH: number = 0,
  public UNFOLDED_BYTE_LENGTH: number = 0,
  public BYTE_BUDGET: number = 0,
  public WITHIN_BYTE_BUDGET: boolean = false,
  public CARD_SHA256: string|Uint8Array|null = null,
  public PROPERTY_COUNT: number = 0,
  public CARRIES_VERIFICATION_CHAIN: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const VERSION = (this.VERSION !== null ? builder.createString(this.VERSION!) : 0);
  const PROFILE = (this.PROFILE !== null ? builder.createString(this.PROFILE!) : 0);
  const CARD_SHA256 = (this.CARD_SHA256 !== null ? builder.createString(this.CARD_SHA256!) : 0);

  return VCFConformance.createVCFConformance(builder,
    VERSION,
    PROFILE,
    this.LINE_ENDING,
    this.FOLD_OCTET_WIDTH,
    this.BYTE_LENGTH,
    this.UNFOLDED_BYTE_LENGTH,
    this.BYTE_BUDGET,
    this.WITHIN_BYTE_BUDGET,
    CARD_SHA256,
    this.PROPERTY_COUNT,
    this.CARRIES_VERIFICATION_CHAIN
  );
}
}
