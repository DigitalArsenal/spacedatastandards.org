// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { CESObjectVector, CESObjectVectorT } from './CESObjectVector.js';
import { cesNormalizationKind } from './cesNormalizationKind.js';
import { cesPoolingKind } from './cesPoolingKind.js';
import { cesSimilarityKind } from './cesSimilarityKind.js';
import { cesVectorEncoding } from './cesVectorEncoding.js';


/**
 * Catalog Embedding Shard.
 *
 * One CES publishes the dense semantic-search vector table for one dataset
 * update: one row per catalog object, alongside that update's $OMM shard and
 * $DPM manifest (DPMAsset.ASSET_KIND = EMBEDDING_SHARD). Vectors are
 * L2-normalised at the encoder's output; similarity between two rows, or
 * between a row and a query vector produced by a paired $QEM, is dot
 * product (equivalent to cosine similarity for unit-norm vectors).
 *
 * A consumer MUST compare MODEL_ID, MODEL_VERSION, MODEL_WEIGHTS_SHA256,
 * POOLING_METHOD and TEMPLATE_VERSION against a QEM record before trusting
 * cross-encoding similarity between a query vector and CES rows; a mismatch
 * on any one field means the two vector spaces are not comparable.
 */
export class CES implements flatbuffers.IUnpackableObject<CEST> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CES {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCES(bb:flatbuffers.ByteBuffer, obj?:CES):CES {
  return (obj || new CES()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCES(bb:flatbuffers.ByteBuffer, obj?:CES):CES {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CES()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$CES');
}

/**
 * Joins to DPM.DATASET_ID for the publication this shard belongs to.
 */
DATASET_ID():string|null
DATASET_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DATASET_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Joins to DPM.UPDATE_ID for the specific update this shard was computed
 * against.
 */
UPDATE_ID():string|null
UPDATE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
UPDATE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Fixed embedding dimensionality shared by every row (e.g. 384).
 */
DIMENSIONS():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Row element encoding used across ROWS.
 */
ENCODING():cesVectorEncoding {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : cesVectorEncoding.INT8;
}

/**
 * Row-vector normalization applied before quantization.
 */
NORMALIZATION():cesNormalizationKind {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : cesNormalizationKind.L2;
}

/**
 * Similarity function rows are intended to be compared with.
 */
SIMILARITY():cesSimilarityKind {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : cesSimilarityKind.DOT_PRODUCT;
}

/**
 * Encoder model identifier/name.
 */
MODEL_ID():string|null
MODEL_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MODEL_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Encoder model version.
 */
MODEL_VERSION():string|null
MODEL_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MODEL_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * SHA-256 of the encoder weights, lowercase hex. Same convention as
 * DPMAsset.BYTE_SHA256.
 */
MODEL_WEIGHTS_SHA256():string|null
MODEL_WEIGHTS_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MODEL_WEIGHTS_SHA256(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Sentence/document pooling method the encoder used.
 */
POOLING_METHOD():cesPoolingKind {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : cesPoolingKind.MEAN;
}

/**
 * Version of the text template documents were rendered from before
 * encoding. Must match a QEM's TEMPLATE_VERSION for compatible retrieval.
 */
TEMPLATE_VERSION():string|null
TEMPLATE_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TEMPLATE_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * One row per catalog object.
 */
ROWS(index: number, obj?:CESObjectVector):CESObjectVector|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? (obj || new CESObjectVector()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

rowsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startCES(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addDatasetId(builder:flatbuffers.Builder, DATASET_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, DATASET_IDOffset, 0);
}

static addUpdateId(builder:flatbuffers.Builder, UPDATE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, UPDATE_IDOffset, 0);
}

static addDimensions(builder:flatbuffers.Builder, DIMENSIONS:number) {
  builder.addFieldInt32(2, DIMENSIONS, 0);
}

static addEncoding(builder:flatbuffers.Builder, ENCODING:cesVectorEncoding) {
  builder.addFieldInt8(3, ENCODING, cesVectorEncoding.INT8);
}

static addNormalization(builder:flatbuffers.Builder, NORMALIZATION:cesNormalizationKind) {
  builder.addFieldInt8(4, NORMALIZATION, cesNormalizationKind.L2);
}

static addSimilarity(builder:flatbuffers.Builder, SIMILARITY:cesSimilarityKind) {
  builder.addFieldInt8(5, SIMILARITY, cesSimilarityKind.DOT_PRODUCT);
}

static addModelId(builder:flatbuffers.Builder, MODEL_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, MODEL_IDOffset, 0);
}

static addModelVersion(builder:flatbuffers.Builder, MODEL_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, MODEL_VERSIONOffset, 0);
}

static addModelWeightsSha256(builder:flatbuffers.Builder, MODEL_WEIGHTS_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(8, MODEL_WEIGHTS_SHA256Offset, 0);
}

static addPoolingMethod(builder:flatbuffers.Builder, POOLING_METHOD:cesPoolingKind) {
  builder.addFieldInt8(9, POOLING_METHOD, cesPoolingKind.MEAN);
}

static addTemplateVersion(builder:flatbuffers.Builder, TEMPLATE_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, TEMPLATE_VERSIONOffset, 0);
}

static addRows(builder:flatbuffers.Builder, ROWSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, ROWSOffset, 0);
}

static createRowsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startRowsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endCES(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishCESBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$CES');
}

static finishSizePrefixedCESBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$CES', true);
}

static createCES(builder:flatbuffers.Builder, DATASET_IDOffset:flatbuffers.Offset, UPDATE_IDOffset:flatbuffers.Offset, DIMENSIONS:number, ENCODING:cesVectorEncoding, NORMALIZATION:cesNormalizationKind, SIMILARITY:cesSimilarityKind, MODEL_IDOffset:flatbuffers.Offset, MODEL_VERSIONOffset:flatbuffers.Offset, MODEL_WEIGHTS_SHA256Offset:flatbuffers.Offset, POOLING_METHOD:cesPoolingKind, TEMPLATE_VERSIONOffset:flatbuffers.Offset, ROWSOffset:flatbuffers.Offset):flatbuffers.Offset {
  CES.startCES(builder);
  CES.addDatasetId(builder, DATASET_IDOffset);
  CES.addUpdateId(builder, UPDATE_IDOffset);
  CES.addDimensions(builder, DIMENSIONS);
  CES.addEncoding(builder, ENCODING);
  CES.addNormalization(builder, NORMALIZATION);
  CES.addSimilarity(builder, SIMILARITY);
  CES.addModelId(builder, MODEL_IDOffset);
  CES.addModelVersion(builder, MODEL_VERSIONOffset);
  CES.addModelWeightsSha256(builder, MODEL_WEIGHTS_SHA256Offset);
  CES.addPoolingMethod(builder, POOLING_METHOD);
  CES.addTemplateVersion(builder, TEMPLATE_VERSIONOffset);
  CES.addRows(builder, ROWSOffset);
  return CES.endCES(builder);
}

unpack(): CEST {
  return new CEST(
    this.DATASET_ID(),
    this.UPDATE_ID(),
    this.DIMENSIONS(),
    this.ENCODING(),
    this.NORMALIZATION(),
    this.SIMILARITY(),
    this.MODEL_ID(),
    this.MODEL_VERSION(),
    this.MODEL_WEIGHTS_SHA256(),
    this.POOLING_METHOD(),
    this.TEMPLATE_VERSION(),
    this.bb!.createObjList<CESObjectVector, CESObjectVectorT>(this.ROWS.bind(this), this.rowsLength())
  );
}


unpackTo(_o: CEST): void {
  _o.DATASET_ID = this.DATASET_ID();
  _o.UPDATE_ID = this.UPDATE_ID();
  _o.DIMENSIONS = this.DIMENSIONS();
  _o.ENCODING = this.ENCODING();
  _o.NORMALIZATION = this.NORMALIZATION();
  _o.SIMILARITY = this.SIMILARITY();
  _o.MODEL_ID = this.MODEL_ID();
  _o.MODEL_VERSION = this.MODEL_VERSION();
  _o.MODEL_WEIGHTS_SHA256 = this.MODEL_WEIGHTS_SHA256();
  _o.POOLING_METHOD = this.POOLING_METHOD();
  _o.TEMPLATE_VERSION = this.TEMPLATE_VERSION();
  _o.ROWS = this.bb!.createObjList<CESObjectVector, CESObjectVectorT>(this.ROWS.bind(this), this.rowsLength());
}
}

export class CEST implements flatbuffers.IGeneratedObject {
constructor(
  public DATASET_ID: string|Uint8Array|null = null,
  public UPDATE_ID: string|Uint8Array|null = null,
  public DIMENSIONS: number = 0,
  public ENCODING: cesVectorEncoding = cesVectorEncoding.INT8,
  public NORMALIZATION: cesNormalizationKind = cesNormalizationKind.L2,
  public SIMILARITY: cesSimilarityKind = cesSimilarityKind.DOT_PRODUCT,
  public MODEL_ID: string|Uint8Array|null = null,
  public MODEL_VERSION: string|Uint8Array|null = null,
  public MODEL_WEIGHTS_SHA256: string|Uint8Array|null = null,
  public POOLING_METHOD: cesPoolingKind = cesPoolingKind.MEAN,
  public TEMPLATE_VERSION: string|Uint8Array|null = null,
  public ROWS: (CESObjectVectorT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const DATASET_ID = (this.DATASET_ID !== null ? builder.createString(this.DATASET_ID!) : 0);
  const UPDATE_ID = (this.UPDATE_ID !== null ? builder.createString(this.UPDATE_ID!) : 0);
  const MODEL_ID = (this.MODEL_ID !== null ? builder.createString(this.MODEL_ID!) : 0);
  const MODEL_VERSION = (this.MODEL_VERSION !== null ? builder.createString(this.MODEL_VERSION!) : 0);
  const MODEL_WEIGHTS_SHA256 = (this.MODEL_WEIGHTS_SHA256 !== null ? builder.createString(this.MODEL_WEIGHTS_SHA256!) : 0);
  const TEMPLATE_VERSION = (this.TEMPLATE_VERSION !== null ? builder.createString(this.TEMPLATE_VERSION!) : 0);
  const ROWS = CES.createRowsVector(builder, builder.createObjectOffsetList(this.ROWS));

  return CES.createCES(builder,
    DATASET_ID,
    UPDATE_ID,
    this.DIMENSIONS,
    this.ENCODING,
    this.NORMALIZATION,
    this.SIMILARITY,
    MODEL_ID,
    MODEL_VERSION,
    MODEL_WEIGHTS_SHA256,
    this.POOLING_METHOD,
    TEMPLATE_VERSION,
    ROWS
  );
}
}
