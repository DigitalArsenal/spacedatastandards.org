// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TRHAntenna, TRHAntennaT } from './TRHAntenna.js';
import { TRHAttestation, TRHAttestationT } from './TRHAttestation.js';
import { TRHInstallation, TRHInstallationT } from './TRHInstallation.js';
import { TRHReceiver, TRHReceiverT } from './TRHReceiver.js';
import { TRHTransmitter, TRHTransmitterT } from './TRHTransmitter.js';
import { TRHTransponder, TRHTransponderT } from './TRHTransponder.js';


/**
 * Tracking Hardware. Installations attach antennas, transmitters, receivers
 * and transponders to either a surface site or a tracked object. Delays are
 * seconds, frequencies hertz, temperatures kelvin and powers watts. JSON
 * projections preserve these IDL-capitalized field names exactly.
 */
export class TRH implements flatbuffers.IUnpackableObject<TRHT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TRH {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTRH(bb:flatbuffers.ByteBuffer, obj?:TRH):TRH {
  return (obj || new TRH()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTRH(bb:flatbuffers.ByteBuffer, obj?:TRH):TRH {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TRH()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$TRH');
}

ANTENNAS(index: number, obj?:TRHAntenna):TRHAntenna|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new TRHAntenna()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

antennasLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

TRANSMITTERS(index: number, obj?:TRHTransmitter):TRHTransmitter|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new TRHTransmitter()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

transmittersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

RECEIVERS(index: number, obj?:TRHReceiver):TRHReceiver|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new TRHReceiver()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

receiversLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

TRANSPONDERS(index: number, obj?:TRHTransponder):TRHTransponder|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new TRHTransponder()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

transpondersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

INSTALLATIONS(index: number, obj?:TRHInstallation):TRHInstallation|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new TRHInstallation()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

installationsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

TRACE_ID():string|null
TRACE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TRACE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

ATTESTATION(obj?:TRHAttestation):TRHAttestation|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new TRHAttestation()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startTRH(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addAntennas(builder:flatbuffers.Builder, ANTENNASOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, ANTENNASOffset, 0);
}

static createAntennasVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAntennasVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTransmitters(builder:flatbuffers.Builder, TRANSMITTERSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, TRANSMITTERSOffset, 0);
}

static createTransmittersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTransmittersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addReceivers(builder:flatbuffers.Builder, RECEIVERSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, RECEIVERSOffset, 0);
}

static createReceiversVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startReceiversVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTransponders(builder:flatbuffers.Builder, TRANSPONDERSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, TRANSPONDERSOffset, 0);
}

static createTranspondersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTranspondersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addInstallations(builder:flatbuffers.Builder, INSTALLATIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, INSTALLATIONSOffset, 0);
}

static createInstallationsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startInstallationsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, TRACE_IDOffset, 0);
}

static addAttestation(builder:flatbuffers.Builder, ATTESTATIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, ATTESTATIONOffset, 0);
}

static endTRH(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishTRHBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TRH');
}

static finishSizePrefixedTRHBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TRH', true);
}


unpack(): TRHT {
  return new TRHT(
    this.bb!.createObjList<TRHAntenna, TRHAntennaT>(this.ANTENNAS.bind(this), this.antennasLength()),
    this.bb!.createObjList<TRHTransmitter, TRHTransmitterT>(this.TRANSMITTERS.bind(this), this.transmittersLength()),
    this.bb!.createObjList<TRHReceiver, TRHReceiverT>(this.RECEIVERS.bind(this), this.receiversLength()),
    this.bb!.createObjList<TRHTransponder, TRHTransponderT>(this.TRANSPONDERS.bind(this), this.transpondersLength()),
    this.bb!.createObjList<TRHInstallation, TRHInstallationT>(this.INSTALLATIONS.bind(this), this.installationsLength()),
    this.TRACE_ID(),
    (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null)
  );
}


unpackTo(_o: TRHT): void {
  _o.ANTENNAS = this.bb!.createObjList<TRHAntenna, TRHAntennaT>(this.ANTENNAS.bind(this), this.antennasLength());
  _o.TRANSMITTERS = this.bb!.createObjList<TRHTransmitter, TRHTransmitterT>(this.TRANSMITTERS.bind(this), this.transmittersLength());
  _o.RECEIVERS = this.bb!.createObjList<TRHReceiver, TRHReceiverT>(this.RECEIVERS.bind(this), this.receiversLength());
  _o.TRANSPONDERS = this.bb!.createObjList<TRHTransponder, TRHTransponderT>(this.TRANSPONDERS.bind(this), this.transpondersLength());
  _o.INSTALLATIONS = this.bb!.createObjList<TRHInstallation, TRHInstallationT>(this.INSTALLATIONS.bind(this), this.installationsLength());
  _o.TRACE_ID = this.TRACE_ID();
  _o.ATTESTATION = (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null);
}
}

export class TRHT implements flatbuffers.IGeneratedObject {
constructor(
  public ANTENNAS: (TRHAntennaT)[] = [],
  public TRANSMITTERS: (TRHTransmitterT)[] = [],
  public RECEIVERS: (TRHReceiverT)[] = [],
  public TRANSPONDERS: (TRHTransponderT)[] = [],
  public INSTALLATIONS: (TRHInstallationT)[] = [],
  public TRACE_ID: string|Uint8Array|null = null,
  public ATTESTATION: TRHAttestationT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ANTENNAS = TRH.createAntennasVector(builder, builder.createObjectOffsetList(this.ANTENNAS));
  const TRANSMITTERS = TRH.createTransmittersVector(builder, builder.createObjectOffsetList(this.TRANSMITTERS));
  const RECEIVERS = TRH.createReceiversVector(builder, builder.createObjectOffsetList(this.RECEIVERS));
  const TRANSPONDERS = TRH.createTranspondersVector(builder, builder.createObjectOffsetList(this.TRANSPONDERS));
  const INSTALLATIONS = TRH.createInstallationsVector(builder, builder.createObjectOffsetList(this.INSTALLATIONS));
  const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID!) : 0);
  const ATTESTATION = (this.ATTESTATION !== null ? this.ATTESTATION!.pack(builder) : 0);

  TRH.startTRH(builder);
  TRH.addAntennas(builder, ANTENNAS);
  TRH.addTransmitters(builder, TRANSMITTERS);
  TRH.addReceivers(builder, RECEIVERS);
  TRH.addTransponders(builder, TRANSPONDERS);
  TRH.addInstallations(builder, INSTALLATIONS);
  TRH.addTraceId(builder, TRACE_ID);
  TRH.addAttestation(builder, ATTESTATION);

  return TRH.endTRH(builder);
}
}
