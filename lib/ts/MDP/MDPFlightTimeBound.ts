// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Time-of-flight bound between two encounters. Adjacent stages give per-leg
 * bounds; non-adjacent stages give cumulative bounds such as
 * "Earth to Mars to Earth within 3 years".
 */
export class MDPFlightTimeBound implements flatbuffers.IUnpackableObject<MDPFlightTimeBoundT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MDPFlightTimeBound {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMDPFlightTimeBound(bb:flatbuffers.ByteBuffer, obj?:MDPFlightTimeBound):MDPFlightTimeBound {
  return (obj || new MDPFlightTimeBound()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMDPFlightTimeBound(bb:flatbuffers.ByteBuffer, obj?:MDPFlightTimeBound):MDPFlightTimeBound {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MDPFlightTimeBound()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Stage indices the bound spans. TO_STAGE_INDEX > FROM_STAGE_INDEX.
 */
FROM_STAGE_INDEX():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

TO_STAGE_INDEX():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Inclusive bounds on elapsed time between those two encounters, days.
 */
MIN_DAYS():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MAX_DAYS():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startMDPFlightTimeBound(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addFromStageIndex(builder:flatbuffers.Builder, FROM_STAGE_INDEX:number) {
  builder.addFieldInt32(0, FROM_STAGE_INDEX, 0);
}

static addToStageIndex(builder:flatbuffers.Builder, TO_STAGE_INDEX:number) {
  builder.addFieldInt32(1, TO_STAGE_INDEX, 0);
}

static addMinDays(builder:flatbuffers.Builder, MIN_DAYS:number) {
  builder.addFieldFloat64(2, MIN_DAYS, 0.0);
}

static addMaxDays(builder:flatbuffers.Builder, MAX_DAYS:number) {
  builder.addFieldFloat64(3, MAX_DAYS, 0.0);
}

static endMDPFlightTimeBound(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMDPFlightTimeBound(builder:flatbuffers.Builder, FROM_STAGE_INDEX:number, TO_STAGE_INDEX:number, MIN_DAYS:number, MAX_DAYS:number):flatbuffers.Offset {
  MDPFlightTimeBound.startMDPFlightTimeBound(builder);
  MDPFlightTimeBound.addFromStageIndex(builder, FROM_STAGE_INDEX);
  MDPFlightTimeBound.addToStageIndex(builder, TO_STAGE_INDEX);
  MDPFlightTimeBound.addMinDays(builder, MIN_DAYS);
  MDPFlightTimeBound.addMaxDays(builder, MAX_DAYS);
  return MDPFlightTimeBound.endMDPFlightTimeBound(builder);
}

unpack(): MDPFlightTimeBoundT {
  return new MDPFlightTimeBoundT(
    this.FROM_STAGE_INDEX(),
    this.TO_STAGE_INDEX(),
    this.MIN_DAYS(),
    this.MAX_DAYS()
  );
}


unpackTo(_o: MDPFlightTimeBoundT): void {
  _o.FROM_STAGE_INDEX = this.FROM_STAGE_INDEX();
  _o.TO_STAGE_INDEX = this.TO_STAGE_INDEX();
  _o.MIN_DAYS = this.MIN_DAYS();
  _o.MAX_DAYS = this.MAX_DAYS();
}
}

export class MDPFlightTimeBoundT implements flatbuffers.IGeneratedObject {
constructor(
  public FROM_STAGE_INDEX: number = 0,
  public TO_STAGE_INDEX: number = 0,
  public MIN_DAYS: number = 0.0,
  public MAX_DAYS: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return MDPFlightTimeBound.createMDPFlightTimeBound(builder,
    this.FROM_STAGE_INDEX,
    this.TO_STAGE_INDEX,
    this.MIN_DAYS,
    this.MAX_DAYS
  );
}
}
