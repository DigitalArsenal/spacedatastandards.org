// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { MDPEncounterStage, MDPEncounterStageT } from './MDPEncounterStage.js';
import { MDPFlightTimeBound, MDPFlightTimeBoundT } from './MDPFlightTimeBound.js';
import { mdpDsmMode } from './mdpDsmMode.js';
import { mdpTransferDirection } from './mdpTransferDirection.js';


/**
 * Mission Design Problem — the definition of a patched-conic broad search over
 * an ordered sequence of body encounters.
 *
 * This is the PROBLEM, not a solution: it states what is being searched and
 * under what constraints. Candidate trajectories produced by solving it are
 * carried in $MDS. Nothing existing covers this: $LMS is a single Lambert
 * boundary-value problem with no body/epoch grid and no sequence, $MPE is an
 * Earth mean-element targeter, $MNF is an Earth manifold sweep, and $MNV is an
 * SSA-detected maneuver on a catalogued Earth object.
 */
export class MDP implements flatbuffers.IUnpackableObject<MDPT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MDP {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMDP(bb:flatbuffers.ByteBuffer, obj?:MDP):MDP {
  return (obj || new MDP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMDP(bb:flatbuffers.ByteBuffer, obj?:MDP):MDP {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MDP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$MDP');
}

/**
 * Stable identifier for this problem definition.
 */
PROBLEM_ID():string
PROBLEM_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
PROBLEM_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Human-readable name.
 */
NAME():string|null
NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Ordered encounter stages. Order in this vector IS the body sequence.
 */
ENCOUNTER_STAGES(index: number, obj?:MDPEncounterStage):MDPEncounterStage|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new MDPEncounterStage()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

encounterStagesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Per-leg and cumulative time-of-flight bounds.
 */
FLIGHT_TIME_BOUNDS(index: number, obj?:MDPFlightTimeBound):MDPFlightTimeBound|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new MDPFlightTimeBound()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

flightTimeBoundsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Cap on total mission duration, days.
 */
MAX_TOTAL_FLIGHT_TIME_DAYS():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Cap on launch characteristic energy C3, km^2/s^2.
 */
MAX_LAUNCH_C3_KM2_S2():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Cap on total post-launch delta-V, km/s.
 */
MAX_TOTAL_DELTA_V_KM_S():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Deep-space maneuver search controls.
 */
DSM_MODE():mdpDsmMode {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : mdpDsmMode.NONE;
}

/**
 * Cap on the magnitude of any single deep-space maneuver, km/s.
 */
MAX_DSM_DELTA_V_KM_S():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Grid step used when searching deep-space maneuver magnitude, km/s.
 */
DSM_GRID_STEP_KM_S():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Lambert controls: maximum number of complete revolutions considered on a
 * leg, and the admissible transfer direction.
 */
MAX_REVOLUTIONS():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

TRANSFER_DIRECTION():mdpTransferDirection {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : mdpTransferDirection.EITHER;
}

/**
 * Output decimation: width of the time bin within which only the best
 * candidate is retained. Zero means no decimation.
 */
OUTPUT_TIME_BIN_DAYS():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Optional spacecraft propulsion limits constraining admissible solutions.
 */
MAX_THRUST_N():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

SPECIFIC_IMPULSE_S():number {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

DRY_MASS_KG():number {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

INITIAL_MASS_KG():number {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Ephemeris source the search was posed against, e.g. a SPICE kernel set
 * identifier. Recorded so a solution set can be reproduced.
 */
EPHEMERIS_SOURCE():string|null
EPHEMERIS_SOURCE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EPHEMERIS_SOURCE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Free-form notes.
 */
COMMENT():string|null
COMMENT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
COMMENT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startMDP(builder:flatbuffers.Builder) {
  builder.startObject(19);
}

static addProblemId(builder:flatbuffers.Builder, PROBLEM_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, PROBLEM_IDOffset, 0);
}

static addName(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, NAMEOffset, 0);
}

static addEncounterStages(builder:flatbuffers.Builder, ENCOUNTER_STAGESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, ENCOUNTER_STAGESOffset, 0);
}

static createEncounterStagesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEncounterStagesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addFlightTimeBounds(builder:flatbuffers.Builder, FLIGHT_TIME_BOUNDSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, FLIGHT_TIME_BOUNDSOffset, 0);
}

static createFlightTimeBoundsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startFlightTimeBoundsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMaxTotalFlightTimeDays(builder:flatbuffers.Builder, MAX_TOTAL_FLIGHT_TIME_DAYS:number) {
  builder.addFieldFloat64(4, MAX_TOTAL_FLIGHT_TIME_DAYS, 0.0);
}

static addMaxLaunchC3Km2S2(builder:flatbuffers.Builder, MAX_LAUNCH_C3_KM2_S2:number) {
  builder.addFieldFloat64(5, MAX_LAUNCH_C3_KM2_S2, 0.0);
}

static addMaxTotalDeltaVKmS(builder:flatbuffers.Builder, MAX_TOTAL_DELTA_V_KM_S:number) {
  builder.addFieldFloat64(6, MAX_TOTAL_DELTA_V_KM_S, 0.0);
}

static addDsmMode(builder:flatbuffers.Builder, DSM_MODE:mdpDsmMode) {
  builder.addFieldInt8(7, DSM_MODE, mdpDsmMode.NONE);
}

static addMaxDsmDeltaVKmS(builder:flatbuffers.Builder, MAX_DSM_DELTA_V_KM_S:number) {
  builder.addFieldFloat64(8, MAX_DSM_DELTA_V_KM_S, 0.0);
}

static addDsmGridStepKmS(builder:flatbuffers.Builder, DSM_GRID_STEP_KM_S:number) {
  builder.addFieldFloat64(9, DSM_GRID_STEP_KM_S, 0.0);
}

static addMaxRevolutions(builder:flatbuffers.Builder, MAX_REVOLUTIONS:number) {
  builder.addFieldInt8(10, MAX_REVOLUTIONS, 0);
}

static addTransferDirection(builder:flatbuffers.Builder, TRANSFER_DIRECTION:mdpTransferDirection) {
  builder.addFieldInt8(11, TRANSFER_DIRECTION, mdpTransferDirection.EITHER);
}

static addOutputTimeBinDays(builder:flatbuffers.Builder, OUTPUT_TIME_BIN_DAYS:number) {
  builder.addFieldFloat64(12, OUTPUT_TIME_BIN_DAYS, 0.0);
}

static addMaxThrustN(builder:flatbuffers.Builder, MAX_THRUST_N:number) {
  builder.addFieldFloat64(13, MAX_THRUST_N, 0.0);
}

static addSpecificImpulseS(builder:flatbuffers.Builder, SPECIFIC_IMPULSE_S:number) {
  builder.addFieldFloat64(14, SPECIFIC_IMPULSE_S, 0.0);
}

static addDryMassKg(builder:flatbuffers.Builder, DRY_MASS_KG:number) {
  builder.addFieldFloat64(15, DRY_MASS_KG, 0.0);
}

static addInitialMassKg(builder:flatbuffers.Builder, INITIAL_MASS_KG:number) {
  builder.addFieldFloat64(16, INITIAL_MASS_KG, 0.0);
}

static addEphemerisSource(builder:flatbuffers.Builder, EPHEMERIS_SOURCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(17, EPHEMERIS_SOURCEOffset, 0);
}

static addComment(builder:flatbuffers.Builder, COMMENTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(18, COMMENTOffset, 0);
}

static endMDP(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // PROBLEM_ID
  return offset;
}

static finishMDPBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MDP');
}

static finishSizePrefixedMDPBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MDP', true);
}

static createMDP(builder:flatbuffers.Builder, PROBLEM_IDOffset:flatbuffers.Offset, NAMEOffset:flatbuffers.Offset, ENCOUNTER_STAGESOffset:flatbuffers.Offset, FLIGHT_TIME_BOUNDSOffset:flatbuffers.Offset, MAX_TOTAL_FLIGHT_TIME_DAYS:number, MAX_LAUNCH_C3_KM2_S2:number, MAX_TOTAL_DELTA_V_KM_S:number, DSM_MODE:mdpDsmMode, MAX_DSM_DELTA_V_KM_S:number, DSM_GRID_STEP_KM_S:number, MAX_REVOLUTIONS:number, TRANSFER_DIRECTION:mdpTransferDirection, OUTPUT_TIME_BIN_DAYS:number, MAX_THRUST_N:number, SPECIFIC_IMPULSE_S:number, DRY_MASS_KG:number, INITIAL_MASS_KG:number, EPHEMERIS_SOURCEOffset:flatbuffers.Offset, COMMENTOffset:flatbuffers.Offset):flatbuffers.Offset {
  MDP.startMDP(builder);
  MDP.addProblemId(builder, PROBLEM_IDOffset);
  MDP.addName(builder, NAMEOffset);
  MDP.addEncounterStages(builder, ENCOUNTER_STAGESOffset);
  MDP.addFlightTimeBounds(builder, FLIGHT_TIME_BOUNDSOffset);
  MDP.addMaxTotalFlightTimeDays(builder, MAX_TOTAL_FLIGHT_TIME_DAYS);
  MDP.addMaxLaunchC3Km2S2(builder, MAX_LAUNCH_C3_KM2_S2);
  MDP.addMaxTotalDeltaVKmS(builder, MAX_TOTAL_DELTA_V_KM_S);
  MDP.addDsmMode(builder, DSM_MODE);
  MDP.addMaxDsmDeltaVKmS(builder, MAX_DSM_DELTA_V_KM_S);
  MDP.addDsmGridStepKmS(builder, DSM_GRID_STEP_KM_S);
  MDP.addMaxRevolutions(builder, MAX_REVOLUTIONS);
  MDP.addTransferDirection(builder, TRANSFER_DIRECTION);
  MDP.addOutputTimeBinDays(builder, OUTPUT_TIME_BIN_DAYS);
  MDP.addMaxThrustN(builder, MAX_THRUST_N);
  MDP.addSpecificImpulseS(builder, SPECIFIC_IMPULSE_S);
  MDP.addDryMassKg(builder, DRY_MASS_KG);
  MDP.addInitialMassKg(builder, INITIAL_MASS_KG);
  MDP.addEphemerisSource(builder, EPHEMERIS_SOURCEOffset);
  MDP.addComment(builder, COMMENTOffset);
  return MDP.endMDP(builder);
}

unpack(): MDPT {
  return new MDPT(
    this.PROBLEM_ID(),
    this.NAME(),
    this.bb!.createObjList<MDPEncounterStage, MDPEncounterStageT>(this.ENCOUNTER_STAGES.bind(this), this.encounterStagesLength()),
    this.bb!.createObjList<MDPFlightTimeBound, MDPFlightTimeBoundT>(this.FLIGHT_TIME_BOUNDS.bind(this), this.flightTimeBoundsLength()),
    this.MAX_TOTAL_FLIGHT_TIME_DAYS(),
    this.MAX_LAUNCH_C3_KM2_S2(),
    this.MAX_TOTAL_DELTA_V_KM_S(),
    this.DSM_MODE(),
    this.MAX_DSM_DELTA_V_KM_S(),
    this.DSM_GRID_STEP_KM_S(),
    this.MAX_REVOLUTIONS(),
    this.TRANSFER_DIRECTION(),
    this.OUTPUT_TIME_BIN_DAYS(),
    this.MAX_THRUST_N(),
    this.SPECIFIC_IMPULSE_S(),
    this.DRY_MASS_KG(),
    this.INITIAL_MASS_KG(),
    this.EPHEMERIS_SOURCE(),
    this.COMMENT()
  );
}


unpackTo(_o: MDPT): void {
  _o.PROBLEM_ID = this.PROBLEM_ID();
  _o.NAME = this.NAME();
  _o.ENCOUNTER_STAGES = this.bb!.createObjList<MDPEncounterStage, MDPEncounterStageT>(this.ENCOUNTER_STAGES.bind(this), this.encounterStagesLength());
  _o.FLIGHT_TIME_BOUNDS = this.bb!.createObjList<MDPFlightTimeBound, MDPFlightTimeBoundT>(this.FLIGHT_TIME_BOUNDS.bind(this), this.flightTimeBoundsLength());
  _o.MAX_TOTAL_FLIGHT_TIME_DAYS = this.MAX_TOTAL_FLIGHT_TIME_DAYS();
  _o.MAX_LAUNCH_C3_KM2_S2 = this.MAX_LAUNCH_C3_KM2_S2();
  _o.MAX_TOTAL_DELTA_V_KM_S = this.MAX_TOTAL_DELTA_V_KM_S();
  _o.DSM_MODE = this.DSM_MODE();
  _o.MAX_DSM_DELTA_V_KM_S = this.MAX_DSM_DELTA_V_KM_S();
  _o.DSM_GRID_STEP_KM_S = this.DSM_GRID_STEP_KM_S();
  _o.MAX_REVOLUTIONS = this.MAX_REVOLUTIONS();
  _o.TRANSFER_DIRECTION = this.TRANSFER_DIRECTION();
  _o.OUTPUT_TIME_BIN_DAYS = this.OUTPUT_TIME_BIN_DAYS();
  _o.MAX_THRUST_N = this.MAX_THRUST_N();
  _o.SPECIFIC_IMPULSE_S = this.SPECIFIC_IMPULSE_S();
  _o.DRY_MASS_KG = this.DRY_MASS_KG();
  _o.INITIAL_MASS_KG = this.INITIAL_MASS_KG();
  _o.EPHEMERIS_SOURCE = this.EPHEMERIS_SOURCE();
  _o.COMMENT = this.COMMENT();
}
}

export class MDPT implements flatbuffers.IGeneratedObject {
constructor(
  public PROBLEM_ID: string|Uint8Array|null = null,
  public NAME: string|Uint8Array|null = null,
  public ENCOUNTER_STAGES: (MDPEncounterStageT)[] = [],
  public FLIGHT_TIME_BOUNDS: (MDPFlightTimeBoundT)[] = [],
  public MAX_TOTAL_FLIGHT_TIME_DAYS: number = 0.0,
  public MAX_LAUNCH_C3_KM2_S2: number = 0.0,
  public MAX_TOTAL_DELTA_V_KM_S: number = 0.0,
  public DSM_MODE: mdpDsmMode = mdpDsmMode.NONE,
  public MAX_DSM_DELTA_V_KM_S: number = 0.0,
  public DSM_GRID_STEP_KM_S: number = 0.0,
  public MAX_REVOLUTIONS: number = 0,
  public TRANSFER_DIRECTION: mdpTransferDirection = mdpTransferDirection.EITHER,
  public OUTPUT_TIME_BIN_DAYS: number = 0.0,
  public MAX_THRUST_N: number = 0.0,
  public SPECIFIC_IMPULSE_S: number = 0.0,
  public DRY_MASS_KG: number = 0.0,
  public INITIAL_MASS_KG: number = 0.0,
  public EPHEMERIS_SOURCE: string|Uint8Array|null = null,
  public COMMENT: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PROBLEM_ID = (this.PROBLEM_ID !== null ? builder.createString(this.PROBLEM_ID!) : 0);
  const NAME = (this.NAME !== null ? builder.createString(this.NAME!) : 0);
  const ENCOUNTER_STAGES = MDP.createEncounterStagesVector(builder, builder.createObjectOffsetList(this.ENCOUNTER_STAGES));
  const FLIGHT_TIME_BOUNDS = MDP.createFlightTimeBoundsVector(builder, builder.createObjectOffsetList(this.FLIGHT_TIME_BOUNDS));
  const EPHEMERIS_SOURCE = (this.EPHEMERIS_SOURCE !== null ? builder.createString(this.EPHEMERIS_SOURCE!) : 0);
  const COMMENT = (this.COMMENT !== null ? builder.createString(this.COMMENT!) : 0);

  return MDP.createMDP(builder,
    PROBLEM_ID,
    NAME,
    ENCOUNTER_STAGES,
    FLIGHT_TIME_BOUNDS,
    this.MAX_TOTAL_FLIGHT_TIME_DAYS,
    this.MAX_LAUNCH_C3_KM2_S2,
    this.MAX_TOTAL_DELTA_V_KM_S,
    this.DSM_MODE,
    this.MAX_DSM_DELTA_V_KM_S,
    this.DSM_GRID_STEP_KM_S,
    this.MAX_REVOLUTIONS,
    this.TRANSFER_DIRECTION,
    this.OUTPUT_TIME_BIN_DAYS,
    this.MAX_THRUST_N,
    this.SPECIFIC_IMPULSE_S,
    this.DRY_MASS_KG,
    this.INITIAL_MASS_KG,
    EPHEMERIS_SOURCE,
    COMMENT
  );
}
}
