// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One chunk that was read and durably committed.
 */
export class IRMChunk implements flatbuffers.IUnpackableObject<IRMChunkT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IRMChunk {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIRMChunk(bb:flatbuffers.ByteBuffer, obj?:IRMChunk):IRMChunk {
  return (obj || new IRMChunk()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIRMChunk(bb:flatbuffers.ByteBuffer, obj?:IRMChunk):IRMChunk {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IRMChunk()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Zero-based position of the chunk in the job's read sequence.
 */
CHUNK_INDEX():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Publisher-stable batch identifier the chunk's records were stored under,
 * verbatim, so a consumer can join the mark to the stored rows.
 */
BATCH_ID():string|null
BATCH_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BATCH_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * First byte of the range that was read.
 */
RANGE_FIRST_BYTE():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Last byte of the range that was read. Interpreted per RANGE_MODE:
 * inclusive under INCLUSIVE_BYTE_RANGE, exclusive under OFFSET_LENGTH.
 */
RANGE_LAST_BYTE():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Bytes the read actually returned, which may be shorter than the range
 * requested.
 */
BYTE_LENGTH():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * SHA-256 of the chunk's bytes as 64 lowercase hexadecimal characters.
 */
CHUNK_SHA256():string|null
CHUNK_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CHUNK_SHA256(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Records the decoder produced from the chunk.
 */
RECORDS_DECODED():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Records the storage lane reported as durably committed. A chunk whose
 * stored count is 0 is not marked at all.
 */
RECORDS_STORED():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Records the storage lane rejected or collapsed as duplicates.
 */
RECORDS_REJECTED():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Number of read attempts this chunk cost, including the successful one.
 */
ATTEMPTS():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp at which the storage lane
 * confirmed the commit.
 */
COMMITTED_AT():string|null
COMMITTED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
COMMITTED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startIRMChunk(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addChunkIndex(builder:flatbuffers.Builder, CHUNK_INDEX:number) {
  builder.addFieldInt32(0, CHUNK_INDEX, 0);
}

static addBatchId(builder:flatbuffers.Builder, BATCH_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, BATCH_IDOffset, 0);
}

static addRangeFirstByte(builder:flatbuffers.Builder, RANGE_FIRST_BYTE:bigint) {
  builder.addFieldInt64(2, RANGE_FIRST_BYTE, BigInt('0'));
}

static addRangeLastByte(builder:flatbuffers.Builder, RANGE_LAST_BYTE:bigint) {
  builder.addFieldInt64(3, RANGE_LAST_BYTE, BigInt('0'));
}

static addByteLength(builder:flatbuffers.Builder, BYTE_LENGTH:bigint) {
  builder.addFieldInt64(4, BYTE_LENGTH, BigInt('0'));
}

static addChunkSha256(builder:flatbuffers.Builder, CHUNK_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(5, CHUNK_SHA256Offset, 0);
}

static addRecordsDecoded(builder:flatbuffers.Builder, RECORDS_DECODED:number) {
  builder.addFieldInt32(6, RECORDS_DECODED, 0);
}

static addRecordsStored(builder:flatbuffers.Builder, RECORDS_STORED:number) {
  builder.addFieldInt32(7, RECORDS_STORED, 0);
}

static addRecordsRejected(builder:flatbuffers.Builder, RECORDS_REJECTED:number) {
  builder.addFieldInt32(8, RECORDS_REJECTED, 0);
}

static addAttempts(builder:flatbuffers.Builder, ATTEMPTS:number) {
  builder.addFieldInt32(9, ATTEMPTS, 0);
}

static addCommittedAt(builder:flatbuffers.Builder, COMMITTED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, COMMITTED_ATOffset, 0);
}

static endIRMChunk(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIRMChunk(builder:flatbuffers.Builder, CHUNK_INDEX:number, BATCH_IDOffset:flatbuffers.Offset, RANGE_FIRST_BYTE:bigint, RANGE_LAST_BYTE:bigint, BYTE_LENGTH:bigint, CHUNK_SHA256Offset:flatbuffers.Offset, RECORDS_DECODED:number, RECORDS_STORED:number, RECORDS_REJECTED:number, ATTEMPTS:number, COMMITTED_ATOffset:flatbuffers.Offset):flatbuffers.Offset {
  IRMChunk.startIRMChunk(builder);
  IRMChunk.addChunkIndex(builder, CHUNK_INDEX);
  IRMChunk.addBatchId(builder, BATCH_IDOffset);
  IRMChunk.addRangeFirstByte(builder, RANGE_FIRST_BYTE);
  IRMChunk.addRangeLastByte(builder, RANGE_LAST_BYTE);
  IRMChunk.addByteLength(builder, BYTE_LENGTH);
  IRMChunk.addChunkSha256(builder, CHUNK_SHA256Offset);
  IRMChunk.addRecordsDecoded(builder, RECORDS_DECODED);
  IRMChunk.addRecordsStored(builder, RECORDS_STORED);
  IRMChunk.addRecordsRejected(builder, RECORDS_REJECTED);
  IRMChunk.addAttempts(builder, ATTEMPTS);
  IRMChunk.addCommittedAt(builder, COMMITTED_ATOffset);
  return IRMChunk.endIRMChunk(builder);
}

unpack(): IRMChunkT {
  return new IRMChunkT(
    this.CHUNK_INDEX(),
    this.BATCH_ID(),
    this.RANGE_FIRST_BYTE(),
    this.RANGE_LAST_BYTE(),
    this.BYTE_LENGTH(),
    this.CHUNK_SHA256(),
    this.RECORDS_DECODED(),
    this.RECORDS_STORED(),
    this.RECORDS_REJECTED(),
    this.ATTEMPTS(),
    this.COMMITTED_AT()
  );
}


unpackTo(_o: IRMChunkT): void {
  _o.CHUNK_INDEX = this.CHUNK_INDEX();
  _o.BATCH_ID = this.BATCH_ID();
  _o.RANGE_FIRST_BYTE = this.RANGE_FIRST_BYTE();
  _o.RANGE_LAST_BYTE = this.RANGE_LAST_BYTE();
  _o.BYTE_LENGTH = this.BYTE_LENGTH();
  _o.CHUNK_SHA256 = this.CHUNK_SHA256();
  _o.RECORDS_DECODED = this.RECORDS_DECODED();
  _o.RECORDS_STORED = this.RECORDS_STORED();
  _o.RECORDS_REJECTED = this.RECORDS_REJECTED();
  _o.ATTEMPTS = this.ATTEMPTS();
  _o.COMMITTED_AT = this.COMMITTED_AT();
}
}

export class IRMChunkT implements flatbuffers.IGeneratedObject {
constructor(
  public CHUNK_INDEX: number = 0,
  public BATCH_ID: string|Uint8Array|null = null,
  public RANGE_FIRST_BYTE: bigint = BigInt('0'),
  public RANGE_LAST_BYTE: bigint = BigInt('0'),
  public BYTE_LENGTH: bigint = BigInt('0'),
  public CHUNK_SHA256: string|Uint8Array|null = null,
  public RECORDS_DECODED: number = 0,
  public RECORDS_STORED: number = 0,
  public RECORDS_REJECTED: number = 0,
  public ATTEMPTS: number = 0,
  public COMMITTED_AT: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const BATCH_ID = (this.BATCH_ID !== null ? builder.createString(this.BATCH_ID!) : 0);
  const CHUNK_SHA256 = (this.CHUNK_SHA256 !== null ? builder.createString(this.CHUNK_SHA256!) : 0);
  const COMMITTED_AT = (this.COMMITTED_AT !== null ? builder.createString(this.COMMITTED_AT!) : 0);

  return IRMChunk.createIRMChunk(builder,
    this.CHUNK_INDEX,
    BATCH_ID,
    this.RANGE_FIRST_BYTE,
    this.RANGE_LAST_BYTE,
    this.BYTE_LENGTH,
    CHUNK_SHA256,
    this.RECORDS_DECODED,
    this.RECORDS_STORED,
    this.RECORDS_REJECTED,
    this.ATTEMPTS,
    COMMITTED_AT
  );
}
}
