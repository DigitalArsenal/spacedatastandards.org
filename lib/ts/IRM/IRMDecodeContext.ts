// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Everything a resuming reader needs to decode a chunk that does not begin at
 * offset 0. Required whenever NEXT_OFFSET is past the source's own header.
 */
export class IRMDecodeContext implements flatbuffers.IUnpackableObject<IRMDecodeContextT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IRMDecodeContext {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIRMDecodeContext(bb:flatbuffers.ByteBuffer, obj?:IRMDecodeContext):IRMDecodeContext {
  return (obj || new IRMDecodeContext()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIRMDecodeContext(bb:flatbuffers.ByteBuffer, obj?:IRMDecodeContext):IRMDecodeContext {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IRMDecodeContext()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Format token the decoder dispatches on, verbatim and lowercase, e.g.
 * `text/csv`. A consumer that does not implement the format refuses the chunk
 * rather than guessing at a layout.
 */
FORMAT():string
FORMAT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
FORMAT(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * The source's header line verbatim, exactly as it appeared at offset 0 and
 * excluding its line terminator. This is the field that makes chunk N>0
 * decodable; without it a headerless chunk parses to zero rows and looks like
 * a clean tail.
 */
HEADER_LINE():string|null
HEADER_LINE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
HEADER_LINE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Byte length the header occupied at the start of the source, including its
 * terminator, so a reader can tell a header-bearing prefix from a data row.
 */
HEADER_BYTE_LENGTH():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Column names in source order, when the publisher parsed them. Advisory:
 * HEADER_LINE is authoritative and a disagreement is resolved in its favour.
 */
COLUMN_NAMES(index: number):string
COLUMN_NAMES(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
COLUMN_NAMES(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

columnNamesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Field delimiter verbatim.
 */
DELIMITER():string|null
DELIMITER(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DELIMITER(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Quote character verbatim.
 */
QUOTE():string|null
QUOTE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
QUOTE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Escape character verbatim.
 */
ESCAPE():string|null
ESCAPE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ESCAPE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Record terminator verbatim, e.g. `\n` or `\r\n`.
 */
LINE_TERMINATOR():string|null
LINE_TERMINATOR(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
LINE_TERMINATOR(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Character encoding label verbatim as the provider states it.
 */
CHARSET():string|null
CHARSET(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CHARSET(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Stream-compression codec applied to the source bytes, verbatim and
 * lowercase. Empty means the ranged bytes are read as-is. Non-empty means
 * NEXT_OFFSET is a position in the COMPRESSED stream and DECODER_STATE,
 * WINDOW and PARTIAL_RECORD are all required to resume.
 */
COMPRESSION():string|null
COMPRESSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
COMPRESSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Position in the DECODED stream corresponding to NEXT_OFFSET, when the
 * source is compressed. 0 with a non-empty COMPRESSION means the decoded
 * stream has not been advanced.
 */
DECODED_OFFSET():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Serialized, pointer-free image of the decoder at the seam — the state a
 * codec with no restart point needs in order to have one. Opaque to every
 * consumer but a decoder whose stamps match exactly. Never partially applied.
 */
DECODER_STATE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

decoderStateLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

decoderStateArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Layout identifier of DECODER_STATE, verbatim, naming the decoder struct the
 * image was taken from. A consumer that does not know the token restarts.
 */
DECODER_STATE_FORMAT():string|null
DECODER_STATE_FORMAT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DECODER_STATE_FORMAT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Version of that layout, verbatim. Any disagreement restarts the job.
 */
DECODER_STATE_VERSION():string|null
DECODER_STATE_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DECODER_STATE_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Byte length DECODER_STATE is expected to have. Carried even though the
 * vector knows its own size, so a size disagreement is detectable BEFORE the
 * image is loaded into a differently shaped struct.
 */
DECODER_STATE_BYTE_LENGTH():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * SHA-256 of DECODER_STATE as 64 lowercase hexadecimal characters.
 */
DECODER_STATE_SHA256():string|null
DECODER_STATE_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DECODER_STATE_SHA256(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Build identifier of the decoder that produced DECODER_STATE, verbatim, so
 * two builds of one codec version are still distinguishable.
 */
DECODER_BUILD_ID():string|null
DECODER_BUILD_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DECODER_BUILD_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Media type of DECODER_STATE, so an unrelated decoder refuses it instead of
 * misreading it.
 */
DECODER_STATE_MEDIA_TYPE():string|null
DECODER_STATE_MEDIA_TYPE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DECODER_STATE_MEDIA_TYPE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The decoder's history window at the seam — the bytes that ARE the codec's
 * back-reference dictionary. Carried separately from DECODER_STATE because it
 * is bulk data of a size the codec fixes, not part of the struct image.
 */
WINDOW(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

windowLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

windowArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Byte length WINDOW is expected to have, for the same detect-before-load
 * reason as DECODER_STATE_BYTE_LENGTH.
 */
WINDOW_BYTE_LENGTH():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Offset into WINDOW at which the codec will next write, when the window is
 * a circular buffer whose contents cannot be interpreted without it.
 */
WINDOW_POSITION():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 44);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * The bytes of the record that straddles the seam: everything the previous
 * chunk decoded of a record it could not finish. A resuming decoder prepends
 * these to the next chunk's decoded output before parsing. Carried as bytes,
 * not text, because a seam may fall inside a multi-byte character.
 */
PARTIAL_RECORD(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 46);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

partialRecordLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 46);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

partialRecordArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 46);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startIRMDecodeContext(builder:flatbuffers.Builder) {
  builder.startObject(22);
}

static addFormat(builder:flatbuffers.Builder, FORMATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, FORMATOffset, 0);
}

static addHeaderLine(builder:flatbuffers.Builder, HEADER_LINEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, HEADER_LINEOffset, 0);
}

static addHeaderByteLength(builder:flatbuffers.Builder, HEADER_BYTE_LENGTH:bigint) {
  builder.addFieldInt64(2, HEADER_BYTE_LENGTH, BigInt('0'));
}

static addColumnNames(builder:flatbuffers.Builder, COLUMN_NAMESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, COLUMN_NAMESOffset, 0);
}

static createColumnNamesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startColumnNamesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addDelimiter(builder:flatbuffers.Builder, DELIMITEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, DELIMITEROffset, 0);
}

static addQuote(builder:flatbuffers.Builder, QUOTEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, QUOTEOffset, 0);
}

static addEscape(builder:flatbuffers.Builder, ESCAPEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, ESCAPEOffset, 0);
}

static addLineTerminator(builder:flatbuffers.Builder, LINE_TERMINATOROffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, LINE_TERMINATOROffset, 0);
}

static addCharset(builder:flatbuffers.Builder, CHARSETOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, CHARSETOffset, 0);
}

static addCompression(builder:flatbuffers.Builder, COMPRESSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, COMPRESSIONOffset, 0);
}

static addDecodedOffset(builder:flatbuffers.Builder, DECODED_OFFSET:bigint) {
  builder.addFieldInt64(10, DECODED_OFFSET, BigInt('0'));
}

static addDecoderState(builder:flatbuffers.Builder, DECODER_STATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, DECODER_STATEOffset, 0);
}

static createDecoderStateVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startDecoderStateVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addDecoderStateFormat(builder:flatbuffers.Builder, DECODER_STATE_FORMATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, DECODER_STATE_FORMATOffset, 0);
}

static addDecoderStateVersion(builder:flatbuffers.Builder, DECODER_STATE_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, DECODER_STATE_VERSIONOffset, 0);
}

static addDecoderStateByteLength(builder:flatbuffers.Builder, DECODER_STATE_BYTE_LENGTH:bigint) {
  builder.addFieldInt64(14, DECODER_STATE_BYTE_LENGTH, BigInt('0'));
}

static addDecoderStateSha256(builder:flatbuffers.Builder, DECODER_STATE_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(15, DECODER_STATE_SHA256Offset, 0);
}

static addDecoderBuildId(builder:flatbuffers.Builder, DECODER_BUILD_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(16, DECODER_BUILD_IDOffset, 0);
}

static addDecoderStateMediaType(builder:flatbuffers.Builder, DECODER_STATE_MEDIA_TYPEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(17, DECODER_STATE_MEDIA_TYPEOffset, 0);
}

static addWindow(builder:flatbuffers.Builder, WINDOWOffset:flatbuffers.Offset) {
  builder.addFieldOffset(18, WINDOWOffset, 0);
}

static createWindowVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startWindowVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addWindowByteLength(builder:flatbuffers.Builder, WINDOW_BYTE_LENGTH:bigint) {
  builder.addFieldInt64(19, WINDOW_BYTE_LENGTH, BigInt('0'));
}

static addWindowPosition(builder:flatbuffers.Builder, WINDOW_POSITION:bigint) {
  builder.addFieldInt64(20, WINDOW_POSITION, BigInt('0'));
}

static addPartialRecord(builder:flatbuffers.Builder, PARTIAL_RECORDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(21, PARTIAL_RECORDOffset, 0);
}

static createPartialRecordVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPartialRecordVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endIRMDecodeContext(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // FORMAT
  return offset;
}

static createIRMDecodeContext(builder:flatbuffers.Builder, FORMATOffset:flatbuffers.Offset, HEADER_LINEOffset:flatbuffers.Offset, HEADER_BYTE_LENGTH:bigint, COLUMN_NAMESOffset:flatbuffers.Offset, DELIMITEROffset:flatbuffers.Offset, QUOTEOffset:flatbuffers.Offset, ESCAPEOffset:flatbuffers.Offset, LINE_TERMINATOROffset:flatbuffers.Offset, CHARSETOffset:flatbuffers.Offset, COMPRESSIONOffset:flatbuffers.Offset, DECODED_OFFSET:bigint, DECODER_STATEOffset:flatbuffers.Offset, DECODER_STATE_FORMATOffset:flatbuffers.Offset, DECODER_STATE_VERSIONOffset:flatbuffers.Offset, DECODER_STATE_BYTE_LENGTH:bigint, DECODER_STATE_SHA256Offset:flatbuffers.Offset, DECODER_BUILD_IDOffset:flatbuffers.Offset, DECODER_STATE_MEDIA_TYPEOffset:flatbuffers.Offset, WINDOWOffset:flatbuffers.Offset, WINDOW_BYTE_LENGTH:bigint, WINDOW_POSITION:bigint, PARTIAL_RECORDOffset:flatbuffers.Offset):flatbuffers.Offset {
  IRMDecodeContext.startIRMDecodeContext(builder);
  IRMDecodeContext.addFormat(builder, FORMATOffset);
  IRMDecodeContext.addHeaderLine(builder, HEADER_LINEOffset);
  IRMDecodeContext.addHeaderByteLength(builder, HEADER_BYTE_LENGTH);
  IRMDecodeContext.addColumnNames(builder, COLUMN_NAMESOffset);
  IRMDecodeContext.addDelimiter(builder, DELIMITEROffset);
  IRMDecodeContext.addQuote(builder, QUOTEOffset);
  IRMDecodeContext.addEscape(builder, ESCAPEOffset);
  IRMDecodeContext.addLineTerminator(builder, LINE_TERMINATOROffset);
  IRMDecodeContext.addCharset(builder, CHARSETOffset);
  IRMDecodeContext.addCompression(builder, COMPRESSIONOffset);
  IRMDecodeContext.addDecodedOffset(builder, DECODED_OFFSET);
  IRMDecodeContext.addDecoderState(builder, DECODER_STATEOffset);
  IRMDecodeContext.addDecoderStateFormat(builder, DECODER_STATE_FORMATOffset);
  IRMDecodeContext.addDecoderStateVersion(builder, DECODER_STATE_VERSIONOffset);
  IRMDecodeContext.addDecoderStateByteLength(builder, DECODER_STATE_BYTE_LENGTH);
  IRMDecodeContext.addDecoderStateSha256(builder, DECODER_STATE_SHA256Offset);
  IRMDecodeContext.addDecoderBuildId(builder, DECODER_BUILD_IDOffset);
  IRMDecodeContext.addDecoderStateMediaType(builder, DECODER_STATE_MEDIA_TYPEOffset);
  IRMDecodeContext.addWindow(builder, WINDOWOffset);
  IRMDecodeContext.addWindowByteLength(builder, WINDOW_BYTE_LENGTH);
  IRMDecodeContext.addWindowPosition(builder, WINDOW_POSITION);
  IRMDecodeContext.addPartialRecord(builder, PARTIAL_RECORDOffset);
  return IRMDecodeContext.endIRMDecodeContext(builder);
}

unpack(): IRMDecodeContextT {
  return new IRMDecodeContextT(
    this.FORMAT(),
    this.HEADER_LINE(),
    this.HEADER_BYTE_LENGTH(),
    this.bb!.createScalarList<string>(this.COLUMN_NAMES.bind(this), this.columnNamesLength()),
    this.DELIMITER(),
    this.QUOTE(),
    this.ESCAPE(),
    this.LINE_TERMINATOR(),
    this.CHARSET(),
    this.COMPRESSION(),
    this.DECODED_OFFSET(),
    this.bb!.createScalarList<number>(this.DECODER_STATE.bind(this), this.decoderStateLength()),
    this.DECODER_STATE_FORMAT(),
    this.DECODER_STATE_VERSION(),
    this.DECODER_STATE_BYTE_LENGTH(),
    this.DECODER_STATE_SHA256(),
    this.DECODER_BUILD_ID(),
    this.DECODER_STATE_MEDIA_TYPE(),
    this.bb!.createScalarList<number>(this.WINDOW.bind(this), this.windowLength()),
    this.WINDOW_BYTE_LENGTH(),
    this.WINDOW_POSITION(),
    this.bb!.createScalarList<number>(this.PARTIAL_RECORD.bind(this), this.partialRecordLength())
  );
}


unpackTo(_o: IRMDecodeContextT): void {
  _o.FORMAT = this.FORMAT();
  _o.HEADER_LINE = this.HEADER_LINE();
  _o.HEADER_BYTE_LENGTH = this.HEADER_BYTE_LENGTH();
  _o.COLUMN_NAMES = this.bb!.createScalarList<string>(this.COLUMN_NAMES.bind(this), this.columnNamesLength());
  _o.DELIMITER = this.DELIMITER();
  _o.QUOTE = this.QUOTE();
  _o.ESCAPE = this.ESCAPE();
  _o.LINE_TERMINATOR = this.LINE_TERMINATOR();
  _o.CHARSET = this.CHARSET();
  _o.COMPRESSION = this.COMPRESSION();
  _o.DECODED_OFFSET = this.DECODED_OFFSET();
  _o.DECODER_STATE = this.bb!.createScalarList<number>(this.DECODER_STATE.bind(this), this.decoderStateLength());
  _o.DECODER_STATE_FORMAT = this.DECODER_STATE_FORMAT();
  _o.DECODER_STATE_VERSION = this.DECODER_STATE_VERSION();
  _o.DECODER_STATE_BYTE_LENGTH = this.DECODER_STATE_BYTE_LENGTH();
  _o.DECODER_STATE_SHA256 = this.DECODER_STATE_SHA256();
  _o.DECODER_BUILD_ID = this.DECODER_BUILD_ID();
  _o.DECODER_STATE_MEDIA_TYPE = this.DECODER_STATE_MEDIA_TYPE();
  _o.WINDOW = this.bb!.createScalarList<number>(this.WINDOW.bind(this), this.windowLength());
  _o.WINDOW_BYTE_LENGTH = this.WINDOW_BYTE_LENGTH();
  _o.WINDOW_POSITION = this.WINDOW_POSITION();
  _o.PARTIAL_RECORD = this.bb!.createScalarList<number>(this.PARTIAL_RECORD.bind(this), this.partialRecordLength());
}
}

export class IRMDecodeContextT implements flatbuffers.IGeneratedObject {
constructor(
  public FORMAT: string|Uint8Array|null = null,
  public HEADER_LINE: string|Uint8Array|null = null,
  public HEADER_BYTE_LENGTH: bigint = BigInt('0'),
  public COLUMN_NAMES: (string)[] = [],
  public DELIMITER: string|Uint8Array|null = null,
  public QUOTE: string|Uint8Array|null = null,
  public ESCAPE: string|Uint8Array|null = null,
  public LINE_TERMINATOR: string|Uint8Array|null = null,
  public CHARSET: string|Uint8Array|null = null,
  public COMPRESSION: string|Uint8Array|null = null,
  public DECODED_OFFSET: bigint = BigInt('0'),
  public DECODER_STATE: (number)[] = [],
  public DECODER_STATE_FORMAT: string|Uint8Array|null = null,
  public DECODER_STATE_VERSION: string|Uint8Array|null = null,
  public DECODER_STATE_BYTE_LENGTH: bigint = BigInt('0'),
  public DECODER_STATE_SHA256: string|Uint8Array|null = null,
  public DECODER_BUILD_ID: string|Uint8Array|null = null,
  public DECODER_STATE_MEDIA_TYPE: string|Uint8Array|null = null,
  public WINDOW: (number)[] = [],
  public WINDOW_BYTE_LENGTH: bigint = BigInt('0'),
  public WINDOW_POSITION: bigint = BigInt('0'),
  public PARTIAL_RECORD: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const FORMAT = (this.FORMAT !== null ? builder.createString(this.FORMAT!) : 0);
  const HEADER_LINE = (this.HEADER_LINE !== null ? builder.createString(this.HEADER_LINE!) : 0);
  const COLUMN_NAMES = IRMDecodeContext.createColumnNamesVector(builder, builder.createObjectOffsetList(this.COLUMN_NAMES));
  const DELIMITER = (this.DELIMITER !== null ? builder.createString(this.DELIMITER!) : 0);
  const QUOTE = (this.QUOTE !== null ? builder.createString(this.QUOTE!) : 0);
  const ESCAPE = (this.ESCAPE !== null ? builder.createString(this.ESCAPE!) : 0);
  const LINE_TERMINATOR = (this.LINE_TERMINATOR !== null ? builder.createString(this.LINE_TERMINATOR!) : 0);
  const CHARSET = (this.CHARSET !== null ? builder.createString(this.CHARSET!) : 0);
  const COMPRESSION = (this.COMPRESSION !== null ? builder.createString(this.COMPRESSION!) : 0);
  const DECODER_STATE = IRMDecodeContext.createDecoderStateVector(builder, this.DECODER_STATE);
  const DECODER_STATE_FORMAT = (this.DECODER_STATE_FORMAT !== null ? builder.createString(this.DECODER_STATE_FORMAT!) : 0);
  const DECODER_STATE_VERSION = (this.DECODER_STATE_VERSION !== null ? builder.createString(this.DECODER_STATE_VERSION!) : 0);
  const DECODER_STATE_SHA256 = (this.DECODER_STATE_SHA256 !== null ? builder.createString(this.DECODER_STATE_SHA256!) : 0);
  const DECODER_BUILD_ID = (this.DECODER_BUILD_ID !== null ? builder.createString(this.DECODER_BUILD_ID!) : 0);
  const DECODER_STATE_MEDIA_TYPE = (this.DECODER_STATE_MEDIA_TYPE !== null ? builder.createString(this.DECODER_STATE_MEDIA_TYPE!) : 0);
  const WINDOW = IRMDecodeContext.createWindowVector(builder, this.WINDOW);
  const PARTIAL_RECORD = IRMDecodeContext.createPartialRecordVector(builder, this.PARTIAL_RECORD);

  return IRMDecodeContext.createIRMDecodeContext(builder,
    FORMAT,
    HEADER_LINE,
    this.HEADER_BYTE_LENGTH,
    COLUMN_NAMES,
    DELIMITER,
    QUOTE,
    ESCAPE,
    LINE_TERMINATOR,
    CHARSET,
    COMPRESSION,
    this.DECODED_OFFSET,
    DECODER_STATE,
    DECODER_STATE_FORMAT,
    DECODER_STATE_VERSION,
    this.DECODER_STATE_BYTE_LENGTH,
    DECODER_STATE_SHA256,
    DECODER_BUILD_ID,
    DECODER_STATE_MEDIA_TYPE,
    WINDOW,
    this.WINDOW_BYTE_LENGTH,
    this.WINDOW_POSITION,
    PARTIAL_RECORD
  );
}
}
