// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { odrEstimatorKind } from './odrEstimatorKind.js';
import { odrProcessNoiseKind } from './odrProcessNoiseKind.js';


export class ODRSolverConfiguration implements flatbuffers.IUnpackableObject<ODRSolverConfigurationT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ODRSolverConfiguration {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsODRSolverConfiguration(bb:flatbuffers.ByteBuffer, obj?:ODRSolverConfiguration):ODRSolverConfiguration {
  return (obj || new ODRSolverConfiguration()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsODRSolverConfiguration(bb:flatbuffers.ByteBuffer, obj?:ODRSolverConfiguration):ODRSolverConfiguration {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ODRSolverConfiguration()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

ESTIMATOR():odrEstimatorKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : odrEstimatorKind.UNSPECIFIED;
}

MAXIMUM_ITERATIONS():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

STATE_CONVERGENCE_TOLERANCE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

RMS_CONVERGENCE_TOLERANCE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

SIGMA_EDIT_THRESHOLD():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

PROCESS_NOISE():odrProcessNoiseKind {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : odrProcessNoiseKind.UNSPECIFIED;
}

PROCESS_NOISE_SPECTRAL_DENSITY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

processNoiseSpectralDensityLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

processNoiseSpectralDensityArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

DYNAMIC_MODEL_CORRELATION_TIME_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

PROPAGATOR_PORT_ID():string
PROPAGATOR_PORT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
PROPAGATOR_PORT_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

PROPAGATOR_CAPABILITY():string
PROPAGATOR_CAPABILITY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
PROPAGATOR_CAPABILITY(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

static startODRSolverConfiguration(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addEstimator(builder:flatbuffers.Builder, ESTIMATOR:odrEstimatorKind) {
  builder.addFieldInt8(0, ESTIMATOR, odrEstimatorKind.UNSPECIFIED);
}

static addMaximumIterations(builder:flatbuffers.Builder, MAXIMUM_ITERATIONS:number) {
  builder.addFieldInt32(1, MAXIMUM_ITERATIONS, 0);
}

static addStateConvergenceTolerance(builder:flatbuffers.Builder, STATE_CONVERGENCE_TOLERANCE:number) {
  builder.addFieldFloat64(2, STATE_CONVERGENCE_TOLERANCE, 0.0);
}

static addRmsConvergenceTolerance(builder:flatbuffers.Builder, RMS_CONVERGENCE_TOLERANCE:number) {
  builder.addFieldFloat64(3, RMS_CONVERGENCE_TOLERANCE, 0.0);
}

static addSigmaEditThreshold(builder:flatbuffers.Builder, SIGMA_EDIT_THRESHOLD:number) {
  builder.addFieldFloat64(4, SIGMA_EDIT_THRESHOLD, 0.0);
}

static addProcessNoise(builder:flatbuffers.Builder, PROCESS_NOISE:odrProcessNoiseKind) {
  builder.addFieldInt8(5, PROCESS_NOISE, odrProcessNoiseKind.UNSPECIFIED);
}

static addProcessNoiseSpectralDensity(builder:flatbuffers.Builder, PROCESS_NOISE_SPECTRAL_DENSITYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, PROCESS_NOISE_SPECTRAL_DENSITYOffset, 0);
}

static createProcessNoiseSpectralDensityVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createProcessNoiseSpectralDensityVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createProcessNoiseSpectralDensityVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startProcessNoiseSpectralDensityVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addDynamicModelCorrelationTimeSeconds(builder:flatbuffers.Builder, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS:number) {
  builder.addFieldFloat64(7, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS, 0.0);
}

static addPropagatorPortId(builder:flatbuffers.Builder, PROPAGATOR_PORT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, PROPAGATOR_PORT_IDOffset, 0);
}

static addPropagatorCapability(builder:flatbuffers.Builder, PROPAGATOR_CAPABILITYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, PROPAGATOR_CAPABILITYOffset, 0);
}

static endODRSolverConfiguration(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 20) // PROPAGATOR_PORT_ID
  builder.requiredField(offset, 22) // PROPAGATOR_CAPABILITY
  return offset;
}

static createODRSolverConfiguration(builder:flatbuffers.Builder, ESTIMATOR:odrEstimatorKind, MAXIMUM_ITERATIONS:number, STATE_CONVERGENCE_TOLERANCE:number, RMS_CONVERGENCE_TOLERANCE:number, SIGMA_EDIT_THRESHOLD:number, PROCESS_NOISE:odrProcessNoiseKind, PROCESS_NOISE_SPECTRAL_DENSITYOffset:flatbuffers.Offset, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS:number, PROPAGATOR_PORT_IDOffset:flatbuffers.Offset, PROPAGATOR_CAPABILITYOffset:flatbuffers.Offset):flatbuffers.Offset {
  ODRSolverConfiguration.startODRSolverConfiguration(builder);
  ODRSolverConfiguration.addEstimator(builder, ESTIMATOR);
  ODRSolverConfiguration.addMaximumIterations(builder, MAXIMUM_ITERATIONS);
  ODRSolverConfiguration.addStateConvergenceTolerance(builder, STATE_CONVERGENCE_TOLERANCE);
  ODRSolverConfiguration.addRmsConvergenceTolerance(builder, RMS_CONVERGENCE_TOLERANCE);
  ODRSolverConfiguration.addSigmaEditThreshold(builder, SIGMA_EDIT_THRESHOLD);
  ODRSolverConfiguration.addProcessNoise(builder, PROCESS_NOISE);
  ODRSolverConfiguration.addProcessNoiseSpectralDensity(builder, PROCESS_NOISE_SPECTRAL_DENSITYOffset);
  ODRSolverConfiguration.addDynamicModelCorrelationTimeSeconds(builder, DYNAMIC_MODEL_CORRELATION_TIME_SECONDS);
  ODRSolverConfiguration.addPropagatorPortId(builder, PROPAGATOR_PORT_IDOffset);
  ODRSolverConfiguration.addPropagatorCapability(builder, PROPAGATOR_CAPABILITYOffset);
  return ODRSolverConfiguration.endODRSolverConfiguration(builder);
}

unpack(): ODRSolverConfigurationT {
  return new ODRSolverConfigurationT(
    this.ESTIMATOR(),
    this.MAXIMUM_ITERATIONS(),
    this.STATE_CONVERGENCE_TOLERANCE(),
    this.RMS_CONVERGENCE_TOLERANCE(),
    this.SIGMA_EDIT_THRESHOLD(),
    this.PROCESS_NOISE(),
    this.bb!.createScalarList<number>(this.PROCESS_NOISE_SPECTRAL_DENSITY.bind(this), this.processNoiseSpectralDensityLength()),
    this.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS(),
    this.PROPAGATOR_PORT_ID(),
    this.PROPAGATOR_CAPABILITY()
  );
}


unpackTo(_o: ODRSolverConfigurationT): void {
  _o.ESTIMATOR = this.ESTIMATOR();
  _o.MAXIMUM_ITERATIONS = this.MAXIMUM_ITERATIONS();
  _o.STATE_CONVERGENCE_TOLERANCE = this.STATE_CONVERGENCE_TOLERANCE();
  _o.RMS_CONVERGENCE_TOLERANCE = this.RMS_CONVERGENCE_TOLERANCE();
  _o.SIGMA_EDIT_THRESHOLD = this.SIGMA_EDIT_THRESHOLD();
  _o.PROCESS_NOISE = this.PROCESS_NOISE();
  _o.PROCESS_NOISE_SPECTRAL_DENSITY = this.bb!.createScalarList<number>(this.PROCESS_NOISE_SPECTRAL_DENSITY.bind(this), this.processNoiseSpectralDensityLength());
  _o.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS = this.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS();
  _o.PROPAGATOR_PORT_ID = this.PROPAGATOR_PORT_ID();
  _o.PROPAGATOR_CAPABILITY = this.PROPAGATOR_CAPABILITY();
}
}

export class ODRSolverConfigurationT implements flatbuffers.IGeneratedObject {
constructor(
  public ESTIMATOR: odrEstimatorKind = odrEstimatorKind.UNSPECIFIED,
  public MAXIMUM_ITERATIONS: number = 0,
  public STATE_CONVERGENCE_TOLERANCE: number = 0.0,
  public RMS_CONVERGENCE_TOLERANCE: number = 0.0,
  public SIGMA_EDIT_THRESHOLD: number = 0.0,
  public PROCESS_NOISE: odrProcessNoiseKind = odrProcessNoiseKind.UNSPECIFIED,
  public PROCESS_NOISE_SPECTRAL_DENSITY: (number)[] = [],
  public DYNAMIC_MODEL_CORRELATION_TIME_SECONDS: number = 0.0,
  public PROPAGATOR_PORT_ID: string|Uint8Array|null = null,
  public PROPAGATOR_CAPABILITY: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PROCESS_NOISE_SPECTRAL_DENSITY = ODRSolverConfiguration.createProcessNoiseSpectralDensityVector(builder, this.PROCESS_NOISE_SPECTRAL_DENSITY);
  const PROPAGATOR_PORT_ID = (this.PROPAGATOR_PORT_ID !== null ? builder.createString(this.PROPAGATOR_PORT_ID!) : 0);
  const PROPAGATOR_CAPABILITY = (this.PROPAGATOR_CAPABILITY !== null ? builder.createString(this.PROPAGATOR_CAPABILITY!) : 0);

  return ODRSolverConfiguration.createODRSolverConfiguration(builder,
    this.ESTIMATOR,
    this.MAXIMUM_ITERATIONS,
    this.STATE_CONVERGENCE_TOLERANCE,
    this.RMS_CONVERGENCE_TOLERANCE,
    this.SIGMA_EDIT_THRESHOLD,
    this.PROCESS_NOISE,
    PROCESS_NOISE_SPECTRAL_DENSITY,
    this.DYNAMIC_MODEL_CORRELATION_TIME_SECONDS,
    PROPAGATOR_PORT_ID,
    PROPAGATOR_CAPABILITY
  );
}
}
