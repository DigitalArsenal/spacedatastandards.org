// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { FRMStateVector, FRMStateVectorT } from './FRMStateVector.js';
import { ODRAttestation, ODRAttestationT } from './ODRAttestation.js';
import { ODREditedObservation, ODREditedObservationT } from './ODREditedObservation.js';
import { ODREstimatedParameter, ODREstimatedParameterT } from './ODREstimatedParameter.js';
import { ODRFilterEpoch, ODRFilterEpochT } from './ODRFilterEpoch.js';
import { ODRIterationStatistics, ODRIterationStatisticsT } from './ODRIterationStatistics.js';
import { ODRSolverConfiguration, ODRSolverConfigurationT } from './ODRSolverConfiguration.js';
import { odrConvergenceReason } from './odrConvergenceReason.js';


/**
 * Estimation Run Report. This is the canonical estimation-family result
 * model. STATE_COVARIANCE is row-major with COVARIANCE_DIMENSION squared
 * entries and is also published as the $OCM named by OCM_CONTENT_ID. The
 * estimator report never substitutes a propagator name for its input port.
 */
export class ODR implements flatbuffers.IUnpackableObject<ODRT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ODR {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsODR(bb:flatbuffers.ByteBuffer, obj?:ODR):ODR {
  return (obj || new ODR()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsODR(bb:flatbuffers.ByteBuffer, obj?:ODR):ODR {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ODR()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$ODR');
}

RUN_ID():string
RUN_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
RUN_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

STARTED_AT():string|null
STARTED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
STARTED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

COMPLETED_AT():string|null
COMPLETED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
COMPLETED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

CONFIGURATION(obj?:ODRSolverConfiguration):ODRSolverConfiguration|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new ODRSolverConfiguration()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

CONVERGENCE_REASON():odrConvergenceReason {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : odrConvergenceReason.UNSPECIFIED;
}

ITERATIONS(index: number, obj?:ODRIterationStatistics):ODRIterationStatistics|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new ODRIterationStatistics()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

iterationsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

EDITED_OBSERVATIONS(index: number, obj?:ODREditedObservation):ODREditedObservation|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new ODREditedObservation()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

editedObservationsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

ESTIMATED_PARAMETERS(index: number, obj?:ODREstimatedParameter):ODREstimatedParameter|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? (obj || new ODREstimatedParameter()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

estimatedParametersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

ESTIMATED_EPOCH_STATE(obj?:FRMStateVector):FRMStateVector|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new FRMStateVector()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

COVARIANCE_DIMENSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

STATE_COVARIANCE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

stateCovarianceLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

stateCovarianceArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

OCM_CONTENT_ID():string
OCM_CONTENT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
OCM_CONTENT_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

FILTER_HISTORY(index: number, obj?:ODRFilterEpoch):ODRFilterEpoch|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? (obj || new ODRFilterEpoch()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

filterHistoryLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

RESIDUAL_RMS():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

TRACE_ID():string|null
TRACE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TRACE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

ATTESTATION(obj?:ODRAttestation):ODRAttestation|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? (obj || new ODRAttestation()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startODR(builder:flatbuffers.Builder) {
  builder.startObject(16);
}

static addRunId(builder:flatbuffers.Builder, RUN_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, RUN_IDOffset, 0);
}

static addStartedAt(builder:flatbuffers.Builder, STARTED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, STARTED_ATOffset, 0);
}

static addCompletedAt(builder:flatbuffers.Builder, COMPLETED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, COMPLETED_ATOffset, 0);
}

static addConfiguration(builder:flatbuffers.Builder, CONFIGURATIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, CONFIGURATIONOffset, 0);
}

static addConvergenceReason(builder:flatbuffers.Builder, CONVERGENCE_REASON:odrConvergenceReason) {
  builder.addFieldInt8(4, CONVERGENCE_REASON, odrConvergenceReason.UNSPECIFIED);
}

static addIterations(builder:flatbuffers.Builder, ITERATIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ITERATIONSOffset, 0);
}

static createIterationsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startIterationsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEditedObservations(builder:flatbuffers.Builder, EDITED_OBSERVATIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, EDITED_OBSERVATIONSOffset, 0);
}

static createEditedObservationsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEditedObservationsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEstimatedParameters(builder:flatbuffers.Builder, ESTIMATED_PARAMETERSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, ESTIMATED_PARAMETERSOffset, 0);
}

static createEstimatedParametersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEstimatedParametersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEstimatedEpochState(builder:flatbuffers.Builder, ESTIMATED_EPOCH_STATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, ESTIMATED_EPOCH_STATEOffset, 0);
}

static addCovarianceDimension(builder:flatbuffers.Builder, COVARIANCE_DIMENSION:number) {
  builder.addFieldInt32(9, COVARIANCE_DIMENSION, 0);
}

static addStateCovariance(builder:flatbuffers.Builder, STATE_COVARIANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, STATE_COVARIANCEOffset, 0);
}

static createStateCovarianceVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createStateCovarianceVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createStateCovarianceVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startStateCovarianceVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addOcmContentId(builder:flatbuffers.Builder, OCM_CONTENT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, OCM_CONTENT_IDOffset, 0);
}

static addFilterHistory(builder:flatbuffers.Builder, FILTER_HISTORYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, FILTER_HISTORYOffset, 0);
}

static createFilterHistoryVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startFilterHistoryVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addResidualRms(builder:flatbuffers.Builder, RESIDUAL_RMS:number) {
  builder.addFieldFloat64(13, RESIDUAL_RMS, 0.0);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, TRACE_IDOffset, 0);
}

static addAttestation(builder:flatbuffers.Builder, ATTESTATIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(15, ATTESTATIONOffset, 0);
}

static endODR(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // RUN_ID
  builder.requiredField(offset, 26) // OCM_CONTENT_ID
  return offset;
}

static finishODRBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$ODR');
}

static finishSizePrefixedODRBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$ODR', true);
}


unpack(): ODRT {
  return new ODRT(
    this.RUN_ID(),
    this.STARTED_AT(),
    this.COMPLETED_AT(),
    (this.CONFIGURATION() !== null ? this.CONFIGURATION()!.unpack() : null),
    this.CONVERGENCE_REASON(),
    this.bb!.createObjList<ODRIterationStatistics, ODRIterationStatisticsT>(this.ITERATIONS.bind(this), this.iterationsLength()),
    this.bb!.createObjList<ODREditedObservation, ODREditedObservationT>(this.EDITED_OBSERVATIONS.bind(this), this.editedObservationsLength()),
    this.bb!.createObjList<ODREstimatedParameter, ODREstimatedParameterT>(this.ESTIMATED_PARAMETERS.bind(this), this.estimatedParametersLength()),
    (this.ESTIMATED_EPOCH_STATE() !== null ? this.ESTIMATED_EPOCH_STATE()!.unpack() : null),
    this.COVARIANCE_DIMENSION(),
    this.bb!.createScalarList<number>(this.STATE_COVARIANCE.bind(this), this.stateCovarianceLength()),
    this.OCM_CONTENT_ID(),
    this.bb!.createObjList<ODRFilterEpoch, ODRFilterEpochT>(this.FILTER_HISTORY.bind(this), this.filterHistoryLength()),
    this.RESIDUAL_RMS(),
    this.TRACE_ID(),
    (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null)
  );
}


unpackTo(_o: ODRT): void {
  _o.RUN_ID = this.RUN_ID();
  _o.STARTED_AT = this.STARTED_AT();
  _o.COMPLETED_AT = this.COMPLETED_AT();
  _o.CONFIGURATION = (this.CONFIGURATION() !== null ? this.CONFIGURATION()!.unpack() : null);
  _o.CONVERGENCE_REASON = this.CONVERGENCE_REASON();
  _o.ITERATIONS = this.bb!.createObjList<ODRIterationStatistics, ODRIterationStatisticsT>(this.ITERATIONS.bind(this), this.iterationsLength());
  _o.EDITED_OBSERVATIONS = this.bb!.createObjList<ODREditedObservation, ODREditedObservationT>(this.EDITED_OBSERVATIONS.bind(this), this.editedObservationsLength());
  _o.ESTIMATED_PARAMETERS = this.bb!.createObjList<ODREstimatedParameter, ODREstimatedParameterT>(this.ESTIMATED_PARAMETERS.bind(this), this.estimatedParametersLength());
  _o.ESTIMATED_EPOCH_STATE = (this.ESTIMATED_EPOCH_STATE() !== null ? this.ESTIMATED_EPOCH_STATE()!.unpack() : null);
  _o.COVARIANCE_DIMENSION = this.COVARIANCE_DIMENSION();
  _o.STATE_COVARIANCE = this.bb!.createScalarList<number>(this.STATE_COVARIANCE.bind(this), this.stateCovarianceLength());
  _o.OCM_CONTENT_ID = this.OCM_CONTENT_ID();
  _o.FILTER_HISTORY = this.bb!.createObjList<ODRFilterEpoch, ODRFilterEpochT>(this.FILTER_HISTORY.bind(this), this.filterHistoryLength());
  _o.RESIDUAL_RMS = this.RESIDUAL_RMS();
  _o.TRACE_ID = this.TRACE_ID();
  _o.ATTESTATION = (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null);
}
}

export class ODRT implements flatbuffers.IGeneratedObject {
constructor(
  public RUN_ID: string|Uint8Array|null = null,
  public STARTED_AT: string|Uint8Array|null = null,
  public COMPLETED_AT: string|Uint8Array|null = null,
  public CONFIGURATION: ODRSolverConfigurationT|null = null,
  public CONVERGENCE_REASON: odrConvergenceReason = odrConvergenceReason.UNSPECIFIED,
  public ITERATIONS: (ODRIterationStatisticsT)[] = [],
  public EDITED_OBSERVATIONS: (ODREditedObservationT)[] = [],
  public ESTIMATED_PARAMETERS: (ODREstimatedParameterT)[] = [],
  public ESTIMATED_EPOCH_STATE: FRMStateVectorT|null = null,
  public COVARIANCE_DIMENSION: number = 0,
  public STATE_COVARIANCE: (number)[] = [],
  public OCM_CONTENT_ID: string|Uint8Array|null = null,
  public FILTER_HISTORY: (ODRFilterEpochT)[] = [],
  public RESIDUAL_RMS: number = 0.0,
  public TRACE_ID: string|Uint8Array|null = null,
  public ATTESTATION: ODRAttestationT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const RUN_ID = (this.RUN_ID !== null ? builder.createString(this.RUN_ID!) : 0);
  const STARTED_AT = (this.STARTED_AT !== null ? builder.createString(this.STARTED_AT!) : 0);
  const COMPLETED_AT = (this.COMPLETED_AT !== null ? builder.createString(this.COMPLETED_AT!) : 0);
  const CONFIGURATION = (this.CONFIGURATION !== null ? this.CONFIGURATION!.pack(builder) : 0);
  const ITERATIONS = ODR.createIterationsVector(builder, builder.createObjectOffsetList(this.ITERATIONS));
  const EDITED_OBSERVATIONS = ODR.createEditedObservationsVector(builder, builder.createObjectOffsetList(this.EDITED_OBSERVATIONS));
  const ESTIMATED_PARAMETERS = ODR.createEstimatedParametersVector(builder, builder.createObjectOffsetList(this.ESTIMATED_PARAMETERS));
  const ESTIMATED_EPOCH_STATE = (this.ESTIMATED_EPOCH_STATE !== null ? this.ESTIMATED_EPOCH_STATE!.pack(builder) : 0);
  const STATE_COVARIANCE = ODR.createStateCovarianceVector(builder, this.STATE_COVARIANCE);
  const OCM_CONTENT_ID = (this.OCM_CONTENT_ID !== null ? builder.createString(this.OCM_CONTENT_ID!) : 0);
  const FILTER_HISTORY = ODR.createFilterHistoryVector(builder, builder.createObjectOffsetList(this.FILTER_HISTORY));
  const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID!) : 0);
  const ATTESTATION = (this.ATTESTATION !== null ? this.ATTESTATION!.pack(builder) : 0);

  ODR.startODR(builder);
  ODR.addRunId(builder, RUN_ID);
  ODR.addStartedAt(builder, STARTED_AT);
  ODR.addCompletedAt(builder, COMPLETED_AT);
  ODR.addConfiguration(builder, CONFIGURATION);
  ODR.addConvergenceReason(builder, this.CONVERGENCE_REASON);
  ODR.addIterations(builder, ITERATIONS);
  ODR.addEditedObservations(builder, EDITED_OBSERVATIONS);
  ODR.addEstimatedParameters(builder, ESTIMATED_PARAMETERS);
  ODR.addEstimatedEpochState(builder, ESTIMATED_EPOCH_STATE);
  ODR.addCovarianceDimension(builder, this.COVARIANCE_DIMENSION);
  ODR.addStateCovariance(builder, STATE_COVARIANCE);
  ODR.addOcmContentId(builder, OCM_CONTENT_ID);
  ODR.addFilterHistory(builder, FILTER_HISTORY);
  ODR.addResidualRms(builder, this.RESIDUAL_RMS);
  ODR.addTraceId(builder, TRACE_ID);
  ODR.addAttestation(builder, ATTESTATION);

  return ODR.endODR(builder);
}
}
