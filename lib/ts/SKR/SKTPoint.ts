// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One point source packed for drawing (24 bytes).
 */
export class SKTPoint implements flatbuffers.IUnpackableObject<SKTPointT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SKTPoint {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

/**
 * Right ascension (or longitude in FRAME) as a fraction of a full turn:
 * degrees = RA_FIXED * 360 / 2^32.
 */
RA_FIXED():number {
  return this.bb!.readUint32(this.bb_pos);
}

/**
 * Declination (or latitude in FRAME): degrees = DEC_FIXED * 90 / 2^31.
 */
DEC_FIXED():number {
  return this.bb!.readInt32(this.bb_pos + 4);
}

/**
 * Proper motion in RA times cos(DEC), milliarcseconds per year. -32768
 * when unknown or out of range (FLAGS bit 2).
 */
PMRA():number {
  return this.bb!.readInt16(this.bb_pos + 8);
}

/**
 * Proper motion in DEC, milliarcseconds per year. -32768 when unknown or
 * out of range (FLAGS bit 2).
 */
PMDEC():number {
  return this.bb!.readInt16(this.bb_pos + 10);
}

/**
 * Parallax in units of 0.02 milliarcseconds. 0 when unknown, not
 * positive or out of range (FLAGS bit 3).
 */
PARALLAX():number {
  return this.bb!.readUint16(this.bb_pos + 12);
}

/**
 * Magnitude in the tile's band, millimagnitudes. -32768 when unknown.
 */
MAG():number {
  return this.bb!.readInt16(this.bb_pos + 14);
}

/**
 * Colour, encoded as the tile's COLOR_ENCODING states.
 */
COLOR():number {
  return this.bb!.readUint16(this.bb_pos + 16);
}

/**
 * Bit flags. 0: variable. 1: member of a multiple system. 2: PMRA/PMDEC
 * not usable. 3: PARALLAX not usable. 4: MAG is modelled, not measured.
 * 5: ROW refers to a `$CSO` rather than a `$STR`. 6: a neighbour lies
 * within the confusion radius. 7: extended source. 8-15: reserved, 0.
 */
FLAGS():number {
  return this.bb!.readUint16(this.bb_pos + 18);
}

/**
 * Index of the source's full record in the tile's row collection
 * (ROW_COLLECTION_CID), from 0. 0xFFFFFFFF when the tile has none.
 */
ROW():number {
  return this.bb!.readUint32(this.bb_pos + 20);
}

static sizeOf():number {
  return 24;
}

static createSKTPoint(builder:flatbuffers.Builder, RA_FIXED: number, DEC_FIXED: number, PMRA: number, PMDEC: number, PARALLAX: number, MAG: number, COLOR: number, FLAGS: number, ROW: number):flatbuffers.Offset {
  builder.prep(4, 24);
  builder.writeInt32(ROW);
  builder.writeInt16(FLAGS);
  builder.writeInt16(COLOR);
  builder.writeInt16(MAG);
  builder.writeInt16(PARALLAX);
  builder.writeInt16(PMDEC);
  builder.writeInt16(PMRA);
  builder.writeInt32(DEC_FIXED);
  builder.writeInt32(RA_FIXED);
  return builder.offset();
}


unpack(): SKTPointT {
  return new SKTPointT(
    this.RA_FIXED(),
    this.DEC_FIXED(),
    this.PMRA(),
    this.PMDEC(),
    this.PARALLAX(),
    this.MAG(),
    this.COLOR(),
    this.FLAGS(),
    this.ROW()
  );
}


unpackTo(_o: SKTPointT): void {
  _o.RA_FIXED = this.RA_FIXED();
  _o.DEC_FIXED = this.DEC_FIXED();
  _o.PMRA = this.PMRA();
  _o.PMDEC = this.PMDEC();
  _o.PARALLAX = this.PARALLAX();
  _o.MAG = this.MAG();
  _o.COLOR = this.COLOR();
  _o.FLAGS = this.FLAGS();
  _o.ROW = this.ROW();
}
}

export class SKTPointT implements flatbuffers.IGeneratedObject {
constructor(
  public RA_FIXED: number = 0,
  public DEC_FIXED: number = 0,
  public PMRA: number = 0,
  public PMDEC: number = 0,
  public PARALLAX: number = 0,
  public MAG: number = 0,
  public COLOR: number = 0,
  public FLAGS: number = 0,
  public ROW: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return SKTPoint.createSKTPoint(builder,
    this.RA_FIXED,
    this.DEC_FIXED,
    this.PMRA,
    this.PMDEC,
    this.PARALLAX,
    this.MAG,
    this.COLOR,
    this.FLAGS,
    this.ROW
  );
}
}
