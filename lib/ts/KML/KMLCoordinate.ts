// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * KML coordinate (longitude, latitude, optional altitude)
 */
export class KMLCoordinate implements flatbuffers.IUnpackableObject<KMLCoordinateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLCoordinate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLCoordinate(bb:flatbuffers.ByteBuffer, obj?:KMLCoordinate):KMLCoordinate {
  return (obj || new KMLCoordinate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLCoordinate(bb:flatbuffers.ByteBuffer, obj?:KMLCoordinate):KMLCoordinate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLCoordinate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Longitude in decimal degrees
 */
LONGITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Latitude in decimal degrees
 */
LATITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Altitude in meters
 */
ALTITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startKMLCoordinate(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addLongitude(builder:flatbuffers.Builder, LONGITUDE:number) {
  builder.addFieldFloat64(0, LONGITUDE, 0.0);
}

static addLatitude(builder:flatbuffers.Builder, LATITUDE:number) {
  builder.addFieldFloat64(1, LATITUDE, 0.0);
}

static addAltitude(builder:flatbuffers.Builder, ALTITUDE:number) {
  builder.addFieldFloat64(2, ALTITUDE, 0.0);
}

static endKMLCoordinate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLCoordinate(builder:flatbuffers.Builder, LONGITUDE:number, LATITUDE:number, ALTITUDE:number):flatbuffers.Offset {
  KMLCoordinate.startKMLCoordinate(builder);
  KMLCoordinate.addLongitude(builder, LONGITUDE);
  KMLCoordinate.addLatitude(builder, LATITUDE);
  KMLCoordinate.addAltitude(builder, ALTITUDE);
  return KMLCoordinate.endKMLCoordinate(builder);
}

unpack(): KMLCoordinateT {
  return new KMLCoordinateT(
    this.LONGITUDE(),
    this.LATITUDE(),
    this.ALTITUDE()
  );
}


unpackTo(_o: KMLCoordinateT): void {
  _o.LONGITUDE = this.LONGITUDE();
  _o.LATITUDE = this.LATITUDE();
  _o.ALTITUDE = this.ALTITUDE();
}
}

export class KMLCoordinateT implements flatbuffers.IGeneratedObject {
constructor(
  public LONGITUDE: number = 0.0,
  public LATITUDE: number = 0.0,
  public ALTITUDE: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return KMLCoordinate.createKMLCoordinate(builder,
    this.LONGITUDE,
    this.LATITUDE,
    this.ALTITUDE
  );
}
}
