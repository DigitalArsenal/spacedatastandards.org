// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * TimeStamp
 */
export class KMLTimeStamp implements flatbuffers.IUnpackableObject<KMLTimeStampT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLTimeStamp {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLTimeStamp(bb:flatbuffers.ByteBuffer, obj?:KMLTimeStamp):KMLTimeStamp {
  return (obj || new KMLTimeStamp()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLTimeStamp(bb:flatbuffers.ByteBuffer, obj?:KMLTimeStamp):KMLTimeStamp {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLTimeStamp()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Time (ISO 8601)
 */
WHEN():string|null
WHEN(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
WHEN(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startKMLTimeStamp(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addWhen(builder:flatbuffers.Builder, WHENOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, WHENOffset, 0);
}

static endKMLTimeStamp(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLTimeStamp(builder:flatbuffers.Builder, WHENOffset:flatbuffers.Offset):flatbuffers.Offset {
  KMLTimeStamp.startKMLTimeStamp(builder);
  KMLTimeStamp.addWhen(builder, WHENOffset);
  return KMLTimeStamp.endKMLTimeStamp(builder);
}

unpack(): KMLTimeStampT {
  return new KMLTimeStampT(
    this.WHEN()
  );
}


unpackTo(_o: KMLTimeStampT): void {
  _o.WHEN = this.WHEN();
}
}

export class KMLTimeStampT implements flatbuffers.IGeneratedObject {
constructor(
  public WHEN: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const WHEN = (this.WHEN !== null ? builder.createString(this.WHEN!) : 0);

  return KMLTimeStamp.createKMLTimeStamp(builder,
    WHEN
  );
}
}
