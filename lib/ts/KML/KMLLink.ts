// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLRefreshMode } from './KMLRefreshMode.js';
import { KMLViewRefreshMode } from './KMLViewRefreshMode.js';


/**
 * Full Link element
 */
export class KMLLink implements flatbuffers.IUnpackableObject<KMLLinkT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLLink {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLLink(bb:flatbuffers.ByteBuffer, obj?:KMLLink):KMLLink {
  return (obj || new KMLLink()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLLink(bb:flatbuffers.ByteBuffer, obj?:KMLLink):KMLLink {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLLink()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * URL
 */
HREF():string|null
HREF(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
HREF(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Refresh mode
 */
REFRESH_MODE():KMLRefreshMode {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLRefreshMode.ON_CHANGE;
}

/**
 * Refresh interval in seconds
 */
REFRESH_INTERVAL():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * View refresh mode
 */
VIEW_REFRESH_MODE():KMLViewRefreshMode {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLViewRefreshMode.NEVER;
}

/**
 * View refresh time in seconds
 */
VIEW_REFRESH_TIME():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * View bound scale
 */
VIEW_BOUND_SCALE():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * View format string
 */
VIEW_FORMAT():string|null
VIEW_FORMAT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VIEW_FORMAT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * HTTP query string
 */
HTTP_QUERY():string|null
HTTP_QUERY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
HTTP_QUERY(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startKMLLink(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addHref(builder:flatbuffers.Builder, HREFOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, HREFOffset, 0);
}

static addRefreshMode(builder:flatbuffers.Builder, REFRESH_MODE:KMLRefreshMode) {
  builder.addFieldInt8(1, REFRESH_MODE, KMLRefreshMode.ON_CHANGE);
}

static addRefreshInterval(builder:flatbuffers.Builder, REFRESH_INTERVAL:number) {
  builder.addFieldFloat64(2, REFRESH_INTERVAL, 0.0);
}

static addViewRefreshMode(builder:flatbuffers.Builder, VIEW_REFRESH_MODE:KMLViewRefreshMode) {
  builder.addFieldInt8(3, VIEW_REFRESH_MODE, KMLViewRefreshMode.NEVER);
}

static addViewRefreshTime(builder:flatbuffers.Builder, VIEW_REFRESH_TIME:number) {
  builder.addFieldFloat64(4, VIEW_REFRESH_TIME, 0.0);
}

static addViewBoundScale(builder:flatbuffers.Builder, VIEW_BOUND_SCALE:number) {
  builder.addFieldFloat64(5, VIEW_BOUND_SCALE, 0.0);
}

static addViewFormat(builder:flatbuffers.Builder, VIEW_FORMATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, VIEW_FORMATOffset, 0);
}

static addHttpQuery(builder:flatbuffers.Builder, HTTP_QUERYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, HTTP_QUERYOffset, 0);
}

static endKMLLink(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLLink(builder:flatbuffers.Builder, HREFOffset:flatbuffers.Offset, REFRESH_MODE:KMLRefreshMode, REFRESH_INTERVAL:number, VIEW_REFRESH_MODE:KMLViewRefreshMode, VIEW_REFRESH_TIME:number, VIEW_BOUND_SCALE:number, VIEW_FORMATOffset:flatbuffers.Offset, HTTP_QUERYOffset:flatbuffers.Offset):flatbuffers.Offset {
  KMLLink.startKMLLink(builder);
  KMLLink.addHref(builder, HREFOffset);
  KMLLink.addRefreshMode(builder, REFRESH_MODE);
  KMLLink.addRefreshInterval(builder, REFRESH_INTERVAL);
  KMLLink.addViewRefreshMode(builder, VIEW_REFRESH_MODE);
  KMLLink.addViewRefreshTime(builder, VIEW_REFRESH_TIME);
  KMLLink.addViewBoundScale(builder, VIEW_BOUND_SCALE);
  KMLLink.addViewFormat(builder, VIEW_FORMATOffset);
  KMLLink.addHttpQuery(builder, HTTP_QUERYOffset);
  return KMLLink.endKMLLink(builder);
}

unpack(): KMLLinkT {
  return new KMLLinkT(
    this.HREF(),
    this.REFRESH_MODE(),
    this.REFRESH_INTERVAL(),
    this.VIEW_REFRESH_MODE(),
    this.VIEW_REFRESH_TIME(),
    this.VIEW_BOUND_SCALE(),
    this.VIEW_FORMAT(),
    this.HTTP_QUERY()
  );
}


unpackTo(_o: KMLLinkT): void {
  _o.HREF = this.HREF();
  _o.REFRESH_MODE = this.REFRESH_MODE();
  _o.REFRESH_INTERVAL = this.REFRESH_INTERVAL();
  _o.VIEW_REFRESH_MODE = this.VIEW_REFRESH_MODE();
  _o.VIEW_REFRESH_TIME = this.VIEW_REFRESH_TIME();
  _o.VIEW_BOUND_SCALE = this.VIEW_BOUND_SCALE();
  _o.VIEW_FORMAT = this.VIEW_FORMAT();
  _o.HTTP_QUERY = this.HTTP_QUERY();
}
}

export class KMLLinkT implements flatbuffers.IGeneratedObject {
constructor(
  public HREF: string|Uint8Array|null = null,
  public REFRESH_MODE: KMLRefreshMode = KMLRefreshMode.ON_CHANGE,
  public REFRESH_INTERVAL: number = 0.0,
  public VIEW_REFRESH_MODE: KMLViewRefreshMode = KMLViewRefreshMode.NEVER,
  public VIEW_REFRESH_TIME: number = 0.0,
  public VIEW_BOUND_SCALE: number = 0.0,
  public VIEW_FORMAT: string|Uint8Array|null = null,
  public HTTP_QUERY: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const HREF = (this.HREF !== null ? builder.createString(this.HREF!) : 0);
  const VIEW_FORMAT = (this.VIEW_FORMAT !== null ? builder.createString(this.VIEW_FORMAT!) : 0);
  const HTTP_QUERY = (this.HTTP_QUERY !== null ? builder.createString(this.HTTP_QUERY!) : 0);

  return KMLLink.createKMLLink(builder,
    HREF,
    this.REFRESH_MODE,
    this.REFRESH_INTERVAL,
    this.VIEW_REFRESH_MODE,
    this.VIEW_REFRESH_TIME,
    this.VIEW_BOUND_SCALE,
    VIEW_FORMAT,
    HTTP_QUERY
  );
}
}
