// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLCoordinate, KMLCoordinateT } from './KMLCoordinate.js';
import { KMLModel, KMLModelT } from './KMLModel.js';


/**
 * gx:Track — time-stamped position track
 */
export class KMLTrack implements flatbuffers.IUnpackableObject<KMLTrackT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLTrack {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLTrack(bb:flatbuffers.ByteBuffer, obj?:KMLTrack):KMLTrack {
  return (obj || new KMLTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLTrack(bb:flatbuffers.ByteBuffer, obj?:KMLTrack):KMLTrack {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Whether to extrude
 */
EXTRUDE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Whether to tessellate
 */
TESSELLATE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Altitude mode
 */
ALTITUDE_MODE():KMLAltitudeMode {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
}

/**
 * Time stamps (ISO 8601)
 */
WHEN(index: number):string
WHEN(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
WHEN(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

whenLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Coordinates (lon lat alt per entry)
 */
COORDS(index: number, obj?:KMLCoordinate):KMLCoordinate|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new KMLCoordinate()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

coordsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Angles (heading tilt roll per entry)
 */
ANGLES(index: number, obj?:KMLCoordinate):KMLCoordinate|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new KMLCoordinate()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

anglesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Model for track visualization
 */
MODEL(obj?:KMLModel):KMLModel|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new KMLModel()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startKMLTrack(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addExtrude(builder:flatbuffers.Builder, EXTRUDE:boolean) {
  builder.addFieldInt8(0, +EXTRUDE, +false);
}

static addTessellate(builder:flatbuffers.Builder, TESSELLATE:boolean) {
  builder.addFieldInt8(1, +TESSELLATE, +false);
}

static addAltitudeMode(builder:flatbuffers.Builder, ALTITUDE_MODE:KMLAltitudeMode) {
  builder.addFieldInt8(2, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
}

static addWhen(builder:flatbuffers.Builder, WHENOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, WHENOffset, 0);
}

static createWhenVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startWhenVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCoords(builder:flatbuffers.Builder, COORDSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, COORDSOffset, 0);
}

static createCoordsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCoordsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addAngles(builder:flatbuffers.Builder, ANGLESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ANGLESOffset, 0);
}

static createAnglesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAnglesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addModel(builder:flatbuffers.Builder, MODELOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, MODELOffset, 0);
}

static endKMLTrack(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): KMLTrackT {
  return new KMLTrackT(
    this.EXTRUDE(),
    this.TESSELLATE(),
    this.ALTITUDE_MODE(),
    this.bb!.createScalarList<string>(this.WHEN.bind(this), this.whenLength()),
    this.bb!.createObjList<KMLCoordinate, KMLCoordinateT>(this.COORDS.bind(this), this.coordsLength()),
    this.bb!.createObjList<KMLCoordinate, KMLCoordinateT>(this.ANGLES.bind(this), this.anglesLength()),
    (this.MODEL() !== null ? this.MODEL()!.unpack() : null)
  );
}


unpackTo(_o: KMLTrackT): void {
  _o.EXTRUDE = this.EXTRUDE();
  _o.TESSELLATE = this.TESSELLATE();
  _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
  _o.WHEN = this.bb!.createScalarList<string>(this.WHEN.bind(this), this.whenLength());
  _o.COORDS = this.bb!.createObjList<KMLCoordinate, KMLCoordinateT>(this.COORDS.bind(this), this.coordsLength());
  _o.ANGLES = this.bb!.createObjList<KMLCoordinate, KMLCoordinateT>(this.ANGLES.bind(this), this.anglesLength());
  _o.MODEL = (this.MODEL() !== null ? this.MODEL()!.unpack() : null);
}
}

export class KMLTrackT implements flatbuffers.IGeneratedObject {
constructor(
  public EXTRUDE: boolean = false,
  public TESSELLATE: boolean = false,
  public ALTITUDE_MODE: KMLAltitudeMode = KMLAltitudeMode.CLAMP_TO_GROUND,
  public WHEN: (string)[] = [],
  public COORDS: (KMLCoordinateT)[] = [],
  public ANGLES: (KMLCoordinateT)[] = [],
  public MODEL: KMLModelT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const WHEN = KMLTrack.createWhenVector(builder, builder.createObjectOffsetList(this.WHEN));
  const COORDS = KMLTrack.createCoordsVector(builder, builder.createObjectOffsetList(this.COORDS));
  const ANGLES = KMLTrack.createAnglesVector(builder, builder.createObjectOffsetList(this.ANGLES));
  const MODEL = (this.MODEL !== null ? this.MODEL!.pack(builder) : 0);

  KMLTrack.startKMLTrack(builder);
  KMLTrack.addExtrude(builder, this.EXTRUDE);
  KMLTrack.addTessellate(builder, this.TESSELLATE);
  KMLTrack.addAltitudeMode(builder, this.ALTITUDE_MODE);
  KMLTrack.addWhen(builder, WHEN);
  KMLTrack.addCoords(builder, COORDS);
  KMLTrack.addAngles(builder, ANGLES);
  KMLTrack.addModel(builder, MODEL);

  return KMLTrack.endKMLTrack(builder);
}
}
