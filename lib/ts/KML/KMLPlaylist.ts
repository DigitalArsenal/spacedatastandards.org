// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLTourPrimitive, KMLTourPrimitiveT } from './KMLTourPrimitive.js';


/**
 * gx:Playlist
 */
export class KMLPlaylist implements flatbuffers.IUnpackableObject<KMLPlaylistT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLPlaylist {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLPlaylist(bb:flatbuffers.ByteBuffer, obj?:KMLPlaylist):KMLPlaylist {
  return (obj || new KMLPlaylist()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLPlaylist(bb:flatbuffers.ByteBuffer, obj?:KMLPlaylist):KMLPlaylist {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLPlaylist()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Tour primitives
 */
PRIMITIVES(index: number, obj?:KMLTourPrimitive):KMLTourPrimitive|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new KMLTourPrimitive()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

primitivesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startKMLPlaylist(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addPrimitives(builder:flatbuffers.Builder, PRIMITIVESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, PRIMITIVESOffset, 0);
}

static createPrimitivesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPrimitivesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endKMLPlaylist(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLPlaylist(builder:flatbuffers.Builder, PRIMITIVESOffset:flatbuffers.Offset):flatbuffers.Offset {
  KMLPlaylist.startKMLPlaylist(builder);
  KMLPlaylist.addPrimitives(builder, PRIMITIVESOffset);
  return KMLPlaylist.endKMLPlaylist(builder);
}

unpack(): KMLPlaylistT {
  return new KMLPlaylistT(
    this.bb!.createObjList<KMLTourPrimitive, KMLTourPrimitiveT>(this.PRIMITIVES.bind(this), this.primitivesLength())
  );
}


unpackTo(_o: KMLPlaylistT): void {
  _o.PRIMITIVES = this.bb!.createObjList<KMLTourPrimitive, KMLTourPrimitiveT>(this.PRIMITIVES.bind(this), this.primitivesLength());
}
}

export class KMLPlaylistT implements flatbuffers.IGeneratedObject {
constructor(
  public PRIMITIVES: (KMLTourPrimitiveT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PRIMITIVES = KMLPlaylist.createPrimitivesVector(builder, builder.createObjectOffsetList(this.PRIMITIVES));

  return KMLPlaylist.createKMLPlaylist(builder,
    PRIMITIVES
  );
}
}
