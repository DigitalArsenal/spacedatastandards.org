// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLLinearRing, KMLLinearRingT } from './KMLLinearRing.js';


/**
 * Polygon geometry
 */
export class KMLPolygon implements flatbuffers.IUnpackableObject<KMLPolygonT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLPolygon {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLPolygon(bb:flatbuffers.ByteBuffer, obj?:KMLPolygon):KMLPolygon {
  return (obj || new KMLPolygon()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLPolygon(bb:flatbuffers.ByteBuffer, obj?:KMLPolygon):KMLPolygon {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLPolygon()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Outer boundary
 */
OUTER_BOUNDARY(obj?:KMLLinearRing):KMLLinearRing|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new KMLLinearRing()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Inner boundaries (holes)
 */
INNER_BOUNDARIES(index: number, obj?:KMLLinearRing):KMLLinearRing|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new KMLLinearRing()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

innerBoundariesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Altitude mode
 */
ALTITUDE_MODE():KMLAltitudeMode {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
}

/**
 * Whether to extrude to ground
 */
EXTRUDE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Whether to tessellate
 */
TESSELLATE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startKMLPolygon(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addOuterBoundary(builder:flatbuffers.Builder, OUTER_BOUNDARYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, OUTER_BOUNDARYOffset, 0);
}

static addInnerBoundaries(builder:flatbuffers.Builder, INNER_BOUNDARIESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, INNER_BOUNDARIESOffset, 0);
}

static createInnerBoundariesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startInnerBoundariesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addAltitudeMode(builder:flatbuffers.Builder, ALTITUDE_MODE:KMLAltitudeMode) {
  builder.addFieldInt8(2, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
}

static addExtrude(builder:flatbuffers.Builder, EXTRUDE:boolean) {
  builder.addFieldInt8(3, +EXTRUDE, +false);
}

static addTessellate(builder:flatbuffers.Builder, TESSELLATE:boolean) {
  builder.addFieldInt8(4, +TESSELLATE, +false);
}

static endKMLPolygon(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLPolygon(builder:flatbuffers.Builder, OUTER_BOUNDARYOffset:flatbuffers.Offset, INNER_BOUNDARIESOffset:flatbuffers.Offset, ALTITUDE_MODE:KMLAltitudeMode, EXTRUDE:boolean, TESSELLATE:boolean):flatbuffers.Offset {
  KMLPolygon.startKMLPolygon(builder);
  KMLPolygon.addOuterBoundary(builder, OUTER_BOUNDARYOffset);
  KMLPolygon.addInnerBoundaries(builder, INNER_BOUNDARIESOffset);
  KMLPolygon.addAltitudeMode(builder, ALTITUDE_MODE);
  KMLPolygon.addExtrude(builder, EXTRUDE);
  KMLPolygon.addTessellate(builder, TESSELLATE);
  return KMLPolygon.endKMLPolygon(builder);
}

unpack(): KMLPolygonT {
  return new KMLPolygonT(
    (this.OUTER_BOUNDARY() !== null ? this.OUTER_BOUNDARY()!.unpack() : null),
    this.bb!.createObjList<KMLLinearRing, KMLLinearRingT>(this.INNER_BOUNDARIES.bind(this), this.innerBoundariesLength()),
    this.ALTITUDE_MODE(),
    this.EXTRUDE(),
    this.TESSELLATE()
  );
}


unpackTo(_o: KMLPolygonT): void {
  _o.OUTER_BOUNDARY = (this.OUTER_BOUNDARY() !== null ? this.OUTER_BOUNDARY()!.unpack() : null);
  _o.INNER_BOUNDARIES = this.bb!.createObjList<KMLLinearRing, KMLLinearRingT>(this.INNER_BOUNDARIES.bind(this), this.innerBoundariesLength());
  _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
  _o.EXTRUDE = this.EXTRUDE();
  _o.TESSELLATE = this.TESSELLATE();
}
}

export class KMLPolygonT implements flatbuffers.IGeneratedObject {
constructor(
  public OUTER_BOUNDARY: KMLLinearRingT|null = null,
  public INNER_BOUNDARIES: (KMLLinearRingT)[] = [],
  public ALTITUDE_MODE: KMLAltitudeMode = KMLAltitudeMode.CLAMP_TO_GROUND,
  public EXTRUDE: boolean = false,
  public TESSELLATE: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const OUTER_BOUNDARY = (this.OUTER_BOUNDARY !== null ? this.OUTER_BOUNDARY!.pack(builder) : 0);
  const INNER_BOUNDARIES = KMLPolygon.createInnerBoundariesVector(builder, builder.createObjectOffsetList(this.INNER_BOUNDARIES));

  return KMLPolygon.createKMLPolygon(builder,
    OUTER_BOUNDARY,
    INNER_BOUNDARIES,
    this.ALTITUDE_MODE,
    this.EXTRUDE,
    this.TESSELLATE
  );
}
}
