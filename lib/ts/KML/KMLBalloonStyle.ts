// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLDisplayMode } from './KMLDisplayMode.js';


/**
 * Balloon style
 */
export class KMLBalloonStyle implements flatbuffers.IUnpackableObject<KMLBalloonStyleT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLBalloonStyle {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLBalloonStyle(bb:flatbuffers.ByteBuffer, obj?:KMLBalloonStyle):KMLBalloonStyle {
  return (obj || new KMLBalloonStyle()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLBalloonStyle(bb:flatbuffers.ByteBuffer, obj?:KMLBalloonStyle):KMLBalloonStyle {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLBalloonStyle()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Background color in aabbggrr hex format
 */
BG_COLOR():string|null
BG_COLOR(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BG_COLOR(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Text color in aabbggrr hex format
 */
TEXT_COLOR():string|null
TEXT_COLOR(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TEXT_COLOR(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Balloon text template (supports $[name], $[description])
 */
TEXT():string|null
TEXT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TEXT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Display mode
 */
DISPLAY_MODE():KMLDisplayMode {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLDisplayMode.DEFAULT;
}

static startKMLBalloonStyle(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addBgColor(builder:flatbuffers.Builder, BG_COLOROffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, BG_COLOROffset, 0);
}

static addTextColor(builder:flatbuffers.Builder, TEXT_COLOROffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, TEXT_COLOROffset, 0);
}

static addText(builder:flatbuffers.Builder, TEXTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, TEXTOffset, 0);
}

static addDisplayMode(builder:flatbuffers.Builder, DISPLAY_MODE:KMLDisplayMode) {
  builder.addFieldInt8(3, DISPLAY_MODE, KMLDisplayMode.DEFAULT);
}

static endKMLBalloonStyle(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLBalloonStyle(builder:flatbuffers.Builder, BG_COLOROffset:flatbuffers.Offset, TEXT_COLOROffset:flatbuffers.Offset, TEXTOffset:flatbuffers.Offset, DISPLAY_MODE:KMLDisplayMode):flatbuffers.Offset {
  KMLBalloonStyle.startKMLBalloonStyle(builder);
  KMLBalloonStyle.addBgColor(builder, BG_COLOROffset);
  KMLBalloonStyle.addTextColor(builder, TEXT_COLOROffset);
  KMLBalloonStyle.addText(builder, TEXTOffset);
  KMLBalloonStyle.addDisplayMode(builder, DISPLAY_MODE);
  return KMLBalloonStyle.endKMLBalloonStyle(builder);
}

unpack(): KMLBalloonStyleT {
  return new KMLBalloonStyleT(
    this.BG_COLOR(),
    this.TEXT_COLOR(),
    this.TEXT(),
    this.DISPLAY_MODE()
  );
}


unpackTo(_o: KMLBalloonStyleT): void {
  _o.BG_COLOR = this.BG_COLOR();
  _o.TEXT_COLOR = this.TEXT_COLOR();
  _o.TEXT = this.TEXT();
  _o.DISPLAY_MODE = this.DISPLAY_MODE();
}
}

export class KMLBalloonStyleT implements flatbuffers.IGeneratedObject {
constructor(
  public BG_COLOR: string|Uint8Array|null = null,
  public TEXT_COLOR: string|Uint8Array|null = null,
  public TEXT: string|Uint8Array|null = null,
  public DISPLAY_MODE: KMLDisplayMode = KMLDisplayMode.DEFAULT
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const BG_COLOR = (this.BG_COLOR !== null ? builder.createString(this.BG_COLOR!) : 0);
  const TEXT_COLOR = (this.TEXT_COLOR !== null ? builder.createString(this.TEXT_COLOR!) : 0);
  const TEXT = (this.TEXT !== null ? builder.createString(this.TEXT!) : 0);

  return KMLBalloonStyle.createKMLBalloonStyle(builder,
    BG_COLOR,
    TEXT_COLOR,
    TEXT,
    this.DISPLAY_MODE
  );
}
}
