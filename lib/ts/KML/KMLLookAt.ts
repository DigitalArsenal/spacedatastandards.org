// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLAltitudeMode } from './KMLAltitudeMode.js';


/**
 * LookAt viewpoint
 */
export class KMLLookAt implements flatbuffers.IUnpackableObject<KMLLookAtT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLLookAt {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLLookAt(bb:flatbuffers.ByteBuffer, obj?:KMLLookAt):KMLLookAt {
  return (obj || new KMLLookAt()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLLookAt(bb:flatbuffers.ByteBuffer, obj?:KMLLookAt):KMLLookAt {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLLookAt()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Longitude of the point being looked at
 */
LONGITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Latitude of the point being looked at
 */
LATITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Altitude of the point being looked at
 */
ALTITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Heading in degrees (0=North)
 */
HEADING():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Tilt in degrees from vertical (0=straight down)
 */
TILT():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Range in meters from the point
 */
RANGE():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Altitude mode
 */
ALTITUDE_MODE():KMLAltitudeMode {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
}

static startKMLLookAt(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addLongitude(builder:flatbuffers.Builder, LONGITUDE:number) {
  builder.addFieldFloat64(0, LONGITUDE, 0.0);
}

static addLatitude(builder:flatbuffers.Builder, LATITUDE:number) {
  builder.addFieldFloat64(1, LATITUDE, 0.0);
}

static addAltitude(builder:flatbuffers.Builder, ALTITUDE:number) {
  builder.addFieldFloat64(2, ALTITUDE, 0.0);
}

static addHeading(builder:flatbuffers.Builder, HEADING:number) {
  builder.addFieldFloat64(3, HEADING, 0.0);
}

static addTilt(builder:flatbuffers.Builder, TILT:number) {
  builder.addFieldFloat64(4, TILT, 0.0);
}

static addRange(builder:flatbuffers.Builder, RANGE:number) {
  builder.addFieldFloat64(5, RANGE, 0.0);
}

static addAltitudeMode(builder:flatbuffers.Builder, ALTITUDE_MODE:KMLAltitudeMode) {
  builder.addFieldInt8(6, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
}

static endKMLLookAt(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLLookAt(builder:flatbuffers.Builder, LONGITUDE:number, LATITUDE:number, ALTITUDE:number, HEADING:number, TILT:number, RANGE:number, ALTITUDE_MODE:KMLAltitudeMode):flatbuffers.Offset {
  KMLLookAt.startKMLLookAt(builder);
  KMLLookAt.addLongitude(builder, LONGITUDE);
  KMLLookAt.addLatitude(builder, LATITUDE);
  KMLLookAt.addAltitude(builder, ALTITUDE);
  KMLLookAt.addHeading(builder, HEADING);
  KMLLookAt.addTilt(builder, TILT);
  KMLLookAt.addRange(builder, RANGE);
  KMLLookAt.addAltitudeMode(builder, ALTITUDE_MODE);
  return KMLLookAt.endKMLLookAt(builder);
}

unpack(): KMLLookAtT {
  return new KMLLookAtT(
    this.LONGITUDE(),
    this.LATITUDE(),
    this.ALTITUDE(),
    this.HEADING(),
    this.TILT(),
    this.RANGE(),
    this.ALTITUDE_MODE()
  );
}


unpackTo(_o: KMLLookAtT): void {
  _o.LONGITUDE = this.LONGITUDE();
  _o.LATITUDE = this.LATITUDE();
  _o.ALTITUDE = this.ALTITUDE();
  _o.HEADING = this.HEADING();
  _o.TILT = this.TILT();
  _o.RANGE = this.RANGE();
  _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
}
}

export class KMLLookAtT implements flatbuffers.IGeneratedObject {
constructor(
  public LONGITUDE: number = 0.0,
  public LATITUDE: number = 0.0,
  public ALTITUDE: number = 0.0,
  public HEADING: number = 0.0,
  public TILT: number = 0.0,
  public RANGE: number = 0.0,
  public ALTITUDE_MODE: KMLAltitudeMode = KMLAltitudeMode.CLAMP_TO_GROUND
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return KMLLookAt.createKMLLookAt(builder,
    this.LONGITUDE,
    this.LATITUDE,
    this.ALTITUDE,
    this.HEADING,
    this.TILT,
    this.RANGE,
    this.ALTITUDE_MODE
  );
}
}
