// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SplitRecipient, SplitRecipientT } from './SplitRecipient.js';
import { memoBindingScheme } from './memoBindingScheme.js';
import { settlementMechanism } from './settlementMechanism.js';


/**
 * Chain Settlement / Smart-Contract Descriptor. The forward description of the
 * exact established splitter contract/program a buyer's wallet targets so
 * payment and platform commission settle atomically on-chain. Referenced by
 * `$CMT.CONTRACTS` and echoed into a store `checkout` quote. Commission is
 * realized by the splitter contract itself when the buyer pays it; there is no
 * off-chain or manual settlement step.
 */
export class SCX implements flatbuffers.IUnpackableObject<SCXT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCX {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCX(bb:flatbuffers.ByteBuffer, obj?:SCX):SCX {
  return (obj || new SCX()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCX(bb:flatbuffers.ByteBuffer, obj?:SCX):SCX {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCX()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$SCX');
}

/**
 * Settlement chain identifier, e.g. "ethereum", "solana", "bitcoin"
 * (matches `$PUR.PAYMENT_CHAIN`)
 */
CHAIN():string
CHAIN(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
CHAIN(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * EVM chainId; 0 for non-EVM chains
 */
CHAIN_ID():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * On-chain settlement mechanism
 */
SETTLEMENT_KIND():settlementMechanism {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : settlementMechanism.NativeTransfer;
}

/**
 * Established split standard id, e.g. "0xsplits.v2" (ETH) or the SOL
 * splitter program name. An audited off-the-shelf standard, not a custom
 * contract.
 */
SPLITTER_STANDARD():string|null
SPLITTER_STANDARD(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SPLITTER_STANDARD(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Deployed splitter contract address (ETH) or splitter program id (SOL) the
 * buyer's payment routes into
 */
SPLITTER_ADDRESS():string|null
SPLITTER_ADDRESS(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SPLITTER_ADDRESS(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The specific split configuration (recipient set + basis points) registered
 * with the splitter
 */
SPLIT_CONFIG_ID():string|null
SPLIT_CONFIG_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SPLIT_CONFIG_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * ERC-20 / SPL mint address for stablecoin settlement; empty = native asset
 */
TOKEN_ADDRESS():string|null
TOKEN_ADDRESS(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TOKEN_ADDRESS(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Token symbol, e.g. "USDC"
 */
TOKEN_SYMBOL():string|null
TOKEN_SYMBOL(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TOKEN_SYMBOL(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Token decimals, e.g. 6 for USDC
 */
TOKEN_DECIMALS():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * The recipient set the split configuration encodes (platform + publisher…),
 * mirrored here for independent verification
 */
SPLIT_RECIPIENTS(index: number, obj?:SplitRecipient):SplitRecipient|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new SplitRecipient()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

splitRecipientsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Confirmations required before the payment is accepted
 */
MIN_CONFIRMATIONS():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * How the order reference is bound on-chain for replay-safe matching
 */
MEMO_SCHEME():memoBindingScheme {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : memoBindingScheme.NoMemo;
}

static startSCX(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addChain(builder:flatbuffers.Builder, CHAINOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, CHAINOffset, 0);
}

static addChainId(builder:flatbuffers.Builder, CHAIN_ID:bigint) {
  builder.addFieldInt64(1, CHAIN_ID, BigInt('0'));
}

static addSettlementKind(builder:flatbuffers.Builder, SETTLEMENT_KIND:settlementMechanism) {
  builder.addFieldInt8(2, SETTLEMENT_KIND, settlementMechanism.NativeTransfer);
}

static addSplitterStandard(builder:flatbuffers.Builder, SPLITTER_STANDARDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, SPLITTER_STANDARDOffset, 0);
}

static addSplitterAddress(builder:flatbuffers.Builder, SPLITTER_ADDRESSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, SPLITTER_ADDRESSOffset, 0);
}

static addSplitConfigId(builder:flatbuffers.Builder, SPLIT_CONFIG_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, SPLIT_CONFIG_IDOffset, 0);
}

static addTokenAddress(builder:flatbuffers.Builder, TOKEN_ADDRESSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, TOKEN_ADDRESSOffset, 0);
}

static addTokenSymbol(builder:flatbuffers.Builder, TOKEN_SYMBOLOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, TOKEN_SYMBOLOffset, 0);
}

static addTokenDecimals(builder:flatbuffers.Builder, TOKEN_DECIMALS:number) {
  builder.addFieldInt8(8, TOKEN_DECIMALS, 0);
}

static addSplitRecipients(builder:flatbuffers.Builder, SPLIT_RECIPIENTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, SPLIT_RECIPIENTSOffset, 0);
}

static createSplitRecipientsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSplitRecipientsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMinConfirmations(builder:flatbuffers.Builder, MIN_CONFIRMATIONS:number) {
  builder.addFieldInt32(10, MIN_CONFIRMATIONS, 0);
}

static addMemoScheme(builder:flatbuffers.Builder, MEMO_SCHEME:memoBindingScheme) {
  builder.addFieldInt8(11, MEMO_SCHEME, memoBindingScheme.NoMemo);
}

static endSCX(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // CHAIN
  return offset;
}

static finishSCXBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SCX');
}

static finishSizePrefixedSCXBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SCX', true);
}

static createSCX(builder:flatbuffers.Builder, CHAINOffset:flatbuffers.Offset, CHAIN_ID:bigint, SETTLEMENT_KIND:settlementMechanism, SPLITTER_STANDARDOffset:flatbuffers.Offset, SPLITTER_ADDRESSOffset:flatbuffers.Offset, SPLIT_CONFIG_IDOffset:flatbuffers.Offset, TOKEN_ADDRESSOffset:flatbuffers.Offset, TOKEN_SYMBOLOffset:flatbuffers.Offset, TOKEN_DECIMALS:number, SPLIT_RECIPIENTSOffset:flatbuffers.Offset, MIN_CONFIRMATIONS:number, MEMO_SCHEME:memoBindingScheme):flatbuffers.Offset {
  SCX.startSCX(builder);
  SCX.addChain(builder, CHAINOffset);
  SCX.addChainId(builder, CHAIN_ID);
  SCX.addSettlementKind(builder, SETTLEMENT_KIND);
  SCX.addSplitterStandard(builder, SPLITTER_STANDARDOffset);
  SCX.addSplitterAddress(builder, SPLITTER_ADDRESSOffset);
  SCX.addSplitConfigId(builder, SPLIT_CONFIG_IDOffset);
  SCX.addTokenAddress(builder, TOKEN_ADDRESSOffset);
  SCX.addTokenSymbol(builder, TOKEN_SYMBOLOffset);
  SCX.addTokenDecimals(builder, TOKEN_DECIMALS);
  SCX.addSplitRecipients(builder, SPLIT_RECIPIENTSOffset);
  SCX.addMinConfirmations(builder, MIN_CONFIRMATIONS);
  SCX.addMemoScheme(builder, MEMO_SCHEME);
  return SCX.endSCX(builder);
}

unpack(): SCXT {
  return new SCXT(
    this.CHAIN(),
    this.CHAIN_ID(),
    this.SETTLEMENT_KIND(),
    this.SPLITTER_STANDARD(),
    this.SPLITTER_ADDRESS(),
    this.SPLIT_CONFIG_ID(),
    this.TOKEN_ADDRESS(),
    this.TOKEN_SYMBOL(),
    this.TOKEN_DECIMALS(),
    this.bb!.createObjList<SplitRecipient, SplitRecipientT>(this.SPLIT_RECIPIENTS.bind(this), this.splitRecipientsLength()),
    this.MIN_CONFIRMATIONS(),
    this.MEMO_SCHEME()
  );
}


unpackTo(_o: SCXT): void {
  _o.CHAIN = this.CHAIN();
  _o.CHAIN_ID = this.CHAIN_ID();
  _o.SETTLEMENT_KIND = this.SETTLEMENT_KIND();
  _o.SPLITTER_STANDARD = this.SPLITTER_STANDARD();
  _o.SPLITTER_ADDRESS = this.SPLITTER_ADDRESS();
  _o.SPLIT_CONFIG_ID = this.SPLIT_CONFIG_ID();
  _o.TOKEN_ADDRESS = this.TOKEN_ADDRESS();
  _o.TOKEN_SYMBOL = this.TOKEN_SYMBOL();
  _o.TOKEN_DECIMALS = this.TOKEN_DECIMALS();
  _o.SPLIT_RECIPIENTS = this.bb!.createObjList<SplitRecipient, SplitRecipientT>(this.SPLIT_RECIPIENTS.bind(this), this.splitRecipientsLength());
  _o.MIN_CONFIRMATIONS = this.MIN_CONFIRMATIONS();
  _o.MEMO_SCHEME = this.MEMO_SCHEME();
}
}

export class SCXT implements flatbuffers.IGeneratedObject {
constructor(
  public CHAIN: string|Uint8Array|null = null,
  public CHAIN_ID: bigint = BigInt('0'),
  public SETTLEMENT_KIND: settlementMechanism = settlementMechanism.NativeTransfer,
  public SPLITTER_STANDARD: string|Uint8Array|null = null,
  public SPLITTER_ADDRESS: string|Uint8Array|null = null,
  public SPLIT_CONFIG_ID: string|Uint8Array|null = null,
  public TOKEN_ADDRESS: string|Uint8Array|null = null,
  public TOKEN_SYMBOL: string|Uint8Array|null = null,
  public TOKEN_DECIMALS: number = 0,
  public SPLIT_RECIPIENTS: (SplitRecipientT)[] = [],
  public MIN_CONFIRMATIONS: number = 0,
  public MEMO_SCHEME: memoBindingScheme = memoBindingScheme.NoMemo
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CHAIN = (this.CHAIN !== null ? builder.createString(this.CHAIN!) : 0);
  const SPLITTER_STANDARD = (this.SPLITTER_STANDARD !== null ? builder.createString(this.SPLITTER_STANDARD!) : 0);
  const SPLITTER_ADDRESS = (this.SPLITTER_ADDRESS !== null ? builder.createString(this.SPLITTER_ADDRESS!) : 0);
  const SPLIT_CONFIG_ID = (this.SPLIT_CONFIG_ID !== null ? builder.createString(this.SPLIT_CONFIG_ID!) : 0);
  const TOKEN_ADDRESS = (this.TOKEN_ADDRESS !== null ? builder.createString(this.TOKEN_ADDRESS!) : 0);
  const TOKEN_SYMBOL = (this.TOKEN_SYMBOL !== null ? builder.createString(this.TOKEN_SYMBOL!) : 0);
  const SPLIT_RECIPIENTS = SCX.createSplitRecipientsVector(builder, builder.createObjectOffsetList(this.SPLIT_RECIPIENTS));

  return SCX.createSCX(builder,
    CHAIN,
    this.CHAIN_ID,
    this.SETTLEMENT_KIND,
    SPLITTER_STANDARD,
    SPLITTER_ADDRESS,
    SPLIT_CONFIG_ID,
    TOKEN_ADDRESS,
    TOKEN_SYMBOL,
    this.TOKEN_DECIMALS,
    SPLIT_RECIPIENTS,
    this.MIN_CONFIRMATIONS,
    this.MEMO_SCHEME
  );
}
}
