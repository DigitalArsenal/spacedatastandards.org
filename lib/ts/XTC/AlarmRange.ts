// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Numeric alarm range
 */
export class AlarmRange implements flatbuffers.IUnpackableObject<AlarmRangeT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AlarmRange {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAlarmRange(bb:flatbuffers.ByteBuffer, obj?:AlarmRange):AlarmRange {
  return (obj || new AlarmRange()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAlarmRange(bb:flatbuffers.ByteBuffer, obj?:AlarmRange):AlarmRange {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new AlarmRange()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Minimum value (inclusive)
 */
MIN_INCLUSIVE():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Maximum value (inclusive)
 */
MAX_INCLUSIVE():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Minimum value (exclusive)
 */
MIN_EXCLUSIVE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Maximum value (exclusive)
 */
MAX_EXCLUSIVE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startAlarmRange(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addMinInclusive(builder:flatbuffers.Builder, MIN_INCLUSIVE:number) {
  builder.addFieldFloat64(0, MIN_INCLUSIVE, 0.0);
}

static addMaxInclusive(builder:flatbuffers.Builder, MAX_INCLUSIVE:number) {
  builder.addFieldFloat64(1, MAX_INCLUSIVE, 0.0);
}

static addMinExclusive(builder:flatbuffers.Builder, MIN_EXCLUSIVE:number) {
  builder.addFieldFloat64(2, MIN_EXCLUSIVE, 0.0);
}

static addMaxExclusive(builder:flatbuffers.Builder, MAX_EXCLUSIVE:number) {
  builder.addFieldFloat64(3, MAX_EXCLUSIVE, 0.0);
}

static endAlarmRange(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createAlarmRange(builder:flatbuffers.Builder, MIN_INCLUSIVE:number, MAX_INCLUSIVE:number, MIN_EXCLUSIVE:number, MAX_EXCLUSIVE:number):flatbuffers.Offset {
  AlarmRange.startAlarmRange(builder);
  AlarmRange.addMinInclusive(builder, MIN_INCLUSIVE);
  AlarmRange.addMaxInclusive(builder, MAX_INCLUSIVE);
  AlarmRange.addMinExclusive(builder, MIN_EXCLUSIVE);
  AlarmRange.addMaxExclusive(builder, MAX_EXCLUSIVE);
  return AlarmRange.endAlarmRange(builder);
}

unpack(): AlarmRangeT {
  return new AlarmRangeT(
    this.MIN_INCLUSIVE(),
    this.MAX_INCLUSIVE(),
    this.MIN_EXCLUSIVE(),
    this.MAX_EXCLUSIVE()
  );
}


unpackTo(_o: AlarmRangeT): void {
  _o.MIN_INCLUSIVE = this.MIN_INCLUSIVE();
  _o.MAX_INCLUSIVE = this.MAX_INCLUSIVE();
  _o.MIN_EXCLUSIVE = this.MIN_EXCLUSIVE();
  _o.MAX_EXCLUSIVE = this.MAX_EXCLUSIVE();
}
}

export class AlarmRangeT implements flatbuffers.IGeneratedObject {
constructor(
  public MIN_INCLUSIVE: number = 0.0,
  public MAX_INCLUSIVE: number = 0.0,
  public MIN_EXCLUSIVE: number = 0.0,
  public MAX_EXCLUSIVE: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return AlarmRange.createAlarmRange(builder,
    this.MIN_INCLUSIVE,
    this.MAX_INCLUSIVE,
    this.MIN_EXCLUSIVE,
    this.MAX_EXCLUSIVE
  );
}
}
