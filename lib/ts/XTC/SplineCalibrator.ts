// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SplinePoint, SplinePointT } from './SplinePoint.js';


/**
 * Spline calibrator - piecewise linear interpolation
 */
export class SplineCalibrator implements flatbuffers.IUnpackableObject<SplineCalibratorT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SplineCalibrator {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSplineCalibrator(bb:flatbuffers.ByteBuffer, obj?:SplineCalibrator):SplineCalibrator {
  return (obj || new SplineCalibrator()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSplineCalibrator(bb:flatbuffers.ByteBuffer, obj?:SplineCalibrator):SplineCalibrator {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SplineCalibrator()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Spline points ordered by raw value
 */
POINTS(index: number, obj?:SplinePoint):SplinePoint|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new SplinePoint()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

pointsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Extrapolate below minimum point
 */
EXTRAPOLATE_LOW():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Extrapolate above maximum point
 */
EXTRAPOLATE_HIGH():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startSplineCalibrator(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addPoints(builder:flatbuffers.Builder, POINTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, POINTSOffset, 0);
}

static createPointsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPointsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addExtrapolateLow(builder:flatbuffers.Builder, EXTRAPOLATE_LOW:boolean) {
  builder.addFieldInt8(1, +EXTRAPOLATE_LOW, +false);
}

static addExtrapolateHigh(builder:flatbuffers.Builder, EXTRAPOLATE_HIGH:boolean) {
  builder.addFieldInt8(2, +EXTRAPOLATE_HIGH, +false);
}

static endSplineCalibrator(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSplineCalibrator(builder:flatbuffers.Builder, POINTSOffset:flatbuffers.Offset, EXTRAPOLATE_LOW:boolean, EXTRAPOLATE_HIGH:boolean):flatbuffers.Offset {
  SplineCalibrator.startSplineCalibrator(builder);
  SplineCalibrator.addPoints(builder, POINTSOffset);
  SplineCalibrator.addExtrapolateLow(builder, EXTRAPOLATE_LOW);
  SplineCalibrator.addExtrapolateHigh(builder, EXTRAPOLATE_HIGH);
  return SplineCalibrator.endSplineCalibrator(builder);
}

unpack(): SplineCalibratorT {
  return new SplineCalibratorT(
    this.bb!.createObjList<SplinePoint, SplinePointT>(this.POINTS.bind(this), this.pointsLength()),
    this.EXTRAPOLATE_LOW(),
    this.EXTRAPOLATE_HIGH()
  );
}


unpackTo(_o: SplineCalibratorT): void {
  _o.POINTS = this.bb!.createObjList<SplinePoint, SplinePointT>(this.POINTS.bind(this), this.pointsLength());
  _o.EXTRAPOLATE_LOW = this.EXTRAPOLATE_LOW();
  _o.EXTRAPOLATE_HIGH = this.EXTRAPOLATE_HIGH();
}
}

export class SplineCalibratorT implements flatbuffers.IGeneratedObject {
constructor(
  public POINTS: (SplinePointT)[] = [],
  public EXTRAPOLATE_LOW: boolean = false,
  public EXTRAPOLATE_HIGH: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const POINTS = SplineCalibrator.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));

  return SplineCalibrator.createSplineCalibrator(builder,
    POINTS,
    this.EXTRAPOLATE_LOW,
    this.EXTRAPOLATE_HIGH
  );
}
}
