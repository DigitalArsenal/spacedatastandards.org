// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { keyMaterialAlgorithm } from './keyMaterialAlgorithm.js';
import { keyMaterialEncoding } from './keyMaterialEncoding.js';
import { keyMaterialRole } from './keyMaterialRole.js';


/**
 * Key Material Frame
 */
export class KMF implements flatbuffers.IUnpackableObject<KMFT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMF {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMF(bb:flatbuffers.ByteBuffer, obj?:KMF):KMF {
  return (obj || new KMF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMF(bb:flatbuffers.ByteBuffer, obj?:KMF):KMF {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$KMF');
}

/**
 * Logical key identifier used across publication and grant records.
 */
KEY_ID():string|null
KEY_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
KEY_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Role the key bytes fulfill for the module flow.
 */
ROLE():keyMaterialRole {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : keyMaterialRole.Unknown;
}

/**
 * Algorithm or key family for the key bytes.
 */
ALGORITHM():keyMaterialAlgorithm {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : keyMaterialAlgorithm.Unknown;
}

/**
 * Encoding of KEY_BYTES.
 */
ENCODING():keyMaterialEncoding {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : keyMaterialEncoding.Unknown;
}

/**
 * Explicit key bytes when a module must receive them on a port.
 * This may be field-encrypted using the SDS/da-flatbuffers header-first
 * encryption flow when transported to a specific recipient.
 */
KEY_BYTES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

keyBytesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

keyBytesArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Logical version of the key material.
 */
VERSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Expiration time in unix milliseconds, or 0 if unbounded.
 */
EXPIRES_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startKMF(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addKeyId(builder:flatbuffers.Builder, KEY_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, KEY_IDOffset, 0);
}

static addRole(builder:flatbuffers.Builder, ROLE:keyMaterialRole) {
  builder.addFieldInt8(1, ROLE, keyMaterialRole.Unknown);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHM:keyMaterialAlgorithm) {
  builder.addFieldInt8(2, ALGORITHM, keyMaterialAlgorithm.Unknown);
}

static addEncoding(builder:flatbuffers.Builder, ENCODING:keyMaterialEncoding) {
  builder.addFieldInt8(3, ENCODING, keyMaterialEncoding.Unknown);
}

static addKeyBytes(builder:flatbuffers.Builder, KEY_BYTESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, KEY_BYTESOffset, 0);
}

static createKeyBytesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startKeyBytesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addVersion(builder:flatbuffers.Builder, VERSION:number) {
  builder.addFieldInt32(5, VERSION, 0);
}

static addExpiresAt(builder:flatbuffers.Builder, EXPIRES_AT:bigint) {
  builder.addFieldInt64(6, EXPIRES_AT, BigInt('0'));
}

static endKMF(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishKMFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$KMF');
}

static finishSizePrefixedKMFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$KMF', true);
}

static createKMF(builder:flatbuffers.Builder, KEY_IDOffset:flatbuffers.Offset, ROLE:keyMaterialRole, ALGORITHM:keyMaterialAlgorithm, ENCODING:keyMaterialEncoding, KEY_BYTESOffset:flatbuffers.Offset, VERSION:number, EXPIRES_AT:bigint):flatbuffers.Offset {
  KMF.startKMF(builder);
  KMF.addKeyId(builder, KEY_IDOffset);
  KMF.addRole(builder, ROLE);
  KMF.addAlgorithm(builder, ALGORITHM);
  KMF.addEncoding(builder, ENCODING);
  KMF.addKeyBytes(builder, KEY_BYTESOffset);
  KMF.addVersion(builder, VERSION);
  KMF.addExpiresAt(builder, EXPIRES_AT);
  return KMF.endKMF(builder);
}

unpack(): KMFT {
  return new KMFT(
    this.KEY_ID(),
    this.ROLE(),
    this.ALGORITHM(),
    this.ENCODING(),
    this.bb!.createScalarList<number>(this.KEY_BYTES.bind(this), this.keyBytesLength()),
    this.VERSION(),
    this.EXPIRES_AT()
  );
}


unpackTo(_o: KMFT): void {
  _o.KEY_ID = this.KEY_ID();
  _o.ROLE = this.ROLE();
  _o.ALGORITHM = this.ALGORITHM();
  _o.ENCODING = this.ENCODING();
  _o.KEY_BYTES = this.bb!.createScalarList<number>(this.KEY_BYTES.bind(this), this.keyBytesLength());
  _o.VERSION = this.VERSION();
  _o.EXPIRES_AT = this.EXPIRES_AT();
}
}

export class KMFT implements flatbuffers.IGeneratedObject {
constructor(
  public KEY_ID: string|Uint8Array|null = null,
  public ROLE: keyMaterialRole = keyMaterialRole.Unknown,
  public ALGORITHM: keyMaterialAlgorithm = keyMaterialAlgorithm.Unknown,
  public ENCODING: keyMaterialEncoding = keyMaterialEncoding.Unknown,
  public KEY_BYTES: (number)[] = [],
  public VERSION: number = 0,
  public EXPIRES_AT: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const KEY_ID = (this.KEY_ID !== null ? builder.createString(this.KEY_ID!) : 0);
  const KEY_BYTES = KMF.createKeyBytesVector(builder, this.KEY_BYTES);

  return KMF.createKMF(builder,
    KEY_ID,
    this.ROLE,
    this.ALGORITHM,
    this.ENCODING,
    KEY_BYTES,
    this.VERSION,
    this.EXPIRES_AT
  );
}
}
