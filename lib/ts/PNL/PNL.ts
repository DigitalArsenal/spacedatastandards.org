// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { PNLPanel, PNLPanelT } from './PNLPanel.js';


/**
 * Panelled (box-wing) Spacecraft Macro Model — the surface geometry and
 * optical properties needed to compute non-conservative forces (solar
 * radiation pressure, atmospheric drag, thermal re-radiation) on one object.
 *
 * This is deliberately NOT $VAM, $BUS or $PHY. $VAM ranks visual assets for
 * rendering and a render mesh is not a force model; $BUS/$OOD are descriptive
 * inventory; $PHY is simulation configuration (integration method, force-type
 * toggles). None of them carries per-panel area, normal, and optical
 * coefficients.
 */
export class PNL implements flatbuffers.IUnpackableObject<PNLT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PNL {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPNL(bb:flatbuffers.ByteBuffer, obj?:PNL):PNL {
  return (obj || new PNL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPNL(bb:flatbuffers.ByteBuffer, obj?:PNL):PNL {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PNL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$PNL');
}

/**
 * Object this macro model describes.
 */
OBJECT_ID():string
OBJECT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
OBJECT_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Advisory object name.
 */
OBJECT_NAME():string|null
OBJECT_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
OBJECT_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Satellite catalog number, when the object is catalogued.
 */
NORAD_CAT_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Body-fixed frame the panel normals are expressed in, e.g. "SC_BODY".
 */
BODY_FRAME():string|null
BODY_FRAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BODY_FRAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Total spacecraft mass the model corresponds to, kilograms.
 */
TOTAL_MASS_KG():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Epoch the model is valid at, ISO 8601. Mass and panel configuration
 * change over a mission; a macro model without an epoch is ambiguous.
 */
EPOCH():string|null
EPOCH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EPOCH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The panels.
 */
PANELS(index: number, obj?:PNLPanel):PNLPanel|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new PNLPanel()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

panelsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Reference cross-sectional area used by a consumer that falls back to a
 * cannonball model, square metres.
 */
REFERENCE_AREA_M2():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Provenance of the geometry and the optical coefficients.
 */
SOURCE():string|null
SOURCE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SOURCE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startPNL(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addObjectId(builder:flatbuffers.Builder, OBJECT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, OBJECT_IDOffset, 0);
}

static addObjectName(builder:flatbuffers.Builder, OBJECT_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, OBJECT_NAMEOffset, 0);
}

static addNoradCatId(builder:flatbuffers.Builder, NORAD_CAT_ID:number) {
  builder.addFieldInt32(2, NORAD_CAT_ID, 0);
}

static addBodyFrame(builder:flatbuffers.Builder, BODY_FRAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, BODY_FRAMEOffset, 0);
}

static addTotalMassKg(builder:flatbuffers.Builder, TOTAL_MASS_KG:number) {
  builder.addFieldFloat64(4, TOTAL_MASS_KG, 0.0);
}

static addEpoch(builder:flatbuffers.Builder, EPOCHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, EPOCHOffset, 0);
}

static addPanels(builder:flatbuffers.Builder, PANELSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, PANELSOffset, 0);
}

static createPanelsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPanelsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addReferenceAreaM2(builder:flatbuffers.Builder, REFERENCE_AREA_M2:number) {
  builder.addFieldFloat64(7, REFERENCE_AREA_M2, 0.0);
}

static addSource(builder:flatbuffers.Builder, SOURCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, SOURCEOffset, 0);
}

static endPNL(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // OBJECT_ID
  return offset;
}

static finishPNLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$PNL');
}

static finishSizePrefixedPNLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$PNL', true);
}

static createPNL(builder:flatbuffers.Builder, OBJECT_IDOffset:flatbuffers.Offset, OBJECT_NAMEOffset:flatbuffers.Offset, NORAD_CAT_ID:number, BODY_FRAMEOffset:flatbuffers.Offset, TOTAL_MASS_KG:number, EPOCHOffset:flatbuffers.Offset, PANELSOffset:flatbuffers.Offset, REFERENCE_AREA_M2:number, SOURCEOffset:flatbuffers.Offset):flatbuffers.Offset {
  PNL.startPNL(builder);
  PNL.addObjectId(builder, OBJECT_IDOffset);
  PNL.addObjectName(builder, OBJECT_NAMEOffset);
  PNL.addNoradCatId(builder, NORAD_CAT_ID);
  PNL.addBodyFrame(builder, BODY_FRAMEOffset);
  PNL.addTotalMassKg(builder, TOTAL_MASS_KG);
  PNL.addEpoch(builder, EPOCHOffset);
  PNL.addPanels(builder, PANELSOffset);
  PNL.addReferenceAreaM2(builder, REFERENCE_AREA_M2);
  PNL.addSource(builder, SOURCEOffset);
  return PNL.endPNL(builder);
}

unpack(): PNLT {
  return new PNLT(
    this.OBJECT_ID(),
    this.OBJECT_NAME(),
    this.NORAD_CAT_ID(),
    this.BODY_FRAME(),
    this.TOTAL_MASS_KG(),
    this.EPOCH(),
    this.bb!.createObjList<PNLPanel, PNLPanelT>(this.PANELS.bind(this), this.panelsLength()),
    this.REFERENCE_AREA_M2(),
    this.SOURCE()
  );
}


unpackTo(_o: PNLT): void {
  _o.OBJECT_ID = this.OBJECT_ID();
  _o.OBJECT_NAME = this.OBJECT_NAME();
  _o.NORAD_CAT_ID = this.NORAD_CAT_ID();
  _o.BODY_FRAME = this.BODY_FRAME();
  _o.TOTAL_MASS_KG = this.TOTAL_MASS_KG();
  _o.EPOCH = this.EPOCH();
  _o.PANELS = this.bb!.createObjList<PNLPanel, PNLPanelT>(this.PANELS.bind(this), this.panelsLength());
  _o.REFERENCE_AREA_M2 = this.REFERENCE_AREA_M2();
  _o.SOURCE = this.SOURCE();
}
}

export class PNLT implements flatbuffers.IGeneratedObject {
constructor(
  public OBJECT_ID: string|Uint8Array|null = null,
  public OBJECT_NAME: string|Uint8Array|null = null,
  public NORAD_CAT_ID: number = 0,
  public BODY_FRAME: string|Uint8Array|null = null,
  public TOTAL_MASS_KG: number = 0.0,
  public EPOCH: string|Uint8Array|null = null,
  public PANELS: (PNLPanelT)[] = [],
  public REFERENCE_AREA_M2: number = 0.0,
  public SOURCE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const OBJECT_ID = (this.OBJECT_ID !== null ? builder.createString(this.OBJECT_ID!) : 0);
  const OBJECT_NAME = (this.OBJECT_NAME !== null ? builder.createString(this.OBJECT_NAME!) : 0);
  const BODY_FRAME = (this.BODY_FRAME !== null ? builder.createString(this.BODY_FRAME!) : 0);
  const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH!) : 0);
  const PANELS = PNL.createPanelsVector(builder, builder.createObjectOffsetList(this.PANELS));
  const SOURCE = (this.SOURCE !== null ? builder.createString(this.SOURCE!) : 0);

  return PNL.createPNL(builder,
    OBJECT_ID,
    OBJECT_NAME,
    this.NORAD_CAT_ID,
    BODY_FRAME,
    this.TOTAL_MASS_KG,
    EPOCH,
    PANELS,
    this.REFERENCE_AREA_M2,
    SOURCE
  );
}
}
