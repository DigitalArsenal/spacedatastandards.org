// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { phbBoundKind } from './phbBoundKind.js';
import { phbDerivation } from './phbDerivation.js';
import { phbMagnitudeSystem } from './phbMagnitudeSystem.js';
import { phbQuantity } from './phbQuantity.js';
import { phbSpectralRegime } from './phbSpectralRegime.js';


/**
 * One source's brightness in one band.
 */
export class PHBMeasurement implements flatbuffers.IUnpackableObject<PHBMeasurementT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PHBMeasurement {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPHBMeasurement(bb:flatbuffers.ByteBuffer, obj?:PHBMeasurement):PHBMeasurement {
  return (obj || new PHBMeasurement()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPHBMeasurement(bb:flatbuffers.ByteBuffer, obj?:PHBMeasurement):PHBMeasurement {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PHBMeasurement()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Band identifier; resolves to a `$PHB` BAND_ID.
 */
BAND_ID():string|null
BAND_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BAND_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Coarse region of the band, copied from its descriptor so a consumer
 * can select a mode without resolving the band.
 */
SPECTRAL_REGIME():phbSpectralRegime {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : phbSpectralRegime.UNSPECIFIED;
}

/**
 * Effective wavelength of the band, nanometres (vacuum). Copied from the
 * descriptor; required for synthesis when the descriptor is not at hand.
 */
WAVELENGTH_EFF_NM():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : NaN;
}

/**
 * Quantity VALUE states.
 */
QUANTITY():phbQuantity {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : phbQuantity.MAGNITUDE;
}

/**
 * Calibration system when QUANTITY is MAGNITUDE.
 */
MAGNITUDE_SYSTEM():phbMagnitudeSystem {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : phbMagnitudeSystem.UNSPECIFIED;
}

/**
 * The measurement, in QUANTITY's units.
 */
VALUE():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : NaN;
}

/**
 * One-sigma uncertainty of VALUE, same units.
 */
UNCERTAINTY():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : NaN;
}

/**
 * Detection or bound.
 */
BOUND():phbBoundKind {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : phbBoundKind.DETECTION;
}

/**
 * How the value was obtained.
 */
DERIVATION():phbDerivation {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : phbDerivation.UNSPECIFIED;
}

/**
 * Mid-time of the observation, Modified Julian Date (TT), for sources
 * whose brightness varies. Absent for a catalogue mean.
 */
EPOCH_MJD():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : NaN;
}

/**
 * Number of observations averaged into VALUE; 0 when unstated.
 */
OBSERVATION_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startPHBMeasurement(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addBandId(builder:flatbuffers.Builder, BAND_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, BAND_IDOffset, 0);
}

static addSpectralRegime(builder:flatbuffers.Builder, SPECTRAL_REGIME:phbSpectralRegime) {
  builder.addFieldInt8(1, SPECTRAL_REGIME, phbSpectralRegime.UNSPECIFIED);
}

static addWavelengthEffNm(builder:flatbuffers.Builder, WAVELENGTH_EFF_NM:number) {
  builder.addFieldFloat64(2, WAVELENGTH_EFF_NM, NaN);
}

static addQuantity(builder:flatbuffers.Builder, QUANTITY:phbQuantity) {
  builder.addFieldInt8(3, QUANTITY, phbQuantity.MAGNITUDE);
}

static addMagnitudeSystem(builder:flatbuffers.Builder, MAGNITUDE_SYSTEM:phbMagnitudeSystem) {
  builder.addFieldInt8(4, MAGNITUDE_SYSTEM, phbMagnitudeSystem.UNSPECIFIED);
}

static addValue(builder:flatbuffers.Builder, VALUE:number) {
  builder.addFieldFloat64(5, VALUE, NaN);
}

static addUncertainty(builder:flatbuffers.Builder, UNCERTAINTY:number) {
  builder.addFieldFloat64(6, UNCERTAINTY, NaN);
}

static addBound(builder:flatbuffers.Builder, BOUND:phbBoundKind) {
  builder.addFieldInt8(7, BOUND, phbBoundKind.DETECTION);
}

static addDerivation(builder:flatbuffers.Builder, DERIVATION:phbDerivation) {
  builder.addFieldInt8(8, DERIVATION, phbDerivation.UNSPECIFIED);
}

static addEpochMjd(builder:flatbuffers.Builder, EPOCH_MJD:number) {
  builder.addFieldFloat64(9, EPOCH_MJD, NaN);
}

static addObservationCount(builder:flatbuffers.Builder, OBSERVATION_COUNT:number) {
  builder.addFieldInt32(10, OBSERVATION_COUNT, 0);
}

static endPHBMeasurement(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPHBMeasurement(builder:flatbuffers.Builder, BAND_IDOffset:flatbuffers.Offset, SPECTRAL_REGIME:phbSpectralRegime, WAVELENGTH_EFF_NM:number, QUANTITY:phbQuantity, MAGNITUDE_SYSTEM:phbMagnitudeSystem, VALUE:number, UNCERTAINTY:number, BOUND:phbBoundKind, DERIVATION:phbDerivation, EPOCH_MJD:number, OBSERVATION_COUNT:number):flatbuffers.Offset {
  PHBMeasurement.startPHBMeasurement(builder);
  PHBMeasurement.addBandId(builder, BAND_IDOffset);
  PHBMeasurement.addSpectralRegime(builder, SPECTRAL_REGIME);
  PHBMeasurement.addWavelengthEffNm(builder, WAVELENGTH_EFF_NM);
  PHBMeasurement.addQuantity(builder, QUANTITY);
  PHBMeasurement.addMagnitudeSystem(builder, MAGNITUDE_SYSTEM);
  PHBMeasurement.addValue(builder, VALUE);
  PHBMeasurement.addUncertainty(builder, UNCERTAINTY);
  PHBMeasurement.addBound(builder, BOUND);
  PHBMeasurement.addDerivation(builder, DERIVATION);
  PHBMeasurement.addEpochMjd(builder, EPOCH_MJD);
  PHBMeasurement.addObservationCount(builder, OBSERVATION_COUNT);
  return PHBMeasurement.endPHBMeasurement(builder);
}

unpack(): PHBMeasurementT {
  return new PHBMeasurementT(
    this.BAND_ID(),
    this.SPECTRAL_REGIME(),
    this.WAVELENGTH_EFF_NM(),
    this.QUANTITY(),
    this.MAGNITUDE_SYSTEM(),
    this.VALUE(),
    this.UNCERTAINTY(),
    this.BOUND(),
    this.DERIVATION(),
    this.EPOCH_MJD(),
    this.OBSERVATION_COUNT()
  );
}


unpackTo(_o: PHBMeasurementT): void {
  _o.BAND_ID = this.BAND_ID();
  _o.SPECTRAL_REGIME = this.SPECTRAL_REGIME();
  _o.WAVELENGTH_EFF_NM = this.WAVELENGTH_EFF_NM();
  _o.QUANTITY = this.QUANTITY();
  _o.MAGNITUDE_SYSTEM = this.MAGNITUDE_SYSTEM();
  _o.VALUE = this.VALUE();
  _o.UNCERTAINTY = this.UNCERTAINTY();
  _o.BOUND = this.BOUND();
  _o.DERIVATION = this.DERIVATION();
  _o.EPOCH_MJD = this.EPOCH_MJD();
  _o.OBSERVATION_COUNT = this.OBSERVATION_COUNT();
}
}

export class PHBMeasurementT implements flatbuffers.IGeneratedObject {
constructor(
  public BAND_ID: string|Uint8Array|null = null,
  public SPECTRAL_REGIME: phbSpectralRegime = phbSpectralRegime.UNSPECIFIED,
  public WAVELENGTH_EFF_NM: number = NaN,
  public QUANTITY: phbQuantity = phbQuantity.MAGNITUDE,
  public MAGNITUDE_SYSTEM: phbMagnitudeSystem = phbMagnitudeSystem.UNSPECIFIED,
  public VALUE: number = NaN,
  public UNCERTAINTY: number = NaN,
  public BOUND: phbBoundKind = phbBoundKind.DETECTION,
  public DERIVATION: phbDerivation = phbDerivation.UNSPECIFIED,
  public EPOCH_MJD: number = NaN,
  public OBSERVATION_COUNT: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const BAND_ID = (this.BAND_ID !== null ? builder.createString(this.BAND_ID!) : 0);

  return PHBMeasurement.createPHBMeasurement(builder,
    BAND_ID,
    this.SPECTRAL_REGIME,
    this.WAVELENGTH_EFF_NM,
    this.QUANTITY,
    this.MAGNITUDE_SYSTEM,
    this.VALUE,
    this.UNCERTAINTY,
    this.BOUND,
    this.DERIVATION,
    this.EPOCH_MJD,
    this.OBSERVATION_COUNT
  );
}
}
