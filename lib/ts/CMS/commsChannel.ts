// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { CmsModulationType } from './CmsModulationType.js';
import { encryptionType } from './encryptionType.js';


/**
 * Transponder Channel
 */
export class commsChannel implements flatbuffers.IUnpackableObject<commsChannelT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):commsChannel {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAscommsChannel(bb:flatbuffers.ByteBuffer, obj?:commsChannel):commsChannel {
  return (obj || new commsChannel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAscommsChannel(bb:flatbuffers.ByteBuffer, obj?:commsChannel):commsChannel {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new commsChannel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Channel identifier
 */
CHANNEL_ID():string|null
CHANNEL_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CHANNEL_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Channel name
 */
NAME():string|null
NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Uplink frequency in MHz
 */
UPLINK_FREQ():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Downlink frequency in MHz
 */
DOWNLINK_FREQ():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Channel bandwidth in MHz
 */
BANDWIDTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Modulation type
 */
MODULATION():CmsModulationType {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : CmsModulationType.BPSK;
}

/**
 * Data rate in Mbps
 */
DATA_RATE():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Encryption method
 */
ENCRYPTION():encryptionType {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : encryptionType.NONE;
}

/**
 * Forward error correction coding rate (e.g., 0.5, 0.75)
 */
FEC_RATE():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Channel power in dBW
 */
POWER():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startcommsChannel(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addChannelId(builder:flatbuffers.Builder, CHANNEL_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, CHANNEL_IDOffset, 0);
}

static addName(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, NAMEOffset, 0);
}

static addUplinkFreq(builder:flatbuffers.Builder, UPLINK_FREQ:number) {
  builder.addFieldFloat64(2, UPLINK_FREQ, 0.0);
}

static addDownlinkFreq(builder:flatbuffers.Builder, DOWNLINK_FREQ:number) {
  builder.addFieldFloat64(3, DOWNLINK_FREQ, 0.0);
}

static addBandwidth(builder:flatbuffers.Builder, BANDWIDTH:number) {
  builder.addFieldFloat64(4, BANDWIDTH, 0.0);
}

static addModulation(builder:flatbuffers.Builder, MODULATION:CmsModulationType) {
  builder.addFieldInt8(5, MODULATION, CmsModulationType.BPSK);
}

static addDataRate(builder:flatbuffers.Builder, DATA_RATE:number) {
  builder.addFieldFloat64(6, DATA_RATE, 0.0);
}

static addEncryption(builder:flatbuffers.Builder, ENCRYPTION:encryptionType) {
  builder.addFieldInt8(7, ENCRYPTION, encryptionType.NONE);
}

static addFecRate(builder:flatbuffers.Builder, FEC_RATE:number) {
  builder.addFieldFloat64(8, FEC_RATE, 0.0);
}

static addPower(builder:flatbuffers.Builder, POWER:number) {
  builder.addFieldFloat64(9, POWER, 0.0);
}

static endcommsChannel(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createcommsChannel(builder:flatbuffers.Builder, CHANNEL_IDOffset:flatbuffers.Offset, NAMEOffset:flatbuffers.Offset, UPLINK_FREQ:number, DOWNLINK_FREQ:number, BANDWIDTH:number, MODULATION:CmsModulationType, DATA_RATE:number, ENCRYPTION:encryptionType, FEC_RATE:number, POWER:number):flatbuffers.Offset {
  commsChannel.startcommsChannel(builder);
  commsChannel.addChannelId(builder, CHANNEL_IDOffset);
  commsChannel.addName(builder, NAMEOffset);
  commsChannel.addUplinkFreq(builder, UPLINK_FREQ);
  commsChannel.addDownlinkFreq(builder, DOWNLINK_FREQ);
  commsChannel.addBandwidth(builder, BANDWIDTH);
  commsChannel.addModulation(builder, MODULATION);
  commsChannel.addDataRate(builder, DATA_RATE);
  commsChannel.addEncryption(builder, ENCRYPTION);
  commsChannel.addFecRate(builder, FEC_RATE);
  commsChannel.addPower(builder, POWER);
  return commsChannel.endcommsChannel(builder);
}

unpack(): commsChannelT {
  return new commsChannelT(
    this.CHANNEL_ID(),
    this.NAME(),
    this.UPLINK_FREQ(),
    this.DOWNLINK_FREQ(),
    this.BANDWIDTH(),
    this.MODULATION(),
    this.DATA_RATE(),
    this.ENCRYPTION(),
    this.FEC_RATE(),
    this.POWER()
  );
}


unpackTo(_o: commsChannelT): void {
  _o.CHANNEL_ID = this.CHANNEL_ID();
  _o.NAME = this.NAME();
  _o.UPLINK_FREQ = this.UPLINK_FREQ();
  _o.DOWNLINK_FREQ = this.DOWNLINK_FREQ();
  _o.BANDWIDTH = this.BANDWIDTH();
  _o.MODULATION = this.MODULATION();
  _o.DATA_RATE = this.DATA_RATE();
  _o.ENCRYPTION = this.ENCRYPTION();
  _o.FEC_RATE = this.FEC_RATE();
  _o.POWER = this.POWER();
}
}

export class commsChannelT implements flatbuffers.IGeneratedObject {
constructor(
  public CHANNEL_ID: string|Uint8Array|null = null,
  public NAME: string|Uint8Array|null = null,
  public UPLINK_FREQ: number = 0.0,
  public DOWNLINK_FREQ: number = 0.0,
  public BANDWIDTH: number = 0.0,
  public MODULATION: CmsModulationType = CmsModulationType.BPSK,
  public DATA_RATE: number = 0.0,
  public ENCRYPTION: encryptionType = encryptionType.NONE,
  public FEC_RATE: number = 0.0,
  public POWER: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CHANNEL_ID = (this.CHANNEL_ID !== null ? builder.createString(this.CHANNEL_ID!) : 0);
  const NAME = (this.NAME !== null ? builder.createString(this.NAME!) : 0);

  return commsChannel.createcommsChannel(builder,
    CHANNEL_ID,
    NAME,
    this.UPLINK_FREQ,
    this.DOWNLINK_FREQ,
    this.BANDWIDTH,
    this.MODULATION,
    this.DATA_RATE,
    this.ENCRYPTION,
    this.FEC_RATE,
    this.POWER
  );
}
}
