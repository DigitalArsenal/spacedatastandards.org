// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Atmosphere and Environment
 */
export class ENV implements flatbuffers.IUnpackableObject<ENVT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ENV {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsENV(bb:flatbuffers.ByteBuffer, obj?:ENV):ENV {
  return (obj || new ENV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsENV(bb:flatbuffers.ByteBuffer, obj?:ENV):ENV {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ENV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$ENV');
}

ATMOSPHERE():string|null
ATMOSPHERE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ATMOSPHERE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

WEATHER():string|null
WEATHER(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
WEATHER(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

TIME_UTC():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

LATITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

LONGITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

SUN_AZIMUTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

SUN_ELEVATION():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

MOON_PHASE():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

ILLUMINATION():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

MAGNETIC_DECLINATION():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

MAGNETIC_INCLINATION():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

RESERVED(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

reservedLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

reservedArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startENV(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addAtmosphere(builder:flatbuffers.Builder, ATMOSPHEREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, ATMOSPHEREOffset, 0);
}

static addWeather(builder:flatbuffers.Builder, WEATHEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, WEATHEROffset, 0);
}

static addTimeUtc(builder:flatbuffers.Builder, TIME_UTC:number) {
  builder.addFieldFloat64(2, TIME_UTC, 0.0);
}

static addLatitude(builder:flatbuffers.Builder, LATITUDE:number) {
  builder.addFieldFloat64(3, LATITUDE, 0.0);
}

static addLongitude(builder:flatbuffers.Builder, LONGITUDE:number) {
  builder.addFieldFloat64(4, LONGITUDE, 0.0);
}

static addSunAzimuth(builder:flatbuffers.Builder, SUN_AZIMUTH:number) {
  builder.addFieldFloat32(5, SUN_AZIMUTH, 0.0);
}

static addSunElevation(builder:flatbuffers.Builder, SUN_ELEVATION:number) {
  builder.addFieldFloat32(6, SUN_ELEVATION, 0.0);
}

static addMoonPhase(builder:flatbuffers.Builder, MOON_PHASE:number) {
  builder.addFieldFloat32(7, MOON_PHASE, 0.0);
}

static addIllumination(builder:flatbuffers.Builder, ILLUMINATION:number) {
  builder.addFieldFloat32(8, ILLUMINATION, 0.0);
}

static addMagneticDeclination(builder:flatbuffers.Builder, MAGNETIC_DECLINATION:number) {
  builder.addFieldFloat32(9, MAGNETIC_DECLINATION, 0.0);
}

static addMagneticInclination(builder:flatbuffers.Builder, MAGNETIC_INCLINATION:number) {
  builder.addFieldFloat32(10, MAGNETIC_INCLINATION, 0.0);
}

static addReserved(builder:flatbuffers.Builder, RESERVEDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, RESERVEDOffset, 0);
}

static createReservedVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startReservedVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endENV(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishENVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$ENV');
}

static finishSizePrefixedENVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$ENV', true);
}

static createENV(builder:flatbuffers.Builder, ATMOSPHEREOffset:flatbuffers.Offset, WEATHEROffset:flatbuffers.Offset, TIME_UTC:number, LATITUDE:number, LONGITUDE:number, SUN_AZIMUTH:number, SUN_ELEVATION:number, MOON_PHASE:number, ILLUMINATION:number, MAGNETIC_DECLINATION:number, MAGNETIC_INCLINATION:number, RESERVEDOffset:flatbuffers.Offset):flatbuffers.Offset {
  ENV.startENV(builder);
  ENV.addAtmosphere(builder, ATMOSPHEREOffset);
  ENV.addWeather(builder, WEATHEROffset);
  ENV.addTimeUtc(builder, TIME_UTC);
  ENV.addLatitude(builder, LATITUDE);
  ENV.addLongitude(builder, LONGITUDE);
  ENV.addSunAzimuth(builder, SUN_AZIMUTH);
  ENV.addSunElevation(builder, SUN_ELEVATION);
  ENV.addMoonPhase(builder, MOON_PHASE);
  ENV.addIllumination(builder, ILLUMINATION);
  ENV.addMagneticDeclination(builder, MAGNETIC_DECLINATION);
  ENV.addMagneticInclination(builder, MAGNETIC_INCLINATION);
  ENV.addReserved(builder, RESERVEDOffset);
  return ENV.endENV(builder);
}

unpack(): ENVT {
  return new ENVT(
    this.ATMOSPHERE(),
    this.WEATHER(),
    this.TIME_UTC(),
    this.LATITUDE(),
    this.LONGITUDE(),
    this.SUN_AZIMUTH(),
    this.SUN_ELEVATION(),
    this.MOON_PHASE(),
    this.ILLUMINATION(),
    this.MAGNETIC_DECLINATION(),
    this.MAGNETIC_INCLINATION(),
    this.bb!.createScalarList<number>(this.RESERVED.bind(this), this.reservedLength())
  );
}


unpackTo(_o: ENVT): void {
  _o.ATMOSPHERE = this.ATMOSPHERE();
  _o.WEATHER = this.WEATHER();
  _o.TIME_UTC = this.TIME_UTC();
  _o.LATITUDE = this.LATITUDE();
  _o.LONGITUDE = this.LONGITUDE();
  _o.SUN_AZIMUTH = this.SUN_AZIMUTH();
  _o.SUN_ELEVATION = this.SUN_ELEVATION();
  _o.MOON_PHASE = this.MOON_PHASE();
  _o.ILLUMINATION = this.ILLUMINATION();
  _o.MAGNETIC_DECLINATION = this.MAGNETIC_DECLINATION();
  _o.MAGNETIC_INCLINATION = this.MAGNETIC_INCLINATION();
  _o.RESERVED = this.bb!.createScalarList<number>(this.RESERVED.bind(this), this.reservedLength());
}
}

export class ENVT implements flatbuffers.IGeneratedObject {
constructor(
  public ATMOSPHERE: string|Uint8Array|null = null,
  public WEATHER: string|Uint8Array|null = null,
  public TIME_UTC: number = 0.0,
  public LATITUDE: number = 0.0,
  public LONGITUDE: number = 0.0,
  public SUN_AZIMUTH: number = 0.0,
  public SUN_ELEVATION: number = 0.0,
  public MOON_PHASE: number = 0.0,
  public ILLUMINATION: number = 0.0,
  public MAGNETIC_DECLINATION: number = 0.0,
  public MAGNETIC_INCLINATION: number = 0.0,
  public RESERVED: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ATMOSPHERE = (this.ATMOSPHERE !== null ? builder.createString(this.ATMOSPHERE!) : 0);
  const WEATHER = (this.WEATHER !== null ? builder.createString(this.WEATHER!) : 0);
  const RESERVED = ENV.createReservedVector(builder, this.RESERVED);

  return ENV.createENV(builder,
    ATMOSPHERE,
    WEATHER,
    this.TIME_UTC,
    this.LATITUDE,
    this.LONGITUDE,
    this.SUN_AZIMUTH,
    this.SUN_ELEVATION,
    this.MOON_PHASE,
    this.ILLUMINATION,
    this.MAGNETIC_DECLINATION,
    this.MAGNETIC_INCLINATION,
    RESERVED
  );
}
}
