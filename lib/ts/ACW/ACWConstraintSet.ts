// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { ACWConstraint, ACWConstraintT } from './ACWConstraint.js';
import { acwConstraintOperator } from './acwConstraintOperator.js';


/**
 * A boolean composition of constraints and nested sets.
 */
export class ACWConstraintSet implements flatbuffers.IUnpackableObject<ACWConstraintSetT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ACWConstraintSet {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsACWConstraintSet(bb:flatbuffers.ByteBuffer, obj?:ACWConstraintSet):ACWConstraintSet {
  return (obj || new ACWConstraintSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsACWConstraintSet(bb:flatbuffers.ByteBuffer, obj?:ACWConstraintSet):ACWConstraintSet {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ACWConstraintSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

OPERATOR():acwConstraintOperator {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : acwConstraintOperator.ALL_OF;
}

CONSTRAINTS(index: number, obj?:ACWConstraint):ACWConstraint|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new ACWConstraint()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

constraintsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Nested sets, combined with the same OPERATOR.
 */
SETS(index: number, obj?:ACWConstraintSet):ACWConstraintSet|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new ACWConstraintSet()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

setsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

LABEL():string|null
LABEL(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
LABEL(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startACWConstraintSet(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addOperator(builder:flatbuffers.Builder, OPERATOR:acwConstraintOperator) {
  builder.addFieldInt8(0, OPERATOR, acwConstraintOperator.ALL_OF);
}

static addConstraints(builder:flatbuffers.Builder, CONSTRAINTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, CONSTRAINTSOffset, 0);
}

static createConstraintsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startConstraintsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSets(builder:flatbuffers.Builder, SETSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, SETSOffset, 0);
}

static createSetsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSetsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addLabel(builder:flatbuffers.Builder, LABELOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, LABELOffset, 0);
}

static endACWConstraintSet(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createACWConstraintSet(builder:flatbuffers.Builder, OPERATOR:acwConstraintOperator, CONSTRAINTSOffset:flatbuffers.Offset, SETSOffset:flatbuffers.Offset, LABELOffset:flatbuffers.Offset):flatbuffers.Offset {
  ACWConstraintSet.startACWConstraintSet(builder);
  ACWConstraintSet.addOperator(builder, OPERATOR);
  ACWConstraintSet.addConstraints(builder, CONSTRAINTSOffset);
  ACWConstraintSet.addSets(builder, SETSOffset);
  ACWConstraintSet.addLabel(builder, LABELOffset);
  return ACWConstraintSet.endACWConstraintSet(builder);
}

unpack(): ACWConstraintSetT {
  return new ACWConstraintSetT(
    this.OPERATOR(),
    this.bb!.createObjList<ACWConstraint, ACWConstraintT>(this.CONSTRAINTS.bind(this), this.constraintsLength()),
    this.bb!.createObjList<ACWConstraintSet, ACWConstraintSetT>(this.SETS.bind(this), this.setsLength()),
    this.LABEL()
  );
}


unpackTo(_o: ACWConstraintSetT): void {
  _o.OPERATOR = this.OPERATOR();
  _o.CONSTRAINTS = this.bb!.createObjList<ACWConstraint, ACWConstraintT>(this.CONSTRAINTS.bind(this), this.constraintsLength());
  _o.SETS = this.bb!.createObjList<ACWConstraintSet, ACWConstraintSetT>(this.SETS.bind(this), this.setsLength());
  _o.LABEL = this.LABEL();
}
}

export class ACWConstraintSetT implements flatbuffers.IGeneratedObject {
constructor(
  public OPERATOR: acwConstraintOperator = acwConstraintOperator.ALL_OF,
  public CONSTRAINTS: (ACWConstraintT)[] = [],
  public SETS: (ACWConstraintSetT)[] = [],
  public LABEL: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CONSTRAINTS = ACWConstraintSet.createConstraintsVector(builder, builder.createObjectOffsetList(this.CONSTRAINTS));
  const SETS = ACWConstraintSet.createSetsVector(builder, builder.createObjectOffsetList(this.SETS));
  const LABEL = (this.LABEL !== null ? builder.createString(this.LABEL!) : 0);

  return ACWConstraintSet.createACWConstraintSet(builder,
    this.OPERATOR,
    CONSTRAINTS,
    SETS,
    LABEL
  );
}
}
