// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { BPFAttestation, BPFAttestationT } from './BPFAttestation.js';
import { BPFModule, BPFModuleT } from './BPFModule.js';
import { BPFPart, BPFPartT } from './BPFPart.js';
import { BPFRuntimeLock, BPFRuntimeLockT } from './BPFRuntimeLock.js';
import { bpfLicenseMode } from './bpfLicenseMode.js';


/**
 * Build Profile — one saved, portable build configuration.
 *
 * PORTABILITY. The export unit is a `$REC` collection carrying this record
 * and, optionally, an `$APP` manifest that names it through an `APPDataRef`
 * with `SDS_TYPE: "BPF"`. The `$APP` wrapper is launcher metadata and
 * carries no authority; all authority over a profile lives in
 * `ATTESTATION`. A bare size-prefixed `$BPF` buffer is also a complete,
 * valid export.
 */
export class BPF implements flatbuffers.IUnpackableObject<BPFT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BPF {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBPF(bb:flatbuffers.ByteBuffer, obj?:BPF):BPF {
  return (obj || new BPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBPF(bb:flatbuffers.ByteBuffer, obj?:BPF):BPF {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BPF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$BPF');
}

/**
 * Stable profile identity, unique per author. Required.
 */
PROFILE_ID():string
PROFILE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
PROFILE_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Display name of the profile. Required.
 */
NAME():string
NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
NAME(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Human-readable summary.
 */
DESCRIPTION():string|null
DESCRIPTION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DESCRIPTION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
 * when the profile was created.
 */
CREATED_AT():string|null
CREATED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CREATED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
 * when the profile was last updated.
 */
UPDATED_AT():string|null
UPDATED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
UPDATED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * 64 lowercase hexadecimal characters encoding SHA-256 of the build
 * template this profile was authored against. Required: a profile that
 * does not say which template it configures cannot be validated against
 * one. A consumer holding a different template MAY still import the
 * profile but MUST mark it as authored against another template and MUST
 * NOT silently re-map its parts.
 */
TEMPLATE_SHA256():string
TEMPLATE_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
TEMPLATE_SHA256(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Template slots and their inclusion decisions. A slot the template
 * exposes and this vector omits is NOT included.
 */
PARTS(index: number, obj?:BPFPart):BPFPart|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new BPFPart()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

partsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Modules selected into the build and their protection tiers.
 */
MODULES(index: number, obj?:BPFModule):BPFModule|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? (obj || new BPFModule()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

modulesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * The runtime restriction the produced artifact enforces. Required, so
 * that an unlocked build is a stated configuration rather than a missing
 * one.
 */
RUNTIME_LOCK(obj?:BPFRuntimeLock):BPFRuntimeLock|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new BPFRuntimeLock()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Distribution-control mode of the produced artifact.
 */
LICENSE_MODE():bpfLicenseMode {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : bpfLicenseMode.UNSPECIFIED;
}

/**
 * Publisher attestation. Absent means the profile is unsigned; see
 * `BPFAttestation` for the fail-closed rule when it is present.
 */
ATTESTATION(obj?:BPFAttestation):BPFAttestation|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? (obj || new BPFAttestation()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startBPF(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addProfileId(builder:flatbuffers.Builder, PROFILE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, PROFILE_IDOffset, 0);
}

static addName(builder:flatbuffers.Builder, NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, NAMEOffset, 0);
}

static addDescription(builder:flatbuffers.Builder, DESCRIPTIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, DESCRIPTIONOffset, 0);
}

static addCreatedAt(builder:flatbuffers.Builder, CREATED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, CREATED_ATOffset, 0);
}

static addUpdatedAt(builder:flatbuffers.Builder, UPDATED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, UPDATED_ATOffset, 0);
}

static addTemplateSha256(builder:flatbuffers.Builder, TEMPLATE_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(5, TEMPLATE_SHA256Offset, 0);
}

static addParts(builder:flatbuffers.Builder, PARTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, PARTSOffset, 0);
}

static createPartsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPartsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addModules(builder:flatbuffers.Builder, MODULESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, MODULESOffset, 0);
}

static createModulesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startModulesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addRuntimeLock(builder:flatbuffers.Builder, RUNTIME_LOCKOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, RUNTIME_LOCKOffset, 0);
}

static addLicenseMode(builder:flatbuffers.Builder, LICENSE_MODE:bpfLicenseMode) {
  builder.addFieldInt8(9, LICENSE_MODE, bpfLicenseMode.UNSPECIFIED);
}

static addAttestation(builder:flatbuffers.Builder, ATTESTATIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, ATTESTATIONOffset, 0);
}

static endBPF(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // PROFILE_ID
  builder.requiredField(offset, 6) // NAME
  builder.requiredField(offset, 14) // TEMPLATE_SHA256
  builder.requiredField(offset, 20) // RUNTIME_LOCK
  return offset;
}

static finishBPFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$BPF');
}

static finishSizePrefixedBPFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$BPF', true);
}


unpack(): BPFT {
  return new BPFT(
    this.PROFILE_ID(),
    this.NAME(),
    this.DESCRIPTION(),
    this.CREATED_AT(),
    this.UPDATED_AT(),
    this.TEMPLATE_SHA256(),
    this.bb!.createObjList<BPFPart, BPFPartT>(this.PARTS.bind(this), this.partsLength()),
    this.bb!.createObjList<BPFModule, BPFModuleT>(this.MODULES.bind(this), this.modulesLength()),
    (this.RUNTIME_LOCK() !== null ? this.RUNTIME_LOCK()!.unpack() : null),
    this.LICENSE_MODE(),
    (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null)
  );
}


unpackTo(_o: BPFT): void {
  _o.PROFILE_ID = this.PROFILE_ID();
  _o.NAME = this.NAME();
  _o.DESCRIPTION = this.DESCRIPTION();
  _o.CREATED_AT = this.CREATED_AT();
  _o.UPDATED_AT = this.UPDATED_AT();
  _o.TEMPLATE_SHA256 = this.TEMPLATE_SHA256();
  _o.PARTS = this.bb!.createObjList<BPFPart, BPFPartT>(this.PARTS.bind(this), this.partsLength());
  _o.MODULES = this.bb!.createObjList<BPFModule, BPFModuleT>(this.MODULES.bind(this), this.modulesLength());
  _o.RUNTIME_LOCK = (this.RUNTIME_LOCK() !== null ? this.RUNTIME_LOCK()!.unpack() : null);
  _o.LICENSE_MODE = this.LICENSE_MODE();
  _o.ATTESTATION = (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null);
}
}

export class BPFT implements flatbuffers.IGeneratedObject {
constructor(
  public PROFILE_ID: string|Uint8Array|null = null,
  public NAME: string|Uint8Array|null = null,
  public DESCRIPTION: string|Uint8Array|null = null,
  public CREATED_AT: string|Uint8Array|null = null,
  public UPDATED_AT: string|Uint8Array|null = null,
  public TEMPLATE_SHA256: string|Uint8Array|null = null,
  public PARTS: (BPFPartT)[] = [],
  public MODULES: (BPFModuleT)[] = [],
  public RUNTIME_LOCK: BPFRuntimeLockT|null = null,
  public LICENSE_MODE: bpfLicenseMode = bpfLicenseMode.UNSPECIFIED,
  public ATTESTATION: BPFAttestationT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PROFILE_ID = (this.PROFILE_ID !== null ? builder.createString(this.PROFILE_ID!) : 0);
  const NAME = (this.NAME !== null ? builder.createString(this.NAME!) : 0);
  const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION!) : 0);
  const CREATED_AT = (this.CREATED_AT !== null ? builder.createString(this.CREATED_AT!) : 0);
  const UPDATED_AT = (this.UPDATED_AT !== null ? builder.createString(this.UPDATED_AT!) : 0);
  const TEMPLATE_SHA256 = (this.TEMPLATE_SHA256 !== null ? builder.createString(this.TEMPLATE_SHA256!) : 0);
  const PARTS = BPF.createPartsVector(builder, builder.createObjectOffsetList(this.PARTS));
  const MODULES = BPF.createModulesVector(builder, builder.createObjectOffsetList(this.MODULES));
  const RUNTIME_LOCK = (this.RUNTIME_LOCK !== null ? this.RUNTIME_LOCK!.pack(builder) : 0);
  const ATTESTATION = (this.ATTESTATION !== null ? this.ATTESTATION!.pack(builder) : 0);

  BPF.startBPF(builder);
  BPF.addProfileId(builder, PROFILE_ID);
  BPF.addName(builder, NAME);
  BPF.addDescription(builder, DESCRIPTION);
  BPF.addCreatedAt(builder, CREATED_AT);
  BPF.addUpdatedAt(builder, UPDATED_AT);
  BPF.addTemplateSha256(builder, TEMPLATE_SHA256);
  BPF.addParts(builder, PARTS);
  BPF.addModules(builder, MODULES);
  BPF.addRuntimeLock(builder, RUNTIME_LOCK);
  BPF.addLicenseMode(builder, this.LICENSE_MODE);
  BPF.addAttestation(builder, ATTESTATION);

  return BPF.endBPF(builder);
}
}
