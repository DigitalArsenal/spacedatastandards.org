// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { FlatBufferTypeRef, FlatBufferTypeRefT } from './FlatBufferTypeRef.js';
import { bufferMutability } from './bufferMutability.js';
import { bufferOwnership } from './bufferOwnership.js';
import { payloadWireFormat } from './payloadWireFormat.js';


/**
 * Typed Arena Buffer — one descriptor for a payload slot in a shared arena.
 */
export class TAB implements flatbuffers.IUnpackableObject<TABT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TAB {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTAB(bb:flatbuffers.ByteBuffer, obj?:TAB):TAB {
  return (obj || new TAB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTAB(bb:flatbuffers.ByteBuffer, obj?:TAB):TAB {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TAB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$TAB');
}

/**
 * Byte offset of the payload body within the arena.
 */
OFFSET():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Byte length of the payload body.
 */
SIZE():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Required start alignment of the payload body (in bytes).
 */
ALIGNMENT():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Wire format for the body.
 */
WIRE_FORMAT():payloadWireFormat {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : payloadWireFormat.FLATBUFFER;
}

/**
 * Optional payload schema identity.
 */
TYPE_REF(obj?:FlatBufferTypeRef):FlatBufferTypeRef|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new FlatBufferTypeRef()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Mutability contract for the slot.
 */
MUTABILITY():bufferMutability {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : bufferMutability.IMMUTABLE;
}

/**
 * Ownership contract for the slot.
 */
OWNERSHIP():bufferOwnership {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : bufferOwnership.HOST_OWNED;
}

/**
 * Optional opaque frame identifier for stream bookkeeping.
 */
FRAME_ID():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Optional port identifier for frames that route to/from a named
 * input or output port on a method (maps to
 * `PLG.PLGPortManifest.PORT_ID`). Empty for arena frames that carry
 * no port routing hint.
 */
PORT_ID():string|null
PORT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PORT_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startTAB(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addOffset(builder:flatbuffers.Builder, OFFSET:number) {
  builder.addFieldInt32(0, OFFSET, 0);
}

static addSize(builder:flatbuffers.Builder, SIZE:number) {
  builder.addFieldInt32(1, SIZE, 0);
}

static addAlignment(builder:flatbuffers.Builder, ALIGNMENT:number) {
  builder.addFieldInt32(2, ALIGNMENT, 0);
}

static addWireFormat(builder:flatbuffers.Builder, WIRE_FORMAT:payloadWireFormat) {
  builder.addFieldInt8(3, WIRE_FORMAT, payloadWireFormat.FLATBUFFER);
}

static addTypeRef(builder:flatbuffers.Builder, TYPE_REFOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, TYPE_REFOffset, 0);
}

static addMutability(builder:flatbuffers.Builder, MUTABILITY:bufferMutability) {
  builder.addFieldInt8(5, MUTABILITY, bufferMutability.IMMUTABLE);
}

static addOwnership(builder:flatbuffers.Builder, OWNERSHIP:bufferOwnership) {
  builder.addFieldInt8(6, OWNERSHIP, bufferOwnership.HOST_OWNED);
}

static addFrameId(builder:flatbuffers.Builder, FRAME_ID:bigint) {
  builder.addFieldInt64(7, FRAME_ID, BigInt('0'));
}

static addPortId(builder:flatbuffers.Builder, PORT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, PORT_IDOffset, 0);
}

static endTAB(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishTABBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TAB');
}

static finishSizePrefixedTABBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TAB', true);
}


unpack(): TABT {
  return new TABT(
    this.OFFSET(),
    this.SIZE(),
    this.ALIGNMENT(),
    this.WIRE_FORMAT(),
    (this.TYPE_REF() !== null ? this.TYPE_REF()!.unpack() : null),
    this.MUTABILITY(),
    this.OWNERSHIP(),
    this.FRAME_ID(),
    this.PORT_ID()
  );
}


unpackTo(_o: TABT): void {
  _o.OFFSET = this.OFFSET();
  _o.SIZE = this.SIZE();
  _o.ALIGNMENT = this.ALIGNMENT();
  _o.WIRE_FORMAT = this.WIRE_FORMAT();
  _o.TYPE_REF = (this.TYPE_REF() !== null ? this.TYPE_REF()!.unpack() : null);
  _o.MUTABILITY = this.MUTABILITY();
  _o.OWNERSHIP = this.OWNERSHIP();
  _o.FRAME_ID = this.FRAME_ID();
  _o.PORT_ID = this.PORT_ID();
}
}

export class TABT implements flatbuffers.IGeneratedObject {
constructor(
  public OFFSET: number = 0,
  public SIZE: number = 0,
  public ALIGNMENT: number = 0,
  public WIRE_FORMAT: payloadWireFormat = payloadWireFormat.FLATBUFFER,
  public TYPE_REF: FlatBufferTypeRefT|null = null,
  public MUTABILITY: bufferMutability = bufferMutability.IMMUTABLE,
  public OWNERSHIP: bufferOwnership = bufferOwnership.HOST_OWNED,
  public FRAME_ID: bigint = BigInt('0'),
  public PORT_ID: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const TYPE_REF = (this.TYPE_REF !== null ? this.TYPE_REF!.pack(builder) : 0);
  const PORT_ID = (this.PORT_ID !== null ? builder.createString(this.PORT_ID!) : 0);

  TAB.startTAB(builder);
  TAB.addOffset(builder, this.OFFSET);
  TAB.addSize(builder, this.SIZE);
  TAB.addAlignment(builder, this.ALIGNMENT);
  TAB.addWireFormat(builder, this.WIRE_FORMAT);
  TAB.addTypeRef(builder, TYPE_REF);
  TAB.addMutability(builder, this.MUTABILITY);
  TAB.addOwnership(builder, this.OWNERSHIP);
  TAB.addFrameId(builder, this.FRAME_ID);
  TAB.addPortId(builder, PORT_ID);

  return TAB.endTAB(builder);
}
}
