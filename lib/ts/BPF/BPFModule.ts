// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { PLG, PLGT } from './PLG.js';
import { bpfProtectionTier } from './bpfProtectionTier.js';


/**
 * One module selected into the build and the tier its payload is protected
 * at.
 */
export class BPFModule implements flatbuffers.IUnpackableObject<BPFModuleT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BPFModule {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBPFModule(bb:flatbuffers.ByteBuffer, obj?:BPFModule):BPFModule {
  return (obj || new BPFModule()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBPFModule(bb:flatbuffers.ByteBuffer, obj?:BPFModule):BPFModule {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BPFModule()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Canonical module identifier. Required, unique within the profile.
 */
MODULE_ID():string
MODULE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
MODULE_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Module version or version range.
 */
MODULE_VERSION():string|null
MODULE_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MODULE_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * True when the module is embedded in the produced artifact.
 */
INCLUDED():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Protection tier applied to this module's payload.
 */
PROTECTION():bpfProtectionTier {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : bpfProtectionTier.UNSPECIFIED;
}

/**
 * 64 lowercase hexadecimal characters encoding SHA-256 of the module's
 * portable (pre-AOT, publication-trailer-stripped) bytes. This is the
 * identity a capability or signature policy keys on.
 */
CONTENT_HASH():string|null
CONTENT_HASH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CONTENT_HASH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Published module descriptor, carried when the authoring context holds
 * it. Advisory: `MODULE_ID` and `CONTENT_HASH` are the identity, and a
 * consumer MUST NOT prefer a carried descriptor over the module's own
 * published manifest.
 */
MODULE_DESCRIPTOR(obj?:PLG):PLG|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new PLG()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startBPFModule(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addModuleId(builder:flatbuffers.Builder, MODULE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, MODULE_IDOffset, 0);
}

static addModuleVersion(builder:flatbuffers.Builder, MODULE_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, MODULE_VERSIONOffset, 0);
}

static addIncluded(builder:flatbuffers.Builder, INCLUDED:boolean) {
  builder.addFieldInt8(2, +INCLUDED, +false);
}

static addProtection(builder:flatbuffers.Builder, PROTECTION:bpfProtectionTier) {
  builder.addFieldInt8(3, PROTECTION, bpfProtectionTier.UNSPECIFIED);
}

static addContentHash(builder:flatbuffers.Builder, CONTENT_HASHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, CONTENT_HASHOffset, 0);
}

static addModuleDescriptor(builder:flatbuffers.Builder, MODULE_DESCRIPTOROffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, MODULE_DESCRIPTOROffset, 0);
}

static endBPFModule(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // MODULE_ID
  return offset;
}


unpack(): BPFModuleT {
  return new BPFModuleT(
    this.MODULE_ID(),
    this.MODULE_VERSION(),
    this.INCLUDED(),
    this.PROTECTION(),
    this.CONTENT_HASH(),
    (this.MODULE_DESCRIPTOR() !== null ? this.MODULE_DESCRIPTOR()!.unpack() : null)
  );
}


unpackTo(_o: BPFModuleT): void {
  _o.MODULE_ID = this.MODULE_ID();
  _o.MODULE_VERSION = this.MODULE_VERSION();
  _o.INCLUDED = this.INCLUDED();
  _o.PROTECTION = this.PROTECTION();
  _o.CONTENT_HASH = this.CONTENT_HASH();
  _o.MODULE_DESCRIPTOR = (this.MODULE_DESCRIPTOR() !== null ? this.MODULE_DESCRIPTOR()!.unpack() : null);
}
}

export class BPFModuleT implements flatbuffers.IGeneratedObject {
constructor(
  public MODULE_ID: string|Uint8Array|null = null,
  public MODULE_VERSION: string|Uint8Array|null = null,
  public INCLUDED: boolean = false,
  public PROTECTION: bpfProtectionTier = bpfProtectionTier.UNSPECIFIED,
  public CONTENT_HASH: string|Uint8Array|null = null,
  public MODULE_DESCRIPTOR: PLGT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const MODULE_ID = (this.MODULE_ID !== null ? builder.createString(this.MODULE_ID!) : 0);
  const MODULE_VERSION = (this.MODULE_VERSION !== null ? builder.createString(this.MODULE_VERSION!) : 0);
  const CONTENT_HASH = (this.CONTENT_HASH !== null ? builder.createString(this.CONTENT_HASH!) : 0);
  const MODULE_DESCRIPTOR = (this.MODULE_DESCRIPTOR !== null ? this.MODULE_DESCRIPTOR!.pack(builder) : 0);

  BPFModule.startBPFModule(builder);
  BPFModule.addModuleId(builder, MODULE_ID);
  BPFModule.addModuleVersion(builder, MODULE_VERSION);
  BPFModule.addIncluded(builder, this.INCLUDED);
  BPFModule.addProtection(builder, this.PROTECTION);
  BPFModule.addContentHash(builder, CONTENT_HASH);
  BPFModule.addModuleDescriptor(builder, MODULE_DESCRIPTOR);

  return BPFModule.endBPFModule(builder);
}
}
