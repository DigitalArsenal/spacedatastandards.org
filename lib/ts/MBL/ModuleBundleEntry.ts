// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { ModuleBundleEntryRole } from './ModuleBundleEntryRole.js';
import { ModulePayloadEncoding } from './ModulePayloadEncoding.js';


/**
 * One payload carried inside the module bundle.
 */
export class ModuleBundleEntry implements flatbuffers.IUnpackableObject<ModuleBundleEntryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ModuleBundleEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsModuleBundleEntry(bb:flatbuffers.ByteBuffer, obj?:ModuleBundleEntry):ModuleBundleEntry {
  return (obj || new ModuleBundleEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsModuleBundleEntry(bb:flatbuffers.ByteBuffer, obj?:ModuleBundleEntry):ModuleBundleEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ModuleBundleEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Stable bundle-local identifier.
 */
entry_id():string
entry_id(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
entry_id(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * High-level semantic role of the payload.
 */
role():ModuleBundleEntryRole {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ModuleBundleEntryRole.AUXILIARY;
}

/**
 * Optional logical section name within the bundle, for example
 * `sds.authorization`.
 */
section_name():string|null
section_name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
section_name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * SDS/shared-module schema identity when the payload is itself a
 * FlatBuffer. Stored as canonical JSON text for portability.
 */
type_ref():string|null
type_ref(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
type_ref(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Encoding used for `payload`.
 */
payload_encoding():ModulePayloadEncoding {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ModulePayloadEncoding.RAW_BYTES;
}

/**
 * Optional media type for transitional payloads such as JSON envelopes.
 */
media_type():string|null
media_type(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
media_type(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Implementation-defined bit flags for signed/encrypted/compressed state.
 */
flags():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * SHA-256 of the payload bytes.
 */
sha256(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

sha256Length():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

sha256Array():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Embedded payload bytes. For single-file deployment this should be
 * populated for every entry.
 */
payload(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

payloadLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

payloadArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Human-readable description for tooling and diagnostics.
 */
description():string|null
description(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
description(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startModuleBundleEntry(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addEntryId(builder:flatbuffers.Builder, entry_idOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, entry_idOffset, 0);
}

static addRole(builder:flatbuffers.Builder, role:ModuleBundleEntryRole) {
  builder.addFieldInt8(1, role, ModuleBundleEntryRole.AUXILIARY);
}

static addSectionName(builder:flatbuffers.Builder, section_nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, section_nameOffset, 0);
}

static addTypeRef(builder:flatbuffers.Builder, type_refOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, type_refOffset, 0);
}

static addPayloadEncoding(builder:flatbuffers.Builder, payload_encoding:ModulePayloadEncoding) {
  builder.addFieldInt8(4, payload_encoding, ModulePayloadEncoding.RAW_BYTES);
}

static addMediaType(builder:flatbuffers.Builder, media_typeOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, media_typeOffset, 0);
}

static addFlags(builder:flatbuffers.Builder, flags:number) {
  builder.addFieldInt32(6, flags, 0);
}

static addSha256(builder:flatbuffers.Builder, sha256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(7, sha256Offset, 0);
}

static createSha256Vector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSha256Vector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, payloadOffset, 0);
}

static createPayloadVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPayloadVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addDescription(builder:flatbuffers.Builder, descriptionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, descriptionOffset, 0);
}

static endModuleBundleEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // entry_id
  return offset;
}

static createModuleBundleEntry(builder:flatbuffers.Builder, entry_idOffset:flatbuffers.Offset, role:ModuleBundleEntryRole, section_nameOffset:flatbuffers.Offset, type_refOffset:flatbuffers.Offset, payload_encoding:ModulePayloadEncoding, media_typeOffset:flatbuffers.Offset, flags:number, sha256Offset:flatbuffers.Offset, payloadOffset:flatbuffers.Offset, descriptionOffset:flatbuffers.Offset):flatbuffers.Offset {
  ModuleBundleEntry.startModuleBundleEntry(builder);
  ModuleBundleEntry.addEntryId(builder, entry_idOffset);
  ModuleBundleEntry.addRole(builder, role);
  ModuleBundleEntry.addSectionName(builder, section_nameOffset);
  ModuleBundleEntry.addTypeRef(builder, type_refOffset);
  ModuleBundleEntry.addPayloadEncoding(builder, payload_encoding);
  ModuleBundleEntry.addMediaType(builder, media_typeOffset);
  ModuleBundleEntry.addFlags(builder, flags);
  ModuleBundleEntry.addSha256(builder, sha256Offset);
  ModuleBundleEntry.addPayload(builder, payloadOffset);
  ModuleBundleEntry.addDescription(builder, descriptionOffset);
  return ModuleBundleEntry.endModuleBundleEntry(builder);
}

unpack(): ModuleBundleEntryT {
  return new ModuleBundleEntryT(
    this.entry_id(),
    this.role(),
    this.section_name(),
    this.type_ref(),
    this.payload_encoding(),
    this.media_type(),
    this.flags(),
    this.bb!.createScalarList<number>(this.sha256.bind(this), this.sha256Length()),
    this.bb!.createScalarList<number>(this.payload.bind(this), this.payloadLength()),
    this.description()
  );
}


unpackTo(_o: ModuleBundleEntryT): void {
  _o.entry_id = this.entry_id();
  _o.role = this.role();
  _o.section_name = this.section_name();
  _o.type_ref = this.type_ref();
  _o.payload_encoding = this.payload_encoding();
  _o.media_type = this.media_type();
  _o.flags = this.flags();
  _o.sha256 = this.bb!.createScalarList<number>(this.sha256.bind(this), this.sha256Length());
  _o.payload = this.bb!.createScalarList<number>(this.payload.bind(this), this.payloadLength());
  _o.description = this.description();
}
}

export class ModuleBundleEntryT implements flatbuffers.IGeneratedObject {
constructor(
  public entry_id: string|Uint8Array|null = null,
  public role: ModuleBundleEntryRole = ModuleBundleEntryRole.AUXILIARY,
  public section_name: string|Uint8Array|null = null,
  public type_ref: string|Uint8Array|null = null,
  public payload_encoding: ModulePayloadEncoding = ModulePayloadEncoding.RAW_BYTES,
  public media_type: string|Uint8Array|null = null,
  public flags: number = 0,
  public sha256: (number)[] = [],
  public payload: (number)[] = [],
  public description: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const entry_id = (this.entry_id !== null ? builder.createString(this.entry_id!) : 0);
  const section_name = (this.section_name !== null ? builder.createString(this.section_name!) : 0);
  const type_ref = (this.type_ref !== null ? builder.createString(this.type_ref!) : 0);
  const media_type = (this.media_type !== null ? builder.createString(this.media_type!) : 0);
  const sha256 = ModuleBundleEntry.createSha256Vector(builder, this.sha256);
  const payload = ModuleBundleEntry.createPayloadVector(builder, this.payload);
  const description = (this.description !== null ? builder.createString(this.description!) : 0);

  return ModuleBundleEntry.createModuleBundleEntry(builder,
    entry_id,
    this.role,
    section_name,
    type_ref,
    this.payload_encoding,
    media_type,
    this.flags,
    sha256,
    payload,
    description
  );
}
}
