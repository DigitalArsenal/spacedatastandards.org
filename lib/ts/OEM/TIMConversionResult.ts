// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TIMInstant, TIMInstantT } from './TIMInstant.js';
import { timConversionStatus } from './timConversionStatus.js';


/**
 * Result of a time conversion request.
 */
export class TIMConversionResult implements flatbuffers.IUnpackableObject<TIMConversionResultT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TIMConversionResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTIMConversionResult(bb:flatbuffers.ByteBuffer, obj?:TIMConversionResult):TIMConversionResult {
  return (obj || new TIMConversionResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTIMConversionResult(bb:flatbuffers.ByteBuffer, obj?:TIMConversionResult):TIMConversionResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TIMConversionResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Original source instant.
 */
SOURCE(obj?:TIMInstant):TIMInstant|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new TIMInstant()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Converted target instant.
 */
TARGET(obj?:TIMInstant):TIMInstant|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new TIMInstant()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Target minus source offset in SI seconds for the requested conversion.
 */
DELTA_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Conversion status.
 */
STATUS():timConversionStatus {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : timConversionStatus.OK;
}

/**
 * Optional error detail when STATUS is not OK.
 */
ERROR_MESSAGE():string|null
ERROR_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ERROR_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Caller trace/correlation identifier copied from the request when present.
 */
TRACE_ID():string|null
TRACE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TRACE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startTIMConversionResult(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addSource(builder:flatbuffers.Builder, SOURCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SOURCEOffset, 0);
}

static addTarget(builder:flatbuffers.Builder, TARGETOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, TARGETOffset, 0);
}

static addDeltaSeconds(builder:flatbuffers.Builder, DELTA_SECONDS:number) {
  builder.addFieldFloat64(2, DELTA_SECONDS, 0.0);
}

static addStatus(builder:flatbuffers.Builder, STATUS:timConversionStatus) {
  builder.addFieldInt8(3, STATUS, timConversionStatus.OK);
}

static addErrorMessage(builder:flatbuffers.Builder, ERROR_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, ERROR_MESSAGEOffset, 0);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, TRACE_IDOffset, 0);
}

static endTIMConversionResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): TIMConversionResultT {
  return new TIMConversionResultT(
    (this.SOURCE() !== null ? this.SOURCE()!.unpack() : null),
    (this.TARGET() !== null ? this.TARGET()!.unpack() : null),
    this.DELTA_SECONDS(),
    this.STATUS(),
    this.ERROR_MESSAGE(),
    this.TRACE_ID()
  );
}


unpackTo(_o: TIMConversionResultT): void {
  _o.SOURCE = (this.SOURCE() !== null ? this.SOURCE()!.unpack() : null);
  _o.TARGET = (this.TARGET() !== null ? this.TARGET()!.unpack() : null);
  _o.DELTA_SECONDS = this.DELTA_SECONDS();
  _o.STATUS = this.STATUS();
  _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
  _o.TRACE_ID = this.TRACE_ID();
}
}

export class TIMConversionResultT implements flatbuffers.IGeneratedObject {
constructor(
  public SOURCE: TIMInstantT|null = null,
  public TARGET: TIMInstantT|null = null,
  public DELTA_SECONDS: number = 0.0,
  public STATUS: timConversionStatus = timConversionStatus.OK,
  public ERROR_MESSAGE: string|Uint8Array|null = null,
  public TRACE_ID: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SOURCE = (this.SOURCE !== null ? this.SOURCE!.pack(builder) : 0);
  const TARGET = (this.TARGET !== null ? this.TARGET!.pack(builder) : 0);
  const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE!) : 0);
  const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID!) : 0);

  TIMConversionResult.startTIMConversionResult(builder);
  TIMConversionResult.addSource(builder, SOURCE);
  TIMConversionResult.addTarget(builder, TARGET);
  TIMConversionResult.addDeltaSeconds(builder, this.DELTA_SECONDS);
  TIMConversionResult.addStatus(builder, this.STATUS);
  TIMConversionResult.addErrorMessage(builder, ERROR_MESSAGE);
  TIMConversionResult.addTraceId(builder, TRACE_ID);

  return TIMConversionResult.endTIMConversionResult(builder);
}
}
