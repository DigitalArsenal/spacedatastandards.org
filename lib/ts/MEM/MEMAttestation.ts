// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Dual-form signature. Presence means both signatures are required and both
 * MUST verify independently; an invalid member rejects the record rather
 * than downgrading it to unsigned.
 */
export class MEMAttestation implements flatbuffers.IUnpackableObject<MEMAttestationT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MEMAttestation {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMEMAttestation(bb:flatbuffers.ByteBuffer, obj?:MEMAttestation):MEMAttestation {
  return (obj || new MEMAttestation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMEMAttestation(bb:flatbuffers.ByteBuffer, obj?:MEMAttestation):MEMAttestation {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MEMAttestation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

SIGNING_PUBLIC_KEY():string
SIGNING_PUBLIC_KEY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
SIGNING_PUBLIC_KEY(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

SIGNED_AT():string|null
SIGNED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SIGNED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signatureArray():Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

CANONICAL_JSON_SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

canonicalJsonSignatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

canonicalJsonSignatureArray():Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

static startMEMAttestation(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addSigningPublicKey(builder:flatbuffers.Builder, SIGNING_PUBLIC_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SIGNING_PUBLIC_KEYOffset, 0);
}

static addSignedAt(builder:flatbuffers.Builder, SIGNED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, SIGNED_ATOffset, 0);
}

static addSignature(builder:flatbuffers.Builder, SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, SIGNATUREOffset, 0);
}

static createSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addCanonicalJsonSignature(builder:flatbuffers.Builder, CANONICAL_JSON_SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, CANONICAL_JSON_SIGNATUREOffset, 0);
}

static createCanonicalJsonSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCanonicalJsonSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endMEMAttestation(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // SIGNING_PUBLIC_KEY
  builder.requiredField(offset, 8) // SIGNATURE
  builder.requiredField(offset, 10) // CANONICAL_JSON_SIGNATURE
  return offset;
}

static createMEMAttestation(builder:flatbuffers.Builder, SIGNING_PUBLIC_KEYOffset:flatbuffers.Offset, SIGNED_ATOffset:flatbuffers.Offset, SIGNATUREOffset:flatbuffers.Offset, CANONICAL_JSON_SIGNATUREOffset:flatbuffers.Offset):flatbuffers.Offset {
  MEMAttestation.startMEMAttestation(builder);
  MEMAttestation.addSigningPublicKey(builder, SIGNING_PUBLIC_KEYOffset);
  MEMAttestation.addSignedAt(builder, SIGNED_ATOffset);
  MEMAttestation.addSignature(builder, SIGNATUREOffset);
  MEMAttestation.addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATUREOffset);
  return MEMAttestation.endMEMAttestation(builder);
}

unpack(): MEMAttestationT {
  return new MEMAttestationT(
    this.SIGNING_PUBLIC_KEY(),
    this.SIGNED_AT(),
    this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength()),
    this.bb!.createScalarList<number>(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength())
  );
}


unpackTo(_o: MEMAttestationT): void {
  _o.SIGNING_PUBLIC_KEY = this.SIGNING_PUBLIC_KEY();
  _o.SIGNED_AT = this.SIGNED_AT();
  _o.SIGNATURE = this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength());
  _o.CANONICAL_JSON_SIGNATURE = this.bb!.createScalarList<number>(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength());
}
}

export class MEMAttestationT implements flatbuffers.IGeneratedObject {
constructor(
  public SIGNING_PUBLIC_KEY: string|Uint8Array|null = null,
  public SIGNED_AT: string|Uint8Array|null = null,
  public SIGNATURE: (number)[] = [],
  public CANONICAL_JSON_SIGNATURE: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SIGNING_PUBLIC_KEY = (this.SIGNING_PUBLIC_KEY !== null ? builder.createString(this.SIGNING_PUBLIC_KEY!) : 0);
  const SIGNED_AT = (this.SIGNED_AT !== null ? builder.createString(this.SIGNED_AT!) : 0);
  const SIGNATURE = MEMAttestation.createSignatureVector(builder, this.SIGNATURE);
  const CANONICAL_JSON_SIGNATURE = MEMAttestation.createCanonicalJsonSignatureVector(builder, this.CANONICAL_JSON_SIGNATURE);

  return MEMAttestation.createMEMAttestation(builder,
    SIGNING_PUBLIC_KEY,
    SIGNED_AT,
    SIGNATURE,
    CANONICAL_JSON_SIGNATURE
  );
}
}
