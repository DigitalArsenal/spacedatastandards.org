// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { memEditingAction } from './memEditingAction.js';
import { memEditingReason } from './memEditingReason.js';


/**
 * One deterministic editing decision. The observation identity is carried
 * from the tracking-data input so rejected sets can be compared exactly.
 */
export class MEMEditingDecision implements flatbuffers.IUnpackableObject<MEMEditingDecisionT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MEMEditingDecision {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMEMEditingDecision(bb:flatbuffers.ByteBuffer, obj?:MEMEditingDecision):MEMEditingDecision {
  return (obj || new MEMEditingDecision()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMEMEditingDecision(bb:flatbuffers.ByteBuffer, obj?:MEMEditingDecision):MEMEditingDecision {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MEMEditingDecision()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

OBSERVATION_ID():string
OBSERVATION_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
OBSERVATION_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

ACTION():memEditingAction {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : memEditingAction.UNSPECIFIED;
}

REASON():memEditingReason {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : memEditingReason.UNSPECIFIED;
}

PREFIT_RESIDUAL():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

NORMALIZED_RESIDUAL():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

ITERATION():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startMEMEditingDecision(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addObservationId(builder:flatbuffers.Builder, OBSERVATION_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, OBSERVATION_IDOffset, 0);
}

static addAction(builder:flatbuffers.Builder, ACTION:memEditingAction) {
  builder.addFieldInt8(1, ACTION, memEditingAction.UNSPECIFIED);
}

static addReason(builder:flatbuffers.Builder, REASON:memEditingReason) {
  builder.addFieldInt8(2, REASON, memEditingReason.UNSPECIFIED);
}

static addPrefitResidual(builder:flatbuffers.Builder, PREFIT_RESIDUAL:number) {
  builder.addFieldFloat64(3, PREFIT_RESIDUAL, 0.0);
}

static addNormalizedResidual(builder:flatbuffers.Builder, NORMALIZED_RESIDUAL:number) {
  builder.addFieldFloat64(4, NORMALIZED_RESIDUAL, 0.0);
}

static addIteration(builder:flatbuffers.Builder, ITERATION:number) {
  builder.addFieldInt32(5, ITERATION, 0);
}

static endMEMEditingDecision(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // OBSERVATION_ID
  return offset;
}

static createMEMEditingDecision(builder:flatbuffers.Builder, OBSERVATION_IDOffset:flatbuffers.Offset, ACTION:memEditingAction, REASON:memEditingReason, PREFIT_RESIDUAL:number, NORMALIZED_RESIDUAL:number, ITERATION:number):flatbuffers.Offset {
  MEMEditingDecision.startMEMEditingDecision(builder);
  MEMEditingDecision.addObservationId(builder, OBSERVATION_IDOffset);
  MEMEditingDecision.addAction(builder, ACTION);
  MEMEditingDecision.addReason(builder, REASON);
  MEMEditingDecision.addPrefitResidual(builder, PREFIT_RESIDUAL);
  MEMEditingDecision.addNormalizedResidual(builder, NORMALIZED_RESIDUAL);
  MEMEditingDecision.addIteration(builder, ITERATION);
  return MEMEditingDecision.endMEMEditingDecision(builder);
}

unpack(): MEMEditingDecisionT {
  return new MEMEditingDecisionT(
    this.OBSERVATION_ID(),
    this.ACTION(),
    this.REASON(),
    this.PREFIT_RESIDUAL(),
    this.NORMALIZED_RESIDUAL(),
    this.ITERATION()
  );
}


unpackTo(_o: MEMEditingDecisionT): void {
  _o.OBSERVATION_ID = this.OBSERVATION_ID();
  _o.ACTION = this.ACTION();
  _o.REASON = this.REASON();
  _o.PREFIT_RESIDUAL = this.PREFIT_RESIDUAL();
  _o.NORMALIZED_RESIDUAL = this.NORMALIZED_RESIDUAL();
  _o.ITERATION = this.ITERATION();
}
}

export class MEMEditingDecisionT implements flatbuffers.IGeneratedObject {
constructor(
  public OBSERVATION_ID: string|Uint8Array|null = null,
  public ACTION: memEditingAction = memEditingAction.UNSPECIFIED,
  public REASON: memEditingReason = memEditingReason.UNSPECIFIED,
  public PREFIT_RESIDUAL: number = 0.0,
  public NORMALIZED_RESIDUAL: number = 0.0,
  public ITERATION: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const OBSERVATION_ID = (this.OBSERVATION_ID !== null ? builder.createString(this.OBSERVATION_ID!) : 0);

  return MEMEditingDecision.createMEMEditingDecision(builder,
    OBSERVATION_ID,
    this.ACTION,
    this.REASON,
    this.PREFIT_RESIDUAL,
    this.NORMALIZED_RESIDUAL,
    this.ITERATION
  );
}
}
