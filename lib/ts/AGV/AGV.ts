// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { AGFProjectionParameters, AGFProjectionParametersT } from './AGFProjectionParameters.js';
import { AGVProvenance, AGVProvenanceT } from './AGVProvenance.js';
import { agfDType } from './agfDType.js';
import { agfPayloadCodec } from './agfPayloadCodec.js';
import { agfProjection } from './agfProjection.js';
import { agfRowOrder } from './agfRowOrder.js';
import { agfVariable } from './agfVariable.js';
import { agvArrayLayout } from './agvArrayLayout.js';
import { agvLevelOrder } from './agvLevelOrder.js';
import { agvVerticalCoordinate } from './agvVerticalCoordinate.js';


/**
 * Atmospheric Gridded Volume
 */
export class AGV implements flatbuffers.IUnpackableObject<AGVT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AGV {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAGV(bb:flatbuffers.ByteBuffer, obj?:AGV):AGV {
  return (obj || new AGV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAGV(bb:flatbuffers.ByteBuffer, obj?:AGV):AGV {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new AGV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$AGV');
}

/**
 * Stable identifier of this volume record.
 */
ID():string
ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Nominal time the volume represents, ISO 8601 UTC. For a forecast this is
 * the validity time.
 */
EPOCH():string
EPOCH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
EPOCH(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Half-open validity window [VALID_START, VALID_END), ISO 8601 UTC.
 */
VALID_START():string|null
VALID_START(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VALID_START(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

VALID_END():string|null
VALID_END(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VALID_END(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Model initialisation time, ISO 8601 UTC. ABSENT for an analysis or an
 * observation-derived volume.
 */
FORECAST_REFERENCE_TIME():string|null
FORECAST_REFERENCE_TIME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
FORECAST_REFERENCE_TIME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Seconds from FORECAST_REFERENCE_TIME to EPOCH. Meaningful ONLY when
 * FORECAST_REFERENCE_TIME is present; NaN otherwise.
 */
FORECAST_LEAD_S():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * The physical quantity, from the `$AGF` vocabulary. It does NOT imply
 * units.
 */
VARIABLE():agfVariable {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agfVariable.UNSPECIFIED;
}

/**
 * UDUNITS-compatible units of the SCALED physical value. Required.
 */
UNITS():string
UNITS(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
UNITS(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Category meanings for a categorical VARIABLE, indexed by raw value.
 */
CATEGORY_LABELS(index: number):string
CATEGORY_LABELS(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
CATEGORY_LABELS(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

categoryLabelsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Element type of the packed array, from the `$AGF` vocabulary.
 */
DTYPE():agfDType {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agfDType.UNSPECIFIED;
}

/**
 * Sentinel marking a cell with no data, in RAW units, before scaling.
 */
FILL_VALUE():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * physical = raw * SCALE_FACTOR + ADD_OFFSET, applied only to non-fill
 * cells.
 */
SCALE_FACTOR():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 1.0;
}

ADD_OFFSET():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Number of columns and rows per level.
 */
NX():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

NY():number {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Order of the NY rows within each z-slab.
 */
ROW_ORDER():agfRowOrder {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agfRowOrder.UNSPECIFIED;
}

/**
 * Projection family of the horizontal grid.
 */
PROJECTION():agfProjection {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agfProjection.UNSPECIFIED;
}

/**
 * Explicit, authoritative georeferencing parameters. PROJ4 and WKT2
 * strings within remain advisory only.
 */
PROJECTION_PARAMETERS(obj?:AGFProjectionParameters):AGFProjectionParameters|null {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? (obj || new AGFProjectionParameters()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Number of levels. The payload holds exactly NX * NY * NZ elements.
 */
NZ():number {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Kind of vertical level, which fixes the units of LEVELS.
 */
VERTICAL_COORDINATE():agvVerticalCoordinate {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agvVerticalCoordinate.UNSPECIFIED;
}

/**
 * The NZ level values themselves, in payload slab order. Required and
 * explicit: never a start-and-step, never a formula. Length MUST equal NZ.
 */
LEVELS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 44);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

levelsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 44);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

levelsArray():Float64Array {
  const offset = this.bb!.__offset(this.bb_pos, 44);
  return new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

/**
 * Direction of the LEVELS sequence.
 */
LEVEL_ORDER():agvLevelOrder {
  const offset = this.bb!.__offset(this.bb_pos, 46);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agvLevelOrder.UNSPECIFIED;
}

/**
 * Hybrid sigma-pressure A coefficients, pascals, one per level (or per
 * level interface, when INTERFACE_COEFFICIENTS is true). REQUIRED for
 * HYBRID_SIGMA and MODEL_LEVEL; without them a level is not a pressure.
 */
HYBRID_A_COEFFICIENTS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 48);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

hybridACoefficientsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 48);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

hybridACoefficientsArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 48);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Hybrid sigma-pressure B coefficients, dimensionless, same length rule.
 */
HYBRID_B_COEFFICIENTS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 50);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

hybridBCoefficientsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 50);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

hybridBCoefficientsArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 50);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * True when the hybrid coefficients are stated at level INTERFACES
 * (NZ + 1 values) rather than at level centres (NZ values).
 */
INTERFACE_COEFFICIENTS():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 52);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Reference surface pressure used with the hybrid coefficients, pascals.
 * NaN when not applicable.
 */
SURFACE_PRESSURE_REFERENCE_PA():number {
  const offset = this.bb!.__offset(this.bb_pos, 54);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Memory layout of the packed volume. LEVEL_MAJOR is the only legal value.
 */
ARRAY_LAYOUT():agvArrayLayout {
  const offset = this.bb!.__offset(this.bb_pos, 56);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agvArrayLayout.UNSPECIFIED;
}

/**
 * Codec applied to the packed array, from the `$AGF` vocabulary.
 */
PAYLOAD_CODEC():agfPayloadCodec {
  const offset = this.bb!.__offset(this.bb_pos, 58);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : agfPayloadCodec.UNSPECIFIED;
}

/**
 * Inline post-codec bytes. LEGAL ONLY when PAYLOAD_SIZE_BYTES is at most
 * 1048576. MUST be absent when PAYLOAD_CID is present.
 */
PAYLOAD(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 60);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

payloadLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 60);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

payloadArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 60);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Content-addressed reference to the identical post-codec byte string.
 * MUST be absent when PAYLOAD is present, and MUST be used above 1048576
 * post-codec bytes — which is the normal case for a volume.
 */
PAYLOAD_CID():string|null
PAYLOAD_CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PAYLOAD_CID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 62);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Lowercase hex SHA-256 of the post-codec byte string. REQUIRED in BOTH
 * payload modes.
 */
PAYLOAD_SHA256():string
PAYLOAD_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
PAYLOAD_SHA256(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 64);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Length of the post-codec byte string, bytes. REQUIRED in BOTH payload
 * modes.
 */
PAYLOAD_SIZE_BYTES():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 66);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Where the volume came from. Required.
 */
PROVENANCE(obj?:AGVProvenance):AGVProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 68);
  return offset ? (obj || new AGVProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Free-text description of the volume, not of its vendor.
 */
DESCRIPTION():string|null
DESCRIPTION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DESCRIPTION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 70);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startAGV(builder:flatbuffers.Builder) {
  builder.startObject(34);
}

static addId(builder:flatbuffers.Builder, IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, IDOffset, 0);
}

static addEpoch(builder:flatbuffers.Builder, EPOCHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, EPOCHOffset, 0);
}

static addValidStart(builder:flatbuffers.Builder, VALID_STARTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, VALID_STARTOffset, 0);
}

static addValidEnd(builder:flatbuffers.Builder, VALID_ENDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, VALID_ENDOffset, 0);
}

static addForecastReferenceTime(builder:flatbuffers.Builder, FORECAST_REFERENCE_TIMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, FORECAST_REFERENCE_TIMEOffset, 0);
}

static addForecastLeadS(builder:flatbuffers.Builder, FORECAST_LEAD_S:number) {
  builder.addFieldFloat64(5, FORECAST_LEAD_S, 0.0);
}

static addVariable(builder:flatbuffers.Builder, VARIABLE:agfVariable) {
  builder.addFieldInt8(6, VARIABLE, agfVariable.UNSPECIFIED);
}

static addUnits(builder:flatbuffers.Builder, UNITSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, UNITSOffset, 0);
}

static addCategoryLabels(builder:flatbuffers.Builder, CATEGORY_LABELSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, CATEGORY_LABELSOffset, 0);
}

static createCategoryLabelsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCategoryLabelsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addDtype(builder:flatbuffers.Builder, DTYPE:agfDType) {
  builder.addFieldInt8(9, DTYPE, agfDType.UNSPECIFIED);
}

static addFillValue(builder:flatbuffers.Builder, FILL_VALUE:number) {
  builder.addFieldFloat64(10, FILL_VALUE, 0.0);
}

static addScaleFactor(builder:flatbuffers.Builder, SCALE_FACTOR:number) {
  builder.addFieldFloat64(11, SCALE_FACTOR, 1.0);
}

static addAddOffset(builder:flatbuffers.Builder, ADD_OFFSET:number) {
  builder.addFieldFloat64(12, ADD_OFFSET, 0.0);
}

static addNx(builder:flatbuffers.Builder, NX:number) {
  builder.addFieldInt32(13, NX, 0);
}

static addNy(builder:flatbuffers.Builder, NY:number) {
  builder.addFieldInt32(14, NY, 0);
}

static addRowOrder(builder:flatbuffers.Builder, ROW_ORDER:agfRowOrder) {
  builder.addFieldInt8(15, ROW_ORDER, agfRowOrder.UNSPECIFIED);
}

static addProjection(builder:flatbuffers.Builder, PROJECTION:agfProjection) {
  builder.addFieldInt8(16, PROJECTION, agfProjection.UNSPECIFIED);
}

static addProjectionParameters(builder:flatbuffers.Builder, PROJECTION_PARAMETERSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(17, PROJECTION_PARAMETERSOffset, 0);
}

static addNz(builder:flatbuffers.Builder, NZ:number) {
  builder.addFieldInt32(18, NZ, 0);
}

static addVerticalCoordinate(builder:flatbuffers.Builder, VERTICAL_COORDINATE:agvVerticalCoordinate) {
  builder.addFieldInt8(19, VERTICAL_COORDINATE, agvVerticalCoordinate.UNSPECIFIED);
}

static addLevels(builder:flatbuffers.Builder, LEVELSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(20, LEVELSOffset, 0);
}

static createLevelsVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createLevelsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createLevelsVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startLevelsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addLevelOrder(builder:flatbuffers.Builder, LEVEL_ORDER:agvLevelOrder) {
  builder.addFieldInt8(21, LEVEL_ORDER, agvLevelOrder.UNSPECIFIED);
}

static addHybridACoefficients(builder:flatbuffers.Builder, HYBRID_A_COEFFICIENTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(22, HYBRID_A_COEFFICIENTSOffset, 0);
}

static createHybridACoefficientsVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createHybridACoefficientsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createHybridACoefficientsVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startHybridACoefficientsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addHybridBCoefficients(builder:flatbuffers.Builder, HYBRID_B_COEFFICIENTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(23, HYBRID_B_COEFFICIENTSOffset, 0);
}

static createHybridBCoefficientsVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createHybridBCoefficientsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createHybridBCoefficientsVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startHybridBCoefficientsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addInterfaceCoefficients(builder:flatbuffers.Builder, INTERFACE_COEFFICIENTS:boolean) {
  builder.addFieldInt8(24, +INTERFACE_COEFFICIENTS, +false);
}

static addSurfacePressureReferencePa(builder:flatbuffers.Builder, SURFACE_PRESSURE_REFERENCE_PA:number) {
  builder.addFieldFloat64(25, SURFACE_PRESSURE_REFERENCE_PA, 0.0);
}

static addArrayLayout(builder:flatbuffers.Builder, ARRAY_LAYOUT:agvArrayLayout) {
  builder.addFieldInt8(26, ARRAY_LAYOUT, agvArrayLayout.UNSPECIFIED);
}

static addPayloadCodec(builder:flatbuffers.Builder, PAYLOAD_CODEC:agfPayloadCodec) {
  builder.addFieldInt8(27, PAYLOAD_CODEC, agfPayloadCodec.UNSPECIFIED);
}

static addPayload(builder:flatbuffers.Builder, PAYLOADOffset:flatbuffers.Offset) {
  builder.addFieldOffset(28, PAYLOADOffset, 0);
}

static createPayloadVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPayloadVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addPayloadCid(builder:flatbuffers.Builder, PAYLOAD_CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(29, PAYLOAD_CIDOffset, 0);
}

static addPayloadSha256(builder:flatbuffers.Builder, PAYLOAD_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(30, PAYLOAD_SHA256Offset, 0);
}

static addPayloadSizeBytes(builder:flatbuffers.Builder, PAYLOAD_SIZE_BYTES:bigint) {
  builder.addFieldInt64(31, PAYLOAD_SIZE_BYTES, BigInt('0'));
}

static addProvenance(builder:flatbuffers.Builder, PROVENANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(32, PROVENANCEOffset, 0);
}

static addDescription(builder:flatbuffers.Builder, DESCRIPTIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(33, DESCRIPTIONOffset, 0);
}

static endAGV(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // ID
  builder.requiredField(offset, 6) // EPOCH
  builder.requiredField(offset, 18) // UNITS
  builder.requiredField(offset, 38) // PROJECTION_PARAMETERS
  builder.requiredField(offset, 44) // LEVELS
  builder.requiredField(offset, 64) // PAYLOAD_SHA256
  builder.requiredField(offset, 68) // PROVENANCE
  return offset;
}

static finishAGVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$AGV');
}

static finishSizePrefixedAGVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$AGV', true);
}


unpack(): AGVT {
  return new AGVT(
    this.ID(),
    this.EPOCH(),
    this.VALID_START(),
    this.VALID_END(),
    this.FORECAST_REFERENCE_TIME(),
    this.FORECAST_LEAD_S(),
    this.VARIABLE(),
    this.UNITS(),
    this.bb!.createScalarList<string>(this.CATEGORY_LABELS.bind(this), this.categoryLabelsLength()),
    this.DTYPE(),
    this.FILL_VALUE(),
    this.SCALE_FACTOR(),
    this.ADD_OFFSET(),
    this.NX(),
    this.NY(),
    this.ROW_ORDER(),
    this.PROJECTION(),
    (this.PROJECTION_PARAMETERS() !== null ? this.PROJECTION_PARAMETERS()!.unpack() : null),
    this.NZ(),
    this.VERTICAL_COORDINATE(),
    this.bb!.createScalarList<number>(this.LEVELS.bind(this), this.levelsLength()),
    this.LEVEL_ORDER(),
    this.bb!.createScalarList<number>(this.HYBRID_A_COEFFICIENTS.bind(this), this.hybridACoefficientsLength()),
    this.bb!.createScalarList<number>(this.HYBRID_B_COEFFICIENTS.bind(this), this.hybridBCoefficientsLength()),
    this.INTERFACE_COEFFICIENTS(),
    this.SURFACE_PRESSURE_REFERENCE_PA(),
    this.ARRAY_LAYOUT(),
    this.PAYLOAD_CODEC(),
    this.bb!.createScalarList<number>(this.PAYLOAD.bind(this), this.payloadLength()),
    this.PAYLOAD_CID(),
    this.PAYLOAD_SHA256(),
    this.PAYLOAD_SIZE_BYTES(),
    (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null),
    this.DESCRIPTION()
  );
}


unpackTo(_o: AGVT): void {
  _o.ID = this.ID();
  _o.EPOCH = this.EPOCH();
  _o.VALID_START = this.VALID_START();
  _o.VALID_END = this.VALID_END();
  _o.FORECAST_REFERENCE_TIME = this.FORECAST_REFERENCE_TIME();
  _o.FORECAST_LEAD_S = this.FORECAST_LEAD_S();
  _o.VARIABLE = this.VARIABLE();
  _o.UNITS = this.UNITS();
  _o.CATEGORY_LABELS = this.bb!.createScalarList<string>(this.CATEGORY_LABELS.bind(this), this.categoryLabelsLength());
  _o.DTYPE = this.DTYPE();
  _o.FILL_VALUE = this.FILL_VALUE();
  _o.SCALE_FACTOR = this.SCALE_FACTOR();
  _o.ADD_OFFSET = this.ADD_OFFSET();
  _o.NX = this.NX();
  _o.NY = this.NY();
  _o.ROW_ORDER = this.ROW_ORDER();
  _o.PROJECTION = this.PROJECTION();
  _o.PROJECTION_PARAMETERS = (this.PROJECTION_PARAMETERS() !== null ? this.PROJECTION_PARAMETERS()!.unpack() : null);
  _o.NZ = this.NZ();
  _o.VERTICAL_COORDINATE = this.VERTICAL_COORDINATE();
  _o.LEVELS = this.bb!.createScalarList<number>(this.LEVELS.bind(this), this.levelsLength());
  _o.LEVEL_ORDER = this.LEVEL_ORDER();
  _o.HYBRID_A_COEFFICIENTS = this.bb!.createScalarList<number>(this.HYBRID_A_COEFFICIENTS.bind(this), this.hybridACoefficientsLength());
  _o.HYBRID_B_COEFFICIENTS = this.bb!.createScalarList<number>(this.HYBRID_B_COEFFICIENTS.bind(this), this.hybridBCoefficientsLength());
  _o.INTERFACE_COEFFICIENTS = this.INTERFACE_COEFFICIENTS();
  _o.SURFACE_PRESSURE_REFERENCE_PA = this.SURFACE_PRESSURE_REFERENCE_PA();
  _o.ARRAY_LAYOUT = this.ARRAY_LAYOUT();
  _o.PAYLOAD_CODEC = this.PAYLOAD_CODEC();
  _o.PAYLOAD = this.bb!.createScalarList<number>(this.PAYLOAD.bind(this), this.payloadLength());
  _o.PAYLOAD_CID = this.PAYLOAD_CID();
  _o.PAYLOAD_SHA256 = this.PAYLOAD_SHA256();
  _o.PAYLOAD_SIZE_BYTES = this.PAYLOAD_SIZE_BYTES();
  _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null);
  _o.DESCRIPTION = this.DESCRIPTION();
}
}

export class AGVT implements flatbuffers.IGeneratedObject {
constructor(
  public ID: string|Uint8Array|null = null,
  public EPOCH: string|Uint8Array|null = null,
  public VALID_START: string|Uint8Array|null = null,
  public VALID_END: string|Uint8Array|null = null,
  public FORECAST_REFERENCE_TIME: string|Uint8Array|null = null,
  public FORECAST_LEAD_S: number = 0.0,
  public VARIABLE: agfVariable = agfVariable.UNSPECIFIED,
  public UNITS: string|Uint8Array|null = null,
  public CATEGORY_LABELS: (string)[] = [],
  public DTYPE: agfDType = agfDType.UNSPECIFIED,
  public FILL_VALUE: number = 0.0,
  public SCALE_FACTOR: number = 1.0,
  public ADD_OFFSET: number = 0.0,
  public NX: number = 0,
  public NY: number = 0,
  public ROW_ORDER: agfRowOrder = agfRowOrder.UNSPECIFIED,
  public PROJECTION: agfProjection = agfProjection.UNSPECIFIED,
  public PROJECTION_PARAMETERS: AGFProjectionParametersT|null = null,
  public NZ: number = 0,
  public VERTICAL_COORDINATE: agvVerticalCoordinate = agvVerticalCoordinate.UNSPECIFIED,
  public LEVELS: (number)[] = [],
  public LEVEL_ORDER: agvLevelOrder = agvLevelOrder.UNSPECIFIED,
  public HYBRID_A_COEFFICIENTS: (number)[] = [],
  public HYBRID_B_COEFFICIENTS: (number)[] = [],
  public INTERFACE_COEFFICIENTS: boolean = false,
  public SURFACE_PRESSURE_REFERENCE_PA: number = 0.0,
  public ARRAY_LAYOUT: agvArrayLayout = agvArrayLayout.UNSPECIFIED,
  public PAYLOAD_CODEC: agfPayloadCodec = agfPayloadCodec.UNSPECIFIED,
  public PAYLOAD: (number)[] = [],
  public PAYLOAD_CID: string|Uint8Array|null = null,
  public PAYLOAD_SHA256: string|Uint8Array|null = null,
  public PAYLOAD_SIZE_BYTES: bigint = BigInt('0'),
  public PROVENANCE: AGVProvenanceT|null = null,
  public DESCRIPTION: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ID = (this.ID !== null ? builder.createString(this.ID!) : 0);
  const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH!) : 0);
  const VALID_START = (this.VALID_START !== null ? builder.createString(this.VALID_START!) : 0);
  const VALID_END = (this.VALID_END !== null ? builder.createString(this.VALID_END!) : 0);
  const FORECAST_REFERENCE_TIME = (this.FORECAST_REFERENCE_TIME !== null ? builder.createString(this.FORECAST_REFERENCE_TIME!) : 0);
  const UNITS = (this.UNITS !== null ? builder.createString(this.UNITS!) : 0);
  const CATEGORY_LABELS = AGV.createCategoryLabelsVector(builder, builder.createObjectOffsetList(this.CATEGORY_LABELS));
  const PROJECTION_PARAMETERS = (this.PROJECTION_PARAMETERS !== null ? this.PROJECTION_PARAMETERS!.pack(builder) : 0);
  const LEVELS = AGV.createLevelsVector(builder, this.LEVELS);
  const HYBRID_A_COEFFICIENTS = AGV.createHybridACoefficientsVector(builder, this.HYBRID_A_COEFFICIENTS);
  const HYBRID_B_COEFFICIENTS = AGV.createHybridBCoefficientsVector(builder, this.HYBRID_B_COEFFICIENTS);
  const PAYLOAD = AGV.createPayloadVector(builder, this.PAYLOAD);
  const PAYLOAD_CID = (this.PAYLOAD_CID !== null ? builder.createString(this.PAYLOAD_CID!) : 0);
  const PAYLOAD_SHA256 = (this.PAYLOAD_SHA256 !== null ? builder.createString(this.PAYLOAD_SHA256!) : 0);
  const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE!.pack(builder) : 0);
  const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION!) : 0);

  AGV.startAGV(builder);
  AGV.addId(builder, ID);
  AGV.addEpoch(builder, EPOCH);
  AGV.addValidStart(builder, VALID_START);
  AGV.addValidEnd(builder, VALID_END);
  AGV.addForecastReferenceTime(builder, FORECAST_REFERENCE_TIME);
  AGV.addForecastLeadS(builder, this.FORECAST_LEAD_S);
  AGV.addVariable(builder, this.VARIABLE);
  AGV.addUnits(builder, UNITS);
  AGV.addCategoryLabels(builder, CATEGORY_LABELS);
  AGV.addDtype(builder, this.DTYPE);
  AGV.addFillValue(builder, this.FILL_VALUE);
  AGV.addScaleFactor(builder, this.SCALE_FACTOR);
  AGV.addAddOffset(builder, this.ADD_OFFSET);
  AGV.addNx(builder, this.NX);
  AGV.addNy(builder, this.NY);
  AGV.addRowOrder(builder, this.ROW_ORDER);
  AGV.addProjection(builder, this.PROJECTION);
  AGV.addProjectionParameters(builder, PROJECTION_PARAMETERS);
  AGV.addNz(builder, this.NZ);
  AGV.addVerticalCoordinate(builder, this.VERTICAL_COORDINATE);
  AGV.addLevels(builder, LEVELS);
  AGV.addLevelOrder(builder, this.LEVEL_ORDER);
  AGV.addHybridACoefficients(builder, HYBRID_A_COEFFICIENTS);
  AGV.addHybridBCoefficients(builder, HYBRID_B_COEFFICIENTS);
  AGV.addInterfaceCoefficients(builder, this.INTERFACE_COEFFICIENTS);
  AGV.addSurfacePressureReferencePa(builder, this.SURFACE_PRESSURE_REFERENCE_PA);
  AGV.addArrayLayout(builder, this.ARRAY_LAYOUT);
  AGV.addPayloadCodec(builder, this.PAYLOAD_CODEC);
  AGV.addPayload(builder, PAYLOAD);
  AGV.addPayloadCid(builder, PAYLOAD_CID);
  AGV.addPayloadSha256(builder, PAYLOAD_SHA256);
  AGV.addPayloadSizeBytes(builder, this.PAYLOAD_SIZE_BYTES);
  AGV.addProvenance(builder, PROVENANCE);
  AGV.addDescription(builder, DESCRIPTION);

  return AGV.endAGV(builder);
}
}
