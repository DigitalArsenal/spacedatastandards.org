// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { snwErrorCode } from './snwErrorCode.js';


/**
 * Batch target detection response header.
 */
export class SNWBatchResponse implements flatbuffers.IUnpackableObject<SNWBatchResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SNWBatchResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSNWBatchResponse(bb:flatbuffers.ByteBuffer, obj?:SNWBatchResponse):SNWBatchResponse {
  return (obj || new SNWBatchResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSNWBatchResponse(bb:flatbuffers.ByteBuffer, obj?:SNWBatchResponse):SNWBatchResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SNWBatchResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Number of detection records written to OUTPUT_OFFSET.
 */
COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Number of targets whose SNR met the detection threshold.
 */
DETECTED_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Byte offset where the detectionResult[] stream starts.
 */
OUTPUT_OFFSET():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Error code (0 == OK).
 */
ERROR_CODE():snwErrorCode {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : snwErrorCode.OK;
}

/**
 * Optional error message when ERROR_CODE != OK.
 */
ERROR_MESSAGE():string|null
ERROR_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ERROR_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startSNWBatchResponse(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addCount(builder:flatbuffers.Builder, COUNT:number) {
  builder.addFieldInt32(0, COUNT, 0);
}

static addDetectedCount(builder:flatbuffers.Builder, DETECTED_COUNT:number) {
  builder.addFieldInt32(1, DETECTED_COUNT, 0);
}

static addOutputOffset(builder:flatbuffers.Builder, OUTPUT_OFFSET:number) {
  builder.addFieldInt32(2, OUTPUT_OFFSET, 0);
}

static addErrorCode(builder:flatbuffers.Builder, ERROR_CODE:snwErrorCode) {
  builder.addFieldInt32(3, ERROR_CODE, snwErrorCode.OK);
}

static addErrorMessage(builder:flatbuffers.Builder, ERROR_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, ERROR_MESSAGEOffset, 0);
}

static endSNWBatchResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSNWBatchResponse(builder:flatbuffers.Builder, COUNT:number, DETECTED_COUNT:number, OUTPUT_OFFSET:number, ERROR_CODE:snwErrorCode, ERROR_MESSAGEOffset:flatbuffers.Offset):flatbuffers.Offset {
  SNWBatchResponse.startSNWBatchResponse(builder);
  SNWBatchResponse.addCount(builder, COUNT);
  SNWBatchResponse.addDetectedCount(builder, DETECTED_COUNT);
  SNWBatchResponse.addOutputOffset(builder, OUTPUT_OFFSET);
  SNWBatchResponse.addErrorCode(builder, ERROR_CODE);
  SNWBatchResponse.addErrorMessage(builder, ERROR_MESSAGEOffset);
  return SNWBatchResponse.endSNWBatchResponse(builder);
}

unpack(): SNWBatchResponseT {
  return new SNWBatchResponseT(
    this.COUNT(),
    this.DETECTED_COUNT(),
    this.OUTPUT_OFFSET(),
    this.ERROR_CODE(),
    this.ERROR_MESSAGE()
  );
}


unpackTo(_o: SNWBatchResponseT): void {
  _o.COUNT = this.COUNT();
  _o.DETECTED_COUNT = this.DETECTED_COUNT();
  _o.OUTPUT_OFFSET = this.OUTPUT_OFFSET();
  _o.ERROR_CODE = this.ERROR_CODE();
  _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
}
}

export class SNWBatchResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public COUNT: number = 0,
  public DETECTED_COUNT: number = 0,
  public OUTPUT_OFFSET: number = 0,
  public ERROR_CODE: snwErrorCode = snwErrorCode.OK,
  public ERROR_MESSAGE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE!) : 0);

  return SNWBatchResponse.createSNWBatchResponse(builder,
    this.COUNT,
    this.DETECTED_COUNT,
    this.OUTPUT_OFFSET,
    this.ERROR_CODE,
    ERROR_MESSAGE
  );
}
}
