// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Space Data Link Security (CCSDS 355.0-B-1)
 */
export class SDL implements flatbuffers.IUnpackableObject<SDLT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SDL {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSDL(bb:flatbuffers.ByteBuffer, obj?:SDL):SDL {
  return (obj || new SDL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSDL(bb:flatbuffers.ByteBuffer, obj?:SDL):SDL {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SDL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$SDL');
}

/**
 * Security Parameter Index
 */
SPI():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Initialization vector length in bytes
 */
IV_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Initialization vector
 */
IV(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

ivLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

ivArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * MAC length in bytes
 */
MAC_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Message authentication code
 */
MAC(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

macLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

macArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Pad length
 */
PAD_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Security payload
 */
PAYLOAD(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

payloadLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

payloadArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startSDL(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addSpi(builder:flatbuffers.Builder, SPI:number) {
  builder.addFieldInt16(0, SPI, 0);
}

static addIvLength(builder:flatbuffers.Builder, IV_LENGTH:number) {
  builder.addFieldInt8(1, IV_LENGTH, 0);
}

static addIv(builder:flatbuffers.Builder, IVOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, IVOffset, 0);
}

static createIvVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startIvVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addMacLength(builder:flatbuffers.Builder, MAC_LENGTH:number) {
  builder.addFieldInt8(3, MAC_LENGTH, 0);
}

static addMac(builder:flatbuffers.Builder, MACOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, MACOffset, 0);
}

static createMacVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startMacVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addPadLength(builder:flatbuffers.Builder, PAD_LENGTH:number) {
  builder.addFieldInt8(5, PAD_LENGTH, 0);
}

static addPayload(builder:flatbuffers.Builder, PAYLOADOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, PAYLOADOffset, 0);
}

static createPayloadVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPayloadVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endSDL(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishSDLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SDL');
}

static finishSizePrefixedSDLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SDL', true);
}

static createSDL(builder:flatbuffers.Builder, SPI:number, IV_LENGTH:number, IVOffset:flatbuffers.Offset, MAC_LENGTH:number, MACOffset:flatbuffers.Offset, PAD_LENGTH:number, PAYLOADOffset:flatbuffers.Offset):flatbuffers.Offset {
  SDL.startSDL(builder);
  SDL.addSpi(builder, SPI);
  SDL.addIvLength(builder, IV_LENGTH);
  SDL.addIv(builder, IVOffset);
  SDL.addMacLength(builder, MAC_LENGTH);
  SDL.addMac(builder, MACOffset);
  SDL.addPadLength(builder, PAD_LENGTH);
  SDL.addPayload(builder, PAYLOADOffset);
  return SDL.endSDL(builder);
}

unpack(): SDLT {
  return new SDLT(
    this.SPI(),
    this.IV_LENGTH(),
    this.bb!.createScalarList<number>(this.IV.bind(this), this.ivLength()),
    this.MAC_LENGTH(),
    this.bb!.createScalarList<number>(this.MAC.bind(this), this.macLength()),
    this.PAD_LENGTH(),
    this.bb!.createScalarList<number>(this.PAYLOAD.bind(this), this.payloadLength())
  );
}


unpackTo(_o: SDLT): void {
  _o.SPI = this.SPI();
  _o.IV_LENGTH = this.IV_LENGTH();
  _o.IV = this.bb!.createScalarList<number>(this.IV.bind(this), this.ivLength());
  _o.MAC_LENGTH = this.MAC_LENGTH();
  _o.MAC = this.bb!.createScalarList<number>(this.MAC.bind(this), this.macLength());
  _o.PAD_LENGTH = this.PAD_LENGTH();
  _o.PAYLOAD = this.bb!.createScalarList<number>(this.PAYLOAD.bind(this), this.payloadLength());
}
}

export class SDLT implements flatbuffers.IGeneratedObject {
constructor(
  public SPI: number = 0,
  public IV_LENGTH: number = 0,
  public IV: (number)[] = [],
  public MAC_LENGTH: number = 0,
  public MAC: (number)[] = [],
  public PAD_LENGTH: number = 0,
  public PAYLOAD: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const IV = SDL.createIvVector(builder, this.IV);
  const MAC = SDL.createMacVector(builder, this.MAC);
  const PAYLOAD = SDL.createPayloadVector(builder, this.PAYLOAD);

  return SDL.createSDL(builder,
    this.SPI,
    this.IV_LENGTH,
    IV,
    this.MAC_LENGTH,
    MAC,
    this.PAD_LENGTH,
    PAYLOAD
  );
}
}
