// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Path reconstruction result.
 */
export class FPCPathResult implements flatbuffers.IUnpackableObject<FPCPathResultT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FPCPathResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFPCPathResult(bb:flatbuffers.ByteBuffer, obj?:FPCPathResult):FPCPathResult {
  return (obj || new FPCPathResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFPCPathResult(bb:flatbuffers.ByteBuffer, obj?:FPCPathResult):FPCPathResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FPCPathResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Source vertex index from the latest SSSP solve.
 */
SOURCE():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Requested target vertex index.
 */
TARGET():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Whether a path exists from SOURCE to TARGET.
 */
REACHABLE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Total distance from SOURCE to TARGET (Infinity if unreachable).
 */
DISTANCE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Vertex sequence from SOURCE to TARGET.
 */
PATH(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

pathLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

pathArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startFPCPathResult(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addSource(builder:flatbuffers.Builder, SOURCE:number) {
  builder.addFieldInt32(0, SOURCE, 0);
}

static addTarget(builder:flatbuffers.Builder, TARGET:number) {
  builder.addFieldInt32(1, TARGET, 0);
}

static addReachable(builder:flatbuffers.Builder, REACHABLE:boolean) {
  builder.addFieldInt8(2, +REACHABLE, +false);
}

static addDistance(builder:flatbuffers.Builder, DISTANCE:number) {
  builder.addFieldFloat64(3, DISTANCE, 0.0);
}

static addPath(builder:flatbuffers.Builder, PATHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, PATHOffset, 0);
}

static createPathVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createPathVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createPathVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startPathVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endFPCPathResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFPCPathResult(builder:flatbuffers.Builder, SOURCE:number, TARGET:number, REACHABLE:boolean, DISTANCE:number, PATHOffset:flatbuffers.Offset):flatbuffers.Offset {
  FPCPathResult.startFPCPathResult(builder);
  FPCPathResult.addSource(builder, SOURCE);
  FPCPathResult.addTarget(builder, TARGET);
  FPCPathResult.addReachable(builder, REACHABLE);
  FPCPathResult.addDistance(builder, DISTANCE);
  FPCPathResult.addPath(builder, PATHOffset);
  return FPCPathResult.endFPCPathResult(builder);
}

unpack(): FPCPathResultT {
  return new FPCPathResultT(
    this.SOURCE(),
    this.TARGET(),
    this.REACHABLE(),
    this.DISTANCE(),
    this.bb!.createScalarList<number>(this.PATH.bind(this), this.pathLength())
  );
}


unpackTo(_o: FPCPathResultT): void {
  _o.SOURCE = this.SOURCE();
  _o.TARGET = this.TARGET();
  _o.REACHABLE = this.REACHABLE();
  _o.DISTANCE = this.DISTANCE();
  _o.PATH = this.bb!.createScalarList<number>(this.PATH.bind(this), this.pathLength());
}
}

export class FPCPathResultT implements flatbuffers.IGeneratedObject {
constructor(
  public SOURCE: number = 0,
  public TARGET: number = 0,
  public REACHABLE: boolean = false,
  public DISTANCE: number = 0.0,
  public PATH: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PATH = FPCPathResult.createPathVector(builder, this.PATH);

  return FPCPathResult.createFPCPathResult(builder,
    this.SOURCE,
    this.TARGET,
    this.REACHABLE,
    this.DISTANCE,
    PATH
  );
}
}
