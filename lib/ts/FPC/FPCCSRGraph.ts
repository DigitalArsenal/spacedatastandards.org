// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Pre-built CSR graph ingestion request.
 */
export class FPCCSRGraph implements flatbuffers.IUnpackableObject<FPCCSRGraphT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FPCCSRGraph {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFPCCSRGraph(bb:flatbuffers.ByteBuffer, obj?:FPCCSRGraph):FPCCSRGraph {
  return (obj || new FPCCSRGraph()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFPCCSRGraph(bb:flatbuffers.ByteBuffer, obj?:FPCCSRGraph):FPCCSRGraph {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FPCCSRGraph()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Number of vertices in the graph.
 */
VERTEX_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * CSR row offsets (V + 1 entries).
 */
OFFSETS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

offsetsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

offsetsArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * CSR destination indices.
 */
DESTINATIONS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

destinationsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

destinationsArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * CSR edge weights.
 */
WEIGHTS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

weightsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

weightsArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startFPCCSRGraph(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addVertexCount(builder:flatbuffers.Builder, VERTEX_COUNT:number) {
  builder.addFieldInt32(0, VERTEX_COUNT, 0);
}

static addOffsets(builder:flatbuffers.Builder, OFFSETSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, OFFSETSOffset, 0);
}

static createOffsetsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createOffsetsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createOffsetsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startOffsetsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addDestinations(builder:flatbuffers.Builder, DESTINATIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, DESTINATIONSOffset, 0);
}

static createDestinationsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createDestinationsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createDestinationsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startDestinationsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addWeights(builder:flatbuffers.Builder, WEIGHTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, WEIGHTSOffset, 0);
}

static createWeightsVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createWeightsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createWeightsVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startWeightsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static endFPCCSRGraph(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFPCCSRGraph(builder:flatbuffers.Builder, VERTEX_COUNT:number, OFFSETSOffset:flatbuffers.Offset, DESTINATIONSOffset:flatbuffers.Offset, WEIGHTSOffset:flatbuffers.Offset):flatbuffers.Offset {
  FPCCSRGraph.startFPCCSRGraph(builder);
  FPCCSRGraph.addVertexCount(builder, VERTEX_COUNT);
  FPCCSRGraph.addOffsets(builder, OFFSETSOffset);
  FPCCSRGraph.addDestinations(builder, DESTINATIONSOffset);
  FPCCSRGraph.addWeights(builder, WEIGHTSOffset);
  return FPCCSRGraph.endFPCCSRGraph(builder);
}

unpack(): FPCCSRGraphT {
  return new FPCCSRGraphT(
    this.VERTEX_COUNT(),
    this.bb!.createScalarList<number>(this.OFFSETS.bind(this), this.offsetsLength()),
    this.bb!.createScalarList<number>(this.DESTINATIONS.bind(this), this.destinationsLength()),
    this.bb!.createScalarList<number>(this.WEIGHTS.bind(this), this.weightsLength())
  );
}


unpackTo(_o: FPCCSRGraphT): void {
  _o.VERTEX_COUNT = this.VERTEX_COUNT();
  _o.OFFSETS = this.bb!.createScalarList<number>(this.OFFSETS.bind(this), this.offsetsLength());
  _o.DESTINATIONS = this.bb!.createScalarList<number>(this.DESTINATIONS.bind(this), this.destinationsLength());
  _o.WEIGHTS = this.bb!.createScalarList<number>(this.WEIGHTS.bind(this), this.weightsLength());
}
}

export class FPCCSRGraphT implements flatbuffers.IGeneratedObject {
constructor(
  public VERTEX_COUNT: number = 0,
  public OFFSETS: (number)[] = [],
  public DESTINATIONS: (number)[] = [],
  public WEIGHTS: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const OFFSETS = FPCCSRGraph.createOffsetsVector(builder, this.OFFSETS);
  const DESTINATIONS = FPCCSRGraph.createDestinationsVector(builder, this.DESTINATIONS);
  const WEIGHTS = FPCCSRGraph.createWeightsVector(builder, this.WEIGHTS);

  return FPCCSRGraph.createFPCCSRGraph(builder,
    this.VERTEX_COUNT,
    OFFSETS,
    DESTINATIONS,
    WEIGHTS
  );
}
}
