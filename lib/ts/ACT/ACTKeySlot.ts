// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One purpose-key slot of an operator.
 */
export class ACTKeySlot implements flatbuffers.IUnpackableObject<ACTKeySlotT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ACTKeySlot {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsACTKeySlot(bb:flatbuffers.ByteBuffer, obj?:ACTKeySlot):ACTKeySlot {
  return (obj || new ACTKeySlot()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsACTKeySlot(bb:flatbuffers.ByteBuffer, obj?:ACTKeySlot):ACTKeySlot {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ACTKeySlot()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Slot name, e.g. "signing", "encryption", "publication".
 */
SLOT():string|null
SLOT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SLOT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Derivation path currently in use.
 */
KEY_PATH():string|null
KEY_PATH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
KEY_PATH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Derivation path the next rotation will use.
 */
NEXT_PATH():string|null
NEXT_PATH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NEXT_PATH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

ROTATABLE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Key algorithm, e.g. "secp256k1", "ed25519", "x25519".
 */
ALGORITHM():string|null
ALGORITHM(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ALGORITHM(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startACTKeySlot(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addSlot(builder:flatbuffers.Builder, SLOTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SLOTOffset, 0);
}

static addKeyPath(builder:flatbuffers.Builder, KEY_PATHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, KEY_PATHOffset, 0);
}

static addNextPath(builder:flatbuffers.Builder, NEXT_PATHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, NEXT_PATHOffset, 0);
}

static addRotatable(builder:flatbuffers.Builder, ROTATABLE:boolean) {
  builder.addFieldInt8(3, +ROTATABLE, +false);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, ALGORITHMOffset, 0);
}

static endACTKeySlot(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createACTKeySlot(builder:flatbuffers.Builder, SLOTOffset:flatbuffers.Offset, KEY_PATHOffset:flatbuffers.Offset, NEXT_PATHOffset:flatbuffers.Offset, ROTATABLE:boolean, ALGORITHMOffset:flatbuffers.Offset):flatbuffers.Offset {
  ACTKeySlot.startACTKeySlot(builder);
  ACTKeySlot.addSlot(builder, SLOTOffset);
  ACTKeySlot.addKeyPath(builder, KEY_PATHOffset);
  ACTKeySlot.addNextPath(builder, NEXT_PATHOffset);
  ACTKeySlot.addRotatable(builder, ROTATABLE);
  ACTKeySlot.addAlgorithm(builder, ALGORITHMOffset);
  return ACTKeySlot.endACTKeySlot(builder);
}

unpack(): ACTKeySlotT {
  return new ACTKeySlotT(
    this.SLOT(),
    this.KEY_PATH(),
    this.NEXT_PATH(),
    this.ROTATABLE(),
    this.ALGORITHM()
  );
}


unpackTo(_o: ACTKeySlotT): void {
  _o.SLOT = this.SLOT();
  _o.KEY_PATH = this.KEY_PATH();
  _o.NEXT_PATH = this.NEXT_PATH();
  _o.ROTATABLE = this.ROTATABLE();
  _o.ALGORITHM = this.ALGORITHM();
}
}

export class ACTKeySlotT implements flatbuffers.IGeneratedObject {
constructor(
  public SLOT: string|Uint8Array|null = null,
  public KEY_PATH: string|Uint8Array|null = null,
  public NEXT_PATH: string|Uint8Array|null = null,
  public ROTATABLE: boolean = false,
  public ALGORITHM: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SLOT = (this.SLOT !== null ? builder.createString(this.SLOT!) : 0);
  const KEY_PATH = (this.KEY_PATH !== null ? builder.createString(this.KEY_PATH!) : 0);
  const NEXT_PATH = (this.NEXT_PATH !== null ? builder.createString(this.NEXT_PATH!) : 0);
  const ALGORITHM = (this.ALGORITHM !== null ? builder.createString(this.ALGORITHM!) : 0);

  return ACTKeySlot.createACTKeySlot(builder,
    SLOT,
    KEY_PATH,
    NEXT_PATH,
    this.ROTATABLE,
    ALGORITHM
  );
}
}
