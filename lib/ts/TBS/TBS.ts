// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TBSConsensus, TBSConsensusT } from './TBSConsensus.js';
import { TBSProvenance, TBSProvenanceT } from './TBSProvenance.js';
import { tbsRadioClass } from './tbsRadioClass.js';


/**
 * Terrestrial Base Station Site.
 *
 * One record describes ONE terrestrial cellular base-station site as
 * reconciled from every provider the publisher consulted. It is the wire
 * record for terrestrial mobile-network infrastructure, and it is not an
 * emitter model ($RFE), a band specification ($RFB), a geodetic tracking
 * station ($GST), a satellite ground station ($SIT), or an observation.
 *
 * A site cannot be re-serialized unattributed. SOURCES and CONSENSUS are both
 * required, so every published position carries the providers it came from and
 * the rule that reconciled them; a consumer that strips attribution has to
 * leave the standard to do it. Unknown values are ABSENT — never zero, never a
 * placeholder, never carried across from a neighbouring site.
 *
 * Identity is ID, the publisher's own. The network addressing fields are the
 * mobile network's, are assigned by its operator, and are reused worldwide;
 * none of them is a global key on its own.
 */
export class TBS implements flatbuffers.IUnpackableObject<TBST> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TBS {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTBS(bb:flatbuffers.ByteBuffer, obj?:TBS):TBS {
  return (obj || new TBS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTBS(bb:flatbuffers.ByteBuffer, obj?:TBS):TBS {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TBS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$TBS');
}

/**
 * Publisher-stable identifier for this site, unique within its record set.
 * Recommended form: "<mcc>-<mnc>-<area-code>-<cell-id>".
 */
ID():string
ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Winning provider's native site identifier, copied verbatim from the
 * SOURCES entry named by CONSENSUS.WINNING_PROVIDER_ID. Never a substitute
 * for ID and never stable across providers.
 */
NATIVE_ID():string|null
NATIVE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NATIVE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

RADIO():tbsRadioClass {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : tbsRadioClass.UNKNOWN;
}

/**
 * Mobile country code from the international mobile-subscriber identity
 * numbering plan. Zero means the source published none.
 */
MCC():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Mobile network code within MCC. Carried as an integer, so decimal leading
 * zeros are NOT preserved: a two- versus three-digit network code is
 * distinguished by the numbering plan of MCC, and the source's own text is
 * retained verbatim in TBSProvenance.NATIVE_ID. Zero means unpublished.
 */
MNC():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Location area code, used by GSM-, CDMA- and UMTS-class access. Zero means
 * unpublished. A site published with a location area code does not also
 * carry a tracking area code; publishing both asserts two access
 * generations for one cell.
 */
LAC():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Tracking area code, used by LTE- and NR-class access. Zero means
 * unpublished.
 */
TAC():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Cell identity verbatim from the source. A string because width, radix and
 * leading zeros are significant and differ by access class: a 16-bit cell
 * identity, a 28-bit evolved cell identity and a 36-bit new-radio cell
 * identity are not interchangeable integers, and this field never converts
 * between them.
 */
CELL_ID():string|null
CELL_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CELL_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Reconciled site position in decimal degrees on WGS 84, produced by the
 * merge described in CONSENSUS. Per-provider positions before
 * reconciliation live in SOURCES[].REPORTED_LATITUDE/REPORTED_LONGITUDE and
 * are never overwritten by this value.
 */
LATITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

LONGITUDE():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Radius in metres of the area within which the site's observations were
 * collected — a positional-uncertainty envelope, not a transmission range,
 * not a coverage guarantee, and never converted from another unit. Zero
 * means unpublished.
 */
RANGE_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Independent observations behind the winning report. Zero means the source
 * publishes no observation count, never that the site was never observed.
 */
SAMPLES():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Mean received signal strength in dBm across those observations, as the
 * source publishes it. Carried in dBm unconverted; a consumer needing watts
 * converts at the join, explicitly. Zero means unpublished.
 */
AVERAGE_SIGNAL_DBM():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * RFC 3339 UTC fixed-millisecond bounds of the observation window backing
 * the winning report.
 */
FIRST_OBSERVED():string|null
FIRST_OBSERVED(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
FIRST_OBSERVED(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

LAST_OBSERVED():string|null
LAST_OBSERVED(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
LAST_OBSERVED(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Network operator name verbatim as the cited source states it. A display
 * name, never an identifier: consumers must not treat it as a stable key.
 */
OPERATOR():string|null
OPERATOR(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
OPERATOR(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Operating centre frequency in MHz, following the $RFB convention. A
 * consumer joining to $IQC, which carries hertz, converts at the join,
 * explicitly. Zero means unpublished.
 */
FREQUENCY_MHZ():number {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Site or structure name verbatim when the source publishes one. Never
 * synthesized from coordinates, operator, or cell identity.
 */
SITE_NAME():string|null
SITE_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SITE_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * ISO 3166-1 alpha-2 country code, uppercase, as the source states it.
 * Derived from MCC only when the source itself derives it.
 */
COUNTRY_CODE():string|null
COUNTRY_CODE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
COUNTRY_CODE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Every provider consulted for this site, including providers that returned
 * nothing. Required: a site with no named sources is not publishable.
 */
SOURCES(index: number, obj?:TBSProvenance):TBSProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? (obj || new TBSProvenance()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

sourcesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * How the entries in SOURCES were reduced to LATITUDE/LONGITUDE. Required:
 * a published position always states the rule that produced it, including
 * SINGLE_SOURCE.
 */
CONSENSUS(obj?:TBSConsensus):TBSConsensus|null {
  const offset = this.bb!.__offset(this.bb_pos, 44);
  return offset ? (obj || new TBSConsensus()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startTBS(builder:flatbuffers.Builder) {
  builder.startObject(21);
}

static addId(builder:flatbuffers.Builder, IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, IDOffset, 0);
}

static addNativeId(builder:flatbuffers.Builder, NATIVE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, NATIVE_IDOffset, 0);
}

static addRadio(builder:flatbuffers.Builder, RADIO:tbsRadioClass) {
  builder.addFieldInt8(2, RADIO, tbsRadioClass.UNKNOWN);
}

static addMcc(builder:flatbuffers.Builder, MCC:number) {
  builder.addFieldInt32(3, MCC, 0);
}

static addMnc(builder:flatbuffers.Builder, MNC:number) {
  builder.addFieldInt32(4, MNC, 0);
}

static addLac(builder:flatbuffers.Builder, LAC:number) {
  builder.addFieldInt32(5, LAC, 0);
}

static addTac(builder:flatbuffers.Builder, TAC:number) {
  builder.addFieldInt32(6, TAC, 0);
}

static addCellId(builder:flatbuffers.Builder, CELL_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, CELL_IDOffset, 0);
}

static addLatitude(builder:flatbuffers.Builder, LATITUDE:number) {
  builder.addFieldFloat64(8, LATITUDE, 0.0);
}

static addLongitude(builder:flatbuffers.Builder, LONGITUDE:number) {
  builder.addFieldFloat64(9, LONGITUDE, 0.0);
}

static addRangeM(builder:flatbuffers.Builder, RANGE_M:number) {
  builder.addFieldFloat64(10, RANGE_M, 0.0);
}

static addSamples(builder:flatbuffers.Builder, SAMPLES:number) {
  builder.addFieldInt32(11, SAMPLES, 0);
}

static addAverageSignalDbm(builder:flatbuffers.Builder, AVERAGE_SIGNAL_DBM:number) {
  builder.addFieldFloat64(12, AVERAGE_SIGNAL_DBM, 0.0);
}

static addFirstObserved(builder:flatbuffers.Builder, FIRST_OBSERVEDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, FIRST_OBSERVEDOffset, 0);
}

static addLastObserved(builder:flatbuffers.Builder, LAST_OBSERVEDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, LAST_OBSERVEDOffset, 0);
}

static addOperator(builder:flatbuffers.Builder, OPERATOROffset:flatbuffers.Offset) {
  builder.addFieldOffset(15, OPERATOROffset, 0);
}

static addFrequencyMhz(builder:flatbuffers.Builder, FREQUENCY_MHZ:number) {
  builder.addFieldFloat64(16, FREQUENCY_MHZ, 0.0);
}

static addSiteName(builder:flatbuffers.Builder, SITE_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(17, SITE_NAMEOffset, 0);
}

static addCountryCode(builder:flatbuffers.Builder, COUNTRY_CODEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(18, COUNTRY_CODEOffset, 0);
}

static addSources(builder:flatbuffers.Builder, SOURCESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(19, SOURCESOffset, 0);
}

static createSourcesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSourcesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addConsensus(builder:flatbuffers.Builder, CONSENSUSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(20, CONSENSUSOffset, 0);
}

static endTBS(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // ID
  builder.requiredField(offset, 42) // SOURCES
  builder.requiredField(offset, 44) // CONSENSUS
  return offset;
}

static finishTBSBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TBS');
}

static finishSizePrefixedTBSBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TBS', true);
}


unpack(): TBST {
  return new TBST(
    this.ID(),
    this.NATIVE_ID(),
    this.RADIO(),
    this.MCC(),
    this.MNC(),
    this.LAC(),
    this.TAC(),
    this.CELL_ID(),
    this.LATITUDE(),
    this.LONGITUDE(),
    this.RANGE_M(),
    this.SAMPLES(),
    this.AVERAGE_SIGNAL_DBM(),
    this.FIRST_OBSERVED(),
    this.LAST_OBSERVED(),
    this.OPERATOR(),
    this.FREQUENCY_MHZ(),
    this.SITE_NAME(),
    this.COUNTRY_CODE(),
    this.bb!.createObjList<TBSProvenance, TBSProvenanceT>(this.SOURCES.bind(this), this.sourcesLength()),
    (this.CONSENSUS() !== null ? this.CONSENSUS()!.unpack() : null)
  );
}


unpackTo(_o: TBST): void {
  _o.ID = this.ID();
  _o.NATIVE_ID = this.NATIVE_ID();
  _o.RADIO = this.RADIO();
  _o.MCC = this.MCC();
  _o.MNC = this.MNC();
  _o.LAC = this.LAC();
  _o.TAC = this.TAC();
  _o.CELL_ID = this.CELL_ID();
  _o.LATITUDE = this.LATITUDE();
  _o.LONGITUDE = this.LONGITUDE();
  _o.RANGE_M = this.RANGE_M();
  _o.SAMPLES = this.SAMPLES();
  _o.AVERAGE_SIGNAL_DBM = this.AVERAGE_SIGNAL_DBM();
  _o.FIRST_OBSERVED = this.FIRST_OBSERVED();
  _o.LAST_OBSERVED = this.LAST_OBSERVED();
  _o.OPERATOR = this.OPERATOR();
  _o.FREQUENCY_MHZ = this.FREQUENCY_MHZ();
  _o.SITE_NAME = this.SITE_NAME();
  _o.COUNTRY_CODE = this.COUNTRY_CODE();
  _o.SOURCES = this.bb!.createObjList<TBSProvenance, TBSProvenanceT>(this.SOURCES.bind(this), this.sourcesLength());
  _o.CONSENSUS = (this.CONSENSUS() !== null ? this.CONSENSUS()!.unpack() : null);
}
}

export class TBST implements flatbuffers.IGeneratedObject {
constructor(
  public ID: string|Uint8Array|null = null,
  public NATIVE_ID: string|Uint8Array|null = null,
  public RADIO: tbsRadioClass = tbsRadioClass.UNKNOWN,
  public MCC: number = 0,
  public MNC: number = 0,
  public LAC: number = 0,
  public TAC: number = 0,
  public CELL_ID: string|Uint8Array|null = null,
  public LATITUDE: number = 0.0,
  public LONGITUDE: number = 0.0,
  public RANGE_M: number = 0.0,
  public SAMPLES: number = 0,
  public AVERAGE_SIGNAL_DBM: number = 0.0,
  public FIRST_OBSERVED: string|Uint8Array|null = null,
  public LAST_OBSERVED: string|Uint8Array|null = null,
  public OPERATOR: string|Uint8Array|null = null,
  public FREQUENCY_MHZ: number = 0.0,
  public SITE_NAME: string|Uint8Array|null = null,
  public COUNTRY_CODE: string|Uint8Array|null = null,
  public SOURCES: (TBSProvenanceT)[] = [],
  public CONSENSUS: TBSConsensusT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ID = (this.ID !== null ? builder.createString(this.ID!) : 0);
  const NATIVE_ID = (this.NATIVE_ID !== null ? builder.createString(this.NATIVE_ID!) : 0);
  const CELL_ID = (this.CELL_ID !== null ? builder.createString(this.CELL_ID!) : 0);
  const FIRST_OBSERVED = (this.FIRST_OBSERVED !== null ? builder.createString(this.FIRST_OBSERVED!) : 0);
  const LAST_OBSERVED = (this.LAST_OBSERVED !== null ? builder.createString(this.LAST_OBSERVED!) : 0);
  const OPERATOR = (this.OPERATOR !== null ? builder.createString(this.OPERATOR!) : 0);
  const SITE_NAME = (this.SITE_NAME !== null ? builder.createString(this.SITE_NAME!) : 0);
  const COUNTRY_CODE = (this.COUNTRY_CODE !== null ? builder.createString(this.COUNTRY_CODE!) : 0);
  const SOURCES = TBS.createSourcesVector(builder, builder.createObjectOffsetList(this.SOURCES));
  const CONSENSUS = (this.CONSENSUS !== null ? this.CONSENSUS!.pack(builder) : 0);

  TBS.startTBS(builder);
  TBS.addId(builder, ID);
  TBS.addNativeId(builder, NATIVE_ID);
  TBS.addRadio(builder, this.RADIO);
  TBS.addMcc(builder, this.MCC);
  TBS.addMnc(builder, this.MNC);
  TBS.addLac(builder, this.LAC);
  TBS.addTac(builder, this.TAC);
  TBS.addCellId(builder, CELL_ID);
  TBS.addLatitude(builder, this.LATITUDE);
  TBS.addLongitude(builder, this.LONGITUDE);
  TBS.addRangeM(builder, this.RANGE_M);
  TBS.addSamples(builder, this.SAMPLES);
  TBS.addAverageSignalDbm(builder, this.AVERAGE_SIGNAL_DBM);
  TBS.addFirstObserved(builder, FIRST_OBSERVED);
  TBS.addLastObserved(builder, LAST_OBSERVED);
  TBS.addOperator(builder, OPERATOR);
  TBS.addFrequencyMhz(builder, this.FREQUENCY_MHZ);
  TBS.addSiteName(builder, SITE_NAME);
  TBS.addCountryCode(builder, COUNTRY_CODE);
  TBS.addSources(builder, SOURCES);
  TBS.addConsensus(builder, CONSENSUS);

  return TBS.endTBS(builder);
}
}
