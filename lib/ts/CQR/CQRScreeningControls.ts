// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TIMInstant, TIMInstantT } from './TIMInstant.js';
import { cqrProbabilityAlgorithm } from './cqrProbabilityAlgorithm.js';


/**
 * Explicit UTC search window and positive resolution, SI seconds/metres.
 */
export class CQRScreeningControls implements flatbuffers.IUnpackableObject<CQRScreeningControlsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CQRScreeningControls {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCQRScreeningControls(bb:flatbuffers.ByteBuffer, obj?:CQRScreeningControls):CQRScreeningControls {
  return (obj || new CQRScreeningControls()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCQRScreeningControls(bb:flatbuffers.ByteBuffer, obj?:CQRScreeningControls):CQRScreeningControls {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CQRScreeningControls()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

START_EPOCH(obj?:TIMInstant):TIMInstant|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new TIMInstant()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

DURATION_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 604800.0;
}

THRESHOLD_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 5000.0;
}

REQUESTED_WORKERS():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 1;
}

COARSE_STEP_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 60.0;
}

REFINEMENT_TOLERANCE_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.001;
}

COMBINED_RADIUS_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 10.0;
}

USE_KD_TREE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : true;
}

USE_DYNAMIC_WINDOW():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : true;
}

USE_PERIGEE_FILTER():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : true;
}

/**
 * Optional progress cadence; PIV carries backpressure, not scientific data.
 */
PROGRESS_INTERVAL_SECONDS():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * True when PROGRESS_INTERVAL_SECONDS carries a value; false means absent.
 */
HAS_PROGRESS_INTERVAL_SECONDS():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

ALGORITHM():cqrProbabilityAlgorithm {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : cqrProbabilityAlgorithm.ALFANO_MAXIMUM;
}

static startCQRScreeningControls(builder:flatbuffers.Builder) {
  builder.startObject(13);
}

static addStartEpoch(builder:flatbuffers.Builder, START_EPOCHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, START_EPOCHOffset, 0);
}

static addDurationSeconds(builder:flatbuffers.Builder, DURATION_SECONDS:number) {
  builder.addFieldFloat64(1, DURATION_SECONDS, 604800.0);
}

static addThresholdM(builder:flatbuffers.Builder, THRESHOLD_M:number) {
  builder.addFieldFloat64(2, THRESHOLD_M, 5000.0);
}

static addRequestedWorkers(builder:flatbuffers.Builder, REQUESTED_WORKERS:number) {
  builder.addFieldInt32(3, REQUESTED_WORKERS, 1);
}

static addCoarseStepSeconds(builder:flatbuffers.Builder, COARSE_STEP_SECONDS:number) {
  builder.addFieldFloat64(4, COARSE_STEP_SECONDS, 60.0);
}

static addRefinementToleranceSeconds(builder:flatbuffers.Builder, REFINEMENT_TOLERANCE_SECONDS:number) {
  builder.addFieldFloat64(5, REFINEMENT_TOLERANCE_SECONDS, 0.001);
}

static addCombinedRadiusM(builder:flatbuffers.Builder, COMBINED_RADIUS_M:number) {
  builder.addFieldFloat64(6, COMBINED_RADIUS_M, 10.0);
}

static addUseKdTree(builder:flatbuffers.Builder, USE_KD_TREE:boolean) {
  builder.addFieldInt8(7, +USE_KD_TREE, +true);
}

static addUseDynamicWindow(builder:flatbuffers.Builder, USE_DYNAMIC_WINDOW:boolean) {
  builder.addFieldInt8(8, +USE_DYNAMIC_WINDOW, +true);
}

static addUsePerigeeFilter(builder:flatbuffers.Builder, USE_PERIGEE_FILTER:boolean) {
  builder.addFieldInt8(9, +USE_PERIGEE_FILTER, +true);
}

static addProgressIntervalSeconds(builder:flatbuffers.Builder, PROGRESS_INTERVAL_SECONDS:number) {
  builder.addFieldFloat64(10, PROGRESS_INTERVAL_SECONDS, 0.0);
}

static addHasProgressIntervalSeconds(builder:flatbuffers.Builder, HAS_PROGRESS_INTERVAL_SECONDS:boolean) {
  builder.addFieldInt8(11, +HAS_PROGRESS_INTERVAL_SECONDS, +false);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHM:cqrProbabilityAlgorithm) {
  builder.addFieldInt8(12, ALGORITHM, cqrProbabilityAlgorithm.ALFANO_MAXIMUM);
}

static endCQRScreeningControls(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // START_EPOCH
  return offset;
}

static createCQRScreeningControls(builder:flatbuffers.Builder, START_EPOCHOffset:flatbuffers.Offset, DURATION_SECONDS:number, THRESHOLD_M:number, REQUESTED_WORKERS:number, COARSE_STEP_SECONDS:number, REFINEMENT_TOLERANCE_SECONDS:number, COMBINED_RADIUS_M:number, USE_KD_TREE:boolean, USE_DYNAMIC_WINDOW:boolean, USE_PERIGEE_FILTER:boolean, PROGRESS_INTERVAL_SECONDS:number, HAS_PROGRESS_INTERVAL_SECONDS:boolean, ALGORITHM:cqrProbabilityAlgorithm):flatbuffers.Offset {
  CQRScreeningControls.startCQRScreeningControls(builder);
  CQRScreeningControls.addStartEpoch(builder, START_EPOCHOffset);
  CQRScreeningControls.addDurationSeconds(builder, DURATION_SECONDS);
  CQRScreeningControls.addThresholdM(builder, THRESHOLD_M);
  CQRScreeningControls.addRequestedWorkers(builder, REQUESTED_WORKERS);
  CQRScreeningControls.addCoarseStepSeconds(builder, COARSE_STEP_SECONDS);
  CQRScreeningControls.addRefinementToleranceSeconds(builder, REFINEMENT_TOLERANCE_SECONDS);
  CQRScreeningControls.addCombinedRadiusM(builder, COMBINED_RADIUS_M);
  CQRScreeningControls.addUseKdTree(builder, USE_KD_TREE);
  CQRScreeningControls.addUseDynamicWindow(builder, USE_DYNAMIC_WINDOW);
  CQRScreeningControls.addUsePerigeeFilter(builder, USE_PERIGEE_FILTER);
  CQRScreeningControls.addProgressIntervalSeconds(builder, PROGRESS_INTERVAL_SECONDS);
  CQRScreeningControls.addHasProgressIntervalSeconds(builder, HAS_PROGRESS_INTERVAL_SECONDS);
  CQRScreeningControls.addAlgorithm(builder, ALGORITHM);
  return CQRScreeningControls.endCQRScreeningControls(builder);
}

unpack(): CQRScreeningControlsT {
  return new CQRScreeningControlsT(
    (this.START_EPOCH() !== null ? this.START_EPOCH()!.unpack() : null),
    this.DURATION_SECONDS(),
    this.THRESHOLD_M(),
    this.REQUESTED_WORKERS(),
    this.COARSE_STEP_SECONDS(),
    this.REFINEMENT_TOLERANCE_SECONDS(),
    this.COMBINED_RADIUS_M(),
    this.USE_KD_TREE(),
    this.USE_DYNAMIC_WINDOW(),
    this.USE_PERIGEE_FILTER(),
    this.PROGRESS_INTERVAL_SECONDS(),
    this.HAS_PROGRESS_INTERVAL_SECONDS(),
    this.ALGORITHM()
  );
}


unpackTo(_o: CQRScreeningControlsT): void {
  _o.START_EPOCH = (this.START_EPOCH() !== null ? this.START_EPOCH()!.unpack() : null);
  _o.DURATION_SECONDS = this.DURATION_SECONDS();
  _o.THRESHOLD_M = this.THRESHOLD_M();
  _o.REQUESTED_WORKERS = this.REQUESTED_WORKERS();
  _o.COARSE_STEP_SECONDS = this.COARSE_STEP_SECONDS();
  _o.REFINEMENT_TOLERANCE_SECONDS = this.REFINEMENT_TOLERANCE_SECONDS();
  _o.COMBINED_RADIUS_M = this.COMBINED_RADIUS_M();
  _o.USE_KD_TREE = this.USE_KD_TREE();
  _o.USE_DYNAMIC_WINDOW = this.USE_DYNAMIC_WINDOW();
  _o.USE_PERIGEE_FILTER = this.USE_PERIGEE_FILTER();
  _o.PROGRESS_INTERVAL_SECONDS = this.PROGRESS_INTERVAL_SECONDS();
  _o.HAS_PROGRESS_INTERVAL_SECONDS = this.HAS_PROGRESS_INTERVAL_SECONDS();
  _o.ALGORITHM = this.ALGORITHM();
}
}

export class CQRScreeningControlsT implements flatbuffers.IGeneratedObject {
constructor(
  public START_EPOCH: TIMInstantT|null = null,
  public DURATION_SECONDS: number = 604800.0,
  public THRESHOLD_M: number = 5000.0,
  public REQUESTED_WORKERS: number = 1,
  public COARSE_STEP_SECONDS: number = 60.0,
  public REFINEMENT_TOLERANCE_SECONDS: number = 0.001,
  public COMBINED_RADIUS_M: number = 10.0,
  public USE_KD_TREE: boolean = true,
  public USE_DYNAMIC_WINDOW: boolean = true,
  public USE_PERIGEE_FILTER: boolean = true,
  public PROGRESS_INTERVAL_SECONDS: number = 0.0,
  public HAS_PROGRESS_INTERVAL_SECONDS: boolean = false,
  public ALGORITHM: cqrProbabilityAlgorithm = cqrProbabilityAlgorithm.ALFANO_MAXIMUM
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const START_EPOCH = (this.START_EPOCH !== null ? this.START_EPOCH!.pack(builder) : 0);

  return CQRScreeningControls.createCQRScreeningControls(builder,
    START_EPOCH,
    this.DURATION_SECONDS,
    this.THRESHOLD_M,
    this.REQUESTED_WORKERS,
    this.COARSE_STEP_SECONDS,
    this.REFINEMENT_TOLERANCE_SECONDS,
    this.COMBINED_RADIUS_M,
    this.USE_KD_TREE,
    this.USE_DYNAMIC_WINDOW,
    this.USE_PERIGEE_FILTER,
    this.PROGRESS_INTERVAL_SECONDS,
    this.HAS_PROGRESS_INTERVAL_SECONDS,
    this.ALGORITHM
  );
}
}
