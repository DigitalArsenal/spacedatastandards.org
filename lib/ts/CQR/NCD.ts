// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { NCDCode500Header, NCDCode500HeaderT } from './NCDCode500Header.js';
import { NCDSP3Header, NCDSP3HeaderT } from './NCDSP3Header.js';
import { NCDScenarioEpochContainer, NCDScenarioEpochContainerT } from './NCDScenarioEpochContainer.js';
import { NCDSegmentDescriptor, NCDSegmentDescriptorT } from './NCDSegmentDescriptor.js';
import { ncdContainerFormat } from './ncdContainerFormat.js';


/**
 * Native Container Descriptor.
 *
 * The file-level facts of an ephemeris, attitude or tracking container that
 * are NOT properties of the messages inside it. $OEM, $AEM and $TDM carry the
 * states, attitudes and observations; none of them can carry the internal
 * file name, the comment area, the segment address ranges, the header accuracy
 * block or the interpolation order the source declared, because those describe
 * the CONTAINER. Without them a read-then-write cycle silently invents header
 * values, and a segmented binary file cannot be reproduced at all.
 *
 * ONE record covers every container class rather than one record per format:
 * the file-level facts are the same kind of fact in each, and the
 * format-specific blocks are optional sub-tables selected by FORMAT. Only the
 * block matching FORMAT is populated.
 */
export class NCD implements flatbuffers.IUnpackableObject<NCDT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):NCD {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsNCD(bb:flatbuffers.ByteBuffer, obj?:NCD):NCD {
  return (obj || new NCD()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsNCD(bb:flatbuffers.ByteBuffer, obj?:NCD):NCD {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new NCD()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$NCD');
}

/**
 * Container format class.
 */
FORMAT():ncdContainerFormat {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ncdContainerFormat.UNSPECIFIED;
}

/**
 * Format name when FORMAT is PROVIDER_DEFINED. Empty otherwise.
 */
PROVIDER_DEFINED_FORMAT_NAME():string|null
PROVIDER_DEFINED_FORMAT_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROVIDER_DEFINED_FORMAT_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Version of the container format as declared by the file itself.
 */
FORMAT_VERSION():string|null
FORMAT_VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
FORMAT_VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Producing system or organization as recorded IN THE FILE.
 */
PRODUCER():string|null
PRODUCER(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PRODUCER(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Creation date recorded in the file, ISO 8601.
 */
CREATION_DATE():string|null
CREATION_DATE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CREATION_DATE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Originator recorded in the file.
 */
ORIGINATOR():string|null
ORIGINATOR(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ORIGINATOR(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Container-internal file name, which is stored inside the file and is
 * independent of the name on disk.
 */
INTERNAL_FILE_NAME():string|null
INTERNAL_FILE_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
INTERNAL_FILE_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Free-text comment area carried inside the container, one entry per line,
 * in file order.
 */
COMMENT_AREA(index: number):string
COMMENT_AREA(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
COMMENT_AREA(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

commentAreaLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Reference frame name as the container itself spells it, before any
 * mapping onto $RFM.
 */
NATIVE_FRAME_NAME():string|null
NATIVE_FRAME_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NATIVE_FRAME_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Integer reference frame code as the container stores it.
 */
NATIVE_FRAME_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

/**
 * Time system as the container itself spells it, before any mapping onto
 * the ratified time-scale vocabulary.
 */
NATIVE_TIME_SYSTEM():string|null
NATIVE_TIME_SYSTEM(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NATIVE_TIME_SYSTEM(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Descriptors of the container's segments, in file order. Empty for a
 * single-span container.
 */
SEGMENTS(index: number, obj?:NCDSegmentDescriptor):NCDSegmentDescriptor|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? (obj || new NCDSegmentDescriptor()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

segmentsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Header block when FORMAT is SP3_C or SP3_D.
 */
SP3_HEADER(obj?:NCDSP3Header):NCDSP3Header|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? (obj || new NCDSP3Header()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Parameters when FORMAT is SCENARIO_EPOCH_EPHEMERIS_TEXT or
 * SCENARIO_EPOCH_ATTITUDE_TEXT.
 */
SCENARIO_CONTAINER(obj?:NCDScenarioEpochContainer):NCDScenarioEpochContainer|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? (obj || new NCDScenarioEpochContainer()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Header words when FORMAT is CODE_500.
 */
CODE_500_HEADER(obj?:NCDCode500Header):NCDCode500Header|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? (obj || new NCDCode500Header()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Total coverage start across all segments, ISO 8601.
 */
START_TIME():string|null
START_TIME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
START_TIME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Total coverage stop across all segments, ISO 8601.
 */
STOP_TIME():string|null
STOP_TIME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
STOP_TIME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Length of the described container in bytes.
 */
SOURCE_BYTE_LENGTH():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * SHA-256 of the described container's exact bytes, lowercase hex. This is
 * what makes the descriptor checkable: a consumer can prove the descriptor
 * belongs to the file it holds.
 */
SOURCE_SHA256():string|null
SOURCE_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SOURCE_SHA256(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Content identifier of the described container when it is addressed by
 * content rather than by path.
 */
SOURCE_CID():string|null
SOURCE_CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SOURCE_CID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startNCD(builder:flatbuffers.Builder) {
  builder.startObject(20);
}

static addFormat(builder:flatbuffers.Builder, FORMAT:ncdContainerFormat) {
  builder.addFieldInt8(0, FORMAT, ncdContainerFormat.UNSPECIFIED);
}

static addProviderDefinedFormatName(builder:flatbuffers.Builder, PROVIDER_DEFINED_FORMAT_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, PROVIDER_DEFINED_FORMAT_NAMEOffset, 0);
}

static addFormatVersion(builder:flatbuffers.Builder, FORMAT_VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, FORMAT_VERSIONOffset, 0);
}

static addProducer(builder:flatbuffers.Builder, PRODUCEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, PRODUCEROffset, 0);
}

static addCreationDate(builder:flatbuffers.Builder, CREATION_DATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, CREATION_DATEOffset, 0);
}

static addOriginator(builder:flatbuffers.Builder, ORIGINATOROffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ORIGINATOROffset, 0);
}

static addInternalFileName(builder:flatbuffers.Builder, INTERNAL_FILE_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, INTERNAL_FILE_NAMEOffset, 0);
}

static addCommentArea(builder:flatbuffers.Builder, COMMENT_AREAOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, COMMENT_AREAOffset, 0);
}

static createCommentAreaVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCommentAreaVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addNativeFrameName(builder:flatbuffers.Builder, NATIVE_FRAME_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, NATIVE_FRAME_NAMEOffset, 0);
}

static addNativeFrameId(builder:flatbuffers.Builder, NATIVE_FRAME_ID:number) {
  builder.addFieldInt32(9, NATIVE_FRAME_ID, 0);
}

static addNativeTimeSystem(builder:flatbuffers.Builder, NATIVE_TIME_SYSTEMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, NATIVE_TIME_SYSTEMOffset, 0);
}

static addSegments(builder:flatbuffers.Builder, SEGMENTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, SEGMENTSOffset, 0);
}

static createSegmentsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSegmentsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSp3Header(builder:flatbuffers.Builder, SP3_HEADEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, SP3_HEADEROffset, 0);
}

static addScenarioContainer(builder:flatbuffers.Builder, SCENARIO_CONTAINEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, SCENARIO_CONTAINEROffset, 0);
}

static addCode500Header(builder:flatbuffers.Builder, CODE_500_HEADEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, CODE_500_HEADEROffset, 0);
}

static addStartTime(builder:flatbuffers.Builder, START_TIMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(15, START_TIMEOffset, 0);
}

static addStopTime(builder:flatbuffers.Builder, STOP_TIMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(16, STOP_TIMEOffset, 0);
}

static addSourceByteLength(builder:flatbuffers.Builder, SOURCE_BYTE_LENGTH:bigint) {
  builder.addFieldInt64(17, SOURCE_BYTE_LENGTH, BigInt('0'));
}

static addSourceSha256(builder:flatbuffers.Builder, SOURCE_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(18, SOURCE_SHA256Offset, 0);
}

static addSourceCid(builder:flatbuffers.Builder, SOURCE_CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(19, SOURCE_CIDOffset, 0);
}

static endNCD(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishNCDBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$NCD');
}

static finishSizePrefixedNCDBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$NCD', true);
}


unpack(): NCDT {
  return new NCDT(
    this.FORMAT(),
    this.PROVIDER_DEFINED_FORMAT_NAME(),
    this.FORMAT_VERSION(),
    this.PRODUCER(),
    this.CREATION_DATE(),
    this.ORIGINATOR(),
    this.INTERNAL_FILE_NAME(),
    this.bb!.createScalarList<string>(this.COMMENT_AREA.bind(this), this.commentAreaLength()),
    this.NATIVE_FRAME_NAME(),
    this.NATIVE_FRAME_ID(),
    this.NATIVE_TIME_SYSTEM(),
    this.bb!.createObjList<NCDSegmentDescriptor, NCDSegmentDescriptorT>(this.SEGMENTS.bind(this), this.segmentsLength()),
    (this.SP3_HEADER() !== null ? this.SP3_HEADER()!.unpack() : null),
    (this.SCENARIO_CONTAINER() !== null ? this.SCENARIO_CONTAINER()!.unpack() : null),
    (this.CODE_500_HEADER() !== null ? this.CODE_500_HEADER()!.unpack() : null),
    this.START_TIME(),
    this.STOP_TIME(),
    this.SOURCE_BYTE_LENGTH(),
    this.SOURCE_SHA256(),
    this.SOURCE_CID()
  );
}


unpackTo(_o: NCDT): void {
  _o.FORMAT = this.FORMAT();
  _o.PROVIDER_DEFINED_FORMAT_NAME = this.PROVIDER_DEFINED_FORMAT_NAME();
  _o.FORMAT_VERSION = this.FORMAT_VERSION();
  _o.PRODUCER = this.PRODUCER();
  _o.CREATION_DATE = this.CREATION_DATE();
  _o.ORIGINATOR = this.ORIGINATOR();
  _o.INTERNAL_FILE_NAME = this.INTERNAL_FILE_NAME();
  _o.COMMENT_AREA = this.bb!.createScalarList<string>(this.COMMENT_AREA.bind(this), this.commentAreaLength());
  _o.NATIVE_FRAME_NAME = this.NATIVE_FRAME_NAME();
  _o.NATIVE_FRAME_ID = this.NATIVE_FRAME_ID();
  _o.NATIVE_TIME_SYSTEM = this.NATIVE_TIME_SYSTEM();
  _o.SEGMENTS = this.bb!.createObjList<NCDSegmentDescriptor, NCDSegmentDescriptorT>(this.SEGMENTS.bind(this), this.segmentsLength());
  _o.SP3_HEADER = (this.SP3_HEADER() !== null ? this.SP3_HEADER()!.unpack() : null);
  _o.SCENARIO_CONTAINER = (this.SCENARIO_CONTAINER() !== null ? this.SCENARIO_CONTAINER()!.unpack() : null);
  _o.CODE_500_HEADER = (this.CODE_500_HEADER() !== null ? this.CODE_500_HEADER()!.unpack() : null);
  _o.START_TIME = this.START_TIME();
  _o.STOP_TIME = this.STOP_TIME();
  _o.SOURCE_BYTE_LENGTH = this.SOURCE_BYTE_LENGTH();
  _o.SOURCE_SHA256 = this.SOURCE_SHA256();
  _o.SOURCE_CID = this.SOURCE_CID();
}
}

export class NCDT implements flatbuffers.IGeneratedObject {
constructor(
  public FORMAT: ncdContainerFormat = ncdContainerFormat.UNSPECIFIED,
  public PROVIDER_DEFINED_FORMAT_NAME: string|Uint8Array|null = null,
  public FORMAT_VERSION: string|Uint8Array|null = null,
  public PRODUCER: string|Uint8Array|null = null,
  public CREATION_DATE: string|Uint8Array|null = null,
  public ORIGINATOR: string|Uint8Array|null = null,
  public INTERNAL_FILE_NAME: string|Uint8Array|null = null,
  public COMMENT_AREA: (string)[] = [],
  public NATIVE_FRAME_NAME: string|Uint8Array|null = null,
  public NATIVE_FRAME_ID: number = 0,
  public NATIVE_TIME_SYSTEM: string|Uint8Array|null = null,
  public SEGMENTS: (NCDSegmentDescriptorT)[] = [],
  public SP3_HEADER: NCDSP3HeaderT|null = null,
  public SCENARIO_CONTAINER: NCDScenarioEpochContainerT|null = null,
  public CODE_500_HEADER: NCDCode500HeaderT|null = null,
  public START_TIME: string|Uint8Array|null = null,
  public STOP_TIME: string|Uint8Array|null = null,
  public SOURCE_BYTE_LENGTH: bigint = BigInt('0'),
  public SOURCE_SHA256: string|Uint8Array|null = null,
  public SOURCE_CID: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PROVIDER_DEFINED_FORMAT_NAME = (this.PROVIDER_DEFINED_FORMAT_NAME !== null ? builder.createString(this.PROVIDER_DEFINED_FORMAT_NAME!) : 0);
  const FORMAT_VERSION = (this.FORMAT_VERSION !== null ? builder.createString(this.FORMAT_VERSION!) : 0);
  const PRODUCER = (this.PRODUCER !== null ? builder.createString(this.PRODUCER!) : 0);
  const CREATION_DATE = (this.CREATION_DATE !== null ? builder.createString(this.CREATION_DATE!) : 0);
  const ORIGINATOR = (this.ORIGINATOR !== null ? builder.createString(this.ORIGINATOR!) : 0);
  const INTERNAL_FILE_NAME = (this.INTERNAL_FILE_NAME !== null ? builder.createString(this.INTERNAL_FILE_NAME!) : 0);
  const COMMENT_AREA = NCD.createCommentAreaVector(builder, builder.createObjectOffsetList(this.COMMENT_AREA));
  const NATIVE_FRAME_NAME = (this.NATIVE_FRAME_NAME !== null ? builder.createString(this.NATIVE_FRAME_NAME!) : 0);
  const NATIVE_TIME_SYSTEM = (this.NATIVE_TIME_SYSTEM !== null ? builder.createString(this.NATIVE_TIME_SYSTEM!) : 0);
  const SEGMENTS = NCD.createSegmentsVector(builder, builder.createObjectOffsetList(this.SEGMENTS));
  const SP3_HEADER = (this.SP3_HEADER !== null ? this.SP3_HEADER!.pack(builder) : 0);
  const SCENARIO_CONTAINER = (this.SCENARIO_CONTAINER !== null ? this.SCENARIO_CONTAINER!.pack(builder) : 0);
  const CODE_500_HEADER = (this.CODE_500_HEADER !== null ? this.CODE_500_HEADER!.pack(builder) : 0);
  const START_TIME = (this.START_TIME !== null ? builder.createString(this.START_TIME!) : 0);
  const STOP_TIME = (this.STOP_TIME !== null ? builder.createString(this.STOP_TIME!) : 0);
  const SOURCE_SHA256 = (this.SOURCE_SHA256 !== null ? builder.createString(this.SOURCE_SHA256!) : 0);
  const SOURCE_CID = (this.SOURCE_CID !== null ? builder.createString(this.SOURCE_CID!) : 0);

  NCD.startNCD(builder);
  NCD.addFormat(builder, this.FORMAT);
  NCD.addProviderDefinedFormatName(builder, PROVIDER_DEFINED_FORMAT_NAME);
  NCD.addFormatVersion(builder, FORMAT_VERSION);
  NCD.addProducer(builder, PRODUCER);
  NCD.addCreationDate(builder, CREATION_DATE);
  NCD.addOriginator(builder, ORIGINATOR);
  NCD.addInternalFileName(builder, INTERNAL_FILE_NAME);
  NCD.addCommentArea(builder, COMMENT_AREA);
  NCD.addNativeFrameName(builder, NATIVE_FRAME_NAME);
  NCD.addNativeFrameId(builder, this.NATIVE_FRAME_ID);
  NCD.addNativeTimeSystem(builder, NATIVE_TIME_SYSTEM);
  NCD.addSegments(builder, SEGMENTS);
  NCD.addSp3Header(builder, SP3_HEADER);
  NCD.addScenarioContainer(builder, SCENARIO_CONTAINER);
  NCD.addCode500Header(builder, CODE_500_HEADER);
  NCD.addStartTime(builder, START_TIME);
  NCD.addStopTime(builder, STOP_TIME);
  NCD.addSourceByteLength(builder, this.SOURCE_BYTE_LENGTH);
  NCD.addSourceSha256(builder, SOURCE_SHA256);
  NCD.addSourceCid(builder, SOURCE_CID);

  return NCD.endNCD(builder);
}
}
