// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * State Vector
 */
export class STV implements flatbuffers.IUnpackableObject<STVT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):STV {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSTV(bb:flatbuffers.ByteBuffer, obj?:STV):STV {
  return (obj || new STV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSTV(bb:flatbuffers.ByteBuffer, obj?:STV):STV {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new STV()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$STV');
}

EPOCH():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

POS_X():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

POS_Y():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

POS_Z():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

VEL_X():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

VEL_Y():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

VEL_Z():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

REF_FRAME():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

FLAGS():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

RESERVED(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

reservedLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

reservedArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startSTV(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addEpoch(builder:flatbuffers.Builder, EPOCH:number) {
  builder.addFieldFloat64(0, EPOCH, 0.0);
}

static addPosX(builder:flatbuffers.Builder, POS_X:number) {
  builder.addFieldFloat64(1, POS_X, 0.0);
}

static addPosY(builder:flatbuffers.Builder, POS_Y:number) {
  builder.addFieldFloat64(2, POS_Y, 0.0);
}

static addPosZ(builder:flatbuffers.Builder, POS_Z:number) {
  builder.addFieldFloat64(3, POS_Z, 0.0);
}

static addVelX(builder:flatbuffers.Builder, VEL_X:number) {
  builder.addFieldFloat64(4, VEL_X, 0.0);
}

static addVelY(builder:flatbuffers.Builder, VEL_Y:number) {
  builder.addFieldFloat64(5, VEL_Y, 0.0);
}

static addVelZ(builder:flatbuffers.Builder, VEL_Z:number) {
  builder.addFieldFloat64(6, VEL_Z, 0.0);
}

static addRefFrame(builder:flatbuffers.Builder, REF_FRAME:number) {
  builder.addFieldInt8(7, REF_FRAME, 0);
}

static addFlags(builder:flatbuffers.Builder, FLAGS:number) {
  builder.addFieldInt8(8, FLAGS, 0);
}

static addReserved(builder:flatbuffers.Builder, RESERVEDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, RESERVEDOffset, 0);
}

static createReservedVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startReservedVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endSTV(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishSTVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$STV');
}

static finishSizePrefixedSTVBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$STV', true);
}

static createSTV(builder:flatbuffers.Builder, EPOCH:number, POS_X:number, POS_Y:number, POS_Z:number, VEL_X:number, VEL_Y:number, VEL_Z:number, REF_FRAME:number, FLAGS:number, RESERVEDOffset:flatbuffers.Offset):flatbuffers.Offset {
  STV.startSTV(builder);
  STV.addEpoch(builder, EPOCH);
  STV.addPosX(builder, POS_X);
  STV.addPosY(builder, POS_Y);
  STV.addPosZ(builder, POS_Z);
  STV.addVelX(builder, VEL_X);
  STV.addVelY(builder, VEL_Y);
  STV.addVelZ(builder, VEL_Z);
  STV.addRefFrame(builder, REF_FRAME);
  STV.addFlags(builder, FLAGS);
  STV.addReserved(builder, RESERVEDOffset);
  return STV.endSTV(builder);
}

unpack(): STVT {
  return new STVT(
    this.EPOCH(),
    this.POS_X(),
    this.POS_Y(),
    this.POS_Z(),
    this.VEL_X(),
    this.VEL_Y(),
    this.VEL_Z(),
    this.REF_FRAME(),
    this.FLAGS(),
    this.bb!.createScalarList<number>(this.RESERVED.bind(this), this.reservedLength())
  );
}


unpackTo(_o: STVT): void {
  _o.EPOCH = this.EPOCH();
  _o.POS_X = this.POS_X();
  _o.POS_Y = this.POS_Y();
  _o.POS_Z = this.POS_Z();
  _o.VEL_X = this.VEL_X();
  _o.VEL_Y = this.VEL_Y();
  _o.VEL_Z = this.VEL_Z();
  _o.REF_FRAME = this.REF_FRAME();
  _o.FLAGS = this.FLAGS();
  _o.RESERVED = this.bb!.createScalarList<number>(this.RESERVED.bind(this), this.reservedLength());
}
}

export class STVT implements flatbuffers.IGeneratedObject {
constructor(
  public EPOCH: number = 0.0,
  public POS_X: number = 0.0,
  public POS_Y: number = 0.0,
  public POS_Z: number = 0.0,
  public VEL_X: number = 0.0,
  public VEL_Y: number = 0.0,
  public VEL_Z: number = 0.0,
  public REF_FRAME: number = 0,
  public FLAGS: number = 0,
  public RESERVED: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const RESERVED = STV.createReservedVector(builder, this.RESERVED);

  return STV.createSTV(builder,
    this.EPOCH,
    this.POS_X,
    this.POS_Y,
    this.POS_Z,
    this.VEL_X,
    this.VEL_Y,
    this.VEL_Z,
    this.REF_FRAME,
    this.FLAGS,
    RESERVED
  );
}
}
