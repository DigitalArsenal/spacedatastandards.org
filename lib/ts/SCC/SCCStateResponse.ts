// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SCN, SCNT } from './SCN.js';
import { SCNReference, SCNReferenceT } from './SCNReference.js';
import { stateRequestKind } from './stateRequestKind.js';


/**
 * State response. SCENARIO carries full scenario state; REFERENCES can carry
 * the lighter reference-list response for reference-only requests.
 */
export class SCCStateResponse implements flatbuffers.IUnpackableObject<SCCStateResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCCStateResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCCStateResponse(bb:flatbuffers.ByteBuffer, obj?:SCCStateResponse):SCCStateResponse {
  return (obj || new SCCStateResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCCStateResponse(bb:flatbuffers.ByteBuffer, obj?:SCCStateResponse):SCCStateResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCCStateResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Request correlation id from the matching state request.
 */
REQUEST_ID():string|null
REQUEST_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REQUEST_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * State payload represented by this response.
 */
REQUEST_KIND():stateRequestKind {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : stateRequestKind.UNKNOWN;
}

/**
 * Full scenario state for scenario-state responses.
 */
SCENARIO(obj?:SCN):SCN|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new SCN()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Reference-list payload for reference-only responses.
 */
REFERENCES(index: number, obj?:SCNReference):SCNReference|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new SCNReference()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

referencesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Zero-based focused reference index, or -1 when no index is focused.
 */
FOCUSED_REFERENCE_INDEX():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : -1;
}

/**
 * Human-readable error message when state cannot be returned.
 */
ERROR_MESSAGE():string|null
ERROR_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ERROR_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startSCCStateResponse(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addRequestId(builder:flatbuffers.Builder, REQUEST_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, REQUEST_IDOffset, 0);
}

static addRequestKind(builder:flatbuffers.Builder, REQUEST_KIND:stateRequestKind) {
  builder.addFieldInt8(1, REQUEST_KIND, stateRequestKind.UNKNOWN);
}

static addScenario(builder:flatbuffers.Builder, SCENARIOOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, SCENARIOOffset, 0);
}

static addReferences(builder:flatbuffers.Builder, REFERENCESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, REFERENCESOffset, 0);
}

static createReferencesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startReferencesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addFocusedReferenceIndex(builder:flatbuffers.Builder, FOCUSED_REFERENCE_INDEX:number) {
  builder.addFieldInt32(4, FOCUSED_REFERENCE_INDEX, -1);
}

static addErrorMessage(builder:flatbuffers.Builder, ERROR_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ERROR_MESSAGEOffset, 0);
}

static endSCCStateResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): SCCStateResponseT {
  return new SCCStateResponseT(
    this.REQUEST_ID(),
    this.REQUEST_KIND(),
    (this.SCENARIO() !== null ? this.SCENARIO()!.unpack() : null),
    this.bb!.createObjList<SCNReference, SCNReferenceT>(this.REFERENCES.bind(this), this.referencesLength()),
    this.FOCUSED_REFERENCE_INDEX(),
    this.ERROR_MESSAGE()
  );
}


unpackTo(_o: SCCStateResponseT): void {
  _o.REQUEST_ID = this.REQUEST_ID();
  _o.REQUEST_KIND = this.REQUEST_KIND();
  _o.SCENARIO = (this.SCENARIO() !== null ? this.SCENARIO()!.unpack() : null);
  _o.REFERENCES = this.bb!.createObjList<SCNReference, SCNReferenceT>(this.REFERENCES.bind(this), this.referencesLength());
  _o.FOCUSED_REFERENCE_INDEX = this.FOCUSED_REFERENCE_INDEX();
  _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
}
}

export class SCCStateResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public REQUEST_ID: string|Uint8Array|null = null,
  public REQUEST_KIND: stateRequestKind = stateRequestKind.UNKNOWN,
  public SCENARIO: SCNT|null = null,
  public REFERENCES: (SCNReferenceT)[] = [],
  public FOCUSED_REFERENCE_INDEX: number = -1,
  public ERROR_MESSAGE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const REQUEST_ID = (this.REQUEST_ID !== null ? builder.createString(this.REQUEST_ID!) : 0);
  const SCENARIO = (this.SCENARIO !== null ? this.SCENARIO!.pack(builder) : 0);
  const REFERENCES = SCCStateResponse.createReferencesVector(builder, builder.createObjectOffsetList(this.REFERENCES));
  const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE!) : 0);

  SCCStateResponse.startSCCStateResponse(builder);
  SCCStateResponse.addRequestId(builder, REQUEST_ID);
  SCCStateResponse.addRequestKind(builder, this.REQUEST_KIND);
  SCCStateResponse.addScenario(builder, SCENARIO);
  SCCStateResponse.addReferences(builder, REFERENCES);
  SCCStateResponse.addFocusedReferenceIndex(builder, this.FOCUSED_REFERENCE_INDEX);
  SCCStateResponse.addErrorMessage(builder, ERROR_MESSAGE);

  return SCCStateResponse.endSCCStateResponse(builder);
}
}
