// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { VSTCameraOptions, VSTCameraOptionsT } from './VSTCameraOptions.js';
import { VSTDisplaySettings, VSTDisplaySettingsT } from './VSTDisplaySettings.js';


/**
 * Viewer State — display and camera state associated with a scenario.
 */
export class VST implements flatbuffers.IUnpackableObject<VSTT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VST {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVST(bb:flatbuffers.ByteBuffer, obj?:VST):VST {
  return (obj || new VST()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVST(bb:flatbuffers.ByteBuffer, obj?:VST):VST {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VST()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$VST');
}

CAMERA(obj?:VSTCameraOptions):VSTCameraOptions|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new VSTCameraOptions()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

DISPLAY_SETTINGS(obj?:VSTDisplaySettings):VSTDisplaySettings|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new VSTDisplaySettings()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

FOCUSED_REFERENCE_ID():string|null
FOCUSED_REFERENCE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
FOCUSED_REFERENCE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

FOCUSED_REFERENCE_INDEX():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : -1;
}

static startVST(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addCamera(builder:flatbuffers.Builder, CAMERAOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, CAMERAOffset, 0);
}

static addDisplaySettings(builder:flatbuffers.Builder, DISPLAY_SETTINGSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, DISPLAY_SETTINGSOffset, 0);
}

static addFocusedReferenceId(builder:flatbuffers.Builder, FOCUSED_REFERENCE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, FOCUSED_REFERENCE_IDOffset, 0);
}

static addFocusedReferenceIndex(builder:flatbuffers.Builder, FOCUSED_REFERENCE_INDEX:number) {
  builder.addFieldInt32(3, FOCUSED_REFERENCE_INDEX, -1);
}

static endVST(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishVSTBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$VST');
}

static finishSizePrefixedVSTBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$VST', true);
}


unpack(): VSTT {
  return new VSTT(
    (this.CAMERA() !== null ? this.CAMERA()!.unpack() : null),
    (this.DISPLAY_SETTINGS() !== null ? this.DISPLAY_SETTINGS()!.unpack() : null),
    this.FOCUSED_REFERENCE_ID(),
    this.FOCUSED_REFERENCE_INDEX()
  );
}


unpackTo(_o: VSTT): void {
  _o.CAMERA = (this.CAMERA() !== null ? this.CAMERA()!.unpack() : null);
  _o.DISPLAY_SETTINGS = (this.DISPLAY_SETTINGS() !== null ? this.DISPLAY_SETTINGS()!.unpack() : null);
  _o.FOCUSED_REFERENCE_ID = this.FOCUSED_REFERENCE_ID();
  _o.FOCUSED_REFERENCE_INDEX = this.FOCUSED_REFERENCE_INDEX();
}
}

export class VSTT implements flatbuffers.IGeneratedObject {
constructor(
  public CAMERA: VSTCameraOptionsT|null = null,
  public DISPLAY_SETTINGS: VSTDisplaySettingsT|null = null,
  public FOCUSED_REFERENCE_ID: string|Uint8Array|null = null,
  public FOCUSED_REFERENCE_INDEX: number = -1
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CAMERA = (this.CAMERA !== null ? this.CAMERA!.pack(builder) : 0);
  const DISPLAY_SETTINGS = (this.DISPLAY_SETTINGS !== null ? this.DISPLAY_SETTINGS!.pack(builder) : 0);
  const FOCUSED_REFERENCE_ID = (this.FOCUSED_REFERENCE_ID !== null ? builder.createString(this.FOCUSED_REFERENCE_ID!) : 0);

  VST.startVST(builder);
  VST.addCamera(builder, CAMERA);
  VST.addDisplaySettings(builder, DISPLAY_SETTINGS);
  VST.addFocusedReferenceId(builder, FOCUSED_REFERENCE_ID);
  VST.addFocusedReferenceIndex(builder, this.FOCUSED_REFERENCE_INDEX);

  return VST.endVST(builder);
}
}
