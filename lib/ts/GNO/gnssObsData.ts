// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { gnssObsType } from './gnssObsType.js';


/**
 * GNSS Observation Data Point
 */
export class gnssObsData implements flatbuffers.IUnpackableObject<gnssObsDataT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):gnssObsData {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsgnssObsData(bb:flatbuffers.ByteBuffer, obj?:gnssObsData):gnssObsData {
  return (obj || new gnssObsData()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsgnssObsData(bb:flatbuffers.ByteBuffer, obj?:gnssObsData):gnssObsData {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new gnssObsData()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Signal type code (e.g., L1C, L2P, L5Q, E1B)
 */
SIGNAL():string|null
SIGNAL(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SIGNAL(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Observation type
 */
OBS_TYPE():gnssObsType {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : gnssObsType.PSEUDORANGE;
}

/**
 * Observation value (units depend on type: m, cycles, Hz, dB-Hz)
 */
VALUE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Loss of lock indicator
 */
LLI():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Signal strength indicator (1-9)
 */
SSI():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startgnssObsData(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addSignal(builder:flatbuffers.Builder, SIGNALOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SIGNALOffset, 0);
}

static addObsType(builder:flatbuffers.Builder, OBS_TYPE:gnssObsType) {
  builder.addFieldInt8(1, OBS_TYPE, gnssObsType.PSEUDORANGE);
}

static addValue(builder:flatbuffers.Builder, VALUE:number) {
  builder.addFieldFloat64(2, VALUE, 0.0);
}

static addLli(builder:flatbuffers.Builder, LLI:number) {
  builder.addFieldInt8(3, LLI, 0);
}

static addSsi(builder:flatbuffers.Builder, SSI:number) {
  builder.addFieldInt8(4, SSI, 0);
}

static endgnssObsData(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static creategnssObsData(builder:flatbuffers.Builder, SIGNALOffset:flatbuffers.Offset, OBS_TYPE:gnssObsType, VALUE:number, LLI:number, SSI:number):flatbuffers.Offset {
  gnssObsData.startgnssObsData(builder);
  gnssObsData.addSignal(builder, SIGNALOffset);
  gnssObsData.addObsType(builder, OBS_TYPE);
  gnssObsData.addValue(builder, VALUE);
  gnssObsData.addLli(builder, LLI);
  gnssObsData.addSsi(builder, SSI);
  return gnssObsData.endgnssObsData(builder);
}

unpack(): gnssObsDataT {
  return new gnssObsDataT(
    this.SIGNAL(),
    this.OBS_TYPE(),
    this.VALUE(),
    this.LLI(),
    this.SSI()
  );
}


unpackTo(_o: gnssObsDataT): void {
  _o.SIGNAL = this.SIGNAL();
  _o.OBS_TYPE = this.OBS_TYPE();
  _o.VALUE = this.VALUE();
  _o.LLI = this.LLI();
  _o.SSI = this.SSI();
}
}

export class gnssObsDataT implements flatbuffers.IGeneratedObject {
constructor(
  public SIGNAL: string|Uint8Array|null = null,
  public OBS_TYPE: gnssObsType = gnssObsType.PSEUDORANGE,
  public VALUE: number = 0.0,
  public LLI: number = 0,
  public SSI: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SIGNAL = (this.SIGNAL !== null ? builder.createString(this.SIGNAL!) : 0);

  return gnssObsData.creategnssObsData(builder,
    SIGNAL,
    this.OBS_TYPE,
    this.VALUE,
    this.LLI,
    this.SSI
  );
}
}
