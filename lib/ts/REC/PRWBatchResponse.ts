// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { propagatorErrorCode } from './propagatorErrorCode.js';
import { propagatorStateFlags } from './propagatorStateFlags.js';


/**
 * Batch propagation response header — describes the stream written to
 * OUTPUT_OFFSET and reports errors. The stream itself is
 * `STATE_VECTOR_SIZE`-tuple rows in the SDS `OCM` STATE_DATA layout.
 */
export class PRWBatchResponse implements flatbuffers.IUnpackableObject<PRWBatchResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PRWBatchResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPRWBatchResponse(bb:flatbuffers.ByteBuffer, obj?:PRWBatchResponse):PRWBatchResponse {
  return (obj || new PRWBatchResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPRWBatchResponse(bb:flatbuffers.ByteBuffer, obj?:PRWBatchResponse):PRWBatchResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PRWBatchResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Number of state vectors written.
 */
COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Offset in the arena where the state-vector stream begins.
 */
OUTPUT_OFFSET():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Components per state vector (6 = PV, 9 = PVA). Mirrors OCM.STATE_VECTOR_SIZE.
 */
STATE_VECTOR_SIZE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Reference frame for the stream (SDS `RFM`-compatible string tag).
 */
REFERENCE_FRAME():string|null
REFERENCE_FRAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
REFERENCE_FRAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Per-entity status flags (same cardinality as COUNT).
 */
FLAGS(index: number):propagatorStateFlags|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : null;
}

flagsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

flagsArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Error code (0 == OK).
 */
ERROR_CODE():propagatorErrorCode {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : propagatorErrorCode.OK;
}

/**
 * Optional error message when ERROR_CODE != OK.
 */
ERROR_MESSAGE():string|null
ERROR_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ERROR_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startPRWBatchResponse(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addCount(builder:flatbuffers.Builder, COUNT:number) {
  builder.addFieldInt32(0, COUNT, 0);
}

static addOutputOffset(builder:flatbuffers.Builder, OUTPUT_OFFSET:number) {
  builder.addFieldInt32(1, OUTPUT_OFFSET, 0);
}

static addStateVectorSize(builder:flatbuffers.Builder, STATE_VECTOR_SIZE:number) {
  builder.addFieldInt8(2, STATE_VECTOR_SIZE, 0);
}

static addReferenceFrame(builder:flatbuffers.Builder, REFERENCE_FRAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, REFERENCE_FRAMEOffset, 0);
}

static addFlags(builder:flatbuffers.Builder, FLAGSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, FLAGSOffset, 0);
}

static createFlagsVector(builder:flatbuffers.Builder, data:propagatorStateFlags[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startFlagsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addErrorCode(builder:flatbuffers.Builder, ERROR_CODE:propagatorErrorCode) {
  builder.addFieldInt32(5, ERROR_CODE, propagatorErrorCode.OK);
}

static addErrorMessage(builder:flatbuffers.Builder, ERROR_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, ERROR_MESSAGEOffset, 0);
}

static endPRWBatchResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPRWBatchResponse(builder:flatbuffers.Builder, COUNT:number, OUTPUT_OFFSET:number, STATE_VECTOR_SIZE:number, REFERENCE_FRAMEOffset:flatbuffers.Offset, FLAGSOffset:flatbuffers.Offset, ERROR_CODE:propagatorErrorCode, ERROR_MESSAGEOffset:flatbuffers.Offset):flatbuffers.Offset {
  PRWBatchResponse.startPRWBatchResponse(builder);
  PRWBatchResponse.addCount(builder, COUNT);
  PRWBatchResponse.addOutputOffset(builder, OUTPUT_OFFSET);
  PRWBatchResponse.addStateVectorSize(builder, STATE_VECTOR_SIZE);
  PRWBatchResponse.addReferenceFrame(builder, REFERENCE_FRAMEOffset);
  PRWBatchResponse.addFlags(builder, FLAGSOffset);
  PRWBatchResponse.addErrorCode(builder, ERROR_CODE);
  PRWBatchResponse.addErrorMessage(builder, ERROR_MESSAGEOffset);
  return PRWBatchResponse.endPRWBatchResponse(builder);
}

unpack(): PRWBatchResponseT {
  return new PRWBatchResponseT(
    this.COUNT(),
    this.OUTPUT_OFFSET(),
    this.STATE_VECTOR_SIZE(),
    this.REFERENCE_FRAME(),
    this.bb!.createScalarList<propagatorStateFlags>(this.FLAGS.bind(this), this.flagsLength()),
    this.ERROR_CODE(),
    this.ERROR_MESSAGE()
  );
}


unpackTo(_o: PRWBatchResponseT): void {
  _o.COUNT = this.COUNT();
  _o.OUTPUT_OFFSET = this.OUTPUT_OFFSET();
  _o.STATE_VECTOR_SIZE = this.STATE_VECTOR_SIZE();
  _o.REFERENCE_FRAME = this.REFERENCE_FRAME();
  _o.FLAGS = this.bb!.createScalarList<propagatorStateFlags>(this.FLAGS.bind(this), this.flagsLength());
  _o.ERROR_CODE = this.ERROR_CODE();
  _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
}
}

export class PRWBatchResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public COUNT: number = 0,
  public OUTPUT_OFFSET: number = 0,
  public STATE_VECTOR_SIZE: number = 0,
  public REFERENCE_FRAME: string|Uint8Array|null = null,
  public FLAGS: (propagatorStateFlags)[] = [],
  public ERROR_CODE: propagatorErrorCode = propagatorErrorCode.OK,
  public ERROR_MESSAGE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const REFERENCE_FRAME = (this.REFERENCE_FRAME !== null ? builder.createString(this.REFERENCE_FRAME!) : 0);
  const FLAGS = PRWBatchResponse.createFlagsVector(builder, this.FLAGS);
  const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE!) : 0);

  return PRWBatchResponse.createPRWBatchResponse(builder,
    this.COUNT,
    this.OUTPUT_OFFSET,
    this.STATE_VECTOR_SIZE,
    REFERENCE_FRAME,
    FLAGS,
    this.ERROR_CODE,
    ERROR_MESSAGE
  );
}
}
