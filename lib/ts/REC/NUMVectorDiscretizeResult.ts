// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { numResultStatus } from './numResultStatus.js';


/**
 * Result of one component-wise vector discretization operation.
 */
export class NUMVectorDiscretizeResult implements flatbuffers.IUnpackableObject<NUMVectorDiscretizeResultT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):NUMVectorDiscretizeResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsNUMVectorDiscretizeResult(bb:flatbuffers.ByteBuffer, obj?:NUMVectorDiscretizeResult):NUMVectorDiscretizeResult {
  return (obj || new NUMVectorDiscretizeResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsNUMVectorDiscretizeResult(bb:flatbuffers.ByteBuffer, obj?:NUMVectorDiscretizeResult):NUMVectorDiscretizeResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new NUMVectorDiscretizeResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

STATUS():numResultStatus {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : numResultStatus.OK;
}

ERROR_MESSAGE():string|null
ERROR_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ERROR_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Quantized output vector.
 */
DISCRETIZED_STATE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

discretizedStateLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

discretizedStateArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Input-minus-output error after any requested carried error was applied.
 */
DISCRETIZATION_ERROR(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

discretizationErrorLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

discretizationErrorArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Caller trace/correlation identifier copied from the request when present.
 */
TRACE_ID():string|null
TRACE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TRACE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startNUMVectorDiscretizeResult(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addStatus(builder:flatbuffers.Builder, STATUS:numResultStatus) {
  builder.addFieldInt8(0, STATUS, numResultStatus.OK);
}

static addErrorMessage(builder:flatbuffers.Builder, ERROR_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ERROR_MESSAGEOffset, 0);
}

static addDiscretizedState(builder:flatbuffers.Builder, DISCRETIZED_STATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, DISCRETIZED_STATEOffset, 0);
}

static createDiscretizedStateVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createDiscretizedStateVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createDiscretizedStateVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startDiscretizedStateVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addDiscretizationError(builder:flatbuffers.Builder, DISCRETIZATION_ERROROffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, DISCRETIZATION_ERROROffset, 0);
}

static createDiscretizationErrorVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createDiscretizationErrorVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createDiscretizationErrorVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startDiscretizationErrorVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, TRACE_IDOffset, 0);
}

static endNUMVectorDiscretizeResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createNUMVectorDiscretizeResult(builder:flatbuffers.Builder, STATUS:numResultStatus, ERROR_MESSAGEOffset:flatbuffers.Offset, DISCRETIZED_STATEOffset:flatbuffers.Offset, DISCRETIZATION_ERROROffset:flatbuffers.Offset, TRACE_IDOffset:flatbuffers.Offset):flatbuffers.Offset {
  NUMVectorDiscretizeResult.startNUMVectorDiscretizeResult(builder);
  NUMVectorDiscretizeResult.addStatus(builder, STATUS);
  NUMVectorDiscretizeResult.addErrorMessage(builder, ERROR_MESSAGEOffset);
  NUMVectorDiscretizeResult.addDiscretizedState(builder, DISCRETIZED_STATEOffset);
  NUMVectorDiscretizeResult.addDiscretizationError(builder, DISCRETIZATION_ERROROffset);
  NUMVectorDiscretizeResult.addTraceId(builder, TRACE_IDOffset);
  return NUMVectorDiscretizeResult.endNUMVectorDiscretizeResult(builder);
}

unpack(): NUMVectorDiscretizeResultT {
  return new NUMVectorDiscretizeResultT(
    this.STATUS(),
    this.ERROR_MESSAGE(),
    this.bb!.createScalarList<number>(this.DISCRETIZED_STATE.bind(this), this.discretizedStateLength()),
    this.bb!.createScalarList<number>(this.DISCRETIZATION_ERROR.bind(this), this.discretizationErrorLength()),
    this.TRACE_ID()
  );
}


unpackTo(_o: NUMVectorDiscretizeResultT): void {
  _o.STATUS = this.STATUS();
  _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
  _o.DISCRETIZED_STATE = this.bb!.createScalarList<number>(this.DISCRETIZED_STATE.bind(this), this.discretizedStateLength());
  _o.DISCRETIZATION_ERROR = this.bb!.createScalarList<number>(this.DISCRETIZATION_ERROR.bind(this), this.discretizationErrorLength());
  _o.TRACE_ID = this.TRACE_ID();
}
}

export class NUMVectorDiscretizeResultT implements flatbuffers.IGeneratedObject {
constructor(
  public STATUS: numResultStatus = numResultStatus.OK,
  public ERROR_MESSAGE: string|Uint8Array|null = null,
  public DISCRETIZED_STATE: (number)[] = [],
  public DISCRETIZATION_ERROR: (number)[] = [],
  public TRACE_ID: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE!) : 0);
  const DISCRETIZED_STATE = NUMVectorDiscretizeResult.createDiscretizedStateVector(builder, this.DISCRETIZED_STATE);
  const DISCRETIZATION_ERROR = NUMVectorDiscretizeResult.createDiscretizationErrorVector(builder, this.DISCRETIZATION_ERROR);
  const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID!) : 0);

  return NUMVectorDiscretizeResult.createNUMVectorDiscretizeResult(builder,
    this.STATUS,
    ERROR_MESSAGE,
    DISCRETIZED_STATE,
    DISCRETIZATION_ERROR,
    TRACE_ID
  );
}
}
