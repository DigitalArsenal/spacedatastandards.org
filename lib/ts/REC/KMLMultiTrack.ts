// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLAltitudeMode } from './KMLAltitudeMode.js';
import { KMLTrack, KMLTrackT } from './KMLTrack.js';


/**
 * gx:MultiTrack
 */
export class KMLMultiTrack implements flatbuffers.IUnpackableObject<KMLMultiTrackT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLMultiTrack {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLMultiTrack(bb:flatbuffers.ByteBuffer, obj?:KMLMultiTrack):KMLMultiTrack {
  return (obj || new KMLMultiTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLMultiTrack(bb:flatbuffers.ByteBuffer, obj?:KMLMultiTrack):KMLMultiTrack {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLMultiTrack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Altitude mode
 */
ALTITUDE_MODE():KMLAltitudeMode {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLAltitudeMode.CLAMP_TO_GROUND;
}

/**
 * Whether to interpolate between tracks
 */
INTERPOLATE():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Child tracks
 */
TRACKS(index: number, obj?:KMLTrack):KMLTrack|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new KMLTrack()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

tracksLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startKMLMultiTrack(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addAltitudeMode(builder:flatbuffers.Builder, ALTITUDE_MODE:KMLAltitudeMode) {
  builder.addFieldInt8(0, ALTITUDE_MODE, KMLAltitudeMode.CLAMP_TO_GROUND);
}

static addInterpolate(builder:flatbuffers.Builder, INTERPOLATE:boolean) {
  builder.addFieldInt8(1, +INTERPOLATE, +false);
}

static addTracks(builder:flatbuffers.Builder, TRACKSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, TRACKSOffset, 0);
}

static createTracksVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTracksVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endKMLMultiTrack(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLMultiTrack(builder:flatbuffers.Builder, ALTITUDE_MODE:KMLAltitudeMode, INTERPOLATE:boolean, TRACKSOffset:flatbuffers.Offset):flatbuffers.Offset {
  KMLMultiTrack.startKMLMultiTrack(builder);
  KMLMultiTrack.addAltitudeMode(builder, ALTITUDE_MODE);
  KMLMultiTrack.addInterpolate(builder, INTERPOLATE);
  KMLMultiTrack.addTracks(builder, TRACKSOffset);
  return KMLMultiTrack.endKMLMultiTrack(builder);
}

unpack(): KMLMultiTrackT {
  return new KMLMultiTrackT(
    this.ALTITUDE_MODE(),
    this.INTERPOLATE(),
    this.bb!.createObjList<KMLTrack, KMLTrackT>(this.TRACKS.bind(this), this.tracksLength())
  );
}


unpackTo(_o: KMLMultiTrackT): void {
  _o.ALTITUDE_MODE = this.ALTITUDE_MODE();
  _o.INTERPOLATE = this.INTERPOLATE();
  _o.TRACKS = this.bb!.createObjList<KMLTrack, KMLTrackT>(this.TRACKS.bind(this), this.tracksLength());
}
}

export class KMLMultiTrackT implements flatbuffers.IGeneratedObject {
constructor(
  public ALTITUDE_MODE: KMLAltitudeMode = KMLAltitudeMode.CLAMP_TO_GROUND,
  public INTERPOLATE: boolean = false,
  public TRACKS: (KMLTrackT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const TRACKS = KMLMultiTrack.createTracksVector(builder, builder.createObjectOffsetList(this.TRACKS));

  return KMLMultiTrack.createKMLMultiTrack(builder,
    this.ALTITUDE_MODE,
    this.INTERPOLATE,
    TRACKS
  );
}
}
