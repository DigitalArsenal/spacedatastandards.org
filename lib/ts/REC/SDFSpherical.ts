// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Spherical SDF parameters.
 */
export class SDFSpherical implements flatbuffers.IUnpackableObject<SDFSphericalT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SDFSpherical {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSDFSpherical(bb:flatbuffers.ByteBuffer, obj?:SDFSpherical):SDFSpherical {
  return (obj || new SDFSpherical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSDFSpherical(bb:flatbuffers.ByteBuffer, obj?:SDFSpherical):SDFSpherical {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SDFSpherical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

RADIUS():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Inner radius for hollow sphere (0 == solid).
 */
INNER_RADIUS():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startSDFSpherical(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addRadius(builder:flatbuffers.Builder, RADIUS:number) {
  builder.addFieldFloat64(0, RADIUS, 0.0);
}

static addInnerRadius(builder:flatbuffers.Builder, INNER_RADIUS:number) {
  builder.addFieldFloat64(1, INNER_RADIUS, 0.0);
}

static endSDFSpherical(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSDFSpherical(builder:flatbuffers.Builder, RADIUS:number, INNER_RADIUS:number):flatbuffers.Offset {
  SDFSpherical.startSDFSpherical(builder);
  SDFSpherical.addRadius(builder, RADIUS);
  SDFSpherical.addInnerRadius(builder, INNER_RADIUS);
  return SDFSpherical.endSDFSpherical(builder);
}

unpack(): SDFSphericalT {
  return new SDFSphericalT(
    this.RADIUS(),
    this.INNER_RADIUS()
  );
}


unpackTo(_o: SDFSphericalT): void {
  _o.RADIUS = this.RADIUS();
  _o.INNER_RADIUS = this.INNER_RADIUS();
}
}

export class SDFSphericalT implements flatbuffers.IGeneratedObject {
constructor(
  public RADIUS: number = 0.0,
  public INNER_RADIUS: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return SDFSpherical.createSDFSpherical(builder,
    this.RADIUS,
    this.INNER_RADIUS
  );
}
}
