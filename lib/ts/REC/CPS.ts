// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { packetCompressionAlgorithm } from './packetCompressionAlgorithm.js';


/**
 * Compressed Packet Stream — a run of fixed-length CCSDS packets losslessly
 * compressed as one stateful stream (CCSDS 124.0-B-1 POCKET+). Decoded output
 * is a sequence of PACKET_COUNT packets of PACKET_LENGTH bytes, each of which
 * projects to an $SPP record. This record carries a compressed packet stream
 * only; it is not a general-purpose compression envelope for SDS records.
 */
export class CPS implements flatbuffers.IUnpackableObject<CPST> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CPS {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCPS(bb:flatbuffers.ByteBuffer, obj?:CPS):CPS {
  return (obj || new CPS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCPS(bb:flatbuffers.ByteBuffer, obj?:CPS):CPS {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CPS()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$CPS');
}

/**
 * Compression algorithm used to produce COMPRESSED_DATA.
 */
ALGORITHM():packetCompressionAlgorithm {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : packetCompressionAlgorithm.POCKET_PLUS;
}

/**
 * Uncompressed length in bytes of EVERY packet in the stream. POCKET+ is
 * defined only for a fixed packet length; a length change starts a new
 * stream and therefore a new $CPS record.
 */
PACKET_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Number of uncompressed packets encoded in COMPRESSED_DATA. NORMATIVE:
 * this is the only output-size bound a decoder has. The compressed
 * bitstream alone does not bound the decoded size, and a compression-ratio
 * guess is unsafe in both directions. Decoders MUST reject a record whose
 * PACKET_COUNT is zero, and MUST allocate exactly
 * PACKET_COUNT * PACKET_LENGTH bytes of output.
 */
PACKET_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * POCKET+ robustness level: the window depth in PACKETS, range 0-7. This is
 * a window depth, NOT a retransmission interval — it does not by itself
 * configure a decoder. See MASK_UPDATE_PERIOD / MASK_SEND_PERIOD /
 * REFERENCE_PERIOD, which are three independent periods.
 */
ROBUSTNESS():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * POCKET+ mask update period (pt), in packets. Independent of ROBUSTNESS,
 * MASK_SEND_PERIOD, and REFERENCE_PERIOD.
 */
MASK_UPDATE_PERIOD():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * POCKET+ mask send period (ft), in packets — how often the change mask is
 * transmitted. Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and
 * REFERENCE_PERIOD.
 */
MASK_SEND_PERIOD():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * POCKET+ reference packet retransmission period (rt), in packets.
 * Independent of ROBUSTNESS, MASK_UPDATE_PERIOD, and MASK_SEND_PERIOD.
 */
REFERENCE_PERIOD():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * True only when the FIRST packet of COMPRESSED_DATA carries both rt=1 and
 * ft=1, i.e. it transmits a full reference packet and a full mask, so this
 * record decodes standalone without its predecessor. There is no
 * synchronization flag in the POCKET+ bitstream itself: this field is the
 * producer's assertion about the first packet, and defaults to false.
 */
SELF_SYNCHRONIZING():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Producer-defined stream identity. Records sharing STREAM_ID decode in
 * SEGMENT_INDEX order as one continuous POCKET+ stream. A missing segment
 * poisons every subsequent segment up to the next segment with
 * SELF_SYNCHRONIZING = true.
 */
STREAM_ID():string|null
STREAM_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
STREAM_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Zero-based index of this segment within STREAM_ID.
 */
SEGMENT_INDEX():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * CCSDS Application Process Identifier of the packets, when known.
 */
APID():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Spacecraft identifier of the packets, when known.
 */
SPACECRAFT_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * UTC epoch of the first packet in the segment, ISO 8601.
 */
START_EPOCH():string|null
START_EPOCH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
START_EPOCH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * UTC epoch of the last packet in the segment, ISO 8601.
 */
STOP_EPOCH():string|null
STOP_EPOCH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
STOP_EPOCH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * The POCKET+ compressed bitstream per CCSDS 124.0-B-1. Each compressed
 * packet is individually octet-aligned; padding is per PACKET, never per
 * stream. Bit-concatenating packets diverges from the reference
 * implementation and MUST NOT be done.
 */
COMPRESSED_DATA(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

compressedDataLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

compressedDataArray():Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

/**
 * PACKET_LENGTH * PACKET_COUNT. Carried so a decoder can check its decoded
 * output length without recomputing the product from a corrupt header.
 */
UNCOMPRESSED_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * CRC-32 (IEEE 802.3) over the concatenated decompressed packets.
 */
UNCOMPRESSED_CRC32():number {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startCPS(builder:flatbuffers.Builder) {
  builder.startObject(17);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHM:packetCompressionAlgorithm) {
  builder.addFieldInt8(0, ALGORITHM, packetCompressionAlgorithm.POCKET_PLUS);
}

static addPacketLength(builder:flatbuffers.Builder, PACKET_LENGTH:number) {
  builder.addFieldInt16(1, PACKET_LENGTH, 0);
}

static addPacketCount(builder:flatbuffers.Builder, PACKET_COUNT:number) {
  builder.addFieldInt32(2, PACKET_COUNT, 0);
}

static addRobustness(builder:flatbuffers.Builder, ROBUSTNESS:number) {
  builder.addFieldInt8(3, ROBUSTNESS, 0);
}

static addMaskUpdatePeriod(builder:flatbuffers.Builder, MASK_UPDATE_PERIOD:number) {
  builder.addFieldInt16(4, MASK_UPDATE_PERIOD, 0);
}

static addMaskSendPeriod(builder:flatbuffers.Builder, MASK_SEND_PERIOD:number) {
  builder.addFieldInt16(5, MASK_SEND_PERIOD, 0);
}

static addReferencePeriod(builder:flatbuffers.Builder, REFERENCE_PERIOD:number) {
  builder.addFieldInt16(6, REFERENCE_PERIOD, 0);
}

static addSelfSynchronizing(builder:flatbuffers.Builder, SELF_SYNCHRONIZING:boolean) {
  builder.addFieldInt8(7, +SELF_SYNCHRONIZING, +false);
}

static addStreamId(builder:flatbuffers.Builder, STREAM_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, STREAM_IDOffset, 0);
}

static addSegmentIndex(builder:flatbuffers.Builder, SEGMENT_INDEX:number) {
  builder.addFieldInt32(9, SEGMENT_INDEX, 0);
}

static addApid(builder:flatbuffers.Builder, APID:number) {
  builder.addFieldInt16(10, APID, 0);
}

static addSpacecraftId(builder:flatbuffers.Builder, SPACECRAFT_ID:number) {
  builder.addFieldInt16(11, SPACECRAFT_ID, 0);
}

static addStartEpoch(builder:flatbuffers.Builder, START_EPOCHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, START_EPOCHOffset, 0);
}

static addStopEpoch(builder:flatbuffers.Builder, STOP_EPOCHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, STOP_EPOCHOffset, 0);
}

static addCompressedData(builder:flatbuffers.Builder, COMPRESSED_DATAOffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, COMPRESSED_DATAOffset, 0);
}

static createCompressedDataVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCompressedDataVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addUncompressedLength(builder:flatbuffers.Builder, UNCOMPRESSED_LENGTH:number) {
  builder.addFieldInt32(15, UNCOMPRESSED_LENGTH, 0);
}

static addUncompressedCrc32(builder:flatbuffers.Builder, UNCOMPRESSED_CRC32:number) {
  builder.addFieldInt32(16, UNCOMPRESSED_CRC32, 0);
}

static endCPS(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 32) // COMPRESSED_DATA
  return offset;
}

static finishCPSBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$CPS');
}

static finishSizePrefixedCPSBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$CPS', true);
}

static createCPS(builder:flatbuffers.Builder, ALGORITHM:packetCompressionAlgorithm, PACKET_LENGTH:number, PACKET_COUNT:number, ROBUSTNESS:number, MASK_UPDATE_PERIOD:number, MASK_SEND_PERIOD:number, REFERENCE_PERIOD:number, SELF_SYNCHRONIZING:boolean, STREAM_IDOffset:flatbuffers.Offset, SEGMENT_INDEX:number, APID:number, SPACECRAFT_ID:number, START_EPOCHOffset:flatbuffers.Offset, STOP_EPOCHOffset:flatbuffers.Offset, COMPRESSED_DATAOffset:flatbuffers.Offset, UNCOMPRESSED_LENGTH:number, UNCOMPRESSED_CRC32:number):flatbuffers.Offset {
  CPS.startCPS(builder);
  CPS.addAlgorithm(builder, ALGORITHM);
  CPS.addPacketLength(builder, PACKET_LENGTH);
  CPS.addPacketCount(builder, PACKET_COUNT);
  CPS.addRobustness(builder, ROBUSTNESS);
  CPS.addMaskUpdatePeriod(builder, MASK_UPDATE_PERIOD);
  CPS.addMaskSendPeriod(builder, MASK_SEND_PERIOD);
  CPS.addReferencePeriod(builder, REFERENCE_PERIOD);
  CPS.addSelfSynchronizing(builder, SELF_SYNCHRONIZING);
  CPS.addStreamId(builder, STREAM_IDOffset);
  CPS.addSegmentIndex(builder, SEGMENT_INDEX);
  CPS.addApid(builder, APID);
  CPS.addSpacecraftId(builder, SPACECRAFT_ID);
  CPS.addStartEpoch(builder, START_EPOCHOffset);
  CPS.addStopEpoch(builder, STOP_EPOCHOffset);
  CPS.addCompressedData(builder, COMPRESSED_DATAOffset);
  CPS.addUncompressedLength(builder, UNCOMPRESSED_LENGTH);
  CPS.addUncompressedCrc32(builder, UNCOMPRESSED_CRC32);
  return CPS.endCPS(builder);
}

unpack(): CPST {
  return new CPST(
    this.ALGORITHM(),
    this.PACKET_LENGTH(),
    this.PACKET_COUNT(),
    this.ROBUSTNESS(),
    this.MASK_UPDATE_PERIOD(),
    this.MASK_SEND_PERIOD(),
    this.REFERENCE_PERIOD(),
    this.SELF_SYNCHRONIZING(),
    this.STREAM_ID(),
    this.SEGMENT_INDEX(),
    this.APID(),
    this.SPACECRAFT_ID(),
    this.START_EPOCH(),
    this.STOP_EPOCH(),
    this.bb!.createScalarList<number>(this.COMPRESSED_DATA.bind(this), this.compressedDataLength()),
    this.UNCOMPRESSED_LENGTH(),
    this.UNCOMPRESSED_CRC32()
  );
}


unpackTo(_o: CPST): void {
  _o.ALGORITHM = this.ALGORITHM();
  _o.PACKET_LENGTH = this.PACKET_LENGTH();
  _o.PACKET_COUNT = this.PACKET_COUNT();
  _o.ROBUSTNESS = this.ROBUSTNESS();
  _o.MASK_UPDATE_PERIOD = this.MASK_UPDATE_PERIOD();
  _o.MASK_SEND_PERIOD = this.MASK_SEND_PERIOD();
  _o.REFERENCE_PERIOD = this.REFERENCE_PERIOD();
  _o.SELF_SYNCHRONIZING = this.SELF_SYNCHRONIZING();
  _o.STREAM_ID = this.STREAM_ID();
  _o.SEGMENT_INDEX = this.SEGMENT_INDEX();
  _o.APID = this.APID();
  _o.SPACECRAFT_ID = this.SPACECRAFT_ID();
  _o.START_EPOCH = this.START_EPOCH();
  _o.STOP_EPOCH = this.STOP_EPOCH();
  _o.COMPRESSED_DATA = this.bb!.createScalarList<number>(this.COMPRESSED_DATA.bind(this), this.compressedDataLength());
  _o.UNCOMPRESSED_LENGTH = this.UNCOMPRESSED_LENGTH();
  _o.UNCOMPRESSED_CRC32 = this.UNCOMPRESSED_CRC32();
}
}

export class CPST implements flatbuffers.IGeneratedObject {
constructor(
  public ALGORITHM: packetCompressionAlgorithm = packetCompressionAlgorithm.POCKET_PLUS,
  public PACKET_LENGTH: number = 0,
  public PACKET_COUNT: number = 0,
  public ROBUSTNESS: number = 0,
  public MASK_UPDATE_PERIOD: number = 0,
  public MASK_SEND_PERIOD: number = 0,
  public REFERENCE_PERIOD: number = 0,
  public SELF_SYNCHRONIZING: boolean = false,
  public STREAM_ID: string|Uint8Array|null = null,
  public SEGMENT_INDEX: number = 0,
  public APID: number = 0,
  public SPACECRAFT_ID: number = 0,
  public START_EPOCH: string|Uint8Array|null = null,
  public STOP_EPOCH: string|Uint8Array|null = null,
  public COMPRESSED_DATA: (number)[] = [],
  public UNCOMPRESSED_LENGTH: number = 0,
  public UNCOMPRESSED_CRC32: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const STREAM_ID = (this.STREAM_ID !== null ? builder.createString(this.STREAM_ID!) : 0);
  const START_EPOCH = (this.START_EPOCH !== null ? builder.createString(this.START_EPOCH!) : 0);
  const STOP_EPOCH = (this.STOP_EPOCH !== null ? builder.createString(this.STOP_EPOCH!) : 0);
  const COMPRESSED_DATA = CPS.createCompressedDataVector(builder, this.COMPRESSED_DATA);

  return CPS.createCPS(builder,
    this.ALGORITHM,
    this.PACKET_LENGTH,
    this.PACKET_COUNT,
    this.ROBUSTNESS,
    this.MASK_UPDATE_PERIOD,
    this.MASK_SEND_PERIOD,
    this.REFERENCE_PERIOD,
    this.SELF_SYNCHRONIZING,
    STREAM_ID,
    this.SEGMENT_INDEX,
    this.APID,
    this.SPACECRAFT_ID,
    START_EPOCH,
    STOP_EPOCH,
    COMPRESSED_DATA,
    this.UNCOMPRESSED_LENGTH,
    this.UNCOMPRESSED_CRC32
  );
}
}
