// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GJNBoundingBox, GJNBoundingBoxT } from './GJNBoundingBox.js';
import { GJNGeometryType } from './GJNGeometryType.js';
import { GJNLinearRing, GJNLinearRingT } from './GJNLinearRing.js';
import { GJNPolygonRings, GJNPolygonRingsT } from './GJNPolygonRings.js';
import { GJNPosition, GJNPositionT } from './GJNPosition.js';


/**
 * GeoJSON Geometry object
 */
export class GJNGeometry implements flatbuffers.IUnpackableObject<GJNGeometryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GJNGeometry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGJNGeometry(bb:flatbuffers.ByteBuffer, obj?:GJNGeometry):GJNGeometry {
  return (obj || new GJNGeometry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGJNGeometry(bb:flatbuffers.ByteBuffer, obj?:GJNGeometry):GJNGeometry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GJNGeometry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Geometry type
 */
TYPE():GJNGeometryType {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : GJNGeometryType.POINT;
}

/**
 * Single position (for Point)
 */
POINT(obj?:GJNPosition):GJNPosition|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new GJNPosition()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Array of positions (for MultiPoint, LineString)
 */
POSITIONS(index: number, obj?:GJNPosition):GJNPosition|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new GJNPosition()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

positionsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Array of position arrays (for MultiLineString, Polygon rings)
 */
RINGS(index: number, obj?:GJNLinearRing):GJNLinearRing|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new GJNLinearRing()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

ringsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Array of polygons each as array of rings (for MultiPolygon)
 */
POLYGON_RINGS(index: number, obj?:GJNPolygonRings):GJNPolygonRings|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new GJNPolygonRings()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

polygonRingsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Child geometries (for GeometryCollection)
 */
GEOMETRIES(index: number, obj?:GJNGeometry):GJNGeometry|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new GJNGeometry()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

geometriesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Bounding box (optional, per RFC 7946 Section 5)
 */
BBOX(obj?:GJNBoundingBox):GJNBoundingBox|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new GJNBoundingBox()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startGJNGeometry(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addType(builder:flatbuffers.Builder, TYPE:GJNGeometryType) {
  builder.addFieldInt8(0, TYPE, GJNGeometryType.POINT);
}

static addPoint(builder:flatbuffers.Builder, POINTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, POINTOffset, 0);
}

static addPositions(builder:flatbuffers.Builder, POSITIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, POSITIONSOffset, 0);
}

static createPositionsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPositionsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addRings(builder:flatbuffers.Builder, RINGSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, RINGSOffset, 0);
}

static createRingsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startRingsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addPolygonRings(builder:flatbuffers.Builder, POLYGON_RINGSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, POLYGON_RINGSOffset, 0);
}

static createPolygonRingsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPolygonRingsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addGeometries(builder:flatbuffers.Builder, GEOMETRIESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, GEOMETRIESOffset, 0);
}

static createGeometriesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startGeometriesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBbox(builder:flatbuffers.Builder, BBOXOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, BBOXOffset, 0);
}

static endGJNGeometry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): GJNGeometryT {
  return new GJNGeometryT(
    this.TYPE(),
    (this.POINT() !== null ? this.POINT()!.unpack() : null),
    this.bb!.createObjList<GJNPosition, GJNPositionT>(this.POSITIONS.bind(this), this.positionsLength()),
    this.bb!.createObjList<GJNLinearRing, GJNLinearRingT>(this.RINGS.bind(this), this.ringsLength()),
    this.bb!.createObjList<GJNPolygonRings, GJNPolygonRingsT>(this.POLYGON_RINGS.bind(this), this.polygonRingsLength()),
    this.bb!.createObjList<GJNGeometry, GJNGeometryT>(this.GEOMETRIES.bind(this), this.geometriesLength()),
    (this.BBOX() !== null ? this.BBOX()!.unpack() : null)
  );
}


unpackTo(_o: GJNGeometryT): void {
  _o.TYPE = this.TYPE();
  _o.POINT = (this.POINT() !== null ? this.POINT()!.unpack() : null);
  _o.POSITIONS = this.bb!.createObjList<GJNPosition, GJNPositionT>(this.POSITIONS.bind(this), this.positionsLength());
  _o.RINGS = this.bb!.createObjList<GJNLinearRing, GJNLinearRingT>(this.RINGS.bind(this), this.ringsLength());
  _o.POLYGON_RINGS = this.bb!.createObjList<GJNPolygonRings, GJNPolygonRingsT>(this.POLYGON_RINGS.bind(this), this.polygonRingsLength());
  _o.GEOMETRIES = this.bb!.createObjList<GJNGeometry, GJNGeometryT>(this.GEOMETRIES.bind(this), this.geometriesLength());
  _o.BBOX = (this.BBOX() !== null ? this.BBOX()!.unpack() : null);
}
}

export class GJNGeometryT implements flatbuffers.IGeneratedObject {
constructor(
  public TYPE: GJNGeometryType = GJNGeometryType.POINT,
  public POINT: GJNPositionT|null = null,
  public POSITIONS: (GJNPositionT)[] = [],
  public RINGS: (GJNLinearRingT)[] = [],
  public POLYGON_RINGS: (GJNPolygonRingsT)[] = [],
  public GEOMETRIES: (GJNGeometryT)[] = [],
  public BBOX: GJNBoundingBoxT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const POINT = (this.POINT !== null ? this.POINT!.pack(builder) : 0);
  const POSITIONS = GJNGeometry.createPositionsVector(builder, builder.createObjectOffsetList(this.POSITIONS));
  const RINGS = GJNGeometry.createRingsVector(builder, builder.createObjectOffsetList(this.RINGS));
  const POLYGON_RINGS = GJNGeometry.createPolygonRingsVector(builder, builder.createObjectOffsetList(this.POLYGON_RINGS));
  const GEOMETRIES = GJNGeometry.createGeometriesVector(builder, builder.createObjectOffsetList(this.GEOMETRIES));
  const BBOX = (this.BBOX !== null ? this.BBOX!.pack(builder) : 0);

  GJNGeometry.startGJNGeometry(builder);
  GJNGeometry.addType(builder, this.TYPE);
  GJNGeometry.addPoint(builder, POINT);
  GJNGeometry.addPositions(builder, POSITIONS);
  GJNGeometry.addRings(builder, RINGS);
  GJNGeometry.addPolygonRings(builder, POLYGON_RINGS);
  GJNGeometry.addGeometries(builder, GEOMETRIES);
  GJNGeometry.addBbox(builder, BBOX);

  return GJNGeometry.endGJNGeometry(builder);
}
}
