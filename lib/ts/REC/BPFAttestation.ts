// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * The publisher's attestation over this profile, in BOTH ratified forms.
 *
 * PRESENCE IS THE STATEMENT. A profile is SIGNED if and only if this table
 * is present; a profile whose `ATTESTATION` is absent is UNSIGNED, and a
 * consumer marks it as such. There is deliberately no boolean saying so: a
 * self-asserted "signed" flag is attacker-controlled and proves nothing,
 * while presence of the table is checkable.
 *
 * FAIL CLOSED. When this table is present, every field below MUST be
 * present and BOTH signatures MUST verify independently against
 * `SIGNING_PUBLIC_KEY`. A profile whose attestation is incomplete, or
 * whose either signature fails, is REJECTED — it is NEVER downgraded to
 * "unsigned and therefore acceptable", which would let stripping one
 * signature launder a tampered profile past an importer that admits
 * unsigned input.
 */
export class BPFAttestation implements flatbuffers.IUnpackableObject<BPFAttestationT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BPFAttestation {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBPFAttestation(bb:flatbuffers.ByteBuffer, obj?:BPFAttestation):BPFAttestation {
  return (obj || new BPFAttestation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBPFAttestation(bb:flatbuffers.ByteBuffer, obj?:BPFAttestation):BPFAttestation {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BPFAttestation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * The signer's LITERAL Ed25519 public key, 64 lowercase hexadecimal
 * characters. A derivation ancestor is structurally absent from this
 * standard: publishing one lets any holder of a child key walk the
 * signer's whole key tree. Rotation is republication.
 */
SIGNING_PUBLIC_KEY():string
SIGNING_PUBLIC_KEY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
SIGNING_PUBLIC_KEY(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ)
 * at which the attestation was produced.
 */
SIGNED_AT():string|null
SIGNED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SIGNED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Ed25519 signature over the size-prefixed FlatBuffer with both 64-byte
 * signature payloads zeroed while preserving their vectors and offsets.
 */
SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signatureArray():Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

/**
 * Ed25519 signature over canonical JSON with IDL field order and
 * capitalization, no insignificant whitespace, and both signature fields
 * omitted.
 */
CANONICAL_JSON_SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

canonicalJsonSignatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

canonicalJsonSignatureArray():Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset));
}

static startBPFAttestation(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addSigningPublicKey(builder:flatbuffers.Builder, SIGNING_PUBLIC_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, SIGNING_PUBLIC_KEYOffset, 0);
}

static addSignedAt(builder:flatbuffers.Builder, SIGNED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, SIGNED_ATOffset, 0);
}

static addSignature(builder:flatbuffers.Builder, SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, SIGNATUREOffset, 0);
}

static createSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addCanonicalJsonSignature(builder:flatbuffers.Builder, CANONICAL_JSON_SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, CANONICAL_JSON_SIGNATUREOffset, 0);
}

static createCanonicalJsonSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCanonicalJsonSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endBPFAttestation(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // SIGNING_PUBLIC_KEY
  builder.requiredField(offset, 8) // SIGNATURE
  builder.requiredField(offset, 10) // CANONICAL_JSON_SIGNATURE
  return offset;
}

static createBPFAttestation(builder:flatbuffers.Builder, SIGNING_PUBLIC_KEYOffset:flatbuffers.Offset, SIGNED_ATOffset:flatbuffers.Offset, SIGNATUREOffset:flatbuffers.Offset, CANONICAL_JSON_SIGNATUREOffset:flatbuffers.Offset):flatbuffers.Offset {
  BPFAttestation.startBPFAttestation(builder);
  BPFAttestation.addSigningPublicKey(builder, SIGNING_PUBLIC_KEYOffset);
  BPFAttestation.addSignedAt(builder, SIGNED_ATOffset);
  BPFAttestation.addSignature(builder, SIGNATUREOffset);
  BPFAttestation.addCanonicalJsonSignature(builder, CANONICAL_JSON_SIGNATUREOffset);
  return BPFAttestation.endBPFAttestation(builder);
}

unpack(): BPFAttestationT {
  return new BPFAttestationT(
    this.SIGNING_PUBLIC_KEY(),
    this.SIGNED_AT(),
    this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength()),
    this.bb!.createScalarList<number>(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength())
  );
}


unpackTo(_o: BPFAttestationT): void {
  _o.SIGNING_PUBLIC_KEY = this.SIGNING_PUBLIC_KEY();
  _o.SIGNED_AT = this.SIGNED_AT();
  _o.SIGNATURE = this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength());
  _o.CANONICAL_JSON_SIGNATURE = this.bb!.createScalarList<number>(this.CANONICAL_JSON_SIGNATURE.bind(this), this.canonicalJsonSignatureLength());
}
}

export class BPFAttestationT implements flatbuffers.IGeneratedObject {
constructor(
  public SIGNING_PUBLIC_KEY: string|Uint8Array|null = null,
  public SIGNED_AT: string|Uint8Array|null = null,
  public SIGNATURE: (number)[] = [],
  public CANONICAL_JSON_SIGNATURE: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const SIGNING_PUBLIC_KEY = (this.SIGNING_PUBLIC_KEY !== null ? builder.createString(this.SIGNING_PUBLIC_KEY!) : 0);
  const SIGNED_AT = (this.SIGNED_AT !== null ? builder.createString(this.SIGNED_AT!) : 0);
  const SIGNATURE = BPFAttestation.createSignatureVector(builder, this.SIGNATURE);
  const CANONICAL_JSON_SIGNATURE = BPFAttestation.createCanonicalJsonSignatureVector(builder, this.CANONICAL_JSON_SIGNATURE);

  return BPFAttestation.createBPFAttestation(builder,
    SIGNING_PUBLIC_KEY,
    SIGNED_AT,
    SIGNATURE,
    CANONICAL_JSON_SIGNATURE
  );
}
}
