// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One placement of a variant in the tile frame. The transform applied to a
 * variant-frame point p is: scale by (SCALE_X, SCALE_Y, SCALE_Z), mirror per
 * SYMMETRY, rotate by YAW_RAD about +Z, translate by (X_M, Y_M, Z_M). The
 * aligned-binary INSTANCES section carries these fields in this order as
 * float32.
 */
export class GCTInstance implements flatbuffers.IUnpackableObject<GCTInstanceT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GCTInstance {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGCTInstance(bb:flatbuffers.ByteBuffer, obj?:GCTInstance):GCTInstance {
  return (obj || new GCTInstance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGCTInstance(bb:flatbuffers.ByteBuffer, obj?:GCTInstance):GCTInstance {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GCTInstance()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Index into GCT.VARIANTS.
 */
VARIANT():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Position of the variant's origin in the tile frame, metres east.
 */
X_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Metres north.
 */
Y_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Metres up (above the tile's ENU origin).
 */
Z_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Scale along the variant frame's axes; 1 is the variant as authored.
 */
SCALE_X():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

SCALE_Y():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

SCALE_Z():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

/**
 * Rotation about +Z, radians, counter-clockwise from east.
 */
YAW_RAD():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * One of eight axis mirror symmetries applied before the yaw: bit 0
 * mirrors x, bit 1 mirrors y, bit 2 swaps x and y.
 */
SYMMETRY():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Strength of the consumer's per-instance erosion and warp in [0, 1] (0:
 * the variant exactly as authored). Values above 2 select a shared deck
 * gap field with hole fraction VALUE - 2, per the producer's convention
 * stated in PRODUCER_NOTES.
 */
EROSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Producer-stable identity of the cloud this instance belongs to, equal
 * across tiles, levels and valid times while the cloud persists — the
 * key a consumer fades and tracks by. 0 means unstated.
 */
CLOUD_ID():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Cluster the instance belongs to (a chain of cumulus, a tower), for
 * grouping; 0 means none.
 */
CLUSTER_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Shape error, metres: the silhouette distance between this placement
 * and the cloud it stands in for. 0 means the instance IS the cloud (a
 * unique variant); a class stand-in placed for a chain of five cumulus
 * carries half the member spacing. A consumer that refines below the
 * shape error requests the unique variant.
 */
SHAPE_ERROR_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Width of the cloud as placed, metres, for size-dependent decisions.
 */
WIDTH_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

static startGCTInstance(builder:flatbuffers.Builder) {
  builder.startObject(14);
}

static addVariant(builder:flatbuffers.Builder, VARIANT:number) {
  builder.addFieldInt32(0, VARIANT, 0);
}

static addXM(builder:flatbuffers.Builder, X_M:number) {
  builder.addFieldFloat32(1, X_M, 0.0);
}

static addYM(builder:flatbuffers.Builder, Y_M:number) {
  builder.addFieldFloat32(2, Y_M, 0.0);
}

static addZM(builder:flatbuffers.Builder, Z_M:number) {
  builder.addFieldFloat32(3, Z_M, 0.0);
}

static addScaleX(builder:flatbuffers.Builder, SCALE_X:number) {
  builder.addFieldFloat32(4, SCALE_X, 1.0);
}

static addScaleY(builder:flatbuffers.Builder, SCALE_Y:number) {
  builder.addFieldFloat32(5, SCALE_Y, 1.0);
}

static addScaleZ(builder:flatbuffers.Builder, SCALE_Z:number) {
  builder.addFieldFloat32(6, SCALE_Z, 1.0);
}

static addYawRad(builder:flatbuffers.Builder, YAW_RAD:number) {
  builder.addFieldFloat32(7, YAW_RAD, 0.0);
}

static addSymmetry(builder:flatbuffers.Builder, SYMMETRY:number) {
  builder.addFieldInt8(8, SYMMETRY, 0);
}

static addErosion(builder:flatbuffers.Builder, EROSION:number) {
  builder.addFieldFloat32(9, EROSION, 0.0);
}

static addCloudId(builder:flatbuffers.Builder, CLOUD_ID:bigint) {
  builder.addFieldInt64(10, CLOUD_ID, BigInt('0'));
}

static addClusterId(builder:flatbuffers.Builder, CLUSTER_ID:number) {
  builder.addFieldInt32(11, CLUSTER_ID, 0);
}

static addShapeErrorM(builder:flatbuffers.Builder, SHAPE_ERROR_M:number) {
  builder.addFieldFloat32(12, SHAPE_ERROR_M, 0.0);
}

static addWidthM(builder:flatbuffers.Builder, WIDTH_M:number) {
  builder.addFieldFloat32(13, WIDTH_M, 0.0);
}

static endGCTInstance(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGCTInstance(builder:flatbuffers.Builder, VARIANT:number, X_M:number, Y_M:number, Z_M:number, SCALE_X:number, SCALE_Y:number, SCALE_Z:number, YAW_RAD:number, SYMMETRY:number, EROSION:number, CLOUD_ID:bigint, CLUSTER_ID:number, SHAPE_ERROR_M:number, WIDTH_M:number):flatbuffers.Offset {
  GCTInstance.startGCTInstance(builder);
  GCTInstance.addVariant(builder, VARIANT);
  GCTInstance.addXM(builder, X_M);
  GCTInstance.addYM(builder, Y_M);
  GCTInstance.addZM(builder, Z_M);
  GCTInstance.addScaleX(builder, SCALE_X);
  GCTInstance.addScaleY(builder, SCALE_Y);
  GCTInstance.addScaleZ(builder, SCALE_Z);
  GCTInstance.addYawRad(builder, YAW_RAD);
  GCTInstance.addSymmetry(builder, SYMMETRY);
  GCTInstance.addErosion(builder, EROSION);
  GCTInstance.addCloudId(builder, CLOUD_ID);
  GCTInstance.addClusterId(builder, CLUSTER_ID);
  GCTInstance.addShapeErrorM(builder, SHAPE_ERROR_M);
  GCTInstance.addWidthM(builder, WIDTH_M);
  return GCTInstance.endGCTInstance(builder);
}

unpack(): GCTInstanceT {
  return new GCTInstanceT(
    this.VARIANT(),
    this.X_M(),
    this.Y_M(),
    this.Z_M(),
    this.SCALE_X(),
    this.SCALE_Y(),
    this.SCALE_Z(),
    this.YAW_RAD(),
    this.SYMMETRY(),
    this.EROSION(),
    this.CLOUD_ID(),
    this.CLUSTER_ID(),
    this.SHAPE_ERROR_M(),
    this.WIDTH_M()
  );
}


unpackTo(_o: GCTInstanceT): void {
  _o.VARIANT = this.VARIANT();
  _o.X_M = this.X_M();
  _o.Y_M = this.Y_M();
  _o.Z_M = this.Z_M();
  _o.SCALE_X = this.SCALE_X();
  _o.SCALE_Y = this.SCALE_Y();
  _o.SCALE_Z = this.SCALE_Z();
  _o.YAW_RAD = this.YAW_RAD();
  _o.SYMMETRY = this.SYMMETRY();
  _o.EROSION = this.EROSION();
  _o.CLOUD_ID = this.CLOUD_ID();
  _o.CLUSTER_ID = this.CLUSTER_ID();
  _o.SHAPE_ERROR_M = this.SHAPE_ERROR_M();
  _o.WIDTH_M = this.WIDTH_M();
}
}

export class GCTInstanceT implements flatbuffers.IGeneratedObject {
constructor(
  public VARIANT: number = 0,
  public X_M: number = 0.0,
  public Y_M: number = 0.0,
  public Z_M: number = 0.0,
  public SCALE_X: number = 1.0,
  public SCALE_Y: number = 1.0,
  public SCALE_Z: number = 1.0,
  public YAW_RAD: number = 0.0,
  public SYMMETRY: number = 0,
  public EROSION: number = 0.0,
  public CLOUD_ID: bigint = BigInt('0'),
  public CLUSTER_ID: number = 0,
  public SHAPE_ERROR_M: number = 0.0,
  public WIDTH_M: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return GCTInstance.createGCTInstance(builder,
    this.VARIANT,
    this.X_M,
    this.Y_M,
    this.Z_M,
    this.SCALE_X,
    this.SCALE_Y,
    this.SCALE_Z,
    this.YAW_RAD,
    this.SYMMETRY,
    this.EROSION,
    this.CLOUD_ID,
    this.CLUSTER_ID,
    this.SHAPE_ERROR_M,
    this.WIDTH_M
  );
}
}
