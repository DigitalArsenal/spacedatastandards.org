// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



export class SCVLatitudeBandStat implements flatbuffers.IUnpackableObject<SCVLatitudeBandStatT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCVLatitudeBandStat {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCVLatitudeBandStat(bb:flatbuffers.ByteBuffer, obj?:SCVLatitudeBandStat):SCVLatitudeBandStat {
  return (obj || new SCVLatitudeBandStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCVLatitudeBandStat(bb:flatbuffers.ByteBuffer, obj?:SCVLatitudeBandStat):SCVLatitudeBandStat {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCVLatitudeBandStat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

MIN_LAT_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MAX_LAT_DEG():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

COVERAGE_FRACTION():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MEAN_REVISIT_SEC():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MAX_GAP_SEC():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MEAN_REDUNDANCY():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startSCVLatitudeBandStat(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addMinLatDeg(builder:flatbuffers.Builder, MIN_LAT_DEG:number) {
  builder.addFieldFloat64(0, MIN_LAT_DEG, 0.0);
}

static addMaxLatDeg(builder:flatbuffers.Builder, MAX_LAT_DEG:number) {
  builder.addFieldFloat64(1, MAX_LAT_DEG, 0.0);
}

static addCoverageFraction(builder:flatbuffers.Builder, COVERAGE_FRACTION:number) {
  builder.addFieldFloat64(2, COVERAGE_FRACTION, 0.0);
}

static addMeanRevisitSec(builder:flatbuffers.Builder, MEAN_REVISIT_SEC:number) {
  builder.addFieldFloat64(3, MEAN_REVISIT_SEC, 0.0);
}

static addMaxGapSec(builder:flatbuffers.Builder, MAX_GAP_SEC:number) {
  builder.addFieldFloat64(4, MAX_GAP_SEC, 0.0);
}

static addMeanRedundancy(builder:flatbuffers.Builder, MEAN_REDUNDANCY:number) {
  builder.addFieldFloat64(5, MEAN_REDUNDANCY, 0.0);
}

static endSCVLatitudeBandStat(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSCVLatitudeBandStat(builder:flatbuffers.Builder, MIN_LAT_DEG:number, MAX_LAT_DEG:number, COVERAGE_FRACTION:number, MEAN_REVISIT_SEC:number, MAX_GAP_SEC:number, MEAN_REDUNDANCY:number):flatbuffers.Offset {
  SCVLatitudeBandStat.startSCVLatitudeBandStat(builder);
  SCVLatitudeBandStat.addMinLatDeg(builder, MIN_LAT_DEG);
  SCVLatitudeBandStat.addMaxLatDeg(builder, MAX_LAT_DEG);
  SCVLatitudeBandStat.addCoverageFraction(builder, COVERAGE_FRACTION);
  SCVLatitudeBandStat.addMeanRevisitSec(builder, MEAN_REVISIT_SEC);
  SCVLatitudeBandStat.addMaxGapSec(builder, MAX_GAP_SEC);
  SCVLatitudeBandStat.addMeanRedundancy(builder, MEAN_REDUNDANCY);
  return SCVLatitudeBandStat.endSCVLatitudeBandStat(builder);
}

unpack(): SCVLatitudeBandStatT {
  return new SCVLatitudeBandStatT(
    this.MIN_LAT_DEG(),
    this.MAX_LAT_DEG(),
    this.COVERAGE_FRACTION(),
    this.MEAN_REVISIT_SEC(),
    this.MAX_GAP_SEC(),
    this.MEAN_REDUNDANCY()
  );
}


unpackTo(_o: SCVLatitudeBandStatT): void {
  _o.MIN_LAT_DEG = this.MIN_LAT_DEG();
  _o.MAX_LAT_DEG = this.MAX_LAT_DEG();
  _o.COVERAGE_FRACTION = this.COVERAGE_FRACTION();
  _o.MEAN_REVISIT_SEC = this.MEAN_REVISIT_SEC();
  _o.MAX_GAP_SEC = this.MAX_GAP_SEC();
  _o.MEAN_REDUNDANCY = this.MEAN_REDUNDANCY();
}
}

export class SCVLatitudeBandStatT implements flatbuffers.IGeneratedObject {
constructor(
  public MIN_LAT_DEG: number = 0.0,
  public MAX_LAT_DEG: number = 0.0,
  public COVERAGE_FRACTION: number = 0.0,
  public MEAN_REVISIT_SEC: number = 0.0,
  public MAX_GAP_SEC: number = 0.0,
  public MEAN_REDUNDANCY: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return SCVLatitudeBandStat.createSCVLatitudeBandStat(builder,
    this.MIN_LAT_DEG,
    this.MAX_LAT_DEG,
    this.COVERAGE_FRACTION,
    this.MEAN_REVISIT_SEC,
    this.MAX_GAP_SEC,
    this.MEAN_REDUNDANCY
  );
}
}
