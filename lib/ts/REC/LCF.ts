// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KRF, KRFT } from './KRF.js';
import { licensingConfigMessageType } from './licensingConfigMessageType.js';
import { licensingConfigRole } from './licensingConfigRole.js';


/**
 * Licensing Configuration Frame
 */
export class LCF implements flatbuffers.IUnpackableObject<LCFT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LCF {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLCF(bb:flatbuffers.ByteBuffer, obj?:LCF):LCF {
  return (obj || new LCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLCF(bb:flatbuffers.ByteBuffer, obj?:LCF):LCF {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LCF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$LCF');
}

/**
 * Distinguishes configuration inputs from runtime status outputs.
 */
MESSAGE_TYPE():licensingConfigMessageType {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : licensingConfigMessageType.Configure;
}

/**
 * Indicates whether the configuration/status applies to server/provider or requester/client behavior.
 */
ROLE():licensingConfigRole {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : licensingConfigRole.Provider;
}

/**
 * Peer ID advertised by the licensing provider.
 */
PROVIDER_PEER_ID():string|null
PROVIDER_PEER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROVIDER_PEER_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Host-managed long-lived signing key used for grant signatures.
 */
PROVIDER_SIGNING_KEY(obj?:KRF):KRF|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new KRF()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Host-managed wrapping key used to protect per-publication content keys for requesters.
 */
PROVIDER_WRAPPING_KEY(obj?:KRF):KRF|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new KRF()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Optional requester signing key reference for requester-hosted flows.
 */
REQUESTER_SIGNING_KEY(obj?:KRF):KRF|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new KRF()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Logical active publication/grant key version.
 */
ACTIVE_KEY_VERSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Runtime expiry in unix milliseconds, or 0 if unbounded.
 */
EXPIRES_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Maximum accepted requester/provider clock skew in milliseconds.
 */
MAX_CLOCK_SKEW_MS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Challenge time-to-live in milliseconds.
 */
CHALLENGE_TTL_MS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Optional capability token bytes returned with successful grants.
 */
CAPABILITY_TOKEN(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

capabilityTokenLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

capabilityTokenArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Whether the runtime has been initialized successfully.
 */
INITIALIZED():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Whether the runtime needs issuer key rotation or refreshed configuration.
 */
NEEDS_ROTATION():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Optional machine-readable status code for status/error responses.
 */
STATUS_CODE():string|null
STATUS_CODE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
STATUS_CODE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Optional human-readable status or error message.
 */
STATUS_MESSAGE():string|null
STATUS_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
STATUS_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startLCF(builder:flatbuffers.Builder) {
  builder.startObject(15);
}

static addMessageType(builder:flatbuffers.Builder, MESSAGE_TYPE:licensingConfigMessageType) {
  builder.addFieldInt8(0, MESSAGE_TYPE, licensingConfigMessageType.Configure);
}

static addRole(builder:flatbuffers.Builder, ROLE:licensingConfigRole) {
  builder.addFieldInt8(1, ROLE, licensingConfigRole.Provider);
}

static addProviderPeerId(builder:flatbuffers.Builder, PROVIDER_PEER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, PROVIDER_PEER_IDOffset, 0);
}

static addProviderSigningKey(builder:flatbuffers.Builder, PROVIDER_SIGNING_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, PROVIDER_SIGNING_KEYOffset, 0);
}

static addProviderWrappingKey(builder:flatbuffers.Builder, PROVIDER_WRAPPING_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, PROVIDER_WRAPPING_KEYOffset, 0);
}

static addRequesterSigningKey(builder:flatbuffers.Builder, REQUESTER_SIGNING_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, REQUESTER_SIGNING_KEYOffset, 0);
}

static addActiveKeyVersion(builder:flatbuffers.Builder, ACTIVE_KEY_VERSION:number) {
  builder.addFieldInt32(6, ACTIVE_KEY_VERSION, 0);
}

static addExpiresAt(builder:flatbuffers.Builder, EXPIRES_AT:bigint) {
  builder.addFieldInt64(7, EXPIRES_AT, BigInt('0'));
}

static addMaxClockSkewMs(builder:flatbuffers.Builder, MAX_CLOCK_SKEW_MS:bigint) {
  builder.addFieldInt64(8, MAX_CLOCK_SKEW_MS, BigInt('0'));
}

static addChallengeTtlMs(builder:flatbuffers.Builder, CHALLENGE_TTL_MS:bigint) {
  builder.addFieldInt64(9, CHALLENGE_TTL_MS, BigInt('0'));
}

static addCapabilityToken(builder:flatbuffers.Builder, CAPABILITY_TOKENOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, CAPABILITY_TOKENOffset, 0);
}

static createCapabilityTokenVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCapabilityTokenVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addInitialized(builder:flatbuffers.Builder, INITIALIZED:boolean) {
  builder.addFieldInt8(11, +INITIALIZED, +false);
}

static addNeedsRotation(builder:flatbuffers.Builder, NEEDS_ROTATION:boolean) {
  builder.addFieldInt8(12, +NEEDS_ROTATION, +false);
}

static addStatusCode(builder:flatbuffers.Builder, STATUS_CODEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, STATUS_CODEOffset, 0);
}

static addStatusMessage(builder:flatbuffers.Builder, STATUS_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, STATUS_MESSAGEOffset, 0);
}

static endLCF(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishLCFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$LCF');
}

static finishSizePrefixedLCFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$LCF', true);
}


unpack(): LCFT {
  return new LCFT(
    this.MESSAGE_TYPE(),
    this.ROLE(),
    this.PROVIDER_PEER_ID(),
    (this.PROVIDER_SIGNING_KEY() !== null ? this.PROVIDER_SIGNING_KEY()!.unpack() : null),
    (this.PROVIDER_WRAPPING_KEY() !== null ? this.PROVIDER_WRAPPING_KEY()!.unpack() : null),
    (this.REQUESTER_SIGNING_KEY() !== null ? this.REQUESTER_SIGNING_KEY()!.unpack() : null),
    this.ACTIVE_KEY_VERSION(),
    this.EXPIRES_AT(),
    this.MAX_CLOCK_SKEW_MS(),
    this.CHALLENGE_TTL_MS(),
    this.bb!.createScalarList<number>(this.CAPABILITY_TOKEN.bind(this), this.capabilityTokenLength()),
    this.INITIALIZED(),
    this.NEEDS_ROTATION(),
    this.STATUS_CODE(),
    this.STATUS_MESSAGE()
  );
}


unpackTo(_o: LCFT): void {
  _o.MESSAGE_TYPE = this.MESSAGE_TYPE();
  _o.ROLE = this.ROLE();
  _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
  _o.PROVIDER_SIGNING_KEY = (this.PROVIDER_SIGNING_KEY() !== null ? this.PROVIDER_SIGNING_KEY()!.unpack() : null);
  _o.PROVIDER_WRAPPING_KEY = (this.PROVIDER_WRAPPING_KEY() !== null ? this.PROVIDER_WRAPPING_KEY()!.unpack() : null);
  _o.REQUESTER_SIGNING_KEY = (this.REQUESTER_SIGNING_KEY() !== null ? this.REQUESTER_SIGNING_KEY()!.unpack() : null);
  _o.ACTIVE_KEY_VERSION = this.ACTIVE_KEY_VERSION();
  _o.EXPIRES_AT = this.EXPIRES_AT();
  _o.MAX_CLOCK_SKEW_MS = this.MAX_CLOCK_SKEW_MS();
  _o.CHALLENGE_TTL_MS = this.CHALLENGE_TTL_MS();
  _o.CAPABILITY_TOKEN = this.bb!.createScalarList<number>(this.CAPABILITY_TOKEN.bind(this), this.capabilityTokenLength());
  _o.INITIALIZED = this.INITIALIZED();
  _o.NEEDS_ROTATION = this.NEEDS_ROTATION();
  _o.STATUS_CODE = this.STATUS_CODE();
  _o.STATUS_MESSAGE = this.STATUS_MESSAGE();
}
}

export class LCFT implements flatbuffers.IGeneratedObject {
constructor(
  public MESSAGE_TYPE: licensingConfigMessageType = licensingConfigMessageType.Configure,
  public ROLE: licensingConfigRole = licensingConfigRole.Provider,
  public PROVIDER_PEER_ID: string|Uint8Array|null = null,
  public PROVIDER_SIGNING_KEY: KRFT|null = null,
  public PROVIDER_WRAPPING_KEY: KRFT|null = null,
  public REQUESTER_SIGNING_KEY: KRFT|null = null,
  public ACTIVE_KEY_VERSION: number = 0,
  public EXPIRES_AT: bigint = BigInt('0'),
  public MAX_CLOCK_SKEW_MS: bigint = BigInt('0'),
  public CHALLENGE_TTL_MS: bigint = BigInt('0'),
  public CAPABILITY_TOKEN: (number)[] = [],
  public INITIALIZED: boolean = false,
  public NEEDS_ROTATION: boolean = false,
  public STATUS_CODE: string|Uint8Array|null = null,
  public STATUS_MESSAGE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID!) : 0);
  const PROVIDER_SIGNING_KEY = (this.PROVIDER_SIGNING_KEY !== null ? this.PROVIDER_SIGNING_KEY!.pack(builder) : 0);
  const PROVIDER_WRAPPING_KEY = (this.PROVIDER_WRAPPING_KEY !== null ? this.PROVIDER_WRAPPING_KEY!.pack(builder) : 0);
  const REQUESTER_SIGNING_KEY = (this.REQUESTER_SIGNING_KEY !== null ? this.REQUESTER_SIGNING_KEY!.pack(builder) : 0);
  const CAPABILITY_TOKEN = LCF.createCapabilityTokenVector(builder, this.CAPABILITY_TOKEN);
  const STATUS_CODE = (this.STATUS_CODE !== null ? builder.createString(this.STATUS_CODE!) : 0);
  const STATUS_MESSAGE = (this.STATUS_MESSAGE !== null ? builder.createString(this.STATUS_MESSAGE!) : 0);

  LCF.startLCF(builder);
  LCF.addMessageType(builder, this.MESSAGE_TYPE);
  LCF.addRole(builder, this.ROLE);
  LCF.addProviderPeerId(builder, PROVIDER_PEER_ID);
  LCF.addProviderSigningKey(builder, PROVIDER_SIGNING_KEY);
  LCF.addProviderWrappingKey(builder, PROVIDER_WRAPPING_KEY);
  LCF.addRequesterSigningKey(builder, REQUESTER_SIGNING_KEY);
  LCF.addActiveKeyVersion(builder, this.ACTIVE_KEY_VERSION);
  LCF.addExpiresAt(builder, this.EXPIRES_AT);
  LCF.addMaxClockSkewMs(builder, this.MAX_CLOCK_SKEW_MS);
  LCF.addChallengeTtlMs(builder, this.CHALLENGE_TTL_MS);
  LCF.addCapabilityToken(builder, CAPABILITY_TOKEN);
  LCF.addInitialized(builder, this.INITIALIZED);
  LCF.addNeedsRotation(builder, this.NEEDS_ROTATION);
  LCF.addStatusCode(builder, STATUS_CODE);
  LCF.addStatusMessage(builder, STATUS_MESSAGE);

  return LCF.endLCF(builder);
}
}
