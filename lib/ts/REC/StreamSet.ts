// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { CustomStream, CustomStreamT } from './CustomStream.js';
import { FixedFrameStream, FixedFrameStreamT } from './FixedFrameStream.js';
import { VariableFrameStream, VariableFrameStreamT } from './VariableFrameStream.js';


/**
 * Collection of streams
 */
export class StreamSet implements flatbuffers.IUnpackableObject<StreamSetT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):StreamSet {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsStreamSet(bb:flatbuffers.ByteBuffer, obj?:StreamSet):StreamSet {
  return (obj || new StreamSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsStreamSet(bb:flatbuffers.ByteBuffer, obj?:StreamSet):StreamSet {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new StreamSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Fixed frame streams
 */
FIXED_FRAME_STREAMS(index: number, obj?:FixedFrameStream):FixedFrameStream|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new FixedFrameStream()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

fixedFrameStreamsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Variable frame streams
 */
VARIABLE_FRAME_STREAMS(index: number, obj?:VariableFrameStream):VariableFrameStream|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new VariableFrameStream()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

variableFrameStreamsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Custom streams
 */
CUSTOM_STREAMS(index: number, obj?:CustomStream):CustomStream|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new CustomStream()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

customStreamsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startStreamSet(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addFixedFrameStreams(builder:flatbuffers.Builder, FIXED_FRAME_STREAMSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, FIXED_FRAME_STREAMSOffset, 0);
}

static createFixedFrameStreamsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startFixedFrameStreamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addVariableFrameStreams(builder:flatbuffers.Builder, VARIABLE_FRAME_STREAMSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, VARIABLE_FRAME_STREAMSOffset, 0);
}

static createVariableFrameStreamsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startVariableFrameStreamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCustomStreams(builder:flatbuffers.Builder, CUSTOM_STREAMSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, CUSTOM_STREAMSOffset, 0);
}

static createCustomStreamsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCustomStreamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endStreamSet(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createStreamSet(builder:flatbuffers.Builder, FIXED_FRAME_STREAMSOffset:flatbuffers.Offset, VARIABLE_FRAME_STREAMSOffset:flatbuffers.Offset, CUSTOM_STREAMSOffset:flatbuffers.Offset):flatbuffers.Offset {
  StreamSet.startStreamSet(builder);
  StreamSet.addFixedFrameStreams(builder, FIXED_FRAME_STREAMSOffset);
  StreamSet.addVariableFrameStreams(builder, VARIABLE_FRAME_STREAMSOffset);
  StreamSet.addCustomStreams(builder, CUSTOM_STREAMSOffset);
  return StreamSet.endStreamSet(builder);
}

unpack(): StreamSetT {
  return new StreamSetT(
    this.bb!.createObjList<FixedFrameStream, FixedFrameStreamT>(this.FIXED_FRAME_STREAMS.bind(this), this.fixedFrameStreamsLength()),
    this.bb!.createObjList<VariableFrameStream, VariableFrameStreamT>(this.VARIABLE_FRAME_STREAMS.bind(this), this.variableFrameStreamsLength()),
    this.bb!.createObjList<CustomStream, CustomStreamT>(this.CUSTOM_STREAMS.bind(this), this.customStreamsLength())
  );
}


unpackTo(_o: StreamSetT): void {
  _o.FIXED_FRAME_STREAMS = this.bb!.createObjList<FixedFrameStream, FixedFrameStreamT>(this.FIXED_FRAME_STREAMS.bind(this), this.fixedFrameStreamsLength());
  _o.VARIABLE_FRAME_STREAMS = this.bb!.createObjList<VariableFrameStream, VariableFrameStreamT>(this.VARIABLE_FRAME_STREAMS.bind(this), this.variableFrameStreamsLength());
  _o.CUSTOM_STREAMS = this.bb!.createObjList<CustomStream, CustomStreamT>(this.CUSTOM_STREAMS.bind(this), this.customStreamsLength());
}
}

export class StreamSetT implements flatbuffers.IGeneratedObject {
constructor(
  public FIXED_FRAME_STREAMS: (FixedFrameStreamT)[] = [],
  public VARIABLE_FRAME_STREAMS: (VariableFrameStreamT)[] = [],
  public CUSTOM_STREAMS: (CustomStreamT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const FIXED_FRAME_STREAMS = StreamSet.createFixedFrameStreamsVector(builder, builder.createObjectOffsetList(this.FIXED_FRAME_STREAMS));
  const VARIABLE_FRAME_STREAMS = StreamSet.createVariableFrameStreamsVector(builder, builder.createObjectOffsetList(this.VARIABLE_FRAME_STREAMS));
  const CUSTOM_STREAMS = StreamSet.createCustomStreamsVector(builder, builder.createObjectOffsetList(this.CUSTOM_STREAMS));

  return StreamSet.createStreamSet(builder,
    FIXED_FRAME_STREAMS,
    VARIABLE_FRAME_STREAMS,
    CUSTOM_STREAMS
  );
}
}
