// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { CanonicalizationRule, CanonicalizationRuleT } from './CanonicalizationRule.js';
import { ModuleBundleEntry, ModuleBundleEntryT } from './ModuleBundleEntry.js';


/**
 * Metadata stored in the REC trailer for one module delivery artifact.
 */
export class MBL implements flatbuffers.IUnpackableObject<MBLT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MBL {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMBL(bb:flatbuffers.ByteBuffer, obj?:MBL):MBL {
  return (obj || new MBL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMBL(bb:flatbuffers.ByteBuffer, obj?:MBL):MBL {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MBL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$MBL');
}

/**
 * Bundle schema version.
 */
bundle_version():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 1;
}

/**
 * Human-readable package format label.
 */
module_format():string|null
module_format(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
module_format(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Canonicalization rule used to compute module hashes.
 */
canonicalization(obj?:CanonicalizationRule):CanonicalizationRule|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new CanonicalizationRule()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * SHA-256 of the wasm module after stripping SDS-owned custom sections.
 */
canonical_module_hash(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

canonicalModuleHashLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

canonicalModuleHashArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * SHA-256 of the canonical plugin manifest bytes.
 */
manifest_hash(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

manifestHashLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

manifestHashArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Legacy ABI export retained for backward compatibility.
 */
manifest_export_symbol():string|null
manifest_export_symbol(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
manifest_export_symbol(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Legacy ABI export retained for backward compatibility.
 */
manifest_size_symbol():string|null
manifest_size_symbol(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
manifest_size_symbol(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Payloads embedded in the bundle.
 */
entries(index: number, obj?:ModuleBundleEntry):ModuleBundleEntry|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? (obj || new ModuleBundleEntry()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

entriesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startMBL(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addBundleVersion(builder:flatbuffers.Builder, bundle_version:number) {
  builder.addFieldInt16(0, bundle_version, 1);
}

static addModuleFormat(builder:flatbuffers.Builder, module_formatOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, module_formatOffset, 0);
}

static addCanonicalization(builder:flatbuffers.Builder, canonicalizationOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, canonicalizationOffset, 0);
}

static addCanonicalModuleHash(builder:flatbuffers.Builder, canonical_module_hashOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, canonical_module_hashOffset, 0);
}

static createCanonicalModuleHashVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCanonicalModuleHashVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addManifestHash(builder:flatbuffers.Builder, manifest_hashOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, manifest_hashOffset, 0);
}

static createManifestHashVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startManifestHashVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addManifestExportSymbol(builder:flatbuffers.Builder, manifest_export_symbolOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, manifest_export_symbolOffset, 0);
}

static addManifestSizeSymbol(builder:flatbuffers.Builder, manifest_size_symbolOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, manifest_size_symbolOffset, 0);
}

static addEntries(builder:flatbuffers.Builder, entriesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, entriesOffset, 0);
}

static createEntriesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEntriesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endMBL(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishMBLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MBL');
}

static finishSizePrefixedMBLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MBL', true);
}


unpack(): MBLT {
  return new MBLT(
    this.bundle_version(),
    this.module_format(),
    (this.canonicalization() !== null ? this.canonicalization()!.unpack() : null),
    this.bb!.createScalarList<number>(this.canonical_module_hash.bind(this), this.canonicalModuleHashLength()),
    this.bb!.createScalarList<number>(this.manifest_hash.bind(this), this.manifestHashLength()),
    this.manifest_export_symbol(),
    this.manifest_size_symbol(),
    this.bb!.createObjList<ModuleBundleEntry, ModuleBundleEntryT>(this.entries.bind(this), this.entriesLength())
  );
}


unpackTo(_o: MBLT): void {
  _o.bundle_version = this.bundle_version();
  _o.module_format = this.module_format();
  _o.canonicalization = (this.canonicalization() !== null ? this.canonicalization()!.unpack() : null);
  _o.canonical_module_hash = this.bb!.createScalarList<number>(this.canonical_module_hash.bind(this), this.canonicalModuleHashLength());
  _o.manifest_hash = this.bb!.createScalarList<number>(this.manifest_hash.bind(this), this.manifestHashLength());
  _o.manifest_export_symbol = this.manifest_export_symbol();
  _o.manifest_size_symbol = this.manifest_size_symbol();
  _o.entries = this.bb!.createObjList<ModuleBundleEntry, ModuleBundleEntryT>(this.entries.bind(this), this.entriesLength());
}
}

export class MBLT implements flatbuffers.IGeneratedObject {
constructor(
  public bundle_version: number = 1,
  public module_format: string|Uint8Array|null = null,
  public canonicalization: CanonicalizationRuleT|null = null,
  public canonical_module_hash: (number)[] = [],
  public manifest_hash: (number)[] = [],
  public manifest_export_symbol: string|Uint8Array|null = null,
  public manifest_size_symbol: string|Uint8Array|null = null,
  public entries: (ModuleBundleEntryT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const module_format = (this.module_format !== null ? builder.createString(this.module_format!) : 0);
  const canonicalization = (this.canonicalization !== null ? this.canonicalization!.pack(builder) : 0);
  const canonical_module_hash = MBL.createCanonicalModuleHashVector(builder, this.canonical_module_hash);
  const manifest_hash = MBL.createManifestHashVector(builder, this.manifest_hash);
  const manifest_export_symbol = (this.manifest_export_symbol !== null ? builder.createString(this.manifest_export_symbol!) : 0);
  const manifest_size_symbol = (this.manifest_size_symbol !== null ? builder.createString(this.manifest_size_symbol!) : 0);
  const entries = MBL.createEntriesVector(builder, builder.createObjectOffsetList(this.entries));

  MBL.startMBL(builder);
  MBL.addBundleVersion(builder, this.bundle_version);
  MBL.addModuleFormat(builder, module_format);
  MBL.addCanonicalization(builder, canonicalization);
  MBL.addCanonicalModuleHash(builder, canonical_module_hash);
  MBL.addManifestHash(builder, manifest_hash);
  MBL.addManifestExportSymbol(builder, manifest_export_symbol);
  MBL.addManifestSizeSymbol(builder, manifest_size_symbol);
  MBL.addEntries(builder, entries);

  return MBL.endMBL(builder);
}
}
