// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { numResultStatus } from './numResultStatus.js';


/**
 * Result of one Basilisk-compatible Gauss-Markov random sequence.
 */
export class NUMGaussMarkovResult implements flatbuffers.IUnpackableObject<NUMGaussMarkovResultT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):NUMGaussMarkovResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsNUMGaussMarkovResult(bb:flatbuffers.ByteBuffer, obj?:NUMGaussMarkovResult):NUMGaussMarkovResult {
  return (obj || new NUMGaussMarkovResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsNUMGaussMarkovResult(bb:flatbuffers.ByteBuffer, obj?:NUMGaussMarkovResult):NUMGaussMarkovResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new NUMGaussMarkovResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

STATUS():numResultStatus {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : numResultStatus.OK;
}

ERROR_MESSAGE():string|null
ERROR_MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ERROR_MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Post-warmup per-state sample means.
 */
MEAN(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

meanLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

meanArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Post-warmup per-state sample standard deviations with n - 1 denominator.
 */
STANDARD_DEVIATION(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

standardDeviationLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

standardDeviationArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Post-warmup per-state minima.
 */
MINIMUM(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

minimumLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

minimumArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Post-warmup per-state maxima.
 */
MAXIMUM(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

maximumLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

maximumArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * State after the final generated sample.
 */
FINAL_STATE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

finalStateLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

finalStateArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Optional flattened sample-major state history.
 */
SAMPLES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

samplesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

samplesArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Caller trace/correlation identifier copied from the request when present.
 */
TRACE_ID():string|null
TRACE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TRACE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startNUMGaussMarkovResult(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addStatus(builder:flatbuffers.Builder, STATUS:numResultStatus) {
  builder.addFieldInt8(0, STATUS, numResultStatus.OK);
}

static addErrorMessage(builder:flatbuffers.Builder, ERROR_MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ERROR_MESSAGEOffset, 0);
}

static addMean(builder:flatbuffers.Builder, MEANOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, MEANOffset, 0);
}

static createMeanVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createMeanVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createMeanVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startMeanVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addStandardDeviation(builder:flatbuffers.Builder, STANDARD_DEVIATIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, STANDARD_DEVIATIONOffset, 0);
}

static createStandardDeviationVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createStandardDeviationVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createStandardDeviationVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startStandardDeviationVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addMinimum(builder:flatbuffers.Builder, MINIMUMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, MINIMUMOffset, 0);
}

static createMinimumVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createMinimumVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createMinimumVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startMinimumVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addMaximum(builder:flatbuffers.Builder, MAXIMUMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, MAXIMUMOffset, 0);
}

static createMaximumVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createMaximumVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createMaximumVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startMaximumVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addFinalState(builder:flatbuffers.Builder, FINAL_STATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, FINAL_STATEOffset, 0);
}

static createFinalStateVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createFinalStateVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createFinalStateVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startFinalStateVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addSamples(builder:flatbuffers.Builder, SAMPLESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, SAMPLESOffset, 0);
}

static createSamplesVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createSamplesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createSamplesVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startSamplesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, TRACE_IDOffset, 0);
}

static endNUMGaussMarkovResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createNUMGaussMarkovResult(builder:flatbuffers.Builder, STATUS:numResultStatus, ERROR_MESSAGEOffset:flatbuffers.Offset, MEANOffset:flatbuffers.Offset, STANDARD_DEVIATIONOffset:flatbuffers.Offset, MINIMUMOffset:flatbuffers.Offset, MAXIMUMOffset:flatbuffers.Offset, FINAL_STATEOffset:flatbuffers.Offset, SAMPLESOffset:flatbuffers.Offset, TRACE_IDOffset:flatbuffers.Offset):flatbuffers.Offset {
  NUMGaussMarkovResult.startNUMGaussMarkovResult(builder);
  NUMGaussMarkovResult.addStatus(builder, STATUS);
  NUMGaussMarkovResult.addErrorMessage(builder, ERROR_MESSAGEOffset);
  NUMGaussMarkovResult.addMean(builder, MEANOffset);
  NUMGaussMarkovResult.addStandardDeviation(builder, STANDARD_DEVIATIONOffset);
  NUMGaussMarkovResult.addMinimum(builder, MINIMUMOffset);
  NUMGaussMarkovResult.addMaximum(builder, MAXIMUMOffset);
  NUMGaussMarkovResult.addFinalState(builder, FINAL_STATEOffset);
  NUMGaussMarkovResult.addSamples(builder, SAMPLESOffset);
  NUMGaussMarkovResult.addTraceId(builder, TRACE_IDOffset);
  return NUMGaussMarkovResult.endNUMGaussMarkovResult(builder);
}

unpack(): NUMGaussMarkovResultT {
  return new NUMGaussMarkovResultT(
    this.STATUS(),
    this.ERROR_MESSAGE(),
    this.bb!.createScalarList<number>(this.MEAN.bind(this), this.meanLength()),
    this.bb!.createScalarList<number>(this.STANDARD_DEVIATION.bind(this), this.standardDeviationLength()),
    this.bb!.createScalarList<number>(this.MINIMUM.bind(this), this.minimumLength()),
    this.bb!.createScalarList<number>(this.MAXIMUM.bind(this), this.maximumLength()),
    this.bb!.createScalarList<number>(this.FINAL_STATE.bind(this), this.finalStateLength()),
    this.bb!.createScalarList<number>(this.SAMPLES.bind(this), this.samplesLength()),
    this.TRACE_ID()
  );
}


unpackTo(_o: NUMGaussMarkovResultT): void {
  _o.STATUS = this.STATUS();
  _o.ERROR_MESSAGE = this.ERROR_MESSAGE();
  _o.MEAN = this.bb!.createScalarList<number>(this.MEAN.bind(this), this.meanLength());
  _o.STANDARD_DEVIATION = this.bb!.createScalarList<number>(this.STANDARD_DEVIATION.bind(this), this.standardDeviationLength());
  _o.MINIMUM = this.bb!.createScalarList<number>(this.MINIMUM.bind(this), this.minimumLength());
  _o.MAXIMUM = this.bb!.createScalarList<number>(this.MAXIMUM.bind(this), this.maximumLength());
  _o.FINAL_STATE = this.bb!.createScalarList<number>(this.FINAL_STATE.bind(this), this.finalStateLength());
  _o.SAMPLES = this.bb!.createScalarList<number>(this.SAMPLES.bind(this), this.samplesLength());
  _o.TRACE_ID = this.TRACE_ID();
}
}

export class NUMGaussMarkovResultT implements flatbuffers.IGeneratedObject {
constructor(
  public STATUS: numResultStatus = numResultStatus.OK,
  public ERROR_MESSAGE: string|Uint8Array|null = null,
  public MEAN: (number)[] = [],
  public STANDARD_DEVIATION: (number)[] = [],
  public MINIMUM: (number)[] = [],
  public MAXIMUM: (number)[] = [],
  public FINAL_STATE: (number)[] = [],
  public SAMPLES: (number)[] = [],
  public TRACE_ID: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ERROR_MESSAGE = (this.ERROR_MESSAGE !== null ? builder.createString(this.ERROR_MESSAGE!) : 0);
  const MEAN = NUMGaussMarkovResult.createMeanVector(builder, this.MEAN);
  const STANDARD_DEVIATION = NUMGaussMarkovResult.createStandardDeviationVector(builder, this.STANDARD_DEVIATION);
  const MINIMUM = NUMGaussMarkovResult.createMinimumVector(builder, this.MINIMUM);
  const MAXIMUM = NUMGaussMarkovResult.createMaximumVector(builder, this.MAXIMUM);
  const FINAL_STATE = NUMGaussMarkovResult.createFinalStateVector(builder, this.FINAL_STATE);
  const SAMPLES = NUMGaussMarkovResult.createSamplesVector(builder, this.SAMPLES);
  const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID!) : 0);

  return NUMGaussMarkovResult.createNUMGaussMarkovResult(builder,
    this.STATUS,
    ERROR_MESSAGE,
    MEAN,
    STANDARD_DEVIATION,
    MINIMUM,
    MAXIMUM,
    FINAL_STATE,
    SAMPLES,
    TRACE_ID
  );
}
}
