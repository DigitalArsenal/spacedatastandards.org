// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { EVLEventLocationRequest, EVLEventLocationRequestT } from './EVLEventLocationRequest.js';
import { EVLEventReport, EVLEventReportT } from './EVLEventReport.js';


/**
 * Event Locator
 *
 * One request and one report for every class of located event: shadow
 * passage, site visibility, sensor-field-of-view intrusion, apsis passage,
 * node crossing, and the generic condition on any $PCE catalog parameter.
 * Every class is the same pair of an event function and a refined root, so a
 * new class is a configuration table and a vocabulary member, never a new
 * record. Entry and exit epochs, duration, the occulting or observed body and
 * the parameter values captured at each event's instants are reported in one
 * shape, and the predicate of the generic class is $PCE's
 * PCEParameterCondition rather than a second spelling of the same idea.
 *
 * Angles are radians, durations seconds, epochs ISO 8601 in a named time
 * system.
 */
export class EVL implements flatbuffers.IUnpackableObject<EVLT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EVL {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEVL(bb:flatbuffers.ByteBuffer, obj?:EVL):EVL {
  return (obj || new EVL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEVL(bb:flatbuffers.ByteBuffer, obj?:EVL):EVL {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EVL()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$EVL');
}

LOCATION_REQUEST(obj?:EVLEventLocationRequest):EVLEventLocationRequest|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new EVLEventLocationRequest()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

EVENT_REPORT(obj?:EVLEventReport):EVLEventReport|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new EVLEventReport()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startEVL(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addLocationRequest(builder:flatbuffers.Builder, LOCATION_REQUESTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, LOCATION_REQUESTOffset, 0);
}

static addEventReport(builder:flatbuffers.Builder, EVENT_REPORTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, EVENT_REPORTOffset, 0);
}

static endEVL(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishEVLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$EVL');
}

static finishSizePrefixedEVLBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$EVL', true);
}


unpack(): EVLT {
  return new EVLT(
    (this.LOCATION_REQUEST() !== null ? this.LOCATION_REQUEST()!.unpack() : null),
    (this.EVENT_REPORT() !== null ? this.EVENT_REPORT()!.unpack() : null)
  );
}


unpackTo(_o: EVLT): void {
  _o.LOCATION_REQUEST = (this.LOCATION_REQUEST() !== null ? this.LOCATION_REQUEST()!.unpack() : null);
  _o.EVENT_REPORT = (this.EVENT_REPORT() !== null ? this.EVENT_REPORT()!.unpack() : null);
}
}

export class EVLT implements flatbuffers.IGeneratedObject {
constructor(
  public LOCATION_REQUEST: EVLEventLocationRequestT|null = null,
  public EVENT_REPORT: EVLEventReportT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const LOCATION_REQUEST = (this.LOCATION_REQUEST !== null ? this.LOCATION_REQUEST!.pack(builder) : 0);
  const EVENT_REPORT = (this.EVENT_REPORT !== null ? this.EVENT_REPORT!.pack(builder) : 0);

  EVL.startEVL(builder);
  EVL.addLocationRequest(builder, LOCATION_REQUEST);
  EVL.addEventReport(builder, EVENT_REPORT);

  return EVL.endEVL(builder);
}
}
