// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Compact single-frame detection result.
 */
export class SDRSingleResult implements flatbuffers.IUnpackableObject<SDRSingleResultT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SDRSingleResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSDRSingleResult(bb:flatbuffers.ByteBuffer, obj?:SDRSingleResult):SDRSingleResult {
  return (obj || new SDRSingleResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSDRSingleResult(bb:flatbuffers.ByteBuffer, obj?:SDRSingleResult):SDRSingleResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SDRSingleResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

DETECTED():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Range to target (meters).
 */
RANGE():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Signal-to-noise ratio (dB).
 */
SNR():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Azimuth angle to target (radians).
 */
AZIMUTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Elevation angle to target (radians).
 */
ELEVATION():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Range rate / Doppler velocity (m/s).
 */
RANGE_RATE():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Detection timestamp (milliseconds since epoch).
 */
TIMESTAMP_MS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Track identifier (0 if not tracking).
 */
TRACK_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startSDRSingleResult(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addDetected(builder:flatbuffers.Builder, DETECTED:boolean) {
  builder.addFieldInt8(0, +DETECTED, +false);
}

static addRange(builder:flatbuffers.Builder, RANGE:number) {
  builder.addFieldFloat64(1, RANGE, 0.0);
}

static addSnr(builder:flatbuffers.Builder, SNR:number) {
  builder.addFieldFloat64(2, SNR, 0.0);
}

static addAzimuth(builder:flatbuffers.Builder, AZIMUTH:number) {
  builder.addFieldFloat64(3, AZIMUTH, 0.0);
}

static addElevation(builder:flatbuffers.Builder, ELEVATION:number) {
  builder.addFieldFloat64(4, ELEVATION, 0.0);
}

static addRangeRate(builder:flatbuffers.Builder, RANGE_RATE:number) {
  builder.addFieldFloat64(5, RANGE_RATE, 0.0);
}

static addTimestampMs(builder:flatbuffers.Builder, TIMESTAMP_MS:bigint) {
  builder.addFieldInt64(6, TIMESTAMP_MS, BigInt('0'));
}

static addTrackId(builder:flatbuffers.Builder, TRACK_ID:number) {
  builder.addFieldInt32(7, TRACK_ID, 0);
}

static endSDRSingleResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSDRSingleResult(builder:flatbuffers.Builder, DETECTED:boolean, RANGE:number, SNR:number, AZIMUTH:number, ELEVATION:number, RANGE_RATE:number, TIMESTAMP_MS:bigint, TRACK_ID:number):flatbuffers.Offset {
  SDRSingleResult.startSDRSingleResult(builder);
  SDRSingleResult.addDetected(builder, DETECTED);
  SDRSingleResult.addRange(builder, RANGE);
  SDRSingleResult.addSnr(builder, SNR);
  SDRSingleResult.addAzimuth(builder, AZIMUTH);
  SDRSingleResult.addElevation(builder, ELEVATION);
  SDRSingleResult.addRangeRate(builder, RANGE_RATE);
  SDRSingleResult.addTimestampMs(builder, TIMESTAMP_MS);
  SDRSingleResult.addTrackId(builder, TRACK_ID);
  return SDRSingleResult.endSDRSingleResult(builder);
}

unpack(): SDRSingleResultT {
  return new SDRSingleResultT(
    this.DETECTED(),
    this.RANGE(),
    this.SNR(),
    this.AZIMUTH(),
    this.ELEVATION(),
    this.RANGE_RATE(),
    this.TIMESTAMP_MS(),
    this.TRACK_ID()
  );
}


unpackTo(_o: SDRSingleResultT): void {
  _o.DETECTED = this.DETECTED();
  _o.RANGE = this.RANGE();
  _o.SNR = this.SNR();
  _o.AZIMUTH = this.AZIMUTH();
  _o.ELEVATION = this.ELEVATION();
  _o.RANGE_RATE = this.RANGE_RATE();
  _o.TIMESTAMP_MS = this.TIMESTAMP_MS();
  _o.TRACK_ID = this.TRACK_ID();
}
}

export class SDRSingleResultT implements flatbuffers.IGeneratedObject {
constructor(
  public DETECTED: boolean = false,
  public RANGE: number = 0.0,
  public SNR: number = 0.0,
  public AZIMUTH: number = 0.0,
  public ELEVATION: number = 0.0,
  public RANGE_RATE: number = 0.0,
  public TIMESTAMP_MS: bigint = BigInt('0'),
  public TRACK_ID: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return SDRSingleResult.createSDRSingleResult(builder,
    this.DETECTED,
    this.RANGE,
    this.SNR,
    this.AZIMUTH,
    this.ELEVATION,
    this.RANGE_RATE,
    this.TIMESTAMP_MS,
    this.TRACK_ID
  );
}
}
