// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { gctLevelDerivation } from './gctLevelDerivation.js';
import { gctSampleEncoding } from './gctSampleEncoding.js';


/**
 * One level of detail of one variant: a Gaussian mixture in the variant's
 * own frame (metres, origin at the variant's reference point, Z up, base of
 * the cloud near Z = 0). Levels are ordered finest first. Every per-Gaussian
 * vector holds exactly 4 * GAUSSIAN_COUNT float32 values — one RGBA texel
 * per Gaussian — in the same Gaussian order.
 */
export class GCTLevel implements flatbuffers.IUnpackableObject<GCTLevelT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GCTLevel {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGCTLevel(bb:flatbuffers.ByteBuffer, obj?:GCTLevel):GCTLevel {
  return (obj || new GCTLevel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGCTLevel(bb:flatbuffers.ByteBuffer, obj?:GCTLevel):GCTLevel {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GCTLevel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Number of Gaussians in this level.
 */
GAUSSIAN_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * The cell this level was merged on, metres: the level's geometric error
 * in the variant frame. Multiply by the instance scale in use.
 */
MERGE_CELL_M():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * How this level was produced.
 */
DERIVATION():gctLevelDerivation {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : gctLevelDerivation.UNSPECIFIED;
}

/**
 * Where the samples live.
 */
ENCODING():gctSampleEncoding {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : gctSampleEncoding.INLINE_FLOAT32;
}

/**
 * Content identifier of the chunk when ENCODING is
 * CONTENT_ADDRESSED_CHUNK.
 */
CHUNK_CID():string|null
CHUNK_CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CHUNK_CID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Encoded chunk length in bytes when ENCODING is CONTENT_ADDRESSED_CHUNK.
 */
CHUNK_BYTE_LENGTH():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Per Gaussian: x, y, z of the centre, metres in the variant frame; w =
 * amplitude in AMPLITUDE_UNITS at the centre.
 */
CENTERS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

centersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

centersArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Per Gaussian: standard deviation along the variant frame's x, y, z
 * axes, metres (axis-aligned; the instance yaw rotates the frame); w = a
 * per-Gaussian seed in [0, 1) for stochastic shading, 0 when unused.
 */
SIGMAS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

sigmasLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

sigmasArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * OPTIONAL. Per Gaussian: x, y, z of the field's boundary normal at the
 * centre (unit vector scaled by a boundary weight in [0, 1]: 0 deep
 * inside, 1 at a crisp condensation interface); w = the local coarse
 * density in [0, 1]. Absent when not computed.
 */
BOUNDARY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

boundaryLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

boundaryArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * OPTIONAL. Per Gaussian: x, y, z of a finer-scale surface normal that
 * adds relief to BOUNDARY; w unused (0). Absent when not computed.
 */
FINE_NORMAL(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

fineNormalLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

fineNormalArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * OPTIONAL. Per Gaussian: optical depth of the field from the centre
 * outward along +x, +y, +z (dimensionless); w unused (0). Absent when
 * not computed.
 */
OPTICAL_DEPTH_POS(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

opticalDepthPosLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

opticalDepthPosArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * OPTIONAL. Per Gaussian: optical depth along -x, -y, -z; w unused (0).
 * Absent when not computed.
 */
OPTICAL_DEPTH_NEG(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

opticalDepthNegLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

opticalDepthNegArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startGCTLevel(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addGaussianCount(builder:flatbuffers.Builder, GAUSSIAN_COUNT:number) {
  builder.addFieldInt32(0, GAUSSIAN_COUNT, 0);
}

static addMergeCellM(builder:flatbuffers.Builder, MERGE_CELL_M:number) {
  builder.addFieldFloat32(1, MERGE_CELL_M, 0.0);
}

static addDerivation(builder:flatbuffers.Builder, DERIVATION:gctLevelDerivation) {
  builder.addFieldInt8(2, DERIVATION, gctLevelDerivation.UNSPECIFIED);
}

static addEncoding(builder:flatbuffers.Builder, ENCODING:gctSampleEncoding) {
  builder.addFieldInt8(3, ENCODING, gctSampleEncoding.INLINE_FLOAT32);
}

static addChunkCid(builder:flatbuffers.Builder, CHUNK_CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, CHUNK_CIDOffset, 0);
}

static addChunkByteLength(builder:flatbuffers.Builder, CHUNK_BYTE_LENGTH:bigint) {
  builder.addFieldInt64(5, CHUNK_BYTE_LENGTH, BigInt('0'));
}

static addCenters(builder:flatbuffers.Builder, CENTERSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, CENTERSOffset, 0);
}

static createCentersVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createCentersVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createCentersVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startCentersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSigmas(builder:flatbuffers.Builder, SIGMASOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, SIGMASOffset, 0);
}

static createSigmasVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createSigmasVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createSigmasVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startSigmasVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBoundary(builder:flatbuffers.Builder, BOUNDARYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, BOUNDARYOffset, 0);
}

static createBoundaryVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createBoundaryVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createBoundaryVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startBoundaryVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addFineNormal(builder:flatbuffers.Builder, FINE_NORMALOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, FINE_NORMALOffset, 0);
}

static createFineNormalVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createFineNormalVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createFineNormalVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startFineNormalVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOpticalDepthPos(builder:flatbuffers.Builder, OPTICAL_DEPTH_POSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, OPTICAL_DEPTH_POSOffset, 0);
}

static createOpticalDepthPosVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createOpticalDepthPosVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createOpticalDepthPosVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startOpticalDepthPosVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOpticalDepthNeg(builder:flatbuffers.Builder, OPTICAL_DEPTH_NEGOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, OPTICAL_DEPTH_NEGOffset, 0);
}

static createOpticalDepthNegVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createOpticalDepthNegVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createOpticalDepthNegVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startOpticalDepthNegVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endGCTLevel(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGCTLevel(builder:flatbuffers.Builder, GAUSSIAN_COUNT:number, MERGE_CELL_M:number, DERIVATION:gctLevelDerivation, ENCODING:gctSampleEncoding, CHUNK_CIDOffset:flatbuffers.Offset, CHUNK_BYTE_LENGTH:bigint, CENTERSOffset:flatbuffers.Offset, SIGMASOffset:flatbuffers.Offset, BOUNDARYOffset:flatbuffers.Offset, FINE_NORMALOffset:flatbuffers.Offset, OPTICAL_DEPTH_POSOffset:flatbuffers.Offset, OPTICAL_DEPTH_NEGOffset:flatbuffers.Offset):flatbuffers.Offset {
  GCTLevel.startGCTLevel(builder);
  GCTLevel.addGaussianCount(builder, GAUSSIAN_COUNT);
  GCTLevel.addMergeCellM(builder, MERGE_CELL_M);
  GCTLevel.addDerivation(builder, DERIVATION);
  GCTLevel.addEncoding(builder, ENCODING);
  GCTLevel.addChunkCid(builder, CHUNK_CIDOffset);
  GCTLevel.addChunkByteLength(builder, CHUNK_BYTE_LENGTH);
  GCTLevel.addCenters(builder, CENTERSOffset);
  GCTLevel.addSigmas(builder, SIGMASOffset);
  GCTLevel.addBoundary(builder, BOUNDARYOffset);
  GCTLevel.addFineNormal(builder, FINE_NORMALOffset);
  GCTLevel.addOpticalDepthPos(builder, OPTICAL_DEPTH_POSOffset);
  GCTLevel.addOpticalDepthNeg(builder, OPTICAL_DEPTH_NEGOffset);
  return GCTLevel.endGCTLevel(builder);
}

unpack(): GCTLevelT {
  return new GCTLevelT(
    this.GAUSSIAN_COUNT(),
    this.MERGE_CELL_M(),
    this.DERIVATION(),
    this.ENCODING(),
    this.CHUNK_CID(),
    this.CHUNK_BYTE_LENGTH(),
    this.bb!.createScalarList<number>(this.CENTERS.bind(this), this.centersLength()),
    this.bb!.createScalarList<number>(this.SIGMAS.bind(this), this.sigmasLength()),
    this.bb!.createScalarList<number>(this.BOUNDARY.bind(this), this.boundaryLength()),
    this.bb!.createScalarList<number>(this.FINE_NORMAL.bind(this), this.fineNormalLength()),
    this.bb!.createScalarList<number>(this.OPTICAL_DEPTH_POS.bind(this), this.opticalDepthPosLength()),
    this.bb!.createScalarList<number>(this.OPTICAL_DEPTH_NEG.bind(this), this.opticalDepthNegLength())
  );
}


unpackTo(_o: GCTLevelT): void {
  _o.GAUSSIAN_COUNT = this.GAUSSIAN_COUNT();
  _o.MERGE_CELL_M = this.MERGE_CELL_M();
  _o.DERIVATION = this.DERIVATION();
  _o.ENCODING = this.ENCODING();
  _o.CHUNK_CID = this.CHUNK_CID();
  _o.CHUNK_BYTE_LENGTH = this.CHUNK_BYTE_LENGTH();
  _o.CENTERS = this.bb!.createScalarList<number>(this.CENTERS.bind(this), this.centersLength());
  _o.SIGMAS = this.bb!.createScalarList<number>(this.SIGMAS.bind(this), this.sigmasLength());
  _o.BOUNDARY = this.bb!.createScalarList<number>(this.BOUNDARY.bind(this), this.boundaryLength());
  _o.FINE_NORMAL = this.bb!.createScalarList<number>(this.FINE_NORMAL.bind(this), this.fineNormalLength());
  _o.OPTICAL_DEPTH_POS = this.bb!.createScalarList<number>(this.OPTICAL_DEPTH_POS.bind(this), this.opticalDepthPosLength());
  _o.OPTICAL_DEPTH_NEG = this.bb!.createScalarList<number>(this.OPTICAL_DEPTH_NEG.bind(this), this.opticalDepthNegLength());
}
}

export class GCTLevelT implements flatbuffers.IGeneratedObject {
constructor(
  public GAUSSIAN_COUNT: number = 0,
  public MERGE_CELL_M: number = 0.0,
  public DERIVATION: gctLevelDerivation = gctLevelDerivation.UNSPECIFIED,
  public ENCODING: gctSampleEncoding = gctSampleEncoding.INLINE_FLOAT32,
  public CHUNK_CID: string|Uint8Array|null = null,
  public CHUNK_BYTE_LENGTH: bigint = BigInt('0'),
  public CENTERS: (number)[] = [],
  public SIGMAS: (number)[] = [],
  public BOUNDARY: (number)[] = [],
  public FINE_NORMAL: (number)[] = [],
  public OPTICAL_DEPTH_POS: (number)[] = [],
  public OPTICAL_DEPTH_NEG: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const CHUNK_CID = (this.CHUNK_CID !== null ? builder.createString(this.CHUNK_CID!) : 0);
  const CENTERS = GCTLevel.createCentersVector(builder, this.CENTERS);
  const SIGMAS = GCTLevel.createSigmasVector(builder, this.SIGMAS);
  const BOUNDARY = GCTLevel.createBoundaryVector(builder, this.BOUNDARY);
  const FINE_NORMAL = GCTLevel.createFineNormalVector(builder, this.FINE_NORMAL);
  const OPTICAL_DEPTH_POS = GCTLevel.createOpticalDepthPosVector(builder, this.OPTICAL_DEPTH_POS);
  const OPTICAL_DEPTH_NEG = GCTLevel.createOpticalDepthNegVector(builder, this.OPTICAL_DEPTH_NEG);

  return GCTLevel.createGCTLevel(builder,
    this.GAUSSIAN_COUNT,
    this.MERGE_CELL_M,
    this.DERIVATION,
    this.ENCODING,
    CHUNK_CID,
    this.CHUNK_BYTE_LENGTH,
    CENTERS,
    SIGMAS,
    BOUNDARY,
    FINE_NORMAL,
    OPTICAL_DEPTH_POS,
    OPTICAL_DEPTH_NEG
  );
}
}
