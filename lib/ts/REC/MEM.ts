// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { MEMAttestation, MEMAttestationT } from './MEMAttestation.js';
import { MEMEditingDecision, MEMEditingDecisionT } from './MEMEditingDecision.js';
import { MEMErrorModel, MEMErrorModelT } from './MEMErrorModel.js';


/**
 * Measurement Error Model. JSON projections preserve these IDL-capitalized
 * field names exactly. The FlatBuffer signature covers the size-prefixed
 * buffer with both signature vectors zeroed; the canonical-JSON signature
 * covers RFC 8785 canonical JSON with both signature fields omitted.
 */
export class MEM implements flatbuffers.IUnpackableObject<MEMT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MEM {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMEM(bb:flatbuffers.ByteBuffer, obj?:MEM):MEM {
  return (obj || new MEM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMEM(bb:flatbuffers.ByteBuffer, obj?:MEM):MEM {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MEM()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$MEM');
}

ERROR_MODELS(index: number, obj?:MEMErrorModel):MEMErrorModel|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new MEMErrorModel()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

errorModelsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

EDITING_LEDGER(index: number, obj?:MEMEditingDecision):MEMEditingDecision|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new MEMEditingDecision()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

editingLedgerLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

RANDOM_SEED():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

TRACE_ID():string|null
TRACE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TRACE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

ATTESTATION(obj?:MEMAttestation):MEMAttestation|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new MEMAttestation()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startMEM(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addErrorModels(builder:flatbuffers.Builder, ERROR_MODELSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, ERROR_MODELSOffset, 0);
}

static createErrorModelsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startErrorModelsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEditingLedger(builder:flatbuffers.Builder, EDITING_LEDGEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, EDITING_LEDGEROffset, 0);
}

static createEditingLedgerVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEditingLedgerVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addRandomSeed(builder:flatbuffers.Builder, RANDOM_SEED:bigint) {
  builder.addFieldInt64(2, RANDOM_SEED, BigInt('0'));
}

static addTraceId(builder:flatbuffers.Builder, TRACE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, TRACE_IDOffset, 0);
}

static addAttestation(builder:flatbuffers.Builder, ATTESTATIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, ATTESTATIONOffset, 0);
}

static endMEM(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishMEMBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MEM');
}

static finishSizePrefixedMEMBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$MEM', true);
}


unpack(): MEMT {
  return new MEMT(
    this.bb!.createObjList<MEMErrorModel, MEMErrorModelT>(this.ERROR_MODELS.bind(this), this.errorModelsLength()),
    this.bb!.createObjList<MEMEditingDecision, MEMEditingDecisionT>(this.EDITING_LEDGER.bind(this), this.editingLedgerLength()),
    this.RANDOM_SEED(),
    this.TRACE_ID(),
    (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null)
  );
}


unpackTo(_o: MEMT): void {
  _o.ERROR_MODELS = this.bb!.createObjList<MEMErrorModel, MEMErrorModelT>(this.ERROR_MODELS.bind(this), this.errorModelsLength());
  _o.EDITING_LEDGER = this.bb!.createObjList<MEMEditingDecision, MEMEditingDecisionT>(this.EDITING_LEDGER.bind(this), this.editingLedgerLength());
  _o.RANDOM_SEED = this.RANDOM_SEED();
  _o.TRACE_ID = this.TRACE_ID();
  _o.ATTESTATION = (this.ATTESTATION() !== null ? this.ATTESTATION()!.unpack() : null);
}
}

export class MEMT implements flatbuffers.IGeneratedObject {
constructor(
  public ERROR_MODELS: (MEMErrorModelT)[] = [],
  public EDITING_LEDGER: (MEMEditingDecisionT)[] = [],
  public RANDOM_SEED: bigint = BigInt('0'),
  public TRACE_ID: string|Uint8Array|null = null,
  public ATTESTATION: MEMAttestationT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ERROR_MODELS = MEM.createErrorModelsVector(builder, builder.createObjectOffsetList(this.ERROR_MODELS));
  const EDITING_LEDGER = MEM.createEditingLedgerVector(builder, builder.createObjectOffsetList(this.EDITING_LEDGER));
  const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID!) : 0);
  const ATTESTATION = (this.ATTESTATION !== null ? this.ATTESTATION!.pack(builder) : 0);

  MEM.startMEM(builder);
  MEM.addErrorModels(builder, ERROR_MODELS);
  MEM.addEditingLedger(builder, EDITING_LEDGER);
  MEM.addRandomSeed(builder, this.RANDOM_SEED);
  MEM.addTraceId(builder, TRACE_ID);
  MEM.addAttestation(builder, ATTESTATION);

  return MEM.endMEM(builder);
}
}
