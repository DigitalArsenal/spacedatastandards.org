// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { packetType } from './packetType.js';


/**
 * Space Packet Protocol (CCSDS 133.0-B-1)
 */
export class SPP implements flatbuffers.IUnpackableObject<SPPT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SPP {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSPP(bb:flatbuffers.ByteBuffer, obj?:SPP):SPP {
  return (obj || new SPP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSPP(bb:flatbuffers.ByteBuffer, obj?:SPP):SPP {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SPP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$SPP');
}

/**
 * Packet version number
 */
VERSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Packet type (TM or TC)
 */
PACKET_TYPE():packetType {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : packetType.TM;
}

/**
 * Secondary header flag
 */
SEC_HDR_FLAG():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Application Process Identifier
 */
APID():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Sequence flags (00=continuation, 01=first, 10=last, 11=standalone)
 */
SEQUENCE_FLAGS():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Sequence count
 */
SEQUENCE_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Data length minus 1
 */
DATA_LENGTH():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Packet data zone
 */
DATA(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

dataLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

dataArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startSPP(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addVersion(builder:flatbuffers.Builder, VERSION:number) {
  builder.addFieldInt8(0, VERSION, 0);
}

static addPacketType(builder:flatbuffers.Builder, PACKET_TYPE:packetType) {
  builder.addFieldInt8(1, PACKET_TYPE, packetType.TM);
}

static addSecHdrFlag(builder:flatbuffers.Builder, SEC_HDR_FLAG:boolean) {
  builder.addFieldInt8(2, +SEC_HDR_FLAG, +false);
}

static addApid(builder:flatbuffers.Builder, APID:number) {
  builder.addFieldInt16(3, APID, 0);
}

static addSequenceFlags(builder:flatbuffers.Builder, SEQUENCE_FLAGS:number) {
  builder.addFieldInt8(4, SEQUENCE_FLAGS, 0);
}

static addSequenceCount(builder:flatbuffers.Builder, SEQUENCE_COUNT:number) {
  builder.addFieldInt16(5, SEQUENCE_COUNT, 0);
}

static addDataLength(builder:flatbuffers.Builder, DATA_LENGTH:number) {
  builder.addFieldInt16(6, DATA_LENGTH, 0);
}

static addData(builder:flatbuffers.Builder, DATAOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, DATAOffset, 0);
}

static createDataVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startDataVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endSPP(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishSPPBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SPP');
}

static finishSizePrefixedSPPBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SPP', true);
}

static createSPP(builder:flatbuffers.Builder, VERSION:number, PACKET_TYPE:packetType, SEC_HDR_FLAG:boolean, APID:number, SEQUENCE_FLAGS:number, SEQUENCE_COUNT:number, DATA_LENGTH:number, DATAOffset:flatbuffers.Offset):flatbuffers.Offset {
  SPP.startSPP(builder);
  SPP.addVersion(builder, VERSION);
  SPP.addPacketType(builder, PACKET_TYPE);
  SPP.addSecHdrFlag(builder, SEC_HDR_FLAG);
  SPP.addApid(builder, APID);
  SPP.addSequenceFlags(builder, SEQUENCE_FLAGS);
  SPP.addSequenceCount(builder, SEQUENCE_COUNT);
  SPP.addDataLength(builder, DATA_LENGTH);
  SPP.addData(builder, DATAOffset);
  return SPP.endSPP(builder);
}

unpack(): SPPT {
  return new SPPT(
    this.VERSION(),
    this.PACKET_TYPE(),
    this.SEC_HDR_FLAG(),
    this.APID(),
    this.SEQUENCE_FLAGS(),
    this.SEQUENCE_COUNT(),
    this.DATA_LENGTH(),
    this.bb!.createScalarList<number>(this.DATA.bind(this), this.dataLength())
  );
}


unpackTo(_o: SPPT): void {
  _o.VERSION = this.VERSION();
  _o.PACKET_TYPE = this.PACKET_TYPE();
  _o.SEC_HDR_FLAG = this.SEC_HDR_FLAG();
  _o.APID = this.APID();
  _o.SEQUENCE_FLAGS = this.SEQUENCE_FLAGS();
  _o.SEQUENCE_COUNT = this.SEQUENCE_COUNT();
  _o.DATA_LENGTH = this.DATA_LENGTH();
  _o.DATA = this.bb!.createScalarList<number>(this.DATA.bind(this), this.dataLength());
}
}

export class SPPT implements flatbuffers.IGeneratedObject {
constructor(
  public VERSION: number = 0,
  public PACKET_TYPE: packetType = packetType.TM,
  public SEC_HDR_FLAG: boolean = false,
  public APID: number = 0,
  public SEQUENCE_FLAGS: number = 0,
  public SEQUENCE_COUNT: number = 0,
  public DATA_LENGTH: number = 0,
  public DATA: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const DATA = SPP.createDataVector(builder, this.DATA);

  return SPP.createSPP(builder,
    this.VERSION,
    this.PACKET_TYPE,
    this.SEC_HDR_FLAG,
    this.APID,
    this.SEQUENCE_FLAGS,
    this.SEQUENCE_COUNT,
    this.DATA_LENGTH,
    DATA
  );
}
}
