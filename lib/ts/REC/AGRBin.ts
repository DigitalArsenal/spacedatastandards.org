// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One histogram bin over [LOW, HIGH).
 */
export class AGRBin implements flatbuffers.IUnpackableObject<AGRBinT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AGRBin {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAGRBin(bb:flatbuffers.ByteBuffer, obj?:AGRBin):AGRBin {
  return (obj || new AGRBin()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAGRBin(bb:flatbuffers.ByteBuffer, obj?:AGRBin):AGRBin {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new AGRBin()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

LOW():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

HIGH():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

COUNT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

SUM():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MIN():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MAX():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MEAN():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startAGRBin(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addLow(builder:flatbuffers.Builder, LOW:number) {
  builder.addFieldFloat64(0, LOW, 0.0);
}

static addHigh(builder:flatbuffers.Builder, HIGH:number) {
  builder.addFieldFloat64(1, HIGH, 0.0);
}

static addCount(builder:flatbuffers.Builder, COUNT:bigint) {
  builder.addFieldInt64(2, COUNT, BigInt('0'));
}

static addSum(builder:flatbuffers.Builder, SUM:number) {
  builder.addFieldFloat64(3, SUM, 0.0);
}

static addMin(builder:flatbuffers.Builder, MIN:number) {
  builder.addFieldFloat64(4, MIN, 0.0);
}

static addMax(builder:flatbuffers.Builder, MAX:number) {
  builder.addFieldFloat64(5, MAX, 0.0);
}

static addMean(builder:flatbuffers.Builder, MEAN:number) {
  builder.addFieldFloat64(6, MEAN, 0.0);
}

static endAGRBin(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createAGRBin(builder:flatbuffers.Builder, LOW:number, HIGH:number, COUNT:bigint, SUM:number, MIN:number, MAX:number, MEAN:number):flatbuffers.Offset {
  AGRBin.startAGRBin(builder);
  AGRBin.addLow(builder, LOW);
  AGRBin.addHigh(builder, HIGH);
  AGRBin.addCount(builder, COUNT);
  AGRBin.addSum(builder, SUM);
  AGRBin.addMin(builder, MIN);
  AGRBin.addMax(builder, MAX);
  AGRBin.addMean(builder, MEAN);
  return AGRBin.endAGRBin(builder);
}

unpack(): AGRBinT {
  return new AGRBinT(
    this.LOW(),
    this.HIGH(),
    this.COUNT(),
    this.SUM(),
    this.MIN(),
    this.MAX(),
    this.MEAN()
  );
}


unpackTo(_o: AGRBinT): void {
  _o.LOW = this.LOW();
  _o.HIGH = this.HIGH();
  _o.COUNT = this.COUNT();
  _o.SUM = this.SUM();
  _o.MIN = this.MIN();
  _o.MAX = this.MAX();
  _o.MEAN = this.MEAN();
}
}

export class AGRBinT implements flatbuffers.IGeneratedObject {
constructor(
  public LOW: number = 0.0,
  public HIGH: number = 0.0,
  public COUNT: bigint = BigInt('0'),
  public SUM: number = 0.0,
  public MIN: number = 0.0,
  public MAX: number = 0.0,
  public MEAN: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return AGRBin.createAGRBin(builder,
    this.LOW,
    this.HIGH,
    this.COUNT,
    this.SUM,
    this.MIN,
    this.MAX,
    this.MEAN
  );
}
}
