// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SCCAssetPicker, SCCAssetPickerT } from './SCCAssetPicker.js';
import { SCCReady, SCCReadyT } from './SCCReady.js';
import { SCCRequestState, SCCRequestStateT } from './SCCRequestState.js';
import { SCCStateResponse, SCCStateResponseT } from './SCCStateResponse.js';
import { SCN, SCNT } from './SCN.js';
import { controlMessageKind } from './controlMessageKind.js';


/**
 * Scenario Controls - message-bus envelope for scenario setup, state
 * requests/responses, startup readiness, and asset-picker delegation.
 */
export class SCC implements flatbuffers.IUnpackableObject<SCCT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCC {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCC(bb:flatbuffers.ByteBuffer, obj?:SCC):SCC {
  return (obj || new SCC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCC(bb:flatbuffers.ByteBuffer, obj?:SCC):SCC {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCC()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$SCC');
}

/**
 * Payload discriminator for the message carried by this envelope.
 */
MESSAGE_KIND():controlMessageKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : controlMessageKind.UNKNOWN;
}

/**
 * Optional trace id for correlating messages across systems.
 */
TRACE_ID():string|null
TRACE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TRACE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Scenario list carried by setup or selection messages.
 */
SCENARIOS(index: number, obj?:SCN):SCN|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new SCN()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

scenariosLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Single scenario payload carried by setup or state messages.
 */
SCENARIO(obj?:SCN):SCN|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new SCN()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Request-current-state payload.
 */
REQUEST_STATE(obj?:SCCRequestState):SCCRequestState|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new SCCRequestState()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Current-state response payload.
 */
STATE_RESPONSE(obj?:SCCStateResponse):SCCStateResponse|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new SCCStateResponse()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Asset-picker request or acknowledgement payload.
 */
ASSET_PICKER(obj?:SCCAssetPicker):SCCAssetPicker|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new SCCAssetPicker()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Startup readiness payload.
 */
READY(obj?:SCCReady):SCCReady|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? (obj || new SCCReady()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startSCC(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addMessageKind(builder:flatbuffers.Builder, MESSAGE_KIND:controlMessageKind) {
  builder.addFieldInt8(0, MESSAGE_KIND, controlMessageKind.UNKNOWN);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, TRACE_IDOffset, 0);
}

static addScenarios(builder:flatbuffers.Builder, SCENARIOSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, SCENARIOSOffset, 0);
}

static createScenariosVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startScenariosVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addScenario(builder:flatbuffers.Builder, SCENARIOOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, SCENARIOOffset, 0);
}

static addRequestState(builder:flatbuffers.Builder, REQUEST_STATEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, REQUEST_STATEOffset, 0);
}

static addStateResponse(builder:flatbuffers.Builder, STATE_RESPONSEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, STATE_RESPONSEOffset, 0);
}

static addAssetPicker(builder:flatbuffers.Builder, ASSET_PICKEROffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, ASSET_PICKEROffset, 0);
}

static addReady(builder:flatbuffers.Builder, READYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, READYOffset, 0);
}

static endSCC(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishSCCBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SCC');
}

static finishSizePrefixedSCCBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$SCC', true);
}


unpack(): SCCT {
  return new SCCT(
    this.MESSAGE_KIND(),
    this.TRACE_ID(),
    this.bb!.createObjList<SCN, SCNT>(this.SCENARIOS.bind(this), this.scenariosLength()),
    (this.SCENARIO() !== null ? this.SCENARIO()!.unpack() : null),
    (this.REQUEST_STATE() !== null ? this.REQUEST_STATE()!.unpack() : null),
    (this.STATE_RESPONSE() !== null ? this.STATE_RESPONSE()!.unpack() : null),
    (this.ASSET_PICKER() !== null ? this.ASSET_PICKER()!.unpack() : null),
    (this.READY() !== null ? this.READY()!.unpack() : null)
  );
}


unpackTo(_o: SCCT): void {
  _o.MESSAGE_KIND = this.MESSAGE_KIND();
  _o.TRACE_ID = this.TRACE_ID();
  _o.SCENARIOS = this.bb!.createObjList<SCN, SCNT>(this.SCENARIOS.bind(this), this.scenariosLength());
  _o.SCENARIO = (this.SCENARIO() !== null ? this.SCENARIO()!.unpack() : null);
  _o.REQUEST_STATE = (this.REQUEST_STATE() !== null ? this.REQUEST_STATE()!.unpack() : null);
  _o.STATE_RESPONSE = (this.STATE_RESPONSE() !== null ? this.STATE_RESPONSE()!.unpack() : null);
  _o.ASSET_PICKER = (this.ASSET_PICKER() !== null ? this.ASSET_PICKER()!.unpack() : null);
  _o.READY = (this.READY() !== null ? this.READY()!.unpack() : null);
}
}

export class SCCT implements flatbuffers.IGeneratedObject {
constructor(
  public MESSAGE_KIND: controlMessageKind = controlMessageKind.UNKNOWN,
  public TRACE_ID: string|Uint8Array|null = null,
  public SCENARIOS: (SCNT)[] = [],
  public SCENARIO: SCNT|null = null,
  public REQUEST_STATE: SCCRequestStateT|null = null,
  public STATE_RESPONSE: SCCStateResponseT|null = null,
  public ASSET_PICKER: SCCAssetPickerT|null = null,
  public READY: SCCReadyT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const TRACE_ID = (this.TRACE_ID !== null ? builder.createString(this.TRACE_ID!) : 0);
  const SCENARIOS = SCC.createScenariosVector(builder, builder.createObjectOffsetList(this.SCENARIOS));
  const SCENARIO = (this.SCENARIO !== null ? this.SCENARIO!.pack(builder) : 0);
  const REQUEST_STATE = (this.REQUEST_STATE !== null ? this.REQUEST_STATE!.pack(builder) : 0);
  const STATE_RESPONSE = (this.STATE_RESPONSE !== null ? this.STATE_RESPONSE!.pack(builder) : 0);
  const ASSET_PICKER = (this.ASSET_PICKER !== null ? this.ASSET_PICKER!.pack(builder) : 0);
  const READY = (this.READY !== null ? this.READY!.pack(builder) : 0);

  SCC.startSCC(builder);
  SCC.addMessageKind(builder, this.MESSAGE_KIND);
  SCC.addTraceId(builder, TRACE_ID);
  SCC.addScenarios(builder, SCENARIOS);
  SCC.addScenario(builder, SCENARIO);
  SCC.addRequestState(builder, REQUEST_STATE);
  SCC.addStateResponse(builder, STATE_RESPONSE);
  SCC.addAssetPicker(builder, ASSET_PICKER);
  SCC.addReady(builder, READY);

  return SCC.endSCC(builder);
}
}
