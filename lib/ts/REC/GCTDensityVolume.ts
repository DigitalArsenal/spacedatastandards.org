// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * A baked density volume of a variant on the same cube as its distance
 * field, for ray marching: N^3 samples of amplitude in AMPLITUDE_UNITS,
 * float16, x fastest. Optional.
 */
export class GCTDensityVolume implements flatbuffers.IUnpackableObject<GCTDensityVolumeT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GCTDensityVolume {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGCTDensityVolume(bb:flatbuffers.ByteBuffer, obj?:GCTDensityVolume):GCTDensityVolume {
  return (obj || new GCTDensityVolume()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGCTDensityVolume(bb:flatbuffers.ByteBuffer, obj?:GCTDensityVolume):GCTDensityVolume {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GCTDensityVolume()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Voxels per axis.
 */
N():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Origin of voxel (0, 0, 0) in the variant frame, metres (3 values).
 */
ORIGIN_M(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

originMLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

originMArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Extent of the cube along each axis, metres (3 values).
 */
SPAN_M(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

spanMLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

spanMArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * N^3 IEEE 754 half-precision samples as their 16-bit patterns, x
 * fastest, then y, then z. Absent when the volume travels as the chunk
 * named by CHUNK_CID.
 */
DATA(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint16(this.bb!.__vector(this.bb_pos + offset) + index * 2) : 0;
}

dataLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

dataArray():Uint16Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint16Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Content identifier of the volume when DATA is absent.
 */
CHUNK_CID():string|null
CHUNK_CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CHUNK_CID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startGCTDensityVolume(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addN(builder:flatbuffers.Builder, N:number) {
  builder.addFieldInt16(0, N, 0);
}

static addOriginM(builder:flatbuffers.Builder, ORIGIN_MOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ORIGIN_MOffset, 0);
}

static createOriginMVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createOriginMVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createOriginMVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startOriginMVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSpanM(builder:flatbuffers.Builder, SPAN_MOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, SPAN_MOffset, 0);
}

static createSpanMVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createSpanMVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createSpanMVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startSpanMVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addData(builder:flatbuffers.Builder, DATAOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, DATAOffset, 0);
}

static createDataVector(builder:flatbuffers.Builder, data:number[]|Uint16Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createDataVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createDataVector(builder:flatbuffers.Builder, data:number[]|Uint16Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(2, data.length, 2);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt16(data[i]!);
  }
  return builder.endVector();
}

static startDataVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(2, numElems, 2);
}

static addChunkCid(builder:flatbuffers.Builder, CHUNK_CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, CHUNK_CIDOffset, 0);
}

static endGCTDensityVolume(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGCTDensityVolume(builder:flatbuffers.Builder, N:number, ORIGIN_MOffset:flatbuffers.Offset, SPAN_MOffset:flatbuffers.Offset, DATAOffset:flatbuffers.Offset, CHUNK_CIDOffset:flatbuffers.Offset):flatbuffers.Offset {
  GCTDensityVolume.startGCTDensityVolume(builder);
  GCTDensityVolume.addN(builder, N);
  GCTDensityVolume.addOriginM(builder, ORIGIN_MOffset);
  GCTDensityVolume.addSpanM(builder, SPAN_MOffset);
  GCTDensityVolume.addData(builder, DATAOffset);
  GCTDensityVolume.addChunkCid(builder, CHUNK_CIDOffset);
  return GCTDensityVolume.endGCTDensityVolume(builder);
}

unpack(): GCTDensityVolumeT {
  return new GCTDensityVolumeT(
    this.N(),
    this.bb!.createScalarList<number>(this.ORIGIN_M.bind(this), this.originMLength()),
    this.bb!.createScalarList<number>(this.SPAN_M.bind(this), this.spanMLength()),
    this.bb!.createScalarList<number>(this.DATA.bind(this), this.dataLength()),
    this.CHUNK_CID()
  );
}


unpackTo(_o: GCTDensityVolumeT): void {
  _o.N = this.N();
  _o.ORIGIN_M = this.bb!.createScalarList<number>(this.ORIGIN_M.bind(this), this.originMLength());
  _o.SPAN_M = this.bb!.createScalarList<number>(this.SPAN_M.bind(this), this.spanMLength());
  _o.DATA = this.bb!.createScalarList<number>(this.DATA.bind(this), this.dataLength());
  _o.CHUNK_CID = this.CHUNK_CID();
}
}

export class GCTDensityVolumeT implements flatbuffers.IGeneratedObject {
constructor(
  public N: number = 0,
  public ORIGIN_M: (number)[] = [],
  public SPAN_M: (number)[] = [],
  public DATA: (number)[] = [],
  public CHUNK_CID: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ORIGIN_M = GCTDensityVolume.createOriginMVector(builder, this.ORIGIN_M);
  const SPAN_M = GCTDensityVolume.createSpanMVector(builder, this.SPAN_M);
  const DATA = GCTDensityVolume.createDataVector(builder, this.DATA);
  const CHUNK_CID = (this.CHUNK_CID !== null ? builder.createString(this.CHUNK_CID!) : 0);

  return GCTDensityVolume.createGCTDensityVolume(builder,
    this.N,
    ORIGIN_M,
    SPAN_M,
    DATA,
    CHUNK_CID
  );
}
}
