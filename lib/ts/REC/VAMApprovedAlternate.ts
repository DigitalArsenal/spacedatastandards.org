// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { VAMTransform, VAMTransformT } from './VAMTransform.js';


/**
 * Exact approved alternate asset bytes and reviewed state.
 */
export class VAMApprovedAlternate implements flatbuffers.IUnpackableObject<VAMApprovedAlternateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VAMApprovedAlternate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVAMApprovedAlternate(bb:flatbuffers.ByteBuffer, obj?:VAMApprovedAlternate):VAMApprovedAlternate {
  return (obj || new VAMApprovedAlternate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVAMApprovedAlternate(bb:flatbuffers.ByteBuffer, obj?:VAMApprovedAlternate):VAMApprovedAlternate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VAMApprovedAlternate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

VARIANT_ID():string
VARIANT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
VARIANT_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Nonempty CIDv1 identifying the exact approved alternate bytes.
 */
CID():string
CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
CID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * SHA-256 of the exact approved alternate bytes as 64 lowercase hexadecimal characters.
 */
BYTE_SHA256():string
BYTE_SHA256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
BYTE_SHA256(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

REVIEWED_TRANSFORM(obj?:VAMTransform):VAMTransform|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new VAMTransform()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

RANK():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startVAMApprovedAlternate(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addVariantId(builder:flatbuffers.Builder, VARIANT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, VARIANT_IDOffset, 0);
}

static addCid(builder:flatbuffers.Builder, CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, CIDOffset, 0);
}

static addByteSha256(builder:flatbuffers.Builder, BYTE_SHA256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(2, BYTE_SHA256Offset, 0);
}

static addReviewedTransform(builder:flatbuffers.Builder, REVIEWED_TRANSFORMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, REVIEWED_TRANSFORMOffset, 0);
}

static addRank(builder:flatbuffers.Builder, RANK:number) {
  builder.addFieldInt32(4, RANK, 0);
}

static endVAMApprovedAlternate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // VARIANT_ID
  builder.requiredField(offset, 6) // CID
  builder.requiredField(offset, 8) // BYTE_SHA256
  builder.requiredField(offset, 10) // REVIEWED_TRANSFORM
  return offset;
}


unpack(): VAMApprovedAlternateT {
  return new VAMApprovedAlternateT(
    this.VARIANT_ID(),
    this.CID(),
    this.BYTE_SHA256(),
    (this.REVIEWED_TRANSFORM() !== null ? this.REVIEWED_TRANSFORM()!.unpack() : null),
    this.RANK()
  );
}


unpackTo(_o: VAMApprovedAlternateT): void {
  _o.VARIANT_ID = this.VARIANT_ID();
  _o.CID = this.CID();
  _o.BYTE_SHA256 = this.BYTE_SHA256();
  _o.REVIEWED_TRANSFORM = (this.REVIEWED_TRANSFORM() !== null ? this.REVIEWED_TRANSFORM()!.unpack() : null);
  _o.RANK = this.RANK();
}
}

export class VAMApprovedAlternateT implements flatbuffers.IGeneratedObject {
constructor(
  public VARIANT_ID: string|Uint8Array|null = null,
  public CID: string|Uint8Array|null = null,
  public BYTE_SHA256: string|Uint8Array|null = null,
  public REVIEWED_TRANSFORM: VAMTransformT|null = null,
  public RANK: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const VARIANT_ID = (this.VARIANT_ID !== null ? builder.createString(this.VARIANT_ID!) : 0);
  const CID = (this.CID !== null ? builder.createString(this.CID!) : 0);
  const BYTE_SHA256 = (this.BYTE_SHA256 !== null ? builder.createString(this.BYTE_SHA256!) : 0);
  const REVIEWED_TRANSFORM = (this.REVIEWED_TRANSFORM !== null ? this.REVIEWED_TRANSFORM!.pack(builder) : 0);

  VAMApprovedAlternate.startVAMApprovedAlternate(builder);
  VAMApprovedAlternate.addVariantId(builder, VARIANT_ID);
  VAMApprovedAlternate.addCid(builder, CID);
  VAMApprovedAlternate.addByteSha256(builder, BYTE_SHA256);
  VAMApprovedAlternate.addReviewedTransform(builder, REVIEWED_TRANSFORM);
  VAMApprovedAlternate.addRank(builder, this.RANK);

  return VAMApprovedAlternate.endVAMApprovedAlternate(builder);
}
}
