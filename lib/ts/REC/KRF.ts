// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { keyReferenceAlgorithm } from './keyReferenceAlgorithm.js';
import { keyReferenceRole } from './keyReferenceRole.js';


/**
 * Key Reference Frame
 */
export class KRF implements flatbuffers.IUnpackableObject<KRFT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KRF {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKRF(bb:flatbuffers.ByteBuffer, obj?:KRF):KRF {
  return (obj || new KRF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKRF(bb:flatbuffers.ByteBuffer, obj?:KRF):KRF {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KRF()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$KRF');
}

/**
 * Logical key identifier used across publication and grant records.
 */
KEY_ID():string|null
KEY_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
KEY_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Host-managed key slot or key handle identifier.
 */
SLOT_ID():string|null
SLOT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SLOT_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Role the referenced key fulfills for the module flow.
 */
ROLE():keyReferenceRole {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : keyReferenceRole.Unknown;
}

/**
 * Algorithm or key family for the referenced key.
 */
ALGORITHM():keyReferenceAlgorithm {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : keyReferenceAlgorithm.Unknown;
}

/**
 * Optional public bytes for verification or peer identity binding.
 */
PUBLIC_KEY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

publicKeyLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

publicKeyArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Logical version of the referenced key.
 */
VERSION():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Expiration time in unix milliseconds, or 0 if unbounded.
 */
EXPIRES_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * True when the secret material remains in the host key slot.
 */
HOST_MANAGED():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : true;
}

static startKRF(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addKeyId(builder:flatbuffers.Builder, KEY_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, KEY_IDOffset, 0);
}

static addSlotId(builder:flatbuffers.Builder, SLOT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, SLOT_IDOffset, 0);
}

static addRole(builder:flatbuffers.Builder, ROLE:keyReferenceRole) {
  builder.addFieldInt8(2, ROLE, keyReferenceRole.Unknown);
}

static addAlgorithm(builder:flatbuffers.Builder, ALGORITHM:keyReferenceAlgorithm) {
  builder.addFieldInt8(3, ALGORITHM, keyReferenceAlgorithm.Unknown);
}

static addPublicKey(builder:flatbuffers.Builder, PUBLIC_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, PUBLIC_KEYOffset, 0);
}

static createPublicKeyVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPublicKeyVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addVersion(builder:flatbuffers.Builder, VERSION:number) {
  builder.addFieldInt32(5, VERSION, 0);
}

static addExpiresAt(builder:flatbuffers.Builder, EXPIRES_AT:bigint) {
  builder.addFieldInt64(6, EXPIRES_AT, BigInt('0'));
}

static addHostManaged(builder:flatbuffers.Builder, HOST_MANAGED:boolean) {
  builder.addFieldInt8(7, +HOST_MANAGED, +true);
}

static endKRF(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishKRFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$KRF');
}

static finishSizePrefixedKRFBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$KRF', true);
}

static createKRF(builder:flatbuffers.Builder, KEY_IDOffset:flatbuffers.Offset, SLOT_IDOffset:flatbuffers.Offset, ROLE:keyReferenceRole, ALGORITHM:keyReferenceAlgorithm, PUBLIC_KEYOffset:flatbuffers.Offset, VERSION:number, EXPIRES_AT:bigint, HOST_MANAGED:boolean):flatbuffers.Offset {
  KRF.startKRF(builder);
  KRF.addKeyId(builder, KEY_IDOffset);
  KRF.addSlotId(builder, SLOT_IDOffset);
  KRF.addRole(builder, ROLE);
  KRF.addAlgorithm(builder, ALGORITHM);
  KRF.addPublicKey(builder, PUBLIC_KEYOffset);
  KRF.addVersion(builder, VERSION);
  KRF.addExpiresAt(builder, EXPIRES_AT);
  KRF.addHostManaged(builder, HOST_MANAGED);
  return KRF.endKRF(builder);
}

unpack(): KRFT {
  return new KRFT(
    this.KEY_ID(),
    this.SLOT_ID(),
    this.ROLE(),
    this.ALGORITHM(),
    this.bb!.createScalarList<number>(this.PUBLIC_KEY.bind(this), this.publicKeyLength()),
    this.VERSION(),
    this.EXPIRES_AT(),
    this.HOST_MANAGED()
  );
}


unpackTo(_o: KRFT): void {
  _o.KEY_ID = this.KEY_ID();
  _o.SLOT_ID = this.SLOT_ID();
  _o.ROLE = this.ROLE();
  _o.ALGORITHM = this.ALGORITHM();
  _o.PUBLIC_KEY = this.bb!.createScalarList<number>(this.PUBLIC_KEY.bind(this), this.publicKeyLength());
  _o.VERSION = this.VERSION();
  _o.EXPIRES_AT = this.EXPIRES_AT();
  _o.HOST_MANAGED = this.HOST_MANAGED();
}
}

export class KRFT implements flatbuffers.IGeneratedObject {
constructor(
  public KEY_ID: string|Uint8Array|null = null,
  public SLOT_ID: string|Uint8Array|null = null,
  public ROLE: keyReferenceRole = keyReferenceRole.Unknown,
  public ALGORITHM: keyReferenceAlgorithm = keyReferenceAlgorithm.Unknown,
  public PUBLIC_KEY: (number)[] = [],
  public VERSION: number = 0,
  public EXPIRES_AT: bigint = BigInt('0'),
  public HOST_MANAGED: boolean = true
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const KEY_ID = (this.KEY_ID !== null ? builder.createString(this.KEY_ID!) : 0);
  const SLOT_ID = (this.SLOT_ID !== null ? builder.createString(this.SLOT_ID!) : 0);
  const PUBLIC_KEY = KRF.createPublicKeyVector(builder, this.PUBLIC_KEY);

  return KRF.createKRF(builder,
    KEY_ID,
    SLOT_ID,
    this.ROLE,
    this.ALGORITHM,
    PUBLIC_KEY,
    this.VERSION,
    this.EXPIRES_AT,
    this.HOST_MANAGED
  );
}
}
