// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { scvResultState } from './scvResultState.js';


export class SCVProgress implements flatbuffers.IUnpackableObject<SCVProgressT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SCVProgress {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSCVProgress(bb:flatbuffers.ByteBuffer, obj?:SCVProgress):SCVProgress {
  return (obj || new SCVProgress()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSCVProgress(bb:flatbuffers.ByteBuffer, obj?:SCVProgress):SCVProgress {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SCVProgress()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

JOB_ID():string|null
JOB_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
JOB_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

TRACE_ID():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

STATUS():scvResultState {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : scvResultState.OK;
}

COMPLETED_SENSORS():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

TOTAL_SENSORS():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

COMPLETED_WINDOWS():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

TOTAL_WINDOWS():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

BACKLOG_REMAINING():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

COMPLETION_FRACTION():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MESSAGE():string|null
MESSAGE(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MESSAGE(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startSCVProgress(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addJobId(builder:flatbuffers.Builder, JOB_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, JOB_IDOffset, 0);
}

static addTraceId(builder:flatbuffers.Builder, TRACE_ID:bigint) {
  builder.addFieldInt64(1, TRACE_ID, BigInt('0'));
}

static addStatus(builder:flatbuffers.Builder, STATUS:scvResultState) {
  builder.addFieldInt8(2, STATUS, scvResultState.OK);
}

static addCompletedSensors(builder:flatbuffers.Builder, COMPLETED_SENSORS:number) {
  builder.addFieldInt32(3, COMPLETED_SENSORS, 0);
}

static addTotalSensors(builder:flatbuffers.Builder, TOTAL_SENSORS:number) {
  builder.addFieldInt32(4, TOTAL_SENSORS, 0);
}

static addCompletedWindows(builder:flatbuffers.Builder, COMPLETED_WINDOWS:number) {
  builder.addFieldInt32(5, COMPLETED_WINDOWS, 0);
}

static addTotalWindows(builder:flatbuffers.Builder, TOTAL_WINDOWS:number) {
  builder.addFieldInt32(6, TOTAL_WINDOWS, 0);
}

static addBacklogRemaining(builder:flatbuffers.Builder, BACKLOG_REMAINING:number) {
  builder.addFieldInt32(7, BACKLOG_REMAINING, 0);
}

static addCompletionFraction(builder:flatbuffers.Builder, COMPLETION_FRACTION:number) {
  builder.addFieldFloat64(8, COMPLETION_FRACTION, 0.0);
}

static addMessage(builder:flatbuffers.Builder, MESSAGEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, MESSAGEOffset, 0);
}

static endSCVProgress(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSCVProgress(builder:flatbuffers.Builder, JOB_IDOffset:flatbuffers.Offset, TRACE_ID:bigint, STATUS:scvResultState, COMPLETED_SENSORS:number, TOTAL_SENSORS:number, COMPLETED_WINDOWS:number, TOTAL_WINDOWS:number, BACKLOG_REMAINING:number, COMPLETION_FRACTION:number, MESSAGEOffset:flatbuffers.Offset):flatbuffers.Offset {
  SCVProgress.startSCVProgress(builder);
  SCVProgress.addJobId(builder, JOB_IDOffset);
  SCVProgress.addTraceId(builder, TRACE_ID);
  SCVProgress.addStatus(builder, STATUS);
  SCVProgress.addCompletedSensors(builder, COMPLETED_SENSORS);
  SCVProgress.addTotalSensors(builder, TOTAL_SENSORS);
  SCVProgress.addCompletedWindows(builder, COMPLETED_WINDOWS);
  SCVProgress.addTotalWindows(builder, TOTAL_WINDOWS);
  SCVProgress.addBacklogRemaining(builder, BACKLOG_REMAINING);
  SCVProgress.addCompletionFraction(builder, COMPLETION_FRACTION);
  SCVProgress.addMessage(builder, MESSAGEOffset);
  return SCVProgress.endSCVProgress(builder);
}

unpack(): SCVProgressT {
  return new SCVProgressT(
    this.JOB_ID(),
    this.TRACE_ID(),
    this.STATUS(),
    this.COMPLETED_SENSORS(),
    this.TOTAL_SENSORS(),
    this.COMPLETED_WINDOWS(),
    this.TOTAL_WINDOWS(),
    this.BACKLOG_REMAINING(),
    this.COMPLETION_FRACTION(),
    this.MESSAGE()
  );
}


unpackTo(_o: SCVProgressT): void {
  _o.JOB_ID = this.JOB_ID();
  _o.TRACE_ID = this.TRACE_ID();
  _o.STATUS = this.STATUS();
  _o.COMPLETED_SENSORS = this.COMPLETED_SENSORS();
  _o.TOTAL_SENSORS = this.TOTAL_SENSORS();
  _o.COMPLETED_WINDOWS = this.COMPLETED_WINDOWS();
  _o.TOTAL_WINDOWS = this.TOTAL_WINDOWS();
  _o.BACKLOG_REMAINING = this.BACKLOG_REMAINING();
  _o.COMPLETION_FRACTION = this.COMPLETION_FRACTION();
  _o.MESSAGE = this.MESSAGE();
}
}

export class SCVProgressT implements flatbuffers.IGeneratedObject {
constructor(
  public JOB_ID: string|Uint8Array|null = null,
  public TRACE_ID: bigint = BigInt('0'),
  public STATUS: scvResultState = scvResultState.OK,
  public COMPLETED_SENSORS: number = 0,
  public TOTAL_SENSORS: number = 0,
  public COMPLETED_WINDOWS: number = 0,
  public TOTAL_WINDOWS: number = 0,
  public BACKLOG_REMAINING: number = 0,
  public COMPLETION_FRACTION: number = 0.0,
  public MESSAGE: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const JOB_ID = (this.JOB_ID !== null ? builder.createString(this.JOB_ID!) : 0);
  const MESSAGE = (this.MESSAGE !== null ? builder.createString(this.MESSAGE!) : 0);

  return SCVProgress.createSCVProgress(builder,
    JOB_ID,
    this.TRACE_ID,
    this.STATUS,
    this.COMPLETED_SENSORS,
    this.TOTAL_SENSORS,
    this.COMPLETED_WINDOWS,
    this.TOTAL_WINDOWS,
    this.BACKLOG_REMAINING,
    this.COMPLETION_FRACTION,
    MESSAGE
  );
}
}
