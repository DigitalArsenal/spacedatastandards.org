// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Conical SDF parameters.
 */
export class SDFConical implements flatbuffers.IUnpackableObject<SDFConicalT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SDFConical {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSDFConical(bb:flatbuffers.ByteBuffer, obj?:SDFConical):SDFConical {
  return (obj || new SDFConical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSDFConical(bb:flatbuffers.ByteBuffer, obj?:SDFConical):SDFConical {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SDFConical()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

HALF_ANGLE():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Inner half-angle for hollow cones (0 == solid).
 */
INNER_HALF_ANGLE():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Minimum range (inner cap distance).
 */
MIN_RANGE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Maximum range (outer cap distance).
 */
MAX_RANGE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Use a flat cap (true) or a spherical cap (false).
 */
FLAT_CAP():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startSDFConical(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addHalfAngle(builder:flatbuffers.Builder, HALF_ANGLE:number) {
  builder.addFieldFloat64(0, HALF_ANGLE, 0.0);
}

static addInnerHalfAngle(builder:flatbuffers.Builder, INNER_HALF_ANGLE:number) {
  builder.addFieldFloat64(1, INNER_HALF_ANGLE, 0.0);
}

static addMinRange(builder:flatbuffers.Builder, MIN_RANGE:number) {
  builder.addFieldFloat64(2, MIN_RANGE, 0.0);
}

static addMaxRange(builder:flatbuffers.Builder, MAX_RANGE:number) {
  builder.addFieldFloat64(3, MAX_RANGE, 0.0);
}

static addFlatCap(builder:flatbuffers.Builder, FLAT_CAP:boolean) {
  builder.addFieldInt8(4, +FLAT_CAP, +false);
}

static endSDFConical(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSDFConical(builder:flatbuffers.Builder, HALF_ANGLE:number, INNER_HALF_ANGLE:number, MIN_RANGE:number, MAX_RANGE:number, FLAT_CAP:boolean):flatbuffers.Offset {
  SDFConical.startSDFConical(builder);
  SDFConical.addHalfAngle(builder, HALF_ANGLE);
  SDFConical.addInnerHalfAngle(builder, INNER_HALF_ANGLE);
  SDFConical.addMinRange(builder, MIN_RANGE);
  SDFConical.addMaxRange(builder, MAX_RANGE);
  SDFConical.addFlatCap(builder, FLAT_CAP);
  return SDFConical.endSDFConical(builder);
}

unpack(): SDFConicalT {
  return new SDFConicalT(
    this.HALF_ANGLE(),
    this.INNER_HALF_ANGLE(),
    this.MIN_RANGE(),
    this.MAX_RANGE(),
    this.FLAT_CAP()
  );
}


unpackTo(_o: SDFConicalT): void {
  _o.HALF_ANGLE = this.HALF_ANGLE();
  _o.INNER_HALF_ANGLE = this.INNER_HALF_ANGLE();
  _o.MIN_RANGE = this.MIN_RANGE();
  _o.MAX_RANGE = this.MAX_RANGE();
  _o.FLAT_CAP = this.FLAT_CAP();
}
}

export class SDFConicalT implements flatbuffers.IGeneratedObject {
constructor(
  public HALF_ANGLE: number = 0.0,
  public INNER_HALF_ANGLE: number = 0.0,
  public MIN_RANGE: number = 0.0,
  public MAX_RANGE: number = 0.0,
  public FLAT_CAP: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return SDFConical.createSDFConical(builder,
    this.HALF_ANGLE,
    this.INNER_HALF_ANGLE,
    this.MIN_RANGE,
    this.MAX_RANGE,
    this.FLAT_CAP
  );
}
}
