// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { weightedEdge, weightedEdgeT } from './weightedEdge.js';


/**
 * Bulk weighted-edge ingestion request.
 */
export class FPCWeightedEdgeList implements flatbuffers.IUnpackableObject<FPCWeightedEdgeListT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FPCWeightedEdgeList {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFPCWeightedEdgeList(bb:flatbuffers.ByteBuffer, obj?:FPCWeightedEdgeList):FPCWeightedEdgeList {
  return (obj || new FPCWeightedEdgeList()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFPCWeightedEdgeList(bb:flatbuffers.ByteBuffer, obj?:FPCWeightedEdgeList):FPCWeightedEdgeList {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FPCWeightedEdgeList()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Optional vertex capacity for implicit graph creation.
 */
VERTEX_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Weighted directed edge records.
 */
EDGES(index: number, obj?:weightedEdge):weightedEdge|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new weightedEdge()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 16, this.bb!) : null;
}

edgesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Build a CSR representation after ingest.
 */
BUILD_GRAPH():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startFPCWeightedEdgeList(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addVertexCount(builder:flatbuffers.Builder, VERTEX_COUNT:number) {
  builder.addFieldInt32(0, VERTEX_COUNT, 0);
}

static addEdges(builder:flatbuffers.Builder, EDGESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, EDGESOffset, 0);
}

static startEdgesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(16, numElems, 8);
}

static addBuildGraph(builder:flatbuffers.Builder, BUILD_GRAPH:boolean) {
  builder.addFieldInt8(2, +BUILD_GRAPH, +false);
}

static endFPCWeightedEdgeList(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFPCWeightedEdgeList(builder:flatbuffers.Builder, VERTEX_COUNT:number, EDGESOffset:flatbuffers.Offset, BUILD_GRAPH:boolean):flatbuffers.Offset {
  FPCWeightedEdgeList.startFPCWeightedEdgeList(builder);
  FPCWeightedEdgeList.addVertexCount(builder, VERTEX_COUNT);
  FPCWeightedEdgeList.addEdges(builder, EDGESOffset);
  FPCWeightedEdgeList.addBuildGraph(builder, BUILD_GRAPH);
  return FPCWeightedEdgeList.endFPCWeightedEdgeList(builder);
}

unpack(): FPCWeightedEdgeListT {
  return new FPCWeightedEdgeListT(
    this.VERTEX_COUNT(),
    this.bb!.createObjList<weightedEdge, weightedEdgeT>(this.EDGES.bind(this), this.edgesLength()),
    this.BUILD_GRAPH()
  );
}


unpackTo(_o: FPCWeightedEdgeListT): void {
  _o.VERTEX_COUNT = this.VERTEX_COUNT();
  _o.EDGES = this.bb!.createObjList<weightedEdge, weightedEdgeT>(this.EDGES.bind(this), this.edgesLength());
  _o.BUILD_GRAPH = this.BUILD_GRAPH();
}
}

export class FPCWeightedEdgeListT implements flatbuffers.IGeneratedObject {
constructor(
  public VERTEX_COUNT: number = 0,
  public EDGES: (weightedEdgeT)[] = [],
  public BUILD_GRAPH: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const EDGES = builder.createStructOffsetList(this.EDGES, FPCWeightedEdgeList.startEdgesVector);

  return FPCWeightedEdgeList.createFPCWeightedEdgeList(builder,
    this.VERTEX_COUNT,
    EDGES,
    this.BUILD_GRAPH
  );
}
}
