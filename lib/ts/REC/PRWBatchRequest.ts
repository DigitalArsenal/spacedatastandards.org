// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Batch propagation request — propagate every entity in ENTITY_HANDLES[] to
 * EPOCH and write the result StateVector stream to OUTPUT_OFFSET in the
 * shared arena.
 */
export class PRWBatchRequest implements flatbuffers.IUnpackableObject<PRWBatchRequestT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PRWBatchRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPRWBatchRequest(bb:flatbuffers.ByteBuffer, obj?:PRWBatchRequest):PRWBatchRequest {
  return (obj || new PRWBatchRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPRWBatchRequest(bb:flatbuffers.ByteBuffer, obj?:PRWBatchRequest):PRWBatchRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PRWBatchRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Target epoch as a Julian date (TIME_SYSTEM is configured on the host).
 */
EPOCH():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Entity handles to propagate (empty = all initialized entities).
 */
ENTITY_HANDLES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

entityHandlesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

entityHandlesArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Output buffer offset in the arena where the StateVector stream begins.
 */
OUTPUT_OFFSET():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Maximum entities to process in this call (0 = unbounded).
 */
MAX_COUNT():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Target reference frame for the output state stream. Matches enum
 * values in SDS `RFM`. If zero, the propagator chooses its native frame.
 */
TARGET_FRAME():string|null
TARGET_FRAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
TARGET_FRAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startPRWBatchRequest(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addEpoch(builder:flatbuffers.Builder, EPOCH:number) {
  builder.addFieldFloat64(0, EPOCH, 0.0);
}

static addEntityHandles(builder:flatbuffers.Builder, ENTITY_HANDLESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ENTITY_HANDLESOffset, 0);
}

static createEntityHandlesVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createEntityHandlesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createEntityHandlesVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startEntityHandlesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOutputOffset(builder:flatbuffers.Builder, OUTPUT_OFFSET:number) {
  builder.addFieldInt32(2, OUTPUT_OFFSET, 0);
}

static addMaxCount(builder:flatbuffers.Builder, MAX_COUNT:number) {
  builder.addFieldInt32(3, MAX_COUNT, 0);
}

static addTargetFrame(builder:flatbuffers.Builder, TARGET_FRAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, TARGET_FRAMEOffset, 0);
}

static endPRWBatchRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPRWBatchRequest(builder:flatbuffers.Builder, EPOCH:number, ENTITY_HANDLESOffset:flatbuffers.Offset, OUTPUT_OFFSET:number, MAX_COUNT:number, TARGET_FRAMEOffset:flatbuffers.Offset):flatbuffers.Offset {
  PRWBatchRequest.startPRWBatchRequest(builder);
  PRWBatchRequest.addEpoch(builder, EPOCH);
  PRWBatchRequest.addEntityHandles(builder, ENTITY_HANDLESOffset);
  PRWBatchRequest.addOutputOffset(builder, OUTPUT_OFFSET);
  PRWBatchRequest.addMaxCount(builder, MAX_COUNT);
  PRWBatchRequest.addTargetFrame(builder, TARGET_FRAMEOffset);
  return PRWBatchRequest.endPRWBatchRequest(builder);
}

unpack(): PRWBatchRequestT {
  return new PRWBatchRequestT(
    this.EPOCH(),
    this.bb!.createScalarList<number>(this.ENTITY_HANDLES.bind(this), this.entityHandlesLength()),
    this.OUTPUT_OFFSET(),
    this.MAX_COUNT(),
    this.TARGET_FRAME()
  );
}


unpackTo(_o: PRWBatchRequestT): void {
  _o.EPOCH = this.EPOCH();
  _o.ENTITY_HANDLES = this.bb!.createScalarList<number>(this.ENTITY_HANDLES.bind(this), this.entityHandlesLength());
  _o.OUTPUT_OFFSET = this.OUTPUT_OFFSET();
  _o.MAX_COUNT = this.MAX_COUNT();
  _o.TARGET_FRAME = this.TARGET_FRAME();
}
}

export class PRWBatchRequestT implements flatbuffers.IGeneratedObject {
constructor(
  public EPOCH: number = 0.0,
  public ENTITY_HANDLES: (number)[] = [],
  public OUTPUT_OFFSET: number = 0,
  public MAX_COUNT: number = 0,
  public TARGET_FRAME: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ENTITY_HANDLES = PRWBatchRequest.createEntityHandlesVector(builder, this.ENTITY_HANDLES);
  const TARGET_FRAME = (this.TARGET_FRAME !== null ? builder.createString(this.TARGET_FRAME!) : 0);

  return PRWBatchRequest.createPRWBatchRequest(builder,
    this.EPOCH,
    ENTITY_HANDLES,
    this.OUTPUT_OFFSET,
    this.MAX_COUNT,
    TARGET_FRAME
  );
}
}
