// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { appSourceKind } from './appSourceKind.js';


/**
 * One upstream data source the app depends on.
 */
export class APPSourceRef implements flatbuffers.IUnpackableObject<APPSourceRefT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):APPSourceRef {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAPPSourceRef(bb:flatbuffers.ByteBuffer, obj?:APPSourceRef):APPSourceRef {
  return (obj || new APPSourceRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAPPSourceRef(bb:flatbuffers.ByteBuffer, obj?:APPSourceRef):APPSourceRef {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new APPSourceRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * App-local stable reference for this source. Required, unique within
 * the manifest.
 */
ID():string
ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Classifies REF.
 */
KIND():appSourceKind {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : appSourceKind.MODULE;
}

/**
 * Source identifier: an APPModuleRef.ID when KIND is MODULE, otherwise a
 * URL or dataset identifier. Required.
 */
REF():string
REF(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
REF(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Human-readable summary.
 */
DESCRIPTION():string|null
DESCRIPTION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DESCRIPTION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startAPPSourceRef(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addId(builder:flatbuffers.Builder, IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, IDOffset, 0);
}

static addKind(builder:flatbuffers.Builder, KIND:appSourceKind) {
  builder.addFieldInt8(1, KIND, appSourceKind.MODULE);
}

static addRef(builder:flatbuffers.Builder, REFOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, REFOffset, 0);
}

static addDescription(builder:flatbuffers.Builder, DESCRIPTIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, DESCRIPTIONOffset, 0);
}

static endAPPSourceRef(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // ID
  builder.requiredField(offset, 8) // REF
  return offset;
}

static createAPPSourceRef(builder:flatbuffers.Builder, IDOffset:flatbuffers.Offset, KIND:appSourceKind, REFOffset:flatbuffers.Offset, DESCRIPTIONOffset:flatbuffers.Offset):flatbuffers.Offset {
  APPSourceRef.startAPPSourceRef(builder);
  APPSourceRef.addId(builder, IDOffset);
  APPSourceRef.addKind(builder, KIND);
  APPSourceRef.addRef(builder, REFOffset);
  APPSourceRef.addDescription(builder, DESCRIPTIONOffset);
  return APPSourceRef.endAPPSourceRef(builder);
}

unpack(): APPSourceRefT {
  return new APPSourceRefT(
    this.ID(),
    this.KIND(),
    this.REF(),
    this.DESCRIPTION()
  );
}


unpackTo(_o: APPSourceRefT): void {
  _o.ID = this.ID();
  _o.KIND = this.KIND();
  _o.REF = this.REF();
  _o.DESCRIPTION = this.DESCRIPTION();
}
}

export class APPSourceRefT implements flatbuffers.IGeneratedObject {
constructor(
  public ID: string|Uint8Array|null = null,
  public KIND: appSourceKind = appSourceKind.MODULE,
  public REF: string|Uint8Array|null = null,
  public DESCRIPTION: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ID = (this.ID !== null ? builder.createString(this.ID!) : 0);
  const REF = (this.REF !== null ? builder.createString(this.REF!) : 0);
  const DESCRIPTION = (this.DESCRIPTION !== null ? builder.createString(this.DESCRIPTION!) : 0);

  return APPSourceRef.createAPPSourceRef(builder,
    ID,
    this.KIND,
    REF,
    DESCRIPTION
  );
}
}
