// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One SigMF capture segment: the point in the sample stream at which the
 * listed tuning took effect. A retuned or frequency-hopped recording has
 * several; a single-tune recording has one.
 */
export class IQCSegment implements flatbuffers.IUnpackableObject<IQCSegmentT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IQCSegment {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIQCSegment(bb:flatbuffers.ByteBuffer, obj?:IQCSegment):IQCSegment {
  return (obj || new IQCSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIQCSegment(bb:flatbuffers.ByteBuffer, obj?:IQCSegment):IQCSegment {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IQCSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * SigMF `core:sample_start` — index of the first sample of this segment.
 */
SAMPLE_START():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * SigMF `core:global_index` — index of this sample in the receiver's
 * free-running sample counter.
 */
GLOBAL_INDEX():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * SigMF `core:header_bytes` — bytes preceding this segment in the data
 * file.
 */
HEADER_BYTES():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * SigMF `core:frequency` — centre frequency of this segment, HERTZ.
 */
CENTER_FREQ_HZ():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * SigMF `core:datetime` — ISO 8601 UTC timestamp of SAMPLE_START.
 */
DATETIME():string|null
DATETIME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
DATETIME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startIQCSegment(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addSampleStart(builder:flatbuffers.Builder, SAMPLE_START:bigint) {
  builder.addFieldInt64(0, SAMPLE_START, BigInt('0'));
}

static addGlobalIndex(builder:flatbuffers.Builder, GLOBAL_INDEX:bigint) {
  builder.addFieldInt64(1, GLOBAL_INDEX, BigInt('0'));
}

static addHeaderBytes(builder:flatbuffers.Builder, HEADER_BYTES:bigint) {
  builder.addFieldInt64(2, HEADER_BYTES, BigInt('0'));
}

static addCenterFreqHz(builder:flatbuffers.Builder, CENTER_FREQ_HZ:number) {
  builder.addFieldFloat64(3, CENTER_FREQ_HZ, 0.0);
}

static addDatetime(builder:flatbuffers.Builder, DATETIMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, DATETIMEOffset, 0);
}

static endIQCSegment(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIQCSegment(builder:flatbuffers.Builder, SAMPLE_START:bigint, GLOBAL_INDEX:bigint, HEADER_BYTES:bigint, CENTER_FREQ_HZ:number, DATETIMEOffset:flatbuffers.Offset):flatbuffers.Offset {
  IQCSegment.startIQCSegment(builder);
  IQCSegment.addSampleStart(builder, SAMPLE_START);
  IQCSegment.addGlobalIndex(builder, GLOBAL_INDEX);
  IQCSegment.addHeaderBytes(builder, HEADER_BYTES);
  IQCSegment.addCenterFreqHz(builder, CENTER_FREQ_HZ);
  IQCSegment.addDatetime(builder, DATETIMEOffset);
  return IQCSegment.endIQCSegment(builder);
}

unpack(): IQCSegmentT {
  return new IQCSegmentT(
    this.SAMPLE_START(),
    this.GLOBAL_INDEX(),
    this.HEADER_BYTES(),
    this.CENTER_FREQ_HZ(),
    this.DATETIME()
  );
}


unpackTo(_o: IQCSegmentT): void {
  _o.SAMPLE_START = this.SAMPLE_START();
  _o.GLOBAL_INDEX = this.GLOBAL_INDEX();
  _o.HEADER_BYTES = this.HEADER_BYTES();
  _o.CENTER_FREQ_HZ = this.CENTER_FREQ_HZ();
  _o.DATETIME = this.DATETIME();
}
}

export class IQCSegmentT implements flatbuffers.IGeneratedObject {
constructor(
  public SAMPLE_START: bigint = BigInt('0'),
  public GLOBAL_INDEX: bigint = BigInt('0'),
  public HEADER_BYTES: bigint = BigInt('0'),
  public CENTER_FREQ_HZ: number = 0.0,
  public DATETIME: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const DATETIME = (this.DATETIME !== null ? builder.createString(this.DATETIME!) : 0);

  return IQCSegment.createIQCSegment(builder,
    this.SAMPLE_START,
    this.GLOBAL_INDEX,
    this.HEADER_BYTES,
    this.CENTER_FREQ_HZ,
    DATETIME
  );
}
}
