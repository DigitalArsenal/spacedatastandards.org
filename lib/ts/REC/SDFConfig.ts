// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SDFComponent, SDFComponentT } from './SDFComponent.js';
import { SDFComposition, SDFCompositionT } from './SDFComposition.js';
import { sdfPrimitive } from './sdfPrimitive.js';


/**
 * Complete SDF configuration.
 */
export class SDFConfig implements flatbuffers.IUnpackableObject<SDFConfigT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SDFConfig {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSDFConfig(bb:flatbuffers.ByteBuffer, obj?:SDFConfig):SDFConfig {
  return (obj || new SDFConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSDFConfig(bb:flatbuffers.ByteBuffer, obj?:SDFConfig):SDFConfig {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SDFConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Root primitive type (useful when COMPONENTS[] is empty).
 */
PRIMITIVE():sdfPrimitive {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : sdfPrimitive.CONE;
}

/**
 * One or more components that may be combined via COMPOSITIONS[].
 */
COMPONENTS(index: number, obj?:SDFComponent):SDFComponent|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new SDFComponent()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

componentsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Composition tree over COMPONENTS[].
 */
COMPOSITIONS(index: number, obj?:SDFComposition):SDFComposition|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new SDFComposition()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

compositionsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * World-to-local transform (4x4 column-major, 16 doubles).
 */
INVERSE_TRANSFORM(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

inverseTransformLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

inverseTransformArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startSDFConfig(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addPrimitive(builder:flatbuffers.Builder, PRIMITIVE:sdfPrimitive) {
  builder.addFieldInt8(0, PRIMITIVE, sdfPrimitive.CONE);
}

static addComponents(builder:flatbuffers.Builder, COMPONENTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, COMPONENTSOffset, 0);
}

static createComponentsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startComponentsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCompositions(builder:flatbuffers.Builder, COMPOSITIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, COMPOSITIONSOffset, 0);
}

static createCompositionsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCompositionsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addInverseTransform(builder:flatbuffers.Builder, INVERSE_TRANSFORMOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, INVERSE_TRANSFORMOffset, 0);
}

static createInverseTransformVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createInverseTransformVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createInverseTransformVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startInverseTransformVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static endSDFConfig(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSDFConfig(builder:flatbuffers.Builder, PRIMITIVE:sdfPrimitive, COMPONENTSOffset:flatbuffers.Offset, COMPOSITIONSOffset:flatbuffers.Offset, INVERSE_TRANSFORMOffset:flatbuffers.Offset):flatbuffers.Offset {
  SDFConfig.startSDFConfig(builder);
  SDFConfig.addPrimitive(builder, PRIMITIVE);
  SDFConfig.addComponents(builder, COMPONENTSOffset);
  SDFConfig.addCompositions(builder, COMPOSITIONSOffset);
  SDFConfig.addInverseTransform(builder, INVERSE_TRANSFORMOffset);
  return SDFConfig.endSDFConfig(builder);
}

unpack(): SDFConfigT {
  return new SDFConfigT(
    this.PRIMITIVE(),
    this.bb!.createObjList<SDFComponent, SDFComponentT>(this.COMPONENTS.bind(this), this.componentsLength()),
    this.bb!.createObjList<SDFComposition, SDFCompositionT>(this.COMPOSITIONS.bind(this), this.compositionsLength()),
    this.bb!.createScalarList<number>(this.INVERSE_TRANSFORM.bind(this), this.inverseTransformLength())
  );
}


unpackTo(_o: SDFConfigT): void {
  _o.PRIMITIVE = this.PRIMITIVE();
  _o.COMPONENTS = this.bb!.createObjList<SDFComponent, SDFComponentT>(this.COMPONENTS.bind(this), this.componentsLength());
  _o.COMPOSITIONS = this.bb!.createObjList<SDFComposition, SDFCompositionT>(this.COMPOSITIONS.bind(this), this.compositionsLength());
  _o.INVERSE_TRANSFORM = this.bb!.createScalarList<number>(this.INVERSE_TRANSFORM.bind(this), this.inverseTransformLength());
}
}

export class SDFConfigT implements flatbuffers.IGeneratedObject {
constructor(
  public PRIMITIVE: sdfPrimitive = sdfPrimitive.CONE,
  public COMPONENTS: (SDFComponentT)[] = [],
  public COMPOSITIONS: (SDFCompositionT)[] = [],
  public INVERSE_TRANSFORM: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const COMPONENTS = SDFConfig.createComponentsVector(builder, builder.createObjectOffsetList(this.COMPONENTS));
  const COMPOSITIONS = SDFConfig.createCompositionsVector(builder, builder.createObjectOffsetList(this.COMPOSITIONS));
  const INVERSE_TRANSFORM = SDFConfig.createInverseTransformVector(builder, this.INVERSE_TRANSFORM);

  return SDFConfig.createSDFConfig(builder,
    this.PRIMITIVE,
    COMPONENTS,
    COMPOSITIONS,
    INVERSE_TRANSFORM
  );
}
}
