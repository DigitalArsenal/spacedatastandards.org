// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SplitRecipient, SplitRecipientT } from './SplitRecipient.js';
import { splitRole } from './splitRole.js';


/**
 * One recipient's share of a sale, expressed in basis points.
 */
export class RevenueSplit implements flatbuffers.IUnpackableObject<RevenueSplitT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RevenueSplit {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRevenueSplit(bb:flatbuffers.ByteBuffer, obj?:RevenueSplit):RevenueSplit {
  return (obj || new RevenueSplit()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRevenueSplit(bb:flatbuffers.ByteBuffer, obj?:RevenueSplit):RevenueSplit {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RevenueSplit()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * The payout recipient
 */
RECIPIENT(obj?:SplitRecipient):SplitRecipient|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new SplitRecipient()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Share in basis points (1/100 of a percent); all splits plus the platform
 * commission sum to 10000
 */
BPS():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * The role this recipient plays
 */
ROLE():splitRole {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : splitRole.Publisher;
}

static startRevenueSplit(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addRecipient(builder:flatbuffers.Builder, RECIPIENTOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, RECIPIENTOffset, 0);
}

static addBps(builder:flatbuffers.Builder, BPS:number) {
  builder.addFieldInt16(1, BPS, 0);
}

static addRole(builder:flatbuffers.Builder, ROLE:splitRole) {
  builder.addFieldInt8(2, ROLE, splitRole.Publisher);
}

static endRevenueSplit(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRevenueSplit(builder:flatbuffers.Builder, RECIPIENTOffset:flatbuffers.Offset, BPS:number, ROLE:splitRole):flatbuffers.Offset {
  RevenueSplit.startRevenueSplit(builder);
  RevenueSplit.addRecipient(builder, RECIPIENTOffset);
  RevenueSplit.addBps(builder, BPS);
  RevenueSplit.addRole(builder, ROLE);
  return RevenueSplit.endRevenueSplit(builder);
}

unpack(): RevenueSplitT {
  return new RevenueSplitT(
    (this.RECIPIENT() !== null ? this.RECIPIENT()!.unpack() : null),
    this.BPS(),
    this.ROLE()
  );
}


unpackTo(_o: RevenueSplitT): void {
  _o.RECIPIENT = (this.RECIPIENT() !== null ? this.RECIPIENT()!.unpack() : null);
  _o.BPS = this.BPS();
  _o.ROLE = this.ROLE();
}
}

export class RevenueSplitT implements flatbuffers.IGeneratedObject {
constructor(
  public RECIPIENT: SplitRecipientT|null = null,
  public BPS: number = 0,
  public ROLE: splitRole = splitRole.Publisher
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const RECIPIENT = (this.RECIPIENT !== null ? this.RECIPIENT!.pack(builder) : 0);

  return RevenueSplit.createRevenueSplit(builder,
    RECIPIENT,
    this.BPS,
    this.ROLE
  );
}
}
