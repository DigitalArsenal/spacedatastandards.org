// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { brokerCustodyModel } from './brokerCustodyModel.js';
import { brokerLiveness } from './brokerLiveness.js';


/**
 * Publisher Key-Broker Descriptor
 */
export class PKB implements flatbuffers.IUnpackableObject<PKBT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PKB {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPKB(bb:flatbuffers.ByteBuffer, obj?:PKB):PKB {
  return (obj || new PKB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPKB(bb:flatbuffers.ByteBuffer, obj?:PKB):PKB {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PKB()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$PKB');
}

/**
 * Stable broker identity, referenced by `$PLG`/`$STO`/`$PUR`/`$LGR`
 */
BROKER_ID():string
BROKER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
BROKER_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * IPFS CID of the publisher `$EPM` that operates this broker
 */
PUBLISHER_EPM_CID():string|null
PUBLISHER_EPM_CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PUBLISHER_EPM_CID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * libp2p peer id of the publisher node hosting the broker
 */
PROVIDER_PEER_ID():string|null
PROVIDER_PEER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROVIDER_PEER_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * libp2p protocol the broker answers on
 * (e.g. "/space-data-network/module-delivery/1.0.0")
 */
PROTOCOL_ID():string|null
PROTOCOL_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROTOCOL_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Dialable multiformat addresses for the broker
 */
MULTIFORMAT_ADDRESSES(index: number):string
MULTIFORMAT_ADDRESSES(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
MULTIFORMAT_ADDRESSES(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

multiformatAddressesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * The broker's public key (from the key server's get_public_key) so the
 * buyer can pin it
 */
BROKER_PUBLIC_KEY(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

brokerPublicKeyLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

brokerPublicKeyArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Custody model (default: full publisher custody)
 */
BROKER_KIND():brokerCustodyModel {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : brokerCustodyModel.SelfHosted;
}

/**
 * Store-maintained liveness status
 */
LIVENESS():brokerLiveness {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : brokerLiveness.Unknown;
}

/**
 * Unix ms of the last successful probe/heartbeat
 */
LAST_HEARTBEAT_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Expected heartbeat cadence in ms
 */
HEARTBEAT_INTERVAL_MS():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Unix ms the store first saw this broker
 */
FIRST_SEEN_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Unix ms this descriptor was last updated
 */
UPDATED_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Ed25519 signature by the publisher `$EPM` over the self-attested endpoint
 * fields; re-verified by the store's live probe
 */
SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signatureArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startPKB(builder:flatbuffers.Builder) {
  builder.startObject(13);
}

static addBrokerId(builder:flatbuffers.Builder, BROKER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, BROKER_IDOffset, 0);
}

static addPublisherEpmCid(builder:flatbuffers.Builder, PUBLISHER_EPM_CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, PUBLISHER_EPM_CIDOffset, 0);
}

static addProviderPeerId(builder:flatbuffers.Builder, PROVIDER_PEER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, PROVIDER_PEER_IDOffset, 0);
}

static addProtocolId(builder:flatbuffers.Builder, PROTOCOL_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, PROTOCOL_IDOffset, 0);
}

static addMultiformatAddresses(builder:flatbuffers.Builder, MULTIFORMAT_ADDRESSESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, MULTIFORMAT_ADDRESSESOffset, 0);
}

static createMultiformatAddressesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startMultiformatAddressesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBrokerPublicKey(builder:flatbuffers.Builder, BROKER_PUBLIC_KEYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, BROKER_PUBLIC_KEYOffset, 0);
}

static createBrokerPublicKeyVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startBrokerPublicKeyVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addBrokerKind(builder:flatbuffers.Builder, BROKER_KIND:brokerCustodyModel) {
  builder.addFieldInt8(6, BROKER_KIND, brokerCustodyModel.SelfHosted);
}

static addLiveness(builder:flatbuffers.Builder, LIVENESS:brokerLiveness) {
  builder.addFieldInt8(7, LIVENESS, brokerLiveness.Unknown);
}

static addLastHeartbeatAt(builder:flatbuffers.Builder, LAST_HEARTBEAT_AT:bigint) {
  builder.addFieldInt64(8, LAST_HEARTBEAT_AT, BigInt('0'));
}

static addHeartbeatIntervalMs(builder:flatbuffers.Builder, HEARTBEAT_INTERVAL_MS:bigint) {
  builder.addFieldInt64(9, HEARTBEAT_INTERVAL_MS, BigInt('0'));
}

static addFirstSeenAt(builder:flatbuffers.Builder, FIRST_SEEN_AT:bigint) {
  builder.addFieldInt64(10, FIRST_SEEN_AT, BigInt('0'));
}

static addUpdatedAt(builder:flatbuffers.Builder, UPDATED_AT:bigint) {
  builder.addFieldInt64(11, UPDATED_AT, BigInt('0'));
}

static addSignature(builder:flatbuffers.Builder, SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, SIGNATUREOffset, 0);
}

static createSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endPKB(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // BROKER_ID
  return offset;
}

static finishPKBBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$PKB');
}

static finishSizePrefixedPKBBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$PKB', true);
}

static createPKB(builder:flatbuffers.Builder, BROKER_IDOffset:flatbuffers.Offset, PUBLISHER_EPM_CIDOffset:flatbuffers.Offset, PROVIDER_PEER_IDOffset:flatbuffers.Offset, PROTOCOL_IDOffset:flatbuffers.Offset, MULTIFORMAT_ADDRESSESOffset:flatbuffers.Offset, BROKER_PUBLIC_KEYOffset:flatbuffers.Offset, BROKER_KIND:brokerCustodyModel, LIVENESS:brokerLiveness, LAST_HEARTBEAT_AT:bigint, HEARTBEAT_INTERVAL_MS:bigint, FIRST_SEEN_AT:bigint, UPDATED_AT:bigint, SIGNATUREOffset:flatbuffers.Offset):flatbuffers.Offset {
  PKB.startPKB(builder);
  PKB.addBrokerId(builder, BROKER_IDOffset);
  PKB.addPublisherEpmCid(builder, PUBLISHER_EPM_CIDOffset);
  PKB.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
  PKB.addProtocolId(builder, PROTOCOL_IDOffset);
  PKB.addMultiformatAddresses(builder, MULTIFORMAT_ADDRESSESOffset);
  PKB.addBrokerPublicKey(builder, BROKER_PUBLIC_KEYOffset);
  PKB.addBrokerKind(builder, BROKER_KIND);
  PKB.addLiveness(builder, LIVENESS);
  PKB.addLastHeartbeatAt(builder, LAST_HEARTBEAT_AT);
  PKB.addHeartbeatIntervalMs(builder, HEARTBEAT_INTERVAL_MS);
  PKB.addFirstSeenAt(builder, FIRST_SEEN_AT);
  PKB.addUpdatedAt(builder, UPDATED_AT);
  PKB.addSignature(builder, SIGNATUREOffset);
  return PKB.endPKB(builder);
}

unpack(): PKBT {
  return new PKBT(
    this.BROKER_ID(),
    this.PUBLISHER_EPM_CID(),
    this.PROVIDER_PEER_ID(),
    this.PROTOCOL_ID(),
    this.bb!.createScalarList<string>(this.MULTIFORMAT_ADDRESSES.bind(this), this.multiformatAddressesLength()),
    this.bb!.createScalarList<number>(this.BROKER_PUBLIC_KEY.bind(this), this.brokerPublicKeyLength()),
    this.BROKER_KIND(),
    this.LIVENESS(),
    this.LAST_HEARTBEAT_AT(),
    this.HEARTBEAT_INTERVAL_MS(),
    this.FIRST_SEEN_AT(),
    this.UPDATED_AT(),
    this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength())
  );
}


unpackTo(_o: PKBT): void {
  _o.BROKER_ID = this.BROKER_ID();
  _o.PUBLISHER_EPM_CID = this.PUBLISHER_EPM_CID();
  _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
  _o.PROTOCOL_ID = this.PROTOCOL_ID();
  _o.MULTIFORMAT_ADDRESSES = this.bb!.createScalarList<string>(this.MULTIFORMAT_ADDRESSES.bind(this), this.multiformatAddressesLength());
  _o.BROKER_PUBLIC_KEY = this.bb!.createScalarList<number>(this.BROKER_PUBLIC_KEY.bind(this), this.brokerPublicKeyLength());
  _o.BROKER_KIND = this.BROKER_KIND();
  _o.LIVENESS = this.LIVENESS();
  _o.LAST_HEARTBEAT_AT = this.LAST_HEARTBEAT_AT();
  _o.HEARTBEAT_INTERVAL_MS = this.HEARTBEAT_INTERVAL_MS();
  _o.FIRST_SEEN_AT = this.FIRST_SEEN_AT();
  _o.UPDATED_AT = this.UPDATED_AT();
  _o.SIGNATURE = this.bb!.createScalarList<number>(this.SIGNATURE.bind(this), this.signatureLength());
}
}

export class PKBT implements flatbuffers.IGeneratedObject {
constructor(
  public BROKER_ID: string|Uint8Array|null = null,
  public PUBLISHER_EPM_CID: string|Uint8Array|null = null,
  public PROVIDER_PEER_ID: string|Uint8Array|null = null,
  public PROTOCOL_ID: string|Uint8Array|null = null,
  public MULTIFORMAT_ADDRESSES: (string)[] = [],
  public BROKER_PUBLIC_KEY: (number)[] = [],
  public BROKER_KIND: brokerCustodyModel = brokerCustodyModel.SelfHosted,
  public LIVENESS: brokerLiveness = brokerLiveness.Unknown,
  public LAST_HEARTBEAT_AT: bigint = BigInt('0'),
  public HEARTBEAT_INTERVAL_MS: bigint = BigInt('0'),
  public FIRST_SEEN_AT: bigint = BigInt('0'),
  public UPDATED_AT: bigint = BigInt('0'),
  public SIGNATURE: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const BROKER_ID = (this.BROKER_ID !== null ? builder.createString(this.BROKER_ID!) : 0);
  const PUBLISHER_EPM_CID = (this.PUBLISHER_EPM_CID !== null ? builder.createString(this.PUBLISHER_EPM_CID!) : 0);
  const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID!) : 0);
  const PROTOCOL_ID = (this.PROTOCOL_ID !== null ? builder.createString(this.PROTOCOL_ID!) : 0);
  const MULTIFORMAT_ADDRESSES = PKB.createMultiformatAddressesVector(builder, builder.createObjectOffsetList(this.MULTIFORMAT_ADDRESSES));
  const BROKER_PUBLIC_KEY = PKB.createBrokerPublicKeyVector(builder, this.BROKER_PUBLIC_KEY);
  const SIGNATURE = PKB.createSignatureVector(builder, this.SIGNATURE);

  return PKB.createPKB(builder,
    BROKER_ID,
    PUBLISHER_EPM_CID,
    PROVIDER_PEER_ID,
    PROTOCOL_ID,
    MULTIFORMAT_ADDRESSES,
    BROKER_PUBLIC_KEY,
    this.BROKER_KIND,
    this.LIVENESS,
    this.LAST_HEARTBEAT_AT,
    this.HEARTBEAT_INTERVAL_MS,
    this.FIRST_SEEN_AT,
    this.UPDATED_AT,
    SIGNATURE
  );
}
}
