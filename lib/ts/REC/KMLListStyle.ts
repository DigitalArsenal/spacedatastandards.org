// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { KMLItemIcon, KMLItemIconT } from './KMLItemIcon.js';
import { KMLListItemType } from './KMLListItemType.js';


/**
 * List style
 */
export class KMLListStyle implements flatbuffers.IUnpackableObject<KMLListStyleT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KMLListStyle {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKMLListStyle(bb:flatbuffers.ByteBuffer, obj?:KMLListStyle):KMLListStyle {
  return (obj || new KMLListStyle()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKMLListStyle(bb:flatbuffers.ByteBuffer, obj?:KMLListStyle):KMLListStyle {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KMLListStyle()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * List item type
 */
LIST_ITEM_TYPE():KMLListItemType {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : KMLListItemType.CHECK;
}

/**
 * Background color
 */
BG_COLOR():string|null
BG_COLOR(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BG_COLOR(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Item icons
 */
ITEM_ICONS(index: number, obj?:KMLItemIcon):KMLItemIcon|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new KMLItemIcon()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

itemIconsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Maximum snippet lines
 */
MAX_SNIPPET_LINES():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startKMLListStyle(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addListItemType(builder:flatbuffers.Builder, LIST_ITEM_TYPE:KMLListItemType) {
  builder.addFieldInt8(0, LIST_ITEM_TYPE, KMLListItemType.CHECK);
}

static addBgColor(builder:flatbuffers.Builder, BG_COLOROffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, BG_COLOROffset, 0);
}

static addItemIcons(builder:flatbuffers.Builder, ITEM_ICONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, ITEM_ICONSOffset, 0);
}

static createItemIconsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startItemIconsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMaxSnippetLines(builder:flatbuffers.Builder, MAX_SNIPPET_LINES:number) {
  builder.addFieldInt32(3, MAX_SNIPPET_LINES, 0);
}

static endKMLListStyle(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createKMLListStyle(builder:flatbuffers.Builder, LIST_ITEM_TYPE:KMLListItemType, BG_COLOROffset:flatbuffers.Offset, ITEM_ICONSOffset:flatbuffers.Offset, MAX_SNIPPET_LINES:number):flatbuffers.Offset {
  KMLListStyle.startKMLListStyle(builder);
  KMLListStyle.addListItemType(builder, LIST_ITEM_TYPE);
  KMLListStyle.addBgColor(builder, BG_COLOROffset);
  KMLListStyle.addItemIcons(builder, ITEM_ICONSOffset);
  KMLListStyle.addMaxSnippetLines(builder, MAX_SNIPPET_LINES);
  return KMLListStyle.endKMLListStyle(builder);
}

unpack(): KMLListStyleT {
  return new KMLListStyleT(
    this.LIST_ITEM_TYPE(),
    this.BG_COLOR(),
    this.bb!.createObjList<KMLItemIcon, KMLItemIconT>(this.ITEM_ICONS.bind(this), this.itemIconsLength()),
    this.MAX_SNIPPET_LINES()
  );
}


unpackTo(_o: KMLListStyleT): void {
  _o.LIST_ITEM_TYPE = this.LIST_ITEM_TYPE();
  _o.BG_COLOR = this.BG_COLOR();
  _o.ITEM_ICONS = this.bb!.createObjList<KMLItemIcon, KMLItemIconT>(this.ITEM_ICONS.bind(this), this.itemIconsLength());
  _o.MAX_SNIPPET_LINES = this.MAX_SNIPPET_LINES();
}
}

export class KMLListStyleT implements flatbuffers.IGeneratedObject {
constructor(
  public LIST_ITEM_TYPE: KMLListItemType = KMLListItemType.CHECK,
  public BG_COLOR: string|Uint8Array|null = null,
  public ITEM_ICONS: (KMLItemIconT)[] = [],
  public MAX_SNIPPET_LINES: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const BG_COLOR = (this.BG_COLOR !== null ? builder.createString(this.BG_COLOR!) : 0);
  const ITEM_ICONS = KMLListStyle.createItemIconsVector(builder, builder.createObjectOffsetList(this.ITEM_ICONS));

  return KMLListStyle.createKMLListStyle(builder,
    this.LIST_ITEM_TYPE,
    BG_COLOR,
    ITEM_ICONS,
    this.MAX_SNIPPET_LINES
  );
}
}
