// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { OPPAreas, OPPAreasT } from './OPPAreas.js';
import { OPPAssetRef, OPPAssetRefT } from './OPPAssetRef.js';
import { OPPDimensions, OPPDimensionsT } from './OPPDimensions.js';
import { OPPMass, OPPMassT } from './OPPMass.js';
import { OPPProvenance, OPPProvenanceT } from './OPPProvenance.js';
import { OPPQuantity, OPPQuantityT } from './OPPQuantity.js';
import { OPPRadarCrossSection, OPPRadarCrossSectionT } from './OPPRadarCrossSection.js';
import { OPPSurface, OPPSurfaceT } from './OPPSurface.js';


/**
 * Object Physical Properties - the sourced physical description of one space
 * object and, when one exists, of the exact 3D asset that represents it.
 *
 * Division of labour with the neighbouring standards. $CAT is catalogue
 * identity and orbit, and its RCS, SIZE and MASS scalars are unattributed
 * convenience fields carried from the catalogue publisher. $BUS is the bus
 * DESIGN, shared by every object built on it. $VAM is the asset manifest: the
 * bytes, the licence, the review and the geometry metrics. $PNL is the
 * articulated panel model consumed by solar-radiation-pressure propagation.
 * $OPP is the per-object physical truth in which EVERY value names its own
 * source, epoch and method. $OPP never restates orbital elements and never
 * duplicates bus-design values; a per-object value that came from the bus
 * design is stated here with METHOD INFERRED_FROM_FAMILY so a consumer can see
 * that it is an approximation.
 *
 * Joins. NORAD_CAT_ID and OBJECT_ID join to $CAT. BUS_ID joins to $BUS.
 * ASSET joins to the exact reviewed $VAM variant. ENTITY_ID is the stable
 * non-catalogue key shared with $VAM for objects that have no catalogue entry.
 *
 * Never-invent law, enforced by the shape rather than by prose: a physical
 * quantity exists only as an OPPQuantity and OPPQuantity.PROVENANCE is
 * required, so there is no encoding for a mass, dimension, area or
 * cross-section that does not name where it came from. Unknown values are
 * absent. A consumer that finds a field missing has learned that no admissible
 * source published it.
 */
export class OPP implements flatbuffers.IUnpackableObject<OPPT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):OPP {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsOPP(bb:flatbuffers.ByteBuffer, obj?:OPP):OPP {
  return (obj || new OPP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsOPP(bb:flatbuffers.ByteBuffer, obj?:OPP):OPP {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new OPP()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$OPP');
}

/**
 * Stable identifier for this record.
 */
ID():string
ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * SemVer 2.0.0 record-format version, not a content revision.
 */
VERSION():string|null
VERSION(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
VERSION(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * $CAT.NORAD_CAT_ID verbatim. Zero when the object has no catalogue number.
 */
NORAD_CAT_ID():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * $CAT.OBJECT_ID verbatim: the COSPAR international designator.
 */
OBJECT_ID():string|null
OBJECT_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
OBJECT_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * $CAT.OBJECT_NAME verbatim, for display only. Never a join key.
 */
OBJECT_NAME():string|null
OBJECT_NAME(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
OBJECT_NAME(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Stable non-catalogue entity key, such as an asset catalogue entity path.
 * MUST equal ASSET.ENTITY_ID when ASSET is present.
 */
ENTITY_ID():string|null
ENTITY_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ENTITY_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Join key to the $BUS record describing this object's bus design; holds
 * that record's BUS.ID verbatim. Empty when the bus is unknown or has no
 * $BUS record.
 */
BUS_ID():string|null
BUS_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BUS_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Bus or platform family name verbatim from BUS_FAMILY_PROVENANCE.SOURCE,
 * for objects whose family is known but has no $BUS record.
 */
BUS_FAMILY():string|null
BUS_FAMILY(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
BUS_FAMILY(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Provenance of BUS_FAMILY. Present whenever BUS_FAMILY is nonempty.
 */
BUS_FAMILY_PROVENANCE(obj?:OPPProvenance):OPPProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new OPPProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Manufacturer or prime contractor as stated by MANUFACTURER_PROVENANCE.
 */
MANUFACTURER():string|null
MANUFACTURER(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
MANUFACTURER(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Provenance of MANUFACTURER. Present whenever MANUFACTURER is nonempty.
 */
MANUFACTURER_PROVENANCE(obj?:OPPProvenance):OPPProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? (obj || new OPPProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

MASS(obj?:OPPMass):OPPMass|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? (obj || new OPPMass()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

DIMENSIONS(obj?:OPPDimensions):OPPDimensions|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? (obj || new OPPDimensions()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

AREAS(obj?:OPPAreas):OPPAreas|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? (obj || new OPPAreas()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * One entry per band, polarization and aspect the sources publish. Empty
 * means no source published a radar cross-section for this object.
 */
RADAR_CROSS_SECTIONS(index: number, obj?:OPPRadarCrossSection):OPPRadarCrossSection|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? (obj || new OPPRadarCrossSection()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

radarCrossSectionsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Visual magnitude, UNITS "mag", with the observing conditions in
 * PROVENANCE.NOTES.
 */
VISUAL_MAGNITUDE(obj?:OPPQuantity):OPPQuantity|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? (obj || new OPPQuantity()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Material and surface inventory.
 */
SURFACES(index: number, obj?:OPPSurface):OPPSurface|null {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? (obj || new OPPSurface()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

surfacesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Binding to the exact reviewed 3D asset this record accompanies. Absent for
 * an object with no asset; the physical values remain valid without it.
 */
ASSET(obj?:OPPAssetRef):OPPAssetRef|null {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? (obj || new OPPAssetRef()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Every source consulted while building this record, including sources that
 * yielded no value. Lets a consumer distinguish "not looked for" from
 * "looked for and not published".
 */
SOURCES(index: number, obj?:OPPProvenance):OPPProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? (obj || new OPPProvenance()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

sourcesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ) when
 * the record was created.
 */
CREATED_AT():string|null
CREATED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
CREATED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * RFC 3339 UTC fixed-millisecond timestamp (YYYY-MM-DDTHH:mm:ss.sssZ) when
 * the record was last updated.
 */
UPDATED_AT():string|null
UPDATED_AT(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
UPDATED_AT(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 44);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * CIDv1 containing a multihash of the exact superseded $OPP bytes.
 */
SUPERSEDES_OPP_CID():string|null
SUPERSEDES_OPP_CID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
SUPERSEDES_OPP_CID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 46);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

NOTES():string|null
NOTES(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
NOTES(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 48);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startOPP(builder:flatbuffers.Builder) {
  builder.startObject(23);
}

static addId(builder:flatbuffers.Builder, IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, IDOffset, 0);
}

static addVersion(builder:flatbuffers.Builder, VERSIONOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, VERSIONOffset, 0);
}

static addNoradCatId(builder:flatbuffers.Builder, NORAD_CAT_ID:number) {
  builder.addFieldInt32(2, NORAD_CAT_ID, 0);
}

static addObjectId(builder:flatbuffers.Builder, OBJECT_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, OBJECT_IDOffset, 0);
}

static addObjectName(builder:flatbuffers.Builder, OBJECT_NAMEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, OBJECT_NAMEOffset, 0);
}

static addEntityId(builder:flatbuffers.Builder, ENTITY_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, ENTITY_IDOffset, 0);
}

static addBusId(builder:flatbuffers.Builder, BUS_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, BUS_IDOffset, 0);
}

static addBusFamily(builder:flatbuffers.Builder, BUS_FAMILYOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, BUS_FAMILYOffset, 0);
}

static addBusFamilyProvenance(builder:flatbuffers.Builder, BUS_FAMILY_PROVENANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, BUS_FAMILY_PROVENANCEOffset, 0);
}

static addManufacturer(builder:flatbuffers.Builder, MANUFACTUREROffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, MANUFACTUREROffset, 0);
}

static addManufacturerProvenance(builder:flatbuffers.Builder, MANUFACTURER_PROVENANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, MANUFACTURER_PROVENANCEOffset, 0);
}

static addMass(builder:flatbuffers.Builder, MASSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, MASSOffset, 0);
}

static addDimensions(builder:flatbuffers.Builder, DIMENSIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, DIMENSIONSOffset, 0);
}

static addAreas(builder:flatbuffers.Builder, AREASOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, AREASOffset, 0);
}

static addRadarCrossSections(builder:flatbuffers.Builder, RADAR_CROSS_SECTIONSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, RADAR_CROSS_SECTIONSOffset, 0);
}

static createRadarCrossSectionsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startRadarCrossSectionsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addVisualMagnitude(builder:flatbuffers.Builder, VISUAL_MAGNITUDEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(15, VISUAL_MAGNITUDEOffset, 0);
}

static addSurfaces(builder:flatbuffers.Builder, SURFACESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(16, SURFACESOffset, 0);
}

static createSurfacesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSurfacesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addAsset(builder:flatbuffers.Builder, ASSETOffset:flatbuffers.Offset) {
  builder.addFieldOffset(17, ASSETOffset, 0);
}

static addSources(builder:flatbuffers.Builder, SOURCESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(18, SOURCESOffset, 0);
}

static createSourcesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSourcesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCreatedAt(builder:flatbuffers.Builder, CREATED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(19, CREATED_ATOffset, 0);
}

static addUpdatedAt(builder:flatbuffers.Builder, UPDATED_ATOffset:flatbuffers.Offset) {
  builder.addFieldOffset(20, UPDATED_ATOffset, 0);
}

static addSupersedesOppCid(builder:flatbuffers.Builder, SUPERSEDES_OPP_CIDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(21, SUPERSEDES_OPP_CIDOffset, 0);
}

static addNotes(builder:flatbuffers.Builder, NOTESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(22, NOTESOffset, 0);
}

static endOPP(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // ID
  return offset;
}

static finishOPPBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$OPP');
}

static finishSizePrefixedOPPBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$OPP', true);
}


unpack(): OPPT {
  return new OPPT(
    this.ID(),
    this.VERSION(),
    this.NORAD_CAT_ID(),
    this.OBJECT_ID(),
    this.OBJECT_NAME(),
    this.ENTITY_ID(),
    this.BUS_ID(),
    this.BUS_FAMILY(),
    (this.BUS_FAMILY_PROVENANCE() !== null ? this.BUS_FAMILY_PROVENANCE()!.unpack() : null),
    this.MANUFACTURER(),
    (this.MANUFACTURER_PROVENANCE() !== null ? this.MANUFACTURER_PROVENANCE()!.unpack() : null),
    (this.MASS() !== null ? this.MASS()!.unpack() : null),
    (this.DIMENSIONS() !== null ? this.DIMENSIONS()!.unpack() : null),
    (this.AREAS() !== null ? this.AREAS()!.unpack() : null),
    this.bb!.createObjList<OPPRadarCrossSection, OPPRadarCrossSectionT>(this.RADAR_CROSS_SECTIONS.bind(this), this.radarCrossSectionsLength()),
    (this.VISUAL_MAGNITUDE() !== null ? this.VISUAL_MAGNITUDE()!.unpack() : null),
    this.bb!.createObjList<OPPSurface, OPPSurfaceT>(this.SURFACES.bind(this), this.surfacesLength()),
    (this.ASSET() !== null ? this.ASSET()!.unpack() : null),
    this.bb!.createObjList<OPPProvenance, OPPProvenanceT>(this.SOURCES.bind(this), this.sourcesLength()),
    this.CREATED_AT(),
    this.UPDATED_AT(),
    this.SUPERSEDES_OPP_CID(),
    this.NOTES()
  );
}


unpackTo(_o: OPPT): void {
  _o.ID = this.ID();
  _o.VERSION = this.VERSION();
  _o.NORAD_CAT_ID = this.NORAD_CAT_ID();
  _o.OBJECT_ID = this.OBJECT_ID();
  _o.OBJECT_NAME = this.OBJECT_NAME();
  _o.ENTITY_ID = this.ENTITY_ID();
  _o.BUS_ID = this.BUS_ID();
  _o.BUS_FAMILY = this.BUS_FAMILY();
  _o.BUS_FAMILY_PROVENANCE = (this.BUS_FAMILY_PROVENANCE() !== null ? this.BUS_FAMILY_PROVENANCE()!.unpack() : null);
  _o.MANUFACTURER = this.MANUFACTURER();
  _o.MANUFACTURER_PROVENANCE = (this.MANUFACTURER_PROVENANCE() !== null ? this.MANUFACTURER_PROVENANCE()!.unpack() : null);
  _o.MASS = (this.MASS() !== null ? this.MASS()!.unpack() : null);
  _o.DIMENSIONS = (this.DIMENSIONS() !== null ? this.DIMENSIONS()!.unpack() : null);
  _o.AREAS = (this.AREAS() !== null ? this.AREAS()!.unpack() : null);
  _o.RADAR_CROSS_SECTIONS = this.bb!.createObjList<OPPRadarCrossSection, OPPRadarCrossSectionT>(this.RADAR_CROSS_SECTIONS.bind(this), this.radarCrossSectionsLength());
  _o.VISUAL_MAGNITUDE = (this.VISUAL_MAGNITUDE() !== null ? this.VISUAL_MAGNITUDE()!.unpack() : null);
  _o.SURFACES = this.bb!.createObjList<OPPSurface, OPPSurfaceT>(this.SURFACES.bind(this), this.surfacesLength());
  _o.ASSET = (this.ASSET() !== null ? this.ASSET()!.unpack() : null);
  _o.SOURCES = this.bb!.createObjList<OPPProvenance, OPPProvenanceT>(this.SOURCES.bind(this), this.sourcesLength());
  _o.CREATED_AT = this.CREATED_AT();
  _o.UPDATED_AT = this.UPDATED_AT();
  _o.SUPERSEDES_OPP_CID = this.SUPERSEDES_OPP_CID();
  _o.NOTES = this.NOTES();
}
}

export class OPPT implements flatbuffers.IGeneratedObject {
constructor(
  public ID: string|Uint8Array|null = null,
  public VERSION: string|Uint8Array|null = null,
  public NORAD_CAT_ID: number = 0,
  public OBJECT_ID: string|Uint8Array|null = null,
  public OBJECT_NAME: string|Uint8Array|null = null,
  public ENTITY_ID: string|Uint8Array|null = null,
  public BUS_ID: string|Uint8Array|null = null,
  public BUS_FAMILY: string|Uint8Array|null = null,
  public BUS_FAMILY_PROVENANCE: OPPProvenanceT|null = null,
  public MANUFACTURER: string|Uint8Array|null = null,
  public MANUFACTURER_PROVENANCE: OPPProvenanceT|null = null,
  public MASS: OPPMassT|null = null,
  public DIMENSIONS: OPPDimensionsT|null = null,
  public AREAS: OPPAreasT|null = null,
  public RADAR_CROSS_SECTIONS: (OPPRadarCrossSectionT)[] = [],
  public VISUAL_MAGNITUDE: OPPQuantityT|null = null,
  public SURFACES: (OPPSurfaceT)[] = [],
  public ASSET: OPPAssetRefT|null = null,
  public SOURCES: (OPPProvenanceT)[] = [],
  public CREATED_AT: string|Uint8Array|null = null,
  public UPDATED_AT: string|Uint8Array|null = null,
  public SUPERSEDES_OPP_CID: string|Uint8Array|null = null,
  public NOTES: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ID = (this.ID !== null ? builder.createString(this.ID!) : 0);
  const VERSION = (this.VERSION !== null ? builder.createString(this.VERSION!) : 0);
  const OBJECT_ID = (this.OBJECT_ID !== null ? builder.createString(this.OBJECT_ID!) : 0);
  const OBJECT_NAME = (this.OBJECT_NAME !== null ? builder.createString(this.OBJECT_NAME!) : 0);
  const ENTITY_ID = (this.ENTITY_ID !== null ? builder.createString(this.ENTITY_ID!) : 0);
  const BUS_ID = (this.BUS_ID !== null ? builder.createString(this.BUS_ID!) : 0);
  const BUS_FAMILY = (this.BUS_FAMILY !== null ? builder.createString(this.BUS_FAMILY!) : 0);
  const BUS_FAMILY_PROVENANCE = (this.BUS_FAMILY_PROVENANCE !== null ? this.BUS_FAMILY_PROVENANCE!.pack(builder) : 0);
  const MANUFACTURER = (this.MANUFACTURER !== null ? builder.createString(this.MANUFACTURER!) : 0);
  const MANUFACTURER_PROVENANCE = (this.MANUFACTURER_PROVENANCE !== null ? this.MANUFACTURER_PROVENANCE!.pack(builder) : 0);
  const MASS = (this.MASS !== null ? this.MASS!.pack(builder) : 0);
  const DIMENSIONS = (this.DIMENSIONS !== null ? this.DIMENSIONS!.pack(builder) : 0);
  const AREAS = (this.AREAS !== null ? this.AREAS!.pack(builder) : 0);
  const RADAR_CROSS_SECTIONS = OPP.createRadarCrossSectionsVector(builder, builder.createObjectOffsetList(this.RADAR_CROSS_SECTIONS));
  const VISUAL_MAGNITUDE = (this.VISUAL_MAGNITUDE !== null ? this.VISUAL_MAGNITUDE!.pack(builder) : 0);
  const SURFACES = OPP.createSurfacesVector(builder, builder.createObjectOffsetList(this.SURFACES));
  const ASSET = (this.ASSET !== null ? this.ASSET!.pack(builder) : 0);
  const SOURCES = OPP.createSourcesVector(builder, builder.createObjectOffsetList(this.SOURCES));
  const CREATED_AT = (this.CREATED_AT !== null ? builder.createString(this.CREATED_AT!) : 0);
  const UPDATED_AT = (this.UPDATED_AT !== null ? builder.createString(this.UPDATED_AT!) : 0);
  const SUPERSEDES_OPP_CID = (this.SUPERSEDES_OPP_CID !== null ? builder.createString(this.SUPERSEDES_OPP_CID!) : 0);
  const NOTES = (this.NOTES !== null ? builder.createString(this.NOTES!) : 0);

  OPP.startOPP(builder);
  OPP.addId(builder, ID);
  OPP.addVersion(builder, VERSION);
  OPP.addNoradCatId(builder, this.NORAD_CAT_ID);
  OPP.addObjectId(builder, OBJECT_ID);
  OPP.addObjectName(builder, OBJECT_NAME);
  OPP.addEntityId(builder, ENTITY_ID);
  OPP.addBusId(builder, BUS_ID);
  OPP.addBusFamily(builder, BUS_FAMILY);
  OPP.addBusFamilyProvenance(builder, BUS_FAMILY_PROVENANCE);
  OPP.addManufacturer(builder, MANUFACTURER);
  OPP.addManufacturerProvenance(builder, MANUFACTURER_PROVENANCE);
  OPP.addMass(builder, MASS);
  OPP.addDimensions(builder, DIMENSIONS);
  OPP.addAreas(builder, AREAS);
  OPP.addRadarCrossSections(builder, RADAR_CROSS_SECTIONS);
  OPP.addVisualMagnitude(builder, VISUAL_MAGNITUDE);
  OPP.addSurfaces(builder, SURFACES);
  OPP.addAsset(builder, ASSET);
  OPP.addSources(builder, SOURCES);
  OPP.addCreatedAt(builder, CREATED_AT);
  OPP.addUpdatedAt(builder, UPDATED_AT);
  OPP.addSupersedesOppCid(builder, SUPERSEDES_OPP_CID);
  OPP.addNotes(builder, NOTES);

  return OPP.endOPP(builder);
}
}
