// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { OPPProvenance, OPPProvenanceT } from './OPPProvenance.js';


/**
 * One scalar physical quantity, its unit and its own provenance.
 *
 * PROVENANCE is required: an OPPQuantity that exists names where it came from.
 * Presence of the table is the signal that a value exists, so VALUE zero is a
 * real zero (0 dBsm is 1 m2) and an unknown quantity omits the whole table.
 */
export class OPPQuantity implements flatbuffers.IUnpackableObject<OPPQuantityT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):OPPQuantity {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsOPPQuantity(bb:flatbuffers.ByteBuffer, obj?:OPPQuantity):OPPQuantity {
  return (obj || new OPPQuantity()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsOPPQuantity(bb:flatbuffers.ByteBuffer, obj?:OPPQuantity):OPPQuantity {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new OPPQuantity()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

VALUE():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Unit symbol verbatim, never silently converted: "kg", "m", "m2", "m2/kg",
 * "kg/m2", "dBsm", "W", "mag". A converted value states the unit it was
 * converted to and records the conversion in PROVENANCE.NOTES with
 * PROVENANCE.METHOD DERIVED.
 */
UNITS():string
UNITS(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
UNITS(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * 1-sigma uncertainty in UNITS. Zero means the source stated no uncertainty;
 * a genuine stated sigma is never exactly zero.
 */
SIGMA():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

PROVENANCE(obj?:OPPProvenance):OPPProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new OPPProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startOPPQuantity(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addValue(builder:flatbuffers.Builder, VALUE:number) {
  builder.addFieldFloat64(0, VALUE, 0.0);
}

static addUnits(builder:flatbuffers.Builder, UNITSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, UNITSOffset, 0);
}

static addSigma(builder:flatbuffers.Builder, SIGMA:number) {
  builder.addFieldFloat64(2, SIGMA, 0.0);
}

static addProvenance(builder:flatbuffers.Builder, PROVENANCEOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, PROVENANCEOffset, 0);
}

static endOPPQuantity(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // UNITS
  builder.requiredField(offset, 10) // PROVENANCE
  return offset;
}


unpack(): OPPQuantityT {
  return new OPPQuantityT(
    this.VALUE(),
    this.UNITS(),
    this.SIGMA(),
    (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null)
  );
}


unpackTo(_o: OPPQuantityT): void {
  _o.VALUE = this.VALUE();
  _o.UNITS = this.UNITS();
  _o.SIGMA = this.SIGMA();
  _o.PROVENANCE = (this.PROVENANCE() !== null ? this.PROVENANCE()!.unpack() : null);
}
}

export class OPPQuantityT implements flatbuffers.IGeneratedObject {
constructor(
  public VALUE: number = 0.0,
  public UNITS: string|Uint8Array|null = null,
  public SIGMA: number = 0.0,
  public PROVENANCE: OPPProvenanceT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const UNITS = (this.UNITS !== null ? builder.createString(this.UNITS!) : 0);
  const PROVENANCE = (this.PROVENANCE !== null ? this.PROVENANCE!.pack(builder) : 0);

  OPPQuantity.startOPPQuantity(builder);
  OPPQuantity.addValue(builder, this.VALUE);
  OPPQuantity.addUnits(builder, UNITS);
  OPPQuantity.addSigma(builder, this.SIGMA);
  OPPQuantity.addProvenance(builder, PROVENANCE);

  return OPPQuantity.endOPPQuantity(builder);
}
}
