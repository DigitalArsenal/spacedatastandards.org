// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Trust Edge Record - Durable directed trust assertion for an SDN trust graph.
 *
 * A TRE records one directed truster -> trustee assertion. The current trust
 * graph is a projection over the latest TRE records by EDGE_ID, with deleted
 * records acting as tombstones. Implementations must still validate the
 * projected graph for acyclicity before accepting it.
 */
export class TRE implements flatbuffers.IUnpackableObject<TRET> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TRE {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTRE(bb:flatbuffers.ByteBuffer, obj?:TRE):TRE {
  return (obj || new TRE()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTRE(bb:flatbuffers.ByteBuffer, obj?:TRE):TRE {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TRE()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('$TRE');
}

/**
 * Stable edge identifier, normally "truster->trustee".
 */
EDGE_ID():string|null
EDGE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EDGE_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Trusting node identifier.
 */
TRUSTER_ID():string
TRUSTER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
TRUSTER_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Trusted node identifier.
 */
TRUSTEE_ID():string
TRUSTEE_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array
TRUSTEE_ID(optionalEncoding?:any):string|Uint8Array {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
}

/**
 * Truster-assigned edge weight in the range [0, 1].
 */
WEIGHT():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

/**
 * Unix timestamp in milliseconds when this edge was last updated.
 */
UPDATED_AT():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

/**
 * True when this record tombstones the edge from the current graph.
 */
DELETED():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Peer ID of the node/provider that issued this trust edge record.
 */
PROVIDER_PEER_ID():string|null
PROVIDER_PEER_ID(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
PROVIDER_PEER_ID(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Provider signature over the trust edge record.
 */
PROVIDER_SIGNATURE(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

providerSignatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

providerSignatureArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startTRE(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addEdgeId(builder:flatbuffers.Builder, EDGE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, EDGE_IDOffset, 0);
}

static addTrusterId(builder:flatbuffers.Builder, TRUSTER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, TRUSTER_IDOffset, 0);
}

static addTrusteeId(builder:flatbuffers.Builder, TRUSTEE_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, TRUSTEE_IDOffset, 0);
}

static addWeight(builder:flatbuffers.Builder, WEIGHT:number) {
  builder.addFieldFloat64(3, WEIGHT, 0.0);
}

static addUpdatedAt(builder:flatbuffers.Builder, UPDATED_AT:bigint) {
  builder.addFieldInt64(4, UPDATED_AT, BigInt('0'));
}

static addDeleted(builder:flatbuffers.Builder, DELETED:boolean) {
  builder.addFieldInt8(5, +DELETED, +false);
}

static addProviderPeerId(builder:flatbuffers.Builder, PROVIDER_PEER_IDOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, PROVIDER_PEER_IDOffset, 0);
}

static addProviderSignature(builder:flatbuffers.Builder, PROVIDER_SIGNATUREOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, PROVIDER_SIGNATUREOffset, 0);
}

static createProviderSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startProviderSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endTRE(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // TRUSTER_ID
  builder.requiredField(offset, 8) // TRUSTEE_ID
  return offset;
}

static finishTREBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TRE');
}

static finishSizePrefixedTREBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, '$TRE', true);
}

static createTRE(builder:flatbuffers.Builder, EDGE_IDOffset:flatbuffers.Offset, TRUSTER_IDOffset:flatbuffers.Offset, TRUSTEE_IDOffset:flatbuffers.Offset, WEIGHT:number, UPDATED_AT:bigint, DELETED:boolean, PROVIDER_PEER_IDOffset:flatbuffers.Offset, PROVIDER_SIGNATUREOffset:flatbuffers.Offset):flatbuffers.Offset {
  TRE.startTRE(builder);
  TRE.addEdgeId(builder, EDGE_IDOffset);
  TRE.addTrusterId(builder, TRUSTER_IDOffset);
  TRE.addTrusteeId(builder, TRUSTEE_IDOffset);
  TRE.addWeight(builder, WEIGHT);
  TRE.addUpdatedAt(builder, UPDATED_AT);
  TRE.addDeleted(builder, DELETED);
  TRE.addProviderPeerId(builder, PROVIDER_PEER_IDOffset);
  TRE.addProviderSignature(builder, PROVIDER_SIGNATUREOffset);
  return TRE.endTRE(builder);
}

unpack(): TRET {
  return new TRET(
    this.EDGE_ID(),
    this.TRUSTER_ID(),
    this.TRUSTEE_ID(),
    this.WEIGHT(),
    this.UPDATED_AT(),
    this.DELETED(),
    this.PROVIDER_PEER_ID(),
    this.bb!.createScalarList<number>(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength())
  );
}


unpackTo(_o: TRET): void {
  _o.EDGE_ID = this.EDGE_ID();
  _o.TRUSTER_ID = this.TRUSTER_ID();
  _o.TRUSTEE_ID = this.TRUSTEE_ID();
  _o.WEIGHT = this.WEIGHT();
  _o.UPDATED_AT = this.UPDATED_AT();
  _o.DELETED = this.DELETED();
  _o.PROVIDER_PEER_ID = this.PROVIDER_PEER_ID();
  _o.PROVIDER_SIGNATURE = this.bb!.createScalarList<number>(this.PROVIDER_SIGNATURE.bind(this), this.providerSignatureLength());
}
}

export class TRET implements flatbuffers.IGeneratedObject {
constructor(
  public EDGE_ID: string|Uint8Array|null = null,
  public TRUSTER_ID: string|Uint8Array|null = null,
  public TRUSTEE_ID: string|Uint8Array|null = null,
  public WEIGHT: number = 0.0,
  public UPDATED_AT: bigint = BigInt('0'),
  public DELETED: boolean = false,
  public PROVIDER_PEER_ID: string|Uint8Array|null = null,
  public PROVIDER_SIGNATURE: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const EDGE_ID = (this.EDGE_ID !== null ? builder.createString(this.EDGE_ID!) : 0);
  const TRUSTER_ID = (this.TRUSTER_ID !== null ? builder.createString(this.TRUSTER_ID!) : 0);
  const TRUSTEE_ID = (this.TRUSTEE_ID !== null ? builder.createString(this.TRUSTEE_ID!) : 0);
  const PROVIDER_PEER_ID = (this.PROVIDER_PEER_ID !== null ? builder.createString(this.PROVIDER_PEER_ID!) : 0);
  const PROVIDER_SIGNATURE = TRE.createProviderSignatureVector(builder, this.PROVIDER_SIGNATURE);

  return TRE.createTRE(builder,
    EDGE_ID,
    TRUSTER_ID,
    TRUSTEE_ID,
    this.WEIGHT,
    this.UPDATED_AT,
    this.DELETED,
    PROVIDER_PEER_ID,
    PROVIDER_SIGNATURE
  );
}
}
