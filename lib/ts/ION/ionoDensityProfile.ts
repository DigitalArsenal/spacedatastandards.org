// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Ionospheric Electron Density Profile
 */
export class ionoDensityProfile implements flatbuffers.IUnpackableObject<ionoDensityProfileT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ionoDensityProfile {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsionoDensityProfile(bb:flatbuffers.ByteBuffer, obj?:ionoDensityProfile):ionoDensityProfile {
  return (obj || new ionoDensityProfile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsionoDensityProfile(bb:flatbuffers.ByteBuffer, obj?:ionoDensityProfile):ionoDensityProfile {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ionoDensityProfile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Observation epoch (ISO 8601)
 */
EPOCH():string|null
EPOCH(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
EPOCH(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Altitudes in km
 */
ALTITUDES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

altitudesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

altitudesArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Electron densities at each altitude in electrons/m^3
 */
DENSITIES(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : 0;
}

densitiesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

densitiesArray():Float64Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Float64Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startionoDensityProfile(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addEpoch(builder:flatbuffers.Builder, EPOCHOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, EPOCHOffset, 0);
}

static addAltitudes(builder:flatbuffers.Builder, ALTITUDESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ALTITUDESOffset, 0);
}

static createAltitudesVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createAltitudesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createAltitudesVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startAltitudesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addDensities(builder:flatbuffers.Builder, DENSITIESOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, DENSITIESOffset, 0);
}

static createDensitiesVector(builder:flatbuffers.Builder, data:number[]|Float64Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createDensitiesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createDensitiesVector(builder:flatbuffers.Builder, data:number[]|Float64Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat64(data[i]!);
  }
  return builder.endVector();
}

static startDensitiesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static endionoDensityProfile(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createionoDensityProfile(builder:flatbuffers.Builder, EPOCHOffset:flatbuffers.Offset, ALTITUDESOffset:flatbuffers.Offset, DENSITIESOffset:flatbuffers.Offset):flatbuffers.Offset {
  ionoDensityProfile.startionoDensityProfile(builder);
  ionoDensityProfile.addEpoch(builder, EPOCHOffset);
  ionoDensityProfile.addAltitudes(builder, ALTITUDESOffset);
  ionoDensityProfile.addDensities(builder, DENSITIESOffset);
  return ionoDensityProfile.endionoDensityProfile(builder);
}

unpack(): ionoDensityProfileT {
  return new ionoDensityProfileT(
    this.EPOCH(),
    this.bb!.createScalarList<number>(this.ALTITUDES.bind(this), this.altitudesLength()),
    this.bb!.createScalarList<number>(this.DENSITIES.bind(this), this.densitiesLength())
  );
}


unpackTo(_o: ionoDensityProfileT): void {
  _o.EPOCH = this.EPOCH();
  _o.ALTITUDES = this.bb!.createScalarList<number>(this.ALTITUDES.bind(this), this.altitudesLength());
  _o.DENSITIES = this.bb!.createScalarList<number>(this.DENSITIES.bind(this), this.densitiesLength());
}
}

export class ionoDensityProfileT implements flatbuffers.IGeneratedObject {
constructor(
  public EPOCH: string|Uint8Array|null = null,
  public ALTITUDES: (number)[] = [],
  public DENSITIES: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const EPOCH = (this.EPOCH !== null ? builder.createString(this.EPOCH!) : 0);
  const ALTITUDES = ionoDensityProfile.createAltitudesVector(builder, this.ALTITUDES);
  const DENSITIES = ionoDensityProfile.createDensitiesVector(builder, this.DENSITIES);

  return ionoDensityProfile.createionoDensityProfile(builder,
    EPOCH,
    ALTITUDES,
    DENSITIES
  );
}
}
