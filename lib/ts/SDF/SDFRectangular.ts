// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Rectangular (pyramidal) SDF parameters.
 */
export class SDFRectangular implements flatbuffers.IUnpackableObject<SDFRectangularT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SDFRectangular {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSDFRectangular(bb:flatbuffers.ByteBuffer, obj?:SDFRectangular):SDFRectangular {
  return (obj || new SDFRectangular()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSDFRectangular(bb:flatbuffers.ByteBuffer, obj?:SDFRectangular):SDFRectangular {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SDFRectangular()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

X_HALF_ANGLE():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

Y_HALF_ANGLE():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MIN_RANGE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

MAX_RANGE():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

static startSDFRectangular(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addXHalfAngle(builder:flatbuffers.Builder, X_HALF_ANGLE:number) {
  builder.addFieldFloat64(0, X_HALF_ANGLE, 0.0);
}

static addYHalfAngle(builder:flatbuffers.Builder, Y_HALF_ANGLE:number) {
  builder.addFieldFloat64(1, Y_HALF_ANGLE, 0.0);
}

static addMinRange(builder:flatbuffers.Builder, MIN_RANGE:number) {
  builder.addFieldFloat64(2, MIN_RANGE, 0.0);
}

static addMaxRange(builder:flatbuffers.Builder, MAX_RANGE:number) {
  builder.addFieldFloat64(3, MAX_RANGE, 0.0);
}

static endSDFRectangular(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSDFRectangular(builder:flatbuffers.Builder, X_HALF_ANGLE:number, Y_HALF_ANGLE:number, MIN_RANGE:number, MAX_RANGE:number):flatbuffers.Offset {
  SDFRectangular.startSDFRectangular(builder);
  SDFRectangular.addXHalfAngle(builder, X_HALF_ANGLE);
  SDFRectangular.addYHalfAngle(builder, Y_HALF_ANGLE);
  SDFRectangular.addMinRange(builder, MIN_RANGE);
  SDFRectangular.addMaxRange(builder, MAX_RANGE);
  return SDFRectangular.endSDFRectangular(builder);
}

unpack(): SDFRectangularT {
  return new SDFRectangularT(
    this.X_HALF_ANGLE(),
    this.Y_HALF_ANGLE(),
    this.MIN_RANGE(),
    this.MAX_RANGE()
  );
}


unpackTo(_o: SDFRectangularT): void {
  _o.X_HALF_ANGLE = this.X_HALF_ANGLE();
  _o.Y_HALF_ANGLE = this.Y_HALF_ANGLE();
  _o.MIN_RANGE = this.MIN_RANGE();
  _o.MAX_RANGE = this.MAX_RANGE();
}
}

export class SDFRectangularT implements flatbuffers.IGeneratedObject {
constructor(
  public X_HALF_ANGLE: number = 0.0,
  public Y_HALF_ANGLE: number = 0.0,
  public MIN_RANGE: number = 0.0,
  public MAX_RANGE: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return SDFRectangular.createSDFRectangular(builder,
    this.X_HALF_ANGLE,
    this.Y_HALF_ANGLE,
    this.MIN_RANGE,
    this.MAX_RANGE
  );
}
}
