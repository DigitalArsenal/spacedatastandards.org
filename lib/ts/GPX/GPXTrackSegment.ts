// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GPXWaypoint, GPXWaypointT } from './GPXWaypoint.js';


/**
 * Track segment (continuous span of track points)
 */
export class GPXTrackSegment implements flatbuffers.IUnpackableObject<GPXTrackSegmentT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GPXTrackSegment {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGPXTrackSegment(bb:flatbuffers.ByteBuffer, obj?:GPXTrackSegment):GPXTrackSegment {
  return (obj || new GPXTrackSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGPXTrackSegment(bb:flatbuffers.ByteBuffer, obj?:GPXTrackSegment):GPXTrackSegment {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GPXTrackSegment()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Ordered track points in this segment
 */
POINTS(index: number, obj?:GPXWaypoint):GPXWaypoint|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new GPXWaypoint()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

pointsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startGPXTrackSegment(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addPoints(builder:flatbuffers.Builder, POINTSOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, POINTSOffset, 0);
}

static createPointsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPointsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endGPXTrackSegment(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGPXTrackSegment(builder:flatbuffers.Builder, POINTSOffset:flatbuffers.Offset):flatbuffers.Offset {
  GPXTrackSegment.startGPXTrackSegment(builder);
  GPXTrackSegment.addPoints(builder, POINTSOffset);
  return GPXTrackSegment.endGPXTrackSegment(builder);
}

unpack(): GPXTrackSegmentT {
  return new GPXTrackSegmentT(
    this.bb!.createObjList<GPXWaypoint, GPXWaypointT>(this.POINTS.bind(this), this.pointsLength())
  );
}


unpackTo(_o: GPXTrackSegmentT): void {
  _o.POINTS = this.bb!.createObjList<GPXWaypoint, GPXWaypointT>(this.POINTS.bind(this), this.pointsLength());
}
}

export class GPXTrackSegmentT implements flatbuffers.IGeneratedObject {
constructor(
  public POINTS: (GPXWaypointT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const POINTS = GPXTrackSegment.createPointsVector(builder, builder.createObjectOffsetList(this.POINTS));

  return GPXTrackSegment.createGPXTrackSegment(builder,
    POINTS
  );
}
}
