// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * RGBA color
 */
export class CZMColor implements flatbuffers.IUnpackableObject<CZMColorT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CZMColor {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCZMColor(bb:flatbuffers.ByteBuffer, obj?:CZMColor):CZMColor {
  return (obj || new CZMColor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCZMColor(bb:flatbuffers.ByteBuffer, obj?:CZMColor):CZMColor {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CZMColor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Red component (0-255)
 */
RED():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Green component (0-255)
 */
GREEN():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Blue component (0-255)
 */
BLUE():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Alpha component (0-255)
 */
ALPHA():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startCZMColor(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addRed(builder:flatbuffers.Builder, RED:number) {
  builder.addFieldInt8(0, RED, 0);
}

static addGreen(builder:flatbuffers.Builder, GREEN:number) {
  builder.addFieldInt8(1, GREEN, 0);
}

static addBlue(builder:flatbuffers.Builder, BLUE:number) {
  builder.addFieldInt8(2, BLUE, 0);
}

static addAlpha(builder:flatbuffers.Builder, ALPHA:number) {
  builder.addFieldInt8(3, ALPHA, 0);
}

static endCZMColor(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createCZMColor(builder:flatbuffers.Builder, RED:number, GREEN:number, BLUE:number, ALPHA:number):flatbuffers.Offset {
  CZMColor.startCZMColor(builder);
  CZMColor.addRed(builder, RED);
  CZMColor.addGreen(builder, GREEN);
  CZMColor.addBlue(builder, BLUE);
  CZMColor.addAlpha(builder, ALPHA);
  return CZMColor.endCZMColor(builder);
}

unpack(): CZMColorT {
  return new CZMColorT(
    this.RED(),
    this.GREEN(),
    this.BLUE(),
    this.ALPHA()
  );
}


unpackTo(_o: CZMColorT): void {
  _o.RED = this.RED();
  _o.GREEN = this.GREEN();
  _o.BLUE = this.BLUE();
  _o.ALPHA = this.ALPHA();
}
}

export class CZMColorT implements flatbuffers.IGeneratedObject {
constructor(
  public RED: number = 0,
  public GREEN: number = 0,
  public BLUE: number = 0,
  public ALPHA: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return CZMColor.createCZMColor(builder,
    this.RED,
    this.GREEN,
    this.BLUE,
    this.ALPHA
  );
}
}
