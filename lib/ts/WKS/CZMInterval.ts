// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Time interval for time-dynamic properties
 */
export class CZMInterval implements flatbuffers.IUnpackableObject<CZMIntervalT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CZMInterval {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCZMInterval(bb:flatbuffers.ByteBuffer, obj?:CZMInterval):CZMInterval {
  return (obj || new CZMInterval()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCZMInterval(bb:flatbuffers.ByteBuffer, obj?:CZMInterval):CZMInterval {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CZMInterval()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * ISO 8601 interval string (e.g. "2012-03-15T10:00:00Z/2012-03-16T10:00:00Z")
 */
INTERVAL():string|null
INTERVAL(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
INTERVAL(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startCZMInterval(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addInterval(builder:flatbuffers.Builder, INTERVALOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, INTERVALOffset, 0);
}

static endCZMInterval(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createCZMInterval(builder:flatbuffers.Builder, INTERVALOffset:flatbuffers.Offset):flatbuffers.Offset {
  CZMInterval.startCZMInterval(builder);
  CZMInterval.addInterval(builder, INTERVALOffset);
  return CZMInterval.endCZMInterval(builder);
}

unpack(): CZMIntervalT {
  return new CZMIntervalT(
    this.INTERVAL()
  );
}


unpackTo(_o: CZMIntervalT): void {
  _o.INTERVAL = this.INTERVAL();
}
}

export class CZMIntervalT implements flatbuffers.IGeneratedObject {
constructor(
  public INTERVAL: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const INTERVAL = (this.INTERVAL !== null ? builder.createString(this.INTERVAL!) : 0);

  return CZMInterval.createCZMInterval(builder,
    INTERVAL
  );
}
}
