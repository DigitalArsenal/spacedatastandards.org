# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# /// Array to support sparse data collections.
class IonoObservationDatum_Ingest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IonoObservationDatum_Ingest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIonoObservationDatum_Ingest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def IonoObservationDatum_IngestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x49\x4F\x4E\x4F", size_prefixed=size_prefixed)

    # IonoObservationDatum_Ingest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Notes for the datum with details of what the data is, units, etc.
    # Example: /// Example: NOTES
    # Constraints: Minimum length = 0, Maximum length = 512
    # IonoObservationDatum_Ingest
    def Notes(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Array to support sparse data collections.
    # Example: /// Example: [1.1, 2.1, 3.1]
    # Constraints: No constraints specified.
    # IonoObservationDatum_Ingest
    def Data(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # IonoObservationDatum_Ingest
    def DataLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # IonoObservationDatum_Ingest
    def DataIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def IonoObservationDatum_IngestStart(builder):
    builder.StartObject(2)

def Start(builder):
    IonoObservationDatum_IngestStart(builder)

def IonoObservationDatum_IngestAddNotes(builder, notes):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(notes), 0)

def AddNotes(builder, notes):
    IonoObservationDatum_IngestAddNotes(builder, notes)

def IonoObservationDatum_IngestAddData(builder, data):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(data), 0)

def AddData(builder, data):
    IonoObservationDatum_IngestAddData(builder, data)

def IonoObservationDatum_IngestStartDataVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDataVector(builder, numElems):
    return IonoObservationDatum_IngestStartDataVector(builder, numElems)

def IonoObservationDatum_IngestEnd(builder):
    return builder.EndObject()

def End(builder):
    return IonoObservationDatum_IngestEnd(builder)

try:
    from typing import List
except:
    pass

class IonoObservationDatum_IngestT(object):

    # IonoObservationDatum_IngestT
    def __init__(self):
        self.notes = None  # type: str
        self.data = None  # type: List[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ionoObservationDatumIngest = IonoObservationDatum_Ingest()
        ionoObservationDatumIngest.Init(buf, pos)
        return cls.InitFromObj(ionoObservationDatumIngest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ionoObservationDatumIngest):
        x = IonoObservationDatum_IngestT()
        x._UnPack(ionoObservationDatumIngest)
        return x

    # IonoObservationDatum_IngestT
    def _UnPack(self, ionoObservationDatumIngest):
        if ionoObservationDatumIngest is None:
            return
        self.notes = ionoObservationDatumIngest.Notes()
        if not ionoObservationDatumIngest.DataIsNone():
            self.data = []
            for i in range(ionoObservationDatumIngest.DataLength()):
                self.data.append(ionoObservationDatumIngest.Data(i))

    # IonoObservationDatum_IngestT
    def Pack(self, builder):
        if self.notes is not None:
            notes = builder.CreateString(self.notes)
        if self.data is not None:
            datalist = []
            for i in range(len(self.data)):
                datalist.append(builder.CreateString(self.data[i]))
            IonoObservationDatum_IngestStartDataVector(builder, len(self.data))
            for i in reversed(range(len(self.data))):
                builder.PrependUOffsetTRelative(datalist[i])
            data = builder.EndVector()
        IonoObservationDatum_IngestStart(builder)
        if self.notes is not None:
            IonoObservationDatum_IngestAddNotes(builder, notes)
        if self.data is not None:
            IonoObservationDatum_IngestAddData(builder, data)
        ionoObservationDatumIngest = IonoObservationDatum_IngestEnd(builder)
        return ionoObservationDatumIngest
