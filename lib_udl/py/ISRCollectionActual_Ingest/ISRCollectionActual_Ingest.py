# automatically generated by the FlatBuffers compiler, do not modify

# namespace: 

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# /// Actual start and stop for the collection.
class ISRCollectionActual_Ingest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ISRCollectionActual_Ingest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsISRCollectionActual_Ingest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ISRCollectionActual_IngestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x49\x53\x52\x43", size_prefixed=size_prefixed)

    # ISRCollectionActual_Ingest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Unique Identifier of actual collection period for historical archive.
    # Example: /// Example: ISRCOLLECTIONACTUAL-ID
    # Constraints: Minimum length = 0, Maximum length = 36
    # ISRCollectionActual_Ingest
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Start time the collection actually occurred, in ISO 8601 UTC format.
    # Example: /// Example: 2021-01-19T01:01:15.001Z
    # Constraints: No constraints specified.
    # ISRCollectionActual_Ingest
    def Start(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Stop time the collection actually occurred, in ISO 8601 UTC format.
    # Example: /// Example: 2021-01-19T01:01:15.001Z
    # Constraints: No constraints specified.
    # ISRCollectionActual_Ingest
    def Stop(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def ISRCollectionActual_IngestStart(builder):
    builder.StartObject(3)

def Start(builder):
    ISRCollectionActual_IngestStart(builder)

def ISRCollectionActual_IngestAddId(builder, id):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(id), 0)

def AddId(builder, id):
    ISRCollectionActual_IngestAddId(builder, id)

def ISRCollectionActual_IngestAddStart(builder, start):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(start), 0)

def AddStart(builder, start):
    ISRCollectionActual_IngestAddStart(builder, start)

def ISRCollectionActual_IngestAddStop(builder, stop):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(stop), 0)

def AddStop(builder, stop):
    ISRCollectionActual_IngestAddStop(builder, stop)

def ISRCollectionActual_IngestEnd(builder):
    return builder.EndObject()

def End(builder):
    return ISRCollectionActual_IngestEnd(builder)


class ISRCollectionActual_IngestT(object):

    # ISRCollectionActual_IngestT
    def __init__(self):
        self.id = None  # type: str
        self.start = None  # type: str
        self.stop = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        isrcollectionActualIngest = ISRCollectionActual_Ingest()
        isrcollectionActualIngest.Init(buf, pos)
        return cls.InitFromObj(isrcollectionActualIngest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, isrcollectionActualIngest):
        x = ISRCollectionActual_IngestT()
        x._UnPack(isrcollectionActualIngest)
        return x

    # ISRCollectionActual_IngestT
    def _UnPack(self, isrcollectionActualIngest):
        if isrcollectionActualIngest is None:
            return
        self.id = isrcollectionActualIngest.Id()
        self.start = isrcollectionActualIngest.Start()
        self.stop = isrcollectionActualIngest.Stop()

    # ISRCollectionActual_IngestT
    def Pack(self, builder):
        if self.id is not None:
            id = builder.CreateString(self.id)
        if self.start is not None:
            start = builder.CreateString(self.start)
        if self.stop is not None:
            stop = builder.CreateString(self.stop)
        ISRCollectionActual_IngestStart(builder)
        if self.id is not None:
            ISRCollectionActual_IngestAddId(builder, id)
        if self.start is not None:
            ISRCollectionActual_IngestAddStart(builder, start)
        if self.stop is not None:
            ISRCollectionActual_IngestAddStop(builder, stop)
        isrcollectionActualIngest = ISRCollectionActual_IngestEnd(builder)
        return isrcollectionActualIngest
