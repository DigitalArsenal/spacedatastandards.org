// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * /// Actual start and stop for the collection.
 */
export class ISRCollectionActual_Ingest implements flatbuffers.IUnpackableObject<ISRCollectionActual_IngestT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ISRCollectionActual_Ingest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsISRCollectionActual_Ingest(bb:flatbuffers.ByteBuffer, obj?:ISRCollectionActual_Ingest):ISRCollectionActual_Ingest {
  return (obj || new ISRCollectionActual_Ingest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsISRCollectionActual_Ingest(bb:flatbuffers.ByteBuffer, obj?:ISRCollectionActual_Ingest):ISRCollectionActual_Ingest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ISRCollectionActual_Ingest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('ISRC');
}

/**
 * Unique Identifier of actual collection period for historical archive.
 * Example: /// Example: ISRCOLLECTIONACTUAL-ID
 * Constraints: Minimum length = 0, Maximum length = 36
 */
id():string|null
id(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
id(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Start time the collection actually occurred, in ISO 8601 UTC format.
 * Example: /// Example: 2021-01-19T01:01:15.001Z
 * Constraints: No constraints specified.
 */
start():string|null
start(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
start(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Stop time the collection actually occurred, in ISO 8601 UTC format.
 * Example: /// Example: 2021-01-19T01:01:15.001Z
 * Constraints: No constraints specified.
 */
stop():string|null
stop(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
stop(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startISRCollectionActual_Ingest(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addId(builder:flatbuffers.Builder, idOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, idOffset, 0);
}

static addStart(builder:flatbuffers.Builder, startOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, startOffset, 0);
}

static addStop(builder:flatbuffers.Builder, stopOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, stopOffset, 0);
}

static endISRCollectionActual_Ingest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishISRCollectionActual_IngestBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'ISRC');
}

static finishSizePrefixedISRCollectionActual_IngestBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'ISRC', true);
}

static createISRCollectionActual_Ingest(builder:flatbuffers.Builder, idOffset:flatbuffers.Offset, startOffset:flatbuffers.Offset, stopOffset:flatbuffers.Offset):flatbuffers.Offset {
  ISRCollectionActual_Ingest.startISRCollectionActual_Ingest(builder);
  ISRCollectionActual_Ingest.addId(builder, idOffset);
  ISRCollectionActual_Ingest.addStart(builder, startOffset);
  ISRCollectionActual_Ingest.addStop(builder, stopOffset);
  return ISRCollectionActual_Ingest.endISRCollectionActual_Ingest(builder);
}

unpack(): ISRCollectionActual_IngestT {
  return new ISRCollectionActual_IngestT(
    this.id(),
    this.start(),
    this.stop()
  );
}


unpackTo(_o: ISRCollectionActual_IngestT): void {
  _o.id = this.id();
  _o.start = this.start();
  _o.stop = this.stop();
}
}

export class ISRCollectionActual_IngestT implements flatbuffers.IGeneratedObject {
constructor(
  public id: string|Uint8Array|null = null,
  public start: string|Uint8Array|null = null,
  public stop: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const id = (this.id !== null ? builder.createString(this.id!) : 0);
  const start = (this.start !== null ? builder.createString(this.start!) : 0);
  const stop = (this.stop !== null ? builder.createString(this.stop!) : 0);

  return ISRCollectionActual_Ingest.createISRCollectionActual_Ingest(builder,
    id,
    start,
    stop
  );
}
}
